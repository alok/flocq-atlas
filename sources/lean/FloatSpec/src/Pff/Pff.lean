import Std.Do.Triple
import FloatSpec.src.Core
import FloatSpec.src.Compat
import Mathlib.Data.Real.Basic
import FloatSpec.src.Calc.Operations
import FloatSpec.src.SimprocWP

-- Legacy Pff library compatibility layer
-- Translated from Coq file: flocq/src/Pff/Pff.v

open Real
open Std.Do

set_option linter.style.haveILetI false

-- Keep semantic float values abstract unless a proof explicitly unfolds them.
-- Lean 4.33 otherwise expands `F2R` before source-to-target bridge equalities fire.
attribute [-simp] FloatSpec.Core.Defs.F2R_run

-- Compatibility definitions for Pff legacy support

-- Coq: `float`
abbrev float (beta : Int) [ValidRadix beta] :=
  FloatSpec.Core.Defs.FlocqFloat beta

-- Coq: `FtoR`
noncomputable def FtoR {beta : Int} [ValidRadix beta] (x : float beta) : ℝ :=
  _root_.F2R (beta:=beta) x

-- Coq: `Fle`
def Fle {beta : Int} [ValidRadix beta] (x y : float beta) : Prop :=
  FtoR x ≤ FtoR y

-- Coq: `Fminus`
def Fminus {beta : Int} [ValidRadix beta] (x y : float beta) : float beta :=
  FloatSpec.Calc.Operations.Fminus (beta:=beta) x y

-- Even number properties
def nat_even (n : Nat) : Prop := ∃ k, n = 2 * k

def nat_odd (n : Nat) : Prop := ∃ k, n = 2 * k + 1

-- Even/Odd lemmas
theorem even_0 : nat_even 0 := ⟨0, rfl⟩

theorem odd_1 : nat_odd 1 := ⟨0, rfl⟩

theorem not_even_1 : ¬nat_even 1 := by
  intro ⟨k, hk⟩
  omega

theorem not_odd_0 : ¬nat_odd 0 := by
  intro ⟨k, hk⟩
  omega

-- Double operation
def nat_double (n : Nat) : Nat := 2 * n

-- Division by 2
def nat_div2 (n : Nat) : Nat := n / 2

-- Even/Odd characterization
theorem even_iff_double (n : Nat) : nat_even n ↔ n = nat_double (nat_div2 n) := by
  constructor
  · intro ⟨k, hk⟩
    simp only [nat_double, nat_div2]
    omega
  · intro h
    simp only [nat_double, nat_div2] at h
    exact ⟨n / 2, h⟩

theorem odd_iff_double (n : Nat) : nat_odd n ↔ n = nat_double (nat_div2 n) + 1 := by
  constructor
  · intro ⟨k, hk⟩
    simp only [nat_double, nat_div2]
    omega
  · intro h
    simp only [nat_double, nat_div2] at h
    exact ⟨n / 2, h⟩

-- Coq: `Even_Odd_double`
theorem Even_Odd_double (n : Nat) :
    (nat_even n ↔ n = nat_double (nat_div2 n)) ∧
    (nat_odd n ↔ n = Nat.succ (nat_double (nat_div2 n))) := by
  constructor
  · exact even_iff_double n
  · simpa [Nat.succ_eq_add_one] using odd_iff_double n

-- Coq: `Even_double`
theorem Even_double (n : Nat) (h : nat_even n) :
    n = nat_double (nat_div2 n) :=
  (Even_Odd_double n).1.mp h

-- Coq: `Odd_double`
theorem Odd_double (n : Nat) (h : nat_odd n) :
    n = Nat.succ (nat_double (nat_div2 n)) :=
  (Even_Odd_double n).2.mp h

-- ---------------------------------------------------------------------------
-- Missing parity lemmas over Nat (Coq compatibility)

noncomputable def Even_0_check : Unit :=
  ()

/-- Coq: `Even_0` — 0 is even. -/
theorem Even_0 :
    ⦃⌜True⌝⦄
    (pure Even_0_check : Id Unit)
    ⦃⇓_ => ⌜nat_even 0⌝⦄ := by
  intro _
  simp [wp, PostCond.noThrow, pure, Even_0_check]
  exact even_0

noncomputable def Even_1_check : Unit :=
  ()

/-- Coq: `Even_1` — 1 is not even. -/
theorem Even_1 :
    ⦃⌜True⌝⦄
    (pure Even_1_check : Id Unit)
    ⦃⇓_ => ⌜¬ nat_even 1⌝⦄ := by
  intro _
  simp [wp, PostCond.noThrow, pure, Even_1_check]
  exact not_even_1

noncomputable def Odd_0_check : Unit :=
  ()

/-- Coq: `Odd_0` — 0 is not odd. -/
theorem Odd_0 :
    ⦃⌜True⌝⦄
    (pure Odd_0_check : Id Unit)
    ⦃⇓_ => ⌜¬ nat_odd 0⌝⦄ := by
  intro _
  simp [wp, PostCond.noThrow, pure, Odd_0_check]
  exact not_odd_0

noncomputable def Odd_1_check : Unit :=
  ()

/-- Coq: `Odd_1` — 1 is odd. -/
theorem Odd_1 :
    ⦃⌜True⌝⦄
    (pure Odd_1_check : Id Unit)
    ⦃⇓_ => ⌜nat_odd 1⌝⦄ := by
  intro _
  simp [wp, PostCond.noThrow, pure, Odd_1_check]
  exact odd_1

-- Legacy tactical support (simplified)
section LegacyTactics

-- Case analysis preserving equality
def case_eq {α β : Type*} (x : α) (f : α → β) : β := f x

-- Simple automation for arithmetic
theorem arith_helper (a b c : Int) : a + b = c → a = c - b := by
  intro h
  linarith

end LegacyTactics

-- Power operations compatibility
theorem pow_inv (r : ℝ) (n : Nat) (h : r ≠ 0) :
  (r^n)⁻¹ = r⁻¹^n := by
  exact (inv_pow r n).symm

theorem pow_neg (r : ℝ) (z : Int) :
  r^(-z) = (r^z)⁻¹ := by
  exact zpow_neg r z

-- Real number compatibility
theorem abs_inv_compat (r : ℝ) : |r⁻¹| = |r|⁻¹ := by
  exact abs_inv r

-- Coq: `Rinv_mult_distr`
theorem Rinv_mult_distr (x y : ℝ) :
    (x * y)⁻¹ = x⁻¹ * y⁻¹ := by
  simpa [mul_comm] using (mul_inv_rev x y)

-- Coq: `Rabs_Rinv`
theorem Rabs_Rinv (x : ℝ) :
    |x⁻¹| = |x|⁻¹ := by
  exact abs_inv x

-- Coq: `Rinv_pow`
theorem Rinv_pow (x : ℝ) (n : Nat) :
    (x ^ n)⁻¹ = x⁻¹ ^ n := by
  exact (inv_pow x n).symm

-- Coq: `Rinv_involutive`
theorem Rinv_involutive (x : ℝ) :
    (x⁻¹)⁻¹ = x := by
  exact inv_inv x

-- Coq: `Rlt_Rminus`
theorem Rlt_Rminus (x y : ℝ) :
    x < y → 0 < y - x := by
  intro h
  linarith

-- Coq: `IZR_neq`
theorem IZR_neq (m n : Int) :
    m ≠ n → (m : ℝ) ≠ (n : ℝ) := by
  intro h hcast
  exact h (Int.cast_injective hcast)

-- Coq: `powerRZ_inv` — integer powers commute with inversion.
theorem powerRZ_inv (r : ℝ) (z : Int) (_hr : r ≠ 0) :
    r⁻¹ ^ z = (r ^ z)⁻¹ := by
  exact inv_zpow r z

-- Coq compat: `powerRZ_neg` — r^(-z) = (r^z)⁻¹
noncomputable def powerRZ_neg_check (r : ℝ) (z : Int) : Unit :=
  ()

theorem powerRZ_neg (r : ℝ) (z : Int) :
    ⦃⌜True⌝⦄
    (pure (powerRZ_neg_check r z) : Id Unit)
    ⦃⇓_ => ⌜r ^ (-z) = (r ^ z)⁻¹⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, powerRZ_neg_check]
  exact zpow_neg r z

-- (reserved for future compatibility lemmas)

-- ---------------------------------------------------------------------------
-- Integer rounding down by 1 (IRNDD) and basic properties (Coq alignment)

-- Coq: `IRNDD (r) = Z.pred (up r)`; predecessor of ceiling function
-- In Coq, `up r` is the smallest integer strictly greater than r.
-- This equals floor(r) in all cases:
--   - When r is not integer: up r = ceil r, so pred(up r) = ceil r - 1 = floor r
--   - When r IS integer: up r = r + 1, so pred(up r) = r = floor r
noncomputable def IRNDD (r : ℝ) : Int := Int.floor r

noncomputable def IRNDD_correct1_check (r : ℝ) : Unit :=
  ()

/-- Coq: `IRNDD_correct1` — IRNDD r ≤ r. -/
theorem IRNDD_correct1 (r : ℝ) :
    ⦃⌜True⌝⦄
    (pure (IRNDD_correct1_check r) : Id Unit)
    ⦃⇓_ => ⌜(IRNDD r : ℝ) ≤ r⌝⦄ := by
  intro _
  simp [wp, PostCond.noThrow, pure, IRNDD_correct1_check, IRNDD]
  -- Goal: (Int.floor r : ℝ) ≤ r
  exact Int.floor_le r

noncomputable def IRNDD_correct2_check (r : ℝ) : Unit :=
  ()

/-- Coq: `IRNDD_correct2` — r < succ (IRNDD r). -/
theorem IRNDD_correct2 (r : ℝ) :
    ⦃⌜True⌝⦄
    (pure (IRNDD_correct2_check r) : Id Unit)
    ⦃⇓_ => ⌜r < ((Int.succ (IRNDD r)) : ℝ)⌝⦄ := by
  intro _
  simp [wp, PostCond.noThrow, pure, IRNDD_correct2_check, IRNDD, Int.succ]

noncomputable def IRNDD_correct3_check (r : ℝ) : Unit :=
  ()

/-- Coq: `IRNDD_correct3` — r - 1 < IRNDD r. -/
theorem IRNDD_correct3 (r : ℝ) :
    ⦃⌜True⌝⦄
    (pure (IRNDD_correct3_check r) : Id Unit)
    ⦃⇓_ => ⌜r - 1 < (IRNDD r : ℝ)⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, IRNDD_correct3_check, IRNDD]
  -- Goal: r - 1 < ↑⌊r⌋
  exact Int.sub_one_lt_floor r

noncomputable def IRNDD_pos_check (r : ℝ) : Unit :=
  ()

/-- Coq: `IRNDD_pos` — 0 ≤ r → 0 ≤ IRNDD r. -/
theorem IRNDD_pos (r : ℝ) :
    ⦃⌜0 ≤ r⌝⦄
    (pure (IRNDD_pos_check r) : Id Unit)
    ⦃⇓_ => ⌜0 ≤ IRNDD r⌝⦄ := by
  intro hr
  simp [wp, PostCond.noThrow, pure, IRNDD_pos_check, IRNDD]
  exact Int.floor_nonneg.mpr hr

noncomputable def IRNDD_eq_check (r : ℝ) (z : Int) : Unit :=
  ()

/-- Coq: `IRNDD_eq` — if z ≤ r < succ z then IRNDD r = z. -/
theorem IRNDD_eq (r : ℝ) (z : Int) :
    ⦃⌜(z : ℝ) ≤ r ∧ r < ((Int.succ z) : ℝ)⌝⦄
    (pure (IRNDD_eq_check r z) : Id Unit)
    ⦃⇓_ => ⌜IRNDD r = z⌝⦄ := by
  intro ⟨hz_le, hz_lt⟩
  simp only [wp, PostCond.noThrow, pure, IRNDD_eq_check, IRNDD]
  -- Goal: Int.floor r = z
  -- Use Int.floor_eq_iff: ⌊a⌋ = z ↔ z ≤ a ∧ a < z + 1
  rw [Int.floor_eq_iff]
  constructor
  · exact hz_le
  · simp only [Int.succ, Int.cast_add, Int.cast_one] at hz_lt
    exact hz_lt

noncomputable def IRNDD_projector_check (z : Int) : Unit :=
  ()

/-- Coq: `IRNDD_projector` — IRNDD z = z for integer inputs. -/
theorem IRNDD_projector (z : Int) :
    ⦃⌜True⌝⦄
    (pure (IRNDD_projector_check z) : Id Unit)
    ⦃⇓_ => ⌜IRNDD (z : ℝ) = z⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, IRNDD_projector_check, IRNDD]
  -- Goal: Int.floor (z : ℝ) = z
  exact Int.floor_intCast z

-- ---------------------------------------------------------------------------
-- Integer parity lemmas (aligned with Coq: Odd/Even over Z)

namespace FloatSpec.PffCompat

/-- Coq: `Odd`.

Direct compatibility definition for Flocq's integer oddness predicate.  Existing
proofs continue to use Mathlib's root `Odd`; this namespaced declaration keeps
the exact Flocq name available without a root-name collision. -/
def Odd (z : Int) : Prop :=
  ∃ z1 : Int, z = 2 * z1 + 1

/-- Coq: `Option`.

Flocq carries a local copy of the usual option type in `Pff.v`.  Lean already
has root `Option`, so the exact compatibility name lives under the Pff
compatibility namespace. -/
inductive Option (A : Type u) : Type u where
  | Some (x : A) : Option A
  | None : Option A

end FloatSpec.PffCompat

-- ---------------------------------------------------------------------------
-- Log/exponential auxiliary lemmas from Coq Pff.v

noncomputable def ln_radix_pos_check (radix : ℝ) : Unit :=
  ()

/-- Coq: `ln_radix_pos` — 0 < ln radix. Requires radix > 1. -/
theorem ln_radix_pos (radix : ℝ) :
    ⦃⌜1 < radix⌝⦄
    (pure (ln_radix_pos_check radix) : Id Unit)
    ⦃⇓_ => ⌜0 < Real.log radix⌝⦄ := by
  intro hradix
  simp only [wp, PostCond.noThrow, pure, ln_radix_pos_check]
  -- Goal: 0 < Real.log radix
  -- Since radix > 1, log radix > log 1 = 0
  exact Real.log_pos hradix

-- Coq: `exp_ln_powerRZ` — exp (ln u * v) = u^v for integer u>0, v:Z
noncomputable def exp_ln_powerRZ_check (u v : Int) : Unit :=
  ()

theorem exp_ln_powerRZ (u v : Int) :
    ⦃⌜0 < u⌝⦄
    (pure (exp_ln_powerRZ_check u v) : Id Unit)
    ⦃⇓_ => ⌜Real.exp (Real.log (u : ℝ) * (v : ℝ)) = (u : ℝ) ^ v⌝⦄ := by
  intro hu
  simp only [wp, PostCond.noThrow, pure, exp_ln_powerRZ_check]
  -- Goal: Real.exp (Real.log (u : ℝ) * (v : ℝ)) = (u : ℝ) ^ v
  -- Use Real.rpow_def_of_pos: for 0 < x, x ^ y = exp(log x * y)
  -- Then use Real.rpow_intCast: x ^ (n : ℝ) = x ^ n
  have hu_pos : (0 : ℝ) < (u : ℝ) := by exact Int.cast_pos.mpr hu
  rw [← Real.rpow_intCast]
  rw [Real.rpow_def_of_pos hu_pos]
  ring_nf
  trivial

-- Coq: `exp_le_inv` — if exp x ≤ exp y then x ≤ y
noncomputable def exp_le_inv_check (x y : ℝ) : Unit :=
  ()

theorem exp_le_inv (x y : ℝ) :
    ⦃⌜Real.exp x ≤ Real.exp y⌝⦄
    (pure (exp_le_inv_check x y) : Id Unit)
    ⦃⇓_ => ⌜x ≤ y⌝⦄ := by
  intro h
  simp only [wp, PostCond.noThrow, pure, exp_le_inv_check]
  rw [Real.exp_le_exp] at h
  exact h

-- Coq: `exp_monotone` — if x ≤ y then exp x ≤ exp y
noncomputable def exp_monotone_check (x y : ℝ) : Unit :=
  ()

theorem exp_monotone (x y : ℝ) :
    ⦃⌜x ≤ y⌝⦄
    (pure (exp_monotone_check x y) : Id Unit)
    ⦃⇓_ => ⌜Real.exp x ≤ Real.exp y⌝⦄ := by
  intro h
  simp only [wp, PostCond.noThrow, pure, exp_monotone_check]
  rw [Real.exp_le_exp]
  exact h

-- Coq: `OddSEven` — if n is odd then succ n is even
noncomputable def OddSEven_check (n : Int) : Unit :=
  ()

theorem OddSEven (n : Int) :
    ⦃⌜Odd n⌝⦄
    (pure (OddSEven_check n) : Id Unit)
    ⦃⇓_ => ⌜Even (Int.succ n)⌝⦄ := by
  intro h
  simp only [wp, PostCond.noThrow, pure, OddSEven_check, Int.succ]
  exact Odd.add_one h

-- Coq: `EvenSOdd` — if n is even then succ n is odd
noncomputable def EvenSOdd_check (n : Int) : Unit :=
  ()

theorem EvenSOdd (n : Int) :
    ⦃⌜Even n⌝⦄
    (pure (EvenSOdd_check n) : Id Unit)
    ⦃⇓_ => ⌜Odd (Int.succ n)⌝⦄ := by
  intro h
  simp only [wp, PostCond.noThrow, pure, EvenSOdd_check, Int.succ]
  exact Even.add_one h

-- Coq: `OddSEvenInv` — if succ n is odd then n is even
noncomputable def OddSEvenInv_check (n : Int) : Unit :=
  ()

theorem OddSEvenInv (n : Int) :
    ⦃⌜Odd (Int.succ n)⌝⦄
    (pure (OddSEvenInv_check n) : Id Unit)
    ⦃⇓_ => ⌜Even n⌝⦄ := by
  intro h
  simp only [wp, PostCond.noThrow, pure, OddSEvenInv_check, Int.succ]
  have h2 : ¬Even (n + 1) := Int.not_even_iff_odd.mpr h
  have h3 : ¬¬Even n := mt Int.even_add_one.mpr h2
  exact not_not.mp h3

-- Coq: `EvenSOddInv` — if succ n is even then n is odd
noncomputable def EvenSOddInv_check (n : Int) : Unit :=
  ()

theorem EvenSOddInv (n : Int) :
    ⦃⌜Even (Int.succ n)⌝⦄
    (pure (EvenSOddInv_check n) : Id Unit)
    ⦃⇓_ => ⌜Odd n⌝⦄ := by
  intro h
  simp only [wp, PostCond.noThrow, pure, EvenSOddInv_check, Int.succ]
  have h2 : ¬Even n := Int.even_add_one.mp h
  exact Int.not_even_iff_odd.mp h2



-- Coq: `Odd1` — one is odd
noncomputable def Odd1_check : Unit :=
  ()

theorem Odd1 :
    ⦃⌜True⌝⦄
    (pure Odd1_check : Id Unit)
    ⦃⇓_ => ⌜Odd (1 : Int)⌝⦄ := by
  intro _; simp [wp, PostCond.noThrow, pure, Odd1_check]

-- Coq: `EvenO` — zero is even (integer parity)
noncomputable def EvenO_check : Unit :=
  ()

theorem EvenO :
    ⦃⌜True⌝⦄
    (pure (EvenO_check) : Id Unit)
  ⦃⇓_ => ⌜Even (0 : Int)⌝⦄ := by
  intro _; simp [wp, PostCond.noThrow, pure, EvenO_check]

-- Coq: `OddOpp` — odd is preserved by integer negation
noncomputable def OddOpp_check (z : Int) : Unit :=
  ()

theorem OddOpp (z : Int) :
    ⦃⌜Odd z⌝⦄
    (pure (OddOpp_check z) : Id Unit)
    ⦃⇓_ => ⌜Odd (-z)⌝⦄ := by
  intro h; simp only [wp, PostCond.noThrow, pure, OddOpp_check]; exact Odd.neg h

-- Coq: `EvenOpp` — even is preserved by integer negation
noncomputable def EvenOpp_check (z : Int) : Unit :=
  ()

theorem EvenOpp (z : Int) :
    ⦃⌜Even z⌝⦄
    (pure (EvenOpp_check z) : Id Unit)
    ⦃⇓_ => ⌜Even (-z)⌝⦄ := by
  intro h; simp only [wp, PostCond.noThrow, pure, EvenOpp_check]; exact Even.neg h

-- Coq: `OddEvenDec` — for any integer, it is either odd or even
noncomputable def OddEvenDec_check (n : Int) : Unit :=
  ()

theorem OddEvenDec (n : Int) :
    ⦃⌜True⌝⦄
    (pure (OddEvenDec_check n) : Id Unit)
    ⦃⇓_ => ⌜Odd n ∨ Even n⌝⦄ := by
  intro _; simp only [wp, PostCond.noThrow, pure, OddEvenDec_check]
  exact (Int.even_or_odd n).symm

-- Coq: `OddNEven` — odd numbers are not even
noncomputable def OddNEven_check (n : Int) : Unit :=
  ()

theorem OddNEven (n : Int) :
    ⦃⌜Odd n⌝⦄
    (pure (OddNEven_check n) : Id Unit)
    ⦃⇓_ => ⌜¬ Even n⌝⦄ := by
  intro h; simp only [wp, PostCond.noThrow, pure, OddNEven_check]; exact Int.not_even_iff_odd.mpr h

-- Coq: `EvenNOdd` — even numbers are not odd
noncomputable def EvenNOdd_check (n : Int) : Unit :=
  ()

theorem EvenNOdd (n : Int) :
    ⦃⌜Even n⌝⦄
    (pure (EvenNOdd_check n) : Id Unit)
    ⦃⇓_ => ⌜¬ Odd n⌝⦄ := by
  intro h; simp only [wp, PostCond.noThrow, pure, EvenNOdd_check]; exact Int.not_odd_iff_even.mpr h

-- Coq: `EvenPlus1` — if n and m are even then n + m is even
noncomputable def EvenPlus1_check (n m : Int) : Unit :=
  ()

theorem EvenPlus1 (n m : Int) :
    ⦃⌜Even n ∧ Even m⌝⦄
    (pure (EvenPlus1_check n m) : Id Unit)
    ⦃⇓_ => ⌜Even (n + m)⌝⦄ := by
  intro ⟨hn, hm⟩
  simp only [wp, PostCond.noThrow, pure, EvenPlus1_check]
  exact Even.add hn hm

-- Coq: `OddPlus2` — if n is even and m is odd then n + m is odd
noncomputable def OddPlus2_check (n m : Int) : Unit :=
  ()

theorem OddPlus2 (n m : Int) :
    ⦃⌜Even n ∧ Odd m⌝⦄
    (pure (OddPlus2_check n m) : Id Unit)
    ⦃⇓_ => ⌜Odd (n + m)⌝⦄ := by
  intro ⟨hn, hm⟩
  simp only [wp, PostCond.noThrow, pure, OddPlus2_check]
  exact Even.add_odd hn hm

-- Coq: `EvenMult1` — if n is even then n*m is even
noncomputable def EvenMult1_check (n m : Int) : Unit :=
  ()

theorem EvenMult1 (n m : Int) :
    ⦃⌜Even n⌝⦄
    (pure (EvenMult1_check n m) : Id Unit)
    ⦃⇓_ => ⌜Even (n * m)⌝⦄ := by
  intro hn; simp only [wp, PostCond.noThrow, pure, EvenMult1_check]; exact hn.mul_right m

-- Coq: `EvenMult2` — if m is even then n*m is even
noncomputable def EvenMult2_check (n m : Int) : Unit :=
  ()

theorem EvenMult2 (n m : Int) :
    ⦃⌜Even m⌝⦄
    (pure (EvenMult2_check n m) : Id Unit)
    ⦃⇓_ => ⌜Even (n * m)⌝⦄ := by
  intro hm; simp only [wp, PostCond.noThrow, pure, EvenMult2_check]; exact hm.mul_left n

-- Coq: `OddMult` — if n and m are odd then n*m is odd
noncomputable def OddMult_check (n m : Int) : Unit :=
  ()

theorem OddMult (n m : Int) :
    ⦃⌜Odd n ∧ Odd m⌝⦄
    (pure (OddMult_check n m) : Id Unit)
    ⦃⇓_ => ⌜Odd (n * m)⌝⦄ := by
  intro ⟨hn, hm⟩; simp only [wp, PostCond.noThrow, pure, OddMult_check]; exact hn.mul hm

-- Coq: `EvenMultInv` — if n*m is even and n is odd then m is even
noncomputable def EvenMultInv_check (n m : Int) : Unit :=
  ()

theorem EvenMultInv (n m : Int) :
    ⦃⌜Even (n * m) ∧ Odd n⌝⦄
    (pure (EvenMultInv_check n m) : Id Unit)
    ⦃⇓_ => ⌜Even m⌝⦄ := by
  intro ⟨hev, hodd⟩
  simp only [wp, PostCond.noThrow, pure, EvenMultInv_check]
  exact (Int.even_mul.mp hev).resolve_left (Int.not_even_iff_odd.mpr hodd)

-- Integer power on Int for natural exponent (compat with Coq Zpower_nat)
def Zpower_nat (n : Int) (k : Nat) : Int := n ^ k

def Zpower_nat_int (n : Int) (k : Nat) : Int := Zpower_nat n k

-- Coq: `EvenExp` — if n is even then n^(S m) is even (nat exponent)
noncomputable def EvenExp_check (n : Int) (m : Nat) : Unit :=
  ()

theorem EvenExp (n : Int) (m : Nat) :
    ⦃⌜Even n⌝⦄
    (pure (EvenExp_check n m) : Id Unit)
    ⦃⇓_ => ⌜Even (Zpower_nat_int n (Nat.succ m))⌝⦄ := by
  intro hev
  simp only [wp, PostCond.noThrow, pure, EvenExp_check, Zpower_nat_int]
  exact hev.pow_of_ne_zero (Nat.succ_ne_zero m)

-- Coq: `OddExp` — if n is odd then n^m is odd (nat exponent)
noncomputable def OddExp_check (n : Int) (m : Nat) : Unit :=
  ()

theorem OddExp (n : Int) (m : Nat) :
    ⦃⌜Odd n⌝⦄
    (pure (OddExp_check n m) : Id Unit)
    ⦃⇓_ => ⌜Odd (Zpower_nat_int n m)⌝⦄ := by
  intro hodd
  simp [wp, PostCond.noThrow, pure, OddExp_check, Zpower_nat_int, ULift.down_up] at hodd ⊢
  exact hodd.pow

-- Float-level parity wrappers and lemmas (Lean skeletons mirroring Coq)
def Feven {beta : Int} [ValidRadix beta]
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Prop :=
  Even p.Fnum

def Fodd {beta : Int} [ValidRadix beta]
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Prop :=
  Odd p.Fnum

noncomputable def FevenOrFodd_check {beta : Int} [ValidRadix beta]
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem FevenOrFodd {beta : Int} [ValidRadix beta]
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜True⌝⦄
    (pure (FevenOrFodd_check (beta:=beta) p) : Id Unit)
    ⦃⇓_ => ⌜Feven (beta:=beta) p ∨ Fodd (beta:=beta) p⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, FevenOrFodd_check, Feven, Fodd]
  exact Int.even_or_odd p.Fnum

-- ---------------------------------------------------------------------------
-- Legacy polymorphic predicate helpers.  These are intentionally named
-- `*Generic`; the source-facing Coq FRound predicates are defined below once
-- concrete floats, boundedness, and min/max semantics are in scope.

-- Totality of a rounding relation P
def TotalP {α : Type} (P : ℝ → α → Prop) : Prop :=
  ∀ r : ℝ, ∃ p : α, P r p

-- Compatibility of P under equal real value and representation equality
def CompatiblePGeneric {α : Type} (P : ℝ → α → Prop) : Prop :=
  ∀ r1 r2 : ℝ, ∀ p q : α, P r1 p → r1 = r2 → p = q → P r2 q

-- Monotonicity: if p < q (reals) and P p p', P q q' then F2R p' ≤ F2R q'.
-- For a generic type α, we require a coercion to ℝ.
-- Coq definition: ∀ (p q : R) (p' q' : float), p < q → P p p' → P q q' → p' ≤ q'
-- where p' ≤ q' is interpreted as FtoR radix p' ≤ FtoR radix q'.
-- For genericity, we parameterize over a function toReal : α → ℝ.
def MonotoneP_with {α : Type} (toReal : α → ℝ) (P : ℝ → α → Prop) : Prop :=
  ∀ (p q : ℝ) (p' q' : α), p < q → P p p' → P q q' → toReal p' ≤ toReal q'

-- For FlocqFloat, the natural toReal is F2R
def MonotoneP_float {beta : Int} [ValidRadix beta] (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop) : Prop :=
  MonotoneP_with (_root_.F2R (beta := beta)) P

-- Generic fallback: without a float interpretation for `α`, monotonicity can
-- only be expressed as representation uniqueness across increasing inputs.
-- Flocq-aligned float developments should use `MonotoneP_float`.
def MonotonePGeneric {α : Type} (P : ℝ → α → Prop) : Prop :=
  ∀ (p q : ℝ) (p' q' : α), p < q → P p p' → P q q' → p' = q'

-- Generic fallback for the Flocq min/max disjunction.  The faithful
-- float-specific content is carried by `isMin`/`isMax`; this generic form
-- requires uniqueness at each rounded real instead of silently returning `True`.
def MinOrMaxPGeneric {α : Type} (P : ℝ → α → Prop) : Prop :=
  ∀ (r : ℝ) (p q : α), P r p → P r q → p = q

-- Rounded-mode package
def RoundedModePGeneric {α : Type} (P : ℝ → α → Prop) : Prop :=
  TotalP P ∧ CompatiblePGeneric P ∧ MinOrMaxPGeneric P ∧ MonotonePGeneric P

-- Representation-level uniqueness retained only for non-source helper proofs.
def UniquePGeneric {α : Type} (P : ℝ → α → Prop) : Prop :=
  ∀ r p q, P r p → P r q → p = q

/-- Coq `UniqueP`: two outputs selected for the same real input denote the
same real number.  FLoCq deliberately does not require equality of float
records, because distinct mantissa/exponent pairs may represent the same real. -/
def UniqueP {beta : Int} [ValidRadix beta]
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop) : Prop :=
  ∀ r p q, P r p → P r q →
    _root_.F2R (beta := beta) p = _root_.F2R (beta := beta) q

-- Generic projector fallback.  Coq's `ProjectorPGeneric` is float-specific and uses
-- bounded floats plus real-value equality; in this generic shell we require
-- uniqueness of outputs at the same real input.
def ProjectorPGeneric {α : Type} (P : ℝ → α → Prop) : Prop :=
  ∀ (r : ℝ) (p q : α), P r p → P r q → p = q

-- ---------------------------------------------------------------------------
-- Source-faithful bound carrier shared by every Pff module.  Coq stores a
-- `positive` mantissa bound and an `N` exponent bound.  We retain the
-- arithmetic-friendly `Int` projections but carry the two refinements in the
-- value, so negative exponents and non-positive mantissa bounds are no longer
-- representable.
structure Fbound where
  dExp : Int := 0
  vNum : Int := 1
  dExp_nonneg : 0 ≤ dExp := by omega
  vNum_pos : 0 < vNum := by omega

/-- Temporary source-compatible spelling used by existing Pff proofs.  This
    is a definitional alias, not a second bound representation. -/
abbrev Fbound_skel := Fbound

/-- Coq Veltkamp helper `plusExp`.

It keeps the same mantissa bound and raises `dExp` by the integer value of
Coq's `Npos (P_of_succ_nat (pred (pred precision)))`, namely
`max 1 (precision - 1)`. -/
def plusExp (b : Fbound_skel) (precision : Nat) : Fbound_skel :=
  { dExp := b.dExp + ((max 1 (precision - 1) : Nat) : Int)
    vNum := b.vNum
    dExp_nonneg := by exact add_nonneg b.dExp_nonneg (Int.natCast_nonneg _)
    vNum_pos := b.vNum_pos }

/-- Coq Veltkamp reduced bound `b'`.

The reduced Veltkamp bound keeps the original exponent field and uses
precision `t - s` for the mantissa bound. -/
noncomputable def Veltkamp_reducedBound (beta : Int) [ValidRadix beta] (b : Fbound_skel) (s t : Nat)
    (hβ : 1 < beta := by omega) :
    Fbound_skel :=
  { dExp := b.dExp
    vNum := Zpower_nat beta (t - s)
    dExp_nonneg := b.dExp_nonneg
    vNum_pos := by
      unfold Zpower_nat
      exact pow_pos (lt_trans Int.zero_lt_one hβ) _ }

/-- Coq: `p'GivesBound`, the mantissa-bound equation for `b'`. -/
theorem p'GivesBound (beta : Int) [ValidRadix beta] (b : Fbound_skel) (s t : Nat)
    (hβ : 1 < beta := by omega) :
    (Veltkamp_reducedBound beta b s t).vNum = Zpower_nat beta (t - s) := by
  rfl

/-- Coq Dekker local bound `bt`.

The Dekker split bound keeps the original exponent field and uses precision
`s` for the mantissa bound. -/
noncomputable def Veltkamp_splitBound (beta : Int) [ValidRadix beta] (b : Fbound_skel) (s : Nat)
    (hβ : 1 < beta := by omega) :
    Fbound_skel :=
  { dExp := b.dExp
    vNum := Zpower_nat beta s
    dExp_nonneg := b.dExp_nonneg
    vNum_pos := by
      unfold Zpower_nat
      exact pow_pos (lt_trans Int.zero_lt_one hβ) _ }

/-- Coq: `p''GivesBound`, the mantissa-bound equation for `bt`. -/
theorem p''GivesBound (beta : Int) [ValidRadix beta] (b : Fbound_skel) (s : Nat)
    (hβ : 1 < beta := by omega) :
    (Veltkamp_splitBound beta b s).vNum = Zpower_nat beta s := by
  rfl

/-- Coq Dekker `b'` from section `Algo2`.

The Coq bound keeps `vNum b` and sets
`dExp b' = N.double (dExp b) + N.double (Npos (P_of_succ_nat t))`,
which coerces to `2 * dExp b + 2 * t + 2` over integers. -/
def Dekker_extendedBound (b : Fbound_skel) (t : Nat) : Fbound_skel :=
  { dExp := 2 * b.dExp + 2 * (t : Int) + 2
    vNum := b.vNum
    dExp_nonneg := by
      have hb := b.dExp_nonneg
      have ht : 0 ≤ (t : Int) := Int.natCast_nonneg t
      omega
    vNum_pos := b.vNum_pos }

/-- Coq: `dExpPrim`, the local exponent of Dekker's `b'` strictly extends `b`.

In Coq this follows from `dExp b : N`; the payload exposes the corresponding
integer nonnegativity fact used by the proof. -/
theorem dExpPrim_from_nonnegative_payload
    (b : Fbound_skel) (t : Nat) (hdExp : 0 ≤ b.dExp) :
    b.dExp < (Dekker_extendedBound b t).dExp := by
  have ht : 0 ≤ (t : Int) := Int.natCast_nonneg t
  dsimp [Dekker_extendedBound]
  omega

/-- Coq: `dExpPrim`.  Nonnegativity is part of the translated `Fbound`. -/
theorem dExpPrim (b : Fbound_skel) (t : Nat) (_hpGe : 4 ≤ t) :
    b.dExp < (Dekker_extendedBound b t).dExp :=
  dExpPrim_from_nonnegative_payload b t b.dExp_nonneg

/-- Coq: `dExpPrimEq`, the integer expansion of Dekker's local `b'` exponent. -/
theorem dExpPrimEq (b : Fbound_skel) (t : Nat) :
    (Dekker_extendedBound b t).dExp = 2 * b.dExp + 2 * (t : Int) + 2 := by
  rfl

-- Coq: Fbounded b d := (Z.abs (Fnum d) < Zpos (vNum b))%Z /\ (- dExp b <= Fexp d)%Z
def Fbounded {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel)
    (f : FloatSpec.Core.Defs.FlocqFloat beta) : Prop :=
  |f.Fnum| < bo.vNum ∧ -bo.dExp ≤ f.Fexp

-- Backward-compatible alias kept for proofs that were already moved to the
-- faithful boundedness predicate.
def Fbounded' {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel)
    (f : FloatSpec.Core.Defs.FlocqFloat beta) : Prop :=
  Fbounded bo f

def isMinGeneric {α : Type} (b : Fbound_skel) (radix : Int) : ℝ → α → Prop :=
  fun _ x => ∀ y : α, y = x

def isMaxGeneric {α : Type} (b : Fbound_skel) (radix : Int) : ℝ → α → Prop :=
  fun _ x => ∀ y : α, y = x

-- Proper Coq-matching isMin predicate for floats
-- Coq: isMin r min := Fbounded b min ∧ (min ≤ r) ∧ (∀ f, Fbounded b f → (f ≤ r) → (f ≤ min))
def isMin {beta : Int} [ValidRadix beta] (b : Fbound_skel) (radix : Int) :
    ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop :=
  fun r min => Fbounded' b min ∧
    (_root_.F2R min ≤ r) ∧
    (∀ f : FloatSpec.Core.Defs.FlocqFloat beta, Fbounded' b f → _root_.F2R f ≤ r → _root_.F2R f ≤ _root_.F2R min)

-- Proper Coq-matching isMax predicate for floats
-- Coq: isMax r max := Fbounded b max ∧ (r ≤ max) ∧ (∀ f, Fbounded b f → (r ≤ f) → (max ≤ f))
def isMax {beta : Int} [ValidRadix beta] (b : Fbound_skel) (radix : Int) :
    ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop :=
  fun r max => Fbounded' b max ∧
    (r ≤ _root_.F2R max) ∧
    (∀ f : FloatSpec.Core.Defs.FlocqFloat beta, Fbounded' b f → r ≤ _root_.F2R f → _root_.F2R max ≤ _root_.F2R f)

-- ---------------------------------------------------------------------------
-- Source-facing FRound predicates.  These definitions deliberately live after
-- `Fbounded`, `isMin`, and `isMax`: unlike the earlier polymorphic scaffolds,
-- every argument and equality below has the concrete semantics of Pff.v.

/-- Coq `CompatibleP`: transport a rounding witness across equality of the
    real input and equality of the represented output, provided the replacement
    representation is bounded. -/
def CompatibleP {beta : Int} [ValidRadix beta] (b : Fbound_skel)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop) : Prop :=
  ∀ (r1 r2 : ℝ) (p q : FloatSpec.Core.Defs.FlocqFloat beta),
    P r1 p → r1 = r2 → _root_.F2R p = _root_.F2R q →
      Fbounded b q → P r2 q

/-- Coq `MonotoneP`: ordered real inputs produce outputs ordered by their
    represented real values. -/
def MonotoneP {beta : Int} [ValidRadix beta]
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop) : Prop :=
  ∀ (p q : ℝ) (p' q' : FloatSpec.Core.Defs.FlocqFloat beta),
    p < q → P p p' → P q q' → _root_.F2R p' ≤ _root_.F2R q'

/-- Coq `MinOrMaxP`: every output selected by `P` is one of the two adjacent
    bounded extremal floats around its real input.  The float index `beta` is
    the source's sole radix; there is deliberately no second radix argument. -/
def MinOrMaxP {beta : Int} [ValidRadix beta] (b : Fbound_skel)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop) : Prop :=
  ∀ (r : ℝ) (p : FloatSpec.Core.Defs.FlocqFloat beta), P r p →
    isMin (beta := beta) b beta r p ∨ isMax (beta := beta) b beta r p

/-- Coq `RoundedModeP`, with the same four components and ordering. -/
def RoundedModeP {beta : Int} [ValidRadix beta] (b : Fbound_skel)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop) : Prop :=
  TotalP P ∧ CompatibleP b P ∧ MinOrMaxP b P ∧ MonotoneP P

/-- Coq `ProjectorP`: rounding a bounded float's real value produces a
    representation of that same real value. -/
def ProjectorP {beta : Int} [ValidRadix beta] (b : Fbound_skel)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop) : Prop :=
  ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
    Fbounded b p → P (_root_.F2R p) q → _root_.F2R p = _root_.F2R q

-- Coq: `MinOrMax z f := isMin b radix z f \/ isMax b radix z f`.
def MinOrMax {beta : Int} [ValidRadix beta] (b : Fbound_skel) (radix : Int) :
    ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop :=
  fun z f => isMin (beta:=beta) b radix z f ∨
    isMax (beta:=beta) b radix z f

-- Float-specific Coq-matching min/max disjunction.
-- Compatibility alias for the older API; its explicit radix is legacy only.
def MinOrMaxP_float {beta : Int} [ValidRadix beta] (b : Fbound_skel) (radix : Int)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop) : Prop :=
  MinOrMaxP b P

-- ---------------------------------------------------------------------------
-- Float-specific rounding properties (matching Coq Pff semantics)

-- Projector property for floats: bounded floats round to themselves
def ProjectorP_float {beta : Int} [ValidRadix beta] (b : Fbound_skel)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop) : Prop :=
  ∀ p : FloatSpec.Core.Defs.FlocqFloat beta,
    Fbounded b p → P (_root_.F2R p) p

-- Projector equality: if P (F2R p) q for bounded p, then F2R p = F2R q
def ProjectorEqP_float {beta : Int} [ValidRadix beta] (b : Fbound_skel)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop) : Prop :=
  ProjectorP b P

-- Full rounded-mode package for floats with all required properties
def RoundedModeP_full {beta : Int} [ValidRadix beta] (b : Fbound_skel)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop) : Prop :=
  TotalP P ∧ CompatiblePGeneric P ∧ MonotoneP_float P ∧
  ProjectorP_float b P ∧ ProjectorEqP_float b P

-- Legacy package specialized to concrete float predicates.
-- The generic helpers above are representation-based; Flocq-facing
-- theorems use this package when the upstream theorem only needs totality,
-- compatibility, min-or-max, and monotonicity.
def RoundedModeP_float {beta : Int} [ValidRadix beta] (b : Fbound_skel) (radix : Int)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop) : Prop :=
  TotalP P ∧ CompatiblePGeneric P ∧
    MinOrMaxP b P ∧ MonotoneP_float P

-- ---------------------------------------------------------------------------
-- Coq-style `Fulp` is defined after `Fnormalize`, because Flocq defines it as
-- the radix power at the normalized exponent of the float.

-- Coq: `RleBoundRoundl` — rounding preserves lower bounds
noncomputable def RleBoundRoundl_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) : Unit :=
  ()

/-- Coq: `RleBoundRoundl` — if `P` forms a rounded mode and `p ≤ r`, then
    any `q` produced by rounding `r` also satisfies `p ≤ q`.

    Note: We use `RoundedModeP_full` which includes the float-specific
    monotonicity and projector properties needed for this proof. -/
theorem RleBoundRoundl_from_full {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) :
    ⦃⌜RoundedModeP_full (beta:=beta) b P ∧ Fbounded (beta:=beta) b p ∧
        (_root_.F2R (beta:=beta) p ≤ r) ∧ P r q⌝⦄
    (pure (RleBoundRoundl_check (beta:=beta) b radix P p q r) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) p ≤ _root_.F2R (beta:=beta) q⌝⦄ := by
  intro ⟨hRoundedMode, hBounded, hLeq, hPrq⟩
  simp only [wp, PostCond.noThrow, pure, RleBoundRoundl_check]
  -- Unpack RoundedModeP_full: TotalP P ∧ CompatiblePGeneric P ∧ MonotoneP_float P ∧
  --                           ProjectorP_float b P ∧ ProjectorEqP_float b P
  obtain ⟨_, _, hMono, hProj, hProjEq⟩ := hRoundedMode
  -- Case split: either F2R p < r or F2R p = r
  rcases hLeq.lt_or_eq with hLt | hEq
  · -- Case: F2R p < r
    -- By ProjectorP_float: P (F2R p) p (bounded floats round to themselves)
    have hPpp : P (_root_.F2R p) p := hProj p hBounded
    -- By MonotoneP_float: if F2R p < r and P (F2R p) p and P r q, then F2R p ≤ F2R q
    exact hMono (_root_.F2R p) r p q hLt hPpp hPrq
  · -- Case: F2R p = r
    -- By ProjectorEqP_float: if Fbounded p and P (F2R p) q, then F2R p = F2R q
    -- Since F2R p = r, we have P (F2R p) q = P r q = hPrq
    have hP_F2R_q : P (_root_.F2R p) q := by rw [hEq]; exact hPrq
    have hEqReal : _root_.F2R p = _root_.F2R q := hProjEq p q hBounded hP_F2R_q
    exact le_of_eq hEqReal

noncomputable def RleBoundRoundr_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) : Unit :=
  ()

/-- Coq: `RleBoundRoundr` — if `P` forms a rounded mode and `r ≤ _root_.F2R p`,
    any `q` produced by rounding `r` also satisfies `_root_.F2R q ≤ _root_.F2R p`.

    Note: We use `RoundedModeP_full` which includes the float-specific
    monotonicity and projector properties needed for this proof (matching RleBoundRoundl). -/
theorem RleBoundRoundr_from_full {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) :
    ⦃⌜RoundedModeP_full (beta:=beta) b P ∧ Fbounded (beta:=beta) b p ∧
        (r ≤ _root_.F2R (beta:=beta) p) ∧ P r q⌝⦄
    (pure (RleBoundRoundr_check (beta:=beta) b radix P p q r) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) q ≤ _root_.F2R (beta:=beta) p⌝⦄ := by
  intro ⟨hRoundedMode, hBounded, hLeq, hPrq⟩
  simp only [wp, PostCond.noThrow, pure, RleBoundRoundr_check]
  -- Unpack RoundedModeP_full: TotalP P ∧ CompatiblePGeneric P ∧ MonotoneP_float P ∧
  --                           ProjectorP_float b P ∧ ProjectorEqP_float b P
  obtain ⟨_, _, hMono, hProj, hProjEq⟩ := hRoundedMode
  -- Case split: either r < F2R p or r = F2R p
  rcases hLeq.lt_or_eq with hLt | hEq
  · -- Case: r < F2R p
    -- By ProjectorP_float: P (F2R p) p (bounded floats round to themselves)
    have hPpp : P (_root_.F2R p) p := hProj p hBounded
    -- By MonotoneP_float: if r < F2R p and P r q and P (F2R p) p, then F2R q ≤ F2R p
    exact hMono r (_root_.F2R p) q p hLt hPrq hPpp
  · -- Case: r = F2R p
    -- By ProjectorEqP_float: if Fbounded p and P (F2R p) q, then F2R p = F2R q
    -- Since r = F2R p, we have P r q = P (F2R p) q
    have hP_F2R_q : P (_root_.F2R p) q := by rw [← hEq]; exact hPrq
    have hEqReal : _root_.F2R p = _root_.F2R q := hProjEq p q hBounded hP_F2R_q
    exact le_of_eq hEqReal.symm

private theorem roundedProjectorOfMode {beta : Int} [ValidRadix beta]
    (b : Fbound_skel)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (hRounded : RoundedModeP b P)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta)
    (hpBounded : Fbounded b p) (hpq : P (_root_.F2R p) q) :
    _root_.F2R p = _root_.F2R q := by
  rcases hRounded.2.2.1 (_root_.F2R p) q hpq with hMin | hMax
  · exact le_antisymm (hMin.2.2 p hpBounded le_rfl) hMin.2.1
  · exact le_antisymm hMax.2.1 (hMax.2.2 p hpBounded le_rfl)

private theorem roundedFixedOfMode {beta : Int} [ValidRadix beta]
    (b : Fbound_skel)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (hRounded : RoundedModeP b P)
    (p : FloatSpec.Core.Defs.FlocqFloat beta)
    (hpBounded : Fbounded b p) : P (_root_.F2R p) p := by
  obtain ⟨q, hq⟩ := hRounded.1 (_root_.F2R p)
  have heq := roundedProjectorOfMode b P hRounded p q hpBounded hq
  exact hRounded.2.1 (_root_.F2R p) (_root_.F2R p) q p
    hq rfl heq.symm hpBounded

/-- FLoCq `RleBoundRoundl`, with projector facts derived from `RoundedModeP`. -/
theorem RleBoundRoundl {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) :
    ⦃⌜RoundedModeP b P ∧ Fbounded b p ∧ _root_.F2R p ≤ r ∧ P r q⌝⦄
    (pure (RleBoundRoundl_check b radix P p q r) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R p ≤ _root_.F2R q⌝⦄ := by
  intro ⟨hRounded, hpBounded, hpr, hrq⟩
  simp only [wp, PostCond.noThrow, pure, RleBoundRoundl_check]
  rcases hpr.lt_or_eq with hlt | heq
  · exact hRounded.2.2.2 (_root_.F2R p) r p q hlt
      (roundedFixedOfMode b P hRounded p hpBounded) hrq
  · have hrq' : P (_root_.F2R p) q := heq.symm ▸ hrq
    exact le_of_eq (roundedProjectorOfMode b P hRounded p q hpBounded hrq')

/-- FLoCq `RleBoundRoundr`, with projector facts derived from `RoundedModeP`. -/
theorem RleBoundRoundr {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) :
    ⦃⌜RoundedModeP b P ∧ Fbounded b p ∧ r ≤ _root_.F2R p ∧ P r q⌝⦄
    (pure (RleBoundRoundr_check b radix P p q r) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R q ≤ _root_.F2R p⌝⦄ := by
  intro ⟨hRounded, hpBounded, hrp, hrq⟩
  simp only [wp, PostCond.noThrow, pure, RleBoundRoundr_check]
  rcases hrp.lt_or_eq with hlt | heq
  · exact hRounded.2.2.2 r (_root_.F2R p) q p hlt hrq
      (roundedFixedOfMode b P hRounded p hpBounded)
  · have hrq' : P (_root_.F2R p) q := heq ▸ hrq
    exact le_of_eq (roundedProjectorOfMode b P hRounded p q hpBounded hrq').symm

/-- Minimal normal mantissa (`nNormMin`) defined using a positive-exponent power. -/
def nNormMin (radix : Int) (precision : Nat) : Int :=
  radix ^ (precision - 1)

/-- Coq: `PosNormMin` — the positive mantissa bound is `radix * nNormMin`. -/
theorem PosNormMin (radix : Int) (b : Fbound_skel) (precision : Nat) :
    0 < precision →
    b.vNum = Zpower_nat radix precision →
    b.vNum = radix * nNormMin radix precision := by
  intro hprecision hvNum
  rw [hvNum]
  cases precision with
  | zero => cases hprecision
  | succ p =>
      simp [Zpower_nat, nNormMin, pow_succ, mul_comm]

/-- Coq: `pPred (vNum b)` is the predecessor of the positive mantissa bound. -/
def pPred (n : Int) : Int :=
  n - 1

-- Coq: `firstNormalPos_eq` — value of the first normal positive float
def firstNormalPos {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat) :
    FloatSpec.Core.Defs.FlocqFloat beta :=
  ⟨nNormMin radix precision, -b.dExp⟩

noncomputable def firstNormalPos_eq_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat) : Unit :=
  ()

/-- Coq: `firstNormalPos_eq` — interpreting the `firstNormalPos` float at
    base `radix` equals the real value `(nNormMin radix precision : ℝ) * (radix : ℝ) ^ (-b.dExp)`.
    Following the file's Hoare-triple convention.

    Note: Requires `beta = radix` since F2R uses the type-level `beta` as base,
    while `firstNormalPos` constructs the mantissa using `radix`. -/
theorem firstNormalPos_eq {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat) :
    ⦃⌜beta = radix⌝⦄
    (pure (firstNormalPos_eq_check (beta:=beta) radix b precision) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) (firstNormalPos (beta:=beta) radix b precision)
            = (nNormMin radix precision : ℝ) * (radix : ℝ) ^ (-b.dExp)⌝⦄ := by
  intro hBetaEqRadix
  simp only [wp, PostCond.noThrow, pure, firstNormalPos_eq_check,
             _root_.F2R, firstNormalPos, nNormMin,
             FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp,
             FloatSpec.Core.Defs.F2R]
  -- The goal is: (radix ^ (precision - 1) : ℝ) * (beta : ℝ) ^ (-b.dExp) =
  --              (radix ^ (precision - 1) : ℝ) * (radix : ℝ) ^ (-b.dExp)
  -- Since beta = radix (from hBetaEqRadix which is ⌜beta = radix⌝.down), this follows
  -- Extract the underlying Int equality from the lifted proposition
  have hEq : beta = radix := hBetaEqRadix
  -- Convert Int equality to Real equality using congruence
  have hEqReal : (beta : ℝ) = (radix : ℝ) := congrArg (Int.cast) hEq
  rw [hEqReal]
  -- Goal is now reflexive: a = a
  rfl

-- ---------------------------------------------------------------------------
-- Closest/Normal predicates (from Pff.v sections)

-- Coq: Closest r p := Fbounded b p /\
--   forall f, Fbounded b f -> Rabs (p - r) <= Rabs (f - r).
def Closest {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ) (r : ℝ)
    (f : FloatSpec.Core.Defs.FlocqFloat beta) : Prop :=
  Fbounded bo f ∧
    ∀ g : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded bo g →
        |_root_.F2R f - r| ≤ |_root_.F2R g - r|

/-- Coq: `Closestbplusb` — closestness for the enlarged `plusExp` bound
restricts to closestness for the original bound. -/
theorem Closestbplusb {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat)
    (z : ℝ) (f : FloatSpec.Core.Defs.FlocqFloat beta)
    (hClosest : Closest (beta:=beta) (plusExp b precision) radix z f)
    (hfBound : Fbounded (beta:=beta) b f) :
    Closest (beta:=beta) b radix z f := by
  constructor
  · exact hfBound
  · intro g hg
    exact hClosest.2 g (by
      rcases hg with ⟨hgNum, hgExp⟩
      constructor
      · simpa [plusExp] using hgNum
      · dsimp [plusExp]
        have hnonneg : (0 : Int) ≤ ((precision - 1 : Nat) : Int) :=
          Int.natCast_nonneg _
        omega)

noncomputable def TwoSumProp_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (a b x y : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `TwoSumProp`.

If `x` is a closest rounded value of `a + b` and `y` represents the residual
`a + b - x`, then the residual is no larger in absolute value than `b`. -/
theorem TwoSumProp {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (a b x y : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) bo a ∧
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) a + _root_.F2R (beta:=beta) b) x ∧
        _root_.F2R (beta:=beta) y =
          _root_.F2R (beta:=beta) a + _root_.F2R (beta:=beta) b -
            _root_.F2R (beta:=beta) x⌝⦄
    (pure (TwoSumProp_check (beta:=beta) bo radix a b x y) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) y| ≤
      |_root_.F2R (beta:=beta) b|⌝⦄ := by
  intro h
  rcases h with ⟨ha, hxClosest, hy⟩
  simp only [wp, PostCond.noThrow, pure, TwoSumProp_check, Id.run,
    ULift.up_down]
  let r : ℝ := _root_.F2R (beta:=beta) a + _root_.F2R (beta:=beta) b
  have hdist :
      |_root_.F2R (beta:=beta) x - r| ≤
        |_root_.F2R (beta:=beta) a - r| :=
    hxClosest.2 a ha
  have hy_abs :
      |_root_.F2R (beta:=beta) y| =
        |_root_.F2R (beta:=beta) x - r| := by
    have hy' :
        _root_.F2R (beta:=beta) y =
          -(_root_.F2R (beta:=beta) x - r) := by
      calc
        _root_.F2R (beta:=beta) y =
            _root_.F2R (beta:=beta) a + _root_.F2R (beta:=beta) b -
              _root_.F2R (beta:=beta) x := hy
        _ = -(_root_.F2R (beta:=beta) x - r) := by
          dsimp [r]
          ring
    exact (congrArg abs hy').trans (abs_neg _)
  have hb_abs :
      |_root_.F2R (beta:=beta) a - r| =
        |_root_.F2R (beta:=beta) b| := by
    have hdiff :
        _root_.F2R (beta:=beta) a - r =
          -_root_.F2R (beta:=beta) b := by
      dsimp [r]
      ring
    rw [hdiff, abs_neg]
  rw [hy_abs]
  exact le_trans hdist (le_of_eq hb_abs)

-- Coq: Fnormal p := Fbounded b p /\ (Zpos (vNum b) <= Z.abs (radix * Fnum p))%Z
def Fnormal {beta : Int} [ValidRadix beta]
    (radix : Int) (bo : Fbound_skel)
    (f : FloatSpec.Core.Defs.FlocqFloat beta) : Prop :=
  Fbounded bo f ∧ bo.vNum ≤ |radix * f.Fnum|

-- Coq: Fsubnormal p := Fbounded b p /\ Fexp p = (- dExp b)%Z /\
--   (Z.abs (radix * Fnum p) < Zpos (vNum b))%Z
def Fsubnormal {beta : Int} [ValidRadix beta]
    (radix : Int) (bo : Fbound_skel)
    (f : FloatSpec.Core.Defs.FlocqFloat beta) : Prop :=
  Fbounded bo f ∧ f.Fexp = -bo.dExp ∧ |radix * f.Fnum| < bo.vNum

-- Backward-compatible alias used by proofs that already avoided the old shell.
def Fnormal' {beta : Int} [ValidRadix beta]
    (radix : Int) (bo : Fbound_skel)
    (f : FloatSpec.Core.Defs.FlocqFloat beta) : Prop :=
  Fnormal radix bo f

-- Backward-compatible alias used by proofs that already avoided the old shell.
def Fsubnormal' {beta : Int} [ValidRadix beta]
    (radix : Int) (bo : Fbound_skel)
    (f : FloatSpec.Core.Defs.FlocqFloat beta) : Prop :=
  Fsubnormal radix bo f

/-- The total behavior of Pff's structurally recursive `digitAux`.

Coq calls `digitAux` with `xO q'`, where `q'` is the binary-positive
representation of `|q|`.  Since the `xO` and `xI` branches are identical,
only that constructor depth matters: it is exactly `log2 |q| + 1` for a
nonzero integer.  Keeping this total behavior is observable because the
surrounding `radixMoreThanOne` section hypothesis is not an exported binder of
`digit`, `Fnormalize`, or `Fulp`. -/
private def pffDigitAuxFuel (radix value : Int) : Int → Nat → Nat
  | _, 0 => 0
  | power, fuel + 1 =>
      if power > value then 0
      else Nat.succ (pffDigitAuxFuel radix value (radix * power) fuel)

private def pffStructuralDigit (radix q : Int) : Nat :=
  if q = 0 then 0
  else pffDigitAuxFuel radix (q.natAbs : Int) 1 (Nat.log2 q.natAbs + 1)

/-- Coq Pff's `digit`, using the existing Core implementation only on the
radix domain where the two algorithms are extensionally equivalent. -/
def pffDigit (radix q : Int) : Nat :=
  if 1 < radix then
    Int.toNat (FloatSpec.Core.Digits.Zdigits radix q)
  else
    pffStructuralDigit radix q

/-- On the mathematical radix domain, Pff's structural digit agrees with the
Core logarithmic implementation used by the rest of FloatSpec. -/
@[simp] theorem pffDigit_of_one_lt (radix q : Int) (hradix : 1 < radix) :
    pffDigit radix q =
      Int.toNat (FloatSpec.Core.Digits.Zdigits radix q) := by
  simp [pffDigit, hradix]

private theorem pffDigit_neg (radix q : Int) :
    pffDigit radix (-q) = pffDigit radix q := by
  by_cases hradix : 1 < radix
  · simp only [pffDigit_of_one_lt radix (-q) hradix,
      pffDigit_of_one_lt radix q hradix]
    have h := FloatSpec.Core.Digits.Zdigits_opp
      (beta := radix) (n := q) (by trivial)
    simp only [wp, PostCond.noThrow, pure, Id.run] at h
    rcases h with ⟨dn, hdn, hd⟩
    exact congrArg Int.toNat (hd.trans hdn.symm)
  · simp [pffDigit, hradix, pffStructuralDigit]

private theorem pffDigit_abs (radix q : Int) :
    pffDigit radix |q| = pffDigit radix q := by
  by_cases hq : 0 ≤ q
  · rw [abs_of_nonneg hq]
  · rw [abs_of_neg (lt_of_not_ge hq), pffDigit_neg]

-- Coq: `Fdigit p := digit radix (Fnum p)`.
def Fdigit {beta : Int} [ValidRadix beta]
    (radix : Int) (x : FloatSpec.Core.Defs.FlocqFloat beta) : Nat :=
  pffDigit radix x.Fnum

-- Coq: `Fshift n x := Float (Fnum x * Zpower_nat radix n) (Fexp x - n)`.
def Fshift {beta : Int} [ValidRadix beta]
    (radix : Int) (n : Nat) (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    FloatSpec.Core.Defs.FlocqFloat beta :=
  ⟨x.Fnum * radix ^ n, x.Fexp - (n : Int)⟩

private theorem Fshift_value {beta : Int} [ValidRadix beta]
    (radix : Int) (n : Nat) (x : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta_radix : beta = radix) (hradix : 1 < radix) :
    _root_.F2R (beta:=beta) (Fshift (beta:=beta) radix n x) =
      _root_.F2R (beta:=beta) x := by
  subst beta
  have hradix_pos_real : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (by omega : (0 : Int) < radix)
  have hradix_ne_real : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos_real
  have hsplit :
      (radix : ℝ) ^ x.Fexp =
        (radix : ℝ) ^ (n : Int) * (radix : ℝ) ^ (x.Fexp - (n : Int)) := by
    have hsum : (n : Int) + (x.Fexp - (n : Int)) = x.Fexp := by omega
    simpa [hsum] using
      (zpow_add₀ hradix_ne_real (n : Int) (x.Fexp - (n : Int)))
  simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, Fshift,
    FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp,
    Int.cast_mul, Int.cast_pow]
  have hzpow_n : (radix : ℝ) ^ (n : Int) = (radix : ℝ) ^ n :=
    zpow_ofNat (radix : ℝ) n
  rw [hsplit]
  rw [hzpow_n]
  ring_nf

-- Predicate: zero mantissa (Coq: `is_Fzero`). Placed early for downstream uses.
def is_Fzero {beta : Int} [ValidRadix beta] (x : FloatSpec.Core.Defs.FlocqFloat beta) : Prop :=
  x.Fnum = 0

/-!
Coq: `FSucc` / `FPred` and `Fnormalize`.

These definitions are placed before `FNSucc`/`FNPred` so the public normalized
neighbor aliases can match Flocq:
`FNSucc x := FSucc (Fnormalize radix b precision x)`.
-/

def FSucc {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    FloatSpec.Core.Defs.FlocqFloat beta :=
  if x.Fnum = pPred b.vNum then
    ⟨nNormMin radix precision, x.Fexp + 1⟩
  else if x.Fnum = -nNormMin radix precision then
    if x.Fexp = -b.dExp then
      ⟨x.Fnum + 1, x.Fexp⟩
    else
      ⟨-pPred b.vNum, x.Fexp - 1⟩
  else
    ⟨x.Fnum + 1, x.Fexp⟩

def FPred {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    FloatSpec.Core.Defs.FlocqFloat beta :=
  if x.Fnum = -pPred b.vNum then
    ⟨-nNormMin radix precision, x.Fexp + 1⟩
  else if x.Fnum = nNormMin radix precision then
    if x.Fexp = -b.dExp then
      ⟨x.Fnum - 1, x.Fexp⟩
    else
      ⟨pPred b.vNum, x.Fexp - 1⟩
  else
    ⟨x.Fnum - 1, x.Fexp⟩

/-- Coq: `Fnormalize`.

Zero is represented with the boundary exponent `-b.dExp`; nonzero floats are
shifted just enough to fit the requested precision without crossing the
minimum exponent. -/
def Fnormalize {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    FloatSpec.Core.Defs.FlocqFloat beta :=
  if p.Fnum = 0 then
    ⟨0, -b.dExp⟩
  else
    Fshift (beta:=beta) radix
      (min (precision - Fdigit (beta:=beta) radix p)
        (Int.natAbs (b.dExp + p.Fexp)))
      p

private lemma Fdigit_Fopp {beta : Int} [ValidRadix beta]
    (radix : Int) (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    Fdigit (beta:=beta) radix (FloatSpec.Calc.Operations.Fopp (beta:=beta) p) =
      Fdigit (beta:=beta) radix p := by
  cases p with
  | mk m e =>
    simpa [Fdigit, FloatSpec.Calc.Operations.Fopp] using pffDigit_neg radix m

private lemma Fdigit_Fabs_early {beta : Int} [ValidRadix beta]
    (radix : Int) (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    Fdigit (beta:=beta) radix (FloatSpec.Calc.Operations.Fabs (beta:=beta) p) =
      Fdigit (beta:=beta) radix p := by
  cases p with
  | mk m e =>
    unfold Fdigit FloatSpec.Calc.Operations.Fabs
    change pffDigit radix (m.natAbs : Int) = pffDigit radix m
    have habs : (|m| : Int) = (m.natAbs : Int) := by
      rw [Int.abs_eq_natAbs]
    rw [← habs]
    exact pffDigit_abs radix m

noncomputable def Fnormalize_Fopp_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `Fnormalize_Fopp` — normalization commutes with float negation. -/
theorem Fnormalize_Fopp {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜True⌝⦄
    (pure (Fnormalize_Fopp_check (beta:=beta) radix b precision p) : Id Unit)
    ⦃⇓_ => ⌜Fnormalize (beta:=beta) radix b precision
          (FloatSpec.Calc.Operations.Fopp (beta:=beta) p) =
        FloatSpec.Calc.Operations.Fopp (beta:=beta)
          (Fnormalize (beta:=beta) radix b precision p)⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, Fnormalize_Fopp_check, Id.run,
    ULift.up_down]
  cases p with
  | mk m e =>
    unfold Fnormalize FloatSpec.Calc.Operations.Fopp Fshift
    by_cases hm : m = 0
    · simp [hm]
    · have hneg : -m ≠ 0 := by omega
      have hfd :
          Fdigit (beta:=beta) radix { Fnum := -m, Fexp := e } =
            Fdigit (beta:=beta) radix { Fnum := m, Fexp := e } := by
        simpa [FloatSpec.Calc.Operations.Fopp] using
          (Fdigit_Fopp (beta:=beta) radix { Fnum := m, Fexp := e })
      simp [hm, hneg, hfd]

noncomputable def Fnormalize_Fabs_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Local bridge: normalization commutes with float absolute value. -/
theorem Fnormalize_Fabs {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜0 < radix⌝⦄
    (pure (Fnormalize_Fabs_check (beta:=beta) radix b precision p) : Id Unit)
    ⦃⇓_ => ⌜Fnormalize (beta:=beta) radix b precision
          (FloatSpec.Calc.Operations.Fabs (beta:=beta) p) =
        FloatSpec.Calc.Operations.Fabs (beta:=beta)
          (Fnormalize (beta:=beta) radix b precision p)⌝⦄ := by
  intro hradix_pos
  simp only [wp, PostCond.noThrow, pure, Fnormalize_Fabs_check, Id.run,
    ULift.up_down]
  cases p with
  | mk m e =>
    unfold Fnormalize FloatSpec.Calc.Operations.Fabs Fshift
    by_cases hm : m = 0
    · simp [hm]
    · have habs_ne : (m.natAbs : Int) ≠ 0 := by
        intro hzero
        have hm_abs_zero : m.natAbs = 0 := by exact_mod_cast hzero
        exact hm (Int.natAbs_eq_zero.mp hm_abs_zero)
      have hfd :
          Fdigit (beta:=beta) radix { Fnum := (m.natAbs : Int), Fexp := e } =
            Fdigit (beta:=beta) radix { Fnum := m, Fexp := e } := by
        simpa [FloatSpec.Calc.Operations.Fabs] using
          (Fdigit_Fabs_early (beta:=beta) radix { Fnum := m, Fexp := e })
      let n :=
        min (precision - Fdigit (beta:=beta) radix { Fnum := m, Fexp := e })
          (Int.natAbs (b.dExp + e))
      have hn :
          min
              (precision -
                Fdigit (beta:=beta) radix { Fnum := (m.natAbs : Int), Fexp := e })
              (Int.natAbs (b.dExp + e)) = n := by
        simp only [n, hfd]
      have hpow_nonneg : 0 ≤ radix ^ n := by
        exact pow_nonneg (le_of_lt hradix_pos) n
      have hnum_abs :
          ((m * radix ^ n).natAbs : Int) = (m.natAbs : Int) * radix ^ n := by
        rw [Int.natAbs_mul, Nat.cast_mul]
        congr 1
        exact Int.natAbs_of_nonneg hpow_nonneg
      simp only [wp, PostCond.noThrow, pure, Id.run, ULift.up_down,
        PredTrans.pure, PredTrans.apply, ULift.down, hm, habs_ne, ite_false]
      change
        ({ Fnum :=
              (m.natAbs : Int) *
                radix ^ min
                  (precision -
                    Fdigit (beta:=beta) radix { Fnum := (m.natAbs : Int), Fexp := e })
                  (Int.natAbs (b.dExp + e)),
            Fexp :=
              e - (min
                (precision -
                  Fdigit (beta:=beta) radix { Fnum := (m.natAbs : Int), Fexp := e })
                (Int.natAbs (b.dExp + e)) : Nat) } :
          FloatSpec.Core.Defs.FlocqFloat beta) =
        { Fnum :=
            ((m * radix ^ min
              (precision - Fdigit (beta:=beta) radix { Fnum := m, Fexp := e })
              (Int.natAbs (b.dExp + e))).natAbs : Int),
          Fexp :=
            e - (min
              (precision - Fdigit (beta:=beta) radix { Fnum := m, Fexp := e })
              (Int.natAbs (b.dExp + e)) : Nat) }
      rw [hn, hnum_abs]

-- ---------------------------------------------------------------------------
-- Parity on normalized floats and neighbor operations

-- Coq uses predicates like FNeven/FNodd and neighbors FNSucc/FNPred.
-- FNeven/FNodd are parity predicates on the normalized representation.
-- The local signatures keep `radix : ℝ` for existing callers; the integer
-- radix used by the translated Pff section is the type-level `beta`.
-- Coq: Definition FNeven (p : float) := Feven (Fnormalize radix b precision p).
def FNeven {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (_radix : ℝ) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Prop :=
  Feven (beta:=beta) (Fnormalize (beta:=beta) beta b precision p)

/-- Local bridge: normalized-even parity is preserved by float absolute value. -/
theorem FNevenFabs {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜0 < beta ∧ FNeven (beta:=beta) b radix precision p⌝⦄
    (pure () : Id Unit)
    ⦃⇓_ => ⌜FNeven (beta:=beta) b radix precision
      (FloatSpec.Calc.Operations.Fabs (beta:=beta) p)⌝⦄ := by
  intro ⟨hbeta_pos, h⟩
  simp only [wp, PostCond.noThrow, pure, Id.run, ULift.up_down]
  simp only [FNeven] at h ⊢
  have hnorm := Fnormalize_Fabs (beta:=beta) beta b precision p
  have hnorm' :
      Fnormalize (beta:=beta) beta b precision
          (FloatSpec.Calc.Operations.Fabs (beta:=beta) p) =
        FloatSpec.Calc.Operations.Fabs (beta:=beta)
          (Fnormalize (beta:=beta) beta b precision p) := by
    simpa only [wp, PostCond.noThrow, pure, Fnormalize_Fabs_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Id.run,
      ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
      hnorm hbeta_pos
  rw [hnorm']
  unfold Feven FloatSpec.Calc.Operations.Fabs
  have hdiv :
      (2 : Int) ∣ (Fnormalize (beta:=beta) beta b precision p).Fnum := h.two_dvd
  have hdiv_abs :
      (2 : Int) ∣
        Int.ofNat (Int.natAbs (Fnormalize (beta:=beta) beta b precision p).Fnum) :=
    (Int.dvd_natAbs (a := (2 : Int))
      (b := (Fnormalize (beta:=beta) beta b precision p).Fnum)).mpr hdiv
  exact even_iff_two_dvd.mpr hdiv_abs

-- Coq: Definition FNodd (p : float) := Fodd (Fnormalize radix b precision p).
def FNodd {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (_radix : ℝ) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Prop :=
  Fodd (beta:=beta) (Fnormalize (beta:=beta) beta b precision p)

/-- Coq: `FNSucc x := FSucc (Fnormalize radix b precision x)`. -/
def FNSucc {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (_radix : ℝ) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : FloatSpec.Core.Defs.FlocqFloat beta :=
  FSucc (beta:=beta) b beta precision
    (Fnormalize (beta:=beta) beta b precision p)

/-- Coq: `FNPred x := FPred (Fnormalize radix b precision x)`. -/
def FNPred {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (_radix : ℝ) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : FloatSpec.Core.Defs.FlocqFloat beta :=
  FPred (beta:=beta) b beta precision
    (Fnormalize (beta:=beta) beta b precision p)

-- The Flocq `FSucc` parity lemmas are stated below, after the faithful
-- boundary-aware `FSucc` definition.

-- EvenClosest: closest rounding with tie-breaking toward even (or uniqueness)
def EvenClosest {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat)
    (r : ℝ)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Prop :=
  Closest (beta:=beta) b radix r p ∧
  (FNeven (beta:=beta) b radix precision p ∨
    (∀ q : FloatSpec.Core.Defs.FlocqFloat beta,
      Closest (beta:=beta) b radix r q →
        _root_.F2R (beta:=beta) q = _root_.F2R (beta:=beta) p))

-- ---------------------------------------------------------------------------
-- Rounding operators (RND_*) and canonicity (skeletons to mirror Coq Pff.v)

-- Coq: Fcanonic a := Fnormal a \/ Fsubnormal a.
def Fcanonic {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel)
    (f : FloatSpec.Core.Defs.FlocqFloat beta) : Prop :=
  Fnormal radix b f ∨ Fsubnormal radix b f

-- Backward-compatible alias used by proofs that already avoided the old shell.
def Fcanonic' {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel)
    (f : FloatSpec.Core.Defs.FlocqFloat beta) : Prop :=
  Fcanonic radix b f

/-- Coq: `FnormalPpred` — the predecessor of the positive mantissa bound is normal. -/
theorem FnormalPpred {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat) (x : Int) :
    1 < radix →
    0 < precision →
    b.vNum = Zpower_nat radix precision →
    -b.dExp ≤ x →
    Fnormal (beta:=beta) radix b
      (⟨pPred b.vNum, x⟩ : FloatSpec.Core.Defs.FlocqFloat beta) := by
  intro hradix hprecision hvNum hx
  have hvge_two : 2 ≤ b.vNum := by
    cases precision with
    | zero => cases hprecision
    | succ p =>
        rw [hvNum, Zpower_nat]
        have hpow_pos : 0 < radix ^ p := pow_pos (by omega) p
        calc
          (2 : Int) ≤ radix := by omega
          _ ≤ radix * radix ^ p := by nlinarith [hpow_pos, hradix]
          _ = radix ^ Nat.succ p := by simpa [pow_succ, mul_comm]
  have hpPred_nonneg : 0 ≤ pPred b.vNum := by
    unfold pPred
    omega
  constructor
  · constructor
    · rw [abs_of_nonneg hpPred_nonneg]
      unfold pPred
      omega
    · exact hx
  · have hmul_nonneg : 0 ≤ radix * pPred b.vNum := by
      exact mul_nonneg (by omega) hpPred_nonneg
    rw [abs_of_nonneg hmul_nonneg]
    unfold pPred
    nlinarith [hradix, hvge_two]

/-- Coq: `FcanonicPpred` — the predecessor normal float is canonical. -/
theorem FcanonicPpred {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat) (x : Int) :
    1 < radix →
    0 < precision →
    b.vNum = Zpower_nat radix precision →
    -b.dExp ≤ x →
    Fcanonic (beta:=beta) radix b
      (⟨pPred b.vNum, x⟩ : FloatSpec.Core.Defs.FlocqFloat beta) := by
  intro hradix hprecision hvNum hx
  exact Or.inl (FnormalPpred (beta:=beta) radix b precision x hradix hprecision hvNum hx)

/-- Coq: `FnormalNnormMin` — the minimal normal mantissa is normal. -/
theorem FnormalNnormMin {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat) (x : Int) :
    1 < radix →
    0 < precision →
    b.vNum = Zpower_nat radix precision →
    -b.dExp ≤ x →
    Fnormal (beta:=beta) radix b
      (⟨nNormMin radix precision, x⟩ : FloatSpec.Core.Defs.FlocqFloat beta) := by
  intro hradix hprecision hvNum hx
  have hnpos : 0 < nNormMin radix precision := by
    cases precision with
    | zero => cases hprecision
    | succ p =>
        simp [nNormMin]
        exact pow_pos (by omega) p
  have hposnorm := PosNormMin radix b precision hprecision hvNum
  constructor
  · constructor
    · rw [abs_of_nonneg (le_of_lt hnpos)]
      rw [hposnorm]
      nlinarith [hradix, hnpos]
    · exact hx
  · have hmul_pos : 0 < radix * nNormMin radix precision := by
      exact mul_pos (by omega) hnpos
    rw [abs_of_pos hmul_pos]
    rw [hposnorm]

/-- Coq: `FcanonicNnormMin` — the minimal normal mantissa is canonical. -/
theorem FcanonicNnormMin {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat) (x : Int) :
    1 < radix →
    0 < precision →
    b.vNum = Zpower_nat radix precision →
    -b.dExp ≤ x →
    Fcanonic (beta:=beta) radix b
      (⟨nNormMin radix precision, x⟩ : FloatSpec.Core.Defs.FlocqFloat beta) := by
  intro hradix hprecision hvNum hx
  exact Or.inl (FnormalNnormMin (beta:=beta) radix b precision x hradix hprecision hvNum hx)

noncomputable def FcanonicBound_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FcanonicBound` — canonical floats are bounded. -/
theorem FcanonicBound {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fcanonic (beta:=beta) radix b p⌝⦄
    (pure (FcanonicBound_check (beta:=beta) radix b p) : Id Unit)
    ⦃⇓_ => ⌜Fbounded (beta:=beta) b p⌝⦄ := by
  intro hcan
  simp only [wp, PostCond.noThrow, pure, FcanonicBound_check]
  rcases hcan with hnormal | hsubnormal
  · exact hnormal.1
  · exact hsubnormal.1

noncomputable def FcanonicFopp_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FcanonicFopp` — canonicity preserved under float negation. -/
theorem FcanonicFopp {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fcanonic (beta:=beta) radix b p⌝⦄
    (pure (FcanonicFopp_check (beta:=beta) radix b p) : Id Unit)
    ⦃⇓_ => ⌜Fcanonic (beta:=beta) radix b (Fopp p)⌝⦄ := by
  intro hcan
  simp only [wp, PostCond.noThrow, pure, FcanonicFopp_check]
  rcases hcan with hnormal | hsubnormal
  · left
    simpa [Fcanonic, Fnormal, Fsubnormal, Fbounded, Fopp,
      FloatSpec.Calc.Operations.Fopp, abs_mul, abs_neg, mul_neg]
      using hnormal
  · right
    simpa [Fcanonic, Fnormal, Fsubnormal, Fbounded, Fopp,
      FloatSpec.Calc.Operations.Fopp, abs_mul, abs_neg, mul_neg]
      using hsubnormal

noncomputable def FcanonicFabs_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FcanonicFabs` — canonicity preserved under float absolute value. -/
theorem FcanonicFabs {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fcanonic (beta:=beta) radix b p⌝⦄
    (pure (FcanonicFabs_check (beta:=beta) radix b p) : Id Unit)
    ⦃⇓_ => ⌜Fcanonic (beta:=beta) radix b (Fabs p)⌝⦄ := by
  intro hcan
  simp only [wp, PostCond.noThrow, pure, FcanonicFabs_check]
  rcases hcan with hnormal | hsubnormal
  · left
    simpa [Fcanonic, Fnormal, Fsubnormal, Fbounded, Fabs,
      FloatSpec.Calc.Operations.Fabs, Int.natCast_natAbs, abs_mul]
      using hnormal
  · right
    simpa [Fcanonic, Fnormal, Fsubnormal, Fbounded, Fabs,
      FloatSpec.Calc.Operations.Fabs, Int.natCast_natAbs, abs_mul]
      using hsubnormal

noncomputable def FSuccSimpl1_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit := ()

/-- Coq: `FSuccSimpl1`. -/
theorem FSuccSimpl1 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜x.Fnum = pPred b.vNum⌝⦄
    (pure (FSuccSimpl1_check (beta:=beta) b radix precision x) : Id Unit)
    ⦃⇓_ => ⌜FSucc (beta:=beta) b radix precision x =
      ⟨nNormMin radix precision, x.Fexp + 1⟩⌝⦄ := by
  intro hx
  have hx' : x.Fnum = pPred b.vNum := by
    simpa using hx
  simp only [wp, PostCond.noThrow, pure, FSuccSimpl1_check, Id.run,
    PredTrans.pure, PredTrans.apply, ULift.down]
  simp [FSucc, hx']

noncomputable def FSuccSimpl2_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit := ()

/-- Coq: `FSuccSimpl2`. -/
theorem FSuccSimpl2 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜pPred b.vNum ≠ -nNormMin radix precision ∧
        x.Fnum = -nNormMin radix precision ∧ x.Fexp ≠ -b.dExp⌝⦄
    (pure (FSuccSimpl2_check (beta:=beta) b radix precision x) : Id Unit)
    ⦃⇓_ => ⌜FSucc (beta:=beta) b radix precision x =
      ⟨-pPred b.vNum, x.Fexp - 1⟩⌝⦄ := by
  intro ⟨hdisjoint, hnum, hexp⟩
  simp only [wp, PostCond.noThrow, pure, FSuccSimpl2_check, Id.run,
    PredTrans.pure, PredTrans.apply, ULift.down]
  have hnot_pred : x.Fnum ≠ pPred b.vNum := by
    intro hpred
    exact hdisjoint (hpred.symm.trans hnum)
  have hnot_boundary : -nNormMin radix precision ≠ pPred b.vNum := by
    intro h
    exact hdisjoint h.symm
  simp [FSucc, hnot_pred, hnot_boundary, hnum, hexp]

noncomputable def FSuccSimpl3_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat) : Unit := ()

/-- Coq: `FSuccSimpl3`. -/
theorem FSuccSimpl3 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat) :
    ⦃⌜pPred b.vNum ≠ -nNormMin radix precision⌝⦄
    (pure (FSuccSimpl3_check (beta:=beta) b radix precision) : Id Unit)
    ⦃⇓_ => ⌜FSucc (beta:=beta) b radix precision
      (⟨-nNormMin radix precision, -b.dExp⟩ :
        FloatSpec.Core.Defs.FlocqFloat beta) =
      ⟨-nNormMin radix precision + 1, -b.dExp⟩⌝⦄ := by
  intro hneq
  simp only [wp, PostCond.noThrow, pure, FSuccSimpl3_check, Id.run,
    ULift.up_down]
  simp [FSucc, hneq.symm]

noncomputable def FSuccSimpl4_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit := ()

/-- Coq: `FSuccSimpl4`. -/
theorem FSuccSimpl4 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜x.Fnum ≠ pPred b.vNum ∧ x.Fnum ≠ -nNormMin radix precision⌝⦄
    (pure (FSuccSimpl4_check (beta:=beta) b radix precision x) : Id Unit)
    ⦃⇓_ => ⌜FSucc (beta:=beta) b radix precision x =
      ⟨x.Fnum + 1, x.Fexp⟩⌝⦄ := by
  intro ⟨hpred, hnorm⟩
  simp only [wp, PostCond.noThrow, pure, FSuccSimpl4_check, Id.run,
    ULift.up_down]
  simp [FSucc, hpred, hnorm]

private lemma pPred_ne_neg_nNormMin
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (hradix : 1 < radix) (hprecision : precision ≠ 0)
    (hvNum : b.vNum = Zpower_nat radix precision) :
    pPred b.vNum ≠ -nNormMin radix precision := by
  have hnpos : 0 < nNormMin radix precision := by
    cases precision with
    | zero => exact (hprecision rfl).elim
    | succ p =>
        simp [nNormMin]
        exact pow_pos (by omega) p
  have hpPred_nonneg : 0 ≤ pPred b.vNum := by
    cases precision with
    | zero => exact (hprecision rfl).elim
    | succ p =>
        rw [hvNum]
        simp [Zpower_nat, pPred, pow_succ]
        have hpow_pos : 0 < radix ^ p := pow_pos (by omega) p
        nlinarith [hradix, hpow_pos]
  intro h
  rw [h] at hpPred_nonneg
  omega

private lemma nNormMin_mul_radix_cast
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (hprecision : precision ≠ 0)
    (hvNum : b.vNum = Zpower_nat radix precision) :
    (nNormMin radix precision : ℝ) * (radix : ℝ) = (b.vNum : ℝ) := by
  have hsucc : precision - 1 + 1 = precision := Nat.sub_one_add_one hprecision
  have hpow_succ : radix ^ precision = radix ^ (precision - 1) * radix := by
    rw [← hsucc]
    exact pow_succ radix (precision - 1)
  rw [hvNum, Zpower_nat, nNormMin, hpow_succ]
  norm_num [Int.cast_mul]

/-- Coq: `FSuccDiff1` — away from the negative minimal-normal boundary,
the successor step has the value of `Float 1 (Fexp x)`. -/
theorem FSuccDiff1 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    beta = radix →
    1 < radix →
    precision ≠ 0 →
    b.vNum = Zpower_nat radix precision →
    x.Fnum ≠ -nNormMin radix precision →
    FtoR (Fminus (beta:=beta) (FSucc (beta:=beta) b radix precision x) x) =
      FtoR (⟨1, x.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat beta) := by
  intro hbeta hradix hprecision hvNum hnot_min
  subst beta
  have hminus :
      FtoR
          (Fminus (beta:=radix) (FSucc (beta:=radix) b radix precision x) x) =
        FtoR (FSucc (beta:=radix) b radix precision x) - FtoR x := by
    have h := FloatSpec.Calc.Operations.F2R_minus (beta:=radix)
      (FSucc (beta:=radix) b radix precision x) x
    simpa [FtoR, Fminus] using h hradix
  rw [hminus]
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (by omega : (0 : Int) < radix)
  have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
  have hnorm_mul :
      (nNormMin radix precision : ℝ) * (radix : ℝ) = (b.vNum : ℝ) :=
    nNormMin_mul_radix_cast radix b precision hprecision hvNum
  unfold FSucc
  by_cases htop : x.Fnum = pPred b.vNum
  · simp only [htop, ↓reduceIte, FtoR, _root_.F2R, FloatSpec.Core.Defs.F2R,
      FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp]
    have hpow :
        (radix : ℝ) ^ (x.Fexp + 1) =
          (radix : ℝ) ^ x.Fexp * (radix : ℝ) := by
      rw [zpow_add₀ hradix_ne, zpow_one]
    have hpPred : (pPred b.vNum : ℝ) = (b.vNum : ℝ) - 1 := by
      simp [pPred]
    rw [hpow, hpPred]
    calc
      (nNormMin radix precision : ℝ) *
            ((radix : ℝ) ^ x.Fexp * (radix : ℝ)) -
          ((b.vNum : ℝ) - 1) * (radix : ℝ) ^ x.Fexp
          =
            ((nNormMin radix precision : ℝ) * (radix : ℝ) -
                ((b.vNum : ℝ) - 1)) *
              (radix : ℝ) ^ x.Fexp := by ring
      _ = ((b.vNum : ℝ) - ((b.vNum : ℝ) - 1)) *
            (radix : ℝ) ^ x.Fexp := by rw [hnorm_mul]
      _ = ((1 : Int) : ℝ) * (radix : ℝ) ^ x.Fexp := by norm_num
  · simp only [htop, ↓reduceIte]
    simp only [hnot_min, ↓reduceIte, FtoR, _root_.F2R,
      FloatSpec.Core.Defs.F2R, FloatSpec.Core.Defs.FlocqFloat.Fnum,
      FloatSpec.Core.Defs.FlocqFloat.Fexp, Int.cast_add, Int.cast_one]
    ring

/-- Coq: `FSuccDiff2` — at the negative minimal-normal boundary with the
minimum exponent, the successor step is one current-exponent ulp. -/
theorem FSuccDiff2 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    beta = radix →
    1 < radix →
    precision ≠ 0 →
    b.vNum = Zpower_nat radix precision →
    x.Fnum = -nNormMin radix precision →
    x.Fexp = -b.dExp →
    FtoR (Fminus (beta:=beta) (FSucc (beta:=beta) b radix precision x) x) =
      FtoR (⟨1, x.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat beta) := by
  intro hbeta hradix hprecision hvNum hnum hexp
  subst beta
  have hdisjoint : pPred b.vNum ≠ -nNormMin radix precision :=
    pPred_ne_neg_nNormMin radix b precision hradix hprecision hvNum
  have hminus :
      FtoR
          (Fminus (beta:=radix) (FSucc (beta:=radix) b radix precision x) x) =
        FtoR (FSucc (beta:=radix) b radix precision x) - FtoR x := by
    have h := FloatSpec.Calc.Operations.F2R_minus (beta:=radix)
      (FSucc (beta:=radix) b radix precision x) x
    simpa [FtoR, Fminus] using h hradix
  rw [hminus]
  unfold FSucc
  have hnot_top : x.Fnum ≠ pPred b.vNum := by
    intro htop
    exact hdisjoint (htop.symm.trans hnum)
  have hnot_top' : -nNormMin radix precision ≠ pPred b.vNum := by
    intro htop
    exact hdisjoint htop.symm
  simp only [hnot_top, hnot_top', hnum, hexp, ↓reduceIte, FtoR, _root_.F2R,
    FloatSpec.Core.Defs.F2R, FloatSpec.Core.Defs.FlocqFloat.Fnum,
    FloatSpec.Core.Defs.FlocqFloat.Fexp, Int.cast_add, Int.cast_neg,
    Int.cast_one]
  ring

/-- Coq: `FSuccDiff3` — crossing the negative minimal-normal boundary away
from the minimum exponent yields one ulp at the predecessor exponent. -/
theorem FSuccDiff3 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    beta = radix →
    1 < radix →
    precision ≠ 0 →
    b.vNum = Zpower_nat radix precision →
    x.Fnum = -nNormMin radix precision →
    x.Fexp ≠ -b.dExp →
    FtoR (Fminus (beta:=beta) (FSucc (beta:=beta) b radix precision x) x) =
      FtoR (⟨1, x.Fexp - 1⟩ : FloatSpec.Core.Defs.FlocqFloat beta) := by
  intro hbeta hradix hprecision hvNum hnum hexp
  subst beta
  have hdisjoint : pPred b.vNum ≠ -nNormMin radix precision :=
    pPred_ne_neg_nNormMin radix b precision hradix hprecision hvNum
  have hminus :
      FtoR
          (Fminus (beta:=radix) (FSucc (beta:=radix) b radix precision x) x) =
        FtoR (FSucc (beta:=radix) b radix precision x) - FtoR x := by
    have h := FloatSpec.Calc.Operations.F2R_minus (beta:=radix)
      (FSucc (beta:=radix) b radix precision x) x
    simpa [FtoR, Fminus] using h hradix
  rw [hminus]
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (by omega : (0 : Int) < radix)
  have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
  have hnorm_mul :
      (nNormMin radix precision : ℝ) * (radix : ℝ) = (b.vNum : ℝ) :=
    nNormMin_mul_radix_cast radix b precision hprecision hvNum
  have hpow :
      (radix : ℝ) ^ x.Fexp =
        (radix : ℝ) ^ (x.Fexp - 1) * (radix : ℝ) := by
    calc
      (radix : ℝ) ^ x.Fexp =
          (radix : ℝ) ^ ((x.Fexp - 1) + 1) := by
            congr 1
            omega
      _ = (radix : ℝ) ^ (x.Fexp - 1) * (radix : ℝ) := by
            rw [zpow_add₀ hradix_ne, zpow_one]
  unfold FSucc
  have hnot_top : x.Fnum ≠ pPred b.vNum := by
    intro htop
    exact hdisjoint (htop.symm.trans hnum)
  have hnot_top' : -nNormMin radix precision ≠ pPred b.vNum := by
    intro htop
    exact hdisjoint htop.symm
  simp only [hnot_top, hnot_top', ↓reduceIte, hnum, hexp, ite_false, FtoR,
    _root_.F2R, FloatSpec.Core.Defs.F2R,
    FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp]
  have hpPred_neg : ((-pPred b.vNum : Int) : ℝ) = -((b.vNum : ℝ) - 1) := by
    simp [pPred]
  rw [hpow, hpPred_neg, Int.cast_neg]
  calc
    -((b.vNum : ℝ) - 1) * (radix : ℝ) ^ (x.Fexp - 1) -
        (-(nNormMin radix precision : ℝ)) *
          ((radix : ℝ) ^ (x.Fexp - 1) * (radix : ℝ))
        =
          ((-(b.vNum : ℝ) + 1) +
              (nNormMin radix precision : ℝ) * (radix : ℝ)) *
            (radix : ℝ) ^ (x.Fexp - 1) := by ring
    _ = ((-(b.vNum : ℝ) + 1) + (b.vNum : ℝ)) *
          (radix : ℝ) ^ (x.Fexp - 1) := by rw [hnorm_mul]
    _ = ((1 : Int) : ℝ) * (radix : ℝ) ^ (x.Fexp - 1) := by norm_num

/-- Coq: `ZltNormMinVnum` — the minimal normal mantissa is strictly below
the positive mantissa bound. -/
theorem ZltNormMinVnum
    (radix : Int) (b : Fbound_skel) (precision : Nat) :
    1 < radix →
    0 < precision →
    b.vNum = Zpower_nat radix precision →
    nNormMin radix precision < b.vNum := by
  intro hradix hprecision hvNum
  have hnpos : 0 < nNormMin radix precision := by
    cases precision with
    | zero => cases hprecision
    | succ p =>
        simp [nNormMin]
        exact pow_pos (by omega) p
  have hposnorm : b.vNum = radix * nNormMin radix precision :=
    PosNormMin radix b precision hprecision hvNum
  rw [hposnorm]
  have hmul :
      (1 : Int) * nNormMin radix precision <
        radix * nNormMin radix precision :=
    mul_lt_mul_of_pos_right hradix hnpos
  simpa using hmul

/-- Coq: `FSuccNormPos` — the successor of a nonnegative normal float is normal. -/
theorem FSuccNormPos {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (a : FloatSpec.Core.Defs.FlocqFloat beta) :
    beta = radix →
    1 < radix →
    0 < precision →
    b.vNum = Zpower_nat radix precision →
    0 ≤ FtoR a →
    Fnormal (beta:=beta) radix b a →
    Fnormal (beta:=beta) radix b
      (FSucc (beta:=beta) b radix precision a) := by
  intro hbeta hradix hprecision hvNum ha_nonneg ha_normal
  subst beta
  have ha_bounded : Fbounded (beta:=radix) b a := ha_normal.1
  have ha_num_nonneg : 0 ≤ a.Fnum :=
    FloatSpec.Core.Float_prop.Fnum_ge_0 (beta:=radix) a hradix
      (by simpa [FtoR] using ha_nonneg)
  have hnpos : 0 < nNormMin radix precision := by
    cases precision with
    | zero => cases hprecision
    | succ p =>
        simp [nNormMin]
        exact pow_pos (by omega) p
  have hposnorm : b.vNum = radix * nNormMin radix precision :=
    PosNormMin radix b precision hprecision hvNum
  have hnorm_lt : nNormMin radix precision < b.vNum :=
    ZltNormMinVnum radix b precision hradix hprecision hvNum
  unfold FSucc
  by_cases htop : a.Fnum = pPred b.vNum
  · simp only [htop, ↓reduceIte]
    constructor
    · constructor
      · rw [abs_of_nonneg (le_of_lt hnpos)]
        exact hnorm_lt
      · exact le_trans ha_bounded.2 (by omega : a.Fexp ≤ a.Fexp + 1)
    · have hmul_pos : 0 < radix * nNormMin radix precision :=
        mul_pos (by omega) hnpos
      rw [abs_of_pos hmul_pos]
      exact le_of_eq hposnorm
  · simp only [htop, ↓reduceIte]
    by_cases hlow : a.Fnum = -nNormMin radix precision
    · exfalso
      omega
    · simp only [hlow, ↓reduceIte]
      constructor
      · constructor
        · have ha_num_lt : a.Fnum < b.vNum := by
            have habs : |a.Fnum| = a.Fnum := abs_of_nonneg ha_num_nonneg
            simpa [habs] using ha_bounded.1
          have hnot_pred : a.Fnum ≠ b.vNum - 1 := by
            intro h
            exact htop (by simpa [pPred, h])
          have hsucc_lt : a.Fnum + 1 < b.vNum := by
            omega
          rw [abs_of_nonneg (by omega : 0 ≤ a.Fnum + 1)]
          exact hsucc_lt
        · exact ha_bounded.2
      · have hold_abs : |radix * a.Fnum| = radix * a.Fnum := by
          rw [abs_of_nonneg]
          exact mul_nonneg (by omega) ha_num_nonneg
        have hnew_abs : |radix * (a.Fnum + 1)| = radix * (a.Fnum + 1) := by
          rw [abs_of_nonneg]
          exact mul_nonneg (by omega) (by omega)
        rw [hnew_abs]
        have hbase : b.vNum ≤ radix * a.Fnum := by
          simpa [hold_abs] using ha_normal.2
        nlinarith [hbase, hradix]

private lemma abs_succ_lt_of_abs_lt_ne_pred
    {m n : Int} (hnpos : 0 < n) :
    |m| < n → m ≠ n - 1 → |m + 1| < n := by
  intro hm hnot
  by_cases hnonneg : 0 ≤ m + 1
  · rw [abs_of_nonneg hnonneg]
    have hm_lt : m < n := lt_of_le_of_lt (le_abs_self m) hm
    have hle : m + 1 ≤ n := by omega
    have hne : m + 1 ≠ n := by
      intro h
      exact hnot (by omega)
    omega
  · have hneg : m + 1 < 0 := lt_of_not_ge hnonneg
    rw [abs_of_neg hneg]
    have hm_abs : |m| = -m := by
      rw [abs_of_neg]
      omega
    have hneg_lt : -m < n := by
      simpa [hm_abs] using hm
    omega

/-- Coq: `FSuccSubnormNotNearNormMin` — away from the positive
normal-minimum predecessor, the successor of a subnormal float remains
subnormal. -/
theorem FSuccSubnormNotNearNormMin {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (a : FloatSpec.Core.Defs.FlocqFloat beta) :
    1 < radix →
    0 < precision →
    b.vNum = Zpower_nat radix precision →
    Fsubnormal (beta:=beta) radix b a →
    a.Fnum ≠ nNormMin radix precision - 1 →
    Fsubnormal (beta:=beta) radix b
      (FSucc (beta:=beta) b radix precision a) := by
  intro hradix hprecision hvNum ha_sub hnot_near
  have hnpos : 0 < nNormMin radix precision := by
    cases precision with
    | zero => cases hprecision
    | succ p =>
        simp [nNormMin]
        exact pow_pos (by omega) p
  have hposnorm : b.vNum = radix * nNormMin radix precision :=
    PosNormMin radix b precision hprecision hvNum
  have hnorm_lt : nNormMin radix precision < b.vNum :=
    ZltNormMinVnum radix b precision hradix hprecision hvNum
  have hradix_pos : 0 < radix := by omega
  have hprod_abs : |radix * a.Fnum| = radix * |a.Fnum| := by
    rw [abs_mul, abs_of_pos hradix_pos]
  have ha_abs_lt_norm : |a.Fnum| < nNormMin radix precision := by
    have hprod_lt :
        radix * |a.Fnum| < radix * nNormMin radix precision := by
      simpa [hprod_abs, hposnorm] using ha_sub.2.2
    nlinarith [hprod_lt, hradix_pos]
  have hmul_abs_lt_vnum {m : Int}
      (hm : |m| < nNormMin radix precision) :
      |radix * m| < b.vNum := by
    have hprod : radix * |m| < radix * nNormMin radix precision :=
      mul_lt_mul_of_pos_left hm hradix_pos
    have habs : |radix * m| = radix * |m| := by
      rw [abs_mul, abs_of_pos hradix_pos]
    simpa [habs, hposnorm] using hprod
  unfold FSucc
  by_cases htop : a.Fnum = pPred b.vNum
  · exfalso
    have hge : nNormMin radix precision ≤ a.Fnum := by
      rw [htop, pPred, hposnorm]
      nlinarith [hradix, hnpos]
    have h_abs_ge : nNormMin radix precision ≤ |a.Fnum| :=
      le_trans hge (le_abs_self a.Fnum)
    omega
  · simp only [htop, ↓reduceIte]
    by_cases hlow : a.Fnum = -nNormMin radix precision
    · simp only [hlow, ha_sub.2.1, ↓reduceIte]
      have hnext_abs : |(-nNormMin radix precision) + 1| <
          nNormMin radix precision := by
        have hnonpos : (-nNormMin radix precision) + 1 ≤ 0 := by omega
        rw [abs_of_nonpos hnonpos]
        omega
      constructor
      · constructor
        · exact lt_trans hnext_abs hnorm_lt
        · exact le_rfl
      · constructor
        · rfl
        · exact hmul_abs_lt_vnum hnext_abs
    · simp only [hlow, ↓reduceIte]
      have hnext_abs : |a.Fnum + 1| < nNormMin radix precision :=
        abs_succ_lt_of_abs_lt_ne_pred hnpos ha_abs_lt_norm hnot_near
      constructor
      · constructor
        · exact lt_trans hnext_abs hnorm_lt
        · exact ha_sub.1.2
      · constructor
        · exact ha_sub.2.1
        · exact hmul_abs_lt_vnum hnext_abs

/-- Coq: `FSuccSubnormNearNormMin` — the successor of the largest positive
subnormal mantissa is the minimal positive normal float. -/
theorem FSuccSubnormNearNormMin {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (a : FloatSpec.Core.Defs.FlocqFloat beta) :
    1 < radix →
    0 < precision →
    b.vNum = Zpower_nat radix precision →
    Fsubnormal (beta:=beta) radix b a →
    a.Fnum = nNormMin radix precision - 1 →
    Fnormal (beta:=beta) radix b
      (FSucc (beta:=beta) b radix precision a) := by
  intro hradix hprecision hvNum ha_sub hnear
  have hnpos : 0 < nNormMin radix precision := by
    cases precision with
    | zero => cases hprecision
    | succ p =>
        simp [nNormMin]
        exact pow_pos (by omega) p
  have hposnorm : b.vNum = radix * nNormMin radix precision :=
    PosNormMin radix b precision hprecision hvNum
  have hnorm_lt : nNormMin radix precision < b.vNum :=
    ZltNormMinVnum radix b precision hradix hprecision hvNum
  unfold FSucc
  by_cases htop : a.Fnum = pPred b.vNum
  · exfalso
    rw [hnear, pPred, hposnorm] at htop
    nlinarith [hradix, hnpos]
  · simp only [htop, ↓reduceIte]
    by_cases hlow : a.Fnum = -nNormMin radix precision
    · exfalso
      rw [hnear] at hlow
      omega
    · simp only [hlow, ↓reduceIte, hnear]
      have hsucc_num : nNormMin radix precision - 1 + 1 =
          nNormMin radix precision := by omega
      have hnear_not_low :
          nNormMin radix precision - 1 ≠ -nNormMin radix precision := by
        omega
      rw [hsucc_num]
      simp only [hnear_not_low, ↓reduceIte]
      exact
        FnormalNnormMin (beta:=beta) radix b precision a.Fexp
          hradix hprecision hvNum (by
            have hxmin : a.Fexp = -b.dExp := ha_sub.2.1
            omega)

/-- Coq: `FSuccSubnormal` — the successor of a subnormal float is canonical. -/
theorem FSuccSubnormal {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (a : FloatSpec.Core.Defs.FlocqFloat beta) :
    1 < radix →
    0 < precision →
    b.vNum = Zpower_nat radix precision →
    Fsubnormal (beta:=beta) radix b a →
    Fcanonic (beta:=beta) radix b
      (FSucc (beta:=beta) b radix precision a) := by
  intro hradix hprecision hvNum ha_sub
  by_cases hnear : a.Fnum = nNormMin radix precision - 1
  · exact Or.inl
      (FSuccSubnormNearNormMin (beta:=beta) radix b precision a
        hradix hprecision hvNum ha_sub hnear)
  · exact Or.inr
      (FSuccSubnormNotNearNormMin (beta:=beta) radix b precision a
        hradix hprecision hvNum ha_sub hnear)

/-- Coq: `FSuccPosNotMax` — successor preserves canonicity for nonnegative
canonical floats. -/
theorem FSuccPosNotMax {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (a : FloatSpec.Core.Defs.FlocqFloat beta) :
    beta = radix →
    1 < radix →
    0 < precision →
    b.vNum = Zpower_nat radix precision →
    0 ≤ FtoR a →
    Fcanonic (beta:=beta) radix b a →
    Fcanonic (beta:=beta) radix b
      (FSucc (beta:=beta) b radix precision a) := by
  intro hbeta hradix hprecision hvNum ha_nonneg ha_can
  rcases ha_can with ha_normal | ha_sub
  · exact Or.inl
      (FSuccNormPos (beta:=beta) radix b precision a
        hbeta hradix hprecision hvNum ha_nonneg ha_normal)
  · exact
      FSuccSubnormal (beta:=beta) radix b precision a
        hradix hprecision hvNum ha_sub

/-- Coq: `FSuccNormNegNormMin` — the successor of the negative minimal
normal boundary at the minimum exponent is subnormal. -/
theorem FSuccNormNegNormMin {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat) :
    1 < radix →
    0 < precision →
    b.vNum = Zpower_nat radix precision →
    Fsubnormal (beta:=beta) radix b
      (FSucc (beta:=beta) b radix precision
        (⟨-nNormMin radix precision, -b.dExp⟩ :
          FloatSpec.Core.Defs.FlocqFloat beta)) := by
  intro hradix hprecision hvNum
  have hprecision_ne : precision ≠ 0 := by omega
  have hnpos : 0 < nNormMin radix precision := by
    cases precision with
    | zero => cases hprecision
    | succ p =>
        simp [nNormMin]
        exact pow_pos (by omega) p
  have hposnorm : b.vNum = radix * nNormMin radix precision :=
    PosNormMin radix b precision hprecision hvNum
  have hnorm_lt : nNormMin radix precision < b.vNum :=
    ZltNormMinVnum radix b precision hradix hprecision hvNum
  have hdisjoint : pPred b.vNum ≠ -nNormMin radix precision :=
    pPred_ne_neg_nNormMin radix b precision hradix hprecision_ne hvNum
  unfold FSucc
  have hnot_top : -nNormMin radix precision ≠ pPred b.vNum := by
    intro h
    exact hdisjoint h.symm
  simp only [hnot_top, ↓reduceIte, ite_eq_left rfl]
  have hnext_abs : |(-nNormMin radix precision) + 1| <
      nNormMin radix precision := by
    have hnonpos : (-nNormMin radix precision) + 1 ≤ 0 := by omega
    rw [abs_of_nonpos hnonpos]
    omega
  have hradix_pos : 0 < radix := by omega
  have hmul_abs_lt :
      |radix * ((-nNormMin radix precision) + 1)| < b.vNum := by
    have hprod : radix * |(-nNormMin radix precision) + 1| <
        radix * nNormMin radix precision :=
      mul_lt_mul_of_pos_left hnext_abs hradix_pos
    have habs : |radix * ((-nNormMin radix precision) + 1)| =
        radix * |(-nNormMin radix precision) + 1| := by
      rw [abs_mul, abs_of_pos hradix_pos]
    simpa [habs, hposnorm] using hprod
  constructor
  · constructor
    · exact lt_trans hnext_abs hnorm_lt
    · exact le_rfl
  · constructor
    · rfl
    · exact hmul_abs_lt

/-- Coq: `nNormMimLtvNum` — under precision greater than one, the minimal
normal mantissa is strictly below the predecessor of the positive bound. -/
theorem nNormMimLtvNum
    (radix : Int) (b : Fbound_skel) (precision : Nat) :
    1 < radix →
    1 < precision →
    b.vNum = Zpower_nat radix precision →
    nNormMin radix precision < pPred b.vNum := by
  intro hradix hprecision hvNum
  have hn_ge_two : 2 ≤ nNormMin radix precision := by
    cases precision with
    | zero => omega
    | succ p =>
        cases p with
        | zero => omega
        | succ q =>
            rw [nNormMin]
            simp only [Nat.succ_eq_add_one, Nat.add_sub_cancel_right]
            rw [pow_succ]
            have hpow_pos : 0 < radix ^ q := pow_pos (by omega) q
            calc
              (2 : Int) ≤ radix := by omega
              _ ≤ radix ^ q * radix := by nlinarith [hradix, hpow_pos]
  have hposnorm : b.vNum = radix * nNormMin radix precision := by
    exact PosNormMin radix b precision (by omega) hvNum
  rw [hposnorm]
  unfold pPred
  nlinarith [hradix, hn_ge_two]

private lemma succ_boundary_parity
    (radix : Int) (precision : Nat)
    (hprecision : 1 < precision) :
    (Even (nNormMin radix precision) ∧ Odd (Zpower_nat radix precision - 1)) ∨
    (Odd (nNormMin radix precision) ∧ Even (Zpower_nat radix precision - 1)) := by
  unfold nNormMin Zpower_nat
  rcases Int.even_or_odd radix with hev | hod
  · left
    constructor
    · have hs : Nat.succ (precision - 2) = precision - 1 := by omega
      rw [← hs]
      exact hev.pow_of_ne_zero (Nat.succ_ne_zero _)
    · have hp : Even (radix ^ precision) := by
        have hs : Nat.succ (precision - 1) = precision := by omega
        rw [← hs]
        exact hev.pow_of_ne_zero (Nat.succ_ne_zero _)
      exact Even.sub_odd hp odd_one
  · right
    constructor
    · exact hod.pow
    · have hp : Odd (radix ^ precision) := hod.pow
      exact Odd.sub_odd hp odd_one

-- Parity behavior of successor (Coq: FevenSucProp)
noncomputable def FevenSucProp_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FevenSucProp`.

Unlike the former local lightweight statement, this theorem targets the real
boundary-aware `FSucc`.  The usual Pff section hypotheses are explicit because
the top/bottom mantissa cases use `b.vNum = radix^precision` and
`1 < precision` to transfer parity across the carry/borrow boundary. -/
theorem FevenSucProp {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜1 < precision ∧ b.vNum = Zpower_nat radix precision⌝⦄
    (pure (FevenSucProp_check (beta:=beta) b radix precision p) : Id Unit)
    ⦃⇓_ => ⌜(Fodd (beta:=beta) p →
        Feven (beta:=beta) (FSucc (beta:=beta) b radix precision p)) ∧
      (Feven (beta:=beta) p →
        Fodd (beta:=beta) (FSucc (beta:=beta) b radix precision p))⌝⦄ := by
  intro h
  rcases h with ⟨hprecision, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, FevenSucProp_check, Id.run,
    ULift.up_down]
  have hpar := succ_boundary_parity radix precision hprecision
  unfold Fodd Feven FSucc
  by_cases htop : p.Fnum = pPred b.vNum
  · simp [htop]
    have hpnum_eq : p.Fnum = Zpower_nat radix precision - 1 := by
      simpa [pPred, hvNum] using htop
    rcases hpar with hpar | hpar
    · rcases hpar with ⟨hnorm_even, hpred_odd⟩
      have hpred_odd_b : Odd (pPred b.vNum) := by
        simpa [pPred, hvNum] using hpred_odd
      constructor
      · intro _
        simpa using hnorm_even
      · intro hp_even
        exfalso
        exact (Int.not_even_iff_odd.mpr hpred_odd_b) hp_even
    · rcases hpar with ⟨hnorm_odd, hpred_even⟩
      have hpred_even_b : Even (pPred b.vNum) := by
        simpa [pPred, hvNum] using hpred_even
      constructor
      · intro hp_odd
        exfalso
        exact (Int.not_odd_iff_even.mpr hpred_even_b) hp_odd
      · intro _
        simpa using hnorm_odd
  · simp [htop]
    by_cases hlow : p.Fnum = -nNormMin radix precision
    · simp [hlow]
      by_cases hexp : p.Fexp = -b.dExp
      · simp [hexp]
      · simp [hexp]
        have hpnum_eq : p.Fnum = -nNormMin radix precision := hlow
        rcases hpar with hpar | hpar
        · rcases hpar with ⟨hnorm_even, hpred_odd⟩
          have hpred_odd_b : Odd (pPred b.vNum) := by
            simpa [pPred, hvNum] using hpred_odd
          constructor
          · intro hp_odd
            exfalso
            exact (Int.not_odd_iff_even.mpr hnorm_even) hp_odd
          · intro _
            exact hpred_odd_b
        · rcases hpar with ⟨hnorm_odd, hpred_even⟩
          have hpred_even_b : Even (pPred b.vNum) := by
            simpa [pPred, hvNum] using hpred_even
          constructor
          · intro _
            exact hpred_even_b
          · intro hp_even
            exfalso
            exact (Int.not_even_iff_odd.mpr hnorm_odd) hp_even
    · simp [hlow]

-- Parity corollaries for successor (Coq: FoddSuc / FevenSuc)
noncomputable def FoddSuc_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FoddSuc` — if a float is odd, its faithful successor is even. -/
theorem FoddSuc {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜1 < precision ∧ b.vNum = Zpower_nat radix precision ∧ Fodd (beta:=beta) p⌝⦄
    (pure (FoddSuc_check (beta:=beta) b radix precision p) : Id Unit)
    ⦃⇓_ => ⌜Feven (beta:=beta) (FSucc (beta:=beta) b radix precision p)⌝⦄ := by
  intro h
  rcases h with ⟨hprecision, hvNum, hp_odd⟩
  have hprop := FevenSucProp (beta:=beta) b radix precision p
  have hres := hprop ⟨hprecision, hvNum⟩
  simpa [wp, PostCond.noThrow, pure, FevenSucProp_check, FoddSuc_check]
    using hres.1 hp_odd

noncomputable def FevenSuc_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FevenSuc` — if a float is even, its faithful successor is odd. -/
theorem FevenSuc {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜1 < precision ∧ b.vNum = Zpower_nat radix precision ∧ Feven (beta:=beta) p⌝⦄
    (pure (FevenSuc_check (beta:=beta) b radix precision p) : Id Unit)
    ⦃⇓_ => ⌜Fodd (beta:=beta) (FSucc (beta:=beta) b radix precision p)⌝⦄ := by
  intro h
  rcases h with ⟨hprecision, hvNum, hp_even⟩
  have hprop := FevenSucProp (beta:=beta) b radix precision p
  have hres := hprop ⟨hprecision, hvNum⟩
  simpa [wp, PostCond.noThrow, pure, FevenSucProp_check, FevenSuc_check]
    using hres.2 hp_even

noncomputable def FPredSimpl1_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit := ()

/-- Coq: `FPredSimpl1`. -/
theorem FPredSimpl1 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜x.Fnum = -pPred b.vNum⌝⦄
    (pure (FPredSimpl1_check (beta:=beta) b radix precision x) : Id Unit)
    ⦃⇓_ => ⌜FPred (beta:=beta) b radix precision x =
      ⟨-nNormMin radix precision, x.Fexp + 1⟩⌝⦄ := by
  intro hx
  have hx' : x.Fnum = -pPred b.vNum := by
    simpa using hx
  simp only [wp, PostCond.noThrow, pure, FPredSimpl1_check, Id.run,
    ULift.up_down]
  simp [FPred, hx']

noncomputable def FPredSimpl2_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit := ()

/-- Coq: `FPredSimpl2`. -/
theorem FPredSimpl2 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜-pPred b.vNum ≠ nNormMin radix precision ∧
        x.Fnum = nNormMin radix precision ∧ x.Fexp ≠ -b.dExp⌝⦄
    (pure (FPredSimpl2_check (beta:=beta) b radix precision x) : Id Unit)
    ⦃⇓_ => ⌜FPred (beta:=beta) b radix precision x =
      ⟨pPred b.vNum, x.Fexp - 1⟩⌝⦄ := by
  intro ⟨hdisjoint, hnum, hexp⟩
  simp only [wp, PostCond.noThrow, pure, FPredSimpl2_check, Id.run,
    ULift.up_down]
  have hnot_pred : x.Fnum ≠ -pPred b.vNum := by
    intro hpred
    exact hdisjoint (hpred.symm.trans hnum)
  have hnot_boundary : nNormMin radix precision ≠ -pPred b.vNum := by
    intro h
    exact hdisjoint h.symm
  simp [FPred, hnot_pred, hnot_boundary, hnum, hexp]

noncomputable def FPredSimpl3_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat) : Unit := ()

/-- Coq: `FPredSimpl3`. -/
theorem FPredSimpl3 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat) :
    ⦃⌜-pPred b.vNum ≠ nNormMin radix precision⌝⦄
    (pure (FPredSimpl3_check (beta:=beta) b radix precision) : Id Unit)
    ⦃⇓_ => ⌜FPred (beta:=beta) b radix precision
      (⟨nNormMin radix precision, -b.dExp⟩ :
        FloatSpec.Core.Defs.FlocqFloat beta) =
      ⟨nNormMin radix precision - 1, -b.dExp⟩⌝⦄ := by
  intro hneq
  simp only [wp, PostCond.noThrow, pure, FPredSimpl3_check, Id.run,
    ULift.up_down]
  simp [FPred, hneq.symm]

noncomputable def FPredSimpl4_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit := ()

/-- Coq: `FPredSimpl4`. -/
theorem FPredSimpl4 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜x.Fnum ≠ -pPred b.vNum ∧ x.Fnum ≠ nNormMin radix precision⌝⦄
    (pure (FPredSimpl4_check (beta:=beta) b radix precision x) : Id Unit)
    ⦃⇓_ => ⌜FPred (beta:=beta) b radix precision x =
      ⟨x.Fnum - 1, x.Fexp⟩⌝⦄ := by
  intro ⟨hpred, hnorm⟩
  simp only [wp, PostCond.noThrow, pure, FPredSimpl4_check, Id.run,
    ULift.up_down]
  simp [FPred, hpred, hnorm]

noncomputable def FPredFopFSucc_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit := ()

/-- Coq: `FPredFopFSucc` — predecessor is successor under float negation. -/
theorem FPredFopFSucc {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (FPredFopFSucc_check (beta:=beta) b radix precision x) : Id Unit)
    ⦃⇓_ => ⌜FPred (beta:=beta) b radix precision x =
      Fopp (beta:=beta) (FSucc (beta:=beta) b radix precision
        (Fopp (beta:=beta) x))⌝⦄ := by
  intro hctx
  rcases hctx with ⟨hradix, hprecision, hvnum⟩
  have hradix_pos : 0 < radix := by omega
  have hvnum_gt_one : (1 : Int) < b.vNum := by
    rw [hvnum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecision
  have hnm_pos : 0 < nNormMin radix precision := by
    unfold nNormMin
    exact pow_pos hradix_pos _
  have hpPred_pos : 0 < pPred b.vNum := by
    unfold pPred
    omega
  have hneg_norm_ne_pred : -nNormMin radix precision ≠ pPred b.vNum := by
    omega
  simp only [wp, PostCond.noThrow, pure, FPredFopFSucc_check, Id.run,
    ULift.up_down]
  unfold FPred FSucc Fopp FloatSpec.Calc.Operations.Fopp
  by_cases hneg_top : x.Fnum = -pPred b.vNum
  · simp [hneg_top]
  · simp [hneg_top]
    by_cases hnorm : x.Fnum = nNormMin radix precision
    · simp [hnorm]
      by_cases hexp : x.Fexp = -b.dExp
      · simp [hexp]
        have hdual_not_top : -nNormMin radix precision ≠ pPred b.vNum :=
          hneg_norm_ne_pred
        simp [hdual_not_top]
        omega
      · simp [hexp, hneg_norm_ne_pred]
    · simp [hnorm]
      have hnot_dual_top : -x.Fnum ≠ pPred b.vNum := by
        intro h
        exact hneg_top (by omega)
      have hnot_dual_norm : -x.Fnum ≠ -nNormMin radix precision := by
        intro h
        exact hnorm (by omega)
      simp [hnot_dual_top, hnot_dual_norm]
      cases x <;> simp at * <;> omega

/-- Coq: `FPredDiff1` — away from the positive minimal-normal boundary,
the predecessor step has the value of `Float 1 (Fexp x)`. -/
theorem FPredDiff1 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    beta = radix →
    1 < radix →
    precision ≠ 0 →
    b.vNum = Zpower_nat radix precision →
    x.Fnum ≠ nNormMin radix precision →
    FtoR (Fminus (beta:=beta) x (FPred (beta:=beta) b radix precision x)) =
      FtoR (⟨1, x.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat beta) := by
  intro hbeta hradix hprecision hvNum hnot_min
  subst beta
  have hminus :
      FtoR
          (Fminus (beta:=radix) x (FPred (beta:=radix) b radix precision x)) =
        FtoR x - FtoR (FPred (beta:=radix) b radix precision x) := by
    have h := FloatSpec.Calc.Operations.F2R_minus (beta:=radix)
      x (FPred (beta:=radix) b radix precision x)
    simpa [FtoR, Fminus] using h hradix
  rw [hminus]
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (by omega : (0 : Int) < radix)
  have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
  have hnorm_mul :
      (nNormMin radix precision : ℝ) * (radix : ℝ) = (b.vNum : ℝ) :=
    nNormMin_mul_radix_cast radix b precision hprecision hvNum
  unfold FPred
  by_cases htop : x.Fnum = -pPred b.vNum
  · simp only [htop, ↓reduceIte, FtoR, _root_.F2R, FloatSpec.Core.Defs.F2R,
      FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp]
    have hpow :
        (radix : ℝ) ^ (x.Fexp + 1) =
          (radix : ℝ) ^ x.Fexp * (radix : ℝ) := by
      rw [zpow_add₀ hradix_ne, zpow_one]
    have hpPred_neg : ((-pPred b.vNum : Int) : ℝ) = -((b.vNum : ℝ) - 1) := by
      simp [pPred]
    rw [hpow, hpPred_neg, Int.cast_neg]
    calc
      -((b.vNum : ℝ) - 1) * (radix : ℝ) ^ x.Fexp -
          (-(nNormMin radix precision : ℝ)) *
            ((radix : ℝ) ^ x.Fexp * (radix : ℝ))
          =
            ((-(b.vNum : ℝ) + 1) +
                (nNormMin radix precision : ℝ) * (radix : ℝ)) *
              (radix : ℝ) ^ x.Fexp := by ring
      _ = ((-(b.vNum : ℝ) + 1) + (b.vNum : ℝ)) *
            (radix : ℝ) ^ x.Fexp := by rw [hnorm_mul]
      _ = ((1 : Int) : ℝ) * (radix : ℝ) ^ x.Fexp := by norm_num
  · simp only [htop, ↓reduceIte]
    simp only [hnot_min, ↓reduceIte, FtoR, _root_.F2R,
      FloatSpec.Core.Defs.F2R, FloatSpec.Core.Defs.FlocqFloat.Fnum,
      FloatSpec.Core.Defs.FlocqFloat.Fexp, Int.cast_sub, Int.cast_one]
    ring

/-- Coq: `FPredDiff2` — at the positive minimal-normal boundary with the
minimum exponent, the predecessor step is one current-exponent ulp. -/
theorem FPredDiff2 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    beta = radix →
    1 < radix →
    precision ≠ 0 →
    b.vNum = Zpower_nat radix precision →
    x.Fnum = nNormMin radix precision →
    x.Fexp = -b.dExp →
    FtoR (Fminus (beta:=beta) x (FPred (beta:=beta) b radix precision x)) =
      FtoR (⟨1, x.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat beta) := by
  intro hbeta hradix hprecision hvNum hnum hexp
  subst beta
  have hdisjoint : -pPred b.vNum ≠ nNormMin radix precision := by
    intro h
    exact pPred_ne_neg_nNormMin radix b precision hradix hprecision hvNum (by omega)
  have hminus :
      FtoR
          (Fminus (beta:=radix) x (FPred (beta:=radix) b radix precision x)) =
        FtoR x - FtoR (FPred (beta:=radix) b radix precision x) := by
    have h := FloatSpec.Calc.Operations.F2R_minus (beta:=radix)
      x (FPred (beta:=radix) b radix precision x)
    simpa [FtoR, Fminus] using h hradix
  rw [hminus]
  unfold FPred
  have hnot_top : x.Fnum ≠ -pPred b.vNum := by
    intro htop
    exact hdisjoint (htop.symm.trans hnum)
  have hnot_top' : nNormMin radix precision ≠ -pPred b.vNum := by
    intro htop
    exact hdisjoint htop.symm
  simp only [hnot_top, hnot_top', hnum, hexp, ↓reduceIte, FtoR, _root_.F2R,
    FloatSpec.Core.Defs.F2R, FloatSpec.Core.Defs.FlocqFloat.Fnum,
    FloatSpec.Core.Defs.FlocqFloat.Fexp, Int.cast_sub, Int.cast_one]
  ring

/-- Coq: `FPredDiff3` — crossing the positive minimal-normal boundary away
from the minimum exponent yields one ulp at the predecessor exponent. -/
theorem FPredDiff3 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    beta = radix →
    1 < radix →
    precision ≠ 0 →
    b.vNum = Zpower_nat radix precision →
    x.Fnum = nNormMin radix precision →
    x.Fexp ≠ -b.dExp →
    FtoR (Fminus (beta:=beta) x (FPred (beta:=beta) b radix precision x)) =
      FtoR (⟨1, x.Fexp - 1⟩ : FloatSpec.Core.Defs.FlocqFloat beta) := by
  intro hbeta hradix hprecision hvNum hnum hexp
  subst beta
  have hdisjoint : -pPred b.vNum ≠ nNormMin radix precision := by
    intro h
    exact pPred_ne_neg_nNormMin radix b precision hradix hprecision hvNum (by omega)
  have hminus :
      FtoR
          (Fminus (beta:=radix) x (FPred (beta:=radix) b radix precision x)) =
        FtoR x - FtoR (FPred (beta:=radix) b radix precision x) := by
    have h := FloatSpec.Calc.Operations.F2R_minus (beta:=radix)
      x (FPred (beta:=radix) b radix precision x)
    simpa [FtoR, Fminus] using h hradix
  rw [hminus]
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (by omega : (0 : Int) < radix)
  have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
  have hnorm_mul :
      (nNormMin radix precision : ℝ) * (radix : ℝ) = (b.vNum : ℝ) :=
    nNormMin_mul_radix_cast radix b precision hprecision hvNum
  have hpow :
      (radix : ℝ) ^ x.Fexp =
        (radix : ℝ) ^ (x.Fexp - 1) * (radix : ℝ) := by
    calc
      (radix : ℝ) ^ x.Fexp =
          (radix : ℝ) ^ ((x.Fexp - 1) + 1) := by
            congr 1
            omega
      _ = (radix : ℝ) ^ (x.Fexp - 1) * (radix : ℝ) := by
            rw [zpow_add₀ hradix_ne, zpow_one]
  unfold FPred
  have hnot_top : x.Fnum ≠ -pPred b.vNum := by
    intro htop
    exact hdisjoint (htop.symm.trans hnum)
  have hnot_top' : nNormMin radix precision ≠ -pPred b.vNum := by
    intro htop
    exact hdisjoint htop.symm
  simp only [hnot_top, hnot_top', ↓reduceIte, hnum, hexp, ite_false, FtoR,
    _root_.F2R, FloatSpec.Core.Defs.F2R,
    FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp]
  have hpPred : (pPred b.vNum : ℝ) = (b.vNum : ℝ) - 1 := by
    simp [pPred]
  rw [hpow, hpPred]
  calc
    (nNormMin radix precision : ℝ) *
        ((radix : ℝ) ^ (x.Fexp - 1) * (radix : ℝ)) -
        ((b.vNum : ℝ) - 1) * (radix : ℝ) ^ (x.Fexp - 1)
        =
          ((nNormMin radix precision : ℝ) * (radix : ℝ) -
              ((b.vNum : ℝ) - 1)) *
            (radix : ℝ) ^ (x.Fexp - 1) := by ring
    _ = ((b.vNum : ℝ) - ((b.vNum : ℝ) - 1)) *
          (radix : ℝ) ^ (x.Fexp - 1) := by rw [hnorm_mul]
    _ = ((1 : Int) : ℝ) * (radix : ℝ) ^ (x.Fexp - 1) := by norm_num

noncomputable def FSuccFopFPred_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit := ()

/-- Dual of `FPredFopFSucc`: successor is predecessor under float negation. -/
theorem FSuccFopFPred {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (FSuccFopFPred_check (beta:=beta) b radix precision x) : Id Unit)
    ⦃⇓_ => ⌜FSucc (beta:=beta) b radix precision x =
      Fopp (beta:=beta) (FPred (beta:=beta) b radix precision
        (Fopp (beta:=beta) x))⌝⦄ := by
  intro hctx
  simp only [wp, PostCond.noThrow, pure, FSuccFopFPred_check, Id.run,
    ULift.up_down]
  have hpred := FPredFopFSucc (beta:=beta) b radix precision
    (Fopp (beta:=beta) x)
  have hpred' :
      FPred (beta:=beta) b radix precision (Fopp (beta:=beta) x) =
        Fopp (beta:=beta) (FSucc (beta:=beta) b radix precision x) := by
    have hff : Fopp (beta:=beta) (Fopp (beta:=beta) x) = x := by
      cases x
      simp [Fopp, FloatSpec.Calc.Operations.Fopp]
    simpa only [wp, PostCond.noThrow, pure, FPredFopFSucc_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Id.run,
      ULift.up, ULift.down, ULift.up_down, hff, Int.cast_ofNat] using
      hpred hctx
  rw [hpred']
  cases FSucc (beta:=beta) b radix precision x
  simp [Fopp, FloatSpec.Calc.Operations.Fopp]

noncomputable def FBoundedSuc_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit := ()

/-- Coq: `FBoundedSuc` — the normalized successor of a bounded float remains
bounded under the precision/radix section assumptions. -/
theorem FBoundedSuc {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b x ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (FBoundedSuc_check (beta:=beta) b radix precision x) : Id Unit)
    ⦃⇓_ => ⌜Fbounded (beta:=beta) b
      (FSucc (beta:=beta) b radix precision x)⌝⦄ := by
  intro h
  rcases h with ⟨hbounded, hradix, hprecision, hvnum⟩
  rcases hbounded with ⟨hnum, hexp⟩
  simp only [wp, PostCond.noThrow, pure, FBoundedSuc_check, Id.run,
    ULift.up_down]
  unfold FSucc
  have hradix_pos : 0 < radix := by omega
  have hvnum_pos : 0 < b.vNum := by
    rw [hvnum, Zpower_nat]
    exact pow_pos hradix_pos precision
  have hvnum_gt_one : (1 : Int) < b.vNum := by
    rw [hvnum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecision
  by_cases htop : x.Fnum = pPred b.vNum
  · simp [htop]
    constructor
    · rw [hvnum, Zpower_nat, nNormMin]
      have hprec_pos : 0 < precision := Nat.pos_of_ne_zero hprecision
      have hpred_lt : precision - 1 < precision := Nat.sub_one_lt hprecision
      have hpow_pos : 0 < radix ^ (precision - 1) := pow_pos hradix_pos _
      rw [abs_of_pos hpow_pos]
      exact pow_lt_pow_right₀ hradix hpred_lt
    · change -b.dExp ≤ x.Fexp + 1
      omega
  · simp [htop]
    by_cases hlow : x.Fnum = -nNormMin radix precision
    · simp [hlow]
      by_cases hexp_min : x.Fexp = -b.dExp
      · simp [hexp_min]
        constructor
        · have hnm_pos : 0 < nNormMin radix precision := by
            unfold nNormMin
            exact pow_pos hradix_pos _
          have hle_abs : |-nNormMin radix precision + 1| ≤ nNormMin radix precision := by
            rw [show -nNormMin radix precision + 1 = -(nNormMin radix precision - 1) by ring]
            rw [abs_neg]
            have hsub_nonneg : 0 ≤ nNormMin radix precision - 1 := by omega
            rw [abs_of_nonneg hsub_nonneg]
            omega
          rw [hlow] at hnum
          have hnm_abs : |nNormMin radix precision| = nNormMin radix precision := by
            exact abs_of_nonneg (le_of_lt hnm_pos)
          rw [abs_neg, hnm_abs] at hnum
          exact lt_of_le_of_lt hle_abs hnum
        · simp
      · simp [hexp_min]
        constructor
        · have hpPred_nonneg : 0 ≤ pPred b.vNum := by
            unfold pPred
            omega
          rw [abs_neg, abs_of_nonneg hpPred_nonneg]
          unfold pPred
          omega
        · have hstrict : -b.dExp < x.Fexp := by omega
          change -b.dExp ≤ x.Fexp - 1
          omega
    · simp [hlow]
      constructor
      · have hx_lt_upper : x.Fnum < b.vNum - 1 := by
          have hx_lt_vnum : x.Fnum < b.vNum :=
            lt_of_le_of_lt (le_abs_self x.Fnum) hnum
          unfold pPred at htop
          omega
        have hx_lower : -b.vNum < x.Fnum := by
          have hneg : -b.vNum < -|x.Fnum| := by omega
          have hle : -|x.Fnum| ≤ x.Fnum := by
            exact neg_abs_le x.Fnum
          exact lt_of_lt_of_le hneg hle
        have habs_lt : |x.Fnum + 1| < b.vNum := by
          rw [abs_lt]
          constructor <;> omega
        exact habs_lt
      · exact hexp

noncomputable def FBoundedPred_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit := ()

/-- Coq: `FBoundedPred` — the normalized predecessor of a bounded float remains
bounded under the precision/radix section assumptions. -/
theorem FBoundedPred {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b x ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (FBoundedPred_check (beta:=beta) b radix precision x) : Id Unit)
    ⦃⇓_ => ⌜Fbounded (beta:=beta) b
      (FPred (beta:=beta) b radix precision x)⌝⦄ := by
  intro h
  rcases h with ⟨hbounded, hradix, hprecision, hvnum⟩
  simp only [wp, PostCond.noThrow, pure, FBoundedPred_check, Id.run,
    ULift.up_down]
  have hopp_bounded : Fbounded (beta:=beta) b (Fopp (beta:=beta) x) := by
    simpa [Fbounded, Fopp, FloatSpec.Calc.Operations.Fopp, abs_neg] using hbounded
  have hsucc_bounded :
      Fbounded (beta:=beta) b
        (FSucc (beta:=beta) b radix precision (Fopp (beta:=beta) x)) := by
    have h := FBoundedSuc (beta:=beta) b radix precision (Fopp (beta:=beta) x)
    simpa only [wp, PostCond.noThrow, pure, FBoundedSuc_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Id.run,
      ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
      h ⟨hopp_bounded, hradix, hprecision, hvnum⟩
  have hpred_eq :
      FPred (beta:=beta) b radix precision x =
        Fopp (beta:=beta)
          (FSucc (beta:=beta) b radix precision (Fopp (beta:=beta) x)) := by
    have h := FPredFopFSucc (beta:=beta) b radix precision x
    simpa only [wp, PostCond.noThrow, pure, FPredFopFSucc_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Id.run,
      ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
      h ⟨hradix, hprecision, hvnum⟩
  rw [hpred_eq]
  simpa [Fbounded, Fopp, FloatSpec.Calc.Operations.Fopp, abs_neg] using hsucc_bounded

noncomputable def FPredSuc_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit := ()

/-- Coq: `FPredSuc` — predecessor cancels successor on canonical floats. -/
theorem FPredSuc {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fcanonic (beta:=beta) radix b x ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (FPredSuc_check (beta:=beta) b radix precision x) : Id Unit)
    ⦃⇓_ => ⌜FPred (beta:=beta) b radix precision
        (FSucc (beta:=beta) b radix precision x) = x⌝⦄ := by
  intro h
  rcases h with ⟨hcan, hradix, hprecision, hvnum⟩
  have hbounded : Fbounded (beta:=beta) b x := by
    have hbound := FcanonicBound (beta:=beta) radix b x
    simpa only [wp, PostCond.noThrow, pure, FcanonicBound_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Id.run,
      ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
      hbound hcan
  rcases hbounded with ⟨hnum, hexp⟩
  simp only [wp, PostCond.noThrow, pure, FPredSuc_check, Id.run,
    ULift.up_down]
  have hradix_pos : 0 < radix := by omega
  have hvnum_pos : 0 < b.vNum := by
    rw [hvnum, Zpower_nat]
    exact pow_pos hradix_pos precision
  have hvnum_gt_one : (1 : Int) < b.vNum := by
    rw [hvnum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecision
  have hnm_pos : 0 < nNormMin radix precision := by
    unfold nNormMin
    exact pow_pos hradix_pos _
  have hpPred_pos : 0 < pPred b.vNum := by
    unfold pPred
    exact sub_pos.mpr hvnum_gt_one
  unfold FSucc FPred
  by_cases htop : x.Fnum = pPred b.vNum
  · simp [htop]
    have hnm_ne_neg_pred : nNormMin radix precision ≠ -pPred b.vNum := by
      omega
    have hexp_succ_ne_min : x.Fexp + 1 ≠ -b.dExp := by
      omega
    simp [hnm_ne_neg_pred, hexp_succ_ne_min]
    cases x <;> simp at * <;> omega
  · simp [htop]
    by_cases hlow : x.Fnum = -nNormMin radix precision
    · simp [hlow]
      by_cases hexp_min : x.Fexp = -b.dExp
      · simp [hexp_min]
        have hnot_neg_pred : -nNormMin radix precision + 1 ≠ -pPred b.vNum := by
          intro heq
          have hb_eq : b.vNum = nNormMin radix precision := by
            unfold pPred at heq
            omega
          have hnm_abs : |nNormMin radix precision| = nNormMin radix precision :=
            abs_of_nonneg (le_of_lt hnm_pos)
          rw [hlow, abs_neg, hnm_abs, hb_eq] at hnum
          omega
        have hnot_norm : -nNormMin radix precision + 1 ≠ nNormMin radix precision := by
          omega
        simp [hnot_neg_pred, hnot_norm]
        cases x <;> simp at * <;> omega
      · simp [hexp_min]
        cases x <;> simp at * <;> omega
    · simp [hlow]
      have hx_succ_not_neg_pred : x.Fnum + 1 ≠ -pPred b.vNum := by
        intro heq
        have hx_abs_ge : b.vNum ≤ |x.Fnum| := by
          have hx_eq : x.Fnum = -b.vNum := by
            unfold pPred at heq
            omega
          rw [hx_eq, abs_neg]
          exact le_abs_self b.vNum
        omega
      by_cases hnorm_succ : x.Fnum + 1 = nNormMin radix precision
      · simp [hx_succ_not_neg_pred, hnorm_succ]
        by_cases hexp_min : x.Fexp = -b.dExp
        · simp [hexp_min]
          have hx_eq : x.Fnum = nNormMin radix precision - 1 := by omega
          cases x
          simp only [FloatSpec.Core.Defs.FlocqFloat.Fnum,
            FloatSpec.Core.Defs.FlocqFloat.Fexp] at hx_eq hexp_min ⊢
          have hnm_ne_neg_pred : nNormMin radix precision ≠ -pPred b.vNum := by
            omega
          simp [hnm_ne_neg_pred]
          exact hexp_min.symm
        · have hcase_false : False := by
            rcases hcan with hnormal | hsubnormal
            · rcases hnormal with ⟨_, hnormal_num⟩
              have hx_nonneg : 0 ≤ x.Fnum := by omega
              have hx_abs : |x.Fnum| = x.Fnum := abs_of_nonneg hx_nonneg
              have hradix_mul_abs :
                  |radix * x.Fnum| = radix * x.Fnum := by
                exact abs_of_nonneg (mul_nonneg (le_of_lt hradix_pos) hx_nonneg)
              rw [hradix_mul_abs] at hnormal_num
              have hx_eq : x.Fnum = nNormMin radix precision - 1 := by omega
              rw [hx_eq, hvnum, Zpower_nat, nNormMin] at hnormal_num
              have hpow_succ :
                  radix ^ precision = radix * radix ^ (precision - 1) := by
                have hsucc : Nat.succ (precision - 1) = precision := by omega
                calc
                  radix ^ precision = radix ^ Nat.succ (precision - 1) := by rw [hsucc]
                  _ = radix ^ (precision - 1) * radix := by rw [pow_succ]
                  _ = radix * radix ^ (precision - 1) := by ring
              rw [hpow_succ] at hnormal_num
              nlinarith [hradix_pos, hnm_pos]
            · exact hexp_min hsubnormal.2.1
          exact False.elim hcase_false
      · simp [hx_succ_not_neg_pred, hnorm_succ]

noncomputable def FSucPred_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit := ()

/-- Coq: `FSucPred` — successor cancels predecessor on canonical floats. -/
theorem FSucPred {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fcanonic (beta:=beta) radix b x ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (FSucPred_check (beta:=beta) b radix precision x) : Id Unit)
    ⦃⇓_ => ⌜FSucc (beta:=beta) b radix precision
        (FPred (beta:=beta) b radix precision x) = x⌝⦄ := by
  intro h
  rcases h with ⟨hcan, hradix, hprecision, hvnum⟩
  simp only [wp, PostCond.noThrow, pure, FSucPred_check, Id.run,
    ULift.up_down]
  have hctx : 1 < radix ∧ precision ≠ 0 ∧
      b.vNum = Zpower_nat radix precision := ⟨hradix, hprecision, hvnum⟩
  have hopp_can :
      Fcanonic (beta:=beta) radix b (Fopp (beta:=beta) x) := by
    have hcan_opp := FcanonicFopp (beta:=beta) radix b x
    simpa only [wp, PostCond.noThrow, pure, FcanonicFopp_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Id.run, ULift.up, ULift.down]
      using hcan_opp hcan
  have hpred_succ_opp :
      FPred (beta:=beta) b radix precision
          (FSucc (beta:=beta) b radix precision (Fopp (beta:=beta) x)) =
        Fopp (beta:=beta) x := by
    have hps := FPredSuc (beta:=beta) b radix precision (Fopp (beta:=beta) x)
    simpa only [wp, PostCond.noThrow, pure, FPredSuc_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Id.run,
      ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
      hps ⟨hopp_can, hradix, hprecision, hvnum⟩
  have hpred_eq :
      FPred (beta:=beta) b radix precision x =
        Fopp (beta:=beta)
          (FSucc (beta:=beta) b radix precision (Fopp (beta:=beta) x)) := by
    have hpf := FPredFopFSucc (beta:=beta) b radix precision x
    simpa only [wp, PostCond.noThrow, pure, FPredFopFSucc_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Id.run,
      ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using hpf hctx
  have hfopp_pred :
      Fopp (beta:=beta) (FPred (beta:=beta) b radix precision x) =
        FSucc (beta:=beta) b radix precision (Fopp (beta:=beta) x) := by
    rw [hpred_eq]
    cases FSucc (beta:=beta) b radix precision (Fopp (beta:=beta) x)
    simp [Fopp, FloatSpec.Calc.Operations.Fopp]
  have hsucc_pred :
      FSucc (beta:=beta) b radix precision
          (FPred (beta:=beta) b radix precision x) =
        Fopp (beta:=beta)
          (FPred (beta:=beta) b radix precision
            (Fopp (beta:=beta)
              (FPred (beta:=beta) b radix precision x))) := by
    have hsf := FSuccFopFPred (beta:=beta) b radix precision
      (FPred (beta:=beta) b radix precision x)
    simpa only [wp, PostCond.noThrow, pure, FSuccFopFPred_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Id.run,
      ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using hsf hctx
  rw [hsucc_pred, hfopp_pred, hpred_succ_opp]
  cases x
  simp [Fopp, FloatSpec.Calc.Operations.Fopp]

noncomputable def FSuccCanonic_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit := ()

/-- Coq: `FSuccCanonic` — successor preserves canonicity under the local
precision/radix section assumptions.  Upstream statement:
`forall a : float, Fcanonic radix b a -> Fcanonic radix b (FSucc a)`. -/
theorem FSuccCanonic {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fcanonic (beta:=beta) radix b x ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (FSuccCanonic_check (beta:=beta) b radix precision x) : Id Unit)
    ⦃⇓_ => ⌜Fcanonic (beta:=beta) radix b
      (FSucc (beta:=beta) b radix precision x)⌝⦄ := by
  intro h
  rcases h with ⟨hcan, hradix, hprecision, hvnum⟩
  have hbounded_succ : Fbounded (beta:=beta) b
      (FSucc (beta:=beta) b radix precision x) := by
    have hbound := FBoundedSuc (beta:=beta) b radix precision x
    have hx_bound : Fbounded (beta:=beta) b x := by
      have hcan_bound := FcanonicBound (beta:=beta) radix b x
      simpa only [wp, PostCond.noThrow, pure, FcanonicBound_check,
        PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Id.run,
        ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
        hcan_bound hcan
    simpa only [wp, PostCond.noThrow, pure, FBoundedSuc_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Id.run,
      ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
      hbound ⟨hx_bound, hradix, hprecision, hvnum⟩
  simp only [wp, PostCond.noThrow, pure, FSuccCanonic_check, Id.run,
    ULift.up_down]
  have hradix_pos : 0 < radix := by omega
  have hnm_pos : 0 < nNormMin radix precision := by
    unfold nNormMin
    exact pow_pos hradix_pos _
  have hpow_succ :
      radix ^ precision = radix * radix ^ (precision - 1) := by
    have hsucc : Nat.succ (precision - 1) = precision := by omega
    calc
      radix ^ precision = radix ^ Nat.succ (precision - 1) := by rw [hsucc]
      _ = radix ^ (precision - 1) * radix := by rw [pow_succ]
      _ = radix * radix ^ (precision - 1) := by ring
  have hvnum_mul_norm : b.vNum = radix * nNormMin radix precision := by
    rw [hvnum, Zpower_nat, nNormMin, hpow_succ]
  have hvnum_pos : 0 < b.vNum := by
    rw [hvnum_mul_norm]
    exact mul_pos hradix_pos hnm_pos
  have hvnum_gt_one : (1 : Int) < b.vNum := by
    rw [hvnum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecision
  have hneg_norm_ne_pred : -nNormMin radix precision ≠ pPred b.vNum := by
    unfold pPred
    omega
  unfold FSucc
  by_cases htop : x.Fnum = pPred b.vNum
  · simp [htop]
    left
    constructor
    · simpa [FSucc, htop] using hbounded_succ
    · rw [hvnum_mul_norm]
      have hmul_pos : 0 < radix * nNormMin radix precision :=
        mul_pos hradix_pos hnm_pos
      rw [abs_of_pos hmul_pos]
  · simp [htop]
    by_cases hlow : x.Fnum = -nNormMin radix precision
    · simp [hlow]
      by_cases hexp_min : x.Fexp = -b.dExp
      · simp [hexp_min]
        right
        constructor
        · simpa [FSucc, htop, hlow, hexp_min, hneg_norm_ne_pred] using hbounded_succ
        · constructor
          · rfl
          · rw [hvnum_mul_norm]
            have hnum_nonpos : -nNormMin radix precision + 1 ≤ 0 := by omega
            have hradix_mul_nonpos :
                radix * (-nNormMin radix precision + 1) ≤ 0 := by
              exact mul_nonpos_of_nonneg_of_nonpos (le_of_lt hradix_pos) hnum_nonpos
            rw [abs_of_nonpos hradix_mul_nonpos]
            nlinarith [hradix_pos, hnm_pos]
      · simp [hexp_min]
        left
        constructor
        · simpa [FSucc, htop, hlow, hexp_min, hneg_norm_ne_pred] using hbounded_succ
        · rw [abs_mul, abs_of_pos hradix_pos]
          have hpPred_nonneg : 0 ≤ pPred b.vNum := by
            unfold pPred
            omega
          rw [abs_neg, abs_of_nonneg hpPred_nonneg]
          unfold pPred
          nlinarith [hradix, hvnum_gt_one]
    · simp [hlow]
      by_cases hnorm_succ : x.Fnum + 1 = nNormMin radix precision
      · simp [hnorm_succ]
        left
        constructor
        · simpa [FSucc, htop, hlow, hnorm_succ] using hbounded_succ
        · rw [hvnum_mul_norm]
          have hmul_pos : 0 < radix * nNormMin radix precision :=
            mul_pos hradix_pos hnm_pos
          rw [abs_of_pos hmul_pos]
      · rcases hcan with hnormal | hsubnormal
        · left
          constructor
          · simpa [FSucc, htop, hlow, hnorm_succ] using hbounded_succ
          · rcases hnormal with ⟨_, hnormal_num⟩
            have hscale_abs :
                |radix * x.Fnum| = radix * |x.Fnum| := by
              rw [abs_mul, abs_of_pos hradix_pos]
            rw [hscale_abs, hvnum_mul_norm] at hnormal_num
            have hnorm_le_abs : nNormMin radix precision ≤ |x.Fnum| := by
              exact Int.le_of_mul_le_mul_left hnormal_num hradix_pos
            have hnorm_le_abs_succ :
                nNormMin radix precision ≤ |x.Fnum + 1| := by
              rcases le_or_gt 0 x.Fnum with hx_nonneg | hx_neg
              · have hx_abs : |x.Fnum| = x.Fnum := abs_of_nonneg hx_nonneg
                rw [hx_abs] at hnorm_le_abs
                have hx_succ_nonneg : 0 ≤ x.Fnum + 1 := by omega
                rw [abs_of_nonneg hx_succ_nonneg]
                omega
              · have hx_abs : |x.Fnum| = -x.Fnum := abs_of_neg hx_neg
                rw [hx_abs] at hnorm_le_abs
                have hx_ne_neg_norm : x.Fnum ≠ -nNormMin radix precision := hlow
                have hx_succ_nonpos : x.Fnum + 1 ≤ 0 := by omega
                rw [abs_of_nonpos hx_succ_nonpos]
                omega
            rw [abs_mul, abs_of_pos hradix_pos, hvnum_mul_norm]
            exact mul_le_mul_of_nonneg_left hnorm_le_abs_succ (le_of_lt hradix_pos)
        · right
          constructor
          · simpa [FSucc, htop, hlow, hnorm_succ] using hbounded_succ
          · rcases hsubnormal with ⟨_, hexp, hsub_num⟩
            constructor
            · exact hexp
            · have hscale_abs :
                |radix * x.Fnum| = radix * |x.Fnum| := by
                rw [abs_mul, abs_of_pos hradix_pos]
              rw [hscale_abs, hvnum_mul_norm] at hsub_num
              have habs_lt_norm : |x.Fnum| < nNormMin radix precision := by
                exact Int.lt_of_mul_lt_mul_left hsub_num (le_of_lt hradix_pos)
              have habs_succ_lt_norm :
                  |x.Fnum + 1| < nNormMin radix precision := by
                rcases le_or_gt 0 x.Fnum with hx_nonneg | hx_neg
                · have hx_abs : |x.Fnum| = x.Fnum := abs_of_nonneg hx_nonneg
                  rw [hx_abs] at habs_lt_norm
                  have hx_succ_nonneg : 0 ≤ x.Fnum + 1 := by omega
                  rw [abs_of_nonneg hx_succ_nonneg]
                  omega
                · have hx_abs : |x.Fnum| = -x.Fnum := abs_of_neg hx_neg
                  rw [hx_abs] at habs_lt_norm
                  by_cases hx_succ_nonneg : 0 ≤ x.Fnum + 1
                  · rw [abs_of_nonneg hx_succ_nonneg]
                    omega
                  · have hx_succ_neg : x.Fnum + 1 < 0 := by omega
                    rw [abs_of_neg hx_succ_neg]
                    omega
              rw [abs_mul, abs_of_pos hradix_pos, hvnum_mul_norm]
              exact mul_lt_mul_of_pos_left habs_succ_lt_norm hradix_pos

/-- Coq: `FSuccNormNegNotNormMin` — the successor of a nonpositive normal
float is normal unless the input is the negative minimal-normal boundary. -/
theorem FSuccNormNegNotNormMin {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    beta = radix →
    1 < radix →
    0 < precision →
    b.vNum = Zpower_nat radix precision →
    FtoR x ≤ 0 →
    Fnormal (beta:=beta) radix b x →
    x ≠ (⟨-nNormMin radix precision, -b.dExp⟩ :
      FloatSpec.Core.Defs.FlocqFloat beta) →
    Fnormal (beta:=beta) radix b
      (FSucc (beta:=beta) b radix precision x) := by
  intro hbeta hradix hprecision hvNum hx_nonpos hx_normal hx_not_boundary
  subst beta
  have hprecision_ne : precision ≠ 0 := by omega
  have hx_bounded : Fbounded (beta:=radix) b x := hx_normal.1
  have hx_num_nonpos : x.Fnum ≤ 0 := by
    exact FloatSpec.Core.Float_prop.le_0_F2R (beta:=radix) x hradix
      (by simpa [FtoR] using hx_nonpos)
  have hsucc_bounded : Fbounded (beta:=radix) b
      (FSucc (beta:=radix) b radix precision x) := by
    have h := FBoundedSuc (beta:=radix) b radix precision x
    simpa only [wp, PostCond.noThrow, pure, FBoundedSuc_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Id.run,
      ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
      h ⟨hx_bounded, hradix, hprecision_ne, hvNum⟩
  have hradix_pos : 0 < radix := by omega
  have hnpos : 0 < nNormMin radix precision := by
    cases precision with
    | zero => cases hprecision
    | succ p =>
        simp [nNormMin]
        exact pow_pos hradix_pos p
  have hvnum_mul_norm : b.vNum = radix * nNormMin radix precision :=
    PosNormMin radix b precision hprecision hvNum
  have hvnum_pos : 0 < b.vNum := by
    rw [hvnum_mul_norm]
    exact mul_pos hradix_pos hnpos
  have hvnum_gt_one : (1 : Int) < b.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecision_ne
  have hdisjoint : pPred b.vNum ≠ -nNormMin radix precision :=
    pPred_ne_neg_nNormMin radix b precision hradix hprecision_ne hvNum
  have hnot_neg_norm_top : -nNormMin radix precision ≠ pPred b.vNum := by
    intro h
    exact hdisjoint h.symm
  unfold FSucc
  by_cases htop : x.Fnum = pPred b.vNum
  · exfalso
    have hpPred_pos : 0 < pPred b.vNum := by
      unfold pPred
      omega
    rw [htop] at hx_num_nonpos
    omega
  · simp only [htop, ↓reduceIte]
    by_cases hlow : x.Fnum = -nNormMin radix precision
    · simp only [hlow, ↓reduceIte]
      by_cases hexp_min : x.Fexp = -b.dExp
      · exfalso
        apply hx_not_boundary
        cases x
        simp_all
      · simp only [hexp_min, ↓reduceIte]
        constructor
        · simpa [FSucc, htop, hlow, hexp_min, hnot_neg_norm_top] using
            hsucc_bounded
        · rw [abs_mul, abs_of_pos hradix_pos]
          have hpPred_nonneg : 0 ≤ pPred b.vNum := by
            unfold pPred
            omega
          rw [abs_neg, abs_of_nonneg hpPred_nonneg]
          unfold pPred
          nlinarith [hradix, hvnum_gt_one]
    · simp only [hlow, ↓reduceIte]
      constructor
      · simpa [FSucc, htop, hlow] using hsucc_bounded
      · have hx_num_ne_zero : x.Fnum ≠ 0 := by
          intro hz
          have hnormal_num := hx_normal.2
          have hvnum_le_zero : b.vNum ≤ 0 := by
            simpa [hz] using hnormal_num
          omega
        have hx_num_neg : x.Fnum < 0 := by omega
        have hscale_abs :
            |radix * x.Fnum| = radix * |x.Fnum| := by
          rw [abs_mul, abs_of_pos hradix_pos]
        have hnorm_le_abs : nNormMin radix precision ≤ |x.Fnum| := by
          have hnormal_num := hx_normal.2
          rw [hscale_abs, hvnum_mul_norm] at hnormal_num
          exact Int.le_of_mul_le_mul_left hnormal_num hradix_pos
        have hnorm_lt_neg_num : nNormMin radix precision < -x.Fnum := by
          have hx_abs : |x.Fnum| = -x.Fnum := abs_of_neg hx_num_neg
          rw [hx_abs] at hnorm_le_abs
          omega
        have hx_succ_nonpos : x.Fnum + 1 ≤ 0 := by omega
        have hnorm_le_abs_succ : nNormMin radix precision ≤ |x.Fnum + 1| := by
          rw [abs_of_nonpos hx_succ_nonpos]
          omega
        rw [abs_mul, abs_of_pos hradix_pos, hvnum_mul_norm]
        exact mul_le_mul_of_nonneg_left hnorm_le_abs_succ (le_of_lt hradix_pos)

noncomputable def FSuccNegCanonic_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit := ()

/-- Coq: `FSuccNegCanonic` — successor preserves canonicity on the
nonpositive branch.  The local all-sign `FSuccCanonic` theorem is stronger,
so this wrapper restores the exact upstream name. -/
theorem FSuccNegCanonic {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜FtoR x ≤ 0 ∧ Fcanonic (beta:=beta) radix b x ∧
        1 < radix ∧ precision ≠ 0 ∧ b.vNum = Zpower_nat radix precision⌝⦄
    (pure (FSuccNegCanonic_check (beta:=beta) b radix precision x) : Id Unit)
    ⦃⇓_ => ⌜Fcanonic (beta:=beta) radix b
      (FSucc (beta:=beta) b radix precision x)⌝⦄ := by
  intro h
  rcases h with ⟨_, hcan, hradix, hprecision, hvnum⟩
  have hsucc := FSuccCanonic (beta:=beta) b radix precision x
  simpa only [wp, PostCond.noThrow, pure, FSuccCanonic_check,
    FSuccNegCanonic_check, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
    Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
    hsucc ⟨hcan, hradix, hprecision, hvnum⟩

noncomputable def FSuccLt_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit := ()

/-- Coq: `FSuccLt` — every float is strictly below its normalized successor.

Upstream statement: `forall a : float, (a < FSucc a)%R`.  The Lean port makes
the section assumptions explicit, including the equality between the `F2R` base
and the Pff radix. -/
theorem FSuccLt {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (FSuccLt_check (beta:=beta) b radix precision x) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) x <
      _root_.F2R (beta:=beta)
        (FSucc (beta:=beta) b radix precision x)⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hradix, hprecision, hvnum⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure, FSuccLt_check, Id.run,
    PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
    ULift.up, ULift.down, ULift.up_down]
  have hradix_pos_int : (0 : Int) < radix := by omega
  have hradix_pos_real : (0 : ℝ) < (radix : ℝ) :=
    Int.cast_pos.mpr hradix_pos_int
  have hradix_ne_real : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos_real
  have hpow_succ :
      radix ^ precision = radix * radix ^ (precision - 1) := by
    have hsucc : Nat.succ (precision - 1) = precision := by omega
    calc
      radix ^ precision = radix ^ Nat.succ (precision - 1) := by rw [hsucc]
      _ = radix ^ (precision - 1) * radix := by rw [pow_succ]
      _ = radix * radix ^ (precision - 1) := by ring
  have hvnum_mul_norm : b.vNum = radix * nNormMin radix precision := by
    rw [hvnum, Zpower_nat, nNormMin, hpow_succ]
  have zpow_succ (e : Int) :
      (radix : ℝ) ^ (e + 1) = (radix : ℝ) ^ e * (radix : ℝ) := by
    simpa [mul_comm] using zpow_add_one₀ hradix_ne_real e
  have zpow_pred (e : Int) :
      (radix : ℝ) ^ e = (radix : ℝ) * (radix : ℝ) ^ (e - 1) := by
    calc
      (radix : ℝ) ^ e = (radix : ℝ) ^ ((e - 1) + 1) := by ring_nf
      _ = (radix : ℝ) * (radix : ℝ) ^ (e - 1) := by
        rw [zpow_succ]
        ring
  unfold FSucc
  by_cases htop : x.Fnum = pPred b.vNum
  · simp [htop, _root_.F2R, FloatSpec.Core.Defs.F2R]
    have hmant_lt_int : pPred b.vNum < radix * nNormMin radix precision := by
      rw [← hvnum_mul_norm]
      unfold pPred
      omega
    have hmant_lt_real :
        (pPred b.vNum : ℝ) <
          ((radix * nNormMin radix precision : Int) : ℝ) :=
      Int.cast_lt.mpr hmant_lt_int
    calc
      (pPred b.vNum : ℝ) * (radix : ℝ) ^ x.Fexp
          < ((radix * nNormMin radix precision : Int) : ℝ) *
              (radix : ℝ) ^ x.Fexp := by
            exact mul_lt_mul_of_pos_right hmant_lt_real
              (zpow_pos hradix_pos_real x.Fexp)
      _ = (nNormMin radix precision : ℝ) *
            (radix : ℝ) ^ (x.Fexp + 1) := by
            rw [zpow_succ]
            simp [Int.cast_mul]
            ring
  · simp only [htop, ite_false]
    by_cases hlow : x.Fnum = -nNormMin radix precision
    · simp only [hlow, ite_true]
      by_cases hexp_min : x.Fexp = -b.dExp
      · simp only [hexp_min, ite_true, _root_.F2R, FloatSpec.Core.Defs.F2R,
          FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp]
        have hpow_pos : 0 < (radix : ℝ) ^ (-b.dExp) :=
          zpow_pos hradix_pos_real (-b.dExp)
        have hmant : (x.Fnum : ℝ) <
            ((-nNormMin radix precision + 1 : Int) : ℝ) := by
          rw [hlow]
          exact_mod_cast
            (show -nNormMin radix precision < -nNormMin radix precision + 1 by omega)
        exact mul_lt_mul_of_pos_right hmant hpow_pos
      · simp only [hexp_min, ite_false, _root_.F2R, FloatSpec.Core.Defs.F2R,
          FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp]
        have hmant_lt_int : radix * x.Fnum < -pPred b.vNum := by
          rw [hlow, hvnum_mul_norm]
          have hmul_neg :
              radix * -nNormMin radix precision =
                -(radix * nNormMin radix precision) := by
            ring
          rw [hmul_neg]
          unfold pPred
          omega
        have hmant_lt_real :
            (radix : ℝ) * (x.Fnum : ℝ) <
              ((-pPred b.vNum : Int) : ℝ) := by
          have hcast :
              ((radix * x.Fnum : Int) : ℝ) <
                ((-pPred b.vNum : Int) : ℝ) :=
            Int.cast_lt.mpr hmant_lt_int
          simpa [Int.cast_mul] using hcast
        rw [hlow] at hmant_lt_real
        have hlt : ((-nNormMin radix precision : Int) : ℝ) *
              (radix : ℝ) ^ x.Fexp <
            ((-pPred b.vNum : Int) : ℝ) *
              (radix : ℝ) ^ (x.Fexp - 1) := by
          calc
          ((-nNormMin radix precision : Int) : ℝ) * (radix : ℝ) ^ x.Fexp
              = ((radix : ℝ) * ((-nNormMin radix precision : Int) : ℝ)) *
                  (radix : ℝ) ^ (x.Fexp - 1) := by
                rw [zpow_pred]
                ring
          _ < ((-pPred b.vNum : Int) : ℝ) *
                (radix : ℝ) ^ (x.Fexp - 1) := by
                exact mul_lt_mul_of_pos_right hmant_lt_real
                  (zpow_pos hradix_pos_real (x.Fexp - 1))
        simpa only [hlow, Int.cast_ofNat] using hlt
    · simp only [hlow, ite_false, _root_.F2R, FloatSpec.Core.Defs.F2R,
        FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp]
      have hmant : (x.Fnum : ℝ) < ((x.Fnum + 1 : Int) : ℝ) := by
        exact_mod_cast (show x.Fnum < x.Fnum + 1 by omega)
      exact mul_lt_mul_of_pos_right hmant (zpow_pos hradix_pos_real x.Fexp)

noncomputable def FPredCanonic_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit := ()

/-- Coq: `FPredCanonic` — predecessor preserves canonicity. -/
theorem FPredCanonic {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fcanonic (beta:=beta) radix b x ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (FPredCanonic_check (beta:=beta) b radix precision x) : Id Unit)
    ⦃⇓_ => ⌜Fcanonic (beta:=beta) radix b
      (FPred (beta:=beta) b radix precision x)⌝⦄ := by
  intro h
  rcases h with ⟨hcan, hradix, hprecision, hvnum⟩
  simp only [wp, PostCond.noThrow, pure, FPredCanonic_check, Id.run,
    ULift.up_down]
  have hopp_can :
      Fcanonic (beta:=beta) radix b (Fopp (beta:=beta) x) := by
    have h := FcanonicFopp (beta:=beta) radix b x
    simpa only [wp, PostCond.noThrow, pure, FcanonicFopp_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Id.run, ULift.up, ULift.down]
      using h hcan
  have hsucc_can :
      Fcanonic (beta:=beta) radix b
        (FSucc (beta:=beta) b radix precision (Fopp (beta:=beta) x)) := by
    have h := FSuccCanonic (beta:=beta) b radix precision (Fopp (beta:=beta) x)
    simpa only [wp, PostCond.noThrow, pure, FSuccCanonic_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Id.run,
      ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
      h ⟨hopp_can, hradix, hprecision, hvnum⟩
  have hpred_eq :
      FPred (beta:=beta) b radix precision x =
        Fopp (beta:=beta)
          (FSucc (beta:=beta) b radix precision (Fopp (beta:=beta) x)) := by
    have h := FPredFopFSucc (beta:=beta) b radix precision x
    simpa only [wp, PostCond.noThrow, pure, FPredFopFSucc_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Id.run,
      ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
      h ⟨hradix, hprecision, hvnum⟩
  rw [hpred_eq]
  have h := FcanonicFopp (beta:=beta) radix b
    (FSucc (beta:=beta) b radix precision (Fopp (beta:=beta) x))
  simpa only [wp, PostCond.noThrow, pure, FcanonicFopp_check,
    PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Id.run, ULift.up, ULift.down]
    using h hsucc_can

/-- Boundedness corollary for the canonical predecessor. -/
theorem FPredBounded {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    Fcanonic (beta:=beta) radix b x →
    1 < radix →
    precision ≠ 0 →
    b.vNum = Zpower_nat radix precision →
    Fbounded (beta:=beta) b
      (FPred (beta:=beta) b radix precision x) := by
  intro hxCan hradix hprecision hvnum
  have hpredCan : Fcanonic (beta:=beta) radix b
      (FPred (beta:=beta) b radix precision x) := by
    have h := FPredCanonic (beta:=beta) b radix precision x
    simpa only [wp, PostCond.noThrow, pure, FPredCanonic_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Id.run,
      ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
      h ⟨hxCan, hradix, hprecision, hvnum⟩
  have hbound := FcanonicBound (beta:=beta) radix b
    (FPred (beta:=beta) b radix precision x)
  simpa only [wp, PostCond.noThrow, pure, FcanonicBound_check,
    PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Id.run,
    ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
    hbound hpredCan

noncomputable def FPredLt_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit := ()

/-- Coq: `FPredLt` — every float is strictly above its normalized predecessor. -/
theorem FPredLt {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (FPredLt_check (beta:=beta) b radix precision x) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta)
        (FPred (beta:=beta) b radix precision x) <
      _root_.F2R (beta:=beta) x⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hradix, hprecision, hvnum⟩
  simp only [wp, PostCond.noThrow, pure, FPredLt_check, Id.run,
    ULift.up_down]
  have hsucc_lt :
      _root_.F2R (beta:=beta) (Fopp (beta:=beta) x) <
        _root_.F2R (beta:=beta)
          (FSucc (beta:=beta) b radix precision (Fopp (beta:=beta) x)) := by
    have h := FSuccLt (beta:=beta) b radix precision (Fopp (beta:=beta) x)
    simpa only [wp, PostCond.noThrow, pure, FSuccLt_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Id.run,
      ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
      h ⟨hbeta, hradix, hprecision, hvnum⟩
  have hpred_eq :
      FPred (beta:=beta) b radix precision x =
        Fopp (beta:=beta)
          (FSucc (beta:=beta) b radix precision (Fopp (beta:=beta) x)) := by
    have h := FPredFopFSucc (beta:=beta) b radix precision x
    simpa only [wp, PostCond.noThrow, pure, FPredFopFSucc_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Id.run,
      ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using h ⟨hradix, hprecision, hvnum⟩
  have hx_opp :
      _root_.F2R (beta:=beta) (Fopp (beta:=beta) x) =
        - _root_.F2R (beta:=beta) x := by
    have h := FloatSpec.Calc.Operations.F2R_opp (beta:=beta) x
    simpa [Fopp, _root_.F2R] using h trivial
  have hsucc_opp :
      _root_.F2R (beta:=beta)
          (Fopp (beta:=beta)
            (FSucc (beta:=beta) b radix precision (Fopp (beta:=beta) x))) =
        - _root_.F2R (beta:=beta)
          (FSucc (beta:=beta) b radix precision (Fopp (beta:=beta) x)) := by
    have h := FloatSpec.Calc.Operations.F2R_opp (beta:=beta)
      (FSucc (beta:=beta) b radix precision (Fopp (beta:=beta) x))
    simpa [Fopp, _root_.F2R] using h trivial
  rw [hpred_eq, hsucc_opp]
  rw [hx_opp] at hsucc_lt
  have hdual := neg_lt_neg hsucc_lt
  simpa using hdual

noncomputable def R0RltRleSucc_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit := ()

/-- Coq: `R0RltRleSucc` — if a float is negative, its normalized successor is
still nonpositive. -/
theorem R0RltRleSucc {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision ∧
        _root_.F2R (beta:=beta) x < 0⌝⦄
    (pure (R0RltRleSucc_check (beta:=beta) b radix precision x) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta)
      (FSucc (beta:=beta) b radix precision x) ≤ 0⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hradix, hprecision, hvnum, hxneg⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure, R0RltRleSucc_check, Id.run,
    ULift.up_down]
  have hxnum_neg : x.Fnum < 0 :=
    FloatSpec.Core.Float_prop.lt_0_F2R (beta:=radix) x hradix hxneg
  have hradix_pos : 0 < radix := by omega
  have hvnum_gt_one : (1 : Int) < b.vNum := by
    rw [hvnum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecision
  have hnm_pos : 0 < nNormMin radix precision := by
    unfold nNormMin
    exact pow_pos hradix_pos _
  have hpPred_pos : 0 < pPred b.vNum := by
    unfold pPred
    omega
  have hle_zero_of_fnum
      (f : FloatSpec.Core.Defs.FlocqFloat radix) (hf : f.Fnum ≤ 0) :
      _root_.F2R (beta:=radix) f ≤ 0 :=
    FloatSpec.Core.Float_prop.F2R_le_0 (beta:=radix) f hradix hf
  unfold FSucc
  by_cases htop : x.Fnum = pPred b.vNum
  · exfalso
    omega
  · simp [htop]
    by_cases hlow : x.Fnum = -nNormMin radix precision
    · simp [hlow]
      by_cases hexp_min : x.Fexp = -b.dExp
      · simp [hexp_min]
        have hmant : -nNormMin radix precision + 1 ≤ 0 := by omega
        simpa [_root_.F2R, FloatSpec.Core.Defs.F2R, zpow_neg] using
          hle_zero_of_fnum
          (⟨-nNormMin radix precision + 1, -b.dExp⟩ :
            FloatSpec.Core.Defs.FlocqFloat radix)
          hmant
      · simp [hexp_min]
        have hmant : -pPred b.vNum ≤ 0 := by omega
        simpa [_root_.F2R, FloatSpec.Core.Defs.F2R] using hle_zero_of_fnum
          (⟨-pPred b.vNum, x.Fexp - 1⟩ :
            FloatSpec.Core.Defs.FlocqFloat radix)
          hmant
    · simp [hlow]
      have hmant : x.Fnum + 1 ≤ 0 := by omega
      simpa [_root_.F2R, FloatSpec.Core.Defs.F2R] using hle_zero_of_fnum
        (⟨x.Fnum + 1, x.Fexp⟩ :
          FloatSpec.Core.Defs.FlocqFloat radix)
        hmant

noncomputable def R0RltRlePred_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit := ()

/-- Coq: `R0RltRlePred` — if a float is positive, its normalized predecessor is
still nonnegative. -/
theorem R0RltRlePred {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision ∧
        0 < _root_.F2R (beta:=beta) x⌝⦄
    (pure (R0RltRlePred_check (beta:=beta) b radix precision x) : Id Unit)
    ⦃⇓_ => ⌜0 ≤ _root_.F2R (beta:=beta)
      (FPred (beta:=beta) b radix precision x)⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hradix, hprecision, hvnum, hxpos⟩
  simp only [wp, PostCond.noThrow, pure, R0RltRlePred_check, Id.run,
    ULift.up_down]
  have hx_opp :
      _root_.F2R (beta:=beta) (Fopp (beta:=beta) x) =
        - _root_.F2R (beta:=beta) x := by
    have h := FloatSpec.Calc.Operations.F2R_opp (beta:=beta) x
    simpa [Fopp, _root_.F2R] using h trivial
  have hopp_neg :
      _root_.F2R (beta:=beta) (Fopp (beta:=beta) x) < 0 := by
    rw [hx_opp]
    exact neg_neg_of_pos hxpos
  have hsucc_nonpos :
      _root_.F2R (beta:=beta)
          (FSucc (beta:=beta) b radix precision (Fopp (beta:=beta) x)) ≤ 0 := by
    have h := R0RltRleSucc (beta:=beta) b radix precision (Fopp (beta:=beta) x)
    simpa only [wp, PostCond.noThrow, pure, R0RltRleSucc_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Id.run,
      ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
      h ⟨hbeta, hradix, hprecision, hvnum, hopp_neg⟩
  have hpred_eq :
      FPred (beta:=beta) b radix precision x =
        Fopp (beta:=beta)
          (FSucc (beta:=beta) b radix precision (Fopp (beta:=beta) x)) := by
    have h := FPredFopFSucc (beta:=beta) b radix precision x
    simpa only [wp, PostCond.noThrow, pure, FPredFopFSucc_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Id.run,
      ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using h ⟨hradix, hprecision, hvnum⟩
  have hsucc_opp :
      _root_.F2R (beta:=beta)
          (Fopp (beta:=beta)
            (FSucc (beta:=beta) b radix precision (Fopp (beta:=beta) x))) =
        - _root_.F2R (beta:=beta)
          (FSucc (beta:=beta) b radix precision (Fopp (beta:=beta) x)) := by
    have h := FloatSpec.Calc.Operations.F2R_opp (beta:=beta)
      (FSucc (beta:=beta) b radix precision (Fopp (beta:=beta) x))
    simpa [Fopp, _root_.F2R] using h trivial
  rw [hpred_eq, hsucc_opp]
  exact neg_nonneg.mpr hsucc_nonpos

-- Relative ordering of canonical floats (Coq: `FcanonicLtPos`)
noncomputable def FcanonicLtPos_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FcanonicLtPos` — for canonical floats `p` and `q`, if
`0 ≤ F2R p < F2R q`, then either the exponent of `p` is strictly
smaller than that of `q`, or the exponents match and the mantissa of `p`
is strictly smaller.

Note: Uses `Fcanonic'`, the Coq-matching canonical predicate.
This theorem requires the canonical form structure to be meaningful.
Requires `1 < beta` (radixMoreThanOne in Coq) and `radix = beta`. -/
theorem FcanonicLtPos {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta)
    (hβ : 1 < beta)
    (hradix : radix = beta) :
    ⦃⌜Fcanonic' (beta:=beta) radix b p ∧
        Fcanonic' (beta:=beta) radix b q ∧
        0 ≤ _root_.F2R (beta:=beta) p ∧
        _root_.F2R (beta:=beta) p < _root_.F2R (beta:=beta) q⌝⦄
    (pure (FcanonicLtPos_check (beta:=beta) radix b p q) : Id Unit)
    ⦃⇓_ => ⌜p.Fexp < q.Fexp ∨
            (p.Fexp = q.Fexp ∧ p.Fnum < q.Fnum)⌝⦄ := by
  intro ⟨hcanP, hcanQ, hPos, hLt⟩
  simp only [wp, PostCond.noThrow, pure, FcanonicLtPos_check, ULift.down_up]
  -- Derive beta > 0 from 1 < beta
  have hbeta_pos : (0 : ℝ) < (beta : ℝ) := by
    have : (0 : Int) < beta := lt_trans (by norm_num : (0 : Int) < 1) hβ
    exact Int.cast_pos.mpr this
  -- Unfold F2R to get: p.Fnum * beta ^ p.Fexp < q.Fnum * beta ^ q.Fexp
  simp only [_root_.F2R, FloatSpec.Core.Defs.F2R] at hLt hPos
  -- Case split on exponent comparison
  by_cases hexp : p.Fexp < q.Fexp
  · left; exact hexp
  · -- p.Fexp ≥ q.Fexp
    push Not at hexp
    by_cases hexp_eq : p.Fexp = q.Fexp
    · -- Exponents equal: compare mantissas
      right
      constructor
      · exact hexp_eq
      · -- Need to show p.Fnum < q.Fnum given F2R p < F2R q and p.Fexp = q.Fexp
        rw [hexp_eq] at hLt
        -- Now hLt : p.Fnum * beta ^ q.Fexp < q.Fnum * beta ^ q.Fexp
        -- Since beta > 0, beta ^ q.Fexp > 0, so we can divide
        have hpow_pos : (0 : ℝ) < (beta : ℝ) ^ q.Fexp := zpow_pos hbeta_pos q.Fexp
        -- Divide both sides by the positive power
        have hpow_ne : (beta : ℝ) ^ q.Fexp ≠ 0 := ne_of_gt hpow_pos
        have h : (p.Fnum : ℝ) < (q.Fnum : ℝ) := by
          calc (p.Fnum : ℝ) = (p.Fnum : ℝ) * (beta : ℝ) ^ q.Fexp / (beta : ℝ) ^ q.Fexp := by
                field_simp
            _ < (q.Fnum : ℝ) * (beta : ℝ) ^ q.Fexp / (beta : ℝ) ^ q.Fexp := by
                apply div_lt_div_of_pos_right hLt hpow_pos
            _ = (q.Fnum : ℝ) := by field_simp
        exact Int.cast_lt.mp h
    · -- p.Fexp > q.Fexp: use the case analysis from Coq proof
      -- The Coq proof uses FnormalLtPos, FsubnormalnormalLtPos, FsubnormalLt
      -- For canonical floats with 0 ≤ F2R p and F2R p < F2R q, when p.Fexp > q.Fexp,
      -- we need to show this leads to one of the disjuncts being true
      have hexp_gt : q.Fexp < p.Fexp := lt_of_le_of_ne hexp (Ne.symm hexp_eq)
      -- Case analysis on whether p and q are normal or subnormal
      rcases hcanP with ⟨hbP, hvnumP⟩ | ⟨hbP, hexpP, hvnumP⟩
      <;> rcases hcanQ with ⟨hbQ, hvnumQ⟩ | ⟨hbQ, hexpQ, hvnumQ⟩
      -- Case 1: Both normal - contradiction via FnormalLtPos logic
      · -- Both normal with p.Fexp > q.Fexp, 0 ≤ F2R p, F2R p < F2R q
        -- This requires detailed analysis of normal float bounds
        -- Coq proof uses FnormalLtPos helper lemma
        -- Key insight: b.vNum ≤ |radix * Fnum| constrains mantissa from below
        -- With radix = beta, the exponent difference dominates
        exfalso
        rw [hradix] at hvnumP hvnumQ
        -- Step 1: Since 0 ≤ F2R p and beta^p.Fexp > 0, we have 0 ≤ p.Fnum
        have hpow_p_pos : (0 : ℝ) < (beta : ℝ) ^ p.Fexp := zpow_pos hbeta_pos p.Fexp
        have hp_fnum_nonneg : (0 : ℤ) ≤ p.Fnum := by
          by_contra hcontra
          push Not at hcontra
          have hneg : (p.Fnum : ℝ) < 0 := Int.cast_lt_zero.mpr hcontra
          have : (p.Fnum : ℝ) * (beta : ℝ) ^ p.Fexp < 0 :=
            mul_neg_of_neg_of_pos hneg hpow_p_pos
          linarith
        -- Step 2: From hvnumP and p.Fnum ≥ 0, get b.vNum ≤ beta * p.Fnum
        have hp_fnum_nonneg_real : (0 : ℝ) ≤ (p.Fnum : ℝ) := Int.cast_nonneg hp_fnum_nonneg
        have hbeta_pos_int : (0 : ℤ) < beta := lt_trans (by norm_num : (0 : ℤ) < 1) hβ
        have hbeta_nonneg_int : (0 : ℤ) ≤ beta := le_of_lt hbeta_pos_int
        have habs_beta_p : |beta * p.Fnum| = beta * p.Fnum := by
          rw [abs_of_nonneg]
          exact mul_nonneg hbeta_nonneg_int hp_fnum_nonneg
        have hvnumP' : (b.vNum : ℤ) ≤ beta * p.Fnum := by
          rw [← habs_beta_p]
          exact hvnumP
        -- Step 3: From hbQ.1, we have |q.Fnum| < b.vNum
        have hq_abs_bound : |q.Fnum| < (b.vNum : ℤ) := hbQ.1
        -- Step 4: Therefore |q.Fnum| < beta * p.Fnum
        have hq_lt_beta_p : |q.Fnum| < beta * p.Fnum := lt_of_lt_of_le hq_abs_bound hvnumP'
        -- Step 5: We have q.Fnum ≤ |q.Fnum| < beta * p.Fnum
        have hq_fnum_lt : q.Fnum < beta * p.Fnum := lt_of_le_of_lt (le_abs_self q.Fnum) hq_lt_beta_p
        -- Step 6: Since p.Fexp > q.Fexp, we have p.Fexp ≥ q.Fexp + 1
        have hexp_ge : p.Fexp ≥ q.Fexp + 1 := hexp_gt
        -- Step 7: Show F2R p ≥ F2R q (contradiction with hLt)
        -- F2R p = p.Fnum * beta^p.Fexp ≥ p.Fnum * beta^(q.Fexp + 1) = (beta * p.Fnum) * beta^q.Fexp
        have hpow_q_pos : (0 : ℝ) < (beta : ℝ) ^ q.Fexp := zpow_pos hbeta_pos q.Fexp
        -- Compute: beta^p.Fexp ≥ beta^(q.Fexp + 1) = beta * beta^q.Fexp
        have hbeta_ge_one : (1 : ℝ) ≤ (beta : ℝ) := by
          have h1lt : (1 : ℤ) < beta := hβ
          have h1le : (1 : ℤ) ≤ beta := le_of_lt h1lt
          have hcast : ((1 : ℤ) : ℝ) ≤ ((beta : ℤ) : ℝ) := Int.cast_le.mpr h1le
          simp only [Int.cast_one] at hcast
          exact hcast
        have hpow_mono : (beta : ℝ) ^ (q.Fexp + 1) ≤ (beta : ℝ) ^ p.Fexp := by
          apply zpow_le_zpow_right₀ hbeta_ge_one hexp_ge
        have hpow_expand : (beta : ℝ) ^ (q.Fexp + 1) = (beta : ℝ) * (beta : ℝ) ^ q.Fexp := by
          rw [zpow_add_one₀ (ne_of_gt hbeta_pos)]
          ring
        -- Now combine:
        have hF2Rp_ge : (p.Fnum : ℝ) * (beta : ℝ) ^ p.Fexp ≥ (beta : ℝ) * (p.Fnum : ℝ) * (beta : ℝ) ^ q.Fexp := by
          calc (p.Fnum : ℝ) * (beta : ℝ) ^ p.Fexp
              ≥ (p.Fnum : ℝ) * (beta : ℝ) ^ (q.Fexp + 1) := by
                  apply mul_le_mul_of_nonneg_left hpow_mono hp_fnum_nonneg_real
            _ = (p.Fnum : ℝ) * ((beta : ℝ) * (beta : ℝ) ^ q.Fexp) := by rw [hpow_expand]
            _ = (beta : ℝ) * (p.Fnum : ℝ) * (beta : ℝ) ^ q.Fexp := by ring
        -- Now we need: beta * p.Fnum > q.Fnum
        have hbeta_p_gt_q : (beta : ℝ) * (p.Fnum : ℝ) > (q.Fnum : ℝ) := by
          have h1 : (beta * p.Fnum : ℤ) > q.Fnum := hq_fnum_lt
          have h2 : (q.Fnum : ℝ) < ((beta * p.Fnum) : ℤ) := Int.cast_lt.mpr h1
          simp only [Int.cast_mul] at h2
          exact h2
        -- Therefore: F2R p > F2R q
        have hF2Rp_gt_F2Rq : (p.Fnum : ℝ) * (beta : ℝ) ^ p.Fexp > (q.Fnum : ℝ) * (beta : ℝ) ^ q.Fexp := by
          calc (p.Fnum : ℝ) * (beta : ℝ) ^ p.Fexp
              ≥ (beta : ℝ) * (p.Fnum : ℝ) * (beta : ℝ) ^ q.Fexp := hF2Rp_ge
            _ > (q.Fnum : ℝ) * (beta : ℝ) ^ q.Fexp := by
                apply mul_lt_mul_of_pos_right hbeta_p_gt_q hpow_q_pos
        -- This contradicts hLt
        linarith
      -- Case 2: p normal, q subnormal
      · -- p normal, q subnormal: q.Fexp = -b.dExp (minimal)
        -- This leads to contradiction via FsubnormalnormalLtPos logic
        -- The subnormal q has smaller magnitude than any normal at higher exp
        exfalso
        rw [hradix] at hvnumP hvnumQ
        -- Step 1: From 0 ≤ F2R p and F2R p < F2R q, both Fnum are nonneg
        have hpow_p_pos : (0 : ℝ) < (beta : ℝ) ^ p.Fexp := zpow_pos hbeta_pos p.Fexp
        have hpow_q_pos : (0 : ℝ) < (beta : ℝ) ^ q.Fexp := zpow_pos hbeta_pos q.Fexp
        have hp_fnum_nonneg : (0 : ℤ) ≤ p.Fnum := by
          by_contra hcontra
          push Not at hcontra
          have hneg : (p.Fnum : ℝ) < 0 := Int.cast_lt_zero.mpr hcontra
          have : (p.Fnum : ℝ) * (beta : ℝ) ^ p.Fexp < 0 :=
            mul_neg_of_neg_of_pos hneg hpow_p_pos
          linarith
        have hq_fnum_nonneg : (0 : ℤ) ≤ q.Fnum := by
          by_contra hcontra
          push Not at hcontra
          have hneg : (q.Fnum : ℝ) < 0 := Int.cast_lt_zero.mpr hcontra
          have hF2Rq_neg : (q.Fnum : ℝ) * (beta : ℝ) ^ q.Fexp < 0 :=
            mul_neg_of_neg_of_pos hneg hpow_q_pos
          -- But F2R p < F2R q and 0 ≤ F2R p, so F2R q > 0
          have hF2Rq_pos : (0 : ℝ) < (q.Fnum : ℝ) * (beta : ℝ) ^ q.Fexp := lt_of_le_of_lt hPos hLt
          linarith
        -- Step 2: Since p is normal and q is subnormal
        -- hvnumP: b.vNum ≤ |beta * p.Fnum| = beta * p.Fnum (since p.Fnum ≥ 0)
        -- hvnumQ: |beta * q.Fnum| < b.vNum, so beta * q.Fnum < b.vNum (since q.Fnum ≥ 0)
        have hbeta_pos_int : (0 : ℤ) < beta := lt_trans (by norm_num : (0 : ℤ) < 1) hβ
        have hbeta_nonneg_int : (0 : ℤ) ≤ beta := le_of_lt hbeta_pos_int
        have habs_beta_p : |beta * p.Fnum| = beta * p.Fnum := by
          rw [abs_of_nonneg]
          exact mul_nonneg hbeta_nonneg_int hp_fnum_nonneg
        have habs_beta_q : |beta * q.Fnum| = beta * q.Fnum := by
          rw [abs_of_nonneg]
          exact mul_nonneg hbeta_nonneg_int hq_fnum_nonneg
        have hvnumP' : (b.vNum : ℤ) ≤ beta * p.Fnum := by rw [← habs_beta_p]; exact hvnumP
        have hvnumQ' : beta * q.Fnum < (b.vNum : ℤ) := by rw [← habs_beta_q]; exact hvnumQ
        -- Step 3: From hvnumQ' < hvnumP', we get q.Fnum < p.Fnum
        have hq_lt_p : beta * q.Fnum < beta * p.Fnum := lt_of_lt_of_le hvnumQ' hvnumP'
        have hp_beta_pos : (0 : ℤ) < beta := hbeta_pos_int
        have hq_fnum_lt_p_fnum : q.Fnum < p.Fnum := by
          have := Int.lt_of_mul_lt_mul_left hq_lt_p (le_of_lt hp_beta_pos)
          omega
        -- Step 4: With p.Fexp > q.Fexp, show F2R p > F2R q
        -- F2R p = p.Fnum * beta^p.Fexp ≥ p.Fnum * beta^(q.Fexp+1) (since p.Fexp ≥ q.Fexp+1)
        --       = (beta * p.Fnum) * beta^q.Fexp > (beta * q.Fnum) * beta^q.Fexp ≥ q.Fnum * beta^q.Fexp
        -- Actually simpler: p.Fnum > q.Fnum and p.Fexp > q.Fexp gives F2R p > F2R q directly
        -- p.Fnum * beta^p.Fexp > q.Fnum * beta^q.Fexp
        have hp_fnum_nonneg_real : (0 : ℝ) ≤ (p.Fnum : ℝ) := Int.cast_nonneg hp_fnum_nonneg
        have hq_fnum_nonneg_real : (0 : ℝ) ≤ (q.Fnum : ℝ) := Int.cast_nonneg hq_fnum_nonneg
        have hbeta_ge_one : (1 : ℝ) ≤ (beta : ℝ) := by
          have h1le : (1 : ℤ) ≤ beta := le_of_lt hβ
          have hcast : ((1 : ℤ) : ℝ) ≤ ((beta : ℤ) : ℝ) := Int.cast_le.mpr h1le
          simp only [Int.cast_one] at hcast
          exact hcast
        have hexp_ge : p.Fexp ≥ q.Fexp + 1 := hexp_gt
        have hpow_mono : (beta : ℝ) ^ q.Fexp ≤ (beta : ℝ) ^ p.Fexp := by
          apply zpow_le_zpow_right₀ hbeta_ge_one (le_of_lt hexp_gt)
        have hq_lt_p_real : (q.Fnum : ℝ) < (p.Fnum : ℝ) := Int.cast_lt.mpr hq_fnum_lt_p_fnum
        -- Now: F2R q = q.Fnum * beta^q.Fexp < p.Fnum * beta^q.Fexp ≤ p.Fnum * beta^p.Fexp = F2R p
        have hF2Rp_ge_F2Rq : (p.Fnum : ℝ) * (beta : ℝ) ^ p.Fexp > (q.Fnum : ℝ) * (beta : ℝ) ^ q.Fexp := by
          calc (q.Fnum : ℝ) * (beta : ℝ) ^ q.Fexp
              < (p.Fnum : ℝ) * (beta : ℝ) ^ q.Fexp := by
                  apply mul_lt_mul_of_pos_right hq_lt_p_real hpow_q_pos
            _ ≤ (p.Fnum : ℝ) * (beta : ℝ) ^ p.Fexp := by
                  apply mul_le_mul_of_nonneg_left hpow_mono hp_fnum_nonneg_real
        linarith
      -- Case 3: p subnormal, q normal
      · -- p subnormal, q normal: p.Fexp = -b.dExp ≤ q.Fexp
        -- This contradicts hexp_gt: q.Fexp < p.Fexp
        have hpExp : p.Fexp = -b.dExp := hexpP
        have hq_lb := hbQ.2  -- -b.dExp ≤ q.Fexp
        omega
      -- Case 4: Both subnormal
      · -- Both subnormal: p.Fexp = q.Fexp = -b.dExp
        have hpExp : p.Fexp = -b.dExp := hexpP
        have hqExp : q.Fexp = -b.dExp := hexpQ
        -- This contradicts hexp_gt: q.Fexp < p.Fexp
        omega

noncomputable def Fcanonic_Rle_Zle_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `Fcanonic_Rle_Zle` — canonical floats ordered by absolute value have
    nondecreasing exponents.

Note: Uses `Fcanonic'`, the Coq-matching canonical predicate.
This theorem requires the canonical form structure to be meaningful.
Requires `1 < beta` (radixMoreThanOne in Coq) and `radix = beta`. -/
theorem Fcanonic_Rle_Zle {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta)
    (hβ : 1 < beta)
    (hradix : radix = beta) :
    ⦃⌜Fcanonic' (beta:=beta) radix b x ∧
        Fcanonic' (beta:=beta) radix b y ∧
        |_root_.F2R (beta:=beta) x|
          ≤ |_root_.F2R (beta:=beta) y|⌝⦄
    (pure (Fcanonic_Rle_Zle_check (beta:=beta) radix b x y) : Id Unit)
    ⦃⇓_ => ⌜x.Fexp ≤ y.Fexp⌝⦄ := by
  intro ⟨hcanX, hcanY, habs_le⟩
  simp only [wp, PostCond.noThrow, pure, Fcanonic_Rle_Zle_check, ULift.down_up]
  -- Derive beta > 0 from 1 < beta
  have hbeta_pos : (0 : ℝ) < (beta : ℝ) := by
    have : (0 : Int) < beta := lt_trans (by norm_num : (0 : Int) < 1) hβ
    exact Int.cast_pos.mpr this
  have hbeta_ge_one_int : (1 : Int) ≤ beta := le_of_lt hβ
  -- Useful lemma: ↑|z| = |↑z| for any integer z
  have int_abs_cast : ∀ z : Int, (↑|z| : ℝ) = |↑z| := fun z => by
    simp only [Int.cast_abs]
  -- Case split: |F2R x| < |F2R y| or |F2R x| = |F2R y|
  rcases habs_le.lt_or_eq with habs_lt | habs_eq
  · -- Case: |F2R x| < |F2R y|
    by_contra h_exp_not_le
    have h_exp_gt : y.Fexp < x.Fexp := not_le.mp h_exp_not_le
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R] at habs_lt
    have hpow_pos_x : (0 : ℝ) < (beta : ℝ) ^ x.Fexp := zpow_pos hbeta_pos x.Fexp
    have hpow_pos_y : (0 : ℝ) < (beta : ℝ) ^ y.Fexp := zpow_pos hbeta_pos y.Fexp
    rw [abs_mul, abs_mul, abs_of_pos hpow_pos_x, abs_of_pos hpow_pos_y] at habs_lt
    -- Convert to use ↑|z| form
    simp only [← int_abs_cast] at habs_lt
    have hd_pos : 0 < x.Fexp - y.Fexp := by omega
    have hpow_factor : (beta : ℝ) ^ x.Fexp = (beta : ℝ) ^ y.Fexp * (beta : ℝ) ^ (x.Fexp - y.Fexp) := by
      rw [← zpow_add₀ (ne_of_gt hbeta_pos)]; congr 1; omega
    -- Rewrite and derive key inequality
    have hpy_ne : (beta : ℝ) ^ y.Fexp ≠ 0 := ne_of_gt hpow_pos_y
    have habs_lt' : (↑|x.Fnum| : ℝ) * (beta : ℝ) ^ x.Fexp < (↑|y.Fnum| : ℝ) * (beta : ℝ) ^ y.Fexp := habs_lt
    rw [hpow_factor] at habs_lt'
    -- habs_lt' : ↑|x.Fnum| * (↑beta ^ y.Fexp * ↑beta ^ (x.Fexp - y.Fexp)) < ↑|y.Fnum| * ↑beta ^ y.Fexp
    have h1 : (↑|x.Fnum| : ℝ) * (beta : ℝ) ^ (x.Fexp - y.Fexp) < (↑|y.Fnum| : ℝ) := by
      -- Rewrite LHS: a * (b * c) = (a * c) * b
      have eq1 : (↑|x.Fnum| : ℝ) * ((beta : ℝ) ^ y.Fexp * (beta : ℝ) ^ (x.Fexp - y.Fexp)) =
                 (↑|x.Fnum| : ℝ) * (beta : ℝ) ^ (x.Fexp - y.Fexp) * (beta : ℝ) ^ y.Fexp := by ring
      rw [eq1] at habs_lt'
      -- Divide both sides by beta^y.Fexp
      have h := div_lt_div_of_pos_right habs_lt' hpow_pos_y
      simp only [mul_div_assoc, div_self hpy_ne, mul_one] at h
      exact h
    rcases hcanX with ⟨⟨hbX_num, hbX_exp⟩, hvnumX⟩ | ⟨⟨hbX_num, hbX_exp⟩, hexpX, hvnumX⟩
    <;> rcases hcanY with ⟨⟨hbY_num, hbY_exp⟩, hvnumY⟩ | ⟨⟨hbY_num, hbY_exp⟩, hexpY, hvnumY⟩
    -- Case 1: x normal, y normal
    · rw [hradix] at hvnumX
      have hvnumX' : (b.vNum : ℝ) ≤ (beta : ℝ) * (↑|x.Fnum| : ℝ) := by
        have h : (b.vNum : ℝ) ≤ (↑|beta * x.Fnum| : ℝ) := Int.cast_le.mpr hvnumX
        simp only [Int.cast_abs, Int.cast_mul, abs_mul, abs_of_pos hbeta_pos] at h
        simp only [Int.cast_abs]
        exact h
      have hvnumY' : (↑|y.Fnum| : ℝ) < (b.vNum : ℝ) := Int.cast_lt.mpr hbY_num
      have hpow_ge_beta : (beta : ℝ) ≤ (beta : ℝ) ^ (x.Fexp - y.Fexp) := by
        have hbeta_ge_one : (1 : ℝ) ≤ (beta : ℝ) := by
          have h : ((1 : Int) : ℝ) ≤ (beta : ℝ) := Int.cast_le.mpr hbeta_ge_one_int
          simp only [Int.cast_one] at h
          exact h
        have := zpow_le_zpow_right₀ hbeta_ge_one (by omega : (1 : ℤ) ≤ x.Fexp - y.Fexp)
        simp only [zpow_one] at this
        exact this
      by_cases hxfnum : x.Fnum = 0
      · simp [hxfnum] at hvnumX'
        simp only [hxfnum, abs_zero, Int.cast_zero, zero_mul] at h1
        have h1' : (0 : ℝ) < (↑|y.Fnum| : ℝ) := h1
        -- hvnumX' gives b.vNum ≤ 0 (as Int)
        -- hvnumY' gives ↑|y.Fnum| < b.vNum (as Real)
        -- h1' gives 0 < ↑|y.Fnum| (as Real)
        -- Contradiction: 0 < ↑|y.Fnum| < b.vNum but b.vNum ≤ 0
        have hvnum_pos : (0 : ℝ) < (b.vNum : ℝ) := lt_trans h1' hvnumY'
        have hvnumX'_real : (b.vNum : ℝ) ≤ 0 := Int.cast_nonpos.mpr hvnumX'
        linarith
      · have hxfnum_pos : 0 < (↑|x.Fnum| : ℝ) := Int.cast_pos.mpr (abs_pos.mpr hxfnum)
        have key : (b.vNum : ℝ) ≤ (↑|x.Fnum| : ℝ) * (beta : ℝ) ^ (x.Fexp - y.Fexp) :=
          calc (b.vNum : ℝ) ≤ (beta : ℝ) * (↑|x.Fnum| : ℝ) := hvnumX'
            _ = (↑|x.Fnum| : ℝ) * (beta : ℝ) := by ring
            _ ≤ (↑|x.Fnum| : ℝ) * (beta : ℝ) ^ (x.Fexp - y.Fexp) :=
                mul_le_mul_of_nonneg_left hpow_ge_beta (le_of_lt hxfnum_pos)
        linarith
    -- Case 2: x normal, y subnormal
    · rw [hradix] at hvnumX
      have hvnumX' : (b.vNum : ℝ) ≤ (beta : ℝ) * (↑|x.Fnum| : ℝ) := by
        have h : (b.vNum : ℝ) ≤ (↑|beta * x.Fnum| : ℝ) := Int.cast_le.mpr hvnumX
        simp only [Int.cast_abs, Int.cast_mul, abs_mul, abs_of_pos hbeta_pos] at h
        simp only [Int.cast_abs]
        exact h
      have hvnumY' : (↑|y.Fnum| : ℝ) < (b.vNum : ℝ) := Int.cast_lt.mpr hbY_num
      have hpow_ge_beta : (beta : ℝ) ≤ (beta : ℝ) ^ (x.Fexp - y.Fexp) := by
        have hbeta_ge_one : (1 : ℝ) ≤ (beta : ℝ) := by
          have h : ((1 : Int) : ℝ) ≤ (beta : ℝ) := Int.cast_le.mpr hbeta_ge_one_int
          simp only [Int.cast_one] at h
          exact h
        have := zpow_le_zpow_right₀ hbeta_ge_one (by omega : (1 : ℤ) ≤ x.Fexp - y.Fexp)
        simp only [zpow_one] at this
        exact this
      by_cases hxfnum : x.Fnum = 0
      · simp [hxfnum] at hvnumX'
        simp only [hxfnum, abs_zero, Int.cast_zero, zero_mul] at h1
        have h1' : (0 : ℝ) < (↑|y.Fnum| : ℝ) := h1
        have hvnum_pos : (0 : ℝ) < (b.vNum : ℝ) := lt_trans h1' hvnumY'
        have hvnumX'_real : (b.vNum : ℝ) ≤ 0 := Int.cast_nonpos.mpr hvnumX'
        linarith
      · have hxfnum_pos : 0 < (↑|x.Fnum| : ℝ) := Int.cast_pos.mpr (abs_pos.mpr hxfnum)
        have key : (b.vNum : ℝ) ≤ (↑|x.Fnum| : ℝ) * (beta : ℝ) ^ (x.Fexp - y.Fexp) :=
          calc (b.vNum : ℝ) ≤ (beta : ℝ) * (↑|x.Fnum| : ℝ) := hvnumX'
            _ = (↑|x.Fnum| : ℝ) * (beta : ℝ) := by ring
            _ ≤ (↑|x.Fnum| : ℝ) * (beta : ℝ) ^ (x.Fexp - y.Fexp) :=
                mul_le_mul_of_nonneg_left hpow_ge_beta (le_of_lt hxfnum_pos)
        linarith
    -- Case 3: x subnormal, y normal
    · rw [hexpX] at h_exp_gt; omega
    -- Case 4: x subnormal, y subnormal
    · rw [hexpX, hexpY] at h_exp_gt; omega
  · -- Case: |F2R x| = |F2R y|
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R] at habs_eq
    have hpow_pos_x : (0 : ℝ) < (beta : ℝ) ^ x.Fexp := zpow_pos hbeta_pos x.Fexp
    have hpow_pos_y : (0 : ℝ) < (beta : ℝ) ^ y.Fexp := zpow_pos hbeta_pos y.Fexp
    rw [abs_mul, abs_mul, abs_of_pos hpow_pos_x, abs_of_pos hpow_pos_y] at habs_eq
    simp only [← int_abs_cast] at habs_eq
    by_cases hxfnum : x.Fnum = 0
    · simp only [hxfnum, abs_zero, Int.cast_zero, zero_mul] at habs_eq
      have hyfnum : y.Fnum = 0 := by
        have h1 : (↑|y.Fnum| : ℝ) * (beta : ℝ) ^ y.Fexp = 0 := by linarith
        rcases mul_eq_zero.mp h1 with hyf | hpow
        · exact abs_eq_zero.mp (Int.cast_eq_zero.mp hyf)
        · exfalso; exact ne_of_gt hpow_pos_y hpow
      rcases hcanX with ⟨⟨hbX_num, hbX_exp⟩, hvnumX⟩ | ⟨⟨hbX_num, hbX_exp⟩, hexpX, hvnumX⟩
      · rw [hradix, hxfnum] at hvnumX; simp at hvnumX
        rw [hxfnum] at hbX_num; simp at hbX_num; omega
      · rcases hcanY with ⟨⟨hbY_num, hbY_exp⟩, hvnumY⟩ | ⟨⟨hbY_num, hbY_exp⟩, hexpY, hvnumY⟩
        · rw [hradix, hyfnum] at hvnumY; simp at hvnumY
          rw [hyfnum] at hbY_num; simp at hbY_num; omega
        · rw [hexpX, hexpY]; simp
    · by_cases hyfnum : y.Fnum = 0
      · simp only [hyfnum, abs_zero, Int.cast_zero, zero_mul] at habs_eq
        have hxfnum_pos : 0 < (↑|x.Fnum| : ℝ) := Int.cast_pos.mpr (abs_pos.mpr hxfnum)
        have h1 : (↑|x.Fnum| : ℝ) * (beta : ℝ) ^ x.Fexp = 0 := habs_eq
        rcases mul_eq_zero.mp h1 with hxf | hpow
        · exact absurd (abs_eq_zero.mp (Int.cast_eq_zero.mp hxf)) hxfnum
        · exact absurd hpow (ne_of_gt hpow_pos_x)
      · -- Both x.Fnum ≠ 0 and y.Fnum ≠ 0
        by_cases hexp_eq : x.Fexp = y.Fexp
        · have h_le : x.Fexp ≤ y.Fexp := le_of_eq hexp_eq
          simp only [PredTrans.pure, Id.run, wp, PostCond.noThrow, PLift.up, h_le]
          trivial
        · by_cases hexp_lt : x.Fexp < y.Fexp
          · have h_le : x.Fexp ≤ y.Fexp := le_of_lt hexp_lt
            simp only [PredTrans.pure, Id.run, wp, PostCond.noThrow, PLift.up, h_le]
            trivial
          · -- x.Fexp > y.Fexp
            have hexp_gt : y.Fexp < x.Fexp := lt_of_le_of_ne (not_lt.mp hexp_lt) (Ne.symm hexp_eq)
            have hd_pos : 0 < x.Fexp - y.Fexp := by omega
            have hpow_factor : (beta : ℝ) ^ x.Fexp = (beta : ℝ) ^ y.Fexp * (beta : ℝ) ^ (x.Fexp - y.Fexp) := by
              rw [← zpow_add₀ (ne_of_gt hbeta_pos)]; congr 1; omega
            -- From equality: |x.Fnum| * beta^x.Fexp = |y.Fnum| * beta^y.Fexp
            have hpy_ne : (beta : ℝ) ^ y.Fexp ≠ 0 := ne_of_gt hpow_pos_y
            have habs_eq' : (↑|x.Fnum| : ℝ) * (beta : ℝ) ^ x.Fexp = (↑|y.Fnum| : ℝ) * (beta : ℝ) ^ y.Fexp := habs_eq
            rw [hpow_factor] at habs_eq'
            have h1 : (↑|x.Fnum| : ℝ) * (beta : ℝ) ^ (x.Fexp - y.Fexp) = (↑|y.Fnum| : ℝ) := by
              field_simp at habs_eq' ⊢
              linarith
            have hpow_ge_beta : (beta : ℝ) ≤ (beta : ℝ) ^ (x.Fexp - y.Fexp) := by
              have hbeta_ge_one : (1 : ℝ) ≤ (beta : ℝ) := by
                have h : ((1 : Int) : ℝ) ≤ (beta : ℝ) := Int.cast_le.mpr hbeta_ge_one_int
                simp only [Int.cast_one] at h
                exact h
              have := zpow_le_zpow_right₀ hbeta_ge_one (by omega : (1 : ℤ) ≤ x.Fexp - y.Fexp)
              simp only [zpow_one] at this
              exact this
            rcases hcanX with ⟨⟨hbX_num, hbX_exp⟩, hvnumX⟩ | ⟨⟨hbX_num, hbX_exp⟩, hexpX, hvnumX⟩
            <;> rcases hcanY with ⟨⟨hbY_num, hbY_exp⟩, hvnumY⟩ | ⟨⟨hbY_num, hbY_exp⟩, hexpY, hvnumY⟩
            -- x normal, y normal
            · rw [hradix] at hvnumX
              have hvnumX' : (b.vNum : ℝ) ≤ (beta : ℝ) * (↑|x.Fnum| : ℝ) := by
                have h : (b.vNum : ℝ) ≤ (↑|beta * x.Fnum| : ℝ) := Int.cast_le.mpr hvnumX
                simp only [Int.cast_abs, Int.cast_mul, abs_mul, abs_of_pos hbeta_pos] at h
                simp only [Int.cast_abs]
                exact h
              have hvnumY' : (↑|y.Fnum| : ℝ) < (b.vNum : ℝ) := Int.cast_lt.mpr hbY_num
              have hxfnum_pos : 0 < (↑|x.Fnum| : ℝ) := Int.cast_pos.mpr (abs_pos.mpr hxfnum)
              have key : (b.vNum : ℝ) ≤ (↑|x.Fnum| : ℝ) * (beta : ℝ) ^ (x.Fexp - y.Fexp) :=
                calc (b.vNum : ℝ) ≤ (beta : ℝ) * (↑|x.Fnum| : ℝ) := hvnumX'
                  _ = (↑|x.Fnum| : ℝ) * (beta : ℝ) := by ring
                  _ ≤ (↑|x.Fnum| : ℝ) * (beta : ℝ) ^ (x.Fexp - y.Fexp) :=
                      mul_le_mul_of_nonneg_left hpow_ge_beta (le_of_lt hxfnum_pos)
              rw [h1] at key; linarith
            -- x normal, y subnormal
            · rw [hradix] at hvnumX
              have hvnumX' : (b.vNum : ℝ) ≤ (beta : ℝ) * (↑|x.Fnum| : ℝ) := by
                have h : (b.vNum : ℝ) ≤ (↑|beta * x.Fnum| : ℝ) := Int.cast_le.mpr hvnumX
                simp only [Int.cast_abs, Int.cast_mul, abs_mul, abs_of_pos hbeta_pos] at h
                simp only [Int.cast_abs]
                exact h
              have hvnumY' : (↑|y.Fnum| : ℝ) < (b.vNum : ℝ) := Int.cast_lt.mpr hbY_num
              have hxfnum_pos : 0 < (↑|x.Fnum| : ℝ) := Int.cast_pos.mpr (abs_pos.mpr hxfnum)
              have key : (b.vNum : ℝ) ≤ (↑|x.Fnum| : ℝ) * (beta : ℝ) ^ (x.Fexp - y.Fexp) :=
                calc (b.vNum : ℝ) ≤ (beta : ℝ) * (↑|x.Fnum| : ℝ) := hvnumX'
                  _ = (↑|x.Fnum| : ℝ) * (beta : ℝ) := by ring
                  _ ≤ (↑|x.Fnum| : ℝ) * (beta : ℝ) ^ (x.Fexp - y.Fexp) :=
                      mul_le_mul_of_nonneg_left hpow_ge_beta (le_of_lt hxfnum_pos)
              rw [h1] at key; linarith
            -- x subnormal, y normal
            · rw [hexpX] at hexp_gt; omega
            -- x subnormal, y subnormal
            · rw [hexpX, hexpY] at hexp_gt; omega

noncomputable def FcanonicLtNeg_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FcanonicLtNeg` — for canonical floats `p` and `q`, if
`_root_.F2R q ≤ 0` and `_root_.F2R p < _root_.F2R q`, then either the exponent
of `q` is strictly smaller than the exponent of `p`, or the exponents match and
the mantissa of `p` is strictly smaller.

Note: Uses `Fcanonic'`, the Coq-matching canonical predicate.
This theorem requires the canonical form structure to be meaningful.
Requires `1 < beta` (radixMoreThanOne in Coq) and `radix = beta`. -/
theorem FcanonicLtNeg {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta)
    (hβ : 1 < beta)
    (hradix : radix = beta) :
    ⦃⌜Fcanonic' (beta:=beta) radix b p ∧
        Fcanonic' (beta:=beta) radix b q ∧
        _root_.F2R (beta:=beta) q ≤ 0 ∧
        _root_.F2R (beta:=beta) p < _root_.F2R (beta:=beta) q⌝⦄
    (pure (FcanonicLtNeg_check (beta:=beta) radix b p q) : Id Unit)
    ⦃⇓_ => ⌜q.Fexp < p.Fexp ∨
            (p.Fexp = q.Fexp ∧ p.Fnum < q.Fnum)⌝⦄ := by
  intro ⟨hcanP, hcanQ, hNeg, hLt⟩
  simp only [wp, PostCond.noThrow, pure, FcanonicLtNeg_check, ULift.down_up]
  -- Derive beta > 0 from 1 < beta
  have hbeta_pos : (0 : ℝ) < (beta : ℝ) := by
    have : (0 : Int) < beta := lt_trans (by norm_num : (0 : Int) < 1) hβ
    exact Int.cast_pos.mpr this
  -- Derive beta ≥ 1 for exponent comparisons
  have hbeta_ge_one_real : (1 : ℝ) ≤ (beta : ℝ) := by
    have h1le : (1 : ℤ) ≤ beta := le_of_lt hβ
    exact_mod_cast h1le
  -- Unfold F2R to get: p.Fnum * beta ^ p.Fexp < q.Fnum * beta ^ q.Fexp
  simp only [_root_.F2R, FloatSpec.Core.Defs.F2R] at hLt hNeg
  -- Case split on exponent comparison
  by_cases hexp : q.Fexp < p.Fexp
  · left; exact hexp
  · -- q.Fexp ≥ p.Fexp
    push Not at hexp
    by_cases hexp_eq : p.Fexp = q.Fexp
    · -- Exponents equal: compare mantissas
      right
      constructor
      · exact hexp_eq
      · -- Need to show p.Fnum < q.Fnum given F2R p < F2R q and p.Fexp = q.Fexp
        rw [hexp_eq] at hLt
        -- Now hLt : p.Fnum * beta ^ q.Fexp < q.Fnum * beta ^ q.Fexp
        -- Since beta > 0, beta ^ q.Fexp > 0, so we can divide
        have hpow_pos : (0 : ℝ) < (beta : ℝ) ^ q.Fexp := zpow_pos hbeta_pos q.Fexp
        -- Divide both sides by the positive power
        have hpow_ne : (beta : ℝ) ^ q.Fexp ≠ 0 := ne_of_gt hpow_pos
        have h : (p.Fnum : ℝ) < (q.Fnum : ℝ) := by
          calc (p.Fnum : ℝ) = (p.Fnum : ℝ) * (beta : ℝ) ^ q.Fexp / (beta : ℝ) ^ q.Fexp := by
                field_simp
            _ < (q.Fnum : ℝ) * (beta : ℝ) ^ q.Fexp / (beta : ℝ) ^ q.Fexp := by
                apply div_lt_div_of_pos_right hLt hpow_pos
            _ = (q.Fnum : ℝ) := by field_simp
        exact Int.cast_lt.mp h
    · -- q.Fexp > p.Fexp: analogous to FcanonicLtPos for negative case
      have hexp_gt : p.Fexp < q.Fexp := lt_of_le_of_ne hexp (fun h => hexp_eq h)
      -- Case analysis on whether p and q are normal or subnormal
      rcases hcanP with ⟨hbP, hvnumP⟩ | ⟨hbP, hexpP, hvnumP⟩
      <;> rcases hcanQ with ⟨hbQ, hvnumQ⟩ | ⟨hbQ, hexpQ, hvnumQ⟩
      -- Case 1: Both normal
      · exfalso
        rw [hradix] at hvnumP hvnumQ
        -- Since F2R q ≤ 0 and beta^q.Fexp > 0, we have q.Fnum ≤ 0
        have hpow_q_pos : (0 : ℝ) < (beta : ℝ) ^ q.Fexp := zpow_pos hbeta_pos q.Fexp
        have hq_fnum_nonpos : q.Fnum ≤ (0 : ℤ) := by
          by_contra hcontra
          push Not at hcontra
          have hpos : (q.Fnum : ℝ) > 0 := Int.cast_pos.mpr hcontra
          have : (q.Fnum : ℝ) * (beta : ℝ) ^ q.Fexp > 0 :=
            mul_pos hpos hpow_q_pos
          linarith
        -- Since F2R p < F2R q ≤ 0, we have F2R p < 0, so p.Fnum < 0
        have hpow_p_pos : (0 : ℝ) < (beta : ℝ) ^ p.Fexp := zpow_pos hbeta_pos p.Fexp
        have hp_fnum_neg : p.Fnum < (0 : ℤ) := by
          by_contra hcontra
          push Not at hcontra
          have hpos : (p.Fnum : ℝ) ≥ 0 := Int.cast_nonneg hcontra
          have : (p.Fnum : ℝ) * (beta : ℝ) ^ p.Fexp ≥ 0 :=
            mul_nonneg hpos (le_of_lt hpow_p_pos)
          linarith
        -- Normal bounds: b.vNum ≤ |beta * Fnum|
        -- For p: b.vNum ≤ |beta * p.Fnum| = -beta * p.Fnum (since p.Fnum < 0)
        -- For q: b.vNum ≤ |beta * q.Fnum| = -beta * q.Fnum (since q.Fnum ≤ 0)
        have hbeta_pos_int : (0 : ℤ) < beta := lt_trans (by norm_num : (0 : ℤ) < 1) hβ
        have habs_beta_p : |beta * p.Fnum| = -(beta * p.Fnum) := by
          rw [abs_of_neg]
          exact mul_neg_of_pos_of_neg hbeta_pos_int hp_fnum_neg
        have hvnumP' : (b.vNum : ℤ) ≤ -(beta * p.Fnum) := by
          rw [← habs_beta_p]; exact hvnumP
        -- From hbP: |p.Fnum| < b.vNum, we have -p.Fnum < b.vNum (since p.Fnum < 0)
        have hp_abs_bound : |p.Fnum| < b.vNum := hbP.1
        have hp_neg_bound : -p.Fnum < b.vNum := by
          rw [abs_of_neg hp_fnum_neg] at hp_abs_bound
          exact hp_abs_bound
        -- From hvnumQ: b.vNum ≤ |beta * q.Fnum|
        -- For normal q with q.Fnum ≤ 0: |beta * q.Fnum| = -beta * q.Fnum = beta * (-q.Fnum)
        -- So b.vNum ≤ beta * (-q.Fnum)
        -- Combined: -p.Fnum < b.vNum ≤ beta * (-q.Fnum), so -p.Fnum < beta * (-q.Fnum)
        -- i.e., q.Fnum < p.Fnum / beta
        -- With q.Fexp > p.Fexp, we need to derive a contradiction
        -- F2R p = p.Fnum * beta^p.Fexp
        -- F2R q = q.Fnum * beta^q.Fexp
        -- We have F2R p < F2R q (both negative)
        -- Since q.Fexp > p.Fexp, beta^q.Fexp > beta^p.Fexp
        -- For negative numbers, with larger exponent, need larger |mantissa| to stay smaller
        -- But the bounds give us the opposite relationship
        have habs_beta_q : |beta * q.Fnum| = -(beta * q.Fnum) := by
          rcases eq_or_lt_of_le hq_fnum_nonpos with hzero | hneg
          · simp only [hzero, mul_zero, neg_zero, abs_zero]
          · rw [abs_of_neg]
            exact mul_neg_of_pos_of_neg hbeta_pos_int hneg
        have hvnumQ' : (b.vNum : ℤ) ≤ -(beta * q.Fnum) := by
          rw [← habs_beta_q]; exact hvnumQ
        -- -(beta * q.Fnum) = beta * (-q.Fnum)
        have hrewrite : -(beta * q.Fnum) = beta * (-q.Fnum) := by ring
        -- Now: -p.Fnum < b.vNum ≤ -beta * q.Fnum = beta * (-q.Fnum)
        have h_key : -p.Fnum < beta * (-q.Fnum) := by
          rw [← hrewrite]; exact lt_of_lt_of_le hp_neg_bound hvnumQ'
        -- Rewrite: -p.Fnum < -beta * q.Fnum (since q.Fnum ≤ 0)
        have h_pfnum_lt : -p.Fnum < -(beta * q.Fnum) := by linarith
        -- p.Fnum > beta * q.Fnum
        have h_fnum_rel : p.Fnum > beta * q.Fnum := by linarith
        -- Now use exponent relationship
        -- F2R p = p.Fnum * beta^p.Fexp
        -- F2R q = q.Fnum * beta^q.Fexp
        -- Since q.Fexp ≥ p.Fexp + 1, we have beta^q.Fexp ≥ beta * beta^p.Fexp
        have hexp_ge : q.Fexp ≥ p.Fexp + 1 := hexp_gt
        have hpow_rel : (beta : ℝ) ^ q.Fexp ≥ (beta : ℝ) * (beta : ℝ) ^ p.Fexp := by
          have h1 : (beta : ℝ) ^ q.Fexp ≥ (beta : ℝ) ^ (p.Fexp + 1) := by
            apply zpow_le_zpow_right₀ hbeta_ge_one_real hexp_ge
          have h2 : (beta : ℝ) ^ (p.Fexp + 1) = (beta : ℝ) * (beta : ℝ) ^ p.Fexp := by
            rw [zpow_add₀ (ne_of_gt hbeta_pos), zpow_one]
            ring
          linarith
        -- We need: F2R q ≤ F2R p (contradiction with hLt: F2R p < F2R q)
        -- F2R q = q.Fnum * beta^q.Fexp (q.Fnum ≤ 0)
        -- F2R p = p.Fnum * beta^p.Fexp (p.Fnum < 0)
        -- From h_fnum_rel: p.Fnum > beta * q.Fnum, so p.Fnum - beta * q.Fnum > 0
        -- But both are negative, so |p.Fnum| < |beta * q.Fnum| = beta * |q.Fnum|
        have hq_fnum_neg_or_zero : q.Fnum ≤ 0 := hq_fnum_nonpos
        -- F2R p = p.Fnum * beta^p.Fexp
        -- F2R q = q.Fnum * beta^q.Fexp ≤ q.Fnum * (beta * beta^p.Fexp) = (beta * q.Fnum) * beta^p.Fexp
        -- Since q.Fnum ≤ 0 and beta^q.Fexp ≥ beta * beta^p.Fexp,
        -- q.Fnum * beta^q.Fexp ≤ q.Fnum * (beta * beta^p.Fexp) (inequality flips)
        have hF2Rq_le : (q.Fnum : ℝ) * (beta : ℝ) ^ q.Fexp ≤ (q.Fnum : ℝ) * ((beta : ℝ) * (beta : ℝ) ^ p.Fexp) := by
          apply mul_le_mul_of_nonpos_left hpow_rel
          exact Int.cast_nonpos.mpr hq_fnum_nonpos
        -- (q.Fnum : ℝ) * ((beta : ℝ) * (beta : ℝ) ^ p.Fexp) = (beta * q.Fnum) * beta^p.Fexp
        have heq : (q.Fnum : ℝ) * ((beta : ℝ) * (beta : ℝ) ^ p.Fexp) = ((beta : ℤ) * q.Fnum : ℝ) * (beta : ℝ) ^ p.Fexp := by
          push_cast
          ring
        rw [heq] at hF2Rq_le
        -- From h_fnum_rel: p.Fnum > beta * q.Fnum, so (p.Fnum : ℝ) > (beta * q.Fnum : ℝ)
        have h_fnum_real : (p.Fnum : ℝ) > ((beta : ℤ) * q.Fnum : ℝ) := by
          exact_mod_cast h_fnum_rel
        -- Since beta^p.Fexp > 0 and p.Fnum > beta * q.Fnum:
        -- p.Fnum * beta^p.Fexp > (beta * q.Fnum) * beta^p.Fexp
        have hF2Rp_gt : (p.Fnum : ℝ) * (beta : ℝ) ^ p.Fexp > ((beta : ℤ) * q.Fnum : ℝ) * (beta : ℝ) ^ p.Fexp := by
          apply mul_lt_mul_of_pos_right h_fnum_real hpow_p_pos
        -- Combining: F2R q ≤ (beta * q.Fnum) * beta^p.Fexp < p.Fnum * beta^p.Fexp = F2R p
        linarith
      -- Case 2: p normal, q subnormal
      · exfalso
        rw [hradix] at hvnumP hvnumQ
        -- Similar analysis: q subnormal means |beta * q.Fnum| < b.vNum
        -- p normal means b.vNum ≤ |beta * p.Fnum|
        -- Combined with exponent relation gives contradiction
        have hpow_q_pos : (0 : ℝ) < (beta : ℝ) ^ q.Fexp := zpow_pos hbeta_pos q.Fexp
        have hpow_p_pos : (0 : ℝ) < (beta : ℝ) ^ p.Fexp := zpow_pos hbeta_pos p.Fexp
        -- Since F2R q ≤ 0 and beta^q.Fexp > 0, we have q.Fnum ≤ 0
        have hq_fnum_nonpos : q.Fnum ≤ (0 : ℤ) := by
          by_contra hcontra
          push Not at hcontra
          have hpos : (q.Fnum : ℝ) > 0 := Int.cast_pos.mpr hcontra
          have : (q.Fnum : ℝ) * (beta : ℝ) ^ q.Fexp > 0 := mul_pos hpos hpow_q_pos
          linarith
        -- Since F2R p < F2R q ≤ 0, we have p.Fnum < 0
        have hp_fnum_neg : p.Fnum < (0 : ℤ) := by
          by_contra hcontra
          push Not at hcontra
          have hpos : (p.Fnum : ℝ) ≥ 0 := Int.cast_nonneg hcontra
          have : (p.Fnum : ℝ) * (beta : ℝ) ^ p.Fexp ≥ 0 := mul_nonneg hpos (le_of_lt hpow_p_pos)
          linarith
        -- For p normal: b.vNum ≤ |beta * p.Fnum|
        -- For q subnormal: |beta * q.Fnum| < b.vNum
        have hbeta_pos_int : (0 : ℤ) < beta := lt_trans (by norm_num : (0 : ℤ) < 1) hβ
        -- hvnumQ gives |beta * q.Fnum| < b.vNum
        -- hvnumP gives b.vNum ≤ |beta * p.Fnum|
        -- So |beta * q.Fnum| < |beta * p.Fnum|
        -- i.e., beta * |q.Fnum| < beta * |p.Fnum|
        -- Since beta > 0: |q.Fnum| < |p.Fnum|
        have h_abs_lt : |beta * q.Fnum| < |beta * p.Fnum| := lt_of_lt_of_le hvnumQ hvnumP
        have h_abs_simp : (beta : ℤ) * |q.Fnum| < (beta : ℤ) * |p.Fnum| := by
          simp only [abs_mul, abs_of_pos hbeta_pos_int] at h_abs_lt
          exact h_abs_lt
        have h_qfnum_lt_pfnum : |q.Fnum| < |p.Fnum| := by
          have hbeta_pos' : (0 : ℤ) < beta := hbeta_pos_int
          exact Int.lt_of_mul_lt_mul_left h_abs_simp (le_of_lt hbeta_pos')
        -- Since p.Fnum < 0 and q.Fnum ≤ 0:
        -- |p.Fnum| = -p.Fnum and |q.Fnum| = -q.Fnum (when q.Fnum < 0) or 0 (when q.Fnum = 0)
        have hp_abs : |p.Fnum| = -p.Fnum := abs_of_neg hp_fnum_neg
        rcases eq_or_lt_of_le hq_fnum_nonpos with hq_zero | hq_neg
        · -- q.Fnum = 0 case
          simp only [hq_zero, Int.cast_zero, zero_mul] at hLt
          -- hLt: p.Fnum * beta^p.Fexp < 0
          -- hNeg: 0 ≤ 0, which is fine
          -- We need contradiction: with q.Fnum = 0, F2R q = 0
          -- F2R p < 0, so hLt says F2R p < 0, fine
          -- But then for q.Fexp > p.Fexp with both canonical, contradiction
          -- Since q is subnormal with q.Fnum = 0, and subnormal needs |Fnum| < vNum
          -- This is fine. The contradiction comes from exponent relationship
          -- q subnormal means q.Fexp = -b.dExp
          -- p is normal, so p.Fexp ≥ -b.dExp
          -- But we have p.Fexp < q.Fexp = -b.dExp, contradiction
          have hq_exp : q.Fexp = -b.dExp := hexpQ
          have hp_exp_ge : -b.dExp ≤ p.Fexp := hbP.2
          omega
        · -- q.Fnum < 0 case
          have hq_abs : |q.Fnum| = -q.Fnum := abs_of_neg hq_neg
          -- |q.Fnum| < |p.Fnum| means -q.Fnum < -p.Fnum, i.e., p.Fnum < q.Fnum
          rw [hp_abs, hq_abs] at h_qfnum_lt_pfnum
          have h_pfnum_lt_qfnum : p.Fnum < q.Fnum := by omega
          -- Now: p.Fnum < q.Fnum (both negative)
          -- And: p.Fexp < q.Fexp
          -- For negative floats, F2R = Fnum * beta^Fexp
          -- F2R p = p.Fnum * beta^p.Fexp (negative)
          -- F2R q = q.Fnum * beta^q.Fexp (negative or zero)
          -- Since p.Fnum < q.Fnum < 0 and p.Fexp < q.Fexp:
          -- |F2R p| = |p.Fnum| * beta^p.Fexp
          -- |F2R q| = |q.Fnum| * beta^q.Fexp
          -- We have |q.Fnum| < |p.Fnum| and beta^q.Fexp > beta^p.Fexp
          -- Need to show |F2R q| < |F2R p|, i.e., F2R q > F2R p (since both negative)
          -- But hLt says F2R p < F2R q, contradiction
          have hbeta_ge_one : (1 : ℝ) ≤ (beta : ℝ) := by
            have h1le : (1 : ℤ) ≤ beta := le_of_lt hβ
            exact_mod_cast h1le
          have hexp_ge : q.Fexp ≥ p.Fexp + 1 := hexp_gt
          have hpow_rel : (beta : ℝ) ^ q.Fexp ≥ (beta : ℝ) * (beta : ℝ) ^ p.Fexp := by
            calc (beta : ℝ) ^ q.Fexp ≥ (beta : ℝ) ^ (p.Fexp + 1) := by
                  apply zpow_le_zpow_right₀ hbeta_ge_one hexp_ge
              _ = (beta : ℝ) ^ p.Fexp * (beta : ℝ) := by
                  rw [zpow_add₀ (ne_of_gt hbeta_pos), zpow_one]
              _ = (beta : ℝ) * (beta : ℝ) ^ p.Fexp := by ring
          -- F2R q = q.Fnum * beta^q.Fexp ≤ q.Fnum * (beta * beta^p.Fexp) (since q.Fnum ≤ 0)
          have hF2Rq_le : (q.Fnum : ℝ) * (beta : ℝ) ^ q.Fexp ≤ (q.Fnum : ℝ) * ((beta : ℝ) * (beta : ℝ) ^ p.Fexp) := by
            apply mul_le_mul_of_nonpos_left hpow_rel
            exact Int.cast_nonpos.mpr (le_of_lt hq_neg)
          -- (q.Fnum : ℝ) * ((beta : ℝ) * (beta : ℝ) ^ p.Fexp) = (beta * q.Fnum) * beta^p.Fexp
          have heq : (q.Fnum : ℝ) * ((beta : ℝ) * (beta : ℝ) ^ p.Fexp) = ((beta : ℤ) * q.Fnum : ℝ) * (beta : ℝ) ^ p.Fexp := by
            push_cast; ring
          rw [heq] at hF2Rq_le
          -- From |q.Fnum| < |p.Fnum| and both negative:
          -- -q.Fnum < -p.Fnum, so p.Fnum < q.Fnum
          -- beta * q.Fnum > beta * p.Fnum (since beta > 0 and q.Fnum > p.Fnum with both negative)
          -- Wait, q.Fnum > p.Fnum means q.Fnum - p.Fnum > 0
          -- beta * q.Fnum - beta * p.Fnum = beta * (q.Fnum - p.Fnum) > 0 since beta > 0
          -- So beta * q.Fnum > beta * p.Fnum
          have h_beta_qfnum_gt : (beta : ℤ) * q.Fnum > (beta : ℤ) * p.Fnum := by
            have := Int.mul_lt_mul_of_pos_left h_pfnum_lt_qfnum hbeta_pos_int
            linarith
          have h_beta_qfnum_gt_real : ((beta : ℤ) * q.Fnum : ℝ) > ((beta : ℤ) * p.Fnum : ℝ) := by
            exact_mod_cast h_beta_qfnum_gt
          -- p.Fnum * beta^p.Fexp > (beta * q.Fnum) * beta^p.Fexp is FALSE
          -- Actually: (beta * p.Fnum) * beta^p.Fexp < (beta * q.Fnum) * beta^p.Fexp
          -- p.Fnum < q.Fnum means p.Fnum < q.Fnum (both negative)
          -- So beta * p.Fnum < beta * q.Fnum (since beta > 0)
          -- Wait, I had the inequality direction wrong above.
          -- From h_pfnum_lt_qfnum: p.Fnum < q.Fnum
          -- So p.Fnum * beta^p.Fexp < q.Fnum * beta^p.Fexp (multiply by positive)
          -- Combined with F2R q ≤ (beta * q.Fnum) * beta^p.Fexp:
          -- We need to compare p.Fnum * beta^p.Fexp with (beta * q.Fnum) * beta^p.Fexp
          -- p.Fnum vs beta * q.Fnum
          -- From h_qfnum_lt_pfnum: |q.Fnum| < |p.Fnum|
          -- i.e., -q.Fnum < -p.Fnum (since both negative), so p.Fnum < q.Fnum (good)
          -- For beta > 1 and q.Fnum < 0:
          -- beta * q.Fnum < q.Fnum < p.Fnum (since beta > 1 and q.Fnum < 0)
          -- Wait: beta * q.Fnum when beta > 1 and q.Fnum < 0:
          -- beta * q.Fnum = beta * (negative) = more negative < q.Fnum
          have h_beta_q_lt_q : (beta : ℤ) * q.Fnum < q.Fnum := by
            have h1 : (1 : ℤ) * q.Fnum = q.Fnum := one_mul q.Fnum
            have hmul : beta * q.Fnum < 1 * q.Fnum := Int.mul_lt_mul_of_neg_right hβ hq_neg
            linarith
          -- So beta * q.Fnum < q.Fnum
          -- And p.Fnum < q.Fnum
          -- Thus we can't directly compare beta * q.Fnum with p.Fnum
          -- Let's use the bound from subnormal/normal differently
          -- Actually, using the exponent bound:
          -- q is subnormal: q.Fexp = -b.dExp
          -- p is normal: p.Fexp ≥ -b.dExp, i.e., p.Fexp ≥ -b.dExp
          -- But we have p.Fexp < q.Fexp
          -- q.Fexp = -b.dExp, so p.Fexp < -b.dExp
          -- But p normal means -b.dExp ≤ p.Fexp
          -- Contradiction!
          have hq_exp : q.Fexp = -b.dExp := hexpQ
          have hp_exp_ge : -b.dExp ≤ p.Fexp := hbP.2
          omega
      -- Case 3: p subnormal, q normal
      · exfalso
        rw [hradix] at hvnumP hvnumQ
        -- p subnormal: p.Fexp = -b.dExp, |beta * p.Fnum| < b.vNum
        -- q normal: -b.dExp ≤ q.Fexp, b.vNum ≤ |beta * q.Fnum|
        -- We have p.Fexp < q.Fexp
        -- From p.Fexp = -b.dExp and -b.dExp ≤ q.Fexp, this is consistent
        -- Need to derive contradiction from the bounds
        have hpow_q_pos : (0 : ℝ) < (beta : ℝ) ^ q.Fexp := zpow_pos hbeta_pos q.Fexp
        have hpow_p_pos : (0 : ℝ) < (beta : ℝ) ^ p.Fexp := zpow_pos hbeta_pos p.Fexp
        have hbeta_pos_int : (0 : ℤ) < beta := lt_trans (by norm_num : (0 : ℤ) < 1) hβ
        -- Since F2R q ≤ 0 and beta^q.Fexp > 0, we have q.Fnum ≤ 0
        have hq_fnum_nonpos : q.Fnum ≤ (0 : ℤ) := by
          by_contra hcontra
          push Not at hcontra
          have hpos : (q.Fnum : ℝ) > 0 := Int.cast_pos.mpr hcontra
          have : (q.Fnum : ℝ) * (beta : ℝ) ^ q.Fexp > 0 := mul_pos hpos hpow_q_pos
          linarith
        -- Since F2R p < F2R q ≤ 0, we have p.Fnum < 0
        have hp_fnum_neg : p.Fnum < (0 : ℤ) := by
          by_contra hcontra
          push Not at hcontra
          have hpos : (p.Fnum : ℝ) ≥ 0 := Int.cast_nonneg hcontra
          have : (p.Fnum : ℝ) * (beta : ℝ) ^ p.Fexp ≥ 0 := mul_nonneg hpos (le_of_lt hpow_p_pos)
          linarith
        -- From hvnumP: |beta * p.Fnum| < b.vNum
        -- From hvnumQ: b.vNum ≤ |beta * q.Fnum|
        -- So |beta * p.Fnum| < |beta * q.Fnum|
        -- i.e., beta * |p.Fnum| < beta * |q.Fnum|
        -- Since beta > 0: |p.Fnum| < |q.Fnum|
        have h_abs_lt : |beta * p.Fnum| < |beta * q.Fnum| := lt_of_lt_of_le hvnumP hvnumQ
        have h_abs_simp : (beta : ℤ) * |p.Fnum| < (beta : ℤ) * |q.Fnum| := by
          simp only [abs_mul, abs_of_pos hbeta_pos_int] at h_abs_lt
          exact h_abs_lt
        have h_pfnum_lt_qfnum_abs : |p.Fnum| < |q.Fnum| := by
          exact Int.lt_of_mul_lt_mul_left h_abs_simp (le_of_lt hbeta_pos_int)
        -- Since p.Fnum < 0 and q.Fnum ≤ 0:
        -- |p.Fnum| = -p.Fnum and |q.Fnum| = -q.Fnum (when q.Fnum < 0)
        have hp_abs : |p.Fnum| = -p.Fnum := abs_of_neg hp_fnum_neg
        rcases eq_or_lt_of_le hq_fnum_nonpos with hq_zero | hq_neg
        · -- q.Fnum = 0: |q.Fnum| = 0, but |p.Fnum| < |q.Fnum| = 0 contradicts |p.Fnum| ≥ 0
          rw [hq_zero] at h_pfnum_lt_qfnum_abs
          simp only [abs_zero] at h_pfnum_lt_qfnum_abs
          exact absurd h_pfnum_lt_qfnum_abs (not_lt.mpr (abs_nonneg p.Fnum))
        · -- q.Fnum < 0
          have hq_abs : |q.Fnum| = -q.Fnum := abs_of_neg hq_neg
          -- |p.Fnum| < |q.Fnum| means -p.Fnum < -q.Fnum, i.e., q.Fnum < p.Fnum
          rw [hp_abs, hq_abs] at h_pfnum_lt_qfnum_abs
          have h_qfnum_lt_pfnum : q.Fnum < p.Fnum := by omega
          -- Now: q.Fnum < p.Fnum (both negative)
          -- And: p.Fexp < q.Fexp
          -- We have p.Fexp = -b.dExp (subnormal)
          -- And hexp_gt: p.Fexp < q.Fexp
          -- q is normal: -b.dExp ≤ q.Fexp
          -- So q.Fexp > p.Fexp = -b.dExp, which is consistent
          -- Now show F2R p ≥ F2R q (contradiction with hLt)
          have hbeta_ge_one : (1 : ℝ) ≤ (beta : ℝ) := by
            have h1le : (1 : ℤ) ≤ beta := le_of_lt hβ
            exact_mod_cast h1le
          have hexp_ge : q.Fexp ≥ p.Fexp + 1 := hexp_gt
          have hpow_rel : (beta : ℝ) ^ q.Fexp ≥ (beta : ℝ) * (beta : ℝ) ^ p.Fexp := by
            calc (beta : ℝ) ^ q.Fexp ≥ (beta : ℝ) ^ (p.Fexp + 1) := by
                  apply zpow_le_zpow_right₀ hbeta_ge_one hexp_ge
              _ = (beta : ℝ) ^ p.Fexp * (beta : ℝ) := by
                  rw [zpow_add₀ (ne_of_gt hbeta_pos), zpow_one]
              _ = (beta : ℝ) * (beta : ℝ) ^ p.Fexp := by ring
          -- F2R p = p.Fnum * beta^p.Fexp
          -- F2R q = q.Fnum * beta^q.Fexp ≤ q.Fnum * (beta * beta^p.Fexp) (since q.Fnum ≤ 0)
          have hF2Rq_le : (q.Fnum : ℝ) * (beta : ℝ) ^ q.Fexp ≤ (q.Fnum : ℝ) * ((beta : ℝ) * (beta : ℝ) ^ p.Fexp) := by
            apply mul_le_mul_of_nonpos_left hpow_rel
            exact Int.cast_nonpos.mpr (le_of_lt hq_neg)
          have heq : (q.Fnum : ℝ) * ((beta : ℝ) * (beta : ℝ) ^ p.Fexp) = ((beta : ℤ) * q.Fnum : ℝ) * (beta : ℝ) ^ p.Fexp := by
            push_cast; ring
          rw [heq] at hF2Rq_le
          -- From h_qfnum_lt_pfnum: q.Fnum < p.Fnum
          -- beta * q.Fnum < beta * p.Fnum (since beta > 0)
          have h_beta_qfnum_lt : (beta : ℤ) * q.Fnum < (beta : ℤ) * p.Fnum := by
            have := Int.mul_lt_mul_of_pos_left h_qfnum_lt_pfnum hbeta_pos_int
            linarith
          have h_beta_qfnum_lt_real : ((beta : ℤ) * q.Fnum : ℝ) < ((beta : ℤ) * p.Fnum : ℝ) := by
            exact_mod_cast h_beta_qfnum_lt
          -- (beta * q.Fnum) * beta^p.Fexp < (beta * p.Fnum) * beta^p.Fexp
          have h1 : ((beta : ℤ) * q.Fnum : ℝ) * (beta : ℝ) ^ p.Fexp < ((beta : ℤ) * p.Fnum : ℝ) * (beta : ℝ) ^ p.Fexp := by
            apply mul_lt_mul_of_pos_right h_beta_qfnum_lt_real hpow_p_pos
          -- (beta * p.Fnum) * beta^p.Fexp = p.Fnum * (beta * beta^p.Fexp)
          -- But we want to compare with p.Fnum * beta^p.Fexp
          -- Note: (beta * p.Fnum) * beta^p.Fexp = p.Fnum * beta^(p.Fexp+1)
          -- And p.Fnum < 0, so p.Fnum * beta^(p.Fexp+1) < p.Fnum * beta^p.Fexp
          -- (since beta^(p.Fexp+1) > beta^p.Fexp and p.Fnum < 0)
          have h_pexp_rel : (beta : ℝ) ^ (p.Fexp + 1) = (beta : ℝ) * (beta : ℝ) ^ p.Fexp := by
            rw [zpow_add₀ (ne_of_gt hbeta_pos), zpow_one]; ring
          have h_pexp_gt : (beta : ℝ) ^ (p.Fexp + 1) > (beta : ℝ) ^ p.Fexp := by
            have hbeta_gt_one : (1 : ℝ) < (beta : ℝ) := by exact_mod_cast hβ
            exact zpow_lt_zpow_right₀ hbeta_gt_one (by omega : p.Fexp < p.Fexp + 1)
          -- p.Fnum * beta^(p.Fexp+1) < p.Fnum * beta^p.Fexp (since p.Fnum < 0)
          have h2 : (p.Fnum : ℝ) * (beta : ℝ) ^ (p.Fexp + 1) < (p.Fnum : ℝ) * (beta : ℝ) ^ p.Fexp := by
            apply mul_lt_mul_of_neg_left h_pexp_gt
            exact Int.cast_lt_zero.mpr hp_fnum_neg
          -- Rewrite (beta * p.Fnum) * beta^p.Fexp = p.Fnum * beta^(p.Fexp+1)
          have heq2 : ((beta : ℤ) * p.Fnum : ℝ) * (beta : ℝ) ^ p.Fexp = (p.Fnum : ℝ) * (beta : ℝ) ^ (p.Fexp + 1) := by
            push_cast
            rw [h_pexp_rel]
            ring
          rw [heq2] at h1
          -- h1: (beta * q.Fnum) * beta^p.Fexp < p.Fnum * beta^(p.Fexp+1)
          -- h2: p.Fnum * beta^(p.Fexp+1) < p.Fnum * beta^p.Fexp
          -- hF2Rq_le: F2R q ≤ (beta * q.Fnum) * beta^p.Fexp
          -- Chain: F2R q ≤ (beta * q.Fnum) * beta^p.Fexp < p.Fnum * beta^(p.Fexp+1) < p.Fnum * beta^p.Fexp = F2R p
          -- So F2R q < F2R p, contradicting hLt: F2R p < F2R q
          linarith
      -- Case 4: Both subnormal
      · -- Both p and q are subnormal
        -- p.Fexp = -b.dExp and q.Fexp = -b.dExp
        -- But we have p.Fexp < q.Fexp, contradiction
        have hp_exp : p.Fexp = -b.dExp := hexpP
        have hq_exp : q.Fexp = -b.dExp := hexpQ
        omega

-- Rounding operators on nonnegative reals and their variants
noncomputable def RND_Min_Pos {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int)
    (r : ℝ) : FloatSpec.Core.Defs.FlocqFloat beta :=
  let firstNormPosValue : ℝ := _root_.F2R (beta:=beta) (firstNormalPos (beta:=beta) radix b p.toNat)
  if firstNormPosValue ≤ r then
    -- Normal case: compute exponent dynamically
    let e : Int := IRNDD (Real.log r / Real.log radix + (-(p - 1)))
    ⟨IRNDD (r * (radix : ℝ) ^ (-e)), e⟩
  else
    -- Subnormal case: fixed exponent
    ⟨IRNDD (r * (radix : ℝ) ^ (b.dExp)), -b.dExp⟩

-- ---------------------------------------------------------------------------
-- Normalization helper (Coq: Fnormalize) and its basic properties

/-- Coq: `Fulp b radix precision p`.

Flocq defines the float ulp as the radix power at the exponent of the
normalized representative of `p`. -/
noncomputable def Fulp {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : ℝ :=
  (radix : ℝ) ^ (Fnormalize (beta:=beta) radix b precision p).Fexp

noncomputable def FulpFabs_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FulpFabs` — the ulp is invariant under float absolute value. -/
theorem FulpFabs {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜0 < radix⌝⦄
    (pure (FulpFabs_check (beta:=beta) b radix precision p) : Id Unit)
    ⦃⇓_ => ⌜Fulp (beta:=beta) b radix precision p =
      Fulp (beta:=beta) b radix precision (Fabs (beta:=beta) p)⌝⦄ := by
  intro hradix_pos
  simp only [wp, PostCond.noThrow, pure, FulpFabs_check, Id.run,
    ULift.up_down]
  have hnorm := Fnormalize_Fabs (beta:=beta) radix b precision p
  have hnorm' :
      Fnormalize (beta:=beta) radix b precision (Fabs (beta:=beta) p) =
        Fabs (beta:=beta) (Fnormalize (beta:=beta) radix b precision p) := by
    simpa only [wp, PostCond.noThrow, pure, Fnormalize_Fabs_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Id.run,
      ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using hnorm hradix_pos
  change (radix : ℝ) ^ (Fnormalize (beta:=beta) radix b precision p).Fexp =
    (radix : ℝ) ^
      (Fnormalize (beta:=beta) radix b precision (Fabs (beta:=beta) p)).Fexp
  rw [hnorm']
  simp [Fabs, FloatSpec.Calc.Operations.Fabs]

noncomputable def FulpLe_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

noncomputable def FulpLe2_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

noncomputable def FulpLeGeneral_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

noncomputable def RoundLeGeneral_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) (z : ℝ) : Unit :=
  ()

noncomputable def Axpy_scale_from_round_and_lower_check
    (eps A c T Y U : ℝ) : Unit :=
  ()

noncomputable def Axpy_u_lower_from_sum_error_check
    (eps T Y U : ℝ) : Unit :=
  ()

noncomputable def Axpy_sum_error_from_closest_ulp_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (u : FloatSpec.Core.Defs.FlocqFloat beta) (S : ℝ) : Unit :=
  ()

noncomputable def Axpy_u_lower_from_closest_sum_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (u : FloatSpec.Core.Defs.FlocqFloat beta) (T Y : ℝ) : Unit :=
  ()

noncomputable def Axpy_scale_from_rounding_inputs_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (t u : FloatSpec.Core.Defs.FlocqFloat beta) (A Y : ℝ) : Unit :=
  ()

noncomputable def Axpy_perturb_from_error_bound_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (a x y u : FloatSpec.Core.Defs.FlocqFloat beta)
    (a1 x1 y1 E : ℝ) : Unit :=
  ()

noncomputable def Axpy_min_or_max_from_rounding_inputs_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (a x y t u : FloatSpec.Core.Defs.FlocqFloat beta)
    (a1 x1 y1 E : ℝ) : Unit :=
  ()

noncomputable def Axpy_opt_from_strict_bound_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (a x y t u : FloatSpec.Core.Defs.FlocqFloat beta)
    (a1 x1 y1 E : ℝ) : Unit :=
  ()

noncomputable def Axpy_opt_from_general_bound_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (a x y t u : FloatSpec.Core.Defs.FlocqFloat beta)
    (a1 x1 y1 E : ℝ) : Unit :=
  ()

noncomputable def Axpy_opt_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (a x y t u : FloatSpec.Core.Defs.FlocqFloat beta)
    (a1 x1 y1 : ℝ) : Unit :=
  ()

noncomputable def ExactSum_Near_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (p q f : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FulpLe` — the normalized ulp of a bounded float is no larger than
the radix power at the float's current exponent. -/
theorem FulpLe {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b p ∧ beta = radix ∧ 1 < radix⌝⦄
    (pure (FulpLe_check (beta:=beta) radix b precision p) : Id Unit)
    ⦃⇓_ => ⌜Fulp (beta:=beta) b radix precision p ≤ (beta : ℝ) ^ p.Fexp⌝⦄ := by
  intro h
  rcases h with ⟨hpBound, hbeta, hradix⟩
  simp only [wp, PostCond.noThrow, pure, FulpLe_check,
    Id.run, ULift.up_down]
  have hnorm_exp_le :
      (Fnormalize (beta:=beta) radix b precision p).Fexp ≤ p.Fexp := by
    unfold Fnormalize
    by_cases hp_zero : p.Fnum = 0
    · simp [hp_zero]
      exact hpBound.2
    · simp [hp_zero, Fshift]
  subst beta
  have hbase_ge_one : (1 : ℝ) ≤ (radix : ℝ) := by
    exact_mod_cast (le_of_lt hradix)
  simpa [Fulp] using zpow_le_zpow_right₀ hbase_ge_one hnorm_exp_le

noncomputable def FnormalizeCorrect_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

noncomputable def FnormalizeBounded_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

noncomputable def FnormalizeCanonic_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

noncomputable def FcanonicFnormalizeEq_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

noncomputable def FcanonicPosFexpRlt_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FcanonicPosFexpRlt` — among nonnegative canonical floats, a strictly
smaller exponent yields a strictly smaller value.

Note: Uses `Fcanonic'`, the Coq-matching canonical predicate.
Requires `radix = beta` and `1 < beta` for the mantissa bounds to imply the result. -/
theorem FcanonicPosFexpRlt {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta)
    (hβ : 1 < beta)
    (hradix : radix = beta) :
    ⦃⌜Fcanonic' (beta:=beta) radix b x ∧
        Fcanonic' (beta:=beta) radix b y ∧
        0 ≤ _root_.F2R (beta:=beta) x ∧
        0 ≤ _root_.F2R (beta:=beta) y ∧
        x.Fexp < y.Fexp⌝⦄
    (pure (FcanonicPosFexpRlt_check (beta:=beta) radix b x y) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) x < _root_.F2R (beta:=beta) y⌝⦄ := by
  intro ⟨hcanX, hcanY, hPosX, hPosY, hExpLt⟩
  simp only [wp, PostCond.noThrow, pure, FcanonicPosFexpRlt_check, ULift.down_up]
  -- Prove by contradiction: assume F2R y ≤ F2R x
  by_contra h_not_lt
  have h_not_lt' : _root_.F2R (beta:=beta) y ≤ _root_.F2R (beta:=beta) x := le_of_not_gt h_not_lt
  -- Case split: F2R y < F2R x or F2R y = F2R x
  rcases lt_or_eq_of_le h_not_lt' with h_lt | h_eq
  · -- Case: F2R y < F2R x (with F2R y ≥ 0)
    -- Apply FcanonicLtPos with p = y, q = x to get: y.Fexp < x.Fexp ∨ (y.Fexp = x.Fexp ∧ y.Fnum < x.Fnum)
    -- First we need to use FcanonicLtPos as a direct implication
    have hLtPos : _root_.F2R (beta:=beta) y < _root_.F2R (beta:=beta) x := h_lt
    -- Extract the conclusion from FcanonicLtPos
    -- FcanonicLtPos says: given canonical y, x with 0 ≤ F2R y and F2R y < F2R x,
    -- we get y.Fexp < x.Fexp ∨ (y.Fexp = x.Fexp ∧ y.Fnum < x.Fnum)
    have hcanY' := hcanY
    have hcanX' := hcanX
    -- We have hExpLt: x.Fexp < y.Fexp
    -- The conclusion would give y.Fexp < x.Fexp (contradicts) or y.Fexp = x.Fexp (contradicts)
    -- Use the structural properties from canonical form
    -- This requires the actual FcanonicLtPos logic
    -- For now, derive contradiction from exponent ordering
    -- From canonical forms, the exponent-value relationship holds
    -- Since both are nonnegative and x.Fexp < y.Fexp, by canonical bounds F2R x < F2R y
    -- But we assumed F2R y ≤ F2R x, contradiction
    -- Need to use the bounds from Fcanonic'
    rcases hcanX with ⟨hbX, hvnumX⟩ | ⟨hbX, hexpX, hvnumX⟩
    · -- x is normal
      rcases hcanY with ⟨hbY, hvnumY⟩ | ⟨hbY, hexpY, hvnumY⟩
      · -- y is normal: both normal
        -- Normal: Fbounded' and vNum ≤ |radix * Fnum|
        -- F2R x = x.Fnum * beta^x.Fexp, F2R y = y.Fnum * beta^y.Fexp
        -- From 0 ≤ F2R x, F2R y and beta > 0, we have x.Fnum, y.Fnum ≥ 0 (when Fexp gives positive power)
        -- Actually, F2R can be 0 when Fnum = 0
        have hbeta_pos : (0 : ℝ) < (beta : ℝ) := by
          have : (0 : Int) < beta := lt_trans (by norm_num : (0 : Int) < 1) hβ
          exact Int.cast_pos.mpr this
        simp only [_root_.F2R, FloatSpec.Core.Defs.F2R] at h_lt hPosX hPosY
        have hpow_x_pos : (0 : ℝ) < (beta : ℝ) ^ x.Fexp := zpow_pos hbeta_pos x.Fexp
        have hpow_y_pos : (0 : ℝ) < (beta : ℝ) ^ y.Fexp := zpow_pos hbeta_pos y.Fexp
        -- From 0 ≤ x.Fnum * beta^x.Fexp and beta^x.Fexp > 0: x.Fnum ≥ 0
        have hx_fnum_nonneg : (0 : ℤ) ≤ x.Fnum := by
          by_contra hcontra
          push Not at hcontra
          have hx_neg : (x.Fnum : ℝ) < 0 := Int.cast_lt_zero.mpr hcontra
          have : (x.Fnum : ℝ) * (beta : ℝ) ^ x.Fexp < 0 := mul_neg_of_neg_of_pos hx_neg hpow_x_pos
          linarith
        have hy_fnum_nonneg : (0 : ℤ) ≤ y.Fnum := by
          by_contra hcontra
          push Not at hcontra
          have hy_neg : (y.Fnum : ℝ) < 0 := Int.cast_lt_zero.mpr hcontra
          have : (y.Fnum : ℝ) * (beta : ℝ) ^ y.Fexp < 0 := mul_neg_of_neg_of_pos hy_neg hpow_y_pos
          linarith
        -- From normal: vNum ≤ |radix * Fnum|
        -- With radix = beta and Fnum ≥ 0: vNum ≤ beta * Fnum
        rw [hradix] at hvnumX hvnumY
        have hvnumX' : (b.vNum : ℤ) ≤ beta * x.Fnum := by
          have h1 : |beta * x.Fnum| = beta * x.Fnum := by
            apply abs_of_nonneg
            apply mul_nonneg
            · exact le_of_lt (lt_trans (by norm_num : (0 : ℤ) < 1) hβ)
            · exact hx_fnum_nonneg
          rw [← h1]; exact hvnumX
        have hvnumY' : (b.vNum : ℤ) ≤ beta * y.Fnum := by
          have h1 : |beta * y.Fnum| = beta * y.Fnum := by
            apply abs_of_nonneg
            apply mul_nonneg
            · exact le_of_lt (lt_trans (by norm_num : (0 : ℤ) < 1) hβ)
            · exact hy_fnum_nonneg
          rw [← h1]; exact hvnumY
        -- From bounded: |Fnum| < vNum
        have hbX' := hbX.1
        have hbY' := hbY.1
        have hx_fnum_bound : x.Fnum < b.vNum := by
          have h1 : |x.Fnum| = x.Fnum := abs_of_nonneg hx_fnum_nonneg
          rw [← h1]; exact hbX'
        have hy_fnum_bound : y.Fnum < b.vNum := by
          have h1 : |y.Fnum| = y.Fnum := abs_of_nonneg hy_fnum_nonneg
          rw [← h1]; exact hbY'
        -- From hExpLt: x.Fexp < y.Fexp, so y.Fexp ≥ x.Fexp + 1
        have hexp_ge : y.Fexp ≥ x.Fexp + 1 := hExpLt
        -- F2R y = y.Fnum * beta^y.Fexp ≥ (vNum/beta) * beta^y.Fexp = vNum * beta^(y.Fexp-1)
        -- F2R x = x.Fnum * beta^x.Fexp < vNum * beta^x.Fexp
        -- Since y.Fexp ≥ x.Fexp + 1, beta^(y.Fexp-1) ≥ beta^x.Fexp
        -- So F2R y ≥ vNum * beta^(y.Fexp-1) ≥ vNum * beta^x.Fexp > F2R x
        -- This shows F2R y > F2R x, contradicting h_lt
        have hbeta_ge_one : (1 : ℝ) ≤ (beta : ℝ) := by
          have h1le : (1 : ℤ) ≤ beta := le_of_lt hβ
          exact_mod_cast h1le
        have hpow_y_ge : (beta : ℝ) ^ y.Fexp ≥ (beta : ℝ) * (beta : ℝ) ^ x.Fexp := by
          calc (beta : ℝ) ^ y.Fexp ≥ (beta : ℝ) ^ (x.Fexp + 1) := by
                apply zpow_le_zpow_right₀ hbeta_ge_one hexp_ge
            _ = (beta : ℝ) ^ x.Fexp * (beta : ℝ) := by
                rw [zpow_add₀ (ne_of_gt hbeta_pos), zpow_one]
            _ = (beta : ℝ) * (beta : ℝ) ^ x.Fexp := by ring
        -- F2R y = y.Fnum * beta^y.Fexp
        -- From vNum ≤ beta * y.Fnum: y.Fnum ≥ vNum / beta
        have hvNum_pos : (0 : ℤ) < b.vNum := by
          -- vNum ≤ beta * y.Fnum and y.Fnum < vNum
          -- So vNum ≤ beta * (vNum - 1) is possible if beta ≥ 2
          -- A stronger bound descriptor would expose `0 < vNum` directly.
          -- For now assume vNum > 0 from beta > 1 and the bounds
          by_contra hcontra
          push Not at hcontra
          have hvNum_nonpos : b.vNum ≤ 0 := hcontra
          have hcontra2 : (b.vNum : ℤ) ≤ beta * y.Fnum := hvnumY'
          have hcontra3 : beta * y.Fnum < beta * (b.vNum : ℤ) := by
            have := Int.mul_lt_mul_of_pos_left hy_fnum_bound (lt_trans (by norm_num : (0 : ℤ) < 1) hβ)
            linarith
          linarith
        have hy_fnum_pos_or_zero : y.Fnum = 0 ∨ 0 < y.Fnum := by
          rcases eq_or_lt_of_le hy_fnum_nonneg with h | h
          · left; omega
          · right; exact h
        rcases hy_fnum_pos_or_zero with hy_zero | hy_pos
        · -- y.Fnum = 0: F2R y = 0
          -- But vNum ≤ beta * 0 = 0, contradiction with vNum > 0
          rw [hy_zero] at hvnumY'
          simp at hvnumY'
          omega
        · -- y.Fnum > 0
          -- F2R y = y.Fnum * beta^y.Fexp
          -- F2R x = x.Fnum * beta^x.Fexp < vNum * beta^x.Fexp (since x.Fnum < vNum)
          have hF2Rx_bound : (x.Fnum : ℝ) * (beta : ℝ) ^ x.Fexp < (b.vNum : ℝ) * (beta : ℝ) ^ x.Fexp := by
            apply mul_lt_mul_of_pos_right _ hpow_x_pos
            exact_mod_cast hx_fnum_bound
          -- From vNum ≤ beta * y.Fnum: vNum / beta ≤ y.Fnum
          -- So F2R y = y.Fnum * beta^y.Fexp ≥ (vNum/beta) * beta^y.Fexp
          --         = vNum * beta^(y.Fexp - 1)
          -- Since y.Fexp ≥ x.Fexp + 1: y.Fexp - 1 ≥ x.Fexp
          -- So vNum * beta^(y.Fexp-1) ≥ vNum * beta^x.Fexp > F2R x
          have hexp_y_minus_1_ge : y.Fexp - 1 ≥ x.Fexp := by omega
          have hpow_y_minus_1_ge : (beta : ℝ) ^ (y.Fexp - 1) ≥ (beta : ℝ) ^ x.Fexp := by
            apply zpow_le_zpow_right₀ hbeta_ge_one hexp_y_minus_1_ge
          -- y.Fnum ≥ vNum / beta (in reals)
          have hy_fnum_ge : (y.Fnum : ℝ) ≥ (b.vNum : ℝ) / (beta : ℝ) := by
            have h1 : (b.vNum : ℝ) ≤ (beta : ℝ) * (y.Fnum : ℝ) := by exact_mod_cast hvnumY'
            have hbeta_pos' : (0 : ℝ) < (beta : ℝ) := hbeta_pos
            calc (y.Fnum : ℝ) = (beta : ℝ) * (y.Fnum : ℝ) / (beta : ℝ) := by field_simp
                 _ ≥ (b.vNum : ℝ) / (beta : ℝ) := by apply div_le_div_of_nonneg_right h1 (le_of_lt hbeta_pos')
          -- F2R y = y.Fnum * beta^y.Fexp
          --       ≥ (vNum / beta) * beta^y.Fexp
          --       = vNum * beta^(y.Fexp - 1)
          have hF2Ry_ge : (y.Fnum : ℝ) * (beta : ℝ) ^ y.Fexp ≥ (b.vNum : ℝ) * (beta : ℝ) ^ (y.Fexp - 1) := by
            calc (y.Fnum : ℝ) * (beta : ℝ) ^ y.Fexp
                ≥ ((b.vNum : ℝ) / (beta : ℝ)) * (beta : ℝ) ^ y.Fexp := by
                    apply mul_le_mul_of_nonneg_right hy_fnum_ge (le_of_lt hpow_y_pos)
              _ = (b.vNum : ℝ) * ((beta : ℝ) ^ y.Fexp / (beta : ℝ)) := by ring
              _ = (b.vNum : ℝ) * (beta : ℝ) ^ (y.Fexp - 1) := by
                    congr 1
                    rw [zpow_sub₀ (ne_of_gt hbeta_pos), zpow_one]
          have hF2Ry_ge' : (b.vNum : ℝ) * (beta : ℝ) ^ (y.Fexp - 1) ≥ (b.vNum : ℝ) * (beta : ℝ) ^ x.Fexp := by
            apply mul_le_mul_of_nonneg_left hpow_y_minus_1_ge
            exact_mod_cast (le_of_lt hvNum_pos)
          -- Chain: F2R y ≥ vNum * beta^(y.Fexp-1) ≥ vNum * beta^x.Fexp > F2R x
          have hF2Ry_gt_F2Rx : (y.Fnum : ℝ) * (beta : ℝ) ^ y.Fexp > (x.Fnum : ℝ) * (beta : ℝ) ^ x.Fexp := by
            calc (y.Fnum : ℝ) * (beta : ℝ) ^ y.Fexp
                ≥ (b.vNum : ℝ) * (beta : ℝ) ^ (y.Fexp - 1) := hF2Ry_ge
              _ ≥ (b.vNum : ℝ) * (beta : ℝ) ^ x.Fexp := hF2Ry_ge'
              _ > (x.Fnum : ℝ) * (beta : ℝ) ^ x.Fexp := hF2Rx_bound
          linarith
      · -- x normal, y subnormal
        -- Similar reasoning but y has smaller mantissa bound
        have hbeta_pos : (0 : ℝ) < (beta : ℝ) := by
          have : (0 : Int) < beta := lt_trans (by norm_num : (0 : Int) < 1) hβ
          exact Int.cast_pos.mpr this
        simp only [_root_.F2R, FloatSpec.Core.Defs.F2R] at h_lt hPosX hPosY
        have hpow_x_pos : (0 : ℝ) < (beta : ℝ) ^ x.Fexp := zpow_pos hbeta_pos x.Fexp
        have hpow_y_pos : (0 : ℝ) < (beta : ℝ) ^ y.Fexp := zpow_pos hbeta_pos y.Fexp
        have hx_fnum_nonneg : (0 : ℤ) ≤ x.Fnum := by
          by_contra hcontra
          push Not at hcontra
          have hx_neg : (x.Fnum : ℝ) < 0 := Int.cast_lt_zero.mpr hcontra
          have : (x.Fnum : ℝ) * (beta : ℝ) ^ x.Fexp < 0 := mul_neg_of_neg_of_pos hx_neg hpow_x_pos
          linarith
        have hy_fnum_nonneg : (0 : ℤ) ≤ y.Fnum := by
          by_contra hcontra
          push Not at hcontra
          have hy_neg : (y.Fnum : ℝ) < 0 := Int.cast_lt_zero.mpr hcontra
          have : (y.Fnum : ℝ) * (beta : ℝ) ^ y.Fexp < 0 := mul_neg_of_neg_of_pos hy_neg hpow_y_pos
          linarith
        rw [hradix] at hvnumX hvnumY
        -- x normal: vNum ≤ |beta * x.Fnum|
        -- y subnormal: |beta * y.Fnum| < vNum, y.Fexp = -dExp
        -- From y.Fexp = -dExp and x normal: -dExp ≤ x.Fexp
        -- But x.Fexp < y.Fexp = -dExp, so x.Fexp < -dExp ≤ x.Fexp, contradiction!
        have hx_exp_ge : -b.dExp ≤ x.Fexp := hbX.2
        have hy_exp_eq : y.Fexp = -b.dExp := hexpY
        -- x.Fexp < y.Fexp = -dExp but -dExp ≤ x.Fexp, contradiction
        omega
    · -- x is subnormal
      rcases hcanY with ⟨hbY, hvnumY⟩ | ⟨hbY, hexpY, hvnumY⟩
      · -- x subnormal, y normal
        have hbeta_pos : (0 : ℝ) < (beta : ℝ) := by
          have : (0 : Int) < beta := lt_trans (by norm_num : (0 : Int) < 1) hβ
          exact Int.cast_pos.mpr this
        simp only [_root_.F2R, FloatSpec.Core.Defs.F2R] at h_lt hPosX hPosY
        have hpow_x_pos : (0 : ℝ) < (beta : ℝ) ^ x.Fexp := zpow_pos hbeta_pos x.Fexp
        have hpow_y_pos : (0 : ℝ) < (beta : ℝ) ^ y.Fexp := zpow_pos hbeta_pos y.Fexp
        have hx_fnum_nonneg : (0 : ℤ) ≤ x.Fnum := by
          by_contra hcontra
          push Not at hcontra
          have hx_neg : (x.Fnum : ℝ) < 0 := Int.cast_lt_zero.mpr hcontra
          have : (x.Fnum : ℝ) * (beta : ℝ) ^ x.Fexp < 0 := mul_neg_of_neg_of_pos hx_neg hpow_x_pos
          linarith
        have hy_fnum_nonneg : (0 : ℤ) ≤ y.Fnum := by
          by_contra hcontra
          push Not at hcontra
          have hy_neg : (y.Fnum : ℝ) < 0 := Int.cast_lt_zero.mpr hcontra
          have : (y.Fnum : ℝ) * (beta : ℝ) ^ y.Fexp < 0 := mul_neg_of_neg_of_pos hy_neg hpow_y_pos
          linarith
        rw [hradix] at hvnumX hvnumY
        -- x subnormal: |beta * x.Fnum| < vNum, x.Fexp = -dExp
        -- y normal: vNum ≤ |beta * y.Fnum|, -dExp ≤ y.Fexp
        -- We need to show F2R x < F2R y, which contradicts h_lt: F2R y < F2R x
        have hbeta_ge_one : (1 : ℝ) ≤ (beta : ℝ) := by
          have h1le : (1 : ℤ) ≤ beta := le_of_lt hβ
          exact_mod_cast h1le
        have hx_exp_eq : x.Fexp = -b.dExp := hexpX
        have hy_exp_ge : -b.dExp ≤ y.Fexp := hbY.2
        -- x.Fexp = -dExp ≤ y.Fexp, and x.Fexp < y.Fexp
        -- So -dExp < y.Fexp, i.e., y.Fexp > -dExp
        have hy_exp_gt : y.Fexp > -b.dExp := by omega
        -- F2R x = x.Fnum * beta^(-dExp)
        -- F2R y = y.Fnum * beta^y.Fexp
        -- From subnormal: |beta * x.Fnum| < vNum, so beta * x.Fnum < vNum (since x.Fnum ≥ 0)
        -- So x.Fnum < vNum / beta
        -- F2R x = x.Fnum * beta^(-dExp) < (vNum/beta) * beta^(-dExp) = vNum * beta^(-dExp-1)
        -- From normal: vNum ≤ beta * y.Fnum
        -- So y.Fnum ≥ vNum / beta
        -- F2R y = y.Fnum * beta^y.Fexp ≥ (vNum/beta) * beta^y.Fexp = vNum * beta^(y.Fexp-1)
        -- Since y.Fexp > -dExp: y.Fexp - 1 ≥ -dExp, so beta^(y.Fexp-1) ≥ beta^(-dExp)
        -- So F2R y ≥ vNum * beta^(y.Fexp-1) ≥ vNum * beta^(-dExp) > F2R x
        have hvnumX' : beta * x.Fnum < b.vNum := by
          have h1 : |beta * x.Fnum| = beta * x.Fnum := by
            apply abs_of_nonneg
            apply mul_nonneg
            · exact le_of_lt (lt_trans (by norm_num : (0 : ℤ) < 1) hβ)
            · exact hx_fnum_nonneg
          rw [← h1]; exact hvnumX
        have hvnumY' : (b.vNum : ℤ) ≤ beta * y.Fnum := by
          have h1 : |beta * y.Fnum| = beta * y.Fnum := by
            apply abs_of_nonneg
            apply mul_nonneg
            · exact le_of_lt (lt_trans (by norm_num : (0 : ℤ) < 1) hβ)
            · exact hy_fnum_nonneg
          rw [← h1]; exact hvnumY
        have hvNum_pos : (0 : ℤ) < b.vNum := by
          have h1 : (b.vNum : ℤ) ≤ beta * y.Fnum := hvnumY'
          have h2 : y.Fnum < b.vNum := by
            have := hbY.1
            have h3 : |y.Fnum| = y.Fnum := abs_of_nonneg hy_fnum_nonneg
            rw [← h3]; exact this
          -- vNum ≤ beta * y.Fnum < beta * vNum
          -- So vNum ≤ beta * y.Fnum < beta * vNum
          -- Dividing by beta: vNum/beta ≤ y.Fnum < vNum
          -- Since beta > 1 and y.Fnum < vNum, we have vNum ≤ beta * (vNum - 1)
          -- This requires vNum > 0
          by_contra hcontra
          push Not at hcontra
          have : (b.vNum : ℤ) ≤ 0 := hcontra
          have h3 : (b.vNum : ℤ) ≤ beta * y.Fnum := hvnumY'
          have h4 : beta * y.Fnum ≥ 0 := mul_nonneg (le_of_lt (lt_trans (by norm_num : (0 : ℤ) < 1) hβ)) hy_fnum_nonneg
          omega
        have hbeta_pos_int : (0 : ℤ) < beta := lt_trans (by norm_num : (0 : ℤ) < 1) hβ
        have hx_fnum_bound : (x.Fnum : ℝ) < (b.vNum : ℝ) / (beta : ℝ) := by
          have h1 : (beta : ℤ) * x.Fnum < b.vNum := hvnumX'
          have h2 : (beta : ℝ) * (x.Fnum : ℝ) < (b.vNum : ℝ) := by exact_mod_cast h1
          calc (x.Fnum : ℝ) = (beta : ℝ) * (x.Fnum : ℝ) / (beta : ℝ) := by field_simp
               _ < (b.vNum : ℝ) / (beta : ℝ) := by apply div_lt_div_of_pos_right h2 hbeta_pos
        rw [hx_exp_eq] at hpow_x_pos
        have hF2Rx_bound : (x.Fnum : ℝ) * (beta : ℝ) ^ (-b.dExp : ℤ) < ((b.vNum : ℝ) / (beta : ℝ)) * (beta : ℝ) ^ (-b.dExp : ℤ) := by
          apply mul_lt_mul_of_pos_right hx_fnum_bound hpow_x_pos
        have hF2Rx_bound' : ((b.vNum : ℝ) / (beta : ℝ)) * (beta : ℝ) ^ (-b.dExp : ℤ) = (b.vNum : ℝ) * (beta : ℝ) ^ (-b.dExp - 1 : ℤ) := by
          rw [zpow_sub₀ (ne_of_gt hbeta_pos), zpow_one]
          ring
        have hy_fnum_ge : (y.Fnum : ℝ) ≥ (b.vNum : ℝ) / (beta : ℝ) := by
          have h1 : (b.vNum : ℝ) ≤ (beta : ℝ) * (y.Fnum : ℝ) := by exact_mod_cast hvnumY'
          calc (y.Fnum : ℝ) = (beta : ℝ) * (y.Fnum : ℝ) / (beta : ℝ) := by field_simp
               _ ≥ (b.vNum : ℝ) / (beta : ℝ) := by apply div_le_div_of_nonneg_right h1 (le_of_lt hbeta_pos)
        have hF2Ry_ge : (y.Fnum : ℝ) * (beta : ℝ) ^ y.Fexp ≥ ((b.vNum : ℝ) / (beta : ℝ)) * (beta : ℝ) ^ y.Fexp := by
          apply mul_le_mul_of_nonneg_right hy_fnum_ge (le_of_lt hpow_y_pos)
        have hF2Ry_ge' : ((b.vNum : ℝ) / (beta : ℝ)) * (beta : ℝ) ^ y.Fexp = (b.vNum : ℝ) * (beta : ℝ) ^ (y.Fexp - 1 : ℤ) := by
          rw [zpow_sub₀ (ne_of_gt hbeta_pos), zpow_one]
          ring
        have hexp_rel : y.Fexp - 1 ≥ -b.dExp := by omega
        have hpow_rel : (beta : ℝ) ^ (y.Fexp - 1 : ℤ) ≥ (beta : ℝ) ^ (-b.dExp : ℤ) := by
          apply zpow_le_zpow_right₀ hbeta_ge_one hexp_rel
        have hF2Ry_ge'' : (b.vNum : ℝ) * (beta : ℝ) ^ (y.Fexp - 1 : ℤ) ≥ (b.vNum : ℝ) * (beta : ℝ) ^ (-b.dExp : ℤ) := by
          apply mul_le_mul_of_nonneg_left hpow_rel
          exact_mod_cast (le_of_lt hvNum_pos)
        -- F2R y ≥ vNum * beta^(y.Fexp-1) ≥ vNum * beta^(-dExp) > vNum * beta^(-dExp-1) ≥ F2R x
        have hpow_rel2 : (beta : ℝ) ^ (-b.dExp : ℤ) > (beta : ℝ) ^ (-b.dExp - 1 : ℤ) := by
          have hbeta_gt_one : (1 : ℝ) < (beta : ℝ) := by exact_mod_cast hβ
          exact zpow_lt_zpow_right₀ hbeta_gt_one (by omega : -b.dExp - 1 < -b.dExp)
        have hvNum_real_pos : (0 : ℝ) < (b.vNum : ℝ) := by exact_mod_cast hvNum_pos
        have hF2Ry_gt : (b.vNum : ℝ) * (beta : ℝ) ^ (-b.dExp : ℤ) > (b.vNum : ℝ) * (beta : ℝ) ^ (-b.dExp - 1 : ℤ) := by
          apply mul_lt_mul_of_pos_left hpow_rel2 hvNum_real_pos
        -- Chain the inequalities
        have hchain : (y.Fnum : ℝ) * (beta : ℝ) ^ y.Fexp > (x.Fnum : ℝ) * (beta : ℝ) ^ x.Fexp := by
          rw [hx_exp_eq]
          calc (y.Fnum : ℝ) * (beta : ℝ) ^ y.Fexp
              ≥ ((b.vNum : ℝ) / (beta : ℝ)) * (beta : ℝ) ^ y.Fexp := hF2Ry_ge
            _ = (b.vNum : ℝ) * (beta : ℝ) ^ (y.Fexp - 1 : ℤ) := hF2Ry_ge'
            _ ≥ (b.vNum : ℝ) * (beta : ℝ) ^ (-b.dExp : ℤ) := hF2Ry_ge''
            _ > (b.vNum : ℝ) * (beta : ℝ) ^ (-b.dExp - 1 : ℤ) := hF2Ry_gt
            _ = ((b.vNum : ℝ) / (beta : ℝ)) * (beta : ℝ) ^ (-b.dExp : ℤ) := hF2Rx_bound'.symm
            _ > (x.Fnum : ℝ) * (beta : ℝ) ^ (-b.dExp : ℤ) := hF2Rx_bound
        linarith
      · -- Both subnormal
        -- x.Fexp = -dExp, y.Fexp = -dExp, but x.Fexp < y.Fexp, contradiction!
        have hx_exp_eq : x.Fexp = -b.dExp := hexpX
        have hy_exp_eq : y.Fexp = -b.dExp := hexpY
        omega
  · -- Case: F2R y = F2R x
    -- If both have the same F2R value and are canonical, they should have the same representation
    -- Use x.Fexp < y.Fexp to derive contradiction
    -- Actually, same F2R value doesn't mean same representation (different mantissa/exponent pairs)
    -- But with canonical form constraints, same value implies same representation
    -- For now, we use a direct argument: same F2R but different exponents
    -- If x.Fnum * beta^x.Fexp = y.Fnum * beta^y.Fexp with x.Fexp < y.Fexp
    -- Then x.Fnum = y.Fnum * beta^(y.Fexp - x.Fexp)
    -- The mantissa bounds from canonical forms prevent this
    have hbeta_pos : (0 : ℝ) < (beta : ℝ) := by
      have : (0 : Int) < beta := lt_trans (by norm_num : (0 : Int) < 1) hβ
      exact Int.cast_pos.mpr this
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R] at h_eq hPosX hPosY
    have hpow_x_pos : (0 : ℝ) < (beta : ℝ) ^ x.Fexp := zpow_pos hbeta_pos x.Fexp
    have hpow_y_pos : (0 : ℝ) < (beta : ℝ) ^ y.Fexp := zpow_pos hbeta_pos y.Fexp
    have hx_fnum_nonneg : (0 : ℤ) ≤ x.Fnum := by
      by_contra hcontra
      push Not at hcontra
      have hx_neg : (x.Fnum : ℝ) < 0 := Int.cast_lt_zero.mpr hcontra
      have : (x.Fnum : ℝ) * (beta : ℝ) ^ x.Fexp < 0 := mul_neg_of_neg_of_pos hx_neg hpow_x_pos
      linarith
    have hy_fnum_nonneg : (0 : ℤ) ≤ y.Fnum := by
      by_contra hcontra
      push Not at hcontra
      have hy_neg : (y.Fnum : ℝ) < 0 := Int.cast_lt_zero.mpr hcontra
      have : (y.Fnum : ℝ) * (beta : ℝ) ^ y.Fexp < 0 := mul_neg_of_neg_of_pos hy_neg hpow_y_pos
      linarith
    -- From h_eq: x.Fnum * beta^x.Fexp = y.Fnum * beta^y.Fexp
    -- Rearranging: x.Fnum / y.Fnum = beta^(y.Fexp - x.Fexp)
    -- Since y.Fexp - x.Fexp ≥ 1, beta^(y.Fexp - x.Fexp) ≥ beta > 1
    -- So x.Fnum > y.Fnum (both nonneg)
    -- But canonical form bounds: |x.Fnum| < vNum, so x.Fnum < vNum
    -- And for normal y: vNum ≤ |beta * y.Fnum| = beta * y.Fnum
    -- So y.Fnum ≥ vNum / beta
    -- x.Fnum = y.Fnum * beta^(y.Fexp - x.Fexp) ≥ (vNum/beta) * beta = vNum
    -- But x.Fnum < vNum, contradiction!
    -- Handle case when y.Fnum = 0
    rcases eq_or_lt_of_le hy_fnum_nonneg with hy_zero | hy_pos
    · -- y.Fnum = 0
      rw [← hy_zero] at h_eq
      simp at h_eq
      -- x.Fnum * beta^x.Fexp = 0, so x.Fnum = 0 (since beta^x.Fexp ≠ 0)
      have hpow_x_ne : (beta : ℝ) ^ x.Fexp ≠ 0 := ne_of_gt hpow_x_pos
      have hx_fnum_zero : (x.Fnum : ℝ) = 0 := by
        -- h_eq : x.Fnum = 0 ∨ beta^x.Fexp = 0
        rcases h_eq with h1 | h2
        · exact_mod_cast h1
        · exfalso; exact hpow_x_ne h2
      have hx_fnum_zero' : x.Fnum = 0 := by exact_mod_cast hx_fnum_zero
      -- Both are zero, so both F2R are 0
      -- For subnormal/normal with Fnum = 0, check canonical constraints
      -- Actually if Fnum = 0, the float represents 0
      -- For normal: vNum ≤ |radix * 0| = 0, which contradicts vNum > 0
      -- So neither can be normal with Fnum = 0
      -- For subnormal with Fnum = 0, it represents 0
      rw [hradix] at hcanX hcanY
      rcases hcanY with ⟨hbY, hvnumY⟩ | ⟨hbY, hexpY, hvnumY⟩
      · -- y normal with Fnum = 0
        rw [← hy_zero] at hvnumY
        simp at hvnumY
        -- vNum ≤ 0 from y normal with Fnum = 0
        -- This is a contradiction with x being canonical
        -- If x is subnormal: |radix * x.Fnum| < vNum, with x.Fnum = 0: 0 < vNum
        -- Combined with hvnumY: vNum ≤ 0 gives contradiction
        rcases hcanX with ⟨hbX, hvnumX⟩ | ⟨hbX, hexpX, hvnumX⟩
        · -- x normal with Fnum = 0: vNum ≤ |radix * 0| = 0
          rw [hx_fnum_zero'] at hvnumX
          simp at hvnumX
          -- hvnumX: vNum ≤ 0, hvnumY: vNum ≤ 0
          -- From bounded: |x.Fnum| < vNum, with x.Fnum = 0: 0 < vNum
          have hbX' := hbX.1
          rw [hx_fnum_zero'] at hbX'
          simp at hbX'
          -- hbX': 0 < vNum
          omega
        · -- x subnormal
          rw [hx_fnum_zero'] at hvnumX
          simp at hvnumX
          -- |0| < vNum means 0 < vNum
          omega
      · -- y subnormal with Fnum = 0: this is valid (represents 0)
        -- But then x.Fexp < y.Fexp = -dExp
        -- x subnormal: x.Fexp = -dExp, contradiction
        -- x normal: -dExp ≤ x.Fexp, but x.Fexp < -dExp, contradiction
        rcases hcanX with ⟨hbX, hvnumX⟩ | ⟨hbX, hexpX, hvnumX⟩
        · -- x normal with Fnum = 0
          rw [hx_fnum_zero'] at hvnumX
          simp at hvnumX
          have hvNum_pos : (0 : ℤ) < b.vNum := by
            rw [← hy_zero] at hvnumY
            simp at hvnumY
            exact hvnumY
          omega
        · -- x subnormal with Fnum = 0
          have hx_exp_eq : x.Fexp = -b.dExp := hexpX
          have hy_exp_eq : y.Fexp = -b.dExp := hexpY
          omega
    · -- y.Fnum > 0
      -- x.Fnum * beta^x.Fexp = y.Fnum * beta^y.Fexp
      -- x.Fnum = y.Fnum * beta^(y.Fexp - x.Fexp)
      have hexp_diff_pos : y.Fexp - x.Fexp ≥ 1 := by omega
      have hbeta_ge_one : (1 : ℝ) ≤ (beta : ℝ) := by
        have h1le : (1 : ℤ) ≤ beta := le_of_lt hβ
        exact_mod_cast h1le
      have hpow_diff_ge_beta : (beta : ℝ) ^ (y.Fexp - x.Fexp) ≥ (beta : ℝ) := by
        calc (beta : ℝ) ^ (y.Fexp - x.Fexp) ≥ (beta : ℝ) ^ (1 : ℤ) := by
              apply zpow_le_zpow_right₀ hbeta_ge_one hexp_diff_pos
          _ = (beta : ℝ) := zpow_one (beta : ℝ)
      have hpow_diff_pos : (0 : ℝ) < (beta : ℝ) ^ (y.Fexp - x.Fexp) := zpow_pos hbeta_pos (y.Fexp - x.Fexp)
      -- From h_eq: x.Fnum * beta^x.Fexp = y.Fnum * beta^y.Fexp
      have h_ratio : (x.Fnum : ℝ) = (y.Fnum : ℝ) * (beta : ℝ) ^ (y.Fexp - x.Fexp) := by
        have hpow_x_ne : (beta : ℝ) ^ x.Fexp ≠ 0 := ne_of_gt hpow_x_pos
        calc (x.Fnum : ℝ) = (x.Fnum : ℝ) * (beta : ℝ) ^ x.Fexp / (beta : ℝ) ^ x.Fexp := by field_simp
             _ = (y.Fnum : ℝ) * (beta : ℝ) ^ y.Fexp / (beta : ℝ) ^ x.Fexp := by rw [h_eq]
             _ = (y.Fnum : ℝ) * ((beta : ℝ) ^ y.Fexp / (beta : ℝ) ^ x.Fexp) := by ring
             _ = (y.Fnum : ℝ) * (beta : ℝ) ^ (y.Fexp - x.Fexp) := by
                  congr 1
                  rw [← zpow_sub₀ (ne_of_gt hbeta_pos)]
      -- So x.Fnum ≥ y.Fnum * beta ≥ y.Fnum (since beta ≥ 1 and y.Fnum > 0)
      have hx_ge_y_times_beta : (x.Fnum : ℝ) ≥ (y.Fnum : ℝ) * (beta : ℝ) := by
        rw [h_ratio]
        have hy_pos_real : (0 : ℝ) < (y.Fnum : ℝ) := Int.cast_pos.mpr hy_pos
        apply mul_le_mul_of_nonneg_left hpow_diff_ge_beta (le_of_lt hy_pos_real)
      -- Now use canonical bounds
      rw [hradix] at hcanX hcanY
      rcases hcanX with ⟨hbX, hvnumX⟩ | ⟨hbX, hexpX, hvnumX⟩
      · -- x normal
        rcases hcanY with ⟨hbY, hvnumY⟩ | ⟨hbY, hexpY, hvnumY⟩
        · -- y normal
          -- x.Fnum < vNum and y.Fnum ≥ vNum/beta
          -- x.Fnum ≥ y.Fnum * beta ≥ (vNum/beta) * beta = vNum
          -- Contradiction: x.Fnum < vNum and x.Fnum ≥ vNum
          have hvnumY' : (b.vNum : ℤ) ≤ beta * y.Fnum := by
            have h1 : |beta * y.Fnum| = beta * y.Fnum := by
              apply abs_of_nonneg
              apply mul_nonneg
              · exact le_of_lt (lt_trans (by norm_num : (0 : ℤ) < 1) hβ)
              · exact le_of_lt hy_pos
            rw [← h1]; exact hvnumY
          have hy_fnum_ge : (y.Fnum : ℝ) ≥ (b.vNum : ℝ) / (beta : ℝ) := by
            have h1 : (b.vNum : ℝ) ≤ (beta : ℝ) * (y.Fnum : ℝ) := by exact_mod_cast hvnumY'
            calc (y.Fnum : ℝ) = (beta : ℝ) * (y.Fnum : ℝ) / (beta : ℝ) := by field_simp
                 _ ≥ (b.vNum : ℝ) / (beta : ℝ) := by apply div_le_div_of_nonneg_right h1 (le_of_lt hbeta_pos)
          have hx_ge_vNum : (x.Fnum : ℝ) ≥ (b.vNum : ℝ) := by
            calc (x.Fnum : ℝ) ≥ (y.Fnum : ℝ) * (beta : ℝ) := hx_ge_y_times_beta
                 _ ≥ ((b.vNum : ℝ) / (beta : ℝ)) * (beta : ℝ) := by
                      apply mul_le_mul_of_nonneg_right hy_fnum_ge (le_of_lt hbeta_pos)
                 _ = (b.vNum : ℝ) := by field_simp
          have hx_fnum_bound : x.Fnum < b.vNum := by
            have h1 : |x.Fnum| = x.Fnum := abs_of_nonneg hx_fnum_nonneg
            rw [← h1]; exact hbX.1
          have hx_lt_vNum : (x.Fnum : ℝ) < (b.vNum : ℝ) := by exact_mod_cast hx_fnum_bound
          linarith
        · -- y subnormal, x normal
          -- y.Fexp = -dExp, -dExp ≤ x.Fexp but x.Fexp < y.Fexp = -dExp, contradiction
          have hx_exp_ge : -b.dExp ≤ x.Fexp := hbX.2
          have hy_exp_eq : y.Fexp = -b.dExp := hexpY
          omega
      · -- x subnormal
        rcases hcanY with ⟨hbY, hvnumY⟩ | ⟨hbY, hexpY, hvnumY⟩
        · -- y normal, x subnormal
          -- This is the main case to handle
          -- x.Fexp = -dExp, -dExp ≤ y.Fexp (from normal)
          -- x.Fexp < y.Fexp means -dExp < y.Fexp
          have hx_exp_eq : x.Fexp = -b.dExp := hexpX
          have hy_exp_ge : -b.dExp ≤ y.Fexp := hbY.2
          -- Same logic: x.Fnum ≥ y.Fnum * beta
          -- For subnormal x: |beta * x.Fnum| < vNum, so beta * x.Fnum < vNum (since x.Fnum ≥ 0)
          -- For normal y: vNum ≤ beta * y.Fnum
          -- So y.Fnum ≥ vNum / beta
          -- x.Fnum ≥ y.Fnum * beta ≥ (vNum/beta) * beta = vNum
          -- But beta * x.Fnum < vNum, so x.Fnum < vNum/beta < vNum
          -- Contradiction: x.Fnum ≥ vNum but x.Fnum < vNum/beta
          have hvnumY' : (b.vNum : ℤ) ≤ beta * y.Fnum := by
            have h1 : |beta * y.Fnum| = beta * y.Fnum := by
              apply abs_of_nonneg
              apply mul_nonneg
              · exact le_of_lt (lt_trans (by norm_num : (0 : ℤ) < 1) hβ)
              · exact le_of_lt hy_pos
            rw [← h1]; exact hvnumY
          have hy_fnum_ge : (y.Fnum : ℝ) ≥ (b.vNum : ℝ) / (beta : ℝ) := by
            have h1 : (b.vNum : ℝ) ≤ (beta : ℝ) * (y.Fnum : ℝ) := by exact_mod_cast hvnumY'
            calc (y.Fnum : ℝ) = (beta : ℝ) * (y.Fnum : ℝ) / (beta : ℝ) := by field_simp
                 _ ≥ (b.vNum : ℝ) / (beta : ℝ) := by apply div_le_div_of_nonneg_right h1 (le_of_lt hbeta_pos)
          have hx_ge_vNum : (x.Fnum : ℝ) ≥ (b.vNum : ℝ) := by
            calc (x.Fnum : ℝ) ≥ (y.Fnum : ℝ) * (beta : ℝ) := hx_ge_y_times_beta
                 _ ≥ ((b.vNum : ℝ) / (beta : ℝ)) * (beta : ℝ) := by
                      apply mul_le_mul_of_nonneg_right hy_fnum_ge (le_of_lt hbeta_pos)
                 _ = (b.vNum : ℝ) := by field_simp
          have hvnumX' : beta * x.Fnum < b.vNum := by
            have h1 : |beta * x.Fnum| = beta * x.Fnum := by
              apply abs_of_nonneg
              apply mul_nonneg
              · exact le_of_lt (lt_trans (by norm_num : (0 : ℤ) < 1) hβ)
              · exact hx_fnum_nonneg
            rw [← h1]; exact hvnumX
          have hx_fnum_lt : (x.Fnum : ℝ) < (b.vNum : ℝ) / (beta : ℝ) := by
            have h1 : (beta : ℤ) * x.Fnum < b.vNum := hvnumX'
            have h2 : (beta : ℝ) * (x.Fnum : ℝ) < (b.vNum : ℝ) := by exact_mod_cast h1
            calc (x.Fnum : ℝ) = (beta : ℝ) * (x.Fnum : ℝ) / (beta : ℝ) := by field_simp
                 _ < (b.vNum : ℝ) / (beta : ℝ) := by apply div_lt_div_of_pos_right h2 hbeta_pos
          have hvNum_pos : (0 : ℝ) < (b.vNum : ℝ) := by
            -- From hx_ge_vNum: x.Fnum ≥ vNum and hvnumX': beta * x.Fnum < vNum
            -- And x.Fnum ≥ 0
            -- If vNum ≤ 0, then hx_ge_vNum says x.Fnum ≥ vNum ≤ 0
            -- So x.Fnum can be 0 or negative. But x.Fnum ≥ 0.
            -- And from hvnumX': beta * x.Fnum < vNum
            -- If vNum ≤ 0 and x.Fnum ≥ 0, beta > 0, then beta * x.Fnum ≥ 0 > vNum
            -- Contradiction with hvnumX': beta * x.Fnum < vNum
            by_contra hcontra
            push Not at hcontra
            have hvNum_nonpos : (b.vNum : ℝ) ≤ 0 := hcontra
            have hvNum_nonpos' : (b.vNum : ℤ) ≤ 0 := by exact_mod_cast hvNum_nonpos
            have hbeta_x_nonneg : (0 : ℤ) ≤ beta * x.Fnum := mul_nonneg (le_of_lt (lt_trans (by norm_num : (0 : ℤ) < 1) hβ)) hx_fnum_nonneg
            have h : beta * x.Fnum < b.vNum := hvnumX'
            omega
          have hbeta_gt_one : (beta : ℝ) > 1 := by exact_mod_cast hβ
          have hcontra : (b.vNum : ℝ) / (beta : ℝ) < (b.vNum : ℝ) := by
            apply div_lt_self hvNum_pos hbeta_gt_one
          linarith
        · -- Both subnormal: x.Fexp = y.Fexp = -dExp, contradicts x.Fexp < y.Fexp
          have hx_exp_eq : x.Fexp = -b.dExp := hexpX
          have hy_exp_eq : y.Fexp = -b.dExp := hexpY
          omega

noncomputable def FSuccPropPos_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) : Unit := ()

/-- Coq: `FSuccPropPos` — if nonnegative canonical floats are strictly ordered,
the normalized successor of the lower one remains below the upper one.

Upstream statement:
`forall x y : float, (0 <= x)%R -> Fcanonic radix b x ->
Fcanonic radix b y -> (x < y)%R -> (FSucc x <= y)%R`.  The Lean port makes the
section hypotheses explicit: `beta = radix`, `1 < radix`, nonzero precision,
and `b.vNum = Zpower_nat radix precision`. -/
theorem FSuccPropPos {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision ∧
        0 ≤ _root_.F2R (beta:=beta) x ∧
        Fcanonic (beta:=beta) radix b x ∧
        Fcanonic (beta:=beta) radix b y ∧
        _root_.F2R (beta:=beta) x < _root_.F2R (beta:=beta) y⌝⦄
    (pure (FSuccPropPos_check (beta:=beta) b radix precision x y) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta)
        (FSucc (beta:=beta) b radix precision x) ≤
        _root_.F2R (beta:=beta) y⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta_radix, hradix, hprecision, hvNum, hx_nonneg, hx_can, hy_can, hxy⟩
  simp only [wp, PostCond.noThrow, pure, FSuccPropPos_check, Id.run,
    ULift.up_down]
  have hbeta_gt : 1 < beta := by omega
  have hradix_eq_beta : radix = beta := hbeta_radix.symm
  have hcmp :
      x.Fexp < y.Fexp ∨ (x.Fexp = y.Fexp ∧ x.Fnum < y.Fnum) :=
    (FcanonicLtPos (beta:=beta) radix b x y hbeta_gt hradix_eq_beta)
      ⟨hx_can, hy_can, hx_nonneg, hxy⟩
  rcases hcmp with hexp_lt | ⟨hexp_eq, hnum_lt⟩
  · have hsucc_can :
        Fcanonic (beta:=beta) radix b
          (FSucc (beta:=beta) b radix precision x) :=
      (FSuccCanonic (beta:=beta) b radix precision x)
        ⟨hx_can, hradix, hprecision, hvNum⟩
    have hsucc_lt :
        _root_.F2R (beta:=beta) x <
          _root_.F2R (beta:=beta) (FSucc (beta:=beta) b radix precision x) :=
      (FSuccLt (beta:=beta) b radix precision x)
        ⟨hbeta_radix, hradix, hprecision, hvNum⟩
    have hy_nonneg : 0 ≤ _root_.F2R (beta:=beta) y :=
      le_trans hx_nonneg (le_of_lt hxy)
    have hsucc_nonneg :
        0 ≤ _root_.F2R (beta:=beta)
          (FSucc (beta:=beta) b radix precision x) :=
      le_trans hx_nonneg (le_of_lt hsucc_lt)
    by_cases htop : x.Fnum = pPred b.vNum
    · have hsucc_exp :
          (FSucc (beta:=beta) b radix precision x).Fexp = x.Fexp + 1 := by
        simp [FSucc, htop]
      by_cases hadj : x.Fexp + 1 = y.Fexp
      · have hy_fnum_nonneg : 0 ≤ y.Fnum :=
          FloatSpec.Core.Float_prop.ge_0_F2R (beta:=beta) y hbeta_gt hy_nonneg
        have hy_bound : Fbounded (beta:=beta) b y :=
          (FcanonicBound (beta:=beta) radix b y) hy_can
        have hy_num_lower : nNormMin radix precision ≤ y.Fnum := by
          rcases hy_can with hy_normal | hy_subnormal
          · rcases hy_normal with ⟨_, hy_normal_num⟩
            have hradix_pos : 0 < radix := by omega
            have hpow_prec_le : radix ^ precision ≤ |radix * y.Fnum| := by
              simpa [Zpower_nat, hvNum] using hy_normal_num
            have hpow_succ :
                radix ^ precision = radix * radix ^ (precision - 1) := by
              have hprec : Nat.succ (precision - 1) = precision := by omega
              calc
                radix ^ precision = radix ^ Nat.succ (precision - 1) := by rw [hprec]
                _ = radix ^ (precision - 1) * radix := by rw [pow_succ]
                _ = radix * radix ^ (precision - 1) := by ring
            have hnormal_abs : nNormMin radix precision ≤ |y.Fnum| := by
              have hmul_le :
                  radix * nNormMin radix precision ≤ radix * |y.Fnum| := by
                rw [nNormMin, ← hpow_succ]
                have hradix_abs : |radix| = radix := abs_of_nonneg (le_of_lt hradix_pos)
                simpa [abs_mul, hradix_abs] using hpow_prec_le
              exact Int.le_of_mul_le_mul_left hmul_le hradix_pos
            simpa [abs_of_nonneg hy_fnum_nonneg] using hnormal_abs
          · exfalso
            have hy_exp : y.Fexp = -b.dExp := hy_subnormal.2.1
            have hx_bound : Fbounded (beta:=beta) b x :=
              (FcanonicBound (beta:=beta) radix b x) hx_can
            rcases hx_bound with ⟨_, hx_exp_lb⟩
            omega
        subst beta
        simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, FSucc,
          FloatSpec.Core.Defs.FlocqFloat.Fnum,
          FloatSpec.Core.Defs.FlocqFloat.Fexp]
        simp [htop]
        rw [hadj]
        exact mul_le_mul_of_nonneg_right (by exact_mod_cast hy_num_lower)
          (le_of_lt (zpow_pos
            (by exact_mod_cast (show (0 : Int) < radix by omega)) y.Fexp))
      · have hsucc_exp_lt : (FSucc (beta:=beta) b radix precision x).Fexp < y.Fexp := by
          rw [hsucc_exp]
          omega
        exact le_of_lt
          ((FcanonicPosFexpRlt (beta:=beta) radix b
            (FSucc (beta:=beta) b radix precision x) y
            hbeta_gt hradix_eq_beta)
            ⟨hsucc_can, hy_can, hsucc_nonneg, hy_nonneg, hsucc_exp_lt⟩)
    · have hnot_low : x.Fnum ≠ -nNormMin radix precision := by
        intro hlow
        have hx_fnum_nonneg : 0 ≤ x.Fnum :=
          FloatSpec.Core.Float_prop.ge_0_F2R (beta:=beta) x hbeta_gt hx_nonneg
        have hnm_pos : 0 < nNormMin radix precision := by
          unfold nNormMin
          exact pow_pos (by omega : 0 < radix) _
        omega
      have hsucc_exp : (FSucc (beta:=beta) b radix precision x).Fexp = x.Fexp := by
        unfold FSucc
        simp [htop, hnot_low]
      have hsucc_exp_lt : (FSucc (beta:=beta) b radix precision x).Fexp < y.Fexp := by
        rw [hsucc_exp]
        exact hexp_lt
      exact le_of_lt
        ((FcanonicPosFexpRlt (beta:=beta) radix b
          (FSucc (beta:=beta) b radix precision x) y
          hbeta_gt hradix_eq_beta)
          ⟨hsucc_can, hy_can, hsucc_nonneg, hy_nonneg, hsucc_exp_lt⟩)
  · have hnot_top : x.Fnum ≠ pPred b.vNum := by
      intro htop
      have hy_bound : Fbounded (beta:=beta) b y :=
        (FcanonicBound (beta:=beta) radix b y) hy_can
      have hy_num_ub : y.Fnum < b.vNum := by
        exact lt_of_le_of_lt (le_abs_self y.Fnum) hy_bound.1
      unfold pPred at htop
      omega
    have hnot_low : x.Fnum ≠ -nNormMin radix precision := by
      intro hlow
      have hx_fnum_nonneg : 0 ≤ x.Fnum :=
        FloatSpec.Core.Float_prop.ge_0_F2R (beta:=beta) x hbeta_gt hx_nonneg
      have hnm_pos : 0 < nNormMin radix precision := by
        unfold nNormMin
        exact pow_pos (by omega : 0 < radix) _
      omega
    have hsucc_num : (FSucc (beta:=beta) b radix precision x).Fnum = x.Fnum + 1 := by
      unfold FSucc
      simp [hnot_top, hnot_low]
    have hsucc_exp : (FSucc (beta:=beta) b radix precision x).Fexp = y.Fexp := by
      unfold FSucc
      simp [hnot_top, hnot_low, hexp_eq]
    have hsucc_num_le : (FSucc (beta:=beta) b radix precision x).Fnum ≤ y.Fnum := by
      rw [hsucc_num]
      omega
    subst beta
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R]
    rw [hsucc_exp]
    exact mul_le_mul_of_nonneg_right (by exact_mod_cast hsucc_num_le)
      (le_of_lt (zpow_pos
        (by exact_mod_cast (show (0 : Int) < radix by omega)) y.Fexp))

noncomputable def FcanonicNegFexpRlt_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FcanonicNegFexpRlt` — among nonpositive canonical floats, a strictly
larger exponent forces a strictly smaller real value.

Note: Uses `Fcanonic'`, the Coq-matching canonical predicate.
Requires `radix = beta` and `1 < beta` for the mantissa bounds to imply the result. -/
theorem FcanonicNegFexpRlt {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta)
    (hβ : 1 < beta)
    (hradix : radix = beta) :
    ⦃⌜Fcanonic' (beta:=beta) radix b x ∧
        Fcanonic' (beta:=beta) radix b y ∧
        _root_.F2R (beta:=beta) x ≤ 0 ∧
        _root_.F2R (beta:=beta) y ≤ 0 ∧
        x.Fexp < y.Fexp⌝⦄
    (pure (FcanonicNegFexpRlt_check (beta:=beta) radix b x y) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) y < _root_.F2R (beta:=beta) x⌝⦄ := by
  intro ⟨hcanX, hcanY, hNegX, hNegY, hExpLt⟩
  simp only [wp, PostCond.noThrow, pure, FcanonicNegFexpRlt_check, ULift.down_up]
  -- Prove by contradiction: assume F2R x ≤ F2R y
  by_contra h_not_lt
  have h_not_lt' : _root_.F2R (beta:=beta) x ≤ _root_.F2R (beta:=beta) y := le_of_not_gt h_not_lt
  -- Case split: F2R x < F2R y or F2R x = F2R y
  rcases lt_or_eq_of_le h_not_lt' with h_lt | h_eq
  · -- Case: F2R x < F2R y (with both ≤ 0)
    -- Apply FcanonicLtNeg with p = x, q = y to get: y.Fexp < x.Fexp ∨ (x.Fexp = y.Fexp ∧ x.Fnum < y.Fnum)
    -- But we have x.Fexp < y.Fexp, so both alternatives give contradiction
    have hLtNegResult : y.Fexp < x.Fexp ∨ (x.Fexp = y.Fexp ∧ x.Fnum < y.Fnum) := by
      -- Extract the result from FcanonicLtNeg
      have hpre : Fcanonic' (beta:=beta) radix b x ∧
                  Fcanonic' (beta:=beta) radix b y ∧
                  _root_.F2R (beta:=beta) y ≤ 0 ∧
                  _root_.F2R (beta:=beta) x < _root_.F2R (beta:=beta) y := ⟨hcanX, hcanY, hNegY, h_lt⟩
      have hspec := FcanonicLtNeg (beta:=beta) radix b x y hβ hradix
      simp only [wp, PostCond.noThrow, pure, FcanonicLtNeg_check, ULift.down_up] at hspec
      exact hspec hpre
    rcases hLtNegResult with hexp_lt | ⟨hexp_eq, _⟩
    · -- y.Fexp < x.Fexp contradicts x.Fexp < y.Fexp
      omega
    · -- x.Fexp = y.Fexp contradicts x.Fexp < y.Fexp
      omega
  · -- Case: F2R x = F2R y
    -- Use FcanonicUnique': canonical floats with same F2R value are equal
    -- So x = y, but then x.Fexp = y.Fexp, contradicting x.Fexp < y.Fexp
    -- For now, derive contradiction from the canonical structure directly
    -- Since F2R x = F2R y and both are canonical with x.Fexp < y.Fexp,
    -- we need to show this is impossible
    have hbeta_pos : (0 : ℝ) < (beta : ℝ) := by
      have : (0 : Int) < beta := lt_trans (by norm_num : (0 : Int) < 1) hβ
      exact Int.cast_pos.mpr this
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R] at h_eq hNegX hNegY
    -- h_eq: x.Fnum * beta^x.Fexp = y.Fnum * beta^y.Fexp
    -- With x.Fexp < y.Fexp and both values ≤ 0
    -- Case analysis on canonical forms
    rcases hcanX with ⟨hbX, hvnumX⟩ | ⟨hbX, hexpX, hvnumX⟩
    <;> rcases hcanY with ⟨hbY, hvnumY⟩ | ⟨hbY, hexpY, hvnumY⟩
    -- Case 1: Both normal
    · exfalso
      rw [hradix] at hvnumX hvnumY
      have hpow_x_pos : (0 : ℝ) < (beta : ℝ) ^ x.Fexp := zpow_pos hbeta_pos x.Fexp
      have hpow_y_pos : (0 : ℝ) < (beta : ℝ) ^ y.Fexp := zpow_pos hbeta_pos y.Fexp
      -- From F2R x ≤ 0 and beta^x.Fexp > 0: x.Fnum ≤ 0
      have hx_fnum_nonpos : x.Fnum ≤ (0 : ℤ) := by
        by_contra hcontra
        push Not at hcontra
        have hx_pos : (x.Fnum : ℝ) > 0 := Int.cast_pos.mpr hcontra
        have : (x.Fnum : ℝ) * (beta : ℝ) ^ x.Fexp > 0 := mul_pos hx_pos hpow_x_pos
        linarith
      have hy_fnum_nonpos : y.Fnum ≤ (0 : ℤ) := by
        by_contra hcontra
        push Not at hcontra
        have hy_pos : (y.Fnum : ℝ) > 0 := Int.cast_pos.mpr hcontra
        have : (y.Fnum : ℝ) * (beta : ℝ) ^ y.Fexp > 0 := mul_pos hy_pos hpow_y_pos
        linarith
      -- If x.Fnum = 0, then F2R x = 0, so F2R y = 0, so y.Fnum = 0
      -- But normal requires vNum ≤ |beta * Fnum|, so Fnum ≠ 0
      have hbeta_pos_int : (0 : ℤ) < beta := lt_trans (by norm_num : (0 : ℤ) < 1) hβ
      have hvnum_pos : (0 : ℤ) < b.vNum := by
        -- Use the bound |x.Fnum| < b.vNum from Fbounded'
        have hbound := hbX.1
        have habs_nonneg : (0 : ℤ) ≤ |x.Fnum| := abs_nonneg _
        omega
      -- Both x.Fnum and y.Fnum are negative (not zero, by normal bound)
      have hx_fnum_neg : x.Fnum < 0 := by
        rcases eq_or_lt_of_le hx_fnum_nonpos with hxz | hxneg
        · exfalso
          simp only [hxz, mul_zero, abs_zero] at hvnumX
          -- hvnumX: b.vNum ≤ 0, but hvnum_pos: 0 < b.vNum
          linarith
        · exact hxneg
      have hy_fnum_neg : y.Fnum < 0 := by
        rcases eq_or_lt_of_le hy_fnum_nonpos with hyz | hyneg
        · exfalso
          simp only [hyz, mul_zero, abs_zero] at hvnumY
          -- hvnumY: b.vNum ≤ 0, but hvnum_pos: 0 < b.vNum
          linarith
        · exact hyneg
      -- F2R x = F2R y with x.Fexp < y.Fexp
      -- x.Fnum * beta^x.Fexp = y.Fnum * beta^y.Fexp
      -- Let d = y.Fexp - x.Fexp > 0
      -- y.Fnum * beta^y.Fexp = y.Fnum * beta^(x.Fexp + d) = y.Fnum * beta^d * beta^x.Fexp
      -- So x.Fnum = y.Fnum * beta^d
      -- Since y.Fnum < 0 and beta^d > 0, x.Fnum < 0 ✓
      -- |x.Fnum| = |y.Fnum| * beta^d
      -- For normal: vNum ≤ beta * |x.Fnum| and |x.Fnum| < vNum
      -- So vNum ≤ beta * |x.Fnum| < beta * vNum
      -- Also: |x.Fnum| = |y.Fnum| * beta^d ≥ |y.Fnum| * beta (since d ≥ 1)
      -- And: vNum ≤ beta * |y.Fnum|, so |y.Fnum| ≥ vNum / beta
      -- Hmm, let's try: |x.Fnum| = |y.Fnum| * beta^d
      -- From |x.Fnum| < vNum: |y.Fnum| * beta^d < vNum
      -- From vNum ≤ beta * |y.Fnum|: vNum / beta ≤ |y.Fnum|
      -- So vNum / beta * beta^d < vNum, i.e., beta^(d-1) < 1
      -- But d ≥ 1, so beta^(d-1) ≥ beta^0 = 1, contradiction when d > 1
      -- When d = 1: beta^0 = 1 < 1 is false
      -- Wait, need d ≥ 1 strictly. We have x.Fexp < y.Fexp, so d = y.Fexp - x.Fexp ≥ 1
      have hd_ge_one : y.Fexp - x.Fexp ≥ 1 := by omega
      have hpow_ne_x : (beta : ℝ) ^ x.Fexp ≠ 0 := ne_of_gt hpow_x_pos
      -- From h_eq: x.Fnum * beta^x.Fexp = y.Fnum * beta^y.Fexp
      have h_fnum_eq : (x.Fnum : ℝ) = (y.Fnum : ℝ) * (beta : ℝ) ^ (y.Fexp - x.Fexp) := by
        have h1 : (x.Fnum : ℝ) * (beta : ℝ) ^ x.Fexp = (y.Fnum : ℝ) * (beta : ℝ) ^ y.Fexp := h_eq
        have h2 : (beta : ℝ) ^ y.Fexp = (beta : ℝ) ^ (y.Fexp - x.Fexp) * (beta : ℝ) ^ x.Fexp := by
          rw [← zpow_add₀ (ne_of_gt hbeta_pos)]
          ring_nf
        rw [h2] at h1
        field_simp at h1
        linarith
      -- |x.Fnum| = |y.Fnum| * beta^(y.Fexp - x.Fexp) since both negative and beta^d > 0
      have hpow_d_pos : (0 : ℝ) < (beta : ℝ) ^ (y.Fexp - x.Fexp) :=
        zpow_pos hbeta_pos (y.Fexp - x.Fexp)
      have h_abs_eq : |(x.Fnum : ℝ)| = |(y.Fnum : ℝ)| * (beta : ℝ) ^ (y.Fexp - x.Fexp) := by
        have hx_neg_real : (x.Fnum : ℝ) < 0 := Int.cast_lt_zero.mpr hx_fnum_neg
        have hy_neg_real : (y.Fnum : ℝ) < 0 := Int.cast_lt_zero.mpr hy_fnum_neg
        rw [abs_of_neg hx_neg_real, abs_of_neg hy_neg_real]
        rw [h_fnum_eq]
        ring
      -- From normal x: |x.Fnum| < vNum and vNum ≤ beta * |x.Fnum|
      have hx_abs_lt : (|x.Fnum| : ℤ) < b.vNum := hbX.1
      have hx_vnum_le : (b.vNum : ℤ) ≤ |beta * x.Fnum| := hvnumX
      have hx_vnum_le' : (b.vNum : ℤ) ≤ beta * |x.Fnum| := by
        have h1 : |beta * x.Fnum| = beta * |x.Fnum| := by
          rw [abs_mul, abs_of_pos hbeta_pos_int]
        rw [h1] at hx_vnum_le; exact hx_vnum_le
      -- From normal y: vNum ≤ beta * |y.Fnum|
      have hy_vnum_le : (b.vNum : ℤ) ≤ |beta * y.Fnum| := hvnumY
      have hy_vnum_le' : (b.vNum : ℤ) ≤ beta * |y.Fnum| := by
        have h1 : |beta * y.Fnum| = beta * |y.Fnum| := by
          rw [abs_mul, abs_of_pos hbeta_pos_int]
        rw [h1] at hy_vnum_le; exact hy_vnum_le
      -- vNum ≤ beta * |y.Fnum| and |x.Fnum| = |y.Fnum| * beta^d
      -- So |x.Fnum| ≥ |y.Fnum| * beta ≥ vNum (when d ≥ 1)
      -- But |x.Fnum| < vNum, contradiction
      have hbeta_ge_one : (1 : ℝ) ≤ (beta : ℝ) := by
        have h1le : (1 : ℤ) ≤ beta := le_of_lt hβ
        exact_mod_cast h1le
      have hpow_ge_beta : (beta : ℝ) ^ (y.Fexp - x.Fexp) ≥ (beta : ℝ) := by
        have h1 : (beta : ℝ) ^ (y.Fexp - x.Fexp) ≥ (beta : ℝ) ^ (1 : ℤ) := by
          apply zpow_le_zpow_right₀ hbeta_ge_one hd_ge_one
        simp only [zpow_one] at h1
        exact h1
      have h_yfnum_abs_pos : (0 : ℝ) < |(y.Fnum : ℝ)| := by
        have hy_neg_real : (y.Fnum : ℝ) < 0 := Int.cast_lt_zero.mpr hy_fnum_neg
        rw [abs_of_neg hy_neg_real]
        linarith
      have h_abs_ge : |(x.Fnum : ℝ)| ≥ |(y.Fnum : ℝ)| * (beta : ℝ) := by
        rw [h_abs_eq]
        apply mul_le_mul_of_nonneg_left hpow_ge_beta (le_of_lt h_yfnum_abs_pos)
      -- From hy_vnum_le': vNum ≤ beta * |y.Fnum|
      have h_vnum_le_real : (b.vNum : ℝ) ≤ (beta : ℝ) * |(y.Fnum : ℝ)| := by
        have hy_neg_real : (y.Fnum : ℝ) < 0 := Int.cast_lt_zero.mpr hy_fnum_neg
        rw [abs_of_neg hy_neg_real]
        have h := hy_vnum_le'
        have hy_abs_int : |y.Fnum| = -y.Fnum := abs_of_neg hy_fnum_neg
        rw [hy_abs_int] at h
        have h' : (b.vNum : ℝ) ≤ (beta : ℝ) * ((-y.Fnum) : ℝ) := by exact_mod_cast h
        exact h'
      -- So |x.Fnum| ≥ |y.Fnum| * beta ≥ vNum
      have h_abs_ge_vnum : |(x.Fnum : ℝ)| ≥ (b.vNum : ℝ) := by
        calc |(x.Fnum : ℝ)| ≥ |(y.Fnum : ℝ)| * (beta : ℝ) := h_abs_ge
          _ = (beta : ℝ) * |(y.Fnum : ℝ)| := by ring
          _ ≥ (b.vNum : ℝ) := h_vnum_le_real
      -- But |x.Fnum| < vNum
      have h_abs_lt_vnum : |(x.Fnum : ℝ)| < (b.vNum : ℝ) := by
        have h := hx_abs_lt
        have hx_neg_real : (x.Fnum : ℝ) < 0 := Int.cast_lt_zero.mpr hx_fnum_neg
        rw [abs_of_neg hx_neg_real]
        have hx_abs_int : |x.Fnum| = -x.Fnum := abs_of_neg hx_fnum_neg
        rw [hx_abs_int] at h
        have h' : ((-x.Fnum) : ℝ) < (b.vNum : ℝ) := by exact_mod_cast h
        exact h'
      linarith
    -- Case 2: x normal, y subnormal
    · exfalso
      -- y subnormal means y.Fexp = -b.dExp
      -- x normal means x.Fexp ≥ -b.dExp
      -- But we have x.Fexp < y.Fexp = -b.dExp, contradiction
      have hy_exp : y.Fexp = -b.dExp := hexpY
      have hx_exp_ge : -b.dExp ≤ x.Fexp := hbX.2
      omega
    -- Case 3: x subnormal, y normal
    · exfalso
      rw [hradix] at hvnumX hvnumY
      have hpow_x_pos : (0 : ℝ) < (beta : ℝ) ^ x.Fexp := zpow_pos hbeta_pos x.Fexp
      have hpow_y_pos : (0 : ℝ) < (beta : ℝ) ^ y.Fexp := zpow_pos hbeta_pos y.Fexp
      have hx_fnum_nonpos : x.Fnum ≤ (0 : ℤ) := by
        by_contra hcontra
        push Not at hcontra
        have hx_pos : (x.Fnum : ℝ) > 0 := Int.cast_pos.mpr hcontra
        have : (x.Fnum : ℝ) * (beta : ℝ) ^ x.Fexp > 0 := mul_pos hx_pos hpow_x_pos
        linarith
      have hy_fnum_nonpos : y.Fnum ≤ (0 : ℤ) := by
        by_contra hcontra
        push Not at hcontra
        have hy_pos : (y.Fnum : ℝ) > 0 := Int.cast_pos.mpr hcontra
        have : (y.Fnum : ℝ) * (beta : ℝ) ^ y.Fexp > 0 := mul_pos hy_pos hpow_y_pos
        linarith
      have hbeta_pos_int : (0 : ℤ) < beta := lt_trans (by norm_num : (0 : ℤ) < 1) hβ
      -- x subnormal: |beta * x.Fnum| < vNum
      -- y normal: vNum ≤ |beta * y.Fnum|
      -- So |beta * x.Fnum| < |beta * y.Fnum|, i.e., |x.Fnum| < |y.Fnum|
      have h_abs_lt : |beta * x.Fnum| < |beta * y.Fnum| := lt_of_lt_of_le hvnumX hvnumY
      have h_abs_fnum_lt : |x.Fnum| < |y.Fnum| := by
        simp only [abs_mul, abs_of_pos hbeta_pos_int] at h_abs_lt
        exact Int.lt_of_mul_lt_mul_left h_abs_lt (le_of_lt hbeta_pos_int)
      -- If F2R x = F2R y and x.Fexp < y.Fexp
      -- x.Fnum * beta^x.Fexp = y.Fnum * beta^y.Fexp
      -- Both nonpositive, so if either is 0, both are 0
      -- But y normal requires y.Fnum ≠ 0 (vNum ≤ |beta * y.Fnum| with vNum > 0)
      have hvnum_pos : (0 : ℤ) < b.vNum := by
        -- From hbY.1: |y.Fnum| < b.vNum
        -- Since |y.Fnum| ≥ 0, we have 0 ≤ |y.Fnum| < b.vNum, so 0 < b.vNum
        have hbound := hbY.1
        have h_abs_nonneg : (0 : ℤ) ≤ |y.Fnum| := abs_nonneg _
        omega
      have hy_fnum_neg : y.Fnum < 0 := by
        rcases eq_or_lt_of_le hy_fnum_nonpos with hyz | hyneg
        · simp only [hyz, mul_zero, abs_zero] at hvnumY; omega
        · exact hyneg
      -- x subnormal means x.Fexp = -b.dExp
      have hx_exp : x.Fexp = -b.dExp := hexpX
      -- y normal means y.Fexp ≥ -b.dExp
      have hy_exp_ge : -b.dExp ≤ y.Fexp := hbY.2
      -- We have x.Fexp < y.Fexp, so -b.dExp < y.Fexp
      have hexp_strict : -b.dExp < y.Fexp := by omega
      -- F2R x = x.Fnum * beta^(-b.dExp)
      -- F2R y = y.Fnum * beta^y.Fexp with y.Fexp > -b.dExp
      -- So F2R y = y.Fnum * beta^y.Fexp
      -- If F2R x = F2R y:
      -- x.Fnum * beta^(-b.dExp) = y.Fnum * beta^y.Fexp
      -- x.Fnum = y.Fnum * beta^(y.Fexp - (-b.dExp)) = y.Fnum * beta^(y.Fexp + b.dExp)
      -- y.Fexp + b.dExp > 0, so beta^(y.Fexp + b.dExp) > 1
      -- |x.Fnum| = |y.Fnum| * beta^(y.Fexp + b.dExp) > |y.Fnum|
      -- But we showed |x.Fnum| < |y.Fnum|, contradiction
      have hd_pos : y.Fexp + b.dExp > 0 := by omega
      have hpow_ne_x : (beta : ℝ) ^ x.Fexp ≠ 0 := ne_of_gt hpow_x_pos
      have h_fnum_eq : (x.Fnum : ℝ) = (y.Fnum : ℝ) * (beta : ℝ) ^ (y.Fexp - x.Fexp) := by
        have h1 : (x.Fnum : ℝ) * (beta : ℝ) ^ x.Fexp = (y.Fnum : ℝ) * (beta : ℝ) ^ y.Fexp := h_eq
        have h2 : (beta : ℝ) ^ y.Fexp = (beta : ℝ) ^ (y.Fexp - x.Fexp) * (beta : ℝ) ^ x.Fexp := by
          rw [← zpow_add₀ (ne_of_gt hbeta_pos)]
          ring_nf
        rw [h2] at h1
        field_simp at h1
        linarith
      have hd_ge_one : y.Fexp - x.Fexp ≥ 1 := by omega
      have hpow_d_gt_one : (beta : ℝ) ^ (y.Fexp - x.Fexp) > 1 := by
        have hbeta_gt_one : (beta : ℝ) > 1 := by exact_mod_cast hβ
        have h1 : (beta : ℝ) ^ (y.Fexp - x.Fexp) ≥ (beta : ℝ) ^ (1 : ℤ) := by
          apply zpow_le_zpow_right₀ (le_of_lt hbeta_gt_one) hd_ge_one
        simp only [zpow_one] at h1
        linarith
      -- x.Fnum ≤ 0, so either x.Fnum = 0 or x.Fnum < 0
      rcases eq_or_lt_of_le hx_fnum_nonpos with hxz | hx_fnum_neg
      · -- x.Fnum = 0, so F2R x = 0, so F2R y = 0, so y.Fnum = 0
        -- But y normal requires vNum ≤ |beta * y.Fnum|, contradiction
        simp only [hxz, Int.cast_zero, zero_mul] at h_eq
        -- h_eq: 0 = y.Fnum * beta^y.Fexp with beta^y.Fexp > 0, so y.Fnum = 0
        have hy_fnum_zero : (y.Fnum : ℝ) = 0 := by
          have hpow_ne : (beta : ℝ) ^ y.Fexp ≠ 0 := ne_of_gt hpow_y_pos
          have h_eq' : (y.Fnum : ℝ) * (beta : ℝ) ^ y.Fexp = 0 := h_eq.symm
          exact (mul_eq_zero.mp h_eq').resolve_right hpow_ne
        have hy_fnum_zero_int : y.Fnum = 0 := by exact_mod_cast hy_fnum_zero
        linarith
      · -- x.Fnum < 0
        have hx_neg_real : (x.Fnum : ℝ) < 0 := Int.cast_lt_zero.mpr hx_fnum_neg
        have hy_neg_real : (y.Fnum : ℝ) < 0 := Int.cast_lt_zero.mpr hy_fnum_neg
        have h_abs_eq : |(x.Fnum : ℝ)| = |(y.Fnum : ℝ)| * (beta : ℝ) ^ (y.Fexp - x.Fexp) := by
          rw [abs_of_neg hx_neg_real, abs_of_neg hy_neg_real]
          rw [h_fnum_eq]
          ring
        have h_yfnum_abs_pos : (0 : ℝ) < |(y.Fnum : ℝ)| := by
          rw [abs_of_neg hy_neg_real]
          linarith
        have h_abs_gt : |(x.Fnum : ℝ)| > |(y.Fnum : ℝ)| := by
          rw [h_abs_eq]
          have h1 : |(y.Fnum : ℝ)| * (beta : ℝ) ^ (y.Fexp - x.Fexp) > |(y.Fnum : ℝ)| * 1 := by
            apply mul_lt_mul_of_pos_left hpow_d_gt_one h_yfnum_abs_pos
          simp at h1
          exact h1
        -- But we have |x.Fnum| < |y.Fnum|
        have h_abs_lt_real : |(x.Fnum : ℝ)| < |(y.Fnum : ℝ)| := by
          have h := h_abs_fnum_lt
          rw [abs_of_neg hx_neg_real, abs_of_neg hy_neg_real]
          have hx_abs_int : |x.Fnum| = -x.Fnum := abs_of_neg hx_fnum_neg
          have hy_abs_int : |y.Fnum| = -y.Fnum := abs_of_neg hy_fnum_neg
          rw [hx_abs_int, hy_abs_int] at h
          have h' : ((-x.Fnum) : ℝ) < ((-y.Fnum) : ℝ) := by exact_mod_cast h
          exact h'
        linarith
    -- Case 4: Both subnormal
    · exfalso
      -- Both subnormal: x.Fexp = -b.dExp and y.Fexp = -b.dExp
      -- But we have x.Fexp < y.Fexp, contradiction
      have hx_exp : x.Fexp = -b.dExp := hexpX
      have hy_exp : y.Fexp = -b.dExp := hexpY
      omega

noncomputable def NormalAndSubNormalNotEq_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `NormalAndSubNormalNotEq` — a normal float and a subnormal float
represent different real numbers.

Note: Uses `Fnormal'` and `Fsubnormal'`, the Coq-matching predicates.
Requires `radix = beta` and `1 < beta`. -/
theorem NormalAndSubNormalNotEq_internal {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta)
    (hβ : 1 < beta)
    (hradix : radix = beta) :
    ⦃⌜Fnormal' (beta:=beta) radix b p ∧
        Fsubnormal' (beta:=beta) radix b q⌝⦄
    (pure (NormalAndSubNormalNotEq_check (beta:=beta) radix b p q) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) p ≠ _root_.F2R (beta:=beta) q⌝⦄ := by
  intro ⟨hNormal, hSubnormal⟩
  simp only [wp, PostCond.noThrow, pure, NormalAndSubNormalNotEq_check, ULift.down_up]
  -- Unfold the definitions
  -- Fnormal' p: Fbounded' b p ∧ vNum ≤ |radix * p.Fnum|
  -- Fsubnormal' q: Fbounded' b q ∧ q.Fexp = -dExp ∧ |radix * q.Fnum| < vNum
  obtain ⟨hBoundedP, hvNumP⟩ := hNormal
  obtain ⟨hBoundedQ, hExpQ, hvNumQ⟩ := hSubnormal
  -- From Fbounded': |p.Fnum| < vNum ∧ -dExp ≤ p.Fexp
  obtain ⟨hFnumP_lt, hExpP_ge⟩ := hBoundedP
  obtain ⟨hFnumQ_lt, _⟩ := hBoundedQ
  -- Key: |radix * q.Fnum| < vNum ≤ |radix * p.Fnum|
  have h_radix_pos : (0 : ℤ) < radix := by rw [hradix]; omega
  have h_radix_ne_zero : radix ≠ 0 := ne_of_gt h_radix_pos
  have h_beta_pos_int : (0 : ℤ) < beta := lt_trans (by norm_num : (0 : ℤ) < 1) hβ
  have h_beta_pos : (0 : ℝ) < (beta : ℝ) := by exact_mod_cast h_beta_pos_int
  have h_beta_gt_one : (1 : ℝ) < (beta : ℝ) := by exact_mod_cast hβ
  -- The core inequality: |radix * q.Fnum| < |radix * p.Fnum|
  have hcore : |radix * q.Fnum| < |radix * p.Fnum| := lt_of_lt_of_le hvNumQ hvNumP
  -- Prove F2R p ≠ F2R q by showing they can't be equal
  intro heq
  -- F2R p = p.Fnum * beta^p.Fexp and F2R q = q.Fnum * beta^q.Fexp
  unfold _root_.F2R at heq
  -- From p.Fexp ≥ -dExp = q.Fexp
  have hExpP_ge_Q : p.Fexp ≥ q.Fexp := by rw [hExpQ]; exact hExpP_ge
  -- Case split: p.Fexp = q.Fexp or p.Fexp > q.Fexp
  rcases eq_or_lt_of_le hExpP_ge_Q with hExpEq | hExpLt
  · -- Case: p.Fexp = q.Fexp
    -- Then p.Fnum * beta^e = q.Fnum * beta^e, so p.Fnum = q.Fnum
    have h_pow_eq : (beta : ℝ) ^ p.Fexp = (beta : ℝ) ^ q.Fexp := by rw [hExpEq]
    have h_pow_pos : (0 : ℝ) < (beta : ℝ) ^ q.Fexp := zpow_pos h_beta_pos q.Fexp
    have h_pow_ne : (beta : ℝ) ^ q.Fexp ≠ 0 := ne_of_gt h_pow_pos
    have hFnumEq : (p.Fnum : ℝ) = (q.Fnum : ℝ) := by
      have h1 : (p.Fnum : ℝ) * (beta : ℝ) ^ p.Fexp = (q.Fnum : ℝ) * (beta : ℝ) ^ q.Fexp := heq
      rw [h_pow_eq] at h1
      exact mul_right_cancel₀ h_pow_ne h1
    have hFnumEqInt : p.Fnum = q.Fnum := by exact_mod_cast hFnumEq
    -- But then |radix * p.Fnum| = |radix * q.Fnum|
    have h_abs_eq : |radix * p.Fnum| = |radix * q.Fnum| := by rw [hFnumEqInt]
    -- This contradicts |radix * q.Fnum| < |radix * p.Fnum|
    omega
  · -- Case: p.Fexp > q.Fexp (i.e., q.Fexp < p.Fexp)
    -- Let d = p.Fexp - q.Fexp > 0
    -- From heq: p.Fnum * beta^p.Fexp = q.Fnum * beta^q.Fexp
    -- So: p.Fnum * beta^d = q.Fnum (after dividing by beta^q.Fexp)
    have hd_pos : p.Fexp - q.Fexp > 0 := by omega
    have hd_ge_one : p.Fexp - q.Fexp ≥ 1 := by omega
    have h_pow_q_pos : (0 : ℝ) < (beta : ℝ) ^ q.Fexp := zpow_pos h_beta_pos q.Fexp
    have h_pow_q_ne : (beta : ℝ) ^ q.Fexp ≠ 0 := ne_of_gt h_pow_q_pos
    have h_pow_d_pos : (0 : ℝ) < (beta : ℝ) ^ (p.Fexp - q.Fexp) := zpow_pos h_beta_pos (p.Fexp - q.Fexp)
    have h_pow_d_ge_beta : (beta : ℝ) ^ (p.Fexp - q.Fexp) ≥ (beta : ℝ) := by
      have h1 : (beta : ℝ) ^ (p.Fexp - q.Fexp) ≥ (beta : ℝ) ^ (1 : ℤ) := by
        apply zpow_le_zpow_right₀ (le_of_lt h_beta_gt_one) hd_ge_one
      simp only [zpow_one] at h1
      exact h1
    have h_pow_d_gt_one : (beta : ℝ) ^ (p.Fexp - q.Fexp) > 1 := by linarith
    -- Rewrite heq to get relationship between Fnum values
    have hFnumRel : (p.Fnum : ℝ) * (beta : ℝ) ^ (p.Fexp - q.Fexp) = (q.Fnum : ℝ) := by
      have h1 : (p.Fnum : ℝ) * (beta : ℝ) ^ p.Fexp = (q.Fnum : ℝ) * (beta : ℝ) ^ q.Fexp := heq
      have h2 : (beta : ℝ) ^ p.Fexp = (beta : ℝ) ^ (p.Fexp - q.Fexp) * (beta : ℝ) ^ q.Fexp := by
        rw [← zpow_add₀ (ne_of_gt h_beta_pos)]
        ring_nf
      rw [h2] at h1
      have h3 : (p.Fnum : ℝ) * ((beta : ℝ) ^ (p.Fexp - q.Fexp) * (beta : ℝ) ^ q.Fexp) =
                (q.Fnum : ℝ) * (beta : ℝ) ^ q.Fexp := h1
      have h4 : (p.Fnum : ℝ) * (beta : ℝ) ^ (p.Fexp - q.Fexp) * (beta : ℝ) ^ q.Fexp =
                (q.Fnum : ℝ) * (beta : ℝ) ^ q.Fexp := by ring_nf; ring_nf at h3; exact h3
      exact mul_right_cancel₀ h_pow_q_ne h4
    -- From hFnumRel: |p.Fnum| * beta^d = |q.Fnum| (taking absolute values)
    -- Note: |a * b| = |a| * |b| for reals, and beta^d > 0
    have h_abs_rel : |(p.Fnum : ℝ)| * (beta : ℝ) ^ (p.Fexp - q.Fexp) = |(q.Fnum : ℝ)| := by
      have h_pow_nonneg : (0 : ℝ) ≤ (beta : ℝ) ^ (p.Fexp - q.Fexp) := le_of_lt h_pow_d_pos
      -- |p.Fnum * beta^d| = |p.Fnum| * |beta^d| = |p.Fnum| * beta^d (since beta^d ≥ 0)
      have h1 : |(p.Fnum : ℝ) * (beta : ℝ) ^ (p.Fexp - q.Fexp)| =
                |(p.Fnum : ℝ)| * |(beta : ℝ) ^ (p.Fexp - q.Fexp)| := abs_mul _ _
      have h2 : |(beta : ℝ) ^ (p.Fexp - q.Fexp)| = (beta : ℝ) ^ (p.Fexp - q.Fexp) :=
        abs_of_nonneg h_pow_nonneg
      rw [h2] at h1
      rw [hFnumRel] at h1
      exact h1.symm
    -- Since beta^d > 1, we have |p.Fnum| < |q.Fnum| (unless p.Fnum = 0)
    -- But from hvNumP: vNum ≤ |radix * p.Fnum|, so p.Fnum ≠ 0 if radix ≠ 0 and vNum > 0
    -- And from hvNumQ: |radix * q.Fnum| < vNum
    -- So |radix * q.Fnum| < |radix * p.Fnum| (from hcore)
    -- But |p.Fnum| * beta^d = |q.Fnum| and beta^d ≥ beta > 1
    -- So |q.Fnum| = |p.Fnum| * beta^d ≥ |p.Fnum| * beta
    -- Thus |radix * q.Fnum| = |radix| * |q.Fnum| ≥ |radix| * |p.Fnum| * beta = |radix * p.Fnum| * beta
    -- But |radix * q.Fnum| < vNum ≤ |radix * p.Fnum|, contradiction if |p.Fnum| ≠ 0
    -- And if |p.Fnum| = 0, then vNum ≤ |radix * 0| = 0, so vNum ≤ 0
    -- But vNum is from Fbound_skel, which should be positive (from vNum ≤ |radix * p.Fnum| and |q.Fnum| < vNum)
    by_cases hp_zero : p.Fnum = 0
    · -- If p.Fnum = 0, then |radix * p.Fnum| = 0, so vNum ≤ 0
      -- But |q.Fnum| < vNum from hFnumQ_lt, so vNum > 0 if q.Fnum ≠ 0
      -- And from hFnumRel: 0 * beta^d = q.Fnum, so q.Fnum = 0
      -- Then |radix * q.Fnum| = 0 < vNum, so vNum > 0
      -- But vNum ≤ |radix * p.Fnum| = 0, contradiction
      have hp_radix_zero : |radix * p.Fnum| = 0 := by simp [hp_zero]
      have hvNum_le_zero : (b.vNum : ℤ) ≤ 0 := by
        calc (b.vNum : ℤ) ≤ |radix * p.Fnum| := hvNumP
             _ = 0 := hp_radix_zero
      have hq_zero : q.Fnum = 0 := by
        have h1 : (p.Fnum : ℝ) * (beta : ℝ) ^ (p.Fexp - q.Fexp) = (q.Fnum : ℝ) := hFnumRel
        simp only [hp_zero, Int.cast_zero, zero_mul] at h1
        exact_mod_cast h1.symm
      have hq_radix_zero : |radix * q.Fnum| = 0 := by simp [hq_zero]
      have hvNum_pos : (0 : ℤ) < b.vNum := by
        have h1 : |radix * q.Fnum| < b.vNum := hvNumQ
        rw [hq_radix_zero] at h1
        exact h1
      omega
    · -- p.Fnum ≠ 0
      -- From h_abs_rel: |p.Fnum| * beta^d = |q.Fnum|
      -- Since beta^d ≥ beta > 1, we have |q.Fnum| > |p.Fnum|
      have hp_abs_pos : (0 : ℝ) < |(p.Fnum : ℝ)| := by
        rw [abs_pos]
        exact_mod_cast hp_zero
      have hq_abs_gt : |(q.Fnum : ℝ)| > |(p.Fnum : ℝ)| := by
        rw [← h_abs_rel]
        have h1 : |(p.Fnum : ℝ)| * (beta : ℝ) ^ (p.Fexp - q.Fexp) > |(p.Fnum : ℝ)| * 1 := by
          apply mul_lt_mul_of_pos_left h_pow_d_gt_one hp_abs_pos
        simp at h1
        exact h1
      -- Now: |radix * q.Fnum| = |radix| * |q.Fnum| > |radix| * |p.Fnum| = |radix * p.Fnum|
      have h_radix_abs : (|radix| : ℤ) = radix := abs_of_pos h_radix_pos
      have h1 : |radix * q.Fnum| = |radix| * |q.Fnum| := abs_mul radix q.Fnum
      have h2 : |radix * p.Fnum| = |radix| * |p.Fnum| := abs_mul radix p.Fnum
      have hq_gt_p : |radix * q.Fnum| > |radix * p.Fnum| := by
        rw [h1, h2, h_radix_abs]
        have h3 : (radix : ℤ) * |q.Fnum| > radix * |p.Fnum| := by
          apply mul_lt_mul_of_pos_left _ h_radix_pos
          have hq_abs_gt_int : |q.Fnum| > |p.Fnum| := by
            have h4 : |(q.Fnum : ℝ)| > |(p.Fnum : ℝ)| := hq_abs_gt
            -- Convert |(x : ℝ)| to (|x| : ℝ) using Int.cast_abs: ↑|a| = |↑a|
            rw [← Int.cast_abs (R := ℝ), ← Int.cast_abs (R := ℝ)] at h4
            exact_mod_cast h4
          exact hq_abs_gt_int
        exact h3
      -- But hcore says |radix * q.Fnum| < |radix * p.Fnum|
      omega

/-- Coq: `NormalAndSubNormalNotEq` with the exported precision-section
contract restored.  The power-bound and precision premises are intentionally
kept even though the internal proof establishes a stronger result. -/
theorem NormalAndSubNormalNotEq (radix : Int) [ValidRadix radix]
    (b : Fbound_skel) (precision : Nat)
    (p q : FloatSpec.Core.Defs.FlocqFloat radix) :
    ⦃⌜1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision ∧
        Fnormal (beta:=radix) radix b p ∧
        Fsubnormal (beta:=radix) radix b q⌝⦄
    (pure (NormalAndSubNormalNotEq_check (beta:=radix) radix b p q) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=radix) p ≠
        _root_.F2R (beta:=radix) q⌝⦄ := by
  intro ⟨hradix, _hprecision, _hvNum, hp, hq⟩
  exact NormalAndSubNormalNotEq_internal (beta:=radix)
    radix b p q hradix rfl ⟨hp, hq⟩

noncomputable def FSuccProp_cross_zero_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) : Unit := ()

/-- Case reducer for `FSuccPropNeg`: if the upper float is nonnegative, the
successor of the negative lower float is still below it. -/
theorem FSuccProp_cross_zero {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision ∧
        _root_.F2R (beta:=beta) x < 0 ∧
        0 ≤ _root_.F2R (beta:=beta) y⌝⦄
    (pure (FSuccProp_cross_zero_check (beta:=beta) b radix precision x y) :
      Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta)
        (FSucc (beta:=beta) b radix precision x) ≤
      _root_.F2R (beta:=beta) y⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hradix, hprecision, hvnum, hxneg, hy_nonneg⟩
  simp only [wp, PostCond.noThrow, pure, FSuccProp_cross_zero_check, Id.run,
    ULift.up_down]
  have hsucc_nonpos :
      _root_.F2R (beta:=beta)
          (FSucc (beta:=beta) b radix precision x) ≤ 0 := by
    have hsucc := R0RltRleSucc (beta:=beta) b radix precision x
    simpa only [wp, PostCond.noThrow, pure, R0RltRleSucc_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Id.run,
      ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
      hsucc ⟨hbeta, hradix, hprecision, hvnum, hxneg⟩
  exact le_trans hsucc_nonpos hy_nonneg

noncomputable def FSucc_same_exp_mantissa_le_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) : Unit := ()

/-- Same-exponent case reducer for successor ordering: if `FSucc x` keeps
`x`'s exponent and increments the mantissa, then any float with the same
exponent and a larger mantissa is above it. -/
theorem FSucc_same_exp_mantissa_le {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜FSucc (beta:=beta) b radix precision x =
          (⟨x.Fnum + 1, x.Fexp⟩ :
            FloatSpec.Core.Defs.FlocqFloat beta) ∧
        x.Fexp = y.Fexp ∧ x.Fnum < y.Fnum ∧ 1 < beta⌝⦄
    (pure (FSucc_same_exp_mantissa_le_check (beta:=beta) b radix precision x y) :
      Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta)
        (FSucc (beta:=beta) b radix precision x) ≤
      _root_.F2R (beta:=beta) y⌝⦄ := by
  intro h
  rcases h with ⟨hsucc, hexp, hnum, hbeta⟩
  simp only [wp, PostCond.noThrow, pure, FSucc_same_exp_mantissa_le_check,
    Id.run, ULift.up_down]
  have hnum_le : x.Fnum + 1 ≤ y.Fnum := by omega
  rw [hsucc]
  simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
    FloatSpec.Core.Defs.FlocqFloat.Fnum,
    FloatSpec.Core.Defs.FlocqFloat.Fexp]
  rw [hexp]
  exact mul_le_mul_of_nonneg_right (by exact_mod_cast hnum_le)
    (le_of_lt (zpow_pos
      (by exact_mod_cast (show (0 : Int) < beta by omega)) y.Fexp))

noncomputable def FcanonicUnique_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FcanonicUnique` — canonical floats that represent the same real
number are equal as floats.

Note: Uses `Fcanonic'`, the Coq-matching canonical predicate.
This theorem requires the canonical form structure to be meaningful.
Requires `1 < beta` (radixMoreThanOne in Coq) and `radix = beta`. -/
theorem FcanonicUnique {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta)
    (hβ : 1 < beta)
    (hradix : radix = beta) :
    ⦃⌜Fcanonic' (beta:=beta) radix b p ∧
        Fcanonic' (beta:=beta) radix b q ∧
        _root_.F2R (beta:=beta) p = _root_.F2R (beta:=beta) q⌝⦄
    (pure (FcanonicUnique_check (beta:=beta) radix b p q) : Id Unit)
    ⦃⇓_ => ⌜p = q⌝⦄ := by
  intro ⟨hcanP, hcanQ, heqF2R⟩
  simp only [wp, PostCond.noThrow, pure, FcanonicUnique_check, ULift.down_up]
  -- Derive beta > 0 from 1 < beta
  have hbeta_pos_int : (0 : ℤ) < beta := lt_trans (by norm_num : (0 : ℤ) < 1) hβ
  have hbeta_pos : (0 : ℝ) < (beta : ℝ) := Int.cast_pos.mpr hbeta_pos_int
  have hbeta_ne_zero : (beta : ℝ) ≠ 0 := ne_of_gt hbeta_pos
  have hbeta_gt_one : (1 : ℝ) < (beta : ℝ) := by exact_mod_cast hβ
  -- Unfold F2R in heqF2R
  simp only [_root_.F2R, FloatSpec.Core.Defs.F2R] at heqF2R
  -- heqF2R : p.Fnum * beta ^ p.Fexp = q.Fnum * beta ^ q.Fexp
  -- Case analysis on canonical form of p and q
  rcases hcanP with ⟨hbP, hvnumP⟩ | ⟨hbP, hexpP, hvnumP⟩
  <;> rcases hcanQ with ⟨hbQ, hvnumQ⟩ | ⟨hbQ, hexpQ, hvnumQ⟩
  -- Case 1: p normal, q normal
  · -- Both normal: use NormalAndSubNormalNotEq logic in reverse
    -- Normal: vNum ≤ |radix * Fnum| and Fbounded'
    rw [hradix] at hvnumP hvnumQ
    -- Need to show p.Fexp = q.Fexp and p.Fnum = q.Fnum
    -- First, show p.Fexp = q.Fexp
    have hexp_eq : p.Fexp = q.Fexp := by
      by_contra hne
      rcases Ne.lt_or_gt hne with hlt | hgt
      · -- p.Fexp < q.Fexp
        -- From heqF2R: p.Fnum * beta^p.Fexp = q.Fnum * beta^q.Fexp
        -- Rearranging: p.Fnum = q.Fnum * beta^(q.Fexp - p.Fexp)
        have hd_pos : q.Fexp - p.Fexp > 0 := by omega
        have hd_ge_one : q.Fexp - p.Fexp ≥ 1 := by omega
        have hpow_p_pos : (0 : ℝ) < (beta : ℝ) ^ p.Fexp := zpow_pos hbeta_pos p.Fexp
        have hpow_p_ne : (beta : ℝ) ^ p.Fexp ≠ 0 := ne_of_gt hpow_p_pos
        have hpow_d_pos : (0 : ℝ) < (beta : ℝ) ^ (q.Fexp - p.Fexp) := zpow_pos hbeta_pos (q.Fexp - p.Fexp)
        have hpow_d_ge_beta : (beta : ℝ) ^ (q.Fexp - p.Fexp) ≥ (beta : ℝ) := by
          have h1 : (beta : ℝ) ^ (q.Fexp - p.Fexp) ≥ (beta : ℝ) ^ (1 : ℤ) :=
            zpow_le_zpow_right₀ (le_of_lt hbeta_gt_one) hd_ge_one
          simp only [zpow_one] at h1
          exact h1
        -- From heqF2R: p.Fnum * beta^p.Fexp = q.Fnum * beta^q.Fexp
        -- Divide by beta^p.Fexp: p.Fnum = q.Fnum * beta^(q.Fexp - p.Fexp)
        have hrel : (p.Fnum : ℝ) = (q.Fnum : ℝ) * (beta : ℝ) ^ (q.Fexp - p.Fexp) := by
          have h1 : (beta : ℝ) ^ q.Fexp = (beta : ℝ) ^ (q.Fexp - p.Fexp) * (beta : ℝ) ^ p.Fexp := by
            rw [← zpow_add₀ hbeta_ne_zero]
            ring_nf
          have h2 : (q.Fnum : ℝ) * (beta : ℝ) ^ q.Fexp =
                    (q.Fnum : ℝ) * ((beta : ℝ) ^ (q.Fexp - p.Fexp) * (beta : ℝ) ^ p.Fexp) := by
            rw [h1]
          rw [h2] at heqF2R
          -- heqF2R : p.Fnum * beta^p.Fexp = q.Fnum * (beta^d * beta^p.Fexp)
          have h3 : (q.Fnum : ℝ) * ((beta : ℝ) ^ (q.Fexp - p.Fexp) * (beta : ℝ) ^ p.Fexp) =
                    (q.Fnum : ℝ) * (beta : ℝ) ^ (q.Fexp - p.Fexp) * (beta : ℝ) ^ p.Fexp := by ring
          rw [h3] at heqF2R
          exact mul_right_cancel₀ hpow_p_ne heqF2R
        -- Now |p.Fnum| = |q.Fnum| * beta^(q.Fexp - p.Fexp) ≥ |q.Fnum| * beta
        have habs_rel : |(p.Fnum : ℝ)| = |(q.Fnum : ℝ)| * (beta : ℝ) ^ (q.Fexp - p.Fexp) := by
          rw [hrel, abs_mul, abs_of_pos hpow_d_pos]
        -- From boundedness: |p.Fnum| < vNum
        have hp_abs_bound : |p.Fnum| < (b.vNum : ℤ) := hbP.1
        -- From normality of q: vNum ≤ |beta * q.Fnum|
        have hq_vnum_le : (b.vNum : ℤ) ≤ |beta * q.Fnum| := hvnumQ
        -- So |p.Fnum| < vNum ≤ |beta * q.Fnum| = beta * |q.Fnum| (since beta > 0)
        have hbeta_abs : |beta| = beta := abs_of_pos hbeta_pos_int
        have hq_abs_le : (b.vNum : ℤ) ≤ beta * |q.Fnum| := by
          calc (b.vNum : ℤ) ≤ |beta * q.Fnum| := hq_vnum_le
            _ = |beta| * |q.Fnum| := abs_mul beta q.Fnum
            _ = beta * |q.Fnum| := by rw [hbeta_abs]
        -- So |p.Fnum| < beta * |q.Fnum|
        have hp_lt_beta_q : |p.Fnum| < beta * |q.Fnum| := lt_of_lt_of_le hp_abs_bound hq_abs_le
        -- But from habs_rel: |p.Fnum| = |q.Fnum| * beta^d ≥ |q.Fnum| * beta
        have hp_ge_q_beta : |(p.Fnum : ℝ)| ≥ |(q.Fnum : ℝ)| * (beta : ℝ) := by
          rw [habs_rel]
          apply mul_le_mul_of_nonneg_left hpow_d_ge_beta (abs_nonneg _)
        -- Contradiction: |p.Fnum| < beta * |q.Fnum| but |p.Fnum| ≥ |q.Fnum| * beta
        have hp_lt_real : |(p.Fnum : ℝ)| < (beta : ℝ) * |(q.Fnum : ℝ)| := by
          have h1 : (|p.Fnum| : ℝ) < ((beta * |q.Fnum|) : ℤ) := by exact_mod_cast hp_lt_beta_q
          simp only [Int.cast_mul, Int.cast_abs] at h1
          exact h1
        have hp_ge_real' : |(p.Fnum : ℝ)| ≥ (beta : ℝ) * |(q.Fnum : ℝ)| := by
          rw [mul_comm] at hp_ge_q_beta
          exact hp_ge_q_beta
        linarith
      · -- q.Fexp < p.Fexp: symmetric argument
        have hd_pos : p.Fexp - q.Fexp > 0 := by omega
        have hd_ge_one : p.Fexp - q.Fexp ≥ 1 := by omega
        have hpow_q_pos : (0 : ℝ) < (beta : ℝ) ^ q.Fexp := zpow_pos hbeta_pos q.Fexp
        have hpow_q_ne : (beta : ℝ) ^ q.Fexp ≠ 0 := ne_of_gt hpow_q_pos
        have hpow_d_pos : (0 : ℝ) < (beta : ℝ) ^ (p.Fexp - q.Fexp) := zpow_pos hbeta_pos (p.Fexp - q.Fexp)
        have hpow_d_ge_beta : (beta : ℝ) ^ (p.Fexp - q.Fexp) ≥ (beta : ℝ) := by
          have h1 : (beta : ℝ) ^ (p.Fexp - q.Fexp) ≥ (beta : ℝ) ^ (1 : ℤ) :=
            zpow_le_zpow_right₀ (le_of_lt hbeta_gt_one) hd_ge_one
          simp only [zpow_one] at h1
          exact h1
        -- From heqF2R: q.Fnum = p.Fnum * beta^(p.Fexp - q.Fexp)
        have hrel : (q.Fnum : ℝ) = (p.Fnum : ℝ) * (beta : ℝ) ^ (p.Fexp - q.Fexp) := by
          have h1 : (beta : ℝ) ^ p.Fexp = (beta : ℝ) ^ (p.Fexp - q.Fexp) * (beta : ℝ) ^ q.Fexp := by
            rw [← zpow_add₀ hbeta_ne_zero]
            ring_nf
          have h2 : (p.Fnum : ℝ) * (beta : ℝ) ^ p.Fexp =
                    (p.Fnum : ℝ) * ((beta : ℝ) ^ (p.Fexp - q.Fexp) * (beta : ℝ) ^ q.Fexp) := by
            rw [h1]
          rw [h2] at heqF2R
          -- heqF2R : p.Fnum * (beta^d * beta^q.Fexp) = q.Fnum * beta^q.Fexp
          have h3 : (p.Fnum : ℝ) * ((beta : ℝ) ^ (p.Fexp - q.Fexp) * (beta : ℝ) ^ q.Fexp) =
                    (p.Fnum : ℝ) * (beta : ℝ) ^ (p.Fexp - q.Fexp) * (beta : ℝ) ^ q.Fexp := by ring
          rw [h3] at heqF2R
          -- heqF2R : p.Fnum * beta^d * beta^q.Fexp = q.Fnum * beta^q.Fexp
          have h4 := mul_right_cancel₀ hpow_q_ne heqF2R
          -- h4 : p.Fnum * beta^d = q.Fnum
          exact h4.symm
        -- Now |q.Fnum| = |p.Fnum| * beta^d ≥ |p.Fnum| * beta
        have habs_rel : |(q.Fnum : ℝ)| = |(p.Fnum : ℝ)| * (beta : ℝ) ^ (p.Fexp - q.Fexp) := by
          rw [hrel, abs_mul, abs_of_pos hpow_d_pos]
        -- From boundedness: |q.Fnum| < vNum
        have hq_abs_bound : |q.Fnum| < (b.vNum : ℤ) := hbQ.1
        -- From normality of p: vNum ≤ |beta * p.Fnum|
        have hp_vnum_le : (b.vNum : ℤ) ≤ |beta * p.Fnum| := hvnumP
        have hbeta_abs : |beta| = beta := abs_of_pos hbeta_pos_int
        have hp_abs_le : (b.vNum : ℤ) ≤ beta * |p.Fnum| := by
          calc (b.vNum : ℤ) ≤ |beta * p.Fnum| := hp_vnum_le
            _ = |beta| * |p.Fnum| := abs_mul beta p.Fnum
            _ = beta * |p.Fnum| := by rw [hbeta_abs]
        have hq_lt_beta_p : |q.Fnum| < beta * |p.Fnum| := lt_of_lt_of_le hq_abs_bound hp_abs_le
        have hq_ge_p_beta : |(q.Fnum : ℝ)| ≥ |(p.Fnum : ℝ)| * (beta : ℝ) := by
          rw [habs_rel]
          apply mul_le_mul_of_nonneg_left hpow_d_ge_beta (abs_nonneg _)
        have hq_lt_real : |(q.Fnum : ℝ)| < (beta : ℝ) * |(p.Fnum : ℝ)| := by
          have h1 : (|q.Fnum| : ℝ) < ((beta * |p.Fnum|) : ℤ) := by exact_mod_cast hq_lt_beta_p
          simp only [Int.cast_mul, Int.cast_abs] at h1
          exact h1
        have hq_ge_real' : |(q.Fnum : ℝ)| ≥ (beta : ℝ) * |(p.Fnum : ℝ)| := by
          rw [mul_comm] at hq_ge_p_beta
          exact hq_ge_p_beta
        linarith
    -- Now p.Fexp = q.Fexp, derive p.Fnum = q.Fnum from heqF2R
    have hfnum_eq : p.Fnum = q.Fnum := by
      have hpow_pos : (0 : ℝ) < (beta : ℝ) ^ q.Fexp := zpow_pos hbeta_pos q.Fexp
      have hpow_ne : (beta : ℝ) ^ q.Fexp ≠ 0 := ne_of_gt hpow_pos
      rw [hexp_eq] at heqF2R
      have h : (p.Fnum : ℝ) = (q.Fnum : ℝ) := mul_right_cancel₀ hpow_ne heqF2R
      exact_mod_cast h
    -- Conclude p = q using structure equality
    cases p
    cases q
    simp only [FloatSpec.Core.Defs.FlocqFloat.mk.injEq]
    exact ⟨hfnum_eq, hexp_eq⟩
  -- Case 2: p normal, q subnormal - contradiction
  · exfalso
    -- Use NormalAndSubNormalNotEq: normal and subnormal can't have same F2R
    have hneq := NormalAndSubNormalNotEq_internal radix b p q hβ hradix
    simp only [wp, PostCond.noThrow, pure, NormalAndSubNormalNotEq_check, ULift.down_up] at hneq
    have hP_normal : Fnormal' radix b p := ⟨hbP, hvnumP⟩
    have hQ_subnormal : Fsubnormal' radix b q := ⟨hbQ, hexpQ, hvnumQ⟩
    exact hneq ⟨hP_normal, hQ_subnormal⟩ heqF2R
  -- Case 3: p subnormal, q normal - contradiction
  · exfalso
    -- By symmetry with case 2
    have hneq := NormalAndSubNormalNotEq_internal radix b q p hβ hradix
    simp only [wp, PostCond.noThrow, pure, NormalAndSubNormalNotEq_check, ULift.down_up] at hneq
    have hQ_normal : Fnormal' radix b q := ⟨hbQ, hvnumQ⟩
    have hP_subnormal : Fsubnormal' radix b p := ⟨hbP, hexpP, hvnumP⟩
    exact hneq ⟨hQ_normal, hP_subnormal⟩ heqF2R.symm
  -- Case 4: p subnormal, q subnormal
  · -- Both subnormal: p.Fexp = q.Fexp = -dExp
    have hexp_eq : p.Fexp = q.Fexp := by rw [hexpP, hexpQ]
    have hfnum_eq : p.Fnum = q.Fnum := by
      have hpow_pos : (0 : ℝ) < (beta : ℝ) ^ q.Fexp := zpow_pos hbeta_pos q.Fexp
      have hpow_ne : (beta : ℝ) ^ q.Fexp ≠ 0 := ne_of_gt hpow_pos
      rw [hexp_eq] at heqF2R
      have h : (p.Fnum : ℝ) = (q.Fnum : ℝ) := mul_right_cancel₀ hpow_ne heqF2R
      exact_mod_cast h
    -- Conclude p = q using structure equality
    cases p
    cases q
    simp only [FloatSpec.Core.Defs.FlocqFloat.mk.injEq]
    exact ⟨hfnum_eq, hexp_eq⟩

-- Coq: `Fweight`
noncomputable def Fweight {beta : Int} [ValidRadix beta]
    (radix : Int) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Int :=
  p.Fnum + p.Fexp * Zpower_nat radix precision

noncomputable def FweightLt_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FweightLt` — the integer rank of nonnegative canonical floats is
strictly monotone with the represented real value. -/
theorem FweightLt {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧
        b.vNum = Zpower_nat radix precision ∧
        Fcanonic' (beta:=beta) radix b p ∧
        Fcanonic' (beta:=beta) radix b q ∧
        0 ≤ _root_.F2R (beta:=beta) p ∧
        _root_.F2R (beta:=beta) p < _root_.F2R (beta:=beta) q⌝⦄
    (pure (FweightLt_check (beta:=beta) radix b precision p q) : Id Unit)
    ⦃⇓_ => ⌜Fweight (beta:=beta) radix precision p <
        Fweight (beta:=beta) radix precision q⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta_radix, hradix, hvNum, hp_can, hq_can, hp_nonneg, hp_lt_q⟩
  simp only [wp, PostCond.noThrow, pure, FweightLt_check, Id.run, ULift.up_down]
  have hbeta_gt : 1 < beta := by omega
  have hradix_beta : radix = beta := hbeta_radix.symm
  have hp_bound : Fbounded (beta:=beta) b p := by
    have hTrip := FcanonicBound (beta:=beta) radix b p
    simpa [wp, PostCond.noThrow, pure, FcanonicBound_check, Id.run,
      ULift.up_down, Fcanonic'] using hTrip hp_can
  have hq_nonneg : 0 ≤ _root_.F2R (beta:=beta) q :=
    le_of_lt (lt_of_le_of_lt hp_nonneg hp_lt_q)
  have hq_num_nonneg : 0 ≤ q.Fnum := by
    have hbeta_pos : (0 : ℝ) < (beta : ℝ) := by
      exact_mod_cast (by omega : (0 : Int) < beta)
    have hpow_pos : (0 : ℝ) < (beta : ℝ) ^ q.Fexp :=
      zpow_pos hbeta_pos q.Fexp
    have hq_nonneg' :
        0 ≤ (q.Fnum : ℝ) * (beta : ℝ) ^ q.Fexp := by
      simpa [_root_.F2R, FloatSpec.Core.Defs.F2R] using hq_nonneg
    have hq_num_real : 0 ≤ (q.Fnum : ℝ) :=
      nonneg_of_mul_nonneg_right (by simpa [mul_comm] using hq_nonneg') hpow_pos
    exact_mod_cast hq_num_real
  have hN_pos : 0 < Zpower_nat radix precision := by
    rw [Zpower_nat]
    exact pow_pos (by omega : 0 < radix) precision
  have hp_num_lt_N : p.Fnum < Zpower_nat radix precision := by
    have hp_abs : |p.Fnum| < b.vNum := hp_bound.1
    rw [hvNum] at hp_abs
    exact lt_of_le_of_lt (le_abs_self p.Fnum) hp_abs
  have hcmp : p.Fexp < q.Fexp ∨
      (p.Fexp = q.Fexp ∧ p.Fnum < q.Fnum) := by
    have hTrip := FcanonicLtPos (beta:=beta) radix b p q hbeta_gt hradix_beta
    simpa [wp, PostCond.noThrow, pure, FcanonicLtPos_check, Id.run,
      ULift.down_up, Fcanonic'] using
      hTrip ⟨hp_can, hq_can, hp_nonneg, hp_lt_q⟩
  rcases hcmp with hexp_lt | ⟨hexp_eq, hnum_lt⟩
  · have hgap : p.Fexp + 1 ≤ q.Fexp := by omega
    have hmul :
        (p.Fexp + 1) * Zpower_nat radix precision ≤
          q.Fexp * Zpower_nat radix precision :=
      mul_le_mul_of_nonneg_right hgap (le_of_lt hN_pos)
    unfold Fweight
    have hleft :
        p.Fnum + p.Fexp * Zpower_nat radix precision <
          (p.Fexp + 1) * Zpower_nat radix precision := by
      ring_nf
      omega
    have hright :
        (p.Fexp + 1) * Zpower_nat radix precision ≤
          q.Fnum + q.Fexp * Zpower_nat radix precision := by
      exact le_trans hmul (by omega)
    exact lt_of_lt_of_le hleft hright
  · unfold Fweight
    rw [hexp_eq]
    simpa [add_comm, add_left_comm, add_assoc] using
      add_lt_add_right hnum_lt (q.Fexp * Zpower_nat radix precision)

noncomputable def FweightEq_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FweightEq` — equal canonical real values have equal weight. -/
theorem FweightEq {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧
        Fcanonic' (beta:=beta) radix b p ∧
        Fcanonic' (beta:=beta) radix b q ∧
        _root_.F2R (beta:=beta) p = _root_.F2R (beta:=beta) q⌝⦄
    (pure (FweightEq_check (beta:=beta) radix b precision p q) : Id Unit)
    ⦃⇓_ => ⌜Fweight (beta:=beta) radix precision p =
        Fweight (beta:=beta) radix precision q⌝⦄ := by
  intro h
  rcases h with ⟨hbeta_radix, hradix, hp_can, hq_can, hpq⟩
  simp only [wp, PostCond.noThrow, pure, FweightEq_check, Id.run, ULift.up_down]
  have hbeta_gt : 1 < beta := by omega
  have huniq := FcanonicUnique (beta:=beta) radix b p q hbeta_gt hbeta_radix.symm
  have hp_eq_q : p = q := by
    simpa [wp, PostCond.noThrow, pure, FcanonicUnique_check, Id.run,
      ULift.down_up, Fcanonic'] using huniq ⟨hp_can, hq_can, hpq⟩
  simpa [hp_eq_q]

noncomputable def FweightZle_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FweightZle` — nonnegative canonical order implies weight order. -/
theorem FweightZle {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧
        b.vNum = Zpower_nat radix precision ∧
        Fcanonic' (beta:=beta) radix b p ∧
        Fcanonic' (beta:=beta) radix b q ∧
        0 ≤ _root_.F2R (beta:=beta) p ∧
        _root_.F2R (beta:=beta) p ≤ _root_.F2R (beta:=beta) q⌝⦄
    (pure (FweightZle_check (beta:=beta) radix b precision p q) : Id Unit)
    ⦃⇓_ => ⌜Fweight (beta:=beta) radix precision p ≤
        Fweight (beta:=beta) radix precision q⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta_radix, hradix, hvNum, hp_can, hq_can, hp_nonneg, hp_le_q⟩
  simp only [wp, PostCond.noThrow, pure, FweightZle_check, Id.run, ULift.up_down]
  rcases lt_or_eq_of_le hp_le_q with hp_lt_q | hp_eq_q
  · have hTrip := FweightLt (beta:=beta) radix b precision p q
    exact le_of_lt (by
      simpa [wp, PostCond.noThrow, pure, FweightLt_check, Id.run,
        ULift.up_down] using
        hTrip ⟨hbeta_radix, hradix, hvNum, hp_can, hq_can, hp_nonneg, hp_lt_q⟩)
  · have hTrip := FweightEq (beta:=beta) radix b precision p q
    exact le_of_eq (by
      simpa [wp, PostCond.noThrow, pure, FweightEq_check, Id.run,
        ULift.up_down] using
        hTrip ⟨hbeta_radix, hradix, hp_can, hq_can, hp_eq_q⟩)

/-- Coq: `FinductNegAux` — induction downward over nonnegative canonical
floats, using the integer gap `Fweight p - Fweight q` as the well-founded
measure. -/
theorem FinductNegAux {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (hbeta_radix : beta = radix)
    (hradix : 1 < radix)
    (hprecision : 1 < precision)
    (hvNum : b.vNum = Zpower_nat radix precision)
    (P : FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p : FloatSpec.Core.Defs.FlocqFloat beta)
    (hp_nonneg : 0 ≤ _root_.F2R (beta:=beta) p)
    (hp_can : Fcanonic (beta:=beta) radix b p)
    (hp : P p)
    (hstep : ∀ q : FloatSpec.Core.Defs.FlocqFloat beta,
      Fcanonic (beta:=beta) radix b q →
      0 < _root_.F2R (beta:=beta) q →
      _root_.F2R (beta:=beta) q ≤ _root_.F2R (beta:=beta) p →
      P q →
      P (FPred (beta:=beta) b radix precision q)) :
    ∀ x : Int, 0 ≤ x →
      ∀ q : FloatSpec.Core.Defs.FlocqFloat beta,
        x =
          Fweight (beta:=beta) radix precision p -
            Fweight (beta:=beta) radix precision q →
        Fcanonic (beta:=beta) radix b q →
        0 ≤ _root_.F2R (beta:=beta) q →
        _root_.F2R (beta:=beta) q ≤ _root_.F2R (beta:=beta) p →
        P q := by
  have hprecision_ne : precision ≠ 0 := by omega
  have hp_can' : Fcanonic' (beta:=beta) radix b p := by
    simpa [Fcanonic'] using hp_can
  have hbeta_gt : 1 < beta := by omega
  have hradix_eq_beta : radix = beta := hbeta_radix.symm
  have hNat :
      ∀ n : Nat,
        ∀ q : FloatSpec.Core.Defs.FlocqFloat beta,
          (n : Int) =
            Fweight (beta:=beta) radix precision p -
              Fweight (beta:=beta) radix precision q →
          Fcanonic (beta:=beta) radix b q →
          0 ≤ _root_.F2R (beta:=beta) q →
          _root_.F2R (beta:=beta) q ≤ _root_.F2R (beta:=beta) p →
          P q := by
    intro n
    induction n using Nat.strong_induction_on with
    | h n ih =>
      intro q hdist hq_can hq_nonneg hq_le_p
      rcases lt_or_eq_of_le hq_le_p with hq_lt_p | hq_eq_p
      · let s : FloatSpec.Core.Defs.FlocqFloat beta :=
          FSucc (beta:=beta) b radix precision q
        have hq_lt_s : _root_.F2R (beta:=beta) q <
            _root_.F2R (beta:=beta) s := by
          have hTrip := FSuccLt (beta:=beta) b radix precision q
          simpa only [wp, PostCond.noThrow, pure, FSuccLt_check, Id.run,
            PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
            ULift.up, ULift.down, ULift.up_down, s, Int.cast_ofNat] using
            hTrip ⟨hbeta_radix, hradix, hprecision_ne, hvNum⟩
        have hs_can : Fcanonic (beta:=beta) radix b s := by
          have hTrip := FSuccCanonic (beta:=beta) b radix precision q
          simpa only [wp, PostCond.noThrow, pure, FSuccCanonic_check, Id.run,
            PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
            ULift.up, ULift.down, ULift.up_down, s, Int.cast_ofNat] using
            hTrip ⟨hq_can, hradix, hprecision_ne, hvNum⟩
        have hs_nonneg : 0 ≤ _root_.F2R (beta:=beta) s :=
          le_trans hq_nonneg (le_of_lt hq_lt_s)
        have hs_pos : 0 < _root_.F2R (beta:=beta) s :=
          lt_of_le_of_lt hq_nonneg hq_lt_s
        have hs_le_p : _root_.F2R (beta:=beta) s ≤
            _root_.F2R (beta:=beta) p := by
          have hTrip := FSuccPropPos (beta:=beta) b radix precision q p
          simpa only [wp, PostCond.noThrow, pure, FSuccPropPos_check, Id.run,
            PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
            ULift.up, ULift.down, ULift.up_down, s, Int.cast_ofNat] using
            hTrip ⟨hbeta_radix, hradix, hprecision_ne, hvNum,
              hq_nonneg, hq_can, hp_can, hq_lt_p⟩
        have hs_can' : Fcanonic' (beta:=beta) radix b s := by
          simpa [Fcanonic'] using hs_can
        have hq_can' : Fcanonic' (beta:=beta) radix b q := by
          simpa [Fcanonic'] using hq_can
        have hs_weight_le_p :
            Fweight (beta:=beta) radix precision s ≤
              Fweight (beta:=beta) radix precision p := by
          have hTrip := FweightZle (beta:=beta) radix b precision s p
          simpa [wp, PostCond.noThrow, pure, FweightZle_check, Id.run,
            ULift.up_down, Fcanonic'] using
            hTrip ⟨hbeta_radix, hradix, hvNum, hs_can', hp_can',
              hs_nonneg, hs_le_p⟩
        have hq_weight_lt_s :
            Fweight (beta:=beta) radix precision q <
              Fweight (beta:=beta) radix precision s := by
          have hTrip := FweightLt (beta:=beta) radix b precision q s
          simpa [wp, PostCond.noThrow, pure, FweightLt_check, Id.run,
            ULift.up_down, Fcanonic'] using
            hTrip ⟨hbeta_radix, hradix, hvNum, hq_can', hs_can',
              hq_nonneg, hq_lt_s⟩
        have hs_dist_nonneg :
            0 ≤
              Fweight (beta:=beta) radix precision p -
                Fweight (beta:=beta) radix precision s := by
          omega
        have hs_dist_lt :
            Fweight (beta:=beta) radix precision p -
                Fweight (beta:=beta) radix precision s <
              Fweight (beta:=beta) radix precision p -
                Fweight (beta:=beta) radix precision q := by
          omega
        have hs_measure_lt :
            (Fweight (beta:=beta) radix precision p -
                Fweight (beta:=beta) radix precision s).toNat < n := by
          have hcast :
              ((Fweight (beta:=beta) radix precision p -
                  Fweight (beta:=beta) radix precision s).toNat : Int) <
                (n : Int) := by
            rw [Int.toNat_of_nonneg hs_dist_nonneg, hdist]
            exact hs_dist_lt
          exact_mod_cast hcast
        have hs_dist_eq :
            ((Fweight (beta:=beta) radix precision p -
                Fweight (beta:=beta) radix precision s).toNat : Int) =
              Fweight (beta:=beta) radix precision p -
                Fweight (beta:=beta) radix precision s :=
          Int.toNat_of_nonneg hs_dist_nonneg
        have hsP : P s :=
          ih
            (Fweight (beta:=beta) radix precision p -
              Fweight (beta:=beta) radix precision s).toNat
            hs_measure_lt
            s
            hs_dist_eq
            hs_can
            hs_nonneg
            hs_le_p
        have hpred_s_eq_q :
            FPred (beta:=beta) b radix precision s = q := by
          have hTrip := FPredSuc (beta:=beta) b radix precision q
          simpa only [wp, PostCond.noThrow, pure, FPredSuc_check, Id.run,
            PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
            ULift.up, ULift.down, ULift.up_down, s, Int.cast_ofNat] using
            hTrip ⟨hq_can, hradix, hprecision_ne, hvNum⟩
        rw [← hpred_s_eq_q]
        exact hstep s hs_can hs_pos hs_le_p hsP
      · have hp_eq_q : p = q := by
          have hTrip := FcanonicUnique (beta:=beta) radix b p q
            hbeta_gt hradix_eq_beta
          simpa [wp, PostCond.noThrow, pure, FcanonicUnique_check, Id.run,
            ULift.down_up, Fcanonic'] using
            hTrip ⟨hp_can, hq_can, hq_eq_p.symm⟩
        simpa [hp_eq_q] using hp
  intro x hx q hdist hq_can hq_nonneg hq_le_p
  have hx_cast : (x.toNat : Int) = x := Int.toNat_of_nonneg hx
  exact hNat x.toNat q (by simpa [hx_cast] using hdist)
    hq_can hq_nonneg hq_le_p

/-- Coq: `FinductNeg` — downward induction over canonical floats between zero
and a nonnegative canonical upper endpoint. -/
theorem FinductNeg {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (hbeta_radix : beta = radix)
    (hradix : 1 < radix)
    (hprecision : 1 < precision)
    (hvNum : b.vNum = Zpower_nat radix precision)
    (P : FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p : FloatSpec.Core.Defs.FlocqFloat beta)
    (hp_nonneg : 0 ≤ _root_.F2R (beta:=beta) p)
    (hp_can : Fcanonic (beta:=beta) radix b p)
    (hp : P p)
    (hstep : ∀ q : FloatSpec.Core.Defs.FlocqFloat beta,
      Fcanonic (beta:=beta) radix b q →
      0 < _root_.F2R (beta:=beta) q →
      _root_.F2R (beta:=beta) q ≤ _root_.F2R (beta:=beta) p →
      P q →
      P (FPred (beta:=beta) b radix precision q)) :
    ∀ q : FloatSpec.Core.Defs.FlocqFloat beta,
      Fcanonic (beta:=beta) radix b q →
      0 ≤ _root_.F2R (beta:=beta) q →
      _root_.F2R (beta:=beta) q ≤ _root_.F2R (beta:=beta) p →
      P q := by
  intro q hq_can hq_nonneg hq_le_p
  have hp_can' : Fcanonic' (beta:=beta) radix b p := by
    simpa [Fcanonic'] using hp_can
  have hq_can' : Fcanonic' (beta:=beta) radix b q := by
    simpa [Fcanonic'] using hq_can
  have hweight_q_le_p :
      Fweight (beta:=beta) radix precision q ≤
        Fweight (beta:=beta) radix precision p := by
    have hTrip := FweightZle (beta:=beta) radix b precision q p
    simpa [wp, PostCond.noThrow, pure, FweightZle_check, Id.run,
      ULift.up_down, Fcanonic'] using
      hTrip ⟨hbeta_radix, hradix, hvNum, hq_can', hp_can',
        hq_nonneg, hq_le_p⟩
  have hdist_nonneg :
      0 ≤
        Fweight (beta:=beta) radix precision p -
          Fweight (beta:=beta) radix precision q := by
    omega
  exact
    FinductNegAux (beta:=beta) b radix precision
      hbeta_radix hradix hprecision hvNum P p
      hp_nonneg hp_can hp hstep
      (Fweight (beta:=beta) radix precision p -
        Fweight (beta:=beta) radix precision q)
      hdist_nonneg q rfl hq_can hq_nonneg hq_le_p

/-- Coq: `radixRangeBoundExp` — if two nonnegative canonical floats are within
one radix factor, their exponents are equal or adjacent. -/
theorem radixRangeBoundExp {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) :
    beta = radix →
    1 < radix →
    precision ≠ 0 →
    b.vNum = Zpower_nat radix precision →
    Fcanonic (beta:=beta) radix b p →
    Fcanonic (beta:=beta) radix b q →
    0 ≤ _root_.F2R (beta:=beta) p →
    _root_.F2R (beta:=beta) p < _root_.F2R (beta:=beta) q →
    _root_.F2R (beta:=beta) q <
      (radix : ℝ) * _root_.F2R (beta:=beta) p →
    p.Fexp = q.Fexp ∨ Int.succ p.Fexp = q.Fexp := by
  intro hbeta_radix hradix hprecision hvNum hp_can hq_can hp_nonneg hp_lt_q
    hq_lt_radix_mul_p
  subst beta
  have hq_nonneg : 0 ≤ _root_.F2R (beta:=radix) q :=
    le_trans hp_nonneg (le_of_lt hp_lt_q)
  have hcmp :
      p.Fexp < q.Fexp ∨ (p.Fexp = q.Fexp ∧ p.Fnum < q.Fnum) :=
    (FcanonicLtPos (beta:=radix) radix b p q hradix rfl)
      ⟨hp_can, hq_can, hp_nonneg, hp_lt_q⟩
  rcases hcmp with hp_exp_lt_q | ⟨hp_exp_eq_q, _hp_num_lt_q⟩
  · right
    by_cases hadj : Int.succ p.Fexp = q.Fexp
    · exact hadj
    · exfalso
      have hgap : Int.succ p.Fexp < q.Fexp := by
        unfold Int.succ at hadj ⊢
        omega
      rcases hp_can with hp_normal | hp_subnormal
      · let r : FloatSpec.Core.Defs.FlocqFloat radix := ⟨p.Fnum, Int.succ p.Fexp⟩
        have hr_can : Fcanonic (beta:=radix) radix b r := by
          left
          rcases hp_normal with ⟨hp_bounded, hp_normal_mant⟩
          refine ⟨⟨?_, ?_⟩, ?_⟩
          · simpa [r] using hp_bounded.1
          ·
            have hp_exp_le_succ : p.Fexp ≤ p.Fexp + 1 := by omega
            exact le_trans hp_bounded.2 hp_exp_le_succ
          · simpa [r] using hp_normal_mant
        have hq_lt_r :
            _root_.F2R (beta:=radix) q < _root_.F2R (beta:=radix) r := by
          have hr_eq :
              _root_.F2R (beta:=radix) r =
                (radix : ℝ) * _root_.F2R (beta:=radix) p := by
            have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
              exact_mod_cast (by omega : (0 : Int) < radix)
            have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
            simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, r,
              FloatSpec.Core.Defs.FlocqFloat.Fnum,
              FloatSpec.Core.Defs.FlocqFloat.Fexp]
            rw [show Int.succ p.Fexp = p.Fexp + 1 by rfl]
            rw [zpow_add_one₀ hradix_ne]
            ring
          exact hr_eq.symm ▸ hq_lt_radix_mul_p
        have hqr_cmp :
            q.Fexp < r.Fexp ∨ (q.Fexp = r.Fexp ∧ q.Fnum < r.Fnum) :=
          (FcanonicLtPos (beta:=radix) radix b q r hradix rfl)
            ⟨hq_can, hr_can, hq_nonneg, hq_lt_r⟩
        rcases hqr_cmp with hq_exp_lt_r | ⟨hq_exp_eq_r, _⟩
        · simp only [r, FloatSpec.Core.Defs.FlocqFloat.Fexp] at hq_exp_lt_r
          omega
        · simp only [r, FloatSpec.Core.Defs.FlocqFloat.Fexp] at hq_exp_eq_r
          omega
      · let r : FloatSpec.Core.Defs.FlocqFloat radix :=
          ⟨nNormMin radix precision, Int.succ p.Fexp⟩
        have hp_exp_eq_min : p.Fexp = -b.dExp := hp_subnormal.2.1
        have hradix_pos_int : (0 : Int) < radix := by omega
        have hradix_pos : (0 : ℝ) < (radix : ℝ) := by exact_mod_cast hradix_pos_int
        have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
        have hnorm_pos : 0 < nNormMin radix precision := by
          unfold nNormMin
          exact pow_pos hradix_pos_int _
        have hnorm_lt_vNum : nNormMin radix precision < b.vNum := by
          rw [hvNum, Zpower_nat]
          have hpow_succ :
              radix ^ precision = radix ^ (precision - 1) * radix := by
            have hprec : Nat.succ (precision - 1) = precision := by omega
            calc
              radix ^ precision = radix ^ Nat.succ (precision - 1) := by rw [hprec]
              _ = radix ^ (precision - 1) * radix := by rw [pow_succ]
          rw [hpow_succ]
          have hpow_pos : 0 < radix ^ (precision - 1) :=
            pow_pos hradix_pos_int _
          have hradix_sub_pos : 0 < radix - 1 := by omega
          have hdiff_pos : 0 < radix ^ (precision - 1) * (radix - 1) :=
            mul_pos hpow_pos hradix_sub_pos
          have hmul_lt : radix ^ (precision - 1) * 1 <
              radix ^ (precision - 1) * radix :=
            by nlinarith
          simpa [nNormMin] using hmul_lt
        have hnorm_mul_eq_vNum :
            radix * nNormMin radix precision = b.vNum := by
          rw [hvNum, Zpower_nat, nNormMin]
          have hpow_succ :
              radix ^ precision = radix * radix ^ (precision - 1) := by
            have hprec : Nat.succ (precision - 1) = precision := by omega
            calc
              radix ^ precision = radix ^ Nat.succ (precision - 1) := by rw [hprec]
              _ = radix ^ (precision - 1) * radix := by rw [pow_succ]
              _ = radix * radix ^ (precision - 1) := by ring
          rw [hpow_succ]
        have hr_can : Fcanonic (beta:=radix) radix b r := by
          left
          refine ⟨⟨?_, ?_⟩, ?_⟩
          · simpa [r, abs_of_pos hnorm_pos] using hnorm_lt_vNum
          · simp [r, Int.succ, hp_exp_eq_min]
          · have hmul_pos : 0 < radix * nNormMin radix precision :=
              mul_pos hradix_pos_int hnorm_pos
            rw [← hnorm_mul_eq_vNum]
            simpa [r, abs_of_pos hmul_pos]
        have hp_lt_first :
            _root_.F2R (beta:=radix) p <
              _root_.F2R (beta:=radix)
                (firstNormalPos (beta:=radix) radix b precision) :=
          by
            have hp_fnum_nonneg : 0 ≤ p.Fnum :=
              FloatSpec.Core.Float_prop.ge_0_F2R (beta := radix) p hradix hp_nonneg
            have hsub_abs : |radix * p.Fnum| < b.vNum := hp_subnormal.2.2
            have hradix_mul_nonneg : 0 ≤ radix * p.Fnum :=
              mul_nonneg (le_of_lt hradix_pos_int) hp_fnum_nonneg
            have hmul_lt : radix * p.Fnum < radix * nNormMin radix precision := by
              rw [← hnorm_mul_eq_vNum] at hsub_abs
              simpa [abs_of_nonneg hradix_mul_nonneg] using hsub_abs
            have hp_fnum_lt : p.Fnum < nNormMin radix precision :=
              Int.lt_of_mul_lt_mul_left hmul_lt (le_of_lt hradix_pos_int)
            have hp_fnum_lt_real :
                (p.Fnum : ℝ) < (nNormMin radix precision : ℝ) := by
              exact_mod_cast hp_fnum_lt
            simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, firstNormalPos,
              FloatSpec.Core.Defs.FlocqFloat.Fnum,
              FloatSpec.Core.Defs.FlocqFloat.Fexp]
            rw [hp_exp_eq_min]
            exact mul_lt_mul_of_pos_right hp_fnum_lt_real
              (zpow_pos hradix_pos (-b.dExp))
        have hradix_mul_p_lt_r :
            (radix : ℝ) * _root_.F2R (beta:=radix) p <
              _root_.F2R (beta:=radix) r := by
          have hmul_lt :
              (radix : ℝ) * _root_.F2R (beta:=radix) p <
                (radix : ℝ) *
                  _root_.F2R (beta:=radix)
                    (firstNormalPos (beta:=radix) radix b precision) :=
            mul_lt_mul_of_pos_left hp_lt_first hradix_pos
          have hfirst_mul_eq :
              (radix : ℝ) *
                  _root_.F2R (beta:=radix)
                    (firstNormalPos (beta:=radix) radix b precision) =
                _root_.F2R (beta:=radix) r := by
            simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, firstNormalPos, r,
              FloatSpec.Core.Defs.FlocqFloat.Fnum,
              FloatSpec.Core.Defs.FlocqFloat.Fexp]
            rw [hp_exp_eq_min]
            rw [show Int.succ (-b.dExp) = -b.dExp + 1 by rfl]
            rw [zpow_add_one₀ hradix_ne]
            ring
          exact hfirst_mul_eq ▸ hmul_lt
        have hq_lt_r :
            _root_.F2R (beta:=radix) q < _root_.F2R (beta:=radix) r :=
          lt_trans hq_lt_radix_mul_p hradix_mul_p_lt_r
        have hqr_cmp :
            q.Fexp < r.Fexp ∨ (q.Fexp = r.Fexp ∧ q.Fnum < r.Fnum) :=
          (FcanonicLtPos (beta:=radix) radix b q r hradix rfl)
            ⟨hq_can, hr_can, hq_nonneg, hq_lt_r⟩
        rcases hqr_cmp with hq_exp_lt_r | ⟨hq_exp_eq_r, _⟩
        · simp only [r, FloatSpec.Core.Defs.FlocqFloat.Fexp] at hq_exp_lt_r
          omega
        · simp only [r, FloatSpec.Core.Defs.FlocqFloat.Fexp] at hq_exp_eq_r
          omega
  · left
    exact hp_exp_eq_q

noncomputable def FcanonicLeastExp_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FcanonicLeastExp` — a canonical float with the same value as a
bounded float has exponent no larger than the bounded one.

Note: Uses `Fcanonic'`, the Coq-matching canonical predicate.
Requires `1 < beta` and `radix = beta` for the mantissa/exponent relationship to work.
Also requires `0 < b.vNum` for the Normal case to be non-vacuous. -/
theorem FcanonicLeastExp_from_explicit_radix_payload {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta)
    (hβ : 1 < beta)
    (hradix : radix = beta)
    (hvNum_pos : 0 < b.vNum) :
    ⦃⌜_root_.F2R (beta:=beta) x = _root_.F2R (beta:=beta) y ∧
        Fbounded' b x ∧
        Fcanonic' (beta:=beta) radix b y⌝⦄
    (pure (FcanonicLeastExp_check (beta:=beta) radix b x y) : Id Unit)
    ⦃⇓_ => ⌜y.Fexp ≤ x.Fexp⌝⦄ := by
  intro ⟨hF2Req, hBoundedX, hCanY⟩
  simp only [wp, PostCond.noThrow, pure, FcanonicLeastExp_check, ULift.down_up]
  -- Derive useful facts about beta
  have hbeta_pos_int : (0 : ℤ) < beta := lt_trans (by norm_num : (0 : ℤ) < 1) hβ
  have hbeta_pos : (0 : ℝ) < (beta : ℝ) := Int.cast_pos.mpr hbeta_pos_int
  have hbeta_ne_zero : (beta : ℝ) ≠ 0 := ne_of_gt hbeta_pos
  have hbeta_ge_one : (1 : ℝ) ≤ (beta : ℝ) := le_of_lt (by exact_mod_cast hβ : (1 : ℝ) < beta)
  -- Key: if F2R x = F2R y, then x.Fnum * beta^x.Fexp = y.Fnum * beta^y.Fexp
  simp only [_root_.F2R, FloatSpec.Core.Defs.F2R] at hF2Req
  -- If y.Fnum = 0, then F2R y = 0, so F2R x = 0, so x.Fnum = 0
  -- In this case, any exponent comparison is valid, but let's handle it
  by_cases hy_zero : y.Fnum = 0
  · -- y.Fnum = 0 means F2R y = 0
    simp only [hy_zero, Int.cast_zero, zero_mul] at hF2Req
    -- So F2R x = 0, meaning x.Fnum * beta^x.Fexp = 0
    -- Since beta^x.Fexp > 0, we have x.Fnum = 0
    have hpow_pos : (0 : ℝ) < (beta : ℝ) ^ x.Fexp := zpow_pos hbeta_pos x.Fexp
    have hx_fnum_zero : (x.Fnum : ℝ) = 0 := by
      -- hF2Req : x.Fnum * beta^x.Fexp = 0
      have hmul_zero : (x.Fnum : ℝ) * (beta : ℝ) ^ x.Fexp = 0 := hF2Req
      rcases mul_eq_zero.mp hmul_zero with h | h
      · exact h
      · exfalso; exact ne_of_gt hpow_pos h
    -- For canonical y with Fnum = 0:
    -- If subnormal, y.Fexp = -dExp (minimum)
    -- x is bounded, so x.Fexp ≥ -dExp
    rcases hCanY with ⟨hbY, hvnumY⟩ | ⟨hbY, hexpY, _⟩
    · -- Normal with Fnum = 0: impossible since vNum > 0 but |radix * 0| = 0
      rw [hy_zero, mul_zero, abs_zero] at hvnumY
      -- hvnumY : vNum ≤ 0, but hvNum_pos : 0 < vNum
      have : b.vNum ≤ 0 := hvnumY
      linarith
    · -- Subnormal: y.Fexp = -dExp
      -- x is bounded, so x.Fexp ≥ -dExp
      have hx_exp_ge : -b.dExp ≤ x.Fexp := hBoundedX.2
      -- y.Fexp = -dExp and x.Fexp ≥ -dExp implies y.Fexp ≤ x.Fexp
      rw [hexpY]
      exact hx_exp_ge
  · -- y.Fnum ≠ 0
    -- Similarly check if x.Fnum = 0
    by_cases hx_zero : x.Fnum = 0
    · -- x.Fnum = 0 means F2R x = 0
      simp only [hx_zero, Int.cast_zero, zero_mul] at hF2Req
      -- So F2R y = 0, meaning y.Fnum * beta^y.Fexp = 0
      have hpow_pos : (0 : ℝ) < (beta : ℝ) ^ y.Fexp := zpow_pos hbeta_pos y.Fexp
      have hy_fnum_zero : (y.Fnum : ℝ) = 0 := by
        -- hF2Req : 0 = y.Fnum * beta^y.Fexp
        have hmul_zero : (y.Fnum : ℝ) * (beta : ℝ) ^ y.Fexp = 0 := hF2Req.symm
        rcases mul_eq_zero.mp hmul_zero with h | h
        · exact h
        · exfalso; exact ne_of_gt hpow_pos h
      -- But we assumed y.Fnum ≠ 0
      simp only [Int.cast_eq_zero] at hy_fnum_zero
      contradiction
    · -- Both x.Fnum ≠ 0 and y.Fnum ≠ 0
      -- From hF2Req: x.Fnum * beta^x.Fexp = y.Fnum * beta^y.Fexp
      -- Rearranging: x.Fnum / y.Fnum = beta^(y.Fexp - x.Fexp)
      -- For canonical y, |y.Fnum| is maximized (bounded below by vNum/radix for normal)
      -- The canonical representation has the smallest possible exponent
      -- We prove by showing x.Fexp < y.Fexp leads to contradiction
      -- First, show y.Fexp ≤ x.Fexp by analyzing the cases
      -- Use decidable on integers
      by_cases hexp_le : y.Fexp ≤ x.Fexp
      · exact hexp_le
      · -- So x.Fexp < y.Fexp
        push Not at hexp_le
        have hexp_lt : x.Fexp < y.Fexp := hexp_le
        -- From F2R equality: x.Fnum * beta^x.Fexp = y.Fnum * beta^y.Fexp
        -- So: x.Fnum = y.Fnum * beta^(y.Fexp - x.Fexp)
        have hexp_diff_pos : 0 < y.Fexp - x.Fexp := by omega
        have hexp_diff_nonneg : 0 ≤ y.Fexp - x.Fexp := le_of_lt hexp_diff_pos
        -- Convert the positive integer difference to a natural number
        set n := (y.Fexp - x.Fexp).toNat with hn_def
        have hn_pos : n > 0 := by
          simp only [gt_iff_lt, hn_def]
          omega
        have hn_eq : y.Fexp - x.Fexp = (n : ℤ) := by
          simp only [hn_def]
          exact (Int.toNat_of_nonneg hexp_diff_nonneg).symm
        -- x.Fnum * beta^x.Fexp = y.Fnum * beta^y.Fexp
        -- x.Fnum = y.Fnum * beta^(y.Fexp - x.Fexp)
        have hfnum_rel : (x.Fnum : ℝ) = (y.Fnum : ℝ) * (beta : ℝ) ^ (y.Fexp - x.Fexp) := by
          have hpow_x_pos : (0 : ℝ) < (beta : ℝ) ^ x.Fexp := zpow_pos hbeta_pos x.Fexp
          have hpow_x_ne : (beta : ℝ) ^ x.Fexp ≠ 0 := ne_of_gt hpow_x_pos
          have h1 : (x.Fnum : ℝ) * (beta : ℝ) ^ x.Fexp = (y.Fnum : ℝ) * (beta : ℝ) ^ y.Fexp := hF2Req
          have h2 : (y.Fnum : ℝ) * (beta : ℝ) ^ y.Fexp =
                    (y.Fnum : ℝ) * (beta : ℝ) ^ (y.Fexp - x.Fexp) * (beta : ℝ) ^ x.Fexp := by
            rw [mul_assoc, ← zpow_add₀ hbeta_ne_zero]
            ring_nf
          calc (x.Fnum : ℝ) = (x.Fnum : ℝ) * (beta : ℝ) ^ x.Fexp / (beta : ℝ) ^ x.Fexp := by
                field_simp
              _ = (y.Fnum : ℝ) * (beta : ℝ) ^ y.Fexp / (beta : ℝ) ^ x.Fexp := by rw [h1]
              _ = (y.Fnum : ℝ) * (beta : ℝ) ^ (y.Fexp - x.Fexp) * (beta : ℝ) ^ x.Fexp / (beta : ℝ) ^ x.Fexp := by
                  rw [h2]
              _ = (y.Fnum : ℝ) * (beta : ℝ) ^ (y.Fexp - x.Fexp) := by field_simp
        -- So |x.Fnum| = |y.Fnum| * beta^(y.Fexp - x.Fexp)
        have habs_rel : |(x.Fnum : ℝ)| = |(y.Fnum : ℝ)| * (beta : ℝ) ^ (y.Fexp - x.Fexp) := by
          rw [hfnum_rel, abs_mul]
          congr 1
          exact abs_of_pos (zpow_pos hbeta_pos _)
        -- Since n > 0, beta^n ≥ beta
        have hpow_ge_beta : (beta : ℝ) ^ (y.Fexp - x.Fexp) ≥ beta := by
          rw [hn_eq, zpow_natCast]
          have h1 : (beta : ℝ) ^ n ≥ (beta : ℝ) ^ 1 := by
            gcongr
            omega
          simp only [pow_one] at h1
          exact h1
        -- So |x.Fnum| ≥ |y.Fnum| * beta
        have habs_ge : |(x.Fnum : ℝ)| ≥ |(y.Fnum : ℝ)| * beta := by
          calc |(x.Fnum : ℝ)| = |(y.Fnum : ℝ)| * (beta : ℝ) ^ (y.Fexp - x.Fexp) := habs_rel
            _ ≥ |(y.Fnum : ℝ)| * beta := by
                apply mul_le_mul_of_nonneg_left hpow_ge_beta (abs_nonneg _)
        -- For x bounded: |x.Fnum| < vNum
        have hx_fnum_bdd : |x.Fnum| < b.vNum := hBoundedX.1
        -- Convert to real
        have hx_fnum_bdd_real : |(x.Fnum : ℝ)| < (b.vNum : ℝ) := by
          rw [← Int.cast_abs]
          exact Int.cast_lt.mpr hx_fnum_bdd
        -- For canonical y (normal case): vNum ≤ |radix * y.Fnum|
        rcases hCanY with ⟨_, hvnumY⟩ | ⟨_, hexpY, hvnumY⟩
        · -- Normal: vNum ≤ |radix * y.Fnum| = |radix| * |y.Fnum|
          rw [hradix] at hvnumY
          have hbeta_abs : |beta| = beta := abs_of_pos hbeta_pos_int
          rw [abs_mul, hbeta_abs] at hvnumY
          -- So vNum ≤ beta * |y.Fnum|
          -- Therefore |y.Fnum| ≥ vNum / beta
          -- And |x.Fnum| ≥ |y.Fnum| * beta ≥ vNum
          have hy_fnum_ne_zero_real : (y.Fnum : ℝ) ≠ 0 := by
            simp only [ne_eq, Int.cast_eq_zero]
            exact hy_zero
          have hy_abs_pos : 0 < |(y.Fnum : ℝ)| := abs_pos.mpr hy_fnum_ne_zero_real
          have hvNum_le_real : (b.vNum : ℝ) ≤ (beta : ℝ) * |(y.Fnum : ℝ)| := by
            calc (b.vNum : ℝ) ≤ (beta * |y.Fnum| : ℤ) := Int.cast_le.mpr hvnumY
              _ = (beta : ℝ) * (|y.Fnum| : ℝ) := by simp [Int.cast_mul, Int.cast_abs]
              _ = (beta : ℝ) * |(y.Fnum : ℝ)| := by rw [← Int.cast_abs]
          -- So |x.Fnum| ≥ |y.Fnum| * beta ≥ vNum
          have hcontra : |(x.Fnum : ℝ)| ≥ (b.vNum : ℝ) := by
            calc |(x.Fnum : ℝ)| ≥ |(y.Fnum : ℝ)| * beta := habs_ge
              _ = beta * |(y.Fnum : ℝ)| := by ring
              _ ≥ (b.vNum : ℝ) := hvNum_le_real
          -- But |x.Fnum| < vNum, contradiction
          linarith
        · -- Subnormal: y.Fexp = -dExp (minimum exponent)
          -- x is bounded, so x.Fexp ≥ -dExp
          have hx_exp_ge : -b.dExp ≤ x.Fexp := hBoundedX.2
          -- But we have x.Fexp < y.Fexp = -dExp
          rw [hexpY] at hexp_lt
          omega

/-- Precision-independent strengthening of Coq `FcanonicLeastExp`. -/
theorem FcanonicLeastExp_from_intrinsic_bound_payload
    {beta : Int} [ValidRadix beta]
    (b : Fbound_skel)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜_root_.F2R (beta:=beta) x = _root_.F2R (beta:=beta) y ∧
        Fbounded b x ∧ Fcanonic' (beta:=beta) beta b y⌝⦄
    (pure (FcanonicLeastExp_check (beta:=beta) beta b x y) : Id Unit)
    ⦃⇓_ => ⌜y.Fexp ≤ x.Fexp⌝⦄ := by
  exact FcanonicLeastExp_from_explicit_radix_payload
    (beta:=beta) beta b x y (ValidRadix.valid (beta:=beta)) rfl b.vNum_pos

/-- Coq `FcanonicLeastExp` with its exported precision section contract. -/
theorem FcanonicLeastExp {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜precision ≠ 0 ∧ b.vNum = Zpower_nat beta precision ∧
        _root_.F2R (beta:=beta) x = _root_.F2R (beta:=beta) y ∧
        Fbounded b x ∧ Fcanonic' (beta:=beta) beta b y⌝⦄
    (pure (FcanonicLeastExp_check (beta:=beta) beta b x y) : Id Unit)
    ⦃⇓_ => ⌜y.Fexp ≤ x.Fexp⌝⦄ := by
  intro ⟨_precisionNotZero, _pGivesBound, hxy, hxBound, hyCanonic⟩
  exact FcanonicLeastExp_from_intrinsic_bound_payload
    (beta:=beta) b x y ⟨hxy, hxBound, hyCanonic⟩

-- Auxiliary boundedness of `RND_Min_Pos` on nonnegative reals (Coq: RND_Min_Pos_bounded_aux)
noncomputable def RND_Min_Pos_bounded_aux_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p e : Int) (r : ℝ) : Unit :=
  ()

/-- Coq: `RND_Min_Pos_bounded_aux`.

The Coq section assumptions `radixMoreThanOne`, `pGreaterThanOne`, and
`pGivesBound` are explicit in the precondition because this Lean port keeps
`radix`, precision, and `Fbound_skel` as ordinary parameters. -/
theorem RND_Min_Pos_bounded_aux {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p e : Int) (r : ℝ) :
    ⦃⌜0 ≤ r ∧ 1 < radix ∧ 1 < p ∧
        b.vNum = Zpower_nat radix p.toNat ∧
        -b.dExp ≤ e ∧ r < (radix : ℝ) ^ (e + p)⌝⦄
    (pure (RND_Min_Pos_bounded_aux_check (beta:=beta) b radix p e r) : Id Unit)
    ⦃⇓_ => ⌜Fbounded (beta:=beta) b
        (FloatSpec.Core.Defs.FlocqFloat.mk
          (IRNDD (r * (radix : ℝ) ^ (-e))) e)⌝⦄ := by
  intro h
  rcases h with ⟨hr_nonneg, hradix_gt, hp_gt, hvNum, he_lb, hr_lt⟩
  simp only [wp, PostCond.noThrow, pure, RND_Min_Pos_bounded_aux_check,
    Id.run, ULift.up_down]
  let scaled : ℝ := r * (radix : ℝ) ^ (-e)
  have hradix_pos_int : (0 : Int) < radix := lt_trans (by decide) hradix_gt
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by exact_mod_cast hradix_pos_int
  have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
  have hscale_nonneg : 0 ≤ scaled := by
    dsimp [scaled]
    exact mul_nonneg hr_nonneg (le_of_lt (zpow_pos hradix_pos (-e)))
  have hfloor_nonneg : 0 ≤ IRNDD scaled := by
    simp [IRNDD]
    exact Int.floor_nonneg.mpr hscale_nonneg
  constructor
  · have habs : |IRNDD scaled| = IRNDD scaled := abs_of_nonneg hfloor_nonneg
    rw [habs, hvNum]
    have hpow_e_pos : 0 < (radix : ℝ) ^ e := zpow_pos hradix_pos e
    have hfloor_le : ((IRNDD scaled : Int) : ℝ) ≤ scaled := by
      simpa [IRNDD] using Int.floor_le scaled
    have hscaled_mul : scaled * (radix : ℝ) ^ e = r := by
      dsimp [scaled]
      calc
        r * (radix : ℝ) ^ (-e) * (radix : ℝ) ^ e =
            r * ((radix : ℝ) ^ (-e) * (radix : ℝ) ^ e) := by ring
        _ = r * (radix : ℝ) ^ ((-e) + e) := by
            rw [← zpow_add₀ hradix_ne]
        _ = r := by simp
    have hfloor_mul_lt :
        ((IRNDD scaled : Int) : ℝ) * (radix : ℝ) ^ e <
          (radix : ℝ) ^ (e + p) := by
      calc
        ((IRNDD scaled : Int) : ℝ) * (radix : ℝ) ^ e ≤
            scaled * (radix : ℝ) ^ e := by
              exact mul_le_mul_of_nonneg_right hfloor_le (le_of_lt hpow_e_pos)
        _ = r := hscaled_mul
        _ < (radix : ℝ) ^ (e + p) := hr_lt
    have hfloor_lt_real : ((IRNDD scaled : Int) : ℝ) < (radix : ℝ) ^ p := by
      apply lt_of_mul_lt_mul_right
      · calc
          ((IRNDD scaled : Int) : ℝ) * (radix : ℝ) ^ e <
              (radix : ℝ) ^ (e + p) := hfloor_mul_lt
          _ = (radix : ℝ) ^ e * (radix : ℝ) ^ p := by
              rw [zpow_add₀ hradix_ne]
          _ = (radix : ℝ) ^ p * (radix : ℝ) ^ e := by ring
      · exact le_of_lt hpow_e_pos
    have hp_nonneg : (0 : Int) ≤ p := by omega
    have hp_toNat : (p.toNat : Int) = p := Int.toNat_of_nonneg hp_nonneg
    have hp_zpow :
        (radix : ℝ) ^ p = ((Zpower_nat radix p.toNat : Int) : ℝ) := by
      calc
        (radix : ℝ) ^ p = (radix : ℝ) ^ ((p.toNat : Nat) : Int) := by
          rw [hp_toNat]
        _ = (radix : ℝ) ^ p.toNat := zpow_ofNat (radix : ℝ) p.toNat
        _ = ((radix ^ p.toNat : Int) : ℝ) := by
          exact (Int.cast_pow radix p.toNat).symm
        _ = ((Zpower_nat radix p.toNat : Int) : ℝ) := by rfl
    rw [hp_zpow] at hfloor_lt_real
    exact Int.cast_lt.mp hfloor_lt_real
  · exact he_lb

noncomputable def RND_Min_Pos_canonic_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int) (r : ℝ) : Unit :=
  ()

/-- Coq: `RND_Min_Pos_canonic`.

The Lean statement makes the Coq section assumptions explicit: the binary
radix used by `F2R` agrees with the Pff radix, the radix and precision are
strictly greater than one, and the bound skeleton has the expected mantissa
bound. -/
theorem RND_Min_Pos_canonic {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int) (r : ℝ) :
    ⦃⌜0 ≤ r ∧ beta = radix ∧ 1 < radix ∧ 1 < p ∧
        b.vNum = Zpower_nat radix p.toNat⌝⦄
    (pure (RND_Min_Pos_canonic_check (beta:=beta) b radix p r) : Id Unit)
    ⦃⇓_ => ⌜Fcanonic (beta:=beta) radix b
        (RND_Min_Pos (beta:=beta) b radix p r)⌝⦄ := by
  intro h
  rcases h with ⟨hr_nonneg, hbeta_eq, hradix_gt, hp_gt, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, RND_Min_Pos_canonic_check,
    Id.run, ULift.up_down]
  unfold RND_Min_Pos
  have hradix_pos_int : (0 : Int) < radix := by omega
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by exact_mod_cast hradix_pos_int
  have hradix_one : (1 : ℝ) < (radix : ℝ) := by exact_mod_cast hradix_gt
  have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
  have hlog_pos : 0 < Real.log (radix : ℝ) := Real.log_pos hradix_one
  have hp_nonneg : (0 : Int) ≤ p := by omega
  have hp_toNat : (p.toNat : Int) = p := Int.toNat_of_nonneg hp_nonneg
  have hp_toNat_pos : 0 < p.toNat := by
    have h : (0 : Int) < (p.toNat : Int) := by
      rw [hp_toNat]
      omega
    exact_mod_cast h
  have hp_toNat_pred_cast : ((p.toNat - 1 : Nat) : Int) = p - 1 := by
    have hle : 1 ≤ p.toNat := by omega
    rw [Int.natCast_sub hle, hp_toNat]
    omega
  have hfirst_value :
      _root_.F2R (beta:=beta) (firstNormalPos (beta:=beta) radix b p.toNat) =
        (radix : ℝ) ^ (p - 1) * (radix : ℝ) ^ (-b.dExp) := by
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, firstNormalPos, nNormMin,
      FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp]
    rw [hbeta_eq]
    have hpow_cast :
        ((radix ^ (p.toNat - 1) : Int) : ℝ) = (radix : ℝ) ^ (p - 1) := by
      rw [Int.cast_pow]
      calc
        (radix : ℝ) ^ (p.toNat - 1) =
            (radix : ℝ) ^ ((p.toNat - 1 : Nat) : Int) := by
              rw [zpow_natCast]
        _ = (radix : ℝ) ^ (p - 1) := by rw [hp_toNat_pred_cast]
    rw [hpow_cast]
  by_cases hnormal_branch :
      _root_.F2R (beta:=beta) (firstNormalPos (beta:=beta) radix b p.toNat) ≤ r
  · simp only [hnormal_branch, ite_true]
    left
    set e : Int := IRNDD (Real.log r / Real.log (radix : ℝ) + (-(p - 1))) with he
    set m : Int := IRNDD (r * (radix : ℝ) ^ (-e)) with hm
    have hfirst_pos :
        0 < _root_.F2R (beta:=beta) (firstNormalPos (beta:=beta) radix b p.toNat) := by
      rw [hfirst_value]
      exact mul_pos (zpow_pos hradix_pos _) (zpow_pos hradix_pos _)
    have hr_pos : 0 < r := lt_of_lt_of_le hfirst_pos hnormal_branch
    have he_lower : -b.dExp ≤ e := by
      have hlog_first_le : Real.log
            (_root_.F2R (beta:=beta) (firstNormalPos (beta:=beta) radix b p.toNat))
          ≤ Real.log r := Real.log_le_log hfirst_pos hnormal_branch
      have hlog_first :
          Real.log (_root_.F2R (beta:=beta) (firstNormalPos (beta:=beta) radix b p.toNat))
            = ((p - 1 - b.dExp : Int) : ℝ) * Real.log (radix : ℝ) := by
          rw [hfirst_value]
          have hcombine :
              (radix : ℝ) ^ (p - 1) * (radix : ℝ) ^ (-b.dExp) =
                (radix : ℝ) ^ (p - 1 + -b.dExp) := by
            rw [zpow_add₀ hradix_ne]
          rw [hcombine, Real.log_zpow]
          ring
      have hreal :
          ((-b.dExp : Int) : ℝ) ≤
            Real.log r / Real.log (radix : ℝ) + (-(p - 1)) := by
        rw [hlog_first] at hlog_first_le
        have hdiv :
            ((p - 1 - b.dExp : Int) : ℝ) ≤
              Real.log r / Real.log (radix : ℝ) := by
          rw [le_div_iff₀ hlog_pos]
          simpa [sub_eq_add_neg, add_comm, add_left_comm, add_assoc] using hlog_first_le
        norm_num at hdiv ⊢
        linarith
      rw [he]
      exact Int.le_floor.mpr hreal
    have he_upper_real :
        Real.log r / Real.log (radix : ℝ) + (-(p - 1)) < ((e + 1 : Int) : ℝ) := by
      rw [he]
      simpa [IRNDD, Int.cast_add, Int.cast_one] using
        Int.lt_floor_add_one
          (Real.log r / Real.log (radix : ℝ) + (-(p - 1)))
    have hr_upper : r < (radix : ℝ) ^ (e + p) := by
      have hlog_lt : Real.log r < ((e + p : Int) : ℝ) * Real.log (radix : ℝ) := by
        have htmp : Real.log r / Real.log (radix : ℝ) < ((e + p : Int) : ℝ) := by
          norm_num at he_upper_real ⊢
          linarith
        calc
          Real.log r = (Real.log r / Real.log (radix : ℝ)) * Real.log (radix : ℝ) := by
            field_simp [ne_of_gt hlog_pos]
          _ < ((e + p : Int) : ℝ) * Real.log (radix : ℝ) := by
            exact mul_lt_mul_of_pos_right htmp hlog_pos
      exact Real.lt_zpow_of_log_lt hradix_pos hlog_lt
    have hbounded : Fbounded (beta:=beta) b
        (FloatSpec.Core.Defs.FlocqFloat.mk m e) := by
      have hspec := RND_Min_Pos_bounded_aux (beta:=beta) b radix p e r
      simp only [wp, PostCond.noThrow, pure, RND_Min_Pos_bounded_aux_check,
        Id.run, ULift.up_down] at hspec
      have hpre : 0 ≤ r ∧ 1 < radix ∧ 1 < p ∧
          b.vNum = Zpower_nat radix p.toNat ∧ -b.dExp ≤ e ∧
          r < (radix : ℝ) ^ (e + p) :=
        ⟨hr_nonneg, hradix_gt, hp_gt, hvNum, he_lower, hr_upper⟩
      simpa [m, e, hm, he] using hspec hpre
    have hm_lower : (radix : ℝ) ^ (p - 1) ≤ (m : ℝ) := by
      have he_floor_le :
          (e : ℝ) ≤ Real.log r / Real.log (radix : ℝ) + (-(p - 1)) := by
        rw [he]
        exact Int.floor_le _
      have hlog_lower :
          ((e + p - 1 : Int) : ℝ) * Real.log (radix : ℝ) ≤ Real.log r := by
        have htmp : ((e + p - 1 : Int) : ℝ) ≤ Real.log r / Real.log (radix : ℝ) := by
          norm_num at he_floor_le ⊢
          linarith
        calc
          ((e + p - 1 : Int) : ℝ) * Real.log (radix : ℝ) ≤
              (Real.log r / Real.log (radix : ℝ)) * Real.log (radix : ℝ) := by
                exact mul_le_mul_of_nonneg_right htmp (le_of_lt hlog_pos)
          _ = Real.log r := by field_simp [ne_of_gt hlog_pos]
      have hpow_le_r : (radix : ℝ) ^ (e + p - 1) ≤ r := by
        exact (Real.zpow_le_iff_le_log hradix_pos hr_pos).2 hlog_lower
      have hscale_le : (radix : ℝ) ^ (p - 1) ≤ r * (radix : ℝ) ^ (-e) := by
        have hpow_pos_e : 0 < (radix : ℝ) ^ (-e) := zpow_pos hradix_pos _
        have hmul := mul_le_mul_of_nonneg_right hpow_le_r (le_of_lt hpow_pos_e)
        calc
          (radix : ℝ) ^ (p - 1) =
              (radix : ℝ) ^ (e + p - 1) * (radix : ℝ) ^ (-e) := by
                rw [← zpow_add₀ hradix_ne]
                ring_nf
          _ ≤ r * (radix : ℝ) ^ (-e) := hmul
      rw [hm]
      have hpow_pred_cast :
          ((radix ^ (p.toNat - 1) : Int) : ℝ) = (radix : ℝ) ^ (p - 1) := by
        rw [Int.cast_pow]
        calc
          (radix : ℝ) ^ (p.toNat - 1) =
              (radix : ℝ) ^ ((p.toNat - 1 : Nat) : Int) := by
                rw [zpow_natCast]
          _ = (radix : ℝ) ^ (p - 1) := by rw [hp_toNat_pred_cast]
      have hint :
          (radix ^ (p.toNat - 1) : Int) ≤
            IRNDD (r * (radix : ℝ) ^ (-e)) := by
        exact Int.le_floor.mpr (by simpa [hpow_pred_cast] using hscale_le)
      have hcast :
          ((radix ^ (p.toNat - 1) : Int) : ℝ) ≤
            ((IRNDD (r * (radix : ℝ) ^ (-e)) : Int) : ℝ) := by
        exact_mod_cast hint
      simpa [hpow_pred_cast] using hcast
    constructor
    · exact hbounded
    · rw [hvNum]
      have hm_nonneg : 0 ≤ m := by
        have hscale_nonneg : 0 ≤ r * (radix : ℝ) ^ (-e) :=
          mul_nonneg hr_nonneg (le_of_lt (zpow_pos hradix_pos _))
        rw [hm, IRNDD]
        exact Int.floor_nonneg.mpr hscale_nonneg
      have hradix_m_pos : 0 ≤ radix * m := by nlinarith
      rw [abs_of_nonneg hradix_m_pos]
      have hpow_cast :
          ((Zpower_nat radix p.toNat : Int) : ℝ) = (radix : ℝ) ^ p := by
        rw [Zpower_nat, Int.cast_pow, ← zpow_natCast, hp_toNat]
      have hgoal_real : ((Zpower_nat radix p.toNat : Int) : ℝ) ≤ ((radix * m : Int) : ℝ) := by
        rw [hpow_cast, Int.cast_mul]
        have hmul := mul_le_mul_of_nonneg_left hm_lower (le_of_lt hradix_pos)
        calc
          (radix : ℝ) ^ p =
              (radix : ℝ) * (radix : ℝ) ^ (p - 1) := by
                calc
                  (radix : ℝ) ^ p =
                      (radix : ℝ) ^ ((1 : Int) + (p - 1)) := by
                        congr 1
                        omega
                  _ = (radix : ℝ) ^ (1 : Int) * (radix : ℝ) ^ (p - 1) := by
                        rw [zpow_add₀ hradix_ne]
                  _ = (radix : ℝ) * (radix : ℝ) ^ (p - 1) := by simp
          _ ≤ (radix : ℝ) * (m : ℝ) := hmul
      exact Int.cast_le.mp hgoal_real
  · simp only [hnormal_branch, ite_false]
    right
    have hr_lt_first :
        r < _root_.F2R (beta:=beta) (firstNormalPos (beta:=beta) radix b p.toNat) :=
      lt_of_not_ge hnormal_branch
    set m : Int := IRNDD (r * (radix : ℝ) ^ b.dExp) with hm
    have hfirst_upper :
        _root_.F2R (beta:=beta) (firstNormalPos (beta:=beta) radix b p.toNat) ≤
          (radix : ℝ) ^ (-b.dExp + p) := by
      rw [hfirst_value]
      calc
        (radix : ℝ) ^ (p - 1) * (radix : ℝ) ^ (-b.dExp)
            = (radix : ℝ) ^ (p - 1 + -b.dExp) := by
                rw [zpow_add₀ hradix_ne]
        _ ≤ (radix : ℝ) ^ (-b.dExp + p) := by
              apply zpow_le_zpow_right₀ (le_of_lt hradix_one)
              omega
    have hr_upper : r < (radix : ℝ) ^ (-b.dExp + p) :=
      lt_of_lt_of_le hr_lt_first hfirst_upper
    have hbounded : Fbounded (beta:=beta) b
        (FloatSpec.Core.Defs.FlocqFloat.mk m (-b.dExp)) := by
      have hspec := RND_Min_Pos_bounded_aux (beta:=beta) b radix p (-b.dExp) r
      simp only [wp, PostCond.noThrow, pure, RND_Min_Pos_bounded_aux_check,
        Id.run, ULift.up_down] at hspec
      have hpre : 0 ≤ r ∧ 1 < radix ∧ 1 < p ∧
          b.vNum = Zpower_nat radix p.toNat ∧ -b.dExp ≤ -b.dExp ∧
          r < (radix : ℝ) ^ (-b.dExp + p) :=
        ⟨hr_nonneg, hradix_gt, hp_gt, hvNum, le_rfl, hr_upper⟩
      simpa [m, hm] using hspec hpre
    refine ⟨hbounded, rfl, ?_⟩
    rw [hvNum]
    have hscale_lt : (m : ℝ) < (radix : ℝ) ^ (p - 1) := by
      have hfirst_value' :
          _root_.F2R (beta:=beta) (firstNormalPos (beta:=beta) radix b p.toNat) =
            (radix : ℝ) ^ (p - 1 + -b.dExp) := by
        rw [hfirst_value, zpow_add₀ hradix_ne]
      have hmul_lt : r * (radix : ℝ) ^ b.dExp < (radix : ℝ) ^ (p - 1) := by
        have hpow_pos : 0 < (radix : ℝ) ^ b.dExp := zpow_pos hradix_pos _
        have hmul := mul_lt_mul_of_pos_right hr_lt_first hpow_pos
        rw [hfirst_value'] at hmul
        calc
          r * (radix : ℝ) ^ b.dExp <
              (radix : ℝ) ^ (p - 1 + -b.dExp) * (radix : ℝ) ^ b.dExp := hmul
          _ = (radix : ℝ) ^ (p - 1) := by
              rw [← zpow_add₀ hradix_ne]
              ring_nf
      rw [hm, IRNDD]
      exact lt_of_le_of_lt (Int.floor_le _) hmul_lt
    have hm_nonneg : 0 ≤ m := by
      have hscale_nonneg : 0 ≤ r * (radix : ℝ) ^ b.dExp :=
        mul_nonneg hr_nonneg (le_of_lt (zpow_pos hradix_pos _))
      rw [hm, IRNDD]
      exact Int.floor_nonneg.mpr hscale_nonneg
    have hradix_m_nonneg : 0 ≤ radix * m := by nlinarith
    rw [abs_of_nonneg hradix_m_nonneg]
    have hpow_cast :
        ((Zpower_nat radix p.toNat : Int) : ℝ) = (radix : ℝ) ^ p := by
      rw [Zpower_nat, Int.cast_pow, ← zpow_natCast, hp_toNat]
    have hgoal_real : ((radix * m : Int) : ℝ) < ((Zpower_nat radix p.toNat : Int) : ℝ) := by
      rw [Int.cast_mul, hpow_cast]
      have hmul : (radix : ℝ) * (m : ℝ) <
          (radix : ℝ) * (radix : ℝ) ^ (p - 1) := by
        exact mul_lt_mul_of_pos_left hscale_lt hradix_pos
      calc
        (radix : ℝ) * (m : ℝ) <
            (radix : ℝ) * (radix : ℝ) ^ (p - 1) := hmul
        _ = (radix : ℝ) ^ p := by
            calc
              (radix : ℝ) * (radix : ℝ) ^ (p - 1) =
                  (radix : ℝ) ^ (1 : Int) * (radix : ℝ) ^ (p - 1) := by simp
              _ = (radix : ℝ) ^ ((1 : Int) + (p - 1)) := by
                    rw [zpow_add₀ hradix_ne]
              _ = (radix : ℝ) ^ p := by
                    congr 1
                    omega
    exact Int.cast_lt.mp hgoal_real

-- Lower rounding on nonnegative reals is ≤ the input (Coq: RND_Min_Pos_Rle)
noncomputable def RND_Min_Pos_Rle_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int) (r : ℝ) : Unit :=
  ()

/-- Coq: `RND_Min_Pos_Rle` — for nonnegative `r`, the value of
    `RND_Min_Pos r` (interpreted in ℝ) is less than or equal to `r`. -/
theorem RND_Min_Pos_Rle {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int) (r : ℝ) :
    ⦃⌜0 ≤ r ∧ beta = radix ∧ 1 < radix⌝⦄
    (pure (RND_Min_Pos_Rle_check (beta:=beta) b radix p r) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (RND_Min_Pos (beta:=beta) b radix p r) ≤ r⌝⦄ := by
  intro ⟨hr, hBetaEq, hRadixGt1⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure, RND_Min_Pos_Rle_check, ULift.down_up,
             PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
             ULift.up, ULift.down, Id.run]
  -- Split on normal vs subnormal case
  by_cases hNormal : _root_.F2R (beta:=radix)
      (firstNormalPos (beta:=radix) radix b p.toNat) ≤ r
  · -- Normal case: F2R ⟨IRNDD(r * radix^(-e)), e⟩ ≤ r
    -- where e = IRNDD (log r / log radix - (p-1))
    rw [RND_Min_Pos, ite_eq_left hNormal]
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
      FloatSpec.Core.Defs.FlocqFloat.Fnum,
      FloatSpec.Core.Defs.FlocqFloat.Fexp]
    -- The key: IRNDD(r * radix^(-e)) * radix^e ≤ r
    -- Because IRNDD(x) ≤ x, we have IRNDD(r * radix^(-e)) ≤ r * radix^(-e)
    -- So IRNDD(r * radix^(-e)) * radix^e ≤ r * radix^(-e) * radix^e = r
    have hRadixPos : (0 : ℝ) < radix := by
      have h1 : (1 : ℤ) < radix := hRadixGt1
      have h2 : ((1 : ℤ) : ℝ) < ((radix : ℤ) : ℝ) := Int.cast_lt.mpr h1
      simp only [Int.cast_one] at h2
      linarith
    have hRadixNe0 : (radix : ℝ) ≠ 0 := ne_of_gt hRadixPos
    let e := IRNDD (Real.log r / Real.log (radix : ℝ) + (-(p - 1)))
    have hFloorLe : (IRNDD (r * (radix : ℝ) ^ (-e)) : ℝ) ≤ r * (radix : ℝ) ^ (-e) := by
      simp only [IRNDD]
      exact Int.floor_le _
    -- radix^e > 0 since radix > 0
    have hRadixPowPos : (0 : ℝ) < (radix : ℝ) ^ e := zpow_pos hRadixPos e
    calc (IRNDD (r * (radix : ℝ) ^ (-e)) : ℝ) * (radix : ℝ) ^ e
        ≤ (r * (radix : ℝ) ^ (-e)) * (radix : ℝ) ^ e := by
          apply mul_le_mul_of_nonneg_right hFloorLe (le_of_lt hRadixPowPos)
      _ = r * ((radix : ℝ) ^ (-e) * (radix : ℝ) ^ e) := by ring
      _ = r * (radix : ℝ) ^ ((-e) + e) := by rw [← zpow_add₀ hRadixNe0]
      _ = r * 1 := by simp only [neg_add_cancel, zpow_zero]
      _ = r := mul_one r
  · -- Subnormal case: F2R ⟨IRNDD(r * radix^(dExp b)), -dExp b⟩ ≤ r
    rw [RND_Min_Pos, ite_eq_right hNormal]
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
      FloatSpec.Core.Defs.FlocqFloat.Fnum,
      FloatSpec.Core.Defs.FlocqFloat.Fexp]
    have hRadixPos : (0 : ℝ) < radix := by
      have h1 : (1 : ℤ) < radix := hRadixGt1
      have h2 : ((1 : ℤ) : ℝ) < ((radix : ℤ) : ℝ) := Int.cast_lt.mpr h1
      simp only [Int.cast_one] at h2
      linarith
    have hRadixNe0 : (radix : ℝ) ≠ 0 := ne_of_gt hRadixPos
    have hFloorLe : (IRNDD (r * (radix : ℝ) ^ b.dExp) : ℝ) ≤ r * (radix : ℝ) ^ b.dExp := by
      simp only [IRNDD]
      exact Int.floor_le _
    have hRadixPowPos : (0 : ℝ) < (radix : ℝ) ^ (-b.dExp) := zpow_pos hRadixPos (-b.dExp)
    calc (IRNDD (r * (radix : ℝ) ^ b.dExp) : ℝ) * (radix : ℝ) ^ (-b.dExp)
        ≤ (r * (radix : ℝ) ^ b.dExp) * (radix : ℝ) ^ (-b.dExp) := by
          apply mul_le_mul_of_nonneg_right hFloorLe (le_of_lt hRadixPowPos)
      _ = r * ((radix : ℝ) ^ b.dExp * (radix : ℝ) ^ (-b.dExp)) := by ring
      _ = r * (radix : ℝ) ^ (b.dExp + (-b.dExp)) := by rw [← zpow_add₀ hRadixNe0]
      _ = r * 1 := by simp only [add_neg_cancel, zpow_zero]
      _ = r := mul_one r

private lemma RND_Min_Pos_nonneg {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int) (r : ℝ)
    (hr : 0 ≤ r) (hBetaEq : beta = radix) (hRadixGt1 : 1 < radix) :
    0 ≤ _root_.F2R (RND_Min_Pos (beta:=beta) b radix p r) := by
  have hRadixPos : (0 : ℝ) < radix := by
    have h1 : (1 : ℤ) < radix := hRadixGt1
    have h2 : ((1 : ℤ) : ℝ) < ((radix : ℤ) : ℝ) := Int.cast_lt.mpr h1
    simp only [Int.cast_one] at h2
    linarith
  have hBetaPos : (0 : ℝ) < beta := by
    rw [hBetaEq]
    exact hRadixPos
  by_cases hbranch :
      _root_.F2R (beta:=beta) (firstNormalPos (beta:=beta) radix b p.toNat) ≤ r
  · have hbranch' :
        ((firstNormalPos (beta:=beta) radix b p.toNat).Fnum : ℝ) *
            (beta : ℝ) ^ (firstNormalPos (beta:=beta) radix b p.toNat).Fexp ≤ r := by
      simpa [_root_.F2R, FloatSpec.Core.Defs.F2R] using hbranch
    simp [RND_Min_Pos, hbranch', _root_.F2R, FloatSpec.Core.Defs.F2R,
      FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp]
    apply mul_nonneg
    · exact_mod_cast (Int.floor_nonneg.mpr
        (mul_nonneg hr (le_of_lt (inv_pos.mpr (zpow_pos hRadixPos _)))))
    · exact le_of_lt (zpow_pos hBetaPos _)
  · have hbranch' :
        ¬ ((firstNormalPos (beta:=beta) radix b p.toNat).Fnum : ℝ) *
            (beta : ℝ) ^ (firstNormalPos (beta:=beta) radix b p.toNat).Fexp ≤ r := by
      simpa [_root_.F2R, FloatSpec.Core.Defs.F2R] using hbranch
    simp [RND_Min_Pos, hbranch', _root_.F2R, FloatSpec.Core.Defs.F2R,
      FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp]
    apply mul_nonneg
    · exact_mod_cast (Int.floor_nonneg.mpr
        (mul_nonneg hr (le_of_lt (zpow_pos hRadixPos _))))
    · exact le_of_lt (inv_pos.mpr (zpow_pos hBetaPos _))

-- Helper lemma: monotonicity of IRNDD (floor)
private lemma IRNDD_monotone {x y : ℝ} (h : x ≤ y) : IRNDD x ≤ IRNDD y := by
  simp only [IRNDD]
  exact Int.floor_le_floor h

-- Helper: for positive base and exponent, zpow is positive
private lemma radix_zpow_pos {radix : ℤ} (hRadix : (1 : ℤ) < radix) (e : ℤ) :
    (0 : ℝ) < (radix : ℝ) ^ e := by
  have hRadixPos : (0 : ℝ) < radix := by
    have h1 : ((1 : ℤ) : ℝ) < ((radix : ℤ) : ℝ) := Int.cast_lt.mpr hRadix
    simp only [Int.cast_one] at h1
    linarith
  exact zpow_pos hRadixPos e

-- Helper: floor scaled by positive power preserves inequality in real value when exponents match
private lemma floor_scaled_mono {radix : ℤ} (hRadix : (1 : ℤ) < radix) (e : ℤ) {x y : ℝ}
    (h : x ≤ y) :
    (IRNDD x : ℝ) * (radix : ℝ) ^ e ≤ (IRNDD y : ℝ) * (radix : ℝ) ^ e := by
  have hPowPos : (0 : ℝ) < (radix : ℝ) ^ e := radix_zpow_pos hRadix e
  exact mul_le_mul_of_nonneg_right (Int.cast_le.mpr (IRNDD_monotone h)) (le_of_lt hPowPos)

-- Helper: log is monotone for positive values
private lemma log_div_log_mono {radix : ℤ} (hRadix : (1 : ℤ) < radix) {r₁ r₂ : ℝ}
    (hr₁_pos : 0 < r₁) (h : r₁ ≤ r₂) :
    Real.log r₁ / Real.log radix ≤ Real.log r₂ / Real.log radix := by
  have hRadixReal : (1 : ℝ) < radix := by
    have h1 : ((1 : ℤ) : ℝ) < ((radix : ℤ) : ℝ) := Int.cast_lt.mpr hRadix
    simp only [Int.cast_one] at h1
    exact h1
  have hLogRadixPos : 0 < Real.log radix := Real.log_pos hRadixReal
  apply div_le_div_of_nonneg_right _ (le_of_lt hLogRadixPos)
  exact Real.log_le_log hr₁_pos h

-- Helper: exponent calculation is monotone for positive values
private lemma exponent_mono {radix p : ℤ} (hRadix : (1 : ℤ) < radix) {r₁ r₂ : ℝ}
    (hr₁_pos : 0 < r₁) (h : r₁ ≤ r₂) :
    IRNDD (Real.log r₁ / Real.log radix + (-(p - 1))) ≤
    IRNDD (Real.log r₂ / Real.log radix + (-(p - 1))) := by
  apply IRNDD_monotone
  have hlog : Real.log r₁ / Real.log radix ≤ Real.log r₂ / Real.log radix :=
    log_div_log_mono hRadix hr₁_pos h
  linarith

-- Monotonicity of `RND_Min_Pos` w.r.t. the real input (Coq: RND_Min_Pos_monotone)
noncomputable def RND_Min_Pos_monotone_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int) (r₁ r₂ : ℝ) : Unit :=
  ()

/-- Coq: `RND_Min_Pos_monotone` — for nonnegative `r₁ ≤ r₂`, the lower rounding
    on nonnegative reals is monotone in the sense that the real value of
    `RND_Min_Pos r₁` is less than or equal to that of `RND_Min_Pos r₂`.
    We mirror the statement using the hoare-triple style.

    Note: The Coq version has implicit hypotheses from Section:
    - radix > 1
    - beta = radix (so the base used in F2R matches the base in RND_Min_Pos)
    These are now explicit in the precondition. -/
theorem RND_Min_Pos_monotone {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int) (r₁ r₂ : ℝ) :
    ⦃⌜0 ≤ r₁ ∧ r₁ ≤ r₂ ∧ beta = radix ∧ 1 < radix ∧ 1 ≤ p⌝⦄
    (pure (RND_Min_Pos_monotone_check (beta:=beta) b radix p r₁ r₂) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (RND_Min_Pos (beta:=beta) b radix p r₁)
            ≤ _root_.F2R (RND_Min_Pos (beta:=beta) b radix p r₂)⌝⦄ := by
  intro ⟨hR1Pos, hR12, hBetaEq, hRadixGt1, hPGe1⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure, RND_Min_Pos_monotone_check, ULift.down_up,
        PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
        ULift.up, ULift.down, Id.run]
  -- Derive key positivity facts
  have hRadixPos : (0 : ℝ) < radix := by
    have h1 : ((1 : ℤ) : ℝ) < ((radix : ℤ) : ℝ) := Int.cast_lt.mpr hRadixGt1
    simp only [Int.cast_one] at h1
    linarith
  have hRadixNe0 : (radix : ℝ) ≠ 0 := ne_of_gt hRadixPos
  have hBetaPos : (0 : ℝ) < radix := hRadixPos
  -- Let firstNP denote the first normal position threshold
  set firstNP := (↑(firstNormalPos (beta:=radix) radix b p.toNat).Fnum : ℝ) *
                  (radix : ℝ) ^ (firstNormalPos (beta:=radix) radix b p.toNat).Fexp with hFirstNP
  have hFirstNP_as_F2R : firstNP = _root_.F2R (beta:=radix)
      (firstNormalPos (beta:=radix) radix b p.toNat) := by
    simp only [hFirstNP, _root_.F2R, FloatSpec.Core.Defs.F2R]
  -- Case analysis on whether r₁ and r₂ are normal or subnormal
  by_cases h1 : firstNP ≤ r₁
  · -- Case 1: r₁ is normal (and hence r₂ is also normal since r₁ ≤ r₂)
    have h2 : firstNP ≤ r₂ := le_trans h1 hR12
    have h1' : _root_.F2R (beta:=radix)
        (firstNormalPos (beta:=radix) radix b p.toNat) ≤ r₁ := by
      rw [← hFirstNP_as_F2R]
      exact h1
    have h2' : _root_.F2R (beta:=radix)
        (firstNormalPos (beta:=radix) radix b p.toNat) ≤ r₂ := by
      rw [← hFirstNP_as_F2R]
      exact h2
    rw [RND_Min_Pos, ite_eq_left h1', RND_Min_Pos, ite_eq_left h2']
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
      FloatSpec.Core.Defs.FlocqFloat.Fnum,
      FloatSpec.Core.Defs.FlocqFloat.Fexp]
    -- Both are normal; need to show the F2R values are ordered
    -- Define exponents for clarity
    set e₁ := IRNDD (Real.log r₁ / Real.log radix + (-(p - 1))) with he₁
    set e₂ := IRNDD (Real.log r₂ / Real.log radix + (-(p - 1))) with he₂
    -- Define mantissas
    set m₁ := IRNDD (r₁ * (radix : ℝ) ^ (-e₁)) with hm₁
    set m₂ := IRNDD (r₂ * (radix : ℝ) ^ (-e₂)) with hm₂
    -- Goal: m₁ * beta^e₁ ≤ m₂ * beta^e₂
    -- Since beta = radix, this is: m₁ * radix^e₁ ≤ m₂ * radix^e₂
    -- First, we need r₁ > 0 for log to be defined (follows from h1 and firstNP > 0)
    have hFirstNPPos : 0 < firstNP := by
      simp only [hFirstNP, firstNormalPos, nNormMin,
                 FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp]
      -- Goal: 0 < (radix^(p.toNat - 1) : ℝ) * beta^(-b.dExp)
      -- Both radix^(p.toNat - 1) > 0 (since radix > 1) and beta^(-b.dExp) > 0 (since beta > 0)
      apply mul_pos
      · -- radix^(p.toNat - 1) > 0
        apply Int.cast_pos.mpr
        apply Int.pow_pos
        omega
      · -- beta^(-b.dExp) > 0
        exact zpow_pos hBetaPos _
    have hr₁_pos : 0 < r₁ := lt_of_lt_of_le hFirstNPPos h1
    have hr₂_pos : 0 < r₂ := lt_of_lt_of_le hFirstNPPos h2
    -- Key property: e₁ ≤ e₂ (exponent monotonicity)
    have hExpMono : e₁ ≤ e₂ := exponent_mono hRadixGt1 hr₁_pos hR12
    -- The round-down values satisfy: m₁ * radix^e₁ ≤ r₁ and m₂ * radix^e₂ ≤ r₂
    -- Also: r₁ < (m₁ + 1) * radix^e₁ and r₂ < (m₂ + 1) * radix^e₂
    -- Since r₁ ≤ r₂ and these are floor-based approximations...
    -- The proof is complex when e₁ < e₂ because we need to account for the binade structure
    -- For e₁ = e₂, the proof is straightforward via floor monotonicity
    by_cases hExpEq : e₁ = e₂
    · -- Same exponent case: direct floor monotonicity
      rw [hExpEq]
      -- Since e₁ = e₂, m₁ = IRNDD(r₁ * radix^(-e₁)) = IRNDD(r₁ * radix^(-e₂))
      have hm₁_eq : m₁ = IRNDD (r₁ * (radix : ℝ) ^ (-e₂)) := by
        simp only [hm₁, hExpEq]
      have hScaled : r₁ * (radix : ℝ) ^ (-e₂) ≤ r₂ * (radix : ℝ) ^ (-e₂) := by
        have hPowPos : (0 : ℝ) < (radix : ℝ) ^ (-e₂) := zpow_pos hRadixPos (-e₂)
        exact mul_le_mul_of_nonneg_right hR12 (le_of_lt hPowPos)
      have hFloorMono : m₁ ≤ m₂ := by
        rw [hm₁_eq, hm₂]
        exact IRNDD_monotone hScaled
      have hBetaPowPos : (0 : ℝ) < (radix : ℝ) ^ e₂ := zpow_pos hBetaPos e₂
      exact mul_le_mul_of_nonneg_right (Int.cast_le.mpr hFloorMono) (le_of_lt hBetaPowPos)
    · -- Different exponent case: e₁ < e₂
      -- This is the complex case requiring the binade structure
      -- m₁ * radix^e₁ ≤ r₁ ≤ r₂
      -- Since e₁ < e₂, r₁ is in a lower binade than r₂
      -- The upper bound of binade e₁ is radix^(e₁+p) (approximately)
      -- And r₂ is at least at the boundary radix^(e₂+p-1)
      -- So m₁ * radix^e₁ < radix^(e₁+p) ≤ radix^(e₂+p-1) ≤ m₂ * radix^e₂
      have hExpLt : e₁ < e₂ := lt_of_le_of_ne hExpMono hExpEq
      -- Key insight: m₁ * radix^e₁ ≤ r₁ ≤ r₂, and m₂ is the floor of r₂ * radix^(-e₂)
      -- Since e₁ < e₂, we have e₂ - e₁ ≥ 1
      -- m₁ * radix^e₁ ≤ r₁ ≤ r₂
      -- m₂ * radix^e₂ ≤ r₂ < (m₂ + 1) * radix^e₂
      -- We need: m₁ * radix^e₁ ≤ m₂ * radix^e₂
      -- Rewrite as: m₁ ≤ m₂ * radix^(e₂ - e₁)
      -- Since m₂ ≥ radix^(p-1) (normal float), m₂ * radix^(e₂-e₁) ≥ radix^(p-1+e₂-e₁)
      -- And m₁ < radix^p (bounded mantissa)
      -- If e₂ - e₁ ≥ 1, then radix^(p-1+1) = radix^p > m₁, so m₁ < radix^p ≤ m₂ * radix^(e₂-e₁)
      -- This works when m₂ ≥ radix^(p-1)

      -- Use transitivity: m₁ * radix^e₁ ≤ r₁ ≤ r₂, and we need to compare with m₂ * radix^e₂
      -- Since m₁ * radix^e₁ ≤ r₁ (floor property) and r₁ ≤ r₂ ≤ m₂ * radix^e₂ + something
      -- Actually, the floor satisfies: m₂ * radix^e₂ ≤ r₂
      -- So we need: m₁ * radix^e₁ ≤ m₂ * radix^e₂
      -- Given: m₁ * radix^e₁ ≤ r₁ ≤ r₂
      -- And: m₂ * radix^e₂ ≤ r₂

      -- The key is that for normal floats, the mantissa m satisfies radix^(p-1) ≤ m < radix^p
      -- So m₁ < radix^p and m₂ ≥ radix^(p-1)
      -- Thus: m₁ * radix^e₁ < radix^p * radix^e₁ = radix^(p+e₁)
      -- And: m₂ * radix^e₂ ≥ radix^(p-1) * radix^e₂ = radix^(p-1+e₂)
      -- Since e₁ < e₂, we have p + e₁ < p + e₂, so radix^(p+e₁) ≤ radix^(p-1+e₂) when e₂ ≥ e₁ + 1
      -- Therefore: m₁ * radix^e₁ < radix^(p+e₁) ≤ radix^(p-1+e₂) ≤ m₂ * radix^e₂

      -- Key strategy: Use transitivity through r₁ and the binade boundaries
      -- Step 1: m₁ * radix^e₁ ≤ r₁ (floor property)
      have hm1_floor : (m₁ : ℝ) ≤ r₁ * (radix : ℝ) ^ (-e₁) := by
        simp only [hm₁, IRNDD]
        exact Int.floor_le _

      have hm1_radix_le_r1 : (m₁ : ℝ) * (radix : ℝ) ^ e₁ ≤ r₁ := by
        have hPowPos : (0 : ℝ) < (radix : ℝ) ^ e₁ := radix_zpow_pos hRadixGt1 e₁
        calc (m₁ : ℝ) * (radix : ℝ) ^ e₁
            ≤ (r₁ * (radix : ℝ) ^ (-e₁)) * (radix : ℝ) ^ e₁ := by
              apply mul_le_mul_of_nonneg_right hm1_floor (le_of_lt hPowPos)
          _ = r₁ * ((radix : ℝ) ^ (-e₁) * (radix : ℝ) ^ e₁) := by ring
          _ = r₁ * (radix : ℝ) ^ ((-e₁) + e₁) := by
              rw [← zpow_add₀ hRadixNe0 (-e₁) e₁]
          _ = r₁ * (radix : ℝ) ^ (0 : ℤ) := by norm_num
          _ = r₁ := by rw [zpow_zero]; ring

      -- Step 2: r₁ ≤ r₂ (hypothesis hR12)
      -- So m₁ * radix^e₁ ≤ r₁ ≤ r₂

      -- Step 3: From the exponent definition, r₂ ≥ radix^(e₂+p-1) for normal floats
      -- The exponent e₂ satisfies: e₂ ≤ log(r₂)/log(radix) - (p-1) < e₂ + 1
      -- Therefore: e₂ + p - 1 ≤ log(r₂)/log(radix) < e₂ + p
      -- Taking exponentials: radix^(e₂+p-1) ≤ r₂ < radix^(e₂+p)

      have hRadixReal : (1 : ℝ) < radix := by
        have h1 : ((1 : ℤ) : ℝ) < ((radix : ℤ) : ℝ) := Int.cast_lt.mpr hRadixGt1
        simp only [Int.cast_one] at h1
        exact h1

      have hLogRadixPos : 0 < Real.log radix := Real.log_pos hRadixReal

      -- From IRNDD definition: e₂ = ⌊log(r₂)/log(radix) - (p-1)⌋
      -- So e₂ ≤ log(r₂)/log(radix) - (p-1)
      have he2_bound : (e₂ : ℝ) ≤ Real.log r₂ / Real.log radix - (p - 1) := by
        simp only [he₂, IRNDD]
        have := Int.floor_le (Real.log r₂ / Real.log radix + (-(p - 1)))
        linarith

      -- From e₂ ≤ log(r₂)/log(radix) - (p-1), we get:
      -- e₂ + p - 1 ≤ log(r₂)/log(radix)
      -- radix^(e₂ + p - 1) ≤ r₂
      have hr2_lower : (radix : ℝ) ^ (e₂ + p - 1) ≤ r₂ := by
        have hExp : (e₂ : ℝ) + (p - 1) ≤ Real.log r₂ / Real.log radix := by linarith
        have hExp2 : ((e₂ + p - 1) : ℤ) = e₂ + (p - 1) := by ring
        rw [show (e₂ + p - 1 : ℤ) = (e₂ : ℤ) + ((p : ℤ) - 1) by ring]
        have hExp3 : (e₂ : ℝ) + ((p : ℝ) - 1) ≤ Real.log r₂ / Real.log radix := hExp
        have hPosRadix : (0 : ℝ) < radix := lt_trans zero_lt_one hRadixReal
        -- We need: radix^(e₂ + p - 1) ≤ r₂
        -- Equivalently: (e₂ + p - 1) * log(radix) ≤ log(r₂)
        have hLogIneq : ((e₂ : ℝ) + ((p : ℝ) - 1)) * Real.log radix ≤ Real.log r₂ := by
          calc ((e₂ : ℝ) + ((p : ℝ) - 1)) * Real.log radix
              ≤ (Real.log r₂ / Real.log radix) * Real.log radix := by
                apply mul_le_mul_of_nonneg_right hExp3 (le_of_lt hLogRadixPos)
            _ = Real.log r₂ := by field_simp
        have hExpIneq := Real.exp_le_exp.mpr hLogIneq
        rw [Real.exp_log hr₂_pos] at hExpIneq
        -- Now we need to show radix^(e₂ + (p - 1)) = exp((e₂ + (p-1)) * log radix)
        have hZpowToExp : (radix : ℝ) ^ ((e₂ : ℤ) + ((p : ℤ) - 1)) =
            Real.exp (((e₂ : ℝ) + ((p : ℝ) - 1)) * Real.log radix) := by
          rw [← Real.rpow_intCast radix ((e₂ : ℤ) + ((p : ℤ) - 1))]
          rw [Real.rpow_def_of_pos hPosRadix]
          congr 1
          simp only [Int.cast_add, Int.cast_sub, Int.cast_one]
          ring
        rw [hZpowToExp]
        exact hExpIneq

      -- Step 4: m₂ ≥ radix^(p-1) (minimum normal mantissa)
      -- Since r₂ ≥ radix^(e₂+p-1), we have r₂ * radix^(-e₂) ≥ radix^(p-1)
      -- And m₂ = ⌊r₂ * radix^(-e₂)⌋ ≥ radix^(p-1) since radix^(p-1) is an integer
      have hm2_lower : (radix : ℝ) ^ (p - 1) ≤ (m₂ : ℝ) := by
        simp only [hm₂, IRNDD]
        -- Need: radix^(p-1) ≤ ⌊r₂ * radix^(-e₂)⌋
        have hScaled : (radix : ℝ) ^ (p - 1) ≤ r₂ * (radix : ℝ) ^ (-e₂) := by
          calc (radix : ℝ) ^ (p - 1)
              = (radix : ℝ) ^ (e₂ + p - 1 + (-e₂)) := by ring_nf
            _ = (radix : ℝ) ^ (e₂ + p - 1) * (radix : ℝ) ^ (-e₂) := by
                rw [zpow_add₀ hRadixNe0]
            _ ≤ r₂ * (radix : ℝ) ^ (-e₂) := by
                apply mul_le_mul_of_nonneg_right hr2_lower
                exact le_of_lt (radix_zpow_pos hRadixGt1 _)
        -- We want: (radix : ℝ) ^ (p - 1) ≤ ↑⌊r₂ * (radix : ℝ) ^ (-e₂)⌋
        -- We know: hScaled : (radix : ℝ) ^ (p - 1) ≤ r₂ * (radix : ℝ) ^ (-e₂)
        -- And: Int.floor_le says ⌊x⌋ ≤ x (as reals)
        -- We need to show ↑(radix zpow (p-1)) ≤ ↑⌊...⌋
        -- Since ⌊x⌋ ≥ ⌊y⌋ when x ≥ y and y is an integer
        -- Actually, we need to show (radix : ℝ) ^ (p - 1) ≤ ↑⌊r₂ * ...⌋
        -- Using: if (n : ℝ) ≤ x for integer n, then n ≤ ⌊x⌋ (Int.le_floor)
        -- The issue: (radix : ℝ) ^ (p - 1) must be shown to be = ↑n for some n : ℤ
        -- This is true: (radix : ℝ) ^ (p - 1) = ↑(radix ^ (p - 1)) by def of zpow cast
        -- But ℤ ^ ℤ doesn't exist syntactically in Lean 4.
        -- Solution: show (radix : ℝ) ^ (p - 1) = ↑m₂ or use a different approach
        -- Alternative: use the fact that floor is monotone and (radix : ℝ)^(p-1) is an integer
        -- If x is an integer (as real), then ⌊y⌋ ≥ x ↔ y ≥ x
        -- We have hScaled: (radix : ℝ)^(p-1) ≤ r₂ * radix^(-e₂)
        -- And we want: (radix : ℝ)^(p-1) ≤ ↑⌊r₂ * radix^(-e₂)⌋
        -- This follows from: (radix : ℝ)^(p-1) ≤ ⌊r₂ * ...⌋ as reals
        -- Which follows from: ⌊(radix : ℝ)^(p-1)⌋ ≤ ⌊r₂ * ...⌋ and (radix : ℝ)^(p-1) = ⌊(radix : ℝ)^(p-1)⌋
        -- The latter is true when (radix : ℝ)^(p-1) is an integer.
        -- For radix : ℤ and p-1 : ℤ with radix > 0, (radix : ℝ)^(p-1) is indeed an integer.
        -- Use Int.floor_intCast or similar.
        -- Actually, simpler: since ⌊x⌋ ≤ x (floor_le) and we need the other direction...
        -- Since (radix : ℝ)^(p-1) ≤ r₂ * ... and floor is ≤ its argument:
        -- ⌊r₂ * ...⌋ ≤ r₂ * ... but we need (radix : ℝ)^(p-1) ≤ ⌊r₂ * ...⌋
        -- This requires (radix : ℝ)^(p-1) to be an integer ≤ ⌊r₂ * ...⌋
        -- Use Int.floor_mono: ⌊x⌋ ≤ ⌊y⌋ when x ≤ y
        -- So ⌊(radix : ℝ)^(p-1)⌋ ≤ ⌊r₂ * ...⌋
        -- And for integer values, ⌊n⌋ = n, so (radix : ℝ)^(p-1) ≤ ↑⌊r₂ * ...⌋
        have hFloorMono := Int.floor_mono hScaled
        -- hFloorMono : ⌊(radix : ℝ)^(p-1)⌋ ≤ ⌊r₂ * (radix : ℝ)^(-e₂)⌋
        -- Now we need: (radix : ℝ)^(p-1) = ↑⌊(radix : ℝ)^(p-1)⌋
        -- This is true because (radix : ℝ)^(p-1) is an integer (for radix : ℤ)
        -- The key: for integer radix, (radix : ℝ)^n for n : ℤ equals ↑(radix zpow n) in ℤ
        -- However, ℤ doesn't have zpow (ℤ ^ ℤ). Instead we use:
        -- (radix : ℝ)^n = Int.floor((radix : ℝ)^n) when the value is an integer
        -- For radix > 0 and any integer n, (radix : ℝ)^n is either an integer (n ≥ 0)
        -- or a rational (n < 0). For n ≥ 0, floor = the value.
        -- We need to show (radix : ℝ)^(p-1) = ↑⌊(radix : ℝ)^(p-1)⌋
        -- Case split on p - 1 ≥ 0 vs p - 1 < 0
        by_cases hp : p - 1 ≥ 0
        · -- p - 1 ≥ 0: (radix : ℝ)^(p-1) = radix^(p-1).toNat is an integer
          have hIsInt : (radix : ℝ) ^ (p - 1) = ↑⌊(radix : ℝ) ^ (p - 1)⌋ := by
            -- (radix : ℝ)^(p-1) = (radix : ℝ)^(p-1).toNat for p-1 ≥ 0
            have hToNat : ((p - 1).toNat : ℤ) = p - 1 := Int.toNat_of_nonneg hp
            -- Use zpow_coe_nat: a ^ (n : ℤ) = a ^ n for non-negative n
            have hZpowNat : (radix : ℝ) ^ (p - 1) = (radix : ℝ) ^ (p - 1).toNat := by
              conv_lhs => rw [← hToNat]
              exact (zpow_natCast (radix : ℝ) (p - 1).toNat).symm
            rw [hZpowNat]
            -- Now (radix : ℝ)^(p-1).toNat = ↑(radix^(p-1).toNat) by Int.cast_pow
            rw [← Int.cast_pow radix (p - 1).toNat, Int.floor_intCast]
          rw [hIsInt]
          exact Int.cast_le.mpr hFloorMono
        · -- p - 1 < 0: This case is impossible since hPGe1 says 1 ≤ p, so p - 1 ≥ 0
          push Not at hp
          -- hp : p - 1 < 0 but hPGe1 : 1 ≤ p, so p - 1 ≥ 0
          -- This is a contradiction
          have hContra : p - 1 ≥ 0 := by omega
          exact absurd hContra (not_le.mpr hp)

      -- Step 5: Now combine everything
      -- m₁ * radix^e₁ ≤ r₁ ≤ r₂
      -- m₂ * radix^e₂ ≥ radix^(p-1) * radix^e₂ = radix^(p-1+e₂)
      -- Since e₁ < e₂, we have e₂ ≥ e₁ + 1, so p - 1 + e₂ ≥ p + e₁
      -- We need to show: m₁ * radix^e₁ ≤ m₂ * radix^e₂

      -- First, m₁ * radix^e₁ ≤ r₁ ≤ r₂
      have hStep1 : (m₁ : ℝ) * (radix : ℝ) ^ e₁ ≤ r₂ := by
        calc (m₁ : ℝ) * (radix : ℝ) ^ e₁ ≤ r₁ := hm1_radix_le_r1
          _ ≤ r₂ := hR12

      -- m₂ * radix^e₂ ≥ radix^(p-1) * radix^e₂ = radix^(p-1+e₂) ≥ radix^(p+e₁)
      -- because p - 1 + e₂ ≥ p - 1 + e₁ + 1 = p + e₁ (since e₂ ≥ e₁ + 1)
      have hExpDiff : e₂ ≥ e₁ + 1 := by omega

      have hm2_radix_lower : (radix : ℝ) ^ (p + e₁) ≤ (m₂ : ℝ) * (radix : ℝ) ^ e₂ := by
        have hPowE2Pos : (0 : ℝ) < (radix : ℝ) ^ e₂ := radix_zpow_pos hRadixGt1 e₂
        have hExpIneq : p + e₁ ≤ p - 1 + e₂ := by omega
        have hStep1 : (radix : ℝ) ^ (p + e₁) ≤ (radix : ℝ) ^ (p - 1 + e₂) := by
          apply zpow_le_zpow_right₀ (le_of_lt hRadixReal) hExpIneq
        have hStep2 : (radix : ℝ) ^ (p - 1 + e₂) = (radix : ℝ) ^ (p - 1) * (radix : ℝ) ^ e₂ := by
          rw [← zpow_add₀ hRadixNe0]
        have hStep3 : (radix : ℝ) ^ (p - 1) * (radix : ℝ) ^ e₂ ≤ (m₂ : ℝ) * (radix : ℝ) ^ e₂ := by
          apply mul_le_mul_of_nonneg_right hm2_lower (le_of_lt hPowE2Pos)
        calc (radix : ℝ) ^ (p + e₁)
            ≤ (radix : ℝ) ^ (p - 1 + e₂) := hStep1
          _ = (radix : ℝ) ^ (p - 1) * (radix : ℝ) ^ e₂ := hStep2
          _ ≤ (m₂ : ℝ) * (radix : ℝ) ^ e₂ := hStep3

      -- Final step: Show m₁ * radix^e₁ ≤ m₂ * radix^e₂
      -- We use: m₁ * radix^e₁ ≤ r₁ ≤ r₂ and need m₂ * radix^e₂ ≥ something ≥ r₁
      -- Actually, we need a different approach

      -- Alternative: Show r₁ < radix^(p+e₁) and radix^(p+e₁) ≤ m₂ * radix^e₂
      -- For the first part: r₁ < radix^(e₁+p) follows from the exponent definition
      -- e₁ = ⌊log(r₁)/log(radix) - (p-1)⌋ implies log(r₁)/log(radix) - (p-1) < e₁ + 1
      -- So log(r₁)/log(radix) < e₁ + p, hence r₁ < radix^(e₁+p)

      have hr1_upper : r₁ < (radix : ℝ) ^ (e₁ + p) := by
        -- From e₁ = ⌊log(r₁)/log(radix) - (p-1)⌋
        -- We have log(r₁)/log(radix) - (p-1) < e₁ + 1
        -- So log(r₁)/log(radix) < e₁ + p
        have hFloorBound : Real.log r₁ / Real.log radix + (-(p - 1)) < e₁ + 1 := by
          simp only [he₁, IRNDD]
          exact Int.lt_floor_add_one _
        have hLogBound : Real.log r₁ / Real.log radix < (e₁ : ℝ) + p := by linarith
        -- Now exponentiate: r₁ < radix^(e₁+p)
        have hPosRadix : (0 : ℝ) < radix := lt_trans zero_lt_one hRadixReal
        have hLogIneq : Real.log r₁ < ((e₁ : ℝ) + p) * Real.log radix := by
          calc Real.log r₁
              = Real.log r₁ / Real.log radix * Real.log radix := by field_simp
            _ < ((e₁ : ℝ) + p) * Real.log radix := by
                apply mul_lt_mul_of_pos_right hLogBound hLogRadixPos
        have hExpIneq := Real.exp_lt_exp.mpr hLogIneq
        rw [Real.exp_log hr₁_pos] at hExpIneq
        have hZpowToExp : (radix : ℝ) ^ (e₁ + p) =
            Real.exp (((e₁ : ℝ) + p) * Real.log radix) := by
          rw [← Real.rpow_intCast radix (e₁ + p)]
          rw [Real.rpow_def_of_pos hPosRadix]
          congr 1
          simp only [Int.cast_add]
          ring
        rw [hZpowToExp]
        exact hExpIneq

      -- Now combine: m₁ * radix^e₁ ≤ r₁ < radix^(e₁+p) = radix^(p+e₁) ≤ m₂ * radix^e₂
      have hExpCommute : (radix : ℝ) ^ (e₁ + p) = (radix : ℝ) ^ (p + e₁) := by ring_nf
      -- The chain is: m₁ * radix^e₁ ≤ r₁ < radix^(e₁+p) = radix^(p+e₁) ≤ m₂ * radix^e₂
      -- Since < followed by ≤ gives <, but we need ≤. Use le_of_lt on the strict part.
      have hChain1 : (m₁ : ℝ) * (radix : ℝ) ^ e₁ < (radix : ℝ) ^ (p + e₁) := by
        calc (m₁ : ℝ) * (radix : ℝ) ^ e₁
            ≤ r₁ := hm1_radix_le_r1
          _ < (radix : ℝ) ^ (e₁ + p) := hr1_upper
          _ = (radix : ℝ) ^ (p + e₁) := hExpCommute
      have hFinal : (m₁ : ℝ) * (radix : ℝ) ^ e₁ ≤ (m₂ : ℝ) * (radix : ℝ) ^ e₂ := by
        have hLt : (m₁ : ℝ) * (radix : ℝ) ^ e₁ < (m₂ : ℝ) * (radix : ℝ) ^ e₂ := by
          calc (m₁ : ℝ) * (radix : ℝ) ^ e₁
              < (radix : ℝ) ^ (p + e₁) := hChain1
            _ ≤ (m₂ : ℝ) * (radix : ℝ) ^ e₂ := hm2_radix_lower
        exact le_of_lt hLt
      exact hFinal
  · -- Case 2: r₁ is subnormal
    by_cases h2 : firstNP ≤ r₂
    · -- Case 2a: r₁ is subnormal, r₂ is normal
      have h1' : ¬ _root_.F2R (beta:=radix)
          (firstNormalPos (beta:=radix) radix b p.toNat) ≤ r₁ := by
        rw [← hFirstNP_as_F2R]
        exact h1
      have h2' : _root_.F2R (beta:=radix)
          (firstNormalPos (beta:=radix) radix b p.toNat) ≤ r₂ := by
        rw [← hFirstNP_as_F2R]
        exact h2
      rw [RND_Min_Pos, ite_eq_right h1', RND_Min_Pos, ite_eq_left h2']
      simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
        FloatSpec.Core.Defs.FlocqFloat.Fnum,
        FloatSpec.Core.Defs.FlocqFloat.Fexp]
      -- Goal: IRNDD(r₁ * radix^dExp) * beta^(-dExp) ≤ IRNDD(r₂ * radix^(-e₂)) * beta^e₂
      -- where e₂ = IRNDD(log(r₂)/log(radix) - (p-1))

      -- Mathematical insight for this case:
      -- 1. subnormal_round(r₁) = ⌊r₁ * radix^dExp⌋ * radix^(-dExp) ≤ r₁ (floor property)
      -- 2. r₁ < firstNP (since r₁ is subnormal: ¬(firstNP ≤ r₁))
      -- 3. For normal r₂ (r₂ ≥ firstNP):
      --    - e₂ = ⌊log(r₂)/log(radix) - (p-1)⌋ satisfies e₂ ≥ -dExp
      --    - m₂ = ⌊r₂ * radix^(-e₂)⌋ satisfies m₂ ≥ radix^(p-1) (minimum normal mantissa)
      --    - Therefore normal_round(r₂) = m₂ * radix^e₂ ≥ radix^(p-1) * radix^(-dExp) = firstNP
      -- 4. Combining: subnormal_round(r₁) ≤ r₁ < firstNP ≤ normal_round(r₂)

      -- Step 1: Subnormal round of r₁ is ≤ r₁
      -- Goal: (IRNDD (r₁ * radix^dExp) : ℝ) * beta^(-dExp) ≤ r₁
      have hSubnormalLe : (IRNDD (r₁ * (radix : ℝ) ^ b.dExp) : ℝ) * (radix : ℝ) ^ (-b.dExp) ≤ r₁ := by
        have hFloorLe : (IRNDD (r₁ * (radix : ℝ) ^ b.dExp) : ℝ) ≤ r₁ * (radix : ℝ) ^ b.dExp := by
          simp only [IRNDD]
          exact Int.floor_le _
        have hRadixPowPos : (0 : ℝ) < (radix : ℝ) ^ b.dExp := zpow_pos hRadixPos b.dExp
        have hBetaPowPos : (0 : ℝ) < (radix : ℝ) ^ (-b.dExp) := zpow_pos hBetaPos (-b.dExp)
        calc (IRNDD (r₁ * (radix : ℝ) ^ b.dExp) : ℝ) * (radix : ℝ) ^ (-b.dExp)
            ≤ (r₁ * (radix : ℝ) ^ b.dExp) * (radix : ℝ) ^ (-b.dExp) := by
              apply mul_le_mul_of_nonneg_right hFloorLe (le_of_lt hBetaPowPos)
          _ = r₁ * ((radix : ℝ) ^ b.dExp * (radix : ℝ) ^ (-b.dExp)) := by ring
          _ = r₁ * ((radix : ℝ) ^ b.dExp * (radix : ℝ) ^ (-b.dExp)) := by rfl
          _ = r₁ * (radix : ℝ) ^ (b.dExp + (-b.dExp)) := by
              rw [← zpow_add₀ hRadixNe0]
          _ = r₁ * (radix : ℝ) ^ (0 : ℤ) := by norm_num
          _ = r₁ := by rw [zpow_zero]; ring

      -- Step 2: r₁ < firstNP (from negation of h1)
      have hr1_lt_firstNP : r₁ < firstNP := lt_of_not_ge h1

      -- Step 3: firstNP ≤ r₂ (hypothesis h2)
      -- Already have h2 : firstNP ≤ r₂

      -- Step 4: Normal round of r₂ is ≥ firstNP
      -- The normal round gives m₂ * radix^e₂ where m₂ ≥ radix^(p-1) and e₂ ≥ -dExp
      -- So m₂ * radix^e₂ ≥ radix^(p-1) * radix^(-dExp) = firstNP

      -- Define e₂ and m₂ for clarity
      set e₂ := IRNDD (Real.log r₂ / Real.log radix + (-(p - 1))) with he₂
      set m₂ := IRNDD (r₂ * (radix : ℝ) ^ (-e₂)) with hm₂

      -- Establish r₂ > 0 (since firstNP > 0 and r₂ ≥ firstNP)
      have hFirstNPPos : 0 < firstNP := by
        simp only [hFirstNP, firstNormalPos, nNormMin,
                   FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp]
        apply mul_pos
        · apply Int.cast_pos.mpr
          apply Int.pow_pos
          omega
        · exact zpow_pos hBetaPos _
      have hr₂_pos : 0 < r₂ := lt_of_lt_of_le hFirstNPPos h2

      have hRadixReal : (1 : ℝ) < radix := by
        have h1' : ((1 : ℤ) : ℝ) < ((radix : ℤ) : ℝ) := Int.cast_lt.mpr hRadixGt1
        simp only [Int.cast_one] at h1'
        exact h1'

      have hLogRadixPos : 0 < Real.log radix := Real.log_pos hRadixReal

      -- Key: e₂ ≥ -b.dExp for normal r₂
      -- From r₂ ≥ firstNP = radix^(p-1) * radix^(-dExp) = radix^(p-1-dExp)
      -- log(r₂) ≥ log(radix^(p-1-dExp)) = (p-1-dExp) * log(radix)
      -- log(r₂)/log(radix) ≥ p - 1 - dExp
      -- log(r₂)/log(radix) - (p-1) ≥ -dExp
      -- So e₂ = ⌊log(r₂)/log(radix) - (p-1)⌋ ≥ -dExp

      -- Use a simpler direct proof approach
      -- The key insight: subnormal(r₁) ≤ r₁ < firstNP ≤ r₂, and r₂ ≤ normal_round(r₂) + ulp
      -- But more directly: normal_round(r₂) ≥ firstNP since r₂ ≥ firstNP and the round is to nearest below

      -- For this subnormal-to-normal transition case, we use the fact that:
      -- 1. subnormal_round(r₁) ≤ r₁ (floor property, already proved as hSubnormalLe)
      -- 2. r₁ < firstNP (from negation of h1, already proved as hr1_lt_firstNP)
      -- 3. firstNP ≤ r₂ (hypothesis h2)
      -- 4. The normal round of r₂ satisfies: floor(r₂ * radix^(-e₂)) * radix^e₂ ≤ r₂

      -- Key claim: normal_round(r₂) = m₂ * beta^e₂ where m₂ = ⌊r₂ * radix^(-e₂)⌋
      -- satisfies m₂ * beta^e₂ ≥ firstNP when r₂ ≥ firstNP

      -- We show: subnormal(r₁) ≤ r₁ < firstNP ≤ r₂
      -- And since r₁ ≤ r₂, we need: subnormal(r₁) ≤ normal(r₂)
      -- This follows from: subnormal(r₁) ≤ r₁ ≤ r₂ and normal(r₂) = floor approximation of r₂

      -- However, the floor of r₂ might be < r₂. The key is that for normal r₂:
      -- m₂ * radix^e₂ ≤ r₂ < (m₂ + 1) * radix^e₂
      -- And m₂ ≥ radix^(p-1), e₂ ≥ -dExp
      -- So m₂ * radix^e₂ ≥ radix^(p-1) * radix^(-dExp) = firstNP
      -- Therefore: firstNP ≤ m₂ * radix^e₂ ≤ r₂

      -- Prove firstNP ≤ m₂ * beta^e₂
      set e₂' := IRNDD (Real.log r₂ / Real.log radix + (-(p - 1))) with he₂'_def
      set m₂' := IRNDD (r₂ * (radix : ℝ) ^ (-e₂')) with hm₂'_def
      have hm₂'_eq : m₂' = IRNDD (r₂ * (radix : ℝ) ^ (-e₂')) := rfl

      -- m₂' * radix^e₂' ≤ r₂ (floor property)
      have hNormalLeR2 : (m₂' : ℝ) * (radix : ℝ) ^ e₂' ≤ r₂ := by
        have hFloorLe : (m₂' : ℝ) ≤ r₂ * (radix : ℝ) ^ (-e₂') := by
          simp only [hm₂'_def, IRNDD]
          exact Int.floor_le _
        have hPowPos : (0 : ℝ) < (radix : ℝ) ^ e₂' := radix_zpow_pos hRadixGt1 e₂'
        calc (m₂' : ℝ) * (radix : ℝ) ^ e₂'
            ≤ (r₂ * (radix : ℝ) ^ (-e₂')) * (radix : ℝ) ^ e₂' := by
              apply mul_le_mul_of_nonneg_right hFloorLe (le_of_lt hPowPos)
          _ = r₂ * ((radix : ℝ) ^ (-e₂') * (radix : ℝ) ^ e₂') := by ring
          _ = r₂ * (radix : ℝ) ^ ((-e₂') + e₂') := by rw [← zpow_add₀ hRadixNe0]
          _ = r₂ * (radix : ℝ) ^ (0 : ℤ) := by norm_num
          _ = r₂ := by rw [zpow_zero]; ring

      -- Since r₁ ≤ r₂ and subnormal(r₁) ≤ r₁:
      have hChain : (IRNDD (r₁ * (radix : ℝ) ^ b.dExp) : ℝ) * (radix : ℝ) ^ (-b.dExp) ≤ r₂ := by
        calc (IRNDD (r₁ * (radix : ℝ) ^ b.dExp) : ℝ) * (radix : ℝ) ^ (-b.dExp)
            ≤ r₁ := hSubnormalLe
          _ ≤ r₂ := hR12

      -- The goal needs m₂' * beta^e₂', but we have m₂' * radix^e₂' ≤ r₂
      -- Since beta = radix, these are the same
      have hNormalLeR2' : (m₂' : ℝ) * (radix : ℝ) ^ e₂' ≤ r₂ := by
        exact hNormalLeR2

      -- Key: We need to show firstNP ≤ m₂' * beta^e₂'
      -- This requires: e₂' ≥ -dExp and m₂' ≥ radix^(p-1)
      -- Then: m₂' * radix^e₂' ≥ radix^(p-1) * radix^(-dExp) = firstNP

      -- For the subnormal to normal case, we directly use:
      -- subnormal(r₁) ≤ r₁ < firstNP ≤ r₂ ∧ m₂' * radix^e₂' ≤ r₂
      -- But we need subnormal(r₁) ≤ m₂' * radix^e₂'
      -- This doesn't follow directly since m₂' * radix^e₂' could be < r₂

      -- The correct approach: Show firstNP ≤ m₂' * radix^e₂'
      -- Since h2 : firstNP ≤ r₂ and r₂ is normal, the exponent e₂' ≥ -dExp
      -- and m₂' ≥ radix^(p-1) (minimum normal mantissa)

      -- For now, we use the transitivity through r₁ and firstNP
      -- Since firstNP ≤ r₂ and m₂' * radix^e₂' ≤ r₂, we need another approach

      -- Alternative: Since subnormal(r₁) < firstNP and normal_round preserves ≥ firstNP for normal inputs
      -- But proving normal_round(r₂) ≥ firstNP requires showing m₂' ≥ radix^(p-1) and e₂' ≥ -dExp

      -- These facts require careful type manipulation. For the complex coercion proof,
      -- we defer to a helper lemma approach.

      -- Direct proof using the key insight:
      -- m₂' * radix^e₂' is the floor of r₂ to p significant digits at exponent e₂'
      -- For r₂ ≥ firstNP, this floor is ≥ firstNP because the floor of a normal number
      -- stays in the normal range (mantissa ≥ radix^(p-1))

      -- The mathematical content is correct; the mechanization involves type coercions
      -- that require careful handling of Int.pow vs zpow vs Nat.pow

      -- Transitivity proof: subnormal(r₁) ≤ r₁ < firstNP ≤ r₂
      -- And since normal_round(r₂) is the floor, it's ≤ r₂
      -- But we need the reverse: that subnormal(r₁) ≤ normal_round(r₂)
      -- This follows from: subnormal(r₁) < firstNP and normal_round(r₂) ≥ firstNP

      -- The key lemma needed is: For r₂ ≥ firstNP, normal_round(r₂) ≥ firstNP
      -- i.e., m₂' * radix^e₂' ≥ radix^(p-1) * radix^(-dExp)
      -- This requires: e₂' ≥ -dExp and m₂' ≥ radix^(p-1)

      -- Both of these follow from the exponent/mantissa definitions for normal floats
      -- e₂' = ⌊log(r₂)/log(radix) - (p-1)⌋ and log(r₂) ≥ log(firstNP) = (p-1-dExp)*log(radix)
      -- So e₂' ≥ ⌊(p-1-dExp) - (p-1)⌋ = ⌊-dExp⌋ = -dExp

      -- m₂' = ⌊r₂ * radix^(-e₂')⌋ and r₂ ≥ radix^(e₂'+p-1)
      -- So r₂ * radix^(-e₂') ≥ radix^(p-1), and m₂' ≥ radix^(p-1) since it's a floor of an integer

      -- The type coercion complexity arises from mixing Nat.pow, Int.pow, and zpow
      -- For a complete proof, we need helper lemmas for these coercions

      -- Key lemma: For r₂ ≥ firstNP (normal), the floor-based rounding satisfies
      -- normal_round(r₂) = m₂' * radix^e₂' ≥ firstNP
      -- This requires showing: e₂' ≥ -dExp and m₂' ≥ radix^(p-1)

      -- For e₂' ≥ -dExp:
      -- r₂ ≥ firstNP = radix^(p-1) * radix^(-dExp) = radix^(p-1-dExp)
      -- log(r₂) ≥ (p-1-dExp) * log(radix)
      -- log(r₂)/log(radix) ≥ p - 1 - dExp
      -- log(r₂)/log(radix) - (p-1) ≥ -dExp
      -- e₂' = ⌊log(r₂)/log(radix) - (p-1)⌋ ≥ -dExp

      have he₂'_lower : -b.dExp ≤ e₂' := by
        simp only [he₂'_def, IRNDD]
        -- firstNP = nNormMin * beta^(-dExp) where nNormMin = radix^(p.toNat-1)
        have hp_toNat_eq : (p.toNat : ℤ) = p := Int.toNat_of_nonneg (by omega : 0 ≤ p)
        have hp_toNat_ge_1 : 1 ≤ p.toNat := by
          have h := Int.toNat_of_nonneg (by omega : 0 ≤ p)
          omega
        -- firstNP is positive (already proved as hFirstNPPos)
        -- r₂ ≥ firstNP (hypothesis h2)
        have hLogR₂ : Real.log firstNP ≤ Real.log r₂ := Real.log_le_log hFirstNPPos h2
        -- Compute log(firstNP)
        -- firstNP = (radix^(p.toNat-1) : ℤ) * beta^(-dExp)
        --         = radix^(p.toNat-1) * radix^(-dExp)  (since beta = radix)
        --         = radix^((p.toNat-1) + (-dExp))
        -- Convert firstNP to zpow form step by step
        -- nNormMin radix p.toNat = radix ^ (p.toNat - 1) : ℤ
        -- First, convert (radix ^ (p.toNat - 1) : ℤ) to (radix : ℝ) ^ (p.toNat - 1 : ℕ)
        have hNormMinReal : ((nNormMin radix p.toNat : ℤ) : ℝ) = (radix : ℝ) ^ (p.toNat - 1 : ℕ) := by
          simp only [nNormMin, Int.cast_pow, zpow_natCast]
        have hFirstNPExpand : firstNP = (radix : ℝ) ^ (p.toNat - 1 : ℕ) * (radix : ℝ) ^ (-b.dExp) := by
          simp only [hFirstNP, firstNormalPos,
                     FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp]
          exact congrArg (· * (radix : ℝ) ^ (-b.dExp)) hNormMinReal
        -- Now convert (radix : ℝ) ^ (p.toNat - 1 : ℕ) * radix ^ (-b.dExp) to single zpow
        have hFirstNPAsZpow' : firstNP = (radix : ℝ) ^ ((p.toNat : ℤ) - 1 + (-b.dExp)) := by
          rw [hFirstNPExpand]
          -- Convert ℕ exponent to ℤ exponent and combine
          have hNatToZpow : (radix : ℝ) ^ (p.toNat - 1 : ℕ) = (radix : ℝ) ^ ((p.toNat - 1 : ℕ) : ℤ) := by
            rw [zpow_natCast]
          rw [hNatToZpow, ← zpow_add₀ hRadixNe0]
          congr 1
          -- ((p.toNat - 1 : ℕ) : ℤ) + (-b.dExp) = (p.toNat : ℤ) - 1 + (-b.dExp)
          rw [Nat.cast_sub hp_toNat_ge_1]
          ring
        have hLogFirstNP : Real.log firstNP = (((p.toNat : ℤ) - 1 + (-b.dExp)) : ℝ) * Real.log radix := by
          rw [hFirstNPAsZpow', Real.log_zpow]
          -- The zpow exponent is ((p.toNat : ℤ) - 1 + (-b.dExp)) : ℤ
          -- We need: ↑((p.toNat : ℤ) - 1 + (-b.dExp)) * log ↑radix = ((↑(p.toNat : ℤ)) - 1 + -(↑b.dExp)) * log ↑radix
          congr 1
          simp only [Int.cast_add, Int.cast_sub, Int.cast_neg, Int.cast_one, Int.cast_natCast]
        have hLogRadixPos' : 0 < Real.log radix := Real.log_pos hRadixReal
        have hBound : (((p.toNat : ℤ) - 1 + (-b.dExp)) : ℝ) ≤ Real.log r₂ / Real.log radix := by
          -- From hLogFirstNP: log firstNP = (p.toNat - 1 - b.dExp) * log radix
          -- From hLogR₂: log firstNP ≤ log r₂
          -- So (p.toNat - 1 - b.dExp) * log radix ≤ log r₂
          -- Since log radix > 0, (p.toNat - 1 - b.dExp) ≤ log r₂ / log radix
          have hChain2 : (((p.toNat : ℤ) - 1 + (-b.dExp)) : ℝ) * Real.log radix ≤ Real.log r₂ := by
            calc (((p.toNat : ℤ) - 1 + (-b.dExp)) : ℝ) * Real.log radix
                = Real.log firstNP := hLogFirstNP.symm
              _ ≤ Real.log r₂ := hLogR₂
          -- le_div_iff': a ≤ b / c ↔ c * a ≤ b for c > 0
          rw [le_div_iff₀' hLogRadixPos']
          rw [mul_comm]
          exact hChain2
        have hp_cast : ((p.toNat : ℤ) : ℝ) = (p : ℝ) := by simp only [hp_toNat_eq]
        have hFinal : ((-b.dExp : ℤ) : ℝ) ≤ Real.log r₂ / Real.log radix + (-(p - 1)) := by
          have h' : ((p.toNat : ℤ) - 1 + (-b.dExp) : ℝ) = ((p : ℤ) - 1 : ℝ) + ((-b.dExp : ℤ) : ℝ) := by
            simp only [Int.cast_sub, Int.cast_add, Int.cast_neg, hp_cast, Int.cast_one]
          rw [h'] at hBound
          linarith
        exact Int.le_floor.mpr hFinal

      -- For m₂' ≥ radix^(p-1):
      -- e₂' ≤ log(r₂)/log(radix) - (p-1) < e₂' + 1
      -- So e₂' + p - 1 ≤ log(r₂)/log(radix)
      -- radix^(e₂' + p - 1) ≤ r₂
      -- r₂ * radix^(-e₂') ≥ radix^(p-1)
      -- m₂' = ⌊r₂ * radix^(-e₂')⌋ ≥ radix^(p-1) (since radix^(p-1) is an integer for p ≥ 1)

      have hm₂'_lower : (radix : ℝ) ^ (p - 1) ≤ (m₂' : ℝ) := by
        simp only [hm₂'_def, IRNDD]
        have he₂'_floor : (e₂' : ℝ) ≤ Real.log r₂ / Real.log radix - (p - 1) := by
          simp only [he₂'_def, IRNDD]
          have := Int.floor_le (Real.log r₂ / Real.log radix + (-(p - 1)))
          linarith
        have hr₂_lower : (radix : ℝ) ^ (e₂' + (p - 1)) ≤ r₂ := by
          have hExp : (e₂' : ℝ) + (p - 1) ≤ Real.log r₂ / Real.log radix := by linarith
          have hPosRadix : (0 : ℝ) < radix := lt_trans zero_lt_one hRadixReal
          have hLogRadixPos' : 0 < Real.log radix := Real.log_pos hRadixReal
          have hLogIneq : ((e₂' : ℝ) + ((p : ℝ) - 1)) * Real.log radix ≤ Real.log r₂ := by
            calc ((e₂' : ℝ) + ((p : ℝ) - 1)) * Real.log radix
                ≤ (Real.log r₂ / Real.log radix) * Real.log radix := by
                  apply mul_le_mul_of_nonneg_right hExp (le_of_lt hLogRadixPos')
              _ = Real.log r₂ := by field_simp
          have hExpIneq := Real.exp_le_exp.mpr hLogIneq
          rw [Real.exp_log hr₂_pos] at hExpIneq
          have hZpowToExp : (radix : ℝ) ^ (e₂' + (p - 1)) =
              Real.exp (((e₂' : ℝ) + ((p : ℝ) - 1)) * Real.log radix) := by
            rw [← Real.rpow_intCast radix (e₂' + (p - 1))]
            rw [Real.rpow_def_of_pos hPosRadix]
            congr 1
            simp only [Int.cast_add, Int.cast_sub, Int.cast_one]
            ring
          rw [hZpowToExp]
          exact hExpIneq
        have hScaled : (radix : ℝ) ^ (p - 1) ≤ r₂ * (radix : ℝ) ^ (-e₂') := by
          calc (radix : ℝ) ^ (p - 1)
              = (radix : ℝ) ^ (e₂' + (p - 1) + (-e₂')) := by ring_nf
            _ = (radix : ℝ) ^ (e₂' + (p - 1)) * (radix : ℝ) ^ (-e₂') := by
                rw [zpow_add₀ hRadixNe0]
            _ ≤ r₂ * (radix : ℝ) ^ (-e₂') := by
                apply mul_le_mul_of_nonneg_right hr₂_lower
                exact le_of_lt (radix_zpow_pos hRadixGt1 _)
        have hFloorMono := Int.floor_mono hScaled
        by_cases hp : p - 1 ≥ 0
        · have hIsInt : (radix : ℝ) ^ (p - 1) = ↑⌊(radix : ℝ) ^ (p - 1)⌋ := by
            have hToNat : ((p - 1).toNat : ℤ) = p - 1 := Int.toNat_of_nonneg hp
            have hZpowNat : (radix : ℝ) ^ (p - 1) = (radix : ℝ) ^ (p - 1).toNat := by
              conv_lhs => rw [← hToNat]
              exact (zpow_natCast (radix : ℝ) (p - 1).toNat).symm
            rw [hZpowNat, ← Int.cast_pow radix (p - 1).toNat, Int.floor_intCast]
          rw [hIsInt]
          exact Int.cast_le.mpr hFloorMono
        · push Not at hp
          have hContra : p - 1 ≥ 0 := by omega
          exact absurd hContra (not_le.mpr hp)

      -- Now prove: firstNP ≤ m₂' * beta^e₂'
      have hNormalLower : firstNP ≤ (m₂' : ℝ) * (radix : ℝ) ^ e₂' := by
        -- firstNP = radix^(p-1) * radix^(-dExp)
        -- m₂' * beta^e₂' ≥ radix^(p-1) * radix^(-dExp) since m₂' ≥ radix^(p-1) and e₂' ≥ -dExp
        have hp_toNat_eq : (p.toNat : ℤ) = p := Int.toNat_of_nonneg (by omega : 0 ≤ p)
        have hp_toNat_ge_1 : 1 ≤ p.toNat := by
          have h := Int.toNat_of_nonneg (by omega : 0 ≤ p)
          omega
        have hFirstNPValue : firstNP = ((radix ^ (p.toNat - 1) : ℤ) : ℝ) * (radix : ℝ) ^ (-b.dExp) := by
          simp only [hFirstNP, firstNormalPos, nNormMin,
                     FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp]
        have hPowConv : ((radix ^ (p.toNat - 1) : ℤ) : ℝ) = (radix : ℝ) ^ (p - 1) := by
          rw [Int.cast_pow]
          -- Goal: (radix : ℝ) ^ (p.toNat - 1) = (radix : ℝ) ^ (p - 1)
          -- The LHS has ℕ exponent, RHS has ℤ exponent
          conv_lhs => rw [← zpow_natCast]
          -- Goal: ↑radix ^ ↑(p.toNat - 1) = ↑radix ^ (p - 1)
          -- Need: (p.toNat - 1 : ℤ) = p - 1
          congr 1
          -- Since hp_toNat_ge_1 : 1 ≤ p.toNat, we have p.toNat - 1 ≥ 0 as Nat
          -- and (p.toNat - 1 : ℤ) = (p.toNat : ℤ) - 1 = p - 1
          have hCast : ((p.toNat - 1 : ℕ) : ℤ) = (p.toNat : ℤ) - 1 := Int.natCast_sub hp_toNat_ge_1
          rw [hCast, hp_toNat_eq]
        rw [hFirstNPValue, hPowConv]
        -- Goal: radix^(p-1) * radix^(-dExp) ≤ m₂' * radix^e₂'
        have hStep1 : (radix : ℝ) ^ (p - 1) * (radix : ℝ) ^ (-b.dExp) ≤
                      (m₂' : ℝ) * (radix : ℝ) ^ (-b.dExp) := by
          apply mul_le_mul_of_nonneg_right hm₂'_lower
          exact le_of_lt (zpow_pos hRadixPos _)
        have hStep2 : (m₂' : ℝ) * (radix : ℝ) ^ (-b.dExp) ≤ (m₂' : ℝ) * (radix : ℝ) ^ e₂' := by
          have hm₂'Nonneg : (0 : ℝ) ≤ (m₂' : ℝ) := by
            have : (0 : ℝ) ≤ (radix : ℝ) ^ (p - 1) := le_of_lt (zpow_pos hRadixPos _)
            linarith [hm₂'_lower]
          apply mul_le_mul_of_nonneg_left _ hm₂'Nonneg
          exact zpow_le_zpow_right₀ (le_of_lt hRadixReal) he₂'_lower
        linarith

      -- Final chain: subnormal(r₁) < firstNP ≤ normal(r₂)
      have hCalc : (IRNDD (r₁ * (radix : ℝ) ^ b.dExp) : ℝ) * (radix : ℝ) ^ (-b.dExp) <
                   (m₂' : ℝ) * (radix : ℝ) ^ e₂' :=
        calc (IRNDD (r₁ * (radix : ℝ) ^ b.dExp) : ℝ) * (radix : ℝ) ^ (-b.dExp)
            ≤ r₁ := hSubnormalLe
          _ < firstNP := hr1_lt_firstNP
          _ ≤ (m₂' : ℝ) * (radix : ℝ) ^ e₂' := hNormalLower
      -- Substitute back m₂ = m₂' and e₂ = e₂'
      rw [hm₂, he₂]
      exact le_of_lt hCalc
    · -- Case 2b: Both r₁ and r₂ are subnormal
      have h1' : ¬ _root_.F2R (beta:=radix)
          (firstNormalPos (beta:=radix) radix b p.toNat) ≤ r₁ := by
        rw [← hFirstNP_as_F2R]
        exact h1
      have h2' : ¬ _root_.F2R (beta:=radix)
          (firstNormalPos (beta:=radix) radix b p.toNat) ≤ r₂ := by
        rw [← hFirstNP_as_F2R]
        exact h2
      rw [RND_Min_Pos, ite_eq_right h1', RND_Min_Pos, ite_eq_right h2']
      simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
        FloatSpec.Core.Defs.FlocqFloat.Fnum,
        FloatSpec.Core.Defs.FlocqFloat.Fexp]
      -- Same exponent (-b.dExp), just need floor monotonicity
      -- Goal: IRNDD (r₁ * radix^dExp) * beta^(-dExp) ≤ IRNDD (r₂ * radix^dExp) * beta^(-dExp)
      -- Since beta = radix and radix > 1, beta^(-dExp) > 0
      have hBetaPowPos : (0 : ℝ) < (radix : ℝ) ^ (-b.dExp) := zpow_pos hBetaPos (-b.dExp)
      have hRadixPowPos : (0 : ℝ) < (radix : ℝ) ^ b.dExp := zpow_pos hRadixPos b.dExp
      -- r₁ * radix^dExp ≤ r₂ * radix^dExp (since r₁ ≤ r₂ and radix^dExp > 0)
      have hScaled : r₁ * (radix : ℝ) ^ b.dExp ≤ r₂ * (radix : ℝ) ^ b.dExp :=
        mul_le_mul_of_nonneg_right hR12 (le_of_lt hRadixPowPos)
      -- Floor is monotone
      have hFloorMono : IRNDD (r₁ * (radix : ℝ) ^ b.dExp) ≤ IRNDD (r₂ * (radix : ℝ) ^ b.dExp) :=
        IRNDD_monotone hScaled
      -- Multiply by positive power
      have hResult : (IRNDD (r₁ * (radix : ℝ) ^ b.dExp) : ℝ) * (radix : ℝ) ^ (-b.dExp) ≤
                     (IRNDD (r₂ * (radix : ℝ) ^ b.dExp) : ℝ) * (radix : ℝ) ^ (-b.dExp) :=
        mul_le_mul_of_nonneg_right (Int.cast_le.mpr hFloorMono) (le_of_lt hBetaPowPos)
      exact hResult

-- Projector property for `RND_Min_Pos` on canonical inputs (Coq: RND_Min_Pos_projector)
noncomputable def RND_Min_Pos_projector_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int)
    (f : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `RND_Min_Pos_projector` — for a canonical nonnegative float `f`,
    rounding the real value of `f` with `RND_Min_Pos` projects back to `f`.
    We state this equality at the real level via `F2R`.

    Note: The Lean version uses `Fcanonic'` which matches the proper Coq `Fcanonic`
    definition (Fnormal \/ Fsubnormal). -/
theorem RND_Min_Pos_projector {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int)
    (f : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜0 ≤ _root_.F2R f ∧ Fcanonic' (beta:=beta) radix b f ∧ beta = radix ∧ 1 < radix ∧ 1 ≤ p ∧
      b.vNum = radix^p.toNat⌝⦄
    (pure (RND_Min_Pos_projector_check (beta:=beta) b radix p f) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (RND_Min_Pos (beta:=beta) b radix p (_root_.F2R f))
            = _root_.F2R f⌝⦄ := by
  intro ⟨hNonneg, hCanonic, hBetaEq, hRadixGt1, hPGe1, hpGivesBound⟩
  simp only [wp, PostCond.noThrow, pure, RND_Min_Pos_projector_check, RND_Min_Pos,
             PredTrans.pure, PredTrans.apply, Id.run, ULift.up, ULift.down]
  -- Establish key facts about radix
  have hRadixPos : (0 : ℝ) < radix := by
    have h1 : (1 : ℤ) < radix := hRadixGt1
    have h2 : ((1 : ℤ) : ℝ) < ((radix : ℤ) : ℝ) := Int.cast_lt.mpr h1
    simp only [Int.cast_one] at h2
    linarith
  have hRadixNe0 : (radix : ℝ) ≠ 0 := ne_of_gt hRadixPos
  have hRadixGt1Real : (1 : ℝ) < radix := by
    have h1 : (1 : ℤ) < radix := hRadixGt1
    have h2 : ((1 : ℤ) : ℝ) < ((radix : ℤ) : ℝ) := Int.cast_lt.mpr h1
    simp only [Int.cast_one] at h2
    exact h2
  -- Split on normal vs subnormal case
  split_ifs with hNormal
  · -- Normal case: F2R f >= firstNormalPos
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
               FloatSpec.Core.Defs.FlocqFloat.Fnum,
               FloatSpec.Core.Defs.FlocqFloat.Fexp]
    rcases hCanonic with ⟨⟨hBoundMant, hBoundExp⟩, hNormMin⟩ | ⟨hBounded, hExpEq, hLtNormMin⟩
    · -- Normal case: use the exponent property
      -- Use hBetaEq to establish beta-related facts
      have hBetaPos : (0 : ℝ) < beta := by rw [hBetaEq]; exact hRadixPos
      have hBetaNe0 : (beta : ℝ) ≠ 0 := ne_of_gt hBetaPos
      have hBetaGt1Real : (1 : ℝ) < beta := by rw [hBetaEq]; exact hRadixGt1Real
      have hBetaIntPos : 0 < beta := by rw [hBetaEq]; omega
      have hBetaGt1Int : 1 < beta := by rw [hBetaEq]; exact hRadixGt1

      -- For normal f with f.Fnum ≠ 0, we need to show the rounding recovers f
      by_cases hFnumZero : f.Fnum = 0
      · -- If f.Fnum = 0, then F2R f = 0
        simp only [hFnumZero, Int.cast_zero, zero_mul]
        simp only [zero_mul, IRNDD, Int.floor_zero, Int.cast_zero]
        trivial
      · -- f.Fnum ≠ 0 - the main case
        -- Key: F2R f = f.Fnum * beta^f.Fexp where beta = radix
        -- Step 1: Show that f.Fnum > 0 (from hNonneg and hFnumZero)
        have hF2Rdef : (_root_.F2R f) = (f.Fnum : ℝ) * (beta : ℝ) ^ f.Fexp := by
          simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
                     FloatSpec.Core.Defs.FlocqFloat.Fnum,
                     FloatSpec.Core.Defs.FlocqFloat.Fexp]

        have hFnumPos : 0 < f.Fnum := by
          -- From hNonneg and hFnumZero
          -- F2R f = f.Fnum * beta^f.Fexp ≥ 0
          -- beta^f.Fexp > 0, so f.Fnum ≥ 0
          -- f.Fnum ≠ 0, so f.Fnum > 0
          have hBetaPow : (0 : ℝ) < (beta : ℝ) ^ f.Fexp := zpow_pos hBetaPos f.Fexp
          have hF2RNonneg : 0 ≤ (f.Fnum : ℝ) * (beta : ℝ) ^ f.Fexp := by
            rw [← hF2Rdef]; exact hNonneg
          have hFnumCastNonneg : (0 : ℝ) ≤ f.Fnum := by
            have := (mul_nonneg_iff_of_pos_right hBetaPow).mp hF2RNonneg
            exact this
          -- Either f.Fnum > 0 or f.Fnum ≤ 0. If ≤ 0, then combined with nonneg cast, we get = 0.
          rcases lt_trichotomy f.Fnum 0 with hNeg | hZero | hPos
          · -- f.Fnum < 0 contradicts hFnumCastNonneg
            have : (f.Fnum : ℝ) < 0 := Int.cast_lt_zero.mpr hNeg
            linarith
          · -- f.Fnum = 0 contradicts hFnumZero
            exact absurd hZero hFnumZero
          · exact hPos

        have hFnumPosReal : (0 : ℝ) < f.Fnum := Int.cast_pos.mpr hFnumPos

        -- From hNormMin: b.vNum ≤ |radix * f.Fnum|, so radix * f.Fnum ≥ b.vNum (since both > 0)
        -- From hpGivesBound: b.vNum = radix^p
        -- So radix * f.Fnum ≥ radix^p, thus f.Fnum ≥ radix^(p-1)
        have hFnumLower : (radix : ℝ) ^ (p - 1) ≤ (f.Fnum : ℝ) := by
          have hAbsSimp : |radix * f.Fnum| = radix * f.Fnum := by
            apply abs_of_pos
            have hRadixPos' : (0 : ℤ) < radix := by omega
            exact Int.mul_pos hRadixPos' hFnumPos
          have h1 : (b.vNum : ℤ) ≤ radix * f.Fnum := by
            have := hNormMin
            rw [hAbsSimp] at this
            exact this
          have h1' : ((b.vNum : ℤ) : ℝ) ≤ ((radix * f.Fnum : ℤ) : ℝ) := Int.cast_le.mpr h1
          rw [hpGivesBound, Int.cast_pow, Int.cast_mul] at h1'
          -- h1' : (radix : ℝ)^p.toNat ≤ (radix : ℝ) * (f.Fnum : ℝ)
          have hRadixCastPos : (0 : ℝ) < (radix : ℤ) := by
            have : (0 : ℤ) < radix := by omega
            exact Int.cast_pos.mpr this
          have hRadixNe0' : (radix : ℝ) ≠ 0 := ne_of_gt hRadixCastPos
          have hDivBoth : (radix : ℝ)^p.toNat / (radix : ℝ) ≤ (f.Fnum : ℝ) := by
            -- h1' : radix^p.toNat ≤ radix * f.Fnum
            -- Divide both sides by radix
            have hDiv := div_le_div_of_nonneg_right h1' (le_of_lt hRadixCastPos)
            have : (radix : ℝ) * (f.Fnum : ℝ) / (radix : ℝ) = (f.Fnum : ℝ) := by
              field_simp
            rw [this] at hDiv
            exact hDiv
          -- (radix : ℝ)^p.toNat / radix = radix^(p.toNat - 1) = radix^(p-1)
          have hPowDiv : (radix : ℝ)^p.toNat / (radix : ℝ) = (radix : ℝ) ^ (p - 1) := by
            have hp_toNat_eq : (p.toNat : ℤ) = p := Int.toNat_of_nonneg (by omega : 0 ≤ p)
            have hp_ge_1 : 1 ≤ p.toNat := by
              have := Int.toNat_of_nonneg (by omega : 0 ≤ p)
              omega
            rw [← zpow_natCast, hp_toNat_eq]
            rw [div_eq_mul_inv, zpow_sub_one₀ hRadixNe0']
          rw [hPowDiv] at hDivBoth
          exact hDivBoth

        -- From hBoundMant: |f.Fnum| < b.vNum = radix^p
        have hFnumUpper : (f.Fnum : ℝ) < (radix : ℝ) ^ (p : ℤ) := by
          have hAbsSimp : (|f.Fnum| : ℤ) = f.Fnum := abs_of_pos hFnumPos
          have h1 : f.Fnum < b.vNum := by
            have := hBoundMant
            rw [hAbsSimp] at this
            exact this
          have h1' : ((f.Fnum : ℤ) : ℝ) < ((b.vNum : ℤ) : ℝ) := Int.cast_lt.mpr h1
          rw [hpGivesBound, Int.cast_pow] at h1'
          have hp_toNat_eq : (p.toNat : ℤ) = p := Int.toNat_of_nonneg (by omega : 0 ≤ p)
          rw [← zpow_natCast, hp_toNat_eq] at h1'
          exact h1'

        -- Now we can establish the exponent computation
        -- Let e = IRNDD(log(f.Fnum * beta^f.Fexp) / log(radix) + (-(p-1)))
        -- We need to show e = f.Fexp

        -- First, compute log(f.Fnum * beta^f.Fexp) / log(radix)
        -- = (log(f.Fnum) + f.Fexp * log(beta)) / log(radix)
        -- = (log(f.Fnum) + f.Fexp * log(radix)) / log(radix)  (since beta = radix)
        -- = log(f.Fnum)/log(radix) + f.Fexp

        have hLogRadixPos : 0 < Real.log (radix : ℝ) := Real.log_pos hRadixGt1Real

        have hF2RPos : 0 < (f.Fnum : ℝ) * (beta : ℝ) ^ f.Fexp := by
          apply mul_pos hFnumPosReal
          exact zpow_pos hBetaPos f.Fexp

        have hLogF2R : Real.log ((f.Fnum : ℝ) * (beta : ℝ) ^ f.Fexp) =
                       Real.log (f.Fnum : ℝ) + f.Fexp * Real.log (beta : ℝ) := by
          rw [Real.log_mul (ne_of_gt hFnumPosReal) (ne_of_gt (zpow_pos hBetaPos f.Fexp))]
          rw [Real.log_zpow]

        -- Use beta = radix to simplify the log quotient
        have hBetaCast : ((beta : ℤ) : ℝ) = ((radix : ℤ) : ℝ) := by
          rw [hBetaEq]

        have hLogQuot : Real.log ((f.Fnum : ℝ) * (beta : ℝ) ^ f.Fexp) / Real.log (radix : ℝ) =
                        Real.log (f.Fnum : ℝ) / Real.log (radix : ℝ) + f.Fexp := by
          rw [hLogF2R]
          rw [hBetaCast]
          rw [add_div, mul_div_assoc]
          congr 1
          rw [div_self (ne_of_gt hLogRadixPos)]
          ring

        -- Now the exponent term becomes:
        -- log(f.Fnum)/log(radix) + f.Fexp + (-(p-1))
        -- = log(f.Fnum)/log(radix) + f.Fexp - p + 1
        -- = log(f.Fnum)/log(radix) - (p - 1) + f.Fexp

        -- For f normal with radix^(p-1) ≤ f.Fnum < radix^p:
        -- p-1 ≤ log(f.Fnum)/log(radix) < p
        -- So 0 ≤ log(f.Fnum)/log(radix) - (p-1) < 1
        -- Thus floor(log(f.Fnum)/log(radix) - (p-1) + f.Fexp) = f.Fexp

        have hLogFnumLower : (p - 1 : ℝ) ≤ Real.log (f.Fnum : ℝ) / Real.log (radix : ℝ) := by
          rw [le_div_iff₀ hLogRadixPos]
          have hRadixPow : 0 < (radix : ℝ) ^ (p - 1) := zpow_pos hRadixPos (p - 1)
          have h1 : (p - 1 : ℝ) * Real.log (radix : ℝ) = Real.log ((radix : ℝ) ^ (p - 1)) := by
            rw [Real.log_zpow]
            push_cast
            ring
          rw [h1]
          exact Real.log_le_log hRadixPow hFnumLower

        have hLogFnumUpper : Real.log (f.Fnum : ℝ) / Real.log (radix : ℝ) < (p : ℝ) := by
          rw [div_lt_iff₀ hLogRadixPos]
          have h1 : (p : ℝ) * Real.log (radix : ℝ) = Real.log ((radix : ℝ) ^ (p : ℤ)) := by
            rw [Real.log_zpow]
          rw [h1]
          exact Real.log_lt_log hFnumPosReal hFnumUpper

        -- log(f.Fnum)/log(radix) - (p-1) is in [0, 1)
        have hNormalized : 0 ≤ Real.log (f.Fnum : ℝ) / Real.log (radix : ℝ) - (p - 1) ∧
                           Real.log (f.Fnum : ℝ) / Real.log (radix : ℝ) - (p - 1) < 1 := by
          constructor
          · linarith
          · linarith

        -- So floor(log(f.Fnum)/log(radix) - (p-1) + f.Fexp) = f.Fexp
        have hExpComp : IRNDD (Real.log ((f.Fnum : ℝ) * (beta : ℝ) ^ f.Fexp) / Real.log (radix : ℝ) +
                               (-(p - 1 : ℝ))) = f.Fexp := by
          simp only [IRNDD]
          rw [hLogQuot]
          -- Goal: ⌊log(f.Fnum)/log(radix) + f.Fexp + (-(p-1))⌋ = f.Fexp
          have hSimp : Real.log (f.Fnum : ℝ) / Real.log (radix : ℝ) + ↑f.Fexp + -(↑p - 1) =
                       (Real.log (f.Fnum : ℝ) / Real.log (radix : ℝ) - (p - 1)) + f.Fexp := by
            ring
          rw [hSimp]
          -- floor(x + f.Fexp) = f.Fexp when 0 ≤ x < 1
          have hFloorAdd := Int.floor_add_intCast (Real.log (f.Fnum : ℝ) / Real.log (radix : ℝ) - (p - 1)) f.Fexp
          rw [hFloorAdd]
          have hFloorZero : ⌊Real.log (f.Fnum : ℝ) / Real.log (radix : ℝ) - (p - 1)⌋ = 0 := by
            apply Int.floor_eq_zero_iff.mpr
            exact ⟨hNormalized.1, hNormalized.2⟩
          rw [hFloorZero]
          ring

        -- Now show f.Fnum * beta^f.Fexp * radix^(-f.Fexp) = f.Fnum
        have hCancelExp : (f.Fnum : ℝ) * (beta : ℝ) ^ f.Fexp * (radix : ℝ) ^ (-f.Fexp) =
                          (f.Fnum : ℝ) := by
          rw [hBetaCast]
          rw [mul_assoc, ← zpow_add₀ hRadixNe0]
          simp only [add_neg_cancel, zpow_zero, mul_one]

        -- The main goal: show IRNDD applied to f.Fnum gives back f.Fnum
        rw [hExpComp]
        rw [hCancelExp]
        simp only [IRNDD, Int.floor_intCast, Int.cast_id]
        trivial
    · -- f is subnormal but we're in the normal branch (f >= firstNormalPos)
      -- Subnormal: f.Fexp = -b.dExp and |radix * f.Fnum| < b.vNum
      -- This means F2R f = f.Fnum * radix^(-b.dExp)
      -- For subnormal, typically |f.Fnum| < radix^(p-1)
      -- So F2R f < radix^(p-1) * radix^(-b.dExp+1) = firstNormalPos (roughly)
      -- But hNormal says F2R f >= firstNormalPos
      -- This is a contradiction

      -- The contradiction comes from hLtNormMin which states |radix * f.Fnum| < b.vNum
      -- Combined with hNormal, we get F2R f >= firstNormalPos
      -- But for subnormal floats (at exponent -b.dExp), this is impossible

      exfalso
      -- We need to derive a contradiction
      -- hNormal: firstNormalPos <= F2R f
      -- hExpEq: f.Fexp = -b.dExp
      -- hLtNormMin: |radix * f.Fnum| < b.vNum

      -- Step 1: From hLtNormMin and hpGivesBound, get |radix * f.Fnum| < radix^p
      have hAbsBound : |radix * f.Fnum| < radix ^ p.toNat := by
        rw [← hpGivesBound]; exact hLtNormMin

      -- Step 2: Since radix > 0, we can divide: |f.Fnum| < radix^(p-1)
      have hRadixPosInt : 0 < radix := by omega
      have hFnumAbsBound : |f.Fnum| < radix ^ (p.toNat - 1) := by
        have h1 : |radix| * |f.Fnum| < radix ^ p.toNat := by
          rw [← abs_mul]; exact hAbsBound
        have h2 : |radix| = radix := abs_of_pos hRadixPosInt
        rw [h2] at h1
        have hp_pos : 0 < p.toNat := by
          have : 0 ≤ p := by omega
          have hp' : (0 : ℤ).toNat ≤ p.toNat := Int.toNat_le_toNat (by omega : (0 : ℤ) ≤ p)
          simp at hp'; omega
        have hp_eq : p.toNat = (p.toNat - 1) + 1 := by omega
        rw [hp_eq, pow_succ, mul_comm (radix ^ (p.toNat - 1)) radix] at h1
        -- h1 : radix * |f.Fnum| < radix * radix ^ (p.toNat - 1)
        have hRadixPosInt' : (0 : ℤ) < radix := hRadixPosInt
        exact (Int.mul_lt_mul_left hRadixPosInt').mp h1

      -- Step 3: Determine sign of f.Fnum from hNonneg
      -- Since F2R f = f.Fnum * beta^f.Fexp ≥ 0 and beta^f.Fexp > 0, we have f.Fnum ≥ 0
      have hBetaPos : (0 : ℝ) < beta := by rw [hBetaEq]; exact hRadixPos
      have hBetaPowPos : (0 : ℝ) < (beta : ℝ) ^ f.Fexp := zpow_pos hBetaPos f.Fexp
      have hF2R_eq : _root_.F2R f = (f.Fnum : ℝ) * (beta : ℝ) ^ f.Fexp := by
        simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
                   FloatSpec.Core.Defs.FlocqFloat.Fnum,
                   FloatSpec.Core.Defs.FlocqFloat.Fexp]
      have hFnumCastNonneg : (0 : ℝ) ≤ (f.Fnum : ℝ) := by
        have h := hNonneg
        rw [hF2R_eq] at h
        exact (mul_nonneg_iff_of_pos_right hBetaPowPos).mp h
      have hFnumNonneg : 0 ≤ f.Fnum := Int.cast_nonneg_iff.mp hFnumCastNonneg

      -- Step 4: From hFnumAbsBound and hFnumNonneg, get f.Fnum < radix^(p-1)
      have hFnumBound : f.Fnum < radix ^ (p.toNat - 1) := by
        have hAbs : |f.Fnum| = f.Fnum := abs_of_nonneg hFnumNonneg
        rw [hAbs] at hFnumAbsBound
        exact hFnumAbsBound

      -- Step 5: Compute F2R of firstNormalPos
      -- firstNormalPos = ⟨nNormMin radix p.toNat, -b.dExp⟩
      -- nNormMin radix p.toNat = radix^(p.toNat - 1)
      -- F2R firstNormalPos = radix^(p.toNat - 1) * beta^(-b.dExp)
      have hFirstNPValue : _root_.F2R (firstNormalPos (beta:=beta) radix b p.toNat) =
                           (radix ^ (p.toNat - 1) : ℤ) * (beta : ℝ) ^ (-b.dExp : ℤ) := by
        simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, firstNormalPos, nNormMin,
                   FloatSpec.Core.Defs.FlocqFloat.Fnum,
                   FloatSpec.Core.Defs.FlocqFloat.Fexp]

      -- Step 6: Compute F2R f using hExpEq
      have hF2R_f : _root_.F2R f = (f.Fnum : ℝ) * (beta : ℝ) ^ (-b.dExp : ℤ) := by
        rw [hF2R_eq, hExpEq]

      -- Step 7: Show F2R f < F2R firstNormalPos
      have hF2R_lt : _root_.F2R f < _root_.F2R (firstNormalPos (beta:=beta) radix b p.toNat) := by
        rw [hFirstNPValue, hF2R_f]
        have hBetaPowPos' : (0 : ℝ) < (beta : ℝ) ^ (-b.dExp : ℤ) := zpow_pos hBetaPos _
        apply mul_lt_mul_of_pos_right _ hBetaPowPos'
        exact Int.cast_lt.mpr hFnumBound

      -- Step 8: This contradicts hNormal
      linarith
  · -- Subnormal case: F2R f < firstNormalPos
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
               FloatSpec.Core.Defs.FlocqFloat.Fnum,
               FloatSpec.Core.Defs.FlocqFloat.Fexp]
    rcases hCanonic with ⟨hBounded, hNormMin⟩ | ⟨hBounded, hExpEq, hLtNormMin⟩
    · -- Normal case but in subnormal branch - need contradiction
      -- For normal float, we should have F2R f >= firstNormalPos
      -- But hNormal (negated) says F2R f < firstNormalPos
      -- This is a contradiction only if normal floats satisfy the threshold

      exfalso
      -- Step 1: From hNormMin and hpGivesBound, get radix^p ≤ |radix * f.Fnum|
      have hRadixPosInt : 0 < radix := by omega
      have hAbsBound : radix ^ p.toNat ≤ |radix * f.Fnum| := by
        rw [← hpGivesBound]; exact hNormMin

      -- Step 2: Since radix > 0, we get radix^(p-1) ≤ |f.Fnum|
      have hFnumAbsLower : radix ^ (p.toNat - 1) ≤ |f.Fnum| := by
        have h1 : |radix| * |f.Fnum| ≥ radix ^ p.toNat := by
          rw [← abs_mul]; exact hAbsBound
        have h2 : |radix| = radix := abs_of_pos hRadixPosInt
        rw [h2] at h1
        have hp_pos : 0 < p.toNat := by
          have : 0 ≤ p := by omega
          have hp' : (0 : ℤ).toNat ≤ p.toNat := Int.toNat_le_toNat (by omega : (0 : ℤ) ≤ p)
          simp at hp'; omega
        have hp_eq : p.toNat = (p.toNat - 1) + 1 := by omega
        rw [hp_eq, pow_succ, mul_comm (radix ^ (p.toNat - 1)) radix] at h1
        -- h1 : radix * radix^(p.toNat - 1) ≤ radix * |f.Fnum|
        exact (Int.mul_le_mul_left hRadixPosInt).mp h1

      -- Step 3: From hNonneg and f.Fnum relationship
      have hBetaPos : (0 : ℝ) < beta := by rw [hBetaEq]; exact hRadixPos
      have hBetaPowPos : (0 : ℝ) < (beta : ℝ) ^ f.Fexp := zpow_pos hBetaPos f.Fexp
      have hF2R_eq : _root_.F2R f = (f.Fnum : ℝ) * (beta : ℝ) ^ f.Fexp := by
        simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
                   FloatSpec.Core.Defs.FlocqFloat.Fnum,
                   FloatSpec.Core.Defs.FlocqFloat.Fexp]
      have hFnumCastNonneg : (0 : ℝ) ≤ (f.Fnum : ℝ) := by
        have h := hNonneg
        rw [hF2R_eq] at h
        exact (mul_nonneg_iff_of_pos_right hBetaPowPos).mp h
      have hFnumNonneg : 0 ≤ f.Fnum := Int.cast_nonneg_iff.mp hFnumCastNonneg

      -- Step 4: From hFnumAbsLower and hFnumNonneg, get radix^(p-1) ≤ f.Fnum
      have hFnumLower : radix ^ (p.toNat - 1) ≤ f.Fnum := by
        have hAbs : |f.Fnum| = f.Fnum := abs_of_nonneg hFnumNonneg
        rw [hAbs] at hFnumAbsLower
        exact hFnumAbsLower

      -- Step 5: From Fbounded', get f.Fexp ≥ -b.dExp
      have hExpLower : -b.dExp ≤ f.Fexp := hBounded.2

      -- Step 6: Since beta > 1, beta^(-b.dExp) ≤ beta^f.Fexp
      have hBetaGt1Real : (1 : ℝ) < beta := by
        have h1 : ((1 : ℤ) : ℝ) < ((radix : ℤ) : ℝ) := Int.cast_lt.mpr hRadixGt1
        simp only [Int.cast_one] at h1
        rw [hBetaEq]; exact h1
      have hBetaPowMono : (beta : ℝ) ^ (-b.dExp : ℤ) ≤ (beta : ℝ) ^ f.Fexp := by
        rw [← Real.rpow_intCast, ← Real.rpow_intCast]
        apply Real.rpow_le_rpow_of_exponent_le (le_of_lt hBetaGt1Real)
        exact Int.cast_le.mpr hExpLower

      -- Step 7: Compute F2R of firstNormalPos
      have hFirstNPValue : _root_.F2R (firstNormalPos (beta:=beta) radix b p.toNat) =
                           (radix ^ (p.toNat - 1) : ℤ) * (beta : ℝ) ^ (-b.dExp : ℤ) := by
        simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, firstNormalPos, nNormMin,
                   FloatSpec.Core.Defs.FlocqFloat.Fnum,
                   FloatSpec.Core.Defs.FlocqFloat.Fexp]

      -- Step 8: Show F2R f ≥ F2R firstNormalPos
      have hF2R_ge : _root_.F2R (firstNormalPos (beta:=beta) radix b p.toNat) ≤ _root_.F2R f := by
        rw [hFirstNPValue, hF2R_eq]
        -- Need: radix^(p-1) * beta^(-b.dExp) ≤ f.Fnum * beta^f.Fexp
        have hRadixPowPos : (0 : ℤ) < radix ^ (p.toNat - 1) := Int.pow_pos hRadixPosInt
        have hRadixPowPosReal : (0 : ℝ) < (radix ^ (p.toNat - 1) : ℤ) := Int.cast_pos.mpr hRadixPowPos
        have hBetaPowPos' : (0 : ℝ) < (beta : ℝ) ^ (-b.dExp : ℤ) := zpow_pos hBetaPos _
        -- f.Fnum ≥ radix^(p-1) and beta^f.Fexp ≥ beta^(-b.dExp)
        calc
          (radix ^ (p.toNat - 1) : ℤ) * (beta : ℝ) ^ (-b.dExp : ℤ)
              ≤ (f.Fnum : ℝ) * (beta : ℝ) ^ (-b.dExp : ℤ) := by
                apply mul_le_mul_of_nonneg_right (Int.cast_le.mpr hFnumLower) (le_of_lt hBetaPowPos')
          _ ≤ (f.Fnum : ℝ) * (beta : ℝ) ^ f.Fexp := by
                apply mul_le_mul_of_nonneg_left hBetaPowMono hFnumCastNonneg

      -- Step 9: This contradicts hNormal
      exact hNormal hF2R_ge
    · -- Subnormal case - this is the valid path
      -- hExpEq : f.Fexp = -b.dExp
      simp only [hExpEq, hBetaEq]
      -- Goal: IRNDD (f.Fnum * radix^(-b.dExp) * radix^(b.dExp)) * radix^(-b.dExp) = f.Fnum * radix^(-b.dExp)
      -- Simplify radix^(-b.dExp) * radix^(b.dExp) = 1
      rw [mul_assoc, ← zpow_add₀ hRadixNe0]
      simp only [neg_add_cancel, zpow_zero, mul_one]
      -- Goal: IRNDD (f.Fnum) * radix^(-b.dExp) = f.Fnum * radix^(-b.dExp)
      -- IRNDD on integer is identity
      simp only [IRNDD]
      rw [Int.floor_intCast]
      rfl

noncomputable def RND_Min_canonic_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int) (r : ℝ) : Unit :=
  ()

-- Upper rounding operators (mirroring Coq RND_Max_*)

/-- Ceiling-based integer rounding (used for upper bound computation).
    Returns the smallest integer greater than or equal to x. -/
noncomputable def IRNDU (r : ℝ) : Int := Int.ceil r

/-- Coq: `RND_Max_Pos` — upper rounding (smallest representable float ≥ r)
    for nonnegative reals.

Flocq defines this as either the lower rounded value itself, when the input is
already represented by `RND_Min_Pos`, or the normalized successor of that lower
rounded value. -/
noncomputable def RND_Max_Pos {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int)
    (r : ℝ) : FloatSpec.Core.Defs.FlocqFloat beta :=
  let mn := RND_Min_Pos (beta:=beta) b radix p r
  if r = _root_.F2R (beta:=beta) mn then
    mn
  else
    FSucc (beta:=beta) b radix p.toNat mn

noncomputable def RND_Max_Pos_canonic_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int) (r : ℝ) : Unit :=
  ()

/-- Coq: `RND_Max_Pos_canonic`.

This follows Flocq's definition of `RND_Max_Pos`: it is either the lower
rounded value itself or the normalized successor of that value. -/
theorem RND_Max_Pos_canonic {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int) (r : ℝ) :
    ⦃⌜0 ≤ r ∧ beta = radix ∧ 1 < radix ∧ 1 < p ∧
        b.vNum = Zpower_nat radix p.toNat⌝⦄
    (pure (RND_Max_Pos_canonic_check (beta:=beta) b radix p r) : Id Unit)
    ⦃⇓_ => ⌜Fcanonic (beta:=beta) radix b
        (RND_Max_Pos (beta:=beta) b radix p r)⌝⦄ := by
  intro h
  rcases h with ⟨hr_nonneg, hbeta_eq, hradix_gt, hp_gt, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, RND_Max_Pos_canonic_check,
    Id.run, ULift.up_down]
  unfold RND_Max_Pos
  have hp_nat_ne : p.toNat ≠ 0 := by
    have hp_pos : (0 : Int) < p := by omega
    exact Nat.ne_of_gt (by
      have hp_toNat : (p.toNat : Int) = p :=
        Int.toNat_of_nonneg (le_of_lt hp_pos)
      omega)
  have hmin_can :
      Fcanonic (beta:=beta) radix b
        (RND_Min_Pos (beta:=beta) b radix p r) := by
    have h := RND_Min_Pos_canonic (beta:=beta) b radix p r
    simpa only [wp, PostCond.noThrow, pure, RND_Min_Pos_canonic_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
      h ⟨hr_nonneg, hbeta_eq, hradix_gt, hp_gt, hvNum⟩
  by_cases hrep :
      r = _root_.F2R (beta:=beta)
        (RND_Min_Pos (beta:=beta) b radix p r)
  · rw [ite_eq_left hrep]
    exact hmin_can
  · rw [ite_eq_right hrep]
    have hsucc := FSuccCanonic (beta:=beta) b radix p.toNat
      (RND_Min_Pos (beta:=beta) b radix p r)
    simpa only [wp, PostCond.noThrow, pure, FSuccCanonic_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
      hsucc ⟨hmin_can, hradix_gt, hp_nat_ne, hvNum⟩

-- Lower rounding correctness on nonnegative reals (Coq: RND_Min_Pos_correct)
noncomputable def RND_Min_Pos_correct_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int) (r : ℝ) : Unit :=
  ()

-- Upper rounding is ≥ the input on nonnegative reals (Coq: RND_Max_Pos_Rle)
noncomputable def RND_Max_Pos_Rle_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int) (r : ℝ) : Unit :=
  ()

/-- Coq: `RND_Max_Pos_Rle` is restored after `RND_Min_Pos_correct`, because
the Flocq proof extracts the lower-rounding extremal property. -/
-- Upper rounding correctness on nonnegative reals (Coq: RND_Max_Pos_correct)
noncomputable def RND_Max_Pos_correct_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int) (r : ℝ) : Unit :=
  ()

-- Roundings of any real (upper rounding)
noncomputable def RND_Min {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int)
    (r : ℝ) : FloatSpec.Core.Defs.FlocqFloat beta :=
  if 0 ≤ r then
    RND_Min_Pos (beta:=beta) b radix p r
  else
    Fopp (beta:=beta) (RND_Max_Pos (beta:=beta) b radix p (-r))

noncomputable def RND_Max {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int)
    (r : ℝ) : FloatSpec.Core.Defs.FlocqFloat beta :=
  if 0 ≤ r then
    RND_Max_Pos (beta:=beta) b radix p r
  else
    Fopp (beta:=beta) (RND_Min_Pos (beta:=beta) b radix p (-r))

/-- Coq: `RND_Min_canonic`.

The signed wrapper follows Flocq's sign split. The positive canonicity payloads
remain explicit because the positive theorems carry section assumptions that
this wrapper does not infer. -/
theorem RND_Min_canonic_from_positive_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int) (r : ℝ) :
    ⦃⌜(∀ s : ℝ, 0 ≤ s →
          Fcanonic (beta:=beta) radix b
            (RND_Min_Pos (beta:=beta) b radix p s)) ∧
        (∀ s : ℝ, 0 ≤ s →
          Fcanonic (beta:=beta) radix b
            (RND_Max_Pos (beta:=beta) b radix p s))⌝⦄
    (pure (RND_Min_canonic_check (beta:=beta) b radix p r) : Id Unit)
    ⦃⇓_ => ⌜Fcanonic (beta:=beta) radix b
        (RND_Min (beta:=beta) b radix p r)⌝⦄ := by
  intro h
  rcases h with ⟨hMinPos, hMaxPos⟩
  simp only [wp, PostCond.noThrow, pure, RND_Min_canonic_check,
    Id.run, ULift.up_down]
  unfold RND_Min
  by_cases hr : 0 ≤ r
  · simp [hr]
    exact hMinPos r hr
  · simp [hr]
    have hneg : 0 ≤ -r := by linarith
    exact FcanonicFopp (beta:=beta) radix b
      (RND_Max_Pos (beta:=beta) b radix p (-r)) (hMaxPos (-r) hneg)

noncomputable def RND_Max_canonic_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int) (r : ℝ) : Unit :=
  ()

/-- Coq: `RND_Max_canonic`.

The signed wrapper follows Flocq's sign split. The positive canonicity payloads
remain explicit because the positive theorems carry section assumptions that
this wrapper does not infer. -/
theorem RND_Max_canonic_from_positive_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int) (r : ℝ) :
    ⦃⌜(∀ s : ℝ, 0 ≤ s →
          Fcanonic (beta:=beta) radix b
            (RND_Min_Pos (beta:=beta) b radix p s)) ∧
        (∀ s : ℝ, 0 ≤ s →
          Fcanonic (beta:=beta) radix b
            (RND_Max_Pos (beta:=beta) b radix p s))⌝⦄
    (pure (RND_Max_canonic_check (beta:=beta) b radix p r) : Id Unit)
    ⦃⇓_ => ⌜Fcanonic (beta:=beta) radix b
        (RND_Max (beta:=beta) b radix p r)⌝⦄ := by
  intro h
  rcases h with ⟨hMinPos, hMaxPos⟩
  simp only [wp, PostCond.noThrow, pure, RND_Max_canonic_check,
    Id.run, ULift.up_down]
  unfold RND_Max
  by_cases hr : 0 ≤ r
  · simp [hr]
    exact hMaxPos r hr
  · simp [hr]
    have hneg : 0 ≤ -r := by linarith
    exact FcanonicFopp (beta:=beta) radix b
      (RND_Min_Pos (beta:=beta) b radix p (-r)) (hMinPos (-r) hneg)


-- Correctness of lower rounding (Coq: RND_Min_correct)
noncomputable def RND_Min_correct_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int)
    (r : ℝ) : Unit :=
  ()

/-- Coq: `RND_Min_correct`.

The signed theorem follows the upstream sign split.  The still-missing positive
correctness proofs are explicit dependencies. -/
theorem RND_Min_correct_from_positive_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int) (r : ℝ) :
    ⦃⌜(∀ s : ℝ, 0 ≤ s →
          isMin (beta:=beta) b radix s
            (RND_Min_Pos (beta:=beta) b radix p s)) ∧
        (∀ s : ℝ, 0 ≤ s →
          isMax (beta:=beta) b radix s
            (RND_Max_Pos (beta:=beta) b radix p s))⌝⦄
    (pure (RND_Min_correct_check (beta:=beta) b radix p r) : Id Unit)
    ⦃⇓_ => ⌜isMin (beta:=beta) b radix r
        (RND_Min (beta:=beta) b radix p r)⌝⦄ := by
  intro hpos
  rcases hpos with ⟨hMinPos, hMaxPos⟩
  simp only [wp, PostCond.noThrow, pure, RND_Min_correct_check,
    Id.run, ULift.up_down]
  unfold RND_Min
  by_cases hr : 0 ≤ r
  · simp only [hr, ite_true]
    exact hMinPos r hr
  · simp only [hr, ite_false]
    have hneg_nonneg : 0 ≤ -r := by linarith
    rcases hMaxPos (-r) hneg_nonneg with ⟨hBounded, hUpper, hLeastUpper⟩
    constructor
    · rcases hBounded with ⟨hNum, hExp⟩
      exact ⟨by simpa [Fopp, FloatSpec.Calc.Operations.Fopp, abs_neg] using hNum,
        by simpa [Fopp, FloatSpec.Calc.Operations.Fopp] using hExp⟩
    constructor
    · have hFopp :
          _root_.F2R (beta:=beta)
              (Fopp (beta:=beta) (RND_Max_Pos (beta:=beta) b radix p (-r))) =
            - _root_.F2R (beta:=beta)
              (RND_Max_Pos (beta:=beta) b radix p (-r)) := by
        have h := FloatSpec.Calc.Operations.F2R_opp (beta:=beta)
          (RND_Max_Pos (beta:=beta) b radix p (-r))
        simpa [Fopp, _root_.F2R] using h trivial
      rw [hFopp]
      linarith
    · intro f hfBounded hfLe
      have hFoppFBounded : Fbounded (beta:=beta) b (Fopp (beta:=beta) f) := by
        simpa [Fbounded, Fbounded', Fopp, FloatSpec.Calc.Operations.Fopp, abs_neg] using hfBounded
      have hFoppF :
          _root_.F2R (beta:=beta) (Fopp (beta:=beta) f) =
            - _root_.F2R (beta:=beta) f := by
        have h := FloatSpec.Calc.Operations.F2R_opp (beta:=beta) f
        simpa [Fopp, _root_.F2R] using h trivial
      have hInput : -r ≤ _root_.F2R (beta:=beta) (Fopp (beta:=beta) f) := by
        rw [hFoppF]
        linarith
      have hLeast := hLeastUpper (Fopp (beta:=beta) f) hFoppFBounded hInput
      have hFoppMax :
          _root_.F2R (beta:=beta)
              (Fopp (beta:=beta) (RND_Max_Pos (beta:=beta) b radix p (-r))) =
            - _root_.F2R (beta:=beta)
              (RND_Max_Pos (beta:=beta) b radix p (-r)) := by
        have h := FloatSpec.Calc.Operations.F2R_opp (beta:=beta)
          (RND_Max_Pos (beta:=beta) b radix p (-r))
        simpa [Fopp, _root_.F2R] using h trivial
      rw [hFoppF] at hLeast
      rw [hFoppMax]
      linarith

-- Correctness of upper rounding (Coq: RND_Max_correct)
noncomputable def RND_Max_correct_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int)
    (r : ℝ) : Unit :=
  ()

/-- Coq: `RND_Max_correct`.

The signed theorem follows the upstream sign split.  The still-missing positive
correctness proofs are explicit dependencies. -/
theorem RND_Max_correct_from_positive_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int) (r : ℝ) :
    ⦃⌜(∀ s : ℝ, 0 ≤ s →
          isMin (beta:=beta) b radix s
            (RND_Min_Pos (beta:=beta) b radix p s)) ∧
        (∀ s : ℝ, 0 ≤ s →
          isMax (beta:=beta) b radix s
            (RND_Max_Pos (beta:=beta) b radix p s))⌝⦄
    (pure (RND_Max_correct_check (beta:=beta) b radix p r) : Id Unit)
    ⦃⇓_ => ⌜isMax (beta:=beta) b radix r
        (RND_Max (beta:=beta) b radix p r)⌝⦄ := by
  intro hpos
  rcases hpos with ⟨hMinPos, hMaxPos⟩
  simp only [wp, PostCond.noThrow, pure, RND_Max_correct_check,
    Id.run, ULift.up_down]
  unfold RND_Max
  by_cases hr : 0 ≤ r
  · simp only [hr, ite_true]
    exact hMaxPos r hr
  · simp only [hr, ite_false]
    have hneg_nonneg : 0 ≤ -r := by linarith
    rcases hMinPos (-r) hneg_nonneg with ⟨hBounded, hLower, hGreatestLower⟩
    constructor
    · rcases hBounded with ⟨hNum, hExp⟩
      exact ⟨by simpa [Fopp, FloatSpec.Calc.Operations.Fopp, abs_neg] using hNum,
        by simpa [Fopp, FloatSpec.Calc.Operations.Fopp] using hExp⟩
    constructor
    · have hFopp :
          _root_.F2R (beta:=beta)
              (Fopp (beta:=beta) (RND_Min_Pos (beta:=beta) b radix p (-r))) =
            - _root_.F2R (beta:=beta)
              (RND_Min_Pos (beta:=beta) b radix p (-r)) := by
        have h := FloatSpec.Calc.Operations.F2R_opp (beta:=beta)
          (RND_Min_Pos (beta:=beta) b radix p (-r))
        simpa [Fopp, _root_.F2R] using h trivial
      rw [hFopp]
      linarith
    · intro f hfBounded hfGe
      have hFoppFBounded : Fbounded (beta:=beta) b (Fopp (beta:=beta) f) := by
        simpa [Fbounded, Fbounded', Fopp, FloatSpec.Calc.Operations.Fopp, abs_neg] using hfBounded
      have hFoppF :
          _root_.F2R (beta:=beta) (Fopp (beta:=beta) f) =
            - _root_.F2R (beta:=beta) f := by
        have h := FloatSpec.Calc.Operations.F2R_opp (beta:=beta) f
        simpa [Fopp, _root_.F2R] using h trivial
      have hInput : _root_.F2R (beta:=beta) (Fopp (beta:=beta) f) ≤ -r := by
        rw [hFoppF]
        linarith
      have hGreatest := hGreatestLower (Fopp (beta:=beta) f) hFoppFBounded hInput
      have hFoppMin :
          _root_.F2R (beta:=beta)
              (Fopp (beta:=beta) (RND_Min_Pos (beta:=beta) b radix p (-r))) =
            - _root_.F2R (beta:=beta)
              (RND_Min_Pos (beta:=beta) b radix p (-r)) := by
        have h := FloatSpec.Calc.Operations.F2R_opp (beta:=beta)
          (RND_Min_Pos (beta:=beta) b radix p (-r))
        simpa [Fopp, _root_.F2R] using h trivial
      rw [hFoppF] at hGreatest
      rw [hFoppMin]
      linarith

/-! Generic nearest Pff rounding with an arbitrary tie-breaking choice.

This is the Flocq `RND_Closest` layer from `Pff2FlocqAux.v`. The local signed
DN/UP wrappers keep their positive-side payloads explicit, so the
correctness/canonicity lemmas below expose the same prerequisites instead of
hiding them in a vacuous theorem shell. -/

noncomputable def RND_Closest {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int)
    (choice : Int → Bool) (r : ℝ) :
    FloatSpec.Core.Defs.FlocqFloat beta :=
  let ru := RND_Max (beta:=beta) b radix p r
  let rd := RND_Min (beta:=beta) b radix p r
  if |_root_.F2R (beta:=beta) ru - r| ≤
      |_root_.F2R (beta:=beta) rd - r| then
    if |_root_.F2R (beta:=beta) ru - r| <
        |_root_.F2R (beta:=beta) rd - r| then
      ru
    else if choice (FloatSpec.Core.Raux.Zfloor
        (FloatSpec.Core.Generic_fmt.scaled_mantissa beta
          (FLT_exp (-b.dExp) p) r)) then
      ru
    else
      rd
  else
    rd

noncomputable def RND_Closest_canonic_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int)
    (choice : Int → Bool) (r : ℝ) : Unit :=
  ()

/-- Coq: `RND_Closest_canonic`.

This follows Flocq's case split over the upper/lower rounded values. -/
theorem RND_Closest_canonic_from_min_max {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int)
    (choice : Int → Bool) (r : ℝ) :
    ⦃⌜Fcanonic (beta:=beta) radix b
          (RND_Min (beta:=beta) b radix p r) ∧
        Fcanonic (beta:=beta) radix b
          (RND_Max (beta:=beta) b radix p r)⌝⦄
    (pure (RND_Closest_canonic_check (beta:=beta) b radix p choice r) : Id Unit)
    ⦃⇓_ => ⌜Fcanonic (beta:=beta) radix b
        (RND_Closest (beta:=beta) b radix p choice r)⌝⦄ := by
  intro h
  rcases h with ⟨hMin, hMax⟩
  simp only [wp, PostCond.noThrow, pure, RND_Closest_canonic_check,
    Id.run, ULift.up_down]
  unfold RND_Closest
  by_cases hle :
      |_root_.F2R (beta:=beta) (RND_Max (beta:=beta) b radix p r) - r| ≤
        |_root_.F2R (beta:=beta) (RND_Min (beta:=beta) b radix p r) - r|
  · rw [ite_eq_left hle]
    by_cases hlt :
        |_root_.F2R (beta:=beta) (RND_Max (beta:=beta) b radix p r) - r| <
          |_root_.F2R (beta:=beta) (RND_Min (beta:=beta) b radix p r) - r|
    · rw [ite_eq_left hlt]
      exact hMax
    · rw [ite_eq_right hlt]
      by_cases hchoice : choice (FloatSpec.Core.Raux.Zfloor
          (FloatSpec.Core.Generic_fmt.scaled_mantissa beta
            (FLT_exp (-b.dExp) p) r))
      · rw [ite_eq_left hchoice]
        exact hMax
      · rw [ite_eq_right hchoice]
        exact hMin
  · rw [ite_eq_right hle]
    exact hMin

noncomputable def RND_Closest_correct_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int)
    (choice : Int → Bool) (r : ℝ) : Unit :=
  ()

-- Even-closest rounding function (Coq: `RND_EvenClosest`)
noncomputable def RND_EvenClosest {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (r : ℝ) : FloatSpec.Core.Defs.FlocqFloat beta :=
  let rmax := RND_Max (beta:=beta) b radix (precision : Int) r
  let rmin := RND_Min (beta:=beta) b radix (precision : Int) r
  if |_root_.F2R (beta:=beta) rmax - r| ≤
      |_root_.F2R (beta:=beta) rmin - r| then
    if |_root_.F2R (beta:=beta) rmax - r| <
        |_root_.F2R (beta:=beta) rmin - r| then
      rmax
    else if Odd rmin.Fnum then
      rmax
    else
      rmin
  else
    rmin

-- Even-closest rounding: canonicity (Coq: RND_EvenClosest_canonic)
noncomputable def RND_EvenClosest_canonic_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (r : ℝ) : Unit :=
  ()

/-- Coq: `RND_EvenClosest_canonic`.

The proof follows Flocq's case split over whether even-closest returns the
signed upper or lower rounding. The signed canonicity wrappers remain explicit
dependencies until their positive canonic payloads are fully restored. -/
theorem RND_EvenClosest_canonic_from_min_max {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (r : ℝ) :
    ⦃⌜Fcanonic (beta:=beta) radix b
          (RND_Min (beta:=beta) b radix (precision : Int) r) ∧
        Fcanonic (beta:=beta) radix b
          (RND_Max (beta:=beta) b radix (precision : Int) r)⌝⦄
    (pure (RND_EvenClosest_canonic_check (beta:=beta) b radix precision r) : Id Unit)
    ⦃⇓_ => ⌜Fcanonic (beta:=beta) radix b
        (RND_EvenClosest (beta:=beta) b radix precision r)⌝⦄ := by
  intro h
  rcases h with ⟨hMin, hMax⟩
  simp only [wp, PostCond.noThrow, pure, RND_EvenClosest_canonic_check,
    Id.run, ULift.up_down]
  unfold RND_EvenClosest
  by_cases hle :
      |_root_.F2R (beta:=beta) (RND_Max (beta:=beta) b radix (precision : Int) r) - r| ≤
        |_root_.F2R (beta:=beta) (RND_Min (beta:=beta) b radix (precision : Int) r) - r|
  · rw [ite_eq_left hle]
    by_cases hlt :
        |_root_.F2R (beta:=beta) (RND_Max (beta:=beta) b radix (precision : Int) r) - r| <
          |_root_.F2R (beta:=beta) (RND_Min (beta:=beta) b radix (precision : Int) r) - r|
    · rw [ite_eq_left hlt]
      exact hMax
    · rw [ite_eq_right hlt]
      by_cases hodd : Odd (RND_Min (beta:=beta) b radix (precision : Int) r).Fnum
      · rw [ite_eq_left hodd]
        exact hMax
      · rw [ite_eq_right hodd]
        exact hMin
  · rw [ite_eq_right hle]
    exact hMin

-- Even-closest rounding: correctness (Coq: RND_EvenClosest_correct)
noncomputable def RND_EvenClosest_correct_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (r : ℝ) : Unit :=
  ()

/-- Coq: `RND_EvenClosest_correct`.

The local proof follows Flocq's implementation case split.  The nearest-point,
min/max disjunction, and endpoint uniqueness facts are explicit here because
their general theorems are declared later in this translated file.  The exact
midpoint odd-min branch depends on the endpoint-successor/parity machinery that
Flocq derives from `MinMax`, `FNSucc`, and canonical uniqueness, so the
remaining tie payload is the faithful parity obligation: if the lower endpoint
is odd, the selected upper endpoint is normalized-even. -/
theorem RND_EvenClosest_correct_from_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (r : ℝ) :
    ⦃⌜isMin (beta:=beta) b radix r
          (RND_Min (beta:=beta) b radix (precision : Int) r) ∧
        isMax (beta:=beta) b radix r
          (RND_Max (beta:=beta) b radix (precision : Int) r) ∧
        ((2 * r ≤
            _root_.F2R (beta:=beta)
              (RND_Min (beta:=beta) b radix (precision : Int) r) +
            _root_.F2R (beta:=beta)
              (RND_Max (beta:=beta) b radix (precision : Int) r)) →
          Closest (beta:=beta) b (radix : ℝ) r
            (RND_Min (beta:=beta) b radix (precision : Int) r)) ∧
        ((_root_.F2R (beta:=beta)
              (RND_Min (beta:=beta) b radix (precision : Int) r) +
            _root_.F2R (beta:=beta)
              (RND_Max (beta:=beta) b radix (precision : Int) r) ≤ 2 * r) →
          Closest (beta:=beta) b (radix : ℝ) r
            (RND_Max (beta:=beta) b radix (precision : Int) r)) ∧
        (∀ q : FloatSpec.Core.Defs.FlocqFloat beta,
          Closest (beta:=beta) b (radix : ℝ) r q →
            isMin (beta:=beta) b radix r q ∨
              isMax (beta:=beta) b radix r q) ∧
        (∀ q : FloatSpec.Core.Defs.FlocqFloat beta,
          isMin (beta:=beta) b radix r q →
            _root_.F2R (beta:=beta) q =
              _root_.F2R (beta:=beta)
                (RND_Min (beta:=beta) b radix (precision : Int) r)) ∧
        (∀ q : FloatSpec.Core.Defs.FlocqFloat beta,
          isMax (beta:=beta) b radix r q →
            _root_.F2R (beta:=beta) q =
              _root_.F2R (beta:=beta)
                (RND_Max (beta:=beta) b radix (precision : Int) r)) ∧
        (¬ Odd (RND_Min (beta:=beta) b radix (precision : Int) r).Fnum →
          FNeven (beta:=beta) b (radix : ℝ) precision
            (RND_Min (beta:=beta) b radix (precision : Int) r)) ∧
        (Odd (RND_Min (beta:=beta) b radix (precision : Int) r).Fnum →
          FNeven (beta:=beta) b (radix : ℝ) precision
            (RND_Max (beta:=beta) b radix (precision : Int) r))⌝⦄
    (pure (RND_EvenClosest_correct_check (beta:=beta) b radix precision r) : Id Unit)
    ⦃⇓_ => ⌜EvenClosest (beta:=beta) b (radix : ℝ) precision r
        (RND_EvenClosest (beta:=beta) b radix precision r)⌝⦄ := by
  intro h
  rcases h with ⟨hMin, hMax, closest_min, closest_max,
    hClosestMinOrMax, hMinUnique, hMaxUnique, hTieMinEven, hTieMaxEven⟩
  simp only [wp, PostCond.noThrow, pure, RND_EvenClosest_correct_check,
    Id.run, ULift.up_down]
  let rd := RND_Min (beta:=beta) b radix (precision : Int) r
  let ru := RND_Max (beta:=beta) b radix (precision : Int) r
  have hrd_le : _root_.F2R (beta:=beta) rd ≤ r := hMin.2.1
  have hle_ru : r ≤ _root_.F2R (beta:=beta) ru := hMax.2.1
  have hrd_nonpos : _root_.F2R (beta:=beta) rd - r ≤ 0 := by
    dsimp [rd] at hrd_le ⊢
    linarith
  have hru_nonneg : 0 ≤ _root_.F2R (beta:=beta) ru - r := by
    dsimp [ru] at hle_ru ⊢
    linarith
  unfold RND_EvenClosest
  dsimp only [rd, ru]
  by_cases hle :
      |_root_.F2R (beta:=beta) ru - r| ≤
        |_root_.F2R (beta:=beta) rd - r|
  · rw [ite_eq_left hle]
    by_cases hlt :
        |_root_.F2R (beta:=beta) ru - r| <
          |_root_.F2R (beta:=beta) rd - r|
    · rw [ite_eq_left hlt]
      have hmid : _root_.F2R (beta:=beta) rd + _root_.F2R (beta:=beta) ru ≤
          2 * r := by
        rw [abs_of_nonneg hru_nonneg, abs_of_nonpos hrd_nonpos] at hlt
        linarith
      have hmaxClosest : Closest (beta:=beta) b (radix : ℝ) r ru := by
        simpa [rd, ru] using closest_max hmid
      refine ⟨hmaxClosest, Or.inr ?_⟩
      intro q hq
      have hqdist := hq.2 ru hMax.1
      rw [abs_of_nonneg hru_nonneg, abs_of_nonpos hrd_nonpos] at hlt
      have hq_ru : _root_.F2R (beta:=beta) q = _root_.F2R (beta:=beta) ru := by
        rcases hClosestMinOrMax q hq with hqMin | hqMax
        · have hq_rd := hMinUnique q hqMin
          have hq_nonpos : _root_.F2R (beta:=beta) q - r ≤ 0 := by
            have hq_le_r : _root_.F2R (beta:=beta) q ≤ r := hqMin.2.1
            linarith
          have hqdist' := hq.2 ru hMax.1
          rw [abs_of_nonpos hq_nonpos, abs_of_nonneg hru_nonneg] at hqdist'
          rw [hq_rd] at hqdist'
          linarith
        · exact hMaxUnique q hqMax
      exact hq_ru
    ·
      rw [ite_eq_right hlt]
      have hdist_eq :
          |_root_.F2R (beta:=beta) ru - r| =
            |_root_.F2R (beta:=beta) rd - r| :=
        le_antisymm hle (le_of_not_gt hlt)
      have hmid_min :
          2 * r ≤ _root_.F2R (beta:=beta) rd + _root_.F2R (beta:=beta) ru := by
        rw [abs_of_nonneg hru_nonneg, abs_of_nonpos hrd_nonpos] at hdist_eq
        linarith
      have hmid_max :
          _root_.F2R (beta:=beta) rd + _root_.F2R (beta:=beta) ru ≤ 2 * r := by
        rw [abs_of_nonneg hru_nonneg, abs_of_nonpos hrd_nonpos] at hdist_eq
        linarith
      by_cases hodd : Odd rd.Fnum
      · rw [ite_eq_left hodd]
        have hEvenRu : FNeven (beta:=beta) b (radix : ℝ) precision ru := by
          simpa [ru] using hTieMaxEven (by simpa [rd] using hodd)
        exact ⟨closest_max hmid_max, Or.inl hEvenRu⟩
      · rw [ite_eq_right hodd]
        have hEven : FNeven (beta:=beta) b (radix : ℝ) precision rd := by
          simpa [rd] using hTieMinEven (by simpa [rd] using hodd)
        exact ⟨closest_min hmid_min, Or.inl hEven⟩
  · rw [ite_eq_right hle]
    have hlt_abs :
        |_root_.F2R (beta:=beta) rd - r| <
          |_root_.F2R (beta:=beta) ru - r| :=
      lt_of_not_ge hle
    have hmid : 2 * r ≤ _root_.F2R (beta:=beta) rd +
        _root_.F2R (beta:=beta) ru := by
      rw [abs_of_nonpos hrd_nonpos, abs_of_nonneg hru_nonneg] at hlt_abs
      linarith
    have hEvenOrOdd := Int.even_or_odd rd.Fnum
    rcases hEvenOrOdd with hEven | hOdd
    · have hEvenNorm : FNeven (beta:=beta) b (radix : ℝ) precision rd := by
        have hnot : ¬ Odd rd.Fnum := Int.not_odd_iff_even.mpr hEven
        simpa [rd] using hTieMinEven (by simpa [rd] using hnot)
      exact ⟨closest_min hmid, Or.inl hEvenNorm⟩
    · exact ⟨closest_min hmid,
        Or.inr (by
          intro q hq
          rcases hClosestMinOrMax q hq with hqMin | hqMax
          · exact hMinUnique q hqMin
          · have hq_ru := hMaxUnique q hqMax
            exfalso
            have hqdist := hq.2 rd hMin.1
            rw [hq_ru, abs_of_nonneg hru_nonneg, abs_of_nonpos hrd_nonpos] at hqdist
            rw [abs_of_nonpos hrd_nonpos, abs_of_nonneg hru_nonneg] at hlt_abs
            linarith)⟩

-- Totality of EvenClosest
noncomputable def EvenClosestTotal_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat) (r : ℝ) : Unit :=
  ()


-- Parity under negation (Coq: FevenFop)
noncomputable def FevenFop_check {beta : Int} [ValidRadix beta]
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FevenFop` — if a float is even, its negation is even. -/
theorem FevenFop {beta : Int} [ValidRadix beta]
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Feven (beta:=beta) p⌝⦄
    (pure (FevenFop_check (beta:=beta) p) : Id Unit)
    ⦃⇓_ => ⌜Feven (beta:=beta) (FloatSpec.Calc.Operations.Fopp (beta:=beta) p)⌝⦄ := by
  intro heven
  simp only [wp, PostCond.noThrow, pure, FevenFop_check, Feven, FloatSpec.Calc.Operations.Fopp] at *
  exact heven.neg

-- Normalized-odd is preserved under equal real value (Coq: FNoddEq)
noncomputable def FNoddEq_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat)
    (f1 f2 : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FNoddEq` — if `f1` and `f2` are canonical, have equal real value,
    and `f1` is FNodd, then `f2` is FNodd.

    NOTE: Since `Fnormalize = id` currently, we require `Fcanonic'` preconditions
    instead of just `Fbounded`. The theorem then follows from `FcanonicUnique`:
    canonical floats with the same real value are equal. -/
theorem FNoddEq_from_canonic_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat)
    (f1 f2 : FloatSpec.Core.Defs.FlocqFloat beta)
    (hβ : 1 < beta) :
    ⦃⌜Fcanonic' (beta:=beta) beta b f1 ∧ Fcanonic' (beta:=beta) beta b f2 ∧
        _root_.F2R f1 = _root_.F2R f2 ∧ FNodd (beta:=beta) b radix precision f1⌝⦄
    (pure (FNoddEq_check (beta:=beta) b radix precision f1 f2) : Id Unit)
    ⦃⇓_ => ⌜FNodd (beta:=beta) b radix precision f2⌝⦄ := by
  intro ⟨hcan1, hcan2, hF2Req, hNodd1⟩
  simp only [wp, PostCond.noThrow, pure, FNoddEq_check, Id.run,
    PredTrans.pure, PredTrans.apply, ULift.up, ULift.down, ULift.up_down,
    ULift.down_up]
  show FNodd (beta:=beta) b radix precision f2
  -- Use FcanonicUnique to show f1 = f2
  have hF1eqF2 : f1 = f2 := by
    have hspec := FcanonicUnique beta b f1 f2 hβ rfl
    simp only [wp, PostCond.noThrow, pure, FcanonicUnique_check, ULift.down_up] at hspec
    exact hspec ⟨hcan1, hcan2, hF2Req⟩
  -- Now the result follows trivially
  rw [← hF1eqF2]
  exact hNodd1

-- Normalized-even is preserved under equal real value (Coq: FNevenEq)
noncomputable def FNevenEq_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat)
    (f1 f2 : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FNevenEq` — if `f1` and `f2` are canonical (in the sense of `Fcanonic'`),
    have equal real value, and `f1` is FNeven, then `f2` is FNeven.

    NOTE: The original Coq theorem uses `Fnormalize` to convert bounded floats to
    canonical forms. Since our `Fnormalize = id`, we strengthen the precondition to
    require `Fcanonic'` directly. We also need `1 < beta` and `radix = beta` to
    apply `FcanonicUnique`. -/
theorem FNevenEq_from_canonic_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat)
    (f1 f2 : FloatSpec.Core.Defs.FlocqFloat beta)
    (hβ : 1 < beta) :
    ⦃⌜Fcanonic' (beta:=beta) beta b f1 ∧ Fcanonic' (beta:=beta) beta b f2 ∧
        _root_.F2R f1 = _root_.F2R f2 ∧ FNeven (beta:=beta) b radix precision f1⌝⦄
    (pure (FNevenEq_check (beta:=beta) b radix precision f1 f2) : Id Unit)
    ⦃⇓_ => ⌜FNeven (beta:=beta) b radix precision f2⌝⦄ := by
  intro ⟨hcan1, hcan2, hF2Req, hFNeven1⟩
  simp only [wp, PostCond.noThrow, pure, FNevenEq_check, Id.run,
    PredTrans.pure, PredTrans.apply, ULift.up, ULift.down, ULift.up_down,
    ULift.down_up]
  show FNeven (beta:=beta) b radix precision f2
  -- Use FcanonicUnique to show f1 = f2
  have hspec := FcanonicUnique (beta:=beta) beta b f1 f2 hβ rfl
  simp only [wp, PostCond.noThrow, pure, FcanonicUnique_check, ULift.down_up] at hspec
  have heq : f1 = f2 := hspec ⟨hcan1, hcan2, hF2Req⟩
  -- Since f1 = f2, their parities are the same
  rw [← heq]
  exact hFNeven1

/-- Zero normalized value gives normalized-even parity.

This is the zero branch used in Coq `Pff2FlocqAux.pff_round_NE_is_round`: if
the normalized representative denotes zero, its mantissa is zero, hence even. -/
theorem FNeven_of_Fnormalize_F2R_zero {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta)
    (hβ : 0 < beta)
    (hzero : _root_.F2R (beta:=beta)
        (Fnormalize (beta:=beta) beta b precision p) = 0) :
    FNeven (beta:=beta) b radix precision p := by
  unfold FNeven Feven
  let nf := Fnormalize (beta:=beta) beta b precision p
  have hnum_zero : nf.Fnum = 0 := by
    have hmul : (nf.Fnum : ℝ) * (beta : ℝ) ^ nf.Fexp = 0 := by
      simpa [_root_.F2R, FloatSpec.Core.Defs.F2R, nf] using hzero
    rcases mul_eq_zero.mp hmul with hmant | hpow
    · exact_mod_cast hmant
    · have hbne : (beta : ℝ) ≠ 0 := by
        exact_mod_cast (ne_of_gt hβ)
      exact False.elim ((zpow_ne_zero nf.Fexp hbne) hpow)
  change Even nf.Fnum
  simpa [hnum_zero]

-- Normalized-even under negation (Coq: FNevenFop)
noncomputable def FNevenFop_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FNevenFop` — if a float is normalized-even, its negation is normalized-even. -/
theorem FNevenFop {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜FNeven (beta:=beta) b radix precision p⌝⦄
    (pure (FNevenFop_check (beta:=beta) b radix precision p) : Id Unit)
    ⦃⇓_ => ⌜FNeven (beta:=beta) b radix precision (FloatSpec.Calc.Operations.Fopp (beta:=beta) p)⌝⦄ := by
  intro h
  simp only [wp, PostCond.noThrow, pure, FNevenFop_check] at h ⊢
  simp only [FNeven, Feven] at h ⊢
  have hnorm_opp : Fnormalize (beta:=beta) beta b precision
        (FloatSpec.Calc.Operations.Fopp (beta:=beta) p) =
      FloatSpec.Calc.Operations.Fopp (beta:=beta)
        (Fnormalize (beta:=beta) beta b precision p) := by
    have hs := Fnormalize_Fopp (beta:=beta) beta b precision p
    simpa only [wp, PostCond.noThrow, pure, Fnormalize_Fopp_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using hs True.intro
  rw [hnorm_opp]
  simpa [FloatSpec.Calc.Operations.Fopp] using h.neg

-- Successor parity for normalized predicates (Coq: FNoddSuc / FNevenSuc)
noncomputable def FNoddSuc_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

noncomputable def FNevenSuc_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

-- Disjunction for normalized parity (Coq: FNevenOrFNodd)
noncomputable def FNevenOrFNodd_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem FNevenOrFNodd {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜True⌝⦄
    (pure (FNevenOrFNodd_check (beta:=beta) b radix precision p) : Id Unit)
    ⦃⇓_ => ⌜FNeven (beta:=beta) b radix precision p ∨ FNodd (beta:=beta) b radix precision p⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, FNevenOrFNodd_check, FNeven, FNodd, Feven, Fodd]
  exact Int.even_or_odd (Fnormalize (beta:=beta) beta b precision p).Fnum

-- Incompatibility of normalized odd and even (Coq: FnOddNEven)
noncomputable def FnOddNEven_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat)
    (n : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem FnOddNEven {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat)
    (n : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜FNodd (beta:=beta) b radix precision n⌝⦄
    (pure (FnOddNEven_check (beta:=beta) b radix precision n) : Id Unit)
    ⦃⇓_ => ⌜¬ FNeven (beta:=beta) b radix precision n⌝⦄ := by
  intro hOdd
  simp only [wp, PostCond.noThrow, pure, FnOddNEven_check, FNeven, FNodd, Feven, Fodd] at *
  exact Int.not_even_iff_odd.mpr hOdd

-- Compatibility of `Closest` w.r.t. equalities (Coq: `ClosestCompatible_from_structural_payload`)
noncomputable def ClosestCompatible_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ) : Unit :=
  ()

/-- Coq: `ClosestCompatible_from_structural_payload` — the `Closest` relation is compatible
    with equality of reals and representations. -/
theorem ClosestCompatible_from_structural_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ) :
    ⦃⌜True⌝⦄
    (pure (ClosestCompatible_check (beta:=beta) bo radix) : Id Unit)
    ⦃⇓_ => ⌜CompatiblePGeneric (Closest (beta:=beta) bo radix)⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, ClosestCompatible_check, PredTrans.pure, PredTrans.apply,
             Id.run, ULift.down]
  intro r1 r2 p q hp hr hpq
  subst r2
  subst q
  exact hp

/-- Coq `ClosestCompatible`: compatibility uses equality of represented
values and requires boundedness of the replacement float. -/
theorem ClosestCompatible {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) :
    CompatibleP b (Closest (beta:=beta) b radix) := by
  intro r₁ r₂ p q hp hr hpq hqBounded
  refine ⟨hqBounded, ?_⟩
  intro f hfBounded
  rw [← hr, ← hpq]
  exact hp.2 f hfBounded

-- Minimal conditions imply `Closest r min` (Coq: `ClosestMin`)
noncomputable def ClosestMin_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (r : ℝ)
    (min max : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `ClosestMin` — if `min` is the greatest bounded float below `r`,
    `max` is the least bounded float above `r`, and the midpoint leans toward
    `min`, then `min` is a closest bounded float to `r`. -/
theorem ClosestMin {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radixZ : Int) (radixR : ℝ)
    (r : ℝ)
    (min max : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜isMin (beta:=beta) bo radixZ r min ∧
        isMax (beta:=beta) bo radixZ r max ∧
        2 * r ≤ _root_.F2R (beta:=beta) min + _root_.F2R (beta:=beta) max⌝⦄
    (pure (ClosestMin_check (beta:=beta) bo radixR r min max) : Id Unit)
    ⦃⇓_ => ⌜Closest (beta:=beta) bo radixR r min⌝⦄ := by
  intro h
  rcases h with ⟨hmin, hmax, hmid⟩
  rcases hmin with ⟨hminBounded, hmin_le, hminGreatest⟩
  rcases hmax with ⟨_hmaxBounded, _hr_le_max, hmaxLeast⟩
  simp only [wp, PostCond.noThrow, pure, ClosestMin_check, Id.run, ULift.up_down]
  constructor
  · exact hminBounded
  · intro f hfBounded
    by_cases hf_le : _root_.F2R (beta:=beta) f ≤ r
    · have hf_min : _root_.F2R (beta:=beta) f ≤ _root_.F2R (beta:=beta) min :=
        hminGreatest f hfBounded hf_le
      have hmin_sub_nonpos : _root_.F2R (beta:=beta) min - r ≤ 0 := by
        linarith
      have hf_sub_nonpos : _root_.F2R (beta:=beta) f - r ≤ 0 := by
        linarith
      rw [abs_of_nonpos hmin_sub_nonpos, abs_of_nonpos hf_sub_nonpos]
      linarith
    · have hr_le_f : r ≤ _root_.F2R (beta:=beta) f := le_of_lt (lt_of_not_ge hf_le)
      have hmax_f : _root_.F2R (beta:=beta) max ≤ _root_.F2R (beta:=beta) f :=
        hmaxLeast f hfBounded hr_le_f
      have hmin_sub_nonpos : _root_.F2R (beta:=beta) min - r ≤ 0 := by
        linarith
      have hf_sub_nonneg : 0 ≤ _root_.F2R (beta:=beta) f - r := by
        linarith
      rw [abs_of_nonpos hmin_sub_nonpos, abs_of_nonneg hf_sub_nonneg]
      linarith

-- Maximal conditions imply `Closest r max` (Coq: `ClosestMax`)
noncomputable def ClosestMax_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (r : ℝ)
    (min max : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `ClosestMax` — if `min` is the greatest bounded float below `r`,
    `max` is the least bounded float above `r`, and the midpoint leans toward
    `max`, then `max` is a closest bounded float to `r`. -/
theorem ClosestMax {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radixZ : Int) (radixR : ℝ)
    (r : ℝ)
    (min max : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜isMin (beta:=beta) bo radixZ r min ∧
        isMax (beta:=beta) bo radixZ r max ∧
        _root_.F2R (beta:=beta) min + _root_.F2R (beta:=beta) max ≤ 2 * r⌝⦄
    (pure (ClosestMax_check (beta:=beta) bo radixR r min max) : Id Unit)
    ⦃⇓_ => ⌜Closest (beta:=beta) bo radixR r max⌝⦄ := by
  intro h
  rcases h with ⟨hmin, hmax, hmid⟩
  rcases hmin with ⟨_hminBounded, _hmin_le, hminGreatest⟩
  rcases hmax with ⟨hmaxBounded, hr_le_max, hmaxLeast⟩
  simp only [wp, PostCond.noThrow, pure, ClosestMax_check, Id.run, ULift.up_down]
  constructor
  · exact hmaxBounded
  · intro f hfBounded
    by_cases hr_le_f : r ≤ _root_.F2R (beta:=beta) f
    · have hmax_f : _root_.F2R (beta:=beta) max ≤ _root_.F2R (beta:=beta) f :=
        hmaxLeast f hfBounded hr_le_f
      have hmax_sub_nonneg : 0 ≤ _root_.F2R (beta:=beta) max - r := by
        linarith
      have hf_sub_nonneg : 0 ≤ _root_.F2R (beta:=beta) f - r := by
        linarith
      rw [abs_of_nonneg hmax_sub_nonneg, abs_of_nonneg hf_sub_nonneg]
      linarith
    · have hf_le_r : _root_.F2R (beta:=beta) f ≤ r := le_of_lt (lt_of_not_ge hr_le_f)
      have hf_min : _root_.F2R (beta:=beta) f ≤ _root_.F2R (beta:=beta) min :=
        hminGreatest f hfBounded hf_le_r
      have hmax_sub_nonneg : 0 ≤ _root_.F2R (beta:=beta) max - r := by
        linarith
      have hf_sub_nonpos : _root_.F2R (beta:=beta) f - r ≤ 0 := by
        linarith
      rw [abs_of_nonneg hmax_sub_nonneg, abs_of_nonpos hf_sub_nonpos]
      linarith

/-- Coq: `RND_Closest_correct`.

The nearest result is closest because the selected branch is either the
restored lower or upper extremal rounded value. -/
theorem RND_Closest_correct_from_min_max {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int)
    (choice : Int → Bool) (r : ℝ) :
    ⦃⌜isMin (beta:=beta) b radix r
          (RND_Min (beta:=beta) b radix p r) ∧
        isMax (beta:=beta) b radix r
          (RND_Max (beta:=beta) b radix p r)⌝⦄
    (pure (RND_Closest_correct_check (beta:=beta) b radix p choice r) : Id Unit)
    ⦃⇓_ => ⌜Closest (beta:=beta) b (radix : ℝ) r
        (RND_Closest (beta:=beta) b radix p choice r)⌝⦄ := by
  intro h
  rcases h with ⟨hMin, hMax⟩
  simp only [wp, PostCond.noThrow, pure, RND_Closest_correct_check,
    Id.run, ULift.up_down]
  let rd := RND_Min (beta:=beta) b radix p r
  let ru := RND_Max (beta:=beta) b radix p r
  have hrd_le : _root_.F2R (beta:=beta) rd ≤ r := hMin.2.1
  have hle_ru : r ≤ _root_.F2R (beta:=beta) ru := hMax.2.1
  have hrd_nonpos : _root_.F2R (beta:=beta) rd - r ≤ 0 := by
    dsimp [rd] at hrd_le ⊢
    linarith
  have hru_nonneg : 0 ≤ _root_.F2R (beta:=beta) ru - r := by
    dsimp [ru] at hle_ru ⊢
    linarith
  have hClosestMin := ClosestMin (beta:=beta) b radix (radix : ℝ) r rd ru
  have hClosestMax := ClosestMax (beta:=beta) b radix (radix : ℝ) r rd ru
  have closest_min
      (hmid : 2 * r ≤ _root_.F2R (beta:=beta) rd + _root_.F2R (beta:=beta) ru) :
      Closest (beta:=beta) b (radix : ℝ) r rd := by
    simpa only [wp, PostCond.noThrow, pure, ClosestMin_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, rd, ru, Int.cast_ofNat] using
      hClosestMin ⟨hMin, hMax, hmid⟩
  have closest_max
      (hmid : _root_.F2R (beta:=beta) rd + _root_.F2R (beta:=beta) ru ≤ 2 * r) :
      Closest (beta:=beta) b (radix : ℝ) r ru := by
    simpa only [wp, PostCond.noThrow, pure, ClosestMax_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, rd, ru, Int.cast_ofNat] using
      hClosestMax ⟨hMin, hMax, hmid⟩
  unfold RND_Closest
  dsimp only [rd, ru]
  by_cases hle :
      |_root_.F2R (beta:=beta) ru - r| ≤
        |_root_.F2R (beta:=beta) rd - r|
  · rw [ite_eq_left hle]
    by_cases hlt :
        |_root_.F2R (beta:=beta) ru - r| <
          |_root_.F2R (beta:=beta) rd - r|
    · rw [ite_eq_left hlt]
      have hmid : _root_.F2R (beta:=beta) rd + _root_.F2R (beta:=beta) ru ≤
          2 * r := by
        rw [abs_of_nonneg hru_nonneg, abs_of_nonpos hrd_nonpos] at hlt
        linarith
      exact closest_max hmid
    · rw [ite_eq_right hlt]
      have hdist_eq :
          |_root_.F2R (beta:=beta) ru - r| =
            |_root_.F2R (beta:=beta) rd - r| :=
        le_antisymm hle (le_of_not_gt hlt)
      have hmid_min :
          2 * r ≤ _root_.F2R (beta:=beta) rd + _root_.F2R (beta:=beta) ru := by
        rw [abs_of_nonneg hru_nonneg, abs_of_nonpos hrd_nonpos] at hdist_eq
        linarith
      have hmid_max :
          _root_.F2R (beta:=beta) rd + _root_.F2R (beta:=beta) ru ≤ 2 * r := by
        rw [abs_of_nonneg hru_nonneg, abs_of_nonpos hrd_nonpos] at hdist_eq
        linarith
      by_cases hchoice : choice (FloatSpec.Core.Raux.Zfloor
          (FloatSpec.Core.Generic_fmt.scaled_mantissa beta
            (FLT_exp (-b.dExp) p) r))
      · rw [ite_eq_left hchoice]
        exact closest_max hmid_max
      · rw [ite_eq_right hchoice]
        exact closest_min hmid_min
  · rw [ite_eq_right hle]
    have hlt_abs :
        |_root_.F2R (beta:=beta) rd - r| <
          |_root_.F2R (beta:=beta) ru - r| :=
      lt_of_not_ge hle
    have hmid : 2 * r ≤ _root_.F2R (beta:=beta) rd +
        _root_.F2R (beta:=beta) ru := by
      rw [abs_of_nonpos hrd_nonpos, abs_of_nonneg hru_nonneg] at hlt_abs
      linarith
    exact closest_min hmid

-- Existence of a closest representation (Coq: `ClosestTotal_from_extrema_payload`)
noncomputable def ClosestTotal_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ) (r : ℝ) : Unit :=
  ()

/-- Coq: `ClosestTotal_from_extrema_payload`.

Upstream proves closest-point existence from `MinEx_from_finite_box_payload` and `MaxEx_from_finite_box_payload`, then chooses
the side with the smaller distance to `r`.  The current Lean theorem keeps
those two existence constructions explicit because the corresponding finite
bounded-float enumeration theorems are defined later in this file. -/
theorem ClosestTotal_from_extrema_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radixZ : Int) (radixR : ℝ) (r : ℝ) :
    ⦃⌜TotalP (isMin (beta:=beta) bo radixZ) ∧
        TotalP (isMax (beta:=beta) bo radixZ)⌝⦄
    (pure (ClosestTotal_check (beta:=beta) bo radixR r) : Id Unit)
    ⦃⇓_ => ⌜∃ p : FloatSpec.Core.Defs.FlocqFloat beta,
      Closest (beta:=beta) bo radixR r p⌝⦄ := by
  intro h
  rcases h with ⟨hMinTotal, hMaxTotal⟩
  simp only [wp, PostCond.noThrow, pure, ClosestTotal_check,
    Id.run, ULift.up_down]
  obtain ⟨min, hmin⟩ := hMinTotal r
  obtain ⟨max, hmax⟩ := hMaxTotal r
  rcases hmin with ⟨hminBounded, hminLe, hminGreatest⟩
  rcases hmax with ⟨hmaxBounded, hmaxGe, hmaxLeast⟩
  by_cases hdist :
      |_root_.F2R (beta:=beta) min - r| ≤
        |_root_.F2R (beta:=beta) max - r|
  · refine ⟨min, ?_⟩
    have hminNonpos : _root_.F2R (beta:=beta) min - r ≤ 0 := by
      linarith
    have hmaxNonneg : 0 ≤ _root_.F2R (beta:=beta) max - r := by
      linarith
    have hmid :
        2 * r ≤ _root_.F2R (beta:=beta) min +
          _root_.F2R (beta:=beta) max := by
      rw [abs_of_nonpos hminNonpos, abs_of_nonneg hmaxNonneg] at hdist
      linarith
    have hClosestMin := ClosestMin (beta:=beta) bo radixZ radixR r min max
    simpa [wp, PostCond.noThrow, pure, ClosestMin_check, Id.run, ULift.up_down, Int.cast_ofNat] using
      hClosestMin ⟨⟨hminBounded, hminLe, hminGreatest⟩,
        ⟨hmaxBounded, hmaxGe, hmaxLeast⟩, hmid⟩
  · push Not at hdist
    refine ⟨max, ?_⟩
    have hminNonpos : _root_.F2R (beta:=beta) min - r ≤ 0 := by
      linarith
    have hmaxNonneg : 0 ≤ _root_.F2R (beta:=beta) max - r := by
      linarith
    have hmid :
        _root_.F2R (beta:=beta) min +
          _root_.F2R (beta:=beta) max ≤ 2 * r := by
      rw [abs_of_nonpos hminNonpos, abs_of_nonneg hmaxNonneg] at hdist
      linarith
    have hClosestMax := ClosestMax (beta:=beta) bo radixZ radixR r min max
    simpa [wp, PostCond.noThrow, pure, ClosestMax_check, Id.run, ULift.up_down, Int.cast_ofNat] using
      hClosestMax ⟨⟨hminBounded, hminLe, hminGreatest⟩,
        ⟨hmaxBounded, hmaxGe, hmaxLeast⟩, hmid⟩

-- Disjunction: any candidate is either a min or a max (Coq: `ClosestMinOrMax`)
noncomputable def ClosestMinOrMax_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ) : Unit :=
  ()

/-- Coq: `ClosestMinOrMax` — every closest rounded float is either the
    greatest bounded float below `r` or the least bounded float above `r`. -/
theorem ClosestMinOrMax_from_split_radix_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radixZ : Int) (radixR : ℝ) :
    ⦃⌜True⌝⦄
    (pure (ClosestMinOrMax_check (beta:=beta) bo radixR) : Id Unit)
    ⦃⇓_ => ⌜MinOrMaxP_float (beta:=beta) bo radixZ
      (Closest (beta:=beta) bo radixR)⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, ClosestMinOrMax_check, MinOrMaxP_float, Id.run, ULift.up_down]
  intro r p hp
  rcases hp with ⟨hpBounded, hpClosest⟩
  by_cases hp_le : _root_.F2R (beta:=beta) p ≤ r
  · left
    refine ⟨hpBounded, hp_le, ?_⟩
    intro f hfBounded hf_le
    have hdist := hpClosest f hfBounded
    have hp_nonpos : _root_.F2R (beta:=beta) p - r ≤ 0 := by linarith
    have hf_nonpos : _root_.F2R (beta:=beta) f - r ≤ 0 := by linarith
    rw [abs_of_nonpos hp_nonpos, abs_of_nonpos hf_nonpos] at hdist
    linarith
  · right
    have hr_le_p : r ≤ _root_.F2R (beta:=beta) p := le_of_lt (lt_of_not_ge hp_le)
    refine ⟨hpBounded, hr_le_p, ?_⟩
    intro f hfBounded hr_le_f
    have hdist := hpClosest f hfBounded
    have hp_nonneg : 0 ≤ _root_.F2R (beta:=beta) p - r := by linarith
    have hf_nonneg : 0 ≤ _root_.F2R (beta:=beta) f - r := by linarith
    rw [abs_of_nonneg hp_nonneg, abs_of_nonneg hf_nonneg] at hdist
    linarith

/-- Coq `ClosestMinOrMax`. -/
theorem ClosestMinOrMax {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) :
    ⦃⌜True⌝⦄
    (pure (ClosestMinOrMax_check (beta:=beta) b (beta : ℝ)) : Id Unit)
    ⦃⇓_ => ⌜MinOrMaxP (beta:=beta) b
      (Closest (beta:=beta) b (beta : ℝ))⌝⦄ := by
  exact ClosestMinOrMax_from_split_radix_payload
    (beta:=beta) b beta (beta : ℝ)

-- Zero case for Closest rounding (Coq: `ClosestZero`)
noncomputable def ClosestZero_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ) (r : ℝ)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `ClosestZero` — if `x` is a closest rounding of `r` and `r = 0`,
    then the real value of `x` is `0`. We phrase this using the project
    `Closest` predicate and `F2R` interpretation.

    Note: `Closest` is now concrete; this port still carries explicit
    hypotheses exposing the Coq proof obligations:
    - `Fbounded' bo x`: the float is bounded
    - `hClosestProp`: `x` minimizes distance to `r` among bounded floats
    These make the theorem provable and match the original Coq semantics. -/
theorem ClosestZero_from_expanded_closest_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ) (r : ℝ)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Closest (beta:=beta) bo radix r x ∧ r = 0 ∧
        Fbounded' bo x ∧
        (∀ g : FloatSpec.Core.Defs.FlocqFloat beta, Fbounded' bo g → |_root_.F2R x - r| ≤ |_root_.F2R g - r|)⌝⦄
    (pure (ClosestZero_check (beta:=beta) bo radix r x) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R x = 0⌝⦄ := by
  intro ⟨_hClosest, hr0, hBounded, hMin⟩
  simp only [wp, PostCond.noThrow, pure, ClosestZero_check]
  subst hr0
  simp only [sub_zero] at hMin
  -- We need to show F2R x = 0
  -- Strategy: the zero float ⟨0, -bo.dExp⟩ is bounded (if bo.vNum > 0)
  -- and F2R ⟨0, -bo.dExp⟩ = 0, so |F2R x| ≤ 0, hence F2R x = 0
  by_cases hvNum : 0 < bo.vNum
  · -- Case: bo.vNum > 0, so the zero float is bounded
    let zeroFloat : FloatSpec.Core.Defs.FlocqFloat beta := ⟨0, -bo.dExp⟩
    have hZeroBounded : Fbounded' (beta:=beta) bo zeroFloat := by
      constructor
      · show |zeroFloat.Fnum| < bo.vNum
        simp only [zeroFloat, abs_zero]
        exact hvNum
      · rfl
    have hZeroF2R : _root_.F2R zeroFloat = 0 := by
      unfold _root_.F2R FloatSpec.Core.Defs.F2R zeroFloat
      ring
    specialize hMin zeroFloat hZeroBounded
    rw [hZeroF2R, abs_zero] at hMin
    exact abs_eq_zero.mp (le_antisymm hMin (abs_nonneg _))
  · -- Case: bo.vNum ≤ 0, so no float is bounded (Fbounded' is vacuously false)
    -- But we have Fbounded' bo x from hClosest, contradiction
    push Not at hvNum
    obtain ⟨hNumBound, _⟩ := hBounded
    have : |x.Fnum| ≥ 0 := abs_nonneg _
    omega

-- Coq: `ClosestZero2` — a closest rounding of zero has real value zero.
noncomputable def ClosestZero2_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ) (r : ℝ)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `ClosestZero2`.

If `x` is a closest rounded float for `r`, and `r = 0`, then `F2R x = 0`.
This exact-name wrapper exposes the upstream theorem name while reusing the
already restored local `ClosestZero` payload. -/
theorem ClosestZero2 {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ) (r : ℝ)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Closest (beta:=beta) bo radix r x ∧ r = 0⌝⦄
    (pure (ClosestZero2_check (beta:=beta) bo radix r x) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R x = 0⌝⦄ := by
  intro h
  rcases h with ⟨hClosest, hr0⟩
  have hBounded : Fbounded' (beta:=beta) bo x := by
    simpa [Fbounded'] using hClosest.1
  have hMin :
      ∀ g : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded' (beta:=beta) bo g →
          |_root_.F2R x - r| ≤ |_root_.F2R g - r| := by
    intro g hg
    exact hClosest.2 g (by simpa [Fbounded'] using hg)
  have hzero := ClosestZero_from_expanded_closest_payload (beta:=beta) bo radix r x
  simpa only [wp, PostCond.noThrow, pure, ClosestZero2_check, Id.run,
    ULift.up_down, Int.cast_ofNat] using hzero ⟨hClosest, hr0, hBounded, hMin⟩

-- ---------------------------------------------------------------------------
-- Min/Max existence over finite lists (from Coq Pff.v)

/-
Coq: `FboundedMboundPos`

Construct a bounded float whose real value equals `m * radix^z` when the
mantissa/exponent bounds hold. The real radix is obtained from the integer
radix via coercion so that `powerRZ` can be reused verbatim.
-/
noncomputable def FboundedMboundPos_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (z m : Int) : Unit :=
  ()

/-- Coq: `FboundedMboundPos`.

The original theorem lives under the Pff section assumptions connecting the
float radix to `radix` and `b.vNum` to `Zpower_nat radix precision`; those
assumptions are explicit here. -/
theorem FboundedMboundPos {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (precision : Nat)
    (z m : Int) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 0 < precision ∧
        b.vNum = Zpower_nat radix precision ∧
        0 ≤ m ∧ m ≤ Zpower_nat radix precision ∧
        -b.dExp ≤ z⌝⦄
    (pure (FboundedMboundPos_check (beta:=beta) b radix z m) : Id Unit)
    ⦃⇓_ => ⌜∃ c : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) b c ∧
        _root_.F2R (beta:=beta) c =
          (m : ℝ) * (radix : ℝ) ^ z⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hradix_gt, hprec_pos, hvNum, hm_nonneg, hm_le, hz⟩
  simp only [wp, PostCond.noThrow, pure, FboundedMboundPos_check, Id.run,
    ULift.up_down]
  have hradix_pos_int : (0 : Int) < radix := lt_trans (by decide) hradix_gt
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by exact_mod_cast hradix_pos_int
  have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
  have hbound_nonneg : 0 ≤ Zpower_nat radix precision := by
    unfold Zpower_nat
    exact pow_nonneg (le_of_lt hradix_pos_int) precision
  rcases lt_or_eq_of_le hm_le with hm_lt | hm_eq
  · refine ⟨⟨m, z⟩, ?_, ?_⟩
    · constructor
      · have habs : |m| = m := abs_of_nonneg hm_nonneg
        simpa [Fbounded, hvNum, habs] using hm_lt
      · exact hz
    · subst beta
      simp [_root_.F2R, FloatSpec.Core.Defs.F2R]
  · let mant : Int := radix ^ (precision - 1)
    refine ⟨⟨mant, z + 1⟩, ?_, ?_⟩
    · constructor
      · have hmant_pos : 0 < mant := by
          dsimp [mant]
          exact pow_pos hradix_pos_int (precision - 1)
        have hsucc : Nat.succ (precision - 1) = precision := by
          omega
        have hbound_eq : Zpower_nat radix precision = mant * radix := by
          dsimp [mant]
          unfold Zpower_nat
          calc
            radix ^ precision = radix ^ Nat.succ (precision - 1) := by rw [hsucc]
            _ = radix ^ (precision - 1) * radix := by rw [pow_succ]
        have hmant_lt : mant < Zpower_nat radix precision := by
          rw [hbound_eq]
          nlinarith
        have habs : |mant| = mant := abs_of_nonneg (le_of_lt hmant_pos)
        simpa [Fbounded, hvNum, habs] using hmant_lt
      · change -b.dExp ≤ z + 1
        omega
    · subst beta
      have hsucc : Nat.succ (precision - 1) = precision := by
        omega
      have hpow_real :
          ((Zpower_nat radix precision : Int) : ℝ) =
            (mant : ℝ) * (radix : ℝ) := by
        dsimp [mant]
        unfold Zpower_nat
        calc
          ((radix ^ precision : Int) : ℝ) =
              ((radix ^ Nat.succ (precision - 1) : Int) : ℝ) := by rw [hsucc]
          _ = ((radix ^ (precision - 1) * radix : Int) : ℝ) := by rw [pow_succ]
          _ = ((radix ^ (precision - 1) : Int) : ℝ) * (radix : ℝ) := by
                rw [Int.cast_mul, Int.cast_pow]
      calc
        _root_.F2R (beta:=radix) (FloatSpec.Core.Defs.FlocqFloat.mk mant (z + 1)) =
            (mant : ℝ) * (radix : ℝ) ^ (z + 1) := by
              simp [_root_.F2R, FloatSpec.Core.Defs.F2R]
        _ = (mant : ℝ) * ((radix : ℝ) ^ z * (radix : ℝ)) := by
              rw [zpow_add₀ hradix_ne, zpow_one]
        _ = ((Zpower_nat radix precision : Int) : ℝ) * (radix : ℝ) ^ z := by
              rw [hpow_real]
              ring
        _ = (m : ℝ) * (radix : ℝ) ^ z := by
              rw [hm_eq]

/-- Coq: `FboundedMbound` — extends `FboundedMboundPos` by allowing
negative mantissas via symmetry. -/
noncomputable def FboundedMbound_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (z m : Int) : Unit :=
  ()

/-- Coq: `FboundedMbound` — construct a bounded float for any signed
    mantissa whose absolute value is within the precision bound. -/
theorem FboundedMbound {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (precision : Nat)
    (z m : Int) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 0 < precision ∧
        b.vNum = Zpower_nat radix precision ∧
        |m| ≤ Zpower_nat radix precision ∧
        -b.dExp ≤ z⌝⦄
    (pure (FboundedMbound_check (beta:=beta) b radix z m) : Id Unit)
    ⦃⇓_ => ⌜∃ c : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) b c ∧
        _root_.F2R (beta:=beta) c =
          (m : ℝ) * (radix : ℝ) ^ z⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hradix_gt, hprec_pos, hvNum, hm_abs_le, hz⟩
  simp only [wp, PostCond.noThrow, pure, FboundedMbound_check, Id.run,
    ULift.up_down]
  by_cases hm_nonneg : 0 ≤ m
  · have hm_abs : |m| = m := abs_of_nonneg hm_nonneg
    have hpos := FboundedMboundPos (beta:=beta) b radix precision z m
    have hres := hpos ⟨hbeta, hradix_gt, hprec_pos, hvNum, hm_nonneg, by
      simpa [hm_abs] using hm_abs_le, hz⟩
    simpa [wp, PostCond.noThrow, pure, FboundedMboundPos_check] using hres
  · have hm_neg : m < 0 := lt_of_not_ge hm_nonneg
    have hm_abs : |m| = -m := abs_of_neg hm_neg
    have hm_pos_nonneg : 0 ≤ -m := by omega
    have hm_pos_le : -m ≤ Zpower_nat radix precision := by
      simpa [hm_abs] using hm_abs_le
    have hpos := FboundedMboundPos (beta:=beta) b radix precision z (-m)
    have hres := hpos ⟨hbeta, hradix_gt, hprec_pos, hvNum, hm_pos_nonneg, hm_pos_le, hz⟩
    rcases hres with ⟨c, hcBounded, hcVal⟩
    refine ⟨Fopp (beta:=beta) c, ?_, ?_⟩
    · simpa [Fbounded, Fopp, FloatSpec.Calc.Operations.Fopp, abs_neg] using hcBounded
    · have hFopp :
          _root_.F2R (beta:=beta) (Fopp (beta:=beta) c) =
            - _root_.F2R (beta:=beta) c := by
        simp [_root_.F2R, FloatSpec.Core.Defs.F2R, Fopp,
          FloatSpec.Calc.Operations.Fopp, neg_mul]
      calc
        _root_.F2R (beta:=beta) (Fopp (beta:=beta) c) =
            - _root_.F2R (beta:=beta) c := hFopp
        _ = - ((-m : Int) : ℝ) * (radix : ℝ) ^ z := by
              rw [hcVal]
              ring
        _ = (m : ℝ) * (radix : ℝ) ^ z := by
              norm_num

noncomputable def FboundedMbound2Pos_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (z m : Int) : Unit :=
  ()

/-- Coq: `FboundedMbound2Pos`.

Positive mantissas up to the precision bound have a bounded float
representative whose exponent is no smaller than the requested exponent. -/
theorem FboundedMbound2Pos {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (precision : Nat)
    (z m : Int) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 0 < precision ∧
        b.vNum = Zpower_nat radix precision ∧
        0 ≤ m ∧ m ≤ Zpower_nat radix precision ∧
        -b.dExp ≤ z⌝⦄
    (pure (FboundedMbound2Pos_check (beta:=beta) b radix z m) : Id Unit)
    ⦃⇓_ => ⌜∃ c : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) b c ∧
        _root_.F2R (beta:=beta) c =
          (m : ℝ) * (radix : ℝ) ^ z ∧
        z ≤ c.Fexp⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hradix_gt, hprec_pos, hvNum, hm_nonneg, hm_le, hz⟩
  simp only [wp, PostCond.noThrow, pure, FboundedMbound2Pos_check, Id.run,
    ULift.up_down]
  have hradix_pos_int : (0 : Int) < radix := lt_trans (by decide) hradix_gt
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by exact_mod_cast hradix_pos_int
  have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
  rcases lt_or_eq_of_le hm_le with hm_lt | hm_eq
  · refine ⟨⟨m, z⟩, ?_, ?_, le_rfl⟩
    · constructor
      · have habs : |m| = m := abs_of_nonneg hm_nonneg
        simpa [Fbounded, hvNum, habs] using hm_lt
      · exact hz
    · subst beta
      simp [_root_.F2R, FloatSpec.Core.Defs.F2R]
  · let mant : Int := radix ^ (precision - 1)
    refine ⟨⟨mant, z + 1⟩, ?_, ?_, ?_⟩
    · constructor
      · have hmant_pos : 0 < mant := by
          dsimp [mant]
          exact pow_pos hradix_pos_int (precision - 1)
        have hsucc : Nat.succ (precision - 1) = precision := by
          omega
        have hbound_eq : Zpower_nat radix precision = mant * radix := by
          dsimp [mant]
          unfold Zpower_nat
          calc
            radix ^ precision = radix ^ Nat.succ (precision - 1) := by rw [hsucc]
            _ = radix ^ (precision - 1) * radix := by rw [pow_succ]
        have hmant_lt : mant < Zpower_nat radix precision := by
          rw [hbound_eq]
          nlinarith
        have habs : |mant| = mant := abs_of_nonneg (le_of_lt hmant_pos)
        simpa [Fbounded, hvNum, habs] using hmant_lt
      · change -b.dExp ≤ z + 1
        omega
    · subst beta
      have hsucc : Nat.succ (precision - 1) = precision := by
        omega
      have hpow_real :
          ((Zpower_nat radix precision : Int) : ℝ) =
            (mant : ℝ) * (radix : ℝ) := by
        dsimp [mant]
        unfold Zpower_nat
        calc
          ((radix ^ precision : Int) : ℝ) =
              ((radix ^ Nat.succ (precision - 1) : Int) : ℝ) := by rw [hsucc]
          _ = ((radix ^ (precision - 1) * radix : Int) : ℝ) := by rw [pow_succ]
          _ = ((radix ^ (precision - 1) : Int) : ℝ) * (radix : ℝ) := by
                rw [Int.cast_mul, Int.cast_pow]
      calc
        _root_.F2R (beta:=radix) (FloatSpec.Core.Defs.FlocqFloat.mk mant (z + 1)) =
            (mant : ℝ) * (radix : ℝ) ^ (z + 1) := by
              simp [_root_.F2R, FloatSpec.Core.Defs.F2R]
        _ = (mant : ℝ) * ((radix : ℝ) ^ z * (radix : ℝ)) := by
              rw [zpow_add₀ hradix_ne, zpow_one]
        _ = ((Zpower_nat radix precision : Int) : ℝ) * (radix : ℝ) ^ z := by
              rw [hpow_real]
              ring
        _ = (m : ℝ) * (radix : ℝ) ^ z := by
              rw [hm_eq]
    · change z ≤ z + 1
      omega

/-- Precision-independent strengthening of Coq `ClosestZero`. -/
theorem ClosestZero_from_closest_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (r : ℝ)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Closest (beta:=beta) b (beta : ℝ) r x ∧ r = 0⌝⦄
    (pure (ClosestZero_check (beta:=beta) b (beta : ℝ) r x) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) x = 0⌝⦄ := by
  intro ⟨hClosest, hr⟩
  exact ClosestZero_from_expanded_closest_payload
    (beta:=beta) b (beta : ℝ) r x
      ⟨hClosest, hr, hClosest.1, hClosest.2⟩

/-- Coq `ClosestZero` with its exported precision section contract. -/
theorem ClosestZero {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat) (r : ℝ)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜1 < precision ∧ b.vNum = Zpower_nat beta precision ∧
        Closest (beta:=beta) b (beta : ℝ) r x ∧ r = 0⌝⦄
    (pure (ClosestZero_check (beta:=beta) b (beta : ℝ) r x) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) x = 0⌝⦄ := by
  intro ⟨_precisionGreaterThanOne, _pGivesBound, hClosest, hr⟩
  exact ClosestZero_from_closest_payload (beta:=beta) b r x
    ⟨hClosest, hr⟩

noncomputable def FboundedMbound2_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (z m : Int) : Unit :=
  ()

/-- Coq: `FboundedMbound2`.

Signed version of `FboundedMbound2Pos`, reducing negative mantissas by
opposition. -/
theorem FboundedMbound2 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (precision : Nat)
    (z m : Int) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 0 < precision ∧
        b.vNum = Zpower_nat radix precision ∧
        |m| ≤ Zpower_nat radix precision ∧
        -b.dExp ≤ z⌝⦄
    (pure (FboundedMbound2_check (beta:=beta) b radix z m) : Id Unit)
    ⦃⇓_ => ⌜∃ c : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) b c ∧
        _root_.F2R (beta:=beta) c =
          (m : ℝ) * (radix : ℝ) ^ z ∧
        z ≤ c.Fexp⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hradix_gt, hprec_pos, hvNum, hm_abs_le, hz⟩
  simp only [wp, PostCond.noThrow, pure, FboundedMbound2_check, Id.run,
    ULift.up_down]
  by_cases hm_nonneg : 0 ≤ m
  · have hm_abs : |m| = m := abs_of_nonneg hm_nonneg
    have hpos := FboundedMbound2Pos (beta:=beta) b radix precision z m
    simpa only [wp, PostCond.noThrow, pure, FboundedMbound2Pos_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using
      hpos ⟨hbeta, hradix_gt, hprec_pos, hvNum, hm_nonneg,
        by simpa [hm_abs] using hm_abs_le, hz⟩
  · have hm_neg : m < 0 := lt_of_not_ge hm_nonneg
    have hm_abs : |m| = -m := abs_of_neg hm_neg
    have hm_pos_nonneg : 0 ≤ -m := by omega
    have hm_pos_le : -m ≤ Zpower_nat radix precision := by
      simpa [hm_abs] using hm_abs_le
    have hpos := FboundedMbound2Pos (beta:=beta) b radix precision z (-m)
    rcases (by
      simpa only [wp, PostCond.noThrow, pure, FboundedMbound2Pos_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using
        hpos ⟨hbeta, hradix_gt, hprec_pos, hvNum, hm_pos_nonneg, hm_pos_le, hz⟩) with
      ⟨c, hcBounded, hcVal, hcExp⟩
    refine ⟨Fopp (beta:=beta) c, ?_, ?_, ?_⟩
    · simpa [Fbounded, Fopp, FloatSpec.Calc.Operations.Fopp, abs_neg] using hcBounded
    · have hFopp :
          _root_.F2R (beta:=beta) (Fopp (beta:=beta) c) =
            - _root_.F2R (beta:=beta) c := by
        simp [_root_.F2R, FloatSpec.Core.Defs.F2R, Fopp,
          FloatSpec.Calc.Operations.Fopp, neg_mul]
      calc
        _root_.F2R (beta:=beta) (Fopp (beta:=beta) c) =
            - _root_.F2R (beta:=beta) c := hFopp
        _ = - ((-m : Int) : ℝ) * (radix : ℝ) ^ z := by
              rw [hcVal]
              ring
        _ = (m : ℝ) * (radix : ℝ) ^ z := by
              norm_num
    · simpa [Fopp, FloatSpec.Calc.Operations.Fopp] using hcExp

/-!
Coq: `MinExList`

For any real `r` and finite list `L` of floats, either every element of `L`
has value strictly greater than `r`, or there exists an element `min ∈ L`
such that `F2R min ≤ r` and it is minimal among those at most `r`.
-/

/-- Helper: the pure mathematical content of `MinExList`, proved by induction on the list. -/
private lemma minExList_aux {beta : Int} [ValidRadix beta]
    (r : ℝ) (L : List (FloatSpec.Core.Defs.FlocqFloat beta)) :
    (∀ f, f ∈ L → r < _root_.F2R f) ∨
    (∃ min, min ∈ L ∧ _root_.F2R min ≤ r ∧
      ∀ f, f ∈ L → _root_.F2R f ≤ r → _root_.F2R f ≤ _root_.F2R min) := by
  induction L with
  | nil => left; intro f hf; exact absurd hf (List.not_mem_nil)
  | cons hd tl ih =>
    by_cases h : _root_.F2R hd ≤ r
    · right
      rcases ih with hall | ⟨min, hmin_mem, hmin_le, hmin_max⟩
      · exact ⟨hd, List.mem_cons_self, h, fun f hf hfr => by
          rcases List.mem_cons.mp hf with heq | hmem
          · exact heq ▸ le_refl _
          · exact absurd hfr (not_le.mpr (hall f hmem))⟩
      · by_cases hcmp : _root_.F2R hd ≤ _root_.F2R min
        · exact ⟨min, List.mem_cons_of_mem _ hmin_mem, hmin_le, fun f hf hfr => by
            rcases List.mem_cons.mp hf with heq | hmem
            · exact heq ▸ hcmp
            · exact hmin_max f hmem hfr⟩
        · push Not at hcmp
          exact ⟨hd, List.mem_cons_self, h, fun f hf hfr => by
            rcases List.mem_cons.mp hf with heq | hmem
            · exact heq ▸ le_refl _
            · exact le_trans (hmin_max f hmem hfr) (le_of_lt hcmp)⟩
    · push Not at h
      rcases ih with hall | ⟨min, hmin_mem, hmin_le, hmin_max⟩
      · left
        intro f hf
        rcases List.mem_cons.mp hf with heq | hmem
        · exact heq ▸ h
        · exact hall f hmem
      · right
        exact ⟨min, List.mem_cons_of_mem _ hmin_mem, hmin_le, fun f hf hfr => by
          rcases List.mem_cons.mp hf with heq | hmem
          · exact absurd hfr (not_le.mpr (heq ▸ h))
          · exact hmin_max f hmem hfr⟩

noncomputable def MinExList_check {beta : Int} [ValidRadix beta]
    (r : ℝ) (L : List (FloatSpec.Core.Defs.FlocqFloat beta)) : Unit :=
  ()

theorem MinExList {beta : Int} [ValidRadix beta]
    (r : ℝ) (L : List (FloatSpec.Core.Defs.FlocqFloat beta)) :
    ⦃⌜True⌝⦄
    (pure (MinExList_check (beta:=beta) r L) : Id Unit)
    ⦃⇓_ => ⌜(∀ f, f ∈ L → r < _root_.F2R f) ∨
            (∃ min, min ∈ L ∧ _root_.F2R min ≤ r ∧
              ∀ f, f ∈ L → _root_.F2R f ≤ r → _root_.F2R f ≤ _root_.F2R min)⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, MinExList_check, ULift.down_up]
  exact minExList_aux r L

/-!
Coq: `MaxExList`

Dual finite-list selection principle used by the `MaxEx_from_finite_box_payload` construction.  Either
every element of the finite list is strictly below `r`, or the list contains a
least element among those whose value is at least `r`.
-/

private lemma maxExList_aux {beta : Int} [ValidRadix beta]
    (r : ℝ) (L : List (FloatSpec.Core.Defs.FlocqFloat beta)) :
    (∀ f, f ∈ L → _root_.F2R f < r) ∨
    (∃ max, max ∈ L ∧ r ≤ _root_.F2R max ∧
      ∀ f, f ∈ L → r ≤ _root_.F2R f → _root_.F2R max ≤ _root_.F2R f) := by
  induction L with
  | nil => left; intro f hf; exact absurd hf (List.not_mem_nil)
  | cons hd tl ih =>
    by_cases h : r ≤ _root_.F2R hd
    · right
      rcases ih with hall | ⟨max, hmax_mem, hmax_ge, hmax_min⟩
      · exact ⟨hd, List.mem_cons_self, h, fun f hf hfr => by
          rcases List.mem_cons.mp hf with heq | hmem
          · exact heq ▸ le_refl _
          · exact absurd hfr (not_le.mpr (hall f hmem))⟩
      · by_cases hcmp : _root_.F2R hd ≤ _root_.F2R max
        · exact ⟨hd, List.mem_cons_self, h, fun f hf hfr => by
            rcases List.mem_cons.mp hf with heq | hmem
            · exact heq ▸ le_refl _
            · exact le_trans hcmp (hmax_min f hmem hfr)⟩
        · push Not at hcmp
          exact ⟨max, List.mem_cons_of_mem _ hmax_mem, hmax_ge, fun f hf hfr => by
            rcases List.mem_cons.mp hf with heq | hmem
            · exact heq ▸ le_of_lt hcmp
            · exact hmax_min f hmem hfr⟩
    · push Not at h
      rcases ih with hall | ⟨max, hmax_mem, hmax_ge, hmax_min⟩
      · left
        intro f hf
        rcases List.mem_cons.mp hf with heq | hmem
        · exact heq ▸ h
        · exact hall f hmem
      · right
        exact ⟨max, List.mem_cons_of_mem _ hmax_mem, hmax_ge, fun f hf hfr => by
          rcases List.mem_cons.mp hf with heq | hmem
          · exact absurd hfr (not_le.mpr (heq ▸ h))
          · exact hmax_min f hmem hfr⟩

noncomputable def MaxExList_check {beta : Int} [ValidRadix beta]
    (r : ℝ) (L : List (FloatSpec.Core.Defs.FlocqFloat beta)) : Unit :=
  ()

theorem MaxExList {beta : Int} [ValidRadix beta]
    (r : ℝ) (L : List (FloatSpec.Core.Defs.FlocqFloat beta)) :
    ⦃⌜True⌝⦄
    (pure (MaxExList_check (beta:=beta) r L) : Id Unit)
    ⦃⇓_ => ⌜(∀ f, f ∈ L → _root_.F2R f < r) ∨
            (∃ max, max ∈ L ∧ r ≤ _root_.F2R max ∧
              ∀ f, f ∈ L → r ≤ _root_.F2R f → _root_.F2R max ≤ _root_.F2R f)⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, MaxExList_check, ULift.down_up]
  exact maxExList_aux r L

-- Equality under strict-leaning midpoint toward min (Coq: `ClosestMinEq`)
noncomputable def ClosestMinEq_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (r : ℝ)
    (min max p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `ClosestMinEq` — a closest value below a midpoint that leans
strictly toward the lower extremum equals that lower extremum. -/
theorem ClosestMinEq {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radixZ : Int) (radixR : ℝ)
    (r : ℝ)
    (min max p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜isMin (beta:=beta) bo radixZ r min ∧
        isMax (beta:=beta) bo radixZ r max ∧
        2 * r < _root_.F2R min + _root_.F2R max ∧
        Closest (beta:=beta) bo radixR r p⌝⦄
    (pure (ClosestMinEq_check (beta:=beta) bo radixR r min max p) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R p = _root_.F2R min⌝⦄ := by
  intro ⟨hmin, hmax, hmid, hclosestP⟩
  simp [wp, PostCond.noThrow, pure, ClosestMinEq_check]
  have hmin_le := hmin.2.1
  have hmax_ge := hmax.2.1
  have hclosest := hclosestP.2 min hmin.1
  have hMinOrMax := ClosestMinOrMax_from_split_radix_payload (beta:=beta) bo radixZ radixR True.intro
  simp only [wp, PostCond.noThrow, pure, ClosestMinOrMax_check, MinOrMaxP_float,
    Id.run, ULift.up_down] at hMinOrMax
  have hor : _root_.F2R p = _root_.F2R min ∨
      _root_.F2R p = _root_.F2R max := by
    rcases hMinOrMax r p hclosestP with hpMin | hpMax
    · left
      exact le_antisymm
        (hmin.2.2 p hpMin.1 hpMin.2.1)
        (hpMin.2.2 min hmin.1 hmin.2.1)
    · right
      exact le_antisymm
        (hpMax.2.2 max hmax.1 hmax.2.1)
        (hmax.2.2 p hpMax.1 hpMax.2.1)
  rcases hor with h | h
  · exact h
  · exfalso
    rw [h] at hclosest
    have h1 : _root_.F2R min - r ≤ 0 := by linarith
    have h2 : 0 ≤ _root_.F2R max - r := by linarith
    rw [abs_of_nonpos h1, abs_of_nonneg h2] at hclosest
    linarith

-- Equality under strict-leaning midpoint toward max (Coq: `ClosestMaxEq`)
noncomputable def ClosestMaxEq_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (r : ℝ)
    (min max p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `ClosestMaxEq` — the symmetric strict-midpoint result for the
upper extremum. -/
theorem ClosestMaxEq {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radixZ : Int) (radixR : ℝ)
    (r : ℝ)
    (min max p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜isMin (beta:=beta) bo radixZ r min ∧
        isMax (beta:=beta) bo radixZ r max ∧
        _root_.F2R min + _root_.F2R max < 2 * r ∧
        Closest (beta:=beta) bo radixR r p⌝⦄
    (pure (ClosestMaxEq_check (beta:=beta) bo radixR r min max p) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R p = _root_.F2R max⌝⦄ := by
  intro ⟨hmin, hmax, hmid, hclosestP⟩
  simp [wp, PostCond.noThrow, pure, ClosestMaxEq_check]
  have hmin_le := hmin.2.1
  have hmax_ge := hmax.2.1
  have hclosest := hclosestP.2 max hmax.1
  have hMinOrMax := ClosestMinOrMax_from_split_radix_payload (beta:=beta) bo radixZ radixR True.intro
  simp only [wp, PostCond.noThrow, pure, ClosestMinOrMax_check, MinOrMaxP_float,
    Id.run, ULift.up_down] at hMinOrMax
  have hor : _root_.F2R p = _root_.F2R min ∨
      _root_.F2R p = _root_.F2R max := by
    rcases hMinOrMax r p hclosestP with hpMin | hpMax
    · left
      exact le_antisymm
        (hmin.2.2 p hpMin.1 hpMin.2.1)
        (hpMin.2.2 min hmin.1 hmin.2.1)
    · right
      exact le_antisymm
        (hpMax.2.2 max hmax.1 hmax.2.1)
        (hmax.2.2 p hpMax.1 hpMax.2.1)
  rcases hor with h | h
  · exfalso
    rw [h] at hclosest
    have h1 : 0 ≤ _root_.F2R max - r := by linarith
    have h2 : _root_.F2R min - r ≤ 0 := by linarith
    rw [abs_of_nonneg h1, abs_of_nonpos h2] at hclosest
    linarith
  · exact h

-- Monotonicity of the Closest relation (Coq: `ClosestMonotone`)
noncomputable def ClosestMonotone_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ) : Unit :=
  ()

/-- Coq: `ClosestMonotone` — closest rounding is monotone in real value.

The Lean statement targets the float-specific monotonicity predicate; the
generic `MonotonePGeneric` fallback is representation equality and is not the Flocq
payload. -/
theorem ClosestMonotone_from_split_radix_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ) :
    ⦃⌜True⌝⦄
    (pure (ClosestMonotone_check (beta:=beta) bo radix) : Id Unit)
    ⦃⇓_ => ⌜MonotoneP_float (Closest (beta:=beta) bo radix)⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, ClosestMonotone_check, Id.run,
    ULift.up_down]
  intro p q p' q' hpq hp' hq'
  show _root_.F2R (beta:=beta) p' ≤ _root_.F2R (beta:=beta) q'
  by_contra hnot
  have hq'p' : _root_.F2R (beta:=beta) q' < _root_.F2R (beta:=beta) p' :=
    lt_of_not_ge hnot
  rcases hp' with ⟨hp'Bounded, hp'Closest⟩
  rcases hq' with ⟨hq'Bounded, hq'Closest⟩
  have hA :
      |_root_.F2R (beta:=beta) p' - p| ≤
        |_root_.F2R (beta:=beta) q' - p| :=
    hp'Closest q' hq'Bounded
  have hB :
      |_root_.F2R (beta:=beta) q' - q| ≤
        |_root_.F2R (beta:=beta) p' - q| :=
    hq'Closest p' hp'Bounded
  have hA2 :
      (_root_.F2R (beta:=beta) p' - p)^2 ≤
        (_root_.F2R (beta:=beta) q' - p)^2 := by
    have h := sq_le_sq.mpr hA
    simpa [sq_abs] using h
  have hB2 :
      (_root_.F2R (beta:=beta) q' - q)^2 ≤
        (_root_.F2R (beta:=beta) p' - q)^2 := by
    have h := sq_le_sq.mpr hB
    simpa [sq_abs] using h
  have hsum_le :
      _root_.F2R (beta:=beta) p' + _root_.F2R (beta:=beta) q' ≤ 2 * p := by
    nlinarith
  have hsum_ge :
      2 * q ≤ _root_.F2R (beta:=beta) p' + _root_.F2R (beta:=beta) q' := by
    nlinarith
  nlinarith

/-- Coq `ClosestMonotone`. -/
theorem ClosestMonotone {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) :
    ⦃⌜True⌝⦄
    (pure (ClosestMonotone_check (beta:=beta) b (beta : ℝ)) : Id Unit)
    ⦃⇓_ => ⌜MonotoneP (Closest (beta:=beta) b (beta : ℝ))⌝⦄ := by
  exact ClosestMonotone_from_split_radix_payload
    (beta:=beta) b (beta : ℝ)

-- Rounded-mode packaging for `Closest` (Coq: `ClosestRoundedModeP_from_total_payload`)
noncomputable def ClosestRoundedModeP_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ) : Unit :=
  ()

/-- Coq: `ClosestRoundedModeP_from_total_payload`.

The local generic `RoundedModePGeneric` is intentionally not the Coq float-specific
package, so this theorem restores the payload as `RoundedModeP_full`.  Totality
remains explicit because `ClosestTotal_from_extrema_payload` is still an open construction over the
bounded-float set. -/
theorem ClosestRoundedModeP_from_total_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ) :
    ⦃⌜TotalP (Closest (beta:=beta) bo radix)⌝⦄
    (pure (ClosestRoundedModeP_check (beta:=beta) bo radix) : Id Unit)
    ⦃⇓_ => ⌜RoundedModeP_full (beta:=beta) bo
      (Closest (beta:=beta) bo radix)⌝⦄ := by
  intro hTotal
  simp only [wp, PostCond.noThrow, pure, ClosestRoundedModeP_check,
    Id.run, ULift.up_down]
  refine ⟨hTotal, ?_, ?_, ?_, ?_⟩
  · have h := ClosestCompatible_from_structural_payload (beta:=beta) bo radix True.intro
    simpa only [wp, PostCond.noThrow, pure, ClosestCompatible_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using h
  · have h := ClosestMonotone_from_split_radix_payload (beta:=beta) bo radix True.intro
    simpa only [wp, PostCond.noThrow, pure, ClosestMonotone_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using h
  · intro p hpBounded
    constructor
    · exact hpBounded
    · intro g hgBounded
      rw [sub_self, abs_zero]
      exact abs_nonneg (_root_.F2R (beta:=beta) g - _root_.F2R (beta:=beta) p)
  · intro p q hpBounded hClosest
    rcases hClosest with ⟨_hqBounded, hMin⟩
    have hdist := hMin p hpBounded
    rw [sub_self, abs_zero] at hdist
    have hzero : |_root_.F2R (beta:=beta) q - _root_.F2R (beta:=beta) p| = 0 :=
      le_antisymm hdist (abs_nonneg _)
    exact (sub_eq_zero.mp (abs_eq_zero.mp hzero)).symm

/-- Coq Veltkamp local lemma `pPos`.

The first Veltkamp rounded value is nonnegative because it rounds the positive
quantity `x * (radix^s + 1)` with the monotone closest-rounding mode. -/
theorem pPos {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (s t : Nat)
    (x p : FloatSpec.Core.Defs.FlocqFloat beta)
    (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hxPos : 0 < _root_.F2R (beta:=beta) x)
    (hpDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)) p) :
    0 ≤ _root_.F2R (beta:=beta) p := by
  have hMono :
      MonotoneP_float (Closest (beta:=beta) b (radix : ℝ)) := by
    have h := ClosestMonotone_from_split_radix_payload (beta:=beta) b (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, ClosestMonotone_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using h trivial
  let zeroFloat : FloatSpec.Core.Defs.FlocqFloat beta := ⟨0, -b.dExp⟩
  have hradix_pos_int : (0 : Int) < radix := lt_trans (by decide) hradix
  have hvNum_pos : 0 < b.vNum := by
    rw [hvNum, Zpower_nat]
    exact pow_pos hradix_pos_int t
  have hZeroBounded : Fbounded (beta:=beta) b zeroFloat := by
    constructor
    · show |zeroFloat.Fnum| < b.vNum
      simp [zeroFloat, hvNum_pos]
    · show -b.dExp ≤ zeroFloat.Fexp
      rfl
  have hZeroF2R : _root_.F2R (beta:=beta) zeroFloat = 0 := by
    unfold _root_.F2R FloatSpec.Core.Defs.F2R zeroFloat
    ring
  have hP0 :
      Closest (beta:=beta) b (radix : ℝ) 0 zeroFloat := by
    constructor
    · exact hZeroBounded
    · intro f _hfBounded
      rw [hZeroF2R, sub_self, abs_zero]
      exact abs_nonneg _
  have hradix_pos_real : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradix_pos_int
  have hpow_pos : 0 < (radix : ℝ) ^ (s : Int) :=
    zpow_pos hradix_pos_real _
  have hfactor_pos : 0 < (radix : ℝ) ^ (s : Int) + 1 := by
    linarith
  have hinput_pos :
      0 < _root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1) :=
    mul_pos hxPos hfactor_pos
  have hmono := hMono 0
    (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1))
    zeroFloat p hinput_pos hP0 hpDef
  rw [hZeroF2R] at hmono
  exact hmono

/-- Coq Veltkamp local lemma `qNeg`.

After `p` rounds the scaled value above `x`, the second rounded value `q`
rounds the nonpositive residual `x - p`, so it is nonpositive. -/
theorem qNeg {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (s t : Nat)
    (x p q : FloatSpec.Core.Defs.FlocqFloat beta)
    (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hxBound : Fbounded (beta:=beta) b x)
    (hxPos : 0 < _root_.F2R (beta:=beta) x)
    (hpDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)) p)
    (hqDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q) :
    _root_.F2R (beta:=beta) q ≤ 0 := by
  have hMono :
      MonotoneP_float (Closest (beta:=beta) b (radix : ℝ)) := by
    have h := ClosestMonotone_from_split_radix_payload (beta:=beta) b (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, ClosestMonotone_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using h trivial
  let zeroFloat : FloatSpec.Core.Defs.FlocqFloat beta := ⟨0, -b.dExp⟩
  have hradix_pos_int : (0 : Int) < radix := lt_trans (by decide) hradix
  have hvNum_pos : 0 < b.vNum := by
    rw [hvNum, Zpower_nat]
    exact pow_pos hradix_pos_int t
  have hZeroBounded : Fbounded (beta:=beta) b zeroFloat := by
    constructor
    · show |zeroFloat.Fnum| < b.vNum
      simp [zeroFloat, hvNum_pos]
    · show -b.dExp ≤ zeroFloat.Fexp
      rfl
  have hZeroF2R : _root_.F2R (beta:=beta) zeroFloat = 0 := by
    unfold _root_.F2R FloatSpec.Core.Defs.F2R zeroFloat
    ring
  have hP0 :
      Closest (beta:=beta) b (radix : ℝ) 0 zeroFloat := by
    constructor
    · exact hZeroBounded
    · intro f _hfBounded
      rw [hZeroF2R, sub_self, abs_zero]
      exact abs_nonneg _
  have hxProject :
      Closest (beta:=beta) b (radix : ℝ)
        (_root_.F2R (beta:=beta) x) x := by
    constructor
    · exact hxBound
    · intro f _hfBounded
      rw [sub_self, abs_zero]
      exact abs_nonneg _
  have hradix_pos_real : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradix_pos_int
  have hpow_pos : 0 < (radix : ℝ) ^ (s : Int) :=
    zpow_pos hradix_pos_real _
  have hfactor_gt_one : 1 < (radix : ℝ) ^ (s : Int) + 1 := by
    linarith
  have hscaled_gt :
      _root_.F2R (beta:=beta) x <
        _root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1) := by
    nlinarith
  have hx_le_p :
      _root_.F2R (beta:=beta) x ≤ _root_.F2R (beta:=beta) p :=
    hMono (_root_.F2R (beta:=beta) x)
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1))
      x p hscaled_gt hxProject hpDef
  have hres_le_zero :
      _root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p ≤ 0 := by
    linarith
  rcases hres_le_zero.lt_or_eq with hres_lt | hres_eq
  · have hmono := hMono
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p)
      0 q zeroFloat hres_lt hqDef hP0
    rw [hZeroF2R] at hmono
    exact hmono
  · have hdist := hqDef.2 zeroFloat hZeroBounded
    rw [hres_eq, hZeroF2R, sub_zero, sub_self, abs_zero] at hdist
    have hqZero : _root_.F2R (beta:=beta) q = 0 :=
      abs_eq_zero.mp (le_antisymm hdist (abs_nonneg _))
    exact le_of_eq hqZero

-- Symmetry under negation on the real side (Coq: `ClosestOpp`)
noncomputable def ClosestOpp_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) : Unit :=
  ()

/-- Coq: `ClosestOpp` — negation preserves closestness. -/
theorem ClosestOpp {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) :
    ⦃⌜Closest (beta:=beta) bo radix r p⌝⦄
    (pure (ClosestOpp_check (beta:=beta) bo radix p r) : Id Unit)
    ⦃⇓_ => ⌜Closest (beta:=beta) bo radix (-r) (Fopp p)⌝⦄ := by
  intro hp
  simp only [wp, PostCond.noThrow, pure, ClosestOpp_check,
    Id.run, ULift.up_down]
  rcases hp with ⟨hpBounded, hpClosest⟩
  constructor
  · simpa [Fbounded, Fopp, FloatSpec.Calc.Operations.Fopp, abs_neg] using hpBounded
  · intro f hfBounded
    have hFoppFBounded : Fbounded (beta:=beta) bo (Fopp f) := by
      simpa [Fbounded, Fopp, FloatSpec.Calc.Operations.Fopp, abs_neg] using hfBounded
    have hClosest := hpClosest (Fopp f) hFoppFBounded
    have hpOpp :
        _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) = - _root_.F2R (beta:=beta) p := by
      have h := (FloatSpec.Calc.Operations.F2R_opp (beta:=beta) p) trivial
      simpa [Fopp, _root_.F2R] using h
    have hfOpp :
        _root_.F2R (beta:=beta) (Fopp (beta:=beta) f) = - _root_.F2R (beta:=beta) f := by
      have h := (FloatSpec.Calc.Operations.F2R_opp (beta:=beta) f) trivial
      simpa [Fopp, _root_.F2R] using h
    calc
      |_root_.F2R (beta:=beta) (Fopp (beta:=beta) p) - -r|
          = |_root_.F2R (beta:=beta) p - r| := by
            have harg :
                _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) - -r =
                  - (_root_.F2R (beta:=beta) p - r) := by
              rw [hpOpp]
              ring
            rw [harg, abs_neg]
      _ ≤ |_root_.F2R (beta:=beta) (Fopp (beta:=beta) f) - r| := hClosest
      _ = |_root_.F2R (beta:=beta) f - -r| := by
            have harg :
                _root_.F2R (beta:=beta) (Fopp (beta:=beta) f) - r =
                  - (_root_.F2R (beta:=beta) f - -r) := by
              rw [hfOpp]
              ring
            rw [harg, abs_neg]

-- Absolute-value symmetry on the real side (Coq: `ClosestFabs`)
noncomputable def ClosestFabs_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) : Unit :=
  ()

/-- Coq: `ClosestFabs` — absolute value preserves closestness. -/
theorem ClosestFabs {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) :
    ⦃⌜1 < beta ∧ Closest (beta:=beta) bo radix r p⌝⦄
    (pure (ClosestFabs_check (beta:=beta) bo radix p r) : Id Unit)
    ⦃⇓_ => ⌜Closest (beta:=beta) bo radix |r| (Fabs p)⌝⦄ := by
  intro ⟨hβ, hp⟩
  simp only [wp, PostCond.noThrow, pure, ClosestFabs_check, Id.run,
    ULift.up_down]
  rcases hp with ⟨hpBounded, hpClosest⟩
  have hFabsBounded : Fbounded (beta:=beta) bo (Fabs (beta:=beta) p) := by
    simpa [Fbounded, Fabs, FloatSpec.Calc.Operations.Fabs, Int.natCast_natAbs]
      using hpBounded
  have hFabsVal :
      _root_.F2R (beta:=beta) (Fabs (beta:=beta) p) =
        |_root_.F2R (beta:=beta) p| := by
    have h := FloatSpec.Calc.Operations.F2R_abs (beta:=beta) p hβ
    simpa [Fabs, _root_.F2R] using h
  constructor
  · exact hFabsBounded
  · intro g hg
    by_cases hr : 0 ≤ r
    · have hr_abs : |r| = r := abs_of_nonneg hr
      calc
        |(_root_.F2R (beta:=beta) (Fabs (beta:=beta) p)) - (|r|)| =
            |(|_root_.F2R (beta:=beta) p|) - (|r|)| := by rw [hFabsVal]
        _ ≤ |_root_.F2R (beta:=beta) p - r| :=
            abs_abs_sub_abs_le_abs_sub _ _
        _ ≤ |_root_.F2R (beta:=beta) g - r| :=
            hpClosest g hg
        _ = |(_root_.F2R (beta:=beta) g) - (|r|)| := by rw [hr_abs]
    · have hr_lt : r < 0 := lt_of_not_ge hr
      have hr_abs : |r| = -r := abs_of_neg hr_lt
      have hFoppBounded : Fbounded (beta:=beta) bo (Fopp (beta:=beta) g) := by
        simpa [Fbounded, Fopp, FloatSpec.Calc.Operations.Fopp, abs_neg] using hg
      have hFoppVal :
          _root_.F2R (beta:=beta) (Fopp (beta:=beta) g) =
            - _root_.F2R (beta:=beta) g := by
        have h := (FloatSpec.Calc.Operations.F2R_opp (beta:=beta) g) trivial
        simpa [Fopp, _root_.F2R] using h
      calc
        |(_root_.F2R (beta:=beta) (Fabs (beta:=beta) p)) - (|r|)| =
            |(|_root_.F2R (beta:=beta) p|) - (|r|)| := by rw [hFabsVal]
        _ ≤ |_root_.F2R (beta:=beta) p - r| :=
            abs_abs_sub_abs_le_abs_sub _ _
        _ ≤ |_root_.F2R (beta:=beta) (Fopp (beta:=beta) g) - r| :=
            hpClosest (Fopp (beta:=beta) g) hFoppBounded
        _ = |(_root_.F2R (beta:=beta) g) - (|r|)| := by
            rw [hFoppVal, hr_abs]
            have harg :
                - _root_.F2R (beta:=beta) g - r =
                  - (_root_.F2R (beta:=beta) g + r) := by
              ring
            rw [harg, abs_neg]
            have htarget :
                _root_.F2R (beta:=beta) g + r =
                  _root_.F2R (beta:=beta) g - -r := by
              ring
            rw [htarget]

-- Ulp inequality for closest rounding (Coq: `ClosestUlp`)
noncomputable def ClosestUlp_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (p : ℝ) (q : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

-- Exponent inequality from closest error (Coq: `ClosestExp`)
noncomputable def ClosestExp_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (x : ℝ) (q : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `ClosestExp`.

Upstream derives this from `ClosestUlp` and `FulpLe`.  This theorem keeps the
`ClosestUlp` inequality explicit because the local `ClosestUlp` theorem is
declared later, after the normalized `FulpSuc`/`FulpPred` stack it depends on. -/
theorem ClosestExp_from_ulp_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (x : ℝ) (q : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Closest (beta:=beta) bo (radix : ℝ) x q ∧
        2 * |x - _root_.F2R (beta:=beta) q| ≤
          Fulp (beta:=beta) bo radix precision q ∧
        beta = radix ∧ 1 < radix⌝⦄
    (pure (ClosestExp_check (beta:=beta) bo radix precision x q) : Id Unit)
    ⦃⇓_ => ⌜2 * |x - _root_.F2R (beta:=beta) q| ≤
        (beta : ℝ) ^ q.Fexp⌝⦄ := by
  intro ⟨hClosest, hClosestUlp, hbeta, hradix⟩
  simp only [wp, PostCond.noThrow, pure, ClosestExp_check,
    Id.run, ULift.up_down]
  have hFulpLe :
      Fulp (beta:=beta) bo radix precision q ≤ (beta : ℝ) ^ q.Fexp := by
    have h := FulpLe (beta:=beta) radix bo precision q
    simpa only [wp, PostCond.noThrow, pure, FulpLe_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
      h ⟨hClosest.1, hbeta, hradix⟩
  exact le_trans hClosestUlp hFulpLe

-- Strict error-exp implication (Coq: `ClosestErrorExpStrict`)
noncomputable def ClosestErrorExpStrict_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) (x : ℝ) : Unit :=
  ()

/-- Coq: `ClosestErrorExpStrict` — if `p` is the closest bounded float to `x`,
    `q` represents the error `x - F2R p`, and `q ≠ 0`, then `Fexp q < Fexp p`.

    Since `Closest` and `Fbounded` are concrete predicates, this port carries
    explicit hypotheses matching the Coq proof's requirements:
    - `hBetaGe1`: the radix β ≥ 1 (needed for zpow monotonicity/positivity)
    - `hClosestExp`: from Coq's `ClosestExp`, the closest-rounding error bound
      `2 * |x - F2R p| ≤ β^(Fexp p)` -/
theorem ClosestErrorExpStrict_from_exp_bound_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) (x : ℝ) :
    ⦃⌜Fbounded (beta:=beta) bo p ∧ Fbounded (beta:=beta) bo q ∧
        Closest (beta:=beta) bo radix x p ∧ _root_.F2R q = x - _root_.F2R p ∧
        _root_.F2R q ≠ 0 ∧
        (1 : ℝ) ≤ (beta : ℝ) ∧
        (2 : ℝ) * |x - _root_.F2R p| ≤ (beta : ℝ) ^ p.Fexp⌝⦄
    (pure (ClosestErrorExpStrict_check (beta:=beta) bo radix p q x) : Id Unit)
    ⦃⇓_ => ⌜q.Fexp < p.Fexp⌝⦄ := by
  intro ⟨_, _, _, hF2Rq, hqNe0, hBetaGe1, hClosestExp⟩
  simp only [wp, PostCond.noThrow, pure, ClosestErrorExpStrict_check,
    Id.run, ULift.up_down]
  -- Goal is ⌜q.Fexp < p.Fexp⌝.down, which is definitionally q.Fexp < p.Fexp
  show q.Fexp < p.Fexp
  -- Prove by contradiction: assume ¬(q.Fexp < p.Fexp), i.e., p.Fexp ≤ q.Fexp
  by_contra h
  push Not at h
  -- h : p.Fexp ≤ q.Fexp
  -- Step 1: β > 0 and β^e > 0 for all e
  have hBetaPos : (0 : ℝ) < (beta : ℝ) := lt_of_lt_of_le one_pos hBetaGe1
  have hPowPos : ∀ e : Int, (0 : ℝ) < (beta : ℝ) ^ e :=
    fun e => zpow_pos hBetaPos e
  -- Step 2: From F2R q ≠ 0, deduce q.Fnum ≠ 0
  have hFnumNe0 : q.Fnum ≠ 0 := by
    intro hc
    apply hqNe0
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, hc, Int.cast_zero, zero_mul]
  -- Step 3: |q.Fnum| ≥ 1
  have hFnumGe1 : (1 : ℝ) ≤ |(q.Fnum : ℝ)| := by
    rw [← Int.cast_abs]
    exact_mod_cast Int.one_le_abs hFnumNe0
  -- Step 4: |F2R q| = |q.Fnum| * β^(q.Fexp), so |F2R q| ≥ β^(q.Fexp)
  have hF2Rabs : |_root_.F2R q| = |(q.Fnum : ℝ)| * (beta : ℝ) ^ q.Fexp := by
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R]
    rw [abs_mul, abs_of_pos (hPowPos q.Fexp)]
  have hF2RqGe : (beta : ℝ) ^ q.Fexp ≤ |_root_.F2R q| := by
    rw [hF2Rabs]
    calc (beta : ℝ) ^ q.Fexp
        = 1 * (beta : ℝ) ^ q.Fexp := by ring
      _ ≤ |(q.Fnum : ℝ)| * (beta : ℝ) ^ q.Fexp :=
          mul_le_mul_of_nonneg_right hFnumGe1 (le_of_lt (hPowPos q.Fexp))
  -- Step 5: β^(p.Fexp) ≤ β^(q.Fexp) (power mono, since p.Fexp ≤ q.Fexp and β ≥ 1)
  have hPowMono : (beta : ℝ) ^ p.Fexp ≤ (beta : ℝ) ^ q.Fexp :=
    zpow_right_mono₀ hBetaGe1 h
  -- Step 6: |F2R q| = |x - F2R p|
  have hAbsEq : |_root_.F2R q| = |x - _root_.F2R p| := by rw [hF2Rq]
  -- Step 7: Chain: 2 * β^(q.Fexp) ≤ 2 * |F2R q| ≤ β^(p.Fexp) ≤ β^(q.Fexp)
  have hContra : 2 * (beta : ℝ) ^ q.Fexp ≤ (beta : ℝ) ^ q.Fexp := calc
    2 * (beta : ℝ) ^ q.Fexp
      ≤ 2 * |_root_.F2R q| := by linarith [hF2RqGe]
    _ = 2 * |x - _root_.F2R p| := by rw [hAbsEq]
    _ ≤ (beta : ℝ) ^ p.Fexp := hClosestExp
    _ ≤ (beta : ℝ) ^ q.Fexp := hPowMono
  -- Step 8: This means β^(q.Fexp) ≤ 0, contradicting positivity
  linarith [hPowPos q.Fexp]

-- Idempotence property for Closest (Coq: `ClosestIdem`)
noncomputable def ClosestIdem_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `ClosestIdem` — if `p` is bounded and `q` is a closest rounding of
    `F2R p`, then `F2R p = F2R q`.

    Note: `Closest` is concrete; this port still carries explicit hypotheses
    matching Coq's `Closest` definition:
    - `Fbounded' bo p`: `p` is bounded
    - `Fbounded' bo q`: `q` is bounded (part of `Closest`)
    - `hClosestProp`: `q` minimizes distance to `F2R p` among bounded floats
    These make the theorem provable and match the original Coq semantics. -/
theorem ClosestIdem_from_expanded_closest_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) bo p ∧ Closest (beta:=beta) bo radix (_root_.F2R p) q ∧
        Fbounded' bo p ∧ Fbounded' bo q ∧
        (∀ g : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded' bo g → |_root_.F2R q - _root_.F2R p| ≤ |_root_.F2R g - _root_.F2R p|)⌝⦄
    (pure (ClosestIdem_check (beta:=beta) bo radix p q) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R p = _root_.F2R q⌝⦄ := by
  intro ⟨_hFb, _hClosest, hBoundedP, _hBoundedQ, hMin⟩
  simp only [wp, PostCond.noThrow, pure, ClosestIdem_check]
  -- Instantiate the minimality condition with p itself
  have h := hMin p hBoundedP
  -- |F2R q - F2R p| ≤ |F2R p - F2R p| = 0
  rw [sub_self, abs_zero] at h
  -- So |F2R q - F2R p| = 0, hence F2R q = F2R p
  have hAbs0 : |_root_.F2R q - _root_.F2R p| = 0 :=
    le_antisymm h (abs_nonneg _)
  rw [abs_eq_zero, sub_eq_zero] at hAbs0
  show _root_.F2R p = _root_.F2R q
  exact hAbs0.symm

/-- Coq `ClosestIdem`. -/
theorem ClosestIdem {beta : Int} [ValidRadix beta]
    (b : Fbound_skel)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b p ∧
        Closest (beta:=beta) b (beta : ℝ) (_root_.F2R p) q⌝⦄
    (pure (ClosestIdem_check (beta:=beta) b (beta : ℝ) p q) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R p = _root_.F2R q⌝⦄ := by
  intro ⟨hp, hClosest⟩
  exact ClosestIdem_from_expanded_closest_payload
    (beta:=beta) b (beta : ℝ) p q
      ⟨hp, hClosest, hp, hClosest.1, hClosest.2⟩

-- Error bound for closest rounding (Coq: `ClosestErrorBound`)
noncomputable def ClosestErrorBound_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) (x : ℝ) : Unit :=
  ()

-- Coq: `ClosestErrorBound` — if `p` is a closest representation of `x` and
-- `q` represents the error `x - F2R p`, then the magnitude of `q` is bounded by
-- `Float 1 (Fexp p) / 2`.
-- This port carries an explicit hypothesis combining ClosestUlp and FulpLe
-- from the original Coq proof.
theorem ClosestErrorBound_from_bound {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) (x : ℝ) :
    ⦃⌜Fbounded (beta:=beta) bo p ∧ Closest (beta:=beta) bo radix x p ∧
        _root_.F2R q = x - _root_.F2R p ∧
        (2 : ℝ) * |x - _root_.F2R p| ≤ (beta : ℝ) ^ p.Fexp⌝⦄
    (pure (ClosestErrorBound_check (beta:=beta) bo radix p q x) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R q| ≤
            _root_.F2R (FloatSpec.Core.Defs.FlocqFloat.mk (beta:=beta) 1 p.Fexp) * (1 / 2 : ℝ)⌝⦄ := by
  intro ⟨_, _, hF2Rq, hUlpBound⟩
  simp only [wp, PostCond.noThrow, pure, ClosestErrorBound_check,
    Id.run, ULift.up_down]
  show |_root_.F2R q| ≤
    _root_.F2R (FloatSpec.Core.Defs.FlocqFloat.mk (beta:=beta) 1 p.Fexp) * (1 / 2 : ℝ)
  -- F2R (Float.mk 1 p.Fexp) = 1 * β^(p.Fexp) = β^(p.Fexp)
  have hF2R1 : _root_.F2R (FloatSpec.Core.Defs.FlocqFloat.mk (beta:=beta) 1 p.Fexp) =
      (beta : ℝ) ^ p.Fexp := by
    simp [_root_.F2R, FloatSpec.Core.Defs.F2R, one_mul]
  rw [hF2R1]
  -- |F2R q| = |x - F2R p| from hF2Rq
  rw [hF2Rq]
  -- Goal: |x - F2R p| ≤ β^(p.Fexp) * (1 / 2)
  -- From hUlpBound: 2 * |x - F2R p| ≤ β^(p.Fexp)
  linarith

-- Inequality lifting for scaling by radix halves (Coq: `FmultRadixInv`)
noncomputable def FmultRadixInv_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (x z : FloatSpec.Core.Defs.FlocqFloat beta) (y : ℝ) : Unit :=
  ()

theorem FmultRadixInv_from_witness {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (x z : FloatSpec.Core.Defs.FlocqFloat beta) (y : ℝ) :
    ⦃⌜Fbounded (beta:=beta) bo x ∧ Closest (beta:=beta) bo radix y z ∧ (1/2 : ℝ) * _root_.F2R x < y ∧
        -- Real hypotheses matching Coq's Closest definition:
        Fbounded' bo z ∧
        (∀ f : FloatSpec.Core.Defs.FlocqFloat beta, Fbounded' bo f →
          |_root_.F2R z - y| ≤ |_root_.F2R f - y|) ∧
        -- Existence of a bounded float w with F2R w ≥ (1/2)*F2R x and F2R w ≤ y
        -- (from MaxEx_from_finite_box_payload / div2IsBetween in the Coq proof)
        (∃ w : FloatSpec.Core.Defs.FlocqFloat beta, Fbounded' bo w ∧
          (1/2 : ℝ) * _root_.F2R x ≤ _root_.F2R w ∧ _root_.F2R w ≤ y)⌝⦄
    (pure (FmultRadixInv_check (beta:=beta) bo radix x z y) : Id Unit)
    ⦃⇓_ => ⌜(1/2 : ℝ) * _root_.F2R x ≤ _root_.F2R z⌝⦄ := by
  intro ⟨_hFb, _hCl, hHalfLt, _hZbnd, hClosest, ⟨w, hWbnd, hWge, hWle⟩⟩
  simp only [wp, PostCond.noThrow, pure, FmultRadixInv_check]
  show (1/2 : ℝ) * _root_.F2R x ≤ _root_.F2R z
  -- By contradiction: suppose F2R z < (1/2) * F2R x
  by_contra h
  push Not at h
  -- Then |F2R z - y| > |F2R w - y| contradicting that z is closest to y
  have hZdist : |_root_.F2R z - y| = y - _root_.F2R z := by
    rw [abs_of_nonpos (by linarith)]
    ring
  have hWdist : |_root_.F2R w - y| = y - _root_.F2R w := by
    rw [abs_of_nonpos (by linarith)]
    ring
  have hContra := hClosest w hWbnd
  rw [hZdist, hWdist] at hContra
  linarith

/-- Coq `FmultRadixInv`, factored after midpoint extrema.

The upstream proof obtains `min` and `max` around `x/2`, proves
`x = min + max` through `div2IsBetween`, and then shows that any closest
rounding of a real strictly above `x/2` cannot lie below `x/2`.  This theorem
restores that core argument without requiring callers to supply an arbitrary
bounded witness in the interval. -/
theorem FmultRadixInv_from_extrema {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (x z min max : FloatSpec.Core.Defs.FlocqFloat beta) (y : ℝ) :
    ⦃⌜Fbounded (beta:=beta) bo x ∧
        Closest (beta:=beta) bo radix y z ∧
        (1 / 2 : ℝ) * _root_.F2R (beta:=beta) x < y ∧
        isMin (beta:=beta) bo 0
          ((1 / 2 : ℝ) * _root_.F2R (beta:=beta) x) min ∧
        isMax (beta:=beta) bo 0
          ((1 / 2 : ℝ) * _root_.F2R (beta:=beta) x) max ∧
        _root_.F2R (beta:=beta) x =
          _root_.F2R (beta:=beta) min + _root_.F2R (beta:=beta) max⌝⦄
    (pure (FmultRadixInv_check (beta:=beta) bo radix x z y) : Id Unit)
    ⦃⇓_ => ⌜(1 / 2 : ℝ) * _root_.F2R (beta:=beta) x ≤
      _root_.F2R (beta:=beta) z⌝⦄ := by
  intro h
  rcases h with
    ⟨_hxBounded, hzClosest, hHalfLtY, hMin, hMax, hxMid⟩
  rcases hzClosest with ⟨hzBounded, hzClosestMin⟩
  rcases hMin with ⟨_hminBounded, hmin_le_half, hminGreatest⟩
  rcases hMax with ⟨hmaxBounded, hhalf_le_max, _hmaxLeast⟩
  simp only [wp, PostCond.noThrow, pure, FmultRadixInv_check]
  by_contra hnot
  have hz_lt_half :
      _root_.F2R (beta:=beta) z <
        (1 / 2 : ℝ) * _root_.F2R (beta:=beta) x :=
    lt_of_not_ge hnot
  have hz_le_min :
      _root_.F2R (beta:=beta) z ≤ _root_.F2R (beta:=beta) min :=
    hminGreatest z hzBounded (le_of_lt hz_lt_half)
  have hhalf_mid :
      (1 / 2 : ℝ) * _root_.F2R (beta:=beta) x =
        (1 / 2 : ℝ) *
          (_root_.F2R (beta:=beta) min + _root_.F2R (beta:=beta) max) := by
    rw [hxMid]
  have hz_lt_y :
      _root_.F2R (beta:=beta) z < y :=
    lt_trans hz_lt_half hHalfLtY
  have hclosestMax := hzClosestMin max hmaxBounded
  by_cases hy_le_max : y ≤ _root_.F2R (beta:=beta) max
  · have hz_y_nonpos : _root_.F2R (beta:=beta) z - y ≤ 0 := by
      linarith
    have hmax_y_nonneg : 0 ≤ _root_.F2R (beta:=beta) max - y := by
      linarith
    rw [abs_of_nonpos hz_y_nonpos, abs_of_nonneg hmax_y_nonneg] at hclosestMax
    have hgap :
        y - _root_.F2R (beta:=beta) min >
          _root_.F2R (beta:=beta) max - y := by
      rw [hhalf_mid] at hHalfLtY
      linarith
    have hdist_gt :
        y - _root_.F2R (beta:=beta) z >
          _root_.F2R (beta:=beta) max - y := by
      linarith
    linarith
  · have hmax_lt_y : _root_.F2R (beta:=beta) max < y :=
      lt_of_not_ge hy_le_max
    have hz_y_nonpos : _root_.F2R (beta:=beta) z - y ≤ 0 := by
      linarith
    have hmax_y_nonpos : _root_.F2R (beta:=beta) max - y ≤ 0 := by
      linarith
    rw [abs_of_nonpos hz_y_nonpos, abs_of_nonpos hmax_y_nonpos] at hclosestMax
    have hz_lt_max :
        _root_.F2R (beta:=beta) z < _root_.F2R (beta:=beta) max :=
      lt_of_lt_of_le hz_lt_half hhalf_le_max
    linarith

-- Symmetric property of Closest (Coq: `ClosestSymmetric`)
noncomputable def ClosestSymmetric_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ) : Unit :=
  ()

/-- Auxiliary predicate: symmetry for rounding relations on floats. -/
def SymmetricP {beta : Int} [ValidRadix beta]
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop) : Prop :=
  ∀ r (p : FloatSpec.Core.Defs.FlocqFloat beta), P r p → P (-r) (Fopp p)

/-- Coq: `ClosestSymmetric` — the `Closest` relation is symmetric under
    real negation accompanied by float negation. -/
theorem ClosestSymmetric {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ) :
    ⦃⌜True⌝⦄
    (pure (ClosestSymmetric_check (beta:=beta) bo radix) : Id Unit)
    ⦃⇓_ => ⌜SymmetricP (Closest (beta:=beta) bo radix)⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, ClosestSymmetric_check,
    Id.run, ULift.up_down]
  intro r p hp
  rcases hp with ⟨hpBounded, hpClosest⟩
  constructor
  · simpa [Fbounded, Fopp, FloatSpec.Calc.Operations.Fopp, abs_neg] using hpBounded
  · intro f hfBounded
    have hFoppFBounded : Fbounded (beta:=beta) bo (Fopp f) := by
      simpa [Fbounded, Fopp, FloatSpec.Calc.Operations.Fopp, abs_neg] using hfBounded
    have hClosest := hpClosest (Fopp f) hFoppFBounded
    have hpOpp :
        _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) = - _root_.F2R (beta:=beta) p := by
      have h := (FloatSpec.Calc.Operations.F2R_opp (beta:=beta) p) trivial
      simpa [Fopp, _root_.F2R] using h
    have hfOpp :
        _root_.F2R (beta:=beta) (Fopp (beta:=beta) f) = - _root_.F2R (beta:=beta) f := by
      have h := (FloatSpec.Calc.Operations.F2R_opp (beta:=beta) f) trivial
      simpa [Fopp, _root_.F2R] using h
    calc
      |_root_.F2R (beta:=beta) (Fopp (beta:=beta) p) - -r|
          = |_root_.F2R (beta:=beta) p - r| := by
            have harg :
                _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) - -r =
                  - (_root_.F2R (beta:=beta) p - r) := by
              rw [hpOpp]
              ring
            rw [harg, abs_neg]
      _ ≤ |_root_.F2R (beta:=beta) (Fopp (beta:=beta) f) - r| := hClosest
      _ = |_root_.F2R (beta:=beta) f - -r| := by
            have harg :
                _root_.F2R (beta:=beta) (Fopp (beta:=beta) f) - r =
                  - (_root_.F2R (beta:=beta) f - -r) := by
              rw [hfOpp]
              ring
            rw [harg, abs_neg]

-- Coq: `ClosestZero1` — if `Closest r f`, `F2R f = 0`, `r = F2R g`, and
-- `-dExp bo ≤ Fexp g`, then `r = 0`.
noncomputable def ClosestZero1_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (r : ℝ)
    (f g : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `ClosestZero1` — under the stated conditions, the rounded value `r`
    must be zero. We mirror the statement using the project Hoare-triple style.

    Note: `Closest` is concrete; this port still carries explicit hypotheses
    matching Coq's `Closest` definition:
    - `Fbounded' bo f`: the float `f` is bounded
    - `hClosestProp`: `f` minimizes distance to `r` among bounded floats
    - `1 < (beta : ℝ)`: radix is at least 2
    - `bo.vNum > 1`: needed to construct bounded witness floats
    These make the theorem provable and match the original Coq semantics. -/
theorem ClosestZero1_from_expanded_closest_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (r : ℝ)
    (f g : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Closest (beta:=beta) bo radix r f ∧
        _root_.F2R f = 0 ∧
        r = _root_.F2R g ∧
        (-bo.dExp : Int) ≤ g.Fexp ∧
        Fbounded' bo f ∧
        (∀ h : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded' bo h → |_root_.F2R f - r| ≤ |_root_.F2R h - r|) ∧
        (1 : ℝ) < (beta : ℝ) ∧
        bo.vNum > 1⌝⦄
    (pure (ClosestZero1_check (beta:=beta) bo radix r f g) : Id Unit)
    ⦃⇓_ => ⌜r = 0⌝⦄ := by
  intro ⟨_, hF2Rf, hRG, hExpG, _, hMin, hBetaGt1, hVNumGt1⟩
  simp only [wp, PostCond.noThrow, pure, ClosestZero1_check, PredTrans.pure, PredTrans.apply,
             Id.run, ULift.down_up]
  -- We have F2R f = 0, so |F2R f - r| = |r|
  rw [hF2Rf, zero_sub] at hMin
  -- Convert goal from ⌜r = 0⌝.down to r = 0
  show r = 0
  -- By contradiction: assume r ≠ 0
  by_contra hr
  -- Since r = F2R g and r ≠ 0, g.Fnum ≠ 0
  have hGnum : g.Fnum ≠ 0 := by
    intro hgz
    exact hr (by rw [hRG, _root_.F2R, FloatSpec.Core.Defs.F2R, hgz, Int.cast_zero, zero_mul])
  -- |g.Fnum| ≥ 1
  have hGnumAbs : (1 : ℤ) ≤ |g.Fnum| := Int.one_le_abs hGnum
  -- β > 0
  have hBetaPos : (0 : ℝ) < (beta : ℝ) := by linarith
  -- β^(-dExp bo) > 0
  have hPowPos : (0 : ℝ) < (beta : ℝ) ^ (-bo.dExp : ℤ) := zpow_pos hBetaPos _
  -- |r| ≥ β^(-dExp bo)
  have hRabs : (beta : ℝ) ^ (-bo.dExp : ℤ) ≤ |r| := by
    rw [hRG, _root_.F2R, FloatSpec.Core.Defs.F2R]
    rw [abs_mul]
    calc (beta : ℝ) ^ (-bo.dExp : ℤ)
        = 1 * (beta : ℝ) ^ (-bo.dExp : ℤ) := by ring
      _ ≤ |(g.Fnum : ℝ)| * |(beta : ℝ) ^ g.Fexp| := by
          apply mul_le_mul
          · exact_mod_cast hGnumAbs
          · rw [abs_of_pos (zpow_pos hBetaPos _)]
            exact zpow_right_mono₀ (le_of_lt hBetaGt1) hExpG
          · exact le_of_lt hPowPos
          · exact abs_nonneg _
  -- Now: construct a bounded float close to r to get contradiction
  -- Consider two cases: r > 0 or r < 0
  rcases ne_iff_lt_or_gt.mp hr with hr_neg | hr_pos
  · -- Case r < 0: use Float (-1) (-dExp bo)
    let witness : FloatSpec.Core.Defs.FlocqFloat beta := ⟨-1, -bo.dExp⟩
    have hWBounded : Fbounded' (beta:=beta) bo witness := by
      refine ⟨?_, le_refl _⟩
      show |(-1 : ℤ)| < bo.vNum
      norm_num
      exact hVNumGt1
    have hWF2R : _root_.F2R witness = -(beta : ℝ) ^ (-bo.dExp : ℤ) := by
      simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, witness]
      push_cast
      ring
    specialize hMin witness hWBounded
    rw [hWF2R] at hMin
    have hRneg : r ≤ -(beta : ℝ) ^ (-bo.dExp : ℤ) := by
      have := abs_of_neg hr_neg
      linarith
    have : |-(beta : ℝ) ^ (-bo.dExp : ℤ) - r| = -(beta : ℝ) ^ (-bo.dExp : ℤ) - r := by
      rw [abs_of_nonneg]; linarith
    rw [this, abs_neg, abs_of_neg hr_neg] at hMin
    linarith
  · -- Case r > 0: use Float 1 (-dExp bo)
    let witness : FloatSpec.Core.Defs.FlocqFloat beta := ⟨1, -bo.dExp⟩
    have hWBounded : Fbounded' (beta:=beta) bo witness := by
      refine ⟨?_, le_refl _⟩
      show |(1 : ℤ)| < bo.vNum
      simp only [abs_one]
      exact hVNumGt1
    have hWF2R : _root_.F2R witness = (beta : ℝ) ^ (-bo.dExp : ℤ) := by
      simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, witness]
      push_cast
      ring
    specialize hMin witness hWBounded
    rw [hWF2R] at hMin
    have hRpos_ge : (beta : ℝ) ^ (-bo.dExp : ℤ) ≤ r := by
      have := abs_of_pos hr_pos
      linarith
    have : |(beta : ℝ) ^ (-bo.dExp : ℤ) - r| = r - (beta : ℝ) ^ (-bo.dExp : ℤ) := by
      rw [abs_of_nonpos]; ring; linarith
    rw [this, abs_neg, abs_of_pos hr_pos] at hMin
    linarith

/-- Coq `ClosestZero1`. -/
theorem ClosestZero1 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat) (r : ℝ)
    (f g : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜b.vNum = Zpower_nat beta precision ∧ 3 ≤ precision ∧
        Closest (beta:=beta) b (beta : ℝ) r f ∧
        _root_.F2R f = 0 ∧ r = _root_.F2R g ∧
        -b.dExp ≤ g.Fexp⌝⦄
    (pure (ClosestZero1_check (beta:=beta) b (beta : ℝ) r f g) : Id Unit)
    ⦃⇓_ => ⌜r = 0⌝⦄ := by
  intro ⟨hvNum, hprecision, hClosest, hf0, hrg, hgexp⟩
  have hvNum_gt : 1 < b.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ (ValidRadix.valid (beta:=beta)) (by omega)
  exact ClosestZero1_from_expanded_closest_payload
    (beta:=beta) b (beta : ℝ) r f g
      ⟨hClosest, hf0, hrg, hgexp, hClosest.1, hClosest.2,
        by exact_mod_cast ValidRadix.valid (beta:=beta), hvNum_gt⟩

/-!
Div-by-2 midpoint characterizations (ported from Coq Pff.v)

This section gives the Hoare-style statements and closed proofs for
`div2IsBetweenPos` and `div2IsBetween`, retaining descriptive helper witnesses
used by the proof reconstruction.
-/

-- Coq: `div2IsBetweenPos` — if 0 ≤ p and min/max are the rounded bounds of p/2,
-- then F2R p = F2R min + F2R max
noncomputable def div2IsBetweenPos_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p min max : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

-- Coq: div2IsBetweenPos — if 0 ≤ F2R p and min/max are the rounded
-- bounds of (1/2) * F2R p among bounded floats, then F2R p = F2R min + F2R max.
-- Modified: precondition inlines real content from Coq isMin/isMax/Sterbenz/PminPos.
theorem div2IsBetweenPos_from_expanded_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p min max : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜0 ≤ _root_.F2R p ∧
        Fbounded (beta:=beta) b p ∧
        isMinGeneric (α:=FloatSpec.Core.Defs.FlocqFloat beta) b radix ((1/2 : ℝ) * _root_.F2R p) min ∧
        isMaxGeneric (α:=FloatSpec.Core.Defs.FlocqFloat beta) b radix ((1/2 : ℝ) * _root_.F2R p) max ∧
        -- Real content of isMin (Coq: Fbounded min, min ≤ 1/2*p, GLB property)
        _root_.F2R min ≤ (1/2 : ℝ) * _root_.F2R p ∧
        (∀ f : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded' (beta:=beta) b f → _root_.F2R f ≤ (1/2 : ℝ) * _root_.F2R p →
          _root_.F2R f ≤ _root_.F2R min) ∧
        -- Real content of isMax (Coq: Fbounded max, 1/2*p ≤ max, LUB property)
        (1/2 : ℝ) * _root_.F2R p ≤ _root_.F2R max ∧
        (∀ f : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded' (beta:=beta) b f → (1/2 : ℝ) * _root_.F2R p ≤ _root_.F2R f →
          _root_.F2R max ≤ _root_.F2R f) ∧
        -- Sterbenz intermediate: p - max is representable as a bounded float
        (∃ d₁ : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded' (beta:=beta) b d₁ ∧ _root_.F2R d₁ = _root_.F2R p - _root_.F2R max) ∧
        -- PminPos intermediate: p - min is representable as a bounded float
        (∃ d₂ : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded' (beta:=beta) b d₂ ∧ _root_.F2R d₂ = _root_.F2R p - _root_.F2R min)⌝⦄
    (pure (div2IsBetweenPos_check (beta:=beta) b radix p min max) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R p = _root_.F2R min + _root_.F2R max⌝⦄ := by
  intro ⟨_hp_nonneg, _hFb, _hIsMin, _hIsMax,
         hmin_le, hmin_glb, hmax_ge, hmax_lub,
         ⟨d₁, hd₁_bnd, hd₁_val⟩, ⟨d₂, hd₂_bnd, hd₂_val⟩⟩
  simp only [wp, PostCond.noThrow, pure, div2IsBetweenPos_check, PredTrans.pure, PredTrans.apply,
             Id.run, ULift.down]
  show _root_.F2R p = _root_.F2R min + _root_.F2R max
  apply le_antisymm
  · -- Show F2R p ≤ F2R min + F2R max
    -- d₁ is a bounded float with F2R d₁ = F2R p - F2R max
    -- Since F2R max ≥ 1/2 * F2R p, we get F2R d₁ ≤ 1/2 * F2R p
    -- By isMins GLB property: F2R d₁ ≤ F2R min
    -- Hence F2R p - F2R max ≤ F2R min, i.e., F2R p ≤ F2R min + F2R max
    have hd₁_le_half : _root_.F2R d₁ ≤ (1/2 : ℝ) * _root_.F2R p := by linarith
    have hd₁_le_min : _root_.F2R d₁ ≤ _root_.F2R min := hmin_glb d₁ hd₁_bnd hd₁_le_half
    linarith
  · -- Show F2R min + F2R max ≤ F2R p
    -- d₂ is a bounded float with F2R d₂ = F2R p - F2R min
    -- Since F2R min ≤ 1/2 * F2R p, we get 1/2 * F2R p ≤ F2R d₂
    -- By isMaxs LUB property: F2R max ≤ F2R d₂
    -- Hence F2R max ≤ F2R p - F2R min, i.e., F2R min + F2R max ≤ F2R p
    have hd₂_ge_half : (1/2 : ℝ) * _root_.F2R p ≤ _root_.F2R d₂ := by linarith
    have hd₂_ge_max : _root_.F2R max ≤ _root_.F2R d₂ := hmax_lub d₂ hd₂_bnd hd₂_ge_half
    linarith

-- Coq: `div2IsBetween` — same as above without the nonnegativity side-condition
noncomputable def div2IsBetween_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p min max : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

-- Modified: precondition inlines real content from Coq isMin/isMax/Sterbenz/PminPos
-- (same as div2IsBetweenPos but without the 0 ≤ F2R p condition).
theorem div2IsBetween_from_expanded_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p min max : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b p ∧
        isMinGeneric (α:=FloatSpec.Core.Defs.FlocqFloat beta) b radix ((1/2 : ℝ) * _root_.F2R p) min ∧
        isMaxGeneric (α:=FloatSpec.Core.Defs.FlocqFloat beta) b radix ((1/2 : ℝ) * _root_.F2R p) max ∧
        -- Real content of isMin (Coq: Fbounded min, min ≤ 1/2*p, GLB property)
        _root_.F2R min ≤ (1/2 : ℝ) * _root_.F2R p ∧
        (∀ f : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded' (beta:=beta) b f → _root_.F2R f ≤ (1/2 : ℝ) * _root_.F2R p →
          _root_.F2R f ≤ _root_.F2R min) ∧
        -- Real content of isMax (Coq: Fbounded max, 1/2*p ≤ max, LUB property)
        (1/2 : ℝ) * _root_.F2R p ≤ _root_.F2R max ∧
        (∀ f : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded' (beta:=beta) b f → (1/2 : ℝ) * _root_.F2R p ≤ _root_.F2R f →
          _root_.F2R max ≤ _root_.F2R f) ∧
        -- Sterbenz intermediate: p - max is representable as a bounded float
        (∃ d₁ : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded' (beta:=beta) b d₁ ∧ _root_.F2R d₁ = _root_.F2R p - _root_.F2R max) ∧
        -- PminPos intermediate: p - min is representable as a bounded float
        (∃ d₂ : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded' (beta:=beta) b d₂ ∧ _root_.F2R d₂ = _root_.F2R p - _root_.F2R min)⌝⦄
    (pure (div2IsBetween_check (beta:=beta) b radix p min max) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R p = _root_.F2R min + _root_.F2R max⌝⦄ := by
  intro ⟨_hFb, _hIsMin, _hIsMax,
         hmin_le, hmin_glb, hmax_ge, hmax_lub,
         ⟨d₁, hd₁_bnd, hd₁_val⟩, ⟨d₂, hd₂_bnd, hd₂_val⟩⟩
  simp only [wp, PostCond.noThrow, pure, div2IsBetween_check, PredTrans.pure, PredTrans.apply,
             Id.run, ULift.down]
  show _root_.F2R p = _root_.F2R min + _root_.F2R max
  apply le_antisymm
  · -- Show F2R p ≤ F2R min + F2R max
    have hd₁_le_half : _root_.F2R d₁ ≤ (1/2 : ℝ) * _root_.F2R p := by linarith
    have hd₁_le_min : _root_.F2R d₁ ≤ _root_.F2R min := hmin_glb d₁ hd₁_bnd hd₁_le_half
    linarith
  · -- Show F2R min + F2R max ≤ F2R p
    have hd₂_ge_half : (1/2 : ℝ) * _root_.F2R p ≤ _root_.F2R d₂ := by linarith
    have hd₂_ge_max : _root_.F2R max ≤ _root_.F2R d₂ := hmax_lub d₂ hd₂_bnd hd₂_ge_half
    linarith

/-- Coq `div2IsBetween`, stated over the restored float extrema predicates.

The older port kept unused generic `isMin`/`isMax` assumptions in the
precondition.  This lemma exposes the actual Flocq payload: concrete lower and
upper bounded extrema around `p/2`, plus the two bounded residual witnesses. -/
theorem div2IsBetween_from_extrema {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p min max : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b p ∧
        isMin (beta:=beta) b radix
          ((1 / 2 : ℝ) * _root_.F2R (beta:=beta) p) min ∧
        isMax (beta:=beta) b radix
          ((1 / 2 : ℝ) * _root_.F2R (beta:=beta) p) max ∧
        (∃ d₁ : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded (beta:=beta) b d₁ ∧
            _root_.F2R (beta:=beta) d₁ =
              _root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) max) ∧
        (∃ d₂ : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded (beta:=beta) b d₂ ∧
            _root_.F2R (beta:=beta) d₂ =
              _root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) min)⌝⦄
    (pure (div2IsBetween_check (beta:=beta) b radix p min max) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) p =
      _root_.F2R (beta:=beta) min + _root_.F2R (beta:=beta) max⌝⦄ := by
  intro h
  rcases h with
    ⟨_hpBounded, hMin, hMax, ⟨d₁, hd₁_bnd, hd₁_val⟩,
      ⟨d₂, hd₂_bnd, hd₂_val⟩⟩
  rcases hMin with ⟨_hmin_bnd, hmin_le, hmin_glb⟩
  rcases hMax with ⟨_hmax_bnd, hmax_ge, hmax_lub⟩
  simp only [wp, PostCond.noThrow, pure, div2IsBetween_check,
    PredTrans.pure, PredTrans.apply, Id.run, ULift.down]
  apply le_antisymm
  · have hd₁_le_half :
        _root_.F2R (beta:=beta) d₁ ≤
          (1 / 2 : ℝ) * _root_.F2R (beta:=beta) p := by
      linarith
    have hd₁_le_min :
        _root_.F2R (beta:=beta) d₁ ≤
          _root_.F2R (beta:=beta) min :=
      hmin_glb d₁ hd₁_bnd hd₁_le_half
    linarith
  · have hd₂_ge_half :
        (1 / 2 : ℝ) * _root_.F2R (beta:=beta) p ≤
          _root_.F2R (beta:=beta) d₂ := by
      linarith
    have hd₂_ge_max :
        _root_.F2R (beta:=beta) max ≤
          _root_.F2R (beta:=beta) d₂ :=
      hmax_lub d₂ hd₂_bnd hd₂_ge_half
    linarith

-- Compatibility of `EvenClosest` (Coq: `EvenClosestCompatible_from_structural_payload`)
noncomputable def EvenClosestCompatible_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat) : Unit :=
  ()

/-- Coq: `EvenClosestCompatible_from_structural_payload` — `EvenClosest` respects equality compat. -/
theorem EvenClosestCompatible_from_structural_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat) :
    ⦃⌜True⌝⦄
    (pure (EvenClosestCompatible_check (beta:=beta) b radix precision) : Id Unit)
    ⦃⇓_ => ⌜CompatiblePGeneric (EvenClosest (beta:=beta) b radix precision)⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, EvenClosestCompatible_check, PredTrans.pure, PredTrans.apply,
             Id.run, ULift.down]
  show CompatiblePGeneric (EvenClosest (beta:=beta) b radix precision)
  intro r1 r2 p q hec hr hpq
  subst hr; subst hpq
  exact hec

-- Min-or-max disjunction for `EvenClosest` (Coq: `EvenClosestMinOrMax`)
noncomputable def EvenClosestMinOrMax_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radixZ : Int) (radixR : ℝ) (precision : Nat) : Unit :=
  ()

/-- Coq: `EvenClosestMinOrMax`.

An `EvenClosest` witness includes a `Closest` witness, so the min-or-max
disjunction follows from the already restored `ClosestMinOrMax` proof. -/
theorem EvenClosestMinOrMax_from_split_radix_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radixZ : Int) (radixR : ℝ) (precision : Nat) :
    ⦃⌜True⌝⦄
    (pure (EvenClosestMinOrMax_check (beta:=beta) b radixZ radixR precision) : Id Unit)
    ⦃⇓_ => ⌜MinOrMaxP_float (beta:=beta) b radixZ
      (EvenClosest (beta:=beta) b radixR precision)⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, EvenClosestMinOrMax_check, MinOrMaxP_float,
    Id.run, ULift.up_down]
  have hClosest := ClosestMinOrMax_from_split_radix_payload (beta:=beta) b radixZ radixR True.intro
  simp only [wp, PostCond.noThrow, pure, ClosestMinOrMax_check, MinOrMaxP_float,
    Id.run, ULift.up_down] at hClosest
  intro r p hp
  exact hClosest r p hp.1

/-- Coq `EvenClosestMinOrMax`. -/
theorem EvenClosestMinOrMax {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat) :
    ⦃⌜True⌝⦄
    (pure (EvenClosestMinOrMax_check
      (beta:=beta) b beta (beta : ℝ) precision) : Id Unit)
    ⦃⇓_ => ⌜MinOrMaxP (beta:=beta) b
      (EvenClosest (beta:=beta) b (beta : ℝ) precision)⌝⦄ := by
  exact EvenClosestMinOrMax_from_split_radix_payload
    (beta:=beta) b beta (beta : ℝ) precision

-- Monotonicity for `EvenClosest` (Coq: `EvenClosestMonotone`)
noncomputable def EvenClosestMonotone_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat) : Unit :=
  ()

/-- Coq: `EvenClosestMonotone` — even-closest rounding is monotone.

The Coq proof applies `ClosestMonotone` after unpacking the two
`EvenClosest` hypotheses; the Lean statement uses the float-specific
`MonotoneP_float` payload. -/
theorem EvenClosestMonotone_from_split_radix_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat) :
    ⦃⌜True⌝⦄
    (pure (EvenClosestMonotone_check (beta:=beta) b radix precision) : Id Unit)
    ⦃⇓_ => ⌜MonotoneP_float (EvenClosest (beta:=beta) b radix precision)⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, EvenClosestMonotone_check, Id.run,
    ULift.up_down]
  have hClosestMono : MonotoneP_float (Closest (beta:=beta) b radix) := by
    have h := ClosestMonotone_from_split_radix_payload (beta:=beta) b radix True.intro
    simpa only [wp, PostCond.noThrow, pure, ClosestMonotone_check, Id.run,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using h
  intro p q p' q' hpq hp' hq'
  exact hClosestMono p q p' q' hpq hp'.1 hq'.1

/-- Coq `EvenClosestMonotone`. -/
theorem EvenClosestMonotone {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat) :
    ⦃⌜True⌝⦄
    (pure (EvenClosestMonotone_check
      (beta:=beta) b (beta : ℝ) precision) : Id Unit)
    ⦃⇓_ => ⌜MonotoneP
      (EvenClosest (beta:=beta) b (beta : ℝ) precision)⌝⦄ := by
  exact EvenClosestMonotone_from_split_radix_payload
    (beta:=beta) b (beta : ℝ) precision

noncomputable def EvenClosestMonotone2_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat)
    (p q : ℝ)
    (p' q' : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `EvenClosestMonotone2` — the `EvenClosest` rounding is monotone under
    weak inequality: if `p ≤ q` and both are rounded by `EvenClosest`, then
    `F2R p' ≤ F2R q'`.

    Note: this port carries explicit hypotheses matching the Coq semantics:
    - `MonotoneP_float`: the real monotonicity property (p < q → F2R p' ≤ F2R q')
    - `UniqueP`: uniqueness of rounding (same real → same float)
    These capture the properties that `EvenClosestMonotone` and `EvenClosestUniqueP`
    provide in the original Coq proof. -/
theorem EvenClosestMonotone2_from_properties {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radixZ : Int) (radixR : ℝ) (precision : Nat)
    (p q : ℝ)
    (p' q' : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜p ≤ q ∧
        EvenClosest (beta:=beta) b radixR precision p p' ∧
        EvenClosest (beta:=beta) b radixR precision q q' ∧
        MonotoneP_float (EvenClosest (beta:=beta) b radixR precision) ∧
        UniqueP (EvenClosest (beta:=beta) b radixR precision)⌝⦄
    (pure (EvenClosestMonotone2_check (beta:=beta) b radixR precision p q p' q') : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) p' ≤ _root_.F2R (beta:=beta) q'⌝⦄ := by
  intro ⟨hpq, hECp, hECq, hMono, hUniq⟩
  simp only [wp, PostCond.noThrow, pure, EvenClosestMonotone2_check]
  show _root_.F2R (beta:=beta) p' ≤ _root_.F2R (beta:=beta) q'
  -- From Coq proof: case split on p ≤ q into p < q or p = q
  rcases lt_or_eq_of_le hpq with hlt | heq
  · -- Case p < q: apply monotonicity
    exact hMono p q p' q' hlt hECp hECq
  · -- Case p = q: apply uniqueness to get p' = q', then ≤ follows
    exact le_of_eq (hUniq q p' q' (heq ▸ hECp) hECq)

-- Symmetric property of EvenClosest (Coq: `EvenClosestSymmetric`)
noncomputable def EvenClosestSymmetric_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat) : Unit :=
  ()

/-- Coq: `EvenClosestSymmetric` — even tie-breaking is symmetric under negation. -/
theorem EvenClosestSymmetric {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat) :
    ⦃⌜True⌝⦄
    (pure (EvenClosestSymmetric_check (beta:=beta) b radix precision) : Id Unit)
    ⦃⇓_ => ⌜SymmetricP (EvenClosest (beta:=beta) b radix precision)⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, EvenClosestSymmetric_check,
    Id.run, ULift.up_down]
  have hClosestSym : SymmetricP (Closest (beta:=beta) b radix) := by
    have h := ClosestSymmetric (beta:=beta) b radix True.intro
    simpa only [wp, PostCond.noThrow, pure, ClosestSymmetric_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using h
  intro r p hp
  rcases hp with ⟨hClosest, hTie⟩
  constructor
  · exact hClosestSym r p hClosest
  ·
    rcases hTie with hEven | hUnique
    · left
      have h := FNevenFop (beta:=beta) b radix precision p hEven
      simpa only [wp, PostCond.noThrow, pure, FNevenFop_check,
        PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
        Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using h
    · right
      intro q hq
      have hqOppClosest : Closest (beta:=beta) b radix r (Fopp q) := by
        simpa using hClosestSym (-r) q hq
      have hFoppq_eq_p :
          _root_.F2R (beta:=beta) (Fopp (beta:=beta) q) =
            _root_.F2R (beta:=beta) p :=
        hUnique (Fopp q) hqOppClosest
      have hF2R_opp_q :
          _root_.F2R (beta:=beta) (Fopp (beta:=beta) q) =
            - _root_.F2R (beta:=beta) q := by
        have h := (FloatSpec.Calc.Operations.F2R_opp (beta:=beta) q) trivial
        simpa [Fopp, _root_.F2R] using h
      have hF2R_opp_p :
          _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) =
            - _root_.F2R (beta:=beta) p := by
        have h := (FloatSpec.Calc.Operations.F2R_opp (beta:=beta) p) trivial
        simpa [Fopp, _root_.F2R] using h
      rw [hF2R_opp_q] at hFoppq_eq_p
      rw [hF2R_opp_p]
      linarith

noncomputable def EvenClosestFabs_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat)
    (f : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) : Unit :=
  ()

/-- Coq: `EvenClosestFabs` — absolute value preserves even-closest rounding. -/
theorem EvenClosestFabs {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat)
    (f : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) :
    ⦃⌜Fcanonic (beta:=beta) beta b f ∧ 1 < beta ∧
        EvenClosest (beta:=beta) b radix precision r f⌝⦄
    (pure (EvenClosestFabs_check (beta:=beta) b radix precision f r) : Id Unit)
    ⦃⇓_ => ⌜EvenClosest (beta:=beta) b radix precision |r|
      (Fabs (beta:=beta) f)⌝⦄ := by
  intro h
  rcases h with ⟨_hfCan, hbeta, hEvenClosest⟩
  rcases hEvenClosest with ⟨hClosest, hTie⟩
  simp only [wp, PostCond.noThrow, pure, EvenClosestFabs_check,
    Id.run, ULift.up_down]
  have hClosestAbs : Closest (beta:=beta) b radix |r| (Fabs (beta:=beta) f) := by
    have h := ClosestFabs (beta:=beta) b radix f r
    simpa only [wp, PostCond.noThrow, pure, ClosestFabs_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using h ⟨hbeta, hClosest⟩
  constructor
  · exact hClosestAbs
  · rcases hTie with hEven | hUnique
    · left
      have h := FNevenFabs (beta:=beta) b radix precision f
      have hbeta_pos : 0 < beta := lt_trans Int.zero_lt_one hbeta
      simpa only [wp, PostCond.noThrow, pure,
        PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
        Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
        h ⟨hbeta_pos, hEven⟩
    · right
      intro q hq
      by_cases hr : 0 ≤ r
      · have hr_abs : |r| = r := abs_of_nonneg hr
        have hqClosest : Closest (beta:=beta) b radix r q := by
          simpa [hr_abs] using hq
        have hq_eq_f :
            _root_.F2R (beta:=beta) q = _root_.F2R (beta:=beta) f :=
          hUnique q hqClosest
        have hfabs_eq_f :
            _root_.F2R (beta:=beta) (Fabs (beta:=beta) f) =
              _root_.F2R (beta:=beta) f :=
          hUnique (Fabs (beta:=beta) f) (by simpa [hr_abs] using hClosestAbs)
        exact hq_eq_f.trans hfabs_eq_f.symm
      · have hr_lt : r < 0 := lt_of_not_ge hr
        have hr_abs : |r| = -r := abs_of_neg hr_lt
        have hClosestSym : SymmetricP (Closest (beta:=beta) b radix) := by
          have h := ClosestSymmetric (beta:=beta) b radix True.intro
          simpa only [wp, PostCond.noThrow, pure, ClosestSymmetric_check,
            PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
            Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using h
        have hqAtNeg : Closest (beta:=beta) b radix (-r) q := by
          simpa [hr_abs] using hq
        have hqOppClosest : Closest (beta:=beta) b radix r (Fopp (beta:=beta) q) := by
          simpa using hClosestSym (-r) q hqAtNeg
        have hfabsAtNeg : Closest (beta:=beta) b radix (-r) (Fabs (beta:=beta) f) := by
          simpa [hr_abs] using hClosestAbs
        have hfabsOppClosest :
            Closest (beta:=beta) b radix r (Fopp (beta:=beta) (Fabs (beta:=beta) f)) := by
          simpa using hClosestSym (-r) (Fabs (beta:=beta) f) hfabsAtNeg
        have hqOpp_eq_f :
            _root_.F2R (beta:=beta) (Fopp (beta:=beta) q) =
              _root_.F2R (beta:=beta) f :=
          hUnique (Fopp (beta:=beta) q) hqOppClosest
        have hfabsOpp_eq_f :
            _root_.F2R (beta:=beta) (Fopp (beta:=beta) (Fabs (beta:=beta) f)) =
              _root_.F2R (beta:=beta) f :=
          hUnique (Fopp (beta:=beta) (Fabs (beta:=beta) f)) hfabsOppClosest
        have hqOppVal :
            _root_.F2R (beta:=beta) (Fopp (beta:=beta) q) =
              -_root_.F2R (beta:=beta) q := by
          have h := (FloatSpec.Calc.Operations.F2R_opp (beta:=beta) q) trivial
          simpa [Fopp, _root_.F2R] using h
        have hfabsOppVal :
            _root_.F2R (beta:=beta) (Fopp (beta:=beta) (Fabs (beta:=beta) f)) =
              -_root_.F2R (beta:=beta) (Fabs (beta:=beta) f) := by
          have h := (FloatSpec.Calc.Operations.F2R_opp
            (beta:=beta) (Fabs (beta:=beta) f)) trivial
          simpa [Fopp, _root_.F2R] using h
        rw [hqOppVal] at hqOpp_eq_f
        rw [hfabsOppVal] at hfabsOpp_eq_f
        linarith

-- Rounded-mode packaging for `EvenClosest` (Coq: `EvenClosestRoundedModeP`)
noncomputable def EvenClosestRoundedModeP_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat) : Unit :=
  ()

/-- Coq: `EvenClosestRoundedModeP`.

The upstream theorem packages `EvenClosestTotal`, `EvenClosestCompatible_from_structural_payload`,
`EvenClosestMinOrMax`, and `EvenClosestMonotone`.  `EvenClosestTotal` remains
explicit because bounded closest-point existence is still an active
construction; the other three components are proved locally. -/
theorem EvenClosestRoundedModeP_from_total {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radixZ : Int) (radixR : ℝ) (precision : Nat) :
    ⦃⌜TotalP (EvenClosest (beta:=beta) b radixR precision)⌝⦄
    (pure (EvenClosestRoundedModeP_check (beta:=beta) b radixR precision) : Id Unit)
    ⦃⇓_ => ⌜RoundedModeP_float (beta:=beta) b radixZ
      (EvenClosest (beta:=beta) b radixR precision)⌝⦄ := by
  intro hTotal
  simp only [wp, PostCond.noThrow, pure, EvenClosestRoundedModeP_check,
    Id.run, ULift.up_down]
  refine ⟨hTotal, ?_, ?_, ?_⟩
  · have h := EvenClosestCompatible_from_structural_payload (beta:=beta) b radixR precision True.intro
    simpa only [wp, PostCond.noThrow, pure, EvenClosestCompatible_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using h
  · have h := EvenClosestMinOrMax_from_split_radix_payload (beta:=beta) b radixZ radixR precision True.intro
    simpa only [wp, PostCond.noThrow, pure, EvenClosestMinOrMax_check, MinOrMaxP_float,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, MinOrMaxP_float, Int.cast_ofNat] using h
  · have h := EvenClosestMonotone_from_split_radix_payload (beta:=beta) b radixR precision True.intro
    simpa only [wp, PostCond.noThrow, pure, EvenClosestMonotone_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using h

/-- Coq: `EvenClosestUniqueP` — the `EvenClosest` rounding mode is unique, i.e.,
    for any real `r` and any two floats `p`, `q` that are both `EvenClosest`
    roundings of `r`, we have `p = q`.

    Note: the second disjunct in `EvenClosest`
    (`∀ q, Closest … r q → q = p`) directly gives uniqueness whenever it holds.
    The
    non-trivial case is when both `p` and `q` satisfy `EvenClosest` via the
    first disjunct alone (`FNeven … p` and `FNeven … q`).  In the real Coq
    proof, this case is resolved by the `MinOrMax` + canonical-form machinery
    which shows that at most one of the two nearest representable floats is
    even. This port carries explicit hypotheses for this core content:
    among floats satisfying the real `Closest'` predicate (bounded +
    distance-minimizing) and having even `Fnum`, there is at most one for
    each real value `r`; and equal real values for closest canonical candidates
    imply equal local float records.

    Section variables from Coq (`radixMoreThanOne`, `precisionGreaterThanOne`,
    `pGivesBound`) are also reflected in the precondition for completeness. -/
noncomputable def EvenClosestUniqueP_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat) : Unit :=
  ()

theorem EvenClosestUniqueP_from_structural_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat) :
    ⦃⌜1 < (beta : ℤ) ∧ 1 < precision ∧ b.vNum = beta ^ precision ∧
      -- Core hypothesis capturing the Coq proof's content:
      -- When both p and q are EvenClosest roundings of the same real r
      -- (and both satisfy the first disjunct, i.e., both have even Fnum),
      -- they must be equal.  In the real Coq proof, this follows from the
      -- MinOrMax property + canonical-form machinery + the fact that among
      -- two adjacent representable floats, at most one is even.
      (∀ (r : ℝ) (p q : FloatSpec.Core.Defs.FlocqFloat beta),
        FNeven (beta := beta) b radix precision p →
        FNeven (beta := beta) b radix precision q →
        Closest (beta := beta) b radix r p →
        Closest (beta := beta) b radix r q →
        p = q) ∧
      (∀ (r : ℝ) (p q : FloatSpec.Core.Defs.FlocqFloat beta),
        Closest (beta := beta) b radix r p →
        Closest (beta := beta) b radix r q →
        _root_.F2R (beta:=beta) p = _root_.F2R (beta:=beta) q →
        p = q)⌝⦄
    (pure (EvenClosestUniqueP_check (beta:=beta) b radix precision) : Id Unit)
    ⦃⇓_ => ⌜UniquePGeneric (EvenClosest (beta:=beta) b radix precision)⌝⦄ := by
  intro ⟨_hRadix, _hPrec, _hBound, hEvenUnique, hRealUnique⟩
  simp only [wp, PostCond.noThrow, pure, EvenClosestUniqueP_check, PredTrans.pure,
             PredTrans.apply, Id.run, ULift.down]
  show UniquePGeneric (EvenClosest (beta := beta) b radix precision)
  intro r p q hp hq
  -- EvenClosest ... r p = Closest ... r p ∧
  --   (FNeven ... p ∨ ∀ q, Closest ... r q → F2R q = F2R p)
  obtain ⟨hcp, hp_even | hp_all⟩ := hp
  obtain ⟨hcq, hq_even | hq_all⟩ := hq
  · -- Both satisfy FNeven (i.e., Even Fnum): appeal to hEvenUnique
    exact hEvenUnique r p q hp_even hq_even hcp hcq
  · -- q satisfies real-value uniqueness; convert it to structural equality.
    exact hRealUnique r p q hcp hcq (hq_all p hcp)
  · -- p satisfies real-value uniqueness; convert it to structural equality.
    obtain ⟨hcq, _⟩ := hq
    exact hRealUnique r p q hcp hcq (hp_all q hcq).symm

-- ---------------------------------------------------------------------------
-- Underflow/Exponent growth lemmas (ported skeletons)

/-- Coq: `Underf_Err`.

Bundled underflow-error predicate relating a closest rounding `a` in bound `b`
to a bounded witness `a'` in the extended bound `b'`. -/
def Underf_Err {beta : Int} [ValidRadix beta]
    (b b' : Fbound_skel) (radix : Int)
    (a a' : FloatSpec.Core.Defs.FlocqFloat beta) (ra n : ℝ) : Prop :=
  Closest (beta:=beta) b (radix : ℝ) ra a ∧
    Fbounded (beta:=beta) b' a' ∧
    |_root_.F2R (beta:=beta) a - _root_.F2R (beta:=beta) a'| ≤
      n * (radix : ℝ) ^ (-(b.dExp)) ∧
    (-b.dExp ≤ a'.Fexp → _root_.F2R (beta:=beta) a =
      _root_.F2R (beta:=beta) a')

-- Coq: `FexpGeUnderf` — from boundedness and a magnitude lower bound on |F2R f|
-- derive a lower bound on the exponent Fexp f. We keep the statement in terms of
-- integers and real powers, mirroring the Coq intent. Proof deferred.
noncomputable def FexpGeUnderf_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (precision e : Int)
    (f : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FexpGeUnderf` — from boundedness and a magnitude lower bound on |F2R f|
    derive a lower bound on the exponent Fexp f.

    Note: `Fbounded` is concrete; this port still carries explicit hypotheses
    matching Coq's section context:
    - `hBeta`: the radix is at least 2
    - `hBound`: `bo.vNum = beta ^ precision` (Coq's `pGivesBound`)
    - `hFb`: the float is bounded (using `Fbounded'`)
    These make the theorem provable and match the original Coq semantics. -/
theorem FexpGeUnderf_from_expanded_bound_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (precision e : Int)
    (f : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) bo f ∧ (beta : ℝ) ^ e ≤ |_root_.F2R f| ∧
        (1 < beta) ∧ ((bo.vNum : ℝ) = (beta : ℝ) ^ precision) ∧
        Fbounded' bo f⌝⦄
    (pure (FexpGeUnderf_check (beta:=beta) bo precision e f) : Id Unit)
    ⦃⇓_ => ⌜e - precision + 1 ≤ f.Fexp⌝⦄ := by
  intro ⟨_, hMag, hBeta, hBound, hFb⟩
  simp only [wp, PostCond.noThrow, pure, FexpGeUnderf_check,
    Id.run, ULift.up_down]
  show e - precision + 1 ≤ f.Fexp
  -- Equivalently e < f.Fexp + precision
  suffices h : e < f.Fexp + precision by omega
  -- Setup: β > 1 as a real, and β ≠ 0
  have hBetaR : (1 : ℝ) < (beta : ℝ) := by exact_mod_cast hBeta
  have hBetaPos : (0 : ℝ) < (beta : ℝ) := by linarith
  have hBetaNe : (beta : ℝ) ≠ 0 := ne_of_gt hBetaPos
  -- Use zpow strict monotonicity: β^e < β^m ↔ e < m (for β > 1)
  rw [show f.Fexp + precision = precision + f.Fexp from by ring]
  rw [← zpow_lt_zpow_iff_right₀ hBetaR]
  -- Now show (beta : ℝ) ^ e < (beta : ℝ) ^ (precision + f.Fexp)
  calc (beta : ℝ) ^ e
    _ ≤ |_root_.F2R f| := hMag
    _ < (beta : ℝ) ^ (precision + f.Fexp) := by
      -- |F2R f| = |f.Fnum * β^(f.Fexp)| = |f.Fnum| * β^(f.Fexp)
      have hExpPos : (0 : ℝ) < (beta : ℝ) ^ f.Fexp := zpow_pos hBetaPos f.Fexp
      rw [_root_.F2R, FloatSpec.Core.Defs.F2R]
      rw [abs_mul, abs_of_pos hExpPos]
      -- |f.Fnum| < bo.vNum and (bo.vNum : ℝ) = β^precision
      have hNumBd : |((f.Fnum : ℤ) : ℝ)| < (beta : ℝ) ^ precision := by
        have h1 := hFb.1
        have h2 : (↑|f.Fnum| : ℝ) < (bo.vNum : ℝ) := by exact_mod_cast h1
        rwa [Int.cast_abs, hBound] at h2
      -- |f.Fnum| * β^(f.Fexp) < β^precision * β^(f.Fexp) = β^(precision + f.Fexp)
      rw [zpow_add₀ hBetaNe]
      exact mul_lt_mul_of_pos_right hNumBd hExpPos

/-- Coq `FexpGeUnderf` from the binary-radix section. -/
theorem FexpGeUnderf
    (b : Fbound_skel) (precision : Nat) (e : Int)
    (f : FloatSpec.Core.Defs.FlocqFloat 2) :
    ⦃⌜b.vNum = Zpower_nat 2 precision ∧ Fbounded b f ∧
        (2 : ℝ) ^ e ≤ |_root_.F2R (beta:=2) f|⌝⦄
    (pure (FexpGeUnderf_check (beta:=2) b (precision : Int) e f) : Id Unit)
    ⦃⇓_ => ⌜e - (precision : Int) + 1 ≤ f.Fexp⌝⦄ := by
  intro ⟨hvNum, hf, hmag⟩
  have hvNumR : (b.vNum : ℝ) = (2 : ℝ) ^ (precision : Int) := by
    rw [hvNum, Zpower_nat]
    norm_num
  exact FexpGeUnderf_from_expanded_bound_payload (beta:=2)
    b (precision : Int) e f
      ⟨hf, hmag, by norm_num, hvNumR, hf⟩

-- Coq: `AddExpGeUnderf` — if `g` is a closest rounding of `f1+f2` and both `f1`
-- and `f2` are sufficiently large in magnitude, then `g` is either zero or has
-- magnitude at least `β^(e-precision+1)`.
--
-- Note: this port carries explicit hypotheses matching Coq's section context and a projection of
-- `plusExactExp`, which is now available separately below:
-- - `hBeta`: the radix is at least 2
-- - `hPrec`: precision is at least 1
-- - `hBound`: `bo.vNum = beta ^ precision` (Coq's `pGivesBound`)
-- - `hFb1`, `hFb2`: real boundedness of `f1` and `f2`
-- - `hPlusExact`: key content of Coq's `plusExactExp` — if g is nonzero, there
--   exists a bounded float `g'` with `F2R g' = F2R g` and whose exponent is at
--   least `min(Fexp f1, Fexp f2)`.
noncomputable def AddExpGeUnderf_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (precision e : Int) (radix : ℝ)
    (f1 f2 g : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem AddExpGeUnderf_from_plusExact_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (precision e : Int) (radix : ℝ)
    (f1 f2 g : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Closest (beta:=beta) bo radix (_root_.F2R f1 + _root_.F2R f2) g ∧
        Fbounded (beta:=beta) bo f1 ∧ Fbounded (beta:=beta) bo f2 ∧
        (beta : ℝ) ^ e ≤ |_root_.F2R f1| ∧ (beta : ℝ) ^ e ≤ |_root_.F2R f2| ∧
        -- Additional real hypotheses exposing the Coq proof obligations.
        (1 < beta) ∧ (1 ≤ precision) ∧ ((bo.vNum : ℝ) = (beta : ℝ) ^ precision) ∧
        Fbounded' bo f1 ∧ Fbounded' bo f2 ∧
        -- Key content of plusExactExp: if g ≠ 0, there exists g' with F2R g' = F2R g
        -- and Fexp g' ≥ min(Fexp f1, Fexp f2) and g' is bounded
        (_root_.F2R g ≠ 0 →
          ∃ g' : FloatSpec.Core.Defs.FlocqFloat beta,
            Fbounded' bo g' ∧ _root_.F2R g' = _root_.F2R g ∧
            min f1.Fexp f2.Fexp ≤ g'.Fexp)⌝⦄
    (pure (AddExpGeUnderf_check (beta:=beta) bo precision e radix f1 f2 g) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R g = 0 ∨ (beta : ℝ) ^ (e - precision + 1) ≤ |_root_.F2R g|⌝⦄ := by
  intro ⟨_, _, _, hMag1, hMag2, hBeta, hPrec, hBound, hFb1, hFb2, hPlusExact⟩
  simp only [wp, PostCond.noThrow, pure, AddExpGeUnderf_check,
    Id.run, ULift.up_down]
  show _root_.F2R g = 0 ∨ (beta : ℝ) ^ (e - precision + 1) ≤ |_root_.F2R g|
  -- Case split: either F2R g = 0 or F2R g ≠ 0
  by_cases hg : _root_.F2R g = 0
  · exact Or.inl hg
  · right
    -- Get g' from plusExactExp hypothesis
    obtain ⟨g', hg'Bd, hg'Eq, hg'Exp⟩ := hPlusExact hg
    -- Setup: β > 1 as real, β > 0, β ≠ 0
    have hBetaR : (1 : ℝ) < (beta : ℝ) := by exact_mod_cast hBeta
    have hBetaPos : (0 : ℝ) < (beta : ℝ) := by linarith
    have hBetaNe : (beta : ℝ) ≠ 0 := ne_of_gt hBetaPos
    -- From FexpGeUnderf applied to f1: e - precision + 1 ≤ Fexp f1
    have hExpF1 : e - precision + 1 ≤ f1.Fexp := by
      suffices h : e < f1.Fexp + precision by omega
      rw [show f1.Fexp + precision = precision + f1.Fexp from by ring]
      rw [← zpow_lt_zpow_iff_right₀ hBetaR]
      calc (beta : ℝ) ^ e
        _ ≤ |_root_.F2R f1| := hMag1
        _ < (beta : ℝ) ^ (precision + f1.Fexp) := by
          have hExpPos : (0 : ℝ) < (beta : ℝ) ^ f1.Fexp := zpow_pos hBetaPos f1.Fexp
          rw [_root_.F2R, FloatSpec.Core.Defs.F2R]
          rw [abs_mul, abs_of_pos hExpPos]
          have hNumBd : |((f1.Fnum : ℤ) : ℝ)| < (beta : ℝ) ^ precision := by
            have h1 := hFb1.1
            have h2 : (↑|f1.Fnum| : ℝ) < (bo.vNum : ℝ) := by exact_mod_cast h1
            rwa [Int.cast_abs, hBound] at h2
          rw [zpow_add₀ hBetaNe]
          exact mul_lt_mul_of_pos_right hNumBd hExpPos
    -- From FexpGeUnderf applied to f2: e - precision + 1 ≤ Fexp f2
    have hExpF2 : e - precision + 1 ≤ f2.Fexp := by
      suffices h : e < f2.Fexp + precision by omega
      rw [show f2.Fexp + precision = precision + f2.Fexp from by ring]
      rw [← zpow_lt_zpow_iff_right₀ hBetaR]
      calc (beta : ℝ) ^ e
        _ ≤ |_root_.F2R f2| := hMag2
        _ < (beta : ℝ) ^ (precision + f2.Fexp) := by
          have hExpPos : (0 : ℝ) < (beta : ℝ) ^ f2.Fexp := zpow_pos hBetaPos f2.Fexp
          rw [_root_.F2R, FloatSpec.Core.Defs.F2R]
          rw [abs_mul, abs_of_pos hExpPos]
          have hNumBd : |((f2.Fnum : ℤ) : ℝ)| < (beta : ℝ) ^ precision := by
            have h1 := hFb2.1
            have h2 : (↑|f2.Fnum| : ℝ) < (bo.vNum : ℝ) := by exact_mod_cast h1
            rwa [Int.cast_abs, hBound] at h2
          rw [zpow_add₀ hBetaNe]
          exact mul_lt_mul_of_pos_right hNumBd hExpPos
    -- From the above: e - precision + 1 ≤ min(Fexp f1, Fexp f2)
    have hExpMin : e - precision + 1 ≤ min f1.Fexp f2.Fexp := by
      simp [le_min_iff]; exact ⟨hExpF1, hExpF2⟩
    -- Therefore e - precision + 1 ≤ Fexp g'
    have hExpG' : e - precision + 1 ≤ g'.Fexp := le_trans hExpMin hg'Exp
    -- Since F2R g' ≠ 0, g'.Fnum ≠ 0, so |g'.Fnum| ≥ 1
    have hg'Ne : _root_.F2R g' ≠ 0 := hg'Eq ▸ hg
    have hFnumNe : g'.Fnum ≠ 0 := by
      intro h
      apply hg'Ne
      unfold _root_.F2R FloatSpec.Core.Defs.F2R
      simp [h]
    have hFnumAbs : 1 ≤ |g'.Fnum| := Int.one_le_abs hFnumNe
    -- |F2R g| = |F2R g'| = |g'.Fnum| * β^(g'.Fexp) ≥ 1 * β^(g'.Fexp) = β^(g'.Fexp) ≥ β^(e-precision+1)
    rw [← hg'Eq]
    unfold _root_.F2R FloatSpec.Core.Defs.F2R
    rw [abs_mul]
    have hExpPos : (0 : ℝ) < (beta : ℝ) ^ g'.Fexp := zpow_pos hBetaPos g'.Fexp
    rw [abs_of_pos hExpPos]
    calc (beta : ℝ) ^ (e - precision + 1)
      _ ≤ (beta : ℝ) ^ g'.Fexp := by
          exact zpow_right_mono₀ hBetaR.le hExpG'
      _ = 1 * (beta : ℝ) ^ g'.Fexp := by ring
      _ ≤ |((g'.Fnum : ℤ) : ℝ)| * (beta : ℝ) ^ g'.Fexp := by
          apply mul_le_mul_of_nonneg_right _ hExpPos.le
          exact_mod_cast hFnumAbs

noncomputable def AddExpGeUnderf2_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (precision e : Int) (radix : ℝ)
    (f1 f2 g : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `AddExpGeUnderf2`.

Nonzero projection of `AddExpGeUnderf`: if the rounded sum is not zero, the
underflow-disjunction collapses to the lower magnitude bound. -/
theorem AddExpGeUnderf2_from_plusExact_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (precision e : Int) (radix : ℝ)
    (f1 f2 g : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Closest (beta:=beta) bo radix (_root_.F2R f1 + _root_.F2R f2) g ∧
        Fbounded (beta:=beta) bo f1 ∧ Fbounded (beta:=beta) bo f2 ∧
        (beta : ℝ) ^ e ≤ |_root_.F2R f1| ∧ (beta : ℝ) ^ e ≤ |_root_.F2R f2| ∧
        (1 < beta) ∧ (1 ≤ precision) ∧ ((bo.vNum : ℝ) = (beta : ℝ) ^ precision) ∧
        Fbounded' bo f1 ∧ Fbounded' bo f2 ∧
        (_root_.F2R g ≠ 0 →
          ∃ g' : FloatSpec.Core.Defs.FlocqFloat beta,
            Fbounded' bo g' ∧ _root_.F2R g' = _root_.F2R g ∧
            min f1.Fexp f2.Fexp ≤ g'.Fexp) ∧
        _root_.F2R g ≠ 0⌝⦄
    (pure (AddExpGeUnderf2_check (beta:=beta) bo precision e radix f1 f2 g) :
      Id Unit)
    ⦃⇓_ => ⌜(beta : ℝ) ^ (e - precision + 1) ≤ |_root_.F2R g|⌝⦄ := by
  intro h
  rcases h with
    ⟨hClosest, hf1Bound, hf2Bound, hMag1, hMag2, hBeta, hPrec,
      hBound, hf1Bound', hf2Bound', hPlusExact, hgNonzero⟩
  simp only [wp, PostCond.noThrow, pure, AddExpGeUnderf2_check,
    Id.run, ULift.up_down]
  have hmain := AddExpGeUnderf_from_plusExact_payload
    (beta:=beta) bo precision e radix f1 f2 g
  have hdisj :
      _root_.F2R g = 0 ∨
        (beta : ℝ) ^ (e - precision + 1) ≤ |_root_.F2R g| := by
    simpa only [wp, PostCond.noThrow, pure, AddExpGeUnderf_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
      hmain ⟨hClosest, hf1Bound, hf2Bound, hMag1, hMag2, hBeta, hPrec,
        hBound, hf1Bound', hf2Bound', hPlusExact⟩
  exact hdisj.resolve_left hgNonzero

-- First projection: RoundedModePGeneric -> CompatiblePGeneric
noncomputable def RoundedModeP_inv2_check {α : Type}
    (P : ℝ → α → Prop) : Unit :=
  ()

theorem RoundedModePGeneric_inv2 {α : Type} (P : ℝ → α → Prop) :
    ⦃⌜RoundedModePGeneric P⌝⦄
    (pure (RoundedModeP_inv2_check P) : Id Unit)
    ⦃⇓_ => ⌜CompatiblePGeneric P⌝⦄ := by
  intro ⟨_, hCompat, _, _⟩
  simp only [wp, PostCond.noThrow, pure, RoundedModeP_inv2_check,
    Id.run, ULift.up_down]
  exact hCompat

-- Fourth projection: RoundedModePGeneric -> MonotonePGeneric
noncomputable def RoundedModeP_inv4_check {α : Type}
    (P : ℝ → α → Prop) : Unit :=
  ()

theorem RoundedModePGeneric_inv4 {α : Type} (P : ℝ → α → Prop) :
    ⦃⌜RoundedModePGeneric P⌝⦄
    (pure (RoundedModeP_inv4_check P) : Id Unit)
    ⦃⇓_ => ⌜MonotonePGeneric P⌝⦄ := by
  intro ⟨_, _, _, hMono⟩
  simp only [wp, PostCond.noThrow, pure, RoundedModeP_inv4_check,
    Id.run, ULift.up_down]
  exact hMono

-- Projection to a projector property
noncomputable def RoundedProjector_check {α : Type}
    (P : ℝ → α → Prop) : Unit :=
  ()

/-- Coq: `RoundedProjector` — a rounded-mode relation is a projector.

For the generic Lean fallback, `ProjectorPGeneric` is the same same-input uniqueness
property carried by `MinOrMaxPGeneric`, the third component of `RoundedModePGeneric`. -/
theorem RoundedProjectorGeneric {α : Type} (P : ℝ → α → Prop) :
    ⦃⌜RoundedModePGeneric P⌝⦄
    (pure (RoundedProjector_check P) : Id Unit)
    ⦃⇓_ => ⌜ProjectorPGeneric P⌝⦄ := by
  intro hRounded
  simp only [wp, PostCond.noThrow, pure, RoundedProjector_check,
    Id.run, ULift.up_down]
  exact hRounded.2.2.1

/-- Coq `RoundedModeP_inv2`. -/
theorem RoundedModeP_inv2 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop) :
    RoundedModeP b P → CompatibleP b P := by
  exact fun h => h.2.1

/-- Coq `RoundedModeP_inv4`. -/
theorem RoundedModeP_inv4 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop) :
    RoundedModeP b P → MonotoneP P := by
  exact fun h => h.2.2.2

/-- Coq `RoundedProjector`: the min/max component of a rounded mode implies
    the source projector property. -/
theorem RoundedProjector {beta : Int} [ValidRadix beta]
    (b : Fbound_skel)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop) :
    RoundedModeP b P → ProjectorP b P := by
  intro hRounded p q hpBounded hpq
  rcases hRounded.2.2.1 (_root_.F2R p) q hpq with hMin | hMax
  · exact le_antisymm
      (hMin.2.2 p hpBounded le_rfl)
      hMin.2.1
  · exact le_antisymm
      hMax.2.1
      (hMax.2.2 p hpBounded le_rfl)

-- Coq: `RoundedModeProjectorIdem` — under RoundedModeP, P p p for bounded p
noncomputable def RoundedModeProjectorIdem_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq `RoundedModeProjectorIdem`: every bounded float is a fixed point of a
    rounded-mode relation. -/
theorem RoundedModeProjectorIdem {beta : Int} [ValidRadix beta]
    (b : Fbound_skel)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p : FloatSpec.Core.Defs.FlocqFloat beta)
    (hRounded : RoundedModeP b P)
    (hpBounded : Fbounded (beta := beta) b p) :
    P (_root_.F2R p) p := by
  obtain ⟨q, hPq⟩ := hRounded.1 (_root_.F2R p)
  have hreal : _root_.F2R p = _root_.F2R q :=
    RoundedProjector b P hRounded p q hpBounded hPq
  exact hRounded.2.1 (_root_.F2R p) (_root_.F2R p) q p
    hPq rfl hreal.symm hpBounded

-- Coq: `RoundedModeBounded` — from P r q under RoundedModeP, q is bounded
noncomputable def RoundedModeBounded_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (r : ℝ) (q : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq `RoundedModeBounded`: every output of a rounded mode is bounded. -/
theorem RoundedModeBounded {beta : Int} [ValidRadix beta]
    (b : Fbound_skel)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (r : ℝ) (q : FloatSpec.Core.Defs.FlocqFloat beta)
    (hRounded : RoundedModeP b P) (hP : P r q) :
    Fbounded (beta := beta) b q := by
  rcases hRounded.2.2.1 r q hP with hMin | hMax
  · exact hMin.1
  · exact hMax.1

/-- Internal compatibility bridge for legacy proof bodies.  It packages a
source `RoundedModeP` without exposing the package in a public source theorem. -/
theorem RoundedModeP.toFullPayload {beta : Int} [ValidRadix beta]
    {b : Fbound_skel}
    {P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop}
    (h : RoundedModeP b P) : RoundedModeP_full b P := by
  refine ⟨h.1, ?_, h.2.2.2, ?_, ?_⟩
  · intro r₁ r₂ p q hp hr hpq
    subst r₂
    subst q
    exact hp
  · intro p hp
    exact RoundedModeProjectorIdem b P p h hp
  · exact RoundedProjector b P h

/-- Internal compatibility bridge for legacy proof bodies. -/
theorem RoundedModeP.toFloatPayload {beta : Int} [ValidRadix beta]
    {b : Fbound_skel}
    {P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop}
    (h : RoundedModeP b P) : RoundedModeP_float b beta P := by
  refine ⟨h.1, ?_, h.2.2.1, h.2.2.2⟩
  intro r₁ r₂ p q hp hr hpq
  subst r₂
  subst q
  exact hp

/-- Compatibility helper for developments still packaged with the explicitly
    named legacy float bundle.  It uses only that bundle's source-faithful
    min/max component. -/
theorem RoundedModeP_float_bounded {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (hRounded : RoundedModeP_float (beta := beta) b radix P)
    {r : ℝ} {q : FloatSpec.Core.Defs.FlocqFloat beta} (hpq : P r q) :
    Fbounded b q := by
  rcases hRounded.2.2.1 r q hpq with hMin | hMax
  · exact hMin.1
  · exact hMax.1


-- ---------------------------------------------------------------------------
-- Coq: `PminPos` — existence of bounded complement to the min rounding

noncomputable def PminPos_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p min : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/- `PminPos` is proved below, after its dependencies `eqExpMax` and
`FboundNext` are available. -/

-- Coq: `RoundedModeProjectorIdemEq` — equality on reals under RoundedModeP
noncomputable def RoundedModeProjectorIdemEq_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq `RoundedModeProjectorIdemEq`: projector equality derived solely from
    rounded-mode semantics, boundedness, and the rounding witness. -/
theorem RoundedModeProjectorIdemEq {beta : Int} [ValidRadix beta]
    (b : Fbound_skel)
    (precision : Nat)
    (_hprecision : 1 < precision)
    (_hvnum : b.vNum = Zpower_nat beta precision)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta)
    (hRounded : RoundedModeP b P)
    (hBnd : Fbounded (beta := beta) b p)
    (hPpq : P (_root_.F2R p) q) :
    _root_.F2R p = _root_.F2R q := by
  exact RoundedProjector b P hRounded p q hBnd hPpq

-- Coq: `RoundedModeUlp_from_rounded_payload` — under a rounded mode P and P p q, |p - q| < Fulp q
noncomputable def RoundedModeUlp_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p : ℝ) (q : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/- The theorem using this checker is declared after the normalized-neighbor
stack (`FulpSuc`/`FulpPred`) it depends on. -/

-- Coq: `RoundedModeErrorExpStrict_from_rounded_payload` — nonzero rounding error has lower exponent
noncomputable def RoundedModeErrorExpStrict_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) (x : ℝ) : Unit :=
  ()

-- Coq: `RoundedModeMult_from_scale_payload` — monotonicity wrt scaling by radix
noncomputable def RoundedModeMult_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (r : ℝ) (q q' : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `RoundedModeMult_from_scale_payload`.

Upstream uses `FBoundedScale` to show that the scaled float
`Float q'.Fnum (q'.Fexp + 1)` is bounded and represents `radix * q'`.  Those
scaling facts are still explicit obligations in this port; the rounding-mode
argument itself is restored through `RleBoundRoundr_from_full`. -/
theorem RoundedModeMult_from_scale_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (r : ℝ) (q q' : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜RoundedModeP_full (beta:=beta) b P ∧
        P r q ∧
        Fbounded (beta:=beta) b ⟨q'.Fnum, q'.Fexp + 1⟩ ∧
        _root_.F2R (beta:=beta) ⟨q'.Fnum, q'.Fexp + 1⟩ =
          radix * _root_.F2R (beta:=beta) q' ∧
        r ≤ radix * _root_.F2R (beta:=beta) q'⌝⦄
    (pure (RoundedModeMult_check (beta:=beta) b radix P r q q') : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) q ≤
        radix * _root_.F2R (beta:=beta) q'⌝⦄ := by
  intro ⟨hRounded, hP, hScaledBounded, hScaledValue, hUpper⟩
  simp only [wp, PostCond.noThrow, pure, RoundedModeMult_check,
    Id.run, ULift.up_down]
  let scaled : FloatSpec.Core.Defs.FlocqFloat beta := ⟨q'.Fnum, q'.Fexp + 1⟩
  have hScaledUpper : r ≤ _root_.F2R (beta:=beta) scaled := by
    rw [hScaledValue]
    exact hUpper
  have hRound := RleBoundRoundr_from_full (beta:=beta) b (radix := 0) P scaled q r
  simp only [wp, PostCond.noThrow, pure, RleBoundRoundr_check,
    PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
    Id.run, ULift.up, ULift.down, ULift.up_down] at hRound
  have hq_le_scaled :
      _root_.F2R (beta:=beta) q ≤ _root_.F2R (beta:=beta) scaled :=
    hRound ⟨hRounded, by simpa [scaled] using hScaledBounded, hScaledUpper, hP⟩
  rw [hScaledValue] at hq_le_scaled
  exact hq_le_scaled

-- Coq: `RoundedModeMultLess_from_scale_payload` — dual inequality for scaling by radix
noncomputable def RoundedModeMultLess_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (r : ℝ) (q q' : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `RoundedModeMultLess_from_scale_payload`; lower-bound dual of `RoundedModeMult_from_scale_payload`. -/
theorem RoundedModeMultLess_from_scale_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (r : ℝ) (q q' : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜RoundedModeP_full (beta:=beta) b P ∧
        P r q ∧
        Fbounded (beta:=beta) b ⟨q'.Fnum, q'.Fexp + 1⟩ ∧
        _root_.F2R (beta:=beta) ⟨q'.Fnum, q'.Fexp + 1⟩ =
          radix * _root_.F2R (beta:=beta) q' ∧
        radix * _root_.F2R (beta:=beta) q' ≤ r⌝⦄
    (pure (RoundedModeMultLess_check (beta:=beta) b radix P r q q') : Id Unit)
    ⦃⇓_ => ⌜radix * _root_.F2R (beta:=beta) q' ≤
        _root_.F2R (beta:=beta) q⌝⦄ := by
  intro ⟨hRounded, hP, hScaledBounded, hScaledValue, hLower⟩
  simp only [wp, PostCond.noThrow, pure, RoundedModeMultLess_check,
    Id.run, ULift.up_down]
  let scaled : FloatSpec.Core.Defs.FlocqFloat beta := ⟨q'.Fnum, q'.Fexp + 1⟩
  have hScaledLower : _root_.F2R (beta:=beta) scaled ≤ r := by
    rw [hScaledValue]
    exact hLower
  have hRound := RleBoundRoundl_from_full (beta:=beta) b (radix := 0) P scaled q r
  simp only [wp, PostCond.noThrow, pure, RleBoundRoundl_check,
    PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
    Id.run, ULift.up, ULift.down, ULift.up_down] at hRound
  have hscaled_le_q :
      _root_.F2R (beta:=beta) scaled ≤ _root_.F2R (beta:=beta) q :=
    hRound ⟨hRounded, by simpa [scaled] using hScaledBounded, hScaledLower, hP⟩
  rw [hScaledValue] at hscaled_le_q
  exact hscaled_le_q

-- Coq: `FnormalBounded` — normal floats are bounded
noncomputable def FnormalBounded_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FnormalBounded` — if a float is normal with respect to `b` and `radix`,
    then it is bounded by `b`. -/
theorem FnormalBounded {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fnormal (beta:=beta) radix b p⌝⦄
    (pure (FnormalBounded_check (beta:=beta) b radix p) : Id Unit)
    ⦃⇓_ => ⌜Fbounded (beta:=beta) b p⌝⦄ := by
  intro hnormal
  simpa only [wp, PostCond.noThrow, pure, FnormalBounded_check,
    PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
    Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using hnormal.1

-- Coq: `FnormalNotZero` — normal floats are not the zero float
noncomputable def FnormalNotZero_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FnormalNotZero` — if `p` is normal w.r.t. `b` and `radix`, then `p` is
    not the zero float.

    Since `Fnormal` is concrete, this port carries explicit hypotheses matching
    the Coq `Fnormal` definition:
    - `0 < b.vNum`: the bound's digit count is positive (in Coq, `vNum` has type `positive`)
    - `b.vNum ≤ |radixInt * p.Fnum|`: the normality condition from Coq
    These make the theorem provable and match the original Coq semantics. -/
theorem FnormalNotZero_from_expanded_normal_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p : FloatSpec.Core.Defs.FlocqFloat beta)
    (radixInt : Int)
    (hvNum_pos : 0 < b.vNum)
    (hNormal : b.vNum ≤ |radixInt * p.Fnum|) :
    ⦃⌜Fnormal (beta:=beta) radix b p⌝⦄
    (pure (FnormalNotZero_check (beta:=beta) b radix p) : Id Unit)
    ⦃⇓_ => ⌜¬ is_Fzero p⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, FnormalNotZero_check, ULift.down_up]
  show ¬ is_Fzero p
  unfold is_Fzero
  intro hzero
  -- hzero : p.Fnum = 0
  -- hNormal : b.vNum ≤ |radixInt * p.Fnum|
  -- hvNum_pos : 0 < b.vNum
  rw [hzero, mul_zero, abs_zero] at hNormal
  -- hNormal : b.vNum ≤ 0, contradicts hvNum_pos : 0 < b.vNum
  omega

/-- Coq `FnormalNotZero`. -/
theorem FnormalNotZero {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fnormal (beta:=beta) beta b p⌝⦄
    (pure (FnormalNotZero_check (beta:=beta) b beta p) : Id Unit)
    ⦃⇓_ => ⌜¬ is_Fzero p⌝⦄ := by
  intro hp
  exact FnormalNotZero_from_expanded_normal_payload
    (beta:=beta) b beta p beta b.vNum_pos hp.2 hp

-- Coq: `FnormalFop` — normality is preserved by float negation
noncomputable def FnormalFop_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FnormalFop` — if a float is normal, then its negation is also normal. -/
theorem FnormalFop {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fnormal (beta:=beta) radix b p⌝⦄
    (pure (FnormalFop_check (beta:=beta) b radix p) : Id Unit)
    ⦃⇓_ => ⌜Fnormal (beta:=beta) radix b (FloatSpec.Calc.Operations.Fopp (beta:=beta) p)⌝⦄ := by
  intro hnormal
  simp only [wp, PostCond.noThrow, pure, FnormalFop_check, Id.run, ULift.up_down]
  simpa [Fnormal, Fbounded, FloatSpec.Calc.Operations.Fopp, abs_mul, abs_neg, mul_neg]
    using hnormal

-- Coq: `FnormalFabs` — normality is preserved by float absolute value
noncomputable def FnormalFabs_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FnormalFabs` — taking the absolute value of a normal float keeps it normal. -/
theorem FnormalFabs {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fnormal (beta:=beta) radix b p⌝⦄
    (pure (FnormalFabs_check (beta:=beta) b radix p) : Id Unit)
    ⦃⇓_ => ⌜Fnormal (beta:=beta) radix b (Fabs (beta:=beta) p)⌝⦄ := by
  intro hnormal
  simp only [wp, PostCond.noThrow, pure, FnormalFabs_check, Id.run, ULift.up_down]
  rcases hnormal with ⟨⟨hnum, hexp⟩, hmant⟩
  refine ⟨⟨?_, ?_⟩, ?_⟩
  · simpa [Fabs, FloatSpec.Calc.Operations.Fabs, Int.natCast_natAbs] using hnum
  · simpa [Fabs, FloatSpec.Calc.Operations.Fabs] using hexp
  · simpa [Fabs, FloatSpec.Calc.Operations.Fabs, Int.natCast_natAbs, abs_mul] using hmant

-- Coq: `FsubnormalFbounded` — subnormal floats are bounded
noncomputable def FsubnormalFbounded_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FsubnormalFbounded` — if a float is subnormal with respect to `b` and
    `radix`, then it is bounded by `b`. -/
theorem FsubnormalFbounded {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fsubnormal (beta:=beta) radix b p⌝⦄
    (pure (FsubnormalFbounded_check (beta:=beta) b radix p) : Id Unit)
    ⦃⇓_ => ⌜Fbounded (beta:=beta) b p⌝⦄ := by
  intro hsub
  simp only [wp, PostCond.noThrow, pure, FsubnormalFbounded_check,
    Id.run, ULift.up_down]
  exact hsub.1

-- Coq: `FsubnormalFexp` — exponent of a subnormal float is fixed
noncomputable def FsubnormalFexp_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FsubnormalFexp` — subnormal floats all share the minimal exponent
    `-b.dExp`.

    Note: `Fsubnormal` is concrete; this port carries explicit hypotheses
    matching Coq's `Fsubnormal` definition:
    - `p.Fexp = -b.dExp`: the exponent is the minimal exponent
    These make the theorem provable and match the original Coq semantics. -/
theorem FsubnormalFexp_from_expanded_subnormal_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fsubnormal (beta:=beta) radix b p ∧ p.Fexp = -b.dExp⌝⦄
    (pure (FsubnormalFexp_check (beta:=beta) b radix p) : Id Unit)
    ⦃⇓_ => ⌜p.Fexp = -b.dExp⌝⦄ := by
  intro ⟨_, hExp⟩
  simp only [wp, PostCond.noThrow, pure, FsubnormalFexp_check,
    Id.run, ULift.up_down]
  exact hExp

/-- Coq `FsubnormalFexp`. -/
theorem FsubnormalFexp {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fsubnormal (beta:=beta) beta b p⌝⦄
    (pure (FsubnormalFexp_check (beta:=beta) b beta p) : Id Unit)
    ⦃⇓_ => ⌜p.Fexp = -b.dExp⌝⦄ := by
  intro hp
  exact FsubnormalFexp_from_expanded_subnormal_payload
    (beta:=beta) b beta p ⟨hp, hp.2.1⟩

-- Coq: `FsubnormFopp` — subnormality preserved by float negation
noncomputable def FsubnormFopp_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FsubnormFopp` — if `p` is subnormal, then so is `Fopp p`. -/
theorem FsubnormFopp {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fsubnormal (beta:=beta) radix b p⌝⦄
    (pure (FsubnormFopp_check (beta:=beta) b radix p) : Id Unit)
    ⦃⇓_ => ⌜Fsubnormal (beta:=beta) radix b
            (FloatSpec.Calc.Operations.Fopp (beta:=beta) p)⌝⦄ := by
  intro hsub
  simp only [wp, PostCond.noThrow, pure, FsubnormFopp_check,
    Id.run, ULift.up_down]
  simpa [Fsubnormal, Fbounded, FloatSpec.Calc.Operations.Fopp, abs_mul, abs_neg, mul_neg]
    using hsub

-- Coq: `FsubnormFabs` — subnormality preserved by float absolute value
noncomputable def FsubnormFabs_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FsubnormFabs` — if `p` is subnormal, then so is `Fabs p`. -/
theorem FsubnormFabs {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fsubnormal (beta:=beta) radix b p⌝⦄
    (pure (FsubnormFabs_check (beta:=beta) b radix p) : Id Unit)
    ⦃⇓_ => ⌜Fsubnormal (beta:=beta) radix b
            (FloatSpec.Calc.Operations.Fabs (beta:=beta) p)⌝⦄ := by
  intro hsub
  simp only [wp, PostCond.noThrow, pure, FsubnormFabs_check,
    Id.run, ULift.up_down]
  simpa [Fsubnormal, Fbounded, FloatSpec.Calc.Operations.Fabs, Nat.cast_natAbs, abs_mul]
    using hsub

-- Coq: `FsubnormalUnique` — subnormal floats equal as reals coincide syntactically
noncomputable def FsubnormalUnique_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FsubnormalUnique` — if two subnormal floats have identical real
    values, they are equal. Mirrors the Coq statement with Hoare triple syntax.

    Note: `Fsubnormal` is concrete; this port carries explicit hypotheses
    matching Coq's `Fsubnormal` definition:
    - `p.Fexp = -b.dExp` and `q.Fexp = -b.dExp`: both have minimal exponent
    - `1 < beta`: radix condition
    These make the theorem provable and match the original Coq semantics. -/
theorem FsubnormalUnique_from_expanded_subnormal_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fsubnormal (beta:=beta) radix b p ∧
        Fsubnormal (beta:=beta) radix b q ∧
        _root_.F2R (beta:=beta) p = _root_.F2R (beta:=beta) q ∧
        p.Fexp = -b.dExp ∧ q.Fexp = -b.dExp ∧ (1 < beta)⌝⦄
    (pure (FsubnormalUnique_check (beta:=beta) b radix p q) : Id Unit)
    ⦃⇓_ => ⌜p = q⌝⦄ := by
  intro ⟨_, _, hF2R, hpe, hqe, hβ⟩
  simp only [wp, PostCond.noThrow, pure, FsubnormalUnique_check,
    Id.run, ULift.up_down]
  show p = q
  -- F2R p = p.Fnum * β ^ p.Fexp, F2R q = q.Fnum * β ^ q.Fexp
  -- Since p.Fexp = q.Fexp = -b.dExp, we have p.Fnum * β^e = q.Fnum * β^e
  -- Since β > 1, β^e ≠ 0, so p.Fnum = q.Fnum
  have hexp_eq : p.Fexp = q.Fexp := by rw [hpe, hqe]
  unfold F2R FloatSpec.Core.Defs.F2R at hF2R
  have hβ_pos : (0 : ℝ) < (beta : ℝ) := by exact_mod_cast (lt_trans Int.zero_lt_one hβ)
  have hpow_pos : (0 : ℝ) < (beta : ℝ) ^ q.Fexp := zpow_pos hβ_pos q.Fexp
  have hpow_ne : ((beta : ℝ) ^ q.Fexp : ℝ) ≠ 0 := ne_of_gt hpow_pos
  rw [hexp_eq] at hF2R
  have hnum_eq : (p.Fnum : ℝ) = (q.Fnum : ℝ) := by
    exact mul_right_cancel₀ hpow_ne hF2R
  have hnum_eq_int : p.Fnum = q.Fnum := by exact_mod_cast hnum_eq
  cases p; cases q; simp at hnum_eq_int hexp_eq ⊢; exact ⟨hnum_eq_int, hexp_eq⟩

/-- Coq `FsubnormalUnique`. -/
theorem FsubnormalUnique {beta : Int} [ValidRadix beta]
    (b : Fbound_skel)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fsubnormal (beta:=beta) beta b p ∧
        Fsubnormal (beta:=beta) beta b q ∧
        _root_.F2R p = _root_.F2R q⌝⦄
    (pure (FsubnormalUnique_check (beta:=beta) b beta p q) : Id Unit)
    ⦃⇓_ => ⌜p = q⌝⦄ := by
  intro ⟨hp, hq, hpq⟩
  exact FsubnormalUnique_from_expanded_subnormal_payload
    (beta:=beta) b beta p q
      ⟨hp, hq, hpq, hp.2.1, hq.2.1, ValidRadix.valid (beta:=beta)⟩

-- Coq: `FsubnormalLt` — ordering subnormal mantissas mirrors real order
noncomputable def FsubnormalLt_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FsubnormalLt` — if two floats are subnormal and their real values
    satisfy `p < q`, then their mantissas follow the same strict order.

    Note: `Fsubnormal` is concrete; this port carries explicit hypotheses
    matching Coq's `Fsubnormal` definition:
    - `p.Fexp = -b.dExp` and `q.Fexp = -b.dExp`: subnormal floats share the minimum exponent
    - `(beta : ℤ) > 0`: the radix is positive
    These make the theorem provable and match the original Coq semantics. -/
theorem FsubnormalLt_from_expanded_subnormal_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fsubnormal (beta:=beta) radix b p ∧
        Fsubnormal (beta:=beta) radix b q ∧
        _root_.F2R (beta:=beta) p < _root_.F2R (beta:=beta) q ∧
        p.Fexp = -b.dExp ∧ q.Fexp = -b.dExp ∧ (beta : ℤ) > 0⌝⦄
    (pure (FsubnormalLt_check (beta:=beta) b radix p q) : Id Unit)
    ⦃⇓_ => ⌜p.Fnum < q.Fnum⌝⦄ := by
  intro ⟨_, _, hlt, hpexp, hqexp, hbeta⟩
  simp only [wp, PostCond.noThrow, pure, FsubnormalLt_check]
  show p.Fnum < q.Fnum
  -- F2R f = f.Fnum * (beta : ℝ) ^ f.Fexp
  -- Since p.Fexp = q.Fexp = -b.dExp, we have:
  -- p.Fnum * β^e < q.Fnum * β^e where e = -b.dExp
  -- Since β > 0, β^e > 0, so we can divide both sides
  unfold _root_.F2R FloatSpec.Core.Defs.F2R at hlt
  rw [hpexp, hqexp] at hlt
  have hbeta_pos : (0 : ℝ) < (beta : ℝ) ^ (-b.dExp) := by
    apply zpow_pos
    exact Int.cast_pos.mpr hbeta
  have h : (p.Fnum : ℝ) < (q.Fnum : ℝ) := by
    nlinarith
  exact_mod_cast h

/-- Coq `FsubnormalLt`. -/
theorem FsubnormalLt {beta : Int} [ValidRadix beta]
    (b : Fbound_skel)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fsubnormal (beta:=beta) beta b p ∧
        Fsubnormal (beta:=beta) beta b q ∧
        _root_.F2R p < _root_.F2R q⌝⦄
    (pure (FsubnormalLt_check (beta:=beta) b beta p q) : Id Unit)
    ⦃⇓_ => ⌜p.Fnum < q.Fnum⌝⦄ := by
  intro ⟨hp, hq, hpq⟩
  exact FsubnormalLt_from_expanded_subnormal_payload
    (beta:=beta) b beta p q
      ⟨hp, hq, hpq, hp.2.1, hq.2.1,
        lt_trans Int.zero_lt_one (ValidRadix.valid (beta:=beta))⟩

-- ---------------------------------------------------------------------------
-- RleRoundedAbs (Coq: Pff.v) — lower bound on |r| from rounding to nearest

noncomputable def RleRoundedAbs_check
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (f : FloatSpec.Core.Defs.FlocqFloat radix) (r : ℝ) : Unit :=
  ()

/-- Coq: `RleRoundedAbs` — if `Closest bo radix r f`, `Fnormal radix bo f` and
    `-(dExp bo) < Fexp f`, then
    `((radix ^ (precision - 1) + - (1 / (2 * radix))) * radix ^ (Fexp f) ≤ |r|)`.

    This is the upstream section payload from Flocq `Pff.v`: integer radix,
    natural precision, `bo.vNum = radix^precision`, `4 ≤ precision`, closest
    rounding, normality, and exponent above the minimum-normal boundary. -/
theorem RleRoundedAbs
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (f : FloatSpec.Core.Defs.FlocqFloat radix) (r : ℝ) :
    ⦃⌜1 < radix ∧ bo.vNum = Zpower_nat radix precision ∧ 4 ≤ precision ∧
        Closest (beta:=radix) bo (radix : ℝ) r f ∧
        Fnormal (beta:=radix) radix bo f ∧ -bo.dExp < f.Fexp⌝⦄
    (pure (RleRoundedAbs_check bo radix precision f r) : Id Unit)
    ⦃⇓_ => ⌜(((radix : ℝ) ^ (precision - 1) + - (1 / (2 * (radix : ℝ)))) *
        ((radix : ℝ) ^ (f.Fexp)) ≤ |r|)⌝⦄ := by
  intro ⟨hRadixGt1, hvNumEq, hPrecisionGe4, hClosest, hNormal, hExpBound⟩
  simp only [wp, PostCond.noThrow, pure, RleRoundedAbs_check]
  show (((radix : ℝ) ^ (precision - 1) + -(1 / (2 * (radix : ℝ)))) *
      (radix : ℝ) ^ f.Fexp) ≤ |r|
  -- Key facts
  have hRadixPosInt : (0 : Int) < radix := by omega
  have hRadixPos : (0 : ℝ) < (radix : ℝ) := by exact_mod_cast hRadixPosInt
  have hRadixR_ne : (radix : ℝ) ≠ 0 := ne_of_gt hRadixPos
  have hRadixGe1 : (1 : ℝ) < (radix : ℝ) := by exact_mod_cast hRadixGt1
  have hBetaExpPos : (0 : ℝ) < (radix : ℝ) ^ f.Fexp := zpow_pos hRadixPos f.Fexp
  have hExpPos : (0 : ℝ) < (radix : ℝ) ^ f.Fexp := hBetaExpPos
  have hExpNonneg : (0 : ℝ) ≤ (radix : ℝ) ^ f.Fexp := le_of_lt hExpPos
  have hPrecisionNe : precision ≠ 0 := by omega
  have hPrecisionPos : 0 < precision := Nat.pos_of_ne_zero hPrecisionNe
  have hFbounded : Fbounded' bo f := hClosest.1
  have hMin : ∀ g : FloatSpec.Core.Defs.FlocqFloat radix, Fbounded' bo g →
      |_root_.F2R f - r| ≤ |_root_.F2R g - r| := hClosest.2
  have hNormMinLeAbsInt : nNormMin radix precision ≤ |f.Fnum| := by
    have hposnorm : bo.vNum = radix * nNormMin radix precision :=
      PosNormMin radix bo precision hPrecisionPos hvNumEq
    have hprod_le : radix * nNormMin radix precision ≤ |radix * f.Fnum| := by
      simpa [hposnorm] using hNormal.2
    have habs_mul : |radix * f.Fnum| = radix * |f.Fnum| := by
      rw [abs_mul, abs_of_nonneg (le_of_lt hRadixPosInt)]
    have hmul : radix * nNormMin radix precision ≤ radix * |f.Fnum| := by
      simpa [habs_mul] using hprod_le
    exact Int.le_of_mul_le_mul_left hmul hRadixPosInt
  have hFnumBound : (radix : ℝ) ^ (precision - 1) ≤ |(f.Fnum : ℝ)| := by
    have hcast : ((nNormMin radix precision : Int) : ℝ) ≤ ((|f.Fnum| : Int) : ℝ) := by
      exact_mod_cast hNormMinLeAbsInt
    simpa [nNormMin] using hcast
  -- |F2R f| = |f.Fnum| * beta^e
  have hF2R_abs : |_root_.F2R f| =
      |(f.Fnum : ℝ)| * (radix : ℝ) ^ f.Fexp := by
    unfold _root_.F2R FloatSpec.Core.Defs.F2R
    rw [abs_mul, abs_of_pos hBetaExpPos]
  -- |F2R f| ≥ radix^(p-1) * radix^e
  have hF2R_lower :
      (radix : ℝ) ^ (precision - 1) * (radix : ℝ) ^ f.Fexp ≤ |_root_.F2R f| := by
    rw [hF2R_abs]
    exact mul_le_mul_of_nonneg_right hFnumBound (le_of_lt hBetaExpPos)
  -- f.Fnum ≠ 0
  have hFnumNe : f.Fnum ≠ 0 := by
    intro h
    simp [h] at hFnumBound
    linarith [pow_pos hRadixPos (precision - 1)]
  have hSignCases : f.Fnum.sign = 1 ∨ f.Fnum.sign = -1 := by
    rcases ne_iff_lt_or_gt.mp hFnumNe with h | h
    · right; exact Int.sign_eq_neg_one_iff_neg.mpr h
    · left; exact Int.sign_eq_one_iff_pos.mpr h
  -- Triangle inequality: |r| ≥ |F2R f| - |F2R f - r|
  have hTriangle : |_root_.F2R f| - |_root_.F2R f - r| ≤ |r| := by
    linarith [abs_sub_abs_le_abs_sub (_root_.F2R f) r]
  -- Case 1: |r| ≥ |F2R f| — trivially sufficient
  by_cases hRgeF : |r| ≥ |_root_.F2R f|
  · calc (((radix : ℝ) ^ (precision - 1) + -(1 / (2 * (radix : ℝ)))) *
          (radix : ℝ) ^ f.Fexp)
        ≤ (radix : ℝ) ^ (precision - 1) * (radix : ℝ) ^ f.Fexp := by
          apply mul_le_mul_of_nonneg_right _ hExpNonneg
          linarith [div_pos (one_pos) (mul_pos (by linarith : (0:ℝ) < 2) hRadixPos)]
      _ ≤ |_root_.F2R f| := hF2R_lower
      _ ≤ |r| := hRgeF
  · -- Case 2: |r| < |F2R f|
    push Not at hRgeF
    set u := _root_.F2R f - r with hu_def
    -- F2R f has the same sign as f.Fnum
    have hF2R_sign : (0 < f.Fnum → 0 < _root_.F2R f) ∧ (f.Fnum < 0 → _root_.F2R f < 0) := by
      constructor
      · intro h; unfold _root_.F2R FloatSpec.Core.Defs.F2R
        exact mul_pos (by exact_mod_cast h) hBetaExpPos
      · intro h; unfold _root_.F2R FloatSpec.Core.Defs.F2R
        exact mul_neg_of_neg_of_pos (by exact_mod_cast h) hBetaExpPos
    -- u has the same sign as F2R f (and sign(f.Fnum))
    have hu_sign : (0 < f.Fnum → 0 < u) ∧ (f.Fnum < 0 → u < 0) := by
      constructor
      · intro h
        have hFpos := hF2R_sign.1 h
        -- |F2R f| = F2R f since F2R f > 0
        have habs_eq : |_root_.F2R f| = _root_.F2R f := abs_of_pos hFpos
        -- |r| < F2R f
        have hRltF : |r| < _root_.F2R f := habs_eq ▸ hRgeF
        -- From |r| < F2R f, we get r < F2R f, hence u > 0
        linarith [le_abs_self r]
      · intro h
        have hFneg := hF2R_sign.2 h
        -- |F2R f| = -F2R f since F2R f < 0
        have habs_eq : |_root_.F2R f| = -_root_.F2R f := abs_of_neg hFneg
        -- |r| < -F2R f
        have hRltNF : |r| < -_root_.F2R f := habs_eq ▸ hRgeF
        -- r ≥ -|r| > F2R f, so u = F2R f - r < 0
        linarith [neg_abs_le r]

    -- Sub-case split on |f.Fnum|
    by_cases hFnumStrict : |(f.Fnum : ℝ)| > (radix : ℝ) ^ (precision - 1)
    · -- Case A: the mantissa is separated from the minimum by at least one integer ulp.
      have hIntGap :
          |(f.Fnum : ℝ)| ≥ (radix : ℝ) ^ (precision - 1) + 1 := by
        have hFnum_abs_int :
            |(f.Fnum : ℝ)| = ((|f.Fnum| : Int) : ℝ) := by
          simpa using
            (Int.cast_abs f.Fnum :
              ((|f.Fnum| : Int) : ℝ) = |(f.Fnum : ℝ)|).symm
        have hPow_int :
            ((radix ^ (precision - 1) : Int) : ℝ) =
              (radix : ℝ) ^ (precision - 1) := by
          norm_num
        have hFS := hFnumStrict
        rw [hFnum_abs_int, ← hPow_int] at hFS
        have hIntStrict : (|f.Fnum| : Int) > radix ^ (precision - 1) := by
          exact_mod_cast hFS
        have hGap : radix ^ (precision - 1) + 1 ≤ (|f.Fnum| : Int) := by omega
        rw [hFnum_abs_int, ← hPow_int]
        exact_mod_cast hGap
      let g : FloatSpec.Core.Defs.FlocqFloat radix :=
        ⟨f.Fnum - Int.sign f.Fnum, f.Fexp⟩
      have hgBounded : Fbounded' bo g := by
        obtain ⟨hNumBound, hExpBound'⟩ := hFbounded
        refine ⟨?_, hExpBound'⟩
        show |f.Fnum - Int.sign f.Fnum| < bo.vNum
        rcases hSignCases with hs | hs <;> rw [hs]
        · have hfp : 0 < f.Fnum := Int.sign_eq_one_iff_pos.mp hs
          have h1 : (|f.Fnum - 1| : Int) = f.Fnum - 1 := by
            rw [Int.abs_eq_natAbs]
            exact Int.natAbs_of_nonneg (by omega)
          have h2 : (|f.Fnum| : Int) = f.Fnum := by
            rw [Int.abs_eq_natAbs]
            exact Int.natAbs_of_nonneg (by omega)
          rw [h2] at hNumBound
          rw [h1]
          omega
        · have hfn : f.Fnum < 0 := Int.sign_eq_neg_one_iff_neg.mp hs
          have h1 : (|f.Fnum - (-1)| : Int) = -(f.Fnum + 1) := by
            rw [show f.Fnum - (-1 : Int) = f.Fnum + 1 from by ring]
            rw [Int.abs_eq_natAbs]
            exact Int.ofNat_natAbs_of_nonpos (by omega)
          have h2 : (|f.Fnum| : Int) = -f.Fnum := by
            rw [Int.abs_eq_natAbs]
            exact Int.ofNat_natAbs_of_nonpos (by omega)
          rw [h2] at hNumBound
          rw [h1]
          omega
      have hgF2R_diff :
          _root_.F2R f - _root_.F2R g =
            (Int.sign f.Fnum : ℝ) * (radix : ℝ) ^ f.Fexp := by
        unfold _root_.F2R FloatSpec.Core.Defs.F2R
        simp only [g]
        push_cast
        ring
      have hMinG := hMin g hgBounded
      have hMinG' :
          |u| ≤ |u - (Int.sign f.Fnum : ℝ) * (radix : ℝ) ^ f.Fexp| := by
        have :
            _root_.F2R g - r =
              u - ((Int.sign f.Fnum : ℝ) * (radix : ℝ) ^ f.Fexp) := by
          linarith [hgF2R_diff]
        rwa [this] at hMinG
      have hErrBound : |u| ≤ (radix : ℝ) ^ f.Fexp / 2 := by
        set delta := (Int.sign f.Fnum : ℝ) * (radix : ℝ) ^ f.Fexp with hdelta_def
        rcases hSignCases with hs | hs
        · have hfp : 0 < f.Fnum := Int.sign_eq_one_iff_pos.mp hs
          have hu_pos : 0 ≤ u := le_of_lt (hu_sign.1 hfp)
          rw [abs_of_nonneg hu_pos] at hMinG' ⊢
          have hdelta_eq : delta = (radix : ℝ) ^ f.Fexp := by simp [hdelta_def, hs]
          by_cases hud : u ≤ delta
          · rw [abs_of_nonpos (by linarith)] at hMinG'
            linarith [hdelta_eq]
          · push Not at hud
            rw [abs_of_nonneg (by linarith)] at hMinG'
            linarith
        · have hfn : f.Fnum < 0 := Int.sign_eq_neg_one_iff_neg.mp hs
          have hu_neg : u ≤ 0 := le_of_lt (hu_sign.2 hfn)
          rw [abs_of_nonpos hu_neg] at hMinG' ⊢
          have hdelta_eq : delta = -(radix : ℝ) ^ f.Fexp := by simp [hdelta_def, hs]
          by_cases hud : delta ≤ u
          · rw [abs_of_nonneg (by linarith)] at hMinG'
            linarith [hdelta_eq]
          · push Not at hud
            rw [abs_of_nonpos (by linarith)] at hMinG'
            linarith
      have h1over2radix : 1 / (2 * (radix : ℝ)) ≤ 1 / 2 := by
        apply div_le_div_of_nonneg_left
        · positivity
        · positivity
        · nlinarith
      calc (((radix : ℝ) ^ (precision - 1) + -(1 / (2 * (radix : ℝ)))) *
          (radix : ℝ) ^ f.Fexp)
          = ((radix : ℝ) ^ (precision - 1) - 1 / (2 * (radix : ℝ))) *
              (radix : ℝ) ^ f.Fexp := by ring
        _ ≤ (|(f.Fnum : ℝ)| - 1 / 2) * (radix : ℝ) ^ f.Fexp := by
          apply mul_le_mul_of_nonneg_right _ (le_of_lt hBetaExpPos)
          have h12r_pos : (0 : ℝ) < 1 / (2 * (radix : ℝ)) := by positivity
          linarith [hIntGap, h1over2radix, h12r_pos]
        _ = |(f.Fnum : ℝ)| * (radix : ℝ) ^ f.Fexp -
              (radix : ℝ) ^ f.Fexp / 2 := by ring
        _ = |_root_.F2R f| - (radix : ℝ) ^ f.Fexp / 2 := by rw [hF2R_abs]
        _ ≤ |r| := by linarith [hTriangle, hErrBound]
    · -- Case B: exact minimum-normal mantissa; step to the predecessor exponent.
      push Not at hFnumStrict
      have hFnumExact :
          |(f.Fnum : ℝ)| = (radix : ℝ) ^ (precision - 1) :=
        le_antisymm hFnumStrict hFnumBound
      let g₂ : FloatSpec.Core.Defs.FlocqFloat radix :=
        ⟨f.Fnum * radix - Int.sign f.Fnum, f.Fexp - 1⟩
      have hg₂Bounded : Fbounded' bo g₂ := by
        obtain ⟨_hNumBd, _hExpBd⟩ := hFbounded
        refine ⟨?_, by show -bo.dExp ≤ f.Fexp - 1; omega⟩
        show |f.Fnum * radix - Int.sign f.Fnum| < bo.vNum
        have hFnumExactInt : |f.Fnum| = nNormMin radix precision := by
          have hFnum_abs_int :
              |(f.Fnum : ℝ)| = ((|f.Fnum| : Int) : ℝ) := by
            simpa using
              (Int.cast_abs f.Fnum :
                ((|f.Fnum| : Int) : ℝ) = |(f.Fnum : ℝ)|).symm
          have hPow_int :
              ((nNormMin radix precision : Int) : ℝ) =
                (radix : ℝ) ^ (precision - 1) := by
            simp [nNormMin]
          have hcast : ((|f.Fnum| : Int) : ℝ) =
              ((nNormMin radix precision : Int) : ℝ) := by
            rw [← hFnum_abs_int, hPow_int]
            exact hFnumExact
          exact_mod_cast hcast
        have hVB2 : (|f.Fnum| : Int) * radix ≤ bo.vNum := by
          have hposnorm := PosNormMin radix bo precision hPrecisionPos hvNumEq
          have hEq : (|f.Fnum| : Int) * radix = bo.vNum := by
            rw [hFnumExactInt, hposnorm]
            ring
          exact le_of_eq hEq
        rcases hSignCases with hs | hs <;> rw [hs]
        · have hfp : 0 < f.Fnum := Int.sign_eq_one_iff_pos.mp hs
          have habs_eq : (|f.Fnum| : Int) = f.Fnum := by
            rw [Int.abs_eq_natAbs]
            exact Int.natAbs_of_nonneg (by omega)
          have h1 : (|f.Fnum * radix - 1| : Int) = f.Fnum * radix - 1 := by
            rw [Int.abs_eq_natAbs]
            exact Int.natAbs_of_nonneg (by nlinarith)
          rw [habs_eq] at hVB2
          rw [h1]
          omega
        · have hfn : f.Fnum < 0 := Int.sign_eq_neg_one_iff_neg.mp hs
          have habs_eq : (|f.Fnum| : Int) = -f.Fnum := by
            rw [Int.abs_eq_natAbs]
            exact Int.ofNat_natAbs_of_nonpos (by omega)
          have h1 : (|f.Fnum * radix - (-1)| : Int) = -f.Fnum * radix - 1 := by
            rw [show f.Fnum * radix - (-1 : Int) = -((-f.Fnum) * radix - 1) from by ring]
            rw [Int.abs_eq_natAbs]
            rw [Int.natAbs_neg]
            exact Int.natAbs_of_nonneg (by nlinarith)
          rw [habs_eq] at hVB2
          rw [h1]
          omega
      have hbe :
          (radix : ℝ) ^ f.Fexp =
            (radix : ℝ) * (radix : ℝ) ^ (f.Fexp - 1) := by
        have := zpow_add₀ hRadixR_ne (1 : Int) (f.Fexp - 1)
        rw [show (1 : Int) + (f.Fexp - 1) = f.Fexp from by ring] at this
        rw [this, zpow_one]
      have hg₂F2R_diff :
          _root_.F2R f - _root_.F2R g₂ =
            (Int.sign f.Fnum : ℝ) * (radix : ℝ) ^ (f.Fexp - 1) := by
        unfold _root_.F2R FloatSpec.Core.Defs.F2R
        simp only [g₂]
        push_cast
        rw [hbe]
        ring
      have hMinG₂ := hMin g₂ hg₂Bounded
      have hMinG₂' :
          |u| ≤ |u - (Int.sign f.Fnum : ℝ) * (radix : ℝ) ^ (f.Fexp - 1)| := by
        have :
            _root_.F2R g₂ - r =
              u - ((Int.sign f.Fnum : ℝ) * (radix : ℝ) ^ (f.Fexp - 1)) := by
          linarith [hg₂F2R_diff]
        rwa [this] at hMinG₂
      have hBetaExpM1Pos : (0 : ℝ) < (radix : ℝ) ^ (f.Fexp - 1) :=
        zpow_pos hRadixPos _
      have hErrBound₂ : |u| ≤ (radix : ℝ) ^ (f.Fexp - 1) / 2 := by
        set delta₂ := (Int.sign f.Fnum : ℝ) * (radix : ℝ) ^ (f.Fexp - 1) with hdelta₂_def
        rcases hSignCases with hs | hs
        · have hfp : 0 < f.Fnum := Int.sign_eq_one_iff_pos.mp hs
          have hu_pos : 0 ≤ u := le_of_lt (hu_sign.1 hfp)
          rw [abs_of_nonneg hu_pos] at hMinG₂' ⊢
          have hdelta_eq : delta₂ = (radix : ℝ) ^ (f.Fexp - 1) := by
            simp [hdelta₂_def, hs]
          by_cases hud : u ≤ delta₂
          · rw [abs_of_nonpos (by linarith)] at hMinG₂'
            linarith [hdelta_eq]
          · push Not at hud
            rw [abs_of_nonneg (by linarith)] at hMinG₂'
            linarith
        · have hfn : f.Fnum < 0 := Int.sign_eq_neg_one_iff_neg.mp hs
          have hu_neg : u ≤ 0 := le_of_lt (hu_sign.2 hfn)
          rw [abs_of_nonpos hu_neg] at hMinG₂' ⊢
          have hdelta_eq : delta₂ = -(radix : ℝ) ^ (f.Fexp - 1) := by
            simp [hdelta₂_def, hs]
          by_cases hud : delta₂ ≤ u
          · rw [abs_of_nonneg (by linarith)] at hMinG₂'
            linarith [hdelta_eq]
          · push Not at hud
            rw [abs_of_nonpos (by linarith)] at hMinG₂'
            linarith
      calc (((radix : ℝ) ^ (precision - 1) + -(1 / (2 * (radix : ℝ)))) *
          (radix : ℝ) ^ f.Fexp)
          = (radix : ℝ) ^ (precision - 1) * (radix : ℝ) ^ f.Fexp -
              1 / (2 * (radix : ℝ)) * (radix : ℝ) ^ f.Fexp := by ring
        _ = |(f.Fnum : ℝ)| * (radix : ℝ) ^ f.Fexp -
              (radix : ℝ) ^ (f.Fexp - 1) / 2 := by
          rw [hFnumExact, hbe]
          field_simp [hRadixR_ne]
        _ = |_root_.F2R f| - (radix : ℝ) ^ (f.Fexp - 1) / 2 := by rw [hF2R_abs]
        _ ≤ |r| := by linarith [hTriangle, hErrBound₂]

-- Coq: `RoundedModeMultAbs` — absolute-value scaling under RoundedModePGeneric

noncomputable def RoundedModeMultAbs_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (r : ℝ) (q q' : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `RoundedModeMultAbs`.

This restores the wrapper payload: the absolute-value result follows by cases
on the sign of `r` from the two directional scaling lemmas
`RoundedModeMult_from_scale_payload` and `RoundedModeMultLess_from_scale_payload`.  Their payloads are passed as
explicit dependencies here rather than hidden behind a trivial postcondition. -/
theorem RoundedModeMultAbs_from_properties {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (r : ℝ) (q q' : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜RoundedModePGeneric P ∧ P r q ∧ Fbounded (beta:=beta) b q' ∧
        |r| ≤ radix * _root_.F2R (beta:=beta) q' ∧
        (∀ (s : ℝ) (p t : FloatSpec.Core.Defs.FlocqFloat beta),
          P s p → Fbounded (beta:=beta) b t →
          s ≤ radix * _root_.F2R (beta:=beta) t →
          _root_.F2R (beta:=beta) p ≤
            radix * _root_.F2R (beta:=beta) t) ∧
        (∀ (s : ℝ) (p t : FloatSpec.Core.Defs.FlocqFloat beta),
          P s p → Fbounded (beta:=beta) b t →
          radix * _root_.F2R (beta:=beta) t ≤ s →
          radix * _root_.F2R (beta:=beta) t ≤
            _root_.F2R (beta:=beta) p) ∧
        (0 ≤ r → 0 ≤ _root_.F2R (beta:=beta) q) ∧
        (r ≤ 0 → _root_.F2R (beta:=beta) q ≤ 0)⌝⦄
    (pure (RoundedModeMultAbs_check (beta:=beta) b radix P r q q') : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) q| ≤
        radix * _root_.F2R (beta:=beta) q'⌝⦄ := by
  intro h
  rcases h with
    ⟨_, hP, hq'Bounded, hAbsR, hMult, hMultLess, hRoundNonneg, hRoundNonpos⟩
  simp only [wp, PostCond.noThrow, pure, RoundedModeMultAbs_check,
    Id.run, ULift.up_down]
  by_cases hr_nonneg : 0 ≤ r
  · have hq_nonneg : 0 ≤ _root_.F2R (beta:=beta) q := hRoundNonneg hr_nonneg
    rw [abs_of_nonneg hq_nonneg]
    have hr_le : r ≤ radix * _root_.F2R (beta:=beta) q' := by
      exact le_trans (le_abs_self r) hAbsR
    exact hMult r q q' hP hq'Bounded hr_le
  · have hr_neg : r < 0 := lt_of_not_ge hr_nonneg
    have hq_nonpos : _root_.F2R (beta:=beta) q ≤ 0 :=
      hRoundNonpos (le_of_lt hr_neg)
    rw [abs_of_nonpos hq_nonpos]
    have hFoppBounded :
        Fbounded (beta:=beta) b (Fopp (beta:=beta) q') := by
      rcases hq'Bounded with ⟨hnum, hexp⟩
      exact ⟨by simpa [Fopp, FloatSpec.Calc.Operations.Fopp, abs_neg] using hnum,
        by simpa [Fopp, FloatSpec.Calc.Operations.Fopp] using hexp⟩
    have hFoppQ' :
        _root_.F2R (beta:=beta) (Fopp (beta:=beta) q') =
          - _root_.F2R (beta:=beta) q' := by
      have h := FloatSpec.Calc.Operations.F2R_opp (beta:=beta) q'
      simpa [Fopp, _root_.F2R] using h trivial
    have hneg_bound :
        radix * _root_.F2R (beta:=beta) (Fopp (beta:=beta) q') ≤ r := by
      rw [hFoppQ']
      have hneg_abs : |r| = -r := abs_of_neg hr_neg
      rw [hneg_abs] at hAbsR
      nlinarith
    have hless := hMultLess r q (Fopp (beta:=beta) q') hP hFoppBounded hneg_bound
    rw [hFoppQ'] at hless
    have hless' :
        -(radix * _root_.F2R (beta:=beta) q') ≤
          _root_.F2R (beta:=beta) q := by
      nlinarith
    have hneg := neg_le_neg hless'
    simpa using hneg

-- Legacy generic scaffold retained under a non-source name.
noncomputable def MinCompatibleGeneric_check {α : Type}
    (b : Fbound_skel) (radix : Int) : Unit :=
  ()

theorem MinCompatibleGeneric {α : Type} (b : Fbound_skel) (radix : Int) :
    ⦃⌜True⌝⦄
    (pure (MinCompatibleGeneric_check (α:=α) b radix) : Id Unit)
    ⦃⇓_ => ⌜CompatiblePGeneric (isMinGeneric (α:=α) b radix)⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, MinCompatibleGeneric_check,
    Id.run, ULift.up_down]
  show CompatiblePGeneric (isMinGeneric (α:=α) b radix)
  intro _ _ p q hp _ hpq
  subst q
  exact hp

/-- Coq `MinCompatible`: the concrete bounded greatest-lower float predicate
is compatible with equal real inputs and equal represented outputs. -/
theorem MinCompatible {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) :
    CompatibleP (beta:=beta) b (isMin (beta:=beta) b radix) := by
  intro r1 r2 p q hp hr hpq hqBounded
  rcases hp with ⟨_hpBounded, hpLe, hpGreatest⟩
  refine ⟨hqBounded, ?_, ?_⟩
  · rw [← hr, ← hpq]
    exact hpLe
  · intro f hfBounded hfLe
    rw [← hpq]
    exact hpGreatest f hfBounded (by simpa [hr] using hfLe)

-- Coq: `MinRoundedModeP_from_total_payload` — RoundedModePGeneric (isMin b radix)
noncomputable def MinRoundedModeP_check {α : Type}
    (b : Fbound_skel) (radix : Int) : Unit :=
  ()

/-- Coq: `MinRoundedModeP_from_total_payload`.

The faithful Lean statement targets the float-specific `isMin` predicate.
Totality is explicit because `MinEx_from_finite_box_payload` is still an active construction. -/
theorem MinRoundedModeP_from_total_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) :
    ⦃⌜TotalP (isMin (beta:=beta) b radix)⌝⦄
    (pure (MinRoundedModeP_check
      (α:=FloatSpec.Core.Defs.FlocqFloat beta) b radix) : Id Unit)
    ⦃⇓_ => ⌜RoundedModeP_full (beta:=beta) b
      (isMin (beta:=beta) b radix)⌝⦄ := by
  intro hTotal
  simp only [wp, PostCond.noThrow, pure, MinRoundedModeP_check,
    Id.run, ULift.up_down]
  refine ⟨hTotal, ?_, ?_, ?_, ?_⟩
  · intro r1 r2 p q hp hr hpq
    subst r2
    subst q
    exact hp
  · intro x y p q hxy hp hq
    rcases hp with ⟨hpBounded, hpLe, _hpGreatest⟩
    rcases hq with ⟨_hqBounded, _hqLe, hqGreatest⟩
    exact hqGreatest p hpBounded (le_trans hpLe (le_of_lt hxy))
  · intro p hpBounded
    exact ⟨hpBounded, le_rfl, fun f _hfBounded hfLe => hfLe⟩
  · intro p q hpBounded hqMin
    rcases hqMin with ⟨_hqBounded, hqLe, hqGreatest⟩
    have hpLeq : _root_.F2R (beta:=beta) p ≤ _root_.F2R (beta:=beta) q :=
      hqGreatest p hpBounded le_rfl
    exact le_antisymm hpLeq hqLe

-- Legacy generic scaffold retained under a non-source name.
noncomputable def MaxCompatibleGeneric_check {α : Type}
    (b : Fbound_skel) (radix : Int) : Unit :=
  ()

theorem MaxCompatibleGeneric {α : Type} (b : Fbound_skel) (radix : Int) :
    ⦃⌜True⌝⦄
    (pure (MaxCompatibleGeneric_check (α:=α) b radix) : Id Unit)
    ⦃⇓_ => ⌜CompatiblePGeneric (isMaxGeneric (α:=α) b radix)⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, MaxCompatibleGeneric_check,
    Id.run, ULift.up_down]
  show CompatiblePGeneric (isMaxGeneric (α:=α) b radix)
  intro _ _ p q hp _ hpq
  subst q
  exact hp

/-- Coq `MaxCompatible`: the concrete bounded least-upper float predicate is
compatible with equal real inputs and equal represented outputs. -/
theorem MaxCompatible {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) :
    CompatibleP (beta:=beta) b (isMax (beta:=beta) b radix) := by
  intro r1 r2 p q hp hr hpq hqBounded
  rcases hp with ⟨_hpBounded, hpLe, hpLeast⟩
  refine ⟨hqBounded, ?_, ?_⟩
  · rw [← hr, ← hpq]
    exact hpLe
  · intro f hfBounded hfLe
    rw [← hpq]
    exact hpLeast f hfBounded (by simpa [hr] using hfLe)

-- Coq: `MaxRoundedModeP_from_total_payload` — RoundedModePGeneric (isMax b radix)
noncomputable def MaxRoundedModeP_check {α : Type}
    (b : Fbound_skel) (radix : Int) : Unit :=
  ()

/-- Coq: `MaxRoundedModeP_from_total_payload`.

The faithful Lean statement targets the float-specific `isMax` predicate.
Totality is explicit because `MaxEx_from_finite_box_payload` is still an active construction. -/
theorem MaxRoundedModeP_from_total_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) :
    ⦃⌜TotalP (isMax (beta:=beta) b radix)⌝⦄
    (pure (MaxRoundedModeP_check
      (α:=FloatSpec.Core.Defs.FlocqFloat beta) b radix) : Id Unit)
    ⦃⇓_ => ⌜RoundedModeP_full (beta:=beta) b
      (isMax (beta:=beta) b radix)⌝⦄ := by
  intro hTotal
  simp only [wp, PostCond.noThrow, pure, MaxRoundedModeP_check,
    Id.run, ULift.up_down]
  refine ⟨hTotal, ?_, ?_, ?_, ?_⟩
  · intro r1 r2 p q hp hr hpq
    subst r2
    subst q
    exact hp
  · intro x y p q hxy hp hq
    rcases hp with ⟨_hpBounded, hpGe, hpLeast⟩
    rcases hq with ⟨hqBounded, hqGe, _hqLeast⟩
    exact hpLeast q hqBounded (le_trans (le_of_lt hxy) hqGe)
  · intro p hpBounded
    exact ⟨hpBounded, le_rfl, fun f _hfBounded hfGe => hfGe⟩
  · intro p q hpBounded hqMax
    rcases hqMax with ⟨_hqBounded, hpLeq, hqLeast⟩
    have hqLep : _root_.F2R (beta:=beta) q ≤ _root_.F2R (beta:=beta) p :=
      hqLeast p hpBounded le_rfl
    exact le_antisymm hpLeq hqLep

-- Coq: `isMin_inv1` — a minimum witness is below the rounded real.
noncomputable def isMin_inv1_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) : Unit :=
  ()

theorem isMin_inv1 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) :
    ⦃⌜isMin (beta:=beta) b radix r p⌝⦄
    (pure (isMin_inv1_check (beta:=beta) b radix p r) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) p ≤ r⌝⦄ := by
  intro h
  simp only [wp, PostCond.noThrow, pure, isMin_inv1_check,
    Id.run, ULift.up_down]
  exact h.2.1

-- Coq: `isMax_inv1` — the rounded real is below a maximum witness.
noncomputable def isMax_inv1_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) : Unit :=
  ()

theorem isMax_inv1 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) :
    ⦃⌜isMax (beta:=beta) b radix r p⌝⦄
    (pure (isMax_inv1_check (beta:=beta) b radix p r) : Id Unit)
    ⦃⇓_ => ⌜r ≤ _root_.F2R (beta:=beta) p⌝⦄ := by
  intro h
  simp only [wp, PostCond.noThrow, pure, isMax_inv1_check,
    Id.run, ULift.up_down]
  exact h.2.1

/-- Coq: `ProjectMin` — minimum witnesses are projectors at the real-value
level.  Coq's statement uses float coercions, so the Lean result states
equality of `F2R` values. -/
theorem ProjectMin {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) :
    ∀ (r : ℝ) (p q : FloatSpec.Core.Defs.FlocqFloat beta),
      isMin (beta:=beta) b radix r p →
      isMin (beta:=beta) b radix r q →
      _root_.F2R (beta:=beta) p = _root_.F2R (beta:=beta) q := by
  intro r p q hp hq
  apply le_antisymm
  · exact hq.2.2 p hp.1 hp.2.1
  · exact hp.2.2 q hq.1 hq.2.1

/-- Coq: `MonotoneMin` — lower projectors are monotone. -/
theorem MonotoneMin {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) :
    ∀ (p q : ℝ) (p' q' : FloatSpec.Core.Defs.FlocqFloat beta),
      p < q →
      isMin (beta:=beta) b radix p p' →
      isMin (beta:=beta) b radix q q' →
      _root_.F2R (beta:=beta) p' ≤ _root_.F2R (beta:=beta) q' := by
  intro p q p' q' hpq hp' hq'
  exact hq'.2.2 p' hp'.1 (le_trans hp'.2.1 (le_of_lt hpq))

/-- Coq: `ProjectMax` — maximum witnesses are projectors at the real-value
level.  Coq's statement uses float coercions, so the Lean result states
equality of `F2R` values. -/
theorem ProjectMax {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) :
    ∀ (r : ℝ) (p q : FloatSpec.Core.Defs.FlocqFloat beta),
      isMax (beta:=beta) b radix r p →
      isMax (beta:=beta) b radix r q →
      _root_.F2R (beta:=beta) p = _root_.F2R (beta:=beta) q := by
  intro r p q hp hq
  apply le_antisymm
  · exact hp.2.2 q hq.1 hq.2.1
  · exact hq.2.2 p hp.1 hp.2.1

/-- Coq: `MonotoneMax` — upper projectors are monotone. -/
theorem MonotoneMax {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) :
    ∀ (p q : ℝ) (p' q' : FloatSpec.Core.Defs.FlocqFloat beta),
      p < q →
      isMax (beta:=beta) b radix p p' →
      isMax (beta:=beta) b radix q q' →
      _root_.F2R (beta:=beta) p' ≤ _root_.F2R (beta:=beta) q' := by
  intro p q p' q' hpq hp' hq'
  exact hp'.2.2 q' hq'.1 (le_trans (le_of_lt hpq) hq'.2.1)

-- Coq: `MinEq` — minimum witnesses are unique at the real-value level.
noncomputable def MinEq_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) : Unit :=
  ()

theorem MinEq {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) :
    ⦃⌜isMin (beta:=beta) b radix r p ∧
        isMin (beta:=beta) b radix r q⌝⦄
    (pure (MinEq_check (beta:=beta) b radix p q r) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) p =
              _root_.F2R (beta:=beta) q⌝⦄ := by
  intro h
  rcases h with ⟨hp, hq⟩
  simp only [wp, PostCond.noThrow, pure, MinEq_check,
    Id.run, ULift.up_down]
  apply le_antisymm
  · exact hq.2.2 p hp.1 hp.2.1
  · exact hp.2.2 q hq.1 hq.2.1

-- Coq: `MaxEq` — maximum witnesses are unique at the real-value level.
noncomputable def MaxEq_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) : Unit :=
  ()

theorem MaxEq {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) :
    ⦃⌜isMax (beta:=beta) b radix r p ∧
        isMax (beta:=beta) b radix r q⌝⦄
    (pure (MaxEq_check (beta:=beta) b radix p q r) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) p =
              _root_.F2R (beta:=beta) q⌝⦄ := by
  intro h
  rcases h with ⟨hp, hq⟩
  simp only [wp, PostCond.noThrow, pure, MaxEq_check,
    Id.run, ULift.up_down]
  apply le_antisymm
  · exact hp.2.2 q hq.1 hq.2.1
  · exact hq.2.2 p hp.1 hp.2.1

-- Coq: `MinOppMax` — negating a minimum witness gives a maximum witness.
noncomputable def MinOppMax_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) : Unit :=
  ()

theorem MinOppMax {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) :
    ⦃⌜isMin (beta:=beta) b radix r p⌝⦄
    (pure (MinOppMax_check (beta:=beta) b radix p r) : Id Unit)
    ⦃⇓_ => ⌜isMax (beta:=beta) b radix (-r) (Fopp (beta:=beta) p)⌝⦄ := by
  intro hMin
  simp only [wp, PostCond.noThrow, pure, MinOppMax_check,
    Id.run, ULift.up_down]
  rcases hMin with ⟨hpBounded, hp_le_r, hGreatestLower⟩
  have hp_opp :
      _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) =
        -_root_.F2R (beta:=beta) p := by
    simp [Fopp, FloatSpec.Calc.Operations.Fopp, _root_.F2R,
      FloatSpec.Core.Defs.F2R]
  constructor
  · simpa [Fbounded, Fbounded', Fopp, FloatSpec.Calc.Operations.Fopp, abs_neg] using hpBounded
  constructor
  · rw [hp_opp]
    linarith
  · intro f hfBounded hr_le_f
    have hf_opp :
        _root_.F2R (beta:=beta) (Fopp (beta:=beta) f) =
          -_root_.F2R (beta:=beta) f := by
      simp [Fopp, FloatSpec.Calc.Operations.Fopp, _root_.F2R,
        FloatSpec.Core.Defs.F2R]
    have hFoppFBounded :
        Fbounded (beta:=beta) b (Fopp (beta:=beta) f) := by
      simpa [Fbounded, Fbounded', Fopp, FloatSpec.Calc.Operations.Fopp, abs_neg] using hfBounded
    have hfopp_le_r : _root_.F2R (beta:=beta) (Fopp (beta:=beta) f) ≤ r := by
      rw [hf_opp]
      linarith
    have hfopp_le_p := hGreatestLower (Fopp (beta:=beta) f) hFoppFBounded hfopp_le_r
    rw [hf_opp] at hfopp_le_p
    rw [hp_opp]
    linarith

-- Coq: `MaxOppMin` — negating a maximum witness gives a minimum witness.
noncomputable def MaxOppMin_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) : Unit :=
  ()

theorem MaxOppMin {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) :
    ⦃⌜isMax (beta:=beta) b radix r p⌝⦄
    (pure (MaxOppMin_check (beta:=beta) b radix p r) : Id Unit)
    ⦃⇓_ => ⌜isMin (beta:=beta) b radix (-r) (Fopp (beta:=beta) p)⌝⦄ := by
  intro hMax
  simp only [wp, PostCond.noThrow, pure, MaxOppMin_check,
    Id.run, ULift.up_down]
  rcases hMax with ⟨hpBounded, r_le_hp, hLeastUpper⟩
  have hp_opp :
      _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) =
        -_root_.F2R (beta:=beta) p := by
    simp [Fopp, FloatSpec.Calc.Operations.Fopp, _root_.F2R,
      FloatSpec.Core.Defs.F2R]
  constructor
  · simpa [Fbounded, Fbounded', Fopp, FloatSpec.Calc.Operations.Fopp, abs_neg] using hpBounded
  constructor
  · rw [hp_opp]
    linarith
  · intro f hfBounded hf_le_negr
    have hf_opp :
        _root_.F2R (beta:=beta) (Fopp (beta:=beta) f) =
          -_root_.F2R (beta:=beta) f := by
      simp [Fopp, FloatSpec.Calc.Operations.Fopp, _root_.F2R,
        FloatSpec.Core.Defs.F2R]
    have hFoppFBounded :
        Fbounded (beta:=beta) b (Fopp (beta:=beta) f) := by
      simpa [Fbounded, Fbounded', Fopp, FloatSpec.Calc.Operations.Fopp, abs_neg] using hfBounded
    have r_le_fopp : r ≤ _root_.F2R (beta:=beta) (Fopp (beta:=beta) f) := by
      rw [hf_opp]
      linarith
    have hp_le_fopp := hLeastUpper (Fopp (beta:=beta) f) hFoppFBounded r_le_fopp
    rw [hf_opp] at hp_le_fopp
    rw [hp_opp]
    linarith

-- Coq: `RleMinR0` — if 0 ≤ r and `isMin b radix r min` then 0 ≤ F2R min
noncomputable def RleMinR0_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (r : ℝ) (min : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `RleMinR0` — a lower extremal rounding of a nonnegative input is
nonnegative. -/
theorem RleMinR0 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (r : ℝ) (min : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜0 ≤ r ∧ isMin (beta:=beta) b radix r min⌝⦄
    (pure (RleMinR0_check (beta:=beta) b radix r min) : Id Unit)
    ⦃⇓_ => ⌜0 ≤ _root_.F2R min⌝⦄ := by
  intro ⟨hr, hMin⟩
  simp only [wp, PostCond.noThrow, pure, RleMinR0_check,
    Id.run, ULift.up_down]
  show 0 ≤ _root_.F2R min
  -- The zero float ⟨0, -b.dExp⟩ is bounded and has F2R = 0
  let zeroFloat : FloatSpec.Core.Defs.FlocqFloat beta := ⟨0, -b.dExp⟩
  have hZeroBounded : Fbounded' (beta:=beta) b zeroFloat := by
    constructor
    · show |zeroFloat.Fnum| < b.vNum
      simp only [zeroFloat, abs_zero]
      exact b.vNum_pos
    · rfl
  have hZeroF2R : _root_.F2R zeroFloat = 0 := by
    unfold _root_.F2R FloatSpec.Core.Defs.F2R zeroFloat
    ring
  -- Since 0 = F2R(zeroFloat) ≤ r, by maximality of min: F2R(zeroFloat) ≤ F2R(min)
  have h0LeR : _root_.F2R zeroFloat ≤ r := by rw [hZeroF2R]; exact hr
  have h0LeMin : _root_.F2R zeroFloat ≤ _root_.F2R min :=
    hMin.2.2 zeroFloat hZeroBounded h0LeR
  rw [hZeroF2R] at h0LeMin
  exact h0LeMin

-- Coq: `RleRoundedR0` — under RoundedModePGeneric P, if P r p and 0 ≤ r then 0 ≤ F2R p
noncomputable def RleRoundedR0_check {beta : Int} [ValidRadix beta]
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) : Unit :=
  ()

/-- Coq: `RleRoundedR0` — any rounded result of a nonnegative input is
nonnegative. -/
theorem RleRoundedR0 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) :
    ⦃⌜RoundedModeP (beta:=beta) b P ∧ P r p ∧ 0 ≤ r⌝⦄
    (pure (RleRoundedR0_check (beta:=beta) P p r) : Id Unit)
    ⦃⇓_ => ⌜0 ≤ _root_.F2R p⌝⦄ := by
  intro ⟨hRMP, hPrp, hr0⟩
  simp only [wp, PostCond.noThrow, pure, RleRoundedR0_check]
  show 0 ≤ _root_.F2R p
  rcases hRMP.2.2.1 r p hPrp with hMin | hMax
  · have h := RleMinR0 (beta:=beta) b beta r p
    simpa only [wp, PostCond.noThrow, pure, RleMinR0_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using h ⟨hr0, hMin⟩
  · exact le_trans hr0 hMax.2.1

-- Coq: `RleMaxR0` — if r ≤ 0 and `isMax b radix r max` then F2R max ≤ 0
noncomputable def RleMaxR0_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (r : ℝ) (max : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `RleMaxR0` — an upper extremal rounding of a nonpositive input is
nonpositive. -/
theorem RleMaxR0 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (r : ℝ) (max : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜r ≤ 0 ∧ isMax (beta:=beta) b radix r max⌝⦄
    (pure (RleMaxR0_check (beta:=beta) b radix r max) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R max ≤ 0⌝⦄ := by
  intro ⟨hr0, hMax⟩
  simp only [wp, PostCond.noThrow, pure, RleMaxR0_check]
  show _root_.F2R max ≤ 0
  -- Construct the zero float ⟨0, -b.dExp⟩ which is bounded and has F2R = 0
  let zeroFloat : FloatSpec.Core.Defs.FlocqFloat beta := ⟨0, -b.dExp⟩
  have hZeroBounded : Fbounded' b zeroFloat := by
    constructor
    · show |zeroFloat.Fnum| < b.vNum
      simp only [zeroFloat, abs_zero]
      exact b.vNum_pos
    · show -b.dExp ≤ zeroFloat.Fexp
      rfl
  have hZeroF2R : _root_.F2R zeroFloat = 0 := by
    unfold _root_.F2R FloatSpec.Core.Defs.F2R zeroFloat
    ring
  -- Since r ≤ 0 = F2R(zeroFloat), by minimality: F2R max ≤ F2R zeroFloat = 0
  have hrLeZero : r ≤ _root_.F2R zeroFloat := by rw [hZeroF2R]; exact hr0
  have hMaxLeZero : _root_.F2R max ≤ _root_.F2R zeroFloat :=
    hMax.2.2 zeroFloat hZeroBounded hrLeZero
  rw [hZeroF2R] at hMaxLeZero
  exact hMaxLeZero

-- Coq: `RleRoundedLessR0` — under RoundedModePGeneric P, if P r p and r ≤ 0 then F2R p ≤ 0
noncomputable def RleRoundedLessR0_check {beta : Int} [ValidRadix beta]
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) : Unit :=
  ()

/-- Coq: `RleRoundedLessR0` — any rounded result of a nonpositive input is
nonpositive. -/
theorem RleRoundedLessR0 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) :
    ⦃⌜RoundedModeP (beta:=beta) b P ∧ P r p ∧ r ≤ 0⌝⦄
    (pure (RleRoundedLessR0_check (beta:=beta) P p r) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R p ≤ 0⌝⦄ := by
  intro ⟨hRMP, hPrp, hr0⟩
  simp only [wp, PostCond.noThrow, pure, RleRoundedLessR0_check]
  show _root_.F2R p ≤ 0
  rcases hRMP.2.2.1 r p hPrp with hMin | hMax
  · exact le_trans hMin.2.1 hr0
  · have h := RleMaxR0 (beta:=beta) b beta r p
    simpa only [wp, PostCond.noThrow, pure, RleMaxR0_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using h ⟨hr0, hMax⟩

-- Coq: `MinUniqueP` — uniqueness for isMin
-- Note: the generic `isMin` fallback is weak, so we add explicit `isMin` hypotheses
-- matching Coq's real isMin definition, and conclude F2R equality (matching Coq's :>R).
noncomputable def MinUniqueP_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) : Unit :=
  ()

/-- Coq: `MinUniqueP` — if two floats are both `isMin` for the same real,
    their real values are equal. This is the core uniqueness lemma for isMin.
    Since the generic `isMin` fallback is weak, we use `isMin` (the Coq definition)
    in explicit hypotheses, following the ClosestZero pattern. -/
theorem MinUniqueP {beta : Int} [ValidRadix beta] (b : Fbound_skel) (radix : Int) :
    ⦃⌜True⌝⦄
    (pure (MinUniqueP_check (beta:=beta) b radix) : Id Unit)
    ⦃⇓_ => ⌜∀ (r : ℝ) (p q : FloatSpec.Core.Defs.FlocqFloat beta),
        isMin b radix r p → isMin b radix r q →
        _root_.F2R p = _root_.F2R q⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, MinUniqueP_check]
  show ∀ (r : ℝ) (p q : FloatSpec.Core.Defs.FlocqFloat beta),
      isMin b radix r p → isMin b radix r q → _root_.F2R p = _root_.F2R q
  intro r p q ⟨hBp, hLep, hGLBp⟩ ⟨hBq, hLeq, hGLBq⟩
  apply le_antisymm
  · -- F2R p ≤ F2R q: by q's GLB property, since p is bounded and F2R p ≤ r
    exact hGLBq p hBp hLep
  · -- F2R q ≤ F2R p: by p's GLB property, since q is bounded and F2R q ≤ r
    exact hGLBp q hBq hLeq

-- Coq: `MaxUniqueP` — uniqueness for isMax
-- Note: the generic `isMax` fallback is weak, so we add explicit `isMax` hypotheses
-- matching Coq's real isMax definition, and conclude F2R equality (matching Coq's :>R).
noncomputable def MaxUniqueP_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) : Unit :=
  ()

/-- Coq: `MaxUniqueP` — if two floats are both `isMax` for the same real,
    their real values are equal. This is the core uniqueness lemma for isMax.
    Since the generic `isMax` fallback is weak, we use `isMax` (the Coq definition)
    in explicit hypotheses, following the MinUniqueP pattern. -/
theorem MaxUniqueP {beta : Int} [ValidRadix beta] (b : Fbound_skel) (radix : Int) :
    ⦃⌜True⌝⦄
    (pure (MaxUniqueP_check (beta:=beta) b radix) : Id Unit)
    ⦃⇓_ => ⌜∀ (r : ℝ) (p q : FloatSpec.Core.Defs.FlocqFloat beta),
        isMax b radix r p → isMax b radix r q →
        _root_.F2R p = _root_.F2R q⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, MaxUniqueP_check]
  show ∀ (r : ℝ) (p q : FloatSpec.Core.Defs.FlocqFloat beta),
      isMax b radix r p → isMax b radix r q → _root_.F2R p = _root_.F2R q
  intro r p q ⟨hBp, hLep, hLUBp⟩ ⟨hBq, hLeq, hLUBq⟩
  apply le_antisymm
  · -- F2R p ≤ F2R q: by p's LUB property, since q is bounded and r ≤ F2R q
    exact hLUBp q hBq hLeq
  · -- F2R q ≤ F2R p: by q's LUB property, since p is bounded and r ≤ F2R p
    exact hLUBq p hBp hLep

-- (Next missing theorems will be added one-by-one after validation.)

-- Coq: `MinOrMaxRep` — representation form for Min/Max predicates
noncomputable def MinOrMaxRep_check {beta : Int} [ValidRadix beta]
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop) : Unit :=
  ()

/-- Coq: `MinOrMaxRep` — under `MinOrMaxPGeneric P`, if `P (F2R p) q` then there exists
    an integer `m` such that `F2R q = F2R ⟨m, p.Fexp⟩`.

    Change record: Fixed variable correspondence to match Coq (Coq uses one float `p`
    for both the real argument via coercion and the exponent source). Also changed
    `q = ⟨m, p.Fexp⟩` (structural equality) to `_root_.F2R q = _root_.F2R ⟨m, p.Fexp⟩`
    (F2R equality) matching Coq's `:>R` coercion. Added explicit `MinOrMaxRepP` hypothesis
    because the generic fallback does not carry the float-specific representation result. -/
theorem MinOrMaxRepGeneric {beta : Int} [ValidRadix beta]
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop) :
    ⦃⌜MinOrMaxPGeneric P ∧
      (∀ (p q : FloatSpec.Core.Defs.FlocqFloat beta),
        P (_root_.F2R p) q → ∃ m : Int,
          _root_.F2R (beta := beta) q = _root_.F2R (beta := beta) ⟨m, p.Fexp⟩)⌝⦄
    (pure (MinOrMaxRep_check (beta:=beta) P) : Id Unit)
    ⦃⇓_ => ⌜∀ (p q : FloatSpec.Core.Defs.FlocqFloat beta),
            P (_root_.F2R p) q → ∃ m : Int,
              _root_.F2R (beta := beta) q = _root_.F2R (beta := beta) ⟨m, p.Fexp⟩⌝⦄ := by
  intro ⟨_, hRep⟩
  simp only [wp, PostCond.noThrow, pure, MinOrMaxRep_check,
    Id.run, ULift.up_down]
  exact hRep

noncomputable def F2R_rep_at_lower_exp_check {beta : Int} [ValidRadix beta]
    (q : FloatSpec.Core.Defs.FlocqFloat beta) (e : Int) : Unit :=
  ()

/-- If a target exponent is no larger than a float's exponent, the float can be
represented at that target exponent by shifting its mantissa. This is the
Pff-facing form of Core `F2R_change_exp` and is one of the representation
steps used by Flocq's `FminRep`. -/
theorem F2R_rep_at_lower_exp {beta : Int} [ValidRadix beta]
    (q : FloatSpec.Core.Defs.FlocqFloat beta) (e : Int) :
    ⦃⌜1 < beta ∧ e ≤ q.Fexp⌝⦄
    (pure (F2R_rep_at_lower_exp_check (beta:=beta) q e) : Id Unit)
    ⦃⇓_ => ⌜∃ m : Int,
      _root_.F2R (beta:=beta) q =
        _root_.F2R (beta:=beta) ⟨m, e⟩⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hexp⟩
  simp only [wp, PostCond.noThrow, pure, F2R_rep_at_lower_exp_check,
    Id.run, ULift.up_down]
  refine ⟨q.Fnum * beta ^ (q.Fexp - e).natAbs, ?_⟩
  exact FloatSpec.Core.Float_prop.F2R_change_exp (beta:=beta) q e hbeta hexp

noncomputable def FmaxRep_from_FminRep_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) : Unit :=
  ()

/-- Coq's `FmaxRep` is a short negation wrapper around `FminRep`.  This lemma
keeps that dependency explicit: once the min-side representation theorem is
available, the max-side representation follows by applying it to `Fopp p` and
`Fopp q`. -/
theorem FmaxRep_from_FminRep {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) :
    ⦃⌜(∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
          isMin (beta:=beta) b radix (_root_.F2R (beta:=beta) p) q →
            ∃ m : Int,
              _root_.F2R (beta:=beta) q =
                _root_.F2R (beta:=beta) ⟨m, p.Fexp⟩)⌝⦄
    (pure (FmaxRep_from_FminRep_check (beta:=beta) b radix) : Id Unit)
    ⦃⇓_ => ⌜∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
          isMax (beta:=beta) b radix (_root_.F2R (beta:=beta) p) q →
            ∃ m : Int,
              _root_.F2R (beta:=beta) q =
                _root_.F2R (beta:=beta) ⟨m, p.Fexp⟩⌝⦄ := by
  intro hMinRep
  simp only [wp, PostCond.noThrow, pure, FmaxRep_from_FminRep_check,
    Id.run, ULift.up_down]
  intro p q hMax
  have hOppMin :
      isMin (beta:=beta) b radix
        (_root_.F2R (beta:=beta) (Fopp (beta:=beta) p))
        (Fopp (beta:=beta) q) := by
    rcases hMax with ⟨hqBounded, hp_le_q, hLeastUpper⟩
    have hp_opp :
        _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) =
          -_root_.F2R (beta:=beta) p := by
      simp [Fopp, FloatSpec.Calc.Operations.Fopp, _root_.F2R,
        FloatSpec.Core.Defs.F2R]
    have hq_opp :
        _root_.F2R (beta:=beta) (Fopp (beta:=beta) q) =
          -_root_.F2R (beta:=beta) q := by
      simp [Fopp, FloatSpec.Calc.Operations.Fopp, _root_.F2R,
        FloatSpec.Core.Defs.F2R]
    constructor
    · simpa [Fbounded, Fbounded', Fopp, FloatSpec.Calc.Operations.Fopp, abs_neg] using hqBounded
    constructor
    · rw [hq_opp, hp_opp]
      linarith
    · intro f hfBounded hfLe
      have hf_opp :
          _root_.F2R (beta:=beta) (Fopp (beta:=beta) f) =
            -_root_.F2R (beta:=beta) f := by
        simp [Fopp, FloatSpec.Calc.Operations.Fopp, _root_.F2R,
          FloatSpec.Core.Defs.F2R]
      have hFoppFBounded :
          Fbounded (beta:=beta) b (Fopp (beta:=beta) f) := by
        simpa [Fbounded, Fbounded', Fopp, FloatSpec.Calc.Operations.Fopp, abs_neg] using hfBounded
      have hp_le_fopp :
          _root_.F2R (beta:=beta) p ≤
            _root_.F2R (beta:=beta) (Fopp (beta:=beta) f) := by
        rw [hf_opp]
        rw [hp_opp] at hfLe
        linarith
      have hq_le_fopp := hLeastUpper (Fopp (beta:=beta) f) hFoppFBounded hp_le_fopp
      rw [hf_opp] at hq_le_fopp
      rw [hq_opp]
      linarith
  obtain ⟨m, hm⟩ := hMinRep (Fopp (beta:=beta) p) (Fopp (beta:=beta) q) hOppMin
  refine ⟨-m, ?_⟩
  simp only [Fopp, FloatSpec.Calc.Operations.Fopp,
    FloatSpec.Core.Defs.FlocqFloat.Fexp] at hm
  simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
    FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp,
    Int.cast_neg]
  simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
    FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp,
    Int.cast_neg] at hm
  linarith

noncomputable def MinOrMaxRep_float_from_rep_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop) : Unit :=
  ()

/-- Float-specific `MinOrMaxRep` factored through the min/max representation
payloads.  This is the non-shell version of the upstream step immediately
above `RoundedModeRep`; the only remaining hard input is the min-side
representation theorem. -/
theorem MinOrMaxRep_float_from_rep {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop) :
    ⦃⌜MinOrMaxP_float (beta:=beta) b radix P ∧
        (∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
          isMin (beta:=beta) b radix (_root_.F2R (beta:=beta) p) q →
            ∃ m : Int,
              _root_.F2R (beta:=beta) q =
                _root_.F2R (beta:=beta) ⟨m, p.Fexp⟩) ∧
        (∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
          isMax (beta:=beta) b radix (_root_.F2R (beta:=beta) p) q →
            ∃ m : Int,
              _root_.F2R (beta:=beta) q =
                _root_.F2R (beta:=beta) ⟨m, p.Fexp⟩)⌝⦄
    (pure (MinOrMaxRep_float_from_rep_check (beta:=beta) b radix P) : Id Unit)
    ⦃⇓_ => ⌜∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
          P (_root_.F2R (beta:=beta) p) q →
            ∃ m : Int,
              _root_.F2R (beta:=beta) q =
                _root_.F2R (beta:=beta) ⟨m, p.Fexp⟩⌝⦄ := by
  intro h
  rcases h with ⟨hMinOrMax, hMinRep, hMaxRep⟩
  simp only [wp, PostCond.noThrow, pure, MinOrMaxRep_float_from_rep_check,
    Id.run, ULift.up_down]
  intro p q hP
  rcases hMinOrMax (_root_.F2R (beta:=beta) p) q hP with hMin | hMax
  · exact hMinRep p q hMin
  · exact hMaxRep p q hMax

noncomputable def RoundedModeRep_float_from_minmax_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop) : Unit :=
  ()

/-- Float-specific `RoundedModeRep` factored through the restored
`MinOrMaxP_float` package and explicit min/max representation payloads. -/
theorem RoundedModeRep_float_from_minmax {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop) :
    ⦃⌜RoundedModeP_float (beta:=beta) b radix P ∧
        (∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
          isMin (beta:=beta) b radix (_root_.F2R (beta:=beta) p) q →
            ∃ m : Int,
              _root_.F2R (beta:=beta) q =
                _root_.F2R (beta:=beta) ⟨m, p.Fexp⟩) ∧
        (∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
          isMax (beta:=beta) b radix (_root_.F2R (beta:=beta) p) q →
            ∃ m : Int,
              _root_.F2R (beta:=beta) q =
                _root_.F2R (beta:=beta) ⟨m, p.Fexp⟩)⌝⦄
    (pure (RoundedModeRep_float_from_minmax_check (beta:=beta) b radix P) : Id Unit)
    ⦃⇓_ => ⌜∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
          P (_root_.F2R (beta:=beta) p) q →
            ∃ m : Int,
              _root_.F2R (beta:=beta) q =
                _root_.F2R (beta:=beta) ⟨m, p.Fexp⟩⌝⦄ := by
  intro h
  rcases h with ⟨hRounded, hMinRep, hMaxRep⟩
  simp only [wp, PostCond.noThrow, pure, RoundedModeRep_float_from_minmax_check,
    Id.run, ULift.up_down]
  have hMinOrMax : MinOrMaxP_float (beta:=beta) b radix P := hRounded.2.2.1
  have h := MinOrMaxRep_float_from_rep (beta:=beta) b radix P
  simpa only [wp, PostCond.noThrow, pure, MinOrMaxRep_float_from_rep_check,
    Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hMinOrMax, hMinRep, hMaxRep⟩

-- ---------------------------------------------------------------------------
-- Max-bound comparison lemmas (around Coq: maxFbounded, maxMax, maxMaxBis)
-- Coq: `MaxFloat` — bounded floats stay below the canonical bound at their exponent
noncomputable def MaxFloat_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (p : FloatSpec.Core.Defs.FlocqFloat beta) (z : Int) : Unit :=
  ()

/-- Coq: `MaxFloat` — if a float `p` is bounded by `b`, then
`|F2R p| < F2R ⟨b.vNum, p.Fexp⟩`.

Coq statement: `Fbounded b x → (Rabs x < Float (Zpos (vNum b)) (Fexp x))%R`.

Since `Fbounded` is concrete, this port keeps explicit `Fbounded'` and
`beta > 1` hypotheses matching the Coq section assumptions. The `z` parameter
is kept for compatibility with downstream callers but the bound uses `b.vNum`
at `p.Fexp` per Coq's original. -/
theorem MaxFloat_from_expanded_bound_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (p : FloatSpec.Core.Defs.FlocqFloat beta) (z : Int) :
    ⦃⌜Fbounded (beta:=beta) b p ∧ p.Fexp ≤ z ∧
        Fbounded' b p ∧ 1 < beta⌝⦄
    (pure (MaxFloat_check (beta:=beta) b p z) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) p| <
            _root_.F2R (beta:=beta) ⟨b.vNum, p.Fexp⟩⌝⦄ := by
  intro ⟨_, _, hBdd, hBeta⟩
  simp only [wp, PostCond.noThrow, pure, MaxFloat_check,
             Id.run, ULift.up_down]
  -- F2R p = p.Fnum * β^p.Fexp
  -- F2R ⟨b.vNum, p.Fexp⟩ = b.vNum * β^p.Fexp
  -- From Fbounded': |p.Fnum| < b.vNum, and β^p.Fexp > 0 (since β > 1 > 0)
  have hBetaPos : (0 : ℝ) < (beta : ℝ) := by exact_mod_cast Int.lt_trans Int.zero_lt_one hBeta
  have hExpPos : (0 : ℝ) < (beta : ℝ) ^ p.Fexp := zpow_pos hBetaPos p.Fexp
  show |_root_.F2R p| < _root_.F2R ⟨b.vNum, p.Fexp⟩
  simp only [_root_.F2R, FloatSpec.Core.Defs.F2R]
  rw [abs_mul, abs_of_pos hExpPos]
  apply mul_lt_mul_of_pos_right _ hExpPos
  -- Need: |(p.Fnum : ℝ)| < (b.vNum : ℝ)
  rw [← Int.cast_abs]
  exact_mod_cast hBdd.1

/-- Coq `MaxFloat`. -/
theorem MaxFloat {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b p⌝⦄
    (pure (MaxFloat_check (beta:=beta) b p p.Fexp) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R p| <
      _root_.F2R (beta:=beta) ⟨b.vNum, p.Fexp⟩⌝⦄ := by
  intro hp
  exact MaxFloat_from_expanded_bound_payload
    (beta:=beta) b p p.Fexp
      ⟨hp, le_rfl, hp, ValidRadix.valid (beta:=beta)⟩



-- We port `maxMax` first. In Coq, it states that if `p` is bounded by `b` and
-- `Fexp p ≤ z`, then `Fabs p < Float (Zpos (vNum b)) z`. Our bound skeleton
-- does not carry `vNum`; we state the result against the canonical unit
-- mantissa at exponent `z`, consistent with other places using `⟨1, z⟩`.
noncomputable def maxMax_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (p : FloatSpec.Core.Defs.FlocqFloat beta) (z : Int) : Unit :=
  ()

/-- Coq: `maxMax` — if `p` is bounded and `p.Fexp ≤ z`, then
`|F2R p| < F2R ⟨b.vNum, z⟩`.

Coq statement: `Fbounded b p -> (Fexp p <= z)%Z -> (Fabs p < Float (Zpos (vNum b)) z)%R`.

Since `Fbounded` is concrete, this port keeps explicit `Fbounded'` and
`beta > 1` hypotheses matching the Coq section assumptions, following the MaxFloat pattern.
Since `Fabs` is modeled directly here, we use `|F2R p|` in the conclusion
instead of `F2R (Fabs p)`.

Change record: Fixed RHS from `⟨1, z⟩` to `⟨b.vNum, z⟩` to match Coq's
`Float (Zpos (vNum b)) z`. Used `|F2R p|` instead of `F2R (Fabs p)` because
`Fabs` is modeled directly in Compat.lean. -/
theorem maxMax_from_expanded_bound_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (p : FloatSpec.Core.Defs.FlocqFloat beta) (z : Int) :
    ⦃⌜Fbounded (beta:=beta) b p ∧ p.Fexp ≤ z ∧
        Fbounded' b p ∧ 1 < beta⌝⦄
    (pure (maxMax_check (beta:=beta) b p z) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) p| <
            _root_.F2R (beta:=beta) ⟨b.vNum, z⟩⌝⦄ := by
  intro ⟨_, hExpLe, hBdd, hBeta⟩
  simp only [wp, PostCond.noThrow, pure, maxMax_check,
             Id.run, ULift.up_down]
  show |_root_.F2R p| < _root_.F2R ⟨b.vNum, z⟩
  have hBetaPos : (0 : ℝ) < (beta : ℝ) := by exact_mod_cast Int.lt_trans Int.zero_lt_one hBeta
  have hExpPos : (0 : ℝ) < (beta : ℝ) ^ p.Fexp := zpow_pos hBetaPos p.Fexp
  have hNumBound : |p.Fnum| < b.vNum := hBdd.1
  have hBetaGe1 : (1 : ℝ) ≤ (beta : ℝ) := by
    have : (1 : ℤ) < beta := hBeta
    exact_mod_cast Int.le_of_lt this
  have hExpMono : (beta : ℝ) ^ p.Fexp ≤ (beta : ℝ) ^ z :=
    zpow_right_mono₀ hBetaGe1 hExpLe
  have hvNumPos : (0 : ℝ) < (b.vNum : ℝ) :=
    Int.cast_pos.mpr (Int.lt_of_le_of_lt (abs_nonneg p.Fnum) hNumBound)
  -- |F2R p| = |p.Fnum * β^p.Fexp| = |p.Fnum| * β^p.Fexp (since β^e > 0)
  simp only [_root_.F2R, FloatSpec.Core.Defs.F2R]
  rw [abs_mul, abs_of_pos hExpPos]
  -- Goal: |↑p.Fnum| * β^p.Fexp < b.vNum * β^z
  have hNumLtR : |↑p.Fnum| < (b.vNum : ℝ) := by
    rw [← Int.cast_abs]
    exact_mod_cast hNumBound
  calc |↑p.Fnum| * (beta : ℝ) ^ p.Fexp
      < (b.vNum : ℝ) * (beta : ℝ) ^ p.Fexp :=
        mul_lt_mul_of_pos_right hNumLtR hExpPos
    _ ≤ (b.vNum : ℝ) * (beta : ℝ) ^ z :=
        mul_le_mul_of_nonneg_left hExpMono (le_of_lt hvNumPos)

/-- Coq `maxMax`. -/
theorem maxMax {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (p : FloatSpec.Core.Defs.FlocqFloat beta) (z : Int) :
    ⦃⌜Fbounded (beta:=beta) b p ∧ p.Fexp ≤ z⌝⦄
    (pure (maxMax_check (beta:=beta) b p z) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R p| < _root_.F2R (beta:=beta) ⟨b.vNum, z⟩⌝⦄ := by
  intro ⟨hp, hexp⟩
  exact maxMax_from_expanded_bound_payload (beta:=beta) b p z
    ⟨hp, hexp, hp, ValidRadix.valid (beta:=beta)⟩

/-- Helper computation for `maxMax1`. Mirrors the Hoare-style pipeline used in
`maxMax` but records the weaker (non-strict) inequality variant from Coq. -/
noncomputable def maxMax1_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (p : FloatSpec.Core.Defs.FlocqFloat beta) (z : Int) : Unit :=
  ()

/-- Coq: `maxMax1` — bounded floats whose exponent is at most `z` stay below the
canonical encoding at exponent `z`.

Change record: Fixed postcondition from `F2R (Fabs p) ≤ F2R ⟨1, z⟩` to
`|F2R p| ≤ F2R ⟨b.vNum - 1, z⟩` to match Coq's `Fabs p ≤ Float (pPred (vNum b)) z`.
Added `Fbounded'` and `1 < beta` hypotheses to expose the bound obligations. -/
theorem maxMax1_from_expanded_bound_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (p : FloatSpec.Core.Defs.FlocqFloat beta) (z : Int) :
    ⦃⌜Fbounded (beta:=beta) b p ∧ p.Fexp ≤ z ∧
        Fbounded' b p ∧ 1 < beta⌝⦄
    (pure (maxMax1_check (beta:=beta) b p z) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) p| ≤
            _root_.F2R (beta:=beta) ⟨b.vNum - 1, z⟩⌝⦄ := by
  intro ⟨_, hExpLe, hBdd, hBeta⟩
  simp only [wp, PostCond.noThrow, pure, maxMax1_check,
             Id.run, ULift.up_down]
  show |_root_.F2R p| ≤ _root_.F2R ⟨b.vNum - 1, z⟩
  have hBetaPos : (0 : ℝ) < (beta : ℝ) := by exact_mod_cast Int.lt_trans Int.zero_lt_one hBeta
  have hExpPos : (0 : ℝ) < (beta : ℝ) ^ p.Fexp := zpow_pos hBetaPos p.Fexp
  have hNumBound : |p.Fnum| < b.vNum := hBdd.1
  have hBetaGe1 : (1 : ℝ) ≤ (beta : ℝ) := by
    have : (1 : ℤ) < beta := hBeta
    exact_mod_cast Int.le_of_lt this
  have hExpMono : (beta : ℝ) ^ p.Fexp ≤ (beta : ℝ) ^ z :=
    zpow_right_mono₀ hBetaGe1 hExpLe
  -- |p.Fnum| ≤ b.vNum - 1 from |p.Fnum| < b.vNum
  have hNumLe : |p.Fnum| ≤ b.vNum - 1 := Int.le_sub_one_of_lt hNumBound
  have hNumLeR : |↑p.Fnum| ≤ ((b.vNum - 1 : ℤ) : ℝ) := by
    rw [← Int.cast_abs]
    exact_mod_cast hNumLe
  -- |F2R p| = |p.Fnum * β^p.Fexp| = |p.Fnum| * β^p.Fexp (since β^e > 0)
  simp only [_root_.F2R, FloatSpec.Core.Defs.F2R]
  rw [abs_mul, abs_of_pos hExpPos]
  -- Goal: |↑p.Fnum| * β^p.Fexp ≤ (b.vNum - 1) * β^z
  -- Use: |p.Fnum| ≤ vNum - 1 and β^p.Fexp ≤ β^z
  have hPredNonneg : (0 : ℝ) ≤ ((b.vNum - 1 : ℤ) : ℝ) := by
    have h1 : (0 : ℝ) ≤ |↑p.Fnum| := abs_nonneg (↑p.Fnum : ℝ)
    linarith [hNumLeR]
  calc |↑p.Fnum| * (beta : ℝ) ^ p.Fexp
      ≤ ((b.vNum - 1 : ℤ) : ℝ) * (beta : ℝ) ^ p.Fexp :=
        mul_le_mul_of_nonneg_right hNumLeR (le_of_lt hExpPos)
    _ ≤ ((b.vNum - 1 : ℤ) : ℝ) * (beta : ℝ) ^ z :=
        mul_le_mul_of_nonneg_left hExpMono hPredNonneg

/-- Coq `maxMax1`. -/
theorem maxMax1 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (p : FloatSpec.Core.Defs.FlocqFloat beta) (z : Int) :
    ⦃⌜Fbounded (beta:=beta) b p ∧ p.Fexp ≤ z⌝⦄
    (pure (maxMax1_check (beta:=beta) b p z) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R p| ≤ _root_.F2R (beta:=beta) ⟨b.vNum - 1, z⟩⌝⦄ := by
  intro ⟨hp, hexp⟩
  exact maxMax1_from_expanded_bound_payload (beta:=beta) b p z
    ⟨hp, hexp, hp, ValidRadix.valid (beta:=beta)⟩

/-- Coq: `maxMaxBis` — bounded floats with exponent strictly below `z` stay
below `nNormMin * β^z`. Mirrors the Coq semantics:
`Fbounded b p → Fexp p < z → Fabs p < Float nNormMin z`.

Change record: Fixed postcondition from `F2R (Fabs p) < F2R ⟨1, z⟩` to
`|F2R p| < F2R ⟨nNormMin beta precision, z⟩` to match Coq's
`Fabs p < Float nNormMin z`. Added `Fbounded'`, `1 < beta`, `precision`,
and `pGivesBound` hypotheses to expose the bound obligations. Uses `|F2R p|`
instead of `F2R (Fabs p)`. -/
theorem maxMaxBis {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (p : FloatSpec.Core.Defs.FlocqFloat beta) (z : Int)
    (precision : Nat) :
    ⦃⌜Fbounded (beta:=beta) b p ∧ p.Fexp < z ∧
        Fbounded' b p ∧ 1 < beta ∧
        b.vNum = beta ^ precision⌝⦄
    (pure (maxMax1_check (beta:=beta) b p z) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) p| <
            _root_.F2R (beta:=beta) ⟨nNormMin beta precision, z⟩⌝⦄ := by
  intro ⟨_, hExpLt, hBdd, hBeta, hPGB⟩
  simp only [wp, PostCond.noThrow, pure, maxMax1_check,
             Id.run, ULift.up_down]
  show |_root_.F2R p| < _root_.F2R ⟨nNormMin beta precision, z⟩
  have hBetaPos : (0 : ℝ) < (beta : ℝ) := by exact_mod_cast Int.lt_trans Int.zero_lt_one hBeta
  have hBetaGe1 : (1 : ℝ) ≤ (beta : ℝ) := by exact_mod_cast Int.le_of_lt hBeta
  have hBetaNe0 : (beta : ℝ) ≠ 0 := ne_of_gt hBetaPos
  have hNumBound : |p.Fnum| < b.vNum := hBdd.1
  have hExpPos : (0 : ℝ) < (beta : ℝ) ^ p.Fexp := zpow_pos hBetaPos p.Fexp
  -- |F2R p| = |p.Fnum| * β^p.Fexp
  have hAbsF2R : |_root_.F2R (beta:=beta) p| = |↑p.Fnum| * (beta : ℝ) ^ p.Fexp := by
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R]
    rw [abs_mul, abs_of_pos hExpPos]
  -- F2R ⟨nNormMin, z⟩ = nNormMin * β^z
  have hRHS : _root_.F2R (beta:=beta) ⟨nNormMin beta precision, z⟩ =
              (nNormMin beta precision : ℝ) * (beta : ℝ) ^ z := by
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R]
  -- Handle precision = 0 separately (degenerate: b.vNum = 1, so p.Fnum = 0)
  by_cases hp : precision = 0
  · -- precision = 0: b.vNum = beta^0 = 1
    have hvNum1 : b.vNum = 1 := by simp [hPGB, hp]
    have hFnum0 : p.Fnum = 0 := by
      have h1 : |p.Fnum| < 1 := hvNum1 ▸ hNumBound
      have h2 := abs_nonneg p.Fnum
      have h3 : |p.Fnum| = 0 := by omega
      exact abs_eq_zero.mp h3
    rw [hAbsF2R]
    simp [hFnum0]
    -- nNormMin beta 0 = beta^(0-1) = beta^0 = 1 (since 0-1=0 in ℕ)
    have hNM : nNormMin beta 0 = 1 := by simp [nNormMin]
    rw [hp, hNM]
    simp
    simpa [_root_.F2R, FloatSpec.Core.Defs.F2R] using zpow_pos hBetaPos z
  · -- precision ≥ 1: use transitivity through F2R ⟨b.vNum, z - 1⟩
    have hExpLe : p.Fexp ≤ z - 1 := Int.le_sub_one_of_lt hExpLt
    have hExpMono : (beta : ℝ) ^ p.Fexp ≤ (beta : ℝ) ^ (z - 1) :=
      zpow_right_mono₀ hBetaGe1 hExpLe
    have hvNumPos : (0 : ℝ) < (b.vNum : ℝ) :=
      Int.cast_pos.mpr (Int.lt_of_le_of_lt (abs_nonneg p.Fnum) hNumBound)
    have hNumLtR : |↑p.Fnum| < (b.vNum : ℝ) := by
      rw [← Int.cast_abs]; exact_mod_cast hNumBound
    -- Step 1: |F2R p| < b.vNum * β^(z-1)
    have hStep1 : |_root_.F2R p| < (b.vNum : ℝ) * (beta : ℝ) ^ (z - 1) := by
      rw [hAbsF2R]
      calc |↑p.Fnum| * (beta : ℝ) ^ p.Fexp
          < (b.vNum : ℝ) * (beta : ℝ) ^ p.Fexp :=
            mul_lt_mul_of_pos_right hNumLtR hExpPos
        _ ≤ (b.vNum : ℝ) * (beta : ℝ) ^ (z - 1) :=
            mul_le_mul_of_nonneg_left hExpMono (le_of_lt hvNumPos)
    -- Step 2: b.vNum * β^(z-1) = nNormMin * β^z
    -- b.vNum = β^precision, nNormMin = β^(precision-1)
    -- β^precision * β^(z-1) = β^(precision-1) * β * β^(z-1) = β^(precision-1) * β^z
    have hp1 : precision - 1 + 1 = precision := Nat.succ_pred hp
    have hEq : (b.vNum : ℝ) * (beta : ℝ) ^ (z - 1) =
               (nNormMin beta precision : ℝ) * (beta : ℝ) ^ z := by
      simp only [nNormMin, hPGB]
      push_cast
      -- Goal: (↑beta) ^ precision * (↑beta) ^ (z - 1) = (↑beta) ^ (precision - 1) * (↑beta) ^ z
      rw [← zpow_natCast (beta : ℝ) precision, ← zpow_natCast (beta : ℝ) (precision - 1)]
      rw [← zpow_add₀ hBetaNe0, ← zpow_add₀ hBetaNe0]
      congr 1
      omega
    rw [hRHS]
    linarith

-- ---------------------------------------------------------------------------
-- Exponent comparison helper lemmas (around Coq: eqExpLess, FboundedShiftLess...)

-- Coq: `eqExpLess` — if `p` is bounded and `F2R p = F2R q`,
-- then there exists a bounded `r` with the same real value as `q`
-- and exponent at least that of `q`.
noncomputable def eqExpLess_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (p q : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem eqExpLess {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (p q : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b p ∧ Fbounded (beta:=beta) b q ∧
        _root_.F2R p = _root_.F2R q⌝⦄
    (pure (eqExpLess_check (beta:=beta) b p q) : Id Unit)
    ⦃⇓_ => ⌜∃ r : FloatSpec.Core.Defs.FlocqFloat beta,
              Fbounded (beta:=beta) b r ∧
              _root_.F2R r = _root_.F2R q ∧
              q.Fexp ≤ r.Fexp⌝⦄ := by
  intro ⟨_hBounded, hqBounded, _hEq⟩
  show ∃ r : FloatSpec.Core.Defs.FlocqFloat beta, Fbounded b r ∧ _root_.F2R r = _root_.F2R q ∧ q.Fexp ≤ r.Fexp
  exact ⟨q, hqBounded, rfl, le_refl _⟩

-- Coq: `FboundedShiftLess` — if `m ≤ n` and `Fshift radix n f` is bounded,
-- then `Fshift radix m f` is also bounded.
noncomputable def FboundedShiftLess_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (f : FloatSpec.Core.Defs.FlocqFloat beta) (n m : Nat) : Unit :=
  ()

theorem FboundedShiftLess {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (f : FloatSpec.Core.Defs.FlocqFloat beta) (n m : Nat) :
    ⦃⌜1 < radix ∧ m ≤ n ∧
        Fbounded (beta:=beta) b (Fshift (beta:=beta) radix n f)⌝⦄
    (pure (FboundedShiftLess_check (beta:=beta) b radix f n m) : Id Unit)
    ⦃⇓_ => ⌜Fbounded (beta:=beta) b (Fshift (beta:=beta) radix m f)⌝⦄ := by
  intro h
  rcases h with ⟨hradix, hmn, hboundN⟩
  simp only [wp, PostCond.noThrow, pure, FboundedShiftLess_check, Id.run,
    ULift.up_down]
  show Fbounded b (Fshift radix m f)
  rcases hboundN with ⟨hnumN, hexpN⟩
  constructor
  · simp only [Fshift, Fbounded, FloatSpec.Core.Defs.FlocqFloat.Fnum] at hnumN ⊢
    have hradix_pos : 0 < radix := by omega
    have hradix_abs : |radix| = radix := abs_of_pos hradix_pos
    have hpow_m_nonneg : 0 ≤ radix ^ m :=
      le_of_lt (Int.pow_pos hradix_pos : 0 < radix ^ m)
    have hpow_n_nonneg : 0 ≤ radix ^ n :=
      le_of_lt (Int.pow_pos hradix_pos : 0 < radix ^ n)
    have hpow_le : radix ^ m ≤ radix ^ n := by
      exact pow_le_pow_right₀ (by omega : 1 ≤ radix) hmn
    have habs_le : |f.Fnum * radix ^ m| ≤ |f.Fnum * radix ^ n| := by
      rw [abs_mul, abs_mul, abs_of_nonneg hpow_m_nonneg, abs_of_nonneg hpow_n_nonneg]
      exact mul_le_mul_of_nonneg_left hpow_le (abs_nonneg f.Fnum)
    exact lt_of_le_of_lt habs_le hnumN
  · simp only [Fshift, Fbounded, FloatSpec.Core.Defs.FlocqFloat.Fexp] at hexpN ⊢
    omega

-- Coq: `eqExpMax` — if `p` and `q` are bounded and |F2R p| ≤ F2R q,
-- then there exists a bounded `r` with F2R r = F2R p and Fexp r ≤ Fexp q.
noncomputable def eqExpMax_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

-- Note: Coq has radix ≥ 2 as section variable; added 1 < beta to precondition
theorem eqExpMax {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧
        Fbounded (beta:=beta) b p ∧ Fbounded (beta:=beta) b q ∧
        |_root_.F2R p| ≤ _root_.F2R q⌝⦄
    (pure (eqExpMax_check (beta:=beta) b radix p q) : Id Unit)
    ⦃⇓_ => ⌜∃ r : FloatSpec.Core.Defs.FlocqFloat beta,
              Fbounded (beta:=beta) b r ∧
              _root_.F2R r = _root_.F2R p ∧
              r.Fexp ≤ q.Fexp⌝⦄ := by
  intro ⟨hbeta_radix, hradix, hpBounded, hqBounded, hAbsLe⟩
  simp only [wp, PostCond.noThrow, pure, eqExpMax_check, Id.run,
    ULift.up_down]
  by_cases hExpLe : p.Fexp ≤ q.Fexp
  · exact ⟨p, hpBounded, rfl, hExpLe⟩
  · have hq_lt_p : q.Fexp < p.Fexp := lt_of_not_ge hExpLe
    let n : Nat := Int.natAbs (p.Fexp - q.Fexp)
    let r : FloatSpec.Core.Defs.FlocqFloat beta := Fshift (beta:=beta) radix n p
    have hdiff_nonneg : 0 ≤ p.Fexp - q.Fexp := by omega
    have hn : (n : Int) = p.Fexp - q.Fexp := by
      simpa [n] using Int.natAbs_of_nonneg hdiff_nonneg
    have hr_exp : r.Fexp = q.Fexp := by
      simp only [r, Fshift, FloatSpec.Core.Defs.FlocqFloat.Fexp]
      rw [hn]
      omega
    have hshift_val : _root_.F2R (beta:=beta) r = _root_.F2R (beta:=beta) p :=
      Fshift_value (beta:=beta) radix n p hbeta_radix hradix
    have hbeta_gt : 1 < beta := by omega
    have hbeta_pos_int : (0 : Int) < beta := lt_trans Int.zero_lt_one hbeta_gt
    have hbeta_pos : (0 : ℝ) < (beta : ℝ) := by exact_mod_cast hbeta_pos_int
    have hpow_pos : 0 < (beta : ℝ) ^ q.Fexp := zpow_pos hbeta_pos q.Fexp
    have hmant_mul_le :
        ((|r.Fnum| : Int) : ℝ) * (beta : ℝ) ^ q.Fexp ≤
          (q.Fnum : ℝ) * (beta : ℝ) ^ q.Fexp := by
      have hAbsShift : |_root_.F2R (beta:=beta) r| ≤ _root_.F2R (beta:=beta) q := by
        rw [hshift_val]
        exact hAbsLe
      simpa [_root_.F2R, FloatSpec.Core.Defs.F2R, hr_exp, abs_mul,
        abs_of_pos hpow_pos] using hAbsShift
    have hmant_le_real : ((|r.Fnum| : Int) : ℝ) ≤ (q.Fnum : ℝ) :=
      le_of_mul_le_mul_right hmant_mul_le hpow_pos
    have hmant_le : |r.Fnum| ≤ q.Fnum := by
      exact_mod_cast hmant_le_real
    have hq_num_lt : q.Fnum < b.vNum :=
      lt_of_le_of_lt (le_abs_self q.Fnum) hqBounded.1
    have hr_num_lt : |r.Fnum| < b.vNum :=
      lt_of_le_of_lt hmant_le hq_num_lt
    have hr_exp_bound : -b.dExp ≤ r.Fexp := by
      rw [hr_exp]
      exact hqBounded.2
    exact ⟨r, ⟨hr_num_lt, hr_exp_bound⟩, hshift_val, by rw [hr_exp]⟩

-- Coq: `FboundNext` — the real value obtained by incrementing a bounded
-- mantissa has a bounded representative, carrying at the mantissa limit.
noncomputable def FboundNext_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem FboundNext {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧ b.vNum = Zpower_nat radix precision ∧
        Fbounded (beta:=beta) b p⌝⦄
    (pure (FboundNext_check (beta:=beta) b radix precision p) : Id Unit)
    ⦃⇓_ => ⌜∃ q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) b q ∧
        _root_.F2R (beta:=beta) q =
          _root_.F2R (beta:=beta)
            (FloatSpec.Core.Defs.FlocqFloat.mk (beta:=beta) (p.Fnum + 1) p.Fexp)⌝⦄ := by
  intro h
  rcases h with ⟨hbeta_radix, hradix, hprecision, hvNum, hpBounded⟩
  simp only [wp, PostCond.noThrow, pure, FboundNext_check, Id.run, ULift.up_down]
  have hradix_pos : 0 < radix := by omega
  have hradixR_pos : (0 : ℝ) < (radix : ℝ) := by exact_mod_cast hradix_pos
  have hradixR_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradixR_pos
  have hvNum_pos : 0 < b.vNum := by
    rw [hvNum, Zpower_nat]
    exact pow_pos hradix_pos precision
  have hp_num_lt : |p.Fnum| < b.vNum := hpBounded.1
  have hp_exp : -b.dExp ≤ p.Fexp := hpBounded.2
  by_cases hsucc_lt : |p.Fnum + 1| < b.vNum
  · refine ⟨⟨p.Fnum + 1, p.Fexp⟩, ?_, rfl⟩
    exact ⟨hsucc_lt, hp_exp⟩
  · have hsucc_le : p.Fnum + 1 ≤ b.vNum := by
      have hp_le : p.Fnum ≤ |p.Fnum| := le_abs_self p.Fnum
      omega
    have hsucc_abs_le : |p.Fnum + 1| ≤ b.vNum := by
      by_cases hnonneg : 0 ≤ p.Fnum + 1
      · rw [abs_of_nonneg hnonneg]
        exact hsucc_le
      · have hneg : p.Fnum + 1 < 0 := lt_of_not_ge hnonneg
        have h_abs_succ_lt : |p.Fnum + 1| < |p.Fnum| := by
          rw [abs_of_neg hneg]
          have hp_neg : p.Fnum < 0 := by omega
          rw [abs_of_neg hp_neg]
          omega
        exact le_of_lt (lt_trans h_abs_succ_lt hp_num_lt)
    have hsucc_abs_eq : |p.Fnum + 1| = b.vNum :=
      le_antisymm hsucc_abs_le (le_of_not_gt hsucc_lt)
    have hsucc_nonneg : 0 ≤ p.Fnum + 1 := by
      by_contra h
      have hneg : p.Fnum + 1 < 0 := lt_of_not_ge h
      have h_abs_succ_lt : |p.Fnum + 1| < |p.Fnum| := by
        rw [abs_of_neg hneg]
        have hp_neg : p.Fnum < 0 := by omega
        rw [abs_of_neg hp_neg]
        omega
      omega
    have hsucc_eq : p.Fnum + 1 = b.vNum := by
      simpa [abs_of_nonneg hsucc_nonneg] using hsucc_abs_eq
    let q : FloatSpec.Core.Defs.FlocqFloat beta :=
      ⟨radix ^ (precision - 1), p.Fexp + 1⟩
    refine ⟨q, ?_, ?_⟩
    · constructor
      · have hpow_lt : radix ^ (precision - 1) < b.vNum := by
          rw [hvNum, Zpower_nat]
          have hpow_succ :
              radix ^ precision = radix ^ (precision - 1) * radix := by
            have hprec : Nat.succ (precision - 1) = precision := by omega
            calc
              radix ^ precision = radix ^ Nat.succ (precision - 1) := by rw [hprec]
              _ = radix ^ (precision - 1) * radix := by rw [pow_succ]
          rw [hpow_succ]
          have hpow_pos : 0 < radix ^ (precision - 1) := pow_pos hradix_pos _
          nlinarith
        have hpow_nonneg : 0 ≤ radix ^ (precision - 1) := le_of_lt (pow_pos hradix_pos _)
        simpa [q, abs_of_nonneg hpow_nonneg] using hpow_lt
      · dsimp [q]
        omega
    · dsimp [q]
      rw [hsucc_eq, hvNum, Zpower_nat]
      have hpow_succ :
          (radix : ℝ) ^ (precision : Int) =
            (radix : ℝ) ^ ((precision - 1 : Nat) : Int) * (radix : ℝ) := by
        have hprec : Nat.succ (precision - 1) = precision := by omega
        calc
          (radix : ℝ) ^ (precision : Int)
              = (radix : ℝ) ^ ((Nat.succ (precision - 1) : Nat) : Int) := by
                  rw [hprec]
          _ = (radix : ℝ) ^ (((precision - 1 : Nat) : Int) + 1) := by rfl
          _ = (radix : ℝ) ^ ((precision - 1 : Nat) : Int) * (radix : ℝ) := by
                  rw [zpow_add₀ hradixR_ne]
                  simp [zpow_one]
      have hcarry :
          ((radix ^ precision : Int) : ℝ) * (beta : ℝ) ^ p.Fexp =
            ((radix ^ (precision - 1) : Int) : ℝ) * (beta : ℝ) ^ (p.Fexp + 1) := by
        have hbetaR : (beta : ℝ) = (radix : ℝ) := by exact_mod_cast hbeta_radix
        calc
          ((radix ^ precision : Int) : ℝ) * (beta : ℝ) ^ p.Fexp
              = (radix : ℝ) ^ (precision : Int) * (beta : ℝ) ^ p.Fexp := by
                  simp
          _ = ((radix : ℝ) ^ ((precision - 1 : Nat) : Int) * (radix : ℝ)) *
                (beta : ℝ) ^ p.Fexp := by
                  rw [hpow_succ]
          _ = (radix : ℝ) ^ ((precision - 1 : Nat) : Int) *
                ((beta : ℝ) ^ p.Fexp * (beta : ℝ)) := by
                  rw [hbetaR]
                  ring
          _ = (radix : ℝ) ^ ((precision - 1 : Nat) : Int) *
                (beta : ℝ) ^ (p.Fexp + 1) := by
                  rw [zpow_add₀ (by simpa [hbetaR] using hradixR_ne)]
                  simp [zpow_one, mul_assoc]
          _ = ((radix ^ (precision - 1) : Int) : ℝ) *
                (beta : ℝ) ^ (p.Fexp + 1) := by
                  simp
      exact hcarry.symm

-- Coq: `RoundedModeRep` — representation form for rounded modes
-- Coq statement: RoundedModePGeneric P → ∀ p q, P p q → ∃ m, q = Float m (Fexp p) :>R
-- Since the generic `MinOrMaxPGeneric` fallback is weak, we add an explicit hypothesis
-- carrying the real content, following the pattern of MinOrMaxRep.
-- Changed postcondition to use F2R equality (matching Coq's :>R coercion).
noncomputable def RoundedModeRep_check {beta : Int} [ValidRadix beta]
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop) : Unit :=
  ()

theorem RoundedModeRepGeneric {beta : Int} [ValidRadix beta]
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop) :
    ⦃⌜RoundedModePGeneric P ∧
      (∀ (p q : FloatSpec.Core.Defs.FlocqFloat beta),
        P (_root_.F2R p) q → ∃ m : Int,
          _root_.F2R (beta := beta) q = _root_.F2R (beta := beta) ⟨m, p.Fexp⟩)⌝⦄
    (pure (RoundedModeRep_check (beta:=beta) P) : Id Unit)
    ⦃⇓_ => ⌜∀ (p q : FloatSpec.Core.Defs.FlocqFloat beta),
            P (_root_.F2R p) q → ∃ m : Int,
              _root_.F2R (beta := beta) q = _root_.F2R (beta := beta) ⟨m, p.Fexp⟩⌝⦄ := by
  intro ⟨_, hRep⟩
  simp only [wp, PostCond.noThrow, pure, RoundedModeRep_check,
    Id.run, ULift.up_down]
  exact hRep

-- Coq: `pow_NR0` — if e ≠ 0 then e^n ≠ 0
noncomputable def pow_NR0_check (e : ℝ) (n : Nat) : Unit :=
  ()

theorem pow_NR0 (e : ℝ) (n : Nat) :
    ⦃⌜e ≠ 0⌝⦄
    (pure (pow_NR0_check e n) : Id Unit)
    ⦃⇓_ => ⌜e ^ n ≠ 0⌝⦄ := by
  intro he
  simp only [wp, PostCond.noThrow, pure, pow_NR0_check, Id.run,
    ULift.up_down]
  show e ^ n ≠ 0
  exact pow_ne_zero n he

-- Coq: `pow_add` — e^(n+m) = e^n * e^m
noncomputable def pow_add_compat_check (e : ℝ) (n m : Nat) : Unit :=
  ()

-- Renamed to avoid clashing with Mathlib's `pow_add`
theorem pow_add_compat (e : ℝ) (n m : Nat) :
    ⦃⌜True⌝⦄
    (pure (pow_add_compat_check e n m) : Id Unit)
    ⦃⇓_ => ⌜e ^ (n + m) = e ^ n * e ^ m⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, pow_add_compat_check, Id.run,
    ULift.up_down]
  show e ^ (n + m) = e ^ n * e ^ m
  exact pow_add e n m

namespace FloatSpec.PffCompat

/-- Coq: `pow_add`.

FloatSpec also exposes the direct theorem form, namespaced to avoid clashing
with Mathlib's root `pow_add`. -/
theorem pow_add (e : ℝ) (n m : Nat) :
    e ^ (n + m) = e ^ n * e ^ m := by
  exact _root_.pow_add e n m

end FloatSpec.PffCompat

-- Coq: `pow_RN_plus` — e ≠ 0 → e^n = e^(n+m) * (e^m)⁻¹
noncomputable def pow_RN_plus_check (e : ℝ) (n m : Nat) : Unit :=
  ()

theorem pow_RN_plus (e : ℝ) (n m : Nat) :
    ⦃⌜e ≠ 0⌝⦄
    (pure (pow_RN_plus_check e n m) : Id Unit)
    ⦃⇓_ => ⌜e ^ n = e ^ (n + m) * (e ^ m)⁻¹⌝⦄ := by
  intro he
  simp only [wp, PostCond.noThrow, pure, pow_RN_plus_check, Id.run,
    ULift.up_down]
  show e ^ n = e ^ (n + m) * (e ^ m)⁻¹
  rw [pow_add]
  field_simp [pow_ne_zero m he, pow_ne_zero n he]

-- Coq: `pow_lt` — 0 < e → 0 < e^n
noncomputable def pow_lt_check (e : ℝ) (n : Nat) : Unit :=
  ()

theorem pow_lt (e : ℝ) (n : Nat) :
    ⦃⌜0 < e⌝⦄
    (pure (pow_lt_check e n) : Id Unit)
    ⦃⇓_ => ⌜0 < e ^ n⌝⦄ := by
  intro he
  simp only [wp, PostCond.noThrow, pure, pow_lt_check, Id.run,
    ULift.up_down]
  show 0 < e ^ n
  exact pow_pos he n

-- Coq: `Rlt_pow_R1` — 1 < e → 0 < n → 1 < e^n
noncomputable def Rlt_pow_R1_check (e : ℝ) (n : Nat) : Unit :=
  ()

theorem Rlt_pow_R1 (e : ℝ) (n : Nat) :
    ⦃⌜1 < e ∧ 0 < n⌝⦄
    (pure (Rlt_pow_R1_check e n) : Id Unit)
    ⦃⇓_ => ⌜1 < e ^ n⌝⦄ := by
  intro ⟨he, hn⟩
  simp only [wp, PostCond.noThrow, pure, Rlt_pow_R1_check, Id.run,
    ULift.up_down]
  show 1 < e ^ n
  exact one_lt_pow₀ he hn.ne'

-- Coq: `Rlt_pow` — 1 < e → n < m → e^n < e^m
noncomputable def Rlt_pow_check (e : ℝ) (n m : Nat) : Unit :=
  ()

theorem Rlt_pow (e : ℝ) (n m : Nat) :
    ⦃⌜1 < e ∧ n < m⌝⦄
    (pure (Rlt_pow_check e n m) : Id Unit)
    ⦃⇓_ => ⌜e ^ n < e ^ m⌝⦄ := by
  intro ⟨he, hnm⟩
  simp only [wp, PostCond.noThrow, pure, Rlt_pow_check, Id.run,
    ULift.up_down]
  show e ^ n < e ^ m
  exact pow_lt_pow_right₀ he hnm

-- Coq: `pow_R1` — r^n = 1 → |r| = 1 ∨ n = 0
noncomputable def pow_R1_check (r : ℝ) (n : Nat) : Unit :=
  ()

theorem pow_R1 (r : ℝ) (n : Nat) :
    ⦃⌜r ^ n = 1⌝⦄
    (pure (pow_R1_check r n) : Id Unit)
    ⦃⇓_ => ⌜|r| = 1 ∨ n = 0⌝⦄ := by
  intro hrn
  simp only [wp, PostCond.noThrow, pure, pow_R1_check, Id.run,
    ULift.up_down]
  show |r| = 1 ∨ n = 0
  by_cases hn : n = 0
  · exact Or.inr hn
  · left
    rw [← abs_pow_eq_one r hn]
    rw [hrn, abs_one]

-- Coq: `Rle_Fexp_eq_Zle` — if x ≤ y and Fexp x = Fexp y then Fnum x ≤ Fnum y
noncomputable def Rle_Fexp_eq_Zle_check {beta : Int} [ValidRadix beta]
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem Rle_Fexp_eq_Zle {beta : Int} [ValidRadix beta]
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜_root_.F2R x ≤ _root_.F2R y ∧ x.Fexp = y.Fexp ∧ (1 : Int) < beta⌝⦄
    (pure (Rle_Fexp_eq_Zle_check (beta:=beta) x y) : Id Unit)
    ⦃⇓_ => ⌜x.Fnum ≤ y.Fnum⌝⦄ := by
  intro ⟨hle, hexp, hβ⟩
  simp only [wp, PostCond.noThrow, pure, Rle_Fexp_eq_Zle_check, Id.run,
    ULift.up_down]
  show x.Fnum ≤ y.Fnum
  -- F2R x = x.Fnum * β^(x.Fexp), F2R y = y.Fnum * β^(y.Fexp)
  -- Since x.Fexp = y.Fexp, we have x.Fnum * β^e ≤ y.Fnum * β^e
  -- β > 0 (from 1 < beta), so β^e > 0, and we can cancel it
  have hbeta_pos : (0 : ℝ) < (beta : ℝ) := by
    have : (0 : Int) < beta := lt_trans (by norm_num : (0 : Int) < 1) hβ
    exact_mod_cast this
  have hpow_pos : (0 : ℝ) < (beta : ℝ) ^ x.Fexp := zpow_pos hbeta_pos x.Fexp
  -- From F2R x ≤ F2R y: x.Fnum * β^(x.Fexp) ≤ y.Fnum * β^(y.Fexp)
  -- Rewrite using x.Fexp = y.Fexp
  have hle' : (x.Fnum : ℝ) * (beta : ℝ) ^ x.Fexp ≤ (y.Fnum : ℝ) * (beta : ℝ) ^ x.Fexp := by
    have := hle
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R] at this
    rwa [← hexp] at this
  -- Cancel β^(x.Fexp) > 0
  have hle_cast : (x.Fnum : ℝ) ≤ (y.Fnum : ℝ) :=
    le_of_mul_le_mul_of_pos_right hle' hpow_pos
  exact_mod_cast hle_cast

-- Coq: `powerRZ_O` — e^0 = 1 (integer exponent)
noncomputable def powerRZ_O_check (e : ℝ) : Unit :=
  ()

theorem powerRZ_O (e : ℝ) :
    ⦃⌜True⌝⦄
    (pure (powerRZ_O_check e) : Id Unit)
    ⦃⇓_ => ⌜e ^ (0 : Int) = (1 : ℝ)⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, powerRZ_O_check, Id.run,
    ULift.up_down]
  show e ^ (0 : Int) = (1 : ℝ)
  exact zpow_zero e

-- Coq: `Zpower_NR0` — 0 ≤ e → 0 ≤ e^n (as integer power on Int)
noncomputable def Zpower_NR0_check (e : Int) (n : Nat) : Unit :=
  ()

theorem Zpower_NR0 (e : Int) (n : Nat) :
    ⦃⌜0 ≤ e⌝⦄
    (pure (Zpower_NR0_check e n) : Id Unit)
    ⦃⇓_ => ⌜0 ≤ (e : Int) ^ n⌝⦄ := by
  intro he
  simp only [wp, PostCond.noThrow, pure, Zpower_NR0_check, Id.run,
    ULift.up_down]
  show 0 ≤ (e : Int) ^ n
  exact pow_nonneg he n

-- Coq: `Zpower_NR1` — 1 ≤ e → 1 ≤ e^n (as integer power on Int)
noncomputable def Zpower_NR1_check (e : Int) (n : Nat) : Unit :=
  ()

theorem Zpower_NR1 (e : Int) (n : Nat) :
    ⦃⌜1 ≤ e⌝⦄
    (pure (Zpower_NR1_check e n) : Id Unit)
    ⦃⇓_ => ⌜1 ≤ (e : Int) ^ n⌝⦄ := by
  intro he
  simp only [wp, PostCond.noThrow, pure, Zpower_NR1_check, Id.run,
    ULift.up_down]
  show 1 ≤ (e : Int) ^ n
  exact one_le_pow₀ he

-- Coq: `powerRZ_1` — e^1 = e (integer exponent)
noncomputable def powerRZ_1_check (e : ℝ) : Unit :=
  ()

theorem powerRZ_1 (e : ℝ) :
    ⦃⌜True⌝⦄
    (pure (powerRZ_1_check e) : Id Unit)
    ⦃⇓_ => ⌜e ^ (1 : Int) = e⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, powerRZ_1_check, Id.run,
    ULift.up_down]
  show e ^ (1 : Int) = e
  exact zpow_one e

-- Coq: `powerRZ_R1` — 1^n = 1 (integer exponent)
noncomputable def powerRZ_R1_check (n : Int) : Unit :=
  ()

theorem powerRZ_R1 (n : Int) :
    ⦃⌜True⌝⦄
    (pure (powerRZ_R1_check n) : Id Unit)
    ⦃⇓_ => ⌜(1 : ℝ) ^ n = (1 : ℝ)⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, powerRZ_R1_check, Id.run,
    ULift.up_down]
  show (1 : ℝ) ^ n = (1 : ℝ)
  exact one_zpow n

-- Coq: `powerRZ_add` — e^(m+n) = e^m * e^n (integer exponent)
noncomputable def powerRZ_add_check (e : ℝ) (m n : Int) : Unit :=
  ()

-- Note: Coq original requires e ≠ 0; precondition corrected to match
theorem powerRZ_add (e : ℝ) (m n : Int) :
    ⦃⌜e ≠ 0⌝⦄
    (pure (powerRZ_add_check e m n) : Id Unit)
    ⦃⇓_ => ⌜e ^ (m + n) = e ^ m * e ^ n⌝⦄ := by
  intro he
  simp only [wp, PostCond.noThrow, pure, powerRZ_add_check, Id.run,
    ULift.up_down]
  show e ^ (m + n) = e ^ m * e ^ n
  exact zpow_add₀ he m n

-- Coq: `powerRZ_Zopp` — e^(-z) = (e^z)⁻¹ for nonzero base
noncomputable def powerRZ_Zopp_check (e : ℝ) (z : Int) : Unit :=
  ()

theorem powerRZ_Zopp (e : ℝ) (z : Int) :
    ⦃⌜e ≠ 0⌝⦄
    (pure (powerRZ_Zopp_check e z) : Id Unit)
    ⦃⇓_ => ⌜e ^ (-z) = (e ^ z)⁻¹⌝⦄ := by
  intro he
  simp only [wp, PostCond.noThrow, pure, powerRZ_Zopp_check, Id.run,
    ULift.up_down]
  show e ^ (-z) = (e ^ z)⁻¹
  exact zpow_neg e z

-- Coq: `powerRZ_Zs` — e^(Z.succ n) = e * e^n for nonzero base
noncomputable def powerRZ_Zs_check (e : ℝ) (n : Int) : Unit :=
  ()

theorem powerRZ_Zs (e : ℝ) (n : Int) :
    ⦃⌜e ≠ 0⌝⦄
    (pure (powerRZ_Zs_check e n) : Id Unit)
    ⦃⇓_ => ⌜e ^ (Int.succ n) = e * e ^ n⌝⦄ := by
  intro he
  simp only [wp, PostCond.noThrow, pure, powerRZ_Zs_check, Id.run,
    ULift.up_down]
  show e ^ (Int.succ n) = e * e ^ n
  unfold Int.succ
  rw [zpow_add₀ he, zpow_one, mul_comm]

-- Coq: `Zpower_nat_Z_powerRZ` — bridge between integer and real powers

noncomputable def Zpower_nat_Z_powerRZ_check (n : Int) (m : Nat) : Unit :=
  ()

theorem Zpower_nat_Z_powerRZ (n : Int) (m : Nat) :
    ⦃⌜True⌝⦄
    (pure (Zpower_nat_Z_powerRZ_check n m) : Id Unit)
    ⦃⇓_ => ⌜(Zpower_nat n m : ℝ) = ( (n : ℝ) ^ (m : Int) )⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, Zpower_nat_Z_powerRZ_check, Id.run,
    ULift.up_down]
  show (Zpower_nat n m : ℝ) = (↑n : ℝ) ^ (↑m : Int)
  simp [Zpower_nat, zpow_natCast, Int.cast_pow]

-- Coq: `powerRZ_lt` — if 0 < e then 0 < e^z (integer exponent)
noncomputable def powerRZ_lt_check (e : ℝ) (z : Int) : Unit :=
  ()

theorem powerRZ_lt (e : ℝ) (z : Int) :
    ⦃⌜0 < e⌝⦄
    (pure (powerRZ_lt_check e z) : Id Unit)
    ⦃⇓_ => ⌜0 < e ^ z⌝⦄ := by
  intro he
  simp only [wp, PostCond.noThrow, pure, powerRZ_lt_check, Id.run,
    ULift.up_down]
  show 0 < e ^ z
  exact zpow_pos he z

-- Coq: `powerRZ_le` — 0 < e → 0 ≤ e^z (integer exponent)
noncomputable def powerRZ_le_check (e : ℝ) (z : Int) : Unit :=
  ()

theorem powerRZ_le (e : ℝ) (z : Int) :
    ⦃⌜0 < e⌝⦄
    (pure (powerRZ_le_check e z) : Id Unit)
    ⦃⇓_ => ⌜0 ≤ e ^ z⌝⦄ := by
  intro he
  simp only [wp, PostCond.noThrow, pure, powerRZ_le_check, Id.run,
    ULift.up_down]
  show 0 ≤ e ^ z
  exact le_of_lt (zpow_pos he z)

-- Coq: `Rlt_powerRZ` — 1 < e → n < m → e^n < e^m
noncomputable def Rlt_powerRZ_check (e : ℝ) (n m : Int) : Unit :=
  ()

theorem Rlt_powerRZ (e : ℝ) (n m : Int) :
    ⦃⌜1 < e ∧ n < m⌝⦄
    (pure (Rlt_powerRZ_check e n m) : Id Unit)
    ⦃⇓_ => ⌜e ^ n < e ^ m⌝⦄ := by
  intro ⟨he, hnm⟩
  simp only [wp, PostCond.noThrow, pure, Rlt_powerRZ_check, Id.run,
    ULift.up_down]
  show e ^ n < e ^ m
  exact zpow_lt_zpow_right₀ he hnm

-- Coq: `Zpower_nat_powerRZ_absolu` — IZR (Zpower_nat n (Z.abs_nat m)) = powerRZ (IZR n) m for m ≥ 0
noncomputable def Zpower_nat_powerRZ_absolu_check (n m : Int) : Unit :=
  ()

theorem Zpower_nat_powerRZ_absolu (n m : Int) :
    ⦃⌜0 ≤ m⌝⦄
    (pure (Zpower_nat_powerRZ_absolu_check n m) : Id Unit)
    ⦃⇓_ => ⌜(Zpower_nat n (Int.toNat (Int.natAbs m)) : ℝ) = (n : ℝ) ^ m⌝⦄ := by
  intro hm
  simp only [wp, PostCond.noThrow, pure, Zpower_nat_powerRZ_absolu_check,
    Id.run, ULift.up_down]
  show (Zpower_nat n (Int.toNat (Int.natAbs m)) : ℝ) = (↑n : ℝ) ^ m
  have hm0 : (0 : ℤ) ≤ m := hm
  simp only [Int.toNat.eq_1, Zpower_nat, Int.cast_pow, Int.natAbs_of_nonneg hm0,
    ← zpow_natCast, Int.toNat_of_nonneg hm0]

-- Coq: `Rle_powerRZ` — 1 ≤ e → n ≤ m → e^n ≤ e^m
noncomputable def Rle_powerRZ_check (e : ℝ) (n m : Int) : Unit :=
  ()

theorem Rle_powerRZ (e : ℝ) (n m : Int) :
    ⦃⌜1 ≤ e ∧ n ≤ m⌝⦄
    (pure (Rle_powerRZ_check e n m) : Id Unit)
    ⦃⇓_ => ⌜e ^ n ≤ e ^ m⌝⦄ := by
  intro ⟨he, hnm⟩
  simp only [wp, PostCond.noThrow, pure, Rle_powerRZ_check, Id.run,
    ULift.up_down]
  show e ^ n ≤ e ^ m
  exact zpow_right_mono₀ he hnm

-- Coq: `Zlt_powerRZ` — 1 ≤ e → e^n < e^m → n < m
noncomputable def Zlt_powerRZ_check (e : ℝ) (n m : Int) : Unit :=
  ()

theorem Zlt_powerRZ (e : ℝ) (n m : Int) :
    ⦃⌜1 ≤ e ∧ e ^ n < e ^ m⌝⦄
    (pure (Zlt_powerRZ_check e n m) : Id Unit)
    ⦃⇓_ => ⌜n < m⌝⦄ := by
  intro ⟨he, hlt⟩
  simp only [wp, PostCond.noThrow, pure, Zlt_powerRZ_check, Id.run,
    ULift.up_down]
  show n < m
  rcases eq_or_lt_of_le he with rfl | he'
  · exfalso; simp [one_zpow] at hlt
  · exact (zpow_right_strictMono₀ he').lt_iff_lt.mp hlt

-- Coq: `Rlt_monotony_exp` — multiply preserves < with positive factor (power)
noncomputable def Rlt_monotony_exp_check (radix : Int) (x y : ℝ) (z : Int) : Unit :=
  ()

theorem Rlt_monotony_exp (radix : Int) (x y : ℝ) (z : Int) :
    ⦃⌜1 < radix ∧ x < y⌝⦄
    (pure (Rlt_monotony_exp_check radix x y z) : Id Unit)
    ⦃⇓_ => ⌜x * (radix : ℝ) ^ z < y * (radix : ℝ) ^ z⌝⦄ := by
  intro ⟨hradix, hxy⟩
  simp only [wp, PostCond.noThrow, pure, Rlt_monotony_exp_check, Id.run,
    ULift.up_down]
  have hradix' : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (lt_trans (by norm_num : (0 : Int) < 1) hradix)
  exact mul_lt_mul_of_pos_right hxy (zpow_pos hradix' z)

-- Coq: `Rle_monotone_exp` — multiply preserves ≤ with positive factor (power)
noncomputable def Rle_monotone_exp_check (radix : Int) (x y : ℝ) (z : Int) : Unit :=
  ()

theorem Rle_monotone_exp (radix : Int) (x y : ℝ) (z : Int) :
    ⦃⌜1 < radix ∧ x ≤ y⌝⦄
    (pure (Rle_monotone_exp_check radix x y z) : Id Unit)
    ⦃⇓_ => ⌜x * (radix : ℝ) ^ z ≤ y * (radix : ℝ) ^ z⌝⦄ := by
  intro ⟨hradix, hxy⟩
  simp only [wp, PostCond.noThrow, pure, Rle_monotone_exp_check, Id.run,
    ULift.up_down]
  have hradix' : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (lt_trans (by norm_num : (0 : Int) < 1) hradix)
  exact mul_le_mul_of_nonneg_right hxy (le_of_lt (zpow_pos hradix' z))

-- Coq: `Rlt_monotony_contra_exp` — cancel positive power factor from <
noncomputable def Rlt_monotony_contra_exp_check (radix : Int) (x y : ℝ) (z : Int) : Unit :=
  ()

theorem Rlt_monotony_contra_exp (radix : Int) (x y : ℝ) (z : Int) :
    ⦃⌜1 < radix ∧ x * (radix : ℝ) ^ z < y * (radix : ℝ) ^ z⌝⦄
    (pure (Rlt_monotony_contra_exp_check radix x y z) : Id Unit)
    ⦃⇓_ => ⌜x < y⌝⦄ := by
  intro ⟨hradix, hxy⟩
  simp only [wp, PostCond.noThrow, pure, Rlt_monotony_contra_exp_check, Id.run, ULift.up_down]
  have hradix' : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (lt_trans (by norm_num : (0 : Int) < 1) hradix)
  exact lt_of_mul_lt_mul_right hxy (le_of_lt (zpow_pos hradix' z))

-- Coq: `Rle_monotony_contra_exp` — cancel positive power factor from ≤
noncomputable def Rle_monotony_contra_exp_check (radix : Int) (x y : ℝ) (z : Int) : Unit :=
  ()

theorem Rle_monotony_contra_exp (radix : Int) (x y : ℝ) (z : Int) :
    ⦃⌜1 < radix ∧ x * (radix : ℝ) ^ z ≤ y * (radix : ℝ) ^ z⌝⦄
    (pure (Rle_monotony_contra_exp_check radix x y z) : Id Unit)
    ⦃⇓_ => ⌜x ≤ y⌝⦄ := by
  intro ⟨hradix, hxy⟩
  simp only [wp, PostCond.noThrow, pure, Rle_monotony_contra_exp_check, Id.run, ULift.up_down]
  have hradix' : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (lt_trans (by norm_num : (0 : Int) < 1) hradix)
  exact le_of_mul_le_mul_right hxy (zpow_pos hradix' z)

-- Coq: `FtoREqInv2` — equality by equal real value and same exponent
noncomputable def FtoREqInv2_check {beta : Int} [ValidRadix beta]
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem FtoREqInv2 {beta : Int} [ValidRadix beta]
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜_root_.F2R p = _root_.F2R q ∧ p.Fexp = q.Fexp ∧ 1 < beta⌝⦄
    (pure (FtoREqInv2_check (beta:=beta) p q) : Id Unit)
    ⦃⇓_ => ⌜p = q⌝⦄ := by
  intro ⟨hval, hexp, hβ⟩
  simp only [wp, PostCond.noThrow, pure, FtoREqInv2_check, Id.run,
    ULift.up_down]
  cases p with
  | mk pn pe =>
  cases q with
  | mk qn qe =>
    simp only [FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp] at hexp
    subst qe
    simp only [_root_.F2R, FloatSpec.Core.Defs.FlocqFloat.Fnum,
      FloatSpec.Core.Defs.FlocqFloat.Fexp] at hval ⊢
    have hβpos : (0 : ℝ) < (beta : ℝ) := by
      exact_mod_cast (lt_trans (by norm_num : (0 : Int) < 1) hβ)
    have hpow_ne : (beta : ℝ) ^ pe ≠ 0 := ne_of_gt (zpow_pos hβpos pe)
    have hreal : (pn : ℝ) = (qn : ℝ) := mul_right_cancel₀ hpow_ne hval
    have hint : pn = qn := by exact_mod_cast hreal
    simp [hint]

-- Coq: `sameExpEq` — if two floats have equal real value and same exponent, they are equal
noncomputable def sameExpEq_check {beta : Int} [ValidRadix beta]
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem sameExpEq {beta : Int} [ValidRadix beta]
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜_root_.F2R p = _root_.F2R q ∧ p.Fexp = q.Fexp ∧ 1 < beta⌝⦄
    (pure (sameExpEq_check (beta:=beta) p q) : Id Unit)
    ⦃⇓_ => ⌜p = q⌝⦄ := by
  -- Mirrors Coq `sameExpEq`; see also `FtoREqInv2`.
  intro h
  simp only [wp, PostCond.noThrow, pure, sameExpEq_check, Id.run,
    ULift.up_down]
  exact (FtoREqInv2 (beta := beta) p q) h

-- Coq: `Rlt_Float_Zlt` — compare mantissas when exponents equal
noncomputable def Rlt_Float_Zlt_check {beta : Int} [ValidRadix beta] (p q r : Int) : Unit :=
  ()

theorem Rlt_Float_Zlt {beta : Int} [ValidRadix beta] (p q r : Int) :
    ⦃⌜1 < beta ∧ _root_.F2R (⟨p, r⟩ : FloatSpec.Core.Defs.FlocqFloat beta) <
         _root_.F2R (⟨q, r⟩ : FloatSpec.Core.Defs.FlocqFloat beta)⌝⦄
    (pure (Rlt_Float_Zlt_check (beta:=beta) p q r) : Id Unit)
    ⦃⇓_ => ⌜p < q⌝⦄ := by
  intro ⟨hβ, hlt⟩
  simp only [wp, PostCond.noThrow, pure, Rlt_Float_Zlt_check, Id.run,
    ULift.up_down]
  simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, FloatSpec.Core.Defs.FlocqFloat.Fnum,
    FloatSpec.Core.Defs.FlocqFloat.Fexp] at hlt
  have hβpos : (0 : ℝ) < (beta : ℝ) := by
    exact_mod_cast (lt_trans (by norm_num : (0 : Int) < 1) hβ)
  have hreal : (p : ℝ) < (q : ℝ) :=
    lt_of_mul_lt_mul_right hlt (le_of_lt (zpow_pos hβpos r))
  exact_mod_cast hreal

-- Coq: `oneExp_le` — with mantissa 1, exponent order preserves real ≤
noncomputable def oneExp_le_check {beta : Int} [ValidRadix beta] (x y : Int) : Unit :=
  ()

theorem oneExp_le {beta : Int} [ValidRadix beta] (x y : Int) :
    ⦃⌜1 < beta ∧ x ≤ y⌝⦄
    (pure (oneExp_le_check (beta:=beta) x y) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (⟨1, x⟩ : FloatSpec.Core.Defs.FlocqFloat beta)
            ≤ _root_.F2R (⟨1, y⟩ : FloatSpec.Core.Defs.FlocqFloat beta)⌝⦄ := by
  intro ⟨hβ, hxy⟩
  simp only [wp, PostCond.noThrow, pure, oneExp_le_check, Id.run,
    ULift.up_down]
  simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, FloatSpec.Core.Defs.FlocqFloat.Fnum,
    FloatSpec.Core.Defs.FlocqFloat.Fexp, Int.cast_one, one_mul]
  have hβR : (1 : ℝ) < (beta : ℝ) := by exact_mod_cast hβ
  exact (zpow_right_strictMono₀ hβR).monotone hxy

-- Coq: `oneExp_Zlt` — with mantissa 1, real < implies exponent <
noncomputable def oneExp_Zlt_check {beta : Int} [ValidRadix beta] (x y : Int) : Unit :=
  ()

theorem oneExp_Zlt {beta : Int} [ValidRadix beta] (x y : Int) :
    ⦃⌜1 < beta ∧ _root_.F2R (⟨1, x⟩ : FloatSpec.Core.Defs.FlocqFloat beta) <
         _root_.F2R (⟨1, y⟩ : FloatSpec.Core.Defs.FlocqFloat beta)⌝⦄
    (pure (oneExp_Zlt_check (beta:=beta) x y) : Id Unit)
    ⦃⇓_ => ⌜x < y⌝⦄ := by
  intro ⟨hβ, hlt⟩
  simp only [wp, PostCond.noThrow, pure, oneExp_Zlt_check, Id.run,
    ULift.up_down]
  simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, FloatSpec.Core.Defs.FlocqFloat.Fnum,
    FloatSpec.Core.Defs.FlocqFloat.Fexp, Int.cast_one, one_mul] at hlt
  have hβR : (1 : ℝ) < (beta : ℝ) := by exact_mod_cast hβ
  exact ((zpow_right_strictMono₀ hβR).lt_iff_lt).mp hlt

-- Coq: `Zle_powerRZ` — 1 < e → e^n ≤ e^m → n ≤ m
noncomputable def Zle_powerRZ_check (e : ℝ) (n m : Int) : Unit :=
  ()

theorem Zle_powerRZ (e : ℝ) (n m : Int) :
    ⦃⌜1 < e ∧ e ^ n ≤ e ^ m⌝⦄
    (pure (Zle_powerRZ_check e n m) : Id Unit)
    ⦃⇓_ => ⌜n ≤ m⌝⦄ := by
  intro ⟨he, hle⟩
  simp only [wp, PostCond.noThrow, pure, Zle_powerRZ_check, Id.run,
    ULift.up_down]
  show n ≤ m
  exact (zpow_right_strictMono₀ he).le_iff_le.mp hle

-- Coq: `Rinv_powerRZ` — (/ (e^n)) = e^(-n) for nonzero base (integer exponent)
noncomputable def Rinv_powerRZ_check (e : ℝ) (n : Int) : Unit :=
  ()

theorem Rinv_powerRZ (e : ℝ) (n : Int) :
    ⦃⌜e ≠ 0⌝⦄
    (pure (Rinv_powerRZ_check e n) : Id Unit)
    ⦃⇓_ => ⌜(e ^ n)⁻¹ = e ^ (-n)⌝⦄ := by
  intro he
  simp only [wp, PostCond.noThrow, pure, Rinv_powerRZ_check, Id.run,
    ULift.up_down]
  show (e ^ n)⁻¹ = e ^ (-n)
  rw [zpow_neg e n]

-- Coq: `Rledouble` — if 0 ≤ r then r ≤ 2r
noncomputable def Rledouble_check (r : ℝ) : Unit :=
  ()

theorem Rledouble (r : ℝ) :
    ⦃⌜0 ≤ r⌝⦄
    (pure (Rledouble_check r) : Id Unit)
    ⦃⇓_ => ⌜r ≤ 2 * r⌝⦄ := by
  intro hr
  simp only [wp, PostCond.noThrow, pure, Rledouble_check, Id.run,
    ULift.up_down]
  show r ≤ 2 * r
  have hr' : 0 ≤ r := hr
  linarith

-- Coq: `Rltdouble` — if 0 < r then r < 2r
noncomputable def Rltdouble_check (r : ℝ) : Unit :=
  ()

theorem Rltdouble (r : ℝ) :
    ⦃⌜0 < r⌝⦄
    (pure (Rltdouble_check r) : Id Unit)
    ⦃⇓_ => ⌜r < 2 * r⌝⦄ := by
  intro hr
  simp only [wp, PostCond.noThrow, pure, Rltdouble_check, Id.run,
    ULift.up_down]
  show r < 2 * r
  have hr' : 0 < r := hr
  linarith

-- Coq: `powerRZ_NOR` — e^n ≠ 0 when e ≠ 0 (integer exponent)
noncomputable def powerRZ_NOR_check (e : ℝ) (n : Int) : Unit :=
  ()

theorem powerRZ_NOR (e : ℝ) (n : Int) :
    ⦃⌜e ≠ 0⌝⦄
    (pure (powerRZ_NOR_check e n) : Id Unit)
    ⦃⇓_ => ⌜e ^ n ≠ 0⌝⦄ := by
  intro he
  simp only [wp, PostCond.noThrow, pure, powerRZ_NOR_check, Id.run,
    ULift.up_down]
  show e ^ n ≠ 0
  exact zpow_ne_zero n he

-- Coq: `Rle_Rinv` — monotonicity of inverse on (0, ∞)
noncomputable def Rle_Rinv_check (x y : ℝ) : Unit :=
  ()

theorem Rle_Rinv (x y : ℝ) :
    ⦃⌜0 < x ∧ x ≤ y⌝⦄
    (pure (Rle_Rinv_check x y) : Id Unit)
    ⦃⇓_ => ⌜y⁻¹ ≤ x⁻¹⌝⦄ := by
  intro ⟨hx, hxy⟩
  simp only [wp, PostCond.noThrow, pure, Rle_Rinv_check, Id.run,
    ULift.up_down]
  show y⁻¹ ≤ x⁻¹
  exact inv_anti₀ hx hxy

-- Hoare-style wrapper for `min_or`
noncomputable def min_or_check (n m : Nat) : Unit :=
  ()

theorem min_or (n m : Nat) :
    ⦃⌜True⌝⦄
    (pure (min_or_check n m) : Id Unit)
    ⦃⇓_ => ⌜(Nat.min n m = n ∧ n ≤ m) ∨ (Nat.min n m = m ∧ m < n)⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, min_or_check, Id.run,
    ULift.up_down]
  show (Nat.min n m = n ∧ n ≤ m) ∨ (Nat.min n m = m ∧ m < n)
  by_cases h : n ≤ m
  · left; exact ⟨Nat.min_eq_left h, h⟩
  · right; push Not at h; exact ⟨Nat.min_eq_right (Nat.le_of_lt h), h⟩

-- Coq: `Zmax`
def Zmax (a b : Int) : Int :=
  max a b

-- Coq: `ZmaxSym` — symmetry of integer max
noncomputable def ZmaxSym_check (a b : Int) : Unit :=
  ()

theorem ZmaxSym (a b : Int) :
    ⦃⌜True⌝⦄
    (pure (ZmaxSym_check a b) : Id Unit)
    ⦃⇓_ => ⌜max a b = max b a⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, ZmaxSym_check, Id.run,
    ULift.up_down]
  show max a b = max b a
  exact max_comm a b

-- Coq: `ZmaxLe1` — left argument ≤ max
noncomputable def ZmaxLe1_check (a b : Int) : Unit :=
  ()

theorem ZmaxLe1 (a b : Int) :
    ⦃⌜True⌝⦄
    (pure (ZmaxLe1_check a b) : Id Unit)
    ⦃⇓_ => ⌜a ≤ max a b⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, ZmaxLe1_check, Id.run,
    ULift.up_down]
  show a ≤ max a b
  exact le_max_left a b

-- Coq: `ZmaxLe2` — right argument ≤ max
noncomputable def ZmaxLe2_check (a b : Int) : Unit :=
  ()

theorem ZmaxLe2 (a b : Int) :
    ⦃⌜True⌝⦄
    (pure (ZmaxLe2_check a b) : Id Unit)
    ⦃⇓_ => ⌜b ≤ max a b⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, ZmaxLe2_check, Id.run,
    ULift.up_down]
  show b ≤ max a b
  exact le_max_right a b

noncomputable def ZleLe_check (x y : Nat) : Unit :=
  ()

theorem ZleLe (x y : Nat) :
    ⦃⌜(Int.ofNat x ≤ Int.ofNat y)⌝⦄
    (pure (ZleLe_check x y) : Id Unit)
    ⦃⇓_ => ⌜x ≤ y⌝⦄ := by
  intro h
  simp only [wp, PostCond.noThrow, pure, ZleLe_check, Id.run,
    ULift.up_down]
  show x ≤ y
  exact Int.ofNat_le.mp h

-- Coq: `Zlt_Zopp` — negate flips strict inequality
noncomputable def Zlt_Zopp_check (x y : Int) : Unit :=
  ()

theorem Zlt_Zopp (x y : Int) :
    ⦃⌜x < y⌝⦄
    (pure (Zlt_Zopp_check x y) : Id Unit)
    ⦃⇓_ => ⌜-y < -x⌝⦄ := by
  intro h
  simp only [wp, PostCond.noThrow, pure, Zlt_Zopp_check, Id.run,
    ULift.up_down]
  show -y < -x
  exact Int.neg_lt_neg h

-- Coq: `Zle_Zopp` — negate flips non-strict inequality
noncomputable def Zle_Zopp_check (x y : Int) : Unit :=
  ()

theorem Zle_Zopp (x y : Int) :
    ⦃⌜x ≤ y⌝⦄
    (pure (Zle_Zopp_check x y) : Id Unit)
    ⦃⇓_ => ⌜-y ≤ -x⌝⦄ := by
  intro h
  simp only [wp, PostCond.noThrow, pure, Zle_Zopp_check, Id.run,
    ULift.up_down]
  show -y ≤ -x
  exact Int.neg_le_neg h

-- Coq: `Zabs_absolu` — absolute value equals natAbs cast
noncomputable def Zabs_absolu_check (z : Int) : Unit :=
  ()

theorem Zabs_absolu (z : Int) :
    ⦃⌜True⌝⦄
    (pure (Zabs_absolu_check z) : Id Unit)
    ⦃⇓_ => ⌜|z| = Int.ofNat (Int.natAbs z)⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, Zabs_absolu_check, Id.run,
    ULift.up_down]
  show |z| = Int.ofNat (Int.natAbs z)
  exact Int.abs_eq_natAbs z

-- Coq: `Zpower_nat_O` — any base to 0 is 1
noncomputable def Zpower_nat_O_check (z : Int) : Unit :=
  ()

theorem Zpower_nat_O (z : Int) :
    ⦃⌜True⌝⦄
    (pure (Zpower_nat_O_check z) : Id Unit)
    ⦃⇓_ => ⌜z^0 = (1 : Int)⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, Zpower_nat_O_check, Id.run,
    ULift.up_down]
  show z ^ 0 = (1 : Int)
  exact pow_zero z

-- Coq: `Zpower_nat_1` — any base to 1 is itself
noncomputable def Zpower_nat_1_check (z : Int) : Unit :=
  ()

theorem Zpower_nat_1 (z : Int) :
    ⦃⌜True⌝⦄
    (pure (Zpower_nat_1_check z) : Id Unit)
    ⦃⇓_ => ⌜z^1 = z⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, Zpower_nat_1_check, Id.run,
    ULift.up_down]
  show z ^ 1 = z
  exact pow_one z

-- Coq: `Zmin_Zmax` — min is always ≤ max
noncomputable def Zmin_Zmax_check (z1 z2 : Int) : Unit :=
  ()

theorem Zmin_Zmax (z1 z2 : Int) :
    ⦃⌜True⌝⦄
    (pure (Zmin_Zmax_check z1 z2) : Id Unit)
    ⦃⇓_ => ⌜min z1 z2 ≤ max z1 z2⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, Zmin_Zmax_check, Id.run,
    ULift.up_down]
  show min z1 z2 ≤ max z1 z2
  exact min_le_max

-- Coq: `Zeq_Zs` — if p ≤ q < succ p, then p = q
noncomputable def Zeq_Zs_check (p q : Int) : Unit :=
  ()

theorem Zeq_Zs (p q : Int) :
    ⦃⌜p ≤ q ∧ q < Int.succ p⌝⦄
    (pure (Zeq_Zs_check p q) : Id Unit)
    ⦃⇓_ => ⌜p = q⌝⦄ := by
  intro ⟨hle, hlt⟩
  simp only [wp, PostCond.noThrow, pure, Zeq_Zs_check, Id.run,
    ULift.up_down]
  show p = q
  simp only [Int.succ] at hlt; omega

-- Coq: `Zopp_Zpred_Zs` — negation distributes over predecessor/successor
noncomputable def Zopp_Zpred_Zs_check (z : Int) : Unit :=
  ()

theorem Zopp_Zpred_Zs (z : Int) :
    ⦃⌜True⌝⦄
    (pure (Zopp_Zpred_Zs_check z) : Id Unit)
    ⦃⇓_ => ⌜-(Int.pred z) = Int.succ (-z)⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, Zopp_Zpred_Zs_check, Id.run,
    ULift.up_down]
  show -(Int.pred z) = Int.succ (-z)
  simp only [Int.pred, Int.succ]; omega

-- Coq: `Zmin_Zle` — lower bound is ≤ minimum of two bounds
noncomputable def Zmin_Zle_check (z1 z2 z3 : Int) : Unit :=
  ()

theorem Zmin_Zle (z1 z2 z3 : Int) :
    ⦃⌜z1 ≤ z2 ∧ z1 ≤ z3⌝⦄
    (pure (Zmin_Zle_check z1 z2 z3) : Id Unit)
    ⦃⇓_ => ⌜z1 ≤ min z2 z3⌝⦄ := by
  intro ⟨h2, h3⟩
  simp only [wp, PostCond.noThrow, pure, Zmin_Zle_check, Id.run,
    ULift.up_down]
  show z1 ≤ min z2 z3
  exact le_min h2 h3

-- Coq: `Zmin_Zlt` — if z1 < z2 and z1 < z3 then z1 < min z2 z3
noncomputable def Zmin_Zlt_check (z1 z2 z3 : Int) : Unit :=
  ()

theorem Zmin_Zlt (z1 z2 z3 : Int) :
    ⦃⌜z1 < z2 ∧ z1 < z3⌝⦄
    (pure (Zmin_Zlt_check z1 z2 z3) : Id Unit)
    ⦃⇓_ => ⌜z1 < min z2 z3⌝⦄ := by
  intro ⟨h2, h3⟩
  simp only [wp, PostCond.noThrow, pure, Zmin_Zlt_check, Id.run,
    ULift.up_down]
  show z1 < min z2 z3
  exact lt_min h2 h3

-- Coq: `Zpred_Zopp_Zs` — predecessor of negation equals negation of successor
noncomputable def Zpred_Zopp_Zs_check (z : Int) : Unit :=
  ()

theorem Zpred_Zopp_Zs (z : Int) :
    ⦃⌜True⌝⦄
    (pure (Zpred_Zopp_Zs_check z) : Id Unit)
    ⦃⇓_ => ⌜Int.pred (-z) = -(Int.succ z)⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, Zpred_Zopp_Zs_check, Id.run,
    ULift.up_down]
  show Int.pred (-z) = -(Int.succ z)
  simp only [Int.pred, Int.succ]; omega

-- Coq: `Zle_Zmult_comp_r` — multiply on the right preserves ≤ for nonnegative multiplier
noncomputable def Zle_Zmult_comp_r_check (x y z : Int) : Unit :=
  ()

theorem Zle_Zmult_comp_r (x y z : Int) :
    ⦃⌜0 ≤ z ∧ x ≤ y⌝⦄
    (pure (Zle_Zmult_comp_r_check x y z) : Id Unit)
    ⦃⇓_ => ⌜x * z ≤ y * z⌝⦄ := by
  intro ⟨hz, hxy⟩
  simp only [wp, PostCond.noThrow, pure, Zle_Zmult_comp_r_check, Id.run,
    ULift.up_down]
  show x * z ≤ y * z
  exact Int.mul_le_mul_of_nonneg_right hxy hz

-- Coq: `Zle_Zmult_comp_l` — multiply on the left preserves ≤ for nonnegative multiplier
noncomputable def Zle_Zmult_comp_l_check (x y z : Int) : Unit :=
  ()

theorem Zle_Zmult_comp_l (x y z : Int) :
    ⦃⌜0 ≤ z ∧ x ≤ y⌝⦄
    (pure (Zle_Zmult_comp_l_check x y z) : Id Unit)
    ⦃⇓_ => ⌜z * x ≤ z * y⌝⦄ := by
  intro ⟨hz, hxy⟩
  simp only [wp, PostCond.noThrow, pure, Zle_Zmult_comp_l_check, Id.run,
    ULift.up_down]
  show z * x ≤ z * y
  exact Int.mul_le_mul_of_nonneg_left hxy hz

-- Coq: `absolu_Zs` — natAbs of succ increments under nonnegativity
noncomputable def absolu_Zs_check (z : Int) : Unit :=
  ()

theorem absolu_Zs (z : Int) :
    ⦃⌜0 ≤ z⌝⦄
    (pure (absolu_Zs_check z) : Id Unit)
    ⦃⇓_ => ⌜Int.natAbs (Int.succ z) = Nat.succ (Int.natAbs z)⌝⦄ := by
  intro hz
  simp only [wp, PostCond.noThrow, pure, absolu_Zs_check, Id.run,
    ULift.up_down]
  show Int.natAbs (Int.succ z) = Nat.succ (Int.natAbs z)
  have hz' : 0 ≤ z := hz
  simp only [Int.succ]
  obtain ⟨n, rfl⟩ := Int.eq_ofNat_of_zero_le hz'
  show Int.natAbs (↑n + 1) = n + 1
  rw [show (↑n + 1 : ℤ) = ↑(n + 1) from by push_cast; ring]
  simp [Int.natAbs]

-- Coq: `Zlt_next` — either m = succ n or succ n < m when n < m
noncomputable def Zlt_next_check (n m : Int) : Unit :=
  ()

theorem Zlt_next (n m : Int) :
    ⦃⌜n < m⌝⦄
    (pure (Zlt_next_check n m) : Id Unit)
    ⦃⇓_ => ⌜m = Int.succ n ∨ Int.succ n < m⌝⦄ := by
  intro h
  simp only [wp, PostCond.noThrow, pure, Zlt_next_check, Id.run,
    ULift.up_down]
  show m = Int.succ n ∨ Int.succ n < m
  have h' : n < m := h
  simp only [Int.succ]
  omega

-- Coq: `Zle_next` — either m = n or succ n ≤ m when n ≤ m
noncomputable def Zle_next_check (n m : Int) : Unit :=
  ()

theorem Zle_next (n m : Int) :
    ⦃⌜n ≤ m⌝⦄
    (pure (Zle_next_check n m) : Id Unit)
    ⦃⇓_ => ⌜m = n ∨ Int.succ n ≤ m⌝⦄ := by
  intro h
  simp only [wp, PostCond.noThrow, pure, Zle_next_check, Id.run,
    ULift.up_down]
  show m = n ∨ Int.succ n ≤ m
  have h' : n ≤ m := h
  simp only [Int.succ]
  omega

-- Coq: `inj_pred` — Z_of_nat (pred n) = Z.pred (Z_of_nat n) for n ≠ 0
noncomputable def inj_pred_check (n : Nat) : Unit :=
  ()

theorem inj_pred (n : Nat) :
    ⦃⌜n ≠ 0⌝⦄
    (pure (inj_pred_check n) : Id Unit)
    ⦃⇓_ => ⌜Int.ofNat (Nat.pred n) = Int.pred (Int.ofNat n)⌝⦄ := by
  intro hn
  simp only [wp, PostCond.noThrow, pure, inj_pred_check, Id.run,
    ULift.up_down]
  show Int.ofNat (Nat.pred n) = Int.pred (Int.ofNat n)
  simp only [Int.pred]
  cases n with
  | zero => contradiction
  | succ k => simp [Nat.pred]

-- Coq: `Zle_abs` — p ≤ Z_of_nat (Z.abs_nat p)
noncomputable def Zle_abs_check (p : Int) : Unit :=
  ()

theorem Zle_abs (p : Int) :
    ⦃⌜True⌝⦄
    (pure (Zle_abs_check p) : Id Unit)
    ⦃⇓_ => ⌜p ≤ Int.ofNat (Int.natAbs p)⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, Zle_abs_check, Id.run,
    ULift.up_down]
  show p ≤ Int.ofNat (Int.natAbs p)
  exact Int.le_natAbs

-- Coq: `inj_abs` — if 0 ≤ x then Z_of_nat (Z.abs_nat x) = x
noncomputable def inj_abs_check (x : Int) : Unit :=
  ()

theorem inj_abs (x : Int) :
    ⦃⌜0 ≤ x⌝⦄
    (pure (inj_abs_check x) : Id Unit)
    ⦃⇓_ => ⌜Int.ofNat (Int.natAbs x) = x⌝⦄ := by
  intro hx
  simp only [wp, PostCond.noThrow, pure, inj_abs_check, Id.run,
    ULift.up_down]
  show Int.ofNat (Int.natAbs x) = x
  exact Int.natAbs_of_nonneg hx

-- Coq `positive` compatibility and `nat_of_P`
structure Positive where
  val : Nat

def nat_of_P (p : Positive) : Nat :=
  p.val.succ

-- Coq's binary `positive` constructors over the predecessor encoding. They let
-- `Pdiv` below follow Coq's structural recursion on `xH`/`xO`/`xI` directly.

/-- Coq `xH`: the positive one. -/
@[flocq_local "Rocq positive constructor xH over the predecessor-encoded Pff positive"]
def Positive.xH : Positive := ⟨0⟩

/-- Coq `xO p`: the positive `2 * p`. -/
@[flocq_local "Rocq positive constructor xO over the predecessor-encoded Pff positive"]
def Positive.xO (p : Positive) : Positive := ⟨2 * p.val + 1⟩

/-- Coq `xI p`: the positive `2 * p + 1`. -/
@[flocq_local "Rocq positive constructor xI over the predecessor-encoded Pff positive"]
def Positive.xI (p : Positive) : Positive := ⟨2 * p.val + 2⟩

/-- The Coq constructor that builds a positive: `xH`, `xO p` or `xI p`. -/
inductive PositiveView where
  | xH
  | xO (p : Positive)
  | xI (p : Positive)

/-- Read a predecessor-encoded positive as Coq's binary `positive`: predecessor
`0` is `xH`, `2k + 1` is `xO ⟨k⟩` and `2k + 2` is `xI ⟨k⟩`. -/
@[flocq_local "Constructor view of the predecessor-encoded Pff positive"]
def Positive.view : Positive → PositiveView
  | ⟨0⟩ => .xH
  | ⟨n + 1⟩ =>
      match n % 2 with
      | 0 => .xO ⟨n / 2⟩
      | _ + 1 => .xI ⟨n / 2⟩

private theorem Positive.view_xO_lt {p p' : Positive} (h : p.view = .xO p') :
    p'.val < p.val := by
  match p, h with
  | ⟨n + 1⟩, h =>
    dsimp only [Positive.view] at h
    generalize n % 2 = r at h
    cases r with
    | zero => cases h; exact Nat.lt_succ_of_le (Nat.div_le_self n 2)
    | succ => exact PositiveView.noConfusion h

private theorem Positive.view_xI_lt {p p' : Positive} (h : p.view = .xI p') :
    p'.val < p.val := by
  match p, h with
  | ⟨n + 1⟩, h =>
    dsimp only [Positive.view] at h
    generalize n % 2 = r at h
    cases r with
    | zero => exact PositiveView.noConfusion h
    | succ => cases h; exact Nat.lt_succ_of_le (Nat.div_le_self n 2)

private theorem Positive.view_eq_xH_iff {p : Positive} :
    p.view = .xH ↔ nat_of_P p = 1 := by
  rcases p with ⟨_ | n⟩
  · simp [Positive.view, nat_of_P]
  · simp only [Positive.view, nat_of_P]
    split <;> simp

private theorem Positive.view_eq_xO_iff {p p' : Positive} :
    p.view = .xO p' ↔ nat_of_P p = 2 * nat_of_P p' := by
  rcases p with ⟨_ | n⟩ <;> rcases p' with ⟨m⟩
  · simp [Positive.view, nat_of_P]
    omega
  · simp only [Positive.view, nat_of_P]
    split <;> simp <;> omega

private theorem Positive.view_eq_xI_iff {p p' : Positive} :
    p.view = .xI p' ↔ nat_of_P p = 2 * nat_of_P p' + 1 := by
  rcases p with ⟨_ | n⟩ <;> rcases p' with ⟨m⟩
  · simp [Positive.view, nat_of_P]
  · simp only [Positive.view, nat_of_P]
    split <;> simp <;> omega

-- ---------------------------------------------------------------------------
-- Coq: Pdiv and its correctness properties over positive numbers

-- Optional-positive to Nat (Coq oZ)
@[flocq_source "src/Pff/Pff.v" 5392 "oZ"]
def oZ (h : Option Positive) : Nat :=
  match h with
  | none => 0
  | some p => nat_of_P p

def nat_to_positive_option : Nat → Option Positive
  | 0 => none
  | n + 1 => some ⟨n⟩

theorem oZ_nat_to_positive_option (n : Nat) :
    oZ (nat_to_positive_option n) = n := by
  cases n <;> rfl

private theorem nat_to_positive_option_oZ (x : Option Positive) :
    nat_to_positive_option (oZ x) = x := by
  rcases x with _ | ⟨n⟩ <;> rfl

/-- Natural-number characterization of `Pdiv`: quotient and remainder by `/`
and `%`. `Pdiv_eq_PdivNat` proves the source-shaped `Pdiv` equal to it. No
`csimp` substitutes it, so compiled code, the kernel and `#reduce` all run the
transcribed `Pdiv`. -/
@[flocq_local "Natural-division characterization proved equal to Pdiv; not a runtime override"]
def PdivNat (p q : Positive) : Option Positive × Option Positive :=
  (nat_to_positive_option (nat_of_P p / nat_of_P q),
    nat_to_positive_option (nat_of_P p % nat_of_P q))

-- Coq: Pdiv — division with remainder on positives, returning quotient/remainder.
-- The body transcribes Coq's `Fixpoint` on `xH`/`xO`/`xI`. In each `Z` match,
-- `Int.ofNat 0`, `Int.ofNat (r' + 1)` and `Int.negSucc _` are Coq's `Z0`,
-- `Zpos ⟨r'⟩` and `Zneg _`, and `Int.ofNat (nat_of_P q)` is `Zpos q`.
@[flocq_source "src/Pff/Pff.v" 5337 "Pdiv"]
def Pdiv (p q : Positive) : Option Positive × Option Positive :=
  match _hp : p.view with
  | .xH =>
      match q.view with
      | .xH => (some .xH, none)
      | .xO _ => (none, some p)
      | .xI _ => (none, some p)
  | .xI p' =>
      match Pdiv p' q with
      | (none, none) =>
          match 1 - Int.ofNat (nat_of_P q) with
          | .ofNat 0 => (some .xH, none)
          | .ofNat (r' + 1) => (some .xH, some ⟨r'⟩)
          | .negSucc _ => (none, some .xH)
      | (none, some r1) =>
          match Int.ofNat (nat_of_P (.xI r1)) - Int.ofNat (nat_of_P q) with
          | .ofNat 0 => (some .xH, none)
          | .ofNat (r' + 1) => (some .xH, some ⟨r'⟩)
          | .negSucc _ => (none, some (.xI r1))
      | (some q1, none) =>
          match 1 - Int.ofNat (nat_of_P q) with
          | .ofNat 0 => (some (.xI q1), none)
          | .ofNat (r' + 1) => (some (.xI q1), some ⟨r'⟩)
          | .negSucc _ => (some (.xO q1), some .xH)
      | (some q1, some r1) =>
          match Int.ofNat (nat_of_P (.xI r1)) - Int.ofNat (nat_of_P q) with
          | .ofNat 0 => (some (.xI q1), none)
          | .ofNat (r' + 1) => (some (.xI q1), some ⟨r'⟩)
          | .negSucc _ => (some (.xO q1), some (.xI r1))
  | .xO p' =>
      match Pdiv p' q with
      | (none, none) => (none, none)
      | (none, some r1) =>
          match Int.ofNat (nat_of_P (.xO r1)) - Int.ofNat (nat_of_P q) with
          | .ofNat 0 => (some .xH, none)
          | .ofNat (r' + 1) => (some .xH, some ⟨r'⟩)
          | .negSucc _ => (none, some (.xO r1))
      | (some q1, none) => (some (.xO q1), none)
      | (some q1, some r1) =>
          match Int.ofNat (nat_of_P (.xO r1)) - Int.ofNat (nat_of_P q) with
          | .ofNat 0 => (some (.xI q1), none)
          | .ofNat (r' + 1) => (some (.xI q1), some ⟨r'⟩)
          | .negSucc _ => (some (.xO q1), some (.xO r1))
termination_by p.val
decreasing_by
  · exact Positive.view_xI_lt _hp
  · exact Positive.view_xO_lt _hp

/-- Coq `Pdiv_correct`: `Pdiv` returns quotient and remainder, with the
remainder below the divisor. Proved as Coq does, by induction on `p` through
each branch of `Pdiv`. -/
@[flocq_source "src/Pff/Pff.v" 5397 "Pdiv_correct"]
theorem Pdiv_correct (p q : Positive) :
    nat_of_P p = oZ (Prod.fst (Pdiv p q)) * nat_of_P q + oZ (Prod.snd (Pdiv p q)) ∧
      oZ (Prod.snd (Pdiv p q)) < nat_of_P q := by
  fun_induction Pdiv p q <;>
    (try simp only [‹Pdiv _ _ = _›] at *) <;>
    simp only [Positive.view_eq_xH_iff, Positive.view_eq_xO_iff, Positive.view_eq_xI_iff,
      Int.ofNat_eq_natCast, oZ, nat_of_P, Positive.xH, Positive.xO, Positive.xI,
      Nat.succ_eq_add_one, Nat.add_mul, Nat.mul_add, Nat.mul_assoc, Nat.one_mul,
      Nat.mul_one, Nat.zero_mul] at * <;>
    omega

/-- The source-shaped `Pdiv` computes natural quotient and remainder. -/
theorem Pdiv_eq_PdivNat : @Pdiv = @PdivNat := by
  funext p q
  obtain ⟨hdecomp, hlt⟩ := Pdiv_correct p q
  obtain ⟨hquot, hrem⟩ := (Nat.div_mod_unique (a := nat_of_P p) (b := nat_of_P q)
    (d := oZ (Pdiv p q).1) (c := oZ (Pdiv p q).2) (Nat.succ_pos q.val)).mpr
    ⟨by rw [hdecomp, Nat.mul_comm, Nat.add_comm], hlt⟩
  rw [PdivNat, hquot, hrem, nat_to_positive_option_oZ, nat_to_positive_option_oZ]

-- Correctness of Pdiv (quotient-remainder form and remainder bound)
noncomputable def Pdiv_correct_check (p q : Positive) : Unit :=
  ()

/-- Legacy Hoare compatibility form of `Pdiv_correct`. -/
theorem Pdiv_correct_spec (p q : Positive) :
    ⦃⌜True⌝⦄
    (pure (Pdiv_correct_check p q) : Id Unit)
    ⦃⇓_ => ⌜nat_of_P p = oZ (Prod.fst (Pdiv p q)) * nat_of_P q + oZ (Prod.snd (Pdiv p q)) ∧
            oZ (Prod.snd (Pdiv p q)) < nat_of_P q⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, Pdiv_correct_check, Id.run,
    ULift.up_down]
  exact Pdiv_correct p q

-- Bridge Option Positive to Int (Coq oZ1)
@[flocq_source "src/Pff/Pff.v" 5594 "oZ1"]
def oZ1 (h : Option Positive) : Int :=
  match h with
  | none => 0
  | some p => Int.ofNat (nat_of_P p)

-- Coq: inj_oZ1 — Int/nat bridge for oZ/oZ1
noncomputable def inj_oZ1_check (z : Option Positive) : Unit :=
  ()

theorem inj_oZ1 (z : Option Positive) :
    ⦃⌜True⌝⦄
    (pure (inj_oZ1_check z) : Id Unit)
    ⦃⇓_ => ⌜oZ1 z = Int.ofNat (oZ z)⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, inj_oZ1_check, Id.run,
    ULift.up_down]
  show oZ1 z = Int.ofNat (oZ z)
  cases z with
  | none => simp [oZ1, oZ]
  | some p => simp [oZ1, oZ]

private theorem oZ1_nat_to_positive_option (n : Nat) :
    oZ1 (nat_to_positive_option n) = n := by
  cases n <;> rfl

-- Coq: Zquotient — integer quotient using positive division on magnitudes.
-- `Int.ofNat (x + 1)` and `Int.negSucc x` are Coq's `Zpos ⟨x⟩` and `Zneg ⟨x⟩`.
@[flocq_source "src/Pff/Pff.v" 5601 "Zquotient"]
def Zquotient (n m : Int) : Int :=
  match n, m with
  | .ofNat 0, _ => 0
  | _, .ofNat 0 => 0
  | .ofNat (x + 1), .ofNat (y + 1) => match Pdiv ⟨x⟩ ⟨y⟩ with | (x, _) => oZ1 x
  | .negSucc x, .negSucc y => match Pdiv ⟨x⟩ ⟨y⟩ with | (x, _) => oZ1 x
  | .ofNat (x + 1), .negSucc y => match Pdiv ⟨x⟩ ⟨y⟩ with | (x, _) => -oZ1 x
  | .negSucc x, .ofNat (y + 1) => match Pdiv ⟨x⟩ ⟨y⟩ with | (x, _) => -oZ1 x

/-- `Zquotient` truncates toward zero: it is Lean's `Int.tdiv`. -/
theorem Zquotient_eq_tdiv (n m : Int) : Zquotient n m = n.tdiv m := by
  rcases n with (_ | x) | x <;> rcases m with (_ | y) | y <;>
    simp [Zquotient, Pdiv_eq_PdivNat, PdivNat, oZ1_nat_to_positive_option, nat_of_P,
      Int.tdiv]

-- Coq: `ZquotientProp` — decomposition m = (Zquotient m n) * n + r with bounds
noncomputable def ZquotientProp_check (m n : Int) : Unit :=
  ()

theorem ZquotientProp (m n : Int) :
    ⦃⌜n ≠ 0⌝⦄
    (pure (ZquotientProp_check m n) : Id Unit)
    ⦃⇓_ => ⌜∃ r : Int,
            m = Zquotient m n * n + r ∧
            |Zquotient m n * n| ≤ |m| ∧
            |r| < |n|⌝⦄ := by
  intro hn
  simp only [wp, PostCond.noThrow, pure, ZquotientProp_check, Id.run]
  refine ⟨m.tmod n, ?_, ?_, ?_⟩
  · rw [Zquotient_eq_tdiv]
    calc
      m = n * m.tdiv n + m.tmod n := (Int.mul_tdiv_add_tmod m n).symm
      _ = m.tdiv n * n + m.tmod n := by ring
  · rw [Zquotient_eq_tdiv]
    rw [Int.abs_eq_natAbs, Int.abs_eq_natAbs]
    exact_mod_cast (by
      rw [Int.natAbs_mul, Int.natAbs_tdiv]
      exact Nat.div_mul_le_self m.natAbs n.natAbs)
  · rw [Int.abs_eq_natAbs, Int.abs_eq_natAbs]
    exact_mod_cast (by
      rw [Int.natAbs_tmod]
      exact Nat.mod_lt m.natAbs (Int.natAbs_pos.mpr hn))

-- Coq: Zdivides — m divides n means n = m * q (note Coq's argument order)
noncomputable def Zdivides (n m : Int) : Prop := ∃ q : Int, n = m * q

-- Coq: `ZdividesZquotient` — if m divides n and m ≠ 0 then
-- n = (Zquotient n m) * m
noncomputable def ZdividesZquotient_check (n m : Int) : Unit :=
  ()

theorem ZdividesZquotient (n m : Int) :
    ⦃⌜m ≠ 0 ∧ Zdivides n m⌝⦄
    (pure (ZdividesZquotient_check n m) : Id Unit)
    ⦃⇓_ => ⌜n = Zquotient n m * m⌝⦄ := by
  intro ⟨hm, q, hq⟩
  simp only [wp, PostCond.noThrow, pure, ZdividesZquotient_check, Id.run,
    ULift.up_down]
  show n = Zquotient n m * m
  subst n
  rw [Zquotient_eq_tdiv]
  exact (Int.tdiv_mul_cancel (show m ∣ m * q from ⟨q, rfl⟩)).symm

-- Coq: `ZdividesZquotientInv` — from decomposition n = (Zquotient n m) * m, deduce divisibility
noncomputable def ZdividesZquotientInv_check (n m : Int) : Unit :=
  ()

theorem ZdividesZquotientInv (n m : Int) :
    ⦃⌜n = Zquotient n m * m⌝⦄
    (pure (ZdividesZquotientInv_check n m) : Id Unit)
    ⦃⇓_ => ⌜Zdivides n m⌝⦄ := by
  intro h
  simp only [wp, PostCond.noThrow, pure, ZdividesZquotientInv_check, Id.run,
    ULift.up_down]
  show Zdivides n m
  have h' : n = Zquotient n m * m := h
  unfold Zdivides
  refine ⟨Zquotient n m, ?_⟩
  linarith [mul_comm (Zquotient n m) m]

-- Coq: `ZdividesMult` — if m divides n then p*m divides p*n
noncomputable def ZdividesMult_check (n m p : Int) : Unit :=
  ()

theorem ZdividesMult (n m p : Int) :
    ⦃⌜Zdivides n m⌝⦄
    (pure (ZdividesMult_check n m p) : Id Unit)
    ⦃⇓_ => ⌜Zdivides (p * n) (p * m)⌝⦄ := by
  intro ⟨q, hq⟩
  simp only [wp, PostCond.noThrow, pure, ZdividesMult_check, Id.run,
    ULift.up_down]
  show Zdivides (p * n) (p * m)
  exact ⟨q, by rw [hq]; ring⟩

-- Coq: `Zeq_mult_simpl` — cancel a nonzero multiplier on both sides of equality
noncomputable def Zeq_mult_simpl_check (a b c : Int) : Unit :=
  ()

theorem Zeq_mult_simpl (a b c : Int) :
    ⦃⌜c ≠ 0 ∧ a * c = b * c⌝⦄
    (pure (Zeq_mult_simpl_check a b c) : Id Unit)
    ⦃⇓_ => ⌜a = b⌝⦄ := by
  intro ⟨hc, h⟩
  simp only [wp, PostCond.noThrow, pure, Zeq_mult_simpl_check, Id.run,
    ULift.up_down]
  show a = b
  exact mul_right_cancel₀ hc h

-- Coq: `ZdividesDiv` — if p ≠ 0 and p*m divides p*n, then m divides n
noncomputable def ZdividesDiv_check (n m p : Int) : Unit :=
  ()

theorem ZdividesDiv (n m p : Int) :
    ⦃⌜p ≠ 0 ∧ Zdivides (p * n) (p * m)⌝⦄
    (pure (ZdividesDiv_check n m p) : Id Unit)
    ⦃⇓_ => ⌜Zdivides n m⌝⦄ := by
  intro ⟨hp, q, hq⟩
  simp only [wp, PostCond.noThrow, pure, ZdividesDiv_check, Id.run,
    ULift.up_down]
  show Zdivides n m
  exact ⟨q, mul_left_cancel₀ hp (by rw [hq]; ring)⟩

-- Direct forms of `ZdividesZquotientInv` and `ZdividesZquotient`, used by
-- `ZdividesP` exactly where Coq's proof script applies those theorems.
private theorem Zdivides_of_Zquotient_mul_eq {n m : Int} (h : Zquotient n m * m = n) :
    Zdivides n m :=
  ⟨Zquotient n m, by rw [Int.mul_comm]; exact h.symm⟩

private theorem Zquotient_mul_eq_of_Zdivides {n m : Int} (h : Zdivides n m) :
    Zquotient n m * m = n := by
  obtain ⟨q, rfl⟩ := h
  rw [Zquotient_eq_tdiv]
  exact Int.tdiv_mul_cancel ⟨q, rfl⟩

-- Coq: `ZdividesP`. Coq cases on `m`: at `Z0` it cases on `n`; at `Zpos p` and
-- `Zneg p` it tests `Z_eq_bool (Zquotient n m * m) n`, here Lean's decidable `=`.
@[flocq_source "src/Pff/Pff.v" 5771 "ZdividesP"]
def ZdividesP (n m : Int) : Decidable (Zdivides n m) :=
  match m with
  | .ofNat 0 =>
      match n with
      | .ofNat 0 => isTrue ⟨0, rfl⟩
      | .ofNat (_ + 1) => isFalse fun ⟨_, h⟩ => by simp at h; omega
      | .negSucc _ => isFalse fun ⟨_, h⟩ => by simp at h
  | .ofNat (p + 1) =>
      if h : Zquotient n (.ofNat (p + 1)) * .ofNat (p + 1) = n then
        isTrue (Zdivides_of_Zquotient_mul_eq h)
      else
        isFalse fun hd => h (Zquotient_mul_eq_of_Zdivides hd)
  | .negSucc p =>
      if h : Zquotient n (.negSucc p) * .negSucc p = n then
        isTrue (Zdivides_of_Zquotient_mul_eq h)
      else
        isFalse fun hd => h (Zquotient_mul_eq_of_Zdivides hd)

-- Coq: `Zdivides1` — every integer divides 1
noncomputable def Zdivides1_check (m : Int) : Unit :=
  ()

theorem Zdivides1 (m : Int) :
    ⦃⌜True⌝⦄
    (pure (Zdivides1_check m) : Id Unit)
    ⦃⇓_ => ⌜Zdivides m 1⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, Zdivides1_check, Id.run,
    ULift.up_down]
  show Zdivides m 1
  exact ⟨m, by ring⟩

-- Coq: `ZDividesLe` — if n ≠ 0 and n divides m then |m| ≤ |n|
noncomputable def ZDividesLe_check (n m : Int) : Unit :=
  ()

/-- Coq: `ZDividesLe` — divisibility bounds the absolute value. -/
theorem ZDividesLe (n m : Int) :
    ⦃⌜n ≠ 0 ∧ Zdivides n m⌝⦄
    (pure (ZDividesLe_check n m) : Id Unit)
    ⦃⇓_ => ⌜|m| ≤ |n|⌝⦄ := by
  intro ⟨hn, q, hq⟩
  simp only [wp, PostCond.noThrow, pure, ZDividesLe_check, Id.run,
    ULift.up_down]
  show |m| ≤ |n|
  by_cases hm : m = 0
  · simp [hm, abs_nonneg]
  · have hq_ne : q ≠ 0 := by
      intro heq; rw [heq, mul_zero] at hq; exact hn hq
    rw [hq, abs_mul]
    calc |m| = |m| * 1 := (mul_one _).symm
    _ ≤ |m| * |q| := by
        apply mul_le_mul_of_nonneg_left (Int.one_le_abs hq_ne) (abs_nonneg m)

-- Coq: `digit` from the Pdigit section, including its exported total behavior.
def digit (n : Int) (q : Int) : Nat :=
  pffDigit n q

private lemma digit_neg (n p : Int) : digit n (-p) = digit n p := by
  exact pffDigit_neg n p

private lemma digit_abs_eq (n p : Int) : digit n (|p|) = digit n p := by
  exact pffDigit_abs n p

-- Context-specific helper for digit/precision lemmas translated from Coq.
noncomputable def digitPredVNumiSPrecision_check
    (radix : Int) (b : Fbound_skel) (precision : Nat) : Unit :=
  ()

/-- Coq: `digitPredVNumiSPrecision` — the digit of `pred (vNum b)` equals the precision
    when the bound's mantissa matches `radix^precision`. -/
theorem digitPredVNumiSPrecision
    (radix : Int) (b : Fbound_skel) (precision : Nat) :
    ⦃⌜precision ≠ 0 ∧ 1 < radix ∧ b.vNum = Zpower_nat radix precision⌝⦄
    (pure (digitPredVNumiSPrecision_check radix b precision) : Id Unit)
    ⦃⇓_ => ⌜digit radix (Int.pred b.vNum) = precision⌝⦄ := by
  intro h
  rcases h with ⟨hprecision, hradix, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, digitPredVNumiSPrecision_check, Id.run, ULift.up_down]
  unfold digit
  rw [pffDigit_of_one_lt radix (Int.pred b.vNum) hradix]
  have hpred_eq : Int.pred b.vNum = radix ^ precision - 1 := by
    rw [hvNum]
    simp only [Zpower_nat, Int.pred]
  have hpow_gt_one : (1 : Int) < radix ^ precision := by
    exact one_lt_pow₀ hradix hprecision
  have hpred_pos : 0 < Int.pred b.vNum := by
    rw [hpred_eq]
    omega
  have hpred_natAbs : ((Int.pred b.vNum).natAbs : Int) = radix ^ precision - 1 := by
    rw [hpred_eq]
    exact Int.natAbs_of_nonneg (by omega)
  have hprec_natAbs : ((precision : Int).natAbs) = precision := by
    simp
  have hprec_pred_natAbs : (((precision : Int) - 1).natAbs) = precision - 1 := by
    omega
  have hpow_low : radix ^ (precision - 1) ≤ radix ^ precision - 1 := by
    have hpow_pred_pos : (1 : Int) ≤ radix ^ (precision - 1) :=
      one_le_pow₀ (le_of_lt hradix)
    have hpow_succ : radix ^ precision = radix ^ (precision - 1) * radix := by
      have hprec : Nat.succ (precision - 1) = precision := by omega
      calc
        radix ^ precision = radix ^ Nat.succ (precision - 1) := by rw [hprec]
        _ = radix ^ (precision - 1) * radix := by rw [pow_succ]
    rw [hpow_succ]
    nlinarith
  have hpow_high : radix ^ precision - 1 < radix ^ precision := by
    omega
  have hzdigits :
      FloatSpec.Core.Digits.Zdigits radix (Int.pred b.vNum) = (precision : Int) := by
    have hpre :
        Int.pred b.vNum ≠ 0 ∧
        radix ^ (((precision : Int) - 1).natAbs) ≤ (Int.pred b.vNum).natAbs ∧
        (Int.pred b.vNum).natAbs < radix ^ ((precision : Int).natAbs) := by
      refine ⟨by omega, ?_, ?_⟩
      · change radix ^ (((precision : Int) - 1).natAbs) ≤
          (((Int.pred b.vNum).natAbs : Nat) : Int)
        rw [hprec_pred_natAbs, hpred_natAbs]
        exact hpow_low
      · change (((Int.pred b.vNum).natAbs : Nat) : Int) <
          radix ^ ((precision : Int).natAbs)
        rw [hprec_natAbs, hpred_natAbs]
        exact hpow_high
    have hunique := FloatSpec.Core.Digits.Zdigits_unique_from_nonzero_payload
      (beta := radix) (h_beta := hradix) (n := Int.pred b.vNum) (e := (precision : Int))
      (hβ := hradix) hpre
    simp only [wp, PostCond.noThrow, pure, Id.run] at hunique
    exact hunique
  rw [hzdigits]
  simp

noncomputable def digitVNumiSPrecision_check
    (radix : Int) (b : Fbound_skel) (precision : Nat) : Unit :=
  ()

/-- Coq: `digitVNumiSPrecision` — the digit of `vNum b` is `precision + 1`
    under the standard bound relationship. -/
theorem digitVNumiSPrecision
    (radix : Int) (b : Fbound_skel) (precision : Nat) :
    ⦃⌜precision ≠ 0 ∧ 1 < radix ∧ b.vNum = Zpower_nat radix precision⌝⦄
    (pure (digitVNumiSPrecision_check radix b precision) : Id Unit)
    ⦃⇓_ => ⌜digit radix b.vNum = Nat.succ precision⌝⦄ := by
  intro h
  rcases h with ⟨_hprecision, hradix, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, digitVNumiSPrecision_check, Id.run]
  unfold digit
  rw [pffDigit_of_one_lt radix b.vNum hradix]
  have hzdigits :
      FloatSpec.Core.Digits.Zdigits radix b.vNum = (precision : Int) + 1 := by
    rw [hvNum]
    unfold Zpower_nat
    exact
      (FloatSpec.Core.Digits.Zdigits_Zpower
        (beta := radix) (k := (precision : Int)) hradix)
        (by exact_mod_cast Nat.zero_le precision)
  rw [hzdigits]
  simp

noncomputable def pGivesDigit_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `pGivesDigit` — bounded floats have digit ≤ precision. -/
theorem pGivesDigit {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b p ∧ Fbounded' b p ∧
        precision ≠ 0 ∧ 1 < radix ∧ b.vNum = Zpower_nat radix precision⌝⦄
    (pure (pGivesDigit_check (beta:=beta) radix b precision p) : Id Unit)
    ⦃⇓_ => ⌜Fdigit (beta:=beta) radix p ≤ precision⌝⦄ := by
  intro h
  rcases h with ⟨_, hbounded, _hprecision, hradix, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, pGivesDigit_check, Id.run]
  unfold Fdigit
  rw [pffDigit_of_one_lt radix p.Fnum hradix]
  have hnum_lt : (p.Fnum.natAbs : Int) < radix ^ precision := by
    have hnum_bound : |p.Fnum| < b.vNum := hbounded.1
    rw [← Int.abs_eq_natAbs]
    rw [hvNum] at hnum_bound
    simpa [Zpower_nat] using hnum_bound
  have hdigits_int :
      FloatSpec.Core.Digits.Zdigits radix p.Fnum ≤ (precision : Int) := by
    have hpre :
        0 ≤ (precision : Int) ∧
          (p.Fnum.natAbs : Int) < radix ^ ((precision : Int).natAbs) := by
      refine ⟨by exact_mod_cast Nat.zero_le precision, ?_⟩
      simpa [Int.natAbs_of_nonneg (show 0 ≤ (precision : Int) by
        exact_mod_cast Nat.zero_le precision)] using hnum_lt
    have h :=
      (FloatSpec.Core.Digits.Zdigits_le_Zpower
        (beta := radix) (h_beta := hradix)
        (x := p.Fnum) (e := (precision : Int)) (hβ := hradix)) hpre
    simpa only [wp, PostCond.noThrow, pure,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using h
  exact Int.toNat_le.mpr hdigits_int

noncomputable def digitGivesBoundedNum_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `digitGivesBoundedNum` — digit bound implies vNum bound. -/
theorem digitGivesBoundedNum {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜precision ≠ 0 ∧ 1 < radix ∧ b.vNum = Zpower_nat radix precision ∧
        Fdigit (beta:=beta) radix p ≤ precision⌝⦄
    (pure (digitGivesBoundedNum_check (beta:=beta) radix b precision p) : Id Unit)
    ⦃⇓_ => ⌜|p.Fnum| < b.vNum⌝⦄ := by
  intro h
  rcases h with ⟨_hprecision, hradix, hvNum, hdigit⟩
  simp only [wp, PostCond.noThrow, pure, digitGivesBoundedNum_check, Id.run]
  unfold Fdigit at hdigit
  rw [pffDigit_of_one_lt radix p.Fnum hradix] at hdigit
  have hdigits_bound :
      FloatSpec.Core.Digits.Zdigits radix p.Fnum ≤ (precision : Int) := by
    have hdigits_nonneg :
        0 ≤ FloatSpec.Core.Digits.Zdigits radix p.Fnum := by
      have hge :=
        FloatSpec.Core.Digits.Zdigits_ge_0
          (beta := radix) (n := p.Fnum) trivial
      simpa only [wp, PostCond.noThrow, pure,
        PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
        Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using hge
    have hdigit_int :
        ((Int.toNat (FloatSpec.Core.Digits.Zdigits radix p.Fnum) : Nat) : Int)
          ≤ (precision : Int) := by
      exact_mod_cast hdigit
    simpa [Int.toNat_of_nonneg hdigits_nonneg] using hdigit_int
  have hnum_lt :
      (p.Fnum.natAbs : Int) < radix ^ ((precision : Int).natAbs) := by
    have hpow :=
      FloatSpec.Core.Digits.Zpower_gt_Zdigits
        (beta := radix) (h_beta := hradix)
        (e := (precision : Int)) (x := p.Fnum) (hβ := hradix)
        trivial
    simpa only [wp, PostCond.noThrow, pure,
      Id.run, Int.cast_ofNat] using hpow hdigits_bound
  rw [hvNum]
  simpa [Zpower_nat, Int.natAbs_of_nonneg
    (show 0 ≤ (precision : Int) by exact_mod_cast Nat.zero_le precision)] using hnum_lt

noncomputable def FnormalPrecision_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FnormalPrecision` — a normal float always has digit `precision`. -/
theorem FnormalPrecision_internal {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fnormal (beta:=beta) radix b p ∧
        Fnormal' (beta:=beta) radix b p ∧
        precision ≠ 0 ∧ 1 < radix ∧ b.vNum = Zpower_nat radix precision⌝⦄
    (pure (FnormalPrecision_check (beta:=beta) radix b precision p) : Id Unit)
    ⦃⇓_ => ⌜Fdigit (beta:=beta) radix p = precision⌝⦄ := by
  intro h
  rcases h with ⟨_, hnormal, hprecision, hradix, hvNum⟩
  rcases hnormal with ⟨hbounded, hnormal_num⟩
  simp only [wp, PostCond.noThrow, pure, FnormalPrecision_check, Id.run]
  apply le_antisymm
  · exact
      (pGivesDigit (beta := beta) radix b precision p)
        ⟨hbounded, hbounded, hprecision, hradix, hvNum⟩
  · unfold Fdigit
    rw [pffDigit_of_one_lt radix p.Fnum hradix]
    have hdigits_nonneg :
        0 ≤ FloatSpec.Core.Digits.Zdigits radix p.Fnum := by
      exact
        FloatSpec.Core.Digits.Zdigits_ge_0
          (beta := radix) (n := p.Fnum) trivial
    have hprecision_le_digits :
        (precision : Int) ≤ FloatSpec.Core.Digits.Zdigits radix p.Fnum := by
      have hradix_pos : 0 < radix := by omega
      have hpow_prec_le : radix ^ precision ≤ |radix * p.Fnum| := by
        have h := hnormal_num
        rw [hvNum] at h
        simpa [Zpower_nat] using h
      have hpow_succ : radix ^ precision = radix * radix ^ (precision - 1) := by
        have hprec : Nat.succ (precision - 1) = precision := by omega
        calc
          radix ^ precision = radix ^ Nat.succ (precision - 1) := by rw [hprec]
          _ = radix ^ (precision - 1) * radix := by rw [pow_succ]
          _ = radix * radix ^ (precision - 1) := by ring
      have habs_mul : |radix * p.Fnum| = radix * |p.Fnum| := by
        rw [abs_mul, abs_of_pos hradix_pos]
      have hpow_pred_le_abs : radix ^ (precision - 1) ≤ |p.Fnum| := by
        have hmul : radix * radix ^ (precision - 1) ≤ radix * |p.Fnum| := by
          simpa [hpow_succ, habs_mul] using hpow_prec_le
        exact (Int.mul_le_mul_left hradix_pos).mp hmul
      have hprec_pred_natAbs : (((precision : Int) - 1).natAbs) = precision - 1 := by
        omega
      have hpow_pred_le_natAbs :
          radix ^ (((precision : Int) - 1).natAbs) ≤ Int.natAbs p.Fnum := by
        rw [hprec_pred_natAbs]
        rw [← Int.abs_eq_natAbs]
        exact hpow_pred_le_abs
      have hdigits_gt :
          (precision : Int) - 1 < FloatSpec.Core.Digits.Zdigits radix p.Fnum :=
        (FloatSpec.Core.Digits.Zdigits_gt_Zpower_from_natAbs_payload
          (beta := radix) (h_beta := hradix)
          (e := (precision : Int) - 1) (x := p.Fnum) (hβ := hradix))
          hpow_pred_le_natAbs
      omega
    exact (Int.le_toNat hdigits_nonneg).mpr hprecision_le_digits

/-- Coq: `FnormalPrecision` with its exact exported section contract.

The implementation theorem above retains a duplicate compatibility predicate
needed by older translated callers.  The public theorem exposes only the
single `Fnormal` premise present in `Pff.v`, and indexes the float directly by
the source radix rather than introducing a second phantom radix. -/
theorem FnormalPrecision (radix : Int) [ValidRadix radix]
    (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat radix) :
    ⦃⌜1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision ∧
        Fnormal (beta:=radix) radix b p⌝⦄
    (pure (FnormalPrecision_check (beta:=radix) radix b precision p) : Id Unit)
    ⦃⇓_ => ⌜Fdigit (beta:=radix) radix p = precision⌝⦄ := by
  intro ⟨hradix, hprecision, hvNum, hnormal⟩
  exact FnormalPrecision_internal (beta:=radix) radix b precision p
    ⟨hnormal, hnormal, hprecision, hradix, hvNum⟩

-- ---------------------------------------------------------------------------
-- Minimal normal mantissa (`nNormMin`) and related Coq lemmas

-- NOTE: `nNormMin` is defined earlier in this file (near firstNormalPos)

noncomputable def nNormPos_check (radix : Int) (precision : Nat) : Unit :=
  ()

/-- Coq: `nNormPos` — minimal normal mantissa is strictly positive. -/
theorem nNormPos (radix : Int) (precision : Nat) :
    ⦃⌜0 < radix⌝⦄
    (pure (nNormPos_check radix precision) : Id Unit)
    ⦃⇓_ => ⌜0 < nNormMin radix precision⌝⦄ := by
  intro hr
  simp only [wp, PostCond.noThrow, pure, nNormPos_check, Id.run,
    ULift.up_down]
  show 0 < nNormMin radix precision
  simp [nNormMin]
  exact pow_pos hr _

noncomputable def digitnNormMin_check (radix : Int) (precision : Nat) : Unit :=
  ()

/-- Coq: `digitnNormMin` — `digit radix nNormMin = precision`. -/
theorem digitnNormMin (radix : Int) (precision : Nat) :
    ⦃⌜precision ≠ 0 ∧ 1 < radix⌝⦄
    (pure (digitnNormMin_check radix precision) : Id Unit)
    ⦃⇓_ => ⌜digit radix (nNormMin radix precision) = precision⌝⦄ := by
  intro ⟨hprecision, hradix⟩
  simp only [wp, PostCond.noThrow, pure, digitnNormMin_check,
    Id.run, ULift.up_down]
  unfold digit nNormMin
  rw [pffDigit_of_one_lt radix (radix ^ (precision - 1)) hradix]
  have hzdigits :
      FloatSpec.Core.Digits.Zdigits radix (radix ^ (precision - 1)) =
        ((precision - 1 : Nat) : Int) + 1 := by
    exact
      (FloatSpec.Core.Digits.Zdigits_Zpower
        (beta := radix) (k := ((precision - 1 : Nat) : Int)) hradix)
        (by exact_mod_cast Nat.zero_le (precision - 1))
  rw [hzdigits]
  have hint : (((precision - 1 : Nat) : Int) + 1) = (precision : Int) := by
    have hsucc : (precision - 1 : Nat) + 1 = precision := Nat.succ_pred hprecision
    exact_mod_cast hsucc
  rw [hint]
  simp

noncomputable def vNumbMoreThanOne_check (b : Fbound_skel) (radix : Int) (precision : Nat) : Unit :=
  ()

/-- Coq: `vNumbMoreThanOne` — when `b.vNum = radix^precision` with positive `radix`
and nonzero `precision`, the bound exceeds 1. -/
theorem vNumbMoreThanOne
    (b : Fbound_skel) (radix : Int) (precision : Nat) :
    ⦃⌜precision ≠ 0 ∧ 1 < radix ∧ b.vNum = Zpower_nat radix precision⌝⦄
    (pure (vNumbMoreThanOne_check b radix precision) : Id Unit)
    ⦃⇓_ => ⌜(1 : Int) < b.vNum⌝⦄ := by
  intro h
  rcases h with ⟨hp, hr, hv⟩
  have hpow : (1 : Int) < Zpower_nat radix precision := by
    simp only [Zpower_nat]
    exact one_lt_pow₀ hr hp
  simpa [hv]

noncomputable def pPredMoreThanOne_check
    (b : Fbound_skel) (radix : Int) (precision : Nat) : Unit :=
  ()

/-- Coq: `pPredMoreThanOne` — the predecessor of the mantissa bound is positive. -/
theorem pPredMoreThanOne
    (b : Fbound_skel) (radix : Int) (precision : Nat) :
    ⦃⌜1 < radix ∧ 1 < precision ∧ b.vNum = Zpower_nat radix precision⌝⦄
    (pure (pPredMoreThanOne_check b radix precision) : Id Unit)
    ⦃⇓_ => ⌜0 < pPred b.vNum⌝⦄ := by
  intro h
  rcases h with ⟨hradix, hprecision, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, pPredMoreThanOne_check, Id.run,
    ULift.up_down]
  have hvNum_gt_one : (1 : Int) < b.vNum := by
    have h := vNumbMoreThanOne b radix precision
    simpa only [wp, PostCond.noThrow, pure, vNumbMoreThanOne_check, Id.run,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using h ⟨by omega, hradix, hvNum⟩
  unfold pPred
  exact sub_pos.mpr hvNum_gt_one

noncomputable def nNrMMimLevNum_check
    (radix : Int) (b : Fbound_skel) (precision : Nat) : Unit :=
  ()

/-- Coq: `nNrMMimLevNum` — minimal mantissa bounded by `vNum` under standard relation. -/
theorem nNrMMimLevNum (radix : Int) (b : Fbound_skel) (precision : Nat) :
    ⦃⌜1 ≤ radix ∧ b.vNum = Zpower_nat radix precision⌝⦄
    (pure (nNrMMimLevNum_check radix b precision) : Id Unit)
    ⦃⇓_ => ⌜nNormMin radix precision ≤ b.vNum⌝⦄ := by
  intro ⟨hr, hb⟩
  simp only [wp, PostCond.noThrow, pure, nNrMMimLevNum_check, Id.run,
    ULift.up_down]
  show nNormMin radix precision ≤ b.vNum
  rw [hb]
  simp only [nNormMin, Zpower_nat]
  exact pow_le_pow_right₀ hr (Nat.sub_le precision 1)

noncomputable def pPredMoreThanRadix_check
    (b : Fbound_skel) (radix : Int) (precision : Nat) : Unit :=
  ()

/-- Coq: `pPredMoreThanRadix` — for precision above one, the top mantissa
predecessor is strictly larger than the radix. -/
theorem pPredMoreThanRadix
    (b : Fbound_skel) (radix : Int) (precision : Nat) :
    ⦃⌜1 < radix ∧ 1 < precision ∧ b.vNum = Zpower_nat radix precision⌝⦄
    (pure (pPredMoreThanRadix_check b radix precision) : Id Unit)
    ⦃⇓_ => ⌜radix < pPred b.vNum⌝⦄ := by
  intro h
  rcases h with ⟨hradix, hprecision, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, pPredMoreThanRadix_check, Id.run,
    ULift.up_down]
  have hradix_le_norm : radix ≤ nNormMin radix precision := by
    unfold nNormMin
    have hexp : 1 ≤ precision - 1 := by omega
    calc
      radix = radix ^ 1 := by simp
      _ ≤ radix ^ (precision - 1) := by
            exact pow_le_pow_right₀ (by omega : 1 ≤ radix) hexp
  have hnorm_lt_pred :
      nNormMin radix precision < pPred b.vNum :=
    nNormMimLtvNum radix b precision hradix hprecision hvNum
  exact lt_of_le_of_lt hradix_le_norm hnorm_lt_pred

-- NOTE: `firstNormalPos` is defined earlier in this file (near nNormMin)

noncomputable def firstNormalPosNormal_check
    {beta : Int} [ValidRadix beta] (radix : Int) (b : Fbound_skel) (precision : Nat) : Unit :=
  ()

/-- Coq: `firstNormalPosNormal` — the first positive normal float is normal. -/
theorem firstNormalPosNormal {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat) :
    ⦃⌜1 < radix ∧ 1 < precision ∧ b.vNum = Zpower_nat radix precision⌝⦄
    (pure (firstNormalPosNormal_check (beta:=beta) radix b precision) : Id Unit)
    ⦃⇓_ => ⌜Fnormal (beta:=beta) radix b
        (firstNormalPos (beta:=beta) radix b precision)⌝⦄ := by
  intro ⟨hradix, hprecision, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, firstNormalPosNormal_check,
    Id.run, ULift.up_down]
  show Fnormal (beta:=beta) radix b
    (firstNormalPos (beta:=beta) radix b precision)
  have hradix_pos : 0 < radix := by omega
  have hpow_pos : 0 < radix ^ (precision - 1) := pow_pos hradix_pos _
  have hsucc : Nat.succ (precision - 1) = precision := by omega
  have hpow_succ : radix ^ precision = radix * radix ^ (precision - 1) := by
    calc
      radix ^ precision = radix ^ Nat.succ (precision - 1) := by rw [hsucc]
      _ = radix ^ (precision - 1) * radix := by rw [pow_succ]
      _ = radix * radix ^ (precision - 1) := by ring
  constructor
  · constructor
    · rw [hvNum]
      simp only [firstNormalPos, nNormMin, Zpower_nat,
        FloatSpec.Core.Defs.FlocqFloat.Fnum]
      rw [abs_of_pos hpow_pos]
      rw [hpow_succ]
      nlinarith
    · simp [firstNormalPos]
  · rw [hvNum]
    simp only [firstNormalPos, nNormMin, Zpower_nat,
      FloatSpec.Core.Defs.FlocqFloat.Fnum]
    rw [hpow_succ]
    rw [abs_of_pos (mul_pos hradix_pos hpow_pos)]

noncomputable def pNormal_absolu_min_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `pNormal_absolu_min` — normal mantissas dominate `nNormMin`. -/
theorem pNormal_absolu_min {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fnormal (beta:=beta) radix b p ∧
        Fnormal' (beta:=beta) radix b p ∧
        precision ≠ 0 ∧ 1 < radix ∧ b.vNum = Zpower_nat radix precision⌝⦄
    (pure (pNormal_absolu_min_check (beta:=beta) radix b precision p) : Id Unit)
    ⦃⇓_ => ⌜nNormMin radix precision ≤ |p.Fnum|⌝⦄ := by
  intro h
  rcases h with ⟨_, hnormal, hprecision, hradix, hvNum⟩
  rcases hnormal with ⟨_, hnormal_num⟩
  simp only [wp, PostCond.noThrow, pure, pNormal_absolu_min_check, Id.run, ULift.up_down]
  show nNormMin radix precision ≤ |p.Fnum|
  have hradix_pos : 0 < radix := by omega
  have hpow_prec_le : radix ^ precision ≤ |radix * p.Fnum| := by
    simpa [Zpower_nat, hvNum] using hnormal_num
  have hpow_succ : radix ^ precision = radix * radix ^ (precision - 1) := by
    have hprec : Nat.succ (precision - 1) = precision := by omega
    calc
      radix ^ precision = radix ^ Nat.succ (precision - 1) := by rw [hprec]
      _ = radix ^ (precision - 1) * radix := by rw [pow_succ]
      _ = radix * radix ^ (precision - 1) := by ring
  have hmul_le : radix * nNormMin radix precision ≤ radix * |p.Fnum| := by
    rw [nNormMin, ← hpow_succ]
    have hradix_abs : |radix| = radix := abs_of_nonneg (le_of_lt hradix_pos)
    simpa [abs_mul, hradix_abs] using hpow_prec_le
  exact Int.le_of_mul_le_mul_left hmul_le hradix_pos

noncomputable def FSuccPropNeg_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) : Unit := ()

/-- Coq: `FSuccPropNeg` — successor ordering for negative lower endpoint. -/
theorem FSuccPropNeg {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision ∧
        _root_.F2R (beta:=beta) x < 0 ∧
        Fcanonic (beta:=beta) radix b x ∧
        Fcanonic (beta:=beta) radix b y ∧
        _root_.F2R (beta:=beta) x < _root_.F2R (beta:=beta) y⌝⦄
    (pure (FSuccPropNeg_check (beta:=beta) b radix precision x y) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta)
        (FSucc (beta:=beta) b radix precision x) ≤
      _root_.F2R (beta:=beta) y⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta_radix, hradix, hprecision, hvnum, hxneg, hx_can, hy_can, hxy⟩
  simp only [wp, PostCond.noThrow, pure, FSuccPropNeg_check, Id.run,
    ULift.up_down]
  by_cases hy_nonneg : 0 ≤ _root_.F2R (beta:=beta) y
  · have hcross := FSuccProp_cross_zero (beta:=beta) b radix precision x y
    simpa only [wp, PostCond.noThrow, pure, FSuccProp_cross_zero_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
      hcross ⟨hbeta_radix, hradix, hprecision, hvnum, hxneg, hy_nonneg⟩
  · have hyneg : _root_.F2R (beta:=beta) y < 0 := lt_of_not_ge hy_nonneg
    have hbeta_gt : 1 < beta := by omega
    have hradix_eq_beta : radix = beta := hbeta_radix.symm
    have hsucc_can :
        Fcanonic (beta:=beta) radix b
          (FSucc (beta:=beta) b radix precision x) :=
      (FSuccCanonic (beta:=beta) b radix precision x)
        ⟨hx_can, hradix, hprecision, hvnum⟩
    have hsucc_nonpos :
        _root_.F2R (beta:=beta)
            (FSucc (beta:=beta) b radix precision x) ≤ 0 := by
      have hsucc := R0RltRleSucc (beta:=beta) b radix precision x
      simpa only [wp, PostCond.noThrow, pure, R0RltRleSucc_check,
        PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
        Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
        hsucc ⟨hbeta_radix, hradix, hprecision, hvnum, hxneg⟩
    have hcmp :
        y.Fexp < x.Fexp ∨ (x.Fexp = y.Fexp ∧ x.Fnum < y.Fnum) :=
      (FcanonicLtNeg (beta:=beta) radix b x y hbeta_gt hradix_eq_beta)
        ⟨hx_can, hy_can, le_of_lt hyneg, hxy⟩
    have hx_fnum_neg : x.Fnum < 0 :=
      FloatSpec.Core.Float_prop.lt_0_F2R (beta:=beta) x hbeta_gt hxneg
    have hy_fnum_neg : y.Fnum < 0 :=
      FloatSpec.Core.Float_prop.lt_0_F2R (beta:=beta) y hbeta_gt hyneg
    have hradix_pos : 0 < radix := by omega
    have hnm_pos : 0 < nNormMin radix precision := by
      unfold nNormMin
      exact pow_pos hradix_pos _
    have hpPred_pos : 0 < pPred b.vNum := by
      unfold pPred
      rw [hvnum, Zpower_nat]
      have hpow_gt_one : (1 : Int) < radix ^ precision :=
        one_lt_pow₀ hradix hprecision
      omega
    have hnot_top : x.Fnum ≠ pPred b.vNum := by
      intro htop
      omega
    rcases hcmp with hexp_lt | ⟨hexp_eq, hnum_lt⟩
    · by_cases hlow : x.Fnum = -nNormMin radix precision
      · by_cases hexp_min : x.Fexp = -b.dExp
        · have hyBound : Fbounded (beta:=beta) b y :=
            (FcanonicBound (beta:=beta) radix b y) hy_can
          rcases hyBound with ⟨_, hyExp⟩
          omega
        · have hsucc_eq :
              FSucc (beta:=beta) b radix precision x =
                (⟨-pPred b.vNum, x.Fexp - 1⟩ :
                  FloatSpec.Core.Defs.FlocqFloat beta) := by
            have hlow_not_top : -nNormMin radix precision ≠ pPred b.vNum := by
              omega
            unfold FSucc
            simp [hnot_top, hlow, hlow_not_top, hexp_min]
          have hnext := Zlt_next y.Fexp x.Fexp
          have hnext' : x.Fexp = Int.succ y.Fexp ∨
              Int.succ y.Fexp < x.Fexp := by
            simpa only [wp, PostCond.noThrow, pure, Zlt_next_check,
              PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
              Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using hnext hexp_lt
          rcases hnext' with hadj | hgap
          · have hsucc_exp : (FSucc (beta:=beta) b radix precision x).Fexp = y.Fexp := by
              rw [hsucc_eq]
              simp only [FloatSpec.Core.Defs.FlocqFloat.Fexp]
              unfold Int.succ at hadj
              omega
            have hy_lower : -pPred b.vNum ≤ y.Fnum := by
              have hyBound : Fbounded (beta:=beta) b y :=
                (FcanonicBound (beta:=beta) radix b y) hy_can
              rcases hyBound with ⟨hyNum, _⟩
              have hle_abs : |y.Fnum| ≤ pPred b.vNum := by
                unfold pPred
                omega
              have habs_neg : |y.Fnum| = -y.Fnum := abs_of_neg hy_fnum_neg
              omega
            rw [hsucc_eq]
            simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
              FloatSpec.Core.Defs.FlocqFloat.Fnum,
              FloatSpec.Core.Defs.FlocqFloat.Fexp]
            have hpow_nonneg : 0 ≤ (beta : ℝ) ^ y.Fexp :=
              le_of_lt (zpow_pos
                (by exact_mod_cast (show (0 : Int) < beta by omega)) y.Fexp)
            have hy_lower_real : ((-pPred b.vNum : Int) : ℝ) ≤ (y.Fnum : ℝ) := by
              exact_mod_cast hy_lower
            have hexp_sub : x.Fexp - 1 = y.Fexp := by
              unfold Int.succ at hadj
              omega
            rw [hexp_sub]
            exact mul_le_mul_of_nonneg_right hy_lower_real hpow_nonneg
          · have hsucc_exp_gt : y.Fexp <
                (FSucc (beta:=beta) b radix precision x).Fexp := by
              rw [hsucc_eq]
              simp only [FloatSpec.Core.Defs.FlocqFloat.Fexp]
              unfold Int.succ at hgap
              omega
            exact le_of_lt
              ((FcanonicNegFexpRlt (beta:=beta) radix b y
                  (FSucc (beta:=beta) b radix precision x)
                  hbeta_gt hradix_eq_beta)
                ⟨hy_can, hsucc_can, le_of_lt hyneg, hsucc_nonpos,
                  hsucc_exp_gt⟩)
      · have hsucc_eq :
            FSucc (beta:=beta) b radix precision x =
              (⟨x.Fnum + 1, x.Fexp⟩ :
                FloatSpec.Core.Defs.FlocqFloat beta) := by
          unfold FSucc
          simp [hnot_top, hlow]
        have hsucc_exp_gt : y.Fexp <
              (FSucc (beta:=beta) b radix precision x).Fexp := by
          rw [hsucc_eq]
          simp only [FloatSpec.Core.Defs.FlocqFloat.Fexp]
          exact hexp_lt
        exact le_of_lt
          ((FcanonicNegFexpRlt (beta:=beta) radix b y
              (FSucc (beta:=beta) b radix precision x)
              hbeta_gt hradix_eq_beta)
            ⟨hy_can, hsucc_can, le_of_lt hyneg, hsucc_nonpos, hsucc_exp_gt⟩)
    · by_cases hlow : x.Fnum = -nNormMin radix precision
      · by_cases hexp_min : x.Fexp = -b.dExp
        · have hsucc_eq :
              FSucc (beta:=beta) b radix precision x =
                (⟨x.Fnum + 1, x.Fexp⟩ :
                  FloatSpec.Core.Defs.FlocqFloat beta) := by
            have hlow_not_top : -nNormMin radix precision ≠ pPred b.vNum := by
              omega
            unfold FSucc
            simp [hnot_top, hlow, hlow_not_top, hexp_min]
          exact
            (FSucc_same_exp_mantissa_le (beta:=beta) b radix precision x y)
              ⟨hsucc_eq, hexp_eq, hnum_lt, hbeta_gt⟩
        · rcases hy_can with hyNormal | hySubnormal
          · have hmin_abs : nNormMin radix precision ≤ |y.Fnum| :=
              (pNormal_absolu_min (beta:=beta) radix b precision y)
                ⟨hyNormal, hyNormal, hprecision, hradix, hvnum⟩
            have hy_le_low : y.Fnum ≤ -nNormMin radix precision := by
              have habs_neg : |y.Fnum| = -y.Fnum := abs_of_neg hy_fnum_neg
              omega
            omega
          · rcases hySubnormal with ⟨_, hyExp, _⟩
            omega
      · have hsucc_eq :
            FSucc (beta:=beta) b radix precision x =
              (⟨x.Fnum + 1, x.Fexp⟩ :
                FloatSpec.Core.Defs.FlocqFloat beta) := by
          unfold FSucc
          simp [hnot_top, hlow]
        exact
          (FSucc_same_exp_mantissa_le (beta:=beta) b radix precision x y)
            ⟨hsucc_eq, hexp_eq, hnum_lt, hbeta_gt⟩

noncomputable def FSuccProp_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) : Unit := ()

/-- Coq: `FSuccProp` — the faithful successor of a canonical lower float is
below any strictly larger canonical float. -/
theorem FSuccProp {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision ∧
        Fcanonic (beta:=beta) radix b x ∧
        Fcanonic (beta:=beta) radix b y ∧
        _root_.F2R (beta:=beta) x < _root_.F2R (beta:=beta) y⌝⦄
    (pure (FSuccProp_check (beta:=beta) b radix precision x y) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta)
        (FSucc (beta:=beta) b radix precision x) ≤
      _root_.F2R (beta:=beta) y⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hradix, hprecision, hvnum, hxcan, hycan, hxy⟩
  simp only [wp, PostCond.noThrow, pure, FSuccProp_check, Id.run,
    ULift.up_down]
  by_cases hx_nonneg : 0 ≤ _root_.F2R (beta:=beta) x
  · exact
      (FSuccPropPos (beta:=beta) b radix precision x y)
        ⟨hbeta, hradix, hprecision, hvnum, hx_nonneg, hxcan, hycan, hxy⟩
  · have hxneg : _root_.F2R (beta:=beta) x < 0 := lt_of_not_ge hx_nonneg
    exact
      (FSuccPropNeg (beta:=beta) b radix precision x y)
        ⟨hbeta, hradix, hprecision, hvnum, hxneg, hxcan, hycan, hxy⟩

noncomputable def FPredProp_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) : Unit := ()

/-- Coq: `FPredProp` — if canonical `x < y`, then `x` is below the
predecessor of `y`. -/
theorem FPredProp {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision ∧
        Fcanonic (beta:=beta) radix b x ∧
        Fcanonic (beta:=beta) radix b y ∧
        _root_.F2R (beta:=beta) x < _root_.F2R (beta:=beta) y⌝⦄
    (pure (FPredProp_check (beta:=beta) b radix precision x y) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) x ≤
      _root_.F2R (beta:=beta)
        (FPred (beta:=beta) b radix precision y)⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hradix, hprecision, hvnum, hxcan, hycan, hxy⟩
  simp only [wp, PostCond.noThrow, pure, FPredProp_check, Id.run,
    ULift.up_down]
  have hxopp_can :
      Fcanonic (beta:=beta) radix b (Fopp (beta:=beta) x) := by
    have h := FcanonicFopp (beta:=beta) radix b x
    simpa only [wp, PostCond.noThrow, pure, FcanonicFopp_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down]
      using h hxcan
  have hyopp_can :
      Fcanonic (beta:=beta) radix b (Fopp (beta:=beta) y) := by
    have h := FcanonicFopp (beta:=beta) radix b y
    simpa only [wp, PostCond.noThrow, pure, FcanonicFopp_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down]
      using h hycan
  have hx_opp :
      _root_.F2R (beta:=beta) (Fopp (beta:=beta) x) =
        - _root_.F2R (beta:=beta) x := by
    have h := FloatSpec.Calc.Operations.F2R_opp (beta:=beta) x
    simpa [Fopp, _root_.F2R, Int.cast_ofNat] using h trivial
  have hy_opp :
      _root_.F2R (beta:=beta) (Fopp (beta:=beta) y) =
        - _root_.F2R (beta:=beta) y := by
    have h := FloatSpec.Calc.Operations.F2R_opp (beta:=beta) y
    simpa [Fopp, _root_.F2R] using h trivial
  have hxy_opp :
      _root_.F2R (beta:=beta) (Fopp (beta:=beta) y) <
        _root_.F2R (beta:=beta) (Fopp (beta:=beta) x) := by
    rw [hy_opp, hx_opp]
    exact neg_lt_neg hxy
  have hsucc :
      _root_.F2R (beta:=beta)
          (FSucc (beta:=beta) b radix precision (Fopp (beta:=beta) y)) ≤
        _root_.F2R (beta:=beta) (Fopp (beta:=beta) x) := by
    have h := FSuccProp (beta:=beta) b radix precision
      (Fopp (beta:=beta) y) (Fopp (beta:=beta) x)
    simpa only [wp, PostCond.noThrow, pure, FSuccProp_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
      h ⟨hbeta, hradix, hprecision, hvnum, hyopp_can, hxopp_can, hxy_opp⟩
  have hpred_eq :
      FPred (beta:=beta) b radix precision y =
        Fopp (beta:=beta)
          (FSucc (beta:=beta) b radix precision (Fopp (beta:=beta) y)) := by
    have h := FPredFopFSucc (beta:=beta) b radix precision y
    simpa only [wp, PostCond.noThrow, pure, FPredFopFSucc_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
      h ⟨hradix, hprecision, hvnum⟩
  have hsucc_opp :
      _root_.F2R (beta:=beta)
          (Fopp (beta:=beta)
            (FSucc (beta:=beta) b radix precision (Fopp (beta:=beta) y))) =
        - _root_.F2R (beta:=beta)
          (FSucc (beta:=beta) b radix precision (Fopp (beta:=beta) y)) := by
    have h := FloatSpec.Calc.Operations.F2R_opp (beta:=beta)
      (FSucc (beta:=beta) b radix precision (Fopp (beta:=beta) y))
    simpa [Fopp, _root_.F2R] using h trivial
  rw [hpred_eq, hsucc_opp]
  rw [hx_opp] at hsucc
  simpa using neg_le_neg hsucc

noncomputable def FnormalLtFirstNormalPos_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FnormalLtFirstNormalPos` — normal nonnegative floats dominate
the first normal positive value. -/
theorem FnormalLtFirstNormalPos {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fnormal (beta:=beta) radix b p ∧
        Fnormal' (beta:=beta) radix b p ∧
        precision ≠ 0 ∧ beta = radix ∧ 1 < radix ∧
        b.vNum = Zpower_nat radix precision ∧
        0 ≤ _root_.F2R (beta:=beta) p⌝⦄
    (pure (FnormalLtFirstNormalPos_check (beta:=beta) radix b precision p) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta)
            (firstNormalPos (beta:=beta) radix b precision)
            ≤ _root_.F2R (beta:=beta) p⌝⦄ := by
  intro h
  rcases h with ⟨hnormal, hnormal', hprecision, hbeta_radix, hradix, hvNum, hp_nonneg⟩
  rcases hnormal' with ⟨hbounded, hnormal_num⟩
  rcases hbounded with ⟨hnum_bound, hexp_lb⟩
  simp only [wp, PostCond.noThrow, pure, FnormalLtFirstNormalPos_check, Id.run, ULift.up_down]
  show _root_.F2R (beta:=beta) (firstNormalPos (beta:=beta) radix b precision)
      ≤ _root_.F2R (beta:=beta) p
  have hbeta : 1 < beta := by omega
  have hbeta_pos : (0 : ℝ) < (beta : ℝ) := by
    exact_mod_cast (lt_trans (by norm_num : (0 : Int) < 1) hbeta)
  have hbeta_ge_one : (1 : ℝ) ≤ (beta : ℝ) := by
    exact_mod_cast (le_of_lt hbeta)
  have hp_fnum_nonneg : 0 ≤ p.Fnum :=
    FloatSpec.Core.Float_prop.ge_0_F2R (beta := beta) p hbeta hp_nonneg
  have hmin_abs : nNormMin radix precision ≤ |p.Fnum| :=
    (pNormal_absolu_min (beta := beta) radix b precision p)
      ⟨hnormal, ⟨⟨hnum_bound, hexp_lb⟩, hnormal_num⟩, hprecision, hradix, hvNum⟩
  have hmin_le_num : nNormMin radix precision ≤ p.Fnum := by
    simpa [abs_of_nonneg hp_fnum_nonneg] using hmin_abs
  have hmin_le_num_real : (nNormMin radix precision : ℝ) ≤ (p.Fnum : ℝ) := by
    exact_mod_cast hmin_le_num
  have hp_fnum_nonneg_real : (0 : ℝ) ≤ (p.Fnum : ℝ) := by
    exact_mod_cast hp_fnum_nonneg
  have hpow_le : (beta : ℝ) ^ (-b.dExp) ≤ (beta : ℝ) ^ p.Fexp :=
    zpow_le_zpow_right₀ hbeta_ge_one hexp_lb
  simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, firstNormalPos,
    FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp]
  calc
    (nNormMin radix precision : ℝ) * (beta : ℝ) ^ (-b.dExp)
        ≤ (p.Fnum : ℝ) * (beta : ℝ) ^ (-b.dExp) := by
          exact mul_le_mul_of_nonneg_right hmin_le_num_real
            (le_of_lt (zpow_pos hbeta_pos (-b.dExp)))
    _ ≤ (p.Fnum : ℝ) * (beta : ℝ) ^ p.Fexp := by
          exact mul_le_mul_of_nonneg_left hpow_le hp_fnum_nonneg_real

noncomputable def FsubnormalDigit_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FsubnormalDigit` — a subnormal float has digit strictly below
`precision`. Mirrors the original statement with Hoare triple syntax. -/
theorem FsubnormalDigit {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fsubnormal (beta:=beta) radix b p ∧
        Fsubnormal' (beta:=beta) radix b p ∧
        precision ≠ 0 ∧ 1 < radix ∧ b.vNum = Zpower_nat radix precision⌝⦄
    (pure (FsubnormalDigit_check (beta:=beta) radix b precision p) : Id Unit)
    ⦃⇓_ => ⌜Fdigit (beta:=beta) radix p < precision⌝⦄ := by
  intro h
  rcases h with ⟨_, hsub, hprecision, hradix, hvNum⟩
  rcases hsub with ⟨_, _hexp, hnum⟩
  simp only [wp, PostCond.noThrow, pure, FsubnormalDigit_check,
    Id.run, ULift.up_down]
  unfold Fdigit
  rw [pffDigit_of_one_lt radix p.Fnum hradix]
  have hradix_pos : 0 < radix := by omega
  have hprec_pred : ((precision - 1 : Nat) : Int) + 1 = (precision : Int) := by
    exact_mod_cast Nat.succ_pred hprecision
  have hpow_succ : radix ^ precision = radix * radix ^ (precision - 1) := by
    have hprec : Nat.succ (precision - 1) = precision := by omega
    calc
      radix ^ precision = radix ^ Nat.succ (precision - 1) := by rw [hprec]
      _ = radix ^ (precision - 1) * radix := by rw [pow_succ]
      _ = radix * radix ^ (precision - 1) := by ring
  have hm_abs_lt : |p.Fnum| < radix ^ (precision - 1) := by
    have hmul_lt : radix * |p.Fnum| < radix * radix ^ (precision - 1) := by
      have hradix_abs : |radix| = radix := abs_of_nonneg (le_of_lt hradix_pos)
      rw [← hpow_succ]
      simpa [hvNum, Zpower_nat, abs_mul, hradix_abs] using hnum
    exact Int.lt_of_mul_lt_mul_left hmul_lt (le_of_lt hradix_pos)
  have hdigits_le :
      FloatSpec.Core.Digits.Zdigits radix p.Fnum ≤ ((precision - 1 : Nat) : Int) := by
    have hpre :
        0 ≤ ((precision - 1 : Nat) : Int) ∧
          (p.Fnum.natAbs : Int) < radix ^ (((precision - 1 : Nat) : Int).natAbs) := by
      refine ⟨by exact_mod_cast Nat.zero_le (precision - 1), ?_⟩
      rw [← Int.abs_eq_natAbs]
      simpa using hm_abs_lt
    exact
      (FloatSpec.Core.Digits.Zdigits_le_Zpower
        (beta := radix) (h_beta := hradix)
        (x := p.Fnum) (e := ((precision - 1 : Nat) : Int)) (hβ := hradix)) hpre
  have hdigits_nonneg :
      0 ≤ FloatSpec.Core.Digits.Zdigits radix p.Fnum := by
    exact
      FloatSpec.Core.Digits.Zdigits_ge_0
        (beta := radix) (n := p.Fnum) trivial
  have hnat_le : Int.toNat (FloatSpec.Core.Digits.Zdigits radix p.Fnum) ≤ precision - 1 :=
    Int.toNat_le.2 hdigits_le
  exact lt_of_le_of_lt hnat_le (Nat.pred_lt hprecision)

-- Coq: `pSubnormal_absolu_min` — subnormal mantissas lie below `nNormMin`.
noncomputable def pSubnormal_absolu_min_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `pSubnormal_absolu_min` — the absolute mantissa of a subnormal is
bounded by `nNormMin`. -/
theorem pSubnormal_absolu_min {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fsubnormal (beta:=beta) radix b p ∧
        Fsubnormal' (beta:=beta) radix b p ∧
        precision ≠ 0 ∧ 1 < radix ∧ b.vNum = Zpower_nat radix precision⌝⦄
    (pure (pSubnormal_absolu_min_check (beta:=beta) radix b precision p) : Id Unit)
    ⦃⇓_ => ⌜|p.Fnum| < nNormMin radix precision⌝⦄ := by
  intro h
  rcases h with ⟨_, hsub, hprecision, hradix, hvNum⟩
  rcases hsub with ⟨_, _hexp, hnum⟩
  simp only [wp, PostCond.noThrow, pure, pSubnormal_absolu_min_check, Id.run, ULift.up_down]
  show |p.Fnum| < nNormMin radix precision
  have hradix_pos : 0 < radix := by omega
  have hpow_succ : radix ^ precision = radix * radix ^ (precision - 1) := by
    have hprec : Nat.succ (precision - 1) = precision := by omega
    calc
      radix ^ precision = radix ^ Nat.succ (precision - 1) := by rw [hprec]
      _ = radix ^ (precision - 1) * radix := by rw [pow_succ]
      _ = radix * radix ^ (precision - 1) := by ring
  have hmul_lt : radix * |p.Fnum| < radix * radix ^ (precision - 1) := by
    have hradix_abs : |radix| = radix := abs_of_nonneg (le_of_lt hradix_pos)
    rw [← hpow_succ]
    simpa [hvNum, Zpower_nat, abs_mul, hradix_abs] using hnum
  simpa [nNormMin] using Int.lt_of_mul_lt_mul_left hmul_lt (le_of_lt hradix_pos)

noncomputable def FsubnormalLtFirstNormalPos_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FsubnormalLtFirstNormalPos` — a nonnegative subnormal float lies
strictly below the first positive normal float. -/
theorem FsubnormalLtFirstNormalPos {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fsubnormal (beta:=beta) radix b p ∧
        Fsubnormal' (beta:=beta) radix b p ∧
        precision ≠ 0 ∧ beta = radix ∧ 1 < radix ∧
        b.vNum = Zpower_nat radix precision ∧
        0 ≤ _root_.F2R (beta:=beta) p⌝⦄
    (pure (FsubnormalLtFirstNormalPos_check (beta:=beta) radix b precision p) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) p <
            _root_.F2R (beta:=beta)
              (firstNormalPos (beta:=beta) radix b precision)⌝⦄ := by
  intro h
  rcases h with ⟨hsub, hsubPrime, hprecision, hbeta_radix, hradix, hvNum, hp_nonneg⟩
  have hsubPrime_full := hsubPrime
  rcases hsubPrime with ⟨_hbounded, hexp, _hnum⟩
  simp only [wp, PostCond.noThrow, pure, FsubnormalLtFirstNormalPos_check, Id.run, ULift.up_down]
  show _root_.F2R (beta:=beta) p <
      _root_.F2R (beta:=beta) (firstNormalPos (beta:=beta) radix b precision)
  have hbeta : 1 < beta := by omega
  have hbeta_pos : (0 : ℝ) < (beta : ℝ) := by
    exact_mod_cast (lt_trans (by norm_num : (0 : Int) < 1) hbeta)
  have hp_fnum_nonneg : 0 ≤ p.Fnum :=
    FloatSpec.Core.Float_prop.ge_0_F2R (beta := beta) p hbeta hp_nonneg
  have hsub_abs : |p.Fnum| < nNormMin radix precision :=
    (pSubnormal_absolu_min (beta := beta) radix b precision p)
      ⟨hsub, hsubPrime_full, hprecision, hradix, hvNum⟩
  have hp_fnum_lt : p.Fnum < nNormMin radix precision := by
    simpa [abs_of_nonneg hp_fnum_nonneg] using hsub_abs
  have hp_fnum_lt_real : (p.Fnum : ℝ) < (nNormMin radix precision : ℝ) := by
    exact_mod_cast hp_fnum_lt
  simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, firstNormalPos,
    FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp]
  rw [hexp]
  exact mul_lt_mul_of_pos_right hp_fnum_lt_real (zpow_pos hbeta_pos (-b.dExp))

noncomputable def FsubnormalnormalLtPos_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FsubnormalnormalLtPos` — a nonnegative subnormal float is strictly
below any nonnegative normal float. -/
theorem FsubnormalnormalLtPos {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fsubnormal (beta:=beta) radix b p ∧
        Fsubnormal' (beta:=beta) radix b p ∧
        Fnormal (beta:=beta) radix b q ∧
        Fnormal' (beta:=beta) radix b q ∧
        precision ≠ 0 ∧ beta = radix ∧ 1 < radix ∧
        b.vNum = Zpower_nat radix precision ∧
        0 ≤ _root_.F2R (beta:=beta) p ∧
        0 ≤ _root_.F2R (beta:=beta) q⌝⦄
    (pure (FsubnormalnormalLtPos_check (beta:=beta) radix b p q) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) p <
            _root_.F2R (beta:=beta) q⌝⦄ := by
  intro h
  rcases h with
    ⟨hsub, hsub', hnormal, hnormal', hprecision, hbeta_radix, hradix, hvNum, hp_nonneg,
      hq_nonneg⟩
  simp only [wp, PostCond.noThrow, pure, FsubnormalnormalLtPos_check, Id.run, ULift.up_down]
  have hp_lt_first :
      _root_.F2R (beta:=beta) p <
        _root_.F2R (beta:=beta) (firstNormalPos (beta:=beta) radix b precision) :=
    (FsubnormalLtFirstNormalPos (beta := beta) radix b precision p)
      ⟨hsub, hsub', hprecision, hbeta_radix, hradix, hvNum, hp_nonneg⟩
  have hfirst_le_q :
      _root_.F2R (beta:=beta) (firstNormalPos (beta:=beta) radix b precision) ≤
        _root_.F2R (beta:=beta) q :=
    (FnormalLtFirstNormalPos (beta := beta) radix b precision q)
      ⟨hnormal, hnormal', hprecision, hbeta_radix, hradix, hvNum, hq_nonneg⟩
  exact lt_of_lt_of_le hp_lt_first hfirst_le_q

noncomputable def FsubnormalnormalLtNeg_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FsubnormalnormalLtNeg` — a nonpositive subnormal float is strictly
above any nonpositive normal float. -/
theorem FsubnormalnormalLtNeg {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fsubnormal (beta:=beta) radix b p ∧
        Fsubnormal' (beta:=beta) radix b p ∧
        Fnormal (beta:=beta) radix b q ∧
        Fnormal' (beta:=beta) radix b q ∧
        precision ≠ 0 ∧ beta = radix ∧ 1 < radix ∧
        b.vNum = Zpower_nat radix precision ∧
        _root_.F2R (beta:=beta) p ≤ 0 ∧
        _root_.F2R (beta:=beta) q ≤ 0⌝⦄
    (pure (FsubnormalnormalLtNeg_check (beta:=beta) radix b p q) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) q <
            _root_.F2R (beta:=beta) p⌝⦄ := by
  intro h
  rcases h with
    ⟨hsub, hsub', hnormal, hnormal', hprecision, hbeta_radix, hradix, hvNum,
      hp_nonpos, hq_nonpos⟩
  simp only [wp, PostCond.noThrow, pure, FsubnormalnormalLtNeg_check, Id.run, ULift.up_down]
  let fp := FloatSpec.Calc.Operations.Fopp (beta:=beta) p
  let fq := FloatSpec.Calc.Operations.Fopp (beta:=beta) q
  have hsub_opp : Fsubnormal (beta:=beta) radix b fp := by
    simpa [fp] using (FsubnormFopp (beta:=beta) b radix p hsub)
  have hsub'_opp : Fsubnormal' (beta:=beta) radix b fp := by
    rcases hsub' with ⟨⟨hnum, hexp_lb⟩, hexp, hmant⟩
    refine ⟨⟨?_, ?_⟩, ?_, ?_⟩
    · simpa [fp, Fbounded', FloatSpec.Calc.Operations.Fopp] using hnum
    · simpa [fp, Fbounded', FloatSpec.Calc.Operations.Fopp] using hexp_lb
    · simpa [fp, FloatSpec.Calc.Operations.Fopp] using hexp
    · simpa [fp, FloatSpec.Calc.Operations.Fopp, abs_mul, abs_neg, mul_neg] using hmant
  have hnormal_opp : Fnormal (beta:=beta) radix b fq := by
    simpa [fq] using (FnormalFop (beta:=beta) b radix q hnormal)
  have hnormal'_opp : Fnormal' (beta:=beta) radix b fq := by
    rcases hnormal' with ⟨⟨hnum, hexp_lb⟩, hmant⟩
    refine ⟨⟨?_, ?_⟩, ?_⟩
    · simpa [fq, Fbounded', FloatSpec.Calc.Operations.Fopp] using hnum
    · simpa [fq, Fbounded', FloatSpec.Calc.Operations.Fopp] using hexp_lb
    · simpa [fq, FloatSpec.Calc.Operations.Fopp, abs_mul, abs_neg, mul_neg] using hmant
  have hp_opp_nonneg : 0 ≤ _root_.F2R (beta:=beta) fp := by
    have hp_eq :
        _root_.F2R (beta:=beta) fp = - _root_.F2R (beta:=beta) p :=
      by
        have h := (FloatSpec.Calc.Operations.F2R_opp (beta := beta) p) trivial
        simpa [fp, wp, PostCond.noThrow, pure, Id.run] using h
    linarith
  have hq_opp_nonneg : 0 ≤ _root_.F2R (beta:=beta) fq := by
    have hq_eq :
        _root_.F2R (beta:=beta) fq = - _root_.F2R (beta:=beta) q :=
      by
        have h := (FloatSpec.Calc.Operations.F2R_opp (beta := beta) q) trivial
        simpa [fq, wp, PostCond.noThrow, pure, Id.run] using h
    linarith
  have hopp_lt :
      _root_.F2R (beta:=beta) fp < _root_.F2R (beta:=beta) fq :=
    (FsubnormalnormalLtPos (beta := beta) radix b precision fp fq)
      ⟨hsub_opp, hsub'_opp, hnormal_opp, hnormal'_opp, hprecision, hbeta_radix,
        hradix, hvNum, hp_opp_nonneg, hq_opp_nonneg⟩
  have hp_eq :
      _root_.F2R (beta:=beta) fp = - _root_.F2R (beta:=beta) p :=
    by
      have h := (FloatSpec.Calc.Operations.F2R_opp (beta := beta) p) trivial
      simpa [fp, wp, PostCond.noThrow, pure, Id.run] using h
  have hq_eq :
      _root_.F2R (beta:=beta) fq = - _root_.F2R (beta:=beta) q :=
    by
      have h := (FloatSpec.Calc.Operations.F2R_opp (beta := beta) q) trivial
      simpa [fq, wp, PostCond.noThrow, pure, Id.run] using h
  have hneg_lt :
      - _root_.F2R (beta:=beta) p < - _root_.F2R (beta:=beta) q := by
    rw [← hp_eq, ← hq_eq]
    exact hopp_lt
  exact neg_lt_neg_iff.mp hneg_lt

noncomputable def FnormalUnique_check {beta : Int} [ValidRadix beta]
    (radix : ℝ) (b : Fbound_skel)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

-- Declared after `FdigitEq`, matching the dependency order in upstream Flocq.

-- Coq: `FnormalLtPos` — ordered normal floats compare via exponent then mantissa.
noncomputable def FnormalLtPos_check {beta : Int} [ValidRadix beta]
    (radix : ℝ) (b : Fbound_skel)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FnormalLtPos` — if `p` and `q` are normal, `0 ≤ F2R p`, and
    `_root_.F2R p < _root_.F2R q`, then either `p.Fexp < q.Fexp` or their
    exponents coincide while `p.Fnum < q.Fnum`.

    Upstream Flocq states this inside the Pff precision section, where
    `1 < radix`, `precision ≠ 0`, and `vNum b = radix^precision` are section
    hypotheses. Lean also carries `beta = radix` because `F2R` is parameterized
    by `beta`, and uses `Fnormal'` for the proper normality predicate. -/
theorem FnormalLtPos {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fnormal (beta:=beta) radix b p ∧
        Fnormal' (beta:=beta) radix b p ∧
        Fnormal (beta:=beta) radix b q ∧
        Fnormal' (beta:=beta) radix b q ∧
        precision ≠ 0 ∧ beta = radix ∧ 1 < radix ∧
        b.vNum = Zpower_nat radix precision ∧
        0 ≤ _root_.F2R (beta:=beta) p ∧
        _root_.F2R (beta:=beta) p < _root_.F2R (beta:=beta) q⌝⦄
    (pure (FnormalLtPos_check (beta:=beta) radix b p q) : Id Unit)
    ⦃⇓_ => ⌜p.Fexp < q.Fexp ∨
            (p.Fexp = q.Fexp ∧ p.Fnum < q.Fnum)⌝⦄ := by
  intro h
  rcases h with
    ⟨hnormal_p, hnormal'_p, hnormal_q, hnormal'_q, hprecision, hbeta_radix,
      hradix, hvNum, hp_nonneg, hp_lt_q⟩
  simp only [wp, PostCond.noThrow, pure, FnormalLtPos_check,
    Id.run, ULift.up_down]
  have hbeta : 1 < beta := by omega
  have hbeta_pos : (0 : ℝ) < (beta : ℝ) := by
    exact_mod_cast (lt_trans (by norm_num : (0 : Int) < 1) hbeta)
  have hp_digit :
      Fdigit (beta:=beta) radix p = precision :=
    (FnormalPrecision_internal (beta:=beta) radix b precision p)
      ⟨hnormal_p, hnormal'_p, hprecision, hradix, hvNum⟩
  have hq_digit :
      Fdigit (beta:=beta) radix q = precision :=
    (FnormalPrecision_internal (beta:=beta) radix b precision q)
      ⟨hnormal_q, hnormal'_q, hprecision, hradix, hvNum⟩
  have hp_nonzero : ¬ is_Fzero p := by
    intro hp_zero
    rcases hnormal'_p with ⟨_, hmant⟩
    unfold is_Fzero at hp_zero
    rw [hp_zero, mul_zero, abs_zero] at hmant
    have hvNum_pos : 0 < b.vNum := by
      rw [hvNum, Zpower_nat]
      exact pow_pos (by omega : 0 < radix) precision
    omega
  by_cases hp_exp_lt : p.Fexp < q.Fexp
  · exact Or.inl hp_exp_lt
  · have hq_exp_le_p : q.Fexp ≤ p.Fexp := by omega
    rcases lt_or_eq_of_le hq_exp_le_p with hq_exp_lt | hq_exp_eq
    · exfalso
      let n : Nat := Int.natAbs (p.Fexp - q.Fexp)
      have hdiff_nonneg : 0 ≤ p.Fexp - q.Fexp := by omega
      have hn_eq : (n : Int) = p.Fexp - q.Fexp :=
        Int.natAbs_of_nonneg hdiff_nonneg
      have hn_pos : 0 < n := by
        have hn_int : (0 : Int) < (n : Int) := by
          rw [hn_eq]
          omega
        exact_mod_cast hn_int
      have hshift_val :
          _root_.F2R (beta:=beta) (Fshift (beta:=beta) radix n p) =
            _root_.F2R (beta:=beta) p := by
        have hradix_pos_real : (0 : ℝ) < (radix : ℝ) := by
          exact_mod_cast (by omega : (0 : Int) < radix)
        have hradix_ne_real : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos_real
        have hbeta_radix_real : (beta : ℝ) = (radix : ℝ) := congrArg (Int.cast) hbeta_radix
        have hsplit :
            (radix : ℝ) ^ p.Fexp =
              (radix : ℝ) ^ (n : Int) * (radix : ℝ) ^ (p.Fexp - (n : Int)) := by
          have hsum : (n : Int) + (p.Fexp - (n : Int)) = p.Fexp := by omega
          simpa [hsum] using
            (zpow_add₀ hradix_ne_real (n : Int) (p.Fexp - (n : Int)))
        simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, Fshift,
          FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp,
          Int.cast_mul, Int.cast_pow]
        rw [hbeta_radix_real, hsplit]
        have hzpow_n : (radix : ℝ) ^ (n : Int) = (radix : ℝ) ^ n :=
          zpow_ofNat (radix : ℝ) n
        rw [hzpow_n]
        ring
      have hshift_lt_q :
          _root_.F2R (beta:=beta) (Fshift (beta:=beta) radix n p) <
            _root_.F2R (beta:=beta) q := by
        rw [hshift_val]
        exact hp_lt_q
      have hshift_exp :
          (Fshift (beta:=beta) radix n p).Fexp = q.Fexp := by
        simp only [Fshift, FloatSpec.Core.Defs.FlocqFloat.Fexp]
        rw [hn_eq]
        omega
      have hshift_fnum_lt : (Fshift (beta:=beta) radix n p).Fnum < q.Fnum := by
        simp only [_root_.F2R, FloatSpec.Core.Defs.F2R] at hshift_lt_q
        rw [hshift_exp] at hshift_lt_q
        have hpow_pos : (0 : ℝ) < (beta : ℝ) ^ q.Fexp := zpow_pos hbeta_pos q.Fexp
        have hreal :
            ((Fshift (beta:=beta) radix n p).Fnum : ℝ) < (q.Fnum : ℝ) :=
          lt_of_mul_lt_mul_right hshift_lt_q (le_of_lt hpow_pos)
        exact_mod_cast hreal
      have hq_nonneg : 0 ≤ _root_.F2R (beta:=beta) q :=
        le_trans hp_nonneg (le_of_lt hp_lt_q)
      have hshift_nonneg :
          0 ≤ _root_.F2R (beta:=beta) (Fshift (beta:=beta) radix n p) := by
        rw [hshift_val]
        exact hp_nonneg
      have hq_fnum_nonneg : 0 ≤ q.Fnum :=
        FloatSpec.Core.Float_prop.ge_0_F2R (beta := beta) q hbeta hq_nonneg
      have hshift_fnum_nonneg : 0 ≤ (Fshift (beta:=beta) radix n p).Fnum :=
        FloatSpec.Core.Float_prop.ge_0_F2R
          (beta := beta) (Fshift (beta:=beta) radix n p) hbeta hshift_nonneg
      have hq_fnum_pos : 0 < q.Fnum := by omega
      have hshift_abs_lt_q_abs :
          Int.natAbs (Fshift (beta:=beta) radix n p).Fnum < Int.natAbs q.Fnum := by
        have hshift_abs :
            Int.natAbs (Fshift (beta:=beta) radix n p).Fnum =
              ((Fshift (beta:=beta) radix n p).Fnum).toNat := by
          omega
        have hq_abs : Int.natAbs q.Fnum = q.Fnum.toNat := by
          omega
        rw [hshift_abs, hq_abs]
        exact (Int.toNat_lt_toNat hq_fnum_pos).mpr hshift_fnum_lt
      have hzdigits_le :
          FloatSpec.Core.Digits.Zdigits radix (Fshift (beta:=beta) radix n p).Fnum ≤
            FloatSpec.Core.Digits.Zdigits radix q.Fnum := by
        have hzd :=
          (FloatSpec.Core.Digits.Zdigits_le_Zdigits
            (beta := radix) (h_beta := hradix)
            (n := (Fshift (beta:=beta) radix n p).Fnum) (m := q.Fnum) (hβ := hradix))
            ⟨by omega, hshift_abs_lt_q_abs⟩
        rcases hzd with ⟨dm, hdm, hle⟩
        simpa [hdm] using hle
      have hfdigit_le :
          Fdigit (beta:=beta) radix (Fshift (beta:=beta) radix n p) ≤
            Fdigit (beta:=beta) radix q := by
        unfold Fdigit
        rw [pffDigit_of_one_lt radix _ hradix,
          pffDigit_of_one_lt radix _ hradix]
        exact Int.toNat_le_toNat hzdigits_le
      have hfd_shift :
          Fdigit (beta:=beta) radix (Fshift (beta:=beta) radix n p) =
            Fdigit (beta:=beta) radix p + n := by
        unfold Fdigit Fshift
        rw [pffDigit_of_one_lt radix _ hradix,
          pffDigit_of_one_lt radix _ hradix]
        have hn_nonneg : 0 ≤ (n : Int) := by exact_mod_cast Nat.zero_le n
        have hmul :=
          (FloatSpec.Core.Digits.Zdigits_mult_Zpower
            (beta := radix) (n := p.Fnum) (k := (n : Int)) (h_beta := hradix))
            ⟨hp_nonzero, hn_nonneg⟩
        rcases hmul with ⟨dn, hdn, hres⟩
        have hdn_nonneg :
            0 ≤ FloatSpec.Core.Digits.Zdigits radix p.Fnum := by
          exact
            FloatSpec.Core.Digits.Zdigits_ge_0
              (beta := radix) (n := p.Fnum) trivial
        have hpow_arg :
            p.Fnum * radix ^ (n : Int).natAbs = p.Fnum * radix ^ n := by
          simp
        have hres' :
            FloatSpec.Core.Digits.Zdigits radix (p.Fnum * radix ^ n) =
              FloatSpec.Core.Digits.Zdigits radix p.Fnum + (n : Int) := by
          simpa [hpow_arg, hdn] using hres
        rw [hres']
        rw [Int.toNat_add hdn_nonneg hn_nonneg]
        simp
      have hprec_add_le : precision + n ≤ precision := by
        calc
          precision + n = Fdigit (beta:=beta) radix (Fshift (beta:=beta) radix n p) := by
            rw [hfd_shift, hp_digit]
          _ ≤ Fdigit (beta:=beta) radix q := hfdigit_le
          _ = precision := hq_digit
      omega
    · right
      refine ⟨hq_exp_eq.symm, ?_⟩
      have hpow_pos : (0 : ℝ) < (beta : ℝ) ^ p.Fexp := zpow_pos hbeta_pos p.Fexp
      have hp_lt_q_unfold := hp_lt_q
      simp only [_root_.F2R, FloatSpec.Core.Defs.F2R] at hp_lt_q_unfold
      rw [hq_exp_eq] at hp_lt_q_unfold
      have hreal : (p.Fnum : ℝ) < (q.Fnum : ℝ) :=
        lt_of_mul_lt_mul_right hp_lt_q_unfold (le_of_lt hpow_pos)
      exact_mod_cast hreal


noncomputable def vNumPrecision_check
    (b : Fbound_skel) (radix : Int) (precision : Nat) (n : Int) : Unit :=
  ()

/-- Coq: `vNumPrecision` — if `digit radix n ≤ precision` then
`|n| < b.vNum`. -/
theorem vNumPrecision
    (b : Fbound_skel) (radix : Int) (precision : Nat) (n : Int) :
    ⦃⌜1 < radix ∧ b.vNum = Zpower_nat radix precision ∧ digit radix n ≤ precision⌝⦄
    (pure (vNumPrecision_check b radix precision n) : Id Unit)
    ⦃⇓_ => ⌜|n| < b.vNum⌝⦄ := by
  intro ⟨hradix, hvNum, hdigit⟩
  simp only [wp, PostCond.noThrow, pure, vNumPrecision_check,
    Id.run, ULift.up_down]
  unfold digit at hdigit
  rw [pffDigit_of_one_lt radix n hradix] at hdigit
  have hdigits_nonneg :
      0 ≤ FloatSpec.Core.Digits.Zdigits radix n := by
    exact
      FloatSpec.Core.Digits.Zdigits_ge_0
        (beta := radix) (n := n) trivial
  have hdigits_bound :
      FloatSpec.Core.Digits.Zdigits radix n ≤ (precision : Int) := by
    have hdigit_int :
        ((Int.toNat (FloatSpec.Core.Digits.Zdigits radix n) : Nat) : Int)
          ≤ (precision : Int) := by
      exact_mod_cast hdigit
    simpa [Int.toNat_of_nonneg hdigits_nonneg] using hdigit_int
  have hnum_lt :
      (n.natAbs : Int) < radix ^ ((precision : Int).natAbs) := by
    have hpow :=
      FloatSpec.Core.Digits.Zpower_gt_Zdigits
        (beta := radix) (h_beta := hradix)
        (e := (precision : Int)) (x := n) (hβ := hradix)
        trivial
    simpa only [wp, PostCond.noThrow, pure,
      Id.run, Int.cast_ofNat] using hpow hdigits_bound
  rw [hvNum]
  simpa [Zpower_nat, Int.natAbs_of_nonneg
    (show 0 ≤ (precision : Int) by exact_mod_cast Nat.zero_le precision)] using hnum_lt

-- Coq: `NotDividesDigit` — if 1 < r and v ≠ 0 then v does not divide r^(digit r v)
noncomputable def NotDividesDigit_check (r v : Int) : Unit :=
  ()

/-- Coq: `NotDividesDigit` — no divisibility at the digit boundary. -/
theorem NotDividesDigit (r v : Int) :
    ⦃⌜1 < r ∧ v ≠ 0⌝⦄
    (pure (NotDividesDigit_check r v) : Id Unit)
    ⦃⇓_ => ⌜¬ Zdivides v (Zpower_nat r (digit r v))⌝⦄ := by
  intro ⟨hr, hv⟩
  simp only [wp, PostCond.noThrow, pure, NotDividesDigit_check,
    Id.run, ULift.up_down]
  intro hdiv
  have hdiv_le :
      |Zpower_nat r (digit r v)| ≤ |v| :=
    (ZDividesLe v (Zpower_nat r (digit r v))) ⟨hv, hdiv⟩
  have hdigits_nonneg :
      0 ≤ FloatSpec.Core.Digits.Zdigits r v := by
    exact
      FloatSpec.Core.Digits.Zdigits_ge_0
        (beta := r) (n := v) trivial
  have hnatAbs :
      (FloatSpec.Core.Digits.Zdigits r v).natAbs =
        Int.toNat (FloatSpec.Core.Digits.Zdigits r v) := by
    apply Nat.cast_injective (R := Int)
    rw [Int.natAbs_of_nonneg hdigits_nonneg,
      Int.toNat_of_nonneg hdigits_nonneg]
  have hupper : |v| < Zpower_nat r (digit r v) := by
    have hbounds :=
      FloatSpec.Core.Digits.Zdigits_correct_from_nonzero_payload r v hr hv
    simpa [Zpower_nat, digit, pffDigit_of_one_lt r v hr, hnatAbs] using hbounds.2
  have hpow_pos : 0 < Zpower_nat r (digit r v) := by
    simp [Zpower_nat]
    exact pow_pos (by omega : 0 < r) _
  have hdiv_le' : Zpower_nat r (digit r v) ≤ |v| := by
    simpa [abs_of_pos hpow_pos] using hdiv_le
  exact (not_lt_of_ge hdiv_le') hupper

-- Coq: `ZquotientPos` — if z1 ≥ 0 and z2 ≥ 0 then Zquotient z1 z2 ≥ 0
noncomputable def ZquotientPos_check (z1 z2 : Int) : Unit :=
  ()

/-- Coq: `ZquotientPos` — positivity of quotient under nonnegativity hypotheses. -/
theorem ZquotientPos (z1 z2 : Int) :
    ⦃⌜0 ≤ z1 ∧ 0 ≤ z2⌝⦄
    (pure (ZquotientPos_check z1 z2) : Id Unit)
    ⦃⇓_ => ⌜0 ≤ Zquotient z1 z2⌝⦄ := by
  intro h
  simp only [wp, PostCond.noThrow, pure, ZquotientPos_check, Id.run,
    ULift.up_down]
  show 0 ≤ Zquotient z1 z2
  rw [Zquotient_eq_tdiv]
  exact Int.tdiv_nonneg h.1 h.2

-- Coq: `inject_nat_convert` — if p = Zpos q then Z_of_nat (nat_of_P q) = p
noncomputable def inject_nat_convert_check (p : Int) (q : Positive) : Unit :=
  ()

theorem inject_nat_convert (p : Int) (q : Positive) :
    ⦃⌜p = Int.ofNat (nat_of_P q)⌝⦄
    (pure (inject_nat_convert_check p q) : Id Unit)
    ⦃⇓_ => ⌜Int.ofNat (nat_of_P q) = p⌝⦄ := by
  intro h
  simp only [wp, PostCond.noThrow, pure, inject_nat_convert_check, Id.run,
    ULift.up_down]
  show Int.ofNat (nat_of_P q) = p
  exact h.symm

-- Coq: `Zabs_eq_opp` — if x ≤ 0 then |x| = -x
noncomputable def Zabs_eq_opp_check (x : Int) : Unit :=
  ()

theorem Zabs_eq_opp (x : Int) :
    ⦃⌜x ≤ 0⌝⦄
    (pure (Zabs_eq_opp_check x) : Id Unit)
    ⦃⇓_ => ⌜|x| = -x⌝⦄ := by
  intro h
  simp only [wp, PostCond.noThrow, pure, Zabs_eq_opp_check, Id.run,
    ULift.up_down]
  show |x| = -x
  exact abs_of_nonpos h

-- Coq: `Zabs_Zs` — |succ z| ≤ succ |z|
noncomputable def Zabs_Zs_check (z : Int) : Unit :=
  ()

theorem Zabs_Zs (z : Int) :
    ⦃⌜True⌝⦄
    (pure (Zabs_Zs_check z) : Id Unit)
    ⦃⇓_ => ⌜|Int.succ z| ≤ Int.succ |z|⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, Zabs_Zs_check, Id.run,
    ULift.up_down]
  show |Int.succ z| ≤ Int.succ |z|
  simp only [Int.succ]
  calc |z + 1| ≤ |z| + |(1 : ℤ)| := abs_add_le z 1
  _ = |z| + 1 := by norm_num

-- Coq: `lt_Zlt_inv` — if Z_of_nat n < Z_of_nat m then n < m
noncomputable def lt_Zlt_inv_check (n m : Nat) : Unit :=
  ()

theorem lt_Zlt_inv (n m : Nat) :
    ⦃⌜Int.ofNat n < Int.ofNat m⌝⦄
    (pure (lt_Zlt_inv_check n m) : Id Unit)
    ⦃⇓_ => ⌜n < m⌝⦄ := by
  intro h
  simp only [wp, PostCond.noThrow, pure, lt_Zlt_inv_check, Id.run,
    ULift.up_down]
  show n < m
  exact Int.ofNat_lt.mp h

-- Coq: `Zle_Zpred` — if x < y then x ≤ pred y
noncomputable def Zle_Zpred_check (x y : Int) : Unit :=
  ()

theorem Zle_Zpred (x y : Int) :
    ⦃⌜x < y⌝⦄
    (pure (Zle_Zpred_check x y) : Id Unit)
    ⦃⇓_ => ⌜x ≤ Int.pred y⌝⦄ := by
  intro h
  simp only [wp, PostCond.noThrow, pure, Zle_Zpred_check, Id.run,
    ULift.up_down]
  show x ≤ Int.pred y
  have h' : x < y := h
  simp only [Int.pred]; omega

-- Coq: `NconvertO` — nat_of_P p <> 0 for positive p
noncomputable def NconvertO_check (p : Positive) : Unit :=
  ()

theorem NconvertO (p : Positive) :
    ⦃⌜True⌝⦄
    (pure (NconvertO_check p) : Id Unit)
    ⦃⇓_ => ⌜nat_of_P p ≠ 0⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, NconvertO_check, Id.run,
    ULift.up_down]
  show nat_of_P p ≠ 0
  simp [nat_of_P]

-- Coq: `convert_not_O` — nat_of_P p <> 0 for positive p (alias of NconvertO)
noncomputable def convert_not_O_check (p : Positive) : Unit :=
  ()

theorem convert_not_O (p : Positive) :
    ⦃⌜True⌝⦄
    (pure (convert_not_O_check p) : Id Unit)
    ⦃⇓_ => ⌜nat_of_P p ≠ 0⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, convert_not_O_check, Id.run,
    ULift.up_down]
  show nat_of_P p ≠ 0
  simp [nat_of_P]

-- Coq: `Zle_Zabs` — z ≤ |z|
noncomputable def Zle_Zabs_check (z : Int) : Unit :=
  ()

theorem Zle_Zabs (z : Int) :
    ⦃⌜True⌝⦄
    (pure (Zle_Zabs_check z) : Id Unit)
    ⦃⇓_ => ⌜z ≤ |z|⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, Zle_Zabs_check, Id.run,
    ULift.up_down]
  show z ≤ |z|
  exact le_abs_self z

-- We declare the `_check` and theorem later after `pff_to_flocq` is defined.

-- Coq: `absolu_lt_nz` — if z ≠ 0 then 0 < Z.abs_nat z
noncomputable def absolu_lt_nz_check (z : Int) : Unit :=
  ()

theorem absolu_lt_nz (z : Int) :
    ⦃⌜z ≠ 0⌝⦄
    (pure (absolu_lt_nz_check z) : Id Unit)
    ⦃⇓_ => ⌜0 < Int.natAbs z⌝⦄ := by
  intro h
  simp only [wp, PostCond.noThrow, pure, absolu_lt_nz_check, Id.run,
    ULift.up_down]
  show 0 < Int.natAbs z
  exact Int.natAbs_pos.mpr h

-- List operations used in Pff
def list_sum (l : List ℝ) : ℝ :=
  l.foldr (· + ·) 0

def list_prod (l : List ℝ) : ℝ :=
  l.foldr (· * ·) 1

-- Enumerating consecutive integers (Coq: mZlist and friends)
def mZlist_aux (p : Int) (n : Nat) : List Int :=
  match n with
  | 0 => [p]
  | Nat.succ n' => p :: mZlist_aux (p + 1) n'

private lemma mZlist_aux_mem (n : Nat) (p q : Int) (hpq : p ≤ q) (hqpn : q ≤ p + ↑n) :
    q ∈ mZlist_aux p n := by
  induction n generalizing p with
  | zero => simp [mZlist_aux]; omega
  | succ n ih =>
    simp only [mZlist_aux]
    rcases eq_or_lt_of_le hpq with rfl | hlt
    · exact List.mem_cons.mpr (Or.inl rfl)
    · right; exact ih (p + 1) (by omega) (by push_cast; omega)

private lemma mZlist_aux_lower (n : Nat) (p q : Int) (hmem : q ∈ mZlist_aux p n) :
    p ≤ q := by
  induction n generalizing p with
  | zero => simp [mZlist_aux] at hmem; omega
  | succ n ih =>
    unfold mZlist_aux at hmem
    rcases List.mem_cons.mp hmem with rfl | hmem'
    · omega
    · have := ih (p + 1) hmem'; omega

private lemma mZlist_aux_upper (n : Nat) (p q : Int) (hmem : q ∈ mZlist_aux p n) :
    q ≤ p + ↑n := by
  induction n generalizing p with
  | zero => simp [mZlist_aux] at hmem; omega
  | succ n ih =>
    unfold mZlist_aux at hmem
    rcases List.mem_cons.mp hmem with rfl | hmem'
    · omega
    · have := ih (p + 1) hmem'; push_cast; omega

noncomputable def mZlist_aux_correct_check (n : Nat) (p q : Int) : Unit :=
  ()

/-- Coq: `mZlist_aux_correct` — if `p ≤ q ≤ p + Z_of_nat n` then `q ∈ mZlist_aux p n`.
We mirror the statement using the project's hoare-triple style; the proof is below. -/
theorem mZlist_aux_correct (n : Nat) (p q : Int) :
    ⦃⌜p ≤ q ∧ q ≤ p + Int.ofNat n⌝⦄
    (pure (mZlist_aux_correct_check n p q) : Id Unit)
    ⦃⇓_ => ⌜List.Mem q (mZlist_aux p n)⌝⦄ := by
  intro ⟨hpq, hqpn⟩
  simp only [wp, PostCond.noThrow, pure, mZlist_aux_correct_check, Id.run,
    ULift.up_down]
  show List.Mem q (mZlist_aux p n)
  exact mZlist_aux_mem n p q hpq hqpn

noncomputable def mZlist_aux_correct_rev1_check (n : Nat) (p q : Int) : Unit :=
  ()

/-- Coq: `mZlist_aux_correct_rev1` — if `q ∈ mZlist_aux p n` then `p ≤ q`.
Hoare-triple wrapper; the proof is below. -/
theorem mZlist_aux_correct_rev1 (n : Nat) (p q : Int) :
    ⦃⌜List.Mem q (mZlist_aux p n)⌝⦄
    (pure (mZlist_aux_correct_rev1_check n p q) : Id Unit)
    ⦃⇓_ => ⌜p ≤ q⌝⦄ := by
  intro hmem
  simp only [wp, PostCond.noThrow, pure, mZlist_aux_correct_rev1_check,
    Id.run, ULift.up_down]
  show p ≤ q
  exact mZlist_aux_lower n p q hmem

noncomputable def mZlist_aux_correct_rev2_check (n : Nat) (p q : Int) : Unit :=
  ()

/-- Coq: `mZlist_aux_correct_rev2` — membership implies upper bound by `p + n`.
Hoare-triple wrapper; the proof is below. -/
theorem mZlist_aux_correct_rev2 (n : Nat) (p q : Int) :
    ⦃⌜List.Mem q (mZlist_aux p n)⌝⦄
    (pure (mZlist_aux_correct_rev2_check n p q) : Id Unit)
    ⦃⇓_ => ⌜q ≤ p + Int.ofNat n⌝⦄ := by
  intro hmem
  simp only [wp, PostCond.noThrow, pure, mZlist_aux_correct_rev2_check,
    Id.run, ULift.up_down]
  show q ≤ p + Int.ofNat n
  exact mZlist_aux_upper n p q hmem

def mZlist (p q : Int) : List Int :=
  let d := q - p
  if h0 : d = 0 then
    [p]
  else if hpos : d > 0 then
    mZlist_aux p (Int.toNat d)
  else
    []

private lemma mZlist_mem (p q r : Int) (hpr : p ≤ r) (hrq : r ≤ q) :
    r ∈ mZlist p q := by
  simp only [mZlist]
  split_ifs with h0 hpos
  · -- d = 0, so p = q = r
    exact List.mem_singleton.mpr (by omega)
  · -- d > 0
    exact mZlist_aux_mem _ p r hpr (by
      have : (q - p).toNat = (q - p).toNat := rfl
      rw [show (↑(q - p).toNat : ℤ) = q - p from Int.toNat_of_nonneg (by omega)]
      omega)
  · -- d < 0, contradicts p ≤ r ≤ q
    exfalso; omega

private lemma mZlist_lower (p q r : Int) (hmem : r ∈ mZlist p q) :
    p ≤ r := by
  simp only [mZlist] at hmem
  split_ifs at hmem with h0 hpos
  · simp at hmem; omega
  · exact mZlist_aux_lower _ p r hmem
  · simp at hmem

private lemma mZlist_upper (p q r : Int) (hmem : r ∈ mZlist p q) :
    r ≤ q := by
  simp only [mZlist] at hmem
  split_ifs at hmem with h0 hpos
  · simp at hmem; omega
  · have h := mZlist_aux_upper _ p r hmem
    rw [show (↑(q - p).toNat : ℤ) = q - p from Int.toNat_of_nonneg (by omega)] at h
    omega
  · simp at hmem

noncomputable def mZlist_correct_check (p q r : Int) : Unit :=
  ()

/-- Coq: `mZlist_correct` — if `p ≤ r ≤ q` then `r ∈ mZlist p q`.
Hoare-triple wrapper; the proof is below. -/
theorem mZlist_correct (p q r : Int) :
    ⦃⌜p ≤ r ∧ r ≤ q⌝⦄
    (pure (mZlist_correct_check p q r) : Id Unit)
    ⦃⇓_ => ⌜List.Mem r (mZlist p q)⌝⦄ := by
  intro ⟨hpr, hrq⟩
  simp only [wp, PostCond.noThrow, pure, mZlist_correct_check, Id.run,
    ULift.up_down]
  show List.Mem r (mZlist p q)
  exact mZlist_mem p q r hpr hrq

noncomputable def mZlist_correct_rev1_check (p q r : Int) : Unit :=
  ()

/-- Coq: `mZlist_correct_rev1` — membership implies lower bound `p ≤ r`. -/
theorem mZlist_correct_rev1 (p q r : Int) :
    ⦃⌜List.Mem r (mZlist p q)⌝⦄
    (pure (mZlist_correct_rev1_check p q r) : Id Unit)
    ⦃⇓_ => ⌜p ≤ r⌝⦄ := by
  intro hmem
  simp only [wp, PostCond.noThrow, pure, mZlist_correct_rev1_check, Id.run,
    ULift.up_down]
  show p ≤ r
  exact mZlist_lower p q r hmem

noncomputable def mZlist_correct_rev2_check (p q r : Int) : Unit :=
  ()

/-- Coq: `mZlist_correct_rev2` — membership implies upper bound `r ≤ q`. -/
theorem mZlist_correct_rev2 (p q r : Int) :
    ⦃⌜List.Mem r (mZlist p q)⌝⦄
    (pure (mZlist_correct_rev2_check p q r) : Id Unit)
    ⦃⇓_ => ⌜r ≤ q⌝⦄ := by
  intro hmem
  simp only [wp, PostCond.noThrow, pure, mZlist_correct_rev2_check, Id.run,
    ULift.up_down]
  show r ≤ q
  exact mZlist_upper p q r hmem

-- Cartesian product list (Coq: mProd)
def mProd {A B : Type} (l1 : List A) (l2 : List B) : List (A × B) :=
  match l2 with
  | [] => []
  | b :: l2' => (l1.map (fun a => (a, b))) ++ mProd l1 l2'

private lemma mProd_mem {A B : Type} (l1 : List A) (l2 : List B) (a : A) (b : B)
    (ha : a ∈ l1) (hb : b ∈ l2) : (a, b) ∈ mProd l1 l2 := by
  induction l2 with
  | nil => simp at hb
  | cons c l2' ih =>
    simp only [mProd, List.mem_append]
    rcases List.mem_cons.mp hb with rfl | hb'
    · left; exact List.mem_map.mpr ⟨a, ha, rfl⟩
    · right; exact ih hb'

private lemma mProd_fst {A B : Type} (l1 : List A) (l2 : List B) (a : A) (b : B)
    (hmem : (a, b) ∈ mProd l1 l2) : a ∈ l1 := by
  induction l2 with
  | nil => simp [mProd] at hmem
  | cons c l2' ih =>
    simp only [mProd, List.mem_append] at hmem
    rcases hmem with hmem1 | hmem2
    · rw [List.mem_map] at hmem1
      obtain ⟨a', ha', heq⟩ := hmem1
      have := (Prod.mk.inj heq).1
      rwa [this] at ha'
    · exact ih hmem2

private lemma mProd_snd {A B : Type} (l1 : List A) (l2 : List B) (a : A) (b : B)
    (hmem : (a, b) ∈ mProd l1 l2) : b ∈ l2 := by
  induction l2 with
  | nil => simp [mProd] at hmem
  | cons c l2' ih =>
    simp only [mProd, List.mem_append] at hmem
    rcases hmem with hmem1 | hmem2
    · rw [List.mem_map] at hmem1
      obtain ⟨_, _, heq⟩ := hmem1
      exact List.mem_cons.mpr (Or.inl (Prod.mk.inj heq).2.symm)
    · exact List.mem_cons_of_mem _ (ih hmem2)

noncomputable def mProd_correct_check {A B : Type}
    (l1 : List A) (l2 : List B) (a : A) (b : B) : Unit :=
  ()

/-- Coq: `mProd_correct` — if `a ∈ l1` and `b ∈ l2` then `(a,b) ∈ mProd l1 l2`. -/
theorem mProd_correct {A B : Type}
    (l1 : List A) (l2 : List B) (a : A) (b : B) :
    ⦃⌜List.Mem a l1 ∧ List.Mem b l2⌝⦄
    (pure (mProd_correct_check l1 l2 a b) : Id Unit)
    ⦃⇓_ => ⌜List.Mem (a, b) (mProd l1 l2)⌝⦄ := by
  intro ⟨ha, hb⟩
  simp only [wp, PostCond.noThrow, pure, mProd_correct_check, Id.run,
    ULift.up_down]
  show List.Mem (a, b) (mProd l1 l2)
  exact mProd_mem l1 l2 a b ha hb

noncomputable def mProd_correct_rev1_check {A B : Type}
    (l1 : List A) (l2 : List B) (a : A) (b : B) : Unit :=
  ()

/-- Coq: `mProd_correct_rev1` — if `(a,b) ∈ mProd l1 l2` then `a ∈ l1`. -/
theorem mProd_correct_rev1 {A B : Type}
    (l1 : List A) (l2 : List B) (a : A) (b : B) :
    ⦃⌜List.Mem (a, b) (mProd l1 l2)⌝⦄
    (pure (mProd_correct_rev1_check l1 l2 a b) : Id Unit)
    ⦃⇓_ => ⌜List.Mem a l1⌝⦄ := by
  intro hmem
  simp only [wp, PostCond.noThrow, pure, mProd_correct_rev1_check, Id.run,
    ULift.up_down]
  show List.Mem a l1
  exact mProd_fst l1 l2 a b hmem

noncomputable def mProd_correct_rev2_check {A B : Type}
    (l1 : List A) (l2 : List B) (a : A) (b : B) : Unit :=
  ()

/-- Coq: `mProd_correct_rev2` — if `(a,b) ∈ mProd l1 l2` then `b ∈ l2`. -/
theorem mProd_correct_rev2 {A B : Type}
    (l1 : List A) (l2 : List B) (a : A) (b : B) :
    ⦃⌜List.Mem (a, b) (mProd l1 l2)⌝⦄
    (pure (mProd_correct_rev2_check l1 l2 a b) : Id Unit)
    ⦃⇓_ => ⌜List.Mem b l2⌝⦄ := by
  intro hmem
  simp only [wp, PostCond.noThrow, pure, mProd_correct_rev2_check, Id.run,
    ULift.up_down]
  show List.Mem b l2
  exact mProd_snd l1 l2 a b hmem

noncomputable def in_map_inv_check {A B : Type}
    (f : A → B) (l : List A) (x : A) : Unit :=
  ()

/-- Coq: `in_map_inv` — if `f` is injective and `f x ∈ map f l` then `x ∈ l`. -/
theorem in_map_inv {A B : Type}
    (f : A → B) (l : List A) (x : A) :
    ⦃⌜(∀ a b, f a = f b → a = b) ∧ List.Mem (f x) (l.map f)⌝⦄
    (pure (in_map_inv_check f l x) : Id Unit)
    ⦃⇓_ => ⌜List.Mem x l⌝⦄ := by
  intro ⟨hinj, hmem⟩
  simp only [wp, PostCond.noThrow, pure, in_map_inv_check, Id.run,
    ULift.up_down]
  show List.Mem x l
  have hinj' : ∀ a b, f a = f b → a = b := hinj
  have hmem' : f x ∈ l.map f := hmem
  rw [List.mem_map] at hmem'
  obtain ⟨y, hy_mem, hy_eq⟩ := hmem'
  exact hinj' x y hy_eq.symm ▸ hy_mem

-- Coq: `boundNat n := Float 1 (digit radix n)`.
def boundNat {beta : Int} [ValidRadix beta]
    (radix : Int) (n : Nat) : FloatSpec.Core.Defs.FlocqFloat beta :=
  ⟨1, Int.ofNat (digit radix (Int.ofNat n))⟩

noncomputable def boundNatCorrect_check {beta : Int} [ValidRadix beta]
    (radix : Int) (n : Nat) : Unit :=
  ()

/-- Coq: `boundNatCorrect`.

The local statement carries the `beta = radix` bridge needed because
`FlocqFloat beta` stores the semantic base as the type parameter. -/
theorem boundNatCorrect {beta : Int} [ValidRadix beta]
    (radix : Int) (n : Nat) :
    ⦃⌜beta = radix ∧ 1 < radix⌝⦄
    (pure (boundNatCorrect_check (beta:=beta) radix n) : Id Unit)
    ⦃⇓_ => ⌜(n : ℝ) < _root_.F2R (beta:=beta)
      (boundNat (beta:=beta) radix n)⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hradix⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure, boundNatCorrect_check, Id.run,
    ULift.up_down]
  let d : Nat := digit radix (Int.ofNat n)
  have hmore_int : (n : Int) < radix ^ d := by
    by_cases hnzero : (Int.ofNat n) = 0
    · have hn : n = 0 := Int.ofNat_eq_zero.mp hnzero
      subst n
      simp [d, digit, pffDigit_of_one_lt radix 0 hradix, Zpower_nat,
        FloatSpec.Core.Digits.Zdigits]
    · have hbounds :=
        FloatSpec.Core.Digits.Zdigits_correct_from_nonzero_payload radix (Int.ofNat n) hradix hnzero
      have hnonneg : 0 ≤ FloatSpec.Core.Digits.Zdigits radix (Int.ofNat n) :=
        FloatSpec.Core.Digits.Zdigits_ge_0 radix (Int.ofNat n) trivial
      have hnat :
          (FloatSpec.Core.Digits.Zdigits radix (Int.ofNat n)).natAbs =
            (FloatSpec.Core.Digits.Zdigits radix (Int.ofNat n)).toNat := by
        apply Nat.cast_injective (R := Int)
        rw [Int.natAbs_of_nonneg hnonneg, Int.toNat_of_nonneg hnonneg]
      have hmore' :
          |(Int.ofNat n)| <
            Zpower_nat radix
              (Int.toNat (FloatSpec.Core.Digits.Zdigits radix (Int.ofNat n))) := by
        rw [← hnat]
        simpa [Zpower_nat] using hbounds.2
      simpa [d, digit, pffDigit, hradix, Zpower_nat] using hmore'
  have hmore_real : (n : ℝ) < ((radix ^ d : Int) : ℝ) := by
    exact_mod_cast hmore_int
  have hpow_cast : ((radix ^ d : Int) : ℝ) = (radix : ℝ) ^ d := by
    norm_num
  calc
    (n : ℝ) < ((radix ^ d : Int) : ℝ) := hmore_real
    _ = (radix : ℝ) ^ d := hpow_cast
    _ = _root_.F2R (beta:=radix) (boundNat (beta:=radix) radix n) := by
      simp [_root_.F2R, FloatSpec.Core.Defs.F2R, boundNat, d]

-- Coq: `boundR r := boundNat (Z.abs_nat (up (Rabs r)))`.
-- Coq's `up` is the least integer strictly above its argument, including at
-- integral inputs; this is `floor x + 1`, not the ordinary ceiling.
noncomputable def boundR {beta : Int} [ValidRadix beta]
    (radix : Int) (r : ℝ) : FloatSpec.Core.Defs.FlocqFloat beta :=
  boundNat (beta:=beta) radix (Int.natAbs (Int.floor |r| + 1))

noncomputable def boundRrOpp_check {beta : Int} [ValidRadix beta]
    (radix : Int) (r : ℝ) : Unit :=
  ()

/-- Coq: `boundRrOpp`. -/
theorem boundRrOpp {beta : Int} [ValidRadix beta]
    (radix : Int) (r : ℝ) :
    ⦃⌜True⌝⦄
    (pure (boundRrOpp_check (beta:=beta) radix r) : Id Unit)
    ⦃⇓_ => ⌜boundR (beta:=beta) radix r =
      boundR (beta:=beta) radix (-r)⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, boundRrOpp_check, Id.run,
    ULift.up_down]
  simp [boundR, abs_neg]

noncomputable def boundRCorrect1_check {beta : Int} [ValidRadix beta]
    (radix : Int) (r : ℝ) : Unit :=
  ()

/-- Coq: `boundRCorrect1` — the positive sentinel `boundR r` is strictly above `r`. -/
theorem boundRCorrect1 {beta : Int} [ValidRadix beta]
    (radix : Int) (r : ℝ) :
    ⦃⌜beta = radix ∧ 1 < radix⌝⦄
    (pure (boundRCorrect1_check (beta:=beta) radix r) : Id Unit)
    ⦃⇓_ => ⌜r < _root_.F2R (beta:=beta)
      (boundR (beta:=beta) radix r)⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hradix⟩
  simp only [wp, PostCond.noThrow, pure, boundRCorrect1_check, Id.run,
    ULift.up_down]
  let u : Int := Int.floor |r| + 1
  have hu_nonneg : 0 ≤ u := by
    have hfloor : 0 ≤ Int.floor |r| := Int.floor_nonneg.mpr (abs_nonneg r)
    dsimp [u]
    omega
  have habs_nat : ((Int.natAbs u : Nat) : ℝ) = (u : ℝ) := by
    rw [Nat.cast_natAbs, abs_of_nonneg hu_nonneg]
  have habs_lt_u : |r| < (u : ℝ) := by
    simpa [u] using (Int.lt_floor_add_one |r|)
  have hr_lt_nat : r < ((Int.natAbs u : Nat) : ℝ) := by
    rw [habs_nat]
    exact lt_of_le_of_lt (le_abs_self r) habs_lt_u
  have hbound := boundNatCorrect (beta:=beta) radix
    (Int.natAbs u)
  have hbound_prop :
      ((Int.natAbs u : Nat) : ℝ) <
        _root_.F2R (beta:=beta)
          (boundNat (beta:=beta) radix (Int.natAbs u)) := by
    simpa [wp, PostCond.noThrow, pure, boundNatCorrect_check]
      using hbound ⟨hbeta, hradix⟩
  exact lt_trans hr_lt_nat (by simpa [boundR, u] using hbound_prop)

noncomputable def boundRCorrect2_check {beta : Int} [ValidRadix beta]
    (radix : Int) (r : ℝ) : Unit :=
  ()

/-- Coq: `boundRCorrect2` — the negative sentinel `Fopp (boundR r)` is strictly below `r`. -/
theorem boundRCorrect2 {beta : Int} [ValidRadix beta]
    (radix : Int) (r : ℝ) :
    ⦃⌜beta = radix ∧ 1 < radix⌝⦄
    (pure (boundRCorrect2_check (beta:=beta) radix r) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta)
      (Fopp (beta:=beta) (boundR (beta:=beta) radix r)) < r⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hradix⟩
  simp only [wp, PostCond.noThrow, pure, boundRCorrect2_check, Id.run,
    ULift.up_down]
  have hpos := boundRCorrect1 (beta:=beta) radix (-r)
  have hpos_prop :
      -r < _root_.F2R (beta:=beta) (boundR (beta:=beta) radix (-r)) := by
    simpa [wp, PostCond.noThrow, pure, boundRCorrect1_check]
      using hpos ⟨hbeta, hradix⟩
  have hopp_prop :
      _root_.F2R (beta:=beta)
        (Fopp (beta:=beta) (boundR (beta:=beta) radix r)) =
        -_root_.F2R (beta:=beta) (boundR (beta:=beta) radix r) := by
    simp [_root_.F2R, FloatSpec.Core.Defs.F2R, Fopp,
      FloatSpec.Calc.Operations.Fopp, neg_mul]
  have hsym := boundRrOpp (beta:=beta) radix r
  have hsym_prop :
      boundR (beta:=beta) radix r = boundR (beta:=beta) radix (-r) := by
    simpa [wp, PostCond.noThrow, pure, boundRrOpp_check]
      using hsym trivial
  rw [hopp_prop, hsym_prop]
  simpa using neg_lt_neg hpos_prop

-- Coq: `mBFloat`.
-- Enumerates every pair `(mantissa, exponent)` inside the finite bounded box
-- used by the min/max existence proofs.
noncomputable def mBFloat {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (r : ℝ) :
    List (FloatSpec.Core.Defs.FlocqFloat beta) :=
  (mProd
      (mZlist (-Int.pred b.vNum) (Int.pred b.vNum))
      (mZlist (-b.dExp) ((boundR (beta:=beta) radix r).Fexp))).map
    (fun p : Int × Int => ⟨p.1, p.2⟩)

private lemma mBFloat_pair_mem {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (r : ℝ)
    (p : FloatSpec.Core.Defs.FlocqFloat beta)
    (hp : p ∈ mBFloat (beta:=beta) b radix r) :
    (p.Fnum, p.Fexp) ∈
      mProd
        (mZlist (-Int.pred b.vNum) (Int.pred b.vNum))
        (mZlist (-b.dExp) ((boundR (beta:=beta) radix r).Fexp)) := by
  unfold mBFloat at hp
  rw [List.mem_map] at hp
  rcases hp with ⟨pair, hpair, hmk⟩
  cases p
  cases pair
  simp_all

noncomputable def mBPadic_Fbounded_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) : Unit :=
  ()

/-- Coq: `mBPadic_Fbounded`.

Every float enumerated by `mBFloat` satisfies the concrete `Fbounded`
predicate.  This is the finite-list bridge used by the upstream `MinEx_from_finite_box_payload` and
`MaxEx_from_finite_box_payload` proofs. -/
theorem mBPadic_Fbounded {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) :
    ⦃⌜p ∈ mBFloat (beta:=beta) b radix r⌝⦄
    (pure (mBPadic_Fbounded_check (beta:=beta) b radix p r) : Id Unit)
    ⦃⇓_ => ⌜Fbounded (beta:=beta) b p⌝⦄ := by
  intro hp
  simp only [wp, PostCond.noThrow, pure, mBPadic_Fbounded_check, Id.run,
    ULift.up_down]
  have hpair := mBFloat_pair_mem (beta:=beta) b radix r p hp
  have hnum_lower :
      -Int.pred b.vNum ≤ p.Fnum := by
    exact mZlist_lower _ _ _ (mProd_fst _ _ _ _ hpair)
  have hnum_upper :
      p.Fnum ≤ Int.pred b.vNum := by
    exact mZlist_upper _ _ _ (mProd_fst _ _ _ _ hpair)
  have hexp_lower :
      -b.dExp ≤ p.Fexp := by
    exact mZlist_lower _ _ _ (mProd_snd _ _ _ _ hpair)
  have hnum_abs : |p.Fnum| < b.vNum := by
    simp only [Int.pred] at hnum_lower hnum_upper
    exact abs_lt.mpr ⟨by omega, by omega⟩
  exact ⟨hnum_abs, hexp_lower⟩

noncomputable def mBFadic_correct1_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (r : ℝ) (q : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `mBFadic_correct1`.

Any bounded float whose exponent is within the finite box used by `mBFloat`
is enumerated by that list.  The upstream Coq theorem derives the exponent
upper bound from the real comparisons with `Fopp (boundR r)` and `boundR r`;
the local statement keeps that finite-box fact explicit so `MinEx_from_finite_box_payload` can use the
same enumeration step without adding an unrelated bridge theorem. -/
theorem mBFadic_correct1_from_exp_bound_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (r : ℝ) (q : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b q ∧
        q.Fexp ≤ (boundR (beta:=beta) radix r).Fexp⌝⦄
    (pure (mBFadic_correct1_check (beta:=beta) b radix r q) : Id Unit)
    ⦃⇓_ => ⌜q ∈ mBFloat (beta:=beta) b radix r⌝⦄ := by
  intro h
  rcases h with ⟨hbounded, hExpUpper⟩
  rcases hbounded with ⟨hnum, hExpLower⟩
  simp only [wp, PostCond.noThrow, pure, mBFadic_correct1_check, Id.run,
    ULift.up_down]
  unfold mBFloat
  rw [List.mem_map]
  refine ⟨(q.Fnum, q.Fexp), ?_, ?_⟩
  · apply mProd_mem
    · apply mZlist_mem
      · simp only [Int.pred]
        have hneg : -b.vNum < q.Fnum := (abs_lt.mp hnum).1
        omega
      · simp only [Int.pred]
        have hpos : q.Fnum < b.vNum := (abs_lt.mp hnum).2
        omega
    · apply mZlist_mem
      · exact hExpLower
      · exact hExpUpper
  · rfl

/-- Coq `mBFadic_correct1`: the two strict sentinel inequalities derive the
finite-box exponent bound; callers need not provide that derived fact. -/
theorem mBFadic_correct1 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (r : ℝ)
    (q : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜¬ is_Fzero q ∧
        _root_.F2R (Fopp (boundR (beta:=beta) beta r)) < _root_.F2R q ∧
        _root_.F2R q < _root_.F2R (boundR (beta:=beta) beta r) ∧
        Fbounded b q⌝⦄
    (pure (mBFadic_correct1_check (beta:=beta) b beta r q) : Id Unit)
    ⦃⇓_ => ⌜q ∈ mBFloat (beta:=beta) b beta r⌝⦄ := by
  intro ⟨hqne, hlower, hupper, hbounded⟩
  let sentinel := boundR (beta:=beta) beta r
  have hbetaInt : 1 < beta := ValidRadix.valid (beta:=beta)
  have hbetaReal : (1 : ℝ) < (beta : ℝ) := by exact_mod_cast hbetaInt
  have hbetaPos : (0 : ℝ) < (beta : ℝ) := lt_trans zero_lt_one hbetaReal
  have hsentinel :
      _root_.F2R (beta:=beta) sentinel =
        (beta : ℝ) ^ sentinel.Fexp := by
    simp [sentinel, boundR, boundNat, _root_.F2R, FloatSpec.Core.Defs.F2R]
  have hopp :
      _root_.F2R (beta:=beta) (Fopp (beta:=beta) sentinel) =
        -_root_.F2R (beta:=beta) sentinel := by
    simp [Fopp, FloatSpec.Calc.Operations.Fopp, _root_.F2R,
      FloatSpec.Core.Defs.F2R]
  have habs_lt :
      |_root_.F2R (beta:=beta) q| <
        _root_.F2R (beta:=beta) sentinel := by
    rw [abs_lt]
    constructor
    · rw [← hopp]
      exact hlower
    · exact hupper
  have hqnum : q.Fnum ≠ 0 := hqne
  have hnum_ge : (1 : ℝ) ≤ |(q.Fnum : ℝ)| := by
    rw [← Int.cast_abs]
    exact_mod_cast Int.one_le_abs hqnum
  have hqabs_ge :
      (beta : ℝ) ^ q.Fexp ≤ |_root_.F2R (beta:=beta) q| := by
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, abs_mul,
      abs_of_pos (zpow_pos hbetaPos q.Fexp)]
    simpa only [one_mul, Int.cast_ofNat] using
      (mul_le_mul_of_nonneg_right hnum_ge
        (le_of_lt (zpow_pos hbetaPos q.Fexp)))
  have hExpUpper : q.Fexp ≤ sentinel.Fexp := by
    by_contra hnot
    have hexp : sentinel.Fexp < q.Fexp := lt_of_not_ge hnot
    have hpow :
        (beta : ℝ) ^ sentinel.Fexp < (beta : ℝ) ^ q.Fexp :=
      (zpow_lt_zpow_iff_right₀ hbetaReal).2 hexp
    rw [hsentinel] at habs_lt
    linarith
  exact mBFadic_correct1_from_exp_bound_payload (beta:=beta) b beta r q
    ⟨hbounded, hExpUpper⟩

noncomputable def mBFadic_correct3_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (r : ℝ) : Unit :=
  ()

/-- Coq: `mBFadic_correct3`.

The negative sentinel `Fopp (boundR r)` is explicitly included in `mBFloat r`.
This is used to rule out the "all elements are above r" branch in `MinEx_from_finite_box_payload`. -/
theorem mBFadic_correct3_from_finite_box_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (r : ℝ) :
    ⦃⌜1 < b.vNum ∧ -b.dExp ≤ (boundR (beta:=beta) radix r).Fexp⌝⦄
    (pure (mBFadic_correct3_check (beta:=beta) b radix r) : Id Unit)
    ⦃⇓_ => ⌜Fopp (beta:=beta) (boundR (beta:=beta) radix r) ∈
      mBFloat (beta:=beta) b radix r⌝⦄ := by
  intro h
  rcases h with ⟨hvNum_pos, hexp_lower⟩
  simp only [wp, PostCond.noThrow, pure, mBFadic_correct3_check, Id.run,
    ULift.up_down]
  unfold mBFloat
  rw [List.mem_map]
  refine ⟨((-1 : Int), (boundR (beta:=beta) radix r).Fexp), ?_, ?_⟩
  · apply mProd_mem
    · apply mZlist_mem
      · simp only [Int.pred]
        omega
      · simp only [Int.pred]
        omega
    · apply mZlist_mem
      · exact hexp_lower
      · exact le_refl _
  · simp [Fopp, FloatSpec.Calc.Operations.Fopp, boundR, boundNat]

/-- Coq `mBFadic_correct3`. -/
theorem mBFadic_correct3 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat) (r : ℝ) :
    ⦃⌜precision ≠ 0 ∧ b.vNum = Zpower_nat beta precision⌝⦄
    (pure (mBFadic_correct3_check (beta:=beta) b beta r) : Id Unit)
    ⦃⇓_ => ⌜Fopp (beta:=beta) (boundR (beta:=beta) beta r) ∈
      mBFloat (beta:=beta) b beta r⌝⦄ := by
  intro ⟨hprecision, hvNum⟩
  have hvNum_gt : 1 < b.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ (ValidRadix.valid (beta:=beta)) hprecision
  have hBoundExp : -b.dExp ≤ (boundR (beta:=beta) beta r).Fexp := by
    have hnonneg : 0 ≤ (boundR (beta:=beta) beta r).Fexp := by
      simp [boundR, boundNat]
    exact le_trans (neg_nonpos.mpr b.dExp_nonneg) hnonneg
  exact mBFadic_correct3_from_finite_box_payload (beta:=beta) b beta r
    ⟨hvNum_gt, hBoundExp⟩

noncomputable def mBFadic_correct2_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (r : ℝ) : Unit :=
  ()

/-- Coq: `mBFadic_correct2`.

The positive sentinel `boundR r` is explicitly included in `mBFloat r`.  This
is the dual sentinel used to rule out the "all elements are below r" branch in
`MaxEx_from_finite_box_payload`. -/
theorem mBFadic_correct2 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (r : ℝ) :
    ⦃⌜1 < b.vNum ∧ -b.dExp ≤ (boundR (beta:=beta) radix r).Fexp⌝⦄
    (pure (mBFadic_correct2_check (beta:=beta) b radix r) : Id Unit)
    ⦃⇓_ => ⌜boundR (beta:=beta) radix r ∈
      mBFloat (beta:=beta) b radix r⌝⦄ := by
  intro h
  rcases h with ⟨hvNum_pos, hexp_lower⟩
  simp only [wp, PostCond.noThrow, pure, mBFadic_correct2_check, Id.run,
    ULift.up_down]
  unfold mBFloat
  rw [List.mem_map]
  refine ⟨((1 : Int), (boundR (beta:=beta) radix r).Fexp), ?_, ?_⟩
  · apply mProd_mem
    · apply mZlist_mem
      · simp only [Int.pred]
        omega
      · simp only [Int.pred]
        omega
    · apply mZlist_mem
      · exact hexp_lower
      · exact le_refl _
  · simp [boundR, boundNat]

noncomputable def mBFadic_correct4_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (r : ℝ) : Unit :=
  ()

/-- Coq: `mBFadic_correct4`.

The canonical zero float at exponent `-dExp` is included in `mBFloat r`. -/
theorem mBFadic_correct4_from_finite_box_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (r : ℝ) :
    ⦃⌜0 < b.vNum ∧ -b.dExp ≤ (boundR (beta:=beta) radix r).Fexp⌝⦄
    (pure (mBFadic_correct4_check (beta:=beta) b radix r) : Id Unit)
    ⦃⇓_ => ⌜(FloatSpec.Core.Defs.FlocqFloat.mk (beta:=beta) 0 (-b.dExp)) ∈
      mBFloat (beta:=beta) b radix r⌝⦄ := by
  intro h
  rcases h with ⟨hvNum_pos, hexp_lower⟩
  simp only [wp, PostCond.noThrow, pure, mBFadic_correct4_check, Id.run,
    ULift.up_down]
  unfold mBFloat
  rw [List.mem_map]
  refine ⟨((0 : Int), -b.dExp), ?_, ?_⟩
  · apply mProd_mem
    · apply mZlist_mem
      · simp only [Int.pred]
        omega
      · simp only [Int.pred]
        omega
    · apply mZlist_mem
      · exact le_refl _
      · exact hexp_lower
  · rfl

/-- Coq `mBFadic_correct4`. -/
theorem mBFadic_correct4 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (r : ℝ) :
    ⦃⌜True⌝⦄
    (pure (mBFadic_correct4_check (beta:=beta) b beta r) : Id Unit)
    ⦃⇓_ => ⌜(FloatSpec.Core.Defs.FlocqFloat.mk (beta:=beta) 0 (-b.dExp)) ∈
      mBFloat (beta:=beta) b beta r⌝⦄ := by
  intro _
  have hBoundExp : -b.dExp ≤ (boundR (beta:=beta) beta r).Fexp := by
    have hnonneg : 0 ≤ (boundR (beta:=beta) beta r).Fexp := by
      simp [boundR, boundNat]
    exact le_trans (neg_nonpos.mpr b.dExp_nonneg) hnonneg
  exact mBFadic_correct4_from_finite_box_payload (beta:=beta) b beta r
    ⟨b.vNum_pos, hBoundExp⟩

noncomputable def MinEx_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (r : ℝ) : Unit :=
  ()

/-- Coq: `MinEx_from_finite_box_payload`.

Existence of a lower extremal bounded float for `r`, stated over the
Coq-matching float predicate `isMin`.  The finite-box side condition is
derived locally: if a bounded candidate lies inside the exponent box it is in
`mBFloat`; otherwise its nonzero mantissa puts it beyond one of the sentinels,
while zero is handled by the canonical zero representative in the list. -/
theorem MinEx_from_finite_box_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (r : ℝ) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < b.vNum ∧
        -b.dExp ≤ (boundR (beta:=beta) radix r).Fexp⌝⦄
    (pure (MinEx_check (beta:=beta) b radix r) : Id Unit)
    ⦃⇓_ => ⌜∃ min : FloatSpec.Core.Defs.FlocqFloat beta,
        isMin (beta:=beta) b radix r min⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hradix, hvNum_gt, hBoundExp⟩
  simp only [wp, PostCond.noThrow, pure, MinEx_check, Id.run, ULift.up_down]
  let L := mBFloat (beta:=beta) b radix r
  let sentinel := boundR (beta:=beta) radix r
  have hSentinel_le : _root_.F2R (beta:=beta) (Fopp (beta:=beta) sentinel) ≤ r := by
    have hcorr := boundRCorrect2 (beta:=beta) radix r
    have hlt : _root_.F2R (beta:=beta) (Fopp (beta:=beta) sentinel) < r := by
      simpa [wp, PostCond.noThrow, pure, boundRCorrect2_check, sentinel]
        using hcorr ⟨hbeta, hradix⟩
    exact le_of_lt hlt
  have hSentinel_pos_gt : r < _root_.F2R (beta:=beta) sentinel := by
    have hcorr := boundRCorrect1 (beta:=beta) radix r
    simpa [wp, PostCond.noThrow, pure, boundRCorrect1_check, sentinel]
      using hcorr ⟨hbeta, hradix⟩
  have hNegSentinel_mem :
      Fopp (beta:=beta) sentinel ∈ L := by
    have hmem := mBFadic_correct3_from_finite_box_payload (beta:=beta) b radix r
    simpa [wp, PostCond.noThrow, pure, mBFadic_correct3_check, L, sentinel]
      using hmem ⟨hvNum_gt, hBoundExp⟩
  have hZero_mem :
      (FloatSpec.Core.Defs.FlocqFloat.mk (beta:=beta) 0 (-b.dExp)) ∈ L := by
    have hmem := mBFadic_correct4_from_finite_box_payload (beta:=beta) b radix r
    simpa [wp, PostCond.noThrow, pure, mBFadic_correct4_check, L]
      using hmem ⟨lt_trans Int.zero_lt_one hvNum_gt, hBoundExp⟩
  have hFiniteBox :
      ∀ f : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) b f → _root_.F2R (beta:=beta) f ≤ r →
          f ∈ L ∨
            _root_.F2R (beta:=beta) f ≤
              _root_.F2R (beta:=beta) (Fopp (beta:=beta) sentinel) ∨
              _root_.F2R (beta:=beta) f = 0 := by
    intro f hf_bounded hf_le
    by_cases hexp_box : f.Fexp ≤ sentinel.Fexp
    · left
      have hmem := mBFadic_correct1_from_exp_bound_payload (beta:=beta) b radix r f
      simpa [wp, PostCond.noThrow, pure, mBFadic_correct1_check, L, sentinel]
        using hmem ⟨hf_bounded, hexp_box⟩
    · push Not at hexp_box
      by_cases hfnum_zero : f.Fnum = 0
      · right
        right
        have hf_eq_zero :
            _root_.F2R (beta:=beta) f = 0 := by
          simp [hfnum_zero, _root_.F2R, FloatSpec.Core.Defs.F2R]
        exact hf_eq_zero
      · subst beta
        have hradix_pos_int : (0 : Int) < radix := lt_trans Int.zero_lt_one hradix
        have hradix_pos_real : (0 : ℝ) < (radix : ℝ) := by
          exact_mod_cast hradix_pos_int
        have hpow_f_pos : (0 : ℝ) < (radix : ℝ) ^ f.Fexp :=
          zpow_pos hradix_pos_real f.Fexp
        have hpow_s_le_f : (radix : ℝ) ^ sentinel.Fexp ≤ (radix : ℝ) ^ f.Fexp := by
          exact zpow_le_zpow_right₀ (by exact_mod_cast (le_of_lt hradix)) (le_of_lt hexp_box)
        have hsentinel_eval :
            _root_.F2R (beta:=radix) sentinel = (radix : ℝ) ^ sentinel.Fexp := by
          simp [sentinel, boundR, boundNat, _root_.F2R, FloatSpec.Core.Defs.F2R]
        have hneg_sentinel_eval :
            _root_.F2R (beta:=radix) (Fopp (beta:=radix) sentinel) =
              -_root_.F2R (beta:=radix) sentinel := by
          simp [Fopp, FloatSpec.Calc.Operations.Fopp, _root_.F2R,
            FloatSpec.Core.Defs.F2R]
        rcases lt_or_gt_of_ne hfnum_zero with hfnum_neg | hfnum_pos
        · right
          left
          have hnum_le : (f.Fnum : ℝ) ≤ -1 := by
            exact_mod_cast (by omega : f.Fnum ≤ -1)
          have hmul_le : (f.Fnum : ℝ) * (radix : ℝ) ^ f.Fexp ≤
              (-1 : ℝ) * (radix : ℝ) ^ f.Fexp := by
            exact mul_le_mul_of_nonneg_right hnum_le (le_of_lt hpow_f_pos)
          have hneg_pow_le : -((radix : ℝ) ^ f.Fexp) ≤ -((radix : ℝ) ^ sentinel.Fexp) :=
            neg_le_neg hpow_s_le_f
          calc
            _root_.F2R (beta:=radix) f
                = (f.Fnum : ℝ) * (radix : ℝ) ^ f.Fexp := by
                    simp [_root_.F2R, FloatSpec.Core.Defs.F2R]
            _ ≤ (-1 : ℝ) * (radix : ℝ) ^ f.Fexp := hmul_le
            _ = -((radix : ℝ) ^ f.Fexp) := by ring
            _ ≤ -((radix : ℝ) ^ sentinel.Fexp) := hneg_pow_le
            _ = -_root_.F2R (beta:=radix) sentinel := by
                    rw [hsentinel_eval]
            _ = _root_.F2R (beta:=radix) (Fopp (beta:=radix) sentinel) := by
                    rw [hneg_sentinel_eval]
        · have hf_ge_sentinel :
              _root_.F2R (beta:=radix) sentinel ≤ _root_.F2R (beta:=radix) f := by
            have hnum_ge_one : (1 : ℝ) ≤ (f.Fnum : ℝ) := by
              exact_mod_cast (by omega : (1 : Int) ≤ f.Fnum)
            calc
              _root_.F2R (beta:=radix) sentinel
                  = (radix : ℝ) ^ sentinel.Fexp := hsentinel_eval
              _ ≤ (radix : ℝ) ^ f.Fexp := hpow_s_le_f
              _ = (1 : ℝ) * (radix : ℝ) ^ f.Fexp := by ring
              _ ≤ (f.Fnum : ℝ) * (radix : ℝ) ^ f.Fexp := by
                    exact mul_le_mul_of_nonneg_right hnum_ge_one (le_of_lt hpow_f_pos)
              _ = _root_.F2R (beta:=radix) f := by
                    simp [_root_.F2R, FloatSpec.Core.Defs.F2R]
          exact False.elim (not_lt_of_ge (le_trans hf_ge_sentinel hf_le) hSentinel_pos_gt)
  have hlist := MinExList (beta:=beta) r L
  have hlist_prop :
      (∀ f, f ∈ L → r < _root_.F2R f) ∨
      (∃ min, min ∈ L ∧ _root_.F2R min ≤ r ∧
        ∀ f, f ∈ L → _root_.F2R f ≤ r → _root_.F2R f ≤ _root_.F2R min) := by
    simpa [wp, PostCond.noThrow, pure, MinExList_check, L] using hlist trivial
  rcases hlist_prop with hall_gt | hmin
  · have hlt := hall_gt (Fopp (beta:=beta) sentinel) hNegSentinel_mem
    exact False.elim ((not_lt_of_ge hSentinel_le) hlt)
  · rcases hmin with ⟨min, hmin_mem, hmin_le, hmax⟩
    refine ⟨min, ?_⟩
    have hmin_bounded : Fbounded (beta:=beta) b min := by
      have hb := mBPadic_Fbounded (beta:=beta) b radix min r
      simpa [wp, PostCond.noThrow, pure, mBPadic_Fbounded_check, L]
        using hb hmin_mem
    refine ⟨hmin_bounded, hmin_le, ?_⟩
    intro f hf_bounded hf_le
    rcases hFiniteBox f hf_bounded hf_le with hf_mem | hf_rest
    · exact hmax f hf_mem hf_le
    · rcases hf_rest with hf_low | hf_zero
      have hsentinel_le_min :=
        hmax (Fopp (beta:=beta) sentinel) hNegSentinel_mem hSentinel_le
      exact le_trans hf_low hsentinel_le_min
      · have hzero_le : _root_.F2R (beta:=beta)
            (FloatSpec.Core.Defs.FlocqFloat.mk (beta:=beta) 0 (-b.dExp)) ≤ r := by
          have hzero_le_r : 0 ≤ r := by
            rw [hf_zero] at hf_le
            exact hf_le
          simpa [_root_.F2R, FloatSpec.Core.Defs.F2R] using hzero_le_r
        have hzero_le_min :=
          hmax (FloatSpec.Core.Defs.FlocqFloat.mk (beta:=beta) 0 (-b.dExp))
            hZero_mem hzero_le
        rw [hf_zero]
        simpa [_root_.F2R, FloatSpec.Core.Defs.F2R] using hzero_le_min

/-- Coq `MinEx`: the finite-box obligations are consequences of the source
section assumptions and stay internal to the proof. -/
theorem MinEx {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat) (r : ℝ) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (MinEx_check (beta:=beta) b radix r) : Id Unit)
    ⦃⇓_ => ⌜∃ min : FloatSpec.Core.Defs.FlocqFloat beta,
        isMin (beta:=beta) b radix r min⌝⦄ := by
  intro ⟨hbeta, hradix, hprecision, hvNum⟩
  subst beta
  have hvNum_gt : 1 < b.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecision
  have hBoundExp : -b.dExp ≤ (boundR (beta:=radix) radix r).Fexp := by
    have hnonneg : 0 ≤ (boundR (beta:=radix) radix r).Fexp := by
      simp [boundR, boundNat]
    exact le_trans (neg_nonpos.mpr b.dExp_nonneg) hnonneg
  exact MinEx_from_finite_box_payload (beta:=radix) b radix r
    ⟨rfl, hradix, hvNum_gt, hBoundExp⟩

noncomputable def MaxEx_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (r : ℝ) : Unit :=
  ()

/-- Coq: `MaxEx_from_finite_box_payload`.

Existence of an upper extremal bounded float for `r`, stated over the
Coq-matching float predicate `isMax`.  The finite-box side condition is
derived locally: if a bounded candidate lies inside the exponent box it is in
`mBFloat`; otherwise its nonzero mantissa puts it beyond the positive sentinel,
while zero is handled by the canonical zero representative in the list. -/
theorem MaxEx_from_finite_box_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (r : ℝ) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < b.vNum ∧
        -b.dExp ≤ (boundR (beta:=beta) radix r).Fexp⌝⦄
    (pure (MaxEx_check (beta:=beta) b radix r) : Id Unit)
    ⦃⇓_ => ⌜∃ max : FloatSpec.Core.Defs.FlocqFloat beta,
        isMax (beta:=beta) b radix r max⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hradix, hvNum_gt, hBoundExp⟩
  simp only [wp, PostCond.noThrow, pure, MaxEx_check, Id.run, ULift.up_down]
  let L := mBFloat (beta:=beta) b radix r
  let sentinel := boundR (beta:=beta) radix r
  have hSentinel_ge : r ≤ _root_.F2R (beta:=beta) sentinel := by
    have hcorr := boundRCorrect1 (beta:=beta) radix r
    have hlt : r < _root_.F2R (beta:=beta) sentinel := by
      simpa [wp, PostCond.noThrow, pure, boundRCorrect1_check, sentinel]
        using hcorr ⟨hbeta, hradix⟩
    exact le_of_lt hlt
  have hSentinel_mem : sentinel ∈ L := by
    have hmem := mBFadic_correct2 (beta:=beta) b radix r
    simpa [wp, PostCond.noThrow, pure, mBFadic_correct2_check, L, sentinel]
      using hmem ⟨hvNum_gt, hBoundExp⟩
  have hZero_mem :
      (FloatSpec.Core.Defs.FlocqFloat.mk (beta:=beta) 0 (-b.dExp)) ∈ L := by
    have hmem := mBFadic_correct4_from_finite_box_payload (beta:=beta) b radix r
    simpa [wp, PostCond.noThrow, pure, mBFadic_correct4_check, L]
      using hmem ⟨lt_trans Int.zero_lt_one hvNum_gt, hBoundExp⟩
  have hFiniteBox :
      ∀ f : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) b f → r ≤ _root_.F2R (beta:=beta) f →
          f ∈ L ∨
            _root_.F2R (beta:=beta) sentinel ≤ _root_.F2R (beta:=beta) f ∨
              _root_.F2R (beta:=beta) f = 0 := by
    intro f hf_bounded hf_ge
    by_cases hexp_box : f.Fexp ≤ sentinel.Fexp
    · left
      have hmem := mBFadic_correct1_from_exp_bound_payload (beta:=beta) b radix r f
      simpa [wp, PostCond.noThrow, pure, mBFadic_correct1_check, L, sentinel]
        using hmem ⟨hf_bounded, hexp_box⟩
    · push Not at hexp_box
      by_cases hfnum_zero : f.Fnum = 0
      · right
        right
        have hf_eq_zero :
            _root_.F2R (beta:=beta) f = 0 := by
          simp [hfnum_zero, _root_.F2R, FloatSpec.Core.Defs.F2R]
        exact hf_eq_zero
      · right
        left
        subst beta
        have hradix_pos_int : (0 : Int) < radix := lt_trans Int.zero_lt_one hradix
        have hradix_pos_real : (0 : ℝ) < (radix : ℝ) := by
          exact_mod_cast hradix_pos_int
        have hpow_f_pos : (0 : ℝ) < (radix : ℝ) ^ f.Fexp :=
          zpow_pos hradix_pos_real f.Fexp
        have hpow_s_le_f : (radix : ℝ) ^ sentinel.Fexp ≤ (radix : ℝ) ^ f.Fexp := by
          exact zpow_le_zpow_right₀ (by exact_mod_cast (le_of_lt hradix)) (le_of_lt hexp_box)
        have hsentinel_eval :
            _root_.F2R (beta:=radix) sentinel = (radix : ℝ) ^ sentinel.Fexp := by
          simp [sentinel, boundR, boundNat, _root_.F2R, FloatSpec.Core.Defs.F2R]
        rcases lt_or_gt_of_ne hfnum_zero with hfnum_neg | hfnum_pos
        · have hneg_sentinel_lt_r :
              -_root_.F2R (beta:=radix) sentinel < r := by
            have hcorr := boundRCorrect2 (beta:=radix) radix r
            have hlt :
                _root_.F2R (beta:=radix)
                  (Fopp (beta:=radix) (boundR (beta:=radix) radix r)) < r := by
              simpa [wp, PostCond.noThrow, pure, boundRCorrect2_check]
                using hcorr ⟨rfl, hradix⟩
            have hopp :
                _root_.F2R (beta:=radix)
                  (Fopp (beta:=radix) sentinel) =
                  -_root_.F2R (beta:=radix) sentinel := by
              simp [Fopp, FloatSpec.Calc.Operations.Fopp, _root_.F2R,
                FloatSpec.Core.Defs.F2R]
            change _root_.F2R (beta:=radix) (Fopp (beta:=radix) sentinel) < r at hlt
            rw [hopp] at hlt
            exact hlt
          have hf_le_neg_sentinel :
              _root_.F2R (beta:=radix) f ≤ -_root_.F2R (beta:=radix) sentinel := by
            have hnum_le : (f.Fnum : ℝ) ≤ -1 := by
              exact_mod_cast (by omega : f.Fnum ≤ -1)
            have hmul_le : (f.Fnum : ℝ) * (radix : ℝ) ^ f.Fexp ≤
                (-1 : ℝ) * (radix : ℝ) ^ f.Fexp := by
              exact mul_le_mul_of_nonneg_right hnum_le (le_of_lt hpow_f_pos)
            have hneg_pow_le : -((radix : ℝ) ^ f.Fexp) ≤ -((radix : ℝ) ^ sentinel.Fexp) :=
              neg_le_neg hpow_s_le_f
            calc
              _root_.F2R (beta:=radix) f
                  = (f.Fnum : ℝ) * (radix : ℝ) ^ f.Fexp := by
                      simp [_root_.F2R, FloatSpec.Core.Defs.F2R]
              _ ≤ (-1 : ℝ) * (radix : ℝ) ^ f.Fexp := hmul_le
              _ = -((radix : ℝ) ^ f.Fexp) := by ring
              _ ≤ -((radix : ℝ) ^ sentinel.Fexp) := hneg_pow_le
              _ = -_root_.F2R (beta:=radix) sentinel := by
                      rw [hsentinel_eval]
          exact False.elim (not_lt_of_ge (le_trans hf_ge hf_le_neg_sentinel) hneg_sentinel_lt_r)
        · have hnum_ge_one : (1 : ℝ) ≤ (f.Fnum : ℝ) := by
            exact_mod_cast (by omega : (1 : Int) ≤ f.Fnum)
          calc
            _root_.F2R (beta:=radix) sentinel
                = (radix : ℝ) ^ sentinel.Fexp := hsentinel_eval
            _ ≤ (radix : ℝ) ^ f.Fexp := hpow_s_le_f
            _ = (1 : ℝ) * (radix : ℝ) ^ f.Fexp := by ring
            _ ≤ (f.Fnum : ℝ) * (radix : ℝ) ^ f.Fexp := by
                  exact mul_le_mul_of_nonneg_right hnum_ge_one (le_of_lt hpow_f_pos)
            _ = _root_.F2R (beta:=radix) f := by
                  simp [_root_.F2R, FloatSpec.Core.Defs.F2R]
  have hlist := MaxExList (beta:=beta) r L
  have hlist_prop :
      (∀ f, f ∈ L → _root_.F2R f < r) ∨
      (∃ max, max ∈ L ∧ r ≤ _root_.F2R max ∧
        ∀ f, f ∈ L → r ≤ _root_.F2R f → _root_.F2R max ≤ _root_.F2R f) := by
    simpa [wp, PostCond.noThrow, pure, MaxExList_check, L] using hlist trivial
  rcases hlist_prop with hall_lt | hmax
  · have hsentinel_mem :
        boundR (beta:=beta) radix r ∈ L := by
      simpa [sentinel] using hSentinel_mem
    have hlt := hall_lt (boundR (beta:=beta) radix r) hsentinel_mem
    exact False.elim ((not_lt_of_ge hSentinel_ge) hlt)
  · rcases hmax with ⟨max, hmax_mem, hmax_ge, hmin⟩
    refine ⟨max, ?_⟩
    have hmax_bounded : Fbounded (beta:=beta) b max := by
      have hb := mBPadic_Fbounded (beta:=beta) b radix max r
      simpa [wp, PostCond.noThrow, pure, mBPadic_Fbounded_check, L]
        using hb hmax_mem
    refine ⟨hmax_bounded, hmax_ge, ?_⟩
    intro f hf_bounded hf_ge
    rcases hFiniteBox f hf_bounded hf_ge with hf_mem | hf_rest
    · exact hmin f hf_mem hf_ge
    · rcases hf_rest with hf_high | hf_zero
      · have hsentinel_mem :
          boundR (beta:=beta) radix r ∈ L := by
          simpa [sentinel] using hSentinel_mem
        have hmax_le_sentinel :=
          hmin (boundR (beta:=beta) radix r) hsentinel_mem hSentinel_ge
        exact le_trans hmax_le_sentinel hf_high
      · have hzero_ge : r ≤ _root_.F2R (beta:=beta)
            (FloatSpec.Core.Defs.FlocqFloat.mk (beta:=beta) 0 (-b.dExp)) := by
          have hr_nonpos : r ≤ 0 := by
            rw [hf_zero] at hf_ge
            exact hf_ge
          simpa [_root_.F2R, FloatSpec.Core.Defs.F2R] using hr_nonpos
        have hmax_le_zero :=
          hmin (FloatSpec.Core.Defs.FlocqFloat.mk (beta:=beta) 0 (-b.dExp))
            hZero_mem hzero_ge
        have hmax_le_zero_real : _root_.F2R (beta:=beta) max ≤ 0 := by
          simpa [_root_.F2R, FloatSpec.Core.Defs.F2R] using hmax_le_zero
        rw [hf_zero]
        exact hmax_le_zero_real

/-- Coq `MaxEx`: the finite-box obligations are consequences of the source
section assumptions and stay internal to the proof. -/
theorem MaxEx {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat) (r : ℝ) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (MaxEx_check (beta:=beta) b radix r) : Id Unit)
    ⦃⇓_ => ⌜∃ max : FloatSpec.Core.Defs.FlocqFloat beta,
        isMax (beta:=beta) b radix r max⌝⦄ := by
  intro ⟨hbeta, hradix, hprecision, hvNum⟩
  subst beta
  have hvNum_gt : 1 < b.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecision
  have hBoundExp : -b.dExp ≤ (boundR (beta:=radix) radix r).Fexp := by
    have hnonneg : 0 ≤ (boundR (beta:=radix) radix r).Fexp := by
      simp [boundR, boundNat]
    exact le_trans (neg_nonpos.mpr b.dExp_nonneg) hnonneg
  exact MaxEx_from_finite_box_payload (beta:=radix) b radix r
    ⟨rfl, hradix, hvNum_gt, hBoundExp⟩

/-- Coq `MinRoundedModeP`: totality and all four rounded-mode fields follow
from the enclosing source section assumptions. -/
theorem MinRoundedModeP {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < precision ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (MinRoundedModeP_check
      (α:=FloatSpec.Core.Defs.FlocqFloat beta) b radix) : Id Unit)
    ⦃⇓_ => ⌜RoundedModeP (beta:=beta) b
      (isMin (beta:=beta) b radix)⌝⦄ := by
  intro ⟨hbeta, hradix, hprecision, hvNum⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure, MinRoundedModeP_check,
    Id.run, ULift.up_down]
  have hTotal : TotalP (isMin (beta:=radix) b radix) := by
    intro r
    exact MinEx (beta:=radix) b radix precision r
      ⟨rfl, hradix, by omega, hvNum⟩
  exact ⟨hTotal, MinCompatible b radix,
    (fun _ _ hp => Or.inl hp), MonotoneMin b radix⟩

/-- Coq `MaxRoundedModeP`. -/
theorem MaxRoundedModeP {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < precision ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (MaxRoundedModeP_check
      (α:=FloatSpec.Core.Defs.FlocqFloat beta) b radix) : Id Unit)
    ⦃⇓_ => ⌜RoundedModeP (beta:=beta) b
      (isMax (beta:=beta) b radix)⌝⦄ := by
  intro ⟨hbeta, hradix, hprecision, hvNum⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure, MaxRoundedModeP_check,
    Id.run, ULift.up_down]
  have hTotal : TotalP (isMax (beta:=radix) b radix) := by
    intro r
    exact MaxEx (beta:=radix) b radix precision r
      ⟨rfl, hradix, by omega, hvNum⟩
  exact ⟨hTotal, MaxCompatible b radix,
    (fun _ _ hp => Or.inr hp), MonotoneMax b radix⟩

/-- Coq `ClosestTotal`: extrema existence is derived rather than supplied by
the caller. -/
theorem ClosestTotal {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < precision ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (ClosestTotal_check (beta:=beta) b (radix : ℝ) 0) : Id Unit)
    ⦃⇓_ => ⌜TotalP (Closest (beta:=beta) b (radix : ℝ))⌝⦄ := by
  intro ⟨hbeta, hradix, hprecision, hvNum⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure, ClosestTotal_check,
    Id.run, ULift.up_down]
  have hMinTotal : TotalP (isMin (beta:=radix) b radix) := by
    intro r
    exact MinEx (beta:=radix) b radix precision r
      ⟨rfl, hradix, by omega, hvNum⟩
  have hMaxTotal : TotalP (isMax (beta:=radix) b radix) := by
    intro r
    exact MaxEx (beta:=radix) b radix precision r
      ⟨rfl, hradix, by omega, hvNum⟩
  intro r
  exact ClosestTotal_from_extrema_payload (beta:=radix) b radix
    (radix : ℝ) r ⟨hMinTotal, hMaxTotal⟩

/-- Coq `ClosestRoundedModeP`: all four fields are reconstructed from the
source section assumptions. -/
theorem ClosestRoundedModeP {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < precision ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (ClosestRoundedModeP_check (beta:=beta) b (radix : ℝ)) : Id Unit)
    ⦃⇓_ => ⌜RoundedModeP (beta:=beta) b
      (Closest (beta:=beta) b (radix : ℝ))⌝⦄ := by
  intro ⟨hbeta, hradix, hprecision, hvNum⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure, ClosestRoundedModeP_check,
    Id.run, ULift.up_down]
  have hTotal : TotalP (Closest (beta:=radix) b (radix : ℝ)) := by
    exact ClosestTotal (beta:=radix) b radix precision
      ⟨rfl, hradix, hprecision, hvNum⟩
  have hMinOrMax : MinOrMaxP (beta:=radix) b
      (Closest (beta:=radix) b (radix : ℝ)) := by
    exact ClosestMinOrMax_from_split_radix_payload (beta:=radix) b radix (radix : ℝ) True.intro
  have hMonotone : MonotoneP (beta:=radix)
      (Closest (beta:=radix) b (radix : ℝ)) := by
    exact ClosestMonotone_from_split_radix_payload (beta:=radix) b (radix : ℝ) True.intro
  exact ⟨hTotal, ClosestCompatible b (radix : ℝ), hMinOrMax, hMonotone⟩

-- Pff's Coq `float` is the same signed-mantissa/exponent record used by Core.
-- Keep only a source-facing name: this is a definitional alias, not a second
-- sign-magnitude carrier (which previously admitted a second representation of
-- zero and lost the source's signed `Fnum`).
abbrev PffFloat (beta : Int) [ValidRadix beta] :=
  FloatSpec.Core.Defs.FlocqFloat beta

namespace PffFloat

/-- Compatibility view used by a few auxiliary sign-magnitude calculations. -/
def mantissa {beta : Int} [ValidRadix beta] (f : PffFloat beta) : Int :=
  f.Fnum.natAbs

def exponent {beta : Int} [ValidRadix beta] (f : PffFloat beta) : Int :=
  f.Fexp

def sign {beta : Int} [ValidRadix beta] (f : PffFloat beta) : Bool :=
  decide (f.Fnum < 0)

end PffFloat

-- Equality of Flocq-style floats by components (Coq: `floatEq`)
-- We mirror Coq's record equality lemma for the Flocq float record
-- (with fields `Fnum` and `Fexp`).
noncomputable def floatEq_check {beta : Int} [ValidRadix beta]
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem floatEq {beta : Int} [ValidRadix beta]
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜p.Fnum = q.Fnum ∧ p.Fexp = q.Fexp⌝⦄
    (pure (floatEq_check p q) : Id Unit)
    ⦃⇓_ => ⌜p = q⌝⦄ := by
  intro ⟨hnum, hexp⟩
  simp only [wp, PostCond.noThrow, pure, floatEq_check, Id.run,
    ULift.up_down]
  show p = q
  cases p; cases q; simp_all

-- Decidability of equality for Core floats (Coq: `floatDec`)
noncomputable def floatDec_check {beta : Int} [ValidRadix beta]
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem floatDec {beta : Int} [ValidRadix beta]
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜True⌝⦄
    (pure (floatDec_check x y) : Id Unit)
    ⦃⇓_ => ⌜x = y ∨ x ≠ y⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, floatDec_check, Id.run,
    ULift.up_down]
  show x = y ∨ x ≠ y
  exact eq_or_ne x y

-- Conversion between Pff and Flocq formats
def pff_to_flocq (beta : Int) [ValidRadix beta]
    (f : PffFloat beta) : FloatSpec.Core.Defs.FlocqFloat beta :=
  f

def flocq_to_pff {beta : Int} [ValidRadix beta]
    (f : FloatSpec.Core.Defs.FlocqFloat beta) : PffFloat beta :=
  f


-- Zero float at exponent z (Coq: `Fzero`)
def Fzero (beta : Int) [ValidRadix beta] (z : Int) : FloatSpec.Core.Defs.FlocqFloat beta :=
  FloatSpec.Core.Defs.FlocqFloat.mk 0 z

-- Coq: `FzeroisReallyZero` — real value of zero float is 0
noncomputable def FzeroisReallyZero_check {beta : Int} [ValidRadix beta] (z : Int) : Unit :=
  ()

theorem FzeroisReallyZero {beta : Int} [ValidRadix beta] (z : Int) :
    ⦃⌜True⌝⦄
    (pure (FzeroisReallyZero_check (beta:=beta) z) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (Fzero beta z) = 0⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, FzeroisReallyZero_check, Id.run,
    ULift.up_down]
  show _root_.F2R (Fzero beta z) = 0
  simp [Fzero, _root_.F2R, FloatSpec.Core.Defs.F2R]

-- Coq: `FzeroisZero` — specialized form using a bound's exponent
noncomputable def FzeroisZero_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) : Unit :=
  ()

theorem FzeroisZero {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) :
    ⦃⌜True⌝⦄
    (pure (FzeroisZero_check (beta:=beta) b) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (Fzero beta (- b.dExp)) = 0⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, FzeroisZero_check, Id.run,
    ULift.up_down]
  show _root_.F2R (Fzero beta (- b.dExp)) = 0
  simp [Fzero, _root_.F2R, FloatSpec.Core.Defs.F2R]

-- Coq: `FboundedFzero` — the zero float is bounded for any bound descriptor
noncomputable def FboundedFzero_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) : Unit :=
  ()

theorem FboundedFzero_from_positive_bound_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) :
    ⦃⌜0 < b.vNum⌝⦄
    (pure (FboundedFzero_check (beta:=beta) b) : Id Unit)
    ⦃⇓_ => ⌜Fbounded (beta:=beta) b (Fzero beta (- b.dExp))⌝⦄ := by
  intro hvNum
  simp only [wp, PostCond.noThrow, pure, FboundedFzero_check, Id.run,
    ULift.up_down]
  show Fbounded b (Fzero beta (-b.dExp))
  exact ⟨by simpa [Fzero] using hvNum, le_refl _⟩

/-- Coq `FboundedFzero`. -/
theorem FboundedFzero {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) :
    ⦃⌜True⌝⦄
    (pure (FboundedFzero_check (beta:=beta) b) : Id Unit)
    ⦃⇓_ => ⌜Fbounded (beta:=beta) b (Fzero beta (-b.dExp))⌝⦄ := by
  intro _
  exact FboundedFzero_from_positive_bound_payload (beta:=beta) b b.vNum_pos

-- Coq: `FboundedZeroSameExp` — boundedness preserved when replacing mantissa by zero at same exponent
noncomputable def FboundedZeroSameExp_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem FboundedZeroSameExp {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b p⌝⦄
    (pure (FboundedZeroSameExp_check (beta:=beta) b p) : Id Unit)
    ⦃⇓_ => ⌜Fbounded (beta:=beta) b (Fzero beta (p.Fexp))⌝⦄ := by
  intro hp
  simp only [wp, PostCond.noThrow, pure, FboundedZeroSameExp_check, Id.run,
    ULift.up_down]
  rcases hp with ⟨hnum, hexp⟩
  refine ⟨?_, hexp⟩
  have hvNum : 0 < b.vNum := lt_of_le_of_lt (abs_nonneg p.Fnum) hnum
  simpa [Fzero] using hvNum

-- Coq: `FBoundedScale` — scaling exponent by natural n preserves boundedness
noncomputable def FBoundedScale_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (p : FloatSpec.Core.Defs.FlocqFloat beta) (n : Nat) : Unit :=
  ()

theorem FBoundedScale {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (p : FloatSpec.Core.Defs.FlocqFloat beta) (n : Nat) :
    ⦃⌜Fbounded (beta:=beta) b p⌝⦄
    (pure (FBoundedScale_check (beta:=beta) b p n) : Id Unit)
    ⦃⇓_ => ⌜Fbounded (beta:=beta) b ⟨p.Fnum, p.Fexp + (Int.ofNat n)⟩⌝⦄ := by
  intro hp
  simp only [wp, PostCond.noThrow, pure, FBoundedScale_check, Id.run,
    ULift.up_down]
  rcases hp with ⟨hnum, hexp⟩
  refine ⟨hnum, ?_⟩
  change -b.dExp ≤ p.Fexp + Int.ofNat n
  have hn : (0 : Int) ≤ Int.ofNat n := Int.natCast_nonneg n
  omega

-- Coq: `FvalScale` — value after scaling exponent equals multiplication by powerRZ
noncomputable def FvalScale_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (p : FloatSpec.Core.Defs.FlocqFloat beta) (n : Nat) : Unit :=
  ()

theorem FvalScale (beta : Int) [ValidRadix beta]
    (b : Fbound_skel) (p : FloatSpec.Core.Defs.FlocqFloat beta) (n : Nat) :
    ⦃⌜(beta : ℝ) ≠ 0⌝⦄
    (pure (FvalScale_check (beta:=beta) b p n) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) ⟨p.Fnum, p.Fexp + (Int.ofNat n)⟩ =
            ((beta : ℝ) ^ (Int.ofNat n)) * _root_.F2R (beta:=beta) p⌝⦄ := by
  intro hb
  simp only [wp, PostCond.noThrow, pure, FvalScale_check, Id.run,
    ULift.up_down]
  show _root_.F2R (beta:=beta) ⟨p.Fnum, p.Fexp + (Int.ofNat n)⟩ =
       ((beta : ℝ) ^ (Int.ofNat n)) * _root_.F2R (beta:=beta) p
  simp only [_root_.F2R, FloatSpec.Core.Defs.F2R]
  rw [zpow_add₀ hb]
  ring

/-- Coq `RoundedModeMult`: the scaled-float witness is constructed internally
from the source rounded mode and bounded operand. -/
theorem RoundedModeMult {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (r : ℝ) (q q' : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < precision ∧
        b.vNum = Zpower_nat radix precision ∧ RoundedModeP b P ∧
        P r q ∧ Fbounded b q' ∧
        r ≤ (radix : ℝ) * _root_.F2R (beta:=beta) q'⌝⦄
    (pure (RoundedModeMult_check (beta:=beta) b (radix : ℝ) P r q q') :
      Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) q ≤
      (radix : ℝ) * _root_.F2R (beta:=beta) q'⌝⦄ := by
  intro ⟨hbeta, hradix, _hprecision, _hvNum, hRounded, hP, hq', hr⟩
  subst beta
  have hScaledBounded : Fbounded (beta:=radix) b
      (⟨q'.Fnum, q'.Fexp + 1⟩ : FloatSpec.Core.Defs.FlocqFloat radix) := by
    exact FBoundedScale (beta:=radix) b q' 1 hq'
  have hScaledValue : _root_.F2R (beta:=radix)
      (⟨q'.Fnum, q'.Fexp + 1⟩ : FloatSpec.Core.Defs.FlocqFloat radix) =
      (radix : ℝ) * _root_.F2R (beta:=radix) q' := by
    have h := FvalScale radix b q' 1
    simpa using h (by exact_mod_cast (by omega : radix ≠ 0))
  exact RoundedModeMult_from_scale_payload (beta:=radix) b (radix : ℝ)
    P r q q' ⟨hRounded.toFullPayload, hP, hScaledBounded, hScaledValue, hr⟩

/-- Coq `RoundedModeMultLess`; lower-bound dual. -/
theorem RoundedModeMultLess {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (r : ℝ) (q q' : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < precision ∧
        b.vNum = Zpower_nat radix precision ∧ RoundedModeP b P ∧
        P r q ∧ Fbounded b q' ∧
        (radix : ℝ) * _root_.F2R (beta:=beta) q' ≤ r⌝⦄
    (pure (RoundedModeMultLess_check (beta:=beta) b (radix : ℝ) P r q q') :
      Id Unit)
    ⦃⇓_ => ⌜(radix : ℝ) * _root_.F2R (beta:=beta) q' ≤
      _root_.F2R (beta:=beta) q⌝⦄ := by
  intro ⟨hbeta, hradix, _hprecision, _hvNum, hRounded, hP, hq', hr⟩
  subst beta
  have hScaledBounded : Fbounded (beta:=radix) b
      (⟨q'.Fnum, q'.Fexp + 1⟩ : FloatSpec.Core.Defs.FlocqFloat radix) := by
    exact FBoundedScale (beta:=radix) b q' 1 hq'
  have hScaledValue : _root_.F2R (beta:=radix)
      (⟨q'.Fnum, q'.Fexp + 1⟩ : FloatSpec.Core.Defs.FlocqFloat radix) =
      (radix : ℝ) * _root_.F2R (beta:=radix) q' := by
    have h := FvalScale radix b q' 1
    simpa using h (by exact_mod_cast (by omega : radix ≠ 0))
  exact RoundedModeMultLess_from_scale_payload (beta:=radix) b (radix : ℝ)
    P r q q' ⟨hRounded.toFullPayload, hP, hScaledBounded, hScaledValue, hr⟩

/-- Coq: `RoundedModeMultAbs`.  Scaling and sign preservation are derived
from `RoundedModeP`; callers supply only the source premises. -/
theorem RoundedModeMultAbs {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (r : ℝ) (q q' : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < precision ∧
        b.vNum = Zpower_nat radix precision ∧
        RoundedModeP (beta:=beta) b P ∧ P r q ∧
        Fbounded (beta:=beta) b q' ∧
        |r| ≤ (radix : ℝ) * _root_.F2R (beta:=beta) q'⌝⦄
    (pure (RoundedModeMultAbs_check (beta:=beta) b (radix : ℝ) P r q q') :
      Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) q| ≤
      (radix : ℝ) * _root_.F2R (beta:=beta) q'⌝⦄ := by
  intro ⟨hbeta, hradix, _hprecision, _hvnum, hRounded, hP, hqBounded,
    hAbs⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure, RoundedModeMultAbs_check,
    PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
    Id.run, ULift.up, ULift.down, ULift.up_down]
  have hradixNe : (radix : ℝ) ≠ 0 := by exact_mod_cast (by omega : radix ≠ 0)
  by_cases hr : 0 ≤ r
  · have hqNonneg : 0 ≤ _root_.F2R (beta:=radix) q := by
      have h := RleRoundedR0 (beta:=radix) b P q r
      simpa only [wp, PostCond.noThrow, pure, RleRoundedR0_check,
        PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
        Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using h ⟨hRounded, hP, hr⟩
    rw [abs_of_nonneg hqNonneg]
    let scaled : FloatSpec.Core.Defs.FlocqFloat radix :=
      ⟨q'.Fnum, q'.Fexp + Int.ofNat 1⟩
    have hScaledBounded : Fbounded (beta:=radix) b scaled := by
      have h := FBoundedScale (beta:=radix) b q' 1
      simpa only [wp, PostCond.noThrow, pure, FBoundedScale_check,
        PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
        Id.run, ULift.up, ULift.down, ULift.up_down, Nat.cast_one, scaled, Int.cast_ofNat] using
        h hqBounded
    have hScaledValue : _root_.F2R (beta:=radix) scaled =
        (radix : ℝ) * _root_.F2R (beta:=radix) q' := by
      have h := FvalScale radix b q' 1
      have h' := h hradixNe
      simp only [wp, PostCond.noThrow, pure, FvalScale_check,
        Id.run, ULift.up_down, Nat.cast_one, zpow_one, scaled] at h'
      norm_num at h'
      simpa [scaled] using h'
    have hScaledUpper : r ≤ _root_.F2R (beta:=radix) scaled := by
      rw [hScaledValue]
      exact le_trans (le_abs_self r) hAbs
    have hRound := RleBoundRoundr (beta:=radix) b radix P scaled q r
    have hResult : _root_.F2R (beta:=radix) q ≤
        _root_.F2R (beta:=radix) scaled := by
      simpa only [wp, PostCond.noThrow, pure, RleBoundRoundr_check,
        PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
        Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
        hRound ⟨hRounded, hScaledBounded, hScaledUpper, hP⟩
    simpa only [hScaledValue, Int.cast_ofNat] using hResult
  · have hrNonpos : r ≤ 0 := le_of_lt (lt_of_not_ge hr)
    have hqNonpos : _root_.F2R (beta:=radix) q ≤ 0 := by
      have h := RleRoundedLessR0 (beta:=radix) b P q r
      simpa only [wp, PostCond.noThrow, pure, RleRoundedLessR0_check,
        PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
        Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using h ⟨hRounded, hP, hrNonpos⟩
    rw [abs_of_nonpos hqNonpos]
    let qOpp := Fopp (beta:=radix) q'
    have hqOppBounded : Fbounded (beta:=radix) b qOpp := by
      rcases hqBounded with ⟨hnum, hexp⟩
      exact ⟨by simpa [qOpp, Fopp, FloatSpec.Calc.Operations.Fopp,
        abs_neg] using hnum,
        by simpa [qOpp, Fopp, FloatSpec.Calc.Operations.Fopp] using hexp⟩
    let scaled : FloatSpec.Core.Defs.FlocqFloat radix :=
      ⟨qOpp.Fnum, qOpp.Fexp + Int.ofNat 1⟩
    have hScaledBounded : Fbounded (beta:=radix) b scaled := by
      have h := FBoundedScale (beta:=radix) b qOpp 1
      simpa only [wp, PostCond.noThrow, pure, FBoundedScale_check,
        PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
        Id.run, ULift.up, ULift.down, ULift.up_down, Nat.cast_one, scaled, Int.cast_ofNat] using
        h hqOppBounded
    have hqOppValue : _root_.F2R (beta:=radix) qOpp =
        -_root_.F2R (beta:=radix) q' := by
      have h := FloatSpec.Calc.Operations.F2R_opp (beta:=radix) q'
      simpa [qOpp, Fopp, _root_.F2R] using h trivial
    have hScaledValue : _root_.F2R (beta:=radix) scaled =
        -((radix : ℝ) * _root_.F2R (beta:=radix) q') := by
      have h := FvalScale radix b qOpp 1
      have h' : _root_.F2R (beta:=radix) scaled =
          (radix : ℝ) * _root_.F2R (beta:=radix) qOpp := by
        have h'' := h hradixNe
        simp only [wp, PostCond.noThrow, pure, FvalScale_check,
          Id.run, ULift.up_down, Nat.cast_one, zpow_one, scaled] at h''
        norm_num at h''
        simpa [scaled] using h''
      rw [h', hqOppValue]
      ring
    have hScaledLeR : _root_.F2R (beta:=radix) scaled ≤ r := by
      rw [hScaledValue]
      rw [abs_of_nonpos hrNonpos] at hAbs
      linarith
    have hRound := RleBoundRoundl (beta:=radix) b radix P scaled q r
    have hScaledLeQ : _root_.F2R (beta:=radix) scaled ≤
        _root_.F2R (beta:=radix) q := by
      simpa only [wp, PostCond.noThrow, pure, RleBoundRoundl_check,
        PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
        Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
        hRound ⟨hRounded, hScaledBounded, hScaledLeR, hP⟩
    rw [hScaledValue] at hScaledLeQ
    simpa using neg_le_neg hScaledLeQ

-- Coq: `maxFbounded` — the maximal mantissa at exponent z is bounded
noncomputable def maxFbounded_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (z : Int) : Unit :=
  ()

theorem maxFbounded {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (z : Int) :
    ⦃⌜- b.dExp ≤ z⌝⦄
    (pure (maxFbounded_check (beta:=beta) b z) : Id Unit)
    ⦃⇓_ => ⌜Fbounded (beta:=beta) b
      (FloatSpec.Core.Defs.FlocqFloat.mk (beta:=beta) (b.vNum - 1) z)⌝⦄ := by
  intro hz
  simp only [wp, PostCond.noThrow, pure, maxFbounded_check, Id.run,
    ULift.up_down]
  constructor
  · have hvNumPos := b.vNum_pos
    have hnonneg : 0 ≤ b.vNum - 1 := by omega
    rw [abs_of_nonneg hnonneg]
    omega
  · exact hz

-- Coq: `oppBounded` — boundedness preserved under negation
noncomputable def oppBounded_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem oppBounded {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b x⌝⦄
    (pure (oppBounded_check (beta:=beta) b x) : Id Unit)
    ⦃⇓_ => ⌜Fbounded (beta:=beta) b (Fopp x)⌝⦄ := by
  intro hx
  simp only [wp, PostCond.noThrow, pure, oppBounded_check, Id.run,
    ULift.up_down]
  simpa [Fbounded, Fopp, FloatSpec.Calc.Operations.Fopp, abs_neg] using hx

-- Coq: `oppBoundedInv` — boundedness inversion under negation
noncomputable def oppBoundedInv_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `oppBoundedInv` — if `Fopp x` is bounded then `x` is bounded.
Hoare-triple style statement mirroring Pff.v; the proof is below. -/
theorem oppBoundedInv {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b (Fopp x)⌝⦄
    (pure (oppBoundedInv_check (beta:=beta) b x) : Id Unit)
    ⦃⇓_ => ⌜Fbounded (beta:=beta) b x⌝⦄ := by
  intro hx
  simp only [wp, PostCond.noThrow, pure, oppBoundedInv_check, Id.run,
    ULift.up_down]
  simpa [Fbounded, Fopp, FloatSpec.Calc.Operations.Fopp, abs_neg] using hx

-- Coq: `absFBounded` — boundedness preserved under absolute value
noncomputable def absFBounded_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (f : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `absFBounded` — if `f` is bounded then `Fabs f` is also bounded.
Hoare-triple style statement; the proof is below. -/
theorem absFBounded {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (f : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b f⌝⦄
    (pure (absFBounded_check (beta:=beta) b f) : Id Unit)
    ⦃⇓_ => ⌜Fbounded (beta:=beta) b (Fabs f)⌝⦄ := by
  intro hf
  simp only [wp, PostCond.noThrow, pure, absFBounded_check, Id.run,
    ULift.up_down]
  simpa [Fbounded, Fabs, FloatSpec.Calc.Operations.Fabs, Int.natCast_natAbs] using hf

-- Coq: `FboundedEqExp` — transfer boundedness along equal value and exp inequality
noncomputable def FboundedEqExp_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (p q : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FboundedEqExp` — if `p` is bounded, `F2R p = F2R q`, and `p.Fexp ≤ q.Fexp`,
    then `q` is bounded. Statement mirrors Pff.v; the proof is below. -/
theorem FboundedEqExp {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (p q : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜1 < beta ∧ Fbounded (beta:=beta) b p ∧
        _root_.F2R p = _root_.F2R q ∧ p.Fexp ≤ q.Fexp⌝⦄
    (pure (FboundedEqExp_check (beta:=beta) b p q) : Id Unit)
    ⦃⇓_ => ⌜Fbounded (beta:=beta) b q⌝⦄ := by
  intro h
  simp only [wp, PostCond.noThrow, pure, FboundedEqExp_check, Id.run,
    ULift.up_down]
  rcases h with ⟨hβ, hp, hpq, hexp⟩
  constructor
  · by_cases hq0 : q.Fnum = 0
    · have hvpos : 0 < b.vNum := lt_of_le_of_lt (abs_nonneg p.Fnum) hp.1
      simpa [hq0] using hvpos
    · have hbeta_pos : (0 : ℝ) < (beta : ℝ) := by
        exact_mod_cast (by omega : (0 : Int) < beta)
      have hp_exp_pos : (0 : ℝ) < (beta : ℝ) ^ p.Fexp :=
        zpow_pos hbeta_pos p.Fexp
      have hq_exp_pos : (0 : ℝ) < (beta : ℝ) ^ q.Fexp :=
        zpow_pos hbeta_pos q.Fexp
      have hscale :
          |(q.Fnum : ℝ)| * (beta : ℝ) ^ q.Fexp =
            |(p.Fnum : ℝ)| * (beta : ℝ) ^ p.Fexp := by
        have hpq_abs := congrArg abs hpq.symm
        simpa [_root_.F2R, FloatSpec.Core.Defs.F2R, abs_mul,
          abs_of_pos hp_exp_pos, abs_of_pos hq_exp_pos,
          Int.cast_abs] using hpq_abs
      have hb_real : |(p.Fnum : ℝ)| < (b.vNum : ℝ) := by
        exact_mod_cast hp.1
      have hpow_mono :
          (beta : ℝ) ^ p.Fexp ≤ (beta : ℝ) ^ q.Fexp :=
        zpow_le_zpow_right₀ (by exact_mod_cast (show (1 : Int) ≤ beta from le_of_lt hβ)) hexp
      have hq_le_pnum : |(q.Fnum : ℝ)| ≤ |(p.Fnum : ℝ)| := by
        have hq_expr :
            |(q.Fnum : ℝ)| =
              |(p.Fnum : ℝ)| * (beta : ℝ) ^ p.Fexp /
                (beta : ℝ) ^ q.Fexp := by
          rw [← hscale]
          field_simp [ne_of_gt hq_exp_pos]
        rw [hq_expr]
        exact (div_le_iff₀ hq_exp_pos).mpr
          (mul_le_mul_of_nonneg_left hpow_mono (abs_nonneg _))
      have hq_lt_real : |(q.Fnum : ℝ)| < (b.vNum : ℝ) :=
        lt_of_le_of_lt hq_le_pnum hb_real
      exact_mod_cast hq_lt_real
  · exact le_trans hp.2 hexp

-- Coq: `is_Fzero_rep1` — zero mantissa implies zero real value
noncomputable def is_Fzero_rep1_check {beta : Int} [ValidRadix beta]
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem is_Fzero_rep1 {beta : Int} [ValidRadix beta]
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜is_Fzero x⌝⦄
    (pure (is_Fzero_rep1_check x) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R x = 0⌝⦄ := by
  intro h
  simp only [wp, PostCond.noThrow, pure, is_Fzero_rep1_check, Id.run,
    ULift.up_down]
  show _root_.F2R x = 0
  have hfz : x.Fnum = 0 := h
  simp [_root_.F2R, FloatSpec.Core.Defs.F2R, hfz]

-- Coq: `is_Fzero_rep2` — zero real value implies zero mantissa
noncomputable def is_Fzero_rep2_check {beta : Int} [ValidRadix beta]
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem is_Fzero_rep2 {beta : Int} [ValidRadix beta]
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜1 < beta ∧ _root_.F2R x = 0⌝⦄
    (pure (is_Fzero_rep2_check x) : Id Unit)
    ⦃⇓_ => ⌜is_Fzero x⌝⦄ := by
  intro ⟨hβ, hx⟩
  simp only [wp, PostCond.noThrow, pure, is_Fzero_rep2_check, Id.run,
    ULift.up_down]
  show is_Fzero x
  exact FloatSpec.Core.Float_prop.eq_0_F2R (beta:=beta) x hβ hx

-- Coq: `NisFzeroComp` — if x is not zero and F2R x = F2R y then y is not zero
noncomputable def NisFzeroComp_check {beta : Int} [ValidRadix beta]
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem NisFzeroComp {beta : Int} [ValidRadix beta]
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜1 < beta ∧ ¬ is_Fzero x ∧ _root_.F2R x = _root_.F2R y⌝⦄
    (pure (NisFzeroComp_check x y) : Id Unit)
    ⦃⇓_ => ⌜¬ is_Fzero y⌝⦄ := by
  intro ⟨hβ, hx_ne, hxy⟩
  simp only [wp, PostCond.noThrow, pure, NisFzeroComp_check, Id.run,
    ULift.up_down]
  show ¬ is_Fzero y
  intro hy
  apply hx_ne
  have hy0 : _root_.F2R y = 0 := by
    have hy_num : y.Fnum = 0 := hy
    simp [_root_.F2R, FloatSpec.Core.Defs.F2R, hy_num]
  have hx0 : _root_.F2R x = 0 := hxy.trans hy0
  exact FloatSpec.Core.Float_prop.eq_0_F2R (beta:=beta) x hβ hx0

-- Coq: `Fle_Zle` — compare two floats of same exponent by their mantissas
-- We mirror the Coq statement Fle_Zle: n1 ≤ n2 → Fle (Float n1 d) (Float n2 d)
-- Pff's source `float` has the signed fields `Fnum` and `Fexp`, exactly as Core.
noncomputable def Fle_Zle_check (beta : Int) [ValidRadix beta] (n1 n2 d : Int) : Unit :=
  ()

theorem Fle_Zle (beta : Int) [ValidRadix beta] (n1 n2 d : Int) :
    ⦃⌜1 < beta ∧ n1 ≤ n2⌝⦄
    (pure (Fle_Zle_check beta n1 n2 d) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (pff_to_flocq beta { Fnum := n1, Fexp := d })
            ≤ _root_.F2R (pff_to_flocq beta { Fnum := n2, Fexp := d })⌝⦄ := by
  intro ⟨hβ, hn⟩
  simp only [wp, PostCond.noThrow, pure, Fle_Zle_check, Id.run,
    ULift.up_down]
  simp only [pff_to_flocq, _root_.F2R, FloatSpec.Core.Defs.F2R,
    FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp]
  have hβpos : (0 : ℝ) < (beta : ℝ) := by
    have : (0 : Int) < beta := lt_trans (by decide) hβ
    exact_mod_cast this
  have hp : 0 ≤ (beta : ℝ) ^ d := le_of_lt (zpow_pos hβpos d)
  exact mul_le_mul_of_nonneg_right (by exact_mod_cast hn) hp

-- Coq: `Rlt_Fexp_eq_Zlt` — if x < y and Fexp x = Fexp y then Fnum x < Fnum y
noncomputable def Rlt_Fexp_eq_Zlt_check {beta : Int} [ValidRadix beta]
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem Rlt_Fexp_eq_Zlt {beta : Int} [ValidRadix beta]
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜1 < beta ∧ _root_.F2R x < _root_.F2R y ∧ x.Fexp = y.Fexp⌝⦄
    (pure (Rlt_Fexp_eq_Zlt_check (beta:=beta) x y) : Id Unit)
    ⦃⇓_ => ⌜x.Fnum < y.Fnum⌝⦄ := by
  intro ⟨hβ, hlt, hexp⟩
  simp only [wp, PostCond.noThrow, pure, Rlt_Fexp_eq_Zlt_check,
    Id.run, ULift.up_down]
  show x.Fnum < y.Fnum
  simp only [_root_.F2R, FloatSpec.Core.Defs.F2R] at hlt
  rw [hexp] at hlt
  have hβpos : (0 : ℝ) < (beta : ℝ) := by
    have : (0 : Int) < beta := lt_trans (by decide) hβ
    exact_mod_cast this
  have hp : 0 < (beta : ℝ) ^ y.Fexp := zpow_pos hβpos y.Fexp
  have hreal : (x.Fnum : ℝ) < y.Fnum := lt_of_mul_lt_mul_right hlt (le_of_lt hp)
  exact_mod_cast hreal

-- Coq: `Fopp_correct` — float negation corresponds to real negation
noncomputable def Fopp_correct_check {beta : Int} [ValidRadix beta]
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem Fopp_correct {beta : Int} [ValidRadix beta]
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜True⌝⦄
    (pure (Fopp_correct_check (beta:=beta) x) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (FloatSpec.Calc.Operations.Fopp (beta:=beta) x) = - _root_.F2R x⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, Fopp_correct_check, Id.run,
    ULift.up_down]
  show _root_.F2R (FloatSpec.Calc.Operations.Fopp (beta:=beta) x) = - _root_.F2R x
  simp [_root_.F2R, FloatSpec.Core.Defs.F2R, FloatSpec.Calc.Operations.Fopp, neg_mul]

-- Coq: `RoundAbsMonotoner_from_rounded_payload` — absolute upper bounds are preserved by rounding
noncomputable def RoundAbsMonotoner_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p : ℝ) (q r : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `RoundAbsMonotoner_from_rounded_payload` — if `|p|` is bounded above by a bounded float
`r`, then any rounded result `q` of `p` is also bounded by `r` in absolute
value. -/
theorem RoundAbsMonotoner_from_rounded_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p : ℝ) (q r : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜RoundedModeP_float (beta:=beta) b radix P ∧
        Fbounded (beta:=beta) b r ∧ P p q ∧
        |p| ≤ _root_.F2R (beta:=beta) r⌝⦄
    (pure (RoundAbsMonotoner_check (beta:=beta) b radix P p q r) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) q| ≤ _root_.F2R (beta:=beta) r⌝⦄ := by
  intro h
  rcases h with ⟨hRounded, hrBound, hP, hpAbsLeR⟩
  simp only [wp, PostCond.noThrow, pure, RoundAbsMonotoner_check,
    Id.run, ULift.up_down]
  let zeroFloat : FloatSpec.Core.Defs.FlocqFloat beta := ⟨0, -b.dExp⟩
  let negR : FloatSpec.Core.Defs.FlocqFloat beta :=
    FloatSpec.Calc.Operations.Fopp (beta:=beta) r
  have hvNum_pos : (0 : Int) < b.vNum :=
    lt_of_le_of_lt (abs_nonneg r.Fnum) hrBound.1
  have hzeroBound : Fbounded (beta:=beta) b zeroFloat := by
    constructor
    · simpa [zeroFloat]
    · simp [zeroFloat]
  have hzeroVal : _root_.F2R (beta:=beta) zeroFloat = 0 := by
    simp [zeroFloat, _root_.F2R, FloatSpec.Core.Defs.F2R]
  have hnegRBound : Fbounded (beta:=beta) b negR := by
    simpa [negR, Fbounded, FloatSpec.Calc.Operations.Fopp, abs_neg] using hrBound
  have hnegRVal : _root_.F2R (beta:=beta) negR = -_root_.F2R (beta:=beta) r := by
    simpa [negR] using
      (FloatSpec.Calc.Operations.F2R_opp (beta:=beta) r) True.intro
  have hminmax := hRounded.2.2.1 p q hP
  by_cases hp_nonneg : 0 ≤ p
  · have hp_le_r : p ≤ _root_.F2R (beta:=beta) r := by
      simpa [abs_of_nonneg hp_nonneg] using hpAbsLeR
    rcases hminmax with hMin | hMax
    · rcases hMin with ⟨_hqBound, hq_le_p, hGreatest⟩
      have hzero_le_p : _root_.F2R (beta:=beta) zeroFloat ≤ p := by
        rw [hzeroVal]
        exact hp_nonneg
      have hq_nonneg : 0 ≤ _root_.F2R (beta:=beta) q := by
        have hzero_le_q := hGreatest zeroFloat hzeroBound hzero_le_p
        rw [hzeroVal] at hzero_le_q
        exact hzero_le_q
      have hq_le_r : _root_.F2R (beta:=beta) q ≤ _root_.F2R (beta:=beta) r :=
        le_trans hq_le_p hp_le_r
      rw [abs_of_nonneg hq_nonneg]
      exact hq_le_r
    · rcases hMax with ⟨_hqBound, hp_le_q, hLeast⟩
      have hq_nonneg : 0 ≤ _root_.F2R (beta:=beta) q :=
        le_trans hp_nonneg hp_le_q
      have hq_le_r : _root_.F2R (beta:=beta) q ≤ _root_.F2R (beta:=beta) r :=
        hLeast r hrBound hp_le_r
      rw [abs_of_nonneg hq_nonneg]
      exact hq_le_r
  · have hp_neg : p < 0 := lt_of_not_ge hp_nonneg
    have hp_le_zero : p ≤ 0 := le_of_lt hp_neg
    have hnegR_le_p : _root_.F2R (beta:=beta) negR ≤ p := by
      have hpAbs_le : -p ≤ _root_.F2R (beta:=beta) r := by
        simpa [abs_of_neg hp_neg] using hpAbsLeR
      calc
        _root_.F2R (beta:=beta) negR = -_root_.F2R (beta:=beta) r := hnegRVal
        _ ≤ p := by linarith
    rcases hminmax with hMin | hMax
    · rcases hMin with ⟨_hqBound, hq_le_p, hGreatest⟩
      have hq_le_zero : _root_.F2R (beta:=beta) q ≤ 0 :=
        le_trans hq_le_p hp_le_zero
      have hnegR_le_q : _root_.F2R (beta:=beta) negR ≤ _root_.F2R (beta:=beta) q :=
        hGreatest negR hnegRBound hnegR_le_p
      have hneg_r_le_q : -_root_.F2R (beta:=beta) r ≤ _root_.F2R (beta:=beta) q := by
        rw [hnegRVal] at hnegR_le_q
        exact hnegR_le_q
      calc
        |_root_.F2R (beta:=beta) q| = -_root_.F2R (beta:=beta) q :=
          abs_of_nonpos hq_le_zero
        _ ≤ _root_.F2R (beta:=beta) r := by linarith
    · rcases hMax with ⟨_hqBound, hp_le_q, hLeast⟩
      have hp_le_zeroFloat : p ≤ _root_.F2R (beta:=beta) zeroFloat := by
        rw [hzeroVal]
        exact hp_le_zero
      have hq_le_zero : _root_.F2R (beta:=beta) q ≤ 0 := by
        have hq_le_zeroFloat := hLeast zeroFloat hzeroBound hp_le_zeroFloat
        rw [hzeroVal] at hq_le_zeroFloat
        exact hq_le_zeroFloat
      have hnegR_le_q : _root_.F2R (beta:=beta) negR ≤ _root_.F2R (beta:=beta) q :=
        le_trans hnegR_le_p hp_le_q
      have hneg_r_le_q : -_root_.F2R (beta:=beta) r ≤ _root_.F2R (beta:=beta) q := by
        rw [hnegRVal] at hnegR_le_q
        exact hnegR_le_q
      calc
        |_root_.F2R (beta:=beta) q| = -_root_.F2R (beta:=beta) q :=
          abs_of_nonpos hq_le_zero
        _ ≤ _root_.F2R (beta:=beta) r := by linarith

-- Coq: `RoundAbsMonotonel_from_rounded_payload` — absolute lower bounds are preserved by rounding
noncomputable def RoundAbsMonotonel_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p : ℝ) (q r : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `RoundAbsMonotonel_from_rounded_payload` — if a bounded float `r` is below `|p|`, then it
is also below the absolute value of any rounded result `q` of `p`. -/
theorem RoundAbsMonotonel_from_rounded_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p : ℝ) (q r : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜RoundedModeP_float (beta:=beta) b radix P ∧
        Fbounded (beta:=beta) b r ∧ P p q ∧
        _root_.F2R (beta:=beta) r ≤ |p|⌝⦄
    (pure (RoundAbsMonotonel_check (beta:=beta) b radix P p q r) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) r ≤ |_root_.F2R (beta:=beta) q|⌝⦄ := by
  intro h
  rcases h with ⟨hRounded, hrBound, hP, hrLeAbsP⟩
  simp only [wp, PostCond.noThrow, pure, RoundAbsMonotonel_check,
    Id.run, ULift.up_down]
  let zeroFloat : FloatSpec.Core.Defs.FlocqFloat beta := ⟨0, -b.dExp⟩
  let negR : FloatSpec.Core.Defs.FlocqFloat beta :=
    FloatSpec.Calc.Operations.Fopp (beta:=beta) r
  have hvNum_pos : (0 : Int) < b.vNum :=
    lt_of_le_of_lt (abs_nonneg r.Fnum) hrBound.1
  have hzeroBound : Fbounded (beta:=beta) b zeroFloat := by
    constructor
    · simpa [zeroFloat]
    · simp [zeroFloat]
  have hzeroVal : _root_.F2R (beta:=beta) zeroFloat = 0 := by
    simp [zeroFloat, _root_.F2R, FloatSpec.Core.Defs.F2R]
  have hnegRBound : Fbounded (beta:=beta) b negR := by
    simpa [negR, Fbounded, FloatSpec.Calc.Operations.Fopp, abs_neg] using hrBound
  have hnegRVal : _root_.F2R (beta:=beta) negR = -_root_.F2R (beta:=beta) r := by
    simpa [negR] using
      (FloatSpec.Calc.Operations.F2R_opp (beta:=beta) r) True.intro
  have hminmax := hRounded.2.2.1 p q hP
  by_cases hp_nonneg : 0 ≤ p
  · have hr_le_p : _root_.F2R (beta:=beta) r ≤ p := by
      simpa [abs_of_nonneg hp_nonneg] using hrLeAbsP
    rcases hminmax with hMin | hMax
    · rcases hMin with ⟨_hqBound, _hq_le_p, hGreatest⟩
      have hzero_le_p : _root_.F2R (beta:=beta) zeroFloat ≤ p := by
        rw [hzeroVal]
        exact hp_nonneg
      have hq_nonneg : 0 ≤ _root_.F2R (beta:=beta) q := by
        have hzero_le_q := hGreatest zeroFloat hzeroBound hzero_le_p
        rw [hzeroVal] at hzero_le_q
        exact hzero_le_q
      have hr_le_q : _root_.F2R (beta:=beta) r ≤ _root_.F2R (beta:=beta) q :=
        hGreatest r hrBound hr_le_p
      exact le_trans hr_le_q (le_of_eq (abs_of_nonneg hq_nonneg).symm)
    · rcases hMax with ⟨_hqBound, hp_le_q, _hLeast⟩
      have hq_nonneg : 0 ≤ _root_.F2R (beta:=beta) q :=
        le_trans hp_nonneg hp_le_q
      have hr_le_q : _root_.F2R (beta:=beta) r ≤ _root_.F2R (beta:=beta) q :=
        le_trans hr_le_p hp_le_q
      exact le_trans hr_le_q (le_of_eq (abs_of_nonneg hq_nonneg).symm)
  · have hp_neg : p < 0 := lt_of_not_ge hp_nonneg
    have hp_le_zero : p ≤ 0 := le_of_lt hp_neg
    have hp_le_negR : p ≤ _root_.F2R (beta:=beta) negR := by
      have hr_le_negp : _root_.F2R (beta:=beta) r ≤ -p := by
        simpa [abs_of_neg hp_neg] using hrLeAbsP
      calc
        p ≤ -_root_.F2R (beta:=beta) r := by linarith
        _ = _root_.F2R (beta:=beta) negR := hnegRVal.symm
    rcases hminmax with hMin | hMax
    · rcases hMin with ⟨_hqBound, hq_le_p, _hGreatest⟩
      have hq_le_zero : _root_.F2R (beta:=beta) q ≤ 0 :=
        le_trans hq_le_p hp_le_zero
      have hq_le_negR : _root_.F2R (beta:=beta) q ≤ _root_.F2R (beta:=beta) negR :=
        le_trans hq_le_p hp_le_negR
      have hq_le_neg_r : _root_.F2R (beta:=beta) q ≤ -_root_.F2R (beta:=beta) r := by
        rw [hnegRVal] at hq_le_negR
        exact hq_le_negR
      calc
        _root_.F2R (beta:=beta) r ≤ -_root_.F2R (beta:=beta) q := by linarith
        _ = |_root_.F2R (beta:=beta) q| := (abs_of_nonpos hq_le_zero).symm
    · rcases hMax with ⟨_hqBound, _hp_le_q, hLeast⟩
      have hp_le_zeroFloat : p ≤ _root_.F2R (beta:=beta) zeroFloat := by
        rw [hzeroVal]
        exact hp_le_zero
      have hq_le_zero : _root_.F2R (beta:=beta) q ≤ 0 := by
        have hq_le_zeroFloat := hLeast zeroFloat hzeroBound hp_le_zeroFloat
        rw [hzeroVal] at hq_le_zeroFloat
        exact hq_le_zeroFloat
      have hq_le_negR : _root_.F2R (beta:=beta) q ≤ _root_.F2R (beta:=beta) negR :=
        hLeast negR hnegRBound hp_le_negR
      have hq_le_neg_r : _root_.F2R (beta:=beta) q ≤ -_root_.F2R (beta:=beta) r := by
        rw [hnegRVal] at hq_le_negR
        exact hq_le_negR
      calc
        _root_.F2R (beta:=beta) r ≤ -_root_.F2R (beta:=beta) q := by linarith
        _ = |_root_.F2R (beta:=beta) q| := (abs_of_nonpos hq_le_zero).symm

noncomputable def pGeUnderf_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (b p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `pGeUnderf`.

In the nonzero branch of the first discriminant underflow hypothesis, the
rounded value `p` of `b*b` inherits the displayed lower magnitude bound by
absolute-value monotonicity of the even-closest rounding mode. -/
theorem pGeUnderf_from_total_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (b p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 0 < precision ∧
        bo.vNum = Zpower_nat radix precision ∧
        TotalP (EvenClosest (beta:=beta) bo (radix : ℝ) precision) ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b) p ∧
        (_root_.F2R (beta:=beta) b = 0 ∨
          (radix : ℝ) ^ (-bo.dExp + 3 * (precision : Int) - 1) ≤
            |_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b|) ∧
        _root_.F2R (beta:=beta) b ≠ 0⌝⦄
    (pure (pGeUnderf_check (beta:=beta) bo radix precision b p) : Id Unit)
    ⦃⇓_ => ⌜(radix : ℝ) ^ (-bo.dExp + 3 * (precision : Int) - 1) ≤
        |_root_.F2R (beta:=beta) p|⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hradix, hprecision, hvNum, hTotal, hpRound, hUnderf, hbNonzero⟩
  simp only [wp, PostCond.noThrow, pure, pGeUnderf_check,
    Id.run, ULift.up_down]
  subst beta
  let e : Int := -bo.dExp + 3 * (precision : Int) - 1
  let witness : FloatSpec.Core.Defs.FlocqFloat radix := ⟨1, e⟩
  have hmag : (radix : ℝ) ^ e ≤
      |_root_.F2R (beta:=radix) b * _root_.F2R (beta:=radix) b| := by
    rcases hUnderf with hbZero | hmag
    · exact False.elim (hbNonzero hbZero)
    · simpa [e] using hmag
  have hradix_pos : 0 < radix := by omega
  have hvNum_gt_one : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    cases precision with
    | zero => cases hprecision
    | succ k =>
        rw [pow_succ, mul_comm]
        have hpow_pos : 0 < radix ^ k := pow_pos hradix_pos k
        have hpow_ge_one : 1 ≤ radix ^ k := by omega
        have hmul_lt : 1 * radix ^ k < radix * radix ^ k :=
          mul_lt_mul_of_pos_right hradix hpow_pos
        exact lt_of_le_of_lt hpow_ge_one (by simpa [one_mul] using hmul_lt)
  have hwitnessBound : Fbounded (beta:=radix) bo witness := by
    constructor
    · simp [witness, hvNum_gt_one]
    · have hprecision_int : (1 : Int) ≤ (precision : Int) := by
        exact_mod_cast hprecision
      simp [witness, e]
      omega
  have hwitnessVal :
      _root_.F2R (beta:=radix) witness = (radix : ℝ) ^ e := by
    simp [witness, _root_.F2R, FloatSpec.Core.Defs.F2R]
  have hwitnessLe :
      _root_.F2R (beta:=radix) witness ≤
        |_root_.F2R (beta:=radix) b * _root_.F2R (beta:=radix) b| := by
    rw [hwitnessVal]
    exact hmag
  have hRounded :
      RoundedModeP_float (beta:=radix) bo radix
        (EvenClosest (beta:=radix) bo (radix : ℝ) precision) := by
    have h := EvenClosestRoundedModeP_from_total (beta:=radix) bo radix (radix : ℝ) precision
    simpa only [wp, PostCond.noThrow, pure, EvenClosestRoundedModeP_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using h hTotal
  have hmono := RoundAbsMonotonel_from_rounded_payload (beta:=radix) bo radix
    (EvenClosest (beta:=radix) bo (radix : ℝ) precision)
    (_root_.F2R (beta:=radix) b * _root_.F2R (beta:=radix) b) p witness
  have hres :
      _root_.F2R (beta:=radix) witness ≤
        |_root_.F2R (beta:=radix) p| := by
    simpa only [wp, PostCond.noThrow, pure, RoundAbsMonotonel_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
      hmono ⟨hRounded, hwitnessBound, hpRound, hwitnessLe⟩
  rw [hwitnessVal] at hres
  simpa [e] using hres

noncomputable def qGeUnderf_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a c q : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `qGeUnderf`.

Symmetric underflow branch for the rounded product `a*c`: when the exact
product is nonzero, the rounded value `q` inherits the lower magnitude bound
from the second underflow hypothesis. -/
theorem qGeUnderf_from_total_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (a c q : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 0 < precision ∧
        bo.vNum = Zpower_nat radix precision ∧
        TotalP (EvenClosest (beta:=beta) bo (radix : ℝ) precision) ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c) q ∧
        (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c = 0 ∨
          (radix : ℝ) ^ (-bo.dExp + 3 * (precision : Int) - 1) ≤
            |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c|) ∧
        _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c ≠ 0⌝⦄
    (pure (qGeUnderf_check (beta:=beta) bo radix precision a c q) : Id Unit)
    ⦃⇓_ => ⌜(radix : ℝ) ^ (-bo.dExp + 3 * (precision : Int) - 1) ≤
        |_root_.F2R (beta:=beta) q|⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hradix, hprecision, hvNum, hTotal, hqRound, hUnderf, hacNonzero⟩
  simp only [wp, PostCond.noThrow, pure, qGeUnderf_check,
    Id.run, ULift.up_down]
  subst beta
  let e : Int := -bo.dExp + 3 * (precision : Int) - 1
  let witness : FloatSpec.Core.Defs.FlocqFloat radix := ⟨1, e⟩
  have hmag : (radix : ℝ) ^ e ≤
      |_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) c| := by
    rcases hUnderf with hacZero | hmag
    · exact False.elim (hacNonzero hacZero)
    · simpa [e] using hmag
  have hradix_pos : 0 < radix := by omega
  have hvNum_gt_one : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    cases precision with
    | zero => cases hprecision
    | succ k =>
        rw [pow_succ, mul_comm]
        have hpow_pos : 0 < radix ^ k := pow_pos hradix_pos k
        have hpow_ge_one : 1 ≤ radix ^ k := by omega
        have hmul_lt : 1 * radix ^ k < radix * radix ^ k :=
          mul_lt_mul_of_pos_right hradix hpow_pos
        exact lt_of_le_of_lt hpow_ge_one (by simpa [one_mul] using hmul_lt)
  have hwitnessBound : Fbounded (beta:=radix) bo witness := by
    constructor
    · simp [witness, hvNum_gt_one]
    · have hprecision_int : (1 : Int) ≤ (precision : Int) := by
        exact_mod_cast hprecision
      simp [witness, e]
      omega
  have hwitnessVal :
      _root_.F2R (beta:=radix) witness = (radix : ℝ) ^ e := by
    simp [witness, _root_.F2R, FloatSpec.Core.Defs.F2R]
  have hwitnessLe :
      _root_.F2R (beta:=radix) witness ≤
        |_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) c| := by
    rw [hwitnessVal]
    exact hmag
  have hRounded :
      RoundedModeP_float (beta:=radix) bo radix
        (EvenClosest (beta:=radix) bo (radix : ℝ) precision) := by
    have h := EvenClosestRoundedModeP_from_total (beta:=radix) bo radix (radix : ℝ) precision
    simpa only [wp, PostCond.noThrow, pure, EvenClosestRoundedModeP_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using h hTotal
  have hmono := RoundAbsMonotonel_from_rounded_payload (beta:=radix) bo radix
    (EvenClosest (beta:=radix) bo (radix : ℝ) precision)
    (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) c) q witness
  have hres :
      _root_.F2R (beta:=radix) witness ≤
        |_root_.F2R (beta:=radix) q| := by
    simpa only [wp, PostCond.noThrow, pure, RoundAbsMonotonel_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
      hmono ⟨hRounded, hwitnessBound, hqRound, hwitnessLe⟩
  rw [hwitnessVal] at hres
  simpa [e] using hres

-- Coq: `Fplus_correct` — float addition corresponds to real addition
noncomputable def Fplus_correct_check {beta : Int} [ValidRadix beta]
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem Fplus_correct {beta : Int} [ValidRadix beta]
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜1 < beta⌝⦄
    (pure (Fplus_correct_check (beta:=beta) x y) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (Fplus (beta:=beta) x y) = _root_.F2R x + _root_.F2R y⌝⦄ := by
  intro hβ
  simp only [wp, PostCond.noThrow, pure, Fplus_correct_check, Id.run,
    ULift.up_down]
  show _root_.F2R (Fplus x y) = _root_.F2R x + _root_.F2R y
  have h := FloatSpec.Calc.Operations.F2R_plus (beta:=beta) x y hβ
  simpa [Fplus, _root_.F2R] using h

-- Coq: `Fminus_correct` — float subtraction corresponds to real subtraction
noncomputable def Fminus_correct_check {beta : Int} [ValidRadix beta]
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem Fminus_correct {beta : Int} [ValidRadix beta]
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜1 < beta⌝⦄
    (pure (Fminus_correct_check (beta:=beta) x y) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (FloatSpec.Calc.Operations.Fminus (beta:=beta) x y) =
            _root_.F2R x - _root_.F2R y⌝⦄ := by
  intro hβ
  simp only [wp, PostCond.noThrow, pure, Fminus_correct_check, Fminus, Id.run,
    ULift.up_down]
  show _root_.F2R (FloatSpec.Calc.Operations.Fminus (beta:=beta) x y) =
      _root_.F2R x - _root_.F2R y
  have h := FloatSpec.Calc.Operations.F2R_minus (beta:=beta) x y hβ
  simpa [_root_.F2R] using h

-- Coq: `Fmult_correct` — float multiplication corresponds to real multiplication
noncomputable def Fmult_correct_check {beta : Int} [ValidRadix beta]
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem Fmult_correct {beta : Int} [ValidRadix beta]
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜1 < beta⌝⦄
    (pure (Fmult_correct_check (beta:=beta) x y) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (FloatSpec.Calc.Operations.Fmult (beta:=beta) x y) =
            _root_.F2R x * _root_.F2R y⌝⦄ := by
  intro hβ
  simp only [wp, PostCond.noThrow, pure, Fmult_correct_check, Id.run,
    ULift.up_down]
  have h := FloatSpec.Calc.Operations.F2R_mult (beta:=beta) x y
  simpa [_root_.F2R, wp, PostCond.noThrow, pure] using h hβ

-- Coq: `Fopp_Fopp` — involutive property of float negation
noncomputable def Fopp_Fopp_check {beta : Int} [ValidRadix beta]
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem Fopp_Fopp {beta : Int} [ValidRadix beta]
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜True⌝⦄
    (pure (Fopp_Fopp_check (beta:=beta) p) : Id Unit)
    ⦃⇓_ => ⌜Fopp (beta:=beta) (Fopp (beta:=beta) p) = p⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, Fopp_Fopp_check, Id.run,
    ULift.up_down]
  show Fopp (Fopp p) = p
  cases p
  simp [Fopp, FloatSpec.Calc.Operations.Fopp]

noncomputable def MinOrMax_Fopp_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (x : ℝ) (f : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `MinOrMax_Fopp`.

Negating both the real target and the float swaps lower and upper witnesses;
double negation returns the original float. -/
theorem MinOrMax_Fopp {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (x : ℝ) (f : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜MinOrMax (beta:=beta) b radix (-x) (Fopp (beta:=beta) f)⌝⦄
    (pure (MinOrMax_Fopp_check (beta:=beta) b radix x f) : Id Unit)
    ⦃⇓_ => ⌜MinOrMax (beta:=beta) b radix x f⌝⦄ := by
  intro h
  simp only [wp, PostCond.noThrow, pure, MinOrMax_Fopp_check,
    Id.run, ULift.up_down]
  rcases h with hMin | hMax
  · right
    have hTrip := MinOppMax (beta:=beta) b radix (Fopp (beta:=beta) f) (-x)
    have hMaxDouble :
        isMax (beta:=beta) b radix x
          (Fopp (beta:=beta) (Fopp (beta:=beta) f)) := by
      simpa only [wp, PostCond.noThrow, pure, MinOppMax_check,
        PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
        Id.run, ULift.up, ULift.down, ULift.up_down, neg_neg, Int.cast_ofNat] using hTrip hMin
    have hDouble :
        Fopp (beta:=beta) (Fopp (beta:=beta) f) = f := by
      have hFopp := Fopp_Fopp (beta:=beta) f
      simpa only [wp, PostCond.noThrow, pure, Fopp_Fopp_check,
        PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
        Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using hFopp True.intro
    simpa [hDouble] using hMaxDouble
  · left
    have hTrip := MaxOppMin (beta:=beta) b radix (Fopp (beta:=beta) f) (-x)
    have hMinDouble :
        isMin (beta:=beta) b radix x
          (Fopp (beta:=beta) (Fopp (beta:=beta) f)) := by
      simpa only [wp, PostCond.noThrow, pure, MaxOppMin_check,
        PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
        Id.run, ULift.up, ULift.down, ULift.up_down, neg_neg, Int.cast_ofNat] using hTrip hMax
    have hDouble :
        Fopp (beta:=beta) (Fopp (beta:=beta) f) = f := by
      have hFopp := Fopp_Fopp (beta:=beta) f
      simpa only [wp, PostCond.noThrow, pure, Fopp_Fopp_check,
        PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
        Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using hFopp True.intro
    simpa [hDouble] using hMinDouble

-- Coq: `Fopp_Fminus` — negation of a subtraction swaps the operands
noncomputable def Fopp_Fminus_check {beta : Int} [ValidRadix beta]
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem Fopp_Fminus {beta : Int} [ValidRadix beta]
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜True⌝⦄
    (pure (Fopp_Fminus_check (beta:=beta) p q) : Id Unit)
    ⦃⇓_ => ⌜Fopp (beta:=beta)
              (FloatSpec.Calc.Operations.Fminus (beta:=beta) p q) =
            FloatSpec.Calc.Operations.Fminus (beta:=beta) q p⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, Fopp_Fminus_check, Id.run,
    ULift.up_down]
  show Fopp (FloatSpec.Calc.Operations.Fminus (beta:=beta) p q) =
      FloatSpec.Calc.Operations.Fminus (beta:=beta) q p
  cases p with
  | mk mp ep =>
    cases q with
    | mk mq eq =>
      by_cases hle : ep ≤ eq
      · by_cases hrev : eq ≤ ep
        · have heq : ep = eq := le_antisymm hle hrev
          subst eq
          simp [Fopp, FloatSpec.Calc.Operations.Fopp, FloatSpec.Calc.Operations.Fminus,
            FloatSpec.Calc.Operations.Fplus, FloatSpec.Calc.Operations.Falign]
        · simp [Fopp, FloatSpec.Calc.Operations.Fopp, FloatSpec.Calc.Operations.Fminus,
            FloatSpec.Calc.Operations.Fplus, FloatSpec.Calc.Operations.Falign, hle, hrev]
      · have hge : eq ≤ ep := le_of_lt (lt_of_not_ge hle)
        simp [Fopp, FloatSpec.Calc.Operations.Fopp, FloatSpec.Calc.Operations.Fminus,
          FloatSpec.Calc.Operations.Fplus, FloatSpec.Calc.Operations.Falign, hle, hge,
          add_comm]

-- Coq: `Fdigit_opp` — digit invariant under negation
noncomputable def Fdigit_opp_check {beta : Int} [ValidRadix beta]
    (radix : Int) (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem Fdigit_opp {beta : Int} [ValidRadix beta]
    (radix : Int) (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜True⌝⦄
    (pure (Fdigit_opp_check (beta:=beta) radix x) : Id Unit)
    ⦃⇓_ => ⌜Fdigit (beta:=beta) radix (Fopp x) = Fdigit (beta:=beta) radix x⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, Fdigit_opp_check, Id.run,
    ULift.up_down]
  show Fdigit radix (Fopp x) = Fdigit radix x
  cases x with
  | mk m e =>
    change digit radix (-m) = digit radix m
    exact digit_neg radix m

-- Coq: `Fopp_Fminus_dist` — negation distributes over subtraction
noncomputable def Fopp_Fminus_dist_check {beta : Int} [ValidRadix beta]
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem Fopp_Fminus_dist {beta : Int} [ValidRadix beta]
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜True⌝⦄
    (pure (Fopp_Fminus_dist_check (beta:=beta) p q) : Id Unit)
    ⦃⇓_ => ⌜Fopp (beta:=beta)
              (FloatSpec.Calc.Operations.Fminus (beta:=beta) p q) =
            FloatSpec.Calc.Operations.Fminus (beta:=beta)
              (Fopp (beta:=beta) p) (Fopp (beta:=beta) q)⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, Fopp_Fminus_dist_check,
    Id.run, ULift.up_down]
  show Fopp (FloatSpec.Calc.Operations.Fminus (beta:=beta) p q) =
      FloatSpec.Calc.Operations.Fminus (beta:=beta) (Fopp p) (Fopp q)
  cases p with
  | mk mp ep =>
    cases q with
    | mk mq eq =>
      by_cases hle : ep ≤ eq
      · simp [Fopp, FloatSpec.Calc.Operations.Fopp, FloatSpec.Calc.Operations.Fminus,
          FloatSpec.Calc.Operations.Fplus, FloatSpec.Calc.Operations.Falign, hle]
        ring
      · have hge : eq ≤ ep := le_of_lt (lt_of_not_ge hle)
        simp [Fopp, FloatSpec.Calc.Operations.Fopp, FloatSpec.Calc.Operations.Fminus,
          FloatSpec.Calc.Operations.Fplus, FloatSpec.Calc.Operations.Falign, hle, hge,
          add_comm]

/-!
Sterbenz lemmas (missing from Coq Pff.v)

We introduce Coq's `SterbenzAux` in the project's Hoare-triple style. It uses
the local `Fbounded` predicate and `Fminus` operation available in this file.
-/

noncomputable def SterbenzAux_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `SterbenzAux` — if `x` and `y` are bounded in the same bound `b` and
    satisfy `F2R y ≤ F2R x ≤ 2 * F2R y`, then `Fminus x y` is bounded in `b`. -/
theorem SterbenzAux {beta : Int} [ValidRadix beta]
    (b : Fbound_skel)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜1 < beta ∧
        Fbounded (beta:=beta) b x ∧
        Fbounded (beta:=beta) b y ∧
        (_root_.F2R (beta:=beta) y) ≤ (_root_.F2R (beta:=beta) x) ∧
        (_root_.F2R (beta:=beta) x) ≤ 2 * (_root_.F2R (beta:=beta) y)⌝⦄
    (pure (SterbenzAux_check (beta:=beta) b x y) : Id Unit)
    ⦃⇓_ => ⌜Fbounded (beta:=beta) b
        (FloatSpec.Calc.Operations.Fminus (beta:=beta) x y)⌝⦄ := by
  intro h
  rcases h with ⟨hβ, hxBounded, hyBounded, hyx, hx2y⟩
  simp only [wp, PostCond.noThrow, pure, SterbenzAux_check, Id.run,
    ULift.up_down]
  let d := FloatSpec.Calc.Operations.Fminus (beta:=beta) x y
  have hdVal : _root_.F2R (beta:=beta) d =
      _root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) y := by
    have hminus := Fminus_correct (beta:=beta) x y
    simpa [d, wp, PostCond.noThrow, pure, Fminus_correct_check] using hminus hβ
  have hyNonneg : 0 ≤ _root_.F2R (beta:=beta) y := by
    have hy_le_2y : _root_.F2R (beta:=beta) y ≤
        2 * _root_.F2R (beta:=beta) y := le_trans hyx hx2y
    nlinarith
  have hsubNonneg :
      0 ≤ _root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) y := by
    nlinarith
  have hdNonneg : 0 ≤ _root_.F2R (beta:=beta) d := by
    rw [hdVal]
    exact hsubNonneg
  have hdLeX : _root_.F2R (beta:=beta) d ≤ _root_.F2R (beta:=beta) x := by
    rw [hdVal]
    nlinarith
  have hdLeY : _root_.F2R (beta:=beta) d ≤ _root_.F2R (beta:=beta) y := by
    rw [hdVal]
    nlinarith
  have hdFnum_nonneg : 0 ≤ d.Fnum := by
    let z : FloatSpec.Core.Defs.FlocqFloat beta := ⟨0, d.Fexp⟩
    have hzVal : _root_.F2R (beta:=beta) z = 0 := by
      simp [z, _root_.F2R, FloatSpec.Core.Defs.F2R]
    have hnumTrip := Rle_Fexp_eq_Zle (beta:=beta) z d
    have hnum : z.Fnum ≤ d.Fnum := by
      simpa only [wp, PostCond.noThrow, pure, Rle_Fexp_eq_Zle_check,
        PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
        Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
        hnumTrip ⟨by rw [hzVal]; exact hdNonneg, rfl, hβ⟩
    simpa [z] using hnum
  constructor
  · by_cases hexp : x.Fexp ≤ y.Fexp
    · have hdExp : d.Fexp = x.Fexp := by
        cases x with
        | mk mx ex =>
          cases y with
          | mk my ey =>
            simp [d, FloatSpec.Calc.Operations.Fminus,
              FloatSpec.Calc.Operations.Fplus, FloatSpec.Calc.Operations.Falign,
              FloatSpec.Calc.Operations.Fopp, hexp]
      have hdFnum_le_x : d.Fnum ≤ x.Fnum := by
        have hnum := Rle_Fexp_eq_Zle (beta:=beta) d x ⟨hdLeX, hdExp, hβ⟩
        simpa using hnum
      have hdAbs_le_xAbs : |d.Fnum| ≤ |x.Fnum| := by
        rw [abs_of_nonneg hdFnum_nonneg]
        exact le_trans hdFnum_le_x (le_abs_self x.Fnum)
      exact lt_of_le_of_lt hdAbs_le_xAbs hxBounded.1
    · have hyxExp : y.Fexp ≤ x.Fexp := le_of_lt (lt_of_not_ge hexp)
      have hdExp : d.Fexp = y.Fexp := by
        cases x with
        | mk mx ex =>
          cases y with
          | mk my ey =>
            simp [d, FloatSpec.Calc.Operations.Fminus,
              FloatSpec.Calc.Operations.Fplus, FloatSpec.Calc.Operations.Falign,
              FloatSpec.Calc.Operations.Fopp, hexp]
      have hdFnum_le_y : d.Fnum ≤ y.Fnum := by
        have hnum := Rle_Fexp_eq_Zle (beta:=beta) d y ⟨hdLeY, hdExp, hβ⟩
        simpa using hnum
      have hdAbs_le_yAbs : |d.Fnum| ≤ |y.Fnum| := by
        rw [abs_of_nonneg hdFnum_nonneg]
        exact le_trans hdFnum_le_y (le_abs_self y.Fnum)
      exact lt_of_le_of_lt hdAbs_le_yAbs hyBounded.1
  · by_cases hexp : x.Fexp ≤ y.Fexp
    · have hdExp : d.Fexp = x.Fexp := by
        cases x with
        | mk mx ex =>
          cases y with
          | mk my ey =>
            simp [d, FloatSpec.Calc.Operations.Fminus,
              FloatSpec.Calc.Operations.Fplus, FloatSpec.Calc.Operations.Falign,
              FloatSpec.Calc.Operations.Fopp, hexp]
      rw [hdExp]
      exact hxBounded.2
    · have hdExp : d.Fexp = y.Fexp := by
        cases x with
        | mk mx ex =>
          cases y with
          | mk my ey =>
            simp [d, FloatSpec.Calc.Operations.Fminus,
              FloatSpec.Calc.Operations.Fplus, FloatSpec.Calc.Operations.Falign,
              FloatSpec.Calc.Operations.Fopp, hexp]
      rw [hdExp]
      exact hyBounded.2

-- Coq: `Sterbenz` — symmetric bound version using 1/2 ≤ x/y ≤ 2
noncomputable def Sterbenz_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `Sterbenz` — if `x` and `y` are bounded in the same bound `b` and
    satisfy `1/2 * F2R y ≤ F2R x ≤ 2 * F2R y`, then `Fminus x y` is bounded. -/
theorem Sterbenz {beta : Int} [ValidRadix beta]
    (b : Fbound_skel)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜1 < beta ∧
        Fbounded (beta:=beta) b x ∧
        Fbounded (beta:=beta) b y ∧
        (1 / 2 : ℝ) * (_root_.F2R (beta:=beta) y) ≤
          (_root_.F2R (beta:=beta) x) ∧
        (_root_.F2R (beta:=beta) x) ≤ 2 * (_root_.F2R (beta:=beta) y)⌝⦄
    (pure (Sterbenz_check (beta:=beta) b x y) : Id Unit)
    ⦃⇓_ => ⌜Fbounded (beta:=beta) b
        (FloatSpec.Calc.Operations.Fminus (beta:=beta) x y)⌝⦄ := by
  intro h
  rcases h with ⟨hβ, hxBounded, hyBounded, hhalf, hx2y⟩
  simp only [wp, PostCond.noThrow, pure, Sterbenz_check, Fminus, Id.run,
    ULift.up_down]
  by_cases hyx : _root_.F2R (beta:=beta) y ≤ _root_.F2R (beta:=beta) x
  · have haux := SterbenzAux (beta:=beta) b x y
    simpa [wp, PostCond.noThrow, pure, SterbenzAux_check] using
      haux ⟨hβ, hxBounded, hyBounded, hyx, hx2y⟩
  · have hxy : _root_.F2R (beta:=beta) x ≤ _root_.F2R (beta:=beta) y :=
      le_of_lt (lt_of_not_ge hyx)
    have hy2x : _root_.F2R (beta:=beta) y ≤ 2 * _root_.F2R (beta:=beta) x := by
      nlinarith
    have haux := SterbenzAux (beta:=beta) b y x
    have hyxMinusBounded :
        Fbounded (beta:=beta) b
          (FloatSpec.Calc.Operations.Fminus (beta:=beta) y x) := by
      simpa [wp, PostCond.noThrow, pure, SterbenzAux_check] using
        haux ⟨hβ, hyBounded, hxBounded, hxy, hy2x⟩
    have hswap :
        Fopp (beta:=beta)
            (FloatSpec.Calc.Operations.Fminus (beta:=beta) x y) =
          FloatSpec.Calc.Operations.Fminus (beta:=beta) y x := by
      have hswapTrip := Fopp_Fminus (beta:=beta) x y
      simpa [wp, PostCond.noThrow, pure, Fopp_Fminus_check] using
        hswapTrip trivial
    have hoppBounded :
        Fbounded (beta:=beta) b
          (Fopp (beta:=beta)
            (FloatSpec.Calc.Operations.Fminus (beta:=beta) x y)) := by
      rw [hswap]
      exact hyxMinusBounded
    have hinv :=
      oppBoundedInv (beta:=beta) b
        (FloatSpec.Calc.Operations.Fminus (beta:=beta) x y)
    simpa [wp, PostCond.noThrow, pure, oppBoundedInv_check] using
      hinv hoppBounded

/-!
Fast2Sum/Dekker support.

Upstream Flocq's `Dekker_FTS` finishes through `MDekkerAux1`: after the first
subtraction is known exact and the rounded addition error is represented by a
bounded float, the second rounded subtraction is an idempotent closest rounding.
This lemma ports that final payload without claiming the full `Dekker_FTS`
stack; the remaining upstream work is to restore `ErrorBoundedIplus` and
`MDekker`.
-/

/-- Coq: `IminusCorrect`.

In the upstream Fast2Sum section, subtraction is defined through addition with
an opposite operand.  This restores the corresponding closest-rounding payload
for the abstract `Iminus` operation. -/
theorem IminusCorrect {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q)) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      Closest (beta:=beta) bo radix
        (_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q)
        (Iminus p q) := by
  intro p q hp hq
  have hqOpp : Fbounded (beta:=beta) bo (Fopp (beta:=beta) q) := by
    have h := oppBounded (beta:=beta) bo q
    simpa [wp, PostCond.noThrow, pure, oppBounded_check, Id.run,
      ULift.up_down] using h hq
  have hclosest := IplusCorrect p (Fopp (beta:=beta) q) hp hqOpp
  have hqVal :
      _root_.F2R (beta:=beta) (Fopp (beta:=beta) q) =
        -_root_.F2R (beta:=beta) q := by
    simp [Fopp, FloatSpec.Calc.Operations.Fopp, _root_.F2R,
      FloatSpec.Core.Defs.F2R]
  rw [IminusPlus]
  rw [hqVal] at hclosest
  simpa only [sub_eq_add_neg, Int.cast_ofNat] using hclosest

/-- Coq: `IplusBounded`. -/
theorem IplusBounded {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (Iplus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q)) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      Fbounded (beta:=beta) bo (Iplus p q) := by
  intro p q hp hq
  exact (IplusCorrect p q hp hq).1

/-- Coq: `IminusBounded`. -/
theorem IminusBounded {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q)) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      Fbounded (beta:=beta) bo (Iminus p q) := by
  intro p q hp hq
  exact (IminusCorrect (beta:=beta) bo radix Iplus Iminus
    IplusCorrect IminusPlus p q hp hq).1

/-- Coq: `IplusOr`.

If the right operand represents zero, closest rounding of `p + q` returns a
float with the same real value as `p`. -/
theorem IplusOr {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (Iplus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q)) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      _root_.F2R (beta:=beta) q = 0 →
      _root_.F2R (beta:=beta) (Iplus p q) =
        _root_.F2R (beta:=beta) p := by
  intro p q hp hq hq0
  have hclosest0 :
      Closest (beta:=beta) bo radix (_root_.F2R (beta:=beta) p)
        (Iplus p q) := by
    have hclosest := IplusCorrect p q hp hq
    rw [hq0] at hclosest
    simpa only [add_zero, Int.cast_ofNat] using hclosest
  have hIdem := ClosestIdem_from_expanded_closest_payload (beta:=beta) bo radix p (Iplus p q)
  have hp_eq :
      _root_.F2R (beta:=beta) p =
        _root_.F2R (beta:=beta) (Iplus p q) := by
    simpa [wp, PostCond.noThrow, pure, ClosestIdem_check, Fbounded'] using
      hIdem ⟨hp, hclosest0, hp, hclosest0.1, hclosest0.2⟩
  exact hp_eq.symm

/-- Coq: `IminusId`.

If the two operands have the same real value, the abstract subtraction returns a
float with real value zero. -/
theorem IminusId {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q)) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      _root_.F2R (beta:=beta) p = _root_.F2R (beta:=beta) q →
      _root_.F2R (beta:=beta) (Iminus p q) = 0 := by
  intro p q hp hq hpq
  let z : FloatSpec.Core.Defs.FlocqFloat beta := ⟨0, -bo.dExp⟩
  have hvNum : 0 < bo.vNum := by
    have hnonneg : 0 ≤ |p.Fnum| := abs_nonneg p.Fnum
    exact lt_of_le_of_lt hnonneg hp.1
  have hzBounded : Fbounded (beta:=beta) bo z := by
    simpa [z, Fzero] using
      (FboundedFzero_from_positive_bound_payload (beta:=beta) bo hvNum)
  have hzVal : _root_.F2R (beta:=beta) z = 0 := by
    simp [z, _root_.F2R, FloatSpec.Core.Defs.F2R]
  have hclosest0 :
      Closest (beta:=beta) bo radix (_root_.F2R (beta:=beta) z)
        (Iminus p q) := by
    have hclosest := IminusCorrect (beta:=beta) bo radix Iplus Iminus
      IplusCorrect IminusPlus p q hp hq
    rw [hpq, sub_self] at hclosest
    rw [hzVal]
    exact hclosest
  have hIdem := ClosestIdem_from_expanded_closest_payload (beta:=beta) bo radix z (Iminus p q)
  have hz_eq :
      _root_.F2R (beta:=beta) z =
        _root_.F2R (beta:=beta) (Iminus p q) := by
    simpa [wp, PostCond.noThrow, pure, ClosestIdem_check, Fbounded'] using
      hIdem ⟨hzBounded, hclosest0, hzBounded, hclosest0.1, hclosest0.2⟩
  rw [← hz_eq, hzVal]

/-- Coq: `IplusCorrectEq`.

If `pq` is a bounded exact representation of `p + q`, then the abstract
closest addition `Iplus p q` has the same real value as the supplied result
`r`. -/
theorem IplusCorrectEq {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (Iplus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q)) :
    ∀ (p q pq : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ),
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      Fbounded (beta:=beta) bo pq →
      r = _root_.F2R (beta:=beta) pq →
      _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q =
        _root_.F2R (beta:=beta) pq →
      _root_.F2R (beta:=beta) (Iplus p q) = r := by
  intro p q pq r hp hq hpq hr hsum
  have hclosest :
      Closest (beta:=beta) bo radix (_root_.F2R (beta:=beta) pq)
        (Iplus p q) := by
    have h := IplusCorrect p q hp hq
    rw [hsum] at h
    exact h
  have hIdem := ClosestIdem_from_expanded_closest_payload (beta:=beta) bo radix pq (Iplus p q)
  have hpq_eq :
      _root_.F2R (beta:=beta) pq =
        _root_.F2R (beta:=beta) (Iplus p q) := by
    simpa [wp, PostCond.noThrow, pure, ClosestIdem_check, Fbounded'] using
      hIdem ⟨hpq, hclosest, hpq, hclosest.1, hclosest.2⟩
  rw [hr, hpq_eq]

/-- Coq: `IminusCorrectEq`.

If `pq` is a bounded exact representation of `p - q`, then the abstract
subtraction `Iminus p q` has the same real value as the supplied result `r`. -/
theorem IminusCorrectEq {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q)) :
    ∀ (p q pq : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ),
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      Fbounded (beta:=beta) bo pq →
      r = _root_.F2R (beta:=beta) pq →
      _root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q =
        _root_.F2R (beta:=beta) pq →
      _root_.F2R (beta:=beta) (Iminus p q) = r := by
  intro p q pq r hp hq hpq hr hdiff
  have hclosest :
      Closest (beta:=beta) bo radix (_root_.F2R (beta:=beta) pq)
        (Iminus p q) := by
    have h := IminusCorrect (beta:=beta) bo radix Iplus Iminus
      IplusCorrect IminusPlus p q hp hq
    rw [hdiff] at h
    exact h
  have hIdem := ClosestIdem_from_expanded_closest_payload (beta:=beta) bo radix pq (Iminus p q)
  have hpq_eq :
      _root_.F2R (beta:=beta) pq =
        _root_.F2R (beta:=beta) (Iminus p q) := by
    simpa [wp, PostCond.noThrow, pure, ClosestIdem_check, Fbounded'] using
      hIdem ⟨hpq, hclosest, hpq, hclosest.1, hclosest.2⟩
  rw [hr, hpq_eq]

/-- Coq: `IplusOl`.

The upstream Fast section derives the left-zero case for abstract rounded
addition from commutativity and `IplusOr`. -/
theorem IplusOl {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (Iplus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (IplusSym :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iplus p q = Iplus q p) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      _root_.F2R (beta:=beta) p = 0 →
      _root_.F2R (beta:=beta) (Iplus p q) =
        _root_.F2R (beta:=beta) q := by
  intro p q hp hq hp0
  have hright :
      _root_.F2R (beta:=beta) (Iplus q p) =
        _root_.F2R (beta:=beta) q :=
    IplusOr (beta:=beta) bo radix Iplus IplusCorrect q p hq hp hp0
  simpa [IplusSym p q] using hright

/-- Coq: `IminusOp`.

Subtraction is addition with an opposite operand, so compatibility of
`Iplus` with `Fopp` transfers to `Iminus`. -/
theorem IminusOp {beta : Int} [ValidRadix beta]
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusOp :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fopp (beta:=beta) (Iplus p q) =
          Iplus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q))
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q)) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      Fopp (beta:=beta) (Iminus p q) =
        Iminus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q) := by
  intro p q
  rw [IminusPlus, IminusPlus]
  exact IplusOp p (Fopp (beta:=beta) q)

noncomputable def eqExpLess_from_bounded_left_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (p q : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Bounded representative at no smaller exponent.

This is the useful payload of Coq `eqExpLess`: if a bounded float `p` has the
same real value as `q`, then that real value has a bounded representative whose
exponent is at least `q.Fexp`. -/
theorem eqExpLess_from_bounded_left {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (p q : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜1 < beta ∧ Fbounded (beta:=beta) b p ∧
        _root_.F2R (beta:=beta) p = _root_.F2R (beta:=beta) q⌝⦄
    (pure (eqExpLess_from_bounded_left_check (beta:=beta) b p q) : Id Unit)
    ⦃⇓_ => ⌜∃ r : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) b r ∧
        _root_.F2R (beta:=beta) r = _root_.F2R (beta:=beta) q ∧
        q.Fexp ≤ r.Fexp⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hpBounded, hpq⟩
  simp only [wp, PostCond.noThrow, pure, eqExpLess_from_bounded_left_check,
    Id.run, ULift.up_down]
  by_cases hq_exp_le : q.Fexp ≤ p.Fexp
  · exact ⟨p, hpBounded, hpq, hq_exp_le⟩
  · have hp_exp_le : p.Fexp ≤ q.Fexp := le_of_lt (lt_of_not_ge hq_exp_le)
    have hqBounded : Fbounded (beta:=beta) b q := by
      have h := FboundedEqExp (beta:=beta) b p q
      simpa only [wp, PostCond.noThrow, pure, FboundedEqExp_check, Id.run,
        PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
        ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
        h ⟨hbeta, hpBounded, hpq, hp_exp_le⟩
    exact ⟨q, hqBounded, rfl, le_rfl⟩

noncomputable def plusExpMin_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p q pq : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `plusExpMin`.

The local Lean statement uses the float-specific rounded-mode pieces explicitly:
`MinOrMaxP_float` supplies boundedness of `pq`, while the representation
payload is the same one used by `RoundedModeRep_float_from_minmax`. -/
theorem plusExpMin_from_rep_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix]
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p q pq : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧
        MinOrMaxP_float (beta:=beta) b radix P ∧
        (∀ a r : FloatSpec.Core.Defs.FlocqFloat beta,
          P (_root_.F2R (beta:=beta) a) r →
            ∃ m : Int,
              _root_.F2R (beta:=beta) r =
                _root_.F2R (beta:=beta)
                  (⟨m, a.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat beta)) ∧
        P (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) pq⌝⦄
    (pure (plusExpMin_check (beta:=beta) b radix P p q pq) : Id Unit)
    ⦃⇓_ => ⌜∃ s : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) b s ∧
        _root_.F2R (beta:=beta) s = _root_.F2R (beta:=beta) pq ∧
        min p.Fexp q.Fexp ≤ s.Fexp⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hradix, hMinOrMax, hRep, hP⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure, plusExpMin_check, Id.run,
    ULift.up_down]
  have hpqBounded : Fbounded (beta:=radix) b pq := by
    rcases hMinOrMax
        (_root_.F2R (beta:=radix) p + _root_.F2R (beta:=radix) q)
        pq hP with hMin | hMax
    · exact hMin.1
    · exact hMax.1
  have hplus_val :
      _root_.F2R (beta:=radix) (Fplus (beta:=radix) p q) =
        _root_.F2R (beta:=radix) p + _root_.F2R (beta:=radix) q := by
    have h := Fplus_correct (beta:=radix) p q
    simpa only [wp, PostCond.noThrow, pure, Fplus_correct_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using h hradix
  have hPplus :
      P (_root_.F2R (beta:=radix) (Fplus (beta:=radix) p q)) pq := by
    rw [hplus_val]
    exact hP
  rcases hRep (Fplus (beta:=radix) p q) pq hPplus with ⟨m, hm⟩
  let rep : FloatSpec.Core.Defs.FlocqFloat radix :=
    ⟨m, (Fplus (beta:=radix) p q).Fexp⟩
  have hrep_val :
      _root_.F2R (beta:=radix) pq = _root_.F2R (beta:=radix) rep := by
    simpa [rep] using hm
  have hless := eqExpLess_from_bounded_left (beta:=radix) b pq rep
  rcases (by
      simpa only [wp, PostCond.noThrow, pure,
        eqExpLess_from_bounded_left_check, Id.run, ULift.up_down, Int.cast_ofNat] using
        hless ⟨hradix, hpqBounded, hrep_val⟩) with
    ⟨s, hsBounded, hsValRep, hrepExpLe⟩
  have hplus_exp :
      (Fplus (beta:=radix) p q).Fexp = min p.Fexp q.Fexp := by
    cases p with
    | mk pm pe =>
      cases q with
      | mk qm qe =>
        by_cases hle : pe ≤ qe
        · simp [Fplus, FloatSpec.Calc.Operations.Fplus,
            FloatSpec.Calc.Operations.Falign, hle, min_eq_left hle]
        · have hqe : qe ≤ pe := le_of_lt (lt_of_not_ge hle)
          simp [Fplus, FloatSpec.Calc.Operations.Fplus,
            FloatSpec.Calc.Operations.Falign, hle, min_eq_right hqe]
  refine ⟨s, hsBounded, ?_, ?_⟩
  · rw [hsValRep, ← hrep_val]
  · simpa [rep, hplus_exp] using hrepExpLe

noncomputable def plusExpUpperBound_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p q pq : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `plusExpUpperBound_from_split_payload`.

The local proof exposes the split rounded-mode hypotheses used elsewhere in
this file.  It bounds the exact sum by the canonical maximal float at exponent
`succ (max p.Fexp q.Fexp)`, transfers that bound through rounding, then uses
`eqExpMax` to obtain a same-value bounded representative at no larger
exponent. -/
theorem plusExpUpperBound_from_split_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix]
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p q pq : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < b.vNum ∧
        RoundedModeP_float (beta:=beta) b radix P ∧
        P (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) pq ∧
        Fbounded (beta:=beta) b p ∧ Fbounded (beta:=beta) b q⌝⦄
    (pure (plusExpUpperBound_check (beta:=beta) b radix P p q pq) : Id Unit)
    ⦃⇓_ => ⌜∃ r : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) b r ∧
        _root_.F2R (beta:=beta) r = _root_.F2R (beta:=beta) pq ∧
        r.Fexp ≤ Int.succ (max p.Fexp q.Fexp)⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hradix, hvNum, hRoundedFloat, hP, hp, hq⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure, plusExpUpperBound_check, Id.run,
    ULift.up_down]
  let z : Int := max p.Fexp q.Fexp
  let upper : FloatSpec.Core.Defs.FlocqFloat radix := ⟨b.vNum - 1, Int.succ z⟩
  have hpExpLe : p.Fexp ≤ z := by
    exact le_max_left p.Fexp q.Fexp
  have hqExpLe : q.Fexp ≤ z := by
    exact le_max_right p.Fexp q.Fexp
  have hsucc_z : z ≤ Int.succ z := by
    simp [Int.succ]
  have hupperBounded : Fbounded (beta:=radix) b upper := by
    constructor
    · have hnonneg : 0 ≤ b.vNum - 1 := by omega
      rw [abs_of_nonneg hnonneg]
      omega
    · have hp_low : -b.dExp ≤ z := le_trans hp.2 hpExpLe
      exact le_trans hp_low hsucc_z
  have hpAbsLeBase :
      |_root_.F2R (beta:=radix) p| ≤
        _root_.F2R (beta:=radix)
          (⟨b.vNum - 1, z⟩ : FloatSpec.Core.Defs.FlocqFloat radix) := by
    have h := maxMax1_from_expanded_bound_payload (beta:=radix) b p z
    simpa only [wp, PostCond.noThrow, pure, maxMax1_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
      h ⟨hp, hpExpLe, hp, hradix⟩
  have hqAbsLeBase :
      |_root_.F2R (beta:=radix) q| ≤
        _root_.F2R (beta:=radix)
          (⟨b.vNum - 1, z⟩ : FloatSpec.Core.Defs.FlocqFloat radix) := by
    have h := maxMax1_from_expanded_bound_payload (beta:=radix) b q z
    simpa only [wp, PostCond.noThrow, pure, maxMax1_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
      h ⟨hq, hqExpLe, hq, hradix⟩
  have hbase_nonneg :
      0 ≤ _root_.F2R (beta:=radix)
        (⟨b.vNum - 1, z⟩ : FloatSpec.Core.Defs.FlocqFloat radix) := by
    have hnum : (0 : Int) ≤ b.vNum - 1 := by omega
    have hnumR : (0 : ℝ) ≤ ((b.vNum - 1 : Int) : ℝ) := by exact_mod_cast hnum
    have hradixR_pos : (0 : ℝ) < (radix : ℝ) := by
      exact_mod_cast (by omega : (0 : Int) < radix)
    have hpow_nonneg : 0 ≤ (radix : ℝ) ^ z :=
      le_of_lt (zpow_pos hradixR_pos z)
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
      FloatSpec.Core.Defs.FlocqFloat.Fnum,
      FloatSpec.Core.Defs.FlocqFloat.Fexp]
    exact mul_nonneg hnumR hpow_nonneg
  have hupper_val :
      _root_.F2R (beta:=radix) upper =
        (radix : ℝ) *
          _root_.F2R (beta:=radix)
            (⟨b.vNum - 1, z⟩ : FloatSpec.Core.Defs.FlocqFloat radix) := by
    have hradix_ne : (radix : ℝ) ≠ 0 := by
      exact_mod_cast (ne_of_gt (by omega : (0 : Int) < radix))
    simp only [upper, _root_.F2R, FloatSpec.Core.Defs.F2R,
      FloatSpec.Core.Defs.FlocqFloat.Fnum,
      FloatSpec.Core.Defs.FlocqFloat.Fexp]
    rw [show Int.succ z = z + 1 by simp [Int.succ]]
    rw [zpow_add₀ hradix_ne]
    simp
    ring
  have hsumAbsLeUpper :
      |_root_.F2R (beta:=radix) p + _root_.F2R (beta:=radix) q| ≤
        _root_.F2R (beta:=radix) upper := by
    have htri :
        |_root_.F2R (beta:=radix) p + _root_.F2R (beta:=radix) q| ≤
          |_root_.F2R (beta:=radix) p| +
            |_root_.F2R (beta:=radix) q| := abs_add_le _ _
    have htwo :
        |_root_.F2R (beta:=radix) p| + |_root_.F2R (beta:=radix) q| ≤
          (2 : ℝ) *
            _root_.F2R (beta:=radix)
              (⟨b.vNum - 1, z⟩ : FloatSpec.Core.Defs.FlocqFloat radix) := by
      linarith [hpAbsLeBase, hqAbsLeBase]
    have htwo_le_radix :
        (2 : ℝ) *
            _root_.F2R (beta:=radix)
              (⟨b.vNum - 1, z⟩ : FloatSpec.Core.Defs.FlocqFloat radix) ≤
          (radix : ℝ) *
            _root_.F2R (beta:=radix)
              (⟨b.vNum - 1, z⟩ : FloatSpec.Core.Defs.FlocqFloat radix) := by
      have hradix2 : (2 : ℝ) ≤ (radix : ℝ) := by exact_mod_cast hradix
      exact mul_le_mul_of_nonneg_right hradix2 hbase_nonneg
    calc
      |_root_.F2R (beta:=radix) p + _root_.F2R (beta:=radix) q|
          ≤ |_root_.F2R (beta:=radix) p| +
              |_root_.F2R (beta:=radix) q| := htri
      _ ≤ (2 : ℝ) *
            _root_.F2R (beta:=radix)
              (⟨b.vNum - 1, z⟩ : FloatSpec.Core.Defs.FlocqFloat radix) := htwo
      _ ≤ (radix : ℝ) *
            _root_.F2R (beta:=radix)
              (⟨b.vNum - 1, z⟩ : FloatSpec.Core.Defs.FlocqFloat radix) := htwo_le_radix
      _ = _root_.F2R (beta:=radix) upper := hupper_val.symm
  have hpqAbsLeUpper :
      |_root_.F2R (beta:=radix) pq| ≤ _root_.F2R (beta:=radix) upper := by
    have h := RoundAbsMonotoner_from_rounded_payload (beta:=radix) b radix P
      (_root_.F2R (beta:=radix) p + _root_.F2R (beta:=radix) q) pq upper
    simpa only [wp, PostCond.noThrow, pure, RoundAbsMonotoner_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
      h ⟨hRoundedFloat, hupperBounded, hP, hsumAbsLeUpper⟩
  have hmax := eqExpMax (beta:=radix) b radix pq upper
  simpa only [wp, PostCond.noThrow, pure, eqExpMax_check, Id.run,
    ULift.up_down, upper, z, Int.cast_ofNat] using
    hmax ⟨rfl, hradix, by
      exact RoundedModeP_float_bounded b radix P hRoundedFloat hP,
      hupperBounded, hpqAbsLeUpper⟩

noncomputable def plusExpBound_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p q pq : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `plusExpBound_from_rep_payload`.

This combines `plusExpMin` and `plusExpUpperBound_from_split_payload`.  If the lower-exponent
representative already satisfies the upper exponent bound, use it directly.
Otherwise shift that representative down to the upper bound; boundedness follows
by comparing against the upper-bound representative with the same real value. -/
theorem plusExpBound_from_rep_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix]
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p q pq : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < b.vNum ∧
        RoundedModeP_float (beta:=beta) b radix P ∧
        (∀ a r : FloatSpec.Core.Defs.FlocqFloat beta,
          P (_root_.F2R (beta:=beta) a) r →
            ∃ m : Int,
              _root_.F2R (beta:=beta) r =
                _root_.F2R (beta:=beta)
                  (⟨m, a.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat beta)) ∧
        P (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) pq ∧
        Fbounded (beta:=beta) b p ∧ Fbounded (beta:=beta) b q⌝⦄
    (pure (plusExpBound_check (beta:=beta) b radix P p q pq) : Id Unit)
    ⦃⇓_ => ⌜∃ r : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) b r ∧
        _root_.F2R (beta:=beta) r = _root_.F2R (beta:=beta) pq ∧
        min p.Fexp q.Fexp ≤ r.Fexp ∧
        r.Fexp ≤ Int.succ (max p.Fexp q.Fexp)⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hradix, hvNum, hRoundedFloat, hRep, hP, hp, hq⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure, plusExpBound_check, Id.run,
    ULift.up_down]
  let upperExp : Int := Int.succ (max p.Fexp q.Fexp)
  have hmin_le_upper : min p.Fexp q.Fexp ≤ upperExp := by
    have hmin_le_max : min p.Fexp q.Fexp ≤ max p.Fexp q.Fexp := by
      exact le_trans (min_le_left p.Fexp q.Fexp) (le_max_left p.Fexp q.Fexp)
    have hmax_le_succ : max p.Fexp q.Fexp ≤ upperExp := by
      simp [upperExp, Int.succ]
    exact le_trans hmin_le_max hmax_le_succ
  have hMinTrip := plusExpMin_from_rep_payload (beta:=radix) b radix P p q pq
  rcases (by
      simpa only [wp, PostCond.noThrow, pure, plusExpMin_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using
        hMinTrip ⟨rfl, hradix, hRoundedFloat.2.2.1, hRep, hP⟩) with
    ⟨rLower, hrLowerBounded, hrLowerVal, hrLowerExpMin⟩
  by_cases hLowerUpper : rLower.Fexp ≤ upperExp
  · exact ⟨rLower, hrLowerBounded, hrLowerVal, hrLowerExpMin, hLowerUpper⟩
  · have hUpperTrip := plusExpUpperBound_from_split_payload (beta:=radix) b radix P p q pq
    rcases (by
        simpa only [wp, PostCond.noThrow, pure, plusExpUpperBound_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          hUpperTrip ⟨rfl, hradix, hvNum, hRoundedFloat, hP, hp, hq⟩) with
      ⟨rUpper, hrUpperBounded, hrUpperVal, hrUpperExpLe⟩
    have hUpperLtLower : upperExp < rLower.Fexp := lt_of_not_ge hLowerUpper
    have hrUpperExpLeLower : rUpper.Fexp ≤ rLower.Fexp :=
      le_trans hrUpperExpLe (le_of_lt hUpperLtLower)
    let nUpper : Nat := Int.natAbs (rLower.Fexp - rUpper.Fexp)
    let nTarget : Nat := Int.natAbs (rLower.Fexp - upperExp)
    have hnUpper :
        ((nUpper : Nat) : Int) = rLower.Fexp - rUpper.Fexp := by
      simpa [nUpper] using
        Int.natAbs_of_nonneg (by omega : 0 ≤ rLower.Fexp - rUpper.Fexp)
    have hnTarget :
        ((nTarget : Nat) : Int) = rLower.Fexp - upperExp := by
      simpa [nTarget] using
        Int.natAbs_of_nonneg (by omega : 0 ≤ rLower.Fexp - upperExp)
    have hnTargetLeUpper : nTarget ≤ nUpper := by
      have hleInt : ((nTarget : Nat) : Int) ≤ ((nUpper : Nat) : Int) := by
        rw [hnTarget, hnUpper]
        omega
      exact_mod_cast hleInt
    let shiftedUpper : FloatSpec.Core.Defs.FlocqFloat radix :=
      Fshift (beta:=radix) radix nUpper rLower
    have hshiftedUpperVal :
        _root_.F2R (beta:=radix) shiftedUpper =
          _root_.F2R (beta:=radix) rUpper := by
      calc
        _root_.F2R (beta:=radix) shiftedUpper =
            _root_.F2R (beta:=radix) rLower := by
          simpa [shiftedUpper] using
            Fshift_value (beta:=radix) radix nUpper rLower rfl hradix
        _ = _root_.F2R (beta:=radix) pq := hrLowerVal
        _ = _root_.F2R (beta:=radix) rUpper := hrUpperVal.symm
    have hshiftedUpperExp : shiftedUpper.Fexp = rUpper.Fexp := by
      simp only [shiftedUpper, Fshift, FloatSpec.Core.Defs.FlocqFloat.Fexp]
      rw [hnUpper]
      omega
    have hshiftedUpperBounded :
        Fbounded (beta:=radix) b shiftedUpper := by
      constructor
      · have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
          exact_mod_cast (by omega : (0 : Int) < radix)
        have hpow_pos : 0 < (radix : ℝ) ^ rUpper.Fexp :=
          zpow_pos hradix_pos rUpper.Fexp
        have hnumReal :
            ((shiftedUpper.Fnum : Int) : ℝ) = (rUpper.Fnum : ℝ) := by
          have hval := hshiftedUpperVal
          simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
            hshiftedUpperExp] at hval
          exact (mul_right_inj' (ne_of_gt hpow_pos)).mp (by
            simpa [mul_comm] using hval)
        have hnumEq : shiftedUpper.Fnum = rUpper.Fnum := by
          exact_mod_cast hnumReal
        simpa [hnumEq] using hrUpperBounded.1
      · simpa [hshiftedUpperExp] using hrUpperBounded.2
    let r : FloatSpec.Core.Defs.FlocqFloat radix :=
      Fshift (beta:=radix) radix nTarget rLower
    have hrBounded : Fbounded (beta:=radix) b r := by
      have h := FboundedShiftLess (beta:=radix) b radix rLower nUpper nTarget
      simpa only [wp, PostCond.noThrow, pure, FboundedShiftLess_check,
        PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
        Id.run, ULift.up, ULift.down, ULift.up_down, shiftedUpper, r, Int.cast_ofNat] using
        h ⟨hradix, hnTargetLeUpper, hshiftedUpperBounded⟩
    have hrVal : _root_.F2R (beta:=radix) r = _root_.F2R (beta:=radix) pq := by
      calc
        _root_.F2R (beta:=radix) r =
            _root_.F2R (beta:=radix) rLower := by
          simpa [r] using Fshift_value (beta:=radix) radix nTarget rLower rfl hradix
        _ = _root_.F2R (beta:=radix) pq := hrLowerVal
    have hrExp : r.Fexp = upperExp := by
      simp only [r, Fshift, FloatSpec.Core.Defs.FlocqFloat.Fexp]
      rw [hnTarget]
      omega
    refine ⟨r, hrBounded, hrVal, ?_, ?_⟩
    · simpa [hrExp] using hmin_le_upper
    · simpa [hrExp, upperExp]

/-- Coq `errorBoundedPlusLe`, factored after the `RoundedModeRep` step.

The upstream proof first obtains a representation of `pq` at the exponent of
`Fplus p q`.  This lemma restores the remaining arithmetic and boundedness
payload from that representation, leaving only the representation-producing
rounded-mode step for the closed theorem. -/
theorem errorBoundedPlusLe_from_rep {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (p q pq : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜1 < beta ∧ Fbounded (beta:=beta) bo p ∧ Fbounded (beta:=beta) bo q ∧
        p.Fexp ≤ q.Fexp ∧
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) pq ∧
        (∃ m : Int,
          _root_.F2R (beta:=beta) pq =
            _root_.F2R (beta:=beta)
              ⟨m, (Fplus (beta:=beta) p q).Fexp⟩)⌝⦄
    (pure () : Id Unit)
    ⦃⇓_ => ⌜∃ error : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) error =
        |_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) pq| ∧
      Fbounded (beta:=beta) bo error ∧
      error.Fexp = min p.Fexp q.Fexp⌝⦄ := by
  intro h
  rcases h with ⟨hβ, hp, hq, hpq_exp, hclosest, hrep⟩
  simp only [wp, PostCond.noThrow, pure, Id.run, ULift.up_down]
  rcases hrep with ⟨m, hrep⟩
  let rep : FloatSpec.Core.Defs.FlocqFloat beta :=
    ⟨m, (Fplus (beta:=beta) p q).Fexp⟩
  let d : FloatSpec.Core.Defs.FlocqFloat beta :=
    FloatSpec.Calc.Operations.Fminus (beta:=beta) rep p
  let error : FloatSpec.Core.Defs.FlocqFloat beta :=
    Fabs (beta:=beta) (FloatSpec.Calc.Operations.Fminus (beta:=beta) q d)
  have hplus_exp : (Fplus (beta:=beta) p q).Fexp = min p.Fexp q.Fexp := by
    cases p with
    | mk pm pe =>
      cases q with
      | mk qm qe =>
        by_cases hle : pe ≤ qe
        · simp [Fplus, FloatSpec.Calc.Operations.Fplus,
            FloatSpec.Calc.Operations.Falign, hle, min_eq_left hle]
        · have hge : qe ≤ pe := le_of_lt (lt_of_not_ge hle)
          simp [Fplus, FloatSpec.Calc.Operations.Fplus,
            FloatSpec.Calc.Operations.Falign, hle, min_eq_right hge]
  have hrep_val : _root_.F2R (beta:=beta) rep = _root_.F2R (beta:=beta) pq := by
    simpa [rep] using hrep.symm
  have hd_val :
      _root_.F2R (beta:=beta) d =
        _root_.F2R (beta:=beta) pq - _root_.F2R (beta:=beta) p := by
    have hminus := Fminus_correct (beta:=beta) rep p
    have hminus_val :
        _root_.F2R (beta:=beta) d =
          _root_.F2R (beta:=beta) rep - _root_.F2R (beta:=beta) p := by
      simpa [d, wp, PostCond.noThrow, pure, Fminus_correct_check, Fminus,
        Id.run, ULift.up_down, Int.cast_ofNat] using hminus hβ
    rw [hminus_val, hrep_val]
  have hraw_val :
      _root_.F2R (beta:=beta)
          (FloatSpec.Calc.Operations.Fminus (beta:=beta) q d) =
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) pq := by
    have hminus := Fminus_correct (beta:=beta) q d
    have hminus_val :
        _root_.F2R (beta:=beta)
            (FloatSpec.Calc.Operations.Fminus (beta:=beta) q d) =
          _root_.F2R (beta:=beta) q - _root_.F2R (beta:=beta) d := by
      simpa [wp, PostCond.noThrow, pure, Fminus_correct_check, Fminus,
        Id.run, ULift.up_down, Int.cast_ofNat] using hminus hβ
    rw [hminus_val, hd_val]
    ring
  have herror_val :
      _root_.F2R (beta:=beta) error =
        |_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) pq| := by
    have habs := FloatSpec.Calc.Operations.F2R_abs (beta:=beta)
      (FloatSpec.Calc.Operations.Fminus (beta:=beta) q d)
    have habs_val :
        _root_.F2R (beta:=beta) error =
          |_root_.F2R (beta:=beta)
            (FloatSpec.Calc.Operations.Fminus (beta:=beta) q d)| := by
      simpa [error, Fabs, _root_.F2R, wp, PostCond.noThrow, pure,
        Id.run, ULift.up_down, Int.cast_ofNat] using habs hβ
    rw [habs_val, hraw_val]
  have hd_exp : d.Fexp = min p.Fexp q.Fexp := by
    have hrep_exp : rep.Fexp = min p.Fexp q.Fexp := by
      simp [rep, hplus_exp]
    have hrep_exp_p : rep.Fexp = p.Fexp := by
      rw [hrep_exp, min_eq_left hpq_exp]
    simp [d, FloatSpec.Calc.Operations.Fminus,
      FloatSpec.Calc.Operations.Fplus, FloatSpec.Calc.Operations.Fopp,
      FloatSpec.Calc.Operations.Falign, hrep_exp_p, min_eq_left hpq_exp]
  have hraw_exp :
      (FloatSpec.Calc.Operations.Fminus (beta:=beta) q d).Fexp =
        min p.Fexp q.Fexp := by
    have hdp : d.Fexp = p.Fexp := by
      rw [hd_exp, min_eq_left hpq_exp]
    by_cases hqd : q.Fexp ≤ d.Fexp
    · have hqp : q.Fexp = p.Fexp := by
        exact le_antisymm (by simpa [hdp] using hqd) hpq_exp
      simp [FloatSpec.Calc.Operations.Fminus,
        FloatSpec.Calc.Operations.Fplus, FloatSpec.Calc.Operations.Fopp,
        FloatSpec.Calc.Operations.Falign, hqd, hqp, hdp, min_eq_left hpq_exp]
    · have hqp_not : ¬ q.Fexp ≤ p.Fexp := by
        intro hqp
        exact hqd (by simpa [hdp] using hqp)
      simp [FloatSpec.Calc.Operations.Fminus,
        FloatSpec.Calc.Operations.Fplus, FloatSpec.Calc.Operations.Fopp,
        FloatSpec.Calc.Operations.Falign, hqd, hqp_not, hdp,
        min_eq_left hpq_exp]
  have herror_exp : error.Fexp = min p.Fexp q.Fexp := by
    simp [error, Fabs, FloatSpec.Calc.Operations.Fabs, hraw_exp]
  have hclosest_dist :
      |_root_.F2R (beta:=beta) pq -
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)| ≤
        |_root_.F2R (beta:=beta) q -
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)| :=
    hclosest.2 q hq
  have herror_abs_le_p :
      |_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) pq| ≤
        |_root_.F2R (beta:=beta) p| := by
    have hleft :
        |_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
            _root_.F2R (beta:=beta) pq| =
          |_root_.F2R (beta:=beta) pq -
            (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)| := by
      rw [← abs_neg]
      congr 1
      ring
    have hright :
        |_root_.F2R (beta:=beta) q -
            (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)| =
          |_root_.F2R (beta:=beta) p| := by
      rw [show _root_.F2R (beta:=beta) q -
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) =
            -_root_.F2R (beta:=beta) p by ring, abs_neg]
    rw [hleft]
    exact le_trans hclosest_dist (le_of_eq hright)
  have herror_bounded : Fbounded (beta:=beta) bo error := by
    constructor
    · have hbeta_pos : (0 : ℝ) < (beta : ℝ) := by
        exact_mod_cast (by omega : (0 : Int) < beta)
      have hpow_pos : (0 : ℝ) < (beta : ℝ) ^ (min p.Fexp q.Fexp) :=
        zpow_pos hbeta_pos _
      have herror_abs_real :
          |(error.Fnum : ℝ)| * (beta : ℝ) ^ (min p.Fexp q.Fexp) =
            _root_.F2R (beta:=beta) error := by
        have herror_num_nonneg : 0 ≤ error.Fnum := by
          simp [error, Fabs, FloatSpec.Calc.Operations.Fabs]
        calc
          |(error.Fnum : ℝ)| * (beta : ℝ) ^ (min p.Fexp q.Fexp)
              = (error.Fnum : ℝ) * (beta : ℝ) ^ (min p.Fexp q.Fexp) := by
                rw [abs_of_nonneg (by exact_mod_cast herror_num_nonneg)]
          _ = _root_.F2R (beta:=beta) error := by
                simp [_root_.F2R, FloatSpec.Core.Defs.F2R, herror_exp]
      have hp_abs_real :
          |_root_.F2R (beta:=beta) p| =
            |(p.Fnum : ℝ)| * (beta : ℝ) ^ (min p.Fexp q.Fexp) := by
        have hp_pow : 0 < (beta : ℝ) ^ p.Fexp := zpow_pos hbeta_pos _
        rw [show min p.Fexp q.Fexp = p.Fexp from min_eq_left hpq_exp]
        simp [_root_.F2R, FloatSpec.Core.Defs.F2R, abs_mul,
          abs_of_pos hp_pow, Int.cast_abs]
      have hmant_le : |(error.Fnum : ℝ)| ≤ |(p.Fnum : ℝ)| := by
        have hscaled :
            |(error.Fnum : ℝ)| * (beta : ℝ) ^ (min p.Fexp q.Fexp) ≤
              |(p.Fnum : ℝ)| * (beta : ℝ) ^ (min p.Fexp q.Fexp) := by
          rw [herror_abs_real, ← hp_abs_real, herror_val]
          exact herror_abs_le_p
        exact le_of_mul_le_mul_right hscaled hpow_pos
      have hp_bound_real : |(p.Fnum : ℝ)| < (bo.vNum : ℝ) := by
        exact_mod_cast hp.1
      exact_mod_cast (lt_of_le_of_lt hmant_le hp_bound_real)
    · rw [herror_exp, min_eq_left hpq_exp]
      exact hp.2
  exact ⟨error, herror_val, herror_bounded, herror_exp⟩

/-- Coq Fast-section bridge for `ErrorBoundedIplus`.

Upstream `ErrorBoundedIplus` is a direct specialization of the global
`errorBoundedPlus` theorem to the abstract addition operation `Iplus`.  This
lemma restores that specialization once the global closest-plus error payload is
available, without pretending that the global theorem has already been ported. -/
theorem ErrorBoundedIplus_from_errorBoundedPlus {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (Iplus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (errorBoundedPlus :
      ∀ p q pq : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) pq →
        ∃ error : FloatSpec.Core.Defs.FlocqFloat beta,
          _root_.F2R (beta:=beta) error =
            _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
              _root_.F2R (beta:=beta) pq ∧
          Fbounded (beta:=beta) bo error ∧
          error.Fexp = min p.Fexp q.Fexp) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      ∃ error : FloatSpec.Core.Defs.FlocqFloat beta,
        _root_.F2R (beta:=beta) error =
          _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
            _root_.F2R (beta:=beta) (Iplus p q) ∧
        Fbounded (beta:=beta) bo error := by
  intro p q hp hq
  rcases errorBoundedPlus p q (Iplus p q) hp hq
      (IplusCorrect p q hp hq) with
    ⟨error, herrorVal, herrorBounded, _herrorExp⟩
  exact ⟨error, herrorVal, herrorBounded⟩

/-- Coq: `ErrorBoundedIplus`.

The Fast-section theorem is the abstract `Iplus` specialization of the global
closest-plus error bound. -/
theorem ErrorBoundedIplus_from_global_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (Iplus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (errorBoundedPlus :
      ∀ p q pq : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) pq →
        ∃ error : FloatSpec.Core.Defs.FlocqFloat beta,
          _root_.F2R (beta:=beta) error =
            _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
              _root_.F2R (beta:=beta) pq ∧
          Fbounded (beta:=beta) bo error ∧
          error.Fexp = min p.Fexp q.Fexp) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      ∃ error : FloatSpec.Core.Defs.FlocqFloat beta,
        _root_.F2R (beta:=beta) error =
          _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
            _root_.F2R (beta:=beta) (Iplus p q) ∧
        Fbounded (beta:=beta) bo error :=
  ErrorBoundedIplus_from_errorBoundedPlus (beta:=beta) bo radix Iplus
    IplusCorrect errorBoundedPlus

theorem MDekkerAux1_from_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) :
    Fbounded (beta:=beta) bo p →
    Fbounded (beta:=beta) bo q →
    _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) =
      _root_.F2R (beta:=beta) (Iplus p q) - _root_.F2R (beta:=beta) p →
    (∃ error : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) error =
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) (Iplus p q) ∧
      Fbounded (beta:=beta) bo error) →
    Closest (beta:=beta) bo radix
      (_root_.F2R (beta:=beta) q -
        _root_.F2R (beta:=beta) (Iminus (Iplus p q) p))
      (Iminus q (Iminus (Iplus p q) p)) →
    _root_.F2R (beta:=beta) (Iminus q (Iminus (Iplus p q) p)) =
      _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
        _root_.F2R (beta:=beta) (Iplus p q) := by
  intro _hp _hq hFirstExact hError hSecondClosest
  rcases hError with ⟨error, herrorVal, herrorBounded⟩
  let result := Iminus q (Iminus (Iplus p q) p)
  have htarget :
      _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) =
        _root_.F2R (beta:=beta) error := by
    rw [hFirstExact, herrorVal]
    ring
  have hClosestError :
      Closest (beta:=beta) bo radix (_root_.F2R (beta:=beta) error) result := by
    rw [htarget] at hSecondClosest
    simpa only [result, Int.cast_ofNat] using hSecondClosest
  have hresultBounded : Fbounded (beta:=beta) bo result := hClosestError.1
  have hresultMin :
      ∀ g : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded' (beta:=beta) bo g →
          |_root_.F2R (beta:=beta) result - _root_.F2R (beta:=beta) error| ≤
            |_root_.F2R (beta:=beta) g - _root_.F2R (beta:=beta) error| :=
    hClosestError.2
  have hIdemTrip := ClosestIdem_from_expanded_closest_payload (beta:=beta) bo radix error result
  have herror_eq_result :
      _root_.F2R (beta:=beta) error = _root_.F2R (beta:=beta) result := by
    simpa [wp, PostCond.noThrow, pure, ClosestIdem_check, Fbounded'] using
      hIdemTrip ⟨herrorBounded, hClosestError, herrorBounded,
        hresultBounded, hresultMin⟩
  rw [← herror_eq_result, herrorVal]

/-- Coq: `MDekkerAux1`, specialized through the global closest-plus error
payload.

This removes the manual error-witness and second-closest hypotheses from
`MDekkerAux1_from_payload`; the only algorithmic premise left is the first
subtraction exactness premise, exactly as in the upstream lemma. -/
theorem MDekkerAux1_from_errorBoundedPlus {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q))
    (errorBoundedPlus :
      ∀ p q pq : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) pq →
        ∃ error : FloatSpec.Core.Defs.FlocqFloat beta,
          _root_.F2R (beta:=beta) error =
            _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
              _root_.F2R (beta:=beta) pq ∧
          Fbounded (beta:=beta) bo error ∧
          error.Fexp = min p.Fexp q.Fexp) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) =
        _root_.F2R (beta:=beta) (Iplus p q) -
          _root_.F2R (beta:=beta) p →
      _root_.F2R (beta:=beta) (Iminus q (Iminus (Iplus p q) p)) =
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) (Iplus p q) := by
  intro p q hp hq hFirstExact
  have hError :=
    ErrorBoundedIplus_from_errorBoundedPlus (beta:=beta) bo radix Iplus
      IplusCorrect errorBoundedPlus p q hp hq
  have hplusBounded :
      Fbounded (beta:=beta) bo (Iplus p q) :=
    IplusBounded (beta:=beta) bo radix Iplus IplusCorrect p q hp hq
  have hfirstBounded :
      Fbounded (beta:=beta) bo (Iminus (Iplus p q) p) :=
    IminusBounded (beta:=beta) bo radix Iplus Iminus IplusCorrect
      IminusPlus (Iplus p q) p hplusBounded hp
  have hSecondClosest :
      Closest (beta:=beta) bo radix
        (_root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) (Iminus (Iplus p q) p))
        (Iminus q (Iminus (Iplus p q) p)) :=
    IminusCorrect (beta:=beta) bo radix Iplus Iminus IplusCorrect
      IminusPlus q (Iminus (Iplus p q) p) hq hfirstBounded
  exact MDekkerAux1_from_payload (beta:=beta) bo radix Iplus Iminus p q
    hp hq hFirstExact hError hSecondClosest

/-- Coq: `MDekkerAux1`.

Fast-section reconstruction of the second Dekker subtraction.  The global
closest-plus error bound is explicit in Lean through `errorBoundedPlus`; the
runtime premise is the same first-subtraction exactness used upstream. -/
theorem MDekkerAux1_from_global_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q))
    (errorBoundedPlus :
      ∀ p q pq : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) pq →
        ∃ error : FloatSpec.Core.Defs.FlocqFloat beta,
          _root_.F2R (beta:=beta) error =
            _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
              _root_.F2R (beta:=beta) pq ∧
          Fbounded (beta:=beta) bo error ∧
          error.Fexp = min p.Fexp q.Fexp) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) =
        _root_.F2R (beta:=beta) (Iplus p q) -
          _root_.F2R (beta:=beta) p →
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      _root_.F2R (beta:=beta) (Iminus q (Iminus (Iplus p q) p)) =
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) (Iplus p q) := by
  intro p q hFirstExact hp hq
  exact MDekkerAux1_from_errorBoundedPlus (beta:=beta) bo radix Iplus Iminus
    IplusCorrect IminusPlus errorBoundedPlus p q hp hq hFirstExact

/-- Coq: `MDekkerAux2`.

If the abstract rounded addition is exact, then the first abstract subtraction
is exact as well.  This is one of the upstream Fast-section support lemmas used
on the path to `MDekker`/`Dekker_FTS`. -/
theorem MDekkerAux2 {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q)) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) (Iplus p q) =
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q →
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) =
        _root_.F2R (beta:=beta) (Iplus p q) -
          _root_.F2R (beta:=beta) p := by
  intro p q hExact hp hq
  have hplusBounded :
      Fbounded (beta:=beta) bo (Iplus p q) :=
    IplusBounded (beta:=beta) bo radix Iplus IplusCorrect p q hp hq
  have hclosest :=
    IminusCorrect (beta:=beta) bo radix Iplus Iminus IplusCorrect
      IminusPlus (Iplus p q) p hplusBounded hp
  have htarget :
      _root_.F2R (beta:=beta) (Iplus p q) -
          _root_.F2R (beta:=beta) p =
        _root_.F2R (beta:=beta) q := by
    rw [hExact]
    ring
  have hclosestQ :
      Closest (beta:=beta) bo radix (_root_.F2R (beta:=beta) q)
        (Iminus (Iplus p q) p) := by
    rw [htarget] at hclosest
    exact hclosest
  have hIdem := ClosestIdem_from_expanded_closest_payload (beta:=beta) bo radix q
    (Iminus (Iplus p q) p)
  have hq_eq :
      _root_.F2R (beta:=beta) q =
        _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) := by
    simpa [wp, PostCond.noThrow, pure, ClosestIdem_check, Fbounded'] using
      hIdem ⟨hq, hclosestQ, hq, hclosestQ.1, hclosestQ.2⟩
  calc
    _root_.F2R (beta:=beta) (Iminus (Iplus p q) p)
        = _root_.F2R (beta:=beta) q := hq_eq.symm
    _ = _root_.F2R (beta:=beta) (Iplus p q) -
          _root_.F2R (beta:=beta) p := htarget.symm

/-- Coq: `MDekkerAux3`.

If the exact float addition `Fplus p q` is bounded, the abstract rounded
addition is exact by closest-rounding idempotence, so `MDekkerAux2` applies. -/
theorem MDekkerAux3 {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q)) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      1 < beta →
      Fbounded (beta:=beta) bo (Fplus (beta:=beta) p q) →
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) =
        _root_.F2R (beta:=beta) (Iplus p q) -
          _root_.F2R (beta:=beta) p := by
  intro p q hβ hFplusBounded hp hq
  have hFplusVal :
      _root_.F2R (beta:=beta) (Fplus (beta:=beta) p q) =
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q := by
    have hTrip := Fplus_correct (beta:=beta) p q
    simpa [wp, PostCond.noThrow, pure, Fplus_correct_check, Id.run,
      ULift.up_down] using hTrip hβ
  have hclosestFplus :
      Closest (beta:=beta) bo radix
        (_root_.F2R (beta:=beta) (Fplus (beta:=beta) p q))
        (Iplus p q) := by
    rw [hFplusVal]
    exact IplusCorrect p q hp hq
  have hIdem := ClosestIdem_from_expanded_closest_payload (beta:=beta) bo radix
    (Fplus (beta:=beta) p q) (Iplus p q)
  have hFplus_eq_Iplus :
      _root_.F2R (beta:=beta) (Fplus (beta:=beta) p q) =
        _root_.F2R (beta:=beta) (Iplus p q) := by
    simpa [wp, PostCond.noThrow, pure, ClosestIdem_check, Fbounded'] using
      hIdem ⟨hFplusBounded, hclosestFplus, hFplusBounded,
        hclosestFplus.1, hclosestFplus.2⟩
  have hExact :
      _root_.F2R (beta:=beta) (Iplus p q) =
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q := by
    rw [← hFplus_eq_Iplus, hFplusVal]
  exact MDekkerAux2 (beta:=beta) bo radix Iplus Iminus IplusCorrect
    IminusPlus p q hExact hp hq

/-- Coq: `MDekkerAux4`.

If the exact float subtraction `(Iplus p q) - p` is bounded, the abstract
subtraction is exact by closest-rounding idempotence. -/
theorem MDekkerAux4 {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q)) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      1 < beta →
      Fbounded (beta:=beta) bo
        (FloatSpec.Calc.Operations.Fminus (beta:=beta) (Iplus p q) p) →
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) =
        _root_.F2R (beta:=beta) (Iplus p q) -
          _root_.F2R (beta:=beta) p := by
  intro p q hβ hFminusBounded hp hq
  have hplusBounded :
      Fbounded (beta:=beta) bo (Iplus p q) :=
    IplusBounded (beta:=beta) bo radix Iplus IplusCorrect p q hp hq
  have hclosest :=
    IminusCorrect (beta:=beta) bo radix Iplus Iminus IplusCorrect
      IminusPlus (Iplus p q) p hplusBounded hp
  have hFminusVal :
      _root_.F2R (beta:=beta)
          (FloatSpec.Calc.Operations.Fminus (beta:=beta) (Iplus p q) p) =
        _root_.F2R (beta:=beta) (Iplus p q) -
          _root_.F2R (beta:=beta) p := by
    have hTrip := Fminus_correct (beta:=beta) (Iplus p q) p
    simpa [wp, PostCond.noThrow, pure, Fminus_correct_check, Fminus, Id.run,
      ULift.up_down] using hTrip hβ
  have hclosestFminus :
      Closest (beta:=beta) bo radix
        (_root_.F2R (beta:=beta)
          (FloatSpec.Calc.Operations.Fminus (beta:=beta) (Iplus p q) p))
        (Iminus (Iplus p q) p) := by
    rw [hFminusVal]
    exact hclosest
  have hIdem := ClosestIdem_from_expanded_closest_payload (beta:=beta) bo radix
    (FloatSpec.Calc.Operations.Fminus (beta:=beta) (Iplus p q) p)
    (Iminus (Iplus p q) p)
  have hFminus_eq_Iminus :
      _root_.F2R (beta:=beta)
          (FloatSpec.Calc.Operations.Fminus (beta:=beta) (Iplus p q) p) =
        _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) := by
    simpa [wp, PostCond.noThrow, pure, ClosestIdem_check, Fbounded'] using
      hIdem ⟨hFminusBounded, hclosestFminus, hFminusBounded,
        hclosestFminus.1, hclosestFminus.2⟩
  calc
    _root_.F2R (beta:=beta) (Iminus (Iplus p q) p)
        = _root_.F2R (beta:=beta)
            (FloatSpec.Calc.Operations.Fminus (beta:=beta) (Iplus p q) p) :=
          hFminus_eq_Iminus.symm
    _ = _root_.F2R (beta:=beta) (Iplus p q) -
          _root_.F2R (beta:=beta) p := hFminusVal

/-- Coq `Dekker1_FTS`, factored after the Sterbenz inequalities.

The upstream proof of `Dekker1_FTS` first proves that
`Fminus (Iplus p q) p` is bounded by Sterbenz, using rounded-mode inequalities
to establish the two real bounds below.  This lemma restores the exactness
payload once those Sterbenz bounds are available. -/
theorem Dekker1_FTS_from_sterbenz_bounds {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q)) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      1 < beta →
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      (1 / 2 : ℝ) * _root_.F2R (beta:=beta) p ≤
        _root_.F2R (beta:=beta) (Iplus p q) →
      _root_.F2R (beta:=beta) (Iplus p q) ≤
        2 * _root_.F2R (beta:=beta) p →
      _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) =
        _root_.F2R (beta:=beta) (Iplus p q) -
          _root_.F2R (beta:=beta) p := by
  intro p q hβ hp hq hhalf htwice
  have hplusBounded :
      Fbounded (beta:=beta) bo (Iplus p q) :=
    IplusBounded (beta:=beta) bo radix Iplus IplusCorrect p q hp hq
  have hFminusBounded :
      Fbounded (beta:=beta) bo
        (FloatSpec.Calc.Operations.Fminus (beta:=beta) (Iplus p q) p) := by
    have hTrip := Sterbenz (beta:=beta) bo (Iplus p q) p
    simpa [wp, PostCond.noThrow, pure, Sterbenz_check, Fminus, Id.run,
      ULift.up_down] using
      hTrip ⟨hβ, hplusBounded, hp, hhalf, htwice⟩
  exact MDekkerAux4 (beta:=beta) bo radix Iplus Iminus IplusCorrect
    IminusPlus p q hβ hFminusBounded hp hq

/-- Coq: `Dekker1_FTS`.

This restores the upstream nonnegative `0 <= q <= p` case.  The Coq section
has binary radix fixed globally; here the binary alignment and closest totality
used by `ClosestRoundedModeP_from_total_payload` are explicit section hypotheses. -/
theorem Dekker1_FTS_from_total_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q)) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      1 < beta →
      (beta : ℝ) = radix →
      radix = 2 →
      TotalP (Closest (beta:=beta) bo radix) →
      0 ≤ _root_.F2R (beta:=beta) q →
      _root_.F2R (beta:=beta) q ≤ _root_.F2R (beta:=beta) p →
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) =
        _root_.F2R (beta:=beta) (Iplus p q) -
          _root_.F2R (beta:=beta) p := by
  intro p q hβ hbetaRadix hradix hTotal hq0 hqle hp hq
  have hp0 : 0 ≤ _root_.F2R (beta:=beta) p := le_trans hq0 hqle
  have hclosest :
      Closest (beta:=beta) bo radix
        (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
        (Iplus p q) :=
    IplusCorrect p q hp hq
  have hhalf :
      (1 / 2 : ℝ) * _root_.F2R (beta:=beta) p ≤
        _root_.F2R (beta:=beta) (Iplus p q) := by
    by_cases hqzero : _root_.F2R (beta:=beta) q = 0
    · have hIplus :
          _root_.F2R (beta:=beta) (Iplus p q) =
            _root_.F2R (beta:=beta) p :=
        IplusOr (beta:=beta) bo radix Iplus IplusCorrect p q hp hq hqzero
      rw [hIplus]
      nlinarith
    · have hqpos : 0 < _root_.F2R (beta:=beta) q :=
        lt_of_le_of_ne hq0 (Ne.symm hqzero)
      have hHalfLt :
          (1 / 2 : ℝ) * _root_.F2R (beta:=beta) p <
            _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q := by
        nlinarith
      have hWitness :
          ∃ w : FloatSpec.Core.Defs.FlocqFloat beta,
            Fbounded' bo w ∧
              (1 / 2 : ℝ) * _root_.F2R (beta:=beta) p ≤
                _root_.F2R (beta:=beta) w ∧
              _root_.F2R (beta:=beta) w ≤
                _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q := by
        refine ⟨p, hp, ?_, ?_⟩
        · nlinarith
        · nlinarith
      have hTrip := FmultRadixInv_from_witness (beta:=beta) bo radix p (Iplus p q)
        (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
      simpa [wp, PostCond.noThrow, pure, FmultRadixInv_check, Fbounded'] using
        hTrip ⟨hp, hclosest, hHalfLt, hclosest.1, hclosest.2, hWitness⟩
  have htwice :
      _root_.F2R (beta:=beta) (Iplus p q) ≤
        2 * _root_.F2R (beta:=beta) p := by
    let scaled : FloatSpec.Core.Defs.FlocqFloat beta := ⟨p.Fnum, p.Fexp + 1⟩
    have hRounded :
        RoundedModeP_full (beta:=beta) bo
          (Closest (beta:=beta) bo radix) := by
      have hTrip := ClosestRoundedModeP_from_total_payload (beta:=beta) bo radix
      simpa [wp, PostCond.noThrow, pure, ClosestRoundedModeP_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using hTrip hTotal
    have hScaledBounded : Fbounded (beta:=beta) bo scaled := by
      have hTrip := FBoundedScale (beta:=beta) bo p 1
      simpa [wp, PostCond.noThrow, pure, FBoundedScale_check, scaled,
        Id.run, ULift.up_down, Int.cast_ofNat] using hTrip hp
    have hScaledValue :
        _root_.F2R (beta:=beta) scaled =
          radix * _root_.F2R (beta:=beta) p := by
      have hβne : (beta : ℝ) ≠ 0 := by
        have hβposInt : (0 : Int) < beta := lt_trans (by decide) hβ
        have hβpos : (0 : ℝ) < beta := by exact_mod_cast hβposInt
        exact ne_of_gt hβpos
      have hTrip := FvalScale beta bo p 1
      have hscale :
          _root_.F2R (beta:=beta) scaled =
            ((beta : ℝ) ^ (Int.ofNat 1)) *
              _root_.F2R (beta:=beta) p := by
        simpa [wp, PostCond.noThrow, pure, FvalScale_check, scaled,
          Id.run, ULift.up_down, Int.cast_ofNat] using hTrip hβne
      simpa [hbetaRadix] using hscale
    have hUpperInput :
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q ≤
          _root_.F2R (beta:=beta) scaled := by
      rw [hScaledValue, hradix]
      nlinarith
    have hTrip := RleBoundRoundr_from_full (beta:=beta) bo 0
      (Closest (beta:=beta) bo radix) scaled (Iplus p q)
      (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
    have hRes :
        _root_.F2R (beta:=beta) (Iplus p q) ≤
          _root_.F2R (beta:=beta) scaled := by
      simpa [wp, PostCond.noThrow, pure, RleBoundRoundr_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        hTrip ⟨hRounded, hScaledBounded, hUpperInput, hclosest⟩
    rwa [hScaledValue, hradix] at hRes
  exact Dekker1_FTS_from_sterbenz_bounds (beta:=beta) bo radix Iplus Iminus
    IplusCorrect IminusPlus p q hβ hp hq hhalf htwice

/-- Coq `Dekker2_FTS`, factored after the `Fplus p q` boundedness proof.

Upstream proves this boundedness by rewriting addition as subtraction from
`Fopp q` and applying Sterbenz.  Once that boundedness is available,
`MDekkerAux3` gives the exact first subtraction. -/
theorem Dekker2_FTS_from_Fplus_bounded {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q)) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      1 < beta →
      Fbounded (beta:=beta) bo (Fplus (beta:=beta) p q) →
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) =
        _root_.F2R (beta:=beta) (Iplus p q) -
          _root_.F2R (beta:=beta) p := by
  intro p q hβ hFplusBounded hp hq
  exact MDekkerAux3 (beta:=beta) bo radix Iplus Iminus IplusCorrect
    IminusPlus p q hβ hFplusBounded hp hq

/-- Coq: `Dekker2_FTS`.

This ports the Sterbenz part of the upstream proof in the case
`0 <= p`, `-q <= p`, and `p <= 2 * -q`.  The Coq section fixes
`radix := 2`; the equality is explicit here because the surrounding Lean
section is parameterized over the real rounding radix. -/
theorem Dekker2_FTS {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q)) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      1 < beta →
      radix = 2 →
      0 ≤ _root_.F2R (beta:=beta) p →
      -_root_.F2R (beta:=beta) q ≤ _root_.F2R (beta:=beta) p →
      _root_.F2R (beta:=beta) p ≤ radix * -_root_.F2R (beta:=beta) q →
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) =
        _root_.F2R (beta:=beta) (Iplus p q) -
          _root_.F2R (beta:=beta) p := by
  intro p q hβ hradix hp0 hnegq_le_p hp_le hpb hqb
  have hqOppBounded : Fbounded (beta:=beta) bo (Fopp (beta:=beta) q) := by
    have h := oppBounded (beta:=beta) bo q
    simpa [wp, PostCond.noThrow, pure, oppBounded_check, Id.run,
      ULift.up_down] using h hqb
  have hqOppVal :
      _root_.F2R (beta:=beta) (Fopp (beta:=beta) q) =
        -_root_.F2R (beta:=beta) q := by
    simp [Fopp, FloatSpec.Calc.Operations.Fopp, _root_.F2R,
      FloatSpec.Core.Defs.F2R]
  have hnegq_nonneg : 0 ≤ -_root_.F2R (beta:=beta) q := by
    have htwo_nonneg : 0 ≤ (2 : ℝ) * -_root_.F2R (beta:=beta) q := by
      rw [hradix] at hp_le
      exact le_trans hp0 hp_le
    nlinarith
  have hhalf :
      (1 / 2 : ℝ) * _root_.F2R (beta:=beta) (Fopp (beta:=beta) q) ≤
        _root_.F2R (beta:=beta) p := by
    rw [hqOppVal]
    nlinarith
  have htwice :
      _root_.F2R (beta:=beta) p ≤
        2 * _root_.F2R (beta:=beta) (Fopp (beta:=beta) q) := by
    rw [hqOppVal]
    simpa [hradix] using hp_le
  have hFminusBounded :
      Fbounded (beta:=beta) bo
        (FloatSpec.Calc.Operations.Fminus (beta:=beta) p
          (Fopp (beta:=beta) q)) := by
    have hTrip := Sterbenz (beta:=beta) bo p (Fopp (beta:=beta) q)
    simpa [wp, PostCond.noThrow, pure, Sterbenz_check, Fminus, Id.run,
      ULift.up_down] using
      hTrip ⟨hβ, hpb, hqOppBounded, hhalf, htwice⟩
  have hFplusBounded :
      Fbounded (beta:=beta) bo (Fplus (beta:=beta) p q) := by
    have hsame :
        FloatSpec.Calc.Operations.Fminus (beta:=beta) p
            (Fopp (beta:=beta) q) =
          Fplus (beta:=beta) p q := by
      cases q
      simp [FloatSpec.Calc.Operations.Fminus, Fplus, Fopp,
        FloatSpec.Calc.Operations.Fopp]
    simpa [hsame] using hFminusBounded
  exact Dekker2_FTS_from_Fplus_bounded (beta:=beta) bo radix Iplus Iminus
    IplusCorrect IminusPlus p q hβ hFplusBounded hpb hqb

/-- Coq `Dekker3`, factored after the boundedness proof for
`Fminus (Iplus p q) p`.

As for `Dekker1_FTS`, the upstream proof spends its work deriving this
boundedness from rounded-mode and Sterbenz facts; exactness then follows from
`MDekkerAux4`. -/
theorem Dekker3_FTS_from_Fminus_bounded {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q)) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      1 < beta →
      Fbounded (beta:=beta) bo
        (FloatSpec.Calc.Operations.Fminus (beta:=beta) (Iplus p q) p) →
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) =
        _root_.F2R (beta:=beta) (Iplus p q) -
          _root_.F2R (beta:=beta) p := by
  intro p q hβ hFminusBounded hp hq
  exact MDekkerAux4 (beta:=beta) bo radix Iplus Iminus IplusCorrect
    IminusPlus p q hβ hFminusBounded hp hq

/-- Coq `Dekker3`, factored at the remaining midpoint witness.

The upstream proof obtains the witness for `FmultRadixInv` from
`MaxEx_from_finite_box_payload`/`div2IsBetween`.  This helper restores the rest of the `Dekker3`
payload: once that bounded half-step witness is available, the lower Sterbenz
inequality follows from `FmultRadixInv`, the upper one from rounded-mode
monotonicity, and the exactness step is `MDekkerAux4`. -/
theorem Dekker3_from_half_witness {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q)) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      1 < beta →
      (beta : ℝ) = radix →
      radix = 2 →
      TotalP (Closest (beta:=beta) bo radix) →
      _root_.F2R (beta:=beta) q ≤ 0 →
      radix * -_root_.F2R (beta:=beta) q <
        _root_.F2R (beta:=beta) p →
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      (∃ w : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded' bo w ∧
          (1 / 2 : ℝ) * _root_.F2R (beta:=beta) p ≤
            _root_.F2R (beta:=beta) w ∧
          _root_.F2R (beta:=beta) w ≤
            _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) →
      _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) =
        _root_.F2R (beta:=beta) (Iplus p q) -
          _root_.F2R (beta:=beta) p := by
  intro p q hβ hbetaRadix hradix hTotal hqle0 hmid hp hq hHalfWitness
  have hclosest :
      Closest (beta:=beta) bo radix
        (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
        (Iplus p q) :=
    IplusCorrect p q hp hq
  have hp0 : 0 ≤ _root_.F2R (beta:=beta) p := by
    rw [hradix] at hmid
    nlinarith
  have hhalf :
      (1 / 2 : ℝ) * _root_.F2R (beta:=beta) p ≤
        _root_.F2R (beta:=beta) (Iplus p q) := by
    have hHalfLt :
        (1 / 2 : ℝ) * _root_.F2R (beta:=beta) p <
          _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q := by
      rw [hradix] at hmid
      nlinarith
    have hTrip := FmultRadixInv_from_witness (beta:=beta) bo radix p (Iplus p q)
      (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
    simpa [wp, PostCond.noThrow, pure, FmultRadixInv_check, Fbounded'] using
      hTrip ⟨hp, hclosest, hHalfLt, hclosest.1, hclosest.2, hHalfWitness⟩
  have htwice :
      _root_.F2R (beta:=beta) (Iplus p q) ≤
        2 * _root_.F2R (beta:=beta) p := by
    let scaled : FloatSpec.Core.Defs.FlocqFloat beta := ⟨p.Fnum, p.Fexp + 1⟩
    have hRounded :
        RoundedModeP_full (beta:=beta) bo
          (Closest (beta:=beta) bo radix) := by
      have hTrip := ClosestRoundedModeP_from_total_payload (beta:=beta) bo radix
      simpa [wp, PostCond.noThrow, pure, ClosestRoundedModeP_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using hTrip hTotal
    have hScaledBounded : Fbounded (beta:=beta) bo scaled := by
      have hTrip := FBoundedScale (beta:=beta) bo p 1
      simpa [wp, PostCond.noThrow, pure, FBoundedScale_check, scaled,
        Id.run, ULift.up_down, Int.cast_ofNat] using hTrip hp
    have hScaledValue :
        _root_.F2R (beta:=beta) scaled =
          radix * _root_.F2R (beta:=beta) p := by
      have hβne : (beta : ℝ) ≠ 0 := by
        have hβposInt : (0 : Int) < beta := lt_trans (by decide) hβ
        have hβpos : (0 : ℝ) < beta := by exact_mod_cast hβposInt
        exact ne_of_gt hβpos
      have hTrip := FvalScale beta bo p 1
      have hscale :
          _root_.F2R (beta:=beta) scaled =
            ((beta : ℝ) ^ (Int.ofNat 1)) *
              _root_.F2R (beta:=beta) p := by
        simpa [wp, PostCond.noThrow, pure, FvalScale_check, scaled,
          Id.run, ULift.up_down, Int.cast_ofNat] using hTrip hβne
      simpa [hbetaRadix] using hscale
    have hUpperInput :
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q ≤
          _root_.F2R (beta:=beta) scaled := by
      rw [hScaledValue, hradix]
      nlinarith
    have hTrip := RleBoundRoundr_from_full (beta:=beta) bo 0
      (Closest (beta:=beta) bo radix) scaled (Iplus p q)
      (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
    have hRes :
        _root_.F2R (beta:=beta) (Iplus p q) ≤
          _root_.F2R (beta:=beta) scaled := by
      simpa [wp, PostCond.noThrow, pure, RleBoundRoundr_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        hTrip ⟨hRounded, hScaledBounded, hUpperInput, hclosest⟩
    rwa [hScaledValue, hradix] at hRes
  have hplusBounded :
      Fbounded (beta:=beta) bo (Iplus p q) :=
    IplusBounded (beta:=beta) bo radix Iplus IplusCorrect p q hp hq
  have hFminusBounded :
      Fbounded (beta:=beta) bo
        (FloatSpec.Calc.Operations.Fminus (beta:=beta) (Iplus p q) p) := by
    have hTrip := Sterbenz (beta:=beta) bo (Iplus p q) p
    simpa [wp, PostCond.noThrow, pure, Sterbenz_check, Fminus, Id.run,
      ULift.up_down] using
      hTrip ⟨hβ, hplusBounded, hp, hhalf, htwice⟩
  exact Dekker3_FTS_from_Fminus_bounded (beta:=beta) bo radix Iplus Iminus
    IplusCorrect IminusPlus p q hβ hFminusBounded hp hq

/-- Coq `Dekker3`, factored after midpoint extrema.

This removes the arbitrary midpoint witness from `Dekker3_from_half_witness`.
The remaining upstream work for the same-name theorem is to construct these
`min`/`max` extrema and the `div2IsBetween` equality from the section
assumptions. -/
theorem Dekker3_from_extrema {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q)) :
    ∀ p q min max : FloatSpec.Core.Defs.FlocqFloat beta,
      1 < beta →
      (beta : ℝ) = radix →
      radix = 2 →
      TotalP (Closest (beta:=beta) bo radix) →
      _root_.F2R (beta:=beta) q ≤ 0 →
      radix * -_root_.F2R (beta:=beta) q <
        _root_.F2R (beta:=beta) p →
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      isMin (beta:=beta) bo 0
        ((1 / 2 : ℝ) * _root_.F2R (beta:=beta) p) min →
      isMax (beta:=beta) bo 0
        ((1 / 2 : ℝ) * _root_.F2R (beta:=beta) p) max →
      _root_.F2R (beta:=beta) p =
        _root_.F2R (beta:=beta) min + _root_.F2R (beta:=beta) max →
      _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) =
        _root_.F2R (beta:=beta) (Iplus p q) -
          _root_.F2R (beta:=beta) p := by
  intro p q min max hβ hbetaRadix hradix hTotal hqle0 hmid hp hq
    hMin hMax hBetween
  have hclosest :
      Closest (beta:=beta) bo radix
        (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
        (Iplus p q) :=
    IplusCorrect p q hp hq
  have hhalf :
      (1 / 2 : ℝ) * _root_.F2R (beta:=beta) p ≤
        _root_.F2R (beta:=beta) (Iplus p q) := by
    have hHalfLt :
        (1 / 2 : ℝ) * _root_.F2R (beta:=beta) p <
          _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q := by
      rw [hradix] at hmid
      nlinarith
    have hTrip := FmultRadixInv_from_extrema (beta:=beta) bo radix
      p (Iplus p q) min max
      (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
    simpa [wp, PostCond.noThrow, pure, FmultRadixInv_check, Fbounded'] using
      hTrip ⟨hp, hclosest, hHalfLt, hMin, hMax, hBetween⟩
  have htwice :
      _root_.F2R (beta:=beta) (Iplus p q) ≤
        2 * _root_.F2R (beta:=beta) p := by
    let scaled : FloatSpec.Core.Defs.FlocqFloat beta := ⟨p.Fnum, p.Fexp + 1⟩
    have hRounded :
        RoundedModeP_full (beta:=beta) bo
          (Closest (beta:=beta) bo radix) := by
      have hTrip := ClosestRoundedModeP_from_total_payload (beta:=beta) bo radix
      simpa [wp, PostCond.noThrow, pure, ClosestRoundedModeP_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using hTrip hTotal
    have hScaledBounded : Fbounded (beta:=beta) bo scaled := by
      have hTrip := FBoundedScale (beta:=beta) bo p 1
      simpa [wp, PostCond.noThrow, pure, FBoundedScale_check, scaled,
        Id.run, ULift.up_down, Int.cast_ofNat] using hTrip hp
    have hScaledValue :
        _root_.F2R (beta:=beta) scaled =
          radix * _root_.F2R (beta:=beta) p := by
      have hβne : (beta : ℝ) ≠ 0 := by
        have hβposInt : (0 : Int) < beta := lt_trans (by decide) hβ
        have hβpos : (0 : ℝ) < beta := by exact_mod_cast hβposInt
        exact ne_of_gt hβpos
      have hTrip := FvalScale beta bo p 1
      have hscale :
          _root_.F2R (beta:=beta) scaled =
            ((beta : ℝ) ^ (Int.ofNat 1)) *
              _root_.F2R (beta:=beta) p := by
        simpa [wp, PostCond.noThrow, pure, FvalScale_check, scaled,
          Id.run, ULift.up_down, Int.cast_ofNat] using hTrip hβne
      simpa [hbetaRadix] using hscale
    have hUpperInput :
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q ≤
          _root_.F2R (beta:=beta) scaled := by
      rw [hScaledValue, hradix]
      nlinarith
    have hTrip := RleBoundRoundr_from_full (beta:=beta) bo 0
      (Closest (beta:=beta) bo radix) scaled (Iplus p q)
      (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
    have hRes :
        _root_.F2R (beta:=beta) (Iplus p q) ≤
          _root_.F2R (beta:=beta) scaled := by
      simpa [wp, PostCond.noThrow, pure, RleBoundRoundr_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        hTrip ⟨hRounded, hScaledBounded, hUpperInput, hclosest⟩
    rwa [hScaledValue, hradix] at hRes
  exact Dekker3_FTS_from_Fminus_bounded (beta:=beta) bo radix Iplus Iminus
    IplusCorrect IminusPlus p q hβ
    (by
      have hplusBounded :
          Fbounded (beta:=beta) bo (Iplus p q) :=
        IplusBounded (beta:=beta) bo radix Iplus IplusCorrect p q hp hq
      have hTrip := Sterbenz (beta:=beta) bo (Iplus p q) p
      simpa [wp, PostCond.noThrow, pure, Sterbenz_check, Fminus, Id.run,
        ULift.up_down] using
        hTrip ⟨hβ, hplusBounded, hp, hhalf, htwice⟩)
    hp hq

/-- Coq: `Dekker3`.

This restores the third Sterbenz case under the explicit Pff section
assumptions needed in Lean to construct the midpoint extrema used by
`FmultRadixInv`: precision is nonzero, `vNum = beta^precision`, and the
`boundR` exponent box contains the midpoint `p/2`. -/
theorem Dekker3_from_midpoint_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ) (precision : Nat)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q)) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      1 < beta →
      (beta : ℝ) = radix →
      radix = 2 →
      precision ≠ 0 →
      bo.vNum = Zpower_nat beta precision →
      1 < bo.vNum →
      -bo.dExp ≤
        (boundR (beta:=beta) beta
          ((1 / 2 : ℝ) * _root_.F2R (beta:=beta) p)).Fexp →
      (∀ min : FloatSpec.Core.Defs.FlocqFloat beta,
        isMin (beta:=beta) bo beta
          ((1 / 2 : ℝ) * _root_.F2R (beta:=beta) p) min →
        ∃ d₂ : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded (beta:=beta) bo d₂ ∧
            _root_.F2R (beta:=beta) d₂ =
              _root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) min) →
      TotalP (Closest (beta:=beta) bo radix) →
      _root_.F2R (beta:=beta) q ≤ 0 →
      radix * -_root_.F2R (beta:=beta) q <
        _root_.F2R (beta:=beta) p →
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) =
        _root_.F2R (beta:=beta) (Iplus p q) -
          _root_.F2R (beta:=beta) p := by
  intro p q hβ hbetaRadix hradix hprecision hvNum hvNum_gt hboundExp
    hPminPayload hTotal hqle0 hmid hp hq
  let half : ℝ := (1 / 2 : ℝ) * _root_.F2R (beta:=beta) p
  have hp0 : 0 ≤ _root_.F2R (beta:=beta) p := by
    rw [hradix] at hmid
    nlinarith
  have hMinExists := MinEx_from_finite_box_payload (beta:=beta) bo beta half
  have hMinPayload :
      ∃ min : FloatSpec.Core.Defs.FlocqFloat beta,
        isMin (beta:=beta) bo beta half min := by
    simpa [wp, PostCond.noThrow, pure, MinEx_check, half] using
      hMinExists ⟨rfl, hβ, hvNum_gt, hboundExp⟩
  rcases hMinPayload with ⟨min, hMin⟩
  have hMaxExists := MaxEx_from_finite_box_payload (beta:=beta) bo beta half
  have hMaxPayload :
      ∃ max : FloatSpec.Core.Defs.FlocqFloat beta,
        isMax (beta:=beta) bo beta half max := by
    simpa [wp, PostCond.noThrow, pure, MaxEx_check, half] using
      hMaxExists ⟨rfl, hβ, hvNum_gt, hboundExp⟩
  rcases hMaxPayload with ⟨max, hMax⟩
  rcases hMin with ⟨hminBounded, hmin_le_half, hminGreatest⟩
  rcases hMax with ⟨hmaxBounded, hhalf_le_max, hmaxLeast⟩
  have hMin' : isMin (beta:=beta) bo beta half min :=
    ⟨hminBounded, hmin_le_half, hminGreatest⟩
  have hMax' : isMax (beta:=beta) bo beta half max :=
    ⟨hmaxBounded, hhalf_le_max, hmaxLeast⟩
  have hD₂ :
      ∃ d₂ : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo d₂ ∧
          _root_.F2R (beta:=beta) d₂ =
            _root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) min := by
    simpa [half] using hPminPayload min hMin'
  have hhalf_nonneg : 0 ≤ half := by
    change 0 ≤ (1 / 2 : ℝ) * _root_.F2R (beta:=beta) p
    exact mul_nonneg (by norm_num) hp0
  have hhalf_le_p : half ≤ _root_.F2R (beta:=beta) p := by
    change (1 / 2 : ℝ) * _root_.F2R (beta:=beta) p ≤
      _root_.F2R (beta:=beta) p
    calc
      (1 / 2 : ℝ) * _root_.F2R (beta:=beta) p ≤
          1 * _root_.F2R (beta:=beta) p :=
        mul_le_mul_of_nonneg_right (by norm_num) hp0
      _ = _root_.F2R (beta:=beta) p := one_mul _
  have hmax_le_p : _root_.F2R (beta:=beta) max ≤ _root_.F2R (beta:=beta) p :=
    hmaxLeast p hp hhalf_le_p
  have hD₁ :
      ∃ d₁ : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo d₁ ∧
          _root_.F2R (beta:=beta) d₁ =
            _root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) max := by
    refine ⟨FloatSpec.Calc.Operations.Fminus (beta:=beta) p max, ?_, ?_⟩
    · have hhalf_max_le_p :
          (1 / 2 : ℝ) * _root_.F2R (beta:=beta) max ≤
            _root_.F2R (beta:=beta) p := by
        have hmax_nonneg : 0 ≤ _root_.F2R (beta:=beta) max :=
          le_trans hhalf_nonneg hhalf_le_max
        calc
          (1 / 2 : ℝ) * _root_.F2R (beta:=beta) max ≤
              1 * _root_.F2R (beta:=beta) max :=
            mul_le_mul_of_nonneg_right (by norm_num) hmax_nonneg
          _ = _root_.F2R (beta:=beta) max := one_mul _
          _ ≤ _root_.F2R (beta:=beta) p := hmax_le_p
      have hp_le_twomax :
          _root_.F2R (beta:=beta) p ≤
            2 * _root_.F2R (beta:=beta) max := by
        change (1 / 2 : ℝ) * _root_.F2R (beta:=beta) p ≤
          _root_.F2R (beta:=beta) max at hhalf_le_max
        calc
          _root_.F2R (beta:=beta) p =
              2 * ((1 / 2 : ℝ) * _root_.F2R (beta:=beta) p) := by ring
          _ ≤ 2 * _root_.F2R (beta:=beta) max :=
            mul_le_mul_of_nonneg_left hhalf_le_max (by norm_num)
      have hTrip := Sterbenz (beta:=beta) bo p max
      simpa [wp, PostCond.noThrow, pure, Sterbenz_check, Fminus, Id.run,
        ULift.up_down] using
        hTrip ⟨hβ, hp, hmaxBounded, hhalf_max_le_p, hp_le_twomax⟩
    · have hTrip := Fminus_correct (beta:=beta) p max
      simpa [wp, PostCond.noThrow, pure, Fminus_correct_check, Fminus, Id.run,
        ULift.up_down] using hTrip hβ
  have hBetween :
      _root_.F2R (beta:=beta) p =
        _root_.F2R (beta:=beta) min + _root_.F2R (beta:=beta) max := by
    have hTrip := div2IsBetween_from_extrema (beta:=beta) bo beta p min max
    simpa [wp, PostCond.noThrow, pure, div2IsBetween_check, half, Fbounded'] using
      hTrip ⟨hp, hMin', hMax', hD₁, hD₂⟩
  exact Dekker3_from_extrema (beta:=beta) bo radix Iplus Iminus
    IplusCorrect IminusPlus p q min max hβ hbetaRadix hradix hTotal hqle0
    hmid hp hq
    (by simpa [half, isMin] using hMin')
    (by simpa [half, isMax] using hMax')
    hBetween

/-- Coq: `MDekkerAux5`.

Exactness for the opposite operands transfers back to the original operands
through the `IplusOp` compatibility of abstract addition with float negation. -/
theorem MDekkerAux5 {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusOp :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fopp (beta:=beta) (Iplus p q) =
          Iplus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q))
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q)) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      _root_.F2R (beta:=beta)
          (Iminus (Iplus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q))
            (Fopp (beta:=beta) p)) =
        _root_.F2R (beta:=beta)
            (Iplus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q)) -
          _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) →
      _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) =
        _root_.F2R (beta:=beta) (Iplus p q) -
          _root_.F2R (beta:=beta) p := by
  intro p q _hp _hq hOppExact
  let r := Iminus (Iplus p q) p
  let ropp :=
    Iminus (Iplus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q))
      (Fopp (beta:=beta) p)
  have hOppFloat :
      Fopp (beta:=beta) r = ropp := by
    simp only [r, ropp]
    rw [IminusPlus, IminusPlus]
    rw [IplusOp]
    rw [IplusOp p q]
  have hFoppRVal :
      _root_.F2R (beta:=beta) (Fopp (beta:=beta) r) =
        -_root_.F2R (beta:=beta) r := by
    simp [Fopp, FloatSpec.Calc.Operations.Fopp, _root_.F2R,
      FloatSpec.Core.Defs.F2R]
  have hroppVal :
      _root_.F2R (beta:=beta) ropp =
        -_root_.F2R (beta:=beta) r := by
    simpa [hOppFloat] using hFoppRVal
  have hIplusOppVal :
      _root_.F2R (beta:=beta)
          (Iplus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q)) =
        -_root_.F2R (beta:=beta) (Iplus p q) := by
    have hFoppIplusVal :
        _root_.F2R (beta:=beta) (Fopp (beta:=beta) (Iplus p q)) =
          -_root_.F2R (beta:=beta) (Iplus p q) := by
      simp [Fopp, FloatSpec.Calc.Operations.Fopp, _root_.F2R,
        FloatSpec.Core.Defs.F2R]
    simpa [IplusOp p q] using hFoppIplusVal
  have hFoppPVal :
      _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) =
        -_root_.F2R (beta:=beta) p := by
    simp [Fopp, FloatSpec.Calc.Operations.Fopp, _root_.F2R,
      FloatSpec.Core.Defs.F2R]
  have hneg :
      -_root_.F2R (beta:=beta) r =
        _root_.F2R (beta:=beta)
            (Iplus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q)) -
          _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) := by
    calc
      -_root_.F2R (beta:=beta) r
          = _root_.F2R (beta:=beta) ropp := hroppVal.symm
      _ = _root_.F2R (beta:=beta)
            (Iplus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q)) -
          _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) := by
            simpa [ropp] using hOppExact
  have hnegTarget :
      -_root_.F2R (beta:=beta) r =
        -(_root_.F2R (beta:=beta) (Iplus p q) -
          _root_.F2R (beta:=beta) p) := by
    rw [hneg, hIplusOppVal, hFoppPVal]
    ring
  have hpos := congrArg (fun x : ℝ => -x) hnegTarget
  simpa [r] using hpos

/-- Coq: `MDekker`, separated from the three Sterbenz-driven case payloads.

The upstream theorem is mostly a real-arithmetic case split over signs and the
relative magnitude of `p` and `q`, delegating exactness to `Dekker1_FTS`,
`Dekker2_FTS`, `Dekker3`, and using `MDekkerAux5` for the negative-`p`
symmetry branch. -/
theorem MDekker_from_case_payloads {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusOp :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fopp (beta:=beta) (Iplus p q) =
          Iplus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q))
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q))
    (Dekker1_FTS :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        0 ≤ _root_.F2R (beta:=beta) q →
        _root_.F2R (beta:=beta) q ≤ _root_.F2R (beta:=beta) p →
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) =
          _root_.F2R (beta:=beta) (Iplus p q) -
            _root_.F2R (beta:=beta) p)
    (Dekker2_FTS :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        0 ≤ _root_.F2R (beta:=beta) p →
        -_root_.F2R (beta:=beta) q ≤ _root_.F2R (beta:=beta) p →
        _root_.F2R (beta:=beta) p ≤ radix * -_root_.F2R (beta:=beta) q →
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) =
          _root_.F2R (beta:=beta) (Iplus p q) -
            _root_.F2R (beta:=beta) p)
    (Dekker3 :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        _root_.F2R (beta:=beta) q ≤ 0 →
        radix * -_root_.F2R (beta:=beta) q < _root_.F2R (beta:=beta) p →
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) =
          _root_.F2R (beta:=beta) (Iplus p q) -
            _root_.F2R (beta:=beta) p) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      |_root_.F2R (beta:=beta) q| ≤ |_root_.F2R (beta:=beta) p| →
      _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) =
        _root_.F2R (beta:=beta) (Iplus p q) -
          _root_.F2R (beta:=beta) p := by
  have hNonnegCase :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        0 ≤ _root_.F2R (beta:=beta) p →
        |_root_.F2R (beta:=beta) q| ≤ |_root_.F2R (beta:=beta) p| →
        _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) =
          _root_.F2R (beta:=beta) (Iplus p q) -
            _root_.F2R (beta:=beta) p := by
    intro p q hp hq hp0 hAbs
    have hpAbs : |_root_.F2R (beta:=beta) p| =
        _root_.F2R (beta:=beta) p := abs_of_nonneg hp0
    have hAbsToP :
        |_root_.F2R (beta:=beta) q| ≤ _root_.F2R (beta:=beta) p := by
      calc
        |_root_.F2R (beta:=beta) q| ≤ |_root_.F2R (beta:=beta) p| := hAbs
        _ = _root_.F2R (beta:=beta) p := hpAbs
    by_cases hq0 : 0 ≤ _root_.F2R (beta:=beta) q
    · have hqAbs : |_root_.F2R (beta:=beta) q| =
          _root_.F2R (beta:=beta) q := abs_of_nonneg hq0
      have hqle : _root_.F2R (beta:=beta) q ≤
          _root_.F2R (beta:=beta) p := by
        calc
          _root_.F2R (beta:=beta) q = |_root_.F2R (beta:=beta) q| := hqAbs.symm
          _ ≤ _root_.F2R (beta:=beta) p := hAbsToP
      exact Dekker1_FTS p q hq0 hqle hp hq
    · have hqle0 : _root_.F2R (beta:=beta) q ≤ 0 :=
        le_of_lt (lt_of_not_ge hq0)
      have hqAbs : |_root_.F2R (beta:=beta) q| =
          -_root_.F2R (beta:=beta) q := abs_of_nonpos hqle0
      have hnegq_le :
          -_root_.F2R (beta:=beta) q ≤ _root_.F2R (beta:=beta) p := by
        calc
          -_root_.F2R (beta:=beta) q = |_root_.F2R (beta:=beta) q| := hqAbs.symm
          _ ≤ _root_.F2R (beta:=beta) p := hAbsToP
      by_cases hmid :
          _root_.F2R (beta:=beta) p ≤
            radix * -_root_.F2R (beta:=beta) q
      · exact Dekker2_FTS p q hp0 hnegq_le hmid hp hq
      · have hmid_lt :
            radix * -_root_.F2R (beta:=beta) q <
              _root_.F2R (beta:=beta) p := lt_of_not_ge hmid
        exact Dekker3 p q hqle0 hmid_lt hp hq
  intro p q hp hq hAbs
  by_cases hp0 : 0 ≤ _root_.F2R (beta:=beta) p
  · exact hNonnegCase p q hp hq hp0 hAbs
  · have hp_le0 : _root_.F2R (beta:=beta) p ≤ 0 :=
      le_of_lt (lt_of_not_ge hp0)
    have hpOppBounded : Fbounded (beta:=beta) bo (Fopp (beta:=beta) p) := by
      have h := oppBounded (beta:=beta) bo p
      simpa [wp, PostCond.noThrow, pure, oppBounded_check, Id.run,
        ULift.up_down] using h hp
    have hqOppBounded : Fbounded (beta:=beta) bo (Fopp (beta:=beta) q) := by
      have h := oppBounded (beta:=beta) bo q
      simpa [wp, PostCond.noThrow, pure, oppBounded_check, Id.run,
        ULift.up_down] using h hq
    have hpOpp0 :
        0 ≤ _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) := by
      have hval :
          _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) =
            -_root_.F2R (beta:=beta) p := by
        simp [Fopp, FloatSpec.Calc.Operations.Fopp, _root_.F2R,
          FloatSpec.Core.Defs.F2R]
      rw [hval]
      exact neg_nonneg.mpr hp_le0
    have hAbsOpp :
        |_root_.F2R (beta:=beta) (Fopp (beta:=beta) q)| ≤
          |_root_.F2R (beta:=beta) (Fopp (beta:=beta) p)| := by
      simpa [Fopp, FloatSpec.Calc.Operations.Fopp, _root_.F2R,
        FloatSpec.Core.Defs.F2R, abs_neg] using hAbs
    have hOppExact :=
      hNonnegCase (Fopp (beta:=beta) p) (Fopp (beta:=beta) q)
        hpOppBounded hqOppBounded hpOpp0 hAbsOpp
    exact MDekkerAux5 (beta:=beta) bo Iplus Iminus IplusOp IminusPlus
      p q hp hq hOppExact

/-- Coq: `MDekker`.

This is the Fast-section case split from upstream Flocq, packaged through the
already restored Lean case lemmas.  The section hypotheses that Lean currently
needs to construct the `Dekker3` midpoint extrema remain explicit here; they
are infrastructure obligations, not proof payloads for the final equality. -/
theorem MDekker_from_midpoint_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ) (precision : Nat)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (IplusOp :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fopp (beta:=beta) (Iplus p q) =
          Iplus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q))
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q))
    (hβ : 1 < beta)
    (hbetaRadix : (beta : ℝ) = radix)
    (hradix : radix = 2)
    (hprecision : precision ≠ 0)
    (hvNum : bo.vNum = Zpower_nat beta precision)
    (hvNum_gt : 1 < bo.vNum)
    (hboundExp :
      ∀ p : FloatSpec.Core.Defs.FlocqFloat beta,
        -bo.dExp ≤
          (boundR (beta:=beta) beta
            ((1 / 2 : ℝ) * _root_.F2R (beta:=beta) p)).Fexp)
    (hPminPayload :
      ∀ p min : FloatSpec.Core.Defs.FlocqFloat beta,
        isMin (beta:=beta) bo beta
          ((1 / 2 : ℝ) * _root_.F2R (beta:=beta) p) min →
        ∃ d₂ : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded (beta:=beta) bo d₂ ∧
            _root_.F2R (beta:=beta) d₂ =
              _root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) min)
    (hTotal : TotalP (Closest (beta:=beta) bo radix)) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      |_root_.F2R (beta:=beta) q| ≤ |_root_.F2R (beta:=beta) p| →
      _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) =
        _root_.F2R (beta:=beta) (Iplus p q) -
          _root_.F2R (beta:=beta) p := by
  refine MDekker_from_case_payloads (beta:=beta) bo radix Iplus Iminus
    IplusOp IminusPlus ?_ ?_ ?_
  · intro p q hq0 hqle hp hq
    exact Dekker1_FTS_from_total_payload (beta:=beta)
      bo radix Iplus Iminus IplusCorrect
      IminusPlus p q hβ hbetaRadix hradix hTotal hq0 hqle hp hq
  · intro p q hp0 hnegq_le_p hp_le hpb hqb
    exact Dekker2_FTS (beta:=beta) bo radix Iplus Iminus IplusCorrect
      IminusPlus p q hβ hradix hp0 hnegq_le_p hp_le hpb hqb
  · intro p q hqle0 hmid hp hq
    exact Dekker3_from_midpoint_payload (beta:=beta)
      bo radix precision Iplus Iminus IplusCorrect
      IminusPlus p q hβ hbetaRadix hradix hprecision hvNum hvNum_gt
      (hboundExp p) (hPminPayload p) hTotal hqle0 hmid hp hq

/-- Coq: `Dekker_FTS`.

This packages the restored first-subtraction exactness theorem with the
closest-plus error representation needed by `MDekkerAux1`.  The remaining
global `errorBoundedPlus` theorem is still an explicit premise; once that
upstream theorem is ported, this statement can be closed to the original Fast
section context. -/
theorem Dekker_FTS_from_global_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ) (precision : Nat)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (IplusOp :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fopp (beta:=beta) (Iplus p q) =
          Iplus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q))
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q))
    (errorBoundedPlus :
      ∀ p q pq : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) pq →
        ∃ error : FloatSpec.Core.Defs.FlocqFloat beta,
          _root_.F2R (beta:=beta) error =
            _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
              _root_.F2R (beta:=beta) pq ∧
          Fbounded (beta:=beta) bo error ∧
          error.Fexp = min p.Fexp q.Fexp)
    (hβ : 1 < beta)
    (hbetaRadix : (beta : ℝ) = radix)
    (hradix : radix = 2)
    (hprecision : precision ≠ 0)
    (hvNum : bo.vNum = Zpower_nat beta precision)
    (hvNum_gt : 1 < bo.vNum)
    (hboundExp :
      ∀ p : FloatSpec.Core.Defs.FlocqFloat beta,
        -bo.dExp ≤
          (boundR (beta:=beta) beta
            ((1 / 2 : ℝ) * _root_.F2R (beta:=beta) p)).Fexp)
    (hPminPayload :
      ∀ p min : FloatSpec.Core.Defs.FlocqFloat beta,
        isMin (beta:=beta) bo beta
          ((1 / 2 : ℝ) * _root_.F2R (beta:=beta) p) min →
        ∃ d₂ : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded (beta:=beta) bo d₂ ∧
            _root_.F2R (beta:=beta) d₂ =
              _root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) min)
    (hTotal : TotalP (Closest (beta:=beta) bo radix)) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      |_root_.F2R (beta:=beta) q| ≤ |_root_.F2R (beta:=beta) p| →
      _root_.F2R (beta:=beta) (Iminus q (Iminus (Iplus p q) p)) =
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) (Iplus p q) := by
  intro p q hp hq hAbs
  have hFirstExact :
      _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) =
        _root_.F2R (beta:=beta) (Iplus p q) -
          _root_.F2R (beta:=beta) p :=
    MDekker_from_midpoint_payload (beta:=beta)
      bo radix precision Iplus Iminus IplusCorrect
      IplusOp IminusPlus hβ hbetaRadix hradix hprecision hvNum hvNum_gt
      hboundExp hPminPayload hTotal p q hp hq hAbs
  exact MDekkerAux1_from_errorBoundedPlus (beta:=beta) bo radix Iplus Iminus
    IplusCorrect IminusPlus errorBoundedPlus p q hp hq hFirstExact

-- Coq: `Fdigit_abs` — digit invariant under absolute value
noncomputable def Fdigit_abs_check {beta : Int} [ValidRadix beta]
    (radix : Int) (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem Fdigit_abs {beta : Int} [ValidRadix beta]
    (radix : Int) (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜True⌝⦄
    (pure (Fdigit_abs_check (beta:=beta) radix x) : Id Unit)
    ⦃⇓_ => ⌜Fdigit (beta:=beta) radix (Fabs (beta:=beta) x) = Fdigit (beta:=beta) radix x⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, Fdigit_abs_check, Id.run,
    ULift.up_down]
  exact Fdigit_Fabs_early radix x

-- Coq: `Fabs_correct1` — if 0 ≤ F2R x then F2R (Fabs x) = F2R x
noncomputable def Fabs_correct1_check {beta : Int} [ValidRadix beta]
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem Fabs_correct1 {beta : Int} [ValidRadix beta]
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜1 < beta ∧ 0 ≤ _root_.F2R x⌝⦄
    (pure (Fabs_correct1_check (beta:=beta) x) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (Fabs (beta:=beta) x) = _root_.F2R x⌝⦄ := by
  intro ⟨hβ, hx⟩
  simp only [wp, PostCond.noThrow, pure, Fabs_correct1_check, Id.run,
    ULift.up_down]
  show _root_.F2R (Fabs x) = _root_.F2R x
  have hnum : 0 ≤ x.Fnum := FloatSpec.Core.Float_prop.ge_0_F2R (beta:=beta) x hβ hx
  cases x with
  | mk m e =>
    simp only [FloatSpec.Core.Defs.FlocqFloat.Fnum] at hnum
    simp [Fabs, FloatSpec.Calc.Operations.Fabs, FloatSpec.Core.Defs.F2R,
      Int.natAbs_of_nonneg hnum]

-- Coq: `Fabs_correct2` — if F2R x ≤ 0 then F2R (Fabs x) = - F2R x
noncomputable def Fabs_correct2_check {beta : Int} [ValidRadix beta]
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem Fabs_correct2 {beta : Int} [ValidRadix beta]
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜1 < beta ∧ _root_.F2R x ≤ 0⌝⦄
    (pure (Fabs_correct2_check (beta:=beta) x) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (Fabs (beta:=beta) x) = - _root_.F2R x⌝⦄ := by
  intro ⟨hβ, hx⟩
  simp only [wp, PostCond.noThrow, pure, Fabs_correct2_check, Id.run,
    ULift.up_down]
  show _root_.F2R (Fabs x) = - _root_.F2R x
  have habs := FloatSpec.Calc.Operations.F2R_abs (beta:=beta) x hβ
  have hxabs : |_root_.F2R x| = - _root_.F2R x := abs_of_nonpos hx
  calc
    _root_.F2R (Fabs x) = |_root_.F2R x| := by
      simpa [Fabs, _root_.F2R] using habs
    _ = - _root_.F2R x := hxabs

-- Coq: `Fabs_correct` — F2R (Fabs x) = |F2R x|
noncomputable def Fabs_correct_check {beta : Int} [ValidRadix beta]
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem Fabs_correct {beta : Int} [ValidRadix beta]
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜1 < beta⌝⦄
    (pure (Fabs_correct_check (beta:=beta) x) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (Fabs (beta:=beta) x) = |_root_.F2R x|⌝⦄ := by
  intro hβ
  simp only [wp, PostCond.noThrow, pure, Fabs_correct_check, Id.run,
    ULift.up_down]
  show _root_.F2R (Fabs x) = |_root_.F2R x|
  have habs := FloatSpec.Calc.Operations.F2R_abs (beta:=beta) x hβ
  simpa [Fabs, _root_.F2R] using habs

-- Coq: `RleFexpFabs` — for nonzero real value, Float 1 (Fexp p) ≤ Fabs p
noncomputable def RleFexpFabs_check {beta : Int} [ValidRadix beta]
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem RleFexpFabs {beta : Int} [ValidRadix beta]
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜1 < beta ∧ _root_.F2R p ≠ 0⌝⦄
    (pure (RleFexpFabs_check (beta:=beta) p) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (FloatSpec.Core.Defs.FlocqFloat.mk (beta:=beta) 1 p.Fexp)
            ≤ _root_.F2R (Fabs (beta:=beta) p)⌝⦄ := by
  intro ⟨hβ, hp_ne⟩
  simp only [wp, PostCond.noThrow, pure, RleFexpFabs_check, Id.run,
    ULift.up_down]
  show _root_.F2R (FloatSpec.Core.Defs.FlocqFloat.mk (beta:=beta) 1 p.Fexp) ≤
      _root_.F2R (Fabs (beta:=beta) p)
  have hnum_ne : p.Fnum ≠ 0 := by
    intro hp0
    apply hp_ne
    simp [_root_.F2R, FloatSpec.Core.Defs.F2R, hp0]
  have hβpos : (0 : ℝ) < (beta : ℝ) := by
    have : (0 : Int) < beta := lt_trans (by decide) hβ
    exact_mod_cast this
  have hp_pow : 0 ≤ (beta : ℝ) ^ p.Fexp := le_of_lt (zpow_pos hβpos p.Fexp)
  have hmant : (1 : ℝ) ≤ (Int.natAbs p.Fnum : ℝ) := by
    have hpos : 0 < Int.natAbs p.Fnum := Int.natAbs_pos.mpr hnum_ne
    exact_mod_cast (Nat.succ_le_iff.mpr hpos : 1 ≤ Int.natAbs p.Fnum)
  simp [Fabs, FloatSpec.Calc.Operations.Fabs, _root_.F2R, FloatSpec.Core.Defs.F2R]
  simpa using mul_le_mul_of_nonneg_right hmant hp_pow

-- Coq: `Fabs_Fzero` — nonzero stays nonzero under absolute value
noncomputable def Fabs_Fzero_check {beta : Int} [ValidRadix beta]
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem Fabs_Fzero {beta : Int} [ValidRadix beta]
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜¬ is_Fzero x⌝⦄
    (pure (Fabs_Fzero_check (beta:=beta) x) : Id Unit)
    ⦃⇓_ => ⌜¬ is_Fzero (Fabs (beta:=beta) x)⌝⦄ := by
  intro h
  simp only [wp, PostCond.noThrow, pure, Fabs_Fzero_check, Id.run,
    ULift.up_down]
  show ¬ is_Fzero (Fabs x)
  cases x with
  | mk m e =>
    simp [is_Fzero, Fabs, FloatSpec.Calc.Operations.Fabs] at h ⊢
    exact h

-- Compatibility operations
-- pff_add: Add two PffFloats by converting through FlocqFloat and using Calc.Operations.Fplus
def pff_add (beta : Int) [ValidRadix beta]
    (x y : PffFloat beta) : PffFloat beta :=
  flocq_to_pff (FloatSpec.Calc.Operations.Fplus beta (pff_to_flocq beta x) (pff_to_flocq beta y))

def pff_mul (beta : Int) [ValidRadix beta]
    (x y : PffFloat beta) : PffFloat beta :=
  flocq_to_pff (FloatSpec.Calc.Operations.Fmult beta (pff_to_flocq beta x) (pff_to_flocq beta y))

-- Error bounds compatibility
noncomputable def pff_error_bound (prec : Int) : ℝ :=
  (2 : ℝ)^(-prec)

-- Legacy rounding modes
inductive PffRounding where
  | RN : PffRounding  -- Round to nearest
  | RZ : PffRounding  -- Round toward zero
  | RP : PffRounding  -- Round toward plus infinity
  | RM : PffRounding  -- Round toward minus infinity

-- Convert Pff rounding to Flocq rounding
-- Maps PffRounding modes to their corresponding Flocq integer rounding functions
noncomputable def pff_to_flocq_rnd (mode : PffRounding) : ℝ → Int :=
  match mode with
  | PffRounding.RN => fun x => FloatSpec.Core.Generic_fmt.Znearest (fun _ => true) x  -- Round to nearest (ties to +inf)
  | PffRounding.RZ => FloatSpec.Core.Raux.Ztrunc  -- Round toward zero
  | PffRounding.RP => FloatSpec.Core.Raux.Zceil   -- Round toward plus infinity
  | PffRounding.RM => FloatSpec.Core.Raux.Zfloor  -- Round toward minus infinity

-- ---------------------------------------------------------------
-- Minimal LSB/MSB infrastructure

-- Coq: `Fixpoint maxDiv (v : Z) (p : nat) {struct p}` in the radix section.
@[flocq_source "src/Pff/Pff.v" 5854 "maxDiv"]
def maxDiv (radix : Int) (v : Int) : Nat → Nat
  | 0 => 0
  | Nat.succ p' =>
      let p := Nat.succ p'
      letI : Decidable (Zdivides v (Zpower_nat radix p)) :=
        ZdividesP v (Zpower_nat radix p)
      if _h : Zdivides v (Zpower_nat radix p) then p else maxDiv radix v p'

-- Number of significant digits of a float at a given radix.
-- (moved earlier)

-- Shift operation on floats. This local model is a no-op over the stored
-- representation; a duplicate definition existed later, so keep only this one.

-- Coq: `FshiftFdigit` — ~is_Fzero x -> Fdigit (Fshift n x) = Fdigit x + n
noncomputable def FshiftFdigit_check {beta : Int} [ValidRadix beta]
    (radix : Int) (n : Nat) (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem FshiftFdigit {beta : Int} [ValidRadix beta]
    (radix : Int) (n : Nat) (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜1 < radix ∧ ¬ is_Fzero x⌝⦄
    (pure (FshiftFdigit_check (beta:=beta) radix n x) : Id Unit)
    ⦃⇓_ => ⌜Fdigit (beta:=beta) radix (Fshift (beta:=beta) radix n x) =
            Fdigit (beta:=beta) radix x + n⌝⦄ := by
  intro ⟨hradix, hx_nonzero⟩
  simp only [wp, PostCond.noThrow, pure, FshiftFdigit_check,
    Id.run, ULift.up_down]
  unfold Fdigit Fshift is_Fzero at *
  rw [pffDigit_of_one_lt radix (x.Fnum * radix ^ n) hradix,
    pffDigit_of_one_lt radix x.Fnum hradix]
  have hn_nonneg : 0 ≤ (n : Int) := by exact_mod_cast Nat.zero_le n
  have hmul :=
    (FloatSpec.Core.Digits.Zdigits_mult_Zpower
      (beta := radix) (n := x.Fnum) (k := (n : Int)) (h_beta := hradix))
      ⟨hx_nonzero, hn_nonneg⟩
  rcases hmul with ⟨dn, hdn, hres⟩
  have hdn_nonneg :
      0 ≤ FloatSpec.Core.Digits.Zdigits radix x.Fnum := by
    exact
      FloatSpec.Core.Digits.Zdigits_ge_0
        (beta := radix) (n := x.Fnum) trivial
  have hpow_arg :
      x.Fnum * radix ^ (n : Int).natAbs = x.Fnum * radix ^ n := by
    simp
  have hres' :
      FloatSpec.Core.Digits.Zdigits radix (x.Fnum * radix ^ n) =
        FloatSpec.Core.Digits.Zdigits radix x.Fnum + (n : Int) := by
    simpa [hpow_arg, hdn] using hres
  rw [hres']
  rw [Int.toNat_add hdn_nonneg hn_nonneg]
  simp

-- Coq: `FshiftCorrect` — shifting does not change the real value
noncomputable def FshiftCorrect_check {beta : Int} [ValidRadix beta]
    (radix : Int) (n : Nat) (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem FshiftCorrect {beta : Int} [ValidRadix beta]
    (radix : Int) (n : Nat) (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix⌝⦄
    (pure (FshiftCorrect_check (beta:=beta) radix n x) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (Fshift (beta:=beta) radix n x) = _root_.F2R x⌝⦄ := by
  intro ⟨hbeta_radix, hradix⟩
  simp only [wp, PostCond.noThrow, pure, FshiftCorrect_check,
    Id.run, ULift.up_down]
  subst beta
  have hradix_pos_real : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (by omega : (0 : Int) < radix)
  have hradix_ne_real : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos_real
  have hsplit :
      (radix : ℝ) ^ x.Fexp =
        (radix : ℝ) ^ (n : Int) * (radix : ℝ) ^ (x.Fexp - (n : Int)) := by
    have hsum : (n : Int) + (x.Fexp - (n : Int)) = x.Fexp := by omega
    simpa [hsum] using
      (zpow_add₀ hradix_ne_real (n : Int) (x.Fexp - (n : Int)))
  simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, Fshift,
    FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp,
    Int.cast_mul, Int.cast_pow]
  have hzpow_n : (radix : ℝ) ^ (n : Int) = (radix : ℝ) ^ n :=
    zpow_ofNat (radix : ℝ) n
  rw [hsplit]
  rw [hzpow_n]
  ring_nf
  trivial

noncomputable def FSuccZleEq_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FSuccZleEq` — if `q` has exponent no smaller than `p`, and lies in
the half-open interval `[p, FSucc p)`, then `p` and `q` have the same real
value. -/
theorem FSuccZleEq {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision ∧
        _root_.F2R (beta:=beta) p ≤ _root_.F2R (beta:=beta) q ∧
        _root_.F2R (beta:=beta) q <
          _root_.F2R (beta:=beta) (FSucc (beta:=beta) b radix precision p) ∧
        p.Fexp ≤ q.Fexp⌝⦄
    (pure (FSuccZleEq_check (beta:=beta) b radix precision p q) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) p =
      _root_.F2R (beta:=beta) q⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hradix, hprecision, hvnum, hpq, hqsucc, hexp_le⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure, FSuccZleEq_check, Id.run,
    ULift.up_down]
  let d : Nat := Int.natAbs (q.Fexp - p.Fexp)
  let qsh : FloatSpec.Core.Defs.FlocqFloat radix :=
    Fshift (beta:=radix) radix d q
  have hd_nonneg : 0 ≤ q.Fexp - p.Fexp := by omega
  have hd : (d : Int) = q.Fexp - p.Fexp := by
    simpa [d] using Int.natAbs_of_nonneg hd_nonneg
  have hqsh_exp : qsh.Fexp = p.Fexp := by
    simp [qsh, Fshift, d, hd]
  have hqsh_val :
      _root_.F2R (beta:=radix) qsh =
        _root_.F2R (beta:=radix) q := by
    have hfc := FshiftCorrect (beta:=radix) radix d q
    simpa [qsh] using hfc ⟨rfl, hradix⟩
  have hp_le_qsh :
      _root_.F2R (beta:=radix) p ≤
        _root_.F2R (beta:=radix) qsh := by
    rw [hqsh_val]
    exact hpq
  have hqsh_lt_succ :
      _root_.F2R (beta:=radix) qsh <
        _root_.F2R (beta:=radix)
          (FSucc (beta:=radix) b radix precision p) := by
    rw [hqsh_val]
    exact hqsucc
  have hqsh_num_ge : p.Fnum ≤ qsh.Fnum := by
    have hle := Rle_Fexp_eq_Zle (beta:=radix) p qsh
    simpa only [wp, PostCond.noThrow, pure, Rle_Fexp_eq_Zle_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
      hle ⟨hp_le_qsh, hqsh_exp.symm, hradix⟩
  have hradix_pos : 0 < radix := by omega
  have hvnum_mul_norm : b.vNum = radix * nNormMin radix precision := by
    rw [hvnum, Zpower_nat, nNormMin]
    have hprec : Nat.succ (precision - 1) = precision := by omega
    calc
      radix ^ precision = radix ^ Nat.succ (precision - 1) := by rw [hprec]
      _ = radix ^ (precision - 1) * radix := by rw [pow_succ]
      _ = radix * radix ^ (precision - 1) := by ring
  have hqsh_num_lt : qsh.Fnum < p.Fnum + 1 := by
    by_cases htop : p.Fnum = pPred b.vNum
    · let ssh : FloatSpec.Core.Defs.FlocqFloat radix :=
        Fshift (beta:=radix) radix 1
          (FSucc (beta:=radix) b radix precision p)
      have hsucc_eq :
          FSucc (beta:=radix) b radix precision p =
            (⟨nNormMin radix precision, p.Fexp + 1⟩ :
              FloatSpec.Core.Defs.FlocqFloat radix) := by
        unfold FSucc
        simp [htop]
      have hssh_exp : ssh.Fexp = qsh.Fexp := by
        simp [ssh, Fshift, hsucc_eq, hqsh_exp]
      have hssh_val :
          _root_.F2R (beta:=radix) ssh =
            _root_.F2R (beta:=radix)
              (FSucc (beta:=radix) b radix precision p) := by
        have hfc := FshiftCorrect (beta:=radix) radix 1
          (FSucc (beta:=radix) b radix precision p)
        simpa [ssh] using hfc ⟨rfl, hradix⟩
      have hlt_num : qsh.Fnum < ssh.Fnum := by
        have hlt := Rlt_Fexp_eq_Zlt (beta:=radix) qsh ssh
        have hlt_val :
            _root_.F2R (beta:=radix) qsh <
              _root_.F2R (beta:=radix) ssh := by
          rw [hssh_val]
          exact hqsh_lt_succ
        simpa only [wp, PostCond.noThrow, pure, Rlt_Fexp_eq_Zlt_check,
          PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
          Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
          hlt ⟨hradix, hlt_val, hssh_exp.symm⟩
      have hssh_num : ssh.Fnum = p.Fnum + 1 := by
        simp [ssh, Fshift, hsucc_eq, htop, pPred, hvnum_mul_norm]
        ring
      omega
    · by_cases hlow : p.Fnum = -nNormMin radix precision
      · by_cases hexp_min : p.Fexp = -b.dExp
        · have hlow_not_top : -nNormMin radix precision ≠ pPred b.vNum := by
            unfold pPred
            rw [hvnum_mul_norm]
            have hnm_pos : 0 < nNormMin radix precision := by
              unfold nNormMin
              exact pow_pos hradix_pos _
            nlinarith
          have hsucc_eq :
              FSucc (beta:=radix) b radix precision p =
                (⟨p.Fnum + 1, p.Fexp⟩ :
                  FloatSpec.Core.Defs.FlocqFloat radix) := by
            unfold FSucc
            simp [htop, hlow, hlow_not_top, hexp_min]
          have hsucc_exp :
              (FSucc (beta:=radix) b radix precision p).Fexp = qsh.Fexp := by
            simp [hsucc_eq, hqsh_exp]
          have hlt_num : qsh.Fnum <
              (FSucc (beta:=radix) b radix precision p).Fnum := by
            have hlt := Rlt_Fexp_eq_Zlt (beta:=radix) qsh
              (FSucc (beta:=radix) b radix precision p)
            simpa only [wp, PostCond.noThrow, pure, Rlt_Fexp_eq_Zlt_check,
              PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
              Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
              hlt ⟨hradix, hqsh_lt_succ, hsucc_exp.symm⟩
          simp [hsucc_eq] at hlt_num
          exact lt_of_le_of_lt hlt_num (by omega)
        · let qsh1 : FloatSpec.Core.Defs.FlocqFloat radix :=
            Fshift (beta:=radix) radix 1 qsh
          have hlow_not_top : -nNormMin radix precision ≠ pPred b.vNum := by
            unfold pPred
            rw [hvnum_mul_norm]
            have hnm_pos : 0 < nNormMin radix precision := by
              unfold nNormMin
              exact pow_pos hradix_pos _
            nlinarith
          have hsucc_eq :
              FSucc (beta:=radix) b radix precision p =
                (⟨-pPred b.vNum, p.Fexp - 1⟩ :
                  FloatSpec.Core.Defs.FlocqFloat radix) := by
            unfold FSucc
            simp [htop, hlow, hlow_not_top, hexp_min]
          have hqsh1_exp :
              qsh1.Fexp =
                (FSucc (beta:=radix) b radix precision p).Fexp := by
            simp [qsh1, Fshift, hqsh_exp, hsucc_eq]
          have hqsh1_val :
              _root_.F2R (beta:=radix) qsh1 =
                _root_.F2R (beta:=radix) qsh := by
            have hfc := FshiftCorrect (beta:=radix) radix 1 qsh
            simpa [qsh1] using hfc ⟨rfl, hradix⟩
          have hlt_shift :
              _root_.F2R (beta:=radix) qsh1 <
                _root_.F2R (beta:=radix)
                  (FSucc (beta:=radix) b radix precision p) := by
            rw [hqsh1_val]
            exact hqsh_lt_succ
          have hlt_num_shift : qsh1.Fnum <
              (FSucc (beta:=radix) b radix precision p).Fnum := by
            have hlt := Rlt_Fexp_eq_Zlt (beta:=radix) qsh1
              (FSucc (beta:=radix) b radix precision p)
            simpa only [wp, PostCond.noThrow, pure, Rlt_Fexp_eq_Zlt_check,
              PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
              Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
              hlt ⟨hradix, hlt_shift, hqsh1_exp⟩
          have hscaled : radix * qsh.Fnum < -pPred b.vNum := by
            simpa [qsh1, Fshift, hsucc_eq, mul_comm] using hlt_num_shift
          have hpnum : p.Fnum = -nNormMin radix precision := hlow
          unfold pPred at hscaled
          rw [hvnum_mul_norm] at hscaled
          by_contra hnot_lt
          have hge : p.Fnum + 1 ≤ qsh.Fnum := le_of_not_gt hnot_lt
          have hscaled_ge :
              radix * (p.Fnum + 1) ≤ radix * qsh.Fnum :=
            mul_le_mul_of_nonneg_left hge (le_of_lt hradix_pos)
          rw [hpnum] at hscaled_ge
          ring_nf at hscaled hscaled_ge
          omega
      · have hsucc_eq :
            FSucc (beta:=radix) b radix precision p =
              (⟨p.Fnum + 1, p.Fexp⟩ :
                FloatSpec.Core.Defs.FlocqFloat radix) := by
          unfold FSucc
          simp [htop, hlow]
        have hsucc_exp :
            (FSucc (beta:=radix) b radix precision p).Fexp = qsh.Fexp := by
          simp [hsucc_eq, hqsh_exp]
        have hlt_num : qsh.Fnum <
            (FSucc (beta:=radix) b radix precision p).Fnum := by
          have hlt := Rlt_Fexp_eq_Zlt (beta:=radix) qsh
            (FSucc (beta:=radix) b radix precision p)
          simpa only [wp, PostCond.noThrow, pure, Rlt_Fexp_eq_Zlt_check,
            PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
            Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
            hlt ⟨hradix, hqsh_lt_succ, hsucc_exp.symm⟩
        simp [hsucc_eq] at hlt_num
        exact lt_of_le_of_lt hlt_num (by omega)
  have hqsh_num_eq : qsh.Fnum = p.Fnum := by omega
  have hp_eq_qsh :
      _root_.F2R (beta:=radix) p =
        _root_.F2R (beta:=radix) qsh := by
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R]
    rw [hqsh_exp, hqsh_num_eq]
  rw [hp_eq_qsh, hqsh_val]
  rfl

/-- Coq: `PminPos` — if `min` is the lower rounded value of `p/2` for a
nonnegative bounded float `p`, then the residual `p - min` is representable by
a bounded float.

The proof follows Flocq's argument.  If `min = p/2`, `min` itself is the
residual.  Otherwise `eqExpMax` aligns `min` to the exponent of `p`, and
`FboundNext` supplies the bounded successor; minimality of `min` forces that
successor to be exactly `p - min`. -/
theorem PminPos {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (p min : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision ∧
        0 ≤ _root_.F2R (beta:=beta) p ∧
        Fbounded (beta:=beta) b p ∧
        isMin (beta:=beta) b radix ((1 / 2 : ℝ) * _root_.F2R (beta:=beta) p) min⌝⦄
    (pure (PminPos_check (beta:=beta) b radix p min) : Id Unit)
    ⦃⇓_ => ⌜∃ c : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) b c ∧
        _root_.F2R (beta:=beta) c =
          _root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) min⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta_radix, hradix, hprecision, hvNum, hp_nonneg, hpBounded, hMin⟩
  simp only [wp, PostCond.noThrow, pure, PminPos_check, Id.run, ULift.up_down]
  rcases hMin with ⟨hminBounded, hmin_le_half, hminGreatest⟩
  by_cases hhalf :
      _root_.F2R (beta:=beta) min = (1 / 2 : ℝ) * _root_.F2R (beta:=beta) p
  · refine ⟨min, hminBounded, ?_⟩
    linarith
  · have hmin_lt_half :
        _root_.F2R (beta:=beta) min <
          (1 / 2 : ℝ) * _root_.F2R (beta:=beta) p :=
      lt_of_le_of_ne hmin_le_half hhalf
    have hzeroBounded : Fbounded (beta:=beta) b
        (FloatSpec.Core.Defs.FlocqFloat.mk (beta:=beta) 0 (-b.dExp)) := by
      constructor
      · simpa [Fbounded] using
          (show (0 : Int) < b.vNum by
            rw [hvNum, Zpower_nat]
            exact pow_pos (by omega : 0 < radix) precision)
      · simp
    have hzero_le_half :
        _root_.F2R (beta:=beta)
            (FloatSpec.Core.Defs.FlocqFloat.mk (beta:=beta) 0 (-b.dExp)) ≤
          (1 / 2 : ℝ) * _root_.F2R (beta:=beta) p := by
      have hhalf_nonneg :
          (0 : ℝ) ≤ (1 / 2 : ℝ) * _root_.F2R (beta:=beta) p := by
        nlinarith
      simpa [_root_.F2R, FloatSpec.Core.Defs.F2R] using hhalf_nonneg
    have hmin_nonneg : 0 ≤ _root_.F2R (beta:=beta) min := by
      have h := hminGreatest
        (FloatSpec.Core.Defs.FlocqFloat.mk (beta:=beta) 0 (-b.dExp))
        hzeroBounded hzero_le_half
      simpa [_root_.F2R, FloatSpec.Core.Defs.F2R] using h
    have hAbsLe : |_root_.F2R (beta:=beta) min| ≤ _root_.F2R (beta:=beta) p := by
      rw [abs_of_nonneg hmin_nonneg]
      nlinarith
    have heqExp := eqExpMax (beta:=beta) b radix min p
    have heqExp' :
        ∃ min' : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded (beta:=beta) b min' ∧
          _root_.F2R (beta:=beta) min' = _root_.F2R (beta:=beta) min ∧
          min'.Fexp ≤ p.Fexp := by
      simpa only [wp, PostCond.noThrow, pure, eqExpMax_check, Id.run,
        PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
        ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
        heqExp ⟨hbeta_radix, hradix, hminBounded, hpBounded, hAbsLe⟩
    rcases heqExp' with ⟨min', hmin'Bounded, hmin'_val, hmin'_exp_le⟩
    have hnext := FboundNext (beta:=beta) b radix precision min'
    have hnext' :
        ∃ smin : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded (beta:=beta) b smin ∧
          _root_.F2R (beta:=beta) smin =
            _root_.F2R (beta:=beta)
              (FloatSpec.Core.Defs.FlocqFloat.mk (beta:=beta)
                (min'.Fnum + 1) min'.Fexp) := by
      simpa only [wp, PostCond.noThrow, pure, FboundNext_check, Id.run,
        PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
        ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
        hnext ⟨hbeta_radix, hradix, hprecision, hvNum, hmin'Bounded⟩
    rcases hnext' with ⟨smin, hsminBounded, hsmin_val⟩
    let n : Nat := Int.natAbs (p.Fexp - min'.Fexp)
    let pshift : FloatSpec.Core.Defs.FlocqFloat beta := Fshift (beta:=beta) radix n p
    have hn : (n : Int) = p.Fexp - min'.Fexp := by
      have hnonneg : 0 ≤ p.Fexp - min'.Fexp := by omega
      simpa [n] using Int.natAbs_of_nonneg hnonneg
    have hpshift_exp : pshift.Fexp = min'.Fexp := by
      simp [pshift, Fshift, hn]
    have hpshift_val : _root_.F2R (beta:=beta) pshift = _root_.F2R (beta:=beta) p := by
      have hcorr := FshiftCorrect (beta:=beta) radix n p
      simpa only [wp, PostCond.noThrow, pure, FshiftCorrect_check, Id.run,
        PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
        ULift.up, ULift.down, ULift.up_down, pshift, Int.cast_ofNat] using
        hcorr ⟨hbeta_radix, hradix⟩
    have hbeta_gt : 1 < beta := by omega
    have hbeta_pos_real : (0 : ℝ) < (beta : ℝ) := by
      exact_mod_cast (by omega : (0 : Int) < beta)
    have hpow_pos : 0 < (beta : ℝ) ^ min'.Fexp :=
      zpow_pos hbeta_pos_real min'.Fexp
    have hdouble_min_lt :
        _root_.F2R (beta:=beta)
            (FloatSpec.Core.Defs.FlocqFloat.mk (beta:=beta)
              (2 * min'.Fnum) min'.Fexp) <
          _root_.F2R (beta:=beta) pshift := by
      have htwice :
          2 * _root_.F2R (beta:=beta) min' <
            _root_.F2R (beta:=beta) pshift := by
        rw [hpshift_val, hmin'_val]
        nlinarith
      calc
        _root_.F2R (beta:=beta)
            (FloatSpec.Core.Defs.FlocqFloat.mk (beta:=beta)
              (2 * min'.Fnum) min'.Fexp)
            = 2 * _root_.F2R (beta:=beta) min' := by
                simp [_root_.F2R, FloatSpec.Core.Defs.F2R]
                ring
        _ < _root_.F2R (beta:=beta) pshift := htwice
    have hlow_num : 2 * min'.Fnum + 1 ≤ pshift.Fnum := by
      have hlt_num : 2 * min'.Fnum < pshift.Fnum := by
        have hcmp := Rlt_Float_Zlt (beta:=beta) (2 * min'.Fnum) pshift.Fnum min'.Fexp
        have hdouble_min_lt_same :
            _root_.F2R (beta:=beta)
                (FloatSpec.Core.Defs.FlocqFloat.mk (beta:=beta)
                  (2 * min'.Fnum) min'.Fexp) <
              _root_.F2R (beta:=beta)
                (FloatSpec.Core.Defs.FlocqFloat.mk (beta:=beta)
                  pshift.Fnum min'.Fexp) := by
          simpa [_root_.F2R, FloatSpec.Core.Defs.F2R, hpshift_exp]
            using hdouble_min_lt
        have hres := hcmp ⟨hbeta_gt, hdouble_min_lt_same⟩
        simpa only [wp, PostCond.noThrow, pure, Rlt_Float_Zlt_check,
          PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
          Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using hres
      omega
    have hsmin_gt_min' :
        _root_.F2R (beta:=beta) min' < _root_.F2R (beta:=beta) smin := by
      rw [hsmin_val]
      simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
        FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp,
        Int.cast_add, Int.cast_one]
      nlinarith [hpow_pos]
    have hnot_high : ¬ 2 * (min'.Fnum + 1) ≤ pshift.Fnum := by
      intro hhi
      have hsmin_le_half :
          _root_.F2R (beta:=beta) smin ≤
            (1 / 2 : ℝ) * _root_.F2R (beta:=beta) p := by
        rw [hsmin_val, ← hpshift_val]
        simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
          FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp,
          hpshift_exp, Int.cast_add, Int.cast_one]
        have hcast : ((2 * (min'.Fnum + 1) : Int) : ℝ) ≤ (pshift.Fnum : ℝ) := by
          exact_mod_cast hhi
        have hmul :
            ((2 * (min'.Fnum + 1) : Int) : ℝ) * (beta : ℝ) ^ min'.Fexp ≤
              (pshift.Fnum : ℝ) * (beta : ℝ) ^ min'.Fexp :=
          mul_le_mul_of_nonneg_right hcast (le_of_lt hpow_pos)
        have hmul' :
            2 * ((min'.Fnum + 1 : Int) : ℝ) * (beta : ℝ) ^ min'.Fexp ≤
              (pshift.Fnum : ℝ) * (beta : ℝ) ^ min'.Fexp := by
          simpa [Int.cast_mul, Int.cast_ofNat, mul_assoc] using hmul
        have hmul'' :
            2 * ((min'.Fnum : ℝ) + 1) * (beta : ℝ) ^ min'.Fexp ≤
              (pshift.Fnum : ℝ) * (beta : ℝ) ^ min'.Fexp := by
          simpa [Int.cast_add, Int.cast_one, mul_assoc] using hmul'
        calc
          ((min'.Fnum : ℝ) + 1) * (beta : ℝ) ^ min'.Fexp
              = (1 / 2 : ℝ) *
                  (2 * ((min'.Fnum : ℝ) + 1) * (beta : ℝ) ^ min'.Fexp) := by ring
          _ ≤ (1 / 2 : ℝ) *
                ((pshift.Fnum : ℝ) * (beta : ℝ) ^ min'.Fexp) := by
              exact mul_le_mul_of_nonneg_left hmul'' (by norm_num)
      have hsmin_le_min := hminGreatest smin hsminBounded hsmin_le_half
      rw [← hmin'_val] at hsmin_le_min
      linarith
    have hnum_eq : pshift.Fnum = 2 * min'.Fnum + 1 := by
      omega
    refine ⟨smin, hsminBounded, ?_⟩
    rw [hsmin_val, ← hmin'_val, ← hpshift_val]
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
      FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp,
      hpshift_exp, hnum_eq, Int.cast_add, Int.cast_mul, Int.cast_ofNat,
      Int.cast_one]
    ring

/-- Coq: `div2IsBetweenPos` — the adjacent lower and upper bounded floats
around half of a nonnegative bounded float sum to that float. -/
theorem div2IsBetweenPos {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (p min max : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < precision ∧
        b.vNum = Zpower_nat radix precision ∧
        0 ≤ _root_.F2R (beta:=beta) p ∧
        Fbounded (beta:=beta) b p ∧
        isMin (beta:=beta) b radix
          ((1 / 2 : ℝ) * _root_.F2R (beta:=beta) p) min ∧
        isMax (beta:=beta) b radix
          ((1 / 2 : ℝ) * _root_.F2R (beta:=beta) p) max⌝⦄
    (pure (div2IsBetweenPos_check (beta:=beta) b radix p min max) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) p =
      _root_.F2R (beta:=beta) min + _root_.F2R (beta:=beta) max⌝⦄ := by
  intro ⟨hbeta, hradix, hprecision, hvnum, hp_nonneg, hpBounded, hMin, hMax⟩
  simp only [wp, PostCond.noThrow, pure, div2IsBetweenPos_check,
    PredTrans.pure, PredTrans.apply, Id.run, ULift.up, ULift.down]
  have hbeta_gt : 1 < beta := by omega
  have hD₂ :
      ∃ d₂ : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) b d₂ ∧
          _root_.F2R (beta:=beta) d₂ =
            _root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) min := by
    have h := PminPos (beta:=beta) b radix precision p min
    simpa only [wp, PostCond.noThrow, pure, PminPos_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
      h ⟨hbeta, hradix, by omega, hvnum, hp_nonneg, hpBounded, hMin⟩
  have hmax_nonneg : 0 ≤ _root_.F2R (beta:=beta) max := by
    nlinarith [hMax.2.1]
  have hmax_le_p :
      _root_.F2R (beta:=beta) max ≤ _root_.F2R (beta:=beta) p := by
    apply hMax.2.2 p hpBounded
    nlinarith
  have hD₁ :
      ∃ d₁ : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) b d₁ ∧
          _root_.F2R (beta:=beta) d₁ =
            _root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) max := by
    refine ⟨FloatSpec.Calc.Operations.Fminus (beta:=beta) p max, ?_, ?_⟩
    · have h := Sterbenz (beta:=beta) b p max
      simpa only [wp, PostCond.noThrow, pure, Sterbenz_check, Fminus,
        PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
        Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
        h ⟨hbeta_gt, hpBounded, hMax.1, by nlinarith, by nlinarith [hMax.2.1]⟩
    · have h := Fminus_correct (beta:=beta) p max
      simpa only [wp, PostCond.noThrow, pure, Fminus_correct_check, Fminus,
        PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
        Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using h hbeta_gt
  have h := div2IsBetween_from_extrema (beta:=beta) b radix p min max
  simpa only [wp, PostCond.noThrow, pure, div2IsBetween_check,
    PredTrans.pure, PredTrans.apply, Id.run, ULift.up, ULift.down, Int.cast_ofNat] using
    h ⟨hpBounded, hMin, hMax, hD₁, hD₂⟩

/-- Coq: `div2IsBetween` — signed form of `div2IsBetweenPos`. -/
theorem div2IsBetween {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (p min max : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < precision ∧
        b.vNum = Zpower_nat radix precision ∧
        Fbounded (beta:=beta) b p ∧
        isMin (beta:=beta) b radix
          ((1 / 2 : ℝ) * _root_.F2R (beta:=beta) p) min ∧
        isMax (beta:=beta) b radix
          ((1 / 2 : ℝ) * _root_.F2R (beta:=beta) p) max⌝⦄
    (pure (div2IsBetween_check (beta:=beta) b radix p min max) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) p =
      _root_.F2R (beta:=beta) min + _root_.F2R (beta:=beta) max⌝⦄ := by
  intro ⟨hbeta, hradix, hprecision, hvnum, hpBounded, hMin, hMax⟩
  simp only [wp, PostCond.noThrow, pure, div2IsBetween_check,
    PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
    Id.run, ULift.up, ULift.down]
  by_cases hp_nonneg : 0 ≤ _root_.F2R (beta:=beta) p
  · have h := div2IsBetweenPos (beta:=beta) b radix precision p min max
    simpa only [wp, PostCond.noThrow, pure, div2IsBetweenPos_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, Int.cast_ofNat] using
      h ⟨hbeta, hradix, hprecision, hvnum, hp_nonneg, hpBounded, hMin, hMax⟩
  · have hp_neg : _root_.F2R (beta:=beta) p < 0 := lt_of_not_ge hp_nonneg
    have hpOppBounded :
        Fbounded (beta:=beta) b (Fopp (beta:=beta) p) := by
      have h := oppBounded (beta:=beta) b p
      simpa only [wp, PostCond.noThrow, pure, oppBounded_check,
        PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
        Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using h hpBounded
    have hMinOpp : isMin (beta:=beta) b radix
        ((1 / 2 : ℝ) * _root_.F2R (beta:=beta) (Fopp (beta:=beta) p))
        (Fopp (beta:=beta) max) := by
      have h := MaxOppMin (beta:=beta) b radix max
        ((1 / 2 : ℝ) * _root_.F2R (beta:=beta) p)
      have h' : isMin (beta:=beta) b radix
          (-((1 / 2 : ℝ) * _root_.F2R (beta:=beta) p))
          (Fopp (beta:=beta) max) := by
        simpa only [wp, PostCond.noThrow, pure, MaxOppMin_check,
          PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
          Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using h hMax
      simpa [Fopp, FloatSpec.Calc.Operations.Fopp, _root_.F2R,
        FloatSpec.Core.Defs.F2R] using h'
    have hMaxOpp : isMax (beta:=beta) b radix
        ((1 / 2 : ℝ) * _root_.F2R (beta:=beta) (Fopp (beta:=beta) p))
        (Fopp (beta:=beta) min) := by
      have h := MinOppMax (beta:=beta) b radix min
        ((1 / 2 : ℝ) * _root_.F2R (beta:=beta) p)
      have h' : isMax (beta:=beta) b radix
          (-((1 / 2 : ℝ) * _root_.F2R (beta:=beta) p))
          (Fopp (beta:=beta) min) := by
        simpa only [wp, PostCond.noThrow, pure, MinOppMax_check,
          PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
          Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using h hMin
      simpa [Fopp, FloatSpec.Calc.Operations.Fopp, _root_.F2R,
        FloatSpec.Core.Defs.F2R] using h'
    have hpOpp_nonneg :
        0 ≤ _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) := by
      have hopp := FloatSpec.Calc.Operations.F2R_opp (beta:=beta) p
      rw [show _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) =
          -_root_.F2R (beta:=beta) p by
        simpa [Fopp, _root_.F2R] using hopp trivial]
      linarith
    have h := div2IsBetweenPos (beta:=beta) b radix precision
      (Fopp (beta:=beta) p) (Fopp (beta:=beta) max) (Fopp (beta:=beta) min)
    have h' :
        _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) =
          _root_.F2R (beta:=beta) (Fopp (beta:=beta) max) +
            _root_.F2R (beta:=beta) (Fopp (beta:=beta) min) := by
      simpa only [wp, PostCond.noThrow, pure, div2IsBetweenPos_check,
        PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
        Id.run, ULift.up, ULift.down, Int.cast_ofNat] using
        h ⟨hbeta, hradix, hprecision, hvnum, hpOpp_nonneg, hpOppBounded,
          hMinOpp, hMaxOpp⟩
    simpa [Fopp, FloatSpec.Calc.Operations.Fopp, _root_.F2R,
      FloatSpec.Core.Defs.F2R, add_comm] using congrArg Neg.neg h'

noncomputable def ExactMinusInterval_sterbenz_case_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel)
    (p r : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Sterbenz branch of Coq's `ExactMinusIntervalAux1`.

When `0 <= p <= r <= 2*p`, the exact difference `r - p` is represented by
`Fminus r p` and remains bounded.  This is the easy branch used before the
upstream predecessor-interval induction takes over for the `2*p < r` case. -/
theorem ExactMinusInterval_sterbenz_case {beta : Int} [ValidRadix beta]
    (b : Fbound_skel)
    (p r : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜1 < beta ∧
        Fbounded (beta:=beta) b p ∧
        Fbounded (beta:=beta) b r ∧
        0 ≤ _root_.F2R (beta:=beta) p ∧
        _root_.F2R (beta:=beta) p ≤ _root_.F2R (beta:=beta) r ∧
        _root_.F2R (beta:=beta) r ≤ 2 * _root_.F2R (beta:=beta) p⌝⦄
    (pure (ExactMinusInterval_sterbenz_case_check (beta:=beta) b p r) : Id Unit)
    ⦃⇓_ => ⌜∃ rp : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) b rp ∧
        _root_.F2R (beta:=beta) rp =
          _root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) p⌝⦄ := by
  intro h
  rcases h with ⟨hβ, hpBound, hrBound, hpNonneg, hp_le_r, hr_le_2p⟩
  simp only [wp, PostCond.noThrow, pure,
    ExactMinusInterval_sterbenz_case_check, Id.run, ULift.up_down]
  refine ⟨FloatSpec.Calc.Operations.Fminus (beta:=beta) r p, ?_, ?_⟩
  · have hhalf_p_le_r :
        (1 / 2 : ℝ) * _root_.F2R (beta:=beta) p ≤
          _root_.F2R (beta:=beta) r := by
      nlinarith
    have hTrip := Sterbenz (beta:=beta) b r p
    simpa [wp, PostCond.noThrow, pure, Sterbenz_check, Fminus, Id.run,
      ULift.up_down] using
      hTrip ⟨hβ, hrBound, hpBound, hhalf_p_le_r, hr_le_2p⟩
  · have hTrip := Fminus_correct (beta:=beta) r p
    simpa [wp, PostCond.noThrow, pure, Fminus_correct_check, Fminus, Id.run,
      ULift.up_down] using hTrip hβ

/-- Branch skeleton of Coq `ExactMinusIntervalAux1`.

This packages the two easy Sterbenz branches from upstream:
if either the upper interval endpoint `q` or the requested point `r` is at most
`2*p`, then `r - p` is represented by `Fminus r p`.  The only remaining
premise is exactly the hard predecessor-induction branch
`2*p < q` and `2*p < r`. -/
theorem ExactMinusIntervalAux1_from_hard_branch {beta : Int} [ValidRadix beta]
    (b : Fbound_skel)
    (p q r : FloatSpec.Core.Defs.FlocqFloat beta)
    (hHard :
      2 * _root_.F2R (beta:=beta) p < _root_.F2R (beta:=beta) q →
      2 * _root_.F2R (beta:=beta) p < _root_.F2R (beta:=beta) r →
      ∃ rp : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) b rp ∧
          _root_.F2R (beta:=beta) rp =
            _root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) p) :
    1 < beta →
    Fbounded (beta:=beta) b p →
    Fbounded (beta:=beta) b r →
    0 ≤ _root_.F2R (beta:=beta) p →
    _root_.F2R (beta:=beta) p ≤ _root_.F2R (beta:=beta) q →
    _root_.F2R (beta:=beta) p ≤ _root_.F2R (beta:=beta) r →
    _root_.F2R (beta:=beta) r ≤ _root_.F2R (beta:=beta) q →
    ∃ rp : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) b rp ∧
        _root_.F2R (beta:=beta) rp =
          _root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) p := by
  intro hβ hpBound hrBound hp_nonneg hp_le_q hp_le_r hr_le_q
  by_cases hq_le_2p :
      _root_.F2R (beta:=beta) q ≤ 2 * _root_.F2R (beta:=beta) p
  · have hr_le_2p :
        _root_.F2R (beta:=beta) r ≤ 2 * _root_.F2R (beta:=beta) p :=
      le_trans hr_le_q hq_le_2p
    have hTrip := ExactMinusInterval_sterbenz_case (beta:=beta) b p r
    simpa [wp, PostCond.noThrow, pure,
      ExactMinusInterval_sterbenz_case_check, Id.run, ULift.up_down, Int.cast_ofNat] using
      hTrip ⟨hβ, hpBound, hrBound, hp_nonneg, hp_le_r, hr_le_2p⟩
  · have hq_gt_2p :
        2 * _root_.F2R (beta:=beta) p < _root_.F2R (beta:=beta) q :=
      lt_of_not_ge hq_le_2p
    by_cases hr_le_2p :
        _root_.F2R (beta:=beta) r ≤ 2 * _root_.F2R (beta:=beta) p
    · have hTrip := ExactMinusInterval_sterbenz_case (beta:=beta) b p r
      simpa [wp, PostCond.noThrow, pure,
        ExactMinusInterval_sterbenz_case_check, Id.run, ULift.up_down, Int.cast_ofNat] using
        hTrip ⟨hβ, hpBound, hrBound, hp_nonneg, hp_le_r, hr_le_2p⟩
    · have hr_gt_2p :
          2 * _root_.F2R (beta:=beta) p < _root_.F2R (beta:=beta) r :=
        lt_of_not_ge hr_le_2p
      exact hHard hq_gt_2p hr_gt_2p

/-- Coq `ExactMinusIntervalAux`, factored at the predecessor step.

Upstream proves the hard branch of `ExactMinusIntervalAux` by downward
induction from the upper endpoint `q`.  This theorem packages that induction
using the restored `FinductNeg`; the remaining premise is exactly the local
predecessor step that turns a representation of `r - p` into one for
`FPred r - p`. -/
theorem ExactMinusIntervalAux_from_pred_step {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta)
    (hStep :
      ∀ r : FloatSpec.Core.Defs.FlocqFloat beta,
        Fcanonic (beta:=beta) beta b r →
        0 < _root_.F2R (beta:=beta) r →
        _root_.F2R (beta:=beta) r ≤ _root_.F2R (beta:=beta) q →
        (∃ rp : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded (beta:=beta) b rp ∧
            _root_.F2R (beta:=beta) rp =
              _root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) p) →
        ∃ rp : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded (beta:=beta) b rp ∧
            _root_.F2R (beta:=beta) rp =
              _root_.F2R (beta:=beta)
                (FPred (beta:=beta) b beta precision r) -
                  _root_.F2R (beta:=beta) p) :
    1 < beta →
    1 < precision →
    b.vNum = Zpower_nat beta precision →
    0 < _root_.F2R (beta:=beta) p →
    2 * _root_.F2R (beta:=beta) p < _root_.F2R (beta:=beta) q →
    Fcanonic (beta:=beta) beta b p →
    Fcanonic (beta:=beta) beta b q →
    (∃ qmp : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) b qmp ∧
        _root_.F2R (beta:=beta) qmp =
          _root_.F2R (beta:=beta) q - _root_.F2R (beta:=beta) p) →
    ∀ r : FloatSpec.Core.Defs.FlocqFloat beta,
      Fcanonic (beta:=beta) beta b r →
      2 * _root_.F2R (beta:=beta) p < _root_.F2R (beta:=beta) r →
      _root_.F2R (beta:=beta) r ≤ _root_.F2R (beta:=beta) q →
      ∃ rp : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) b rp ∧
          _root_.F2R (beta:=beta) rp =
            _root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) p := by
  intro hβ hprecision hvnum hp_pos hq_gt_2p _hpCan hqCan hqmp r hrCan hr_gt_2p hr_le_q
  let P : FloatSpec.Core.Defs.FlocqFloat beta → Prop :=
    fun x =>
      ∃ xp : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) b xp ∧
          _root_.F2R (beta:=beta) xp =
            _root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p
  have hq_nonneg : 0 ≤ _root_.F2R (beta:=beta) q := by nlinarith
  have hr_nonneg : 0 ≤ _root_.F2R (beta:=beta) r := by nlinarith
  have hInd := FinductNeg (beta:=beta) b beta precision rfl hβ hprecision hvnum
    P q hq_nonneg hqCan hqmp
    (by
      intro x hxCan hxPos hxLe hxP
      exact hStep x hxCan hxPos hxLe hxP)
  exact hInd r hrCan hr_nonneg hr_le_q

/-- Coq-shaped hard-branch induction for `ExactMinusIntervalAux`.

The upstream proof applies `FinductNeg` to the predicate
`fun x => 2*p < x -> exists xp, xp = x - p`.  This is weaker and faithful to
the Coq proof: the predecessor step only has to handle the hard branch
`2*p < FPred r`; the boundary `FPred r <= 2*p` belongs to
`ExactMinusIntervalAux1`/Sterbenz rather than this induction step. -/
theorem ExactMinusIntervalAux_from_hard_pred_step {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta)
    (hStep :
      ∀ r : FloatSpec.Core.Defs.FlocqFloat beta,
        Fcanonic (beta:=beta) beta b r →
        0 < _root_.F2R (beta:=beta) r →
        _root_.F2R (beta:=beta) r ≤ _root_.F2R (beta:=beta) q →
        (2 * _root_.F2R (beta:=beta) p < _root_.F2R (beta:=beta) r →
          ∃ rp : FloatSpec.Core.Defs.FlocqFloat beta,
            Fbounded (beta:=beta) b rp ∧
              _root_.F2R (beta:=beta) rp =
                _root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) p) →
        2 * _root_.F2R (beta:=beta) p <
          _root_.F2R (beta:=beta)
            (FPred (beta:=beta) b beta precision r) →
        ∃ rp : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded (beta:=beta) b rp ∧
            _root_.F2R (beta:=beta) rp =
              _root_.F2R (beta:=beta)
                (FPred (beta:=beta) b beta precision r) -
                  _root_.F2R (beta:=beta) p) :
    1 < beta →
    1 < precision →
    b.vNum = Zpower_nat beta precision →
    0 < _root_.F2R (beta:=beta) p →
    2 * _root_.F2R (beta:=beta) p < _root_.F2R (beta:=beta) q →
    Fcanonic (beta:=beta) beta b p →
    Fcanonic (beta:=beta) beta b q →
    (∃ qmp : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) b qmp ∧
        _root_.F2R (beta:=beta) qmp =
          _root_.F2R (beta:=beta) q - _root_.F2R (beta:=beta) p) →
    ∀ r : FloatSpec.Core.Defs.FlocqFloat beta,
      Fcanonic (beta:=beta) beta b r →
      2 * _root_.F2R (beta:=beta) p < _root_.F2R (beta:=beta) r →
      _root_.F2R (beta:=beta) r ≤ _root_.F2R (beta:=beta) q →
      ∃ rp : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) b rp ∧
          _root_.F2R (beta:=beta) rp =
            _root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) p := by
  intro hβ hprecision hvnum hp_pos hq_gt_2p _hpCan hqCan hqmp r hrCan hr_gt_2p hr_le_q
  let P : FloatSpec.Core.Defs.FlocqFloat beta → Prop :=
    fun x =>
      2 * _root_.F2R (beta:=beta) p < _root_.F2R (beta:=beta) x →
        ∃ xp : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded (beta:=beta) b xp ∧
            _root_.F2R (beta:=beta) xp =
              _root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p
  have hq_nonneg : 0 ≤ _root_.F2R (beta:=beta) q := by nlinarith
  have hqP : P q := by
    intro _hq_gt
    exact hqmp
  have hInd := FinductNeg (beta:=beta) b beta precision rfl hβ hprecision hvnum
    P q hq_nonneg hqCan hqP
    (by
      intro x hxCan hxPos hxLe hxP hxPredHard
      exact hStep x hxCan hxPos hxLe hxP hxPredHard)
  have hr_nonneg : 0 ≤ _root_.F2R (beta:=beta) r := by nlinarith
  exact hInd r hrCan hr_nonneg hr_le_q hr_gt_2p

/-- Boundary subcase for the predecessor step in Coq `ExactMinusIntervalAux`.

When the predecessor has moved back into the Sterbenz range
`p <= FPred r <= 2*p`, exactness of `FPred r - p` is immediate.  The
remaining predecessor-step work is therefore the strictly-above-boundary case.
-/
theorem ExactMinusIntervalAux_pred_sterbenz_case {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (p r : FloatSpec.Core.Defs.FlocqFloat beta) :
    1 < beta →
    Fbounded (beta:=beta) b p →
    Fbounded (beta:=beta) b
      (FPred (beta:=beta) b beta precision r) →
    0 ≤ _root_.F2R (beta:=beta) p →
    _root_.F2R (beta:=beta) p ≤
      _root_.F2R (beta:=beta) (FPred (beta:=beta) b beta precision r) →
    _root_.F2R (beta:=beta) (FPred (beta:=beta) b beta precision r) ≤
      2 * _root_.F2R (beta:=beta) p →
    ∃ rp : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) b rp ∧
        _root_.F2R (beta:=beta) rp =
          _root_.F2R (beta:=beta)
              (FPred (beta:=beta) b beta precision r) -
            _root_.F2R (beta:=beta) p := by
  intro hβ hpBound hpredBound hpNonneg hpLePred hpredLe
  have hTrip := ExactMinusInterval_sterbenz_case (beta:=beta) b p
    (FPred (beta:=beta) b beta precision r)
  simpa [wp, PostCond.noThrow, pure,
    ExactMinusInterval_sterbenz_case_check, Id.run, ULift.up_down, Int.cast_ofNat] using
    hTrip ⟨hβ, hpBound, hpredBound, hpNonneg, hpLePred, hpredLe⟩

-- Coq: `minusRoundRep_from_rounded_payload`
noncomputable def minusRoundRep_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p q qmp _qmmp : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `minusRoundRep_from_rounded_payload`.

If `qmp` is a rounded representative of `q - p`, with `0 <= p <= q`, then
`q - qmp` is representable by a bounded float.  The local Lean rounding-mode
payload is split across `RoundedModeP_full` (projector/monotone facts) and
`RoundedModeP_float` (min-or-max/boundedness facts), so both packages are
carried explicitly; the remaining radix/precision hypotheses are the section
facts required by `MinEx_from_finite_box_payload`, `MaxEx_from_finite_box_payload`, and `PminPos`. -/
theorem minusRoundRep_from_rounded_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p q qmp _qmmp : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision ∧ 1 < b.vNum ∧
        -b.dExp ≤
          (boundR (beta:=beta) radix
            ((1 / 2 : ℝ) * _root_.F2R (beta:=beta) q)).Fexp ∧
        RoundedModeP_full (beta:=beta) b P ∧
        RoundedModeP_float (beta:=beta) b radix P ∧
        0 ≤ _root_.F2R (beta:=beta) p ∧
        _root_.F2R (beta:=beta) p ≤ _root_.F2R (beta:=beta) q ∧
        P (_root_.F2R (beta:=beta) q - _root_.F2R (beta:=beta) p) qmp ∧
        Fbounded (beta:=beta) b p ∧ Fbounded (beta:=beta) b q⌝⦄
    (pure (minusRoundRep_check (beta:=beta) b radix precision P p q qmp _qmmp) :
      Id Unit)
    ⦃⇓_ => ⌜∃ r : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) b r ∧
        _root_.F2R (beta:=beta) r =
          _root_.F2R (beta:=beta) q - _root_.F2R (beta:=beta) qmp⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta_radix, hradix, hprecision, hvNum, hvNum_gt, hBoundExp,
      hRoundedFull, hRoundedFloat, hp_nonneg, hp_le_q, hPqmp, hp, hq⟩
  simp only [wp, PostCond.noThrow, pure, minusRoundRep_check, Id.run]
  have hbeta_gt : 1 < beta := by omega
  have hRoundedFullPkg := hRoundedFull
  rcases hRoundedFull with
    ⟨_hTotalFull, hCompatFull, _hMonoFull, _hProjFull, hProjEq⟩
  rcases hRoundedFloat with
    ⟨_hTotalFloat, _hCompatFloat, hMinOrMax, _hMonoFloat⟩
  let halfq : ℝ := (1 / 2 : ℝ) * _root_.F2R (beta:=beta) q
  by_cases hhalf_le_p : halfq ≤ _root_.F2R (beta:=beta) p
  · refine ⟨p, hp, ?_⟩
    let rep := FloatSpec.Calc.Operations.Fminus (beta:=beta) q p
    have hhalf_p_le_q :
        (1 / 2 : ℝ) * _root_.F2R (beta:=beta) p ≤
          _root_.F2R (beta:=beta) q := by
      have hhalf_le_p : (1 / 2 : ℝ) * _root_.F2R (beta:=beta) p ≤
          _root_.F2R (beta:=beta) p := by
        calc
          _ ≤ 1 * _root_.F2R (beta:=beta) p :=
            mul_le_mul_of_nonneg_right (by norm_num) hp_nonneg
          _ = _root_.F2R (beta:=beta) p := one_mul _
      exact le_trans hhalf_le_p hp_le_q
    have hq_le_twop :
        _root_.F2R (beta:=beta) q ≤
          2 * _root_.F2R (beta:=beta) p := by
      change (1 / 2 : ℝ) * _root_.F2R (beta:=beta) q ≤
        _root_.F2R (beta:=beta) p at hhalf_le_p
      calc
        _root_.F2R (beta:=beta) q =
            2 * ((1 / 2 : ℝ) * _root_.F2R (beta:=beta) q) := by ring
        _ ≤ 2 * _root_.F2R (beta:=beta) p :=
          mul_le_mul_of_nonneg_left hhalf_le_p (by norm_num)
    have hrepBounded : Fbounded (beta:=beta) b rep := by
      have hTrip := Sterbenz (beta:=beta) b q p
      simpa [wp, PostCond.noThrow, pure, Sterbenz_check, Fminus, rep, Id.run,
        ULift.up_down] using
        hTrip ⟨hbeta_gt, hq, hp, hhalf_p_le_q, hq_le_twop⟩
    have hrepVal :
        _root_.F2R (beta:=beta) rep =
          _root_.F2R (beta:=beta) q - _root_.F2R (beta:=beta) p := by
      have hTrip := Fminus_correct (beta:=beta) q p
      simpa [wp, PostCond.noThrow, pure, Fminus_correct_check, Fminus, rep, Id.run,
        ULift.up_down] using hTrip hbeta_gt
    have hPRep : P (_root_.F2R (beta:=beta) rep) qmp :=
      hCompatFull
        (_root_.F2R (beta:=beta) q - _root_.F2R (beta:=beta) p)
        (_root_.F2R (beta:=beta) rep) qmp qmp hPqmp hrepVal.symm rfl
    have hrep_eq_qmp :
        _root_.F2R (beta:=beta) rep = _root_.F2R (beta:=beta) qmp :=
      hProjEq rep qmp hrepBounded hPRep
    rw [← hrep_eq_qmp, hrepVal]
    ring
  · have hp_lt_half : _root_.F2R (beta:=beta) p < halfq :=
      lt_of_not_ge hhalf_le_p
    have hq_nonneg : 0 ≤ _root_.F2R (beta:=beta) q := le_trans hp_nonneg hp_le_q
    have hqmpBounded : Fbounded (beta:=beta) b qmp := by
      rcases hMinOrMax
          (_root_.F2R (beta:=beta) q - _root_.F2R (beta:=beta) p) qmp hPqmp with
        hMin | hMax
      · exact hMin.1
      · exact hMax.1
    have hMinExists := MinEx_from_finite_box_payload (beta:=beta) b radix halfq
    have hMinPayload :
        ∃ min : FloatSpec.Core.Defs.FlocqFloat beta,
          isMin (beta:=beta) b radix halfq min := by
      simpa [wp, PostCond.noThrow, pure, MinEx_check, halfq] using
        hMinExists ⟨hbeta_radix, hradix, hvNum_gt, hBoundExp⟩
    rcases hMinPayload with ⟨min, hMin⟩
    have hMaxExists := MaxEx_from_finite_box_payload (beta:=beta) b radix halfq
    have hMaxPayload :
        ∃ max : FloatSpec.Core.Defs.FlocqFloat beta,
          isMax (beta:=beta) b radix halfq max := by
      simpa [wp, PostCond.noThrow, pure, MaxEx_check, halfq] using
        hMaxExists ⟨hbeta_radix, hradix, hvNum_gt, hBoundExp⟩
    rcases hMaxPayload with ⟨max, hMax⟩
    rcases hMin with ⟨hminBounded, hmin_le_half, hminGreatest⟩
    rcases hMax with ⟨hmaxBounded, hhalf_le_max, hmaxLeast⟩
    have hMin' : isMin (beta:=beta) b radix halfq min :=
      ⟨hminBounded, hmin_le_half, hminGreatest⟩
    have hMax' : isMax (beta:=beta) b radix halfq max :=
      ⟨hmaxBounded, hhalf_le_max, hmaxLeast⟩
    have hD₂ :
        ∃ d₂ : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded (beta:=beta) b d₂ ∧
            _root_.F2R (beta:=beta) d₂ =
              _root_.F2R (beta:=beta) q - _root_.F2R (beta:=beta) min := by
      have hTrip := PminPos (beta:=beta) b radix precision q min
      simpa [wp, PostCond.noThrow, pure, PminPos_check, halfq] using
        hTrip ⟨hbeta_radix, hradix, hprecision, hvNum, hq_nonneg, hq, hMin'⟩
    have hmax_le_q : _root_.F2R (beta:=beta) max ≤ _root_.F2R (beta:=beta) q := by
      have hhalf_le_q : halfq ≤ _root_.F2R (beta:=beta) q := by
        change (1 / 2 : ℝ) * _root_.F2R (beta:=beta) q ≤
          _root_.F2R (beta:=beta) q
        calc
          _ ≤ 1 * _root_.F2R (beta:=beta) q :=
            mul_le_mul_of_nonneg_right (by norm_num) hq_nonneg
          _ = _root_.F2R (beta:=beta) q := one_mul _
      exact hmaxLeast q hq hhalf_le_q
    have hD₁ :
        ∃ d₁ : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded (beta:=beta) b d₁ ∧
            _root_.F2R (beta:=beta) d₁ =
              _root_.F2R (beta:=beta) q - _root_.F2R (beta:=beta) max := by
      refine ⟨FloatSpec.Calc.Operations.Fminus (beta:=beta) q max, ?_, ?_⟩
      · have hhalf_max_le_q :
            (1 / 2 : ℝ) * _root_.F2R (beta:=beta) max ≤
              _root_.F2R (beta:=beta) q := by
          have hhalf_nonneg : 0 ≤ halfq := by
            change 0 ≤ (1 / 2 : ℝ) * _root_.F2R (beta:=beta) q
            exact mul_nonneg (by norm_num) hq_nonneg
          have hmax_nonneg : 0 ≤ _root_.F2R (beta:=beta) max :=
            le_trans hhalf_nonneg hhalf_le_max
          calc
            _ ≤ 1 * _root_.F2R (beta:=beta) max :=
              mul_le_mul_of_nonneg_right (by norm_num) hmax_nonneg
            _ = _root_.F2R (beta:=beta) max := one_mul _
            _ ≤ _root_.F2R (beta:=beta) q := hmax_le_q
        have hq_le_twomax :
            _root_.F2R (beta:=beta) q ≤
              2 * _root_.F2R (beta:=beta) max := by
          change (1 / 2 : ℝ) * _root_.F2R (beta:=beta) q ≤
            _root_.F2R (beta:=beta) max at hhalf_le_max
          calc
            _root_.F2R (beta:=beta) q =
                2 * ((1 / 2 : ℝ) * _root_.F2R (beta:=beta) q) := by ring
            _ ≤ 2 * _root_.F2R (beta:=beta) max :=
              mul_le_mul_of_nonneg_left hhalf_le_max (by norm_num)
        have hTrip := Sterbenz (beta:=beta) b q max
        simpa [wp, PostCond.noThrow, pure, Sterbenz_check, Fminus, Id.run,
          ULift.up_down] using
          hTrip ⟨hbeta_gt, hq, hmaxBounded, hhalf_max_le_q, hq_le_twomax⟩
      · have hTrip := Fminus_correct (beta:=beta) q max
        simpa [wp, PostCond.noThrow, pure, Fminus_correct_check, Fminus, Id.run,
          ULift.up_down] using hTrip hbeta_gt
    have hBetween :
        _root_.F2R (beta:=beta) q =
          _root_.F2R (beta:=beta) min + _root_.F2R (beta:=beta) max := by
      have hTrip := div2IsBetween_from_extrema (beta:=beta) b radix q min max
      simpa [wp, PostCond.noThrow, pure, div2IsBetween_check, halfq,
        Fbounded'] using hTrip ⟨hq, hMin', hMax', hD₁, hD₂⟩
    have hp_le_min : _root_.F2R (beta:=beta) p ≤ _root_.F2R (beta:=beta) min :=
      hminGreatest p hp (le_of_lt hp_lt_half)
    have hmax_le_input :
        _root_.F2R (beta:=beta) max ≤
          _root_.F2R (beta:=beta) q - _root_.F2R (beta:=beta) p := by
      nlinarith
    have hmax_le_qmp :
        _root_.F2R (beta:=beta) max ≤ _root_.F2R (beta:=beta) qmp := by
      have hTrip := RleBoundRoundl_from_full (beta:=beta) b radix P max qmp
        (_root_.F2R (beta:=beta) q - _root_.F2R (beta:=beta) p)
      simpa [wp, PostCond.noThrow, pure, RleBoundRoundl_check, Id.run,
        ULift.up_down] using
        hTrip ⟨hRoundedFullPkg, hmaxBounded, hmax_le_input, hPqmp⟩
    have hqmp_le_q :
        _root_.F2R (beta:=beta) qmp ≤ _root_.F2R (beta:=beta) q := by
      have hinput_le_q :
          _root_.F2R (beta:=beta) q - _root_.F2R (beta:=beta) p ≤
            _root_.F2R (beta:=beta) q := by
        nlinarith
      have hTrip := RleBoundRoundr_from_full (beta:=beta) b radix P q qmp
        (_root_.F2R (beta:=beta) q - _root_.F2R (beta:=beta) p)
      simpa [wp, PostCond.noThrow, pure, RleBoundRoundr_check, Id.run,
        ULift.up_down] using
        hTrip ⟨hRoundedFullPkg, hq, hinput_le_q, hPqmp⟩
    have hhalf_le_qmp : halfq ≤ _root_.F2R (beta:=beta) qmp :=
      le_trans hhalf_le_max hmax_le_qmp
    have hhalf_qmp_le_q :
        (1 / 2 : ℝ) * _root_.F2R (beta:=beta) qmp ≤
          _root_.F2R (beta:=beta) q := by
      nlinarith
    have hq_le_two_qmp :
        _root_.F2R (beta:=beta) q ≤
          2 * _root_.F2R (beta:=beta) qmp := by
      dsimp [halfq] at hhalf_le_qmp
      nlinarith
    refine ⟨FloatSpec.Calc.Operations.Fminus (beta:=beta) q qmp, ?_, ?_⟩
    · have hTrip := Sterbenz (beta:=beta) b q qmp
      simpa [wp, PostCond.noThrow, pure, Sterbenz_check, Fminus, Id.run,
        ULift.up_down] using
        hTrip ⟨hbeta_gt, hq, hqmpBounded, hhalf_qmp_le_q, hq_le_two_qmp⟩
    · have hTrip := Fminus_correct (beta:=beta) q qmp
      simpa [wp, PostCond.noThrow, pure, Fminus_correct_check, Fminus, Id.run,
        ULift.up_down] using hTrip hbeta_gt

/-- Coq: `FnormalizeCorrect` — the normalized float represents the same real
value as the original.  The Lean port states explicitly that the `F2R` base
`beta` is the Flocq section radix. -/
theorem FnormalizeCorrect {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix⌝⦄
    (pure (FnormalizeCorrect_check (beta:=beta) radix b precision p) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta)
            (Fnormalize (beta:=beta) radix b precision p) =
            _root_.F2R (beta:=beta) p⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hradix⟩
  simp only [wp, PostCond.noThrow, pure, FnormalizeCorrect_check,
    Id.run, ULift.up_down]
  unfold Fnormalize
  by_cases hp_zero : p.Fnum = 0
  · simp [hp_zero, _root_.F2R, FloatSpec.Core.Defs.F2R]
  · simp [hp_zero]
    exact (FshiftCorrect (beta:=beta) radix
      (min (precision - Fdigit (beta:=beta) radix p)
        (Int.natAbs (b.dExp + p.Fexp))) p) ⟨hbeta, hradix⟩

/-- Coq: `FulpLe2`.

For a bounded float whose normalized representative is normal, its ulp is
bounded by the magnitude of the value times `radix^(1 - precision)`. -/
theorem FulpLe2 {beta : Int} [ValidRadix beta]
    (radix : Int) [ValidRadix radix] (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b p ∧
        Fnormal (beta:=beta) radix b
          (Fnormalize (beta:=beta) radix b precision p) ∧
        beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (FulpLe2_check (beta:=beta) radix b precision p) : Id Unit)
    ⦃⇓_ => ⌜Fulp (beta:=beta) b radix precision p ≤
        |_root_.F2R (beta:=beta) p| *
          (radix : ℝ) ^ (1 - (precision : Int))⌝⦄ := by
  intro h
  rcases h with ⟨_hpBound, hnormal, hbeta, hradix, _hprecision, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, FulpLe2_check,
    PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
    Id.run, ULift.up, ULift.down, ULift.up_down]
  subst beta
  let nf : FloatSpec.Core.Defs.FlocqFloat radix :=
    Fnormalize (beta:=radix) radix b precision p
  have hcorr : _root_.F2R (beta:=radix) nf =
      _root_.F2R (beta:=radix) p := by
    have h := FnormalizeCorrect (beta:=radix) radix b precision p
    simpa only [wp, PostCond.noThrow, pure, FnormalizeCorrect_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, nf, Int.cast_ofNat] using h ⟨rfl, hradix⟩
  have hradix_pos_int : (0 : Int) < radix := lt_trans (by decide) hradix
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradix_pos_int
  have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
  have hpow_rel_pos :
      0 < (radix : ℝ) ^ (1 - (precision : Int)) :=
    zpow_pos hradix_pos _
  have hnf_normal : Fnormal (beta:=radix) radix b nf := by
    simpa [nf] using hnormal
  have hnum_int : b.vNum ≤ |radix * nf.Fnum| := hnf_normal.2
  have hnum_real :
      (b.vNum : ℝ) ≤ ((|radix * nf.Fnum| : Int) : ℝ) := by
    exact_mod_cast hnum_int
  have hvNum_real : (b.vNum : ℝ) = (radix : ℝ) ^ (precision : Int) := by
    rw [hvNum, Zpower_nat, Int.cast_pow, ← zpow_natCast]
  have habs_mul :
      ((|radix * nf.Fnum| : Int) : ℝ) =
        (radix : ℝ) * |(nf.Fnum : ℝ)| := by
    rw [abs_mul, Int.cast_mul, Int.cast_abs, Int.cast_abs]
    rw [abs_of_pos hradix_pos]
  have hnum_bound :
      (radix : ℝ) ^ (precision : Int) ≤
        (radix : ℝ) * |(nf.Fnum : ℝ)| := by
    calc
      (radix : ℝ) ^ (precision : Int) = (b.vNum : ℝ) := hvNum_real.symm
      _ ≤ ((|radix * nf.Fnum| : Int) : ℝ) := hnum_real
      _ = (radix : ℝ) * |(nf.Fnum : ℝ)| := habs_mul
  have hpow_cancel :
      (radix : ℝ) ^ (precision : Int) *
          (radix : ℝ) ^ (1 - (precision : Int)) =
        (radix : ℝ) := by
    calc
      (radix : ℝ) ^ (precision : Int) *
          (radix : ℝ) ^ (1 - (precision : Int))
          = (radix : ℝ) ^ ((precision : Int) + (1 - (precision : Int))) := by
              rw [← zpow_add₀ hradix_ne]
      _ = (radix : ℝ) := by
              norm_num
  have hpow_cancel_nat :
      (radix : ℝ) ^ precision *
          (radix : ℝ) ^ (1 - (precision : Int)) =
        (radix : ℝ) := by
    simpa [zpow_natCast] using hpow_cancel
  have hscale :
      1 ≤ |(nf.Fnum : ℝ)| * (radix : ℝ) ^ (1 - (precision : Int)) := by
    have hmul := mul_le_mul_of_nonneg_right hnum_bound (le_of_lt hpow_rel_pos)
    have hmul' :
        (radix : ℝ) ≤
          (radix : ℝ) * (|(nf.Fnum : ℝ)| *
            (radix : ℝ) ^ (1 - (precision : Int))) := by
      simpa [hpow_cancel_nat, mul_assoc, mul_comm, mul_left_comm] using hmul
    have hunit :
        (radix : ℝ) * 1 ≤
          (radix : ℝ) *
            (|(nf.Fnum : ℝ)| *
              (radix : ℝ) ^ (1 - (precision : Int))) := by
      simpa using hmul'
    exact le_of_mul_le_mul_left hunit hradix_pos
  have hexp_pos : 0 < (radix : ℝ) ^ nf.Fexp := zpow_pos hradix_pos _
  have hF2R_abs :
      |_root_.F2R (beta:=radix) nf| =
        |(nf.Fnum : ℝ)| * (radix : ℝ) ^ nf.Fexp := by
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R]
    rw [abs_mul, abs_of_pos hexp_pos]
  have hmain :
      (radix : ℝ) ^ nf.Fexp ≤
        |_root_.F2R (beta:=radix) nf| *
          (radix : ℝ) ^ (1 - (precision : Int)) := by
    calc
      (radix : ℝ) ^ nf.Fexp = (radix : ℝ) ^ nf.Fexp * 1 := by ring
      _ ≤ (radix : ℝ) ^ nf.Fexp *
            (|(nf.Fnum : ℝ)| *
              (radix : ℝ) ^ (1 - (precision : Int))) :=
              mul_le_mul_of_nonneg_left hscale (le_of_lt hexp_pos)
      _ = |_root_.F2R (beta:=radix) nf| *
            (radix : ℝ) ^ (1 - (precision : Int)) := by
              rw [hF2R_abs]
              ring
  rw [← hcorr]
  simpa only [Fulp, nf, Int.cast_ofNat] using hmain

/-- Coq: `FnormalizeBounded` — normalization preserves boundedness. -/
theorem FnormalizeBounded {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b p ∧ Fbounded' b p ∧
        precision ≠ 0 ∧ 1 < radix ∧ b.vNum = Zpower_nat radix precision⌝⦄
    (pure (FnormalizeBounded_check (beta:=beta) radix b precision p) : Id Unit)
    ⦃⇓_ => ⌜Fbounded (beta:=beta) b
            (Fnormalize (beta:=beta) radix b precision p)⌝⦄ := by
  intro h
  rcases h with ⟨hbounded, hbounded', hprecision, hradix, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, FnormalizeBounded_check,
    Id.run, ULift.up_down]
  unfold Fnormalize
  by_cases hp_zero : p.Fnum = 0
  · simp [hp_zero, Fbounded]
    rw [hvNum]
    have hpow_pos : 0 < Zpower_nat radix precision := by
      unfold Zpower_nat
      exact pow_pos (by omega : 0 < radix) precision
    exact hpow_pos
  · simp [hp_zero]
    constructor
    · have hfd_shift :
          Fdigit (beta:=beta) radix
              (Fshift (beta:=beta) radix
                (min (precision - Fdigit (beta:=beta) radix p)
                  (Int.natAbs (b.dExp + p.Fexp))) p) ≤ precision := by
          have hshift_digit :=
            (FshiftFdigit (beta:=beta) radix
              (min (precision - Fdigit (beta:=beta) radix p)
                (Int.natAbs (b.dExp + p.Fexp))) p)
              ⟨hradix, hp_zero⟩
          simp only [wp, PostCond.noThrow, pure, FshiftFdigit_check,
            Id.run, ULift.up_down] at hshift_digit
          rw [hshift_digit]
          have hpdigit :
              Fdigit (beta:=beta) radix p ≤ precision :=
            (pGivesDigit (beta:=beta) radix b precision p)
              ⟨hbounded, hbounded', hprecision, hradix, hvNum⟩
          omega
      exact
        (digitGivesBoundedNum (beta:=beta) radix b precision
          (Fshift (beta:=beta) radix
            (min (precision - Fdigit (beta:=beta) radix p)
              (Int.natAbs (b.dExp + p.Fexp))) p))
          ⟨hprecision, hradix, hvNum, hfd_shift⟩
    · simp only [Fshift, FloatSpec.Core.Defs.FlocqFloat.Fexp]
      have hmin_le :
          (min (precision - Fdigit (beta:=beta) radix p)
            (Int.natAbs (b.dExp + p.Fexp)) : Nat) ≤
              Int.natAbs (b.dExp + p.Fexp) :=
          Nat.min_le_right _ _
      have hsum_nonneg : 0 ≤ b.dExp + p.Fexp := by
        have hexp := hbounded.2
        omega
      have hnat_eq :
          ((Int.natAbs (b.dExp + p.Fexp) : Nat) : Int) =
            b.dExp + p.Fexp :=
        Int.natAbs_of_nonneg hsum_nonneg
      have hmin_bound :
          ((min (precision - Fdigit (beta:=beta) radix p)
            (Int.natAbs (b.dExp + p.Fexp)) : Nat) : Int) ≤
            b.dExp + p.Fexp := by
        exact le_trans (by exact_mod_cast hmin_le) (le_of_eq hnat_eq)
      omega

private lemma Fdigit_pos_of_nonzero {beta : Int} [ValidRadix beta]
    (radix : Int) (p : FloatSpec.Core.Defs.FlocqFloat beta)
    (hradix : 1 < radix) (hp_nonzero : p.Fnum ≠ 0) :
    0 < Fdigit (beta:=beta) radix p := by
  unfold Fdigit
  rw [pffDigit_of_one_lt radix p.Fnum hradix]
  have hgt : 0 < FloatSpec.Core.Digits.Zdigits radix p.Fnum :=
    FloatSpec.Core.Digits.Zdigits_gt_0 radix p.Fnum hradix hp_nonzero
  have hnonneg : 0 ≤ FloatSpec.Core.Digits.Zdigits radix p.Fnum := le_of_lt hgt
  have htoNat :
      ((FloatSpec.Core.Digits.Zdigits radix p.Fnum).toNat : Int) =
        FloatSpec.Core.Digits.Zdigits radix p.Fnum :=
    Int.toNat_of_nonneg hnonneg
  have hpos_int :
      (0 : Int) < (FloatSpec.Core.Digits.Zdigits radix p.Fnum).toNat := by
    rw [htoNat]
    exact hgt
  exact_mod_cast hpos_int

private lemma Fdigit_less_abs {beta : Int} [ValidRadix beta]
    (radix : Int) (p : FloatSpec.Core.Defs.FlocqFloat beta)
    (hradix : 1 < radix) (hp_nonzero : p.Fnum ≠ 0) :
    Zpower_nat radix (Nat.pred (Fdigit (beta:=beta) radix p)) ≤ |p.Fnum| := by
  unfold Fdigit
  rw [pffDigit_of_one_lt radix p.Fnum hradix]
  have hbounds := FloatSpec.Core.Digits.Zdigits_correct_from_nonzero_payload radix p.Fnum hradix hp_nonzero
  let d := FloatSpec.Core.Digits.Zdigits radix p.Fnum
  have hgt : 0 < d := by
    simpa [d] using FloatSpec.Core.Digits.Zdigits_gt_0 radix p.Fnum hradix hp_nonzero
  have hpred_cast : ((Nat.pred d.toNat : Nat) : Int) = d - 1 := by
    have hdto : (d.toNat : Int) = d := Int.toNat_of_nonneg (le_of_lt hgt)
    cases hd : d.toNat with
    | zero =>
        rw [hd] at hdto
        omega
    | succ k =>
        simp only [Nat.pred_succ]
        rw [hd] at hdto
        omega
  have hpred :
      (d - 1).natAbs = Nat.pred d.toNat := by
    apply Nat.cast_injective (R := Int)
    rw [Int.natAbs_of_nonneg (by omega : 0 ≤ d - 1)]
    exact hpred_cast.symm
  simpa [Zpower_nat, d, hpred] using hbounds.1

private lemma abs_lt_Fdigit_pow {beta : Int} [ValidRadix beta]
    (radix : Int) (p : FloatSpec.Core.Defs.FlocqFloat beta)
    (hradix : 1 < radix) :
    |p.Fnum| < Zpower_nat radix (Fdigit (beta:=beta) radix p) := by
  unfold Fdigit
  rw [pffDigit_of_one_lt radix p.Fnum hradix]
  by_cases hp_zero : p.Fnum = 0
  · rw [hp_zero]
    simp [Zpower_nat, FloatSpec.Core.Digits.Zdigits]
  · have hbounds := FloatSpec.Core.Digits.Zdigits_correct_from_nonzero_payload radix p.Fnum hradix hp_zero
    have hnonneg : 0 ≤ FloatSpec.Core.Digits.Zdigits radix p.Fnum :=
      FloatSpec.Core.Digits.Zdigits_ge_0 radix p.Fnum trivial
    have hnat :
        (FloatSpec.Core.Digits.Zdigits radix p.Fnum).natAbs =
          (FloatSpec.Core.Digits.Zdigits radix p.Fnum).toNat := by
      apply Nat.cast_injective (R := Int)
      rw [Int.natAbs_of_nonneg hnonneg, Int.toNat_of_nonneg hnonneg]
    simpa [Zpower_nat, hnat] using hbounds.2

/-- Coq: `FnormalizeCanonic` — normalization produces a canonical float.

The Lean statement makes the usual Pff section hypotheses explicit, matching
the already restored `FnormalizeBounded` theorem. -/
theorem FnormalizeCanonic {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b p ∧ Fbounded' b p ∧
        precision ≠ 0 ∧ 1 < radix ∧ b.vNum = Zpower_nat radix precision⌝⦄
    (pure (FnormalizeCanonic_check (beta:=beta) radix b precision p) : Id Unit)
    ⦃⇓_ => ⌜Fcanonic (beta:=beta) radix b
        (Fnormalize (beta:=beta) radix b precision p)⌝⦄ := by
  intro h
  rcases h with ⟨hbounded, hbounded', hprecision, hradix, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, FnormalizeCanonic_check,
    Id.run, ULift.up_down]
  have hnorm_bounded :
      Fbounded (beta:=beta) b
        (Fnormalize (beta:=beta) radix b precision p) := by
    have hfb := FnormalizeBounded (beta:=beta) radix b precision p
    simpa only [wp, PostCond.noThrow, pure, FnormalizeBounded_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
        hfb ⟨hbounded, hbounded', hprecision, hradix, hvNum⟩
  unfold Fnormalize
  by_cases hp_zero : p.Fnum = 0
  · simp [hp_zero, Fcanonic, Fsubnormal, Fbounded]
    rw [hvNum]
    have hpow_pos : 0 < Zpower_nat radix precision := by
      unfold Zpower_nat
      exact pow_pos (by omega : 0 < radix) precision
    exact Or.inr hpow_pos
  · simp [hp_zero] at hnorm_bounded ⊢
    set d : Nat := Fdigit (beta:=beta) radix p with hd
    set a : Nat := precision - d with ha
    set bshift : Nat := Int.natAbs (b.dExp + p.Fexp) with hbshift
    set n : Nat := min a bshift with hn
    have hshift_fnum :
        (Fshift (beta:=beta) radix n p).Fnum = p.Fnum * radix ^ n := by
      simp [Fshift]
    have hshift_exp :
        (Fshift (beta:=beta) radix n p).Fexp = p.Fexp - (n : Int) := by
      simp [Fshift]
    have hsum_nonneg : 0 ≤ b.dExp + p.Fexp := by
      have hexp := hbounded.2
      omega
    have hbshift_int : ((bshift : Nat) : Int) = b.dExp + p.Fexp := by
      simpa [bshift, hbshift] using Int.natAbs_of_nonneg hsum_nonneg
    by_cases hle : a ≤ bshift
    · left
      have hn_eq_a : n = a := by
        simp [n, hn, Nat.min_eq_left hle]
      have hd_pos : 0 < d := by
        simpa [d, hd] using
          Fdigit_pos_of_nonzero (beta:=beta) radix p hradix hp_zero
      have hdigit_less :
          Zpower_nat radix (Nat.pred d) ≤ |p.Fnum| := by
        simpa [d, hd] using
          Fdigit_less_abs (beta:=beta) radix p hradix hp_zero
      have hnorm_branch :
          Fbounded (beta:=beta) b
            (Fshift (beta:=beta) radix n p) := by
        have hmin_eq_n :
            min (precision - Fdigit (beta:=beta) radix p)
              (Int.natAbs (b.dExp + p.Fexp)) = n := by
          rw [hn_eq_a, ← hd, ← ha, ← hbshift]
          exact Nat.min_eq_left hle
        simpa [Fnormalize, hp_zero, hmin_eq_n] using hnorm_bounded
      refine ⟨hnorm_branch, ?_⟩
      rw [hvNum, hshift_fnum]
      have hradix_pos : 0 < radix := by omega
      have hradix_nonneg : 0 ≤ radix := le_of_lt hradix_pos
      have hpow_nonneg : 0 ≤ radix ^ n := pow_nonneg hradix_nonneg n
      have habs_eq :
          |radix * (p.Fnum * radix ^ n)| =
            radix * (|p.Fnum| * radix ^ n) := by
        rw [abs_mul, abs_mul, abs_of_nonneg hradix_nonneg,
          abs_of_nonneg hpow_nonneg]
      rw [habs_eq]
      have hmul_le :
          radix * (Zpower_nat radix (Nat.pred d) * radix ^ n) ≤
            radix * (|p.Fnum| * radix ^ n) := by
        apply mul_le_mul_of_nonneg_left
        · apply mul_le_mul_of_nonneg_right
          · exact hdigit_less
          · exact hpow_nonneg
        · exact hradix_nonneg
      have hpow_eq :
          radix * (Zpower_nat radix (Nat.pred d) * radix ^ n) =
            Zpower_nat radix precision := by
        have hd_le_precision : d ≤ precision :=
          (pGivesDigit (beta:=beta) radix b precision p)
            ⟨hbounded, hbounded', hprecision, hradix, hvNum⟩
        have hd_pred_add : Nat.pred d + 1 = d := Nat.succ_pred_eq_of_pos hd_pos
        have hn_nat : n = precision - d := by
          simp [hn_eq_a, a, ha]
        have hsum : Nat.pred d + 1 + (precision - d) = precision := by
          omega
        calc
          radix * (Zpower_nat radix (Nat.pred d) * radix ^ n)
              = radix ^ (Nat.pred d + 1 + n) := by
                  simp [Zpower_nat, pow_add, mul_assoc, mul_comm, mul_left_comm]
          _ = radix ^ precision := by
                  rw [hn_nat, hsum]
          _ = Zpower_nat radix precision := by
                  rfl
      rw [← hpow_eq]
      exact hmul_le
    · right
      have hb_lt_a : bshift < a := Nat.lt_of_not_ge hle
      have hn_eq_b : n = bshift := by
        simp [n, hn, Nat.min_eq_right (le_of_lt hb_lt_a)]
      have hnorm_branch :
          Fbounded (beta:=beta) b
            (Fshift (beta:=beta) radix n p) := by
        have hmin_eq_n :
            min (precision - Fdigit (beta:=beta) radix p)
              (Int.natAbs (b.dExp + p.Fexp)) = n := by
          rw [hn_eq_b, ← hd, ← ha, ← hbshift]
          exact Nat.min_eq_right (le_of_lt hb_lt_a)
        simpa [Fnormalize, hp_zero, hmin_eq_n] using hnorm_bounded
      refine ⟨hnorm_branch, ?_, ?_⟩
      · rw [hshift_exp, hn_eq_b]
        omega
      · have hdigit_more : |p.Fnum| < Zpower_nat radix d := by
          simpa [d, hd] using
            abs_lt_Fdigit_pow (beta:=beta) radix p hradix
        rw [hvNum, hshift_fnum]
        have hradix_pos : 0 < radix := by omega
        have hradix_nonneg : 0 ≤ radix := le_of_lt hradix_pos
        have hpow_nonneg : 0 ≤ radix ^ n := pow_nonneg hradix_nonneg n
        have habs_eq :
            |radix * (p.Fnum * radix ^ n)| =
              radix * (|p.Fnum| * radix ^ n) := by
          rw [abs_mul, abs_mul, abs_of_nonneg hradix_nonneg,
            abs_of_nonneg hpow_nonneg]
        rw [habs_eq]
        have hmul_lt :
            radix * (|p.Fnum| * radix ^ n) <
              radix * (Zpower_nat radix d * radix ^ n) := by
          apply mul_lt_mul_of_pos_left
          · apply mul_lt_mul_of_pos_right
            · exact hdigit_more
            · exact pow_pos hradix_pos n
          · exact hradix_pos
        have hpow_le :
            radix * (Zpower_nat radix d * radix ^ n) ≤
              Zpower_nat radix precision := by
          have hd_plus_n_lt : d + n < precision := by
            have hna : n < precision - d := by
              simpa [n, hn, a, ha, hn_eq_b] using hb_lt_a
            have hd_le_precision : d ≤ precision :=
              (pGivesDigit (beta:=beta) radix b precision p)
                ⟨hbounded, hbounded', hprecision, hradix, hvNum⟩
            omega
          have hpow_exp_le : 1 + (d + n) ≤ precision := by omega
          have hpow_eq :
              radix * (Zpower_nat radix d * radix ^ n) =
                Zpower_nat radix (1 + (d + n)) := by
            simp [Zpower_nat, pow_add]
          rw [hpow_eq]
          exact pow_le_pow_right₀ (by omega : 1 ≤ radix) hpow_exp_le
        exact lt_of_lt_of_le hmul_lt hpow_le

/-- Coq: `FNoddEq` — normalized odd parity depends only on the real value of
two bounded floats. -/
theorem FNoddEq {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat)
    (f1 f2 : FloatSpec.Core.Defs.FlocqFloat beta)
    (hβ : 1 < beta) (hprecision : 1 < precision)
    (hvnum : b.vNum = Zpower_nat beta precision) :
    ⦃⌜Fbounded (beta:=beta) b f1 ∧ Fbounded (beta:=beta) b f2 ∧
        _root_.F2R f1 = _root_.F2R f2 ∧
        FNodd (beta:=beta) b radix precision f1⌝⦄
    (pure (FNoddEq_check (beta:=beta) b radix precision f1 f2) : Id Unit)
    ⦃⇓_ => ⌜FNodd (beta:=beta) b radix precision f2⌝⦄ := by
  intro ⟨hbounded1, hbounded2, hF2Req, hodd1⟩
  simp only [wp, PostCond.noThrow, pure, FNoddEq_check, ULift.down_up]
  let nf1 := Fnormalize (beta:=beta) beta b precision f1
  let nf2 := Fnormalize (beta:=beta) beta b precision f2
  have hcan1 : Fcanonic (beta:=beta) beta b nf1 := by
    have h := FnormalizeCanonic (beta:=beta) beta b precision f1
    simpa only [wp, PostCond.noThrow, pure, FnormalizeCanonic_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Fbounded', nf1, Int.cast_ofNat] using
      h ⟨hbounded1, hbounded1, by omega, hβ, hvnum⟩
  have hcan2 : Fcanonic (beta:=beta) beta b nf2 := by
    have h := FnormalizeCanonic (beta:=beta) beta b precision f2
    simpa only [wp, PostCond.noThrow, pure, FnormalizeCanonic_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Fbounded', nf2, Int.cast_ofNat] using
      h ⟨hbounded2, hbounded2, by omega, hβ, hvnum⟩
  have hvalue1 : _root_.F2R (beta:=beta) nf1 = _root_.F2R (beta:=beta) f1 := by
    have h := FnormalizeCorrect (beta:=beta) beta b precision f1
    simpa only [wp, PostCond.noThrow, pure, FnormalizeCorrect_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, nf1, Int.cast_ofNat] using h ⟨rfl, hβ⟩
  have hvalue2 : _root_.F2R (beta:=beta) nf2 = _root_.F2R (beta:=beta) f2 := by
    have h := FnormalizeCorrect (beta:=beta) beta b precision f2
    simpa only [wp, PostCond.noThrow, pure, FnormalizeCorrect_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, nf2, Int.cast_ofNat] using h ⟨rfl, hβ⟩
  have heq : nf1 = nf2 := by
    have h := FcanonicUnique (beta:=beta) beta b nf1 nf2 hβ rfl
    simp only [wp, PostCond.noThrow, pure, FcanonicUnique_check,
      ULift.down_up] at h
    apply h
    refine ⟨?_, ?_, ?_⟩
    · simpa [Fcanonic'] using hcan1
    · simpa [Fcanonic'] using hcan2
    · rw [hvalue1, hvalue2]
      exact hF2Req
  have hresult : FNodd (beta:=beta) b radix precision f2 := by
    unfold FNodd
    change Fodd nf2
    rw [← heq]
    exact hodd1
  exact hresult

/-- Coq: `FNevenEq` — normalized even parity depends only on the real value
of two bounded floats. -/
theorem FNevenEq {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat)
    (f1 f2 : FloatSpec.Core.Defs.FlocqFloat beta)
    (hβ : 1 < beta) (hprecision : 1 < precision)
    (hvnum : b.vNum = Zpower_nat beta precision) :
    ⦃⌜Fbounded (beta:=beta) b f1 ∧ Fbounded (beta:=beta) b f2 ∧
        _root_.F2R f1 = _root_.F2R f2 ∧
        FNeven (beta:=beta) b radix precision f1⌝⦄
    (pure (FNevenEq_check (beta:=beta) b radix precision f1 f2) : Id Unit)
    ⦃⇓_ => ⌜FNeven (beta:=beta) b radix precision f2⌝⦄ := by
  intro ⟨hbounded1, hbounded2, hF2Req, heven1⟩
  simp only [wp, PostCond.noThrow, pure, FNevenEq_check,
    PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
    Id.run, ULift.up, ULift.down, ULift.up_down, ULift.down_up]
  let nf1 := Fnormalize (beta:=beta) beta b precision f1
  let nf2 := Fnormalize (beta:=beta) beta b precision f2
  have hcan1 : Fcanonic (beta:=beta) beta b nf1 := by
    have h := FnormalizeCanonic (beta:=beta) beta b precision f1
    simpa only [wp, PostCond.noThrow, pure, FnormalizeCanonic_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Fbounded', nf1, Int.cast_ofNat] using
      h ⟨hbounded1, hbounded1, by omega, hβ, hvnum⟩
  have hcan2 : Fcanonic (beta:=beta) beta b nf2 := by
    have h := FnormalizeCanonic (beta:=beta) beta b precision f2
    simpa only [wp, PostCond.noThrow, pure, FnormalizeCanonic_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Fbounded', nf2, Int.cast_ofNat] using
      h ⟨hbounded2, hbounded2, by omega, hβ, hvnum⟩
  have hvalue1 : _root_.F2R (beta:=beta) nf1 = _root_.F2R (beta:=beta) f1 := by
    have h := FnormalizeCorrect (beta:=beta) beta b precision f1
    simpa only [wp, PostCond.noThrow, pure, FnormalizeCorrect_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, nf1, Int.cast_ofNat] using h ⟨rfl, hβ⟩
  have hvalue2 : _root_.F2R (beta:=beta) nf2 = _root_.F2R (beta:=beta) f2 := by
    have h := FnormalizeCorrect (beta:=beta) beta b precision f2
    simpa only [wp, PostCond.noThrow, pure, FnormalizeCorrect_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, nf2, Int.cast_ofNat] using h ⟨rfl, hβ⟩
  have heq : nf1 = nf2 := by
    have h := FcanonicUnique (beta:=beta) beta b nf1 nf2 hβ rfl
    simp only [wp, PostCond.noThrow, pure, FcanonicUnique_check,
      ULift.down_up] at h
    apply h
    refine ⟨?_, ?_, ?_⟩
    · simpa [Fcanonic'] using hcan1
    · simpa [Fcanonic'] using hcan2
    · rw [hvalue1, hvalue2]
      exact hF2Req
  have hresult : FNeven (beta:=beta) b radix precision f2 := by
    unfold FNeven
    change Feven nf2
    rw [← heq]
    exact heven1
  exact hresult

/-- Coq `EvenClosestCompatible`: represented-value compatibility, including
transport of the normalized even tie-breaker. -/
theorem EvenClosestCompatible {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < precision ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (EvenClosestCompatible_check (beta:=beta) b (radix : ℝ) precision) :
      Id Unit)
    ⦃⇓_ => ⌜CompatibleP (beta:=beta) b
      (EvenClosest (beta:=beta) b (radix : ℝ) precision)⌝⦄ := by
  intro ⟨hbeta, hradix, hprecision, hvNum⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure, EvenClosestCompatible_check,
    Id.run, ULift.up_down]
  intro r₁ r₂ p q hp hr hpq hqBound
  rcases hp with ⟨hpClosest, hpEven | hpOnly⟩
  · constructor
    · constructor
      · exact hqBound
      · intro g hg
        rw [← hr, ← hpq]
        exact hpClosest.2 g hg
    · left
      exact FNevenEq b (radix : ℝ) precision p q hradix hprecision hvNum
        ⟨hpClosest.1, hqBound, hpq, hpEven⟩
  · constructor
    · constructor
      · exact hqBound
      · intro g hg
        rw [← hr, ← hpq]
        exact hpClosest.2 g hg
    · right
      intro q₀ hq₀
      have hq₀' : Closest (beta:=radix) b (radix : ℝ) r₁ q₀ := by
        simpa [hr] using hq₀
      exact (hpOnly q₀ hq₀').trans hpq

/-- Coq: `FulpLeGeneral`.

The Axpy estimates need a bound that also covers the subnormal normalized
representative. In the normal case this is `FulpLe2`; in the subnormal case
`Fulp` is the boundary exponent `radix^(-dExp)`, so the additional nonnegative
term closes the estimate. -/
theorem FulpLeGeneral {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b p ∧ Fbounded' (beta:=beta) b p ∧
        beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (FulpLeGeneral_check (beta:=beta) radix b precision p) : Id Unit)
    ⦃⇓_ => ⌜Fulp (beta:=beta) b radix precision p ≤
        |_root_.F2R (beta:=beta) p| *
          (radix : ℝ) ^ (1 - (precision : Int)) +
        (radix : ℝ) ^ (-b.dExp)⌝⦄ := by
  intro h
  rcases h with ⟨hpBound, hpBound', hbeta, hradix, hprecision, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, FulpLeGeneral_check,
    PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
    Id.run, ULift.up, ULift.down, ULift.up_down]
  subst beta
  have hradix_pos_int : (0 : Int) < radix := lt_trans (by decide) hradix
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradix_pos_int
  have hscale_nonneg :
      0 ≤ |(_root_.F2R (beta:=radix) p)| *
        (radix : ℝ) ^ (1 - (precision : Int)) := by
    exact mul_nonneg (abs_nonneg _)
      (le_of_lt (zpow_pos hradix_pos _))
  have hcan :
      Fcanonic (beta:=radix) radix b
        (Fnormalize (beta:=radix) radix b precision p) := by
    have h := FnormalizeCanonic (beta:=radix) radix b precision p
    simpa only [wp, PostCond.noThrow, pure, FnormalizeCanonic_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
      h ⟨hpBound, hpBound', hprecision, hradix, hvNum⟩
  rcases hcan with hnormal | hsubnormal
  · have hle :
        Fulp (beta:=radix) b radix precision p ≤
          |(_root_.F2R (beta:=radix) p)| *
            (radix : ℝ) ^ (1 - (precision : Int)) := by
      have h := FulpLe2 (beta:=radix) radix b precision p
      simpa only [wp, PostCond.noThrow, pure, FulpLe2_check,
        PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
        Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
        h ⟨hpBound, hnormal, rfl, hradix, hprecision, hvNum⟩
    exact le_trans hle (le_add_of_nonneg_right (le_of_lt (zpow_pos hradix_pos _)))
  · have hnf_exp :
        (Fnormalize (beta:=radix) radix b precision p).Fexp = -b.dExp :=
      hsubnormal.2.1
    have hboundary :
        Fulp (beta:=radix) b radix precision p =
          (radix : ℝ) ^ (-b.dExp) := by
      simp [Fulp, hnf_exp]
    rw [hboundary]
    exact le_add_of_nonneg_left hscale_nonneg

/-- Coq `ExactMinusInterval`, factored at the remaining canonical interval.

The upstream theorem first normalizes `p`, `q`, and `r`, then delegates to
`ExactMinusIntervalAux1`.  This helper packages that normalization step.  The
only remaining premise is the canonical interval payload itself, so downstream
proofs do not need to repeat the normalization bookkeeping while the hard
predecessor induction is still being ported. -/
theorem ExactMinusInterval_from_Aux1 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (hAux1 :
      ∀ p q r : FloatSpec.Core.Defs.FlocqFloat beta,
        Fcanonic (beta:=beta) beta b p →
        Fcanonic (beta:=beta) beta b q →
        Fcanonic (beta:=beta) beta b r →
        0 ≤ _root_.F2R (beta:=beta) p →
        _root_.F2R (beta:=beta) p ≤ _root_.F2R (beta:=beta) q →
        _root_.F2R (beta:=beta) p ≤ _root_.F2R (beta:=beta) r →
        _root_.F2R (beta:=beta) r ≤ _root_.F2R (beta:=beta) q →
        (∃ qmp : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded (beta:=beta) b qmp ∧
            _root_.F2R (beta:=beta) qmp =
              _root_.F2R (beta:=beta) q - _root_.F2R (beta:=beta) p) →
        ∃ rp : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded (beta:=beta) b rp ∧
            _root_.F2R (beta:=beta) rp =
              _root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) p) :
    1 < beta →
    precision ≠ 0 →
    b.vNum = Zpower_nat beta precision →
    ∀ p q r : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) b p →
      Fbounded (beta:=beta) b q →
      Fbounded (beta:=beta) b r →
      0 ≤ _root_.F2R (beta:=beta) p →
      _root_.F2R (beta:=beta) p ≤ _root_.F2R (beta:=beta) q →
      _root_.F2R (beta:=beta) p ≤ _root_.F2R (beta:=beta) r →
      _root_.F2R (beta:=beta) r ≤ _root_.F2R (beta:=beta) q →
      (∃ qmp : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) b qmp ∧
          _root_.F2R (beta:=beta) qmp =
            _root_.F2R (beta:=beta) q - _root_.F2R (beta:=beta) p) →
      ∃ rp : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) b rp ∧
          _root_.F2R (beta:=beta) rp =
            _root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) p := by
  intro hβ hprecision hvnum p q r hp hq hr hp_nonneg hp_le_q hp_le_r hr_le_q hqmp
  let np := Fnormalize (beta:=beta) beta b precision p
  let nq := Fnormalize (beta:=beta) beta b precision q
  let nr := Fnormalize (beta:=beta) beta b precision r
  have hnp :
      _root_.F2R (beta:=beta) np = _root_.F2R (beta:=beta) p := by
    have hcorr := FnormalizeCorrect (beta:=beta) beta b precision p
    simpa only [wp, PostCond.noThrow, pure, FnormalizeCorrect_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, np, Int.cast_ofNat] using hcorr ⟨rfl, hβ⟩
  have hnq :
      _root_.F2R (beta:=beta) nq = _root_.F2R (beta:=beta) q := by
    have hcorr := FnormalizeCorrect (beta:=beta) beta b precision q
    simpa only [wp, PostCond.noThrow, pure, FnormalizeCorrect_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, nq, Int.cast_ofNat] using hcorr ⟨rfl, hβ⟩
  have hnr :
      _root_.F2R (beta:=beta) nr = _root_.F2R (beta:=beta) r := by
    have hcorr := FnormalizeCorrect (beta:=beta) beta b precision r
    simpa only [wp, PostCond.noThrow, pure, FnormalizeCorrect_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, nr, Int.cast_ofNat] using hcorr ⟨rfl, hβ⟩
  have hnpCan : Fcanonic (beta:=beta) beta b np := by
    have hcan := FnormalizeCanonic (beta:=beta) beta b precision p
    simpa only [wp, PostCond.noThrow, pure, FnormalizeCanonic_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Fbounded', np, Int.cast_ofNat] using
      hcan ⟨hp, hp, hprecision, hβ, hvnum⟩
  have hnqCan : Fcanonic (beta:=beta) beta b nq := by
    have hcan := FnormalizeCanonic (beta:=beta) beta b precision q
    simpa only [wp, PostCond.noThrow, pure, FnormalizeCanonic_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Fbounded', nq, Int.cast_ofNat] using
      hcan ⟨hq, hq, hprecision, hβ, hvnum⟩
  have hnrCan : Fcanonic (beta:=beta) beta b nr := by
    have hcan := FnormalizeCanonic (beta:=beta) beta b precision r
    simpa only [wp, PostCond.noThrow, pure, FnormalizeCanonic_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Fbounded', nr, Int.cast_ofNat] using
      hcan ⟨hr, hr, hprecision, hβ, hvnum⟩
  have hqmp_norm :
      ∃ qmp : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) b qmp ∧
          _root_.F2R (beta:=beta) qmp =
            _root_.F2R (beta:=beta) nq - _root_.F2R (beta:=beta) np := by
    rcases hqmp with ⟨qmp, hqmpBounded, hqmpVal⟩
    refine ⟨qmp, hqmpBounded, ?_⟩
    rw [hnq, hnp]
    exact hqmpVal
  rcases hAux1 np nq nr hnpCan hnqCan hnrCan
      (by rw [hnp]; exact hp_nonneg)
      (by rw [hnp, hnq]; exact hp_le_q)
      (by rw [hnp, hnr]; exact hp_le_r)
      (by rw [hnr, hnq]; exact hr_le_q)
      hqmp_norm with ⟨rp, hrpBounded, hrpVal⟩
  refine ⟨rp, hrpBounded, ?_⟩
  rw [hnr, hnp] at hrpVal
  exact hrpVal

/-- Setup block from the hard predecessor step of Coq `ExactMinusIntervalAux`.

Given a bounded representative of `r - p` in the branch `2*p < r`, its
normalization is nonnegative, strictly below `r`, and lies in the same or next
lower exponent binade relative to `r`.  This packages the normalization and
`radixRangeBoundExp` part of the upstream proof. -/
theorem ExactMinusIntervalAux_pred_setup {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (p r rp : FloatSpec.Core.Defs.FlocqFloat beta) :
    1 < beta →
    (beta : ℝ) = 2 →
    precision ≠ 0 →
    b.vNum = Zpower_nat beta precision →
    0 < _root_.F2R (beta:=beta) p →
    2 * _root_.F2R (beta:=beta) p < _root_.F2R (beta:=beta) r →
    Fcanonic (beta:=beta) beta b r →
    Fbounded (beta:=beta) b rp →
    _root_.F2R (beta:=beta) rp =
      _root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) p →
    let nrp := Fnormalize (beta:=beta) beta b precision rp
    0 ≤ _root_.F2R (beta:=beta) nrp ∧
      _root_.F2R (beta:=beta) nrp < _root_.F2R (beta:=beta) r ∧
      (nrp.Fexp = r.Fexp ∨ Int.succ nrp.Fexp = r.Fexp) := by
  intro hβ hbetaTwo hprecision hvnum hp_pos hr_gt_2p hrCan hrpBound hrpVal nrp
  have hnrpVal :
      _root_.F2R (beta:=beta) nrp = _root_.F2R (beta:=beta) rp := by
    have hcorr := FnormalizeCorrect (beta:=beta) beta b precision rp
    simpa only [wp, PostCond.noThrow, pure, FnormalizeCorrect_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, nrp, Int.cast_ofNat] using hcorr ⟨rfl, hβ⟩
  have hnrpCan : Fcanonic (beta:=beta) beta b nrp := by
    have hcan := FnormalizeCanonic (beta:=beta) beta b precision rp
    simpa only [wp, PostCond.noThrow, pure, FnormalizeCanonic_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Fbounded', nrp, Int.cast_ofNat] using
      hcan ⟨hrpBound, hrpBound, hprecision, hβ, hvnum⟩
  have hnrp_nonneg : 0 ≤ _root_.F2R (beta:=beta) nrp := by
    rw [hnrpVal, hrpVal]
    nlinarith
  have hnrp_lt_r : _root_.F2R (beta:=beta) nrp <
      _root_.F2R (beta:=beta) r := by
    rw [hnrpVal, hrpVal]
    nlinarith
  have hr_lt_beta_nrp : _root_.F2R (beta:=beta) r <
      (beta : ℝ) * _root_.F2R (beta:=beta) nrp := by
    rw [hnrpVal, hrpVal, hbetaTwo]
    nlinarith
  have hexp := radixRangeBoundExp (beta:=beta) b beta precision nrp r
    rfl hβ hprecision hvnum hnrpCan hrCan hnrp_nonneg hnrp_lt_r
    hr_lt_beta_nrp
  exact ⟨hnrp_nonneg, hnrp_lt_r, hexp⟩

/-- Same-exponent arithmetic branch of Coq `ExactMinusIntervalAux`.

After `ExactMinusIntervalAux_pred_setup`, one branch has the normalized
representative of `r - p` in the same exponent binade as `r`.  If the
predecessor of `r` decreases that same exponent by one ulp, then decrementing
the representative mantissa gives a bounded representative of `FPred r - p`.
-/
theorem ExactMinusIntervalAux_pred_same_exp_case {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (p r rp : FloatSpec.Core.Defs.FlocqFloat beta) :
    let nrp := Fnormalize (beta:=beta) beta b precision rp
    1 < beta →
    Fbounded (beta:=beta) b nrp →
    0 < _root_.F2R (beta:=beta) nrp →
    nrp.Fexp = r.Fexp →
    _root_.F2R (beta:=beta)
        (FPred (beta:=beta) b beta precision r) =
      _root_.F2R (beta:=beta) r - (beta : ℝ) ^ r.Fexp →
    _root_.F2R (beta:=beta) nrp =
      _root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) p →
    ∃ pred_rp : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) b pred_rp ∧
        _root_.F2R (beta:=beta) pred_rp =
          _root_.F2R (beta:=beta)
              (FPred (beta:=beta) b beta precision r) -
            _root_.F2R (beta:=beta) p := by
  intro nrp hβ hnrpBound hnrp_pos hsame_exp hpredVal hnrpVal
  let pred_rp : FloatSpec.Core.Defs.FlocqFloat beta :=
    ⟨nrp.Fnum - 1, nrp.Fexp⟩
  refine ⟨pred_rp, ?_, ?_⟩
  · rcases hnrpBound with ⟨hnum, hexp⟩
    have hm_pos : 0 < nrp.Fnum :=
      FloatSpec.Core.Float_prop.gt_0_F2R (beta:=beta) nrp hβ hnrp_pos
    constructor
    · have hm_minus_nonneg : 0 ≤ nrp.Fnum - 1 := by omega
      have hm_abs : |nrp.Fnum| = nrp.Fnum := abs_of_pos hm_pos
      have hm_minus_abs : |nrp.Fnum - 1| = nrp.Fnum - 1 :=
        abs_of_nonneg hm_minus_nonneg
      simp only [pred_rp, Fbounded, FloatSpec.Core.Defs.FlocqFloat.Fnum]
      rw [hm_minus_abs]
      rw [hm_abs] at hnum
      omega
    · simpa [pred_rp] using hexp
  · have hpred_rp_val :
        _root_.F2R (beta:=beta) pred_rp =
          _root_.F2R (beta:=beta) nrp - (beta : ℝ) ^ nrp.Fexp := by
      simp [pred_rp, _root_.F2R, FloatSpec.Core.Defs.F2R]
      ring
    calc
      _root_.F2R (beta:=beta) pred_rp
          = _root_.F2R (beta:=beta) nrp -
              (beta : ℝ) ^ nrp.Fexp := hpred_rp_val
      _ = (_root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) p) -
              (beta : ℝ) ^ r.Fexp := by rw [hnrpVal, hsame_exp]
      _ = (_root_.F2R (beta:=beta) r - (beta : ℝ) ^ r.Fexp) -
              _root_.F2R (beta:=beta) p := by ring
      _ = _root_.F2R (beta:=beta)
              (FPred (beta:=beta) b beta precision r) -
            _root_.F2R (beta:=beta) p := by rw [← hpredVal]

/-- Same-exponent non-boundary branch of Coq `ExactMinusIntervalAux`.

This packages the upstream `FPredSimpl4` branch when the normalized
representative of `r - p` has the same exponent as `r`: the predecessor is one
ulp below `r` at that same exponent, so the generic same-exponent arithmetic
lemma applies directly. -/
theorem ExactMinusIntervalAux_pred_same_exp_non_normmin_case {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (p r rp : FloatSpec.Core.Defs.FlocqFloat beta) :
    let nrp := Fnormalize (beta:=beta) beta b precision rp
    1 < beta →
    Fbounded (beta:=beta) b nrp →
    0 < _root_.F2R (beta:=beta) nrp →
    nrp.Fexp = r.Fexp →
    r.Fnum ≠ -pPred b.vNum →
    r.Fnum ≠ nNormMin beta precision →
    _root_.F2R (beta:=beta) nrp =
      _root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) p →
    ∃ pred_rp : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) b pred_rp ∧
        _root_.F2R (beta:=beta) pred_rp =
          _root_.F2R (beta:=beta)
              (FPred (beta:=beta) b beta precision r) -
            _root_.F2R (beta:=beta) p := by
  intro nrp hβ hnrpBound hnrp_pos hsame_exp hnot_neg_pred hnot_norm hnrpVal
  have hfpredEq :
      FPred (beta:=beta) b beta precision r =
        ⟨r.Fnum - 1, r.Fexp⟩ := by
    have hTrip := FPredSimpl4 (beta:=beta) b beta precision r
    simpa only [wp, PostCond.noThrow, pure, FPredSimpl4_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
      hTrip ⟨hnot_neg_pred, hnot_norm⟩
  have hpredVal :
      _root_.F2R (beta:=beta)
          (FPred (beta:=beta) b beta precision r) =
        _root_.F2R (beta:=beta) r - (beta : ℝ) ^ r.Fexp := by
    rw [hfpredEq]
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
      FloatSpec.Core.Defs.FlocqFloat.Fnum,
      FloatSpec.Core.Defs.FlocqFloat.Fexp]
    norm_num [Int.cast_sub, Int.cast_one]
    ring
  exact ExactMinusIntervalAux_pred_same_exp_case (beta:=beta)
    b precision p r rp hβ hnrpBound hnrp_pos hsame_exp hpredVal hnrpVal

/-- Same-exponent minimum-normal branch of Coq `ExactMinusIntervalAux`.

At the minimum exponent, `FPredSimpl3` says that the predecessor of the minimal
normal mantissa stays in the same exponent and decrements the mantissa by one.
This is the remaining same-exponent boundary case before the adjacent-exponent
branches take over. -/
theorem ExactMinusIntervalAux_pred_same_exp_normmin_minexp_case {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (p r rp : FloatSpec.Core.Defs.FlocqFloat beta) :
    let nrp := Fnormalize (beta:=beta) beta b precision rp
    1 < beta →
    precision ≠ 0 →
    b.vNum = Zpower_nat beta precision →
    Fbounded (beta:=beta) b nrp →
    0 < _root_.F2R (beta:=beta) nrp →
    nrp.Fexp = r.Fexp →
    r.Fnum = nNormMin beta precision →
    r.Fexp = -b.dExp →
    _root_.F2R (beta:=beta) nrp =
      _root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) p →
    ∃ pred_rp : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) b pred_rp ∧
        _root_.F2R (beta:=beta) pred_rp =
          _root_.F2R (beta:=beta)
              (FPred (beta:=beta) b beta precision r) -
            _root_.F2R (beta:=beta) p := by
  intro nrp hβ hprecision hvnum hnrpBound hnrp_pos hsame_exp hrnum hrexp hnrpVal
  have hdisjoint : -pPred b.vNum ≠ nNormMin beta precision := by
    have hnm_pos : 0 < nNormMin beta precision := by
      unfold nNormMin
      exact pow_pos (by omega : (0 : Int) < beta) _
    have hvpos : 0 < b.vNum := by
      rw [hvnum]
      unfold Zpower_nat
      exact pow_pos (by omega : (0 : Int) < beta) _
    have hpPred_nonneg : 0 ≤ pPred b.vNum := by
      unfold pPred
      omega
    omega
  have hfpredBase :
      FPred (beta:=beta) b beta precision
          (⟨nNormMin beta precision, -b.dExp⟩ :
            FloatSpec.Core.Defs.FlocqFloat beta) =
        ⟨nNormMin beta precision - 1, -b.dExp⟩ := by
    have hTrip := FPredSimpl3 (beta:=beta) b beta precision
    simpa only [wp, PostCond.noThrow, pure, FPredSimpl3_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using hTrip hdisjoint
  have hfpredEq :
      FPred (beta:=beta) b beta precision r =
        ⟨r.Fnum - 1, r.Fexp⟩ := by
    cases r with
    | mk fnum fexp =>
        simp only [FloatSpec.Core.Defs.FlocqFloat.Fnum,
          FloatSpec.Core.Defs.FlocqFloat.Fexp] at hrnum hrexp hfpredBase ⊢
        subst fnum
        subst fexp
        simpa using hfpredBase
  have hpredVal :
      _root_.F2R (beta:=beta)
          (FPred (beta:=beta) b beta precision r) =
        _root_.F2R (beta:=beta) r - (beta : ℝ) ^ r.Fexp := by
    rw [hfpredEq]
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
      FloatSpec.Core.Defs.FlocqFloat.Fnum,
      FloatSpec.Core.Defs.FlocqFloat.Fexp]
    norm_num [Int.cast_sub, Int.cast_one]
    ring
  exact ExactMinusIntervalAux_pred_same_exp_case (beta:=beta)
    b precision p r rp hβ hnrpBound hnrp_pos hsame_exp hpredVal hnrpVal

/-- One-ulp arithmetic branch of Coq `ExactMinusIntervalAux`.

This is the common constructive step behind the same-exponent branch and the
normal-boundary adjacent-exponent branch: if `FPred r` is exactly one ulp below
`r` at the normalized representative exponent, then decrementing the
representative mantissa gives a bounded representative of `FPred r - p`. -/
theorem ExactMinusIntervalAux_pred_one_ulp_case {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (p r rp : FloatSpec.Core.Defs.FlocqFloat beta) :
    let nrp := Fnormalize (beta:=beta) beta b precision rp
    1 < beta →
    Fbounded (beta:=beta) b nrp →
    0 < _root_.F2R (beta:=beta) nrp →
    _root_.F2R (beta:=beta)
        (FPred (beta:=beta) b beta precision r) =
      _root_.F2R (beta:=beta) r - (beta : ℝ) ^ nrp.Fexp →
    _root_.F2R (beta:=beta) nrp =
      _root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) p →
    ∃ pred_rp : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) b pred_rp ∧
        _root_.F2R (beta:=beta) pred_rp =
          _root_.F2R (beta:=beta)
              (FPred (beta:=beta) b beta precision r) -
            _root_.F2R (beta:=beta) p := by
  intro nrp hβ hnrpBound hnrp_pos hpredVal hnrpVal
  let pred_rp : FloatSpec.Core.Defs.FlocqFloat beta :=
    ⟨nrp.Fnum - 1, nrp.Fexp⟩
  refine ⟨pred_rp, ?_, ?_⟩
  · rcases hnrpBound with ⟨hnum, hexp⟩
    have hm_pos : 0 < nrp.Fnum :=
      FloatSpec.Core.Float_prop.gt_0_F2R (beta:=beta) nrp hβ hnrp_pos
    constructor
    · have hm_minus_nonneg : 0 ≤ nrp.Fnum - 1 := by omega
      have hm_abs : |nrp.Fnum| = nrp.Fnum := abs_of_pos hm_pos
      have hm_minus_abs : |nrp.Fnum - 1| = nrp.Fnum - 1 :=
        abs_of_nonneg hm_minus_nonneg
      simp only [pred_rp, Fbounded, FloatSpec.Core.Defs.FlocqFloat.Fnum]
      rw [hm_minus_abs]
      rw [hm_abs] at hnum
      omega
    · simpa [pred_rp] using hexp
  · have hpred_rp_val :
        _root_.F2R (beta:=beta) pred_rp =
          _root_.F2R (beta:=beta) nrp - (beta : ℝ) ^ nrp.Fexp := by
      simp [pred_rp, _root_.F2R, FloatSpec.Core.Defs.F2R]
      ring
    calc
      _root_.F2R (beta:=beta) pred_rp
          = _root_.F2R (beta:=beta) nrp -
              (beta : ℝ) ^ nrp.Fexp := hpred_rp_val
      _ = (_root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) p) -
              (beta : ℝ) ^ nrp.Fexp := by rw [hnrpVal]
      _ = (_root_.F2R (beta:=beta) r - (beta : ℝ) ^ nrp.Fexp) -
              _root_.F2R (beta:=beta) p := by ring
      _ = _root_.F2R (beta:=beta)
              (FPred (beta:=beta) b beta precision r) -
            _root_.F2R (beta:=beta) p := by rw [← hpredVal]

/-- Normal-boundary adjacent-exponent branch of Coq `ExactMinusIntervalAux`.

This corresponds to the upstream branch using `FPredSimpl2`: when `r` has the
minimal normal mantissa away from the minimum exponent and the normalized
representative of `r - p` sits one exponent below `r`, `FPred r` is one ulp
below `r` at that representative exponent. -/
theorem ExactMinusIntervalAux_pred_adjacent_normmin_case {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (p r rp : FloatSpec.Core.Defs.FlocqFloat beta) :
    let nrp := Fnormalize (beta:=beta) beta b precision rp
    1 < beta →
    precision ≠ 0 →
    b.vNum = Zpower_nat beta precision →
    Fbounded (beta:=beta) b nrp →
    0 < _root_.F2R (beta:=beta) nrp →
    Int.succ nrp.Fexp = r.Fexp →
    r.Fnum = nNormMin beta precision →
    r.Fexp ≠ -b.dExp →
    _root_.F2R (beta:=beta) nrp =
      _root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) p →
    ∃ pred_rp : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) b pred_rp ∧
        _root_.F2R (beta:=beta) pred_rp =
          _root_.F2R (beta:=beta)
              (FPred (beta:=beta) b beta precision r) -
            _root_.F2R (beta:=beta) p := by
  intro nrp hβ hprecision hvnum hnrpBound hnrp_pos hsucc hrnum hrexp_ne hnrpVal
  have hβ_pos : (0 : ℝ) < (beta : ℝ) := by
    exact_mod_cast (by omega : (0 : Int) < beta)
  have hβ_ne : (beta : ℝ) ≠ 0 := ne_of_gt hβ_pos
  have hdisjoint : -pPred b.vNum ≠ nNormMin beta precision := by
    have hnm_pos : 0 < nNormMin beta precision := by
      unfold nNormMin
      exact pow_pos (by omega : (0 : Int) < beta) _
    have hvpos : 0 < b.vNum := by
      rw [hvnum]
      unfold Zpower_nat
      exact pow_pos (by omega : (0 : Int) < beta) _
    have hpPred_nonneg : 0 ≤ pPred b.vNum := by
      unfold pPred
      omega
    omega
  have hfpredEq :
      FPred (beta:=beta) b beta precision r =
        ⟨pPred b.vNum, r.Fexp - 1⟩ := by
    have hTrip := FPredSimpl2 (beta:=beta) b beta precision r
    simpa only [wp, PostCond.noThrow, pure, FPredSimpl2_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
      hTrip ⟨hdisjoint, hrnum, hrexp_ne⟩
  have hnorm_mul :
      (nNormMin beta precision : ℝ) * (beta : ℝ) = (b.vNum : ℝ) := by
    have hsucc_nat : precision - 1 + 1 = precision :=
      Nat.sub_one_add_one hprecision
    have hpow_succ : beta ^ precision = beta ^ (precision - 1) * beta := by
      rw [← hsucc_nat]
      exact pow_succ beta (precision - 1)
    rw [hvnum, Zpower_nat, nNormMin, hpow_succ]
    norm_num [Int.cast_mul]
  have hExpPred : r.Fexp - 1 = nrp.Fexp := by
    unfold Int.succ at hsucc
    omega
  have hpow_r :
      (beta : ℝ) ^ r.Fexp =
        (beta : ℝ) ^ nrp.Fexp * (beta : ℝ) := by
    have hr_exp : r.Fexp = nrp.Fexp + 1 := by
      unfold Int.succ at hsucc
      omega
    rw [hr_exp, zpow_add₀ hβ_ne, zpow_one]
  have hpredVal :
        _root_.F2R (beta:=beta)
          (FPred (beta:=beta) b beta precision r) =
        _root_.F2R (beta:=beta) r - (beta : ℝ) ^ nrp.Fexp := by
    rw [hfpredEq]
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
      FloatSpec.Core.Defs.FlocqFloat.Fnum,
      FloatSpec.Core.Defs.FlocqFloat.Fexp]
    rw [hrnum]
    rw [hExpPred, hpow_r]
    have hpPred : (pPred b.vNum : ℝ) = (b.vNum : ℝ) - 1 := by
      simp [pPred]
    rw [hpPred]
    calc
      ((b.vNum : ℝ) - 1) * (beta : ℝ) ^ nrp.Fexp
          =
            (nNormMin beta precision : ℝ) *
                ((beta : ℝ) ^ nrp.Fexp * (beta : ℝ)) -
              (beta : ℝ) ^ nrp.Fexp := by
              rw [← hnorm_mul]
              ring
      _ = (nNormMin beta precision : ℝ) *
              ((beta : ℝ) ^ nrp.Fexp * (beta : ℝ)) -
            (beta : ℝ) ^ nrp.Fexp := rfl
  exact ExactMinusIntervalAux_pred_one_ulp_case (beta:=beta)
    b precision p r rp hβ hnrpBound hnrp_pos hpredVal hnrpVal

/-- Adjacent-exponent arithmetic branch of Coq `ExactMinusIntervalAux`.

When `FPred r` is one `beta`-scaled ulp below `r` at the normalized
representative exponent, decrementing the representative mantissa by `beta`
gives a bounded representative of `FPred r - p`. -/
theorem ExactMinusIntervalAux_pred_beta_ulp_case {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (p r rp : FloatSpec.Core.Defs.FlocqFloat beta) :
    let nrp := Fnormalize (beta:=beta) beta b precision rp
    1 < beta →
    1 < precision →
    b.vNum = Zpower_nat beta precision →
    Fbounded (beta:=beta) b nrp →
    0 ≤ _root_.F2R (beta:=beta) nrp →
    _root_.F2R (beta:=beta)
        (FPred (beta:=beta) b beta precision r) =
      _root_.F2R (beta:=beta) r -
        (beta : ℝ) * (beta : ℝ) ^ nrp.Fexp →
    _root_.F2R (beta:=beta) nrp =
      _root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) p →
    ∃ pred_rp : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) b pred_rp ∧
        _root_.F2R (beta:=beta) pred_rp =
          _root_.F2R (beta:=beta)
              (FPred (beta:=beta) b beta precision r) -
            _root_.F2R (beta:=beta) p := by
  intro nrp hβ hprecision hvnum hnrpBound hnrp_nonneg hpredVal hnrpVal
  let pred_rp : FloatSpec.Core.Defs.FlocqFloat beta :=
    ⟨nrp.Fnum - beta, nrp.Fexp⟩
  refine ⟨pred_rp, ?_, ?_⟩
  · rcases hnrpBound with ⟨hnum, hexp⟩
    constructor
    · have hnrp_num_nonneg : 0 ≤ nrp.Fnum := by
        exact FloatSpec.Core.Float_prop.ge_0_F2R
          (beta:=beta) nrp hβ hnrp_nonneg
      by_cases hle : nrp.Fnum ≤ beta
      · have habs : |nrp.Fnum - beta| = beta - nrp.Fnum := by
          rw [abs_of_nonpos (by omega : nrp.Fnum - beta ≤ 0)]
          omega
        have hle_beta : |nrp.Fnum - beta| ≤ beta := by
          rw [habs]
          omega
        have hbeta_lt_bound : beta < b.vNum := by
          rw [hvnum]
          have hpow :
              beta ^ (1 : Nat) < beta ^ precision :=
            pow_lt_pow_right₀ (by omega : (1 : Int) < beta)
              (by omega : (1 : Nat) < precision)
          simpa [Zpower_nat] using hpow
        exact lt_of_le_of_lt hle_beta hbeta_lt_bound
      · have hgt : beta < nrp.Fnum := lt_of_not_ge hle
        have hnrp_abs : |nrp.Fnum| = nrp.Fnum := abs_of_pos (by omega)
        have hdiff_abs : |nrp.Fnum - beta| = nrp.Fnum - beta :=
          abs_of_nonneg (by omega)
        rw [hdiff_abs]
        rw [hnrp_abs] at hnum
        omega
    · simpa [pred_rp] using hexp
  · have hpred_rp_val :
        _root_.F2R (beta:=beta) pred_rp =
          _root_.F2R (beta:=beta) nrp -
            (beta : ℝ) * (beta : ℝ) ^ nrp.Fexp := by
      simp [pred_rp, _root_.F2R, FloatSpec.Core.Defs.F2R]
      ring
    calc
      _root_.F2R (beta:=beta) pred_rp
          = _root_.F2R (beta:=beta) nrp -
              (beta : ℝ) * (beta : ℝ) ^ nrp.Fexp := hpred_rp_val
      _ = (_root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) p) -
              (beta : ℝ) * (beta : ℝ) ^ nrp.Fexp := by rw [hnrpVal]
      _ = (_root_.F2R (beta:=beta) r -
              (beta : ℝ) * (beta : ℝ) ^ nrp.Fexp) -
              _root_.F2R (beta:=beta) p := by ring
      _ = _root_.F2R (beta:=beta)
              (FPred (beta:=beta) b beta precision r) -
            _root_.F2R (beta:=beta) p := by rw [← hpredVal]

/-- Non-minimal adjacent-exponent branch of Coq `ExactMinusIntervalAux`.

This corresponds to the upstream `FPredSimpl4` branch after
`radixRangeBoundExp`: the normalized representative of `r - p` is one exponent
below `r`, while `r` is not at the normal-boundary mantissa. -/
theorem ExactMinusIntervalAux_pred_adjacent_non_normmin_case {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (p r rp : FloatSpec.Core.Defs.FlocqFloat beta) :
    let nrp := Fnormalize (beta:=beta) beta b precision rp
    1 < beta →
    1 < precision →
    b.vNum = Zpower_nat beta precision →
    Fbounded (beta:=beta) b nrp →
    0 ≤ _root_.F2R (beta:=beta) nrp →
    Int.succ nrp.Fexp = r.Fexp →
    r.Fnum ≠ -pPred b.vNum →
    r.Fnum ≠ nNormMin beta precision →
    _root_.F2R (beta:=beta) nrp =
      _root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) p →
    ∃ pred_rp : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) b pred_rp ∧
        _root_.F2R (beta:=beta) pred_rp =
          _root_.F2R (beta:=beta)
              (FPred (beta:=beta) b beta precision r) -
            _root_.F2R (beta:=beta) p := by
  intro nrp hβ hprecision hvnum hnrpBound hnrp_nonneg hsucc hnot_neg_pred
    hnot_norm hnrpVal
  have hβ_pos : (0 : ℝ) < (beta : ℝ) := by
    exact_mod_cast (by omega : (0 : Int) < beta)
  have hβ_ne : (beta : ℝ) ≠ 0 := ne_of_gt hβ_pos
  have hfpredEq :
      FPred (beta:=beta) b beta precision r =
        ⟨r.Fnum - 1, r.Fexp⟩ := by
    have hTrip := FPredSimpl4 (beta:=beta) b beta precision r
    simpa only [wp, PostCond.noThrow, pure, FPredSimpl4_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
      hTrip ⟨hnot_neg_pred, hnot_norm⟩
  have hpow_r :
      (beta : ℝ) ^ r.Fexp =
        (beta : ℝ) ^ nrp.Fexp * (beta : ℝ) := by
    have hr_exp : r.Fexp = nrp.Fexp + 1 := by
      unfold Int.succ at hsucc
      omega
    rw [hr_exp, zpow_add₀ hβ_ne, zpow_one]
  have hpredVal :
      _root_.F2R (beta:=beta)
          (FPred (beta:=beta) b beta precision r) =
        _root_.F2R (beta:=beta) r -
          (beta : ℝ) * (beta : ℝ) ^ nrp.Fexp := by
    rw [hfpredEq]
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
      FloatSpec.Core.Defs.FlocqFloat.Fnum,
      FloatSpec.Core.Defs.FlocqFloat.Fexp]
    rw [hpow_r]
    norm_num [Int.cast_sub, Int.cast_add, Int.cast_one]
    ring
  exact ExactMinusIntervalAux_pred_beta_ulp_case (beta:=beta)
    b precision p r rp hβ hprecision hvnum hnrpBound hnrp_nonneg hpredVal hnrpVal

/-- Branch dispatcher for the constructive predecessor step in Coq
`ExactMinusIntervalAux`.

Once the upstream setup has normalized the representative of `r - p`, proved
that it lies in the same or adjacent exponent binade, and ruled out the
same-exponent normal-boundary/impossible case, the predecessor arithmetic is a
case split over the four `FPredSimpl` branches restored above. -/
theorem ExactMinusIntervalAux_pred_constructive_cases {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (p r rp : FloatSpec.Core.Defs.FlocqFloat beta) :
    let nrp := Fnormalize (beta:=beta) beta b precision rp
    1 < beta →
    1 < precision →
    b.vNum = Zpower_nat beta precision →
    Fbounded (beta:=beta) b nrp →
    0 < _root_.F2R (beta:=beta) r →
    0 < _root_.F2R (beta:=beta) nrp →
    (nrp.Fexp = r.Fexp ∨ Int.succ nrp.Fexp = r.Fexp) →
    (nrp.Fexp = r.Fexp →
      r.Fnum = nNormMin beta precision →
      r.Fexp ≠ -b.dExp →
      False) →
    _root_.F2R (beta:=beta) nrp =
      _root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) p →
    ∃ pred_rp : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) b pred_rp ∧
        _root_.F2R (beta:=beta) pred_rp =
          _root_.F2R (beta:=beta)
              (FPred (beta:=beta) b beta precision r) -
            _root_.F2R (beta:=beta) p := by
  intro nrp hβ hprecision hvnum hnrpBound hr_pos hnrp_pos hexpSplit
    hsame_norm_impossible hnrpVal
  have hprecision_ne : precision ≠ 0 := by omega
  have hnot_neg_pred : r.Fnum ≠ -pPred b.vNum := by
    have hr_num_pos : 0 < r.Fnum :=
      FloatSpec.Core.Float_prop.gt_0_F2R (beta:=beta) r hβ hr_pos
    have hvpos : 0 < b.vNum := by
      rw [hvnum]
      unfold Zpower_nat
      exact pow_pos (by omega : (0 : Int) < beta) _
    have hpPred_nonneg : 0 ≤ pPred b.vNum := by
      unfold pPred
      omega
    intro h
    omega
  rcases hexpSplit with hsame | hsucc
  · by_cases hnorm : r.Fnum = nNormMin beta precision
    · by_cases hminexp : r.Fexp = -b.dExp
      · exact ExactMinusIntervalAux_pred_same_exp_normmin_minexp_case
          (beta:=beta) b precision p r rp hβ hprecision_ne hvnum
          hnrpBound hnrp_pos hsame hnorm hminexp hnrpVal
      · exact False.elim (hsame_norm_impossible hsame hnorm hminexp)
    · exact ExactMinusIntervalAux_pred_same_exp_non_normmin_case (beta:=beta)
        b precision p r rp hβ hnrpBound hnrp_pos hsame hnot_neg_pred hnorm
        hnrpVal
  · by_cases hnorm : r.Fnum = nNormMin beta precision
    · by_cases hminexp : r.Fexp = -b.dExp
      · have hnrp_exp_lt : nrp.Fexp < -b.dExp := by
          unfold Int.succ at hsucc
          omega
        exact False.elim (not_le_of_gt hnrp_exp_lt hnrpBound.2)
      · exact ExactMinusIntervalAux_pred_adjacent_normmin_case (beta:=beta)
          b precision p r rp hβ hprecision_ne hvnum hnrpBound hnrp_pos
          hsucc hnorm hminexp hnrpVal
    · exact ExactMinusIntervalAux_pred_adjacent_non_normmin_case (beta:=beta)
        b precision p r rp hβ hprecision hvnum hnrpBound (le_of_lt hnrp_pos)
        hsucc hnot_neg_pred hnorm hnrpVal

/-- Normalization/radix-range wrapper for the constructive predecessor step.

This composes `ExactMinusIntervalAux_pred_setup` with the branch dispatcher
above.  The remaining explicit premise is the upstream contradiction ruling
out the same-exponent normal-boundary case away from the minimum exponent; the
next layer obtains that contradiction from `FcanonicLtPos` and
`pNormal_absolu_min`. -/
theorem ExactMinusIntervalAux_pred_constructive_from_setup {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (p r rp : FloatSpec.Core.Defs.FlocqFloat beta) :
    1 < beta →
    (beta : ℝ) = 2 →
    1 < precision →
    b.vNum = Zpower_nat beta precision →
    0 < _root_.F2R (beta:=beta) p →
    2 * _root_.F2R (beta:=beta) p < _root_.F2R (beta:=beta) r →
    Fcanonic (beta:=beta) beta b r →
    Fbounded (beta:=beta) b rp →
    _root_.F2R (beta:=beta) rp =
      _root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) p →
    (let nrp := Fnormalize (beta:=beta) beta b precision rp;
      nrp.Fexp = r.Fexp →
      r.Fnum = nNormMin beta precision →
      r.Fexp ≠ -b.dExp →
      False) →
    ∃ pred_rp : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) b pred_rp ∧
        _root_.F2R (beta:=beta) pred_rp =
          _root_.F2R (beta:=beta)
              (FPred (beta:=beta) b beta precision r) -
            _root_.F2R (beta:=beta) p := by
  intro hβ hbetaTwo hprecision hvnum hp_pos hr_gt_2p hrCan hrpBound hrpVal
    hsame_norm_impossible
  let nrp := Fnormalize (beta:=beta) beta b precision rp
  have hprecision_ne : precision ≠ 0 := by omega
  have hnrpBound : Fbounded (beta:=beta) b nrp := by
    have hcan := FnormalizeBounded (beta:=beta) beta b precision rp
    simpa only [wp, PostCond.noThrow, pure, FnormalizeBounded_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Fbounded', nrp, Int.cast_ofNat] using
      hcan ⟨hrpBound, hrpBound, hprecision_ne, hβ, hvnum⟩
  have hnrpVal :
      _root_.F2R (beta:=beta) nrp =
        _root_.F2R (beta:=beta) rp := by
    have hcorr := FnormalizeCorrect (beta:=beta) beta b precision rp
    simpa only [wp, PostCond.noThrow, pure, FnormalizeCorrect_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, nrp, Int.cast_ofNat] using hcorr ⟨rfl, hβ⟩
  have hnrp_pos : 0 < _root_.F2R (beta:=beta) nrp := by
    rw [hnrpVal, hrpVal]
    nlinarith
  have hsetup := ExactMinusIntervalAux_pred_setup (beta:=beta) b precision p r rp
    hβ hbetaTwo hprecision_ne hvnum hp_pos hr_gt_2p hrCan hrpBound hrpVal
  have hexpSplit :
      nrp.Fexp = r.Fexp ∨ Int.succ nrp.Fexp = r.Fexp := by
    simpa [nrp] using hsetup.2.2
  exact ExactMinusIntervalAux_pred_constructive_cases (beta:=beta)
    b precision p r rp hβ hprecision hvnum hnrpBound
    (by nlinarith) hnrp_pos hexpSplit (by simpa [nrp] using hsame_norm_impossible)
    (by simpa [nrp] using hnrpVal.trans hrpVal)

/-- Coq `ExactMinusIntervalAux` same-exponent boundary contradiction.

If the normalized representative of `r - p` has the same exponent as `r`, is
strictly below `r`, and `r` is the minimal normal mantissa away from the minimum
exponent, then `FcanonicLtPos` forces the representative mantissa below
`nNormMin`; canonicity and `pNormal_absolu_min` force it at least
`nNormMin`, a contradiction. -/
theorem ExactMinusIntervalAux_same_exp_normmin_non_minexp_contradiction {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (nrp r : FloatSpec.Core.Defs.FlocqFloat beta) :
    1 < beta →
    precision ≠ 0 →
    b.vNum = Zpower_nat beta precision →
    Fcanonic (beta:=beta) beta b nrp →
    Fcanonic (beta:=beta) beta b r →
    0 ≤ _root_.F2R (beta:=beta) nrp →
    _root_.F2R (beta:=beta) nrp < _root_.F2R (beta:=beta) r →
    nrp.Fexp = r.Fexp →
    r.Fnum = nNormMin beta precision →
    r.Fexp ≠ -b.dExp →
    False := by
  intro hβ hprecision hvnum hnrpCan hrCan hnrp_nonneg hnrp_lt_r hsame
    hrnum hrexp_ne
  have hcmp :
      nrp.Fexp < r.Fexp ∨
        (nrp.Fexp = r.Fexp ∧ nrp.Fnum < r.Fnum) := by
    have hTrip := FcanonicLtPos (beta:=beta) beta b nrp r hβ rfl
    simpa only [wp, PostCond.noThrow, pure, FcanonicLtPos_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, ULift.down_up, Fcanonic', Int.cast_ofNat] using
      hTrip ⟨hnrpCan, hrCan, hnrp_nonneg, hnrp_lt_r⟩
  have hnum_lt : nrp.Fnum < nNormMin beta precision := by
    rcases hcmp with hexp_lt | ⟨_hexp_eq, hnum_lt_r⟩
    · omega
    · omega
  have hnrp_normal : Fnormal (beta:=beta) beta b nrp := by
    rcases hnrpCan with hnormal | hsubnormal
    · exact hnormal
    · rcases hsubnormal with ⟨_hbound, hexp, _hsmall⟩
      exfalso
      omega
  have hmin_le_abs : nNormMin beta precision ≤ |nrp.Fnum| := by
    have hTrip := pNormal_absolu_min (beta:=beta) beta b precision nrp
    simpa only [wp, PostCond.noThrow, pure, pNormal_absolu_min_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Fnormal', Int.cast_ofNat] using
      hTrip ⟨hnrp_normal, hnrp_normal, hprecision, hβ, hvnum⟩
  have hnrp_num_nonneg : 0 ≤ nrp.Fnum :=
    FloatSpec.Core.Float_prop.ge_0_F2R (beta:=beta) nrp hβ hnrp_nonneg
  have habs : |nrp.Fnum| = nrp.Fnum := abs_of_nonneg hnrp_num_nonneg
  rw [habs] at hmin_le_abs
  omega

/-- Constructive hard predecessor step with the normal-boundary contradiction
discharged internally.

This is the closed form of the strictly-above-boundary arithmetic used by
Coq's `ExactMinusIntervalAux`: from a bounded representative of `r - p`, it
builds a bounded representative of `FPred r - p`. -/
theorem ExactMinusIntervalAux_pred_constructive_closed {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (p r rp : FloatSpec.Core.Defs.FlocqFloat beta) :
    1 < beta →
    (beta : ℝ) = 2 →
    1 < precision →
    b.vNum = Zpower_nat beta precision →
    0 < _root_.F2R (beta:=beta) p →
    2 * _root_.F2R (beta:=beta) p < _root_.F2R (beta:=beta) r →
    Fcanonic (beta:=beta) beta b r →
    Fbounded (beta:=beta) b rp →
    _root_.F2R (beta:=beta) rp =
      _root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) p →
    ∃ pred_rp : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) b pred_rp ∧
        _root_.F2R (beta:=beta) pred_rp =
          _root_.F2R (beta:=beta)
              (FPred (beta:=beta) b beta precision r) -
            _root_.F2R (beta:=beta) p := by
  intro hβ hbetaTwo hprecision hvnum hp_pos hr_gt_2p hrCan hrpBound hrpVal
  let nrp := Fnormalize (beta:=beta) beta b precision rp
  have hprecision_ne : precision ≠ 0 := by omega
  have hnrpCan : Fcanonic (beta:=beta) beta b nrp := by
    have hcan := FnormalizeCanonic (beta:=beta) beta b precision rp
    simpa only [wp, PostCond.noThrow, pure, FnormalizeCanonic_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Fbounded', nrp, Int.cast_ofNat] using
      hcan ⟨hrpBound, hrpBound, hprecision_ne, hβ, hvnum⟩
  have hsetup := ExactMinusIntervalAux_pred_setup (beta:=beta) b precision p r rp
    hβ hbetaTwo hprecision_ne hvnum hp_pos hr_gt_2p hrCan hrpBound hrpVal
  have hsame_norm_impossible :
      (let nrp := Fnormalize (beta:=beta) beta b precision rp;
        nrp.Fexp = r.Fexp →
        r.Fnum = nNormMin beta precision →
        r.Fexp ≠ -b.dExp →
        False) := by
    dsimp
    intro hsame hrnum hrexp_ne
    exact ExactMinusIntervalAux_same_exp_normmin_non_minexp_contradiction
      (beta:=beta) b precision nrp r hβ hprecision_ne hvnum hnrpCan hrCan
      hsetup.1 hsetup.2.1 hsame hrnum hrexp_ne
  exact ExactMinusIntervalAux_pred_constructive_from_setup (beta:=beta)
    b precision p r rp hβ hbetaTwo hprecision hvnum hp_pos hr_gt_2p
    hrCan hrpBound hrpVal hsame_norm_impossible

/-- Hard predecessor step for the Coq-shaped induction predicate.

The induction hypothesis gives a representative of `r - p` only under the hard
premise `2*p < r`.  The extra premise `2*p < FPred r`, together with
`FPredLt`, supplies that hard premise for the current `r`; the closed
constructive step then produces `FPred r - p`. -/
theorem ExactMinusIntervalAux_hard_pred_step {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (p r : FloatSpec.Core.Defs.FlocqFloat beta) :
    1 < beta →
    (beta : ℝ) = 2 →
    1 < precision →
    b.vNum = Zpower_nat beta precision →
    0 < _root_.F2R (beta:=beta) p →
    Fcanonic (beta:=beta) beta b r →
    (2 * _root_.F2R (beta:=beta) p < _root_.F2R (beta:=beta) r →
      ∃ rp : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) b rp ∧
          _root_.F2R (beta:=beta) rp =
            _root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) p) →
    2 * _root_.F2R (beta:=beta) p <
      _root_.F2R (beta:=beta) (FPred (beta:=beta) b beta precision r) →
    ∃ pred_rp : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) b pred_rp ∧
        _root_.F2R (beta:=beta) pred_rp =
          _root_.F2R (beta:=beta)
              (FPred (beta:=beta) b beta precision r) -
            _root_.F2R (beta:=beta) p := by
  intro hβ hbetaTwo hprecision hvnum hp_pos hrCan hCurrent hpredHard
  have hprecision_ne : precision ≠ 0 := by omega
  have hpred_lt_r :
      _root_.F2R (beta:=beta) (FPred (beta:=beta) b beta precision r) <
        _root_.F2R (beta:=beta) r := by
    have hTrip := FPredLt (beta:=beta) b beta precision r
    simpa only [wp, PostCond.noThrow, pure, FPredLt_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using
      hTrip ⟨rfl, hβ, hprecision_ne, hvnum⟩
  have hr_gt_2p :
      2 * _root_.F2R (beta:=beta) p < _root_.F2R (beta:=beta) r :=
    lt_trans hpredHard hpred_lt_r
  rcases hCurrent hr_gt_2p with ⟨rp, hrpBound, hrpVal⟩
  exact ExactMinusIntervalAux_pred_constructive_closed (beta:=beta)
    b precision p r rp hβ hbetaTwo hprecision hvnum hp_pos hr_gt_2p
    hrCan hrpBound hrpVal

/-- Coq: `ExactMinusIntervalAux`.

This is the hard branch of the exact-minus interval theorem.  The proof now
matches the upstream structure: downward induction over canonical floats using
the predicate `2*p < r -> exists r', r' = r - p`, with the predecessor step
provided by `ExactMinusIntervalAux_hard_pred_step`. -/
theorem ExactMinusIntervalAux {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) :
    1 < beta →
    (beta : ℝ) = 2 →
    1 < precision →
    b.vNum = Zpower_nat beta precision →
    0 < _root_.F2R (beta:=beta) p →
    2 * _root_.F2R (beta:=beta) p < _root_.F2R (beta:=beta) q →
    Fcanonic (beta:=beta) beta b p →
    Fcanonic (beta:=beta) beta b q →
    (∃ qmp : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) b qmp ∧
        _root_.F2R (beta:=beta) qmp =
          _root_.F2R (beta:=beta) q - _root_.F2R (beta:=beta) p) →
    ∀ r : FloatSpec.Core.Defs.FlocqFloat beta,
      Fcanonic (beta:=beta) beta b r →
      2 * _root_.F2R (beta:=beta) p < _root_.F2R (beta:=beta) r →
      _root_.F2R (beta:=beta) r ≤ _root_.F2R (beta:=beta) q →
      ∃ rp : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) b rp ∧
          _root_.F2R (beta:=beta) rp =
            _root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) p := by
  intro hβ hbetaTwo hprecision hvnum hp_pos hq_gt_2p hpCan hqCan hqmp
  refine ExactMinusIntervalAux_from_hard_pred_step (beta:=beta)
    b precision p q ?_ hβ hprecision hvnum hp_pos hq_gt_2p hpCan hqCan hqmp
  intro r hrCan _hr_pos _hr_le_q hCurrent hpredHard
  exact ExactMinusIntervalAux_hard_pred_step (beta:=beta)
    b precision p r hβ hbetaTwo hprecision hvnum hp_pos hrCan hCurrent
    hpredHard

/-- Coq: `ExactMinusIntervalAux1`.

This wraps `ExactMinusIntervalAux` with the two easy Sterbenz branches.  If
`p = 0`, the hard branch is immediate by choosing `r` itself; otherwise the
hard branch delegates to `ExactMinusIntervalAux`. -/
theorem ExactMinusIntervalAux1 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) :
    1 < beta →
    (beta : ℝ) = 2 →
    1 < precision →
    b.vNum = Zpower_nat beta precision →
    0 ≤ _root_.F2R (beta:=beta) p →
    _root_.F2R (beta:=beta) p ≤ _root_.F2R (beta:=beta) q →
    Fcanonic (beta:=beta) beta b p →
    Fcanonic (beta:=beta) beta b q →
    (∃ qmp : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) b qmp ∧
        _root_.F2R (beta:=beta) qmp =
          _root_.F2R (beta:=beta) q - _root_.F2R (beta:=beta) p) →
    ∀ r : FloatSpec.Core.Defs.FlocqFloat beta,
      Fcanonic (beta:=beta) beta b r →
      _root_.F2R (beta:=beta) p ≤ _root_.F2R (beta:=beta) r →
      _root_.F2R (beta:=beta) r ≤ _root_.F2R (beta:=beta) q →
      ∃ rp : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) b rp ∧
          _root_.F2R (beta:=beta) rp =
            _root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) p := by
  intro hβ hbetaTwo hprecision hvnum hp_nonneg hp_le_q hpCan hqCan hqmp r
    hrCan hp_le_r hr_le_q
  have hpBound : Fbounded (beta:=beta) b p := by
    have hTrip := FcanonicBound (beta:=beta) beta b p
    simpa only [wp, PostCond.noThrow, pure, FcanonicBound_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using hTrip hpCan
  have hrBound : Fbounded (beta:=beta) b r := by
    have hTrip := FcanonicBound (beta:=beta) beta b r
    simpa only [wp, PostCond.noThrow, pure, FcanonicBound_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using hTrip hrCan
  refine ExactMinusIntervalAux1_from_hard_branch (beta:=beta) b p q r ?_
    hβ hpBound hrBound hp_nonneg hp_le_q hp_le_r hr_le_q
  intro hq_gt_2p hr_gt_2p
  rcases lt_or_eq_of_le hp_nonneg with hp_pos | hp_zero
  · exact ExactMinusIntervalAux (beta:=beta) b precision p q
      hβ hbetaTwo hprecision hvnum hp_pos hq_gt_2p hpCan hqCan hqmp
      r hrCan hr_gt_2p hr_le_q
  · refine ⟨r, hrBound, ?_⟩
    nlinarith

/-- Coq: `ExactMinusInterval`.

The bounded-input theorem follows the upstream normalization step packaged in
`ExactMinusInterval_from_Aux1`, now using the restored canonical interval
payload `ExactMinusIntervalAux1`. -/
theorem ExactMinusInterval {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat) :
    1 < beta →
    (beta : ℝ) = 2 →
    1 < precision →
    b.vNum = Zpower_nat beta precision →
    ∀ p q r : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) b p →
      Fbounded (beta:=beta) b q →
      Fbounded (beta:=beta) b r →
      0 ≤ _root_.F2R (beta:=beta) p →
      _root_.F2R (beta:=beta) p ≤ _root_.F2R (beta:=beta) q →
      _root_.F2R (beta:=beta) p ≤ _root_.F2R (beta:=beta) r →
      _root_.F2R (beta:=beta) r ≤ _root_.F2R (beta:=beta) q →
      (∃ qmp : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) b qmp ∧
          _root_.F2R (beta:=beta) qmp =
            _root_.F2R (beta:=beta) q - _root_.F2R (beta:=beta) p) →
      ∃ rp : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) b rp ∧
          _root_.F2R (beta:=beta) rp =
            _root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) p := by
  intro hβ hbetaTwo hprecision hvnum
  have hprecision_ne : precision ≠ 0 := by omega
  have hAux1 :
      ∀ p q r : FloatSpec.Core.Defs.FlocqFloat beta,
        Fcanonic (beta:=beta) beta b p →
        Fcanonic (beta:=beta) beta b q →
        Fcanonic (beta:=beta) beta b r →
        0 ≤ _root_.F2R (beta:=beta) p →
        _root_.F2R (beta:=beta) p ≤ _root_.F2R (beta:=beta) q →
        _root_.F2R (beta:=beta) p ≤ _root_.F2R (beta:=beta) r →
        _root_.F2R (beta:=beta) r ≤ _root_.F2R (beta:=beta) q →
        (∃ qmp : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded (beta:=beta) b qmp ∧
            _root_.F2R (beta:=beta) qmp =
              _root_.F2R (beta:=beta) q - _root_.F2R (beta:=beta) p) →
        ∃ rp : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded (beta:=beta) b rp ∧
            _root_.F2R (beta:=beta) rp =
              _root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) p := by
    intro p q r hpCan hqCan hrCan hp_nonneg hp_le_q hp_le_r hr_le_q hqmp
    exact ExactMinusIntervalAux1 (beta:=beta) b precision p q
      hβ hbetaTwo hprecision hvnum hp_nonneg hp_le_q hpCan hqCan hqmp
      r hrCan hp_le_r hr_le_q
  exact ExactMinusInterval_from_Aux1 (beta:=beta) b precision hAux1
    hβ hprecision_ne hvnum

/-- Coq: `FcanonicFnormalizeEq` — a canonical float is unchanged by
normalization. -/
theorem FcanonicFnormalizeEq {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fcanonic (beta:=beta) radix b p ∧ beta = radix ∧ 1 < radix ∧
        precision ≠ 0 ∧ b.vNum = Zpower_nat radix precision⌝⦄
    (pure (FcanonicFnormalizeEq_check (beta:=beta) radix b precision p) : Id Unit)
    ⦃⇓_ => ⌜Fnormalize (beta:=beta) radix b precision p = p⌝⦄ := by
  intro h
  rcases h with ⟨hpCan, hbeta, hradix, hprecision, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, FcanonicFnormalizeEq_check,
    Id.run, ULift.up_down]
  have hpBound : Fbounded (beta:=beta) b p := by
    have hbound := FcanonicBound (beta:=beta) radix b p
    simpa only [wp, PostCond.noThrow, pure, FcanonicBound_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down]
      using hbound hpCan
  have hnormCan :
      Fcanonic (beta:=beta) radix b
        (Fnormalize (beta:=beta) radix b precision p) := by
    have hcan := FnormalizeCanonic (beta:=beta) radix b precision p
    simpa only [wp, PostCond.noThrow, pure, FnormalizeCanonic_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Fbounded', Int.cast_ofNat] using
      hcan ⟨hpBound, hpBound, hprecision, hradix, hvNum⟩
  have hnormEq :
      _root_.F2R (beta:=beta)
          (Fnormalize (beta:=beta) radix b precision p) =
        _root_.F2R (beta:=beta) p := by
    have hcorr := FnormalizeCorrect (beta:=beta) radix b precision p
    simpa only [wp, PostCond.noThrow, pure, FnormalizeCorrect_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Int.cast_ofNat] using hcorr ⟨hbeta, hradix⟩
  have hbeta_gt : 1 < beta := by omega
  have huniq := FcanonicUnique (beta:=beta) radix b
    (Fnormalize (beta:=beta) radix b precision p) p hbeta_gt hbeta.symm
  simpa only [wp, PostCond.noThrow, pure, FcanonicUnique_check,
    ULift.down_up, Fcanonic', Int.cast_ofNat] using huniq ⟨hnormCan, hpCan, hnormEq⟩

noncomputable def FNSuccCanonic_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FNSuccCanonic` — the normalized successor of a bounded float is
canonical.  This is the `FNSucc` wrapper over the already restored
`FnormalizeCanonic` and `FSuccCanonic` payloads. -/
theorem FNSuccCanonic {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b p ∧ 1 < beta ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat beta precision⌝⦄
    (pure (FNSuccCanonic_check (beta:=beta) b radix precision p) : Id Unit)
    ⦃⇓_ => ⌜Fcanonic (beta:=beta) beta b
      (FNSucc (beta:=beta) b radix precision p)⌝⦄ := by
  intro h
  rcases h with ⟨hpBound, hbeta, hprecision, hvnum⟩
  simp only [wp, PostCond.noThrow, pure, FNSuccCanonic_check,
    Id.run, ULift.up_down]
  have hnorm_can :
      Fcanonic (beta:=beta) beta b
        (Fnormalize (beta:=beta) beta b precision p) := by
    have hcan := FnormalizeCanonic (beta:=beta) beta b precision p
    simpa [wp, PostCond.noThrow, pure, FnormalizeCanonic_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down, Fbounded'] using
      hcan ⟨hpBound, hpBound, hprecision, hbeta, hvnum⟩
  have hsucc := FSuccCanonic (beta:=beta) b beta precision
    (Fnormalize (beta:=beta) beta b precision p)
  simpa [wp, PostCond.noThrow, pure, FSuccCanonic_check,
    Id.run, ULift.up_down, FNSucc] using
    hsucc ⟨hnorm_can, hbeta, hprecision, hvnum⟩

noncomputable def FNSuccLt_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FNSuccLt` — every float is strictly below its normalized successor. -/
theorem FNSuccLt {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜1 < beta ∧ precision ≠ 0 ∧ b.vNum = Zpower_nat beta precision⌝⦄
    (pure (FNSuccLt_check (beta:=beta) b radix precision p) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) p <
      _root_.F2R (beta:=beta)
        (FNSucc (beta:=beta) b radix precision p)⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hprecision, hvnum⟩
  simp only [wp, PostCond.noThrow, pure, FNSuccLt_check, Id.run,
    ULift.up_down]
  have hnorm :
      _root_.F2R (beta:=beta)
          (Fnormalize (beta:=beta) beta b precision p) =
        _root_.F2R (beta:=beta) p := by
    have hcorr := FnormalizeCorrect (beta:=beta) beta b precision p
    simpa [wp, PostCond.noThrow, pure, FnormalizeCorrect_check,
      PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Id.run, ULift.up, ULift.down, ULift.up_down] using hcorr ⟨rfl, hbeta⟩
  have hsucc := FSuccLt (beta:=beta) b beta precision
    (Fnormalize (beta:=beta) beta b precision p)
  have hsucc' :
      _root_.F2R (beta:=beta)
          (Fnormalize (beta:=beta) beta b precision p) <
        _root_.F2R (beta:=beta)
          (FNSucc (beta:=beta) b radix precision p) := by
    simpa [wp, PostCond.noThrow, pure, FSuccLt_check,
      Id.run, ULift.up_down, FNSucc] using
      hsucc ⟨rfl, hbeta, hprecision, hvnum⟩
  rw [hnorm] at hsucc'
  exact hsucc'

noncomputable def FNPredFopFNSucc_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FNPredFopFNSucc` — normalized predecessor is normalized successor
under float negation. -/
theorem FNPredFopFNSucc {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜1 < beta ∧ precision ≠ 0 ∧ b.vNum = Zpower_nat beta precision⌝⦄
    (pure (FNPredFopFNSucc_check (beta:=beta) b radix precision p) : Id Unit)
    ⦃⇓_ => ⌜FNPred (beta:=beta) b radix precision p =
      Fopp (beta:=beta)
        (FNSucc (beta:=beta) b radix precision (Fopp (beta:=beta) p))⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hprecision, hvnum⟩
  simp only [wp, PostCond.noThrow, pure, FNPredFopFNSucc_check,
    Id.run, ULift.up_down]
  have hnorm_opp :
      Fnormalize (beta:=beta) beta b precision (Fopp (beta:=beta) p) =
        Fopp (beta:=beta) (Fnormalize (beta:=beta) beta b precision p) := by
    have hnorm := Fnormalize_Fopp (beta:=beta) beta b precision p
    simpa [wp, PostCond.noThrow, pure, Fnormalize_Fopp_check,
      Id.run, ULift.up_down, Fopp] using hnorm True.intro
  have hpred := FPredFopFSucc (beta:=beta) b beta precision
    (Fnormalize (beta:=beta) beta b precision p)
  have hpred' :
      FPred (beta:=beta) b beta precision
          (Fnormalize (beta:=beta) beta b precision p) =
        Fopp (beta:=beta)
          (FSucc (beta:=beta) b beta precision
            (Fopp (beta:=beta)
              (Fnormalize (beta:=beta) beta b precision p))) := by
    simpa [wp, PostCond.noThrow, pure, FPredFopFSucc_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using hpred ⟨hbeta, hprecision, hvnum⟩
  simpa [FNPred, FNSucc, hnorm_opp] using hpred'

noncomputable def FNPredCanonic_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FNPredCanonic` — the normalized predecessor of a bounded float is
canonical. -/
theorem FNPredCanonic {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b p ∧ 1 < beta ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat beta precision⌝⦄
    (pure (FNPredCanonic_check (beta:=beta) b radix precision p) : Id Unit)
    ⦃⇓_ => ⌜Fcanonic (beta:=beta) beta b
      (FNPred (beta:=beta) b radix precision p)⌝⦄ := by
  intro h
  rcases h with ⟨hpBound, hbeta, hprecision, hvnum⟩
  simp only [wp, PostCond.noThrow, pure, FNPredCanonic_check,
    Id.run, ULift.up_down]
  have hnorm_can :
      Fcanonic (beta:=beta) beta b
        (Fnormalize (beta:=beta) beta b precision p) := by
    have hcan := FnormalizeCanonic (beta:=beta) beta b precision p
    simpa [wp, PostCond.noThrow, pure, FnormalizeCanonic_check,
      Id.run, ULift.up_down, Fbounded'] using
      hcan ⟨hpBound, hpBound, hprecision, hbeta, hvnum⟩
  have hpred := FPredCanonic (beta:=beta) b beta precision
    (Fnormalize (beta:=beta) beta b precision p)
  simpa [wp, PostCond.noThrow, pure, FPredCanonic_check,
    Id.run, ULift.up_down, FNPred] using
    hpred ⟨hnorm_can, hbeta, hprecision, hvnum⟩

noncomputable def FNPredLt_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FNPredLt` — every float is strictly above its normalized
predecessor. -/
theorem FNPredLt {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜1 < beta ∧ precision ≠ 0 ∧ b.vNum = Zpower_nat beta precision⌝⦄
    (pure (FNPredLt_check (beta:=beta) b radix precision p) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta)
        (FNPred (beta:=beta) b radix precision p) <
      _root_.F2R (beta:=beta) p⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hprecision, hvnum⟩
  simp only [wp, PostCond.noThrow, pure, FNPredLt_check, Id.run,
    ULift.up_down]
  have hnorm :
      _root_.F2R (beta:=beta)
          (Fnormalize (beta:=beta) beta b precision p) =
        _root_.F2R (beta:=beta) p := by
    have hcorr := FnormalizeCorrect (beta:=beta) beta b precision p
    simpa [wp, PostCond.noThrow, pure, FnormalizeCorrect_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using hcorr ⟨rfl, hbeta⟩
  have hpred := FPredLt (beta:=beta) b beta precision
    (Fnormalize (beta:=beta) beta b precision p)
  have hpred' :
      _root_.F2R (beta:=beta)
          (FNPred (beta:=beta) b radix precision p) <
        _root_.F2R (beta:=beta)
          (Fnormalize (beta:=beta) beta b precision p) := by
    simpa [wp, PostCond.noThrow, pure, FPredLt_check,
      Id.run, ULift.up_down, FNPred] using
      hpred ⟨rfl, hbeta, hprecision, hvnum⟩
  rw [hnorm] at hpred'
  exact hpred'

noncomputable def FNSuccProp_from_FSuccProp_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Normalized successor order, factored through the exact `FSuccProp` payload.

This is the non-circular wrapper used by Flocq's `FNSuccProp`: once the
all-sign canonical successor theorem is available for `FSucc`, normalization
transfers it to arbitrary bounded floats. -/
theorem FNSuccProp_from_FSuccProp {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b x ∧ Fbounded (beta:=beta) b y ∧
        _root_.F2R (beta:=beta) x < _root_.F2R (beta:=beta) y ∧
        1 < beta ∧ precision ≠ 0 ∧ b.vNum = Zpower_nat beta precision ∧
        (∀ a c : FloatSpec.Core.Defs.FlocqFloat beta,
          Fcanonic (beta:=beta) beta b a →
          Fcanonic (beta:=beta) beta b c →
          _root_.F2R (beta:=beta) a < _root_.F2R (beta:=beta) c →
          _root_.F2R (beta:=beta)
              (FSucc (beta:=beta) b beta precision a) ≤
            _root_.F2R (beta:=beta) c)⌝⦄
    (pure (FNSuccProp_from_FSuccProp_check (beta:=beta) b radix precision x y) :
      Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta)
        (FNSucc (beta:=beta) b radix precision x) ≤
      _root_.F2R (beta:=beta) y⌝⦄ := by
  intro h
  rcases h with
    ⟨hxBound, hyBound, hxy, hbeta, hprecision, hvnum, hsuccProp⟩
  simp only [wp, PostCond.noThrow, pure, FNSuccProp_from_FSuccProp_check,
    Id.run, ULift.up_down]
  have hxNormCan :
      Fcanonic (beta:=beta) beta b
        (Fnormalize (beta:=beta) beta b precision x) := by
    have hcan := FnormalizeCanonic (beta:=beta) beta b precision x
    simpa [wp, PostCond.noThrow, pure, FnormalizeCanonic_check,
      Id.run, ULift.up_down, Fbounded'] using
      hcan ⟨hxBound, hxBound, hprecision, hbeta, hvnum⟩
  have hyNormCan :
      Fcanonic (beta:=beta) beta b
        (Fnormalize (beta:=beta) beta b precision y) := by
    have hcan := FnormalizeCanonic (beta:=beta) beta b precision y
    simpa [wp, PostCond.noThrow, pure, FnormalizeCanonic_check,
      Id.run, ULift.up_down, Fbounded'] using
      hcan ⟨hyBound, hyBound, hprecision, hbeta, hvnum⟩
  have hxNormEq :
      _root_.F2R (beta:=beta)
          (Fnormalize (beta:=beta) beta b precision x) =
        _root_.F2R (beta:=beta) x := by
    have hcorr := FnormalizeCorrect (beta:=beta) beta b precision x
    simpa [wp, PostCond.noThrow, pure, FnormalizeCorrect_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using hcorr ⟨rfl, hbeta⟩
  have hyNormEq :
      _root_.F2R (beta:=beta)
          (Fnormalize (beta:=beta) beta b precision y) =
        _root_.F2R (beta:=beta) y := by
    have hcorr := FnormalizeCorrect (beta:=beta) beta b precision y
    simpa [wp, PostCond.noThrow, pure, FnormalizeCorrect_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using hcorr ⟨rfl, hbeta⟩
  have hxyNorm :
      _root_.F2R (beta:=beta)
          (Fnormalize (beta:=beta) beta b precision x) <
        _root_.F2R (beta:=beta)
          (Fnormalize (beta:=beta) beta b precision y) := by
    rw [hxNormEq, hyNormEq]
    exact hxy
  have hsucc := hsuccProp
    (Fnormalize (beta:=beta) beta b precision x)
    (Fnormalize (beta:=beta) beta b precision y)
    hxNormCan hyNormCan hxyNorm
  rw [hyNormEq] at hsucc
  simpa [FNSucc] using hsucc

noncomputable def FNSuccProp_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FNSuccProp` — normalized successor ordering for bounded floats. -/
theorem FNSuccProp {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b x ∧ Fbounded (beta:=beta) b y ∧
        _root_.F2R (beta:=beta) x < _root_.F2R (beta:=beta) y ∧
        1 < beta ∧ precision ≠ 0 ∧ b.vNum = Zpower_nat beta precision⌝⦄
    (pure (FNSuccProp_check (beta:=beta) b radix precision x y) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta)
        (FNSucc (beta:=beta) b radix precision x) ≤
      _root_.F2R (beta:=beta) y⌝⦄ := by
  intro h
  rcases h with ⟨hxBound, hyBound, hxy, hbeta, hprecision, hvnum⟩
  simp only [wp, PostCond.noThrow, pure, FNSuccProp_check, Id.run,
    ULift.up_down]
  have hwrap := FNSuccProp_from_FSuccProp (beta:=beta) b radix precision x y
  refine hwrap ?_
  refine ⟨hxBound, hyBound, hxy, hbeta, hprecision, hvnum, ?_⟩
  intro a c haCan hcCan hac
  exact
    (FSuccProp (beta:=beta) b beta precision a c)
      ⟨rfl, hbeta, hprecision, hvnum, haCan, hcCan, hac⟩

noncomputable def ClosestSuccPred_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (z : ℝ) (f : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `ClosestSuccPred` — if a canonical float is no farther from `z`
than both normalized neighbors, then it is a closest bounded float. -/
theorem ClosestSuccPred {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (z : ℝ) (f : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fcanonic (beta:=beta) radix b f ∧
        |z - _root_.F2R (beta:=beta) f| ≤
          |z - _root_.F2R (beta:=beta)
            (FSucc (beta:=beta) b radix precision f)| ∧
        |z - _root_.F2R (beta:=beta) f| ≤
          |z - _root_.F2R (beta:=beta)
            (FPred (beta:=beta) b radix precision f)| ∧
        beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (ClosestSuccPred_check (beta:=beta) b radix precision z f) : Id Unit)
    ⦃⇓_ => ⌜Closest (beta:=beta) b (radix : ℝ) z f⌝⦄ := by
  intro h
  rcases h with
    ⟨hfCan, hdistSucc, hdistPred, hbeta, hradix, hprecision, hvnum⟩
  simp only [wp, PostCond.noThrow, pure, ClosestSuccPred_check,
    Id.run, ULift.up_down]
  have hfBound : Fbounded (beta:=beta) b f := by
    have hbound := FcanonicBound (beta:=beta) radix b f
    simpa [wp, PostCond.noThrow, pure, FcanonicBound_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using hbound hfCan
  let succ : FloatSpec.Core.Defs.FlocqFloat beta :=
    FSucc (beta:=beta) b radix precision f
  let pred : FloatSpec.Core.Defs.FlocqFloat beta :=
    FPred (beta:=beta) b radix precision f
  have hsucc_lt :
      _root_.F2R (beta:=beta) f <
        _root_.F2R (beta:=beta) succ := by
    have hlt := FSuccLt (beta:=beta) b radix precision f
    simpa [wp, PostCond.noThrow, pure, FSuccLt_check,
      Id.run, ULift.up_down, succ] using
      hlt ⟨hbeta, hradix, hprecision, hvnum⟩
  have hpred_lt :
      _root_.F2R (beta:=beta) pred <
        _root_.F2R (beta:=beta) f := by
    have hlt := FPredLt (beta:=beta) b radix precision f
    simpa [wp, PostCond.noThrow, pure, FPredLt_check,
      Id.run, ULift.up_down, pred] using
      hlt ⟨hbeta, hradix, hprecision, hvnum⟩
  have hpredBound : Fbounded (beta:=beta) b pred := by
    have h := FBoundedPred (beta:=beta) b radix precision f
    simpa [wp, PostCond.noThrow, pure, FBoundedPred_check,
      Id.run, ULift.up_down, pred] using
      h ⟨hfBound, hradix, hprecision, hvnum⟩
  have hpredCan : Fcanonic (beta:=beta) radix b pred := by
    have h := FPredCanonic (beta:=beta) b radix precision f
    simpa [wp, PostCond.noThrow, pure, FPredCanonic_check,
      Id.run, ULift.up_down, pred] using
      h ⟨hfCan, hradix, hprecision, hvnum⟩
  have hFNSuccPred :
      FNSucc (beta:=beta) b (radix : ℝ) precision pred = f := by
    have hnorm :
        Fnormalize (beta:=beta) beta b precision pred = pred := by
      have h := FcanonicFnormalizeEq (beta:=beta) beta b precision pred
      simpa [wp, PostCond.noThrow, pure, FcanonicFnormalizeEq_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨by simpa [hbeta] using hpredCan, rfl, by omega, hprecision,
          by simpa [hbeta] using hvnum⟩
    have hsuccPred :
        FSucc (beta:=beta) b radix precision pred = f := by
      have h := FSucPred (beta:=beta) b radix precision f
      simpa [wp, PostCond.noThrow, pure, FSucPred_check,
        Id.run, ULift.up_down, pred] using
        h ⟨hfCan, hradix, hprecision, hvnum⟩
    have hsuccPredBeta :
        FSucc (beta:=beta) b beta precision pred = f := by
      simpa [hbeta] using hsuccPred
    simpa [FNSucc, hnorm] using hsuccPredBeta
  have hFNSuccF :
      FNSucc (beta:=beta) b (radix : ℝ) precision f = succ := by
    have hnorm :
        Fnormalize (beta:=beta) beta b precision f = f := by
      have h := FcanonicFnormalizeEq (beta:=beta) beta b precision f
      simpa [wp, PostCond.noThrow, pure, FcanonicFnormalizeEq_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨by simpa [hbeta] using hfCan, rfl, by omega, hprecision,
          by simpa [hbeta] using hvnum⟩
    have hsuccEq :
        FSucc (beta:=beta) b beta precision f = succ := by
      simp [succ, hbeta]
    simpa [FNSucc, hnorm] using hsuccEq
  have hpred_le_z :
      _root_.F2R (beta:=beta) pred ≤ z := by
    by_contra hnot
    have hz_lt_pred : z < _root_.F2R (beta:=beta) pred := lt_of_not_ge hnot
    have hz_lt_f : z < _root_.F2R (beta:=beta) f :=
      lt_trans hz_lt_pred hpred_lt
    have hdist :
        _root_.F2R (beta:=beta) f - z ≤
          _root_.F2R (beta:=beta) pred - z := by
      calc
        _root_.F2R (beta:=beta) f - z =
            |z - _root_.F2R (beta:=beta) f| := by
              rw [abs_of_neg (sub_neg.mpr hz_lt_f)]
              ring
        _ ≤ |z - _root_.F2R (beta:=beta) pred| := by
              simpa [pred] using hdistPred
        _ = _root_.F2R (beta:=beta) pred - z := by
              rw [abs_of_neg (sub_neg.mpr hz_lt_pred)]
              ring
    linarith
  have hz_le_succ :
      z ≤ _root_.F2R (beta:=beta) succ := by
    by_contra hnot
    have hsucc_lt_z : _root_.F2R (beta:=beta) succ < z := lt_of_not_ge hnot
    have hf_lt_z : _root_.F2R (beta:=beta) f < z :=
      lt_trans hsucc_lt hsucc_lt_z
    have hdist :
        z - _root_.F2R (beta:=beta) f ≤
          z - _root_.F2R (beta:=beta) succ := by
      calc
        z - _root_.F2R (beta:=beta) f =
            |z - _root_.F2R (beta:=beta) f| := by
              rw [abs_of_pos (sub_pos.mpr hf_lt_z)]
        _ ≤ |z - _root_.F2R (beta:=beta) succ| := by
              simpa [succ] using hdistSucc
        _ = z - _root_.F2R (beta:=beta) succ := by
              rw [abs_of_pos (sub_pos.mpr hsucc_lt_z)]
    linarith
  constructor
  · exact hfBound
  · intro g hgBound
    by_cases hg_le_pred :
        _root_.F2R (beta:=beta) g ≤ _root_.F2R (beta:=beta) pred
    · have hg_le_z :
          _root_.F2R (beta:=beta) g ≤ z :=
        le_trans hg_le_pred hpred_le_z
      have hdist :
          |z - _root_.F2R (beta:=beta) f| ≤
            |z - _root_.F2R (beta:=beta) g| := by
        calc
          |z - _root_.F2R (beta:=beta) f|
              ≤ |z - _root_.F2R (beta:=beta) pred| := by
                simpa [pred] using hdistPred
          _ = z - _root_.F2R (beta:=beta) pred := by
                rw [abs_of_nonneg (sub_nonneg.mpr hpred_le_z)]
          _ ≤ z - _root_.F2R (beta:=beta) g := by linarith
          _ = |z - _root_.F2R (beta:=beta) g| := by
                rw [abs_of_nonneg (sub_nonneg.mpr hg_le_z)]
      simpa [abs_sub_comm] using hdist
    · have hpred_lt_g :
          _root_.F2R (beta:=beta) pred < _root_.F2R (beta:=beta) g :=
        lt_of_not_ge hg_le_pred
      have hf_le_g :
          _root_.F2R (beta:=beta) f ≤ _root_.F2R (beta:=beta) g := by
        have hprop := FNSuccProp (beta:=beta) b (radix : ℝ) precision pred g
        have hle :
            _root_.F2R (beta:=beta)
                (FNSucc (beta:=beta) b (radix : ℝ) precision pred) ≤
              _root_.F2R (beta:=beta) g := by
          simpa [wp, PostCond.noThrow, pure, FNSuccProp_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using
            hprop ⟨hpredBound, hgBound, hpred_lt_g, by omega, hprecision,
              by simpa [hbeta] using hvnum⟩
        rwa [hFNSuccPred] at hle
      by_cases hf_lt_g :
          _root_.F2R (beta:=beta) f < _root_.F2R (beta:=beta) g
      · have hsucc_le_g :
            _root_.F2R (beta:=beta) succ ≤ _root_.F2R (beta:=beta) g := by
          have hprop := FNSuccProp (beta:=beta) b (radix : ℝ) precision f g
          have hle :
              _root_.F2R (beta:=beta)
                  (FNSucc (beta:=beta) b (radix : ℝ) precision f) ≤
                _root_.F2R (beta:=beta) g := by
            simpa [wp, PostCond.noThrow, pure, FNSuccProp_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using
              hprop ⟨hfBound, hgBound, hf_lt_g, by omega, hprecision,
                by simpa [hbeta] using hvnum⟩
          rwa [hFNSuccF] at hle
        have hz_le_g : z ≤ _root_.F2R (beta:=beta) g :=
          le_trans hz_le_succ hsucc_le_g
        have hdist :
            |z - _root_.F2R (beta:=beta) f| ≤
              |z - _root_.F2R (beta:=beta) g| := by
          calc
            |z - _root_.F2R (beta:=beta) f|
                ≤ |z - _root_.F2R (beta:=beta) succ| := by
                  simpa [succ] using hdistSucc
            _ = _root_.F2R (beta:=beta) succ - z := by
                  rw [abs_of_nonpos (sub_nonpos.mpr hz_le_succ)]
                  ring
            _ ≤ _root_.F2R (beta:=beta) g - z := by linarith
            _ = |z - _root_.F2R (beta:=beta) g| := by
                  rw [abs_of_nonpos (sub_nonpos.mpr hz_le_g)]
                  ring
        simpa [abs_sub_comm] using hdist
      · have hg_eq_f :
            _root_.F2R (beta:=beta) g = _root_.F2R (beta:=beta) f :=
          le_antisymm (le_of_not_gt hf_lt_g) hf_le_g
        rw [hg_eq_f]

noncomputable def MinMax_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) : Unit :=
  ()

/-- Coq: `MinMax` — a strict minimum yields the next maximum via `FNSucc`. -/
theorem MinMax {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) :
    ⦃⌜isMin (beta:=beta) b radix r p ∧
        _root_.F2R (beta:=beta) p ≠ r ∧
        beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (MinMax_check (beta:=beta) b radix precision p r) : Id Unit)
    ⦃⇓_ => ⌜isMax (beta:=beta) b radix r
      (FNSucc (beta:=beta) b (radix : ℝ) precision p)⌝⦄ := by
  intro h
  rcases h with ⟨hMin, hp_ne_r, hbeta_radix, hradix, hprecision, hvnum⟩
  subst beta
  rcases hMin with ⟨hpBound, hp_le_r, hGreatestLower⟩
  simp only [wp, PostCond.noThrow, pure, MinMax_check, Id.run,
    ULift.up_down]
  have hsuccCan :
      Fcanonic (beta:=radix) radix b
        (FNSucc (beta:=radix) b (radix : ℝ) precision p) := by
    have hcan := FNSuccCanonic (beta:=radix) b (radix : ℝ) precision p
    simpa [wp, PostCond.noThrow, pure, FNSuccCanonic_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      hcan ⟨hpBound, hradix, hprecision, hvnum⟩
  have hsuccBound :
      Fbounded (beta:=radix) b
        (FNSucc (beta:=radix) b (radix : ℝ) precision p) :=
    (FcanonicBound (beta:=radix) radix b
      (FNSucc (beta:=radix) b (radix : ℝ) precision p)) hsuccCan
  constructor
  · exact hsuccBound
  constructor
  · by_contra hnot
    have hsucc_lt_r :
        _root_.F2R (beta:=radix)
          (FNSucc (beta:=radix) b (radix : ℝ) precision p) < r :=
      lt_of_not_ge hnot
    have hsucc_le_p := hGreatestLower
      (FNSucc (beta:=radix) b (radix : ℝ) precision p)
      hsuccBound (le_of_lt hsucc_lt_r)
    have hp_lt_succ :
        _root_.F2R (beta:=radix) p <
          _root_.F2R (beta:=radix)
            (FNSucc (beta:=radix) b (radix : ℝ) precision p) := by
      have hlt := FNSuccLt (beta:=radix) b (radix : ℝ) precision p
      simpa [wp, PostCond.noThrow, pure, FNSuccLt_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using hlt ⟨hradix, hprecision, hvnum⟩
    exact (not_le_of_gt hp_lt_succ) hsucc_le_p
  · intro f hfBound hr_le_f
    have hp_lt_f :
        _root_.F2R (beta:=radix) p < _root_.F2R (beta:=radix) f := by
      by_contra hnot
      have hf_le_p : _root_.F2R (beta:=radix) f ≤ _root_.F2R (beta:=radix) p :=
        le_of_not_gt hnot
      have hp_eq_r : _root_.F2R (beta:=radix) p = r :=
        le_antisymm hp_le_r (le_trans hr_le_f hf_le_p)
      exact hp_ne_r hp_eq_r
    have hprop := FNSuccProp (beta:=radix) b (radix : ℝ) precision p f
    simpa [wp, PostCond.noThrow, pure, FNSuccProp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      hprop ⟨hpBound, hfBound, hp_lt_f, hradix, hprecision, hvnum⟩

noncomputable def MaxMin_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) : Unit :=
  ()

/-- Coq: `MaxMin` — a strict maximum yields the previous minimum via
`FNPred`. -/
theorem MaxMin {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) :
    ⦃⌜isMax (beta:=beta) b radix r p ∧
        r ≠ _root_.F2R (beta:=beta) p ∧
        beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (MaxMin_check (beta:=beta) b radix precision p r) : Id Unit)
    ⦃⇓_ => ⌜isMin (beta:=beta) b radix r
      (FNPred (beta:=beta) b (radix : ℝ) precision p)⌝⦄ := by
  intro h
  rcases h with ⟨hMax, hr_ne_p, hbeta_radix, hradix, hprecision, hvnum⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure, MaxMin_check, Id.run,
    ULift.up_down]
  have hOppMin :
      isMin (beta:=radix) b radix (-r) (Fopp (beta:=radix) p) := by
    have h := MaxOppMin (beta:=radix) b radix p r
    simpa [wp, PostCond.noThrow, pure, MaxOppMin_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hMax
  have hFopp_ne :
      _root_.F2R (beta:=radix) (Fopp (beta:=radix) p) ≠ -r := by
    intro hcontra
    have hp_opp :
        _root_.F2R (beta:=radix) (Fopp (beta:=radix) p) =
          -_root_.F2R (beta:=radix) p := by
      simp [Fopp, FloatSpec.Calc.Operations.Fopp, _root_.F2R,
        FloatSpec.Core.Defs.F2R]
    rw [hp_opp] at hcontra
    exact hr_ne_p (by linarith)
  have hNegMax :
      isMax (beta:=radix) b radix (-r)
        (FNSucc (beta:=radix) b (radix : ℝ) precision
          (Fopp (beta:=radix) p)) := by
    have h := MinMax (beta:=radix) b radix precision
      (Fopp (beta:=radix) p) (-r)
    simpa [wp, PostCond.noThrow, pure, MinMax_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hOppMin, hFopp_ne, rfl, hradix, hprecision, hvnum⟩
  have hBack :
      isMin (beta:=radix) b radix r
        (Fopp (beta:=radix)
          (FNSucc (beta:=radix) b (radix : ℝ) precision
            (Fopp (beta:=radix) p))) := by
    have h := MaxOppMin (beta:=radix) b radix
      (FNSucc (beta:=radix) b (radix : ℝ) precision
        (Fopp (beta:=radix) p)) (-r)
    simpa using h hNegMax
  have hpred_eq :
      FNPred (beta:=radix) b (radix : ℝ) precision p =
        Fopp (beta:=radix)
          (FNSucc (beta:=radix) b (radix : ℝ) precision
            (Fopp (beta:=radix) p)) := by
    have h := FNPredFopFNSucc (beta:=radix) b (radix : ℝ) precision p
    simpa [wp, PostCond.noThrow, pure, FNPredFopFNSucc_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hradix, hprecision, hvnum⟩
  simpa [hpred_eq]
    using hBack

noncomputable def FminRep_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FminRep` — a minimum of the real value of `p` has a representation
at exponent `Fexp p`.

This follows the upstream split.  If the normalized minimum has exponent no
larger than `p`, then the half-open successor interval collapses it to `p` by
`FSuccZleEq`.  Otherwise the normalized minimum can be shifted down to
`p.Fexp` while preserving its real value. -/
theorem FminRep {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision ∧
        isMin (beta:=beta) b radix (_root_.F2R (beta:=beta) p) q⌝⦄
    (pure (FminRep_check (beta:=beta) b radix precision p q) : Id Unit)
    ⦃⇓_ => ⌜∃ m : Int,
      _root_.F2R (beta:=beta) q =
        _root_.F2R (beta:=beta) ⟨m, p.Fexp⟩⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hradix, hprecision, hvnum, hMin⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure, FminRep_check, Id.run,
    ULift.up_down]
  let qn : FloatSpec.Core.Defs.FlocqFloat radix :=
    Fnormalize (beta:=radix) radix b precision q
  have hqn_val :
      _root_.F2R (beta:=radix) qn =
        _root_.F2R (beta:=radix) q := by
    have hcorr := FnormalizeCorrect (beta:=radix) radix b precision q
    simpa [qn] using hcorr ⟨rfl, hradix⟩
  by_cases hle_exp : qn.Fexp ≤ p.Fexp
  · have hq_le_p :
        _root_.F2R (beta:=radix) qn ≤
          _root_.F2R (beta:=radix) p := by
      rw [hqn_val]
      exact hMin.2.1
    have hp_lt_succ :
        _root_.F2R (beta:=radix) p <
          _root_.F2R (beta:=radix)
            (FSucc (beta:=radix) b radix precision qn) := by
      by_cases hp_eq_q : _root_.F2R (beta:=radix) p =
          _root_.F2R (beta:=radix) q
      · have hlt := FNSuccLt (beta:=radix) b (radix : ℝ) precision q
        have hlt' :
            _root_.F2R (beta:=radix) q <
              _root_.F2R (beta:=radix)
                (FNSucc (beta:=radix) b (radix : ℝ) precision q) := by
          simpa [wp, PostCond.noThrow, pure, FNSuccLt_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using hlt ⟨hradix, hprecision, hvnum⟩
        have hsucc_eq :
            FNSucc (beta:=radix) b (radix : ℝ) precision q =
              FSucc (beta:=radix) b radix precision qn := by
          simp [FNSucc, qn]
        rw [hp_eq_q]
        simpa [hsucc_eq] using hlt'
      · have hq_ne_p :
            _root_.F2R (beta:=radix) q ≠
              _root_.F2R (beta:=radix) p := by
          intro hqp
          exact hp_eq_q hqp.symm
        have hMax := MinMax (beta:=radix) b radix precision q
          (_root_.F2R (beta:=radix) p)
        have hMax' :
            isMax (beta:=radix) b radix
              (_root_.F2R (beta:=radix) p)
              (FNSucc (beta:=radix) b (radix : ℝ) precision q) := by
          simpa [wp, PostCond.noThrow, pure, MinMax_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using
            hMax ⟨hMin, hq_ne_p, rfl, hradix, hprecision, hvnum⟩
        have hp_le_succ := hMax'.2.1
        have hsucc_eq :
            FNSucc (beta:=radix) b (radix : ℝ) precision q =
              FSucc (beta:=radix) b radix precision qn := by
          simp [FNSucc, qn]
        rw [hsucc_eq] at hp_le_succ
        by_contra hnot
        have hsucc_le_p :
            _root_.F2R (beta:=radix)
                (FSucc (beta:=radix) b radix precision qn) ≤
              _root_.F2R (beta:=radix) p :=
          le_of_not_gt hnot
        have hsuccBound :
            Fbounded (beta:=radix) b
              (FSucc (beta:=radix) b radix precision qn) := by
          rw [← hsucc_eq]
          exact hMax'.1
        have hsucc_le_q := hMin.2.2
          (FSucc (beta:=radix) b radix precision qn)
          hsuccBound hsucc_le_p
        have hq_lt_succ :
            _root_.F2R (beta:=radix) q <
              _root_.F2R (beta:=radix)
                (FSucc (beta:=radix) b radix precision qn) := by
          have hlt := FNSuccLt (beta:=radix) b (radix : ℝ) precision q
          have hlt' :
              _root_.F2R (beta:=radix) q <
                _root_.F2R (beta:=radix)
                  (FNSucc (beta:=radix) b (radix : ℝ) precision q) := by
            simpa [wp, PostCond.noThrow, pure, FNSuccLt_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using hlt ⟨hradix, hprecision, hvnum⟩
          simpa [hsucc_eq] using hlt'
        exact (not_le_of_gt hq_lt_succ) hsucc_le_q
    have hqn_eq_p :
        _root_.F2R (beta:=radix) qn =
          _root_.F2R (beta:=radix) p := by
      have hcollapse := FSuccZleEq (beta:=radix) b radix precision qn p
      simpa [wp, PostCond.noThrow, pure, FSuccZleEq_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        hcollapse ⟨rfl, hradix, hprecision, hvnum, hq_le_p,
          hp_lt_succ, hle_exp⟩
    refine ⟨p.Fnum, ?_⟩
    rw [← hqn_val, hqn_eq_p]
  · have hlt_exp : p.Fexp < qn.Fexp := lt_of_not_ge hle_exp
    have hrep := F2R_rep_at_lower_exp (beta:=radix) qn p.Fexp
    have hrep' :
        ∃ m : Int,
          _root_.F2R (beta:=radix) qn =
            _root_.F2R (beta:=radix) ⟨m, p.Fexp⟩ := by
      simpa [wp, PostCond.noThrow, pure, F2R_rep_at_lower_exp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        hrep ⟨hradix, le_of_lt hlt_exp⟩
    rcases hrep' with ⟨m, hm⟩
    exact ⟨m, by rw [← hqn_val]; exact hm⟩

noncomputable def FmaxRep_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FmaxRep` — a maximum of the real value of `p` has a representation
at exponent `Fexp p`. -/
theorem FmaxRep {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision ∧
        isMax (beta:=beta) b radix (_root_.F2R (beta:=beta) p) q⌝⦄
    (pure (FmaxRep_check (beta:=beta) b radix precision p q) : Id Unit)
    ⦃⇓_ => ⌜∃ m : Int,
      _root_.F2R (beta:=beta) q =
        _root_.F2R (beta:=beta) ⟨m, p.Fexp⟩⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hradix, hprecision, hvnum, hMax⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure, FmaxRep_check, Id.run,
    ULift.up_down]
  have hMinRep :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat radix,
        isMin (beta:=radix) b radix (_root_.F2R (beta:=radix) p) q →
          ∃ m : Int,
            _root_.F2R (beta:=radix) q =
              _root_.F2R (beta:=radix) ⟨m, p.Fexp⟩ := by
    intro p q hMin
    have h := FminRep (beta:=radix) b radix precision p q
    simpa [wp, PostCond.noThrow, pure, FminRep_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, hradix, hprecision, hvnum, hMin⟩
  have hwrap := FmaxRep_from_FminRep (beta:=radix) b radix
  have hwrap' :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat radix,
        isMax (beta:=radix) b radix (_root_.F2R (beta:=radix) p) q →
          ∃ m : Int,
            _root_.F2R (beta:=radix) q =
              _root_.F2R (beta:=radix) ⟨m, p.Fexp⟩ := by
    simpa [wp, PostCond.noThrow, pure, FmaxRep_from_FminRep_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using hwrap hMinRep
  exact hwrap' p q hMax

/-- Coq `MinOrMaxRep`.  The radix, precision, and mantissa-bound assumptions
    are the enclosing `FRound` section hypotheses in Pff.v; they are explicit
    theorem arguments in Lean. -/
theorem MinOrMaxRep {radix : Int} [ValidRadix radix]
    (b : Fbound_skel) (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat radix → Prop)
    (hprecision : 1 < precision)
    (hvnum : b.vNum = Zpower_nat radix precision)
    (hMinOrMax : MinOrMaxP b P) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat radix,
      P (_root_.F2R p) q → ∃ m : Int,
        _root_.F2R q = _root_.F2R
          (FloatSpec.Core.Defs.FlocqFloat.mk m p.Fexp :
            FloatSpec.Core.Defs.FlocqFloat radix) := by
  intro p q hpq
  have hradix : 1 < radix := ValidRadix.valid
  have hprecision0 : precision ≠ 0 := by omega
  rcases hMinOrMax (_root_.F2R p) q hpq with hMin | hMax
  · have h := FminRep (beta := radix) b radix precision p q
    simpa [wp, PostCond.noThrow, pure, FminRep_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, hradix, hprecision0, hvnum, hMin⟩
  · have h := FmaxRep (beta := radix) b radix precision p q
    simpa [wp, PostCond.noThrow, pure, FmaxRep_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, hradix, hprecision0, hvnum, hMax⟩

/-- Coq `RoundedModeRep`, obtained from the min-or-max component of the
    rounded-mode package rather than from an assumed representation result. -/
theorem RoundedModeRep {radix : Int} [ValidRadix radix]
    (b : Fbound_skel) (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat radix → Prop)
    (hprecision : 1 < precision)
    (hvnum : b.vNum = Zpower_nat radix precision)
    (hRounded : RoundedModeP b P) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat radix,
      P (_root_.F2R p) q → ∃ m : Int,
        _root_.F2R q = _root_.F2R
          (FloatSpec.Core.Defs.FlocqFloat.mk m p.Fexp :
            FloatSpec.Core.Defs.FlocqFloat radix) := by
  exact MinOrMaxRep b precision P hprecision hvnum hRounded.2.2.1

/-- Coq: `errorBoundedPlusLe`.

This closes the factored `errorBoundedPlusLe_from_rep` theorem by deriving the
representation of the closest result from the restored min/max representation
stack.  The usual Pff section hypotheses are explicit in Lean. -/
theorem errorBoundedPlusLe_from_format_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat) :
    beta = radix →
    1 < radix →
    precision ≠ 0 →
    1 < bo.vNum →
    bo.vNum = Zpower_nat radix precision →
    ∀ p q pq : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      p.Fexp ≤ q.Fexp →
      Closest (beta:=beta) bo (radix : ℝ)
        (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) pq →
      ∃ error : FloatSpec.Core.Defs.FlocqFloat beta,
        _root_.F2R (beta:=beta) error =
          |_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
            _root_.F2R (beta:=beta) pq| ∧
        Fbounded (beta:=beta) bo error ∧
        error.Fexp = min p.Fexp q.Fexp := by
  intro hbeta hradix hprecision hvNum_gt hvNum p q pq hp hq hpq_exp hclosest
  subst beta
  have hBoundExp :
      ∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=radix) radix r).Fexp := by
    intro r
    exact le_trans (neg_nonpos.mpr bo.dExp_nonneg) (by simp [boundR, boundNat])
  have hMinTotal : TotalP (isMin (beta:=radix) bo radix) := by
    intro r
    have h := MinEx_from_finite_box_payload (beta:=radix) bo radix r
    simpa [wp, PostCond.noThrow, pure, MinEx_check, Id.run,
      ULift.up_down] using h ⟨rfl, hradix, hvNum_gt, hBoundExp r⟩
  have hMaxTotal : TotalP (isMax (beta:=radix) bo radix) := by
    intro r
    have h := MaxEx_from_finite_box_payload (beta:=radix) bo radix r
    simpa [wp, PostCond.noThrow, pure, MaxEx_check, Id.run,
      ULift.up_down] using h ⟨rfl, hradix, hvNum_gt, hBoundExp r⟩
  have hTotal : TotalP (Closest (beta:=radix) bo (radix : ℝ)) := by
    intro r
    have h := ClosestTotal_from_extrema_payload (beta:=radix) bo radix (radix : ℝ) r
    simpa [wp, PostCond.noThrow, pure, ClosestTotal_check, Id.run,
      ULift.up_down] using h ⟨hMinTotal, hMaxTotal⟩
  have hCompat : CompatiblePGeneric (Closest (beta:=radix) bo (radix : ℝ)) := by
    have h := ClosestCompatible_from_structural_payload (beta:=radix) bo (radix : ℝ)
    simpa [wp, PostCond.noThrow, pure, ClosestCompatible_check,
      PredTrans.pure, PredTrans.apply, Id.run, ULift.down] using h trivial
  have hMinMax :
      MinOrMaxP_float (beta:=radix) bo radix
        (Closest (beta:=radix) bo (radix : ℝ)) := by
    have h := ClosestMinOrMax_from_split_radix_payload (beta:=radix) bo radix (radix : ℝ)
    simpa [wp, PostCond.noThrow, pure, ClosestMinOrMax_check, MinOrMaxP_float,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hMono : MonotoneP_float (Closest (beta:=radix) bo (radix : ℝ)) := by
    have h := ClosestMonotone_from_split_radix_payload (beta:=radix) bo (radix : ℝ)
    simpa [wp, PostCond.noThrow, pure, ClosestMonotone_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hMinRep :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat radix,
        isMin (beta:=radix) bo radix (_root_.F2R (beta:=radix) p) q →
          ∃ m : Int,
            _root_.F2R (beta:=radix) q =
              _root_.F2R (beta:=radix) ⟨m, p.Fexp⟩ := by
    intro p q hmin
    have h := FminRep (beta:=radix) bo radix precision p q
    simpa [wp, PostCond.noThrow, pure, FminRep_check, Id.run,
      ULift.up_down] using h ⟨rfl, hradix, hprecision, hvNum, hmin⟩
  have hMaxRep :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat radix,
        isMax (beta:=radix) bo radix (_root_.F2R (beta:=radix) p) q →
          ∃ m : Int,
            _root_.F2R (beta:=radix) q =
              _root_.F2R (beta:=radix) ⟨m, p.Fexp⟩ := by
    have h := FmaxRep_from_FminRep (beta:=radix) bo radix
    simpa [wp, PostCond.noThrow, pure, FmaxRep_from_FminRep_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hMinRep
  have hRepAll :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat radix,
        Closest (beta:=radix) bo (radix : ℝ) (_root_.F2R (beta:=radix) p) q →
          ∃ m : Int,
            _root_.F2R (beta:=radix) q =
              _root_.F2R (beta:=radix) ⟨m, p.Fexp⟩ := by
    have h := RoundedModeRep_float_from_minmax (beta:=radix) bo radix
      (Closest (beta:=radix) bo (radix : ℝ))
    simpa [wp, PostCond.noThrow, pure,
      RoundedModeRep_float_from_minmax_check, Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨⟨hTotal, hCompat, hMinMax, hMono⟩, hMinRep, hMaxRep⟩
  have hplus_val :
      _root_.F2R (beta:=radix) (Fplus (beta:=radix) p q) =
        _root_.F2R (beta:=radix) p + _root_.F2R (beta:=radix) q := by
    have h := Fplus_correct (beta:=radix) p q
    simpa [wp, PostCond.noThrow, pure, Fplus_correct_check, Id.run,
      ULift.up_down] using h hradix
  have hclosest_plus :
      Closest (beta:=radix) bo (radix : ℝ)
        (_root_.F2R (beta:=radix) (Fplus (beta:=radix) p q)) pq := by
    rw [hplus_val]
    exact hclosest
  have hrep :
      ∃ m : Int,
        _root_.F2R (beta:=radix) pq =
          _root_.F2R (beta:=radix)
            ⟨m, (Fplus (beta:=radix) p q).Fexp⟩ :=
    hRepAll (Fplus (beta:=radix) p q) pq hclosest_plus
  have hle := errorBoundedPlusLe_from_rep (beta:=radix) bo (radix : ℝ) p q pq
  simpa [wp, PostCond.noThrow, pure, Id.run, ULift.up_down, Int.cast_ofNat] using
    hle ⟨hradix, hp, hq, hpq_exp, hclosest, hrep⟩

noncomputable def ClosestRoundedModeRep_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat) : Unit :=
  ()

/-- Coq helper used by `LeExpRound`: a closest-rounded result has a
representation at the source float's exponent. -/
theorem ClosestRoundedModeRep {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        1 < bo.vNum ∧ bo.vNum = Zpower_nat radix precision ∧
        (∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=beta) radix r).Fexp)⌝⦄
    (pure (ClosestRoundedModeRep_check (beta:=beta) bo radix precision) : Id Unit)
    ⦃⇓_ => ⌜∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Closest (beta:=beta) bo (radix : ℝ) (_root_.F2R (beta:=beta) p) q →
          ∃ m : Int,
            _root_.F2R (beta:=beta) q =
              _root_.F2R (beta:=beta) ⟨m, p.Fexp⟩⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hradix, hprecision, hvNum_gt, hvNum, hBoundExp⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure, ClosestRoundedModeRep_check,
    Id.run, ULift.up_down]
  have hMinTotal : TotalP (isMin (beta:=radix) bo radix) := by
    intro r
    have h := MinEx_from_finite_box_payload (beta:=radix) bo radix r
    simpa [wp, PostCond.noThrow, pure, MinEx_check, Id.run,
      ULift.up_down] using h ⟨rfl, hradix, hvNum_gt, hBoundExp r⟩
  have hMaxTotal : TotalP (isMax (beta:=radix) bo radix) := by
    intro r
    have h := MaxEx_from_finite_box_payload (beta:=radix) bo radix r
    simpa [wp, PostCond.noThrow, pure, MaxEx_check, Id.run,
      ULift.up_down] using h ⟨rfl, hradix, hvNum_gt, hBoundExp r⟩
  have hTotal : TotalP (Closest (beta:=radix) bo (radix : ℝ)) := by
    intro r
    have h := ClosestTotal_from_extrema_payload (beta:=radix) bo radix (radix : ℝ) r
    simpa [wp, PostCond.noThrow, pure, ClosestTotal_check, Id.run,
      ULift.up_down] using h ⟨hMinTotal, hMaxTotal⟩
  have hCompat : CompatiblePGeneric (Closest (beta:=radix) bo (radix : ℝ)) := by
    have h := ClosestCompatible_from_structural_payload (beta:=radix) bo (radix : ℝ)
    simpa [wp, PostCond.noThrow, pure, ClosestCompatible_check,
      PredTrans.pure, PredTrans.apply, Id.run, ULift.down] using h trivial
  have hMinMax :
      MinOrMaxP_float (beta:=radix) bo radix
        (Closest (beta:=radix) bo (radix : ℝ)) := by
    have h := ClosestMinOrMax_from_split_radix_payload (beta:=radix) bo radix (radix : ℝ)
    simpa [wp, PostCond.noThrow, pure, ClosestMinOrMax_check, MinOrMaxP_float,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hMono : MonotoneP_float (Closest (beta:=radix) bo (radix : ℝ)) := by
    have h := ClosestMonotone_from_split_radix_payload (beta:=radix) bo (radix : ℝ)
    simpa [wp, PostCond.noThrow, pure, ClosestMonotone_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hMinRep :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat radix,
        isMin (beta:=radix) bo radix (_root_.F2R (beta:=radix) p) q →
          ∃ m : Int,
            _root_.F2R (beta:=radix) q =
              _root_.F2R (beta:=radix) ⟨m, p.Fexp⟩ := by
    intro p q hmin
    have h := FminRep (beta:=radix) bo radix precision p q
    simpa [wp, PostCond.noThrow, pure, FminRep_check, Id.run,
      ULift.up_down] using h ⟨rfl, hradix, hprecision, hvNum, hmin⟩
  have hMaxRep :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat radix,
        isMax (beta:=radix) bo radix (_root_.F2R (beta:=radix) p) q →
          ∃ m : Int,
            _root_.F2R (beta:=radix) q =
              _root_.F2R (beta:=radix) ⟨m, p.Fexp⟩ := by
    have h := FmaxRep_from_FminRep (beta:=radix) bo radix
    simpa [wp, PostCond.noThrow, pure, FmaxRep_from_FminRep_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hMinRep
  have h := RoundedModeRep_float_from_minmax (beta:=radix) bo radix
    (Closest (beta:=radix) bo (radix : ℝ))
  simpa [wp, PostCond.noThrow, pure,
    RoundedModeRep_float_from_minmax_check, Id.run, ULift.up_down, Int.cast_ofNat] using
    h ⟨⟨hTotal, hCompat, hMinMax, hMono⟩, hMinRep, hMaxRep⟩

noncomputable def LeExpRound_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (f g : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `LeExpRound`.

If `g` is a closest rounded value of the real represented by `f`, then `g` has
an equal-real-value bounded representative whose exponent is at least
`f.Fexp`. -/
theorem LeExpRound_from_format_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (f g : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        1 < bo.vNum ∧ bo.vNum = Zpower_nat radix precision ∧
        Closest (beta:=beta) bo (radix : ℝ) (_root_.F2R (beta:=beta) f) g⌝⦄
    (pure (LeExpRound_check (beta:=beta) bo radix precision f g) : Id Unit)
    ⦃⇓_ => ⌜∃ g' : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo g' ∧
        _root_.F2R (beta:=beta) g' = _root_.F2R (beta:=beta) g ∧
        f.Fexp ≤ g'.Fexp⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hradix, hprecision, hvNum_gt, hvNum, hclosest⟩
  subst beta
  have hBoundExp :
      ∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=radix) radix r).Fexp := by
    intro r
    exact le_trans (neg_nonpos.mpr bo.dExp_nonneg) (by simp [boundR, boundNat])
  simp only [wp, PostCond.noThrow, pure, LeExpRound_check, Id.run,
    ULift.up_down]
  by_cases hfg : f.Fexp ≤ g.Fexp
  · exact ⟨g, hclosest.1, rfl, hfg⟩
  · have hrepAll :
        ∀ p q : FloatSpec.Core.Defs.FlocqFloat radix,
          Closest (beta:=radix) bo (radix : ℝ)
              (_root_.F2R (beta:=radix) p) q →
            ∃ m : Int,
              _root_.F2R (beta:=radix) q =
                _root_.F2R (beta:=radix) ⟨m, p.Fexp⟩ := by
      have h := ClosestRoundedModeRep (beta:=radix) bo radix precision
      simpa [wp, PostCond.noThrow, pure, ClosestRoundedModeRep_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨rfl, hradix, hprecision, hvNum_gt, hvNum, hBoundExp⟩
    rcases hrepAll f g hclosest with ⟨m, hm⟩
    let g' : FloatSpec.Core.Defs.FlocqFloat radix := ⟨m, f.Fexp⟩
    have hg_exp_le : g.Fexp ≤ g'.Fexp := by
      dsimp [g']
      exact le_of_lt (lt_of_not_ge hfg)
    have hg_bound : Fbounded (beta:=radix) bo g' := by
      have hEq : _root_.F2R (beta:=radix) g = _root_.F2R (beta:=radix) g' := by
        simpa [g'] using hm
      have h := FboundedEqExp (beta:=radix) bo g g'
      simpa [wp, PostCond.noThrow, pure, FboundedEqExp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hradix, hclosest.1, hEq, hg_exp_le⟩
    refine ⟨g', hg_bound, ?_, by simp [g']⟩
    simpa [g'] using hm.symm

noncomputable def LeExpRound2_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat) (n : Int)
    (f g : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `LeExpRound2`, the lower-bound corollary of `LeExpRound`. -/
theorem LeExpRound2_from_format_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat) (n : Int)
    (f g : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        1 < bo.vNum ∧ bo.vNum = Zpower_nat radix precision ∧
        Closest (beta:=beta) bo (radix : ℝ) (_root_.F2R (beta:=beta) f) g ∧
        n ≤ f.Fexp⌝⦄
    (pure (LeExpRound2_check (beta:=beta) bo radix precision n f g) : Id Unit)
    ⦃⇓_ => ⌜∃ g' : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo g' ∧
        _root_.F2R (beta:=beta) g' = _root_.F2R (beta:=beta) g ∧
        n ≤ g'.Fexp⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hradix, hprecision, hvNum_gt, hvNum, hclosest, hn⟩
  have hle := LeExpRound_from_format_payload (beta:=beta) bo radix precision f g
  rcases (by
      simpa [wp, PostCond.noThrow, pure, LeExpRound_check, Id.run,
        ULift.up_down] using
        hle ⟨hbeta, hradix, hprecision, hvNum_gt, hvNum, hclosest⟩) with
    ⟨g', hgBound, hgVal, hfg⟩
  simp only [wp, PostCond.noThrow, pure, LeExpRound2_check, Id.run,
    ULift.up_down]
  exact ⟨g', hgBound, hgVal, le_trans hn hfg⟩

/-- Coq: `errorBoundedPlusAbs`. -/
theorem errorBoundedPlusAbs_from_format_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat) :
    beta = radix →
    1 < radix →
    precision ≠ 0 →
    1 < bo.vNum →
    bo.vNum = Zpower_nat radix precision →
    ∀ p q pq : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      Closest (beta:=beta) bo (radix : ℝ)
        (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) pq →
      ∃ error : FloatSpec.Core.Defs.FlocqFloat beta,
        _root_.F2R (beta:=beta) error =
          |_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
            _root_.F2R (beta:=beta) pq| ∧
        Fbounded (beta:=beta) bo error ∧
        error.Fexp = min p.Fexp q.Fexp := by
  intro hbeta hradix hprecision hvNum_gt hvNum p q pq hp hq hclosest
  by_cases hpq_exp : p.Fexp ≤ q.Fexp
  · exact errorBoundedPlusLe_from_format_payload (beta:=beta) bo radix precision hbeta hradix
      hprecision hvNum_gt hvNum p q pq hp hq hpq_exp hclosest
  · have hqp_exp : q.Fexp ≤ p.Fexp := le_of_lt (lt_of_not_ge hpq_exp)
    have hclosest_swap :
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) q + _root_.F2R (beta:=beta) p) pq := by
      simpa [add_comm] using hclosest
    rcases errorBoundedPlusLe_from_format_payload (beta:=beta) bo radix precision hbeta hradix
        hprecision hvNum_gt hvNum q p pq hq hp hqp_exp
        hclosest_swap with
      ⟨error, hval, hbounded, hexp⟩
    refine ⟨error, ?_, hbounded, ?_⟩
    · simpa [add_comm] using hval
    · simpa [min_comm] using hexp

/-- Coq: `errorBoundedPlus`. -/
theorem errorBoundedPlus_from_format_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat) :
    beta = radix →
    1 < radix →
    precision ≠ 0 →
    1 < bo.vNum →
    bo.vNum = Zpower_nat radix precision →
    ∀ p q pq : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      Closest (beta:=beta) bo (radix : ℝ)
        (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) pq →
      ∃ error : FloatSpec.Core.Defs.FlocqFloat beta,
        _root_.F2R (beta:=beta) error =
          _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
            _root_.F2R (beta:=beta) pq ∧
        Fbounded (beta:=beta) bo error ∧
        error.Fexp = min p.Fexp q.Fexp := by
  intro hbeta hradix hprecision hvNum_gt hvNum p q pq hp hq hclosest
  rcases errorBoundedPlusAbs_from_format_payload (beta:=beta) bo radix precision hbeta hradix
      hprecision hvNum_gt hvNum p q pq hp hq hclosest with
    ⟨errorAbs, hAbsVal, hAbsBounded, hAbsExp⟩
  let delta : ℝ :=
    _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
      _root_.F2R (beta:=beta) pq
  by_cases hneg : delta < 0
  · refine ⟨Fopp (beta:=beta) errorAbs, ?_, ?_, ?_⟩
    · have hopp := Fopp_correct (beta:=beta) errorAbs
      have hopp_val :
          _root_.F2R (beta:=beta) (Fopp (beta:=beta) errorAbs) =
            -_root_.F2R (beta:=beta) errorAbs := by
        simpa [wp, PostCond.noThrow, pure, Fopp_correct_check, Id.run,
          ULift.up_down] using hopp trivial
      rw [hopp_val, hAbsVal]
      change
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
            _root_.F2R (beta:=beta) pq < 0 at hneg
      rw [abs_of_neg hneg]
      ring
    · have h := oppBounded (beta:=beta) bo errorAbs
      simpa [wp, PostCond.noThrow, pure, oppBounded_check, Id.run,
        ULift.up_down] using h hAbsBounded
    · simpa [Fopp, FloatSpec.Calc.Operations.Fopp] using hAbsExp
  · have hnonneg : 0 ≤ delta := le_of_not_gt hneg
    refine ⟨errorAbs, ?_, hAbsBounded, hAbsExp⟩
    rw [hAbsVal]
    change
      0 ≤ _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
        _root_.F2R (beta:=beta) pq at hnonneg
    rw [abs_of_nonneg hnonneg]

/-- Exact Coq `errorBoundedPlusLe` contract. -/
theorem errorBoundedPlusLe {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hprecision : 1 < precision)
    (hvNum : bo.vNum = Zpower_nat radix precision) :
    ∀ p q pq : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo p → Fbounded (beta:=beta) bo q →
      p.Fexp ≤ q.Fexp →
      Closest (beta:=beta) bo (radix : ℝ)
        (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) pq →
      ∃ error : FloatSpec.Core.Defs.FlocqFloat beta,
        _root_.F2R (beta:=beta) error =
          |_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
            _root_.F2R (beta:=beta) pq| ∧
        Fbounded (beta:=beta) bo error ∧
        error.Fexp = min p.Fexp q.Fexp := by
  have hprecision_ne : precision ≠ 0 := by omega
  have hvNum_gt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecision_ne
  letI : ValidRadix radix := ⟨hradix⟩
  exact errorBoundedPlusLe_from_format_payload (beta:=beta) bo radix precision
    hbeta hradix hprecision_ne hvNum_gt hvNum

/-- Exact Coq `LeExpRound` contract. -/
theorem LeExpRound {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (f g : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧
        bo.vNum = Zpower_nat radix precision ∧ 3 ≤ precision ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) f) g⌝⦄
    (pure (LeExpRound_check (beta:=beta) bo radix precision f g) : Id Unit)
    ⦃⇓_ => ⌜∃ g' : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo g' ∧
        _root_.F2R (beta:=beta) g' = _root_.F2R (beta:=beta) g ∧
        f.Fexp ≤ g'.Fexp⌝⦄ := by
  intro ⟨hbeta, hradix, hvNum, hprecision, hclosest⟩
  have hprecision_ne : precision ≠ 0 := by omega
  have hvNum_gt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecision_ne
  letI : ValidRadix radix := ⟨hradix⟩
  simpa [wp, PostCond.noThrow, pure, LeExpRound_check, Id.run,
    ULift.up_down] using
    LeExpRound_from_format_payload (beta:=beta) bo radix precision f g
      ⟨hbeta, hradix, hprecision_ne, hvNum_gt, hvNum, hclosest⟩

/-- Exact Coq `LeExpRound2` contract. -/
theorem LeExpRound2 {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (n : Int) (f g : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧
        bo.vNum = Zpower_nat radix precision ∧ 3 ≤ precision ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) f) g ∧ n ≤ f.Fexp⌝⦄
    (pure (LeExpRound2_check (beta:=beta) bo radix precision n f g) : Id Unit)
    ⦃⇓_ => ⌜∃ g' : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo g' ∧
        _root_.F2R (beta:=beta) g' = _root_.F2R (beta:=beta) g ∧
        n ≤ g'.Fexp⌝⦄ := by
  intro ⟨hbeta, hradix, hvNum, hprecision, hclosest, hn⟩
  have hprecision_ne : precision ≠ 0 := by omega
  have hvNum_gt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecision_ne
  letI : ValidRadix radix := ⟨hradix⟩
  simpa [wp, PostCond.noThrow, pure, LeExpRound2_check, Id.run,
    ULift.up_down] using
    LeExpRound2_from_format_payload (beta:=beta) bo radix precision n f g
      ⟨hbeta, hradix, hprecision_ne, hvNum_gt, hvNum, hclosest, hn⟩

/-- Exact Coq `errorBoundedPlusAbs` contract. -/
theorem errorBoundedPlusAbs {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hprecision : 1 < precision)
    (hvNum : bo.vNum = Zpower_nat radix precision) :
    ∀ p q pq : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo p → Fbounded (beta:=beta) bo q →
      Closest (beta:=beta) bo (radix : ℝ)
        (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) pq →
      ∃ error : FloatSpec.Core.Defs.FlocqFloat beta,
        _root_.F2R (beta:=beta) error =
          |_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
            _root_.F2R (beta:=beta) pq| ∧
        Fbounded (beta:=beta) bo error ∧
        error.Fexp = min p.Fexp q.Fexp := by
  have hprecision_ne : precision ≠ 0 := by omega
  have hvNum_gt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecision_ne
  letI : ValidRadix radix := ⟨hradix⟩
  exact errorBoundedPlusAbs_from_format_payload (beta:=beta) bo radix precision
    hbeta hradix hprecision_ne hvNum_gt hvNum

/-- Exact Coq `errorBoundedPlus` contract. -/
theorem errorBoundedPlus {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hprecision : 1 < precision)
    (hvNum : bo.vNum = Zpower_nat radix precision) :
    ∀ p q pq : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo p → Fbounded (beta:=beta) bo q →
      Closest (beta:=beta) bo (radix : ℝ)
        (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) pq →
      ∃ error : FloatSpec.Core.Defs.FlocqFloat beta,
        _root_.F2R (beta:=beta) error =
          _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
            _root_.F2R (beta:=beta) pq ∧
        Fbounded (beta:=beta) bo error ∧
        error.Fexp = min p.Fexp q.Fexp := by
  have hprecision_ne : precision ≠ 0 := by omega
  have hvNum_gt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecision_ne
  letI : ValidRadix radix := ⟨hradix⟩
  exact errorBoundedPlus_from_format_payload (beta:=beta) bo radix precision
    hbeta hradix hprecision_ne hvNum_gt hvNum

/-- Mantissa-bound core for the multiplication-error min/max branches.

This is the final arithmetic packaging step common to Coq
`errorBoundedMultMin` and `errorBoundedMultMax`: after the rounded product has
been represented at exponent `p.Fexp + q.Fexp`, it remains to bound the
integer mantissa error.  The quotient/min/max parts of the upstream proof are
exactly what discharge `hnum_bound`; this helper turns that payload into the
bounded error float required by `errorBoundedMult`. -/
theorem errorBoundedMult_from_same_exp_mantissa_bound {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int)
    (p q f : FloatSpec.Core.Defs.FlocqFloat beta) (m : Int)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hrepr :
      _root_.F2R (beta:=beta) f =
        _root_.F2R (beta:=beta)
          (⟨m, p.Fexp + q.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat beta))
    (hnum_bound : |p.Fnum * q.Fnum - m| < bo.vNum)
    (hexp : -bo.dExp ≤ p.Fexp + q.Fexp) :
    ∃ error : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) error =
        _root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) f ∧
      Fbounded (beta:=beta) bo error ∧
      error.Fexp = p.Fexp + q.Fexp := by
  let error : FloatSpec.Core.Defs.FlocqFloat beta :=
    ⟨p.Fnum * q.Fnum - m, p.Fexp + q.Fexp⟩
  refine ⟨error, ?_, ?_, rfl⟩
  · have hβ : 1 < beta := by omega
    have hprod := Fmult_correct (beta:=beta) p q
    have hprod_val :
        _root_.F2R (beta:=beta)
            (FloatSpec.Calc.Operations.Fmult (beta:=beta) p q) =
          _root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q := by
      simpa [wp, PostCond.noThrow, pure, Fmult_correct_check, Id.run,
        ULift.up_down] using hprod hβ
    have hprod_def :
        FloatSpec.Calc.Operations.Fmult (beta:=beta) p q =
          (⟨p.Fnum * q.Fnum, p.Fexp + q.Fexp⟩ :
            FloatSpec.Core.Defs.FlocqFloat beta) := rfl
    rw [← hprod_val, hprod_def, hrepr]
    simp [_root_.F2R, FloatSpec.Core.Defs.F2R, error, sub_mul]
  · exact ⟨hnum_bound, hexp⟩

/-- Cancel the common positive radix power from two same-exponent `F2R` values.

This is the integer form of the `Rle_monotony_contra_exp` step used in Coq
proofs such as `errorBoundedMultMin` and `errorBoundedMultMax`. -/
theorem F2R_le_same_exp_mantissa_le {beta : Int} [ValidRadix beta]
    (radix : Int) (m n e : Int)
    (hbeta : beta = radix)
    (hradix : 0 < radix)
    (hle :
      _root_.F2R (beta:=beta)
          (⟨m, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta) ≤
        _root_.F2R (beta:=beta)
          (⟨n, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta)) :
    m ≤ n := by
  subst beta
  have hpow_pos : 0 < (radix : ℝ) ^ e := zpow_pos (by exact_mod_cast hradix) e
  have hreal : (m : ℝ) ≤ (n : ℝ) := by
    apply le_of_mul_le_mul_right ?_ hpow_pos
    simpa [_root_.F2R, FloatSpec.Core.Defs.F2R] using hle
  exact Int.cast_le.mp hreal

/-- Same-exponent mantissa comparison when the left float is known only through
an `F2R` representation witness. -/
theorem F2R_le_same_exp_mantissa_le_of_left_repr {beta : Int} [ValidRadix beta]
    (radix : Int) (f : FloatSpec.Core.Defs.FlocqFloat beta)
    (m n e : Int)
    (hbeta : beta = radix)
    (hradix : 0 < radix)
    (hrepr :
      _root_.F2R (beta:=beta) f =
        _root_.F2R (beta:=beta)
          (⟨m, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta))
    (hle :
      _root_.F2R (beta:=beta) f ≤
        _root_.F2R (beta:=beta)
          (⟨n, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta)) :
    m ≤ n :=
  F2R_le_same_exp_mantissa_le (beta:=beta) radix m n e hbeta hradix
    (by
      rw [hrepr] at hle
      exact hle)

/-- Same-exponent mantissa comparison when the right float is known only through
an `F2R` representation witness. -/
theorem F2R_le_same_exp_mantissa_le_of_right_repr {beta : Int} [ValidRadix beta]
    (radix : Int) (f : FloatSpec.Core.Defs.FlocqFloat beta)
    (m n e : Int)
    (hbeta : beta = radix)
    (hradix : 0 < radix)
    (hrepr :
      _root_.F2R (beta:=beta) f =
        _root_.F2R (beta:=beta)
          (⟨n, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta))
    (hle :
      _root_.F2R (beta:=beta)
          (⟨m, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta) ≤
        _root_.F2R (beta:=beta) f) :
    m ≤ n :=
  F2R_le_same_exp_mantissa_le (beta:=beta) radix m n e hbeta hradix
    (by
      rw [hrepr] at hle
      exact hle)

/-- Same-exponent mantissa inequalities lift to real-value inequalities. -/
theorem F2R_le_of_same_exp_mantissa_le {beta : Int} [ValidRadix beta]
    (radix : Int) (m n e : Int)
    (hbeta : beta = radix)
    (hradix : 0 < radix)
    (hmn : m ≤ n) :
    _root_.F2R (beta:=beta)
        (⟨m, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta) ≤
      _root_.F2R (beta:=beta)
        (⟨n, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta) := by
  subst beta
  have hpow_nonneg : 0 ≤ (radix : ℝ) ^ e :=
    le_of_lt (zpow_pos (by exact_mod_cast hradix) e)
  simpa [_root_.F2R, FloatSpec.Core.Defs.F2R] using
    mul_le_mul_of_nonneg_right (Int.cast_le.mpr hmn) hpow_nonneg

/-- Min-side bucket inequality from an `isMin` witness.

If a bounded same-exponent bucket lies below the exact real target, then the
greatest-lower-bound part of `isMin` puts it below the chosen rounded value.
The same-exponent comparison then converts that real inequality into a
mantissa inequality. -/
theorem errorBoundedMultMin_bucket_le_from_isMin {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (r : ℝ)
    (fmin : FloatSpec.Core.Defs.FlocqFloat beta)
    (bucket m e : Int)
    (hbeta : beta = radix)
    (hradix : 0 < radix)
    (hmin : isMin (beta:=beta) bo radix r fmin)
    (hrepr :
      _root_.F2R (beta:=beta) fmin =
        _root_.F2R (beta:=beta)
          (⟨m, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta))
    (hbucket_bounded :
      Fbounded' (beta:=beta) bo
        (⟨bucket, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta))
    (hbucket_le_r :
      _root_.F2R (beta:=beta)
          (⟨bucket, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta) ≤ r) :
    bucket ≤ m := by
  rcases hmin with ⟨_hfmin_bounded, _hfmin_le_r, hglb⟩
  have hreal :
      _root_.F2R (beta:=beta)
          (⟨bucket, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta) ≤
        _root_.F2R (beta:=beta) fmin :=
    hglb (⟨bucket, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta)
      hbucket_bounded hbucket_le_r
  exact F2R_le_same_exp_mantissa_le_of_right_repr (beta:=beta)
    radix fmin bucket m e hbeta hradix hrepr hreal

/-- Max-side bucket inequality from an `isMax` witness.

If a bounded same-exponent bucket lies above the exact real target, then the
least-upper-bound part of `isMax` puts the chosen rounded value below that
bucket; the same-exponent comparison recovers the mantissa inequality. -/
theorem errorBoundedMultMax_bucket_le_from_isMax {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (r : ℝ)
    (fmax : FloatSpec.Core.Defs.FlocqFloat beta)
    (m bucket e : Int)
    (hbeta : beta = radix)
    (hradix : 0 < radix)
    (hmax : isMax (beta:=beta) bo radix r fmax)
    (hrepr :
      _root_.F2R (beta:=beta) fmax =
        _root_.F2R (beta:=beta)
          (⟨m, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta))
    (hbucket_bounded :
      Fbounded' (beta:=beta) bo
        (⟨bucket, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta))
    (hr_le_bucket :
      r ≤ _root_.F2R (beta:=beta)
          (⟨bucket, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta)) :
    m ≤ bucket := by
  rcases hmax with ⟨_hfmax_bounded, _hr_le_fmax, hlub⟩
  have hreal :
      _root_.F2R (beta:=beta) fmax ≤
        _root_.F2R (beta:=beta)
          (⟨bucket, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta) :=
    hlub (⟨bucket, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta)
      hbucket_bounded hr_le_bucket
  exact F2R_le_same_exp_mantissa_le_of_left_repr (beta:=beta)
    radix fmax m bucket e hbeta hradix hrepr hreal

/-- Min-side bucket inequality from a bounded candidate with a same-exponent
real-value representation.

This matches the shape produced by `FboundedMbound` in Coq
`errorBoundedMultMin`: the bounded witness may be represented at a shifted
exponent, but its real value is the same as a same-exponent bucket mantissa. -/
theorem errorBoundedMultMin_bucket_le_from_isMin_candidate_repr {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (r : ℝ)
    (fmin candidate : FloatSpec.Core.Defs.FlocqFloat beta)
    (bucket m e : Int)
    (hbeta : beta = radix)
    (hradix : 0 < radix)
    (hmin : isMin (beta:=beta) bo radix r fmin)
    (hfmin_repr :
      _root_.F2R (beta:=beta) fmin =
        _root_.F2R (beta:=beta)
          (⟨m, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta))
    (hcandidate_repr :
      _root_.F2R (beta:=beta) candidate =
        _root_.F2R (beta:=beta)
          (⟨bucket, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta))
    (hcandidate_bounded : Fbounded' (beta:=beta) bo candidate)
    (hcandidate_le_r : _root_.F2R (beta:=beta) candidate ≤ r) :
    bucket ≤ m := by
  rcases hmin with ⟨_hfmin_bounded, _hfmin_le_r, hglb⟩
  have hreal_candidate :
      _root_.F2R (beta:=beta) candidate ≤ _root_.F2R (beta:=beta) fmin :=
    hglb candidate hcandidate_bounded hcandidate_le_r
  have hreal_bucket :
      _root_.F2R (beta:=beta)
          (⟨bucket, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta) ≤
        _root_.F2R (beta:=beta) fmin := by
    rw [hcandidate_repr] at hreal_candidate
    exact hreal_candidate
  exact F2R_le_same_exp_mantissa_le_of_right_repr (beta:=beta)
    radix fmin bucket m e hbeta hradix hfmin_repr hreal_bucket

/-- Max-side bucket inequality from a bounded candidate with a same-exponent
real-value representation.

This is the upper-rounding companion of
`errorBoundedMultMin_bucket_le_from_isMin_candidate_repr`. -/
theorem errorBoundedMultMax_bucket_le_from_isMax_candidate_repr {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (r : ℝ)
    (fmax candidate : FloatSpec.Core.Defs.FlocqFloat beta)
    (m bucket e : Int)
    (hbeta : beta = radix)
    (hradix : 0 < radix)
    (hmax : isMax (beta:=beta) bo radix r fmax)
    (hfmax_repr :
      _root_.F2R (beta:=beta) fmax =
        _root_.F2R (beta:=beta)
          (⟨m, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta))
    (hcandidate_repr :
      _root_.F2R (beta:=beta) candidate =
        _root_.F2R (beta:=beta)
          (⟨bucket, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta))
    (hcandidate_bounded : Fbounded' (beta:=beta) bo candidate)
    (hr_le_candidate : r ≤ _root_.F2R (beta:=beta) candidate) :
    m ≤ bucket := by
  rcases hmax with ⟨_hfmax_bounded, _hr_le_fmax, hlub⟩
  have hreal_candidate :
      _root_.F2R (beta:=beta) fmax ≤ _root_.F2R (beta:=beta) candidate :=
    hlub candidate hcandidate_bounded hr_le_candidate
  have hreal_bucket :
      _root_.F2R (beta:=beta) fmax ≤
        _root_.F2R (beta:=beta)
          (⟨bucket, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta) := by
    rw [hcandidate_repr] at hreal_candidate
    exact hreal_candidate
  exact F2R_le_same_exp_mantissa_le_of_left_repr (beta:=beta)
    radix fmax m bucket e hbeta hradix hfmax_repr hreal_bucket

/-- Build the shifted-exponent bounded candidate used for quotient buckets.

Coq's multiplication-error proof constructs a bounded float with value
`bucket * radix^(precision + e)`.  This helper identifies that value with the
same-exponent bucket mantissa
`bucket * Zpower_nat radix precision` at exponent `e`, which is the form needed
by the integer quotient estimates. -/
theorem FboundedMbound_zpower_bucket_candidate {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (bucket e : Int)
    (hbeta : beta = radix)
    (hradix : 1 < radix)
    (hprecision : 0 < precision)
    (hvnum : bo.vNum = Zpower_nat radix precision)
    (hbucket_abs : |bucket| ≤ Zpower_nat radix precision)
    (hexp : -bo.dExp ≤ (precision : Int) + e) :
    ∃ candidate : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo candidate ∧
      _root_.F2R (beta:=beta) candidate =
        _root_.F2R (beta:=beta)
          (⟨bucket * Zpower_nat radix precision, e⟩ :
            FloatSpec.Core.Defs.FlocqFloat beta) := by
  have hmbound := FboundedMbound (beta:=beta) bo radix precision
    ((precision : Int) + e) bucket
  have hcandidate :
      ∃ candidate : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo candidate ∧
        _root_.F2R (beta:=beta) candidate =
          (bucket : ℝ) * (radix : ℝ) ^ ((precision : Int) + e) := by
    simpa [wp, PostCond.noThrow, pure, FboundedMbound_check, Id.run,
      ULift.up_down] using
      hmbound ⟨hbeta, hradix, hprecision, hvnum, hbucket_abs, hexp⟩
  rcases hcandidate with ⟨candidate, hcandidate_bounded, hcandidate_val⟩
  subst beta
  refine ⟨candidate, hcandidate_bounded, ?_⟩
  rw [hcandidate_val]
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (lt_trans (by decide : (0 : Int) < 1) hradix)
  have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
  have hpow_cast :
      ((Zpower_nat radix precision : Int) : ℝ) =
        (radix : ℝ) ^ (precision : Int) := by
    unfold Zpower_nat
    rw [Int.cast_pow]
    simp
  simp [_root_.F2R, FloatSpec.Core.Defs.F2R, Int.cast_mul, hpow_cast,
    zpow_add₀ hradix_ne]
  ring

/-- Product mantissa bound used before applying quotient estimates.

If both input mantissas are strictly bounded by `n`, then their product is
bounded by the square bucket size. -/
theorem abs_mul_le_square_of_abs_lt (a b n : Int)
    (hn : 0 ≤ n)
    (ha : |a| < n)
    (hb : |b| < n) :
    |a * b| ≤ n * n := by
  rw [abs_mul]
  exact mul_le_mul (le_of_lt ha) (le_of_lt hb) (abs_nonneg b) hn

/-- Quotient-size arithmetic core for `errorBoundedMultMin/Max`.

This is the integer cancellation step needed to feed `FboundedMbound`: if the
scaled quotient bucket is no larger than a product bounded by `n*n`, then the
quotient itself is bounded by `n`. -/
theorem quotient_abs_le_of_mul_abs_le_square
    (quot n prod : Int)
    (hnpos : 0 < n)
    (hquot_abs : |quot * n| ≤ |prod|)
    (hprod_abs : |prod| ≤ n * n) :
    |quot| ≤ n := by
  have hn_nonneg : 0 ≤ n := le_of_lt hnpos
  have hqn : |quot * n| = |quot| * n := by
    rw [abs_mul, abs_of_nonneg hn_nonneg]
  have hmul : |quot| * n ≤ n * n := by
    calc
      |quot| * n = |quot * n| := hqn.symm
      _ ≤ |prod| := hquot_abs
      _ ≤ n * n := hprod_abs
  exact le_of_mul_le_mul_right hmul hnpos

/-- `ZquotientProp`-backed quotient-size bound for the multiplication-error
bucket candidate. -/
theorem Zquotient_abs_le_of_product_abs_le_square
    (prod n : Int)
    (hnpos : 0 < n)
    (hprod_abs : |prod| ≤ n * n) :
    |Zquotient prod n| ≤ n := by
  have hn_ne : n ≠ 0 := ne_of_gt hnpos
  have hspec := ZquotientProp prod n
  have hquot :
      ∃ rem : Int,
        prod = Zquotient prod n * n + rem ∧
        |Zquotient prod n * n| ≤ |prod| ∧
        |rem| < |n| := by
    simpa [wp, PostCond.noThrow, pure, ZquotientProp_check, Id.run,
      ULift.up_down] using hspec hn_ne
  rcases hquot with ⟨rem, hdecomp, hquot_abs, hrem_abs⟩
  exact quotient_abs_le_of_mul_abs_le_square
    (Zquotient prod n) n prod hnpos hquot_abs hprod_abs

/-- Nonzero-remainder max-side quotient-size bound.

In the upper branch of Coq `errorBoundedMultMax`, the candidate bucket is the
successor quotient.  The positive remainder forces the quotient to be strictly
below `n`, so the successor is still a bounded mantissa. -/
theorem Zquotient_succ_abs_le_of_product_abs_le_square
    (prod n : Int)
    (hnpos : 0 < n)
    (hprod_nonneg : 0 ≤ prod)
    (hprod_abs : |prod| ≤ n * n)
    (hrem_pos : 0 < prod - Zquotient prod n * n) :
    |Zquotient prod n + 1| ≤ n := by
  have hprod_le : prod ≤ n * n := by
    simpa [abs_of_nonneg hprod_nonneg] using hprod_abs
  have hquot_nonneg : 0 ≤ Zquotient prod n := by
    have h := ZquotientPos prod n
    simpa [wp, PostCond.noThrow, pure, ZquotientPos_check, Id.run,
      ULift.up_down] using h ⟨hprod_nonneg, le_of_lt hnpos⟩
  have hquot_mul_lt_prod : Zquotient prod n * n < prod := by
    omega
  have hquot_lt_n : Zquotient prod n < n := by
    nlinarith
  have hsucc_nonneg : 0 ≤ Zquotient prod n + 1 := by
    omega
  rw [abs_of_nonneg hsucc_nonneg]
  omega

/-- Truncated-division remainder is nonnegative for nonnegative products.

This is the sign fact needed to split the max-side quotient branch into the
exact-zero and strictly-positive remainder cases. -/
theorem Zquotient_remainder_nonneg_of_nonneg
    (prod n : Int)
    (hprod_nonneg : 0 ≤ prod) :
    0 ≤ prod - Zquotient prod n * n := by
  rw [Zquotient_eq_tdiv]
  have hdecomp : prod = prod.tdiv n * n + prod.tmod n := by
    calc
      prod = n * prod.tdiv n + prod.tmod n := (Int.mul_tdiv_add_tmod prod n).symm
      _ = prod.tdiv n * n + prod.tmod n := by ring
  have hmod_nonneg : 0 ≤ prod.tmod n := Int.tmod_nonneg n hprod_nonneg
  omega

/-- If `prod` is nonnegative, an absolute bound by `prod` gives an upper bound
on the signed bucket. -/
theorem bucket_le_product_of_abs_le_nonneg
    (bucket prod : Int)
    (hprod_nonneg : 0 ≤ prod)
    (habs : |bucket| ≤ |prod|) :
    bucket ≤ prod := by
  have habs' : |bucket| ≤ prod := by
    simpa [abs_of_nonneg hprod_nonneg] using habs
  exact (abs_le.mp habs').2

/-- Min-side real comparison between a quotient bucket and the exact product. -/
theorem Zquotient_bucket_F2R_le_product {beta : Int} [ValidRadix beta]
    (radix : Int) (prod n e : Int)
    (hbeta : beta = radix)
    (hradix : 0 < radix)
    (hprod_nonneg : 0 ≤ prod)
    (hquot_abs : |Zquotient prod n * n| ≤ |prod|) :
    _root_.F2R (beta:=beta)
        (⟨Zquotient prod n * n, e⟩ :
          FloatSpec.Core.Defs.FlocqFloat beta) ≤
      _root_.F2R (beta:=beta)
        (⟨prod, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta) := by
  have hbucket_le :
      Zquotient prod n * n ≤ prod :=
    bucket_le_product_of_abs_le_nonneg
      (Zquotient prod n * n) prod hprod_nonneg hquot_abs
  exact F2R_le_of_same_exp_mantissa_le (beta:=beta)
    radix (Zquotient prod n * n) prod e hbeta hradix hbucket_le

/-- Max-side integer comparison from quotient decomposition. -/
theorem product_le_next_quotient_bucket_from_decomp
    (prod n quot rem : Int)
    (hdecomp : prod = quot * n + rem)
    (hrem_lt : rem < n) :
    prod ≤ (quot + 1) * n := by
  have hbucket : (quot + 1) * n = quot * n + n := by ring
  omega

/-- Max-side real comparison between the exact product and the next quotient
bucket. -/
theorem product_F2R_le_next_Zquotient_bucket {beta : Int} [ValidRadix beta]
    (radix : Int) (prod n rem e : Int)
    (hbeta : beta = radix)
    (hradix : 0 < radix)
    (hdecomp : prod = Zquotient prod n * n + rem)
    (hrem_lt : rem < n) :
    _root_.F2R (beta:=beta)
        (⟨prod, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta) ≤
      _root_.F2R (beta:=beta)
        (⟨(Zquotient prod n + 1) * n, e⟩ :
          FloatSpec.Core.Defs.FlocqFloat beta) := by
  have hprod_le :
      prod ≤ (Zquotient prod n + 1) * n :=
    product_le_next_quotient_bucket_from_decomp
      prod n (Zquotient prod n) rem hdecomp hrem_lt
  exact F2R_le_of_same_exp_mantissa_le (beta:=beta)
    radix prod ((Zquotient prod n + 1) * n) e hbeta hradix hprod_le

/-- Min-side strict mantissa bound assembled from the quotient bucket stack.

This packages the core remaining arithmetic of Coq `errorBoundedMultMin` once
the rounded product has been represented at the multiplication exponent. -/
theorem errorBoundedMultMin_mantissa_bound_from_quotient {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (prod e m : Int) (fmin : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix)
    (hradix : 1 < radix)
    (hprecision : 0 < precision)
    (hvnum : bo.vNum = Zpower_nat radix precision)
    (hprod_nonneg : 0 ≤ prod)
    (hprod_abs : |prod| ≤ Zpower_nat radix precision * Zpower_nat radix precision)
    (hexp : -bo.dExp ≤ e)
    (hmin :
      isMin (beta:=beta) bo radix
        (_root_.F2R (beta:=beta)
          (⟨prod, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta)) fmin)
    (hrepr :
      _root_.F2R (beta:=beta) fmin =
        _root_.F2R (beta:=beta)
          (⟨m, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta)) :
    |prod - m| < bo.vNum := by
  let n : Int := Zpower_nat radix precision
  have hradix_pos : 0 < radix := lt_trans (by decide : (0 : Int) < 1) hradix
  have hnpos : 0 < n := by
    dsimp [n, Zpower_nat]
    exact pow_pos hradix_pos precision
  have hquot_size : |Zquotient prod n| ≤ n := by
    exact Zquotient_abs_le_of_product_abs_le_square prod n hnpos (by
      simpa [n] using hprod_abs)
  have hexp_shift : -bo.dExp ≤ (precision : Int) + e := by
    have hprec_nonneg : (0 : Int) ≤ (precision : Int) := Int.natCast_nonneg _
    omega
  rcases FboundedMbound_zpower_bucket_candidate (beta:=beta)
      bo radix precision (Zquotient prod n) e hbeta hradix hprecision hvnum
      (by simpa [n] using hquot_size) (by simpa using hexp_shift) with
    ⟨candidate, hcandidate_bounded, hcandidate_repr⟩
  have hn_ne : n ≠ 0 := ne_of_gt hnpos
  have hspec := ZquotientProp prod n
  have hquot :
      ∃ rem : Int,
        prod = Zquotient prod n * n + rem ∧
        |Zquotient prod n * n| ≤ |prod| ∧
        |rem| < |n| := by
    simpa [wp, PostCond.noThrow, pure, ZquotientProp_check, Id.run,
      ULift.up_down] using hspec hn_ne
  rcases hquot with ⟨rem, hdecomp, hquot_abs, hrem_abs⟩
  have hbucket_le_prod_real :
      _root_.F2R (beta:=beta)
          (⟨Zquotient prod n * n, e⟩ :
            FloatSpec.Core.Defs.FlocqFloat beta) ≤
        _root_.F2R (beta:=beta)
          (⟨prod, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta) := by
    exact Zquotient_bucket_F2R_le_product (beta:=beta) radix prod n e
      hbeta hradix_pos hprod_nonneg hquot_abs
  have hcandidate_le_prod :
      _root_.F2R (beta:=beta) candidate ≤
        _root_.F2R (beta:=beta)
          (⟨prod, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta) := by
    rw [hcandidate_repr]
    exact hbucket_le_prod_real
  have hquot_le_m :
      Zquotient prod n * n ≤ m := by
    exact errorBoundedMultMin_bucket_le_from_isMin_candidate_repr (beta:=beta)
      bo radix
      (_root_.F2R (beta:=beta)
        (⟨prod, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta))
      fmin candidate (Zquotient prod n * n) m e hbeta hradix_pos hmin hrepr
      hcandidate_repr (by simpa [Fbounded'] using hcandidate_bounded)
      hcandidate_le_prod
  have hm_le_prod : m ≤ prod := by
    rcases hmin with ⟨_hfmin_bounded, hfmin_le_prod, _hglb⟩
    exact F2R_le_same_exp_mantissa_le_of_left_repr (beta:=beta)
      radix fmin m prod e hbeta hradix_pos hrepr hfmin_le_prod
  have hrem_lt_n : rem < n := by
    have hrem_le_abs : rem ≤ |rem| := le_abs_self rem
    have hrem_abs_lt_n : |rem| < n := by
      simpa [abs_of_nonneg (le_of_lt hnpos)] using hrem_abs
    exact lt_of_le_of_lt hrem_le_abs hrem_abs_lt_n
  have hbound : |prod - m| < n := by
    have hdiff_nonneg : 0 ≤ prod - m := by omega
    have hdiff_le_rem : prod - m ≤ rem := by omega
    rw [abs_of_nonneg hdiff_nonneg]
    exact lt_of_le_of_lt hdiff_le_rem hrem_lt_n
  simpa [n, hvnum] using hbound

/-- Exact-remainder max-side mantissa bound.

When the product is exactly a quotient bucket, that bucket is a bounded upper
candidate equal to the product itself.  The `isMax` upper-bound property and
the defining lower inequality therefore force the represented max mantissa to
equal the product mantissa. -/
theorem errorBoundedMultMax_mantissa_bound_of_exact_quotient {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (prod e m : Int) (fmax : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix)
    (hradix : 1 < radix)
    (hprecision : 0 < precision)
    (hvnum : bo.vNum = Zpower_nat radix precision)
    (hprod_nonneg : 0 ≤ prod)
    (hprod_abs : |prod| ≤ Zpower_nat radix precision * Zpower_nat radix precision)
    (hexp : -bo.dExp ≤ e)
    (hrem_zero :
      prod -
        Zquotient prod (Zpower_nat radix precision) *
          Zpower_nat radix precision = 0)
    (hmax :
      isMax (beta:=beta) bo radix
        (_root_.F2R (beta:=beta)
          (⟨prod, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta)) fmax)
    (hrepr :
      _root_.F2R (beta:=beta) fmax =
        _root_.F2R (beta:=beta)
          (⟨m, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta)) :
    |prod - m| < bo.vNum := by
  let n : Int := Zpower_nat radix precision
  have hradix_pos : 0 < radix := lt_trans (by decide : (0 : Int) < 1) hradix
  have hnpos : 0 < n := by
    dsimp [n, Zpower_nat]
    exact pow_pos hradix_pos precision
  have hquot_size : |Zquotient prod n| ≤ n := by
    exact Zquotient_abs_le_of_product_abs_le_square prod n hnpos (by
      simpa [n] using hprod_abs)
  have hexp_shift : -bo.dExp ≤ (precision : Int) + e := by
    have hprec_nonneg : (0 : Int) ≤ (precision : Int) := Int.natCast_nonneg _
    omega
  rcases FboundedMbound_zpower_bucket_candidate (beta:=beta)
      bo radix precision (Zquotient prod n) e hbeta hradix hprecision hvnum
      (by simpa [n] using hquot_size) (by simpa using hexp_shift) with
    ⟨candidate, hcandidate_bounded, hcandidate_repr⟩
  have hrem_zero_n : prod - Zquotient prod n * n = 0 := by
    simpa [n] using hrem_zero
  have hbucket_eq_prod : Zquotient prod n * n = prod := by
    omega
  have hprod_le_candidate :
      _root_.F2R (beta:=beta)
          (⟨prod, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta) ≤
        _root_.F2R (beta:=beta) candidate := by
    have hcandidate_eq :
        _root_.F2R (beta:=beta) candidate =
          _root_.F2R (beta:=beta)
            (⟨prod, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta) := by
      simpa [n, hbucket_eq_prod] using hcandidate_repr
    exact le_of_eq hcandidate_eq.symm
  have hm_le_prod : m ≤ prod := by
    have hm_le_bucket :
        m ≤ Zquotient prod n * n := by
      exact errorBoundedMultMax_bucket_le_from_isMax_candidate_repr (beta:=beta)
        bo radix
        (_root_.F2R (beta:=beta)
          (⟨prod, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta))
        fmax candidate m (Zquotient prod n * n) e hbeta hradix_pos
        hmax hrepr hcandidate_repr (by simpa [Fbounded'] using hcandidate_bounded)
        hprod_le_candidate
    simpa [hbucket_eq_prod] using hm_le_bucket
  have hprod_le_m : prod ≤ m := by
    rcases hmax with ⟨_hfmax_bounded, hprod_le_fmax, _hlub⟩
    exact F2R_le_same_exp_mantissa_le_of_right_repr (beta:=beta)
      radix fmax prod m e hbeta hradix_pos hrepr hprod_le_fmax
  have hzero : |prod - m| = 0 := by
    have hdiff : prod - m = 0 := by omega
    simp [hdiff]
  have hbo_pos : 0 < bo.vNum := by
    simpa [n, hvnum] using hnpos
  simpa [hzero] using hbo_pos

/-- Integer quotient core for Coq `errorBoundedMultMin`.

Once the product mantissa has been decomposed as `quot * n + rem`, the min-side
argument only needs the lower rounded mantissa to sit between `quot * n` and
the exact product.  The strict error bound then follows from the quotient
remainder bound. -/
theorem errorBoundedMultMin_quotient_error_bound_from_decomp
    (prod m n quot rem : Int)
    (hdecomp : prod = quot * n + rem)
    (hrem_lt : rem < n)
    (hm_le_prod : m ≤ prod)
    (hquot_le_m : quot * n ≤ m) :
    |prod - m| < n := by
  have hdiff_nonneg : 0 ≤ prod - m := by omega
  have hdiff_le_rem : prod - m ≤ rem := by omega
  rw [abs_of_nonneg hdiff_nonneg]
  exact lt_of_le_of_lt hdiff_le_rem hrem_lt

/-- Coq `errorBoundedMultMin` quotient estimate, using `ZquotientProp`.

This packages the exact integer division step used by the min-side
multiplication-error proof: the quotient decomposition supplies the remainder
bound, while the rounded mantissa lies between the quotient bucket and the
exact product. -/
theorem errorBoundedMultMin_quotient_error_bound
    (prod m n : Int)
    (hnpos : 0 < n)
    (hm_le_prod : m ≤ prod)
    (hquot_le_m : Zquotient prod n * n ≤ m) :
    |prod - m| < n := by
  have hn_ne : n ≠ 0 := ne_of_gt hnpos
  have hspec := ZquotientProp prod n
  have hquot :
      ∃ rem : Int,
        prod = Zquotient prod n * n + rem ∧
        |Zquotient prod n * n| ≤ |prod| ∧
        |rem| < |n| := by
    simpa [wp, PostCond.noThrow, pure, ZquotientProp_check, Id.run,
      ULift.up_down] using hspec hn_ne
  rcases hquot with ⟨rem, hdecomp, _habs_bucket, hrem_abs_lt⟩
  have hrem_lt_n : rem < n := by
    have hrem_le_abs : rem ≤ |rem| := le_abs_self rem
    have hrem_abs_lt_n : |rem| < n := by
      simpa [abs_of_nonneg (le_of_lt hnpos)] using hrem_abs_lt
    exact lt_of_le_of_lt hrem_le_abs hrem_abs_lt_n
  exact errorBoundedMultMin_quotient_error_bound_from_decomp
    prod m n (Zquotient prod n) rem hdecomp hrem_lt_n hm_le_prod
    hquot_le_m

/-- Integer quotient core for the nonzero branch of Coq `errorBoundedMultMax`.

The zero-remainder max branch is exact in Flocq.  In the remaining branch,
`0 < rem`, the upper rounded mantissa is at most the next quotient bucket, so
the distance from the product is strictly below one bucket width. -/
theorem errorBoundedMultMax_quotient_error_bound_from_decomp
    (prod m n quot rem : Int)
    (hdecomp : prod = quot * n + rem)
    (hrem_pos : 0 < rem)
    (hrem_lt : rem < n)
    (hprod_le_m : prod ≤ m)
    (hm_le_next : m ≤ (quot + 1) * n) :
    |prod - m| < n := by
  have hdiff_nonpos : prod - m ≤ 0 := by omega
  have hm_le_bucket : m ≤ quot * n + n := by
    calc
      m ≤ (quot + 1) * n := hm_le_next
      _ = quot * n + n := by ring
  have hdiff_lt : m - prod < n := by omega
  rw [abs_of_nonpos hdiff_nonpos]
  omega

/-- Coq `errorBoundedMultMax` quotient estimate, using `ZquotientProp`.

This is the nonzero-remainder branch.  The exact-zero remainder branch in
Flocq returns a zero error; when the remainder is positive, the upper rounded
mantissa is bounded by the next quotient bucket. -/
theorem errorBoundedMultMax_quotient_error_bound
    (prod m n : Int)
    (hnpos : 0 < n)
    (hrem_pos : 0 < prod - Zquotient prod n * n)
    (hprod_le_m : prod ≤ m)
    (hm_le_next : m ≤ (Zquotient prod n + 1) * n) :
    |prod - m| < n := by
  have hn_ne : n ≠ 0 := ne_of_gt hnpos
  have hspec := ZquotientProp prod n
  have hquot :
      ∃ rem : Int,
        prod = Zquotient prod n * n + rem ∧
        |Zquotient prod n * n| ≤ |prod| ∧
        |rem| < |n| := by
    simpa [wp, PostCond.noThrow, pure, ZquotientProp_check, Id.run,
      ULift.up_down] using hspec hn_ne
  rcases hquot with ⟨rem, hdecomp, _habs_bucket, hrem_abs_lt⟩
  have hrem_eq : rem = prod - Zquotient prod n * n := by omega
  have hrem_pos' : 0 < rem := by omega
  have hrem_lt_n : rem < n := by
    have hrem_le_abs : rem ≤ |rem| := le_abs_self rem
    have hrem_abs_lt_n : |rem| < n := by
      simpa [abs_of_nonneg (le_of_lt hnpos)] using hrem_abs_lt
    exact lt_of_le_of_lt hrem_le_abs hrem_abs_lt_n
  exact errorBoundedMultMax_quotient_error_bound_from_decomp
    prod m n (Zquotient prod n) rem hdecomp hrem_pos' hrem_lt_n
    hprod_le_m hm_le_next

/-- Max-side strict mantissa bound assembled from the successor quotient bucket.

This is the nonzero-remainder companion of
`errorBoundedMultMin_mantissa_bound_from_quotient`: the rounded product is
represented at the multiplication exponent, the successor quotient bucket is
shown bounded, and the `isMax` extremum property gives the upper mantissa
inequality needed by the quotient error estimate. -/
theorem errorBoundedMultMax_mantissa_bound_from_quotient {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (prod e m : Int) (fmax : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix)
    (hradix : 1 < radix)
    (hprecision : 0 < precision)
    (hvnum : bo.vNum = Zpower_nat radix precision)
    (hprod_nonneg : 0 ≤ prod)
    (hprod_abs : |prod| ≤ Zpower_nat radix precision * Zpower_nat radix precision)
    (hexp : -bo.dExp ≤ e)
    (hrem_pos :
      0 < prod -
        Zquotient prod (Zpower_nat radix precision) *
          Zpower_nat radix precision)
    (hmax :
      isMax (beta:=beta) bo radix
        (_root_.F2R (beta:=beta)
          (⟨prod, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta)) fmax)
    (hrepr :
      _root_.F2R (beta:=beta) fmax =
        _root_.F2R (beta:=beta)
          (⟨m, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta)) :
    |prod - m| < bo.vNum := by
  let n : Int := Zpower_nat radix precision
  have hradix_pos : 0 < radix := lt_trans (by decide : (0 : Int) < 1) hradix
  have hnpos : 0 < n := by
    dsimp [n, Zpower_nat]
    exact pow_pos hradix_pos precision
  have hnext_size : |Zquotient prod n + 1| ≤ n := by
    exact Zquotient_succ_abs_le_of_product_abs_le_square prod n hnpos
      hprod_nonneg (by simpa [n] using hprod_abs) (by simpa [n] using hrem_pos)
  have hexp_shift : -bo.dExp ≤ (precision : Int) + e := by
    have hprec_nonneg : (0 : Int) ≤ (precision : Int) := Int.natCast_nonneg _
    omega
  rcases FboundedMbound_zpower_bucket_candidate (beta:=beta)
      bo radix precision (Zquotient prod n + 1) e hbeta hradix hprecision
      hvnum (by simpa [n] using hnext_size) (by simpa using hexp_shift) with
    ⟨candidate, hcandidate_bounded, hcandidate_repr⟩
  have hn_ne : n ≠ 0 := ne_of_gt hnpos
  have hspec := ZquotientProp prod n
  have hquot :
      ∃ rem : Int,
        prod = Zquotient prod n * n + rem ∧
        |Zquotient prod n * n| ≤ |prod| ∧
        |rem| < |n| := by
    simpa [wp, PostCond.noThrow, pure, ZquotientProp_check, Id.run,
      ULift.up_down] using hspec hn_ne
  rcases hquot with ⟨rem, hdecomp, _hquot_abs, hrem_abs⟩
  have hrem_lt_n : rem < n := by
    have hrem_le_abs : rem ≤ |rem| := le_abs_self rem
    have hrem_abs_lt_n : |rem| < n := by
      simpa [abs_of_nonneg (le_of_lt hnpos)] using hrem_abs
    exact lt_of_le_of_lt hrem_le_abs hrem_abs_lt_n
  have hprod_le_next_real :
      _root_.F2R (beta:=beta)
          (⟨prod, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta) ≤
        _root_.F2R (beta:=beta)
          (⟨(Zquotient prod n + 1) * n, e⟩ :
            FloatSpec.Core.Defs.FlocqFloat beta) := by
    exact product_F2R_le_next_Zquotient_bucket (beta:=beta)
      radix prod n rem e hbeta hradix_pos hdecomp hrem_lt_n
  have hprod_le_candidate :
      _root_.F2R (beta:=beta)
          (⟨prod, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta) ≤
        _root_.F2R (beta:=beta) candidate := by
    rw [hcandidate_repr]
    exact hprod_le_next_real
  have hm_le_next :
      m ≤ (Zquotient prod n + 1) * n := by
    exact errorBoundedMultMax_bucket_le_from_isMax_candidate_repr (beta:=beta)
      bo radix
      (_root_.F2R (beta:=beta)
        (⟨prod, e⟩ : FloatSpec.Core.Defs.FlocqFloat beta))
      fmax candidate m ((Zquotient prod n + 1) * n) e hbeta hradix_pos
      hmax hrepr hcandidate_repr (by simpa [Fbounded'] using hcandidate_bounded)
      hprod_le_candidate
  have hprod_le_m : prod ≤ m := by
    rcases hmax with ⟨_hfmax_bounded, hprod_le_fmax, _hlub⟩
    exact F2R_le_same_exp_mantissa_le_of_right_repr (beta:=beta)
      radix fmax prod m e hbeta hradix_pos hrepr hprod_le_fmax
  have hbound : |prod - m| < n := by
    exact errorBoundedMultMax_quotient_error_bound prod m n hnpos
      (by simpa [n] using hrem_pos) hprod_le_m hm_le_next
  simpa [n, hvnum] using hbound

noncomputable def errorBoundedMultMin_from_quotient_bound_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (p q fmin : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Factored `errorBoundedMultMin` payload after the quotient arithmetic.

Flocq's `errorBoundedMultMin` first represents the lower rounded product at
the exponent of `Fmult p q`, then proves a strict mantissa-error bound by
integer quotient arithmetic.  This helper discharges everything except that
last quotient bound. -/
theorem errorBoundedMultMin_from_quotient_bound {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (p q fmin : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        bo.vNum = Zpower_nat radix precision ∧
        Fbounded (beta:=beta) bo p ∧
        Fbounded (beta:=beta) bo q ∧
        0 ≤ _root_.F2R (beta:=beta) p ∧
        0 ≤ _root_.F2R (beta:=beta) q ∧
        -bo.dExp ≤ p.Fexp + q.Fexp ∧
        isMin (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q) fmin ∧
        (∀ m : Int,
          _root_.F2R (beta:=beta) fmin =
            _root_.F2R (beta:=beta)
              (⟨m, p.Fexp + q.Fexp⟩ :
                FloatSpec.Core.Defs.FlocqFloat beta) →
          |p.Fnum * q.Fnum - m| < bo.vNum)⌝⦄
    (pure (errorBoundedMultMin_from_quotient_bound_check
      (beta:=beta) bo radix precision p q fmin) : Id Unit)
    ⦃⇓_ => ⌜∃ error : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) error =
        _root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) fmin ∧
      Fbounded (beta:=beta) bo error ∧
      error.Fexp = p.Fexp + q.Fexp⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hradix, hprecision, hvnum, _hp, _hq, _hp_nonneg, _hq_nonneg,
      hexp, hmin, hquot_bound⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure,
    errorBoundedMultMin_from_quotient_bound_check, Id.run, ULift.up_down]
  have hprod_val :
      _root_.F2R (beta:=radix)
          (FloatSpec.Calc.Operations.Fmult (beta:=radix) p q) =
        _root_.F2R (beta:=radix) p * _root_.F2R (beta:=radix) q := by
    have h := Fmult_correct (beta:=radix) p q
    simpa [wp, PostCond.noThrow, pure, Fmult_correct_check, Id.run,
      ULift.up_down] using h hradix
  have hmin_prod :
      isMin (beta:=radix) bo radix
        (_root_.F2R (beta:=radix)
          (FloatSpec.Calc.Operations.Fmult (beta:=radix) p q)) fmin := by
    rw [hprod_val]
    exact hmin
  have hrep_spec := FminRep (beta:=radix) bo radix precision
    (FloatSpec.Calc.Operations.Fmult (beta:=radix) p q) fmin
  have hrep_exists :
      ∃ m : Int,
        _root_.F2R (beta:=radix) fmin =
          _root_.F2R (beta:=radix)
            (⟨m, p.Fexp + q.Fexp⟩ :
              FloatSpec.Core.Defs.FlocqFloat radix) := by
    have hraw :
        ∃ m : Int,
          _root_.F2R (beta:=radix) fmin =
            _root_.F2R (beta:=radix)
              (⟨m, (FloatSpec.Calc.Operations.Fmult (beta:=radix) p q).Fexp⟩ :
                FloatSpec.Core.Defs.FlocqFloat radix) := by
      simpa [wp, PostCond.noThrow, pure, FminRep_check, Id.run,
        ULift.up_down] using
        hrep_spec ⟨rfl, hradix, hprecision, hvnum, hmin_prod⟩
    simpa [FloatSpec.Calc.Operations.Fmult] using hraw
  rcases hrep_exists with ⟨m, hrepr⟩
  exact errorBoundedMult_from_same_exp_mantissa_bound (beta:=radix)
    bo radix p q fmin m rfl hradix hrepr (hquot_bound m hrepr) hexp

noncomputable def errorBoundedMultMin_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (p q fmin : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq `errorBoundedMultMin`, lower rounded product branch.

The branch is now reduced to the quotient stack:
bounded nonnegative input mantissas give the product-square bound, and
`errorBoundedMultMin_mantissa_bound_from_quotient` supplies the strict
same-exponent mantissa bound required by
`errorBoundedMultMin_from_quotient_bound`. -/
theorem errorBoundedMultMin {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (p q fmin : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        bo.vNum = Zpower_nat radix precision ∧
        Fbounded (beta:=beta) bo p ∧
        Fbounded (beta:=beta) bo q ∧
        0 ≤ _root_.F2R (beta:=beta) p ∧
        0 ≤ _root_.F2R (beta:=beta) q ∧
        -bo.dExp ≤ p.Fexp + q.Fexp ∧
        isMin (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q) fmin⌝⦄
    (pure (errorBoundedMultMin_check
      (beta:=beta) bo radix precision p q fmin) : Id Unit)
    ⦃⇓_ => ⌜∃ error : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) error =
        _root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) fmin ∧
      Fbounded (beta:=beta) bo error ∧
      error.Fexp = p.Fexp + q.Fexp⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hradix, hprecision, hvnum, hp, hq, hp_nonneg, hq_nonneg,
      hexp, hmin⟩
  simp only [wp, PostCond.noThrow, pure, errorBoundedMultMin_check, Id.run,
    ULift.up_down]
  have hprecision_pos : 0 < precision := Nat.pos_of_ne_zero hprecision
  have hbeta_gt : 1 < beta := by omega
  have hp_num_nonneg : 0 ≤ p.Fnum :=
    FloatSpec.Core.Float_prop.ge_0_F2R (beta:=beta) p hbeta_gt hp_nonneg
  have hq_num_nonneg : 0 ≤ q.Fnum :=
    FloatSpec.Core.Float_prop.ge_0_F2R (beta:=beta) q hbeta_gt hq_nonneg
  have hprod_nonneg : 0 ≤ p.Fnum * q.Fnum :=
    mul_nonneg hp_num_nonneg hq_num_nonneg
  have hradix_pos : 0 < radix := lt_trans (by decide : (0 : Int) < 1) hradix
  have hvnum_nonneg : 0 ≤ Zpower_nat radix precision := by
    unfold Zpower_nat
    exact pow_nonneg (le_of_lt hradix_pos) precision
  have hp_abs : |p.Fnum| < Zpower_nat radix precision := by
    simpa [hvnum] using hp.1
  have hq_abs : |q.Fnum| < Zpower_nat radix precision := by
    simpa [hvnum] using hq.1
  have hprod_abs :
      |p.Fnum * q.Fnum| ≤
        Zpower_nat radix precision * Zpower_nat radix precision :=
    abs_mul_le_square_of_abs_lt p.Fnum q.Fnum
      (Zpower_nat radix precision) hvnum_nonneg hp_abs hq_abs
  have hprod_float_val :
      _root_.F2R (beta:=beta)
          (⟨p.Fnum * q.Fnum, p.Fexp + q.Fexp⟩ :
            FloatSpec.Core.Defs.FlocqFloat beta) =
        _root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q := by
    have h := Fmult_correct (beta:=beta) p q
    have hraw :
        _root_.F2R (beta:=beta)
            (FloatSpec.Calc.Operations.Fmult (beta:=beta) p q) =
          _root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q := by
      simpa [wp, PostCond.noThrow, pure, Fmult_correct_check, Id.run,
        ULift.up_down] using h hbeta_gt
    simpa [FloatSpec.Calc.Operations.Fmult] using hraw
  have hmin_prod :
      isMin (beta:=beta) bo radix
        (_root_.F2R (beta:=beta)
          (⟨p.Fnum * q.Fnum, p.Fexp + q.Fexp⟩ :
            FloatSpec.Core.Defs.FlocqFloat beta)) fmin := by
    rw [hprod_float_val]
    exact hmin
  have hquot_bound :
      ∀ m : Int,
        _root_.F2R (beta:=beta) fmin =
          _root_.F2R (beta:=beta)
            (⟨m, p.Fexp + q.Fexp⟩ :
              FloatSpec.Core.Defs.FlocqFloat beta) →
        |p.Fnum * q.Fnum - m| < bo.vNum := by
    intro m hrepr
    exact errorBoundedMultMin_mantissa_bound_from_quotient (beta:=beta)
      bo radix precision (p.Fnum * q.Fnum) (p.Fexp + q.Fexp) m fmin
      hbeta hradix hprecision_pos hvnum hprod_nonneg hprod_abs hexp hmin_prod
      hrepr
  have hbranch := errorBoundedMultMin_from_quotient_bound
    (beta:=beta) bo radix precision p q fmin
  simpa [wp, PostCond.noThrow, pure,
    errorBoundedMultMin_from_quotient_bound_check, Id.run, ULift.up_down, Int.cast_ofNat] using
    hbranch ⟨hbeta, hradix, hprecision, hvnum, hp, hq, hp_nonneg, hq_nonneg,
      hexp, hmin, hquot_bound⟩

noncomputable def errorBoundedMultMax_from_quotient_bound_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (p q fmax : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Factored `errorBoundedMultMax` payload after the quotient arithmetic.

This is the max-side companion of `errorBoundedMultMin_from_quotient_bound`.
The restored `FmaxRep_from_FminRep` bridge supplies the same-exponent
representation of the upper rounded product; the only remaining upstream
payload is the strict quotient/mantissa bound. -/
theorem errorBoundedMultMax_from_quotient_bound {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (p q fmax : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        bo.vNum = Zpower_nat radix precision ∧
        Fbounded (beta:=beta) bo p ∧
        Fbounded (beta:=beta) bo q ∧
        0 ≤ _root_.F2R (beta:=beta) p ∧
        0 ≤ _root_.F2R (beta:=beta) q ∧
        -bo.dExp ≤ p.Fexp + q.Fexp ∧
        isMax (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q) fmax ∧
        (∀ m : Int,
          _root_.F2R (beta:=beta) fmax =
            _root_.F2R (beta:=beta)
              (⟨m, p.Fexp + q.Fexp⟩ :
                FloatSpec.Core.Defs.FlocqFloat beta) →
          |p.Fnum * q.Fnum - m| < bo.vNum)⌝⦄
    (pure (errorBoundedMultMax_from_quotient_bound_check
      (beta:=beta) bo radix precision p q fmax) : Id Unit)
    ⦃⇓_ => ⌜∃ error : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) error =
        _root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) fmax ∧
      Fbounded (beta:=beta) bo error ∧
      error.Fexp = p.Fexp + q.Fexp⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hradix, hprecision, hvnum, _hp, _hq, _hp_nonneg, _hq_nonneg,
      hexp, hmax, hquot_bound⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure,
    errorBoundedMultMax_from_quotient_bound_check, Id.run, ULift.up_down]
  have hprod_val :
      _root_.F2R (beta:=radix)
          (FloatSpec.Calc.Operations.Fmult (beta:=radix) p q) =
        _root_.F2R (beta:=radix) p * _root_.F2R (beta:=radix) q := by
    have h := Fmult_correct (beta:=radix) p q
    simpa [wp, PostCond.noThrow, pure, Fmult_correct_check, Id.run,
      ULift.up_down] using h hradix
  have hmax_prod :
      isMax (beta:=radix) bo radix
        (_root_.F2R (beta:=radix)
          (FloatSpec.Calc.Operations.Fmult (beta:=radix) p q)) fmax := by
    rw [hprod_val]
    exact hmax
  have hMinRep :
      ∀ src dst : FloatSpec.Core.Defs.FlocqFloat radix,
        isMin (beta:=radix) bo radix (_root_.F2R (beta:=radix) src) dst →
          ∃ m : Int,
            _root_.F2R (beta:=radix) dst =
              _root_.F2R (beta:=radix) ⟨m, src.Fexp⟩ := by
    intro src dst hmin_src
    have h := FminRep (beta:=radix) bo radix precision src dst
    simpa [wp, PostCond.noThrow, pure, FminRep_check, Id.run,
      ULift.up_down] using h ⟨rfl, hradix, hprecision, hvnum, hmin_src⟩
  have hMaxRep :
      ∀ src dst : FloatSpec.Core.Defs.FlocqFloat radix,
        isMax (beta:=radix) bo radix (_root_.F2R (beta:=radix) src) dst →
          ∃ m : Int,
            _root_.F2R (beta:=radix) dst =
              _root_.F2R (beta:=radix) ⟨m, src.Fexp⟩ := by
    have h := FmaxRep_from_FminRep (beta:=radix) bo radix
    simpa [wp, PostCond.noThrow, pure, FmaxRep_from_FminRep_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hMinRep
  have hrep_exists :
      ∃ m : Int,
        _root_.F2R (beta:=radix) fmax =
          _root_.F2R (beta:=radix)
            (⟨m, p.Fexp + q.Fexp⟩ :
              FloatSpec.Core.Defs.FlocqFloat radix) := by
    have hraw := hMaxRep
      (FloatSpec.Calc.Operations.Fmult (beta:=radix) p q) fmax hmax_prod
    simpa [FloatSpec.Calc.Operations.Fmult] using hraw
  rcases hrep_exists with ⟨m, hrepr⟩
  exact errorBoundedMult_from_same_exp_mantissa_bound (beta:=radix)
    bo radix p q fmax m rfl hradix hrepr (hquot_bound m hrepr) hexp

noncomputable def errorBoundedMultMax_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (p q fmax : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq `errorBoundedMultMax`, upper rounded product branch.

The zero-remainder branch is exact; the nonzero branch uses the successor
quotient bucket assembled in `errorBoundedMultMax_mantissa_bound_from_quotient`.
-/
theorem errorBoundedMultMax {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (p q fmax : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        bo.vNum = Zpower_nat radix precision ∧
        Fbounded (beta:=beta) bo p ∧
        Fbounded (beta:=beta) bo q ∧
        0 ≤ _root_.F2R (beta:=beta) p ∧
        0 ≤ _root_.F2R (beta:=beta) q ∧
        -bo.dExp ≤ p.Fexp + q.Fexp ∧
        isMax (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q) fmax⌝⦄
    (pure (errorBoundedMultMax_check
      (beta:=beta) bo radix precision p q fmax) : Id Unit)
    ⦃⇓_ => ⌜∃ error : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) error =
        _root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) fmax ∧
      Fbounded (beta:=beta) bo error ∧
      error.Fexp = p.Fexp + q.Fexp⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hradix, hprecision, hvnum, hp, hq, hp_nonneg, hq_nonneg,
      hexp, hmax⟩
  simp only [wp, PostCond.noThrow, pure, errorBoundedMultMax_check, Id.run,
    ULift.up_down]
  have hprecision_pos : 0 < precision := Nat.pos_of_ne_zero hprecision
  have hbeta_gt : 1 < beta := by omega
  have hp_num_nonneg : 0 ≤ p.Fnum :=
    FloatSpec.Core.Float_prop.ge_0_F2R (beta:=beta) p hbeta_gt hp_nonneg
  have hq_num_nonneg : 0 ≤ q.Fnum :=
    FloatSpec.Core.Float_prop.ge_0_F2R (beta:=beta) q hbeta_gt hq_nonneg
  have hprod_nonneg : 0 ≤ p.Fnum * q.Fnum :=
    mul_nonneg hp_num_nonneg hq_num_nonneg
  have hradix_pos : 0 < radix := lt_trans (by decide : (0 : Int) < 1) hradix
  have hvnum_nonneg : 0 ≤ Zpower_nat radix precision := by
    unfold Zpower_nat
    exact pow_nonneg (le_of_lt hradix_pos) precision
  have hp_abs : |p.Fnum| < Zpower_nat radix precision := by
    simpa [hvnum] using hp.1
  have hq_abs : |q.Fnum| < Zpower_nat radix precision := by
    simpa [hvnum] using hq.1
  have hprod_abs :
      |p.Fnum * q.Fnum| ≤
        Zpower_nat radix precision * Zpower_nat radix precision :=
    abs_mul_le_square_of_abs_lt p.Fnum q.Fnum
      (Zpower_nat radix precision) hvnum_nonneg hp_abs hq_abs
  have hprod_float_val :
      _root_.F2R (beta:=beta)
          (⟨p.Fnum * q.Fnum, p.Fexp + q.Fexp⟩ :
            FloatSpec.Core.Defs.FlocqFloat beta) =
        _root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q := by
    have h := Fmult_correct (beta:=beta) p q
    have hraw :
        _root_.F2R (beta:=beta)
            (FloatSpec.Calc.Operations.Fmult (beta:=beta) p q) =
          _root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q := by
      simpa [wp, PostCond.noThrow, pure, Fmult_correct_check, Id.run,
        ULift.up_down] using h hbeta_gt
    simpa [FloatSpec.Calc.Operations.Fmult] using hraw
  have hmax_prod :
      isMax (beta:=beta) bo radix
        (_root_.F2R (beta:=beta)
          (⟨p.Fnum * q.Fnum, p.Fexp + q.Fexp⟩ :
            FloatSpec.Core.Defs.FlocqFloat beta)) fmax := by
    rw [hprod_float_val]
    exact hmax
  have hquot_bound :
      ∀ m : Int,
        _root_.F2R (beta:=beta) fmax =
          _root_.F2R (beta:=beta)
            (⟨m, p.Fexp + q.Fexp⟩ :
              FloatSpec.Core.Defs.FlocqFloat beta) →
        |p.Fnum * q.Fnum - m| < bo.vNum := by
    intro m hrepr
    by_cases hrem_zero :
        p.Fnum * q.Fnum -
          Zquotient (p.Fnum * q.Fnum) (Zpower_nat radix precision) *
            Zpower_nat radix precision = 0
    · exact errorBoundedMultMax_mantissa_bound_of_exact_quotient (beta:=beta)
        bo radix precision (p.Fnum * q.Fnum) (p.Fexp + q.Fexp) m fmax
        hbeta hradix hprecision_pos hvnum hprod_nonneg hprod_abs hexp
        hrem_zero hmax_prod hrepr
    · have hrem_nonneg :
          0 ≤ p.Fnum * q.Fnum -
            Zquotient (p.Fnum * q.Fnum) (Zpower_nat radix precision) *
              Zpower_nat radix precision :=
        Zquotient_remainder_nonneg_of_nonneg (p.Fnum * q.Fnum)
          (Zpower_nat radix precision) hprod_nonneg
      have hrem_pos :
          0 < p.Fnum * q.Fnum -
            Zquotient (p.Fnum * q.Fnum) (Zpower_nat radix precision) *
              Zpower_nat radix precision := by
        omega
      exact errorBoundedMultMax_mantissa_bound_from_quotient (beta:=beta)
        bo radix precision (p.Fnum * q.Fnum) (p.Fexp + q.Fexp) m fmax
        hbeta hradix hprecision_pos hvnum hprod_nonneg hprod_abs hexp
        hrem_pos hmax_prod hrepr
  have hbranch := errorBoundedMultMax_from_quotient_bound
    (beta:=beta) bo radix precision p q fmax
  simpa [wp, PostCond.noThrow, pure,
    errorBoundedMultMax_from_quotient_bound_check, Id.run, ULift.up_down, Int.cast_ofNat] using
    hbranch ⟨hbeta, hradix, hprecision, hvnum, hp, hq, hp_nonneg, hq_nonneg,
      hexp, hmax, hquot_bound⟩

noncomputable def multExpUpperBound_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p q pq : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `multExpUpperBound_from_split_payload`.

Any rounded product has a bounded representative whose exponent is no larger
than `precision + p.Fexp + q.Fexp`.  The proof bounds the exact product by the
maximal mantissa at that exponent, transfers the absolute bound through the
rounded-mode predicate, and then normalizes the representative with `eqExpMax`.
-/
theorem multExpUpperBound_from_split_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p q pq : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧ 1 < bo.vNum ∧
        bo.vNum = Zpower_nat radix precision ∧
        RoundedModeP_float (beta:=beta) bo radix P ∧
        Fbounded (beta:=beta) bo p ∧ Fbounded (beta:=beta) bo q ∧
        -bo.dExp ≤ p.Fexp + q.Fexp ∧
        P (_root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q) pq⌝⦄
    (pure (multExpUpperBound_check
      (beta:=beta) bo radix precision P p q pq) : Id Unit)
    ⦃⇓_ => ⌜∃ r : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo r ∧
      _root_.F2R (beta:=beta) r = _root_.F2R (beta:=beta) pq ∧
      r.Fexp ≤ (precision : Int) + (p.Fexp + q.Fexp)⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hradix, hprecision, hvNum_gt, hvNum,
      hRoundedFloat, hp, hq, hexp, hP⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure, multExpUpperBound_check,
    Id.run, ULift.up_down]
  let z : Int := p.Fexp + q.Fexp
  let upper : FloatSpec.Core.Defs.FlocqFloat radix :=
    ⟨bo.vNum - 1, (precision : Int) + z⟩
  have hradix_pos_int : (0 : Int) < radix := by omega
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by exact_mod_cast hradix_pos_int
  have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
  have hupperBounded : Fbounded (beta:=radix) bo upper := by
    constructor
    · have hnonneg : 0 ≤ bo.vNum - 1 := by omega
      rw [abs_of_nonneg hnonneg]
      omega
    · dsimp [upper, z]
      exact le_trans hexp (by omega)
  have hpAbsLe :
      |_root_.F2R (beta:=radix) p| ≤
        _root_.F2R (beta:=radix)
          (⟨bo.vNum - 1, p.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat radix) := by
    have h := maxMax1_from_expanded_bound_payload (beta:=radix) bo p p.Fexp
    simpa [wp, PostCond.noThrow, pure, maxMax1_check, Id.run,
      ULift.up_down] using h ⟨hp, le_rfl, hp, hradix⟩
  have hqAbsLe :
      |_root_.F2R (beta:=radix) q| ≤
        _root_.F2R (beta:=radix)
          (⟨bo.vNum - 1, q.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat radix) := by
    have h := maxMax1_from_expanded_bound_payload (beta:=radix) bo q q.Fexp
    simpa [wp, PostCond.noThrow, pure, maxMax1_check, Id.run,
      ULift.up_down] using h ⟨hq, le_rfl, hq, hradix⟩
  have hbaseP_nonneg :
      0 ≤ _root_.F2R (beta:=radix)
        (⟨bo.vNum - 1, p.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat radix) := by
    have hnum : (0 : Int) ≤ bo.vNum - 1 := by omega
    have hnumR : (0 : ℝ) ≤ ((bo.vNum - 1 : Int) : ℝ) := by exact_mod_cast hnum
    have hpow_nonneg : 0 ≤ (radix : ℝ) ^ p.Fexp :=
      le_of_lt (zpow_pos hradix_pos p.Fexp)
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
      FloatSpec.Core.Defs.FlocqFloat.Fnum,
      FloatSpec.Core.Defs.FlocqFloat.Fexp]
    exact mul_nonneg hnumR hpow_nonneg
  have hprodAbsLeBase :
      |_root_.F2R (beta:=radix) p * _root_.F2R (beta:=radix) q| ≤
        _root_.F2R (beta:=radix)
          (⟨bo.vNum - 1, p.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat radix) *
        _root_.F2R (beta:=radix)
          (⟨bo.vNum - 1, q.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat radix) := by
    rw [abs_mul]
    exact mul_le_mul hpAbsLe hqAbsLe (abs_nonneg _) hbaseP_nonneg
  have hpred_le_pow :
      ((bo.vNum - 1 : Int) : ℝ) ≤ (radix : ℝ) ^ (precision : Int) := by
    have hle_int : bo.vNum - 1 ≤ bo.vNum := by omega
    have hle_real : ((bo.vNum - 1 : Int) : ℝ) ≤ (bo.vNum : ℝ) := by
      exact_mod_cast hle_int
    have hv_cast : (bo.vNum : ℝ) = (radix : ℝ) ^ precision := by
      simp [hvNum, Zpower_nat]
    have hv_cast_int : (bo.vNum : ℝ) = (radix : ℝ) ^ (precision : Int) := by
      rw [hv_cast]
      rw [← zpow_natCast]
    exact le_trans hle_real (le_of_eq hv_cast_int)
  have hbaseProdLeUpper :
      _root_.F2R (beta:=radix)
          (⟨bo.vNum - 1, p.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat radix) *
        _root_.F2R (beta:=radix)
          (⟨bo.vNum - 1, q.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat radix) ≤
        _root_.F2R (beta:=radix) upper := by
    let A : ℝ := ((bo.vNum - 1 : Int) : ℝ)
    have hA_nonneg : 0 ≤ A := by
      have hnonneg : (0 : Int) ≤ bo.vNum - 1 := by omega
      simpa [A] using
        (show (0 : ℝ) ≤ ((bo.vNum - 1 : Int) : ℝ) by exact_mod_cast hnonneg)
    have hpow_z_nonneg : 0 ≤ (radix : ℝ) ^ z :=
      le_of_lt (zpow_pos hradix_pos z)
    have hA_sq_le : A * A ≤ A * (radix : ℝ) ^ (precision : Int) := by
      exact mul_le_mul_of_nonneg_left (by simpa [A] using hpred_le_pow) hA_nonneg
    have hmul_exp :
        (radix : ℝ) ^ p.Fexp * (radix : ℝ) ^ q.Fexp =
          (radix : ℝ) ^ z := by
      rw [← zpow_add₀ hradix_ne]
    have hupper_exp :
        (radix : ℝ) ^ ((precision : Int) + z) =
          (radix : ℝ) ^ (precision : Int) * (radix : ℝ) ^ z := by
      exact zpow_add₀ hradix_ne (precision : Int) z
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
      FloatSpec.Core.Defs.FlocqFloat.Fnum,
      FloatSpec.Core.Defs.FlocqFloat.Fexp, upper]
    calc
      A * (radix : ℝ) ^ p.Fexp * (A * (radix : ℝ) ^ q.Fexp)
          = (A * A) * (radix : ℝ) ^ z := by
            rw [← hmul_exp]
            ring
      _ ≤ (A * (radix : ℝ) ^ (precision : Int)) * (radix : ℝ) ^ z :=
            mul_le_mul_of_nonneg_right hA_sq_le hpow_z_nonneg
      _ = A * (radix : ℝ) ^ ((precision : Int) + z) := by
            rw [hupper_exp]
            ring
  have hprodAbsLeUpper :
      |_root_.F2R (beta:=radix) p * _root_.F2R (beta:=radix) q| ≤
        _root_.F2R (beta:=radix) upper :=
    le_trans hprodAbsLeBase hbaseProdLeUpper
  have hpqAbsLeUpper :
      |_root_.F2R (beta:=radix) pq| ≤ _root_.F2R (beta:=radix) upper := by
    have h := RoundAbsMonotoner_from_rounded_payload (beta:=radix) bo radix P
      (_root_.F2R (beta:=radix) p * _root_.F2R (beta:=radix) q) pq upper
    simpa [wp, PostCond.noThrow, pure, RoundAbsMonotoner_check, Id.run,
      ULift.up_down] using h ⟨hRoundedFloat, hupperBounded, hP, hprodAbsLeUpper⟩
  have hpqBounded : Fbounded (beta:=radix) bo pq := by
    exact RoundedModeP_float_bounded bo radix P hRoundedFloat hP
  have hmax := eqExpMax (beta:=radix) bo radix pq upper
  simpa [wp, PostCond.noThrow, pure, eqExpMax_check, Id.run,
    ULift.up_down, upper, z] using
    hmax ⟨rfl, hradix, hpqBounded, hupperBounded, hpqAbsLeUpper⟩

/-- Coq: `errorBoundedMultExp_aux`.

Normalize a bounded real representation of the integer product `n1 * n2` so
that the resulting exponent lies in `[0, precision]`.  This is the arithmetic
payload used by the multiplication error-bound proof when the rounded product
is already exact. -/
theorem errorBoundedMultExp_aux
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (hradix : 1 < radix)
    (hprecision : precision ≠ 0)
    (hvNum : bo.vNum = Zpower_nat radix precision)
    (n1 n2 : Int)
    (hn1 : |n1| < bo.vNum)
    (hn2 : |n2| < bo.vNum)
    (hrepr : ∃ ny ey : Int,
      ((n1 * n2 : Int) : ℝ) = (ny : ℝ) * (radix : ℝ) ^ ey ∧
      |ny| < bo.vNum) :
    ∃ nx ex : Int,
      ((n1 * n2 : Int) : ℝ) = (nx : ℝ) * (radix : ℝ) ^ ex ∧
      |nx| < bo.vNum ∧ 0 ≤ ex ∧ ex ≤ (precision : Int) := by
  rcases hrepr with ⟨ny, ey, hrepr, hny⟩
  have hradix_pos_int : (0 : Int) < radix := by omega
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by exact_mod_cast hradix_pos_int
  have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
  have hradix_ge_one : (1 : ℝ) ≤ (radix : ℝ) := by exact_mod_cast (le_of_lt hradix)
  have hv_cast_nat : (bo.vNum : ℝ) = (radix : ℝ) ^ precision := by
    simp [hvNum, Zpower_nat]
  have hv_cast : (bo.vNum : ℝ) = (radix : ℝ) ^ (precision : Int) := by
    rw [hv_cast_nat, ← zpow_natCast]
  have hv_pos_int : 0 < bo.vNum := by
    have hpow_pos : 0 < Zpower_nat radix precision := by
      dsimp [Zpower_nat]
      exact pow_pos hradix_pos_int precision
    omega
  have hv_pos : (0 : ℝ) < (bo.vNum : ℝ) := by exact_mod_cast hv_pos_int
  by_cases hey_nonneg : 0 ≤ ey
  · by_cases hey_le : ey ≤ (precision : Int)
    · exact ⟨ny, ey, hrepr, hny, hey_nonneg, hey_le⟩
    · let k : Int := ey - (precision : Int)
      let scale : Int := Zpower_nat radix k.natAbs
      let nx : Int := ny * scale
      have hk_pos : 0 ≤ k := by
        dsimp [k]
        omega
      have hk_natAbs_cast : ((k.natAbs : Nat) : Int) = k :=
        Int.natAbs_of_nonneg hk_pos
      have hscale_cast : (scale : ℝ) = (radix : ℝ) ^ k := by
        dsimp [scale, Zpower_nat]
        simp [Int.cast_pow, ← zpow_natCast, hk_natAbs_cast]
      have hpow_split :
          (radix : ℝ) ^ ey =
            (radix : ℝ) ^ k * (radix : ℝ) ^ (precision : Int) := by
        have hk_add : k + (precision : Int) = ey := by
          dsimp [k]
          omega
        rw [← zpow_add₀ hradix_ne, hk_add]
      have hrepr' :
          ((n1 * n2 : Int) : ℝ) =
            (nx : ℝ) * (radix : ℝ) ^ (precision : Int) := by
        calc
          ((n1 * n2 : Int) : ℝ)
              = (ny : ℝ) * (radix : ℝ) ^ ey := hrepr
          _ = (ny : ℝ) *
                ((radix : ℝ) ^ k * (radix : ℝ) ^ (precision : Int)) := by
                rw [hpow_split]
          _ = ((ny : ℝ) * (scale : ℝ)) *
                (radix : ℝ) ^ (precision : Int) := by
                rw [hscale_cast]
                ring
          _ = (nx : ℝ) * (radix : ℝ) ^ (precision : Int) := by
                simp [nx, Int.cast_mul]
      have hprod_abs_lt :
          |((n1 * n2 : Int) : ℝ)| < (bo.vNum : ℝ) * (bo.vNum : ℝ) := by
        have hn1R : |(n1 : ℝ)| < (bo.vNum : ℝ) := by exact_mod_cast hn1
        have hn2R : |(n2 : ℝ)| < (bo.vNum : ℝ) := by exact_mod_cast hn2
        have hn1_nonneg : 0 ≤ |(n1 : ℝ)| := abs_nonneg _
        have hn2_nonneg : 0 ≤ |(n2 : ℝ)| := abs_nonneg _
        have hmul :
            |(n1 : ℝ)| * |(n2 : ℝ)| <
              (bo.vNum : ℝ) * (bo.vNum : ℝ) := by
          nlinarith
        simpa [Int.cast_mul, abs_mul] using hmul
      have hnx_abs_mul :
          |(nx : ℝ)| * (bo.vNum : ℝ) = |((n1 * n2 : Int) : ℝ)| := by
        rw [hrepr', hv_cast, abs_mul, abs_of_pos (zpow_pos hradix_pos _)]
      have hnx_abs_lt_real : |(nx : ℝ)| < (bo.vNum : ℝ) := by
        have hscaled : |(nx : ℝ)| * (bo.vNum : ℝ) <
            (bo.vNum : ℝ) * (bo.vNum : ℝ) := by
          rw [hnx_abs_mul]
          exact hprod_abs_lt
        nlinarith
      have hnx_abs_lt : |nx| < bo.vNum := by exact_mod_cast hnx_abs_lt_real
      exact ⟨nx, (precision : Int), hrepr', hnx_abs_lt,
        by exact_mod_cast Nat.zero_le precision, le_rfl⟩
  · have hey_lt : ey < 0 := lt_of_not_ge hey_nonneg
    let nx : Int := n1 * n2
    have hrepr' :
        ((n1 * n2 : Int) : ℝ) = (nx : ℝ) * (radix : ℝ) ^ (0 : Int) := by
      dsimp [nx]
      simp
    have hpow_le_one : (radix : ℝ) ^ ey ≤ 1 := by
      simpa using
        (zpow_right_mono₀ hradix_ge_one (by omega : ey ≤ (0 : Int)))
    have hny_abs_real : |(ny : ℝ)| < (bo.vNum : ℝ) := by exact_mod_cast hny
    have hprod_abs_eq :
        |((n1 * n2 : Int) : ℝ)| = |(ny : ℝ)| * (radix : ℝ) ^ ey := by
      rw [hrepr, abs_mul, abs_of_pos (zpow_pos hradix_pos ey)]
    have hprod_abs_lt_real :
        |((n1 * n2 : Int) : ℝ)| < (bo.vNum : ℝ) := by
      rw [hprod_abs_eq]
      calc
        |(ny : ℝ)| * (radix : ℝ) ^ ey ≤ |(ny : ℝ)| * 1 :=
          mul_le_mul_of_nonneg_left hpow_le_one (abs_nonneg _)
        _ = |(ny : ℝ)| := by ring
        _ < (bo.vNum : ℝ) := hny_abs_real
    have hnx_abs_lt : |nx| < bo.vNum := by
      dsimp [nx]
      exact_mod_cast hprod_abs_lt_real
    exact ⟨nx, 0, hrepr', hnx_abs_lt, le_rfl,
      by exact_mod_cast Nat.zero_le precision⟩

private theorem eqExpMax_between {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int)
    (p : FloatSpec.Core.Defs.FlocqFloat beta)
    (lower upperExp : Int)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum_gt : 1 < bo.vNum)
    (hpBound : Fbounded (beta:=beta) bo p)
    (hbound_lower : -bo.dExp ≤ lower)
    (hlower_p : lower ≤ p.Fexp)
    (hlower_upper : lower ≤ upperExp)
    (hAbsLe :
      |_root_.F2R (beta:=beta) p| ≤
        _root_.F2R (beta:=beta)
          (⟨bo.vNum - 1, upperExp⟩ :
            FloatSpec.Core.Defs.FlocqFloat beta)) :
    ∃ r : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo r ∧
      _root_.F2R (beta:=beta) r = _root_.F2R (beta:=beta) p ∧
      lower ≤ r.Fexp ∧ r.Fexp ≤ upperExp := by
  by_cases hExpLe : p.Fexp ≤ upperExp
  · exact ⟨p, hpBound, rfl, hlower_p, hExpLe⟩
  · have hupper_lt : upperExp < p.Fexp := lt_of_not_ge hExpLe
    let n : Nat := Int.natAbs (p.Fexp - upperExp)
    let r : FloatSpec.Core.Defs.FlocqFloat beta := Fshift (beta:=beta) radix n p
    have hdiff_nonneg : 0 ≤ p.Fexp - upperExp := by omega
    have hn : (n : Int) = p.Fexp - upperExp := by
      simpa [n] using Int.natAbs_of_nonneg hdiff_nonneg
    have hr_exp : r.Fexp = upperExp := by
      simp only [r, Fshift, FloatSpec.Core.Defs.FlocqFloat.Fexp]
      rw [hn]
      omega
    have hshift_val : _root_.F2R (beta:=beta) r = _root_.F2R (beta:=beta) p :=
      Fshift_value (beta:=beta) radix n p hbeta hradix
    have hbeta_gt : 1 < beta := by omega
    have hbeta_pos_int : (0 : Int) < beta := lt_trans Int.zero_lt_one hbeta_gt
    have hbeta_pos : (0 : ℝ) < (beta : ℝ) := by exact_mod_cast hbeta_pos_int
    have hpow_pos : 0 < (beta : ℝ) ^ upperExp := zpow_pos hbeta_pos upperExp
    have hmant_mul_le :
        ((|r.Fnum| : Int) : ℝ) * (beta : ℝ) ^ upperExp ≤
          ((bo.vNum - 1 : Int) : ℝ) * (beta : ℝ) ^ upperExp := by
      have hAbsShift :
          |_root_.F2R (beta:=beta) r| ≤
            _root_.F2R (beta:=beta)
              (⟨bo.vNum - 1, upperExp⟩ :
                FloatSpec.Core.Defs.FlocqFloat beta) := by
        rw [hshift_val]
        exact hAbsLe
      simpa [_root_.F2R, FloatSpec.Core.Defs.F2R, hr_exp, abs_mul,
        abs_of_pos hpow_pos] using hAbsShift
    have hmant_le_real : ((|r.Fnum| : Int) : ℝ) ≤ ((bo.vNum - 1 : Int) : ℝ) :=
      le_of_mul_le_mul_right hmant_mul_le hpow_pos
    have hmant_le : |r.Fnum| ≤ bo.vNum - 1 := by
      exact_mod_cast hmant_le_real
    have hr_num_lt : |r.Fnum| < bo.vNum := lt_of_le_of_lt hmant_le (by omega)
    have hr_exp_bound : -bo.dExp ≤ r.Fexp := by
      rw [hr_exp]
      exact le_trans hbound_lower hlower_upper
    exact ⟨r, ⟨hr_num_lt, hr_exp_bound⟩, hshift_val, by
      rw [hr_exp]
      exact hlower_upper, by rw [hr_exp]⟩

-- Coq: `errorBoundedMultPos_from_rounded_payload` — nonnegative product rounding error bound
noncomputable def errorBoundedMultPos_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p q f : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `errorBoundedMultPos_from_rounded_payload`.

For nonnegative bounded inputs, any rounded product has an exact bounded error
represented at exponent `p.Fexp + q.Fexp`.  This wrapper is the upstream
min/max dispatch over the already restored `errorBoundedMultMin` and
`errorBoundedMultMax` branches. -/
theorem errorBoundedMultPos_from_rounded_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p q f : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        bo.vNum = Zpower_nat radix precision ∧
        RoundedModeP_float (beta:=beta) bo radix P ∧
        Fbounded (beta:=beta) bo p ∧
        Fbounded (beta:=beta) bo q ∧
        0 ≤ _root_.F2R (beta:=beta) p ∧
        0 ≤ _root_.F2R (beta:=beta) q ∧
        -bo.dExp ≤ p.Fexp + q.Fexp ∧
        P (_root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q) f⌝⦄
    (pure (errorBoundedMultPos_check
      (beta:=beta) bo radix precision P p q f) : Id Unit)
    ⦃⇓_ => ⌜∃ error : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) error =
        _root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) f ∧
      Fbounded (beta:=beta) bo error ∧
      error.Fexp = p.Fexp + q.Fexp⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hradix, hprecision, hvnum, hRounded, hp, hq, hp_nonneg,
      hq_nonneg, hexp, hP⟩
  simp only [wp, PostCond.noThrow, pure, errorBoundedMultPos_check,
    Id.run, ULift.up_down]
  rcases hRounded.2.2.1
      (_root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q) f hP with
    hMin | hMax
  · have hbranch := errorBoundedMultMin (beta:=beta) bo radix precision p q f
    simpa [wp, PostCond.noThrow, pure, errorBoundedMultMin_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      hbranch ⟨hbeta, hradix, hprecision, hvnum, hp, hq, hp_nonneg, hq_nonneg,
        hexp, hMin⟩
  · have hbranch := errorBoundedMultMax (beta:=beta) bo radix precision p q f
    simpa [wp, PostCond.noThrow, pure, errorBoundedMultMax_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      hbranch ⟨hbeta, hradix, hprecision, hvnum, hp, hq, hp_nonneg, hq_nonneg,
        hexp, hMax⟩

-- Coq: `errorBoundedMultNeg_from_rounded_payload` — mixed-sign product rounding error bound
noncomputable def errorBoundedMultNeg_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p q f : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `errorBoundedMultNeg_from_rounded_payload`.

This is the mixed-sign counterpart of `errorBoundedMultPos_from_rounded_payload`: when the left
input is nonpositive and the right input is nonnegative, the product branch is
reduced to the nonnegative min/max arithmetic branches by negating the left
input and the rounded result. -/
theorem errorBoundedMultNeg_from_rounded_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p q f : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        bo.vNum = Zpower_nat radix precision ∧
        RoundedModeP_float (beta:=beta) bo radix P ∧
        Fbounded (beta:=beta) bo p ∧
        Fbounded (beta:=beta) bo q ∧
        _root_.F2R (beta:=beta) p ≤ 0 ∧
        0 ≤ _root_.F2R (beta:=beta) q ∧
        -bo.dExp ≤ p.Fexp + q.Fexp ∧
        P (_root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q) f⌝⦄
    (pure (errorBoundedMultNeg_check
      (beta:=beta) bo radix precision P p q f) : Id Unit)
    ⦃⇓_ => ⌜∃ error : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) error =
        _root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) f ∧
      Fbounded (beta:=beta) bo error ∧
      error.Fexp = p.Fexp + q.Fexp⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hradix, hprecision, hvnum, hRounded, hp, hq, hp_nonpos,
      hq_nonneg, hexp, hP⟩
  simp only [wp, PostCond.noThrow, pure, errorBoundedMultNeg_check,
    Id.run, ULift.up_down]
  have hpOppBound : Fbounded (beta:=beta) bo (Fopp (beta:=beta) p) := by
    have h := oppBounded (beta:=beta) bo p
    simpa [wp, PostCond.noThrow, pure, oppBounded_check, Id.run,
      ULift.up_down] using h hp
  have hpOppVal :
      _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) =
        -_root_.F2R (beta:=beta) p := by
    have h := FloatSpec.Calc.Operations.F2R_opp (beta:=beta) p
    simpa [Fopp, _root_.F2R] using h trivial
  have hfOppVal :
      _root_.F2R (beta:=beta) (Fopp (beta:=beta) f) =
        -_root_.F2R (beta:=beta) f := by
    have h := FloatSpec.Calc.Operations.F2R_opp (beta:=beta) f
    simpa [Fopp, _root_.F2R] using h trivial
  have hpOppNonneg :
      0 ≤ _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) := by
    rw [hpOppVal]
    linarith
  have hExpOpp :
      -bo.dExp ≤ (Fopp (beta:=beta) p).Fexp + q.Fexp := by
    simpa [Fopp, FloatSpec.Calc.Operations.Fopp] using hexp
  have hprodOpp :
      _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) *
          _root_.F2R (beta:=beta) q =
        -(_root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q) := by
    rw [hpOppVal]
    ring
  rcases hRounded.2.2.1
      (_root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q) f hP with
    hMin | hMax
  · have hMaxOpp :
        isMax (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) (Fopp (beta:=beta) p) *
            _root_.F2R (beta:=beta) q)
          (Fopp (beta:=beta) f) := by
      have hOpp := MinOppMax (beta:=beta) bo radix f
        (_root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q)
      have hraw :
          isMax (beta:=beta) bo radix
            (-(_root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q))
            (Fopp (beta:=beta) f) := by
        simpa [wp, PostCond.noThrow, pure, MinOppMax_check, Id.run,
          ULift.up_down] using hOpp hMin
      rw [hprodOpp]
      exact hraw
    have hbranch := errorBoundedMultMax (beta:=beta) bo radix precision
      (Fopp (beta:=beta) p) q (Fopp (beta:=beta) f)
    rcases (by
      simpa [wp, PostCond.noThrow, pure, errorBoundedMultMax_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        hbranch ⟨hbeta, hradix, hprecision, hvnum, hpOppBound, hq,
          hpOppNonneg, hq_nonneg, hExpOpp, hMaxOpp⟩) with
      ⟨error, herrorVal, herrorBound, herrorExp⟩
    refine ⟨Fopp (beta:=beta) error, ?_, ?_, ?_⟩
    · have hErrOppVal :
          _root_.F2R (beta:=beta) (Fopp (beta:=beta) error) =
            -_root_.F2R (beta:=beta) error := by
        have h := FloatSpec.Calc.Operations.F2R_opp (beta:=beta) error
        simpa [Fopp, _root_.F2R] using h trivial
      change
        _root_.F2R (beta:=beta) error =
          _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) *
              _root_.F2R (beta:=beta) q -
            _root_.F2R (beta:=beta) (Fopp (beta:=beta) f) at herrorVal
      rw [hErrOppVal, herrorVal, hprodOpp, hfOppVal]
      ring
    · have h := oppBounded (beta:=beta) bo error
      simpa [wp, PostCond.noThrow, pure, oppBounded_check, Id.run,
        ULift.up_down] using h herrorBound
    · simpa [Fopp, FloatSpec.Calc.Operations.Fopp] using herrorExp
  · have hMinOpp :
        isMin (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) (Fopp (beta:=beta) p) *
            _root_.F2R (beta:=beta) q)
          (Fopp (beta:=beta) f) := by
      have hOpp := MaxOppMin (beta:=beta) bo radix f
        (_root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q)
      have hraw :
          isMin (beta:=beta) bo radix
            (-(_root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q))
            (Fopp (beta:=beta) f) := by
        simpa [wp, PostCond.noThrow, pure, MaxOppMin_check, Id.run,
          ULift.up_down] using hOpp hMax
      rw [hprodOpp]
      exact hraw
    have hbranch := errorBoundedMultMin (beta:=beta) bo radix precision
      (Fopp (beta:=beta) p) q (Fopp (beta:=beta) f)
    rcases (by
      simpa [wp, PostCond.noThrow, pure, errorBoundedMultMin_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        hbranch ⟨hbeta, hradix, hprecision, hvnum, hpOppBound, hq,
          hpOppNonneg, hq_nonneg, hExpOpp, hMinOpp⟩) with
      ⟨error, herrorVal, herrorBound, herrorExp⟩
    refine ⟨Fopp (beta:=beta) error, ?_, ?_, ?_⟩
    · have hErrOppVal :
          _root_.F2R (beta:=beta) (Fopp (beta:=beta) error) =
            -_root_.F2R (beta:=beta) error := by
        have h := FloatSpec.Calc.Operations.F2R_opp (beta:=beta) error
        simpa [Fopp, _root_.F2R] using h trivial
      change
        _root_.F2R (beta:=beta) error =
          _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) *
              _root_.F2R (beta:=beta) q -
            _root_.F2R (beta:=beta) (Fopp (beta:=beta) f) at herrorVal
      rw [hErrOppVal, herrorVal, hprodOpp, hfOppVal]
      ring
    · have h := oppBounded (beta:=beta) bo error
      simpa [wp, PostCond.noThrow, pure, oppBounded_check, Id.run,
        ULift.up_down] using h herrorBound
    · simpa [Fopp, FloatSpec.Calc.Operations.Fopp] using herrorExp

/-- Sign reduction for the closest-rounding multiplication error theorem.

This factors the easy all-sign dispatch from Flocq's `errorBoundedMult`.
The remaining arithmetic payload is the nonnegative branch: once a future
`errorBoundedMultMin`/`errorBoundedMultMax` port proves that branch, this lemma
extends it to arbitrary input signs using `ClosestOpp` and `oppBounded`. -/
theorem errorBoundedMultClosest_from_nonneg {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int)
    (hNonneg :
      ∀ p q f : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        0 ≤ _root_.F2R (beta:=beta) p →
        0 ≤ _root_.F2R (beta:=beta) q →
        -bo.dExp ≤ p.Fexp + q.Fexp →
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q) f →
        ∃ error : FloatSpec.Core.Defs.FlocqFloat beta,
          _root_.F2R (beta:=beta) error =
            _root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q -
              _root_.F2R (beta:=beta) f ∧
          Fbounded (beta:=beta) bo error ∧
          error.Fexp = p.Fexp + q.Fexp) :
    ∀ p q f : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      -bo.dExp ≤ p.Fexp + q.Fexp →
      Closest (beta:=beta) bo (radix : ℝ)
        (_root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q) f →
      ∃ error : FloatSpec.Core.Defs.FlocqFloat beta,
        _root_.F2R (beta:=beta) error =
          _root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q -
            _root_.F2R (beta:=beta) f ∧
        Fbounded (beta:=beta) bo error ∧
        error.Fexp = p.Fexp + q.Fexp := by
  intro p q f hp hq hexp hclosest
  by_cases hp_nonneg : 0 ≤ _root_.F2R (beta:=beta) p
  · by_cases hq_nonneg : 0 ≤ _root_.F2R (beta:=beta) q
    · exact hNonneg p q f hp hq hp_nonneg hq_nonneg hexp hclosest
    · have hq_neg : _root_.F2R (beta:=beta) q < 0 := lt_of_not_ge hq_nonneg
      have hqOppBound : Fbounded (beta:=beta) bo (Fopp (beta:=beta) q) := by
        have h := oppBounded (beta:=beta) bo q
        simpa [wp, PostCond.noThrow, pure, oppBounded_check, Id.run,
          ULift.up_down] using h hq
      have hfOppClosest :
          Closest (beta:=beta) bo (radix : ℝ)
            (_root_.F2R (beta:=beta) p *
              _root_.F2R (beta:=beta) (Fopp (beta:=beta) q))
            (Fopp (beta:=beta) f) := by
        have hopp := ClosestOpp (beta:=beta) bo (radix : ℝ) f
          (_root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q)
        have hclosestNeg :
            Closest (beta:=beta) bo (radix : ℝ)
              (-(_root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q))
              (Fopp (beta:=beta) f) := by
          simpa [wp, PostCond.noThrow, pure, ClosestOpp_check, Id.run,
            ULift.up_down] using hopp hclosest
        have hqOppVal :
            _root_.F2R (beta:=beta) (Fopp (beta:=beta) q) =
              -_root_.F2R (beta:=beta) q := by
          have h := FloatSpec.Calc.Operations.F2R_opp (beta:=beta) q
          simpa [Fopp, _root_.F2R] using h trivial
        rw [hqOppVal]
        convert hclosestNeg using 1 <;> ring
      have hqOppNonneg :
          0 ≤ _root_.F2R (beta:=beta) (Fopp (beta:=beta) q) := by
        have hqOppVal :
            _root_.F2R (beta:=beta) (Fopp (beta:=beta) q) =
              -_root_.F2R (beta:=beta) q := by
          have h := FloatSpec.Calc.Operations.F2R_opp (beta:=beta) q
          simpa [Fopp, _root_.F2R] using h trivial
        rw [hqOppVal]
        linarith
      have hExpOpp :
          -bo.dExp ≤ p.Fexp + (Fopp (beta:=beta) q).Fexp := by
        simpa [Fopp, FloatSpec.Calc.Operations.Fopp] using hexp
      rcases hNonneg p (Fopp (beta:=beta) q) (Fopp (beta:=beta) f)
          hp hqOppBound hp_nonneg hqOppNonneg hExpOpp hfOppClosest with
        ⟨error, herrorVal, herrorBound, herrorExp⟩
      refine ⟨Fopp (beta:=beta) error, ?_, ?_, ?_⟩
      · have hErrOppVal :
            _root_.F2R (beta:=beta) (Fopp (beta:=beta) error) =
              -_root_.F2R (beta:=beta) error := by
          have h := FloatSpec.Calc.Operations.F2R_opp (beta:=beta) error
          simpa [Fopp, _root_.F2R] using h trivial
        have hqOppVal :
            _root_.F2R (beta:=beta) (Fopp (beta:=beta) q) =
              -_root_.F2R (beta:=beta) q := by
          have h := FloatSpec.Calc.Operations.F2R_opp (beta:=beta) q
          simpa [Fopp, _root_.F2R] using h trivial
        have hfOppVal :
            _root_.F2R (beta:=beta) (Fopp (beta:=beta) f) =
              -_root_.F2R (beta:=beta) f := by
          have h := FloatSpec.Calc.Operations.F2R_opp (beta:=beta) f
          simpa [Fopp, _root_.F2R] using h trivial
        rw [hErrOppVal, herrorVal, hqOppVal, hfOppVal]
        ring
      · have h := oppBounded (beta:=beta) bo error
        simpa [wp, PostCond.noThrow, pure, oppBounded_check, Id.run,
          ULift.up_down] using h herrorBound
      · simpa [Fopp, FloatSpec.Calc.Operations.Fopp] using herrorExp
  · have hp_neg : _root_.F2R (beta:=beta) p < 0 := lt_of_not_ge hp_nonneg
    by_cases hq_nonneg : 0 ≤ _root_.F2R (beta:=beta) q
    · have hpOppBound : Fbounded (beta:=beta) bo (Fopp (beta:=beta) p) := by
        have h := oppBounded (beta:=beta) bo p
        simpa [wp, PostCond.noThrow, pure, oppBounded_check, Id.run,
          ULift.up_down] using h hp
      have hfOppClosest :
          Closest (beta:=beta) bo (radix : ℝ)
            (_root_.F2R (beta:=beta) (Fopp (beta:=beta) p) *
              _root_.F2R (beta:=beta) q)
            (Fopp (beta:=beta) f) := by
        have hopp := ClosestOpp (beta:=beta) bo (radix : ℝ) f
          (_root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q)
        have hclosestNeg :
            Closest (beta:=beta) bo (radix : ℝ)
              (-(_root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q))
              (Fopp (beta:=beta) f) := by
          simpa [wp, PostCond.noThrow, pure, ClosestOpp_check, Id.run,
            ULift.up_down] using hopp hclosest
        have hpOppVal :
            _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) =
              -_root_.F2R (beta:=beta) p := by
          have h := FloatSpec.Calc.Operations.F2R_opp (beta:=beta) p
          simpa [Fopp, _root_.F2R] using h trivial
        rw [hpOppVal]
        convert hclosestNeg using 1 <;> ring
      have hpOppNonneg :
          0 ≤ _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) := by
        have hpOppVal :
            _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) =
              -_root_.F2R (beta:=beta) p := by
          have h := FloatSpec.Calc.Operations.F2R_opp (beta:=beta) p
          simpa [Fopp, _root_.F2R] using h trivial
        rw [hpOppVal]
        linarith
      have hExpOpp :
          -bo.dExp ≤ (Fopp (beta:=beta) p).Fexp + q.Fexp := by
        simpa [Fopp, FloatSpec.Calc.Operations.Fopp, add_comm] using hexp
      rcases hNonneg (Fopp (beta:=beta) p) q (Fopp (beta:=beta) f)
          hpOppBound hq hpOppNonneg hq_nonneg hExpOpp hfOppClosest with
        ⟨error, herrorVal, herrorBound, herrorExp⟩
      refine ⟨Fopp (beta:=beta) error, ?_, ?_, ?_⟩
      · have hErrOppVal :
            _root_.F2R (beta:=beta) (Fopp (beta:=beta) error) =
              -_root_.F2R (beta:=beta) error := by
          have h := FloatSpec.Calc.Operations.F2R_opp (beta:=beta) error
          simpa [Fopp, _root_.F2R] using h trivial
        have hpOppVal :
            _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) =
              -_root_.F2R (beta:=beta) p := by
          have h := FloatSpec.Calc.Operations.F2R_opp (beta:=beta) p
          simpa [Fopp, _root_.F2R] using h trivial
        have hfOppVal :
            _root_.F2R (beta:=beta) (Fopp (beta:=beta) f) =
              -_root_.F2R (beta:=beta) f := by
          have h := FloatSpec.Calc.Operations.F2R_opp (beta:=beta) f
          simpa [Fopp, _root_.F2R] using h trivial
        rw [hErrOppVal, herrorVal, hpOppVal, hfOppVal]
        ring
      · have h := oppBounded (beta:=beta) bo error
        simpa [wp, PostCond.noThrow, pure, oppBounded_check, Id.run,
          ULift.up_down] using h herrorBound
      · simpa [Fopp, FloatSpec.Calc.Operations.Fopp, add_comm] using herrorExp
    · have hq_neg : _root_.F2R (beta:=beta) q < 0 := lt_of_not_ge hq_nonneg
      have hpOppBound : Fbounded (beta:=beta) bo (Fopp (beta:=beta) p) := by
        have h := oppBounded (beta:=beta) bo p
        simpa [wp, PostCond.noThrow, pure, oppBounded_check, Id.run,
          ULift.up_down] using h hp
      have hqOppBound : Fbounded (beta:=beta) bo (Fopp (beta:=beta) q) := by
        have h := oppBounded (beta:=beta) bo q
        simpa [wp, PostCond.noThrow, pure, oppBounded_check, Id.run,
          ULift.up_down] using h hq
      have hpOppNonneg :
          0 ≤ _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) := by
        have hpOppVal :
            _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) =
              -_root_.F2R (beta:=beta) p := by
          have h := FloatSpec.Calc.Operations.F2R_opp (beta:=beta) p
          simpa [Fopp, _root_.F2R] using h trivial
        rw [hpOppVal]
        linarith
      have hqOppNonneg :
          0 ≤ _root_.F2R (beta:=beta) (Fopp (beta:=beta) q) := by
        have hqOppVal :
            _root_.F2R (beta:=beta) (Fopp (beta:=beta) q) =
              -_root_.F2R (beta:=beta) q := by
          have h := FloatSpec.Calc.Operations.F2R_opp (beta:=beta) q
          simpa [Fopp, _root_.F2R] using h trivial
        rw [hqOppVal]
        linarith
      have hsameClosest :
          Closest (beta:=beta) bo (radix : ℝ)
            (_root_.F2R (beta:=beta) (Fopp (beta:=beta) p) *
              _root_.F2R (beta:=beta) (Fopp (beta:=beta) q)) f := by
        have hpOppVal :
            _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) =
              -_root_.F2R (beta:=beta) p := by
          have h := FloatSpec.Calc.Operations.F2R_opp (beta:=beta) p
          simpa [Fopp, _root_.F2R] using h trivial
        have hqOppVal :
            _root_.F2R (beta:=beta) (Fopp (beta:=beta) q) =
              -_root_.F2R (beta:=beta) q := by
          have h := FloatSpec.Calc.Operations.F2R_opp (beta:=beta) q
          simpa [Fopp, _root_.F2R] using h trivial
        rw [hpOppVal, hqOppVal]
        convert hclosest using 1 <;> ring
      have hExpOpp :
          -bo.dExp ≤ (Fopp (beta:=beta) p).Fexp +
            (Fopp (beta:=beta) q).Fexp := by
        simpa [Fopp, FloatSpec.Calc.Operations.Fopp] using hexp
      rcases hNonneg (Fopp (beta:=beta) p) (Fopp (beta:=beta) q) f
          hpOppBound hqOppBound hpOppNonneg hqOppNonneg hExpOpp
          hsameClosest with
        ⟨error, herrorVal, herrorBound, herrorExp⟩
      refine ⟨error, ?_, herrorBound, ?_⟩
      · have hpOppVal :
            _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) =
              -_root_.F2R (beta:=beta) p := by
          have h := FloatSpec.Calc.Operations.F2R_opp (beta:=beta) p
          simpa [Fopp, _root_.F2R] using h trivial
        have hqOppVal :
            _root_.F2R (beta:=beta) (Fopp (beta:=beta) q) =
              -_root_.F2R (beta:=beta) q := by
          have h := FloatSpec.Calc.Operations.F2R_opp (beta:=beta) q
          simpa [Fopp, _root_.F2R] using h trivial
        rw [herrorVal, hpOppVal, hqOppVal]
        ring
      · simpa [Fopp, FloatSpec.Calc.Operations.Fopp] using herrorExp

/-- Nonnegative closest-case dispatch for the multiplication error theorem.

This is the other lightweight part of Flocq's `errorBoundedMultPos_from_rounded_payload`: a closest
rounded value is either the min or max rounded value, so the nonnegative
multiplication branch follows once the min and max arithmetic branches are
available. -/
theorem errorBoundedMultClosest_nonneg_from_minmax {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int)
    (hMin :
      ∀ p q fmin : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        0 ≤ _root_.F2R (beta:=beta) p →
        0 ≤ _root_.F2R (beta:=beta) q →
        -bo.dExp ≤ p.Fexp + q.Fexp →
        isMin (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q) fmin →
        ∃ error : FloatSpec.Core.Defs.FlocqFloat beta,
          _root_.F2R (beta:=beta) error =
            _root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q -
              _root_.F2R (beta:=beta) fmin ∧
          Fbounded (beta:=beta) bo error ∧
          error.Fexp = p.Fexp + q.Fexp)
    (hMax :
      ∀ p q fmax : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        0 ≤ _root_.F2R (beta:=beta) p →
        0 ≤ _root_.F2R (beta:=beta) q →
        -bo.dExp ≤ p.Fexp + q.Fexp →
        isMax (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q) fmax →
        ∃ error : FloatSpec.Core.Defs.FlocqFloat beta,
          _root_.F2R (beta:=beta) error =
            _root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q -
              _root_.F2R (beta:=beta) fmax ∧
          Fbounded (beta:=beta) bo error ∧
          error.Fexp = p.Fexp + q.Fexp) :
    ∀ p q f : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      0 ≤ _root_.F2R (beta:=beta) p →
      0 ≤ _root_.F2R (beta:=beta) q →
      -bo.dExp ≤ p.Fexp + q.Fexp →
      Closest (beta:=beta) bo (radix : ℝ)
        (_root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q) f →
      ∃ error : FloatSpec.Core.Defs.FlocqFloat beta,
        _root_.F2R (beta:=beta) error =
          _root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q -
            _root_.F2R (beta:=beta) f ∧
        Fbounded (beta:=beta) bo error ∧
        error.Fexp = p.Fexp + q.Fexp := by
  intro p q f hp hq hp_nonneg hq_nonneg hexp hclosest
  have hMinOrMax :
      MinOrMaxP_float (beta:=beta) bo radix
        (Closest (beta:=beta) bo (radix : ℝ)) := by
    have h := ClosestMinOrMax_from_split_radix_payload (beta:=beta) bo radix (radix : ℝ)
    simpa [wp, PostCond.noThrow, pure, ClosestMinOrMax_check, MinOrMaxP_float,
      Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
  rcases hMinOrMax
      (_root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q) f
      hclosest with hmin | hmax
  · exact hMin p q f hp hq hp_nonneg hq_nonneg hexp hmin
  · exact hMax p q f hp hq hp_nonneg hq_nonneg hexp hmax

/-- Closest-rounding multiplication error from min/max arithmetic branches.

This combines `errorBoundedMultClosest_nonneg_from_minmax` with
`errorBoundedMultClosest_from_nonneg`, isolating the only remaining Flocq
payload to the concrete min/max quotient bounds. -/
theorem errorBoundedMultClosest_from_minmax {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int)
    (hMin :
      ∀ p q fmin : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        0 ≤ _root_.F2R (beta:=beta) p →
        0 ≤ _root_.F2R (beta:=beta) q →
        -bo.dExp ≤ p.Fexp + q.Fexp →
        isMin (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q) fmin →
        ∃ error : FloatSpec.Core.Defs.FlocqFloat beta,
          _root_.F2R (beta:=beta) error =
            _root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q -
              _root_.F2R (beta:=beta) fmin ∧
          Fbounded (beta:=beta) bo error ∧
          error.Fexp = p.Fexp + q.Fexp)
    (hMax :
      ∀ p q fmax : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        0 ≤ _root_.F2R (beta:=beta) p →
        0 ≤ _root_.F2R (beta:=beta) q →
        -bo.dExp ≤ p.Fexp + q.Fexp →
        isMax (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q) fmax →
        ∃ error : FloatSpec.Core.Defs.FlocqFloat beta,
          _root_.F2R (beta:=beta) error =
            _root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q -
              _root_.F2R (beta:=beta) fmax ∧
          Fbounded (beta:=beta) bo error ∧
          error.Fexp = p.Fexp + q.Fexp) :
    ∀ p q f : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      -bo.dExp ≤ p.Fexp + q.Fexp →
      Closest (beta:=beta) bo (radix : ℝ)
        (_root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q) f →
      ∃ error : FloatSpec.Core.Defs.FlocqFloat beta,
        _root_.F2R (beta:=beta) error =
          _root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q -
            _root_.F2R (beta:=beta) f ∧
        Fbounded (beta:=beta) bo error ∧
        error.Fexp = p.Fexp + q.Fexp := by
  exact errorBoundedMultClosest_from_nonneg (beta:=beta) bo radix
    (errorBoundedMultClosest_nonneg_from_minmax (beta:=beta) bo radix hMin hMax)

noncomputable def errorBoundedMult_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (p q f : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq `errorBoundedMult`.

This exposes the full closest-rounded multiplication error theorem from the
proved nonnegative min/max branches plus the existing sign-reduction bridge. -/
theorem errorBoundedMult {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (p q f : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        bo.vNum = Zpower_nat radix precision ∧
        Fbounded (beta:=beta) bo p ∧
        Fbounded (beta:=beta) bo q ∧
        -bo.dExp ≤ p.Fexp + q.Fexp ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q) f⌝⦄
    (pure (errorBoundedMult_check
      (beta:=beta) bo radix precision p q f) : Id Unit)
    ⦃⇓_ => ⌜∃ error : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) error =
        _root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) f ∧
      Fbounded (beta:=beta) bo error ∧
      error.Fexp = p.Fexp + q.Fexp⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hradix, hprecision, hvnum, hp, hq, hexp, hclosest⟩
  simp only [wp, PostCond.noThrow, pure, errorBoundedMult_check, Id.run,
    ULift.up_down]
  have hMin :
      ∀ p q fmin : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        0 ≤ _root_.F2R (beta:=beta) p →
        0 ≤ _root_.F2R (beta:=beta) q →
        -bo.dExp ≤ p.Fexp + q.Fexp →
        isMin (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q) fmin →
        ∃ error : FloatSpec.Core.Defs.FlocqFloat beta,
          _root_.F2R (beta:=beta) error =
            _root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q -
              _root_.F2R (beta:=beta) fmin ∧
          Fbounded (beta:=beta) bo error ∧
          error.Fexp = p.Fexp + q.Fexp := by
    intro p q fmin hp hq hp_nonneg hq_nonneg hexp hmin
    have hbranch := errorBoundedMultMin (beta:=beta) bo radix precision p q fmin
    simpa [wp, PostCond.noThrow, pure, errorBoundedMultMin_check, Id.run,
      ULift.up_down] using
      hbranch ⟨hbeta, hradix, hprecision, hvnum, hp, hq, hp_nonneg, hq_nonneg,
        hexp, hmin⟩
  have hMax :
      ∀ p q fmax : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        0 ≤ _root_.F2R (beta:=beta) p →
        0 ≤ _root_.F2R (beta:=beta) q →
        -bo.dExp ≤ p.Fexp + q.Fexp →
        isMax (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q) fmax →
        ∃ error : FloatSpec.Core.Defs.FlocqFloat beta,
          _root_.F2R (beta:=beta) error =
            _root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q -
              _root_.F2R (beta:=beta) fmax ∧
          Fbounded (beta:=beta) bo error ∧
          error.Fexp = p.Fexp + q.Fexp := by
    intro p q fmax hp hq hp_nonneg hq_nonneg hexp hmax
    have hbranch := errorBoundedMultMax (beta:=beta) bo radix precision p q fmax
    simpa [wp, PostCond.noThrow, pure, errorBoundedMultMax_check, Id.run,
      ULift.up_down] using
      hbranch ⟨hbeta, hradix, hprecision, hvnum, hp, hq, hp_nonneg, hq_nonneg,
        hexp, hmax⟩
  exact errorBoundedMultClosest_from_minmax (beta:=beta) bo radix hMin hMax
    p q f hp hq hexp hclosest

noncomputable def zPos_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a x b z : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq FMA approximation local lemma `zPos`.

The rounded value `z` is nonnegative because it is a closest rounding of the
nonnegative real `a * x + b`. -/
theorem zPos_from_total_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a x b z : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧
        bo.vNum = Zpower_nat radix precision ∧
        TotalP (Closest (beta:=beta) bo (radix : ℝ)) ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
            _root_.F2R (beta:=beta) b) z ∧
        0 ≤ _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
          _root_.F2R (beta:=beta) b⌝⦄
    (pure (zPos_check (beta:=beta) bo radix precision a x b z) : Id Unit)
    ⦃⇓_ => ⌜0 ≤ _root_.F2R (beta:=beta) z⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hradix, hvNum, hTotal, hzClosest, hinput_nonneg⟩
  simp only [wp, PostCond.noThrow, pure, zPos_check, Id.run, ULift.up_down]
  let input : ℝ :=
    _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
      _root_.F2R (beta:=beta) b
  have hRounded :
      RoundedModeP_full (beta:=beta) bo
        (Closest (beta:=beta) bo (radix : ℝ)) := by
    have hTrip := ClosestRoundedModeP_from_total_payload (beta:=beta) bo (radix : ℝ)
    simpa [wp, PostCond.noThrow, pure, ClosestRoundedModeP_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using hTrip hTotal
  let zeroFloat : FloatSpec.Core.Defs.FlocqFloat beta := ⟨0, -bo.dExp⟩
  have hradix_pos : (0 : Int) < radix := lt_trans (by decide) hradix
  have hvNum_pos : 0 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact pow_pos hradix_pos precision
  have hZeroBounded : Fbounded (beta:=beta) bo zeroFloat := by
    constructor
    · show |zeroFloat.Fnum| < bo.vNum
      simp [zeroFloat, hvNum_pos]
    · show -bo.dExp ≤ zeroFloat.Fexp
      rfl
  have hZeroF2R : _root_.F2R (beta:=beta) zeroFloat = 0 := by
    unfold _root_.F2R FloatSpec.Core.Defs.F2R zeroFloat
    ring
  have hRound := RleBoundRoundl_from_full (beta:=beta) bo radix
    (Closest (beta:=beta) bo (radix : ℝ)) zeroFloat z input
  have hinput_nonneg' : _root_.F2R (beta:=beta) zeroFloat ≤ input := by
    rw [hZeroF2R]
    exact hinput_nonneg
  have hzClosest' :
      Closest (beta:=beta) bo (radix : ℝ) input z := by
    simpa [input] using hzClosest
  have hle :
      _root_.F2R (beta:=beta) zeroFloat ≤
        _root_.F2R (beta:=beta) z := by
    simpa [wp, PostCond.noThrow, pure, RleBoundRoundl_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      hRound ⟨hRounded, hZeroBounded, hinput_nonneg', hzClosest'⟩
  rw [hZeroF2R] at hle
  exact hle

/-- Coq `zPos`: closest rounding preserves the nonnegativity of the source
FMA expression.  Totality is derived from the enclosing precision and bound
assumptions. -/
theorem zPos {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a x b z : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧
        bo.vNum = Zpower_nat radix precision ∧ 3 ≤ precision ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
            _root_.F2R (beta:=beta) b) z ∧
        0 ≤ _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
          _root_.F2R (beta:=beta) b⌝⦄
    (pure (zPos_check (beta:=beta) bo radix precision a x b z) : Id Unit)
    ⦃⇓_ => ⌜0 ≤ _root_.F2R (beta:=beta) z⌝⦄ := by
  intro ⟨hbeta, hradix, hvNum, hprecision, hzDef, hPosit⟩
  have hTotal : TotalP (Closest (beta:=beta) bo (radix : ℝ)) :=
    ClosestTotal (beta:=beta) bo radix precision
      ⟨hbeta, hradix, by omega, hvNum⟩
  exact zPos_from_total_payload (beta:=beta) bo radix precision a x b z
    ⟨hbeta, hradix, hvNum, hTotal, hzDef, hPosit⟩

noncomputable def uhPos_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (ph b uh : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq FMA approximation local lemma `uhPos`.

The rounded value `uh` is nonnegative because it is a closest rounding of the
nonnegative real `ph + b`.  Upstream derives this input nonnegativity from the
preceding product rounding and the global positivity premise. -/
theorem uhPos_from_nonnegative_sum_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (ph b uh : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧
        bo.vNum = Zpower_nat radix precision ∧
        TotalP (Closest (beta:=beta) bo (radix : ℝ)) ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) ph + _root_.F2R (beta:=beta) b) uh ∧
        0 ≤ _root_.F2R (beta:=beta) ph + _root_.F2R (beta:=beta) b⌝⦄
    (pure (uhPos_check (beta:=beta) bo radix precision ph b uh) : Id Unit)
    ⦃⇓_ => ⌜0 ≤ _root_.F2R (beta:=beta) uh⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hradix, hvNum, hTotal, huhClosest, hinput_nonneg⟩
  simp only [wp, PostCond.noThrow, pure, uhPos_check, Id.run, ULift.up_down]
  let input : ℝ := _root_.F2R (beta:=beta) ph + _root_.F2R (beta:=beta) b
  have hRounded :
      RoundedModeP_full (beta:=beta) bo
        (Closest (beta:=beta) bo (radix : ℝ)) := by
    have hTrip := ClosestRoundedModeP_from_total_payload (beta:=beta) bo (radix : ℝ)
    simpa [wp, PostCond.noThrow, pure, ClosestRoundedModeP_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using hTrip hTotal
  let zeroFloat : FloatSpec.Core.Defs.FlocqFloat beta := ⟨0, -bo.dExp⟩
  have hradix_pos : (0 : Int) < radix := lt_trans (by decide) hradix
  have hvNum_pos : 0 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact pow_pos hradix_pos precision
  have hZeroBounded : Fbounded (beta:=beta) bo zeroFloat := by
    constructor
    · show |zeroFloat.Fnum| < bo.vNum
      simp [zeroFloat, hvNum_pos]
    · show -bo.dExp ≤ zeroFloat.Fexp
      rfl
  have hZeroF2R : _root_.F2R (beta:=beta) zeroFloat = 0 := by
    unfold _root_.F2R FloatSpec.Core.Defs.F2R zeroFloat
    ring
  have hRound := RleBoundRoundl_from_full (beta:=beta) bo radix
    (Closest (beta:=beta) bo (radix : ℝ)) zeroFloat uh input
  have hinput_nonneg' : _root_.F2R (beta:=beta) zeroFloat ≤ input := by
    rw [hZeroF2R]
    exact hinput_nonneg
  have huhClosest' :
      Closest (beta:=beta) bo (radix : ℝ) input uh := by
    simpa [input] using huhClosest
  have hle :
      _root_.F2R (beta:=beta) zeroFloat ≤
        _root_.F2R (beta:=beta) uh := by
    simpa [wp, PostCond.noThrow, pure, RleBoundRoundl_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      hRound ⟨hRounded, hZeroBounded, hinput_nonneg', huhClosest'⟩
  rw [hZeroF2R] at hle
  exact hle

/-- Coq `uhPos`: nonnegativity of the rounded product-plus-addend is derived
from the preceding product rounding, boundedness of `b`, and the global FMA
nonnegativity premise. -/
theorem uhPos {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a x b ph uh : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧
        bo.vNum = Zpower_nat radix precision ∧ 3 ≤ precision ∧
        Fbounded (beta:=beta) bo b ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) ph ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) ph + _root_.F2R (beta:=beta) b) uh ∧
        0 ≤ _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
          _root_.F2R (beta:=beta) b⌝⦄
    (pure (uhPos_check (beta:=beta) bo radix precision ph b uh) : Id Unit)
    ⦃⇓_ => ⌜0 ≤ _root_.F2R (beta:=beta) uh⌝⦄ := by
  intro ⟨hbeta, hradix, hvNum, hprecision, hbBound, hphDef, huhDef, hPosit⟩
  have hTotal : TotalP (Closest (beta:=beta) bo (radix : ℝ)) :=
    ClosestTotal (beta:=beta) bo radix precision
      ⟨hbeta, hradix, by omega, hvNum⟩
  have hRounded :
      RoundedModeP_full (beta:=beta) bo
        (Closest (beta:=beta) bo (radix : ℝ)) := by
    have h := ClosestRoundedModeP_from_total_payload
      (beta:=beta) bo (radix : ℝ)
    simpa [wp, PostCond.noThrow, pure, ClosestRoundedModeP_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hTotal
  have hbOppBound :
      Fbounded (beta:=beta) bo (Fopp (beta:=beta) b) := by
    have h := oppBounded (beta:=beta) bo b
    simpa [wp, PostCond.noThrow, pure, oppBounded_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hbBound
  have hbOppValue :
      _root_.F2R (beta:=beta) (Fopp (beta:=beta) b) =
        -_root_.F2R (beta:=beta) b := by
    have h := Fopp_correct (beta:=beta) b
    simpa [wp, PostCond.noThrow, pure, Fopp_correct_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
  have hinput :
      _root_.F2R (beta:=beta) (Fopp (beta:=beta) b) ≤
        _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x := by
    rw [hbOppValue]
    linarith
  have hphLower :
      _root_.F2R (beta:=beta) (Fopp (beta:=beta) b) ≤
        _root_.F2R (beta:=beta) ph := by
    have h := RleBoundRoundl_from_full (beta:=beta) bo radix
      (Closest (beta:=beta) bo (radix : ℝ)) (Fopp (beta:=beta) b) ph
      (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x)
    simpa [wp, PostCond.noThrow, pure, RleBoundRoundl_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hRounded, hbOppBound, hinput, hphDef⟩
  have hsumNonneg :
      0 ≤ _root_.F2R (beta:=beta) ph + _root_.F2R (beta:=beta) b := by
    rw [hbOppValue] at hphLower
    linarith
  exact uhPos_from_nonnegative_sum_payload (beta:=beta) bo radix precision ph b uh
    ⟨hbeta, hradix, hvNum, hTotal, huhDef, hsumNonneg⟩

noncomputable def FmaErr_product_error_witness_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a x u1 u2 : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- First product-error witness used by Coq `FmaErr`.

Upstream starts the FMA reconstruction by applying `errorBoundedMult` to the
rounded product `u1`, obtaining a bounded float whose value is
`a*x - u1`.  This helper packages that exact specialization and identifies it
with the already-named product error `u2`. -/
theorem FmaErr_product_error_witness {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (a x u1 u2 : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        bo.vNum = Zpower_nat radix precision ∧
        Fbounded (beta:=beta) bo a ∧
        Fbounded (beta:=beta) bo x ∧
        -bo.dExp ≤ a.Fexp + x.Fexp ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) u1 ∧
        _root_.F2R (beta:=beta) u2 =
          _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
            _root_.F2R (beta:=beta) u1⌝⦄
    (pure (FmaErr_product_error_witness_check
      (beta:=beta) bo radix precision a x u1 u2) : Id Unit)
    ⦃⇓_ => ⌜∃ u2' : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) u2' = _root_.F2R (beta:=beta) u2 ∧
      Fbounded (beta:=beta) bo u2' ∧
      u2'.Fexp = a.Fexp + x.Fexp⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hradix, hprecision, hvnum, ha, hx, hexp, hu1_closest, hu2_val⟩
  simp only [wp, PostCond.noThrow, pure, FmaErr_product_error_witness_check,
    Id.run, ULift.up_down]
  have hmult := errorBoundedMult (beta:=beta) bo radix precision a x u1
  rcases (by
    simpa [wp, PostCond.noThrow, pure, errorBoundedMult_check, Id.run,
      ULift.up_down] using
      hmult ⟨hbeta, hradix, hprecision, hvnum, ha, hx, hexp, hu1_closest⟩) with
    ⟨err, herr_val, herr_bounded, herr_exp⟩
  exact ⟨err, herr_val.trans hu2_val.symm, herr_bounded, herr_exp⟩

noncomputable def FmaErr_add_error_witness_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (y u2 al1 al2 : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Second addition-error witness used by Coq `FmaErr`.

After `u2` has been represented as a bounded product-error float, upstream
applies `errorBoundedPlus` to `y + u2` rounded to `al1`.  This helper packages
that specialization and identifies the resulting bounded error with
`al2 = y + u2 - al1`. -/
theorem FmaErr_add_error_witness {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (y u2 al1 al2 : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        1 < bo.vNum ∧
        bo.vNum = Zpower_nat radix precision ∧
        (∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=beta) radix r).Fexp) ∧
        Fbounded (beta:=beta) bo y ∧
        Fbounded (beta:=beta) bo u2 ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) u2) al1 ∧
        _root_.F2R (beta:=beta) al2 =
          _root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) u2 -
            _root_.F2R (beta:=beta) al1⌝⦄
    (pure (FmaErr_add_error_witness_check
      (beta:=beta) bo radix precision y u2 al1 al2) : Id Unit)
    ⦃⇓_ => ⌜∃ al2' : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) al2' = _root_.F2R (beta:=beta) al2 ∧
      Fbounded (beta:=beta) bo al2' ∧
      al2'.Fexp = min y.Fexp u2.Fexp⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hradix, hprecision, hvnum_gt, hvnum, hBoundExp, hy, hu2,
      hal1_closest, hal2_val⟩
  simp only [wp, PostCond.noThrow, pure, FmaErr_add_error_witness_check,
    Id.run, ULift.up_down]
  rcases errorBoundedPlus_from_format_payload (beta:=beta) bo radix precision hbeta hradix
      hprecision hvnum_gt hvnum y u2 al1 hy hu2 hal1_closest with
    ⟨err, herr_val, herr_bounded, herr_exp⟩
  exact ⟨err, herr_val.trans hal2_val.symm, herr_bounded, herr_exp⟩

noncomputable def FmaErr_be1_error_witness_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (u1 al1 be1 be2 : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Third addition-error witness used by Coq `FmaErr`.

After the product and first addition errors have bounded representatives,
upstream applies `errorBoundedPlus` once more to the rounded sum `be1` of
`u1 + al1`.  This helper packages that specialization and identifies the
resulting bounded error with `be2 = u1 + al1 - be1`. -/
theorem FmaErr_be1_error_witness {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (u1 al1 be1 be2 : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        1 < bo.vNum ∧
        bo.vNum = Zpower_nat radix precision ∧
        (∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=beta) radix r).Fexp) ∧
        Fbounded (beta:=beta) bo u1 ∧
        Fbounded (beta:=beta) bo al1 ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) u1 + _root_.F2R (beta:=beta) al1) be1 ∧
        _root_.F2R (beta:=beta) be2 =
          _root_.F2R (beta:=beta) u1 + _root_.F2R (beta:=beta) al1 -
            _root_.F2R (beta:=beta) be1⌝⦄
    (pure (FmaErr_be1_error_witness_check
      (beta:=beta) bo radix precision u1 al1 be1 be2) : Id Unit)
    ⦃⇓_ => ⌜∃ be2' : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) be2' = _root_.F2R (beta:=beta) be2 ∧
      Fbounded (beta:=beta) bo be2' ∧
      be2'.Fexp = min u1.Fexp al1.Fexp⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hradix, hprecision, hvnum_gt, hvnum, hBoundExp, hu1, hal1,
      hbe1_closest, hbe2_val⟩
  simp only [wp, PostCond.noThrow, pure, FmaErr_be1_error_witness_check,
    Id.run, ULift.up_down]
  rcases errorBoundedPlus_from_format_payload (beta:=beta) bo radix precision hbeta hradix
      hprecision hvnum_gt hvnum u1 al1 be1 hu1 hal1 hbe1_closest with
    ⟨err, herr_val, herr_bounded, herr_exp⟩
  exact ⟨err, herr_val.trans hbe2_val.symm, herr_bounded, herr_exp⟩

noncomputable def Fma_FTS_plus_leexp_witness_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat) (n : Int)
    (p q g : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Plus-rounding lower-exponent witness used by Coq `Fma_FTS`.

The upstream `Fma_FTS` proof repeatedly applies `LeExpRound2` to a rounded
addition after rewriting the exact source float with `Fplus_correct`.  This
helper packages that pattern: a closest rounding of `p + q` has a bounded
same-value representative whose exponent remains above any lower bound below
both input exponents. -/
theorem Fma_FTS_plus_leexp_witness {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat) (n : Int)
    (p q g : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        1 < bo.vNum ∧
        bo.vNum = Zpower_nat radix precision ∧
        (∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=beta) radix r).Fexp) ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) g ∧
        n ≤ min p.Fexp q.Fexp⌝⦄
    (pure (Fma_FTS_plus_leexp_witness_check
      (beta:=beta) bo radix precision n p q g) : Id Unit)
    ⦃⇓_ => ⌜∃ g' : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo g' ∧
      _root_.F2R (beta:=beta) g' = _root_.F2R (beta:=beta) g ∧
      n ≤ g'.Fexp⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hradix, hprecision, hvnum_gt, hvnum, hBoundExp, hg_closest, hn⟩
  simp only [wp, PostCond.noThrow, pure, Fma_FTS_plus_leexp_witness_check,
    Id.run, ULift.up_down]
  have hbeta_gt : 1 < beta := by omega
  have hplus_val :
      _root_.F2R (beta:=beta) (Fplus (beta:=beta) p q) =
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q := by
    have h := Fplus_correct (beta:=beta) p q
    simpa [wp, PostCond.noThrow, pure, Fplus_correct_check, Id.run,
      ULift.up_down] using h hbeta_gt
  have hplus_exp :
      (Fplus (beta:=beta) p q).Fexp = min p.Fexp q.Fexp := by
    cases p with
    | mk pm pe =>
      cases q with
      | mk qm qe =>
        by_cases hle : pe ≤ qe
        · simp [Fplus, FloatSpec.Calc.Operations.Fplus,
            FloatSpec.Calc.Operations.Falign, hle, min_eq_left hle]
        · have hqe : qe ≤ pe := le_of_lt (lt_of_not_ge hle)
          simp [Fplus, FloatSpec.Calc.Operations.Fplus,
            FloatSpec.Calc.Operations.Falign, hle, min_eq_right hqe]
  have hclosest_plus :
      Closest (beta:=beta) bo (radix : ℝ)
        (_root_.F2R (beta:=beta) (Fplus (beta:=beta) p q)) g := by
    rw [hplus_val]
    exact hg_closest
  have hle := LeExpRound2_from_format_payload (beta:=beta) bo radix precision n
    (Fplus (beta:=beta) p q) g
  simpa [wp, PostCond.noThrow, pure, LeExpRound2_check, Id.run,
    ULift.up_down] using
    hle ⟨hbeta, hradix, hprecision, hvnum_gt, hvnum,
      hclosest_plus, by simpa [hplus_exp] using hn⟩

noncomputable def Fma_FTS_mult_leexp_witness_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat) (n : Int)
    (p q g : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Product-rounding lower-exponent witness used by Coq `Fma_FTS`.

This is the multiplication analogue of `Fma_FTS_plus_leexp_witness`: upstream
uses `Fmult_correct` to turn a closest rounding of `p * q` into a
`LeExpRound2` call on the exact float product. -/
theorem Fma_FTS_mult_leexp_witness {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat) (n : Int)
    (p q g : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        1 < bo.vNum ∧
        bo.vNum = Zpower_nat radix precision ∧
        (∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=beta) radix r).Fexp) ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q) g ∧
        n ≤ p.Fexp + q.Fexp⌝⦄
    (pure (Fma_FTS_mult_leexp_witness_check
      (beta:=beta) bo radix precision n p q g) : Id Unit)
    ⦃⇓_ => ⌜∃ g' : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo g' ∧
      _root_.F2R (beta:=beta) g' = _root_.F2R (beta:=beta) g ∧
      n ≤ g'.Fexp⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hradix, hprecision, hvnum_gt, hvnum, hBoundExp, hg_closest, hn⟩
  simp only [wp, PostCond.noThrow, pure, Fma_FTS_mult_leexp_witness_check,
    Id.run, ULift.up_down]
  have hbeta_gt : 1 < beta := by omega
  have hmult_val :
      _root_.F2R (beta:=beta)
          (FloatSpec.Calc.Operations.Fmult (beta:=beta) p q) =
        _root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q := by
    have h := Fmult_correct (beta:=beta) p q
    simpa [wp, PostCond.noThrow, pure, Fmult_correct_check, Id.run,
      ULift.up_down] using h hbeta_gt
  have hmult_exp :
      (FloatSpec.Calc.Operations.Fmult (beta:=beta) p q).Fexp =
        p.Fexp + q.Fexp := by
    cases p <;> cases q <;> rfl
  have hclosest_mult :
      Closest (beta:=beta) bo (radix : ℝ)
        (_root_.F2R (beta:=beta)
          (FloatSpec.Calc.Operations.Fmult (beta:=beta) p q)) g := by
    rw [hmult_val]
    exact hg_closest
  have hle := LeExpRound2_from_format_payload (beta:=beta) bo radix precision n
    (FloatSpec.Calc.Operations.Fmult (beta:=beta) p q) g
  simpa [wp, PostCond.noThrow, pure, LeExpRound2_check, Id.run,
    ULift.up_down] using
    hle ⟨hbeta, hradix, hprecision, hvnum_gt, hvnum,
      hclosest_mult, by simpa [hmult_exp] using hn⟩

noncomputable def Fma_FTS_minus_leexp_witness_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat) (n : Int)
    (p q g : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Subtraction-rounding lower-exponent witness used by Coq `Fma_FTS`.

This packages the `Fminus_correct` plus `LeExpRound2` step used for the
rounded correction term `gat`: a closest rounding of `p - q` has a bounded
same-value representative whose exponent stays above any lower bound below
both input exponents. -/
theorem Fma_FTS_minus_leexp_witness {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat) (n : Int)
    (p q g : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        1 < bo.vNum ∧
        bo.vNum = Zpower_nat radix precision ∧
        (∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=beta) radix r).Fexp) ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q) g ∧
        n ≤ min p.Fexp q.Fexp⌝⦄
    (pure (Fma_FTS_minus_leexp_witness_check
      (beta:=beta) bo radix precision n p q g) : Id Unit)
    ⦃⇓_ => ⌜∃ g' : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo g' ∧
      _root_.F2R (beta:=beta) g' = _root_.F2R (beta:=beta) g ∧
      n ≤ g'.Fexp⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hradix, hprecision, hvnum_gt, hvnum, hBoundExp, hg_closest, hn⟩
  simp only [wp, PostCond.noThrow, pure, Fma_FTS_minus_leexp_witness_check,
    Id.run, ULift.up_down]
  have hbeta_gt : 1 < beta := by omega
  have hminus_val :
      _root_.F2R (beta:=beta)
          (FloatSpec.Calc.Operations.Fminus (beta:=beta) p q) =
        _root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q := by
    have h := Fminus_correct (beta:=beta) p q
    simpa [wp, PostCond.noThrow, pure, Fminus_correct_check, Fminus, Id.run,
      ULift.up_down] using h hbeta_gt
  have hminus_exp :
      (FloatSpec.Calc.Operations.Fminus (beta:=beta) p q).Fexp =
        min p.Fexp q.Fexp := by
    cases p with
    | mk pm pe =>
      cases q with
      | mk qm qe =>
        by_cases hle : pe ≤ qe
        · simp [FloatSpec.Calc.Operations.Fminus, FloatSpec.Calc.Operations.Fopp,
            Fplus, FloatSpec.Calc.Operations.Fplus,
            FloatSpec.Calc.Operations.Falign, hle, min_eq_left hle]
        · have hqe : qe ≤ pe := le_of_lt (lt_of_not_ge hle)
          simp [FloatSpec.Calc.Operations.Fminus, FloatSpec.Calc.Operations.Fopp,
            Fplus, FloatSpec.Calc.Operations.Fplus,
            FloatSpec.Calc.Operations.Falign, hle, min_eq_right hqe]
  have hclosest_minus :
      Closest (beta:=beta) bo (radix : ℝ)
        (_root_.F2R (beta:=beta)
          (FloatSpec.Calc.Operations.Fminus (beta:=beta) p q)) g := by
    rw [hminus_val]
    exact hg_closest
  have hle := LeExpRound2_from_format_payload (beta:=beta) bo radix precision n
    (FloatSpec.Calc.Operations.Fminus (beta:=beta) p q) g
  simpa [wp, PostCond.noThrow, pure, LeExpRound2_check, Id.run,
    ULift.up_down] using
    hle ⟨hbeta, hradix, hprecision, hvnum_gt, hvnum,
      hclosest_minus, by simpa [hminus_exp] using hn⟩

noncomputable def Fma_FTS_be2_error_witness_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (u1 al1 u1' al1' be1 be2 : FloatSpec.Core.Defs.FlocqFloat beta) :
    Unit :=
  ()

/-- `be2` bounded-error witness used by Coq `Fma_FTS`.

After `LeExpRound2` replaces `u1` and `al1` by bounded same-value
representatives `u1'` and `al1'`, upstream applies `errorBoundedPlus` to the
rounded sum `be1`.  This helper packages the value rewrites and produces the
bounded representative of `be2 = u1 + al1 - be1`. -/
theorem Fma_FTS_be2_error_witness {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (u1 al1 u1' al1' be1 be2 : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        1 < bo.vNum ∧
        bo.vNum = Zpower_nat radix precision ∧
        (∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=beta) radix r).Fexp) ∧
        Fbounded (beta:=beta) bo u1' ∧
        Fbounded (beta:=beta) bo al1' ∧
        _root_.F2R (beta:=beta) u1' = _root_.F2R (beta:=beta) u1 ∧
        _root_.F2R (beta:=beta) al1' = _root_.F2R (beta:=beta) al1 ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) u1 + _root_.F2R (beta:=beta) al1) be1 ∧
        _root_.F2R (beta:=beta) be2 =
          _root_.F2R (beta:=beta) u1 + _root_.F2R (beta:=beta) al1 -
            _root_.F2R (beta:=beta) be1⌝⦄
    (pure (Fma_FTS_be2_error_witness_check
      (beta:=beta) bo radix precision u1 al1 u1' al1' be1 be2) : Id Unit)
    ⦃⇓_ => ⌜∃ be2' : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) be2' = _root_.F2R (beta:=beta) be2 ∧
      Fbounded (beta:=beta) bo be2' ∧
      be2'.Fexp = min u1'.Fexp al1'.Fexp⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hradix, hprecision, hvnum_gt, hvnum, hBoundExp, hu1', hal1',
      hu1_val, hal1_val, hbe1_closest, hbe2_val⟩
  simp only [wp, PostCond.noThrow, pure, Fma_FTS_be2_error_witness_check,
    Id.run, ULift.up_down]
  have hbe1_closest' :
      Closest (beta:=beta) bo (radix : ℝ)
        (_root_.F2R (beta:=beta) u1' + _root_.F2R (beta:=beta) al1') be1 := by
    rw [hu1_val, hal1_val]
    exact hbe1_closest
  rcases errorBoundedPlus_from_format_payload (beta:=beta) bo radix precision hbeta hradix
      hprecision hvnum_gt hvnum u1' al1' be1 hu1' hal1'
      hbe1_closest' with
    ⟨err, herr_val, herr_bounded, herr_exp⟩
  have herr_val' :
      _root_.F2R (beta:=beta) err = _root_.F2R (beta:=beta) be2 := by
    calc
      _root_.F2R (beta:=beta) err =
          _root_.F2R (beta:=beta) u1' + _root_.F2R (beta:=beta) al1' -
            _root_.F2R (beta:=beta) be1 := herr_val
      _ = _root_.F2R (beta:=beta) u1 + _root_.F2R (beta:=beta) al1 -
            _root_.F2R (beta:=beta) be1 := by rw [hu1_val, hal1_val]
      _ = _root_.F2R (beta:=beta) be2 := hbe2_val.symm
  exact ⟨err, herr_val', herr_bounded, herr_exp⟩

noncomputable def Fma_FTS_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a x y r1 u1 u2 al1 al2 be1 be2 gat ga :
      FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `Fma_FTS`.

This restores the bounded-witness payload used after `FmaErr`: the product
error and addition-error witnesses are normalized to a shared lower exponent,
then the final rounded correction `ga` is represented at an exponent no smaller
than the represented `al2` error. -/
theorem Fma_FTS_from_closest_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (a x y r1 u1 u2 al1 al2 be1 be2 gat ga :
      FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        1 < bo.vNum ∧
        bo.vNum = Zpower_nat radix precision ∧
        Fbounded (beta:=beta) bo a ∧
        Fbounded (beta:=beta) bo x ∧
        Fbounded (beta:=beta) bo y ∧
        -bo.dExp ≤ a.Fexp + x.Fexp ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) u1 ∧
        _root_.F2R (beta:=beta) u2 =
          _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
            _root_.F2R (beta:=beta) u1 ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) u2) al1 ∧
        _root_.F2R (beta:=beta) al2 =
          _root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) u2 -
            _root_.F2R (beta:=beta) al1 ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
            _root_.F2R (beta:=beta) y) r1 ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) u1 + _root_.F2R (beta:=beta) al1) be1 ∧
        _root_.F2R (beta:=beta) be2 =
          _root_.F2R (beta:=beta) u1 + _root_.F2R (beta:=beta) al1 -
            _root_.F2R (beta:=beta) be1 ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) be1 - _root_.F2R (beta:=beta) r1) gat ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) gat + _root_.F2R (beta:=beta) be2) ga⌝⦄
    (pure (Fma_FTS_check
      (beta:=beta) bo radix precision a x y r1 u1 u2 al1 al2 be1 be2 gat ga) :
      Id Unit)
    ⦃⇓_ => ⌜∃ ga_e al2_e : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) ga_e = _root_.F2R (beta:=beta) ga ∧
      _root_.F2R (beta:=beta) al2_e = _root_.F2R (beta:=beta) al2 ∧
      Fbounded (beta:=beta) bo ga_e ∧
      Fbounded (beta:=beta) bo al2_e ∧
      al2_e.Fexp ≤ ga_e.Fexp⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hradix, hprecision, hvnum_gt, hvnum, ha, hx, hy, hexp,
      hu1_closest, hu2_val, hal1_closest, hal2_val, hr1_closest,
      hbe1_closest, hbe2_val, hgat_closest, hga_closest⟩
  simp only [wp, PostCond.noThrow, pure, Fma_FTS_check, Id.run, ULift.up_down]
  have hBoundExp :
      ∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=beta) radix r).Fexp := by
    intro r
    exact le_trans (neg_nonpos.mpr bo.dExp_nonneg) (by simp [boundR, boundNat])
  let n : Int := min y.Fexp (a.Fexp + x.Fexp)
  have hβ : 1 < beta := by omega
  rcases (by
      have h := FmaErr_product_error_witness
        (beta:=beta) bo radix precision a x u1 u2
      simpa [wp, PostCond.noThrow, pure,
        FmaErr_product_error_witness_check, Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hbeta, hradix, hprecision, hvnum, ha, hx, hexp, hu1_closest,
          hu2_val⟩) with
    ⟨u2', hu2'_val, hu2'_bound, hu2'_exp⟩
  have hal1_closest' :
      Closest (beta:=beta) bo (radix : ℝ)
        (_root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) u2') al1 := by
    simpa [hu2'_val] using hal1_closest
  have hal2_val' :
      _root_.F2R (beta:=beta) al2 =
        _root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) u2' -
          _root_.F2R (beta:=beta) al1 := by
    simpa [hu2'_val] using hal2_val
  rcases (by
      have h := FmaErr_add_error_witness
        (beta:=beta) bo radix precision y u2' al1 al2
      simpa [wp, PostCond.noThrow, pure, FmaErr_add_error_witness_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hbeta, hradix, hprecision, hvnum_gt, hvnum, hBoundExp, hy,
          hu2'_bound, hal1_closest', hal2_val'⟩) with
    ⟨al2', hal2'_val, hal2'_bound, hal2'_exp⟩
  have hal2'_exp_n : al2'.Fexp = n := by
    rw [hal2'_exp, hu2'_exp]
  rcases (by
      have h := Fma_FTS_plus_leexp_witness
        (beta:=beta) bo radix precision n y u2' al1
      simpa [wp, PostCond.noThrow, pure, Fma_FTS_plus_leexp_witness_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hbeta, hradix, hprecision, hvnum_gt, hvnum, hBoundExp,
          hal1_closest', by simp [n, hu2'_exp]⟩) with
    ⟨al1', hal1'_bound, hal1'_val, hal1'_exp⟩
  rcases (by
      have h := Fma_FTS_mult_leexp_witness
        (beta:=beta) bo radix precision n a x u1
      simpa [wp, PostCond.noThrow, pure, Fma_FTS_mult_leexp_witness_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hbeta, hradix, hprecision, hvnum_gt, hvnum, hBoundExp,
          hu1_closest, by
            dsimp [n]
            exact min_le_right _ _⟩) with
    ⟨u1', hu1'_bound, hu1'_val, hu1'_exp⟩
  have hmult_val :
      _root_.F2R (beta:=beta)
          (FloatSpec.Calc.Operations.Fmult (beta:=beta) a x) =
        _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x := by
    have h := Fmult_correct (beta:=beta) a x
    simpa [wp, PostCond.noThrow, pure, Fmult_correct_check, Id.run,
      ULift.up_down] using h hβ
  have hax_exp :
      (FloatSpec.Calc.Operations.Fmult (beta:=beta) a x).Fexp =
        a.Fexp + x.Fexp := by
    cases a <;> cases x <;> rfl
  have hr1_closest' :
      Closest (beta:=beta) bo (radix : ℝ)
        (_root_.F2R (beta:=beta)
            (FloatSpec.Calc.Operations.Fmult (beta:=beta) a x) +
          _root_.F2R (beta:=beta) y) r1 := by
    rw [hmult_val]
    exact hr1_closest
  rcases (by
      have h := Fma_FTS_plus_leexp_witness
        (beta:=beta) bo radix precision n
          (FloatSpec.Calc.Operations.Fmult (beta:=beta) a x) y r1
      simpa [wp, PostCond.noThrow, pure, Fma_FTS_plus_leexp_witness_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hbeta, hradix, hprecision, hvnum_gt, hvnum, hBoundExp,
          hr1_closest', by
            dsimp [n]
            rw [hax_exp, min_comm]⟩) with
    ⟨r1', hr1'_bound, hr1'_val, hr1'_exp⟩
  have hbe1_closest' :
      Closest (beta:=beta) bo (radix : ℝ)
        (_root_.F2R (beta:=beta) u1' + _root_.F2R (beta:=beta) al1') be1 := by
    simpa [hu1'_val, hal1'_val] using hbe1_closest
  rcases (by
      have h := Fma_FTS_plus_leexp_witness
        (beta:=beta) bo radix precision n u1' al1' be1
      simpa [wp, PostCond.noThrow, pure, Fma_FTS_plus_leexp_witness_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hbeta, hradix, hprecision, hvnum_gt, hvnum, hBoundExp,
          hbe1_closest', le_min hu1'_exp hal1'_exp⟩) with
    ⟨be1', hbe1'_bound, hbe1'_val, hbe1'_exp⟩
  rcases (by
      have h := Fma_FTS_be2_error_witness
        (beta:=beta) bo radix precision u1 al1 u1' al1' be1 be2
      simpa [wp, PostCond.noThrow, pure, Fma_FTS_be2_error_witness_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hbeta, hradix, hprecision, hvnum_gt, hvnum, hBoundExp, hu1'_bound,
          hal1'_bound, hu1'_val, hal1'_val, hbe1_closest, hbe2_val⟩) with
    ⟨be2', hbe2'_val, hbe2'_bound, hbe2'_exp⟩
  have hbe2'_exp_ge : n ≤ be2'.Fexp := by
    rw [hbe2'_exp]
    exact le_min hu1'_exp hal1'_exp
  have hgat_closest' :
      Closest (beta:=beta) bo (radix : ℝ)
        (_root_.F2R (beta:=beta) be1' - _root_.F2R (beta:=beta) r1') gat := by
    simpa [hbe1'_val, hr1'_val] using hgat_closest
  rcases (by
      have h := Fma_FTS_minus_leexp_witness
        (beta:=beta) bo radix precision n be1' r1' gat
      simpa [wp, PostCond.noThrow, pure, Fma_FTS_minus_leexp_witness_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hbeta, hradix, hprecision, hvnum_gt, hvnum, hBoundExp,
          hgat_closest', le_min hbe1'_exp hr1'_exp⟩) with
    ⟨gat', hgat'_bound, hgat'_val, hgat'_exp⟩
  have hga_closest' :
      Closest (beta:=beta) bo (radix : ℝ)
        (_root_.F2R (beta:=beta) gat' + _root_.F2R (beta:=beta) be2') ga := by
    simpa [hgat'_val, hbe2'_val] using hga_closest
  rcases (by
      have h := Fma_FTS_plus_leexp_witness
        (beta:=beta) bo radix precision n gat' be2' ga
      simpa [wp, PostCond.noThrow, pure, Fma_FTS_plus_leexp_witness_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hbeta, hradix, hprecision, hvnum_gt, hvnum, hBoundExp,
          hga_closest', le_min hgat'_exp hbe2'_exp_ge⟩) with
    ⟨ga', hga'_bound, hga'_val, hga'_exp⟩
  have hal2_le_ga : al2'.Fexp ≤ ga'.Fexp := by
    rw [hal2'_exp_n]
    exact hga'_exp
  exact ⟨ga', al2', hga'_val, hal2'_val, hga'_bound, hal2'_bound, hal2_le_ga⟩

/-- Exact Coq `Fma_FTS` contract, retaining the abstract rounding predicate. -/
theorem Fma_FTS {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : bo.vNum = Zpower_nat radix precision)
    (hprecision : 3 ≤ precision)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (hPClosest : ∀ r f, P r f → Closest (beta:=beta) bo (radix : ℝ) r f)
    (a x y r1 u1 u2 al1 al2 be1 be2 gat ga :
      FloatSpec.Core.Defs.FlocqFloat beta)
    (ha : Fbounded (beta:=beta) bo a)
    (hx : Fbounded (beta:=beta) bo x)
    (hy : Fbounded (beta:=beta) bo y)
    (hexp : -bo.dExp ≤ a.Fexp + x.Fexp)
    (hu1 : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) u1)
    (hu2 : _root_.F2R (beta:=beta) u2 =
      _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
        _root_.F2R (beta:=beta) u1)
    (hal1 : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) u2) al1)
    (hal2 : _root_.F2R (beta:=beta) al2 =
      _root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) u2 -
        _root_.F2R (beta:=beta) al1)
    (hbe2 : _root_.F2R (beta:=beta) be2 =
      _root_.F2R (beta:=beta) u1 + _root_.F2R (beta:=beta) al1 -
        _root_.F2R (beta:=beta) be1)
    (hgat : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) be1 - _root_.F2R (beta:=beta) r1) gat)
    (hga : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) gat + _root_.F2R (beta:=beta) be2) ga)
    (hr1 : P (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
      _root_.F2R (beta:=beta) y) r1)
    (hbe1 : P (_root_.F2R (beta:=beta) u1 +
      _root_.F2R (beta:=beta) al1) be1) :
    ∃ ga_e al2_e : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) ga_e = _root_.F2R (beta:=beta) ga ∧
      _root_.F2R (beta:=beta) al2_e = _root_.F2R (beta:=beta) al2 ∧
      Fbounded (beta:=beta) bo ga_e ∧
      Fbounded (beta:=beta) bo al2_e ∧
      al2_e.Fexp ≤ ga_e.Fexp := by
  have hprecision_ne : precision ≠ 0 := by omega
  have hvNum_gt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecision_ne
  letI : ValidRadix radix := ⟨hradix⟩
  have h := Fma_FTS_from_closest_payload (beta:=beta) bo radix precision
    a x y r1 u1 u2 al1 al2 be1 be2 gat ga
  simpa [wp, PostCond.noThrow, pure, Fma_FTS_check, Id.run,
    ULift.up_down] using h ⟨hbeta, hradix, hprecision_ne, hvNum_gt, hvNum,
      ha, hx, hy, hexp, hu1, hu2, hal1, hal2, hPClosest _ _ hr1,
      hPClosest _ _ hbe1, hbe2, hgat, hga⟩

noncomputable def FmaErr_reconstruct_from_ga_correction_check {beta : Int} [ValidRadix beta]
    (a x y r1 u1 u2 al1 al2 be1 be2 ga :
      FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Algebraic reconstruction core used by Coq `FmaErr`.

The hard part of upstream `FmaErr` is proving the correction value represented
by `ga`.  Once that value is available, the final equality follows only from
the product error, the first addition error, and the `be2` definition. -/
theorem FmaErr_reconstruct_from_ga_correction {beta : Int} [ValidRadix beta]
    (a x y r1 u1 u2 al1 al2 be1 be2 ga :
      FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜_root_.F2R (beta:=beta) u2 =
          _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
            _root_.F2R (beta:=beta) u1 ∧
        _root_.F2R (beta:=beta) al2 =
          _root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) u2 -
            _root_.F2R (beta:=beta) al1 ∧
        _root_.F2R (beta:=beta) be2 =
          _root_.F2R (beta:=beta) u1 + _root_.F2R (beta:=beta) al1 -
            _root_.F2R (beta:=beta) be1 ∧
        _root_.F2R (beta:=beta) ga =
          _root_.F2R (beta:=beta) be1 - _root_.F2R (beta:=beta) r1 +
            _root_.F2R (beta:=beta) be2⌝⦄
    (pure (FmaErr_reconstruct_from_ga_correction_check
      (beta:=beta) a x y r1 u1 u2 al1 al2 be1 be2 ga) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
          _root_.F2R (beta:=beta) y =
        _root_.F2R (beta:=beta) r1 + _root_.F2R (beta:=beta) ga +
          _root_.F2R (beta:=beta) al2⌝⦄ := by
  intro h
  rcases h with ⟨hu2, hal2, hbe2, hga⟩
  simp only [wp, PostCond.noThrow, pure,
    FmaErr_reconstruct_from_ga_correction_check, Id.run, ULift.up_down,
    PredTrans.pure, PredTrans.apply, ULift.down]
  change _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
        _root_.F2R (beta:=beta) y =
      _root_.F2R (beta:=beta) r1 + _root_.F2R (beta:=beta) ga +
        _root_.F2R (beta:=beta) al2
  rw [hga, hal2, hbe2, hu2]
  ring

noncomputable def FmaErr_ga_value_of_be2_zero_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (gat be2 ga : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Zero-`be2` correction branch used by Coq `FmaErr`.

When the second compensation error represents zero, the final correction
rounding has exact input `gat`.  Since `gat` is already bounded, the
closest-rounding projector property forces the rounded correction `ga` to have
the same real value as `gat`. -/
theorem FmaErr_ga_value_of_be2_zero {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (gat be2 ga : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜TotalP (Closest (beta:=beta) bo radix) ∧
        Fbounded (beta:=beta) bo gat ∧
        _root_.F2R (beta:=beta) be2 = 0 ∧
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) gat + _root_.F2R (beta:=beta) be2) ga⌝⦄
    (pure (FmaErr_ga_value_of_be2_zero_check
      (beta:=beta) bo radix gat be2 ga) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) ga =
      _root_.F2R (beta:=beta) gat⌝⦄ := by
  intro h
  rcases h with ⟨hTotal, hgat_bound, hbe2_zero, hga_closest⟩
  simp only [wp, PostCond.noThrow, pure,
    FmaErr_ga_value_of_be2_zero_check, Id.run, ULift.up_down]
  have hRounded : RoundedModeP_full (beta:=beta) bo (Closest (beta:=beta) bo radix) := by
    have h := ClosestRoundedModeP_from_total_payload (beta:=beta) bo radix
    simpa [wp, PostCond.noThrow, pure, ClosestRoundedModeP_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hTotal
  rcases hRounded with ⟨_, _, _, _, hProjEq⟩
  have hga_closest' :
      Closest (beta:=beta) bo radix (_root_.F2R (beta:=beta) gat) ga := by
    rw [hbe2_zero, add_zero] at hga_closest
    exact hga_closest
  exact (hProjEq gat ga hgat_bound hga_closest').symm

noncomputable def FmaErr_gat_value_of_exact_difference_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (be1 r1 diff gat : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Exact-difference branch used by Coq `FmaErr` for the `gat` correction.

Upstream repeatedly uses `RoundedModeProjectorIdemEq` when the exact input
`be1 - r1` is already represented by a bounded float.  This helper packages
that step for the closest-rounding relation: a closest rounding of that exact
bounded value represents the same real number. -/
theorem FmaErr_gat_value_of_exact_difference {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (be1 r1 diff gat : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜TotalP (Closest (beta:=beta) bo radix) ∧
        Fbounded (beta:=beta) bo diff ∧
        _root_.F2R (beta:=beta) diff =
          _root_.F2R (beta:=beta) be1 - _root_.F2R (beta:=beta) r1 ∧
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) be1 - _root_.F2R (beta:=beta) r1) gat⌝⦄
    (pure (FmaErr_gat_value_of_exact_difference_check
      (beta:=beta) bo radix be1 r1 diff gat) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) gat =
      _root_.F2R (beta:=beta) diff⌝⦄ := by
  intro h
  rcases h with ⟨hTotal, hdiff_bound, hdiff_val, hgat_closest⟩
  simp only [wp, PostCond.noThrow, pure,
    FmaErr_gat_value_of_exact_difference_check, Id.run, ULift.up_down]
  have hRounded : RoundedModeP_full (beta:=beta) bo (Closest (beta:=beta) bo radix) := by
    have h := ClosestRoundedModeP_from_total_payload (beta:=beta) bo radix
    simpa [wp, PostCond.noThrow, pure, ClosestRoundedModeP_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hTotal
  rcases hRounded with ⟨_, _, _, _, hProjEq⟩
  have hgat_closest' :
      Closest (beta:=beta) bo radix (_root_.F2R (beta:=beta) diff) gat := by
    rw [hdiff_val]
    exact hgat_closest
  exact (hProjEq diff gat hdiff_bound hgat_closest').symm

noncomputable def FmaErr_aux1_from_difference_payload_check
    {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (a x y r1 u1 u2 al1 al2 be1 be2 diff gat ga :
      FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FmaErr_aux1`, the zero-`be2` FMA correction branch.

This branch is the one selected in upstream `FmaErr_aux` when `be2` represents
zero.  The two rounded correction steps are discharged by the local projector
helpers above, and the remaining equality is the algebraic reconstruction used
by `FmaErr`. -/
theorem FmaErr_aux1_from_difference_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (a x y r1 u1 u2 al1 al2 be1 be2 diff gat ga :
      FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜TotalP (Closest (beta:=beta) bo radix) ∧
        Fbounded (beta:=beta) bo diff ∧
        _root_.F2R (beta:=beta) diff =
          _root_.F2R (beta:=beta) be1 - _root_.F2R (beta:=beta) r1 ∧
        _root_.F2R (beta:=beta) be2 = 0 ∧
        _root_.F2R (beta:=beta) u2 =
          _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
            _root_.F2R (beta:=beta) u1 ∧
        _root_.F2R (beta:=beta) al2 =
          _root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) u2 -
            _root_.F2R (beta:=beta) al1 ∧
        _root_.F2R (beta:=beta) be2 =
          _root_.F2R (beta:=beta) u1 + _root_.F2R (beta:=beta) al1 -
            _root_.F2R (beta:=beta) be1 ∧
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) be1 - _root_.F2R (beta:=beta) r1) gat ∧
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) gat + _root_.F2R (beta:=beta) be2) ga⌝⦄
    (pure (FmaErr_aux1_from_difference_payload_check
      (beta:=beta) bo radix a x y r1 u1 u2 al1 al2 be1 be2 diff gat ga) :
      Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
          _root_.F2R (beta:=beta) y =
        _root_.F2R (beta:=beta) r1 + _root_.F2R (beta:=beta) ga +
          _root_.F2R (beta:=beta) al2⌝⦄ := by
  intro h
  rcases h with
    ⟨hTotal, hdiff_bound, hdiff_val, hbe2_zero, hu2_val, hal2_val, hbe2_val,
      hgat_closest, hga_closest⟩
  simp only [wp, PostCond.noThrow, pure,
    FmaErr_aux1_from_difference_payload_check, Id.run,
    ULift.up_down]
  have hgat_val :
      _root_.F2R (beta:=beta) gat = _root_.F2R (beta:=beta) diff := by
    have h := FmaErr_gat_value_of_exact_difference
      (beta:=beta) bo radix be1 r1 diff gat
    simpa [wp, PostCond.noThrow, pure,
      FmaErr_gat_value_of_exact_difference_check, Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hTotal, hdiff_bound, hdiff_val, hgat_closest⟩
  have hga_gat :
      _root_.F2R (beta:=beta) ga = _root_.F2R (beta:=beta) gat := by
    have h := FmaErr_ga_value_of_be2_zero (beta:=beta) bo radix gat be2 ga
    simpa [wp, PostCond.noThrow, pure, FmaErr_ga_value_of_be2_zero_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hTotal, hgat_closest.1, hbe2_zero, hga_closest⟩
  have hga_val :
      _root_.F2R (beta:=beta) ga =
        _root_.F2R (beta:=beta) be1 - _root_.F2R (beta:=beta) r1 +
          _root_.F2R (beta:=beta) be2 := by
    rw [hga_gat, hgat_val, hdiff_val, hbe2_zero]
    ring
  have hmain := FmaErr_reconstruct_from_ga_correction
    (beta:=beta) a x y r1 u1 u2 al1 al2 be1 be2 ga
  simpa [wp, PostCond.noThrow, pure,
    FmaErr_reconstruct_from_ga_correction_check, Id.run, ULift.up_down, Int.cast_ofNat] using
    hmain ⟨hu2_val, hal2_val, hbe2_val, hga_val⟩

noncomputable def FmaErr_ga_value_of_correction_witness_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (gat be2 corr ga : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Final projector step used by Coq `FmaErr_aux2`.

After upstream `gaCorrect` constructs a bounded float representing
`gat + be2`, the closest rounding defining `ga` must have the same real value as
that witness.  This helper isolates that final rounded-mode idempotence step. -/
theorem FmaErr_ga_value_of_correction_witness {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (gat be2 corr ga : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜TotalP (Closest (beta:=beta) bo radix) ∧
        Fbounded (beta:=beta) bo corr ∧
        _root_.F2R (beta:=beta) corr =
          _root_.F2R (beta:=beta) gat + _root_.F2R (beta:=beta) be2 ∧
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) gat + _root_.F2R (beta:=beta) be2) ga⌝⦄
    (pure (FmaErr_ga_value_of_correction_witness_check
      (beta:=beta) bo radix gat be2 corr ga) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) ga =
      _root_.F2R (beta:=beta) corr⌝⦄ := by
  intro h
  rcases h with ⟨hTotal, hcorr_bound, hcorr_val, hga_closest⟩
  simp only [wp, PostCond.noThrow, pure,
    FmaErr_ga_value_of_correction_witness_check, Id.run, ULift.up_down]
  have hRounded : RoundedModeP_full (beta:=beta) bo (Closest (beta:=beta) bo radix) := by
    have h := ClosestRoundedModeP_from_total_payload (beta:=beta) bo radix
    simpa [wp, PostCond.noThrow, pure, ClosestRoundedModeP_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hTotal
  rcases hRounded with ⟨_, _, _, _, hProjEq⟩
  have hga_closest' :
      Closest (beta:=beta) bo radix (_root_.F2R (beta:=beta) corr) ga := by
    rw [hcorr_val]
    exact hga_closest
  exact (hProjEq corr ga hcorr_bound hga_closest').symm

noncomputable def FmaErr_aux2_from_correction_payload_check
    {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (a x y r1 u1 u2 al1 al2 be1 be2 diff gat corr ga :
      FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FmaErr_aux2`, assembled from the `gatCorrect` and `gaCorrect` witnesses.

The hard work in the upstream nonzero-`be2` branch is constructing bounded
witnesses for `be1 - r1` and for `be1 - r1 + be2`.  Once those witnesses are
available, this theorem performs the rounded-mode projector steps and the final
algebraic reconstruction. -/
theorem FmaErr_aux2_from_correction_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (a x y r1 u1 u2 al1 al2 be1 be2 diff gat corr ga :
      FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜TotalP (Closest (beta:=beta) bo radix) ∧
        Fbounded (beta:=beta) bo diff ∧
        _root_.F2R (beta:=beta) diff =
          _root_.F2R (beta:=beta) be1 - _root_.F2R (beta:=beta) r1 ∧
        Fbounded (beta:=beta) bo corr ∧
        _root_.F2R (beta:=beta) corr =
          _root_.F2R (beta:=beta) gat + _root_.F2R (beta:=beta) be2 ∧
        _root_.F2R (beta:=beta) u2 =
          _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
            _root_.F2R (beta:=beta) u1 ∧
        _root_.F2R (beta:=beta) al2 =
          _root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) u2 -
            _root_.F2R (beta:=beta) al1 ∧
        _root_.F2R (beta:=beta) be2 =
          _root_.F2R (beta:=beta) u1 + _root_.F2R (beta:=beta) al1 -
            _root_.F2R (beta:=beta) be1 ∧
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) be1 - _root_.F2R (beta:=beta) r1) gat ∧
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) gat + _root_.F2R (beta:=beta) be2) ga⌝⦄
    (pure (FmaErr_aux2_from_correction_payload_check
      (beta:=beta) bo radix a x y r1 u1 u2 al1 al2 be1 be2 diff gat corr ga) :
      Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
          _root_.F2R (beta:=beta) y =
        _root_.F2R (beta:=beta) r1 + _root_.F2R (beta:=beta) ga +
          _root_.F2R (beta:=beta) al2⌝⦄ := by
  intro h
  rcases h with
    ⟨hTotal, hdiff_bound, hdiff_val, hcorr_bound, hcorr_val, hu2_val,
      hal2_val, hbe2_val, hgat_closest, hga_closest⟩
  simp only [wp, PostCond.noThrow, pure,
    FmaErr_aux2_from_correction_payload_check, Id.run,
    ULift.up_down]
  have hgat_val :
      _root_.F2R (beta:=beta) gat = _root_.F2R (beta:=beta) diff := by
    have h := FmaErr_gat_value_of_exact_difference
      (beta:=beta) bo radix be1 r1 diff gat
    simpa [wp, PostCond.noThrow, pure,
      FmaErr_gat_value_of_exact_difference_check, Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hTotal, hdiff_bound, hdiff_val, hgat_closest⟩
  have hga_corr :
      _root_.F2R (beta:=beta) ga = _root_.F2R (beta:=beta) corr := by
    have h := FmaErr_ga_value_of_correction_witness
      (beta:=beta) bo radix gat be2 corr ga
    simpa [wp, PostCond.noThrow, pure,
      FmaErr_ga_value_of_correction_witness_check, Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hTotal, hcorr_bound, hcorr_val, hga_closest⟩
  have hga_val :
      _root_.F2R (beta:=beta) ga =
        _root_.F2R (beta:=beta) be1 - _root_.F2R (beta:=beta) r1 +
          _root_.F2R (beta:=beta) be2 := by
    rw [hga_corr, hcorr_val, hgat_val, hdiff_val]
  have hmain := FmaErr_reconstruct_from_ga_correction
    (beta:=beta) a x y r1 u1 u2 al1 al2 be1 be2 ga
  simpa [wp, PostCond.noThrow, pure,
    FmaErr_reconstruct_from_ga_correction_check, Id.run, ULift.up_down, Int.cast_ofNat] using
    hmain ⟨hu2_val, hal2_val, hbe2_val, hga_val⟩

noncomputable def FmaErr_aux_from_correction_split_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (a x y r1 u1 u2 al1 al2 be1 be2 diff gat corr ga :
      FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FmaErr_aux`, dispatching the final correction proof.

The upstream proof splits on whether `be2` represents zero.  The zero branch is
`FmaErr_aux1`; the nonzero branch is `FmaErr_aux2` once `gaCorrect` has built a
bounded witness for `gat + be2`.  This theorem restores that branch dispatcher
without assuming the still-missing midpoint/size construction of the witness. -/
theorem FmaErr_aux_from_correction_split {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (a x y r1 u1 u2 al1 al2 be1 be2 diff gat corr ga :
      FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜TotalP (Closest (beta:=beta) bo radix) ∧
        Fbounded (beta:=beta) bo diff ∧
        _root_.F2R (beta:=beta) diff =
          _root_.F2R (beta:=beta) be1 - _root_.F2R (beta:=beta) r1 ∧
        _root_.F2R (beta:=beta) u2 =
          _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
            _root_.F2R (beta:=beta) u1 ∧
        _root_.F2R (beta:=beta) al2 =
          _root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) u2 -
            _root_.F2R (beta:=beta) al1 ∧
        _root_.F2R (beta:=beta) be2 =
          _root_.F2R (beta:=beta) u1 + _root_.F2R (beta:=beta) al1 -
            _root_.F2R (beta:=beta) be1 ∧
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) be1 - _root_.F2R (beta:=beta) r1) gat ∧
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) gat + _root_.F2R (beta:=beta) be2) ga ∧
        (_root_.F2R (beta:=beta) be2 = 0 ∨
          (Fbounded (beta:=beta) bo corr ∧
            _root_.F2R (beta:=beta) corr =
              _root_.F2R (beta:=beta) gat + _root_.F2R (beta:=beta) be2))⌝⦄
    (pure (FmaErr_aux_from_correction_split_check
      (beta:=beta) bo radix a x y r1 u1 u2 al1 al2 be1 be2 diff gat corr ga) :
      Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
          _root_.F2R (beta:=beta) y =
        _root_.F2R (beta:=beta) r1 + _root_.F2R (beta:=beta) ga +
          _root_.F2R (beta:=beta) al2⌝⦄ := by
  intro h
  rcases h with
    ⟨hTotal, hdiff_bound, hdiff_val, hu2_val, hal2_val, hbe2_val,
      hgat_closest, hga_closest, hsplit⟩
  simp only [wp, PostCond.noThrow, pure, FmaErr_aux_from_correction_split_check, Id.run,
    ULift.up_down]
  rcases hsplit with hbe2_zero | ⟨hcorr_bound, hcorr_val⟩
  · have h := FmaErr_aux1_from_difference_payload (beta:=beta) bo radix
      a x y r1 u1 u2 al1 al2 be1 be2 diff gat ga
    simpa [wp, PostCond.noThrow, pure,
      FmaErr_aux1_from_difference_payload_check, Id.run,
      ULift.up_down] using
      h ⟨hTotal, hdiff_bound, hdiff_val, hbe2_zero, hu2_val, hal2_val,
        hbe2_val, hgat_closest, hga_closest⟩
  · have h := FmaErr_aux2_from_correction_payload (beta:=beta) bo radix
      a x y r1 u1 u2 al1 al2 be1 be2 diff gat corr ga
    simpa [wp, PostCond.noThrow, pure,
      FmaErr_aux2_from_correction_payload_check, Id.run,
      ULift.up_down] using
      h ⟨hTotal, hdiff_bound, hdiff_val, hcorr_bound, hcorr_val, hu2_val,
        hal2_val, hbe2_val, hgat_closest, hga_closest⟩

noncomputable def FmaErr_core_from_aux_and_FTS_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a x y r1 u1 u2 al1 al2 be1 be2 diff gat corr ga :
      FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Core composition behind Coq `FmaErr`.

The remaining hard work for full `FmaErr` is constructing the correction split
used by `gaCorrect`.  Once that split is available, `FmaErr_aux` gives the
real-valued reconstruction equality and `Fma_FTS` gives the bounded
representatives needed by the wrapper layer. -/
theorem FmaErr_core_from_aux_and_FTS {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (a x y r1 u1 u2 al1 al2 be1 be2 diff gat corr ga :
      FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        1 < bo.vNum ∧
        bo.vNum = Zpower_nat radix precision ∧
        (∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=beta) radix r).Fexp) ∧
        TotalP (Closest (beta:=beta) bo (radix : ℝ)) ∧
        Fbounded (beta:=beta) bo a ∧
        Fbounded (beta:=beta) bo x ∧
        Fbounded (beta:=beta) bo y ∧
        -bo.dExp ≤ a.Fexp + x.Fexp ∧
        Fbounded (beta:=beta) bo diff ∧
        _root_.F2R (beta:=beta) diff =
          _root_.F2R (beta:=beta) be1 - _root_.F2R (beta:=beta) r1 ∧
        _root_.F2R (beta:=beta) u2 =
          _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
            _root_.F2R (beta:=beta) u1 ∧
        _root_.F2R (beta:=beta) al2 =
          _root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) u2 -
            _root_.F2R (beta:=beta) al1 ∧
        _root_.F2R (beta:=beta) be2 =
          _root_.F2R (beta:=beta) u1 + _root_.F2R (beta:=beta) al1 -
            _root_.F2R (beta:=beta) be1 ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) u1 ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) u2) al1 ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
            _root_.F2R (beta:=beta) y) r1 ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) u1 + _root_.F2R (beta:=beta) al1) be1 ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) be1 - _root_.F2R (beta:=beta) r1) gat ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) gat + _root_.F2R (beta:=beta) be2) ga ∧
        (_root_.F2R (beta:=beta) be2 = 0 ∨
          (Fbounded (beta:=beta) bo corr ∧
            _root_.F2R (beta:=beta) corr =
              _root_.F2R (beta:=beta) gat + _root_.F2R (beta:=beta) be2))⌝⦄
    (pure (FmaErr_core_from_aux_and_FTS_check
      (beta:=beta) bo radix precision
        a x y r1 u1 u2 al1 al2 be1 be2 diff gat corr ga) : Id Unit)
    ⦃⇓_ => ⌜(_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
          _root_.F2R (beta:=beta) y =
        _root_.F2R (beta:=beta) r1 + _root_.F2R (beta:=beta) ga +
          _root_.F2R (beta:=beta) al2) ∧
      ∃ ga_e al2_e : FloatSpec.Core.Defs.FlocqFloat beta,
        _root_.F2R (beta:=beta) ga_e = _root_.F2R (beta:=beta) ga ∧
        _root_.F2R (beta:=beta) al2_e = _root_.F2R (beta:=beta) al2 ∧
        Fbounded (beta:=beta) bo ga_e ∧
        Fbounded (beta:=beta) bo al2_e ∧
        al2_e.Fexp ≤ ga_e.Fexp⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hradix, hprecision, hvnum_gt, hvnum, hBoundExp, hTotal,
      ha, hx, hy, hprod_exp, hdiff_bound, hdiff_val, hu2_val, hal2_val,
      hbe2_val, hu1_closest, hal1_closest, hr1_closest, hbe1_closest,
      hgat_closest, hga_closest, hsplit⟩
  simp only [wp, PostCond.noThrow, pure, FmaErr_core_from_aux_and_FTS_check,
    Id.run, ULift.up_down]
  have heq :
      _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
          _root_.F2R (beta:=beta) y =
        _root_.F2R (beta:=beta) r1 + _root_.F2R (beta:=beta) ga +
          _root_.F2R (beta:=beta) al2 := by
    have h := FmaErr_aux_from_correction_split (beta:=beta) bo (radix : ℝ)
      a x y r1 u1 u2 al1 al2 be1 be2 diff gat corr ga
    simpa [wp, PostCond.noThrow, pure, FmaErr_aux_from_correction_split_check, Id.run,
      ULift.up_down] using
      h ⟨hTotal, hdiff_bound, hdiff_val, hu2_val, hal2_val, hbe2_val,
        hgat_closest, hga_closest, hsplit⟩
  have hfts :
      ∃ ga_e al2_e : FloatSpec.Core.Defs.FlocqFloat beta,
        _root_.F2R (beta:=beta) ga_e = _root_.F2R (beta:=beta) ga ∧
        _root_.F2R (beta:=beta) al2_e = _root_.F2R (beta:=beta) al2 ∧
        Fbounded (beta:=beta) bo ga_e ∧
        Fbounded (beta:=beta) bo al2_e ∧
        al2_e.Fexp ≤ ga_e.Fexp := by
    have h := Fma_FTS_from_closest_payload (beta:=beta) bo radix precision
      a x y r1 u1 u2 al1 al2 be1 be2 gat ga
    simpa [wp, PostCond.noThrow, pure, Fma_FTS_check, Id.run,
      ULift.up_down] using
      h ⟨hbeta, hradix, hprecision, hvnum_gt, hvnum, ha, hx, hy,
        hprod_exp, hu1_closest, hu2_val, hal1_closest, hal2_val, hr1_closest,
        hbe1_closest, hbe2_val, hgat_closest, hga_closest⟩
  exact ⟨heq, hfts⟩

noncomputable def FmaErr_from_correction_split_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a x y r1 u1 u2 al1 al2 be1 be2 diff gat corr ga :
      FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FmaErr`.

This is the restored lower FMA-error reconstruction payload once the remaining
`gaCorrect` correction split has been supplied.  It exposes the upstream name
while keeping that split explicit, and delegates the reconstruction and
bounded-representative packaging to `FmaErr_core_from_aux_and_FTS`. -/
theorem FmaErr_from_correction_split {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (a x y r1 u1 u2 al1 al2 be1 be2 diff gat corr ga :
      FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        1 < bo.vNum ∧
        bo.vNum = Zpower_nat radix precision ∧
        (∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=beta) radix r).Fexp) ∧
        TotalP (Closest (beta:=beta) bo (radix : ℝ)) ∧
        Fbounded (beta:=beta) bo a ∧
        Fbounded (beta:=beta) bo x ∧
        Fbounded (beta:=beta) bo y ∧
        -bo.dExp ≤ a.Fexp + x.Fexp ∧
        Fbounded (beta:=beta) bo diff ∧
        _root_.F2R (beta:=beta) diff =
          _root_.F2R (beta:=beta) be1 - _root_.F2R (beta:=beta) r1 ∧
        _root_.F2R (beta:=beta) u2 =
          _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
            _root_.F2R (beta:=beta) u1 ∧
        _root_.F2R (beta:=beta) al2 =
          _root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) u2 -
            _root_.F2R (beta:=beta) al1 ∧
        _root_.F2R (beta:=beta) be2 =
          _root_.F2R (beta:=beta) u1 + _root_.F2R (beta:=beta) al1 -
            _root_.F2R (beta:=beta) be1 ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) u1 ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) u2) al1 ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
            _root_.F2R (beta:=beta) y) r1 ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) u1 + _root_.F2R (beta:=beta) al1) be1 ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) be1 - _root_.F2R (beta:=beta) r1) gat ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) gat + _root_.F2R (beta:=beta) be2) ga ∧
        (_root_.F2R (beta:=beta) be2 = 0 ∨
          (Fbounded (beta:=beta) bo corr ∧
            _root_.F2R (beta:=beta) corr =
              _root_.F2R (beta:=beta) gat + _root_.F2R (beta:=beta) be2))⌝⦄
    (pure (FmaErr_from_correction_split_check
      (beta:=beta) bo radix precision
        a x y r1 u1 u2 al1 al2 be1 be2 diff gat corr ga) : Id Unit)
    ⦃⇓_ => ⌜(_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
          _root_.F2R (beta:=beta) y =
        _root_.F2R (beta:=beta) r1 + _root_.F2R (beta:=beta) ga +
          _root_.F2R (beta:=beta) al2) ∧
      ∃ ga_e al2_e : FloatSpec.Core.Defs.FlocqFloat beta,
        _root_.F2R (beta:=beta) ga_e = _root_.F2R (beta:=beta) ga ∧
        _root_.F2R (beta:=beta) al2_e = _root_.F2R (beta:=beta) al2 ∧
        Fbounded (beta:=beta) bo ga_e ∧
        Fbounded (beta:=beta) bo al2_e ∧
        al2_e.Fexp ≤ ga_e.Fexp⌝⦄ := by
  intro h
  simp only [wp, PostCond.noThrow, pure, FmaErr_from_correction_split_check, Id.run, ULift.up_down]
  have hcore := FmaErr_core_from_aux_and_FTS
    (beta:=beta) bo radix precision
      a x y r1 u1 u2 al1 al2 be1 be2 diff gat corr ga
  simpa [wp, PostCond.noThrow, pure, FmaErr_core_from_aux_and_FTS_check,
    Id.run, ULift.up_down, Int.cast_ofNat] using hcore h

noncomputable def FmaErr_gaCorrect_of_al2_zero_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (a x y r1 u1 u2 al1 al2 be1 be2 :
      FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- First branch of Coq `gaCorrect`, when `al2` represents zero.

In this branch, upstream proves `be1` and `r1` have the same real value using
the rounded-mode compatibility hypothesis `P2`; then `be2` itself is the
bounded correction witness for `be1 - r1 + be2`. -/
theorem FmaErr_gaCorrect_of_al2_zero {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (a x y r1 u1 u2 al1 al2 be1 be2 :
      FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜(∀ r s : ℝ, ∀ f g : FloatSpec.Core.Defs.FlocqFloat beta,
          P r f → P s g → r = s →
            _root_.F2R (beta:=beta) f = _root_.F2R (beta:=beta) g) ∧
        P (_root_.F2R (beta:=beta) u1 + _root_.F2R (beta:=beta) al1) be1 ∧
        P (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
          _root_.F2R (beta:=beta) y) r1 ∧
        Fbounded (beta:=beta) bo be2 ∧
        _root_.F2R (beta:=beta) al2 = 0 ∧
        _root_.F2R (beta:=beta) u2 =
          _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
            _root_.F2R (beta:=beta) u1 ∧
        _root_.F2R (beta:=beta) al2 =
          _root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) u2 -
            _root_.F2R (beta:=beta) al1⌝⦄
    (pure (FmaErr_gaCorrect_of_al2_zero_check
      (beta:=beta) bo P a x y r1 u1 u2 al1 al2 be1 be2) : Id Unit)
    ⦃⇓_ => ⌜∃ corr : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) corr =
        _root_.F2R (beta:=beta) be1 - _root_.F2R (beta:=beta) r1 +
          _root_.F2R (beta:=beta) be2 ∧
      Fbounded (beta:=beta) bo corr⌝⦄ := by
  intro h
  rcases h with
    ⟨hCompat, hbe1P, hr1P, hbe2_bound, hal2_zero, hu2_val, hal2_val⟩
  simp only [wp, PostCond.noThrow, pure, FmaErr_gaCorrect_of_al2_zero_check,
    Id.run, ULift.up_down]
  have hInputs :
      _root_.F2R (beta:=beta) u1 + _root_.F2R (beta:=beta) al1 =
        _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
          _root_.F2R (beta:=beta) y := by
    nlinarith
  have hbe1_r1 :
      _root_.F2R (beta:=beta) be1 = _root_.F2R (beta:=beta) r1 :=
    hCompat
      (_root_.F2R (beta:=beta) u1 + _root_.F2R (beta:=beta) al1)
      (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
        _root_.F2R (beta:=beta) y)
      be1 r1 hbe1P hr1P hInputs
  refine ⟨be2, ?_, hbe2_bound⟩
  rw [hbe1_r1]
  ring

noncomputable def FmaErr_al2_zero_of_u2_zero_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (y u2 al1 al2 : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- `u2 = 0` subcase used inside Coq `gaCorrect`.

In the second `gaCorrect` branch, upstream derives a contradiction from
`al2 ≠ 0` by showing that if `u2` represents zero then the rounded value `al1`
is exactly `y`, hence the represented `al2 = y + u2 - al1` is zero. -/
theorem FmaErr_al2_zero_of_u2_zero {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (y u2 al1 al2 : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜TotalP (Closest (beta:=beta) bo radix) ∧
        Fbounded (beta:=beta) bo y ∧
        _root_.F2R (beta:=beta) u2 = 0 ∧
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) u2) al1 ∧
        _root_.F2R (beta:=beta) al2 =
          _root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) u2 -
            _root_.F2R (beta:=beta) al1⌝⦄
    (pure (FmaErr_al2_zero_of_u2_zero_check
      (beta:=beta) bo radix y u2 al1 al2) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) al2 = 0⌝⦄ := by
  intro h
  rcases h with ⟨hTotal, hy_bound, hu2_zero, hal1_closest, hal2_val⟩
  simp only [wp, PostCond.noThrow, pure, FmaErr_al2_zero_of_u2_zero_check,
    Id.run, ULift.up_down]
  change _root_.F2R (beta:=beta) al2 = 0
  have hRounded : RoundedModeP_full (beta:=beta) bo (Closest (beta:=beta) bo radix) := by
    have h := ClosestRoundedModeP_from_total_payload (beta:=beta) bo radix
    simpa [wp, PostCond.noThrow, pure, ClosestRoundedModeP_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hTotal
  rcases hRounded with ⟨_, _, _, _, hProjEq⟩
  have hal1_closest' :
      Closest (beta:=beta) bo radix (_root_.F2R (beta:=beta) y) al1 := by
    rw [hu2_zero, add_zero] at hal1_closest
    exact hal1_closest
  have hy_al1 :
      _root_.F2R (beta:=beta) y = _root_.F2R (beta:=beta) al1 :=
    hProjEq y al1 hy_bound hal1_closest'
  rw [hal2_val, hu2_zero, hy_al1]
  ring

noncomputable def FmaErr_gaCorrect_of_be1_eq_r1_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel)
    (be1 r1 be2 : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Midpoint first branch of Coq `gaCorrect`.

After upstream `Midpoint_aux` proves that `be1` and `r1` have the same real
value, the correction target `be1 - r1 + be2` is represented by `be2` itself.
This packages the `exists be2; rewrite H; ring` branch used before the harder
bounded-sum branch. -/
theorem FmaErr_gaCorrect_of_be1_eq_r1 {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel)
    (be1 r1 be2 : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) bo be2 ∧
        _root_.F2R (beta:=beta) be1 = _root_.F2R (beta:=beta) r1⌝⦄
    (pure (FmaErr_gaCorrect_of_be1_eq_r1_check
      (beta:=beta) bo be1 r1 be2) : Id Unit)
    ⦃⇓_ => ⌜∃ corr : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) corr =
        _root_.F2R (beta:=beta) be1 - _root_.F2R (beta:=beta) r1 +
          _root_.F2R (beta:=beta) be2 ∧
      Fbounded (beta:=beta) bo corr⌝⦄ := by
  intro h
  rcases h with ⟨hbe2_bound, hbe1_r1⟩
  simp only [wp, PostCond.noThrow, pure, FmaErr_gaCorrect_of_be1_eq_r1_check,
    Id.run, ULift.up_down]
  refine ⟨be2, ?_, hbe2_bound⟩
  rw [hbe1_r1]
  ring

/-- Coq discriminant helper `P_positive`.

A closest rounded value of a nonnegative real is nonnegative: otherwise the
bounded zero float would be strictly closer to the input than the negative
candidate. -/
theorem P_positive {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ) (r : ℝ)
    (p : FloatSpec.Core.Defs.FlocqFloat beta)
    (hp : Closest (beta:=beta) bo radix r p)
    (hr_nonneg : 0 ≤ r) :
    0 ≤ _root_.F2R (beta:=beta) p := by
  by_contra hp_not_nonneg
  have hp_neg : _root_.F2R (beta:=beta) p < 0 :=
    lt_of_not_ge hp_not_nonneg
  let zeroFloat : FloatSpec.Core.Defs.FlocqFloat beta := ⟨0, -bo.dExp⟩
  have hvNum_pos : 0 < bo.vNum :=
    lt_of_le_of_lt (abs_nonneg p.Fnum) hp.1.1
  have hZeroBounded : Fbounded (beta:=beta) bo zeroFloat := by
    constructor
    · show |zeroFloat.Fnum| < bo.vNum
      simp [zeroFloat, hvNum_pos]
    · show -bo.dExp ≤ zeroFloat.Fexp
      rfl
  have hZeroF2R : _root_.F2R (beta:=beta) zeroFloat = 0 := by
    unfold _root_.F2R FloatSpec.Core.Defs.F2R zeroFloat
    ring
  have hdist :
      |_root_.F2R (beta:=beta) p - r| ≤
        |_root_.F2R (beta:=beta) zeroFloat - r| :=
    hp.2 zeroFloat hZeroBounded
  have hp_dist :
      |_root_.F2R (beta:=beta) p - r| =
        r - _root_.F2R (beta:=beta) p := by
    have hnonpos : _root_.F2R (beta:=beta) p - r ≤ 0 := by
      linarith
    rw [abs_of_nonpos hnonpos]
    ring
  have hzero_dist :
      |_root_.F2R (beta:=beta) zeroFloat - r| = r := by
    rw [hZeroF2R, zero_sub, abs_neg, abs_of_nonneg hr_nonneg]
  rw [hp_dist, hzero_dist] at hdist
  linarith

noncomputable def Half_Closest_Round_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (r : ℝ)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq discriminant helper `Half_Closest_Round`.

At radix 2, halving a closest rounded float by decrementing its exponent gives
a closest rounded value for the halved real input, provided the decremented
exponent remains inside the bound. -/
theorem Half_Closest_Round {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (r : ℝ)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = 2 ∧ -bo.dExp ≤ x.Fexp - 1 ∧
        Closest (beta:=beta) bo (2 : ℝ) r x⌝⦄
    (pure (Half_Closest_Round_check (beta:=beta) bo r x) : Id Unit)
    ⦃⇓_ => ⌜Closest (beta:=beta) bo (2 : ℝ) (r / 2)
      ({ Fnum := x.Fnum, Fexp := x.Fexp - 1 } :
        FloatSpec.Core.Defs.FlocqFloat beta)⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hexp, hxClosest⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure, Half_Closest_Round_check,
    Id.run, ULift.up_down]
  let xHalf : FloatSpec.Core.Defs.FlocqFloat 2 :=
    { Fnum := x.Fnum, Fexp := x.Fexp - 1 }
  have htwo_ne : ((2 : Int) : ℝ) ≠ 0 := by norm_num
  have hxHalfVal :
      _root_.F2R (beta:=2) xHalf =
        _root_.F2R (beta:=2) x / 2 := by
    have hpow :
        ((2 : Int) : ℝ) ^ x.Fexp =
          ((2 : Int) : ℝ) ^ (x.Fexp - 1) * 2 := by
      have hsum : x.Fexp - 1 + 1 = x.Fexp := by omega
      calc
        ((2 : Int) : ℝ) ^ x.Fexp =
            ((2 : Int) : ℝ) ^ (x.Fexp - 1 + 1) := by rw [hsum]
        _ = ((2 : Int) : ℝ) ^ (x.Fexp - 1) *
              ((2 : Int) : ℝ) ^ (1 : Int) := by
              rw [zpow_add₀ htwo_ne]
        _ = ((2 : Int) : ℝ) ^ (x.Fexp - 1) * 2 := by norm_num
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, xHalf,
      FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp]
    rw [hpow]
    ring
  constructor
  · rcases hxClosest.1 with ⟨hnum, _⟩
    exact ⟨hnum, hexp⟩
  · intro g hg
    let gDouble : FloatSpec.Core.Defs.FlocqFloat 2 :=
      { Fnum := g.Fnum, Fexp := g.Fexp + 1 }
    have hgDoubleBounded : Fbounded (beta:=2) bo gDouble := by
      rcases hg with ⟨hnum, hgexp⟩
      constructor
      · simpa [gDouble] using hnum
      · change -bo.dExp ≤ g.Fexp + 1
        omega
    have hgDoubleVal :
        _root_.F2R (beta:=2) gDouble =
          2 * _root_.F2R (beta:=2) g := by
      have hpow :
          ((2 : Int) : ℝ) ^ (g.Fexp + 1) =
            ((2 : Int) : ℝ) ^ g.Fexp * 2 := by
        calc
          ((2 : Int) : ℝ) ^ (g.Fexp + 1) =
              ((2 : Int) : ℝ) ^ g.Fexp *
                ((2 : Int) : ℝ) ^ (1 : Int) := by
                rw [zpow_add₀ htwo_ne]
          _ = ((2 : Int) : ℝ) ^ g.Fexp * 2 := by norm_num
      simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, gDouble,
        FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp]
      rw [hpow]
      ring
    have hclosest := hxClosest.2 gDouble hgDoubleBounded
    have hx_scaled :
        |_root_.F2R (beta:=2) x - r| =
          2 * |_root_.F2R (beta:=2) xHalf - r / 2| := by
      rw [hxHalfVal]
      have harg :
          _root_.F2R (beta:=2) x / 2 - r / 2 =
            (_root_.F2R (beta:=2) x - r) / 2 := by ring
      rw [harg, abs_div]
      norm_num
      ring
    have hg_scaled :
        |_root_.F2R (beta:=2) gDouble - r| =
          2 * |_root_.F2R (beta:=2) g - r / 2| := by
      rw [hgDoubleVal]
      have harg :
          2 * _root_.F2R (beta:=2) g - r =
            2 * (_root_.F2R (beta:=2) g - r / 2) := by ring
      rw [harg, abs_mul]
      norm_num
    rw [hx_scaled, hg_scaled] at hclosest
    linarith

noncomputable def FNeven_double_of_Fnormal_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Parity-preservation component of Coq `Twice_EvenClosest_Round`.

For a normal radix-2 float, incrementing the exponent keeps the normalized
mantissa unchanged.  Thus the normalized-even side condition in an
`EvenClosest` witness survives the doubling step. -/
theorem FNeven_double_of_Fnormal {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = 2 ∧ precision ≠ 0 ∧
        bo.vNum = Zpower_nat 2 precision ∧
        Fnormal (beta:=beta) 2 bo x ∧
        FNeven (beta:=beta) bo (2 : ℝ) precision x⌝⦄
    (pure (FNeven_double_of_Fnormal_check (beta:=beta) bo precision x) :
      Id Unit)
    ⦃⇓_ => ⌜FNeven (beta:=beta) bo (2 : ℝ) precision
      ({ Fnum := x.Fnum, Fexp := x.Fexp + 1 } :
        FloatSpec.Core.Defs.FlocqFloat beta)⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hprecision, hvNum, hxNormal, hxEven⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure, FNeven_double_of_Fnormal_check,
    Id.run, ULift.up_down]
  let xDouble : FloatSpec.Core.Defs.FlocqFloat 2 :=
    { Fnum := x.Fnum, Fexp := x.Fexp + 1 }
  have hradix : (1 : Int) < 2 := by decide
  have hxCan : Fcanonic (beta:=2) 2 bo x := Or.inl hxNormal
  have hxNorm :
      Fnormalize (beta:=2) 2 bo precision x = x := by
    have h := FcanonicFnormalizeEq (beta:=2) 2 bo precision x
    simpa [wp, PostCond.noThrow, pure, FcanonicFnormalizeEq_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hxCan, rfl, hradix, hprecision, hvNum⟩
  have hxDoubleNormal : Fnormal (beta:=2) 2 bo xDouble := by
    constructor
    · constructor
      · simpa [xDouble] using hxNormal.1.1
      · change -bo.dExp ≤ x.Fexp + 1
        exact le_trans hxNormal.1.2 (by omega)
    · simpa [xDouble] using hxNormal.2
  have hxDoubleCan : Fcanonic (beta:=2) 2 bo xDouble := Or.inl hxDoubleNormal
  have hxDoubleNorm :
      Fnormalize (beta:=2) 2 bo precision xDouble = xDouble := by
    have h := FcanonicFnormalizeEq (beta:=2) 2 bo precision xDouble
    simpa [wp, PostCond.noThrow, pure, FcanonicFnormalizeEq_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hxDoubleCan, rfl, hradix, hprecision, hvNum⟩
  simpa [FNeven, Feven, xDouble, hxNorm, hxDoubleNorm] using hxEven

noncomputable def Twice_EvenClosest_Round_from_closest_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) : Unit :=
  ()

/-- Final tie-breaking step for Coq `Twice_EvenClosest_Round`.

The only missing part of the full upstream theorem is the scaled closestness
argument.  Once the doubled float is known to be closest to `2 * r`, the
normal/parity payload above turns it into an `EvenClosest` result. -/
theorem Twice_EvenClosest_Round_from_closest {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) :
    ⦃⌜beta = 2 ∧ precision ≠ 0 ∧
        bo.vNum = Zpower_nat 2 precision ∧
        Fnormal (beta:=beta) 2 bo x ∧
        FNeven (beta:=beta) bo (2 : ℝ) precision x ∧
        Closest (beta:=beta) bo (2 : ℝ) (2 * r)
          ({ Fnum := x.Fnum, Fexp := x.Fexp + 1 } :
            FloatSpec.Core.Defs.FlocqFloat beta)⌝⦄
    (pure (Twice_EvenClosest_Round_from_closest_check
      (beta:=beta) bo precision x r) : Id Unit)
    ⦃⇓_ => ⌜EvenClosest (beta:=beta) bo (2 : ℝ) precision (2 * r)
      ({ Fnum := x.Fnum, Fexp := x.Fexp + 1 } :
        FloatSpec.Core.Defs.FlocqFloat beta)⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hprecision, hvNum, hxNormal, hxEven, hxDoubleClosest⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure,
    Twice_EvenClosest_Round_from_closest_check, Id.run, ULift.up_down]
  constructor
  · exact hxDoubleClosest
  · left
    have h := FNeven_double_of_Fnormal (beta:=2) bo precision x
    simpa [wp, PostCond.noThrow, pure,
      FNeven_double_of_Fnormal_check, Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, hprecision, hvNum, hxNormal, hxEven⟩

noncomputable def Closest_double_of_halvable_competitors_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) : Unit :=
  ()

/-- Scaled closestness core for Coq `Twice_EvenClosest_Round`.

If every bounded competitor for the doubled target has a bounded half with the
corresponding real value, closestness at `r` scales to closestness at `2 * r`.
The remaining work for the full upstream theorem is exactly proving this
halvability premise at the subnormal boundary. -/
theorem Closest_double_of_halvable_competitors {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) :
    ⦃⌜beta = 2 ∧
        Fbounded (beta:=beta) bo
          ({ Fnum := x.Fnum, Fexp := x.Fexp + 1 } :
            FloatSpec.Core.Defs.FlocqFloat beta) ∧
        _root_.F2R (beta:=beta)
          ({ Fnum := x.Fnum, Fexp := x.Fexp + 1 } :
            FloatSpec.Core.Defs.FlocqFloat beta) =
          2 * _root_.F2R (beta:=beta) x ∧
        Closest (beta:=beta) bo (2 : ℝ) r x ∧
        (∀ g : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded (beta:=beta) bo g →
            ∃ gh : FloatSpec.Core.Defs.FlocqFloat beta,
              Fbounded (beta:=beta) bo gh ∧
                _root_.F2R (beta:=beta) gh =
                  _root_.F2R (beta:=beta) g / 2)⌝⦄
    (pure (Closest_double_of_halvable_competitors_check
      (beta:=beta) bo x r) : Id Unit)
    ⦃⇓_ => ⌜Closest (beta:=beta) bo (2 : ℝ) (2 * r)
      ({ Fnum := x.Fnum, Fexp := x.Fexp + 1 } :
        FloatSpec.Core.Defs.FlocqFloat beta)⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hxDoubleBound, hxDoubleVal, hxClosest, hhalve⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure,
    Closest_double_of_halvable_competitors_check, Id.run, ULift.up_down]
  constructor
  · exact hxDoubleBound
  · intro g hg
    rcases hhalve g hg with ⟨gh, hghBound, hghVal⟩
    have hclosest := hxClosest.2 gh hghBound
    have hx_scaled :
        |_root_.F2R (beta:=2)
              ({ Fnum := x.Fnum, Fexp := x.Fexp + 1 } :
                FloatSpec.Core.Defs.FlocqFloat 2) - 2 * r| =
          2 * |_root_.F2R (beta:=2) x - r| := by
      rw [hxDoubleVal]
      have harg :
          2 * _root_.F2R (beta:=2) x - 2 * r =
            2 * (_root_.F2R (beta:=2) x - r) := by ring
      rw [harg, abs_mul]
      norm_num
    have hg_scaled :
        |_root_.F2R (beta:=2) g - 2 * r| =
          2 * |_root_.F2R (beta:=2) gh - r| := by
      rw [hghVal]
      have harg :
          _root_.F2R (beta:=2) g / 2 - r =
            (_root_.F2R (beta:=2) g - 2 * r) / 2 := by ring
      rw [harg, abs_div]
      norm_num
      ring
    rw [hx_scaled, hg_scaled]
    nlinarith

noncomputable def Fbounded_half_even_or_high_exp_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (g : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Bounded halving lemma for the remaining `Twice_EvenClosest_Round` boundary.

A bounded radix-2 float can be halved as a bounded float if either its exponent
can be decremented without underflow, or its mantissa is even and can be halved
at the same exponent. -/
theorem Fbounded_half_even_or_high_exp {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (g : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = 2 ∧ Fbounded (beta:=beta) bo g ∧
        (-bo.dExp ≤ g.Fexp - 1 ∨ Even g.Fnum)⌝⦄
    (pure (Fbounded_half_even_or_high_exp_check (beta:=beta) bo g) : Id Unit)
    ⦃⇓_ => ⌜∃ gh : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo gh ∧
        _root_.F2R (beta:=beta) gh =
          _root_.F2R (beta:=beta) g / 2⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hgBound, hcase⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure,
    Fbounded_half_even_or_high_exp_check, Id.run, ULift.up_down]
  rcases hgBound with ⟨hgNum, hgExp⟩
  rcases hcase with hgExpPred | hgEven
  · let gh : FloatSpec.Core.Defs.FlocqFloat 2 :=
      { Fnum := g.Fnum, Fexp := g.Fexp - 1 }
    refine ⟨gh, ?_, ?_⟩
    · exact ⟨by simpa [gh] using hgNum, by simpa [gh] using hgExpPred⟩
    · have htwo_ne : ((2 : Int) : ℝ) ≠ 0 := by norm_num
      have hpow :
          ((2 : Int) : ℝ) ^ g.Fexp =
            ((2 : Int) : ℝ) ^ (g.Fexp - 1) * 2 := by
        have hsum : g.Fexp - 1 + 1 = g.Fexp := by omega
        calc
          ((2 : Int) : ℝ) ^ g.Fexp =
              ((2 : Int) : ℝ) ^ (g.Fexp - 1 + 1) := by rw [hsum]
          _ = ((2 : Int) : ℝ) ^ (g.Fexp - 1) *
                ((2 : Int) : ℝ) ^ (1 : Int) := by
                rw [zpow_add₀ htwo_ne]
          _ = ((2 : Int) : ℝ) ^ (g.Fexp - 1) * 2 := by norm_num
      simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, gh,
        FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp]
      rw [hpow]
      ring
  · rcases hgEven with ⟨k, hk⟩
    let gh : FloatSpec.Core.Defs.FlocqFloat 2 :=
      { Fnum := k, Fexp := g.Fexp }
    refine ⟨gh, ?_, ?_⟩
    · constructor
      · have hgNum' : |2 * k| < bo.vNum := by
          have hkk : k + k = 2 * k := by ring
          simpa [hk, hkk] using hgNum
        have hkAbsLe : |k| ≤ |2 * k| := by
          rw [abs_mul]
          have hkAbsNonneg : 0 ≤ |k| := abs_nonneg k
          norm_num
          omega
        exact lt_of_le_of_lt hkAbsLe hgNum'
      · simpa [gh] using hgExp
    · simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, gh,
        FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp]
      rw [hk]
      rw [Int.cast_add]
      ring_nf

noncomputable def Closest_double_of_even_or_high_competitors_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) : Unit :=
  ()

/-- Boundary bridge for Coq `Twice_EvenClosest_Round`.

This packages the checked halving lemma into the scaled closestness proof: if
every bounded competitor for `2*r` either has room to decrement its exponent or
has an even mantissa, then the doubled float is closest to `2*r`. -/
theorem Closest_double_of_even_or_high_competitors {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) :
    ⦃⌜beta = 2 ∧
        Fbounded (beta:=beta) bo
          ({ Fnum := x.Fnum, Fexp := x.Fexp + 1 } :
            FloatSpec.Core.Defs.FlocqFloat beta) ∧
        _root_.F2R (beta:=beta)
          ({ Fnum := x.Fnum, Fexp := x.Fexp + 1 } :
            FloatSpec.Core.Defs.FlocqFloat beta) =
          2 * _root_.F2R (beta:=beta) x ∧
        Closest (beta:=beta) bo (2 : ℝ) r x ∧
        (∀ g : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded (beta:=beta) bo g →
            -bo.dExp ≤ g.Fexp - 1 ∨ Even g.Fnum)⌝⦄
    (pure (Closest_double_of_even_or_high_competitors_check
      (beta:=beta) bo x r) : Id Unit)
    ⦃⇓_ => ⌜Closest (beta:=beta) bo (2 : ℝ) (2 * r)
      ({ Fnum := x.Fnum, Fexp := x.Fexp + 1 } :
        FloatSpec.Core.Defs.FlocqFloat beta)⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hxDoubleBound, hxDoubleVal, hxClosest, hEvenOrHigh⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure,
    Closest_double_of_even_or_high_competitors_check, Id.run, ULift.up_down]
  have hhalve :
      ∀ g : FloatSpec.Core.Defs.FlocqFloat 2,
        Fbounded (beta:=2) bo g →
          ∃ gh : FloatSpec.Core.Defs.FlocqFloat 2,
            Fbounded (beta:=2) bo gh ∧
              _root_.F2R (beta:=2) gh =
                _root_.F2R (beta:=2) g / 2 := by
    intro g hg
    have h := Fbounded_half_even_or_high_exp (beta:=2) bo g
    simpa [wp, PostCond.noThrow, pure,
      Fbounded_half_even_or_high_exp_check, Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, hg, hEvenOrHigh g hg⟩
  have hclosest := Closest_double_of_halvable_competitors (beta:=2) bo x r
  simpa [wp, PostCond.noThrow, pure,
    Closest_double_of_halvable_competitors_check, Id.run, ULift.up_down, Int.cast_ofNat] using
    hclosest ⟨rfl, hxDoubleBound, hxDoubleVal, hxClosest, hhalve⟩

noncomputable def Twice_EvenClosest_Round_from_even_or_high_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) : Unit :=
  ()

/-- Composed boundary-reduced form of Coq `Twice_EvenClosest_Round`.

The only remaining unproved boundary payload is now the arithmetic fact that
actual closestness competitors satisfy the even-or-high-exponent premise. -/
theorem Twice_EvenClosest_Round_from_even_or_high {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) :
    ⦃⌜beta = 2 ∧ precision ≠ 0 ∧
        bo.vNum = Zpower_nat 2 precision ∧
        Fnormal (beta:=beta) 2 bo x ∧
        FNeven (beta:=beta) bo (2 : ℝ) precision x ∧
        EvenClosest (beta:=beta) bo (2 : ℝ) precision r x ∧
        (∀ g : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded (beta:=beta) bo g →
            -bo.dExp ≤ g.Fexp - 1 ∨ Even g.Fnum)⌝⦄
    (pure (Twice_EvenClosest_Round_from_even_or_high_check
      (beta:=beta) bo precision x r) : Id Unit)
    ⦃⇓_ => ⌜EvenClosest (beta:=beta) bo (2 : ℝ) precision (2 * r)
      ({ Fnum := x.Fnum, Fexp := x.Fexp + 1 } :
        FloatSpec.Core.Defs.FlocqFloat beta)⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hprecision, hvNum, hxNormal, hxEven, hxEvenClosest, hEvenOrHigh⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure,
    Twice_EvenClosest_Round_from_even_or_high_check, Id.run, ULift.up_down]
  let xDouble : FloatSpec.Core.Defs.FlocqFloat 2 :=
    { Fnum := x.Fnum, Fexp := x.Fexp + 1 }
  have hxDoubleBound : Fbounded (beta:=2) bo xDouble := by
    rcases hxNormal with ⟨hxBound, _hxMagnitude⟩
    constructor
    · simpa [xDouble] using hxBound.1
    · change -bo.dExp ≤ x.Fexp + 1
      exact le_trans hxBound.2 (by omega)
  have hxDoubleVal :
      _root_.F2R (beta:=2) xDouble =
        2 * _root_.F2R (beta:=2) x := by
    have htwo_ne : ((2 : Int) : ℝ) ≠ 0 := by norm_num
    have hpow :
        ((2 : Int) : ℝ) ^ (x.Fexp + 1) =
          ((2 : Int) : ℝ) ^ x.Fexp * 2 := by
      calc
        ((2 : Int) : ℝ) ^ (x.Fexp + 1) =
            ((2 : Int) : ℝ) ^ x.Fexp *
              ((2 : Int) : ℝ) ^ (1 : Int) := by
              rw [zpow_add₀ htwo_ne]
        _ = ((2 : Int) : ℝ) ^ x.Fexp * 2 := by norm_num
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, xDouble,
      FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp]
    rw [hpow]
    ring
  have hxDoubleClosest :
      Closest (beta:=2) bo (2 : ℝ) (2 * r) xDouble := by
    have h := Closest_double_of_even_or_high_competitors (beta:=2) bo x r
    simpa [wp, PostCond.noThrow, pure,
      Closest_double_of_even_or_high_competitors_check, Id.run, ULift.up_down,
      xDouble] using
      h ⟨rfl, hxDoubleBound, by simpa [xDouble] using hxDoubleVal,
        hxEvenClosest.1, hEvenOrHigh⟩
  have hfinal := Twice_EvenClosest_Round_from_closest (beta:=2) bo precision x r
  simpa [wp, PostCond.noThrow, pure,
    Twice_EvenClosest_Round_from_closest_check, Id.run, ULift.up_down,
    xDouble] using
    hfinal ⟨rfl, hprecision, hvNum, hxNormal, hxEven,
      by simpa [xDouble] using hxDoubleClosest⟩

noncomputable def Twice_EvenClosest_Round_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) : Unit :=
  ()

noncomputable def Fulp_le_twice_l_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (precision : Nat)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq discriminant helper `Fulp_le_twice_l`.

If a normal nonnegative float `x` is at most half of a bounded float `y`, then
the ulp at `y` is at least twice the ulp at `x`. -/
theorem Fulp_le_twice_l {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (precision : Nat)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = 2 ∧ precision ≠ 0 ∧ bo.vNum = Zpower_nat 2 precision ∧
        0 ≤ _root_.F2R (beta:=beta) x ∧
        Fnormal (beta:=beta) 2 bo x ∧
        Fbounded (beta:=beta) bo y ∧
        2 * _root_.F2R (beta:=beta) x ≤ _root_.F2R (beta:=beta) y⌝⦄
    (pure (Fulp_le_twice_l_check (beta:=beta) bo precision x y) : Id Unit)
    ⦃⇓_ => ⌜2 * Fulp (beta:=beta) bo 2 precision x ≤
      Fulp (beta:=beta) bo 2 precision y⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hprecision, hvNum, hx_nonneg, hx_normal, hy_bounded, hxy⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure, Fulp_le_twice_l_check,
    Id.run, ULift.up_down]
  let xDouble : FloatSpec.Core.Defs.FlocqFloat 2 :=
    { Fnum := x.Fnum, Fexp := x.Fexp + 1 }
  let yn : FloatSpec.Core.Defs.FlocqFloat 2 :=
    Fnormalize (beta:=2) 2 bo precision y
  have htwo_pos : (0 : ℝ) < ((2 : Int) : ℝ) := by norm_num
  have htwo_ne : ((2 : Int) : ℝ) ≠ 0 := ne_of_gt htwo_pos
  have hradix : (1 : Int) < 2 := by decide
  have hx_can : Fcanonic (beta:=2) 2 bo x := Or.inl hx_normal
  have hx_norm :
      Fnormalize (beta:=2) 2 bo precision x = x := by
    have h := FcanonicFnormalizeEq (beta:=2) 2 bo precision x
    simpa [wp, PostCond.noThrow, pure, FcanonicFnormalizeEq_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hx_can, rfl, hradix, hprecision, hvNum⟩
  have hxDouble_val :
      _root_.F2R (beta:=2) xDouble =
        2 * _root_.F2R (beta:=2) x := by
    have hpow :
        ((2 : Int) : ℝ) ^ (x.Fexp + 1) =
          ((2 : Int) : ℝ) ^ x.Fexp * 2 := by
      calc
        ((2 : Int) : ℝ) ^ (x.Fexp + 1) =
            ((2 : Int) : ℝ) ^ x.Fexp * ((2 : Int) : ℝ) ^ (1 : Int) := by
              rw [zpow_add₀ htwo_ne]
        _ = ((2 : Int) : ℝ) ^ x.Fexp * 2 := by norm_num
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, xDouble,
      FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp]
    rw [hpow]
    ring
  have hxDouble_can : Fcanonic (beta:=2) 2 bo xDouble := by
    left
    constructor
    · constructor
      · simpa [xDouble] using hx_normal.1.1
      · change -bo.dExp ≤ x.Fexp + 1
        exact le_trans hx_normal.1.2 (by omega)
    · simpa [xDouble] using hx_normal.2
  have hyn_val :
      _root_.F2R (beta:=2) yn = _root_.F2R (beta:=2) y := by
    have h := FnormalizeCorrect (beta:=2) 2 bo precision y
    simpa [wp, PostCond.noThrow, pure, FnormalizeCorrect_check,
      Id.run, ULift.up_down, yn] using h ⟨rfl, hradix⟩
  have hyn_can : Fcanonic (beta:=2) 2 bo yn := by
    have h := FnormalizeCanonic (beta:=2) 2 bo precision y
    simpa [wp, PostCond.noThrow, pure, FnormalizeCanonic_check,
      Id.run, ULift.up_down, yn] using
      h ⟨hy_bounded, hy_bounded, hprecision, hradix, hvNum⟩
  have hy_nonneg : 0 ≤ _root_.F2R (beta:=2) y := by
    nlinarith
  have hxDouble_nonneg : 0 ≤ _root_.F2R (beta:=2) xDouble := by
    rw [hxDouble_val]
    nlinarith
  have h_abs_le :
      |_root_.F2R (beta:=2) xDouble| ≤ |_root_.F2R (beta:=2) yn| := by
    rw [abs_of_nonneg hxDouble_nonneg, hyn_val, abs_of_nonneg hy_nonneg]
    rw [hxDouble_val]
    exact hxy
  have hexp_le :
      xDouble.Fexp ≤ yn.Fexp := by
    have h := Fcanonic_Rle_Zle (beta:=2) 2 bo xDouble yn hradix rfl
    simpa [wp, PostCond.noThrow, pure, Fcanonic_Rle_Zle_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hxDouble_can, hyn_can, h_abs_le⟩
  have hpow_le :
      ((2 : Int) : ℝ) ^ (x.Fexp + 1) ≤ ((2 : Int) : ℝ) ^ yn.Fexp :=
    zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ ((2 : Int) : ℝ)) hexp_le
  have hleft :
      2 * Fulp (beta:=2) bo 2 precision x =
        ((2 : Int) : ℝ) ^ (x.Fexp + 1) := by
    simp [Fulp, hx_norm]
    calc
      2 * ((2 : Int) : ℝ) ^ x.Fexp =
          ((2 : Int) : ℝ) ^ x.Fexp * 2 := by ring
      _ = ((2 : Int) : ℝ) ^ (x.Fexp + 1) := by
          rw [zpow_add₀ htwo_ne, zpow_one]
          norm_num
  have hright :
      Fulp (beta:=2) bo 2 precision y = ((2 : Int) : ℝ) ^ yn.Fexp := by
    simp [Fulp, yn]
  rw [hleft, hright]
  exact hpow_le

noncomputable def Fulp_le_twice_r_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (precision : Nat)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq discriminant helper `Fulp_le_twice_r`.

Symmetric ulp comparison: if a bounded nonnegative float `x` is at most twice
a normal float `y`, then the ulp at `x` is at most twice the ulp at `y`. -/
theorem Fulp_le_twice_r {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (precision : Nat)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = 2 ∧ precision ≠ 0 ∧ bo.vNum = Zpower_nat 2 precision ∧
        0 ≤ _root_.F2R (beta:=beta) x ∧
        Fbounded (beta:=beta) bo x ∧
        Fnormal (beta:=beta) 2 bo y ∧
        _root_.F2R (beta:=beta) x ≤ 2 * _root_.F2R (beta:=beta) y⌝⦄
    (pure (Fulp_le_twice_r_check (beta:=beta) bo precision x y) : Id Unit)
    ⦃⇓_ => ⌜Fulp (beta:=beta) bo 2 precision x ≤
      2 * Fulp (beta:=beta) bo 2 precision y⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hprecision, hvNum, hx_nonneg, hx_bounded, hy_normal, hxy⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure, Fulp_le_twice_r_check,
    Id.run, ULift.up_down]
  let xn : FloatSpec.Core.Defs.FlocqFloat 2 :=
    Fnormalize (beta:=2) 2 bo precision x
  let yDouble : FloatSpec.Core.Defs.FlocqFloat 2 :=
    { Fnum := y.Fnum, Fexp := y.Fexp + 1 }
  have htwo_pos : (0 : ℝ) < ((2 : Int) : ℝ) := by norm_num
  have htwo_ne : ((2 : Int) : ℝ) ≠ 0 := ne_of_gt htwo_pos
  have hradix : (1 : Int) < 2 := by decide
  have hx_val :
      _root_.F2R (beta:=2) xn = _root_.F2R (beta:=2) x := by
    have h := FnormalizeCorrect (beta:=2) 2 bo precision x
    simpa [wp, PostCond.noThrow, pure, FnormalizeCorrect_check,
      Id.run, ULift.up_down, xn] using h ⟨rfl, hradix⟩
  have hx_can : Fcanonic (beta:=2) 2 bo xn := by
    have h := FnormalizeCanonic (beta:=2) 2 bo precision x
    simpa [wp, PostCond.noThrow, pure, FnormalizeCanonic_check,
      Id.run, ULift.up_down, xn] using
      h ⟨hx_bounded, hx_bounded, hprecision, hradix, hvNum⟩
  have hy_can : Fcanonic (beta:=2) 2 bo y := Or.inl hy_normal
  have hy_norm :
      Fnormalize (beta:=2) 2 bo precision y = y := by
    have h := FcanonicFnormalizeEq (beta:=2) 2 bo precision y
    simpa [wp, PostCond.noThrow, pure, FcanonicFnormalizeEq_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hy_can, rfl, hradix, hprecision, hvNum⟩
  have hyDouble_val :
      _root_.F2R (beta:=2) yDouble =
        2 * _root_.F2R (beta:=2) y := by
    have hpow :
        ((2 : Int) : ℝ) ^ (y.Fexp + 1) =
          ((2 : Int) : ℝ) ^ y.Fexp * 2 := by
      calc
        ((2 : Int) : ℝ) ^ (y.Fexp + 1) =
            ((2 : Int) : ℝ) ^ y.Fexp * ((2 : Int) : ℝ) ^ (1 : Int) := by
              rw [zpow_add₀ htwo_ne]
        _ = ((2 : Int) : ℝ) ^ y.Fexp * 2 := by norm_num
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, yDouble,
      FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp]
    rw [hpow]
    ring
  have hyDouble_can : Fcanonic (beta:=2) 2 bo yDouble := by
    left
    constructor
    · constructor
      · simpa [yDouble] using hy_normal.1.1
      · change -bo.dExp ≤ y.Fexp + 1
        exact le_trans hy_normal.1.2 (by omega)
    · simpa [yDouble] using hy_normal.2
  have hy_nonneg : 0 ≤ _root_.F2R (beta:=2) y := by
    nlinarith
  have hyDouble_nonneg : 0 ≤ _root_.F2R (beta:=2) yDouble := by
    rw [hyDouble_val]
    nlinarith
  have h_abs_le :
      |_root_.F2R (beta:=2) xn| ≤ |_root_.F2R (beta:=2) yDouble| := by
    rw [hx_val, abs_of_nonneg hx_nonneg, abs_of_nonneg hyDouble_nonneg]
    rw [hyDouble_val]
    exact hxy
  have hexp_le :
      xn.Fexp ≤ yDouble.Fexp := by
    have h := Fcanonic_Rle_Zle (beta:=2) 2 bo xn yDouble hradix rfl
    simpa [wp, PostCond.noThrow, pure, Fcanonic_Rle_Zle_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hx_can, hyDouble_can, h_abs_le⟩
  have hpow_le :
      ((2 : Int) : ℝ) ^ xn.Fexp ≤ ((2 : Int) : ℝ) ^ (y.Fexp + 1) :=
    zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ ((2 : Int) : ℝ)) hexp_le
  have hright :
      2 * Fulp (beta:=2) bo 2 precision y =
        ((2 : Int) : ℝ) ^ (y.Fexp + 1) := by
    simp [Fulp, hy_norm]
    calc
      2 * ((2 : Int) : ℝ) ^ y.Fexp =
          ((2 : Int) : ℝ) ^ y.Fexp * 2 := by ring
      _ = ((2 : Int) : ℝ) ^ (y.Fexp + 1) := by
          rw [zpow_add₀ htwo_ne, zpow_one]
          norm_num
  have hleft :
      Fulp (beta:=2) bo 2 precision x = ((2 : Int) : ℝ) ^ xn.Fexp := by
    simp [Fulp, xn]
  rw [hleft, hright]
  exact hpow_le

noncomputable def Fulp_le_twice_r_round_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (precision : Nat)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) : Unit :=
  ()

/-- Coq discriminant helper `Fulp_le_twice_r_round`.

This is the final `Fulp_le_twice_r` handoff after monotonicity of nearest-even
rounding has compared `x` with the doubled rounded value of `y`.  The doubled
rounding payload is kept explicit for reuse by callers that already have it. -/
theorem Fulp_le_twice_r_round_from_rounding_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (precision : Nat)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) :
    ⦃⌜beta = 2 ∧ precision ≠ 0 ∧ bo.vNum = Zpower_nat 2 precision ∧
        0 ≤ _root_.F2R (beta:=beta) x ∧
        Fbounded (beta:=beta) bo x ∧
        Fnormal (beta:=beta) 2 bo y ∧
        -bo.dExp ≤ y.Fexp - 1 ∧
        _root_.F2R (beta:=beta) x ≤ 2 * r ∧
        EvenClosest (beta:=beta) bo (2 : ℝ) precision r y ∧
        EvenClosest (beta:=beta) bo (2 : ℝ) precision (2 * r)
          ({ Fnum := y.Fnum, Fexp := y.Fexp + 1 } :
            FloatSpec.Core.Defs.FlocqFloat beta) ∧
        MonotoneP_float (EvenClosest (beta:=beta) bo (2 : ℝ) precision) ∧
        UniqueP (EvenClosest (beta:=beta) bo (2 : ℝ) precision)⌝⦄
    (pure (Fulp_le_twice_r_round_check (beta:=beta) bo precision x y r) :
      Id Unit)
    ⦃⇓_ => ⌜Fulp (beta:=beta) bo 2 precision x ≤
      2 * Fulp (beta:=beta) bo 2 precision y⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hprecision, hvNum, hx_nonneg, hx_bounded, hy_normal, _hy_pred_exp,
      hx_le_round_arg, _hy_round, hy_double_round, hMono, hUnique⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure, Fulp_le_twice_r_round_check,
    Id.run, ULift.up_down]
  let yDouble : FloatSpec.Core.Defs.FlocqFloat 2 :=
    { Fnum := y.Fnum, Fexp := y.Fexp + 1 }
  have htwo_ne : ((2 : Int) : ℝ) ≠ 0 := by norm_num
  have hyDouble_val :
      _root_.F2R (beta:=2) yDouble =
        2 * _root_.F2R (beta:=2) y := by
    have hpow :
        ((2 : Int) : ℝ) ^ (y.Fexp + 1) =
          ((2 : Int) : ℝ) ^ y.Fexp * 2 := by
      calc
        ((2 : Int) : ℝ) ^ (y.Fexp + 1) =
            ((2 : Int) : ℝ) ^ y.Fexp * ((2 : Int) : ℝ) ^ (1 : Int) := by
              rw [zpow_add₀ htwo_ne]
        _ = ((2 : Int) : ℝ) ^ y.Fexp * 2 := by norm_num
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, yDouble,
      FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp]
    rw [hpow]
    ring
  have hx_exact :
      EvenClosest (beta:=2) bo (2 : ℝ) precision
        (_root_.F2R (beta:=2) x) x := by
    constructor
    · constructor
      · exact hx_bounded
      · intro g hg
        rw [sub_self, abs_zero]
        exact abs_nonneg (_root_.F2R (beta:=2) g - _root_.F2R (beta:=2) x)
    · right
      intro q hqClosest
      have hdist := hqClosest.2 x hx_bounded
      rw [sub_self, abs_zero] at hdist
      have hzero :
          |_root_.F2R (beta:=2) q - _root_.F2R (beta:=2) x| = 0 :=
        le_antisymm hdist
          (abs_nonneg (_root_.F2R (beta:=2) q - _root_.F2R (beta:=2) x))
      exact sub_eq_zero.mp (abs_eq_zero.mp hzero)
  have hx_le_yDouble :
      _root_.F2R (beta:=2) x ≤ _root_.F2R (beta:=2) yDouble := by
    have hmono := EvenClosestMonotone2_from_properties (beta:=2) bo 2
      (2 : ℝ) precision
      (_root_.F2R (beta:=2) x) (2 * r) x yDouble
    simpa [wp, PostCond.noThrow, pure, EvenClosestMonotone2_check,
      Id.run, ULift.up_down, yDouble] using
      hmono ⟨hx_le_round_arg, hx_exact, hy_double_round, hMono, hUnique⟩
  have hx_le_y :
      _root_.F2R (beta:=2) x ≤ 2 * _root_.F2R (beta:=2) y := by
    rw [hyDouble_val] at hx_le_yDouble
    exact hx_le_yDouble
  have h := Fulp_le_twice_r (beta:=2) bo precision x y
  simpa [wp, PostCond.noThrow, pure, Fulp_le_twice_r_check,
    Id.run, ULift.up_down, Int.cast_ofNat] using
    h ⟨rfl, hprecision, hvNum, hx_nonneg, hx_bounded, hy_normal, hx_le_y⟩

/-- Coq discriminant helper `Q_positive`.

In the second discriminant branch, `3 * |p - q| < p + q` and nonnegativity of
`p` force `q` to be positive.  The Pff-specific proof obtains `0 <= p` from
`P_positive`; this factored Lean lemma keeps only the real arithmetic core. -/
theorem Q_positive (p q : ℝ)
    (hp_nonneg : 0 ≤ p)
    (hSecond : 3 * |p - q| < p + q) :
    0 < q := by
  by_contra hq_not_pos
  have hq_nonpos : q ≤ 0 := le_of_not_gt hq_not_pos
  have hdiff_nonneg : 0 ≤ p - q := by linarith
  rw [abs_of_nonneg hdiff_nonneg] at hSecond
  nlinarith

/-- Coq discriminant helper `Q_le_two_P`.

Under the same second-branch inequality, positive `q` cannot exceed `2*p`. -/
theorem Q_le_two_P (p q : ℝ)
    (hq_pos : 0 < q)
    (hSecond : 3 * |p - q| < p + q) :
    q ≤ 2 * p := by
  by_contra hnot
  have hgt : 2 * p < q := lt_of_not_ge hnot
  have hdiff_neg : p - q < 0 := by nlinarith
  rw [abs_of_neg hdiff_neg] at hSecond
  nlinarith

/-- Coq discriminant helper `P_le_two_Q`.

Symmetric companion to `Q_le_two_P`: nonnegative `p` cannot exceed `2*q` in
the second discriminant branch. -/
theorem P_le_two_Q (p q : ℝ)
    (hp_nonneg : 0 ≤ p)
    (hSecond : 3 * |p - q| < p + q) :
    p ≤ 2 * q := by
  by_contra hnot
  have hgt : 2 * q < p := lt_of_not_ge hnot
  have hdiff_nonneg : 0 ≤ p - q := by nlinarith
  rw [abs_of_nonneg hdiff_nonneg] at hSecond
  nlinarith

/-- Coq discriminant helper `t_exact`.

In the second discriminant branch, Sterbenz makes the subtraction `p - q`
representable in the current Pff bound.  Since `t` is an even-closest rounding
of the same real value, closest-rounding idempotence forces its real value to
be exactly `p - q`. -/
noncomputable def t_exact_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ) (precision : Nat)
    (p q t : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem t_exact {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ) (precision : Nat)
    (p q t : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜1 < beta ∧
        Fbounded (beta:=beta) bo p ∧
        Fbounded (beta:=beta) bo q ∧
        0 ≤ _root_.F2R (beta:=beta) p ∧
        3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| <
          _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q ∧
        EvenClosest (beta:=beta) bo radix precision
          (_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q) t⌝⦄
    (pure (t_exact_check (beta:=beta) bo radix precision p q t) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) t =
      _root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q⌝⦄ := by
  intro hpre
  rcases hpre with ⟨hβ, hpBound, hqBound, hpNonneg, hSecond, hRoundt⟩
  simp only [wp, PostCond.noThrow, pure, t_exact_check, Id.run, ULift.up_down]
  let diff := FloatSpec.Calc.Operations.Fminus (beta:=beta) p q
  have hqPos : 0 < _root_.F2R (beta:=beta) q :=
    Q_positive (_root_.F2R (beta:=beta) p) (_root_.F2R (beta:=beta) q)
      hpNonneg hSecond
  have hqLeTwoP :
      _root_.F2R (beta:=beta) q ≤
        2 * _root_.F2R (beta:=beta) p :=
    Q_le_two_P (_root_.F2R (beta:=beta) p) (_root_.F2R (beta:=beta) q)
      hqPos hSecond
  have hpLeTwoQ :
      _root_.F2R (beta:=beta) p ≤
        2 * _root_.F2R (beta:=beta) q :=
    P_le_two_Q (_root_.F2R (beta:=beta) p) (_root_.F2R (beta:=beta) q)
      hpNonneg hSecond
  have hhalf :
      (1 / 2 : ℝ) * _root_.F2R (beta:=beta) q ≤
        _root_.F2R (beta:=beta) p := by
    nlinarith
  have hdiffBound : Fbounded (beta:=beta) bo diff := by
    have hSterbenz := Sterbenz (beta:=beta) bo p q
    simpa [wp, PostCond.noThrow, pure, Sterbenz_check, Fminus, diff] using
      hSterbenz ⟨hβ, hpBound, hqBound, hhalf, hpLeTwoQ⟩
  have hdiffVal :
      _root_.F2R (beta:=beta) diff =
        _root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q := by
    have hminus := Fminus_correct (beta:=beta) p q
    simpa [wp, PostCond.noThrow, pure, Fminus_correct_check, Fminus, diff] using
      hminus hβ
  have hClosestT :
      Closest (beta:=beta) bo radix (_root_.F2R (beta:=beta) diff) t := by
    rw [hdiffVal]
    exact hRoundt.1
  have hdiffEqT :
      _root_.F2R (beta:=beta) diff = _root_.F2R (beta:=beta) t := by
    have hIdem := ClosestIdem_from_expanded_closest_payload (beta:=beta) bo radix diff t
    simpa [wp, PostCond.noThrow, pure, ClosestIdem_check, Fbounded'] using
      hIdem ⟨hdiffBound, hClosestT, hdiffBound, hClosestT.1, hClosestT.2⟩
  exact hdiffEqT.symm.trans hdiffVal

/-- Coq: `MKnuth`.

This is the first Knuth payload from upstream `TwoSum.v`: once the first
subtraction `(Iplus p q) - p` is exact, the correction sum recovers the
addition error. The usual Pff radix/precision hypotheses are explicit because
the local `errorBoundedPlus` theorem is packaged that way. -/
theorem MKnuth_from_format_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (IplusSym :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iplus p q = Iplus q p)
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q))
    (hbeta : beta = radix)
    (hradix : 1 < radix)
    (hprecision : precision ≠ 0)
    (hvNum_gt : 1 < bo.vNum)
    (hvNum : bo.vNum = Zpower_nat radix precision) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) =
        _root_.F2R (beta:=beta) (Iplus p q) -
          _root_.F2R (beta:=beta) p →
      _root_.F2R (beta:=beta)
          (Iplus
            (Iminus p
              (Iminus (Iplus p q) (Iminus (Iplus p q) p)))
            (Iminus q (Iminus (Iplus p q) p))) =
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) (Iplus p q) := by
  intro p q hp hq hFirst
  let c := Iminus (Iplus p q) p
  let b := Iminus (Iplus p q) c
  let a := Iminus p b
  let d := Iminus q c
  have hpqBounded : Fbounded (beta:=beta) bo (Iplus p q) :=
    IplusBounded (beta:=beta) bo (radix : ℝ) Iplus IplusCorrect p q hp hq
  have hcBounded : Fbounded (beta:=beta) bo c :=
    IminusBounded (beta:=beta) bo (radix : ℝ) Iplus Iminus IplusCorrect
      IminusPlus (Iplus p q) p hpqBounded hp
  have hbBounded : Fbounded (beta:=beta) bo b :=
    IminusBounded (beta:=beta) bo (radix : ℝ) Iplus Iminus IplusCorrect
      IminusPlus (Iplus p q) c hpqBounded hcBounded
  have haBounded : Fbounded (beta:=beta) bo a :=
    IminusBounded (beta:=beta) bo (radix : ℝ) Iplus Iminus IplusCorrect
      IminusPlus p b hp hbBounded
  have hdBounded : Fbounded (beta:=beta) bo d :=
    IminusBounded (beta:=beta) bo (radix : ℝ) Iplus Iminus IplusCorrect
      IminusPlus q c hq hcBounded
  have htargetB :
      _root_.F2R (beta:=beta) (Iplus p q) -
          _root_.F2R (beta:=beta) c =
        _root_.F2R (beta:=beta) p := by
    change _root_.F2R (beta:=beta) (Iplus p q) -
        _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) =
      _root_.F2R (beta:=beta) p
    rw [hFirst]
    ring
  have hclosestB :
      Closest (beta:=beta) bo (radix : ℝ) (_root_.F2R (beta:=beta) p) b := by
    have h := IminusCorrect (beta:=beta) bo (radix : ℝ) Iplus Iminus
      IplusCorrect IminusPlus (Iplus p q) c hpqBounded hcBounded
    rw [← htargetB]
    exact h
  have hp_eq_b :
      _root_.F2R (beta:=beta) p = _root_.F2R (beta:=beta) b := by
    have h := ClosestIdem_from_expanded_closest_payload (beta:=beta) bo (radix : ℝ) p b
    simpa [wp, PostCond.noThrow, pure, ClosestIdem_check, Fbounded'] using
      h ⟨hp, hclosestB, hp, hclosestB.1, hclosestB.2⟩
  have ha_zero : _root_.F2R (beta:=beta) a = 0 :=
    IminusId (beta:=beta) bo (radix : ℝ) Iplus Iminus IplusCorrect
      IminusPlus p b hp hbBounded hp_eq_b
  have hleft_zero :
      _root_.F2R (beta:=beta) (Iplus a d) =
        _root_.F2R (beta:=beta) d :=
    IplusOl (beta:=beta) bo (radix : ℝ) Iplus IplusCorrect IplusSym
      a d haBounded hdBounded ha_zero
  have hError :=
    errorBoundedPlus_from_format_payload (beta:=beta) bo radix precision hbeta hradix
      hprecision hvNum_gt hvNum p q (Iplus p q) hp hq
      (IplusCorrect p q hp hq)
  rcases hError with ⟨error, herrorVal, herrorBounded, _herrorExp⟩
  have htargetD :
      _root_.F2R (beta:=beta) q - _root_.F2R (beta:=beta) c =
        _root_.F2R (beta:=beta) error := by
    change _root_.F2R (beta:=beta) q -
        _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) =
      _root_.F2R (beta:=beta) error
    rw [hFirst, herrorVal]
    ring
  have hclosestD :
      Closest (beta:=beta) bo (radix : ℝ)
        (_root_.F2R (beta:=beta) error) d := by
    have h := IminusCorrect (beta:=beta) bo (radix : ℝ) Iplus Iminus
      IplusCorrect IminusPlus q c hq hcBounded
    rw [← htargetD]
    exact h
  have herror_eq_d :
      _root_.F2R (beta:=beta) error = _root_.F2R (beta:=beta) d := by
    have h := ClosestIdem_from_expanded_closest_payload (beta:=beta) bo (radix : ℝ) error d
    simpa [wp, PostCond.noThrow, pure, ClosestIdem_check, Fbounded'] using
      h ⟨herrorBounded, hclosestD, herrorBounded, hclosestD.1, hclosestD.2⟩
  have hd_val :
      _root_.F2R (beta:=beta) d =
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) (Iplus p q) := by
    rw [← herror_eq_d, herrorVal]
  simpa [a, b, c, d] using hleft_zero.trans hd_val

/-- Coq: `MKnuth1`.

This is the generic Knuth correction payload once the two subtractions
`q - c` and `(Iplus p q) - c` are exact. It is the proof component used by the
harder interval cases in upstream Pff. -/
theorem MKnuth1_from_format_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q))
    (hbeta : beta = radix)
    (hradix : 1 < radix)
    (hprecision : precision ≠ 0)
    (hvNum_gt : 1 < bo.vNum)
    (hvNum : bo.vNum = Zpower_nat radix precision) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      _root_.F2R (beta:=beta) (Iminus q (Iminus (Iplus p q) p)) =
        _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) →
      _root_.F2R (beta:=beta)
          (Iminus (Iplus p q) (Iminus (Iplus p q) p)) =
        _root_.F2R (beta:=beta) (Iplus p q) -
          _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) →
      _root_.F2R (beta:=beta)
          (Iplus
            (Iminus p
              (Iminus (Iplus p q) (Iminus (Iplus p q) p)))
            (Iminus q (Iminus (Iplus p q) p))) =
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) (Iplus p q) := by
  intro p q hp hq hD hB
  let c := Iminus (Iplus p q) p
  let bb := Iminus (Iplus p q) c
  let a := Iminus p bb
  let d := Iminus q c
  have hpqBounded : Fbounded (beta:=beta) bo (Iplus p q) :=
    IplusBounded (beta:=beta) bo (radix : ℝ) Iplus IplusCorrect p q hp hq
  have hopBounded : Fbounded (beta:=beta) bo (Fopp (beta:=beta) p) :=
    oppBounded (beta:=beta) bo p hp
  have hcBounded : Fbounded (beta:=beta) bo c :=
    IminusBounded (beta:=beta) bo (radix : ℝ) Iplus Iminus IplusCorrect
      IminusPlus (Iplus p q) p hpqBounded hp
  have hbbBounded : Fbounded (beta:=beta) bo bb :=
    IminusBounded (beta:=beta) bo (radix : ℝ) Iplus Iminus IplusCorrect
      IminusPlus (Iplus p q) c hpqBounded hcBounded
  have haBounded : Fbounded (beta:=beta) bo a :=
    IminusBounded (beta:=beta) bo (radix : ℝ) Iplus Iminus IplusCorrect
      IminusPlus p bb hp hbbBounded
  have hdBounded : Fbounded (beta:=beta) bo d :=
    IminusBounded (beta:=beta) bo (radix : ℝ) Iplus Iminus IplusCorrect
      IminusPlus q c hq hcBounded
  have hFoppP := Fopp_correct (beta:=beta) p
  have hFoppPVal :
      _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) =
        -_root_.F2R (beta:=beta) p := by
    simpa [wp, PostCond.noThrow, pure, Fopp_correct_check, Id.run,
      ULift.up_down] using hFoppP trivial
  have hclosestC :
      Closest (beta:=beta) bo (radix : ℝ)
        (_root_.F2R (beta:=beta) (Iplus p q) +
          _root_.F2R (beta:=beta) (Fopp (beta:=beta) p)) c := by
    have h := IminusCorrect (beta:=beta) bo (radix : ℝ) Iplus Iminus
      IplusCorrect IminusPlus (Iplus p q) p hpqBounded hp
    rw [hFoppPVal]
    simpa [c, sub_eq_add_neg] using h
  have hAuxError :=
    errorBoundedPlus_from_format_payload (beta:=beta) bo radix precision hbeta hradix
      hprecision hvNum_gt hvNum (Iplus p q) (Fopp (beta:=beta) p)
      c hpqBounded hopBounded hclosestC
  rcases hAuxError with ⟨auxError, hAuxVal, hAuxBounded, _hAuxExp⟩
  have hAuxOppBounded : Fbounded (beta:=beta) bo (Fopp (beta:=beta) auxError) :=
    oppBounded (beta:=beta) bo auxError hAuxBounded
  have hAuxOpp := Fopp_correct (beta:=beta) auxError
  have hAuxOppVal :
      _root_.F2R (beta:=beta) (Fopp (beta:=beta) auxError) =
        -_root_.F2R (beta:=beta) auxError := by
    simpa [wp, PostCond.noThrow, pure, Fopp_correct_check, Id.run,
      ULift.up_down] using hAuxOpp trivial
  have hA :
      _root_.F2R (beta:=beta) a =
        _root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) bb := by
    have hdiff :
        _root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) bb =
          _root_.F2R (beta:=beta) (Fopp (beta:=beta) auxError) := by
      change _root_.F2R (beta:=beta) p -
          _root_.F2R (beta:=beta)
            (Iminus (Iplus p q) (Iminus (Iplus p q) p)) =
        _root_.F2R (beta:=beta) (Fopp (beta:=beta) auxError)
      rw [hAuxOppVal, hAuxVal, hB, hFoppPVal]
      ring
    have h := IminusCorrectEq (beta:=beta) bo (radix : ℝ) Iplus Iminus
      IplusCorrect IminusPlus p bb (Fopp (beta:=beta) auxError)
      (_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) bb)
      hp hbbBounded hAuxOppBounded hdiff hdiff
    simpa [a] using h
  have hMainError :=
    errorBoundedPlus_from_format_payload (beta:=beta) bo radix precision hbeta hradix
      hprecision hvNum_gt hvNum p q (Iplus p q) hp hq
      (IplusCorrect p q hp hq)
  rcases hMainError with ⟨mainError, hMainVal, hMainBounded, _hMainExp⟩
  have hsum :
      _root_.F2R (beta:=beta) a + _root_.F2R (beta:=beta) d =
        _root_.F2R (beta:=beta) mainError := by
    rw [hA]
    change
      (_root_.F2R (beta:=beta) p -
          _root_.F2R (beta:=beta)
            (Iminus (Iplus p q) (Iminus (Iplus p q) p))) +
        _root_.F2R (beta:=beta) (Iminus q (Iminus (Iplus p q) p)) =
        _root_.F2R (beta:=beta) mainError
    rw [hD, hB, hMainVal]
    ring
  have htarget :
      _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) (Iplus p q) =
        _root_.F2R (beta:=beta) mainError := by
    rw [hMainVal]
  have hfinal := IplusCorrectEq (beta:=beta) bo (radix : ℝ) Iplus
    IplusCorrect a d mainError
    (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
      _root_.F2R (beta:=beta) (Iplus p q))
    haBounded hdBounded hMainBounded htarget hsum
  simpa [a, bb, c, d] using hfinal

/-- Closed variant of `MDekker` that derives the midpoint minimum payload from
`PminPos` in the only case where `Dekker3` needs it. -/
theorem MDekker_closed {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ) (precision : Nat)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (IplusOp :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fopp (beta:=beta) (Iplus p q) =
          Iplus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q))
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q))
    (hβ : 1 < beta)
    (hbetaRadix : (beta : ℝ) = radix)
    (hradix : radix = 2)
    (hprecision : precision ≠ 0)
    (hvNum : bo.vNum = Zpower_nat beta precision)
    (hvNum_gt : 1 < bo.vNum)
    (hBoundExpAll :
      ∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=beta) beta r).Fexp)
    (hTotal : TotalP (Closest (beta:=beta) bo radix)) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      |_root_.F2R (beta:=beta) q| ≤ |_root_.F2R (beta:=beta) p| →
      _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) =
        _root_.F2R (beta:=beta) (Iplus p q) -
          _root_.F2R (beta:=beta) p := by
  refine MDekker_from_case_payloads (beta:=beta) bo radix Iplus Iminus
    IplusOp IminusPlus ?_ ?_ ?_
  · intro p q hq0 hqle hp hq
    exact Dekker1_FTS_from_total_payload (beta:=beta)
      bo radix Iplus Iminus IplusCorrect
      IminusPlus p q hβ hbetaRadix hradix hTotal hq0 hqle hp hq
  · intro p q hp0 hnegq_le_p hp_le hpb hqb
    exact Dekker2_FTS (beta:=beta) bo radix Iplus Iminus IplusCorrect
      IminusPlus p q hβ hradix hp0 hnegq_le_p hp_le hpb hqb
  · intro p q hqle0 hmid hp hq
    have hp0 : 0 ≤ _root_.F2R (beta:=beta) p := by
      rw [hradix] at hmid
      nlinarith
    have hPminPayloadP :
        ∀ min : FloatSpec.Core.Defs.FlocqFloat beta,
          isMin (beta:=beta) bo beta
            ((1 / 2 : ℝ) * _root_.F2R (beta:=beta) p) min →
          ∃ d₂ : FloatSpec.Core.Defs.FlocqFloat beta,
            Fbounded (beta:=beta) bo d₂ ∧
              _root_.F2R (beta:=beta) d₂ =
                _root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) min := by
      intro min hMin
      have h := PminPos (beta:=beta) bo beta precision p min
      simpa [wp, PostCond.noThrow, pure, PminPos_check, Id.run,
        ULift.up_down] using
        h ⟨rfl, hβ, hprecision, hvNum, hp0, hp, hMin⟩
    exact Dekker3_from_midpoint_payload (beta:=beta)
      bo radix precision Iplus Iminus IplusCorrect
      IminusPlus p q hβ hbetaRadix hradix hprecision hvNum hvNum_gt
      (hBoundExpAll ((1 / 2 : ℝ) * _root_.F2R (beta:=beta) p))
      hPminPayloadP hTotal hqle0 hmid hp hq

/-- Coq: `MKnuth2`.

The magnitude-bounded Knuth case follows by first applying the restored
`MDekker_closed` theorem to obtain exactness of `(Iplus p q) - p`, then
finishing with `MKnuth`. Upstream's Fast section fixes radix 2, represented
here by `(beta : ℝ) = 2`. -/
theorem MKnuth2_internal {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (precision : Nat)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo (2 : ℝ)
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (IplusSym :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iplus p q = Iplus q p)
    (IplusOp :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fopp (beta:=beta) (Iplus p q) =
          Iplus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q))
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q))
    (hβ : 1 < beta)
    (hbetaTwo : (beta : ℝ) = 2)
    (hprecision : precision ≠ 0)
    (hvNum : bo.vNum = Zpower_nat beta precision)
    (hvNum_gt : 1 < bo.vNum)
    (hBoundExp :
      ∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=beta) beta r).Fexp)
    (hTotal : TotalP (Closest (beta:=beta) bo (2 : ℝ))) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      |_root_.F2R (beta:=beta) q| ≤ |_root_.F2R (beta:=beta) p| →
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      _root_.F2R (beta:=beta)
          (Iplus
            (Iminus p
              (Iminus (Iplus p q) (Iminus (Iplus p q) p)))
            (Iminus q (Iminus (Iplus p q) p))) =
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) (Iplus p q) := by
  intro p q hAbs hp hq
  have hFirst :
      _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) =
        _root_.F2R (beta:=beta) (Iplus p q) -
          _root_.F2R (beta:=beta) p :=
    MDekker_closed (beta:=beta) bo (2 : ℝ) precision Iplus Iminus
      IplusCorrect IplusOp IminusPlus hβ hbetaTwo rfl hprecision hvNum
      hvNum_gt hBoundExp hTotal p q hp hq hAbs
  have hIplusCorrectBeta :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo (beta : ℝ)
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q) := by
    intro p q hp hq
    simpa [hbetaTwo] using IplusCorrect p q hp hq
  exact MKnuth_from_format_payload (beta:=beta) bo beta precision Iplus Iminus
    hIplusCorrectBeta IplusSym IminusPlus rfl hβ hprecision hvNum_gt hvNum
    p q hp hq hFirst

/-- Coq: `MKnuth2` with the exact exported Fast-section contract.

The source fixes radix two, requires `1 < precision`, and exports
`IplusComp`.  Auxiliary existence facts used by the Lean proof are derived
here from the refined `Fbound`; they are not leaked into the theorem API. -/
theorem MKnuth2
    (bo : Fbound_skel) (precision : Nat)
    (Iplus : FloatSpec.Core.Defs.FlocqFloat 2 →
      FloatSpec.Core.Defs.FlocqFloat 2 →
      FloatSpec.Core.Defs.FlocqFloat 2)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat 2,
        Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
        Closest (beta:=2) bo (2 : ℝ)
          (_root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q) (Iplus p q))
    (IplusComp :
      ∀ p q r s : FloatSpec.Core.Defs.FlocqFloat 2,
        Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
        Fbounded (beta:=2) bo r → Fbounded (beta:=2) bo s →
        _root_.F2R (beta:=2) p = _root_.F2R (beta:=2) r →
        _root_.F2R (beta:=2) q = _root_.F2R (beta:=2) s →
        _root_.F2R (beta:=2) (Iplus p q) =
          _root_.F2R (beta:=2) (Iplus r s))
    (IplusSym : ∀ p q, Iplus p q = Iplus q p)
    (IplusOp : ∀ p q,
      Fopp (beta:=2) (Iplus p q) =
        Iplus (Fopp (beta:=2) p) (Fopp (beta:=2) q))
    (Iminus : FloatSpec.Core.Defs.FlocqFloat 2 →
      FloatSpec.Core.Defs.FlocqFloat 2 →
      FloatSpec.Core.Defs.FlocqFloat 2)
    (IminusPlus : ∀ p q,
      Iminus p q = Iplus p (Fopp (beta:=2) q))
    (hprecision : 1 < precision)
    (hvNum : bo.vNum = Zpower_nat 2 precision)
    (p q : FloatSpec.Core.Defs.FlocqFloat 2)
    (hAbs : |_root_.F2R (beta:=2) q| ≤ |_root_.F2R (beta:=2) p|)
    (hp : Fbounded (beta:=2) bo p)
    (hq : Fbounded (beta:=2) bo q) :
    _root_.F2R (beta:=2)
        (Iplus
          (Iminus p (Iminus (Iplus p q) (Iminus (Iplus p q) p)))
          (Iminus q (Iminus (Iplus p q) p))) =
      _root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q -
        _root_.F2R (beta:=2) (Iplus p q) := by
  have hprecision_ne : precision ≠ 0 := by omega
  have hvNum_gt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    have hpow_ge_two : (2 : Int) ≤ 2 ^ precision := by
      calc
        (2 : Int) = 2 ^ (1 : Nat) := by norm_num
        _ ≤ 2 ^ precision := pow_le_pow_right₀ (by norm_num) (by omega)
    omega
  have hBoundExp :
      ∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=2) 2 r).Fexp := by
    intro r
    have hnonneg : (0 : Int) ≤ (boundR (beta:=2) 2 r).Fexp := by
      simp [boundR, boundNat]
    have hdExp_nonneg : (0 : Int) ≤ bo.dExp := bo.dExp_nonneg
    omega
  have hMinTotal : TotalP (isMin (beta:=2) bo 2) := by
    intro r
    have h := MinEx_from_finite_box_payload (beta:=2) bo 2 r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨rfl, by norm_num, hvNum_gt, hBoundExp r⟩
  have hMaxTotal : TotalP (isMax (beta:=2) bo 2) := by
    intro r
    have h := MaxEx_from_finite_box_payload (beta:=2) bo 2 r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MaxEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨rfl, by norm_num, hvNum_gt, hBoundExp r⟩
  have hTotal : TotalP (Closest (beta:=2) bo (2 : ℝ)) := by
    intro r
    have h := ClosestTotal_from_extrema_payload (beta:=2) bo 2 (2 : ℝ) r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestTotal_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨hMinTotal, hMaxTotal⟩
  exact MKnuth2_internal (beta:=2) bo precision Iplus Iminus IplusCorrect
    IplusSym IplusOp IminusPlus (by norm_num) rfl hprecision_ne hvNum
    hvNum_gt hBoundExp hTotal p q hAbs hp hq

/-- Coq: `MKnuthOpp`.

The Knuth correction term is compatible with simultaneous float negation of
the inputs. Upstream states this through the real coercion `:> R`; locally we
state the corresponding `F2R` equality. -/
theorem MKnuthOpp {beta : Int} [ValidRadix beta]
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusOp :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fopp (beta:=beta) (Iplus p q) =
          Iplus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q))
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q)) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta)
          (Iplus
            (Iminus p
              (Iminus (Iplus p q) (Iminus (Iplus p q) p)))
            (Iminus q (Iminus (Iplus p q) p))) =
        _root_.F2R (beta:=beta)
          (Fopp (beta:=beta)
            (Iplus
              (Iminus (Fopp (beta:=beta) p)
                (Iminus (Iplus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q))
                  (Iminus
                    (Iplus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q))
                    (Fopp (beta:=beta) p))))
              (Iminus (Fopp (beta:=beta) q)
                (Iminus
                  (Iplus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q))
                  (Fopp (beta:=beta) p))))) := by
  intro p q
  have hIminusOp :=
    IminusOp (beta:=beta) Iplus Iminus IplusOp IminusPlus
  have hstruct :
      Fopp (beta:=beta)
          (Iplus
            (Iminus (Fopp (beta:=beta) p)
              (Iminus (Iplus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q))
                (Iminus
                  (Iplus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q))
                  (Fopp (beta:=beta) p))))
            (Iminus (Fopp (beta:=beta) q)
              (Iminus
                (Iplus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q))
                (Fopp (beta:=beta) p)))) =
        Iplus
          (Iminus p
            (Iminus (Iplus p q) (Iminus (Iplus p q) p)))
          (Iminus q (Iminus (Iplus p q) p)) := by
    rw [IplusOp]
    repeat rw [hIminusOp]
    repeat rw [IplusOp]
    simp [Fopp, FloatSpec.Calc.Operations.Fopp]
  rw [hstruct]

/-- Coq: `MKnuth3`.

Fast-section Knuth case where `q` lies between `-p` and `2 * -p`.  The exact
addition is obtained by representing `p + q` as `Fminus q (Fopp p)` and using
Sterbenz. -/
theorem MKnuth3_from_format_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (precision : Nat)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo (2 : ℝ)
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (IplusSym :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iplus p q = Iplus q p)
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q))
    (hβ : 1 < beta)
    (hbetaTwo : (beta : ℝ) = 2)
    (hprecision : precision ≠ 0)
    (hvNum : bo.vNum = Zpower_nat beta precision)
    (hvNum_gt : 1 < bo.vNum) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      0 ≤ _root_.F2R (beta:=beta) q →
      _root_.F2R (beta:=beta) q ≤ 2 * -_root_.F2R (beta:=beta) p →
      -_root_.F2R (beta:=beta) p ≤ _root_.F2R (beta:=beta) q →
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      _root_.F2R (beta:=beta)
          (Iplus
            (Iminus p
              (Iminus (Iplus p q) (Iminus (Iplus p q) p)))
            (Iminus q (Iminus (Iplus p q) p))) =
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) (Iplus p q) := by
  intro p q hq0 hq_le_2negp hnegp_le_q hp hq
  have hIplusCorrectBeta :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo (beta : ℝ)
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q) := by
    intro p q hp hq
    simpa [hbetaTwo] using IplusCorrect p q hp hq
  have hopBounded : Fbounded (beta:=beta) bo (Fopp (beta:=beta) p) :=
    oppBounded (beta:=beta) bo p hp
  have hFoppP := Fopp_correct (beta:=beta) p
  have hFoppPVal :
      _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) =
        -_root_.F2R (beta:=beta) p := by
    simpa [wp, PostCond.noThrow, pure, Fopp_correct_check, Id.run,
      ULift.up_down] using hFoppP trivial
  let rep := FloatSpec.Calc.Operations.Fminus (beta:=beta) q (Fopp (beta:=beta) p)
  have hrepBounded : Fbounded (beta:=beta) bo rep := by
    have hSter := Sterbenz (beta:=beta) bo q (Fopp (beta:=beta) p)
    have hhalf :
        (1 / 2 : ℝ) * _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) ≤
          _root_.F2R (beta:=beta) q := by
      rw [hFoppPVal]
      nlinarith
    have hupper :
        _root_.F2R (beta:=beta) q ≤
          2 * _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) := by
      rw [hFoppPVal]
      exact hq_le_2negp
    simpa [wp, PostCond.noThrow, pure, Sterbenz_check, Fminus, rep] using
      hSter ⟨hβ, hq, hopBounded, hhalf, hupper⟩
  have hrepVal :
      _root_.F2R (beta:=beta) rep =
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q := by
    have hminus := Fminus_correct (beta:=beta) q (Fopp (beta:=beta) p)
    have hminusVal :
        _root_.F2R (beta:=beta) rep =
          _root_.F2R (beta:=beta) q -
            _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) := by
      simpa [wp, PostCond.noThrow, pure, Fminus_correct_check, Fminus, Id.run,
        ULift.up_down, rep] using hminus hβ
    rw [hminusVal, hFoppPVal]
    ring
  have hExactPlus :
      _root_.F2R (beta:=beta) (Iplus p q) =
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q := by
    have h := IplusCorrectEq (beta:=beta) bo (beta : ℝ) Iplus
      hIplusCorrectBeta p q rep
      (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
      hp hq hrepBounded (Eq.symm hrepVal)
    exact h (by rw [hrepVal])
  have hFirst :
      _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) =
        _root_.F2R (beta:=beta) (Iplus p q) -
          _root_.F2R (beta:=beta) p := by
    have hpqBounded : Fbounded (beta:=beta) bo (Iplus p q) :=
      IplusBounded (beta:=beta) bo (beta : ℝ) Iplus hIplusCorrectBeta p q hp hq
    have h := IminusCorrectEq (beta:=beta) bo (beta : ℝ) Iplus Iminus
      hIplusCorrectBeta IminusPlus (Iplus p q) p q
      (_root_.F2R (beta:=beta) q) hpqBounded hp hq rfl
    have htoq :
        _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) =
          _root_.F2R (beta:=beta) q := by
      exact h (by
        rw [hExactPlus]
        ring)
    rw [htoq, hExactPlus]
    ring
  exact MKnuth_from_format_payload (beta:=beta) bo beta precision Iplus Iminus
    hIplusCorrectBeta IplusSym IminusPlus rfl hβ hprecision hvNum_gt hvNum
    p q hp hq hFirst

/-- Coq: `MKnuth4`.

Fast-section Knuth case where `p < 0`, `q > 0`, and `q` is past the
`2 * -p` boundary. The proof follows upstream: derive the four rounded-mode
inequalities around `s = Iplus p q` and `c = Iminus s p`, use Sterbenz to
make the two remaining subtractions exact, then apply `MKnuth1`. -/
theorem MKnuth4_from_total_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (precision : Nat)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo (2 : ℝ)
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q))
    (hβ : 1 < beta)
    (hbetaTwo : (beta : ℝ) = 2)
    (hprecision : precision ≠ 0)
    (hvNum : bo.vNum = Zpower_nat beta precision)
    (hvNum_gt : 1 < bo.vNum)
    (hTotal : TotalP (Closest (beta:=beta) bo (2 : ℝ))) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      0 < -_root_.F2R (beta:=beta) p →
      0 < _root_.F2R (beta:=beta) q →
      2 * -_root_.F2R (beta:=beta) p <
        _root_.F2R (beta:=beta) q →
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      _root_.F2R (beta:=beta)
          (Iplus
            (Iminus p
              (Iminus (Iplus p q) (Iminus (Iplus p q) p)))
            (Iminus q (Iminus (Iplus p q) p))) =
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) (Iplus p q) := by
  intro p q hp_neg hq_pos hboundary hp hq
  have hBoundExp :
      ∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=beta) beta r).Fexp := by
    intro r
    exact le_trans (neg_nonpos.mpr bo.dExp_nonneg) (by simp [boundR, boundNat])
  let s := Iplus p q
  let c := Iminus s p
  have hIplusCorrectBeta :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo (beta : ℝ)
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q) := by
    intro p q hp hq
    simpa [hbetaTwo] using IplusCorrect p q hp hq
  have hsClosest :
      Closest (beta:=beta) bo (2 : ℝ)
        (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) s := by
    simpa [s] using IplusCorrect p q hp hq
  have hsBounded : Fbounded (beta:=beta) bo s :=
    IplusBounded (beta:=beta) bo (2 : ℝ) Iplus IplusCorrect p q hp hq
  have hcClosest :
      Closest (beta:=beta) bo (2 : ℝ)
        (_root_.F2R (beta:=beta) s - _root_.F2R (beta:=beta) p) c := by
    have h := IminusCorrect (beta:=beta) bo (2 : ℝ) Iplus Iminus
      IplusCorrect IminusPlus s p hsBounded hp
    simpa [c] using h
  have hcBounded : Fbounded (beta:=beta) bo c :=
    IminusBounded (beta:=beta) bo (2 : ℝ) Iplus Iminus IplusCorrect
      IminusPlus s p hsBounded hp
  have hRounded :
      RoundedModeP_full (beta:=beta) bo
        (Closest (beta:=beta) bo (2 : ℝ)) := by
    have hTrip := ClosestRoundedModeP_from_total_payload (beta:=beta) bo (2 : ℝ)
    simpa [wp, PostCond.noThrow, pure, ClosestRoundedModeP_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using hTrip hTotal

  let halfq : ℝ := (1 / 2 : ℝ) * _root_.F2R (beta:=beta) q
  have hMinExists := MinEx_from_finite_box_payload (beta:=beta) bo beta halfq
  have hMinPayload :
      ∃ min : FloatSpec.Core.Defs.FlocqFloat beta,
        isMin (beta:=beta) bo beta halfq min := by
    simpa [wp, PostCond.noThrow, pure, MinEx_check, halfq] using
      hMinExists ⟨rfl, hβ, hvNum_gt, hBoundExp halfq⟩
  rcases hMinPayload with ⟨min, hMin⟩
  have hMaxExists := MaxEx_from_finite_box_payload (beta:=beta) bo beta halfq
  have hMaxPayload :
      ∃ max : FloatSpec.Core.Defs.FlocqFloat beta,
        isMax (beta:=beta) bo beta halfq max := by
    simpa [wp, PostCond.noThrow, pure, MaxEx_check, halfq] using
      hMaxExists ⟨rfl, hβ, hvNum_gt, hBoundExp halfq⟩
  rcases hMaxPayload with ⟨max, hMax⟩
  rcases hMin with ⟨hminBounded, hmin_le_half, hminGreatest⟩
  rcases hMax with ⟨hmaxBounded, hhalf_le_max, hmaxLeast⟩
  have hMin' : isMin (beta:=beta) bo beta halfq min :=
    ⟨hminBounded, hmin_le_half, hminGreatest⟩
  have hMax' : isMax (beta:=beta) bo beta halfq max :=
    ⟨hmaxBounded, hhalf_le_max, hmaxLeast⟩
  have hq_nonneg : 0 ≤ _root_.F2R (beta:=beta) q := le_of_lt hq_pos
  have hD₂ :
      ∃ d₂ : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo d₂ ∧
          _root_.F2R (beta:=beta) d₂ =
            _root_.F2R (beta:=beta) q - _root_.F2R (beta:=beta) min := by
    have h := PminPos (beta:=beta) bo beta precision q min
    simpa [wp, PostCond.noThrow, pure, PminPos_check, halfq] using
      h ⟨rfl, hβ, hprecision, hvNum, hq_nonneg, hq, hMin'⟩
  have hmax_le_q : _root_.F2R (beta:=beta) max ≤ _root_.F2R (beta:=beta) q := by
    have hhalf_le_q : halfq ≤ _root_.F2R (beta:=beta) q := by
      change (1 / 2 : ℝ) * _root_.F2R (beta:=beta) q ≤
        _root_.F2R (beta:=beta) q
      nlinarith [hq_nonneg]
    exact hmaxLeast q hq hhalf_le_q
  have hD₁ :
      ∃ d₁ : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo d₁ ∧
          _root_.F2R (beta:=beta) d₁ =
            _root_.F2R (beta:=beta) q - _root_.F2R (beta:=beta) max := by
    refine ⟨FloatSpec.Calc.Operations.Fminus (beta:=beta) q max, ?_, ?_⟩
    · have hhalf_max_le_q :
          (1 / 2 : ℝ) * _root_.F2R (beta:=beta) max ≤
            _root_.F2R (beta:=beta) q := by
        nlinarith
      have hq_le_twomax :
          _root_.F2R (beta:=beta) q ≤
            2 * _root_.F2R (beta:=beta) max := by
        change (1 / 2 : ℝ) * _root_.F2R (beta:=beta) q ≤
          _root_.F2R (beta:=beta) max at hhalf_le_max
        nlinarith [hhalf_le_max]
      have hTrip := Sterbenz (beta:=beta) bo q max
      simpa [wp, PostCond.noThrow, pure, Sterbenz_check, Fminus, Id.run,
        ULift.up_down] using
        hTrip ⟨hβ, hq, hmaxBounded, hhalf_max_le_q, hq_le_twomax⟩
    · have hTrip := Fminus_correct (beta:=beta) q max
      simpa [wp, PostCond.noThrow, pure, Fminus_correct_check, Fminus, Id.run,
        ULift.up_down] using hTrip hβ
  have hBetweenQ :
      _root_.F2R (beta:=beta) q =
        _root_.F2R (beta:=beta) min + _root_.F2R (beta:=beta) max := by
    have hTrip := div2IsBetween_from_extrema (beta:=beta) bo beta q min max
    simpa [wp, PostCond.noThrow, pure, div2IsBetween_check, halfq,
      Fbounded'] using hTrip ⟨hq, hMin', hMax', hD₁, hD₂⟩

  have hRle1 : halfq ≤ _root_.F2R (beta:=beta) s := by
    have hHalfLt :
        halfq < _root_.F2R (beta:=beta) p +
          _root_.F2R (beta:=beta) q := by
      change (1 / 2 : ℝ) * _root_.F2R (beta:=beta) q <
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q
      nlinarith [hboundary]
    have hTrip := FmultRadixInv_from_extrema (beta:=beta) bo (2 : ℝ)
      q s min max (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
    simpa [wp, PostCond.noThrow, pure, FmultRadixInv_check, Fbounded',
      halfq] using hTrip ⟨hq, hsClosest, hHalfLt, hMin', hMax', hBetweenQ⟩
  have hs_le_q : _root_.F2R (beta:=beta) s ≤ _root_.F2R (beta:=beta) q := by
    have hinput :
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q ≤
          _root_.F2R (beta:=beta) q := by
      nlinarith
    have hTrip := RleBoundRoundr_from_full (beta:=beta) bo 0
      (Closest (beta:=beta) bo (2 : ℝ)) q s
      (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
    simpa [wp, PostCond.noThrow, pure, RleBoundRoundr_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using hTrip ⟨hRounded, hq, hinput, hsClosest⟩
  have hnegp_le_s :
      -_root_.F2R (beta:=beta) p ≤ _root_.F2R (beta:=beta) s := by
    have hpOppBounded : Fbounded (beta:=beta) bo (Fopp (beta:=beta) p) :=
      oppBounded (beta:=beta) bo p hp
    have hpOppVal :
        _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) =
          -_root_.F2R (beta:=beta) p := by
      have h := Fopp_correct (beta:=beta) p
      simpa [wp, PostCond.noThrow, pure, Fopp_correct_check, Id.run,
        ULift.up_down] using h trivial
    have hpOppClosest :
        Closest (beta:=beta) bo (2 : ℝ)
          (_root_.F2R (beta:=beta) (Fopp (beta:=beta) p))
          (Fopp (beta:=beta) p) := by
      refine ⟨hpOppBounded, ?_⟩
      intro g hg
      rw [sub_self, abs_zero]
      exact abs_nonneg (_root_.F2R (beta:=beta) g -
        _root_.F2R (beta:=beta) (Fopp (beta:=beta) p))
    have hmonoTrip := ClosestMonotone_from_split_radix_payload (beta:=beta) bo (2 : ℝ)
    have hmono : MonotoneP_float (Closest (beta:=beta) bo (2 : ℝ)) := by
      simpa [wp, PostCond.noThrow, pure, ClosestMonotone_check, Id.run,
        ULift.up_down] using hmonoTrip True.intro
    have hlt :
        _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) <
          _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q := by
      rw [hpOppVal]
      nlinarith
    have hres := hmono
      (_root_.F2R (beta:=beta) (Fopp (beta:=beta) p))
      (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
      (Fopp (beta:=beta) p) s hlt hpOppClosest hsClosest
    rw [hpOppVal] at hres
    exact hres
  have hRle2 : _root_.F2R (beta:=beta) s ≤ _root_.F2R (beta:=beta) c := by
    have hinput :
        _root_.F2R (beta:=beta) s ≤
          _root_.F2R (beta:=beta) s - _root_.F2R (beta:=beta) p := by
      nlinarith
    have hTrip := RleBoundRoundl_from_full (beta:=beta) bo 0
      (Closest (beta:=beta) bo (2 : ℝ)) s c
      (_root_.F2R (beta:=beta) s - _root_.F2R (beta:=beta) p)
    simpa [wp, PostCond.noThrow, pure, RleBoundRoundl_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using hTrip ⟨hRounded, hsBounded, hinput, hcClosest⟩
  have hRle3 : _root_.F2R (beta:=beta) c ≤ 2 * _root_.F2R (beta:=beta) s := by
    let scaled : FloatSpec.Core.Defs.FlocqFloat beta := ⟨s.Fnum, s.Fexp + 1⟩
    have hScaledBounded : Fbounded (beta:=beta) bo scaled := by
      have hTrip := FBoundedScale (beta:=beta) bo s 1
      simpa [wp, PostCond.noThrow, pure, FBoundedScale_check, scaled,
        Id.run, ULift.up_down, Int.cast_ofNat] using hTrip hsBounded
    have hScaledValue :
        _root_.F2R (beta:=beta) scaled =
          (2 : ℝ) * _root_.F2R (beta:=beta) s := by
      have hβne : (beta : ℝ) ≠ 0 := by
        have hβposInt : (0 : Int) < beta := lt_trans (by decide) hβ
        have hβpos : (0 : ℝ) < beta := by exact_mod_cast hβposInt
        exact ne_of_gt hβpos
      have hTrip := FvalScale beta bo s 1
      have hscale :
          _root_.F2R (beta:=beta) scaled =
            ((beta : ℝ) ^ (Int.ofNat 1)) *
              _root_.F2R (beta:=beta) s := by
        simpa [wp, PostCond.noThrow, pure, FvalScale_check, scaled,
          Id.run, ULift.up_down, Int.cast_ofNat] using hTrip hβne
      simpa [hbetaTwo] using hscale
    have hUpper :
        _root_.F2R (beta:=beta) s - _root_.F2R (beta:=beta) p ≤
          2 * _root_.F2R (beta:=beta) s := by
      nlinarith
    have hTrip := RoundedModeMult_from_scale_payload (beta:=beta) bo (2 : ℝ)
      (Closest (beta:=beta) bo (2 : ℝ))
      (_root_.F2R (beta:=beta) s - _root_.F2R (beta:=beta) p) c s
    simpa [wp, PostCond.noThrow, pure, RoundedModeMult_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      hTrip ⟨hRounded, hcClosest, hScaledBounded, hScaledValue, hUpper⟩

  have hc_pos : 0 < _root_.F2R (beta:=beta) c := by
    have hs_pos : 0 < _root_.F2R (beta:=beta) s := by
      have : 0 < halfq := by
        change 0 < (1 / 2 : ℝ) * _root_.F2R (beta:=beta) q
        positivity
      exact lt_of_lt_of_le this hRle1
    exact lt_of_lt_of_le hs_pos hRle2
  have hB :
      _root_.F2R (beta:=beta) (Iminus s c) =
        _root_.F2R (beta:=beta) s - _root_.F2R (beta:=beta) c := by
    let rep := FloatSpec.Calc.Operations.Fminus (beta:=beta) s c
    have hrepBounded : Fbounded (beta:=beta) bo rep := by
      have hhalf :
          (1 / 2 : ℝ) * _root_.F2R (beta:=beta) c ≤
            _root_.F2R (beta:=beta) s := by
        nlinarith
      have hupper :
          _root_.F2R (beta:=beta) s ≤
            2 * _root_.F2R (beta:=beta) c := by
        nlinarith
      have hTrip := Sterbenz (beta:=beta) bo s c
      simpa [wp, PostCond.noThrow, pure, Sterbenz_check, Fminus, rep, Id.run,
        ULift.up_down] using
        hTrip ⟨hβ, hsBounded, hcBounded, hhalf, hupper⟩
    have hrepVal :
        _root_.F2R (beta:=beta) rep =
          _root_.F2R (beta:=beta) s - _root_.F2R (beta:=beta) c := by
      have hTrip := Fminus_correct (beta:=beta) s c
      simpa [wp, PostCond.noThrow, pure, Fminus_correct_check, Fminus, rep, Id.run,
        ULift.up_down] using hTrip hβ
    have h := IminusCorrectEq (beta:=beta) bo (2 : ℝ) Iplus Iminus
      IplusCorrect IminusPlus s c rep
      (_root_.F2R (beta:=beta) s - _root_.F2R (beta:=beta) c)
      hsBounded hcBounded hrepBounded (Eq.symm hrepVal)
    simpa [rep] using h (Eq.symm hrepVal)
  have hD :
      _root_.F2R (beta:=beta) (Iminus q c) =
        _root_.F2R (beta:=beta) q - _root_.F2R (beta:=beta) c := by
    let rep := FloatSpec.Calc.Operations.Fminus (beta:=beta) q c
    have hrepBounded : Fbounded (beta:=beta) bo rep := by
      have hhalf :
          (1 / 2 : ℝ) * _root_.F2R (beta:=beta) c ≤
            _root_.F2R (beta:=beta) q := by
        nlinarith
      have hupper :
          _root_.F2R (beta:=beta) q ≤
            2 * _root_.F2R (beta:=beta) c := by
        have hhalfq_le_c : halfq ≤ _root_.F2R (beta:=beta) c :=
          le_trans hRle1 hRle2
        change (1 / 2 : ℝ) * _root_.F2R (beta:=beta) q ≤
          _root_.F2R (beta:=beta) c at hhalfq_le_c
        nlinarith [hhalfq_le_c]
      have hTrip := Sterbenz (beta:=beta) bo q c
      simpa [wp, PostCond.noThrow, pure, Sterbenz_check, Fminus, rep, Id.run,
        ULift.up_down] using
        hTrip ⟨hβ, hq, hcBounded, hhalf, hupper⟩
    have hrepVal :
        _root_.F2R (beta:=beta) rep =
          _root_.F2R (beta:=beta) q - _root_.F2R (beta:=beta) c := by
      have hTrip := Fminus_correct (beta:=beta) q c
      simpa [wp, PostCond.noThrow, pure, Fminus_correct_check, Fminus, rep, Id.run,
        ULift.up_down] using hTrip hβ
    have h := IminusCorrectEq (beta:=beta) bo (2 : ℝ) Iplus Iminus
      IplusCorrect IminusPlus q c rep
      (_root_.F2R (beta:=beta) q - _root_.F2R (beta:=beta) c)
      hq hcBounded hrepBounded (Eq.symm hrepVal)
    simpa [rep] using h (Eq.symm hrepVal)
  have hM := MKnuth1_from_format_payload (beta:=beta) bo beta precision Iplus Iminus
    hIplusCorrectBeta IminusPlus rfl hβ hprecision hvNum_gt hvNum
    p q hp hq
  simpa [s, c] using hM hD hB

/-- Exactness subgoal used by Coq `MKnuth5`.

For `s = Iplus p q` and `c = Iminus s p`, upstream proves
`Iminus s c = s - c` by applying `minusRoundRep_from_rounded_payload` to the rounded subtraction
`c` of `s - p`, then projecting the exact representative through
`IminusCorrectEq`. -/
theorem MKnuth5_s_minus_c_exact {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (precision : Nat)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo (beta : ℝ)
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q))
    (hβ : 1 < beta)
    (hprecision : precision ≠ 0)
    (hvNum : bo.vNum = Zpower_nat beta precision)
    (hvNum_gt : 1 < bo.vNum)
    (hBoundExp :
      ∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=beta) beta r).Fexp)
    (hTotal : TotalP (Closest (beta:=beta) bo (beta : ℝ))) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      0 ≤ _root_.F2R (beta:=beta) p →
      _root_.F2R (beta:=beta) p ≤
        _root_.F2R (beta:=beta) (Iplus p q) →
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      _root_.F2R (beta:=beta)
          (Iminus (Iplus p q) (Iminus (Iplus p q) p)) =
        _root_.F2R (beta:=beta) (Iplus p q) -
          _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) := by
  intro p q hp_nonneg hp_le_s hp hq
  let s := Iplus p q
  let c := Iminus s p
  have hsBounded : Fbounded (beta:=beta) bo s :=
    IplusBounded (beta:=beta) bo (beta : ℝ) Iplus IplusCorrect p q hp hq
  have hcBounded : Fbounded (beta:=beta) bo c :=
    IminusBounded (beta:=beta) bo (beta : ℝ) Iplus Iminus IplusCorrect
      IminusPlus s p hsBounded hp
  have hRoundedFull :
      RoundedModeP_full (beta:=beta) bo
        (Closest (beta:=beta) bo (beta : ℝ)) := by
    have h := ClosestRoundedModeP_from_total_payload (beta:=beta) bo (beta : ℝ)
    simpa [wp, PostCond.noThrow, pure, ClosestRoundedModeP_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hTotal
  have hRoundedFloat :
      RoundedModeP_float (beta:=beta) bo beta
        (Closest (beta:=beta) bo (beta : ℝ)) := by
    refine ⟨hTotal, ?_, ?_, ?_⟩
    · have h := ClosestCompatible_from_structural_payload (beta:=beta) bo (beta : ℝ)
      simpa [wp, PostCond.noThrow, pure, ClosestCompatible_check,
        PredTrans.pure, PredTrans.apply, Id.run, ULift.down] using h trivial
    · have h := ClosestMinOrMax_from_split_radix_payload (beta:=beta) bo beta (beta : ℝ)
      simpa [wp, PostCond.noThrow, pure, ClosestMinOrMax_check, MinOrMaxP_float,
        Id.run, ULift.up_down, MinOrMaxP_float] using h trivial
    · have h := ClosestMonotone_from_split_radix_payload (beta:=beta) bo (beta : ℝ)
      simpa [wp, PostCond.noThrow, pure, ClosestMonotone_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hcClosest :
      Closest (beta:=beta) bo (beta : ℝ)
        (_root_.F2R (beta:=beta) s - _root_.F2R (beta:=beta) p) c := by
    have h := IminusCorrect (beta:=beta) bo (beta : ℝ) Iplus Iminus
      IplusCorrect IminusPlus s p hsBounded hp
    simpa [c] using h
  have hRepTrip := minusRoundRep_from_rounded_payload (beta:=beta) bo beta precision
    (Closest (beta:=beta) bo (beta : ℝ)) p s c c
  have hRep :
      ∃ r : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo r ∧
          _root_.F2R (beta:=beta) r =
            _root_.F2R (beta:=beta) s - _root_.F2R (beta:=beta) c := by
    simpa [wp, PostCond.noThrow, pure, minusRoundRep_check, Id.run,
      ULift.up_down, s, c] using
      hRepTrip ⟨rfl, hβ, hprecision, hvNum, hvNum_gt,
        hBoundExp ((1 / 2 : ℝ) * _root_.F2R (beta:=beta) s),
        hRoundedFull, hRoundedFloat, hp_nonneg, hp_le_s, hcClosest,
        hp, hsBounded⟩
  rcases hRep with ⟨rep, hrepBounded, hrepVal⟩
  have hExact := IminusCorrectEq (beta:=beta) bo (beta : ℝ) Iplus Iminus
    IplusCorrect IminusPlus s c rep
    (_root_.F2R (beta:=beta) s - _root_.F2R (beta:=beta) c)
    hsBounded hcBounded hrepBounded (Eq.symm hrepVal)
  simpa [s, c] using hExact (Eq.symm hrepVal)

/-- First branch of Coq `MKnuth5`.

In the positive `p < q` case, upstream first handles the subcase
`q <= c`, where `c = Iminus (Iplus p q) p`.  The second exact subtraction
`q - c` is then a direct Sterbenz subtraction because `c <= s <= 2*q`.
The complementary `c < q` subcase is the one that still needs the full
`ExactMinusInterval` predecessor induction. -/
theorem MKnuth5_q_le_c {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (precision : Nat)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo (2 : ℝ)
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q))
    (hβ : 1 < beta)
    (hbetaTwo : (beta : ℝ) = 2)
    (hprecision : precision ≠ 0)
    (hvNum : bo.vNum = Zpower_nat beta precision)
    (hvNum_gt : 1 < bo.vNum)
    (hBoundExp :
      ∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=beta) beta r).Fexp)
    (hTotal : TotalP (Closest (beta:=beta) bo (2 : ℝ))) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      0 < _root_.F2R (beta:=beta) p →
      _root_.F2R (beta:=beta) p < _root_.F2R (beta:=beta) q →
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      _root_.F2R (beta:=beta) p ≤
        _root_.F2R (beta:=beta) (Iplus p q) →
      _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) ≤
        _root_.F2R (beta:=beta) (Iplus p q) →
      _root_.F2R (beta:=beta) (Iplus p q) ≤
        2 * _root_.F2R (beta:=beta) q →
      _root_.F2R (beta:=beta) q ≤
        _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) →
      _root_.F2R (beta:=beta)
          (Iplus
            (Iminus p
              (Iminus (Iplus p q) (Iminus (Iplus p q) p)))
            (Iminus q (Iminus (Iplus p q) p))) =
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) (Iplus p q) := by
  intro p q hp_pos hp_lt_q hp hq
  let s := Iplus p q
  let c := Iminus s p
  intro hp_le_s hc_le_s hs_le_2q hq_le_c
  have hIplusCorrectBeta :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo (beta : ℝ)
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q) := by
    intro p q hp hq
    simpa [hbetaTwo] using IplusCorrect p q hp hq
  have hsBounded : Fbounded (beta:=beta) bo s :=
    IplusBounded (beta:=beta) bo (2 : ℝ) Iplus IplusCorrect p q hp hq
  have hcBounded : Fbounded (beta:=beta) bo c :=
    IminusBounded (beta:=beta) bo (2 : ℝ) Iplus Iminus IplusCorrect
      IminusPlus s p hsBounded hp
  have hTotalBeta : TotalP (Closest (beta:=beta) bo (beta : ℝ)) := by
    simpa [hbetaTwo] using hTotal
  have hB :
      _root_.F2R (beta:=beta) (Iminus s c) =
        _root_.F2R (beta:=beta) s - _root_.F2R (beta:=beta) c := by
    have h := MKnuth5_s_minus_c_exact (beta:=beta) bo precision Iplus Iminus
      hIplusCorrectBeta IminusPlus hβ hprecision hvNum hvNum_gt hBoundExp
      hTotalBeta p q (le_of_lt hp_pos) hp_le_s hp hq
    simpa [s, c] using h
  have hD :
      _root_.F2R (beta:=beta) (Iminus q c) =
        _root_.F2R (beta:=beta) q - _root_.F2R (beta:=beta) c := by
    let rep := FloatSpec.Calc.Operations.Fminus (beta:=beta) q c
    have hrepBounded : Fbounded (beta:=beta) bo rep := by
      have hhalf :
          (1 / 2 : ℝ) * _root_.F2R (beta:=beta) c ≤
            _root_.F2R (beta:=beta) q := by
        nlinarith
      have hupper :
          _root_.F2R (beta:=beta) q ≤
            2 * _root_.F2R (beta:=beta) c := by
        nlinarith
      have hTrip := Sterbenz (beta:=beta) bo q c
      simpa [wp, PostCond.noThrow, pure, Sterbenz_check, Fminus, rep, Id.run,
        ULift.up_down] using
        hTrip ⟨hβ, hq, hcBounded, hhalf, hupper⟩
    have hrepVal :
        _root_.F2R (beta:=beta) rep =
          _root_.F2R (beta:=beta) q - _root_.F2R (beta:=beta) c := by
      have hTrip := Fminus_correct (beta:=beta) q c
      simpa [wp, PostCond.noThrow, pure, Fminus_correct_check, Fminus, rep, Id.run,
        ULift.up_down] using hTrip hβ
    have h := IminusCorrectEq (beta:=beta) bo (2 : ℝ) Iplus Iminus
      IplusCorrect IminusPlus q c rep
      (_root_.F2R (beta:=beta) q - _root_.F2R (beta:=beta) c)
      hq hcBounded hrepBounded (Eq.symm hrepVal)
    simpa [rep] using h (Eq.symm hrepVal)
  have hM := MKnuth1_from_format_payload (beta:=beta) bo beta precision Iplus Iminus
    hIplusCorrectBeta IminusPlus rfl hβ hprecision hvNum_gt hvNum
    p q hp hq
  simpa [s, c] using hM hD hB

/-- Complementary branch of Coq `MKnuth5`, factored at `ExactMinusInterval`.

In the positive `p < q` case with `c = Iminus (Iplus p q) p`, this handles
the `c < q` branch once the exact interval theorem can provide a bounded
representative for `q - c` from the already exact representative of `s - c`.
-/
theorem MKnuth5_c_lt_q_from_interval {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (precision : Nat)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo (2 : ℝ)
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q))
    (hInterval :
      ∀ a b r : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo a →
        Fbounded (beta:=beta) bo b →
        Fbounded (beta:=beta) bo r →
        0 ≤ _root_.F2R (beta:=beta) a →
        _root_.F2R (beta:=beta) a ≤ _root_.F2R (beta:=beta) b →
        _root_.F2R (beta:=beta) a ≤ _root_.F2R (beta:=beta) r →
        _root_.F2R (beta:=beta) r ≤ _root_.F2R (beta:=beta) b →
        (∃ ab : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded (beta:=beta) bo ab ∧
            _root_.F2R (beta:=beta) ab =
              _root_.F2R (beta:=beta) b - _root_.F2R (beta:=beta) a) →
        ∃ ar : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded (beta:=beta) bo ar ∧
            _root_.F2R (beta:=beta) ar =
              _root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) a)
    (hβ : 1 < beta)
    (hbetaTwo : (beta : ℝ) = 2)
    (hprecision : precision ≠ 0)
    (hvNum : bo.vNum = Zpower_nat beta precision)
    (hvNum_gt : 1 < bo.vNum)
    (hBoundExp :
      ∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=beta) beta r).Fexp)
    (hTotal : TotalP (Closest (beta:=beta) bo (2 : ℝ))) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      0 < _root_.F2R (beta:=beta) p →
      _root_.F2R (beta:=beta) p < _root_.F2R (beta:=beta) q →
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      _root_.F2R (beta:=beta) p ≤
        _root_.F2R (beta:=beta) (Iplus p q) →
      0 ≤ _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) →
      _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) ≤
        _root_.F2R (beta:=beta) (Iplus p q) →
      _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) <
        _root_.F2R (beta:=beta) q →
      _root_.F2R (beta:=beta) q ≤
        _root_.F2R (beta:=beta) (Iplus p q) →
      _root_.F2R (beta:=beta)
          (Iplus
            (Iminus p
              (Iminus (Iplus p q) (Iminus (Iplus p q) p)))
            (Iminus q (Iminus (Iplus p q) p))) =
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) (Iplus p q) := by
  intro p q hp_pos hp_lt_q hp hq
  let s := Iplus p q
  let c := Iminus s p
  intro hp_le_s hc_nonneg hc_le_s hc_lt_q hq_le_s
  have hIplusCorrectBeta :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo (beta : ℝ)
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q) := by
    intro p q hp hq
    simpa [hbetaTwo] using IplusCorrect p q hp hq
  have hsBounded : Fbounded (beta:=beta) bo s :=
    IplusBounded (beta:=beta) bo (2 : ℝ) Iplus IplusCorrect p q hp hq
  have hcBounded : Fbounded (beta:=beta) bo c :=
    IminusBounded (beta:=beta) bo (2 : ℝ) Iplus Iminus IplusCorrect
      IminusPlus s p hsBounded hp
  have hTotalBeta : TotalP (Closest (beta:=beta) bo (beta : ℝ)) := by
    simpa [hbetaTwo] using hTotal
  have hB :
      _root_.F2R (beta:=beta) (Iminus s c) =
        _root_.F2R (beta:=beta) s - _root_.F2R (beta:=beta) c := by
    have h := MKnuth5_s_minus_c_exact (beta:=beta) bo precision Iplus Iminus
      hIplusCorrectBeta IminusPlus hβ hprecision hvNum hvNum_gt hBoundExp
      hTotalBeta p q (le_of_lt hp_pos) hp_le_s hp hq
    simpa [s, c] using h
  have hscBounded : Fbounded (beta:=beta) bo (Iminus s c) :=
    IminusBounded (beta:=beta) bo (2 : ℝ) Iplus Iminus IplusCorrect
      IminusPlus s c hsBounded hcBounded
  have hD :
      _root_.F2R (beta:=beta) (Iminus q c) =
        _root_.F2R (beta:=beta) q - _root_.F2R (beta:=beta) c := by
    rcases hInterval c s q hcBounded hsBounded hq hc_nonneg hc_le_s
        (le_of_lt hc_lt_q) hq_le_s ⟨Iminus s c, hscBounded, hB⟩ with
      ⟨rep, hrepBounded, hrepVal⟩
    have h := IminusCorrectEq (beta:=beta) bo (2 : ℝ) Iplus Iminus
      IplusCorrect IminusPlus q c rep
      (_root_.F2R (beta:=beta) q - _root_.F2R (beta:=beta) c)
      hq hcBounded hrepBounded (Eq.symm hrepVal)
    exact h (Eq.symm hrepVal)
  have hM := MKnuth1_from_format_payload (beta:=beta) bo beta precision Iplus Iminus
    hIplusCorrectBeta IminusPlus rfl hβ hprecision hvNum_gt hvNum
    p q hp hq
  simpa [s, c] using hM hD hB

/-- Positive case of Coq `MKnuth5`, factored at `ExactMinusInterval`.

This packages the side-condition work from the upstream proof: closest
monotonicity gives `p <= s` and `q <= s`, rounded-mode nonnegativity gives
`0 <= c`, and radix scaling gives `s <= 2*q`.  The remaining exact-minus
payload is still explicit as `hInterval`; once `ExactMinusInterval` is closed,
this becomes the direct upstream `MKnuth5` theorem. -/
theorem MKnuth5_from_interval {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (precision : Nat)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo (2 : ℝ)
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q))
    (hInterval :
      ∀ a b r : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo a →
        Fbounded (beta:=beta) bo b →
        Fbounded (beta:=beta) bo r →
        0 ≤ _root_.F2R (beta:=beta) a →
        _root_.F2R (beta:=beta) a ≤ _root_.F2R (beta:=beta) b →
        _root_.F2R (beta:=beta) a ≤ _root_.F2R (beta:=beta) r →
        _root_.F2R (beta:=beta) r ≤ _root_.F2R (beta:=beta) b →
        (∃ ab : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded (beta:=beta) bo ab ∧
            _root_.F2R (beta:=beta) ab =
              _root_.F2R (beta:=beta) b - _root_.F2R (beta:=beta) a) →
        ∃ ar : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded (beta:=beta) bo ar ∧
            _root_.F2R (beta:=beta) ar =
              _root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) a)
    (hβ : 1 < beta)
    (hbetaTwo : (beta : ℝ) = 2)
    (hprecision : precision ≠ 0)
    (hvNum : bo.vNum = Zpower_nat beta precision)
    (hvNum_gt : 1 < bo.vNum)
    (hBoundExp :
      ∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=beta) beta r).Fexp)
    (hTotal : TotalP (Closest (beta:=beta) bo (2 : ℝ))) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      0 < _root_.F2R (beta:=beta) p →
      _root_.F2R (beta:=beta) p < _root_.F2R (beta:=beta) q →
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      _root_.F2R (beta:=beta)
          (Iplus
            (Iminus p
              (Iminus (Iplus p q) (Iminus (Iplus p q) p)))
            (Iminus q (Iminus (Iplus p q) p))) =
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) (Iplus p q) := by
  intro p q hp_pos hp_lt_q hp hq
  let s := Iplus p q
  let c := Iminus s p
  have hq_pos : 0 < _root_.F2R (beta:=beta) q := lt_trans hp_pos hp_lt_q
  have hsClosest :
      Closest (beta:=beta) bo (2 : ℝ)
        (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) s := by
    simpa [s] using IplusCorrect p q hp hq
  have hsBounded : Fbounded (beta:=beta) bo s :=
    IplusBounded (beta:=beta) bo (2 : ℝ) Iplus IplusCorrect p q hp hq
  have hcClosest :
      Closest (beta:=beta) bo (2 : ℝ)
        (_root_.F2R (beta:=beta) s - _root_.F2R (beta:=beta) p) c := by
    have h := IminusCorrect (beta:=beta) bo (2 : ℝ) Iplus Iminus
      IplusCorrect IminusPlus s p hsBounded hp
    simpa [c] using h
  have hcBounded : Fbounded (beta:=beta) bo c := hcClosest.1
  have hRounded :
      RoundedModeP_full (beta:=beta) bo
        (Closest (beta:=beta) bo (2 : ℝ)) := by
    have hTrip := ClosestRoundedModeP_from_total_payload (beta:=beta) bo (2 : ℝ)
    simpa [wp, PostCond.noThrow, pure, ClosestRoundedModeP_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using hTrip hTotal
  have hmono : MonotoneP_float (Closest (beta:=beta) bo (2 : ℝ)) :=
    hRounded.2.2.1
  have hpProject :
      Closest (beta:=beta) bo (2 : ℝ)
        (_root_.F2R (beta:=beta) p) p :=
    hRounded.2.2.2.1 p hp
  have hqProject :
      Closest (beta:=beta) bo (2 : ℝ)
        (_root_.F2R (beta:=beta) q) q :=
    hRounded.2.2.2.1 q hq
  have hsProject :
      Closest (beta:=beta) bo (2 : ℝ)
        (_root_.F2R (beta:=beta) s) s :=
    hRounded.2.2.2.1 s hsBounded
  have hp_le_s :
      _root_.F2R (beta:=beta) p ≤ _root_.F2R (beta:=beta) s := by
    have hlt :
        _root_.F2R (beta:=beta) p <
          _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q := by
      nlinarith
    exact hmono _ _ p s hlt hpProject hsClosest
  have hq_le_s :
      _root_.F2R (beta:=beta) q ≤ _root_.F2R (beta:=beta) s := by
    have hlt :
        _root_.F2R (beta:=beta) q <
          _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q := by
      nlinarith
    exact hmono _ _ q s hlt hqProject hsClosest
  have hc_nonneg : 0 ≤ _root_.F2R (beta:=beta) c := by
    have hinput_nonneg :
        0 ≤ _root_.F2R (beta:=beta) s - _root_.F2R (beta:=beta) p := by
      nlinarith
    rcases eq_or_lt_of_le hinput_nonneg with hzero | hpos
    · have hczero : _root_.F2R (beta:=beta) c = 0 := by
        have hTrip := ClosestZero_from_expanded_closest_payload (beta:=beta) bo (2 : ℝ)
          (_root_.F2R (beta:=beta) s - _root_.F2R (beta:=beta) p) c
        simpa [wp, PostCond.noThrow, pure, ClosestZero_check, Fbounded',
          Id.run, ULift.up_down, Int.cast_ofNat] using
          hTrip ⟨hcClosest, hzero.symm, hcBounded, hcClosest.2⟩
      linarith
    · let zf : FloatSpec.Core.Defs.FlocqFloat beta := ⟨0, -bo.dExp⟩
      have hzfBounded : Fbounded (beta:=beta) bo zf := by
        constructor
        · show |zf.Fnum| < bo.vNum
          simpa [zf] using (lt_trans zero_lt_one hvNum_gt)
        · show -bo.dExp ≤ zf.Fexp
          simp [zf]
      have hzfClosest :
          Closest (beta:=beta) bo (2 : ℝ) 0 zf := by
        refine ⟨hzfBounded, ?_⟩
        intro g hg
        simpa [zf, _root_.F2R, FloatSpec.Core.Defs.F2R] using
          (abs_nonneg (_root_.F2R (beta:=beta) g - 0))
      have hres := hmono 0
        (_root_.F2R (beta:=beta) s - _root_.F2R (beta:=beta) p)
        zf c hpos hzfClosest hcClosest
      simpa [zf, _root_.F2R, FloatSpec.Core.Defs.F2R] using hres
  have hc_le_s :
      _root_.F2R (beta:=beta) c ≤ _root_.F2R (beta:=beta) s := by
    have hlt :
        _root_.F2R (beta:=beta) s - _root_.F2R (beta:=beta) p <
          _root_.F2R (beta:=beta) s := by
      nlinarith
    exact hmono _ _ c s hlt hcClosest hsProject
  have hs_le_2q :
      _root_.F2R (beta:=beta) s ≤ 2 * _root_.F2R (beta:=beta) q := by
    let scaled : FloatSpec.Core.Defs.FlocqFloat beta := ⟨q.Fnum, q.Fexp + 1⟩
    have hScaledBounded : Fbounded (beta:=beta) bo scaled := by
      have hTrip := FBoundedScale (beta:=beta) bo q 1
      simpa [wp, PostCond.noThrow, pure, FBoundedScale_check, scaled,
        Id.run, ULift.up_down, Int.cast_ofNat] using hTrip hq
    have hScaledValue :
        _root_.F2R (beta:=beta) scaled =
          (2 : ℝ) * _root_.F2R (beta:=beta) q := by
      have hβne : (beta : ℝ) ≠ 0 := by
        have hβposInt : (0 : Int) < beta := lt_trans (by decide) hβ
        have hβpos : (0 : ℝ) < beta := by exact_mod_cast hβposInt
        exact ne_of_gt hβpos
      have hTrip := FvalScale beta bo q 1
      have hscale :
          _root_.F2R (beta:=beta) scaled =
            ((beta : ℝ) ^ (Int.ofNat 1)) *
              _root_.F2R (beta:=beta) q := by
        simpa [wp, PostCond.noThrow, pure, FvalScale_check, scaled,
          Id.run, ULift.up_down, Int.cast_ofNat] using hTrip hβne
      simpa [hbetaTwo] using hscale
    have hUpper :
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q ≤
          2 * _root_.F2R (beta:=beta) q := by
      nlinarith
    have hTrip := RoundedModeMult_from_scale_payload (beta:=beta) bo (2 : ℝ)
      (Closest (beta:=beta) bo (2 : ℝ))
      (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) s q
    simpa [wp, PostCond.noThrow, pure, RoundedModeMult_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      hTrip ⟨hRounded, hsClosest, hScaledBounded, hScaledValue, hUpper⟩
  by_cases hq_le_c :
      _root_.F2R (beta:=beta) q ≤ _root_.F2R (beta:=beta) c
  · exact MKnuth5_q_le_c (beta:=beta) bo precision Iplus Iminus
      IplusCorrect IminusPlus hβ hbetaTwo hprecision hvNum hvNum_gt
      hBoundExp hTotal p q hp_pos hp_lt_q hp hq hp_le_s hc_le_s
      hs_le_2q hq_le_c
  · have hc_lt_q :
        _root_.F2R (beta:=beta) c < _root_.F2R (beta:=beta) q :=
      lt_of_not_ge hq_le_c
    exact MKnuth5_c_lt_q_from_interval (beta:=beta) bo precision Iplus Iminus
      IplusCorrect IminusPlus hInterval hβ hbetaTwo hprecision hvNum hvNum_gt
      hBoundExp hTotal p q hp_pos hp_lt_q hp hq hp_le_s hc_nonneg hc_le_s
      hc_lt_q hq_le_s

/-- Coq: `MKnuth5`.

The positive `p < q` Knuth branch is now closed by the restored
`ExactMinusInterval`, removing the explicit interval premise from
`MKnuth5_from_interval`. -/
theorem MKnuth5_from_total_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (precision : Nat)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo (2 : ℝ)
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q))
    (hβ : 1 < beta)
    (hbetaTwo : (beta : ℝ) = 2)
    (hprecision : 1 < precision)
    (hvNum : bo.vNum = Zpower_nat beta precision)
    (hvNum_gt : 1 < bo.vNum)
    (hTotal : TotalP (Closest (beta:=beta) bo (2 : ℝ))) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      0 < _root_.F2R (beta:=beta) p →
      _root_.F2R (beta:=beta) p < _root_.F2R (beta:=beta) q →
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      _root_.F2R (beta:=beta)
          (Iplus
            (Iminus p
              (Iminus (Iplus p q) (Iminus (Iplus p q) p)))
            (Iminus q (Iminus (Iplus p q) p))) =
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) (Iplus p q) := by
  have hBoundExp :
      ∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=beta) beta r).Fexp := by
    intro r
    exact le_trans (neg_nonpos.mpr bo.dExp_nonneg) (by simp [boundR, boundNat])
  have hprecision_ne : precision ≠ 0 := by omega
  have hInterval := ExactMinusInterval (beta:=beta) bo precision
    hβ hbetaTwo hprecision hvNum
  exact MKnuth5_from_interval (beta:=beta) bo precision Iplus Iminus
    IplusCorrect IminusPlus hInterval hβ hbetaTwo hprecision_ne hvNum
    hvNum_gt hBoundExp hTotal

/-- Coq: `MKnuth6`.

If the abstract addition is already exact, the first subtraction is exact by
`IminusCorrectEq`, so the generic `MKnuth` payload applies. -/
theorem MKnuth6_from_format_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (IplusSym :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iplus p q = Iplus q p)
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q))
    (hbeta : beta = radix)
    (hradix : 1 < radix)
    (hprecision : precision ≠ 0)
    (hvNum_gt : 1 < bo.vNum)
    (hvNum : bo.vNum = Zpower_nat radix precision) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) (Iplus p q) =
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q →
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      _root_.F2R (beta:=beta)
          (Iplus
            (Iminus p
              (Iminus (Iplus p q) (Iminus (Iplus p q) p)))
            (Iminus q (Iminus (Iplus p q) p))) =
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) (Iplus p q) := by
  intro p q hExact hp hq
  have hpqBounded : Fbounded (beta:=beta) bo (Iplus p q) :=
    IplusBounded (beta:=beta) bo (radix : ℝ) Iplus IplusCorrect p q hp hq
  have hfirst_to_q :
      _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) =
        _root_.F2R (beta:=beta) q := by
    have h := IminusCorrectEq (beta:=beta) bo (radix : ℝ) Iplus Iminus
      IplusCorrect IminusPlus (Iplus p q) p q (_root_.F2R (beta:=beta) q)
      hpqBounded hp hq rfl
    exact h (by
      rw [hExact]
      ring)
  have hFirst :
      _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) =
        _root_.F2R (beta:=beta) (Iplus p q) -
          _root_.F2R (beta:=beta) p := by
    rw [hfirst_to_q, hExact]
    ring
  exact MKnuth_from_format_payload (beta:=beta) bo radix precision Iplus Iminus IplusCorrect
    IplusSym IminusPlus hbeta hradix hprecision hvNum_gt hvNum
    p q hp hq hFirst

/-- Coq `MKnuth7`, factored at the remaining positive `MKnuth5` case.

Upstream `MKnuth7` is a short sign split after `MKnuth5`: for nonnegative
positive `p` it delegates to `MKnuth5`; at `p = 0` it uses `MKnuth6`; and for
negative `p` it delegates to `MKnuth3` or `MKnuth4` depending on the
`q <= 2*(-p)` boundary. -/
theorem MKnuth7_from_MKnuth5 {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (precision : Nat)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo (2 : ℝ)
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (IplusSym :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iplus p q = Iplus q p)
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q))
    (hβ : 1 < beta)
    (hbetaTwo : (beta : ℝ) = 2)
    (hprecision : precision ≠ 0)
    (hvNum : bo.vNum = Zpower_nat beta precision)
    (hvNum_gt : 1 < bo.vNum)
    (hBoundExp :
      ∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=beta) beta r).Fexp)
    (hTotal : TotalP (Closest (beta:=beta) bo (2 : ℝ)))
    (MKnuth5 :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        0 < _root_.F2R (beta:=beta) p →
        _root_.F2R (beta:=beta) p < _root_.F2R (beta:=beta) q →
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        _root_.F2R (beta:=beta)
            (Iplus
              (Iminus p
                (Iminus (Iplus p q) (Iminus (Iplus p q) p)))
              (Iminus q (Iminus (Iplus p q) p))) =
          _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
            _root_.F2R (beta:=beta) (Iplus p q)) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      |_root_.F2R (beta:=beta) p| < _root_.F2R (beta:=beta) q →
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      _root_.F2R (beta:=beta)
          (Iplus
            (Iminus p
              (Iminus (Iplus p q) (Iminus (Iplus p q) p)))
            (Iminus q (Iminus (Iplus p q) p))) =
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) (Iplus p q) := by
  intro p q hAbs hp hq
  have hq_pos : 0 < _root_.F2R (beta:=beta) q :=
    lt_of_le_of_lt (abs_nonneg _) hAbs
  have hIplusCorrectBeta :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo (beta : ℝ)
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q) := by
    intro p q hp hq
    simpa [hbetaTwo] using IplusCorrect p q hp hq
  by_cases hp_nonneg : 0 ≤ _root_.F2R (beta:=beta) p
  · rcases lt_or_eq_of_le hp_nonneg with hp_pos | hp_zero
    · have hp_lt_q : _root_.F2R (beta:=beta) p < _root_.F2R (beta:=beta) q := by
        rw [abs_of_nonneg hp_nonneg] at hAbs
        exact hAbs
      exact MKnuth5 p q hp_pos hp_lt_q hp hq
    · have hExact :
        _root_.F2R (beta:=beta) (Iplus p q) =
          _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q := by
        have h := IplusCorrectEq (beta:=beta) bo (beta : ℝ) Iplus
          hIplusCorrectBeta p q q (_root_.F2R (beta:=beta) q)
          hp hq hq rfl
        have hq_exact :
            _root_.F2R (beta:=beta) (Iplus p q) =
              _root_.F2R (beta:=beta) q :=
          h (by rw [← hp_zero]; ring)
        calc
          _root_.F2R (beta:=beta) (Iplus p q)
              = _root_.F2R (beta:=beta) q := hq_exact
          _ = _root_.F2R (beta:=beta) p +
                _root_.F2R (beta:=beta) q := by
                rw [← hp_zero]
                ring
      exact MKnuth6_from_format_payload (beta:=beta) bo beta precision Iplus Iminus
        hIplusCorrectBeta IplusSym IminusPlus rfl hβ hprecision hvNum_gt
        hvNum p q hExact hp hq
  · have hp_neg : _root_.F2R (beta:=beta) p < 0 := lt_of_not_ge hp_nonneg
    by_cases hq_le_2negp :
        _root_.F2R (beta:=beta) q ≤ 2 * -_root_.F2R (beta:=beta) p
    · have hnegp_le_q : -_root_.F2R (beta:=beta) p ≤ _root_.F2R (beta:=beta) q := by
        rw [abs_of_neg hp_neg] at hAbs
        exact le_of_lt hAbs
      exact MKnuth3_from_format_payload (beta:=beta) bo precision Iplus Iminus IplusCorrect
        IplusSym IminusPlus hβ hbetaTwo hprecision hvNum hvNum_gt
        p q (le_of_lt hq_pos) hq_le_2negp hnegp_le_q hp hq
    · have hboundary :
        2 * -_root_.F2R (beta:=beta) p < _root_.F2R (beta:=beta) q :=
        lt_of_not_ge hq_le_2negp
      exact MKnuth4_from_total_payload (beta:=beta) bo precision Iplus Iminus IplusCorrect
        IminusPlus hβ hbetaTwo hprecision hvNum hvNum_gt hTotal
        p q (by linarith) hq_pos hboundary hp hq

/-- Absolute-value Knuth case, factored at `ExactMinusInterval`.

This composes the restored positive-case side-condition theorem
`MKnuth5_from_interval` with the sign split in `MKnuth7_from_MKnuth5`.  The
only remaining premise is the exact interval payload; after that is closed,
this is the direct upstream `MKnuth7` theorem. -/
theorem MKnuth7_from_interval {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (precision : Nat)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo (2 : ℝ)
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (IplusSym :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iplus p q = Iplus q p)
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q))
    (hInterval :
      ∀ a b r : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo a →
        Fbounded (beta:=beta) bo b →
        Fbounded (beta:=beta) bo r →
        0 ≤ _root_.F2R (beta:=beta) a →
        _root_.F2R (beta:=beta) a ≤ _root_.F2R (beta:=beta) b →
        _root_.F2R (beta:=beta) a ≤ _root_.F2R (beta:=beta) r →
        _root_.F2R (beta:=beta) r ≤ _root_.F2R (beta:=beta) b →
        (∃ ab : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded (beta:=beta) bo ab ∧
            _root_.F2R (beta:=beta) ab =
              _root_.F2R (beta:=beta) b - _root_.F2R (beta:=beta) a) →
        ∃ ar : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded (beta:=beta) bo ar ∧
            _root_.F2R (beta:=beta) ar =
              _root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) a)
    (hβ : 1 < beta)
    (hbetaTwo : (beta : ℝ) = 2)
    (hprecision : precision ≠ 0)
    (hvNum : bo.vNum = Zpower_nat beta precision)
    (hvNum_gt : 1 < bo.vNum)
    (hBoundExp :
      ∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=beta) beta r).Fexp)
    (hTotal : TotalP (Closest (beta:=beta) bo (2 : ℝ))) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      |_root_.F2R (beta:=beta) p| < _root_.F2R (beta:=beta) q →
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      _root_.F2R (beta:=beta)
          (Iplus
            (Iminus p
              (Iminus (Iplus p q) (Iminus (Iplus p q) p)))
            (Iminus q (Iminus (Iplus p q) p))) =
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) (Iplus p q) := by
  have hM5 :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        0 < _root_.F2R (beta:=beta) p →
        _root_.F2R (beta:=beta) p < _root_.F2R (beta:=beta) q →
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        _root_.F2R (beta:=beta)
            (Iplus
              (Iminus p
                (Iminus (Iplus p q) (Iminus (Iplus p q) p)))
              (Iminus q (Iminus (Iplus p q) p))) =
          _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
            _root_.F2R (beta:=beta) (Iplus p q) :=
    MKnuth5_from_interval (beta:=beta) bo precision Iplus Iminus
      IplusCorrect IminusPlus hInterval hβ hbetaTwo hprecision hvNum
      hvNum_gt hBoundExp hTotal
  exact MKnuth7_from_MKnuth5 (beta:=beta) bo precision Iplus Iminus
    IplusCorrect IplusSym IminusPlus hβ hbetaTwo hprecision hvNum
    hvNum_gt hBoundExp hTotal hM5

/-- Coq: `MKnuth7`.

This removes the explicit interval premise from `MKnuth7_from_interval` by
using the restored direct `MKnuth5`. -/
theorem MKnuth7_from_total_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (precision : Nat)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo (2 : ℝ)
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (IplusSym :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iplus p q = Iplus q p)
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q))
    (hβ : 1 < beta)
    (hbetaTwo : (beta : ℝ) = 2)
    (hprecision : 1 < precision)
    (hvNum : bo.vNum = Zpower_nat beta precision)
    (hvNum_gt : 1 < bo.vNum)
    (hTotal : TotalP (Closest (beta:=beta) bo (2 : ℝ))) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      |_root_.F2R (beta:=beta) p| < _root_.F2R (beta:=beta) q →
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      _root_.F2R (beta:=beta)
          (Iplus
            (Iminus p
              (Iminus (Iplus p q) (Iminus (Iplus p q) p)))
            (Iminus q (Iminus (Iplus p q) p))) =
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) (Iplus p q) := by
  have hBoundExp :
      ∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=beta) beta r).Fexp := by
    intro r
    exact le_trans (neg_nonpos.mpr bo.dExp_nonneg) (by simp [boundR, boundNat])
  have hprecision_ne : precision ≠ 0 := by omega
  have hM5 :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        0 < _root_.F2R (beta:=beta) p →
        _root_.F2R (beta:=beta) p < _root_.F2R (beta:=beta) q →
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        _root_.F2R (beta:=beta)
            (Iplus
              (Iminus p
                (Iminus (Iplus p q) (Iminus (Iplus p q) p)))
              (Iminus q (Iminus (Iplus p q) p))) =
          _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
            _root_.F2R (beta:=beta) (Iplus p q) :=
    MKnuth5_from_total_payload (beta:=beta) bo precision Iplus Iminus IplusCorrect
      IminusPlus hβ hbetaTwo hprecision hvNum hvNum_gt hTotal
  exact MKnuth7_from_MKnuth5 (beta:=beta) bo precision Iplus Iminus
    IplusCorrect IplusSym IminusPlus hβ hbetaTwo hprecision_ne hvNum
    hvNum_gt hBoundExp hTotal hM5

/-- Coq `Knuth`, factored at `ExactMinusInterval`.

The upstream theorem is a final magnitude/sign split after `MKnuth2` and
`MKnuth7`.  This version composes those restored payloads while the exact
interval theorem is still explicit. -/
theorem Knuth_from_interval {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (precision : Nat)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo (2 : ℝ)
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (IplusSym :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iplus p q = Iplus q p)
    (IplusOp :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fopp (beta:=beta) (Iplus p q) =
          Iplus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q))
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q))
    (hInterval :
      ∀ a b r : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo a →
        Fbounded (beta:=beta) bo b →
        Fbounded (beta:=beta) bo r →
        0 ≤ _root_.F2R (beta:=beta) a →
        _root_.F2R (beta:=beta) a ≤ _root_.F2R (beta:=beta) b →
        _root_.F2R (beta:=beta) a ≤ _root_.F2R (beta:=beta) r →
        _root_.F2R (beta:=beta) r ≤ _root_.F2R (beta:=beta) b →
        (∃ ab : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded (beta:=beta) bo ab ∧
            _root_.F2R (beta:=beta) ab =
              _root_.F2R (beta:=beta) b - _root_.F2R (beta:=beta) a) →
        ∃ ar : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded (beta:=beta) bo ar ∧
            _root_.F2R (beta:=beta) ar =
              _root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) a)
    (hβ : 1 < beta)
    (hbetaTwo : (beta : ℝ) = 2)
    (hprecision : precision ≠ 0)
    (hvNum : bo.vNum = Zpower_nat beta precision)
    (hvNum_gt : 1 < bo.vNum)
    (hBoundExp :
      ∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=beta) beta r).Fexp)
    (hTotal : TotalP (Closest (beta:=beta) bo (2 : ℝ))) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      _root_.F2R (beta:=beta)
          (Iplus
            (Iminus p
              (Iminus (Iplus p q) (Iminus (Iplus p q) p)))
            (Iminus q (Iminus (Iplus p q) p))) =
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) (Iplus p q) := by
  intro p q hp hq
  by_cases hq_le_p : |_root_.F2R (beta:=beta) q| ≤
      |_root_.F2R (beta:=beta) p|
  · exact MKnuth2_internal (beta:=beta) bo precision Iplus Iminus IplusCorrect
      IplusSym IplusOp IminusPlus hβ hbetaTwo hprecision hvNum hvNum_gt
      hBoundExp hTotal p q hq_le_p hp hq
  · have hp_lt_q_abs : |_root_.F2R (beta:=beta) p| <
        |_root_.F2R (beta:=beta) q| := lt_of_not_ge hq_le_p
    by_cases hq_nonneg : 0 ≤ _root_.F2R (beta:=beta) q
    · have hp_lt_q : |_root_.F2R (beta:=beta) p| <
          _root_.F2R (beta:=beta) q := by
        rw [abs_of_nonneg hq_nonneg] at hp_lt_q_abs
        exact hp_lt_q_abs
      exact MKnuth7_from_interval (beta:=beta) bo precision Iplus Iminus
        IplusCorrect IplusSym IminusPlus hInterval hβ hbetaTwo hprecision
        hvNum hvNum_gt hBoundExp hTotal p q hp_lt_q hp hq
    · have hq_neg : _root_.F2R (beta:=beta) q < 0 := lt_of_not_ge hq_nonneg
      have hpOppBound : Fbounded (beta:=beta) bo (Fopp (beta:=beta) p) := by
        have hTrip := oppBounded (beta:=beta) bo p
        simpa [wp, PostCond.noThrow, pure, oppBounded_check, Id.run,
          ULift.up_down] using hTrip hp
      have hqOppBound : Fbounded (beta:=beta) bo (Fopp (beta:=beta) q) := by
        have hTrip := oppBounded (beta:=beta) bo q
        simpa [wp, PostCond.noThrow, pure, oppBounded_check, Id.run,
          ULift.up_down] using hTrip hq
      have hpOppVal :
          _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) =
            -_root_.F2R (beta:=beta) p := by
        have hTrip := Fopp_correct (beta:=beta) p
        simpa [wp, PostCond.noThrow, pure, Fopp_correct_check, Id.run,
          ULift.up_down] using hTrip True.intro
      have hqOppVal :
          _root_.F2R (beta:=beta) (Fopp (beta:=beta) q) =
            -_root_.F2R (beta:=beta) q := by
        have hTrip := Fopp_correct (beta:=beta) q
        simpa [wp, PostCond.noThrow, pure, Fopp_correct_check, Id.run,
          ULift.up_down] using hTrip True.intro
      have hsumOppVal :
          _root_.F2R (beta:=beta)
              (Iplus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q)) =
            -_root_.F2R (beta:=beta) (Iplus p q) := by
        rw [← IplusOp p q]
        have hTrip := Fopp_correct (beta:=beta) (Iplus p q)
        simpa [wp, PostCond.noThrow, pure, Fopp_correct_check, Id.run,
          ULift.up_down] using hTrip True.intro
      have hAbsOpp :
          |_root_.F2R (beta:=beta) (Fopp (beta:=beta) p)| <
            _root_.F2R (beta:=beta) (Fopp (beta:=beta) q) := by
        rw [hpOppVal, hqOppVal, abs_neg]
        rw [abs_of_neg hq_neg] at hp_lt_q_abs
        exact hp_lt_q_abs
      have hOppCase := MKnuth7_from_interval (beta:=beta) bo precision
        Iplus Iminus IplusCorrect IplusSym IminusPlus hInterval hβ
        hbetaTwo hprecision hvNum hvNum_gt hBoundExp hTotal
        (Fopp (beta:=beta) p) (Fopp (beta:=beta) q)
        hAbsOpp hpOppBound hqOppBound
      have hOppStruct := MKnuthOpp (beta:=beta) Iplus Iminus IplusOp
        IminusPlus p q
      have hFoppResult :
          _root_.F2R (beta:=beta)
              (Fopp (beta:=beta)
                (Iplus
                  (Iminus (Fopp (beta:=beta) p)
                    (Iminus
                      (Iplus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q))
                      (Iminus
                        (Iplus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q))
                        (Fopp (beta:=beta) p))))
                  (Iminus (Fopp (beta:=beta) q)
                    (Iminus
                      (Iplus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q))
                      (Fopp (beta:=beta) p))))) =
            -_root_.F2R (beta:=beta)
                (Iplus
                  (Iminus (Fopp (beta:=beta) p)
                    (Iminus
                      (Iplus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q))
                      (Iminus
                        (Iplus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q))
                        (Fopp (beta:=beta) p))))
                  (Iminus (Fopp (beta:=beta) q)
                    (Iminus
                      (Iplus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q))
                      (Fopp (beta:=beta) p)))) := by
        have hTrip := Fopp_correct (beta:=beta)
          (Iplus
            (Iminus (Fopp (beta:=beta) p)
              (Iminus
                (Iplus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q))
                (Iminus
                  (Iplus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q))
                  (Fopp (beta:=beta) p))))
            (Iminus (Fopp (beta:=beta) q)
              (Iminus
                (Iplus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q))
                (Fopp (beta:=beta) p)))
        )
        simpa [wp, PostCond.noThrow, pure, Fopp_correct_check, Id.run,
          ULift.up_down] using hTrip True.intro
      calc
        _root_.F2R (beta:=beta)
            (Iplus
              (Iminus p
                (Iminus (Iplus p q) (Iminus (Iplus p q) p)))
              (Iminus q (Iminus (Iplus p q) p)))
            =
          _root_.F2R (beta:=beta)
            (Fopp (beta:=beta)
              (Iplus
                (Iminus (Fopp (beta:=beta) p)
                  (Iminus
                    (Iplus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q))
                    (Iminus
                      (Iplus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q))
                      (Fopp (beta:=beta) p))))
                (Iminus (Fopp (beta:=beta) q)
                  (Iminus
                    (Iplus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q))
                    (Fopp (beta:=beta) p))))) := hOppStruct
        _ = -_root_.F2R (beta:=beta)
              (Iplus
                (Iminus (Fopp (beta:=beta) p)
                  (Iminus
                    (Iplus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q))
                    (Iminus
                      (Iplus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q))
                      (Fopp (beta:=beta) p))))
                (Iminus (Fopp (beta:=beta) q)
                  (Iminus
                    (Iplus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q))
                    (Fopp (beta:=beta) p)))) := hFoppResult
        _ = _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
              _root_.F2R (beta:=beta) (Iplus p q) := by
          rw [hOppCase, hpOppVal, hqOppVal, hsumOppVal]
          ring

/-- Coq: `Knuth`.

This closes the final Knuth magnitude/sign split by supplying the restored
`ExactMinusInterval` to `Knuth_from_interval`. -/
theorem Knuth_from_total_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (precision : Nat)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo (2 : ℝ)
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (IplusSym :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iplus p q = Iplus q p)
    (IplusOp :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fopp (beta:=beta) (Iplus p q) =
          Iplus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q))
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q))
    (hβ : 1 < beta)
    (hbetaTwo : (beta : ℝ) = 2)
    (hprecision : 1 < precision)
    (hvNum : bo.vNum = Zpower_nat beta precision)
    (hvNum_gt : 1 < bo.vNum)
    (hTotal : TotalP (Closest (beta:=beta) bo (2 : ℝ))) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      _root_.F2R (beta:=beta)
          (Iplus
            (Iminus p
              (Iminus (Iplus p q) (Iminus (Iplus p q) p)))
            (Iminus q (Iminus (Iplus p q) p))) =
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) (Iplus p q) := by
  have hBoundExp :
      ∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=beta) beta r).Fexp := by
    intro r
    exact le_trans (neg_nonpos.mpr bo.dExp_nonneg) (by simp [boundR, boundNat])
  have hprecision_ne : precision ≠ 0 := by omega
  have hInterval := ExactMinusInterval (beta:=beta) bo precision
    hβ hbetaTwo hprecision hvNum
  exact Knuth_from_interval (beta:=beta) bo precision Iplus Iminus
    IplusCorrect IplusSym IplusOp IminusPlus hInterval hβ hbetaTwo
    hprecision_ne hvNum hvNum_gt hBoundExp hTotal

/-- Exact exported Coq `MKnuth` contract. -/
theorem MKnuth
    (bo : Fbound_skel) (precision : Nat)
    (hprecision : 1 < precision)
    (hvNum : bo.vNum = Zpower_nat 2 precision)
    (Iplus : FloatSpec.Core.Defs.FlocqFloat 2 →
      FloatSpec.Core.Defs.FlocqFloat 2 →
      FloatSpec.Core.Defs.FlocqFloat 2)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat 2,
        Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
        Closest (beta:=2) bo (2 : ℝ)
          (_root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q) (Iplus p q))
    (_IplusCompatible :
      ∀ p q r s : FloatSpec.Core.Defs.FlocqFloat 2,
        Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
        Fbounded (beta:=2) bo r → Fbounded (beta:=2) bo s →
        _root_.F2R (beta:=2) p = _root_.F2R (beta:=2) r →
        _root_.F2R (beta:=2) q = _root_.F2R (beta:=2) s →
        _root_.F2R (beta:=2) (Iplus p q) =
          _root_.F2R (beta:=2) (Iplus r s))
    (IplusSym : ∀ p q, Iplus p q = Iplus q p)
    (Iminus : FloatSpec.Core.Defs.FlocqFloat 2 →
      FloatSpec.Core.Defs.FlocqFloat 2 →
      FloatSpec.Core.Defs.FlocqFloat 2)
    (IminusPlus : ∀ p q, Iminus p q = Iplus p (Fopp (beta:=2) q)) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat 2,
      Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
      _root_.F2R (beta:=2) (Iminus (Iplus p q) p) =
        _root_.F2R (beta:=2) (Iplus p q) - _root_.F2R (beta:=2) p →
      _root_.F2R (beta:=2)
          (Iplus
            (Iminus p (Iminus (Iplus p q) (Iminus (Iplus p q) p)))
            (Iminus q (Iminus (Iplus p q) p))) =
        _root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q -
          _root_.F2R (beta:=2) (Iplus p q) := by
  have hprecision_ne : precision ≠ 0 := by omega
  have hvNum_gt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ (by decide) hprecision_ne
  exact MKnuth_from_format_payload (beta:=2) bo 2 precision Iplus Iminus
    IplusCorrect IplusSym IminusPlus rfl (by decide) hprecision_ne hvNum_gt hvNum

/-- Exact exported Coq `MKnuth1` contract. -/
theorem MKnuth1
    (bo : Fbound_skel) (precision : Nat)
    (hprecision : 1 < precision)
    (hvNum : bo.vNum = Zpower_nat 2 precision)
    (Iplus : FloatSpec.Core.Defs.FlocqFloat 2 →
      FloatSpec.Core.Defs.FlocqFloat 2 → FloatSpec.Core.Defs.FlocqFloat 2)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat 2,
        Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
        Closest (beta:=2) bo (2 : ℝ)
          (_root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q) (Iplus p q))
    (_IplusCompatible :
      ∀ p q r s : FloatSpec.Core.Defs.FlocqFloat 2,
        Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
        Fbounded (beta:=2) bo r → Fbounded (beta:=2) bo s →
        _root_.F2R (beta:=2) p = _root_.F2R (beta:=2) r →
        _root_.F2R (beta:=2) q = _root_.F2R (beta:=2) s →
        _root_.F2R (beta:=2) (Iplus p q) =
          _root_.F2R (beta:=2) (Iplus r s))
    (Iminus : FloatSpec.Core.Defs.FlocqFloat 2 →
      FloatSpec.Core.Defs.FlocqFloat 2 → FloatSpec.Core.Defs.FlocqFloat 2)
    (IminusPlus : ∀ p q, Iminus p q = Iplus p (Fopp (beta:=2) q)) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat 2,
      Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
      _root_.F2R (beta:=2) (Iminus q (Iminus (Iplus p q) p)) =
        _root_.F2R (beta:=2) q -
          _root_.F2R (beta:=2) (Iminus (Iplus p q) p) →
      _root_.F2R (beta:=2)
          (Iminus (Iplus p q) (Iminus (Iplus p q) p)) =
        _root_.F2R (beta:=2) (Iplus p q) -
          _root_.F2R (beta:=2) (Iminus (Iplus p q) p) →
      _root_.F2R (beta:=2)
          (Iplus
            (Iminus p (Iminus (Iplus p q) (Iminus (Iplus p q) p)))
            (Iminus q (Iminus (Iplus p q) p))) =
        _root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q -
          _root_.F2R (beta:=2) (Iplus p q) := by
  have hprecision_ne : precision ≠ 0 := by omega
  have hvNum_gt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ (by decide) hprecision_ne
  exact MKnuth1_from_format_payload (beta:=2) bo 2 precision Iplus Iminus
    IplusCorrect IminusPlus rfl (by decide) hprecision_ne hvNum_gt hvNum

/-- Exact exported Coq `MKnuth3` contract. -/
theorem MKnuth3
    (bo : Fbound_skel) (precision : Nat)
    (hprecision : 1 < precision)
    (hvNum : bo.vNum = Zpower_nat 2 precision)
    (Iplus : FloatSpec.Core.Defs.FlocqFloat 2 →
      FloatSpec.Core.Defs.FlocqFloat 2 → FloatSpec.Core.Defs.FlocqFloat 2)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat 2,
        Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
        Closest (beta:=2) bo (2 : ℝ)
          (_root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q) (Iplus p q))
    (_IplusCompatible :
      ∀ p q r s : FloatSpec.Core.Defs.FlocqFloat 2,
        Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
        Fbounded (beta:=2) bo r → Fbounded (beta:=2) bo s →
        _root_.F2R (beta:=2) p = _root_.F2R (beta:=2) r →
        _root_.F2R (beta:=2) q = _root_.F2R (beta:=2) s →
        _root_.F2R (beta:=2) (Iplus p q) =
          _root_.F2R (beta:=2) (Iplus r s))
    (IplusSym : ∀ p q, Iplus p q = Iplus q p)
    (Iminus : FloatSpec.Core.Defs.FlocqFloat 2 →
      FloatSpec.Core.Defs.FlocqFloat 2 → FloatSpec.Core.Defs.FlocqFloat 2)
    (IminusPlus : ∀ p q, Iminus p q = Iplus p (Fopp (beta:=2) q)) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat 2,
      0 ≤ _root_.F2R (beta:=2) q →
      _root_.F2R (beta:=2) q ≤ 2 * -_root_.F2R (beta:=2) p →
      -_root_.F2R (beta:=2) p ≤ _root_.F2R (beta:=2) q →
      Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
      _root_.F2R (beta:=2)
          (Iplus
            (Iminus p (Iminus (Iplus p q) (Iminus (Iplus p q) p)))
            (Iminus q (Iminus (Iplus p q) p))) =
        _root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q -
          _root_.F2R (beta:=2) (Iplus p q) := by
  have hprecision_ne : precision ≠ 0 := by omega
  have hvNum_gt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ (by decide) hprecision_ne
  exact MKnuth3_from_format_payload (beta:=2) bo precision Iplus Iminus
    IplusCorrect IplusSym IminusPlus (by decide) rfl hprecision_ne hvNum hvNum_gt

/-- Exact exported Coq `MKnuth4` contract. -/
theorem MKnuth4
    (bo : Fbound_skel) (precision : Nat)
    (hprecision : 1 < precision)
    (hvNum : bo.vNum = Zpower_nat 2 precision)
    (Iplus : FloatSpec.Core.Defs.FlocqFloat 2 →
      FloatSpec.Core.Defs.FlocqFloat 2 → FloatSpec.Core.Defs.FlocqFloat 2)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat 2,
        Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
        Closest (beta:=2) bo (2 : ℝ)
          (_root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q) (Iplus p q))
    (_IplusCompatible :
      ∀ p q r s : FloatSpec.Core.Defs.FlocqFloat 2,
        Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
        Fbounded (beta:=2) bo r → Fbounded (beta:=2) bo s →
        _root_.F2R (beta:=2) p = _root_.F2R (beta:=2) r →
        _root_.F2R (beta:=2) q = _root_.F2R (beta:=2) s →
        _root_.F2R (beta:=2) (Iplus p q) =
          _root_.F2R (beta:=2) (Iplus r s))
    (Iminus : FloatSpec.Core.Defs.FlocqFloat 2 →
      FloatSpec.Core.Defs.FlocqFloat 2 → FloatSpec.Core.Defs.FlocqFloat 2)
    (IminusPlus : ∀ p q, Iminus p q = Iplus p (Fopp (beta:=2) q)) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat 2,
      0 < -_root_.F2R (beta:=2) p → 0 < _root_.F2R (beta:=2) q →
      2 * -_root_.F2R (beta:=2) p < _root_.F2R (beta:=2) q →
      Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
      _root_.F2R (beta:=2)
          (Iplus
            (Iminus p (Iminus (Iplus p q) (Iminus (Iplus p q) p)))
            (Iminus q (Iminus (Iplus p q) p))) =
        _root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q -
          _root_.F2R (beta:=2) (Iplus p q) := by
  have hprecision_ne : precision ≠ 0 := by omega
  have hvNum_gt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ (by decide) hprecision_ne
  have hTotal : TotalP (Closest (beta:=2) bo (2 : ℝ)) :=
    ClosestTotal (beta:=2) bo 2 precision ⟨rfl, by decide, hprecision, hvNum⟩
  exact MKnuth4_from_total_payload (beta:=2) bo precision Iplus Iminus
    IplusCorrect IminusPlus (by decide) rfl hprecision_ne hvNum hvNum_gt hTotal

/-- Exact exported Coq `MKnuth5` contract. -/
theorem MKnuth5
    (bo : Fbound_skel) (precision : Nat)
    (hprecision : 1 < precision)
    (hvNum : bo.vNum = Zpower_nat 2 precision)
    (Iplus : FloatSpec.Core.Defs.FlocqFloat 2 →
      FloatSpec.Core.Defs.FlocqFloat 2 → FloatSpec.Core.Defs.FlocqFloat 2)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat 2,
        Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
        Closest (beta:=2) bo (2 : ℝ)
          (_root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q) (Iplus p q))
    (_IplusCompatible :
      ∀ p q r s : FloatSpec.Core.Defs.FlocqFloat 2,
        Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
        Fbounded (beta:=2) bo r → Fbounded (beta:=2) bo s →
        _root_.F2R (beta:=2) p = _root_.F2R (beta:=2) r →
        _root_.F2R (beta:=2) q = _root_.F2R (beta:=2) s →
        _root_.F2R (beta:=2) (Iplus p q) =
          _root_.F2R (beta:=2) (Iplus r s))
    (Iminus : FloatSpec.Core.Defs.FlocqFloat 2 →
      FloatSpec.Core.Defs.FlocqFloat 2 → FloatSpec.Core.Defs.FlocqFloat 2)
    (IminusPlus : ∀ p q, Iminus p q = Iplus p (Fopp (beta:=2) q)) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat 2,
      0 < _root_.F2R (beta:=2) p →
      _root_.F2R (beta:=2) p < _root_.F2R (beta:=2) q →
      Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
      _root_.F2R (beta:=2)
          (Iplus
            (Iminus p (Iminus (Iplus p q) (Iminus (Iplus p q) p)))
            (Iminus q (Iminus (Iplus p q) p))) =
        _root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q -
          _root_.F2R (beta:=2) (Iplus p q) := by
  have hprecision_ne : precision ≠ 0 := by omega
  have hvNum_gt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ (by decide) hprecision_ne
  have hTotal : TotalP (Closest (beta:=2) bo (2 : ℝ)) :=
    ClosestTotal (beta:=2) bo 2 precision ⟨rfl, by decide, hprecision, hvNum⟩
  exact MKnuth5_from_total_payload (beta:=2) bo precision Iplus Iminus
    IplusCorrect IminusPlus (by decide) rfl hprecision hvNum hvNum_gt hTotal

/-- Exact exported Coq `MKnuth6` contract. -/
theorem MKnuth6
    (bo : Fbound_skel) (precision : Nat)
    (hprecision : 1 < precision)
    (hvNum : bo.vNum = Zpower_nat 2 precision)
    (Iplus : FloatSpec.Core.Defs.FlocqFloat 2 →
      FloatSpec.Core.Defs.FlocqFloat 2 → FloatSpec.Core.Defs.FlocqFloat 2)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat 2,
        Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
        Closest (beta:=2) bo (2 : ℝ)
          (_root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q) (Iplus p q))
    (_IplusCompatible :
      ∀ p q r s : FloatSpec.Core.Defs.FlocqFloat 2,
        Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
        Fbounded (beta:=2) bo r → Fbounded (beta:=2) bo s →
        _root_.F2R (beta:=2) p = _root_.F2R (beta:=2) r →
        _root_.F2R (beta:=2) q = _root_.F2R (beta:=2) s →
        _root_.F2R (beta:=2) (Iplus p q) =
          _root_.F2R (beta:=2) (Iplus r s))
    (IplusSym : ∀ p q, Iplus p q = Iplus q p)
    (Iminus : FloatSpec.Core.Defs.FlocqFloat 2 →
      FloatSpec.Core.Defs.FlocqFloat 2 → FloatSpec.Core.Defs.FlocqFloat 2)
    (IminusPlus : ∀ p q, Iminus p q = Iplus p (Fopp (beta:=2) q)) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat 2,
      _root_.F2R (beta:=2) (Iplus p q) =
        _root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q →
      Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
      _root_.F2R (beta:=2)
          (Iplus
            (Iminus p (Iminus (Iplus p q) (Iminus (Iplus p q) p)))
            (Iminus q (Iminus (Iplus p q) p))) =
        _root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q -
          _root_.F2R (beta:=2) (Iplus p q) := by
  have hprecision_ne : precision ≠ 0 := by omega
  have hvNum_gt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ (by decide) hprecision_ne
  exact MKnuth6_from_format_payload (beta:=2) bo 2 precision Iplus Iminus
    IplusCorrect IplusSym IminusPlus rfl (by decide) hprecision_ne hvNum_gt hvNum

/-- Exact exported Coq `MKnuth7` contract. -/
theorem MKnuth7
    (bo : Fbound_skel) (precision : Nat)
    (hprecision : 1 < precision)
    (hvNum : bo.vNum = Zpower_nat 2 precision)
    (Iplus : FloatSpec.Core.Defs.FlocqFloat 2 →
      FloatSpec.Core.Defs.FlocqFloat 2 → FloatSpec.Core.Defs.FlocqFloat 2)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat 2,
        Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
        Closest (beta:=2) bo (2 : ℝ)
          (_root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q) (Iplus p q))
    (_IplusCompatible :
      ∀ p q r s : FloatSpec.Core.Defs.FlocqFloat 2,
        Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
        Fbounded (beta:=2) bo r → Fbounded (beta:=2) bo s →
        _root_.F2R (beta:=2) p = _root_.F2R (beta:=2) r →
        _root_.F2R (beta:=2) q = _root_.F2R (beta:=2) s →
        _root_.F2R (beta:=2) (Iplus p q) =
          _root_.F2R (beta:=2) (Iplus r s))
    (IplusSym : ∀ p q, Iplus p q = Iplus q p)
    (Iminus : FloatSpec.Core.Defs.FlocqFloat 2 →
      FloatSpec.Core.Defs.FlocqFloat 2 → FloatSpec.Core.Defs.FlocqFloat 2)
    (IminusPlus : ∀ p q, Iminus p q = Iplus p (Fopp (beta:=2) q)) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat 2,
      |_root_.F2R (beta:=2) p| < _root_.F2R (beta:=2) q →
      Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
      _root_.F2R (beta:=2)
          (Iplus
            (Iminus p (Iminus (Iplus p q) (Iminus (Iplus p q) p)))
            (Iminus q (Iminus (Iplus p q) p))) =
        _root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q -
          _root_.F2R (beta:=2) (Iplus p q) := by
  have hprecision_ne : precision ≠ 0 := by omega
  have hvNum_gt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ (by decide) hprecision_ne
  have hTotal : TotalP (Closest (beta:=2) bo (2 : ℝ)) :=
    ClosestTotal (beta:=2) bo 2 precision ⟨rfl, by decide, hprecision, hvNum⟩
  exact MKnuth7_from_total_payload (beta:=2) bo precision Iplus Iminus
    IplusCorrect IplusSym IminusPlus (by decide) rfl hprecision hvNum hvNum_gt hTotal

/-- Exact exported Coq `Knuth` contract. -/
theorem Knuth
    (bo : Fbound_skel) (precision : Nat)
    (hprecision : 1 < precision)
    (hvNum : bo.vNum = Zpower_nat 2 precision)
    (Iplus : FloatSpec.Core.Defs.FlocqFloat 2 →
      FloatSpec.Core.Defs.FlocqFloat 2 → FloatSpec.Core.Defs.FlocqFloat 2)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat 2,
        Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
        Closest (beta:=2) bo (2 : ℝ)
          (_root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q) (Iplus p q))
    (_IplusCompatible :
      ∀ p q r s : FloatSpec.Core.Defs.FlocqFloat 2,
        Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
        Fbounded (beta:=2) bo r → Fbounded (beta:=2) bo s →
        _root_.F2R (beta:=2) p = _root_.F2R (beta:=2) r →
        _root_.F2R (beta:=2) q = _root_.F2R (beta:=2) s →
        _root_.F2R (beta:=2) (Iplus p q) =
          _root_.F2R (beta:=2) (Iplus r s))
    (IplusSym : ∀ p q, Iplus p q = Iplus q p)
    (IplusOp : ∀ p q,
      Fopp (beta:=2) (Iplus p q) =
        Iplus (Fopp (beta:=2) p) (Fopp (beta:=2) q))
    (Iminus : FloatSpec.Core.Defs.FlocqFloat 2 →
      FloatSpec.Core.Defs.FlocqFloat 2 → FloatSpec.Core.Defs.FlocqFloat 2)
    (IminusPlus : ∀ p q, Iminus p q = Iplus p (Fopp (beta:=2) q)) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat 2,
      Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
      _root_.F2R (beta:=2)
          (Iplus
            (Iminus p (Iminus (Iplus p q) (Iminus (Iplus p q) p)))
            (Iminus q (Iminus (Iplus p q) p))) =
        _root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q -
          _root_.F2R (beta:=2) (Iplus p q) := by
  have hprecision_ne : precision ≠ 0 := by omega
  have hvNum_gt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ (by decide) hprecision_ne
  have hTotal : TotalP (Closest (beta:=2) bo (2 : ℝ)) :=
    ClosestTotal (beta:=2) bo 2 precision ⟨rfl, by decide, hprecision, hvNum⟩
  exact Knuth_from_total_payload (beta:=2) bo precision Iplus Iminus
    IplusCorrect IplusSym IplusOp IminusPlus (by decide) rfl hprecision
    hvNum hvNum_gt hTotal

/-- Closed form of Coq `Dekker_FTS` after restoring `errorBoundedPlus`.

The earlier restored `Dekker_FTS` kept the global closest-plus error payload as
an explicit premise to avoid a forward reference.  Once `errorBoundedPlus` is
available, downstream wrappers can use this theorem instead: the only remaining
section side condition is the finite exponent box needed by `MinEx_from_finite_box_payload`/`MaxEx_from_finite_box_payload`.
-/
theorem Dekker_FTS_closed {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ) (precision : Nat)
    (Iplus Iminus :
      FloatSpec.Core.Defs.FlocqFloat beta →
        FloatSpec.Core.Defs.FlocqFloat beta →
          FloatSpec.Core.Defs.FlocqFloat beta)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
          (Iplus p q))
    (IplusOp :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Fopp (beta:=beta) (Iplus p q) =
          Iplus (Fopp (beta:=beta) p) (Fopp (beta:=beta) q))
    (IminusPlus :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
        Iminus p q = Iplus p (Fopp (beta:=beta) q))
    (hβ : 1 < beta)
    (hbetaRadix : (beta : ℝ) = radix)
    (hradix : radix = 2)
    (hprecision : precision ≠ 0)
    (hvNum : bo.vNum = Zpower_nat beta precision)
    (hvNum_gt : 1 < bo.vNum)
    (hBoundExpAll :
      ∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=beta) beta r).Fexp)
    (hTotal : TotalP (Closest (beta:=beta) bo radix)) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      |_root_.F2R (beta:=beta) q| ≤ |_root_.F2R (beta:=beta) p| →
      _root_.F2R (beta:=beta) (Iminus q (Iminus (Iplus p q) p)) =
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) (Iplus p q) := by
  have hError :
      ∀ p q pq : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo p →
        Fbounded (beta:=beta) bo q →
        Closest (beta:=beta) bo radix
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) pq →
        ∃ error : FloatSpec.Core.Defs.FlocqFloat beta,
          _root_.F2R (beta:=beta) error =
            _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
              _root_.F2R (beta:=beta) pq ∧
          Fbounded (beta:=beta) bo error ∧
          error.Fexp = min p.Fexp q.Fexp := by
    intro p q pq hp hq hclosest
    have hclosest' :
        Closest (beta:=beta) bo (beta : ℝ)
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) pq := by
      simpa [hbetaRadix] using hclosest
    exact errorBoundedPlus_from_format_payload (beta:=beta) bo beta precision rfl hβ hprecision
      hvNum_gt hvNum p q pq hp hq hclosest'
  intro p q hp hq hAbs
  have hFirstExact :
      _root_.F2R (beta:=beta) (Iminus (Iplus p q) p) =
        _root_.F2R (beta:=beta) (Iplus p q) -
          _root_.F2R (beta:=beta) p :=
    MDekker_closed (beta:=beta) bo radix precision Iplus Iminus IplusCorrect
      IplusOp IminusPlus hβ hbetaRadix hradix hprecision hvNum hvNum_gt
      hBoundExpAll hTotal p q hp hq hAbs
  exact MDekkerAux1_from_errorBoundedPlus (beta:=beta) bo radix Iplus Iminus
    IplusCorrect IminusPlus hError p q hp hq hFirstExact

/-- Coq: `ErrorBoundedIplus`, with only the binary Fast-section context. -/
theorem ErrorBoundedIplus
    (bo : Fbound_skel) (precision : Nat)
    (Iplus : FloatSpec.Core.Defs.FlocqFloat 2 →
      FloatSpec.Core.Defs.FlocqFloat 2 →
      FloatSpec.Core.Defs.FlocqFloat 2)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat 2,
        Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
        Closest (beta:=2) bo (2 : ℝ)
          (_root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q) (Iplus p q))
    (hprecision : 1 < precision)
    (hvNum : bo.vNum = Zpower_nat 2 precision) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat 2,
      Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
      ∃ error : FloatSpec.Core.Defs.FlocqFloat 2,
        _root_.F2R (beta:=2) error =
          _root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q -
            _root_.F2R (beta:=2) (Iplus p q) ∧
        Fbounded (beta:=2) bo error := by
  have hprecision_ne : precision ≠ 0 := by omega
  have hvNum_gt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ (by decide) hprecision_ne
  have hError :
      ∀ p q pq : FloatSpec.Core.Defs.FlocqFloat 2,
        Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
        Closest (beta:=2) bo (2 : ℝ)
          (_root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q) pq →
        ∃ error : FloatSpec.Core.Defs.FlocqFloat 2,
          _root_.F2R (beta:=2) error =
            _root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q -
              _root_.F2R (beta:=2) pq ∧
          Fbounded (beta:=2) bo error ∧
          error.Fexp = min p.Fexp q.Fexp :=
    errorBoundedPlus_from_format_payload (beta:=2) bo 2 precision rfl (by decide)
      hprecision_ne hvNum_gt hvNum
  exact ErrorBoundedIplus_from_errorBoundedPlus (beta:=2) bo (2 : ℝ)
    Iplus IplusCorrect hError

/-- Coq: `MDekkerAux1`, with the global error theorem derived internally. -/
theorem MDekkerAux1
    (bo : Fbound_skel) (precision : Nat)
    (Iplus Iminus : FloatSpec.Core.Defs.FlocqFloat 2 →
      FloatSpec.Core.Defs.FlocqFloat 2 →
      FloatSpec.Core.Defs.FlocqFloat 2)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat 2,
        Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
        Closest (beta:=2) bo (2 : ℝ)
          (_root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q) (Iplus p q))
    (IminusPlus : ∀ p q, Iminus p q = Iplus p (Fopp (beta:=2) q))
    (hprecision : 1 < precision)
    (hvNum : bo.vNum = Zpower_nat 2 precision) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat 2,
      _root_.F2R (beta:=2) (Iminus (Iplus p q) p) =
        _root_.F2R (beta:=2) (Iplus p q) - _root_.F2R (beta:=2) p →
      Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
      _root_.F2R (beta:=2) (Iminus q (Iminus (Iplus p q) p)) =
        _root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q -
          _root_.F2R (beta:=2) (Iplus p q) := by
  have hprecision_ne : precision ≠ 0 := by omega
  have hvNum_gt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ (by decide) hprecision_ne
  have hError :
      ∀ p q pq : FloatSpec.Core.Defs.FlocqFloat 2,
        Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
        Closest (beta:=2) bo (2 : ℝ)
          (_root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q) pq →
        ∃ error : FloatSpec.Core.Defs.FlocqFloat 2,
          _root_.F2R (beta:=2) error =
            _root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q -
              _root_.F2R (beta:=2) pq ∧
          Fbounded (beta:=2) bo error ∧
          error.Fexp = min p.Fexp q.Fexp :=
    errorBoundedPlus_from_format_payload (beta:=2) bo 2 precision rfl (by decide)
      hprecision_ne hvNum_gt hvNum
  intro p q hFirst hp hq
  exact MDekkerAux1_from_errorBoundedPlus (beta:=2) bo (2 : ℝ)
    Iplus Iminus IplusCorrect IminusPlus hError p q hp hq hFirst

/-- Coq: `Dekker1_FTS`, with closest totality derived from the section. -/
theorem Dekker1_FTS
    (bo : Fbound_skel) (precision : Nat)
    (Iplus Iminus : FloatSpec.Core.Defs.FlocqFloat 2 →
      FloatSpec.Core.Defs.FlocqFloat 2 →
      FloatSpec.Core.Defs.FlocqFloat 2)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat 2,
        Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
        Closest (beta:=2) bo (2 : ℝ)
          (_root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q) (Iplus p q))
    (IminusPlus : ∀ p q, Iminus p q = Iplus p (Fopp (beta:=2) q))
    (hprecision : 1 < precision)
    (hvNum : bo.vNum = Zpower_nat 2 precision) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat 2,
      0 ≤ _root_.F2R (beta:=2) q →
      _root_.F2R (beta:=2) q ≤ _root_.F2R (beta:=2) p →
      Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
      _root_.F2R (beta:=2) (Iminus (Iplus p q) p) =
        _root_.F2R (beta:=2) (Iplus p q) - _root_.F2R (beta:=2) p := by
  have hTotal : TotalP (Closest (beta:=2) bo (2 : ℝ)) :=
    ClosestTotal (beta:=2) bo 2 precision
      ⟨rfl, by decide, hprecision, hvNum⟩
  intro p q hq0 hqle hp hq
  exact Dekker1_FTS_from_total_payload (beta:=2) bo (2 : ℝ)
    Iplus Iminus IplusCorrect IminusPlus p q (by decide) rfl rfl hTotal
    hq0 hqle hp hq

/-- Coq: `Dekker3`; midpoint extrema and representability are internal. -/
theorem Dekker3
    (bo : Fbound_skel) (precision : Nat)
    (Iplus Iminus : FloatSpec.Core.Defs.FlocqFloat 2 →
      FloatSpec.Core.Defs.FlocqFloat 2 →
      FloatSpec.Core.Defs.FlocqFloat 2)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat 2,
        Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
        Closest (beta:=2) bo (2 : ℝ)
          (_root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q) (Iplus p q))
    (IminusPlus : ∀ p q, Iminus p q = Iplus p (Fopp (beta:=2) q))
    (hprecision : 1 < precision)
    (hvNum : bo.vNum = Zpower_nat 2 precision) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat 2,
      _root_.F2R (beta:=2) q ≤ 0 →
      2 * -_root_.F2R (beta:=2) q < _root_.F2R (beta:=2) p →
      Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
      _root_.F2R (beta:=2) (Iminus (Iplus p q) p) =
        _root_.F2R (beta:=2) (Iplus p q) - _root_.F2R (beta:=2) p := by
  have hprecision_ne : precision ≠ 0 := by omega
  have hvNum_gt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ (by decide) hprecision_ne
  have hTotal : TotalP (Closest (beta:=2) bo (2 : ℝ)) :=
    ClosestTotal (beta:=2) bo 2 precision
      ⟨rfl, by decide, hprecision, hvNum⟩
  intro p q hqle0 hmid hp hq
  have hp0 : 0 ≤ _root_.F2R (beta:=2) p := by nlinarith
  have hPminPayload :
      ∀ min : FloatSpec.Core.Defs.FlocqFloat 2,
        isMin (beta:=2) bo 2
          ((1 / 2 : ℝ) * _root_.F2R (beta:=2) p) min →
        ∃ d₂ : FloatSpec.Core.Defs.FlocqFloat 2,
          Fbounded (beta:=2) bo d₂ ∧
            _root_.F2R (beta:=2) d₂ =
              _root_.F2R (beta:=2) p - _root_.F2R (beta:=2) min := by
    intro min hMin
    have h := PminPos (beta:=2) bo 2 precision p min
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, PminPos_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, by decide, hprecision_ne, hvNum, hp0, hp, hMin⟩
  have hBoundExp :
      -bo.dExp ≤
        (boundR (beta:=2) 2
          ((1 / 2 : ℝ) * _root_.F2R (beta:=2) p)).Fexp := by
    exact le_trans (neg_nonpos.mpr bo.dExp_nonneg)
      (by simp [boundR, boundNat])
  exact Dekker3_from_midpoint_payload (beta:=2) bo (2 : ℝ) precision
    Iplus Iminus IplusCorrect IminusPlus p q (by decide) rfl rfl
    hprecision_ne hvNum hvNum_gt hBoundExp hPminPayload hTotal
    hqle0 hmid hp hq

/-- Coq: `MDekker`, with all midpoint and totality payloads derived. -/
theorem MDekker
    (bo : Fbound_skel) (precision : Nat)
    (Iplus Iminus : FloatSpec.Core.Defs.FlocqFloat 2 →
      FloatSpec.Core.Defs.FlocqFloat 2 →
      FloatSpec.Core.Defs.FlocqFloat 2)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat 2,
        Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
        Closest (beta:=2) bo (2 : ℝ)
          (_root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q) (Iplus p q))
    (IplusOp : ∀ p q,
      Fopp (beta:=2) (Iplus p q) =
        Iplus (Fopp (beta:=2) p) (Fopp (beta:=2) q))
    (IminusPlus : ∀ p q, Iminus p q = Iplus p (Fopp (beta:=2) q))
    (hprecision : 1 < precision)
    (hvNum : bo.vNum = Zpower_nat 2 precision) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat 2,
      Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
      |_root_.F2R (beta:=2) q| ≤ |_root_.F2R (beta:=2) p| →
      _root_.F2R (beta:=2) (Iminus (Iplus p q) p) =
        _root_.F2R (beta:=2) (Iplus p q) - _root_.F2R (beta:=2) p := by
  have hprecision_ne : precision ≠ 0 := by omega
  have hvNum_gt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ (by decide) hprecision_ne
  have hBoundExp :
      ∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=2) 2 r).Fexp := by
    intro r
    exact le_trans (neg_nonpos.mpr bo.dExp_nonneg)
      (by simp [boundR, boundNat])
  have hTotal : TotalP (Closest (beta:=2) bo (2 : ℝ)) :=
    ClosestTotal (beta:=2) bo 2 precision
      ⟨rfl, by decide, hprecision, hvNum⟩
  exact MDekker_closed (beta:=2) bo (2 : ℝ) precision Iplus Iminus
    IplusCorrect IplusOp IminusPlus (by decide) rfl rfl hprecision_ne
    hvNum hvNum_gt hBoundExp hTotal

/-- Coq: `Dekker_FTS`, composed only from the exact Fast-section lemmas. -/
theorem Dekker_FTS
    (bo : Fbound_skel) (precision : Nat)
    (Iplus Iminus : FloatSpec.Core.Defs.FlocqFloat 2 →
      FloatSpec.Core.Defs.FlocqFloat 2 →
      FloatSpec.Core.Defs.FlocqFloat 2)
    (IplusCorrect :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat 2,
        Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
        Closest (beta:=2) bo (2 : ℝ)
          (_root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q) (Iplus p q))
    (IplusOp : ∀ p q,
      Fopp (beta:=2) (Iplus p q) =
        Iplus (Fopp (beta:=2) p) (Fopp (beta:=2) q))
    (IminusPlus : ∀ p q, Iminus p q = Iplus p (Fopp (beta:=2) q))
    (hprecision : 1 < precision)
    (hvNum : bo.vNum = Zpower_nat 2 precision) :
    ∀ p q : FloatSpec.Core.Defs.FlocqFloat 2,
      Fbounded (beta:=2) bo p → Fbounded (beta:=2) bo q →
      |_root_.F2R (beta:=2) q| ≤ |_root_.F2R (beta:=2) p| →
      _root_.F2R (beta:=2) (Iminus q (Iminus (Iplus p q) p)) =
        _root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q -
          _root_.F2R (beta:=2) (Iplus p q) := by
  intro p q hp hq hAbs
  have hFirst := MDekker bo precision Iplus Iminus IplusCorrect IplusOp
    IminusPlus hprecision hvNum p q hp hq hAbs
  exact MDekkerAux1 bo precision Iplus Iminus IplusCorrect IminusPlus
    hprecision hvNum p q hFirst hp hq


/-- Coq: `FNoddSuc` — for bounded `p`, normalized-odd implies successor is normalized-even. -/
theorem FNoddSuc {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b p ∧ FNodd (beta:=beta) b radix precision p ∧
        1 < beta ∧ 1 < precision ∧ b.vNum = Zpower_nat beta precision⌝⦄
    (pure (FNoddSuc_check (beta:=beta) b radix precision p) : Id Unit)
    ⦃⇓_ => ⌜FNeven (beta:=beta) b radix precision
      (FNSucc (beta:=beta) b radix precision p)⌝⦄ := by
  intro h
  rcases h with ⟨hpBound, hodd, hbeta, hprecision, hvnum⟩
  have hprecision_ne : precision ≠ 0 := by omega
  have hnorm_can :
      Fcanonic (beta:=beta) beta b
        (Fnormalize (beta:=beta) beta b precision p) := by
    have hcan := FnormalizeCanonic (beta:=beta) beta b precision p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
      Id.run, ULift.up_down, Fbounded', Int.cast_ofNat] using
      hcan ⟨hpBound, hpBound, hprecision_ne, hbeta, hvnum⟩
  have hsucc_can :
      Fcanonic (beta:=beta) beta b
        (FNSucc (beta:=beta) b radix precision p) := by
    have hcan := FSuccCanonic (beta:=beta) b beta precision
      (Fnormalize (beta:=beta) beta b precision p)
    simpa [wp, PostCond.noThrow, pure, FSuccCanonic_check, FNSucc] using
      hcan ⟨hnorm_can, hbeta, hprecision_ne, hvnum⟩
  have hnorm_succ :
      Fnormalize (beta:=beta) beta b precision
          (FNSucc (beta:=beta) b radix precision p) =
        FNSucc (beta:=beta) b radix precision p := by
    have heq := FcanonicFnormalizeEq (beta:=beta) beta b precision
      (FNSucc (beta:=beta) b radix precision p)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      heq ⟨hsucc_can, rfl, hbeta, hprecision_ne, hvnum⟩
  simp only [wp, PostCond.noThrow, pure, FNoddSuc_check, FNeven, FNodd] at hodd ⊢
  rw [hnorm_succ]
  have hs := FoddSuc (beta:=beta) b beta precision
    (Fnormalize (beta:=beta) beta b precision p)
  simpa [wp, PostCond.noThrow, pure, FoddSuc_check, FNSucc]
    using hs ⟨hprecision, hvnum, hodd⟩

/-- Coq: `FNevenSuc` — for bounded `p`, normalized-even implies successor is normalized-odd. -/
theorem FNevenSuc {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : ℝ) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b p ∧ FNeven (beta:=beta) b radix precision p ∧
        1 < beta ∧ 1 < precision ∧ b.vNum = Zpower_nat beta precision⌝⦄
    (pure (FNevenSuc_check (beta:=beta) b radix precision p) : Id Unit)
    ⦃⇓_ => ⌜FNodd (beta:=beta) b radix precision
      (FNSucc (beta:=beta) b radix precision p)⌝⦄ := by
  intro h
  rcases h with ⟨hpBound, heven, hbeta, hprecision, hvnum⟩
  have hprecision_ne : precision ≠ 0 := by omega
  have hnorm_can :
      Fcanonic (beta:=beta) beta b
        (Fnormalize (beta:=beta) beta b precision p) := by
    have hcan := FnormalizeCanonic (beta:=beta) beta b precision p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
      Id.run, ULift.up_down, Fbounded', Int.cast_ofNat] using
      hcan ⟨hpBound, hpBound, hprecision_ne, hbeta, hvnum⟩
  have hsucc_can :
      Fcanonic (beta:=beta) beta b
        (FNSucc (beta:=beta) b radix precision p) := by
    have hcan := FSuccCanonic (beta:=beta) b beta precision
      (Fnormalize (beta:=beta) beta b precision p)
    simpa [wp, PostCond.noThrow, pure, FSuccCanonic_check, FNSucc] using
      hcan ⟨hnorm_can, hbeta, hprecision_ne, hvnum⟩
  have hnorm_succ :
      Fnormalize (beta:=beta) beta b precision
          (FNSucc (beta:=beta) b radix precision p) =
        FNSucc (beta:=beta) b radix precision p := by
    have heq := FcanonicFnormalizeEq (beta:=beta) beta b precision
      (FNSucc (beta:=beta) b radix precision p)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      heq ⟨hsucc_can, rfl, hbeta, hprecision_ne, hvnum⟩
  simp only [wp, PostCond.noThrow, pure, FNevenSuc_check, FNodd, FNeven] at heven ⊢
  rw [hnorm_succ]
  have hs := FevenSuc (beta:=beta) b beta precision
    (Fnormalize (beta:=beta) beta b precision p)
  simpa [wp, PostCond.noThrow, pure, FevenSuc_check, FNSucc]
    using hs ⟨hprecision, hvnum, heven⟩

/-- Coq: `EvenClosestUniqueP`.  Under the section assumptions, two
even-nearest results for one input denote the same real value. -/
theorem EvenClosestUniqueP {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < precision ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (EvenClosestUniqueP_check (beta:=beta) b (radix : ℝ) precision) :
      Id Unit)
    ⦃⇓_ => ⌜UniqueP
      (EvenClosest (beta:=beta) b (radix : ℝ) precision)⌝⦄ := by
  intro ⟨hbeta, hradix, hprecision, hvnum⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure, EvenClosestUniqueP_check,
    Id.run, ULift.up_down]
  have hMinUnique :
      ∀ (r : ℝ) (p q : FloatSpec.Core.Defs.FlocqFloat radix),
        isMin (beta:=radix) b radix r p →
        isMin (beta:=radix) b radix r q →
        _root_.F2R (beta:=radix) p = _root_.F2R (beta:=radix) q := by
    have h := MinUniqueP (beta:=radix) b radix
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinUniqueP_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
  have hMaxUnique :
      ∀ (r : ℝ) (p q : FloatSpec.Core.Defs.FlocqFloat radix),
        isMax (beta:=radix) b radix r p →
        isMax (beta:=radix) b radix r q →
        _root_.F2R (beta:=radix) p = _root_.F2R (beta:=radix) q := by
    have h := MaxUniqueP (beta:=radix) b radix
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MaxUniqueP_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
  have hMinOrMax :
      MinOrMaxP (beta:=radix) b
        (EvenClosest (beta:=radix) b (radix : ℝ) precision) := by
    have h := EvenClosestMinOrMax_from_split_radix_payload (beta:=radix) b radix
      (radix : ℝ) precision
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestMinOrMax_check, MinOrMaxP_float,
      Id.run, ULift.up_down, MinOrMaxP_float, Int.cast_ofNat] using h True.intro
  intro r p q hp hq
  rcases hp with ⟨hpClosest, hpEven | hpOnly⟩
  · rcases hq with ⟨hqClosest, hqEven | hqOnly⟩
    · rcases hMinOrMax r p ⟨hpClosest, Or.inl hpEven⟩ with hpMin | hpMax
      <;> rcases hMinOrMax r q ⟨hqClosest, Or.inl hqEven⟩ with hqMin | hqMax
      · exact hMinUnique r p q hpMin hqMin
      · by_contra hpq
        have hp_ne_r : _root_.F2R (beta:=radix) p ≠ r := by
          intro hpr
          have hq_le_p := hqMax.2.2 p hpMin.1 hpr.ge
          exact hpq (le_antisymm
            (le_trans hpMin.2.1 hqMax.2.1) hq_le_p)
        have hsMax : isMax (beta:=radix) b radix r
            (FNSucc (beta:=radix) b (radix : ℝ) precision p) := by
          have h := MinMax (beta:=radix) b radix precision p r
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinMax_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using
            h ⟨hpMin, hp_ne_r, rfl, hradix, by omega, hvnum⟩
        have hsEq := hMaxUnique r
          (FNSucc (beta:=radix) b (radix : ℝ) precision p) q hsMax hqMax
        have hsOdd : FNodd (beta:=radix) b (radix : ℝ) precision
            (FNSucc (beta:=radix) b (radix : ℝ) precision p) := by
          have h := FNevenSuc (beta:=radix) b (radix : ℝ) precision p
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FNevenSuc_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using
            h ⟨hpMin.1, hpEven, hradix, hprecision, hvnum⟩
        have hqOdd : FNodd (beta:=radix) b (radix : ℝ) precision q := by
          have h := FNoddEq (beta:=radix) b (radix : ℝ) precision
            (FNSucc (beta:=radix) b (radix : ℝ) precision p) q
            hradix hprecision hvnum
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FNoddEq_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hsMax.1, hqMax.1, hsEq, hsOdd⟩
        have hnotEven := FnOddNEven (beta:=radix) b (radix : ℝ)
          precision q hqOdd
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnOddNEven_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using hnotEven hqEven
      · by_contra hpq
        have hq_ne_r : _root_.F2R (beta:=radix) q ≠ r := by
          intro hqr
          have hp_le_q := hpMax.2.2 q hqMin.1 hqr.ge
          exact hpq (le_antisymm hp_le_q
            (le_trans hqMin.2.1 hpMax.2.1))
        have hsMax : isMax (beta:=radix) b radix r
            (FNSucc (beta:=radix) b (radix : ℝ) precision q) := by
          have h := MinMax (beta:=radix) b radix precision q r
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinMax_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using
            h ⟨hqMin, hq_ne_r, rfl, hradix, by omega, hvnum⟩
        have hsEq := hMaxUnique r
          (FNSucc (beta:=radix) b (radix : ℝ) precision q) p hsMax hpMax
        have hsOdd : FNodd (beta:=radix) b (radix : ℝ) precision
            (FNSucc (beta:=radix) b (radix : ℝ) precision q) := by
          have h := FNevenSuc (beta:=radix) b (radix : ℝ) precision q
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FNevenSuc_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using
            h ⟨hqMin.1, hqEven, hradix, hprecision, hvnum⟩
        have hpOdd : FNodd (beta:=radix) b (radix : ℝ) precision p := by
          have h := FNoddEq (beta:=radix) b (radix : ℝ) precision
            (FNSucc (beta:=radix) b (radix : ℝ) precision q) p
            hradix hprecision hvnum
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FNoddEq_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hsMax.1, hpMax.1, hsEq, hsOdd⟩
        have hnotEven := FnOddNEven (beta:=radix) b (radix : ℝ)
          precision p hpOdd
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnOddNEven_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using hnotEven hpEven
      · exact hMaxUnique r p q hpMax hqMax
    · exact hqOnly p hpClosest
  · rcases hq with ⟨hqClosest, _⟩
    exact (hpOnly q hqClosest).symm

/-- Coq: `EvenClosestMonotone2`.  The strict monotonicity and equal-input
uniqueness facts are derived from the preceding source theorems. -/
theorem EvenClosestMonotone2 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (p q : ℝ) (p' q' : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < precision ∧
        b.vNum = Zpower_nat radix precision ∧ p ≤ q ∧
        EvenClosest (beta:=beta) b (radix : ℝ) precision p p' ∧
        EvenClosest (beta:=beta) b (radix : ℝ) precision q q'⌝⦄
    (pure (EvenClosestMonotone2_check (beta:=beta) b (radix : ℝ)
      precision p q p' q') : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) p' ≤
      _root_.F2R (beta:=beta) q'⌝⦄ := by
  intro ⟨hbeta, hradix, hprecision, hvnum, hpq, hp, hq⟩
  have hMono := EvenClosestMonotone_from_split_radix_payload (beta:=beta) b (radix : ℝ)
    precision True.intro
  simp only [wp, PostCond.noThrow, pure, EvenClosestMonotone_check,
    Id.run, ULift.up_down] at hMono
  have hUnique := EvenClosestUniqueP (beta:=beta) b radix precision
    ⟨hbeta, hradix, hprecision, hvnum⟩
  simp only [wp, PostCond.noThrow, pure, EvenClosestUniqueP_check,
    Id.run, ULift.up_down] at hUnique
  simp only [wp, PostCond.noThrow, pure, EvenClosestMonotone2_check,
    Id.run, ULift.up_down]
  rcases lt_or_eq_of_le hpq with hpq | rfl
  · exact hMono p q p' q' hpq hp hq
  · exact le_of_eq (hUnique p p' q' hp hq)

/-- Coq: `EvenClosestTotal` — the even-nearest relation is total. -/
theorem EvenClosestTotal {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < precision ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (EvenClosestTotal_check (beta:=beta) b (radix : ℝ) precision 0) :
      Id Unit)
    ⦃⇓_ => ⌜TotalP
      (EvenClosest (beta:=beta) b (radix : ℝ) precision)⌝⦄ := by
  classical
  intro h
  rcases h with ⟨hbeta, hradix, hprecision_gt, hvnum⟩
  subst beta
  have hprecision_ne : precision ≠ 0 := by omega
  have hvnum_gt : 1 < b.vNum := by
    rw [hvnum, Zpower_nat]
    exact one_lt_pow₀ hradix (by omega)
  have hBoundExp :
      ∀ r : ℝ, -b.dExp ≤ (boundR (beta:=radix) radix r).Fexp := by
    intro r
    have hnonneg : 0 ≤ (boundR (beta:=radix) radix r).Fexp := by
      simp [boundR, boundNat]
    exact le_trans (neg_nonpos.mpr b.dExp_nonneg) hnonneg
  simp only [wp, PostCond.noThrow, pure, EvenClosestTotal_check, Id.run,
    ULift.up_down]
  intro r
  have hmin_ex := MinEx_from_finite_box_payload (beta:=radix) b radix r
  have hmin_ex' :
      ∃ min : FloatSpec.Core.Defs.FlocqFloat radix,
        isMin (beta:=radix) b radix r min := by
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using
      hmin_ex ⟨rfl, hradix, hvnum_gt, hBoundExp r⟩
  rcases hmin_ex' with ⟨min, hMin⟩
  have hmax_ex := MaxEx_from_finite_box_payload (beta:=radix) b radix r
  have hmax_ex' :
      ∃ max : FloatSpec.Core.Defs.FlocqFloat radix,
        isMax (beta:=radix) b radix r max := by
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MaxEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using
      hmax_ex ⟨rfl, hradix, hvnum_gt, hBoundExp r⟩
  rcases hmax_ex' with ⟨max, hMax⟩
  have hr_le_max : r ≤ _root_.F2R (beta:=radix) max := hMax.2.1
  have hClosestMinOrMax :
      MinOrMaxP_float (beta:=radix) b radix
        (Closest (beta:=radix) b (radix : ℝ)) := by
    have hcm := ClosestMinOrMax_from_split_radix_payload (beta:=radix) b radix (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMinOrMax_check, MinOrMaxP_float,
      Id.run, ULift.up_down, Int.cast_ofNat] using hcm trivial
  have hMinUnique :
      ∀ q : FloatSpec.Core.Defs.FlocqFloat radix,
        isMin (beta:=radix) b radix r q →
          _root_.F2R (beta:=radix) q =
            _root_.F2R (beta:=radix) min := by
    intro q hq
    have hm := MinEq (beta:=radix) b radix q min r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinEq_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using hm ⟨hq, hMin⟩
  have hMaxUnique :
      ∀ q : FloatSpec.Core.Defs.FlocqFloat radix,
        isMax (beta:=radix) b radix r q →
          _root_.F2R (beta:=radix) q =
            _root_.F2R (beta:=radix) max := by
    intro q hq
    have hm := MaxEq (beta:=radix) b radix q max r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MaxEq_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using hm ⟨hq, hMax⟩
  by_cases hdist_le :
      r - _root_.F2R (beta:=radix) min ≤
        _root_.F2R (beta:=radix) max - r
  · by_cases hdist_lt :
        r - _root_.F2R (beta:=radix) min <
          _root_.F2R (beta:=radix) max - r
    · have hmid_min :
          2 * r ≤ _root_.F2R (beta:=radix) min +
            _root_.F2R (beta:=radix) max := by
        linarith
      have hclosest_min :
          Closest (beta:=radix) b (radix : ℝ) r min := by
        have hc := ClosestMin (beta:=radix) b radix (radix : ℝ) r min max
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMin_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using hc ⟨hMin, hMax, hmid_min⟩
      refine ⟨min, hclosest_min, Or.inr ?_⟩
      intro q hq
      rcases hClosestMinOrMax r q hq with hqMin | hqMax
      · exact hMinUnique q hqMin
      · have hq_max := hMaxUnique q hqMax
        exfalso
        have hqdist := hq.2 min hMin.1
        have hmax_nonneg : 0 ≤ _root_.F2R (beta:=radix) max - r := by
          exact sub_nonneg.mpr hMax.2.1
        have hmin_nonpos : _root_.F2R (beta:=radix) min - r ≤ 0 := by
          exact sub_nonpos.mpr hMin.2.1
        rw [hq_max] at hqdist
        rw [abs_of_nonneg hmax_nonneg, abs_of_nonpos hmin_nonpos] at hqdist
        linarith
    · have hmid_min :
          2 * r ≤ _root_.F2R (beta:=radix) min +
            _root_.F2R (beta:=radix) max := by
        linarith
      have hmid_max :
          _root_.F2R (beta:=radix) min +
            _root_.F2R (beta:=radix) max ≤ 2 * r := by
        have hle_rev :
            _root_.F2R (beta:=radix) max - r ≤
              r - _root_.F2R (beta:=radix) min := le_of_not_gt hdist_lt
        linarith
      have hpar := FNevenOrFNodd (beta:=radix) b (radix : ℝ) precision min
      have hpar' :
          FNeven (beta:=radix) b (radix : ℝ) precision min ∨
            FNodd (beta:=radix) b (radix : ℝ) precision min := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FNevenOrFNodd_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using hpar trivial
      rcases hpar' with hmin_even | hmin_odd
      · have hclosest_min :
            Closest (beta:=radix) b (radix : ℝ) r min := by
          have hc := ClosestMin (beta:=radix) b radix (radix : ℝ) r min max
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMin_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using hc ⟨hMin, hMax, hmid_min⟩
        exact ⟨min, hclosest_min, Or.inl hmin_even⟩
      · by_cases hmin_eq_r : _root_.F2R (beta:=radix) min = r
        · have hmax_eq_r : _root_.F2R (beta:=radix) max = r := by
            apply le_antisymm
            · have hmax_le_min := hMax.2.2 min hMin.1 (by rw [hmin_eq_r])
              rw [hmin_eq_r] at hmax_le_min
              exact hmax_le_min
            · exact hr_le_max
          have hclosest_min :
              Closest (beta:=radix) b (radix : ℝ) r min := by
            have hc := ClosestMin (beta:=radix) b radix (radix : ℝ) r min max
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMin_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using hc ⟨hMin, hMax, hmid_min⟩
          refine ⟨min, hclosest_min, Or.inr ?_⟩
          intro q hq
          rcases hClosestMinOrMax r q hq with hqMin | hqMax
          · exact hMinUnique q hqMin
          · rw [hMaxUnique q hqMax, hmax_eq_r, ← hmin_eq_r]
        · let smin := FNSucc (beta:=radix) b (radix : ℝ) precision min
          have hMinMax := MinMax (beta:=radix) b radix precision min r
          have hsmax :
              isMax (beta:=radix) b radix r smin := by
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinMax_check, smin,
              Id.run, ULift.up_down, Int.cast_ofNat] using
              hMinMax ⟨hMin, hmin_eq_r, rfl, hradix, hprecision_ne, hvnum⟩
          have hsmax_eq_max :
              _root_.F2R (beta:=radix) smin =
                _root_.F2R (beta:=radix) max := by
            have hm := MaxEq (beta:=radix) b radix smin max r
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MaxEq_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using hm ⟨hsmax, hMax⟩
          have hmid_smax :
              _root_.F2R (beta:=radix) min +
                _root_.F2R (beta:=radix) smin ≤ 2 * r := by
            rw [hsmax_eq_max]
            exact hmid_max
          have hclosest_smin :
              Closest (beta:=radix) b (radix : ℝ) r smin := by
            have hc := ClosestMax (beta:=radix) b radix (radix : ℝ) r min smin
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMax_check,
              Id.run, ULift.up_down, smin, Int.cast_ofNat] using hc ⟨hMin, hsmax, hmid_smax⟩
          have hsmin_even :
              FNeven (beta:=radix) b (radix : ℝ) precision smin := by
            have hs := FNoddSuc (beta:=radix) b (radix : ℝ) precision min
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FNoddSuc_check, smin,
              Id.run, ULift.up_down, Int.cast_ofNat] using
              hs ⟨hMin.1, hmin_odd, hradix, hprecision_gt, hvnum⟩
          exact ⟨smin, hclosest_smin, Or.inl hsmin_even⟩
  · have hmid_max :
        _root_.F2R (beta:=radix) min +
          _root_.F2R (beta:=radix) max ≤ 2 * r := by
      have hlt :
          _root_.F2R (beta:=radix) max - r <
            r - _root_.F2R (beta:=radix) min := lt_of_not_ge hdist_le
      linarith
    have hclosest_max :
        Closest (beta:=radix) b (radix : ℝ) r max := by
      have hc := ClosestMax (beta:=radix) b radix (radix : ℝ) r min max
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMax_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using hc ⟨hMin, hMax, hmid_max⟩
    refine ⟨max, hclosest_max, Or.inr ?_⟩
    intro q hq
    rcases hClosestMinOrMax r q hq with hqMin | hqMax
    · have hq_min := hMinUnique q hqMin
      exfalso
      have hqdist := hq.2 max hMax.1
      have hmin_nonpos : _root_.F2R (beta:=radix) min - r ≤ 0 := by
        exact sub_nonpos.mpr hMin.2.1
      have hmax_nonneg : 0 ≤ _root_.F2R (beta:=radix) max - r := by
        exact sub_nonneg.mpr hMax.2.1
      rw [hq_min] at hqdist
      rw [abs_of_nonpos hmin_nonpos, abs_of_nonneg hmax_nonneg] at hqdist
      linarith
    · exact hMaxUnique q hqMax

/-- Coq `pGeUnderf` from the fixed binary-radix discriminant section. -/
theorem pGeUnderf
    (bnd : Fbound_skel) (precision : Nat)
    (b p : FloatSpec.Core.Defs.FlocqFloat 2) :
    ⦃⌜1 < precision ∧ bnd.vNum = Zpower_nat 2 precision ∧
        EvenClosest (beta:=2) bnd 2 precision
          (_root_.F2R b * _root_.F2R b) p ∧
        (_root_.F2R b = 0 ∨
          (2 : ℝ) ^ (-bnd.dExp + 3 * (precision : Int) - 1) ≤
            |_root_.F2R b * _root_.F2R b|) ∧
        _root_.F2R b ≠ 0⌝⦄
    (pure (pGeUnderf_check (beta:=2) bnd 2 precision b p) : Id Unit)
    ⦃⇓_ => ⌜(2 : ℝ) ^ (-bnd.dExp + 3 * (precision : Int) - 1) ≤
      |_root_.F2R p|⌝⦄ := by
  intro ⟨hprecision, hvNum, hRound, hUnderf, hbne⟩
  have hTotal :
      TotalP (EvenClosest (beta:=2) bnd 2 precision) :=
    EvenClosestTotal (beta:=2) bnd 2 precision
      ⟨rfl, by omega, hprecision, hvNum⟩
  exact pGeUnderf_from_total_payload (beta:=2) bnd 2 precision b p
    ⟨rfl, by omega, by omega, hvNum, hTotal, hRound, hUnderf, hbne⟩

/-- Coq `qGeUnderf` from the fixed binary-radix discriminant section. -/
theorem qGeUnderf
    (bnd : Fbound_skel) (precision : Nat)
    (a c q : FloatSpec.Core.Defs.FlocqFloat 2) :
    ⦃⌜1 < precision ∧ bnd.vNum = Zpower_nat 2 precision ∧
        EvenClosest (beta:=2) bnd 2 precision
          (_root_.F2R a * _root_.F2R c) q ∧
        (_root_.F2R a * _root_.F2R c = 0 ∨
          (2 : ℝ) ^ (-bnd.dExp + 3 * (precision : Int) - 1) ≤
            |_root_.F2R a * _root_.F2R c|) ∧
        _root_.F2R a * _root_.F2R c ≠ 0⌝⦄
    (pure (qGeUnderf_check (beta:=2) bnd 2 precision a c q) : Id Unit)
    ⦃⇓_ => ⌜(2 : ℝ) ^ (-bnd.dExp + 3 * (precision : Int) - 1) ≤
      |_root_.F2R q|⌝⦄ := by
  intro ⟨hprecision, hvNum, hRound, hUnderf, hacne⟩
  have hTotal :
      TotalP (EvenClosest (beta:=2) bnd 2 precision) :=
    EvenClosestTotal (beta:=2) bnd 2 precision
      ⟨rfl, by omega, hprecision, hvNum⟩
  exact qGeUnderf_from_total_payload (beta:=2) bnd 2 precision a c q
    ⟨rfl, by omega, by omega, hvNum, hTotal, hRound, hUnderf, hacne⟩

/-- Coq: `EvenClosestRoundedModeP`.

The four rounded-mode properties are derived from the section assumptions;
none is supplied by the caller. -/
theorem EvenClosestRoundedModeP {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < precision ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (EvenClosestRoundedModeP_check (beta:=beta) b (radix : ℝ) precision) :
      Id Unit)
    ⦃⇓_ => ⌜RoundedModeP (beta:=beta) b
      (EvenClosest (beta:=beta) b (radix : ℝ) precision)⌝⦄ := by
  intro ⟨hbeta, hradix, hprecision, hvnum⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure, EvenClosestRoundedModeP_check,
    Id.run, ULift.up_down]
  have hTotal :
      TotalP (EvenClosest (beta:=radix) b (radix : ℝ) precision) := by
    have h := EvenClosestTotal (beta:=radix) b radix precision
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestTotal_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hprecision, hvnum⟩
  have hCompatible :
      CompatibleP (beta:=radix) b
        (EvenClosest (beta:=radix) b (radix : ℝ) precision) := by
    intro r₁ r₂ p q hp hr hpq hqBound
    rcases hp with ⟨hpClosest, hpEven | hpOnly⟩
    · constructor
      · constructor
        · exact hqBound
        · intro g hg
          rw [← hpq, ← hr]
          exact hpClosest.2 g hg
      · left
        have h := FNevenEq (beta:=radix) b (radix : ℝ) precision p q
          hradix hprecision hvnum
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FNevenEq_check,
          ULift.down_up, Int.cast_ofNat] using h ⟨hpClosest.1, hqBound, hpq, hpEven⟩
    · constructor
      · constructor
        · exact hqBound
        · intro g hg
          rw [← hpq, ← hr]
          exact hpClosest.2 g hg
      · right
        intro q₀ hq₀
        have hq₀' : Closest (beta:=radix) b (radix : ℝ) r₁ q₀ := by
          simpa [hr] using hq₀
        exact (hpOnly q₀ hq₀').trans hpq
  have hMinOrMax :
      MinOrMaxP (beta:=radix) b
        (EvenClosest (beta:=radix) b (radix : ℝ) precision) := by
    have h := EvenClosestMinOrMax_from_split_radix_payload (beta:=radix) b radix
      (radix : ℝ) precision
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestMinOrMax_check, MinOrMaxP_float,
      Id.run, ULift.up_down, MinOrMaxP_float, Int.cast_ofNat] using h True.intro
  have hMonotone :
      MonotoneP (beta:=radix)
        (EvenClosest (beta:=radix) b (radix : ℝ) precision) := by
    have h := EvenClosestMonotone_from_split_radix_payload (beta:=radix) b (radix : ℝ) precision
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestMonotone_check,
      Id.run, ULift.up_down, MonotoneP, MonotoneP_float,
      MonotoneP_with, Int.cast_ofNat] using h True.intro
  exact ⟨hTotal, hCompatible, hMinOrMax, hMonotone⟩

noncomputable def CanonicFulp_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `CanonicFulp` — for a canonical float, `Fulp` is the radix power at
the float exponent. -/
theorem CanonicFulp {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fcanonic (beta:=beta) radix b p ∧ beta = radix ∧ 1 < radix ∧
        precision ≠ 0 ∧ b.vNum = Zpower_nat radix precision⌝⦄
    (pure (CanonicFulp_check (beta:=beta) radix b precision p) : Id Unit)
    ⦃⇓_ => ⌜Fulp (beta:=beta) b radix precision p = (radix : ℝ) ^ p.Fexp⌝⦄ := by
  intro h
  rcases h with ⟨hpCan, hbeta, hradix, hprecision, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, CanonicFulp_check,
    Id.run, ULift.up_down]
  have hnorm := FcanonicFnormalizeEq (beta:=beta) radix b precision p
  have hnorm_eq :
      Fnormalize (beta:=beta) radix b precision p = p := by
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      hnorm ⟨hpCan, hbeta, hradix, hprecision, hvNum⟩
  simp [Fulp, hnorm_eq]

noncomputable def Fulp_zero_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `Fulp_zero` — zero floats have the minimum-bound ulp. -/
theorem Fulp_zero {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜is_Fzero p⌝⦄
    (pure (Fulp_zero_check (beta:=beta) radix b precision p) : Id Unit)
    ⦃⇓_ => ⌜Fulp (beta:=beta) b radix precision p = (radix : ℝ) ^ (-b.dExp)⌝⦄ := by
  intro hp_zero
  simp only [wp, PostCond.noThrow, pure, Fulp_zero_check,
    Id.run, ULift.up_down]
  have hnum : p.Fnum = 0 := by simpa [is_Fzero] using hp_zero
  simp [Fulp, Fnormalize, hnum]

noncomputable def FulpComp_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FulpComp` — bounded floats with the same real value have the same
`Fulp`. -/
theorem FulpComp {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b p ∧ Fbounded (beta:=beta) b q ∧
        _root_.F2R (beta:=beta) p = _root_.F2R (beta:=beta) q ∧
        beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (FulpComp_check (beta:=beta) radix b precision p q) : Id Unit)
    ⦃⇓_ => ⌜Fulp (beta:=beta) b radix precision p =
        Fulp (beta:=beta) b radix precision q⌝⦄ := by
  intro h
  rcases h with ⟨hpBound, hqBound, hpq, hbeta, hradix, hprecision, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, FulpComp_check,
    Id.run, ULift.up_down]
  have hpNormCan :
      Fcanonic (beta:=beta) radix b
        (Fnormalize (beta:=beta) radix b precision p) := by
    have hcan := FnormalizeCanonic (beta:=beta) radix b precision p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
      Id.run, ULift.up_down, Fbounded', Int.cast_ofNat] using
      hcan ⟨hpBound, hpBound, hprecision, hradix, hvNum⟩
  have hqNormCan :
      Fcanonic (beta:=beta) radix b
        (Fnormalize (beta:=beta) radix b precision q) := by
    have hcan := FnormalizeCanonic (beta:=beta) radix b precision q
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
      Id.run, ULift.up_down, Fbounded', Int.cast_ofNat] using
      hcan ⟨hqBound, hqBound, hprecision, hradix, hvNum⟩
  have hpNormVal :
      _root_.F2R (beta:=beta)
          (Fnormalize (beta:=beta) radix b precision p) =
        _root_.F2R (beta:=beta) p := by
    have hcorr := FnormalizeCorrect (beta:=beta) radix b precision p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using hcorr ⟨hbeta, hradix⟩
  have hqNormVal :
      _root_.F2R (beta:=beta)
          (Fnormalize (beta:=beta) radix b precision q) =
        _root_.F2R (beta:=beta) q := by
    have hcorr := FnormalizeCorrect (beta:=beta) radix b precision q
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using hcorr ⟨hbeta, hradix⟩
  have hnormVal :
      _root_.F2R (beta:=beta)
          (Fnormalize (beta:=beta) radix b precision p) =
        _root_.F2R (beta:=beta)
          (Fnormalize (beta:=beta) radix b precision q) := by
    rw [hpNormVal, hqNormVal, hpq]
  have hbeta_gt : 1 < beta := by omega
  have hnormEq := FcanonicUnique (beta:=beta) radix b
    (Fnormalize (beta:=beta) radix b precision p)
    (Fnormalize (beta:=beta) radix b precision q) hbeta_gt hbeta.symm
  have hnormFloatEq :
      Fnormalize (beta:=beta) radix b precision p =
        Fnormalize (beta:=beta) radix b precision q := by
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicUnique_check,
      ULift.down_up, Fcanonic', Int.cast_ofNat] using
      hnormEq ⟨hpNormCan, hqNormCan, hnormVal⟩
  simp [Fulp, hnormFloatEq]

noncomputable def FulpGe_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FulpGe` — a bounded float's magnitude is controlled by the largest
mantissa and its normalized ulp. -/
theorem FulpGe {beta : Int} [ValidRadix beta]
    (radix : Int) [ValidRadix radix] (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b p ∧ beta = radix ∧ 1 < radix ∧
        precision ≠ 0 ∧ b.vNum = Zpower_nat radix precision⌝⦄
    (pure (FulpGe_check (beta:=beta) radix b precision p) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) p| ≤
        ((radix : ℝ) ^ (precision : Int) - 1) *
          Fulp (beta:=beta) b radix precision p⌝⦄ := by
  intro h
  rcases h with ⟨hpBound, hbeta, hradix, hprecision, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, FulpGe_check,
    Id.run, ULift.up_down]
  subst beta
  let nf : FloatSpec.Core.Defs.FlocqFloat radix :=
    Fnormalize (beta:=radix) radix b precision p
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (by omega : (0 : Int) < radix)
  have hcorr :
      _root_.F2R (beta:=radix) nf =
        _root_.F2R (beta:=radix) p := by
    have h := FnormalizeCorrect (beta:=radix) radix b precision p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
      Id.run, ULift.up_down, nf, Int.cast_ofNat] using h ⟨rfl, hradix⟩
  have hnfBound : Fbounded (beta:=radix) b nf := by
    have h := FnormalizeBounded (beta:=radix) radix b precision p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeBounded_check,
      Id.run, ULift.up_down, Fbounded', nf, Int.cast_ofNat] using
      h ⟨hpBound, hpBound, hprecision, hradix, hvNum⟩
  have hnum_lt : |nf.Fnum| < Zpower_nat radix precision := by
    simpa [hvNum] using hnfBound.1
  have hnum_le_pred : |nf.Fnum| ≤ Zpower_nat radix precision - 1 := by
    omega
  have hnum_le_real :
      ((|nf.Fnum| : Int) : ℝ) ≤
        (radix : ℝ) ^ (precision : Int) - 1 := by
    calc
      ((|nf.Fnum| : Int) : ℝ)
          ≤ ((Zpower_nat radix precision - 1 : Int) : ℝ) := by
              exact_mod_cast hnum_le_pred
      _ = (Zpower_nat radix precision : ℝ) - 1 := by norm_num
      _ = (radix : ℝ) ^ (precision : Int) - 1 := by
              simp [Zpower_nat, zpow_natCast, Int.cast_pow]
  have hscale_nonneg : 0 ≤ (radix : ℝ) ^ nf.Fexp :=
    le_of_lt (zpow_pos hradix_pos _)
  have habs_nf :
      |_root_.F2R (beta:=radix) nf| =
        ((|nf.Fnum| : Int) : ℝ) * (radix : ℝ) ^ nf.Fexp := by
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R]
    rw [abs_mul, abs_of_pos (zpow_pos hradix_pos nf.Fexp), Int.cast_abs]
  have hnf_abs_le :
      |_root_.F2R (beta:=radix) nf| ≤
        ((radix : ℝ) ^ (precision : Int) - 1) *
          (radix : ℝ) ^ nf.Fexp := by
    rw [habs_nf]
    exact mul_le_mul_of_nonneg_right hnum_le_real hscale_nonneg
  calc
    |_root_.F2R (beta:=radix) p|
        = |_root_.F2R (beta:=radix) nf| := by rw [hcorr]
    _ ≤ ((radix : ℝ) ^ (precision : Int) - 1) *
          (radix : ℝ) ^ nf.Fexp := hnf_abs_le
    _ = ((radix : ℝ) ^ (precision : Int) - 1) *
          Fulp (beta:=radix) b radix precision p := by
            simp [Fulp, nf]

noncomputable def LeFulpPos_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `LeFulpPos` — on nonnegative bounded floats, `Fulp` is monotone. -/
theorem LeFulpPos {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b x ∧ Fbounded (beta:=beta) b y ∧
        0 ≤ _root_.F2R (beta:=beta) x ∧
        _root_.F2R (beta:=beta) x ≤ _root_.F2R (beta:=beta) y ∧
        beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (LeFulpPos_check (beta:=beta) radix b precision x y) : Id Unit)
    ⦃⇓_ => ⌜Fulp (beta:=beta) b radix precision x ≤
        Fulp (beta:=beta) b radix precision y⌝⦄ := by
  intro h
  rcases h with
    ⟨hxBound, hyBound, hxNonneg, hxy, hbeta, hradix, hprecision, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, LeFulpPos_check,
    Id.run, ULift.up_down]
  have hnxCan :
      Fcanonic (beta:=beta) radix b
        (Fnormalize (beta:=beta) radix b precision x) := by
    have hcan := FnormalizeCanonic (beta:=beta) radix b precision x
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
      Id.run, ULift.up_down, Fbounded', Int.cast_ofNat] using
      hcan ⟨hxBound, hxBound, hprecision, hradix, hvNum⟩
  have hnyCan :
      Fcanonic (beta:=beta) radix b
        (Fnormalize (beta:=beta) radix b precision y) := by
    have hcan := FnormalizeCanonic (beta:=beta) radix b precision y
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
      Id.run, ULift.up_down, Fbounded', Int.cast_ofNat] using
      hcan ⟨hyBound, hyBound, hprecision, hradix, hvNum⟩
  have hnxVal :
      _root_.F2R (beta:=beta)
          (Fnormalize (beta:=beta) radix b precision x) =
        _root_.F2R (beta:=beta) x := by
    have hcorr := FnormalizeCorrect (beta:=beta) radix b precision x
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using hcorr ⟨hbeta, hradix⟩
  have hnyVal :
      _root_.F2R (beta:=beta)
          (Fnormalize (beta:=beta) radix b precision y) =
        _root_.F2R (beta:=beta) y := by
    have hcorr := FnormalizeCorrect (beta:=beta) radix b precision y
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using hcorr ⟨hbeta, hradix⟩
  have hyNonneg : 0 ≤ _root_.F2R (beta:=beta) y := le_trans hxNonneg hxy
  have hAbsLe :
      |_root_.F2R (beta:=beta)
          (Fnormalize (beta:=beta) radix b precision x)| ≤
        |_root_.F2R (beta:=beta)
          (Fnormalize (beta:=beta) radix b precision y)| := by
    rw [hnxVal, hnyVal, abs_of_nonneg hxNonneg, abs_of_nonneg hyNonneg]
    exact hxy
  have hbeta_gt : 1 < beta := by omega
  have hExpLe :
      (Fnormalize (beta:=beta) radix b precision x).Fexp ≤
        (Fnormalize (beta:=beta) radix b precision y).Fexp := by
    have hcanon := Fcanonic_Rle_Zle (beta:=beta) radix b
      (Fnormalize (beta:=beta) radix b precision x)
      (Fnormalize (beta:=beta) radix b precision y) hbeta_gt hbeta.symm
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fcanonic_Rle_Zle_check,
      ULift.down_up, Fcanonic', Int.cast_ofNat] using hcanon ⟨hnxCan, hnyCan, hAbsLe⟩
  have hbase_ge_one : (1 : ℝ) ≤ (radix : ℝ) := by
    exact_mod_cast (le_of_lt hradix)
  simpa [Fulp] using zpow_le_zpow_right₀ hbase_ge_one hExpLe

noncomputable def FulpFPredGePos_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (f : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FulpFPredGePos` — the predecessor of a positive canonical float has
ulp no larger than the float itself. -/
theorem FulpFPredGePos {beta : Int} [ValidRadix beta]
    (radix : Int) [ValidRadix radix] (b : Fbound_skel) (precision : Nat)
    (f : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b f ∧
        Fcanonic (beta:=beta) radix b f ∧
        0 < _root_.F2R (beta:=beta) f ∧
        beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (FulpFPredGePos_check (beta:=beta) radix b precision f) : Id Unit)
    ⦃⇓_ => ⌜Fulp (beta:=beta) b radix precision
          (FPred (beta:=beta) b radix precision f) ≤
        Fulp (beta:=beta) b radix precision f⌝⦄ := by
  intro h
  rcases h with ⟨hfBound, _hfCan, hfPos, hbeta, hradix, hprecision, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, FulpFPredGePos_check,
    Id.run, ULift.up_down]
  have hpredBound :
      Fbounded (beta:=beta) b
        (FPred (beta:=beta) b radix precision f) := by
    have h := FBoundedPred (beta:=beta) b radix precision f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FBoundedPred_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hfBound, hradix, hprecision, hvNum⟩
  have hpredNonneg :
      0 ≤ _root_.F2R (beta:=beta)
        (FPred (beta:=beta) b radix precision f) := by
    have h := R0RltRlePred (beta:=beta) b radix precision f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, R0RltRlePred_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hbeta, hradix, hprecision, hvNum, hfPos⟩
  have hpredLe :
      _root_.F2R (beta:=beta)
          (FPred (beta:=beta) b radix precision f) ≤
        _root_.F2R (beta:=beta) f := by
    have h := FPredLt (beta:=beta) b radix precision f
    exact le_of_lt
      (by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FPredLt_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          h ⟨hbeta, hradix, hprecision, hvNum⟩)
  have hmono := LeFulpPos (beta:=beta) radix b precision
    (FPred (beta:=beta) b radix precision f) f
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, LeFulpPos_check,
    Id.run, ULift.up_down, Int.cast_ofNat] using
    hmono ⟨hpredBound, hfBound, hpredNonneg, hpredLe, hbeta, hradix,
      hprecision, hvNum⟩

noncomputable def FulpFPredLe_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (f : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FulpFPredLe` — an ulp is at most one radix multiple of the
predecessor ulp. -/
theorem FulpFPredLe {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (f : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b f ∧
        Fcanonic (beta:=beta) radix b f ∧
        beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (FulpFPredLe_check (beta:=beta) radix b precision f) : Id Unit)
    ⦃⇓_ => ⌜Fulp (beta:=beta) b radix precision f ≤
      (radix : ℝ) *
        Fulp (beta:=beta) b radix precision
          (FPred (beta:=beta) b radix precision f)⌝⦄ := by
  intro h
  rcases h with ⟨_hfBound, hfCan, hbeta, hradix, hprecision, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, FulpFPredLe_check,
    Id.run, ULift.up_down]
  have hfUlp :
      Fulp (beta:=beta) b radix precision f = (radix : ℝ) ^ f.Fexp := by
    have h := CanonicFulp (beta:=beta) radix b precision f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hfCan, hbeta, hradix, hprecision, hvNum⟩
  have hpredCan :
      Fcanonic (beta:=beta) radix b
        (FPred (beta:=beta) b radix precision f) := by
    have h := FPredCanonic (beta:=beta) b radix precision f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FPredCanonic_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hfCan, hradix, hprecision, hvNum⟩
  have hpredUlp :
      Fulp (beta:=beta) b radix precision
          (FPred (beta:=beta) b radix precision f) =
        (radix : ℝ) ^ (FPred (beta:=beta) b radix precision f).Fexp := by
    have h := CanonicFulp (beta:=beta) radix b precision
      (FPred (beta:=beta) b radix precision f)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hpredCan, hbeta, hradix, hprecision, hvNum⟩
  rw [hfUlp, hpredUlp]
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (by omega : (0 : Int) < radix)
  have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
  have hbase_ge_one : (1 : ℝ) ≤ (radix : ℝ) := by
    exact_mod_cast (le_of_lt hradix)
  have hExpLe :
      f.Fexp ≤ (FPred (beta:=beta) b radix precision f).Fexp + 1 := by
    unfold FPred
    by_cases htop : f.Fnum = -pPred b.vNum
    · simp [htop]
      omega
    · simp [htop]
      by_cases hnorm : f.Fnum = nNormMin radix precision
      · simp [hnorm]
        by_cases hexp : f.Fexp = -b.dExp
        · simp [hexp]
        · simp [hexp]
      · simp [hnorm]
  calc
    (radix : ℝ) ^ f.Fexp
        ≤ (radix : ℝ) ^
            ((FPred (beta:=beta) b radix precision f).Fexp + 1) := by
          exact zpow_le_zpow_right₀ hbase_ge_one hExpLe
    _ = (radix : ℝ) *
          (radix : ℝ) ^
            (FPred (beta:=beta) b radix precision f).Fexp := by
          rw [zpow_add₀ hradix_ne, zpow_one]
          ring

noncomputable def FSuccDiffPos_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FSuccDiffPos` — for nonnegative floats, the successor step has
real value exactly one ulp at the current exponent. -/
theorem FSuccDiffPos {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision ∧
        0 ≤ _root_.F2R (beta:=beta) x⌝⦄
    (pure (FSuccDiffPos_check (beta:=beta) radix b precision x) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta)
          (FloatSpec.Calc.Operations.Fminus (beta:=beta)
            (FSucc (beta:=beta) b radix precision x) x) =
        (radix : ℝ) ^ x.Fexp⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hradix, hprecision, hvNum, hxNonneg⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure, FSuccDiffPos_check,
    Id.run, ULift.up_down]
  have hxnum_nonneg : 0 ≤ x.Fnum :=
    FloatSpec.Core.Float_prop.Fnum_ge_0 (beta:=radix) x hradix hxNonneg
  have hminus :
      _root_.F2R
          (FloatSpec.Calc.Operations.Fminus (beta:=radix)
            (FSucc (beta:=radix) b radix precision x) x) =
        _root_.F2R (beta:=radix)
          (FSucc (beta:=radix) b radix precision x) -
        _root_.F2R (beta:=radix) x := by
    have h := Fminus_correct (beta:=radix)
      (FSucc (beta:=radix) b radix precision x) x
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hradix
  rw [hminus]
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (by omega : (0 : Int) < radix)
  have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
  have hradix_pos_int : 0 < radix := by omega
  have hnorm_mul :
      (nNormMin radix precision : ℝ) * (radix : ℝ) = (b.vNum : ℝ) := by
    have hsucc : precision - 1 + 1 = precision := Nat.sub_one_add_one hprecision
    have hpow_succ : radix ^ precision = radix ^ (precision - 1) * radix := by
      rw [← hsucc]
      exact pow_succ radix (precision - 1)
    rw [hvNum, Zpower_nat, nNormMin, hpow_succ]
    norm_num [Int.cast_mul]
  unfold FSucc
  by_cases htop : x.Fnum = pPred b.vNum
  · simp only [htop, ↓reduceIte, _root_.F2R, FloatSpec.Core.Defs.F2R,
      FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp]
    have hpow :
        (radix : ℝ) ^ (x.Fexp + 1) =
          (radix : ℝ) ^ x.Fexp * (radix : ℝ) := by
      rw [zpow_add₀ hradix_ne, zpow_one]
    have hpPred : (pPred b.vNum : ℝ) = (b.vNum : ℝ) - 1 := by
      simp [pPred]
    rw [hpow, hpPred]
    calc
      (nNormMin radix precision : ℝ) *
            ((radix : ℝ) ^ x.Fexp * (radix : ℝ)) -
          ((b.vNum : ℝ) - 1) * (radix : ℝ) ^ x.Fexp
          =
            ((nNormMin radix precision : ℝ) * (radix : ℝ) -
                ((b.vNum : ℝ) - 1)) *
              (radix : ℝ) ^ x.Fexp := by ring
      _ = ((b.vNum : ℝ) - ((b.vNum : ℝ) - 1)) *
            (radix : ℝ) ^ x.Fexp := by rw [hnorm_mul]
      _ = (radix : ℝ) ^ x.Fexp := by ring
  · simp only [htop, ↓reduceIte]
    by_cases hlow : x.Fnum = -nNormMin radix precision
    · exfalso
      have hnm_pos : 0 < nNormMin radix precision := by
        unfold nNormMin
        exact pow_pos hradix_pos_int _
      omega
    · simp only [hlow, ↓reduceIte, _root_.F2R, FloatSpec.Core.Defs.F2R,
        FloatSpec.Core.Defs.FlocqFloat.Fnum,
        FloatSpec.Core.Defs.FlocqFloat.Fexp]
      norm_num [Int.cast_add]
      ring

noncomputable def FSuccUlpPos_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FSuccUlpPos` — for canonical nonnegative floats, the successor
step is exactly one normalized ulp. -/
theorem FSuccUlpPos {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fcanonic (beta:=beta) radix b x ∧
        0 ≤ _root_.F2R (beta:=beta) x ∧
        beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (FSuccUlpPos_check (beta:=beta) radix b precision x) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta)
          (FloatSpec.Calc.Operations.Fminus (beta:=beta)
            (FSucc (beta:=beta) b radix precision x) x) =
        Fulp (beta:=beta) b radix precision x⌝⦄ := by
  intro h
  rcases h with ⟨hxCan, hxNonneg, hbeta, hradix, hprecision, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, FSuccUlpPos_check,
    Id.run, ULift.up_down]
  have hdiff := FSuccDiffPos (beta:=beta) radix b precision x
  have hdiff' :
      _root_.F2R (beta:=beta)
          (FloatSpec.Calc.Operations.Fminus (beta:=beta)
            (FSucc (beta:=beta) b radix precision x) x) =
        (radix : ℝ) ^ x.Fexp := by
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FSuccDiffPos_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      hdiff ⟨hbeta, hradix, hprecision, hvNum, hxNonneg⟩
  have hfulp := CanonicFulp (beta:=beta) radix b precision x
  have hfulp' :
      Fulp (beta:=beta) b radix precision x = (radix : ℝ) ^ x.Fexp := by
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      hfulp ⟨hxCan, hbeta, hradix, hprecision, hvNum⟩
  exact hdiff'.trans hfulp'.symm

noncomputable def FpredUlpPos_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FpredUlpPos` — a positive canonical float is the predecessor plus
the predecessor ulp. -/
theorem FpredUlpPos {beta : Int} [ValidRadix beta]
    (radix : Int) [ValidRadix radix] (b : Fbound_skel) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fcanonic (beta:=beta) radix b x ∧
        0 < _root_.F2R (beta:=beta) x ∧
        beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (FpredUlpPos_check (beta:=beta) radix b precision x) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta)
          (FPred (beta:=beta) b radix precision x) +
        Fulp (beta:=beta) b radix precision
          (FPred (beta:=beta) b radix precision x) =
        _root_.F2R (beta:=beta) x⌝⦄ := by
  intro h
  rcases h with ⟨hxCan, hxPos, hbeta, hradix, hprecision, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, FpredUlpPos_check,
    Id.run, ULift.up_down]
  let pred := FPred (beta:=beta) b radix precision x
  have hpredCan : Fcanonic (beta:=beta) radix b pred := by
    have h := FPredCanonic (beta:=beta) b radix precision x
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FPredCanonic_check,
      Id.run, ULift.up_down, pred, Int.cast_ofNat] using
      h ⟨hxCan, hradix, hprecision, hvNum⟩
  have hpredNonneg : 0 ≤ _root_.F2R (beta:=beta) pred := by
    have h := R0RltRlePred (beta:=beta) b radix precision x
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, R0RltRlePred_check,
      Id.run, ULift.up_down, pred, Int.cast_ofNat] using
      h ⟨hbeta, hradix, hprecision, hvNum, hxPos⟩
  have hsucc_eq :
      FSucc (beta:=beta) b radix precision pred = x := by
    have h := FSucPred (beta:=beta) b radix precision x
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FSucPred_check,
      Id.run, ULift.up_down, pred, Int.cast_ofNat] using
      h ⟨hxCan, hradix, hprecision, hvNum⟩
  have hsucc_ulp :
      _root_.F2R (beta:=beta)
          (FloatSpec.Calc.Operations.Fminus (beta:=beta)
            (FSucc (beta:=beta) b radix precision pred) pred) =
        Fulp (beta:=beta) b radix precision pred := by
    have h := FSuccUlpPos (beta:=beta) radix b precision pred
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FSuccUlpPos_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hpredCan, hpredNonneg, hbeta, hradix, hprecision, hvNum⟩
  have hminus :
      _root_.F2R (beta:=beta)
          (FloatSpec.Calc.Operations.Fminus (beta:=beta)
            (FSucc (beta:=beta) b radix precision pred) pred) =
        _root_.F2R (beta:=beta)
            (FSucc (beta:=beta) b radix precision pred) -
          _root_.F2R (beta:=beta) pred := by
    have h := Fminus_correct (beta:=beta)
      (FSucc (beta:=beta) b radix precision pred) pred
    have hbeta_gt : 1 < beta := by omega
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hbeta_gt
  have hstep :
      _root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) pred =
        Fulp (beta:=beta) b radix precision pred := by
    rw [hminus] at hsucc_ulp
    rw [hsucc_eq] at hsucc_ulp
    exact hsucc_ulp
  calc
    _root_.F2R (beta:=beta) pred +
        Fulp (beta:=beta) b radix precision pred
        = _root_.F2R (beta:=beta) pred +
            (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) pred) := by
          rw [hstep]
    _ = _root_.F2R (beta:=beta) x := by ring

noncomputable def ImplyClosest_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (z : ℝ) (f : FloatSpec.Core.Defs.FlocqFloat beta) (e : Int) : Unit :=
  ()

/-- Coq: `ImplyClosest` — the local binade and half-ulp hypotheses imply
closestness. -/
theorem ImplyClosest {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (z : ℝ) (f : FloatSpec.Core.Defs.FlocqFloat beta) (e : Int) :
    ⦃⌜Fbounded (beta:=beta) b f ∧
        Fcanonic (beta:=beta) radix b f ∧
        (radix : ℝ) ^ (e + (precision : Int) - 1) ≤ z ∧
        z ≤ (radix : ℝ) ^ (e + (precision : Int)) ∧
        (radix : ℝ) ^ (e + (precision : Int) - 1) ≤
          _root_.F2R (beta:=beta) f ∧
        -b.dExp ≤ e ∧
        beta = radix ∧ 1 < radix ∧ 1 < precision ∧
        b.vNum = Zpower_nat radix precision ∧
        |z - _root_.F2R (beta:=beta) f| ≤ (radix : ℝ) ^ e / 2⌝⦄
    (pure (ImplyClosest_check (beta:=beta) b radix precision z f e) : Id Unit)
    ⦃⇓_ => ⌜Closest (beta:=beta) b (radix : ℝ) z f⌝⦄ := by
  intro h
  rcases h with ⟨_hfBound, hfCan, hzGe, _hzLe, hfGe, heGe, hbeta, hradix,
    hprecision_gt, hvNum, hdist⟩
  simp only [wp, PostCond.noThrow, pure, ImplyClosest_check, Id.run]
  have hprecision : precision ≠ 0 := by omega
  have hprecision_pos : 0 < precision := by omega
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (by omega : (0 : Int) < radix)
  have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
  have hbase_ge_one : (1 : ℝ) ≤ (radix : ℝ) := by
    exact_mod_cast (le_of_lt hradix)
  have hpowe_pos : 0 < (radix : ℝ) ^ e := zpow_pos hradix_pos e
  let lower : FloatSpec.Core.Defs.FlocqFloat beta :=
    ⟨nNormMin radix precision, e⟩
  have hlowerCan : Fcanonic (beta:=beta) radix b lower := by
    have h := FcanonicNnormMin (beta:=beta) radix b precision e
    simpa only [lower, Int.cast_ofNat] using h hradix hprecision_pos hvNum heGe
  have hlowerVal :
      _root_.F2R (beta:=beta) lower =
        (radix : ℝ) ^ (e + (precision : Int) - 1) := by
    dsimp [lower, _root_.F2R, FloatSpec.Core.Defs.F2R, nNormMin]
    have hcast_beta : (beta : ℝ) = (radix : ℝ) := by
      exact_mod_cast hbeta
    rw [hcast_beta]
    have hpow_nat :
        ((radix ^ (precision - 1) : Int) : ℝ) =
          (radix : ℝ) ^ ((precision - 1 : Nat) : Int) := by
      simp [Int.cast_pow]
    rw [hpow_nat]
    have hprec_cast :
        ((precision - 1 : Nat) : Int) = (precision : Int) - 1 := by
      omega
    rw [hprec_cast]
    calc
      (radix : ℝ) ^ ((precision : Int) - 1) * (radix : ℝ) ^ e
          = (radix : ℝ) ^ (((precision : Int) - 1) + e) := by
              rw [zpow_add₀ hradix_ne]
      _ = (radix : ℝ) ^ (e + (precision : Int) - 1) := by
              congr 1
              ring
  have hlowerNonneg : 0 ≤ _root_.F2R (beta:=beta) lower := by
    rw [hlowerVal]
    exact le_of_lt (zpow_pos hradix_pos _)
  have hfGe' :
      _root_.F2R (beta:=beta) lower ≤ _root_.F2R (beta:=beta) f := by
    rw [hlowerVal]
    exact hfGe
  have hfNonneg : 0 ≤ _root_.F2R (beta:=beta) f := by
    exact le_trans hlowerNonneg hfGe'
  have hfPos : 0 < _root_.F2R (beta:=beta) f :=
    lt_of_lt_of_le
      (by
        rw [hlowerVal]
        exact zpow_pos hradix_pos (e + (precision : Int) - 1))
      hfGe'
  have he_le_fexp : e ≤ f.Fexp := by
    have habs :
        |_root_.F2R (beta:=beta) lower| ≤
          |_root_.F2R (beta:=beta) f| := by
      rw [abs_of_nonneg hlowerNonneg, abs_of_nonneg hfNonneg]
      exact hfGe'
    have hbeta_gt : 1 < beta := by omega
    have h := Fcanonic_Rle_Zle (beta:=beta) radix b lower f
      hbeta_gt hbeta.symm
    have hle : lower.Fexp ≤ f.Fexp := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fcanonic_Rle_Zle_check,
        Id.run, ULift.up_down, Fcanonic', lower, Int.cast_ofNat] using
        h ⟨hlowerCan, hfCan, habs⟩
    simpa [lower] using hle
  let succ : FloatSpec.Core.Defs.FlocqFloat beta :=
    FSucc (beta:=beta) b radix precision f
  let pred : FloatSpec.Core.Defs.FlocqFloat beta :=
    FPred (beta:=beta) b radix precision f
  have hsucc_gap :
      _root_.F2R (beta:=beta) succ - _root_.F2R (beta:=beta) f =
        (radix : ℝ) ^ f.Fexp := by
    have hdiff := FSuccDiffPos (beta:=beta) radix b precision f
    have hdiff' :
        _root_.F2R (beta:=beta)
            (FloatSpec.Calc.Operations.Fminus (beta:=beta) succ f) =
          (radix : ℝ) ^ f.Fexp := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FSuccDiffPos_check,
        Id.run, ULift.up_down, succ, Int.cast_ofNat] using
        hdiff ⟨hbeta, hradix, hprecision, hvNum, hfNonneg⟩
    have hminus := Fminus_correct (beta:=beta) succ f
    have hminus' :
        _root_.F2R (beta:=beta)
            (FloatSpec.Calc.Operations.Fminus (beta:=beta) succ f) =
          _root_.F2R (beta:=beta) succ - _root_.F2R (beta:=beta) f := by
      have hbeta_gt : 1 < beta := by omega
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus,
        Id.run, ULift.up_down, succ, Int.cast_ofNat] using hminus hbeta_gt
    exact hminus'.symm.trans hdiff'
  have hpowe_le_succ_gap :
      (radix : ℝ) ^ e ≤
        _root_.F2R (beta:=beta) succ - _root_.F2R (beta:=beta) f := by
    rw [hsucc_gap]
    exact zpow_le_zpow_right₀ hbase_ge_one he_le_fexp
  have hdistSucc :
      |z - _root_.F2R (beta:=beta) f| ≤
        |z - _root_.F2R (beta:=beta) succ| := by
    have hz_minus_f_le :
        z - _root_.F2R (beta:=beta) f ≤ (radix : ℝ) ^ e / 2 := by
      exact le_trans (le_abs_self _) hdist
    have hhalf_le_succ_minus_z :
        (radix : ℝ) ^ e / 2 ≤ _root_.F2R (beta:=beta) succ - z := by
      nlinarith [hpowe_le_succ_gap, hz_minus_f_le]
    have hz_le_succ : z ≤ _root_.F2R (beta:=beta) succ := by
      nlinarith [hpowe_pos, hhalf_le_succ_minus_z]
    rw [abs_of_nonpos (sub_nonpos.mpr hz_le_succ)]
    nlinarith [hdist, hhalf_le_succ_minus_z]
  have hpred_lt_f :
      _root_.F2R (beta:=beta) pred < _root_.F2R (beta:=beta) f := by
    have h := FPredLt (beta:=beta) b radix precision f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FPredLt_check,
      Id.run, ULift.up_down, pred, Int.cast_ofNat] using
      h ⟨hbeta, hradix, hprecision, hvNum⟩
  have hdistPred :
      |z - _root_.F2R (beta:=beta) f| ≤
        |z - _root_.F2R (beta:=beta) pred| := by
    by_cases hf_le_z : _root_.F2R (beta:=beta) f ≤ z
    · have hpred_le_z : _root_.F2R (beta:=beta) pred ≤ z :=
        le_trans (le_of_lt hpred_lt_f) hf_le_z
      rw [abs_of_nonneg (sub_nonneg.mpr hf_le_z),
        abs_of_nonneg (sub_nonneg.mpr hpred_le_z)]
      linarith
    · have hz_lt_f : z < _root_.F2R (beta:=beta) f :=
        lt_of_not_ge hf_le_z
      have hlower_lt_f :
          _root_.F2R (beta:=beta) lower < _root_.F2R (beta:=beta) f := by
        have hzGe' : _root_.F2R (beta:=beta) lower ≤ z := by
          rw [hlowerVal]
          exact hzGe
        exact lt_of_le_of_lt hzGe' hz_lt_f
      have hpredCan : Fcanonic (beta:=beta) radix b pred := by
        have h := FPredCanonic (beta:=beta) b radix precision f
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FPredCanonic_check,
          Id.run, ULift.up_down, pred, Int.cast_ofNat] using
          h ⟨hfCan, hradix, hprecision, hvNum⟩
      have hlower_le_pred :
          _root_.F2R (beta:=beta) lower ≤
            _root_.F2R (beta:=beta) pred := by
        have h := FPredProp (beta:=beta) b radix precision lower f
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FPredProp_check,
          Id.run, ULift.up_down, pred, Int.cast_ofNat] using
          h ⟨hbeta, hradix, hprecision, hvNum, hlowerCan, hfCan,
            hlower_lt_f⟩
      have hpredNonneg : 0 ≤ _root_.F2R (beta:=beta) pred :=
        le_trans hlowerNonneg hlower_le_pred
      have he_le_predexp : e ≤ pred.Fexp := by
        have habs :
            |_root_.F2R (beta:=beta) lower| ≤
              |_root_.F2R (beta:=beta) pred| := by
          rw [abs_of_nonneg hlowerNonneg, abs_of_nonneg hpredNonneg]
          exact hlower_le_pred
        have hbeta_gt : 1 < beta := by omega
        have h := Fcanonic_Rle_Zle (beta:=beta) radix b lower pred
          hbeta_gt hbeta.symm
        have hle : lower.Fexp ≤ pred.Fexp := by
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fcanonic_Rle_Zle_check,
            Id.run, ULift.up_down, Fcanonic', lower, pred, Int.cast_ofNat] using
            h ⟨hlowerCan, hpredCan, habs⟩
        simpa [lower] using hle
      have hpredUlp :
          Fulp (beta:=beta) b radix precision pred =
            (radix : ℝ) ^ pred.Fexp := by
        have h := CanonicFulp (beta:=beta) radix b precision pred
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
          Id.run, ULift.up_down, pred, Int.cast_ofNat] using
          h ⟨hpredCan, hbeta, hradix, hprecision, hvNum⟩
      have hstep :
          _root_.F2R (beta:=beta) pred +
              Fulp (beta:=beta) b radix precision pred =
            _root_.F2R (beta:=beta) f := by
        have h := FpredUlpPos (beta:=beta) radix b precision f
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FpredUlpPos_check,
          Id.run, ULift.up_down, pred, Int.cast_ofNat] using
          h ⟨hfCan, hfPos, hbeta, hradix, hprecision, hvNum⟩
      have hpowe_le_gap :
          (radix : ℝ) ^ e ≤
            _root_.F2R (beta:=beta) f - _root_.F2R (beta:=beta) pred := by
        have hpow_le :
            (radix : ℝ) ^ e ≤ (radix : ℝ) ^ pred.Fexp :=
          zpow_le_zpow_right₀ hbase_ge_one he_le_predexp
        rw [← hpredUlp] at hpow_le
        nlinarith
      have hf_minus_z_le :
          _root_.F2R (beta:=beta) f - z ≤ (radix : ℝ) ^ e / 2 := by
        have hleabs :
            _root_.F2R (beta:=beta) f - z ≤
              |z - _root_.F2R (beta:=beta) f| := by
          rw [abs_of_neg (sub_neg.mpr hz_lt_f)]
          linarith
        exact le_trans hleabs hdist
      have hhalf_le_z_minus_pred :
          (radix : ℝ) ^ e / 2 ≤ z - _root_.F2R (beta:=beta) pred := by
        nlinarith [hpowe_le_gap, hf_minus_z_le]
      have hpred_le_z : _root_.F2R (beta:=beta) pred ≤ z := by
        nlinarith [hpowe_pos, hhalf_le_z_minus_pred]
      rw [abs_of_neg (sub_neg.mpr hz_lt_f),
        abs_of_nonneg (sub_nonneg.mpr hpred_le_z)]
      nlinarith [hdist, hhalf_le_z_minus_pred]
  have hclosest := ClosestSuccPred (beta:=beta) b radix precision z f
  exact hclosest ⟨hfCan, hdistSucc, hdistPred, hbeta, hradix, hprecision, hvNum⟩

noncomputable def ImplyClosestStrict_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (z : ℝ) (f : FloatSpec.Core.Defs.FlocqFloat beta) (e : Int) : Unit :=
  ()

/-- Coq: `ImplyClosestStrict` — a strict half-ulp error bound makes the closest
rounded value unique at the level of represented real values. -/
theorem ImplyClosestStrict {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (z : ℝ) (f : FloatSpec.Core.Defs.FlocqFloat beta) (e : Int) :
    ⦃⌜Fbounded (beta:=beta) b f ∧
        Fcanonic (beta:=beta) radix b f ∧
        (radix : ℝ) ^ (e + (precision : Int) - 1) ≤ z ∧
        z ≤ (radix : ℝ) ^ (e + (precision : Int)) ∧
        (radix : ℝ) ^ (e + (precision : Int) - 1) ≤
          _root_.F2R (beta:=beta) f ∧
        -b.dExp ≤ e ∧
        beta = radix ∧ 1 < radix ∧ 1 < precision ∧
        b.vNum = Zpower_nat radix precision ∧
        |z - _root_.F2R (beta:=beta) f| < (radix : ℝ) ^ e / 2⌝⦄
    (pure (ImplyClosestStrict_check (beta:=beta) b radix precision z f e) : Id Unit)
    ⦃⇓_ => ⌜∀ g : FloatSpec.Core.Defs.FlocqFloat beta,
        Closest (beta:=beta) b (radix : ℝ) z g →
          _root_.F2R (beta:=beta) f = _root_.F2R (beta:=beta) g⌝⦄ := by
  intro h
  rcases h with ⟨hfBound, hfCan, hzGe, hzLe, hfGe, heGe, hbeta, hradix,
    hprecision_gt, hvNum, hdist⟩
  simp only [wp, PostCond.noThrow, pure, ImplyClosestStrict_check, Id.run]
  have hprecision : precision ≠ 0 := by omega
  have hprecision_pos : 0 < precision := by omega
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (by omega : (0 : Int) < radix)
  have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
  have hbase_ge_one : (1 : ℝ) ≤ (radix : ℝ) := by
    exact_mod_cast (le_of_lt hradix)
  have hpowe_pos : 0 < (radix : ℝ) ^ e := zpow_pos hradix_pos e
  let lower : FloatSpec.Core.Defs.FlocqFloat beta :=
    ⟨nNormMin radix precision, e⟩
  have hlowerCan : Fcanonic (beta:=beta) radix b lower := by
    have h := FcanonicNnormMin (beta:=beta) radix b precision e
    simpa only [lower, Int.cast_ofNat] using h hradix hprecision_pos hvNum heGe
  have hlowerVal :
      _root_.F2R (beta:=beta) lower =
        (radix : ℝ) ^ (e + (precision : Int) - 1) := by
    dsimp [lower, _root_.F2R, FloatSpec.Core.Defs.F2R, nNormMin]
    have hcast_beta : (beta : ℝ) = (radix : ℝ) := by
      exact_mod_cast hbeta
    rw [hcast_beta]
    have hpow_nat :
        ((radix ^ (precision - 1) : Int) : ℝ) =
          (radix : ℝ) ^ ((precision - 1 : Nat) : Int) := by
      simp [Int.cast_pow]
    rw [hpow_nat]
    have hprec_cast :
        ((precision - 1 : Nat) : Int) = (precision : Int) - 1 := by
      omega
    rw [hprec_cast]
    calc
      (radix : ℝ) ^ ((precision : Int) - 1) * (radix : ℝ) ^ e
          = (radix : ℝ) ^ (((precision : Int) - 1) + e) := by
              rw [zpow_add₀ hradix_ne]
      _ = (radix : ℝ) ^ (e + (precision : Int) - 1) := by
              congr 1
              ring
  have hlowerNonneg : 0 ≤ _root_.F2R (beta:=beta) lower := by
    rw [hlowerVal]
    exact le_of_lt (zpow_pos hradix_pos _)
  have hfGe' :
      _root_.F2R (beta:=beta) lower ≤ _root_.F2R (beta:=beta) f := by
    rw [hlowerVal]
    exact hfGe
  have hfNonneg : 0 ≤ _root_.F2R (beta:=beta) f := by
    exact le_trans hlowerNonneg hfGe'
  have hfPos : 0 < _root_.F2R (beta:=beta) f :=
    lt_of_lt_of_le
      (by
        rw [hlowerVal]
        exact zpow_pos hradix_pos (e + (precision : Int) - 1))
      hfGe'
  have he_le_fexp : e ≤ f.Fexp := by
    have habs :
        |_root_.F2R (beta:=beta) lower| ≤
          |_root_.F2R (beta:=beta) f| := by
      rw [abs_of_nonneg hlowerNonneg, abs_of_nonneg hfNonneg]
      exact hfGe'
    have hbeta_gt : 1 < beta := by omega
    have h := Fcanonic_Rle_Zle (beta:=beta) radix b lower f
      hbeta_gt hbeta.symm
    have hle : lower.Fexp ≤ f.Fexp := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fcanonic_Rle_Zle_check,
        Id.run, ULift.up_down, Fcanonic', lower, Int.cast_ofNat] using
        h ⟨hlowerCan, hfCan, habs⟩
    simpa [lower] using hle
  let succ : FloatSpec.Core.Defs.FlocqFloat beta :=
    FSucc (beta:=beta) b radix precision f
  let pred : FloatSpec.Core.Defs.FlocqFloat beta :=
    FPred (beta:=beta) b radix precision f
  have hsucc_gap :
      _root_.F2R (beta:=beta) succ - _root_.F2R (beta:=beta) f =
        (radix : ℝ) ^ f.Fexp := by
    have hdiff := FSuccDiffPos (beta:=beta) radix b precision f
    have hdiff' :
        _root_.F2R (beta:=beta)
            (FloatSpec.Calc.Operations.Fminus (beta:=beta) succ f) =
          (radix : ℝ) ^ f.Fexp := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FSuccDiffPos_check,
        Id.run, ULift.up_down, succ, Int.cast_ofNat] using
        hdiff ⟨hbeta, hradix, hprecision, hvNum, hfNonneg⟩
    have hminus := Fminus_correct (beta:=beta) succ f
    have hminus' :
        _root_.F2R (beta:=beta)
            (FloatSpec.Calc.Operations.Fminus (beta:=beta) succ f) =
          _root_.F2R (beta:=beta) succ - _root_.F2R (beta:=beta) f := by
      have hbeta_gt : 1 < beta := by omega
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus,
        Id.run, ULift.up_down, succ, Int.cast_ofNat] using hminus hbeta_gt
    exact hminus'.symm.trans hdiff'
  have hpowe_le_succ_gap :
      (radix : ℝ) ^ e ≤
        _root_.F2R (beta:=beta) succ - _root_.F2R (beta:=beta) f := by
    rw [hsucc_gap]
    exact zpow_le_zpow_right₀ hbase_ge_one he_le_fexp
  have hdistSucc :
      |z - _root_.F2R (beta:=beta) f| <
        |z - _root_.F2R (beta:=beta) succ| := by
    have hz_minus_f_lt :
        z - _root_.F2R (beta:=beta) f < (radix : ℝ) ^ e / 2 :=
      lt_of_le_of_lt (le_abs_self _) hdist
    have hhalf_lt_succ_minus_z :
        (radix : ℝ) ^ e / 2 < _root_.F2R (beta:=beta) succ - z := by
      nlinarith [hpowe_le_succ_gap, hz_minus_f_lt]
    have hz_lt_succ : z < _root_.F2R (beta:=beta) succ := by
      nlinarith [hpowe_pos, hhalf_lt_succ_minus_z]
    rw [abs_of_nonpos (sub_nonpos.mpr (le_of_lt hz_lt_succ))]
    nlinarith [hdist, hhalf_lt_succ_minus_z]
  have hpred_lt_f :
      _root_.F2R (beta:=beta) pred < _root_.F2R (beta:=beta) f := by
    have h := FPredLt (beta:=beta) b radix precision f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FPredLt_check,
      Id.run, ULift.up_down, pred, Int.cast_ofNat] using
      h ⟨hbeta, hradix, hprecision, hvNum⟩
  have hdistPred :
      |z - _root_.F2R (beta:=beta) f| <
        |z - _root_.F2R (beta:=beta) pred| := by
    by_cases hf_le_z : _root_.F2R (beta:=beta) f ≤ z
    · have hpred_le_z : _root_.F2R (beta:=beta) pred ≤ z :=
        le_trans (le_of_lt hpred_lt_f) hf_le_z
      rw [abs_of_nonneg (sub_nonneg.mpr hf_le_z),
        abs_of_nonneg (sub_nonneg.mpr hpred_le_z)]
      linarith
    · have hz_lt_f : z < _root_.F2R (beta:=beta) f :=
        lt_of_not_ge hf_le_z
      have hlower_lt_f :
          _root_.F2R (beta:=beta) lower < _root_.F2R (beta:=beta) f := by
        have hzGe' : _root_.F2R (beta:=beta) lower ≤ z := by
          rw [hlowerVal]
          exact hzGe
        exact lt_of_le_of_lt hzGe' hz_lt_f
      have hpredCan : Fcanonic (beta:=beta) radix b pred := by
        have h := FPredCanonic (beta:=beta) b radix precision f
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FPredCanonic_check,
          Id.run, ULift.up_down, pred, Int.cast_ofNat] using
          h ⟨hfCan, hradix, hprecision, hvNum⟩
      have hlower_le_pred :
          _root_.F2R (beta:=beta) lower ≤
            _root_.F2R (beta:=beta) pred := by
        have h := FPredProp (beta:=beta) b radix precision lower f
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FPredProp_check,
          Id.run, ULift.up_down, pred, Int.cast_ofNat] using
          h ⟨hbeta, hradix, hprecision, hvNum, hlowerCan, hfCan,
            hlower_lt_f⟩
      have hpredNonneg : 0 ≤ _root_.F2R (beta:=beta) pred :=
        le_trans hlowerNonneg hlower_le_pred
      have he_le_predexp : e ≤ pred.Fexp := by
        have habs :
            |_root_.F2R (beta:=beta) lower| ≤
              |_root_.F2R (beta:=beta) pred| := by
          rw [abs_of_nonneg hlowerNonneg, abs_of_nonneg hpredNonneg]
          exact hlower_le_pred
        have hbeta_gt : 1 < beta := by omega
        have h := Fcanonic_Rle_Zle (beta:=beta) radix b lower pred
          hbeta_gt hbeta.symm
        have hle : lower.Fexp ≤ pred.Fexp := by
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fcanonic_Rle_Zle_check,
            Id.run, ULift.up_down, Fcanonic', lower, pred, Int.cast_ofNat] using
            h ⟨hlowerCan, hpredCan, habs⟩
        simpa [lower] using hle
      have hpredUlp :
          Fulp (beta:=beta) b radix precision pred =
            (radix : ℝ) ^ pred.Fexp := by
        have h := CanonicFulp (beta:=beta) radix b precision pred
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
          Id.run, ULift.up_down, pred, Int.cast_ofNat] using
          h ⟨hpredCan, hbeta, hradix, hprecision, hvNum⟩
      have hstep :
          _root_.F2R (beta:=beta) pred +
              Fulp (beta:=beta) b radix precision pred =
            _root_.F2R (beta:=beta) f := by
        have h := FpredUlpPos (beta:=beta) radix b precision f
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FpredUlpPos_check,
          Id.run, ULift.up_down, pred, Int.cast_ofNat] using
          h ⟨hfCan, hfPos, hbeta, hradix, hprecision, hvNum⟩
      have hpowe_le_gap :
          (radix : ℝ) ^ e ≤
            _root_.F2R (beta:=beta) f - _root_.F2R (beta:=beta) pred := by
        have hpow_le :
            (radix : ℝ) ^ e ≤ (radix : ℝ) ^ pred.Fexp :=
          zpow_le_zpow_right₀ hbase_ge_one he_le_predexp
        rw [← hpredUlp] at hpow_le
        nlinarith
      have hf_minus_z_lt :
          _root_.F2R (beta:=beta) f - z < (radix : ℝ) ^ e / 2 := by
        have hleabs :
            _root_.F2R (beta:=beta) f - z =
              |z - _root_.F2R (beta:=beta) f| := by
          rw [abs_of_neg (sub_neg.mpr hz_lt_f)]
          ring
        rw [hleabs]
        exact hdist
      have hhalf_lt_z_minus_pred :
          (radix : ℝ) ^ e / 2 < z - _root_.F2R (beta:=beta) pred := by
        nlinarith [hpowe_le_gap, hf_minus_z_lt]
      have hpred_lt_z : _root_.F2R (beta:=beta) pred < z := by
        nlinarith [hpowe_pos, hhalf_lt_z_minus_pred]
      rw [abs_of_neg (sub_neg.mpr hz_lt_f),
        abs_of_nonneg (sub_nonneg.mpr (le_of_lt hpred_lt_z))]
      nlinarith [hf_minus_z_lt, hhalf_lt_z_minus_pred]
  have hclosest_f : Closest (beta:=beta) b (radix : ℝ) z f := by
    have h := ImplyClosest (beta:=beta) b radix precision z f e
    exact h ⟨hfBound, hfCan, hzGe, hzLe, hfGe, heGe, hbeta, hradix,
      hprecision_gt, hvNum, le_of_lt hdist⟩
  have hpredBound : Fbounded (beta:=beta) b pred := by
    have h := FBoundedPred (beta:=beta) b radix precision f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FBoundedPred_check,
      Id.run, ULift.up_down, pred, Int.cast_ofNat] using
      h ⟨hfBound, hradix, hprecision, hvNum⟩
  have hpredCan : Fcanonic (beta:=beta) radix b pred := by
    have h := FPredCanonic (beta:=beta) b radix precision f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FPredCanonic_check,
      Id.run, ULift.up_down, pred, Int.cast_ofNat] using
      h ⟨hfCan, hradix, hprecision, hvNum⟩
  have hFNSuccPred :
      FNSucc (beta:=beta) b (radix : ℝ) precision pred = f := by
    have hnorm :
        Fnormalize (beta:=beta) beta b precision pred = pred := by
      have h := FcanonicFnormalizeEq (beta:=beta) beta b precision pred
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨by simpa [hbeta] using hpredCan, rfl, by omega, hprecision,
          by simpa [hbeta] using hvNum⟩
    have hsuccPred :
        FSucc (beta:=beta) b radix precision pred = f := by
      have h := FSucPred (beta:=beta) b radix precision f
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FSucPred_check,
        Id.run, ULift.up_down, pred, Int.cast_ofNat] using
        h ⟨hfCan, hradix, hprecision, hvNum⟩
    have hsuccPredBeta :
        FSucc (beta:=beta) b beta precision pred = f := by
      simpa [hbeta] using hsuccPred
    simpa [FNSucc, hnorm] using hsuccPredBeta
  have hFNSuccF :
      FNSucc (beta:=beta) b (radix : ℝ) precision f = succ := by
    have hnorm :
        Fnormalize (beta:=beta) beta b precision f = f := by
      have h := FcanonicFnormalizeEq (beta:=beta) beta b precision f
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨by simpa [hbeta] using hfCan, rfl, by omega, hprecision,
          by simpa [hbeta] using hvNum⟩
    have hsuccEq :
        FSucc (beta:=beta) b beta precision f = succ := by
      simp [succ, hbeta]
    simpa [FNSucc, hnorm] using hsuccEq
  have hpred_le_z :
      _root_.F2R (beta:=beta) pred ≤ z := by
    by_contra hnot
    have hz_lt_pred : z < _root_.F2R (beta:=beta) pred := lt_of_not_ge hnot
    have hz_lt_f : z < _root_.F2R (beta:=beta) f :=
      lt_trans hz_lt_pred hpred_lt_f
    have hdist_le :
        _root_.F2R (beta:=beta) f - z <
          _root_.F2R (beta:=beta) pred - z := by
      calc
        _root_.F2R (beta:=beta) f - z =
            |z - _root_.F2R (beta:=beta) f| := by
              rw [abs_of_neg (sub_neg.mpr hz_lt_f)]
              ring
        _ < |z - _root_.F2R (beta:=beta) pred| := hdistPred
        _ = _root_.F2R (beta:=beta) pred - z := by
              rw [abs_of_neg (sub_neg.mpr hz_lt_pred)]
              ring
    linarith
  have hz_le_succ :
      z ≤ _root_.F2R (beta:=beta) succ := by
    by_contra hnot
    have hsucc_lt_z : _root_.F2R (beta:=beta) succ < z := lt_of_not_ge hnot
    have hf_lt_z : _root_.F2R (beta:=beta) f < z :=
      lt_trans (by
        have hlt := FSuccLt (beta:=beta) b radix precision f
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FSuccLt_check,
          Id.run, ULift.up_down, succ, Int.cast_ofNat] using
          hlt ⟨hbeta, hradix, hprecision, hvNum⟩) hsucc_lt_z
    have hdist_le :
        z - _root_.F2R (beta:=beta) f <
          z - _root_.F2R (beta:=beta) succ := by
      calc
        z - _root_.F2R (beta:=beta) f =
            |z - _root_.F2R (beta:=beta) f| := by
              rw [abs_of_pos (sub_pos.mpr hf_lt_z)]
        _ < |z - _root_.F2R (beta:=beta) succ| := hdistSucc
        _ = z - _root_.F2R (beta:=beta) succ := by
              rw [abs_of_pos (sub_pos.mpr hsucc_lt_z)]
    linarith
  intro g hgClosest
  have hg_to_f :
      |_root_.F2R (beta:=beta) g - z| ≤
        |_root_.F2R (beta:=beta) f - z| :=
    hgClosest.2 f hclosest_f.1
  by_cases hg_le_pred :
      _root_.F2R (beta:=beta) g ≤ _root_.F2R (beta:=beta) pred
  · have hg_le_z : _root_.F2R (beta:=beta) g ≤ z :=
      le_trans hg_le_pred hpred_le_z
    have hstrict :
        |_root_.F2R (beta:=beta) f - z| <
          |_root_.F2R (beta:=beta) g - z| := by
      calc
        |_root_.F2R (beta:=beta) f - z|
            = |z - _root_.F2R (beta:=beta) f| := abs_sub_comm _ _
        _ < |z - _root_.F2R (beta:=beta) pred| := hdistPred
        _ = z - _root_.F2R (beta:=beta) pred := by
              rw [abs_of_nonneg (sub_nonneg.mpr hpred_le_z)]
        _ ≤ z - _root_.F2R (beta:=beta) g := by linarith
        _ = |_root_.F2R (beta:=beta) g - z| := by
              rw [abs_of_nonpos (sub_nonpos.mpr hg_le_z)]
              ring
    exact (False.elim (not_lt_of_ge hg_to_f hstrict))
  · have hpred_lt_g :
        _root_.F2R (beta:=beta) pred < _root_.F2R (beta:=beta) g :=
      lt_of_not_ge hg_le_pred
    have hf_le_g :
        _root_.F2R (beta:=beta) f ≤ _root_.F2R (beta:=beta) g := by
      have hprop := FNSuccProp (beta:=beta) b (radix : ℝ) precision pred g
      have hle :
          _root_.F2R (beta:=beta)
              (FNSucc (beta:=beta) b (radix : ℝ) precision pred) ≤
            _root_.F2R (beta:=beta) g := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FNSuccProp_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          hprop ⟨hpredBound, hgClosest.1, hpred_lt_g, by omega, hprecision,
            by simpa [hbeta] using hvNum⟩
      rwa [hFNSuccPred] at hle
    by_cases hf_lt_g :
        _root_.F2R (beta:=beta) f < _root_.F2R (beta:=beta) g
    · have hsucc_le_g :
          _root_.F2R (beta:=beta) succ ≤ _root_.F2R (beta:=beta) g := by
        have hprop := FNSuccProp (beta:=beta) b (radix : ℝ) precision f g
        have hle :
            _root_.F2R (beta:=beta)
                (FNSucc (beta:=beta) b (radix : ℝ) precision f) ≤
              _root_.F2R (beta:=beta) g := by
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FNSuccProp_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using
            hprop ⟨hfBound, hgClosest.1, hf_lt_g, by omega, hprecision,
              by simpa [hbeta] using hvNum⟩
        rwa [hFNSuccF] at hle
      have hz_le_g : z ≤ _root_.F2R (beta:=beta) g :=
        le_trans hz_le_succ hsucc_le_g
      have hstrict :
          |_root_.F2R (beta:=beta) f - z| <
            |_root_.F2R (beta:=beta) g - z| := by
        calc
          |_root_.F2R (beta:=beta) f - z|
              = |z - _root_.F2R (beta:=beta) f| := abs_sub_comm _ _
          _ < |z - _root_.F2R (beta:=beta) succ| := hdistSucc
          _ = _root_.F2R (beta:=beta) succ - z := by
                rw [abs_of_nonpos (sub_nonpos.mpr hz_le_succ)]
                ring
          _ ≤ _root_.F2R (beta:=beta) g - z := by linarith
          _ = |_root_.F2R (beta:=beta) g - z| := by
                rw [abs_of_nonneg (sub_nonneg.mpr hz_le_g)]
      exact (False.elim (not_lt_of_ge hg_to_f hstrict))
    · exact le_antisymm hf_le_g (le_of_not_gt hf_lt_g)

noncomputable def ImplyClosestStrict2_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (z : ℝ) (f : FloatSpec.Core.Defs.FlocqFloat beta) (e : Int) : Unit :=
  ()

/-- Coq: `ImplyClosestStrict2` — the strict half-ulp hypothesis gives both
closestness and uniqueness of the represented real value. -/
theorem ImplyClosestStrict2 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (z : ℝ) (f : FloatSpec.Core.Defs.FlocqFloat beta) (e : Int) :
    ⦃⌜Fbounded (beta:=beta) b f ∧
        Fcanonic (beta:=beta) radix b f ∧
        (radix : ℝ) ^ (e + (precision : Int) - 1) ≤ z ∧
        z ≤ (radix : ℝ) ^ (e + (precision : Int)) ∧
        (radix : ℝ) ^ (e + (precision : Int) - 1) ≤
          _root_.F2R (beta:=beta) f ∧
        -b.dExp ≤ e ∧
        beta = radix ∧ 1 < radix ∧ 1 < precision ∧
        b.vNum = Zpower_nat radix precision ∧
        |z - _root_.F2R (beta:=beta) f| < (radix : ℝ) ^ e / 2⌝⦄
    (pure (ImplyClosestStrict2_check (beta:=beta) b radix precision z f e) : Id Unit)
    ⦃⇓_ => ⌜Closest (beta:=beta) b (radix : ℝ) z f ∧
        ∀ g : FloatSpec.Core.Defs.FlocqFloat beta,
          Closest (beta:=beta) b (radix : ℝ) z g →
            _root_.F2R (beta:=beta) f = _root_.F2R (beta:=beta) g⌝⦄ := by
  intro h
  rcases h with ⟨hfBound, hfCan, hzGe, hzLe, hfGe, heGe, hbeta, hradix,
    hprecision_gt, hvNum, hdist⟩
  simp only [wp, PostCond.noThrow, pure, ImplyClosestStrict2_check, Id.run]
  constructor
  · have hclosest := ImplyClosest (beta:=beta) b radix precision z f e
    exact hclosest ⟨hfBound, hfCan, hzGe, hzLe, hfGe, heGe, hbeta, hradix,
      hprecision_gt, hvNum, le_of_lt hdist⟩
  · have hstrict := ImplyClosestStrict (beta:=beta) b radix precision z f e
    exact hstrict ⟨hfBound, hfCan, hzGe, hzLe, hfGe, heGe, hbeta, hradix,
      hprecision_gt, hvNum, hdist⟩

noncomputable def ClosestImplyEven_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (z : ℝ) (f : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `ClosestImplyEven`.

If `z` is exactly halfway above a nonnegative canonical float and `f` is an
even-closest rounding of `z`, then the tie-breaking clause forces `f` to be
normalized-even. -/
theorem ClosestImplyEven {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (z : ℝ) (f : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜EvenClosest (beta:=beta) b (radix : ℝ) precision z f ∧
        (∃ g : FloatSpec.Core.Defs.FlocqFloat beta,
          z = _root_.F2R (beta:=beta) g + (radix : ℝ) ^ g.Fexp / 2 ∧
          Fcanonic (beta:=beta) radix b g ∧ 0 ≤ g.Fnum) ∧
        beta = radix ∧ 1 < radix ∧ 1 < precision ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (ClosestImplyEven_check (beta:=beta) b radix precision z f) : Id Unit)
    ⦃⇓_ => ⌜FNeven (beta:=beta) b (radix : ℝ) precision f⌝⦄ := by
  intro h
  rcases h with ⟨hEvenClosest, hmid, hbeta, hradix, hprecision_gt, hvNum⟩
  rcases hmid with ⟨g, hz_mid, hgCan, hgNum_nonneg⟩
  simp only [wp, PostCond.noThrow, pure, ClosestImplyEven_check,
    Id.run, ULift.up_down]
  let succ : FloatSpec.Core.Defs.FlocqFloat beta :=
    FSucc (beta:=beta) b radix precision g
  let pred : FloatSpec.Core.Defs.FlocqFloat beta :=
    FPred (beta:=beta) b radix precision g
  let succ2 : FloatSpec.Core.Defs.FlocqFloat beta :=
    FSucc (beta:=beta) b radix precision succ
  have hprecision : precision ≠ 0 := by omega
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (by omega : (0 : Int) < radix)
  have hpow_pos : 0 < (radix : ℝ) ^ g.Fexp :=
    zpow_pos hradix_pos g.Fexp
  have hgBound : Fbounded (beta:=beta) b g := by
    have h := FcanonicBound (beta:=beta) radix b g
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicBound_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hgCan
  have hsuccCan : Fcanonic (beta:=beta) radix b succ := by
    have h := FSuccCanonic (beta:=beta) b radix precision g
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FSuccCanonic_check,
      Id.run, ULift.up_down, succ, Int.cast_ofNat] using
      h ⟨hgCan, hradix, hprecision, hvNum⟩
  have hsuccBound : Fbounded (beta:=beta) b succ := by
    have h := FcanonicBound (beta:=beta) radix b succ
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicBound_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hsuccCan
  have hg_not_low : g.Fnum ≠ -nNormMin radix precision := by
    have hnpos : 0 < nNormMin radix precision := by
      unfold nNormMin
      exact pow_pos (by omega : 0 < radix) _
    omega
  have hsucc_sub :
      _root_.F2R (beta:=beta) succ - _root_.F2R (beta:=beta) g =
        (radix : ℝ) ^ g.Fexp := by
    have hdiff := FSuccDiff1 (beta:=beta) b radix precision g
      hbeta hradix hprecision hvNum hg_not_low
    have hbeta_gt : 1 < beta := by omega
    have hminus :
        _root_.F2R (beta:=beta) (Fminus (beta:=beta) succ g) =
          _root_.F2R (beta:=beta) succ - _root_.F2R (beta:=beta) g := by
      have h := Fminus_correct (beta:=beta) succ g
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus,
        Fminus, Id.run, ULift.up_down, Int.cast_ofNat] using h hbeta_gt
    calc
      _root_.F2R (beta:=beta) succ - _root_.F2R (beta:=beta) g =
          _root_.F2R (beta:=beta) (Fminus (beta:=beta) succ g) := hminus.symm
      _ = FtoR (Fminus (beta:=beta) succ g) := rfl
      _ = FtoR (⟨1, g.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat beta) := hdiff
      _ = (radix : ℝ) ^ g.Fexp := by
            simp [FtoR, _root_.F2R, FloatSpec.Core.Defs.F2R, hbeta]
  have hsucc_val :
      _root_.F2R (beta:=beta) succ =
        _root_.F2R (beta:=beta) g + (radix : ℝ) ^ g.Fexp := by
    linarith
  have hg_lt_z : _root_.F2R (beta:=beta) g < z := by
    rw [hz_mid]
    nlinarith
  have hz_lt_succ : z < _root_.F2R (beta:=beta) succ := by
    rw [hz_mid, hsucc_val]
    nlinarith
  have hzg_nonneg : 0 ≤ z - _root_.F2R (beta:=beta) g := by
    linarith
  have hzs_nonpos : z - _root_.F2R (beta:=beta) succ ≤ 0 := by
    linarith
  have hdist_g_succ_eq :
      |z - _root_.F2R (beta:=beta) g| =
        |z - _root_.F2R (beta:=beta) succ| := by
    rw [abs_of_nonneg hzg_nonneg, abs_of_nonpos hzs_nonpos]
    linarith
  have hpred_lt_g :
      _root_.F2R (beta:=beta) pred < _root_.F2R (beta:=beta) g := by
    have h := FPredLt (beta:=beta) b radix precision g
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FPredLt_check,
      Id.run, ULift.up_down, pred, Int.cast_ofNat] using
      h ⟨hbeta, hradix, hprecision, hvNum⟩
  have hdist_g_pred :
      |z - _root_.F2R (beta:=beta) g| ≤
        |z - _root_.F2R (beta:=beta) pred| := by
    have hpred_le_z : _root_.F2R (beta:=beta) pred ≤ z :=
      le_trans (le_of_lt hpred_lt_g) (le_of_lt hg_lt_z)
    rw [abs_of_nonneg hzg_nonneg,
      abs_of_nonneg (sub_nonneg.mpr hpred_le_z)]
    linarith
  have hClosest_g : Closest (beta:=beta) b (radix : ℝ) z g := by
    have h := ClosestSuccPred (beta:=beta) b radix precision z g
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestSuccPred_check,
      Id.run, ULift.up_down, succ, pred, Int.cast_ofNat] using
      h ⟨hgCan, le_of_eq hdist_g_succ_eq, hdist_g_pred,
        hbeta, hradix, hprecision, hvNum⟩
  have hsucc2_gt_succ :
      _root_.F2R (beta:=beta) succ <
        _root_.F2R (beta:=beta) succ2 := by
    have h := FSuccLt (beta:=beta) b radix precision succ
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FSuccLt_check,
      Id.run, ULift.up_down, succ2, Int.cast_ofNat] using
      h ⟨hbeta, hradix, hprecision, hvNum⟩
  have hdist_succ_succ2 :
      |z - _root_.F2R (beta:=beta) succ| ≤
        |z - _root_.F2R (beta:=beta) succ2| := by
    have hz_le_succ2 : z ≤ _root_.F2R (beta:=beta) succ2 :=
      le_trans (le_of_lt hz_lt_succ) (le_of_lt hsucc2_gt_succ)
    rw [abs_of_nonpos hzs_nonpos,
      abs_of_nonpos (sub_nonpos.mpr hz_le_succ2)]
    linarith
  have hpred_succ :
      FPred (beta:=beta) b radix precision succ = g := by
    have h := FPredSuc (beta:=beta) b radix precision g
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FPredSuc_check,
      Id.run, ULift.up_down, succ, Int.cast_ofNat] using
      h ⟨hgCan, hradix, hprecision, hvNum⟩
  have hdist_succ_pred :
      |z - _root_.F2R (beta:=beta) succ| ≤
        |z - _root_.F2R (beta:=beta)
          (FPred (beta:=beta) b radix precision succ)| := by
    rw [hpred_succ]
    exact le_of_eq hdist_g_succ_eq.symm
  have hClosest_succ : Closest (beta:=beta) b (radix : ℝ) z succ := by
    have h := ClosestSuccPred (beta:=beta) b radix precision z succ
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestSuccPred_check,
      Id.run, ULift.up_down, succ2, Int.cast_ofNat] using
      h ⟨hsuccCan, hdist_succ_succ2, hdist_succ_pred,
        hbeta, hradix, hprecision, hvNum⟩
  have hMinOrMax :
      isMin (beta:=beta) b radix z f ∨ isMax (beta:=beta) b radix z f := by
    have h := EvenClosestMinOrMax_from_split_radix_payload (beta:=beta) b radix (radix : ℝ) precision
    exact h True.intro z f hEvenClosest
  rcases hEvenClosest with ⟨_, hTie⟩
  rcases hTie with hEven | hUnique
  · exact hEven
  · exfalso
    rcases hMinOrMax with hMin | hMax
    · have hf_lt_succ :
          _root_.F2R (beta:=beta) f < _root_.F2R (beta:=beta) succ :=
        lt_of_le_of_lt hMin.2.1 hz_lt_succ
      have hEq := hUnique succ hClosest_succ
      linarith
    · have hg_lt_f :
            _root_.F2R (beta:=beta) g < _root_.F2R (beta:=beta) f :=
        lt_of_lt_of_le hg_lt_z hMax.2.1
      have hEq := hUnique g hClosest_g
      linarith

noncomputable def ClosestImplyEven_int_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (z m : ℝ) (f : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

noncomputable def MinOrMax2_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (z : ℝ) (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `MinOrMax2`.

If a positive canonical bounded float `p` lies below `z` and `z` is less than
one ulp away from `p`, then `p` is the lower endpoint around `z`. -/
theorem MinOrMax2 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (z : ℝ) (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b p ∧
        Fcanonic (beta:=beta) radix b p ∧
        0 < _root_.F2R (beta:=beta) p ∧
        |z - _root_.F2R (beta:=beta) p| <
          Fulp (beta:=beta) b radix precision p ∧
        _root_.F2R (beta:=beta) p ≤ z ∧
        beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (MinOrMax2_check (beta:=beta) b radix precision z p) : Id Unit)
    ⦃⇓_ => ⌜MinOrMax (beta:=beta) b radix z p⌝⦄ := by
  intro h
  rcases h with
    ⟨hpBound, hpCan, hpPos, hdist, hp_le_z, hbeta, hradix, hprecision, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, MinOrMax2_check,
    Id.run, ULift.up_down]
  left
  constructor
  · exact hpBound
  constructor
  · exact hp_le_z
  · intro f hfBound hf_le_z
    let nf : FloatSpec.Core.Defs.FlocqFloat beta :=
      Fnormalize (beta:=beta) radix b precision f
    let succ : FloatSpec.Core.Defs.FlocqFloat beta :=
      FSucc (beta:=beta) b radix precision p
    have hnf_val :
        _root_.F2R (beta:=beta) nf = _root_.F2R (beta:=beta) f := by
      have h := FnormalizeCorrect (beta:=beta) radix b precision f
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
        Id.run, ULift.up_down, nf, Int.cast_ofNat] using h ⟨hbeta, hradix⟩
    have hnf_bound : Fbounded (beta:=beta) b nf := by
      have h := FnormalizeBounded (beta:=beta) radix b precision f
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeBounded_check,
        Id.run, ULift.up_down, Fbounded', nf, Int.cast_ofNat] using
        h ⟨hfBound, hfBound, hprecision, hradix, hvNum⟩
    have hnf_can : Fcanonic (beta:=beta) radix b nf := by
      have h := FnormalizeCanonic (beta:=beta) radix b precision f
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
        Id.run, ULift.up_down, Fbounded', nf, Int.cast_ofNat] using
        h ⟨hfBound, hfBound, hprecision, hradix, hvNum⟩
    have hsucc_can : Fcanonic (beta:=beta) radix b succ := by
      have h := FSuccCanonic (beta:=beta) b radix precision p
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FSuccCanonic_check,
        Id.run, ULift.up_down, succ, Int.cast_ofNat] using
        h ⟨hpCan, hradix, hprecision, hvNum⟩
    have hpred_succ :
        FPred (beta:=beta) b radix precision succ = p := by
      have h := FPredSuc (beta:=beta) b radix precision p
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FPredSuc_check,
        Id.run, ULift.up_down, succ, Int.cast_ofNat] using
        h ⟨hpCan, hradix, hprecision, hvNum⟩
    have hsucc_gt_p :
        _root_.F2R (beta:=beta) p < _root_.F2R (beta:=beta) succ := by
      have h := FSuccLt (beta:=beta) b radix precision p
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FSuccLt_check,
        Id.run, ULift.up_down, succ, Int.cast_ofNat] using
        h ⟨hbeta, hradix, hprecision, hvNum⟩
    have hsucc_pos : 0 < _root_.F2R (beta:=beta) succ :=
      lt_trans hpPos hsucc_gt_p
    have hsucc_step :
        _root_.F2R (beta:=beta) p +
            Fulp (beta:=beta) b radix precision p =
          _root_.F2R (beta:=beta) succ := by
      have h := FpredUlpPos (beta:=beta) radix b precision succ
      have h' :
          _root_.F2R (beta:=beta)
              (FPred (beta:=beta) b radix precision succ) +
            Fulp (beta:=beta) b radix precision
              (FPred (beta:=beta) b radix precision succ) =
            _root_.F2R (beta:=beta) succ := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FpredUlpPos_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          h ⟨hsucc_can, hsucc_pos, hbeta, hradix, hprecision, hvNum⟩
      simpa [hpred_succ] using h'
    have hz_lt_succ : z < _root_.F2R (beta:=beta) succ := by
      have hsub_nonneg : 0 ≤ z - _root_.F2R (beta:=beta) p := by
        linarith
      have hsub_lt :
          z - _root_.F2R (beta:=beta) p <
            Fulp (beta:=beta) b radix precision p := by
        rw [abs_of_nonneg hsub_nonneg] at hdist
        exact hdist
      linarith
    have hnf_lt_succ :
        _root_.F2R (beta:=beta) nf < _root_.F2R (beta:=beta) succ := by
      rw [hnf_val]
      exact lt_of_le_of_lt hf_le_z hz_lt_succ
    have hnf_le_pred :
        _root_.F2R (beta:=beta) nf ≤
          _root_.F2R (beta:=beta)
            (FPred (beta:=beta) b radix precision succ) := by
      have h := FPredProp (beta:=beta) b radix precision nf succ
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FPredProp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hbeta, hradix, hprecision, hvNum, hnf_can, hsucc_can, hnf_lt_succ⟩
    calc
      _root_.F2R (beta:=beta) f =
          _root_.F2R (beta:=beta) nf := hnf_val.symm
      _ ≤ _root_.F2R (beta:=beta)
            (FPred (beta:=beta) b radix precision succ) := hnf_le_pred
      _ = _root_.F2R (beta:=beta) p := by rw [hpred_succ]

noncomputable def MinOrMax1_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (z : ℝ) (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `MinOrMax1`.

A positive canonical bounded float within one predecessor ulp of `z` is either
the lower or upper extremal bounded float around `z`. -/
theorem MinOrMax1 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (z : ℝ) (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b p ∧
        Fcanonic (beta:=beta) radix b p ∧
        0 < _root_.F2R (beta:=beta) p ∧
        |z - _root_.F2R (beta:=beta) p| <
          Fulp (beta:=beta) b radix precision
            (FPred (beta:=beta) b radix precision p) ∧
        beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (MinOrMax1_check (beta:=beta) b radix precision z p) : Id Unit)
    ⦃⇓_ => ⌜MinOrMax (beta:=beta) b radix z p⌝⦄ := by
  intro h
  rcases h with
    ⟨hpBound, hpCan, hpPos, hdist, hbeta, hradix, hprecision, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, MinOrMax1_check,
    Id.run, ULift.up_down]
  by_cases hsub_nonneg : 0 ≤ z - _root_.F2R (beta:=beta) p
  · have hp_le_z : _root_.F2R (beta:=beta) p ≤ z := by linarith
    have hpred_ulp_le :
        Fulp (beta:=beta) b radix precision
            (FPred (beta:=beta) b radix precision p) ≤
          Fulp (beta:=beta) b radix precision p := by
      have h := FulpFPredGePos (beta:=beta) radix b precision p
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpFPredGePos_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hpBound, hpCan, hpPos, hbeta, hradix, hprecision, hvNum⟩
    have hdist_p :
        |z - _root_.F2R (beta:=beta) p| <
          Fulp (beta:=beta) b radix precision p :=
      lt_of_lt_of_le hdist hpred_ulp_le
    have h := MinOrMax2 (beta:=beta) b radix precision z p
    exact
      (by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinOrMax2_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          h ⟨hpBound, hpCan, hpPos, hdist_p, hp_le_z,
            hbeta, hradix, hprecision, hvNum⟩)
  · have hsub_neg : z - _root_.F2R (beta:=beta) p < 0 := lt_of_not_ge hsub_nonneg
    have hz_le_p : z ≤ _root_.F2R (beta:=beta) p := by linarith
    right
    constructor
    · exact hpBound
    constructor
    · exact hz_le_p
    · intro f hfBound hz_le_f
      let nf : FloatSpec.Core.Defs.FlocqFloat beta :=
        Fnormalize (beta:=beta) radix b precision f
      let pred : FloatSpec.Core.Defs.FlocqFloat beta :=
        FPred (beta:=beta) b radix precision p
      have hnf_val :
          _root_.F2R (beta:=beta) nf = _root_.F2R (beta:=beta) f := by
        have h := FnormalizeCorrect (beta:=beta) radix b precision f
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
          Id.run, ULift.up_down, nf, Int.cast_ofNat] using h ⟨hbeta, hradix⟩
      have hnf_can : Fcanonic (beta:=beta) radix b nf := by
        have h := FnormalizeCanonic (beta:=beta) radix b precision f
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
          Id.run, ULift.up_down, Fbounded', nf, Int.cast_ofNat] using
          h ⟨hfBound, hfBound, hprecision, hradix, hvNum⟩
      have hpred_can : Fcanonic (beta:=beta) radix b pred := by
        have h := FPredCanonic (beta:=beta) b radix precision p
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FPredCanonic_check,
          Id.run, ULift.up_down, pred, Int.cast_ofNat] using
          h ⟨hpCan, hradix, hprecision, hvNum⟩
      have hsucc_eq :
          FSucc (beta:=beta) b radix precision pred = p := by
        have h := FSucPred (beta:=beta) b radix precision p
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FSucPred_check,
          Id.run, ULift.up_down, pred, Int.cast_ofNat] using
          h ⟨hpCan, hradix, hprecision, hvNum⟩
      have hstep :
          _root_.F2R (beta:=beta) pred +
            Fulp (beta:=beta) b radix precision pred =
          _root_.F2R (beta:=beta) p := by
        have h := FpredUlpPos (beta:=beta) radix b precision p
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FpredUlpPos_check,
          Id.run, ULift.up_down, pred, Int.cast_ofNat] using
          h ⟨hpCan, hpPos, hbeta, hradix, hprecision, hvNum⟩
      have hp_sub_z_lt :
          _root_.F2R (beta:=beta) p - z <
            Fulp (beta:=beta) b radix precision pred := by
        have hdist' :
            -(z - _root_.F2R (beta:=beta) p) <
              Fulp (beta:=beta) b radix precision pred := by
          rw [abs_of_neg hsub_neg] at hdist
          exact hdist
        linarith
      have hpred_lt_z : _root_.F2R (beta:=beta) pred < z := by
        linarith
      have hpred_lt_nf :
          _root_.F2R (beta:=beta) pred < _root_.F2R (beta:=beta) nf := by
        rw [hnf_val]
        exact lt_of_lt_of_le hpred_lt_z hz_le_f
      have hsucc_le_nf :
          _root_.F2R (beta:=beta)
              (FSucc (beta:=beta) b radix precision pred) ≤
            _root_.F2R (beta:=beta) nf := by
        have h := FSuccProp (beta:=beta) b radix precision pred nf
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FSuccProp_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          h ⟨hbeta, hradix, hprecision, hvNum, hpred_can, hnf_can,
            hpred_lt_nf⟩
      calc
        _root_.F2R (beta:=beta) p =
            _root_.F2R (beta:=beta)
              (FSucc (beta:=beta) b radix precision pred) := by
                rw [hsucc_eq]
        _ ≤ _root_.F2R (beta:=beta) nf := hsucc_le_nf
        _ = _root_.F2R (beta:=beta) f := hnf_val

noncomputable def FcanonicZeroEq_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Canonical zero is represented by the boundary zero float. -/
theorem FcanonicZeroEq {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fcanonic (beta:=beta) radix b p ∧
        _root_.F2R (beta:=beta) p = 0 ∧
        beta = radix ∧ 1 < radix ∧ 0 < b.vNum⌝⦄
    (pure (FcanonicZeroEq_check (beta:=beta) b radix p) : Id Unit)
    ⦃⇓_ => ⌜p = Fzero beta (-b.dExp)⌝⦄ := by
  intro h
  rcases h with ⟨hpCan, hpZero, hbeta, hradix, hvNumPos⟩
  simp only [wp, PostCond.noThrow, pure, FcanonicZeroEq_check,
    Id.run, ULift.up_down]
  have hzeroCan : Fcanonic (beta:=beta) radix b (Fzero beta (-b.dExp)) := by
    right
    constructor
    · exact (FboundedFzero_from_positive_bound_payload (beta:=beta) b) hvNumPos
    constructor
    · simp [Fzero]
    · simpa [Fzero] using hvNumPos
  have hzeroVal : _root_.F2R (beta:=beta) (Fzero beta (-b.dExp)) = 0 := by
    have h := FzeroisZero (beta:=beta) b
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FzeroisZero_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
  have huniq := FcanonicUnique (beta:=beta) radix b p (Fzero beta (-b.dExp))
    (by omega) hbeta.symm
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicUnique_check,
    ULift.down_up, Fcanonic', Int.cast_ofNat] using huniq ⟨hpCan, hzeroCan, hpZero.trans hzeroVal.symm⟩

noncomputable def FpredUlpZero_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Zero-valued analogue of `FpredUlpPos` used by Coq `MinOrMax3_aux`. -/
theorem FpredUlpZero {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fcanonic (beta:=beta) radix b p ∧
        _root_.F2R (beta:=beta) p = 0 ∧
        beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (FpredUlpZero_check (beta:=beta) b radix precision p) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta)
          (FPred (beta:=beta) b radix precision p) +
        Fulp (beta:=beta) b radix precision
          (FPred (beta:=beta) b radix precision p) = 0⌝⦄ := by
  intro h
  rcases h with ⟨hpCan, hpZero, hbeta, hradix, hprecision, hvNum⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure, FpredUlpZero_check,
    Id.run, ULift.up_down]
  let pred := FPred (beta:=radix) b radix precision p
  have hvNumPos : 0 < b.vNum := by
    rw [hvNum, Zpower_nat]
    exact pow_pos (by omega : 0 < radix) precision
  have hpZeroFloat : p = Fzero radix (-b.dExp) := by
    have h := FcanonicZeroEq (beta:=radix) b radix p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicZeroEq_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hpCan, hpZero, rfl, hradix, hvNumPos⟩
  have hpPredPos : 0 < pPred b.vNum := by
    unfold pPred
    have hvNumGtOne : (1 : Int) < b.vNum := by
      rw [hvNum, Zpower_nat]
      exact one_lt_pow₀ hradix hprecision
    omega
  have hnormPos : 0 < nNormMin radix precision := by
    unfold nNormMin
    exact pow_pos (by omega : 0 < radix) _
  have hpred_eq :
      pred =
        (⟨-1, -b.dExp⟩ : FloatSpec.Core.Defs.FlocqFloat radix) := by
    simp only [pred]
    rw [hpZeroFloat]
    have hnot_negpred : (0 : Int) ≠ -pPred b.vNum := by omega
    have hnot_norm : (0 : Int) ≠ nNormMin radix precision := by omega
    simp [FPred, Fzero, hnot_negpred, hnot_norm]
  have hpredCan : Fcanonic (beta:=radix) radix b pred := by
    have h := FPredCanonic (beta:=radix) b radix precision p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FPredCanonic_check,
      Id.run, ULift.up_down, pred, Int.cast_ofNat] using
      h ⟨hpCan, hradix, hprecision, hvNum⟩
  have hfulp :
      Fulp (beta:=radix) b radix precision pred = (radix : ℝ) ^ pred.Fexp := by
    have h := CanonicFulp (beta:=radix) radix b precision pred
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hpredCan, rfl, hradix, hprecision, hvNum⟩
  change _root_.F2R (beta:=radix) pred +
      Fulp (beta:=radix) b radix precision pred = 0
  rw [hfulp, hpred_eq]
  simp [_root_.F2R, FloatSpec.Core.Defs.F2R]

noncomputable def MinOrMax3_aux_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (z : ℝ) (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `MinOrMax3_aux`, the nonpositive side of the zero case. -/
theorem MinOrMax3_aux {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (z : ℝ) (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b p ∧
        Fcanonic (beta:=beta) radix b p ∧
        _root_.F2R (beta:=beta) p = 0 ∧
        z ≤ 0 ∧
        -z < Fulp (beta:=beta) b radix precision
          (FPred (beta:=beta) b radix precision p) ∧
        beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (MinOrMax3_aux_check (beta:=beta) b radix precision z p) : Id Unit)
    ⦃⇓_ => ⌜MinOrMax (beta:=beta) b radix z p⌝⦄ := by
  intro h
  rcases h with
    ⟨hpBound, hpCan, hpZero, hzNonpos, hdist, hbeta, hradix, hprecision, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, MinOrMax3_aux_check,
    Id.run, ULift.up_down]
  right
  constructor
  · exact hpBound
  constructor
  · linarith
  · intro f hfBound hz_le_f
    let nf : FloatSpec.Core.Defs.FlocqFloat beta :=
      Fnormalize (beta:=beta) radix b precision f
    let pred : FloatSpec.Core.Defs.FlocqFloat beta :=
      FPred (beta:=beta) b radix precision p
    have hnf_val :
        _root_.F2R (beta:=beta) nf = _root_.F2R (beta:=beta) f := by
      have h := FnormalizeCorrect (beta:=beta) radix b precision f
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
        Id.run, ULift.up_down, nf, Int.cast_ofNat] using h ⟨hbeta, hradix⟩
    have hnf_can : Fcanonic (beta:=beta) radix b nf := by
      have h := FnormalizeCanonic (beta:=beta) radix b precision f
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
        Id.run, ULift.up_down, Fbounded', nf, Int.cast_ofNat] using
        h ⟨hfBound, hfBound, hprecision, hradix, hvNum⟩
    have hpred_can : Fcanonic (beta:=beta) radix b pred := by
      have h := FPredCanonic (beta:=beta) b radix precision p
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FPredCanonic_check,
        Id.run, ULift.up_down, pred, Int.cast_ofNat] using
        h ⟨hpCan, hradix, hprecision, hvNum⟩
    have hsucc_eq :
        FSucc (beta:=beta) b radix precision pred = p := by
      have h := FSucPred (beta:=beta) b radix precision p
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FSucPred_check,
        Id.run, ULift.up_down, pred, Int.cast_ofNat] using
        h ⟨hpCan, hradix, hprecision, hvNum⟩
    have hstep :
        _root_.F2R (beta:=beta) pred +
          Fulp (beta:=beta) b radix precision pred = 0 := by
      have h := FpredUlpZero (beta:=beta) b radix precision p
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FpredUlpZero_check,
        Id.run, ULift.up_down, pred, Int.cast_ofNat] using
        h ⟨hpCan, hpZero, hbeta, hradix, hprecision, hvNum⟩
    have hpred_lt_z : _root_.F2R (beta:=beta) pred < z := by
      linarith
    have hpred_lt_nf :
        _root_.F2R (beta:=beta) pred < _root_.F2R (beta:=beta) nf := by
      rw [hnf_val]
      exact lt_of_lt_of_le hpred_lt_z hz_le_f
    have hsucc_le_nf :
        _root_.F2R (beta:=beta)
            (FSucc (beta:=beta) b radix precision pred) ≤
          _root_.F2R (beta:=beta) nf := by
      have h := FSuccProp (beta:=beta) b radix precision pred nf
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FSuccProp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hbeta, hradix, hprecision, hvNum, hpred_can, hnf_can, hpred_lt_nf⟩
    calc
      _root_.F2R (beta:=beta) p =
          _root_.F2R (beta:=beta)
            (FSucc (beta:=beta) b radix precision pred) := by
              rw [hsucc_eq]
      _ ≤ _root_.F2R (beta:=beta) nf := hsucc_le_nf
      _ = _root_.F2R (beta:=beta) f := hnf_val

noncomputable def MinOrMax3_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (z : ℝ) (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `MinOrMax3`, the signed zero-valued closeness lemma. -/
theorem MinOrMax3 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (z : ℝ) (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b p ∧
        Fcanonic (beta:=beta) radix b p ∧
        _root_.F2R (beta:=beta) p = 0 ∧
        |z - _root_.F2R (beta:=beta) p| <
          Fulp (beta:=beta) b radix precision
            (FPred (beta:=beta) b radix precision p) ∧
        beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (MinOrMax3_check (beta:=beta) b radix precision z p) : Id Unit)
    ⦃⇓_ => ⌜MinOrMax (beta:=beta) b radix z p⌝⦄ := by
  intro h
  rcases h with ⟨hpBound, hpCan, hpZero, hdist, hbeta, hradix, hprecision, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, MinOrMax3_check,
    Id.run, ULift.up_down]
  by_cases hzNonpos : z ≤ 0
  · have hdist_neg :
        -z < Fulp (beta:=beta) b radix precision
          (FPred (beta:=beta) b radix precision p) := by
      have hdist_abs :
          |z| < Fulp (beta:=beta) b radix precision
            (FPred (beta:=beta) b radix precision p) := by
        rw [hpZero, sub_zero] at hdist
        exact hdist
      simpa [abs_of_nonpos hzNonpos] using hdist_abs
    have h := MinOrMax3_aux (beta:=beta) b radix precision z p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinOrMax3_aux_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hpBound, hpCan, hpZero, hzNonpos, hdist_neg,
        hbeta, hradix, hprecision, hvNum⟩
  · have hzPos : 0 < z := lt_of_not_ge hzNonpos
    have hvNumPos : 0 < b.vNum := by
      rw [hvNum, Zpower_nat]
      exact pow_pos (by omega : 0 < radix) precision
    have hpZeroFloat : p = Fzero beta (-b.dExp) := by
      have h := FcanonicZeroEq (beta:=beta) b radix p
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicZeroEq_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hpCan, hpZero, hbeta, hradix, hvNumPos⟩
    have hFopp_eq : Fopp (beta:=beta) p = p := by
      rw [hpZeroFloat]
      simp [Fzero, Fopp, FloatSpec.Calc.Operations.Fopp]
    have hFoppBound : Fbounded (beta:=beta) b (Fopp (beta:=beta) p) := by
      have h := oppBounded (beta:=beta) b p
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, oppBounded_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h hpBound
    have hFoppCan : Fcanonic (beta:=beta) radix b (Fopp (beta:=beta) p) := by
      have h := FcanonicFopp (beta:=beta) radix b p
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFopp_check, Int.cast_ofNat] using h hpCan
    have hFoppZero : _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) = 0 := by
      rw [hFopp_eq, hpZero]
    have hdist_pos :
        -(-z) < Fulp (beta:=beta) b radix precision
          (FPred (beta:=beta) b radix precision (Fopp (beta:=beta) p)) := by
      have hz_sub_nonneg : 0 ≤ z - _root_.F2R (beta:=beta) p := by linarith
      have hz_lt :
          z < Fulp (beta:=beta) b radix precision
            (FPred (beta:=beta) b radix precision p) := by
        have hdist_abs :
            |z| < Fulp (beta:=beta) b radix precision
              (FPred (beta:=beta) b radix precision p) := by
          rw [hpZero, sub_zero] at hdist
          exact hdist
        simpa [abs_of_nonneg (le_of_lt hzPos)] using hdist_abs
      simpa [hFopp_eq] using hz_lt
    have haux : MinOrMax (beta:=beta) b radix (-z) (Fopp (beta:=beta) p) := by
      have h := MinOrMax3_aux (beta:=beta) b radix precision (-z) (Fopp (beta:=beta) p)
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinOrMax3_aux_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hFoppBound, hFoppCan, hFoppZero, by linarith, hdist_pos,
          hbeta, hradix, hprecision, hvNum⟩
    have h := MinOrMax_Fopp (beta:=beta) b radix z p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinOrMax_Fopp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h haux

noncomputable def Axpy_aux2_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (a x y t u : FloatSpec.Core.Defs.FlocqFloat beta)
    (a1 x1 y1 : ℝ) : Unit :=
  ()

noncomputable def Axpy_aux1_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (a x y t u : FloatSpec.Core.Defs.FlocqFloat beta)
    (a1 x1 y1 : ℝ) : Unit :=
  ()

noncomputable def Axpy_aux3_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (a x y t u : FloatSpec.Core.Defs.FlocqFloat beta)
    (a1 x1 y1 : ℝ) : Unit :=
  ()

noncomputable def Axpy_aux1_aux3_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (a x t u : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

noncomputable def Axpy_aux1_aux1_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (a x t u : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

noncomputable def AxpyPos_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (a x y t u : FloatSpec.Core.Defs.FlocqFloat beta)
    (a1 x1 y1 : ℝ) : Unit :=
  ()

noncomputable def Axpy_tFlessu_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (a x y t u : FloatSpec.Core.Defs.FlocqFloat beta)
    (a1 x1 y1 : ℝ) : Unit :=
  ()

noncomputable def Axpy_tFlessu_zero_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (a x y t u : FloatSpec.Core.Defs.FlocqFloat beta)
    (a1 x1 y1 : ℝ) : Unit :=
  ()

/-- Coq: `FLess`.

The neighbor closer to zero: successor for negative floats, predecessor
otherwise. -/
noncomputable def FLess {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    FloatSpec.Core.Defs.FlocqFloat beta :=
  if _root_.F2R (beta:=beta) p < 0 then
    FSucc (beta:=beta) b radix precision p
  else
    FPred (beta:=beta) b radix precision p

noncomputable def FLessBounded_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq `FLess` boundedness case split.

Upstream discharges boundedness of the neighbor selected by `FLess` by
splitting on the sign of `u`, then calling `FBoundedSuc` or `FBoundedPred`.
This helper packages that exact local step for the `UlpFlessuGe` path. -/
theorem FLessBounded {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b p ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (FLessBounded_check (beta:=beta) b radix precision p) : Id Unit)
    ⦃⇓_ => ⌜Fbounded (beta:=beta) b
      (FLess (beta:=beta) b radix precision p)⌝⦄ := by
  intro h
  rcases h with ⟨hpBound, hradix, hprecision, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, FLessBounded_check,
    Id.run, ULift.up_down]
  by_cases hpNeg : _root_.F2R (beta:=beta) p < 0
  · have h := FBoundedSuc (beta:=beta) b radix precision p
    simpa [FLess, hpNeg] using
      h ⟨hpBound, hradix, hprecision, hvNum⟩
  · have h := FBoundedPred (beta:=beta) b radix precision p
    simpa [FLess, hpNeg] using
      h ⟨hpBound, hradix, hprecision, hvNum⟩

noncomputable def FulpGe_FLess_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq `UlpFlessuGe` local step: the `FulpGe` bound for the neighbor selected
by `FLess`. -/
theorem FulpGe_FLess {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b p ∧ beta = radix ∧ 1 < radix ∧
        precision ≠ 0 ∧ b.vNum = Zpower_nat radix precision⌝⦄
    (pure (FulpGe_FLess_check (beta:=beta) b radix precision p) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta)
          (FLess (beta:=beta) b radix precision p)| ≤
        ((radix : ℝ) ^ (precision : Int) - 1) *
          Fulp (beta:=beta) b radix precision
            (FLess (beta:=beta) b radix precision p)⌝⦄ := by
  intro h
  rcases h with ⟨hpBound, hbeta, hradix, hprecision, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, FulpGe_FLess_check,
    Id.run, ULift.up_down]
  have hlessBound :
      Fbounded (beta:=beta) b
        (FLess (beta:=beta) b radix precision p) := by
    have h := FLessBounded (beta:=beta) b radix precision p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FLessBounded_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hpBound, hradix, hprecision, hvNum⟩
  have h := FulpGe (beta:=beta) radix b precision
    (FLess (beta:=beta) b radix precision p)
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpGe_check,
    Id.run, ULift.up_down, Int.cast_ofNat] using
    h ⟨hlessBound, hbeta, hradix, hprecision, hvNum⟩

noncomputable def UlpFlessuGe_final_scale_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) (E : ℝ) : Unit :=
  ()

/-- Coq `UlpFlessuGe`, final scale step.

After the real-arithmetic part of the upstream proof reduces the target to a
multiple of `|FLess p|`, `FulpGe` on the selected neighbor turns that bound into
the required quarter-ulp estimate. -/
theorem UlpFlessuGe_final_scale {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) (E : ℝ) :
    ⦃⌜Fbounded (beta:=beta) b p ∧ beta = radix ∧ 1 < radix ∧
        precision ≠ 0 ∧ b.vNum = Zpower_nat radix precision ∧
        E ≤ (4 * ((radix : ℝ) ^ (precision : Int) - 1))⁻¹ *
          |_root_.F2R (beta:=beta)
            (FLess (beta:=beta) b radix precision p)|⌝⦄
    (pure (UlpFlessuGe_final_scale_check
      (beta:=beta) b radix precision p E) : Id Unit)
    ⦃⇓_ => ⌜E ≤ (1 / 4 : ℝ) *
        Fulp (beta:=beta) b radix precision
          (FLess (beta:=beta) b radix precision p)⌝⦄ := by
  intro h
  rcases h with ⟨hpBound, hbeta, hradix, hprecision, hvNum, hE⟩
  simp only [wp, PostCond.noThrow, pure, UlpFlessuGe_final_scale_check,
    Id.run, ULift.up_down]
  let D : ℝ := (radix : ℝ) ^ (precision : Int) - 1
  let less : FloatSpec.Core.Defs.FlocqFloat beta :=
    FLess (beta:=beta) b radix precision p
  let U : ℝ := Fulp (beta:=beta) b radix precision less
  have hradix_gt_one : (1 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradix
  have hprecision_pos : (0 : Int) < (precision : Int) := by
    exact_mod_cast Nat.pos_of_ne_zero hprecision
  have hD_pos : 0 < D := by
    have hpow_lt :
        (radix : ℝ) ^ (0 : Int) < (radix : ℝ) ^ (precision : Int) := by
      exact zpow_lt_zpow_right₀ hradix_gt_one hprecision_pos
    have hpow_gt_one : 1 < (radix : ℝ) ^ (precision : Int) := by
      simpa using hpow_lt
    exact sub_pos.mpr hpow_gt_one
  have hD_nonneg : 0 ≤ D := le_of_lt hD_pos
  have hD_ne : D ≠ 0 := ne_of_gt hD_pos
  have hscale_nonneg : 0 ≤ (4 * D)⁻¹ := by
    exact inv_nonneg.mpr (mul_nonneg (by norm_num : (0 : ℝ) ≤ 4) hD_nonneg)
  have hless_bound :
      |_root_.F2R (beta:=beta) less| ≤ D * U := by
    have hfg := FulpGe_FLess (beta:=beta) b radix precision p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpGe_FLess_check,
      Id.run, ULift.up_down, D, less, U, Int.cast_ofNat] using
      hfg ⟨hpBound, hbeta, hradix, hprecision, hvNum⟩
  have hscaled :
      (4 * D)⁻¹ * |_root_.F2R (beta:=beta) less| ≤
        (4 * D)⁻¹ * (D * U) :=
    mul_le_mul_of_nonneg_left hless_bound hscale_nonneg
  have hcoef :
      (4 * D)⁻¹ * (D * U) = (1 / 4 : ℝ) * U := by
    field_simp [hD_ne]
  calc
    E ≤ (4 * D)⁻¹ * |_root_.F2R (beta:=beta) less| := by
      simpa [D, less] using hE
    _ ≤ (4 * D)⁻¹ * (D * U) := hscaled
    _ = (1 / 4 : ℝ) * U := hcoef

noncomputable def UlpFlessuGe_aux_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

noncomputable def FulpSucCan_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FulpSucCan` — the successor step from a canonical float is at most
one normalized ulp. -/
theorem FulpSucCan {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fcanonic (beta:=beta) radix b p ∧ beta = radix ∧ 1 < radix ∧
        precision ≠ 0 ∧ b.vNum = Zpower_nat radix precision⌝⦄
    (pure (FulpSucCan_check (beta:=beta) radix b precision p) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta)
          (FSucc (beta:=beta) b radix precision p) -
        _root_.F2R (beta:=beta) p ≤
        Fulp (beta:=beta) b radix precision p⌝⦄ := by
  intro h
  rcases h with ⟨hpCan, hbeta, hradix, hprecision, hvNum⟩
  simp [wp, PostCond.noThrow, pure, FulpSucCan_check]
  have hfulp :
      Fulp (beta:=beta) b radix precision p = (radix : ℝ) ^ p.Fexp := by
    have h := CanonicFulp (beta:=beta) radix b precision p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hpCan, hbeta, hradix, hprecision, hvNum⟩
  subst beta
  rw [hfulp]
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (by omega : (0 : Int) < radix)
  have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
  have hbase_ge_one : (1 : ℝ) ≤ (radix : ℝ) := by
    exact_mod_cast (le_of_lt hradix)
  have hnorm_mul :
      (nNormMin radix precision : ℝ) * (radix : ℝ) = (b.vNum : ℝ) := by
    have hsucc : precision - 1 + 1 = precision := Nat.sub_one_add_one hprecision
    have hpow_succ : radix ^ precision = radix ^ (precision - 1) * radix := by
      rw [← hsucc]
      exact pow_succ radix (precision - 1)
    rw [hvNum, Zpower_nat, nNormMin, hpow_succ]
    norm_num [Int.cast_mul]
  unfold FSucc
  by_cases htop : p.Fnum = pPred b.vNum
  · simp only [htop, ↓reduceIte, _root_.F2R, FloatSpec.Core.Defs.F2R,
      FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp]
    have hpow :
        (radix : ℝ) ^ (p.Fexp + 1) =
          (radix : ℝ) ^ p.Fexp * (radix : ℝ) := by
      rw [zpow_add₀ hradix_ne, zpow_one]
    have hstep :
        (nNormMin radix precision : ℝ) * (radix : ℝ) ^ (p.Fexp + 1) -
            (pPred b.vNum : ℝ) * (radix : ℝ) ^ p.Fexp =
          (radix : ℝ) ^ p.Fexp := by
      rw [hpow]
      have hpPred : (pPred b.vNum : ℝ) = (b.vNum : ℝ) - 1 := by
        simp [pPred]
      rw [hpPred]
      calc
        (nNormMin radix precision : ℝ) *
              ((radix : ℝ) ^ p.Fexp * (radix : ℝ)) -
            ((b.vNum : ℝ) - 1) * (radix : ℝ) ^ p.Fexp
            =
              ((nNormMin radix precision : ℝ) * (radix : ℝ) -
                  ((b.vNum : ℝ) - 1)) *
                (radix : ℝ) ^ p.Fexp := by ring
        _ = ((b.vNum : ℝ) - ((b.vNum : ℝ) - 1)) *
              (radix : ℝ) ^ p.Fexp := by rw [hnorm_mul]
        _ = (radix : ℝ) ^ p.Fexp := by ring
    nlinarith [hstep]
  · simp only [htop, ↓reduceIte]
    by_cases hlow : p.Fnum = -nNormMin radix precision
    · simp only [hlow, ↓reduceIte]
      by_cases hexp : p.Fexp = -b.dExp
      · simp only [hexp, ↓reduceIte, _root_.F2R, FloatSpec.Core.Defs.F2R,
          FloatSpec.Core.Defs.FlocqFloat.Fnum,
        FloatSpec.Core.Defs.FlocqFloat.Fexp]
        have hbranch :
            ((-nNormMin radix precision + 1 : Int) : ℝ) *
                (radix : ℝ) ^ (-b.dExp) =
              (radix : ℝ) ^ (-b.dExp) +
                ((-nNormMin radix precision : Int) : ℝ) *
                  (radix : ℝ) ^ (-b.dExp) := by
          norm_num [Int.cast_add, Int.cast_neg]
          ring
        rw [hlow]
        exact le_of_eq hbranch
      · simp only [hexp, ↓reduceIte, _root_.F2R, FloatSpec.Core.Defs.F2R,
          FloatSpec.Core.Defs.FlocqFloat.Fnum,
          FloatSpec.Core.Defs.FlocqFloat.Fexp]
        have hpow :
            (radix : ℝ) ^ p.Fexp =
              (radix : ℝ) ^ (p.Fexp - 1) * (radix : ℝ) := by
          have hsum : (p.Fexp - 1) + 1 = p.Fexp := by omega
          simpa [hsum] using
            (zpow_add₀ hradix_ne (p.Fexp - 1) (1 : Int))
        have hstep :
            (-(pPred b.vNum) : ℝ) * (radix : ℝ) ^ (p.Fexp - 1) -
                (-(nNormMin radix precision) : ℝ) * (radix : ℝ) ^ p.Fexp =
              (radix : ℝ) ^ (p.Fexp - 1) := by
          rw [hpow]
          have hpPred : (pPred b.vNum : ℝ) = (b.vNum : ℝ) - 1 := by
            simp [pPred]
          rw [hpPred]
          calc
            -((b.vNum : ℝ) - 1) * (radix : ℝ) ^ (p.Fexp - 1) -
                -(nNormMin radix precision : ℝ) *
                    ((radix : ℝ) ^ (p.Fexp - 1) * (radix : ℝ))
                =
                  ((nNormMin radix precision : ℝ) * (radix : ℝ) -
                      ((b.vNum : ℝ) - 1)) *
                    (radix : ℝ) ^ (p.Fexp - 1) := by ring
            _ = ((b.vNum : ℝ) - ((b.vNum : ℝ) - 1)) *
                  (radix : ℝ) ^ (p.Fexp - 1) := by rw [hnorm_mul]
            _ = (radix : ℝ) ^ (p.Fexp - 1) := by ring
        have hpow_le :
            (radix : ℝ) ^ (p.Fexp - 1) ≤ (radix : ℝ) ^ p.Fexp :=
          zpow_le_zpow_right₀ hbase_ge_one (by omega : p.Fexp - 1 ≤ p.Fexp)
        have htarget :
            ((-pPred b.vNum : Int) : ℝ) * (radix : ℝ) ^ (p.Fexp - 1) -
                ((-nNormMin radix precision : Int) : ℝ) *
                  (radix : ℝ) ^ p.Fexp =
              (radix : ℝ) ^ (p.Fexp - 1) := by
          simpa [Int.cast_neg] using hstep
        have hdiff_le :
            ((-pPred b.vNum : Int) : ℝ) * (radix : ℝ) ^ (p.Fexp - 1) -
                ((-nNormMin radix precision : Int) : ℝ) *
                  (radix : ℝ) ^ p.Fexp ≤
              (radix : ℝ) ^ p.Fexp := by
          rw [htarget]
          exact hpow_le
        rw [hlow]
        nlinarith [hdiff_le]
    · simp only [hlow, ↓reduceIte, _root_.F2R, FloatSpec.Core.Defs.F2R,
        FloatSpec.Core.Defs.FlocqFloat.Fnum,
        FloatSpec.Core.Defs.FlocqFloat.Fexp]
      rw [show p.Fnum + 1 = p.Fnum + 1 by rfl]
      have hbranch :
          ((p.Fnum + 1 : Int) : ℝ) * (radix : ℝ) ^ p.Fexp =
            (radix : ℝ) ^ p.Fexp + (p.Fnum : ℝ) * (radix : ℝ) ^ p.Fexp := by
        norm_num [Int.cast_add]
        ring
      exact le_of_eq hbranch

noncomputable def FulpPredCan_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FulpPredCan` — the predecessor step from a canonical float is at most
one normalized ulp. -/
theorem FulpPredCan {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fcanonic (beta:=beta) radix b p ∧ beta = radix ∧ 1 < radix ∧
        precision ≠ 0 ∧ b.vNum = Zpower_nat radix precision⌝⦄
    (pure (FulpPredCan_check (beta:=beta) radix b precision p) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) p -
        _root_.F2R (beta:=beta)
          (FPred (beta:=beta) b radix precision p) ≤
        Fulp (beta:=beta) b radix precision p⌝⦄ := by
  intro h
  rcases h with ⟨hpCan, hbeta, hradix, hprecision, hvNum⟩
  simp [wp, PostCond.noThrow, pure, FulpPredCan_check]
  have hfulp :
      Fulp (beta:=beta) b radix precision p = (radix : ℝ) ^ p.Fexp := by
    have h := CanonicFulp (beta:=beta) radix b precision p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hpCan, hbeta, hradix, hprecision, hvNum⟩
  subst beta
  rw [hfulp]
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (by omega : (0 : Int) < radix)
  have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
  have hbase_ge_one : (1 : ℝ) ≤ (radix : ℝ) := by
    exact_mod_cast (le_of_lt hradix)
  have hnorm_mul :
      (nNormMin radix precision : ℝ) * (radix : ℝ) = (b.vNum : ℝ) := by
    have hsucc : precision - 1 + 1 = precision := Nat.sub_one_add_one hprecision
    have hpow_succ : radix ^ precision = radix ^ (precision - 1) * radix := by
      rw [← hsucc]
      exact pow_succ radix (precision - 1)
    rw [hvNum, Zpower_nat, nNormMin, hpow_succ]
    norm_num [Int.cast_mul]
  unfold FPred
  by_cases htop : p.Fnum = -pPred b.vNum
  · simp only [htop, ↓reduceIte, _root_.F2R, FloatSpec.Core.Defs.F2R,
      FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp]
    have hpow :
        (radix : ℝ) ^ (p.Fexp + 1) =
          (radix : ℝ) ^ p.Fexp * (radix : ℝ) := by
      rw [zpow_add₀ hradix_ne, zpow_one]
    have hstep :
        (-(pPred b.vNum) : ℝ) * (radix : ℝ) ^ p.Fexp -
            (-(nNormMin radix precision) : ℝ) * (radix : ℝ) ^ (p.Fexp + 1) =
          (radix : ℝ) ^ p.Fexp := by
      rw [hpow]
      have hpPred : (pPred b.vNum : ℝ) = (b.vNum : ℝ) - 1 := by
        simp [pPred]
      rw [hpPred]
      calc
        -((b.vNum : ℝ) - 1) * (radix : ℝ) ^ p.Fexp -
            -(nNormMin radix precision : ℝ) *
              ((radix : ℝ) ^ p.Fexp * (radix : ℝ))
            =
              ((nNormMin radix precision : ℝ) * (radix : ℝ) -
                  ((b.vNum : ℝ) - 1)) *
                (radix : ℝ) ^ p.Fexp := by ring
        _ = ((b.vNum : ℝ) - ((b.vNum : ℝ) - 1)) *
              (radix : ℝ) ^ p.Fexp := by rw [hnorm_mul]
        _ = (radix : ℝ) ^ p.Fexp := by ring
    have htarget :
        ((-pPred b.vNum : Int) : ℝ) * (radix : ℝ) ^ p.Fexp =
          (radix : ℝ) ^ p.Fexp +
            ((-nNormMin radix precision : Int) : ℝ) *
              (radix : ℝ) ^ (p.Fexp + 1) := by
      have hstep' :
          ((-pPred b.vNum : Int) : ℝ) * (radix : ℝ) ^ p.Fexp -
              ((-nNormMin radix precision : Int) : ℝ) *
                (radix : ℝ) ^ (p.Fexp + 1) =
            (radix : ℝ) ^ p.Fexp := by
        simpa [Int.cast_neg] using hstep
      nlinarith [hstep']
    exact le_of_eq htarget
  · simp only [htop, ↓reduceIte]
    by_cases hlow : p.Fnum = nNormMin radix precision
    · simp only [hlow, ↓reduceIte]
      by_cases hexp : p.Fexp = -b.dExp
      · simp only [hexp, ↓reduceIte, _root_.F2R, FloatSpec.Core.Defs.F2R,
          FloatSpec.Core.Defs.FlocqFloat.Fnum,
          FloatSpec.Core.Defs.FlocqFloat.Fexp]
        have hbranch :
            (nNormMin radix precision : ℝ) * (radix : ℝ) ^ (-b.dExp) =
              (radix : ℝ) ^ (-b.dExp) +
                ((nNormMin radix precision - 1 : Int) : ℝ) *
                  (radix : ℝ) ^ (-b.dExp) := by
          norm_num [Int.cast_sub]
          ring
        rw [hlow]
        exact le_of_eq hbranch
      · simp only [hexp, ↓reduceIte, _root_.F2R, FloatSpec.Core.Defs.F2R,
          FloatSpec.Core.Defs.FlocqFloat.Fnum,
          FloatSpec.Core.Defs.FlocqFloat.Fexp]
        have hpow :
            (radix : ℝ) ^ p.Fexp =
              (radix : ℝ) ^ (p.Fexp - 1) * (radix : ℝ) := by
          have hsum : (p.Fexp - 1) + 1 = p.Fexp := by omega
          simpa [hsum] using
            (zpow_add₀ hradix_ne (p.Fexp - 1) (1 : Int))
        have hstep :
            (nNormMin radix precision : ℝ) * (radix : ℝ) ^ p.Fexp -
                (pPred b.vNum : ℝ) * (radix : ℝ) ^ (p.Fexp - 1) =
              (radix : ℝ) ^ (p.Fexp - 1) := by
          rw [hpow]
          have hpPred : (pPred b.vNum : ℝ) = (b.vNum : ℝ) - 1 := by
            simp [pPred]
          rw [hpPred]
          calc
            (nNormMin radix precision : ℝ) *
                  ((radix : ℝ) ^ (p.Fexp - 1) * (radix : ℝ)) -
                ((b.vNum : ℝ) - 1) * (radix : ℝ) ^ (p.Fexp - 1)
                =
                  ((nNormMin radix precision : ℝ) * (radix : ℝ) -
                      ((b.vNum : ℝ) - 1)) *
                    (radix : ℝ) ^ (p.Fexp - 1) := by ring
            _ = ((b.vNum : ℝ) - ((b.vNum : ℝ) - 1)) *
                  (radix : ℝ) ^ (p.Fexp - 1) := by rw [hnorm_mul]
            _ = (radix : ℝ) ^ (p.Fexp - 1) := by ring
        have hpow_le :
            (radix : ℝ) ^ (p.Fexp - 1) ≤ (radix : ℝ) ^ p.Fexp :=
          zpow_le_zpow_right₀ hbase_ge_one (by omega : p.Fexp - 1 ≤ p.Fexp)
        have hdiff_le :
            (nNormMin radix precision : ℝ) * (radix : ℝ) ^ p.Fexp -
                (pPred b.vNum : ℝ) * (radix : ℝ) ^ (p.Fexp - 1) ≤
              (radix : ℝ) ^ p.Fexp := by
          rw [hstep]
          exact hpow_le
        rw [hlow]
        nlinarith [hdiff_le]
    · simp only [hlow, ↓reduceIte, _root_.F2R, FloatSpec.Core.Defs.F2R,
        FloatSpec.Core.Defs.FlocqFloat.Fnum,
        FloatSpec.Core.Defs.FlocqFloat.Fexp]
      rw [show p.Fnum - 1 = p.Fnum - 1 by rfl]
      have hbranch :
          (p.Fnum : ℝ) * (radix : ℝ) ^ p.Fexp =
            (radix : ℝ) ^ p.Fexp +
              ((p.Fnum - 1 : Int) : ℝ) * (radix : ℝ) ^ p.Fexp := by
        norm_num [Int.cast_sub]
        ring
      exact le_of_eq hbranch

noncomputable def FulpSuc_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FulpSuc` — the normalized successor step from a bounded float is at
most one normalized ulp.

This states the expanded Flocq payload using `FSucc (Fnormalize p)`, because the
early local `FNSucc` alias is still a lightweight parity helper. -/
theorem FulpSuc {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b p ∧ beta = radix ∧ 1 < radix ∧
        precision ≠ 0 ∧ b.vNum = Zpower_nat radix precision⌝⦄
    (pure (FulpSuc_check (beta:=beta) radix b precision p) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta)
          (FSucc (beta:=beta) b radix precision
            (Fnormalize (beta:=beta) radix b precision p)) -
        _root_.F2R (beta:=beta) p ≤
        Fulp (beta:=beta) b radix precision p⌝⦄ := by
  intro h
  rcases h with ⟨hpBound, hbeta, hradix, hprecision, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, FulpSuc_check,
    Id.run, ULift.up_down]
  let np := Fnormalize (beta:=beta) radix b precision p
  have hnpBound : Fbounded (beta:=beta) b np := by
    have h := FnormalizeBounded (beta:=beta) radix b precision p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeBounded_check,
      Id.run, ULift.up_down, Fbounded', np, Int.cast_ofNat] using
      h ⟨hpBound, hpBound, hprecision, hradix, hvNum⟩
  have hnpCan : Fcanonic (beta:=beta) radix b np := by
    have h := FnormalizeCanonic (beta:=beta) radix b precision p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
      Id.run, ULift.up_down, Fbounded', np, Int.cast_ofNat] using
      h ⟨hpBound, hpBound, hprecision, hradix, hvNum⟩
  have hnpCorrect :
      _root_.F2R (beta:=beta) np = _root_.F2R (beta:=beta) p := by
    have h := FnormalizeCorrect (beta:=beta) radix b precision p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
      Id.run, ULift.up_down, np, Int.cast_ofNat] using h ⟨hbeta, hradix⟩
  have hcan := FulpSucCan (beta:=beta) radix b precision np
  have hstep :
      _root_.F2R (beta:=beta) (FSucc (beta:=beta) b radix precision np) -
        _root_.F2R (beta:=beta) np ≤
        Fulp (beta:=beta) b radix precision np := by
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpSucCan_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      hcan ⟨hnpCan, hbeta, hradix, hprecision, hvNum⟩
  have hfulp :
      Fulp (beta:=beta) b radix precision np =
        Fulp (beta:=beta) b radix precision p := by
    have hcomp := FulpComp (beta:=beta) radix b precision np p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpComp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      hcomp ⟨hnpBound, hpBound, hnpCorrect, hbeta, hradix, hprecision, hvNum⟩
  rw [← hnpCorrect]
  exact le_trans hstep (le_of_eq hfulp)

noncomputable def FulpPred_check {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `FulpPred` — the normalized predecessor step from a bounded float is
at most one normalized ulp.

As with `FulpSuc`, this uses the expanded normalized-neighbor expression rather
than the older lightweight `FNPred` alias. -/
theorem FulpPred {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b p ∧ beta = radix ∧ 1 < radix ∧
        precision ≠ 0 ∧ b.vNum = Zpower_nat radix precision⌝⦄
    (pure (FulpPred_check (beta:=beta) radix b precision p) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) p -
        _root_.F2R (beta:=beta)
          (FPred (beta:=beta) b radix precision
            (Fnormalize (beta:=beta) radix b precision p)) ≤
        Fulp (beta:=beta) b radix precision p⌝⦄ := by
  intro h
  rcases h with ⟨hpBound, hbeta, hradix, hprecision, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, FulpPred_check,
    Id.run, ULift.up_down]
  let np := Fnormalize (beta:=beta) radix b precision p
  have hnpBound : Fbounded (beta:=beta) b np := by
    have h := FnormalizeBounded (beta:=beta) radix b precision p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeBounded_check,
      Id.run, ULift.up_down, Fbounded', np, Int.cast_ofNat] using
      h ⟨hpBound, hpBound, hprecision, hradix, hvNum⟩
  have hnpCan : Fcanonic (beta:=beta) radix b np := by
    have h := FnormalizeCanonic (beta:=beta) radix b precision p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
      Id.run, ULift.up_down, Fbounded', np, Int.cast_ofNat] using
      h ⟨hpBound, hpBound, hprecision, hradix, hvNum⟩
  have hnpCorrect :
      _root_.F2R (beta:=beta) np = _root_.F2R (beta:=beta) p := by
    have h := FnormalizeCorrect (beta:=beta) radix b precision p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
      Id.run, ULift.up_down, np, Int.cast_ofNat] using h ⟨hbeta, hradix⟩
  have hcan := FulpPredCan (beta:=beta) radix b precision np
  have hstep :
      _root_.F2R (beta:=beta) np -
        _root_.F2R (beta:=beta) (FPred (beta:=beta) b radix precision np) ≤
        Fulp (beta:=beta) b radix precision np := by
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpPredCan_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      hcan ⟨hnpCan, hbeta, hradix, hprecision, hvNum⟩
  have hfulp :
      Fulp (beta:=beta) b radix precision np =
        Fulp (beta:=beta) b radix precision p := by
    have hcomp := FulpComp (beta:=beta) radix b precision np p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpComp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      hcomp ⟨hnpBound, hpBound, hnpCorrect, hbeta, hradix, hprecision, hvNum⟩
  rw [← hnpCorrect]
  exact le_trans hstep (le_of_eq hfulp)

/-- Coq: `RoundedModeUlp_from_rounded_payload` — under a rounded mode `P` and `P p q`,
`|p - q| < Fulp q`.

The proof follows the upstream min/max split. If `q` is the lower rounded
endpoint, `p` must lie strictly below the normalized successor of `q`; if `q`
is the upper endpoint, it must lie strictly above the normalized predecessor.
The local statement uses `RoundedModeP_float`, the Coq-matching rounded-mode
package carrying the min/max payload needed for this argument. -/
theorem RoundedModeUlp_from_rounded_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p : ℝ) (q : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜RoundedModeP_float (beta:=beta) b radix P ∧ P p q ∧
        beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (RoundedModeUlp_check (beta:=beta) b radix precision P p q) : Id Unit)
    ⦃⇓_ => ⌜|p - _root_.F2R q| < Fulp (beta:=beta) b radix precision q⌝⦄ := by
  intro h
  rcases h with ⟨hRounded, hP, hbeta, hradix, hprecision, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, RoundedModeUlp_check,
    Id.run, ULift.up_down]
  have hMinOrMax := hRounded.2.2.1
  by_cases hpq : p = _root_.F2R (beta:=beta) q
  · have hfulp_pos : 0 < Fulp (beta:=beta) b radix precision q := by
      have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
        exact_mod_cast (by omega : (0 : Int) < radix)
      unfold Fulp
      exact zpow_pos hradix_pos _
    rw [hpq, sub_self, abs_zero]
    exact hfulp_pos
  rcases hMinOrMax p q hP with hMin | hMax
  · let nq := Fnormalize (beta:=beta) radix b precision q
    let succ := FSucc (beta:=beta) b radix precision nq
    rcases hMin with ⟨hqBounded, hq_le_p, hGreatest⟩
    have hnq_can : Fcanonic (beta:=beta) radix b nq := by
      have hcan := FnormalizeCanonic (beta:=beta) radix b precision q
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
        Id.run, ULift.up_down, Fbounded', nq, Int.cast_ofNat] using
        hcan ⟨hqBounded, hqBounded, hprecision, hradix, hvNum⟩
    have hsucc_can : Fcanonic (beta:=beta) radix b succ := by
      have hcan := FSuccCanonic (beta:=beta) b radix precision nq
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FSuccCanonic_check,
        Id.run, ULift.up_down, succ, Int.cast_ofNat] using
        hcan ⟨hnq_can, hradix, hprecision, hvNum⟩
    have hsucc_bound : Fbounded (beta:=beta) b succ := by
      have hb := FcanonicBound (beta:=beta) radix b succ
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicBound_check,
        Id.run, ULift.up_down, Fbounded', Int.cast_ofNat] using hb hsucc_can
    have hnq_val :
        _root_.F2R (beta:=beta) nq = _root_.F2R (beta:=beta) q := by
      have hc := FnormalizeCorrect (beta:=beta) radix b precision q
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
        Id.run, ULift.up_down, nq, Int.cast_ofNat] using hc ⟨hbeta, hradix⟩
    have hsucc_gt :
        _root_.F2R (beta:=beta) q < _root_.F2R (beta:=beta) succ := by
      have hlt := FSuccLt (beta:=beta) b radix precision nq
      have hlt' :
          _root_.F2R (beta:=beta) nq <
            _root_.F2R (beta:=beta) succ := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FSuccLt_check,
          Id.run, ULift.up_down, succ, Int.cast_ofNat] using hlt ⟨hbeta, hradix, hprecision, hvNum⟩
      simpa [hnq_val] using hlt'
    have hp_lt_succ : p < _root_.F2R (beta:=beta) succ := by
      by_contra hnot
      have hsucc_le_p : _root_.F2R (beta:=beta) succ ≤ p := le_of_not_gt hnot
      have hsucc_le_q : _root_.F2R (beta:=beta) succ ≤ _root_.F2R (beta:=beta) q :=
        hGreatest succ hsucc_bound hsucc_le_p
      exact not_lt_of_ge hsucc_le_q hsucc_gt
    have hstep :
        _root_.F2R (beta:=beta) succ - _root_.F2R (beta:=beta) q ≤
          Fulp (beta:=beta) b radix precision q := by
      have hs := FulpSuc (beta:=beta) radix b precision q
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpSuc_check,
        Id.run, ULift.up_down, nq, succ, Int.cast_ofNat] using
        hs ⟨hqBounded, hbeta, hradix, hprecision, hvNum⟩
    have hdist_lt :
        p - _root_.F2R (beta:=beta) q <
          _root_.F2R (beta:=beta) succ - _root_.F2R (beta:=beta) q := by
      linarith
    have habs :
        |p - _root_.F2R (beta:=beta) q| =
          p - _root_.F2R (beta:=beta) q := abs_of_nonneg (by linarith)
    rw [habs]
    exact lt_of_lt_of_le hdist_lt hstep
  · let nq := Fnormalize (beta:=beta) radix b precision q
    let pred := FPred (beta:=beta) b radix precision nq
    rcases hMax with ⟨hqBounded, hp_le_q, hLeast⟩
    have hnq_can : Fcanonic (beta:=beta) radix b nq := by
      have hcan := FnormalizeCanonic (beta:=beta) radix b precision q
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
        Id.run, ULift.up_down, Fbounded', nq, Int.cast_ofNat] using
        hcan ⟨hqBounded, hqBounded, hprecision, hradix, hvNum⟩
    have hpred_can : Fcanonic (beta:=beta) radix b pred := by
      have hcan := FPredCanonic (beta:=beta) b radix precision nq
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FPredCanonic_check,
        Id.run, ULift.up_down, pred, Int.cast_ofNat] using
        hcan ⟨hnq_can, hradix, hprecision, hvNum⟩
    have hpred_bound : Fbounded (beta:=beta) b pred := by
      have hb := FcanonicBound (beta:=beta) radix b pred
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicBound_check,
        Id.run, ULift.up_down, Fbounded', Int.cast_ofNat] using hb hpred_can
    have hnq_val :
        _root_.F2R (beta:=beta) nq = _root_.F2R (beta:=beta) q := by
      have hc := FnormalizeCorrect (beta:=beta) radix b precision q
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
        Id.run, ULift.up_down, nq, Int.cast_ofNat] using hc ⟨hbeta, hradix⟩
    have hpred_lt :
        _root_.F2R (beta:=beta) pred < _root_.F2R (beta:=beta) q := by
      have hlt := FPredLt (beta:=beta) b radix precision nq
      have hlt' :
          _root_.F2R (beta:=beta) pred <
            _root_.F2R (beta:=beta) nq := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FPredLt_check,
          Id.run, ULift.up_down, pred, Int.cast_ofNat] using hlt ⟨hbeta, hradix, hprecision, hvNum⟩
      simpa [hnq_val] using hlt'
    have hpred_lt_p : _root_.F2R (beta:=beta) pred < p := by
      by_contra hnot
      have hp_le_pred : p ≤ _root_.F2R (beta:=beta) pred := le_of_not_gt hnot
      have hq_le_pred : _root_.F2R (beta:=beta) q ≤ _root_.F2R (beta:=beta) pred :=
        hLeast pred hpred_bound hp_le_pred
      exact not_lt_of_ge hq_le_pred hpred_lt
    have hstep :
        _root_.F2R (beta:=beta) q - _root_.F2R (beta:=beta) pred ≤
          Fulp (beta:=beta) b radix precision q := by
      have hp := FulpPred (beta:=beta) radix b precision q
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpPred_check,
        Id.run, ULift.up_down, nq, pred, Int.cast_ofNat] using
        hp ⟨hqBounded, hbeta, hradix, hprecision, hvNum⟩
    have hdist_lt :
        _root_.F2R (beta:=beta) q - p <
          _root_.F2R (beta:=beta) q - _root_.F2R (beta:=beta) pred := by
      linarith
    have habs :
        |p - _root_.F2R (beta:=beta) q| =
          _root_.F2R (beta:=beta) q - p := by
      rw [abs_of_nonpos (by linarith : p - _root_.F2R (beta:=beta) q ≤ 0)]
      ring
    rw [habs]
    exact lt_of_lt_of_le hdist_lt hstep

/-- Coq: `RoundedModeErrorExpStrict_from_rounded_payload` — if `p` is the rounded value of `x`
and the represented error `q = x - p` is nonzero, then the error exponent is
strictly below the rounded float exponent. -/
theorem RoundedModeErrorExpStrict_from_rounded_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) (x : ℝ) :
    ⦃⌜RoundedModeP_float (beta:=beta) b radix P ∧
        Fbounded (beta:=beta) b p ∧ Fbounded (beta:=beta) b q ∧
        P x p ∧ _root_.F2R (beta:=beta) q = x - _root_.F2R (beta:=beta) p ∧
        _root_.F2R (beta:=beta) q ≠ 0 ∧
        beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (RoundedModeErrorExpStrict_check (beta:=beta) b radix precision P p q x) :
      Id Unit)
    ⦃⇓_ => ⌜q.Fexp < p.Fexp⌝⦄ := by
  intro h
  rcases h with
    ⟨hRounded, hpBound, _hqBound, hPxp, hqVal, hqNe,
      hbeta, hradix, hprecision, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, RoundedModeErrorExpStrict_check,
    Id.run, ULift.up_down]
  have hβ : 1 < beta := by omega
  have hβgeR : (1 : ℝ) ≤ (beta : ℝ) := by
    exact_mod_cast (le_of_lt hβ)
  have hUnit_le_abs :
      _root_.F2R
          (FloatSpec.Core.Defs.FlocqFloat.mk (beta:=beta) 1 q.Fexp) ≤
        _root_.F2R (beta:=beta) (Fabs (beta:=beta) q) := by
    have h := RleFexpFabs (beta:=beta) q
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RleFexpFabs_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hβ, hqNe⟩
  have hAbs_eq :
      _root_.F2R (beta:=beta) (Fabs (beta:=beta) q) =
        |_root_.F2R (beta:=beta) q| := by
    have h := Fabs_correct (beta:=beta) q
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fabs_correct_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hβ
  have hRoundUlp :
      |x - _root_.F2R (beta:=beta) p| <
        Fulp (beta:=beta) b radix precision p := by
    have h := RoundedModeUlp_from_rounded_payload (beta:=beta) b radix precision P x p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RoundedModeUlp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hRounded, hPxp, hbeta, hradix, hprecision, hvNum⟩
  have hAbs_lt_fulp :
      _root_.F2R (beta:=beta) (Fabs (beta:=beta) q) <
        Fulp (beta:=beta) b radix precision p := by
    calc
      _root_.F2R (beta:=beta) (Fabs (beta:=beta) q)
          = |_root_.F2R (beta:=beta) q| := hAbs_eq
      _ = |x - _root_.F2R (beta:=beta) p| := by rw [hqVal]
      _ < Fulp (beta:=beta) b radix precision p := hRoundUlp
  have hUnit_lt_fulp :
      _root_.F2R
          (FloatSpec.Core.Defs.FlocqFloat.mk (beta:=beta) 1 q.Fexp) <
        Fulp (beta:=beta) b radix precision p :=
    lt_of_le_of_lt hUnit_le_abs hAbs_lt_fulp
  have hFulp_le_pow :
      Fulp (beta:=beta) b radix precision p ≤ (beta : ℝ) ^ p.Fexp := by
    have h := FulpLe (beta:=beta) radix b precision p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpLe_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hpBound, hbeta, hradix⟩
  have hPow_lt :
      (beta : ℝ) ^ q.Fexp < (beta : ℝ) ^ p.Fexp := by
    have hUnit_lt_pow :
        _root_.F2R
            (FloatSpec.Core.Defs.FlocqFloat.mk (beta:=beta) 1 q.Fexp) <
          (beta : ℝ) ^ p.Fexp :=
      lt_of_lt_of_le hUnit_lt_fulp hFulp_le_pow
    simpa [_root_.F2R, FloatSpec.Core.Defs.F2R] using hUnit_lt_pow
  have h := Zlt_powerRZ (beta : ℝ) q.Fexp p.Fexp
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Zlt_powerRZ_check,
    Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hβgeR, hPow_lt⟩

/-- Coq: `errorBoundedMultExpPos_from_split_payload`.

For nonnegative bounded inputs, a rounded product has a bounded same-value
representative `r` with exponent between the exact product exponent and the
standard upper bound, plus a bounded error `s` at the exact product exponent.
-/
theorem errorBoundedMultExpPos_from_split_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (hbeta : beta = radix) (hradix : 1 < radix) (hprecision : precision ≠ 0)
    (hvNum_gt : 1 < bo.vNum)
    (hvNum : bo.vNum = Zpower_nat radix precision)
    (hRoundedFloat : RoundedModeP_float (beta:=beta) bo radix P)
    (p q pq : FloatSpec.Core.Defs.FlocqFloat beta)
    (hp : Fbounded (beta:=beta) bo p)
    (hq : Fbounded (beta:=beta) bo q)
    (hp_nonneg : 0 ≤ _root_.F2R (beta:=beta) p)
    (hq_nonneg : 0 ≤ _root_.F2R (beta:=beta) q)
    (hP : P (_root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q) pq)
    (hexp : -bo.dExp ≤ p.Fexp + q.Fexp) :
    ∃ r s : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo r ∧
      Fbounded (beta:=beta) bo s ∧
      _root_.F2R (beta:=beta) r = _root_.F2R (beta:=beta) pq ∧
      _root_.F2R (beta:=beta) s =
        _root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) r ∧
      s.Fexp = p.Fexp + q.Fexp ∧
      s.Fexp ≤ r.Fexp ∧
      r.Fexp ≤ (precision : Int) + (p.Fexp + q.Fexp) := by
  subst beta
  let z : Int := p.Fexp + q.Fexp
  let prod : ℝ := _root_.F2R (beta:=radix) p * _root_.F2R (beta:=radix) q
  have hradix_pos_int : (0 : Int) < radix := by omega
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by exact_mod_cast hradix_pos_int
  have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
  have hvNum_pos : 0 < bo.vNum := by omega
  have hz_bound : -bo.dExp ≤ z := by simpa [z] using hexp
  have hpqBounded : Fbounded (beta:=radix) bo pq := by
    exact RoundedModeP_float_bounded bo radix P hRoundedFloat hP
  have hprod_expand :
      prod = (((p.Fnum * q.Fnum : Int) : ℝ) * (radix : ℝ) ^ z) := by
    dsimp [prod, z]
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
      FloatSpec.Core.Defs.FlocqFloat.Fnum,
      FloatSpec.Core.Defs.FlocqFloat.Fexp, Int.cast_mul]
    calc
      (p.Fnum : ℝ) * (radix : ℝ) ^ p.Fexp *
          ((q.Fnum : ℝ) * (radix : ℝ) ^ q.Fexp)
          = ((p.Fnum : ℝ) * (q.Fnum : ℝ)) *
              ((radix : ℝ) ^ p.Fexp * (radix : ℝ) ^ q.Fexp) := by ring
      _ = ((p.Fnum : ℝ) * (q.Fnum : ℝ)) * (radix : ℝ) ^ (p.Fexp + q.Fexp) := by
            rw [zpow_add₀ hradix_ne]
      _ = ((p.Fnum : ℝ) * (q.Fnum : ℝ)) * (radix : ℝ) ^ z := by rfl
  by_cases hzero : prod - _root_.F2R (beta:=radix) pq = 0
  · have hprod_eq_pq : prod = _root_.F2R (beta:=radix) pq := by linarith
    by_cases hpq_zero : _root_.F2R (beta:=radix) pq = 0
    · let zero : FloatSpec.Core.Defs.FlocqFloat radix := Fzero radix z
      have hzero_val : _root_.F2R (beta:=radix) zero = 0 := by
        have h := FzeroisReallyZero (beta:=radix) z
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FzeroisReallyZero_check,
          Id.run, ULift.up_down, zero, Int.cast_ofNat] using h True.intro
      have hzero_bound : Fbounded (beta:=radix) bo zero := by
        dsimp [zero, Fzero, Fbounded]
        exact ⟨by simpa using hvNum_pos, hz_bound⟩
      refine ⟨zero, zero, hzero_bound, hzero_bound, ?_, ?_, by rfl, le_rfl, ?_⟩
      · rw [hzero_val, hpq_zero]
      · rw [hzero_val]
        linarith
      · dsimp [zero, Fzero, z]
        omega
    · have hpq_repr :
          ((p.Fnum * q.Fnum : Int) : ℝ) =
            (pq.Fnum : ℝ) * (radix : ℝ) ^ (pq.Fexp - z) := by
        have hzpow_ne : (radix : ℝ) ^ z ≠ 0 := ne_of_gt (zpow_pos hradix_pos z)
        apply mul_right_cancel₀ hzpow_ne
        calc
          ((p.Fnum * q.Fnum : Int) : ℝ) * (radix : ℝ) ^ z
              = prod := hprod_expand.symm
          _ = _root_.F2R (beta:=radix) pq := hprod_eq_pq
          _ = (pq.Fnum : ℝ) * (radix : ℝ) ^ pq.Fexp := by
                simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
                  FloatSpec.Core.Defs.FlocqFloat.Fnum,
                  FloatSpec.Core.Defs.FlocqFloat.Fexp]
          _ = ((pq.Fnum : ℝ) * (radix : ℝ) ^ (pq.Fexp - z)) *
                (radix : ℝ) ^ z := by
                rw [mul_assoc, ← zpow_add₀ hradix_ne]
                congr 2
                omega
      rcases errorBoundedMultExp_aux bo radix precision hradix hprecision hvNum
          p.Fnum q.Fnum hp.1 hq.1
          ⟨pq.Fnum, pq.Fexp - z, hpq_repr, hpqBounded.1⟩ with
        ⟨nx, ex, hnx_repr, hnx_bound, hex_nonneg, hex_le⟩
      let r : FloatSpec.Core.Defs.FlocqFloat radix := ⟨nx, ex + z⟩
      let s : FloatSpec.Core.Defs.FlocqFloat radix := Fzero radix z
      have hs_val_zero : _root_.F2R (beta:=radix) s = 0 := by
        have h := FzeroisReallyZero (beta:=radix) z
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FzeroisReallyZero_check,
          Id.run, ULift.up_down, s, Int.cast_ofNat] using h True.intro
      have hs_bound : Fbounded (beta:=radix) bo s := by
        dsimp [s, Fzero, Fbounded]
        exact ⟨by simpa using hvNum_pos, hz_bound⟩
      have hr_bound : Fbounded (beta:=radix) bo r := by
        dsimp [r, Fbounded]
        exact ⟨hnx_bound, by omega⟩
      have hr_val_prod : _root_.F2R (beta:=radix) r = prod := by
        dsimp [r]
        simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
          FloatSpec.Core.Defs.FlocqFloat.Fnum,
          FloatSpec.Core.Defs.FlocqFloat.Fexp]
        calc
          (nx : ℝ) * (radix : ℝ) ^ (ex + z)
              = ((nx : ℝ) * (radix : ℝ) ^ ex) * (radix : ℝ) ^ z := by
                rw [zpow_add₀ hradix_ne]
                ring
          _ = ((p.Fnum * q.Fnum : Int) : ℝ) * (radix : ℝ) ^ z := by
                rw [← hnx_repr]
          _ = prod := hprod_expand.symm
      refine ⟨r, s, hr_bound, hs_bound, ?_, ?_, by rfl, ?_, ?_⟩
      · exact hr_val_prod.trans hprod_eq_pq
      · rw [hs_val_zero, hr_val_prod]
        linarith
      · dsimp [r, s, Fzero, z]
        omega
      · dsimp [r, z]
        omega
  · have hErr := errorBoundedMultPos_from_rounded_payload (beta:=radix) bo radix precision P p q pq
    rcases (by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, errorBoundedMultPos_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        hErr ⟨rfl, hradix, hprecision, hvNum, hRoundedFloat, hp, hq,
          hp_nonneg, hq_nonneg, hz_bound, hP⟩) with
      ⟨s, hs_val, hs_bound, hs_exp⟩
    have hs_ne : _root_.F2R (beta:=radix) s ≠ 0 := by
      intro hs0
      exact hzero (by simpa [prod, hs_val] using hs0)
    have hs_lt_pq : s.Fexp < pq.Fexp := by
      have h := RoundedModeErrorExpStrict_from_rounded_payload (beta:=radix) bo radix precision P pq s prod
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RoundedModeErrorExpStrict_check,
        Id.run, ULift.up_down, prod, Int.cast_ofNat] using
        h ⟨hRoundedFloat, hpqBounded, hs_bound, hP, by simpa [prod] using hs_val,
          hs_ne, rfl, hradix, hprecision, hvNum⟩
    have hz_le_pq : z ≤ pq.Fexp := by
      rw [hs_exp] at hs_lt_pq
      dsimp [z]
      omega
    let upperExp : Int := (precision : Int) + z
    let upper : FloatSpec.Core.Defs.FlocqFloat radix := ⟨bo.vNum - 1, upperExp⟩
    have hupperBounded : Fbounded (beta:=radix) bo upper := by
      constructor
      · have hnonneg : 0 ≤ bo.vNum - 1 := by omega
        rw [abs_of_nonneg hnonneg]
        omega
      · dsimp [upper, upperExp, z]
        omega
    have hpAbsLe :
        |_root_.F2R (beta:=radix) p| ≤
          _root_.F2R (beta:=radix)
            (⟨bo.vNum - 1, p.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat radix) := by
      have h := maxMax1_from_expanded_bound_payload (beta:=radix) bo p p.Fexp
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, maxMax1_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using h ⟨hp, le_rfl, hp, hradix⟩
    have hqAbsLe :
        |_root_.F2R (beta:=radix) q| ≤
          _root_.F2R (beta:=radix)
            (⟨bo.vNum - 1, q.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat radix) := by
      have h := maxMax1_from_expanded_bound_payload (beta:=radix) bo q q.Fexp
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, maxMax1_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using h ⟨hq, le_rfl, hq, hradix⟩
    have hbaseP_nonneg :
        0 ≤ _root_.F2R (beta:=radix)
          (⟨bo.vNum - 1, p.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat radix) := by
      have hnum : (0 : Int) ≤ bo.vNum - 1 := by omega
      have hnumR : (0 : ℝ) ≤ ((bo.vNum - 1 : Int) : ℝ) := by exact_mod_cast hnum
      have hpow_nonneg : 0 ≤ (radix : ℝ) ^ p.Fexp :=
        le_of_lt (zpow_pos hradix_pos p.Fexp)
      simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
        FloatSpec.Core.Defs.FlocqFloat.Fnum,
        FloatSpec.Core.Defs.FlocqFloat.Fexp]
      exact mul_nonneg hnumR hpow_nonneg
    have hprodAbsLeBase :
        |_root_.F2R (beta:=radix) p * _root_.F2R (beta:=radix) q| ≤
          _root_.F2R (beta:=radix)
            (⟨bo.vNum - 1, p.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat radix) *
          _root_.F2R (beta:=radix)
            (⟨bo.vNum - 1, q.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat radix) := by
      rw [abs_mul]
      exact mul_le_mul hpAbsLe hqAbsLe (abs_nonneg _) hbaseP_nonneg
    have hpred_le_pow :
        ((bo.vNum - 1 : Int) : ℝ) ≤ (radix : ℝ) ^ (precision : Int) := by
      have hle_int : bo.vNum - 1 ≤ bo.vNum := by omega
      have hle_real : ((bo.vNum - 1 : Int) : ℝ) ≤ (bo.vNum : ℝ) := by
        exact_mod_cast hle_int
      have hv_cast : (bo.vNum : ℝ) = (radix : ℝ) ^ precision := by
        simp [hvNum, Zpower_nat]
      have hv_cast_int : (bo.vNum : ℝ) = (radix : ℝ) ^ (precision : Int) := by
        rw [hv_cast, ← zpow_natCast]
      exact le_trans hle_real (le_of_eq hv_cast_int)
    have hbaseProdLeUpper :
        _root_.F2R (beta:=radix)
            (⟨bo.vNum - 1, p.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat radix) *
          _root_.F2R (beta:=radix)
            (⟨bo.vNum - 1, q.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat radix) ≤
          _root_.F2R (beta:=radix) upper := by
      let A : ℝ := ((bo.vNum - 1 : Int) : ℝ)
      have hA_nonneg : 0 ≤ A := by
        have hnonneg : (0 : Int) ≤ bo.vNum - 1 := by omega
        simpa [A] using
          (show (0 : ℝ) ≤ ((bo.vNum - 1 : Int) : ℝ) by exact_mod_cast hnonneg)
      have hpow_z_nonneg : 0 ≤ (radix : ℝ) ^ z :=
        le_of_lt (zpow_pos hradix_pos z)
      have hA_sq_le : A * A ≤ A * (radix : ℝ) ^ (precision : Int) := by
        exact mul_le_mul_of_nonneg_left (by simpa [A] using hpred_le_pow) hA_nonneg
      have hmul_exp :
          (radix : ℝ) ^ p.Fexp * (radix : ℝ) ^ q.Fexp =
            (radix : ℝ) ^ z := by
        rw [← zpow_add₀ hradix_ne]
      have hupper_exp :
          (radix : ℝ) ^ upperExp =
            (radix : ℝ) ^ (precision : Int) * (radix : ℝ) ^ z := by
        dsimp [upperExp]
        exact zpow_add₀ hradix_ne (precision : Int) z
      simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
        FloatSpec.Core.Defs.FlocqFloat.Fnum,
        FloatSpec.Core.Defs.FlocqFloat.Fexp, upper]
      calc
        A * (radix : ℝ) ^ p.Fexp * (A * (radix : ℝ) ^ q.Fexp)
            = (A * A) * (radix : ℝ) ^ z := by
              rw [← hmul_exp]
              ring
        _ ≤ (A * (radix : ℝ) ^ (precision : Int)) * (radix : ℝ) ^ z :=
              mul_le_mul_of_nonneg_right hA_sq_le hpow_z_nonneg
        _ = A * (radix : ℝ) ^ upperExp := by
              rw [hupper_exp]
              ring
    have hprodAbsLeUpper :
        |prod| ≤ _root_.F2R (beta:=radix) upper := by
      dsimp [prod]
      exact le_trans hprodAbsLeBase hbaseProdLeUpper
    have hpqAbsLeUpper :
        |_root_.F2R (beta:=radix) pq| ≤ _root_.F2R (beta:=radix) upper := by
      have h := RoundAbsMonotoner_from_rounded_payload (beta:=radix) bo radix P prod pq upper
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RoundAbsMonotoner_check, Id.run,
        ULift.up_down, prod, Int.cast_ofNat] using h ⟨hRoundedFloat, hupperBounded, hP, hprodAbsLeUpper⟩
    rcases eqExpMax_between (beta:=radix) bo radix pq z upperExp rfl hradix
        hvNum_gt hpqBounded hz_bound hz_le_pq (by dsimp [upperExp]; omega)
        (by simpa [upper] using hpqAbsLeUpper) with
      ⟨r, hr_bound, hr_val, hz_le_r, hr_upper⟩
    refine ⟨r, s, hr_bound, hs_bound, hr_val, ?_, hs_exp, ?_, hr_upper⟩
    · rw [hs_val, hr_val]
    · rw [hs_exp]
      exact hz_le_r

private theorem errorBoundedMultExp_from_error {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (hbeta : beta = radix) (hradix : 1 < radix) (hprecision : precision ≠ 0)
    (hvNum_gt : 1 < bo.vNum)
    (hvNum : bo.vNum = Zpower_nat radix precision)
    (hRoundedFloat : RoundedModeP_float (beta:=beta) bo radix P)
    (p q pq s : FloatSpec.Core.Defs.FlocqFloat beta)
    (hp : Fbounded (beta:=beta) bo p)
    (hq : Fbounded (beta:=beta) bo q)
    (hpqBounded : Fbounded (beta:=beta) bo pq)
    (hs_bound : Fbounded (beta:=beta) bo s)
    (hP : P (_root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q) pq)
    (hexp : -bo.dExp ≤ p.Fexp + q.Fexp)
    (hs_val :
      _root_.F2R (beta:=beta) s =
        _root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) pq)
    (hs_exp : s.Fexp = p.Fexp + q.Fexp) :
    ∃ r t : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo r ∧
      Fbounded (beta:=beta) bo t ∧
      _root_.F2R (beta:=beta) r = _root_.F2R (beta:=beta) pq ∧
      _root_.F2R (beta:=beta) t =
        _root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) r ∧
      t.Fexp = p.Fexp + q.Fexp ∧
      t.Fexp ≤ r.Fexp ∧
      r.Fexp ≤ (precision : Int) + (p.Fexp + q.Fexp) := by
  subst beta
  let z : Int := p.Fexp + q.Fexp
  let prod : ℝ := _root_.F2R (beta:=radix) p * _root_.F2R (beta:=radix) q
  have hradix_pos_int : (0 : Int) < radix := by omega
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by exact_mod_cast hradix_pos_int
  have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
  have hvNum_pos : 0 < bo.vNum := by omega
  have hz_bound : -bo.dExp ≤ z := by simpa [z] using hexp
  have hs_val_prod :
      _root_.F2R (beta:=radix) s = prod - _root_.F2R (beta:=radix) pq := by
    simpa [prod] using hs_val
  have hprod_expand :
      prod = (((p.Fnum * q.Fnum : Int) : ℝ) * (radix : ℝ) ^ z) := by
    dsimp [prod, z]
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
      FloatSpec.Core.Defs.FlocqFloat.Fnum,
      FloatSpec.Core.Defs.FlocqFloat.Fexp, Int.cast_mul]
    calc
      (p.Fnum : ℝ) * (radix : ℝ) ^ p.Fexp *
          ((q.Fnum : ℝ) * (radix : ℝ) ^ q.Fexp)
          = ((p.Fnum : ℝ) * (q.Fnum : ℝ)) *
              ((radix : ℝ) ^ p.Fexp * (radix : ℝ) ^ q.Fexp) := by ring
      _ = ((p.Fnum : ℝ) * (q.Fnum : ℝ)) * (radix : ℝ) ^ (p.Fexp + q.Fexp) := by
            rw [zpow_add₀ hradix_ne]
      _ = ((p.Fnum : ℝ) * (q.Fnum : ℝ)) * (radix : ℝ) ^ z := by rfl
  by_cases hs_zero : _root_.F2R (beta:=radix) s = 0
  · have hprod_eq_pq : prod = _root_.F2R (beta:=radix) pq := by
      rw [hs_val_prod] at hs_zero
      linarith
    by_cases hpq_zero : _root_.F2R (beta:=radix) pq = 0
    · let zero : FloatSpec.Core.Defs.FlocqFloat radix := Fzero radix z
      have hzero_val : _root_.F2R (beta:=radix) zero = 0 := by
        have h := FzeroisReallyZero (beta:=radix) z
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FzeroisReallyZero_check,
          Id.run, ULift.up_down, zero, Int.cast_ofNat] using h True.intro
      have hzero_bound : Fbounded (beta:=radix) bo zero := by
        dsimp [zero, Fzero, Fbounded]
        exact ⟨by simpa using hvNum_pos, hz_bound⟩
      refine ⟨zero, s, hzero_bound, hs_bound, ?_, ?_, hs_exp, ?_, ?_⟩
      · rw [hzero_val, hpq_zero]
      · calc
          _root_.F2R (beta:=radix) s = 0 := hs_zero
          _ = prod - _root_.F2R (beta:=radix) zero := by
            rw [hzero_val]
            linarith
      · rw [hs_exp]
        dsimp [zero, Fzero, z]
        exact le_rfl
      · dsimp [zero, Fzero, z]
        omega
    · have hpq_repr :
          ((p.Fnum * q.Fnum : Int) : ℝ) =
            (pq.Fnum : ℝ) * (radix : ℝ) ^ (pq.Fexp - z) := by
        have hzpow_ne : (radix : ℝ) ^ z ≠ 0 := ne_of_gt (zpow_pos hradix_pos z)
        apply mul_right_cancel₀ hzpow_ne
        calc
          ((p.Fnum * q.Fnum : Int) : ℝ) * (radix : ℝ) ^ z
              = prod := hprod_expand.symm
          _ = _root_.F2R (beta:=radix) pq := hprod_eq_pq
          _ = (pq.Fnum : ℝ) * (radix : ℝ) ^ pq.Fexp := by
                simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
                  FloatSpec.Core.Defs.FlocqFloat.Fnum,
                  FloatSpec.Core.Defs.FlocqFloat.Fexp]
          _ = ((pq.Fnum : ℝ) * (radix : ℝ) ^ (pq.Fexp - z)) *
                (radix : ℝ) ^ z := by
                rw [mul_assoc, ← zpow_add₀ hradix_ne]
                congr 2
                omega
      rcases errorBoundedMultExp_aux bo radix precision hradix hprecision hvNum
          p.Fnum q.Fnum hp.1 hq.1
          ⟨pq.Fnum, pq.Fexp - z, hpq_repr, hpqBounded.1⟩ with
        ⟨nx, ex, hnx_repr, hnx_bound, _hex_nonneg, hex_le⟩
      let r : FloatSpec.Core.Defs.FlocqFloat radix := ⟨nx, ex + z⟩
      have hr_bound : Fbounded (beta:=radix) bo r := by
        dsimp [r, Fbounded]
        exact ⟨hnx_bound, by omega⟩
      have hr_val_prod : _root_.F2R (beta:=radix) r = prod := by
        dsimp [r]
        simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
          FloatSpec.Core.Defs.FlocqFloat.Fnum,
          FloatSpec.Core.Defs.FlocqFloat.Fexp]
        calc
          (nx : ℝ) * (radix : ℝ) ^ (ex + z)
              = ((nx : ℝ) * (radix : ℝ) ^ ex) * (radix : ℝ) ^ z := by
                rw [zpow_add₀ hradix_ne]
                ring
          _ = ((p.Fnum * q.Fnum : Int) : ℝ) * (radix : ℝ) ^ z := by
                rw [← hnx_repr]
          _ = prod := hprod_expand.symm
      refine ⟨r, s, hr_bound, hs_bound, ?_, ?_, hs_exp, ?_, ?_⟩
      · exact hr_val_prod.trans hprod_eq_pq
      · calc
          _root_.F2R (beta:=radix) s = 0 := hs_zero
          _ = prod - _root_.F2R (beta:=radix) r := by
            rw [hr_val_prod]
            linarith
      · rw [hs_exp]
        dsimp [r, z]
        omega
      · dsimp [r, z]
        omega
  · have hs_lt_pq : s.Fexp < pq.Fexp := by
      have h := RoundedModeErrorExpStrict_from_rounded_payload (beta:=radix) bo radix precision P pq s prod
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RoundedModeErrorExpStrict_check,
        Id.run, ULift.up_down, prod, Int.cast_ofNat] using
        h ⟨hRoundedFloat, hpqBounded, hs_bound, hP, hs_val_prod, hs_zero,
          rfl, hradix, hprecision, hvNum⟩
    have hz_le_pq : z ≤ pq.Fexp := by
      rw [hs_exp] at hs_lt_pq
      dsimp [z]
      omega
    let upperExp : Int := (precision : Int) + z
    let upper : FloatSpec.Core.Defs.FlocqFloat radix := ⟨bo.vNum - 1, upperExp⟩
    have hupperBounded : Fbounded (beta:=radix) bo upper := by
      constructor
      · have hnonneg : 0 ≤ bo.vNum - 1 := by omega
        rw [abs_of_nonneg hnonneg]
        omega
      · dsimp [upper, upperExp, z]
        omega
    have hpAbsLe :
        |_root_.F2R (beta:=radix) p| ≤
          _root_.F2R (beta:=radix)
            (⟨bo.vNum - 1, p.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat radix) := by
      have h := maxMax1_from_expanded_bound_payload (beta:=radix) bo p p.Fexp
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, maxMax1_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using h ⟨hp, le_rfl, hp, hradix⟩
    have hqAbsLe :
        |_root_.F2R (beta:=radix) q| ≤
          _root_.F2R (beta:=radix)
            (⟨bo.vNum - 1, q.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat radix) := by
      have h := maxMax1_from_expanded_bound_payload (beta:=radix) bo q q.Fexp
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, maxMax1_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using h ⟨hq, le_rfl, hq, hradix⟩
    have hbaseP_nonneg :
        0 ≤ _root_.F2R (beta:=radix)
          (⟨bo.vNum - 1, p.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat radix) := by
      have hnum : (0 : Int) ≤ bo.vNum - 1 := by omega
      have hnumR : (0 : ℝ) ≤ ((bo.vNum - 1 : Int) : ℝ) := by exact_mod_cast hnum
      have hpow_nonneg : 0 ≤ (radix : ℝ) ^ p.Fexp :=
        le_of_lt (zpow_pos hradix_pos p.Fexp)
      simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
        FloatSpec.Core.Defs.FlocqFloat.Fnum,
        FloatSpec.Core.Defs.FlocqFloat.Fexp]
      exact mul_nonneg hnumR hpow_nonneg
    have hprodAbsLeBase :
        |_root_.F2R (beta:=radix) p * _root_.F2R (beta:=radix) q| ≤
          _root_.F2R (beta:=radix)
            (⟨bo.vNum - 1, p.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat radix) *
          _root_.F2R (beta:=radix)
            (⟨bo.vNum - 1, q.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat radix) := by
      rw [abs_mul]
      exact mul_le_mul hpAbsLe hqAbsLe (abs_nonneg _) hbaseP_nonneg
    have hpred_le_pow :
        ((bo.vNum - 1 : Int) : ℝ) ≤ (radix : ℝ) ^ (precision : Int) := by
      have hle_int : bo.vNum - 1 ≤ bo.vNum := by omega
      have hle_real : ((bo.vNum - 1 : Int) : ℝ) ≤ (bo.vNum : ℝ) := by
        exact_mod_cast hle_int
      have hv_cast : (bo.vNum : ℝ) = (radix : ℝ) ^ precision := by
        simp [hvNum, Zpower_nat]
      have hv_cast_int : (bo.vNum : ℝ) = (radix : ℝ) ^ (precision : Int) := by
        rw [hv_cast, ← zpow_natCast]
      exact le_trans hle_real (le_of_eq hv_cast_int)
    have hbaseProdLeUpper :
        _root_.F2R (beta:=radix)
            (⟨bo.vNum - 1, p.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat radix) *
          _root_.F2R (beta:=radix)
            (⟨bo.vNum - 1, q.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat radix) ≤
          _root_.F2R (beta:=radix) upper := by
      let A : ℝ := ((bo.vNum - 1 : Int) : ℝ)
      have hA_nonneg : 0 ≤ A := by
        have hnonneg : (0 : Int) ≤ bo.vNum - 1 := by omega
        simpa [A] using
          (show (0 : ℝ) ≤ ((bo.vNum - 1 : Int) : ℝ) by exact_mod_cast hnonneg)
      have hpow_z_nonneg : 0 ≤ (radix : ℝ) ^ z :=
        le_of_lt (zpow_pos hradix_pos z)
      have hA_sq_le : A * A ≤ A * (radix : ℝ) ^ (precision : Int) := by
        exact mul_le_mul_of_nonneg_left (by simpa [A] using hpred_le_pow) hA_nonneg
      have hmul_exp :
          (radix : ℝ) ^ p.Fexp * (radix : ℝ) ^ q.Fexp =
            (radix : ℝ) ^ z := by
        rw [← zpow_add₀ hradix_ne]
      have hupper_exp :
          (radix : ℝ) ^ upperExp =
            (radix : ℝ) ^ (precision : Int) * (radix : ℝ) ^ z := by
        dsimp [upperExp]
        exact zpow_add₀ hradix_ne (precision : Int) z
      simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
        FloatSpec.Core.Defs.FlocqFloat.Fnum,
        FloatSpec.Core.Defs.FlocqFloat.Fexp, upper]
      calc
        A * (radix : ℝ) ^ p.Fexp * (A * (radix : ℝ) ^ q.Fexp)
            = (A * A) * (radix : ℝ) ^ z := by
              rw [← hmul_exp]
              ring
        _ ≤ (A * (radix : ℝ) ^ (precision : Int)) * (radix : ℝ) ^ z :=
              mul_le_mul_of_nonneg_right hA_sq_le hpow_z_nonneg
        _ = A * (radix : ℝ) ^ upperExp := by
              rw [hupper_exp]
              ring
    have hprodAbsLeUpper :
        |prod| ≤ _root_.F2R (beta:=radix) upper := by
      dsimp [prod]
      exact le_trans hprodAbsLeBase hbaseProdLeUpper
    have hpqAbsLeUpper :
        |_root_.F2R (beta:=radix) pq| ≤ _root_.F2R (beta:=radix) upper := by
      have h := RoundAbsMonotoner_from_rounded_payload (beta:=radix) bo radix P prod pq upper
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RoundAbsMonotoner_check, Id.run,
        ULift.up_down, prod, Int.cast_ofNat] using h ⟨hRoundedFloat, hupperBounded, hP, hprodAbsLeUpper⟩
    rcases eqExpMax_between (beta:=radix) bo radix pq z upperExp rfl hradix
        hvNum_gt hpqBounded hz_bound hz_le_pq (by dsimp [upperExp]; omega)
        (by simpa [upper] using hpqAbsLeUpper) with
      ⟨r, hr_bound, hr_val, hz_le_r, hr_upper⟩
    refine ⟨r, s, hr_bound, hs_bound, hr_val, ?_, hs_exp, ?_, hr_upper⟩
    · rw [hs_val_prod, hr_val]
    · rw [hs_exp]
      exact hz_le_r

/-- Coq: `errorBoundedMultExp_from_split_payload`.

The all-sign multiplication exponent/error theorem.  The arithmetic payload is
`errorBoundedMultPos_from_rounded_payload`/`errorBoundedMultNeg_from_rounded_payload`; this wrapper normalizes the sign
cases and then constructs the same-value rounded representative. -/
theorem errorBoundedMultExp_from_split_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (hbeta : beta = radix) (hradix : 1 < radix) (hprecision : precision ≠ 0)
    (hvNum_gt : 1 < bo.vNum)
    (hvNum : bo.vNum = Zpower_nat radix precision)
    (hRoundedFloat : RoundedModeP_float (beta:=beta) bo radix P)
    (p q pq : FloatSpec.Core.Defs.FlocqFloat beta)
    (hp : Fbounded (beta:=beta) bo p)
    (hq : Fbounded (beta:=beta) bo q)
    (hP : P (_root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q) pq)
    (hexp : -bo.dExp ≤ p.Fexp + q.Fexp) :
    ∃ r s : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo r ∧
      Fbounded (beta:=beta) bo s ∧
      _root_.F2R (beta:=beta) r = _root_.F2R (beta:=beta) pq ∧
      _root_.F2R (beta:=beta) s =
        _root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) r ∧
      s.Fexp = p.Fexp + q.Fexp ∧
      s.Fexp ≤ r.Fexp ∧
      r.Fexp ≤ (precision : Int) + (p.Fexp + q.Fexp) := by
  subst beta
  have hcompat : CompatiblePGeneric P := hRoundedFloat.2.1
  have hpqBounded : Fbounded (beta:=radix) bo pq := by
    let prod : ℝ := _root_.F2R (beta:=radix) p * _root_.F2R (beta:=radix) q
    exact RoundedModeP_float_bounded bo radix P hRoundedFloat hP
  let finish
      (s : FloatSpec.Core.Defs.FlocqFloat radix)
      (hs_bound : Fbounded (beta:=radix) bo s)
      (hs_val :
        _root_.F2R (beta:=radix) s =
          _root_.F2R (beta:=radix) p * _root_.F2R (beta:=radix) q -
            _root_.F2R (beta:=radix) pq)
      (hs_exp : s.Fexp = p.Fexp + q.Fexp) :
      ∃ r t : FloatSpec.Core.Defs.FlocqFloat radix,
        Fbounded (beta:=radix) bo r ∧
        Fbounded (beta:=radix) bo t ∧
        _root_.F2R (beta:=radix) r = _root_.F2R (beta:=radix) pq ∧
        _root_.F2R (beta:=radix) t =
          _root_.F2R (beta:=radix) p * _root_.F2R (beta:=radix) q -
            _root_.F2R (beta:=radix) r ∧
        t.Fexp = p.Fexp + q.Fexp ∧
        t.Fexp ≤ r.Fexp ∧
        r.Fexp ≤ (precision : Int) + (p.Fexp + q.Fexp) :=
    errorBoundedMultExp_from_error (beta:=radix) bo radix precision P rfl
      hradix hprecision hvNum_gt hvNum hRoundedFloat p q pq s
      hp hq hpqBounded hs_bound hP hexp hs_val hs_exp
  by_cases hp_nonneg : 0 ≤ _root_.F2R (beta:=radix) p
  · by_cases hq_nonneg : 0 ≤ _root_.F2R (beta:=radix) q
    · have hbranch := errorBoundedMultPos_from_rounded_payload (beta:=radix) bo radix precision P p q pq
      rcases (by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, errorBoundedMultPos_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          hbranch ⟨rfl, hradix, hprecision, hvNum, hRoundedFloat, hp, hq,
            hp_nonneg, hq_nonneg, hexp, hP⟩) with
        ⟨s, hs_val, hs_bound, hs_exp⟩
      exact finish s hs_bound hs_val hs_exp
    · have hq_nonpos : _root_.F2R (beta:=radix) q ≤ 0 := le_of_lt (lt_of_not_ge hq_nonneg)
      have hPswap : P (_root_.F2R (beta:=radix) q * _root_.F2R (beta:=radix) p) pq :=
        hcompat
          (_root_.F2R (beta:=radix) p * _root_.F2R (beta:=radix) q)
          (_root_.F2R (beta:=radix) q * _root_.F2R (beta:=radix) p)
          pq pq hP (by ring) rfl
      have hbranch := errorBoundedMultNeg_from_rounded_payload (beta:=radix) bo radix precision P q p pq
      rcases (by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, errorBoundedMultNeg_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          hbranch ⟨rfl, hradix, hprecision, hvNum, hRoundedFloat, hq, hp,
            hq_nonpos, hp_nonneg, by simpa [add_comm] using hexp, hPswap⟩) with
        ⟨s, hs_val, hs_bound, hs_exp⟩
      have hs_val' :
          _root_.F2R (beta:=radix) s =
            _root_.F2R (beta:=radix) p * _root_.F2R (beta:=radix) q -
              _root_.F2R (beta:=radix) pq := by
        rw [hs_val]
        ring
      have hs_exp' : s.Fexp = p.Fexp + q.Fexp := by
        rw [hs_exp]
        omega
      exact finish s hs_bound hs_val' hs_exp'
  · have hp_nonpos : _root_.F2R (beta:=radix) p ≤ 0 := le_of_lt (lt_of_not_ge hp_nonneg)
    by_cases hq_nonneg : 0 ≤ _root_.F2R (beta:=radix) q
    · have hbranch := errorBoundedMultNeg_from_rounded_payload (beta:=radix) bo radix precision P p q pq
      rcases (by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, errorBoundedMultNeg_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          hbranch ⟨rfl, hradix, hprecision, hvNum, hRoundedFloat, hp, hq,
            hp_nonpos, hq_nonneg, hexp, hP⟩) with
        ⟨s, hs_val, hs_bound, hs_exp⟩
      exact finish s hs_bound hs_val hs_exp
    · have hq_nonpos : _root_.F2R (beta:=radix) q ≤ 0 := le_of_lt (lt_of_not_ge hq_nonneg)
      have hpOppBound : Fbounded (beta:=radix) bo (Fopp (beta:=radix) p) := by
        have h := oppBounded (beta:=radix) bo p
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, oppBounded_check, Id.run,
          ULift.up_down, Int.cast_ofNat] using h hp
      have hqOppBound : Fbounded (beta:=radix) bo (Fopp (beta:=radix) q) := by
        have h := oppBounded (beta:=radix) bo q
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, oppBounded_check, Id.run,
          ULift.up_down, Int.cast_ofNat] using h hq
      have hpOppVal :
          _root_.F2R (beta:=radix) (Fopp (beta:=radix) p) =
            -_root_.F2R (beta:=radix) p := by
        have h := FloatSpec.Calc.Operations.F2R_opp (beta:=radix) p
        simpa [Fopp, _root_.F2R] using h trivial
      have hqOppVal :
          _root_.F2R (beta:=radix) (Fopp (beta:=radix) q) =
            -_root_.F2R (beta:=radix) q := by
        have h := FloatSpec.Calc.Operations.F2R_opp (beta:=radix) q
        simpa [Fopp, _root_.F2R] using h trivial
      have hpOppNonneg :
          0 ≤ _root_.F2R (beta:=radix) (Fopp (beta:=radix) p) := by
        rw [hpOppVal]
        linarith
      have hqOppNonneg :
          0 ≤ _root_.F2R (beta:=radix) (Fopp (beta:=radix) q) := by
        rw [hqOppVal]
        linarith
      have hprodOpp :
          _root_.F2R (beta:=radix) p * _root_.F2R (beta:=radix) q =
            _root_.F2R (beta:=radix) (Fopp (beta:=radix) p) *
              _root_.F2R (beta:=radix) (Fopp (beta:=radix) q) := by
        rw [hpOppVal, hqOppVal]
        ring
      have hPopp :
          P (_root_.F2R (beta:=radix) (Fopp (beta:=radix) p) *
              _root_.F2R (beta:=radix) (Fopp (beta:=radix) q)) pq :=
        hcompat
          (_root_.F2R (beta:=radix) p * _root_.F2R (beta:=radix) q)
          (_root_.F2R (beta:=radix) (Fopp (beta:=radix) p) *
            _root_.F2R (beta:=radix) (Fopp (beta:=radix) q))
          pq pq hP hprodOpp rfl
      have hbranch := errorBoundedMultPos_from_rounded_payload (beta:=radix) bo radix precision P
        (Fopp (beta:=radix) p) (Fopp (beta:=radix) q) pq
      rcases (by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, errorBoundedMultPos_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          hbranch ⟨rfl, hradix, hprecision, hvNum, hRoundedFloat,
            hpOppBound, hqOppBound, hpOppNonneg, hqOppNonneg,
            by simpa [Fopp, FloatSpec.Calc.Operations.Fopp] using hexp, hPopp⟩) with
        ⟨s, hs_val, hs_bound, hs_exp⟩
      have hs_val' :
          _root_.F2R (beta:=radix) s =
            _root_.F2R (beta:=radix) p * _root_.F2R (beta:=radix) q -
              _root_.F2R (beta:=radix) pq := by
        rw [hs_val, ← hprodOpp]
      have hs_exp' : s.Fexp = p.Fexp + q.Fexp := by
        simpa [Fopp, FloatSpec.Calc.Operations.Fopp] using hs_exp
      exact finish s hs_bound hs_val' hs_exp'

/-- Coq: `errorBoundedMultClosest_aux`.

For a closest-rounded product with nonzero residual, normalize the same-value
rounded representative from `errorBoundedMultExp_from_split_payload` so the result is canonical
while preserving the exponent bounds needed by the downstream closest theorem.
-/
theorem errorBoundedMultClosest_aux_from_format_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat) :
    beta = radix →
    1 < radix →
    precision ≠ 0 →
    1 < bo.vNum →
    bo.vNum = Zpower_nat radix precision →
    ∀ p q pq : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      Closest (beta:=beta) bo (radix : ℝ)
        (_root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q) pq →
      -bo.dExp ≤ p.Fexp + q.Fexp →
      _root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) pq ≠ 0 →
      ∃ r s : FloatSpec.Core.Defs.FlocqFloat beta,
        Fcanonic (beta:=beta) radix bo r ∧
        Fbounded (beta:=beta) bo r ∧
        Fbounded (beta:=beta) bo s ∧
        _root_.F2R (beta:=beta) r = _root_.F2R (beta:=beta) pq ∧
        _root_.F2R (beta:=beta) s =
          _root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q -
            _root_.F2R (beta:=beta) r ∧
        s.Fexp = p.Fexp + q.Fexp ∧
        s.Fexp ≤ r.Fexp ∧
        r.Fexp ≤ (precision : Int) + (p.Fexp + q.Fexp) := by
  intro hbeta hradix hprecision hvNum_gt hvNum p q pq hp hq hclosest hexp hnonzero
  subst beta
  have hBoundExp :
      ∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=radix) radix r).Fexp := by
    intro r
    exact le_trans (neg_nonpos.mpr bo.dExp_nonneg) (by simp [boundR, boundNat])
  let prod : ℝ := _root_.F2R (beta:=radix) p * _root_.F2R (beta:=radix) q
  have hvNum_pos : 0 < bo.vNum := by omega
  have hMinTotal : TotalP (isMin (beta:=radix) bo radix) := by
    intro r
    have h := MinEx_from_finite_box_payload (beta:=radix) bo radix r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hvNum_gt, hBoundExp r⟩
  have hMaxTotal : TotalP (isMax (beta:=radix) bo radix) := by
    intro r
    have h := MaxEx_from_finite_box_payload (beta:=radix) bo radix r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MaxEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hvNum_gt, hBoundExp r⟩
  have hTotal : TotalP (Closest (beta:=radix) bo (radix : ℝ)) := by
    intro r
    have h := ClosestTotal_from_extrema_payload (beta:=radix) bo radix (radix : ℝ) r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestTotal_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨hMinTotal, hMaxTotal⟩
  have hCompat : CompatiblePGeneric (Closest (beta:=radix) bo (radix : ℝ)) := by
    have h := ClosestCompatible_from_structural_payload (beta:=radix) bo (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestCompatible_check,
      PredTrans.pure, PredTrans.apply, Id.run, ULift.down, Int.cast_ofNat] using h trivial
  have hMinMax :
      MinOrMaxP_float (beta:=radix) bo radix
        (Closest (beta:=radix) bo (radix : ℝ)) := by
    have h := ClosestMinOrMax_from_split_radix_payload (beta:=radix) bo radix (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMinOrMax_check, MinOrMaxP_float,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hMono : MonotoneP_float (Closest (beta:=radix) bo (radix : ℝ)) := by
    have h := ClosestMonotone_from_split_radix_payload (beta:=radix) bo (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMonotone_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hRoundedFloat :
      RoundedModeP_float (beta:=radix) bo radix
        (Closest (beta:=radix) bo (radix : ℝ)) :=
    ⟨hTotal, hCompat, hMinMax, hMono⟩
  have hErr := errorBoundedMult (beta:=radix) bo radix precision p q pq
  rcases (by
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, errorBoundedMult_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      hErr ⟨rfl, hradix, hprecision, hvNum, hp, hq, hexp, hclosest⟩) with
    ⟨s, hs_val, hs_bound, hs_exp⟩
  rcases errorBoundedMultExp_from_error (beta:=radix) bo radix precision
      (Closest (beta:=radix) bo (radix : ℝ)) rfl hradix hprecision
      hvNum_gt hvNum hRoundedFloat p q pq s hp hq hclosest.1 hs_bound
      hclosest hexp hs_val hs_exp with
    ⟨r0, t, hr0_bound, ht_bound, hr0_val, ht_val, ht_exp, ht_le_r0, hr0_upper⟩
  let r : FloatSpec.Core.Defs.FlocqFloat radix :=
    Fnormalize (beta:=radix) radix bo precision r0
  have hr_bound : Fbounded (beta:=radix) bo r := by
    have h := FnormalizeBounded (beta:=radix) radix bo precision r0
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeBounded_check,
      Id.run, ULift.up_down, r, Int.cast_ofNat] using
      h ⟨hr0_bound, hr0_bound, hprecision, hradix, hvNum⟩
  have hr_can : Fcanonic (beta:=radix) radix bo r := by
    have h := FnormalizeCanonic (beta:=radix) radix bo precision r0
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
      Id.run, ULift.up_down, r, Int.cast_ofNat] using
      h ⟨hr0_bound, hr0_bound, hprecision, hradix, hvNum⟩
  have hr_val_r0 : _root_.F2R (beta:=radix) r = _root_.F2R (beta:=radix) r0 := by
    have h := FnormalizeCorrect (beta:=radix) radix bo precision r0
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
      Id.run, ULift.up_down, r, Int.cast_ofNat] using h ⟨rfl, hradix⟩
  have hr_val_pq : _root_.F2R (beta:=radix) r = _root_.F2R (beta:=radix) pq :=
    hr_val_r0.trans hr0_val
  have hclosest_r :
      Closest (beta:=radix) bo (radix : ℝ) prod r := by
    constructor
    · exact hr_bound
    · intro f hf
      have hdist := hclosest.2 f hf
      simpa [prod, hr_val_pq] using hdist
  have ht_val_pq :
      _root_.F2R (beta:=radix) t =
        prod - _root_.F2R (beta:=radix) pq := by
    rw [ht_val, hr0_val]
  have ht_ne : _root_.F2R (beta:=radix) t ≠ 0 := by
    intro ht_zero
    exact hnonzero (by
      rw [← ht_val_pq]
      exact ht_zero)
  have ht_val_r :
      _root_.F2R (beta:=radix) t =
        prod - _root_.F2R (beta:=radix) r := by
    rw [ht_val, hr_val_r0]
  have ht_lt_r : t.Fexp < r.Fexp := by
    have h := RoundedModeErrorExpStrict_from_rounded_payload (beta:=radix) bo radix precision
      (Closest (beta:=radix) bo (radix : ℝ)) r t prod
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RoundedModeErrorExpStrict_check,
      Id.run, ULift.up_down, prod, Int.cast_ofNat] using
      h ⟨hRoundedFloat, hr_bound, ht_bound, hclosest_r, ht_val_r, ht_ne,
        rfl, hradix, hprecision, hvNum⟩
  have hr_exp_le_r0 : r.Fexp ≤ r0.Fexp := by
    have h := FcanonicLeastExp_from_explicit_radix_payload (beta:=radix) radix bo r0 r hradix rfl hvNum_pos
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicLeastExp_check,
      ULift.down_up, Fcanonic', Int.cast_ofNat] using h ⟨hr_val_r0.symm, hr0_bound, hr_can⟩
  refine ⟨r, t, hr_can, hr_bound, ht_bound, hr_val_pq, ?_, ht_exp, ?_, ?_⟩
  · exact ht_val_r
  · exact le_of_lt ht_lt_r
  · exact le_trans hr_exp_le_r0 hr0_upper

/-- Exact exported Coq `errorBoundedMultClosest_aux` contract. -/
theorem errorBoundedMultClosest_aux {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hprecision : 1 < precision)
    (hvNum : bo.vNum = Zpower_nat radix precision) :
    ∀ p q pq : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      Closest (beta:=beta) bo (radix : ℝ)
        (_root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q) pq →
      -bo.dExp ≤ p.Fexp + q.Fexp →
      _root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) pq ≠ 0 →
      ∃ r s : FloatSpec.Core.Defs.FlocqFloat beta,
        Fcanonic (beta:=beta) radix bo r ∧
        Fbounded (beta:=beta) bo r ∧
        Fbounded (beta:=beta) bo s ∧
        _root_.F2R (beta:=beta) r = _root_.F2R (beta:=beta) pq ∧
        _root_.F2R (beta:=beta) s =
          _root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q -
            _root_.F2R (beta:=beta) r ∧
        s.Fexp = p.Fexp + q.Fexp ∧
        s.Fexp ≤ r.Fexp ∧
        r.Fexp ≤ (precision : Int) + (p.Fexp + q.Fexp) := by
  have hprecision_ne : precision ≠ 0 := by omega
  have hvNum_gt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecision_ne
  letI : ValidRadix radix := ⟨hradix⟩
  exact errorBoundedMultClosest_aux_from_format_payload (beta:=beta)
    bo radix precision hbeta hradix hprecision_ne hvNum_gt hvNum

/-- Coq: `ClosestUlp`.

The proof follows the upstream min/max split.  If `q` is the lower closest
endpoint, the closestness inequality is instantiated at the normalized
successor of `q`; if `q` is the upper endpoint, it is instantiated at the
normalized predecessor.  The local statement makes the usual Pff section
assumptions explicit because `FulpSuc`/`FulpPred` are restored with those
assumptions exposed. -/
theorem ClosestUlp {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (p : ℝ) (q : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Closest (beta:=beta) bo (radix : ℝ) p q ∧
        beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        bo.vNum = Zpower_nat radix precision⌝⦄
    (pure (ClosestUlp_check (beta:=beta) bo (radix : ℝ) p q) : Id Unit)
    ⦃⇓_ => ⌜2 * |p - _root_.F2R (beta:=beta) q| ≤
        Fulp (beta:=beta) bo radix precision q⌝⦄ := by
  intro h
  rcases h with ⟨hClosest, hbeta, hradix, hprecision, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, ClosestUlp_check,
    Id.run, ULift.up_down]
  by_cases hpq : p = _root_.F2R (beta:=beta) q
  · have hfulp_pos : 0 < Fulp (beta:=beta) bo radix precision q := by
      have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
        exact_mod_cast (by omega : (0 : Int) < radix)
      unfold Fulp
      exact zpow_pos hradix_pos _
    rw [hpq, sub_self, abs_zero]
    norm_num
    exact le_of_lt hfulp_pos
  have hMinOrMax :
      MinOrMaxP_float (beta:=beta) bo radix
        (Closest (beta:=beta) bo (radix : ℝ)) := by
    have h := ClosestMinOrMax_from_split_radix_payload (beta:=beta) bo radix (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMinOrMax_check, MinOrMaxP_float,
      Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
  rcases hMinOrMax p q hClosest with hMin | hMax
  · let nq := Fnormalize (beta:=beta) radix bo precision q
    let succ := FSucc (beta:=beta) bo radix precision nq
    have hnq_can : Fcanonic (beta:=beta) radix bo nq := by
      have h := FnormalizeCanonic (beta:=beta) radix bo precision q
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
        Id.run, ULift.up_down, Fbounded', nq, Int.cast_ofNat] using
        h ⟨hClosest.1, hClosest.1, hprecision, hradix, hvNum⟩
    have hsucc_can : Fcanonic (beta:=beta) radix bo succ := by
      have h := FSuccCanonic (beta:=beta) bo radix precision nq
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FSuccCanonic_check,
        Id.run, ULift.up_down, succ, Int.cast_ofNat] using
        h ⟨hnq_can, hradix, hprecision, hvNum⟩
    have hsucc_bound : Fbounded (beta:=beta) bo succ := by
      have h := FcanonicBound (beta:=beta) radix bo succ
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicBound_check,
        Id.run, ULift.up_down, Fbounded', Int.cast_ofNat] using h hsucc_can
    have hnq_val :
        _root_.F2R (beta:=beta) nq = _root_.F2R (beta:=beta) q := by
      have h := FnormalizeCorrect (beta:=beta) radix bo precision q
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
        Id.run, ULift.up_down, nq, Int.cast_ofNat] using h ⟨hbeta, hradix⟩
    have hsucc_gt :
        _root_.F2R (beta:=beta) q < _root_.F2R (beta:=beta) succ := by
      have h := FSuccLt (beta:=beta) bo radix precision nq
      have hlt :
          _root_.F2R (beta:=beta) nq <
            _root_.F2R (beta:=beta) succ := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FSuccLt_check,
          Id.run, ULift.up_down, succ, Int.cast_ofNat] using
          h ⟨hbeta, hradix, hprecision, hvNum⟩
      simpa [hnq_val] using hlt
    have hp_le_succ : p ≤ _root_.F2R (beta:=beta) succ := by
      by_contra hnot
      have hsucc_le_p : _root_.F2R (beta:=beta) succ ≤ p := le_of_not_ge hnot
      have hsucc_le_q : _root_.F2R (beta:=beta) succ ≤ _root_.F2R (beta:=beta) q :=
        hMin.2.2 succ hsucc_bound hsucc_le_p
      exact not_lt_of_ge hsucc_le_q hsucc_gt
    have hdist := hClosest.2 succ hsucc_bound
    have hq_le_p : _root_.F2R (beta:=beta) q ≤ p := hMin.2.1
    have hdist' :
        p - _root_.F2R (beta:=beta) q ≤
          _root_.F2R (beta:=beta) succ - p := by
      have hq_nonpos : _root_.F2R (beta:=beta) q - p ≤ 0 := by linarith
      have hsucc_nonneg : 0 ≤ _root_.F2R (beta:=beta) succ - p := by linarith
      simpa [abs_of_nonpos hq_nonpos, abs_of_nonneg hsucc_nonneg]
        using hdist
    have hstep :
        _root_.F2R (beta:=beta) succ - _root_.F2R (beta:=beta) q ≤
          Fulp (beta:=beta) bo radix precision q := by
      have h := FulpSuc (beta:=beta) radix bo precision q
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpSuc_check,
        Id.run, ULift.up_down, nq, succ, Int.cast_ofNat] using
        h ⟨hClosest.1, hbeta, hradix, hprecision, hvNum⟩
    have htwice :
        2 * (p - _root_.F2R (beta:=beta) q) ≤
          _root_.F2R (beta:=beta) succ - _root_.F2R (beta:=beta) q := by
      linarith
    have habs : |p - _root_.F2R (beta:=beta) q| =
        p - _root_.F2R (beta:=beta) q := abs_of_nonneg (by linarith)
    rw [habs]
    exact le_trans htwice hstep
  · let nq := Fnormalize (beta:=beta) radix bo precision q
    let pred := FPred (beta:=beta) bo radix precision nq
    have hnq_can : Fcanonic (beta:=beta) radix bo nq := by
      have h := FnormalizeCanonic (beta:=beta) radix bo precision q
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
        Id.run, ULift.up_down, Fbounded', nq, Int.cast_ofNat] using
        h ⟨hClosest.1, hClosest.1, hprecision, hradix, hvNum⟩
    have hpred_can : Fcanonic (beta:=beta) radix bo pred := by
      have h := FPredCanonic (beta:=beta) bo radix precision nq
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FPredCanonic_check,
        Id.run, ULift.up_down, pred, Int.cast_ofNat] using
        h ⟨hnq_can, hradix, hprecision, hvNum⟩
    have hpred_bound : Fbounded (beta:=beta) bo pred := by
      have h := FcanonicBound (beta:=beta) radix bo pred
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicBound_check,
        Id.run, ULift.up_down, Fbounded', Int.cast_ofNat] using h hpred_can
    have hnq_val :
        _root_.F2R (beta:=beta) nq = _root_.F2R (beta:=beta) q := by
      have h := FnormalizeCorrect (beta:=beta) radix bo precision q
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
        Id.run, ULift.up_down, nq, Int.cast_ofNat] using h ⟨hbeta, hradix⟩
    have hpred_lt :
        _root_.F2R (beta:=beta) pred < _root_.F2R (beta:=beta) q := by
      have h := FPredLt (beta:=beta) bo radix precision nq
      have hlt :
          _root_.F2R (beta:=beta) pred <
            _root_.F2R (beta:=beta) nq := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FPredLt_check,
          Id.run, ULift.up_down, pred, Int.cast_ofNat] using
          h ⟨hbeta, hradix, hprecision, hvNum⟩
      simpa [hnq_val] using hlt
    have hpred_le_p : _root_.F2R (beta:=beta) pred ≤ p := by
      by_contra hnot
      have hp_le_pred : p ≤ _root_.F2R (beta:=beta) pred := le_of_not_ge hnot
      have hq_le_pred : _root_.F2R (beta:=beta) q ≤ _root_.F2R (beta:=beta) pred :=
        hMax.2.2 pred hpred_bound hp_le_pred
      exact not_lt_of_ge hq_le_pred hpred_lt
    have hdist := hClosest.2 pred hpred_bound
    have hp_le_q : p ≤ _root_.F2R (beta:=beta) q := hMax.2.1
    have hdist' :
        _root_.F2R (beta:=beta) q - p ≤
          p - _root_.F2R (beta:=beta) pred := by
      have hq_nonneg : 0 ≤ _root_.F2R (beta:=beta) q - p := by linarith
      have hpred_nonpos : _root_.F2R (beta:=beta) pred - p ≤ 0 := by linarith
      simpa [abs_of_nonneg hq_nonneg, abs_of_nonpos hpred_nonpos]
        using hdist
    have hstep :
        _root_.F2R (beta:=beta) q - _root_.F2R (beta:=beta) pred ≤
          Fulp (beta:=beta) bo radix precision q := by
      have h := FulpPred (beta:=beta) radix bo precision q
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpPred_check,
        Id.run, ULift.up_down, nq, pred, Int.cast_ofNat] using
        h ⟨hClosest.1, hbeta, hradix, hprecision, hvNum⟩
    have htwice :
        2 * (_root_.F2R (beta:=beta) q - p) ≤
          _root_.F2R (beta:=beta) q - _root_.F2R (beta:=beta) pred := by
      linarith
    have habs : |p - _root_.F2R (beta:=beta) q| =
        _root_.F2R (beta:=beta) q - p := by
      rw [abs_of_nonpos (by linarith : p - _root_.F2R (beta:=beta) q ≤ 0)]
      ring
    rw [habs]
    exact le_trans htwice hstep

/-- Coq `ClosestExp`. -/
theorem ClosestExp {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : ℝ) (q : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < precision ∧
        b.vNum = Zpower_nat radix precision ∧
        Closest (beta:=beta) b (radix : ℝ) x q⌝⦄
    (pure (ClosestExp_check (beta:=beta) b radix precision x q) : Id Unit)
    ⦃⇓_ => ⌜2 * |x - _root_.F2R (beta:=beta) q| ≤
      (beta : ℝ) ^ q.Fexp⌝⦄ := by
  intro ⟨hbeta, hradix, hprecision, hvNum, hClosest⟩
  have hUlp : 2 * |x - _root_.F2R (beta:=beta) q| ≤
      Fulp (beta:=beta) b radix precision q := by
    exact ClosestUlp (beta:=beta) b radix precision x q
      ⟨hClosest, hbeta, hradix, by omega, hvNum⟩
  exact ClosestExp_from_ulp_payload (beta:=beta) b radix precision x q
    ⟨hClosest, hUlp, hbeta, hradix⟩

/-- Coq `ClosestErrorExpStrict`. -/
theorem ClosestErrorExpStrict {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) (x : ℝ) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < precision ∧
        b.vNum = Zpower_nat radix precision ∧
        Fbounded b p ∧ Fbounded b q ∧
        Closest (beta:=beta) b (radix : ℝ) x p ∧
        _root_.F2R q = x - _root_.F2R p ∧ _root_.F2R q ≠ 0⌝⦄
    (pure (ClosestErrorExpStrict_check
      (beta:=beta) b (radix : ℝ) p q x) : Id Unit)
    ⦃⇓_ => ⌜q.Fexp < p.Fexp⌝⦄ := by
  intro ⟨hbeta, hradix, hprecision, hvNum, hp, hq, hClosest, hqval, hqne⟩
  have hExp : 2 * |x - _root_.F2R (beta:=beta) p| ≤
      (beta : ℝ) ^ p.Fexp := by
    exact ClosestExp (beta:=beta) b radix precision x p
      ⟨hbeta, hradix, hprecision, hvNum, hClosest⟩
  have hbetaReal : (1 : ℝ) ≤ (beta : ℝ) := by
    subst beta
    exact_mod_cast (le_of_lt hradix)
  exact ClosestErrorExpStrict_from_exp_bound_payload
    (beta:=beta) b (radix : ℝ) p q x
      ⟨hp, hq, hClosest, hqval, hqne, hbetaReal, hExp⟩

/-- Coq: `FmultRadixInv` — a closest result for an input strictly above
`x/2` cannot lie below `x/2`. -/
theorem FmultRadixInv {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (x z : FloatSpec.Core.Defs.FlocqFloat beta) (y : ℝ) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < precision ∧
        bo.vNum = Zpower_nat radix precision ∧
        Fbounded (beta:=beta) bo x ∧
        Closest (beta:=beta) bo (radix : ℝ) y z ∧
        (1 / 2 : ℝ) * _root_.F2R (beta:=beta) x < y⌝⦄
    (pure (FmultRadixInv_check (beta:=beta) bo (radix : ℝ) x z y) : Id Unit)
    ⦃⇓_ => ⌜(1 / 2 : ℝ) * _root_.F2R (beta:=beta) x ≤
      _root_.F2R (beta:=beta) z⌝⦄ := by
  intro ⟨hbeta, hradix, hprecision, hvnum, hxBounded, hzClosest, hhalf⟩
  simp only [wp, PostCond.noThrow, pure, FmultRadixInv_check,
    Id.run, ULift.up_down]
  let half := (1 / 2 : ℝ) * _root_.F2R (beta:=beta) x
  have hvNum_gt : 1 < bo.vNum := by
    rw [hvnum, Zpower_nat]
    have hpow : radix ^ (1 : Nat) ≤ radix ^ precision :=
      pow_le_pow_right₀ (by omega : (1 : Int) ≤ radix) (by omega)
    have hradix_le : radix ≤ radix ^ precision := by
      simpa only [pow_one, Int.cast_ofNat] using hpow
    exact lt_of_lt_of_le hradix hradix_le
  have hBoundExp : -bo.dExp ≤ (boundR (beta:=beta) radix half).Fexp := by
    have hnonneg : (0 : Int) ≤ (boundR (beta:=beta) radix half).Fexp := by
      simp [boundR, boundNat]
    exact le_trans (neg_nonpos.mpr bo.dExp_nonneg) hnonneg
  have hMinExists := MinEx_from_finite_box_payload (beta:=beta) bo radix half
  have hMinPayload :
      ∃ min : FloatSpec.Core.Defs.FlocqFloat beta,
        isMin (beta:=beta) bo radix half min := by
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinEx_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      hMinExists ⟨hbeta, hradix, hvNum_gt, hBoundExp⟩
  rcases hMinPayload with ⟨min, hMin⟩
  have hMaxExists := MaxEx_from_finite_box_payload (beta:=beta) bo radix half
  have hMaxPayload :
      ∃ max : FloatSpec.Core.Defs.FlocqFloat beta,
        isMax (beta:=beta) bo radix half max := by
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MaxEx_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      hMaxExists ⟨hbeta, hradix, hvNum_gt, hBoundExp⟩
  rcases hMaxPayload with ⟨max, hMax⟩
  have hxMid : _root_.F2R (beta:=beta) x =
      _root_.F2R (beta:=beta) min + _root_.F2R (beta:=beta) max := by
    have h := div2IsBetween (beta:=beta) bo radix precision x min max
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, div2IsBetween_check,
      PredTrans.pure, PredTrans.apply, Id.run, ULift.up, ULift.down, half, Int.cast_ofNat] using
      h ⟨hbeta, hradix, hprecision, hvnum, hxBounded, hMin, hMax⟩
  have h := FmultRadixInv_from_extrema (beta:=beta) bo (radix : ℝ)
    x z min max y
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FmultRadixInv_check,
    Id.run, ULift.up_down, isMin, isMax, half, Int.cast_ofNat] using
    h ⟨hxBounded, hzClosest, hhalf, hMin, hMax, hxMid⟩

/-- Coq: `ClosestErrorBound` — the represented error of a closest rounded
value is at most half of the radix power at its exponent. -/
theorem ClosestErrorBound {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) (x : ℝ) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < precision ∧
        bo.vNum = Zpower_nat radix precision ∧
        Fbounded (beta:=beta) bo p ∧
        Closest (beta:=beta) bo (radix : ℝ) x p ∧
        _root_.F2R (beta:=beta) q = x - _root_.F2R (beta:=beta) p⌝⦄
    (pure (ClosestErrorBound_check (beta:=beta) bo (radix : ℝ) p q x) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) q| ≤
      _root_.F2R (beta:=beta)
        (FloatSpec.Core.Defs.FlocqFloat.mk (beta:=beta) 1 p.Fexp) *
        (1 / 2 : ℝ)⌝⦄ := by
  intro ⟨hbeta, hradix, hprecision, hvnum, hpBounded, hpClosest, hqValue⟩
  simp only [wp, PostCond.noThrow, pure, ClosestErrorBound_check,
    Id.run, ULift.up_down]
  have hUlp : 2 * |x - _root_.F2R (beta:=beta) p| ≤
      Fulp (beta:=beta) bo radix precision p := by
    have h := ClosestUlp (beta:=beta) bo radix precision x p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hpClosest, hbeta, hradix, by omega, hvnum⟩
  have hFulp : Fulp (beta:=beta) bo radix precision p ≤
      (beta : ℝ) ^ p.Fexp := by
    have h := FulpLe (beta:=beta) radix bo precision p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpLe_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hpBounded, hbeta, hradix⟩
  have hpow : 2 * |x - _root_.F2R (beta:=beta) p| ≤
      (beta : ℝ) ^ p.Fexp := le_trans hUlp hFulp
  have hone : _root_.F2R (beta:=beta)
      (FloatSpec.Core.Defs.FlocqFloat.mk (beta:=beta) 1 p.Fexp) =
      (beta : ℝ) ^ p.Fexp := by
    simp [_root_.F2R, FloatSpec.Core.Defs.F2R]
  rw [hqValue, hone]
  have hhalf : |x - _root_.F2R (beta:=beta) p| ≤
      (beta : ℝ) ^ p.Fexp / 2 :=
    (le_div_iff₀ (by norm_num : (0 : ℝ) < 2)).2 (by
      simpa [mul_comm] using hpow)
  simpa [div_eq_mul_inv] using hhalf

/-- Coq `Twice_EvenClosest_Round`.

At radix two, doubling a normal nearest-even result by incrementing its
exponent gives the nearest-even result for the doubled input. -/
theorem Twice_EvenClosest_Round {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) :
    ⦃⌜beta = 2 ∧ 1 < precision ∧
        bo.vNum = Zpower_nat 2 precision ∧
        -bo.dExp ≤ x.Fexp - 1 ∧
        Fnormal (beta:=beta) 2 bo x ∧
        EvenClosest (beta:=beta) bo (2 : ℝ) precision r x⌝⦄
    (pure (Twice_EvenClosest_Round_check
      (beta:=beta) bo precision x r) : Id Unit)
    ⦃⇓_ => ⌜EvenClosest (beta:=beta) bo (2 : ℝ) precision (2 * r)
      ({ Fnum := x.Fnum, Fexp := x.Fexp + 1 } :
        FloatSpec.Core.Defs.FlocqFloat beta)⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hprecision, hvNum, hxPredExp, hxNormal, hxEvenClosest⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure, Twice_EvenClosest_Round_check,
    Id.run, ULift.up_down]
  have hprecision_ne : precision ≠ 0 := by omega
  have hvNum_pos : 0 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    positivity
  have hpowPrecision_ge_four :
      (4 : ℝ) ≤ (2 : ℝ) ^ precision := by
    have hprec : (2 : Int) ≤ (precision : Int) := by omega
    have hpow :=
      zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ (2 : ℝ)) hprec
    norm_num at hpow
    simpa [zpow_natCast] using hpow
  let xDouble : FloatSpec.Core.Defs.FlocqFloat 2 :=
    { Fnum := x.Fnum, Fexp := x.Fexp + 1 }
  have hxDoubleVal :
      _root_.F2R (beta:=2) xDouble =
        2 * _root_.F2R (beta:=2) x := by
    have htwo_ne : ((2 : Int) : ℝ) ≠ 0 := by norm_num
    have hpow :
        ((2 : Int) : ℝ) ^ (x.Fexp + 1) =
          ((2 : Int) : ℝ) ^ x.Fexp * 2 := by
      calc
        ((2 : Int) : ℝ) ^ (x.Fexp + 1) =
            ((2 : Int) : ℝ) ^ x.Fexp *
              ((2 : Int) : ℝ) ^ (1 : Int) := by
                rw [zpow_add₀ htwo_ne]
        _ = ((2 : Int) : ℝ) ^ x.Fexp * 2 := by norm_num
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, xDouble,
      FloatSpec.Core.Defs.FlocqFloat.Fnum,
      FloatSpec.Core.Defs.FlocqFloat.Fexp]
    rw [hpow]
    ring
  have hxDoubleBound : Fbounded (beta:=2) bo xDouble := by
    constructor
    · simpa [xDouble] using hxNormal.1.1
    · change -bo.dExp ≤ x.Fexp + 1
      omega
  have hxDoubleClosest :
      Closest (beta:=2) bo (2 : ℝ) (2 * r) xDouble := by
    constructor
    · exact hxDoubleBound
    · intro f hfBound
      by_cases hfHigh : -bo.dExp ≤ f.Fexp - 1
      · have hhalf := Fbounded_half_even_or_high_exp (beta:=2) bo f
        have hhalf' :
            ∃ fh : FloatSpec.Core.Defs.FlocqFloat 2,
              Fbounded (beta:=2) bo fh ∧
                _root_.F2R (beta:=2) fh =
                  _root_.F2R (beta:=2) f / 2 := by
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
            Fbounded_half_even_or_high_exp_check, Id.run,
            ULift.up_down, Int.cast_ofNat] using hhalf ⟨rfl, hfBound, Or.inl hfHigh⟩
        rcases hhalf' with ⟨fh, hfhBound, hfhVal⟩
        have hdist := hxEvenClosest.1.2 fh hfhBound
        have hfVal :
            _root_.F2R (beta:=2) f =
              2 * _root_.F2R (beta:=2) fh := by
          linarith
        calc
          |_root_.F2R (beta:=2) xDouble - 2 * r| =
              2 * |_root_.F2R (beta:=2) x - r| := by
                rw [hxDoubleVal]
                have harg :
                    2 * _root_.F2R (beta:=2) x - 2 * r =
                      2 * (_root_.F2R (beta:=2) x - r) := by ring
                rw [harg, abs_mul]
                norm_num
          _ ≤ 2 * |_root_.F2R (beta:=2) fh - r| :=
            mul_le_mul_of_nonneg_left hdist (by norm_num)
          _ = |_root_.F2R (beta:=2) f - 2 * r| := by
            rw [hfVal]
            have harg :
                2 * _root_.F2R (beta:=2) fh - 2 * r =
                  2 * (_root_.F2R (beta:=2) fh - r) := by ring
            rw [harg, abs_mul]
            norm_num
      · have hfExp : f.Fexp = -bo.dExp := by
          have hfExpLower := hfBound.2
          omega
        have hxCan : Fcanonic (beta:=2) 2 bo x := Or.inl hxNormal
        have hxClosestUlpRaw :=
          ClosestUlp (beta:=2) bo 2 precision r x
        have hxClosestUlp :
            2 * |r - _root_.F2R (beta:=2) x| ≤
              Fulp (beta:=2) bo 2 precision x := by
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using
            hxClosestUlpRaw
              ⟨hxEvenClosest.1, rfl, by norm_num, hprecision_ne, hvNum⟩
        have hxUlpRaw := CanonicFulp (beta:=2) 2 bo precision x
        have hxUlp :
            Fulp (beta:=2) bo 2 precision x =
              (2 : ℝ) ^ x.Fexp := by
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using
            hxUlpRaw ⟨hxCan, rfl, by norm_num, hprecision_ne, hvNum⟩
        have hxError :
            2 * |_root_.F2R (beta:=2) x - r| ≤
              (2 : ℝ) ^ x.Fexp := by
          calc
            2 * |_root_.F2R (beta:=2) x - r| =
                2 * |r - _root_.F2R (beta:=2) x| := by
                  rw [abs_sub_comm]
            _ ≤ Fulp (beta:=2) bo 2 precision x := hxClosestUlp
            _ = (2 : ℝ) ^ x.Fexp := hxUlp
        have hxErrorFour :
            4 * |_root_.F2R (beta:=2) x - r| ≤
              2 * (2 : ℝ) ^ x.Fexp := by
          linarith
        have hfNumLeInt :
            |f.Fnum| ≤ Zpower_nat 2 precision - 1 := by
          have hfNumLt := hfBound.1
          rw [← hvNum]
          omega
        have hfNumLeReal :
            (|f.Fnum| : ℝ) ≤ (2 : ℝ) ^ precision - 1 := by
          have hcast :
              ((|f.Fnum| : Int) : ℝ) ≤
                ((Zpower_nat 2 precision - 1 : Int) : ℝ) :=
            Int.cast_le.mpr hfNumLeInt
          simpa [Zpower_nat, Int.cast_pow] using hcast
        have hfPowLe :
            (2 : ℝ) ^ f.Fexp ≤ (2 : ℝ) ^ (x.Fexp - 1) :=
          zpow_le_zpow_right₀ (by norm_num) (by omega)
        have hfAbsVal :
            |_root_.F2R (beta:=2) f| =
              (|f.Fnum| : ℝ) * (2 : ℝ) ^ f.Fexp := by
          simp [_root_.F2R, FloatSpec.Core.Defs.F2R, abs_mul,
            abs_of_pos (zpow_pos (by norm_num : (0 : ℝ) < 2) f.Fexp)]
        have hfAbsBound :
            |_root_.F2R (beta:=2) f| ≤
              ((2 : ℝ) ^ precision - 1) *
                (2 : ℝ) ^ (x.Fexp - 1) := by
          rw [hfAbsVal]
          calc
            (|f.Fnum| : ℝ) * (2 : ℝ) ^ f.Fexp ≤
                ((2 : ℝ) ^ precision - 1) *
                  (2 : ℝ) ^ f.Fexp :=
              mul_le_mul_of_nonneg_right hfNumLeReal
                (le_of_lt (zpow_pos (by norm_num) _))
            _ ≤ ((2 : ℝ) ^ precision - 1) *
                  (2 : ℝ) ^ (x.Fexp - 1) :=
              mul_le_mul_of_nonneg_left hfPowLe (by linarith)
        have hxNumLowerReal :
            (2 : ℝ) ^ precision ≤ 2 * (|x.Fnum| : ℝ) := by
          have hcast :
              (bo.vNum : ℝ) ≤ ((|2 * x.Fnum| : Int) : ℝ) :=
            Int.cast_le.mpr hxNormal.2
          simpa [hvNum, Zpower_nat, Int.cast_pow, abs_mul] using hcast
        have hxAbsVal :
            |_root_.F2R (beta:=2) x| =
              (|x.Fnum| : ℝ) * (2 : ℝ) ^ x.Fexp := by
          simp [_root_.F2R, FloatSpec.Core.Defs.F2R, abs_mul,
            abs_of_pos (zpow_pos (by norm_num : (0 : ℝ) < 2) x.Fexp)]
        have hxMagnitudeLower :
            (2 : ℝ) ^ precision * (2 : ℝ) ^ x.Fexp ≤
              2 * |_root_.F2R (beta:=2) x| := by
          calc
            (2 : ℝ) ^ precision * (2 : ℝ) ^ x.Fexp ≤
                (2 * (|x.Fnum| : ℝ)) * (2 : ℝ) ^ x.Fexp :=
              mul_le_mul_of_nonneg_right hxNumLowerReal
                (le_of_lt (zpow_pos (by norm_num) _))
            _ = 2 * |_root_.F2R (beta:=2) x| := by
              rw [hxAbsVal]
              ring
        have hpowSplit :
            (2 : ℝ) ^ x.Fexp =
              2 * (2 : ℝ) ^ (x.Fexp - 1) := by
          calc
            (2 : ℝ) ^ x.Fexp =
                (2 : ℝ) ^ (x.Fexp - 1 + 1) := by
                  congr 1
                  omega
            _ = (2 : ℝ) ^ (x.Fexp - 1) *
                  (2 : ℝ) ^ (1 : Int) := by
                    rw [zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0)]
            _ = 2 * (2 : ℝ) ^ (x.Fexp - 1) := by
              norm_num
              ring
        have hcapacityNonneg :
            0 ≤ ((2 : ℝ) ^ precision - 3) *
              (2 : ℝ) ^ (x.Fexp - 1) :=
          mul_nonneg (by linarith)
            (le_of_lt (zpow_pos (by norm_num) _))
        have hcapacity :
            ((2 : ℝ) ^ precision - 1) *
                  (2 : ℝ) ^ (x.Fexp - 1) +
                2 * (2 : ℝ) ^ x.Fexp ≤
              (2 : ℝ) ^ precision * (2 : ℝ) ^ x.Fexp := by
          rw [hpowSplit]
          nlinarith
        have hseparation :
            |_root_.F2R (beta:=2) f| +
                  4 * |_root_.F2R (beta:=2) x - r| ≤
                2 * |_root_.F2R (beta:=2) x| := by
          have hsum :
              |_root_.F2R (beta:=2) f| +
                    4 * |_root_.F2R (beta:=2) x - r| ≤
                  ((2 : ℝ) ^ precision - 1) *
                      (2 : ℝ) ^ (x.Fexp - 1) +
                    2 * (2 : ℝ) ^ x.Fexp := by
            linarith
          exact le_trans hsum (le_trans hcapacity hxMagnitudeLower)
        have hxrTriangle :
            |_root_.F2R (beta:=2) x| ≤
              |_root_.F2R (beta:=2) x - r| + |r| := by
          simpa only [sub_add_cancel, Int.cast_ofNat] using
            (abs_add_le (_root_.F2R (beta:=2) x - r) r)
        have htargetTriangle :
            2 * |r| - |_root_.F2R (beta:=2) f| ≤
              |2 * r - _root_.F2R (beta:=2) f| := by
          calc
            2 * |r| - |_root_.F2R (beta:=2) f| =
                |2 * r| - |_root_.F2R (beta:=2) f| := by
                  rw [abs_mul]
                  norm_num
            _ ≤ |(|2 * r| - |_root_.F2R (beta:=2) f|)| :=
              le_abs_self _
            _ ≤ |2 * r - _root_.F2R (beta:=2) f| :=
              abs_abs_sub_abs_le_abs_sub _ _
        calc
          |_root_.F2R (beta:=2) xDouble - 2 * r| =
              2 * |_root_.F2R (beta:=2) x - r| := by
                rw [hxDoubleVal]
                have harg :
                    2 * _root_.F2R (beta:=2) x - 2 * r =
                      2 * (_root_.F2R (beta:=2) x - r) := by ring
                rw [harg, abs_mul]
                norm_num
          _ ≤ 2 * |_root_.F2R (beta:=2) x| -
                2 * |_root_.F2R (beta:=2) x - r| -
                |_root_.F2R (beta:=2) f| := by
            linarith
          _ ≤ 2 * |r| - |_root_.F2R (beta:=2) f| := by
            linarith
          _ ≤ |2 * r - _root_.F2R (beta:=2) f| := htargetTriangle
          _ = |_root_.F2R (beta:=2) f - 2 * r| := by
            rw [abs_sub_comm]
  constructor
  · exact hxDoubleClosest
  · rcases hxEvenClosest.2 with hxEven | hxUnique
    · left
      have h := FNeven_double_of_Fnormal (beta:=2) bo precision x
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
        FNeven_double_of_Fnormal_check, Id.run, ULift.up_down,
        xDouble, Int.cast_ofNat] using h ⟨rfl, hprecision_ne, hvNum, hxNormal, hxEven⟩
    · right
      intro q hqClosest
      have hxNumNe : x.Fnum ≠ 0 := by
        intro hxNum
        have hxNormalNum := hxNormal.2
        rw [hxNum, mul_zero, abs_zero] at hxNormalNum
        omega
      have hxValNe : _root_.F2R (beta:=2) x ≠ 0 := by
        simp only [_root_.F2R, FloatSpec.Core.Defs.F2R]
        exact mul_ne_zero (by exact_mod_cast hxNumNe)
          (ne_of_gt (zpow_pos (by norm_num) x.Fexp))
      let zeroFloat : FloatSpec.Core.Defs.FlocqFloat 2 :=
        { Fnum := 0, Fexp := -bo.dExp }
      have hzeroBound : Fbounded (beta:=2) bo zeroFloat := by
        constructor
        · simpa [zeroFloat] using hvNum_pos
        · simp [zeroFloat]
      have hzeroVal : _root_.F2R (beta:=2) zeroFloat = 0 := by
        simp [_root_.F2R, FloatSpec.Core.Defs.F2R, zeroFloat]
      have hrNe : r ≠ 0 := by
        intro hr
        subst r
        have hdist := hxEvenClosest.1.2 zeroFloat hzeroBound
        have hxAbsNonpos : |_root_.F2R (beta:=2) x| ≤ 0 := by
          simpa [hzeroVal] using hdist
        have hxAbsZero : |_root_.F2R (beta:=2) x| = 0 :=
          le_antisymm hxAbsNonpos (abs_nonneg _)
        exact hxValNe (abs_eq_zero.mp hxAbsZero)
      have htargetAbsLt : |r| < |2 * r| := by
        rw [abs_mul]
        norm_num
        nlinarith [abs_pos.mpr hrNe]
      have hxAbsClosest :
          Closest (beta:=2) bo (2 : ℝ) |r| (Fabs (beta:=2) x) := by
        have h := ClosestFabs (beta:=2) bo (2 : ℝ) x r
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestFabs_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨by norm_num, hxEvenClosest.1⟩
      have hqAbsClosest :
          Closest (beta:=2) bo (2 : ℝ) |2 * r| (Fabs (beta:=2) q) := by
        have h := ClosestFabs (beta:=2) bo (2 : ℝ) q (2 * r)
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestFabs_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨by norm_num, hqClosest⟩
      have hClosestMono :
          MonotoneP_float (Closest (beta:=2) bo (2 : ℝ)) := by
        have h := ClosestMonotone_from_split_radix_payload (beta:=2) bo (2 : ℝ)
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMonotone_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
      have hFabsLe :
          _root_.F2R (beta:=2) (Fabs (beta:=2) x) ≤
            _root_.F2R (beta:=2) (Fabs (beta:=2) q) :=
        hClosestMono |r| |2 * r| (Fabs (beta:=2) x)
          (Fabs (beta:=2) q) htargetAbsLt hxAbsClosest hqAbsClosest
      have hxFabsVal :
          _root_.F2R (beta:=2) (Fabs (beta:=2) x) =
            |_root_.F2R (beta:=2) x| := by
        have h := FloatSpec.Calc.Operations.F2R_abs (beta:=2) x
          (by norm_num : (1 : Int) < 2)
        simpa [Fabs, _root_.F2R] using h
      have hqFabsVal :
          _root_.F2R (beta:=2) (Fabs (beta:=2) q) =
            |_root_.F2R (beta:=2) q| := by
        have h := FloatSpec.Calc.Operations.F2R_abs (beta:=2) q
          (by norm_num : (1 : Int) < 2)
        simpa [Fabs, _root_.F2R] using h
      have hxqAbs :
          |_root_.F2R (beta:=2) x| ≤ |_root_.F2R (beta:=2) q| := by
        rw [← hxFabsVal, ← hqFabsVal]
        exact hFabsLe
      let qNorm : FloatSpec.Core.Defs.FlocqFloat 2 :=
        Fnormalize (beta:=2) 2 bo precision q
      have hqNormCan : Fcanonic (beta:=2) 2 bo qNorm := by
        have h := FnormalizeCanonic (beta:=2) 2 bo precision q
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
          Id.run, ULift.up_down, Fbounded', qNorm, Int.cast_ofNat] using
          h ⟨hqClosest.1, hqClosest.1, hprecision_ne, by norm_num, hvNum⟩
      have hqNormVal :
          _root_.F2R (beta:=2) qNorm = _root_.F2R (beta:=2) q := by
        have h := FnormalizeCorrect (beta:=2) 2 bo precision q
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
          Id.run, ULift.up_down, qNorm, Int.cast_ofNat] using h ⟨rfl, by norm_num⟩
      have hxExpLeNorm : x.Fexp ≤ qNorm.Fexp := by
        have h := Fcanonic_Rle_Zle (beta:=2) 2 bo x qNorm
          (by norm_num) rfl
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fcanonic_Rle_Zle_check,
          ULift.down_up, Int.cast_ofNat] using
          h ⟨Or.inl hxNormal, hqNormCan, by simpa [hqNormVal] using hxqAbs⟩
      have hqNormExpLe : qNorm.Fexp ≤ q.Fexp := by
        have h := FcanonicLeastExp_from_explicit_radix_payload (beta:=2) 2 bo q qNorm
          (by norm_num) rfl hvNum_pos
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicLeastExp_check,
          ULift.down_up, Fcanonic', Int.cast_ofNat] using
          h ⟨hqNormVal.symm, hqClosest.1, hqNormCan⟩
      have hqPredExp : -bo.dExp ≤ q.Fexp - 1 := by
        omega
      let qHalf : FloatSpec.Core.Defs.FlocqFloat 2 :=
        { Fnum := q.Fnum, Fexp := q.Fexp - 1 }
      have hqHalfClosest :
          Closest (beta:=2) bo (2 : ℝ) r qHalf := by
        have h := Half_Closest_Round (beta:=2) bo (2 * r) q
        have h' :
            Closest (beta:=2) bo (2 : ℝ) ((2 * r) / 2)
              ({ Fnum := q.Fnum, Fexp := q.Fexp - 1 } :
                FloatSpec.Core.Defs.FlocqFloat 2) := by
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Half_Closest_Round_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hqPredExp, hqClosest⟩
        simpa [qHalf] using h'
      have hqHalfEqX :
          _root_.F2R (beta:=2) qHalf =
            _root_.F2R (beta:=2) x :=
        hxUnique qHalf hqHalfClosest
      have hqHalfVal :
          _root_.F2R (beta:=2) qHalf =
            _root_.F2R (beta:=2) q / 2 := by
        have htwo_ne : ((2 : Int) : ℝ) ≠ 0 := by norm_num
        have hpow :
            ((2 : Int) : ℝ) ^ q.Fexp =
              ((2 : Int) : ℝ) ^ (q.Fexp - 1) * 2 := by
          calc
            ((2 : Int) : ℝ) ^ q.Fexp =
                ((2 : Int) : ℝ) ^ (q.Fexp - 1 + 1) := by
                  congr 1
                  omega
            _ = ((2 : Int) : ℝ) ^ (q.Fexp - 1) *
                  ((2 : Int) : ℝ) ^ (1 : Int) := by
                    rw [zpow_add₀ htwo_ne]
            _ = ((2 : Int) : ℝ) ^ (q.Fexp - 1) * 2 := by
              norm_num
        simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, qHalf,
          FloatSpec.Core.Defs.FlocqFloat.Fnum,
          FloatSpec.Core.Defs.FlocqFloat.Fexp]
        rw [hpow]
        ring
      rw [hxDoubleVal]
      linarith

/-- Coq `Fulp_le_twice_r_round`: the doubled-round, monotonicity, and
uniqueness facts used by the proof are consequences of the source section
assumptions. -/
theorem Fulp_le_twice_r_round
    (bo : Fbound_skel) (precision : Nat)
    (x y : FloatSpec.Core.Defs.FlocqFloat 2) (r : ℝ) :
    ⦃⌜1 < precision ∧
        bo.vNum = Zpower_nat 2 precision ∧
        0 ≤ _root_.F2R (beta:=2) x ∧
        Fbounded (beta:=2) bo x ∧
        Fnormal (beta:=2) 2 bo y ∧
        -bo.dExp ≤ y.Fexp - 1 ∧
        _root_.F2R (beta:=2) x ≤ 2 * r ∧
        EvenClosest (beta:=2) bo (2 : ℝ) precision r y⌝⦄
    (pure (Fulp_le_twice_r_round_check (beta:=2) bo precision x y r) :
      Id Unit)
    ⦃⇓_ => ⌜Fulp (beta:=2) bo 2 precision x ≤
      2 * Fulp (beta:=2) bo 2 precision y⌝⦄ := by
  intro ⟨hprecision, hvNum, hxNonneg, hxBound, hyNormal, hyExp,
    hxLe, hyRound⟩
  have hprecision_ne : precision ≠ 0 := by omega
  have hyDouble :
      EvenClosest (beta:=2) bo (2 : ℝ) precision (2 * r)
        ({ Fnum := y.Fnum, Fexp := y.Fexp + 1 } :
          FloatSpec.Core.Defs.FlocqFloat 2) := by
    have h := Twice_EvenClosest_Round (beta:=2) bo precision y r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Twice_EvenClosest_Round_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, hprecision, hvNum, hyExp, hyNormal, hyRound⟩
  have hMono :
      MonotoneP_float (EvenClosest (beta:=2) bo (2 : ℝ) precision) := by
    have h := EvenClosestMonotone_from_split_radix_payload
      (beta:=2) bo (2 : ℝ) precision
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestMonotone_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
  have hUnique :
      UniqueP (EvenClosest (beta:=2) bo (2 : ℝ) precision) := by
    have h := EvenClosestUniqueP (beta:=2) bo 2 precision
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestUniqueP_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨rfl, by decide, hprecision, hvNum⟩
  exact Fulp_le_twice_r_round_from_rounding_payload
    (beta:=2) bo precision x y r
    ⟨rfl, hprecision_ne, hvNum, hxNonneg, hxBound, hyNormal, hyExp,
      hxLe, hyRound, hyDouble, hMono, hUnique⟩

/-- Coq: `errorBoundedMultClosest`.

Final closest-rounded multiplication error theorem: the rounded product can be
represented canonically, and its nonzero residual can be represented at
exponent `Fexp r - precision`.
-/
theorem errorBoundedMultClosest_from_format_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat) :
    beta = radix →
    1 < radix →
    precision ≠ 0 →
    1 < bo.vNum →
    bo.vNum = Zpower_nat radix precision →
    ∀ p q pq : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      Closest (beta:=beta) bo (radix : ℝ)
        (_root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q) pq →
      -bo.dExp ≤ p.Fexp + q.Fexp →
      -bo.dExp ≤
        (Fnormalize (beta:=beta) radix bo precision pq).Fexp - precision →
      ∃ r s : FloatSpec.Core.Defs.FlocqFloat beta,
        Fcanonic (beta:=beta) radix bo r ∧
        Fbounded (beta:=beta) bo r ∧
        Fbounded (beta:=beta) bo s ∧
        _root_.F2R (beta:=beta) r = _root_.F2R (beta:=beta) pq ∧
        _root_.F2R (beta:=beta) s =
          _root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q -
            _root_.F2R (beta:=beta) r ∧
        s.Fexp = r.Fexp - precision := by
  intro hbeta hradix hprecision hvNum_gt hvNum p q pq hp hq hclosest
    hexp hnormLower
  subst beta
  let prod : ℝ := _root_.F2R (beta:=radix) p * _root_.F2R (beta:=radix) q
  let nr : FloatSpec.Core.Defs.FlocqFloat radix :=
    Fnormalize (beta:=radix) radix bo precision pq
  have hvNum_pos : 0 < bo.vNum := by omega
  have hnr_can : Fcanonic (beta:=radix) radix bo nr := by
    have h := FnormalizeCanonic (beta:=radix) radix bo precision pq
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
      Id.run, ULift.up_down, nr, Int.cast_ofNat] using
      h ⟨hclosest.1, hclosest.1, hprecision, hradix, hvNum⟩
  have hnr_bound : Fbounded (beta:=radix) bo nr := by
    have h := FnormalizeBounded (beta:=radix) radix bo precision pq
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeBounded_check,
      Id.run, ULift.up_down, nr, Int.cast_ofNat] using
      h ⟨hclosest.1, hclosest.1, hprecision, hradix, hvNum⟩
  have hnr_val : _root_.F2R (beta:=radix) nr = _root_.F2R (beta:=radix) pq := by
    have h := FnormalizeCorrect (beta:=radix) radix bo precision pq
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
      Id.run, ULift.up_down, nr, Int.cast_ofNat] using h ⟨rfl, hradix⟩
  by_cases hzero :
      prod - _root_.F2R (beta:=radix) pq = 0
  · let s : FloatSpec.Core.Defs.FlocqFloat radix :=
      Fzero radix (nr.Fexp - precision)
    have hs_bound : Fbounded (beta:=radix) bo s := by
      constructor
      · simpa [s, Fzero] using hvNum_pos
      · change -bo.dExp ≤ nr.Fexp - (precision : Int)
        simpa [nr] using hnormLower
    have hs_val :
        _root_.F2R (beta:=radix) s =
          prod - _root_.F2R (beta:=radix) nr := by
      have hs_zero : _root_.F2R (beta:=radix) s = 0 := by
        have h := FzeroisReallyZero (beta:=radix) (nr.Fexp - precision)
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FzeroisReallyZero_check,
          Id.run, ULift.up_down, s, Int.cast_ofNat] using h trivial
      rw [hs_zero, hnr_val]
      exact hzero.symm
    refine ⟨nr, s, hnr_can, hnr_bound, hs_bound, hnr_val, hs_val, ?_⟩
    rfl
  · rcases errorBoundedMultClosest_aux_from_format_payload (beta:=radix) bo radix precision
      rfl hradix hprecision hvNum_gt hvNum p q pq hp hq hclosest
      hexp (by simpa [prod] using hzero) with
      ⟨r, s, hr_can, hr_bound, hs_bound, hr_val, hs_val, hs_exp,
        _hs_le_r, hr_upper⟩
    let e : Int := r.Fexp - precision
    have hr_eq_nr : r = nr := by
      have huniq := FcanonicUnique (beta:=radix) radix bo r nr hradix rfl
      have hval : _root_.F2R (beta:=radix) r = _root_.F2R (beta:=radix) nr := by
        rw [hr_val, hnr_val]
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicUnique_check,
        ULift.down_up, Fcanonic', Int.cast_ofNat] using huniq ⟨hr_can, hnr_can, hval⟩
    have he_lower : -bo.dExp ≤ e := by
      dsimp [e]
      rw [hr_eq_nr]
      simpa [nr] using hnormLower
    have he_le_s : e ≤ s.Fexp := by
      dsimp [e]
      rw [hs_exp]
      omega
    have hrep := F2R_rep_at_lower_exp (beta:=radix) s e
    rcases (by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, F2R_rep_at_lower_exp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using hrep ⟨hradix, he_le_s⟩) with
      ⟨m, hm_repr⟩
    let s' : FloatSpec.Core.Defs.FlocqFloat radix := ⟨m, e⟩
    have hclosest_r :
        Closest (beta:=radix) bo (radix : ℝ) prod r := by
      constructor
      · exact hr_bound
      · intro f hf
        have hdist := hclosest.2 f hf
        simpa [prod, hr_val] using hdist
    have hclosestUlp :
        2 * |prod - _root_.F2R (beta:=radix) r| ≤
          Fulp (beta:=radix) bo radix precision r := by
      have h := ClosestUlp (beta:=radix) bo radix precision prod r
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hclosest_r, rfl, hradix, hprecision, hvNum⟩
    have hFulpLe :
        Fulp (beta:=radix) bo radix precision r ≤ (radix : ℝ) ^ r.Fexp := by
      have h := FulpLe (beta:=radix) radix bo precision r
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpLe_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hr_bound, rfl, hradix⟩
    have hClosestExp :
        2 * |prod - _root_.F2R (beta:=radix) r| ≤ (radix : ℝ) ^ r.Fexp :=
      le_trans hclosestUlp hFulpLe
    have herrorBound :
        |_root_.F2R (beta:=radix) s| ≤
          _root_.F2R (beta:=radix)
            (FloatSpec.Core.Defs.FlocqFloat.mk (beta:=radix) 1 r.Fexp) *
            (1 / 2 : ℝ) := by
      have h := ClosestErrorBound_from_bound (beta:=radix) bo (radix : ℝ) r s prod
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestErrorBound_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hr_bound, hclosest_r, hs_val, hClosestExp⟩
    have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
      exact_mod_cast (by omega : (0 : Int) < radix)
    have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
    have he_pow_pos : 0 < (radix : ℝ) ^ e := zpow_pos hradix_pos e
    have hpow_prec_pos : 0 < (radix : ℝ) ^ (precision : Int) :=
      zpow_pos hradix_pos (precision : Int)
    have hpow_split :
        (radix : ℝ) ^ r.Fexp =
          (radix : ℝ) ^ e * (radix : ℝ) ^ (precision : Int) := by
      have heq : e + (precision : Int) = r.Fexp := by
        dsimp [e]
        omega
      rw [← heq, zpow_add₀ hradix_ne]
    have hscaled :
        ((|m| : Int) : ℝ) * (radix : ℝ) ^ e ≤
          (radix : ℝ) ^ r.Fexp * (1 / 2 : ℝ) := by
      have hrepr_abs : |_root_.F2R (beta:=radix) s'| =
          |_root_.F2R (beta:=radix) s| := by
        rw [show _root_.F2R (beta:=radix) s' = _root_.F2R (beta:=radix) s from
          by simpa [s'] using hm_repr.symm]
      have hbound_s' :
          |_root_.F2R (beta:=radix) s'| ≤
            (radix : ℝ) ^ r.Fexp * (1 / 2 : ℝ) := by
        simpa [_root_.F2R, FloatSpec.Core.Defs.F2R] using
          (hrepr_abs.trans_le herrorBound)
      simpa [s', _root_.F2R, FloatSpec.Core.Defs.F2R, abs_mul,
        abs_of_pos he_pow_pos, Int.cast_abs] using hbound_s'
    have hm_le_half :
        ((|m| : Int) : ℝ) ≤ (radix : ℝ) ^ (precision : Int) / 2 := by
      rw [hpow_split] at hscaled
      nlinarith [he_pow_pos, hscaled]
    have hm_lt_pow :
        ((|m| : Int) : ℝ) < (radix : ℝ) ^ (precision : Int) := by
      nlinarith [hm_le_half, hpow_prec_pos]
    have hvNum_real :
        (bo.vNum : ℝ) = (radix : ℝ) ^ (precision : Int) := by
      rw [hvNum]
      unfold Zpower_nat
      rw [Int.cast_pow]
      exact (zpow_ofNat (radix : ℝ) precision).symm
    have hm_bound_real : ((|m| : Int) : ℝ) < (bo.vNum : ℝ) := by
      rw [hvNum_real]
      exact hm_lt_pow
    have hm_bound : |m| < bo.vNum := by
      exact_mod_cast hm_bound_real
    have hs'_bound : Fbounded (beta:=radix) bo s' := by
      exact ⟨by simpa [s'] using hm_bound, by simpa [s', e] using he_lower⟩
    have hs'_val :
        _root_.F2R (beta:=radix) s' =
          prod - _root_.F2R (beta:=radix) r := by
      rw [show _root_.F2R (beta:=radix) s' = _root_.F2R (beta:=radix) s from
        by simpa [s'] using hm_repr.symm]
      exact hs_val
    refine ⟨r, s', hr_can, hr_bound, hs'_bound, hr_val, hs'_val, ?_⟩
    rfl

/-- Exact exported Coq `errorBoundedMultClosest` contract. -/
theorem errorBoundedMultClosest {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hprecision : 1 < precision)
    (hvNum : bo.vNum = Zpower_nat radix precision) :
    ∀ p q pq : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      Closest (beta:=beta) bo (radix : ℝ)
        (_root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q) pq →
      -bo.dExp ≤ p.Fexp + q.Fexp →
      -bo.dExp ≤
        (Fnormalize (beta:=beta) radix bo precision pq).Fexp - precision →
      ∃ r s : FloatSpec.Core.Defs.FlocqFloat beta,
        Fcanonic (beta:=beta) radix bo r ∧
        Fbounded (beta:=beta) bo r ∧
        Fbounded (beta:=beta) bo s ∧
        _root_.F2R (beta:=beta) r = _root_.F2R (beta:=beta) pq ∧
        _root_.F2R (beta:=beta) s =
          _root_.F2R (beta:=beta) p * _root_.F2R (beta:=beta) q -
            _root_.F2R (beta:=beta) r ∧
        s.Fexp = r.Fexp - precision := by
  have hprecision_ne : precision ≠ 0 := by omega
  have hvNum_gt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecision_ne
  letI : ValidRadix radix := ⟨hradix⟩
  exact errorBoundedMultClosest_from_format_payload (beta:=beta)
    bo radix precision hbeta hradix hprecision_ne hvNum_gt hvNum

/-- Coq: `errorBoundedMultClosest_Can`.

Binary closest-rounded multiplication error for a canonical rounded product:
under the upstream no-underflow magnitude premise, the residual is a bounded
float at exponent `g.Fexp - precision`, with mantissa bounded by
`2^(precision - 1)`.  This is the binary/canonical wrapper around the already
restored `errorBoundedMultClosest`. -/
theorem errorBoundedMultClosest_Can {beta : Int} [ValidRadix beta]
      (bo : Fbound_skel) (precision : Nat)
      (f1 f2 g : FloatSpec.Core.Defs.FlocqFloat beta) :
      beta = 2 →
      1 < precision →
      bo.vNum = Zpower_nat 2 precision →
      Fbounded (beta:=beta) bo f1 →
      Fbounded (beta:=beta) bo f2 →
      Closest (beta:=beta) bo (2 : ℝ)
        (_root_.F2R (beta:=beta) f1 * _root_.F2R (beta:=beta) f2) g →
      (2 : ℝ) ^ (-bo.dExp + 2 * (precision : Int) - 1) ≤
        |_root_.F2R (beta:=beta) f1 * _root_.F2R (beta:=beta) f2| →
      Fcanonic (beta:=beta) 2 bo g →
      ∃ s : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo s ∧
        _root_.F2R (beta:=beta) s =
          _root_.F2R (beta:=beta) f1 * _root_.F2R (beta:=beta) f2 -
            _root_.F2R (beta:=beta) g ∧
        s.Fexp = g.Fexp - precision ∧
        ((|s.Fnum| : Int) : ℝ) ≤ (2 : ℝ) ^ ((precision : Int) - 1) := by
    intro hbeta hprecision_gt hvNum hf1 hf2 hclosest hprodLower hgCan
    subst beta
    have hprecision_ne : precision ≠ 0 := by omega
    have hvNum_gt : 1 < bo.vNum := by
      rw [hvNum, Zpower_nat]
      calc
        (1 : Int) < 2 := by norm_num
        _ = 2 ^ (1 : Nat) := by simp
        _ ≤ 2 ^ precision := by
          exact pow_le_pow_right₀ (by norm_num : (1 : Int) ≤ 2)
            (by omega : 1 ≤ precision)
    have hvNum_real : (bo.vNum : ℝ) = (2 : ℝ) ^ (precision : Int) := by
      rw [hvNum, Zpower_nat, Int.cast_pow]
      exact (zpow_natCast (2 : ℝ) precision).symm
    have htwo_pos : (0 : ℝ) < 2 := by norm_num
    have htwo_ne : (2 : ℝ) ≠ 0 := by norm_num
    have htwo_one_le : (1 : ℝ) ≤ 2 := by norm_num
    have htwo_one_lt : (1 : ℝ) < 2 := by norm_num
    have hAbsF2R_lt_pow
        (x : FloatSpec.Core.Defs.FlocqFloat 2)
        (hx : Fbounded (beta:=2) bo x) :
        |_root_.F2R (beta:=2) x| <
          (2 : ℝ) ^ ((precision : Int) + x.Fexp) := by
      have hnum_lt_v : (((|x.Fnum| : Int) : ℝ) < (bo.vNum : ℝ)) := by
        exact_mod_cast hx.1
      have hnum_lt_pow : |(x.Fnum : ℝ)| < (2 : ℝ) ^ (precision : Int) := by
        simpa [Int.cast_abs, hvNum_real] using hnum_lt_v
      have hpow_pos : 0 < (2 : ℝ) ^ x.Fexp := zpow_pos htwo_pos x.Fexp
      calc
      |_root_.F2R (beta:=2) x| =
          |(x.Fnum : ℝ)| * (2 : ℝ) ^ x.Fexp := by
            simp only [_root_.F2R, FloatSpec.Core.Defs.F2R]
            rw [abs_mul]
            simpa [abs_of_pos hpow_pos]
        _ < (2 : ℝ) ^ (precision : Int) * (2 : ℝ) ^ x.Fexp :=
            mul_lt_mul_of_pos_right hnum_lt_pow hpow_pos
        _ = (2 : ℝ) ^ ((precision : Int) + x.Fexp) := by
            rw [← zpow_add₀ htwo_ne]
    have hProdAbs_lt :
        |_root_.F2R (beta:=2) f1 * _root_.F2R (beta:=2) f2| <
          (2 : ℝ) ^ (2 * (precision : Int) + f1.Fexp + f2.Fexp) := by
      have hf1_lt := hAbsF2R_lt_pow f1 hf1
      have hf2_lt := hAbsF2R_lt_pow f2 hf2
      have hpow1_pos :
          0 < (2 : ℝ) ^ ((precision : Int) + f1.Fexp) :=
        zpow_pos htwo_pos _
      have hpow2_pos :
          0 < (2 : ℝ) ^ ((precision : Int) + f2.Fexp) :=
        zpow_pos htwo_pos _
      calc
        |_root_.F2R (beta:=2) f1 * _root_.F2R (beta:=2) f2| =
            |_root_.F2R (beta:=2) f1| * |_root_.F2R (beta:=2) f2| := by
              rw [abs_mul]
        _ < (2 : ℝ) ^ ((precision : Int) + f1.Fexp) *
              (2 : ℝ) ^ ((precision : Int) + f2.Fexp) := by
              nlinarith [hf1_lt, hf2_lt, abs_nonneg (_root_.F2R (beta:=2) f1),
                abs_nonneg (_root_.F2R (beta:=2) f2), hpow1_pos, hpow2_pos]
        _ = (2 : ℝ) ^ (2 * (precision : Int) + f1.Fexp + f2.Fexp) := by
              rw [← zpow_add₀ htwo_ne]
              ring_nf
    have hProductExpLt :
        -bo.dExp + 2 * (precision : Int) - 1 <
          2 * (precision : Int) + f1.Fexp + f2.Fexp := by
      have hpow_lt :
          (2 : ℝ) ^ (-bo.dExp + 2 * (precision : Int) - 1) <
            (2 : ℝ) ^ (2 * (precision : Int) + f1.Fexp + f2.Fexp) :=
        lt_of_le_of_lt hprodLower hProdAbs_lt
      have h := Zlt_powerRZ (2 : ℝ)
        (-bo.dExp + 2 * (precision : Int) - 1)
        (2 * (precision : Int) + f1.Fexp + f2.Fexp)
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Zlt_powerRZ_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using h ⟨htwo_one_le, hpow_lt⟩
    have hExpProduct : -bo.dExp ≤ f1.Fexp + f2.Fexp := by
      omega
    let lower : FloatSpec.Core.Defs.FlocqFloat 2 :=
      ⟨1, -bo.dExp + 2 * (precision : Int) - 1⟩
    have hlowerBound : Fbounded (beta:=2) bo lower := by
      constructor
      · have hOneLt : (1 : Int) < bo.vNum := hvNum_gt
        simpa [lower] using hOneLt
      · dsimp [lower]
        omega
    have hlowerVal :
        _root_.F2R (beta:=2) lower =
          (2 : ℝ) ^ (-bo.dExp + 2 * (precision : Int) - 1) := by
      simp [lower, _root_.F2R, FloatSpec.Core.Defs.F2R]
    have hMinMax :
        MinOrMaxP_float (beta:=2) bo 2 (Closest (beta:=2) bo (2 : ℝ)) := by
      have h := ClosestMinOrMax_from_split_radix_payload (beta:=2) bo 2 (2 : ℝ)
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMinOrMax_check, MinOrMaxP_float,
        Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
    let zeroFloat : FloatSpec.Core.Defs.FlocqFloat 2 := ⟨0, -bo.dExp⟩
    have hzeroBound : Fbounded (beta:=2) bo zeroFloat := by
      constructor
      · have hVNumPos : (0 : Int) < bo.vNum := by omega
        simpa [zeroFloat] using hVNumPos
      · simp [zeroFloat]
    have hzeroVal : _root_.F2R (beta:=2) zeroFloat = 0 := by
      simp [zeroFloat, _root_.F2R, FloatSpec.Core.Defs.F2R]
    let negLower : FloatSpec.Core.Defs.FlocqFloat 2 :=
      Fopp (beta:=2) lower
    have hnegLowerBound : Fbounded (beta:=2) bo negLower := by
      have h := oppBounded (beta:=2) bo lower
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, oppBounded_check, Id.run,
        ULift.up_down, negLower, Int.cast_ofNat] using h hlowerBound
    have hnegLowerVal :
        _root_.F2R (beta:=2) negLower =
          -_root_.F2R (beta:=2) lower := by
      have h := FloatSpec.Calc.Operations.F2R_opp (beta:=2) lower
      simpa [negLower, Fopp, _root_.F2R] using h trivial
    have hlowerNonneg : 0 ≤ _root_.F2R (beta:=2) lower := by
      rw [hlowerVal]
      exact le_of_lt (zpow_pos htwo_pos _)
    have hlowerLeGAbs :
        (2 : ℝ) ^ (-bo.dExp + 2 * (precision : Int) - 1) ≤
          |_root_.F2R (beta:=2) g| := by
      let prod : ℝ :=
        _root_.F2R (beta:=2) f1 * _root_.F2R (beta:=2) f2
      have hgMinMax := hMinMax prod g (by simpa [prod] using hclosest)
      have hlowerLeProdAbs :
          _root_.F2R (beta:=2) lower ≤ |prod| := by
        simpa [prod, hlowerVal] using hprodLower
      have hlowerLeG :
          _root_.F2R (beta:=2) lower ≤
            |_root_.F2R (beta:=2) g| := by
        by_cases hprodNonneg : 0 ≤ prod
        · have hlowerLeProd : _root_.F2R (beta:=2) lower ≤ prod := by
            simpa [abs_of_nonneg hprodNonneg] using hlowerLeProdAbs
          rcases hgMinMax with hgMin | hgMax
          · rcases hgMin with ⟨_hgBound, _hgLe, hGreatest⟩
            have hlowerLeGVal := hGreatest lower hlowerBound hlowerLeProd
            have hgNonneg : 0 ≤ _root_.F2R (beta:=2) g :=
              le_trans hlowerNonneg hlowerLeGVal
            simpa [abs_of_nonneg hgNonneg] using hlowerLeGVal
          · rcases hgMax with ⟨_hgBound, hprodLeG, _hLeast⟩
            have hlowerLeGVal := le_trans hlowerLeProd hprodLeG
            have hgNonneg : 0 ≤ _root_.F2R (beta:=2) g :=
              le_trans hlowerNonneg hlowerLeGVal
            simpa [abs_of_nonneg hgNonneg] using hlowerLeGVal
        · have hprodNeg : prod < 0 := lt_of_not_ge hprodNonneg
          have hlowerLeNegProd :
              _root_.F2R (beta:=2) lower ≤ -prod := by
            simpa [abs_of_neg hprodNeg] using hlowerLeProdAbs
          have hprodLeNegLower : prod ≤ _root_.F2R (beta:=2) negLower := by
            rw [hnegLowerVal]
            linarith
          rcases hgMinMax with hgMin | hgMax
          · rcases hgMin with ⟨_hgBound, hgLeProd, _hGreatest⟩
            have hgLeNegLower :
                _root_.F2R (beta:=2) g ≤ _root_.F2R (beta:=2) negLower :=
              le_trans hgLeProd hprodLeNegLower
            have hgNonpos : _root_.F2R (beta:=2) g ≤ 0 :=
              le_trans hgLeProd (le_of_lt hprodNeg)
            rw [abs_of_nonpos hgNonpos, hnegLowerVal] at *
            linarith
          · rcases hgMax with ⟨_hgBound, _hprodLeG, hLeast⟩
            have hgLeNegLower :=
              hLeast negLower hnegLowerBound hprodLeNegLower
            have hgLeZero : _root_.F2R (beta:=2) g ≤ 0 := by
              have hprodLeZero : prod ≤ _root_.F2R (beta:=2) zeroFloat := by
                simpa [hzeroVal] using le_of_lt hprodNeg
              have := hLeast zeroFloat hzeroBound hprodLeZero
              simpa [hzeroVal] using this
            rw [abs_of_nonpos hgLeZero, hnegLowerVal] at *
            linarith
      simpa [hlowerVal] using hlowerLeG
    have hGAbs_lt :
        |_root_.F2R (beta:=2) g| <
          (2 : ℝ) ^ ((precision : Int) + g.Fexp) :=
      hAbsF2R_lt_pow g hclosest.1
    have hGExpLt :
        -bo.dExp + 2 * (precision : Int) - 1 <
          (precision : Int) + g.Fexp := by
      have hpow_lt :
          (2 : ℝ) ^ (-bo.dExp + 2 * (precision : Int) - 1) <
            (2 : ℝ) ^ ((precision : Int) + g.Fexp) :=
        lt_of_le_of_lt hlowerLeGAbs hGAbs_lt
      have h := Zlt_powerRZ (2 : ℝ)
        (-bo.dExp + 2 * (precision : Int) - 1)
        ((precision : Int) + g.Fexp)
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Zlt_powerRZ_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using h ⟨htwo_one_le, hpow_lt⟩
    let prod : ℝ :=
      _root_.F2R (beta:=2) f1 * _root_.F2R (beta:=2) f2
    let z : Int := f1.Fexp + f2.Fexp
    let upperExp : Int := (precision : Int) + z
    let upper : FloatSpec.Core.Defs.FlocqFloat 2 :=
      ⟨bo.vNum - 1, upperExp⟩
    have hupperBounded : Fbounded (beta:=2) bo upper := by
      constructor
      · have hnonneg : 0 ≤ bo.vNum - 1 := by omega
        rw [abs_of_nonneg hnonneg]
        omega
      · dsimp [upper, upperExp, z]
        omega
    have hf1AbsLe :
        |_root_.F2R (beta:=2) f1| ≤
          _root_.F2R (beta:=2)
            (⟨bo.vNum - 1, f1.Fexp⟩ :
              FloatSpec.Core.Defs.FlocqFloat 2) := by
      have h := maxMax1_from_expanded_bound_payload (beta:=2) bo f1 f1.Fexp
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, maxMax1_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using h ⟨hf1, le_rfl, hf1, by norm_num⟩
    have hf2AbsLe :
        |_root_.F2R (beta:=2) f2| ≤
          _root_.F2R (beta:=2)
            (⟨bo.vNum - 1, f2.Fexp⟩ :
              FloatSpec.Core.Defs.FlocqFloat 2) := by
      have h := maxMax1_from_expanded_bound_payload (beta:=2) bo f2 f2.Fexp
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, maxMax1_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using h ⟨hf2, le_rfl, hf2, by norm_num⟩
    have hbaseF1Nonneg :
        0 ≤ _root_.F2R (beta:=2)
          (⟨bo.vNum - 1, f1.Fexp⟩ :
            FloatSpec.Core.Defs.FlocqFloat 2) := by
      have hnum : (0 : Int) ≤ bo.vNum - 1 := by omega
      have hnumR : (0 : ℝ) ≤ ((bo.vNum - 1 : Int) : ℝ) := by
        exact_mod_cast hnum
      have hpowNonneg : 0 ≤ (2 : ℝ) ^ f1.Fexp :=
        le_of_lt (zpow_pos htwo_pos f1.Fexp)
      simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
        FloatSpec.Core.Defs.FlocqFloat.Fnum,
        FloatSpec.Core.Defs.FlocqFloat.Fexp]
      exact mul_nonneg hnumR hpowNonneg
    have hprodAbsLeBase :
        |prod| ≤
          _root_.F2R (beta:=2)
              (⟨bo.vNum - 1, f1.Fexp⟩ :
                FloatSpec.Core.Defs.FlocqFloat 2) *
            _root_.F2R (beta:=2)
              (⟨bo.vNum - 1, f2.Fexp⟩ :
                FloatSpec.Core.Defs.FlocqFloat 2) := by
      dsimp [prod]
      rw [abs_mul]
      exact mul_le_mul hf1AbsLe hf2AbsLe
        (abs_nonneg _) hbaseF1Nonneg
    have hpredLePow :
        ((bo.vNum - 1 : Int) : ℝ) ≤ (2 : ℝ) ^ (precision : Int) := by
      have hleInt : bo.vNum - 1 ≤ bo.vNum := by omega
      have hleReal :
          ((bo.vNum - 1 : Int) : ℝ) ≤ (bo.vNum : ℝ) := by
        exact_mod_cast hleInt
      exact le_trans hleReal (le_of_eq hvNum_real)
    have hbaseProdLeUpper :
        _root_.F2R (beta:=2)
              (⟨bo.vNum - 1, f1.Fexp⟩ :
                FloatSpec.Core.Defs.FlocqFloat 2) *
            _root_.F2R (beta:=2)
              (⟨bo.vNum - 1, f2.Fexp⟩ :
                FloatSpec.Core.Defs.FlocqFloat 2) ≤
          _root_.F2R (beta:=2) upper := by
      let A : ℝ := ((bo.vNum - 1 : Int) : ℝ)
      have hANonneg : 0 ≤ A := by
        have hnonneg : (0 : Int) ≤ bo.vNum - 1 := by omega
        simpa [A] using
          (show (0 : ℝ) ≤ ((bo.vNum - 1 : Int) : ℝ) by
            exact_mod_cast hnonneg)
      have hpowZNonneg : 0 ≤ (2 : ℝ) ^ z :=
        le_of_lt (zpow_pos htwo_pos z)
      have hASqLe : A * A ≤ A * (2 : ℝ) ^ (precision : Int) :=
        mul_le_mul_of_nonneg_left (by simpa [A] using hpredLePow) hANonneg
      have hmulExp :
          (2 : ℝ) ^ f1.Fexp * (2 : ℝ) ^ f2.Fexp =
            (2 : ℝ) ^ z := by
        dsimp [z]
        rw [← zpow_add₀ htwo_ne]
      have hupperExp :
          (2 : ℝ) ^ upperExp =
            (2 : ℝ) ^ (precision : Int) * (2 : ℝ) ^ z := by
        dsimp [upperExp]
        exact zpow_add₀ htwo_ne (precision : Int) z
      simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
        FloatSpec.Core.Defs.FlocqFloat.Fnum,
        FloatSpec.Core.Defs.FlocqFloat.Fexp, upper]
      calc
        A * (2 : ℝ) ^ f1.Fexp * (A * (2 : ℝ) ^ f2.Fexp) =
            (A * A) * (2 : ℝ) ^ z := by
              rw [← hmulExp]
              ring
        _ ≤ (A * (2 : ℝ) ^ (precision : Int)) * (2 : ℝ) ^ z :=
          mul_le_mul_of_nonneg_right hASqLe hpowZNonneg
        _ = A * (2 : ℝ) ^ upperExp := by
          rw [hupperExp]
          ring
    have hprodAbsLeUpper :
        |prod| ≤ _root_.F2R (beta:=2) upper :=
      le_trans hprodAbsLeBase hbaseProdLeUpper
    have hprodBounds :
        -_root_.F2R (beta:=2) upper ≤ prod ∧
          prod ≤ _root_.F2R (beta:=2) upper :=
      abs_le.mp hprodAbsLeUpper
    have hupperNonneg : 0 ≤ _root_.F2R (beta:=2) upper :=
      le_trans (abs_nonneg prod) hprodAbsLeUpper
    let lowerUpper : FloatSpec.Core.Defs.FlocqFloat 2 :=
      Fopp (beta:=2) upper
    have hlowerUpperBounded : Fbounded (beta:=2) bo lowerUpper := by
      have h := oppBounded (beta:=2) bo upper
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, oppBounded_check, Id.run,
        ULift.up_down, lowerUpper, Int.cast_ofNat] using h hupperBounded
    have hlowerUpperVal :
        _root_.F2R (beta:=2) lowerUpper =
          -_root_.F2R (beta:=2) upper := by
      have h := FloatSpec.Calc.Operations.F2R_opp (beta:=2) upper
      simpa [lowerUpper, Fopp, _root_.F2R] using h trivial
    have hclosestProd : Closest (beta:=2) bo (2 : ℝ) prod g := by
      simpa [prod] using hclosest
    have hgLeUpper :
        _root_.F2R (beta:=2) g ≤ _root_.F2R (beta:=2) upper := by
      by_contra hnot
      have hlt :
          _root_.F2R (beta:=2) upper < _root_.F2R (beta:=2) g :=
        lt_of_not_ge hnot
      have hdist := hclosestProd.2 upper hupperBounded
      have hgSubNonneg : 0 ≤ _root_.F2R (beta:=2) g - prod := by
        linarith [hprodBounds.2]
      have huSubNonneg : 0 ≤ _root_.F2R (beta:=2) upper - prod := by
        linarith [hprodBounds.2]
      rw [abs_of_nonneg hgSubNonneg, abs_of_nonneg huSubNonneg] at hdist
      linarith
    have hlowerUpperLeG :
        -_root_.F2R (beta:=2) upper ≤ _root_.F2R (beta:=2) g := by
      by_contra hnot
      have hlt :
          _root_.F2R (beta:=2) g <
            -_root_.F2R (beta:=2) upper := lt_of_not_ge hnot
      have hdist := hclosestProd.2 lowerUpper hlowerUpperBounded
      rw [hlowerUpperVal] at hdist
      have hgSubNonpos : _root_.F2R (beta:=2) g - prod ≤ 0 := by
        linarith [hprodBounds.1]
      have hlSubNonpos :
          -_root_.F2R (beta:=2) upper - prod ≤ 0 := by
        linarith [hprodBounds.1]
      rw [abs_of_nonpos hgSubNonpos, abs_of_nonpos hlSubNonpos] at hdist
      linarith
    have hgAbsLeUpper :
        |_root_.F2R (beta:=2) g| ≤ _root_.F2R (beta:=2) upper :=
      abs_le.mpr ⟨hlowerUpperLeG, hgLeUpper⟩
    have hupperCan : Fcanonic (beta:=2) 2 bo upper := by
      have hlowerExp : -bo.dExp ≤ upperExp := by
        dsimp [upperExp, z]
        omega
      simpa [upper, pPred] using
        (FcanonicPpred (beta:=2) 2 bo precision upperExp
          (by norm_num) (by omega) hvNum hlowerExp)
    have hupperAbs :
        |_root_.F2R (beta:=2) upper| =
          _root_.F2R (beta:=2) upper :=
      abs_of_nonneg hupperNonneg
    have hgExpLeUpper : g.Fexp ≤ upper.Fexp := by
      have h := Fcanonic_Rle_Zle (beta:=2) 2 bo g upper
        (by norm_num) rfl
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fcanonic_Rle_Zle_check,
        ULift.down_up, Fcanonic', Int.cast_ofNat] using
        h ⟨hgCan, hupperCan, by simpa [hupperAbs] using hgAbsLeUpper⟩
    have hgExpLe : g.Fexp ≤ (precision : Int) + z := by
      simpa [upper, upperExp] using hgExpLeUpper
    let e : Int := g.Fexp - precision
    have heLower : -bo.dExp ≤ e := by
      dsimp [e]
      omega
    have heLeZ : e ≤ z := by
      dsimp [e]
      omega
    have hErr := errorBoundedMult (beta:=2) bo 2 precision f1 f2 g
    rcases (by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, errorBoundedMult_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        hErr ⟨rfl, by norm_num, hprecision_ne, hvNum, hf1, hf2,
          by simpa [z] using hExpProduct, hclosest⟩) with
      ⟨s0, hs0Val, _hs0Bound, hs0Exp⟩
    have heLeS0 : e ≤ s0.Fexp := by
      rw [hs0Exp]
      exact heLeZ
    have hrep := F2R_rep_at_lower_exp (beta:=2) s0 e
    rcases (by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, F2R_rep_at_lower_exp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using hrep ⟨by norm_num, heLeS0⟩) with
      ⟨m, hm⟩
    let s : FloatSpec.Core.Defs.FlocqFloat 2 := ⟨m, e⟩
    have hsVal :
        _root_.F2R (beta:=2) s =
          _root_.F2R (beta:=2) f1 * _root_.F2R (beta:=2) f2 -
            _root_.F2R (beta:=2) g := by
      calc
        _root_.F2R (beta:=2) s = _root_.F2R (beta:=2) s0 := by
          simpa [s] using hm.symm
        _ = _ := hs0Val
    have hsExp : s.Fexp = g.Fexp - precision := by
      rfl
    have hclosestUlp :
        2 *
            |_root_.F2R (beta:=2) f1 * _root_.F2R (beta:=2) f2 -
              _root_.F2R (beta:=2) g| ≤
          Fulp (beta:=2) bo 2 precision g := by
      have h := ClosestUlp (beta:=2) bo 2 precision
        (_root_.F2R (beta:=2) f1 * _root_.F2R (beta:=2) f2) g
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hclosest, rfl, by norm_num, hprecision_ne, hvNum⟩
    have hcanFulp :
        Fulp (beta:=2) bo 2 precision g = (2 : ℝ) ^ g.Fexp := by
      have h := CanonicFulp (beta:=2) 2 bo precision g
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hgCan, rfl, by norm_num, hprecision_ne, hvNum⟩
    have hsAbsBound :
        2 * |_root_.F2R (beta:=2) s| ≤ (2 : ℝ) ^ g.Fexp := by
      calc
        2 * |_root_.F2R (beta:=2) s| =
            2 *
              |_root_.F2R (beta:=2) f1 * _root_.F2R (beta:=2) f2 -
                _root_.F2R (beta:=2) g| := by rw [hsVal]
        _ ≤ Fulp (beta:=2) bo 2 precision g := hclosestUlp
        _ = (2 : ℝ) ^ g.Fexp := hcanFulp
    have hsAbsF2R :
        |_root_.F2R (beta:=2) s| =
          ((|s.Fnum| : Int) : ℝ) * (2 : ℝ) ^ s.Fexp := by
      have hpow_pos : 0 < (2 : ℝ) ^ s.Fexp := zpow_pos htwo_pos s.Fexp
      simp only [_root_.F2R, FloatSpec.Core.Defs.F2R]
      rw [abs_mul]
      simpa [abs_of_pos hpow_pos, Int.cast_abs]
    have hpow_split :
        (2 : ℝ) ^ g.Fexp =
          (2 : ℝ) ^ s.Fexp * (2 : ℝ) ^ (precision : Int) := by
      rw [hsExp]
      calc
        (2 : ℝ) ^ g.Fexp =
            (2 : ℝ) ^ ((g.Fexp - (precision : Int)) + (precision : Int)) := by
              congr 1
              omega
        _ = (2 : ℝ) ^ (g.Fexp - (precision : Int)) *
              (2 : ℝ) ^ (precision : Int) := by
              rw [zpow_add₀ htwo_ne]
    have hscaled :
        2 * (((|s.Fnum| : Int) : ℝ) * (2 : ℝ) ^ s.Fexp) ≤
          (2 : ℝ) ^ s.Fexp * (2 : ℝ) ^ (precision : Int) := by
      simpa [hsAbsF2R, hpow_split] using hsAbsBound
    have hmant_half :
        ((|s.Fnum| : Int) : ℝ) ≤ (2 : ℝ) ^ (precision : Int) / 2 := by
      have hpow_s_pos : 0 < (2 : ℝ) ^ s.Fexp := zpow_pos htwo_pos s.Fexp
      nlinarith
    have hhalf_eq :
        (2 : ℝ) ^ (precision : Int) / 2 =
          (2 : ℝ) ^ ((precision : Int) - 1) := by
      have hexp : (precision : Int) - 1 + 1 = (precision : Int) := by omega
      calc
        (2 : ℝ) ^ (precision : Int) / 2 =
            ((2 : ℝ) ^ ((precision : Int) - 1) * (2 : ℝ)) / 2 := by
              rw [← hexp, zpow_add₀ htwo_ne]
              norm_num
        _ = (2 : ℝ) ^ ((precision : Int) - 1) := by ring
    have hmant :
        ((|s.Fnum| : Int) : ℝ) ≤
          (2 : ℝ) ^ ((precision : Int) - 1) := by
      rw [← hhalf_eq]
      exact hmant_half
    have hpowPredLt :
        (2 : ℝ) ^ ((precision : Int) - 1) <
          (2 : ℝ) ^ (precision : Int) :=
      zpow_lt_zpow_right₀ htwo_one_lt (by omega)
    have hmantLtVNumReal :
        ((|s.Fnum| : Int) : ℝ) < (bo.vNum : ℝ) := by
      rw [hvNum_real]
      exact lt_of_le_of_lt hmant hpowPredLt
    have hmantLtVNum : |s.Fnum| < bo.vNum := by
      exact_mod_cast hmantLtVNumReal
    have hsBound : Fbounded (beta:=2) bo s := by
      constructor
      · exact hmantLtVNum
      · simpa [s] using heLower
    exact ⟨s, hsBound, hsVal, hsExp, hmant⟩

/-- Coq: `ClosestImplyEven_int`.

Integer-midpoint form of `ClosestImplyEven`: if `z` is the midpoint
`radix^(Fexp f) * (m + 1/2)` with integer `m`, then even-closest rounding
forces the canonical nonnegative float `f` to be normalized-even. -/
theorem ClosestImplyEven_int {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (z m : ℝ) (f : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Even radix ∧
        EvenClosest (beta:=beta) b (radix : ℝ) precision z f ∧
        Fcanonic (beta:=beta) radix b f ∧
        0 ≤ _root_.F2R (beta:=beta) f ∧
        z = (radix : ℝ) ^ f.Fexp * (m + 1 / 2) ∧
        (∃ n : Int, (n : ℝ) = m) ∧
        beta = radix ∧ 1 < radix ∧ 1 < precision ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (ClosestImplyEven_int_check (beta:=beta) b radix precision z m f) :
      Id Unit)
    ⦃⇓_ => ⌜FNeven (beta:=beta) b (radix : ℝ) precision f⌝⦄ := by
  intro h
  rcases h with
    ⟨hEvenRadix, hEvenClosest, hfCan, hfNonneg, hz, ⟨n, hn⟩,
      hbeta, hradix, hprecision_gt, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, ClosestImplyEven_int_check,
    Id.run, ULift.up_down]
  have hprecision : precision ≠ 0 := by omega
  have hprecision_pos : 0 < precision := by omega
  have hbeta_gt : 1 < beta := by omega
  have hradix_pos_int : (0 : Int) < radix := by omega
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradix_pos_int
  have hpow_pos : 0 < (radix : ℝ) ^ f.Fexp :=
    zpow_pos hradix_pos f.Fexp
  have hfNum_nonneg : 0 ≤ f.Fnum :=
    FloatSpec.Core.Float_prop.ge_0_F2R (beta:=beta) f hbeta_gt hfNonneg
  have hnorm_eq_f :
      Fnormalize (beta:=beta) beta b precision f = f := by
    have h := FcanonicFnormalizeEq (beta:=beta) beta b precision f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨by simpa [hbeta] using hfCan, rfl, hbeta_gt, hprecision,
        by simpa [hbeta] using hvNum⟩
  by_cases hfZero : f.Fnum = 0
  · unfold FNeven
    rw [hnorm_eq_f]
    simp [Feven, hfZero]
  by_cases hfMin : f.Fnum = nNormMin radix precision
  · unfold FNeven
    rw [hnorm_eq_f]
    unfold Feven
    rw [hfMin]
    unfold nNormMin
    have hs : Nat.succ (precision - 2) = precision - 1 := by omega
    rw [← hs]
    exact hEvenRadix.pow_of_ne_zero (Nat.succ_ne_zero _)
  have hfNum_pos : 0 < f.Fnum := lt_of_le_of_ne hfNum_nonneg (Ne.symm hfZero)
  have hclosest_ulp :
      2 * |z - _root_.F2R (beta:=beta) f| ≤
        Fulp (beta:=beta) b radix precision f := by
    have h := ClosestUlp (beta:=beta) b radix precision z f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hEvenClosest.1, hbeta, hradix, hprecision, hvNum⟩
  have hclosest_exp :
      2 * |z - _root_.F2R (beta:=beta) f| ≤
        (radix : ℝ) ^ f.Fexp := by
    have h := ClosestExp_from_ulp_payload (beta:=beta) b radix precision z f
    have hle :
        2 * |z - _root_.F2R (beta:=beta) f| ≤
          (beta : ℝ) ^ f.Fexp := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestExp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hEvenClosest.1, hclosest_ulp, hbeta, hradix⟩
    simpa [hbeta] using hle
  let mid : FloatSpec.Core.Defs.FlocqFloat beta := ⟨n, f.Fexp⟩
  have hdist_coeff :
      |(n : ℝ) + 1 / 2 - (f.Fnum : ℝ)| ≤ (1 / 2 : ℝ) := by
    have hdist_eq :
        |z - _root_.F2R (beta:=beta) f| =
          |(n : ℝ) + 1 / 2 - (f.Fnum : ℝ)| *
            (radix : ℝ) ^ f.Fexp := by
      rw [hz, ← hn]
      dsimp [_root_.F2R, FloatSpec.Core.Defs.F2R]
      have hcast : (beta : ℝ) = (radix : ℝ) := by
        exact_mod_cast hbeta
      rw [hcast]
      have hfactor :
          (radix : ℝ) ^ f.Fexp * ((n : ℝ) + 1 / 2) -
              (f.Fnum : ℝ) * (radix : ℝ) ^ f.Fexp =
            ((n : ℝ) + 1 / 2 - (f.Fnum : ℝ)) *
              (radix : ℝ) ^ f.Fexp := by
        ring
      rw [hfactor, abs_mul, abs_of_pos hpow_pos]
    have hscaled :
        2 * (|(n : ℝ) + 1 / 2 - (f.Fnum : ℝ)| *
            (radix : ℝ) ^ f.Fexp) ≤
          (radix : ℝ) ^ f.Fexp := by
      simpa [hdist_eq] using hclosest_exp
    nlinarith [hpow_pos]
  have hn_le_fnum : n ≤ f.Fnum := by
    have hright := (abs_le.mp hdist_coeff).2
    have hreal : (n : ℝ) ≤ (f.Fnum : ℝ) := by linarith
    exact_mod_cast hreal
  have hfnum_pred_le_n : f.Fnum - 1 ≤ n := by
    have hleft := (abs_le.mp hdist_coeff).1
    have hreal : ((f.Fnum - 1 : Int) : ℝ) ≤ (n : ℝ) := by
      norm_num at hleft ⊢
      linarith
    exact_mod_cast hreal
  have hn_nonneg : 0 ≤ n := by
    omega
  have hmid_val :
      z = _root_.F2R (beta:=beta) mid + (radix : ℝ) ^ mid.Fexp / 2 := by
    rw [hz, ← hn]
    dsimp [mid, _root_.F2R, FloatSpec.Core.Defs.F2R]
    have hcast : (beta : ℝ) = (radix : ℝ) := by
      exact_mod_cast hbeta
    rw [hcast]
    ring
  have hmidCan : Fcanonic (beta:=beta) radix b mid := by
    rcases hfCan with hfNorm | hfSub
    · left
      rcases hfNorm with ⟨hfBound, hfNormMant⟩
      have hfNum_lt_vNum : f.Fnum < b.vNum := by
        have habs : |f.Fnum| = f.Fnum := abs_of_nonneg hfNum_nonneg
        simpa [habs] using hfBound.1
      have hn_lt_vNum : n < b.vNum := lt_of_le_of_lt hn_le_fnum hfNum_lt_vNum
      have hmidBound : Fbounded (beta:=beta) b mid := by
        constructor
        · dsimp [mid]
          rw [abs_of_nonneg hn_nonneg]
          exact hn_lt_vNum
        · exact hfBound.2
      constructor
      · exact hmidBound
      · have hposnorm := PosNormMin radix b precision hprecision_pos hvNum
        have hnorm_le_fnum : nNormMin radix precision ≤ f.Fnum := by
          have hradix_mul_le : radix * nNormMin radix precision ≤ radix * f.Fnum := by
            have hfNormMant' :
                b.vNum ≤ radix * f.Fnum := by
              have hmul_nonneg : 0 ≤ radix * f.Fnum := by
                exact mul_nonneg (by omega) hfNum_nonneg
              simpa [abs_of_nonneg hmul_nonneg] using hfNormMant
            simpa [hposnorm] using hfNormMant'
          exact (Int.mul_le_mul_left hradix_pos_int).mp hradix_mul_le
        have hnorm_lt_fnum : nNormMin radix precision < f.Fnum := by
          exact lt_of_le_of_ne hnorm_le_fnum (Ne.symm hfMin)
        have hnorm_le_n : nNormMin radix precision ≤ n := by omega
        have hmul_nonneg : 0 ≤ radix * n := by
          exact mul_nonneg (by omega) hn_nonneg
        rw [abs_of_nonneg hmul_nonneg]
        rw [hposnorm]
        exact Int.mul_le_mul_of_nonneg_left hnorm_le_n (by omega : 0 ≤ radix)
    · right
      rcases hfSub with ⟨hfBound, hfExp, hfSubMant⟩
      have hfNum_lt_vNum : f.Fnum < b.vNum := by
        have habs : |f.Fnum| = f.Fnum := abs_of_nonneg hfNum_nonneg
        simpa [habs] using hfBound.1
      have hn_lt_vNum : n < b.vNum := lt_of_le_of_lt hn_le_fnum hfNum_lt_vNum
      have hmidBound : Fbounded (beta:=beta) b mid := by
        constructor
        · dsimp [mid]
          rw [abs_of_nonneg hn_nonneg]
          exact hn_lt_vNum
        · exact hfBound.2
      constructor
      · exact hmidBound
      constructor
      · exact hfExp
      · have hmul_f_nonneg : 0 ≤ radix * f.Fnum := by
          exact mul_nonneg (by omega) hfNum_nonneg
        have hmul_n_nonneg : 0 ≤ radix * n := by
          exact mul_nonneg (by omega) hn_nonneg
        rw [abs_of_nonneg hmul_n_nonneg]
        have hfSubMant' : radix * f.Fnum < b.vNum := by
          simpa [abs_of_nonneg hmul_f_nonneg] using hfSubMant
        exact lt_of_le_of_lt
          (Int.mul_le_mul_of_nonneg_left hn_le_fnum (by omega : 0 ≤ radix))
          hfSubMant'
  have hmid : ∃ g : FloatSpec.Core.Defs.FlocqFloat beta,
      z = _root_.F2R (beta:=beta) g + (radix : ℝ) ^ g.Fexp / 2 ∧
      Fcanonic (beta:=beta) radix b g ∧ 0 ≤ g.Fnum :=
    ⟨mid, hmid_val, hmidCan, hn_nonneg⟩
  have h := ClosestImplyEven (beta:=beta) b radix precision z f
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestImplyEven_check,
    Id.run, ULift.up_down, Int.cast_ofNat] using
    h ⟨hEvenClosest, hmid, hbeta, hradix, hprecision_gt, hvNum⟩

/-- Coq: `plusExact1`.

If the closest rounded sum has exponent no greater than both input exponents,
then the rounding error must be zero, so the rounded result is exact. -/
theorem plusExact1_from_format_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat) :
    beta = radix →
    1 < radix →
    precision ≠ 0 →
    1 < bo.vNum →
    bo.vNum = Zpower_nat radix precision →
    ∀ p q r : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      Closest (beta:=beta) bo (radix : ℝ)
        (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) r →
      r.Fexp ≤ min p.Fexp q.Fexp →
      _root_.F2R (beta:=beta) r =
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q := by
  intro hbeta hradix hprecision hvNum_gt hvNum p q r hp hq hclosest hrExp
  rcases errorBoundedPlus_from_format_payload (beta:=beta) bo radix precision hbeta hradix
      hprecision hvNum_gt hvNum p q r hp hq hclosest with
    ⟨error, herrorVal, herrorBounded, herrorExp⟩
  let sum : ℝ := _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q
  have hclosestSum : Closest (beta:=beta) bo (radix : ℝ) sum r := by
    dsimp [sum]
    simpa using hclosest
  have herrorVal' :
      _root_.F2R (beta:=beta) error = sum - _root_.F2R (beta:=beta) r := by
    dsimp [sum]
    simpa [sub_eq_add_neg, add_comm, add_left_comm, add_assoc] using herrorVal
  by_cases hzero : _root_.F2R (beta:=beta) error = 0
  · have hdelta_zero : sum - _root_.F2R (beta:=beta) r = 0 := by
      rw [← herrorVal']
      exact hzero
    dsimp [sum] at hdelta_zero
    linarith
  · have hclosestUlp :
        2 * |sum - _root_.F2R (beta:=beta) r| ≤
          Fulp (beta:=beta) bo radix precision r := by
      have h := ClosestUlp (beta:=beta) bo radix precision sum r
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using
        h ⟨hclosestSum, hbeta, hradix, hprecision, hvNum⟩
    have hFulpLe :
        Fulp (beta:=beta) bo radix precision r ≤ (beta : ℝ) ^ r.Fexp := by
      have h := FulpLe (beta:=beta) radix bo precision r
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpLe_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using h ⟨hclosest.1, hbeta, hradix⟩
    have hClosestExp :
        2 * |sum - _root_.F2R (beta:=beta) r| ≤ (beta : ℝ) ^ r.Fexp :=
      le_trans hclosestUlp hFulpLe
    have hbeta_ge : (1 : ℝ) ≤ (beta : ℝ) := by
      have hbeta_int_ge : (1 : Int) ≤ beta := by
        rw [hbeta]
        exact le_of_lt hradix
      exact_mod_cast hbeta_int_ge
    have hstrict : error.Fexp < r.Fexp := by
      have h := ClosestErrorExpStrict_from_exp_bound_payload (beta:=beta) bo (radix : ℝ) r error sum
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestErrorExpStrict_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hclosest.1, herrorBounded, hclosestSum, herrorVal',
          hzero, hbeta_ge, hClosestExp⟩
    have hmin_lt : min p.Fexp q.Fexp < r.Fexp := by
      simpa [herrorExp] using hstrict
    exact False.elim ((not_lt_of_ge hrExp) hmin_lt)

/-- Exact exported Coq `plusExact1` contract. -/
theorem plusExact1 {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hprecision : 1 < precision)
    (hvNum : bo.vNum = Zpower_nat radix precision) :
    ∀ p q r : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      Closest (beta:=beta) bo (radix : ℝ)
        (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) r →
      r.Fexp ≤ min p.Fexp q.Fexp →
      _root_.F2R (beta:=beta) r =
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q := by
  have hprecision_ne : precision ≠ 0 := by omega
  have hvNum_gt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecision_ne
  letI : ValidRadix radix := ⟨hradix⟩
  exact plusExact1_from_format_payload (beta:=beta) bo radix precision
    hbeta hradix hprecision_ne hvNum_gt hvNum

/-- Coq: `plusExact2Aux`.

For a nonnegative canonical `p`, if the closest rounded sum has exponent below
`pred (Fexp p)`, then the rounded result is exact. -/
theorem plusExact2Aux_from_format_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat) :
    beta = radix →
    1 < radix →
    precision ≠ 0 →
    1 < bo.vNum →
    bo.vNum = Zpower_nat radix precision →
    ∀ p q r : FloatSpec.Core.Defs.FlocqFloat beta,
      0 ≤ _root_.F2R (beta:=beta) p →
      Fcanonic (beta:=beta) radix bo p →
      Fbounded (beta:=beta) bo q →
      Closest (beta:=beta) bo (radix : ℝ)
        (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) r →
      r.Fexp < Int.pred p.Fexp →
      _root_.F2R (beta:=beta) r =
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q := by
  intro hbeta hradix hprecision hvNum_gt hvNum p q r
    hp_nonneg hp_can hq hclosest hr_lt
  subst beta
  have hBoundExp :
      ∀ y : ℝ, -bo.dExp ≤ (boundR (beta:=radix) radix y).Fexp := by
    intro y
    exact le_trans (neg_nonpos.mpr bo.dExp_nonneg) (by simp [boundR, boundNat])
  have hp : Fbounded (beta:=radix) bo p := by
    have h := FcanonicBound (beta:=radix) radix bo p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicBound_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hp_can
  have hmain :
      r.Fexp ≤ min p.Fexp q.Fexp := by
    by_cases hpq : p.Fexp ≤ q.Fexp
    · rw [min_eq_left hpq]
      simp [Int.pred] at hr_lt
      omega
    · have hq_lt_p : q.Fexp < p.Fexp := lt_of_not_ge hpq
      by_cases hsucc : p.Fexp = q.Fexp + 1
      · rw [min_eq_right (le_of_lt hq_lt_p)]
        rw [hsucc] at hr_lt
        simp [Int.pred] at hr_lt
        omega
      · have hq_lt_pred : q.Fexp < Int.pred p.Fexp := by
          simp [Int.pred]
          omega
        exfalso
        let a : FloatSpec.Core.Defs.FlocqFloat radix :=
          ⟨nNormMin radix precision, Int.pred p.Fexp⟩
        have ha_bound : Fbounded (beta:=radix) bo a := by
          have ha_can : Fcanonic (beta:=radix) radix bo a := by
            have h := FcanonicNnormMin (beta:=radix) radix bo precision
              (Int.pred p.Fexp)
            simpa only [a, Int.cast_ofNat] using h hradix (Nat.pos_iff_ne_zero.mpr hprecision)
              hvNum (le_trans hq.2 (le_of_lt hq_lt_pred))
          have h := FcanonicBound (beta:=radix) radix bo a
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicBound_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using h ha_can
        have hTotal : TotalP (Closest (beta:=radix) bo (radix : ℝ)) := by
          intro x
          have hMinTotal : TotalP (isMin (beta:=radix) bo radix) := by
            intro y
            have h := MinEx_from_finite_box_payload (beta:=radix) bo radix y
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinEx_check, Id.run,
              ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hvNum_gt, hBoundExp y⟩
          have hMaxTotal : TotalP (isMax (beta:=radix) bo radix) := by
            intro y
            have h := MaxEx_from_finite_box_payload (beta:=radix) bo radix y
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MaxEx_check, Id.run,
              ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hvNum_gt, hBoundExp y⟩
          have h := ClosestTotal_from_extrema_payload (beta:=radix) bo radix (radix : ℝ) x
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestTotal_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hMinTotal, hMaxTotal⟩
        have hRounded :
            RoundedModeP_full (beta:=radix) bo
              (Closest (beta:=radix) bo (radix : ℝ)) := by
          have h := ClosestRoundedModeP_from_total_payload (beta:=radix) bo (radix : ℝ)
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestRoundedModeP_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using h hTotal
        rcases hRounded with ⟨_, _, hMono, hProj, _⟩
        have ha_closest :
            Closest (beta:=radix) bo (radix : ℝ)
              (_root_.F2R (beta:=radix) a) a :=
          hProj a ha_bound
        have ha_le_sum :
            _root_.F2R (beta:=radix) a ≤
              _root_.F2R (beta:=radix) p + _root_.F2R (beta:=radix) q := by
          have hradix_pos_int : (0 : Int) < radix := by omega
          have hradix_pos : (0 : ℝ) < (radix : ℝ) := by exact_mod_cast hradix_pos_int
          have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
          have hradix_two : (2 : ℝ) ≤ (radix : ℝ) := by
            exact_mod_cast (by omega : (2 : Int) ≤ radix)
          have hnmin_pos_int : 0 < nNormMin radix precision := by
            cases precision with
            | zero => exact False.elim (hprecision rfl)
            | succ k =>
                simp [nNormMin]
                exact pow_pos hradix_pos_int k
          have hnmin_pos : (0 : ℝ) < (nNormMin radix precision : ℝ) := by
            exact_mod_cast hnmin_pos_int
          have ha_pos : 0 < _root_.F2R (beta:=radix) a := by
            dsimp [a, _root_.F2R, FloatSpec.Core.Defs.F2R]
            exact mul_pos hnmin_pos (zpow_pos hradix_pos _)
          rcases hp_can with hp_norm | hp_sub
          · let ap : FloatSpec.Core.Defs.FlocqFloat radix :=
              ⟨nNormMin radix precision, p.Fexp⟩
            have hap_scale :
                _root_.F2R (beta:=radix) ap =
                  (radix : ℝ) * _root_.F2R (beta:=radix) a := by
              dsimp [ap, a, _root_.F2R, FloatSpec.Core.Defs.F2R]
              rw [show p.Fexp = Int.pred p.Fexp + 1 by simp [Int.pred]]
              rw [zpow_add₀ hradix_ne]
              simp [Int.pred]
              ring
            have hmin_abs : nNormMin radix precision ≤ |p.Fnum| := by
              have h := pNormal_absolu_min (beta:=radix) radix bo precision p
              simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, pNormal_absolu_min_check,
                Id.run, ULift.up_down, Int.cast_ofNat] using
                h ⟨hp_norm, hp_norm, hprecision, hradix, hvNum⟩
            have hp_num_nonneg : 0 ≤ p.Fnum := by
              exact FloatSpec.Core.Float_prop.ge_0_F2R (beta:=radix) p
                hradix hp_nonneg
            have hmin_num_int : nNormMin radix precision ≤ p.Fnum := by
              simpa [abs_of_nonneg hp_num_nonneg] using hmin_abs
            have hmin_num : (nNormMin radix precision : ℝ) ≤ (p.Fnum : ℝ) := by
              exact_mod_cast hmin_num_int
            have hap_le_p :
                _root_.F2R (beta:=radix) ap ≤ _root_.F2R (beta:=radix) p := by
              have hpow_nonneg : 0 ≤ (radix : ℝ) ^ p.Fexp :=
                le_of_lt (zpow_pos hradix_pos _)
              dsimp [ap, _root_.F2R, FloatSpec.Core.Defs.F2R]
              exact mul_le_mul_of_nonneg_right hmin_num hpow_nonneg
            have htwo_a_le_ap :
                2 * _root_.F2R (beta:=radix) a ≤
                  _root_.F2R (beta:=radix) ap := by
              rw [hap_scale]
              exact mul_le_mul_of_nonneg_right hradix_two (le_of_lt ha_pos)
            by_cases hq_nonneg : 0 ≤ _root_.F2R (beta:=radix) q
            · linarith
            · have hq_neg : _root_.F2R (beta:=radix) q < 0 :=
                lt_of_not_ge hq_nonneg
              have hq_abs_lt_a :
                  |_root_.F2R (beta:=radix) q| <
                    _root_.F2R (beta:=radix) a := by
                have h := maxMaxBis (beta:=radix) bo q (Int.pred p.Fexp) precision
                have hvPow : bo.vNum = radix ^ precision := by
                  simpa [Zpower_nat] using hvNum
                simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, maxMax1_check,
                  Id.run, ULift.up_down, a, Int.cast_ofNat] using
                  h ⟨hq, hq_lt_pred, hq, hradix, hvPow⟩
              have hneg_q_lt_a :
                  -_root_.F2R (beta:=radix) q <
                    _root_.F2R (beta:=radix) a := by
                rwa [abs_of_neg hq_neg] at hq_abs_lt_a
              linarith
          · have hp_exp : p.Fexp = -bo.dExp := hp_sub.2.1
            have hq_low : q.Fexp < -bo.dExp := by
              rw [hp_exp] at hq_lt_pred
              simp [Int.pred] at hq_lt_pred
              omega
            exact False.elim ((not_lt_of_ge hq.2) hq_low)
        have ha_le_r :
            _root_.F2R (beta:=radix) a ≤ _root_.F2R (beta:=radix) r := by
          by_cases heq :
              _root_.F2R (beta:=radix) a =
                _root_.F2R (beta:=radix) p + _root_.F2R (beta:=radix) q
          · rw [heq]
            have hdist := hclosest.2 a ha_bound
            rw [← heq, sub_self, abs_zero] at hdist
            have hz : |_root_.F2R (beta:=radix) r -
                _root_.F2R (beta:=radix) a| = 0 :=
              le_antisymm hdist (abs_nonneg _)
            have hsub :
                _root_.F2R (beta:=radix) r -
                  _root_.F2R (beta:=radix) a = 0 :=
              abs_eq_zero.mp hz
            linarith
          · have hlt :
                _root_.F2R (beta:=radix) a <
                  _root_.F2R (beta:=radix) p + _root_.F2R (beta:=radix) q :=
              lt_of_le_of_ne ha_le_sum heq
            exact hMono (_root_.F2R (beta:=radix) a)
              (_root_.F2R (beta:=radix) p + _root_.F2R (beta:=radix) q)
              a r hlt ha_closest hclosest
        have hr_lt_a :
            _root_.F2R (beta:=radix) r < _root_.F2R (beta:=radix) a := by
          have hradix_pos_int : (0 : Int) < radix := by omega
          have hradix_pos : (0 : ℝ) < (radix : ℝ) := by exact_mod_cast hradix_pos_int
          have hnmin_pos_int : 0 < nNormMin radix precision := by
            cases precision with
            | zero => exact False.elim (hprecision rfl)
            | succ k =>
                simp [nNormMin]
                exact pow_pos hradix_pos_int k
          have hnmin_pos : (0 : ℝ) < (nNormMin radix precision : ℝ) := by
            exact_mod_cast hnmin_pos_int
          have ha_pos : 0 < _root_.F2R (beta:=radix) a := by
            dsimp [a, _root_.F2R, FloatSpec.Core.Defs.F2R]
            exact mul_pos hnmin_pos (zpow_pos hradix_pos _)
          by_cases hr_nonneg : 0 ≤ _root_.F2R (beta:=radix) r
          · have hr_abs_lt_a :
                |_root_.F2R (beta:=radix) r| <
                  _root_.F2R (beta:=radix) a := by
              have h := maxMaxBis (beta:=radix) bo r (Int.pred p.Fexp) precision
              have hvPow : bo.vNum = radix ^ precision := by
                simpa [Zpower_nat] using hvNum
              simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, maxMax1_check,
                Id.run, ULift.up_down, a, Int.cast_ofNat] using
                h ⟨hclosest.1, hr_lt, hclosest.1, hradix, hvPow⟩
            rwa [abs_of_nonneg hr_nonneg] at hr_abs_lt_a
          · have hr_neg : _root_.F2R (beta:=radix) r < 0 :=
              lt_of_not_ge hr_nonneg
            linarith
        exact not_lt_of_ge ha_le_r hr_lt_a
  exact plusExact1_from_format_payload (beta:=radix) bo radix precision rfl hradix hprecision
    hvNum_gt hvNum p q r hp hq hclosest hmain

/-- Exact exported Coq `plusExact2Aux` contract. -/
theorem plusExact2Aux {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hprecision : 1 < precision)
    (hvNum : bo.vNum = Zpower_nat radix precision) :
    ∀ p q r : FloatSpec.Core.Defs.FlocqFloat beta,
      0 ≤ _root_.F2R (beta:=beta) p →
      Fcanonic (beta:=beta) radix bo p →
      Fbounded (beta:=beta) bo q →
      Closest (beta:=beta) bo (radix : ℝ)
        (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) r →
      r.Fexp < Int.pred p.Fexp →
      _root_.F2R (beta:=beta) r =
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q := by
  have hprecision_ne : precision ≠ 0 := by omega
  have hvNum_gt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecision_ne
  letI : ValidRadix radix := ⟨hradix⟩
  exact plusExact2Aux_from_format_payload (beta:=beta) bo radix precision
    hbeta hradix hprecision_ne hvNum_gt hvNum

/-- Coq: `plusExact2`.

If the closest rounded sum has exponent below `pred (Fexp p)` and `p` is
canonical, then the rounded result is exact. -/
theorem plusExact2_from_format_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat) :
    beta = radix →
    1 < radix →
    precision ≠ 0 →
    1 < bo.vNum →
    bo.vNum = Zpower_nat radix precision →
    ∀ p q r : FloatSpec.Core.Defs.FlocqFloat beta,
      Fcanonic (beta:=beta) radix bo p →
      Fbounded (beta:=beta) bo q →
      Closest (beta:=beta) bo (radix : ℝ)
        (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) r →
      r.Fexp < Int.pred p.Fexp →
      _root_.F2R (beta:=beta) r =
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q := by
  intro hbeta hradix hprecision hvNum_gt hvNum p q r
    hp_can hq_bound hclosest hr_lt
  by_cases hp_nonneg : 0 ≤ _root_.F2R (beta:=beta) p
  · exact plusExact2Aux_from_format_payload (beta:=beta) bo radix precision hbeta hradix
      hprecision hvNum_gt hvNum p q r hp_nonneg hp_can
      hq_bound hclosest hr_lt
  · have hp_neg : _root_.F2R (beta:=beta) p < 0 := lt_of_not_ge hp_nonneg
    have hp_opp_val :
        _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) =
          -_root_.F2R (beta:=beta) p := by
      have h := Fopp_correct (beta:=beta) p
      simpa [wp, PostCond.noThrow, pure, Fopp_correct_check, Id.run,
        ULift.up_down, Fopp] using h True.intro
    have hq_opp_val :
        _root_.F2R (beta:=beta) (Fopp (beta:=beta) q) =
          -_root_.F2R (beta:=beta) q := by
      have h := Fopp_correct (beta:=beta) q
      simpa [wp, PostCond.noThrow, pure, Fopp_correct_check, Id.run,
        ULift.up_down, Fopp] using h True.intro
    have hr_opp_val :
        _root_.F2R (beta:=beta) (Fopp (beta:=beta) r) =
          -_root_.F2R (beta:=beta) r := by
      have h := Fopp_correct (beta:=beta) r
      simpa [wp, PostCond.noThrow, pure, Fopp_correct_check, Id.run,
        ULift.up_down, Fopp] using h True.intro
    have hp_opp_nonneg : 0 ≤ _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) := by
      rw [hp_opp_val]
      linarith
    have hp_opp_can : Fcanonic (beta:=beta) radix bo (Fopp (beta:=beta) p) := by
      have h := FcanonicFopp (beta:=beta) radix bo p
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFopp_check, Int.cast_ofNat] using h hp_can
    have hq_opp_bound : Fbounded (beta:=beta) bo (Fopp (beta:=beta) q) := by
      have h := oppBounded (beta:=beta) bo q
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, oppBounded_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using h hq_bound
    have hclosest_opp_sum :
        Closest (beta:=beta) bo (radix : ℝ)
          (-(_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q))
          (Fopp (beta:=beta) r) := by
      have h := ClosestOpp (beta:=beta) bo (radix : ℝ) r
        (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestOpp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h hclosest
    have hclosest_opp :
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) (Fopp (beta:=beta) p) +
            _root_.F2R (beta:=beta) (Fopp (beta:=beta) q))
          (Fopp (beta:=beta) r) := by
      simpa [hp_opp_val, hq_opp_val, neg_add, add_comm] using hclosest_opp_sum
    have hr_opp_lt :
        (Fopp (beta:=beta) r).Fexp < Int.pred (Fopp (beta:=beta) p).Fexp := by
      simpa [Fopp, FloatSpec.Calc.Operations.Fopp] using hr_lt
    have haux :
        _root_.F2R (beta:=beta) (Fopp (beta:=beta) r) =
          _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) +
            _root_.F2R (beta:=beta) (Fopp (beta:=beta) q) :=
      plusExact2Aux_from_format_payload (beta:=beta) bo radix precision hbeta hradix hprecision
        hvNum_gt hvNum (Fopp (beta:=beta) p) (Fopp (beta:=beta) q)
        (Fopp (beta:=beta) r) hp_opp_nonneg hp_opp_can hq_opp_bound
        hclosest_opp hr_opp_lt
    rw [hr_opp_val, hp_opp_val, hq_opp_val] at haux
    linarith

/-- Exact exported Coq `plusExact2` contract. -/
theorem plusExact2 {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hprecision : 1 < precision)
    (hvNum : bo.vNum = Zpower_nat radix precision) :
    ∀ p q r : FloatSpec.Core.Defs.FlocqFloat beta,
      Fcanonic (beta:=beta) radix bo p →
      Fbounded (beta:=beta) bo q →
      Closest (beta:=beta) bo (radix : ℝ)
        (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) r →
      r.Fexp < Int.pred p.Fexp →
      _root_.F2R (beta:=beta) r =
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q := by
  have hprecision_ne : precision ≠ 0 := by omega
  have hvNum_gt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecision_ne
  letI : ValidRadix radix := ⟨hradix⟩
  exact plusExact2_from_format_payload (beta:=beta) bo radix precision
    hbeta hradix hprecision_ne hvNum_gt hvNum

noncomputable def LeExp1_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (b ph uh ul : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq `ErrFmaApprox` local lemma `LeExp1`.

In the inexact `uh` branch, if the residual `ul = ph + b - uh` is nonzero,
then `uh` cannot have exponent strictly below `pred (Fexp ph)`: otherwise
`plusExact2` would make the rounded sum exact and force `ul` to be zero. -/
theorem LeExp1_from_format_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (b ph uh ul : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        1 < bo.vNum ∧ bo.vNum = Zpower_nat radix precision ∧
        Fbounded (beta:=beta) bo b ∧
        Fnormal (beta:=beta) radix bo ph ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) ph + _root_.F2R (beta:=beta) b) uh ∧
        _root_.F2R (beta:=beta) ul =
          _root_.F2R (beta:=beta) ph + _root_.F2R (beta:=beta) b -
            _root_.F2R (beta:=beta) uh ∧
        ¬ _root_.F2R (beta:=beta) ul = 0⌝⦄
    (pure (LeExp1_check (beta:=beta) bo radix precision b ph uh ul) : Id Unit)
    ⦃⇓_ => ⌜ph.Fexp ≤ uh.Fexp + 1⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hradix, hprecision, hvNum_gt, hvNum, hbBound,
      hphNormal, huhClosest, hulDef, hCase2⟩
  simp only [wp, PostCond.noThrow, pure, LeExp1_check, Id.run, ULift.up_down]
  by_contra hle
  have huh_lt : uh.Fexp < Int.pred ph.Fexp := by
    have hsum_lt : uh.Fexp + 1 < ph.Fexp := not_le.mp hle
    simp [Int.pred]
    omega
  have huhExact :
      _root_.F2R (beta:=beta) uh =
        _root_.F2R (beta:=beta) ph + _root_.F2R (beta:=beta) b :=
    plusExact2_from_format_payload (beta:=beta) bo radix precision hbeta hradix hprecision
      hvNum_gt hvNum ph b uh (Or.inl hphNormal) hbBound
      huhClosest huh_lt
  have hulZero : _root_.F2R (beta:=beta) ul = 0 := by
    calc
      _root_.F2R (beta:=beta) ul =
          _root_.F2R (beta:=beta) ph + _root_.F2R (beta:=beta) b -
            _root_.F2R (beta:=beta) uh := hulDef
      _ = 0 := by
        rw [huhExact]
        ring
  exact hCase2 hulZero

/-- Exact exported Coq `LeExp1` contract. -/
theorem LeExp1 {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (b ph uh ul : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧
        bo.vNum = Zpower_nat radix precision ∧ 4 ≤ precision ∧
        Fbounded (beta:=beta) bo b ∧
        Fnormal (beta:=beta) radix bo ph ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) ph + _root_.F2R (beta:=beta) b) uh ∧
        _root_.F2R (beta:=beta) ul =
          _root_.F2R (beta:=beta) ph + _root_.F2R (beta:=beta) b -
            _root_.F2R (beta:=beta) uh ∧
        _root_.F2R (beta:=beta) ul ≠ 0⌝⦄
    (pure (LeExp1_check (beta:=beta) bo radix precision b ph uh ul) : Id Unit)
    ⦃⇓_ => ⌜ph.Fexp ≤ uh.Fexp + 1⌝⦄ := by
  intro ⟨hbeta, hradix, hvNum, hprecision, hb, hph, huh, hul, hul_ne⟩
  have hprecision_ne : precision ≠ 0 := by omega
  have hvNum_gt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecision_ne
  letI : ValidRadix radix := ⟨hradix⟩
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, LeExp1_check, Id.run,
    ULift.up_down, Int.cast_ofNat] using
    LeExp1_from_format_payload (beta:=beta) bo radix precision b ph uh ul
      ⟨hbeta, hradix, hprecision_ne, hvNum_gt, hvNum, hb, hph, huh,
        hul, hul_ne⟩

/-- Coq: `plusExactExp`.

The rounded sum can be split into an exponent-bounded rounded part `s` with the
same real value as `pq` and a bounded residual `r` at the minimum input
exponent. -/
theorem plusExactExp_from_format_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat) :
    beta = radix →
    1 < radix →
    precision ≠ 0 →
    1 < bo.vNum →
    bo.vNum = Zpower_nat radix precision →
    ∀ p q pq : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      Closest (beta:=beta) bo (radix : ℝ)
        (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) pq →
      ∃ r s : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo r ∧
        Fbounded (beta:=beta) bo s ∧
        _root_.F2R (beta:=beta) s = _root_.F2R (beta:=beta) pq ∧
        _root_.F2R (beta:=beta) r =
          _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
            _root_.F2R (beta:=beta) s ∧
        r.Fexp = min p.Fexp q.Fexp ∧
        r.Fexp ≤ s.Fexp ∧
        s.Fexp ≤ Int.succ (max p.Fexp q.Fexp) := by
  intro hbeta hradix hprecision hvNum_gt hvNum p q pq hp hq hclosest
  subst beta
  have hBoundExp :
      ∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=radix) radix r).Fexp := by
    intro r
    exact le_trans (neg_nonpos.mpr bo.dExp_nonneg) (by simp [boundR, boundNat])
  let P : ℝ → FloatSpec.Core.Defs.FlocqFloat radix → Prop :=
    Closest (beta:=radix) bo (radix : ℝ)
  have hMinTotal : TotalP (isMin (beta:=radix) bo radix) := by
    intro r
    have h := MinEx_from_finite_box_payload (beta:=radix) bo radix r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hvNum_gt, hBoundExp r⟩
  have hMaxTotal : TotalP (isMax (beta:=radix) bo radix) := by
    intro r
    have h := MaxEx_from_finite_box_payload (beta:=radix) bo radix r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MaxEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hvNum_gt, hBoundExp r⟩
  have hTotal : TotalP P := by
    intro r
    have h := ClosestTotal_from_extrema_payload (beta:=radix) bo radix (radix : ℝ) r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestTotal_check, Id.run,
      ULift.up_down, P, Int.cast_ofNat] using h ⟨hMinTotal, hMaxTotal⟩
  have hCompat : CompatiblePGeneric P := by
    have h := ClosestCompatible_from_structural_payload (beta:=radix) bo (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestCompatible_check,
      PredTrans.pure, PredTrans.apply, Id.run, ULift.down, P, Int.cast_ofNat] using h trivial
  have hMinMax : MinOrMaxP_float (beta:=radix) bo radix P := by
    have h := ClosestMinOrMax_from_split_radix_payload (beta:=radix) bo radix (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMinOrMax_check, MinOrMaxP_float,
      Id.run, ULift.up_down, P, Int.cast_ofNat] using h trivial
  have hMono : MonotoneP_float P := by
    have h := ClosestMonotone_from_split_radix_payload (beta:=radix) bo (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMonotone_check,
      Id.run, ULift.up_down, P, Int.cast_ofNat] using h trivial
  have hRoundedFloat : RoundedModeP_float (beta:=radix) bo radix P :=
    ⟨hTotal, hCompat, hMinMax, hMono⟩
  have hRep :
      ∀ a r : FloatSpec.Core.Defs.FlocqFloat radix,
        P (_root_.F2R (beta:=radix) a) r →
          ∃ m : Int,
            _root_.F2R (beta:=radix) r =
              _root_.F2R (beta:=radix)
                (⟨m, a.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat radix) := by
    have h := ClosestRoundedModeRep (beta:=radix) bo radix precision
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestRoundedModeRep_check,
      Id.run, ULift.up_down, P, Int.cast_ofNat] using
      h ⟨rfl, hradix, hprecision, hvNum_gt, hvNum, hBoundExp⟩
  have hP :
      P (_root_.F2R (beta:=radix) p + _root_.F2R (beta:=radix) q) pq := hclosest
  let upperExp : Int := Int.succ (max p.Fexp q.Fexp)
  have hmin_le_upper : min p.Fexp q.Fexp ≤ upperExp := by
    have hmin_le_max : min p.Fexp q.Fexp ≤ max p.Fexp q.Fexp :=
      le_trans (min_le_left p.Fexp q.Fexp) (le_max_left p.Fexp q.Fexp)
    have hmax_le_succ : max p.Fexp q.Fexp ≤ upperExp := by
      simp [upperExp, Int.succ]
    exact le_trans hmin_le_max hmax_le_succ
  have hLower :
      ∃ s : FloatSpec.Core.Defs.FlocqFloat radix,
        Fbounded (beta:=radix) bo s ∧
        _root_.F2R (beta:=radix) s = _root_.F2R (beta:=radix) pq ∧
        min p.Fexp q.Fexp ≤ s.Fexp := by
    have h := plusExpMin_from_rep_payload (beta:=radix) bo radix P p q pq
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, plusExpMin_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hMinMax, hRep, hP⟩
  have hUpper :
      ∃ s : FloatSpec.Core.Defs.FlocqFloat radix,
        Fbounded (beta:=radix) bo s ∧
        _root_.F2R (beta:=radix) s = _root_.F2R (beta:=radix) pq ∧
        s.Fexp ≤ upperExp := by
    let z : Int := max p.Fexp q.Fexp
    let upper : FloatSpec.Core.Defs.FlocqFloat radix := ⟨bo.vNum - 1, Int.succ z⟩
    have hpExpLe : p.Fexp ≤ z := le_max_left p.Fexp q.Fexp
    have hqExpLe : q.Fexp ≤ z := le_max_right p.Fexp q.Fexp
    have hsucc_z : z ≤ Int.succ z := by
      simp [Int.succ]
    have hupperBounded : Fbounded (beta:=radix) bo upper := by
      constructor
      · have hnonneg : 0 ≤ bo.vNum - 1 := by omega
        rw [abs_of_nonneg hnonneg]
        omega
      · have hp_low : -bo.dExp ≤ z := le_trans hp.2 hpExpLe
        exact le_trans hp_low hsucc_z
    have hpAbsLeBase :
        |_root_.F2R (beta:=radix) p| ≤
          _root_.F2R (beta:=radix)
            (⟨bo.vNum - 1, z⟩ : FloatSpec.Core.Defs.FlocqFloat radix) := by
      have h := maxMax1_from_expanded_bound_payload (beta:=radix) bo p z
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, maxMax1_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using h ⟨hp, hpExpLe, hp, hradix⟩
    have hqAbsLeBase :
        |_root_.F2R (beta:=radix) q| ≤
          _root_.F2R (beta:=radix)
            (⟨bo.vNum - 1, z⟩ : FloatSpec.Core.Defs.FlocqFloat radix) := by
      have h := maxMax1_from_expanded_bound_payload (beta:=radix) bo q z
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, maxMax1_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using h ⟨hq, hqExpLe, hq, hradix⟩
    have hbase_nonneg :
        0 ≤ _root_.F2R (beta:=radix)
          (⟨bo.vNum - 1, z⟩ : FloatSpec.Core.Defs.FlocqFloat radix) := by
      have hnum : (0 : Int) ≤ bo.vNum - 1 := by omega
      have hnumR : (0 : ℝ) ≤ ((bo.vNum - 1 : Int) : ℝ) := by exact_mod_cast hnum
      have hradixR_pos : (0 : ℝ) < (radix : ℝ) := by
        exact_mod_cast (by omega : (0 : Int) < radix)
      have hpow_nonneg : 0 ≤ (radix : ℝ) ^ z :=
        le_of_lt (zpow_pos hradixR_pos z)
      simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
        FloatSpec.Core.Defs.FlocqFloat.Fnum,
        FloatSpec.Core.Defs.FlocqFloat.Fexp]
      exact mul_nonneg hnumR hpow_nonneg
    have hupper_val :
        _root_.F2R (beta:=radix) upper =
          (radix : ℝ) *
            _root_.F2R (beta:=radix)
              (⟨bo.vNum - 1, z⟩ : FloatSpec.Core.Defs.FlocqFloat radix) := by
      have hradix_ne : (radix : ℝ) ≠ 0 := by
        exact_mod_cast (ne_of_gt (by omega : (0 : Int) < radix))
      simp only [upper, _root_.F2R, FloatSpec.Core.Defs.F2R,
        FloatSpec.Core.Defs.FlocqFloat.Fnum,
        FloatSpec.Core.Defs.FlocqFloat.Fexp]
      rw [show Int.succ z = z + 1 by simp [Int.succ]]
      rw [zpow_add₀ hradix_ne]
      simp
      ring
    have hsumAbsLeUpper :
        |_root_.F2R (beta:=radix) p + _root_.F2R (beta:=radix) q| ≤
          _root_.F2R (beta:=radix) upper := by
      have htri :
          |_root_.F2R (beta:=radix) p + _root_.F2R (beta:=radix) q| ≤
            |_root_.F2R (beta:=radix) p| + |_root_.F2R (beta:=radix) q| :=
        abs_add_le _ _
      have htwo :
          |_root_.F2R (beta:=radix) p| + |_root_.F2R (beta:=radix) q| ≤
            (2 : ℝ) *
              _root_.F2R (beta:=radix)
                (⟨bo.vNum - 1, z⟩ : FloatSpec.Core.Defs.FlocqFloat radix) := by
        linarith [hpAbsLeBase, hqAbsLeBase]
      have htwo_le_radix :
          (2 : ℝ) *
              _root_.F2R (beta:=radix)
                (⟨bo.vNum - 1, z⟩ : FloatSpec.Core.Defs.FlocqFloat radix) ≤
            (radix : ℝ) *
              _root_.F2R (beta:=radix)
                (⟨bo.vNum - 1, z⟩ : FloatSpec.Core.Defs.FlocqFloat radix) := by
        have hradix2 : (2 : ℝ) ≤ (radix : ℝ) := by exact_mod_cast hradix
        exact mul_le_mul_of_nonneg_right hradix2 hbase_nonneg
      calc
        |_root_.F2R (beta:=radix) p + _root_.F2R (beta:=radix) q|
            ≤ |_root_.F2R (beta:=radix) p| + |_root_.F2R (beta:=radix) q| := htri
        _ ≤ (2 : ℝ) *
              _root_.F2R (beta:=radix)
                (⟨bo.vNum - 1, z⟩ : FloatSpec.Core.Defs.FlocqFloat radix) := htwo
        _ ≤ (radix : ℝ) *
              _root_.F2R (beta:=radix)
                (⟨bo.vNum - 1, z⟩ : FloatSpec.Core.Defs.FlocqFloat radix) := htwo_le_radix
        _ = _root_.F2R (beta:=radix) upper := hupper_val.symm
    have hpqAbsLeUpper :
        |_root_.F2R (beta:=radix) pq| ≤ _root_.F2R (beta:=radix) upper := by
      have h := RoundAbsMonotoner_from_rounded_payload (beta:=radix) bo radix P
        (_root_.F2R (beta:=radix) p + _root_.F2R (beta:=radix) q) pq upper
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RoundAbsMonotoner_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using h ⟨hRoundedFloat, hupperBounded, hP, hsumAbsLeUpper⟩
    have hmax := eqExpMax (beta:=radix) bo radix pq upper
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, eqExpMax_check, Id.run,
      ULift.up_down, upper, z, upperExp, Int.cast_ofNat] using
      hmax ⟨rfl, hradix, hclosest.1, hupperBounded, hpqAbsLeUpper⟩
  have hsExp :
      ∃ s : FloatSpec.Core.Defs.FlocqFloat radix,
        Fbounded (beta:=radix) bo s ∧
        _root_.F2R (beta:=radix) s = _root_.F2R (beta:=radix) pq ∧
        min p.Fexp q.Fexp ≤ s.Fexp ∧
        s.Fexp ≤ upperExp := by
    rcases hLower with ⟨rLower, hrLowerBounded, hrLowerVal, hrLowerExpMin⟩
    by_cases hLowerUpper : rLower.Fexp ≤ upperExp
    · exact ⟨rLower, hrLowerBounded, hrLowerVal, hrLowerExpMin, hLowerUpper⟩
    · rcases hUpper with ⟨rUpper, hrUpperBounded, hrUpperVal, hrUpperExpLe⟩
      have hUpperLtLower : upperExp < rLower.Fexp := lt_of_not_ge hLowerUpper
      have hrUpperExpLeLower : rUpper.Fexp ≤ rLower.Fexp :=
        le_trans hrUpperExpLe (le_of_lt hUpperLtLower)
      let nUpper : Nat := Int.natAbs (rLower.Fexp - rUpper.Fexp)
      let nTarget : Nat := Int.natAbs (rLower.Fexp - upperExp)
      have hnUpper :
          ((nUpper : Nat) : Int) = rLower.Fexp - rUpper.Fexp := by
        simpa [nUpper] using
          Int.natAbs_of_nonneg (by omega : 0 ≤ rLower.Fexp - rUpper.Fexp)
      have hnTarget :
          ((nTarget : Nat) : Int) = rLower.Fexp - upperExp := by
        simpa [nTarget] using
          Int.natAbs_of_nonneg (by omega : 0 ≤ rLower.Fexp - upperExp)
      have hnTargetLeUpper : nTarget ≤ nUpper := by
        have hleInt : ((nTarget : Nat) : Int) ≤ ((nUpper : Nat) : Int) := by
          rw [hnTarget, hnUpper]
          omega
        exact_mod_cast hleInt
      let shiftedUpper : FloatSpec.Core.Defs.FlocqFloat radix :=
        Fshift (beta:=radix) radix nUpper rLower
      have hshiftedUpperVal :
          _root_.F2R (beta:=radix) shiftedUpper =
            _root_.F2R (beta:=radix) rUpper := by
        calc
          _root_.F2R (beta:=radix) shiftedUpper =
              _root_.F2R (beta:=radix) rLower := by
            simpa [shiftedUpper] using
              Fshift_value (beta:=radix) radix nUpper rLower rfl hradix
          _ = _root_.F2R (beta:=radix) pq := hrLowerVal
          _ = _root_.F2R (beta:=radix) rUpper := hrUpperVal.symm
      have hshiftedUpperExp : shiftedUpper.Fexp = rUpper.Fexp := by
        simp only [shiftedUpper, Fshift, FloatSpec.Core.Defs.FlocqFloat.Fexp]
        rw [hnUpper]
        omega
      have hshiftedUpperBounded :
          Fbounded (beta:=radix) bo shiftedUpper := by
        constructor
        · have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
            exact_mod_cast (by omega : (0 : Int) < radix)
          have hpow_pos : 0 < (radix : ℝ) ^ rUpper.Fexp :=
            zpow_pos hradix_pos rUpper.Fexp
          have hnumReal :
              ((shiftedUpper.Fnum : Int) : ℝ) = (rUpper.Fnum : ℝ) := by
            have hval := hshiftedUpperVal
            simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
              hshiftedUpperExp] at hval
            exact (mul_right_inj' (ne_of_gt hpow_pos)).mp (by
              simpa [mul_comm] using hval)
          have hnumEq : shiftedUpper.Fnum = rUpper.Fnum := by
            exact_mod_cast hnumReal
          simpa [hnumEq] using hrUpperBounded.1
        · simpa [hshiftedUpperExp] using hrUpperBounded.2
      let s : FloatSpec.Core.Defs.FlocqFloat radix :=
        Fshift (beta:=radix) radix nTarget rLower
      have hsBounded : Fbounded (beta:=radix) bo s := by
        have h := FboundedShiftLess (beta:=radix) bo radix rLower nUpper nTarget
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FboundedShiftLess_check, Id.run,
          ULift.up_down, shiftedUpper, s, Int.cast_ofNat] using
          h ⟨hradix, hnTargetLeUpper, hshiftedUpperBounded⟩
      have hsVal : _root_.F2R (beta:=radix) s = _root_.F2R (beta:=radix) pq := by
        calc
          _root_.F2R (beta:=radix) s =
              _root_.F2R (beta:=radix) rLower := by
            simpa [s] using Fshift_value (beta:=radix) radix nTarget rLower rfl hradix
          _ = _root_.F2R (beta:=radix) pq := hrLowerVal
      have hsExp : s.Fexp = upperExp := by
        simp only [s, Fshift, FloatSpec.Core.Defs.FlocqFloat.Fexp]
        rw [hnTarget]
        omega
      refine ⟨s, hsBounded, hsVal, ?_, ?_⟩
      · simpa [hsExp] using hmin_le_upper
      · simpa [hsExp, upperExp]
  rcases hsExp with ⟨s, hsBounded, hsVal, hsMin, hsUpper⟩
  by_cases hzero :
      _root_.F2R (beta:=radix) p + _root_.F2R (beta:=radix) q -
        _root_.F2R (beta:=radix) pq = 0
  · let r : FloatSpec.Core.Defs.FlocqFloat radix := Fzero radix (min p.Fexp q.Fexp)
    have hrBounded : Fbounded (beta:=radix) bo r := by
      by_cases hpq_exp : p.Fexp ≤ q.Fexp
      · have h := FboundedZeroSameExp (beta:=radix) bo p
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FboundedZeroSameExp_check,
          Id.run, ULift.up_down, r, min_eq_left hpq_exp, Int.cast_ofNat] using h hp
      · have hqp_exp : q.Fexp ≤ p.Fexp := le_of_lt (lt_of_not_ge hpq_exp)
        have h := FboundedZeroSameExp (beta:=radix) bo q
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FboundedZeroSameExp_check,
          Id.run, ULift.up_down, r, min_eq_right hqp_exp, Int.cast_ofNat] using h hq
    have hrVal0 : _root_.F2R (beta:=radix) r = 0 := by
      have h := FzeroisReallyZero (beta:=radix) (min p.Fexp q.Fexp)
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FzeroisReallyZero_check,
        Id.run, ULift.up_down, r, Int.cast_ofNat] using h True.intro
    refine ⟨r, s, hrBounded, hsBounded, hsVal, ?_, rfl, ?_, hsUpper⟩
    · rw [hrVal0, hsVal]
      exact hzero.symm
    · simpa [r, Fzero] using hsMin
  · rcases errorBoundedPlus_from_format_payload (beta:=radix) bo radix precision rfl hradix
        hprecision hvNum_gt hvNum p q pq hp hq hclosest with
      ⟨error, herrorVal, herrorBounded, herrorExp⟩
    refine ⟨error, s, herrorBounded, hsBounded, hsVal, ?_, herrorExp, ?_, hsUpper⟩
    · simpa [hsVal] using herrorVal
    · simpa [herrorExp] using hsMin

/-- Exact exported Coq `plusExactExp` contract. -/
theorem plusExactExp {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hprecision : 1 < precision)
    (hvNum : bo.vNum = Zpower_nat radix precision) :
    ∀ p q pq : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      Closest (beta:=beta) bo (radix : ℝ)
        (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) pq →
      ∃ r s : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo r ∧
        Fbounded (beta:=beta) bo s ∧
        _root_.F2R (beta:=beta) s = _root_.F2R (beta:=beta) pq ∧
        _root_.F2R (beta:=beta) r =
          _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
            _root_.F2R (beta:=beta) s ∧
        r.Fexp = min p.Fexp q.Fexp ∧
        r.Fexp ≤ s.Fexp ∧
        s.Fexp ≤ Int.succ (max p.Fexp q.Fexp) := by
  have hprecision_ne : precision ≠ 0 := by omega
  have hvNum_gt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecision_ne
  letI : ValidRadix radix := ⟨hradix⟩
  exact plusExactExp_from_format_payload (beta:=beta) bo radix precision
    hbeta hradix hprecision_ne hvNum_gt hvNum

/-- Coq: `AddExpGeUnderf`.  The `plusExactExp` witness is constructed here,
as in FLoCq, rather than required from the caller. -/
theorem AddExpGeUnderf {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (precision : Nat)
    (f1 f2 g : FloatSpec.Core.Defs.FlocqFloat beta) (e : Int) :
    ⦃⌜beta = 2 ∧ 1 < precision ∧
        bo.vNum = Zpower_nat 2 precision ∧
        Closest (beta:=beta) bo (2 : ℝ)
          (_root_.F2R (beta:=beta) f1 + _root_.F2R (beta:=beta) f2) g ∧
        Fbounded (beta:=beta) bo f1 ∧ Fbounded (beta:=beta) bo f2 ∧
        (2 : ℝ) ^ e ≤ |_root_.F2R (beta:=beta) f1| ∧
        (2 : ℝ) ^ e ≤ |_root_.F2R (beta:=beta) f2|⌝⦄
    (pure (AddExpGeUnderf_check (beta:=beta) bo (precision : Int) e
      (2 : ℝ) f1 f2 g) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) g = 0 ∨
      (2 : ℝ) ^ (e - (precision : Int) + 1) ≤
        |_root_.F2R (beta:=beta) g|⌝⦄ := by
  intro ⟨hbeta, hprecision, hvnum, hclosest, hf1, hf2, hmag1, hmag2⟩
  subst beta
  have hprecision_ne : precision ≠ 0 := by omega
  have hvnum_gt : 1 < bo.vNum := by
    rw [hvnum, Zpower_nat]
    exact one_lt_pow₀ (by norm_num : (1 : Int) < 2) hprecision_ne
  have hBoundExp :
      ∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=2) 2 r).Fexp := by
    intro r
    exact le_trans (neg_nonpos.mpr bo.dExp_nonneg) (by
      simp [boundR, boundNat])
  have hPlusExact :
      _root_.F2R (beta:=2) g ≠ 0 →
        ∃ g' : FloatSpec.Core.Defs.FlocqFloat 2,
          Fbounded' bo g' ∧
          _root_.F2R (beta:=2) g' = _root_.F2R (beta:=2) g ∧
          min f1.Fexp f2.Fexp ≤ g'.Fexp := by
    intro _
    rcases plusExactExp_from_format_payload (beta:=2) bo 2 precision rfl (by norm_num)
        hprecision_ne hvnum_gt hvnum f1 f2 g hf1 hf2 hclosest with
      ⟨_residual, rounded, _hResidual, hRounded, hRoundedValue,
        _hResidualValue, hResidualExp, hResidualLeRounded, _hRoundedUpper⟩
    exact ⟨rounded, hRounded, hRoundedValue,
      by simpa [hResidualExp] using hResidualLeRounded⟩
  have hvnumReal : (bo.vNum : ℝ) = (2 : ℝ) ^ (precision : Int) := by
    rw [hvnum, Zpower_nat, Int.cast_pow, ← zpow_natCast]
    norm_num
  have h := AddExpGeUnderf_from_plusExact_payload (beta:=2) bo
    (precision : Int) e (2 : ℝ) f1 f2 g
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, AddExpGeUnderf_check,
    Id.run, ULift.up_down, Int.cast_ofNat] using
    h ⟨hclosest, hf1, hf2, hmag1, hmag2, by norm_num, by omega,
      hvnumReal, hf1, hf2, hPlusExact⟩

/-- Coq: `AddExpGeUnderf2`, the nonzero projection of
`AddExpGeUnderf`. -/
theorem AddExpGeUnderf2 {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (precision : Nat)
    (f1 f2 g : FloatSpec.Core.Defs.FlocqFloat beta) (e : Int) :
    ⦃⌜beta = 2 ∧ 1 < precision ∧
        bo.vNum = Zpower_nat 2 precision ∧
        Closest (beta:=beta) bo (2 : ℝ)
          (_root_.F2R (beta:=beta) f1 + _root_.F2R (beta:=beta) f2) g ∧
        Fbounded (beta:=beta) bo f1 ∧ Fbounded (beta:=beta) bo f2 ∧
        (2 : ℝ) ^ e ≤ |_root_.F2R (beta:=beta) f1| ∧
        (2 : ℝ) ^ e ≤ |_root_.F2R (beta:=beta) f2| ∧
        _root_.F2R (beta:=beta) g ≠ 0⌝⦄
    (pure (AddExpGeUnderf2_check (beta:=beta) bo (precision : Int) e
      (2 : ℝ) f1 f2 g) : Id Unit)
    ⦃⇓_ => ⌜(2 : ℝ) ^ (e - (precision : Int) + 1) ≤
      |_root_.F2R (beta:=beta) g|⌝⦄ := by
  intro ⟨hbeta, hprecision, hvnum, hclosest, hf1, hf2, hmag1, hmag2,
    hgNonzero⟩
  simp only [wp, PostCond.noThrow, pure, AddExpGeUnderf2_check,
    Id.run, ULift.up_down]
  have h := AddExpGeUnderf (beta:=beta) bo precision f1 f2 g e
  have hresult : _root_.F2R (beta:=beta) g = 0 ∨
      (2 : ℝ) ^ (e - (precision : Int) + 1) ≤
        |_root_.F2R (beta:=beta) g| := by
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, AddExpGeUnderf_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hbeta, hprecision, hvnum, hclosest, hf1, hf2, hmag1, hmag2⟩
  exact hresult.resolve_left hgNonzero

/-- Coq: `AddExpGe1Underf`.

If one canonical addend is at least `radix^e` in magnitude and `e - 1` is not
below the underflow exponent, a closest rounded sum is either zero or has
magnitude at least `radix^(e - precision)`. -/
theorem AddExpGe1Underf_from_generic_radix_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat) :
    beta = radix →
    1 < radix →
    precision ≠ 0 →
    1 < bo.vNum →
    bo.vNum = Zpower_nat radix precision →
    ∀ f1 f2 g : FloatSpec.Core.Defs.FlocqFloat beta, ∀ e : Int,
      Closest (beta:=beta) bo (radix : ℝ)
        (_root_.F2R (beta:=beta) f1 + _root_.F2R (beta:=beta) f2) g →
      Fcanonic (beta:=beta) radix bo f1 →
      Fcanonic (beta:=beta) radix bo f2 →
      (radix : ℝ) ^ e ≤ |_root_.F2R (beta:=beta) f1| →
      -bo.dExp ≤ e - 1 →
      _root_.F2R (beta:=beta) g = 0 ∨
        (radix : ℝ) ^ (e - (precision : Int)) ≤ |_root_.F2R (beta:=beta) g| := by
  intro hbeta hradix hprecision hvNum_gt hvNum f1 f2 g e
    hclosest hf1Can hf2Can hmag1 heUnder
  subst beta
  have hBoundExp :
      ∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=radix) radix r).Fexp := by
    intro r
    exact le_trans (neg_nonpos.mpr bo.dExp_nonneg) (by simp [boundR, boundNat])
  have hf1Bound : Fbounded (beta:=radix) bo f1 := by
    have h := FcanonicBound (beta:=radix) radix bo f1
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicBound_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hf1Can
  have hf2Bound : Fbounded (beta:=radix) bo f2 := by
    have h := FcanonicBound (beta:=radix) radix bo f2
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicBound_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hf2Can
  have hprecision_pos : 0 < precision := Nat.pos_iff_ne_zero.mpr hprecision
  have hprecision_int : (1 : Int) ≤ (precision : Int) := by
    exact_mod_cast hprecision_pos
  have hradixR : (1 : ℝ) < (radix : ℝ) := by exact_mod_cast hradix
  have hradixR_pos : (0 : ℝ) < (radix : ℝ) := by linarith
  have hradixR_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradixR_pos
  have hradixR_ge : (1 : ℝ) ≤ (radix : ℝ) := le_of_lt hradixR
  have hvNumReal : (bo.vNum : ℝ) = (radix : ℝ) ^ (precision : Int) := by
    rw [hvNum, Zpower_nat, Int.cast_pow, ← zpow_natCast]
  have hMinTotal : TotalP (isMin (beta:=radix) bo radix) := by
    intro r
    have h := MinEx_from_finite_box_payload (beta:=radix) bo radix r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hvNum_gt, hBoundExp r⟩
  have hMaxTotal : TotalP (isMax (beta:=radix) bo radix) := by
    intro r
    have h := MaxEx_from_finite_box_payload (beta:=radix) bo radix r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MaxEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hvNum_gt, hBoundExp r⟩
  have hTotal : TotalP (Closest (beta:=radix) bo (radix : ℝ)) := by
    intro r
    have h := ClosestTotal_from_extrema_payload (beta:=radix) bo radix (radix : ℝ) r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestTotal_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨hMinTotal, hMaxTotal⟩
  have hCompat : CompatiblePGeneric (Closest (beta:=radix) bo (radix : ℝ)) := by
    have h := ClosestCompatible_from_structural_payload (beta:=radix) bo (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestCompatible_check,
      PredTrans.pure, PredTrans.apply, Id.run, ULift.down, Int.cast_ofNat] using h trivial
  have hMinMax :
      MinOrMaxP_float (beta:=radix) bo radix
        (Closest (beta:=radix) bo (radix : ℝ)) := by
    have h := ClosestMinOrMax_from_split_radix_payload (beta:=radix) bo radix (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMinOrMax_check, MinOrMaxP_float,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hMono : MonotoneP_float (Closest (beta:=radix) bo (radix : ℝ)) := by
    have h := ClosestMonotone_from_split_radix_payload (beta:=radix) bo (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMonotone_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hRoundedFloat :
      RoundedModeP_float (beta:=radix) bo radix
        (Closest (beta:=radix) bo (radix : ℝ)) :=
    ⟨hTotal, hCompat, hMinMax, hMono⟩
  have hPlusExact :
      _root_.F2R (beta:=radix) g ≠ 0 →
        ∃ g' : FloatSpec.Core.Defs.FlocqFloat radix,
          Fbounded' bo g' ∧
          _root_.F2R (beta:=radix) g' = _root_.F2R (beta:=radix) g ∧
          min f1.Fexp f2.Fexp ≤ g'.Fexp := by
    intro _
    rcases plusExactExp_from_format_payload (beta:=radix) bo radix precision rfl hradix hprecision
        hvNum_gt hvNum f1 f2 g hf1Bound hf2Bound hclosest with
      ⟨_r, s, _hrBound, hsBound, hsVal, _hrVal, hrExp, hrLeS, _hsUpper⟩
    exact ⟨s, hsBound, hsVal, by simpa [hrExp] using hrLeS⟩
  have runAdd :
      ∀ e0 : Int,
        (radix : ℝ) ^ e0 ≤ |_root_.F2R (beta:=radix) f1| →
        (radix : ℝ) ^ e0 ≤ |_root_.F2R (beta:=radix) f2| →
        _root_.F2R (beta:=radix) g = 0 ∨
          (radix : ℝ) ^ (e0 - (precision : Int) + 1) ≤
            |_root_.F2R (beta:=radix) g| := by
    intro e0 h1 h2
    have h := AddExpGeUnderf_from_plusExact_payload
      (beta:=radix) bo (precision : Int) e0
      (radix : ℝ) f1 f2 g
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, AddExpGeUnderf_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hclosest, hf1Bound, hf2Bound, h1, h2, hradix, hprecision_int,
        hvNumReal, hf1Bound, hf2Bound, hPlusExact⟩
  by_cases hgZero : _root_.F2R (beta:=radix) g = 0
  · exact Or.inl hgZero
  right
  by_cases hle12 :
      |_root_.F2R (beta:=radix) f1| ≤ |_root_.F2R (beta:=radix) f2|
  · have hmag2 : (radix : ℝ) ^ e ≤ |_root_.F2R (beta:=radix) f2| :=
      le_trans hmag1 hle12
    have hres := runAdd e hmag1 hmag2
    have hstrong :
        (radix : ℝ) ^ (e - (precision : Int) + 1) ≤
          |_root_.F2R (beta:=radix) g| :=
      hres.resolve_left hgZero
    have hpow_le :
        (radix : ℝ) ^ (e - (precision : Int)) ≤
          (radix : ℝ) ^ (e - (precision : Int) + 1) :=
      zpow_right_mono₀ hradixR_ge (by omega)
    exact le_trans hpow_le hstrong
  · have hlt21 : |_root_.F2R (beta:=radix) f2| < |_root_.F2R (beta:=radix) f1| :=
      lt_of_not_ge hle12
    have hpow_e_sub_one_le_half :
        (radix : ℝ) ^ (e - 1) ≤ |_root_.F2R (beta:=radix) f1| / 2 := by
      have hpow_e :
          (radix : ℝ) ^ e =
            (radix : ℝ) ^ (e - 1) * (radix : ℝ) := by
        calc
          (radix : ℝ) ^ e = (radix : ℝ) ^ ((e - 1) + 1) := by
            congr 1
            omega
          _ = (radix : ℝ) ^ (e - 1) * (radix : ℝ) := by
            rw [zpow_add₀ hradixR_ne, zpow_one]
      have htwo_le :
          2 * (radix : ℝ) ^ (e - 1) ≤ (radix : ℝ) ^ e := by
        rw [hpow_e]
        have hradix_two : (2 : ℝ) ≤ (radix : ℝ) := by
          exact_mod_cast (by omega : (2 : Int) ≤ radix)
        calc
          2 * (radix : ℝ) ^ (e - 1) =
              (radix : ℝ) ^ (e - 1) * 2 := by ring
          _ ≤ (radix : ℝ) ^ (e - 1) * (radix : ℝ) :=
              mul_le_mul_of_nonneg_left hradix_two
                (le_of_lt (zpow_pos hradixR_pos (e - 1)))
      nlinarith [hmag1, htwo_le]
    have hmag1_e_sub_one :
        (radix : ℝ) ^ (e - 1) ≤ |_root_.F2R (beta:=radix) f1| := by
      have hpow_le : (radix : ℝ) ^ (e - 1) ≤ (radix : ℝ) ^ e :=
        zpow_right_mono₀ hradixR_ge (by omega)
      exact le_trans hpow_le hmag1
    by_cases hsmall :
        |_root_.F2R (beta:=radix) f2| ≤ |_root_.F2R (beta:=radix) f1| / 2
    · let a : FloatSpec.Core.Defs.FlocqFloat radix := ⟨1, e - 1⟩
      have haBound : Fbounded (beta:=radix) bo a := by
        constructor
        · dsimp [a]
          simpa using hvNum_gt
        · dsimp [a]
          exact heUnder
      have haVal :
          _root_.F2R (beta:=radix) a = (radix : ℝ) ^ (e - 1) := by
        simp [a, _root_.F2R, FloatSpec.Core.Defs.F2R]
      have htri :
          |_root_.F2R (beta:=radix) f1| - |_root_.F2R (beta:=radix) f2| ≤
            |_root_.F2R (beta:=radix) f1 + _root_.F2R (beta:=radix) f2| := by
        have h := abs_sub_abs_le_abs_sub (_root_.F2R (beta:=radix) f1)
          (-_root_.F2R (beta:=radix) f2)
        rw [abs_neg, sub_neg_eq_add] at h
        exact h
      have hsumLower :
          (radix : ℝ) ^ (e - 1) ≤
            |_root_.F2R (beta:=radix) f1 + _root_.F2R (beta:=radix) f2| := by
        have hhalf_le_diff :
            |_root_.F2R (beta:=radix) f1| / 2 ≤
              |_root_.F2R (beta:=radix) f1| - |_root_.F2R (beta:=radix) f2| := by
          nlinarith
        exact le_trans hpow_e_sub_one_le_half (le_trans hhalf_le_diff htri)
      have hround := RoundAbsMonotonel_from_rounded_payload (beta:=radix) bo radix
        (Closest (beta:=radix) bo (radix : ℝ))
        (_root_.F2R (beta:=radix) f1 + _root_.F2R (beta:=radix) f2) g a
      have hround_le :
          _root_.F2R (beta:=radix) a ≤ |_root_.F2R (beta:=radix) g| := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RoundAbsMonotonel_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          hround ⟨hRoundedFloat, haBound, hclosest, by simpa [haVal] using hsumLower⟩
      have hpow_le :
          (radix : ℝ) ^ (e - (precision : Int)) ≤ (radix : ℝ) ^ (e - 1) :=
        zpow_right_mono₀ hradixR_ge (by omega)
      exact le_trans hpow_le (by simpa [haVal] using hround_le)
    · have hlarge2 :
          |_root_.F2R (beta:=radix) f1| / 2 < |_root_.F2R (beta:=radix) f2| :=
        lt_of_not_ge hsmall
      have hmag2_e_sub_one :
          (radix : ℝ) ^ (e - 1) ≤ |_root_.F2R (beta:=radix) f2| :=
        le_trans hpow_e_sub_one_le_half (le_of_lt hlarge2)
      have hres := runAdd (e - 1) hmag1_e_sub_one hmag2_e_sub_one
      have hstrong :
          (radix : ℝ) ^ ((e - 1) - (precision : Int) + 1) ≤
            |_root_.F2R (beta:=radix) g| :=
        hres.resolve_left hgZero
      simpa [sub_eq_add_neg, add_comm, add_left_comm, add_assoc] using hstrong

/-- Exact exported Coq `AddExpGe1Underf` binary contract. -/
theorem AddExpGe1Underf
    (bo : Fbound_skel) (precision : Nat)
    (hprecision : 1 < precision)
    (hvNum : bo.vNum = Zpower_nat 2 precision)
    (_hprecision4 : 4 ≤ precision) :
    ∀ f1 f2 g : FloatSpec.Core.Defs.FlocqFloat 2, ∀ e : Int,
      Closest (beta:=2) bo (2 : ℝ)
        (_root_.F2R (beta:=2) f1 + _root_.F2R (beta:=2) f2) g →
      Fcanonic (beta:=2) 2 bo f1 →
      Fcanonic (beta:=2) 2 bo f2 →
      (2 : ℝ) ^ e ≤ |_root_.F2R (beta:=2) f1| →
      -bo.dExp ≤ e - 1 →
      _root_.F2R (beta:=2) g = 0 ∨
        (2 : ℝ) ^ (e - (precision : Int)) ≤
          |_root_.F2R (beta:=2) g| := by
  have hprecision_ne : precision ≠ 0 := by omega
  have hvNum_gt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ (by decide) hprecision_ne
  exact AddExpGe1Underf_from_generic_radix_payload (beta:=2)
    bo 2 precision rfl (by decide) hprecision_ne hvNum_gt hvNum

/-- Coq: `AddExpGe1Underf2`.

The bounded-input form of `AddExpGe1Underf`: normalize both inputs to obtain
canonical representatives, then eliminate the zero branch using the explicit
nonzero rounded result hypothesis. -/
theorem AddExpGe1Underf2_from_generic_radix_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat) :
    beta = radix →
    1 < radix →
    precision ≠ 0 →
    1 < bo.vNum →
    bo.vNum = Zpower_nat radix precision →
    ∀ f1 f2 g : FloatSpec.Core.Defs.FlocqFloat beta, ∀ e : Int,
      Closest (beta:=beta) bo (radix : ℝ)
        (_root_.F2R (beta:=beta) f1 + _root_.F2R (beta:=beta) f2) g →
      Fbounded (beta:=beta) bo f1 →
      Fbounded (beta:=beta) bo f2 →
      (radix : ℝ) ^ e ≤ |_root_.F2R (beta:=beta) f1| →
      -bo.dExp ≤ e - 1 →
      _root_.F2R (beta:=beta) g ≠ 0 →
      (radix : ℝ) ^ (e - (precision : Int)) ≤
        |_root_.F2R (beta:=beta) g| := by
  intro hbeta hradix hprecision hvNum_gt hvNum f1 f2 g e
    hclosest hf1Bound hf2Bound hmag1 heUnder hgNonzero
  subst beta
  let nf1 : FloatSpec.Core.Defs.FlocqFloat radix :=
    Fnormalize (beta:=radix) radix bo precision f1
  let nf2 : FloatSpec.Core.Defs.FlocqFloat radix :=
    Fnormalize (beta:=radix) radix bo precision f2
  have hnf1Correct :
      _root_.F2R (beta:=radix) nf1 = _root_.F2R (beta:=radix) f1 := by
    have h := FnormalizeCorrect (beta:=radix) radix bo precision f1
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
      Id.run, ULift.up_down, nf1, Int.cast_ofNat] using h ⟨rfl, hradix⟩
  have hnf2Correct :
      _root_.F2R (beta:=radix) nf2 = _root_.F2R (beta:=radix) f2 := by
    have h := FnormalizeCorrect (beta:=radix) radix bo precision f2
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
      Id.run, ULift.up_down, nf2, Int.cast_ofNat] using h ⟨rfl, hradix⟩
  have hnf1Canonic : Fcanonic (beta:=radix) radix bo nf1 := by
    have h := FnormalizeCanonic (beta:=radix) radix bo precision f1
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
      Id.run, ULift.up_down, Fbounded', nf1, Int.cast_ofNat] using
      h ⟨hf1Bound, hf1Bound, hprecision, hradix, hvNum⟩
  have hnf2Canonic : Fcanonic (beta:=radix) radix bo nf2 := by
    have h := FnormalizeCanonic (beta:=radix) radix bo precision f2
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
      Id.run, ULift.up_down, Fbounded', nf2, Int.cast_ofNat] using
      h ⟨hf2Bound, hf2Bound, hprecision, hradix, hvNum⟩
  have hclosestNorm :
      Closest (beta:=radix) bo (radix : ℝ)
        (_root_.F2R (beta:=radix) nf1 + _root_.F2R (beta:=radix) nf2) g := by
    simpa [hnf1Correct, hnf2Correct] using hclosest
  have hmagNorm :
      (radix : ℝ) ^ e ≤ |_root_.F2R (beta:=radix) nf1| := by
    simpa [hnf1Correct] using hmag1
  have hres :=
    AddExpGe1Underf_from_generic_radix_payload (beta:=radix) bo radix precision rfl hradix hprecision
      hvNum_gt hvNum nf1 nf2 g e hclosestNorm hnf1Canonic
      hnf2Canonic hmagNorm heUnder
  exact hres.resolve_left (fun hgZero => hgNonzero hgZero)

/-- Exact exported Coq `AddExpGe1Underf2` binary contract. -/
theorem AddExpGe1Underf2
    (bo : Fbound_skel) (precision : Nat)
    (hprecision : 1 < precision)
    (hvNum : bo.vNum = Zpower_nat 2 precision)
    (_hprecision4 : 4 ≤ precision) :
    ∀ f1 f2 g : FloatSpec.Core.Defs.FlocqFloat 2, ∀ e : Int,
      Closest (beta:=2) bo (2 : ℝ)
        (_root_.F2R (beta:=2) f1 + _root_.F2R (beta:=2) f2) g →
      Fbounded (beta:=2) bo f1 →
      Fbounded (beta:=2) bo f2 →
      (2 : ℝ) ^ e ≤ |_root_.F2R (beta:=2) f1| →
      -bo.dExp ≤ e - 1 →
      _root_.F2R (beta:=2) g ≠ 0 →
      (2 : ℝ) ^ (e - (precision : Int)) ≤
        |_root_.F2R (beta:=2) g| := by
  have hprecision_ne : precision ≠ 0 := by omega
  have hvNum_gt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ (by decide) hprecision_ne
  exact AddExpGe1Underf2_from_generic_radix_payload (beta:=2)
    bo 2 precision rfl (by decide) hprecision_ne hvNum_gt hvNum

/-- Coq: `plusExactR0`.

If the closest rounded sum is represented by a zero-valued float, the exact
sum is zero as well. -/
theorem plusExactR0_from_format_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat) :
    beta = radix →
    1 < radix →
    precision ≠ 0 →
    1 < bo.vNum →
    bo.vNum = Zpower_nat radix precision →
    ∀ p q r : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      Closest (beta:=beta) bo (radix : ℝ)
        (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) r →
      _root_.F2R (beta:=beta) r = 0 →
      _root_.F2R (beta:=beta) r =
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q := by
  intro hbeta hradix hprecision hvNum_gt hvNum p q r hp hq hclosest hrZero
  let z : FloatSpec.Core.Defs.FlocqFloat beta := Fzero beta (-bo.dExp)
  have hzVal : _root_.F2R (beta:=beta) z = 0 := by
    have h := FzeroisZero (beta:=beta) bo
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FzeroisZero_check, Id.run,
      ULift.up_down, z, Int.cast_ofNat] using h True.intro
  have hzBound : Fbounded (beta:=beta) bo z := by
    have h := FboundedFzero_from_positive_bound_payload (beta:=beta) bo
    have hvNum_pos : 0 < bo.vNum := by omega
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FboundedFzero_check, Id.run,
      ULift.up_down, z, Int.cast_ofNat] using h hvNum_pos
  have hclosestZero :
      Closest (beta:=beta) bo (radix : ℝ)
        (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) z := by
    constructor
    · exact hzBound
    · intro g hg
      have hdist := hclosest.2 g hg
      simpa [hzVal, hrZero] using hdist
  have hzExp : z.Fexp ≤ min p.Fexp q.Fexp := by
    dsimp [z, Fzero]
    exact le_min hp.2 hq.2
  have hexact := plusExact1_from_format_payload (beta:=beta) bo radix precision hbeta hradix
    hprecision hvNum_gt hvNum p q z hp hq hclosestZero hzExp
  rw [hrZero]
  exact hzVal.symm.trans hexact

/-- Exact exported Coq `plusExactR0` contract. -/
theorem plusExactR0 {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hprecision : 1 < precision)
    (hvNum : bo.vNum = Zpower_nat radix precision) :
    ∀ p q r : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo p →
      Fbounded (beta:=beta) bo q →
      Closest (beta:=beta) bo (radix : ℝ)
        (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) r →
      _root_.F2R (beta:=beta) r = 0 →
      _root_.F2R (beta:=beta) r =
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q := by
  have hprecision_ne : precision ≠ 0 := by omega
  have hvNum_gt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecision_ne
  letI : ValidRadix radix := ⟨hradix⟩
  exact plusExactR0_from_format_payload (beta:=beta) bo radix precision
    hbeta hradix hprecision_ne hvNum_gt hvNum

/-- Coq: `Axpy_aux2`, the subnormal `t` branch of the Axpy min/max proof. -/
theorem Axpy_aux2 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (a x y t u : FloatSpec.Core.Defs.FlocqFloat beta)
    (a1 x1 y1 : ℝ) :
    ⦃⌜Fcanonic (beta:=beta) 2 b u ∧
        Fsubnormal (beta:=beta) 2 b t ∧
        0 < _root_.F2R (beta:=beta) u ∧
        _root_.F2R (beta:=beta) u =
          _root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) y ∧
        |_root_.F2R (beta:=beta) y - y1| +
          |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
            a1 * x1| <
          (1 / 4 : ℝ) * Fulp (beta:=beta) b 2 precision
            (FPred (beta:=beta) b 2 precision u) ∧
        Closest (beta:=beta) b (2 : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) t ∧
        beta = 2 ∧ 1 < precision ∧ b.vNum = Zpower_nat 2 precision⌝⦄
    (pure (Axpy_aux2_check (beta:=beta) b precision a x y t u a1 x1 y1) : Id Unit)
    ⦃⇓_ => ⌜MinOrMax (beta:=beta) b 2 (a1 * x1 + y1) u⌝⦄ := by
  intro h
  rcases h with
    ⟨huCan, htSub, huPos, huEq, hperturb, htClosest,
      hbeta, hprecision_gt, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, Axpy_aux2_check,
    Id.run, ULift.up_down]
  have hprecision : precision ≠ 0 := by omega
  let ulp : ℝ := Fulp (beta:=beta) b 2 precision
    (FPred (beta:=beta) b 2 precision u)
  let A : ℝ := _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x
  let T : ℝ := _root_.F2R (beta:=beta) t
  let Y : ℝ := _root_.F2R (beta:=beta) y
  let U : ℝ := _root_.F2R (beta:=beta) u
  have huBound : Fbounded (beta:=beta) b u := by
    have h := FcanonicBound (beta:=beta) 2 b u
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicBound_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h huCan
  have htCan : Fcanonic (beta:=beta) 2 b t := Or.inr htSub
  have htUlp :
      Fulp (beta:=beta) b 2 precision t = (2 : ℝ) ^ (-b.dExp) := by
    have hcanulp :
        Fulp (beta:=beta) b 2 precision t = (2 : ℝ) ^ t.Fexp := by
      have h := CanonicFulp (beta:=beta) 2 b precision t
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨htCan, hbeta, by decide, hprecision, hvNum⟩
    rw [hcanulp, htSub.2.1]
  have hboundary_le_pred :
      (2 : ℝ) ^ (-b.dExp) ≤ ulp := by
    let pred : FloatSpec.Core.Defs.FlocqFloat beta :=
      FPred (beta:=beta) b 2 precision u
    have hpredBound : Fbounded (beta:=beta) b pred := by
      have h := FBoundedPred (beta:=beta) b 2 precision u
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FBoundedPred_check,
        Id.run, ULift.up_down, pred, Int.cast_ofNat] using
        h ⟨huBound, by decide, hprecision, hvNum⟩
    have hnormBound :
        Fbounded (beta:=beta) b
          (Fnormalize (beta:=beta) 2 b precision pred) := by
      have h := FnormalizeBounded (beta:=beta) 2 b precision pred
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeBounded_check,
        Id.run, ULift.up_down, Fbounded', Int.cast_ofNat] using
        h ⟨hpredBound, hpredBound, hprecision, by decide, hvNum⟩
    have hexp_le :
        -b.dExp ≤ (Fnormalize (beta:=beta) 2 b precision pred).Fexp :=
      hnormBound.2
    have hpow_le :
        (2 : ℝ) ^ (-b.dExp) ≤
          (2 : ℝ) ^ (Fnormalize (beta:=beta) 2 b precision pred).Fexp :=
      zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2) hexp_le
    simpa [ulp, pred, Fulp] using hpow_le
  have htClosestUlp :
      2 * |A - T| ≤ Fulp (beta:=beta) b 2 precision t := by
    have h := ClosestUlp (beta:=beta) b 2 precision A t
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, A, T, Int.cast_ofNat] using
      h ⟨htClosest, hbeta, by decide, hprecision, hvNum⟩
  have ht_dist_le : |A - T| ≤ (1 / 2 : ℝ) * (2 : ℝ) ^ (-b.dExp) := by
    rw [← htUlp]
    nlinarith
  have ht_dist_le_ulp : |A - T| ≤ (1 / 2 : ℝ) * ulp := by
    have hmul := mul_le_mul_of_nonneg_left hboundary_le_pred
      (by norm_num : (0 : ℝ) ≤ 1 / 2)
    exact le_trans ht_dist_le hmul
  have hres_zero : |T + Y - U| = 0 := by
    have hzero : T + Y - U = 0 := by
      dsimp [T, Y, U]
      linarith
    rw [hzero, abs_zero]
  have hperturb' :
      |y1 - Y| + |a1 * x1 - A| < (1 / 4 : ℝ) * ulp := by
    dsimp [A, Y, ulp] at hperturb ⊢
    simpa [abs_sub_comm, add_comm] using hperturb
  have hdist_tri :
      |a1 * x1 + y1 - U| ≤
        |y1 - Y| + |a1 * x1 - A| + (|A - T| + |T + Y - U|) := by
    have hdecomp :
        a1 * x1 + y1 - U =
          (y1 - Y) + (a1 * x1 - A) + ((A - T) + (T + Y - U)) := by
      ring
    rw [hdecomp]
    calc
      |(y1 - Y) + (a1 * x1 - A) + ((A - T) + (T + Y - U))|
          ≤ |(y1 - Y) + (a1 * x1 - A)| +
              |(A - T) + (T + Y - U)| := by
                simpa [add_assoc] using
                  abs_add_le ((y1 - Y) + (a1 * x1 - A))
                    ((A - T) + (T + Y - U))
      _ ≤ (|y1 - Y| + |a1 * x1 - A|) +
              (|A - T| + |T + Y - U|) := by
                exact add_le_add (abs_add_le (y1 - Y) (a1 * x1 - A))
                  (abs_add_le (A - T) (T + Y - U))
      _ = |y1 - Y| + |a1 * x1 - A| + (|A - T| + |T + Y - U|) := by
                ring
  have hdist_lt :
      |a1 * x1 + y1 - U| < ulp := by
    have htotal :
        |a1 * x1 + y1 - U| <
          (1 / 4 : ℝ) * ulp + ((1 / 2 : ℝ) * ulp + 0) := by
      rw [hres_zero] at hdist_tri
      nlinarith
    have hulp_pos : 0 < ulp := by
      dsimp [ulp, Fulp]
      exact zpow_pos (by norm_num : (0 : ℝ) < 2) _
    nlinarith
  have h := MinOrMax1 (beta:=beta) b 2 precision (a1 * x1 + y1) u
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinOrMax1_check,
    Id.run, ULift.up_down, U, ulp, Int.cast_ofNat] using
    h ⟨huBound, huCan, huPos, by simpa [U, ulp] using hdist_lt,
      hbeta, by decide, hprecision, hvNum⟩

/-- Coq: `Axpy_aux1`, the positive normal-branch Axpy min/max reduction.

The theorem assumes the already-factored quarter-ulp product error bound and
uses closestness of `u` to `t + y` to choose the lower or upper extremal case. -/
theorem Axpy_aux1 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (a x y t u : FloatSpec.Core.Defs.FlocqFloat beta)
    (a1 x1 y1 : ℝ) :
    ⦃⌜Fcanonic (beta:=beta) 2 b u ∧
        |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
          _root_.F2R (beta:=beta) t| ≤
          (1 / 4 : ℝ) * Fulp (beta:=beta) b 2 precision
            (FPred (beta:=beta) b 2 precision u) ∧
        0 < _root_.F2R (beta:=beta) u ∧
        (4 : ℝ) * |_root_.F2R (beta:=beta) t| ≤
          |_root_.F2R (beta:=beta) u| ∧
        |_root_.F2R (beta:=beta) y - y1| +
          |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
            a1 * x1| <
          (1 / 4 : ℝ) * Fulp (beta:=beta) b 2 precision
            (FPred (beta:=beta) b 2 precision u) ∧
        Closest (beta:=beta) b (2 : ℝ)
          (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) y) u ∧
        beta = 2 ∧ 1 < precision ∧ b.vNum = Zpower_nat 2 precision⌝⦄
    (pure (Axpy_aux1_check (beta:=beta) b precision a x y t u a1 x1 y1) : Id Unit)
    ⦃⇓_ => ⌜MinOrMax (beta:=beta) b 2 (a1 * x1 + y1) u⌝⦄ := by
  intro h
  rcases h with
    ⟨huCan, hblop, huPos, _htScale, hperturb, huClosest,
      hbeta, hprecision_gt, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, Axpy_aux1_check,
    Id.run, ULift.up_down]
  have hprecision : precision ≠ 0 := by omega
  let ulp : ℝ := Fulp (beta:=beta) b 2 precision
    (FPred (beta:=beta) b 2 precision u)
  let A : ℝ := _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x
  let T : ℝ := _root_.F2R (beta:=beta) t
  let Y : ℝ := _root_.F2R (beta:=beta) y
  let U : ℝ := _root_.F2R (beta:=beta) u
  let S : ℝ := T + Y
  let Z : ℝ := a1 * x1 + y1
  let pred : FloatSpec.Core.Defs.FlocqFloat beta :=
    FPred (beta:=beta) b 2 precision u
  have huBound : Fbounded (beta:=beta) b u := by
    have h := FcanonicBound (beta:=beta) 2 b u
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicBound_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h huCan
  have hpredCan : Fcanonic (beta:=beta) 2 b pred := by
    have h := FPredCanonic (beta:=beta) b 2 precision u
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FPredCanonic_check,
      Id.run, ULift.up_down, pred, Int.cast_ofNat] using
      h ⟨huCan, by decide, hprecision, hvNum⟩
  have hpredBound : Fbounded (beta:=beta) b pred := by
    have h := FcanonicBound (beta:=beta) 2 b pred
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicBound_check,
      Id.run, ULift.up_down, pred, Int.cast_ofNat] using h hpredCan
  have hulp_pos : 0 < ulp := by
    dsimp [ulp, pred, Fulp]
    exact zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hpred_step :
      _root_.F2R (beta:=beta) pred + ulp = U := by
    have h := FpredUlpPos (beta:=beta) 2 b precision u
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FpredUlpPos_check,
      Id.run, ULift.up_down, pred, ulp, U, Int.cast_ofNat] using
      h ⟨huCan, huPos, hbeta, by decide, hprecision, hvNum⟩
  have huClosestUlp :
      2 * |S - U| ≤ Fulp (beta:=beta) b 2 precision u := by
    have h := ClosestUlp (beta:=beta) b 2 precision S u
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, S, U, Int.cast_ofNat] using
      h ⟨huClosest, hbeta, by decide, hprecision, hvNum⟩
  have huUlp_le_pred :
      Fulp (beta:=beta) b 2 precision u ≤ (2 : ℝ) * ulp := by
    have h := FulpFPredLe (beta:=beta) 2 b precision u
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpFPredLe_check,
      Id.run, ULift.up_down, pred, ulp, Int.cast_ofNat] using
      h ⟨huBound, huCan, hbeta, by decide, hprecision, hvNum⟩
  have hpred_ulp_le_u :
      ulp ≤ Fulp (beta:=beta) b 2 precision u := by
    have h := FulpFPredGePos (beta:=beta) 2 b precision u
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpFPredGePos_check,
      Id.run, ULift.up_down, pred, ulp, Int.cast_ofNat] using
      h ⟨huBound, huCan, huPos, hbeta, by decide, hprecision, hvNum⟩
  have hperturb' :
      |y1 - Y| + |a1 * x1 - A| < (1 / 4 : ℝ) * ulp := by
    dsimp [A, Y, ulp] at hperturb ⊢
    simpa [abs_sub_comm, add_comm] using hperturb
  have hblop' : |A - T| ≤ (1 / 4 : ℝ) * ulp := by
    dsimp [A, T, ulp] at hblop ⊢
    simpa using hblop
  have hdist_tri :
      |Z - U| ≤
        |y1 - Y| + |a1 * x1 - A| + (|A - T| + |S - U|) := by
    have hdecomp :
        Z - U =
          (y1 - Y) + (a1 * x1 - A) + ((A - T) + (S - U)) := by
      dsimp [Z, S]
      ring
    rw [hdecomp]
    calc
      |(y1 - Y) + (a1 * x1 - A) + ((A - T) + (S - U))|
          ≤ |(y1 - Y) + (a1 * x1 - A)| +
              |(A - T) + (S - U)| := by
                simpa [add_assoc] using
                  abs_add_le ((y1 - Y) + (a1 * x1 - A))
                    ((A - T) + (S - U))
      _ ≤ (|y1 - Y| + |a1 * x1 - A|) +
              (|A - T| + |S - U|) := by
                exact add_le_add (abs_add_le (y1 - Y) (a1 * x1 - A))
                  (abs_add_le (A - T) (S - U))
      _ = |y1 - Y| + |a1 * x1 - A| + (|A - T| + |S - U|) := by
                ring
  have hdist_main :
      |Z - U| < (1 / 2 : ℝ) * ulp + |S - U| := by
    have hsum_lt :
        |y1 - Y| + |a1 * x1 - A| + (|A - T| + |S - U|)
          < (1 / 2 : ℝ) * ulp + |S - U| := by
      nlinarith
    exact lt_of_le_of_lt hdist_tri hsum_lt
  by_cases hS_le_U : S ≤ U
  · have hclosest_half_pred : |S - U| ≤ (1 / 2 : ℝ) * ulp := by
      by_contra hnot
      have hgt : (1 / 2 : ℝ) * ulp < |S - U| := lt_of_not_ge hnot
      have hSU_nonpos : S - U ≤ 0 := by linarith
      have hUS_nonneg : 0 ≤ U - S := by linarith
      have habs_SU : |S - U| = U - S := by
        rw [abs_of_nonpos hSU_nonpos]
        ring
      have hgt' : (1 / 2 : ℝ) * ulp < U - S := by
        simpa [habs_SU] using hgt
      have hpred_val : _root_.F2R (beta:=beta) pred = U - ulp := by
        linarith
      have hclosest_pred :
          |U - S| ≤ |_root_.F2R (beta:=beta) pred - S| := by
        have h := huClosest.2 pred hpredBound
        simpa [U] using h
      have hpred_dist_lt :
          |_root_.F2R (beta:=beta) pred - S| < |U - S| := by
        rw [hpred_val]
        have habs_US : |U - S| = U - S := by
          rw [abs_of_nonneg hUS_nonneg]
        rw [habs_US]
        apply abs_lt.mpr
        constructor
        · nlinarith
        · nlinarith [hulp_pos]
      exact not_lt_of_ge hclosest_pred hpred_dist_lt
    have hdist_lt : |Z - U| < ulp := by
      have hmain := hdist_main
      nlinarith
    have h := MinOrMax1 (beta:=beta) b 2 precision Z u
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinOrMax1_check,
      Id.run, ULift.up_down, Z, U, pred, ulp, Int.cast_ofNat] using
      h ⟨huBound, huCan, huPos, by simpa [Z, U, pred, ulp] using hdist_lt,
        hbeta, by decide, hprecision, hvNum⟩
  · have hU_lt_S : U < S := lt_of_not_ge hS_le_U
    by_cases hS_le_half : S ≤ U + (1 / 2 : ℝ) * ulp
    · have habs_le : |S - U| ≤ (1 / 2 : ℝ) * ulp := by
        have hnonneg : 0 ≤ S - U := by linarith
        rw [abs_of_nonneg hnonneg]
        linarith
      have hdist_lt : |Z - U| < ulp := by
        have hmain := hdist_main
        nlinarith
      have h := MinOrMax1 (beta:=beta) b 2 precision Z u
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinOrMax1_check,
        Id.run, ULift.up_down, Z, U, pred, ulp, Int.cast_ofNat] using
        h ⟨huBound, huCan, huPos, by simpa [Z, U, pred, ulp] using hdist_lt,
          hbeta, by decide, hprecision, hvNum⟩
    · have hhalf_lt : U + (1 / 2 : ℝ) * ulp < S := lt_of_not_ge hS_le_half
      have hU_le_Z : U ≤ Z := by
        have herr1 :
            -((1 / 4 : ℝ) * ulp) <
              (y1 - Y) + (a1 * x1 - A) := by
          let E : ℝ := (y1 - Y) + (a1 * x1 - A)
          have habs_le :
              |E| ≤ |y1 - Y| + |a1 * x1 - A| := by
            dsimp [E]
            exact abs_add_le (y1 - Y) (a1 * x1 - A)
          have habs_lt : |E| < (1 / 4 : ℝ) * ulp :=
            lt_of_le_of_lt habs_le hperturb'
          have hneg := neg_abs_le E
          dsimp [E] at hneg habs_lt ⊢
          nlinarith
        have herr2 :
            -((1 / 4 : ℝ) * ulp) ≤ A - T := by
          have h := neg_abs_le (A - T)
          nlinarith
        have hdecomp :
            Z - U =
              ((y1 - Y) + (a1 * x1 - A)) + (A - T) + (S - U) := by
          dsimp [Z, S]
          ring
        have hzu_pos : 0 < Z - U := by
          rw [hdecomp]
          nlinarith
        linarith
      have hdist_lt_u : |Z - U| < Fulp (beta:=beta) b 2 precision u := by
        have hmain := hdist_main
        have hhalf_u :
            (1 / 2 : ℝ) * ulp ≤
              (1 / 2 : ℝ) * Fulp (beta:=beta) b 2 precision u := by
          nlinarith
        have hS_u :
            |S - U| ≤ (1 / 2 : ℝ) * Fulp (beta:=beta) b 2 precision u := by
          nlinarith
        have hsum_le :
            (1 / 2 : ℝ) * ulp + |S - U| ≤
              (1 / 2 : ℝ) * Fulp (beta:=beta) b 2 precision u +
                (1 / 2 : ℝ) * Fulp (beta:=beta) b 2 precision u :=
          add_le_add hhalf_u hS_u
        have hsum_eq :
            (1 / 2 : ℝ) * Fulp (beta:=beta) b 2 precision u +
                (1 / 2 : ℝ) * Fulp (beta:=beta) b 2 precision u =
              Fulp (beta:=beta) b 2 precision u := by
          ring
        exact lt_of_lt_of_le hmain
          (calc
            (1 / 2 : ℝ) * ulp + |S - U| ≤
                (1 / 2 : ℝ) * Fulp (beta:=beta) b 2 precision u +
                  (1 / 2 : ℝ) * Fulp (beta:=beta) b 2 precision u := hsum_le
            _ = Fulp (beta:=beta) b 2 precision u := hsum_eq)
      have h := MinOrMax2 (beta:=beta) b 2 precision Z u
      have hdist_lt_u' :
          |Z - _root_.F2R (beta:=beta) u| <
            Fulp (beta:=beta) b 2 precision u := by
        simpa [U] using hdist_lt_u
      have hU_le_Z' : _root_.F2R (beta:=beta) u ≤ Z := by
        simpa [U] using hU_le_Z
      have hres : MinOrMax (beta:=beta) b 2 Z u := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinOrMax2_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          h ⟨huBound, huCan, huPos, hdist_lt_u', hU_le_Z',
            hbeta, by decide, hprecision, hvNum⟩
      simpa [Z] using hres

/-- Coq: `Axpy_aux1_aux1`, the normal `t` Axpy error estimate.

The normality of `t` lets us compare the ulp of `4 * t` with the ulp of `u`
using `LeFulpPos`; `FulpFPredLe` then transfers the bound to the predecessor
ulp used by the Axpy min/max proof. -/
theorem Axpy_aux1_aux1 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (a x t u : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fnormal (beta:=beta) 2 b t ∧
        Fcanonic (beta:=beta) 2 b u ∧
        0 < _root_.F2R (beta:=beta) u ∧
        (4 : ℝ) * |_root_.F2R (beta:=beta) t| ≤
          |_root_.F2R (beta:=beta) u| ∧
        Closest (beta:=beta) b (2 : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) t ∧
        beta = 2 ∧ 1 < precision ∧ b.vNum = Zpower_nat 2 precision⌝⦄
    (pure (Axpy_aux1_aux1_check (beta:=beta) b precision a x t u) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
          _root_.F2R (beta:=beta) t| ≤
        (1 / 4 : ℝ) * Fulp (beta:=beta) b 2 precision
          (FPred (beta:=beta) b 2 precision u)⌝⦄ := by
  intro h
  rcases h with
    ⟨htNorm, huCan, _huPos, htScale, htClosest,
      hbeta, hprecision_gt, hvNum⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure, Axpy_aux1_aux1_check,
    Id.run, ULift.up_down]
  have hprecision : precision ≠ 0 := by omega
  let A : ℝ := _root_.F2R (beta:=2) a * _root_.F2R (beta:=2) x
  let T : ℝ := _root_.F2R (beta:=2) t
  let shifted : FloatSpec.Core.Defs.FlocqFloat 2 := ⟨t.Fnum, t.Fexp + 2⟩
  let pred : FloatSpec.Core.Defs.FlocqFloat 2 :=
    FPred (beta:=2) b 2 precision u
  have htBound : Fbounded (beta:=2) b t := htNorm.1
  have huBound : Fbounded (beta:=2) b u := by
    have h := FcanonicBound (beta:=2) 2 b u
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicBound_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h huCan
  have hshiftNorm : Fnormal (beta:=2) 2 b shifted := by
    rcases htNorm with ⟨⟨htNum, htExp⟩, htMant⟩
    refine ⟨⟨?_, ?_⟩, ?_⟩
    · simpa [shifted] using htNum
    · dsimp [shifted]
      omega
    · simpa [shifted] using htMant
  have hshiftAbsNorm : Fnormal (beta:=2) 2 b (Fabs (beta:=2) shifted) := by
    have h := FnormalFabs (beta:=2) b 2 shifted
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalFabs_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hshiftNorm
  have hshiftAbsBound : Fbounded (beta:=2) b (Fabs (beta:=2) shifted) :=
    hshiftAbsNorm.1
  have huAbsCan : Fcanonic (beta:=2) 2 b (Fabs (beta:=2) u) := by
    have h := FcanonicFabs (beta:=2) 2 b u
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFabs_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h huCan
  have huAbsBound : Fbounded (beta:=2) b (Fabs (beta:=2) u) := by
    have h := FcanonicBound (beta:=2) 2 b (Fabs (beta:=2) u)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicBound_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h huAbsCan
  have hshiftVal :
      _root_.F2R (beta:=2) shifted = (4 : ℝ) * T := by
    dsimp [shifted, T, _root_.F2R, FloatSpec.Core.Defs.F2R]
    have htwo_ne : (2 : ℝ) ≠ 0 := by norm_num
    calc
      (t.Fnum : ℝ) * (2 : ℝ) ^ (t.Fexp + 2)
          = (t.Fnum : ℝ) *
              ((2 : ℝ) ^ t.Fexp * (2 : ℝ) ^ (2 : Int)) := by
                rw [zpow_add₀ htwo_ne]
      _ = (4 : ℝ) * ((t.Fnum : ℝ) * (2 : ℝ) ^ t.Fexp) := by
                norm_num
                ring
  have hshiftAbsVal :
      _root_.F2R (beta:=2) (Fabs (beta:=2) shifted) =
        (4 : ℝ) * |T| := by
    have hcorr : _root_.F2R (beta:=2) (Fabs (beta:=2) shifted) =
        |_root_.F2R (beta:=2) shifted| := by
      have h := Fabs_correct (beta:=2) shifted
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fabs_correct_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h (by norm_num)
    rw [hcorr, hshiftVal, abs_mul, abs_of_nonneg (by norm_num : (0 : ℝ) ≤ 4)]
  have huAbsVal :
      _root_.F2R (beta:=2) (Fabs (beta:=2) u) =
        |_root_.F2R (beta:=2) u| := by
    have h := Fabs_correct (beta:=2) u
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fabs_correct_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h (by norm_num)
  have hshiftAbsNonneg :
      0 ≤ _root_.F2R (beta:=2) (Fabs (beta:=2) shifted) := by
    rw [hshiftAbsVal]
    nlinarith [abs_nonneg T]
  have hshiftAbsLeUAbs :
      _root_.F2R (beta:=2) (Fabs (beta:=2) shifted) ≤
        _root_.F2R (beta:=2) (Fabs (beta:=2) u) := by
    rw [hshiftAbsVal, huAbsVal]
    simpa [T] using htScale
  have hmono :
      Fulp (beta:=2) b 2 precision (Fabs (beta:=2) shifted) ≤
        Fulp (beta:=2) b 2 precision (Fabs (beta:=2) u) := by
    have h := LeFulpPos (beta:=2) 2 b precision
      (Fabs (beta:=2) shifted) (Fabs (beta:=2) u)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, LeFulpPos_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hshiftAbsBound, huAbsBound, hshiftAbsNonneg, hshiftAbsLeUAbs,
        rfl, by decide, hprecision, hvNum⟩
  have htCan : Fcanonic (beta:=2) 2 b t := Or.inl htNorm
  have htUlp :
      Fulp (beta:=2) b 2 precision t = (2 : ℝ) ^ t.Fexp := by
    have h := CanonicFulp (beta:=2) 2 b precision t
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨htCan, rfl, by decide, hprecision, hvNum⟩
  have hshiftAbsUlp :
      Fulp (beta:=2) b 2 precision (Fabs (beta:=2) shifted) =
        (4 : ℝ) * Fulp (beta:=2) b 2 precision t := by
    have hshiftCan : Fcanonic (beta:=2) 2 b (Fabs (beta:=2) shifted) :=
      Or.inl hshiftAbsNorm
    have h := CanonicFulp (beta:=2) 2 b precision (Fabs (beta:=2) shifted)
    have hshiftUlp :
        Fulp (beta:=2) b 2 precision (Fabs (beta:=2) shifted) =
          (2 : ℝ) ^ (Fabs (beta:=2) shifted).Fexp := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hshiftCan, rfl, by decide, hprecision, hvNum⟩
    rw [hshiftUlp, htUlp]
    dsimp [shifted, Fabs, FloatSpec.Calc.Operations.Fabs]
    have htwo_ne : (2 : ℝ) ≠ 0 := by norm_num
    calc
      (2 : ℝ) ^ (t.Fexp + 2)
          = (2 : ℝ) ^ t.Fexp * (2 : ℝ) ^ (2 : Int) := by
                rw [zpow_add₀ htwo_ne]
      _ = (4 : ℝ) * (2 : ℝ) ^ t.Fexp := by
                norm_num
                ring
  have huAbsUlp :
      Fulp (beta:=2) b 2 precision (Fabs (beta:=2) u) =
        Fulp (beta:=2) b 2 precision u := by
    have huUlp : Fulp (beta:=2) b 2 precision u = (2 : ℝ) ^ u.Fexp := by
      have h := CanonicFulp (beta:=2) 2 b precision u
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨huCan, rfl, by decide, hprecision, hvNum⟩
    have huAbsUlp' :
        Fulp (beta:=2) b 2 precision (Fabs (beta:=2) u) =
          (2 : ℝ) ^ (Fabs (beta:=2) u).Fexp := by
      have h := CanonicFulp (beta:=2) 2 b precision (Fabs (beta:=2) u)
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨huAbsCan, rfl, by decide, hprecision, hvNum⟩
    rw [huAbsUlp', huUlp]
    simp [Fabs, FloatSpec.Calc.Operations.Fabs]
  have htUlp_le_u :
      (4 : ℝ) * Fulp (beta:=2) b 2 precision t ≤
        Fulp (beta:=2) b 2 precision u := by
    simpa [hshiftAbsUlp, huAbsUlp] using hmono
  have huUlp_le_pred :
      Fulp (beta:=2) b 2 precision u ≤
        (2 : ℝ) * Fulp (beta:=2) b 2 precision pred := by
    have h := FulpFPredLe (beta:=2) 2 b precision u
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpFPredLe_check,
      Id.run, ULift.up_down, pred, Int.cast_ofNat] using
      h ⟨huBound, huCan, rfl, by decide, hprecision, hvNum⟩
  have htUlp_le_pred_half :
      Fulp (beta:=2) b 2 precision t ≤
        (1 / 2 : ℝ) * Fulp (beta:=2) b 2 precision pred := by
    nlinarith
  have htClosestUlp :
      2 * |A - T| ≤ Fulp (beta:=2) b 2 precision t := by
    have h := ClosestUlp (beta:=2) b 2 precision A t
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, A, T, Int.cast_ofNat] using
      h ⟨htClosest, rfl, by decide, hprecision, hvNum⟩
  have hdist_half :
      |A - T| ≤ (1 / 2 : ℝ) * Fulp (beta:=2) b 2 precision t := by
    nlinarith
  have hquarter :
      (1 / 2 : ℝ) * Fulp (beta:=2) b 2 precision t ≤
        (1 / 4 : ℝ) * Fulp (beta:=2) b 2 precision pred := by
    nlinarith
  exact le_trans hdist_half hquarter

/-- Coq: `Axpy_aux1_aux3`, the subnormal `t` error estimate from a predecessor
exponent lower bound. -/
theorem Axpy_aux1_aux3 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (a x t u : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fsubnormal (beta:=beta) 2 b t ∧
        Fcanonic (beta:=beta) 2 b u ∧
        0 < _root_.F2R (beta:=beta) u ∧
        -b.dExp + 1 ≤
          (FPred (beta:=beta) b 2 precision u).Fexp ∧
        Closest (beta:=beta) b (2 : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) t ∧
        beta = 2 ∧ 1 < precision ∧ b.vNum = Zpower_nat 2 precision⌝⦄
    (pure (Axpy_aux1_aux3_check (beta:=beta) b precision a x t u) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
          _root_.F2R (beta:=beta) t| ≤
        (1 / 4 : ℝ) * Fulp (beta:=beta) b 2 precision
          (FPred (beta:=beta) b 2 precision u)⌝⦄ := by
  intro h
  rcases h with
    ⟨htSub, huCan, _huPos, hpredExp, htClosest, hbeta, hprecision_gt, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, Axpy_aux1_aux3_check,
    Id.run, ULift.up_down]
  have hprecision : precision ≠ 0 := by omega
  let A : ℝ := _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x
  let T : ℝ := _root_.F2R (beta:=beta) t
  let pred : FloatSpec.Core.Defs.FlocqFloat beta :=
    FPred (beta:=beta) b 2 precision u
  have htCan : Fcanonic (beta:=beta) 2 b t := Or.inr htSub
  have htUlp :
      Fulp (beta:=beta) b 2 precision t = (2 : ℝ) ^ (-b.dExp) := by
    have hcanulp :
        Fulp (beta:=beta) b 2 precision t = (2 : ℝ) ^ t.Fexp := by
      have h := CanonicFulp (beta:=beta) 2 b precision t
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨htCan, hbeta, by decide, hprecision, hvNum⟩
    rw [hcanulp, htSub.2.1]
  have hpredCan : Fcanonic (beta:=beta) 2 b pred := by
    have h := FPredCanonic (beta:=beta) b 2 precision u
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FPredCanonic_check,
      Id.run, ULift.up_down, pred, Int.cast_ofNat] using
      h ⟨huCan, by decide, hprecision, hvNum⟩
  have hpredUlp :
      Fulp (beta:=beta) b 2 precision pred = (2 : ℝ) ^ pred.Fexp := by
    have h := CanonicFulp (beta:=beta) 2 b precision pred
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
      Id.run, ULift.up_down, pred, Int.cast_ofNat] using
      h ⟨hpredCan, hbeta, by decide, hprecision, hvNum⟩
  have htClosestUlp :
      2 * |A - T| ≤ Fulp (beta:=beta) b 2 precision t := by
    have h := ClosestUlp (beta:=beta) b 2 precision A t
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, A, T, Int.cast_ofNat] using
      h ⟨htClosest, hbeta, by decide, hprecision, hvNum⟩
  have hdist_half : |A - T| ≤ (1 / 2 : ℝ) * (2 : ℝ) ^ (-b.dExp) := by
    rw [← htUlp]
    nlinarith
  have htwo_mul :
      (2 : ℝ) * (2 : ℝ) ^ (-b.dExp) = (2 : ℝ) ^ (-b.dExp + 1) := by
    have htwo_ne : (2 : ℝ) ≠ 0 := by norm_num
    calc
      (2 : ℝ) * (2 : ℝ) ^ (-b.dExp)
          = (2 : ℝ) ^ (-b.dExp) * (2 : ℝ) ^ (1 : Int) := by
              norm_num [mul_comm]
      _ = (2 : ℝ) ^ (-b.dExp + 1) := by
              rw [← zpow_add₀ htwo_ne]
  have hboundary :
      (2 : ℝ) * (2 : ℝ) ^ (-b.dExp) ≤ Fulp (beta:=beta) b 2 precision pred := by
    rw [hpredUlp, htwo_mul]
    exact zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2) hpredExp
  have hquarter :
      (1 / 2 : ℝ) * (2 : ℝ) ^ (-b.dExp) ≤
        (1 / 4 : ℝ) * Fulp (beta:=beta) b 2 precision pred := by
    have hmul := mul_le_mul_of_nonneg_left hboundary
      (by norm_num : (0 : ℝ) ≤ 1 / 4)
    nlinarith
  exact le_trans hdist_half hquarter

private theorem bounded_sum_eq_of_lt_min
    (b : Fbound_skel)
    (t y u : FloatSpec.Core.Defs.FlocqFloat 2)
    (htBound : Fbounded (beta:=2) b t)
    (hyBound : Fbounded (beta:=2) b y)
    (huBound : Fbounded (beta:=2) b u)
    (hsmall : |_root_.F2R (beta:=2) t + _root_.F2R (beta:=2) y -
        _root_.F2R (beta:=2) u| < (2 : ℝ) ^ (-b.dExp)) :
    _root_.F2R (beta:=2) u =
      _root_.F2R (beta:=2) t + _root_.F2R (beta:=2) y := by
  have htExp : -b.dExp ≤ t.Fexp := htBound.2
  have hyExp : -b.dExp ≤ y.Fexp := hyBound.2
  have huExp : -b.dExp ≤ u.Fexp := huBound.2
  let nt : Nat := Int.natAbs (t.Fexp - (-b.dExp))
  let ny : Nat := Int.natAbs (y.Fexp - (-b.dExp))
  let nu : Nat := Int.natAbs (u.Fexp - (-b.dExp))
  have hnt : (nt : Int) = t.Fexp - (-b.dExp) := by
    simpa [nt] using
      Int.natAbs_of_nonneg (by omega : 0 ≤ t.Fexp - (-b.dExp))
  have hny : (ny : Int) = y.Fexp - (-b.dExp) := by
    simpa [ny] using
      Int.natAbs_of_nonneg (by omega : 0 ≤ y.Fexp - (-b.dExp))
  have hnu : (nu : Int) = u.Fexp - (-b.dExp) := by
    simpa [nu] using
      Int.natAbs_of_nonneg (by omega : 0 ≤ u.Fexp - (-b.dExp))
  let ts : FloatSpec.Core.Defs.FlocqFloat 2 := Fshift (beta:=2) 2 nt t
  let ys : FloatSpec.Core.Defs.FlocqFloat 2 := Fshift (beta:=2) 2 ny y
  let us : FloatSpec.Core.Defs.FlocqFloat 2 := Fshift (beta:=2) 2 nu u
  have htsExp : ts.Fexp = -b.dExp := by
    simp only [ts, Fshift, FloatSpec.Core.Defs.FlocqFloat.Fexp]
    rw [hnt]
    omega
  have hysExp : ys.Fexp = -b.dExp := by
    simp only [ys, Fshift, FloatSpec.Core.Defs.FlocqFloat.Fexp]
    rw [hny]
    omega
  have husExp : us.Fexp = -b.dExp := by
    simp only [us, Fshift, FloatSpec.Core.Defs.FlocqFloat.Fexp]
    rw [hnu]
    omega
  have htsVal : _root_.F2R (beta:=2) ts = _root_.F2R (beta:=2) t := by
    simpa [ts] using Fshift_value (beta:=2) 2 nt t rfl (by decide)
  have hysVal : _root_.F2R (beta:=2) ys = _root_.F2R (beta:=2) y := by
    simpa [ys] using Fshift_value (beta:=2) 2 ny y rfl (by decide)
  have husVal : _root_.F2R (beta:=2) us = _root_.F2R (beta:=2) u := by
    simpa [us] using Fshift_value (beta:=2) 2 nu u rfl (by decide)
  let q : ℝ := (2 : ℝ) ^ (-b.dExp)
  have hqPos : 0 < q := zpow_pos (by norm_num) _
  have htVal : _root_.F2R (beta:=2) t = (ts.Fnum : ℝ) * q := by
    rw [← htsVal]
    simp [_root_.F2R, FloatSpec.Core.Defs.F2R, htsExp, q]
  have hyVal : _root_.F2R (beta:=2) y = (ys.Fnum : ℝ) * q := by
    rw [← hysVal]
    simp [_root_.F2R, FloatSpec.Core.Defs.F2R, hysExp, q]
  have huVal : _root_.F2R (beta:=2) u = (us.Fnum : ℝ) * q := by
    rw [← husVal]
    simp [_root_.F2R, FloatSpec.Core.Defs.F2R, husExp, q]
  rw [htVal, hyVal, huVal] at hsmall
  have hcombine :
      (ts.Fnum : ℝ) * q + (ys.Fnum : ℝ) * q - (us.Fnum : ℝ) * q =
        ((ts.Fnum + ys.Fnum - us.Fnum : Int) : ℝ) * q := by
    push_cast
    ring
  rw [hcombine, abs_mul, abs_of_pos hqPos] at hsmall
  have hcoeffAbs :
      |((ts.Fnum + ys.Fnum - us.Fnum : Int) : ℝ)| < 1 := by
    change |((ts.Fnum + ys.Fnum - us.Fnum : Int) : ℝ)| * q < q at hsmall
    nlinarith
  have hcoeffAbsInt : |ts.Fnum + ys.Fnum - us.Fnum| < 1 := by
    exact_mod_cast hcoeffAbs
  have hcoeffAbsNonneg : 0 ≤ |ts.Fnum + ys.Fnum - us.Fnum| :=
    abs_nonneg _
  have hcoeffAbsZero : |ts.Fnum + ys.Fnum - us.Fnum| = 0 := by omega
  have hcoeffZero : ts.Fnum + ys.Fnum - us.Fnum = 0 :=
    abs_eq_zero.mp hcoeffAbsZero
  have hnumEq : us.Fnum = ts.Fnum + ys.Fnum := by omega
  rw [huVal, htVal, hyVal, hnumEq]
  push_cast
  ring

/-- Coq: `Axpy_aux3`, the remaining subnormal `t` Axpy min/max branch.

When `t + y` lies below `u`, closestness rules out the predecessor-boundary
case and `ExactSum_Near` reduces to the already restored `Axpy_aux2`.  When
`t + y` lies above `u`, the negative `u - (t + y)` term tightens the distance
estimate enough for `MinOrMax1`. -/
theorem Axpy_aux3 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (a x y t u : FloatSpec.Core.Defs.FlocqFloat beta)
    (a1 x1 y1 : ℝ) :
    ⦃⌜Fcanonic (beta:=beta) 2 b u ∧
        Fsubnormal (beta:=beta) 2 b t ∧
        Fbounded (beta:=beta) b y ∧
        0 < _root_.F2R (beta:=beta) u ∧
        (FPred (beta:=beta) b 2 precision u).Fexp = -b.dExp ∧
        -b.dExp + 1 ≤ u.Fexp ∧
        |_root_.F2R (beta:=beta) y - y1| +
          |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
            a1 * x1| <
          (1 / 4 : ℝ) * Fulp (beta:=beta) b 2 precision
            (FPred (beta:=beta) b 2 precision u) ∧
        Closest (beta:=beta) b (2 : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) t ∧
        Closest (beta:=beta) b (2 : ℝ)
          (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) y) u ∧
        beta = 2 ∧ 1 < precision ∧ b.vNum = Zpower_nat 2 precision⌝⦄
    (pure (Axpy_aux3_check (beta:=beta) b precision a x y t u a1 x1 y1) :
      Id Unit)
    ⦃⇓_ => ⌜MinOrMax (beta:=beta) b 2 (a1 * x1 + y1) u⌝⦄ := by
  intro h
  rcases h with
    ⟨huCan, htSub, hyBound, huPos, hpredExpEq, huExp, hperturb,
      htClosest, huClosest, hbeta, hprecision_gt, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, Axpy_aux3_check,
    Id.run, ULift.up_down]
  have hprecision : precision ≠ 0 := by omega
  let ulp : ℝ := Fulp (beta:=beta) b 2 precision
    (FPred (beta:=beta) b 2 precision u)
  let A : ℝ := _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x
  let T : ℝ := _root_.F2R (beta:=beta) t
  let Y : ℝ := _root_.F2R (beta:=beta) y
  let U : ℝ := _root_.F2R (beta:=beta) u
  let S : ℝ := T + Y
  let Z : ℝ := a1 * x1 + y1
  let pred : FloatSpec.Core.Defs.FlocqFloat beta :=
    FPred (beta:=beta) b 2 precision u
  have huBound : Fbounded (beta:=beta) b u := by
    have h := FcanonicBound (beta:=beta) 2 b u
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicBound_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h huCan
  have htBound : Fbounded (beta:=beta) b t := htSub.1
  have htCan : Fcanonic (beta:=beta) 2 b t := Or.inr htSub
  have hpredCan : Fcanonic (beta:=beta) 2 b pred := by
    have h := FPredCanonic (beta:=beta) b 2 precision u
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FPredCanonic_check,
      Id.run, ULift.up_down, pred, Int.cast_ofNat] using
      h ⟨huCan, by decide, hprecision, hvNum⟩
  have hpredBound : Fbounded (beta:=beta) b pred := by
    have h := FcanonicBound (beta:=beta) 2 b pred
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicBound_check,
      Id.run, ULift.up_down, pred, Int.cast_ofNat] using h hpredCan
  have hulp_pos : 0 < ulp := by
    dsimp [ulp, pred, Fulp]
    exact zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hboundary_pos : 0 < (2 : ℝ) ^ (-b.dExp) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hpredUlp :
      ulp = (2 : ℝ) ^ (-b.dExp) := by
    have h := CanonicFulp (beta:=beta) 2 b precision pred
    have h' :
        Fulp (beta:=beta) b 2 precision pred = (2 : ℝ) ^ pred.Fexp := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
        Id.run, ULift.up_down, pred, Int.cast_ofNat] using
        h ⟨hpredCan, hbeta, by decide, hprecision, hvNum⟩
    dsimp [ulp]
    rw [h', hpredExpEq]
  have htUlp :
      Fulp (beta:=beta) b 2 precision t = (2 : ℝ) ^ (-b.dExp) := by
    have h := CanonicFulp (beta:=beta) 2 b precision t
    have h' :
        Fulp (beta:=beta) b 2 precision t = (2 : ℝ) ^ t.Fexp := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨htCan, hbeta, by decide, hprecision, hvNum⟩
    rw [h', htSub.2.1]
  have hpred_step :
      _root_.F2R (beta:=beta) pred + ulp = U := by
    have h := FpredUlpPos (beta:=beta) 2 b precision u
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FpredUlpPos_check,
      Id.run, ULift.up_down, pred, ulp, U, Int.cast_ofNat] using
      h ⟨huCan, huPos, hbeta, by decide, hprecision, hvNum⟩
  have htClosestUlp :
      2 * |A - T| ≤ Fulp (beta:=beta) b 2 precision t := by
    have h := ClosestUlp (beta:=beta) b 2 precision A t
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, A, T, Int.cast_ofNat] using
      h ⟨htClosest, hbeta, by decide, hprecision, hvNum⟩
  have ht_dist_half : |A - T| ≤ (1 / 2 : ℝ) * ulp := by
    rw [hpredUlp, ← htUlp]
    nlinarith
  have huClosestUlp :
      2 * |S - U| ≤ Fulp (beta:=beta) b 2 precision u := by
    have h := ClosestUlp (beta:=beta) b 2 precision S u
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, S, U, Int.cast_ofNat] using
      h ⟨huClosest, hbeta, by decide, hprecision, hvNum⟩
  have huUlp_le_twice_pred :
      Fulp (beta:=beta) b 2 precision u ≤ (2 : ℝ) * ulp := by
    have h := FulpFPredLe (beta:=beta) 2 b precision u
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpFPredLe_check,
      Id.run, ULift.up_down, pred, ulp, Int.cast_ofNat] using
      h ⟨huBound, huCan, hbeta, by decide, hprecision, hvNum⟩
  have huUlp_ge_twice_boundary :
      (2 : ℝ) * ((2 : ℝ) ^ (-b.dExp)) ≤
        Fulp (beta:=beta) b 2 precision u := by
    have huUlp :
        Fulp (beta:=beta) b 2 precision u = (2 : ℝ) ^ u.Fexp := by
      have h := CanonicFulp (beta:=beta) 2 b precision u
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨huCan, hbeta, by decide, hprecision, hvNum⟩
    have htwo_mul :
        (2 : ℝ) * (2 : ℝ) ^ (-b.dExp) = (2 : ℝ) ^ (-b.dExp + 1) := by
      have htwo_ne : (2 : ℝ) ≠ 0 := by norm_num
      calc
        (2 : ℝ) * (2 : ℝ) ^ (-b.dExp)
            = (2 : ℝ) ^ (-b.dExp) * (2 : ℝ) ^ (1 : Int) := by
                norm_num [mul_comm]
        _ = (2 : ℝ) ^ (-b.dExp + 1) := by
                rw [← zpow_add₀ htwo_ne]
    rw [huUlp, htwo_mul]
    exact zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2) huExp
  have hperturb' :
      |y1 - Y| + |a1 * x1 - A| < (1 / 4 : ℝ) * ulp := by
    dsimp [A, Y, ulp] at hperturb ⊢
    simpa [abs_sub_comm, add_comm] using hperturb
  have hdist_tri :
      |Z - U| ≤
        |y1 - Y| + |a1 * x1 - A| + (|A - T| + |S - U|) := by
    have hdecomp :
        Z - U =
          (y1 - Y) + (a1 * x1 - A) + ((A - T) + (S - U)) := by
      dsimp [Z, S]
      ring
    rw [hdecomp]
    calc
      |(y1 - Y) + (a1 * x1 - A) + ((A - T) + (S - U))|
          ≤ |(y1 - Y) + (a1 * x1 - A)| +
              |(A - T) + (S - U)| := by
                simpa [add_assoc] using
                  abs_add_le ((y1 - Y) + (a1 * x1 - A))
                    ((A - T) + (S - U))
      _ ≤ (|y1 - Y| + |a1 * x1 - A|) +
              (|A - T| + |S - U|) := by
                exact add_le_add (abs_add_le (y1 - Y) (a1 * x1 - A))
                  (abs_add_le (A - T) (S - U))
      _ = |y1 - Y| + |a1 * x1 - A| + (|A - T| + |S - U|) := by
                ring
  have hsum_exact_of_le (hS_le_U : S ≤ U) : S = U := by
    have hdist_le_boundary : |S - U| ≤ (2 : ℝ) ^ (-b.dExp) := by
      rw [← hpredUlp]
      nlinarith
    have hdist_lt_boundary : |S - U| < (2 : ℝ) ^ (-b.dExp) := by
      by_contra hnot
      have hge : (2 : ℝ) ^ (-b.dExp) ≤ |S - U| := le_of_not_gt hnot
      have hdist_eq : |S - U| = (2 : ℝ) ^ (-b.dExp) :=
        le_antisymm hdist_le_boundary hge
      have hSU_nonpos : S - U ≤ 0 := by linarith
      have hUS_eq : U - S = (2 : ℝ) ^ (-b.dExp) := by
        have habs : |S - U| = U - S := by
          rw [abs_of_nonpos hSU_nonpos]
          ring
        linarith
      have hpred_val : _root_.F2R (beta:=beta) pred = S := by
        rw [hpredUlp] at hpred_step
        linarith
      have hclosest_pred :
          |U - S| ≤ |_root_.F2R (beta:=beta) pred - S| := by
        have h := huClosest.2 pred hpredBound
        simpa [U] using h
      have hzero : |U - S| ≤ 0 := by
        simpa [hpred_val] using hclosest_pred
      have hUS_abs_pos : 0 < |U - S| := by
        rw [abs_of_nonneg (by linarith : 0 ≤ U - S)]
        linarith
      linarith
    have hsumExact :
        _root_.F2R (beta:=beta) u =
          _root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) y := by
      subst beta
      exact bounded_sum_eq_of_lt_min b t y u htBound hyBound huBound
        (by simpa [S, T, Y, U] using hdist_lt_boundary)
    simpa [S, T, Y, U] using hsumExact.symm
  by_cases hU_le_Z : U ≤ Z
  · have hhalf_u : |S - U| ≤
        (1 / 2 : ℝ) * Fulp (beta:=beta) b 2 precision u := by
      nlinarith
    have hdist_lt_u :
        |Z - U| < Fulp (beta:=beta) b 2 precision u := by
      have hmain :
          |Z - U| <
            (1 / 4 : ℝ) * ulp + ((1 / 2 : ℝ) * ulp +
              (1 / 2 : ℝ) * Fulp (beta:=beta) b 2 precision u) := by
        nlinarith
      have hthree :
          (1 / 4 : ℝ) * ulp + (1 / 2 : ℝ) * ulp ≤
            (1 / 2 : ℝ) * Fulp (beta:=beta) b 2 precision u := by
        rw [hpredUlp]
        nlinarith
      nlinarith
    have h := MinOrMax2 (beta:=beta) b 2 precision Z u
    have hdist_lt_u' :
        |Z - _root_.F2R (beta:=beta) u| <
          Fulp (beta:=beta) b 2 precision u := by
      simpa [U] using hdist_lt_u
    have hU_le_Z' : _root_.F2R (beta:=beta) u ≤ Z := by
      simpa [U] using hU_le_Z
    have hres : MinOrMax (beta:=beta) b 2 Z u := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinOrMax2_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨huBound, huCan, huPos, hdist_lt_u', hU_le_Z',
          hbeta, by decide, hprecision, hvNum⟩
    simpa [Z] using hres
  · have hZ_lt_U : Z < U := lt_of_not_ge hU_le_Z
    by_cases hS_le_U : S ≤ U
    · have hsum_eq : S = U := hsum_exact_of_le hS_le_U
      have huEq : U = T + Y := by
        dsimp [S] at hsum_eq
        exact hsum_eq.symm
      have h := Axpy_aux2 (beta:=beta) b precision a x y t u a1 x1 y1
      have hres : MinOrMax (beta:=beta) b 2 Z u := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Axpy_aux2_check,
          Id.run, ULift.up_down, Z, T, Y, U, Int.cast_ofNat] using
          h ⟨huCan, htSub, huPos, by simpa [T, Y, U] using huEq,
            hperturb, htClosest, hbeta, hprecision_gt, hvNum⟩
      simpa [Z] using hres
    · have hU_lt_S : U < S := lt_of_not_ge hS_le_U
      have hdist_lt_pred : |Z - U| < ulp := by
        have hZU_nonpos : Z - U ≤ 0 := by linarith
        rw [abs_of_nonpos hZU_nonpos]
        have herr1 :
            (Y - y1) + (A - a1 * x1) ≤
              |y1 - Y| + |a1 * x1 - A| := by
          have h :=
            abs_add_le (Y - y1) (A - a1 * x1)
          have habs_nonneg :
              (Y - y1) + (A - a1 * x1) ≤
                |(Y - y1) + (A - a1 * x1)| :=
            le_abs_self _
          have habs_le :
              |(Y - y1) + (A - a1 * x1)| ≤
                |Y - y1| + |A - a1 * x1| := h
          nlinarith [abs_sub_comm y1 Y, abs_sub_comm (a1 * x1) A]
        have hdecomp :
            U - Z =
              (U - S) + (T - A) + ((Y - y1) + (A - a1 * x1)) := by
          dsimp [Z, S]
          ring
        have hT_A : T - A ≤ |A - T| := by
          have h := neg_le_abs (A - T)
          linarith
        have hupper :
            U - Z <
              0 + ((1 / 2 : ℝ) * ulp) + ((1 / 4 : ℝ) * ulp) := by
          rw [hdecomp]
          have hUS : U - S < 0 := by linarith
          nlinarith
        nlinarith
      have h := MinOrMax1 (beta:=beta) b 2 precision Z u
      have hdist_lt_pred' :
          |Z - _root_.F2R (beta:=beta) u| <
            Fulp (beta:=beta) b 2 precision
              (FPred (beta:=beta) b 2 precision u) := by
        simpa [Z, U, pred, ulp] using hdist_lt_pred
      have hres : MinOrMax (beta:=beta) b 2 Z u := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinOrMax1_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          h ⟨huBound, huCan, huPos, hdist_lt_pred',
            hbeta, by decide, hprecision, hvNum⟩
      simpa [Z] using hres

/-- Coq: `AxpyPos`, the positive `u` Axpy min/max reduction.

The upstream proof splits the canonical `t` case.  Normal `t` uses
`Axpy_aux1_aux1` followed by `Axpy_aux1`; subnormal `t` uses either the
boundary predecessor branch `Axpy_aux3` or the larger predecessor exponent
estimate `Axpy_aux1_aux3` followed by `Axpy_aux1`. -/
theorem AxpyPos {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (a x y t u : FloatSpec.Core.Defs.FlocqFloat beta)
    (a1 x1 y1 : ℝ) :
    ⦃⌜Fcanonic (beta:=beta) 2 b u ∧
        Fcanonic (beta:=beta) 2 b t ∧
        Fbounded (beta:=beta) b y ∧
        0 < _root_.F2R (beta:=beta) u ∧
        (4 : ℝ) * |_root_.F2R (beta:=beta) t| ≤
          |_root_.F2R (beta:=beta) u| ∧
        |_root_.F2R (beta:=beta) y - y1| +
          |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
            a1 * x1| <
          (1 / 4 : ℝ) * Fulp (beta:=beta) b 2 precision
            (FPred (beta:=beta) b 2 precision u) ∧
        Closest (beta:=beta) b (2 : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) t ∧
        Closest (beta:=beta) b (2 : ℝ)
          (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) y) u ∧
        beta = 2 ∧ 1 < precision ∧ b.vNum = Zpower_nat 2 precision⌝⦄
    (pure (AxpyPos_check (beta:=beta) b precision a x y t u a1 x1 y1) :
      Id Unit)
    ⦃⇓_ => ⌜MinOrMax (beta:=beta) b 2 (a1 * x1 + y1) u⌝⦄ := by
  intro h
  rcases h with
    ⟨huCan, htCan, hyBound, huPos, htScale, hperturb, htClosest,
      huClosest, hbeta, hprecision_gt, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, AxpyPos_check, Id.run, ULift.up_down]
  have hprecision : precision ≠ 0 := by omega
  have huBound : Fbounded (beta:=beta) b u := by
    have h := FcanonicBound (beta:=beta) 2 b u
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicBound_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h huCan
  have hpredBound :
      Fbounded (beta:=beta) b (FPred (beta:=beta) b 2 precision u) := by
    have h := FBoundedPred (beta:=beta) b 2 precision u
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FBoundedPred_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨huBound, by decide, hprecision, hvNum⟩
  rcases htCan with htNorm | htSub
  · have hblop :
        |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
            _root_.F2R (beta:=beta) t| ≤
          (1 / 4 : ℝ) * Fulp (beta:=beta) b 2 precision
            (FPred (beta:=beta) b 2 precision u) := by
      have h := Axpy_aux1_aux1 (beta:=beta) b precision a x t u
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Axpy_aux1_aux1_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨htNorm, huCan, huPos, htScale, htClosest, hbeta, hprecision_gt, hvNum⟩
    have h := Axpy_aux1 (beta:=beta) b precision a x y t u a1 x1 y1
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Axpy_aux1_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨huCan, hblop, huPos, htScale, hperturb, huClosest,
        hbeta, hprecision_gt, hvNum⟩
  · rcases eq_or_lt_of_le huBound.2 with huExpEq | huExpLt
    · have huUlp :
          Fulp (beta:=beta) b 2 precision u = (2 : ℝ) ^ (-b.dExp) := by
        have h := CanonicFulp (beta:=beta) 2 b precision u
        have h' :
            Fulp (beta:=beta) b 2 precision u = (2 : ℝ) ^ u.Fexp := by
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using
            h ⟨huCan, hbeta, by decide, hprecision, hvNum⟩
        rw [h', ← huExpEq]
      have huClosestUlp :
          2 * |_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) y -
              _root_.F2R (beta:=beta) u| ≤
            Fulp (beta:=beta) b 2 precision u := by
        have h := ClosestUlp (beta:=beta) b 2 precision
          (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) y) u
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          h ⟨huClosest, hbeta, by decide, hprecision, hvNum⟩
      have hboundaryPos : 0 < (2 : ℝ) ^ (-b.dExp) :=
        zpow_pos (by norm_num) _
      have hsumSmall :
          |_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) y -
              _root_.F2R (beta:=beta) u| < (2 : ℝ) ^ (-b.dExp) := by
        rw [huUlp] at huClosestUlp
        nlinarith
      subst beta
      have huEq :
          _root_.F2R (beta:=2) u =
            _root_.F2R (beta:=2) t + _root_.F2R (beta:=2) y :=
        bounded_sum_eq_of_lt_min b t y u htSub.1 hyBound huBound hsumSmall
      have h := Axpy_aux2 (beta:=2) b precision a x y t u a1 x1 y1
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Axpy_aux2_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨huCan, htSub, huPos, huEq, hperturb, htClosest,
          rfl, hprecision_gt, hvNum⟩
    · have huExpHigh : -b.dExp + 1 ≤ u.Fexp := by omega
      rcases eq_or_lt_of_le hpredBound.2 with hpredExpEq | hpredExpLt
      · have h := Axpy_aux3 (beta:=beta) b precision a x y t u a1 x1 y1
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Axpy_aux3_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          h ⟨huCan, htSub, hyBound, huPos, hpredExpEq.symm, huExpHigh,
            hperturb, htClosest, huClosest, hbeta, hprecision_gt, hvNum⟩
      · have hpredHigh :
            -b.dExp + 1 ≤ (FPred (beta:=beta) b 2 precision u).Fexp := by
          omega
        have hblop :
          |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
              _root_.F2R (beta:=beta) t| ≤
            (1 / 4 : ℝ) * Fulp (beta:=beta) b 2 precision
              (FPred (beta:=beta) b 2 precision u) := by
          have h := Axpy_aux1_aux3 (beta:=beta) b precision a x t u
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Axpy_aux1_aux3_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using
            h ⟨htSub, huCan, huPos, hpredHigh, htClosest,
              hbeta, hprecision_gt, hvNum⟩
        have h := Axpy_aux1 (beta:=beta) b precision a x y t u a1 x1 y1
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Axpy_aux1_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          h ⟨huCan, hblop, huPos, htScale, hperturb, huClosest,
            hbeta, hprecision_gt, hvNum⟩

/-- Nonzero helper for Coq `Axpy_tFlessu`.

This restores the signed dispatch around `AxpyPos` when `u` is nonzero.
The public `Axpy_tFlessu` below adds the zero branch, matching the upstream
case split while keeping this reusable nonzero proof factored out. -/
theorem Axpy_tFlessu_nonzero {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (a x y t u : FloatSpec.Core.Defs.FlocqFloat beta)
    (a1 x1 y1 : ℝ) :
    ⦃⌜Fbounded (beta:=beta) b a ∧
        Fbounded (beta:=beta) b x ∧
        Fbounded (beta:=beta) b y ∧
        Fbounded (beta:=beta) b t ∧
        Fbounded (beta:=beta) b u ∧
        Closest (beta:=beta) b (2 : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) t ∧
        Closest (beta:=beta) b (2 : ℝ)
          (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) y) u ∧
        Fcanonic (beta:=beta) 2 b u ∧
        Fcanonic (beta:=beta) 2 b t ∧
        (4 : ℝ) * |_root_.F2R (beta:=beta) t| ≤
          |_root_.F2R (beta:=beta) u| ∧
        |_root_.F2R (beta:=beta) y - y1| +
          |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
            a1 * x1| <
          (1 / 4 : ℝ) * Fulp (beta:=beta) b 2 precision
            (FLess (beta:=beta) b 2 precision u) ∧
        _root_.F2R (beta:=beta) u ≠ 0 ∧
        beta = 2 ∧ 1 < precision ∧ b.vNum = Zpower_nat 2 precision⌝⦄
    (pure (Axpy_tFlessu_check (beta:=beta) b precision a x y t u a1 x1 y1) :
      Id Unit)
    ⦃⇓_ => ⌜MinOrMax (beta:=beta) b 2 (a1 * x1 + y1) u⌝⦄ := by
  intro h
  rcases h with
    ⟨haBound, _hxBound, hyBound, _htBound, _huBound, htClosest, huClosest,
      huCan, htCan, htScale, hperturb, huNonzero,
      hbeta, hprecision_gt, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, Axpy_tFlessu_check,
    Id.run, ULift.up_down]
  have hF2R_opp (q : FloatSpec.Core.Defs.FlocqFloat beta) :
      _root_.F2R (beta:=beta) (Fopp (beta:=beta) q) =
        -_root_.F2R (beta:=beta) q := by
    have h := FloatSpec.Calc.Operations.F2R_opp (beta:=beta) q
    simpa [Fopp, _root_.F2R] using h trivial
  by_cases huNeg : _root_.F2R (beta:=beta) u < 0
  · have haOppBound : Fbounded (beta:=beta) b (Fopp (beta:=beta) a) := by
      have h := oppBounded (beta:=beta) b a
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, oppBounded_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h haBound
    have hyOppBound : Fbounded (beta:=beta) b (Fopp (beta:=beta) y) := by
      have h := oppBounded (beta:=beta) b y
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, oppBounded_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h hyBound
    have htOppCan : Fcanonic (beta:=beta) 2 b (Fopp (beta:=beta) t) := by
      have h := FcanonicFopp (beta:=beta) 2 b t
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFopp_check, Int.cast_ofNat] using h htCan
    have huOppCan : Fcanonic (beta:=beta) 2 b (Fopp (beta:=beta) u) := by
      have h := FcanonicFopp (beta:=beta) 2 b u
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFopp_check, Int.cast_ofNat] using h huCan
    have huOppPos : 0 < _root_.F2R (beta:=beta) (Fopp (beta:=beta) u) := by
      rw [hF2R_opp]
      exact neg_pos.mpr huNeg
    have htOppClosest :
        Closest (beta:=beta) b (2 : ℝ)
          (_root_.F2R (beta:=beta) (Fopp (beta:=beta) a) *
            _root_.F2R (beta:=beta) x) (Fopp (beta:=beta) t) := by
      have h := ClosestOpp (beta:=beta) b (2 : ℝ) t
        (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x)
      have hopp :
          Closest (beta:=beta) b (2 : ℝ)
            (-(_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x))
            (Fopp (beta:=beta) t) := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestOpp_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h htClosest
      simpa [hF2R_opp, neg_mul] using hopp
    have huOppClosest :
        Closest (beta:=beta) b (2 : ℝ)
          (_root_.F2R (beta:=beta) (Fopp (beta:=beta) t) +
            _root_.F2R (beta:=beta) (Fopp (beta:=beta) y))
          (Fopp (beta:=beta) u) := by
      have h := ClosestOpp (beta:=beta) b (2 : ℝ) u
        (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) y)
      have hopp :
          Closest (beta:=beta) b (2 : ℝ)
            (-(_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) y))
            (Fopp (beta:=beta) u) := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestOpp_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h huClosest
      simpa [hF2R_opp, neg_add, add_comm] using hopp
    have hscaleOpp :
        (4 : ℝ) * |_root_.F2R (beta:=beta) (Fopp (beta:=beta) t)| ≤
          |_root_.F2R (beta:=beta) (Fopp (beta:=beta) u)| := by
      simpa [hF2R_opp, abs_neg] using htScale
    have hFulp_Fopp (q : FloatSpec.Core.Defs.FlocqFloat beta) :
        Fulp (beta:=beta) b 2 precision (Fopp (beta:=beta) q) =
          Fulp (beta:=beta) b 2 precision q := by
      have hnorm := Fnormalize_Fopp (beta:=beta) 2 b precision q
      have hnorm' :
          Fnormalize (beta:=beta) 2 b precision (Fopp (beta:=beta) q) =
            Fopp (beta:=beta) (Fnormalize (beta:=beta) 2 b precision q) := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fnormalize_Fopp_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using hnorm True.intro
      calc
        Fulp (beta:=beta) b 2 precision (Fopp (beta:=beta) q)
            = (2 : ℝ) ^
                (Fnormalize (beta:=beta) 2 b precision
                  (Fopp (beta:=beta) q)).Fexp := rfl
        _ = (2 : ℝ) ^
                (Fopp (beta:=beta)
                  (Fnormalize (beta:=beta) 2 b precision q)).Fexp := by
              rw [hnorm']
        _ = Fulp (beta:=beta) b 2 precision q := by
              simp [Fulp, Fopp, FloatSpec.Calc.Operations.Fopp]
    have hpred_dual :
        FPred (beta:=beta) b 2 precision (Fopp (beta:=beta) u) =
          Fopp (beta:=beta) (FSucc (beta:=beta) b 2 precision u) := by
      have h := FPredFopFSucc (beta:=beta) b 2 precision (Fopp (beta:=beta) u)
      have h' :
          FPred (beta:=beta) b 2 precision (Fopp (beta:=beta) u) =
            Fopp (beta:=beta)
              (FSucc (beta:=beta) b 2 precision
                (Fopp (beta:=beta) (Fopp (beta:=beta) u))) := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FPredFopFSucc_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          h ⟨by decide, (by omega : precision ≠ 0), hvNum⟩
      have hfopp2 :
          Fopp (beta:=beta) (Fopp (beta:=beta) u) = u := by
        have hff := Fopp_Fopp (beta:=beta) u
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fopp_Fopp_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using hff True.intro
      simpa [hfopp2] using h'
    have hperturbOpp :
        |_root_.F2R (beta:=beta) (Fopp (beta:=beta) y) - (-y1)| +
          |_root_.F2R (beta:=beta) (Fopp (beta:=beta) a) *
              _root_.F2R (beta:=beta) x - (-a1) * x1| <
          (1 / 4 : ℝ) * Fulp (beta:=beta) b 2 precision
            (FPred (beta:=beta) b 2 precision (Fopp (beta:=beta) u)) := by
      have hless :
          FLess (beta:=beta) b 2 precision u =
            FSucc (beta:=beta) b 2 precision u := by
        simp [FLess, huNeg]
      have hperturbSucc :
          |_root_.F2R (beta:=beta) y - y1| +
            |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
              a1 * x1| <
            (1 / 4 : ℝ) * Fulp (beta:=beta) b 2 precision
              (FSucc (beta:=beta) b 2 precision u) := by
        simpa [hless] using hperturb
      have hperturbSuccSym :
          |y1 - _root_.F2R (beta:=beta) y| +
            |a1 * x1 -
              _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x| <
            (1 / 4 : ℝ) * Fulp (beta:=beta) b 2 precision
              (FSucc (beta:=beta) b 2 precision u) := by
        simpa [abs_sub_comm] using hperturbSucc
      rw [hpred_dual, hFulp_Fopp]
      simpa [hF2R_opp, abs_neg, sub_eq_add_neg, add_comm, add_left_comm,
        add_assoc, mul_comm, mul_left_comm, mul_assoc] using hperturbSuccSym
    have hneg := AxpyPos (beta:=beta) b precision
      (Fopp (beta:=beta) a) x (Fopp (beta:=beta) y)
      (Fopp (beta:=beta) t) (Fopp (beta:=beta) u) (-a1) x1 (-y1)
    have hnegRes :
        MinOrMax (beta:=beta) b 2 ((-a1) * x1 + (-y1))
          (Fopp (beta:=beta) u) := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, AxpyPos_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        hneg ⟨huOppCan, htOppCan, hyOppBound, huOppPos, hscaleOpp,
          hperturbOpp, htOppClosest, huOppClosest, hbeta,
          hprecision_gt, hvNum⟩
    have hnegTarget :
        MinOrMax (beta:=beta) b 2 (-(a1 * x1 + y1))
          (Fopp (beta:=beta) u) := by
      simpa [neg_mul, sub_eq_add_neg, add_comm, add_left_comm, add_assoc] using hnegRes
    have h := MinOrMax_Fopp (beta:=beta) b 2 (a1 * x1 + y1) u
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinOrMax_Fopp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hnegTarget
  · have huPos : 0 < _root_.F2R (beta:=beta) u := by
      have hnonneg : 0 ≤ _root_.F2R (beta:=beta) u := le_of_not_gt huNeg
      exact lt_of_le_of_ne hnonneg (Ne.symm huNonzero)
    have hless :
        FLess (beta:=beta) b 2 precision u =
          FPred (beta:=beta) b 2 precision u := by
      simp [FLess, huNeg]
    have hperturbPred :
        |_root_.F2R (beta:=beta) y - y1| +
          |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
            a1 * x1| <
          (1 / 4 : ℝ) * Fulp (beta:=beta) b 2 precision
            (FPred (beta:=beta) b 2 precision u) := by
      simpa [hless] using hperturb
    have hpos := AxpyPos (beta:=beta) b precision a x y t u a1 x1 y1
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, AxpyPos_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
      hpos ⟨huCan, htCan, hyBound, huPos, htScale, hperturbPred,
        htClosest, huClosest, hbeta, hprecision_gt, hvNum⟩

/-- Zero-valued branch of Coq `Axpy_tFlessu`.

When `u` is zero, the scale hypothesis forces `t` to be zero.  Since `u` is
closest to `t + y`, the bounded candidate `y` then has distance zero and hence
also represents zero.  The perturbation bound plus `ClosestUlp` for `t` gives
the small-distance hypothesis required by `MinOrMax3`. -/
theorem Axpy_tFlessu_zero {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (a x y t u : FloatSpec.Core.Defs.FlocqFloat beta)
    (a1 x1 y1 : ℝ) :
    ⦃⌜Fbounded (beta:=beta) b y ∧
        Fbounded (beta:=beta) b u ∧
        Closest (beta:=beta) b (2 : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) t ∧
        Closest (beta:=beta) b (2 : ℝ)
          (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) y) u ∧
        Fcanonic (beta:=beta) 2 b u ∧
        Fcanonic (beta:=beta) 2 b t ∧
        (4 : ℝ) * |_root_.F2R (beta:=beta) t| ≤
          |_root_.F2R (beta:=beta) u| ∧
        |_root_.F2R (beta:=beta) y - y1| +
          |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
            a1 * x1| <
          (1 / 4 : ℝ) * Fulp (beta:=beta) b 2 precision
            (FPred (beta:=beta) b 2 precision u) ∧
        _root_.F2R (beta:=beta) u = 0 ∧
        beta = 2 ∧ 1 < precision ∧ b.vNum = Zpower_nat 2 precision⌝⦄
    (pure (Axpy_tFlessu_zero_check (beta:=beta) b precision a x y t u a1 x1 y1) :
      Id Unit)
    ⦃⇓_ => ⌜MinOrMax (beta:=beta) b 2 (a1 * x1 + y1) u⌝⦄ := by
  intro h
  rcases h with
    ⟨hyBound, huBound, htClosest, huClosest, huCan, htCan, htScale,
      hperturb, huZero, hbeta, hprecision_gt, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, Axpy_tFlessu_zero_check,
    Id.run, ULift.up_down]
  let A : ℝ := _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x
  let T : ℝ := _root_.F2R (beta:=beta) t
  let Y : ℝ := _root_.F2R (beta:=beta) y
  let U : ℝ := _root_.F2R (beta:=beta) u
  let pred : FloatSpec.Core.Defs.FlocqFloat beta :=
    FPred (beta:=beta) b 2 precision u
  have hprecision_ne : precision ≠ 0 := by omega
  have hvNumPos : 0 < b.vNum := by
    rw [hvNum, Zpower_nat]
    exact pow_pos (by decide : (0 : Int) < 2) precision
  have htAbsZero : |T| = 0 := by
    have hscale_zero : (4 : ℝ) * |T| ≤ 0 := by
      simpa [T, U, huZero] using htScale
    have hnonneg : 0 ≤ |T| := abs_nonneg _
    nlinarith
  have htZero : T = 0 := abs_eq_zero.mp htAbsZero
  have hyZero : Y = 0 := by
    have hdist := huClosest.2 y hyBound
    have hY_le_zero : |Y| ≤ 0 := by
      simpa [T, U, Y, htZero, huZero, sub_eq_add_neg, add_comm,
        add_left_comm, add_assoc] using hdist
    exact abs_eq_zero.mp (le_antisymm hY_le_zero (abs_nonneg Y))
  have htFulp_le_pred :
      Fulp (beta:=beta) b 2 precision t ≤
        Fulp (beta:=beta) b 2 precision pred := by
    have htZeroFloat : t = Fzero beta (-b.dExp) := by
      have h := FcanonicZeroEq (beta:=beta) b 2 t
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicZeroEq_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨htCan, by simpa [T] using htZero, hbeta, by decide, hvNumPos⟩
    have htFulp :
        Fulp (beta:=beta) b 2 precision t = (2 : ℝ) ^ (-b.dExp) := by
      have h := CanonicFulp (beta:=beta) 2 b precision t
      have h' :
          Fulp (beta:=beta) b 2 precision t = (2 : ℝ) ^ t.Fexp := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          h ⟨htCan, hbeta, by decide, hprecision_ne, hvNum⟩
      rw [h', htZeroFloat]
      simp [Fzero]
    have hpredBound : Fbounded (beta:=beta) b pred := by
      have h := FBoundedPred (beta:=beta) b 2 precision u
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FBoundedPred_check,
        Id.run, ULift.up_down, pred, Int.cast_ofNat] using
        h ⟨huBound, by decide, hprecision_ne, hvNum⟩
    have hnormBound :
        Fbounded (beta:=beta) b
          (Fnormalize (beta:=beta) 2 b precision pred) := by
      have h := FnormalizeBounded (beta:=beta) 2 b precision pred
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeBounded_check,
        Id.run, ULift.up_down, Fbounded', Int.cast_ofNat] using
        h ⟨hpredBound, hpredBound, hprecision_ne, by decide, hvNum⟩
    have hpow_le :
        (2 : ℝ) ^ (-b.dExp) ≤
          (2 : ℝ) ^
            (Fnormalize (beta:=beta) 2 b precision pred).Fexp :=
      zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2) hnormBound.2
    calc
      Fulp (beta:=beta) b 2 precision t
          = (2 : ℝ) ^ (-b.dExp) := htFulp
      _ ≤ (2 : ℝ) ^
          (Fnormalize (beta:=beta) 2 b precision pred).Fexp := hpow_le
      _ = Fulp (beta:=beta) b 2 precision pred := by
          simp [Fulp, pred]
  have hA_half_pred :
      |A| ≤ (1 / 2 : ℝ) * Fulp (beta:=beta) b 2 precision pred := by
    have htClosestUlp :
        2 * |A - T| ≤ Fulp (beta:=beta) b 2 precision t := by
      have h := ClosestUlp (beta:=beta) b 2 precision A t
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
        Id.run, ULift.up_down, A, Int.cast_ofNat] using
        h ⟨htClosest, hbeta, by decide, hprecision_ne, hvNum⟩
    have hA_half_t : |A| ≤ (1 / 2 : ℝ) * Fulp (beta:=beta) b 2 precision t := by
      have hAT : |A - T| = |A| := by simp [T, htZero]
      nlinarith
    exact le_trans hA_half_t
      (mul_le_mul_of_nonneg_left htFulp_le_pred (by norm_num : 0 ≤ (1 / 2 : ℝ)))
  have hpredFulp_pos :
      0 < Fulp (beta:=beta) b 2 precision pred := by
    dsimp [Fulp]
    exact zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hperturb_sym :
      |y1 - Y| + |a1 * x1 - A| <
        (1 / 4 : ℝ) * Fulp (beta:=beta) b 2 precision pred := by
    simpa [A, Y, pred, abs_sub_comm] using hperturb
  have hdist :
      |(a1 * x1 + y1) - U| <
        Fulp (beta:=beta) b 2 precision pred := by
    have htri :
        |(a1 * x1 + y1) - U| ≤ |y1 - Y| + |a1 * x1 - A| + |A| := by
      have htarget :
          (a1 * x1 + y1) - U = (y1 - Y) + (a1 * x1 - A) + A := by
        dsimp [A, Y, U] at *
        rw [huZero, hyZero]
        ring
      calc
        |(a1 * x1 + y1) - U|
            = |(y1 - Y) + (a1 * x1 - A) + A| := by rw [htarget]
        _ ≤ |(y1 - Y) + (a1 * x1 - A)| + |A| := abs_add_le _ _
        _ ≤ |y1 - Y| + |a1 * x1 - A| + |A| := by
          have h := abs_add_le (y1 - Y) (a1 * x1 - A)
          linarith
    nlinarith
  have h := MinOrMax3 (beta:=beta) b 2 precision (a1 * x1 + y1) u
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinOrMax3_check,
    Id.run, ULift.up_down, pred, U, Int.cast_ofNat] using
    h ⟨huBound, huCan, huZero, by simpa [U, pred] using hdist,
      hbeta, by decide, hprecision_ne, hvNum⟩

/-- Coq: `Axpy_tFlessu`, signed dispatch around `AxpyPos`.

Negative and positive nonzero branches use `Axpy_tFlessu_nonzero`; the
zero branch is the explicit `Axpy_tFlessu_zero` proof above. -/
theorem Axpy_tFlessu {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (a x y t u : FloatSpec.Core.Defs.FlocqFloat beta)
    (a1 x1 y1 : ℝ) :
    ⦃⌜Fbounded (beta:=beta) b a ∧
        Fbounded (beta:=beta) b x ∧
        Fbounded (beta:=beta) b y ∧
        Fbounded (beta:=beta) b t ∧
        Fbounded (beta:=beta) b u ∧
        Closest (beta:=beta) b (2 : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) t ∧
        Closest (beta:=beta) b (2 : ℝ)
          (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) y) u ∧
        Fcanonic (beta:=beta) 2 b u ∧
        Fcanonic (beta:=beta) 2 b t ∧
        (4 : ℝ) * |_root_.F2R (beta:=beta) t| ≤
          |_root_.F2R (beta:=beta) u| ∧
        |_root_.F2R (beta:=beta) y - y1| +
          |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
            a1 * x1| <
          (1 / 4 : ℝ) * Fulp (beta:=beta) b 2 precision
            (FLess (beta:=beta) b 2 precision u) ∧
        beta = 2 ∧ 1 < precision ∧ b.vNum = Zpower_nat 2 precision⌝⦄
    (pure (Axpy_tFlessu_check (beta:=beta) b precision a x y t u a1 x1 y1) :
      Id Unit)
    ⦃⇓_ => ⌜MinOrMax (beta:=beta) b 2 (a1 * x1 + y1) u⌝⦄ := by
  intro h
  rcases h with
    ⟨haBound, hxBound, hyBound, htBound, huBound, htClosest, huClosest,
      huCan, htCan, htScale, hperturb,
      hbeta, hprecision_gt, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, Axpy_tFlessu_check,
    Id.run, ULift.up_down]
  by_cases huZero : _root_.F2R (beta:=beta) u = 0
  · have hless :
        FLess (beta:=beta) b 2 precision u =
          FPred (beta:=beta) b 2 precision u := by
      simp [FLess, huZero]
    have hperturbPred :
        |_root_.F2R (beta:=beta) y - y1| +
          |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
            a1 * x1| <
          (1 / 4 : ℝ) * Fulp (beta:=beta) b 2 precision
            (FPred (beta:=beta) b 2 precision u) := by
      simpa [hless] using hperturb
    have hzero := Axpy_tFlessu_zero (beta:=beta) b precision a x y t u a1 x1 y1
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Axpy_tFlessu_zero_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      hzero ⟨hyBound, huBound, htClosest, huClosest, huCan, htCan, htScale,
        hperturbPred, huZero, hbeta, hprecision_gt, hvNum⟩
  · have hnonzero := Axpy_tFlessu_nonzero (beta:=beta) b precision
      a x y t u a1 x1 y1
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Axpy_tFlessu_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      hnonzero ⟨haBound, hxBound, hyBound, htBound, huBound, htClosest,
        huClosest, huCan, htCan, htScale, hperturb, huZero,
        hbeta, hprecision_gt, hvNum⟩

/-- Coq: `UlpFlessuGe_aux`.

The neighbor selected by `FLess` is at least `Fulp p` closer to zero in
absolute value.  The positive branch is the predecessor step; the negative
branch is its sign-dual through `Fopp`. -/
theorem UlpFlessuGe_aux {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b p ∧
        Fcanonic (beta:=beta) radix b p ∧
        beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (UlpFlessuGe_aux_check (beta:=beta) b radix precision p) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) p| -
        Fulp (beta:=beta) b radix precision p ≤
      |_root_.F2R (beta:=beta)
        (FLess (beta:=beta) b radix precision p)|⌝⦄ := by
  intro h
  rcases h with ⟨hpBound, hpCan, hbeta, hradix, hprecision, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, UlpFlessuGe_aux_check,
    Id.run, ULift.up_down]
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (by omega : (0 : Int) < radix)
  have hctx : 1 < radix ∧ precision ≠ 0 ∧
      b.vNum = Zpower_nat radix precision := ⟨hradix, hprecision, hvNum⟩
  have hFulp_nonneg (q : FloatSpec.Core.Defs.FlocqFloat beta) :
      0 ≤ Fulp (beta:=beta) b radix precision q := by
    dsimp [Fulp]
    exact le_of_lt (zpow_pos hradix_pos _)
  have hFulp_Fopp (q : FloatSpec.Core.Defs.FlocqFloat beta) :
      Fulp (beta:=beta) b radix precision (Fopp (beta:=beta) q) =
        Fulp (beta:=beta) b radix precision q := by
    have hnorm := Fnormalize_Fopp (beta:=beta) radix b precision q
    have hnorm' :
        Fnormalize (beta:=beta) radix b precision (Fopp (beta:=beta) q) =
          Fopp (beta:=beta) (Fnormalize (beta:=beta) radix b precision q) := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fnormalize_Fopp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using hnorm True.intro
    calc
      Fulp (beta:=beta) b radix precision (Fopp (beta:=beta) q)
          = (radix : ℝ) ^
              (Fnormalize (beta:=beta) radix b precision
                (Fopp (beta:=beta) q)).Fexp := rfl
      _ = (radix : ℝ) ^
              (Fopp (beta:=beta)
                (Fnormalize (beta:=beta) radix b precision q)).Fexp := by
            rw [hnorm']
      _ = Fulp (beta:=beta) b radix precision q := by
            simp [Fulp, Fopp, FloatSpec.Calc.Operations.Fopp]
  have hF2R_opp (q : FloatSpec.Core.Defs.FlocqFloat beta) :
      _root_.F2R (beta:=beta) (Fopp (beta:=beta) q) =
        -_root_.F2R (beta:=beta) q := by
    have h := FloatSpec.Calc.Operations.F2R_opp (beta:=beta) q
    simpa [Fopp, _root_.F2R] using h trivial
  have hpos_case
      (q : FloatSpec.Core.Defs.FlocqFloat beta)
      (hqBound : Fbounded (beta:=beta) b q)
      (hqCan : Fcanonic (beta:=beta) radix b q)
      (hqPos : 0 < _root_.F2R (beta:=beta) q) :
      |_root_.F2R (beta:=beta) q| -
          Fulp (beta:=beta) b radix precision q ≤
        |_root_.F2R (beta:=beta)
          (FPred (beta:=beta) b radix precision q)| := by
    let pred := FPred (beta:=beta) b radix precision q
    have hpred_nonneg : 0 ≤ _root_.F2R (beta:=beta) pred := by
      have h := R0RltRlePred (beta:=beta) b radix precision q
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, R0RltRlePred_check,
        Id.run, ULift.up_down, pred, Int.cast_ofNat] using
        h ⟨hbeta, hradix, hprecision, hvNum, hqPos⟩
    have hpred_step :
        _root_.F2R (beta:=beta) pred +
          Fulp (beta:=beta) b radix precision pred =
        _root_.F2R (beta:=beta) q := by
      have h := FpredUlpPos (beta:=beta) radix b precision q
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FpredUlpPos_check,
        Id.run, ULift.up_down, pred, Int.cast_ofNat] using
        h ⟨hqCan, hqPos, hbeta, hradix, hprecision, hvNum⟩
    have hpred_ulp_le :
        Fulp (beta:=beta) b radix precision pred ≤
          Fulp (beta:=beta) b radix precision q := by
      have h := FulpFPredGePos (beta:=beta) radix b precision q
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpFPredGePos_check,
        Id.run, ULift.up_down, pred, Int.cast_ofNat] using
        h ⟨hqBound, hqCan, hqPos, hbeta, hradix, hprecision, hvNum⟩
    rw [abs_of_pos hqPos, abs_of_nonneg hpred_nonneg]
    linarith
  by_cases hpNeg : _root_.F2R (beta:=beta) p < 0
  · have hoppBound : Fbounded (beta:=beta) b (Fopp (beta:=beta) p) := by
      have h := oppBounded (beta:=beta) b p
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, oppBounded_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h hpBound
    have hoppCan : Fcanonic (beta:=beta) radix b (Fopp (beta:=beta) p) := by
      have h := FcanonicFopp (beta:=beta) radix b p
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFopp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h hpCan
    have hoppPos : 0 < _root_.F2R (beta:=beta) (Fopp (beta:=beta) p) := by
      rw [hF2R_opp]
      exact neg_pos.mpr hpNeg
    have hdual := hpos_case (Fopp (beta:=beta) p) hoppBound hoppCan hoppPos
    have hpred_dual :
        FPred (beta:=beta) b radix precision (Fopp (beta:=beta) p) =
          Fopp (beta:=beta) (FSucc (beta:=beta) b radix precision p) := by
      have h := FPredFopFSucc (beta:=beta) b radix precision
        (Fopp (beta:=beta) p)
      have h' :
          FPred (beta:=beta) b radix precision (Fopp (beta:=beta) p) =
            Fopp (beta:=beta)
              (FSucc (beta:=beta) b radix precision
                (Fopp (beta:=beta) (Fopp (beta:=beta) p))) := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FPredFopFSucc_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h hctx
      have hfopp2 :
          Fopp (beta:=beta) (Fopp (beta:=beta) p) = p := by
        have hff := Fopp_Fopp (beta:=beta) p
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fopp_Fopp_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using hff True.intro
      simpa [hfopp2] using h'
    have hdual' :
        |_root_.F2R (beta:=beta) p| -
            Fulp (beta:=beta) b radix precision p ≤
          |_root_.F2R (beta:=beta)
            (FSucc (beta:=beta) b radix precision p)| := by
      rw [← hFulp_Fopp p]
      have habs_opp :
          |_root_.F2R (beta:=beta) (Fopp (beta:=beta) p)| =
            |_root_.F2R (beta:=beta) p| := by
        rw [hF2R_opp, abs_neg]
      have hpred_abs :
          |_root_.F2R (beta:=beta)
              (FPred (beta:=beta) b radix precision (Fopp (beta:=beta) p))| =
            |_root_.F2R (beta:=beta)
              (FSucc (beta:=beta) b radix precision p)| := by
        rw [hpred_dual, hF2R_opp, abs_neg]
      simpa [habs_opp, hpred_abs] using hdual
    simpa [FLess, hpNeg] using hdual'
  · by_cases hpPos : 0 < _root_.F2R (beta:=beta) p
    · have hres := hpos_case p hpBound hpCan hpPos
      simpa [FLess, hpNeg] using hres
    · have hpZero : _root_.F2R (beta:=beta) p = 0 := by
        linarith
      have hnonneg : 0 ≤
          |_root_.F2R (beta:=beta)
            (FPred (beta:=beta) b radix precision p)| :=
        abs_nonneg _
      have hleft_nonpos :
          |_root_.F2R (beta:=beta) p| -
              Fulp (beta:=beta) b radix precision p ≤ 0 := by
        rw [hpZero, abs_zero]
        simpa using neg_nonpos.mpr (hFulp_nonneg p)
      have hres :
          |_root_.F2R (beta:=beta) p| -
              Fulp (beta:=beta) b radix precision p ≤
            |_root_.F2R (beta:=beta)
              (FPred (beta:=beta) b radix precision p)| :=
        le_trans hleft_nonpos hnonneg
      simpa [FLess, hpNeg] using hres

noncomputable def UlpFlessuGe_from_abs_sub_fulp_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) (E : ℝ) : Unit :=
  ()

/-- Coq `UlpFlessuGe`, composition of the auxiliary neighbor estimate with the
final scale step.

This packages the upstream transition from a bound by `|u| - Fulp u` to the
quarter-ulp bound on `FLess u`; the remaining `UlpFlessuGe` work is the
larger real-arithmetic reduction to this premise. -/
theorem UlpFlessuGe_from_abs_sub_fulp {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) (E : ℝ) :
    ⦃⌜Fbounded (beta:=beta) b p ∧
        Fcanonic (beta:=beta) radix b p ∧
        beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision ∧
        E ≤ (4 * ((radix : ℝ) ^ (precision : Int) - 1))⁻¹ *
          (|_root_.F2R (beta:=beta) p| -
            Fulp (beta:=beta) b radix precision p)⌝⦄
    (pure (UlpFlessuGe_from_abs_sub_fulp_check
      (beta:=beta) b radix precision p E) : Id Unit)
    ⦃⇓_ => ⌜E ≤ (1 / 4 : ℝ) *
        Fulp (beta:=beta) b radix precision
          (FLess (beta:=beta) b radix precision p)⌝⦄ := by
  intro h
  rcases h with ⟨hpBound, hpCan, hbeta, hradix, hprecision, hvNum, hE⟩
  simp only [wp, PostCond.noThrow, pure, UlpFlessuGe_from_abs_sub_fulp_check,
    Id.run, ULift.up_down]
  let D : ℝ := (radix : ℝ) ^ (precision : Int) - 1
  let less : FloatSpec.Core.Defs.FlocqFloat beta :=
    FLess (beta:=beta) b radix precision p
  have hradix_gt_one : (1 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradix
  have hprecision_pos : (0 : Int) < (precision : Int) := by
    exact_mod_cast Nat.pos_of_ne_zero hprecision
  have hD_pos : 0 < D := by
    have hpow_lt :
        (radix : ℝ) ^ (0 : Int) < (radix : ℝ) ^ (precision : Int) := by
      exact zpow_lt_zpow_right₀ hradix_gt_one hprecision_pos
    have hpow_gt_one : 1 < (radix : ℝ) ^ (precision : Int) := by
      simpa using hpow_lt
    exact sub_pos.mpr hpow_gt_one
  have hD_nonneg : 0 ≤ D := le_of_lt hD_pos
  have hscale_nonneg : 0 ≤ (4 * D)⁻¹ := by
    exact inv_nonneg.mpr (mul_nonneg (by norm_num : (0 : ℝ) ≤ 4) hD_nonneg)
  have haux :
      |_root_.F2R (beta:=beta) p| -
          Fulp (beta:=beta) b radix precision p ≤
        |_root_.F2R (beta:=beta) less| := by
    have h := UlpFlessuGe_aux (beta:=beta) b radix precision p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, UlpFlessuGe_aux_check,
      Id.run, ULift.up_down, less, Int.cast_ofNat] using
      h ⟨hpBound, hpCan, hbeta, hradix, hprecision, hvNum⟩
  have hscaled :
      (4 * D)⁻¹ *
          (|_root_.F2R (beta:=beta) p| -
            Fulp (beta:=beta) b radix precision p) ≤
        (4 * D)⁻¹ * |_root_.F2R (beta:=beta) less| :=
    mul_le_mul_of_nonneg_left haux hscale_nonneg
  have hE_less :
      E ≤ (4 * D)⁻¹ * |_root_.F2R (beta:=beta) less| :=
    le_trans (by simpa [D] using hE) hscaled
  have hfinal := UlpFlessuGe_final_scale (beta:=beta) b radix precision p E
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, UlpFlessuGe_final_scale_check,
    Id.run, ULift.up_down, Int.cast_ofNat] using
    hfinal ⟨hpBound, hbeta, hradix, hprecision, hvNum,
      by simpa [D, less] using hE_less⟩

noncomputable def UlpFlessuGe_from_general_fulp_bound_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) (E : ℝ) : Unit :=
  ()

/-- Coq `UlpFlessuGe`, `FulpLeGeneral` reduction step.

The upstream proof first replaces `Fulp u` by the general bound
`|u| * radix^(1 - precision) + radix^(-dExp)`, then delegates to the
`UlpFlessuGe_aux` estimate and the final scale step. -/
theorem UlpFlessuGe_from_general_fulp_bound {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) (E : ℝ) :
    ⦃⌜Fbounded (beta:=beta) b p ∧
        Fbounded' (beta:=beta) b p ∧
        Fcanonic (beta:=beta) radix b p ∧
        beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision ∧
        E ≤ (4 * ((radix : ℝ) ^ (precision : Int) - 1))⁻¹ *
          (|_root_.F2R (beta:=beta) p| -
            (|_root_.F2R (beta:=beta) p| *
              (radix : ℝ) ^ (1 - (precision : Int)) +
             (radix : ℝ) ^ (-b.dExp)))⌝⦄
    (pure (UlpFlessuGe_from_general_fulp_bound_check
      (beta:=beta) b radix precision p E) : Id Unit)
    ⦃⇓_ => ⌜E ≤ (1 / 4 : ℝ) *
        Fulp (beta:=beta) b radix precision
          (FLess (beta:=beta) b radix precision p)⌝⦄ := by
  intro h
  rcases h with
    ⟨hpBound, hpBound', hpCan, hbeta, hradix, hprecision, hvNum, hE⟩
  simp only [wp, PostCond.noThrow, pure,
    UlpFlessuGe_from_general_fulp_bound_check, Id.run, ULift.up_down]
  let D : ℝ := (radix : ℝ) ^ (precision : Int) - 1
  let A : ℝ := |_root_.F2R (beta:=beta) p| *
      (radix : ℝ) ^ (1 - (precision : Int)) +
    (radix : ℝ) ^ (-b.dExp)
  have hradix_gt_one : (1 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradix
  have hprecision_pos : (0 : Int) < (precision : Int) := by
    exact_mod_cast Nat.pos_of_ne_zero hprecision
  have hD_pos : 0 < D := by
    have hpow_lt :
        (radix : ℝ) ^ (0 : Int) < (radix : ℝ) ^ (precision : Int) := by
      exact zpow_lt_zpow_right₀ hradix_gt_one hprecision_pos
    have hpow_gt_one : 1 < (radix : ℝ) ^ (precision : Int) := by
      simpa using hpow_lt
    exact sub_pos.mpr hpow_gt_one
  have hD_nonneg : 0 ≤ D := le_of_lt hD_pos
  have hscale_nonneg : 0 ≤ (4 * D)⁻¹ := by
    exact inv_nonneg.mpr (mul_nonneg (by norm_num : (0 : ℝ) ≤ 4) hD_nonneg)
  have hfulp_le_A :
      Fulp (beta:=beta) b radix precision p ≤ A := by
    have h := FulpLeGeneral (beta:=beta) radix b precision p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpLeGeneral_check,
      Id.run, ULift.up_down, A, Int.cast_ofNat] using
      h ⟨hpBound, hpBound', hbeta, hradix, hprecision, hvNum⟩
  have hsub :
      |_root_.F2R (beta:=beta) p| - A ≤
        |_root_.F2R (beta:=beta) p| -
          Fulp (beta:=beta) b radix precision p := by
    linarith
  have hscaled :
      (4 * D)⁻¹ * (|_root_.F2R (beta:=beta) p| - A) ≤
        (4 * D)⁻¹ *
          (|_root_.F2R (beta:=beta) p| -
            Fulp (beta:=beta) b radix precision p) :=
    mul_le_mul_of_nonneg_left hsub hscale_nonneg
  have hE_abs :
      E ≤ (4 * D)⁻¹ *
          (|_root_.F2R (beta:=beta) p| -
            Fulp (beta:=beta) b radix precision p) :=
    le_trans (by simpa [D, A] using hE) hscaled
  have hprev := UlpFlessuGe_from_abs_sub_fulp (beta:=beta) b radix precision p E
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, UlpFlessuGe_from_abs_sub_fulp_check,
    Id.run, ULift.up_down, Int.cast_ofNat] using
    hprev ⟨hpBound, hpCan, hbeta, hradix, hprecision, hvNum,
      by simpa [D] using hE_abs⟩

noncomputable def UlpFlessuGe2_from_general_bound_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) (E : ℝ) : Unit :=
  ()

/-- Coq `UlpFlessuGe2`, final strict handoff after the coefficient estimate.

Once the large arithmetic expression is proved strictly below the general
`FulpLeGeneral` bound, the non-strict `UlpFlessuGe` reduction above gives the
strict quarter-ulp conclusion needed by `Axpy_opt`. -/
theorem UlpFlessuGe2_from_general_bound {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) (E : ℝ) :
    ⦃⌜Fbounded (beta:=beta) b p ∧
        Fbounded' (beta:=beta) b p ∧
        Fcanonic (beta:=beta) radix b p ∧
        beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision ∧
        E < (4 * ((radix : ℝ) ^ (precision : Int) - 1))⁻¹ *
          (|_root_.F2R (beta:=beta) p| -
            (|_root_.F2R (beta:=beta) p| *
              (radix : ℝ) ^ (1 - (precision : Int)) +
             (radix : ℝ) ^ (-b.dExp)))⌝⦄
    (pure (UlpFlessuGe2_from_general_bound_check
      (beta:=beta) b radix precision p E) : Id Unit)
    ⦃⇓_ => ⌜E < (1 / 4 : ℝ) *
        Fulp (beta:=beta) b radix precision
          (FLess (beta:=beta) b radix precision p)⌝⦄ := by
  intro h
  rcases h with
    ⟨hpBound, hpBound', hpCan, hbeta, hradix, hprecision, hvNum, hE⟩
  simp only [wp, PostCond.noThrow, pure,
    UlpFlessuGe2_from_general_bound_check, Id.run, ULift.up_down]
  let G : ℝ :=
    (4 * ((radix : ℝ) ^ (precision : Int) - 1))⁻¹ *
      (|_root_.F2R (beta:=beta) p| -
        (|_root_.F2R (beta:=beta) p| *
          (radix : ℝ) ^ (1 - (precision : Int)) +
         (radix : ℝ) ^ (-b.dExp)))
  have hG_le :
      G ≤ (1 / 4 : ℝ) *
        Fulp (beta:=beta) b radix precision
          (FLess (beta:=beta) b radix precision p) := by
    have hprev :=
      UlpFlessuGe_from_general_fulp_bound (beta:=beta) b radix precision p G
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      UlpFlessuGe_from_general_fulp_bound_check, Id.run, ULift.up_down, Int.cast_ofNat] using
      hprev ⟨hpBound, hpBound', hpCan, hbeta, hradix, hprecision, hvNum,
        by rfl⟩
  exact lt_of_lt_of_le (by simpa [G] using hE) hG_le

/-- Coq: `RoundLeGeneral`.

For the radix-2 Axpy setting, closestness bounds the rounding distance by one
half ulp.  Combining that with `FulpLeGeneral` and solving the resulting
linear inequality gives the upstream bound on the rounded value. -/
theorem RoundLeGeneral_internal {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) (z : ℝ) :
    ⦃⌜Fbounded (beta:=beta) b p ∧ Fbounded' (beta:=beta) b p ∧
        Closest (beta:=beta) b (2 : ℝ) z p ∧
        beta = 2 ∧ precision ≠ 0 ∧ b.vNum = Zpower_nat 2 precision⌝⦄
    (pure (RoundLeGeneral_check (beta:=beta) b precision p z) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) p| ≤
        |z| * (1 - (2 : ℝ) ^ (-(precision : Int)))⁻¹ +
        (2 : ℝ) ^ (-b.dExp - 1) *
          (1 - (2 : ℝ) ^ (-(precision : Int)))⁻¹⌝⦄ := by
  intro h
  rcases h with ⟨hpBound, hpBound', hClosest, hbeta, hprecision, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, RoundLeGeneral_check,
    Id.run, ULift.up_down]
  subst beta
  let pp : ℝ := _root_.F2R (beta:=2) p
  let eps : ℝ := (2 : ℝ) ^ (-(precision : Int))
  let den : ℝ := 1 - eps
  let c : ℝ := (2 : ℝ) ^ (-b.dExp - 1)
  have hprecision_pos : (0 : Int) < (precision : Int) := by
    exact_mod_cast Nat.pos_of_ne_zero hprecision
  have heps_lt_one : eps < 1 := by
    have hpow_lt :
        (2 : ℝ) ^ (-(precision : Int)) < (2 : ℝ) ^ (0 : Int) := by
      exact zpow_lt_zpow_right₀ (by norm_num : (1 : ℝ) < 2) (by omega)
    simpa [eps] using hpow_lt
  have hden_pos : 0 < den := by
    dsimp [den]
    linarith
  have hden_nonneg : 0 ≤ den := le_of_lt hden_pos
  have hden_inv_nonneg : 0 ≤ den⁻¹ := inv_nonneg.mpr hden_nonneg
  have hden_ne : den ≠ 0 := ne_of_gt hden_pos
  have hpow_half_one :
      (1 / 2 : ℝ) * (2 : ℝ) ^ (1 - (precision : Int)) = eps := by
    have htwo_ne : (2 : ℝ) ≠ 0 := by norm_num
    have hhalf : (1 / 2 : ℝ) = (2 : ℝ) ^ (-1 : Int) := by
      norm_num [zpow_neg_one]
    calc
      (1 / 2 : ℝ) * (2 : ℝ) ^ (1 - (precision : Int))
          = (2 : ℝ) ^ (-1 : Int) * (2 : ℝ) ^ (1 - (precision : Int)) := by
            rw [hhalf]
      _ = (2 : ℝ) ^ ((-1 : Int) + (1 - (precision : Int))) := by
            rw [← zpow_add₀ htwo_ne]
      _ = eps := by
            congr 1
            omega
  have hpow_half_exp :
      (1 / 2 : ℝ) * (2 : ℝ) ^ (-b.dExp) = c := by
    have htwo_ne : (2 : ℝ) ≠ 0 := by norm_num
    have hhalf : (1 / 2 : ℝ) = (2 : ℝ) ^ (-1 : Int) := by
      norm_num [zpow_neg_one]
    calc
      (1 / 2 : ℝ) * (2 : ℝ) ^ (-b.dExp)
          = (2 : ℝ) ^ (-1 : Int) * (2 : ℝ) ^ (-b.dExp) := by
            rw [hhalf]
      _ = (2 : ℝ) ^ ((-1 : Int) + (-b.dExp)) := by
            rw [← zpow_add₀ htwo_ne]
      _ = c := by
            congr 1
            omega
  have hclosest_ulp :
      2 * |z - pp| ≤ Fulp (beta:=2) b 2 precision p := by
    have h := ClosestUlp (beta:=2) b 2 precision z p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, pp, Int.cast_ofNat] using
      h ⟨hClosest, rfl, by decide, hprecision, hvNum⟩
  have hdist_half :
      |z - pp| ≤ (1 / 2 : ℝ) * Fulp (beta:=2) b 2 precision p := by
    nlinarith
  have hfulp :
      Fulp (beta:=2) b 2 precision p ≤
        |pp| * (2 : ℝ) ^ (1 - (precision : Int)) +
        (2 : ℝ) ^ (-b.dExp) := by
    have h := FulpLeGeneral (beta:=2) 2 b precision p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpLeGeneral_check,
      Id.run, ULift.up_down, pp, Int.cast_ofNat] using
      h ⟨hpBound, hpBound', rfl, by decide, hprecision, hvNum⟩
  have hhalf_fulp :
      (1 / 2 : ℝ) * Fulp (beta:=2) b 2 precision p ≤ |pp| * eps + c := by
    have hmul := mul_le_mul_of_nonneg_left hfulp (by norm_num : 0 ≤ (1 / 2 : ℝ))
    calc
      (1 / 2 : ℝ) * Fulp (beta:=2) b 2 precision p
          ≤ (1 / 2 : ℝ) *
              (|pp| * (2 : ℝ) ^ (1 - (precision : Int)) +
                (2 : ℝ) ^ (-b.dExp)) := hmul
      _ = |pp| * eps + c := by
            rw [mul_add]
            calc
              (1 / 2 : ℝ) * (|pp| * (2 : ℝ) ^ (1 - (precision : Int))) +
                    1 / 2 * (2 : ℝ) ^ (-b.dExp)
                  = |pp| * ((1 / 2 : ℝ) *
                      (2 : ℝ) ^ (1 - (precision : Int))) +
                    (1 / 2 : ℝ) * (2 : ℝ) ^ (-b.dExp) := by ring
              _ = |pp| * eps + c := by
                    rw [hpow_half_one, hpow_half_exp]
  have hdist_bound : |z - pp| ≤ |pp| * eps + c :=
    le_trans hdist_half hhalf_fulp
  have htri : |pp| ≤ |z| + |z - pp| := by
    calc
      |pp| = |z + (pp - z)| := by
        congr 1
        ring
      _ ≤ |z| + |pp - z| := abs_add_le z (pp - z)
      _ = |z| + |z - pp| := by rw [abs_sub_comm]
  have hpre : |pp| ≤ |z| + |pp| * eps + c := by
    linarith
  have hmul_den : |pp| * den ≤ |z| + c := by
    dsimp [den, eps]
    nlinarith [hpre]
  have hmul_inv := mul_le_mul_of_nonneg_right hmul_den hden_inv_nonneg
  calc
    |pp| = |pp| * den * den⁻¹ := by
      rw [mul_assoc, mul_inv_cancel₀ hden_ne, mul_one]
    _ ≤ (|z| + c) * den⁻¹ := hmul_inv
    _ = |z| * den⁻¹ + c * den⁻¹ := by ring

/-- Coq: `RoundLeGeneral` at the Fast/Axpy section's fixed radix two.
The public contract retains `1 < precision`, contains one boundedness premise,
and does not expose the internal duplicate `Fbounded'`. -/
theorem RoundLeGeneral (b : Fbound_skel) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat 2) (z : ℝ) :
    ⦃⌜1 < precision ∧ b.vNum = Zpower_nat 2 precision ∧
        Fbounded (beta:=2) b p ∧
        Closest (beta:=2) b (2 : ℝ) z p⌝⦄
    (pure (RoundLeGeneral_check (beta:=2) b precision p z) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=2) p| ≤
        |z| * (1 - (2 : ℝ) ^ (-(precision : Int)))⁻¹ +
        (2 : ℝ) ^ (-b.dExp - 1) *
          (1 - (2 : ℝ) ^ (-(precision : Int)))⁻¹⌝⦄ := by
  intro ⟨hprecision, hvNum, hpBound, hClosest⟩
  have hprecision_ne : precision ≠ 0 := by omega
  exact RoundLeGeneral_internal (beta:=2) b precision p z
    ⟨hpBound, hpBound, hClosest, rfl, hprecision_ne, hvNum⟩

/-- Coq `Axpy_opt` scale algebra.

Upstream first derives `(5 + 4*eps) * |t| ≤ |y|` from `RoundLeGeneral` and the
large-y hypothesis, then combines it with the lower estimate
`(|y| - |t|)/(1 + eps) ≤ |u|` to obtain the scale premise
`4 * |t| ≤ |u|` required by `Axpy_tFlessu`.  This lemma packages only that
real-arithmetic cut; later Axpy work still has to supply the two analytic
premises from closestness and ulp estimates. -/
theorem Axpy_scale_from_round_and_lower
    (eps A c T Y U : ℝ) :
    ⦃⌜0 < 1 - eps ∧
        0 < 1 + eps ∧
        |T| ≤ (|A| + c) * (1 - eps)⁻¹ ∧
        (5 + 4 * eps) * ((|A| + c) * (1 - eps)⁻¹) ≤ |Y| ∧
        (|Y| - |T|) * (1 + eps)⁻¹ ≤ |U|⌝⦄
    (pure (Axpy_scale_from_round_and_lower_check eps A c T Y U) : Id Unit)
    ⦃⇓_ => ⌜(4 : ℝ) * |T| ≤ |U|⌝⦄ := by
  intro h
  rcases h with ⟨_hden_pos, hsum_pos, hT_bound, hY_dom, hU_lower⟩
  simp only [wp, PostCond.noThrow, pure, Axpy_scale_from_round_and_lower_check,
    Id.run, ULift.up_down]
  have hcoef_nonneg : 0 ≤ 5 + 4 * eps := by
    linarith
  have hscale_t : (5 + 4 * eps) * |T| ≤ |Y| := by
    exact le_trans (mul_le_mul_of_nonneg_left hT_bound hcoef_nonneg) hY_dom
  have hsum_nonneg : 0 ≤ 1 + eps := le_of_lt hsum_pos
  have hsum_ne : 1 + eps ≠ 0 := ne_of_gt hsum_pos
  have hpre_div : (4 : ℝ) * |T| * (1 + eps) ≤ |Y| - |T| := by
    nlinarith [hscale_t]
  have hpre_inv :=
    mul_le_mul_of_nonneg_right hpre_div (inv_nonneg.mpr hsum_nonneg)
  have hfour_le_lower :
      (4 : ℝ) * |T| ≤ (|Y| - |T|) * (1 + eps)⁻¹ := by
    calc
      (4 : ℝ) * |T|
          = ((4 : ℝ) * |T| * (1 + eps)) * (1 + eps)⁻¹ := by
            rw [mul_assoc, mul_inv_cancel₀ hsum_ne, mul_one]
      _ ≤ (|Y| - |T|) * (1 + eps)⁻¹ := hpre_inv
  exact le_trans hfour_le_lower hU_lower

/-- Coq `Axpy_opt` lower estimate for the rounded sum.

This packages the real-valued part of the upstream derivation of
`(|y| - |t|)/(1 + eps) ≤ |u|`: reverse triangle bounds `|y|-|t|` by
`|y+t|`, and the closest-rounding error estimate bounds `|y+t|` by
`|u| * (1 + eps)`.  Later Axpy work still has to supply the premise
`|y+t-u| ≤ |u|*eps` from `ClosestUlp`, `FulpLe2`, and exactness of the rounded
sum. -/
theorem Axpy_u_lower_from_sum_error
    (eps T Y U : ℝ) :
    ⦃⌜0 < 1 + eps ∧
        |Y + T - U| ≤ |U| * eps⌝⦄
    (pure (Axpy_u_lower_from_sum_error_check eps T Y U) : Id Unit)
    ⦃⇓_ => ⌜(|Y| - |T|) * (1 + eps)⁻¹ ≤ |U|⌝⦄ := by
  intro h
  rcases h with ⟨hsum_pos, hdist⟩
  simp only [wp, PostCond.noThrow, pure, Axpy_u_lower_from_sum_error_check,
    Id.run, ULift.up_down]
  have hsum_nonneg : 0 ≤ 1 + eps := le_of_lt hsum_pos
  have hsum_ne : 1 + eps ≠ 0 := ne_of_gt hsum_pos
  have hrev : |Y| - |T| ≤ |Y + T| := by
    have htri :
        |Y| ≤ |Y + T| + |T| := by
      calc
        |Y| = |(Y + T) + (-T)| := by
          congr 1
          ring
        _ ≤ |Y + T| + |-T| := abs_add_le _ _
        _ = |Y + T| + |T| := by rw [abs_neg]
    linarith
  have hcenter : |Y + T| ≤ |U| + |Y + T - U| := by
    calc
      |Y + T| = |U + (Y + T - U)| := by
        congr 1
        ring
      _ ≤ |U| + |Y + T - U| := abs_add_le _ _
  have hpre : |Y| - |T| ≤ |U| * (1 + eps) := by
    nlinarith
  have hmul := mul_le_mul_of_nonneg_right hpre (inv_nonneg.mpr hsum_nonneg)
  calc
    (|Y| - |T|) * (1 + eps)⁻¹
        ≤ (|U| * (1 + eps)) * (1 + eps)⁻¹ := hmul
    _ = |U| := by
      rw [mul_assoc, mul_inv_cancel₀ hsum_ne, mul_one]

/-- Coq `Axpy_opt` rounded-sum error estimate.

This packages the ulp-to-relative-error step used before
`Axpy_u_lower_from_sum_error`: closestness gives a half-ulp bound on the
rounded sum, while `FulpLe2` bounds that ulp by `|u| * 2^(1-precision)` in the
normal branch. -/
theorem Axpy_sum_error_from_closest_ulp {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (u : FloatSpec.Core.Defs.FlocqFloat beta) (S : ℝ) :
    ⦃⌜Closest (beta:=beta) b (2 : ℝ) S u ∧
        Fbounded (beta:=beta) b u ∧
        Fnormal (beta:=beta) 2 b
          (Fnormalize (beta:=beta) 2 b precision u) ∧
        beta = 2 ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat 2 precision⌝⦄
    (pure (Axpy_sum_error_from_closest_ulp_check
      (beta:=beta) b precision u S) : Id Unit)
    ⦃⇓_ => ⌜|S - _root_.F2R (beta:=beta) u| ≤
        |_root_.F2R (beta:=beta) u| *
          (2 : ℝ) ^ (-(precision : Int))⌝⦄ := by
  intro h
  rcases h with ⟨hClosest, huBound, huNormal, hbeta, hprecision, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, Axpy_sum_error_from_closest_ulp_check,
    Id.run, ULift.up_down]
  subst beta
  let U : ℝ := _root_.F2R (beta:=2) u
  let eps : ℝ := (2 : ℝ) ^ (-(precision : Int))
  have hpow_half_one :
      (1 / 2 : ℝ) * (2 : ℝ) ^ (1 - (precision : Int)) = eps := by
    have htwo_ne : (2 : ℝ) ≠ 0 := by norm_num
    have hhalf : (1 / 2 : ℝ) = (2 : ℝ) ^ (-1 : Int) := by
      norm_num [zpow_neg_one]
    calc
      (1 / 2 : ℝ) * (2 : ℝ) ^ (1 - (precision : Int))
          = (2 : ℝ) ^ (-1 : Int) *
              (2 : ℝ) ^ (1 - (precision : Int)) := by
            rw [hhalf]
      _ = (2 : ℝ) ^ ((-1 : Int) + (1 - (precision : Int))) := by
            rw [← zpow_add₀ htwo_ne]
      _ = eps := by
            congr 1
            omega
  have hclosest_ulp :
      2 * |S - U| ≤ Fulp (beta:=2) b 2 precision u := by
    have h := ClosestUlp (beta:=2) b 2 precision S u
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, U, Int.cast_ofNat] using
      h ⟨hClosest, rfl, by decide, hprecision, hvNum⟩
  have hdist_half :
      |S - U| ≤ (1 / 2 : ℝ) * Fulp (beta:=2) b 2 precision u := by
    nlinarith
  have hfulp :
      Fulp (beta:=2) b 2 precision u ≤
        |U| * (2 : ℝ) ^ (1 - (precision : Int)) := by
    have h := FulpLe2 (beta:=2) 2 b precision u
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpLe2_check,
      Id.run, ULift.up_down, U, Int.cast_ofNat] using
      h ⟨huBound, huNormal, rfl, by decide, hprecision, hvNum⟩
  have hhalf_fulp :
      (1 / 2 : ℝ) * Fulp (beta:=2) b 2 precision u ≤ |U| * eps := by
    have hmul := mul_le_mul_of_nonneg_left hfulp
      (by norm_num : 0 ≤ (1 / 2 : ℝ))
    calc
      (1 / 2 : ℝ) * Fulp (beta:=2) b 2 precision u
          ≤ (1 / 2 : ℝ) *
              (|U| * (2 : ℝ) ^ (1 - (precision : Int))) := hmul
      _ = |U| * eps := by
            calc
              (1 / 2 : ℝ) *
                  (|U| * (2 : ℝ) ^ (1 - (precision : Int)))
                  = |U| * ((1 / 2 : ℝ) *
                      (2 : ℝ) ^ (1 - (precision : Int))) := by ring
              _ = |U| * eps := by rw [hpow_half_one]
  exact le_trans hdist_half hhalf_fulp

/-- Coq `Axpy_opt` rounded-sum lower bound.

This combines the closest-rounding ulp estimate for the concrete sum `Y + T`
with the real reverse-triangle step in `Axpy_u_lower_from_sum_error`. -/
theorem Axpy_u_lower_from_closest_sum {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (u : FloatSpec.Core.Defs.FlocqFloat beta) (T Y : ℝ) :
    ⦃⌜0 < 1 + (2 : ℝ) ^ (-(precision : Int)) ∧
        Closest (beta:=beta) b (2 : ℝ) (Y + T) u ∧
        Fbounded (beta:=beta) b u ∧
        Fnormal (beta:=beta) 2 b
          (Fnormalize (beta:=beta) 2 b precision u) ∧
        beta = 2 ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat 2 precision⌝⦄
    (pure (Axpy_u_lower_from_closest_sum_check
      (beta:=beta) b precision u T Y) : Id Unit)
    ⦃⇓_ => ⌜(|Y| - |T|) *
        (1 + (2 : ℝ) ^ (-(precision : Int)))⁻¹ ≤
        |_root_.F2R (beta:=beta) u|⌝⦄ := by
  intro h
  rcases h with
    ⟨hsum_pos, hClosest, huBound, huNormal, hbeta, hprecision, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, Axpy_u_lower_from_closest_sum_check,
    Id.run, ULift.up_down]
  let eps : ℝ := (2 : ℝ) ^ (-(precision : Int))
  let U : ℝ := _root_.F2R (beta:=beta) u
  have hsum_error : |Y + T - U| ≤ |U| * eps := by
    have h := Axpy_sum_error_from_closest_ulp (beta:=beta) b precision u (Y + T)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Axpy_sum_error_from_closest_ulp_check,
      Id.run, ULift.up_down, eps, U, Int.cast_ofNat] using
      h ⟨hClosest, huBound, huNormal, hbeta, hprecision, hvNum⟩
  have hlower := Axpy_u_lower_from_sum_error eps T Y U
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Axpy_u_lower_from_sum_error_check,
    Id.run, ULift.up_down, eps, U, Int.cast_ofNat] using
    hlower ⟨hsum_pos, hsum_error⟩

/-- A closest subnormal radix-2 result is an exact sum when both inputs are
bounded. Any distinct result has an adjacent bounded minimum-exponent float
strictly closer to the exact sum. -/
private theorem closest_sum_eq_of_subnormal
    (b : Fbound_skel)
    (t y u : FloatSpec.Core.Defs.FlocqFloat 2)
    (htBound : Fbounded (beta:=2) b t)
    (hyBound : Fbounded (beta:=2) b y)
    (huClosest : Closest (beta:=2) b (2 : ℝ)
      (_root_.F2R (beta:=2) t + _root_.F2R (beta:=2) y) u)
    (huSubnormal : Fsubnormal (beta:=2) 2 b u)
    (hvNum_gt : 1 < b.vNum) :
    _root_.F2R (beta:=2) u =
      _root_.F2R (beta:=2) t + _root_.F2R (beta:=2) y := by
  have htExp : -b.dExp ≤ t.Fexp := htBound.2
  have hyExp : -b.dExp ≤ y.Fexp := hyBound.2
  let nt : Nat := Int.natAbs (t.Fexp - (-b.dExp))
  let ny : Nat := Int.natAbs (y.Fexp - (-b.dExp))
  have hnt : (nt : Int) = t.Fexp - (-b.dExp) := by
    simpa [nt] using
      Int.natAbs_of_nonneg (by omega : 0 ≤ t.Fexp - (-b.dExp))
  have hny : (ny : Int) = y.Fexp - (-b.dExp) := by
    simpa [ny] using
      Int.natAbs_of_nonneg (by omega : 0 ≤ y.Fexp - (-b.dExp))
  let ts : FloatSpec.Core.Defs.FlocqFloat 2 := Fshift (beta:=2) 2 nt t
  let ys : FloatSpec.Core.Defs.FlocqFloat 2 := Fshift (beta:=2) 2 ny y
  have htsExp : ts.Fexp = -b.dExp := by
    simp only [ts, Fshift, FloatSpec.Core.Defs.FlocqFloat.Fexp]
    rw [hnt]
    omega
  have hysExp : ys.Fexp = -b.dExp := by
    simp only [ys, Fshift, FloatSpec.Core.Defs.FlocqFloat.Fexp]
    rw [hny]
    omega
  have htsVal : _root_.F2R (beta:=2) ts = _root_.F2R (beta:=2) t := by
    simpa [ts] using Fshift_value (beta:=2) 2 nt t rfl (by decide)
  have hysVal : _root_.F2R (beta:=2) ys = _root_.F2R (beta:=2) y := by
    simpa [ys] using Fshift_value (beta:=2) 2 ny y rfl (by decide)
  let q : ℝ := (2 : ℝ) ^ (-b.dExp)
  let sumNum : Int := ts.Fnum + ys.Fnum
  have hqPos : 0 < q := zpow_pos (by norm_num) _
  have htVal : _root_.F2R (beta:=2) t = (ts.Fnum : ℝ) * q := by
    rw [← htsVal]
    simp [_root_.F2R, FloatSpec.Core.Defs.F2R, htsExp, q]
  have hyVal : _root_.F2R (beta:=2) y = (ys.Fnum : ℝ) * q := by
    rw [← hysVal]
    simp [_root_.F2R, FloatSpec.Core.Defs.F2R, hysExp, q]
  have huVal : _root_.F2R (beta:=2) u = (u.Fnum : ℝ) * q := by
    simp [_root_.F2R, FloatSpec.Core.Defs.F2R, huSubnormal.2.1, q]
  have hsumVal :
      _root_.F2R (beta:=2) t + _root_.F2R (beta:=2) y =
        (sumNum : ℝ) * q := by
    rw [htVal, hyVal]
    simp only [sumNum, Int.cast_add]
    ring
  by_contra hne
  have hnumNe : u.Fnum ≠ sumNum := by
    intro hnum
    apply hne
    rw [huVal, hsumVal, hnum]
  have huSmall : |2 * u.Fnum| < b.vNum := huSubnormal.2.2
  rw [abs_lt] at huSmall
  rcases lt_or_gt_of_ne hnumNe with hlt | hgt
  · let g : FloatSpec.Core.Defs.FlocqFloat 2 :=
      ⟨u.Fnum + 1, -b.dExp⟩
    have hgNum : |u.Fnum + 1| < b.vNum := by
      rw [abs_lt]
      constructor <;> omega
    have hgBound : Fbounded (beta:=2) b g := by
      exact ⟨by simpa [g] using hgNum, by simp [g]⟩
    have hgVal : _root_.F2R (beta:=2) g = ((u.Fnum + 1 : Int) : ℝ) * q := by
      simp [g, q, _root_.F2R, FloatSpec.Core.Defs.F2R]
    have hdist := huClosest.2 g hgBound
    rw [huVal, hsumVal, hgVal] at hdist
    have hltReal : (u.Fnum : ℝ) < (sumNum : ℝ) := by
      exact_mod_cast hlt
    have hstepReal : ((u.Fnum + 1 : Int) : ℝ) ≤ (sumNum : ℝ) := by
      exact_mod_cast (by omega : u.Fnum + 1 ≤ sumNum)
    have hleftNonpos :
        (u.Fnum : ℝ) * q - (sumNum : ℝ) * q ≤ 0 := by
      nlinarith
    have hrightNonpos :
        ((u.Fnum + 1 : Int) : ℝ) * q - (sumNum : ℝ) * q ≤ 0 := by
      nlinarith
    rw [abs_of_nonpos hleftNonpos, abs_of_nonpos hrightNonpos] at hdist
    push_cast at hdist
    nlinarith
  · let g : FloatSpec.Core.Defs.FlocqFloat 2 :=
      ⟨u.Fnum - 1, -b.dExp⟩
    have hgNum : |u.Fnum - 1| < b.vNum := by
      rw [abs_lt]
      constructor <;> omega
    have hgBound : Fbounded (beta:=2) b g := by
      exact ⟨by simpa [g] using hgNum, by simp [g]⟩
    have hgVal : _root_.F2R (beta:=2) g = ((u.Fnum - 1 : Int) : ℝ) * q := by
      simp [g, q, _root_.F2R, FloatSpec.Core.Defs.F2R]
    have hdist := huClosest.2 g hgBound
    rw [huVal, hsumVal, hgVal] at hdist
    have hgtReal : (sumNum : ℝ) < (u.Fnum : ℝ) := by
      exact_mod_cast hgt
    have hstepReal : (sumNum : ℝ) ≤ ((u.Fnum - 1 : Int) : ℝ) := by
      exact_mod_cast (by omega : sumNum ≤ u.Fnum - 1)
    have hleftNonneg :
        0 ≤ (u.Fnum : ℝ) * q - (sumNum : ℝ) * q := by
      nlinarith
    have hrightNonneg :
        0 ≤ ((u.Fnum - 1 : Int) : ℝ) * q - (sumNum : ℝ) * q := by
      nlinarith
    rw [abs_of_nonneg hleftNonneg, abs_of_nonneg hrightNonneg] at hdist
    push_cast at hdist
    nlinarith

private theorem UlpFlessuGe_coefficient_bound
    (eps eps2 D c A T Y U : ℝ)
    (hden : 0 < 1 - eps) (hsum : 0 < 1 + eps) (hD : 0 < D)
    (hcoef : 0 ≤ 1 - eps2)
    (hT : |T| ≤ (|A| + c) * (1 - eps)⁻¹)
    (hU : (|Y| - |T|) * (1 + eps)⁻¹ ≤ |U|)
    (heps2 : eps2 = 2 * eps) :
    (4 * D * (1 + eps))⁻¹ * ((1 - eps2) * |Y|) -
      (4 * D * (1 + eps) * (1 - eps))⁻¹ * ((1 - eps2) * |A|) -
      c * ((2 * D)⁻¹ +
        (4 * D * (1 + eps) * (1 - eps))⁻¹ * (1 - eps2)) ≤
    (4 * D)⁻¹ * (|U| - (|U| * eps2 + 2 * c)) := by
  have hden_ne : 1 - eps ≠ 0 := ne_of_gt hden
  have hsum_ne : 1 + eps ≠ 0 := ne_of_gt hsum
  have hD_ne : D ≠ 0 := ne_of_gt hD
  have hprod_pos : 0 < 4 * D * (1 + eps) := by nlinarith
  have hprod2_pos : 0 < 4 * D := by nlinarith
  have hK_nonneg : 0 ≤ (4 * D * (1 + eps))⁻¹ * (1 - eps2) := by
    exact mul_nonneg (inv_nonneg.mpr (le_of_lt hprod_pos)) hcoef
  have hround_sub : |Y| - (|A| + c) * (1 - eps)⁻¹ ≤ |Y| - |T| := by
    linarith [hT]
  have hstep1 :
      (4 * D * (1 + eps))⁻¹ * (1 - eps2) *
          (|Y| - (|A| + c) * (1 - eps)⁻¹) ≤
        (4 * D * (1 + eps))⁻¹ * (1 - eps2) * (|Y| - |T|) := by
    exact mul_le_mul_of_nonneg_left hround_sub hK_nonneg
  have hK2_nonneg : 0 ≤ (4 * D)⁻¹ * (1 - eps2) := by
    exact mul_nonneg (inv_nonneg.mpr (le_of_lt hprod2_pos)) hcoef
  have hstep2 :
      (4 * D)⁻¹ * (1 - eps2) * ((|Y| - |T|) * (1 + eps)⁻¹) ≤
        (4 * D)⁻¹ * (1 - eps2) * |U| := by
    exact mul_le_mul_of_nonneg_left hU hK2_nonneg
  have hleft_eq :
      (4 * D * (1 + eps))⁻¹ * ((1 - eps2) * |Y|) -
        (4 * D * (1 + eps) * (1 - eps))⁻¹ * ((1 - eps2) * |A|) -
        c * ((2 * D)⁻¹ +
          (4 * D * (1 + eps) * (1 - eps))⁻¹ * (1 - eps2)) =
      (4 * D * (1 + eps))⁻¹ * (1 - eps2) *
        (|Y| - (|A| + c) * (1 - eps)⁻¹) - c * (2 * D)⁻¹ := by
    field_simp [hden_ne, hsum_ne, hD_ne] <;> ring
  have hmid_eq :
      (4 * D * (1 + eps))⁻¹ * (1 - eps2) * (|Y| - |T|) =
      (4 * D)⁻¹ * (1 - eps2) * ((|Y| - |T|) * (1 + eps)⁻¹) := by
    field_simp [hsum_ne, hD_ne] <;> ring
  have hright_eq :
      (4 * D)⁻¹ * (1 - eps2) * |U| - c * (2 * D)⁻¹ =
      (4 * D)⁻¹ * (|U| - (|U| * eps2 + 2 * c)) := by
    subst eps2
    field_simp [hD_ne] <;> ring
  calc
    (4 * D * (1 + eps))⁻¹ * ((1 - eps2) * |Y|) -
      (4 * D * (1 + eps) * (1 - eps))⁻¹ * ((1 - eps2) * |A|) -
      c * ((2 * D)⁻¹ +
        (4 * D * (1 + eps) * (1 - eps))⁻¹ * (1 - eps2))
        = (4 * D * (1 + eps))⁻¹ * (1 - eps2) *
          (|Y| - (|A| + c) * (1 - eps)⁻¹) - c * (2 * D)⁻¹ := hleft_eq
    _ ≤ (4 * D * (1 + eps))⁻¹ * (1 - eps2) * (|Y| - |T|) -
          c * (2 * D)⁻¹ := by linarith
    _ = (4 * D)⁻¹ * (1 - eps2) * ((|Y| - |T|) * (1 + eps)⁻¹) -
          c * (2 * D)⁻¹ := by rw [hmid_eq]
    _ ≤ (4 * D)⁻¹ * (1 - eps2) * |U| - c * (2 * D)⁻¹ := by
          linarith
    _ = (4 * D)⁻¹ * (|U| - (|U| * eps2 + 2 * c)) := hright_eq

private theorem UlpFlessuGe2_coefficient_strict
    (eps eps2 D q A Y : ℝ)
    (heps_pos : 0 < eps) (heps_le_quarter : eps ≤ 1 / 4)
    (hD_pos : 0 < D) (hq_pos : 0 < q)
    (heps2 : eps2 = 2 * eps) (hD_eps : D * eps = 1 - eps) :
    eps / 4 * ((1 - eps2) * |Y|) - eps / 4 * |A| - q / 4 <
      (4 * D * (1 + eps))⁻¹ * ((1 - eps2) * |Y|) -
        (4 * D * (1 + eps) * (1 - eps))⁻¹ * ((1 - eps2) * |A|) -
        (q / 2) * ((2 * D)⁻¹ +
          (4 * D * (1 + eps) * (1 - eps))⁻¹ * (1 - eps2)) := by
  have heps_lt_one : eps < 1 := by nlinarith
  have hden_pos : 0 < 1 - eps := by nlinarith
  have hden_ne : 1 - eps ≠ 0 := ne_of_gt hden_pos
  have hsum_pos : 0 < 1 + eps := by nlinarith
  have hcoef_nonneg : 0 ≤ 1 - eps2 := by
    rw [heps2]
    nlinarith
  have hden1_pos : 0 < 4 * D * (1 + eps) := by nlinarith
  have hden1_ne : 4 * D * (1 + eps) ≠ 0 := ne_of_gt hden1_pos
  have hden2_pos : 0 < 4 * D * (1 + eps) * (1 - eps) := by nlinarith
  have hden2_ne : 4 * D * (1 + eps) * (1 - eps) ≠ 0 := ne_of_gt hden2_pos
  have hD2_pos : 0 < 2 * D := by nlinarith
  have hD2_ne : 2 * D ≠ 0 := ne_of_gt hD2_pos
  have hcoefY_base : eps / 4 < (4 * D * (1 + eps))⁻¹ := by
    have heps_sq_pos : 0 < eps * eps := mul_pos heps_pos heps_pos
    have hmul : (eps / 4) * (4 * D * (1 + eps)) < 1 := by
      calc
        (eps / 4) * (4 * D * (1 + eps)) = (D * eps) * (1 + eps) := by ring
        _ = (1 - eps) * (1 + eps) := by rw [hD_eps]
        _ = 1 - eps * eps := by ring
        _ < 1 := by nlinarith
    have hmul2 : (eps / 4) * (4 * D * (1 + eps)) <
        (4 * D * (1 + eps))⁻¹ * (4 * D * (1 + eps)) := by
      rw [inv_mul_cancel₀ hden1_ne]
      exact hmul
    exact lt_of_mul_lt_mul_right hmul2 (le_of_lt hden1_pos)
  have hcoefY :
      eps / 4 * (1 - eps2) ≤ (4 * D * (1 + eps))⁻¹ * (1 - eps2) := by
    exact mul_le_mul_of_nonneg_right (le_of_lt hcoefY_base) hcoef_nonneg
  have hcoefA :
      (4 * D * (1 + eps) * (1 - eps))⁻¹ * (1 - eps2) ≤ eps / 4 := by
    have hmul :
        ((4 * D * (1 + eps) * (1 - eps))⁻¹ * (1 - eps2)) *
            (4 * D * (1 + eps) * (1 - eps)) ≤
          (eps / 4) * (4 * D * (1 + eps) * (1 - eps)) := by
      field_simp [hden2_ne, hden_ne]
      rw [heps2]
      have hpoly : 1 - 2 * eps ≤ (1 - eps) * (1 - eps) * (1 + eps) := by
        have hpos : 0 ≤ eps * (1 - eps + eps * eps) := by
          exact mul_nonneg (le_of_lt heps_pos)
            (by nlinarith [mul_nonneg (le_of_lt heps_pos) (le_of_lt heps_pos)])
        nlinarith
      have htarget :
          (1 - eps) * (1 - eps) * (1 + eps) =
            D * eps * (1 + eps) * (1 - eps) := by
        rw [hD_eps]
        ring
      nlinarith
    exact le_of_mul_le_mul_right hmul hden2_pos
  have hbracket_lt_half :
      (2 * D)⁻¹ +
          (4 * D * (1 + eps) * (1 - eps))⁻¹ * (1 - eps2) < 1 / 2 := by
    have hmul :
        ((2 * D)⁻¹ +
              (4 * D * (1 + eps) * (1 - eps))⁻¹ * (1 - eps2)) *
            (4 * D * (1 + eps) * (1 - eps)) <
          (1 / 2) * (4 * D * (1 + eps) * (1 - eps)) := by
      field_simp [hden2_ne, hD2_ne, hden_ne]
      rw [heps2]
      have hpoly_pos : 0 < 2 - 5 * eps + 4 * eps * eps * eps := by
        have hmain : 5 * eps ≤ 5 * (1 / 4 : ℝ) := by nlinarith
        have hcube_nonneg : 0 ≤ 4 * eps * eps * eps := by positivity
        nlinarith
      have hcalc :
          eps * (2 * (1 + eps) * (1 - eps) + (1 - 2 * eps)) <
            2 * (1 - eps) * (1 + eps) * (1 - eps) := by
        nlinarith
      have htarget :
          2 * (1 - eps) * (1 + eps) * (1 - eps) =
            2 * (D * eps) * (1 + eps) * (1 - eps) := by
        rw [hD_eps]
      nlinarith
    exact lt_of_mul_lt_mul_right hmul (le_of_lt hden2_pos)
  have hconst :
      -(q / 4) <
        -((q / 2) *
          ((2 * D)⁻¹ +
            (4 * D * (1 + eps) * (1 - eps))⁻¹ * (1 - eps2))) := by
    have :
        (q / 2) *
            ((2 * D)⁻¹ +
              (4 * D * (1 + eps) * (1 - eps))⁻¹ * (1 - eps2)) <
          q / 4 := by
      have hqhalf_pos : 0 < q / 2 := by positivity
      calc
        (q / 2) *
            ((2 * D)⁻¹ +
              (4 * D * (1 + eps) * (1 - eps))⁻¹ * (1 - eps2))
            < (q / 2) * (1 / 2) :=
                mul_lt_mul_of_pos_left hbracket_lt_half hqhalf_pos
        _ = q / 4 := by ring
    linarith
  have hYpart :
      eps / 4 * ((1 - eps2) * |Y|) ≤
        (4 * D * (1 + eps))⁻¹ * ((1 - eps2) * |Y|) := by
    calc
      eps / 4 * ((1 - eps2) * |Y|) = (eps / 4 * (1 - eps2)) * |Y| := by ring
      _ ≤ ((4 * D * (1 + eps))⁻¹ * (1 - eps2)) * |Y| :=
        mul_le_mul_of_nonneg_right hcoefY (abs_nonneg Y)
      _ = (4 * D * (1 + eps))⁻¹ * ((1 - eps2) * |Y|) := by ring
  have hApart :
      - (eps / 4 * |A|) ≤
        - ((4 * D * (1 + eps) * (1 - eps))⁻¹ * ((1 - eps2) * |A|)) := by
    have hmulA :
        (4 * D * (1 + eps) * (1 - eps))⁻¹ * ((1 - eps2) * |A|) ≤
          eps / 4 * |A| := by
      calc
        (4 * D * (1 + eps) * (1 - eps))⁻¹ * ((1 - eps2) * |A|)
            = ((4 * D * (1 + eps) * (1 - eps))⁻¹ * (1 - eps2)) * |A| := by
              ring
        _ ≤ (eps / 4) * |A| := mul_le_mul_of_nonneg_right hcoefA (abs_nonneg A)
        _ = eps / 4 * |A| := by ring
    linarith
  linarith

noncomputable def UlpFlessuGe_check {beta : Int} [ValidRadix beta]
    (_b : Fbound_skel) (_precision : Nat)
    (_a _x _y _t _u : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

noncomputable def UlpFlessuGe2_check {beta : Int} [ValidRadix beta]
    (_b : Fbound_skel) (_precision : Nat)
    (_a _x _y _t _u : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `UlpFlessuGe`.

Restores the full Axpy-section coefficient inequality.  The proof follows the
upstream reductions: `RoundLeGeneral` bounds `t`, closestness of the rounded sum
gives the lower estimate for `u`, the displayed coefficient is reduced to the
general `Fulp` bound, and the existing `UlpFlessuGe` helper stack finishes the
quarter-ulp comparison. -/
theorem UlpFlessuGe {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (a x y t u : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b a ∧
        Fbounded (beta:=beta) b x ∧
        Fbounded (beta:=beta) b y ∧
        Fbounded (beta:=beta) b t ∧
        Fbounded (beta:=beta) b u ∧
        Closest (beta:=beta) b (2 : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) t ∧
        Closest (beta:=beta) b (2 : ℝ)
          (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) y) u ∧
        Fcanonic (beta:=beta) 2 b u ∧
        beta = 2 ∧ 1 < precision ∧ b.vNum = Zpower_nat 2 precision⌝⦄
    (pure (UlpFlessuGe_check (beta:=beta) b precision a x y t u) : Id Unit)
    ⦃⇓_ => ⌜
      (4 * ((2 : ℝ) ^ (precision : Int) - 1) *
          (1 + (2 : ℝ) ^ (-(precision : Int))))⁻¹ *
        ((1 - (2 : ℝ) ^ (1 - (precision : Int))) *
          |_root_.F2R (beta:=beta) y|) -
      (4 * ((2 : ℝ) ^ (precision : Int) - 1) *
          (1 + (2 : ℝ) ^ (-(precision : Int))) *
          (1 - (2 : ℝ) ^ (-(precision : Int))))⁻¹ *
        ((1 - (2 : ℝ) ^ (1 - (precision : Int))) *
          |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x|) -
      (2 : ℝ) ^ (-b.dExp - 1) *
        ((2 * ((2 : ℝ) ^ (precision : Int) - 1))⁻¹ +
          (4 * ((2 : ℝ) ^ (precision : Int) - 1) *
              (1 + (2 : ℝ) ^ (-(precision : Int))) *
              (1 - (2 : ℝ) ^ (-(precision : Int))))⁻¹ *
            (1 - (2 : ℝ) ^ (1 - (precision : Int)))) ≤
      (1 / 4 : ℝ) *
        Fulp (beta:=beta) b 2 precision
          (FLess (beta:=beta) b 2 precision u)⌝⦄ := by
  intro h
  rcases h with
    ⟨_haBound, _hxBound, hyBound, htBound, huBound, htClosest, huClosest,
      huCan, hbeta, hprecision_gt, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, UlpFlessuGe_check,
    Id.run, ULift.up_down]
  subst beta
  let A : ℝ := _root_.F2R (beta:=2) a * _root_.F2R (beta:=2) x
  let T : ℝ := _root_.F2R (beta:=2) t
  let Y : ℝ := _root_.F2R (beta:=2) y
  let U : ℝ := _root_.F2R (beta:=2) u
  let eps : ℝ := (2 : ℝ) ^ (-(precision : Int))
  let eps2 : ℝ := (2 : ℝ) ^ (1 - (precision : Int))
  let D : ℝ := (2 : ℝ) ^ (precision : Int) - 1
  let c : ℝ := (2 : ℝ) ^ (-b.dExp - 1)
  have hprecision_ne : precision ≠ 0 := by omega
  have hprecision_pos : (0 : Int) < (precision : Int) := by
    exact_mod_cast Nat.pos_of_ne_zero hprecision_ne
  have hprecision_gt_one_int : (1 : Int) < (precision : Int) := by
    exact_mod_cast hprecision_gt
  have heps_pos : 0 < eps := by
    exact zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have heps_lt_one : eps < 1 := by
    have hpow_lt :
        (2 : ℝ) ^ (-(precision : Int)) < (2 : ℝ) ^ (0 : Int) := by
      exact zpow_lt_zpow_right₀ (by norm_num : (1 : ℝ) < 2) (by omega)
    simpa [eps] using hpow_lt
  have heps2_eq : eps2 = 2 * eps := by
    have htwo_ne : (2 : ℝ) ≠ 0 := by norm_num
    calc
      eps2 = (2 : ℝ) ^ ((1 : Int) + (-(precision : Int))) := by
          rfl
      _ = (2 : ℝ) ^ (1 : Int) * (2 : ℝ) ^ (-(precision : Int)) := by
          rw [zpow_add₀ htwo_ne]
      _ = 2 * eps := by simp [eps]
  have heps2_lt_one : eps2 < 1 := by
    have hpow_lt :
        (2 : ℝ) ^ (1 - (precision : Int)) < (2 : ℝ) ^ (0 : Int) := by
      exact zpow_lt_zpow_right₀ (by norm_num : (1 : ℝ) < 2) (by omega)
    simpa [eps2] using hpow_lt
  have hcoef_nonneg : 0 ≤ 1 - eps2 := by linarith
  have hden_pos : 0 < 1 - eps := by linarith
  have hsum_pos : 0 < 1 + eps := by linarith
  have hD_pos : 0 < D := by
    have hpow_lt :
        (2 : ℝ) ^ (0 : Int) < (2 : ℝ) ^ (precision : Int) := by
      exact zpow_lt_zpow_right₀ (by norm_num : (1 : ℝ) < 2) hprecision_pos
    have hpow_gt_one : 1 < (2 : ℝ) ^ (precision : Int) := by
      simpa using hpow_lt
    exact sub_pos.mpr hpow_gt_one
  have htBound' : Fbounded' (beta:=2) b t := by
    simpa [Fbounded'] using htBound
  have huBound' : Fbounded' (beta:=2) b u := by
    simpa [Fbounded'] using huBound
  have hround_t :
      |T| ≤ (|A| + c) * (1 - eps)⁻¹ := by
    have h := RoundLeGeneral_internal (beta:=2) b precision t A
    have hraw :
        |T| ≤ |A| * (1 - eps)⁻¹ + c * (1 - eps)⁻¹ := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RoundLeGeneral_check,
        Id.run, ULift.up_down, A, T, eps, c, Int.cast_ofNat] using
        h ⟨htBound, htBound', htClosest, rfl, hprecision_ne, hvNum⟩
    calc
      |T| ≤ |A| * (1 - eps)⁻¹ + c * (1 - eps)⁻¹ := hraw
      _ = (|A| + c) * (1 - eps)⁻¹ := by ring
  have hlower_u :
      (|Y| - |T|) * (1 + eps)⁻¹ ≤ |U| := by
    rcases huCan with huNormal | huSubnormal
    · have h := Axpy_u_lower_from_closest_sum (beta:=2) b precision u T Y
      have huClosest_comm :
          Closest (beta:=2) b (2 : ℝ) (Y + T) u := by
        simpa [T, Y, add_comm, add_left_comm, add_assoc] using huClosest
      have huNormEq :
          Fnormalize (beta:=2) 2 b precision u = u := by
        have hnorm := FcanonicFnormalizeEq (beta:=2) 2 b precision u
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          hnorm ⟨Or.inl huNormal, rfl, by decide, hprecision_ne, hvNum⟩
      have huNormalNorm :
          Fnormal (beta:=2) 2 b (Fnormalize (beta:=2) 2 b precision u) := by
        simpa [huNormEq] using huNormal
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
        Axpy_u_lower_from_closest_sum_check, Id.run, ULift.up_down,
        eps, T, Y, U, Int.cast_ofNat] using
        h ⟨hsum_pos, huClosest_comm, huBound, huNormalNorm, rfl, hprecision_ne,
          hvNum⟩
    · have hvNum_gt : 1 < b.vNum := by
        rw [hvNum]
        have hpow_int : (1 : Int) < (2 : Int) ^ precision := by
          exact_mod_cast (Nat.one_lt_pow hprecision_ne (by decide : 1 < 2))
        simpa [Zpower_nat] using hpow_int
      have hexact : U = T + Y := by
        have h := closest_sum_eq_of_subnormal b t y u htBound hyBound
          huClosest huSubnormal hvNum_gt
        simpa [T, Y, U] using h
      have htri : |Y| - |T| ≤ |U| := by
        have hrev : |Y| ≤ |Y + T| + |T| := by
          calc
            |Y| = |(Y + T) + (-T)| := by
                congr 1
                ring
            _ ≤ |Y + T| + |-T| := abs_add_le _ _
            _ = |Y + T| + |T| := by rw [abs_neg]
        have hsum_abs : |Y + T| = |U| := by
          rw [hexact]
          ring_nf
        linarith
      by_cases hnonpos : |Y| - |T| ≤ 0
      · have hleft_nonpos :
            (|Y| - |T|) * (1 + eps)⁻¹ ≤ 0 := by
          exact mul_nonpos_of_nonpos_of_nonneg hnonpos
            (inv_nonneg.mpr (le_of_lt hsum_pos))
        exact le_trans hleft_nonpos (abs_nonneg U)
      · have hdiff_pos : 0 < |Y| - |T| := lt_of_not_ge hnonpos
        have hinv_le_one : (1 + eps)⁻¹ ≤ 1 :=
          inv_le_one_of_one_le₀ (by linarith [heps_pos] : (1 : ℝ) ≤ 1 + eps)
        have hmul_le :
            (|Y| - |T|) * (1 + eps)⁻¹ ≤ |Y| - |T| := by
          calc
            (|Y| - |T|) * (1 + eps)⁻¹ ≤ (|Y| - |T|) * 1 :=
              mul_le_mul_of_nonneg_left hinv_le_one (le_of_lt hdiff_pos)
            _ = |Y| - |T| := by ring
        exact le_trans hmul_le htri
  have hcoeff_general :
      (4 * D * (1 + eps))⁻¹ * ((1 - eps2) * |Y|) -
        (4 * D * (1 + eps) * (1 - eps))⁻¹ * ((1 - eps2) * |A|) -
        c * ((2 * D)⁻¹ +
          (4 * D * (1 + eps) * (1 - eps))⁻¹ * (1 - eps2)) ≤
      (4 * D)⁻¹ * (|U| - (|U| * eps2 + 2 * c)) :=
    UlpFlessuGe_coefficient_bound eps eps2 D c A T Y U hden_pos hsum_pos
      hD_pos hcoef_nonneg hround_t hlower_u heps2_eq
  have hboundary_eq : (2 : ℝ) ^ (-b.dExp) = 2 * c := by
    have htwo_ne : (2 : ℝ) ≠ 0 := by norm_num
    calc
      (2 : ℝ) ^ (-b.dExp) =
          (2 : ℝ) ^ ((1 : Int) + (-b.dExp - 1)) := by
            congr 1
            omega
        _ = (2 : ℝ) ^ (1 : Int) * (2 : ℝ) ^ (-b.dExp - 1) := by
              rw [zpow_add₀ htwo_ne]
        _ = 2 * c := by simp [c]
  have hgeneral :
      (4 * ((2 : ℝ) ^ (precision : Int) - 1))⁻¹ *
        (|_root_.F2R (beta:=2) u| -
          (|_root_.F2R (beta:=2) u| *
            (2 : ℝ) ^ (1 - (precision : Int)) +
           (2 : ℝ) ^ (-b.dExp))) =
      (4 * D)⁻¹ * (|U| - (|U| * eps2 + 2 * c)) := by
    rw [hboundary_eq]
  have hE_general :
      (4 * ((2 : ℝ) ^ (precision : Int) - 1) *
          (1 + (2 : ℝ) ^ (-(precision : Int))))⁻¹ *
        ((1 - (2 : ℝ) ^ (1 - (precision : Int))) *
          |_root_.F2R (beta:=2) y|) -
      (4 * ((2 : ℝ) ^ (precision : Int) - 1) *
          (1 + (2 : ℝ) ^ (-(precision : Int))) *
          (1 - (2 : ℝ) ^ (-(precision : Int))))⁻¹ *
        ((1 - (2 : ℝ) ^ (1 - (precision : Int))) *
          |_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) x|) -
      (2 : ℝ) ^ (-b.dExp - 1) *
        ((2 * ((2 : ℝ) ^ (precision : Int) - 1))⁻¹ +
          (4 * ((2 : ℝ) ^ (precision : Int) - 1) *
              (1 + (2 : ℝ) ^ (-(precision : Int))) *
              (1 - (2 : ℝ) ^ (-(precision : Int))))⁻¹ *
            (1 - (2 : ℝ) ^ (1 - (precision : Int)))) ≤
        (4 * ((2 : ℝ) ^ (precision : Int) - 1))⁻¹ *
          (|_root_.F2R (beta:=2) u| -
            (|_root_.F2R (beta:=2) u| *
              (2 : ℝ) ^ (1 - (precision : Int)) +
             (2 : ℝ) ^ (-b.dExp))) := by
      refine le_trans ?_ (le_of_eq hgeneral.symm)
      simpa only [D, A, Y, U, eps, eps2, c, Int.cast_ofNat] using hcoeff_general
  have hfinish := UlpFlessuGe_from_general_fulp_bound (beta:=2) b 2 precision u
    ((4 * ((2 : ℝ) ^ (precision : Int) - 1) *
          (1 + (2 : ℝ) ^ (-(precision : Int))))⁻¹ *
        ((1 - (2 : ℝ) ^ (1 - (precision : Int))) *
          |_root_.F2R (beta:=2) y|) -
      (4 * ((2 : ℝ) ^ (precision : Int) - 1) *
          (1 + (2 : ℝ) ^ (-(precision : Int))) *
          (1 - (2 : ℝ) ^ (-(precision : Int))))⁻¹ *
        ((1 - (2 : ℝ) ^ (1 - (precision : Int))) *
          |_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) x|) -
      (2 : ℝ) ^ (-b.dExp - 1) *
        ((2 * ((2 : ℝ) ^ (precision : Int) - 1))⁻¹ +
          (4 * ((2 : ℝ) ^ (precision : Int) - 1) *
              (1 + (2 : ℝ) ^ (-(precision : Int))) *
              (1 - (2 : ℝ) ^ (-(precision : Int))))⁻¹ *
            (1 - (2 : ℝ) ^ (1 - (precision : Int)))))
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
    UlpFlessuGe_from_general_fulp_bound_check, Id.run, ULift.up_down, Int.cast_ofNat] using
    hfinish ⟨huBound, huBound', huCan, rfl, by decide, hprecision_ne, hvNum,
      hE_general⟩

/-- Coq: `UlpFlessuGe2`.

Strict coefficient form from the Axpy auxiliary section.  This first compares
the strict upstream coefficient expression with the non-strict coefficient form
proved by `UlpFlessuGe`, then uses that public theorem for the final ulp bound. -/
theorem UlpFlessuGe2 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (a x y t u : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b a ∧
        Fbounded (beta:=beta) b x ∧
        Fbounded (beta:=beta) b y ∧
        Fbounded (beta:=beta) b t ∧
        Fbounded (beta:=beta) b u ∧
        Closest (beta:=beta) b (2 : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) t ∧
        Closest (beta:=beta) b (2 : ℝ)
          (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) y) u ∧
        Fcanonic (beta:=beta) 2 b u ∧
        beta = 2 ∧ 1 < precision ∧ b.vNum = Zpower_nat 2 precision⌝⦄
    (pure (UlpFlessuGe2_check (beta:=beta) b precision a x y t u) : Id Unit)
    ⦃⇓_ => ⌜
      (2 : ℝ) ^ (-(precision : Int) - 2) *
          (1 - (2 : ℝ) ^ (1 - (precision : Int))) *
          |_root_.F2R (beta:=beta) y| -
      ((2 : ℝ) ^ (-(precision : Int) - 2) *
          |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x|) -
      (2 : ℝ) ^ (-b.dExp - 2) <
      (1 / 4 : ℝ) *
        Fulp (beta:=beta) b 2 precision
          (FLess (beta:=beta) b 2 precision u)⌝⦄ := by
  intro h
  rcases h with
    ⟨haBound, hxBound, hyBound, htBound, huBound, htClosest, huClosest,
      huCan, hbeta, hprecision_gt, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, UlpFlessuGe2_check,
    Id.run, ULift.up_down]
  subst beta
  let A : ℝ := _root_.F2R (beta:=2) a * _root_.F2R (beta:=2) x
  let Y : ℝ := _root_.F2R (beta:=2) y
  let eps : ℝ := (2 : ℝ) ^ (-(precision : Int))
  let eps2 : ℝ := (2 : ℝ) ^ (1 - (precision : Int))
  let D : ℝ := (2 : ℝ) ^ (precision : Int) - 1
  let q : ℝ := (2 : ℝ) ^ (-b.dExp)
  have htwo_ne : (2 : ℝ) ≠ 0 := by norm_num
  have hprecision_ne : precision ≠ 0 := by omega
  have hprecision_pos : (0 : Int) < (precision : Int) := by
    exact_mod_cast Nat.pos_of_ne_zero hprecision_ne
  have heps_pos : 0 < eps := by
    exact zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have heps_le_quarter : eps ≤ 1 / 4 := by
    have hpow_le :
        (2 : ℝ) ^ (-(precision : Int)) ≤ (2 : ℝ) ^ (-2 : Int) := by
      exact zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by omega)
    have hpow_eq : (2 : ℝ) ^ (-2 : Int) = 1 / 4 := by
      norm_num [zpow_neg]
    exact le_trans hpow_le (le_of_eq hpow_eq)
  have heps2_eq : eps2 = 2 * eps := by
    calc
      eps2 = (2 : ℝ) ^ ((1 : Int) + (-(precision : Int))) := by
          rfl
      _ = (2 : ℝ) ^ (1 : Int) * (2 : ℝ) ^ (-(precision : Int)) := by
          rw [zpow_add₀ htwo_ne]
      _ = 2 * eps := by simp [eps]
  have hD_pos : 0 < D := by
    have hpow_lt :
        (2 : ℝ) ^ (0 : Int) < (2 : ℝ) ^ (precision : Int) := by
      exact zpow_lt_zpow_right₀ (by norm_num : (1 : ℝ) < 2) hprecision_pos
    have hpow_gt_one : 1 < (2 : ℝ) ^ (precision : Int) := by
      simpa using hpow_lt
    exact sub_pos.mpr hpow_gt_one
  have hq_pos : 0 < q := by
    exact zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hD_eps : D * eps = 1 - eps := by
    calc
      D * eps = ((2 : ℝ) ^ (precision : Int) - 1) *
          (2 : ℝ) ^ (-(precision : Int)) := by rfl
      _ = (2 : ℝ) ^ (precision : Int) * (2 : ℝ) ^ (-(precision : Int)) -
          (2 : ℝ) ^ (-(precision : Int)) := by ring
      _ = (2 : ℝ) ^ ((precision : Int) + (-(precision : Int))) -
          (2 : ℝ) ^ (-(precision : Int)) := by rw [← zpow_add₀ htwo_ne]
      _ = 1 - eps := by
        have hsum : (precision : Int) + (-(precision : Int)) = 0 := by omega
        rw [hsum]
        simp [eps]
  have hshift_precision :
      (2 : ℝ) ^ (-(precision : Int) - 2) = eps / 4 := by
    calc
      (2 : ℝ) ^ (-(precision : Int) - 2) =
          (2 : ℝ) ^ (-(precision : Int)) * (2 : ℝ) ^ (-2 : Int) := by
            rw [show -(precision : Int) - 2 =
              -(precision : Int) + (-2 : Int) by omega]
            rw [zpow_add₀ htwo_ne]
      _ = eps / 4 := by
        norm_num [eps, zpow_neg]
        ring
  have hshift_q2 : (2 : ℝ) ^ (-b.dExp - 1) = q / 2 := by
    calc
      (2 : ℝ) ^ (-b.dExp - 1) =
          (2 : ℝ) ^ (-b.dExp) * (2 : ℝ) ^ (-1 : Int) := by
            rw [show -b.dExp - 1 = -b.dExp + (-1 : Int) by omega]
            rw [zpow_add₀ htwo_ne]
      _ = q / 2 := by
        norm_num [q, zpow_neg]
        ring
  have hshift_q4 : (2 : ℝ) ^ (-b.dExp - 2) = q / 4 := by
    calc
      (2 : ℝ) ^ (-b.dExp - 2) =
          (2 : ℝ) ^ (-b.dExp) * (2 : ℝ) ^ (-2 : Int) := by
            rw [show -b.dExp - 2 = -b.dExp + (-2 : Int) by omega]
            rw [zpow_add₀ htwo_ne]
      _ = q / 4 := by
        norm_num [q, zpow_neg]
        ring
  have hstrict_core :
      eps / 4 * ((1 - eps2) * |Y|) - eps / 4 * |A| - q / 4 <
        (4 * D * (1 + eps))⁻¹ * ((1 - eps2) * |Y|) -
          (4 * D * (1 + eps) * (1 - eps))⁻¹ * ((1 - eps2) * |A|) -
          (q / 2) * ((2 * D)⁻¹ +
            (4 * D * (1 + eps) * (1 - eps))⁻¹ * (1 - eps2)) :=
    UlpFlessuGe2_coefficient_strict eps eps2 D q A Y heps_pos
      heps_le_quarter hD_pos hq_pos heps2_eq hD_eps
  have hstrict :
      (2 : ℝ) ^ (-(precision : Int) - 2) *
          (1 - (2 : ℝ) ^ (1 - (precision : Int))) *
          |_root_.F2R (beta:=2) y| -
      ((2 : ℝ) ^ (-(precision : Int) - 2) *
          |_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) x|) -
      (2 : ℝ) ^ (-b.dExp - 2) <
      (4 * ((2 : ℝ) ^ (precision : Int) - 1) *
          (1 + (2 : ℝ) ^ (-(precision : Int))))⁻¹ *
        ((1 - (2 : ℝ) ^ (1 - (precision : Int))) *
          |_root_.F2R (beta:=2) y|) -
      (4 * ((2 : ℝ) ^ (precision : Int) - 1) *
          (1 + (2 : ℝ) ^ (-(precision : Int))) *
          (1 - (2 : ℝ) ^ (-(precision : Int))))⁻¹ *
        ((1 - (2 : ℝ) ^ (1 - (precision : Int))) *
          |_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) x|) -
      (2 : ℝ) ^ (-b.dExp - 1) *
        ((2 * ((2 : ℝ) ^ (precision : Int) - 1))⁻¹ +
          (4 * ((2 : ℝ) ^ (precision : Int) - 1) *
              (1 + (2 : ℝ) ^ (-(precision : Int))) *
              (1 - (2 : ℝ) ^ (-(precision : Int))))⁻¹ *
            (1 - (2 : ℝ) ^ (1 - (precision : Int)))) := by
    rw [hshift_precision, hshift_q4, hshift_q2]
    simpa only [D, A, Y, eps, eps2, q, mul_assoc, Int.cast_ofNat] using hstrict_core
  have hmain := UlpFlessuGe (beta:=2) b precision a x y t u
  have hle :
      (4 * ((2 : ℝ) ^ (precision : Int) - 1) *
          (1 + (2 : ℝ) ^ (-(precision : Int))))⁻¹ *
        ((1 - (2 : ℝ) ^ (1 - (precision : Int))) *
          |_root_.F2R (beta:=2) y|) -
      (4 * ((2 : ℝ) ^ (precision : Int) - 1) *
          (1 + (2 : ℝ) ^ (-(precision : Int))) *
          (1 - (2 : ℝ) ^ (-(precision : Int))))⁻¹ *
        ((1 - (2 : ℝ) ^ (1 - (precision : Int))) *
          |_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) x|) -
      (2 : ℝ) ^ (-b.dExp - 1) *
        ((2 * ((2 : ℝ) ^ (precision : Int) - 1))⁻¹ +
          (4 * ((2 : ℝ) ^ (precision : Int) - 1) *
              (1 + (2 : ℝ) ^ (-(precision : Int))) *
              (1 - (2 : ℝ) ^ (-(precision : Int))))⁻¹ *
            (1 - (2 : ℝ) ^ (1 - (precision : Int)))) ≤
      (1 / 4 : ℝ) *
        Fulp (beta:=2) b 2 precision
          (FLess (beta:=2) b 2 precision u) := by
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, UlpFlessuGe_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      hmain ⟨haBound, hxBound, hyBound, htBound, huBound, htClosest, huClosest,
        huCan, rfl, hprecision_gt, hvNum⟩
  exact lt_of_lt_of_le hstrict hle

/-- Coq `Axpy_opt` scale premise from rounded inputs.

This composes `RoundLeGeneral`, the rounded-sum lower estimate, and the
real-arithmetic scale cut to produce the `4 * |t| ≤ |u|` premise consumed by
`Axpy_tFlessu`. -/
theorem Axpy_scale_from_rounding_inputs {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (t y u : FloatSpec.Core.Defs.FlocqFloat beta) (A : ℝ) :
    ⦃⌜Fbounded (beta:=beta) b t ∧
        Fbounded (beta:=beta) b y ∧
        Closest (beta:=beta) b (2 : ℝ) A t ∧
        Closest (beta:=beta) b (2 : ℝ)
          (_root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) t) u ∧
        Fbounded (beta:=beta) b u ∧
        Fcanonic (beta:=beta) 2 b u ∧
        beta = 2 ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat 2 precision ∧
        (5 + 4 * (2 : ℝ) ^ (-(precision : Int))) *
          ((|A| + (2 : ℝ) ^ (-b.dExp - 1)) *
            (1 - (2 : ℝ) ^ (-(precision : Int)))⁻¹) ≤
          |_root_.F2R (beta:=beta) y|⌝⦄
    (pure (Axpy_scale_from_rounding_inputs_check
      (beta:=beta) b precision t u A (_root_.F2R (beta:=beta) y)) : Id Unit)
    ⦃⇓_ => ⌜(4 : ℝ) * |_root_.F2R (beta:=beta) t| ≤
        |_root_.F2R (beta:=beta) u|⌝⦄ := by
  intro h
  rcases h with
    ⟨htBound, hyBound, htClosest, huClosest, huBound, huCan,
      hbeta, hprecision, hvNum, hdom⟩
  simp only [wp, PostCond.noThrow, pure, Axpy_scale_from_rounding_inputs_check,
    Id.run, ULift.up_down]
  subst beta
  let eps : ℝ := (2 : ℝ) ^ (-(precision : Int))
  let c : ℝ := (2 : ℝ) ^ (-b.dExp - 1)
  let T : ℝ := _root_.F2R (beta:=2) t
  let Y : ℝ := _root_.F2R (beta:=2) y
  let U : ℝ := _root_.F2R (beta:=2) u
  have hprecision_pos : (0 : Int) < (precision : Int) := by
    exact_mod_cast Nat.pos_of_ne_zero hprecision
  have heps_pos : 0 < eps := by
    exact zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have heps_lt_one : eps < 1 := by
    have hpow_lt :
        (2 : ℝ) ^ (-(precision : Int)) < (2 : ℝ) ^ (0 : Int) := by
      exact zpow_lt_zpow_right₀ (by norm_num : (1 : ℝ) < 2) (by omega)
    simpa [eps] using hpow_lt
  have hden_pos : 0 < 1 - eps := by
    linarith
  have hsum_pos : 0 < 1 + eps := by
    linarith
  have htBound' : Fbounded' (beta:=2) b t := by
    simpa [Fbounded'] using htBound
  have ht_round :
      |T| ≤ (|A| + c) * (1 - eps)⁻¹ := by
    have h := RoundLeGeneral_internal (beta:=2) b precision t A
    have hraw :
        |T| ≤ |A| * (1 - eps)⁻¹ + c * (1 - eps)⁻¹ := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RoundLeGeneral_check,
        Id.run, ULift.up_down, eps, c, T, Int.cast_ofNat] using
        h ⟨htBound, htBound', htClosest, rfl, hprecision, hvNum⟩
    calc
      |T| ≤ |A| * (1 - eps)⁻¹ + c * (1 - eps)⁻¹ := hraw
      _ = (|A| + c) * (1 - eps)⁻¹ := by ring
  have hu_lower :
      (|Y| - |T|) * (1 + eps)⁻¹ ≤ |U| := by
    rcases huCan with huNormal | huSubnormal
    · have h := Axpy_u_lower_from_closest_sum (beta:=2) b precision u T Y
      have huNormEq :
          Fnormalize (beta:=2) 2 b precision u = u := by
        have hnorm := FcanonicFnormalizeEq (beta:=2) 2 b precision u
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          hnorm ⟨Or.inl huNormal, rfl, by decide, hprecision, hvNum⟩
      have huNormalNorm :
          Fnormal (beta:=2) 2 b (Fnormalize (beta:=2) 2 b precision u) := by
        simpa [huNormEq] using huNormal
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
        Axpy_u_lower_from_closest_sum_check, Id.run, ULift.up_down,
        eps, T, Y, U, Int.cast_ofNat] using
        h ⟨hsum_pos, huClosest, huBound, huNormalNorm, rfl, hprecision, hvNum⟩
    · have hvNum_gt : 1 < b.vNum := by
        rw [hvNum]
        have hpow_int : (1 : Int) < (2 : Int) ^ precision := by
          exact_mod_cast (Nat.one_lt_pow hprecision (by decide : 1 < 2))
        simpa [Zpower_nat] using hpow_int
      have hexact : U = T + Y := by
        have h := closest_sum_eq_of_subnormal b t y u htBound hyBound
          (by simpa [T, Y, add_comm, add_left_comm, add_assoc] using huClosest)
          huSubnormal hvNum_gt
        simpa [T, Y, U] using h
      have htri : |Y| - |T| ≤ |U| := by
        have hrev : |Y| ≤ |Y + T| + |T| := by
          calc
            |Y| = |(Y + T) + (-T)| := by
                congr 1
                ring
            _ ≤ |Y + T| + |-T| := abs_add_le _ _
            _ = |Y + T| + |T| := by rw [abs_neg]
        have hsum_abs : |Y + T| = |U| := by
          rw [hexact]
          ring_nf
        linarith
      by_cases hnonpos : |Y| - |T| ≤ 0
      · have hleft_nonpos :
            (|Y| - |T|) * (1 + eps)⁻¹ ≤ 0 := by
          exact mul_nonpos_of_nonpos_of_nonneg hnonpos
            (inv_nonneg.mpr (le_of_lt hsum_pos))
        exact le_trans hleft_nonpos (abs_nonneg U)
      · have hdiff_pos : 0 < |Y| - |T| := lt_of_not_ge hnonpos
        have hinv_le_one : (1 + eps)⁻¹ ≤ 1 :=
          inv_le_one_of_one_le₀ (by linarith [heps_pos] : (1 : ℝ) ≤ 1 + eps)
        have hmul_le :
            (|Y| - |T|) * (1 + eps)⁻¹ ≤ |Y| - |T| := by
          calc
            (|Y| - |T|) * (1 + eps)⁻¹ ≤ (|Y| - |T|) * 1 :=
              mul_le_mul_of_nonneg_left hinv_le_one (le_of_lt hdiff_pos)
            _ = |Y| - |T| := by ring
        exact le_trans hmul_le htri
  have hdom' :
      (5 + 4 * eps) * ((|A| + c) * (1 - eps)⁻¹) ≤ |Y| := by
    simpa [eps, c] using hdom
  have hscale := Axpy_scale_from_round_and_lower eps A c T Y U
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Axpy_scale_from_round_and_lower_check,
    Id.run, ULift.up_down, T, U, Int.cast_ofNat] using
    hscale ⟨hden_pos, hsum_pos, ht_round, hdom', hu_lower⟩

/-- Coq `Axpy_opt` perturbation handoff.

The final line of the upstream proof turns the user-facing perturbation
hypothesis `H2` into the strict quarter-ulp bound required by `Axpy_tFlessu`
using `UlpFlessuGe2`.  This helper packages the non-arithmetic part of that
handoff: symmetry of the absolute-value differences and transitivity from a
non-strict error estimate to a strict `Fulp (FLess u)` estimate. -/
theorem Axpy_perturb_from_error_bound {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (a x y u : FloatSpec.Core.Defs.FlocqFloat beta)
    (a1 x1 y1 E : ℝ) :
    ⦃⌜|y1 - _root_.F2R (beta:=beta) y| +
          |a1 * x1 -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x| ≤ E ∧
        E < (1 / 4 : ℝ) * Fulp (beta:=beta) b 2 precision
          (FLess (beta:=beta) b 2 precision u)⌝⦄
    (pure (Axpy_perturb_from_error_bound_check
      (beta:=beta) b precision a x y u a1 x1 y1 E) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) y - y1| +
          |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
            a1 * x1| <
        (1 / 4 : ℝ) * Fulp (beta:=beta) b 2 precision
          (FLess (beta:=beta) b 2 precision u)⌝⦄ := by
  intro h
  rcases h with ⟨hbound, hstrict⟩
  simp only [wp, PostCond.noThrow, pure, Axpy_perturb_from_error_bound_check,
    Id.run, ULift.up_down]
  have hbound' :
      |_root_.F2R (beta:=beta) y - y1| +
          |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
            a1 * x1| ≤ E := by
    simpa [abs_sub_comm] using hbound
  exact lt_of_le_of_lt hbound' hstrict

/-- Coq `Axpy_opt` final MinOrMax bridge, factored before the last numeric cut.

Once the rounded-input hypotheses give the scale premise and the user-facing
error bound has already been turned into the strict `FLess` perturbation
bound, the restored `Axpy_tFlessu` theorem gives the desired min/max
conclusion.  This keeps the remaining `Axpy_opt` work localized to proving the
numeric `UlpFlessuGe2` estimate and carrying the predecessor side cases. -/
theorem Axpy_min_or_max_from_rounding_inputs {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (a x y t u : FloatSpec.Core.Defs.FlocqFloat beta)
    (a1 x1 y1 E : ℝ) :
    ⦃⌜Fbounded (beta:=beta) b a ∧
        Fbounded (beta:=beta) b x ∧
        Fbounded (beta:=beta) b y ∧
        Fbounded (beta:=beta) b t ∧
        Fbounded' (beta:=beta) b t ∧
        Fbounded (beta:=beta) b u ∧
        Closest (beta:=beta) b (2 : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) t ∧
        Closest (beta:=beta) b (2 : ℝ)
          (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) y) u ∧
        Fcanonic (beta:=beta) 2 b u ∧
        Fcanonic (beta:=beta) 2 b t ∧
        Fnormal (beta:=beta) 2 b
          (Fnormalize (beta:=beta) 2 b precision u) ∧
        (5 + 4 * (2 : ℝ) ^ (-(precision : Int))) *
          ((|_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x| +
              (2 : ℝ) ^ (-b.dExp - 1)) *
            (1 - (2 : ℝ) ^ (-(precision : Int)))⁻¹) ≤
          |_root_.F2R (beta:=beta) y| ∧
        |y1 - _root_.F2R (beta:=beta) y| +
          |a1 * x1 -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x| ≤ E ∧
        E < (1 / 4 : ℝ) * Fulp (beta:=beta) b 2 precision
          (FLess (beta:=beta) b 2 precision u) ∧
        (((FPred (beta:=beta) b 2 precision u).Fexp = -b.dExp ∧
            -b.dExp + 1 ≤ u.Fexp) ∨
          -b.dExp + 1 ≤ (FPred (beta:=beta) b 2 precision u).Fexp) ∧
        (((FPred (beta:=beta) b 2 precision (Fopp (beta:=beta) u)).Fexp =
            -b.dExp ∧ -b.dExp + 1 ≤ (Fopp (beta:=beta) u).Fexp) ∨
          -b.dExp + 1 ≤
            (FPred (beta:=beta) b 2 precision (Fopp (beta:=beta) u)).Fexp) ∧
        beta = 2 ∧ 1 < precision ∧ b.vNum = Zpower_nat 2 precision ∧
        (∀ r : ℝ, -b.dExp ≤ (boundR (beta:=beta) 2 r).Fexp)⌝⦄
    (pure (Axpy_min_or_max_from_rounding_inputs_check
      (beta:=beta) b precision a x y t u a1 x1 y1 E) : Id Unit)
    ⦃⇓_ => ⌜MinOrMax (beta:=beta) b 2 (a1 * x1 + y1) u⌝⦄ := by
  intro h
  rcases h with
    ⟨haBound, hxBound, hyBound, htBound, htBound', huBound,
      htClosest, huClosest, huCan, htCan, huNormal, hdom, hEbound,
      hEstrict, hpredPos, hpredNeg, hbeta, hprecision_gt, hvNum, hBoundExp⟩
  simp only [wp, PostCond.noThrow, pure,
    Axpy_min_or_max_from_rounding_inputs_check, Id.run, ULift.up_down]
  have hprecision_ne : precision ≠ 0 := by omega
  have huClosest_comm :
      Closest (beta:=beta) b (2 : ℝ)
        (_root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) t) u := by
    simpa [add_comm, add_left_comm, add_assoc] using huClosest
  have hscale :
      (4 : ℝ) * |_root_.F2R (beta:=beta) t| ≤
        |_root_.F2R (beta:=beta) u| := by
    have h := Axpy_scale_from_rounding_inputs (beta:=beta) b precision t y u
      (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Axpy_scale_from_rounding_inputs_check, Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨htBound, hyBound, htClosest, huClosest_comm, huBound, huCan,
        hbeta, hprecision_ne, hvNum, hdom⟩
  have hperturb :
      |_root_.F2R (beta:=beta) y - y1| +
          |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
            a1 * x1| <
        (1 / 4 : ℝ) * Fulp (beta:=beta) b 2 precision
          (FLess (beta:=beta) b 2 precision u) := by
    have h := Axpy_perturb_from_error_bound
      (beta:=beta) b precision a x y u a1 x1 y1 E
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Axpy_perturb_from_error_bound_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hEbound, hEstrict⟩
  have hmain := Axpy_tFlessu (beta:=beta) b precision a x y t u a1 x1 y1
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Axpy_tFlessu_check,
    Id.run, ULift.up_down, Int.cast_ofNat] using
    hmain ⟨haBound, hxBound, hyBound, htBound, huBound, htClosest,
      huClosest, huCan, htCan, hscale, hperturb,
      hbeta, hprecision_gt, hvNum⟩

/-- Coq `Axpy_opt` after the strict coefficient estimate has been isolated.

This packages the already-restored rounded-input scale proof with the upstream
large-`y` hypothesis shape.  The remaining full `Axpy_opt` payload is the
nonlinear coefficient proof that turns the user perturbation bound into the
strict quarter-ulp bound on `FLess u`, plus the predecessor side conditions. -/
theorem Axpy_opt_from_strict_bound {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (a x y t u : FloatSpec.Core.Defs.FlocqFloat beta)
    (a1 x1 y1 E : ℝ) :
    ⦃⌜Fbounded (beta:=beta) b a ∧
        Fbounded (beta:=beta) b x ∧
        Fbounded (beta:=beta) b y ∧
        Fbounded (beta:=beta) b t ∧
        Fbounded' (beta:=beta) b t ∧
        Fbounded (beta:=beta) b u ∧
        Closest (beta:=beta) b (2 : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) t ∧
        Closest (beta:=beta) b (2 : ℝ)
          (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) y) u ∧
        Fcanonic (beta:=beta) 2 b u ∧
        Fcanonic (beta:=beta) 2 b t ∧
        Fnormal (beta:=beta) 2 b
          (Fnormalize (beta:=beta) 2 b precision u) ∧
        (5 + 4 * (2 : ℝ) ^ (-(precision : Int))) *
            (1 - (2 : ℝ) ^ (-(precision : Int)))⁻¹ *
            (|_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x| +
              (2 : ℝ) ^ (-b.dExp - 1)) ≤
          |_root_.F2R (beta:=beta) y| ∧
        |y1 - _root_.F2R (beta:=beta) y| +
          |a1 * x1 -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x| ≤ E ∧
        E < (1 / 4 : ℝ) * Fulp (beta:=beta) b 2 precision
          (FLess (beta:=beta) b 2 precision u) ∧
        (((FPred (beta:=beta) b 2 precision u).Fexp = -b.dExp ∧
            -b.dExp + 1 ≤ u.Fexp) ∨
          -b.dExp + 1 ≤ (FPred (beta:=beta) b 2 precision u).Fexp) ∧
        (((FPred (beta:=beta) b 2 precision (Fopp (beta:=beta) u)).Fexp =
            -b.dExp ∧ -b.dExp + 1 ≤ (Fopp (beta:=beta) u).Fexp) ∨
          -b.dExp + 1 ≤
            (FPred (beta:=beta) b 2 precision (Fopp (beta:=beta) u)).Fexp) ∧
        beta = 2 ∧ 1 < precision ∧ b.vNum = Zpower_nat 2 precision ∧
        (∀ r : ℝ, -b.dExp ≤ (boundR (beta:=beta) 2 r).Fexp)⌝⦄
    (pure (Axpy_opt_from_strict_bound_check
      (beta:=beta) b precision a x y t u a1 x1 y1 E) : Id Unit)
    ⦃⇓_ => ⌜MinOrMax (beta:=beta) b 2 (a1 * x1 + y1) u⌝⦄ := by
  intro h
  rcases h with
    ⟨haBound, hxBound, hyBound, htBound, htBound', huBound,
      htClosest, huClosest, huCan, htCan, huNormal, hlarge, hEbound,
      hEstrict, hpredPos, hpredNeg, hbeta, hprecision_gt, hvNum, hBoundExp⟩
  simp only [wp, PostCond.noThrow, pure,
    Axpy_opt_from_strict_bound_check, Id.run, ULift.up_down]
  have hdom :
      (5 + 4 * (2 : ℝ) ^ (-(precision : Int))) *
        ((|_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x| +
            (2 : ℝ) ^ (-b.dExp - 1)) *
          (1 - (2 : ℝ) ^ (-(precision : Int)))⁻¹) ≤
        |_root_.F2R (beta:=beta) y| := by
    simpa [mul_assoc, mul_left_comm, mul_comm] using hlarge
  have hmain := Axpy_min_or_max_from_rounding_inputs
    (beta:=beta) b precision a x y t u a1 x1 y1 E
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
    Axpy_min_or_max_from_rounding_inputs_check, Id.run, ULift.up_down, Int.cast_ofNat] using
    hmain ⟨haBound, hxBound, hyBound, htBound, htBound', huBound,
      htClosest, huClosest, huCan, htCan, huNormal, hdom, hEbound, hEstrict,
      hpredPos, hpredNeg, hbeta, hprecision_gt, hvNum, hBoundExp⟩

/-- Coq `Axpy_opt` after reducing the perturbation estimate to the
`UlpFlessuGe2` general coefficient bound.

This composes the strict handoff `UlpFlessuGe2_from_general_bound` with
`Axpy_opt_from_strict_bound`.  The remaining full upstream `Axpy_opt` work is
the arithmetic proof of the displayed general coefficient inequality and the
predecessor exponent side cases. -/
theorem Axpy_opt_from_general_bound {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (a x y t u : FloatSpec.Core.Defs.FlocqFloat beta)
    (a1 x1 y1 E : ℝ) :
    ⦃⌜Fbounded (beta:=beta) b a ∧
        Fbounded (beta:=beta) b x ∧
        Fbounded (beta:=beta) b y ∧
        Fbounded (beta:=beta) b t ∧
        Fbounded' (beta:=beta) b t ∧
        Fbounded (beta:=beta) b u ∧
        Closest (beta:=beta) b (2 : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) t ∧
        Closest (beta:=beta) b (2 : ℝ)
          (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) y) u ∧
        Fcanonic (beta:=beta) 2 b u ∧
        Fcanonic (beta:=beta) 2 b t ∧
        Fnormal (beta:=beta) 2 b
          (Fnormalize (beta:=beta) 2 b precision u) ∧
        (5 + 4 * (2 : ℝ) ^ (-(precision : Int))) *
            (1 - (2 : ℝ) ^ (-(precision : Int)))⁻¹ *
            (|_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x| +
              (2 : ℝ) ^ (-b.dExp - 1)) ≤
          |_root_.F2R (beta:=beta) y| ∧
        |y1 - _root_.F2R (beta:=beta) y| +
          |a1 * x1 -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x| ≤ E ∧
        E < (4 * ((2 : ℝ) ^ (precision : Int) - 1))⁻¹ *
          (|_root_.F2R (beta:=beta) u| -
            (|_root_.F2R (beta:=beta) u| *
              (2 : ℝ) ^ (1 - (precision : Int)) +
             (2 : ℝ) ^ (-b.dExp))) ∧
        (((FPred (beta:=beta) b 2 precision u).Fexp = -b.dExp ∧
            -b.dExp + 1 ≤ u.Fexp) ∨
          -b.dExp + 1 ≤ (FPred (beta:=beta) b 2 precision u).Fexp) ∧
        (((FPred (beta:=beta) b 2 precision (Fopp (beta:=beta) u)).Fexp =
            -b.dExp ∧ -b.dExp + 1 ≤ (Fopp (beta:=beta) u).Fexp) ∨
          -b.dExp + 1 ≤
            (FPred (beta:=beta) b 2 precision (Fopp (beta:=beta) u)).Fexp) ∧
        beta = 2 ∧ 1 < precision ∧ b.vNum = Zpower_nat 2 precision ∧
        (∀ r : ℝ, -b.dExp ≤ (boundR (beta:=beta) 2 r).Fexp)⌝⦄
    (pure (Axpy_opt_from_general_bound_check
      (beta:=beta) b precision a x y t u a1 x1 y1 E) : Id Unit)
    ⦃⇓_ => ⌜MinOrMax (beta:=beta) b 2 (a1 * x1 + y1) u⌝⦄ := by
  intro h
  rcases h with
    ⟨haBound, hxBound, hyBound, htBound, htBound', huBound,
      htClosest, huClosest, huCan, htCan, huNormal, hlarge, hEbound,
      hEgeneral, hpredPos, hpredNeg, hbeta, hprecision_gt, hvNum, hBoundExp⟩
  simp only [wp, PostCond.noThrow, pure,
    Axpy_opt_from_general_bound_check, Id.run, ULift.up_down]
  have hprecision_ne : precision ≠ 0 := by omega
  have huBound' : Fbounded' (beta:=beta) b u := by
    simpa [Fbounded'] using huBound
  have hEstrict :
      E < (1 / 4 : ℝ) * Fulp (beta:=beta) b 2 precision
        (FLess (beta:=beta) b 2 precision u) := by
    have h := UlpFlessuGe2_from_general_bound (beta:=beta) b 2 precision u E
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      UlpFlessuGe2_from_general_bound_check, Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨huBound, huBound', huCan, hbeta, by decide, hprecision_ne, hvNum,
        hEgeneral⟩
  have hmain := Axpy_opt_from_strict_bound
    (beta:=beta) b precision a x y t u a1 x1 y1 E
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
    Axpy_opt_from_strict_bound_check, Id.run, ULift.up_down, Int.cast_ofNat] using
    hmain ⟨haBound, hxBound, hyBound, htBound, htBound', huBound,
      htClosest, huClosest, huCan, htCan, huNormal, hlarge, hEbound, hEstrict,
      hpredPos, hpredNeg, hbeta, hprecision_gt, hvNum, hBoundExp⟩

/-- Coq: `Axpy_opt`.

The optimized Axpy error criterion implies that `u` is one of the two
extremal bounded floats around the perturbed exact result. -/
theorem Axpy_opt {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (a x y t u : FloatSpec.Core.Defs.FlocqFloat beta)
    (a1 x1 y1 : ℝ) :
    ⦃⌜Fbounded (beta:=beta) b a ∧
        Fbounded (beta:=beta) b x ∧
        Fbounded (beta:=beta) b y ∧
        Fbounded (beta:=beta) b t ∧
        Fbounded (beta:=beta) b u ∧
        Closest (beta:=beta) b (2 : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) t ∧
        Closest (beta:=beta) b (2 : ℝ)
          (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) y) u ∧
        Fcanonic (beta:=beta) 2 b u ∧
        Fcanonic (beta:=beta) 2 b t ∧
        (5 + 4 * (2 : ℝ) ^ (-(precision : Int))) *
            (1 - (2 : ℝ) ^ (-(precision : Int)))⁻¹ *
            (|_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x| +
              (2 : ℝ) ^ (-b.dExp - 1)) ≤
          |_root_.F2R (beta:=beta) y| ∧
        |y1 - _root_.F2R (beta:=beta) y| +
            |a1 * x1 -
              _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x| ≤
          (2 : ℝ) ^ (-(precision : Int) - 2) *
              (1 - (2 : ℝ) ^ (1 - (precision : Int))) *
              |_root_.F2R (beta:=beta) y| -
            ((2 : ℝ) ^ (-(precision : Int) - 2) *
              |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x|) -
            (2 : ℝ) ^ (-b.dExp - 2) ∧
        beta = 2 ∧ 1 < precision ∧ b.vNum = Zpower_nat 2 precision⌝⦄
    (pure (Axpy_opt_check (beta:=beta) b precision a x y t u a1 x1 y1) :
      Id Unit)
    ⦃⇓_ => ⌜MinOrMax (beta:=beta) b 2 (a1 * x1 + y1) u⌝⦄ := by
  intro h
  rcases h with
    ⟨haBound, hxBound, hyBound, htBound, huBound, htClosest, huClosest,
      huCan, htCan, hlarge, herror, hbeta, hprecision_gt, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, Axpy_opt_check,
    Id.run, ULift.up_down]
  have hprecision : precision ≠ 0 := by omega
  have huClosestComm :
      Closest (beta:=beta) b (2 : ℝ)
        (_root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) t) u := by
    simpa [add_comm, add_left_comm, add_assoc] using huClosest
  have hdom :
      (5 + 4 * (2 : ℝ) ^ (-(precision : Int))) *
        ((|_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x| +
            (2 : ℝ) ^ (-b.dExp - 1)) *
          (1 - (2 : ℝ) ^ (-(precision : Int)))⁻¹) ≤
        |_root_.F2R (beta:=beta) y| := by
    simpa [mul_assoc, mul_left_comm, mul_comm] using hlarge
  have hscale :
      (4 : ℝ) * |_root_.F2R (beta:=beta) t| ≤
        |_root_.F2R (beta:=beta) u| := by
    have h := Axpy_scale_from_rounding_inputs (beta:=beta) b precision t y u
      (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      Axpy_scale_from_rounding_inputs_check, Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨htBound, hyBound, htClosest, huClosestComm, huBound, huCan,
        hbeta, hprecision, hvNum, hdom⟩
  have hstrict :
      (2 : ℝ) ^ (-(precision : Int) - 2) *
          (1 - (2 : ℝ) ^ (1 - (precision : Int))) *
          |_root_.F2R (beta:=beta) y| -
        ((2 : ℝ) ^ (-(precision : Int) - 2) *
          |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x|) -
        (2 : ℝ) ^ (-b.dExp - 2) <
      (1 / 4 : ℝ) * Fulp (beta:=beta) b 2 precision
        (FLess (beta:=beta) b 2 precision u) := by
    have h := UlpFlessuGe2 (beta:=beta) b precision a x y t u
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, UlpFlessuGe2_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨haBound, hxBound, hyBound, htBound, huBound, htClosest, huClosest,
        huCan, hbeta, hprecision_gt, hvNum⟩
  have hperturb :
      |_root_.F2R (beta:=beta) y - y1| +
          |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
            a1 * x1| <
        (1 / 4 : ℝ) * Fulp (beta:=beta) b 2 precision
          (FLess (beta:=beta) b 2 precision u) := by
    have herror' :
        |_root_.F2R (beta:=beta) y - y1| +
            |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
              a1 * x1| ≤
          (2 : ℝ) ^ (-(precision : Int) - 2) *
              (1 - (2 : ℝ) ^ (1 - (precision : Int))) *
              |_root_.F2R (beta:=beta) y| -
            ((2 : ℝ) ^ (-(precision : Int) - 2) *
              |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x|) -
            (2 : ℝ) ^ (-b.dExp - 2) := by
      simpa [abs_sub_comm] using herror
    exact lt_of_le_of_lt herror' hstrict
  have hmain := Axpy_tFlessu (beta:=beta) b precision a x y t u a1 x1 y1
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Axpy_tFlessu_check,
    Id.run, ULift.up_down, Int.cast_ofNat] using
    hmain ⟨haBound, hxBound, hyBound, htBound, huBound, htClosest,
      huClosest, huCan, htCan, hscale, hperturb,
      hbeta, hprecision_gt, hvNum⟩

/-- Coq: `ExactSum_Near`.

If a closest rounded sum is within the minimum exponent quantum of the exact
sum, the error float produced by `errorBoundedPlus` has mantissa zero, so the
rounded result is exact. -/
theorem ExactSum_Near {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (p q f : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) b p ∧
        Fbounded (beta:=beta) b q ∧
        Fbounded (beta:=beta) b f ∧
        Closest (beta:=beta) b (2 : ℝ)
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) f ∧
        p.Fexp = -b.dExp ∧
        |_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q -
          _root_.F2R (beta:=beta) f| < (2 : ℝ) ^ (-b.dExp) ∧
        beta = 2 ∧ 1 < precision ∧
        b.vNum = Zpower_nat 2 precision⌝⦄
    (pure (ExactSum_Near_check (beta:=beta) b precision p q f) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q =
        _root_.F2R (beta:=beta) f⌝⦄ := by
  intro h
  rcases h with
    ⟨hpBound, hqBound, _hfBound, hClosest, hpExp, hsmall,
      hbeta, hprecision_gt, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, ExactSum_Near_check,
    Id.run, ULift.up_down]
  subst beta
  have hprecision_ne : precision ≠ 0 := by omega
  have hvNum_gt : 1 < b.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ (by omega : (1 : Int) < 2) hprecision_ne
  rcases errorBoundedPlus_from_format_payload (beta:=2) b 2 precision
      rfl (by decide) hprecision_ne hvNum_gt hvNum p q f hpBound hqBound
      hClosest with
    ⟨err, herrVal, herrBound, herrExp⟩
  have hqExp : -b.dExp ≤ q.Fexp := hqBound.2
  have hminExp : min p.Fexp q.Fexp = -b.dExp := by
    rw [hpExp]
    exact min_eq_left hqExp
  have herrExp' : err.Fexp = -b.dExp := by
    rw [herrExp, hminExp]
  have hscale_pos : 0 < (2 : ℝ) ^ (-b.dExp) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have herrSmall : |_root_.F2R (beta:=2) err| < (2 : ℝ) ^ (-b.dExp) := by
    simpa [herrVal] using hsmall
  have herrAbs :
      |_root_.F2R (beta:=2) err| =
        |(err.Fnum : ℝ)| * (2 : ℝ) ^ (-b.dExp) := by
    simp [_root_.F2R, FloatSpec.Core.Defs.F2R, herrExp', abs_mul,
      abs_of_pos hscale_pos]
  have hmant_abs_lt_one : |(err.Fnum : ℝ)| < 1 := by
    have hlt : |(err.Fnum : ℝ)| * (2 : ℝ) ^ (-b.dExp) <
        1 * (2 : ℝ) ^ (-b.dExp) := by
      simpa [herrAbs] using herrSmall
    nlinarith [hscale_pos, hlt]
  have hmant_abs_int_lt_one : |err.Fnum| < 1 := by
    exact_mod_cast hmant_abs_lt_one
  have hmant_abs_nonneg : 0 ≤ |err.Fnum| := abs_nonneg err.Fnum
  have hmant_abs_zero : |err.Fnum| = 0 := by
    omega
  have hmant_zero : err.Fnum = 0 := abs_eq_zero.mp hmant_abs_zero
  have herrZero : _root_.F2R (beta:=2) err = 0 := by
    simp [_root_.F2R, FloatSpec.Core.Defs.F2R, hmant_zero]
  have hdelta_zero :
      _root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q -
        _root_.F2R (beta:=2) f = 0 := by
    rw [← herrVal, herrZero]
  exact sub_eq_zero.mp hdelta_zero

noncomputable def delta_inf_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q d : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `delta_inf`.

The first discriminant estimate is the triangle inequality around the three
rounding errors, with each error bounded by one half ulp via `ClosestUlp`. -/
theorem delta_inf_internal {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q d : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b') p ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c) q ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q) d ∧
        beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        bo.vNum = Zpower_nat radix precision⌝⦄
    (pure (delta_inf_check (beta:=beta) bo radix precision a b b' c p q d) :
      Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) d -
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| ≤
        (1 / 2 : ℝ) * Fulp (beta:=beta) bo radix precision d +
          ((1 / 2 : ℝ) * Fulp (beta:=beta) bo radix precision p +
            (1 / 2 : ℝ) * Fulp (beta:=beta) bo radix precision q)⌝⦄ := by
  intro h
  rcases h with
    ⟨hRoundp, hRoundq, hRoundd, hbeta, hradix, hprecision, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, delta_inf_check, Id.run,
    ULift.up_down]
  let A : ℝ := _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c
  let B : ℝ := _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b'
  let P : ℝ := _root_.F2R (beta:=beta) p
  let Q : ℝ := _root_.F2R (beta:=beta) q
  let D : ℝ := _root_.F2R (beta:=beta) d
  have hd_ulp :
      2 * |(P - Q) - D| ≤ Fulp (beta:=beta) bo radix precision d := by
    have h := ClosestUlp (beta:=beta) bo radix precision (P - Q) d
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, P, Q, D, Int.cast_ofNat] using
      h ⟨hRoundd.1, hbeta, hradix, hprecision, hvNum⟩
  have hp_ulp :
      2 * |B - P| ≤ Fulp (beta:=beta) bo radix precision p := by
    have h := ClosestUlp (beta:=beta) bo radix precision B p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, B, P, Int.cast_ofNat] using
      h ⟨hRoundp.1, hbeta, hradix, hprecision, hvNum⟩
  have hq_ulp :
      2 * |A - Q| ≤ Fulp (beta:=beta) bo radix precision q := by
    have h := ClosestUlp (beta:=beta) bo radix precision A q
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, A, Q, Int.cast_ofNat] using
      h ⟨hRoundq.1, hbeta, hradix, hprecision, hvNum⟩
  have hd_half :
      |(P - Q) - D| ≤
        (1 / 2 : ℝ) * Fulp (beta:=beta) bo radix precision d := by
    nlinarith
  have hp_half :
      |B - P| ≤
        (1 / 2 : ℝ) * Fulp (beta:=beta) bo radix precision p := by
    nlinarith
  have hq_half :
      |A - Q| ≤
        (1 / 2 : ℝ) * Fulp (beta:=beta) bo radix precision q := by
    nlinarith
  have htri1 :
      |D - (B - A)| ≤ |(P - Q) - D| + |(B - P) - (A - Q)| := by
    calc
      |D - (B - A)| =
          |-(((P - Q) - D) + ((B - P) - (A - Q)))| := by
            congr 1
            ring
      _ = |((P - Q) - D) + ((B - P) - (A - Q))| := by
            rw [abs_neg]
      _ ≤ |(P - Q) - D| + |(B - P) - (A - Q)| := abs_add_le _ _
  have htri2 :
      |(B - P) - (A - Q)| ≤ |B - P| + |A - Q| := by
    calc
      |(B - P) - (A - Q)| = |(B - P) + -(A - Q)| := by ring
      _ ≤ |B - P| + |-(A - Q)| := abs_add_le _ _
      _ = |B - P| + |A - Q| := by rw [abs_neg]
  calc
    |D - (B - A)| ≤ |(P - Q) - D| + |(B - P) - (A - Q)| := htri1
    _ ≤ |(P - Q) - D| + (|B - P| + |A - Q|) := by
      linarith
    _ ≤ (1 / 2 : ℝ) * Fulp (beta:=beta) bo radix precision d +
          ((1 / 2 : ℝ) * Fulp (beta:=beta) bo radix precision p +
            (1 / 2 : ℝ) * Fulp (beta:=beta) bo radix precision q) := by
      linarith

/-- Coq: `delta_inf` with the `Discriminant1` section contract restored.
The radix is definitionally two, and the source's `1 < precision` hypothesis
is retained instead of weakening it to nonzero precision. -/
theorem delta_inf (bo : Fbound_skel) (precision : Nat)
    (a b b' c p q d : FloatSpec.Core.Defs.FlocqFloat 2) :
    ⦃⌜1 < precision ∧ bo.vNum = Zpower_nat 2 precision ∧
        EvenClosest (beta:=2) bo (2 : ℝ) precision
          (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b') p ∧
        EvenClosest (beta:=2) bo (2 : ℝ) precision
          (_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c) q ∧
        EvenClosest (beta:=2) bo (2 : ℝ) precision
          (_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q) d⌝⦄
    (pure (delta_inf_check (beta:=2) bo 2 precision a b b' c p q d) :
      Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=2) d -
          (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b' -
            _root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c)| ≤
        (1 / 2 : ℝ) * Fulp (beta:=2) bo 2 precision d +
          ((1 / 2 : ℝ) * Fulp (beta:=2) bo 2 precision p +
            (1 / 2 : ℝ) * Fulp (beta:=2) bo 2 precision q)⌝⦄ := by
  intro ⟨hprecision, hvNum, hRoundp, hRoundq, hRoundd⟩
  have hprecision_ne : precision ≠ 0 := by omega
  exact delta_inf_internal (beta:=2) bo 2 precision a b b' c p q d
    ⟨hRoundp, hRoundq, hRoundd, rfl, by norm_num, hprecision_ne, hvNum⟩

noncomputable def discri1_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q d : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq discriminant theorem `discri1`, factored at the branch ulp comparison.

Upstream derives the final ulp-comparison disjunction from the first
discriminant case split.  This Lean counterpart keeps that comparison package
explicit and composes it with `delta_inf`, so the exact lower name is present
without hiding the remaining case-split payload. -/
theorem discri1_from_ulp_comparison_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q d : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b') p ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c) q ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q) d ∧
        beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        bo.vNum = Zpower_nat radix precision ∧
        ((Fulp (beta:=beta) bo radix precision p ≤
              2 * Fulp (beta:=beta) bo radix precision d ∧
            2 * Fulp (beta:=beta) bo radix precision q ≤
              Fulp (beta:=beta) bo radix precision p) ∨
          (Fulp (beta:=beta) bo radix precision q ≤
              2 * Fulp (beta:=beta) bo radix precision d ∧
            2 * Fulp (beta:=beta) bo radix precision p ≤
              Fulp (beta:=beta) bo radix precision q) ∨
          (Fulp (beta:=beta) bo radix precision p ≤
              Fulp (beta:=beta) bo radix precision d ∧
            Fulp (beta:=beta) bo radix precision q ≤
              Fulp (beta:=beta) bo radix precision d))⌝⦄
    (pure (discri1_check (beta:=beta) bo radix precision a b b' c p q d) :
      Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) d -
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| ≤
        2 * Fulp (beta:=beta) bo radix precision d⌝⦄ := by
  intro hpre
  rcases hpre with
    ⟨hRoundp, hRoundq, hRoundd, hbeta, hradix, hprecision, hvNum, hcmp⟩
  simp only [wp, PostCond.noThrow, pure, discri1_check, Id.run, ULift.up_down]
  let ud := Fulp (beta:=beta) bo radix precision d
  let up := Fulp (beta:=beta) bo radix precision p
  let uq := Fulp (beta:=beta) bo radix precision q
  have hdelta :
      |_root_.F2R (beta:=beta) d -
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| ≤
        (1 / 2 : ℝ) * ud + ((1 / 2 : ℝ) * up + (1 / 2 : ℝ) * uq) := by
    have h := delta_inf_internal (beta:=beta) bo radix precision a b b' c p q d
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, delta_inf_check, Id.run,
      ULift.up_down, ud, up, uq, Int.cast_ofNat] using
      h ⟨hRoundp, hRoundq, hRoundd, hbeta, hradix, hprecision, hvNum⟩
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (lt_trans Int.zero_lt_one hradix)
  have hud_nonneg : 0 ≤ ud := by
    dsimp [ud, Fulp]
    exact le_of_lt (zpow_pos hradix_pos _)
  have htail :
      (1 / 2 : ℝ) * up + (1 / 2 : ℝ) * uq ≤
        (3 / 2 : ℝ) * ud := by
    rcases hcmp with hleft | hrest
    · rcases hleft with ⟨hup, huq⟩
      nlinarith
    · rcases hrest with hright | hmid
      · rcases hright with ⟨huq, hup⟩
        nlinarith
      · rcases hmid with ⟨hup, huq⟩
        nlinarith
  calc
    |_root_.F2R (beta:=beta) d -
        (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
          _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)|
        ≤ (1 / 2 : ℝ) * ud + ((1 / 2 : ℝ) * up + (1 / 2 : ℝ) * uq) := hdelta
    _ ≤ 2 * ud := by nlinarith

/-- Coq `discri1` (Discriminant1), with its source section hypotheses.

The ulp-comparison disjunction used by the factored proof is derived here from
the first-case inequality, normality, and nearest-even monotonicity. -/
theorem discri1 {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q d : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = 2 ∧ radix = 2 ∧ 1 < precision ∧
        bo.vNum = Zpower_nat radix precision ∧
        Fbounded (beta:=beta) bo a ∧ Fbounded (beta:=beta) bo b ∧
        Fbounded (beta:=beta) bo b' ∧ Fbounded (beta:=beta) bo c ∧
        Fbounded (beta:=beta) bo p ∧ Fbounded (beta:=beta) bo q ∧
        Fbounded (beta:=beta) bo d ∧
        -bo.dExp ≤ d.Fexp - 1 ∧
        Fnormal (beta:=beta) radix bo d ∧
        Fnormal (beta:=beta) radix bo q ∧
        Fnormal (beta:=beta) radix bo p ∧
        0 ≤ _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b') p ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c) q ∧
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q ≤
          3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q) d⌝⦄
    (pure (discri1_check (beta:=beta) bo radix precision a b b' c p q d) :
      Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) d -
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| ≤
        2 * Fulp (beta:=beta) bo radix precision d⌝⦄ := by
  intro hpre
  rcases hpre with
    ⟨hbeta, hradix, hprecision, hvNum, _haBound, _hbBound, _hb'Bound,
      _hcBound, hpBound, hqBound, hdBound, hU1, hdNormal, hqNormal,
      hpNormal, hSquare, hRoundp, hRoundq, hFirst, hRoundd⟩
  subst beta
  subst radix
  simp only [wp, PostCond.noThrow, pure, discri1_check, Id.run, ULift.up_down]
  have hprecision_ne : precision ≠ 0 := by omega
  have hpNonneg : 0 ≤ _root_.F2R (beta:=2) p :=
    P_positive bo (2 : ℝ)
      (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b') p
      hRoundp.1 hSquare
  have hMono : MonotoneP_float (EvenClosest (beta:=2) bo (2 : ℝ) precision) := by
    have h := EvenClosestMonotone_from_split_radix_payload (beta:=2) bo (2 : ℝ) precision
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestMonotone_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
  have hUnique : UniqueP (EvenClosest (beta:=2) bo (2 : ℝ) precision) := by
    have h := EvenClosestUniqueP (beta:=2) bo 2 precision
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestUniqueP_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨rfl, by decide, hprecision, hvNum⟩
  have exactRound (f : FloatSpec.Core.Defs.FlocqFloat 2)
      (hf : Fbounded (beta:=2) bo f) :
      EvenClosest (beta:=2) bo (2 : ℝ) precision (_root_.F2R (beta:=2) f) f := by
    constructor
    · constructor
      · exact hf
      · intro g hg
        rw [sub_self, abs_zero]
        exact abs_nonneg (_root_.F2R (beta:=2) g - _root_.F2R (beta:=2) f)
    · right
      intro g hg
      have hdist := hg.2 f hf
      rw [sub_self, abs_zero] at hdist
      exact sub_eq_zero.mp (abs_eq_zero.mp
        (le_antisymm hdist
          (abs_nonneg (_root_.F2R (beta:=2) g - _root_.F2R (beta:=2) f))))
  have hcmp :
      ((Fulp (beta:=2) bo 2 precision p ≤ 2 * Fulp (beta:=2) bo 2 precision d ∧
          2 * Fulp (beta:=2) bo 2 precision q ≤ Fulp (beta:=2) bo 2 precision p) ∨
        (Fulp (beta:=2) bo 2 precision q ≤ 2 * Fulp (beta:=2) bo 2 precision d ∧
          2 * Fulp (beta:=2) bo 2 precision p ≤ Fulp (beta:=2) bo 2 precision q) ∨
        (Fulp (beta:=2) bo 2 precision p ≤ Fulp (beta:=2) bo 2 precision d ∧
          Fulp (beta:=2) bo 2 precision q ≤ Fulp (beta:=2) bo 2 precision d)) := by
    by_cases hqp : _root_.F2R (beta:=2) q ≤ _root_.F2R (beta:=2) p
    · by_cases hqNonneg : 0 ≤ _root_.F2R (beta:=2) q
      · have habs : |_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q| =
            _root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q :=
          abs_of_nonneg (sub_nonneg.mpr hqp)
        have htwiceq : 2 * _root_.F2R (beta:=2) q ≤ _root_.F2R (beta:=2) p := by
          rw [habs] at hFirst
          linarith
        have hp_le : _root_.F2R (beta:=2) p ≤
            2 * (_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q) := by
          linarith
        have hqUlp : 2 * Fulp (beta:=2) bo 2 precision q ≤
            Fulp (beta:=2) bo 2 precision p := by
          have h := Fulp_le_twice_l (beta:=2) bo precision q p
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fulp_le_twice_l_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using
            h ⟨rfl, hprecision_ne, hvNum, hqNonneg, hqNormal, hpBound, htwiceq⟩
        have hdDouble : EvenClosest (beta:=2) bo (2 : ℝ) precision
            (2 * (_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q))
            ({ Fnum := d.Fnum, Fexp := d.Fexp + 1 } :
              FloatSpec.Core.Defs.FlocqFloat 2) := by
          have h := Twice_EvenClosest_Round (beta:=2) bo precision d
            (_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q)
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Twice_EvenClosest_Round_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using
            h ⟨rfl, hprecision, hvNum, hU1, hdNormal, hRoundd⟩
        have hpUlp : Fulp (beta:=2) bo 2 precision p ≤
            2 * Fulp (beta:=2) bo 2 precision d := by
          have h := Fulp_le_twice_r_round_from_rounding_payload (beta:=2) bo precision p d
            (_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q)
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fulp_le_twice_r_round_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using
            h ⟨rfl, hprecision_ne, hvNum, hpNonneg, hpBound, hdNormal,
              hU1, hp_le, hRoundd, hdDouble, hMono, hUnique⟩
        exact Or.inl ⟨hpUlp, hqUlp⟩
      · have hqNeg : _root_.F2R (beta:=2) q < 0 := lt_of_not_ge hqNonneg
        have hp_le_input : _root_.F2R (beta:=2) p ≤
            _root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q := by linarith
        have hp_le_d : _root_.F2R (beta:=2) p ≤ _root_.F2R (beta:=2) d := by
          have h := EvenClosestMonotone2_from_properties (beta:=2) bo 2
            (2 : ℝ) precision (_root_.F2R (beta:=2) p)
            (_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q) p d
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestMonotone2_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using
            h ⟨hp_le_input, exactRound p hpBound, hRoundd, hMono, hUnique⟩
        have hpUlp : Fulp (beta:=2) bo 2 precision p ≤
            Fulp (beta:=2) bo 2 precision d := by
          have h := LeFulpPos (beta:=2) 2 bo precision p d
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, LeFulpPos_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using
            h ⟨hpBound, hdBound, hpNonneg, hp_le_d, rfl, by decide,
              hprecision_ne, hvNum⟩
        have hqAbsBound : Fbounded (beta:=2) bo (Fabs (beta:=2) q) := by
          have h := absFBounded (beta:=2) bo q
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, absFBounded_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using h hqBound
        have hdAbsBound : Fbounded (beta:=2) bo (Fabs (beta:=2) d) := by
          have h := absFBounded (beta:=2) bo d
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, absFBounded_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using h hdBound
        have hF2RAbsQ : _root_.F2R (beta:=2) (Fabs (beta:=2) q) =
            -_root_.F2R (beta:=2) q := by
          have h := Fabs_correct (beta:=2) q
          have h' : _root_.F2R (beta:=2) (Fabs (beta:=2) q) =
              |_root_.F2R (beta:=2) q| := by
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fabs_correct_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using
              h (by norm_num : (1 : Int) < 2)
          simpa [abs_of_neg hqNeg] using h'
        have hdAbsRound : EvenClosest (beta:=2) bo (2 : ℝ) precision
            |_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q|
            (Fabs (beta:=2) d) := by
          have h := EvenClosestFabs (beta:=2) bo (2 : ℝ) precision d
            (_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q)
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestFabs_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨Or.inl hdNormal, by decide, hRoundd⟩
        have hqAbs_le_input : _root_.F2R (beta:=2) (Fabs (beta:=2) q) ≤
            |_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q| := by
          rw [hF2RAbsQ, abs_of_nonneg (sub_nonneg.mpr hqp)]
          linarith
        have hqAbs_le_dAbs : _root_.F2R (beta:=2) (Fabs (beta:=2) q) ≤
            _root_.F2R (beta:=2) (Fabs (beta:=2) d) := by
          have h := EvenClosestMonotone2_from_properties (beta:=2) bo 2
            (2 : ℝ) precision (_root_.F2R (beta:=2) (Fabs (beta:=2) q))
            |_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q|
            (Fabs (beta:=2) q) (Fabs (beta:=2) d)
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestMonotone2_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using
            h ⟨hqAbs_le_input, exactRound (Fabs q) hqAbsBound,
              hdAbsRound, hMono, hUnique⟩
        have hqAbsNonneg : 0 ≤ _root_.F2R (beta:=2) (Fabs (beta:=2) q) := by
          rw [hF2RAbsQ]
          linarith
        have hqUlpAbs : Fulp (beta:=2) bo 2 precision (Fabs (beta:=2) q) ≤
            Fulp (beta:=2) bo 2 precision (Fabs (beta:=2) d) := by
          have h := LeFulpPos (beta:=2) 2 bo precision (Fabs q) (Fabs d)
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, LeFulpPos_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using
            h ⟨hqAbsBound, hdAbsBound, hqAbsNonneg, hqAbs_le_dAbs,
              rfl, by decide, hprecision_ne, hvNum⟩
        have hqFulpAbs := FulpFabs (beta:=2) bo 2 precision q
        have hdFulpAbs := FulpFabs (beta:=2) bo 2 precision d
        simp only [wp, PostCond.noThrow, pure, FulpFabs_check,
          Id.run, ULift.up_down] at hqFulpAbs hdFulpAbs
        have hqUlp : Fulp (beta:=2) bo 2 precision q ≤
            Fulp (beta:=2) bo 2 precision d := by
          rw [hqFulpAbs (by norm_num : (0 : Int) < 2),
            hdFulpAbs (by norm_num : (0 : Int) < 2)]
          exact hqUlpAbs
        exact Or.inr (Or.inr ⟨hpUlp, hqUlp⟩)
    · have hpq' : _root_.F2R (beta:=2) p < _root_.F2R (beta:=2) q :=
        lt_of_not_ge hqp
      have hqNonneg : 0 ≤ _root_.F2R (beta:=2) q :=
        le_trans hpNonneg (le_of_lt hpq')
      have habs : |_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q| =
          _root_.F2R (beta:=2) q - _root_.F2R (beta:=2) p := by
        rw [abs_of_neg (sub_neg.mpr hpq')]
        ring
      have htwicep : 2 * _root_.F2R (beta:=2) p ≤
          _root_.F2R (beta:=2) q := by
        rw [habs] at hFirst
        linarith
      have hq_le : _root_.F2R (beta:=2) q ≤
          2 * (_root_.F2R (beta:=2) q - _root_.F2R (beta:=2) p) := by
        linarith
      have hpUlp : 2 * Fulp (beta:=2) bo 2 precision p ≤
          Fulp (beta:=2) bo 2 precision q := by
        have h := Fulp_le_twice_l (beta:=2) bo precision p q
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fulp_le_twice_l_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          h ⟨rfl, hprecision_ne, hvNum, hpNonneg, hpNormal, hqBound, htwicep⟩
      have hdAbsNormal : Fnormal (beta:=2) 2 bo (Fabs (beta:=2) d) := by
        have h := FnormalFabs (beta:=2) bo 2 d
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalFabs_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h hdNormal
      have hdAbsRound : EvenClosest (beta:=2) bo (2 : ℝ) precision
          (_root_.F2R (beta:=2) q - _root_.F2R (beta:=2) p)
          (Fabs (beta:=2) d) := by
        have h := EvenClosestFabs (beta:=2) bo (2 : ℝ) precision d
          (_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q)
        have h' : EvenClosest (beta:=2) bo (2 : ℝ) precision
            |_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q|
            (Fabs (beta:=2) d) := by
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestFabs_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨Or.inl hdNormal, by decide, hRoundd⟩
        simpa [habs] using h'
      have hU1abs : -bo.dExp ≤ (Fabs (beta:=2) d).Fexp - 1 := by
        simpa [Fabs, FloatSpec.Calc.Operations.Fabs] using hU1
      have hdAbsDouble : EvenClosest (beta:=2) bo (2 : ℝ) precision
          (2 * (_root_.F2R (beta:=2) q - _root_.F2R (beta:=2) p))
          ({ Fnum := (Fabs (beta:=2) d).Fnum,
             Fexp := (Fabs (beta:=2) d).Fexp + 1 } :
            FloatSpec.Core.Defs.FlocqFloat 2) := by
        have h := Twice_EvenClosest_Round (beta:=2) bo precision (Fabs d)
          (_root_.F2R (beta:=2) q - _root_.F2R (beta:=2) p)
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Twice_EvenClosest_Round_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          h ⟨rfl, hprecision, hvNum, hU1abs, hdAbsNormal, hdAbsRound⟩
      have hqUlpAbs : Fulp (beta:=2) bo 2 precision q ≤
          2 * Fulp (beta:=2) bo 2 precision (Fabs (beta:=2) d) := by
        have h := Fulp_le_twice_r_round_from_rounding_payload (beta:=2) bo precision q (Fabs d)
          (_root_.F2R (beta:=2) q - _root_.F2R (beta:=2) p)
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fulp_le_twice_r_round_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          h ⟨rfl, hprecision_ne, hvNum, hqNonneg, hqBound, hdAbsNormal,
            hU1abs, hq_le, hdAbsRound, hdAbsDouble, hMono, hUnique⟩
      have hdFulpAbs := FulpFabs (beta:=2) bo 2 precision d
      simp only [wp, PostCond.noThrow, pure, FulpFabs_check,
        Id.run, ULift.up_down] at hdFulpAbs
      have hdFulpAbsEq : Fulp (beta:=2) bo 2 precision d =
          Fulp (beta:=2) bo 2 precision (Fabs (beta:=2) d) :=
        hdFulpAbs (by norm_num : (0 : Int) < 2)
      have hqUlp : Fulp (beta:=2) bo 2 precision q ≤
          2 * Fulp (beta:=2) bo 2 precision d := by
        rw [← hdFulpAbsEq] at hqUlpAbs
        exact hqUlpAbs
      exact Or.inr (Or.inl ⟨hqUlp, hpUlp⟩)
  have h := discri1_from_ulp_comparison_payload (beta:=2) bo 2 precision
    a b b' c p q d
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri1_check, Id.run,
    ULift.up_down, Int.cast_ofNat] using
    h ⟨hRoundp, hRoundq, hRoundd, rfl, by decide, hprecision_ne, hvNum, hcmp⟩

/-- Coq discriminant helper `dp_dq_le`.

The upstream proof bounds the exact multiplication residuals `dp` and `dq` by
one half ulp via `ClosestUlp`, then compares the two ulps using
`Fulp_le_twice_l`/`Fulp_le_twice_r`.  The two ulp-comparability facts remain
explicit here so callers can choose the appropriate first- or second-branch
normality route. -/
noncomputable def dp_dq_le_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) (dp dq : ℝ) : Unit :=
  ()

theorem dp_dq_le_from_ulp_comparison_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) (rp rq dp dq : ℝ) :
    ⦃⌜Closest (beta:=beta) bo (radix : ℝ) rp p ∧
        Closest (beta:=beta) bo (radix : ℝ) rq q ∧
        dp = rp - _root_.F2R (beta:=beta) p ∧
        dq = rq - _root_.F2R (beta:=beta) q ∧
        beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        bo.vNum = Zpower_nat radix precision ∧
        Fulp (beta:=beta) bo radix precision q ≤
          2 * Fulp (beta:=beta) bo radix precision p ∧
        Fulp (beta:=beta) bo radix precision p ≤
          2 * Fulp (beta:=beta) bo radix precision q⌝⦄
    (pure (dp_dq_le_check (beta:=beta) bo radix precision p q dp dq) : Id Unit)
    ⦃⇓_ => ⌜|dp - dq| ≤
      (3 / 2 : ℝ) *
        min (Fulp (beta:=beta) bo radix precision p)
          (Fulp (beta:=beta) bo radix precision q)⌝⦄ := by
  intro hpre
  rcases hpre with
    ⟨hRoundp, hRoundq, hdpEq, hdqEq, hbeta, hradix, hprecision, hvNum,
      hq_le_twice_p, hp_le_twice_q⟩
  simp only [wp, PostCond.noThrow, pure, dp_dq_le_check, Id.run, ULift.up_down]
  let up := Fulp (beta:=beta) bo radix precision p
  let uq := Fulp (beta:=beta) bo radix precision q
  have hpUlp :
      2 * |rp - _root_.F2R (beta:=beta) p| ≤ up := by
    have h := ClosestUlp (beta:=beta) bo radix precision rp p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, up, Int.cast_ofNat] using
      h ⟨hRoundp, hbeta, hradix, hprecision, hvNum⟩
  have hqUlp :
      2 * |rq - _root_.F2R (beta:=beta) q| ≤ uq := by
    have h := ClosestUlp (beta:=beta) bo radix precision rq q
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, uq, Int.cast_ofNat] using
      h ⟨hRoundq, hbeta, hradix, hprecision, hvNum⟩
  have hdpUlp : 2 * |dp| ≤ up := by
    simpa [hdpEq, up] using hpUlp
  have hdqUlp : 2 * |dq| ≤ uq := by
    simpa [hdqEq, uq] using hqUlp
  have htri : |dp - dq| ≤ |dp| + |dq| := by
    simpa [sub_eq_add_neg, abs_neg] using abs_add_le dp (-dq)
  have hsum_half : |dp| + |dq| ≤ (1 / 2 : ℝ) * (up + uq) := by
    nlinarith
  have hsum_min : up + uq ≤ 3 * min up uq := by
    rcases le_total up uq with hupq | hqup
    · have hmin : min up uq = up := min_eq_left hupq
      rw [hmin]
      nlinarith [hq_le_twice_p]
    · have hmin : min up uq = uq := min_eq_right hqup
      rw [hmin]
      nlinarith [hp_le_twice_q]
  calc
    |dp - dq| ≤ |dp| + |dq| := htri
    _ ≤ (1 / 2 : ℝ) * (up + uq) := hsum_half
    _ ≤ (3 / 2 : ℝ) * min up uq := by nlinarith

/-- Coq: `dp_dq_le`, with the Discriminant2 section hypotheses restored. -/
theorem dp_dq_le
    (bo : Fbound_skel) (precision : Nat)
    (a b b' c p q dp dq : FloatSpec.Core.Defs.FlocqFloat 2) :
    ⦃⌜1 < precision ∧ bo.vNum = Zpower_nat 2 precision ∧
        Fbounded (beta:=2) bo p ∧ Fbounded (beta:=2) bo q ∧
        Fnormal (beta:=2) 2 bo p ∧ Fnormal (beta:=2) 2 bo q ∧
        0 ≤ _root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b' ∧
        EvenClosest (beta:=2) bo (2 : ℝ) precision
          (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b') p ∧
        EvenClosest (beta:=2) bo (2 : ℝ) precision
          (_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c) q ∧
        3 * |_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q| <
          _root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q ∧
        _root_.F2R (beta:=2) dp =
          _root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b' -
            _root_.F2R (beta:=2) p ∧
        _root_.F2R (beta:=2) dq =
          _root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c -
            _root_.F2R (beta:=2) q⌝⦄
    (pure (dp_dq_le_check (beta:=2) bo 2 precision p q
      (_root_.F2R (beta:=2) dp) (_root_.F2R (beta:=2) dq)) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=2) dp - _root_.F2R (beta:=2) dq| ≤
      (3 / 2 : ℝ) *
        min (Fulp (beta:=2) bo 2 precision p)
          (Fulp (beta:=2) bo 2 precision q)⌝⦄ := by
  intro ⟨hprecision, hvNum, _hpBound, _hqBound, hpNormal, hqNormal, hSquare,
    hRoundp, hRoundq, hSecond, hdpEq, hdqEq⟩
  have hprecision_ne : precision ≠ 0 := by omega
  have hpNonneg : 0 ≤ _root_.F2R (beta:=2) p :=
    P_positive bo (2 : ℝ)
      (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b') p
      hRoundp.1 hSquare
  have hqPos : 0 < _root_.F2R (beta:=2) q :=
    Q_positive (_root_.F2R (beta:=2) p) (_root_.F2R (beta:=2) q)
      hpNonneg hSecond
  have hqLe : _root_.F2R (beta:=2) q ≤
      2 * _root_.F2R (beta:=2) p :=
    Q_le_two_P (_root_.F2R (beta:=2) p) (_root_.F2R (beta:=2) q)
      hqPos hSecond
  have hpLe : _root_.F2R (beta:=2) p ≤
      2 * _root_.F2R (beta:=2) q :=
    P_le_two_Q (_root_.F2R (beta:=2) p) (_root_.F2R (beta:=2) q)
      hpNonneg hSecond
  have hqUlp : Fulp (beta:=2) bo 2 precision q ≤
      2 * Fulp (beta:=2) bo 2 precision p := by
    have h := Fulp_le_twice_r (beta:=2) bo precision q p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fulp_le_twice_r_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, hprecision_ne, hvNum, le_of_lt hqPos, hRoundq.1.1,
        hpNormal, hqLe⟩
  have hpUlp : Fulp (beta:=2) bo 2 precision p ≤
      2 * Fulp (beta:=2) bo 2 precision q := by
    have h := Fulp_le_twice_r (beta:=2) bo precision p q
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fulp_le_twice_r_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, hprecision_ne, hvNum, hpNonneg, hRoundp.1.1,
        hqNormal, hpLe⟩
  exact dp_dq_le_from_ulp_comparison_payload (beta:=2) bo 2 precision p q
    (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b')
    (_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c)
    (_root_.F2R (beta:=2) dp) (_root_.F2R (beta:=2) dq)
    ⟨hRoundp.1, hRoundq.1, hdpEq, hdqEq, rfl, by decide,
      hprecision_ne, hvNum, hqUlp, hpUlp⟩

noncomputable def discri2_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q t s d : FloatSpec.Core.Defs.FlocqFloat beta)
    (dp dq : ℝ) : Unit :=
  ()

set_option maxHeartbeats 800000 in
/-- Coq discriminant theorem `discri2`, factored at the half-error branch.

The upstream proof derives the half-error premise for `s` from the same
absolute-value monotonicity argument over the rounded residual.  This Lean
statement keeps that branch fact explicit while proving the rest of the theorem
from `t_exact`, `dp_dq_le`, `EvenClosestFabs`, `EvenClosestMonotone2`,
`LeFulpPos`, and the two `ClosestUlp` estimates. -/
theorem discri2_from_half_error_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q t s d : FloatSpec.Core.Defs.FlocqFloat beta)
    (dp dq : ℝ) :
    ⦃⌜EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b') p ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c) q ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q) t ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision (dp - dq) s ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) s) d ∧
        dp = _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
          _root_.F2R (beta:=beta) p ∧
        dq = _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c -
          _root_.F2R (beta:=beta) q ∧
        Fbounded (beta:=beta) bo p ∧
        Fbounded (beta:=beta) bo q ∧
        Fbounded (beta:=beta) bo s ∧
        Fbounded (beta:=beta) bo d ∧
        Fcanonic (beta:=beta) beta bo s ∧
        Fcanonic (beta:=beta) beta bo d ∧
        0 ≤ _root_.F2R (beta:=beta) p ∧
        3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| <
          _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q ∧
        3 *
          min (Fulp (beta:=beta) bo radix precision p)
            (Fulp (beta:=beta) bo radix precision q) ≤
          |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| ∧
        |_root_.F2R (beta:=beta) s| ≤
          |_root_.F2R (beta:=beta) t| / 2 ∧
        Fulp (beta:=beta) bo radix precision q ≤
          2 * Fulp (beta:=beta) bo radix precision p ∧
        Fulp (beta:=beta) bo radix precision p ≤
          2 * Fulp (beta:=beta) bo radix precision q ∧
        MonotoneP_float (EvenClosest (beta:=beta) bo (radix : ℝ) precision) ∧
        UniqueP (EvenClosest (beta:=beta) bo (radix : ℝ) precision) ∧
        beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        bo.vNum = Zpower_nat radix precision⌝⦄
    (pure (discri2_check (beta:=beta) bo radix precision a b b' c p q t s d dp dq) :
      Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) d -
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| ≤
        2 * Fulp (beta:=beta) bo radix precision d⌝⦄ := by
  intro hpre
  rcases hpre with
    ⟨hRoundp, hRoundq, hRoundt, hRounds, hRoundd, hdpEq, hdqEq,
      hpBound, hqBound, hsBound, hdBound, hsCan, hdCan, hpNonneg,
      hSecond, hlarge, hsHalf, hq_le_twice_p, hp_le_twice_q, hMono,
      hUnique, hbeta, hradix, hprecision, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, discri2_check, Id.run, ULift.up_down]
  let A : ℝ := _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c
  let B : ℝ := _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b'
  let P : ℝ := _root_.F2R (beta:=beta) p
  let Q : ℝ := _root_.F2R (beta:=beta) q
  let T : ℝ := _root_.F2R (beta:=beta) t
  let S : ℝ := _root_.F2R (beta:=beta) s
  let D : ℝ := _root_.F2R (beta:=beta) d
  let ud := Fulp (beta:=beta) bo radix precision d
  let us := Fulp (beta:=beta) bo radix precision s
  let up := Fulp (beta:=beta) bo radix precision p
  let uq := Fulp (beta:=beta) bo radix precision q
  have hbeta_gt : 1 < beta := by omega
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (lt_trans Int.zero_lt_one hradix)
  have htExact : T = P - Q := by
    have h := t_exact (beta:=beta) bo (radix : ℝ) precision p q t
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, t_exact_check, Id.run,
      ULift.up_down, P, Q, T, Int.cast_ofNat] using
      h ⟨hbeta_gt, hpBound, hqBound, hpNonneg, by simpa [P, Q] using hSecond,
        hRoundt⟩
  have hdpdqBound :
      |dp - dq| ≤ (3 / 2 : ℝ) * min up uq := by
    have h := dp_dq_le_from_ulp_comparison_payload
      (beta:=beta) bo radix precision p q B A dp dq
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, dp_dq_le_check, Id.run,
      ULift.up_down, up, uq, B, A, Int.cast_ofNat] using
      h ⟨hRoundp.1, hRoundq.1, by simpa [B, P] using hdpEq,
        by simpa [A, Q] using hdqEq, hbeta, hradix, hprecision, hvNum,
        by simpa [up, uq] using hq_le_twice_p,
        by simpa [up, uq] using hp_le_twice_q⟩
  have hdpdq_half_T : |dp - dq| ≤ |T| / 2 := by
    have hlarge' : 3 * min up uq ≤ |T| := by
      simpa [T, P, Q, htExact] using hlarge
    nlinarith
  have hS_half : |S| ≤ |T| / 2 := by
    simpa [S, T] using hsHalf
  have hT_le : |T| ≤ |T + S| + |S| := by
    calc
      |T| = |(T + S) + -S| := by
        congr 1
        ring
      _ ≤ |T + S| + |-S| := abs_add_le _ _
      _ = |T + S| + |S| := by rw [abs_neg]
  have hhalf_le_abs_sum : |T| / 2 ≤ |T + S| := by
    nlinarith [abs_nonneg T, abs_nonneg (T + S), abs_nonneg S, hT_le, hS_half]
  have hdpdq_abs_le_target : |dp - dq| ≤ |T + S| :=
    le_trans hdpdq_half_T hhalf_le_abs_sum
  have hsAbsRound :
      EvenClosest (beta:=beta) bo (radix : ℝ) precision |dp - dq|
        (Fabs (beta:=beta) s) := by
    have h := EvenClosestFabs (beta:=beta) bo (radix : ℝ) precision s (dp - dq)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestFabs_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hsCan, hbeta_gt, hRounds⟩
  have hdAbsRound :
      EvenClosest (beta:=beta) bo (radix : ℝ) precision |T + S|
        (Fabs (beta:=beta) d) := by
    have h := EvenClosestFabs (beta:=beta) bo (radix : ℝ) precision d (T + S)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestFabs_check,
      Id.run, ULift.up_down, T, S, Int.cast_ofNat] using h ⟨hdCan, hbeta_gt, hRoundd⟩
  have hAbsLe :
      _root_.F2R (beta:=beta) (Fabs (beta:=beta) s) ≤
        _root_.F2R (beta:=beta) (Fabs (beta:=beta) d) := by
    have h := EvenClosestMonotone2_from_properties (beta:=beta) bo radix
      (radix : ℝ) precision
      |dp - dq| |T + S| (Fabs (beta:=beta) s) (Fabs (beta:=beta) d)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestMonotone2_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hdpdq_abs_le_target, hsAbsRound, hdAbsRound, hMono, hUnique⟩
  have hsAbsBound : Fbounded (beta:=beta) bo (Fabs (beta:=beta) s) := by
    have h := absFBounded (beta:=beta) bo s
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, absFBounded_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hsBound
  have hdAbsBound : Fbounded (beta:=beta) bo (Fabs (beta:=beta) d) := by
    have h := absFBounded (beta:=beta) bo d
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, absFBounded_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hdBound
  have hF2R_abs_s :
      _root_.F2R (beta:=beta) (Fabs (beta:=beta) s) = |S| := by
    have h := Fabs_correct (beta:=beta) s
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fabs_correct_check,
      Id.run, ULift.up_down, S, Int.cast_ofNat] using h hbeta_gt
  have hFulpAbsLe :
      Fulp (beta:=beta) bo radix precision (Fabs (beta:=beta) s) ≤
        Fulp (beta:=beta) bo radix precision (Fabs (beta:=beta) d) := by
    have h := LeFulpPos (beta:=beta) radix bo precision
      (Fabs (beta:=beta) s) (Fabs (beta:=beta) d)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, LeFulpPos_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hsAbsBound, hdAbsBound, by rw [hF2R_abs_s]; exact abs_nonneg S,
        hAbsLe, hbeta, hradix, hprecision, hvNum⟩
  have hFulp_s_abs :
      us = Fulp (beta:=beta) bo radix precision (Fabs (beta:=beta) s) := by
    have h := FulpFabs (beta:=beta) bo radix precision s
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpFabs_check, Id.run,
      ULift.up_down, us, Int.cast_ofNat] using h (by exact_mod_cast (lt_trans Int.zero_lt_one hradix))
  have hFulp_d_abs :
      ud = Fulp (beta:=beta) bo radix precision (Fabs (beta:=beta) d) := by
    have h := FulpFabs (beta:=beta) bo radix precision d
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpFabs_check, Id.run,
      ULift.up_down, ud, Int.cast_ofNat] using h (by exact_mod_cast (lt_trans Int.zero_lt_one hradix))
  have hs_le_ud : us ≤ ud := by
    rw [hFulp_s_abs, hFulp_d_abs]
    exact hFulpAbsLe
  have hdUlp :
      2 * |(T + S) - D| ≤ ud := by
    have h := ClosestUlp (beta:=beta) bo radix precision (T + S) d
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, T, S, D, ud, Int.cast_ofNat] using
      h ⟨hRoundd.1, hbeta, hradix, hprecision, hvNum⟩
  have hsUlp :
      2 * |(dp - dq) - S| ≤ us := by
    have h := ClosestUlp (beta:=beta) bo radix precision (dp - dq) s
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, S, us, Int.cast_ofNat] using
      h ⟨hRounds.1, hbeta, hradix, hprecision, hvNum⟩
  have hres_eq :
      (T + S) - (B - A) = -((dp - dq) - S) := by
    dsimp [A, B, P, Q, T, S] at *
    nlinarith [htExact, hdpEq, hdqEq]
  have hres_bound :
      2 * |(T + S) - (B - A)| ≤ us := by
    rw [hres_eq, abs_neg]
    exact hsUlp
  have htri :
      |D - (B - A)| ≤ |D - (T + S)| + |(T + S) - (B - A)| := by
    calc
      |D - (B - A)| =
          |(D - (T + S)) + ((T + S) - (B - A))| := by
            congr 1
            ring
      _ ≤ |D - (T + S)| + |(T + S) - (B - A)| := abs_add_le _ _
  have hd_half : |D - (T + S)| ≤ (1 / 2 : ℝ) * ud := by
    rw [abs_sub_comm D (T + S)]
    nlinarith
  have hs_half : |(T + S) - (B - A)| ≤ (1 / 2 : ℝ) * us := by
    nlinarith
  calc
    |D - (B - A)| ≤ |D - (T + S)| + |(T + S) - (B - A)| := htri
    _ ≤ (1 / 2 : ℝ) * ud + (1 / 2 : ℝ) * us := by linarith
    _ ≤ 2 * ud := by
      have hud_nonneg : 0 ≤ ud := by
        dsimp [ud, Fulp]
        exact le_of_lt (zpow_pos hradix_pos _)
      nlinarith

/-- Coq `discri2` (Discriminant2), with the half-error and ulp-comparison
payloads reconstructed from the source hypotheses. -/
theorem discri2 {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q t dp dq s d : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = 2 ∧ radix = 2 ∧ 1 < precision ∧
        bo.vNum = Zpower_nat radix precision ∧
        Fbounded (beta:=beta) bo a ∧ Fbounded (beta:=beta) bo b ∧
        Fbounded (beta:=beta) bo b' ∧ Fbounded (beta:=beta) bo c ∧
        Fbounded (beta:=beta) bo p ∧ Fbounded (beta:=beta) bo q ∧
        Fbounded (beta:=beta) bo d ∧ Fbounded (beta:=beta) bo t ∧
        Fbounded (beta:=beta) bo s ∧ Fbounded (beta:=beta) bo dp ∧
        Fbounded (beta:=beta) bo dq ∧ Fcanonic (beta:=beta) radix bo s ∧
        -bo.dExp ≤ t.Fexp - 1 ∧
        (radix : ℝ) ^ (-bo.dExp + 2 * (precision : Int) - 1) ≤
          |_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b'| ∧
        (radix : ℝ) ^ (-bo.dExp + 2 * (precision : Int) - 1) ≤
          |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c| ∧
        Fnormal (beta:=beta) radix bo p ∧ Fnormal (beta:=beta) radix bo q ∧
        Fnormal (beta:=beta) radix bo d ∧
        0 ≤ _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b') p ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c) q ∧
        3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| <
          _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q) t ∧
        _root_.F2R (beta:=beta) dp =
          _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
            _root_.F2R (beta:=beta) p ∧
        _root_.F2R (beta:=beta) dq =
          _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c -
            _root_.F2R (beta:=beta) q ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) dp - _root_.F2R (beta:=beta) dq) s ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) s) d ∧
        _root_.F2R (beta:=beta) p ≠ _root_.F2R (beta:=beta) q ∧
        3 * min (Fulp (beta:=beta) bo radix precision p)
              (Fulp (beta:=beta) bo radix precision q) ≤
          |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q|⌝⦄
    (pure (discri2_check (beta:=beta) bo radix precision a b b' c p q t s d
      (_root_.F2R (beta:=beta) dp) (_root_.F2R (beta:=beta) dq)) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) d -
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| ≤
        2 * Fulp (beta:=beta) bo radix precision d⌝⦄ := by
  intro hpre
  rcases hpre with
    ⟨hbeta, hradix, hprecision, hvNum, _haBound, _hbBound, _hb'Bound,
      _hcBound, hpBound, hqBound, hdBound, htBound, hsBound, _hdpBound,
      _hdqBound, hsCan, hU1, _hU2, _hU3, hpNormal, hqNormal, hdNormal,
      hSquare, hRoundp, hRoundq, hSecond, hRoundt, hdpEq, hdqEq, hRounds,
      hRoundd, _hpNeq, hlarge⟩
  subst beta
  subst radix
  simp only [wp, PostCond.noThrow, pure, discri2_check, Id.run, ULift.up_down]
  have hprecision_ne : precision ≠ 0 := by omega
  have hpNonneg : 0 ≤ _root_.F2R (beta:=2) p :=
    P_positive bo (2 : ℝ)
      (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b') p
      hRoundp.1 hSquare
  have hqPos : 0 < _root_.F2R (beta:=2) q :=
    Q_positive (_root_.F2R (beta:=2) p) (_root_.F2R (beta:=2) q)
      hpNonneg hSecond
  have hqLeTwoP : _root_.F2R (beta:=2) q ≤ 2 * _root_.F2R (beta:=2) p :=
    Q_le_two_P (_root_.F2R (beta:=2) p) (_root_.F2R (beta:=2) q)
      hqPos hSecond
  have hpLeTwoQ : _root_.F2R (beta:=2) p ≤ 2 * _root_.F2R (beta:=2) q :=
    P_le_two_Q (_root_.F2R (beta:=2) p) (_root_.F2R (beta:=2) q)
      hpNonneg hSecond
  have hqUlp : Fulp (beta:=2) bo 2 precision q ≤
      2 * Fulp (beta:=2) bo 2 precision p := by
    have h := Fulp_le_twice_r (beta:=2) bo precision q p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fulp_le_twice_r_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, hprecision_ne, hvNum, le_of_lt hqPos, hqBound, hpNormal, hqLeTwoP⟩
  have hpUlp : Fulp (beta:=2) bo 2 precision p ≤
      2 * Fulp (beta:=2) bo 2 precision q := by
    have h := Fulp_le_twice_r (beta:=2) bo precision p q
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fulp_le_twice_r_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, hprecision_ne, hvNum, hpNonneg, hpBound, hqNormal, hpLeTwoQ⟩
  have htExact : _root_.F2R (beta:=2) t =
      _root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q := by
    have h := t_exact (beta:=2) bo (2 : ℝ) precision p q t
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, t_exact_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using
      h ⟨by decide, hpBound, hqBound, hpNonneg, hSecond, hRoundt⟩
  have hdpdq : |_root_.F2R (beta:=2) dp - _root_.F2R (beta:=2) dq| ≤
      (3 / 2 : ℝ) * min (Fulp (beta:=2) bo 2 precision p)
        (Fulp (beta:=2) bo 2 precision q) := by
    have h := dp_dq_le_from_ulp_comparison_payload
      (beta:=2) bo 2 precision p q
      (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b')
      (_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c)
      (_root_.F2R (beta:=2) dp) (_root_.F2R (beta:=2) dq)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, dp_dq_le_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hRoundp.1, hRoundq.1, hdpEq, hdqEq, rfl, by decide,
        hprecision_ne, hvNum, hqUlp, hpUlp⟩
  have hdpdqHalf : |_root_.F2R (beta:=2) dp - _root_.F2R (beta:=2) dq| ≤
      |_root_.F2R (beta:=2) t| / 2 := by
    rw [htExact]
    nlinarith
  let tHalf : FloatSpec.Core.Defs.FlocqFloat 2 :=
    { Fnum := t.Fnum, Fexp := t.Fexp - 1 }
  have htHalfBound : Fbounded (beta:=2) bo tHalf := by
    rcases htBound with ⟨hnum, _hexp⟩
    exact ⟨by simpa [tHalf] using hnum, by simpa [tHalf] using hU1⟩
  have htHalfVal : _root_.F2R (beta:=2) tHalf =
      _root_.F2R (beta:=2) t / 2 := by
    have hpow : (2 : ℝ) ^ t.Fexp = (2 : ℝ) ^ (t.Fexp - 1) * 2 := by
      rw [show t.Fexp = t.Fexp - 1 + 1 by omega, zpow_add₀ (by norm_num)]
      norm_num
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, tHalf,
      FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp]
    norm_num at hpow ⊢
    rw [hpow]
    ring
  have exactRound (f : FloatSpec.Core.Defs.FlocqFloat 2)
      (hf : Fbounded (beta:=2) bo f) :
      EvenClosest (beta:=2) bo (2 : ℝ) precision (_root_.F2R (beta:=2) f) f := by
    constructor
    · constructor
      · exact hf
      · intro g hg
        rw [sub_self, abs_zero]
        exact abs_nonneg (_root_.F2R (beta:=2) g - _root_.F2R (beta:=2) f)
    · right
      intro g hg
      have hdist := hg.2 f hf
      rw [sub_self, abs_zero] at hdist
      exact sub_eq_zero.mp (abs_eq_zero.mp
        (le_antisymm hdist
          (abs_nonneg (_root_.F2R (beta:=2) g - _root_.F2R (beta:=2) f))))
  have hsAbsRound : EvenClosest (beta:=2) bo (2 : ℝ) precision
      |_root_.F2R (beta:=2) dp - _root_.F2R (beta:=2) dq|
      (Fabs (beta:=2) s) := by
    have h := EvenClosestFabs (beta:=2) bo (2 : ℝ) precision s
      (_root_.F2R (beta:=2) dp - _root_.F2R (beta:=2) dq)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestFabs_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hsCan, by decide, hRounds⟩
  have htHalfAbsBound : Fbounded (beta:=2) bo (Fabs (beta:=2) tHalf) := by
    have h := absFBounded (beta:=2) bo tHalf
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, absFBounded_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h htHalfBound
  have htHalfAbsVal : _root_.F2R (beta:=2) (Fabs (beta:=2) tHalf) =
      |_root_.F2R (beta:=2) t| / 2 := by
    have h := Fabs_correct (beta:=2) tHalf
    have h' : _root_.F2R (beta:=2) (Fabs (beta:=2) tHalf) =
        |_root_.F2R (beta:=2) tHalf| := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fabs_correct_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h (by norm_num : (1 : Int) < 2)
    rw [h', htHalfVal, abs_div, abs_of_pos (by norm_num : (0 : ℝ) < 2)]
  have hMono : MonotoneP_float (EvenClosest (beta:=2) bo (2 : ℝ) precision) := by
    have h := EvenClosestMonotone_from_split_radix_payload (beta:=2) bo (2 : ℝ) precision
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestMonotone_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
  have hUnique : UniqueP (EvenClosest (beta:=2) bo (2 : ℝ) precision) := by
    have h := EvenClosestUniqueP (beta:=2) bo 2 precision
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestUniqueP_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨rfl, by decide, hprecision, hvNum⟩
  have hsAbsLe : _root_.F2R (beta:=2) (Fabs (beta:=2) s) ≤
      _root_.F2R (beta:=2) (Fabs (beta:=2) tHalf) := by
    have h := EvenClosestMonotone2_from_properties (beta:=2) bo 2
      (2 : ℝ) precision
      |_root_.F2R (beta:=2) dp - _root_.F2R (beta:=2) dq|
      (_root_.F2R (beta:=2) (Fabs (beta:=2) tHalf))
      (Fabs (beta:=2) s) (Fabs (beta:=2) tHalf)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestMonotone2_check,
      Id.run, ULift.up_down, htHalfAbsVal, Int.cast_ofNat] using
      h ⟨by simpa [htHalfAbsVal] using hdpdqHalf, hsAbsRound,
        exactRound (Fabs tHalf) htHalfAbsBound, hMono, hUnique⟩
  have hsAbsVal : _root_.F2R (beta:=2) (Fabs (beta:=2) s) =
      |_root_.F2R (beta:=2) s| := by
    have h := Fabs_correct (beta:=2) s
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fabs_correct_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h (by norm_num : (1 : Int) < 2)
  have hsHalf : |_root_.F2R (beta:=2) s| ≤ |_root_.F2R (beta:=2) t| / 2 := by
    simpa [hsAbsVal, htHalfAbsVal] using hsAbsLe
  have h := discri2_from_half_error_payload (beta:=2) bo 2 precision
    a b b' c p q t s d (_root_.F2R (beta:=2) dp) (_root_.F2R (beta:=2) dq)
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri2_check, Id.run,
    ULift.up_down, Int.cast_ofNat] using
    h ⟨hRoundp, hRoundq, hRoundt, hRounds, hRoundd, hdpEq, hdqEq,
      hpBound, hqBound, hsBound, hdBound, hsCan, Or.inl hdNormal,
      hpNonneg, hSecond, hlarge, hsHalf, hqUlp, hpUlp, hMono, hUnique,
      rfl, by decide, hprecision_ne, hvNum⟩

noncomputable def discri3_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q t s d : FloatSpec.Core.Defs.FlocqFloat beta)
    (dp dq : ℝ) : Unit :=
  ()

/-- Coq discriminant theorem `discri3`.

If the exact compensation residual `dp - dq` is representable by a bounded Pff
float, then the rounded discriminant value has the standard `2 * Fulp d` error
bound.  This is the exactness branch used by the later discriminant case split. -/
theorem discri3_from_residual_witness_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q t s d : FloatSpec.Core.Defs.FlocqFloat beta)
    (dp dq : ℝ) :
    ⦃⌜EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) s) d ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision (dp - dq) s ∧
        _root_.F2R (beta:=beta) t =
          _root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q ∧
        dp = _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
          _root_.F2R (beta:=beta) p ∧
        dq = _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c -
          _root_.F2R (beta:=beta) q ∧
        (∃ f : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded (beta:=beta) bo f ∧
            _root_.F2R (beta:=beta) f = dp - dq) ∧
        beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        bo.vNum = Zpower_nat radix precision⌝⦄
    (pure (discri3_check (beta:=beta) bo radix precision a b b' c p q t s d dp dq) :
      Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) d -
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| ≤
        2 * Fulp (beta:=beta) bo radix precision d⌝⦄ := by
  intro hpre
  rcases hpre with
    ⟨hRoundd, hRounds, htExact, hdpEq, hdqEq, ⟨f, hfBound, hfVal⟩,
      hbeta, hradix, hprecision, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, discri3_check, Id.run, ULift.up_down]
  let A : ℝ := _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c
  let B : ℝ := _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b'
  let P : ℝ := _root_.F2R (beta:=beta) p
  let Q : ℝ := _root_.F2R (beta:=beta) q
  let T : ℝ := _root_.F2R (beta:=beta) t
  let S : ℝ := _root_.F2R (beta:=beta) s
  let D : ℝ := _root_.F2R (beta:=beta) d
  have hsClosest :
      Closest (beta:=beta) bo (radix : ℝ) (_root_.F2R (beta:=beta) f) s := by
    simpa [hfVal] using hRounds.1
  have hf_eq_s :
      _root_.F2R (beta:=beta) f = _root_.F2R (beta:=beta) s := by
    have h := ClosestIdem_from_expanded_closest_payload (beta:=beta) bo (radix : ℝ) f s
    simpa [wp, PostCond.noThrow, pure, ClosestIdem_check, Fbounded'] using
      h ⟨hfBound, hsClosest, hfBound, hsClosest.1, hsClosest.2⟩
  have hsVal : S = dp - dq := by
    dsimp [S]
    exact hf_eq_s.symm.trans hfVal
  have htarget : T + S = B - A := by
    dsimp [A, B, P, Q, T, S]
    nlinarith [htExact, hsVal, hdpEq, hdqEq]
  have hdClosest :
      Closest (beta:=beta) bo (radix : ℝ) (B - A) d := by
    rw [← htarget]
    exact hRoundd.1
  have hdUlp :
      2 * |(B - A) - D| ≤ Fulp (beta:=beta) bo radix precision d := by
    have h := ClosestUlp (beta:=beta) bo radix precision (B - A) d
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, D, Int.cast_ofNat] using
      h ⟨hdClosest, hbeta, hradix, hprecision, hvNum⟩
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (lt_trans Int.zero_lt_one hradix)
  have hFulp_nonneg : 0 ≤ Fulp (beta:=beta) bo radix precision d := by
    dsimp [Fulp]
    exact le_of_lt (zpow_pos hradix_pos _)
  have hhalf :
      |(B - A) - D| ≤ (1 / 2 : ℝ) * Fulp (beta:=beta) bo radix precision d := by
    nlinarith
  calc
    |D - (B - A)| = |(B - A) - D| := abs_sub_comm D (B - A)
    _ ≤ (1 / 2 : ℝ) * Fulp (beta:=beta) bo radix precision d := hhalf
    _ ≤ 2 * Fulp (beta:=beta) bo radix precision d := by nlinarith

/-- Coq `discri3` (Discriminant2), including its source section context.

The bounded residual witness is the theorem's explicit Coq premise; the exact
rounded subtraction identity is derived from `Roundt` as in the source. -/
theorem discri3 {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q t dp dq s d : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = 2 ∧ radix = 2 ∧ 1 < precision ∧
        bo.vNum = Zpower_nat radix precision ∧
        Fbounded (beta:=beta) bo a ∧ Fbounded (beta:=beta) bo b ∧
        Fbounded (beta:=beta) bo b' ∧ Fbounded (beta:=beta) bo c ∧
        Fbounded (beta:=beta) bo p ∧ Fbounded (beta:=beta) bo q ∧
        Fbounded (beta:=beta) bo d ∧ Fbounded (beta:=beta) bo t ∧
        Fbounded (beta:=beta) bo s ∧ Fbounded (beta:=beta) bo dp ∧
        Fbounded (beta:=beta) bo dq ∧ Fcanonic (beta:=beta) radix bo s ∧
        -bo.dExp ≤ t.Fexp - 1 ∧
        (radix : ℝ) ^ (-bo.dExp + 2 * (precision : Int) - 1) ≤
          |_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b'| ∧
        (radix : ℝ) ^ (-bo.dExp + 2 * (precision : Int) - 1) ≤
          |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c| ∧
        Fnormal (beta:=beta) radix bo p ∧ Fnormal (beta:=beta) radix bo q ∧
        Fnormal (beta:=beta) radix bo d ∧
        0 ≤ _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b') p ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c) q ∧
        3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| <
          _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q) t ∧
        _root_.F2R (beta:=beta) dp =
          _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
            _root_.F2R (beta:=beta) p ∧
        _root_.F2R (beta:=beta) dq =
          _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c -
            _root_.F2R (beta:=beta) q ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) dp - _root_.F2R (beta:=beta) dq) s ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) s) d ∧
        _root_.F2R (beta:=beta) p ≠ _root_.F2R (beta:=beta) q ∧
        (∃ f : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded (beta:=beta) bo f ∧ _root_.F2R (beta:=beta) f =
            _root_.F2R (beta:=beta) dp - _root_.F2R (beta:=beta) dq)⌝⦄
    (pure (discri3_check (beta:=beta) bo radix precision a b b' c p q t s d
      (_root_.F2R (beta:=beta) dp) (_root_.F2R (beta:=beta) dq)) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) d -
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| ≤
        2 * Fulp (beta:=beta) bo radix precision d⌝⦄ := by
  intro hpre
  rcases hpre with
    ⟨hbeta, hradix, hprecision, hvNum, _haBound, _hbBound, _hb'Bound,
      _hcBound, hpBound, hqBound, _hdBound, _htBound, _hsBound, _hdpBound,
      _hdqBound, _hsCan, _hU1, _hU2, _hU3, _hpNormal, _hqNormal, _hdNormal,
      hSquare, hRoundp, _hRoundq, hSecond, hRoundt, hdpEq, hdqEq, hRounds,
      hRoundd, _hpNeq, hResidual⟩
  subst beta
  subst radix
  simp only [wp, PostCond.noThrow, pure, discri3_check, Id.run, ULift.up_down]
  have hpNonneg : 0 ≤ _root_.F2R (beta:=2) p :=
    P_positive bo (2 : ℝ)
      (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b') p
      hRoundp.1 hSquare
  have htExact : _root_.F2R (beta:=2) t =
      _root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q := by
    have h := t_exact (beta:=2) bo (2 : ℝ) precision p q t
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, t_exact_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using
      h ⟨by decide, hpBound, hqBound, hpNonneg, hSecond, hRoundt⟩
  have h := discri3_from_residual_witness_payload (beta:=2) bo 2 precision
    a b b' c p q t s d (_root_.F2R (beta:=2) dp) (_root_.F2R (beta:=2) dq)
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri3_check, Id.run,
    ULift.up_down, Int.cast_ofNat] using
    h ⟨hRoundd, hRounds, htExact, hdpEq, hdqEq, hResidual,
      rfl, by decide, (by omega : precision ≠ 0), hvNum⟩

/-- Internal arithmetic endpoint used by Coq `discri4`: two multiplication
residuals with the same exponent have a bounded exact difference.  The only
overflow case shifts a mantissa of magnitude `2^precision` by one exponent. -/
private theorem bounded_residual_difference_same_exp
    (bo : Fbound_skel) (precision : Nat)
    (sp sq : FloatSpec.Core.Defs.FlocqFloat 2)
    (hprecision : 1 < precision)
    (hvNum : bo.vNum = Zpower_nat 2 precision)
    (hspBound : Fbounded (beta:=2) bo sp)
    (hsqBound : Fbounded (beta:=2) bo sq)
    (hexp : sp.Fexp = sq.Fexp)
    (hspNum : ((|sp.Fnum| : Int) : ℝ) ≤ (2 : ℝ) ^ ((precision : Int) - 1))
    (hsqNum : ((|sq.Fnum| : Int) : ℝ) ≤ (2 : ℝ) ^ ((precision : Int) - 1)) :
    ∃ f : FloatSpec.Core.Defs.FlocqFloat 2,
      Fbounded (beta:=2) bo f ∧
      _root_.F2R (beta:=2) f =
        _root_.F2R (beta:=2) sp - _root_.F2R (beta:=2) sq := by
  let k : Nat := precision - 1
  have hk : k + 1 = precision := by omega
  have hpowCast : (2 : ℝ) ^ ((precision : Int) - 1) =
      (((2 : Int) ^ k : Int) : ℝ) := by
    have hexpNat : (precision : Int) - 1 = (k : Int) := by
      simp [k]
      omega
    rw [hexpNat, zpow_natCast]
    norm_num
  have hspNumInt : |sp.Fnum| ≤ (2 : Int) ^ k := by
    rw [hpowCast] at hspNum
    exact_mod_cast hspNum
  have hsqNumInt : |sq.Fnum| ≤ (2 : Int) ^ k := by
    rw [hpowCast] at hsqNum
    exact_mod_cast hsqNum
  let m : Int := sp.Fnum - sq.Fnum
  have hmBound : |m| ≤ bo.vNum := by
    have htwoPow : 2 * (2 : Int) ^ k = bo.vNum := by
      rw [hvNum, Zpower_nat]
      rw [← hk]
      simp [pow_succ, mul_comm]
    dsimp [m]
    rw [abs_le] at hspNumInt hsqNumInt ⊢
    omega
  by_cases hmStrict : |m| < bo.vNum
  · let f : FloatSpec.Core.Defs.FlocqFloat 2 := ⟨m, sq.Fexp⟩
    refine ⟨f, ?_, ?_⟩
    · exact ⟨by simpa [f] using hmStrict, by simpa [f] using hsqBound.2⟩
    · simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, f,
        FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp]
      dsimp [m]
      rw [hexp]
      push_cast
      ring
  · have hmEq : |m| = bo.vNum := le_antisymm hmBound (le_of_not_gt hmStrict)
    have hpowBound : (2 : Int) ^ k < bo.vNum := by
      have hpowPos : 0 < (2 : Int) ^ k := by positivity
      have htwoPow : 2 * (2 : Int) ^ k = bo.vNum := by
        rw [hvNum, Zpower_nat]
        rw [← hk]
        simp [pow_succ, mul_comm]
      omega
    by_cases hmNonneg : 0 ≤ m
    · have hmVal : m = bo.vNum := by
        rw [abs_of_nonneg hmNonneg] at hmEq
        exact hmEq
      let f : FloatSpec.Core.Defs.FlocqFloat 2 :=
        ⟨(2 : Int) ^ k, sq.Fexp + 1⟩
      refine ⟨f, ?_, ?_⟩
      · exact ⟨by simpa [f] using hpowBound,
          by change -bo.dExp ≤ sq.Fexp + 1; exact le_trans hsqBound.2 (by omega)⟩
      · have htwoPow : 2 * (2 : Int) ^ k = bo.vNum := by
          rw [hvNum, Zpower_nat]
          rw [← hk]
          simp [pow_succ, mul_comm]
        have hzpow : (2 : ℝ) ^ (sq.Fexp + 1) =
            (2 : ℝ) ^ sq.Fexp * 2 := by
          rw [zpow_add₀ (by norm_num)]
          norm_num
        simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, f,
          FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp]
        norm_num at hzpow ⊢
        rw [hzpow, hexp]
        dsimp [m] at hmVal
        have hmValR : (sp.Fnum : ℝ) - (sq.Fnum : ℝ) = (bo.vNum : ℝ) := by
          exact_mod_cast hmVal
        have htwoPowR : (2 : ℝ) * (((2 : Int) ^ k : Int) : ℝ) =
            (bo.vNum : ℝ) := by exact_mod_cast htwoPow
        push_cast at htwoPowR
        have hcastPow : (((2 : Int) ^ k : Int) : ℝ) = (2 : ℝ) ^ k := by
          norm_num
        have hdiff : (sp.Fnum : ℝ) - (sq.Fnum : ℝ) =
            2 * (((2 : Int) ^ k : Int) : ℝ) := by
          rw [hmValR, ← htwoPowR, hcastPow]
        push_cast
        rw [show (sp.Fnum : ℝ) * (2 : ℝ) ^ sq.Fexp -
            (sq.Fnum : ℝ) * (2 : ℝ) ^ sq.Fexp =
            ((sp.Fnum : ℝ) - (sq.Fnum : ℝ)) * (2 : ℝ) ^ sq.Fexp by ring,
          hdiff]
        rw [hcastPow]
        ring

    · have hmNeg : m < 0 := lt_of_not_ge hmNonneg
      have hmVal : m = -bo.vNum := by
        rw [abs_of_neg hmNeg] at hmEq
        omega
      let f : FloatSpec.Core.Defs.FlocqFloat 2 :=
        ⟨-((2 : Int) ^ k), sq.Fexp + 1⟩
      refine ⟨f, ?_, ?_⟩
      · exact ⟨by simpa [f, abs_neg] using hpowBound,
          by change -bo.dExp ≤ sq.Fexp + 1; exact le_trans hsqBound.2 (by omega)⟩
      · have htwoPow : 2 * (2 : Int) ^ k = bo.vNum := by
          rw [hvNum, Zpower_nat]
          rw [← hk]
          simp [pow_succ, mul_comm]
        have hzpow : (2 : ℝ) ^ (sq.Fexp + 1) =
            (2 : ℝ) ^ sq.Fexp * 2 := by
          rw [zpow_add₀ (by norm_num)]
          norm_num
        simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, f,
          FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp]
        norm_num at hzpow ⊢
        rw [hzpow, hexp]
        dsimp [m] at hmVal
        have hmValR : (sp.Fnum : ℝ) - (sq.Fnum : ℝ) = -(bo.vNum : ℝ) := by
          exact_mod_cast hmVal
        have htwoPowR : (2 : ℝ) * (((2 : Int) ^ k : Int) : ℝ) =
            (bo.vNum : ℝ) := by exact_mod_cast htwoPow
        push_cast at htwoPowR
        have hcastPow : (((2 : Int) ^ k : Int) : ℝ) = (2 : ℝ) ^ k := by
          norm_num
        have hdiff : (sp.Fnum : ℝ) - (sq.Fnum : ℝ) =
            -(2 * (((2 : Int) ^ k : Int) : ℝ)) := by
          rw [hmValR, ← htwoPowR, hcastPow]
        push_cast
        rw [show (sp.Fnum : ℝ) * (2 : ℝ) ^ sq.Fexp -
            (sq.Fnum : ℝ) * (2 : ℝ) ^ sq.Fexp =
            ((sp.Fnum : ℝ) - (sq.Fnum : ℝ)) * (2 : ℝ) ^ sq.Fexp by ring,
          hdiff]
        rw [hcastPow]
        ring

/-- Normalize the sole one-bit mantissa overflow possible in the binary
residual constructions. -/
private theorem bounded_dyadic_of_mantissa_le
    (bo : Fbound_skel) (precision : Nat) (m e : Int)
    (_hprecision : 1 < precision)
    (hvNum : bo.vNum = Zpower_nat 2 precision)
    (he : -bo.dExp ≤ e) (hm : |m| ≤ bo.vNum) :
    ∃ f : FloatSpec.Core.Defs.FlocqFloat 2,
      Fbounded (beta:=2) bo f ∧
      _root_.F2R (beta:=2) f = (m : ℝ) * (2 : ℝ) ^ e := by
  by_cases hlt : |m| < bo.vNum
  · exact ⟨⟨m, e⟩, ⟨hlt, he⟩, rfl⟩
  · have habs : |m| = bo.vNum := le_antisymm hm (le_of_not_gt hlt)
    let k : Nat := precision - 1
    have hk : k + 1 = precision := by omega
    have htwoPow : 2 * (2 : Int) ^ k = bo.vNum := by
      rw [hvNum, Zpower_nat, ← hk]
      simp [pow_succ, mul_comm]
    have hkPos : 0 < (2 : Int) ^ k := by positivity
    have hkBound : (2 : Int) ^ k < bo.vNum := by omega
    by_cases hm0 : 0 ≤ m
    · have hmVal : m = bo.vNum := by simpa [abs_of_nonneg hm0] using habs
      let f : FloatSpec.Core.Defs.FlocqFloat 2 := ⟨(2 : Int) ^ k, e + 1⟩
      refine ⟨f, ⟨by simpa [f] using hkBound, by dsimp [f]; omega⟩, ?_⟩
      have hzpow : (2 : ℝ) ^ (e + 1) = (2 : ℝ) ^ e * 2 := by
        rw [zpow_add₀ (by norm_num)]
        norm_num
      have htwoPowR : (2 : ℝ) * (((2 : Int) ^ k : Int) : ℝ) =
          (bo.vNum : ℝ) := by exact_mod_cast htwoPow
      have hmValR : (m : ℝ) = (bo.vNum : ℝ) := by exact_mod_cast hmVal
      simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, f,
        FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp]
      norm_num at hzpow ⊢
      rw [hzpow, hmValR, ← htwoPowR]
      push_cast
      ring
    · have hmNeg : m < 0 := lt_of_not_ge hm0
      have hmVal : m = -bo.vNum := by
        rw [abs_of_neg hmNeg] at habs
        omega
      let f : FloatSpec.Core.Defs.FlocqFloat 2 := ⟨-((2 : Int) ^ k), e + 1⟩
      refine ⟨f, ⟨by simpa [f, abs_neg] using hkBound, by dsimp [f]; omega⟩, ?_⟩
      have hzpow : (2 : ℝ) ^ (e + 1) = (2 : ℝ) ^ e * 2 := by
        rw [zpow_add₀ (by norm_num)]
        norm_num
      have htwoPowR : (2 : ℝ) * (((2 : Int) ^ k : Int) : ℝ) =
          (bo.vNum : ℝ) := by exact_mod_cast htwoPow
      have hmValR : (m : ℝ) = -(bo.vNum : ℝ) := by exact_mod_cast hmVal
      simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, f,
        FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp]
      norm_num at hzpow ⊢
      rw [hzpow, hmValR, ← htwoPowR]
      push_cast
      ring

noncomputable def discri4_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q t s d : FloatSpec.Core.Defs.FlocqFloat beta)
    (dp dq : ℝ) : Unit :=
  ()

/-- Coq discriminant theorem `discri4`, factored at the residual witness.

Upstream obtains the bounded witness for `dp - dq` from two applications of
`errorBoundedMultClosest_Can` when `p` and `q` have the same exponent.  That
canonical multiplication-error witness construction is still a separate lower
payload locally, so this theorem exposes its result and delegates the final
error estimate to `discri3`. -/
theorem discri4_from_residual_witness_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q t s d : FloatSpec.Core.Defs.FlocqFloat beta)
    (dp dq : ℝ) :
    ⦃⌜EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) s) d ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision (dp - dq) s ∧
        _root_.F2R (beta:=beta) t =
          _root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q ∧
        dp = _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
          _root_.F2R (beta:=beta) p ∧
        dq = _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c -
          _root_.F2R (beta:=beta) q ∧
        p.Fexp = q.Fexp ∧
        (∃ f : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded (beta:=beta) bo f ∧
            _root_.F2R (beta:=beta) f = dp - dq) ∧
        beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        bo.vNum = Zpower_nat radix precision⌝⦄
    (pure (discri4_check (beta:=beta) bo radix precision a b b' c p q t s d dp dq) :
      Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) d -
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| ≤
        2 * Fulp (beta:=beta) bo radix precision d⌝⦄ := by
  intro hpre
  rcases hpre with
    ⟨hRoundd, hRounds, htExact, hdpEq, hdqEq, _hpqExp, hResidual,
      hbeta, hradix, hprecision, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, discri4_check, Id.run, ULift.up_down]
  have h := discri3_from_residual_witness_payload (beta:=beta) bo radix precision a b b' c p q t s d dp dq
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri3_check, Id.run, ULift.up_down, Int.cast_ofNat] using
    h ⟨hRoundd, hRounds, htExact, hdpEq, hdqEq, hResidual,
      hbeta, hradix, hprecision, hvNum⟩

/-- Coq `discri4` (Discriminant2).  Equal exponents of the two rounded
products make the exact multiplication residuals share an exponent; their
difference is reconstructed as a bounded float internally. -/
theorem discri4 {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q t dp dq s d : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = 2 ∧ radix = 2 ∧ 1 < precision ∧
        bo.vNum = Zpower_nat radix precision ∧
        Fbounded (beta:=beta) bo a ∧ Fbounded (beta:=beta) bo b ∧
        Fbounded (beta:=beta) bo b' ∧ Fbounded (beta:=beta) bo c ∧
        Fbounded (beta:=beta) bo p ∧ Fbounded (beta:=beta) bo q ∧
        Fbounded (beta:=beta) bo d ∧ Fbounded (beta:=beta) bo t ∧
        Fbounded (beta:=beta) bo s ∧ Fbounded (beta:=beta) bo dp ∧
        Fbounded (beta:=beta) bo dq ∧ Fcanonic (beta:=beta) radix bo s ∧
        -bo.dExp ≤ t.Fexp - 1 ∧
        (radix : ℝ) ^ (-bo.dExp + 2 * (precision : Int) - 1) ≤
          |_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b'| ∧
        (radix : ℝ) ^ (-bo.dExp + 2 * (precision : Int) - 1) ≤
          |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c| ∧
        Fnormal (beta:=beta) radix bo p ∧ Fnormal (beta:=beta) radix bo q ∧
        Fnormal (beta:=beta) radix bo d ∧
        0 ≤ _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b') p ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c) q ∧
        3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| <
          _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q) t ∧
        _root_.F2R (beta:=beta) dp =
          _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
            _root_.F2R (beta:=beta) p ∧
        _root_.F2R (beta:=beta) dq =
          _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c -
            _root_.F2R (beta:=beta) q ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) dp - _root_.F2R (beta:=beta) dq) s ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) s) d ∧
        _root_.F2R (beta:=beta) p ≠ _root_.F2R (beta:=beta) q ∧
        p.Fexp = q.Fexp⌝⦄
    (pure (discri4_check (beta:=beta) bo radix precision a b b' c p q t s d
      (_root_.F2R (beta:=beta) dp) (_root_.F2R (beta:=beta) dq)) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) d -
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| ≤
        2 * Fulp (beta:=beta) bo radix precision d⌝⦄ := by
  intro hpre
  rcases hpre with
    ⟨hbeta, hradix, hprecision, hvNum, haBound, hbBound, hb'Bound,
      hcBound, hpBound, hqBound, _hdBound, _htBound, _hsBound, _hdpBound,
      _hdqBound, _hsCan, _hU1, hU2, hU3, hpNormal, hqNormal, _hdNormal,
      hSquare, hRoundp, hRoundq, hSecond, hRoundt, hdpEq, hdqEq, hRounds,
      hRoundd, _hpNeq, hpqExp⟩
  subst beta
  subst radix
  simp only [wp, PostCond.noThrow, pure, discri4_check, Id.run, ULift.up_down]
  rcases errorBoundedMultClosest_Can bo precision b b' p rfl hprecision hvNum
      hbBound hb'Bound hRoundp.1 hU2 (Or.inl hpNormal) with
    ⟨sp, hspBound, hspVal, hspExp, hspNum⟩
  rcases errorBoundedMultClosest_Can bo precision a c q rfl hprecision hvNum
      haBound hcBound hRoundq.1 hU3 (Or.inl hqNormal) with
    ⟨sq, hsqBound, hsqVal, hsqExp, hsqNum⟩
  have hsameExp : sp.Fexp = sq.Fexp := by omega
  rcases bounded_residual_difference_same_exp bo precision sp sq hprecision
      hvNum hspBound hsqBound hsameExp hspNum hsqNum with
    ⟨f, hfBound, hfVal⟩
  have hResidual : _root_.F2R (beta:=2) f =
      _root_.F2R (beta:=2) dp - _root_.F2R (beta:=2) dq := by
    rw [hfVal, hspVal, hsqVal, hdpEq, hdqEq]
  have hpNonneg : 0 ≤ _root_.F2R (beta:=2) p :=
    P_positive bo (2 : ℝ)
      (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b') p
      hRoundp.1 hSquare
  have htExact : _root_.F2R (beta:=2) t =
      _root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q := by
    have h := t_exact (beta:=2) bo (2 : ℝ) precision p q t
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, t_exact_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using
      h ⟨by decide, hpBound, hqBound, hpNonneg, hSecond, hRoundt⟩
  have h := discri3_from_residual_witness_payload (beta:=2) bo 2 precision
    a b b' c p q t s d (_root_.F2R (beta:=2) dp) (_root_.F2R (beta:=2) dq)
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri3_check, Id.run,
    ULift.up_down, Int.cast_ofNat] using
    h ⟨hRoundd, hRounds, htExact, hdpEq, hdqEq, ⟨f, hfBound, hResidual⟩,
      rfl, by decide, (by omega : precision ≠ 0), hvNum⟩

noncomputable def discri5_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q t s d : FloatSpec.Core.Defs.FlocqFloat beta)
    (dp dq : ℝ) : Unit :=
  ()

/-- Coq discriminant theorem `discri5`, factored at the residual witness.

Upstream uses the same-sign premise `0 < dp*dq` to build a bounded float for
`dp - dq` from the two canonical multiplication-error witnesses.  This local
theorem keeps that constructed witness explicit and delegates the final error
estimate to `discri3`, matching the already restored `discri4` factoring. -/
theorem discri5_from_residual_witness_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q t s d : FloatSpec.Core.Defs.FlocqFloat beta)
    (dp dq : ℝ) :
    ⦃⌜EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) s) d ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision (dp - dq) s ∧
        _root_.F2R (beta:=beta) t =
          _root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q ∧
        dp = _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
          _root_.F2R (beta:=beta) p ∧
        dq = _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c -
          _root_.F2R (beta:=beta) q ∧
        0 < dp * dq ∧
        (∃ f : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded (beta:=beta) bo f ∧
            _root_.F2R (beta:=beta) f = dp - dq) ∧
        beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        bo.vNum = Zpower_nat radix precision⌝⦄
    (pure (discri5_check (beta:=beta) bo radix precision a b b' c p q t s d dp dq) :
      Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) d -
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| ≤
        2 * Fulp (beta:=beta) bo radix precision d⌝⦄ := by
  intro hpre
  rcases hpre with
    ⟨hRoundd, hRounds, htExact, hdpEq, hdqEq, _hsameSign, hResidual,
      hbeta, hradix, hprecision, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, discri5_check, Id.run, ULift.up_down]
  have h := discri3_from_residual_witness_payload (beta:=beta) bo radix precision a b b' c p q t s d dp dq
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri3_check, Id.run, ULift.up_down, Int.cast_ofNat] using
    h ⟨hRoundd, hRounds, htExact, hdpEq, hdqEq, hResidual,
      hbeta, hradix, hprecision, hvNum⟩

/-- Coq `discri5` (Discriminant3).  Same-sign multiplication residuals are
aligned at the smaller exponent; a possible one-bit mantissa overflow is
normalized internally. -/
theorem discri5 {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q t dp dq s d : FloatSpec.Core.Defs.FlocqFloat beta)
    (e : Int) :
    ⦃⌜beta = 2 ∧ radix = 2 ∧ 1 < precision ∧
        bo.vNum = Zpower_nat radix precision ∧
        Fbounded (beta:=beta) bo a ∧ Fbounded (beta:=beta) bo b ∧
        Fbounded (beta:=beta) bo b' ∧ Fbounded (beta:=beta) bo c ∧
        Fbounded (beta:=beta) bo p ∧ Fbounded (beta:=beta) bo q ∧
        Fbounded (beta:=beta) bo d ∧ Fbounded (beta:=beta) bo t ∧
        Fbounded (beta:=beta) bo s ∧ Fbounded (beta:=beta) bo dp ∧
        Fbounded (beta:=beta) bo dq ∧ Fcanonic (beta:=beta) radix bo s ∧
        -bo.dExp ≤ d.Fexp - 1 ∧
        (radix : ℝ) ^ (-bo.dExp + 2 * (precision : Int) - 1) ≤
          |_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b'| ∧
        (radix : ℝ) ^ (-bo.dExp + 2 * (precision : Int) - 1) ≤
          |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c| ∧
        Fnormal (beta:=beta) radix bo p ∧ Fnormal (beta:=beta) radix bo q ∧
        Fnormal (beta:=beta) radix bo d ∧
        0 ≤ _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b') p ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c) q ∧
        0 ≤ _root_.F2R (beta:=beta) p ∧ 0 ≤ _root_.F2R (beta:=beta) q ∧
        3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| <
          _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q) t ∧
        _root_.F2R (beta:=beta) dp =
          _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
            _root_.F2R (beta:=beta) p ∧
        _root_.F2R (beta:=beta) dq =
          _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c -
            _root_.F2R (beta:=beta) q ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) dp - _root_.F2R (beta:=beta) dq) s ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) s) d ∧
        _root_.F2R (beta:=beta) p ≠ _root_.F2R (beta:=beta) q ∧
        p = ⟨Zpower_nat radix (precision - 1), e + 1⟩ ∧
        _root_.F2R (beta:=beta) p = (radix : ℝ) ^ ((precision : Int) + e) ∧
        q.Fexp = e ∧
        0 < _root_.F2R (beta:=beta) dp * _root_.F2R (beta:=beta) dq⌝⦄
    (pure (discri5_check (beta:=beta) bo radix precision a b b' c p q t s d
      (_root_.F2R (beta:=beta) dp) (_root_.F2R (beta:=beta) dq)) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) d -
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| ≤
        2 * Fulp (beta:=beta) bo radix precision d⌝⦄ := by
  intro hpre
  rcases hpre with
    ⟨hbeta, hradix, hprecision, hvNum, haBound, hbBound, hb'Bound,
      hcBound, hpBound, hqBound, _hdBound, _htBound, _hsBound, _hdpBound,
      _hdqBound, _hsCan, _hU1, hU2, hU3, hpNormal, hqNormal, _hdNormal,
      _hSquare, hRoundp, hRoundq, hpNonneg, _hqNonneg, hSecond, hRoundt,
      hdpEq, hdqEq, hRounds, hRoundd, _hpNeq, hpForm, _hpVal, hqExp,
      hsameSign⟩
  subst beta
  subst radix
  simp only [wp, PostCond.noThrow, pure, discri5_check, Id.run, ULift.up_down]
  rcases errorBoundedMultClosest_Can bo precision b b' p rfl hprecision hvNum
      hbBound hb'Bound hRoundp.1 hU2 (Or.inl hpNormal) with
    ⟨sp, hspBound, hspVal, hspExp, hspNum⟩
  rcases errorBoundedMultClosest_Can bo precision a c q rfl hprecision hvNum
      haBound hcBound hRoundq.1 hU3 (Or.inl hqNormal) with
    ⟨sq, hsqBound, hsqVal, hsqExp, hsqNum⟩
  have hpExp : p.Fexp = e + 1 := by simpa [hpForm]
  have hspSqExp : sp.Fexp = sq.Fexp + 1 := by omega
  let k : Nat := precision - 1
  have hpowCast : (2 : ℝ) ^ ((precision : Int) - 1) =
      (((2 : Int) ^ k : Int) : ℝ) := by
    have hexpNat : (precision : Int) - 1 = (k : Int) := by simp [k]; omega
    rw [hexpNat, zpow_natCast]
    norm_num
  have hspNumInt : |sp.Fnum| ≤ (2 : Int) ^ k := by
    rw [hpowCast] at hspNum
    exact_mod_cast hspNum
  have hsqNumInt : |sq.Fnum| ≤ (2 : Int) ^ k := by
    rw [hpowCast] at hsqNum
    exact_mod_cast hsqNum
  have hresProduct : 0 < _root_.F2R (beta:=2) sp * _root_.F2R (beta:=2) sq := by
    rw [hspVal, hsqVal, ← hdpEq, ← hdqEq]
    exact hsameSign
  have hsign : (0 < sp.Fnum ∧ 0 < sq.Fnum) ∨
      (sp.Fnum < 0 ∧ sq.Fnum < 0) := by
    rcases (mul_pos_iff.mp hresProduct) with hpos | hneg
    · left
      constructor
      · have hsppow : 0 < (2 : ℝ) ^ sp.Fexp := zpow_pos (by norm_num) _
        simp only [_root_.F2R, FloatSpec.Core.Defs.F2R] at hpos
        norm_num at hpos
        have : (0 : ℝ) < (sp.Fnum : ℝ) := by
          rcases mul_pos_iff.mp hpos.1 with h | h
          · exact h.1
          · linarith [h.2, hsppow]
        exact_mod_cast this
      · have hsqpow : 0 < (2 : ℝ) ^ sq.Fexp := zpow_pos (by norm_num) _
        simp only [_root_.F2R, FloatSpec.Core.Defs.F2R] at hpos
        norm_num at hpos
        have : (0 : ℝ) < (sq.Fnum : ℝ) := by
          rcases mul_pos_iff.mp hpos.2 with h | h
          · exact h.1
          · linarith [h.2, hsqpow]
        exact_mod_cast this
    · right
      constructor
      · have hsppow : 0 < (2 : ℝ) ^ sp.Fexp := zpow_pos (by norm_num) _
        simp only [_root_.F2R, FloatSpec.Core.Defs.F2R] at hneg
        norm_num at hneg
        have : (sp.Fnum : ℝ) < 0 := by
          rcases mul_neg_iff.mp hneg.1 with h | h
          · linarith [h.2, hsppow]
          · exact h.1
        exact_mod_cast this
      · have hsqpow : 0 < (2 : ℝ) ^ sq.Fexp := zpow_pos (by norm_num) _
        simp only [_root_.F2R, FloatSpec.Core.Defs.F2R] at hneg
        norm_num at hneg
        have : (sq.Fnum : ℝ) < 0 := by
          rcases mul_neg_iff.mp hneg.2 with h | h
          · linarith [h.2, hsqpow]
          · exact h.1
        exact_mod_cast this
  let m : Int := 2 * sp.Fnum - sq.Fnum
  have hm : |m| ≤ bo.vNum := by
    have hk : k + 1 = precision := by omega
    have htwoPow : 2 * (2 : Int) ^ k = bo.vNum := by
      rw [hvNum, Zpower_nat, ← hk]
      simp [pow_succ, mul_comm]
    dsimp [m]
    rcases hsign with hpos | hneg
    · rcases hpos with ⟨hspPos, hsqPos⟩
      rw [abs_of_pos hspPos] at hspNumInt
      rw [abs_of_pos hsqPos] at hsqNumInt
      rw [abs_le]
      constructor <;> omega
    · rcases hneg with ⟨hspNeg, hsqNeg⟩
      rw [abs_of_neg hspNeg] at hspNumInt
      rw [abs_of_neg hsqNeg] at hsqNumInt
      rw [abs_le]
      constructor <;> omega
  rcases bounded_dyadic_of_mantissa_le bo precision m sq.Fexp hprecision
      hvNum hsqBound.2 hm with ⟨f, hfBound, hfVal⟩
  have hdiffVal : _root_.F2R (beta:=2) f =
      _root_.F2R (beta:=2) sp - _root_.F2R (beta:=2) sq := by
    rw [hfVal]
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R]
    dsimp [m]
    rw [hspSqExp, zpow_add₀ (by norm_num)]
    norm_num
    push_cast
    ring
  have hResidual : _root_.F2R (beta:=2) f =
      _root_.F2R (beta:=2) dp - _root_.F2R (beta:=2) dq := by
    rw [hdiffVal, hspVal, hsqVal, hdpEq, hdqEq]
  have htExact : _root_.F2R (beta:=2) t =
      _root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q := by
    have h := t_exact (beta:=2) bo (2 : ℝ) precision p q t
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, t_exact_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using
      h ⟨by decide, hpBound, hqBound, hpNonneg, hSecond, hRoundt⟩
  have h := discri3_from_residual_witness_payload (beta:=2) bo 2 precision
    a b b' c p q t s d (_root_.F2R (beta:=2) dp) (_root_.F2R (beta:=2) dq)
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri3_check, Id.run,
    ULift.up_down, Int.cast_ofNat] using
    h ⟨hRoundd, hRounds, htExact, hdpEq, hdqEq, ⟨f, hfBound, hResidual⟩,
      rfl, by decide, (by omega : precision ≠ 0), hvNum⟩

noncomputable def discri6_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q t s d : FloatSpec.Core.Defs.FlocqFloat beta)
    (dp dq : ℝ) : Unit :=
  ()

/-- Coq discriminant theorem `discri6`, factored through the second-branch
payload.

Upstream uses the sign assumptions `0 < dp` and `dq < 0` to derive the
half-error and monotonicity facts consumed by the second discriminant branch.
Those branch facts remain explicit locally; this restores the same lower name
and delegates the final estimate to `discri2`. -/
theorem discri6_from_half_error_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q t s d : FloatSpec.Core.Defs.FlocqFloat beta)
    (dp dq : ℝ) :
    ⦃⌜EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b') p ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c) q ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q) t ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision (dp - dq) s ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) s) d ∧
        dp = _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
          _root_.F2R (beta:=beta) p ∧
        dq = _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c -
          _root_.F2R (beta:=beta) q ∧
        Fbounded (beta:=beta) bo p ∧
        Fbounded (beta:=beta) bo q ∧
        Fbounded (beta:=beta) bo s ∧
        Fbounded (beta:=beta) bo d ∧
        Fcanonic (beta:=beta) beta bo s ∧
        Fcanonic (beta:=beta) beta bo d ∧
        0 ≤ _root_.F2R (beta:=beta) p ∧
        3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| <
          _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q ∧
        3 *
          min (Fulp (beta:=beta) bo radix precision p)
            (Fulp (beta:=beta) bo radix precision q) ≤
          |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| ∧
        |_root_.F2R (beta:=beta) s| ≤
          |_root_.F2R (beta:=beta) t| / 2 ∧
        Fulp (beta:=beta) bo radix precision q ≤
          2 * Fulp (beta:=beta) bo radix precision p ∧
        Fulp (beta:=beta) bo radix precision p ≤
          2 * Fulp (beta:=beta) bo radix precision q ∧
        MonotoneP_float (EvenClosest (beta:=beta) bo (radix : ℝ) precision) ∧
        UniqueP (EvenClosest (beta:=beta) bo (radix : ℝ) precision) ∧
        0 < dp ∧ (dq < 0) ∧
        beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        bo.vNum = Zpower_nat radix precision⌝⦄
    (pure (discri6_check (beta:=beta) bo radix precision a b b' c p q t s d dp dq) :
      Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) d -
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| ≤
        2 * Fulp (beta:=beta) bo radix precision d⌝⦄ := by
  intro hpre
  rcases hpre with
    ⟨hRoundp, hRoundq, hRoundt, hRounds, hRoundd, hdpEq, hdqEq,
      hpBound, hqBound, hsBound, hdBound, hsCan, hdCan, hpNonneg,
      hSecond, hlarge, hsHalf, hq_le_twice_p, hp_le_twice_q, hMono,
      hUnique, _hdp_pos, _hdq_neg, hbeta, hradix, hprecision, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, discri6_check, Id.run, ULift.up_down]
  have h := discri2_from_half_error_payload (beta:=beta) bo radix precision a b b' c p q t s d dp dq
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri2_check, Id.run, ULift.up_down, Int.cast_ofNat] using
    h ⟨hRoundp, hRoundq, hRoundt, hRounds, hRoundd, hdpEq, hdqEq,
      hpBound, hqBound, hsBound, hdBound, hsCan, hdCan, hpNonneg,
      hSecond, hlarge, hsHalf, hq_le_twice_p, hp_le_twice_q, hMono,
      hUnique, hbeta, hradix, hprecision, hvNum⟩

/-- Coq `discri6` (Discriminant3), the `0 < dp`, `dq < 0` branch. -/
theorem discri6 {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q t dp dq s d : FloatSpec.Core.Defs.FlocqFloat beta)
    (e : Int) :
    ⦃⌜beta = 2 ∧ radix = 2 ∧ 1 < precision ∧
        bo.vNum = Zpower_nat radix precision ∧
        Fbounded (beta:=beta) bo a ∧ Fbounded (beta:=beta) bo b ∧
        Fbounded (beta:=beta) bo b' ∧ Fbounded (beta:=beta) bo c ∧
        Fbounded (beta:=beta) bo p ∧ Fbounded (beta:=beta) bo q ∧
        Fbounded (beta:=beta) bo d ∧ Fbounded (beta:=beta) bo t ∧
        Fbounded (beta:=beta) bo s ∧ Fbounded (beta:=beta) bo dp ∧
        Fbounded (beta:=beta) bo dq ∧ Fcanonic (beta:=beta) radix bo s ∧
        -bo.dExp ≤ d.Fexp - 1 ∧
        (radix : ℝ) ^ (-bo.dExp + 2 * (precision : Int) - 1) ≤
          |_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b'| ∧
        (radix : ℝ) ^ (-bo.dExp + 2 * (precision : Int) - 1) ≤
          |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c| ∧
        Fnormal (beta:=beta) radix bo p ∧ Fnormal (beta:=beta) radix bo q ∧
        Fnormal (beta:=beta) radix bo d ∧
        0 ≤ _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b') p ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c) q ∧
        0 ≤ _root_.F2R (beta:=beta) p ∧ 0 ≤ _root_.F2R (beta:=beta) q ∧
        3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| <
          _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q) t ∧
        _root_.F2R (beta:=beta) dp =
          _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
            _root_.F2R (beta:=beta) p ∧
        _root_.F2R (beta:=beta) dq =
          _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c -
            _root_.F2R (beta:=beta) q ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) dp - _root_.F2R (beta:=beta) dq) s ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) s) d ∧
        _root_.F2R (beta:=beta) p ≠ _root_.F2R (beta:=beta) q ∧
        p = ⟨Zpower_nat radix (precision - 1), e + 1⟩ ∧
        _root_.F2R (beta:=beta) p = (radix : ℝ) ^ ((precision : Int) + e) ∧
        q.Fexp = e ∧ 0 < _root_.F2R (beta:=beta) dp ∧
        _root_.F2R (beta:=beta) dq < 0⌝⦄
    (pure (discri6_check (beta:=beta) bo radix precision a b b' c p q t s d
      (_root_.F2R (beta:=beta) dp) (_root_.F2R (beta:=beta) dq)) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) d -
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| ≤
        2 * Fulp (beta:=beta) bo radix precision d⌝⦄ := by
  intro hpre
  rcases hpre with
    ⟨hbeta, hradix, hprecision, hvNum, _haBound, _hbBound, _hb'Bound,
      _hcBound, hpBound, hqBound, hdBound, _htBound, hsBound, _hdpBound,
      _hdqBound, hsCan, hU1, _hU2, _hU3, hpNormal, hqNormal, hdNormal,
      _hSquare, hRoundp, hRoundq, hpNonneg, _hqNonneg, hSecond, hRoundt,
      hdpEq, hdqEq, hRounds, hRoundd, _hpNeq, hpForm, hpVal, hqExp,
      hdpPos, hdqNeg⟩
  subst beta
  subst radix
  simp only [wp, PostCond.noThrow, pure, discri6_check, Id.run, ULift.up_down]
  have hprecision_ne : precision ≠ 0 := by omega
  have htExact : _root_.F2R (beta:=2) t =
      _root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q := by
    have h := t_exact (beta:=2) bo (2 : ℝ) precision p q t
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, t_exact_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using
      h ⟨by decide, hpBound, hqBound, hpNonneg, hSecond, hRoundt⟩
  have hpNormEq : Fnormalize (beta:=2) 2 bo precision p = p := by
    have h := FcanonicFnormalizeEq (beta:=2) 2 bo precision p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨Or.inl hpNormal, rfl, by decide, hprecision_ne, hvNum⟩
  have hqNormEq : Fnormalize (beta:=2) 2 bo precision q = q := by
    have h := FcanonicFnormalizeEq (beta:=2) 2 bo precision q
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨Or.inl hqNormal, rfl, by decide, hprecision_ne, hvNum⟩
  have hpExp : p.Fexp = e + 1 := by simpa [hpForm]
  have hpUlp : Fulp (beta:=2) bo 2 precision p = (2 : ℝ) ^ (e + 1) := by
    simp [Fulp, hpNormEq, hpExp]
  have hqUlp : Fulp (beta:=2) bo 2 precision q = (2 : ℝ) ^ e := by
    simp [Fulp, hqNormEq, hqExp]
  have hqNumNonneg : 0 ≤ q.Fnum := by
    have hpowPos : 0 < (2 : ℝ) ^ q.Fexp := zpow_pos (by norm_num) _
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R] at _hqNonneg
    norm_num at _hqNonneg hpowPos
    have : (0 : ℝ) ≤ (q.Fnum : ℝ) := by
      by_contra hnot
      have hneg : (q.Fnum : ℝ) < 0 := lt_of_not_ge hnot
      have hprodNeg := mul_neg_of_neg_of_pos hneg hpowPos
      linarith
    exact_mod_cast this
  have hqNumLe : q.Fnum ≤ bo.vNum - 1 := by
    have := hqBound.1
    rw [abs_of_nonneg hqNumNonneg] at this
    omega
  have hvNumReal : (bo.vNum : ℝ) = (2 : ℝ) ^ (precision : Int) := by
    rw [hvNum, Zpower_nat, Int.cast_pow]
    exact (zpow_natCast (2 : ℝ) precision).symm
  have hqUpper : _root_.F2R (beta:=2) q ≤
      ((2 : ℝ) ^ (precision : Int) - 1) * (2 : ℝ) ^ e := by
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R]
    rw [hqExp]
    have hcast : (q.Fnum : ℝ) ≤ (bo.vNum : ℝ) - 1 := by exact_mod_cast hqNumLe
    rw [hvNumReal] at hcast
    exact mul_le_mul_of_nonneg_right hcast (le_of_lt (zpow_pos (by norm_num) e))
  have htLower : (2 : ℝ) ^ e ≤ _root_.F2R (beta:=2) t := by
    rw [htExact, hpVal]
    have hpowAdd : (2 : ℝ) ^ ((precision : Int) + e) =
        (2 : ℝ) ^ (precision : Int) * (2 : ℝ) ^ e := by
      rw [zpow_add₀ (by norm_num)]
    norm_num at hpVal ⊢
    rw [hpowAdd]
    nlinarith [zpow_pos (by norm_num : (0 : ℝ) < 2) e]
  have hsNonneg : 0 ≤ _root_.F2R (beta:=2) s :=
    P_positive bo (2 : ℝ)
      (_root_.F2R (beta:=2) dp - _root_.F2R (beta:=2) dq) s
      hRounds.1 (by linarith)
  have hdpErr : 2 * |_root_.F2R (beta:=2) dp| ≤
      Fulp (beta:=2) bo 2 precision p := by
    have h := ClosestUlp (beta:=2) bo 2 precision
      (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b') p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, hdpEq, Int.cast_ofNat] using
      h ⟨hRoundp.1, rfl, by decide, hprecision_ne, hvNum⟩
  have hdqErr : 2 * |_root_.F2R (beta:=2) dq| ≤
      Fulp (beta:=2) bo 2 precision q := by
    have h := ClosestUlp (beta:=2) bo 2 precision
      (_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c) q
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, hdqEq, Int.cast_ofNat] using
      h ⟨hRoundq.1, rfl, by decide, hprecision_ne, hvNum⟩
  have hresTri : |_root_.F2R (beta:=2) dp - _root_.F2R (beta:=2) dq| ≤
      |_root_.F2R (beta:=2) dp| + |_root_.F2R (beta:=2) dq| := by
    simpa [sub_eq_add_neg, abs_neg] using
      abs_add_le (_root_.F2R (beta:=2) dp) (-_root_.F2R (beta:=2) dq)
  have hinput : |_root_.F2R (beta:=2) dp - _root_.F2R (beta:=2) dq| ≤
      2 * |_root_.F2R (beta:=2) t + _root_.F2R (beta:=2) s| := by
    have hpowSucc : (2 : ℝ) ^ (e + 1) = 2 * (2 : ℝ) ^ e := by
      rw [zpow_add₀ (by norm_num)]
      norm_num
      ring
    rw [hpUlp, hpowSucc] at hdpErr
    rw [hqUlp] at hdqErr
    have htSNonneg : 0 ≤ _root_.F2R (beta:=2) t + _root_.F2R (beta:=2) s := by
      nlinarith [zpow_pos (by norm_num : (0 : ℝ) < 2) e]
    rw [abs_of_nonneg htSNonneg]
    nlinarith [hresTri, zpow_pos (by norm_num : (0 : ℝ) < 2) e]
  have hsAbsRound : EvenClosest (beta:=2) bo (2 : ℝ) precision
      |_root_.F2R (beta:=2) dp - _root_.F2R (beta:=2) dq|
      (Fabs (beta:=2) s) := by
    have h := EvenClosestFabs (beta:=2) bo (2 : ℝ) precision s
      (_root_.F2R (beta:=2) dp - _root_.F2R (beta:=2) dq)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestFabs_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hsCan, by decide, hRounds⟩
  have hdAbsNormal : Fnormal (beta:=2) 2 bo (Fabs (beta:=2) d) := by
    have h := FnormalFabs (beta:=2) bo 2 d
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalFabs_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hdNormal
  have hdAbsRound : EvenClosest (beta:=2) bo (2 : ℝ) precision
      |_root_.F2R (beta:=2) t + _root_.F2R (beta:=2) s|
      (Fabs (beta:=2) d) := by
    have h := EvenClosestFabs (beta:=2) bo (2 : ℝ) precision d
      (_root_.F2R (beta:=2) t + _root_.F2R (beta:=2) s)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestFabs_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨Or.inl hdNormal, by decide, hRoundd⟩
  have hU1Abs : -bo.dExp ≤ (Fabs (beta:=2) d).Fexp - 1 := by
    simpa [Fabs, FloatSpec.Calc.Operations.Fabs] using hU1
  let d2 : FloatSpec.Core.Defs.FlocqFloat 2 :=
    { Fnum := (Fabs (beta:=2) d).Fnum, Fexp := (Fabs (beta:=2) d).Fexp + 1 }
  have hd2Round : EvenClosest (beta:=2) bo (2 : ℝ) precision
      (2 * |_root_.F2R (beta:=2) t + _root_.F2R (beta:=2) s|) d2 := by
    have h := Twice_EvenClosest_Round (beta:=2) bo precision (Fabs d)
      |_root_.F2R (beta:=2) t + _root_.F2R (beta:=2) s|
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Twice_EvenClosest_Round_check,
      Id.run, ULift.up_down, d2, Int.cast_ofNat] using
      h ⟨rfl, hprecision, hvNum, hU1Abs, hdAbsNormal, hdAbsRound⟩
  have hMono : MonotoneP_float (EvenClosest (beta:=2) bo (2 : ℝ) precision) := by
    have h := EvenClosestMonotone_from_split_radix_payload (beta:=2) bo (2 : ℝ) precision
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestMonotone_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
  have hUnique : UniqueP (EvenClosest (beta:=2) bo (2 : ℝ) precision) := by
    have h := EvenClosestUniqueP (beta:=2) bo 2 precision
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestUniqueP_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨rfl, by decide, hprecision, hvNum⟩
  have hsLeD2 : _root_.F2R (beta:=2) (Fabs (beta:=2) s) ≤
      _root_.F2R (beta:=2) d2 := by
    have h := EvenClosestMonotone2_from_properties (beta:=2) bo 2
      (2 : ℝ) precision
      |_root_.F2R (beta:=2) dp - _root_.F2R (beta:=2) dq|
      (2 * |_root_.F2R (beta:=2) t + _root_.F2R (beta:=2) s|)
      (Fabs s) d2
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestMonotone2_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hinput, hsAbsRound, hd2Round, hMono, hUnique⟩
  have hd2Val : _root_.F2R (beta:=2) d2 =
      2 * _root_.F2R (beta:=2) (Fabs (beta:=2) d) := by
    have hzpow : (2 : ℝ) ^ ((Fabs (beta:=2) d).Fexp + 1) =
        (2 : ℝ) ^ (Fabs (beta:=2) d).Fexp * 2 := by
      rw [zpow_add₀ (by norm_num)]
      norm_num
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, d2,
      FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp]
    norm_num at hzpow ⊢
    rw [hzpow]
    ring
  have hsAbsBound : Fbounded (beta:=2) bo (Fabs (beta:=2) s) := by
    have h := absFBounded (beta:=2) bo s
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, absFBounded_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hsBound
  have hdAbsBound : Fbounded (beta:=2) bo (Fabs (beta:=2) d) := by
    have h := absFBounded (beta:=2) bo d
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, absFBounded_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hdBound
  have hsAbsNonneg : 0 ≤ _root_.F2R (beta:=2) (Fabs (beta:=2) s) := by
    have h := Fabs_correct (beta:=2) s
    have h' : _root_.F2R (beta:=2) (Fabs (beta:=2) s) =
        |_root_.F2R (beta:=2) s| := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fabs_correct_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h (by norm_num : (1 : Int) < 2)
    rw [h']
    exact abs_nonneg _
  have hsUlpAbs : Fulp (beta:=2) bo 2 precision (Fabs (beta:=2) s) ≤
      2 * Fulp (beta:=2) bo 2 precision (Fabs (beta:=2) d) := by
    have h := Fulp_le_twice_r (beta:=2) bo precision (Fabs s) (Fabs d)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fulp_le_twice_r_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, hprecision_ne, hvNum, hsAbsNonneg, hsAbsBound, hdAbsNormal,
        by rw [← hd2Val]; exact hsLeD2⟩
  have hsFulpAbs := FulpFabs (beta:=2) bo 2 precision s
  have hdFulpAbs := FulpFabs (beta:=2) bo 2 precision d
  simp only [wp, PostCond.noThrow, pure, FulpFabs_check,
    Id.run, ULift.up_down] at hsFulpAbs hdFulpAbs
  have hsUlp : Fulp (beta:=2) bo 2 precision s ≤
      2 * Fulp (beta:=2) bo 2 precision d := by
    rw [hsFulpAbs (by norm_num : (0 : Int) < 2),
      hdFulpAbs (by norm_num : (0 : Int) < 2)]
    exact hsUlpAbs
  have hdErr : 2 * |(_root_.F2R (beta:=2) t + _root_.F2R (beta:=2) s) -
      _root_.F2R (beta:=2) d| ≤ Fulp (beta:=2) bo 2 precision d := by
    have h := ClosestUlp (beta:=2) bo 2 precision
      (_root_.F2R (beta:=2) t + _root_.F2R (beta:=2) s) d
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hRoundd.1, rfl, by decide, hprecision_ne, hvNum⟩
  have hsErr : 2 * |(_root_.F2R (beta:=2) dp - _root_.F2R (beta:=2) dq) -
      _root_.F2R (beta:=2) s| ≤ Fulp (beta:=2) bo 2 precision s := by
    have h := ClosestUlp (beta:=2) bo 2 precision
      (_root_.F2R (beta:=2) dp - _root_.F2R (beta:=2) dq) s
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hRounds.1, rfl, by decide, hprecision_ne, hvNum⟩
  have hdecomp : _root_.F2R (beta:=2) d -
      (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b' -
        _root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c) =
      -((_root_.F2R (beta:=2) t + _root_.F2R (beta:=2) s) -
          _root_.F2R (beta:=2) d) -
        ((_root_.F2R (beta:=2) dp - _root_.F2R (beta:=2) dq) -
          _root_.F2R (beta:=2) s) := by
    rw [hdpEq, hdqEq, htExact]
    ring
  rw [hdecomp]
  have htri := abs_add_le
    (-((_root_.F2R (beta:=2) t + _root_.F2R (beta:=2) s) -
      _root_.F2R (beta:=2) d))
    (-((_root_.F2R (beta:=2) dp - _root_.F2R (beta:=2) dq) -
      _root_.F2R (beta:=2) s))
  rw [abs_neg, abs_neg] at htri
  have hdUlpNonneg : 0 ≤ Fulp (beta:=2) bo 2 precision d :=
    le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
  have hdHalf : |(_root_.F2R (beta:=2) t + _root_.F2R (beta:=2) s) -
      _root_.F2R (beta:=2) d| ≤ Fulp (beta:=2) bo 2 precision d / 2 := by
    linarith
  have hsHalf : |(_root_.F2R (beta:=2) dp - _root_.F2R (beta:=2) dq) -
      _root_.F2R (beta:=2) s| ≤ Fulp (beta:=2) bo 2 precision s / 2 := by
    linarith
  calc
    |-((_root_.F2R (beta:=2) t + _root_.F2R (beta:=2) s) -
        _root_.F2R (beta:=2) d) -
        ((_root_.F2R (beta:=2) dp - _root_.F2R (beta:=2) dq) -
          _root_.F2R (beta:=2) s)|
        ≤ |(_root_.F2R (beta:=2) t + _root_.F2R (beta:=2) s) -
            _root_.F2R (beta:=2) d| +
          |(_root_.F2R (beta:=2) dp - _root_.F2R (beta:=2) dq) -
            _root_.F2R (beta:=2) s| := by simpa [sub_eq_add_neg] using htri
    _ ≤ Fulp (beta:=2) bo 2 precision d / 2 +
          Fulp (beta:=2) bo 2 precision s / 2 := add_le_add hdHalf hsHalf
    _ ≤ 2 * Fulp (beta:=2) bo 2 precision d := by linarith

/-- The common Coq section context of Discriminant3 (`discri5`–`discri8`). -/
def discri3_source_context {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q t dp dq s d : FloatSpec.Core.Defs.FlocqFloat beta)
    (e : Int) : Prop :=
  beta = 2 ∧ radix = 2 ∧ 1 < precision ∧
  bo.vNum = Zpower_nat radix precision ∧
  Fbounded (beta:=beta) bo a ∧ Fbounded (beta:=beta) bo b ∧
  Fbounded (beta:=beta) bo b' ∧ Fbounded (beta:=beta) bo c ∧
  Fbounded (beta:=beta) bo p ∧ Fbounded (beta:=beta) bo q ∧
  Fbounded (beta:=beta) bo d ∧ Fbounded (beta:=beta) bo t ∧
  Fbounded (beta:=beta) bo s ∧ Fbounded (beta:=beta) bo dp ∧
  Fbounded (beta:=beta) bo dq ∧ Fcanonic (beta:=beta) radix bo s ∧
  -bo.dExp ≤ d.Fexp - 1 ∧
  (radix : ℝ) ^ (-bo.dExp + 2 * (precision : Int) - 1) ≤
    |_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b'| ∧
  (radix : ℝ) ^ (-bo.dExp + 2 * (precision : Int) - 1) ≤
    |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c| ∧
  Fnormal (beta:=beta) radix bo p ∧ Fnormal (beta:=beta) radix bo q ∧
  Fnormal (beta:=beta) radix bo d ∧
  0 ≤ _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' ∧
  EvenClosest (beta:=beta) bo (radix : ℝ) precision
    (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b') p ∧
  EvenClosest (beta:=beta) bo (radix : ℝ) precision
    (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c) q ∧
  0 ≤ _root_.F2R (beta:=beta) p ∧ 0 ≤ _root_.F2R (beta:=beta) q ∧
  3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| <
    _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q ∧
  EvenClosest (beta:=beta) bo (radix : ℝ) precision
    (_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q) t ∧
  _root_.F2R (beta:=beta) dp =
    _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
      _root_.F2R (beta:=beta) p ∧
  _root_.F2R (beta:=beta) dq =
    _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c -
      _root_.F2R (beta:=beta) q ∧
  EvenClosest (beta:=beta) bo (radix : ℝ) precision
    (_root_.F2R (beta:=beta) dp - _root_.F2R (beta:=beta) dq) s ∧
  EvenClosest (beta:=beta) bo (radix : ℝ) precision
    (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) s) d ∧
  _root_.F2R (beta:=beta) p ≠ _root_.F2R (beta:=beta) q ∧
  p = ⟨Zpower_nat radix (precision - 1), e + 1⟩ ∧
  _root_.F2R (beta:=beta) p = (radix : ℝ) ^ ((precision : Int) + e) ∧
  q.Fexp = e

noncomputable def discri7_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q t s d : FloatSpec.Core.Defs.FlocqFloat beta)
    (dp dq : ℝ) : Unit :=
  ()

/-- Coq discriminant theorem `discri7`, factored at the residual witness.

The upstream proof handles the symmetric opposite-sign branch `dp < 0` and
`0 < dq` by constructing a bounded witness for `dp - dq`.  This local theorem
keeps that constructed witness explicit, matching the factoring used by
`discri4` and `discri5`, and delegates the final estimate to `discri3`. -/
theorem discri7_from_residual_witness_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q t s d : FloatSpec.Core.Defs.FlocqFloat beta)
    (dp dq : ℝ) :
    ⦃⌜EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) s) d ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision (dp - dq) s ∧
        _root_.F2R (beta:=beta) t =
          _root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q ∧
        dp = _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
          _root_.F2R (beta:=beta) p ∧
        dq = _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c -
          _root_.F2R (beta:=beta) q ∧
        dp < 0 ∧ 0 < dq ∧
        (∃ f : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded (beta:=beta) bo f ∧
            _root_.F2R (beta:=beta) f = dp - dq) ∧
        beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        bo.vNum = Zpower_nat radix precision⌝⦄
    (pure (discri7_check (beta:=beta) bo radix precision a b b' c p q t s d dp dq) :
      Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) d -
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| ≤
        2 * Fulp (beta:=beta) bo radix precision d⌝⦄ := by
  intro hpre
  rcases hpre with
    ⟨hRoundd, hRounds, htExact, hdpEq, hdqEq, _hdp_neg, _hdq_pos, hResidual,
      hbeta, hradix, hprecision, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, discri7_check, Id.run, ULift.up_down]
  have h := discri3_from_residual_witness_payload (beta:=beta) bo radix precision a b b' c p q t s d dp dq
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri3_check, Id.run, ULift.up_down, Int.cast_ofNat] using
    h ⟨hRoundd, hRounds, htExact, hdpEq, hdqEq, hResidual,
      hbeta, hradix, hprecision, hvNum⟩

/-- Coq `discri7` (Discriminant3), the `dp < 0`, `0 < dq` branch. -/
theorem discri7 {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q t dp dq s d : FloatSpec.Core.Defs.FlocqFloat beta)
    (e : Int) :
    ⦃⌜discri3_source_context (beta:=beta) bo radix precision
          a b b' c p q t dp dq s d e ∧
        _root_.F2R (beta:=beta) dp < 0 ∧
        0 < _root_.F2R (beta:=beta) dq⌝⦄
    (pure (discri7_check (beta:=beta) bo radix precision a b b' c p q t s d
      (_root_.F2R (beta:=beta) dp) (_root_.F2R (beta:=beta) dq)) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) d -
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| ≤
        2 * Fulp (beta:=beta) bo radix precision d⌝⦄ := by
  intro hpre
  rcases hpre with ⟨hctx, hdpNeg, hdqPos⟩
  dsimp [discri3_source_context] at hctx
  rcases hctx with
    ⟨hbeta, hradix, hprecision, hvNum, haBound, hbBound, hb'Bound,
      hcBound, hpBound, hqBound, _hdBound, _htBound, _hsBound, _hdpBound,
      _hdqBound, _hsCan, _hU1, hU2, hU3, hpNormal, hqNormal, _hdNormal,
      _hSquare, hRoundp, hRoundq, hpNonneg, _hqNonneg, hSecond, hRoundt,
      hdpEq, hdqEq, hRounds, hRoundd, _hpNeq, hpForm, hpVal, hqExp⟩
  subst beta
  subst radix
  simp only [wp, PostCond.noThrow, pure, discri7_check, Id.run, ULift.up_down]
  rcases errorBoundedMultClosest_Can bo precision b b' p rfl hprecision hvNum
      hbBound hb'Bound hRoundp.1 hU2 (Or.inl hpNormal) with
    ⟨sp, hspBound, hspVal, hspExp, hspNum⟩
  rcases errorBoundedMultClosest_Can bo precision a c q rfl hprecision hvNum
      haBound hcBound hRoundq.1 hU3 (Or.inl hqNormal) with
    ⟨sq, hsqBound, hsqVal, hsqExp, hsqNum⟩
  have hpExp : p.Fexp = e + 1 := by simpa [hpForm]
  have hspSqExp : sp.Fexp = sq.Fexp + 1 := by omega
  let k : Nat := precision - 1
  have hk : k + 1 = precision := by omega
  have hpowCast : (2 : ℝ) ^ ((precision : Int) - 1) =
      (((2 : Int) ^ k : Int) : ℝ) := by
    have hexpNat : (precision : Int) - 1 = (k : Int) := by simp [k]; omega
    rw [hexpNat, zpow_natCast]
    norm_num
  have hsqNumInt : |sq.Fnum| ≤ (2 : Int) ^ k := by
    rw [hpowCast] at hsqNum
    exact_mod_cast hsqNum
  have hspValDp : _root_.F2R (beta:=2) sp = _root_.F2R (beta:=2) dp := by
    rw [hspVal, hdpEq]
  have hsqValDq : _root_.F2R (beta:=2) sq = _root_.F2R (beta:=2) dq := by
    rw [hsqVal, hdqEq]
  have hvNumGt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact_mod_cast Nat.one_lt_pow (by omega : precision ≠ 0) (by decide : 1 < 2)
  let predP : FloatSpec.Core.Defs.FlocqFloat 2 := ⟨bo.vNum - 1, e⟩
  have hpredBound : Fbounded (beta:=2) bo predP := by
    constructor
    · have hnonneg : 0 ≤ bo.vNum - 1 := by omega
      simp [predP, abs_of_nonneg hnonneg]
    · have := hqBound.2
      simpa [predP, hqExp] using this
  have hvNumReal : (bo.vNum : ℝ) = (2 : ℝ) ^ (precision : Int) := by
    rw [hvNum, Zpower_nat, Int.cast_pow]
    exact (zpow_natCast (2 : ℝ) precision).symm
  have hpredVal : _root_.F2R (beta:=2) predP =
      _root_.F2R (beta:=2) p - (2 : ℝ) ^ e := by
    rw [hpVal]
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, predP,
      FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp]
    push_cast
    rw [hvNumReal, zpow_add₀ (by norm_num)]
    ring
  have hclosestPred := hRoundp.1.2 predP hpredBound
  have hdpHalf : 2 * |_root_.F2R (beta:=2) dp| ≤ (2 : ℝ) ^ e := by
    rw [← hspValDp] at hdpNeg
    rw [← hspValDp]
    have hprodEq : _root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b' =
        _root_.F2R (beta:=2) p + _root_.F2R (beta:=2) sp := by
      rw [hspVal]
      ring
    rw [hprodEq, hpredVal] at hclosestPred
    have hpowPos : 0 < (2 : ℝ) ^ e := zpow_pos (by norm_num) _
    rw [abs_of_pos (by linarith : 0 <
      _root_.F2R (beta:=2) p -
        (_root_.F2R (beta:=2) p + _root_.F2R (beta:=2) sp))]
      at hclosestPred
    by_cases hle : |_root_.F2R (beta:=2) sp| ≤ (2 : ℝ) ^ e
    · have hspAbs : |_root_.F2R (beta:=2) sp| =
          -_root_.F2R (beta:=2) sp := abs_of_neg hdpNeg
      have hright : |(_root_.F2R (beta:=2) p - (2 : ℝ) ^ e) -
          (_root_.F2R (beta:=2) p + _root_.F2R (beta:=2) sp)| =
          (2 : ℝ) ^ e - |_root_.F2R (beta:=2) sp| := by
        have : -(2 : ℝ) ^ e - _root_.F2R (beta:=2) sp ≤ 0 := by
          rw [hspAbs] at hle
          linarith
        rw [show _root_.F2R (beta:=2) p - (2 : ℝ) ^ e -
            (_root_.F2R (beta:=2) p + _root_.F2R (beta:=2) sp) =
            -(2 : ℝ) ^ e - _root_.F2R (beta:=2) sp by ring,
          abs_of_nonpos this, hspAbs]
        ring
      rw [hright] at hclosestPred
      rw [hspAbs] at hclosestPred ⊢
      nlinarith
    · have hgt : (2 : ℝ) ^ e < |_root_.F2R (beta:=2) sp| := lt_of_not_ge hle
      have hspAbs : |_root_.F2R (beta:=2) sp| =
          -_root_.F2R (beta:=2) sp := abs_of_neg hdpNeg
      have hright : |(_root_.F2R (beta:=2) p - (2 : ℝ) ^ e) -
          (_root_.F2R (beta:=2) p + _root_.F2R (beta:=2) sp)| =
          |_root_.F2R (beta:=2) sp| - (2 : ℝ) ^ e := by
        have : 0 < -(2 : ℝ) ^ e - _root_.F2R (beta:=2) sp := by
          rw [hspAbs] at hgt
          linarith
        rw [show _root_.F2R (beta:=2) p - (2 : ℝ) ^ e -
            (_root_.F2R (beta:=2) p + _root_.F2R (beta:=2) sp) =
            -(2 : ℝ) ^ e - _root_.F2R (beta:=2) sp by ring,
          abs_of_pos this, hspAbs]
        ring
      rw [hright, hspAbs] at hclosestPred
      nlinarith
  let sp' : FloatSpec.Core.Defs.FlocqFloat 2 :=
    ⟨2 * sp.Fnum, sp.Fexp - 1⟩
  have hsp'Val : _root_.F2R (beta:=2) sp' = _root_.F2R (beta:=2) sp := by
    have hzpow : (2 : ℝ) ^ sp.Fexp = (2 : ℝ) ^ (sp.Fexp - 1) * 2 := by
      rw [show sp.Fexp = sp.Fexp - 1 + 1 by omega, zpow_add₀ (by norm_num)]
      norm_num
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, sp',
      FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp]
    norm_num at hzpow ⊢
    rw [hzpow]
    push_cast
    ring
  have hsp'Exp : sp'.Fexp = sq.Fexp := by simp [sp', hspSqExp]
  have hsp'Num : ((|sp'.Fnum| : Int) : ℝ) ≤
      (2 : ℝ) ^ ((precision : Int) - 1) := by
    have hscalePos : 0 < (2 : ℝ) ^ (sp.Fexp - 1) := zpow_pos (by norm_num) _
    have habsVal : |_root_.F2R (beta:=2) sp| =
        (((|sp'.Fnum| : Int) : ℝ) * (2 : ℝ) ^ (sp.Fexp - 1)) := by
      rw [← hsp'Val]
      simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, sp',
        FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp]
      norm_num at hscalePos
      simpa [abs_mul, abs_of_pos hscalePos, Int.cast_abs]
    have hpowIdentity : (2 : ℝ) ^ ((precision : Int) - 1) *
        (2 : ℝ) ^ (sp.Fexp - 1) = (2 : ℝ) ^ (e - 1) := by
      rw [← zpow_add₀ (by norm_num)]
      rw [hspExp, hpExp]
      congr 1
      omega
    have hpowE : (2 : ℝ) ^ e = 2 * (2 : ℝ) ^ (e - 1) := by
      rw [show e = e - 1 + 1 by omega, zpow_add₀ (by norm_num)]
      norm_num
      ring
    rw [← hspValDp] at hdpHalf
    rw [habsVal, hpowE] at hdpHalf
    apply (le_of_mul_le_mul_right ?_ hscalePos)
    rw [hpowIdentity]
    nlinarith
  have hsp'Bound : Fbounded (beta:=2) bo sp' := by
    have hnumReal : ((|sp'.Fnum| : Int) : ℝ) < (bo.vNum : ℝ) := by
      rw [hvNumReal]
      have hpowLt : (2 : ℝ) ^ ((precision : Int) - 1) <
          (2 : ℝ) ^ (precision : Int) := by
        exact (zpow_lt_zpow_iff_right₀ (by norm_num : (1 : ℝ) < 2)).2
          (by omega)
      exact lt_of_le_of_lt hsp'Num hpowLt
    constructor
    · exact_mod_cast hnumReal
    · rw [hsp'Exp]
      exact hsqBound.2
  rcases bounded_residual_difference_same_exp bo precision sp' sq hprecision
      hvNum hsp'Bound hsqBound hsp'Exp hsp'Num hsqNum with
    ⟨f, hfBound, hfVal⟩
  have hResidual : _root_.F2R (beta:=2) f =
      _root_.F2R (beta:=2) dp - _root_.F2R (beta:=2) dq := by
    rw [hfVal, hsp'Val, hspValDp, hsqValDq]
  have htExact : _root_.F2R (beta:=2) t =
      _root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q := by
    have h := t_exact (beta:=2) bo (2 : ℝ) precision p q t
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, t_exact_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using
      h ⟨by decide, hpBound, hqBound, hpNonneg, hSecond, hRoundt⟩
  have h := discri3_from_residual_witness_payload (beta:=2) bo 2 precision
    a b b' c p q t s d (_root_.F2R (beta:=2) dp) (_root_.F2R (beta:=2) dq)
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri3_check, Id.run,
    ULift.up_down, Int.cast_ofNat] using
    h ⟨hRoundd, hRounds, htExact, hdpEq, hdqEq, ⟨f, hfBound, hResidual⟩,
      rfl, by decide, (by omega : precision ≠ 0), hvNum⟩

noncomputable def discri8_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q t s d : FloatSpec.Core.Defs.FlocqFloat beta)
    (dp dq : ℝ) : Unit :=
  ()

/-- Coq discriminant theorem `discri8`, factored at the sign case split.

Upstream performs a four-way sign split on `dp` and `dq`, using exact residual
witnesses in the zero and same-sign branches, `discri6` for `0 < dp`, `dq < 0`,
and `discri7` for the symmetric opposite-sign branch.  This local theorem keeps
the still-unported residual-witness and half-error branch payloads explicit,
while restoring the `discri8` case-split name and composing the checked lower
branches. -/
theorem discri8_from_sign_split_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q t s d : FloatSpec.Core.Defs.FlocqFloat beta)
    (dp dq : ℝ) :
    ⦃⌜EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b') p ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c) q ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q) t ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision (dp - dq) s ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) s) d ∧
        _root_.F2R (beta:=beta) t =
          _root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q ∧
        dp = _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
          _root_.F2R (beta:=beta) p ∧
        dq = _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c -
          _root_.F2R (beta:=beta) q ∧
        beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        bo.vNum = Zpower_nat radix precision ∧
        ((0 < dp * dq ∧
            ∃ f : FloatSpec.Core.Defs.FlocqFloat beta,
              Fbounded (beta:=beta) bo f ∧
                _root_.F2R (beta:=beta) f = dp - dq) ∨
          (0 < dp ∧ (dq < 0) ∧
            Fbounded (beta:=beta) bo p ∧
            Fbounded (beta:=beta) bo q ∧
            Fbounded (beta:=beta) bo s ∧
            Fbounded (beta:=beta) bo d ∧
            Fcanonic (beta:=beta) beta bo s ∧
            Fcanonic (beta:=beta) beta bo d ∧
            0 ≤ _root_.F2R (beta:=beta) p ∧
            3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| <
              _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q ∧
            3 *
              min (Fulp (beta:=beta) bo radix precision p)
                (Fulp (beta:=beta) bo radix precision q) ≤
              |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| ∧
            |_root_.F2R (beta:=beta) s| ≤
              |_root_.F2R (beta:=beta) t| / 2 ∧
            Fulp (beta:=beta) bo radix precision q ≤
              2 * Fulp (beta:=beta) bo radix precision p ∧
            Fulp (beta:=beta) bo radix precision p ≤
              2 * Fulp (beta:=beta) bo radix precision q ∧
            MonotoneP_float (EvenClosest (beta:=beta) bo (radix : ℝ) precision) ∧
            UniqueP (EvenClosest (beta:=beta) bo (radix : ℝ) precision)) ∨
          (dp < 0 ∧ 0 < dq ∧
            ∃ f : FloatSpec.Core.Defs.FlocqFloat beta,
              Fbounded (beta:=beta) bo f ∧
                _root_.F2R (beta:=beta) f = dp - dq) ∨
          (∃ f : FloatSpec.Core.Defs.FlocqFloat beta,
            Fbounded (beta:=beta) bo f ∧
              _root_.F2R (beta:=beta) f = dp - dq))⌝⦄
    (pure (discri8_check (beta:=beta) bo radix precision a b b' c p q t s d dp dq) :
      Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) d -
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| ≤
        2 * Fulp (beta:=beta) bo radix precision d⌝⦄ := by
  intro hpre
  rcases hpre with
    ⟨hRoundp, hRoundq, hRoundt, hRounds, hRoundd, htExact, hdpEq, hdqEq,
      hbeta, hradix, hprecision, hvNum, hbranch⟩
  simp only [wp, PostCond.noThrow, pure, discri8_check, Id.run, ULift.up_down]
  rcases hbranch with hsame | hrest
  · rcases hsame with ⟨hsameSign, hResidual⟩
    have h := discri5_from_residual_witness_payload (beta:=beta) bo radix precision a b b' c p q t s d dp dq
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri5_check, Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hRoundd, hRounds, htExact, hdpEq, hdqEq, hsameSign, hResidual,
        hbeta, hradix, hprecision, hvNum⟩
  rcases hrest with hsecond | hrest
  · rcases hsecond with
      ⟨hdp_pos, hdq_neg, hpBound, hqBound, hsBound, hdBound, hsCan, hdCan,
        hpNonneg, hSecond, hlarge, hsHalf, hq_le_twice_p, hp_le_twice_q,
        hMono, hUnique⟩
    have h := discri6_from_half_error_payload (beta:=beta) bo radix precision a b b' c p q t s d dp dq
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri6_check, Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hRoundp, hRoundq, hRoundt, hRounds, hRoundd, hdpEq, hdqEq,
        hpBound, hqBound, hsBound, hdBound, hsCan, hdCan, hpNonneg,
        hSecond, hlarge, hsHalf, hq_le_twice_p, hp_le_twice_q, hMono,
        hUnique, hdp_pos, hdq_neg, hbeta, hradix, hprecision, hvNum⟩
  rcases hrest with hsymm | hResidual
  · rcases hsymm with ⟨hdp_neg, hdq_pos, hResidual⟩
    have h := discri7_from_residual_witness_payload (beta:=beta) bo radix precision a b b' c p q t s d dp dq
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri7_check, Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hRoundd, hRounds, htExact, hdpEq, hdqEq, hdp_neg, hdq_pos, hResidual,
        hbeta, hradix, hprecision, hvNum⟩
  · have h := discri3_from_residual_witness_payload (beta:=beta) bo radix precision a b b' c p q t s d dp dq
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri3_check, Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hRoundd, hRounds, htExact, hdpEq, hdqEq, hResidual,
        hbeta, hradix, hprecision, hvNum⟩

/-- Coq `discri8` (Discriminant3), dispatching the four residual-sign cases
without exposing the constructed bounded residual witness. -/
theorem discri8 {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q t dp dq s d : FloatSpec.Core.Defs.FlocqFloat beta)
    (e : Int) :
    ⦃⌜discri3_source_context (beta:=beta) bo radix precision
      a b b' c p q t dp dq s d e⌝⦄
    (pure (discri8_check (beta:=beta) bo radix precision a b b' c p q t s d
      (_root_.F2R (beta:=beta) dp) (_root_.F2R (beta:=beta) dq)) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) d -
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| ≤
        2 * Fulp (beta:=beta) bo radix precision d⌝⦄ := by
  intro hctx
  have hctxRaw : discri3_source_context (beta:=beta) bo radix precision
      a b b' c p q t dp dq s d e := by simpa using hctx
  have hctx' := hctx
  dsimp [discri3_source_context] at hctx'
  rcases hctx' with
    ⟨hbeta, hradix, hprecision, hvNum, _haBound, _hbBound, _hb'Bound,
      _hcBound, hpBound, hqBound, _hdBound, _htBound, _hsBound, hdpBound,
      hdqBound, _hsCan, _hU1, _hU2, _hU3, _hpNormal, _hqNormal, _hdNormal,
      _hSquare, _hRoundp, _hRoundq, hpNonneg, _hqNonneg, hSecond, hRoundt,
      hdpEq, hdqEq, hRounds, hRoundd, _hpNeq, _hpForm, _hpVal, _hqExp⟩
  simp only [wp, PostCond.noThrow, pure, discri8_check, Id.run, ULift.up_down]
  have htExact : _root_.F2R (beta:=beta) t =
      _root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q := by
    have h := t_exact (beta:=beta) bo (radix : ℝ) precision p q t
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, t_exact_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using
      h ⟨by omega, hpBound, hqBound, hpNonneg, hSecond, hRoundt⟩
  have finishResidual
      (hres : ∃ f : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded (beta:=beta) bo f ∧ _root_.F2R (beta:=beta) f =
          _root_.F2R (beta:=beta) dp - _root_.F2R (beta:=beta) dq) :
      |_root_.F2R (beta:=beta) d -
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| ≤
        2 * Fulp (beta:=beta) bo radix precision d := by
    have h := discri3_from_residual_witness_payload (beta:=beta) bo radix precision
      a b b' c p q t s d (_root_.F2R (beta:=beta) dp)
      (_root_.F2R (beta:=beta) dq)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri3_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using
      h ⟨hRoundd, hRounds, htExact, hdpEq, hdqEq, hres,
        by omega, by omega, (by omega : precision ≠ 0), hvNum⟩
  rcases lt_trichotomy (_root_.F2R (beta:=beta) dp) 0 with hdpNeg | hdpZero | hdpPos
  · rcases lt_trichotomy (_root_.F2R (beta:=beta) dq) 0 with hdqNeg | hdqZero | hdqPos
    · have h := discri5 (beta:=beta) bo radix precision
        a b b' c p q t dp dq s d e
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri5_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using h (by
          simpa [discri3_source_context, and_assoc] using
            (And.intro hctxRaw (mul_pos_of_neg_of_neg hdpNeg hdqNeg)))
    · apply finishResidual
      exact ⟨dp, hdpBound, by rw [hdqZero, sub_zero]⟩
    · have h := discri7 (beta:=beta) bo radix precision
        a b b' c p q t dp dq s d e
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri7_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using h ⟨hctxRaw, hdpNeg, hdqPos⟩
  · rcases lt_trichotomy (_root_.F2R (beta:=beta) dq) 0 with hdqNeg | hdqZero | hdqPos
    · have hdqOppBound : Fbounded (beta:=beta) bo (Fopp (beta:=beta) dq) := by
        have h := oppBounded (beta:=beta) bo dq
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, oppBounded_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h hdqBound
      have hdqOppVal : _root_.F2R (beta:=beta) (Fopp (beta:=beta) dq) =
          -_root_.F2R (beta:=beta) dq := by
        have h := Fopp_correct (beta:=beta) dq
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fopp_correct_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
      apply finishResidual
      exact ⟨Fopp dq, hdqOppBound, by rw [hdqOppVal, hdpZero]; ring⟩
    · apply finishResidual
      exact ⟨dp, hdpBound, by rw [hdqZero, sub_zero]⟩
    · have hdqOppBound : Fbounded (beta:=beta) bo (Fopp (beta:=beta) dq) := by
        have h := oppBounded (beta:=beta) bo dq
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, oppBounded_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h hdqBound
      have hdqOppVal : _root_.F2R (beta:=beta) (Fopp (beta:=beta) dq) =
          -_root_.F2R (beta:=beta) dq := by
        have h := Fopp_correct (beta:=beta) dq
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fopp_correct_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
      apply finishResidual
      exact ⟨Fopp dq, hdqOppBound, by rw [hdqOppVal, hdpZero]; ring⟩
  · rcases lt_trichotomy (_root_.F2R (beta:=beta) dq) 0 with hdqNeg | hdqZero | hdqPos
    · have h := discri6 (beta:=beta) bo radix precision
        a b b' c p q t dp dq s d e
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri6_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using h (by
          simpa [discri3_source_context, and_assoc] using
            (And.intro hctxRaw (And.intro hdpPos hdqNeg)))
    · apply finishResidual
      exact ⟨dp, hdpBound, by rw [hdqZero, sub_zero]⟩
    · have h := discri5 (beta:=beta) bo radix precision
        a b b' c p q t dp dq s d e
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri5_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using h (by
          simpa [discri3_source_context, and_assoc] using
            (And.intro hctxRaw (mul_pos hdpPos hdqPos)))

/-- Final arithmetic step of Coq Veltkamp local lemma `RleRRounded`.

This factors the part after `ClosestUlp` and the relative ulp estimate used in
Coq as `FulpLe2`: once the rounding error is bounded by one ulp and that ulp is
bounded by `|f| * radix^(1 - precision)`, the rounded input `z` satisfies the
relative bound used throughout the Veltkamp proof. -/
theorem RleRRounded_from_Fulp_rel {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (f : FloatSpec.Core.Defs.FlocqFloat beta) (z : ℝ)
    (hClosestUlp :
      2 * |z - _root_.F2R (beta:=beta) f| ≤
        Fulp (beta:=beta) b radix precision f)
    (hFulpRel :
      Fulp (beta:=beta) b radix precision f ≤
        |_root_.F2R (beta:=beta) f| *
          (radix : ℝ) ^ (1 - (precision : Int))) :
    |z| ≤ |_root_.F2R (beta:=beta) f| *
        (1 + (radix : ℝ) ^ (1 - (precision : Int)) / 2) := by
  have htri : |z| ≤ |z - _root_.F2R (beta:=beta) f| +
      |_root_.F2R (beta:=beta) f| := by
    calc
      |z| = |(z - _root_.F2R (beta:=beta) f) +
          _root_.F2R (beta:=beta) f| := by ring_nf
      _ ≤ |z - _root_.F2R (beta:=beta) f| +
          |_root_.F2R (beta:=beta) f| := abs_add_le _ _
  have hdiff_le :
      |z - _root_.F2R (beta:=beta) f| ≤
        (|_root_.F2R (beta:=beta) f| *
          (radix : ℝ) ^ (1 - (precision : Int))) / 2 := by
    nlinarith
  calc
    |z| ≤ |z - _root_.F2R (beta:=beta) f| +
        |_root_.F2R (beta:=beta) f| := htri
    _ ≤ (|_root_.F2R (beta:=beta) f| *
          (radix : ℝ) ^ (1 - (precision : Int))) / 2 +
        |_root_.F2R (beta:=beta) f| := by
          linarith
    _ = |_root_.F2R (beta:=beta) f| *
        (1 + (radix : ℝ) ^ (1 - (precision : Int)) / 2) := by
          ring

private lemma fbounded_abs_F2R_ge_min_exp {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (f : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : 1 < beta)
    (hfBound : Fbounded (beta:=beta) b f)
    (hfNonzero : _root_.F2R (beta:=beta) f ≠ 0) :
    (beta : ℝ) ^ (-b.dExp) ≤ |_root_.F2R (beta:=beta) f| := by
  rcases hfBound with ⟨_, hfExp⟩
  have hbeta_pos : (0 : ℝ) < (beta : ℝ) := by
    exact_mod_cast (lt_trans Int.zero_lt_one hbeta)
  have hbeta_one : (1 : ℝ) ≤ (beta : ℝ) := by
    exact_mod_cast (le_of_lt hbeta)
  have hfnum_ne : f.Fnum ≠ 0 := by
    intro hfnum_zero
    apply hfNonzero
    simp [_root_.F2R, FloatSpec.Core.Defs.F2R, hfnum_zero]
  have hfnum_abs_pos : 0 < |f.Fnum| := abs_pos.mpr hfnum_ne
  have hfnum_abs_ge_one_int : (1 : Int) ≤ |f.Fnum| := by
    omega
  have hfnum_abs_ge_one_cast : (1 : ℝ) ≤ ((|f.Fnum| : Int) : ℝ) := by
    exact_mod_cast hfnum_abs_ge_one_int
  have hfnum_abs_ge_one : (1 : ℝ) ≤ |(f.Fnum : ℝ)| := by
    simpa [Int.cast_abs] using hfnum_abs_ge_one_cast
  have hpow_mono :
      (beta : ℝ) ^ (-b.dExp) ≤ (beta : ℝ) ^ f.Fexp :=
    zpow_le_zpow_right₀ hbeta_one hfExp
  have hpow_min_pos : 0 < (beta : ℝ) ^ (-b.dExp) :=
    zpow_pos hbeta_pos _
  calc
    (beta : ℝ) ^ (-b.dExp)
        = (1 : ℝ) * (beta : ℝ) ^ (-b.dExp) := by ring
    _ ≤ |(f.Fnum : ℝ)| * (beta : ℝ) ^ (-b.dExp) := by
          exact mul_le_mul_of_nonneg_right hfnum_abs_ge_one
            (le_of_lt hpow_min_pos)
    _ ≤ |(f.Fnum : ℝ)| * (beta : ℝ) ^ f.Fexp := by
          exact mul_le_mul_of_nonneg_left hpow_mono (abs_nonneg _)
    _ = |_root_.F2R (beta:=beta) f| := by
          simp [_root_.F2R, FloatSpec.Core.Defs.F2R, abs_mul,
            abs_of_pos (zpow_pos hbeta_pos f.Fexp)]

private lemma firstNormalPos_plusExp_eq_min_exp {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hprecision : 1 < precision) :
    _root_.F2R (beta:=beta)
        (firstNormalPos (beta:=beta) radix (plusExp b precision) precision) =
      (beta : ℝ) ^ (-b.dExp) := by
  subst beta
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (lt_trans Int.zero_lt_one hradix)
  have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
  have hmax : max 1 (precision - 1) = precision - 1 := by
    omega
  simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, firstNormalPos, nNormMin,
    plusExp, FloatSpec.Core.Defs.FlocqFloat.Fnum,
    FloatSpec.Core.Defs.FlocqFloat.Fexp]
  rw [hmax]
  have hpow_cast :
      ((radix ^ (precision - 1) : Int) : ℝ) =
        (radix : ℝ) ^ (precision - 1 : Nat) := by
    simp [Int.cast_pow]
  rw [hpow_cast]
  rw [← zpow_natCast]
  calc
    (radix : ℝ) ^ ((precision - 1 : Nat) : Int) *
        (radix : ℝ) ^ (-(b.dExp + ((precision - 1 : Nat) : Int)))
        = (radix : ℝ) ^
            (((precision - 1 : Nat) : Int) +
              (-(b.dExp + ((precision - 1 : Nat) : Int)))) := by
              rw [zpow_add₀ hradix_ne]
    _ = (radix : ℝ) ^ (-b.dExp) := by
          congr 1
          ring

/-- Coq: `bimplybplusNorm` from section `VeltS`.

Any nonzero float bounded by `b` has an equal real representative that is
normal for the enlarged `plusExp b` bound. -/
theorem bimplybplusNorm {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (s t : Nat)
    (f : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hpGivesBound : b.vNum = Zpower_nat radix t)
    (hSLe : 2 ≤ s) (_hSGe : s ≤ t - 2)
    (hfBound : Fbounded (beta:=beta) b f)
    (hfNonzero : _root_.F2R (beta:=beta) f ≠ 0) :
    ∃ g : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) g = _root_.F2R (beta:=beta) f ∧
        Fnormal (beta:=beta) radix (plusExp b t) g := by
  let g : FloatSpec.Core.Defs.FlocqFloat beta :=
    Fnormalize (beta:=beta) radix (plusExp b t) t f
  have ht_ne : t ≠ 0 := by
    omega
  have hvNumPlus : (plusExp b t).vNum = Zpower_nat radix t := by
    simpa [plusExp] using hpGivesBound
  have hfBoundPlus : Fbounded (beta:=beta) (plusExp b t) f := by
    rcases hfBound with ⟨hfNum, hfExp⟩
    constructor
    · simpa [plusExp] using hfNum
    · dsimp [plusExp]
      have hnonneg : (0 : Int) ≤ ((t - 1 : Nat) : Int) :=
        Int.natCast_nonneg _
      omega
  have hgEq : _root_.F2R (beta:=beta) g = _root_.F2R (beta:=beta) f := by
    have h := FnormalizeCorrect (beta:=beta) radix (plusExp b t) t f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
      Id.run, ULift.up_down, g, Int.cast_ofNat] using h ⟨hbeta, hradix⟩
  have hgCan :
      Fcanonic (beta:=beta) radix (plusExp b t) g := by
    have h := FnormalizeCanonic (beta:=beta) radix (plusExp b t) t f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
      Id.run, ULift.up_down, Fbounded', g, Int.cast_ofNat] using
      h ⟨hfBoundPlus, hfBoundPlus, ht_ne, hradix, hvNumPlus⟩
  refine ⟨g, hgEq, ?_⟩
  rcases hgCan with hgNormal | hgSub
  · exact hgNormal
  · exfalso
    have hbeta_gt : 1 < beta := by
      omega
    have hLower :
        (beta : ℝ) ^ (-b.dExp) ≤ |_root_.F2R (beta:=beta) f| :=
      fbounded_abs_F2R_ge_min_exp (beta:=beta) b f hbeta_gt hfBound hfNonzero
    have hFirst :
        _root_.F2R (beta:=beta)
            (firstNormalPos (beta:=beta) radix (plusExp b t) t) =
          (beta : ℝ) ^ (-b.dExp) :=
      firstNormalPos_plusExp_eq_min_exp (beta:=beta) b radix t hbeta hradix
        (by omega)
    have hgAbsSub :
        Fsubnormal (beta:=beta) radix (plusExp b t)
          (Fabs (beta:=beta) g) := by
      have h := FsubnormFabs (beta:=beta) (plusExp b t) radix g
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FsubnormFabs_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h hgSub
    have hgAbsEq :
        _root_.F2R (beta:=beta) (Fabs (beta:=beta) g) =
          |_root_.F2R (beta:=beta) g| := by
      have h := Fabs_correct (beta:=beta) g
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fabs_correct_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h hbeta_gt
    have hgAbsNonneg :
        0 ≤ _root_.F2R (beta:=beta) (Fabs (beta:=beta) g) := by
      rw [hgAbsEq]
      exact abs_nonneg _
    have hSubLt :
        _root_.F2R (beta:=beta) (Fabs (beta:=beta) g) <
          _root_.F2R (beta:=beta)
            (firstNormalPos (beta:=beta) radix (plusExp b t) t) := by
      have h := FsubnormalLtFirstNormalPos
        (beta:=beta) radix (plusExp b t) t (Fabs (beta:=beta) g)
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
        FsubnormalLtFirstNormalPos_check, Id.run, ULift.up_down, Fbounded', Int.cast_ofNat] using
        h ⟨hgAbsSub, hgAbsSub, ht_ne, hbeta, hradix, hvNumPlus, hgAbsNonneg⟩
    have hAbsG_lt :
        |_root_.F2R (beta:=beta) g| < (beta : ℝ) ^ (-b.dExp) := by
      simpa [hgAbsEq, hFirst] using hSubLt
    have hAbsF_lt :
        |_root_.F2R (beta:=beta) f| < (beta : ℝ) ^ (-b.dExp) := by
      simpa [hgEq] using hAbsG_lt
    exact (not_lt_of_ge hLower) hAbsF_lt

noncomputable def ClosestErrorBoundNormal_aux_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (f : FloatSpec.Core.Defs.FlocqFloat beta) (z : ℝ) : Unit :=
  ()

/-- Coq: `ClosestErrorBoundNormal_aux`.

This is the normal-case closest-rounding error bound before rewriting the
precision term from `radix / vNum` to `radix^(1-precision)`. -/
theorem ClosestErrorBoundNormal_aux {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (f : FloatSpec.Core.Defs.FlocqFloat beta) (z : ℝ) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision ∧
        Fbounded (beta:=beta) b f ∧
        Fnormal (beta:=beta) radix b
          (Fnormalize (beta:=beta) radix b precision f) ∧
        Closest (beta:=beta) b (radix : ℝ) z f⌝⦄
    (pure (ClosestErrorBoundNormal_aux_check
      (beta:=beta) b radix precision f z) : Id Unit)
    ⦃⇓_ => ⌜|z - _root_.F2R (beta:=beta) f| ≤
        |_root_.F2R (beta:=beta) f| *
          ((1 / 2 : ℝ) * ((radix : ℝ) * ((b.vNum : ℝ)⁻¹)))⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hradix, hprecision, hvNum, hfBound, hnormal, hClosest⟩
  simp only [wp, PostCond.noThrow, pure, ClosestErrorBoundNormal_aux_check,
    Id.run, ULift.up_down]
  have hClosestUlp :
      2 * |z - _root_.F2R (beta:=beta) f| ≤
        Fulp (beta:=beta) b radix precision f := by
    have h := ClosestUlp (beta:=beta) b radix precision z f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hClosest, hbeta, hradix, hprecision, hvNum⟩
  have hFulpRel :
      Fulp (beta:=beta) b radix precision f ≤
        |_root_.F2R (beta:=beta) f| *
          (radix : ℝ) ^ (1 - (precision : Int)) := by
    have h := FulpLe2 (beta:=beta) radix b precision f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpLe2_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hfBound, hnormal, hbeta, hradix, hprecision, hvNum⟩
  have hpow_coeff :
      (radix : ℝ) ^ (1 - (precision : Int)) =
        (radix : ℝ) * ((b.vNum : ℝ)⁻¹) := by
    have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
      exact_mod_cast (lt_trans Int.zero_lt_one hradix)
    have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
    have hprecision_cast :
        ((Zpower_nat radix precision : Int) : ℝ) =
          (radix : ℝ) ^ (precision : Nat) := by
      simp [Zpower_nat]
    have hvNum_real : (b.vNum : ℝ) = (radix : ℝ) ^ (precision : Nat) := by
      rw [hvNum, hprecision_cast]
    rw [hvNum_real]
    calc
      (radix : ℝ) ^ (1 - (precision : Int))
          = (radix : ℝ) ^ (1 : Int) *
              ((radix : ℝ) ^ ((precision : Int)))⁻¹ := by
            rw [sub_eq_add_neg, zpow_add₀ hradix_ne, zpow_neg]
      _ = (radix : ℝ) * (((radix : ℝ) ^ (precision : Nat))⁻¹) := by
            norm_num
  have hdist_rel :
      |z - _root_.F2R (beta:=beta) f| ≤
        (|_root_.F2R (beta:=beta) f| *
          (radix : ℝ) ^ (1 - (precision : Int))) / 2 := by
    nlinarith
  calc
    |z - _root_.F2R (beta:=beta) f|
        ≤ (|_root_.F2R (beta:=beta) f| *
            (radix : ℝ) ^ (1 - (precision : Int))) / 2 := hdist_rel
    _ = |_root_.F2R (beta:=beta) f| *
          ((1 / 2 : ℝ) * ((radix : ℝ) * ((b.vNum : ℝ)⁻¹))) := by
            rw [hpow_coeff]
            ring

noncomputable def ClosestErrorBoundNormal_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (f : FloatSpec.Core.Defs.FlocqFloat beta) (z : ℝ) : Unit :=
  ()

/-- Coq: `ClosestErrorBoundNormal`.

Normal-case closest-rounding error bound in the rewritten precision form. -/
theorem ClosestErrorBoundNormal_internal {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (f : FloatSpec.Core.Defs.FlocqFloat beta) (z : ℝ) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision ∧
        Fbounded (beta:=beta) b f ∧
        Fnormal (beta:=beta) radix b
          (Fnormalize (beta:=beta) radix b precision f) ∧
        Closest (beta:=beta) b (radix : ℝ) z f⌝⦄
    (pure (ClosestErrorBoundNormal_check
      (beta:=beta) b radix precision f z) : Id Unit)
    ⦃⇓_ => ⌜|z - _root_.F2R (beta:=beta) f| ≤
        |_root_.F2R (beta:=beta) f| *
          ((1 / 2 : ℝ) * (radix : ℝ) ^ (1 - (precision : Int)))⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hradix, hprecision, hvNum, hfBound, hnormal, hClosest⟩
  simp only [wp, PostCond.noThrow, pure, ClosestErrorBoundNormal_check,
    Id.run, ULift.up_down]
  have hClosestUlp :
      2 * |z - _root_.F2R (beta:=beta) f| ≤
        Fulp (beta:=beta) b radix precision f := by
    have h := ClosestUlp (beta:=beta) b radix precision z f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hClosest, hbeta, hradix, hprecision, hvNum⟩
  have hFulpRel :
      Fulp (beta:=beta) b radix precision f ≤
        |_root_.F2R (beta:=beta) f| *
          (radix : ℝ) ^ (1 - (precision : Int)) := by
    have h := FulpLe2 (beta:=beta) radix b precision f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpLe2_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hfBound, hnormal, hbeta, hradix, hprecision, hvNum⟩
  have hdist_rel :
      |z - _root_.F2R (beta:=beta) f| ≤
        (|_root_.F2R (beta:=beta) f| *
          (radix : ℝ) ^ (1 - (precision : Int))) / 2 := by
    nlinarith
  calc
    |z - _root_.F2R (beta:=beta) f|
        ≤ (|_root_.F2R (beta:=beta) f| *
            (radix : ℝ) ^ (1 - (precision : Int))) / 2 := hdist_rel
    _ = |_root_.F2R (beta:=beta) f| *
          ((1 / 2 : ℝ) * (radix : ℝ) ^ (1 - (precision : Int))) := by
            ring

/-- Coq: `ClosestErrorBoundNormal` with the exact exported hypotheses.
`Closest` already contains boundedness, so no duplicate `Fbounded` premise is
exposed; the source's strict `1 < precision` is preserved. -/
theorem ClosestErrorBoundNormal (b : Fbound_skel)
    (radix : Int) [ValidRadix radix] (precision : Nat) (z : ℝ)
    (f : FloatSpec.Core.Defs.FlocqFloat radix) :
    ⦃⌜1 < radix ∧ 1 < precision ∧
        b.vNum = Zpower_nat radix precision ∧
        Closest (beta:=radix) b (radix : ℝ) z f ∧
        Fnormal (beta:=radix) radix b
          (Fnormalize (beta:=radix) radix b precision f)⌝⦄
    (pure (ClosestErrorBoundNormal_check
      (beta:=radix) b radix precision f z) : Id Unit)
    ⦃⇓_ => ⌜|z - _root_.F2R (beta:=radix) f| ≤
        |_root_.F2R (beta:=radix) f| *
          ((1 / 2 : ℝ) * (radix : ℝ) ^ (1 - (precision : Int)))⌝⦄ := by
  intro ⟨hradix, hprecision, hvNum, hClosest, hnormal⟩
  have hprecision_ne : precision ≠ 0 := by omega
  exact ClosestErrorBoundNormal_internal (beta:=radix)
    b radix precision f z
      ⟨rfl, hradix, hprecision_ne, hvNum, hClosest.1, hnormal, hClosest⟩

/-- Coq Veltkamp local lemma `RleRRounded`.

A closest rounded value `z` to a normal float `f` is bounded by the standard
relative `1 + radix^(1 - precision) / 2` factor. -/
theorem RleRRounded {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (f : FloatSpec.Core.Defs.FlocqFloat beta) (z : ℝ)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hprecision : precision ≠ 0)
    (hvNum : b.vNum = Zpower_nat radix precision)
    (hnormal : Fnormal (beta:=beta) radix b f)
    (hClosest : Closest (beta:=beta) b (radix : ℝ) z f) :
    |z| ≤ |_root_.F2R (beta:=beta) f| *
        (1 + (radix : ℝ) ^ (1 - (precision : Int)) / 2) := by
  have hClosestUlp :
      2 * |z - _root_.F2R (beta:=beta) f| ≤
        Fulp (beta:=beta) b radix precision f := by
    have h := ClosestUlp (beta:=beta) b radix precision z f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hClosest, hbeta, hradix, hprecision, hvNum⟩
  have hNormEq :
      Fnormalize (beta:=beta) radix b precision f = f := by
    have h := FcanonicFnormalizeEq (beta:=beta) radix b precision f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨Or.inl hnormal, hbeta, hradix, hprecision, hvNum⟩
  have hNormalNorm :
      Fnormal (beta:=beta) radix b
        (Fnormalize (beta:=beta) radix b precision f) := by
    simpa [hNormEq] using hnormal
  have hFulpRel :
      Fulp (beta:=beta) b radix precision f ≤
        |_root_.F2R (beta:=beta) f| *
          (radix : ℝ) ^ (1 - (precision : Int)) := by
    have h := FulpLe2 (beta:=beta) radix b precision f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpLe2_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hnormal.1, hNormalNorm, hbeta, hradix, hprecision, hvNum⟩
  exact RleRRounded_from_Fulp_rel (beta:=beta) b radix precision f z
    hClosestUlp hFulpRel

/-- Coq Veltkamp local lemma `hxExact`.

The third Veltkamp rounded value is exact: rounding `q + p` returns the
representable sum `p + q`.  This is the upstream `ClosestIdem`/`SterbenzAux`
argument using the local rounded definitions `pDef` and `qDef`. -/
theorem hxExact {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x p q hx : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (ht_ne : t ≠ 0)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hxBound : Fbounded (beta:=beta) b x)
    (hpDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)) p)
    (hqDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hhxDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) q + _root_.F2R (beta:=beta) p) hx)
    (hxPos : 0 < _root_.F2R (beta:=beta) x)
    (hpNormal : Fnormal (beta:=beta) radix b p)
    (hqNormal : Fnormal (beta:=beta) radix b q)
    (hs_ge : 2 ≤ s) (hs_le : s ≤ t - 2) :
    _root_.F2R (beta:=beta) hx =
      _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q := by
  have hbeta_gt : 1 < beta := by
    rw [hbeta]
    exact hradix
  have hpBound : Fbounded (beta:=beta) b p := hpDef.1
  have hqBound : Fbounded (beta:=beta) b q := hqDef.1
  have hpNonneg :
      0 ≤ _root_.F2R (beta:=beta) p :=
    pPos (beta:=beta) b radix s t x p hradix hvNum hxPos hpDef
  have hqNonpos :
      _root_.F2R (beta:=beta) q ≤ 0 :=
    qNeg (beta:=beta) b radix s t x p q hradix hvNum
      hxBound hxPos hpDef hqDef
  have hMono :
      MonotoneP_float (Closest (beta:=beta) b (radix : ℝ)) := by
    have h := ClosestMonotone_from_split_radix_payload (beta:=beta) b (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMonotone_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hProject (f : FloatSpec.Core.Defs.FlocqFloat beta)
      (hf : Fbounded (beta:=beta) b f) :
      Closest (beta:=beta) b (radix : ℝ)
        (_root_.F2R (beta:=beta) f) f := by
    constructor
    · exact hf
    · intro g _hgBounded
      rw [sub_self, abs_zero]
      exact abs_nonneg _
  have hOppQ :
      Closest (beta:=beta) b (radix : ℝ)
        (-(_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p))
        (Fopp (beta:=beta) q) := by
    have h := ClosestOpp (beta:=beta) b (radix : ℝ) q
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p)
    simpa [wp, PostCond.noThrow, pure, ClosestOpp_check, Id.run,
      ULift.up_down] using h hqDef
  have hFoppQ :
      _root_.F2R (beta:=beta) (Fopp (beta:=beta) q) =
        -_root_.F2R (beta:=beta) q := by
    have h := (FloatSpec.Calc.Operations.F2R_opp (beta:=beta) q) trivial
    simpa [Fopp, _root_.F2R] using h
  have hFoppQBound : Fbounded (beta:=beta) b (Fopp (beta:=beta) q) := by
    simpa [Fbounded, Fopp, FloatSpec.Calc.Operations.Fopp, abs_neg] using hqBound
  have hProjectP :
      Closest (beta:=beta) b (radix : ℝ)
        (_root_.F2R (beta:=beta) p) p := hProject p hpBound
  have hOppInputLt :
      -(_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) <
        _root_.F2R (beta:=beta) p := by
    nlinarith
  have hFoppQ_le_p :
      _root_.F2R (beta:=beta) (Fopp (beta:=beta) q) ≤
        _root_.F2R (beta:=beta) p :=
    hMono
      (-(_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p))
      (_root_.F2R (beta:=beta) p)
      (Fopp (beta:=beta) q) p hOppInputLt hOppQ hProjectP
  have hpAbs :
      |_root_.F2R (beta:=beta) p| =
        _root_.F2R (beta:=beta) p :=
    abs_of_nonneg hpNonneg
  have hqAbs :
      |_root_.F2R (beta:=beta) q| =
        -_root_.F2R (beta:=beta) q :=
    abs_of_nonpos hqNonpos
  have hFoppQAbs :
      |_root_.F2R (beta:=beta) (Fopp (beta:=beta) q)| =
        _root_.F2R (beta:=beta) (Fopp (beta:=beta) q) := by
    rw [hFoppQ]
    exact abs_of_nonneg (by linarith)
  have ht_ge_three : 3 ≤ t := by
    omega
  have hfactor_pos :
      0 < (radix : ℝ) ^ (s : Int) + 1 := by
    have hradix_pos_int : (0 : Int) < radix := lt_trans (by decide) hradix
    have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
      exact_mod_cast hradix_pos_int
    have hpow_pos : 0 < (radix : ℝ) ^ (s : Int) :=
      zpow_pos hradix_pos _
    linarith
  have hpRounded :
      |_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)| ≤
        |_root_.F2R (beta:=beta) p| *
          (1 + (radix : ℝ) ^ (1 - (t : Int)) / 2) :=
    RleRRounded (beta:=beta) b radix t p
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1))
      hbeta hradix ht_ne hvNum hpNormal hpDef
  have hp_lower :
      _root_.F2R (beta:=beta) x ≤
        _root_.F2R (beta:=beta) p *
          (1 + (radix : ℝ) ^ (1 - (t : Int)) / 2) /
            ((radix : ℝ) ^ (s : Int) + 1) := by
    have hx_nonneg : 0 ≤ _root_.F2R (beta:=beta) x := le_of_lt hxPos
    have hinput_abs :
        |_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)| =
          _root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1) := by
      rw [abs_of_nonneg]
      exact mul_nonneg hx_nonneg (le_of_lt hfactor_pos)
    have hpRounded' :
        _root_.F2R (beta:=beta) x *
            ((radix : ℝ) ^ (s : Int) + 1) ≤
          _root_.F2R (beta:=beta) p *
            (1 + (radix : ℝ) ^ (1 - (t : Int)) / 2) := by
      have hpRounded'' := hpRounded
      rw [hinput_abs, hpAbs] at hpRounded''
      exact hpRounded''
    exact (le_div_iff₀ hfactor_pos).2 hpRounded'
  have hqRounded :
      |_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p| ≤
        |_root_.F2R (beta:=beta) q| *
          (1 + (radix : ℝ) ^ (1 - (t : Int)) / 2) :=
    RleRRounded (beta:=beta) b radix t q
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p)
      hbeta hradix ht_ne hvNum hqNormal hqDef
  have hx_le_p : _root_.F2R (beta:=beta) x ≤ _root_.F2R (beta:=beta) p := by
    have hProj : Closest (beta:=beta) b (radix : ℝ)
        (_root_.F2R (beta:=beta) x) x := hProject x hxBound
    have hradix_pos_int : (0 : Int) < radix := lt_trans (by decide) hradix
    have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
      exact_mod_cast hradix_pos_int
    have hpow_pos : 0 < (radix : ℝ) ^ (s : Int) :=
      zpow_pos hradix_pos _
    have hscaled_gt :
        _root_.F2R (beta:=beta) x <
          _root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1) := by
      nlinarith
    exact hMono (_root_.F2R (beta:=beta) x)
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1))
      x p hscaled_gt hProj hpDef
  have hq_abs_input :
      |_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p| =
        _root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) x := by
    calc
      |_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p| =
          -(_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) :=
        abs_of_nonpos (sub_nonpos.mpr hx_le_p)
      _ = _root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) x := by ring
  have hp_le_two_oppq :
      _root_.F2R (beta:=beta) p ≤
        2 * _root_.F2R (beta:=beta) (Fopp (beta:=beta) q) := by
    let eps : ℝ := (radix : ℝ) ^ (1 - (t : Int))
    let powS : ℝ := (radix : ℝ) ^ (s : Int)
    let A : ℝ := 1 + eps / 2
    let B : ℝ := powS + 1
    let k : ℝ := 1 - A / B
    have hqBoundFromRounded :
        _root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) x ≤
          (-_root_.F2R (beta:=beta) q) *
            (1 + (radix : ℝ) ^ (1 - (t : Int)) / 2) := by
      simpa [hq_abs_input, hqAbs] using hqRounded
    have hradix_pos_int : (0 : Int) < radix := lt_trans (by decide) hradix
    have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
      exact_mod_cast hradix_pos_int
    have hradix_one_le : (1 : ℝ) ≤ (radix : ℝ) := by
      exact_mod_cast (le_of_lt hradix)
    have hradix_two_le : (2 : ℝ) ≤ (radix : ℝ) := by
      exact_mod_cast (by omega : (2 : Int) ≤ radix)
    have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
    have hsmall_pow :
        (radix : ℝ) ^ (1 - (t : Int)) ≤ 1 := by
      exact zpow_le_one_of_nonpos₀ hradix_one_le (by omega)
    have hcoeff_le : 1 + (radix : ℝ) ^ (1 - (t : Int)) / 2 ≤ (3 / 2 : ℝ) := by
      nlinarith
    have hq_nonneg : 0 ≤ -_root_.F2R (beta:=beta) q := by linarith
    have hpowS_pos : 0 < powS := by
      dsimp [powS]
      exact zpow_pos hradix_pos _
    have hB_pos : 0 < B := by
      dsimp [B]
      linarith
    have hB_ne : B ≠ 0 := ne_of_gt hB_pos
    have hpowS_ge_four : (4 : ℝ) ≤ powS := by
      have hs_two : (2 : Int) ≤ (s : Int) := by omega
      have hpow_two_le : (radix : ℝ) ^ (2 : Int) ≤ powS := by
        dsimp [powS]
        exact zpow_le_zpow_right₀ hradix_one_le hs_two
      have hfour_le_rsq : (4 : ℝ) ≤ (radix : ℝ) ^ (2 : Int) := by
        norm_num [zpow_ofNat]
        nlinarith
      exact le_trans hfour_le_rsq hpow_two_le
    have heps_mul_powS_le_half : eps * powS ≤ (1 / 2 : ℝ) := by
      have hmul_pow :
          eps * powS = (radix : ℝ) ^ (1 - (t : Int) + (s : Int)) := by
        dsimp [eps, powS]
        rw [← zpow_add₀ hradix_ne]
      have hexp_le : 1 - (t : Int) + (s : Int) ≤ -(1 : Int) := by
        omega
      have hpow_le :
          (radix : ℝ) ^ (1 - (t : Int) + (s : Int)) ≤
            (radix : ℝ) ^ (-(1 : Int)) :=
        zpow_le_zpow_right₀ hradix_one_le hexp_le
      have hinv_le_half :
          (radix : ℝ) ^ (-(1 : Int)) ≤ (1 / 2 : ℝ) := by
        have hone_div : (1 : ℝ) / (radix : ℝ) ≤ (1 : ℝ) / 2 :=
          one_div_le_one_div_of_le (by norm_num) hradix_two_le
        simpa [zpow_neg, one_div] using hone_div
      rw [hmul_pow]
      exact le_trans hpow_le hinv_le_half
    have heps_le_eighth : eps ≤ (1 / 8 : ℝ) := by
      have heps_le_half_div : eps ≤ (1 / 2 : ℝ) / powS := by
        exact (le_div_iff₀ hpowS_pos).2 heps_mul_powS_le_half
      have hhalf_div_le : (1 / 2 : ℝ) / powS ≤ (1 / 8 : ℝ) := by
        exact (div_le_iff₀ hpowS_pos).2 (by nlinarith)
      exact le_trans heps_le_half_div hhalf_div_le
    have hA_le_17 : A ≤ (17 / 16 : ℝ) := by
      dsimp [A, eps] at *
      nlinarith
    have hB_ge_five : (5 : ℝ) ≤ B := by
      dsimp [B]
      nlinarith
    have hk_pos : 0 < k := by
      have hA_lt_B : A < B := by
        nlinarith
      have hA_div_lt_one : A / B < 1 := by
        have hA_lt_one_mul_B : A < 1 * B := by simpa using hA_lt_B
        exact (div_lt_iff₀ hB_pos).2 hA_lt_one_mul_B
      dsimp [k]
      linarith
    have hp_lower_mul :
          _root_.F2R (beta:=beta) x ≤
          _root_.F2R (beta:=beta) p * (A / B) := by
      dsimp [A, B, eps, powS]
      simpa [mul_div_assoc] using hp_lower
    have hp_k_le_p_minus_x :
        _root_.F2R (beta:=beta) p * k ≤
          _root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) x := by
      calc
        _root_.F2R (beta:=beta) p * k =
            _root_.F2R (beta:=beta) p -
              _root_.F2R (beta:=beta) p * (A / B) := by
              dsimp [k]
              ring
        _ ≤ _root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) x := by
              linarith
    have hp_k_le_oppA :
        _root_.F2R (beta:=beta) p * k ≤
          (-_root_.F2R (beta:=beta) q) * A := by
      have hqBoundA :
          _root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) x ≤
            (-_root_.F2R (beta:=beta) q) * A := by
        simpa [A, eps] using hqBoundFromRounded
      exact le_trans hp_k_le_p_minus_x hqBoundA
    have hA_le_two_k : A ≤ 2 * k := by
      have hmain_mul : A * (B + 2) ≤ 2 * B := by
        change (1 + eps / 2) * (powS + 1 + 2) ≤ 2 * (powS + 1)
        nlinarith only [hpowS_ge_four, heps_mul_powS_le_half, heps_le_eighth]
      have hmul : A * B ≤ (2 * k) * B := by
        have hright : (2 * k) * B = 2 * B - 2 * A := by
          calc
            (2 * k) * B = (2 * (1 - A / B)) * B := by rfl
            _ = 2 * B - 2 * A := by field_simp [hB_ne]
        rw [hright]
        nlinarith only [hmain_mul]
      exact le_of_mul_le_mul_right hmul hB_pos
    have hopp_nonneg : 0 ≤ _root_.F2R (beta:=beta) (Fopp (beta:=beta) q) := by
      rw [hFoppQ]
      linarith
    have hoppA_le :
        _root_.F2R (beta:=beta) (Fopp (beta:=beta) q) * A ≤
          _root_.F2R (beta:=beta) (Fopp (beta:=beta) q) * (2 * k) :=
      mul_le_mul_of_nonneg_left hA_le_two_k hopp_nonneg
    have hp_k_le_two :
        _root_.F2R (beta:=beta) p * k ≤
          (2 * _root_.F2R (beta:=beta) (Fopp (beta:=beta) q)) * k := by
      have htmp :
          _root_.F2R (beta:=beta) p * k ≤
            _root_.F2R (beta:=beta) (Fopp (beta:=beta) q) * (2 * k) := by
        exact le_trans (by simpa [hFoppQ] using hp_k_le_oppA) hoppA_le
      nlinarith only [htmp]
    exact le_of_mul_le_mul_right hp_k_le_two hk_pos
  let pq : FloatSpec.Core.Defs.FlocqFloat beta :=
    FloatSpec.Calc.Operations.Fminus (beta:=beta) p (Fopp (beta:=beta) q)
  have hpqBound : Fbounded (beta:=beta) b pq := by
    have h := SterbenzAux (beta:=beta) b p (Fopp (beta:=beta) q)
    simpa [wp, PostCond.noThrow, pure, SterbenzAux_check, pq] using
      h ⟨hbeta_gt, hpBound, hFoppQBound, hFoppQ_le_p, hp_le_two_oppq⟩
  have hpqVal :
      _root_.F2R (beta:=beta) pq =
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q := by
    have hminus := Fminus_correct (beta:=beta) p (Fopp (beta:=beta) q)
    have hval :
        _root_.F2R (beta:=beta)
            (FloatSpec.Calc.Operations.Fminus (beta:=beta) p (Fopp (beta:=beta) q)) =
          _root_.F2R (beta:=beta) p -
            _root_.F2R (beta:=beta) (Fopp (beta:=beta) q) := by
      simpa [wp, PostCond.noThrow, pure, Fminus_correct_check] using hminus hbeta_gt
    simp [pq, hval, hFoppQ]
  have hhxDef' :
      Closest (beta:=beta) b (radix : ℝ) (_root_.F2R (beta:=beta) pq) hx := by
    simpa [hpqVal, add_comm] using hhxDef
  have hidem := ClosestIdem_from_expanded_closest_payload (beta:=beta) b (radix : ℝ) pq hx
  have hpq_hx :
      _root_.F2R (beta:=beta) pq = _root_.F2R (beta:=beta) hx := by
    simpa [wp, PostCond.noThrow, pure, ClosestIdem_check, Fbounded'] using
      hidem ⟨hpqBound, hhxDef', hpqBound, hhxDef'.1, hhxDef'.2⟩
  rw [← hpq_hx, hpqVal]

/-- Coq Veltkamp local lemma `eqLeep`.

The exponent of the second rounded Veltkamp value is no larger than the
exponent of the first.  This is the upstream `Fcanonic_Rle_Zle` argument after
rewriting the signs of `q` and `p`. -/
theorem eqLeep {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (s t : Nat)
    (x p q : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hxBound : Fbounded (beta:=beta) b x)
    (hpDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)) p)
    (hqDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hxPos : 0 < _root_.F2R (beta:=beta) x)
    (hpNormal : Fnormal (beta:=beta) radix b p)
    (hqNormal : Fnormal (beta:=beta) radix b q) :
    q.Fexp ≤ p.Fexp := by
  have hbeta_gt : 1 < beta := by
    rw [hbeta]
    exact hradix
  have hpNonneg :
      0 ≤ _root_.F2R (beta:=beta) p :=
    pPos (beta:=beta) b radix s t x p hradix hvNum hxPos hpDef
  have hqNonpos :
      _root_.F2R (beta:=beta) q ≤ 0 :=
    qNeg (beta:=beta) b radix s t x p q hradix hvNum
      hxBound hxPos hpDef hqDef
  have hMono :
      MonotoneP_float (Closest (beta:=beta) b (radix : ℝ)) := by
    have h := ClosestMonotone_from_split_radix_payload (beta:=beta) b (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMonotone_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hOppQ :
      Closest (beta:=beta) b (radix : ℝ)
        (-(_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p))
        (Fopp (beta:=beta) q) := by
    have h := ClosestOpp (beta:=beta) b (radix : ℝ) q
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p)
    simpa [wp, PostCond.noThrow, pure, ClosestOpp_check, Id.run,
      ULift.up_down] using h hqDef
  have hFoppQ :
      _root_.F2R (beta:=beta) (Fopp (beta:=beta) q) =
        -_root_.F2R (beta:=beta) q := by
    have h := (FloatSpec.Calc.Operations.F2R_opp (beta:=beta) q) trivial
    simpa [Fopp, _root_.F2R] using h
  have hProjectP :
      Closest (beta:=beta) b (radix : ℝ)
        (_root_.F2R (beta:=beta) p) p := by
    constructor
    · exact hpDef.1
    · intro f _hfBounded
      rw [sub_self, abs_zero]
      exact abs_nonneg _
  have hOppInputLt :
      -(_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) <
        _root_.F2R (beta:=beta) p := by
    nlinarith
  have hFoppQ_le_p :
      _root_.F2R (beta:=beta) (Fopp (beta:=beta) q) ≤
        _root_.F2R (beta:=beta) p :=
    hMono
      (-(_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p))
      (_root_.F2R (beta:=beta) p)
      (Fopp (beta:=beta) q) p hOppInputLt hOppQ hProjectP
  have hpAbs :
      |_root_.F2R (beta:=beta) p| =
        _root_.F2R (beta:=beta) p :=
    abs_of_nonneg hpNonneg
  have hqAbs :
      |_root_.F2R (beta:=beta) q| =
        -_root_.F2R (beta:=beta) q :=
    abs_of_nonpos hqNonpos
  have habs_le :
      |_root_.F2R (beta:=beta) q| ≤
        |_root_.F2R (beta:=beta) p| := by
    calc
      |_root_.F2R (beta:=beta) q| =
          -_root_.F2R (beta:=beta) q := hqAbs
      _ = _root_.F2R (beta:=beta) (Fopp (beta:=beta) q) := hFoppQ.symm
      _ ≤ _root_.F2R (beta:=beta) p := hFoppQ_le_p
      _ = |_root_.F2R (beta:=beta) p| := hpAbs.symm
  have h := Fcanonic_Rle_Zle (beta:=beta) radix b q p hbeta_gt hbeta.symm
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fcanonic_Rle_Zle_check,
    Id.run, ULift.up_down, Int.cast_ofNat] using
    h ⟨Or.inl hqNormal, Or.inl hpNormal, habs_le⟩

/-- Coq Veltkamp local lemma `epLe`.

The exponent of the first rounded Veltkamp value is no larger than the
exponent of the comparison float `Float (Fnum x) (s + 1 + Fexp x)`.  This is
the upstream `Fcanonic_Rle_Zle` argument after projecting the comparison float
and using closest-rounding monotonicity. -/
theorem epLe {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (s t : Nat)
    (x p : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hpDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)) p)
    (hxPos : 0 < _root_.F2R (beta:=beta) x)
    (hpNormal : Fnormal (beta:=beta) radix b p)
    (hxNormal : Fnormal (beta:=beta) radix b x)
    (hs_ge : 2 ≤ s) :
    p.Fexp ≤ (s : Int) + 1 + x.Fexp := by
  have hbeta_gt : 1 < beta := by
    rw [hbeta]
    exact hradix
  have hpNonneg :
      0 ≤ _root_.F2R (beta:=beta) p :=
    pPos (beta:=beta) b radix s t x p hradix hvNum hxPos hpDef
  have hMono :
      MonotoneP_float (Closest (beta:=beta) b (radix : ℝ)) := by
    have h := ClosestMonotone_from_split_radix_payload (beta:=beta) b (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMonotone_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  let xp : FloatSpec.Core.Defs.FlocqFloat beta :=
    ⟨x.Fnum, (s : Int) + 1 + x.Fexp⟩
  have hxpNormal : Fnormal (beta:=beta) radix b xp := by
    rcases hxNormal with ⟨hxBound, hxMant⟩
    constructor
    · constructor
      · simpa [xp] using hxBound.1
      · exact le_trans hxBound.2 (by omega : x.Fexp ≤ (s : Int) + 1 + x.Fexp)
    · simpa [xp] using hxMant
  have hProjectXp :
      Closest (beta:=beta) b (radix : ℝ)
        (_root_.F2R (beta:=beta) xp) xp := by
    constructor
    · exact hxpNormal.1
    · intro f _hfBounded
      rw [sub_self, abs_zero]
      exact abs_nonneg _
  have hradix_pos_int : (0 : Int) < radix := lt_trans (by decide) hradix
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradix_pos_int
  have hradix_one_lt : (1 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradix
  have hradix_one_le : (1 : ℝ) ≤ (radix : ℝ) := le_of_lt hradix_one_lt
  have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
  have hpow_s_pos : 0 < (radix : ℝ) ^ (s : Int) :=
    zpow_pos hradix_pos _
  have hfactor_lt_pow :
      (radix : ℝ) ^ (s : Int) + 1 <
        (radix : ℝ) ^ ((s : Int) + 1) := by
    have hs_two_int : (2 : Int) ≤ (s : Int) := by exact_mod_cast hs_ge
    have hpow_two_le :
        (radix : ℝ) ^ (2 : Int) ≤ (radix : ℝ) ^ (s : Int) :=
      zpow_le_zpow_right₀ hradix_one_le hs_two_int
    have htwo_le_radix : (2 : ℝ) ≤ (radix : ℝ) := by
      exact_mod_cast (by omega : (2 : Int) ≤ radix)
    have hpow_two_eq : (radix : ℝ) ^ (2 : Int) = (radix : ℝ) * (radix : ℝ) := by
      norm_num [zpow_ofNat, pow_two]
    have htwo_le_pow_s : (2 : ℝ) ≤ (radix : ℝ) ^ (s : Int) := by
      calc
        (2 : ℝ) ≤ (radix : ℝ) := htwo_le_radix
        _ ≤ (radix : ℝ) * (radix : ℝ) := by
          nlinarith [hradix_one_le]
        _ = (radix : ℝ) ^ (2 : Int) := hpow_two_eq.symm
        _ ≤ (radix : ℝ) ^ (s : Int) := hpow_two_le
    calc
      (radix : ℝ) ^ (s : Int) + 1
          < (radix : ℝ) ^ (s : Int) + (radix : ℝ) ^ (s : Int) := by
            linarith
      _ = (radix : ℝ) ^ (s : Int) * (2 : ℝ) := by ring
      _ ≤ (radix : ℝ) ^ (s : Int) * (radix : ℝ) :=
            mul_le_mul_of_nonneg_left htwo_le_radix (le_of_lt hpow_s_pos)
      _ = (radix : ℝ) ^ ((s : Int) + 1) := by
            rw [zpow_add₀ hradix_ne, zpow_one]
  have hinput_lt :
      _root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1) <
        _root_.F2R (beta:=beta) xp := by
    have hscaled :
        _root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1) <
          _root_.F2R (beta:=beta) x *
            (radix : ℝ) ^ ((s : Int) + 1) :=
      mul_lt_mul_of_pos_left hfactor_lt_pow hxPos
    have hxp_val :
        _root_.F2R (beta:=beta) xp =
          _root_.F2R (beta:=beta) x *
            (radix : ℝ) ^ ((s : Int) + 1) := by
      dsimp [xp, _root_.F2R, FloatSpec.Core.Defs.F2R]
      have hradix_cast : (radix : ℝ) = (beta : ℝ) := by
        exact_mod_cast hbeta.symm
      have hbeta_pos_real : (0 : ℝ) < (beta : ℝ) := by
        have : (0 : Int) < beta := lt_trans (by decide) hbeta_gt
        exact_mod_cast this
      have hbeta_ne : (beta : ℝ) ≠ 0 := ne_of_gt hbeta_pos_real
      rw [hradix_cast]
      rw [show (s : Int) + 1 + x.Fexp = x.Fexp + ((s : Int) + 1) by ring]
      rw [zpow_add₀ hbeta_ne]
      ring
    simpa [hxp_val]
      using hscaled
  have hp_le_xp :
      _root_.F2R (beta:=beta) p ≤ _root_.F2R (beta:=beta) xp :=
    hMono
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1))
      (_root_.F2R (beta:=beta) xp)
      p xp hinput_lt hpDef hProjectXp
  have hpAbs :
      |_root_.F2R (beta:=beta) p| =
        _root_.F2R (beta:=beta) p :=
    abs_of_nonneg hpNonneg
  have hxpNonneg : 0 ≤ _root_.F2R (beta:=beta) xp :=
    le_trans hpNonneg hp_le_xp
  have hxpAbs :
      |_root_.F2R (beta:=beta) xp| =
        _root_.F2R (beta:=beta) xp :=
    abs_of_nonneg hxpNonneg
  have habs_le :
      |_root_.F2R (beta:=beta) p| ≤
        |_root_.F2R (beta:=beta) xp| := by
    simpa [hpAbs, hxpAbs] using hp_le_xp
  have h := Fcanonic_Rle_Zle (beta:=beta) radix b p xp hbeta_gt hbeta.symm
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fcanonic_Rle_Zle_check,
    Id.run, ULift.up_down, xp, Int.cast_ofNat] using
    h ⟨Or.inl hpNormal, Or.inl hxpNormal, habs_le⟩

private theorem eqLe_low {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x p q : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hsGe : 2 ≤ s) (hsLe : s ≤ t - 2)
    (hxBound : Fbounded (beta:=beta) b x)
    (hpDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)) p)
    (hqDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hxPos : 0 < _root_.F2R (beta:=beta) x)
    (hpNormal : Fnormal (beta:=beta) radix b p)
    (hqNormal : Fnormal (beta:=beta) radix b q)
    (hxNormal : Fnormal (beta:=beta) radix b x)
    (hxNumLow : x.Fnum ≤ Zpower_nat radix t - radix - 1) :
    q.Fexp ≤ (s : Int) + x.Fexp := by
  subst beta
  have htNe : t ≠ 0 := by omega
  have hradixPosInt : (0 : Int) < radix := by omega
  have hradixPos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradixPosInt
  have hradixOneLe : (1 : ℝ) ≤ (radix : ℝ) := by
    exact_mod_cast (le_of_lt hradix)
  have hradixNe : (radix : ℝ) ≠ 0 := ne_of_gt hradixPos
  have hxNumPos : 0 < x.Fnum :=
    FloatSpec.Core.Float_prop.gt_0_F2R (beta:=radix) x hradix hxPos
  let g : FloatSpec.Core.Defs.FlocqFloat radix :=
    ⟨x.Fnum + radix, x.Fexp + (s : Int)⟩
  have hgNumPos : 0 < g.Fnum := by
    dsimp [g]
    omega
  have hgNormal : Fnormal (beta:=radix) radix b g := by
    constructor
    · constructor
      · dsimp [g]
        rw [abs_of_pos (by omega : 0 < x.Fnum + radix), hvNum]
        omega
      · dsimp [g]
        have hsNonneg : (0 : Int) ≤ (s : Int) := Int.natCast_nonneg _
        exact le_trans hxBound.2 (by omega)
    · have hxScaledPos : 0 < radix * x.Fnum :=
        mul_pos hradixPosInt hxNumPos
      have hgScaledPos : 0 < radix * (x.Fnum + radix) := by
        nlinarith
      have hxMant := hxNormal.2
      dsimp [g]
      rw [abs_of_pos hxScaledPos] at hxMant
      rw [abs_of_pos hgScaledPos]
      exact le_trans hxMant (by nlinarith)
  have hpNonneg : 0 ≤ _root_.F2R (beta:=radix) p :=
    pPos (beta:=radix) b radix s t x p hradix hvNum hxPos hpDef
  have hqNonpos : _root_.F2R (beta:=radix) q ≤ 0 :=
    qNeg (beta:=radix) b radix s t x p q hradix hvNum hxBound hxPos hpDef hqDef
  have hMono :
      MonotoneP_float (Closest (beta:=radix) b (radix : ℝ)) := by
    have h := ClosestMonotone_from_split_radix_payload (beta:=radix) b (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMonotone_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hxProject : Closest (beta:=radix) b (radix : ℝ)
      (_root_.F2R (beta:=radix) x) x := by
    constructor
    · exact hxBound
    · intro f _hfBounded
      rw [sub_self, abs_zero]
      exact abs_nonneg _
  have hpowSPos : 0 < (radix : ℝ) ^ (s : Int) :=
    zpow_pos hradixPos _
  have hscaledGt :
      _root_.F2R (beta:=radix) x <
        _root_.F2R (beta:=radix) x * ((radix : ℝ) ^ (s : Int) + 1) := by
    nlinarith
  have hxLeP :
      _root_.F2R (beta:=radix) x ≤ _root_.F2R (beta:=radix) p :=
    hMono _ _ x p hscaledGt hxProject hpDef
  have hpUlp :
      2 * |(_root_.F2R (beta:=radix) x *
          ((radix : ℝ) ^ (s : Int) + 1)) -
          _root_.F2R (beta:=radix) p| ≤
        Fulp (beta:=radix) b radix t p := by
    have h := ClosestUlp (beta:=radix) b radix t
      (_root_.F2R (beta:=radix) x * ((radix : ℝ) ^ (s : Int) + 1)) p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hpDef, rfl, hradix, htNe, hvNum⟩
  have hpErr :
      2 * |(_root_.F2R (beta:=radix) x *
          ((radix : ℝ) ^ (s : Int) + 1)) -
          _root_.F2R (beta:=radix) p| ≤
        (radix : ℝ) ^ p.Fexp := by
    have h := ClosestExp_from_ulp_payload (beta:=radix) b radix t
      (_root_.F2R (beta:=radix) x * ((radix : ℝ) ^ (s : Int) + 1)) p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestExp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hpDef, hpUlp, rfl, hradix⟩
  have hqUlp :
      2 * |(_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p) -
          _root_.F2R (beta:=radix) q| ≤
        Fulp (beta:=radix) b radix t q := by
    have h := ClosestUlp (beta:=radix) b radix t
      (_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p) q
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hqDef, rfl, hradix, htNe, hvNum⟩
  have hqErr :
      2 * |(_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p) -
          _root_.F2R (beta:=radix) q| ≤
        (radix : ℝ) ^ q.Fexp := by
    have h := ClosestExp_from_ulp_payload (beta:=radix) b radix t
      (_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p) q
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestExp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hqDef, hqUlp, rfl, hradix⟩
  have hqExpLeP : q.Fexp ≤ p.Fexp :=
    eqLeep (beta:=radix) b radix s t x p q rfl hradix hvNum hxBound
      hpDef hqDef hxPos hpNormal hqNormal
  have hpExpLe : p.Fexp ≤ (s : Int) + 1 + x.Fexp :=
    epLe (beta:=radix) b radix s t x p rfl hradix hvNum hpDef hxPos
      hpNormal hxNormal hsGe
  have hpowQLeP : (radix : ℝ) ^ q.Fexp ≤ (radix : ℝ) ^ p.Fexp :=
    zpow_le_zpow_right₀ hradixOneLe hqExpLeP
  have hpowPLeTop :
      (radix : ℝ) ^ p.Fexp ≤
        (radix : ℝ) ^ ((s : Int) + 1 + x.Fexp) :=
    zpow_le_zpow_right₀ hradixOneLe hpExpLe
  have hpMinusInputLe :
      _root_.F2R (beta:=radix) p -
          _root_.F2R (beta:=radix) x * ((radix : ℝ) ^ (s : Int) + 1) ≤
        |(_root_.F2R (beta:=radix) x *
          ((radix : ℝ) ^ (s : Int) + 1)) -
          _root_.F2R (beta:=radix) p| := by
    rw [abs_sub_comm]
    exact le_abs_self _
  have hpMinusXLe :
      _root_.F2R (beta:=radix) p - _root_.F2R (beta:=radix) x ≤
        _root_.F2R (beta:=radix) x * (radix : ℝ) ^ (s : Int) +
          (radix : ℝ) ^ p.Fexp / 2 := by
    nlinarith [hpErr, hpMinusInputLe]
  have hqAbsLe :
      |_root_.F2R (beta:=radix) q| ≤
        _root_.F2R (beta:=radix) p - _root_.F2R (beta:=radix) x +
          (radix : ℝ) ^ q.Fexp / 2 := by
    calc
      |_root_.F2R (beta:=radix) q| =
          |-(_root_.F2R (beta:=radix) q)| := by rw [abs_neg]
      _ = |(_root_.F2R (beta:=radix) p - _root_.F2R (beta:=radix) x) +
          ((_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p) -
            _root_.F2R (beta:=radix) q)| := by congr 1 <;> ring
      _ ≤ |_root_.F2R (beta:=radix) p - _root_.F2R (beta:=radix) x| +
          |(_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p) -
            _root_.F2R (beta:=radix) q| := abs_add_le _ _
      _ = (_root_.F2R (beta:=radix) p - _root_.F2R (beta:=radix) x) +
          |(_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p) -
            _root_.F2R (beta:=radix) q| := by
            rw [abs_of_nonneg (sub_nonneg.mpr hxLeP)]
      _ ≤ _root_.F2R (beta:=radix) p - _root_.F2R (beta:=radix) x +
          (radix : ℝ) ^ q.Fexp / 2 := by nlinarith [hqErr]
  have hqAbsCore :
      |_root_.F2R (beta:=radix) q| ≤
        _root_.F2R (beta:=radix) x * (radix : ℝ) ^ (s : Int) +
          (radix : ℝ) ^ p.Fexp := by
    nlinarith [hqAbsLe, hpMinusXLe, hpowQLeP]
  have hgValue :
      _root_.F2R (beta:=radix) g =
        _root_.F2R (beta:=radix) x * (radix : ℝ) ^ (s : Int) +
          (radix : ℝ) ^ ((s : Int) + 1 + x.Fexp) := by
    dsimp [g, _root_.F2R, FloatSpec.Core.Defs.F2R]
    have hbase :
        (radix : ℝ) ^ (x.Fexp + (s : Int)) =
          (radix : ℝ) ^ x.Fexp * (radix : ℝ) ^ (s : Int) := by
      rw [zpow_add₀ hradixNe]
    have htop :
        (radix : ℝ) ^ ((s : Int) + 1 + x.Fexp) =
          (radix : ℝ) * (radix : ℝ) ^ (x.Fexp + (s : Int)) := by
      calc
        (radix : ℝ) ^ ((s : Int) + 1 + x.Fexp) =
            (radix : ℝ) ^ (1 + (x.Fexp + (s : Int))) := by
              congr 1
              ring
        _ = (radix : ℝ) ^ (1 : Int) *
            (radix : ℝ) ^ (x.Fexp + (s : Int)) := by
              rw [zpow_add₀ hradixNe]
        _ = (radix : ℝ) * (radix : ℝ) ^ (x.Fexp + (s : Int)) := by
              rw [zpow_one]
    simp only [Int.cast_add, hbase, htop]
    ring
  have hgNonneg : 0 ≤ _root_.F2R (beta:=radix) g := by
    dsimp [g, _root_.F2R, FloatSpec.Core.Defs.F2R]
    exact mul_nonneg (by exact_mod_cast (le_of_lt hgNumPos))
      (le_of_lt (zpow_pos hradixPos _))
  have habsLe :
      |_root_.F2R (beta:=radix) q| ≤ |_root_.F2R (beta:=radix) g| := by
    rw [abs_of_nonneg hgNonneg, hgValue]
    exact le_trans hqAbsCore (add_le_add_right hpowPLeTop _)
  have h := Fcanonic_Rle_Zle (beta:=radix) radix b q g hradix rfl
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fcanonic_Rle_Zle_check,
    Id.run, ULift.up_down, g, add_comm, Int.cast_ofNat] using
    h ⟨Or.inl hqNormal, Or.inl hgNormal, habsLe⟩

private theorem velt_p_upper {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x p : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hsGe : 2 ≤ s) (hsLe : s ≤ t - 2)
    (hxBound : Fbounded (beta:=beta) b x)
    (hxPos : 0 < _root_.F2R (beta:=beta) x)
    (hpDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)) p) :
    _root_.F2R (beta:=beta) p ≤
      (radix : ℝ) ^ (x.Fexp + (t : Int) + (s : Int)) +
        (radix : ℝ) ^ (x.Fexp + (t : Int)) := by
  subst beta
  have htPos : 0 < t := by omega
  have htsPos : 0 < t - s - 1 := by omega
  have hradixPosInt : (0 : Int) < radix := by omega
  have hradixPos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradixPosInt
  have hradixNe : (radix : ℝ) ≠ 0 := ne_of_gt hradixPos
  have hxNumPos : 0 < x.Fnum :=
    FloatSpec.Core.Float_prop.gt_0_F2R (beta:=radix) x hradix hxPos
  have hxNumLt : x.Fnum < Zpower_nat radix t := by
    have hnum := hxBound.1
    rw [abs_of_pos hxNumPos, hvNum] at hnum
    exact hnum
  let j : FloatSpec.Core.Defs.FlocqFloat radix :=
    ⟨Zpower_nat radix (t - 1) + Zpower_nat radix (t - s - 1),
      x.Fexp + (s : Int) + 1⟩
  have hpowPredPos : 0 < radix ^ (t - 1) := pow_pos hradixPosInt _
  have hpowSmallPos : 0 < radix ^ (t - s - 1) := pow_pos hradixPosInt _
  have hsmallExpLt : t - s - 1 < t - 1 := by omega
  have hsmallPowLt :
      radix ^ (t - s - 1) < radix ^ (t - 1) :=
    pow_lt_pow_right₀ hradix hsmallExpLt
  have hsumLtTwo :
      radix ^ (t - 1) + radix ^ (t - s - 1) <
        2 * radix ^ (t - 1) := by omega
  have htwoLeRadix : (2 : Int) ≤ radix := by omega
  have htwoPowLe :
      2 * radix ^ (t - 1) ≤ radix * radix ^ (t - 1) :=
    mul_le_mul_of_nonneg_right htwoLeRadix (le_of_lt hpowPredPos)
  have hpowT : radix * radix ^ (t - 1) = radix ^ t := by
    conv_rhs => rw [show t = (t - 1) + 1 by omega, pow_succ]
    ring
  have hjNumPos : 0 < j.Fnum := by
    dsimp [j, Zpower_nat]
    omega
  have hjBound : Fbounded (beta:=radix) b j := by
    constructor
    · dsimp [j, Zpower_nat]
      rw [abs_of_pos (by omega : 0 < radix ^ (t - 1) + radix ^ (t - s - 1)),
        hvNum, Zpower_nat]
      exact lt_of_lt_of_le hsumLtTwo (by simpa [hpowT] using htwoPowLe)
    · dsimp [j]
      have hsNonneg : (0 : Int) ≤ (s : Int) := Int.natCast_nonneg _
      exact le_trans hxBound.2 (by omega)
  have hpredCast :
      (Zpower_nat radix (t - 1) : ℝ) =
        (radix : ℝ) ^ (((t - 1 : Nat) : Int)) := by
    have h := Zpower_nat_Z_powerRZ radix (t - 1)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Zpower_nat_Z_powerRZ_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hsmallCast :
      (Zpower_nat radix (t - s - 1) : ℝ) =
        (radix : ℝ) ^ (((t - s - 1 : Nat) : Int)) := by
    have h := Zpower_nat_Z_powerRZ radix (t - s - 1)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Zpower_nat_Z_powerRZ_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hjValue :
      _root_.F2R (beta:=radix) j =
        (radix : ℝ) ^ (x.Fexp + (t : Int) + (s : Int)) +
          (radix : ℝ) ^ (x.Fexp + (t : Int)) := by
    dsimp [j, _root_.F2R, FloatSpec.Core.Defs.F2R]
    rw [Int.cast_add, hpredCast, hsmallCast]
    calc
      ((radix : ℝ) ^ (((t - 1 : Nat) : Int)) +
          (radix : ℝ) ^ (((t - s - 1 : Nat) : Int))) *
          (radix : ℝ) ^ (x.Fexp + (s : Int) + 1) =
        (radix : ℝ) ^ (((t - 1 : Nat) : Int)) *
            (radix : ℝ) ^ (x.Fexp + (s : Int) + 1) +
          (radix : ℝ) ^ (((t - s - 1 : Nat) : Int)) *
            (radix : ℝ) ^ (x.Fexp + (s : Int) + 1) := by ring
      _ = (radix : ℝ) ^
            (((t - 1 : Nat) : Int) + (x.Fexp + (s : Int) + 1)) +
          (radix : ℝ) ^
            (((t - s - 1 : Nat) : Int) + (x.Fexp + (s : Int) + 1)) := by
              rw [← zpow_add₀ hradixNe, ← zpow_add₀ hradixNe]
      _ = (radix : ℝ) ^ (x.Fexp + (t : Int) + (s : Int)) +
          (radix : ℝ) ^ (x.Fexp + (t : Int)) := by
            congr 1 <;> congr 1 <;> omega
  have htCast :
      (Zpower_nat radix t : ℝ) = (radix : ℝ) ^ (t : Int) := by
    have h := Zpower_nat_Z_powerRZ radix t
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Zpower_nat_Z_powerRZ_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hxNumLtReal : (x.Fnum : ℝ) < (Zpower_nat radix t : ℝ) := by
    exact_mod_cast hxNumLt
  have hfactorPos : 0 < (radix : ℝ) ^ (s : Int) + 1 := by
    have := zpow_pos hradixPos (s : Int)
    linarith
  have hscalePos :
      0 < (radix : ℝ) ^ x.Fexp * ((radix : ℝ) ^ (s : Int) + 1) :=
    mul_pos (zpow_pos hradixPos _) hfactorPos
  have hinputRaw :
      (x.Fnum : ℝ) *
          ((radix : ℝ) ^ x.Fexp * ((radix : ℝ) ^ (s : Int) + 1)) <
        (Zpower_nat radix t : ℝ) *
          ((radix : ℝ) ^ x.Fexp * ((radix : ℝ) ^ (s : Int) + 1)) :=
    mul_lt_mul_of_pos_right hxNumLtReal hscalePos
  have hupperValue :
      (Zpower_nat radix t : ℝ) *
          ((radix : ℝ) ^ x.Fexp * ((radix : ℝ) ^ (s : Int) + 1)) =
        (radix : ℝ) ^ (x.Fexp + (t : Int) + (s : Int)) +
          (radix : ℝ) ^ (x.Fexp + (t : Int)) := by
    rw [htCast]
    have htx :
        (radix : ℝ) ^ (t : Int) * (radix : ℝ) ^ x.Fexp =
          (radix : ℝ) ^ (x.Fexp + (t : Int)) := by
      rw [← zpow_add₀ hradixNe]
      congr 1
      ring
    rw [← mul_assoc, htx]
    rw [mul_add]
    simp only [mul_one]
    rw [← zpow_add₀ hradixNe]
  have hinputLt :
      _root_.F2R (beta:=radix) x * ((radix : ℝ) ^ (s : Int) + 1) <
        _root_.F2R (beta:=radix) j := by
    rw [hjValue, ← hupperValue]
    simpa [_root_.F2R, FloatSpec.Core.Defs.F2R, mul_assoc] using hinputRaw
  have hMono :
      MonotoneP_float (Closest (beta:=radix) b (radix : ℝ)) := by
    have h := ClosestMonotone_from_split_radix_payload (beta:=radix) b (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMonotone_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hjProject : Closest (beta:=radix) b (radix : ℝ)
      (_root_.F2R (beta:=radix) j) j := by
    constructor
    · exact hjBound
    · intro f _hfBounded
      rw [sub_self, abs_zero]
      exact abs_nonneg _
  exact hjValue ▸ hMono _ _ p j hinputLt hpDef hjProject

private theorem eqLe_high {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x p q hx : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hsGe : 2 ≤ s) (hsLe : s ≤ t - 2)
    (hxBound : Fbounded (beta:=beta) b x)
    (hpDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)) p)
    (hqDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hhxDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) q + _root_.F2R (beta:=beta) p) hx)
    (hxPos : 0 < _root_.F2R (beta:=beta) x)
    (hpNormal : Fnormal (beta:=beta) radix b p)
    (hqNormal : Fnormal (beta:=beta) radix b q)
    (hxNormal : Fnormal (beta:=beta) radix b x)
    (hxNumHigh : Zpower_nat radix t - radix ≤ x.Fnum) :
    q.Fexp ≤ (s : Int) + x.Fexp ∨
      (_root_.F2R (beta:=beta) q =
          -(radix : ℝ) ^ ((t : Int) + (s : Int) + x.Fexp) ∧
        |_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) hx| ≤
          (radix : ℝ) ^ ((s : Int) + x.Fexp) / 2) := by
  subst beta
  have htNe : t ≠ 0 := by omega
  have hradixPosInt : (0 : Int) < radix := by omega
  have hradixPos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradixPosInt
  have hradixOneLe : (1 : ℝ) ≤ (radix : ℝ) := by
    exact_mod_cast (le_of_lt hradix)
  have hradixNe : (radix : ℝ) ≠ 0 := ne_of_gt hradixPos
  have hxNumPos : 0 < x.Fnum :=
    FloatSpec.Core.Float_prop.gt_0_F2R (beta:=radix) x hradix hxPos
  have hqExpLeP : q.Fexp ≤ p.Fexp :=
    eqLeep (beta:=radix) b radix s t x p q rfl hradix hvNum hxBound
      hpDef hqDef hxPos hpNormal hqNormal
  have hpExpLe : p.Fexp ≤ (s : Int) + 1 + x.Fexp :=
    epLe (beta:=radix) b radix s t x p rfl hradix hvNum hpDef hxPos
      hpNormal hxNormal hsGe
  by_cases hqExpLow : q.Fexp ≤ (s : Int) + x.Fexp
  · exact Or.inl hqExpLow
  · right
    have hqExpEq : q.Fexp = (s : Int) + 1 + x.Fexp := by omega
    have hstepPos :
        0 < (radix : ℝ) ^ ((s : Int) + 1 + x.Fexp) :=
      zpow_pos hradixPos _
    have hsmallPos : 0 < (radix : ℝ) ^ ((s : Int) + x.Fexp) :=
      zpow_pos hradixPos _
    let qpos : FloatSpec.Core.Defs.FlocqFloat radix :=
      ⟨nNormMin radix t, (s : Int) + 1 + x.Fexp⟩
    let q0 : FloatSpec.Core.Defs.FlocqFloat radix :=
      ⟨-nNormMin radix t, (s : Int) + 1 + x.Fexp⟩
    have hqExpBound : -b.dExp ≤ (s : Int) + 1 + x.Fexp := by
      have hsNonneg : (0 : Int) ≤ (s : Int) := Int.natCast_nonneg _
      exact le_trans hxBound.2 (by omega)
    have hqposNormal : Fnormal (beta:=radix) radix b qpos := by
      exact FnormalNnormMin (beta:=radix) radix b t
        ((s : Int) + 1 + x.Fexp) hradix (by omega) hvNum hqExpBound
    have hq0Normal : Fnormal (beta:=radix) radix b q0 := by
      have h := FnormalFop (beta:=radix) b radix qpos
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalFop_check,
        Id.run, ULift.up_down, qpos, q0, FloatSpec.Calc.Operations.Fopp,
        neg_neg, Int.cast_ofNat] using h hqposNormal
    have hqNonpos : _root_.F2R (beta:=radix) q ≤ 0 :=
      qNeg (beta:=radix) b radix s t x p q hradix hvNum hxBound hxPos hpDef hqDef
    have hqNumNonpos : q.Fnum ≤ 0 :=
      FloatSpec.Core.Float_prop.le_0_F2R (beta:=radix) q hradix hqNonpos
    have hqAbsMin : nNormMin radix t ≤ |q.Fnum| := by
      have h := pNormal_absolu_min (beta:=radix) radix b t q
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, pNormal_absolu_min_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hqNormal, hqNormal, htNe, hradix, hvNum⟩
    have hqNumLe : q.Fnum ≤ -nNormMin radix t := by
      rw [abs_of_nonpos hqNumNonpos] at hqAbsMin
      omega
    have hnormMinCast :
        (nNormMin radix t : ℝ) = (radix : ℝ) ^ ((t : Int) - 1) := by
      unfold nNormMin
      rw [Int.cast_pow]
      rw [← zpow_natCast]
      congr 1
      omega
    have hq0Value :
        _root_.F2R (beta:=radix) q0 =
          -(radix : ℝ) ^ ((t : Int) + (s : Int) + x.Fexp) := by
      dsimp [q0, _root_.F2R, FloatSpec.Core.Defs.F2R]
      rw [Int.cast_neg, hnormMinCast]
      calc
        -(radix : ℝ) ^ ((t : Int) - 1) *
            (radix : ℝ) ^ ((s : Int) + 1 + x.Fexp) =
          -((radix : ℝ) ^ ((t : Int) - 1) *
            (radix : ℝ) ^ ((s : Int) + 1 + x.Fexp)) := by ring
        _ = -((radix : ℝ) ^ (((t : Int) - 1) +
            ((s : Int) + 1 + x.Fexp))) := by
              exact congrArg Neg.neg
                (zpow_add₀ hradixNe ((t : Int) - 1)
                  ((s : Int) + 1 + x.Fexp)).symm
        _ = -(radix : ℝ) ^ ((t : Int) + (s : Int) + x.Fexp) := by
              congr 2
              ring
    have hpUlp :
        2 * |(_root_.F2R (beta:=radix) x *
            ((radix : ℝ) ^ (s : Int) + 1)) -
            _root_.F2R (beta:=radix) p| ≤
          Fulp (beta:=radix) b radix t p := by
      have h := ClosestUlp (beta:=radix) b radix t
        (_root_.F2R (beta:=radix) x * ((radix : ℝ) ^ (s : Int) + 1)) p
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hpDef, rfl, hradix, htNe, hvNum⟩
    have hpErr :
        2 * |(_root_.F2R (beta:=radix) x *
            ((radix : ℝ) ^ (s : Int) + 1)) -
            _root_.F2R (beta:=radix) p| ≤
          (radix : ℝ) ^ p.Fexp := by
      have h := ClosestExp_from_ulp_payload (beta:=radix) b radix t
        (_root_.F2R (beta:=radix) x * ((radix : ℝ) ^ (s : Int) + 1)) p
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestExp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hpDef, hpUlp, rfl, hradix⟩
    have hpPowLeStep :
        (radix : ℝ) ^ p.Fexp ≤
          (radix : ℝ) ^ ((s : Int) + 1 + x.Fexp) :=
      zpow_le_zpow_right₀ hradixOneLe hpExpLe
    have hpMinusInputLe :
        _root_.F2R (beta:=radix) p -
            _root_.F2R (beta:=radix) x * ((radix : ℝ) ^ (s : Int) + 1) ≤
          |(_root_.F2R (beta:=radix) x *
            ((radix : ℝ) ^ (s : Int) + 1)) -
            _root_.F2R (beta:=radix) p| := by
      rw [abs_sub_comm]
      exact le_abs_self _
    have hpRoundUpper :
        _root_.F2R (beta:=radix) p -
            _root_.F2R (beta:=radix) x * ((radix : ℝ) ^ (s : Int) + 1) ≤
          (radix : ℝ) ^ ((s : Int) + 1 + x.Fexp) / 2 := by
      nlinarith [hpErr, hpPowLeStep, hpMinusInputLe]
    have hxNumUpper : x.Fnum ≤ Zpower_nat radix t - 1 := by
      have hnum := hxBound.1
      rw [abs_of_pos hxNumPos, hvNum] at hnum
      omega
    have htCast :
        (Zpower_nat radix t : ℝ) = (radix : ℝ) ^ (t : Int) := by
      have h := Zpower_nat_Z_powerRZ radix t
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Zpower_nat_Z_powerRZ_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
    have hxNumUpperReal :
        (x.Fnum : ℝ) ≤ (Zpower_nat radix t - 1 : Int) := by
      exact_mod_cast hxNumUpper
    have hxScaleLe :
        _root_.F2R (beta:=radix) x * (radix : ℝ) ^ (s : Int) ≤
          (radix : ℝ) ^ ((t : Int) + (s : Int) + x.Fexp) -
            (radix : ℝ) ^ ((s : Int) + x.Fexp) := by
      have hmul := mul_le_mul_of_nonneg_right hxNumUpperReal
        (le_of_lt (zpow_pos hradixPos (x.Fexp + (s : Int))))
      calc
        _root_.F2R (beta:=radix) x * (radix : ℝ) ^ (s : Int) =
            (x.Fnum : ℝ) * (radix : ℝ) ^ (x.Fexp + (s : Int)) := by
              dsimp [_root_.F2R, FloatSpec.Core.Defs.F2R]
              rw [zpow_add₀ hradixNe]
              ring
        _ ≤ ((Zpower_nat radix t - 1 : Int) : ℝ) *
            (radix : ℝ) ^ (x.Fexp + (s : Int)) := by
              exact hmul
        _ = (radix : ℝ) ^ ((t : Int) + (s : Int) + x.Fexp) -
            (radix : ℝ) ^ ((s : Int) + x.Fexp) := by
              rw [Int.cast_sub, Int.cast_one, htCast]
              have hprod :
                  (radix : ℝ) ^ (t : Int) *
                      (radix : ℝ) ^ (x.Fexp + (s : Int)) =
                    (radix : ℝ) ^ ((t : Int) + (s : Int) + x.Fexp) := by
                rw [← zpow_add₀ hradixNe]
                congr 1
                ring
              rw [sub_mul, one_mul, hprod]
              congr 1
              ring
    have hinputMid :
        _root_.F2R (beta:=radix) q0 -
            (radix : ℝ) ^ ((s : Int) + 1 + x.Fexp) / 2 <
          _root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p := by
      rw [hq0Value]
      nlinarith [hpRoundUpper, hxScaleLe, hsmallPos]
    have hqNumEq : q.Fnum = -nNormMin radix t := by
      by_contra hne
      have hqNumPred : q.Fnum ≤ -nNormMin radix t - 1 := by omega
      have hqNumPredReal :
          (q.Fnum : ℝ) ≤ ((-nNormMin radix t - 1 : Int) : ℝ) := by
        exact_mod_cast hqNumPred
      have hqGap :
          _root_.F2R (beta:=radix) q ≤
            _root_.F2R (beta:=radix) q0 -
              (radix : ℝ) ^ ((s : Int) + 1 + x.Fexp) := by
        have hmul := mul_le_mul_of_nonneg_right hqNumPredReal
          (le_of_lt hstepPos)
        dsimp [q0, _root_.F2R, FloatSpec.Core.Defs.F2R]
        rw [hqExpEq]
        push_cast at hmul ⊢
        nlinarith
      have hdist := hqDef.2 q0 hq0Normal.1
      have hsq := sq_le_sq.mpr hdist
      have hleftPos :
          0 < _root_.F2R (beta:=radix) q0 -
            _root_.F2R (beta:=radix) q := by
        linarith
      have hrightPos :
          0 < 2 * (_root_.F2R (beta:=radix) x -
              _root_.F2R (beta:=radix) p) -
            _root_.F2R (beta:=radix) q -
            _root_.F2R (beta:=radix) q0 := by
        linarith
      have hprodPos := mul_pos hleftPos hrightPos
      have hprodNonpos :
          (_root_.F2R (beta:=radix) q0 - _root_.F2R (beta:=radix) q) *
              (2 * (_root_.F2R (beta:=radix) x -
                  _root_.F2R (beta:=radix) p) -
                _root_.F2R (beta:=radix) q -
                _root_.F2R (beta:=radix) q0) ≤ 0 := by
        nlinarith [hsq]
      exact (not_le_of_gt hprodPos) hprodNonpos
    have hqEqQ0 : q = q0 := by
      cases q with
      | mk m e =>
          simp only [FloatSpec.Core.Defs.FlocqFloat.Fnum,
            FloatSpec.Core.Defs.FlocqFloat.Fexp] at hqNumEq hqExpEq
          simp [q0, hqNumEq, hqExpEq]
    have hqValue :
        _root_.F2R (beta:=radix) q =
          -(radix : ℝ) ^ ((t : Int) + (s : Int) + x.Fexp) := by
      rw [hqEqQ0]
      exact hq0Value
    refine ⟨hqValue, ?_⟩
    have hpUpper := velt_p_upper (beta:=radix) b radix s t x p rfl hradix
      hvNum hsGe hsLe hxBound hxPos hpDef
    have hpUpper' :
        _root_.F2R (beta:=radix) p ≤
          (radix : ℝ) ^ ((t : Int) + (s : Int) + x.Fexp) +
            (radix : ℝ) ^ ((t : Int) + x.Fexp) := by
      simpa only [add_assoc, add_comm, add_left_comm, Int.cast_ofNat] using hpUpper
    have hxNumHighReal :
        ((Zpower_nat radix t - radix : Int) : ℝ) ≤ (x.Fnum : ℝ) := by
      exact_mod_cast hxNumHigh
    have hxLower :
        (radix : ℝ) ^ ((t : Int) + x.Fexp) -
            (radix : ℝ) ^ (1 + x.Fexp) ≤
          _root_.F2R (beta:=radix) x := by
      have hmul := mul_le_mul_of_nonneg_right hxNumHighReal
        (le_of_lt (zpow_pos hradixPos x.Fexp))
      calc
        (radix : ℝ) ^ ((t : Int) + x.Fexp) -
            (radix : ℝ) ^ (1 + x.Fexp) =
          ((Zpower_nat radix t - radix : Int) : ℝ) *
            (radix : ℝ) ^ x.Fexp := by
              rw [Int.cast_sub, htCast]
              rw [sub_mul]
              have htProd :
                  (radix : ℝ) ^ (t : Int) * (radix : ℝ) ^ x.Fexp =
                    (radix : ℝ) ^ ((t : Int) + x.Fexp) := by
                rw [← zpow_add₀ hradixNe]
              have hradixProd :
                  (radix : ℝ) * (radix : ℝ) ^ x.Fexp =
                    (radix : ℝ) ^ (1 + x.Fexp) := by
                calc
                  (radix : ℝ) * (radix : ℝ) ^ x.Fexp =
                      (radix : ℝ) ^ (1 : Int) *
                        (radix : ℝ) ^ x.Fexp := by rw [zpow_one]
                  _ = (radix : ℝ) ^ (1 + x.Fexp) := by
                        rw [zpow_add₀ hradixNe]
              rw [htProd, hradixProd]
        _ ≤ (x.Fnum : ℝ) * (radix : ℝ) ^ x.Fexp := hmul
        _ = _root_.F2R (beta:=radix) x := by rfl
    have hLowerSideGap :
        _root_.F2R (beta:=radix) p - _root_.F2R (beta:=radix) x -
            (radix : ℝ) ^ ((t : Int) + (s : Int) + x.Fexp) ≤
          (radix : ℝ) ^ (1 + x.Fexp) := by
      nlinarith [hpUpper', hxLower]
    have htwoPowLeSmall :
        2 * (radix : ℝ) ^ (1 + x.Fexp) ≤
          (radix : ℝ) ^ ((s : Int) + x.Fexp) := by
      have htwoLeRadix : (2 : ℝ) ≤ (radix : ℝ) := by
        exact_mod_cast (by omega : (2 : Int) ≤ radix)
      have hfirst :
          2 * (radix : ℝ) ^ (1 + x.Fexp) ≤
            (radix : ℝ) * (radix : ℝ) ^ (1 + x.Fexp) :=
        mul_le_mul_of_nonneg_right htwoLeRadix
          (le_of_lt (zpow_pos hradixPos _))
      have hmulPow :
          (radix : ℝ) * (radix : ℝ) ^ (1 + x.Fexp) =
            (radix : ℝ) ^ (2 + x.Fexp) := by
        calc
          (radix : ℝ) * (radix : ℝ) ^ (1 + x.Fexp) =
              (radix : ℝ) ^ (1 : Int) *
                (radix : ℝ) ^ (1 + x.Fexp) := by rw [zpow_one]
          _ = (radix : ℝ) ^ (1 + (1 + x.Fexp)) := by
                exact (zpow_add₀ hradixNe (1 : Int) (1 + x.Fexp)).symm
          _ = (radix : ℝ) ^ (2 + x.Fexp) := by
                congr 1
                ring
      have hpowMono :
          (radix : ℝ) ^ (2 + x.Fexp) ≤
            (radix : ℝ) ^ ((s : Int) + x.Fexp) :=
        zpow_le_zpow_right₀ hradixOneLe (by omega)
      exact le_trans hfirst (hmulPow ▸ hpowMono)
    have hhxExact :
        _root_.F2R (beta:=radix) hx =
          _root_.F2R (beta:=radix) p + _root_.F2R (beta:=radix) q :=
      hxExact (beta:=radix) b radix s t x p q hx rfl hradix htNe hvNum
        hxBound hpDef hqDef hhxDef hxPos hpNormal hqNormal hsGe hsLe
    rw [hhxExact]
    have hresRewrite :
        _root_.F2R (beta:=radix) x -
            (_root_.F2R (beta:=radix) p + _root_.F2R (beta:=radix) q) =
          (_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p) -
            _root_.F2R (beta:=radix) q := by ring
    rw [hresRewrite]
    by_cases hresLeQ :
        _root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p ≤
          _root_.F2R (beta:=radix) q
    · rw [abs_of_nonpos (sub_nonpos.mpr hresLeQ), hqValue]
      nlinarith [hLowerSideGap, htwoPowLeSmall]
    · have hqLtRes :
          _root_.F2R (beta:=radix) q <
            _root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p :=
        lt_of_not_ge hresLeQ
      let qplus := FNSucc (beta:=radix) b (radix : ℝ) t q
      have hqCan : Fcanonic (beta:=radix) radix b q := Or.inl hqNormal
      have hqNormEq : Fnormalize (beta:=radix) radix b t q = q := by
        have h := FcanonicFnormalizeEq (beta:=radix) radix b t q
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hqCan, rfl, hradix, htNe, hvNum⟩
      have hqplusCan : Fcanonic (beta:=radix) radix b qplus := by
        have h := FNSuccCanonic (beta:=radix) b (radix : ℝ) t q
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FNSuccCanonic_check,
          Id.run, ULift.up_down, qplus, Int.cast_ofNat] using
          h ⟨hqDef.1, hradix, htNe, hvNum⟩
      have hqplusBound : Fbounded (beta:=radix) b qplus := by
        have h := FcanonicBound (beta:=radix) radix b qplus
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicBound_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h hqplusCan
      have hqExpNotMin : q.Fexp ≠ -b.dExp := by
        rw [hqExpEq]
        have hsPos : (0 : Int) < (s : Int) + 1 := by omega
        exact ne_of_gt (lt_of_le_of_lt hxBound.2 (by omega))
      have hsuccDiff := FSuccDiff3 (beta:=radix) b radix t q rfl hradix
        htNe hvNum hqNumEq hqExpNotMin
      have hminusVal :
          _root_.F2R (beta:=radix)
              (FloatSpec.Calc.Operations.Fminus (beta:=radix)
                (FSucc (beta:=radix) b radix t q) q) =
            _root_.F2R (beta:=radix) (FSucc (beta:=radix) b radix t q) -
              _root_.F2R (beta:=radix) q := by
        have h := Fminus_correct (beta:=radix)
          (FSucc (beta:=radix) b radix t q) q
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus,
          Id.run, ULift.up_down, Int.cast_ofNat] using h hradix
      have hqplusEq : qplus = FSucc (beta:=radix) b radix t q := by
        simp only [qplus, FNSucc, hqNormEq]
      have hgap :
          _root_.F2R (beta:=radix) qplus - _root_.F2R (beta:=radix) q =
            (radix : ℝ) ^ ((s : Int) + x.Fexp) := by
        rw [hqplusEq]
        calc
          _root_.F2R (beta:=radix) (FSucc (beta:=radix) b radix t q) -
              _root_.F2R (beta:=radix) q =
            _root_.F2R (beta:=radix)
              (FloatSpec.Calc.Operations.Fminus (beta:=radix)
                (FSucc (beta:=radix) b radix t q) q) := hminusVal.symm
          _ = _root_.F2R (beta:=radix)
              (⟨1, q.Fexp - 1⟩ : FloatSpec.Core.Defs.FlocqFloat radix) := by
                simpa [FtoR, Fminus] using hsuccDiff
          _ = (radix : ℝ) ^ ((s : Int) + x.Fexp) := by
                dsimp [_root_.F2R, FloatSpec.Core.Defs.F2R]
                rw [hqExpEq]
                congr 1
                ring
      have hresLeQplus :
          _root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p ≤
            _root_.F2R (beta:=radix) qplus := by
        by_contra hnot
        have hqplusLtRes :
            _root_.F2R (beta:=radix) qplus <
              _root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p :=
          lt_of_not_ge hnot
        have hdist := hqDef.2 qplus hqplusBound
        have hqDist :
            |_root_.F2R (beta:=radix) q -
                (_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p)| =
              (_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p) -
                _root_.F2R (beta:=radix) q := by
          rw [abs_of_nonpos (sub_nonpos.mpr (le_of_lt hqLtRes))]
          ring
        have hqplusDist :
            |_root_.F2R (beta:=radix) qplus -
                (_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p)| =
              (_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p) -
                _root_.F2R (beta:=radix) qplus := by
          rw [abs_of_nonpos (sub_nonpos.mpr (le_of_lt hqplusLtRes))]
          ring
        rw [hqDist, hqplusDist] at hdist
        linarith [hgap, hsmallPos]
      have hdist := hqDef.2 qplus hqplusBound
      have hqDist :
          |_root_.F2R (beta:=radix) q -
              (_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p)| =
            (_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p) -
              _root_.F2R (beta:=radix) q := by
        rw [abs_of_nonpos (sub_nonpos.mpr (le_of_lt hqLtRes))]
        ring
      have hqplusDist :
          |_root_.F2R (beta:=radix) qplus -
              (_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p)| =
            _root_.F2R (beta:=radix) qplus -
              (_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p) := by
        rw [abs_of_nonneg (sub_nonneg.mpr hresLeQplus)]
      rw [hqDist, hqplusDist] at hdist
      have htargetDist :
          |(_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p) -
              _root_.F2R (beta:=radix) q| =
            (_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p) -
              _root_.F2R (beta:=radix) q := by
        rw [abs_of_nonneg (sub_nonneg.mpr (le_of_lt hqLtRes))]
      rw [htargetDist]
      linarith [hgap]

/-- Coq Veltkamp local lemma `eqLe`.

The second rounded residual either has exponent at most `s + Fexp x`, or it
is the negative minimal-normal boundary value and the reconstructed high part
has the upstream half-ulp residual bound. -/
theorem eqLe {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x p q hx : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hsGe : 2 ≤ s) (hsLe : s ≤ t - 2)
    (hxBound : Fbounded (beta:=beta) b x)
    (hpDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)) p)
    (hqDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hhxDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) q + _root_.F2R (beta:=beta) p) hx)
    (hxPos : 0 < _root_.F2R (beta:=beta) x)
    (hpNormal : Fnormal (beta:=beta) radix b p)
    (hqNormal : Fnormal (beta:=beta) radix b q)
    (hxNormal : Fnormal (beta:=beta) radix b x) :
    q.Fexp ≤ (s : Int) + x.Fexp ∨
      (_root_.F2R (beta:=beta) q =
          -(radix : ℝ) ^ ((t : Int) + (s : Int) + x.Fexp) ∧
        |_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) hx| ≤
          (radix : ℝ) ^ ((s : Int) + x.Fexp) / 2) := by
  by_cases hxHigh : Zpower_nat radix t - radix ≤ x.Fnum
  · exact eqLe_high (beta:=beta) b radix s t x p q hx hbeta hradix hvNum
      hsGe hsLe hxBound hpDef hqDef hhxDef hxPos hpNormal hqNormal hxNormal hxHigh
  · left
    apply eqLe_low (beta:=beta) b radix s t x p q hbeta hradix hvNum hsGe hsLe
      hxBound hpDef hqDef hxPos hpNormal hqNormal hxNormal
    omega

private theorem eqGe_of_min_le_opp {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x q : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (htPos : 0 < t)
    (hxBound : Fbounded (beta:=beta) b x)
    (hqNormal : Fnormal (beta:=beta) radix b q)
    (hLower :
      _root_.F2R (beta:=beta)
          (⟨nNormMin radix t, (s : Int) + x.Fexp⟩ :
            FloatSpec.Core.Defs.FlocqFloat beta) ≤
        _root_.F2R (beta:=beta) (Fopp (beta:=beta) q)) :
    (s : Int) + x.Fexp ≤ q.Fexp := by
  subst beta
  let g : FloatSpec.Core.Defs.FlocqFloat radix :=
    ⟨nNormMin radix t, (s : Int) + x.Fexp⟩
  have hgExpBound : -b.dExp ≤ (s : Int) + x.Fexp := by
    exact le_trans hxBound.2 (by omega)
  have hgNormal : Fnormal (beta:=radix) radix b g := by
    exact FnormalNnormMin (beta:=radix) radix b t
      ((s : Int) + x.Fexp) hradix htPos hvNum hgExpBound
  have hqOppNormal :
      Fnormal (beta:=radix) radix b (Fopp (beta:=radix) q) := by
    have h := FnormalFop (beta:=radix) b radix q
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalFop_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hqNormal
  have hradixPos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (lt_trans (by decide : (0 : Int) < 1) hradix)
  have hnPos : 0 < nNormMin radix t := by
    unfold nNormMin
    exact pow_pos (by omega) _
  have hgNonneg : 0 ≤ _root_.F2R (beta:=radix) g := by
    dsimp [g, _root_.F2R, FloatSpec.Core.Defs.F2R]
    exact mul_nonneg (by exact_mod_cast (le_of_lt hnPos))
      (le_of_lt (zpow_pos hradixPos _))
  have hLower' :
      _root_.F2R (beta:=radix) g ≤
        _root_.F2R (beta:=radix) (Fopp (beta:=radix) q) := by
    simpa only [g, Int.cast_ofNat] using hLower
  have hqOppNonneg :
      0 ≤ _root_.F2R (beta:=radix) (Fopp (beta:=radix) q) :=
    le_trans hgNonneg hLower'
  have habsLe :
      |_root_.F2R (beta:=radix) g| ≤
        |_root_.F2R (beta:=radix) (Fopp (beta:=radix) q)| := by
    simpa [abs_of_nonneg hgNonneg, abs_of_nonneg hqOppNonneg] using hLower'
  have h := Fcanonic_Rle_Zle (beta:=radix) radix b g
    (Fopp (beta:=radix) q) hradix rfl
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fcanonic_Rle_Zle_check,
    Id.run, ULift.up_down, g, Fopp, FloatSpec.Calc.Operations.Fopp, Int.cast_ofNat] using
    h ⟨Or.inl hgNormal, Or.inl hqOppNormal, habsLe⟩

private theorem velt_p_lower_middle {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x p : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hsGe : 2 ≤ s) (hsLe : s ≤ t - 2)
    (hxBound : Fbounded (beta:=beta) b x)
    (hpDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)) p)
    (hxNumMiddle : nNormMin radix t + 1 ≤ x.Fnum) :
    (radix : ℝ) ^ (x.Fexp + (t : Int) + (s : Int) - 1) +
        (radix : ℝ) ^ (x.Fexp + (t : Int) - 1) +
        (radix : ℝ) ^ (x.Fexp + (s : Int)) ≤
      _root_.F2R (beta:=beta) p := by
  subst beta
  have htPos : 0 < t := by omega
  have htsPos : 0 < t - s - 1 := by omega
  have hradixPosInt : (0 : Int) < radix := by omega
  have hradixPos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradixPosInt
  have hradixNe : (radix : ℝ) ≠ 0 := ne_of_gt hradixPos
  let j : FloatSpec.Core.Defs.FlocqFloat radix :=
    ⟨Zpower_nat radix (t - 1) + Zpower_nat radix (t - s - 1) + 1,
      x.Fexp + (s : Int)⟩
  have hpowPredPos : 0 < radix ^ (t - 1) := pow_pos hradixPosInt _
  have hpowSmallPos : 0 < radix ^ (t - s - 1) := pow_pos hradixPosInt _
  have hsmallExpLe : t - s - 1 ≤ t - 2 := by omega
  have hsmallPowLe :
      radix ^ (t - s - 1) ≤ radix ^ (t - 2) :=
    pow_le_pow_right₀ (by omega : (1 : Int) ≤ radix) hsmallExpLe
  have honeLtSmaller : 1 < radix ^ (t - 2) := by
    have h := pow_lt_pow_right₀ hradix (by omega : 0 < t - 2)
    simpa using h
  have hsmallPlusOneLt :
      radix ^ (t - s - 1) + 1 < 2 * radix ^ (t - 2) := by
    omega
  have htwoLeRadix : (2 : Int) ≤ radix := by omega
  have htwoSmallerLe :
      2 * radix ^ (t - 2) ≤ radix * radix ^ (t - 2) :=
    mul_le_mul_of_nonneg_right htwoLeRadix
      (le_of_lt (pow_pos hradixPosInt _))
  have hpredPow : radix * radix ^ (t - 2) = radix ^ (t - 1) := by
    conv_rhs => rw [show t - 1 = (t - 2) + 1 by omega, pow_succ]
    ring
  have htailLtPred :
      radix ^ (t - s - 1) + 1 < radix ^ (t - 1) :=
    lt_of_lt_of_le hsmallPlusOneLt (by simpa [hpredPow] using htwoSmallerLe)
  have hsumLtTwoPred :
      radix ^ (t - 1) + radix ^ (t - s - 1) + 1 <
        2 * radix ^ (t - 1) := by omega
  have htwoPredLe :
      2 * radix ^ (t - 1) ≤ radix * radix ^ (t - 1) :=
    mul_le_mul_of_nonneg_right htwoLeRadix (le_of_lt hpowPredPos)
  have hpowT : radix * radix ^ (t - 1) = radix ^ t := by
    conv_rhs => rw [show t = (t - 1) + 1 by omega, pow_succ]
    ring
  have hjNumPos : 0 < j.Fnum := by
    dsimp [j, Zpower_nat]
    omega
  have hjBound : Fbounded (beta:=radix) b j := by
    constructor
    · dsimp [j, Zpower_nat]
      rw [abs_of_pos (by omega :
        0 < radix ^ (t - 1) + radix ^ (t - s - 1) + 1),
        hvNum, Zpower_nat]
      exact lt_of_lt_of_le hsumLtTwoPred (by simpa [hpowT] using htwoPredLe)
    · dsimp [j]
      exact le_trans hxBound.2 (by omega)
  have hpredCast :
      (Zpower_nat radix (t - 1) : ℝ) =
        (radix : ℝ) ^ (((t - 1 : Nat) : Int)) := by
    have h := Zpower_nat_Z_powerRZ radix (t - 1)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Zpower_nat_Z_powerRZ_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hsmallCast :
      (Zpower_nat radix (t - s - 1) : ℝ) =
        (radix : ℝ) ^ (((t - s - 1 : Nat) : Int)) := by
    have h := Zpower_nat_Z_powerRZ radix (t - s - 1)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Zpower_nat_Z_powerRZ_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hpredProd :
      (radix : ℝ) ^ (((t - 1 : Nat) : Int)) *
          (radix : ℝ) ^ (x.Fexp + (s : Int)) =
        (radix : ℝ) ^ (x.Fexp + (t : Int) + (s : Int) - 1) := by
    rw [← zpow_add₀ hradixNe]
    congr 1
    omega
  have hsmallProd :
      (radix : ℝ) ^ (((t - s - 1 : Nat) : Int)) *
          (radix : ℝ) ^ (x.Fexp + (s : Int)) =
        (radix : ℝ) ^ (x.Fexp + (t : Int) - 1) := by
    rw [← zpow_add₀ hradixNe]
    congr 1
    omega
  have hjValue :
      _root_.F2R (beta:=radix) j =
        (radix : ℝ) ^ (x.Fexp + (t : Int) + (s : Int) - 1) +
          (radix : ℝ) ^ (x.Fexp + (t : Int) - 1) +
          (radix : ℝ) ^ (x.Fexp + (s : Int)) := by
    dsimp [j, _root_.F2R, FloatSpec.Core.Defs.F2R]
    rw [Int.cast_add, Int.cast_add, Int.cast_one, hpredCast, hsmallCast]
    rw [add_mul, add_mul, one_mul, hpredProd, hsmallProd]
  have hxNumMiddleReal :
      ((nNormMin radix t + 1 : Int) : ℝ) ≤ (x.Fnum : ℝ) := by
    exact_mod_cast hxNumMiddle
  have hfactorPos : 0 < (radix : ℝ) ^ (s : Int) + 1 := by
    linarith [zpow_pos hradixPos (s : Int)]
  have hscalePos :
      0 < (radix : ℝ) ^ x.Fexp * ((radix : ℝ) ^ (s : Int) + 1) :=
    mul_pos (zpow_pos hradixPos _) hfactorPos
  have hinputRaw :
      ((nNormMin radix t + 1 : Int) : ℝ) *
          ((radix : ℝ) ^ x.Fexp * ((radix : ℝ) ^ (s : Int) + 1)) ≤
        (x.Fnum : ℝ) *
          ((radix : ℝ) ^ x.Fexp * ((radix : ℝ) ^ (s : Int) + 1)) :=
    mul_le_mul_of_nonneg_right hxNumMiddleReal (le_of_lt hscalePos)
  have hnormMinCast :
      (nNormMin radix t : ℝ) =
        (radix : ℝ) ^ (((t - 1 : Nat) : Int)) := by
    unfold nNormMin
    rw [Int.cast_pow, ← zpow_natCast]
  have hnormPredProd :
      (radix : ℝ) ^ (((t - 1 : Nat) : Int)) *
          (radix : ℝ) ^ x.Fexp =
        (radix : ℝ) ^ (x.Fexp + (t : Int) - 1) := by
    rw [← zpow_add₀ hradixNe]
    congr 1
    omega
  have hnormPredScaleProd :
      (radix : ℝ) ^ (x.Fexp + (t : Int) - 1) *
          (radix : ℝ) ^ (s : Int) =
        (radix : ℝ) ^ (x.Fexp + (t : Int) + (s : Int) - 1) := by
    rw [← zpow_add₀ hradixNe]
    congr 1
    ring
  have hxScaleProd :
      (radix : ℝ) ^ x.Fexp * (radix : ℝ) ^ (s : Int) =
        (radix : ℝ) ^ (x.Fexp + (s : Int)) := by
    rw [zpow_add₀ hradixNe]
  have hlowerValue :
      ((nNormMin radix t + 1 : Int) : ℝ) *
          ((radix : ℝ) ^ x.Fexp * ((radix : ℝ) ^ (s : Int) + 1)) =
        (radix : ℝ) ^ (x.Fexp + (t : Int) + (s : Int) - 1) +
          (radix : ℝ) ^ (x.Fexp + (t : Int) - 1) +
          (radix : ℝ) ^ (x.Fexp + (s : Int)) +
          (radix : ℝ) ^ x.Fexp := by
    rw [Int.cast_add, Int.cast_one, hnormMinCast]
    calc
      ((radix : ℝ) ^ (((t - 1 : Nat) : Int)) + 1) *
          ((radix : ℝ) ^ x.Fexp * ((radix : ℝ) ^ (s : Int) + 1)) =
        ((radix : ℝ) ^ (((t - 1 : Nat) : Int)) *
            (radix : ℝ) ^ x.Fexp) * ((radix : ℝ) ^ (s : Int) + 1) +
          (radix : ℝ) ^ x.Fexp * ((radix : ℝ) ^ (s : Int) + 1) := by
            ring
      _ = (radix : ℝ) ^ (x.Fexp + (t : Int) - 1) *
            ((radix : ℝ) ^ (s : Int) + 1) +
          (radix : ℝ) ^ x.Fexp * ((radix : ℝ) ^ (s : Int) + 1) := by
            rw [hnormPredProd]
      _ = (radix : ℝ) ^ (x.Fexp + (t : Int) + (s : Int) - 1) +
          (radix : ℝ) ^ (x.Fexp + (t : Int) - 1) +
          (radix : ℝ) ^ (x.Fexp + (s : Int)) +
          (radix : ℝ) ^ x.Fexp := by
            rw [mul_add, mul_one, mul_add, mul_one,
              hnormPredScaleProd, hxScaleProd]
            ring
  have hinputLt :
      _root_.F2R (beta:=radix) j <
        _root_.F2R (beta:=radix) x * ((radix : ℝ) ^ (s : Int) + 1) := by
    have hpowXPos := zpow_pos hradixPos x.Fexp
    rw [hjValue]
    have hstrict :
        (radix : ℝ) ^ (x.Fexp + (t : Int) + (s : Int) - 1) +
              (radix : ℝ) ^ (x.Fexp + (t : Int) - 1) +
              (radix : ℝ) ^ (x.Fexp + (s : Int)) <
          ((nNormMin radix t + 1 : Int) : ℝ) *
            ((radix : ℝ) ^ x.Fexp * ((radix : ℝ) ^ (s : Int) + 1)) := by
      rw [hlowerValue]
      linarith
    exact lt_of_lt_of_le hstrict (by
      simpa [_root_.F2R, FloatSpec.Core.Defs.F2R, mul_assoc] using hinputRaw)
  have hMono :
      MonotoneP_float (Closest (beta:=radix) b (radix : ℝ)) := by
    have h := ClosestMonotone_from_split_radix_payload (beta:=radix) b (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMonotone_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hjProject : Closest (beta:=radix) b (radix : ℝ)
      (_root_.F2R (beta:=radix) j) j := by
    constructor
    · exact hjBound
    · intro f _hfBounded
      rw [sub_self, abs_zero]
      exact abs_nonneg _
  exact hjValue ▸ hMono _ _ j p hinputLt hjProject hpDef

private theorem eqGe_middle {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x p q : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hsGe : 2 ≤ s) (hsLe : s ≤ t - 2)
    (hxBound : Fbounded (beta:=beta) b x)
    (hpDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)) p)
    (hqDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hqNormal : Fnormal (beta:=beta) radix b q)
    (hxNumMiddle : nNormMin radix t + 1 ≤ x.Fnum)
    (hxNumBelowLarge : x.Fnum < nNormMin radix t + radix) :
    (s : Int) + x.Fexp ≤ q.Fexp := by
  subst beta
  have htPos : 0 < t := by omega
  have hradixPosInt : (0 : Int) < radix := by omega
  have hradixPos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradixPosInt
  have hradixNe : (radix : ℝ) ≠ 0 := ne_of_gt hradixPos
  have hradixOneLt : (1 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradix
  have hpLower := velt_p_lower_middle (beta:=radix) b radix s t x p rfl
    hradix hvNum hsGe hsLe hxBound hpDef hxNumMiddle
  let g : FloatSpec.Core.Defs.FlocqFloat radix :=
    ⟨nNormMin radix t, (s : Int) + x.Fexp⟩
  have hgExpBound : -b.dExp ≤ (s : Int) + x.Fexp := by
    exact le_trans hxBound.2 (by omega)
  have hgNormal : Fnormal (beta:=radix) radix b g :=
    FnormalNnormMin (beta:=radix) radix b t ((s : Int) + x.Fexp)
      hradix htPos hvNum hgExpBound
  have hgBound : Fbounded (beta:=radix) b g := hgNormal.1
  have hnormMinCast :
      (nNormMin radix t : ℝ) =
        (radix : ℝ) ^ (((t - 1 : Nat) : Int)) := by
    unfold nNormMin
    rw [Int.cast_pow, ← zpow_natCast]
  have hgValue :
      _root_.F2R (beta:=radix) g =
        (radix : ℝ) ^ (x.Fexp + (t : Int) + (s : Int) - 1) := by
    dsimp [g, _root_.F2R, FloatSpec.Core.Defs.F2R]
    rw [hnormMinCast, ← zpow_add₀ hradixNe]
    congr 1
    omega
  have hxNumUpper : x.Fnum ≤ nNormMin radix t + radix - 1 := by omega
  have hxNumUpperReal :
      (x.Fnum : ℝ) ≤ ((nNormMin radix t + radix - 1 : Int) : ℝ) := by
    exact_mod_cast hxNumUpper
  have hmulUpper := mul_le_mul_of_nonneg_right hxNumUpperReal
    (le_of_lt (zpow_pos hradixPos x.Fexp))
  have hnormProd :
      (radix : ℝ) ^ (((t - 1 : Nat) : Int)) *
          (radix : ℝ) ^ x.Fexp =
        (radix : ℝ) ^ (x.Fexp + (t : Int) - 1) := by
    rw [← zpow_add₀ hradixNe]
    congr 1
    omega
  have hradixProd :
      (radix : ℝ) * (radix : ℝ) ^ x.Fexp =
        (radix : ℝ) ^ (1 + x.Fexp) := by
    calc
      (radix : ℝ) * (radix : ℝ) ^ x.Fexp =
          (radix : ℝ) ^ (1 : Int) * (radix : ℝ) ^ x.Fexp := by
            rw [zpow_one]
      _ = (radix : ℝ) ^ (1 + x.Fexp) := (zpow_add₀ hradixNe _ _).symm
  have hxUpper :
      _root_.F2R (beta:=radix) x ≤
        (radix : ℝ) ^ (x.Fexp + (t : Int) - 1) +
          (radix : ℝ) ^ (1 + x.Fexp) -
          (radix : ℝ) ^ x.Fexp := by
    calc
      _root_.F2R (beta:=radix) x =
          (x.Fnum : ℝ) * (radix : ℝ) ^ x.Fexp := by rfl
      _ ≤ ((nNormMin radix t + radix - 1 : Int) : ℝ) *
          (radix : ℝ) ^ x.Fexp := hmulUpper
      _ = (radix : ℝ) ^ (x.Fexp + (t : Int) - 1) +
          (radix : ℝ) ^ (1 + x.Fexp) -
          (radix : ℝ) ^ x.Fexp := by
            rw [Int.cast_sub, Int.cast_add, Int.cast_one, hnormMinCast]
            rw [sub_mul, add_mul, one_mul, hnormProd, hradixProd]
  have hpowOneLtS :
      (radix : ℝ) ^ (1 + x.Fexp) <
        (radix : ℝ) ^ (x.Fexp + (s : Int)) :=
    zpow_lt_zpow_right₀ hradixOneLt (by omega)
  have hpowXPos : 0 < (radix : ℝ) ^ x.Fexp := zpow_pos hradixPos _
  have hxLtTail :
      _root_.F2R (beta:=radix) x <
        (radix : ℝ) ^ (x.Fexp + (t : Int) - 1) +
          (radix : ℝ) ^ (x.Fexp + (s : Int)) := by
    linarith
  have hgLtOppInput :
      _root_.F2R (beta:=radix) g <
        -(_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p) := by
    rw [hgValue]
    linarith [hpLower, hxLtTail]
  have hMono :
      MonotoneP_float (Closest (beta:=radix) b (radix : ℝ)) := by
    have h := ClosestMonotone_from_split_radix_payload (beta:=radix) b (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMonotone_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hgProject : Closest (beta:=radix) b (radix : ℝ)
      (_root_.F2R (beta:=radix) g) g := by
    constructor
    · exact hgBound
    · intro f _hfBounded
      rw [sub_self, abs_zero]
      exact abs_nonneg _
  have hOppQ : Closest (beta:=radix) b (radix : ℝ)
      (-(_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p))
      (Fopp (beta:=radix) q) := by
    have h := ClosestOpp (beta:=radix) b (radix : ℝ) q
      (_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestOpp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hqDef
  have hLower :
      _root_.F2R (beta:=radix) g ≤
        _root_.F2R (beta:=radix) (Fopp (beta:=radix) q) :=
    hMono _ _ g (Fopp (beta:=radix) q) hgLtOppInput hgProject hOppQ
  exact eqGe_of_min_le_opp (beta:=radix) b radix s t x q rfl hradix
    hvNum htPos hxBound hqNormal (by simpa only [g, Int.cast_ofNat] using hLower)

private theorem eqGe_large {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x p q : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hsGe : 2 ≤ s) (hsLe : s ≤ t - 2)
    (hxBound : Fbounded (beta:=beta) b x)
    (hpDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)) p)
    (hqDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hxPos : 0 < _root_.F2R (beta:=beta) x)
    (hpNormal : Fnormal (beta:=beta) radix b p)
    (hqNormal : Fnormal (beta:=beta) radix b q)
    (hxNormal : Fnormal (beta:=beta) radix b x)
    (hxNumLarge : nNormMin radix t + radix ≤ x.Fnum) :
    (s : Int) + x.Fexp ≤ q.Fexp := by
  subst beta
  have htPos : 0 < t := by omega
  have htNe : t ≠ 0 := Nat.ne_of_gt htPos
  have hradixPosInt : (0 : Int) < radix := by omega
  have hradixPos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradixPosInt
  have hradixOneLe : (1 : ℝ) ≤ (radix : ℝ) := by
    exact_mod_cast (le_of_lt hradix)
  have hradixNe : (radix : ℝ) ≠ 0 := ne_of_gt hradixPos
  let g : FloatSpec.Core.Defs.FlocqFloat radix :=
    ⟨nNormMin radix t, (s : Int) + x.Fexp⟩
  have hnormMinCast :
      (nNormMin radix t : ℝ) =
        (radix : ℝ) ^ (((t - 1 : Nat) : Int)) := by
    unfold nNormMin
    rw [Int.cast_pow, ← zpow_natCast]
  have hgValue :
      _root_.F2R (beta:=radix) g =
        (radix : ℝ) ^ (x.Fexp + (t : Int) + (s : Int) - 1) := by
    dsimp [g, _root_.F2R, FloatSpec.Core.Defs.F2R]
    rw [hnormMinCast, ← zpow_add₀ hradixNe]
    congr 1
    omega
  have hpUlp :
      2 * |(_root_.F2R (beta:=radix) x *
          ((radix : ℝ) ^ (s : Int) + 1)) -
          _root_.F2R (beta:=radix) p| ≤
        Fulp (beta:=radix) b radix t p := by
    have h := ClosestUlp (beta:=radix) b radix t
      (_root_.F2R (beta:=radix) x * ((radix : ℝ) ^ (s : Int) + 1)) p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hpDef, rfl, hradix, htNe, hvNum⟩
  have hpErr :
      2 * |(_root_.F2R (beta:=radix) x *
          ((radix : ℝ) ^ (s : Int) + 1)) -
          _root_.F2R (beta:=radix) p| ≤
        (radix : ℝ) ^ p.Fexp := by
    have h := ClosestExp_from_ulp_payload (beta:=radix) b radix t
      (_root_.F2R (beta:=radix) x * ((radix : ℝ) ^ (s : Int) + 1)) p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestExp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hpDef, hpUlp, rfl, hradix⟩
  have hqUlp :
      2 * |(_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p) -
          _root_.F2R (beta:=radix) q| ≤
        Fulp (beta:=radix) b radix t q := by
    have h := ClosestUlp (beta:=radix) b radix t
      (_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p) q
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hqDef, rfl, hradix, htNe, hvNum⟩
  have hqErr :
      2 * |(_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p) -
          _root_.F2R (beta:=radix) q| ≤
        (radix : ℝ) ^ q.Fexp := by
    have h := ClosestExp_from_ulp_payload (beta:=radix) b radix t
      (_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p) q
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestExp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hqDef, hqUlp, rfl, hradix⟩
  have hqExpLeP : q.Fexp ≤ p.Fexp :=
    eqLeep (beta:=radix) b radix s t x p q rfl hradix hvNum hxBound
      hpDef hqDef hxPos hpNormal hqNormal
  have hpExpLe : p.Fexp ≤ (s : Int) + 1 + x.Fexp :=
    epLe (beta:=radix) b radix s t x p rfl hradix hvNum hpDef hxPos
      hpNormal hxNormal hsGe
  have hpPowLeTop :
      (radix : ℝ) ^ p.Fexp ≤
        (radix : ℝ) ^ ((s : Int) + 1 + x.Fexp) :=
    zpow_le_zpow_right₀ hradixOneLe hpExpLe
  have hqPowLeTop :
      (radix : ℝ) ^ q.Fexp ≤
        (radix : ℝ) ^ ((s : Int) + 1 + x.Fexp) :=
    zpow_le_zpow_right₀ hradixOneLe (le_trans hqExpLeP hpExpLe)
  have hpAbsBound :
      |(_root_.F2R (beta:=radix) x * ((radix : ℝ) ^ (s : Int) + 1)) -
          _root_.F2R (beta:=radix) p| ≤
        (radix : ℝ) ^ ((s : Int) + 1 + x.Fexp) / 2 := by
    nlinarith
  have hqAbsBound :
      |(_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p) -
          _root_.F2R (beta:=radix) q| ≤
        (radix : ℝ) ^ ((s : Int) + 1 + x.Fexp) / 2 := by
    nlinarith
  have hxNumLargeReal :
      ((nNormMin radix t + radix : Int) : ℝ) ≤ (x.Fnum : ℝ) := by
    exact_mod_cast hxNumLarge
  have hmulLarge := mul_le_mul_of_nonneg_right hxNumLargeReal
    (le_of_lt (zpow_pos hradixPos (x.Fexp + (s : Int))))
  have hnormProd :
      (radix : ℝ) ^ (((t - 1 : Nat) : Int)) *
          (radix : ℝ) ^ (x.Fexp + (s : Int)) =
        (radix : ℝ) ^ (x.Fexp + (t : Int) + (s : Int) - 1) := by
    rw [← zpow_add₀ hradixNe]
    congr 1
    omega
  have hradixProd :
      (radix : ℝ) * (radix : ℝ) ^ (x.Fexp + (s : Int)) =
        (radix : ℝ) ^ ((s : Int) + 1 + x.Fexp) := by
    calc
      (radix : ℝ) * (radix : ℝ) ^ (x.Fexp + (s : Int)) =
          (radix : ℝ) ^ (1 : Int) *
            (radix : ℝ) ^ (x.Fexp + (s : Int)) := by rw [zpow_one]
      _ = (radix : ℝ) ^ (1 + (x.Fexp + (s : Int))) :=
        (zpow_add₀ hradixNe _ _).symm
      _ = (radix : ℝ) ^ ((s : Int) + 1 + x.Fexp) := by
        congr 1
        ring
  have hxScaled :
      _root_.F2R (beta:=radix) x * (radix : ℝ) ^ (s : Int) =
        (x.Fnum : ℝ) * (radix : ℝ) ^ (x.Fexp + (s : Int)) := by
    dsimp [_root_.F2R, FloatSpec.Core.Defs.F2R]
    rw [zpow_add₀ hradixNe]
    ring
  have hscaleLower :
      _root_.F2R (beta:=radix) g +
          (radix : ℝ) ^ ((s : Int) + 1 + x.Fexp) ≤
        _root_.F2R (beta:=radix) x * (radix : ℝ) ^ (s : Int) := by
    rw [hgValue, hxScaled]
    calc
      (radix : ℝ) ^ (x.Fexp + (t : Int) + (s : Int) - 1) +
          (radix : ℝ) ^ ((s : Int) + 1 + x.Fexp) =
        ((nNormMin radix t + radix : Int) : ℝ) *
          (radix : ℝ) ^ (x.Fexp + (s : Int)) := by
            rw [Int.cast_add, hnormMinCast, add_mul, hnormProd, hradixProd]
      _ ≤ (x.Fnum : ℝ) *
          (radix : ℝ) ^ (x.Fexp + (s : Int)) := hmulLarge
  have hpRoundLower :
      _root_.F2R (beta:=radix) x * ((radix : ℝ) ^ (s : Int) + 1) -
          (radix : ℝ) ^ ((s : Int) + 1 + x.Fexp) / 2 ≤
        _root_.F2R (beta:=radix) p := by
    have hneg := neg_abs_le
      (_root_.F2R (beta:=radix) p -
        _root_.F2R (beta:=radix) x * ((radix : ℝ) ^ (s : Int) + 1))
    rw [abs_sub_comm] at hneg
    nlinarith
  have hqResidualLower :
      -((radix : ℝ) ^ ((s : Int) + 1 + x.Fexp) / 2) ≤
        (_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p) -
          _root_.F2R (beta:=radix) q := by
    exact le_trans (neg_le_neg hqAbsBound) (neg_abs_le _)
  have hFoppQ :
      _root_.F2R (beta:=radix) (Fopp (beta:=radix) q) =
        -_root_.F2R (beta:=radix) q := by
    have h := (FloatSpec.Calc.Operations.F2R_opp (beta:=radix) q) trivial
    simpa [Fopp, _root_.F2R] using h
  have hLower :
      _root_.F2R (beta:=radix) g ≤
        _root_.F2R (beta:=radix) (Fopp (beta:=radix) q) := by
    rw [hFoppQ]
    nlinarith [hpRoundLower, hqResidualLower, hscaleLower]
  exact eqGe_of_min_le_opp (beta:=radix) b radix s t x q rfl hradix
    hvNum htPos hxBound hqNormal (by simpa only [g, Int.cast_ofNat] using hLower)

private theorem eqGe_exact_min {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x p q : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hsGe : 2 ≤ s) (hsLe : s ≤ t - 2)
    (hxBound : Fbounded (beta:=beta) b x)
    (hpDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)) p)
    (hqDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hqNormal : Fnormal (beta:=beta) radix b q)
    (hxNumExact : x.Fnum = nNormMin radix t) :
    (s : Int) + x.Fexp ≤ q.Fexp := by
  subst beta
  have htPos : 0 < t := by omega
  have hradixPosInt : (0 : Int) < radix := by omega
  have hradixPos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradixPosInt
  have hradixNe : (radix : ℝ) ≠ 0 := ne_of_gt hradixPos
  let g : FloatSpec.Core.Defs.FlocqFloat radix :=
    ⟨nNormMin radix t, (s : Int) + x.Fexp⟩
  have hgExpBound : -b.dExp ≤ (s : Int) + x.Fexp := by
    exact le_trans hxBound.2 (by omega)
  have hgNormal : Fnormal (beta:=radix) radix b g :=
    FnormalNnormMin (beta:=radix) radix b t ((s : Int) + x.Fexp)
      hradix htPos hvNum hgExpBound
  let jp : FloatSpec.Core.Defs.FlocqFloat radix :=
    ⟨Zpower_nat radix s + 1, x.Fexp + (t : Int) - 1⟩
  have hpowSPos : 0 < radix ^ s := pow_pos hradixPosInt _
  have hpowPredPos : 0 < radix ^ (t - 1) := pow_pos hradixPosInt _
  have hsLtPred : s < t - 1 := by omega
  have hpowSLtPred : radix ^ s < radix ^ (t - 1) :=
    pow_lt_pow_right₀ hradix hsLtPred
  have honeLtPred : 1 < radix ^ (t - 1) := by
    have h := pow_lt_pow_right₀ hradix (by omega : 0 < t - 1)
    simpa using h
  have hsumLtTwoPred : radix ^ s + 1 < 2 * radix ^ (t - 1) := by
    omega
  have htwoLeRadix : (2 : Int) ≤ radix := by omega
  have htwoPredLe :
      2 * radix ^ (t - 1) ≤ radix * radix ^ (t - 1) :=
    mul_le_mul_of_nonneg_right htwoLeRadix (le_of_lt hpowPredPos)
  have hpowT : radix * radix ^ (t - 1) = radix ^ t := by
    conv_rhs => rw [show t = (t - 1) + 1 by omega, pow_succ]
    ring
  have hjpBound : Fbounded (beta:=radix) b jp := by
    constructor
    · dsimp [jp, Zpower_nat]
      rw [abs_of_pos (by omega : 0 < radix ^ s + 1), hvNum, Zpower_nat]
      exact lt_of_lt_of_le hsumLtTwoPred (by simpa [hpowT] using htwoPredLe)
    · dsimp [jp]
      exact le_trans hxBound.2 (by omega)
  have hpowSCast :
      (Zpower_nat radix s : ℝ) = (radix : ℝ) ^ (s : Int) := by
    have h := Zpower_nat_Z_powerRZ radix s
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Zpower_nat_Z_powerRZ_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hnormMinCast :
      (nNormMin radix t : ℝ) =
        (radix : ℝ) ^ (((t - 1 : Nat) : Int)) := by
    unfold nNormMin
    rw [Int.cast_pow, ← zpow_natCast]
  have hnormProd :
      (radix : ℝ) ^ (((t - 1 : Nat) : Int)) *
          (radix : ℝ) ^ x.Fexp =
        (radix : ℝ) ^ (x.Fexp + (t : Int) - 1) := by
    rw [← zpow_add₀ hradixNe]
    congr 1
    omega
  have hxValue :
      _root_.F2R (beta:=radix) x =
        (radix : ℝ) ^ (x.Fexp + (t : Int) - 1) := by
    dsimp [_root_.F2R, FloatSpec.Core.Defs.F2R]
    rw [hxNumExact, hnormMinCast, hnormProd]
  have hjpValue :
      _root_.F2R (beta:=radix) jp =
        _root_.F2R (beta:=radix) x * ((radix : ℝ) ^ (s : Int) + 1) := by
    change ((Zpower_nat radix s + 1 : Int) : ℝ) *
        (radix : ℝ) ^ (x.Fexp + (t : Int) - 1) =
      _root_.F2R (beta:=radix) x * ((radix : ℝ) ^ (s : Int) + 1)
    rw [Int.cast_add, Int.cast_one, hpowSCast, hxValue]
    ring
  have hpValue :
      _root_.F2R (beta:=radix) p =
        _root_.F2R (beta:=radix) x * ((radix : ℝ) ^ (s : Int) + 1) := by
    have hdist := hpDef.2 jp hjpBound
    rw [hjpValue, sub_self, abs_zero] at hdist
    have habsZero :
        |_root_.F2R (beta:=radix) p -
          _root_.F2R (beta:=radix) x * ((radix : ℝ) ^ (s : Int) + 1)| = 0 :=
      le_antisymm hdist (abs_nonneg _)
    exact sub_eq_zero.mp (abs_eq_zero.mp habsZero)
  have hgValue :
      _root_.F2R (beta:=radix) g =
        (radix : ℝ) ^ (x.Fexp + (t : Int) + (s : Int) - 1) := by
    dsimp [g, _root_.F2R, FloatSpec.Core.Defs.F2R]
    rw [hnormMinCast, ← zpow_add₀ hradixNe]
    congr 1
    omega
  have hxScaleValue :
      _root_.F2R (beta:=radix) x * (radix : ℝ) ^ (s : Int) =
        _root_.F2R (beta:=radix) g := by
    rw [hxValue, hgValue, ← zpow_add₀ hradixNe]
    congr 1
    ring
  have hResidual :
      _root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p =
        -_root_.F2R (beta:=radix) g := by
    rw [hpValue]
    nlinarith [hxScaleValue]
  let q0 : FloatSpec.Core.Defs.FlocqFloat radix := Fopp (beta:=radix) g
  have hq0Normal : Fnormal (beta:=radix) radix b q0 := by
    have h := FnormalFop (beta:=radix) b radix g
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalFop_check,
      Id.run, ULift.up_down, q0, Int.cast_ofNat] using h hgNormal
  have hq0Value :
      _root_.F2R (beta:=radix) q0 = -_root_.F2R (beta:=radix) g := by
    have h := (FloatSpec.Calc.Operations.F2R_opp (beta:=radix) g) trivial
    simpa [q0, Fopp, _root_.F2R] using h
  have hqValue :
      _root_.F2R (beta:=radix) q = _root_.F2R (beta:=radix) q0 := by
    have hdist := hqDef.2 q0 hq0Normal.1
    rw [hResidual] at hdist
    have hdist' :
        |_root_.F2R (beta:=radix) q - _root_.F2R (beta:=radix) q0| ≤ 0 := by
      simpa [hq0Value] using hdist
    have habsZero :
        |_root_.F2R (beta:=radix) q - _root_.F2R (beta:=radix) q0| = 0 :=
      le_antisymm hdist' (abs_nonneg _)
    exact sub_eq_zero.mp (abs_eq_zero.mp habsZero)
  have hFoppQ :
      _root_.F2R (beta:=radix) (Fopp (beta:=radix) q) =
        -_root_.F2R (beta:=radix) q := by
    have h := (FloatSpec.Calc.Operations.F2R_opp (beta:=radix) q) trivial
    simpa [Fopp, _root_.F2R] using h
  have hLower :
      _root_.F2R (beta:=radix) g ≤
        _root_.F2R (beta:=radix) (Fopp (beta:=radix) q) := by
    rw [hFoppQ, hqValue, hq0Value]
    linarith
  exact eqGe_of_min_le_opp (beta:=radix) b radix s t x q rfl hradix
    hvNum htPos hxBound hqNormal (by simpa only [g, Int.cast_ofNat] using hLower)

/-! Coq Veltkamp local lemma `eqGe`.

The second rounded residual cannot have exponent below `s + Fexp x`.  The
proof follows the upstream three-way mantissa split: a two-error estimate for
large mantissas, a monotone closest-rounding comparison in the middle range,
and exact representability at the minimal normal mantissa. -/
theorem eqGe {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (s t : Nat)
    (x p q hx : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hsGe : 2 ≤ s) (hsLe : s ≤ t - 2)
    (hxBound : Fbounded (beta:=beta) b x)
    (hpDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)) p)
    (hqDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hhxDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) q + _root_.F2R (beta:=beta) p) hx)
    (hxPos : 0 < _root_.F2R (beta:=beta) x)
    (hpNormal : Fnormal (beta:=beta) radix b p)
    (hqNormal : Fnormal (beta:=beta) radix b q)
    (hxNormal : Fnormal (beta:=beta) radix b x) :
    (s : Int) + x.Fexp ≤ q.Fexp := by
  subst beta
  by_cases hxLarge : nNormMin radix t + radix ≤ x.Fnum
  · exact eqGe_large (beta:=radix) b radix s t x p q rfl hradix hvNum
      hsGe hsLe hxBound hpDef hqDef hxPos hpNormal hqNormal hxNormal hxLarge
  by_cases hxMiddle : nNormMin radix t + 1 ≤ x.Fnum
  · exact eqGe_middle (beta:=radix) b radix s t x p q rfl hradix hvNum
      hsGe hsLe hxBound hpDef hqDef hqNormal hxMiddle (by omega)
  have htNe : t ≠ 0 := by omega
  have hxNumPos : 0 < x.Fnum :=
    FloatSpec.Core.Float_prop.gt_0_F2R (beta:=radix) x hradix hxPos
  have hxNumMin : nNormMin radix t ≤ x.Fnum := by
    have h := pNormal_absolu_min (beta:=radix) radix b t x
    have hAbs : nNormMin radix t ≤ |x.Fnum| := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, pNormal_absolu_min_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hxNormal, hxNormal, htNe, hradix, hvNum⟩
    simpa [abs_of_pos hxNumPos] using hAbs
  have hxNumExact : x.Fnum = nNormMin radix t := by omega
  exact eqGe_exact_min (beta:=radix) b radix s t x p q rfl hradix hvNum
    hsGe hsLe hxBound hpDef hqDef hqNormal hxNumExact

/-! Coq Veltkamp local lemma `eqEqual`.

Combines the exact public `eqLe` disjunction with the exact public `eqGe`
lower exponent bound, preserving the negative minimal-normal boundary and
half-ulp residual payload from the second `eqLe` branch. -/
theorem eqEqual {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x p q hx : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hsGe : 2 ≤ s) (hsLe : s ≤ t - 2)
    (hxBound : Fbounded (beta:=beta) b x)
    (hpDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)) p)
    (hqDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hhxDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) q + _root_.F2R (beta:=beta) p) hx)
    (hxPos : 0 < _root_.F2R (beta:=beta) x)
    (hpNormal : Fnormal (beta:=beta) radix b p)
    (hqNormal : Fnormal (beta:=beta) radix b q)
    (hxNormal : Fnormal (beta:=beta) radix b x) :
    q.Fexp = (s : Int) + x.Fexp ∨
      (_root_.F2R (beta:=beta) q =
          -(radix : ℝ) ^ ((t : Int) + (s : Int) + x.Fexp) ∧
        |_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) hx| ≤
          (radix : ℝ) ^ ((s : Int) + x.Fexp) / 2) := by
  have hle := eqLe (beta:=beta) b radix s t x p q hx hbeta hradix hvNum
    hsGe hsLe hxBound hpDef hqDef hhxDef hxPos hpNormal hqNormal hxNormal
  have hge := eqGe (beta:=beta) b radix s t x p q hx hbeta hradix hvNum
    hsGe hsLe hxBound hpDef hqDef hhxDef hxPos hpNormal hqNormal hxNormal
  rcases hle with hle | hboundary
  · exact Or.inl (le_antisymm hle hge)
  · exact Or.inr hboundary

private theorem Veltkamp_aux_aux_low {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x p q hx : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hsGe : 2 ≤ s) (hsLe : s ≤ t - 2)
    (hxBound : Fbounded (beta:=beta) b x)
    (hpDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)) p)
    (hqDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hhxDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) q + _root_.F2R (beta:=beta) p) hx)
    (hxPos : 0 < _root_.F2R (beta:=beta) x)
    (hpNormal : Fnormal (beta:=beta) radix b p)
    (hqNormal : Fnormal (beta:=beta) radix b q)
    (hxNormal : Fnormal (beta:=beta) radix b x)
    (hxNumLow : (x.Fnum : ℝ) <
      (radix : ℝ) ^ (((t - 1 : Nat) : Int)) +
        (radix : ℝ) ^ (s : Int) / 2) :
    _root_.F2R (beta:=beta) hx =
      (radix : ℝ) ^ ((t : Int) - 1 + x.Fexp) := by
  subst beta
  have htPos : 0 < t := by omega
  have htNe : t ≠ 0 := Nat.ne_of_gt htPos
  have htGtOne : 1 < t := by omega
  have hradixPosInt : (0 : Int) < radix := by omega
  have hradixPos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradixPosInt
  have hradixNe : (radix : ℝ) ≠ 0 := ne_of_gt hradixPos
  have hradixTwo : (2 : ℝ) ≤ (radix : ℝ) := by
    exact_mod_cast (by omega : (2 : Int) ≤ radix)
  have hradixOne : (1 : ℝ) ≤ (radix : ℝ) := le_trans (by norm_num) hradixTwo
  have hxNumPos : 0 < x.Fnum :=
    FloatSpec.Core.Float_prop.gt_0_F2R (beta:=radix) x hradix hxPos
  have hxNumMin : nNormMin radix t ≤ x.Fnum := by
    have h := pNormal_absolu_min (beta:=radix) radix b t x
    have hAbs : nNormMin radix t ≤ |x.Fnum| := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, pNormal_absolu_min_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hxNormal, hxNormal, htNe, hradix, hvNum⟩
    simpa [abs_of_pos hxNumPos] using hAbs
  let eps : Int := x.Fnum - Zpower_nat radix (t - 1)
  have hepsNonneg : 0 ≤ eps := by
    dsimp [eps, nNormMin, Zpower_nat] at hxNumMin ⊢
    exact sub_nonneg.mpr hxNumMin
  have hpredCast :
      (Zpower_nat radix (t - 1) : ℝ) =
        (radix : ℝ) ^ (((t - 1 : Nat) : Int)) := by
    have h := Zpower_nat_Z_powerRZ radix (t - 1)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Zpower_nat_Z_powerRZ_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hpowSCast :
      (Zpower_nat radix s : ℝ) = (radix : ℝ) ^ (s : Int) := by
    have h := Zpower_nat_Z_powerRZ radix s
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Zpower_nat_Z_powerRZ_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hepsCast :
      (eps : ℝ) = (x.Fnum : ℝ) -
        (radix : ℝ) ^ (((t - 1 : Nat) : Int)) := by
    dsimp [eps]
    rw [Int.cast_sub, hpredCast]
  have hepsLtHalf : (eps : ℝ) < (radix : ℝ) ^ (s : Int) / 2 := by
    rw [hepsCast]
    linarith
  have hpowSPos : 0 < (radix : ℝ) ^ (s : Int) := zpow_pos hradixPos _
  have hepsLtPowReal : (eps : ℝ) < (radix : ℝ) ^ (s : Int) := by
    linarith
  have hepsLtPow : eps < Zpower_nat radix s := by
    exact_mod_cast (show (eps : ℝ) < (Zpower_nat radix s : ℝ) by
      simpa only [hpowSCast, Int.cast_ofNat] using hepsLtPowReal)
  have hxNumDecomp : x.Fnum = Zpower_nat radix (t - 1) + eps := by
    dsimp [eps]
    omega
  let yp : FloatSpec.Core.Defs.FlocqFloat radix :=
    ⟨Zpower_nat radix (t - 1) + Zpower_nat radix (t - s - 1) + eps,
      (s : Int) + x.Fexp⟩
  let yq : FloatSpec.Core.Defs.FlocqFloat radix :=
    ⟨Zpower_nat radix (t - 1) + eps, (s : Int) + x.Fexp⟩
  have hpowPredPos : 0 < Zpower_nat radix (t - 1) := by
    exact pow_pos hradixPosInt _
  have hpowSmallPos : 0 < Zpower_nat radix (t - s - 1) := by
    exact pow_pos hradixPosInt _
  have hsmallExpLe : t - s - 1 ≤ t - 2 := by omega
  have hpowSmallLe :
      Zpower_nat radix (t - s - 1) ≤ Zpower_nat radix (t - 2) := by
    exact pow_le_pow_right₀ (by omega : (1 : Int) ≤ radix) hsmallExpLe
  have hpowSLe : Zpower_nat radix s ≤ Zpower_nat radix (t - 2) := by
    exact pow_le_pow_right₀ (by omega : (1 : Int) ≤ radix) (by omega)
  have htailLt :
      Zpower_nat radix (t - s - 1) + eps <
        2 * Zpower_nat radix (t - 2) := by
    omega
  have htwoSmallLePred :
      2 * Zpower_nat radix (t - 2) ≤ Zpower_nat radix (t - 1) := by
    have htwoLe : (2 : Int) ≤ radix := by omega
    have hpowNonneg : 0 ≤ Zpower_nat radix (t - 2) :=
      le_of_lt (pow_pos hradixPosInt _)
    have hmul := mul_le_mul_of_nonneg_right htwoLe hpowNonneg
    calc
      2 * Zpower_nat radix (t - 2) ≤
          radix * Zpower_nat radix (t - 2) := hmul
      _ = Zpower_nat radix (t - 1) := by
        dsimp [Zpower_nat]
        rw [show t - 1 = (t - 2) + 1 by omega, pow_succ]
        ring
  have htwoPredLePowT :
      2 * Zpower_nat radix (t - 1) ≤ Zpower_nat radix t := by
    have htwoLe : (2 : Int) ≤ radix := by omega
    have hpowNonneg : 0 ≤ Zpower_nat radix (t - 1) :=
      le_of_lt hpowPredPos
    have hmul := mul_le_mul_of_nonneg_right htwoLe hpowNonneg
    have hpowSucc :
        Zpower_nat radix t = Zpower_nat radix (t - 1) * radix := by
      dsimp [Zpower_nat]
      conv_lhs => rw [show t = (t - 1) + 1 by omega, pow_succ]
    calc
      2 * Zpower_nat radix (t - 1) ≤
          radix * Zpower_nat radix (t - 1) := hmul
      _ = Zpower_nat radix (t - 1) * radix := by ring
      _ = Zpower_nat radix t := hpowSucc.symm
  have hypNumPos :
      0 < Zpower_nat radix (t - 1) +
        Zpower_nat radix (t - s - 1) + eps := by omega
  have hypNumLt :
      Zpower_nat radix (t - 1) +
          Zpower_nat radix (t - s - 1) + eps < Zpower_nat radix t := by
    omega
  have hExpBound : -b.dExp ≤ (s : Int) + x.Fexp := by
    exact le_trans hxBound.2 (by omega)
  have hypBound : Fbounded (beta:=radix) b yp := by
    constructor
    · dsimp [yp]
      rw [abs_of_pos hypNumPos, hvNum]
      exact hypNumLt
    · simpa only [yp, Int.cast_ofNat] using hExpBound
  have hyqNum : Zpower_nat radix (t - 1) + eps = x.Fnum := by
    omega
  have hyqBound : Fbounded (beta:=radix) b yq := by
    constructor
    · dsimp [yq]
      rw [hyqNum]
      exact hxBound.1
    · simpa only [yq, Int.cast_ofNat] using hExpBound
  have hypNumGeX :
      x.Fnum ≤ Zpower_nat radix (t - 1) +
        Zpower_nat radix (t - s - 1) + eps := by
    rw [hxNumDecomp]
    omega
  have hxProdPos : 0 < radix * x.Fnum := mul_pos hradixPosInt hxNumPos
  have hypProdPos :
      0 < radix * (Zpower_nat radix (t - 1) +
        Zpower_nat radix (t - s - 1) + eps) :=
    mul_pos hradixPosInt hypNumPos
  have hypNormal : Fnormal (beta:=radix) radix b yp := by
    refine ⟨hypBound, ?_⟩
    dsimp [yp]
    rw [abs_of_pos hypProdPos]
    have hnormal := hxNormal.2
    rw [abs_of_pos hxProdPos] at hnormal
    exact le_trans hnormal
      (mul_le_mul_of_nonneg_left hypNumGeX (le_of_lt hradixPosInt))
  have hyqNormal : Fnormal (beta:=radix) radix b yq := by
    refine ⟨hyqBound, ?_⟩
    dsimp [yq]
    rw [hyqNum]
    exact hxNormal.2
  let A : ℝ := (radix : ℝ) ^ (((t - 1 : Nat) : Int))
  let B : ℝ := (radix : ℝ) ^ (s : Int)
  let E : ℝ := (radix : ℝ) ^ x.Fexp
  let D : ℝ := (eps : ℝ)
  have hApos : 0 < A := by dsimp [A]; exact zpow_pos hradixPos _
  have hBpos : 0 < B := by dsimp [B]; exact zpow_pos hradixPos _
  have hEpos : 0 < E := by dsimp [E]; exact zpow_pos hradixPos _
  have hDnonneg : 0 ≤ D := by
    dsimp [D]
    exact_mod_cast hepsNonneg
  have hDltHalf : D < B / 2 := by simpa only [D, B, Int.cast_ofNat] using hepsLtHalf
  have hBgeFour : (4 : ℝ) ≤ B := by
    have hpowTwoLe : (radix : ℝ) ^ (2 : Int) ≤ B := by
      dsimp [B]
      exact zpow_le_zpow_right₀ hradixOne (by omega)
    have hfourLe : (4 : ℝ) ≤ (radix : ℝ) ^ (2 : Int) := by
      rw [zpow_ofNat]
      nlinarith
    exact le_trans hfourLe hpowTwoLe
  have hAgeTwoB : 2 * B ≤ A := by
    have hpowSuccLe : (radix : ℝ) ^ ((s : Int) + 1) ≤ A := by
      dsimp [A]
      exact zpow_le_zpow_right₀ hradixOne (by omega)
    have htwoBLe : 2 * B ≤ (radix : ℝ) * B := by
      nlinarith [hBpos]
    have hsucc : (radix : ℝ) ^ ((s : Int) + 1) = (radix : ℝ) * B := by
      dsimp [B]
      rw [zpow_add₀ hradixNe, zpow_one]
      ring
    rw [hsucc] at hpowSuccLe
    exact le_trans htwoBLe hpowSuccLe
  have hpCoeffUpper : (A + D) * (B + 1) ≤ 2 * A * B := by
    have hBminus : 0 ≤ B - 1 := by linarith
    have hBplus : 0 ≤ B + 1 := by linarith
    have hDterm : D * (B + 1) ≤ (B / 2) * (B + 1) :=
      mul_le_mul_of_nonneg_right (le_of_lt hDltHalf) hBplus
    have hpoly : (B / 2) * (B + 1) ≤ 2 * B * (B - 1) := by
      nlinarith
    have hAterm : 2 * B * (B - 1) ≤ A * (B - 1) :=
      mul_le_mul_of_nonneg_right hAgeTwoB hBminus
    nlinarith
  have htwoABLeRadix : 2 * A * B ≤ (radix : ℝ) * A * B := by
    nlinarith [mul_pos hApos hBpos]
  have hsmallCast :
      (Zpower_nat radix (t - s - 1) : ℝ) =
        (radix : ℝ) ^ (((t - s - 1 : Nat) : Int)) := by
    have h := Zpower_nat_Z_powerRZ radix (t - s - 1)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Zpower_nat_Z_powerRZ_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hsmallMulB :
      (radix : ℝ) ^ (((t - s - 1 : Nat) : Int)) * B = A := by
    dsimp [A, B]
    rw [← zpow_add₀ hradixNe]
    congr 1
    omega
  have hxValue : _root_.F2R (beta:=radix) x = E * (A + D) := by
    dsimp [E, A, D, _root_.F2R, FloatSpec.Core.Defs.F2R]
    rw [hxNumDecomp, Int.cast_add, hpredCast]
    ring
  have hypValue :
      _root_.F2R (beta:=radix) yp = E * (A * B + A + D * B) := by
    dsimp [yp, E, A, B, D, _root_.F2R, FloatSpec.Core.Defs.F2R]
    rw [Int.cast_add, Int.cast_add, hpredCast, hsmallCast]
    rw [zpow_add₀ hradixNe]
    have hsmallMul :
        (radix : ℝ) ^ (((t - s - 1 : Nat) : Int)) *
            (radix : ℝ) ^ (s : Int) =
          (radix : ℝ) ^ (((t - 1 : Nat) : Int)) := by
      simpa only [A, B, Int.cast_ofNat] using hsmallMulB
    rw [show
      ((radix : ℝ) ^ (((t - 1 : Nat) : Int)) +
          (radix : ℝ) ^ (((t - s - 1 : Nat) : Int)) + (eps : ℝ)) *
          ((radix : ℝ) ^ (s : Int) * (radix : ℝ) ^ x.Fexp) =
        (radix : ℝ) ^ x.Fexp *
          ((radix : ℝ) ^ (((t - 1 : Nat) : Int)) *
              (radix : ℝ) ^ (s : Int) +
            (radix : ℝ) ^ (((t - s - 1 : Nat) : Int)) *
              (radix : ℝ) ^ (s : Int) +
            (eps : ℝ) * (radix : ℝ) ^ (s : Int)) by ring,
      hsmallMul]
  have hyqValue :
      _root_.F2R (beta:=radix) yq = E * (A * B + D * B) := by
    dsimp [yq, E, A, B, D, _root_.F2R, FloatSpec.Core.Defs.F2R]
    rw [Int.cast_add, hpredCast]
    rw [zpow_add₀ hradixNe]
    ring
  have hLowerPow :
      (radix : ℝ) ^ ((s : Int) + x.Fexp + (t : Int) - 1) =
        E * (A * B) := by
    calc
      (radix : ℝ) ^ ((s : Int) + x.Fexp + (t : Int) - 1) =
          (radix : ℝ) ^
            (x.Fexp + (((t - 1 : Nat) : Int)) + (s : Int)) := by
              congr 1
              omega
      _ = (radix : ℝ) ^ x.Fexp *
            (radix : ℝ) ^ (((t - 1 : Nat) : Int)) *
              (radix : ℝ) ^ (s : Int) := by
                rw [zpow_add₀ hradixNe, zpow_add₀ hradixNe]
      _ = E * (A * B) := by
        dsimp [E, A, B]
        ring
  have hUpperPow :
      (radix : ℝ) ^ ((s : Int) + x.Fexp + (t : Int)) =
        E * ((radix : ℝ) * A * B) := by
    calc
      (radix : ℝ) ^ ((s : Int) + x.Fexp + (t : Int)) =
          (radix : ℝ) ^
            (x.Fexp + (((t - 1 : Nat) : Int)) + 1 + (s : Int)) := by
              congr 1
              omega
      _ = (radix : ℝ) ^ x.Fexp *
            (radix : ℝ) ^ (((t - 1 : Nat) : Int)) *
              (radix : ℝ) ^ (1 : Int) * (radix : ℝ) ^ (s : Int) := by
                rw [zpow_add₀ hradixNe, zpow_add₀ hradixNe,
                  zpow_add₀ hradixNe]
      _ = E * ((radix : ℝ) * A * B) := by
        rw [zpow_one]
        ring
  have hpInputValue :
      _root_.F2R (beta:=radix) x * (B + 1) =
        E * ((A + D) * (B + 1)) := by
    rw [hxValue]
    ring
  have hpInputLower :
      (radix : ℝ) ^ ((s : Int) + x.Fexp + (t : Int) - 1) ≤
        _root_.F2R (beta:=radix) x * (B + 1) := by
    rw [hLowerPow, hpInputValue]
    apply mul_le_mul_of_nonneg_left _ (le_of_lt hEpos)
    have hDBnonneg : 0 ≤ D * B := mul_nonneg hDnonneg (le_of_lt hBpos)
    nlinarith only [hApos, hDnonneg, hDBnonneg]
  have hpInputUpper :
      _root_.F2R (beta:=radix) x * (B + 1) ≤
        (radix : ℝ) ^ ((s : Int) + x.Fexp + (t : Int)) := by
    rw [hUpperPow, hpInputValue]
    exact mul_le_mul_of_nonneg_left
      (le_trans hpCoeffUpper htwoABLeRadix) (le_of_lt hEpos)
  have hypLower :
      (radix : ℝ) ^ ((s : Int) + x.Fexp + (t : Int) - 1) ≤
        _root_.F2R (beta:=radix) yp := by
    rw [hLowerPow, hypValue]
    apply mul_le_mul_of_nonneg_left _ (le_of_lt hEpos)
    have hDBnonneg : 0 ≤ D * B := mul_nonneg hDnonneg (le_of_lt hBpos)
    linarith only [hApos, hDBnonneg]
  have hscalePow :
      (radix : ℝ) ^ ((s : Int) + x.Fexp) = E * B := by
    dsimp [E, B]
    rw [show (s : Int) + x.Fexp = x.Fexp + (s : Int) by ring,
      zpow_add₀ hradixNe]
  have hpDist :
      |_root_.F2R (beta:=radix) x * (B + 1) -
          _root_.F2R (beta:=radix) yp| <
        (radix : ℝ) ^ ((s : Int) + x.Fexp) / 2 := by
    rw [hpInputValue, hypValue, hscalePow]
    have hdiff :
        E * ((A + D) * (B + 1)) - E * (A * B + A + D * B) = E * D := by
      ring
    rw [hdiff, abs_of_nonneg (mul_nonneg (le_of_lt hEpos) hDnonneg)]
    have hmul := mul_lt_mul_of_pos_left hDltHalf hEpos
    calc
      E * D < E * (B / 2) := hmul
      _ = E * B / 2 := by ring
  have hpUnique :
      ∀ g : FloatSpec.Core.Defs.FlocqFloat radix,
        Closest (beta:=radix) b (radix : ℝ)
          (_root_.F2R (beta:=radix) x * (B + 1)) g →
          _root_.F2R (beta:=radix) yp = _root_.F2R (beta:=radix) g := by
    have h := ImplyClosestStrict (beta:=radix) b radix t
      (_root_.F2R (beta:=radix) x * (B + 1)) yp
      ((s : Int) + x.Fexp)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ImplyClosestStrict_check,
      Id.run, Int.cast_ofNat] using h ⟨hypBound, Or.inl hypNormal, hpInputLower, hpInputUpper,
        hypLower, hExpBound, rfl, hradix, htGtOne, hvNum, hpDist⟩
  have hpValue : _root_.F2R (beta:=radix) p = _root_.F2R (beta:=radix) yp := by
    exact (hpUnique p (by simpa only [B, Int.cast_ofNat] using hpDef)).symm
  have hqInputValue :
      -(_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p) =
        E * (A * B + D * B - D) := by
    rw [hxValue, hpValue, hypValue]
    ring
  have hqInputLower :
      (radix : ℝ) ^ ((s : Int) + x.Fexp + (t : Int) - 1) ≤
        -(_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p) := by
    rw [hLowerPow, hqInputValue]
    have hBminus : 0 ≤ B - 1 := by linarith
    have htailNonneg : 0 ≤ D * (B - 1) := mul_nonneg hDnonneg hBminus
    apply mul_le_mul_of_nonneg_left _ (le_of_lt hEpos)
    calc
      A * B ≤ A * B + D * (B - 1) := le_add_of_nonneg_right htailNonneg
      _ = A * B + D * B - D := by ring
  have hqCoeffUpper : A * B + D * B - D ≤ 2 * A * B := by
    have hpExpanded : (A + D) * (B + 1) = A * B + A + D * B + D := by ring
    rw [hpExpanded] at hpCoeffUpper
    exact le_trans (by nlinarith only [hApos, hDnonneg]) hpCoeffUpper
  have hqInputUpper :
      -(_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p) ≤
        (radix : ℝ) ^ ((s : Int) + x.Fexp + (t : Int)) := by
    rw [hUpperPow, hqInputValue]
    exact mul_le_mul_of_nonneg_left
      (le_trans hqCoeffUpper htwoABLeRadix) (le_of_lt hEpos)
  have hyqLower :
      (radix : ℝ) ^ ((s : Int) + x.Fexp + (t : Int) - 1) ≤
        _root_.F2R (beta:=radix) yq := by
    rw [hLowerPow, hyqValue]
    apply mul_le_mul_of_nonneg_left _ (le_of_lt hEpos)
    exact le_add_of_nonneg_right (mul_nonneg hDnonneg (le_of_lt hBpos))
  have hqDist :
      |-(_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p) -
          _root_.F2R (beta:=radix) yq| <
        (radix : ℝ) ^ ((s : Int) + x.Fexp) / 2 := by
    rw [hqInputValue, hyqValue, hscalePow]
    have hdiff :
        E * (A * B + D * B - D) - E * (A * B + D * B) = -(E * D) := by
      ring
    rw [hdiff, abs_neg, abs_of_nonneg (mul_nonneg (le_of_lt hEpos) hDnonneg)]
    have hmul := mul_lt_mul_of_pos_left hDltHalf hEpos
    calc
      E * D < E * (B / 2) := hmul
      _ = E * B / 2 := by ring
  have hqUnique :
      ∀ g : FloatSpec.Core.Defs.FlocqFloat radix,
        Closest (beta:=radix) b (radix : ℝ)
          (-(_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p)) g →
          _root_.F2R (beta:=radix) yq = _root_.F2R (beta:=radix) g := by
    have h := ImplyClosestStrict (beta:=radix) b radix t
      (-(_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p)) yq
      ((s : Int) + x.Fexp)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ImplyClosestStrict_check,
      Id.run, Int.cast_ofNat] using h ⟨hyqBound, Or.inl hyqNormal, hqInputLower, hqInputUpper,
        hyqLower, hExpBound, rfl, hradix, htGtOne, hvNum, hqDist⟩
  have hOppQ :
      Closest (beta:=radix) b (radix : ℝ)
        (-(_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p))
        (Fopp (beta:=radix) q) := by
    have h := ClosestOpp (beta:=radix) b (radix : ℝ) q
      (_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestOpp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hqDef
  have hyqOppValue :
      _root_.F2R (beta:=radix) yq =
        _root_.F2R (beta:=radix) (Fopp (beta:=radix) q) :=
    hqUnique (Fopp (beta:=radix) q) hOppQ
  have hFoppQ :
      _root_.F2R (beta:=radix) (Fopp (beta:=radix) q) =
        -_root_.F2R (beta:=radix) q := by
    have h := (FloatSpec.Calc.Operations.F2R_opp (beta:=radix) q) trivial
    simpa [Fopp, _root_.F2R] using h
  have hqValue : _root_.F2R (beta:=radix) q =
      -_root_.F2R (beta:=radix) yq := by
    linarith
  have hhxValue := hxExact (beta:=radix) b radix s t x p q hx rfl hradix
    htNe hvNum hxBound hpDef hqDef hhxDef hxPos hpNormal hqNormal hsGe hsLe
  rw [hhxValue, hpValue, hqValue, hypValue, hyqValue]
  have hthreshold :
      (radix : ℝ) ^ ((t : Int) - 1 + x.Fexp) = E * A := by
    calc
      (radix : ℝ) ^ ((t : Int) - 1 + x.Fexp) =
          (radix : ℝ) ^
            (x.Fexp + (((t - 1 : Nat) : Int))) := by
              congr 1
              omega
      _ = E * A := by
        dsimp [E, A]
        rw [zpow_add₀ hradixNe]
  rw [hthreshold]
  ring

/-! Coq Veltkamp local lemma `Veltkamp_aux_aux`.

Any canonical representative of the reconstructed high part under the reduced
`t - s` bound that stays within the half-ulp residual window is at least the
lower endpoint of `x`'s binade.  The low-mantissa branch reconstructs the high
part exactly; the complementary branch follows directly from the residual
bound. -/
theorem Veltkamp_aux_aux {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x p q hx : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hsGe : 2 ≤ s) (hsLe : s ≤ t - 2)
    (hxBound : Fbounded (beta:=beta) b x)
    (hpDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)) p)
    (hqDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hhxDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) q + _root_.F2R (beta:=beta) p) hx)
    (hxPos : 0 < _root_.F2R (beta:=beta) x)
    (hpNormal : Fnormal (beta:=beta) radix b p)
    (hqNormal : Fnormal (beta:=beta) radix b q)
    (hxNormal : Fnormal (beta:=beta) radix b x)
    (v : FloatSpec.Core.Defs.FlocqFloat beta)
    (hvValue : _root_.F2R (beta:=beta) v = _root_.F2R (beta:=beta) hx)
    (hvCanonic : Fcanonic (beta:=beta) radix
      (Veltkamp_reducedBound radix b s t) v)
    (hvResidual :
      |_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) v| ≤
        (radix : ℝ) ^ ((s : Int) + x.Fexp) / 2) :
    (radix : ℝ) ^ ((t : Int) - 1 + x.Fexp) ≤
      _root_.F2R (beta:=beta) v := by
  subst beta
  have hradixPosInt : (0 : Int) < radix := by omega
  have hradixPos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradixPosInt
  have hradixNe : (radix : ℝ) ≠ 0 := ne_of_gt hradixPos
  have hEpos : 0 < (radix : ℝ) ^ x.Fexp := zpow_pos hradixPos _
  by_cases hlarge :
      (radix : ℝ) ^ (((t - 1 : Nat) : Int)) +
          (radix : ℝ) ^ (s : Int) / 2 ≤ (x.Fnum : ℝ)
  · have hlargeMul :=
      mul_le_mul_of_nonneg_right hlarge (le_of_lt hEpos)
    have hxValue :
        _root_.F2R (beta:=radix) x =
          (x.Fnum : ℝ) * (radix : ℝ) ^ x.Fexp := by rfl
    have hthreshold :
        (radix : ℝ) ^ ((t : Int) - 1 + x.Fexp) =
          (radix : ℝ) ^ (((t - 1 : Nat) : Int)) *
            (radix : ℝ) ^ x.Fexp := by
      calc
        (radix : ℝ) ^ ((t : Int) - 1 + x.Fexp) =
            (radix : ℝ) ^
              ((((t - 1 : Nat) : Int)) + x.Fexp) := by
                congr 1
                omega
        _ = (radix : ℝ) ^ (((t - 1 : Nat) : Int)) *
              (radix : ℝ) ^ x.Fexp := zpow_add₀ hradixNe _ _
    have hhalfScale :
        (radix : ℝ) ^ ((s : Int) + x.Fexp) / 2 =
          ((radix : ℝ) ^ (s : Int) / 2) *
            (radix : ℝ) ^ x.Fexp := by
      rw [zpow_add₀ hradixNe]
      ring
    have hsumLeX :
        (radix : ℝ) ^ ((t : Int) - 1 + x.Fexp) +
            (radix : ℝ) ^ ((s : Int) + x.Fexp) / 2 ≤
          _root_.F2R (beta:=radix) x := by
      rw [hthreshold, hhalfScale, hxValue]
      nlinarith only [hlargeMul]
    have hresidualOneSided :
        _root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) v ≤
          (radix : ℝ) ^ ((s : Int) + x.Fexp) / 2 :=
      le_trans (le_abs_self _) hvResidual
    linarith only [hsumLeX, hresidualOneSided]
  · have hxNumLow : (x.Fnum : ℝ) <
        (radix : ℝ) ^ (((t - 1 : Nat) : Int)) +
          (radix : ℝ) ^ (s : Int) / 2 := lt_of_not_ge hlarge
    have hhxValue := Veltkamp_aux_aux_low (beta:=radix) b radix s t x p q hx
      rfl hradix hvNum hsGe hsLe hxBound hpDef hqDef hhxDef hxPos hpNormal
      hqNormal hxNormal hxNumLow
    rw [hvValue, hhxValue]

private theorem Veltkamp_aux_residual {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (s t : Nat)
    (x p q hx : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hsGe : 2 ≤ s) (hsLe : s ≤ t - 2)
    (hxBound : Fbounded (beta:=beta) b x)
    (hpDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)) p)
    (hqDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hhxDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) q + _root_.F2R (beta:=beta) p) hx)
    (hxPos : 0 < _root_.F2R (beta:=beta) x)
    (hpNormal : Fnormal (beta:=beta) radix b p)
    (hqNormal : Fnormal (beta:=beta) radix b q)
    (hxNormal : Fnormal (beta:=beta) radix b x) :
    |_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) hx| ≤
      (radix : ℝ) ^ ((s : Int) + x.Fexp) / 2 := by
  subst beta
  have htNe : t ≠ 0 := by omega
  have heq := eqEqual (beta:=radix) b radix s t x p q hx rfl hradix
    hvNum hsGe hsLe hxBound hpDef hqDef hhxDef hxPos hpNormal hqNormal hxNormal
  rcases heq with hqExp | hboundary
  · have hqUlp :
        2 * |(_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p) -
            _root_.F2R (beta:=radix) q| ≤
          Fulp (beta:=radix) b radix t q := by
      have h := ClosestUlp (beta:=radix) b radix t
        (_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p) q
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hqDef, rfl, hradix, htNe, hvNum⟩
    have hqError :
        2 * |(_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p) -
            _root_.F2R (beta:=radix) q| ≤
          (radix : ℝ) ^ q.Fexp := by
      have h := ClosestExp_from_ulp_payload (beta:=radix) b radix t
        (_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p) q
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestExp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hqDef, hqUlp, rfl, hradix⟩
    have hhxValue := hxExact (beta:=radix) b radix s t x p q hx rfl hradix
      htNe hvNum hxBound hpDef hqDef hhxDef hxPos hpNormal hqNormal hsGe hsLe
    calc
      |_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) hx| =
          |(_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p) -
            _root_.F2R (beta:=radix) q| := by rw [hhxValue]; congr 1 <;> ring
      _ ≤ (radix : ℝ) ^ ((s : Int) + x.Fexp) / 2 := by
        rw [← hqExp]
        nlinarith
  · exact hboundary.2

private theorem Veltkamp_aux_reduced_rep {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x p q hx : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hsGe : 2 ≤ s) (hsLe : s ≤ t - 2)
    (hxBound : Fbounded (beta:=beta) b x)
    (hpDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)) p)
    (hqDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hhxDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) q + _root_.F2R (beta:=beta) p) hx)
    (hxPos : 0 < _root_.F2R (beta:=beta) x)
    (hpNormal : Fnormal (beta:=beta) radix b p)
    (hqNormal : Fnormal (beta:=beta) radix b q)
    (hxNormal : Fnormal (beta:=beta) radix b x)
    (hResidual :
      |_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) hx| ≤
        (radix : ℝ) ^ ((s : Int) + x.Fexp) / 2) :
    ∃ v : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) v = _root_.F2R (beta:=beta) hx ∧
        Fcanonic (beta:=beta) radix (Veltkamp_reducedBound radix b s t) v := by
  subst beta
  let u : Nat := t - s
  let e : Int := (s : Int) + x.Fexp
  let sum : FloatSpec.Core.Defs.FlocqFloat radix := Fplus (beta:=radix) p q
  let rb : Fbound_skel := Veltkamp_reducedBound radix b s t
  have htNe : t ≠ 0 := by omega
  have huPos : 0 < u := by dsimp [u]; omega
  have huNe : u ≠ 0 := Nat.ne_of_gt huPos
  have hradixPosInt : (0 : Int) < radix := by omega
  have hradixPos : (0 : ℝ) < (radix : ℝ) := by exact_mod_cast hradixPosInt
  have hradixNe : (radix : ℝ) ≠ 0 := ne_of_gt hradixPos
  have hradixOne : (1 : ℝ) ≤ (radix : ℝ) := by
    exact_mod_cast (le_of_lt hradix)
  have hsumValue :
      _root_.F2R (beta:=radix) sum =
        _root_.F2R (beta:=radix) p + _root_.F2R (beta:=radix) q := by
    have h := Fplus_correct (beta:=radix) p q
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fplus_correct_check,
      Id.run, ULift.up_down, sum, Int.cast_ofNat] using h hradix
  have hhxValue := hxExact (beta:=radix) b radix s t x p q hx rfl hradix
    htNe hvNum hxBound hpDef hqDef hhxDef hxPos hpNormal hqNormal hsGe hsLe
  have hsumHx :
      _root_.F2R (beta:=radix) sum = _root_.F2R (beta:=radix) hx := by
    rw [hsumValue, hhxValue]
  have hsumExpMin : sum.Fexp = min p.Fexp q.Fexp := by
    cases p with
    | mk pm pe =>
      cases q with
      | mk qm qe =>
        by_cases hle : pe ≤ qe
        · simp [sum, Fplus, FloatSpec.Calc.Operations.Fplus,
            FloatSpec.Calc.Operations.Falign, hle, min_eq_left hle]
        · have hqe : qe ≤ pe := le_of_lt (lt_of_not_ge hle)
          simp [sum, Fplus, FloatSpec.Calc.Operations.Fplus,
            FloatSpec.Calc.Operations.Falign, hle, min_eq_right hqe]
  have hqLeP : q.Fexp ≤ p.Fexp :=
    eqLeep (beta:=radix) b radix s t x p q rfl hradix hvNum hxBound
      hpDef hqDef hxPos hpNormal hqNormal
  have hsumExp : sum.Fexp = q.Fexp := by
    rw [hsumExpMin, min_eq_right hqLeP]
  have heLeQ : e ≤ q.Fexp := by
    simpa only [e, Int.cast_ofNat] using
      (eqGe (beta:=radix) b radix s t x p q hx rfl hradix hvNum hsGe hsLe
        hxBound hpDef hqDef hhxDef hxPos hpNormal hqNormal hxNormal)
  have heLeSum : e ≤ sum.Fexp := by rw [hsumExp]; exact heLeQ
  have hxNumPos : 0 < x.Fnum :=
    FloatSpec.Core.Float_prop.gt_0_F2R (beta:=radix) x hradix hxPos
  have hxNumLt : x.Fnum < Zpower_nat radix t := by
    have h := hxBound.1
    rw [abs_of_pos hxNumPos, hvNum] at h
    exact h
  have hpowTCast :
      (Zpower_nat radix t : ℝ) = (radix : ℝ) ^ (t : Int) := by
    have h := Zpower_nat_Z_powerRZ radix t
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Zpower_nat_Z_powerRZ_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hxNumRealLt : (x.Fnum : ℝ) < (radix : ℝ) ^ (t : Int) := by
    rw [← hpowTCast]
    exact_mod_cast hxNumLt
  have hxPowPos : 0 < (radix : ℝ) ^ x.Fexp := zpow_pos hradixPos _
  have hxTop :
      _root_.F2R (beta:=radix) x <
        (radix : ℝ) ^ ((t : Int) + x.Fexp) := by
    calc
      _root_.F2R (beta:=radix) x =
          (x.Fnum : ℝ) * (radix : ℝ) ^ x.Fexp := rfl
      _ < (radix : ℝ) ^ (t : Int) * (radix : ℝ) ^ x.Fexp :=
        mul_lt_mul_of_pos_right hxNumRealLt hxPowPos
      _ = (radix : ℝ) ^ ((t : Int) + x.Fexp) := by
        rw [zpow_add₀ hradixNe]
  have hhxAbsUpper :
      |_root_.F2R (beta:=radix) hx| <
        (radix : ℝ) ^ ((t : Int) + x.Fexp) +
          (radix : ℝ) ^ e / 2 := by
    have htri :
        |_root_.F2R (beta:=radix) hx| ≤
          |_root_.F2R (beta:=radix) x| +
            |_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) hx| := by
      calc
        |_root_.F2R (beta:=radix) hx| =
            |_root_.F2R (beta:=radix) x -
              (_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) hx)| := by
                congr 1 <;> ring
        _ ≤ |_root_.F2R (beta:=radix) x| +
              |_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) hx| :=
            abs_sub _ _
    rw [abs_of_pos hxPos] at htri
    simpa only [e, Int.cast_ofNat] using
      (lt_of_le_of_lt htri (add_lt_add_of_lt_of_le hxTop hResidual))
  have huCast : (u : Int) = (t : Int) - (s : Int) := by
    dsimp [u]
    omega
  have htopFactor :
      (radix : ℝ) ^ ((t : Int) + x.Fexp) =
        (radix : ℝ) ^ e * (radix : ℝ) ^ (u : Int) := by
    rw [← zpow_add₀ hradixNe]
    congr 1
    dsimp [e]
    omega
  have hsumScale :
      (radix : ℝ) ^ e ≤ (radix : ℝ) ^ sum.Fexp :=
    zpow_le_zpow_right₀ hradixOne heLeSum
  have hcoeffPos :
      0 < (radix : ℝ) ^ (u : Int) + 1 / 2 := by
    positivity
  have hhxAbsScaled :
      |_root_.F2R (beta:=radix) hx| <
        (radix : ℝ) ^ sum.Fexp *
          ((radix : ℝ) ^ (u : Int) + 1 / 2) := by
    rw [htopFactor] at hhxAbsUpper
    have hfactor :
        (radix : ℝ) ^ e * ((radix : ℝ) ^ (u : Int) + 1 / 2) ≤
          (radix : ℝ) ^ sum.Fexp *
            ((radix : ℝ) ^ (u : Int) + 1 / 2) :=
      mul_le_mul_of_nonneg_right hsumScale (le_of_lt hcoeffPos)
    apply lt_of_lt_of_le _ hfactor
    nlinarith [hhxAbsUpper]
  have hsumPowPos : 0 < (radix : ℝ) ^ sum.Fexp := zpow_pos hradixPos _
  have hsumAbsValue :
      |_root_.F2R (beta:=radix) sum| =
        |(sum.Fnum : ℝ)| * (radix : ℝ) ^ sum.Fexp := by
    simp [_root_.F2R, FloatSpec.Core.Defs.F2R, abs_mul,
      abs_of_pos hsumPowPos]
  have hsumNumRealLt :
      |(sum.Fnum : ℝ)| < (radix : ℝ) ^ (u : Int) + 1 / 2 := by
    rw [← hsumHx, hsumAbsValue] at hhxAbsScaled
    nlinarith
  have hsumNumRealLtSucc :
      |(sum.Fnum : ℝ)| < (radix : ℝ) ^ (u : Int) + 1 := by
    linarith
  have hpowUCast :
      (Zpower_nat radix u : ℝ) = (radix : ℝ) ^ (u : Int) := by
    have h := Zpower_nat_Z_powerRZ radix u
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Zpower_nat_Z_powerRZ_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hsumNumLtSucc : |sum.Fnum| < Zpower_nat radix u + 1 := by
    exact_mod_cast (show (|sum.Fnum| : ℝ) < (Zpower_nat radix u : ℝ) + 1 by
      simpa only [Int.cast_abs, hpowUCast, Int.cast_add, Int.cast_one, Int.cast_ofNat] using
        hsumNumRealLtSucc)
  have hsumNumBound : |sum.Fnum| ≤ Zpower_nat radix u := by omega
  have hsumExpBound : -rb.dExp ≤ sum.Fexp := by
    dsimp [rb, Veltkamp_reducedBound]
    exact le_trans hxBound.2 (le_trans (by dsimp [e]; omega) heLeSum)
  have hrvNum : rb.vNum = Zpower_nat radix u := by
    dsimp [rb, u]
    exact p'GivesBound radix b s t
  have hmb := FboundedMbound (beta:=radix) rb radix u sum.Fexp sum.Fnum
  rcases (by
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FboundedMbound_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      hmb ⟨rfl, hradix, huPos, hrvNum, hsumNumBound, hsumExpBound⟩) with
    ⟨c, hcBound, hcValue⟩
  have hcHx : _root_.F2R (beta:=radix) c = _root_.F2R (beta:=radix) hx := by
    rw [hcValue, ← hsumHx]
    rfl
  let v : FloatSpec.Core.Defs.FlocqFloat radix :=
    Fnormalize (beta:=radix) radix rb u c
  have hvValue :
      _root_.F2R (beta:=radix) v = _root_.F2R (beta:=radix) hx := by
    have h := FnormalizeCorrect (beta:=radix) radix rb u c
    have hvc : _root_.F2R (beta:=radix) v = _root_.F2R (beta:=radix) c := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
        Id.run, ULift.up_down, v, Int.cast_ofNat] using h ⟨rfl, hradix⟩
    exact hvc.trans hcHx
  have hvCan : Fcanonic (beta:=radix) radix rb v := by
    have h := FnormalizeCanonic (beta:=radix) radix rb u c
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
      Id.run, ULift.up_down, Fbounded', v, Int.cast_ofNat] using
      h ⟨hcBound, hcBound, huNe, hradix, hrvNum⟩
  exact ⟨v, hvValue, hvCan⟩

private theorem VeltkampEven1_candidate {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x p q hx : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hsGe : 2 ≤ s) (hsLe : s ≤ t - 2)
    (hxBound : Fbounded (beta:=beta) b x)
    (hpDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)) p)
    (hqDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hhxDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) q + _root_.F2R (beta:=beta) p) hx)
    (hxPos : 0 < _root_.F2R (beta:=beta) x)
    (hpNormal : Fnormal (beta:=beta) radix b p)
    (hqNormal : Fnormal (beta:=beta) radix b q)
    (hxNormal : Fnormal (beta:=beta) radix b x)
    (hEvenRadix : Even radix)
    (hResidual :
      |_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) hx| ≤
        (radix : ℝ) ^ ((s : Int) + x.Fexp) / 2) :
    ∃ v : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) v = _root_.F2R (beta:=beta) hx ∧
        Fcanonic (beta:=beta) radix
          (Veltkamp_reducedBound radix b s t) v ∧
        (FNeven (beta:=beta) (Veltkamp_reducedBound radix b s t)
            (radix : ℝ) (t - s) v ∨
          v.Fexp ≤ (s : Int) + x.Fexp) := by
  subst beta
  let u : Nat := t - s
  let e : Int := (s : Int) + x.Fexp
  let rb : Fbound_skel := Veltkamp_reducedBound radix b s t
  let sum : FloatSpec.Core.Defs.FlocqFloat radix := Fplus (beta:=radix) p q
  have htNe : t ≠ 0 := by omega
  have huGtOne : 1 < u := by dsimp [u]; omega
  have huPos : 0 < u := lt_trans Nat.zero_lt_one huGtOne
  have huNe : u ≠ 0 := Nat.ne_of_gt huPos
  have hradixPosInt : (0 : Int) < radix := by omega
  have hradixPos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradixPosInt
  have hradixNe : (radix : ℝ) ≠ 0 := ne_of_gt hradixPos
  have hsumValue :
      _root_.F2R (beta:=radix) sum =
        _root_.F2R (beta:=radix) p + _root_.F2R (beta:=radix) q := by
    have h := Fplus_correct (beta:=radix) p q
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fplus_correct_check,
      Id.run, ULift.up_down, sum, Int.cast_ofNat] using h hradix
  have hhxValue := hxExact (beta:=radix) b radix s t x p q hx rfl hradix
    htNe hvNum hxBound hpDef hqDef hhxDef hxPos hpNormal hqNormal hsGe hsLe
  have hsumHx :
      _root_.F2R (beta:=radix) sum = _root_.F2R (beta:=radix) hx := by
    rw [hsumValue, hhxValue]
  have hsumExpMin : sum.Fexp = min p.Fexp q.Fexp := by
    cases p with
    | mk pm pe =>
      cases q with
      | mk qm qe =>
        by_cases hle : pe ≤ qe
        · simp [sum, Fplus, FloatSpec.Calc.Operations.Fplus,
            FloatSpec.Calc.Operations.Falign, hle, min_eq_left hle]
        · have hqe : qe ≤ pe := le_of_lt (lt_of_not_ge hle)
          simp [sum, Fplus, FloatSpec.Calc.Operations.Fplus,
            FloatSpec.Calc.Operations.Falign, hle, min_eq_right hqe]
  have hqLeP : q.Fexp ≤ p.Fexp :=
    eqLeep (beta:=radix) b radix s t x p q rfl hradix hvNum hxBound
      hpDef hqDef hxPos hpNormal hqNormal
  have hsumExp : sum.Fexp = q.Fexp := by
    rw [hsumExpMin, min_eq_right hqLeP]
  have heLeSum : e ≤ sum.Fexp := by
    rw [hsumExp]
    simpa only [e, Int.cast_ofNat] using
      (eqGe (beta:=radix) b radix s t x p q hx rfl hradix hvNum hsGe hsLe
        hxBound hpDef hqDef hhxDef hxPos hpNormal hqNormal hxNormal)
  let k : Nat := Int.toNat (sum.Fexp - e)
  let m : Int := sum.Fnum * Zpower_nat radix k
  have hdiffNonneg : 0 ≤ sum.Fexp - e := sub_nonneg.mpr heLeSum
  have hkCast : (k : Int) = sum.Fexp - e := by
    simpa [k] using Int.toNat_of_nonneg hdiffNonneg
  have hpowKCast :
      (Zpower_nat radix k : ℝ) =
        (radix : ℝ) ^ (sum.Fexp - e) := by
    have h := Zpower_nat_Z_powerRZ radix k
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Zpower_nat_Z_powerRZ_check,
      Id.run, ULift.up_down, hkCast, Int.cast_ofNat] using h trivial
  have hsumPowSplit :
      (radix : ℝ) ^ sum.Fexp =
        (radix : ℝ) ^ (sum.Fexp - e) * (radix : ℝ) ^ e := by
    calc
      (radix : ℝ) ^ sum.Fexp =
          (radix : ℝ) ^ ((sum.Fexp - e) + e) := by congr 1 <;> ring
      _ = (radix : ℝ) ^ (sum.Fexp - e) * (radix : ℝ) ^ e :=
        zpow_add₀ hradixNe _ _
  have hmRepr :
      _root_.F2R (beta:=radix) hx =
        (m : ℝ) * (radix : ℝ) ^ e := by
    rw [← hsumHx]
    dsimp [_root_.F2R, FloatSpec.Core.Defs.F2R, m]
    rw [Int.cast_mul, hpowKCast, hsumPowSplit]
    ring
  have hxNumPos : 0 < x.Fnum :=
    FloatSpec.Core.Float_prop.gt_0_F2R (beta:=radix) x hradix hxPos
  have hxNumLt : x.Fnum < Zpower_nat radix t := by
    have h := hxBound.1
    rw [abs_of_pos hxNumPos, hvNum] at h
    exact h
  have hpowTCast :
      (Zpower_nat radix t : ℝ) = (radix : ℝ) ^ (t : Int) := by
    have h := Zpower_nat_Z_powerRZ radix t
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Zpower_nat_Z_powerRZ_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hxNumRealLt : (x.Fnum : ℝ) < (radix : ℝ) ^ (t : Int) := by
    rw [← hpowTCast]
    exact_mod_cast hxNumLt
  have hxPowPos : 0 < (radix : ℝ) ^ x.Fexp := zpow_pos hradixPos _
  have hxTop :
      _root_.F2R (beta:=radix) x <
        (radix : ℝ) ^ ((t : Int) + x.Fexp) := by
    calc
      _root_.F2R (beta:=radix) x =
          (x.Fnum : ℝ) * (radix : ℝ) ^ x.Fexp := rfl
      _ < (radix : ℝ) ^ (t : Int) * (radix : ℝ) ^ x.Fexp :=
        mul_lt_mul_of_pos_right hxNumRealLt hxPowPos
      _ = (radix : ℝ) ^ ((t : Int) + x.Fexp) := by
        rw [zpow_add₀ hradixNe]
  have hhxAbsUpper :
      |_root_.F2R (beta:=radix) hx| <
        (radix : ℝ) ^ ((t : Int) + x.Fexp) +
          (radix : ℝ) ^ e / 2 := by
    have htri :
        |_root_.F2R (beta:=radix) hx| ≤
          |_root_.F2R (beta:=radix) x| +
            |_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) hx| := by
      calc
        |_root_.F2R (beta:=radix) hx| =
            |_root_.F2R (beta:=radix) x -
              (_root_.F2R (beta:=radix) x -
                _root_.F2R (beta:=radix) hx)| := by congr 1 <;> ring
        _ ≤ |_root_.F2R (beta:=radix) x| +
              |_root_.F2R (beta:=radix) x -
                _root_.F2R (beta:=radix) hx| := abs_sub _ _
    rw [abs_of_pos hxPos] at htri
    simpa only [e, Int.cast_ofNat] using
      (lt_of_le_of_lt htri (add_lt_add_of_lt_of_le hxTop hResidual))
  have htopFactor :
      (radix : ℝ) ^ ((t : Int) + x.Fexp) =
        (radix : ℝ) ^ e * (radix : ℝ) ^ (u : Int) := by
    rw [← zpow_add₀ hradixNe]
    congr 1
    dsimp [e, u]
    omega
  have hePowPos : 0 < (radix : ℝ) ^ e := zpow_pos hradixPos _
  have hmAbsRealLt :
      (|m| : ℝ) < (radix : ℝ) ^ (u : Int) + 1 / 2 := by
    rw [hmRepr, abs_mul, abs_of_pos hePowPos, htopFactor] at hhxAbsUpper
    have hmul :
        (radix : ℝ) ^ e * |(m : ℝ)| <
          (radix : ℝ) ^ e * ((radix : ℝ) ^ (u : Int) + 1 / 2) := by
      nlinarith [hhxAbsUpper]
    simpa only [Int.cast_abs, Int.cast_ofNat] using
      (lt_of_mul_lt_mul_left hmul hePowPos.le)
  have hpowUCast :
      (Zpower_nat radix u : ℝ) = (radix : ℝ) ^ (u : Int) := by
    have h := Zpower_nat_Z_powerRZ radix u
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Zpower_nat_Z_powerRZ_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hmLtSucc : |m| < Zpower_nat radix u + 1 := by
    exact_mod_cast (show (|m| : ℝ) < (Zpower_nat radix u : ℝ) + 1 by
      rw [hpowUCast]
      linarith)
  have hmBound : |m| ≤ Zpower_nat radix u := by omega
  have heBound : -rb.dExp ≤ e := by
    dsimp [rb, Veltkamp_reducedBound, e]
    exact le_trans hxBound.2 (by omega)
  have hrvNum : rb.vNum = Zpower_nat radix u := by
    dsimp [rb, u]
    exact p'GivesBound radix b s t
  by_cases hmStrict : |m| < Zpower_nat radix u
  · let c : FloatSpec.Core.Defs.FlocqFloat radix := ⟨m, e⟩
    have hcBound : Fbounded (beta:=radix) rb c := by
      exact ⟨by simpa only [c, hrvNum, Int.cast_ofNat] using hmStrict,
        by simpa only [c, Int.cast_ofNat] using heBound⟩
    let v : FloatSpec.Core.Defs.FlocqFloat radix :=
      Fnormalize (beta:=radix) radix rb u c
    have hvValue :
        _root_.F2R (beta:=radix) v = _root_.F2R (beta:=radix) hx := by
      have h := FnormalizeCorrect (beta:=radix) radix rb u c
      have hvc :
          _root_.F2R (beta:=radix) v = _root_.F2R (beta:=radix) c := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
          Id.run, ULift.up_down, v, Int.cast_ofNat] using h ⟨rfl, hradix⟩
      calc
        _root_.F2R (beta:=radix) v =
            _root_.F2R (beta:=radix) c := hvc
        _ = (m : ℝ) * (radix : ℝ) ^ e := rfl
        _ = _root_.F2R (beta:=radix) hx := hmRepr.symm
    have hvCan : Fcanonic (beta:=radix) radix rb v := by
      have h := FnormalizeCanonic (beta:=radix) radix rb u c
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
        Id.run, ULift.up_down, Fbounded', v, Int.cast_ofNat] using
        h ⟨hcBound, hcBound, huNe, hradix, hrvNum⟩
    have hvExp : v.Fexp ≤ e := by
      dsimp [v, Fnormalize]
      by_cases hmZero : m = 0
      · simp [c, hmZero, heBound]
      · simp [c, hmZero, Fshift]
    exact ⟨v, hvValue, hvCan, Or.inr (by simpa only [e, Int.cast_ofNat] using hvExp)⟩
  · have hmEq : |m| = Zpower_nat radix u := by omega
    let base : FloatSpec.Core.Defs.FlocqFloat radix :=
      ⟨nNormMin radix u, e + 1⟩
    have hbaseCan : Fcanonic (beta:=radix) radix rb base := by
      exact FcanonicNnormMin (beta:=radix) radix rb u (e + 1) hradix huPos
        hrvNum (by omega)
    have hbaseValue :
        _root_.F2R (beta:=radix) base =
          (Zpower_nat radix u : ℝ) * (radix : ℝ) ^ e := by
      have hpredPow :
          ((radix ^ (u - 1) : Int) : ℝ) =
            (radix : ℝ) ^ (((u - 1 : Nat) : Int)) := by
        rw [Int.cast_pow]
        exact (zpow_ofNat (radix : ℝ) (u - 1)).symm
      have huPow :
          ((radix ^ u : Int) : ℝ) = (radix : ℝ) ^ (u : Int) := by
        rw [Int.cast_pow]
        exact (zpow_ofNat (radix : ℝ) u).symm
      dsimp [base, _root_.F2R, FloatSpec.Core.Defs.F2R, nNormMin,
        Zpower_nat]
      rw [hpredPow, huPow]
      have huPredCast : ((u - 1 : Nat) : Int) = (u : Int) - 1 := by omega
      rw [huPredCast]
      rw [← zpow_add₀ hradixNe, ← zpow_add₀ hradixNe]
      congr 1
      omega
    have hbaseEven :
        FNeven (beta:=radix) rb (radix : ℝ) u base := by
      have hnorm : Fnormalize (beta:=radix) radix rb u base = base := by
        have h := FcanonicFnormalizeEq (beta:=radix) radix rb u base
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          h ⟨hbaseCan, rfl, hradix, huNe, hrvNum⟩
      unfold FNeven Feven
      rw [hnorm]
      dsimp [base, nNormMin]
      exact hEvenRadix.pow_of_ne_zero (by omega)
    by_cases hmNonneg : 0 ≤ m
    · have hmPosEq : m = Zpower_nat radix u := by
        simpa [abs_of_nonneg hmNonneg] using hmEq
      have hvalue :
          _root_.F2R (beta:=radix) base =
            _root_.F2R (beta:=radix) hx := by
        rw [hbaseValue, hmRepr, hmPosEq]
      exact ⟨base, hvalue, hbaseCan, Or.inl hbaseEven⟩
    · have hmNeg : m < 0 := lt_of_not_ge hmNonneg
      have hmNegEq : m = -Zpower_nat radix u := by
        have h := hmEq
        rw [abs_of_neg hmNeg] at h
        omega
      let v : FloatSpec.Core.Defs.FlocqFloat radix := Fopp (beta:=radix) base
      have hvCan : Fcanonic (beta:=radix) radix rb v := by
        have h := FcanonicFopp (beta:=radix) radix rb base
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFopp_check,
          Id.run, ULift.up_down, v, Int.cast_ofNat] using h hbaseCan
      have hvValue :
          _root_.F2R (beta:=radix) v = _root_.F2R (beta:=radix) hx := by
        have hopp :
            _root_.F2R (beta:=radix) v =
              -_root_.F2R (beta:=radix) base := by
          have h := (FloatSpec.Calc.Operations.F2R_opp (beta:=radix) base) trivial
          simpa [v, Fopp, _root_.F2R] using h
        rw [hopp, hbaseValue, hmRepr, hmNegEq]
        norm_num [Int.cast_neg]
      have hvEven : FNeven (beta:=radix) rb (radix : ℝ) u v := by
        have h := FNevenFop (beta:=radix) rb (radix : ℝ) u base
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FNevenFop_check,
          Id.run, ULift.up_down, v, Int.cast_ofNat] using h hbaseEven
      exact ⟨v, hvValue, hvCan, Or.inl hvEven⟩

private theorem VeltkampEven1_tie_even {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x p q hx v : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hsGe : 2 ≤ s) (hsLe : s ≤ t - 2)
    (hxBound : Fbounded (beta:=beta) b x)
    (hpDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)) p)
    (hqDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hhxDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) q + _root_.F2R (beta:=beta) p) hx)
    (hxPos : 0 < _root_.F2R (beta:=beta) x)
    (hpNormal : Fnormal (beta:=beta) radix b p)
    (hqNormal : Fnormal (beta:=beta) radix b q)
    (hxNormal : Fnormal (beta:=beta) radix b x)
    (hpDefEven : EvenClosest (beta:=beta) b (radix : ℝ) t
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)) p)
    (hqDefEven : EvenClosest (beta:=beta) b (radix : ℝ) t
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hEvenRadix : Even radix)
    (hvValue : _root_.F2R (beta:=beta) v = _root_.F2R (beta:=beta) hx)
    (hvCan : Fcanonic (beta:=beta) radix
      (Veltkamp_reducedBound radix b s t) v)
    (hvExp : v.Fexp = (s : Int) + x.Fexp)
    (hTie :
      |_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) v| =
        (radix : ℝ) ^ ((s : Int) + x.Fexp) / 2) :
    FNeven (beta:=beta) (Veltkamp_reducedBound radix b s t)
      (radix : ℝ) (t - s) v := by
  subst beta
  let e : Int := (s : Int) + x.Fexp
  let rb : Fbound_skel := Veltkamp_reducedBound radix b s t
  let u : Nat := t - s
  have htGtOne : 1 < t := by omega
  have htNe : t ≠ 0 := by omega
  have huGtOne : 1 < u := by dsimp [u]; omega
  have huNe : u ≠ 0 := by omega
  have hradixPosInt : (0 : Int) < radix := by omega
  have hradixPos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradixPosInt
  have hradixNe : (radix : ℝ) ≠ 0 := ne_of_gt hradixPos
  have hePowPos : 0 < (radix : ℝ) ^ e := zpow_pos hradixPos _
  have hvExpE : v.Fexp = e := by simpa only [e, Int.cast_ofNat] using hvExp
  have hvExpanded :
      _root_.F2R (beta:=radix) v =
        (v.Fnum : ℝ) * (radix : ℝ) ^ e := by
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, hvExpE]
  have hmid :
      ∃ eps : ℝ, (eps = 1 ∨ eps = -1) ∧
        _root_.F2R (beta:=radix) x =
          _root_.F2R (beta:=radix) v + eps * (radix : ℝ) ^ e / 2 := by
    by_cases hdiff : 0 ≤
        _root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) v
    · refine ⟨1, Or.inl rfl, ?_⟩
      rw [abs_of_nonneg hdiff] at hTie
      simpa only [e, one_mul, Int.cast_ofNat] using (by linarith [hTie])
    · have hdiffNeg :
          _root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) v < 0 :=
        lt_of_not_ge hdiff
      refine ⟨-1, Or.inr rfl, ?_⟩
      rw [abs_of_neg hdiffNeg] at hTie
      simpa only [e, neg_mul, one_mul, Int.cast_ofNat] using (by linarith [hTie])
  rcases hmid with ⟨eps, heps, hxMid⟩
  have heLeQ : e ≤ q.Fexp := by
    simpa only [e, Int.cast_ofNat] using
      (eqGe (beta:=radix) b radix s t x p q hx rfl hradix hvNum hsGe hsLe
        hxBound hpDef hqDef hhxDef hxPos hpNormal hqNormal hxNormal)
  have hqLeP : q.Fexp ≤ p.Fexp :=
    eqLeep (beta:=radix) b radix s t x p q rfl hradix hvNum hxBound
      hpDef hqDef hxPos hpNormal hqNormal
  have hpLe : p.Fexp ≤ e + 1 := by
    have h := epLe (beta:=radix) b radix s t x p rfl hradix hvNum hpDef
      hxPos hpNormal hxNormal hsGe
    dsimp [e]
    omega
  have hpCases : p.Fexp = e ∨ p.Fexp = e + 1 := by omega
  have hqCases : q.Fexp = e ∨ q.Fexp = e + 1 := by omega
  let mp : Int := if p.Fexp = e then p.Fnum else p.Fnum * radix
  let mq : Int := if q.Fexp = e then q.Fnum else q.Fnum * radix
  have hsuccPow :
      (radix : ℝ) ^ (e + 1) =
        (radix : ℝ) * (radix : ℝ) ^ e := by
    rw [zpow_add₀ hradixNe, zpow_one]
    ring
  have hpCoeffValue :
      _root_.F2R (beta:=radix) p =
        (mp : ℝ) * (radix : ℝ) ^ e := by
    rcases hpCases with hpEq | hpEq
    · simp [mp, hpEq, _root_.F2R, FloatSpec.Core.Defs.F2R]
    · have hpNe : p.Fexp ≠ e := by omega
      rw [show mp = p.Fnum * radix by simp [mp, hpNe]]
      simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, Int.cast_mul, hpEq,
        hsuccPow]
      ring
  have hqCoeffValue :
      _root_.F2R (beta:=radix) q =
        (mq : ℝ) * (radix : ℝ) ^ e := by
    rcases hqCases with hqEq | hqEq
    · simp [mq, hqEq, _root_.F2R, FloatSpec.Core.Defs.F2R]
    · have hqNe : q.Fexp ≠ e := by omega
      rw [show mq = q.Fnum * radix by simp [mq, hqNe]]
      simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, Int.cast_mul, hqEq,
        hsuccPow]
      ring
  have hhxValue := hxExact (beta:=radix) b radix s t x p q hx rfl hradix
    htNe hvNum hxBound hpDef hqDef hhxDef hxPos hpNormal hqNormal hsGe hsLe
  have hvSum :
      _root_.F2R (beta:=radix) v =
        _root_.F2R (beta:=radix) p + _root_.F2R (beta:=radix) q := by
    rw [hvValue, hhxValue]
  have hvNumEq : v.Fnum = mp + mq := by
    rw [hvExpanded, hpCoeffValue, hqCoeffValue] at hvSum
    have hcast : (v.Fnum : ℝ) = (mp + mq : Int) := by
      norm_num
      nlinarith [hvSum, hePowPos]
    exact_mod_cast hcast
  have hpowSCast :
      (Zpower_nat radix s : ℝ) = (radix : ℝ) ^ (s : Int) := by
    have h := Zpower_nat_Z_powerRZ radix s
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Zpower_nat_Z_powerRZ_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hhalfPow :
      ∃ half : Int, (half : ℝ) = (radix : ℝ) ^ (s : Int) / 2 := by
    rcases hEvenRadix with ⟨r, hr⟩
    let half : Int := r * Zpower_nat radix (s - 1)
    refine ⟨half, ?_⟩
    have hsSucc : Nat.succ (s - 1) = s := by omega
    have hpowPredCast :
        (Zpower_nat radix (s - 1) : ℝ) =
          (radix : ℝ) ^ (((s - 1 : Nat) : Int)) := by
      have h := Zpower_nat_Z_powerRZ radix (s - 1)
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Zpower_nat_Z_powerRZ_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
    have hsPredCast : ((s - 1 : Nat) : Int) = (s : Int) - 1 := by omega
    dsimp [half]
    rw [Int.cast_mul, hpowPredCast, hsPredCast]
    have hradixCast : (radix : ℝ) = (r : ℝ) + (r : ℝ) := by
      exact_mod_cast hr
    rw [show (radix : ℝ) ^ (s : Int) =
        (radix : ℝ) ^ ((s : Int) - 1) * (radix : ℝ) by
      calc
        (radix : ℝ) ^ (s : Int) =
            (radix : ℝ) ^ (((s : Int) - 1) + 1) := by congr 1 <;> omega
        _ = (radix : ℝ) ^ ((s : Int) - 1) * (radix : ℝ) ^ (1 : Int) :=
          zpow_add₀ hradixNe _ _
        _ = _ := by rw [zpow_one]]
    rw [hradixCast]
    ring
  have hpNonneg : 0 ≤ _root_.F2R (beta:=radix) p :=
    pPos (beta:=radix) b radix s t x p hradix hvNum hxPos hpDef
  have hmpEven : Even mp := by
    by_cases hpExpEq : p.Fexp = e
    · let pmid : ℝ :=
        (v.Fnum : ℝ) * (radix : ℝ) ^ (s : Int) + (v.Fnum : ℝ) +
          eps * (radix : ℝ) ^ (s : Int) / 2 + (eps - 1) / 2
      have hpInputMid :
          _root_.F2R (beta:=radix) x *
              ((radix : ℝ) ^ (s : Int) + 1) =
            (radix : ℝ) ^ p.Fexp * (pmid + 1 / 2) := by
        rw [hpExpEq, hxMid, hvExpanded]
        dsimp [pmid]
        ring
      have hpmidInt : ∃ n : Int, (n : ℝ) = pmid := by
        rcases hhalfPow with ⟨half, hhalf⟩
        rcases heps with hepsPos | hepsNeg
        · refine ⟨v.Fnum * Zpower_nat radix s + v.Fnum + half, ?_⟩
          dsimp [pmid]
          rw [hepsPos, Int.cast_add, Int.cast_add, Int.cast_mul,
            hpowSCast, hhalf]
          norm_num
        · refine ⟨v.Fnum * Zpower_nat radix s + v.Fnum - half - 1, ?_⟩
          dsimp [pmid]
          rw [hepsNeg, Int.cast_sub, Int.cast_sub, Int.cast_add,
            Int.cast_mul, hpowSCast, hhalf]
          norm_num
          ring
      have hpFN : FNeven (beta:=radix) b (radix : ℝ) t p := by
        have h := ClosestImplyEven_int (beta:=radix) b radix t
          (_root_.F2R (beta:=radix) x * ((radix : ℝ) ^ (s : Int) + 1))
          pmid p
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestImplyEven_int_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          h ⟨hEvenRadix, hpDefEven, Or.inl hpNormal, hpNonneg, hpInputMid,
            hpmidInt, rfl, hradix, htGtOne, hvNum⟩
      have hpNorm : Fnormalize (beta:=radix) radix b t p = p := by
        have h := FcanonicFnormalizeEq (beta:=radix) radix b t p
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          h ⟨Or.inl hpNormal, rfl, hradix, htNe, hvNum⟩
      unfold FNeven Feven at hpFN
      rw [hpNorm] at hpFN
      simpa [mp, hpExpEq] using hpFN
    · have hpSucc : p.Fexp = e + 1 := hpCases.resolve_left hpExpEq
      have hprod : Even (p.Fnum * radix) := hEvenRadix.mul_left p.Fnum
      simpa [mp, hpExpEq] using hprod
  have hqNonpos : _root_.F2R (beta:=radix) q ≤ 0 :=
    qNeg (beta:=radix) b radix s t x p q hradix hvNum hxBound hxPos
      hpDef hqDef
  have hmqEven : Even mq := by
    by_cases hqExpEq : q.Fexp = e
    · have hsym :
          SymmetricP (EvenClosest (beta:=radix) b (radix : ℝ) t) := by
        have h := EvenClosestSymmetric (beta:=radix) b (radix : ℝ) t
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestSymmetric_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
      have hqOppEven :
          EvenClosest (beta:=radix) b (radix : ℝ) t
            (-(_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p))
            (Fopp (beta:=radix) q) := hsym _ q hqDefEven
      have hqOppCan :
          Fcanonic (beta:=radix) radix b (Fopp (beta:=radix) q) := by
        have h := FcanonicFopp (beta:=radix) radix b q
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFopp_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h (Or.inl hqNormal)
      have hqOppNonneg :
          0 ≤ _root_.F2R (beta:=radix) (Fopp (beta:=radix) q) := by
        have h := (FloatSpec.Calc.Operations.F2R_opp (beta:=radix) q) trivial
        have hopp :
            _root_.F2R (beta:=radix) (Fopp (beta:=radix) q) =
              -_root_.F2R (beta:=radix) q := by
          simpa [Fopp, _root_.F2R] using h
        rw [hopp]
        linarith
      let qmid : ℝ := (mp : ℝ) - (v.Fnum : ℝ) - (eps + 1) / 2
      have hqInputMid :
          -(_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p) =
            (radix : ℝ) ^ (Fopp (beta:=radix) q).Fexp * (qmid + 1 / 2) := by
        rw [show (Fopp (beta:=radix) q).Fexp = q.Fexp by rfl, hqExpEq,
          hpCoeffValue, hxMid, hvExpanded]
        dsimp [qmid]
        ring
      have hqmidInt : ∃ n : Int, (n : ℝ) = qmid := by
        rcases heps with hepsPos | hepsNeg
        · refine ⟨mp - v.Fnum - 1, ?_⟩
          dsimp [qmid]
          rw [hepsPos, Int.cast_sub, Int.cast_sub]
          norm_num
        · refine ⟨mp - v.Fnum, ?_⟩
          dsimp [qmid]
          rw [hepsNeg, Int.cast_sub]
          norm_num
      have hqOppFN :
          FNeven (beta:=radix) b (radix : ℝ) t (Fopp (beta:=radix) q) := by
        have h := ClosestImplyEven_int (beta:=radix) b radix t
          (-(_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p))
          qmid (Fopp (beta:=radix) q)
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestImplyEven_int_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          h ⟨hEvenRadix, hqOppEven, hqOppCan, hqOppNonneg, hqInputMid,
            hqmidInt, rfl, hradix, htGtOne, hvNum⟩
      have hqFN : FNeven (beta:=radix) b (radix : ℝ) t q := by
        have h := FNevenFop (beta:=radix) b (radix : ℝ) t
          (Fopp (beta:=radix) q)
        have hback :
            FNeven (beta:=radix) b (radix : ℝ) t
              (Fopp (beta:=radix) (Fopp (beta:=radix) q)) := by
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FNevenFop_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using h hqOppFN
        simpa [Fopp, FloatSpec.Calc.Operations.Fopp] using hback
      have hqNorm : Fnormalize (beta:=radix) radix b t q = q := by
        have h := FcanonicFnormalizeEq (beta:=radix) radix b t q
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          h ⟨Or.inl hqNormal, rfl, hradix, htNe, hvNum⟩
      unfold FNeven Feven at hqFN
      rw [hqNorm] at hqFN
      simpa [mq, hqExpEq] using hqFN
    · have hqSucc : q.Fexp = e + 1 := hqCases.resolve_left hqExpEq
      have hprod : Even (q.Fnum * radix) := hEvenRadix.mul_left q.Fnum
      simpa [mq, hqExpEq] using hprod
  have hvNumEven : Even v.Fnum := by
    rw [hvNumEq]
    exact hmpEven.add hmqEven
  have hrvNum : rb.vNum = Zpower_nat radix u := by
    dsimp [rb, u]
    exact p'GivesBound radix b s t
  have hvNorm : Fnormalize (beta:=radix) radix rb u v = v := by
    have h := FcanonicFnormalizeEq (beta:=radix) radix rb u v
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hvCan, rfl, hradix, huNe, hrvNum⟩
  unfold FNeven Feven
  rw [hvNorm]
  exact hvNumEven

/-! Coq Veltkamp local lemma `Veltkamp_aux`.

The reconstructed high part is within half an ulp at exponent `s + Fexp x`
and has a canonical representative in the reduced `t - s` format.  That
representative is a closest rounding of `x`, and its exponent is no smaller
than the residual exponent. -/
theorem Veltkamp_aux {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x p q hx : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hsGe : 2 ≤ s) (hsLe : s ≤ t - 2)
    (hxBound : Fbounded (beta:=beta) b x)
    (hpDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)) p)
    (hqDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hhxDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) q + _root_.F2R (beta:=beta) p) hx)
    (hxPos : 0 < _root_.F2R (beta:=beta) x)
    (hpNormal : Fnormal (beta:=beta) radix b p)
    (hqNormal : Fnormal (beta:=beta) radix b q)
    (hxNormal : Fnormal (beta:=beta) radix b x) :
    |_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) hx| ≤
        (radix : ℝ) ^ ((s : Int) + x.Fexp) / 2 ∧
      ∃ hx' : FloatSpec.Core.Defs.FlocqFloat beta,
        _root_.F2R (beta:=beta) hx' = _root_.F2R (beta:=beta) hx ∧
          Closest (beta:=beta) (Veltkamp_reducedBound radix b s t)
            (radix : ℝ) (_root_.F2R (beta:=beta) x) hx' ∧
          (s : Int) + x.Fexp ≤ hx'.Fexp := by
  subst beta
  let u : Nat := t - s
  let e : Int := (s : Int) + x.Fexp
  let rb : Fbound_skel := Veltkamp_reducedBound radix b s t
  have htPos : 0 < t := by omega
  have htNe : t ≠ 0 := Nat.ne_of_gt htPos
  have huGtOne : 1 < u := by dsimp [u]; omega
  have huPos : 0 < u := lt_trans Nat.zero_lt_one huGtOne
  have hradixPosInt : (0 : Int) < radix := by omega
  have hradixPos : (0 : ℝ) < (radix : ℝ) := by exact_mod_cast hradixPosInt
  have hradixNe : (radix : ℝ) ≠ 0 := ne_of_gt hradixPos
  have hResidual := Veltkamp_aux_residual (beta:=radix) b radix s t x p q hx
    rfl hradix hvNum hsGe hsLe hxBound hpDef hqDef hhxDef hxPos hpNormal
    hqNormal hxNormal
  rcases Veltkamp_aux_reduced_rep (beta:=radix) b radix s t x p q hx rfl
      hradix hvNum hsGe hsLe hxBound hpDef hqDef hhxDef hxPos hpNormal
      hqNormal hxNormal hResidual with
    ⟨v, hvValue, hvCan⟩
  have hvBound : Fbounded (beta:=radix) rb v := by
    have h := FcanonicBound (beta:=radix) radix rb v
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicBound_check,
      Id.run, ULift.up_down, rb, Int.cast_ofNat] using h hvCan
  have hvResidual :
      |_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) v| ≤
        (radix : ℝ) ^ e / 2 := by
    rw [hvValue]
    simpa only [e, Int.cast_ofNat] using hResidual
  have hxNumPos : 0 < x.Fnum :=
    FloatSpec.Core.Float_prop.gt_0_F2R (beta:=radix) x hradix hxPos
  have hxNumMin : nNormMin radix t ≤ x.Fnum := by
    have h := pNormal_absolu_min (beta:=radix) radix b t x
    have hAbs : nNormMin radix t ≤ |x.Fnum| := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, pNormal_absolu_min_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hxNormal, hxNormal, htNe, hradix, hvNum⟩
    simpa [abs_of_pos hxNumPos] using hAbs
  have hpowPredCast :
      (Zpower_nat radix (t - 1) : ℝ) =
        (radix : ℝ) ^ (((t - 1 : Nat) : Int)) := by
    have h := Zpower_nat_Z_powerRZ radix (t - 1)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Zpower_nat_Z_powerRZ_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hxNumMinReal :
      (radix : ℝ) ^ (((t - 1 : Nat) : Int)) ≤ (x.Fnum : ℝ) := by
    rw [← hpowPredCast]
    exact_mod_cast (show Zpower_nat radix (t - 1) ≤ x.Fnum by
      simpa only [nNormMin, Zpower_nat, Int.cast_ofNat] using hxNumMin)
  have hxPowPos : 0 < (radix : ℝ) ^ x.Fexp := zpow_pos hradixPos _
  have hxLower :
      (radix : ℝ) ^ ((t : Int) - 1 + x.Fexp) ≤
        _root_.F2R (beta:=radix) x := by
    calc
      (radix : ℝ) ^ ((t : Int) - 1 + x.Fexp) =
          (radix : ℝ) ^ (((t - 1 : Nat) : Int)) *
            (radix : ℝ) ^ x.Fexp := by
              rw [← zpow_add₀ hradixNe]
              congr 1
              omega
      _ ≤ (x.Fnum : ℝ) * (radix : ℝ) ^ x.Fexp :=
        mul_le_mul_of_nonneg_right hxNumMinReal (le_of_lt hxPowPos)
      _ = _root_.F2R (beta:=radix) x := rfl
  have hxNumLt : x.Fnum < Zpower_nat radix t := by
    have h := hxBound.1
    rw [abs_of_pos hxNumPos, hvNum] at h
    exact h
  have hpowTCast :
      (Zpower_nat radix t : ℝ) = (radix : ℝ) ^ (t : Int) := by
    have h := Zpower_nat_Z_powerRZ radix t
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Zpower_nat_Z_powerRZ_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hxNumRealLt : (x.Fnum : ℝ) < (radix : ℝ) ^ (t : Int) := by
    rw [← hpowTCast]
    exact_mod_cast hxNumLt
  have hxUpper :
      _root_.F2R (beta:=radix) x ≤
        (radix : ℝ) ^ ((t : Int) + x.Fexp) := by
    apply le_of_lt
    calc
      _root_.F2R (beta:=radix) x =
          (x.Fnum : ℝ) * (radix : ℝ) ^ x.Fexp := rfl
      _ < (radix : ℝ) ^ (t : Int) * (radix : ℝ) ^ x.Fexp :=
        mul_lt_mul_of_pos_right hxNumRealLt hxPowPos
      _ = (radix : ℝ) ^ ((t : Int) + x.Fexp) := by
        rw [zpow_add₀ hradixNe]
  have hvLower :
      (radix : ℝ) ^ ((t : Int) - 1 + x.Fexp) ≤
        _root_.F2R (beta:=radix) v :=
    Veltkamp_aux_aux (beta:=radix) b radix s t x p q hx rfl hradix hvNum
      hsGe hsLe hxBound hpDef hqDef hhxDef hxPos hpNormal hqNormal hxNormal v
      hvValue hvCan hvResidual
  have heBound : -rb.dExp ≤ e := by
    dsimp [rb, Veltkamp_reducedBound, e]
    exact le_trans hxBound.2 (by omega)
  have hrvNum : rb.vNum = Zpower_nat radix u := by
    dsimp [rb, u]
    exact p'GivesBound radix b s t
  have hLowerExp :
      e + (u : Int) - 1 = (t : Int) - 1 + x.Fexp := by
    dsimp [e, u]
    omega
  have hUpperExp : e + (u : Int) = (t : Int) + x.Fexp := by
    dsimp [e, u]
    omega
  have hvClosest :
      Closest (beta:=radix) rb (radix : ℝ)
        (_root_.F2R (beta:=radix) x) v := by
    have h := ImplyClosest (beta:=radix) rb radix u
      (_root_.F2R (beta:=radix) x) v e
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ImplyClosest_check,
      Id.run, rb, Int.cast_ofNat] using h ⟨hvBound, hvCan, by simpa only [hLowerExp, Int.cast_ofNat] using hxLower,
        by simpa only [hUpperExp, Int.cast_ofNat] using hxUpper,
        by simpa only [hLowerExp, Int.cast_ofNat] using hvLower, heBound, rfl, hradix,
        huGtOne, hrvNum, hvResidual⟩
  let lower : FloatSpec.Core.Defs.FlocqFloat radix :=
    ⟨nNormMin radix u, e⟩
  have hlowerCan : Fcanonic (beta:=radix) radix rb lower := by
    exact FcanonicNnormMin (beta:=radix) radix rb u e hradix huPos hrvNum heBound
  have hlowerValue :
      _root_.F2R (beta:=radix) lower =
        (radix : ℝ) ^ (e + (u : Int) - 1) := by
    dsimp [lower, _root_.F2R, FloatSpec.Core.Defs.F2R, nNormMin]
    have hpowNat :
        ((radix ^ (u - 1) : Int) : ℝ) =
          (radix : ℝ) ^ ((u - 1 : Nat) : Int) := by
      simp [Int.cast_pow]
    rw [hpowNat]
    have huCast : ((u - 1 : Nat) : Int) = (u : Int) - 1 := by omega
    rw [huCast]
    calc
      (radix : ℝ) ^ ((u : Int) - 1) * (radix : ℝ) ^ e =
          (radix : ℝ) ^ (((u : Int) - 1) + e) := by
            exact (zpow_add₀ hradixNe ((u : Int) - 1) e).symm
      _ = (radix : ℝ) ^ (e + (u : Int) - 1) := by
            congr 1
            ring
  have hlowerNonneg : 0 ≤ _root_.F2R (beta:=radix) lower := by
    rw [hlowerValue]
    exact le_of_lt (zpow_pos hradixPos _)
  have hvNonneg : 0 ≤ _root_.F2R (beta:=radix) v := by
    rw [← hLowerExp] at hvLower
    exact le_trans (by simpa [hlowerValue] using hlowerNonneg) hvLower
  have habsLower :
      |_root_.F2R (beta:=radix) lower| ≤
        |_root_.F2R (beta:=radix) v| := by
    rw [abs_of_nonneg hlowerNonneg, abs_of_nonneg hvNonneg, hlowerValue,
      hLowerExp]
    exact hvLower
  have heLeV : e ≤ v.Fexp := by
    have h := Fcanonic_Rle_Zle (beta:=radix) radix rb lower v hradix rfl
    have hle : lower.Fexp ≤ v.Fexp := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fcanonic_Rle_Zle_check,
        Id.run, ULift.up_down, Fcanonic', lower, rb, Int.cast_ofNat] using
        h ⟨hlowerCan, hvCan, habsLower⟩
    simpa only [lower, Int.cast_ofNat] using hle
  refine ⟨hResidual, v, hvValue, ?_, ?_⟩
  · simpa only [rb, Int.cast_ofNat] using hvClosest
  · simpa only [e, Int.cast_ofNat] using heLeV

/-! Coq Veltkamp local lemma `VeltkampEven1`.

For an even radix, even-closest rounding of the two Veltkamp split stages
forces the reconstructed high part to be the even-closest reduced-precision
rounding of `x`. -/
theorem VeltkampEven1 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x p q hx : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hsGe : 2 ≤ s) (hsLe : s ≤ t - 2)
    (hxBound : Fbounded (beta:=beta) b x)
    (hpDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)) p)
    (hqDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hhxDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) q + _root_.F2R (beta:=beta) p) hx)
    (hxPos : 0 < _root_.F2R (beta:=beta) x)
    (hpNormal : Fnormal (beta:=beta) radix b p)
    (hqNormal : Fnormal (beta:=beta) radix b q)
    (hxNormal : Fnormal (beta:=beta) radix b x)
    (hpDefEven : EvenClosest (beta:=beta) b (radix : ℝ) t
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)) p)
    (hqDefEven : EvenClosest (beta:=beta) b (radix : ℝ) t
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hhxDefEven : EvenClosest (beta:=beta) b (radix : ℝ) t
      (_root_.F2R (beta:=beta) q + _root_.F2R (beta:=beta) p) hx)
    (hEvenRadix : Even radix) :
    ∃ hx' : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) hx' = _root_.F2R (beta:=beta) hx ∧
        EvenClosest (beta:=beta) (Veltkamp_reducedBound radix b s t)
          (radix : ℝ) (t - s) (_root_.F2R (beta:=beta) x) hx' := by
  subst beta
  let u : Nat := t - s
  let e : Int := (s : Int) + x.Fexp
  let rb : Fbound_skel := Veltkamp_reducedBound radix b s t
  have htPos : 0 < t := by omega
  have htNe : t ≠ 0 := Nat.ne_of_gt htPos
  have huGtOne : 1 < u := by dsimp [u]; omega
  have huPos : 0 < u := lt_trans Nat.zero_lt_one huGtOne
  have hradixPosInt : (0 : Int) < radix := by omega
  have hradixPos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradixPosInt
  have hradixNe : (radix : ℝ) ≠ 0 := ne_of_gt hradixPos
  rcases Veltkamp_aux (beta:=radix) b radix s t x p q hx rfl hradix hvNum
      hsGe hsLe hxBound hpDef hqDef hhxDef hxPos hpNormal hqNormal hxNormal with
    ⟨hResidual, w, hwValue, hwClosest, hwExp⟩
  rcases VeltkampEven1_candidate (beta:=radix) b radix s t x p q hx rfl
      hradix hvNum hsGe hsLe hxBound hpDef hqDef hhxDef hxPos hpNormal
      hqNormal hxNormal hEvenRadix hResidual with
    ⟨v, hvValue, hvCan, hvEvenOrExp⟩
  have hvBound : Fbounded (beta:=radix) rb v := by
    have h := FcanonicBound (beta:=radix) radix rb v
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicBound_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hvCan
  have hvClosest :
      Closest (beta:=radix) rb (radix : ℝ)
        (_root_.F2R (beta:=radix) x) v := by
    constructor
    · exact hvBound
    · intro g hg
      have h := hwClosest.2 g (by simpa only [rb, Int.cast_ofNat] using hg)
      simpa only [hvValue, hwValue, Int.cast_ofNat] using h
  have hvResidual :
      |_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) v| ≤
        (radix : ℝ) ^ e / 2 := by
    rw [hvValue]
    simpa only [e, Int.cast_ofNat] using hResidual
  have hxNumPos : 0 < x.Fnum :=
    FloatSpec.Core.Float_prop.gt_0_F2R (beta:=radix) x hradix hxPos
  have hxNumMin : nNormMin radix t ≤ x.Fnum := by
    have h := pNormal_absolu_min (beta:=radix) radix b t x
    have hAbs : nNormMin radix t ≤ |x.Fnum| := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, pNormal_absolu_min_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hxNormal, hxNormal, htNe, hradix, hvNum⟩
    simpa [abs_of_pos hxNumPos] using hAbs
  have hpowPredCast :
      (Zpower_nat radix (t - 1) : ℝ) =
        (radix : ℝ) ^ (((t - 1 : Nat) : Int)) := by
    have h := Zpower_nat_Z_powerRZ radix (t - 1)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Zpower_nat_Z_powerRZ_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hxNumMinReal :
      (radix : ℝ) ^ (((t - 1 : Nat) : Int)) ≤ (x.Fnum : ℝ) := by
    rw [← hpowPredCast]
    exact_mod_cast (show Zpower_nat radix (t - 1) ≤ x.Fnum by
      simpa only [nNormMin, Zpower_nat, Int.cast_ofNat] using hxNumMin)
  have hxPowPos : 0 < (radix : ℝ) ^ x.Fexp := zpow_pos hradixPos _
  have hxLower :
      (radix : ℝ) ^ ((t : Int) - 1 + x.Fexp) ≤
        _root_.F2R (beta:=radix) x := by
    calc
      (radix : ℝ) ^ ((t : Int) - 1 + x.Fexp) =
          (radix : ℝ) ^ (((t - 1 : Nat) : Int)) *
            (radix : ℝ) ^ x.Fexp := by
              rw [← zpow_add₀ hradixNe]
              congr 1
              omega
      _ ≤ (x.Fnum : ℝ) * (radix : ℝ) ^ x.Fexp :=
        mul_le_mul_of_nonneg_right hxNumMinReal (le_of_lt hxPowPos)
      _ = _root_.F2R (beta:=radix) x := rfl
  have hxNumLt : x.Fnum < Zpower_nat radix t := by
    have h := hxBound.1
    rw [abs_of_pos hxNumPos, hvNum] at h
    exact h
  have hpowTCast :
      (Zpower_nat radix t : ℝ) = (radix : ℝ) ^ (t : Int) := by
    have h := Zpower_nat_Z_powerRZ radix t
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Zpower_nat_Z_powerRZ_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hxNumRealLt : (x.Fnum : ℝ) < (radix : ℝ) ^ (t : Int) := by
    rw [← hpowTCast]
    exact_mod_cast hxNumLt
  have hxUpper :
      _root_.F2R (beta:=radix) x ≤
        (radix : ℝ) ^ ((t : Int) + x.Fexp) := by
    apply le_of_lt
    calc
      _root_.F2R (beta:=radix) x =
          (x.Fnum : ℝ) * (radix : ℝ) ^ x.Fexp := rfl
      _ < (radix : ℝ) ^ (t : Int) * (radix : ℝ) ^ x.Fexp :=
        mul_lt_mul_of_pos_right hxNumRealLt hxPowPos
      _ = (radix : ℝ) ^ ((t : Int) + x.Fexp) := by
        rw [zpow_add₀ hradixNe]
  have hvLower :
      (radix : ℝ) ^ ((t : Int) - 1 + x.Fexp) ≤
        _root_.F2R (beta:=radix) v :=
    Veltkamp_aux_aux (beta:=radix) b radix s t x p q hx rfl hradix hvNum
      hsGe hsLe hxBound hpDef hqDef hhxDef hxPos hpNormal hqNormal hxNormal v
      hvValue hvCan hvResidual
  have heBound : -rb.dExp ≤ e := by
    dsimp [rb, Veltkamp_reducedBound, e]
    exact le_trans hxBound.2 (by omega)
  have hrvNum : rb.vNum = Zpower_nat radix u := by
    dsimp [rb, u]
    exact p'GivesBound radix b s t
  have hLowerExp :
      e + (u : Int) - 1 = (t : Int) - 1 + x.Fexp := by
    dsimp [e, u]
    omega
  have hUpperExp : e + (u : Int) = (t : Int) + x.Fexp := by
    dsimp [e, u]
    omega
  let lower : FloatSpec.Core.Defs.FlocqFloat radix :=
    ⟨nNormMin radix u, e⟩
  have hlowerCan : Fcanonic (beta:=radix) radix rb lower := by
    exact FcanonicNnormMin (beta:=radix) radix rb u e hradix huPos hrvNum
      heBound
  have hlowerValue :
      _root_.F2R (beta:=radix) lower =
        (radix : ℝ) ^ (e + (u : Int) - 1) := by
    dsimp [lower, _root_.F2R, FloatSpec.Core.Defs.F2R, nNormMin]
    have hpowNat :
        ((radix ^ (u - 1) : Int) : ℝ) =
          (radix : ℝ) ^ ((u - 1 : Nat) : Int) := by
      simp [Int.cast_pow]
    rw [hpowNat]
    have huCast : ((u - 1 : Nat) : Int) = (u : Int) - 1 := by omega
    rw [huCast]
    calc
      (radix : ℝ) ^ ((u : Int) - 1) * (radix : ℝ) ^ e =
          (radix : ℝ) ^ (((u : Int) - 1) + e) := by
            exact (zpow_add₀ hradixNe ((u : Int) - 1) e).symm
      _ = (radix : ℝ) ^ (e + (u : Int) - 1) := by
        congr 1
        ring
  have hlowerNonneg : 0 ≤ _root_.F2R (beta:=radix) lower := by
    rw [hlowerValue]
    exact le_of_lt (zpow_pos hradixPos _)
  have hvNonneg : 0 ≤ _root_.F2R (beta:=radix) v := by
    rw [← hLowerExp] at hvLower
    exact le_trans (by simpa [hlowerValue] using hlowerNonneg) hvLower
  have habsLower :
      |_root_.F2R (beta:=radix) lower| ≤
        |_root_.F2R (beta:=radix) v| := by
    rw [abs_of_nonneg hlowerNonneg, abs_of_nonneg hvNonneg, hlowerValue,
      hLowerExp]
    exact hvLower
  have heLeV : e ≤ v.Fexp := by
    have h := Fcanonic_Rle_Zle (beta:=radix) radix rb lower v hradix rfl
    have hle : lower.Fexp ≤ v.Fexp := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fcanonic_Rle_Zle_check,
        Id.run, ULift.up_down, Fcanonic', lower, rb, Int.cast_ofNat] using
        h ⟨hlowerCan, hvCan, habsLower⟩
    simpa only [lower, Int.cast_ofNat] using hle
  refine ⟨v, hvValue, ?_⟩
  rcases hvEvenOrExp with hvEven | hvExpLe
  · exact ⟨hvClosest, Or.inl (by simpa only [rb, u, Int.cast_ofNat] using hvEven)⟩
  · have hvExpEq : v.Fexp = e := le_antisymm hvExpLe heLeV
    by_cases hStrict :
        |_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) v| <
          (radix : ℝ) ^ e / 2
    · have h := ImplyClosestStrict2 (beta:=radix) rb radix u
        (_root_.F2R (beta:=radix) x) v e
      have hstrictResult :
          Closest (beta:=radix) rb (radix : ℝ)
              (_root_.F2R (beta:=radix) x) v ∧
            ∀ g : FloatSpec.Core.Defs.FlocqFloat radix,
              Closest (beta:=radix) rb (radix : ℝ)
                  (_root_.F2R (beta:=radix) x) g →
                _root_.F2R (beta:=radix) v = _root_.F2R (beta:=radix) g := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ImplyClosestStrict2_check,
          Id.run, Int.cast_ofNat] using
          h ⟨hvBound, hvCan, by simpa only [hLowerExp, Int.cast_ofNat] using hxLower,
            by simpa only [hUpperExp, Int.cast_ofNat] using hxUpper,
            by simpa only [hLowerExp, Int.cast_ofNat] using hvLower, heBound, rfl, hradix,
            huGtOne, hrvNum, hStrict⟩
      refine ⟨hvClosest, Or.inr ?_⟩
      intro g hg
      exact (hstrictResult.2 g hg).symm
    · have hTie :
          |_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) v| =
            (radix : ℝ) ^ e / 2 :=
        le_antisymm hvResidual (le_of_not_gt hStrict)
      have hvEven := VeltkampEven1_tie_even (beta:=radix) b radix s t
        x p q hx v rfl hradix hvNum hsGe hsLe hxBound hpDef hqDef hhxDef
        hxPos hpNormal hqNormal hxNormal hpDefEven hqDefEven hEvenRadix
        hvValue hvCan (by simpa only [e, Int.cast_ofNat] using hvExpEq)
        (by simpa only [e, Int.cast_ofNat] using hTie)
      exact ⟨hvClosest, Or.inl (by simpa only [rb, u, Int.cast_ofNat] using hvEven)⟩

private theorem VeltkampEven2_no_tie {beta : Int} [ValidRadix beta]
    (radix : Int) (s : Nat)
    (x v : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hsGe : 2 ≤ s) (hOddRadix : Odd radix)
    (hvExp : (s : Int) + x.Fexp ≤ v.Fexp)
    (hTie :
      |_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) v| =
        (radix : ℝ) ^ ((s : Int) + x.Fexp) / 2) : False := by
  subst beta
  have hradixPosInt : (0 : Int) < radix := by omega
  have hradixPos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradixPosInt
  have hradixNe : (radix : ℝ) ≠ 0 := ne_of_gt hradixPos
  let k : Nat := Int.toNat (v.Fexp - x.Fexp)
  let m : Int := x.Fnum - v.Fnum * Zpower_nat radix k
  have hdiffNonneg : 0 ≤ v.Fexp - x.Fexp := by omega
  have hkCast : (k : Int) = v.Fexp - x.Fexp := by
    simpa [k] using Int.toNat_of_nonneg hdiffNonneg
  have hpowKCast :
      (Zpower_nat radix k : ℝ) =
        (radix : ℝ) ^ (v.Fexp - x.Fexp) := by
    have h := Zpower_nat_Z_powerRZ radix k
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Zpower_nat_Z_powerRZ_check,
      Id.run, ULift.up_down, hkCast, Int.cast_ofNat] using h trivial
  have hvPowSplit :
      (radix : ℝ) ^ v.Fexp =
        (radix : ℝ) ^ (v.Fexp - x.Fexp) *
          (radix : ℝ) ^ x.Fexp := by
    calc
      (radix : ℝ) ^ v.Fexp =
          (radix : ℝ) ^ ((v.Fexp - x.Fexp) + x.Fexp) := by
            congr 1
            ring
      _ = (radix : ℝ) ^ (v.Fexp - x.Fexp) *
            (radix : ℝ) ^ x.Fexp := zpow_add₀ hradixNe _ _
  have hmRepr :
      _root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) v =
        (m : ℝ) * (radix : ℝ) ^ x.Fexp := by
    dsimp [_root_.F2R, FloatSpec.Core.Defs.F2R, m]
    rw [Int.cast_sub, Int.cast_mul, hpowKCast, hvPowSplit]
    ring
  have hsPowSplit :
      (radix : ℝ) ^ ((s : Int) + x.Fexp) =
        (radix : ℝ) ^ (s : Int) * (radix : ℝ) ^ x.Fexp :=
    zpow_add₀ hradixNe _ _
  have hxPowPos : 0 < (radix : ℝ) ^ x.Fexp := zpow_pos hradixPos _
  rw [hmRepr, abs_mul, abs_of_pos hxPowPos, hsPowSplit] at hTie
  have hrealEq :
      (radix : ℝ) ^ (s : Int) = 2 * |(m : ℝ)| := by
    nlinarith [hTie, hxPowPos]
  have hpowSCast :
      (Zpower_nat radix s : ℝ) = (radix : ℝ) ^ (s : Int) := by
    have h := Zpower_nat_Z_powerRZ radix s
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Zpower_nat_Z_powerRZ_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hintEq : Zpower_nat radix s = 2 * |m| := by
    exact_mod_cast (show (Zpower_nat radix s : ℝ) = (2 * |m| : Int) by
      rw [hpowSCast, Int.cast_mul, Int.cast_ofNat, Int.cast_abs]
      exact hrealEq)
  have hEvenPow : Even (Zpower_nat radix s) := by
    refine ⟨|m|, ?_⟩
    rw [hintEq]
    ring
  have hOddPow : Odd (Zpower_nat radix s) := by
    simpa only [Zpower_nat, Int.cast_ofNat] using hOddRadix.pow
  exact (Int.not_even_iff_odd.mpr hOddPow) hEvenPow

/-! Coq Veltkamp local lemma `VeltkampEven2`.

For an odd radix, an exact reduced-format midpoint is impossible: after
scaling by the exponent of `x`, it would make `radix^s` even.  The resulting
strict closestness supplies the unique-value branch of `EvenClosest`. -/
theorem VeltkampEven2 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x p q hx : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hsGe : 2 ≤ s) (hsLe : s ≤ t - 2)
    (hxBound : Fbounded (beta:=beta) b x)
    (hpDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)) p)
    (hqDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hhxDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) q + _root_.F2R (beta:=beta) p) hx)
    (hxPos : 0 < _root_.F2R (beta:=beta) x)
    (hpNormal : Fnormal (beta:=beta) radix b p)
    (hqNormal : Fnormal (beta:=beta) radix b q)
    (hxNormal : Fnormal (beta:=beta) radix b x)
    (hpDefEven : EvenClosest (beta:=beta) b (radix : ℝ) t
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)) p)
    (hqDefEven : EvenClosest (beta:=beta) b (radix : ℝ) t
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hhxDefEven : EvenClosest (beta:=beta) b (radix : ℝ) t
      (_root_.F2R (beta:=beta) q + _root_.F2R (beta:=beta) p) hx)
    (hOddRadix : Odd radix) :
    ∃ hx' : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) hx' = _root_.F2R (beta:=beta) hx ∧
        EvenClosest (beta:=beta) (Veltkamp_reducedBound radix b s t)
          (radix : ℝ) (t - s) (_root_.F2R (beta:=beta) x) hx' := by
  subst beta
  let u : Nat := t - s
  let e : Int := (s : Int) + x.Fexp
  let rb : Fbound_skel := Veltkamp_reducedBound radix b s t
  have htPos : 0 < t := by omega
  have htNe : t ≠ 0 := Nat.ne_of_gt htPos
  have huGtOne : 1 < u := by dsimp [u]; omega
  have huPos : 0 < u := lt_trans Nat.zero_lt_one huGtOne
  have hradixPosInt : (0 : Int) < radix := by omega
  have hradixPos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradixPosInt
  have hradixNe : (radix : ℝ) ≠ 0 := ne_of_gt hradixPos
  rcases Veltkamp_aux (beta:=radix) b radix s t x p q hx rfl hradix hvNum
      hsGe hsLe hxBound hpDef hqDef hhxDef hxPos hpNormal hqNormal hxNormal with
    ⟨hResidual, w, hwValue, hwClosest, hwExp⟩
  rcases Veltkamp_aux_reduced_rep (beta:=radix) b radix s t x p q hx rfl
      hradix hvNum hsGe hsLe hxBound hpDef hqDef hhxDef hxPos hpNormal
      hqNormal hxNormal hResidual with
    ⟨v, hvValue, hvCan⟩
  have hvBound : Fbounded (beta:=radix) rb v := by
    have h := FcanonicBound (beta:=radix) radix rb v
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicBound_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hvCan
  have hvResidual :
      |_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) v| ≤
        (radix : ℝ) ^ e / 2 := by
    rw [hvValue]
    simpa only [e, Int.cast_ofNat] using hResidual
  have hxNumPos : 0 < x.Fnum :=
    FloatSpec.Core.Float_prop.gt_0_F2R (beta:=radix) x hradix hxPos
  have hxNumMin : nNormMin radix t ≤ x.Fnum := by
    have h := pNormal_absolu_min (beta:=radix) radix b t x
    have hAbs : nNormMin radix t ≤ |x.Fnum| := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, pNormal_absolu_min_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hxNormal, hxNormal, htNe, hradix, hvNum⟩
    simpa [abs_of_pos hxNumPos] using hAbs
  have hpowPredCast :
      (Zpower_nat radix (t - 1) : ℝ) =
        (radix : ℝ) ^ (((t - 1 : Nat) : Int)) := by
    have h := Zpower_nat_Z_powerRZ radix (t - 1)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Zpower_nat_Z_powerRZ_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hxNumMinReal :
      (radix : ℝ) ^ (((t - 1 : Nat) : Int)) ≤ (x.Fnum : ℝ) := by
    rw [← hpowPredCast]
    exact_mod_cast (show Zpower_nat radix (t - 1) ≤ x.Fnum by
      simpa only [nNormMin, Zpower_nat, Int.cast_ofNat] using hxNumMin)
  have hxPowPos : 0 < (radix : ℝ) ^ x.Fexp := zpow_pos hradixPos _
  have hxLower :
      (radix : ℝ) ^ ((t : Int) - 1 + x.Fexp) ≤
        _root_.F2R (beta:=radix) x := by
    calc
      (radix : ℝ) ^ ((t : Int) - 1 + x.Fexp) =
          (radix : ℝ) ^ (((t - 1 : Nat) : Int)) *
            (radix : ℝ) ^ x.Fexp := by
              rw [← zpow_add₀ hradixNe]
              congr 1
              omega
      _ ≤ (x.Fnum : ℝ) * (radix : ℝ) ^ x.Fexp :=
        mul_le_mul_of_nonneg_right hxNumMinReal (le_of_lt hxPowPos)
      _ = _root_.F2R (beta:=radix) x := rfl
  have hxNumLt : x.Fnum < Zpower_nat radix t := by
    have h := hxBound.1
    rw [abs_of_pos hxNumPos, hvNum] at h
    exact h
  have hpowTCast :
      (Zpower_nat radix t : ℝ) = (radix : ℝ) ^ (t : Int) := by
    have h := Zpower_nat_Z_powerRZ radix t
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Zpower_nat_Z_powerRZ_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hxNumRealLt : (x.Fnum : ℝ) < (radix : ℝ) ^ (t : Int) := by
    rw [← hpowTCast]
    exact_mod_cast hxNumLt
  have hxUpper :
      _root_.F2R (beta:=radix) x ≤
        (radix : ℝ) ^ ((t : Int) + x.Fexp) := by
    apply le_of_lt
    calc
      _root_.F2R (beta:=radix) x =
          (x.Fnum : ℝ) * (radix : ℝ) ^ x.Fexp := rfl
      _ < (radix : ℝ) ^ (t : Int) * (radix : ℝ) ^ x.Fexp :=
        mul_lt_mul_of_pos_right hxNumRealLt hxPowPos
      _ = (radix : ℝ) ^ ((t : Int) + x.Fexp) := by
        rw [zpow_add₀ hradixNe]
  have hvLower :
      (radix : ℝ) ^ ((t : Int) - 1 + x.Fexp) ≤
        _root_.F2R (beta:=radix) v :=
    Veltkamp_aux_aux (beta:=radix) b radix s t x p q hx rfl hradix hvNum
      hsGe hsLe hxBound hpDef hqDef hhxDef hxPos hpNormal hqNormal hxNormal v
      hvValue hvCan hvResidual
  have heBound : -rb.dExp ≤ e := by
    dsimp [rb, Veltkamp_reducedBound, e]
    exact le_trans hxBound.2 (by omega)
  have hrvNum : rb.vNum = Zpower_nat radix u := by
    dsimp [rb, u]
    exact p'GivesBound radix b s t
  have hLowerExp :
      e + (u : Int) - 1 = (t : Int) - 1 + x.Fexp := by
    dsimp [e, u]
    omega
  have hUpperExp : e + (u : Int) = (t : Int) + x.Fexp := by
    dsimp [e, u]
    omega
  let lower : FloatSpec.Core.Defs.FlocqFloat radix :=
    ⟨nNormMin radix u, e⟩
  have hlowerCan : Fcanonic (beta:=radix) radix rb lower := by
    exact FcanonicNnormMin (beta:=radix) radix rb u e hradix huPos hrvNum
      heBound
  have hlowerValue :
      _root_.F2R (beta:=radix) lower =
        (radix : ℝ) ^ (e + (u : Int) - 1) := by
    dsimp [lower, _root_.F2R, FloatSpec.Core.Defs.F2R, nNormMin]
    have hpowNat :
        ((radix ^ (u - 1) : Int) : ℝ) =
          (radix : ℝ) ^ ((u - 1 : Nat) : Int) := by
      simp [Int.cast_pow]
    rw [hpowNat]
    have huCast : ((u - 1 : Nat) : Int) = (u : Int) - 1 := by omega
    rw [huCast]
    calc
      (radix : ℝ) ^ ((u : Int) - 1) * (radix : ℝ) ^ e =
          (radix : ℝ) ^ (((u : Int) - 1) + e) := by
            exact (zpow_add₀ hradixNe ((u : Int) - 1) e).symm
      _ = (radix : ℝ) ^ (e + (u : Int) - 1) := by
        congr 1
        ring
  have hlowerNonneg : 0 ≤ _root_.F2R (beta:=radix) lower := by
    rw [hlowerValue]
    exact le_of_lt (zpow_pos hradixPos _)
  have hvNonneg : 0 ≤ _root_.F2R (beta:=radix) v := by
    rw [← hLowerExp] at hvLower
    exact le_trans (by simpa [hlowerValue] using hlowerNonneg) hvLower
  have habsLower :
      |_root_.F2R (beta:=radix) lower| ≤
        |_root_.F2R (beta:=radix) v| := by
    rw [abs_of_nonneg hlowerNonneg, abs_of_nonneg hvNonneg, hlowerValue,
      hLowerExp]
    exact hvLower
  have heLeV : e ≤ v.Fexp := by
    have h := Fcanonic_Rle_Zle (beta:=radix) radix rb lower v hradix rfl
    have hle : lower.Fexp ≤ v.Fexp := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fcanonic_Rle_Zle_check,
        Id.run, ULift.up_down, Fcanonic', lower, rb, Int.cast_ofNat] using
        h ⟨hlowerCan, hvCan, habsLower⟩
    simpa only [lower, Int.cast_ofNat] using hle
  have hStrict :
      |_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) v| <
        (radix : ℝ) ^ e / 2 := by
    by_contra hnot
    have hTie :
        |_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) v| =
          (radix : ℝ) ^ e / 2 :=
      le_antisymm hvResidual (le_of_not_gt hnot)
    exact VeltkampEven2_no_tie (beta:=radix) radix s x v rfl hradix hsGe
      hOddRadix (by simpa only [e, Int.cast_ofNat] using heLeV) (by simpa only [e, Int.cast_ofNat] using hTie)
  have h := ImplyClosestStrict2 (beta:=radix) rb radix u
    (_root_.F2R (beta:=radix) x) v e
  have hstrictResult :
      Closest (beta:=radix) rb (radix : ℝ)
          (_root_.F2R (beta:=radix) x) v ∧
        ∀ g : FloatSpec.Core.Defs.FlocqFloat radix,
          Closest (beta:=radix) rb (radix : ℝ)
              (_root_.F2R (beta:=radix) x) g →
            _root_.F2R (beta:=radix) v = _root_.F2R (beta:=radix) g := by
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ImplyClosestStrict2_check,
      Id.run, Int.cast_ofNat] using
      h ⟨hvBound, hvCan, by simpa only [hLowerExp, Int.cast_ofNat] using hxLower,
        by simpa only [hUpperExp, Int.cast_ofNat] using hxUpper,
        by simpa only [hLowerExp, Int.cast_ofNat] using hvLower, heBound, rfl, hradix,
        huGtOne, hrvNum, hStrict⟩
  refine ⟨v, hvValue, hstrictResult.1, Or.inr ?_⟩
  intro g hg
  exact (hstrictResult.2 g hg).symm

private theorem Veltkamp_pos_normal_of_first_lt
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (z : ℝ) (f : FloatSpec.Core.Defs.FlocqFloat radix)
    (hradix : 1 < radix) (hprecision : 1 < precision)
    (hvNum : b.vNum = Zpower_nat radix precision)
    (hfCan : Fcanonic (beta:=radix) radix b f)
    (hfClosest : Closest (beta:=radix) b (radix : ℝ) z f)
    (hFirstLt :
      _root_.F2R (beta:=radix)
          (firstNormalPos (beta:=radix) radix b precision) < z) :
    Fnormal (beta:=radix) radix b f := by
  rcases hfCan with hfNormal | hfSubnormal
  · exact hfNormal
  · have hfirstNormal :
        Fnormal (beta:=radix) radix b
          (firstNormalPos (beta:=radix) radix b precision) := by
      have h := firstNormalPosNormal (beta:=radix) radix b precision
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, firstNormalPosNormal_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hradix, hprecision, hvNum⟩
    have hfirstClosest :
        Closest (beta:=radix) b (radix : ℝ)
          (_root_.F2R (beta:=radix)
            (firstNormalPos (beta:=radix) radix b precision))
          (firstNormalPos (beta:=radix) radix b precision) := by
      constructor
      · exact hfirstNormal.1
      · intro g _hg
        rw [sub_self, abs_zero]
        exact abs_nonneg _
    have hmono :
        MonotoneP_float (Closest (beta:=radix) b (radix : ℝ)) := by
      have h := ClosestMonotone_from_split_radix_payload (beta:=radix) b (radix : ℝ)
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMonotone_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
    have hfirstLeF :
        _root_.F2R (beta:=radix)
            (firstNormalPos (beta:=radix) radix b precision) ≤
          _root_.F2R (beta:=radix) f :=
      hmono _ _ _ _ hFirstLt hfirstClosest hfClosest
    have hradixPosInt : (0 : Int) < radix := by omega
    have hradixPos : (0 : ℝ) < (radix : ℝ) := by
      exact_mod_cast hradixPosInt
    have hnormMinPos : 0 < nNormMin radix precision := by
      unfold nNormMin
      exact pow_pos hradixPosInt _
    have hfirstPos :
        0 < _root_.F2R (beta:=radix)
          (firstNormalPos (beta:=radix) radix b precision) := by
      change 0 < (nNormMin radix precision : ℝ) *
        (radix : ℝ) ^ (-b.dExp)
      exact mul_pos (by exact_mod_cast hnormMinPos) (zpow_pos hradixPos _)
    have hfNonneg : 0 ≤ _root_.F2R (beta:=radix) f :=
      le_trans (le_of_lt hfirstPos) hfirstLeF
    have hfLtFirst :
        _root_.F2R (beta:=radix) f <
          _root_.F2R (beta:=radix)
            (firstNormalPos (beta:=radix) radix b precision) :=
      (FsubnormalLtFirstNormalPos (beta:=radix) radix b precision f)
        ⟨hfSubnormal, hfSubnormal, Nat.ne_of_gt (lt_trans Nat.zero_lt_one
          hprecision), rfl, hradix, hvNum, hfNonneg⟩
    exfalso
    exact (not_lt_of_ge hfirstLeF) hfLtFirst

/-! Coq positive-input Veltkamp wrapper `Veltkamp_pos`.

Normality of the two rounded intermediates follows from monotonicity and the
normal/subnormal separation at `firstNormalPos`; the exact residual and
reduced-format witness then come from `Veltkamp_aux`. -/
theorem Veltkamp_pos {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x p q hx : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hsGe : 2 ≤ s) (hsLe : s ≤ t - 2)
    (hxNormal : Fnormal (beta:=beta) radix b x)
    (hpCan : Fcanonic (beta:=beta) radix b p)
    (hqCan : Fcanonic (beta:=beta) radix b q)
    (hxPos : 0 < _root_.F2R (beta:=beta) x)
    (hpDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)) p)
    (hqDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hhxDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) q + _root_.F2R (beta:=beta) p) hx) :
    |_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) hx| ≤
        (radix : ℝ) ^ ((s : Int) + x.Fexp) / 2 ∧
      ∃ hx' : FloatSpec.Core.Defs.FlocqFloat beta,
        _root_.F2R (beta:=beta) hx' = _root_.F2R (beta:=beta) hx ∧
          Closest (beta:=beta) (Veltkamp_reducedBound radix b s t)
            (radix : ℝ) (_root_.F2R (beta:=beta) x) hx' ∧
          (s : Int) + x.Fexp ≤ hx'.Fexp := by
  subst beta
  have htGtOne : 1 < t := by omega
  have htNe : t ≠ 0 := Nat.ne_of_gt (lt_trans Nat.zero_lt_one htGtOne)
  have hxBound : Fbounded (beta:=radix) b x := hxNormal.1
  have hfirstLeX :
      _root_.F2R (beta:=radix)
          (firstNormalPos (beta:=radix) radix b t) ≤
        _root_.F2R (beta:=radix) x :=
    (FnormalLtFirstNormalPos (beta:=radix) radix b t x)
      ⟨hxNormal, hxNormal, htNe, rfl, hradix, hvNum, le_of_lt hxPos⟩
  have hradixPosInt : (0 : Int) < radix := by omega
  have hradixPos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradixPosInt
  have hradixNe : (radix : ℝ) ≠ 0 := ne_of_gt hradixPos
  have hpowSPos : 0 < (radix : ℝ) ^ (s : Int) :=
    zpow_pos hradixPos _
  have hxLtScaled :
      _root_.F2R (beta:=radix) x <
        _root_.F2R (beta:=radix) x *
          ((radix : ℝ) ^ (s : Int) + 1) := by
    nlinarith
  have hfirstLtScaled :
      _root_.F2R (beta:=radix)
          (firstNormalPos (beta:=radix) radix b t) <
        _root_.F2R (beta:=radix) x *
          ((radix : ℝ) ^ (s : Int) + 1) :=
    lt_of_le_of_lt hfirstLeX hxLtScaled
  have hpNormal : Fnormal (beta:=radix) radix b p :=
    Veltkamp_pos_normal_of_first_lt b radix t
      (_root_.F2R (beta:=radix) x *
        ((radix : ℝ) ^ (s : Int) + 1)) p
      hradix htGtOne hvNum hpCan hpDef hfirstLtScaled
  have hmono :
      MonotoneP_float (Closest (beta:=radix) b (radix : ℝ)) := by
    have h := ClosestMonotone_from_split_radix_payload (beta:=radix) b (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMonotone_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  let xShift : FloatSpec.Core.Defs.FlocqFloat radix :=
    ⟨x.Fnum, x.Fexp + 2⟩
  have hxShiftBound : Fbounded (beta:=radix) b xShift := by
    constructor
    · simpa only [xShift, Int.cast_ofNat] using hxBound.1
    · dsimp [xShift]
      exact le_trans hxBound.2 (by omega)
  have hxShiftValue :
      _root_.F2R (beta:=radix) xShift =
        _root_.F2R (beta:=radix) x * (radix : ℝ) ^ (2 : Int) := by
    dsimp [xShift, _root_.F2R, FloatSpec.Core.Defs.F2R]
    rw [zpow_add₀ hradixNe]
    ring
  have hxShiftClosest :
      Closest (beta:=radix) b (radix : ℝ)
        (_root_.F2R (beta:=radix) xShift) xShift := by
    constructor
    · exact hxShiftBound
    · intro g _hg
      rw [sub_self, abs_zero]
      exact abs_nonneg _
  have hradixOneLe : (1 : ℝ) ≤ (radix : ℝ) := by
    exact_mod_cast (le_of_lt hradix)
  have hpowTwoLeS :
      (radix : ℝ) ^ (2 : Int) ≤ (radix : ℝ) ^ (s : Int) :=
    zpow_le_zpow_right₀ hradixOneLe (by omega)
  have hxShiftLtScaled :
      _root_.F2R (beta:=radix) xShift <
        _root_.F2R (beta:=radix) x *
          ((radix : ℝ) ^ (s : Int) + 1) := by
    rw [hxShiftValue]
    calc
      _root_.F2R (beta:=radix) x * (radix : ℝ) ^ (2 : Int) ≤
          _root_.F2R (beta:=radix) x * (radix : ℝ) ^ (s : Int) :=
        mul_le_mul_of_nonneg_left hpowTwoLeS (le_of_lt hxPos)
      _ < _root_.F2R (beta:=radix) x *
          ((radix : ℝ) ^ (s : Int) + 1) := by nlinarith
  have hxShiftLeP :
      _root_.F2R (beta:=radix) xShift ≤
        _root_.F2R (beta:=radix) p :=
    hmono _ _ xShift p hxShiftLtScaled hxShiftClosest hpDef
  have hradixTwoLe : (2 : ℝ) ≤ (radix : ℝ) := by
    exact_mod_cast (by omega : (2 : Int) ≤ radix)
  have hpowTwoGeFour :
      (4 : ℝ) ≤ (radix : ℝ) ^ (2 : Int) := by
    norm_num [zpow_ofNat]
    nlinarith
  have hpowTwoGtTwo : (2 : ℝ) < (radix : ℝ) ^ (2 : Int) :=
    lt_of_lt_of_le (by norm_num) hpowTwoGeFour
  have htwoXLtShift :
      2 * _root_.F2R (beta:=radix) x <
        _root_.F2R (beta:=radix) xShift := by
    rw [hxShiftValue]
    have h := mul_lt_mul_of_pos_left hpowTwoGtTwo hxPos
    nlinarith
  have htwoXLtP :
      2 * _root_.F2R (beta:=radix) x <
        _root_.F2R (beta:=radix) p :=
    lt_of_lt_of_le htwoXLtShift hxShiftLeP
  have hfirstLtOppInput :
      _root_.F2R (beta:=radix)
          (firstNormalPos (beta:=radix) radix b t) <
        -(_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p) := by
    have hxLtOppInput :
        _root_.F2R (beta:=radix) x <
          -(_root_.F2R (beta:=radix) x -
            _root_.F2R (beta:=radix) p) := by
      nlinarith
    exact lt_of_le_of_lt hfirstLeX hxLtOppInput
  have hqOppCan :
      Fcanonic (beta:=radix) radix b (Fopp (beta:=radix) q) := by
    have h := FcanonicFopp (beta:=radix) radix b q
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFopp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hqCan
  have hqOppClosest :
      Closest (beta:=radix) b (radix : ℝ)
        (-(_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p))
        (Fopp (beta:=radix) q) := by
    have h := ClosestOpp (beta:=radix) b (radix : ℝ) q
      (_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestOpp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hqDef
  have hqOppNormal :
      Fnormal (beta:=radix) radix b (Fopp (beta:=radix) q) :=
    Veltkamp_pos_normal_of_first_lt b radix t
      (-(_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p))
      (Fopp (beta:=radix) q) hradix htGtOne hvNum hqOppCan hqOppClosest
      hfirstLtOppInput
  have hqNormal : Fnormal (beta:=radix) radix b q := by
    have h := FnormalFop (beta:=radix) b radix (Fopp (beta:=radix) q)
    have hdouble :
        Fnormal (beta:=radix) radix b
          (Fopp (beta:=radix) (Fopp (beta:=radix) q)) := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalFop_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h hqOppNormal
    simpa [Fopp, FloatSpec.Calc.Operations.Fopp] using hdouble
  exact Veltkamp_aux (beta:=radix) b radix s t x p q hx rfl hradix hvNum
    hsGe hsLe hxBound hpDef hqDef hhxDef hxPos hpNormal hqNormal hxNormal

/-! Coq sign-symmetric Veltkamp wrapper `VeltkampN_aux`.

The positive branch is `Veltkamp_pos`.  For a negative normal input, negate
all four floats, apply the positive theorem, and negate its reduced witness
back; normality excludes the remaining zero case. -/
theorem VeltkampN_aux {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x p q hx : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hsGe : 2 ≤ s) (hsLe : s ≤ t - 2)
    (hxNormal : Fnormal (beta:=beta) radix b x)
    (hpCan : Fcanonic (beta:=beta) radix b p)
    (hqCan : Fcanonic (beta:=beta) radix b q)
    (hpDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)) p)
    (hqDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hhxDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) q + _root_.F2R (beta:=beta) p) hx) :
    |_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) hx| ≤
        (radix : ℝ) ^ ((s : Int) + x.Fexp) / 2 ∧
      ∃ hx' : FloatSpec.Core.Defs.FlocqFloat beta,
        _root_.F2R (beta:=beta) hx' = _root_.F2R (beta:=beta) hx ∧
          Closest (beta:=beta) (Veltkamp_reducedBound radix b s t)
            (radix : ℝ) (_root_.F2R (beta:=beta) x) hx' ∧
          (s : Int) + x.Fexp ≤ hx'.Fexp := by
  subst beta
  have hradixPosInt : (0 : Int) < radix := by omega
  have hvNumPos : 0 < b.vNum := by
    rw [hvNum, Zpower_nat]
    exact pow_pos hradixPosInt _
  have hxNumNe : x.Fnum ≠ 0 := by
    intro hxNumZero
    have hnormalMant := hxNormal.2
    rw [hxNumZero, mul_zero, abs_zero] at hnormalMant
    omega
  have hxValueNe : _root_.F2R (beta:=radix) x ≠ 0 :=
    FloatSpec.Core.Float_prop.F2R_neq_0 (beta:=radix) x hradix hxNumNe
  by_cases hxNonneg : 0 ≤ _root_.F2R (beta:=radix) x
  · have hxPos : 0 < _root_.F2R (beta:=radix) x :=
      lt_of_le_of_ne hxNonneg hxValueNe.symm
    exact Veltkamp_pos (beta:=radix) b radix s t x p q hx rfl hradix
      hvNum hsGe hsLe hxNormal hpCan hqCan hxPos hpDef hqDef hhxDef
  · have hxNeg : _root_.F2R (beta:=radix) x < 0 := lt_of_not_ge hxNonneg
    have hoppValue (f : FloatSpec.Core.Defs.FlocqFloat radix) :
        _root_.F2R (beta:=radix) (Fopp (beta:=radix) f) =
          -_root_.F2R (beta:=radix) f := by
      have h := (FloatSpec.Calc.Operations.F2R_opp (beta:=radix) f) trivial
      simpa [Fopp, _root_.F2R] using h
    have hxOppNormal :
        Fnormal (beta:=radix) radix b (Fopp (beta:=radix) x) := by
      have h := FnormalFop (beta:=radix) b radix x
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalFop_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h hxNormal
    have hpOppCan :
        Fcanonic (beta:=radix) radix b (Fopp (beta:=radix) p) := by
      have h := FcanonicFopp (beta:=radix) radix b p
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFopp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h hpCan
    have hqOppCan :
        Fcanonic (beta:=radix) radix b (Fopp (beta:=radix) q) := by
      have h := FcanonicFopp (beta:=radix) radix b q
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFopp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h hqCan
    have hxOppPos :
        0 < _root_.F2R (beta:=radix) (Fopp (beta:=radix) x) := by
      rw [hoppValue]
      linarith
    have hpOppDef :
        Closest (beta:=radix) b (radix : ℝ)
          (_root_.F2R (beta:=radix) (Fopp (beta:=radix) x) *
            ((radix : ℝ) ^ (s : Int) + 1))
          (Fopp (beta:=radix) p) := by
      have h := ClosestOpp (beta:=radix) b (radix : ℝ) p
        (_root_.F2R (beta:=radix) x *
          ((radix : ℝ) ^ (s : Int) + 1))
      have hOpp :
          Closest (beta:=radix) b (radix : ℝ)
            (-(_root_.F2R (beta:=radix) x *
              ((radix : ℝ) ^ (s : Int) + 1)))
            (Fopp (beta:=radix) p) := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestOpp_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h hpDef
      rw [hoppValue]
      convert hOpp using 1 <;> ring
    have hqOppDef :
        Closest (beta:=radix) b (radix : ℝ)
          (_root_.F2R (beta:=radix) (Fopp (beta:=radix) x) -
            _root_.F2R (beta:=radix) (Fopp (beta:=radix) p))
          (Fopp (beta:=radix) q) := by
      have h := ClosestOpp (beta:=radix) b (radix : ℝ) q
        (_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p)
      have hOpp :
          Closest (beta:=radix) b (radix : ℝ)
            (-(_root_.F2R (beta:=radix) x -
              _root_.F2R (beta:=radix) p))
            (Fopp (beta:=radix) q) := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestOpp_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h hqDef
      rw [hoppValue, hoppValue]
      convert hOpp using 1 <;> ring
    have hhxOppDef :
        Closest (beta:=radix) b (radix : ℝ)
          (_root_.F2R (beta:=radix) (Fopp (beta:=radix) q) +
            _root_.F2R (beta:=radix) (Fopp (beta:=radix) p))
          (Fopp (beta:=radix) hx) := by
      have h := ClosestOpp (beta:=radix) b (radix : ℝ) hx
        (_root_.F2R (beta:=radix) q + _root_.F2R (beta:=radix) p)
      have hOpp :
          Closest (beta:=radix) b (radix : ℝ)
            (-(_root_.F2R (beta:=radix) q +
              _root_.F2R (beta:=radix) p))
            (Fopp (beta:=radix) hx) := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestOpp_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h hhxDef
      rw [hoppValue, hoppValue]
      convert hOpp using 1 <;> ring
    rcases Veltkamp_pos (beta:=radix) b radix s t
        (Fopp (beta:=radix) x) (Fopp (beta:=radix) p)
        (Fopp (beta:=radix) q) (Fopp (beta:=radix) hx)
        rfl hradix hvNum hsGe hsLe hxOppNormal hpOppCan hqOppCan hxOppPos
        hpOppDef hqOppDef hhxOppDef with
      ⟨hResidual, v, hvValue, hvClosest, hvExp⟩
    have hResidual' :
        |_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) hx| ≤
          (radix : ℝ) ^ ((s : Int) + x.Fexp) / 2 := by
      have hAbs :
          |_root_.F2R (beta:=radix) (Fopp (beta:=radix) x) -
              _root_.F2R (beta:=radix) (Fopp (beta:=radix) hx)| =
            |_root_.F2R (beta:=radix) x -
              _root_.F2R (beta:=radix) hx| := by
        rw [hoppValue, hoppValue]
        have harg :
            -_root_.F2R (beta:=radix) x -
                -_root_.F2R (beta:=radix) hx =
              -(_root_.F2R (beta:=radix) x -
                _root_.F2R (beta:=radix) hx) := by
          ring
        rw [harg, abs_neg]
      rw [← hAbs]
      simpa [Fopp, FloatSpec.Calc.Operations.Fopp] using hResidual
    refine ⟨hResidual', Fopp (beta:=radix) v, ?_, ?_, ?_⟩
    · rw [hoppValue, hvValue, hoppValue]
      ring
    · have h := ClosestOpp (beta:=radix)
          (Veltkamp_reducedBound radix b s t) (radix : ℝ) v
          (_root_.F2R (beta:=radix) (Fopp (beta:=radix) x))
      have hOpp :
          Closest (beta:=radix) (Veltkamp_reducedBound radix b s t)
            (radix : ℝ)
            (-_root_.F2R (beta:=radix) (Fopp (beta:=radix) x))
            (Fopp (beta:=radix) v) := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestOpp_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h hvClosest
      rw [hoppValue] at hOpp
      simpa using hOpp
    · simpa [Fopp, FloatSpec.Calc.Operations.Fopp] using hvExp

/-! Coq normalized Veltkamp wrapper `VeltkampN`.

The closest-rounding premises already make `p` and `q` bounded.  Normalize
them into canonical representatives with unchanged real values, transfer the
three closestness hypotheses, and apply `VeltkampN_aux`. -/
theorem VeltkampN {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x p q hx : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hsGe : 2 ≤ s) (hsLe : s ≤ t - 2)
    (hxNormal : Fnormal (beta:=beta) radix b x)
    (hpDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)) p)
    (hqDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hhxDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) q + _root_.F2R (beta:=beta) p) hx) :
    |_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) hx| ≤
        (radix : ℝ) ^ ((s : Int) + x.Fexp) / 2 ∧
      ∃ hx' : FloatSpec.Core.Defs.FlocqFloat beta,
        _root_.F2R (beta:=beta) hx' = _root_.F2R (beta:=beta) hx ∧
          Closest (beta:=beta) (Veltkamp_reducedBound radix b s t)
            (radix : ℝ) (_root_.F2R (beta:=beta) x) hx' ∧
          (s : Int) + x.Fexp ≤ hx'.Fexp := by
  subst beta
  have htNe : t ≠ 0 := by omega
  let np : FloatSpec.Core.Defs.FlocqFloat radix :=
    Fnormalize (beta:=radix) radix b t p
  let nq : FloatSpec.Core.Defs.FlocqFloat radix :=
    Fnormalize (beta:=radix) radix b t q
  have hpBound : Fbounded (beta:=radix) b p := hpDef.1
  have hqBound : Fbounded (beta:=radix) b q := hqDef.1
  have hnpValue :
      _root_.F2R (beta:=radix) np = _root_.F2R (beta:=radix) p := by
    have h := FnormalizeCorrect (beta:=radix) radix b t p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
      Id.run, ULift.up_down, np, Int.cast_ofNat] using h ⟨rfl, hradix⟩
  have hnqValue :
      _root_.F2R (beta:=radix) nq = _root_.F2R (beta:=radix) q := by
    have h := FnormalizeCorrect (beta:=radix) radix b t q
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
      Id.run, ULift.up_down, nq, Int.cast_ofNat] using h ⟨rfl, hradix⟩
  have hnpBound : Fbounded (beta:=radix) b np := by
    have h := FnormalizeBounded (beta:=radix) radix b t p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeBounded_check,
      Id.run, ULift.up_down, np, Int.cast_ofNat] using
      h ⟨hpBound, hpBound, htNe, hradix, hvNum⟩
  have hnqBound : Fbounded (beta:=radix) b nq := by
    have h := FnormalizeBounded (beta:=radix) radix b t q
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeBounded_check,
      Id.run, ULift.up_down, nq, Int.cast_ofNat] using
      h ⟨hqBound, hqBound, htNe, hradix, hvNum⟩
  have hnpCan : Fcanonic (beta:=radix) radix b np := by
    have h := FnormalizeCanonic (beta:=radix) radix b t p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
      Id.run, ULift.up_down, np, Int.cast_ofNat] using
      h ⟨hpBound, hpBound, htNe, hradix, hvNum⟩
  have hnqCan : Fcanonic (beta:=radix) radix b nq := by
    have h := FnormalizeCanonic (beta:=radix) radix b t q
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
      Id.run, ULift.up_down, nq, Int.cast_ofNat] using
      h ⟨hqBound, hqBound, htNe, hradix, hvNum⟩
  have hnpClosest :
      Closest (beta:=radix) b (radix : ℝ)
        (_root_.F2R (beta:=radix) x *
          ((radix : ℝ) ^ (s : Int) + 1)) np := by
    constructor
    · exact hnpBound
    · intro g hg
      rw [hnpValue]
      exact hpDef.2 g hg
  have hnqClosest :
      Closest (beta:=radix) b (radix : ℝ)
        (_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) np) nq := by
    constructor
    · exact hnqBound
    · intro g hg
      rw [hnpValue, hnqValue]
      exact hqDef.2 g hg
  have hhxClosest :
      Closest (beta:=radix) b (radix : ℝ)
        (_root_.F2R (beta:=radix) nq + _root_.F2R (beta:=radix) np) hx := by
    rw [hnqValue, hnpValue]
    exact hhxDef
  exact VeltkampN_aux (beta:=radix) b radix s t x np nq hx rfl hradix
    hvNum hsGe hsLe hxNormal hnpCan hnqCan hnpClosest hnqClosest hhxClosest

private theorem Veltkamp_pos_q_normal
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x p q : FloatSpec.Core.Defs.FlocqFloat radix)
    (hradix : 1 < radix) (hvNum : b.vNum = Zpower_nat radix t)
    (hsGe : 2 ≤ s) (hsLe : s ≤ t - 2)
    (hxNormal : Fnormal (beta:=radix) radix b x)
    (hqCan : Fcanonic (beta:=radix) radix b q)
    (hxPos : 0 < _root_.F2R (beta:=radix) x)
    (hpDef : Closest (beta:=radix) b (radix : ℝ)
      (_root_.F2R (beta:=radix) x * ((radix : ℝ) ^ (s : Int) + 1)) p)
    (hqDef : Closest (beta:=radix) b (radix : ℝ)
      (_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p) q) :
    Fnormal (beta:=radix) radix b q := by
  have htGtOne : 1 < t := by omega
  have htNe : t ≠ 0 := Nat.ne_of_gt (lt_trans Nat.zero_lt_one htGtOne)
  have hxBound : Fbounded (beta:=radix) b x := hxNormal.1
  have hfirstLeX :
      _root_.F2R (beta:=radix)
          (firstNormalPos (beta:=radix) radix b t) ≤
        _root_.F2R (beta:=radix) x :=
    (FnormalLtFirstNormalPos (beta:=radix) radix b t x)
      ⟨hxNormal, hxNormal, htNe, rfl, hradix, hvNum, le_of_lt hxPos⟩
  have hradixPosInt : (0 : Int) < radix := by omega
  have hradixPos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradixPosInt
  have hradixNe : (radix : ℝ) ≠ 0 := ne_of_gt hradixPos
  have hmono :
      MonotoneP_float (Closest (beta:=radix) b (radix : ℝ)) := by
    have h := ClosestMonotone_from_split_radix_payload (beta:=radix) b (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMonotone_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  let xShift : FloatSpec.Core.Defs.FlocqFloat radix :=
    ⟨x.Fnum, x.Fexp + 2⟩
  have hxShiftBound : Fbounded (beta:=radix) b xShift := by
    constructor
    · simpa only [xShift, Int.cast_ofNat] using hxBound.1
    · dsimp [xShift]
      exact le_trans hxBound.2 (by omega)
  have hxShiftValue :
      _root_.F2R (beta:=radix) xShift =
        _root_.F2R (beta:=radix) x * (radix : ℝ) ^ (2 : Int) := by
    dsimp [xShift, _root_.F2R, FloatSpec.Core.Defs.F2R]
    rw [zpow_add₀ hradixNe]
    ring
  have hxShiftClosest :
      Closest (beta:=radix) b (radix : ℝ)
        (_root_.F2R (beta:=radix) xShift) xShift := by
    constructor
    · exact hxShiftBound
    · intro g _hg
      rw [sub_self, abs_zero]
      exact abs_nonneg _
  have hradixOneLe : (1 : ℝ) ≤ (radix : ℝ) := by
    exact_mod_cast (le_of_lt hradix)
  have hpowTwoLeS :
      (radix : ℝ) ^ (2 : Int) ≤ (radix : ℝ) ^ (s : Int) :=
    zpow_le_zpow_right₀ hradixOneLe (by omega)
  have hxShiftLtScaled :
      _root_.F2R (beta:=radix) xShift <
        _root_.F2R (beta:=radix) x *
          ((radix : ℝ) ^ (s : Int) + 1) := by
    rw [hxShiftValue]
    calc
      _root_.F2R (beta:=radix) x * (radix : ℝ) ^ (2 : Int) ≤
          _root_.F2R (beta:=radix) x * (radix : ℝ) ^ (s : Int) :=
        mul_le_mul_of_nonneg_left hpowTwoLeS (le_of_lt hxPos)
      _ < _root_.F2R (beta:=radix) x *
          ((radix : ℝ) ^ (s : Int) + 1) := by nlinarith
  have hxShiftLeP :
      _root_.F2R (beta:=radix) xShift ≤
        _root_.F2R (beta:=radix) p :=
    hmono _ _ xShift p hxShiftLtScaled hxShiftClosest hpDef
  have hradixTwoLe : (2 : ℝ) ≤ (radix : ℝ) := by
    exact_mod_cast (by omega : (2 : Int) ≤ radix)
  have hpowTwoGeFour :
      (4 : ℝ) ≤ (radix : ℝ) ^ (2 : Int) := by
    norm_num [zpow_ofNat]
    nlinarith
  have hpowTwoGtTwo : (2 : ℝ) < (radix : ℝ) ^ (2 : Int) :=
    lt_of_lt_of_le (by norm_num) hpowTwoGeFour
  have htwoXLtShift :
      2 * _root_.F2R (beta:=radix) x <
        _root_.F2R (beta:=radix) xShift := by
    rw [hxShiftValue]
    have h := mul_lt_mul_of_pos_left hpowTwoGtTwo hxPos
    nlinarith
  have htwoXLtP :
      2 * _root_.F2R (beta:=radix) x <
        _root_.F2R (beta:=radix) p :=
    lt_of_lt_of_le htwoXLtShift hxShiftLeP
  have hfirstLtOppInput :
      _root_.F2R (beta:=radix)
          (firstNormalPos (beta:=radix) radix b t) <
        -(_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p) := by
    have hxLtOppInput :
        _root_.F2R (beta:=radix) x <
          -(_root_.F2R (beta:=radix) x -
            _root_.F2R (beta:=radix) p) := by
      nlinarith
    exact lt_of_le_of_lt hfirstLeX hxLtOppInput
  have hqOppCan :
      Fcanonic (beta:=radix) radix b (Fopp (beta:=radix) q) := by
    have h := FcanonicFopp (beta:=radix) radix b q
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFopp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hqCan
  have hqOppClosest :
      Closest (beta:=radix) b (radix : ℝ)
        (-(_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p))
        (Fopp (beta:=radix) q) := by
    have h := ClosestOpp (beta:=radix) b (radix : ℝ) q
      (_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestOpp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hqDef
  have hqOppNormal :
      Fnormal (beta:=radix) radix b (Fopp (beta:=radix) q) :=
    Veltkamp_pos_normal_of_first_lt b radix t
      (-(_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p))
      (Fopp (beta:=radix) q) hradix htGtOne hvNum hqOppCan hqOppClosest
      hfirstLtOppInput
  have h := FnormalFop (beta:=radix) b radix (Fopp (beta:=radix) q)
  have hdouble :
      Fnormal (beta:=radix) radix b
        (Fopp (beta:=radix) (Fopp (beta:=radix) q)) := by
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalFop_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hqOppNormal
  simpa [Fopp, FloatSpec.Calc.Operations.Fopp] using hdouble

/-! Coq positive-input even-rounding wrapper `VeltkampEven_pos`.

The `Closest` components yield normal intermediates.  Even radix then uses
`VeltkampEven1`; odd radix uses the no-midpoint theorem `VeltkampEven2`. -/
theorem VeltkampEven_pos {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x p q hx : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hsGe : 2 ≤ s) (hsLe : s ≤ t - 2)
    (hxNormal : Fnormal (beta:=beta) radix b x)
    (hpCan : Fcanonic (beta:=beta) radix b p)
    (hqCan : Fcanonic (beta:=beta) radix b q)
    (hxPos : 0 < _root_.F2R (beta:=beta) x)
    (hpDefEven : EvenClosest (beta:=beta) b (radix : ℝ) t
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)) p)
    (hqDefEven : EvenClosest (beta:=beta) b (radix : ℝ) t
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hhxDefEven : EvenClosest (beta:=beta) b (radix : ℝ) t
      (_root_.F2R (beta:=beta) q + _root_.F2R (beta:=beta) p) hx) :
    ∃ hx' : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) hx' = _root_.F2R (beta:=beta) hx ∧
        EvenClosest (beta:=beta) (Veltkamp_reducedBound radix b s t)
          (radix : ℝ) (t - s) (_root_.F2R (beta:=beta) x) hx' := by
  subst beta
  have hpDef := hpDefEven.1
  have hqDef := hqDefEven.1
  have hhxDef := hhxDefEven.1
  have htGtOne : 1 < t := by omega
  have htNe : t ≠ 0 := Nat.ne_of_gt (lt_trans Nat.zero_lt_one htGtOne)
  have hfirstLeX :
      _root_.F2R (beta:=radix)
          (firstNormalPos (beta:=radix) radix b t) ≤
        _root_.F2R (beta:=radix) x :=
    (FnormalLtFirstNormalPos (beta:=radix) radix b t x)
      ⟨hxNormal, hxNormal, htNe, rfl, hradix, hvNum, le_of_lt hxPos⟩
  have hradixPosInt : (0 : Int) < radix := by omega
  have hradixPos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradixPosInt
  have hpowSPos : 0 < (radix : ℝ) ^ (s : Int) :=
    zpow_pos hradixPos _
  have hxLtScaled :
      _root_.F2R (beta:=radix) x <
        _root_.F2R (beta:=radix) x *
          ((radix : ℝ) ^ (s : Int) + 1) := by
    nlinarith
  have hpNormal : Fnormal (beta:=radix) radix b p :=
    Veltkamp_pos_normal_of_first_lt b radix t
      (_root_.F2R (beta:=radix) x *
        ((radix : ℝ) ^ (s : Int) + 1)) p
      hradix htGtOne hvNum hpCan hpDef
      (lt_of_le_of_lt hfirstLeX hxLtScaled)
  have hqNormal : Fnormal (beta:=radix) radix b q :=
    Veltkamp_pos_q_normal b radix s t x p q hradix hvNum hsGe hsLe hxNormal
      hqCan hxPos hpDef hqDef
  have hxBound : Fbounded (beta:=radix) b x := hxNormal.1
  rcases Int.even_or_odd radix with hEvenRadix | hOddRadix
  · exact VeltkampEven1 (beta:=radix) b radix s t x p q hx rfl hradix
      hvNum hsGe hsLe hxBound hpDef hqDef hhxDef hxPos hpNormal hqNormal
      hxNormal hpDefEven hqDefEven hhxDefEven hEvenRadix
  · exact VeltkampEven2 (beta:=radix) b radix s t x p q hx rfl hradix
      hvNum hsGe hsLe hxBound hpDef hqDef hhxDef hxPos hpNormal hqNormal
      hxNormal hpDefEven hqDefEven hhxDefEven hOddRadix

/-! Coq sign-symmetric even-rounding wrapper `VeltkampEvenN_aux`.

The positive branch is `VeltkampEven_pos`. For a negative normal input, negate
all four floats, transport the three even-closest premises by symmetry, apply
the positive theorem, and negate its reduced witness back. -/
theorem VeltkampEvenN_aux {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x p q hx : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hsGe : 2 ≤ s) (hsLe : s ≤ t - 2)
    (hxNormal : Fnormal (beta:=beta) radix b x)
    (hpCan : Fcanonic (beta:=beta) radix b p)
    (hqCan : Fcanonic (beta:=beta) radix b q)
    (hpDefEven : EvenClosest (beta:=beta) b (radix : ℝ) t
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)) p)
    (hqDefEven : EvenClosest (beta:=beta) b (radix : ℝ) t
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hhxDefEven : EvenClosest (beta:=beta) b (radix : ℝ) t
      (_root_.F2R (beta:=beta) q + _root_.F2R (beta:=beta) p) hx) :
    ∃ hx' : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) hx' = _root_.F2R (beta:=beta) hx ∧
        EvenClosest (beta:=beta) (Veltkamp_reducedBound radix b s t)
          (radix : ℝ) (t - s) (_root_.F2R (beta:=beta) x) hx' := by
  subst beta
  have hradixPosInt : (0 : Int) < radix := by omega
  have hvNumPos : 0 < b.vNum := by
    rw [hvNum, Zpower_nat]
    exact pow_pos hradixPosInt _
  have hxNumNe : x.Fnum ≠ 0 := by
    intro hxNumZero
    have hnormalMant := hxNormal.2
    rw [hxNumZero, mul_zero, abs_zero] at hnormalMant
    omega
  have hxValueNe : _root_.F2R (beta:=radix) x ≠ 0 :=
    FloatSpec.Core.Float_prop.F2R_neq_0 (beta:=radix) x hradix hxNumNe
  by_cases hxNonneg : 0 ≤ _root_.F2R (beta:=radix) x
  · have hxPos : 0 < _root_.F2R (beta:=radix) x :=
      lt_of_le_of_ne hxNonneg hxValueNe.symm
    exact VeltkampEven_pos (beta:=radix) b radix s t x p q hx rfl hradix
      hvNum hsGe hsLe hxNormal hpCan hqCan hxPos hpDefEven hqDefEven hhxDefEven
  · have hxNeg : _root_.F2R (beta:=radix) x < 0 := lt_of_not_ge hxNonneg
    have hoppValue (f : FloatSpec.Core.Defs.FlocqFloat radix) :
        _root_.F2R (beta:=radix) (Fopp (beta:=radix) f) =
          -_root_.F2R (beta:=radix) f := by
      have h := (FloatSpec.Calc.Operations.F2R_opp (beta:=radix) f) trivial
      simpa [Fopp, _root_.F2R] using h
    have hxOppNormal :
        Fnormal (beta:=radix) radix b (Fopp (beta:=radix) x) := by
      have h := FnormalFop (beta:=radix) b radix x
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalFop_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h hxNormal
    have hpOppCan :
        Fcanonic (beta:=radix) radix b (Fopp (beta:=radix) p) := by
      have h := FcanonicFopp (beta:=radix) radix b p
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFopp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h hpCan
    have hqOppCan :
        Fcanonic (beta:=radix) radix b (Fopp (beta:=radix) q) := by
      have h := FcanonicFopp (beta:=radix) radix b q
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFopp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h hqCan
    have hxOppPos :
        0 < _root_.F2R (beta:=radix) (Fopp (beta:=radix) x) := by
      rw [hoppValue]
      linarith
    have hsym :
        SymmetricP (EvenClosest (beta:=radix) b (radix : ℝ) t) := by
      have h := EvenClosestSymmetric (beta:=radix) b (radix : ℝ) t
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestSymmetric_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
    have hpOppDefEven :
        EvenClosest (beta:=radix) b (radix : ℝ) t
          (_root_.F2R (beta:=radix) (Fopp (beta:=radix) x) *
            ((radix : ℝ) ^ (s : Int) + 1))
          (Fopp (beta:=radix) p) := by
      have hOpp := hsym
        (_root_.F2R (beta:=radix) x * ((radix : ℝ) ^ (s : Int) + 1))
        p hpDefEven
      rw [hoppValue]
      convert hOpp using 1 <;> ring
    have hqOppDefEven :
        EvenClosest (beta:=radix) b (radix : ℝ) t
          (_root_.F2R (beta:=radix) (Fopp (beta:=radix) x) -
            _root_.F2R (beta:=radix) (Fopp (beta:=radix) p))
          (Fopp (beta:=radix) q) := by
      have hOpp := hsym
        (_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p)
        q hqDefEven
      rw [hoppValue, hoppValue]
      convert hOpp using 1 <;> ring
    have hhxOppDefEven :
        EvenClosest (beta:=radix) b (radix : ℝ) t
          (_root_.F2R (beta:=radix) (Fopp (beta:=radix) q) +
            _root_.F2R (beta:=radix) (Fopp (beta:=radix) p))
          (Fopp (beta:=radix) hx) := by
      have hOpp := hsym
        (_root_.F2R (beta:=radix) q + _root_.F2R (beta:=radix) p)
        hx hhxDefEven
      rw [hoppValue, hoppValue]
      convert hOpp using 1 <;> ring
    rcases VeltkampEven_pos (beta:=radix) b radix s t
        (Fopp (beta:=radix) x) (Fopp (beta:=radix) p)
        (Fopp (beta:=radix) q) (Fopp (beta:=radix) hx)
        rfl hradix hvNum hsGe hsLe hxOppNormal hpOppCan hqOppCan hxOppPos
        hpOppDefEven hqOppDefEven hhxOppDefEven with
      ⟨v, hvValue, hvEven⟩
    refine ⟨Fopp (beta:=radix) v, ?_, ?_⟩
    · rw [hoppValue, hvValue, hoppValue]
      ring
    · have hsymReduced :
          SymmetricP (EvenClosest (beta:=radix)
            (Veltkamp_reducedBound radix b s t) (radix : ℝ) (t - s)) := by
        have h := EvenClosestSymmetric (beta:=radix)
          (Veltkamp_reducedBound radix b s t) (radix : ℝ) (t - s)
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestSymmetric_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
      have hOpp := hsymReduced
        (_root_.F2R (beta:=radix) (Fopp (beta:=radix) x)) v hvEven
      rw [hoppValue] at hOpp
      simpa using hOpp

/-! Coq normalized nearest-even Veltkamp wrapper `VeltkampEvenN`.

Normalize the rounded intermediates into canonical representatives, preserve
both branches of each `EvenClosest` premise, and apply `VeltkampEvenN_aux`. -/
theorem VeltkampEvenN {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x p q hx : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hsGe : 2 ≤ s) (hsLe : s ≤ t - 2)
    (hxNormal : Fnormal (beta:=beta) radix b x)
    (hpDefEven : EvenClosest (beta:=beta) b (radix : ℝ) t
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)) p)
    (hqDefEven : EvenClosest (beta:=beta) b (radix : ℝ) t
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hhxDefEven : EvenClosest (beta:=beta) b (radix : ℝ) t
      (_root_.F2R (beta:=beta) q + _root_.F2R (beta:=beta) p) hx) :
    ∃ hx' : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) hx' = _root_.F2R (beta:=beta) hx ∧
        EvenClosest (beta:=beta) (Veltkamp_reducedBound radix b s t)
          (radix : ℝ) (t - s) (_root_.F2R (beta:=beta) x) hx' := by
  subst beta
  have htNe : t ≠ 0 := by omega
  let np : FloatSpec.Core.Defs.FlocqFloat radix :=
    Fnormalize (beta:=radix) radix b t p
  let nq : FloatSpec.Core.Defs.FlocqFloat radix :=
    Fnormalize (beta:=radix) radix b t q
  have hpBound : Fbounded (beta:=radix) b p := hpDefEven.1.1
  have hqBound : Fbounded (beta:=radix) b q := hqDefEven.1.1
  have hnpValue :
      _root_.F2R (beta:=radix) np = _root_.F2R (beta:=radix) p := by
    have h := FnormalizeCorrect (beta:=radix) radix b t p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
      Id.run, ULift.up_down, np, Int.cast_ofNat] using h ⟨rfl, hradix⟩
  have hnqValue :
      _root_.F2R (beta:=radix) nq = _root_.F2R (beta:=radix) q := by
    have h := FnormalizeCorrect (beta:=radix) radix b t q
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
      Id.run, ULift.up_down, nq, Int.cast_ofNat] using h ⟨rfl, hradix⟩
  have hnpBound : Fbounded (beta:=radix) b np := by
    have h := FnormalizeBounded (beta:=radix) radix b t p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeBounded_check,
      Id.run, ULift.up_down, np, Int.cast_ofNat] using
      h ⟨hpBound, hpBound, htNe, hradix, hvNum⟩
  have hnqBound : Fbounded (beta:=radix) b nq := by
    have h := FnormalizeBounded (beta:=radix) radix b t q
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeBounded_check,
      Id.run, ULift.up_down, nq, Int.cast_ofNat] using
      h ⟨hqBound, hqBound, htNe, hradix, hvNum⟩
  have hnpCan : Fcanonic (beta:=radix) radix b np := by
    have h := FnormalizeCanonic (beta:=radix) radix b t p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
      Id.run, ULift.up_down, np, Int.cast_ofNat] using
      h ⟨hpBound, hpBound, htNe, hradix, hvNum⟩
  have hnqCan : Fcanonic (beta:=radix) radix b nq := by
    have h := FnormalizeCanonic (beta:=radix) radix b t q
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
      Id.run, ULift.up_down, nq, Int.cast_ofNat] using
      h ⟨hqBound, hqBound, htNe, hradix, hvNum⟩
  have hnpNorm : Fnormalize (beta:=radix) radix b t np = np := by
    have h := FcanonicFnormalizeEq (beta:=radix) radix b t np
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hnpCan, rfl, hradix, htNe, hvNum⟩
  have hnqNorm : Fnormalize (beta:=radix) radix b t nq = nq := by
    have h := FcanonicFnormalizeEq (beta:=radix) radix b t nq
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hnqCan, rfl, hradix, htNe, hvNum⟩
  have hnpClosest :
      Closest (beta:=radix) b (radix : ℝ)
        (_root_.F2R (beta:=radix) x *
          ((radix : ℝ) ^ (s : Int) + 1)) np := by
    constructor
    · exact hnpBound
    · intro g hg
      rw [hnpValue]
      exact hpDefEven.1.2 g hg
  have hnqClosest :
      Closest (beta:=radix) b (radix : ℝ)
        (_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) np) nq := by
    constructor
    · exact hnqBound
    · intro g hg
      rw [hnpValue, hnqValue]
      exact hqDefEven.1.2 g hg
  have hnpEven :
      EvenClosest (beta:=radix) b (radix : ℝ) t
        (_root_.F2R (beta:=radix) x *
          ((radix : ℝ) ^ (s : Int) + 1)) np := by
    refine ⟨hnpClosest, ?_⟩
    rcases hpDefEven.2 with hpEven | hpUnique
    · left
      unfold FNeven at hpEven ⊢
      rw [hnpNorm]
      simpa only [np, Int.cast_ofNat] using hpEven
    · right
      intro g hg
      calc
        _root_.F2R (beta:=radix) g = _root_.F2R (beta:=radix) p :=
          hpUnique g hg
        _ = _root_.F2R (beta:=radix) np := hnpValue.symm
  have hnqEven :
      EvenClosest (beta:=radix) b (radix : ℝ) t
        (_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) np) nq := by
    refine ⟨hnqClosest, ?_⟩
    rcases hqDefEven.2 with hqEven | hqUnique
    · left
      unfold FNeven at hqEven ⊢
      rw [hnqNorm]
      simpa only [nq, Int.cast_ofNat] using hqEven
    · right
      intro g hg
      have hg' :
          Closest (beta:=radix) b (radix : ℝ)
            (_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p) g := by
        simpa only [hnpValue, Int.cast_ofNat] using hg
      calc
        _root_.F2R (beta:=radix) g = _root_.F2R (beta:=radix) q :=
          hqUnique g hg'
        _ = _root_.F2R (beta:=radix) nq := hnqValue.symm
  have hhxEven :
      EvenClosest (beta:=radix) b (radix : ℝ) t
        (_root_.F2R (beta:=radix) nq + _root_.F2R (beta:=radix) np) hx := by
    rw [hnqValue, hnpValue]
    exact hhxDefEven
  exact VeltkampEvenN_aux (beta:=radix) b radix s t x np nq hx rfl hradix
    hvNum hsGe hsLe hxNormal hnpCan hnqCan hnpEven hnqEven hhxEven

/-! Coq Veltkamp bound-extension lemma `Closestbbplus`.

Competitors newly admitted by `plusExp` have exponent below the original
minimum and magnitude below the first interior radix threshold. Inside that
threshold the exact float has an exact original-bound representative; outside
it, the threshold float on the same side is no farther from the exact value. -/
theorem Closestbbplus {beta : Int} [ValidRadix beta]
    (b0 : Fbound_skel) (radix : Int) [ValidRadix radix] (t n : Nat)
    (fext f : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b0.vNum = Zpower_nat radix n) (hn : 1 < n)
    (hfextExp : -b0.dExp ≤ fext.Fexp)
    (hfClosest : Closest (beta:=beta) b0 (radix : ℝ)
      (_root_.F2R (beta:=beta) fext) f) :
    Closest (beta:=beta) (plusExp b0 t) (radix : ℝ)
      (_root_.F2R (beta:=beta) fext) f := by
  subst beta
  have hradixPosInt : (0 : Int) < radix := by omega
  have hradixPos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradixPosInt
  have hradixOne : (1 : ℝ) ≤ (radix : ℝ) := by
    exact_mod_cast (le_of_lt hradix)
  have hradixNe : (radix : ℝ) ≠ 0 := ne_of_gt hradixPos
  have hnPredCast : ((n - 1 : Nat) : Int) = (n : Int) - 1 := by
    omega
  let mantissaThreshold : Int := Zpower_nat radix (n - 1)
  let threshold : ℝ :=
    (radix : ℝ) ^ ((n : Int) - 1 - b0.dExp)
  have hmantissaThresholdPos : 0 < mantissaThreshold := by
    dsimp [mantissaThreshold, Zpower_nat]
    exact pow_pos hradixPosInt _
  have hmantissaThresholdLt : mantissaThreshold < b0.vNum := by
    rw [hvNum]
    dsimp [mantissaThreshold, Zpower_nat]
    exact pow_lt_pow_right₀ hradix (by omega)
  have hthresholdPos : 0 < threshold := by
    exact zpow_pos hradixPos _
  let upper : FloatSpec.Core.Defs.FlocqFloat radix :=
    ⟨mantissaThreshold, -b0.dExp⟩
  let lower : FloatSpec.Core.Defs.FlocqFloat radix :=
    Fopp (beta:=radix) upper
  have hupperBound : Fbounded (beta:=radix) b0 upper := by
    constructor
    · dsimp [upper]
      rw [abs_of_pos hmantissaThresholdPos]
      exact hmantissaThresholdLt
    · simp [upper]
  have hlowerBound : Fbounded (beta:=radix) b0 lower := by
    simpa [lower, Fbounded, Fopp, FloatSpec.Calc.Operations.Fopp, abs_neg]
      using hupperBound
  have hthresholdScale :
      threshold = (mantissaThreshold : ℝ) * (radix : ℝ) ^ (-b0.dExp) := by
    dsimp [threshold, mantissaThreshold, Zpower_nat]
    rw [Int.cast_pow, ← zpow_natCast, hnPredCast]
    rw [← zpow_add₀ hradixNe]
    congr 1
  have hupperValue :
      _root_.F2R (beta:=radix) upper = threshold := by
    dsimp [upper, _root_.F2R, FloatSpec.Core.Defs.F2R]
    rw [hthresholdScale]
  have hlowerValue :
      _root_.F2R (beta:=radix) lower = -threshold := by
    have h := (FloatSpec.Calc.Operations.F2R_opp (beta:=radix) upper) trivial
    calc
      _root_.F2R (beta:=radix) lower =
          -_root_.F2R (beta:=radix) upper := by
        simpa [lower, Fopp, _root_.F2R] using h
      _ = -threshold := by rw [hupperValue]
  constructor
  · rcases hfClosest.1 with ⟨hfNum, hfExp⟩
    constructor
    · simpa [plusExp] using hfNum
    · dsimp [plusExp]
      have htNonneg : (0 : Int) ≤ ((t - 1 : Nat) : Int) :=
        Int.natCast_nonneg _
      omega
  · intro g hgPlus
    by_cases hgOldExp : -b0.dExp ≤ g.Fexp
    · apply hfClosest.2 g
      constructor
      · simpa [plusExp] using hgPlus.1
      · exact hgOldExp
    · have hgExpLe : g.Fexp ≤ -b0.dExp - 1 := by omega
      have hgNumInt : |g.Fnum| < Zpower_nat radix n := by
        simpa [plusExp, hvNum] using hgPlus.1
      have hgNumRealCast :
          ((|g.Fnum| : Int) : ℝ) < ((Zpower_nat radix n : Int) : ℝ) := by
        exact_mod_cast hgNumInt
      have hgNumReal : |(g.Fnum : ℝ)| < (radix : ℝ) ^ n := by
        simpa [Int.cast_abs, Zpower_nat, Int.cast_pow] using hgNumRealCast
      have hgPowPos : 0 < (radix : ℝ) ^ g.Fexp :=
        zpow_pos hradixPos _
      have hgPowLe :
          (radix : ℝ) ^ g.Fexp ≤
            (radix : ℝ) ^ (-b0.dExp - 1) :=
        zpow_le_zpow_right₀ hradixOne hgExpLe
      have hgAbsValue :
          |_root_.F2R (beta:=radix) g| =
            |(g.Fnum : ℝ)| * (radix : ℝ) ^ g.Fexp := by
        simp [_root_.F2R, FloatSpec.Core.Defs.F2R, abs_mul,
          abs_of_pos hgPowPos]
      have hgMagnitudeLt :
          |_root_.F2R (beta:=radix) g| < threshold := by
        rw [hgAbsValue]
        calc
          |(g.Fnum : ℝ)| * (radix : ℝ) ^ g.Fexp <
              (radix : ℝ) ^ n * (radix : ℝ) ^ g.Fexp :=
            mul_lt_mul_of_pos_right hgNumReal hgPowPos
          _ ≤ (radix : ℝ) ^ n *
              (radix : ℝ) ^ (-b0.dExp - 1) :=
            mul_le_mul_of_nonneg_left hgPowLe (by positivity)
          _ = threshold := by
            dsimp [threshold]
            rw [← zpow_natCast, ← zpow_add₀ hradixNe]
            congr 1
            ring
      let shift : Nat := (fext.Fexp + b0.dExp).toNat
      have hshiftCast : (shift : Int) = fext.Fexp + b0.dExp := by
        dsimp [shift]
        rw [Int.toNat_of_nonneg]
        omega
      let exactMantissa : Int := fext.Fnum * radix ^ shift
      let exactFloat : FloatSpec.Core.Defs.FlocqFloat radix :=
        ⟨exactMantissa, -b0.dExp⟩
      have hexactValue :
          _root_.F2R (beta:=radix) exactFloat =
            _root_.F2R (beta:=radix) fext := by
        dsimp [exactFloat, exactMantissa, _root_.F2R,
          FloatSpec.Core.Defs.F2R]
        rw [Int.cast_mul, Int.cast_pow, ← zpow_natCast]
        calc
          (fext.Fnum : ℝ) * (radix : ℝ) ^ (shift : Int) *
              (radix : ℝ) ^ (-b0.dExp) =
              (fext.Fnum : ℝ) *
                ((radix : ℝ) ^ (shift : Int) *
                  (radix : ℝ) ^ (-b0.dExp)) := by ring
          _ = (fext.Fnum : ℝ) *
                (radix : ℝ) ^ ((shift : Int) + (-b0.dExp)) := by
              rw [zpow_add₀ hradixNe]
          _ = (fext.Fnum : ℝ) * (radix : ℝ) ^ fext.Fexp := by
              rw [hshiftCast]
              congr 2
              ring
      by_cases hzInside :
          |_root_.F2R (beta:=radix) fext| < threshold
      · have hscalePos : 0 < (radix : ℝ) ^ (-b0.dExp) :=
          zpow_pos hradixPos _
        have hexactAbs :
            |_root_.F2R (beta:=radix) exactFloat| =
              |(exactMantissa : ℝ)| * (radix : ℝ) ^ (-b0.dExp) := by
          dsimp [exactFloat, _root_.F2R, FloatSpec.Core.Defs.F2R]
          rw [abs_mul, abs_of_pos hscalePos]
        have hexactMantissaRealLt :
            |(exactMantissa : ℝ)| < (mantissaThreshold : ℝ) := by
          rw [← hexactValue] at hzInside
          rw [hexactAbs, hthresholdScale] at hzInside
          exact lt_of_mul_lt_mul_right hzInside (le_of_lt hscalePos)
        have hexactMantissaIntLt :
            |exactMantissa| < mantissaThreshold := by
          exact_mod_cast (show
            ((|exactMantissa| : Int) : ℝ) < (mantissaThreshold : ℝ) by
              simpa [Int.cast_abs] using hexactMantissaRealLt)
        have hexactBound : Fbounded (beta:=radix) b0 exactFloat := by
          constructor
          · dsimp [exactFloat]
            exact lt_trans hexactMantissaIntLt hmantissaThresholdLt
          · simp [exactFloat]
        have hzeroError := hfClosest.2 exactFloat hexactBound
        rw [hexactValue, sub_self, abs_zero] at hzeroError
        exact le_trans hzeroError (abs_nonneg _)
      · have hthresholdLeAbs :
            threshold ≤ |_root_.F2R (beta:=radix) fext| :=
          le_of_not_gt hzInside
        by_cases hzNonneg : 0 ≤ _root_.F2R (beta:=radix) fext
        · have hthresholdLeZ :
              threshold ≤ _root_.F2R (beta:=radix) fext := by
            simpa [abs_of_nonneg hzNonneg] using hthresholdLeAbs
          have hgLtZ :
              _root_.F2R (beta:=radix) g <
                _root_.F2R (beta:=radix) fext :=
            lt_of_le_of_lt (le_abs_self _) (lt_of_lt_of_le
              hgMagnitudeLt hthresholdLeZ)
          have hgLtThreshold :
              _root_.F2R (beta:=radix) g < threshold :=
            lt_of_le_of_lt (le_abs_self _) hgMagnitudeLt
          have hupperErrorLe :
              |_root_.F2R (beta:=radix) upper -
                  _root_.F2R (beta:=radix) fext| ≤
                |_root_.F2R (beta:=radix) g -
                  _root_.F2R (beta:=radix) fext| := by
            rw [hupperValue, abs_of_nonpos (by linarith),
              abs_of_nonpos (by linarith)]
            linarith
          exact le_trans (hfClosest.2 upper hupperBound) hupperErrorLe
        · have hzNeg : _root_.F2R (beta:=radix) fext < 0 :=
            lt_of_not_ge hzNonneg
          have hzLeNegThreshold :
              _root_.F2R (beta:=radix) fext ≤ -threshold := by
            rw [abs_of_neg hzNeg] at hthresholdLeAbs
            linarith
          have hzLtG :
              _root_.F2R (beta:=radix) fext <
                _root_.F2R (beta:=radix) g := by
            have hnegAbsLeG :
                -|_root_.F2R (beta:=radix) g| ≤
                  _root_.F2R (beta:=radix) g := neg_abs_le _
            linarith
          have hlowerErrorLe :
              |_root_.F2R (beta:=radix) lower -
                  _root_.F2R (beta:=radix) fext| ≤
                |_root_.F2R (beta:=radix) g -
                  _root_.F2R (beta:=radix) fext| := by
            rw [hlowerValue, abs_of_nonneg (by linarith),
              abs_of_nonneg (by linarith)]
            have hnegThresholdLeG :
                -threshold ≤ _root_.F2R (beta:=radix) g := by
              have hnegAbsLeG :
                  -|_root_.F2R (beta:=radix) g| ≤
                    _root_.F2R (beta:=radix) g := neg_abs_le _
              linarith
            linarith
          exact le_trans (hfClosest.2 lower hlowerBound) hlowerErrorLe

/-! Coq nearest-even bound-restriction lemma `EvenClosestbplusb`.

When normalization under the original bound is normal, canonical uniqueness
transfers normalized parity from `plusExp`. In the subnormal case, the rounding
error is an integer multiple of the minimum radix unit, while `ClosestUlp`
bounds twice that error by one unit, forcing exactness and uniqueness. -/
theorem EvenClosestbplusb {beta : Int} [ValidRadix beta]
    (b0 : Fbound_skel) (radix : Int) [ValidRadix radix] (t n : Nat)
    (fext f : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b0.vNum = Zpower_nat radix n) (hn : 1 < n)
    (hfextExp : -b0.dExp ≤ fext.Fexp)
    (hEvenPlus : EvenClosest (beta:=beta) (plusExp b0 t) (radix : ℝ) n
      (_root_.F2R (beta:=beta) fext) f)
    (hfBound : Fbounded (beta:=beta) b0 f) :
    EvenClosest (beta:=beta) b0 (radix : ℝ) n
      (_root_.F2R (beta:=beta) fext) f := by
  subst beta
  have hnNe : n ≠ 0 := by omega
  have hradixPosInt : (0 : Int) < radix := by omega
  have hradixPos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradixPosInt
  have hradixNe : (radix : ℝ) ≠ 0 := ne_of_gt hradixPos
  have hvNumPlus : (plusExp b0 t).vNum = Zpower_nat radix n := by
    simpa [plusExp] using hvNum
  have hClosest :
      Closest (beta:=radix) b0 (radix : ℝ)
        (_root_.F2R (beta:=radix) fext) f :=
    Closestbplusb b0 (radix : ℝ) t
      (_root_.F2R (beta:=radix) fext) f hEvenPlus.1 hfBound
  let nf : FloatSpec.Core.Defs.FlocqFloat radix :=
    Fnormalize (beta:=radix) radix b0 n f
  have hnfCan : Fcanonic (beta:=radix) radix b0 nf := by
    have h := FnormalizeCanonic (beta:=radix) radix b0 n f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
      Id.run, ULift.up_down, nf, Int.cast_ofNat] using
      h ⟨hfBound, hfBound, hnNe, hradix, hvNum⟩
  have hnfValue :
      _root_.F2R (beta:=radix) nf = _root_.F2R (beta:=radix) f := by
    have h := FnormalizeCorrect (beta:=radix) radix b0 n f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
      Id.run, ULift.up_down, nf, Int.cast_ofNat] using h ⟨rfl, hradix⟩
  refine ⟨hClosest, ?_⟩
  rcases hnfCan with hnfNormal | hnfSubnormal
  · rcases hEvenPlus.2 with hEven | hUnique
    · left
      let nfPlus : FloatSpec.Core.Defs.FlocqFloat radix :=
        Fnormalize (beta:=radix) radix (plusExp b0 t) n f
      have hnfNormalPlus :
          Fnormal (beta:=radix) radix (plusExp b0 t) nf := by
        constructor
        · rcases hnfNormal.1 with ⟨hnum, hexp⟩
          constructor
          · simpa [plusExp] using hnum
          · dsimp [plusExp]
            have htNonneg : (0 : Int) ≤ ((t - 1 : Nat) : Int) :=
              Int.natCast_nonneg _
            omega
        · simpa [plusExp] using hnfNormal.2
      have hnfPlusCan :
          Fcanonic (beta:=radix) radix (plusExp b0 t) nfPlus := by
        have h := FnormalizeCanonic (beta:=radix) radix (plusExp b0 t) n f
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
          Id.run, ULift.up_down, nfPlus, Int.cast_ofNat] using
          h ⟨hEvenPlus.1.1, hEvenPlus.1.1, hnNe, hradix, hvNumPlus⟩
      have hnfPlusValue :
          _root_.F2R (beta:=radix) nfPlus =
            _root_.F2R (beta:=radix) f := by
        have h := FnormalizeCorrect (beta:=radix) radix (plusExp b0 t) n f
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
          Id.run, ULift.up_down, nfPlus, Int.cast_ofNat] using h ⟨rfl, hradix⟩
      have hnfEq : nfPlus = nf := by
        have h := FcanonicUnique (beta:=radix) radix (plusExp b0 t)
          nfPlus nf hradix rfl
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicUnique_check,
          ULift.down_up, Fcanonic', Int.cast_ofNat] using
          h ⟨hnfPlusCan, Or.inl hnfNormalPlus,
            hnfPlusValue.trans hnfValue.symm⟩
      unfold FNeven at hEven ⊢
      simpa only [nfPlus, nf, hnfEq, Int.cast_ofNat] using hEven
    · right
      intro q hq
      have hqPlus :
          Closest (beta:=radix) (plusExp b0 t) (radix : ℝ)
            (_root_.F2R (beta:=radix) fext) q :=
        Closestbbplus b0 radix t n fext q rfl hradix hvNum hn
          hfextExp hq
      exact hUnique q hqPlus
  · right
    intro q hq
    have hdeltaPos : 0 < (radix : ℝ) ^ (-b0.dExp) :=
      zpow_pos hradixPos _
    have herrorUlp :
        2 * |_root_.F2R (beta:=radix) fext -
            _root_.F2R (beta:=radix) f| ≤
          Fulp (beta:=radix) b0 radix n f := by
      have h := ClosestUlp (beta:=radix) b0 radix n
        (_root_.F2R (beta:=radix) fext) f
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hClosest, rfl, hradix, hnNe, hvNum⟩
    have herrorLe :
        2 * |_root_.F2R (beta:=radix) fext -
            _root_.F2R (beta:=radix) f| ≤
          (radix : ℝ) ^ (-b0.dExp) := by
      simpa [Fulp, nf, hnfSubnormal.2.1] using herrorUlp
    let shift : Nat := (fext.Fexp + b0.dExp).toNat
    have hshiftCast : (shift : Int) = fext.Fexp + b0.dExp := by
      dsimp [shift]
      rw [Int.toNat_of_nonneg]
      omega
    let exactMantissa : Int := fext.Fnum * radix ^ shift
    have hfextScale :
        _root_.F2R (beta:=radix) fext =
          (exactMantissa : ℝ) * (radix : ℝ) ^ (-b0.dExp) := by
      dsimp [exactMantissa, _root_.F2R, FloatSpec.Core.Defs.F2R]
      rw [Int.cast_mul, Int.cast_pow, ← zpow_natCast]
      calc
        (fext.Fnum : ℝ) * (radix : ℝ) ^ fext.Fexp =
            (fext.Fnum : ℝ) *
              (radix : ℝ) ^ ((shift : Int) + (-b0.dExp)) := by
          rw [hshiftCast]
          congr 2
          ring
        _ = (fext.Fnum : ℝ) *
              ((radix : ℝ) ^ (shift : Int) *
                (radix : ℝ) ^ (-b0.dExp)) := by
          rw [zpow_add₀ hradixNe]
        _ = ((fext.Fnum : ℝ) * (radix : ℝ) ^ (shift : Int)) *
              (radix : ℝ) ^ (-b0.dExp) := by ring
    have hfScale :
        _root_.F2R (beta:=radix) f =
          (nf.Fnum : ℝ) * (radix : ℝ) ^ (-b0.dExp) := by
      rw [← hnfValue]
      simp [_root_.F2R, FloatSpec.Core.Defs.F2R, hnfSubnormal.2.1]
    have herrorScale :
        |_root_.F2R (beta:=radix) fext -
            _root_.F2R (beta:=radix) f| =
          |((exactMantissa - nf.Fnum : Int) : ℝ)| *
            (radix : ℝ) ^ (-b0.dExp) := by
      rw [hfextScale, hfScale]
      have hfactor :
          (exactMantissa : ℝ) * (radix : ℝ) ^ (-b0.dExp) -
              (nf.Fnum : ℝ) * (radix : ℝ) ^ (-b0.dExp) =
            ((exactMantissa - nf.Fnum : Int) : ℝ) *
              (radix : ℝ) ^ (-b0.dExp) := by
        push_cast
        ring
      rw [hfactor, abs_mul, abs_of_pos hdeltaPos]
    have hdiffZero : exactMantissa - nf.Fnum = 0 := by
      by_contra hdiffNe
      have hdiffInt : (1 : Int) ≤ |exactMantissa - nf.Fnum| :=
        Int.one_le_abs hdiffNe
      have hdiffReal :
          (1 : ℝ) ≤ |((exactMantissa - nf.Fnum : Int) : ℝ)| := by
        exact_mod_cast (show
          (1 : Int) ≤ |exactMantissa - nf.Fnum| from hdiffInt)
      have hdeltaLeError :
          (radix : ℝ) ^ (-b0.dExp) ≤
            |_root_.F2R (beta:=radix) fext -
              _root_.F2R (beta:=radix) f| := by
        rw [herrorScale]
        simpa using mul_le_mul_of_nonneg_right hdiffReal
          (le_of_lt hdeltaPos)
      nlinarith
    have hfExact :
        _root_.F2R (beta:=radix) f =
          _root_.F2R (beta:=radix) fext := by
      rw [hfScale, hfextScale]
      have hmantissaEq : exactMantissa = nf.Fnum := by omega
      rw [hmantissaEq]
    have hqError := hq.2 f hfBound
    rw [hfExact, sub_self, abs_zero] at hqError
    have hqDiffZero :
        _root_.F2R (beta:=radix) q -
            _root_.F2R (beta:=radix) fext = 0 := by
      exact abs_eq_zero.mp
        (le_antisymm hqError (abs_nonneg _))
    linarith

private lemma FSucc_exp_le_add_one {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    (FSucc (beta:=beta) b radix precision x).Fexp ≤ x.Fexp + 1 := by
  unfold FSucc
  split_ifs <;> simp <;> omega

/-! Coq closest-exponent separation lemma `ClosestClosest`.

After taking absolute values, normalize the lower-exponent result and place its
normalized successor strictly between the two closest values. That successor is
bounded, so one of the two original closestness inequalities is contradicted,
depending on which side of the successor contains the exact value. -/
theorem ClosestClosest {beta : Int} [ValidRadix beta]
    (b0 : Fbound_skel) (radix : Int) [ValidRadix radix] (n : Nat) (z : ℝ)
    (f1 f2 : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b0.vNum = Zpower_nat radix n) (hn : 1 < n)
    (hf1 : Closest (beta:=beta) b0 (radix : ℝ) z f1)
    (hf2 : Closest (beta:=beta) b0 (radix : ℝ) z f2)
    (hf2Normal : Fnormal (beta:=beta) radix b0 f2)
    (hExp : f1.Fexp ≤ f2.Fexp - 2) : False := by
  subst beta
  have hnNe : n ≠ 0 := by omega
  have hradixPosInt : (0 : Int) < radix := by omega
  have hvNumPos : 0 < b0.vNum := by
    rw [hvNum, Zpower_nat]
    exact pow_pos hradixPosInt _
  have hf1Abs :
      Closest (beta:=radix) b0 (radix : ℝ) |z| (Fabs f1) := by
    have h := ClosestFabs (beta:=radix) b0 (radix : ℝ) f1 z
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestFabs_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hradix, hf1⟩
  have hf2Abs :
      Closest (beta:=radix) b0 (radix : ℝ) |z| (Fabs f2) := by
    have h := ClosestFabs (beta:=radix) b0 (radix : ℝ) f2 z
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestFabs_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hradix, hf2⟩
  let nf1 : FloatSpec.Core.Defs.FlocqFloat radix :=
    Fnormalize (beta:=radix) radix b0 n (Fabs f1)
  let af2 : FloatSpec.Core.Defs.FlocqFloat radix := Fabs f2
  have hnf1Can : Fcanonic (beta:=radix) radix b0 nf1 := by
    have h := FnormalizeCanonic (beta:=radix) radix b0 n (Fabs f1)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
      Id.run, ULift.up_down, nf1, Int.cast_ofNat] using
      h ⟨hf1Abs.1, hf1Abs.1, hnNe, hradix, hvNum⟩
  have hnf1Value :
      _root_.F2R (beta:=radix) nf1 =
        _root_.F2R (beta:=radix) (Fabs f1) := by
    have h := FnormalizeCorrect (beta:=radix) radix b0 n (Fabs f1)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
      Id.run, ULift.up_down, nf1, Int.cast_ofNat] using h ⟨rfl, hradix⟩
  have haf2Normal : Fnormal (beta:=radix) radix b0 af2 := by
    constructor
    · have h := absFBounded (beta:=radix) b0 f2
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, absFBounded_check,
        Id.run, ULift.up_down, af2, Int.cast_ofNat] using h hf2Normal.1
    · simpa [af2, Fabs, FloatSpec.Calc.Operations.Fabs,
        Int.natCast_natAbs, abs_mul] using hf2Normal.2
  have haf2Can : Fcanonic (beta:=radix) radix b0 af2 :=
    Or.inl haf2Normal
  have hnf1Nonneg : 0 ≤ _root_.F2R (beta:=radix) nf1 := by
    rw [hnf1Value]
    have h := FloatSpec.Calc.Operations.F2R_abs (beta:=radix) f1 hradix
    rw [show _root_.F2R (beta:=radix) (Fabs f1) =
        |_root_.F2R (beta:=radix) f1| by simpa [Fabs, _root_.F2R] using h]
    exact abs_nonneg _
  have haf2Nonneg : 0 ≤ _root_.F2R (beta:=radix) af2 := by
    have h := FloatSpec.Calc.Operations.F2R_abs (beta:=radix) f2 hradix
    rw [show _root_.F2R (beta:=radix) af2 =
        |_root_.F2R (beta:=radix) f2| by
          simpa [af2, Fabs, _root_.F2R] using h]
    exact abs_nonneg _
  have hnf1ExpLe : nf1.Fexp ≤ (Fabs f1).Fexp := by
    have h := FcanonicLeastExp_from_explicit_radix_payload (beta:=radix) radix b0 (Fabs f1) nf1
      hradix rfl hvNumPos
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicLeastExp_check,
      ULift.down_up, Fbounded', Fcanonic', nf1, Int.cast_ofNat] using
      h ⟨hnf1Value.symm, hf1Abs.1, hnf1Can⟩
  have hnf1ExpGap : nf1.Fexp ≤ af2.Fexp - 2 := by
    have hfabs1Exp : (Fabs f1).Fexp = f1.Fexp := by
      simp [Fabs, FloatSpec.Calc.Operations.Fabs]
    have hfabs2Exp : af2.Fexp = f2.Fexp := by
      simp [af2, Fabs, FloatSpec.Calc.Operations.Fabs]
    rw [hfabs1Exp] at hnf1ExpLe
    rw [hfabs2Exp]
    omega
  let succ : FloatSpec.Core.Defs.FlocqFloat radix :=
    FNSucc (beta:=radix) b0 (radix : ℝ) n (Fabs f1)
  have hsuccCan : Fcanonic (beta:=radix) radix b0 succ := by
    have h := FNSuccCanonic (beta:=radix) b0 (radix : ℝ) n (Fabs f1)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FNSuccCanonic_check,
      Id.run, ULift.up_down, succ, Int.cast_ofNat] using
      h ⟨hf1Abs.1, hradix, hnNe, hvNum⟩
  have hsuccBound : Fbounded (beta:=radix) b0 succ :=
    (FcanonicBound (beta:=radix) radix b0 succ) hsuccCan
  have hnf1LtSucc :
      _root_.F2R (beta:=radix) nf1 <
        _root_.F2R (beta:=radix) succ := by
    have h := FNSuccLt (beta:=radix) b0 (radix : ℝ) n (Fabs f1)
    have h' :
        _root_.F2R (beta:=radix) (Fabs f1) <
          _root_.F2R (beta:=radix) succ := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FNSuccLt_check,
        Id.run, ULift.up_down, succ, Int.cast_ofNat] using h ⟨hradix, hnNe, hvNum⟩
    simpa [hnf1Value] using h'
  have hsuccNonneg : 0 ≤ _root_.F2R (beta:=radix) succ :=
    le_trans hnf1Nonneg (le_of_lt hnf1LtSucc)
  have hsuccExpLe : succ.Fexp ≤ nf1.Fexp + 1 := by
    simpa [succ, FNSucc, nf1] using
      (FSucc_exp_le_add_one (beta:=radix) b0 radix n nf1)
  have hsuccExpLt : succ.Fexp < af2.Fexp := by omega
  have hsuccLtAf2 :
      _root_.F2R (beta:=radix) succ <
        _root_.F2R (beta:=radix) af2 := by
    have h := FcanonicPosFexpRlt (beta:=radix) radix b0 succ af2 hradix rfl
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicPosFexpRlt_check,
      ULift.down_up, Fcanonic', Int.cast_ofNat] using
      h ⟨hsuccCan, haf2Can, hsuccNonneg, haf2Nonneg, hsuccExpLt⟩
  have hf1Dist := hf1Abs.2 succ hsuccBound
  have hf2Dist := hf2Abs.2 succ hsuccBound
  by_cases hzLe : |z| ≤ _root_.F2R (beta:=radix) succ
  · have hsuccDiffNonneg :
        0 ≤ _root_.F2R (beta:=radix) succ - |z| := by linarith
    have haf2DiffNonneg :
        0 ≤ _root_.F2R (beta:=radix) af2 - |z| := by linarith
    rw [abs_of_nonneg haf2DiffNonneg, abs_of_nonneg hsuccDiffNonneg] at hf2Dist
    exact (not_le_of_gt hsuccLtAf2) (by linarith)
  · have hzGt : _root_.F2R (beta:=radix) succ < |z| := lt_of_not_ge hzLe
    have hnf1DiffNonpos :
        _root_.F2R (beta:=radix) nf1 - |z| ≤ 0 := by linarith
    have hsuccDiffNonpos :
        _root_.F2R (beta:=radix) succ - |z| ≤ 0 := by linarith
    rw [← hnf1Value] at hf1Dist
    rw [abs_of_nonpos hnf1DiffNonpos, abs_of_nonpos hsuccDiffNonpos] at hf1Dist
    exact (not_le_of_gt hnf1LtSucc) (by linarith)

/-! Coq nearest-even bound-extension lemma `EvenClosestbbplus`.

Above the original minimum exponent, parity and uniqueness transfer to the
enlarged bound; a newly admitted lower-exponent competitor is excluded by
`ClosestClosest`. At the original minimum exponent, the exact input and rounded
result lie on the same minimum-unit lattice, so the closestness bound forces an
exact result and hence uniqueness under the enlarged bound. -/
theorem EvenClosestbbplus {beta : Int} [ValidRadix beta]
    (b0 : Fbound_skel) (radix : Int) [ValidRadix radix] (t n : Nat)
    (fext f : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b0.vNum = Zpower_nat radix n) (hn : 1 < n)
    (hfextExp : -b0.dExp ≤ fext.Fexp)
    (hEven0 : EvenClosest (beta:=beta) b0 (radix : ℝ) n
      (_root_.F2R (beta:=beta) fext) f) :
    EvenClosest (beta:=beta) (plusExp b0 t) (radix : ℝ) n
      (_root_.F2R (beta:=beta) fext) f := by
  subst beta
  have hnNe : n ≠ 0 := by omega
  have hradixPosInt : (0 : Int) < radix := by omega
  have hradixPos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradixPosInt
  have hradixNe : (radix : ℝ) ≠ 0 := ne_of_gt hradixPos
  have hvNumPlus : (plusExp b0 t).vNum = Zpower_nat radix n := by
    simpa [plusExp] using hvNum
  have hClosest0 := hEven0.1
  have hClosestPlus :
      Closest (beta:=radix) (plusExp b0 t) (radix : ℝ)
        (_root_.F2R (beta:=radix) fext) f :=
    Closestbbplus b0 radix t n fext f rfl hradix hvNum hn
      hfextExp hClosest0
  let nf : FloatSpec.Core.Defs.FlocqFloat radix :=
    Fnormalize (beta:=radix) radix b0 n f
  have hnfBound0 : Fbounded (beta:=radix) b0 nf := by
    have h := FnormalizeBounded (beta:=radix) radix b0 n f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeBounded_check,
      Id.run, ULift.up_down, Fbounded', nf, Int.cast_ofNat] using
      h ⟨hClosest0.1, hClosest0.1, hnNe, hradix, hvNum⟩
  have hnfCan0 : Fcanonic (beta:=radix) radix b0 nf := by
    have h := FnormalizeCanonic (beta:=radix) radix b0 n f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
      Id.run, ULift.up_down, nf, Int.cast_ofNat] using
      h ⟨hClosest0.1, hClosest0.1, hnNe, hradix, hvNum⟩
  have hnfValue :
      _root_.F2R (beta:=radix) nf = _root_.F2R (beta:=radix) f := by
    have h := FnormalizeCorrect (beta:=radix) radix b0 n f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
      Id.run, ULift.up_down, nf, Int.cast_ofNat] using h ⟨rfl, hradix⟩
  have hnfBoundPlus : Fbounded (beta:=radix) (plusExp b0 t) nf := by
    rcases hnfBound0 with ⟨hnum, hexp⟩
    constructor
    · simpa [plusExp] using hnum
    · dsimp [plusExp]
      have htNonneg : (0 : Int) ≤ ((t - 1 : Nat) : Int) :=
        Int.natCast_nonneg _
      omega
  have hnfClosestPlus :
      Closest (beta:=radix) (plusExp b0 t) (radix : ℝ)
        (_root_.F2R (beta:=radix) fext) nf := by
    constructor
    · exact hnfBoundPlus
    · intro g hg
      simpa [hnfValue] using hClosestPlus.2 g hg
  refine ⟨hClosestPlus, ?_⟩
  by_cases hnfAbove : -b0.dExp < nf.Fexp
  · have hnfNormal0 : Fnormal (beta:=radix) radix b0 nf := by
      rcases hnfCan0 with hnormal | hsubnormal
      · exact hnormal
      · exfalso
        have hnfAtMin := hsubnormal.2.1
        omega
    have hnfNormalPlus :
        Fnormal (beta:=radix) radix (plusExp b0 t) nf := by
      constructor
      · exact hnfBoundPlus
      · simpa [plusExp] using hnfNormal0.2
    rcases hEven0.2 with hEven | hUnique
    · left
      let nfPlus : FloatSpec.Core.Defs.FlocqFloat radix :=
        Fnormalize (beta:=radix) radix (plusExp b0 t) n f
      have hnfPlusCan :
          Fcanonic (beta:=radix) radix (plusExp b0 t) nfPlus := by
        have h := FnormalizeCanonic (beta:=radix) radix (plusExp b0 t) n f
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
          Id.run, ULift.up_down, nfPlus, Int.cast_ofNat] using
          h ⟨hClosestPlus.1, hClosestPlus.1, hnNe, hradix, hvNumPlus⟩
      have hnfPlusValue :
          _root_.F2R (beta:=radix) nfPlus =
            _root_.F2R (beta:=radix) f := by
        have h := FnormalizeCorrect (beta:=radix) radix (plusExp b0 t) n f
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
          Id.run, ULift.up_down, nfPlus, Int.cast_ofNat] using h ⟨rfl, hradix⟩
      have hnfEq : nfPlus = nf := by
        have h := FcanonicUnique (beta:=radix) radix (plusExp b0 t)
          nfPlus nf hradix rfl
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicUnique_check,
          ULift.down_up, Fcanonic', Int.cast_ofNat] using
          h ⟨hnfPlusCan, Or.inl hnfNormalPlus,
            hnfPlusValue.trans hnfValue.symm⟩
      unfold FNeven at hEven ⊢
      simpa only [nfPlus, nf, hnfEq, Int.cast_ofNat] using hEven
    · right
      intro q hqPlus
      by_cases hqExp : -b0.dExp ≤ q.Fexp
      · have hqBound0 : Fbounded (beta:=radix) b0 q := by
          exact ⟨by simpa [plusExp] using hqPlus.1.1, hqExp⟩
        have hq0 :
            Closest (beta:=radix) b0 (radix : ℝ)
              (_root_.F2R (beta:=radix) fext) q :=
          Closestbplusb b0 (radix : ℝ) t
            (_root_.F2R (beta:=radix) fext) q hqPlus hqBound0
        exact hUnique q hq0
      · have hqGap : q.Fexp ≤ nf.Fexp - 2 := by omega
        exact False.elim (ClosestClosest (beta:=radix)
          (plusExp b0 t) radix n
          (_root_.F2R (beta:=radix) fext) q nf rfl hradix
          hvNumPlus hn hqPlus hnfClosestPlus hnfNormalPlus hqGap)
  · have hnfExpEq : nf.Fexp = -b0.dExp := by
      have hnfExpBound := hnfBound0.2
      omega
    right
    intro q hqPlus
    have herrorUlp :
        2 * |_root_.F2R (beta:=radix) fext -
            _root_.F2R (beta:=radix) f| ≤
          Fulp (beta:=radix) b0 radix n f := by
      have h := ClosestUlp (beta:=radix) b0 radix n
        (_root_.F2R (beta:=radix) fext) f
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hClosest0, rfl, hradix, hnNe, hvNum⟩
    have herrorLe :
        2 * |_root_.F2R (beta:=radix) fext -
            _root_.F2R (beta:=radix) f| ≤
          (radix : ℝ) ^ (-b0.dExp) := by
      simpa [Fulp, nf, hnfExpEq] using herrorUlp
    let shift : Nat := (fext.Fexp + b0.dExp).toNat
    have hshiftCast : (shift : Int) = fext.Fexp + b0.dExp := by
      dsimp [shift]
      rw [Int.toNat_of_nonneg]
      omega
    let exactMantissa : Int := fext.Fnum * radix ^ shift
    have hfextScale :
        _root_.F2R (beta:=radix) fext =
          (exactMantissa : ℝ) * (radix : ℝ) ^ (-b0.dExp) := by
      dsimp [exactMantissa, _root_.F2R, FloatSpec.Core.Defs.F2R]
      rw [Int.cast_mul, Int.cast_pow, ← zpow_natCast]
      calc
        (fext.Fnum : ℝ) * (radix : ℝ) ^ fext.Fexp =
            (fext.Fnum : ℝ) *
              (radix : ℝ) ^ ((shift : Int) + (-b0.dExp)) := by
          rw [hshiftCast]
          congr 2
          ring
        _ = (fext.Fnum : ℝ) *
              ((radix : ℝ) ^ (shift : Int) *
                (radix : ℝ) ^ (-b0.dExp)) := by
          rw [zpow_add₀ hradixNe]
        _ = ((fext.Fnum : ℝ) * (radix : ℝ) ^ (shift : Int)) *
              (radix : ℝ) ^ (-b0.dExp) := by ring
    have hfScale :
        _root_.F2R (beta:=radix) f =
          (nf.Fnum : ℝ) * (radix : ℝ) ^ (-b0.dExp) := by
      rw [← hnfValue]
      simp [_root_.F2R, FloatSpec.Core.Defs.F2R, hnfExpEq]
    have hdeltaPos : 0 < (radix : ℝ) ^ (-b0.dExp) :=
      zpow_pos hradixPos _
    have herrorScale :
        |_root_.F2R (beta:=radix) fext -
            _root_.F2R (beta:=radix) f| =
          |((exactMantissa - nf.Fnum : Int) : ℝ)| *
            (radix : ℝ) ^ (-b0.dExp) := by
      rw [hfextScale, hfScale]
      have hfactor :
          (exactMantissa : ℝ) * (radix : ℝ) ^ (-b0.dExp) -
              (nf.Fnum : ℝ) * (radix : ℝ) ^ (-b0.dExp) =
            ((exactMantissa - nf.Fnum : Int) : ℝ) *
              (radix : ℝ) ^ (-b0.dExp) := by
        push_cast
        ring
      rw [hfactor, abs_mul, abs_of_pos hdeltaPos]
    have hdiffZero : exactMantissa - nf.Fnum = 0 := by
      by_contra hdiffNe
      have hdiffInt : (1 : Int) ≤ |exactMantissa - nf.Fnum| :=
        Int.one_le_abs hdiffNe
      have hdiffReal :
          (1 : ℝ) ≤ |((exactMantissa - nf.Fnum : Int) : ℝ)| := by
        exact_mod_cast (show
          (1 : Int) ≤ |exactMantissa - nf.Fnum| from hdiffInt)
      have hdeltaLeError :
          (radix : ℝ) ^ (-b0.dExp) ≤
            |_root_.F2R (beta:=radix) fext -
              _root_.F2R (beta:=radix) f| := by
        rw [herrorScale]
        simpa using mul_le_mul_of_nonneg_right hdiffReal
          (le_of_lt hdeltaPos)
      nlinarith
    have hfExact :
        _root_.F2R (beta:=radix) f =
          _root_.F2R (beta:=radix) fext := by
      rw [hfScale, hfextScale]
      have hmantissaEq : exactMantissa = nf.Fnum := by omega
      rw [hmantissaEq]
    have hqError := hqPlus.2 f hClosestPlus.1
    rw [hfExact, sub_self, abs_zero] at hqError
    have hqDiffZero :
        _root_.F2R (beta:=radix) q -
            _root_.F2R (beta:=radix) fext = 0 := by
      exact abs_eq_zero.mp
        (le_antisymm hqError (abs_nonneg _))
    linarith

private lemma Fplus_exp_eq_min {beta : Int} [ValidRadix beta]
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) :
    (Fplus (beta:=beta) x y).Fexp = min x.Fexp y.Fexp := by
  cases x with
  | mk xm xe =>
    cases y with
    | mk ym ye =>
      by_cases hle : xe ≤ ye
      · simp [Fplus, FloatSpec.Calc.Operations.Fplus,
          FloatSpec.Calc.Operations.Falign, hle, min_eq_left hle]
      · have hge : ye ≤ xe := le_of_lt (lt_of_not_ge hle)
        simp [Fplus, FloatSpec.Calc.Operations.Fplus,
          FloatSpec.Calc.Operations.Falign, hle, min_eq_right hge]

private lemma Fminus_exp_eq_min {beta : Int} [ValidRadix beta]
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) :
    (Fminus (beta:=beta) x y).Fexp = min x.Fexp y.Fexp := by
  unfold Fminus FloatSpec.Calc.Operations.Fminus
  simpa [Fopp, FloatSpec.Calc.Operations.Fopp] using
    (Fplus_exp_eq_min x (Fopp (beta:=beta) y))

/-! Coq subnormal Veltkamp lemma `VeltkampS`.

Normalize a nonzero subnormal input under the enlarged exponent bound, apply
`VeltkampN`, and then move the reduced-bound closest witness back to the
original exponent range. The zero case is represented directly. -/
theorem VeltkampS {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x p q hx : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hsGe : 2 ≤ s) (hsLe : s ≤ t - 2)
    (hxSub : Fsubnormal (beta:=beta) radix b x)
    (hpDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)) p)
    (hqDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hhxDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) q + _root_.F2R (beta:=beta) p) hx) :
    |_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) hx| ≤
        (radix : ℝ) ^ ((s : Int) + x.Fexp) / 2 ∧
      ∃ hx' : FloatSpec.Core.Defs.FlocqFloat beta,
        _root_.F2R (beta:=beta) hx' = _root_.F2R (beta:=beta) hx ∧
          Closest (beta:=beta) (Veltkamp_reducedBound radix b s t)
            (radix : ℝ) (_root_.F2R (beta:=beta) x) hx' := by
  subst beta
  have htGtOne : 1 < t := by omega
  have htNe : t ≠ 0 := by omega
  have hredPrecNe : t - s ≠ 0 := by omega
  have hradixPosInt : (0 : Int) < radix := by omega
  have hradixPos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradixPosInt
  have hradixGeOne : (1 : ℝ) ≤ (radix : ℝ) := by
    exact_mod_cast (show (1 : Int) ≤ radix from le_of_lt hradix)
  have hvNumPos : 0 < b.vNum := by
    rw [hvNum, Zpower_nat]
    exact pow_pos hradixPosInt _
  let bRed : Fbound_skel := Veltkamp_reducedBound radix b s t
  let bPlus : Fbound_skel := plusExp b t
  let bRedPlus : Fbound_skel := Veltkamp_reducedBound radix bPlus s t
  have hvNumPlus : bPlus.vNum = Zpower_nat radix t := by
    simpa [bPlus, plusExp] using hvNum
  have hvNumRed : bRed.vNum = Zpower_nat radix (t - s) := by
    simp [bRed, Veltkamp_reducedBound]
  have hvNumRedPlus : bRedPlus.vNum = Zpower_nat radix (t - s) := by
    simp [bRedPlus, bPlus, Veltkamp_reducedBound]
  have hvNumRedPos : 0 < bRed.vNum := by
    rw [hvNumRed, Zpower_nat]
    exact pow_pos hradixPosInt _
  have hvNumRedPlusGt : 1 < bRedPlus.vNum := by
    rw [hvNumRedPlus, Zpower_nat]
    exact one_lt_pow₀ hradix hredPrecNe
  by_cases hxZero : _root_.F2R (beta:=radix) x = 0
  · let z0 : FloatSpec.Core.Defs.FlocqFloat radix := ⟨0, -b.dExp⟩
    have hz0Bound : Fbounded (beta:=radix) b z0 := by
      exact ⟨by simpa [z0] using hvNumPos, by simp [z0]⟩
    have hz0Value : _root_.F2R (beta:=radix) z0 = 0 := by
      simp [z0, _root_.F2R, FloatSpec.Core.Defs.F2R]
    have hpError := hpDef.2 z0 hz0Bound
    have hpZero : _root_.F2R (beta:=radix) p = 0 := by
      have hle : |_root_.F2R (beta:=radix) p| ≤ 0 := by
        simpa [hxZero, hz0Value] using hpError
      exact abs_eq_zero.mp (le_antisymm hle (abs_nonneg _))
    have hqError := hqDef.2 z0 hz0Bound
    have hqZero : _root_.F2R (beta:=radix) q = 0 := by
      have hle : |_root_.F2R (beta:=radix) q| ≤ 0 := by
        simpa [hxZero, hpZero, hz0Value] using hqError
      exact abs_eq_zero.mp (le_antisymm hle (abs_nonneg _))
    have hhxError := hhxDef.2 z0 hz0Bound
    have hhxZero : _root_.F2R (beta:=radix) hx = 0 := by
      have hle : |_root_.F2R (beta:=radix) hx| ≤ 0 := by
        simpa [hqZero, hpZero, hz0Value] using hhxError
      exact abs_eq_zero.mp (le_antisymm hle (abs_nonneg _))
    constructor
    · rw [hxZero, hhxZero, sub_self, abs_zero]
      positivity
    · let zRed : FloatSpec.Core.Defs.FlocqFloat radix := ⟨0, -bRed.dExp⟩
      have hzRedBound : Fbounded (beta:=radix) bRed zRed := by
        exact ⟨by simpa [zRed] using hvNumRedPos, by simp [zRed]⟩
      have hzRedValue : _root_.F2R (beta:=radix) zRed = 0 := by
        simp [zRed, _root_.F2R, FloatSpec.Core.Defs.F2R]
      refine ⟨zRed, ?_, ?_⟩
      · rw [hzRedValue, hhxZero]
      · constructor
        · exact hzRedBound
        · intro g hg
          simp [hzRedValue, hxZero]
  · rcases bimplybplusNorm (beta:=radix) b radix s t x rfl hradix
        hvNum hsGe hsLe hxSub.1 hxZero with
      ⟨xn, hxnValue, hxnNormal⟩
    have hxBoundPlus : Fbounded (beta:=radix) bPlus x := by
      rcases hxSub.1 with ⟨hnum, hexp⟩
      constructor
      · simpa [bPlus, plusExp] using hnum
      · dsimp [bPlus, plusExp]
        have htNonneg : (0 : Int) ≤ ((t - 1 : Nat) : Int) :=
          Int.natCast_nonneg _
        omega
    have hxnExpLe : xn.Fexp ≤ x.Fexp := by
      have h := FcanonicLeastExp_from_explicit_radix_payload (beta:=radix) radix bPlus x xn
        hradix rfl (by simpa [bPlus, plusExp] using hvNumPos)
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicLeastExp_check,
        ULift.down_up, Fbounded', Fcanonic', Int.cast_ofNat] using
        h ⟨hxnValue.symm, hxBoundPlus, Or.inl hxnNormal⟩
    let xs : FloatSpec.Core.Defs.FlocqFloat radix :=
      ⟨x.Fnum, x.Fexp + (s : Int)⟩
    have hxsValue :
        _root_.F2R (beta:=radix) xs =
          _root_.F2R (beta:=radix) x * (radix : ℝ) ^ (s : Int) := by
      simp only [xs, _root_.F2R, FloatSpec.Core.Defs.F2R,
        FloatSpec.Core.Defs.FlocqFloat.Fnum,
        FloatSpec.Core.Defs.FlocqFloat.Fexp]
      rw [zpow_add₀ (ne_of_gt hradixPos)]
      ring
    let pin : FloatSpec.Core.Defs.FlocqFloat radix := Fplus x xs
    have hpinValue :
        _root_.F2R (beta:=radix) pin =
          _root_.F2R (beta:=radix) x *
            ((radix : ℝ) ^ (s : Int) + 1) := by
      have h := Fplus_correct (beta:=radix) x xs
      have h' : _root_.F2R (beta:=radix) pin =
          _root_.F2R (beta:=radix) x +
            _root_.F2R (beta:=radix) xs := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fplus_correct_check,
          Id.run, ULift.up_down, pin, Int.cast_ofNat] using h hradix
      rw [h', hxsValue]
      ring
    have hpinExp : -b.dExp ≤ pin.Fexp := by
      have hmin := Fplus_exp_eq_min x xs
      have hxLeXs : x.Fexp ≤ xs.Fexp := by simp [xs]
      rw [hmin, min_eq_left hxLeXs]
      exact hxSub.1.2
    have hpPin :
        Closest (beta:=radix) b (radix : ℝ)
          (_root_.F2R (beta:=radix) pin) p := by
      simpa [hpinValue] using hpDef
    have hpPlus :
        Closest (beta:=radix) bPlus (radix : ℝ)
          (_root_.F2R (beta:=radix) xn *
            ((radix : ℝ) ^ (s : Int) + 1)) p := by
      have h := Closestbbplus b radix t t pin p rfl hradix hvNum
        htGtOne hpinExp hpPin
      simpa [bPlus, hpinValue, hxnValue] using h
    let qin : FloatSpec.Core.Defs.FlocqFloat radix := Fminus x p
    have hqinValue :
        _root_.F2R (beta:=radix) qin =
          _root_.F2R (beta:=radix) x -
            _root_.F2R (beta:=radix) p := by
      have h := Fminus_correct (beta:=radix) x p
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus,
        Id.run, ULift.up_down, qin, Int.cast_ofNat] using h hradix
    have hqinExp : -b.dExp ≤ qin.Fexp := by
      have hmin := Fminus_exp_eq_min x p
      rw [hmin]
      exact le_min hxSub.1.2 hpDef.1.2
    have hqQin :
        Closest (beta:=radix) b (radix : ℝ)
          (_root_.F2R (beta:=radix) qin) q := by
      simpa [hqinValue] using hqDef
    have hqPlus :
        Closest (beta:=radix) bPlus (radix : ℝ)
          (_root_.F2R (beta:=radix) xn -
            _root_.F2R (beta:=radix) p) q := by
      have h := Closestbbplus b radix t t qin q rfl hradix hvNum
        htGtOne hqinExp hqQin
      simpa [bPlus, hqinValue, hxnValue] using h
    let hxin : FloatSpec.Core.Defs.FlocqFloat radix := Fplus q p
    have hhxinValue :
        _root_.F2R (beta:=radix) hxin =
          _root_.F2R (beta:=radix) q +
            _root_.F2R (beta:=radix) p := by
      have h := Fplus_correct (beta:=radix) q p
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fplus_correct_check,
        Id.run, ULift.up_down, hxin, Int.cast_ofNat] using h hradix
    have hhxinExp : -b.dExp ≤ hxin.Fexp := by
      have hmin := Fplus_exp_eq_min q p
      rw [hmin]
      exact le_min hqDef.1.2 hpDef.1.2
    have hhxHxin :
        Closest (beta:=radix) b (radix : ℝ)
          (_root_.F2R (beta:=radix) hxin) hx := by
      simpa [hhxinValue] using hhxDef
    have hhxPlus :
        Closest (beta:=radix) bPlus (radix : ℝ)
          (_root_.F2R (beta:=radix) q +
            _root_.F2R (beta:=radix) p) hx := by
      have h := Closestbbplus b radix t t hxin hx rfl hradix hvNum
        htGtOne hhxinExp hhxHxin
      simpa [bPlus, hhxinValue] using h
    rcases VeltkampN (beta:=radix) bPlus radix s t xn p q hx
        rfl hradix hvNumPlus hsGe hsLe hxnNormal hpPlus hqPlus hhxPlus with
      ⟨hResidual, w, hwValue, hwClosest, hwExp⟩
    have hwClosestX :
        Closest (beta:=radix) bRedPlus (radix : ℝ)
          (_root_.F2R (beta:=radix) x) w := by
      simpa [bRedPlus, bPlus, hxnValue] using hwClosest
    constructor
    · calc
        |_root_.F2R (beta:=radix) x -
            _root_.F2R (beta:=radix) hx| =
            |_root_.F2R (beta:=radix) xn -
              _root_.F2R (beta:=radix) hx| := by rw [hxnValue]
        _ ≤ (radix : ℝ) ^ ((s : Int) + xn.Fexp) / 2 := hResidual
        _ ≤ (radix : ℝ) ^ ((s : Int) + x.Fexp) / 2 := by
          exact div_le_div_of_nonneg_right
            (zpow_le_zpow_right₀ hradixGeOne (by omega)) (by norm_num)
    · have hwClosestPlus :
          Closest (beta:=radix) (plusExp bRed t) (radix : ℝ)
            (_root_.F2R (beta:=radix) x) w := by
        simpa [bRed, bPlus, bRedPlus, Veltkamp_reducedBound, plusExp] using
          hwClosestX
      by_cases hwExpOld : -b.dExp ≤ w.Fexp
      · have hwBoundRed : Fbounded (beta:=radix) bRed w := by
          constructor
          · simpa [bRed, bPlus, bRedPlus, Veltkamp_reducedBound] using
              hwClosest.1.1
          · simpa [bRed, Veltkamp_reducedBound] using hwExpOld
        refine ⟨w, hwValue, ?_⟩
        exact Closestbplusb bRed (radix : ℝ) t
          (_root_.F2R (beta:=radix) x) w hwClosestPlus hwBoundRed
      · have hMinMax :
            MinOrMaxP_float (beta:=radix) bRedPlus radix
              (Closest (beta:=radix) bRedPlus (radix : ℝ)) := by
          have h := ClosestMinOrMax_from_split_radix_payload (beta:=radix) bRedPlus radix (radix : ℝ)
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMinOrMax_check, MinOrMaxP_float,
            Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
        have hRep : ∃ m : Int,
            _root_.F2R (beta:=radix) w =
              _root_.F2R (beta:=radix) ⟨m, x.Fexp⟩ := by
          rcases hMinMax (_root_.F2R (beta:=radix) x) w hwClosestX with
            hMin | hMax
          · have h := FminRep (beta:=radix) bRedPlus radix (t - s) x w
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FminRep_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using
              h ⟨rfl, hradix, hredPrecNe, hvNumRedPlus, hMin⟩
          · have h := FmaxRep (beta:=radix) bRedPlus radix (t - s) x w
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FmaxRep_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using
              h ⟨rfl, hradix, hredPrecNe, hvNumRedPlus, hMax⟩
        rcases hRep with ⟨m, hm⟩
        let g : FloatSpec.Core.Defs.FlocqFloat radix := ⟨m, x.Fexp⟩
        have hwExpLeG : w.Fexp ≤ g.Fexp := by
          dsimp [g]
          have hxAtMin := hxSub.2.1
          omega
        have hgBoundPlus : Fbounded (beta:=radix) bRedPlus g := by
          have h := FboundedEqExp (beta:=radix) bRedPlus w g
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FboundedEqExp_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using
            h ⟨hradix, hwClosestX.1, by simpa [g] using hm, hwExpLeG⟩
        have hgBoundRed : Fbounded (beta:=radix) bRed g := by
          constructor
          · simpa [bRed, bRedPlus, bPlus, Veltkamp_reducedBound] using
              hgBoundPlus.1
          · dsimp [g, bRed, Veltkamp_reducedBound]
            rw [hxSub.2.1]
        have hgClosestPlusRed :
            Closest (beta:=radix) (plusExp bRed t) (radix : ℝ)
              (_root_.F2R (beta:=radix) x) g := by
          constructor
          · simpa [bRed, bRedPlus, bPlus, Veltkamp_reducedBound, plusExp] using
              hgBoundPlus
          · intro y hy
            have hy' : Fbounded (beta:=radix) bRedPlus y := by
              simpa [bRed, bRedPlus, bPlus, Veltkamp_reducedBound, plusExp] using
                hy
            have hdist := hwClosestX.2 y hy'
            simpa [g, hm] using hdist
        refine ⟨g, ?_, ?_⟩
        · have hgValueW :
              _root_.F2R (beta:=radix) g =
                _root_.F2R (beta:=radix) w := by
              simpa [g] using hm.symm
          exact hgValueW.trans hwValue
        · exact Closestbplusb bRed (radix : ℝ) t
            (_root_.F2R (beta:=radix) x) g hgClosestPlusRed hgBoundRed

/-! Coq subnormal nearest-even Veltkamp lemma `VeltkampEvenS`.

Normalize a nonzero subnormal input under the enlarged exponent bound, apply
`VeltkampEvenN`, and transfer its nearest-even reduced-bound witness back to
the original exponent range. The zero case uses a normalized-even zero
representative directly. -/
theorem VeltkampEvenS {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x p q hx : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hsGe : 2 ≤ s) (hsLe : s ≤ t - 2)
    (hxSub : Fsubnormal (beta:=beta) radix b x)
    (hpDefEven : EvenClosest (beta:=beta) b (radix : ℝ) t
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)) p)
    (hqDefEven : EvenClosest (beta:=beta) b (radix : ℝ) t
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hhxDefEven : EvenClosest (beta:=beta) b (radix : ℝ) t
      (_root_.F2R (beta:=beta) q + _root_.F2R (beta:=beta) p) hx) :
    ∃ hx' : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) hx' = _root_.F2R (beta:=beta) hx ∧
        EvenClosest (beta:=beta) (Veltkamp_reducedBound radix b s t)
          (radix : ℝ) (t - s) (_root_.F2R (beta:=beta) x) hx' := by
  subst beta
  have htGtOne : 1 < t := by omega
  have hredPrecNe : t - s ≠ 0 := by omega
  have hredPrecGtOne : 1 < t - s := by omega
  have hradixPosInt : (0 : Int) < radix := by omega
  have hradixPos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradixPosInt
  have hvNumPos : 0 < b.vNum := by
    rw [hvNum, Zpower_nat]
    exact pow_pos hradixPosInt _
  let bRed : Fbound_skel := Veltkamp_reducedBound radix b s t
  let bPlus : Fbound_skel := plusExp b t
  let bRedPlus : Fbound_skel := Veltkamp_reducedBound radix bPlus s t
  have hvNumPlus : bPlus.vNum = Zpower_nat radix t := by
    simpa [bPlus, plusExp] using hvNum
  have hvNumRed : bRed.vNum = Zpower_nat radix (t - s) := by
    simp [bRed, Veltkamp_reducedBound]
  have hvNumRedPlus : bRedPlus.vNum = Zpower_nat radix (t - s) := by
    simp [bRedPlus, bPlus, Veltkamp_reducedBound]
  have hvNumRedPos : 0 < bRed.vNum := by
    rw [hvNumRed, Zpower_nat]
    exact pow_pos hradixPosInt _
  have hxExpRed : -bRed.dExp ≤ x.Fexp := by
    simpa [bRed, Veltkamp_reducedBound] using hxSub.1.2
  by_cases hxZero : _root_.F2R (beta:=radix) x = 0
  · let z0 : FloatSpec.Core.Defs.FlocqFloat radix := ⟨0, -b.dExp⟩
    have hz0Bound : Fbounded (beta:=radix) b z0 := by
      exact ⟨by simpa [z0] using hvNumPos, by simp [z0]⟩
    have hz0Value : _root_.F2R (beta:=radix) z0 = 0 := by
      simp [z0, _root_.F2R, FloatSpec.Core.Defs.F2R]
    have hpError := hpDefEven.1.2 z0 hz0Bound
    have hpZero : _root_.F2R (beta:=radix) p = 0 := by
      have hle : |_root_.F2R (beta:=radix) p| ≤ 0 := by
        simpa [hxZero, hz0Value] using hpError
      exact abs_eq_zero.mp (le_antisymm hle (abs_nonneg _))
    have hqError := hqDefEven.1.2 z0 hz0Bound
    have hqZero : _root_.F2R (beta:=radix) q = 0 := by
      have hle : |_root_.F2R (beta:=radix) q| ≤ 0 := by
        simpa [hxZero, hpZero, hz0Value] using hqError
      exact abs_eq_zero.mp (le_antisymm hle (abs_nonneg _))
    have hhxError := hhxDefEven.1.2 z0 hz0Bound
    have hhxZero : _root_.F2R (beta:=radix) hx = 0 := by
      have hle : |_root_.F2R (beta:=radix) hx| ≤ 0 := by
        simpa [hqZero, hpZero, hz0Value] using hhxError
      exact abs_eq_zero.mp (le_antisymm hle (abs_nonneg _))
    let zRed : FloatSpec.Core.Defs.FlocqFloat radix := ⟨0, -bRed.dExp⟩
    have hzRedBound : Fbounded (beta:=radix) bRed zRed := by
      exact ⟨by simpa [zRed] using hvNumRedPos, by simp [zRed]⟩
    have hzRedValue : _root_.F2R (beta:=radix) zRed = 0 := by
      simp [zRed, _root_.F2R, FloatSpec.Core.Defs.F2R]
    have hzRedClosest :
        Closest (beta:=radix) bRed (radix : ℝ)
          (_root_.F2R (beta:=radix) x) zRed := by
      constructor
      · exact hzRedBound
      · intro g hg
        simp [hzRedValue, hxZero]
    have hzRedEven :
        FNeven (beta:=radix) bRed (radix : ℝ) (t - s) zRed := by
      have h := FnormalizeCorrect (beta:=radix) radix bRed (t - s) zRed
      have hnormValue :
          _root_.F2R (beta:=radix)
              (Fnormalize (beta:=radix) radix bRed (t - s) zRed) =
            _root_.F2R (beta:=radix) zRed := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix⟩
      exact FNeven_of_Fnormalize_F2R_zero bRed (radix : ℝ) (t - s) zRed
        hradixPosInt (by simpa [hzRedValue] using hnormValue)
    exact ⟨zRed, by simpa [hzRedValue] using hhxZero.symm,
      hzRedClosest, Or.inl hzRedEven⟩
  · rcases bimplybplusNorm (beta:=radix) b radix s t x rfl hradix
        hvNum hsGe hsLe hxSub.1 hxZero with
      ⟨xn, hxnValue, hxnNormal⟩
    let xs : FloatSpec.Core.Defs.FlocqFloat radix :=
      ⟨x.Fnum, x.Fexp + (s : Int)⟩
    have hxsValue :
        _root_.F2R (beta:=radix) xs =
          _root_.F2R (beta:=radix) x * (radix : ℝ) ^ (s : Int) := by
      simp only [xs, _root_.F2R, FloatSpec.Core.Defs.F2R]
      rw [zpow_add₀ (ne_of_gt hradixPos)]
      ring
    let pin : FloatSpec.Core.Defs.FlocqFloat radix := Fplus x xs
    have hpinValue :
        _root_.F2R (beta:=radix) pin =
          _root_.F2R (beta:=radix) x *
            ((radix : ℝ) ^ (s : Int) + 1) := by
      have h := Fplus_correct (beta:=radix) x xs
      have h' : _root_.F2R (beta:=radix) pin =
          _root_.F2R (beta:=radix) x +
            _root_.F2R (beta:=radix) xs := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fplus_correct_check,
          Id.run, ULift.up_down, pin, Int.cast_ofNat] using h hradix
      rw [h', hxsValue]
      ring
    have hpinExp : -b.dExp ≤ pin.Fexp := by
      have hmin := Fplus_exp_eq_min x xs
      have hxLeXs : x.Fexp ≤ xs.Fexp := by simp [xs]
      rw [hmin, min_eq_left hxLeXs]
      exact hxSub.1.2
    have hpPin :
        EvenClosest (beta:=radix) b (radix : ℝ) t
          (_root_.F2R (beta:=radix) pin) p := by
      simpa [hpinValue] using hpDefEven
    have hpPlus :
        EvenClosest (beta:=radix) bPlus (radix : ℝ) t
          (_root_.F2R (beta:=radix) xn *
            ((radix : ℝ) ^ (s : Int) + 1)) p := by
      have h := EvenClosestbbplus b radix t t pin p rfl hradix hvNum
        htGtOne hpinExp hpPin
      simpa [bPlus, hpinValue, hxnValue] using h
    let qin : FloatSpec.Core.Defs.FlocqFloat radix := Fminus x p
    have hqinValue :
        _root_.F2R (beta:=radix) qin =
          _root_.F2R (beta:=radix) x -
            _root_.F2R (beta:=radix) p := by
      have h := Fminus_correct (beta:=radix) x p
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus,
        Id.run, ULift.up_down, qin, Int.cast_ofNat] using h hradix
    have hqinExp : -b.dExp ≤ qin.Fexp := by
      have hmin := Fminus_exp_eq_min x p
      rw [hmin]
      exact le_min hxSub.1.2 hpDefEven.1.1.2
    have hqQin :
        EvenClosest (beta:=radix) b (radix : ℝ) t
          (_root_.F2R (beta:=radix) qin) q := by
      simpa [hqinValue] using hqDefEven
    have hqPlus :
        EvenClosest (beta:=radix) bPlus (radix : ℝ) t
          (_root_.F2R (beta:=radix) xn -
            _root_.F2R (beta:=radix) p) q := by
      have h := EvenClosestbbplus b radix t t qin q rfl hradix hvNum
        htGtOne hqinExp hqQin
      simpa [bPlus, hqinValue, hxnValue] using h
    let hxin : FloatSpec.Core.Defs.FlocqFloat radix := Fplus q p
    have hhxinValue :
        _root_.F2R (beta:=radix) hxin =
          _root_.F2R (beta:=radix) q +
            _root_.F2R (beta:=radix) p := by
      have h := Fplus_correct (beta:=radix) q p
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fplus_correct_check,
        Id.run, ULift.up_down, hxin, Int.cast_ofNat] using h hradix
    have hhxinExp : -b.dExp ≤ hxin.Fexp := by
      have hmin := Fplus_exp_eq_min q p
      rw [hmin]
      exact le_min hqDefEven.1.1.2 hpDefEven.1.1.2
    have hhxHxin :
        EvenClosest (beta:=radix) b (radix : ℝ) t
          (_root_.F2R (beta:=radix) hxin) hx := by
      simpa [hhxinValue] using hhxDefEven
    have hhxPlus :
        EvenClosest (beta:=radix) bPlus (radix : ℝ) t
          (_root_.F2R (beta:=radix) q +
            _root_.F2R (beta:=radix) p) hx := by
      have h := EvenClosestbbplus b radix t t hxin hx rfl hradix hvNum
        htGtOne hhxinExp hhxHxin
      simpa [bPlus, hhxinValue] using h
    rcases VeltkampEvenN (beta:=radix) bPlus radix s t xn p q hx
        rfl hradix hvNumPlus hsGe hsLe hxnNormal hpPlus hqPlus hhxPlus with
      ⟨w, hwValue, hwEven⟩
    have hwEvenX :
        EvenClosest (beta:=radix) bRedPlus (radix : ℝ) (t - s)
          (_root_.F2R (beta:=radix) x) w := by
      simpa [bRedPlus, bPlus, hxnValue] using hwEven
    have hwEvenPlusRed :
        EvenClosest (beta:=radix) (plusExp bRed t) (radix : ℝ) (t - s)
          (_root_.F2R (beta:=radix) x) w := by
      simpa [bRed, bPlus, bRedPlus, Veltkamp_reducedBound, plusExp] using
        hwEvenX
    by_cases hwExpOld : -b.dExp ≤ w.Fexp
    · have hwBoundRed : Fbounded (beta:=radix) bRed w := by
        constructor
        · simpa [bRed, bPlus, bRedPlus, Veltkamp_reducedBound] using
            hwEven.1.1.1
        · simpa [bRed, Veltkamp_reducedBound] using hwExpOld
      refine ⟨w, hwValue, ?_⟩
      exact EvenClosestbplusb bRed radix t (t - s) x w rfl hradix
        hvNumRed hredPrecGtOne hxExpRed hwEvenPlusRed hwBoundRed
    · have hMinMax :
          MinOrMaxP_float (beta:=radix) bRedPlus radix
            (EvenClosest (beta:=radix) bRedPlus (radix : ℝ) (t - s)) := by
        have h := EvenClosestMinOrMax_from_split_radix_payload (beta:=radix) bRedPlus radix
          (radix : ℝ) (t - s)
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestMinOrMax_check, MinOrMaxP_float,
          Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
      have hRep : ∃ m : Int,
          _root_.F2R (beta:=radix) w =
            _root_.F2R (beta:=radix) ⟨m, x.Fexp⟩ := by
        rcases hMinMax (_root_.F2R (beta:=radix) x) w hwEvenX with
          hMin | hMax
        · have h := FminRep (beta:=radix) bRedPlus radix (t - s) x w
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FminRep_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using
            h ⟨rfl, hradix, hredPrecNe, hvNumRedPlus, hMin⟩
        · have h := FmaxRep (beta:=radix) bRedPlus radix (t - s) x w
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FmaxRep_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using
            h ⟨rfl, hradix, hredPrecNe, hvNumRedPlus, hMax⟩
      rcases hRep with ⟨m, hm⟩
      let g : FloatSpec.Core.Defs.FlocqFloat radix := ⟨m, x.Fexp⟩
      have hwValueG :
          _root_.F2R (beta:=radix) w =
            _root_.F2R (beta:=radix) g := by
        simpa [g] using hm
      have hwExpLeG : w.Fexp ≤ g.Fexp := by
        dsimp [g]
        have hxAtMin := hxSub.2.1
        omega
      have hgBoundPlus : Fbounded (beta:=radix) bRedPlus g := by
        have h := FboundedEqExp (beta:=radix) bRedPlus w g
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FboundedEqExp_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          h ⟨hradix, hwEvenX.1.1, hwValueG, hwExpLeG⟩
      have hgBoundRed : Fbounded (beta:=radix) bRed g := by
        constructor
        · simpa [bRed, bRedPlus, bPlus, Veltkamp_reducedBound] using
            hgBoundPlus.1
        · dsimp [g, bRed, Veltkamp_reducedBound]
          rw [hxSub.2.1]
      have hgClosestPlus :
          Closest (beta:=radix) bRedPlus (radix : ℝ)
            (_root_.F2R (beta:=radix) x) g := by
        constructor
        · exact hgBoundPlus
        · intro y hy
          have hdist := hwEvenX.1.2 y hy
          simpa [hwValueG] using hdist
      have hgEvenPlus :
          EvenClosest (beta:=radix) bRedPlus (radix : ℝ) (t - s)
            (_root_.F2R (beta:=radix) x) g := by
        refine ⟨hgClosestPlus, ?_⟩
        rcases hwEvenX.2 with hwParity | hwUnique
        · left
          let nw : FloatSpec.Core.Defs.FlocqFloat radix :=
            Fnormalize (beta:=radix) radix bRedPlus (t - s) w
          let ng : FloatSpec.Core.Defs.FlocqFloat radix :=
            Fnormalize (beta:=radix) radix bRedPlus (t - s) g
          have hnwCan : Fcanonic (beta:=radix) radix bRedPlus nw := by
            have h := FnormalizeCanonic (beta:=radix) radix bRedPlus
              (t - s) w
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
              Id.run, ULift.up_down, nw, Int.cast_ofNat] using
              h ⟨hwEvenX.1.1, hwEvenX.1.1, hredPrecNe, hradix, hvNumRedPlus⟩
          have hngCan : Fcanonic (beta:=radix) radix bRedPlus ng := by
            have h := FnormalizeCanonic (beta:=radix) radix bRedPlus
              (t - s) g
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
              Id.run, ULift.up_down, ng, Int.cast_ofNat] using
              h ⟨hgBoundPlus, hgBoundPlus, hredPrecNe, hradix, hvNumRedPlus⟩
          have hnwValue :
              _root_.F2R (beta:=radix) nw =
                _root_.F2R (beta:=radix) w := by
            have h := FnormalizeCorrect (beta:=radix) radix bRedPlus
              (t - s) w
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
              Id.run, ULift.up_down, nw, Int.cast_ofNat] using h ⟨rfl, hradix⟩
          have hngValue :
              _root_.F2R (beta:=radix) ng =
                _root_.F2R (beta:=radix) g := by
            have h := FnormalizeCorrect (beta:=radix) radix bRedPlus
              (t - s) g
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
              Id.run, ULift.up_down, ng, Int.cast_ofNat] using h ⟨rfl, hradix⟩
          have hnEq : nw = ng := by
            have h := FcanonicUnique (beta:=radix) radix bRedPlus nw ng
              hradix rfl
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicUnique_check,
              ULift.down_up, Fcanonic', Int.cast_ofNat] using
              h ⟨hnwCan, hngCan,
                hnwValue.trans (hwValueG.trans hngValue.symm)⟩
          unfold FNeven at hwParity ⊢
          simpa only [nw, ng, hnEq, Int.cast_ofNat] using hwParity
        · right
          intro y hy
          exact (hwUnique y hy).trans hwValueG
      have hgEvenPlusRed :
          EvenClosest (beta:=radix) (plusExp bRed t) (radix : ℝ) (t - s)
            (_root_.F2R (beta:=radix) x) g := by
        simpa [bRed, bPlus, bRedPlus, Veltkamp_reducedBound, plusExp] using
          hgEvenPlus
      refine ⟨g, ?_, ?_⟩
      · exact hwValueG.symm.trans hwValue
      · exact EvenClosestbplusb bRed radix t (t - s) x g rfl hradix
          hvNumRed hredPrecGtOne hxExpRed hgEvenPlusRed hgBoundRed

/-! Coq bounded-input nearest-even Veltkamp theorem `VeltkampEven`.

Normalize the bounded input, split its canonical representative into normal and
subnormal cases, and delegate to `VeltkampEvenN` or `VeltkampEvenS`. -/
theorem VeltkampEven {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x p q hx : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hsGe : 2 ≤ s) (hsLe : s ≤ t - 2)
    (hxBound : Fbounded (beta:=beta) b x)
    (hpDefEven : EvenClosest (beta:=beta) b (radix : ℝ) t
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)) p)
    (hqDefEven : EvenClosest (beta:=beta) b (radix : ℝ) t
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hhxDefEven : EvenClosest (beta:=beta) b (radix : ℝ) t
      (_root_.F2R (beta:=beta) q + _root_.F2R (beta:=beta) p) hx) :
    ∃ hx' : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) hx' = _root_.F2R (beta:=beta) hx ∧
        EvenClosest (beta:=beta) (Veltkamp_reducedBound radix b s t)
          (radix : ℝ) (t - s) (_root_.F2R (beta:=beta) x) hx' := by
  subst beta
  have htNe : t ≠ 0 := by omega
  let nx : FloatSpec.Core.Defs.FlocqFloat radix :=
    Fnormalize (beta:=radix) radix b t x
  have hnxCan : Fcanonic (beta:=radix) radix b nx := by
    have h := FnormalizeCanonic (beta:=radix) radix b t x
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
      Id.run, ULift.up_down, nx, Int.cast_ofNat] using
      h ⟨hxBound, hxBound, htNe, hradix, hvNum⟩
  have hnxValue :
      _root_.F2R (beta:=radix) nx = _root_.F2R (beta:=radix) x := by
    have h := FnormalizeCorrect (beta:=radix) radix b t x
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
      Id.run, ULift.up_down, nx, Int.cast_ofNat] using h ⟨rfl, hradix⟩
  have hpNx :
      EvenClosest (beta:=radix) b (radix : ℝ) t
        (_root_.F2R (beta:=radix) nx *
          ((radix : ℝ) ^ (s : Int) + 1)) p := by
    simpa [hnxValue] using hpDefEven
  have hqNx :
      EvenClosest (beta:=radix) b (radix : ℝ) t
        (_root_.F2R (beta:=radix) nx -
          _root_.F2R (beta:=radix) p) q := by
    simpa [hnxValue] using hqDefEven
  rcases hnxCan with hnxNormal | hnxSubnormal
  · have h := VeltkampEvenN (beta:=radix) b radix s t nx p q hx
      rfl hradix hvNum hsGe hsLe hnxNormal hpNx hqNx hhxDefEven
    simpa [hnxValue] using h
  · have h := VeltkampEvenS (beta:=radix) b radix s t nx p q hx
      rfl hradix hvNum hsGe hsLe hnxSubnormal hpNx hqNx hhxDefEven
    simpa [hnxValue] using h

/-- Coq Veltkamp tail lemma `Veltkamp_tail_aux`.

The subtraction of an equal-value reduced Veltkamp witness preserves the input
exponent and has a mantissa bounded by half of `radix ^ s`. -/
theorem Veltkamp_tail_aux {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x p q hx tx : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hsGe : 2 ≤ s) (hsLe : s ≤ t - 2)
    (hxCan : Fcanonic (beta:=beta) radix b x)
    (hpDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)) p)
    (hqDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hhxDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) q + _root_.F2R (beta:=beta) p) hx)
    (htxDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) hx) tx) :
    ∃ v : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) v = _root_.F2R (beta:=beta) hx ∧
        (Fminus (beta:=beta) x v).Fexp = x.Fexp ∧
          (((|(Fminus (beta:=beta) x v).Fnum| : Int) : ℝ) ≤
            (radix : ℝ) ^ (s : Int) / 2) := by
  subst beta
  have hradixPosInt : (0 : Int) < radix := by omega
  have hradixPos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradixPosInt
  have hradixNe : (radix : ℝ) ≠ 0 := ne_of_gt hradixPos
  have hCore :
      |_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) hx| ≤
          (radix : ℝ) ^ ((s : Int) + x.Fexp) / 2 ∧
        ∃ v : FloatSpec.Core.Defs.FlocqFloat radix,
          _root_.F2R (beta:=radix) v = _root_.F2R (beta:=radix) hx ∧
            Closest (beta:=radix) (Veltkamp_reducedBound radix b s t)
              (radix : ℝ) (_root_.F2R (beta:=radix) x) v ∧
            x.Fexp ≤ v.Fexp := by
    rcases hxCan with hxNormal | hxSubnormal
    · rcases VeltkampN (beta:=radix) b radix s t x p q hx rfl hradix
          hvNum hsGe hsLe hxNormal hpDef hqDef hhxDef with
        ⟨hResidual, v, hvValue, hvClosest, hvExp⟩
      refine ⟨hResidual, v, hvValue, hvClosest, ?_⟩
      omega
    · rcases VeltkampS (beta:=radix) b radix s t x p q hx rfl hradix
          hvNum hsGe hsLe hxSubnormal hpDef hqDef hhxDef with
        ⟨hResidual, v, hvValue, hvClosest⟩
      have hxAtMin := hxSubnormal.2.1
      have hvExpBound := hvClosest.1.2
      dsimp [Veltkamp_reducedBound] at hvExpBound
      exact ⟨hResidual, v, hvValue, hvClosest, by omega⟩
  rcases hCore with ⟨hResidual, v, hvValue, hvClosest, hxExpLeV⟩
  let d : FloatSpec.Core.Defs.FlocqFloat radix := Fminus x v
  have hdExp : d.Fexp = x.Fexp := by
    dsimp [d]
    rw [Fminus_exp_eq_min, min_eq_left hxExpLeV]
  have hdValue :
      _root_.F2R (beta:=radix) d =
        _root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) v := by
    have h := Fminus_correct (beta:=radix) x v
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus,
      Id.run, ULift.up_down, d, Int.cast_ofNat] using h hradix
  have hAbsScale :
      |_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) v| =
        ((|d.Fnum| : Int) : ℝ) * (radix : ℝ) ^ x.Fexp := by
    rw [← hdValue]
    simp [_root_.F2R, FloatSpec.Core.Defs.F2R, hdExp, abs_mul,
      abs_of_pos (zpow_pos hradixPos x.Fexp)]
  have hResidualV :
      |_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) v| ≤
        (radix : ℝ) ^ ((s : Int) + x.Fexp) / 2 := by
    simpa [hvValue] using hResidual
  rw [hAbsScale, zpow_add₀ hradixNe] at hResidualV
  refine ⟨v, hvValue, ?_, ?_⟩
  · exact hdExp
  · have hscalePos : 0 < (radix : ℝ) ^ x.Fexp :=
      zpow_pos hradixPos x.Fexp
    nlinarith

/-! Coq binary-radix Veltkamp tail theorem `Veltkamp_tail2`.

Normalize the bounded input, apply `Veltkamp_tail_aux`, and re-encode the exact
residual in the `s - 1` split bound.  Binary radix turns the auxiliary
`2 ^ s / 2` mantissa estimate into the required `2 ^ (s - 1)` bound. -/
theorem Veltkamp_tail2 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x p q hx tx : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hsGe : 2 ≤ s) (hsLe : s ≤ t - 2)
    (hradixTwo : radix = 2)
    (hxBound : Fbounded (beta:=beta) b x)
    (hpDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)) p)
    (hqDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hhxDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) q + _root_.F2R (beta:=beta) p) hx)
    (htxDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) hx) tx) :
    ∃ tx' : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) tx' = _root_.F2R (beta:=beta) tx ∧
        _root_.F2R (beta:=beta) hx + _root_.F2R (beta:=beta) tx' =
          _root_.F2R (beta:=beta) x ∧
        Fbounded (beta:=beta) (Veltkamp_splitBound radix b (s - 1)) tx' ∧
        (Fnormalize (beta:=beta) radix b t x).Fexp ≤ tx'.Fexp := by
  subst beta
  subst radix
  have htNe : t ≠ 0 := by omega
  let nx : FloatSpec.Core.Defs.FlocqFloat 2 :=
    Fnormalize (beta:=2) 2 b t x
  have hnxCan : Fcanonic (beta:=2) 2 b nx := by
    have h := FnormalizeCanonic (beta:=2) 2 b t x
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
      Id.run, ULift.up_down, nx, Int.cast_ofNat] using
      h ⟨hxBound, hxBound, htNe, hradix, hvNum⟩
  have hnxBound : Fbounded (beta:=2) b nx := by
    have h := FnormalizeBounded (beta:=2) 2 b t x
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeBounded_check,
      Id.run, ULift.up_down, nx, Int.cast_ofNat] using
      h ⟨hxBound, hxBound, htNe, hradix, hvNum⟩
  have hnxValue :
      _root_.F2R (beta:=2) nx = _root_.F2R (beta:=2) x := by
    have h := FnormalizeCorrect (beta:=2) 2 b t x
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
      Id.run, ULift.up_down, nx, Int.cast_ofNat] using h ⟨rfl, hradix⟩
  have hpNx :
      Closest (beta:=2) b (2 : ℝ)
        (_root_.F2R (beta:=2) nx * ((2 : ℝ) ^ (s : Int) + 1)) p := by
    simpa [hnxValue] using hpDef
  have hqNx :
      Closest (beta:=2) b (2 : ℝ)
        (_root_.F2R (beta:=2) nx - _root_.F2R (beta:=2) p) q := by
    simpa [hnxValue] using hqDef
  have htxNx :
      Closest (beta:=2) b (2 : ℝ)
        (_root_.F2R (beta:=2) nx - _root_.F2R (beta:=2) hx) tx := by
    simpa [hnxValue] using htxDef
  rcases Veltkamp_tail_aux (beta:=2) b 2 s t nx p q hx tx rfl hradix
      hvNum hsGe hsLe hnxCan hpNx hqNx hhxDef htxNx with
    ⟨v, hvValue, hdExp, hdMantissa⟩
  let d : FloatSpec.Core.Defs.FlocqFloat 2 := Fminus nx v
  have hdExp' : d.Fexp = nx.Fexp := by
    simpa only [d, Int.cast_ofNat] using hdExp
  have hdMantissa' :
      (((|d.Fnum| : Int) : ℝ) ≤ (2 : ℝ) ^ (s : Int) / 2) := by
    simpa only [d, Int.cast_ofNat] using hdMantissa
  have hpowPredCast :
      (Zpower_nat 2 (s - 1) : ℝ) =
        (2 : ℝ) ^ (((s - 1 : Nat) : Int)) := by
    have h := Zpower_nat_Z_powerRZ 2 (s - 1)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Zpower_nat_Z_powerRZ_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hsPredCast : ((s - 1 : Nat) : Int) = (s : Int) - 1 := by
    omega
  have hhalfPow :
      (2 : ℝ) ^ (s : Int) / 2 = (Zpower_nat 2 (s - 1) : ℝ) := by
    rw [hpowPredCast, hsPredCast]
    rw [show (2 : ℝ) ^ (s : Int) =
        (2 : ℝ) ^ ((s : Int) - 1) * 2 by
      calc
        (2 : ℝ) ^ (s : Int) =
            (2 : ℝ) ^ (((s : Int) - 1) + 1) := by congr 1 <;> omega
        _ = (2 : ℝ) ^ ((s : Int) - 1) * (2 : ℝ) ^ (1 : Int) :=
          zpow_add₀ (by norm_num) _ _
        _ = _ := by rw [zpow_one]]
    ring
  have hdMantissaInt : |d.Fnum| ≤ Zpower_nat 2 (s - 1) := by
    exact_mod_cast (show ((|d.Fnum| : Int) : ℝ) ≤
        (Zpower_nat 2 (s - 1) : ℝ) by
      rw [← hhalfPow]
      exact hdMantissa')
  have hnxExpMin :
      -(Veltkamp_splitBound 2 b (s - 1)).dExp ≤ nx.Fexp := by
    simpa [Veltkamp_splitBound] using hnxBound.2
  have hbounded :=
    FboundedMbound2 (beta:=2) (Veltkamp_splitBound 2 b (s - 1))
      2 (s - 1) nx.Fexp d.Fnum
  rcases (by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FboundedMbound2_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        hbounded ⟨rfl, hradix, by omega, rfl, hdMantissaInt, hnxExpMin⟩) with
    ⟨c, hcTailBound, hcScaledValue, hnxExpLeC⟩
  have hdValue :
      _root_.F2R (beta:=2) d =
        _root_.F2R (beta:=2) nx - _root_.F2R (beta:=2) v := by
    have h := Fminus_correct (beta:=2) nx v
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus,
      Id.run, ULift.up_down, d, Int.cast_ofNat] using h hradix
  have hcValueD :
      _root_.F2R (beta:=2) c = _root_.F2R (beta:=2) d := by
    calc
      _root_.F2R (beta:=2) c =
          (d.Fnum : ℝ) * (2 : ℝ) ^ nx.Fexp := hcScaledValue
      _ = _root_.F2R (beta:=2) d := by
        simp [_root_.F2R, FloatSpec.Core.Defs.F2R, hdExp']
  have hcResidual :
      _root_.F2R (beta:=2) c =
        _root_.F2R (beta:=2) x - _root_.F2R (beta:=2) hx := by
    calc
      _root_.F2R (beta:=2) c = _root_.F2R (beta:=2) d := hcValueD
      _ = _root_.F2R (beta:=2) nx - _root_.F2R (beta:=2) v := hdValue
      _ = _root_.F2R (beta:=2) x - _root_.F2R (beta:=2) hx := by
        rw [hnxValue, hvValue]
  have hcOriginalBound : Fbounded (beta:=2) b c := by
    constructor
    · calc
        |c.Fnum| < (Veltkamp_splitBound 2 b (s - 1)).vNum := hcTailBound.1
        _ = Zpower_nat 2 (s - 1) := rfl
        _ < b.vNum := by
          rw [hvNum]
          exact pow_lt_pow_right₀ hradix (by omega)
    · simpa [Veltkamp_splitBound] using hcTailBound.2
  have htxValue :
      _root_.F2R (beta:=2) tx =
        _root_.F2R (beta:=2) x - _root_.F2R (beta:=2) hx := by
    have hdist := htxDef.2 c hcOriginalBound
    rw [hcResidual, sub_self, abs_zero] at hdist
    have hzero :
        |_root_.F2R (beta:=2) tx -
          (_root_.F2R (beta:=2) x - _root_.F2R (beta:=2) hx)| = 0 :=
      le_antisymm hdist (abs_nonneg _)
    exact sub_eq_zero.mp (abs_eq_zero.mp hzero)
  refine ⟨c, hcResidual.trans htxValue.symm, ?_, hcTailBound, ?_⟩
  · rw [hcResidual]
    ring
  · simpa only [nx, Int.cast_ofNat] using hnxExpLeC

/-! Coq useful Veltkamp theorem `VeltkampU`.

Combine the normal/subnormal Veltkamp residual theorem with the tail
construction.  The latter residual lies strictly inside the `s`-digit split
bound, so closestness identifies its value with `tx`. -/
theorem VeltkampU {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x p q hx tx : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hsGe : 2 ≤ s) (hsLe : s ≤ t - 2)
    (hxCan : Fcanonic (beta:=beta) radix b x)
    (hpDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * ((radix : ℝ) ^ (s : Int) + 1)) p)
    (hqDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hhxDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) q + _root_.F2R (beta:=beta) p) hx)
    (htxDef : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) hx) tx) :
    |_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) hx| ≤
        (radix : ℝ) ^ ((s : Int) + x.Fexp) / 2 ∧
      _root_.F2R (beta:=beta) x =
        _root_.F2R (beta:=beta) hx + _root_.F2R (beta:=beta) tx ∧
      (∃ hx' : FloatSpec.Core.Defs.FlocqFloat beta,
        _root_.F2R (beta:=beta) hx' = _root_.F2R (beta:=beta) hx ∧
          Fbounded (beta:=beta) (Veltkamp_reducedBound radix b s t) hx' ∧
          (Fnormal (beta:=beta) radix b x →
            (s : Int) + x.Fexp ≤ hx'.Fexp)) ∧
      ∃ tx' : FloatSpec.Core.Defs.FlocqFloat beta,
        _root_.F2R (beta:=beta) tx' = _root_.F2R (beta:=beta) tx ∧
          Fbounded (beta:=beta) (Veltkamp_splitBound radix b s) tx' ∧
          x.Fexp ≤ tx'.Fexp := by
  subst beta
  have hxBound : Fbounded (beta:=radix) b x := by
    have h := FcanonicBound (beta:=radix) radix b x
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicBound_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hxCan
  have hHead :
      |_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) hx| ≤
          (radix : ℝ) ^ ((s : Int) + x.Fexp) / 2 ∧
        ∃ hx' : FloatSpec.Core.Defs.FlocqFloat radix,
          _root_.F2R (beta:=radix) hx' = _root_.F2R (beta:=radix) hx ∧
            Fbounded (beta:=radix) (Veltkamp_reducedBound radix b s t) hx' ∧
            (Fnormal (beta:=radix) radix b x →
              (s : Int) + x.Fexp ≤ hx'.Fexp) := by
    by_cases hxNormal : Fnormal (beta:=radix) radix b x
    · rcases VeltkampN (beta:=radix) b radix s t x p q hx rfl hradix
          hvNum hsGe hsLe hxNormal hpDef hqDef hhxDef with
        ⟨hResidual, hx', hhxValue, hhxClosest, hhxExp⟩
      exact ⟨hResidual, hx', hhxValue, hhxClosest.1, fun _ ↦ hhxExp⟩
    · have hxSub : Fsubnormal (beta:=radix) radix b x :=
        hxCan.resolve_left hxNormal
      rcases VeltkampS (beta:=radix) b radix s t x p q hx rfl hradix
          hvNum hsGe hsLe hxSub hpDef hqDef hhxDef with
        ⟨hResidual, hx', hhxValue, hhxClosest⟩
      exact ⟨hResidual, hx', hhxValue, hhxClosest.1,
        fun hnormal ↦ (hxNormal hnormal).elim⟩
  rcases Veltkamp_tail_aux (beta:=radix) b radix s t x p q hx tx rfl hradix
      hvNum hsGe hsLe hxCan hpDef hqDef hhxDef htxDef with
    ⟨v, hvValue, hdExp, hdMantissa⟩
  let d : FloatSpec.Core.Defs.FlocqFloat radix := Fminus x v
  have hdExp' : d.Fexp = x.Fexp := by
    simpa only [d, Int.cast_ofNat] using hdExp
  have hdMantissa' :
      (((|d.Fnum| : Int) : ℝ) ≤ (radix : ℝ) ^ (s : Int) / 2) := by
    simpa only [d, Int.cast_ofNat] using hdMantissa
  have hpowSCast :
      (Zpower_nat radix s : ℝ) = (radix : ℝ) ^ (s : Int) := by
    have h := Zpower_nat_Z_powerRZ radix s
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Zpower_nat_Z_powerRZ_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hradixPos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (show (0 : Int) < radix by omega)
  have hpowSPos : 0 < (radix : ℝ) ^ (s : Int) :=
    zpow_pos hradixPos _
  have hdMantissaReal :
      ((|d.Fnum| : Int) : ℝ) < (Zpower_nat radix s : ℝ) := by
    rw [hpowSCast]
    nlinarith
  have hdMantissaInt : |d.Fnum| < Zpower_nat radix s := by
    exact_mod_cast hdMantissaReal
  have hdBound :
      Fbounded (beta:=radix) (Veltkamp_splitBound radix b s) d := by
    constructor
    · simpa [Veltkamp_splitBound] using hdMantissaInt
    · simpa [Veltkamp_splitBound, hdExp'] using hxBound.2
  have hdOriginalBound : Fbounded (beta:=radix) b d := by
    constructor
    · calc
        |d.Fnum| < (Veltkamp_splitBound radix b s).vNum := hdBound.1
        _ = Zpower_nat radix s := rfl
        _ < b.vNum := by
          rw [hvNum]
          exact pow_lt_pow_right₀ hradix (by omega)
    · simpa [Veltkamp_splitBound] using hdBound.2
  have hdValue :
      _root_.F2R (beta:=radix) d =
        _root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) v := by
    have h := Fminus_correct (beta:=radix) x v
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus,
      Id.run, ULift.up_down, d, Int.cast_ofNat] using h hradix
  have hdResidual :
      _root_.F2R (beta:=radix) d =
        _root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) hx := by
    rw [hdValue, hvValue]
  have htxValue :
      _root_.F2R (beta:=radix) tx =
        _root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) hx := by
    have hdist := htxDef.2 d hdOriginalBound
    rw [hdResidual, sub_self, abs_zero] at hdist
    have hzero :
        |_root_.F2R (beta:=radix) tx -
          (_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) hx)| = 0 :=
      le_antisymm hdist (abs_nonneg _)
    exact sub_eq_zero.mp (abs_eq_zero.mp hzero)
  rcases hHead with ⟨hResidual, hx', hhxValue, hhxBound, hhxExp⟩
  refine ⟨hResidual, ?_, ⟨hx', hhxValue, hhxBound, hhxExp⟩, d, ?_,
    hdBound, ?_⟩
  · rw [htxValue]
    ring
  · exact hdResidual.trans htxValue.symm
  · rw [hdExp']

/-! Coq generic Dekker theorem `BoundedL`.

Rescale the mantissa of `x` down to exponent `e`.  The strict magnitude bound
then gives the required mantissa bound after canceling the positive
`radix ^ e` factor. -/
theorem BoundedL {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (r : ℝ) (x : FloatSpec.Core.Defs.FlocqFloat beta) (e : Int)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix precision)
    (_hprecision : 1 < precision)
    (heLe : e ≤ x.Fexp) (heMin : -b.dExp ≤ e)
    (hxValue : _root_.F2R (beta:=beta) x = r)
    (hrBound : |r| < (radix : ℝ) ^ (e + (precision : Int))) :
    ∃ x' : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) x' = r ∧
        Fbounded (beta:=beta) b x' ∧ x'.Fexp = e := by
  subst beta
  let shift : Nat := Int.natAbs (x.Fexp - e)
  let x' : FloatSpec.Core.Defs.FlocqFloat radix :=
    Fshift (beta:=radix) radix shift x
  have hdiffNonneg : 0 ≤ x.Fexp - e := by omega
  have hshiftCast : (shift : Int) = x.Fexp - e := by
    simpa only [shift, Int.cast_ofNat] using Int.natAbs_of_nonneg hdiffNonneg
  have hx'Exp : x'.Fexp = e := by
    simp only [x', Fshift, FloatSpec.Core.Defs.FlocqFloat.Fexp]
    rw [hshiftCast]
    omega
  have hx'Value : _root_.F2R (beta:=radix) x' = r := by
    exact (Fshift_value (beta:=radix) radix shift x rfl hradix).trans hxValue
  have hradixPos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (show (0 : Int) < radix by omega)
  have hradixNe : (radix : ℝ) ≠ 0 := ne_of_gt hradixPos
  have hscalePos : 0 < (radix : ℝ) ^ e := zpow_pos hradixPos e
  have hscaledMantissa :
      ((|x'.Fnum| : Int) : ℝ) * (radix : ℝ) ^ e <
        (radix : ℝ) ^ ((precision : Int)) * (radix : ℝ) ^ e := by
    calc
      ((|x'.Fnum| : Int) : ℝ) * (radix : ℝ) ^ e =
          |_root_.F2R (beta:=radix) x'| := by
            simp [_root_.F2R, FloatSpec.Core.Defs.F2R, hx'Exp, abs_mul,
              abs_of_pos hscalePos, Int.cast_abs]
      _ = |r| := congrArg abs hx'Value
      _ < (radix : ℝ) ^ (e + (precision : Int)) := hrBound
      _ = (radix : ℝ) ^ ((precision : Int)) * (radix : ℝ) ^ e := by
        rw [zpow_add₀ hradixNe]
        ring
  have hmantissaReal :
      ((|x'.Fnum| : Int) : ℝ) < (radix : ℝ) ^ ((precision : Int)) :=
    lt_of_mul_lt_mul_right hscaledMantissa (le_of_lt hscalePos)
  have hpowCast :
      (Zpower_nat radix precision : ℝ) =
        (radix : ℝ) ^ ((precision : Int)) := by
    have h := Zpower_nat_Z_powerRZ radix precision
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Zpower_nat_Z_powerRZ_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hmantissa : |x'.Fnum| < Zpower_nat radix precision := by
    exact_mod_cast (show ((|x'.Fnum| : Int) : ℝ) <
        (Zpower_nat radix precision : ℝ) by
      rw [hpowCast]
      exact hmantissaReal)
  refine ⟨x', hx'Value, ?_, hx'Exp⟩
  constructor
  · simpa [hvNum] using hmantissa
  · simpa [hx'Exp] using heMin

/-! Coq generic Dekker theorem `Closestbbext`.

An arbitrary extended bound with the same mantissa limit and a strictly larger
local exponent is a `plusExp` bound for the exact exponent gap, so closestness
extends by `Closestbbplus`. -/
theorem Closestbbext {beta : Int} [ValidRadix beta]
    (b bext : Fbound_skel) (radix : Int) (precision : Nat)
    (fext f : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix precision)
    (hprecision : 1 < precision)
    (hbextNum : bext.vNum = b.vNum)
    (hbextExp : b.dExp < bext.dExp)
    (hfextExp : -b.dExp ≤ fext.Fexp)
    (hfClosest : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) fext) f) :
    Closest (beta:=beta) bext (radix : ℝ)
      (_root_.F2R (beta:=beta) fext) f := by
  subst beta
  let t : Nat := (bext.dExp - b.dExp + 1).toNat
  have htCast : (t : Int) = bext.dExp - b.dExp + 1 := by
    dsimp [t]
    rw [Int.toNat_of_nonneg]
    omega
  have htPredCast : ((t - 1 : Nat) : Int) = bext.dExp - b.dExp := by
    have htPos : 0 < t := by
      omega
    have hpred : ((t - 1 : Nat) : Int) = (t : Int) - 1 := by
      omega
    omega
  have hbextEq : plusExp b t = bext := by
    cases b with
    | mk bd bv hbd hbv =>
      cases bext with
      | mk ed ev hed hev =>
        simp only [plusExp] at htPredCast hbextNum ⊢
        congr <;> omega
  have hplus :
      Closest (beta:=radix) (plusExp b t) (radix : ℝ)
        (_root_.F2R (beta:=radix) fext) f :=
    Closestbbplus b radix t precision fext f rfl hradix hvNum
      hprecision hfextExp hfClosest
  simpa [hbextEq] using hplus

/-! Coq Algo2 theorem `Veltkampb'`.

The four Veltkamp split roundings under `b` remain closest roundings under the
Dekker extended bound `b'`.  The represented inputs are exactly the upstream
`Fplus`/`Fminus`/`Fmult` witnesses, and the transfer step is `Closestbbext`. -/
theorem Veltkampb' {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (t : Nat)
    (f pf qf hf tf : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hpGe : 4 ≤ t)
    (hbextExp : b.dExp < (Dekker_extendedBound b t).dExp)
    (hfBound : Fbounded (beta:=beta) b f)
    (hA1 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) f *
        ((radix : ℝ) ^ (((t - Nat.div2 t : Nat) : Int)) + 1)) pf)
    (hA2 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) f - _root_.F2R (beta:=beta) pf) qf)
    (hA3 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) qf + _root_.F2R (beta:=beta) pf) hf)
    (hA4 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) f - _root_.F2R (beta:=beta) hf) tf) :
    Closest (beta:=beta) (Dekker_extendedBound b t) (radix : ℝ)
      (_root_.F2R (beta:=beta) f *
        ((radix : ℝ) ^ (((t - Nat.div2 t : Nat) : Int)) + 1)) pf ∧
    Closest (beta:=beta) (Dekker_extendedBound b t) (radix : ℝ)
      (_root_.F2R (beta:=beta) f - _root_.F2R (beta:=beta) pf) qf ∧
    Closest (beta:=beta) (Dekker_extendedBound b t) (radix : ℝ)
      (_root_.F2R (beta:=beta) qf + _root_.F2R (beta:=beta) pf) hf ∧
    Closest (beta:=beta) (Dekker_extendedBound b t) (radix : ℝ)
      (_root_.F2R (beta:=beta) f - _root_.F2R (beta:=beta) hf) tf := by
  subst beta
  let s : Nat := t - Nat.div2 t
  have htGtOne : 1 < t := by omega
  let scale : FloatSpec.Core.Defs.FlocqFloat radix := ⟨1, (s : Int)⟩
  let fScale : FloatSpec.Core.Defs.FlocqFloat radix :=
    FloatSpec.Calc.Operations.Fmult (beta:=radix) f scale
  let fScaled : FloatSpec.Core.Defs.FlocqFloat radix := Fplus fScale f
  have hfScaledValue :
      _root_.F2R (beta:=radix) fScaled =
        _root_.F2R (beta:=radix) f *
          ((radix : ℝ) ^ (((t - Nat.div2 t : Nat) : Int)) + 1) := by
    have hplus := Fplus_correct (beta:=radix) fScale f
    have hplusValue :
        _root_.F2R (beta:=radix) fScaled =
          _root_.F2R (beta:=radix) fScale + _root_.F2R (beta:=radix) f := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fplus_correct_check, Id.run,
        ULift.up_down, fScaled, Int.cast_ofNat] using hplus hradix
    have hmult := Fmult_correct (beta:=radix) f scale
    have hscaleValue :
        _root_.F2R (beta:=radix) fScale =
          _root_.F2R (beta:=radix) f * (radix : ℝ) ^ (s : Int) := by
      have hmultValue :
          _root_.F2R (beta:=radix) fScale =
            _root_.F2R (beta:=radix) f * _root_.F2R (beta:=radix) scale := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fmult_correct_check, Id.run,
          ULift.up_down, fScale, Int.cast_ofNat] using hmult hradix
      rw [hmultValue]
      simp [scale, _root_.F2R, FloatSpec.Core.Defs.F2R]
    rw [hplusValue, hscaleValue]
    simp [s]
    ring
  have hfScaledExp : -b.dExp ≤ fScaled.Fexp := by
    have hplusExp :
        fScaled.Fexp = min fScale.Fexp f.Fexp := by
      simpa [fScaled] using Fplus_exp_eq_min (beta:=radix) fScale f
    have hfScaleExp : fScale.Fexp = f.Fexp + (s : Int) := by
      simp [fScale, scale, FloatSpec.Calc.Operations.Fmult]
    rw [hplusExp, hfScaleExp, min_eq_right]
    · exact hfBound.2
    · omega
  have hA1Ext :
      Closest (beta:=radix) (Dekker_extendedBound b t) (radix : ℝ)
        (_root_.F2R (beta:=radix) f *
          ((radix : ℝ) ^ (((t - Nat.div2 t : Nat) : Int)) + 1)) pf := by
    have hA1Value :
        Closest (beta:=radix) b (radix : ℝ)
          (_root_.F2R (beta:=radix) fScaled) pf := by
      simpa [hfScaledValue] using hA1
    have h := Closestbbext (beta:=radix) b (Dekker_extendedBound b t)
      radix t fScaled pf rfl hradix hvNum htGtOne rfl hbextExp
      hfScaledExp hA1Value
    simpa [hfScaledValue] using h
  let fMinusPf : FloatSpec.Core.Defs.FlocqFloat radix := Fminus f pf
  have hfMinusPfValue :
      _root_.F2R (beta:=radix) fMinusPf =
        _root_.F2R (beta:=radix) f - _root_.F2R (beta:=radix) pf := by
    have h := Fminus_correct (beta:=radix) f pf
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus, Id.run,
      ULift.up_down, fMinusPf, Int.cast_ofNat] using h hradix
  have hfMinusPfExp : -b.dExp ≤ fMinusPf.Fexp := by
    rw [show fMinusPf.Fexp = min f.Fexp pf.Fexp by
      simpa [fMinusPf] using Fminus_exp_eq_min (beta:=radix) f pf]
    exact le_min hfBound.2 hA1.1.2
  have hA2Ext :
      Closest (beta:=radix) (Dekker_extendedBound b t) (radix : ℝ)
        (_root_.F2R (beta:=radix) f - _root_.F2R (beta:=radix) pf) qf := by
    have hA2Value :
        Closest (beta:=radix) b (radix : ℝ)
          (_root_.F2R (beta:=radix) fMinusPf) qf := by
      simpa [hfMinusPfValue] using hA2
    have h := Closestbbext (beta:=radix) b (Dekker_extendedBound b t)
      radix t fMinusPf qf rfl hradix hvNum htGtOne rfl hbextExp
      hfMinusPfExp hA2Value
    simpa [hfMinusPfValue] using h
  let qPlusPf : FloatSpec.Core.Defs.FlocqFloat radix := Fplus qf pf
  have hqPlusPfValue :
      _root_.F2R (beta:=radix) qPlusPf =
        _root_.F2R (beta:=radix) qf + _root_.F2R (beta:=radix) pf := by
    have h := Fplus_correct (beta:=radix) qf pf
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fplus_correct_check, Id.run,
      ULift.up_down, qPlusPf, Int.cast_ofNat] using h hradix
  have hqPlusPfExp : -b.dExp ≤ qPlusPf.Fexp := by
    rw [show qPlusPf.Fexp = min qf.Fexp pf.Fexp by
      simpa [qPlusPf] using Fplus_exp_eq_min (beta:=radix) qf pf]
    exact le_min hA2.1.2 hA1.1.2
  have hA3Ext :
      Closest (beta:=radix) (Dekker_extendedBound b t) (radix : ℝ)
        (_root_.F2R (beta:=radix) qf + _root_.F2R (beta:=radix) pf) hf := by
    have hA3Value :
        Closest (beta:=radix) b (radix : ℝ)
          (_root_.F2R (beta:=radix) qPlusPf) hf := by
      simpa [hqPlusPfValue] using hA3
    have h := Closestbbext (beta:=radix) b (Dekker_extendedBound b t)
      radix t qPlusPf hf rfl hradix hvNum htGtOne rfl hbextExp
      hqPlusPfExp hA3Value
    simpa [hqPlusPfValue] using h
  let fMinusHf : FloatSpec.Core.Defs.FlocqFloat radix := Fminus f hf
  have hfMinusHfValue :
      _root_.F2R (beta:=radix) fMinusHf =
        _root_.F2R (beta:=radix) f - _root_.F2R (beta:=radix) hf := by
    have h := Fminus_correct (beta:=radix) f hf
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus, Id.run,
      ULift.up_down, fMinusHf, Int.cast_ofNat] using h hradix
  have hfMinusHfExp : -b.dExp ≤ fMinusHf.Fexp := by
    rw [show fMinusHf.Fexp = min f.Fexp hf.Fexp by
      simpa [fMinusHf] using Fminus_exp_eq_min (beta:=radix) f hf]
    exact le_min hfBound.2 hA3.1.2
  have hA4Ext :
      Closest (beta:=radix) (Dekker_extendedBound b t) (radix : ℝ)
        (_root_.F2R (beta:=radix) f - _root_.F2R (beta:=radix) hf) tf := by
    have hA4Value :
        Closest (beta:=radix) b (radix : ℝ)
          (_root_.F2R (beta:=radix) fMinusHf) tf := by
      simpa [hfMinusHfValue] using hA4
    have h := Closestbbext (beta:=radix) b (Dekker_extendedBound b t)
      radix t fMinusHf tf rfl hradix hvNum htGtOne rfl hbextExp
      hfMinusHfExp hA4Value
    simpa [hfMinusHfValue] using h
  exact ⟨hA1Ext, hA2Ext, hA3Ext, hA4Ext⟩

/-! Coq Algo2 theorem `NormalbPrim`.

Every nonzero canonical float under the original Dekker bound has a normal
representative under the extended bound. -/
theorem NormalbPrim_from_nonnegative_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (t : Nat)
    (f : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hpGe : 4 ≤ t)
    (hdExp : 0 ≤ b.dExp)
    (hfCan : Fcanonic (beta:=beta) radix b f)
    (hfNonzero : _root_.F2R (beta:=beta) f ≠ 0) :
    ∃ f' : FloatSpec.Core.Defs.FlocqFloat beta,
      Fnormal (beta:=beta) radix (Dekker_extendedBound b t) f' ∧
        _root_.F2R (beta:=beta) f' = _root_.F2R (beta:=beta) f ∧
        -((t : Int)) - b.dExp ≤ f'.Fexp := by
  subst beta
  let b' : Fbound_skel := Dekker_extendedBound b t
  let nf : FloatSpec.Core.Defs.FlocqFloat radix :=
    Fnormalize (beta:=radix) radix b' t f
  have htNe : t ≠ 0 := by omega
  have hb'Num : b'.vNum = Zpower_nat radix t := by
    simpa [b', Dekker_extendedBound] using hvNum
  have hfBound : Fbounded (beta:=radix) b f := by
    have h := FcanonicBound (beta:=radix) radix b f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicBound_check, Int.cast_ofNat] using h hfCan
  have hbExt : b.dExp < b'.dExp := by
    simpa [b'] using dExpPrim_from_nonnegative_payload b t hdExp
  have hfBoundExt : Fbounded (beta:=radix) b' f := by
    constructor
    · simpa [b', Dekker_extendedBound] using hfBound.1
    · have hlower : -b'.dExp ≤ -b.dExp := by omega
      exact le_trans hlower hfBound.2
  have hnfCan : Fcanonic (beta:=radix) radix b' nf := by
    have h := FnormalizeCanonic (beta:=radix) radix b' t f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
      Id.run, ULift.up_down, Fbounded', nf, Int.cast_ofNat] using
      h ⟨hfBoundExt, hfBoundExt, htNe, hradix, hb'Num⟩
  have hnfValue :
      _root_.F2R (beta:=radix) nf = _root_.F2R (beta:=radix) f := by
    have h := FnormalizeCorrect (beta:=radix) radix b' t f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
      Id.run, ULift.up_down, nf, Int.cast_ofNat] using h ⟨rfl, hradix⟩
  have hfNumNonzero : f.Fnum ≠ 0 := by
    intro hfNum
    apply hfNonzero
    simp [hfNum, _root_.F2R, FloatSpec.Core.Defs.F2R]
  have hnfExpLower : -((t : Int)) - b.dExp ≤ nf.Fexp := by
    have hshiftLe :
        ((min (t - Fdigit (beta:=radix) radix f)
          (Int.natAbs (b'.dExp + f.Fexp)) : Nat) : Int) ≤ (t : Int) := by
      have hmin :
          min (t - Fdigit (beta:=radix) radix f)
              (Int.natAbs (b'.dExp + f.Fexp)) ≤
            t - Fdigit (beta:=radix) radix f :=
        Nat.min_le_left _ _
      have hsub : t - Fdigit (beta:=radix) radix f ≤ t :=
        Nat.sub_le _ _
      exact_mod_cast le_trans hmin hsub
    have hnfExp :
        nf.Fexp =
          f.Fexp - ((min (t - Fdigit (beta:=radix) radix f)
            (Int.natAbs (b'.dExp + f.Fexp)) : Nat) : Int) := by
      dsimp [nf]
      simp [Fnormalize, hfNumNonzero, Fshift]
    have hfExpLower : -b.dExp ≤ f.Fexp := hfBound.2
    rw [hnfExp]
    omega
  have hnfNormal : Fnormal (beta:=radix) radix b' nf := by
    rcases hnfCan with hnormal | hsubnormal
    · exact hnormal
    · exfalso
      have hb'Exp : b'.dExp = 2 * b.dExp + 2 * (t : Int) + 2 := by
        simpa [b'] using dExpPrimEq b t
      have hsubExp : nf.Fexp = -b'.dExp := hsubnormal.2.1
      omega
  exact ⟨nf, hnfNormal, hnfValue, hnfExpLower⟩

/-- Coq: `NormalbPrim`; `Fbound_skel` already carries `0 ≤ dExp`. -/
theorem NormalbPrim {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (t : Nat)
    (f : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hpGe : 4 ≤ t)
    (hfCan : Fcanonic (beta:=beta) radix b f)
    (hfNonzero : _root_.F2R (beta:=beta) f ≠ 0) :
    ∃ f' : FloatSpec.Core.Defs.FlocqFloat beta,
      Fnormal (beta:=beta) radix (Dekker_extendedBound b t) f' ∧
        _root_.F2R (beta:=beta) f' = _root_.F2R (beta:=beta) f ∧
        -((t : Int)) - b.dExp ≤ f'.Fexp :=
  NormalbPrim_from_nonnegative_payload b radix t f hbeta hradix hvNum hpGe
    b.dExp_nonneg hfCan hfNonzero

private lemma Fbounded_of_same_vNum_of_exp_ge {beta : Int} [ValidRadix beta]
    (b b' : Fbound_skel) (f : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbNum : b.vNum = b'.vNum) (hfBound : Fbounded (beta:=beta) b' f)
    (hfExp : -b.dExp ≤ f.Fexp) :
    Fbounded (beta:=beta) b f := by
  exact ⟨by simpa [hbNum] using hfBound.1, hfExp⟩

private lemma firstNormalPos_value_pos {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (hbeta : beta = radix) (hradix : 1 < radix) :
    0 < _root_.F2R (beta:=beta)
      (firstNormalPos (beta:=beta) radix b precision) := by
  subst beta
  have hradixPosInt : (0 : Int) < radix := by omega
  have hradixPos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradixPosInt
  have hnormMinPos : 0 < nNormMin radix precision := by
    unfold nNormMin
    exact pow_pos hradixPosInt _
  simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, firstNormalPos]
  exact mul_pos (by exact_mod_cast hnormMinPos) (zpow_pos hradixPos _)

private lemma abs_under_extended_below_firstNormal {beta : Int} [ValidRadix beta]
    (b b' : Fbound_skel) (radix : Int) (precision : Nat)
    (a' : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hprecision : 1 < precision)
    (hvNum : b.vNum = Zpower_nat radix precision)
    (hbNum : b.vNum = b'.vNum)
    (ha'Bound : Fbounded (beta:=beta) b' a')
    (ha'ExpLt : a'.Fexp < -b.dExp) :
    |_root_.F2R (beta:=beta) a'| <
      _root_.F2R (beta:=beta)
        (firstNormalPos (beta:=beta) radix b precision) := by
  subst beta
  have hradixPosInt : (0 : Int) < radix := by omega
  have hradixPos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradixPosInt
  have hradixOne : (1 : ℝ) ≤ (radix : ℝ) := by
    exact_mod_cast (le_of_lt hradix)
  have hradixNe : (radix : ℝ) ≠ 0 := ne_of_gt hradixPos
  have haNumReal :
      |(a'.Fnum : ℝ)| < (radix : ℝ) ^ (precision : Int) := by
    have haNumCast :
        ((|a'.Fnum| : Int) : ℝ) < (b.vNum : ℝ) := by
      exact_mod_cast (show |a'.Fnum| < b.vNum by
        simpa [hbNum] using ha'Bound.1)
    have hvNumReal : (b.vNum : ℝ) = (radix : ℝ) ^ (precision : Int) := by
      rw [hvNum, Zpower_nat, Int.cast_pow, ← zpow_natCast]
    simpa [Int.cast_abs, hvNumReal] using haNumCast
  have haPowPos : 0 < (radix : ℝ) ^ a'.Fexp := zpow_pos hradixPos _
  have haPowLe :
      (radix : ℝ) ^ a'.Fexp ≤ (radix : ℝ) ^ (-b.dExp - 1) :=
    zpow_le_zpow_right₀ hradixOne (by omega)
  have hAbsValue :
      |_root_.F2R (beta:=radix) a'| =
        |(a'.Fnum : ℝ)| * (radix : ℝ) ^ a'.Fexp := by
    simp [_root_.F2R, FloatSpec.Core.Defs.F2R, abs_mul,
      abs_of_pos haPowPos]
  have hFirstValue :
      _root_.F2R (beta:=radix)
          (firstNormalPos (beta:=radix) radix b precision) =
        (radix : ℝ) ^ ((precision : Int) - 1 - b.dExp) := by
    have hpredCast : ((precision - 1 : Nat) : Int) =
        (precision : Int) - 1 := by
      omega
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, firstNormalPos, nNormMin]
    rw [Int.cast_pow, ← zpow_natCast]
    rw [hpredCast]
    rw [← zpow_add₀ hradixNe]
    congr 1
  rw [hAbsValue, hFirstValue]
  calc
    |(a'.Fnum : ℝ)| * (radix : ℝ) ^ a'.Fexp
        < (radix : ℝ) ^ (precision : Int) * (radix : ℝ) ^ a'.Fexp :=
          mul_lt_mul_of_pos_right haNumReal haPowPos
    _ ≤ (radix : ℝ) ^ (precision : Int) *
          (radix : ℝ) ^ (-b.dExp - 1) :=
          mul_le_mul_of_nonneg_left haPowLe (by positivity)
    _ = (radix : ℝ) ^ ((precision : Int) - 1 - b.dExp) := by
          rw [← zpow_add₀ hradixNe]
          congr 1
          ring

private lemma closest_abs_le_of_input_abs_le {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (z : ℝ) (f : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hprecision : 1 < precision)
    (hvNum : b.vNum = Zpower_nat radix precision)
    (hzAbs :
      |z| ≤ _root_.F2R (beta:=beta)
        (firstNormalPos (beta:=beta) radix b precision))
    (hfClosest : Closest (beta:=beta) b (radix : ℝ) z f) :
    |_root_.F2R (beta:=beta) f| ≤
      _root_.F2R (beta:=beta)
        (firstNormalPos (beta:=beta) radix b precision) := by
  subst beta
  let upper : FloatSpec.Core.Defs.FlocqFloat radix :=
    firstNormalPos (beta:=radix) radix b precision
  let lower : FloatSpec.Core.Defs.FlocqFloat radix :=
    Fopp (beta:=radix) upper
  have hupperNormal : Fnormal (beta:=radix) radix b upper := by
    have h := firstNormalPosNormal (beta:=radix) radix b precision
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, firstNormalPosNormal_check,
      Id.run, ULift.up_down, upper, Int.cast_ofNat] using h ⟨hradix, hprecision, hvNum⟩
  have hupperBound : Fbounded (beta:=radix) b upper := hupperNormal.1
  have hupperPos :
      0 < _root_.F2R (beta:=radix) upper := by
    exact firstNormalPos_value_pos (beta:=radix) b radix precision rfl hradix
  have hlowerBound : Fbounded (beta:=radix) b lower := by
    simpa [lower, Fbounded, Fopp, FloatSpec.Calc.Operations.Fopp, abs_neg]
      using hupperBound
  have hlowerValue :
      _root_.F2R (beta:=radix) lower =
        -_root_.F2R (beta:=radix) upper := by
    simpa [lower] using
      (FloatSpec.Calc.Operations.F2R_opp (beta:=radix) upper) True.intro
  have hminmax :
      isMin (beta:=radix) b radix z f ∨
        isMax (beta:=radix) b radix z f := by
    have h := ClosestMinOrMax_from_split_radix_payload (beta:=radix) b radix (radix : ℝ)
    have hMinOrMaxP :
        MinOrMaxP_float (beta:=radix) b radix
          (Closest (beta:=radix) b (radix : ℝ)) := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMinOrMax_check, MinOrMaxP_float,
        Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
    exact hMinOrMaxP z f hfClosest
  have hz_le_upper : z ≤ _root_.F2R (beta:=radix) upper :=
    le_trans (le_abs_self z) hzAbs
  have hlower_le_z : _root_.F2R (beta:=radix) lower ≤ z := by
    rw [hlowerValue]
    exact le_trans (neg_le_neg hzAbs) (neg_abs_le z)
  rcases hminmax with hmin | hmax
  · rcases hmin with ⟨_hfBound, hf_le_z, hgreatest⟩
    have hlower_le_f : _root_.F2R (beta:=radix) lower ≤
        _root_.F2R (beta:=radix) f :=
      hgreatest lower hlowerBound hlower_le_z
    have hf_le_upper : _root_.F2R (beta:=radix) f ≤
        _root_.F2R (beta:=radix) upper :=
      le_trans hf_le_z hz_le_upper
    rw [abs_le]
    exact ⟨by simpa [hlowerValue] using hlower_le_f, hf_le_upper⟩
  · rcases hmax with ⟨_hfBound, hz_le_f, hleast⟩
    have hf_le_upper : _root_.F2R (beta:=radix) f ≤
        _root_.F2R (beta:=radix) upper :=
      hleast upper hupperBound hz_le_upper
    have hlower_le_f : _root_.F2R (beta:=radix) lower ≤
        _root_.F2R (beta:=radix) f :=
      le_trans hlower_le_z hz_le_f
    rw [abs_le]
    exact ⟨by simpa [hlowerValue] using hlower_le_f, hf_le_upper⟩

/-! Coq generic Dekker theorem `Underf_Err1`. -/
theorem Underf_Err1 {beta : Int} [ValidRadix beta]
    (b b' : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (a' a : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix precision)
    (hprecision : 1 < precision)
    (hbNum : b.vNum = b'.vNum) (_hbExp : b.dExp ≤ b'.dExp)
    (ha'Bound : Fbounded (beta:=beta) b' a')
    (hClosest : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) a') a) :
    Underf_Err (beta:=beta) b b' radix a a'
      (_root_.F2R (beta:=beta) a') (1 / 2 : ℝ) := by
  constructor
  · exact hClosest
  constructor
  · exact ha'Bound
  constructor
  · by_cases ha'Exp : -b.dExp ≤ a'.Fexp
    · have ha'OldBound : Fbounded (beta:=beta) b a' :=
        Fbounded_of_same_vNum_of_exp_ge b b' a' hbNum ha'Bound ha'Exp
      have hzero := hClosest.2 a' ha'OldBound
      have hdiff :
          _root_.F2R (beta:=beta) a -
              _root_.F2R (beta:=beta) a' = 0 := by
        exact abs_eq_zero.mp (le_antisymm (by simpa using hzero) (abs_nonneg _))
      rw [hdiff, abs_zero]
      have hradixPos : (0 : ℝ) < (radix : ℝ) := by
        exact_mod_cast (show (0 : Int) < radix by omega)
      positivity
    · have ha'ExpLt : a'.Fexp < -b.dExp := lt_of_not_ge ha'Exp
      have hprecisionNe : precision ≠ 0 := by omega
      have hradixPos : (0 : ℝ) < (radix : ℝ) := by
        exact_mod_cast (show (0 : Int) < radix by omega)
      have hunitPos : 0 < (radix : ℝ) ^ (-b.dExp) :=
        zpow_pos hradixPos _
      have haAbsLeFirst :
          |_root_.F2R (beta:=beta) a'| ≤
            _root_.F2R (beta:=beta)
              (firstNormalPos (beta:=beta) radix b precision) :=
        le_of_lt (abs_under_extended_below_firstNormal b b' radix precision a'
          hbeta hradix hprecision hvNum hbNum ha'Bound ha'ExpLt)
      have haAbsLeFirstRounded :
          |_root_.F2R (beta:=beta) a| ≤
            _root_.F2R (beta:=beta)
              (firstNormalPos (beta:=beta) radix b precision) :=
        closest_abs_le_of_input_abs_le b radix precision
          (_root_.F2R (beta:=beta) a') a hbeta hradix hprecision hvNum
          haAbsLeFirst hClosest
      let nf : FloatSpec.Core.Defs.FlocqFloat beta :=
        Fnormalize (beta:=beta) radix b precision a
      have hnCan : Fcanonic (beta:=beta) radix b nf := by
        have h := FnormalizeCanonic (beta:=beta) radix b precision a
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
          Id.run, ULift.up_down, Fbounded', nf, Int.cast_ofNat] using
          h ⟨hClosest.1, hClosest.1, hprecisionNe, hradix, hvNum⟩
      have hnValue :
          _root_.F2R (beta:=beta) nf = _root_.F2R (beta:=beta) a := by
        have h := FnormalizeCorrect (beta:=beta) radix b precision a
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
          Id.run, ULift.up_down, nf, Int.cast_ofNat] using h ⟨hbeta, hradix⟩
      have hnBound : Fbounded (beta:=beta) b nf :=
        (FcanonicBound (beta:=beta) radix b nf) hnCan
      have hfirstCan : Fcanonic (beta:=beta) radix b
          (firstNormalPos (beta:=beta) radix b precision) := by
        exact Or.inl ((firstNormalPosNormal (beta:=beta) radix b precision)
          ⟨hradix, hprecision, hvNum⟩)
      have hnfAbsLeFirst :
          |_root_.F2R (beta:=beta) nf| ≤
            |_root_.F2R (beta:=beta)
              (firstNormalPos (beta:=beta) radix b precision)| := by
        rw [hnValue]
        have hfirstPos := firstNormalPos_value_pos b radix precision hbeta hradix
        simpa [abs_of_pos hfirstPos] using haAbsLeFirstRounded
      have hnExpLe :
          nf.Fexp ≤
            (firstNormalPos (beta:=beta) radix b precision).Fexp := by
        have hbetaGt : 1 < beta := by
          omega
        have hradixEqBeta : radix = beta := hbeta.symm
        have h := Fcanonic_Rle_Zle (beta:=beta) radix b nf
          (firstNormalPos (beta:=beta) radix b precision)
          hbetaGt hradixEqBeta
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fcanonic_Rle_Zle_check,
          ULift.down_up, Fcanonic', Int.cast_ofNat] using h ⟨hnCan, hfirstCan, hnfAbsLeFirst⟩
      have hnExpEq : nf.Fexp = -b.dExp := by
        simp only [firstNormalPos] at hnExpLe
        have hnExpLower : -b.dExp ≤ nf.Fexp := hnBound.2
        omega
      have hfulpMin :
          Fulp (beta:=beta) b radix precision a =
            (radix : ℝ) ^ (-b.dExp) := by
        simp [Fulp, nf, hnExpEq]
      have hClosestUlp :
          2 * |_root_.F2R (beta:=beta) a' -
              _root_.F2R (beta:=beta) a| ≤
            Fulp (beta:=beta) b radix precision a := by
        have h := ClosestUlp (beta:=beta) b radix precision
          (_root_.F2R (beta:=beta) a') a
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          h ⟨hClosest, hbeta, hradix, hprecisionNe, hvNum⟩
      have hdist :
          |_root_.F2R (beta:=beta) a - _root_.F2R (beta:=beta) a'| ≤
            (radix : ℝ) ^ (-b.dExp) / 2 := by
        rw [abs_sub_comm]
        rw [hfulpMin] at hClosestUlp
        nlinarith
      simpa [one_div, div_eq_mul_inv, mul_comm, mul_left_comm, mul_assoc]
        using hdist
  · intro ha'Exp
    have ha'OldBound : Fbounded (beta:=beta) b a' :=
      Fbounded_of_same_vNum_of_exp_ge b b' a' hbNum ha'Bound ha'Exp
    have hzero := hClosest.2 a' ha'OldBound
    exact sub_eq_zero.mp (abs_eq_zero.mp
      (le_antisymm (by simpa using hzero) (abs_nonneg _)))

noncomputable def ClosestRoundeGeNormal_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (z : ℝ) (f : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `ClosestRoundeGeNormal`.

A closest rounded normal float bounds the exact real input by the usual
`1 + radix^(1 - precision) / 2` relative factor. -/
theorem ClosestRoundeGeNormal {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (z : ℝ) (f : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 3 ≤ precision ∧
        b.vNum = Zpower_nat radix precision ∧
        Closest (beta:=beta) b (radix : ℝ) z f ∧
        Fnormal (beta:=beta) radix b f⌝⦄
    (pure (ClosestRoundeGeNormal_check (beta:=beta) b radix precision z f) :
      Id Unit)
    ⦃⇓_ => ⌜|z| ≤ |_root_.F2R (beta:=beta) f| *
        (1 + (radix : ℝ) ^ (1 - (precision : Int)) / 2)⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hradix, hprecision, hvNum, hClosest, hnormal⟩
  simp only [wp, PostCond.noThrow, pure, ClosestRoundeGeNormal_check,
    Id.run, ULift.up_down]
  have hprecision_ne : precision ≠ 0 := by omega
  exact RleRRounded (beta:=beta) b radix precision f z hbeta hradix
    hprecision_ne hvNum hnormal hClosest

noncomputable def ClosestRoundeLeNormal_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (z : ℝ) (f : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `ClosestRoundeLeNormal`.

A closest rounded normal float is bounded by the exact input divided by
`1 - radix^(1 - precision) / 2`. -/
theorem ClosestRoundeLeNormal {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (z : ℝ) (f : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 3 ≤ precision ∧
        b.vNum = Zpower_nat radix precision ∧
        Closest (beta:=beta) b (radix : ℝ) z f ∧
        Fnormal (beta:=beta) radix b f⌝⦄
    (pure (ClosestRoundeLeNormal_check (beta:=beta) b radix precision z f) :
      Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) f| ≤
        |z| / (1 - (radix : ℝ) ^ (1 - (precision : Int)) / 2)⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hradix, hprecision, hvNum, hClosest, hnormal⟩
  simp only [wp, PostCond.noThrow, pure, ClosestRoundeLeNormal_check,
    Id.run, ULift.up_down]
  let F : ℝ := _root_.F2R (beta:=beta) f
  let eps : ℝ := (radix : ℝ) ^ (1 - (precision : Int))
  have hprecision_ne : precision ≠ 0 := by omega
  have hNormEq :
      Fnormalize (beta:=beta) radix b precision f = f := by
    have h := FcanonicFnormalizeEq (beta:=beta) radix b precision f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨Or.inl hnormal, hbeta, hradix, hprecision_ne, hvNum⟩
  have hNormalNorm :
      Fnormal (beta:=beta) radix b
        (Fnormalize (beta:=beta) radix b precision f) := by
    simpa [hNormEq] using hnormal
  have hdist :
      |z - F| ≤ |F| * ((1 / 2 : ℝ) * eps) := by
    have h := ClosestErrorBoundNormal_internal (beta:=beta) b radix precision f z
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestErrorBoundNormal_check,
      Id.run, ULift.up_down, F, eps, Int.cast_ofNat] using
      h ⟨hbeta, hradix, hprecision_ne, hvNum, hnormal.1, hNormalNorm, hClosest⟩
  have htri : |F| ≤ |z| + |z - F| := by
    calc
      |F| = |z + (F - z)| := by ring_nf
      _ ≤ |z| + |F - z| := abs_add_le _ _
      _ = |z| + |z - F| := by rw [abs_sub_comm]
  have hden_pos : 0 < 1 - eps / 2 := by
    have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
      exact_mod_cast (lt_trans Int.zero_lt_one hradix)
    have hradix_one_le : (1 : ℝ) ≤ (radix : ℝ) := by
      exact_mod_cast (le_of_lt hradix)
    have hpow_le_one : eps ≤ 1 := by
      dsimp [eps]
      exact zpow_le_one_of_nonpos₀ hradix_one_le (by omega)
    have hpow_pos : 0 < eps := by
      dsimp [eps]
      exact zpow_pos hradix_pos _
    linarith
  have hmul : |F| * (1 - eps / 2) ≤ |z| := by
    nlinarith
  have hresult : |F| ≤ |z| / (1 - eps / 2) :=
    (le_div_iff₀ hden_pos).2 hmul
  simpa [F, eps] using hresult

/-- Coq generic Dekker local lemma `x2y2Le`.

If both low parts are bounded by half of their radix-scaled magnitudes, their
product is bounded by one quarter of the combined scale. -/
theorem x2y2Le (radix : Int) (s : Nat) (ex ey : Int) (x2 y2 : ℝ)
    (hradix : 1 < radix)
    (hx2Le : |x2| ≤ (radix : ℝ) ^ ((s : Int) + ex) / 2)
    (hy2Le : |y2| ≤ (radix : ℝ) ^ ((s : Int) + ey) / 2) :
    |x2 * y2| ≤
      (radix : ℝ) ^ (2 * (s : Int) + ex + ey) / 4 := by
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (lt_trans Int.zero_lt_one hradix)
  have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
  have hx_bound_nonneg :
      0 ≤ (radix : ℝ) ^ ((s : Int) + ex) / 2 := by
    positivity
  have hmul_bound :
      |x2| * |y2| ≤
        ((radix : ℝ) ^ ((s : Int) + ex) / 2) *
          ((radix : ℝ) ^ ((s : Int) + ey) / 2) := by
    exact mul_le_mul hx2Le hy2Le (abs_nonneg y2) hx_bound_nonneg
  have hpow_mul :
      (radix : ℝ) ^ ((s : Int) + ex) *
          (radix : ℝ) ^ ((s : Int) + ey) =
        (radix : ℝ) ^ (2 * (s : Int) + ex + ey) := by
    rw [← zpow_add₀ hradix_ne]
    congr 1
    ring
  calc
    |x2 * y2| = |x2| * |y2| := abs_mul x2 y2
    _ ≤ ((radix : ℝ) ^ ((s : Int) + ex) / 2) *
          ((radix : ℝ) ^ ((s : Int) + ey) / 2) := hmul_bound
    _ = ((radix : ℝ) ^ ((s : Int) + ex) *
          (radix : ℝ) ^ ((s : Int) + ey)) / 4 := by ring
    _ = (radix : ℝ) ^ (2 * (s : Int) + ex + ey) / 4 := by rw [hpow_mul]

/-- Coq generic Dekker local lemma `x2y1Le`.

This is the real-inequality core of the Flocq local proof.  The magnitude
bound on `y` is the premise Flocq derives from `Fnormal`. -/
theorem x2y1Le (radix : Int) (s t : Nat) (ex ey : Int) (x2 y y1 y2 : ℝ)
    (hradix : 1 < radix)
    (hyEq : y = y1 + y2)
    (hx2Le : |x2| ≤ (radix : ℝ) ^ ((s : Int) + ex) / 2)
    (hyAbs : |y| < (radix : ℝ) ^ ((t : Int) + ey))
    (hx2y2Le :
      |x2 * y2| ≤ (radix : ℝ) ^ (2 * (s : Int) + ex + ey) / 4) :
    |x2 * y1| <
      (radix : ℝ) ^ ((t : Int) + (s : Int) + ex + ey) / 2 +
        (radix : ℝ) ^ (2 * (s : Int) + ex + ey) / 4 := by
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (lt_trans Int.zero_lt_one hradix)
  have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
  have hrewrite : x2 * y1 = x2 * y + -(x2 * y2) := by
    rw [hyEq]
    ring
  have htri :
      |x2 * y1| ≤ |x2 * y| + |x2 * y2| := by
    rw [hrewrite]
    simpa [abs_neg] using abs_add_le (x2 * y) (-(x2 * y2))
  have hx_bound_pos :
      0 < (radix : ℝ) ^ ((s : Int) + ex) / 2 := by
    have hpow : 0 < (radix : ℝ) ^ ((s : Int) + ex) := zpow_pos hradix_pos _
    linarith
  have hxy :
      |x2 * y| <
        ((radix : ℝ) ^ ((s : Int) + ex) / 2) *
          (radix : ℝ) ^ ((t : Int) + ey) := by
    calc
      |x2 * y| = |x2| * |y| := abs_mul x2 y
      _ < ((radix : ℝ) ^ ((s : Int) + ex) / 2) *
            (radix : ℝ) ^ ((t : Int) + ey) :=
          mul_lt_mul_of_le_of_lt_of_nonneg_of_pos hx2Le hyAbs
            (abs_nonneg y) hx_bound_pos
  have hpow_mul :
      (radix : ℝ) ^ ((s : Int) + ex) *
          (radix : ℝ) ^ ((t : Int) + ey) =
        (radix : ℝ) ^ ((t : Int) + (s : Int) + ex + ey) := by
    rw [← zpow_add₀ hradix_ne]
    congr 1
    ring
  have hxy' :
      |x2 * y| <
        (radix : ℝ) ^ ((t : Int) + (s : Int) + ex + ey) / 2 := by
    calc
      |x2 * y| <
          ((radix : ℝ) ^ ((s : Int) + ex) / 2) *
            (radix : ℝ) ^ ((t : Int) + ey) := hxy
      _ = (radix : ℝ) ^ ((t : Int) + (s : Int) + ex + ey) / 2 := by
          rw [← hpow_mul]
          ring
  exact lt_of_le_of_lt htri <|
    lt_of_lt_of_le
      (add_lt_add_left hxy' |x2 * y2|)
      (add_le_add_right hx2y2Le
        ((radix : ℝ) ^ ((t : Int) + (s : Int) + ex + ey) / 2))

/-- Coq generic Dekker local lemma `x1y2Le`.

Symmetric counterpart of `x2y1Le`; the magnitude bound on `x` is the premise
Flocq derives from `Fnormal`. -/
theorem x1y2Le (radix : Int) (s t : Nat) (ex ey : Int) (x x1 x2 y2 : ℝ)
    (hradix : 1 < radix)
    (hxEq : x = x1 + x2)
    (hxAbs : |x| < (radix : ℝ) ^ ((t : Int) + ex))
    (hy2Le : |y2| ≤ (radix : ℝ) ^ ((s : Int) + ey) / 2)
    (hx2y2Le :
      |x2 * y2| ≤ (radix : ℝ) ^ (2 * (s : Int) + ex + ey) / 4) :
    |x1 * y2| <
      (radix : ℝ) ^ ((t : Int) + (s : Int) + ex + ey) / 2 +
        (radix : ℝ) ^ (2 * (s : Int) + ex + ey) / 4 := by
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (lt_trans Int.zero_lt_one hradix)
  have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
  have hrewrite : x1 * y2 = x * y2 + -(x2 * y2) := by
    rw [hxEq]
    ring
  have htri :
      |x1 * y2| ≤ |x * y2| + |x2 * y2| := by
    rw [hrewrite]
    simpa [abs_neg] using abs_add_le (x * y2) (-(x2 * y2))
  have hy_bound_pos :
      0 < (radix : ℝ) ^ ((s : Int) + ey) / 2 := by
    have hpow : 0 < (radix : ℝ) ^ ((s : Int) + ey) := zpow_pos hradix_pos _
    linarith
  have hxy :
      |x * y2| <
        (radix : ℝ) ^ ((t : Int) + ex) *
          ((radix : ℝ) ^ ((s : Int) + ey) / 2) := by
    calc
      |x * y2| = |x| * |y2| := abs_mul x y2
      _ ≤ |x| * ((radix : ℝ) ^ ((s : Int) + ey) / 2) :=
          mul_le_mul_of_nonneg_left hy2Le (abs_nonneg x)
      _ < (radix : ℝ) ^ ((t : Int) + ex) *
            ((radix : ℝ) ^ ((s : Int) + ey) / 2) :=
          mul_lt_mul_of_pos_right hxAbs hy_bound_pos
  have hpow_mul :
      (radix : ℝ) ^ ((t : Int) + ex) *
          (radix : ℝ) ^ ((s : Int) + ey) =
        (radix : ℝ) ^ ((t : Int) + (s : Int) + ex + ey) := by
    rw [← zpow_add₀ hradix_ne]
    congr 1
    ring
  have hxy' :
      |x * y2| <
        (radix : ℝ) ^ ((t : Int) + (s : Int) + ex + ey) / 2 := by
    calc
      |x * y2| <
          (radix : ℝ) ^ ((t : Int) + ex) *
            ((radix : ℝ) ^ ((s : Int) + ey) / 2) := hxy
      _ = (radix : ℝ) ^ ((t : Int) + (s : Int) + ex + ey) / 2 := by
          rw [← hpow_mul]
          ring
  exact lt_of_le_of_lt htri <|
    lt_of_lt_of_le
      (add_lt_add_left hxy' |x2 * y2|)
      (add_le_add_right hx2y2Le
        ((radix : ℝ) ^ ((t : Int) + (s : Int) + ex + ey) / 2))

/-- Coq generic Dekker local lemma `powerRZSumRle`.

For radix at least two, the sum of two powers is bounded by the successor power
of the larger exponent. -/
theorem powerRZSumRle (radix : Int) (e1 e2 : Int)
    (hradix : 1 < radix) (he : e2 ≤ e1) :
    (radix : ℝ) ^ e1 + (radix : ℝ) ^ e2 ≤
      (radix : ℝ) ^ (e1 + 1) := by
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (lt_trans Int.zero_lt_one hradix)
  have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
  have hradix_one_le : (1 : ℝ) ≤ (radix : ℝ) := by
    exact_mod_cast (le_of_lt hradix)
  have hpow_mono :
      (radix : ℝ) ^ e2 ≤ (radix : ℝ) ^ e1 :=
    zpow_le_zpow_right₀ hradix_one_le he
  have htwo_le_radix : (2 : ℝ) ≤ (radix : ℝ) := by
    exact_mod_cast (by omega : (2 : Int) ≤ radix)
  have hpow_nonneg : 0 ≤ (radix : ℝ) ^ e1 := le_of_lt (zpow_pos hradix_pos e1)
  calc
    (radix : ℝ) ^ e1 + (radix : ℝ) ^ e2
        ≤ (radix : ℝ) ^ e1 + (radix : ℝ) ^ e1 := by
          exact add_le_add (le_refl _) hpow_mono
    _ = (radix : ℝ) ^ e1 * 2 := by ring
    _ ≤ (radix : ℝ) ^ e1 * (radix : ℝ) := by
          exact mul_le_mul_of_nonneg_left htwo_le_radix hpow_nonneg
    _ = (radix : ℝ) ^ (e1 + 1) := by
          rw [zpow_add₀ hradix_ne]
          simp

/-- Coq Dekker local lemma `SLe` for `s = t - Nat.div2 t`. -/
theorem SLe (t : Nat) (pGe : 4 ≤ t) :
    (2 : Int) ≤ (t : Int) - (Nat.div2 t : Int) := by
  rw [Nat.div2_val]
  have hmul : (t / 2) * 2 ≤ t := Nat.div_mul_le_self t 2
  omega

/-- Coq Dekker local lemma `SGe` for `s = t - Nat.div2 t`. -/
theorem SGe (t : Nat) (pGe : 4 ≤ t) :
    (t : Int) - (Nat.div2 t : Int) ≤ (t : Int) - 2 := by
  rw [Nat.div2_val]
  have hdiv_ge : 2 ≤ t / 2 :=
    (Nat.le_div_iff_mul_le (by decide : 0 < 2)).2 pGe
  omega

/-- Coq Dekker local lemma `s2Ge`: `t <= s + s`. -/
theorem s2Ge (t : Nat) :
    (t : Int) ≤
      ((t : Int) - (Nat.div2 t : Int)) +
        ((t : Int) - (Nat.div2 t : Int)) := by
  rw [Nat.div2_val]
  have hmul : (t / 2) * 2 ≤ t := Nat.div_mul_le_self t 2
  omega

/-- Coq Dekker local lemma `s2Le`: `s + s <= t + 1`. -/
theorem s2Le_internal (t : Nat) :
    ((t : Int) - (Nat.div2 t : Int)) +
        ((t : Int) - (Nat.div2 t : Int)) ≤
      (t : Int) + 1 := by
  rw [Nat.div2_val]
  have hmod : t = 2 * (t / 2) + t % 2 := (Nat.div_add_mod t 2).symm
  have hmod_lt : t % 2 < 2 := Nat.mod_lt t (by decide : 0 < 2)
  omega

/-- Coq Dekker local lemma `s2Le` with the section variables that survive
generalization.  Although the proof does not use the bound or `4 ≤ t`, they
are part of the exported Coq contract and therefore remain public. -/
theorem s2Le (_b : Fbound_skel) (t : Nat) (_pGe : 4 ≤ t) :
    ((t : Int) - (Nat.div2 t : Int)) +
        ((t : Int) - (Nat.div2 t : Int)) ≤
      (t : Int) + 1 :=
  s2Le_internal t

/-- Coq generic FMA local lemma `UnMoinsPos`.

For precision at least three, the factor
`1 - radix^(1 - precision) / 2` is positive. -/
theorem UnMoinsPos (radix : Int) (precision : Nat)
    (hradix : 1 < radix) (hprecision : 3 ≤ precision) :
    0 < 1 - (radix : ℝ) ^ (1 - (precision : Int)) / 2 := by
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (lt_trans Int.zero_lt_one hradix)
  have hradix_one_le : (1 : ℝ) ≤ (radix : ℝ) := by
    exact_mod_cast (le_of_lt hradix)
  have hpow_le_one :
      (radix : ℝ) ^ (1 - (precision : Int)) ≤ 1 :=
    zpow_le_one_of_nonpos₀ hradix_one_le (by omega)
  have hpow_pos : 0 < (radix : ℝ) ^ (1 - (precision : Int)) :=
    zpow_pos hradix_pos _
  linarith

/-- Coq generic FMA local lemma `abeLeab`.

This is the real-inequality payload of the Flocq proof after the upstream
context has derived `|e| <= |a + b| / 4` from the ulp bound. -/
theorem abeLeab_from_quarter_error_payload (a b e : ℝ)
    (he : |e| ≤ |a + b| / 4) :
    |a + b| ≤ |a + b + e| * 4 / 3 := by
  have htri : |a + b| ≤ |a + b + e| + |e| := by
    calc
      |a + b| = |(a + b + e) + (-e)| := by ring_nf
      _ ≤ |a + b + e| + |-e| := abs_add_le _ _
      _ = |a + b + e| + |e| := by rw [abs_neg]
  have hmain : |a + b| ≤ |a + b + e| + |a + b| / 4 := by
    exact le_trans htri (add_le_add_right he _)
  nlinarith [abs_nonneg (a + b)]

/-- Coq: `abeLeab`, with the section's ulp-error premise restored. -/
theorem abeLeab {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (a b e : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hprecision : 3 ≤ precision)
    (hvNum : bo.vNum = Zpower_nat radix precision)
    (heLeb : |_root_.F2R (beta:=beta) e| ≤ (1 / 2 : ℝ) *
      Fulp (beta:=beta) bo radix precision b)
    (hbCan : Fcanonic (beta:=beta) radix bo b)
    (hbAbsLe : |_root_.F2R (beta:=beta) b| ≤
      |_root_.F2R (beta:=beta) a|)
    (hsumLarge : 2 * (radix : ℝ) ^ b.Fexp ≤
      |_root_.F2R (beta:=beta) a + _root_.F2R (beta:=beta) b|) :
    |_root_.F2R (beta:=beta) a + _root_.F2R (beta:=beta) b| ≤
      |_root_.F2R (beta:=beta) a + _root_.F2R (beta:=beta) b +
        _root_.F2R (beta:=beta) e| * 4 / 3 := by
  have hprecision_ne : precision ≠ 0 := by omega
  have hnorm : Fnormalize (beta:=beta) radix bo precision b = b := by
    have h := FcanonicFnormalizeEq (beta:=beta) radix bo precision b
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hbCan, hbeta, hradix, hprecision_ne, hvNum⟩
  have hFulpEq :
      Fulp (beta:=beta) bo radix precision b = (radix : ℝ) ^ b.Fexp := by
    simp [Fulp, hnorm]
  have heQuarter :
      |_root_.F2R (beta:=beta) e| ≤
        |_root_.F2R (beta:=beta) a + _root_.F2R (beta:=beta) b| / 4 := by
    rw [hFulpEq] at heLeb
    nlinarith
  exact abeLeab_from_quarter_error_payload
    (_root_.F2R (beta:=beta) a) (_root_.F2R (beta:=beta) b)
      (_root_.F2R (beta:=beta) e) heQuarter

noncomputable def xLe2y_aux1_check {beta : Int} [ValidRadix beta]
    (_bo : Fbound_skel) (_radix : Int) (_precision : Nat)
    (_a _b _e _x _y : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq generic FMA local lemma `xLe2y_aux1`.

In the exact-power branch, the rounded value `x` is the exact sum `a + b`.
The even radix gives a bounded half-ulp witness below `|a + b + e|`, and
absolute monotonicity of closest rounding transfers that lower bound to `y`. -/
theorem xLe2y_aux1 {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b e x y : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ bo.vNum = Zpower_nat radix precision ∧
        3 ≤ precision ∧ Even radix ∧
        |_root_.F2R (beta:=beta) e| ≤
          (1 / 2 : ℝ) * Fulp (beta:=beta) bo radix precision b ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a + _root_.F2R (beta:=beta) b) x ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a + _root_.F2R (beta:=beta) b +
            _root_.F2R (beta:=beta) e) y ∧
        Fcanonic (beta:=beta) radix bo b ∧ -bo.dExp < b.Fexp ∧
        |_root_.F2R (beta:=beta) b| ≤ |_root_.F2R (beta:=beta) a| ∧
        (radix : ℝ) ^ b.Fexp =
          |_root_.F2R (beta:=beta) a + _root_.F2R (beta:=beta) b|⌝⦄
    (pure (xLe2y_aux1_check (beta:=beta) bo radix precision a b e x y) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) x| ≤
      2 * |_root_.F2R (beta:=beta) y|⌝⦄ := by
  intro hpre
  rcases hpre with
    ⟨hbeta, hradix, hvNum, hprecision_ge, hEven, heBound, hxClosest, hyClosest,
      hbCan, hbExpGt, _hbAbsLe, habPow⟩
  subst beta
  have hBoundExp :
      ∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=radix) radix r).Fexp := by
    intro r
    exact le_trans (neg_nonpos.mpr bo.dExp_nonneg) (by simp [boundR, boundNat])
  simp only [wp, PostCond.noThrow, pure, xLe2y_aux1_check, Id.run, ULift.up_down]
  let r : ℝ := _root_.F2R (beta:=radix) a + _root_.F2R (beta:=radix) b
  let r' : ℝ := r + _root_.F2R (beta:=radix) e
  let unitB : FloatSpec.Core.Defs.FlocqFloat radix := ⟨1, b.Fexp⟩
  have hprecision_ne : precision ≠ 0 := by omega
  have hvNum_gt_one : (1 : Int) < bo.vNum := by
    have h := vNumbMoreThanOne bo radix precision
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, vNumbMoreThanOne_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨hprecision_ne, hradix, hvNum⟩
  have hunitBound : Fbounded (beta:=radix) bo unitB := by
    exact ⟨by simpa [unitB] using hvNum_gt_one, le_of_lt hbExpGt⟩
  have hunitVal : _root_.F2R (beta:=radix) unitB = (radix : ℝ) ^ b.Fexp := by
    simp [unitB, _root_.F2R, FloatSpec.Core.Defs.F2R]
  have hunitValR : _root_.F2R (beta:=radix) unitB = |r| := by
    simpa [r, hunitVal] using habPow
  have hxExact : _root_.F2R (beta:=radix) x = r := by
    by_cases hr_nonneg : 0 ≤ r
    · have hunitValR' : _root_.F2R (beta:=radix) unitB = r := by
        simpa [abs_of_nonneg hr_nonneg] using hunitValR
      have hdist := hxClosest.2 unitB hunitBound
      have hzero : |_root_.F2R (beta:=radix) x - r| = 0 := by
        rw [hunitValR', sub_self, abs_zero] at hdist
        exact le_antisymm hdist (abs_nonneg _)
      exact sub_eq_zero.mp (abs_eq_zero.mp hzero)
    · have hr_neg : r < 0 := lt_of_not_ge hr_nonneg
      let negUnit : FloatSpec.Core.Defs.FlocqFloat radix := Fopp (beta:=radix) unitB
      have hnegUnitBound : Fbounded (beta:=radix) bo negUnit := by
        have h := oppBounded (beta:=radix) bo unitB
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, oppBounded_check, Id.run,
          ULift.up_down, negUnit, Int.cast_ofNat] using h hunitBound
      have hnegUnitVal :
          _root_.F2R (beta:=radix) negUnit = r := by
        have hopp :
            _root_.F2R (beta:=radix) negUnit =
              -_root_.F2R (beta:=radix) unitB := by
          simpa [negUnit] using
            (FloatSpec.Calc.Operations.F2R_opp (beta:=radix) unitB) True.intro
        rw [hopp, hunitValR, abs_of_neg hr_neg]
        ring
      have hdist := hxClosest.2 negUnit hnegUnitBound
      have hzero : |_root_.F2R (beta:=radix) x - r| = 0 := by
        rw [hnegUnitVal, sub_self, abs_zero] at hdist
        exact le_antisymm hdist (abs_nonneg _)
      exact sub_eq_zero.mp (abs_eq_zero.mp hzero)
  have hbBound : Fbounded (beta:=radix) bo b := by
    have h := FcanonicBound (beta:=radix) radix bo b
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicBound_check, Int.cast_ofNat] using h hbCan
  have hFulpEq :
      Fulp (beta:=radix) bo radix precision b = (radix : ℝ) ^ b.Fexp := by
    have hnorm := FcanonicFnormalizeEq (beta:=radix) radix bo precision b
    have hnorm' :
        Fnormalize (beta:=radix) radix bo precision b = b := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        hnorm ⟨hbCan, rfl, hradix, hprecision_ne, hvNum⟩
    simp [Fulp, hnorm']
  have heBoundR : |_root_.F2R (beta:=radix) e| ≤ (1 / 2 : ℝ) * |r| := by
    calc
      |_root_.F2R (beta:=radix) e| ≤
          (1 / 2 : ℝ) * Fulp (beta:=radix) bo radix precision b := heBound
      _ = (1 / 2 : ℝ) * (radix : ℝ) ^ b.Fexp := by rw [hFulpEq]
      _ = (1 / 2 : ℝ) * |r| := by rw [habPow]
  rcases hEven with ⟨halfRadix, hradix_half⟩
  let halfB : FloatSpec.Core.Defs.FlocqFloat radix := ⟨halfRadix, b.Fexp - 1⟩
  have hradix_pos_int : (0 : Int) < radix := by omega
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by exact_mod_cast hradix_pos_int
  have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
  have hhalf_nonneg : (0 : Int) ≤ halfRadix := by omega
  have hhalf_abs : |halfRadix| = halfRadix := abs_of_nonneg hhalf_nonneg
  have hhalfBound : Fbounded (beta:=radix) bo halfB := by
    constructor
    · have hhalf_lt : |halfRadix| < bo.vNum := by
        rw [hhalf_abs]
        have hhalf_le : halfRadix ≤ radix := by omega
        have hradix_lt_vNum : radix < bo.vNum := by
          rw [hvNum, Zpower_nat]
          cases precision with
          | zero => omega
          | succ p' =>
              cases p' with
              | zero => omega
              | succ p'' =>
                  rw [pow_succ, pow_succ]
                  have hpow_pos : 0 < radix ^ p'' := pow_pos hradix_pos_int p''
                  have hpow_ge_one : 1 ≤ radix ^ p'' := by omega
                  have hradix_sq_pos : 0 ≤ radix * radix := by nlinarith
                  have hsq_gt : radix < radix * radix := by nlinarith
                  have hright_ge :
                      radix * radix ≤ radix ^ p'' * radix * radix := by
                    have hright_ge' :
                        1 * (radix * radix) ≤ radix ^ p'' * (radix * radix) :=
                      mul_le_mul_of_nonneg_right hpow_ge_one hradix_sq_pos
                    simpa [one_mul, mul_assoc] using hright_ge'
                  exact lt_of_lt_of_le hsq_gt hright_ge
        exact lt_of_le_of_lt hhalf_le hradix_lt_vNum
      simpa [Fbounded, halfB] using hhalf_lt
    · simp [halfB]
      omega
  have hhalfVal : _root_.F2R (beta:=radix) halfB = (1 / 2 : ℝ) * |r| := by
    calc
      _root_.F2R (beta:=radix) halfB
          = (halfRadix : ℝ) * (radix : ℝ) ^ (b.Fexp - 1) := by
              simp [halfB, _root_.F2R, FloatSpec.Core.Defs.F2R]
      _ = ((radix : ℝ) / 2) * (radix : ℝ) ^ (b.Fexp - 1) := by
              have hhalf : (halfRadix : ℝ) = (radix : ℝ) / 2 := by
                have hcast : (radix : ℝ) = (2 : ℝ) * halfRadix := by
                  have hcast' : (radix : ℝ) =
                      (halfRadix : ℝ) + (halfRadix : ℝ) := by
                    exact_mod_cast hradix_half
                  nlinarith
                nlinarith
              rw [hhalf]
      _ = (1 / 2 : ℝ) * (radix : ℝ) ^ b.Fexp := by
              have hpow :
                  (radix : ℝ) ^ (b.Fexp - 1) * (radix : ℝ) =
                    (radix : ℝ) ^ b.Fexp := by
                have hsum : b.Fexp - 1 + 1 = b.Fexp := by omega
                calc
                  (radix : ℝ) ^ (b.Fexp - 1) * (radix : ℝ)
                      = (radix : ℝ) ^ (b.Fexp - 1) * (radix : ℝ) ^ (1 : Int) := by
                          rw [zpow_one]
                  _ = (radix : ℝ) ^ (b.Fexp - 1 + 1) := by
                          rw [zpow_add₀ hradix_ne]
                  _ = (radix : ℝ) ^ b.Fexp := by rw [hsum]
              calc
                ((radix : ℝ) / 2) * (radix : ℝ) ^ (b.Fexp - 1)
                    = (1 / 2 : ℝ) *
                        ((radix : ℝ) ^ (b.Fexp - 1) * (radix : ℝ)) := by
                        ring
                _ = (1 / 2 : ℝ) * (radix : ℝ) ^ b.Fexp := by rw [hpow]
      _ = (1 / 2 : ℝ) * |r| := by rw [habPow]
  have hhalfLeAbsR' : _root_.F2R (beta:=radix) halfB ≤ |r'| := by
    have htri : |r| ≤ |r'| + |_root_.F2R (beta:=radix) e| := by
      calc
        |r| = |r' + (-_root_.F2R (beta:=radix) e)| := by simp [r']
        _ ≤ |r'| + |-_root_.F2R (beta:=radix) e| := abs_add_le _ _
        _ = |r'| + |_root_.F2R (beta:=radix) e| := by rw [abs_neg]
    have hmain : (1 / 2 : ℝ) * |r| ≤ |r'| := by
      nlinarith [abs_nonneg r]
    simpa [hhalfVal] using hmain
  have hMinTotal : TotalP (isMin (beta:=radix) bo radix) := by
    intro z
    have h := MinEx_from_finite_box_payload (beta:=radix) bo radix z
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hvNum_gt_one, hBoundExp z⟩
  have hMaxTotal : TotalP (isMax (beta:=radix) bo radix) := by
    intro z
    have h := MaxEx_from_finite_box_payload (beta:=radix) bo radix z
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MaxEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hvNum_gt_one, hBoundExp z⟩
  have hTotalClosest : TotalP (Closest (beta:=radix) bo (radix : ℝ)) := by
    intro z
    have h := ClosestTotal_from_extrema_payload (beta:=radix) bo radix (radix : ℝ) z
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestTotal_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨hMinTotal, hMaxTotal⟩
  have hRounded :
      RoundedModeP_float (beta:=radix) bo radix
        (Closest (beta:=radix) bo (radix : ℝ)) := by
    refine ⟨hTotalClosest, ?_, ?_, ?_⟩
    · have h := ClosestCompatible_from_structural_payload (beta:=radix) bo (radix : ℝ)
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestCompatible_check,
        PredTrans.pure, PredTrans.apply, Id.run, ULift.down, Int.cast_ofNat] using h trivial
    · have h := ClosestMinOrMax_from_split_radix_payload (beta:=radix) bo radix (radix : ℝ)
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMinOrMax_check, MinOrMaxP_float,
        Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
    · have h := ClosestMonotone_from_split_radix_payload (beta:=radix) bo (radix : ℝ)
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMonotone_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hmono := RoundAbsMonotonel_from_rounded_payload (beta:=radix) bo radix
    (Closest (beta:=radix) bo (radix : ℝ)) r' y halfB
  have hhalfLeY :
      _root_.F2R (beta:=radix) halfB ≤ |_root_.F2R (beta:=radix) y| := by
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RoundAbsMonotonel_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      hmono ⟨hRounded, hhalfBound, hyClosest, hhalfLeAbsR'⟩
  calc
    |_root_.F2R (beta:=radix) x| = |r| := by rw [hxExact]
    _ = 2 * _root_.F2R (beta:=radix) halfB := by rw [hhalfVal]; ring
    _ ≤ 2 * |_root_.F2R (beta:=radix) y| := by nlinarith

private lemma xLe2y_sum_trichotomy {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix]
    (a b : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (haCan : Fcanonic (beta:=beta) radix bo a)
    (hbCan : Fcanonic (beta:=beta) radix bo b)
    (hbAbsLe :
      |_root_.F2R (beta:=beta) b| ≤ |_root_.F2R (beta:=beta) a|) :
    (_root_.F2R (beta:=beta) a + _root_.F2R (beta:=beta) b = 0) ∨
      (radix : ℝ) ^ b.Fexp =
        |_root_.F2R (beta:=beta) a + _root_.F2R (beta:=beta) b| ∨
      2 * (radix : ℝ) ^ b.Fexp ≤
        |_root_.F2R (beta:=beta) a + _root_.F2R (beta:=beta) b| := by
  subst beta
  let sum : FloatSpec.Core.Defs.FlocqFloat radix := Fplus (beta:=radix) a b
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (lt_trans Int.zero_lt_one hradix)
  have hpow_pos : 0 < (radix : ℝ) ^ b.Fexp := zpow_pos hradix_pos _
  have hbExpLeA : b.Fexp ≤ a.Fexp := by
    have h := Fcanonic_Rle_Zle (beta:=radix) radix bo b a hradix rfl
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fcanonic_Rle_Zle_check,
      ULift.down_up, Fcanonic', Int.cast_ofNat] using h ⟨hbCan, haCan, hbAbsLe⟩
  have hsumExp : sum.Fexp = b.Fexp := by
    have h := Fplus_exp_eq_min (beta:=radix) a b
    simpa [sum, min_eq_right hbExpLeA] using h
  have hsumValue :
      _root_.F2R (beta:=radix) sum =
        _root_.F2R (beta:=radix) a + _root_.F2R (beta:=radix) b := by
    have h := Fplus_correct (beta:=radix) a b
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fplus_correct_check, Id.run,
      ULift.up_down, sum, Int.cast_ofNat] using h hradix
  have hsumAbs :
      |_root_.F2R (beta:=radix) a + _root_.F2R (beta:=radix) b| =
        |(sum.Fnum : ℝ)| * (radix : ℝ) ^ b.Fexp := by
    calc
      |_root_.F2R (beta:=radix) a + _root_.F2R (beta:=radix) b|
          = |_root_.F2R (beta:=radix) sum| := by rw [hsumValue]
      _ = |(sum.Fnum : ℝ) * (radix : ℝ) ^ sum.Fexp| := by
            simp only [_root_.F2R, FloatSpec.Core.Defs.F2R]
      _ = |(sum.Fnum : ℝ)| * (radix : ℝ) ^ sum.Fexp := by
            rw [abs_mul, abs_of_pos (zpow_pos hradix_pos sum.Fexp)]
      _ = |(sum.Fnum : ℝ)| * (radix : ℝ) ^ b.Fexp := by rw [hsumExp]
  by_cases hzero : sum.Fnum = 0
  · left
    calc
      _root_.F2R (beta:=radix) a + _root_.F2R (beta:=radix) b =
          _root_.F2R (beta:=radix) sum := hsumValue.symm
      _ = 0 := by simp [_root_.F2R, FloatSpec.Core.Defs.F2R, hzero]
  · have hnum_pos : 0 < |sum.Fnum| := abs_pos.mpr hzero
    by_cases hunit : |sum.Fnum| = 1
    · right
      left
      have hcoef : |(sum.Fnum : ℝ)| = 1 := by
        rw [← Int.cast_abs]
        exact_mod_cast hunit
      calc
        (radix : ℝ) ^ b.Fexp =
            |(sum.Fnum : ℝ)| * (radix : ℝ) ^ b.Fexp := by rw [hcoef, one_mul]
        _ = |_root_.F2R (beta:=radix) a +
              _root_.F2R (beta:=radix) b| := hsumAbs.symm
    · right
      right
      have hnum_ge_two : (2 : Int) ≤ |sum.Fnum| := by omega
      have hcoef : (2 : ℝ) ≤ |(sum.Fnum : ℝ)| := by
        rw [← Int.cast_abs]
        exact_mod_cast hnum_ge_two
      calc
        2 * (radix : ℝ) ^ b.Fexp ≤
            |(sum.Fnum : ℝ)| * (radix : ℝ) ^ b.Fexp :=
              mul_le_mul_of_nonneg_right hcoef (le_of_lt hpow_pos)
        _ = |_root_.F2R (beta:=radix) a +
              _root_.F2R (beta:=radix) b| := hsumAbs.symm

private lemma xLe2y_factor_bound (radix : Int) (precision : Nat)
    (hradix : 1 < radix) (hprecision : 3 ≤ precision) :
    (1 + (radix : ℝ) ^ (1 - (precision : Int)) / 2) * 4 / 3 ≤
      2 * (1 - (radix : ℝ) ^ (1 - (precision : Int)) / 2) := by
  let eps : ℝ := (radix : ℝ) ^ (1 - (precision : Int))
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (lt_trans Int.zero_lt_one hradix)
  have hradix_one_le : (1 : ℝ) ≤ (radix : ℝ) := by
    exact_mod_cast (le_of_lt hradix)
  have hradix_two_le : (2 : ℝ) ≤ (radix : ℝ) := by
    exact_mod_cast (by omega : (2 : Int) ≤ radix)
  have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
  have heps_le_neg_two :
      eps ≤ (radix : ℝ) ^ (-(2 : Int)) := by
    dsimp [eps]
    exact zpow_le_zpow_right₀ hradix_one_le (by omega)
  have hradix_sq_ge_four : (4 : ℝ) ≤ (radix : ℝ) ^ (2 : Int) := by
    norm_num [zpow_ofNat]
    nlinarith
  have hneg_two_le_quarter :
      (radix : ℝ) ^ (-(2 : Int)) ≤ (1 / 4 : ℝ) := by
    have hpow_pos : 0 < (radix : ℝ) ^ (2 : Int) := zpow_pos hradix_pos _
    have hdiv := one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 4)
      hradix_sq_ge_four
    simpa [zpow_neg, one_div, hradix_ne, zpow_ofNat] using hdiv
  have heps_le_quarter : eps ≤ (1 / 4 : ℝ) :=
    le_trans heps_le_neg_two hneg_two_le_quarter
  dsimp [eps] at heps_le_quarter ⊢
  nlinarith

noncomputable def xLe2y_aux2_check {beta : Int} [ValidRadix beta]
    (_bo : Fbound_skel) (_radix : Int) (_precision : Nat)
    (_a _b _e _x _y : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq generic FMA local lemma `xLe2y_aux2`.

This restores the full GenericA branch immediately after `xLe2y_aux1`: the
zero and exact-power cases follow the upstream `Fplus`/`Fabs` split, and the
large-sum case combines the normal closest-rounding relative bounds with
`abeLeab`. -/
theorem xLe2y_aux2 {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b e x y : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ bo.vNum = Zpower_nat radix precision ∧
        3 ≤ precision ∧ Even radix ∧
        |_root_.F2R (beta:=beta) e| ≤
          (1 / 2 : ℝ) * Fulp (beta:=beta) bo radix precision b ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a + _root_.F2R (beta:=beta) b) x ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a + _root_.F2R (beta:=beta) b +
            _root_.F2R (beta:=beta) e) y ∧
        Fnormal (beta:=beta) radix bo x ∧
        Fnormal (beta:=beta) radix bo y ∧
        Fcanonic (beta:=beta) radix bo b ∧
        Fcanonic (beta:=beta) radix bo a ∧
        -bo.dExp < b.Fexp ∧
        |_root_.F2R (beta:=beta) b| ≤ |_root_.F2R (beta:=beta) a|⌝⦄
    (pure (xLe2y_aux2_check (beta:=beta) bo radix precision a b e x y) :
      Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) x| ≤
      2 * |_root_.F2R (beta:=beta) y|⌝⦄ := by
  intro hpre
  rcases hpre with
    ⟨hbeta, hradix, hvNum, hprecision_ge, hEven, heBound, hxClosest, hyClosest,
      hxNormal, hyNormal, hbCan, haCan, hbExpGt, hbAbsLe⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure, xLe2y_aux2_check, Id.run, ULift.up_down]
  let r : ℝ := _root_.F2R (beta:=radix) a + _root_.F2R (beta:=radix) b
  let r' : ℝ := r + _root_.F2R (beta:=radix) e
  have hprecision_ne : precision ≠ 0 := by omega
  have hvNum_gt_one : (1 : Int) < bo.vNum := by
    have h := vNumbMoreThanOne bo radix precision
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, vNumbMoreThanOne_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨hprecision_ne, hradix, hvNum⟩
  have hbBound : Fbounded (beta:=radix) bo b := by
    have h := FcanonicBound (beta:=radix) radix bo b
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicBound_check, Int.cast_ofNat] using h hbCan
  have hFulpEq :
      Fulp (beta:=radix) bo radix precision b = (radix : ℝ) ^ b.Fexp := by
    have hnorm := FcanonicFnormalizeEq (beta:=radix) radix bo precision b
    have hnorm' :
        Fnormalize (beta:=radix) radix bo precision b = b := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        hnorm ⟨hbCan, rfl, hradix, hprecision_ne, hvNum⟩
    simp [Fulp, hnorm']
  rcases xLe2y_sum_trichotomy (beta:=radix) bo radix a b rfl hradix haCan hbCan hbAbsLe with
    hsumZero | hsumExact | hsumLarge
  · have hzeroBound : Fbounded (beta:=radix) bo (Fzero radix (-bo.dExp)) := by
      have h := FboundedFzero_from_positive_bound_payload (beta:=radix) bo
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FboundedFzero_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using h (lt_trans Int.zero_lt_one hvNum_gt_one)
    have hzeroVal :
        _root_.F2R (beta:=radix) (Fzero radix (-bo.dExp)) = 0 := by
      have h := FzeroisZero (beta:=radix) bo
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FzeroisZero_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using h True.intro
    have hxValZero : _root_.F2R (beta:=radix) x = 0 := by
      have hdist := hxClosest.2 (Fzero radix (-bo.dExp)) hzeroBound
      have hdist0 : |_root_.F2R (beta:=radix) x - 0| = 0 := by
        rw [hsumZero, hzeroVal, sub_self, abs_zero] at hdist
        exact le_antisymm hdist (abs_nonneg _)
      exact sub_eq_zero.mp (abs_eq_zero.mp hdist0)
    rw [hxValZero, abs_zero]
    exact mul_nonneg (by norm_num) (abs_nonneg (_root_.F2R (beta:=radix) y))
  · have h := xLe2y_aux1 (beta:=radix) bo radix precision a b e x y
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, xLe2y_aux1_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, hradix, hvNum, hprecision_ge, hEven, heBound, hxClosest, hyClosest,
        hbCan, hbExpGt, hbAbsLe, hsumExact⟩
  · have hden_pos :
        0 < 1 - (radix : ℝ) ^ (1 - (precision : Int)) / 2 :=
      UnMoinsPos radix precision hradix hprecision_ge
    have heQuarter : |_root_.F2R (beta:=radix) e| ≤ |r| / 4 := by
      calc
        |_root_.F2R (beta:=radix) e| ≤
            (1 / 2 : ℝ) * Fulp (beta:=radix) bo radix precision b := heBound
        _ = (1 / 2 : ℝ) * (radix : ℝ) ^ b.Fexp := by rw [hFulpEq]
        _ ≤ |r| / 4 := by
              have hsumLargeR :
                  2 * (radix : ℝ) ^ b.Fexp ≤ |r| := by
                simpa [r] using hsumLarge
              nlinarith [hsumLargeR]
    have hxLe :
        |_root_.F2R (beta:=radix) x| ≤
          |r| / (1 - (radix : ℝ) ^ (1 - (precision : Int)) / 2) := by
      have h := ClosestRoundeLeNormal (beta:=radix) bo radix precision r x
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestRoundeLeNormal_check,
        Id.run, ULift.up_down, r, Int.cast_ofNat] using
        h ⟨rfl, hradix, hprecision_ge, hvNum, hxClosest, hxNormal⟩
    have hrLe :
        |r| ≤ |r'| * 4 / 3 := by
      exact abeLeab_from_quarter_error_payload
        (_root_.F2R (beta:=radix) a) (_root_.F2R (beta:=radix) b)
        (_root_.F2R (beta:=radix) e)
        heQuarter
    have hyGe :
        |r'| ≤ |_root_.F2R (beta:=radix) y| *
          (1 + (radix : ℝ) ^ (1 - (precision : Int)) / 2) := by
      have h := ClosestRoundeGeNormal (beta:=radix) bo radix precision r' y
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestRoundeGeNormal_check,
        Id.run, ULift.up_down, r, r', Int.cast_ofNat] using
        h ⟨rfl, hradix, hprecision_ge, hvNum, hyClosest, hyNormal⟩
    let den : ℝ := 1 - (radix : ℝ) ^ (1 - (precision : Int)) / 2
    let unplus : ℝ := 1 + (radix : ℝ) ^ (1 - (precision : Int)) / 2
    have hden_pos' : 0 < den := by simpa [den] using hden_pos
    have hxMul : den * |_root_.F2R (beta:=radix) x| ≤ |r| := by
      have h := (le_div_iff₀ hden_pos').1 (by simpa [den] using hxLe)
      nlinarith
    have hfactor : unplus * 4 / 3 ≤ 2 * den := by
      simpa [unplus, den] using xLe2y_factor_bound radix precision hradix hprecision_ge
    have hmain :
        den * |_root_.F2R (beta:=radix) x| ≤
          den * (2 * |_root_.F2R (beta:=radix) y|) := by
      have hchain :
          den * |_root_.F2R (beta:=radix) x| ≤
            |_root_.F2R (beta:=radix) y| * (unplus * 4 / 3) := by
        calc
          den * |_root_.F2R (beta:=radix) x| ≤ |r| := hxMul
          _ ≤ |r'| * 4 / 3 := hrLe
          _ ≤ (|_root_.F2R (beta:=radix) y| * unplus) * 4 / 3 := by
                exact div_le_div_of_nonneg_right
                  (mul_le_mul_of_nonneg_right hyGe (by norm_num)) (by norm_num)
          _ = |_root_.F2R (beta:=radix) y| * (unplus * 4 / 3) := by ring
      calc
        den * |_root_.F2R (beta:=radix) x| ≤
            |_root_.F2R (beta:=radix) y| * (unplus * 4 / 3) := hchain
        _ ≤ |_root_.F2R (beta:=radix) y| * (2 * den) :=
              mul_le_mul_of_nonneg_left hfactor
                (abs_nonneg (_root_.F2R (beta:=radix) y))
        _ = den * (2 * |_root_.F2R (beta:=radix) y|) := by ring
    by_contra hnot
    have hlt :
        2 * |_root_.F2R (beta:=radix) y| <
          |_root_.F2R (beta:=radix) x| := lt_of_not_ge hnot
    have hmul_lt :
        den * (2 * |_root_.F2R (beta:=radix) y|) <
          den * |_root_.F2R (beta:=radix) x| :=
      mul_lt_mul_of_pos_left hlt hden_pos'
    exact not_lt_of_ge hmain hmul_lt

private lemma yLe2x_sum_zero_or_unit {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix]
    (a b : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (haCan : Fcanonic (beta:=beta) radix bo a)
    (hbCan : Fcanonic (beta:=beta) radix bo b)
    (hbAbsLe :
      |_root_.F2R (beta:=beta) b| ≤ |_root_.F2R (beta:=beta) a|) :
    (_root_.F2R (beta:=beta) a + _root_.F2R (beta:=beta) b = 0) ∨
      (radix : ℝ) ^ b.Fexp ≤
        |_root_.F2R (beta:=beta) a + _root_.F2R (beta:=beta) b| := by
  subst beta
  let sum : FloatSpec.Core.Defs.FlocqFloat radix := Fplus (beta:=radix) a b
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (lt_trans Int.zero_lt_one hradix)
  have hpow_pos : 0 < (radix : ℝ) ^ b.Fexp := zpow_pos hradix_pos _
  have hbExpLeA : b.Fexp ≤ a.Fexp := by
    have h := Fcanonic_Rle_Zle (beta:=radix) radix bo b a hradix rfl
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fcanonic_Rle_Zle_check,
      ULift.down_up, Fcanonic', Int.cast_ofNat] using h ⟨hbCan, haCan, hbAbsLe⟩
  have hsumExp : sum.Fexp = b.Fexp := by
    have h := Fplus_exp_eq_min (beta:=radix) a b
    simpa [sum, min_eq_right hbExpLeA] using h
  have hsumValue :
      _root_.F2R (beta:=radix) sum =
        _root_.F2R (beta:=radix) a + _root_.F2R (beta:=radix) b := by
    have h := Fplus_correct (beta:=radix) a b
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fplus_correct_check, Id.run,
      ULift.up_down, sum, Int.cast_ofNat] using h hradix
  have hsumAbs :
      |_root_.F2R (beta:=radix) a + _root_.F2R (beta:=radix) b| =
        |(sum.Fnum : ℝ)| * (radix : ℝ) ^ b.Fexp := by
    calc
      |_root_.F2R (beta:=radix) a + _root_.F2R (beta:=radix) b|
          = |_root_.F2R (beta:=radix) sum| := by rw [hsumValue]
      _ = |(sum.Fnum : ℝ) * (radix : ℝ) ^ sum.Fexp| := by
            simp only [_root_.F2R, FloatSpec.Core.Defs.F2R]
      _ = |(sum.Fnum : ℝ)| * (radix : ℝ) ^ sum.Fexp := by
            rw [abs_mul, abs_of_pos (zpow_pos hradix_pos sum.Fexp)]
      _ = |(sum.Fnum : ℝ)| * (radix : ℝ) ^ b.Fexp := by rw [hsumExp]
  by_cases hzero : sum.Fnum = 0
  · left
    calc
      _root_.F2R (beta:=radix) a + _root_.F2R (beta:=radix) b =
          _root_.F2R (beta:=radix) sum := hsumValue.symm
      _ = 0 := by simp [_root_.F2R, FloatSpec.Core.Defs.F2R, hzero]
  · right
    have hnum_ge_one : (1 : Int) ≤ |sum.Fnum| := by
      have hnum_pos : (0 : Int) < |sum.Fnum| := abs_pos.mpr hzero
      omega
    have hcoef : (1 : ℝ) ≤ |(sum.Fnum : ℝ)| := by
      rw [← Int.cast_abs]
      exact_mod_cast hnum_ge_one
    calc
      (radix : ℝ) ^ b.Fexp =
          1 * (radix : ℝ) ^ b.Fexp := by ring
      _ ≤ |(sum.Fnum : ℝ)| * (radix : ℝ) ^ b.Fexp :=
            mul_le_mul_of_nonneg_right hcoef (le_of_lt hpow_pos)
      _ = |_root_.F2R (beta:=radix) a +
            _root_.F2R (beta:=radix) b| := hsumAbs.symm

private lemma yLe2x_factor_bound (radix : Int) (precision : Nat)
    (hradix : 1 < radix) (hprecision : 3 ≤ precision) :
    (1 + (radix : ℝ) ^ (1 - (precision : Int)) / 2) * 3 / 2 ≤
      2 * (1 - (radix : ℝ) ^ (1 - (precision : Int)) / 2) := by
  let eps : ℝ := (radix : ℝ) ^ (1 - (precision : Int))
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (lt_trans Int.zero_lt_one hradix)
  have hradix_one_le : (1 : ℝ) ≤ (radix : ℝ) := by
    exact_mod_cast (le_of_lt hradix)
  have hradix_two_le : (2 : ℝ) ≤ (radix : ℝ) := by
    exact_mod_cast (by omega : (2 : Int) ≤ radix)
  have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
  have heps_le_neg_two :
      eps ≤ (radix : ℝ) ^ (-(2 : Int)) := by
    dsimp [eps]
    exact zpow_le_zpow_right₀ hradix_one_le (by omega)
  have hradix_sq_ge_four : (4 : ℝ) ≤ (radix : ℝ) ^ (2 : Int) := by
    norm_num [zpow_ofNat]
    nlinarith
  have hneg_two_le_quarter :
      (radix : ℝ) ^ (-(2 : Int)) ≤ (1 / 4 : ℝ) := by
    have hpow_pos : 0 < (radix : ℝ) ^ (2 : Int) := zpow_pos hradix_pos _
    have hdiv := one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 4)
      hradix_sq_ge_four
    simpa [zpow_neg, one_div, hradix_ne, zpow_ofNat] using hdiv
  have heps_le_quarter : eps ≤ (1 / 4 : ℝ) :=
    le_trans heps_le_neg_two hneg_two_le_quarter
  dsimp [eps] at heps_le_quarter ⊢
  nlinarith

noncomputable def yLe2x_aux_check {beta : Int} [ValidRadix beta]
    (_bo : Fbound_skel) (_radix : Int) (_precision : Nat)
    (_a _b _x _y : FloatSpec.Core.Defs.FlocqFloat beta) (_e : ℝ) : Unit :=
  ()

/-- Coq generic FMA local lemma `yLe2x_aux`.

This restores the upstream GenericA companion of `xLe2y_aux2`: after the
zero-or-at-least-one-radix-unit `Fplus` split, the zero branch contradicts
`x` nonzero, and the positive branch combines the normal closest-rounding
relative bounds around `a + b` and `a + b + e`. -/
theorem yLe2x_aux {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b x y : FloatSpec.Core.Defs.FlocqFloat beta) (e : ℝ) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ bo.vNum = Zpower_nat radix precision ∧
        3 ≤ precision ∧
        |e| ≤ (1 / 2 : ℝ) * Fulp (beta:=beta) bo radix precision b ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a + _root_.F2R (beta:=beta) b) x ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a + _root_.F2R (beta:=beta) b + e) y ∧
        Fnormal (beta:=beta) radix bo x ∧
        Fnormal (beta:=beta) radix bo y ∧
        Fcanonic (beta:=beta) radix bo a ∧
        Fcanonic (beta:=beta) radix bo b ∧
        |_root_.F2R (beta:=beta) b| ≤ |_root_.F2R (beta:=beta) a| ∧
        _root_.F2R (beta:=beta) x ≠ 0⌝⦄
    (pure (yLe2x_aux_check (beta:=beta) bo radix precision a b x y e) :
      Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) y| ≤
      2 * |_root_.F2R (beta:=beta) x|⌝⦄ := by
  intro hpre
  rcases hpre with
    ⟨hbeta, hradix, hvNum, hprecision_ge, heBound, hxClosest, hyClosest,
      hxNormal, hyNormal, haCan, hbCan, hbAbsLe, hxNonzero⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure, yLe2x_aux_check, Id.run, ULift.up_down]
  let r : ℝ := _root_.F2R (beta:=radix) a + _root_.F2R (beta:=radix) b
  let r' : ℝ := r + e
  have hprecision_ne : precision ≠ 0 := by omega
  have hvNum_gt_one : (1 : Int) < bo.vNum := by
    have h := vNumbMoreThanOne bo radix precision
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, vNumbMoreThanOne_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨hprecision_ne, hradix, hvNum⟩
  have hFulpEq :
      Fulp (beta:=radix) bo radix precision b = (radix : ℝ) ^ b.Fexp := by
    have hnorm := FcanonicFnormalizeEq (beta:=radix) radix bo precision b
    have hnorm' :
        Fnormalize (beta:=radix) radix bo precision b = b := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        hnorm ⟨hbCan, rfl, hradix, hprecision_ne, hvNum⟩
    simp [Fulp, hnorm']
  rcases yLe2x_sum_zero_or_unit (beta:=radix) bo radix a b rfl hradix haCan hbCan hbAbsLe with
    hsumZero | hsumUnit
  · have hzeroBound : Fbounded (beta:=radix) bo (Fzero radix (-bo.dExp)) := by
      have h := FboundedFzero_from_positive_bound_payload (beta:=radix) bo
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FboundedFzero_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using h (lt_trans Int.zero_lt_one hvNum_gt_one)
    have hzeroVal :
        _root_.F2R (beta:=radix) (Fzero radix (-bo.dExp)) = 0 := by
      have h := FzeroisZero (beta:=radix) bo
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FzeroisZero_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using h True.intro
    have hxValZero : _root_.F2R (beta:=radix) x = 0 := by
      have hdist := hxClosest.2 (Fzero radix (-bo.dExp)) hzeroBound
      have hdist0 : |_root_.F2R (beta:=radix) x - 0| = 0 := by
        rw [hsumZero, hzeroVal, sub_self, abs_zero] at hdist
        exact le_antisymm hdist (abs_nonneg _)
      exact sub_eq_zero.mp (abs_eq_zero.mp hdist0)
    exact (hxNonzero hxValZero).elim
  · have hden_pos :
        0 < 1 - (radix : ℝ) ^ (1 - (precision : Int)) / 2 :=
      UnMoinsPos radix precision hradix hprecision_ge
    have heHalf : |e| ≤ |r| / 2 := by
      calc
        |e| ≤ (1 / 2 : ℝ) * Fulp (beta:=radix) bo radix precision b := heBound
        _ = (1 / 2 : ℝ) * (radix : ℝ) ^ b.Fexp := by rw [hFulpEq]
        _ ≤ (1 / 2 : ℝ) * |r| := by
              have hunitR : (radix : ℝ) ^ b.Fexp ≤ |r| := by
                simpa [r] using hsumUnit
              nlinarith
        _ = |r| / 2 := by ring
    have hyLe :
        |_root_.F2R (beta:=radix) y| ≤
          |r'| / (1 - (radix : ℝ) ^ (1 - (precision : Int)) / 2) := by
      have h := ClosestRoundeLeNormal (beta:=radix) bo radix precision r' y
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestRoundeLeNormal_check,
        Id.run, ULift.up_down, r, r', Int.cast_ofNat] using
        h ⟨rfl, hradix, hprecision_ge, hvNum, hyClosest, hyNormal⟩
    have hr'Le :
        |r'| ≤ |r| * 3 / 2 := by
      have htri : |r'| ≤ |r| + |e| := by
        simpa [r'] using abs_add_le r e
      calc
        |r'| ≤ |r| + |e| := htri
        _ ≤ |r| + |r| / 2 := by nlinarith [heHalf]
        _ = |r| * 3 / 2 := by ring
    have hxGe :
        |r| ≤ |_root_.F2R (beta:=radix) x| *
          (1 + (radix : ℝ) ^ (1 - (precision : Int)) / 2) := by
      have h := ClosestRoundeGeNormal (beta:=radix) bo radix precision r x
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestRoundeGeNormal_check,
        Id.run, ULift.up_down, r, Int.cast_ofNat] using
        h ⟨rfl, hradix, hprecision_ge, hvNum, hxClosest, hxNormal⟩
    let den : ℝ := 1 - (radix : ℝ) ^ (1 - (precision : Int)) / 2
    let unplus : ℝ := 1 + (radix : ℝ) ^ (1 - (precision : Int)) / 2
    have hden_pos' : 0 < den := by simpa [den] using hden_pos
    have hyMul : den * |_root_.F2R (beta:=radix) y| ≤ |r'| := by
      have h := (le_div_iff₀ hden_pos').1 (by simpa [den] using hyLe)
      nlinarith
    have hfactor : unplus * 3 / 2 ≤ 2 * den := by
      simpa [unplus, den] using yLe2x_factor_bound radix precision hradix hprecision_ge
    have hmain :
        den * |_root_.F2R (beta:=radix) y| ≤
          den * (2 * |_root_.F2R (beta:=radix) x|) := by
      have hchain :
          den * |_root_.F2R (beta:=radix) y| ≤
            |_root_.F2R (beta:=radix) x| * (unplus * 3 / 2) := by
        calc
          den * |_root_.F2R (beta:=radix) y| ≤ |r'| := hyMul
          _ ≤ |r| * 3 / 2 := hr'Le
          _ ≤ (|_root_.F2R (beta:=radix) x| * unplus) * 3 / 2 := by
                exact div_le_div_of_nonneg_right
                  (mul_le_mul_of_nonneg_right hxGe (by norm_num)) (by norm_num)
          _ = |_root_.F2R (beta:=radix) x| * (unplus * 3 / 2) := by ring
      calc
        den * |_root_.F2R (beta:=radix) y| ≤
            |_root_.F2R (beta:=radix) x| * (unplus * 3 / 2) := hchain
        _ ≤ |_root_.F2R (beta:=radix) x| * (2 * den) :=
              mul_le_mul_of_nonneg_left hfactor
                (abs_nonneg (_root_.F2R (beta:=radix) x))
        _ = den * (2 * |_root_.F2R (beta:=radix) x|) := by ring
    by_contra hnot
    have hlt :
        2 * |_root_.F2R (beta:=radix) x| <
          |_root_.F2R (beta:=radix) y| := lt_of_not_ge hnot
    have hmul_lt :
        den * (2 * |_root_.F2R (beta:=radix) x|) <
          den * |_root_.F2R (beta:=radix) y| :=
      mul_lt_mul_of_pos_left hlt hden_pos'
    exact not_lt_of_ge hmain hmul_lt

noncomputable def xLe2y_check {beta : Int} [ValidRadix beta]
    (_bo : Fbound_skel) (_radix : Int) (_precision : Nat)
    (_a _b _e _x _y : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq GenericB lemma `xLe2y`.

This is the exported GenericB branch following `yLe2x_aux`: compare
`|b|` and `|a|`, then reuse the GenericA payload `xLe2y_aux2`, swapping
`a` and `b` in the second branch exactly as upstream does.  The sign-transfer
hypothesis used later by `Subexact` is intentionally not part of this lemma. -/
theorem xLe2y {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b e x y : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ bo.vNum = Zpower_nat radix precision ∧
        3 ≤ precision ∧ Even radix ∧
        |_root_.F2R (beta:=beta) e| ≤
          (1 / 2 : ℝ) * Fulp (beta:=beta) bo radix precision a ∧
        |_root_.F2R (beta:=beta) e| ≤
          (1 / 2 : ℝ) * Fulp (beta:=beta) bo radix precision b ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a + _root_.F2R (beta:=beta) b) x ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a + _root_.F2R (beta:=beta) b +
            _root_.F2R (beta:=beta) e) y ∧
        Fnormal (beta:=beta) radix bo x ∧
        Fnormal (beta:=beta) radix bo y ∧
        Fcanonic (beta:=beta) radix bo b ∧
        Fcanonic (beta:=beta) radix bo a ∧
        -bo.dExp < b.Fexp ∧
        -bo.dExp < a.Fexp⌝⦄
    (pure (xLe2y_check (beta:=beta) bo radix precision a b e x y) :
      Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) x| ≤
      2 * |_root_.F2R (beta:=beta) y|⌝⦄ := by
  intro hpre
  rcases hpre with
    ⟨hbeta, hradix, hvNum, hprecision_ge, hEven, heLea, heLeb, hxClosest,
      hyClosest, hxNormal, hyNormal, hbCan, haCan, hbExpGt, haExpGt⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure, xLe2y_check, Id.run, ULift.up_down]
  by_cases hbLeA :
      |_root_.F2R (beta:=radix) b| ≤ |_root_.F2R (beta:=radix) a|
  · have h := xLe2y_aux2 (beta:=radix) bo radix precision a b e x y
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, xLe2y_aux2_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, hradix, hvNum, hprecision_ge, hEven, heLeb, hxClosest, hyClosest,
        hxNormal, hyNormal, hbCan, haCan, hbExpGt, hbLeA⟩
  · have haLeB :
        |_root_.F2R (beta:=radix) a| ≤ |_root_.F2R (beta:=radix) b| :=
      le_of_lt (lt_of_not_ge hbLeA)
    have hxClosest_swap :
        Closest (beta:=radix) bo (radix : ℝ)
          (_root_.F2R (beta:=radix) b + _root_.F2R (beta:=radix) a) x := by
      simpa [add_comm] using hxClosest
    have hyClosest_swap :
        Closest (beta:=radix) bo (radix : ℝ)
          (_root_.F2R (beta:=radix) b + _root_.F2R (beta:=radix) a +
            _root_.F2R (beta:=radix) e) y := by
      simpa [add_comm, add_left_comm, add_assoc] using hyClosest
    have h := xLe2y_aux2 (beta:=radix) bo radix precision b a e x y
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, xLe2y_aux2_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, hradix, hvNum, hprecision_ge, hEven, heLea, hxClosest_swap,
        hyClosest_swap, hxNormal, hyNormal, haCan, hbCan, haExpGt, haLeB⟩

noncomputable def yLe2x_check {beta : Int} [ValidRadix beta]
    (_bo : Fbound_skel) (_radix : Int) (_precision : Nat)
    (_a _b _x _y : FloatSpec.Core.Defs.FlocqFloat beta) (_e : ℝ) : Unit :=
  ()

/-- Coq GenericB lemma `yLe2x`.

This is the exported GenericB companion of `xLe2y`: under nonzero rounded
`x`, compare `|b|` and `|a|`, then reuse the GenericA payload `yLe2x_aux`,
swapping `a` and `b` in the second branch exactly as upstream does. -/
theorem yLe2x {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b x y : FloatSpec.Core.Defs.FlocqFloat beta) (e : ℝ) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ bo.vNum = Zpower_nat radix precision ∧
        3 ≤ precision ∧
        |e| ≤ (1 / 2 : ℝ) * Fulp (beta:=beta) bo radix precision a ∧
        |e| ≤ (1 / 2 : ℝ) * Fulp (beta:=beta) bo radix precision b ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a + _root_.F2R (beta:=beta) b) x ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a + _root_.F2R (beta:=beta) b + e) y ∧
        Fnormal (beta:=beta) radix bo x ∧
        Fnormal (beta:=beta) radix bo y ∧
        Fcanonic (beta:=beta) radix bo a ∧
        Fcanonic (beta:=beta) radix bo b ∧
        _root_.F2R (beta:=beta) x ≠ 0⌝⦄
    (pure (yLe2x_check (beta:=beta) bo radix precision a b x y e) :
      Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) y| ≤
      2 * |_root_.F2R (beta:=beta) x|⌝⦄ := by
  intro hpre
  rcases hpre with
    ⟨hbeta, hradix, hvNum, hprecision_ge, heLea, heLeb, hxClosest,
      hyClosest, hxNormal, hyNormal, haCan, hbCan, hxNonzero⟩
  subst beta
  simp only [wp, PostCond.noThrow, pure, yLe2x_check, Id.run, ULift.up_down]
  by_cases hbLeA :
      |_root_.F2R (beta:=radix) b| ≤ |_root_.F2R (beta:=radix) a|
  · have h := yLe2x_aux (beta:=radix) bo radix precision a b x y e
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, yLe2x_aux_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, hradix, hvNum, hprecision_ge, heLeb, hxClosest, hyClosest,
        hxNormal, hyNormal, haCan, hbCan, hbLeA, hxNonzero⟩
  · have haLeB :
        |_root_.F2R (beta:=radix) a| ≤ |_root_.F2R (beta:=radix) b| :=
      le_of_lt (lt_of_not_ge hbLeA)
    have hxClosest_swap :
        Closest (beta:=radix) bo (radix : ℝ)
          (_root_.F2R (beta:=radix) b + _root_.F2R (beta:=radix) a) x := by
      simpa [add_comm] using hxClosest
    have hyClosest_swap :
        Closest (beta:=radix) bo (radix : ℝ)
          (_root_.F2R (beta:=radix) b + _root_.F2R (beta:=radix) a + e) y := by
      simpa [add_comm, add_left_comm, add_assoc] using hyClosest
    have h := yLe2x_aux (beta:=radix) bo radix precision b a x y e
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, yLe2x_aux_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, hradix, hvNum, hprecision_ge, heLea, hxClosest_swap,
        hyClosest_swap, hxNormal, hyNormal, hbCan, haCan, haLeB, hxNonzero⟩

/-- Coq GenericB lemma `Subexact`.

Under the full GenericB payload, the subtraction of the two rounded normal
floats is exactly representable with the minimum input exponent.  This mirrors
upstream `Pff.v`: derive nonzeroness of `x` from normality, split on the sign
of `y`, use `Fminus x y` in the nonnegative branch and
`Fopp (Fminus (Fopp x) (Fopp y))` in the nonpositive branch, and discharge
boundedness by Sterbenz from `xLe2y` and `yLe2x`. -/
theorem Subexact {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b e x y : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : bo.vNum = Zpower_nat radix precision)
    (hprecision_ge : 3 ≤ precision) (hEven : Even radix)
    (heLea : |_root_.F2R (beta:=beta) e| ≤
      (1 / 2 : ℝ) * Fulp (beta:=beta) bo radix precision a)
    (heLeb : |_root_.F2R (beta:=beta) e| ≤
      (1 / 2 : ℝ) * Fulp (beta:=beta) bo radix precision b)
    (hxClosest : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) a + _root_.F2R (beta:=beta) b) x)
    (hyClosest : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) a + _root_.F2R (beta:=beta) b +
        _root_.F2R (beta:=beta) e) y)
    (hxNormal : Fnormal (beta:=beta) radix bo x)
    (hyNormal : Fnormal (beta:=beta) radix bo y)
    (hbCan : Fcanonic (beta:=beta) radix bo b)
    (haCan : Fcanonic (beta:=beta) radix bo a)
    (hbExpGt : -bo.dExp < b.Fexp)
    (haExpGt : -bo.dExp < a.Fexp)
    (hdsd : (0 ≤ _root_.F2R (beta:=beta) y →
        0 ≤ _root_.F2R (beta:=beta) x) ∧
      (_root_.F2R (beta:=beta) y ≤ 0 →
        _root_.F2R (beta:=beta) x ≤ 0)) :
    ∃ v : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) v =
        _root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) y ∧
        Fbounded (beta:=beta) bo v ∧
        v.Fexp = min x.Fexp y.Fexp := by
  subst beta
  have hvNum_pos : 0 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact pow_pos (by omega : 0 < radix) precision
  have hxNotFzero : ¬ is_Fzero x := by
    have h := FnormalNotZero_from_expanded_normal_payload (beta:=radix) bo radix x radix hvNum_pos hxNormal.2
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalNotZero_check,
      ULift.down_up, Int.cast_ofNat] using h hxNormal
  have hxNumNe : x.Fnum ≠ 0 := by
    simpa [is_Fzero] using hxNotFzero
  have hxValueNe : _root_.F2R (beta:=radix) x ≠ 0 :=
    FloatSpec.Core.Float_prop.F2R_neq_0 (beta:=radix) x hradix hxNumNe
  have hxBound : Fbounded (beta:=radix) bo x := hxNormal.1
  have hyBound : Fbounded (beta:=radix) bo y := hyNormal.1
  have hxAbsLe2y : |_root_.F2R (beta:=radix) x| ≤
      2 * |_root_.F2R (beta:=radix) y| := by
    have h := xLe2y (beta:=radix) bo radix precision a b e x y
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, xLe2y_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, hradix, hvNum, hprecision_ge, hEven, heLea, heLeb,
        hxClosest, hyClosest, hxNormal, hyNormal, hbCan, haCan, hbExpGt,
        haExpGt⟩
  have hyAbsLe2x : |_root_.F2R (beta:=radix) y| ≤
      2 * |_root_.F2R (beta:=radix) x| := by
    have h := yLe2x (beta:=radix) bo radix precision a b x y
      (_root_.F2R (beta:=radix) e)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, yLe2x_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, hradix, hvNum, hprecision_ge, heLea, heLeb, hxClosest,
        hyClosest, hxNormal, hyNormal, haCan, hbCan, hxValueNe⟩
  by_cases hyNonneg : 0 ≤ _root_.F2R (beta:=radix) y
  · let v : FloatSpec.Core.Defs.FlocqFloat radix := Fminus (beta:=radix) x y
    refine ⟨v, ?_, ?_, ?_⟩
    · have h := Fminus_correct (beta:=radix) x y
      simpa [v, Fminus, wp, PostCond.noThrow, pure, Fminus_correct_check, Fminus,
        Id.run, ULift.up_down, Int.cast_ofNat] using h hradix
    · have hxNonneg : 0 ≤ _root_.F2R (beta:=radix) x := hdsd.1 hyNonneg
      have hhalf :
          (1 / 2 : ℝ) * _root_.F2R (beta:=radix) y ≤
            _root_.F2R (beta:=radix) x := by
        have hyLe2x :
            _root_.F2R (beta:=radix) y ≤
              2 * _root_.F2R (beta:=radix) x := by
          simpa [abs_of_nonneg hyNonneg, abs_of_nonneg hxNonneg] using hyAbsLe2x
        nlinarith
      have hxLe2y :
          _root_.F2R (beta:=radix) x ≤
            2 * _root_.F2R (beta:=radix) y := by
        simpa [abs_of_nonneg hxNonneg, abs_of_nonneg hyNonneg] using hxAbsLe2y
      have h := Sterbenz (beta:=radix) bo x y
      simpa [v, Fminus, wp, PostCond.noThrow, pure, Sterbenz_check, Fminus,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hradix, hxBound, hyBound, hhalf, hxLe2y⟩
    · simpa [v] using Fminus_exp_eq_min (beta:=radix) x y
  · have hyNonpos : _root_.F2R (beta:=radix) y ≤ 0 :=
      le_of_lt (lt_of_not_ge hyNonneg)
    let d : FloatSpec.Core.Defs.FlocqFloat radix :=
      Fminus (beta:=radix) (Fopp (beta:=radix) x) (Fopp (beta:=radix) y)
    let v : FloatSpec.Core.Defs.FlocqFloat radix := Fopp (beta:=radix) d
    refine ⟨v, ?_, ?_, ?_⟩
    · have hdVal :
          _root_.F2R (beta:=radix) d =
            _root_.F2R (beta:=radix) (Fopp (beta:=radix) x) -
              _root_.F2R (beta:=radix) (Fopp (beta:=radix) y) := by
        have h := Fminus_correct (beta:=radix)
          (Fopp (beta:=radix) x) (Fopp (beta:=radix) y)
        simpa [d, Fminus, wp, PostCond.noThrow, pure, Fminus_correct_check, Fminus,
          Id.run, ULift.up_down, Int.cast_ofNat] using h hradix
      have hxOppVal :
          _root_.F2R (beta:=radix) (Fopp (beta:=radix) x) =
            -_root_.F2R (beta:=radix) x := by
        have h := Fopp_correct (beta:=radix) x
        simpa [Fopp, wp, PostCond.noThrow, pure, Fopp_correct_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
      have hyOppVal :
          _root_.F2R (beta:=radix) (Fopp (beta:=radix) y) =
            -_root_.F2R (beta:=radix) y := by
        have h := Fopp_correct (beta:=radix) y
        simpa [Fopp, wp, PostCond.noThrow, pure, Fopp_correct_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
      have hvVal :
          _root_.F2R (beta:=radix) v = -_root_.F2R (beta:=radix) d := by
        have h := Fopp_correct (beta:=radix) d
        simpa [v, Fopp, wp, PostCond.noThrow, pure, Fopp_correct_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
      rw [hvVal, hdVal, hxOppVal, hyOppVal]
      ring
    · have hxNonpos : _root_.F2R (beta:=radix) x ≤ 0 := hdsd.2 hyNonpos
      have hxOppBound : Fbounded (beta:=radix) bo (Fopp (beta:=radix) x) := by
        have h := oppBounded (beta:=radix) bo x
        simpa [Fopp, wp, PostCond.noThrow, pure, oppBounded_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h hxBound
      have hyOppBound : Fbounded (beta:=radix) bo (Fopp (beta:=radix) y) := by
        have h := oppBounded (beta:=radix) bo y
        simpa [Fopp, wp, PostCond.noThrow, pure, oppBounded_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h hyBound
      have hxOppVal :
          _root_.F2R (beta:=radix) (Fopp (beta:=radix) x) =
            -_root_.F2R (beta:=radix) x := by
        have h := Fopp_correct (beta:=radix) x
        simpa [Fopp, wp, PostCond.noThrow, pure, Fopp_correct_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
      have hyOppVal :
          _root_.F2R (beta:=radix) (Fopp (beta:=radix) y) =
            -_root_.F2R (beta:=radix) y := by
        have h := Fopp_correct (beta:=radix) y
        simpa [Fopp, wp, PostCond.noThrow, pure, Fopp_correct_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
      have hhalf :
          (1 / 2 : ℝ) *
              _root_.F2R (beta:=radix) (Fopp (beta:=radix) y) ≤
            _root_.F2R (beta:=radix) (Fopp (beta:=radix) x) := by
        have hyLe2x :
            -_root_.F2R (beta:=radix) y ≤
              2 * (-_root_.F2R (beta:=radix) x) := by
          simpa [abs_of_nonpos hyNonpos, abs_of_nonpos hxNonpos] using hyAbsLe2x
        rw [hxOppVal, hyOppVal]
        nlinarith
      have hxLe2y :
          _root_.F2R (beta:=radix) (Fopp (beta:=radix) x) ≤
            2 * _root_.F2R (beta:=radix) (Fopp (beta:=radix) y) := by
        have hxLe2y' :
            -_root_.F2R (beta:=radix) x ≤
              2 * (-_root_.F2R (beta:=radix) y) := by
          simpa [abs_of_nonpos hxNonpos, abs_of_nonpos hyNonpos] using hxAbsLe2y
        rw [hxOppVal, hyOppVal]
        exact hxLe2y'
      have hdBound : Fbounded (beta:=radix) bo d := by
        have h := Sterbenz (beta:=radix) bo
          (Fopp (beta:=radix) x) (Fopp (beta:=radix) y)
        simpa [d, Fminus, wp, PostCond.noThrow, pure, Sterbenz_check, Fminus,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          h ⟨hradix, hxOppBound, hyOppBound, hhalf, hxLe2y⟩
      have h := oppBounded (beta:=radix) bo d
      simpa [v, Fopp, wp, PostCond.noThrow, pure, oppBounded_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h hdBound
    · simpa [v, d, Fopp, FloatSpec.Calc.Operations.Fopp] using
        Fminus_exp_eq_min (beta:=radix)
          (Fopp (beta:=radix) x) (Fopp (beta:=radix) y)

private theorem Closest_exact_of_bounded {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (p : FloatSpec.Core.Defs.FlocqFloat beta)
    (hpBound : Fbounded (beta:=beta) bo p) :
    Closest (beta:=beta) bo radix (_root_.F2R (beta:=beta) p) p := by
  constructor
  · exact hpBound
  · intro g hg
    rw [sub_self, abs_zero]
    exact abs_nonneg (_root_.F2R (beta:=beta) g - _root_.F2R (beta:=beta) p)

private theorem Closest_lower_bound_of_bounded {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ)
    (hpBound : Fbounded (beta:=beta) bo p)
    (hpr : _root_.F2R (beta:=beta) p ≤ r)
    (hqClosest : Closest (beta:=beta) bo radix r q) :
    _root_.F2R (beta:=beta) p ≤ _root_.F2R (beta:=beta) q := by
  by_cases hlt : _root_.F2R (beta:=beta) p < r
  · have hMono : MonotoneP_float (Closest (beta:=beta) bo radix) := by
      have h := ClosestMonotone_from_split_radix_payload (beta:=beta) bo radix
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMonotone_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
    exact hMono (_root_.F2R (beta:=beta) p) r p q hlt
      (Closest_exact_of_bounded (beta:=beta) bo radix p hpBound) hqClosest
  · have heq : _root_.F2R (beta:=beta) p = r := le_antisymm hpr (le_of_not_gt hlt)
    have hdist :
        |_root_.F2R (beta:=beta) q - r| ≤
          |_root_.F2R (beta:=beta) p - r| :=
      hqClosest.2 p hpBound
    rw [heq, sub_self, abs_zero] at hdist
    have hq_eq : _root_.F2R (beta:=beta) q = r := by
      exact sub_eq_zero.mp (abs_eq_zero.mp (le_antisymm hdist (abs_nonneg _)))
    rw [hq_eq]
    exact hpr

private theorem Closest_upper_bound_of_bounded {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : ℝ)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ)
    (hrp : r ≤ _root_.F2R (beta:=beta) p)
    (hqClosest : Closest (beta:=beta) bo radix r q)
    (hpBound : Fbounded (beta:=beta) bo p) :
    _root_.F2R (beta:=beta) q ≤ _root_.F2R (beta:=beta) p := by
  by_cases hlt : r < _root_.F2R (beta:=beta) p
  · have hMono : MonotoneP_float (Closest (beta:=beta) bo radix) := by
      have h := ClosestMonotone_from_split_radix_payload (beta:=beta) bo radix
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMonotone_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
    exact hMono r (_root_.F2R (beta:=beta) p) q p hlt hqClosest
      (Closest_exact_of_bounded (beta:=beta) bo radix p hpBound)
  · have heq : r = _root_.F2R (beta:=beta) p := le_antisymm hrp (le_of_not_gt hlt)
    have hdist :
        |_root_.F2R (beta:=beta) q - r| ≤
          |_root_.F2R (beta:=beta) p - r| :=
      hqClosest.2 p hpBound
    rw [← heq, sub_self, abs_zero] at hdist
    have hq_eq : _root_.F2R (beta:=beta) q = r := by
      exact sub_eq_zero.mp (abs_eq_zero.mp (le_antisymm hdist (abs_nonneg _)))
    rw [hq_eq, heq]

/-- Coq Be2Zero lemma `gatCorrect`.

The subtraction `be1 - r1` is exactly representable with exponent
`min be1.Fexp r1.Fexp`.  This is the public upstream `gatCorrect` payload:
the two error bounds are obtained from `TwoSumProp`/`ClosestUlp`, and the
sign-transfer pair needed by `Subexact` follows from closest-rounding
monotonicity. -/
theorem gatCorrect {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a x y r1 u1 u2 al1 al2 be1 :
      FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : bo.vNum = Zpower_nat radix precision)
    (hprecision_ge : 3 ≤ precision) (hEven : Even radix)
    (hyBound : Fbounded (beta:=beta) bo y)
    (hbe1Normal : Fnormal (beta:=beta) radix bo be1)
    (hr1Normal : Fnormal (beta:=beta) radix bo r1)
    (hal1Can : Fcanonic (beta:=beta) radix bo al1)
    (hu1Can : Fcanonic (beta:=beta) radix bo u1)
    (hal1ExpGt : -bo.dExp < al1.Fexp)
    (hu1ExpGt : -bo.dExp < u1.Fexp)
    (hr1Def : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
        _root_.F2R (beta:=beta) y) r1)
    (hu1Def : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) u1)
    (hu2Def : _root_.F2R (beta:=beta) u2 =
      _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
        _root_.F2R (beta:=beta) u1)
    (hal1Def : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) u2) al1)
    (hal2Def : _root_.F2R (beta:=beta) al2 =
      _root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) u2 -
        _root_.F2R (beta:=beta) al1)
    (hbe1Def : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) u1 + _root_.F2R (beta:=beta) al1) be1) :
    ∃ v : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) v =
        _root_.F2R (beta:=beta) be1 - _root_.F2R (beta:=beta) r1 ∧
      Fbounded (beta:=beta) bo v ∧
      v.Fexp = min be1.Fexp r1.Fexp := by
  subst beta
  have hprecision_ne : precision ≠ 0 := by omega
  have hu1Bound : Fbounded (beta:=radix) bo u1 := by
    have h := FcanonicBound (beta:=radix) radix bo u1
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicBound_check, Int.cast_ofNat] using h hu1Can
  have hneg_u1_bound :
      Fbounded (beta:=radix) bo (Fopp (beta:=radix) u1) := by
    have h := oppBounded (beta:=radix) bo u1
    simpa [Fopp, wp, PostCond.noThrow, pure, oppBounded_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hu1Bound
  have hneg_u1_val :
      _root_.F2R (beta:=radix) (Fopp (beta:=radix) u1) =
        -_root_.F2R (beta:=radix) u1 := by
    have h := Fopp_correct (beta:=radix) u1
    simpa [Fopp, wp, PostCond.noThrow, pure, Fopp_correct_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hu2_abs_bound :
      |_root_.F2R (beta:=radix) u2| ≤
        (1 / 2 : ℝ) * Fulp (beta:=radix) bo radix precision u1 := by
    have hcu :
        2 * |_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x -
            _root_.F2R (beta:=radix) u1| ≤
          Fulp (beta:=radix) bo radix precision u1 := by
      have h := ClosestUlp (beta:=radix) bo radix precision
        (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x) u1
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hu1Def, rfl, hradix, hprecision_ne, hvNum⟩
    rw [hu2Def]
    nlinarith
  have hal2_le_u2 :
      |_root_.F2R (beta:=radix) al2| ≤ |_root_.F2R (beta:=radix) u2| := by
    have h := TwoSumProp (beta:=radix) bo (radix : ℝ) y u2 al1 al2
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, TwoSumProp_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨hyBound, hal1Def, hal2Def⟩
  have hal2_u1_ulp :
      |_root_.F2R (beta:=radix) al2| ≤
        (1 / 2 : ℝ) * Fulp (beta:=radix) bo radix precision u1 := by
    exact le_trans hal2_le_u2 hu2_abs_bound
  have hal2_al1_ulp :
      |_root_.F2R (beta:=radix) al2| ≤
        (1 / 2 : ℝ) * Fulp (beta:=radix) bo radix precision al1 := by
    have hcu :
        2 * |(_root_.F2R (beta:=radix) y + _root_.F2R (beta:=radix) u2) -
            _root_.F2R (beta:=radix) al1| ≤
          Fulp (beta:=radix) bo radix precision al1 := by
      have h := ClosestUlp (beta:=radix) bo radix precision
        (_root_.F2R (beta:=radix) y + _root_.F2R (beta:=radix) u2) al1
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hal1Def, rfl, hradix, hprecision_ne, hvNum⟩
    have hal2_abs_eq :
        |_root_.F2R (beta:=radix) al2| =
          |(_root_.F2R (beta:=radix) y + _root_.F2R (beta:=radix) u2) -
            _root_.F2R (beta:=radix) al1| := by
      rw [hal2Def]
    rw [hal2_abs_eq]
    nlinarith
  have hr1ClosestSubexact :
      Closest (beta:=radix) bo (radix : ℝ)
        (_root_.F2R (beta:=radix) u1 + _root_.F2R (beta:=radix) al1 +
          _root_.F2R (beta:=radix) al2) r1 := by
    have harg :
        _root_.F2R (beta:=radix) u1 + _root_.F2R (beta:=radix) al1 +
            _root_.F2R (beta:=radix) al2 =
          _root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
            _root_.F2R (beta:=radix) y := by
      rw [hal2Def, hu2Def]
      ring
    simpa [harg] using hr1Def
  have hr1ValueNe : _root_.F2R (beta:=radix) r1 ≠ 0 := by
    have hvNum_pos : 0 < bo.vNum := by
      rw [hvNum, Zpower_nat]
      exact pow_pos (by omega : 0 < radix) precision
    have hnotZero : ¬ is_Fzero r1 := by
      have h := FnormalNotZero_from_expanded_normal_payload (beta:=radix) bo radix r1 radix hvNum_pos
        hr1Normal.2
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalNotZero_check,
        ULift.down_up, Int.cast_ofNat] using h hr1Normal
    have hnum : r1.Fnum ≠ 0 := by
      simpa [is_Fzero] using hnotZero
    exact FloatSpec.Core.Float_prop.F2R_neq_0 (beta:=radix) r1 hradix hnum
  have hSign :
      (0 ≤ _root_.F2R (beta:=radix) r1 →
          0 ≤ _root_.F2R (beta:=radix) be1) ∧
        (_root_.F2R (beta:=radix) r1 ≤ 0 →
          _root_.F2R (beta:=radix) be1 ≤ 0) := by
    let s : ℝ :=
      _root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
        _root_.F2R (beta:=radix) y
    let t : ℝ := _root_.F2R (beta:=radix) u1 + _root_.F2R (beta:=radix) al1
    by_cases hs_nonneg : 0 ≤ s
    · have hinput_lower :
          _root_.F2R (beta:=radix) (Fopp (beta:=radix) u1) ≤
            _root_.F2R (beta:=radix) y + _root_.F2R (beta:=radix) u2 := by
        rw [hneg_u1_val, hu2Def]
        change 0 ≤
          _root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
            _root_.F2R (beta:=radix) y at hs_nonneg
        linarith
      have hal1_lower :
          _root_.F2R (beta:=radix) (Fopp (beta:=radix) u1) ≤
            _root_.F2R (beta:=radix) al1 :=
        Closest_lower_bound_of_bounded (beta:=radix) bo (radix : ℝ)
          (Fopp (beta:=radix) u1) al1
          (_root_.F2R (beta:=radix) y + _root_.F2R (beta:=radix) u2)
          hneg_u1_bound hinput_lower hal1Def
      have ht_nonneg : 0 ≤ t := by
        dsimp [t]
        rw [hneg_u1_val] at hal1_lower
        linarith
      constructor
      · intro _
        exact P_positive (beta:=radix) bo (radix : ℝ) t be1 hbe1Def ht_nonneg
      · intro hr1_nonpos
        have hr1_nonneg : 0 ≤ _root_.F2R (beta:=radix) r1 := by
          exact P_positive (beta:=radix) bo (radix : ℝ) s r1 hr1Def hs_nonneg
        have hr1_zero : _root_.F2R (beta:=radix) r1 = 0 :=
          le_antisymm hr1_nonpos hr1_nonneg
        exact False.elim (hr1ValueNe hr1_zero)
    · have hs_nonpos : s ≤ 0 := le_of_lt (lt_of_not_ge hs_nonneg)
      have hinput_upper :
          _root_.F2R (beta:=radix) y + _root_.F2R (beta:=radix) u2 ≤
            _root_.F2R (beta:=radix) (Fopp (beta:=radix) u1) := by
        rw [hneg_u1_val, hu2Def]
        change
          _root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
            _root_.F2R (beta:=radix) y ≤ 0 at hs_nonpos
        linarith
      have hal1_upper :
          _root_.F2R (beta:=radix) al1 ≤
            _root_.F2R (beta:=radix) (Fopp (beta:=radix) u1) :=
        Closest_upper_bound_of_bounded (beta:=radix) bo (radix : ℝ)
          (Fopp (beta:=radix) u1) al1
          (_root_.F2R (beta:=radix) y + _root_.F2R (beta:=radix) u2)
          hinput_upper hal1Def hneg_u1_bound
      have ht_nonpos : t ≤ 0 := by
        dsimp [t]
        rw [hneg_u1_val] at hal1_upper
        linarith
      constructor
      · intro hr1_nonneg
        have hr1_nonpos : _root_.F2R (beta:=radix) r1 ≤ 0 := by
          have hnegClosest : Closest (beta:=radix) bo (radix : ℝ) (-s)
              (Fopp (beta:=radix) r1) := by
            have h := ClosestOpp (beta:=radix) bo (radix : ℝ) r1 s
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestOpp_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using h hr1Def
          have hnonneg_neg_s : 0 ≤ -s := by linarith
          have hnonneg_opp :
              0 ≤ _root_.F2R (beta:=radix) (Fopp (beta:=radix) r1) :=
            P_positive (beta:=radix) bo (radix : ℝ) (-s)
              (Fopp (beta:=radix) r1) hnegClosest hnonneg_neg_s
          have hopp :
              _root_.F2R (beta:=radix) (Fopp (beta:=radix) r1) =
                -_root_.F2R (beta:=radix) r1 := by
            have h := Fopp_correct (beta:=radix) r1
            simpa [Fopp, wp, PostCond.noThrow, pure, Fopp_correct_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
          linarith
        have hr1_zero : _root_.F2R (beta:=radix) r1 = 0 :=
          le_antisymm hr1_nonpos hr1_nonneg
        exact False.elim (hr1ValueNe hr1_zero)
      · intro _
        have hnegClosest : Closest (beta:=radix) bo (radix : ℝ) (-t)
            (Fopp (beta:=radix) be1) := by
          have h := ClosestOpp (beta:=radix) bo (radix : ℝ) be1 t
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestOpp_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using h hbe1Def
        have hnonneg_neg_t : 0 ≤ -t := by linarith
        have hnonneg_opp :
            0 ≤ _root_.F2R (beta:=radix) (Fopp (beta:=radix) be1) :=
          P_positive (beta:=radix) bo (radix : ℝ) (-t)
            (Fopp (beta:=radix) be1) hnegClosest hnonneg_neg_t
        have hopp :
            _root_.F2R (beta:=radix) (Fopp (beta:=radix) be1) =
              -_root_.F2R (beta:=radix) be1 := by
          have h := Fopp_correct (beta:=radix) be1
          simpa [Fopp, wp, PostCond.noThrow, pure, Fopp_correct_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
        linarith
  exact Subexact (beta:=radix) bo radix precision u1 al1 al2 be1 r1
    rfl hradix hvNum hprecision_ge hEven
    hal2_u1_ulp hal2_al1_ulp hbe1Def hr1ClosestSubexact hbe1Normal
    hr1Normal hal1Can hu1Can hal1ExpGt hu1ExpGt hSign

/-- Coq Be2NonZero lemma `Expr1`.

The exponent of the nonzero `r1` result is at most one above the exponent of
`be1`.  This is the upstream `Fcanonic_Rle_Zle` argument: compare `r1` with
the scaled float `Float (Fnum be1) (Fexp be1 + 1)`, whose value is
`radix * be1`, and obtain the absolute-value comparison from `yLe2x`,
`TwoSumProp`, and `ClosestUlp`. -/
theorem Expr1 {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (a x y r1 u1 u2 al1 al2 be1 :
      FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : bo.vNum = Zpower_nat radix precision)
    (hprecision_ge : 3 ≤ precision)
    (hyBound : Fbounded (beta:=beta) bo y)
    (hbe1Normal : Fnormal (beta:=beta) radix bo be1)
    (hr1Normal : Fnormal (beta:=beta) radix bo r1)
    (hu1Can : Fcanonic (beta:=beta) radix bo u1)
    (hal1Can : Fcanonic (beta:=beta) radix bo al1)
    (hr1Def : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
        _root_.F2R (beta:=beta) y) r1)
    (hu1Def : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) u1)
    (hu2Def : _root_.F2R (beta:=beta) u2 =
      _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
        _root_.F2R (beta:=beta) u1)
    (hal1Def : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) u2) al1)
    (hal2Def : _root_.F2R (beta:=beta) al2 =
      _root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) u2 -
        _root_.F2R (beta:=beta) al1)
    (hbe1Def : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) u1 + _root_.F2R (beta:=beta) al1) be1) :
    r1.Fexp ≤ be1.Fexp + 1 := by
  subst beta
  have hprecision_ne : precision ≠ 0 := by omega
  have hvNum_pos : 0 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact pow_pos (by omega : 0 < radix) precision
  have hbe1ValueNe : _root_.F2R (beta:=radix) be1 ≠ 0 := by
    have hnotZero : ¬ is_Fzero be1 := by
      have h := FnormalNotZero_from_expanded_normal_payload (beta:=radix) bo radix be1 radix hvNum_pos
        hbe1Normal.2
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalNotZero_check,
        ULift.down_up, Int.cast_ofNat] using h hbe1Normal
    have hnum : be1.Fnum ≠ 0 := by
      simpa [is_Fzero] using hnotZero
    exact FloatSpec.Core.Float_prop.F2R_neq_0 (beta:=radix) be1 hradix hnum
  have hal2_u1_ulp :
      |_root_.F2R (beta:=radix) al2| ≤
        (1 / 2 : ℝ) * Fulp (beta:=radix) bo radix precision u1 := by
    have hu2_abs_bound :
        |_root_.F2R (beta:=radix) u2| ≤
          (1 / 2 : ℝ) * Fulp (beta:=radix) bo radix precision u1 := by
      have hcu :
          2 * |_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x -
              _root_.F2R (beta:=radix) u1| ≤
            Fulp (beta:=radix) bo radix precision u1 := by
        have h := ClosestUlp (beta:=radix) bo radix precision
          (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x) u1
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          h ⟨hu1Def, rfl, hradix, hprecision_ne, hvNum⟩
      rw [hu2Def]
      nlinarith
    have hal2_le_u2 :
        |_root_.F2R (beta:=radix) al2| ≤ |_root_.F2R (beta:=radix) u2| := by
      have h := TwoSumProp (beta:=radix) bo (radix : ℝ) y u2 al1 al2
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, TwoSumProp_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using h ⟨hyBound, hal1Def, hal2Def⟩
    exact le_trans hal2_le_u2 hu2_abs_bound
  have hal2_al1_ulp :
      |_root_.F2R (beta:=radix) al2| ≤
        (1 / 2 : ℝ) * Fulp (beta:=radix) bo radix precision al1 := by
    have hcu :
        2 * |(_root_.F2R (beta:=radix) y + _root_.F2R (beta:=radix) u2) -
            _root_.F2R (beta:=radix) al1| ≤
          Fulp (beta:=radix) bo radix precision al1 := by
      have h := ClosestUlp (beta:=radix) bo radix precision
        (_root_.F2R (beta:=radix) y + _root_.F2R (beta:=radix) u2) al1
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hal1Def, rfl, hradix, hprecision_ne, hvNum⟩
    have hal2_abs_eq :
        |_root_.F2R (beta:=radix) al2| =
          |(_root_.F2R (beta:=radix) y + _root_.F2R (beta:=radix) u2) -
            _root_.F2R (beta:=radix) al1| := by
      rw [hal2Def]
    rw [hal2_abs_eq]
    nlinarith
  have hr1ClosestGeneric :
      Closest (beta:=radix) bo (radix : ℝ)
        (_root_.F2R (beta:=radix) u1 + _root_.F2R (beta:=radix) al1 +
          _root_.F2R (beta:=radix) al2) r1 := by
    have harg :
        _root_.F2R (beta:=radix) u1 + _root_.F2R (beta:=radix) al1 +
            _root_.F2R (beta:=radix) al2 =
          _root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
            _root_.F2R (beta:=radix) y := by
      rw [hal2Def, hu2Def]
      ring
    simpa [harg] using hr1Def
  have hr1AbsLe2Be1 :
      |_root_.F2R (beta:=radix) r1| ≤
        2 * |_root_.F2R (beta:=radix) be1| := by
    have h := yLe2x (beta:=radix) bo radix precision u1 al1 be1 r1
      (_root_.F2R (beta:=radix) al2)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, yLe2x_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, hradix, hvNum, hprecision_ge, hal2_u1_ulp, hal2_al1_ulp,
        hbe1Def, hr1ClosestGeneric, hbe1Normal, hr1Normal, hu1Can, hal1Can,
        hbe1ValueNe⟩
  let scaled : FloatSpec.Core.Defs.FlocqFloat radix :=
    ⟨be1.Fnum, be1.Fexp + 1⟩
  have hscaledNormal : Fnormal (beta:=radix) radix bo scaled := by
    constructor
    · constructor
      · simpa [scaled] using hbe1Normal.1.1
      · dsimp [scaled]
        exact le_trans hbe1Normal.1.2 (by omega : be1.Fexp ≤ be1.Fexp + 1)
    · simpa [scaled] using hbe1Normal.2
  have hscaledValue :
      _root_.F2R (beta:=radix) scaled =
        (radix : ℝ) * _root_.F2R (beta:=radix) be1 := by
    have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
      exact_mod_cast (by omega : (0 : Int) < radix)
    have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
    dsimp [scaled, _root_.F2R, FloatSpec.Core.Defs.F2R]
    rw [zpow_add₀ hradix_ne, zpow_one]
    ring
  have hAbsLeScaled :
      |_root_.F2R (beta:=radix) r1| ≤
        |_root_.F2R (beta:=radix) scaled| := by
    have htwo_le_radix : (2 : ℝ) ≤ (radix : ℝ) := by
      exact_mod_cast (by omega : (2 : Int) ≤ radix)
    have hradix_nonneg : 0 ≤ (radix : ℝ) := by
      exact_mod_cast (by omega : (0 : Int) ≤ radix)
    calc
      |_root_.F2R (beta:=radix) r1|
          ≤ 2 * |_root_.F2R (beta:=radix) be1| := hr1AbsLe2Be1
      _ ≤ (radix : ℝ) * |_root_.F2R (beta:=radix) be1| :=
          mul_le_mul_of_nonneg_right htwo_le_radix (abs_nonneg _)
      _ = |_root_.F2R (beta:=radix) scaled| := by
          rw [hscaledValue, abs_mul, abs_of_nonneg hradix_nonneg]
  have hExpLeScaled : r1.Fexp ≤ scaled.Fexp := by
    have h := Fcanonic_Rle_Zle (beta:=radix) radix bo r1 scaled hradix rfl
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fcanonic_Rle_Zle_check,
      Id.run, ULift.up_down, Fcanonic', Int.cast_ofNat] using
      h ⟨Or.inl hr1Normal, Or.inl hscaledNormal, hAbsLeScaled⟩
  simpa [scaled] using hExpLeScaled

/-- Coq Be2NonZero lemma `Expbe1`.

The exponent of `be1` is at most one above the exponent of the nonzero `r1`
result.  This is the upstream `Fcanonic_Rle_Zle` argument dual to `Expr1`:
compare `be1` with the scaled float `Float (Fnum r1) (Fexp r1 + 1)`,
whose value is `radix * r1`, and obtain the absolute-value comparison from
`xLe2y`, `TwoSumProp`, and `ClosestUlp`. -/
theorem Expbe1 {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a x y r1 u1 u2 al1 al2 be1 :
      FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : bo.vNum = Zpower_nat radix precision)
    (hprecision_ge : 3 ≤ precision) (hEven : Even radix)
    (hyBound : Fbounded (beta:=beta) bo y)
    (hbe1Normal : Fnormal (beta:=beta) radix bo be1)
    (hr1Normal : Fnormal (beta:=beta) radix bo r1)
    (hu1Can : Fcanonic (beta:=beta) radix bo u1)
    (hal1Can : Fcanonic (beta:=beta) radix bo al1)
    (hu1ExpGt : -bo.dExp < u1.Fexp)
    (hal1ExpGt : -bo.dExp < al1.Fexp)
    (hr1Def : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
        _root_.F2R (beta:=beta) y) r1)
    (hu1Def : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) u1)
    (hu2Def : _root_.F2R (beta:=beta) u2 =
      _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
        _root_.F2R (beta:=beta) u1)
    (hal1Def : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) u2) al1)
    (hal2Def : _root_.F2R (beta:=beta) al2 =
      _root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) u2 -
        _root_.F2R (beta:=beta) al1)
    (hbe1Def : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) u1 + _root_.F2R (beta:=beta) al1) be1) :
    be1.Fexp ≤ r1.Fexp + 1 := by
  subst beta
  have hprecision_ne : precision ≠ 0 := by omega
  have hal2_u1_ulp :
      |_root_.F2R (beta:=radix) al2| ≤
        (1 / 2 : ℝ) * Fulp (beta:=radix) bo radix precision u1 := by
    have hu2_abs_bound :
        |_root_.F2R (beta:=radix) u2| ≤
          (1 / 2 : ℝ) * Fulp (beta:=radix) bo radix precision u1 := by
      have hcu :
          2 * |_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x -
              _root_.F2R (beta:=radix) u1| ≤
            Fulp (beta:=radix) bo radix precision u1 := by
        have h := ClosestUlp (beta:=radix) bo radix precision
          (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x) u1
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          h ⟨hu1Def, rfl, hradix, hprecision_ne, hvNum⟩
      rw [hu2Def]
      nlinarith
    have hal2_le_u2 :
        |_root_.F2R (beta:=radix) al2| ≤ |_root_.F2R (beta:=radix) u2| := by
      have h := TwoSumProp (beta:=radix) bo (radix : ℝ) y u2 al1 al2
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, TwoSumProp_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using h ⟨hyBound, hal1Def, hal2Def⟩
    exact le_trans hal2_le_u2 hu2_abs_bound
  have hal2_al1_ulp :
      |_root_.F2R (beta:=radix) al2| ≤
        (1 / 2 : ℝ) * Fulp (beta:=radix) bo radix precision al1 := by
    have hcu :
        2 * |(_root_.F2R (beta:=radix) y + _root_.F2R (beta:=radix) u2) -
            _root_.F2R (beta:=radix) al1| ≤
          Fulp (beta:=radix) bo radix precision al1 := by
      have h := ClosestUlp (beta:=radix) bo radix precision
        (_root_.F2R (beta:=radix) y + _root_.F2R (beta:=radix) u2) al1
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hal1Def, rfl, hradix, hprecision_ne, hvNum⟩
    have hal2_abs_eq :
        |_root_.F2R (beta:=radix) al2| =
          |(_root_.F2R (beta:=radix) y + _root_.F2R (beta:=radix) u2) -
            _root_.F2R (beta:=radix) al1| := by
      rw [hal2Def]
    rw [hal2_abs_eq]
    nlinarith
  have hr1ClosestGeneric :
      Closest (beta:=radix) bo (radix : ℝ)
        (_root_.F2R (beta:=radix) u1 + _root_.F2R (beta:=radix) al1 +
          _root_.F2R (beta:=radix) al2) r1 := by
    have harg :
        _root_.F2R (beta:=radix) u1 + _root_.F2R (beta:=radix) al1 +
            _root_.F2R (beta:=radix) al2 =
          _root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
            _root_.F2R (beta:=radix) y := by
      rw [hal2Def, hu2Def]
      ring
    simpa [harg] using hr1Def
  have hbe1AbsLe2r1 :
      |_root_.F2R (beta:=radix) be1| ≤
        2 * |_root_.F2R (beta:=radix) r1| := by
    have h := xLe2y (beta:=radix) bo radix precision u1 al1 al2 be1 r1
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, xLe2y_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, hradix, hvNum, hprecision_ge, hEven, hal2_u1_ulp, hal2_al1_ulp,
        hbe1Def, hr1ClosestGeneric, hbe1Normal, hr1Normal, hal1Can, hu1Can,
        hal1ExpGt, hu1ExpGt⟩
  let scaled : FloatSpec.Core.Defs.FlocqFloat radix :=
    ⟨r1.Fnum, r1.Fexp + 1⟩
  have hscaledNormal : Fnormal (beta:=radix) radix bo scaled := by
    constructor
    · constructor
      · simpa [scaled] using hr1Normal.1.1
      · dsimp [scaled]
        exact le_trans hr1Normal.1.2 (by omega : r1.Fexp ≤ r1.Fexp + 1)
    · simpa [scaled] using hr1Normal.2
  have hscaledValue :
      _root_.F2R (beta:=radix) scaled =
        (radix : ℝ) * _root_.F2R (beta:=radix) r1 := by
    have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
      exact_mod_cast (by omega : (0 : Int) < radix)
    have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
    dsimp [scaled, _root_.F2R, FloatSpec.Core.Defs.F2R]
    rw [zpow_add₀ hradix_ne, zpow_one]
    ring
  have hAbsLeScaled :
      |_root_.F2R (beta:=radix) be1| ≤
        |_root_.F2R (beta:=radix) scaled| := by
    have htwo_le_radix : (2 : ℝ) ≤ (radix : ℝ) := by
      exact_mod_cast (by omega : (2 : Int) ≤ radix)
    have hradix_nonneg : 0 ≤ (radix : ℝ) := by
      exact_mod_cast (by omega : (0 : Int) ≤ radix)
    calc
      |_root_.F2R (beta:=radix) be1|
          ≤ 2 * |_root_.F2R (beta:=radix) r1| := hbe1AbsLe2r1
      _ ≤ (radix : ℝ) * |_root_.F2R (beta:=radix) r1| :=
          mul_le_mul_of_nonneg_right htwo_le_radix (abs_nonneg _)
      _ = |_root_.F2R (beta:=radix) scaled| := by
          rw [hscaledValue, abs_mul, abs_of_nonneg hradix_nonneg]
  have hExpLeScaled : be1.Fexp ≤ scaled.Fexp := by
    have h := Fcanonic_Rle_Zle (beta:=radix) radix bo be1 scaled hradix rfl
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fcanonic_Rle_Zle_check,
      Id.run, ULift.up_down, Fcanonic', Int.cast_ofNat] using
      h ⟨Or.inl hbe1Normal, Or.inl hscaledNormal, hAbsLeScaled⟩
  simpa [scaled] using hExpLeScaled

noncomputable def RoundLeNormal_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (f : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) : Unit :=
  ()

/-- Coq discriminant helper `RoundLeNormal`.

For radix two, a closest rounded normal float cannot be much larger than the
exact real input.  The denominator positivity side condition is kept explicit;
upstream derives it from `3 <= precision`. -/
theorem RoundLeNormal_from_denominator_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (f : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) :
    ⦃⌜Closest (beta:=beta) b (2 : ℝ) r f ∧
        Fnormal (beta:=beta) 2 b f ∧
        beta = 2 ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat 2 precision ∧
        0 < 1 - (2 : ℝ) ^ (-(precision : Int))⌝⦄
    (pure (RoundLeNormal_check (beta:=beta) b precision f r) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) f| ≤
        |r| / (1 - (2 : ℝ) ^ (-(precision : Int)))⌝⦄ := by
  intro hpre
  rcases hpre with ⟨hClosest, hnormal, hbeta, hprecision, hvNum, hden_pos⟩
  simp only [wp, PostCond.noThrow, pure, RoundLeNormal_check, Id.run, ULift.up_down]
  let F : ℝ := _root_.F2R (beta:=beta) f
  let eps : ℝ := (2 : ℝ) ^ (-(precision : Int))
  have hradix : (1 : Int) < 2 := by decide
  have hClosestUlp :
      2 * |r - F| ≤ Fulp (beta:=beta) b 2 precision f := by
    have h := ClosestUlp (beta:=beta) b 2 precision r f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, F, Int.cast_ofNat] using
      h ⟨hClosest, hbeta, hradix, hprecision, hvNum⟩
  have hNormEq :
      Fnormalize (beta:=beta) 2 b precision f = f := by
    have h := FcanonicFnormalizeEq (beta:=beta) 2 b precision f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨Or.inl hnormal, hbeta, hradix, hprecision, hvNum⟩
  have hNormalNorm :
      Fnormal (beta:=beta) 2 b (Fnormalize (beta:=beta) 2 b precision f) := by
    simpa [hNormEq] using hnormal
  have hFulpRel :
      Fulp (beta:=beta) b 2 precision f ≤
        |F| * (2 : ℝ) ^ (1 - (precision : Int)) := by
    have h := FulpLe2 (beta:=beta) 2 b precision f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpLe2_check,
      Id.run, ULift.up_down, F, Int.cast_ofNat] using
      h ⟨hnormal.1, hNormalNorm, hbeta, hradix, hprecision, hvNum⟩
  have hpow :
      (2 : ℝ) ^ (1 - (precision : Int)) = 2 * eps := by
    have htwo_ne : (2 : ℝ) ≠ 0 := by norm_num
    have hexp : (1 - (precision : Int)) = 1 + (-(precision : Int)) := by ring
    rw [hexp, zpow_add₀ htwo_ne, zpow_one]
  have hFulpRel' :
      Fulp (beta:=beta) b 2 precision f ≤ |F| * (2 * eps) := by
    simpa [hpow] using hFulpRel
  have hdist : |r - F| ≤ |F| * eps := by
    nlinarith
  have htri : |F| ≤ |r| + |r - F| := by
    calc
      |F| = |r + (F - r)| := by ring_nf
      _ ≤ |r| + |F - r| := abs_add_le _ _
      _ = |r| + |r - F| := by rw [abs_sub_comm]
  have hmul : (1 - eps) * |F| ≤ |r| := by
    nlinarith
  have hden_pos_eps : 0 < 1 - eps := by
    simpa [eps] using hden_pos
  have hmul' : |F| * (1 - eps) ≤ |r| := by
    nlinarith
  have hresult : |F| ≤ |r| / (1 - eps) :=
    (le_div_iff₀ hden_pos_eps).2 hmul'
  simpa [F, eps] using hresult

/-- Coq: `RoundLeNormal`; the full exported precision context is retained. -/
theorem RoundLeNormal
    (b : Fbound_skel) (precision : Nat)
    (f : FloatSpec.Core.Defs.FlocqFloat 2) (r : ℝ) :
    ⦃⌜1 < precision ∧ b.vNum = Zpower_nat 2 precision ∧
        3 ≤ precision ∧
        Closest (beta:=2) b (2 : ℝ) r f ∧
        Fnormal (beta:=2) 2 b f⌝⦄
    (pure (RoundLeNormal_check (beta:=2) b precision f r) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=2) f| ≤
        |r| / (1 - (2 : ℝ) ^ (-(precision : Int)))⌝⦄ := by
  intro ⟨hprecision, hvNum, _hprecisionThree, hClosest, hnormal⟩
  have hprecision_ne : precision ≠ 0 := by omega
  have hpow_lt :
      (2 : ℝ) ^ (-(precision : Int)) < (2 : ℝ) ^ (0 : Int) := by
    exact zpow_lt_zpow_right₀ (by norm_num : (1 : ℝ) < 2) (by omega)
  exact RoundLeNormal_from_denominator_payload (beta:=2) b precision f r
    ⟨hClosest, hnormal, rfl, hprecision_ne, hvNum, by simpa using hpow_lt⟩

noncomputable def RoundGeNormal_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (f : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) : Unit :=
  ()

/-- Coq discriminant helper `RoundGeNormal`.

For radix two, an exact real input rounded to a normal float is bounded above
by the rounded value times the standard `1 + 2^-precision` factor. -/
theorem RoundGeNormal {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (precision : Nat)
    (f : FloatSpec.Core.Defs.FlocqFloat beta) (r : ℝ) :
    ⦃⌜Closest (beta:=beta) b (2 : ℝ) r f ∧
        Fnormal (beta:=beta) 2 b f ∧
        beta = 2 ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat 2 precision⌝⦄
    (pure (RoundGeNormal_check (beta:=beta) b precision f r) : Id Unit)
    ⦃⇓_ => ⌜|r| ≤ |_root_.F2R (beta:=beta) f| *
        (1 + (2 : ℝ) ^ (-(precision : Int)))⌝⦄ := by
  intro hpre
  rcases hpre with ⟨hClosest, hnormal, hbeta, hprecision, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, RoundGeNormal_check, Id.run, ULift.up_down]
  have hrr := RleRRounded (beta:=beta) b 2 precision f r
    hbeta (by decide) hprecision hvNum hnormal hClosest
  have hpow :
      (2 : ℝ) ^ (1 - (precision : Int)) / 2 =
        (2 : ℝ) ^ (-(precision : Int)) := by
    have htwo_ne : (2 : ℝ) ≠ 0 := by norm_num
    have hexp : (1 - (precision : Int)) = 1 + (-(precision : Int)) := by ring
    rw [hexp, zpow_add₀ htwo_ne, zpow_one]
    ring
  simpa [hpow] using hrr

noncomputable def dexact_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (p q t d : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq discriminant helper `dexact`.

When the first discriminant branch defines `d` as the rounded subtraction
result `t`, Sterbenz exactness for the second-branch inequality gives
`F2R d = F2R p - F2R q`. -/
theorem dexact {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (p q t d : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fbounded (beta:=beta) bo p ∧
        Fbounded (beta:=beta) bo q ∧
        0 ≤ _root_.F2R (beta:=beta) p ∧
        3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| <
          _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q) t ∧
        d = t ∧
        beta = radix ∧ 1 < radix⌝⦄
    (pure (dexact_check (beta:=beta) bo radix precision p q t d) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) d =
      _root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q⌝⦄ := by
  intro hpre
  rcases hpre with
    ⟨hpBound, hqBound, hpNonneg, hSecond, hRoundt, hdDef, hbeta, hradix⟩
  simp only [wp, PostCond.noThrow, pure, dexact_check, Id.run, ULift.up_down]
  have hbeta_gt : 1 < beta := by omega
  have h := t_exact (beta:=beta) bo (radix : ℝ) precision p q t
  have ht :
      _root_.F2R (beta:=beta) t =
        _root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q := by
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, t_exact_check, Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hbeta_gt, hpBound, hqBound, hpNonneg, hSecond, hRoundt⟩
  simpa [hdDef] using ht

noncomputable def IneqEq_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (p q t u v : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq discriminant helper `IneqEq`.

In the first compensated discriminant branch, `u` rounds `3 * |t|` and `v`
rounds `p + q`.  Once `t` is known exact and the branch has `v <= u`,
monotonicity gives the reverse inequality, hence both rounded values have the
same real value. -/
theorem IneqEq {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (p q t u v : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜_root_.F2R (beta:=beta) t =
          _root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (3 * |_root_.F2R (beta:=beta) t|) u ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) v ∧
        3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| <
          _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q ∧
        _root_.F2R (beta:=beta) v ≤ _root_.F2R (beta:=beta) u⌝⦄
    (pure (IneqEq_check (beta:=beta) bo radix precision p q t u v) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) v = _root_.F2R (beta:=beta) u⌝⦄ := by
  intro hpre
  rcases hpre with ⟨htExact, hRoundu, hRoundv, hCase1, hCase2⟩
  simp only [wp, PostCond.noThrow, pure, IneqEq_check, Id.run, ULift.up_down]
  have hCompat : CompatiblePGeneric (EvenClosest (beta:=beta) bo (radix : ℝ) precision) := by
    have h := EvenClosestCompatible_from_structural_payload (beta:=beta) bo (radix : ℝ) precision
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestCompatible_check,
      PredTrans.pure, PredTrans.apply, Id.run, ULift.down, Int.cast_ofNat] using h True.intro
  have hRoundu' :
      EvenClosest (beta:=beta) bo (radix : ℝ) precision
        (3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q|) u := by
    refine hCompat (3 * |_root_.F2R (beta:=beta) t|)
      (3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q|) u u
      hRoundu ?_ rfl
    rw [htExact]
  have hMono : MonotoneP_float (EvenClosest (beta:=beta) bo (radix : ℝ) precision) := by
    have h := EvenClosestMonotone_from_split_radix_payload (beta:=beta) bo (radix : ℝ) precision
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestMonotone_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
  have hulev :
      _root_.F2R (beta:=beta) u ≤ _root_.F2R (beta:=beta) v :=
    hMono (3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q|)
      (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q)
      u v hCase1 hRoundu' hRoundv
  exact le_antisymm hCase2 hulev

/-- If two real inputs have the same normal even-closest result, their
distance is at most one ulp of that result.  This is the quantitative core of
the discrete branch in Coq's `discri10`: each input is within half an ulp, and
canonical uniqueness identifies the two result records (not merely their real
values). -/
private theorem abs_sub_le_fulp_of_evenClosest_eq
    (bo : Fbound_skel) (precision : Nat) (x y : ℝ)
    (u v : FloatSpec.Core.Defs.FlocqFloat 2)
    (hprecision : precision ≠ 0)
    (hvNum : bo.vNum = Zpower_nat 2 precision)
    (huNormal : Fnormal (beta:=2) 2 bo u)
    (hvNormal : Fnormal (beta:=2) 2 bo v)
    (hRoundu : EvenClosest (beta:=2) bo (2 : ℝ) precision x u)
    (hRoundv : EvenClosest (beta:=2) bo (2 : ℝ) precision y v)
    (huv : _root_.F2R (beta:=2) u = _root_.F2R (beta:=2) v) :
    |y - x| ≤ Fulp (beta:=2) bo 2 precision u := by
  have huvFloat : u = v := by
    have h := FcanonicUnique (beta:=2) 2 bo u v (by decide) rfl
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicUnique_check,
      Id.run, ULift.up_down, Fcanonic', Int.cast_ofNat] using
      h ⟨Or.inl huNormal, Or.inl hvNormal, huv⟩
  subst v
  have huErr : 2 * |x - _root_.F2R (beta:=2) u| ≤
      Fulp (beta:=2) bo 2 precision u := by
    have h := ClosestUlp (beta:=2) bo 2 precision x u
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hRoundu.1, rfl, by decide, hprecision, hvNum⟩
  have hvErr : 2 * |y - _root_.F2R (beta:=2) u| ≤
      Fulp (beta:=2) bo 2 precision u := by
    have h := ClosestUlp (beta:=2) bo 2 precision y u
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hRoundv.1, rfl, by decide, hprecision, hvNum⟩
  have htri : |y - x| ≤
      |y - _root_.F2R (beta:=2) u| +
        |x - _root_.F2R (beta:=2) u| := by
    calc
      |y - x| = |(y - _root_.F2R (beta:=2) u) +
          (-(x - _root_.F2R (beta:=2) u))| := by ring_nf
      _ ≤ |y - _root_.F2R (beta:=2) u| +
          |-(x - _root_.F2R (beta:=2) u)| :=
            abs_add_le _ _
      _ = |y - _root_.F2R (beta:=2) u| +
          |x - _root_.F2R (beta:=2) u| := by rw [abs_neg]
  nlinarith

/-- The common-rounding branch of `discri10` forces `q` to lie just above
`p/2`.  This is the source proof's rational inequality, written without a
division so later integer-mantissa arguments can consume it directly. -/
private theorem discri10_q_ratio
    (bo : Fbound_skel) (precision : Nat)
    (p q t u v : FloatSpec.Core.Defs.FlocqFloat 2)
    (hprecision3 : 3 ≤ precision)
    (hvNum : bo.vNum = Zpower_nat 2 precision)
    (huNormal : Fnormal (beta:=2) 2 bo u)
    (hvNormal : Fnormal (beta:=2) 2 bo v)
    (hDNonneg : 0 ≤ _root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q)
    (htExact : _root_.F2R (beta:=2) t =
      _root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q)
    (hRoundu : EvenClosest (beta:=2) bo (2 : ℝ) precision
      (3 * |_root_.F2R (beta:=2) t|) u)
    (hRoundv : EvenClosest (beta:=2) bo (2 : ℝ) precision
      (_root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q) v)
    (hCase1 : 3 * |_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q| <
      _root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q)
    (huv : _root_.F2R (beta:=2) u = _root_.F2R (beta:=2) v) :
    let eps : ℝ := (2 : ℝ) ^ (-(precision : Int))
    (2 + eps) * _root_.F2R (beta:=2) q ≤
      (1 + 2 * eps) * _root_.F2R (beta:=2) p := by
  let eps : ℝ := (2 : ℝ) ^ (-(precision : Int))
  have hprecisionNe : precision ≠ 0 := by omega
  have hepsPos : 0 < eps := by
    dsimp [eps]
    positivity
  have hepsLe : eps ≤ (1 / 8 : ℝ) := by
    have hexp : -(precision : Int) ≤ (-3 : Int) := by omega
    have h := zpow_right_mono₀ (by norm_num : (1 : ℝ) ≤ 2) hexp
    norm_num at h
    simpa [eps, zpow_neg] using h
  have hden : 0 < 1 - eps := by linarith
  have hinputGap :
      abs ((_root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q) -
          3 * |_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q|) ≤
        Fulp (beta:=2) bo 2 precision u := by
    have h := abs_sub_le_fulp_of_evenClosest_eq bo precision
      (3 * |_root_.F2R (beta:=2) t|)
      (_root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q) u v
      hprecisionNe hvNum huNormal hvNormal hRoundu hRoundv huv
    simpa [htExact] using h
  have hgap :
      4 * _root_.F2R (beta:=2) q - 2 * _root_.F2R (beta:=2) p ≤
        Fulp (beta:=2) bo 2 precision u := by
    rw [abs_of_nonneg hDNonneg] at hinputGap hCase1
    have hnonneg : 0 ≤
        (_root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q) -
          3 * (_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q) :=
      le_of_lt (sub_pos.mpr hCase1)
    rw [abs_of_nonneg hnonneg] at hinputGap
    nlinarith
  have huAbs : |_root_.F2R (beta:=2) u| ≤
      (3 * (_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q)) /
        (1 - eps) := by
    have h := RoundLeNormal_from_denominator_payload (beta:=2) bo precision u
      (3 * |_root_.F2R (beta:=2) t|)
    have h' : |_root_.F2R (beta:=2) u| ≤
        abs (3 * |_root_.F2R (beta:=2) t|) / (1 - eps) := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RoundLeNormal_check,
        Id.run, ULift.up_down, eps, Int.cast_ofNat] using
        h ⟨hRoundu.1, huNormal, rfl, hprecisionNe, hvNum, hden⟩
    rw [htExact, abs_of_nonneg hDNonneg] at h'
    simpa [abs_of_nonneg hDNonneg] using h'
  have huNormEq : Fnormalize (beta:=2) 2 bo precision u = u := by
    have h := FcanonicFnormalizeEq (beta:=2) 2 bo precision u
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨Or.inl huNormal, rfl, by decide, hprecisionNe, hvNum⟩
  have huUlp : Fulp (beta:=2) bo 2 precision u ≤
      |_root_.F2R (beta:=2) u| * (2 * eps) := by
    have h := FulpLe2 (beta:=2) 2 bo precision u
    have h' : Fulp (beta:=2) bo 2 precision u ≤
        |_root_.F2R (beta:=2) u| * (2 : ℝ) ^ (1 - (precision : Int)) := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpLe2_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨huNormal.1, by simpa [huNormEq] using huNormal,
          rfl, by decide, hprecisionNe, hvNum⟩
    have hpow : (2 : ℝ) ^ (1 - (precision : Int)) = 2 * eps := by
      rw [show 1 - (precision : Int) = 1 + (-(precision : Int)) by ring,
        zpow_add₀ (by norm_num), zpow_one]
    simpa [hpow] using h'
  have hulpscaled : Fulp (beta:=2) bo 2 precision u ≤
      (6 * eps * (_root_.F2R (beta:=2) p -
        _root_.F2R (beta:=2) q)) / (1 - eps) := by
    have hnonneg : 0 ≤ 2 * eps := by positivity
    have hmul := mul_le_mul_of_nonneg_right huAbs hnonneg
    calc
      Fulp (beta:=2) bo 2 precision u
          ≤ |_root_.F2R (beta:=2) u| * (2 * eps) := huUlp
      _ ≤ ((3 * (_root_.F2R (beta:=2) p -
          _root_.F2R (beta:=2) q)) / (1 - eps)) * (2 * eps) := hmul
      _ = (6 * eps * (_root_.F2R (beta:=2) p -
          _root_.F2R (beta:=2) q)) / (1 - eps) := by ring
  have hcross :
      (4 * _root_.F2R (beta:=2) q - 2 * _root_.F2R (beta:=2) p) *
          (1 - eps) ≤
        6 * eps * (_root_.F2R (beta:=2) p -
          _root_.F2R (beta:=2) q) := by
    apply (le_div_iff₀ hden).mp
    exact le_trans hgap hulpscaled
  dsimp [eps]
  change (2 + eps) * _root_.F2R (beta:=2) q ≤
    (1 + 2 * eps) * _root_.F2R (beta:=2) p
  nlinarith

/-- Mantissa interval of a nonnegative normal binary float.  This is the
small arithmetic fact used by the discrete exponent branches below. -/
private theorem nonnegative_normal_binary_mantissa_bounds
    (bo : Fbound_skel) (precision : Nat)
    (f : FloatSpec.Core.Defs.FlocqFloat 2)
    (hprecision : 1 < precision)
    (hvNum : bo.vNum = Zpower_nat 2 precision)
    (hfNormal : Fnormal (beta:=2) 2 bo f)
    (hfNonneg : 0 ≤ _root_.F2R (beta:=2) f) :
    (2 : Int) ^ (precision - 1) ≤ f.Fnum ∧
      f.Fnum < (2 : Int) ^ precision := by
  have hpowPos : 0 < (2 : ℝ) ^ f.Fexp := zpow_pos (by norm_num) _
  have hfNumNonnegR : 0 ≤ (f.Fnum : ℝ) := by
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R] at hfNonneg
    norm_num at hfNonneg hpowPos
    by_contra hnot
    have hneg : (f.Fnum : ℝ) < 0 := lt_of_not_ge hnot
    have hprodNeg := mul_neg_of_neg_of_pos hneg hpowPos
    linarith
  have hfNumNonneg : 0 ≤ f.Fnum := by exact_mod_cast hfNumNonnegR
  have hfull : 2 * (2 : Int) ^ (precision - 1) = (2 : Int) ^ precision := by
    have hk : precision - 1 + 1 = precision := by omega
    calc
      2 * (2 : Int) ^ (precision - 1) =
          (2 : Int) ^ (precision - 1) * 2 := by ring
      _ = (2 : Int) ^ (precision - 1 + 1) := (pow_succ _ _).symm
      _ = (2 : Int) ^ precision := by rw [hk]
  have hLowerRaw := hfNormal.2
  rw [hvNum, Zpower_nat,
    abs_of_nonneg (mul_nonneg (by norm_num) hfNumNonneg)] at hLowerRaw
  have hUpperRaw := hfNormal.1.1
  rw [hvNum, Zpower_nat, abs_of_nonneg hfNumNonneg] at hUpperRaw
  constructor <;> omega

/-- Exponent adjacency for positive normal binary floats whose real values
are within a factor two. -/
private theorem normal_binary_exponents_adjacent
    (bo : Fbound_skel)
    (p q : FloatSpec.Core.Defs.FlocqFloat 2)
    (hpNormal : Fnormal (beta:=2) 2 bo p)
    (hqNormal : Fnormal (beta:=2) 2 bo q)
    (hpNonneg : 0 ≤ _root_.F2R (beta:=2) p)
    (hqNonneg : 0 ≤ _root_.F2R (beta:=2) q)
    (hqLeP : _root_.F2R (beta:=2) q ≤ _root_.F2R (beta:=2) p)
    (hpLeTwoQ : _root_.F2R (beta:=2) p ≤
      2 * _root_.F2R (beta:=2) q) :
    q.Fexp ≤ p.Fexp ∧ p.Fexp ≤ q.Fexp + 1 := by
  have hqExpLe : q.Fexp ≤ p.Fexp := by
    have h := Fcanonic_Rle_Zle (beta:=2) 2 bo q p (by decide) rfl
    have habsLe : |_root_.F2R (beta:=2) q| ≤
        |_root_.F2R (beta:=2) p| := by
      rw [abs_of_nonneg hqNonneg, abs_of_nonneg hpNonneg]
      exact hqLeP
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fcanonic_Rle_Zle_check,
      Id.run, ULift.up_down, Fcanonic', Int.cast_ofNat] using
      h ⟨Or.inl hqNormal, Or.inl hpNormal, habsLe⟩
  let q2 : FloatSpec.Core.Defs.FlocqFloat 2 :=
    { Fnum := q.Fnum, Fexp := q.Fexp + 1 }
  have hq2Normal : Fnormal (beta:=2) 2 bo q2 := by
    rcases hqNormal with ⟨⟨hqNum, hqExp⟩, hqMant⟩
    exact ⟨⟨by simpa [q2] using hqNum, by dsimp [q2]; omega⟩,
      by simpa [q2] using hqMant⟩
  have hq2Val : _root_.F2R (beta:=2) q2 =
      2 * _root_.F2R (beta:=2) q := by
    have htwo : (2 : ℝ) ≠ 0 := by norm_num
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, q2,
      FloatSpec.Core.Defs.FlocqFloat.Fnum,
      FloatSpec.Core.Defs.FlocqFloat.Fexp]
    change (q.Fnum : ℝ) * (2 : ℝ) ^ (q.Fexp + 1) =
      2 * ((q.Fnum : ℝ) * (2 : ℝ) ^ q.Fexp)
    rw [show q.Fexp + 1 = 1 + q.Fexp by ring,
      zpow_add₀ htwo, zpow_one]
    ring
  have hq2Nonneg : 0 ≤ _root_.F2R (beta:=2) q2 := by
    rw [hq2Val]
    positivity
  have hpExpLe : p.Fexp ≤ q2.Fexp := by
    have h := Fcanonic_Rle_Zle (beta:=2) 2 bo p q2 (by decide) rfl
    have habsLe : |_root_.F2R (beta:=2) p| ≤
        |_root_.F2R (beta:=2) q2| := by
      rw [abs_of_nonneg hpNonneg, abs_of_nonneg hq2Nonneg, hq2Val]
      exact hpLeTwoQ
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fcanonic_Rle_Zle_check,
      Id.run, ULift.up_down, Fcanonic', Int.cast_ofNat] using
      h ⟨Or.inl hpNormal, Or.inl hq2Normal, habsLe⟩
  exact ⟨hqExpLe, by simpa [q2] using hpExpLe⟩

/-- In the equal-`p`/`q`-exponent branch of Coq `discri10`, the exact
difference cannot normalize two or more exponent positions below `p`.  The
proof follows the source argument: round the exact lower-binade witness,
compare it monotonically with `p+q`, and combine that with the upper bound on
the common result of rounding `3*|p-q|`. -/
private theorem discri10_same_exp_d_exp_ge
    (bo : Fbound_skel) (precision : Nat)
    (p q t d u v : FloatSpec.Core.Defs.FlocqFloat 2)
    (hprecision : 1 < precision)
    (hprecision3 : 3 ≤ precision)
    (hvNum : bo.vNum = Zpower_nat 2 precision)
    (hpNormal : Fnormal (beta:=2) 2 bo p)
    (hqNormal : Fnormal (beta:=2) 2 bo q)
    (hdCan : Fcanonic (beta:=2) 2 bo d)
    (huNormal : Fnormal (beta:=2) 2 bo u)
    (hvNormal : Fnormal (beta:=2) 2 bo v)
    (hpNonneg : 0 ≤ _root_.F2R (beta:=2) p)
    (hqNonneg : 0 ≤ _root_.F2R (beta:=2) q)
    (hDNonneg : 0 ≤ _root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q)
    (hU0 : -bo.dExp ≤ p.Fexp - 2)
    (hpqExp : p.Fexp = q.Fexp)
    (htExact : _root_.F2R (beta:=2) t =
      _root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q)
    (hdExact : _root_.F2R (beta:=2) d =
      _root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q)
    (hRoundu : EvenClosest (beta:=2) bo (2 : ℝ) precision
      (3 * |_root_.F2R (beta:=2) t|) u)
    (hRoundv : EvenClosest (beta:=2) bo (2 : ℝ) precision
      (_root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q) v)
    (huv : _root_.F2R (beta:=2) u = _root_.F2R (beta:=2) v) :
    p.Fexp - 1 ≤ d.Fexp := by
  have hprecisionNe : precision ≠ 0 := by omega
  let eps : ℝ := (2 : ℝ) ^ (-(precision : Int))
  have hepsPos : 0 < eps := by dsimp [eps]; positivity
  have hepsLe : eps ≤ (1 / 8 : ℝ) := by
    have hexp : -(precision : Int) ≤ (-3 : Int) := by omega
    have h := zpow_right_mono₀ (by norm_num : (1 : ℝ) ≤ 2) hexp
    norm_num at h
    simpa [eps, zpow_neg] using h
  have hden : 0 < 1 - eps := by linarith
  let n : Int := nNormMin 2 precision
  let f : FloatSpec.Core.Defs.FlocqFloat 2 :=
    { Fnum := n, Fexp := p.Fexp - 1 }
  let g : FloatSpec.Core.Defs.FlocqFloat 2 :=
    { Fnum := 1, Fexp := p.Fexp + (precision : Int) }
  have hnPos : 0 < n := by
    dsimp [n, nNormMin]
    positivity
  have hnumBound : n < bo.vNum := by
    rw [hvNum]
    dsimp [n, nNormMin, Zpower_nat]
    exact pow_lt_pow_right₀ (by decide : (1 : Int) < 2) (by omega)
  have hfNormal : Fnormal (beta:=2) 2 bo f := by
    constructor
    · constructor
      · simpa [f, abs_of_pos hnPos] using hnumBound
      · dsimp [f]
        omega
    · have hposNorm : bo.vNum = 2 * n := by
        simpa [n] using PosNormMin 2 bo precision (by omega) hvNum
      rw [hposNorm]
      simp [f, abs_of_pos hnPos]
  have hgBound : Fbounded (beta:=2) bo g := by
    constructor
    · have hvGt : 1 < bo.vNum := by
        rw [hvNum, Zpower_nat]
        exact one_lt_pow₀ (by decide : (1 : Int) < 2) (by omega)
      simpa [g] using hvGt
    · dsimp [g]
      omega
  have hpowN : (n : ℝ) = (2 : ℝ) ^ ((precision : Int) - 1) := by
    dsimp [n, nNormMin]
    rw [Int.cast_pow, ← zpow_natCast]
    congr 1
    omega
  have hfVal : _root_.F2R (beta:=2) f =
      (2 : ℝ) ^ ((precision : Int) - 2 + p.Fexp) := by
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, f,
      FloatSpec.Core.Defs.FlocqFloat.Fnum,
      FloatSpec.Core.Defs.FlocqFloat.Fexp]
    rw [hpowN]
    change (2 : ℝ) ^ ((precision : Int) - 1) *
        (2 : ℝ) ^ (p.Fexp - 1) =
      (2 : ℝ) ^ ((precision : Int) - 2 + p.Fexp)
    rw [← zpow_add₀ (by norm_num)]
    congr 1
    ring
  have hgVal : _root_.F2R (beta:=2) g =
      2 * (2 : ℝ) ^ ((precision : Int) - 1 + p.Fexp) := by
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, g,
      FloatSpec.Core.Defs.FlocqFloat.Fnum,
      FloatSpec.Core.Defs.FlocqFloat.Fexp, Int.cast_one, one_mul]
    change (2 : ℝ) ^ (p.Fexp + (precision : Int)) =
      2 * (2 : ℝ) ^ ((precision : Int) - 1 + p.Fexp)
    rw [show p.Fexp + (precision : Int) =
      1 + ((precision : Int) - 1 + p.Fexp) by ring,
      zpow_add₀ (by norm_num), zpow_one]
  rcases nonnegative_normal_binary_mantissa_bounds bo precision p hprecision
      hvNum hpNormal hpNonneg with ⟨hpNumLo, _hpNumHi⟩
  rcases nonnegative_normal_binary_mantissa_bounds bo precision q hprecision
      hvNum hqNormal hqNonneg with ⟨hqNumLo, _hqNumHi⟩
  have hscalePos : 0 < (2 : ℝ) ^ p.Fexp := by positivity
  have hpLower : (2 : ℝ) ^ ((precision : Int) - 1 + p.Fexp) ≤
      _root_.F2R (beta:=2) p := by
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R]
    change (2 : ℝ) ^ ((precision : Int) - 1 + p.Fexp) ≤
      (p.Fnum : ℝ) * (2 : ℝ) ^ p.Fexp
    rw [show (precision : Int) - 1 + p.Fexp =
        ((precision : Int) - 1) + p.Fexp by ring,
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0), ← hpowN]
    exact mul_le_mul_of_nonneg_right (by exact_mod_cast hpNumLo)
      (le_of_lt hscalePos)
  have hqLower : (2 : ℝ) ^ ((precision : Int) - 1 + p.Fexp) ≤
      _root_.F2R (beta:=2) q := by
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R]
    rw [hpqExp]
    change (2 : ℝ) ^ ((precision : Int) - 1 + q.Fexp) ≤
      (q.Fnum : ℝ) * (2 : ℝ) ^ q.Fexp
    rw [show (precision : Int) - 1 + q.Fexp =
        ((precision : Int) - 1) + q.Fexp by ring,
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0), ← hpowN]
    exact mul_le_mul_of_nonneg_right (by exact_mod_cast hqNumLo)
      (le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) q.Fexp))
  have hgLeSum : _root_.F2R (beta:=2) g ≤
      _root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q := by
    rw [hgVal]
    linarith
  have hRounded : RoundedModeP (beta:=2) bo
      (EvenClosest (beta:=2) bo (2 : ℝ) precision) := by
    have h := EvenClosestRoundedModeP (beta:=2) bo 2 precision
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestRoundedModeP_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨rfl, by decide, hprecision, hvNum⟩
  have hRoundg : EvenClosest (beta:=2) bo (2 : ℝ) precision
      (_root_.F2R (beta:=2) g) g :=
    RoundedModeProjectorIdem bo
      (EvenClosest (beta:=2) bo (2 : ℝ) precision) g hRounded hgBound
  have hgLeV : _root_.F2R (beta:=2) g ≤ _root_.F2R (beta:=2) v := by
    have h := EvenClosestMonotone2 (beta:=2) bo 2 precision
      (_root_.F2R (beta:=2) g)
      (_root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q) g v
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestMonotone2_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, by decide, hprecision, hvNum, hgLeSum, hRoundg, hRoundv⟩
  have hvNonneg : 0 ≤ _root_.F2R (beta:=2) v := by
    exact P_positive bo (2 : ℝ)
      (_root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q) v
      hRoundv.1 (by linarith)
  have huAbs : |_root_.F2R (beta:=2) u| ≤
      (3 * (_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q)) /
        (1 - eps) := by
    have h := RoundLeNormal_from_denominator_payload (beta:=2) bo precision u
      (3 * |_root_.F2R (beta:=2) t|)
    have h' : |_root_.F2R (beta:=2) u| ≤
        abs (3 * |_root_.F2R (beta:=2) t|) / (1 - eps) := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RoundLeNormal_check,
        Id.run, ULift.up_down, eps, Int.cast_ofNat] using
        h ⟨hRoundu.1, huNormal, rfl, hprecisionNe, hvNum, hden⟩
    rw [htExact, abs_of_nonneg hDNonneg] at h'
    simpa [abs_of_nonneg hDNonneg] using h'
  have hvScaled : _root_.F2R (beta:=2) v * (1 - eps) ≤
      3 * (_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q) := by
    have huNonneg : 0 ≤ _root_.F2R (beta:=2) u := by linarith [huv]
    rw [abs_of_nonneg huNonneg] at huAbs
    have hcross := (le_div_iff₀ hden).mp huAbs
    rw [huv] at hcross
    nlinarith
  have hthreeF : 3 * _root_.F2R (beta:=2) f ≤
      _root_.F2R (beta:=2) g * (1 - eps) := by
    rw [hfVal, hgVal]
    have hpowPos : 0 < (2 : ℝ) ^ ((precision : Int) - 2 + p.Fexp) := by
      positivity
    have hpowRel : (2 : ℝ) ^ ((precision : Int) - 1 + p.Fexp) =
        2 * (2 : ℝ) ^ ((precision : Int) - 2 + p.Fexp) := by
      rw [show (precision : Int) - 1 + p.Fexp =
        1 + ((precision : Int) - 2 + p.Fexp) by ring,
        zpow_add₀ (by norm_num), zpow_one]
    rw [hpowRel]
    nlinarith
  have hfLeD : _root_.F2R (beta:=2) f ≤
      _root_.F2R (beta:=2) d := by
    rw [hdExact]
    nlinarith [hthreeF, mul_le_mul_of_nonneg_right hgLeV
      (le_of_lt hden), hvScaled]
  have hfNonneg : 0 ≤ _root_.F2R (beta:=2) f := by
    rw [hfVal]
    positivity
  have hdNonneg : 0 ≤ _root_.F2R (beta:=2) d := by
    rw [hdExact]
    exact hDNonneg
  have h := Fcanonic_Rle_Zle (beta:=2) 2 bo f d (by decide) rfl
  have habsLe : |_root_.F2R (beta:=2) f| ≤
      |_root_.F2R (beta:=2) d| := by
    rw [abs_of_nonneg hfNonneg, abs_of_nonneg hdNonneg]
    exact hfLeD
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fcanonic_Rle_Zle_check,
    Id.run, ULift.up_down, Fcanonic', f, Int.cast_ofNat] using
    h ⟨Or.inl hfNormal, hdCan, habsLe⟩

/-- The remaining strict-exponent subcase in Coq `discri10` is impossible.
Normal binary mantissas put `p` and `q` in adjacent binades.  Canonicity of
the exact difference then makes its integer coefficient strictly smaller than
the lower normal mantissa.  The common-rounding ratio forces the opposite:
after cancelling the common power of two, even the smallest possible integer
gap is too large. -/
private theorem discri10_no_lower_q_exp
    (bo : Fbound_skel) (precision : Nat)
    (p q t d u v : FloatSpec.Core.Defs.FlocqFloat 2)
    (hprecision : 1 < precision)
    (hprecision3 : 3 ≤ precision)
    (hvNum : bo.vNum = Zpower_nat 2 precision)
    (hpNormal : Fnormal (beta:=2) 2 bo p)
    (hqNormal : Fnormal (beta:=2) 2 bo q)
    (hdCan : Fcanonic (beta:=2) 2 bo d)
    (huNormal : Fnormal (beta:=2) 2 bo u)
    (hvNormal : Fnormal (beta:=2) 2 bo v)
    (hpNonneg : 0 ≤ _root_.F2R (beta:=2) p)
    (hqPos : 0 < _root_.F2R (beta:=2) q)
    (hqLeP : _root_.F2R (beta:=2) q ≤ _root_.F2R (beta:=2) p)
    (hpLeTwoQ : _root_.F2R (beta:=2) p ≤
      2 * _root_.F2R (beta:=2) q)
    (hDNonneg : 0 ≤ _root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q)
    (hU0 : -bo.dExp ≤ p.Fexp - 2)
    (htExact : _root_.F2R (beta:=2) t =
      _root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q)
    (hdExact : _root_.F2R (beta:=2) d =
      _root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q)
    (hRoundu : EvenClosest (beta:=2) bo (2 : ℝ) precision
      (3 * |_root_.F2R (beta:=2) t|) u)
    (hRoundv : EvenClosest (beta:=2) bo (2 : ℝ) precision
      (_root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q) v)
    (hCase1 : 3 * |_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q| <
      _root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q)
    (huv : _root_.F2R (beta:=2) u = _root_.F2R (beta:=2) v)
    (hqExpLt : q.Fexp < p.Fexp)
    (hdExpLt : d.Fexp < q.Fexp) : False := by
  have hqNonneg : 0 ≤ _root_.F2R (beta:=2) q := le_of_lt hqPos
  have hadj := normal_binary_exponents_adjacent bo p q hpNormal hqNormal
    hpNonneg hqNonneg hqLeP hpLeTwoQ
  have hpqExp : p.Fexp = q.Fexp + 1 := by omega
  have hdBound : Fbounded (beta:=2) bo d := by
    have h := FcanonicBound (beta:=2) 2 bo d
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicBound_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hdCan
  let n : Int := nNormMin 2 precision
  have hnPos : 0 < n := by
    dsimp [n, nNormMin]
    positivity
  have hvNumN : bo.vNum = 2 * n := by
    simpa [n] using PosNormMin 2 bo precision (by omega) hvNum
  rcases nonnegative_normal_binary_mantissa_bounds bo precision p hprecision
      hvNum hpNormal hpNonneg with ⟨hpLo, hpHi⟩
  rcases nonnegative_normal_binary_mantissa_bounds bo precision q hprecision
      hvNum hqNormal hqNonneg with ⟨hqLo, hqHi⟩
  have hscalePos : 0 < (2 : ℝ) ^ q.Fexp := by positivity
  have hpVal : _root_.F2R (beta:=2) p =
      (2 * (p.Fnum : ℝ)) * (2 : ℝ) ^ q.Fexp := by
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R]
    rw [hpqExp, zpow_add₀ (by norm_num), zpow_one]
    ring
  have hqVal : _root_.F2R (beta:=2) q =
      (q.Fnum : ℝ) * (2 : ℝ) ^ q.Fexp := rfl
  have hcoeffNonneg : 0 ≤ 2 * p.Fnum - q.Fnum := by
    rw [hpVal, hqVal] at hDNonneg
    have hcast : (0 : ℝ) ≤ 2 * (p.Fnum : ℝ) - (q.Fnum : ℝ) := by
      nlinarith
    exact_mod_cast hcast
  have hcoeffLtN : 2 * p.Fnum - q.Fnum < n := by
    let coeff : Int := 2 * p.Fnum - q.Fnum
    let factor : ℝ := (2 : ℝ) ^ (q.Fexp - d.Fexp)
    have hfactorGe : (2 : ℝ) ≤ factor := by
      have hexp : (1 : Int) ≤ q.Fexp - d.Fexp := by omega
      have h := zpow_right_mono₀ (by norm_num : (1 : ℝ) ≤ 2) hexp
      simpa [factor] using h
    have hdpowPos : 0 < (2 : ℝ) ^ d.Fexp := by positivity
    have hqpow : (2 : ℝ) ^ q.Fexp =
        (2 : ℝ) ^ d.Fexp * factor := by
      dsimp [factor]
      rw [← zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0)]
      congr 1
      ring
    have hreal : (d.Fnum : ℝ) * (2 : ℝ) ^ d.Fexp =
        (coeff : ℝ) * (2 : ℝ) ^ q.Fexp := by
      calc
        (d.Fnum : ℝ) * (2 : ℝ) ^ d.Fexp =
            _root_.F2R (beta:=2) d := rfl
        _ = _root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q := hdExact
        _ = (coeff : ℝ) * (2 : ℝ) ^ q.Fexp := by
          rw [hpVal, hqVal]
          dsimp [coeff]
          push_cast
          ring
    rw [hqpow] at hreal
    have hdNumEq : (d.Fnum : ℝ) = (coeff : ℝ) * factor := by
      have hmul : (d.Fnum : ℝ) * (2 : ℝ) ^ d.Fexp =
          ((coeff : ℝ) * factor) * (2 : ℝ) ^ d.Fexp := by
        calc
          (d.Fnum : ℝ) * (2 : ℝ) ^ d.Fexp =
              (coeff : ℝ) * ((2 : ℝ) ^ d.Fexp * factor) := hreal
          _ = ((coeff : ℝ) * factor) * (2 : ℝ) ^ d.Fexp := by ring
      exact (mul_right_cancel₀ (ne_of_gt hdpowPos)) hmul
    have hcoeffNonnegR : (0 : ℝ) ≤ (coeff : ℝ) := by
      exact_mod_cast hcoeffNonneg
    have hdNumGe : (2 : ℝ) * (coeff : ℝ) ≤ (d.Fnum : ℝ) := by
      rw [hdNumEq]
      simpa [mul_comm] using
        (mul_le_mul_of_nonneg_left hfactorGe hcoeffNonnegR)
    have hdNumNonnegR : (0 : ℝ) ≤ (d.Fnum : ℝ) :=
      le_trans (mul_nonneg (by norm_num) hcoeffNonnegR) hdNumGe
    have hdNumLt : (d.Fnum : ℝ) < 2 * (n : ℝ) := by
      have hdNumInt : |d.Fnum| < 2 * n := by
        simpa [hvNumN] using hdBound.1
      rw [abs_of_nonneg (by exact_mod_cast hdNumNonnegR)] at hdNumInt
      exact_mod_cast hdNumInt
    have hcoeffLt : (coeff : ℝ) < (n : ℝ) := by nlinarith
    exact_mod_cast hcoeffLt
  have hmpLtMq : p.Fnum < q.Fnum := by
    rw [abs_of_nonneg hDNonneg, hpVal, hqVal] at hCase1
    have hcast : (p.Fnum : ℝ) < (q.Fnum : ℝ) := by
      nlinarith
    exact_mod_cast hcast
  have hratio := discri10_q_ratio bo precision p q t u v hprecision3 hvNum
    huNormal hvNormal hDNonneg htExact hRoundu hRoundv hCase1 huv
  let eps : ℝ := (2 : ℝ) ^ (-(precision : Int))
  have hratio' : (2 + eps) * (q.Fnum : ℝ) ≤
      (1 + 2 * eps) * (2 * (p.Fnum : ℝ)) := by
    have h := hratio
    dsimp [eps] at h ⊢
    rw [hpVal, hqVal] at h
    have hmul : ((2 + (2 : ℝ) ^ (-(precision : Int))) * (q.Fnum : ℝ)) *
          (2 : ℝ) ^ q.Fexp ≤
        ((1 + 2 * (2 : ℝ) ^ (-(precision : Int))) *
          (2 * (p.Fnum : ℝ))) * (2 : ℝ) ^ q.Fexp := by
      nlinarith [h]
    nlinarith
  have hepsN : eps * (n : ℝ) = 1 / 2 := by
    dsimp [eps, n, nNormMin]
    rw [Int.cast_pow, ← zpow_natCast]
    change (2 : ℝ) ^ (-(precision : Int)) *
        (2 : ℝ) ^ ((precision - 1 : Nat) : Int) = 1 / 2
    rw [← zpow_add₀ (by norm_num)]
    have hexp : -(precision : Int) + ((precision - 1 : Nat) : Int) = -1 := by
      omega
    rw [hexp]
    norm_num
  let k : Int := q.Fnum - p.Fnum
  have hkPos : 0 < k := by dsimp [k]; omega
  have hpk : 2 * p.Fnum - q.Fnum = p.Fnum - k := by
    dsimp [k]
    ring
  have hkLower : p.Fnum - n < k := by omega
  have hratioK : (2 + eps) * (k : ℝ) ≤
      3 * eps * (p.Fnum : ℝ) := by
    have hkCast : (k : ℝ) = (q.Fnum : ℝ) - (p.Fnum : ℝ) := by
      simp [k]
    rw [hkCast]
    nlinarith [hratio']
  have hepsPos : 0 < eps := by dsimp [eps]; positivity
  by_cases hkOne : k = 1
  · have hpLoN : n ≤ p.Fnum := by
      simpa [n, nNormMin] using hpLo
    have hpNum : p.Fnum = n := by omega
    rw [hkOne, hpNum] at hratioK
    norm_num at hratioK
    nlinarith [hepsN]
  · have hkTwo : 2 ≤ k := by omega
    have hpLtTwoN : p.Fnum < 2 * n := by
      have hpowEq : (2 : Int) ^ precision = 2 * n := by
        rw [← hvNumN, hvNum]
        rfl
      omega
    have hepsP : eps * (p.Fnum : ℝ) < 1 := by
      have hpCast : (p.Fnum : ℝ) < 2 * (n : ℝ) := by exact_mod_cast hpLtTwoN
      have hm := mul_lt_mul_of_pos_left hpCast hepsPos
      nlinarith [hepsN]
    have hkCast : (2 : ℝ) ≤ (k : ℝ) := by exact_mod_cast hkTwo
    nlinarith [hratioK]

noncomputable def discri9_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q t s d : FloatSpec.Core.Defs.FlocqFloat beta)
    (dp dq : ℝ) : Unit :=
  ()

/-- Coq discriminant theorem `discri9`, factored at the branch dispatch.

Upstream first splits between the direct subtraction branch, the large-residual
second branch, exponent-adjacent compensated branches handled by `discri8`, and
the same-exponent residual branch handled by `discri4`.  This local theorem
restores the dispatch name while keeping those branch payloads explicit. -/
theorem discri9_from_branch_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q t s d : FloatSpec.Core.Defs.FlocqFloat beta)
    (dp dq : ℝ) :
    ⦃⌜EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b') p ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c) q ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q) t ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision (dp - dq) s ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) s) d ∧
        _root_.F2R (beta:=beta) t =
          _root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q ∧
        dp = _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
          _root_.F2R (beta:=beta) p ∧
        dq = _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c -
          _root_.F2R (beta:=beta) q ∧
        beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        bo.vNum = Zpower_nat radix precision ∧
        ((EvenClosest (beta:=beta) bo (radix : ℝ) precision
              (_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q) d ∧
            ((Fulp (beta:=beta) bo radix precision p ≤
                  2 * Fulp (beta:=beta) bo radix precision d ∧
                2 * Fulp (beta:=beta) bo radix precision q ≤
                  Fulp (beta:=beta) bo radix precision p) ∨
              (Fulp (beta:=beta) bo radix precision q ≤
                  2 * Fulp (beta:=beta) bo radix precision d ∧
                2 * Fulp (beta:=beta) bo radix precision p ≤
                  Fulp (beta:=beta) bo radix precision q) ∨
              (Fulp (beta:=beta) bo radix precision p ≤
                  Fulp (beta:=beta) bo radix precision d ∧
                Fulp (beta:=beta) bo radix precision q ≤
                  Fulp (beta:=beta) bo radix precision d))) ∨
          (Fbounded (beta:=beta) bo p ∧
            Fbounded (beta:=beta) bo q ∧
            Fbounded (beta:=beta) bo s ∧
            Fbounded (beta:=beta) bo d ∧
            Fcanonic (beta:=beta) beta bo s ∧
            Fcanonic (beta:=beta) beta bo d ∧
            0 ≤ _root_.F2R (beta:=beta) p ∧
            3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| <
              _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q ∧
            3 *
              min (Fulp (beta:=beta) bo radix precision p)
                (Fulp (beta:=beta) bo radix precision q) ≤
              |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| ∧
            |_root_.F2R (beta:=beta) s| ≤
              |_root_.F2R (beta:=beta) t| / 2 ∧
            Fulp (beta:=beta) bo radix precision q ≤
              2 * Fulp (beta:=beta) bo radix precision p ∧
            Fulp (beta:=beta) bo radix precision p ≤
              2 * Fulp (beta:=beta) bo radix precision q ∧
            MonotoneP_float (EvenClosest (beta:=beta) bo (radix : ℝ) precision) ∧
            UniqueP (EvenClosest (beta:=beta) bo (radix : ℝ) precision)) ∨
          ((0 < dp * dq ∧
              ∃ f : FloatSpec.Core.Defs.FlocqFloat beta,
                Fbounded (beta:=beta) bo f ∧
                  _root_.F2R (beta:=beta) f = dp - dq) ∨
            (0 < dp ∧ (dq < 0) ∧
              Fbounded (beta:=beta) bo p ∧
              Fbounded (beta:=beta) bo q ∧
              Fbounded (beta:=beta) bo s ∧
              Fbounded (beta:=beta) bo d ∧
              Fcanonic (beta:=beta) beta bo s ∧
              Fcanonic (beta:=beta) beta bo d ∧
              0 ≤ _root_.F2R (beta:=beta) p ∧
              3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| <
                _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q ∧
              3 *
                min (Fulp (beta:=beta) bo radix precision p)
                  (Fulp (beta:=beta) bo radix precision q) ≤
                |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| ∧
              |_root_.F2R (beta:=beta) s| ≤
                |_root_.F2R (beta:=beta) t| / 2 ∧
              Fulp (beta:=beta) bo radix precision q ≤
                2 * Fulp (beta:=beta) bo radix precision p ∧
              Fulp (beta:=beta) bo radix precision p ≤
                2 * Fulp (beta:=beta) bo radix precision q ∧
              MonotoneP_float (EvenClosest (beta:=beta) bo (radix : ℝ) precision) ∧
              UniqueP (EvenClosest (beta:=beta) bo (radix : ℝ) precision)) ∨
            (dp < 0 ∧ 0 < dq ∧
              ∃ f : FloatSpec.Core.Defs.FlocqFloat beta,
                Fbounded (beta:=beta) bo f ∧
                  _root_.F2R (beta:=beta) f = dp - dq) ∨
            (∃ f : FloatSpec.Core.Defs.FlocqFloat beta,
              Fbounded (beta:=beta) bo f ∧
                _root_.F2R (beta:=beta) f = dp - dq)) ∨
          (p.Fexp = q.Fexp ∧
            ∃ f : FloatSpec.Core.Defs.FlocqFloat beta,
              Fbounded (beta:=beta) bo f ∧
                _root_.F2R (beta:=beta) f = dp - dq))⌝⦄
    (pure (discri9_check (beta:=beta) bo radix precision a b b' c p q t s d dp dq) :
      Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) d -
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| ≤
        2 * Fulp (beta:=beta) bo radix precision d⌝⦄ := by
  intro hpre
  rcases hpre with
    ⟨hRoundp, hRoundq, hRoundt, hRounds, hRoundd, htExact, hdpEq, hdqEq,
      hbeta, hradix, hprecision, hvNum, hbranch⟩
  simp only [wp, PostCond.noThrow, pure, discri9_check, Id.run, ULift.up_down]
  rcases hbranch with hfirst | hrest
  · rcases hfirst with ⟨hRounddDirect, hcmp⟩
    have h := discri1_from_ulp_comparison_payload (beta:=beta) bo radix precision a b b' c p q d
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri1_check, Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hRoundp, hRoundq, hRounddDirect, hbeta, hradix, hprecision, hvNum, hcmp⟩
  rcases hrest with hsecond | hrest
  · rcases hsecond with
      ⟨hpBound, hqBound, hsBound, hdBound, hsCan, hdCan, hpNonneg,
        hSecond, hlarge, hsHalf, hq_le_twice_p, hp_le_twice_q, hMono, hUnique⟩
    have h := discri2_from_half_error_payload (beta:=beta) bo radix precision a b b' c p q t s d dp dq
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri2_check, Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hRoundp, hRoundq, hRoundt, hRounds, hRoundd, hdpEq, hdqEq,
        hpBound, hqBound, hsBound, hdBound, hsCan, hdCan, hpNonneg,
        hSecond, hlarge, hsHalf, hq_le_twice_p, hp_le_twice_q, hMono,
        hUnique, hbeta, hradix, hprecision, hvNum⟩
  rcases hrest with hcase8 | hsameExp
  · have h := discri8_from_sign_split_payload (beta:=beta) bo radix precision a b b' c p q t s d dp dq
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri8_check, Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hRoundp, hRoundq, hRoundt, hRounds, hRoundd, htExact, hdpEq, hdqEq,
        hbeta, hradix, hprecision, hvNum, hcase8⟩
  · rcases hsameExp with ⟨hpqExp, hResidual⟩
    have h := discri4_from_residual_witness_payload (beta:=beta) bo radix precision a b b' c p q t s d dp dq
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri4_check, Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hRoundd, hRounds, htExact, hdpEq, hdqEq, hpqExp, hResidual,
        hbeta, hradix, hprecision, hvNum⟩

def discri9_precondition {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q t s d : FloatSpec.Core.Defs.FlocqFloat beta)
    (dp dq : ℝ) : Prop :=
  EvenClosest (beta:=beta) bo (radix : ℝ) precision
      (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b') p ∧
    EvenClosest (beta:=beta) bo (radix : ℝ) precision
      (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c) q ∧
    EvenClosest (beta:=beta) bo (radix : ℝ) precision
      (_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q) t ∧
    EvenClosest (beta:=beta) bo (radix : ℝ) precision (dp - dq) s ∧
    EvenClosest (beta:=beta) bo (radix : ℝ) precision
      (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) s) d ∧
    _root_.F2R (beta:=beta) t =
      _root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q ∧
    dp = _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
      _root_.F2R (beta:=beta) p ∧
    dq = _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c -
      _root_.F2R (beta:=beta) q ∧
    beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
    bo.vNum = Zpower_nat radix precision ∧
    ((EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q) d ∧
        ((Fulp (beta:=beta) bo radix precision p ≤
              2 * Fulp (beta:=beta) bo radix precision d ∧
            2 * Fulp (beta:=beta) bo radix precision q ≤
              Fulp (beta:=beta) bo radix precision p) ∨
          (Fulp (beta:=beta) bo radix precision q ≤
              2 * Fulp (beta:=beta) bo radix precision d ∧
            2 * Fulp (beta:=beta) bo radix precision p ≤
              Fulp (beta:=beta) bo radix precision q) ∨
          (Fulp (beta:=beta) bo radix precision p ≤
              Fulp (beta:=beta) bo radix precision d ∧
            Fulp (beta:=beta) bo radix precision q ≤
              Fulp (beta:=beta) bo radix precision d))) ∨
      (Fbounded (beta:=beta) bo p ∧
        Fbounded (beta:=beta) bo q ∧
        Fbounded (beta:=beta) bo s ∧
        Fbounded (beta:=beta) bo d ∧
        Fcanonic (beta:=beta) beta bo s ∧
        Fcanonic (beta:=beta) beta bo d ∧
        0 ≤ _root_.F2R (beta:=beta) p ∧
        3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| <
          _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q ∧
        3 *
          min (Fulp (beta:=beta) bo radix precision p)
            (Fulp (beta:=beta) bo radix precision q) ≤
          |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| ∧
        |_root_.F2R (beta:=beta) s| ≤
          |_root_.F2R (beta:=beta) t| / 2 ∧
        Fulp (beta:=beta) bo radix precision q ≤
          2 * Fulp (beta:=beta) bo radix precision p ∧
        Fulp (beta:=beta) bo radix precision p ≤
          2 * Fulp (beta:=beta) bo radix precision q ∧
        MonotoneP_float (EvenClosest (beta:=beta) bo (radix : ℝ) precision) ∧
        UniqueP (EvenClosest (beta:=beta) bo (radix : ℝ) precision)) ∨
      ((0 < dp * dq ∧
          ∃ f : FloatSpec.Core.Defs.FlocqFloat beta,
            Fbounded (beta:=beta) bo f ∧
              _root_.F2R (beta:=beta) f = dp - dq) ∨
        (0 < dp ∧ (dq < 0) ∧
          Fbounded (beta:=beta) bo p ∧
          Fbounded (beta:=beta) bo q ∧
          Fbounded (beta:=beta) bo s ∧
          Fbounded (beta:=beta) bo d ∧
          Fcanonic (beta:=beta) beta bo s ∧
          Fcanonic (beta:=beta) beta bo d ∧
          0 ≤ _root_.F2R (beta:=beta) p ∧
          3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| <
            _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q ∧
          3 *
            min (Fulp (beta:=beta) bo radix precision p)
              (Fulp (beta:=beta) bo radix precision q) ≤
            |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| ∧
          |_root_.F2R (beta:=beta) s| ≤
            |_root_.F2R (beta:=beta) t| / 2 ∧
          Fulp (beta:=beta) bo radix precision q ≤
            2 * Fulp (beta:=beta) bo radix precision p ∧
          Fulp (beta:=beta) bo radix precision p ≤
            2 * Fulp (beta:=beta) bo radix precision q ∧
          MonotoneP_float (EvenClosest (beta:=beta) bo (radix : ℝ) precision) ∧
          UniqueP (EvenClosest (beta:=beta) bo (radix : ℝ) precision)) ∨
        (dp < 0 ∧ 0 < dq ∧
          ∃ f : FloatSpec.Core.Defs.FlocqFloat beta,
            Fbounded (beta:=beta) bo f ∧
              _root_.F2R (beta:=beta) f = dp - dq) ∨
        (∃ f : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded (beta:=beta) bo f ∧
            _root_.F2R (beta:=beta) f = dp - dq)) ∨
      (p.Fexp = q.Fexp ∧
        ∃ f : FloatSpec.Core.Defs.FlocqFloat beta,
          Fbounded (beta:=beta) bo f ∧
            _root_.F2R (beta:=beta) f = dp - dq))

/-- In the close second branch of `discri9`, a larger exponent can only be
one binade above the smaller operand, and its mantissa is the lower normal
boundary.  This is exactly the representation fact constructed in the Coq
proof before invoking `discri8`. -/
private theorem upper_normal_boundary_of_close
    (bo : Fbound_skel) (precision : Nat)
    (p q : FloatSpec.Core.Defs.FlocqFloat 2)
    (hprecision : 1 < precision)
    (hvNum : bo.vNum = Zpower_nat 2 precision)
    (hpNormal : Fnormal (beta:=2) 2 bo p)
    (hqNormal : Fnormal (beta:=2) 2 bo q)
    (hpNonneg : 0 ≤ _root_.F2R (beta:=2) p)
    (hqNonneg : 0 ≤ _root_.F2R (beta:=2) q)
    (hSecond : 3 * |_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q| <
      _root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q)
    (hSmall : |_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q| <
      3 * min ((2 : ℝ) ^ p.Fexp) ((2 : ℝ) ^ q.Fexp))
    (hqExpLt : q.Fexp < p.Fexp) :
    q.Fexp = p.Fexp - 1 ∧
      p = ⟨Zpower_nat 2 (precision - 1), q.Fexp + 1⟩ ∧
      _root_.F2R (beta:=2) p = (2 : ℝ) ^ ((precision : Int) + q.Fexp) := by
  rcases nonnegative_normal_binary_mantissa_bounds bo precision p hprecision
      hvNum hpNormal hpNonneg with ⟨hpLo, hpHi⟩
  rcases nonnegative_normal_binary_mantissa_bounds bo precision q hprecision
      hvNum hqNormal hqNonneg with ⟨hqLo, hqHi⟩
  let lo : Int := (2 : Int) ^ (precision - 1)
  have hloPos : 0 < lo := by positivity
  have hfull : (2 : Int) ^ precision = 2 * lo := by
    dsimp [lo]
    have hk : precision - 1 + 1 = precision := by omega
    symm
    calc
      2 * (2 : Int) ^ (precision - 1) = (2 : Int) ^ (precision - 1) * 2 := by ring
      _ = (2 : Int) ^ (precision - 1 + 1) := (pow_succ _ _).symm
      _ = (2 : Int) ^ precision := by rw [hk]
  have hpLo' : lo ≤ p.Fnum := by simpa [lo] using hpLo
  have hqLo' : lo ≤ q.Fnum := by simpa [lo] using hqLo
  have hpHi' : p.Fnum < 2 * lo := by simpa [hfull] using hpHi
  have hqHi' : q.Fnum < 2 * lo := by simpa [hfull] using hqHi
  have hpScalePos : 0 < (2 : ℝ) ^ p.Fexp := zpow_pos (by norm_num) _
  have hqScalePos : 0 < (2 : ℝ) ^ q.Fexp := zpow_pos (by norm_num) _
  have hpLower : (lo : ℝ) * (2 : ℝ) ^ p.Fexp ≤
      _root_.F2R (beta:=2) p := by
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R]
    exact mul_le_mul_of_nonneg_right (by exact_mod_cast hpLo') (le_of_lt hpScalePos)
  have hqUpper : _root_.F2R (beta:=2) q <
      ((2 * lo : Int) : ℝ) * (2 : ℝ) ^ q.Fexp := by
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R]
    exact mul_lt_mul_of_pos_right (by exact_mod_cast hqHi') hqScalePos
  have hExpLower : p.Fexp - 1 ≤ q.Fexp := by
    by_contra hnot
    have hqLe : q.Fexp ≤ p.Fexp - 2 := by omega
    have hpowLe : (2 : ℝ) ^ q.Fexp ≤ (2 : ℝ) ^ (p.Fexp - 2) :=
      zpow_right_mono₀ (by norm_num) hqLe
    have htwoLoNonneg : (0 : ℝ) ≤ ((2 * lo : Int) : ℝ) := by positivity
    have hqUpper' : _root_.F2R (beta:=2) q <
        ((2 * lo : Int) : ℝ) * (2 : ℝ) ^ (p.Fexp - 2) :=
      lt_of_lt_of_le hqUpper (mul_le_mul_of_nonneg_left hpowLe htwoLoNonneg)
    have hscaleIdentity :
        ((2 * lo : Int) : ℝ) * (2 : ℝ) ^ (p.Fexp - 2) =
          ((lo : ℝ) * (2 : ℝ) ^ p.Fexp) / 2 := by
      have hz : (2 : ℝ) ^ p.Fexp = (2 : ℝ) ^ (p.Fexp - 2) * 4 := by
        rw [show p.Fexp = p.Fexp - 2 + 2 by omega, zpow_add₀ (by norm_num)]
        norm_num
      push_cast
      rw [hz]
      ring
    rw [hscaleIdentity] at hqUpper'
    have hqLtHalfP : _root_.F2R (beta:=2) q <
        _root_.F2R (beta:=2) p / 2 := lt_of_lt_of_le hqUpper' (by linarith)
    have habs : |_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q| =
        _root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q := by
      rw [abs_of_nonneg]
      linarith
    rw [habs] at hSecond
    linarith
  have hqExp : q.Fexp = p.Fexp - 1 := by omega
  have hqLtP : _root_.F2R (beta:=2) q < _root_.F2R (beta:=2) p := by
    have hpowQ : (2 : ℝ) ^ q.Fexp = (2 : ℝ) ^ p.Fexp / 2 := by
      rw [hqExp]
      have hz : (2 : ℝ) ^ p.Fexp = (2 : ℝ) ^ (p.Fexp - 1) * 2 := by
        rw [show p.Fexp = p.Fexp - 1 + 1 by omega, zpow_add₀ (by norm_num)]
        norm_num
      linarith
    rw [hpowQ] at hqUpper
    push_cast at hqUpper
    have : _root_.F2R (beta:=2) q < (lo : ℝ) * (2 : ℝ) ^ p.Fexp := by
      convert hqUpper using 1 <;> ring
    exact lt_of_lt_of_le this hpLower
  have hpNum : p.Fnum = lo := by
    by_contra hne
    have hpNext : lo + 1 ≤ p.Fnum := by omega
    have hqLast : q.Fnum ≤ 2 * lo - 1 := by omega
    have hpLower' : ((lo + 1 : Int) : ℝ) * (2 : ℝ) ^ p.Fexp ≤
        _root_.F2R (beta:=2) p := by
      simp only [_root_.F2R, FloatSpec.Core.Defs.F2R]
      exact mul_le_mul_of_nonneg_right (by exact_mod_cast hpNext) (le_of_lt hpScalePos)
    have hqUpper' : _root_.F2R (beta:=2) q ≤
        ((2 * lo - 1 : Int) : ℝ) * (2 : ℝ) ^ q.Fexp := by
      simp only [_root_.F2R, FloatSpec.Core.Defs.F2R]
      exact mul_le_mul_of_nonneg_right (by exact_mod_cast hqLast) (le_of_lt hqScalePos)
    have hpowQ : (2 : ℝ) ^ q.Fexp = (2 : ℝ) ^ p.Fexp / 2 := by
      rw [hqExp]
      have hz : (2 : ℝ) ^ p.Fexp = (2 : ℝ) ^ (p.Fexp - 1) * 2 := by
        rw [show p.Fexp = p.Fexp - 1 + 1 by omega, zpow_add₀ (by norm_num)]
        norm_num
      linarith
    have hdiff : 3 * (2 : ℝ) ^ q.Fexp ≤
        _root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q := by
      rw [hpowQ] at hqUpper' ⊢
      push_cast at hpLower' hqUpper' ⊢
      nlinarith
    have habs : |_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q| =
        _root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q :=
      abs_of_pos (sub_pos.mpr hqLtP)
    have hpowOrder : (2 : ℝ) ^ q.Fexp ≤ (2 : ℝ) ^ p.Fexp :=
      zpow_right_mono₀ (by norm_num) (by omega)
    rw [habs, min_eq_right hpowOrder] at hSmall
    linarith
  have hpFloat : p = ⟨Zpower_nat 2 (precision - 1), q.Fexp + 1⟩ := by
    cases p with
    | mk pn pe =>
        simp_all [lo, Zpower_nat]
  have hpVal : _root_.F2R (beta:=2) p =
      (2 : ℝ) ^ ((precision : Int) + q.Fexp) := by
    rw [hpFloat]
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
      FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp,
      Zpower_nat, Int.cast_pow]
    norm_num
    rw [← zpow_natCast, ← zpow_add₀ (by norm_num)]
    congr 1
    omega
  exact ⟨hqExp, hpFloat, hpVal⟩

/-- Coq `discri9` (Discriminant4), with the branch package reconstructed from
the source section hypotheses. -/
theorem discri9 {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b c p q t dp dq s d : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = 2 ∧ radix = 2 ∧ 1 < precision ∧
        bo.vNum = Zpower_nat radix precision ∧
        Fbounded (beta:=beta) bo a ∧ Fbounded (beta:=beta) bo b ∧
        Fbounded (beta:=beta) bo c ∧ Fbounded (beta:=beta) bo p ∧
        Fbounded (beta:=beta) bo q ∧ Fbounded (beta:=beta) bo d ∧
        Fbounded (beta:=beta) bo t ∧
        (3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| <
            _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q →
          Fbounded (beta:=beta) bo s) ∧
        (3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| <
            _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q →
          Fbounded (beta:=beta) bo dp) ∧
        (3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| <
            _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q →
          Fbounded (beta:=beta) bo dq) ∧
        (3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| <
            _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q →
          Fcanonic (beta:=beta) radix bo s) ∧
        -bo.dExp ≤ d.Fexp - 1 ∧ -bo.dExp ≤ t.Fexp - 1 ∧
        (radix : ℝ) ^ (-bo.dExp + 2 * (precision : Int) - 1) ≤
          |_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b| ∧
        (radix : ℝ) ^ (-bo.dExp + 2 * (precision : Int) - 1) ≤
          |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c| ∧
        Fnormal (beta:=beta) radix bo p ∧ Fnormal (beta:=beta) radix bo q ∧
        Fnormal (beta:=beta) radix bo d ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b) p ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c) q ∧
        (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q ≤
            3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| →
          EvenClosest (beta:=beta) bo (radix : ℝ) precision
            (_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q) d) ∧
        (3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| <
            _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q →
          EvenClosest (beta:=beta) bo (radix : ℝ) precision
            (_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q) t) ∧
        (3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| <
            _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q →
          _root_.F2R (beta:=beta) dp =
            _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b -
              _root_.F2R (beta:=beta) p) ∧
        (3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| <
            _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q →
          _root_.F2R (beta:=beta) dq =
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c -
              _root_.F2R (beta:=beta) q) ∧
        (3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| <
            _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q →
          EvenClosest (beta:=beta) bo (radix : ℝ) precision
            (_root_.F2R (beta:=beta) dp - _root_.F2R (beta:=beta) dq) s) ∧
        (3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| <
            _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q →
          EvenClosest (beta:=beta) bo (radix : ℝ) precision
            (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) s) d)⌝⦄
    (pure (discri9_check (beta:=beta) bo radix precision a b b c p q t s d
      (_root_.F2R (beta:=beta) dp) (_root_.F2R (beta:=beta) dq)) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) d -
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| ≤
        2 * Fulp (beta:=beta) bo radix precision d⌝⦄ := by
  intro hpre
  rcases hpre with
    ⟨hbeta, hradix, hprecision, hvNum, haBound, hbBound, hcBound, hpBound,
      hqBound, hdBound, htBound, hsBoundIf, hdpBoundIf, hdqBoundIf, hsCanIf,
      hU0, hU1, hU2, hU3, hpNormal, hqNormal, hdNormal, hRoundp, hRoundq,
      hFirstRound, hRoundtIf, hdpEqIf, hdqEqIf, hRoundsIf, hRounddIf⟩
  subst beta
  subst radix
  simp only [wp, PostCond.noThrow, pure, discri9_check, Id.run, ULift.up_down]
  have hprecisionNe : precision ≠ 0 := by omega
  have hSquare : 0 ≤ _root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b :=
    mul_self_nonneg _
  have hpNonneg : 0 ≤ _root_.F2R (beta:=2) p :=
    P_positive bo (2 : ℝ)
      (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b) p hRoundp.1 hSquare
  by_cases hFirst : _root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q ≤
      3 * |_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q|
  · have h := discri1 (beta:=2) bo 2 precision a b b c p q d
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri1_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h
      ⟨rfl, rfl, hprecision, hvNum, haBound, hbBound, hbBound, hcBound,
        hpBound, hqBound, hdBound, hU0, hdNormal, hqNormal, hpNormal,
        hSquare, hRoundp, hRoundq, hFirst, hFirstRound hFirst⟩
  · have hSecond : 3 * |_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q| <
        _root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q := lt_of_not_ge hFirst
    have hsBound := hsBoundIf hSecond
    have hdpBound := hdpBoundIf hSecond
    have hdqBound := hdqBoundIf hSecond
    have hsCan := hsCanIf hSecond
    have hRoundt := hRoundtIf hSecond
    have hdpEq := hdpEqIf hSecond
    have hdqEq := hdqEqIf hSecond
    have hRounds := hRoundsIf hSecond
    have hRoundd := hRounddIf hSecond
    have hqPos : 0 < _root_.F2R (beta:=2) q :=
      Q_positive _ _ hpNonneg hSecond
    have htExact : _root_.F2R (beta:=2) t =
        _root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q := by
      have h := t_exact (beta:=2) bo (2 : ℝ) precision p q t
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, t_exact_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using
        h ⟨by decide, hpBound, hqBound, hpNonneg, hSecond, hRoundt⟩
    by_cases hLarge : 3 * min (Fulp (beta:=2) bo 2 precision p)
          (Fulp (beta:=2) bo 2 precision q) ≤
        |_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q|
    · have h := discri2 (beta:=2) bo 2 precision
        a b b c p q t dp dq s d
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri2_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using h
        ⟨rfl, rfl, hprecision, hvNum, haBound, hbBound, hbBound, hcBound,
          hpBound, hqBound, hdBound, htBound, hsBound, hdpBound, hdqBound,
          hsCan, hU1, hU2, hU3, hpNormal, hqNormal, hdNormal, hSquare,
          hRoundp, hRoundq, hSecond, hRoundt, hdpEq, hdqEq, hRounds,
          hRoundd, by
            intro hpq
            have hpUlpPos : 0 < Fulp (beta:=2) bo 2 precision p := by
              dsimp [Fulp]
              positivity
            have hqUlpPos : 0 < Fulp (beta:=2) bo 2 precision q := by
              dsimp [Fulp]
              positivity
            have hminPos : 0 < min (Fulp (beta:=2) bo 2 precision p)
                (Fulp (beta:=2) bo 2 precision q) := lt_min hpUlpPos hqUlpPos
            rw [hpq, sub_self, abs_zero] at hLarge
            linarith,
          hLarge⟩
    · have hSmallFulp : |_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q| <
          3 * min (Fulp (beta:=2) bo 2 precision p)
            (Fulp (beta:=2) bo 2 precision q) := lt_of_not_ge hLarge
      have hpUlp : Fulp (beta:=2) bo 2 precision p = (2 : ℝ) ^ p.Fexp := by
        have h := CanonicFulp (beta:=2) 2 bo precision p
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check, Id.run,
          ULift.up_down, Int.cast_ofNat] using
          h ⟨Or.inl hpNormal, rfl, by decide, hprecisionNe, hvNum⟩
      have hqUlp : Fulp (beta:=2) bo 2 precision q = (2 : ℝ) ^ q.Fexp := by
        have h := CanonicFulp (beta:=2) 2 bo precision q
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check, Id.run,
          ULift.up_down, Int.cast_ofNat] using
          h ⟨Or.inl hqNormal, rfl, by decide, hprecisionNe, hvNum⟩
      have hSmall : |_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q| <
          3 * min ((2 : ℝ) ^ p.Fexp) ((2 : ℝ) ^ q.Fexp) := by
        simpa [hpUlp, hqUlp] using hSmallFulp
      rcases lt_trichotomy q.Fexp p.Fexp with hqExpLt | hExpEq | hpExpLt
      · rcases upper_normal_boundary_of_close bo precision p q hprecision hvNum
            hpNormal hqNormal hpNonneg (le_of_lt hqPos) hSecond hSmall hqExpLt with
          ⟨hqExp, hpForm, hpVal⟩
        have hctx : discri3_source_context (beta:=2) bo 2 precision
            a b b c p q t dp dq s d q.Fexp := by
          exact ⟨rfl, rfl, hprecision, hvNum, haBound, hbBound, hbBound,
            hcBound, hpBound, hqBound, hdBound, htBound, hsBound, hdpBound,
            hdqBound, hsCan, hU0, hU2, hU3, hpNormal, hqNormal, hdNormal,
            hSquare, hRoundp, hRoundq, hpNonneg, le_of_lt hqPos, hSecond,
            hRoundt, hdpEq, hdqEq, hRounds, hRoundd,
            by
              intro hpq
              have hpqFloat : p = q := by
                have h := FcanonicUnique (beta:=2) 2 bo p q (by decide) rfl
                simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicUnique_check,
                  Id.run, ULift.up_down, Fcanonic', Int.cast_ofNat] using
                  h ⟨Or.inl hpNormal, Or.inl hqNormal, hpq⟩
              subst q
              exact (lt_irrefl p.Fexp hqExpLt),
            hpForm, hpVal, rfl⟩
        have h := discri8 (beta:=2) bo 2 precision
          a b b c p q t dp dq s d q.Fexp
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri8_check, Id.run,
          ULift.up_down, Int.cast_ofNat] using h hctx
      · by_cases hpNeq : _root_.F2R (beta:=2) p ≠ _root_.F2R (beta:=2) q
        · have h := discri4 (beta:=2) bo 2 precision
            a b b c p q t dp dq s d
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri4_check, Id.run,
            ULift.up_down, Int.cast_ofNat] using h
            ⟨rfl, rfl, hprecision, hvNum, haBound, hbBound, hbBound, hcBound,
              hpBound, hqBound, hdBound, htBound, hsBound, hdpBound, hdqBound,
              hsCan, hU1, hU2, hU3, hpNormal, hqNormal, hdNormal, hSquare,
              hRoundp, hRoundq, hSecond, hRoundt, hdpEq, hdqEq, hRounds,
              hRoundd, hpNeq, hExpEq.symm⟩
        · rcases errorBoundedMultClosest_Can bo precision b b p rfl hprecision
              hvNum hbBound hbBound hRoundp.1 hU2 (Or.inl hpNormal) with
            ⟨sp, hspBound, hspVal, hspExp, hspNum⟩
          rcases errorBoundedMultClosest_Can bo precision a c q rfl hprecision
              hvNum haBound hcBound hRoundq.1 hU3 (Or.inl hqNormal) with
            ⟨sq, hsqBound, hsqVal, hsqExp, hsqNum⟩
          have hsameExp : sp.Fexp = sq.Fexp := by omega
          rcases bounded_residual_difference_same_exp bo precision sp sq
              hprecision hvNum hspBound hsqBound hsameExp hspNum hsqNum with
            ⟨f, hfBound, hfVal⟩
          have hResidual : _root_.F2R (beta:=2) f =
              _root_.F2R (beta:=2) dp - _root_.F2R (beta:=2) dq := by
            rw [hfVal, hspVal, hsqVal, hdpEq, hdqEq]
          have h := discri3_from_residual_witness_payload (beta:=2) bo 2
            precision a b b c p q t s d (_root_.F2R (beta:=2) dp)
              (_root_.F2R (beta:=2) dq)
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri3_check, Id.run,
            ULift.up_down, Int.cast_ofNat] using h
            ⟨hRoundd, hRounds, htExact, hdpEq, hdqEq,
              ⟨f, hfBound, hResidual⟩, rfl, by decide, hprecisionNe, hvNum⟩
      · rcases upper_normal_boundary_of_close bo precision q p hprecision hvNum
            hqNormal hpNormal (le_of_lt hqPos) hpNonneg
            (by simpa [abs_sub_comm, add_comm] using hSecond)
            (by simpa [abs_sub_comm, min_comm] using hSmall) hpExpLt with
          ⟨hpExp, hqForm, hqVal⟩
        change |_root_.F2R (beta:=2) d -
              (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b -
                _root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c)| ≤
            2 * Fulp (beta:=2) bo 2 precision d
        have hsymm := EvenClosestSymmetric (beta:=2) bo (2 : ℝ) precision
        have hSymmetric : SymmetricP (EvenClosest (beta:=2) bo (2 : ℝ) precision) := by
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestSymmetric_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using hsymm True.intro
        have htOppBound : Fbounded (beta:=2) bo (Fopp (beta:=2) t) := by
          have h := oppBounded (beta:=2) bo t
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, oppBounded_check, Id.run,
            ULift.up_down, Int.cast_ofNat] using h htBound
        have hsOppBound : Fbounded (beta:=2) bo (Fopp (beta:=2) s) := by
          have h := oppBounded (beta:=2) bo s
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, oppBounded_check, Id.run,
            ULift.up_down, Int.cast_ofNat] using h hsBound
        have hdOppBound : Fbounded (beta:=2) bo (Fopp (beta:=2) d) := by
          have h := oppBounded (beta:=2) bo d
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, oppBounded_check, Id.run,
            ULift.up_down, Int.cast_ofNat] using h hdBound
        have hsOppCan : Fcanonic (beta:=2) 2 bo (Fopp (beta:=2) s) := by
          have h := FcanonicFopp (beta:=2) 2 bo s
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFopp_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using h hsCan
        have hdOppNormal : Fnormal (beta:=2) 2 bo (Fopp (beta:=2) d) := by
          have h := FnormalFop (beta:=2) bo 2 d
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalFop_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using h hdNormal
        have htOppVal : _root_.F2R (beta:=2) (Fopp t) =
            -_root_.F2R (beta:=2) t := by
          have h := Fopp_correct (beta:=2) t
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fopp_correct_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
        have hsOppVal : _root_.F2R (beta:=2) (Fopp s) =
            -_root_.F2R (beta:=2) s := by
          have h := Fopp_correct (beta:=2) s
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fopp_correct_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
        have hdOppVal : _root_.F2R (beta:=2) (Fopp d) =
            -_root_.F2R (beta:=2) d := by
          have h := Fopp_correct (beta:=2) d
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fopp_correct_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
        have hProductNonneg : 0 ≤
            _root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c := by
          by_contra hneg
          have hRMP : RoundedModeP (beta:=2) bo
              (EvenClosest (beta:=2) bo (2 : ℝ) precision) := by
            have h := EvenClosestRoundedModeP (beta:=2) bo 2 precision
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
              EvenClosestRoundedModeP_check, Id.run, ULift.up_down, Int.cast_ofNat] using
              h ⟨rfl, by decide, hprecision, hvNum⟩
          have hqNonpos : _root_.F2R (beta:=2) q ≤ 0 := by
            have h := RleRoundedLessR0 (beta:=2) bo
              (EvenClosest (beta:=2) bo (2 : ℝ) precision) q
              (_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c)
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RleRoundedLessR0_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using h
              ⟨hRMP, hRoundq, le_of_not_ge hneg⟩
          linarith
        have hctx : discri3_source_context (beta:=2) bo 2 precision
            b a c b q p (Fopp t) dq dp (Fopp s) (Fopp d) p.Fexp := by
          exact ⟨rfl, rfl, hprecision, hvNum, hbBound, haBound, hcBound,
            hbBound, hqBound, hpBound, hdOppBound, htOppBound, hsOppBound,
            hdqBound, hdpBound, hsOppCan, by simpa [Fopp,
              FloatSpec.Calc.Operations.Fopp] using hU0, hU3, hU2,
            hqNormal, hpNormal, hdOppNormal, hProductNonneg,
            hRoundq, by simpa [mul_comm] using hRoundp, le_of_lt hqPos,
            hpNonneg, by simpa [abs_sub_comm, add_comm] using hSecond,
            by simpa [htOppVal] using hSymmetric _ _ hRoundt,
            hdqEq, hdpEq,
            by simpa [hsOppVal, sub_eq_add_neg, add_comm] using
              hSymmetric _ _ hRounds,
            by simpa [htOppVal, hsOppVal, hdOppVal, sub_eq_add_neg,
              add_comm, add_left_comm] using
              hSymmetric _ _ hRoundd,
            by
              intro hqp
              have hqpFloat : q = p := by
                have h := FcanonicUnique (beta:=2) 2 bo q p (by decide) rfl
                simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicUnique_check,
                  Id.run, ULift.up_down, Fcanonic', Int.cast_ofNat] using
                  h ⟨Or.inl hqNormal, Or.inl hpNormal, hqp⟩
              subst p
              exact (lt_irrefl q.Fexp hpExpLt),
            hqForm, hqVal, rfl⟩
        have h := discri8 (beta:=2) bo 2 precision
          b a c b q p (Fopp t) dq dp (Fopp s) (Fopp d) p.Fexp
        have hbound : |_root_.F2R (beta:=2) (Fopp d) -
              (_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c -
                _root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b)| ≤
            2 * Fulp (beta:=2) bo 2 precision (Fopp d) := by
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri8_check, Id.run,
            ULift.up_down, Int.cast_ofNat] using h hctx
        have hdOppUlp : Fulp (beta:=2) bo 2 precision (Fopp d) =
            Fulp (beta:=2) bo 2 precision d := by
          have hnorm := Fnormalize_Fopp (beta:=2) 2 bo precision d
          have hnorm' :
              Fnormalize (beta:=2) 2 bo precision (Fopp d) =
                Fopp (Fnormalize (beta:=2) 2 bo precision d) := by
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fnormalize_Fopp_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using hnorm True.intro
          calc
            Fulp (beta:=2) bo 2 precision (Fopp d) =
                (2 : ℝ) ^
                  (Fnormalize (beta:=2) 2 bo precision (Fopp d)).Fexp := rfl
            _ = (2 : ℝ) ^
                  (Fopp (Fnormalize (beta:=2) 2 bo precision d)).Fexp := by
                rw [hnorm']
            _ = Fulp (beta:=2) bo 2 precision d := by
                simp [Fulp, Fopp, FloatSpec.Calc.Operations.Fopp]
        rw [hdOppVal, hdOppUlp] at hbound
        have habs : |_root_.F2R (beta:=2) d -
              (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b -
                _root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c)| =
            |-_root_.F2R (beta:=2) d -
              (_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c -
                _root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b)| := by
          rw [← abs_neg]
          congr 1
          ring
        rw [habs]
        exact hbound

noncomputable def discri10_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q t s d : FloatSpec.Core.Defs.FlocqFloat beta)
    (dp dq : ℝ) : Unit :=
  ()

/-- Coq discriminant theorem `discri10`, factored at the `discri9` payload.

Upstream derives the `discri9` branch package from `q <= p` and the surrounding
normality/case hypotheses.  This local theorem keeps that package explicit and
composes it with the checked `discri9` dispatcher. -/
theorem discri10_from_discri9_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q t s d : FloatSpec.Core.Defs.FlocqFloat beta)
    (dp dq : ℝ) :
    ⦃⌜_root_.F2R (beta:=beta) q ≤ _root_.F2R (beta:=beta) p ∧
        discri9_precondition (beta:=beta) bo radix precision
          a b b' c p q t s d dp dq⌝⦄
    (pure (discri10_check (beta:=beta) bo radix precision a b b' c p q t s d dp dq) :
      Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) d -
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| ≤
        2 * Fulp (beta:=beta) bo radix precision d⌝⦄ := by
  intro hpre
  rcases hpre with ⟨_hq_le_p, hdiscri9⟩
  simp only [wp, PostCond.noThrow, pure, discri10_check, Id.run, ULift.up_down]
  have h := discri9_from_branch_payload (beta:=beta) bo radix precision a b b' c p q t s d dp dq
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri9_check, Id.run, ULift.up_down,
    discri9_precondition, Int.cast_ofNat] using h hdiscri9

/- Coq `discri10` (Discriminant1A), with the complete section contract.
The proof reconstructs the source's discrete exponent argument internally;
the legacy `discri9` branch payload is not exposed by this public theorem. -/
set_option maxHeartbeats 1200000 in
theorem discri10 {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q t d u v dp dq :
      FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = 2 ∧ radix = 2 ∧ 1 < precision ∧ 3 ≤ precision ∧
        bo.vNum = Zpower_nat radix precision ∧
        Fbounded (beta:=beta) bo a ∧ Fbounded (beta:=beta) bo b ∧
        Fbounded (beta:=beta) bo b' ∧ Fbounded (beta:=beta) bo c ∧
        Fbounded (beta:=beta) bo p ∧ Fbounded (beta:=beta) bo q ∧
        Fbounded (beta:=beta) bo d ∧ Fbounded (beta:=beta) bo u ∧
        Fbounded (beta:=beta) bo v ∧
        Fcanonic (beta:=beta) radix bo d ∧
        Fnormal (beta:=beta) radix bo q ∧
        Fnormal (beta:=beta) radix bo p ∧
        Fnormal (beta:=beta) radix bo v ∧
        Fnormal (beta:=beta) radix bo u ∧
        -bo.dExp ≤ p.Fexp - 2 ∧
        0 ≤ _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b') p ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c) q ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q) t ∧
        d = t ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (3 * |_root_.F2R (beta:=beta) t|) u ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) v ∧
        _root_.F2R (beta:=beta) dp =
          _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
            _root_.F2R (beta:=beta) p ∧
        _root_.F2R (beta:=beta) dq =
          _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c -
            _root_.F2R (beta:=beta) q ∧
        3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| <
          _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q ∧
        _root_.F2R (beta:=beta) v ≤ _root_.F2R (beta:=beta) u ∧
        _root_.F2R (beta:=beta) q ≤ _root_.F2R (beta:=beta) p⌝⦄
    (pure (discri10_check (beta:=beta) bo radix precision
      a b b' c p q t t d (_root_.F2R (beta:=beta) dp)
        (_root_.F2R (beta:=beta) dq)) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) d -
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| ≤
        2 * Fulp (beta:=beta) bo radix precision d⌝⦄ := by
  intro hpre
  rcases hpre with
    ⟨hbeta, hradix, hprecision, hprecision3, hvNum, _haBound, _hbBound,
      _hb'Bound, _hcBound, hpBound, hqBound, _hdBound, _huBound, _hvBound,
      hdCan, hqNormal, hpNormal, hvNormal, huNormal, hU0, hSquare, hRoundp,
      hRoundq, hRoundt, hdDef, hRoundu, hRoundv, hdpEq, hdqEq, hCase1,
      hCase2, hqLeP⟩
  subst beta
  subst radix
  simp only [wp, PostCond.noThrow, pure, discri10_check, Id.run, ULift.up_down]
  have hprecisionNe : precision ≠ 0 := by omega
  have hpNonneg : 0 ≤ _root_.F2R (beta:=2) p :=
    P_positive bo (2 : ℝ)
      (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b') p
      hRoundp.1 hSquare
  have hqPos : 0 < _root_.F2R (beta:=2) q :=
    Q_positive _ _ hpNonneg hCase1
  have hqNonneg : 0 ≤ _root_.F2R (beta:=2) q := le_of_lt hqPos
  have hpLeTwoQ : _root_.F2R (beta:=2) p ≤
      2 * _root_.F2R (beta:=2) q :=
    P_le_two_Q _ _ hpNonneg hCase1
  have hDNonneg : 0 ≤
      _root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q := by linarith
  have htExact : _root_.F2R (beta:=2) t =
      _root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q := by
    have h := dexact (beta:=2) bo 2 precision p q t t
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, dexact_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hpBound, hqBound, hpNonneg, hCase1, hRoundt, rfl, rfl, by decide⟩
  have hdExact : _root_.F2R (beta:=2) d =
      _root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q := by
    simpa [hdDef] using htExact
  have huv : _root_.F2R (beta:=2) u = _root_.F2R (beta:=2) v := by
    have h := IneqEq (beta:=2) bo 2 precision p q t u v
    have h' : _root_.F2R (beta:=2) v = _root_.F2R (beta:=2) u := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, IneqEq_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨htExact, hRoundu, hRoundv, hCase1, hCase2⟩
    exact h'.symm
  have hadj := normal_binary_exponents_adjacent bo p q hpNormal hqNormal
    hpNonneg hqNonneg hqLeP hpLeTwoQ
  have hpUlp : Fulp (beta:=2) bo 2 precision p = (2 : ℝ) ^ p.Fexp := by
    have h := CanonicFulp (beta:=2) 2 bo precision p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨Or.inl hpNormal, rfl, by decide, hprecisionNe, hvNum⟩
  have hqUlp : Fulp (beta:=2) bo 2 precision q = (2 : ℝ) ^ q.Fexp := by
    have h := CanonicFulp (beta:=2) 2 bo precision q
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨Or.inl hqNormal, rfl, by decide, hprecisionNe, hvNum⟩
  have hdUlp : Fulp (beta:=2) bo 2 precision d = (2 : ℝ) ^ d.Fexp := by
    have h := CanonicFulp (beta:=2) 2 bo precision d
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hdCan, rfl, by decide, hprecisionNe, hvNum⟩
  have hErrorEq :
      |_root_.F2R (beta:=2) d -
          (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b' -
            _root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c)| =
        |_root_.F2R (beta:=2) dp - _root_.F2R (beta:=2) dq| := by
    rw [hdExact, hdpEq, hdqEq]
    rw [← abs_neg]
    congr 1
    ring
  have hpErr : 2 * |_root_.F2R (beta:=2) dp| ≤
      Fulp (beta:=2) bo 2 precision p := by
    have h := ClosestUlp (beta:=2) bo 2 precision
      (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b') p
    have h' := h ⟨hRoundp.1, rfl, by decide, hprecisionNe, hvNum⟩
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, hdpEq, Int.cast_ofNat] using h'
  have hqErr : 2 * |_root_.F2R (beta:=2) dq| ≤
      Fulp (beta:=2) bo 2 precision q := by
    have h := ClosestUlp (beta:=2) bo 2 precision
      (_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c) q
    have h' := h ⟨hRoundq.1, rfl, by decide, hprecisionNe, hvNum⟩
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, hdqEq, Int.cast_ofNat] using h'
  have hErrorSum :
      |_root_.F2R (beta:=2) dp - _root_.F2R (beta:=2) dq| ≤
        (Fulp (beta:=2) bo 2 precision p +
          Fulp (beta:=2) bo 2 precision q) / 2 := by
    have htri : |_root_.F2R (beta:=2) dp - _root_.F2R (beta:=2) dq| ≤
        |_root_.F2R (beta:=2) dp| + |_root_.F2R (beta:=2) dq| := by
      simpa [sub_eq_add_neg] using
        (abs_add_le (_root_.F2R (beta:=2) dp)
          (-_root_.F2R (beta:=2) dq))
    nlinarith
  have hdExpLeQ : d.Fexp ≤ q.Fexp := by
    have hdNonneg : 0 ≤ _root_.F2R (beta:=2) d := by
      rw [hdExact]
      exact hDNonneg
    have hdLeQ : _root_.F2R (beta:=2) d ≤ _root_.F2R (beta:=2) q := by
      rw [hdExact]
      nlinarith
    have h := Fcanonic_Rle_Zle (beta:=2) 2 bo d q (by decide) rfl
    have habsLe : |_root_.F2R (beta:=2) d| ≤
        |_root_.F2R (beta:=2) q| := by
      rw [abs_of_nonneg hdNonneg, abs_of_nonneg hqNonneg]
      exact hdLeQ
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fcanonic_Rle_Zle_check,
      Id.run, ULift.up_down, Fcanonic', Int.cast_ofNat] using
      h ⟨hdCan, Or.inl hqNormal, habsLe⟩
  rcases lt_or_eq_of_le hdExpLeQ with hdExpLt | hdExpEq
  · rcases lt_or_eq_of_le hadj.1 with hqExpLt | hqExpEq
    · exact False.elim (discri10_no_lower_q_exp bo precision p q t d u v
        hprecision hprecision3 hvNum hpNormal hqNormal hdCan huNormal
        hvNormal hpNonneg hqPos hqLeP hpLeTwoQ hDNonneg hU0 htExact
        hdExact hRoundu hRoundv hCase1 huv hqExpLt hdExpLt)
    · have hdExpGe : p.Fexp - 1 ≤ d.Fexp :=
        discri10_same_exp_d_exp_ge bo precision p q t d u v hprecision
          hprecision3 hvNum hpNormal hqNormal hdCan huNormal hvNormal
          hpNonneg hqNonneg hDNonneg hU0 hqExpEq.symm htExact hdExact
          hRoundu hRoundv huv
      have hupLe : Fulp (beta:=2) bo 2 precision p ≤
          2 * Fulp (beta:=2) bo 2 precision d := by
        rw [hpUlp, hdUlp]
        have hpow := zpow_right_mono₀ (by norm_num : (1 : ℝ) ≤ 2)
          (show p.Fexp ≤ d.Fexp + 1 by omega)
        have hdPow : (2 : ℝ) ^ (d.Fexp + 1) =
            2 * (2 : ℝ) ^ d.Fexp := by
          rw [show d.Fexp + 1 = 1 + d.Fexp by ring,
            zpow_add₀ (by norm_num), zpow_one]
        change (2 : ℝ) ^ p.Fexp ≤ (2 : ℝ) ^ (d.Fexp + 1) at hpow
        rw [hdPow] at hpow
        exact hpow
      have hpqUlp : Fulp (beta:=2) bo 2 precision p =
          Fulp (beta:=2) bo 2 precision q := by
        rw [hpUlp, hqUlp, hqExpEq]
      calc
        |_root_.F2R (beta:=2) d -
            (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b' -
              _root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c)| =
            |_root_.F2R (beta:=2) dp - _root_.F2R (beta:=2) dq| := hErrorEq
        _ ≤ (Fulp (beta:=2) bo 2 precision p +
              Fulp (beta:=2) bo 2 precision q) / 2 := hErrorSum
        _ = Fulp (beta:=2) bo 2 precision p := by rw [← hpqUlp]; ring
        _ ≤ 2 * Fulp (beta:=2) bo 2 precision d := hupLe
  · have hqLeTwoP : Fulp (beta:=2) bo 2 precision q ≤
        2 * Fulp (beta:=2) bo 2 precision p := by
      rw [hqUlp, hpUlp]
      have hpow := zpow_right_mono₀ (by norm_num : (1 : ℝ) ≤ 2) hadj.1
      have hpPowPos : 0 < (2 : ℝ) ^ p.Fexp := by positivity
      nlinarith
    have hpLeTwoQ : Fulp (beta:=2) bo 2 precision p ≤
        2 * Fulp (beta:=2) bo 2 precision q := by
      rw [hpUlp, hqUlp]
      have hpow := zpow_right_mono₀ (by norm_num : (1 : ℝ) ≤ 2) hadj.2
      have hqpow : (2 : ℝ) ^ (q.Fexp + 1) =
          2 * (2 : ℝ) ^ q.Fexp := by
        rw [show q.Fexp + 1 = 1 + q.Fexp by ring,
          zpow_add₀ (by norm_num), zpow_one]
      change (2 : ℝ) ^ p.Fexp ≤ (2 : ℝ) ^ (q.Fexp + 1) at hpow
      rw [hqpow] at hpow
      exact hpow
    have hdpdq : |_root_.F2R (beta:=2) dp - _root_.F2R (beta:=2) dq| ≤
        (3 / 2 : ℝ) * min (Fulp (beta:=2) bo 2 precision p)
          (Fulp (beta:=2) bo 2 precision q) := by
      have h := dp_dq_le_from_ulp_comparison_payload
        (beta:=2) bo 2 precision p q
        (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b')
        (_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c)
        (_root_.F2R (beta:=2) dp) (_root_.F2R (beta:=2) dq)
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, dp_dq_le_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hRoundp.1, hRoundq.1, hdpEq, hdqEq, rfl, by decide,
          hprecisionNe, hvNum, hqLeTwoP, hpLeTwoQ⟩
    have hminLe : min (Fulp (beta:=2) bo 2 precision p)
        (Fulp (beta:=2) bo 2 precision q) ≤
          Fulp (beta:=2) bo 2 precision q := min_le_right _ _
    have hqEqD : Fulp (beta:=2) bo 2 precision q =
        Fulp (beta:=2) bo 2 precision d := by
      rw [hqUlp, hdUlp, hdExpEq]
    have hqUlpPos : 0 < Fulp (beta:=2) bo 2 precision q := by
      dsimp [Fulp]
      positivity
    calc
      |_root_.F2R (beta:=2) d -
          (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b' -
            _root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c)| =
          |_root_.F2R (beta:=2) dp - _root_.F2R (beta:=2) dq| := hErrorEq
      _ ≤ (3 / 2 : ℝ) * min (Fulp (beta:=2) bo 2 precision p)
            (Fulp (beta:=2) bo 2 precision q) := hdpdq
      _ ≤ (3 / 2 : ℝ) * Fulp (beta:=2) bo 2 precision q :=
        mul_le_mul_of_nonneg_left hminLe (by norm_num)
      _ ≤ 2 * Fulp (beta:=2) bo 2 precision q := by nlinarith
      _ = 2 * Fulp (beta:=2) bo 2 precision d := by rw [hqEqD]

noncomputable def Fulp_Fopp_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Negating a float preserves its ulp. -/
theorem Fulp_Fopp {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜True⌝⦄
    (pure (Fulp_Fopp_check (beta:=beta) bo radix precision p) : Id Unit)
    ⦃⇓_ => ⌜Fulp (beta:=beta) bo radix precision (Fopp (beta:=beta) p) =
        Fulp (beta:=beta) bo radix precision p⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, Fulp_Fopp_check, Id.run, ULift.up_down]
  have hnorm := Fnormalize_Fopp (beta:=beta) radix bo precision p
  have hnorm' :
      Fnormalize (beta:=beta) radix bo precision (Fopp (beta:=beta) p) =
        Fopp (beta:=beta) (Fnormalize (beta:=beta) radix bo precision p) := by
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fnormalize_Fopp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using hnorm True.intro
  calc
    Fulp (beta:=beta) bo radix precision (Fopp (beta:=beta) p)
        = (radix : ℝ) ^
            (Fnormalize (beta:=beta) radix bo precision
              (Fopp (beta:=beta) p)).Fexp := rfl
    _ = (radix : ℝ) ^
            (Fopp (beta:=beta)
              (Fnormalize (beta:=beta) radix bo precision p)).Fexp := by
          rw [hnorm']
    _ = Fulp (beta:=beta) bo radix precision p := by
          simp [Fulp, Fopp, FloatSpec.Calc.Operations.Fopp]

/-- Scaling a real magnitude by an integral radix power scales the normalized
ulp by at most the same factor.  This is the local comparison used in Coq's
`discri12` proof. -/
private theorem fulp_le_zpow_mul_of_abs_le
    (bo : Fbound_skel) (precision : Nat)
    (x y : FloatSpec.Core.Defs.FlocqFloat 2) (i : Int)
    (hprecision : precision ≠ 0)
    (hvNum : bo.vNum = Zpower_nat 2 precision)
    (hi : 0 ≤ i)
    (hxBound : Fbounded (beta:=2) bo x)
    (hyNormal : Fnormal (beta:=2) 2 bo y)
    (hxy : |_root_.F2R (beta:=2) x| ≤
      (2 : ℝ) ^ i * |_root_.F2R (beta:=2) y|) :
    Fulp (beta:=2) bo 2 precision x ≤
      (2 : ℝ) ^ i * Fulp (beta:=2) bo 2 precision y := by
  let xn := Fnormalize (beta:=2) 2 bo precision x
  let ys : FloatSpec.Core.Defs.FlocqFloat 2 :=
    { Fnum := y.Fnum, Fexp := i + y.Fexp }
  have hradix : (1 : Int) < 2 := by decide
  have hxnCan : Fcanonic (beta:=2) 2 bo xn := by
    have h := FnormalizeCanonic (beta:=2) 2 bo precision x
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
      Id.run, ULift.up_down, Fbounded', xn, Int.cast_ofNat] using
      h ⟨hxBound, hxBound, hprecision, hradix, hvNum⟩
  have hxnVal : _root_.F2R (beta:=2) xn = _root_.F2R (beta:=2) x := by
    have h := FnormalizeCorrect (beta:=2) 2 bo precision x
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
      Id.run, ULift.up_down, xn, Int.cast_ofNat] using h ⟨rfl, hradix⟩
  have hysNormal : Fnormal (beta:=2) 2 bo ys := by
    rcases hyNormal with ⟨⟨hyNum, hyExp⟩, hyMant⟩
    exact ⟨⟨by simpa [ys] using hyNum, by dsimp [ys]; omega⟩,
      by simpa [ys] using hyMant⟩
  have hysVal : |_root_.F2R (beta:=2) ys| =
      (2 : ℝ) ^ i * |_root_.F2R (beta:=2) y| := by
    have htwo : (2 : ℝ) ≠ 0 := by norm_num
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, ys,
      FloatSpec.Core.Defs.FlocqFloat.Fnum,
      FloatSpec.Core.Defs.FlocqFloat.Fexp, abs_mul, Int.cast_abs]
    change |(y.Fnum : ℝ)| * |(2 : ℝ) ^ (i + y.Fexp)| =
      (2 : ℝ) ^ i * (|(y.Fnum : ℝ)| * |(2 : ℝ) ^ y.Fexp|)
    rw [abs_of_pos (zpow_pos (by norm_num : (0 : ℝ) < 2) _),
      abs_of_pos (zpow_pos (by norm_num : (0 : ℝ) < 2) _),
      zpow_add₀ htwo]
    ring
  have hExp : xn.Fexp ≤ ys.Fexp := by
    have h := Fcanonic_Rle_Zle (beta:=2) 2 bo xn ys hradix rfl
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fcanonic_Rle_Zle_check,
      Id.run, ULift.up_down, Fcanonic', Int.cast_ofNat] using
      h ⟨hxnCan, Or.inl hysNormal, by simpa [hxnVal, hysVal] using hxy⟩
  have hyUlp : Fulp (beta:=2) bo 2 precision y = (2 : ℝ) ^ y.Fexp := by
    have h := CanonicFulp (beta:=2) 2 bo precision y
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨Or.inl hyNormal, rfl, hradix, hprecision, hvNum⟩
  have hyNorm : Fnormalize (beta:=2) 2 bo precision y = y := by
    have h := FcanonicFnormalizeEq (beta:=2) 2 bo precision y
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨Or.inl hyNormal, rfl, hradix, hprecision, hvNum⟩
  have hpow : (2 : ℝ) ^ xn.Fexp ≤ (2 : ℝ) ^ ys.Fexp :=
    zpow_right_mono₀ (by norm_num) hExp
  simpa [Fulp, xn, ys, hyNorm, hyUlp,
    zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0),
    mul_comm] using hpow

noncomputable def discri11_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q t s d : FloatSpec.Core.Defs.FlocqFloat beta)
    (dp dq : ℝ) : Unit :=
  ()

/-- Coq discriminant theorem `discri11`, factored at the two `discri10`
payloads.

Upstream splits on `q <= p`.  The opposite branch applies `discri10` to the
swapped products and the negated difference; the branch payload for that
swapped call remains explicit here, matching the surrounding discriminant
factoring. -/
theorem discri11_from_ordered_payloads {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q t s d : FloatSpec.Core.Defs.FlocqFloat beta)
    (dp dq : ℝ) :
    ⦃⌜(_root_.F2R (beta:=beta) q ≤ _root_.F2R (beta:=beta) p ∧
          discri9_precondition (beta:=beta) bo radix precision
            a b b' c p q t s d dp dq) ∨
        (_root_.F2R (beta:=beta) p < _root_.F2R (beta:=beta) q ∧
          discri9_precondition (beta:=beta) bo radix precision
            b a c b' q p (Fopp (beta:=beta) t) s
            (Fopp (beta:=beta) d) dq dp)⌝⦄
    (pure (discri11_check (beta:=beta) bo radix precision a b b' c p q t s d dp dq) :
      Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) d -
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| ≤
        2 * Fulp (beta:=beta) bo radix precision d⌝⦄ := by
  intro hpre
  simp only [wp, PostCond.noThrow, pure, discri11_check, Id.run, ULift.up_down]
  rcases hpre with hdirect | hswapped
  · rcases hdirect with ⟨hq_le_p, hpayload⟩
    have h := discri10_from_discri9_payload (beta:=beta) bo radix precision a b b' c p q t s d dp dq
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri10_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨hq_le_p, hpayload⟩
  · rcases hswapped with ⟨hp_lt_q, hpayload⟩
    have hp_le_q : _root_.F2R (beta:=beta) p ≤ _root_.F2R (beta:=beta) q :=
      le_of_lt hp_lt_q
    have hswap := discri10_from_discri9_payload (beta:=beta) bo radix precision
      b a c b' q p (Fopp (beta:=beta) t) s (Fopp (beta:=beta) d) dq dp
    have hswap' :
        |_root_.F2R (beta:=beta) (Fopp (beta:=beta) d) -
            (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c -
              _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b')| ≤
          2 * Fulp (beta:=beta) bo radix precision (Fopp (beta:=beta) d) := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri10_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using hswap ⟨hp_le_q, hpayload⟩
    have hdOpp :
        _root_.F2R (beta:=beta) (Fopp (beta:=beta) d) =
          -_root_.F2R (beta:=beta) d := by
      have h := FloatSpec.Calc.Operations.F2R_opp (beta:=beta) d
      simpa [Fopp, _root_.F2R] using h trivial
    have htarget_abs :
        |_root_.F2R (beta:=beta) d -
            (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
              _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| =
          |_root_.F2R (beta:=beta) (Fopp (beta:=beta) d) -
            (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c -
              _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b')| := by
      rw [hdOpp]
      rw [← abs_neg (_root_.F2R (beta:=beta) d -
        (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
          _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c))]
      congr 1
      ring
    have hFulpOpp :
        Fulp (beta:=beta) bo radix precision (Fopp (beta:=beta) d) =
          Fulp (beta:=beta) bo radix precision d := by
      have h := Fulp_Fopp (beta:=beta) bo radix precision d
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fulp_Fopp_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using h True.intro
    simpa [htarget_abs, hFulpOpp] using hswap'

/- Coq `discri11` (Discriminant2A), restoring the complete source section
contract.  The reverse-order branch applies source-shaped `discri10` to the
swapped products and the negated exact difference. -/
set_option maxHeartbeats 1200000 in
theorem discri11 {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q t d u v dp dq :
      FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = 2 ∧ radix = 2 ∧ 1 < precision ∧ 3 ≤ precision ∧
        bo.vNum = Zpower_nat radix precision ∧
        Fbounded (beta:=beta) bo a ∧ Fbounded (beta:=beta) bo b ∧
        Fbounded (beta:=beta) bo b' ∧ Fbounded (beta:=beta) bo c ∧
        Fbounded (beta:=beta) bo p ∧ Fbounded (beta:=beta) bo q ∧
        Fbounded (beta:=beta) bo d ∧ Fbounded (beta:=beta) bo u ∧
        Fbounded (beta:=beta) bo v ∧
        Fcanonic (beta:=beta) radix bo d ∧
        Fnormal (beta:=beta) radix bo q ∧
        Fnormal (beta:=beta) radix bo p ∧
        Fnormal (beta:=beta) radix bo v ∧
        Fnormal (beta:=beta) radix bo u ∧
        -bo.dExp ≤ p.Fexp - 2 ∧ -bo.dExp ≤ q.Fexp - 2 ∧
        0 ≤ _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b') p ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c) q ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q) t ∧
        d = t ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (3 * |_root_.F2R (beta:=beta) t|) u ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) v ∧
        _root_.F2R (beta:=beta) dp =
          _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
            _root_.F2R (beta:=beta) p ∧
        _root_.F2R (beta:=beta) dq =
          _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c -
            _root_.F2R (beta:=beta) q ∧
        3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| <
          _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q ∧
        _root_.F2R (beta:=beta) v ≤ _root_.F2R (beta:=beta) u⌝⦄
    (pure (discri11_check (beta:=beta) bo radix precision
      a b b' c p q t t d (_root_.F2R (beta:=beta) dp)
        (_root_.F2R (beta:=beta) dq)) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) d -
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| ≤
        2 * Fulp (beta:=beta) bo radix precision d⌝⦄ := by
  intro hpre
  rcases hpre with
    ⟨hbeta, hradix, hprecision, hprecision3, hvNum, haBound, hbBound,
      hb'Bound, hcBound, hpBound, hqBound, hdBound, huBound, hvBound,
      hdCan, hqNormal, hpNormal, hvNormal, huNormal, hU0, hU1, hSquare,
      hRoundp, hRoundq, hRoundt, hdDef, hRoundu, hRoundv, hdpEq, hdqEq,
      hCase1, hCase2⟩
  subst beta
  subst radix
  simp only [wp, PostCond.noThrow, pure, discri11_check, Id.run, ULift.up_down]
  by_cases hqLeP : _root_.F2R (beta:=2) q ≤ _root_.F2R (beta:=2) p
  · have h := discri10 (beta:=2) bo 2 precision
      a b b' c p q t d u v dp dq
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri10_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, rfl, hprecision, hprecision3, hvNum, haBound, hbBound,
        hb'Bound, hcBound, hpBound, hqBound, hdBound, huBound, hvBound,
        hdCan, hqNormal, hpNormal, hvNormal, huNormal, hU0, hSquare,
        hRoundp, hRoundq, hRoundt, hdDef, hRoundu, hRoundv, hdpEq,
        hdqEq, hCase1, hCase2, hqLeP⟩
  · have hpLeQ : _root_.F2R (beta:=2) p ≤ _root_.F2R (beta:=2) q :=
      le_of_lt (lt_of_not_ge hqLeP)
    have hpNonneg : 0 ≤ _root_.F2R (beta:=2) p :=
      P_positive bo (2 : ℝ)
        (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b') p
        hRoundp.1 hSquare
    have hqPos : 0 < _root_.F2R (beta:=2) q :=
      Q_positive _ _ hpNonneg hCase1
    have hacNonneg : 0 ≤
        _root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c := by
      by_contra hneg
      have hRounded : RoundedModeP (beta:=2) bo
          (EvenClosest (beta:=2) bo (2 : ℝ) precision) := by
        have h := EvenClosestRoundedModeP (beta:=2) bo 2 precision
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
          EvenClosestRoundedModeP_check, Id.run, ULift.up_down, Int.cast_ofNat] using
          h ⟨rfl, by decide, hprecision, hvNum⟩
      have hqNonpos : _root_.F2R (beta:=2) q ≤ 0 := by
        have h := RleRoundedLessR0 (beta:=2) bo
          (EvenClosest (beta:=2) bo (2 : ℝ) precision) q
          (_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c)
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RleRoundedLessR0_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          h ⟨hRounded, hRoundq, le_of_not_ge hneg⟩
      linarith
    have hSymmetric : SymmetricP
        (EvenClosest (beta:=2) bo (2 : ℝ) precision) := by
      have h := EvenClosestSymmetric (beta:=2) bo (2 : ℝ) precision
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestSymmetric_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
    have hdOppBound : Fbounded (beta:=2) bo (Fopp (beta:=2) d) := by
      have h := oppBounded (beta:=2) bo d
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, oppBounded_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h hdBound
    have hdOppCan : Fcanonic (beta:=2) 2 bo (Fopp (beta:=2) d) := by
      have h := FcanonicFopp (beta:=2) 2 bo d
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFopp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h hdCan
    have htOppRound : EvenClosest (beta:=2) bo (2 : ℝ) precision
        (_root_.F2R (beta:=2) q - _root_.F2R (beta:=2) p)
          (Fopp (beta:=2) t) := by
      have h := hSymmetric _ _ hRoundt
      simpa [Fopp, FloatSpec.Calc.Operations.Fopp, sub_eq_add_neg] using h
    have hdOppDef : Fopp (beta:=2) d = Fopp (beta:=2) t := by
      rw [hdDef]
    have hRounduSwap : EvenClosest (beta:=2) bo (2 : ℝ) precision
        (3 * |_root_.F2R (beta:=2) (Fopp (beta:=2) t)|) u := by
      simpa [Fopp, FloatSpec.Calc.Operations.Fopp, _root_.F2R,
        FloatSpec.Core.Defs.F2R, abs_neg] using hRoundu
    have hRoundvSwap : EvenClosest (beta:=2) bo (2 : ℝ) precision
        (_root_.F2R (beta:=2) q + _root_.F2R (beta:=2) p) v := by
      simpa [add_comm] using hRoundv
    have hCase1Swap :
        3 * |_root_.F2R (beta:=2) q - _root_.F2R (beta:=2) p| <
          _root_.F2R (beta:=2) q + _root_.F2R (beta:=2) p := by
      simpa [abs_sub_comm, add_comm] using hCase1
    have hswap := discri10 (beta:=2) bo 2 precision
      b a c b' q p (Fopp t) (Fopp d) u v dq dp
    have hbound :
        |_root_.F2R (beta:=2) (Fopp d) -
            (_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c -
              _root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b')| ≤
          2 * Fulp (beta:=2) bo 2 precision (Fopp d) := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri10_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        hswap ⟨rfl, rfl, hprecision, hprecision3, hvNum, hbBound,
          haBound, hcBound, hb'Bound, hqBound, hpBound, hdOppBound,
          huBound, hvBound, hdOppCan, hpNormal, hqNormal, hvNormal,
          huNormal, hU1, hacNonneg, hRoundq,
          by simpa [mul_comm] using hRoundp, htOppRound, hdOppDef,
          hRounduSwap, hRoundvSwap, hdqEq,
          by simpa [mul_comm] using hdpEq, hCase1Swap, hCase2, hpLeQ⟩
    have hdOppVal : _root_.F2R (beta:=2) (Fopp d) =
        -_root_.F2R (beta:=2) d := by
      simp [Fopp, FloatSpec.Calc.Operations.Fopp, _root_.F2R,
        FloatSpec.Core.Defs.F2R]
    have hFulpOpp : Fulp (beta:=2) bo 2 precision (Fopp d) =
        Fulp (beta:=2) bo 2 precision d := by
      have h := Fulp_Fopp (beta:=2) bo 2 precision d
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fulp_Fopp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
    rw [hdOppVal, hFulpOpp] at hbound
    have habs : |_root_.F2R (beta:=2) d -
          (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b' -
            _root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c)| =
        |-_root_.F2R (beta:=2) d -
          (_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c -
            _root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b')| := by
      rw [← abs_neg]
      congr 1
      ring
    rw [habs]
    exact hbound

noncomputable def discri12_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q t d u v s : FloatSpec.Core.Defs.FlocqFloat beta)
    (dp dq : ℝ) : Unit :=
  ()

/-- Coq discriminant theorem `discri12`, factored at the `discri11` payload.

Upstream derives this payload from the `p + q <= 3 * |p - q|` and `u < v`
branch arithmetic, using `RoundGeNormal`, `RoundLeNormal`, and ulp comparison
estimates.  The local theorem keeps the derived payload explicit and restores
the exact theorem name used by `discri13`. -/
theorem discri12_from_discri11_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q t d u v s : FloatSpec.Core.Defs.FlocqFloat beta)
    (dp dq : ℝ) :
    ⦃⌜_root_.F2R (beta:=beta) q ≤ _root_.F2R (beta:=beta) p ∧
        discri9_precondition (beta:=beta) bo radix precision
          a b b' c p q t s d dp dq⌝⦄
    (pure (discri12_check (beta:=beta) bo radix precision a b b' c p q t d u v s dp dq) :
      Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) d -
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| ≤
        2 * Fulp (beta:=beta) bo radix precision d⌝⦄ := by
  intro hpre
  rcases hpre with ⟨hq_le_p, hpayload⟩
  simp only [wp, PostCond.noThrow, pure, discri12_check, Id.run, ULift.up_down]
  have h := discri11_from_ordered_payloads (beta:=beta) bo radix precision a b b' c p q t s d dp dq
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri11_check, Id.run,
    ULift.up_down, Int.cast_ofNat] using h (Or.inl ⟨hq_le_p, hpayload⟩)

/- Coq `discri12` (Discriminant3A), with the complete section contract.
The scale comparisons used by the factored legacy proof are reconstructed
from the rounding and normality hypotheses. -/
set_option maxHeartbeats 1200000 in
theorem discri12 {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q t dp dq s d u v : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = 2 ∧ radix = 2 ∧ 1 < precision ∧ 4 ≤ precision ∧
        bo.vNum = Zpower_nat radix precision ∧
        Fbounded (beta:=beta) bo a ∧ Fbounded (beta:=beta) bo b ∧
        Fbounded (beta:=beta) bo b' ∧ Fbounded (beta:=beta) bo c ∧
        Fbounded (beta:=beta) bo p ∧ Fbounded (beta:=beta) bo q ∧
        Fbounded (beta:=beta) bo d ∧ Fbounded (beta:=beta) bo t ∧
        Fbounded (beta:=beta) bo s ∧
        (_root_.F2R (beta:=beta) u < _root_.F2R (beta:=beta) v →
          Fbounded (beta:=beta) bo dp) ∧
        (_root_.F2R (beta:=beta) u < _root_.F2R (beta:=beta) v →
          Fbounded (beta:=beta) bo dq) ∧
        Fcanonic (beta:=beta) radix bo v ∧
        Fcanonic (beta:=beta) radix bo s ∧
        -bo.dExp ≤ t.Fexp - 1 ∧
        (radix : ℝ) ^ (-bo.dExp + 2 * (precision : Int) - 1) ≤
          |_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b'| ∧
        (radix : ℝ) ^ (-bo.dExp + 2 * (precision : Int) - 1) ≤
          |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c| ∧
        Fnormal (beta:=beta) radix bo p ∧
        Fnormal (beta:=beta) radix bo q ∧
        Fnormal (beta:=beta) radix bo d ∧
        Fnormal (beta:=beta) radix bo t ∧
        Fnormal (beta:=beta) radix bo u ∧
        Fnormal (beta:=beta) radix bo v ∧
        0 ≤ _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b') p ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c) q ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (3 * |_root_.F2R (beta:=beta) t|) u ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) v ∧
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q ≤
          3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| ∧
        _root_.F2R (beta:=beta) u < _root_.F2R (beta:=beta) v ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q) t ∧
        _root_.F2R (beta:=beta) dp =
          _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
            _root_.F2R (beta:=beta) p ∧
        _root_.F2R (beta:=beta) dq =
          _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c -
            _root_.F2R (beta:=beta) q ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) dp - _root_.F2R (beta:=beta) dq) s ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) s) d ∧
        _root_.F2R (beta:=beta) q ≤ _root_.F2R (beta:=beta) p⌝⦄
    (pure (discri12_check (beta:=beta) bo radix precision a b b' c p q t d u v s
      (_root_.F2R (beta:=beta) dp) (_root_.F2R (beta:=beta) dq)) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) d -
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| ≤
        2 * Fulp (beta:=beta) bo radix precision d⌝⦄ := by
  intro hpre
  rcases hpre with
    ⟨hbeta, hradix, hprecision, hprecision4, hvNum, _haBound, _hbBound,
      _hb'Bound, _hcBound, hpBound, hqBound, hdBound, htBound, hsBound,
      hdpBoundIf, hdqBoundIf, _hvCan, hsCan, _hU1, _hU2, _hU3, hpNormal,
      hqNormal, hdNormal, htNormal, huNormal, hvNormal, hSquare, hRoundp,
      hRoundq, hRoundu, hRoundv, hCase1, hCase2, hRoundt, hdpEq, hdqEq,
      hRounds, hRoundd, hqLeP⟩
  subst beta
  subst radix
  simp only [wp, PostCond.noThrow, pure, discri12_check, Id.run, ULift.up_down]
  have hprecisionNe : precision ≠ 0 := by omega
  have hRounded : RoundedModeP (beta:=2) bo
      (EvenClosest (beta:=2) bo (2 : ℝ) precision) := by
    have h := EvenClosestRoundedModeP (beta:=2) bo 2 precision
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestRoundedModeP_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨rfl, by decide, hprecision, hvNum⟩
  have hTotal : TotalP (EvenClosest (beta:=2) bo (2 : ℝ) precision) := by
    have h := EvenClosestTotal (beta:=2) bo 2 precision
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestTotal_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨rfl, by decide, hprecision, hvNum⟩
  have hRoundedFloat : RoundedModeP_float (beta:=2) bo 2
      (EvenClosest (beta:=2) bo (2 : ℝ) precision) := by
    have h := EvenClosestRoundedModeP_from_total (beta:=2) bo 2 (2 : ℝ) precision
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestRoundedModeP_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hTotal
  have hMono : MonotoneP_float
      (EvenClosest (beta:=2) bo (2 : ℝ) precision) := by
    have h := EvenClosestMonotone_from_split_radix_payload (beta:=2) bo (2 : ℝ) precision
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestMonotone_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
  have hpNonneg : 0 ≤ _root_.F2R (beta:=2) p :=
    P_positive bo (2 : ℝ)
      (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b') p
      hRoundp.1 hSquare
  have hdiffNonneg : 0 ≤ _root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q :=
    sub_nonneg.mpr hqLeP
  have htNonneg : 0 ≤ _root_.F2R (beta:=2) t :=
    P_positive bo (2 : ℝ)
      (_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q) t
      hRoundt.1 hdiffNonneg
  have huNonneg : 0 ≤ _root_.F2R (beta:=2) u :=
    P_positive bo (2 : ℝ) (3 * |_root_.F2R (beta:=2) t|) u
      hRoundu.1 (mul_nonneg (by norm_num) (abs_nonneg _))
  have hvNonneg : 0 ≤ _root_.F2R (beta:=2) v :=
    le_trans huNonneg (le_of_lt hCase2)
  have hqPos : 0 < _root_.F2R (beta:=2) q := by
    by_contra hqNotPos
    have hqNonpos : _root_.F2R (beta:=2) q ≤ 0 := le_of_not_gt hqNotPos
    have hpLeInput : _root_.F2R (beta:=2) p ≤
        |_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q| := by
      rw [abs_of_nonneg hdiffNonneg]
      linarith
    have hpLeTAbs : _root_.F2R (beta:=2) p ≤
        |_root_.F2R (beta:=2) t| := by
      have h := RoundAbsMonotonel_from_rounded_payload (beta:=2) bo 2
        (EvenClosest (beta:=2) bo (2 : ℝ) precision)
        (_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q) t p
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RoundAbsMonotonel_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hRoundedFloat, hpBound, hRoundt, hpLeInput⟩
    have hInputLe : _root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q ≤
        3 * |_root_.F2R (beta:=2) t| := by
      nlinarith [abs_nonneg (_root_.F2R (beta:=2) t)]
    have h := EvenClosestMonotone2 (beta:=2) bo 2 precision
      (_root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q)
      (3 * |_root_.F2R (beta:=2) t|) v u
    have hvLeU : _root_.F2R (beta:=2) v ≤ _root_.F2R (beta:=2) u := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestMonotone2_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨rfl, by decide, hprecision, hvNum, hInputLe, hRoundv, hRoundu⟩
    exact (not_le_of_gt hCase2) hvLeU
  let eps : ℝ := (2 : ℝ) ^ (-(precision : Int))
  have hepsPos : 0 < eps := by dsimp [eps]; positivity
  have hepsLe : eps ≤ (1 / 16 : ℝ) := by
    have hpow : (2 : ℝ) ^ (-(precision : Int)) ≤ (2 : ℝ) ^ (-4 : Int) :=
      zpow_right_mono₀ (by norm_num) (by omega)
    norm_num [eps, zpow_neg] at hpow ⊢
    exact hpow
  have hdenPos : 0 < 1 - eps := by nlinarith
  have htRoundGe :
      |_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q| ≤
        |_root_.F2R (beta:=2) t| * (1 + eps) := by
    have h := RoundGeNormal (beta:=2) bo precision t
      (_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RoundGeNormal_check,
      Id.run, ULift.up_down, eps, Int.cast_ofNat] using
      h ⟨hRoundt.1, htNormal, rfl, hprecisionNe, hvNum⟩
  have huRoundGe : 3 * |_root_.F2R (beta:=2) t| ≤
      |_root_.F2R (beta:=2) u| * (1 + eps) := by
    have h := RoundGeNormal (beta:=2) bo precision u
      (3 * |_root_.F2R (beta:=2) t|)
    have h' : abs (3 * |_root_.F2R (beta:=2) t|) ≤
        |_root_.F2R (beta:=2) u| * (1 + eps) := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RoundGeNormal_check,
        Id.run, ULift.up_down, eps, Int.cast_ofNat] using
        h ⟨hRoundu.1, huNormal, rfl, hprecisionNe, hvNum⟩
    simpa [abs_mul, abs_of_nonneg (abs_nonneg (_root_.F2R (beta:=2) t))] using h'
  have hvRoundLe : |_root_.F2R (beta:=2) v| ≤
      |_root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q| / (1 - eps) := by
    have h := RoundLeNormal_from_denominator_payload (beta:=2) bo precision v
      (_root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RoundLeNormal_check,
      Id.run, ULift.up_down, eps, Int.cast_ofNat] using
      h ⟨hRoundv.1, hvNormal, rfl, hprecisionNe, hvNum, hdenPos⟩
  have hsumPos : 0 < _root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q :=
    add_pos_of_nonneg_of_pos hpNonneg hqPos
  have hchain : 3 * (_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q) ≤
      |_root_.F2R (beta:=2) v| * (1 + eps) ^ 2 := by
    rw [abs_of_nonneg hdiffNonneg] at htRoundGe
    have huv : |_root_.F2R (beta:=2) u| ≤ |_root_.F2R (beta:=2) v| := by
      rw [abs_of_nonneg huNonneg, abs_of_nonneg hvNonneg]
      exact le_of_lt hCase2
    nlinarith [mul_nonneg (abs_nonneg (_root_.F2R (beta:=2) t))
      (le_of_lt (by nlinarith : 0 < 1 + eps)),
      mul_le_mul_of_nonneg_right huv (by nlinarith : 0 ≤ 1 + eps)]
  have hvDen : |_root_.F2R (beta:=2) v| * (1 - eps) ≤
      _root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q := by
    rw [abs_of_pos hsumPos] at hvRoundLe
    exact (le_div_iff₀ hdenPos).mp hvRoundLe
  have hcoef : (1 + eps) ^ 2 ≤ (3 / 2 : ℝ) * (1 - eps) := by
    nlinarith [sq_nonneg eps]
  have hcoefV := mul_le_mul_of_nonneg_left hcoef
    (abs_nonneg (_root_.F2R (beta:=2) v))
  have hdiffHalf : _root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q ≤
      (_root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q) / 2 := by
    nlinarith
  have hpLeThreeQ : _root_.F2R (beta:=2) p ≤
      3 * _root_.F2R (beta:=2) q := by nlinarith
  have htwoQLeP : 2 * _root_.F2R (beta:=2) q ≤
      _root_.F2R (beta:=2) p := by
    rw [abs_of_nonneg hdiffNonneg] at hCase1
    linarith
  have hqLeT : _root_.F2R (beta:=2) q ≤ _root_.F2R (beta:=2) t := by
    have hqLeInput : _root_.F2R (beta:=2) q ≤
        |_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q| := by
      rw [abs_of_nonneg hdiffNonneg]
      linarith
    have h := RoundAbsMonotonel_from_rounded_payload (beta:=2) bo 2
      (EvenClosest (beta:=2) bo (2 : ℝ) precision)
      (_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q) t q
    have hqLeTAbs : _root_.F2R (beta:=2) q ≤ |_root_.F2R (beta:=2) t| := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RoundAbsMonotonel_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hRoundedFloat, hqBound, hRoundt, hqLeInput⟩
    simpa [abs_of_nonneg htNonneg] using hqLeTAbs
  let up := Fulp (beta:=2) bo 2 precision p
  let uq := Fulp (beta:=2) bo 2 precision q
  let ut := Fulp (beta:=2) bo 2 precision t
  let us := Fulp (beta:=2) bo 2 precision s
  let ud := Fulp (beta:=2) bo 2 precision d
  have hupLe4uq : up ≤ 4 * uq := by
    have habs : |_root_.F2R (beta:=2) p| ≤
        (2 : ℝ) ^ (2 : Int) * |_root_.F2R (beta:=2) q| := by
      rw [abs_of_nonneg hpNonneg, abs_of_pos hqPos]
      norm_num
      linarith
    simpa [up, uq, show (2 : ℝ) ^ (2 : Int) = 4 by norm_num] using
      fulp_le_zpow_mul_of_abs_le bo precision p q 2 hprecisionNe hvNum
        (by omega) hpBound hqNormal habs
  have huqLeUt : uq ≤ ut := by
    have h := LeFulpPos (beta:=2) 2 bo precision q t
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, LeFulpPos_check,
      Id.run, ULift.up_down, uq, ut, Int.cast_ofNat] using
      h ⟨hqBound, htBound, le_of_lt hqPos, hqLeT, rfl, by decide,
        hprecisionNe, hvNum⟩
  have hdpUlp : 2 * |_root_.F2R (beta:=2) dp| ≤ up := by
    have h := ClosestUlp (beta:=2) bo 2 precision
      (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b') p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, up, hdpEq, Int.cast_ofNat] using
      h ⟨hRoundp.1, rfl, by decide, hprecisionNe, hvNum⟩
  have hdqUlp : 2 * |_root_.F2R (beta:=2) dq| ≤ uq := by
    have h := ClosestUlp (beta:=2) bo 2 precision
      (_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c) q
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, uq, hdqEq, Int.cast_ofNat] using
      h ⟨hRoundq.1, rfl, by decide, hprecisionNe, hvNum⟩
  have hResidual : |_root_.F2R (beta:=2) dp - _root_.F2R (beta:=2) dq| ≤
      3 * uq := by
    have htri := abs_sub (_root_.F2R (beta:=2) dp) (_root_.F2R (beta:=2) dq)
    nlinarith [abs_nonneg (_root_.F2R (beta:=2) dp),
      abs_nonneg (_root_.F2R (beta:=2) dq)]
  let threeUq : FloatSpec.Core.Defs.FlocqFloat 2 := ⟨3, q.Fexp⟩
  have hthreeUqBound : Fbounded (beta:=2) bo threeUq := by
    constructor
    · change (3 : Int) < bo.vNum
      rw [hvNum, Zpower_nat]
      have hp8 : (8 : Int) ≤ (2 : Int) ^ precision := by
        have hpow : (2 : Nat) ^ 3 ≤ (2 : Nat) ^ precision :=
          Nat.pow_le_pow_right (by norm_num) (by omega)
        exact_mod_cast hpow
      omega
    · exact hqNormal.1.2
  have hthreeUqVal : _root_.F2R (beta:=2) threeUq = 3 * uq := by
    have hqUlp : uq = (2 : ℝ) ^ q.Fexp := by
      have h := CanonicFulp (beta:=2) 2 bo precision q
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
        Id.run, ULift.up_down, uq, Int.cast_ofNat] using
        h ⟨Or.inl hqNormal, rfl, by decide, hprecisionNe, hvNum⟩
    simp [threeUq, _root_.F2R, FloatSpec.Core.Defs.F2R, hqUlp]
  have hsLe3uq : |_root_.F2R (beta:=2) s| ≤ 3 * uq := by
    have h := RoundAbsMonotoner_from_rounded_payload (beta:=2) bo 2
      (EvenClosest (beta:=2) bo (2 : ℝ) precision)
      (_root_.F2R (beta:=2) dp - _root_.F2R (beta:=2) dq) s threeUq
    have hs : |_root_.F2R (beta:=2) s| ≤ _root_.F2R (beta:=2) threeUq := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RoundAbsMonotoner_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hRoundedFloat, hthreeUqBound, hRounds,
          by simpa [hthreeUqVal] using hResidual⟩
    simpa [hthreeUqVal] using hs
  have htUlpSmall : ut ≤ _root_.F2R (beta:=2) t / 8 := by
    have hnorm : Fnormal (beta:=2) 2 bo
        (Fnormalize (beta:=2) 2 bo precision t) := by
      have h := FcanonicFnormalizeEq (beta:=2) 2 bo precision t
      have heq : Fnormalize (beta:=2) 2 bo precision t = t := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          h ⟨Or.inl htNormal, rfl, by decide, hprecisionNe, hvNum⟩
      simpa [heq] using htNormal
    have h := FulpLe2 (beta:=2) 2 bo precision t
    have h' : ut ≤ |_root_.F2R (beta:=2) t| *
        (2 : ℝ) ^ (1 - (precision : Int)) := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpLe2_check,
        Id.run, ULift.up_down, ut, Int.cast_ofNat] using
        h ⟨htBound, hnorm, rfl, by decide, hprecisionNe, hvNum⟩
    have he : (2 : ℝ) ^ (1 - (precision : Int)) ≤ 1 / 8 := by
      have hz := zpow_right_mono₀ (by norm_num : (1 : ℝ) ≤ 2)
        (show 1 - (precision : Int) ≤ (-3 : Int) by omega)
      norm_num [zpow_neg] at hz ⊢
      exact hz
    rw [abs_of_nonneg htNonneg] at h'
    nlinarith [mul_le_mul_of_nonneg_left he htNonneg]
  have hsLe3ut : |_root_.F2R (beta:=2) s| ≤ 3 * ut := by
    nlinarith
  let lower : FloatSpec.Core.Defs.FlocqFloat 2 := ⟨t.Fnum - 3, t.Fexp⟩
  have htNumPos : 0 < t.Fnum := by
    exact (FloatSpec.Core.Defs.F2R_pos_iff t (by decide)).mp
      (lt_of_lt_of_le hqPos hqLeT)
  have htNumGe8 : (8 : Int) ≤ t.Fnum := by
    have hmant : bo.vNum ≤ 2 * t.Fnum := by
      simpa [abs_of_pos (mul_pos (by norm_num : (0 : Int) < 2) htNumPos)] using
        htNormal.2
    have hv8 : (16 : Int) ≤ bo.vNum := by
      rw [hvNum, Zpower_nat]
      have hpow : (2 : Nat) ^ 4 ≤ (2 : Nat) ^ precision :=
        Nat.pow_le_pow_right (by norm_num) (by omega)
      exact_mod_cast hpow
    omega
  have hlowerBound : Fbounded (beta:=2) bo lower := by
    constructor
    · rw [abs_of_nonneg (show 0 ≤ t.Fnum - 3 by omega)]
      have htMant := htBound.1
      rw [abs_of_pos htNumPos] at htMant
      omega
    · exact htBound.2
  have hlowerVal : _root_.F2R (beta:=2) lower =
      _root_.F2R (beta:=2) t - 3 * ut := by
    have htUlp : ut = (2 : ℝ) ^ t.Fexp := by
      have h := CanonicFulp (beta:=2) 2 bo precision t
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
        Id.run, ULift.up_down, ut, Int.cast_ofNat] using
        h ⟨Or.inl htNormal, rfl, by decide, hprecisionNe, hvNum⟩
    simp only [lower, _root_.F2R, FloatSpec.Core.Defs.F2R,
      FloatSpec.Core.Defs.FlocqFloat.Fnum,
      FloatSpec.Core.Defs.FlocqFloat.Fexp, Int.cast_sub, Int.cast_ofNat]
    rw [htUlp]
    ring
  have hlowerInput : _root_.F2R (beta:=2) lower ≤
      |_root_.F2R (beta:=2) t + _root_.F2R (beta:=2) s| := by
    have hrev : |_root_.F2R (beta:=2) t| - |_root_.F2R (beta:=2) s| ≤
        |_root_.F2R (beta:=2) t + _root_.F2R (beta:=2) s| := by
      have h := abs_sub_abs_le_abs_sub
        (_root_.F2R (beta:=2) t) (-_root_.F2R (beta:=2) s)
      simpa [abs_neg, sub_neg_eq_add] using h
    rw [abs_of_nonneg htNonneg] at hrev
    rw [hlowerVal]
    nlinarith
  have hlowerLeD : _root_.F2R (beta:=2) lower ≤
      |_root_.F2R (beta:=2) d| := by
    have h := RoundAbsMonotonel_from_rounded_payload (beta:=2) bo 2
      (EvenClosest (beta:=2) bo (2 : ℝ) precision)
      (_root_.F2R (beta:=2) t + _root_.F2R (beta:=2) s) d lower
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RoundAbsMonotonel_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hRoundedFloat, hlowerBound, hRoundd, hlowerInput⟩
  have htLe2D : _root_.F2R (beta:=2) t ≤
      2 * |_root_.F2R (beta:=2) d| := by
    rw [hlowerVal] at hlowerLeD
    nlinarith
  have hdAbsNormal : Fnormal (beta:=2) 2 bo (Fabs (beta:=2) d) := by
    have h := FnormalFabs (beta:=2) bo 2 d
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalFabs_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hdNormal
  have hdAbsVal : _root_.F2R (beta:=2) (Fabs (beta:=2) d) =
      |_root_.F2R (beta:=2) d| := by
    have h := Fabs_correct (beta:=2) d
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fabs_correct_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h (by decide : (1 : Int) < 2)
  have hutLe2ud : ut ≤ 2 * ud := by
    have h := Fulp_le_twice_r (beta:=2) bo precision t (Fabs d)
    have h' : ut ≤ 2 * Fulp (beta:=2) bo 2 precision (Fabs d) := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fulp_le_twice_r_check,
        Id.run, ULift.up_down, ut, Int.cast_ofNat] using
        h ⟨rfl, hprecisionNe, hvNum, htNonneg, htBound, hdAbsNormal,
          by simpa [hdAbsVal] using htLe2D⟩
    have hf := FulpFabs (beta:=2) bo 2 precision d
    have hf' : ud = Fulp (beta:=2) bo 2 precision (Fabs d) := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpFabs_check,
        Id.run, ULift.up_down, ud, Int.cast_ofNat] using hf (by norm_num : (0 : Int) < 2)
    simpa [hf'] using h'
  have hsLeD : |_root_.F2R (beta:=2) s| ≤ |_root_.F2R (beta:=2) d| := by
    rw [hlowerVal] at hlowerLeD
    nlinarith
  have hsAbsBound : Fbounded (beta:=2) bo (Fabs (beta:=2) s) := by
    have h := absFBounded (beta:=2) bo s
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, absFBounded_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hsBound
  have hdAbsBound : Fbounded (beta:=2) bo (Fabs (beta:=2) d) := hdAbsNormal.1
  have hsAbsVal : _root_.F2R (beta:=2) (Fabs (beta:=2) s) =
      |_root_.F2R (beta:=2) s| := by
    have h := Fabs_correct (beta:=2) s
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fabs_correct_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h (by decide : (1 : Int) < 2)
  have husLeUd : us ≤ ud := by
    have h := LeFulpPos (beta:=2) 2 bo precision (Fabs s) (Fabs d)
    have h' : Fulp (beta:=2) bo 2 precision (Fabs s) ≤
        Fulp (beta:=2) bo 2 precision (Fabs d) := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, LeFulpPos_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hsAbsBound, hdAbsBound, by rw [hsAbsVal]; positivity,
          by simpa [hsAbsVal, hdAbsVal] using hsLeD, rfl, by decide,
          hprecisionNe, hvNum⟩
    have hsF := FulpFabs (beta:=2) bo 2 precision s
    have hdF := FulpFabs (beta:=2) bo 2 precision d
    have hsF' : us = Fulp (beta:=2) bo 2 precision (Fabs s) := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpFabs_check,
        Id.run, ULift.up_down, us, Int.cast_ofNat] using hsF (by norm_num : (0 : Int) < 2)
    have hdF' : ud = Fulp (beta:=2) bo 2 precision (Fabs d) := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpFabs_check,
        Id.run, ULift.up_down, ud, Int.cast_ofNat] using hdF (by norm_num : (0 : Int) < 2)
    simpa [hsF', hdF'] using h'
  have hdErr : 2 * |(_root_.F2R (beta:=2) t + _root_.F2R (beta:=2) s) -
        _root_.F2R (beta:=2) d| ≤ ud := by
    have h := ClosestUlp (beta:=2) bo 2 precision
      (_root_.F2R (beta:=2) t + _root_.F2R (beta:=2) s) d
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, ud, Int.cast_ofNat] using
      h ⟨hRoundd.1, rfl, by decide, hprecisionNe, hvNum⟩
  have htErr : 2 * |(_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q) -
        _root_.F2R (beta:=2) t| ≤ ut := by
    have h := ClosestUlp (beta:=2) bo 2 precision
      (_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q) t
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, ut, Int.cast_ofNat] using
      h ⟨hRoundt.1, rfl, by decide, hprecisionNe, hvNum⟩
  have hsErr : 2 * |(_root_.F2R (beta:=2) dp - _root_.F2R (beta:=2) dq) -
        _root_.F2R (beta:=2) s| ≤ us := by
    have h := ClosestUlp (beta:=2) bo 2 precision
      (_root_.F2R (beta:=2) dp - _root_.F2R (beta:=2) dq) s
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, us, Int.cast_ofNat] using
      h ⟨hRounds.1, rfl, by decide, hprecisionNe, hvNum⟩
  have hdecomp : |_root_.F2R (beta:=2) d -
        (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b' -
          _root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c)| ≤
      |(_root_.F2R (beta:=2) t + _root_.F2R (beta:=2) s) -
          _root_.F2R (beta:=2) d| +
      |(_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q) -
          _root_.F2R (beta:=2) t| +
      |(_root_.F2R (beta:=2) dp - _root_.F2R (beta:=2) dq) -
          _root_.F2R (beta:=2) s| := by
    rw [hdpEq, hdqEq]
    calc
      |_root_.F2R (beta:=2) d -
          (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b' -
            _root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c)| =
          |-(((_root_.F2R (beta:=2) t + _root_.F2R (beta:=2) s) -
              _root_.F2R (beta:=2) d) +
            ((_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q) -
              _root_.F2R (beta:=2) t) +
            ((_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b' -
                _root_.F2R (beta:=2) p) -
              ((_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c -
                _root_.F2R (beta:=2) q)) - _root_.F2R (beta:=2) s))| := by
                congr 1 <;> ring
      _ ≤ |(_root_.F2R (beta:=2) t + _root_.F2R (beta:=2) s) -
              _root_.F2R (beta:=2) d| +
            |(_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q) -
              _root_.F2R (beta:=2) t| +
            |((_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b' -
                _root_.F2R (beta:=2) p) -
              (_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c -
                _root_.F2R (beta:=2) q)) - _root_.F2R (beta:=2) s| := by
                rw [abs_neg]
                exact le_trans (abs_add_le _ _)
                  (add_le_add_left (abs_add_le _ _) _)
      _ = _ := by rfl
  have hdErr' : |(_root_.F2R (beta:=2) t + _root_.F2R (beta:=2) s) -
        _root_.F2R (beta:=2) d| ≤ ud / 2 := by nlinarith
  have htErr' : |(_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q) -
        _root_.F2R (beta:=2) t| ≤ ut / 2 := by nlinarith
  have hsErr' : |(_root_.F2R (beta:=2) dp - _root_.F2R (beta:=2) dq) -
        _root_.F2R (beta:=2) s| ≤ us / 2 := by nlinarith
  have hfinal : |_root_.F2R (beta:=2) d -
        (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b' -
          _root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c)| ≤ 2 * ud := by
    calc
      _ ≤ ud / 2 + ut / 2 + us / 2 :=
        le_trans hdecomp (add_le_add (add_le_add hdErr' htErr') hsErr')
      _ ≤ ud / 2 + (2 * ud) / 2 + ud / 2 := by
        gcongr
      _ = 2 * ud := by ring
  simpa [ud] using hfinal

noncomputable def discri13_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q t d u v s : FloatSpec.Core.Defs.FlocqFloat beta)
    (dp dq : ℝ) : Unit :=
  ()

/-- Coq discriminant theorem `discri13`, factored at the two `discri12`
payloads.

This is the large-branch analogue of `discri11`: the direct case calls
`discri12`, while the opposite order calls `discri12` on swapped products and
negated compensated terms. -/
theorem discri13_from_ordered_payloads {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q t d u v s : FloatSpec.Core.Defs.FlocqFloat beta)
    (dp dq : ℝ) :
    ⦃⌜(_root_.F2R (beta:=beta) q ≤ _root_.F2R (beta:=beta) p ∧
          discri9_precondition (beta:=beta) bo radix precision
            a b b' c p q t s d dp dq) ∨
        (_root_.F2R (beta:=beta) p < _root_.F2R (beta:=beta) q ∧
          discri9_precondition (beta:=beta) bo radix precision
            b a c b' q p (Fopp (beta:=beta) t) (Fopp (beta:=beta) s)
            (Fopp (beta:=beta) d) dq dp)⌝⦄
    (pure (discri13_check (beta:=beta) bo radix precision a b b' c p q t d u v s dp dq) :
      Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) d -
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| ≤
        2 * Fulp (beta:=beta) bo radix precision d⌝⦄ := by
  intro hpre
  simp only [wp, PostCond.noThrow, pure, discri13_check, Id.run, ULift.up_down]
  rcases hpre with hdirect | hswapped
  · rcases hdirect with ⟨hq_le_p, hpayload⟩
    have h := discri12_from_discri11_payload (beta:=beta) bo radix precision a b b' c p q t d u v s dp dq
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri12_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨hq_le_p, hpayload⟩
  · rcases hswapped with ⟨hp_lt_q, hpayload⟩
    have hp_le_q : _root_.F2R (beta:=beta) p ≤ _root_.F2R (beta:=beta) q :=
      le_of_lt hp_lt_q
    have hswap := discri12_from_discri11_payload (beta:=beta) bo radix precision
      b a c b' q p (Fopp (beta:=beta) t) (Fopp (beta:=beta) d) u v
      (Fopp (beta:=beta) s) dq dp
    have hswap' :
        |_root_.F2R (beta:=beta) (Fopp (beta:=beta) d) -
            (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c -
              _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b')| ≤
          2 * Fulp (beta:=beta) bo radix precision (Fopp (beta:=beta) d) := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri12_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using hswap ⟨hp_le_q, hpayload⟩
    have hdOpp :
        _root_.F2R (beta:=beta) (Fopp (beta:=beta) d) =
          -_root_.F2R (beta:=beta) d := by
      have h := FloatSpec.Calc.Operations.F2R_opp (beta:=beta) d
      simpa [Fopp, _root_.F2R] using h trivial
    have htarget_abs :
        |_root_.F2R (beta:=beta) d -
            (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
              _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| =
          |_root_.F2R (beta:=beta) (Fopp (beta:=beta) d) -
            (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c -
              _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b')| := by
      rw [hdOpp]
      rw [← abs_neg (_root_.F2R (beta:=beta) d -
        (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
          _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c))]
      congr 1
      ring
    have hFulpOpp :
        Fulp (beta:=beta) bo radix precision (Fopp (beta:=beta) d) =
          Fulp (beta:=beta) bo radix precision d := by
      have h := Fulp_Fopp (beta:=beta) bo radix precision d
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fulp_Fopp_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using h True.intro
    simpa [htarget_abs, hFulpOpp] using hswap'

/-- Coq `discri13` (Discriminant4A), with its source section hypotheses.
The reverse-order branch invokes source-shaped `discri12` on the swapped
products and the negated compensated sum. -/
theorem discri13 {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q t dp dq s d u v : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = 2 ∧ radix = 2 ∧ 1 < precision ∧ 4 ≤ precision ∧
        bo.vNum = Zpower_nat radix precision ∧
        Fbounded (beta:=beta) bo a ∧ Fbounded (beta:=beta) bo b ∧
        Fbounded (beta:=beta) bo b' ∧ Fbounded (beta:=beta) bo c ∧
        Fbounded (beta:=beta) bo p ∧ Fbounded (beta:=beta) bo q ∧
        Fbounded (beta:=beta) bo d ∧ Fbounded (beta:=beta) bo t ∧
        Fbounded (beta:=beta) bo s ∧
        (_root_.F2R (beta:=beta) u < _root_.F2R (beta:=beta) v →
          Fbounded (beta:=beta) bo dp) ∧
        (_root_.F2R (beta:=beta) u < _root_.F2R (beta:=beta) v →
          Fbounded (beta:=beta) bo dq) ∧
        Fcanonic (beta:=beta) radix bo v ∧
        Fcanonic (beta:=beta) radix bo s ∧
        -bo.dExp ≤ t.Fexp - 1 ∧
        (radix : ℝ) ^ (-bo.dExp + 2 * (precision : Int) - 1) ≤
          |_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b'| ∧
        (radix : ℝ) ^ (-bo.dExp + 2 * (precision : Int) - 1) ≤
          |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c| ∧
        Fnormal (beta:=beta) radix bo p ∧
        Fnormal (beta:=beta) radix bo q ∧
        Fnormal (beta:=beta) radix bo d ∧
        Fnormal (beta:=beta) radix bo t ∧
        Fnormal (beta:=beta) radix bo u ∧
        Fnormal (beta:=beta) radix bo v ∧
        0 ≤ _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b') p ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c) q ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (3 * |_root_.F2R (beta:=beta) t|) u ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) v ∧
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q ≤
          3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| ∧
        _root_.F2R (beta:=beta) u < _root_.F2R (beta:=beta) v ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q) t ∧
        _root_.F2R (beta:=beta) dp =
          _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
            _root_.F2R (beta:=beta) p ∧
        _root_.F2R (beta:=beta) dq =
          _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c -
            _root_.F2R (beta:=beta) q ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) dp - _root_.F2R (beta:=beta) dq) s ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) s) d⌝⦄
    (pure (discri13_check (beta:=beta) bo radix precision a b b' c p q t d u v s
      (_root_.F2R (beta:=beta) dp) (_root_.F2R (beta:=beta) dq)) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) d -
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b' -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| ≤
        2 * Fulp (beta:=beta) bo radix precision d⌝⦄ := by
  intro hpre
  rcases hpre with
    ⟨hbeta, hradix, hprecision, hprecision4, hvNum, haBound, hbBound,
      hb'Bound, hcBound, hpBound, hqBound, hdBound, htBound, hsBound,
      hdpBoundIf, hdqBoundIf, hvCan, hsCan, hU1, hU2, hU3, hpNormal,
      hqNormal, hdNormal, htNormal, huNormal, hvNormal, hSquare, hRoundp,
      hRoundq, hRoundu, hRoundv, hCase1, hCase2, hRoundt, hdpEq, hdqEq,
      hRounds, hRoundd⟩
  subst beta
  subst radix
  simp only [wp, PostCond.noThrow, pure, discri13_check, Id.run, ULift.up_down]
  change |_root_.F2R (beta:=2) d -
        (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b' -
          _root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c)| ≤
      2 * Fulp (beta:=2) bo 2 precision d
  by_cases hqLeP : _root_.F2R (beta:=2) q ≤ _root_.F2R (beta:=2) p
  · have h := discri12 (beta:=2) bo 2 precision
      a b b' c p q t dp dq s d u v
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri12_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h
      ⟨rfl, rfl, hprecision, hprecision4, hvNum, haBound, hbBound,
        hb'Bound, hcBound, hpBound, hqBound, hdBound, htBound, hsBound,
        hdpBoundIf, hdqBoundIf, hvCan, hsCan, hU1, hU2, hU3, hpNormal,
        hqNormal, hdNormal, htNormal, huNormal, hvNormal, hSquare, hRoundp,
        hRoundq, hRoundu, hRoundv, hCase1, hCase2, hRoundt, hdpEq,
        hdqEq, hRounds, hRoundd, hqLeP⟩
  · have hpLtQ := lt_of_not_ge hqLeP
    have hpNonneg : 0 ≤ _root_.F2R (beta:=2) p :=
      P_positive bo (2 : ℝ)
        (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b') p
        hRoundp.1 hSquare
    have hRounded : RoundedModeP (beta:=2) bo
        (EvenClosest (beta:=2) bo (2 : ℝ) precision) := by
      have h := EvenClosestRoundedModeP (beta:=2) bo 2 precision
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestRoundedModeP_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨rfl, by decide, hprecision, hvNum⟩
    have hAcNonneg : 0 ≤
        _root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c := by
      by_contra hac
      have hqNonpos : _root_.F2R (beta:=2) q ≤ 0 := by
        have h := RleRoundedLessR0 (beta:=2) bo
          (EvenClosest (beta:=2) bo (2 : ℝ) precision) q
          (_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c)
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RleRoundedLessR0_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          h ⟨hRounded, hRoundq, le_of_not_ge hac⟩
      linarith
    have hSymmetric : SymmetricP
        (EvenClosest (beta:=2) bo (2 : ℝ) precision) := by
      have h := EvenClosestSymmetric (beta:=2) bo (2 : ℝ) precision
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestSymmetric_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
    have oppVal (x : FloatSpec.Core.Defs.FlocqFloat 2) :
        _root_.F2R (beta:=2) (Fopp x) = -_root_.F2R (beta:=2) x := by
      have h := Fopp_correct (beta:=2) x
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fopp_correct_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
    have oppBound (x : FloatSpec.Core.Defs.FlocqFloat 2)
        (hx : Fbounded (beta:=2) bo x) : Fbounded (beta:=2) bo (Fopp x) := by
      have h := oppBounded (beta:=2) bo x
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, oppBounded_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h hx
    have oppNormal (x : FloatSpec.Core.Defs.FlocqFloat 2)
        (hx : Fnormal (beta:=2) 2 bo x) : Fnormal (beta:=2) 2 bo (Fopp x) := by
      have h := FnormalFop (beta:=2) bo 2 x
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalFop_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h hx
    have hsOppCan : Fcanonic (beta:=2) 2 bo (Fopp s) := by
      have h := FcanonicFopp (beta:=2) 2 bo s
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFopp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h hsCan
    have hswap := discri12 (beta:=2) bo 2 precision
      b a c b' q p (Fopp t) dq dp (Fopp s) (Fopp d) u v
    have hswapBound :
        |_root_.F2R (beta:=2) (Fopp d) -
            (_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c -
              _root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b')| ≤
          2 * Fulp (beta:=2) bo 2 precision (Fopp d) := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri12_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using hswap
        ⟨rfl, rfl, hprecision, hprecision4, hvNum, hbBound, haBound,
          hcBound, hb'Bound, hqBound, hpBound, oppBound d hdBound,
          oppBound t htBound, oppBound s hsBound,
          fun _ => hdqBoundIf hCase2, fun _ => hdpBoundIf hCase2,
          hvCan, hsOppCan, by simpa [Fopp,
            FloatSpec.Calc.Operations.Fopp] using hU1, hU3, hU2,
          hqNormal, hpNormal, oppNormal d hdNormal, oppNormal t htNormal,
          huNormal, hvNormal, hAcNonneg, hRoundq,
          by simpa [mul_comm] using hRoundp,
          by simpa [oppVal] using hRoundu,
          by simpa [add_comm] using hRoundv,
          by simpa [oppVal, abs_sub_comm, add_comm] using hCase1,
          hCase2,
          by simpa [oppVal, sub_eq_add_neg, add_comm] using
            hSymmetric _ _ hRoundt,
          by simpa [mul_comm] using hdqEq,
          by simpa [mul_comm] using hdpEq,
          by simpa [oppVal, sub_eq_add_neg, add_comm] using
            hSymmetric _ _ hRounds,
          by simpa [oppVal, sub_eq_add_neg, add_comm, add_left_comm] using
            hSymmetric _ _ hRoundd,
          le_of_lt hpLtQ⟩
    have hFulpOpp : Fulp (beta:=2) bo 2 precision (Fopp d) =
        Fulp (beta:=2) bo 2 precision d := by
      have h := Fulp_Fopp (beta:=2) bo 2 precision d
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fulp_Fopp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
    rw [oppVal, hFulpOpp] at hswapBound
    calc
      |_root_.F2R (beta:=2) d -
          (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b' -
            _root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c)| =
          |-_root_.F2R (beta:=2) d -
            (_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c -
              _root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b')| := by
            rw [← abs_neg]
            congr 1
            ring
      _ ≤ 2 * Fulp (beta:=2) bo 2 precision d := hswapBound

def discri11_precondition {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q t s d : FloatSpec.Core.Defs.FlocqFloat beta)
    (dp dq : ℝ) : Prop :=
  (_root_.F2R (beta:=beta) q ≤ _root_.F2R (beta:=beta) p ∧
      discri9_precondition (beta:=beta) bo radix precision
        a b b' c p q t s d dp dq) ∨
    (_root_.F2R (beta:=beta) p < _root_.F2R (beta:=beta) q ∧
      discri9_precondition (beta:=beta) bo radix precision
        b a c b' q p (Fopp (beta:=beta) t) s
        (Fopp (beta:=beta) d) dq dp)

def discri13_precondition {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b b' c p q t s d : FloatSpec.Core.Defs.FlocqFloat beta)
    (dp dq : ℝ) : Prop :=
  (_root_.F2R (beta:=beta) q ≤ _root_.F2R (beta:=beta) p ∧
      discri9_precondition (beta:=beta) bo radix precision
        a b b' c p q t s d dp dq) ∨
    (_root_.F2R (beta:=beta) p < _root_.F2R (beta:=beta) q ∧
      discri9_precondition (beta:=beta) bo radix precision
        b a c b' q p (Fopp (beta:=beta) t) (Fopp (beta:=beta) s)
        (Fopp (beta:=beta) d) dq dp)

def discri14_precondition {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b c p q t d u v s : FloatSpec.Core.Defs.FlocqFloat beta)
    (dp dq : ℝ) : Prop :=
  ((_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q ≤
        3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q|) ∧
      _root_.F2R (beta:=beta) v ≤ _root_.F2R (beta:=beta) u ∧
      discri9_precondition (beta:=beta) bo radix precision
        a b b c p q t s d dp dq) ∨
    ((_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q ≤
        3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q|) ∧
      _root_.F2R (beta:=beta) u < _root_.F2R (beta:=beta) v ∧
      discri13_precondition (beta:=beta) bo radix precision
        a b b c p q t s d dp dq) ∨
    (3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| <
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q ∧
      _root_.F2R (beta:=beta) v ≤ _root_.F2R (beta:=beta) u ∧
      discri11_precondition (beta:=beta) bo radix precision
        a b b c p q t s d dp dq) ∨
    (3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| <
        _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q ∧
      _root_.F2R (beta:=beta) u < _root_.F2R (beta:=beta) v ∧
      discri9_precondition (beta:=beta) bo radix precision
        a b b c p q t s d dp dq)

noncomputable def discri14_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b c p q t d u v s : FloatSpec.Core.Defs.FlocqFloat beta)
    (dp dq : ℝ) : Unit :=
  ()

/-- Coq discriminant theorem `discri14`, factored at the four case payloads.

Upstream splits on `p + q <= 3 * |p - q|` and `v <= u`.  The direct
same-rounding branches call `discri9`; the large-`p+q`, `u < v` branch calls
`discri13`; and the large-`p+q`, `v <= u` branch calls `discri11` after the
residual bounded-witness payloads have been derived. -/
theorem discri14_from_branch_payloads {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b c p q t d u v s : FloatSpec.Core.Defs.FlocqFloat beta)
    (dp dq : ℝ) :
    ⦃⌜((_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q ≤
            3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q|) ∧
          _root_.F2R (beta:=beta) v ≤ _root_.F2R (beta:=beta) u ∧
          discri9_precondition (beta:=beta) bo radix precision
            a b b c p q t s d dp dq) ∨
        ((_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q ≤
            3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q|) ∧
          _root_.F2R (beta:=beta) u < _root_.F2R (beta:=beta) v ∧
          discri13_precondition (beta:=beta) bo radix precision
            a b b c p q t s d dp dq) ∨
        (3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| <
            _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q ∧
          _root_.F2R (beta:=beta) v ≤ _root_.F2R (beta:=beta) u ∧
          discri11_precondition (beta:=beta) bo radix precision
            a b b c p q t s d dp dq) ∨
        (3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| <
            _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q ∧
          _root_.F2R (beta:=beta) u < _root_.F2R (beta:=beta) v ∧
          discri9_precondition (beta:=beta) bo radix precision
            a b b c p q t s d dp dq)⌝⦄
    (pure (discri14_check (beta:=beta) bo radix precision a b c p q t d u v s dp dq) :
      Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) d -
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| ≤
        2 * Fulp (beta:=beta) bo radix precision d⌝⦄ := by
  intro hpre
  simp only [wp, PostCond.noThrow, pure, discri14_check, Id.run, ULift.up_down]
  rcases hpre with hsmall_round | hrest
  · rcases hsmall_round with ⟨_hcase, _hv_le_u, hpayload⟩
    have h := discri9_from_branch_payload (beta:=beta) bo radix precision a b b c p q t s d dp dq
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri9_check, Id.run,
      ULift.up_down, discri9_precondition, Int.cast_ofNat] using h hpayload
  rcases hrest with hsmall_comp | hrest
  · rcases hsmall_comp with ⟨_hcase, _hu_lt_v, hpayload⟩
    have h := discri13_from_ordered_payloads (beta:=beta) bo radix precision a b b c p q t d u v s dp dq
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri13_check, Id.run,
      ULift.up_down, discri13_precondition, Int.cast_ofNat] using h hpayload
  rcases hrest with hlarge_round | hlarge_comp
  · rcases hlarge_round with ⟨_hcase, _hv_le_u, hpayload⟩
    have h := discri11_from_ordered_payloads (beta:=beta) bo radix precision a b b c p q t s d dp dq
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri11_check, Id.run,
      ULift.up_down, discri11_precondition, Int.cast_ofNat] using h hpayload
  · rcases hlarge_comp with ⟨_hcase, _hu_lt_v, hpayload⟩
    have h := discri9_from_branch_payload (beta:=beta) bo radix precision a b b c p q t s d dp dq
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri9_check, Id.run,
      ULift.up_down, discri9_precondition, Int.cast_ofNat] using h hpayload

/- Coq `discri14` (Discriminant5), with its full source hypotheses.  The four
algorithmic cases are dispatched internally to source-shaped `discri9`,
`discri13`, and `discri11`; in the latter branch the product-error witnesses
recover the two exponent premises required by `discri11`. -/
set_option maxHeartbeats 1600000 in
theorem discri14 {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b c p q t dp dq s d u v :
      FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = 2 ∧ radix = 2 ∧ 1 < precision ∧ 4 ≤ precision ∧
        bo.vNum = Zpower_nat radix precision ∧
        Fbounded (beta:=beta) bo a ∧ Fbounded (beta:=beta) bo b ∧
        Fbounded (beta:=beta) bo c ∧ Fbounded (beta:=beta) bo p ∧
        Fbounded (beta:=beta) bo q ∧ Fbounded (beta:=beta) bo d ∧
        Fbounded (beta:=beta) bo t ∧
        (_root_.F2R (beta:=beta) u < _root_.F2R (beta:=beta) v →
          Fbounded (beta:=beta) bo s) ∧
        (_root_.F2R (beta:=beta) u < _root_.F2R (beta:=beta) v →
          Fbounded (beta:=beta) bo dp) ∧
        (_root_.F2R (beta:=beta) u < _root_.F2R (beta:=beta) v →
          Fbounded (beta:=beta) bo dq) ∧
        Fbounded (beta:=beta) bo u ∧ Fbounded (beta:=beta) bo v ∧
        (_root_.F2R (beta:=beta) u < _root_.F2R (beta:=beta) v →
          Fcanonic (beta:=beta) radix bo s) ∧
        -bo.dExp ≤ d.Fexp - 1 ∧ -bo.dExp ≤ t.Fexp - 1 ∧
        (radix : ℝ) ^ (-bo.dExp + 2 * (precision : Int) - 1) ≤
          |_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b| ∧
        (radix : ℝ) ^ (-bo.dExp + 2 * (precision : Int) - 1) ≤
          |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c| ∧
        Fnormal (beta:=beta) radix bo p ∧
        Fnormal (beta:=beta) radix bo q ∧
        Fnormal (beta:=beta) radix bo d ∧
        Fnormal (beta:=beta) radix bo u ∧
        Fnormal (beta:=beta) radix bo v ∧
        Fnormal (beta:=beta) radix bo t ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b) p ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c) q ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q) t ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (3 * |_root_.F2R (beta:=beta) t|) u ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) v ∧
        (_root_.F2R (beta:=beta) v ≤ _root_.F2R (beta:=beta) u →
          EvenClosest (beta:=beta) bo (radix : ℝ) precision
            (_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q) d) ∧
        _root_.F2R (beta:=beta) dp =
          _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b -
            _root_.F2R (beta:=beta) p ∧
        _root_.F2R (beta:=beta) dq =
          _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c -
            _root_.F2R (beta:=beta) q ∧
        (_root_.F2R (beta:=beta) u < _root_.F2R (beta:=beta) v →
          EvenClosest (beta:=beta) bo (radix : ℝ) precision
            (_root_.F2R (beta:=beta) dp - _root_.F2R (beta:=beta) dq) s) ∧
        (_root_.F2R (beta:=beta) u < _root_.F2R (beta:=beta) v →
          EvenClosest (beta:=beta) bo (radix : ℝ) precision
            (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) s) d)⌝⦄
    (pure (discri14_check (beta:=beta) bo radix precision a b c p q t d u v s
      (_root_.F2R (beta:=beta) dp) (_root_.F2R (beta:=beta) dq)) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) d -
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| ≤
        2 * Fulp (beta:=beta) bo radix precision d⌝⦄ := by
  intro hpre
  rcases hpre with
    ⟨hbeta, hradix, hprecision, hprecision4, hvNum, haBound, hbBound,
      hcBound, hpBound, hqBound, hdBound, htBound, hsBoundIf, hdpBoundIf,
      hdqBoundIf, huBound, hvBound, hsCanIf, hU0, hU1, hU2, hU3,
      hpNormal, hqNormal, hdNormal, huNormal, hvNormal, htNormal, hRoundp,
      hRoundq, hRoundt, hRoundu, hRoundv, hFRoundd, hdpEq, hdqEq,
      hSRounds, hSRoundd⟩
  subst beta
  subst radix
  simp only [wp, PostCond.noThrow, pure, discri14_check, Id.run, ULift.up_down]
  by_cases hFirst : _root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q ≤
      3 * |_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q|
  · by_cases hvLeU : _root_.F2R (beta:=2) v ≤ _root_.F2R (beta:=2) u
    · have h := discri9 (beta:=2) bo 2 precision
        a b c p q t dp dq s d
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri9_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨rfl, rfl, hprecision, hvNum, haBound, hbBound, hcBound,
          hpBound, hqBound, hdBound, htBound,
          (fun hs => False.elim ((not_lt_of_ge hFirst) hs)),
          (fun hs => False.elim ((not_lt_of_ge hFirst) hs)),
          (fun hs => False.elim ((not_lt_of_ge hFirst) hs)),
          (fun hs => False.elim ((not_lt_of_ge hFirst) hs)),
          hU0, hU1, hU2, hU3, hpNormal, hqNormal, hdNormal,
          hRoundp, hRoundq, (fun _ => hFRoundd hvLeU),
          (fun hs => False.elim ((not_lt_of_ge hFirst) hs)),
          (fun hs => False.elim ((not_lt_of_ge hFirst) hs)),
          (fun hs => False.elim ((not_lt_of_ge hFirst) hs)),
          (fun hs => False.elim ((not_lt_of_ge hFirst) hs)),
          (fun hs => False.elim ((not_lt_of_ge hFirst) hs))⟩
    · have huLtV := lt_of_not_ge hvLeU
      have h := discri13 (beta:=2) bo 2 precision
        a b b c p q t dp dq s d u v
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri13_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨rfl, rfl, hprecision, hprecision4, hvNum, haBound, hbBound,
          hbBound, hcBound, hpBound, hqBound, hdBound, htBound,
          hsBoundIf huLtV, hdpBoundIf, hdqBoundIf, Or.inl hvNormal,
          hsCanIf huLtV, hU1, hU2, hU3, hpNormal, hqNormal, hdNormal,
          htNormal, huNormal, hvNormal, mul_self_nonneg _, hRoundp,
          hRoundq, hRoundu, hRoundv, hFirst, huLtV, hRoundt, hdpEq,
          hdqEq, hSRounds huLtV, hSRoundd huLtV⟩
  · have hSecond : 3 * |_root_.F2R (beta:=2) p -
        _root_.F2R (beta:=2) q| <
      _root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q :=
      lt_of_not_ge hFirst
    by_cases hvLeU : _root_.F2R (beta:=2) v ≤ _root_.F2R (beta:=2) u
    · rcases errorBoundedMultClosest_Can bo precision b b p rfl hprecision
          hvNum hbBound hbBound hRoundp.1 hU2 (Or.inl hpNormal) with
        ⟨sp, hspBound, _hspVal, hspExp, _hspNum⟩
      rcases errorBoundedMultClosest_Can bo precision a c q rfl hprecision
          hvNum haBound hcBound hRoundq.1 hU3 (Or.inl hqNormal) with
        ⟨sq, hsqBound, _hsqVal, hsqExp, _hsqNum⟩
      have hpExpLower : -bo.dExp ≤ p.Fexp - 2 := by
        have hspLower := hspBound.2
        rw [hspExp] at hspLower
        omega
      have hqExpLower : -bo.dExp ≤ q.Fexp - 2 := by
        have hsqLower := hsqBound.2
        rw [hsqExp] at hsqLower
        omega
      have hdtValue : _root_.F2R (beta:=2) d =
          _root_.F2R (beta:=2) t := by
        have h := EvenClosestUniqueP (beta:=2) bo 2 precision
        have hUnique : UniqueP
            (EvenClosest (beta:=2) bo (2 : ℝ) precision) := by
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestUniqueP_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using
            h ⟨rfl, by decide, hprecision, hvNum⟩
        exact hUnique
          (_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q)
          d t (hFRoundd hvLeU) hRoundt
      have hdt : d = t := by
        have h := FcanonicUnique (beta:=2) 2 bo d t (by decide) rfl
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicUnique_check,
          Id.run, ULift.up_down, Fcanonic', Int.cast_ofNat] using
          h ⟨Or.inl hdNormal, Or.inl htNormal, hdtValue⟩
      have h := discri11 (beta:=2) bo 2 precision
        a b b c p q t d u v dp dq
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri11_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨rfl, rfl, hprecision, (by omega), hvNum, haBound, hbBound,
          hbBound, hcBound, hpBound, hqBound, hdBound, huBound, hvBound,
          Or.inl hdNormal, hqNormal, hpNormal, hvNormal, huNormal,
          hpExpLower, hqExpLower, mul_self_nonneg _, hRoundp, hRoundq,
          hRoundt, hdt, hRoundu, hRoundv, hdpEq, hdqEq, hSecond, hvLeU⟩
    · have huLtV := lt_of_not_ge hvLeU
      have h := discri9 (beta:=2) bo 2 precision
        a b c p q t dp dq s d
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri9_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨rfl, rfl, hprecision, hvNum, haBound, hbBound, hcBound,
          hpBound, hqBound, hdBound, htBound,
          (fun _ => hsBoundIf huLtV),
          (fun _ => hdpBoundIf huLtV), (fun _ => hdqBoundIf huLtV),
          (fun _ => hsCanIf huLtV),
          hU0, hU1, hU2, hU3, hpNormal, hqNormal, hdNormal,
          hRoundp, hRoundq,
          (fun hf => False.elim ((not_le_of_gt hSecond) hf)),
          (fun _ => hRoundt), (fun _ => hdpEq), (fun _ => hdqEq),
          (fun _ => hSRounds huLtV), (fun _ => hSRoundd huLtV)⟩

noncomputable def discri15_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b c p q t d u v s : FloatSpec.Core.Defs.FlocqFloat beta)
    (dp dq : ℝ) : Unit :=
  ()

def discri15_precondition {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b c p q t d u v s : FloatSpec.Core.Defs.FlocqFloat beta)
    (dp dq : ℝ) : Prop :=
  discri14_precondition (beta:=beta) bo radix precision
      a b c
      (Fnormalize (beta:=beta) radix bo precision p)
      (Fnormalize (beta:=beta) radix bo precision q)
      (Fnormalize (beta:=beta) radix bo precision t)
      (Fnormalize (beta:=beta) radix bo precision d)
      (Fnormalize (beta:=beta) radix bo precision u)
      (Fnormalize (beta:=beta) radix bo precision v)
      (Fnormalize (beta:=beta) radix bo precision s)
      dp dq ∧
    _root_.F2R (beta:=beta)
      (Fnormalize (beta:=beta) radix bo precision d) =
      _root_.F2R (beta:=beta) d ∧
    Fulp (beta:=beta) bo radix precision
      (Fnormalize (beta:=beta) radix bo precision d) =
      Fulp (beta:=beta) bo radix precision d

/-- Coq discriminant theorem `discri15`, factored at the normalization handoff.

Upstream proves the required normality and value-preservation facts for the
rounded intermediates, normalizes `p`, `q`, `t`, `s`, `d`, `u`, and `v`, then
delegates to `discri14`.  This local theorem keeps that normalized
`discri14` payload and the final `d` value/ulp rewrites explicit. -/
theorem discri15_from_normalization_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b c p q t d u v s : FloatSpec.Core.Defs.FlocqFloat beta)
    (dp dq : ℝ) :
    ⦃⌜discri15_precondition (beta:=beta) bo radix precision
        a b c p q t d u v s dp dq⌝⦄
    (pure (discri15_check (beta:=beta) bo radix precision a b c p q t d u v s dp dq) :
      Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) d -
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| ≤
        2 * Fulp (beta:=beta) bo radix precision d⌝⦄ := by
  intro hpre
  dsimp [discri15_precondition] at hpre
  rcases hpre with ⟨hpayload, hnorm_d, hfulp_d⟩
  simp only [wp, PostCond.noThrow, pure, discri15_check, Id.run, ULift.up_down]
  have h := discri14_from_branch_payloads (beta:=beta) bo radix precision
    a b c
    (Fnormalize (beta:=beta) radix bo precision p)
    (Fnormalize (beta:=beta) radix bo precision q)
    (Fnormalize (beta:=beta) radix bo precision t)
    (Fnormalize (beta:=beta) radix bo precision d)
    (Fnormalize (beta:=beta) radix bo precision u)
    (Fnormalize (beta:=beta) radix bo precision v)
    (Fnormalize (beta:=beta) radix bo precision s)
    dp dq
  have hnorm_bound :
      |_root_.F2R (beta:=beta) (Fnormalize (beta:=beta) radix bo precision d) -
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| ≤
        2 * Fulp (beta:=beta) bo radix precision
          (Fnormalize (beta:=beta) radix bo precision d) := by
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri14_check, Id.run,
      ULift.up_down, discri14_precondition, Int.cast_ofNat] using h hpayload
  simpa [hnorm_d, hfulp_d] using hnorm_bound

/-- Transport an even-closest witness to another bounded representation of
the same real value.  This is the concrete output-compatibility property used
by Coq's `EvenClosestCompatible_from_structural_payload`; unlike the older generic compatibility
shell, it deliberately compares represented real values. -/
private theorem evenClosest_of_eq_value
    (bo : Fbound_skel) (precision : Nat) (r : ℝ)
    (f g : FloatSpec.Core.Defs.FlocqFloat 2)
    (hprecision : 1 < precision)
    (hvNum : bo.vNum = Zpower_nat 2 precision)
    (hf : EvenClosest (beta:=2) bo (2 : ℝ) precision r f)
    (hgBound : Fbounded (beta:=2) bo g)
    (hvalue : _root_.F2R (beta:=2) g = _root_.F2R (beta:=2) f) :
    EvenClosest (beta:=2) bo (2 : ℝ) precision r g := by
  have hgClosest : Closest (beta:=2) bo (2 : ℝ) r g := by
    refine ⟨hgBound, ?_⟩
    intro q hqBound
    simpa [hvalue] using hf.1.2 q hqBound
  refine ⟨hgClosest, ?_⟩
  rcases hf.2 with hfEven | hfUnique
  · left
    have h := FNevenEq (beta:=2) bo (2 : ℝ) precision f g
      (by decide) hprecision hvNum
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FNevenEq_check,
      ULift.down_up, Int.cast_ofNat] using h ⟨hf.1.1, hgBound, hvalue.symm, hfEven⟩
  · right
    intro q hqClosest
    exact (hfUnique q hqClosest).trans hvalue.symm

/-- A bounded float at or above the first-normal magnitude has a normal
normalized representation.  This is the local form of the normalization
argument at the start of Coq `discri15`. -/
private theorem normalize_normal_of_abs_ge
    (bo : Fbound_skel) (precision : Nat)
    (f : FloatSpec.Core.Defs.FlocqFloat 2)
    (hprecision : 1 < precision)
    (hvNum : bo.vNum = Zpower_nat 2 precision)
    (hfBound : Fbounded (beta:=2) bo f)
    (hmag : (2 : ℝ) ^ (-bo.dExp + (precision : Int) - 1) ≤
      |_root_.F2R (beta:=2) f|) :
    Fnormal (beta:=2) 2 bo
      (Fnormalize (beta:=2) 2 bo precision f) := by
  let nf := Fnormalize (beta:=2) 2 bo precision f
  have hprecisionNe : precision ≠ 0 := by omega
  have hnfBound : Fbounded (beta:=2) bo nf := by
    have h := FnormalizeBounded (beta:=2) 2 bo precision f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeBounded_check,
      Id.run, ULift.up_down, Fbounded', nf, Int.cast_ofNat] using
      h ⟨hfBound, hfBound, hprecisionNe, by decide, hvNum⟩
  have hnfCan : Fcanonic (beta:=2) 2 bo nf := by
    have h := FnormalizeCanonic (beta:=2) 2 bo precision f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
      Id.run, ULift.up_down, Fbounded', nf, Int.cast_ofNat] using
      h ⟨hfBound, hfBound, hprecisionNe, by decide, hvNum⟩
  rcases hnfCan with hnormal | hsubnormal
  · exact hnormal
  · have hnfValue : _root_.F2R (beta:=2) nf =
        _root_.F2R (beta:=2) f := by
      have h := FnormalizeCorrect (beta:=2) 2 bo precision f
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
        Id.run, ULift.up_down, nf, Int.cast_ofNat] using h ⟨rfl, by decide⟩
    have habsSub : Fsubnormal (beta:=2) 2 bo
        (Fabs (beta:=2) nf) := by
      have h := FsubnormFabs (beta:=2) bo 2 nf
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FsubnormFabs_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h hsubnormal
    have habsValue : _root_.F2R (beta:=2) (Fabs (beta:=2) nf) =
        |_root_.F2R (beta:=2) nf| := by
      have h := Fabs_correct (beta:=2) nf
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fabs_correct_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h (show (1 : Int) < 2 by decide)
    have hltFirst : _root_.F2R (beta:=2) (Fabs (beta:=2) nf) <
        _root_.F2R (beta:=2)
          (firstNormalPos (beta:=2) 2 bo precision) := by
      have h := FsubnormalLtFirstNormalPos (beta:=2) 2 bo precision
        (Fabs (beta:=2) nf)
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
        FsubnormalLtFirstNormalPos_check, Id.run, ULift.up_down,
        Fsubnormal', Int.cast_ofNat] using
        h ⟨habsSub, habsSub, hprecisionNe, rfl, by decide, hvNum,
          by rw [habsValue]; exact abs_nonneg _⟩
    have hfirstValue :
        _root_.F2R (beta:=2)
            (firstNormalPos (beta:=2) 2 bo precision) =
          (2 : ℝ) ^ (-bo.dExp + (precision : Int) - 1) := by
      simp only [firstNormalPos, nNormMin, _root_.F2R,
        FloatSpec.Core.Defs.F2R,
        FloatSpec.Core.Defs.FlocqFloat.Fnum,
        FloatSpec.Core.Defs.FlocqFloat.Fexp, Int.cast_pow,
        Int.cast_ofNat]
      have hpred : ((precision - 1 : Nat) : Int) =
          (precision : Int) - 1 := by omega
      rw [show -bo.dExp + (precision : Int) - 1 =
        ((precision : Int) - 1) + (-bo.dExp) by ring]
      rw [zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0)]
      rw [← hpred]
      simp [zpow_natCast]
    rw [habsValue, hfirstValue, hnfValue] at hltFirst
    exact False.elim ((not_lt_of_ge hmag) hltFirst)

/- Coq Discriminant6 `discri15`.  All normalization facts consumed by
`discri14` are reconstructed from the source section hypotheses. -/
set_option maxHeartbeats 1600000 in
theorem discri15 {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b c p q t dp dq s d u v :
      FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜ beta = 2 ∧ radix = 2 ∧ 1 < precision ∧ 4 ≤ precision ∧
        bo.vNum = Zpower_nat radix precision ∧
        Fbounded (beta:=beta) bo a ∧ Fbounded (beta:=beta) bo b ∧
        Fbounded (beta:=beta) bo c ∧
        (_root_.F2R (beta:=beta) u < _root_.F2R (beta:=beta) v →
          Fbounded (beta:=beta) bo dp) ∧
        (_root_.F2R (beta:=beta) u < _root_.F2R (beta:=beta) v →
          Fbounded (beta:=beta) bo dq) ∧
        (radix : ℝ) ^ (-bo.dExp + 2 * (precision : Int) - 1) ≤
          |_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b| ∧
        (radix : ℝ) ^ (-bo.dExp + 2 * (precision : Int) - 1) ≤
          |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c| ∧
        (radix : ℝ) ^ (-bo.dExp + (precision : Int)) ≤
          |_root_.F2R (beta:=beta) d| ∧
        (radix : ℝ) ^ (-bo.dExp + (precision : Int) - 1) ≤
          |_root_.F2R (beta:=beta) u| ∧
        (radix : ℝ) ^ (-bo.dExp + (precision : Int) - 1) ≤
          |_root_.F2R (beta:=beta) v| ∧
        (radix : ℝ) ^ (-bo.dExp + (precision : Int)) ≤
          |_root_.F2R (beta:=beta) t| ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b) p ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c) q ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q) t ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (3 * |_root_.F2R (beta:=beta) t|) u ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) v ∧
        (_root_.F2R (beta:=beta) v ≤ _root_.F2R (beta:=beta) u →
          EvenClosest (beta:=beta) bo (radix : ℝ) precision
            (_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q) d) ∧
        _root_.F2R (beta:=beta) dp =
          _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b -
            _root_.F2R (beta:=beta) p ∧
        _root_.F2R (beta:=beta) dq =
          _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c -
            _root_.F2R (beta:=beta) q ∧
        (_root_.F2R (beta:=beta) u < _root_.F2R (beta:=beta) v →
          EvenClosest (beta:=beta) bo (radix : ℝ) precision
            (_root_.F2R (beta:=beta) dp - _root_.F2R (beta:=beta) dq) s) ∧
        (_root_.F2R (beta:=beta) u < _root_.F2R (beta:=beta) v →
          EvenClosest (beta:=beta) bo (radix : ℝ) precision
            (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) s) d)⌝⦄
    (pure (discri15_check (beta:=beta) bo radix precision a b c p q t d u v s
      (_root_.F2R (beta:=beta) dp) (_root_.F2R (beta:=beta) dq)) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) d -
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| ≤
        2 * Fulp (beta:=beta) bo radix precision d⌝⦄ := by
  intro hpre
  rcases hpre with
    ⟨hbeta, hradix, hprecision, hprecision4, hvNum, haBound, hbBound,
      hcBound, hdpBoundIf, hdqBoundIf, hU1, hU2, hU4, hU5, hU6, hU7,
      hRoundp, hRoundq, hRoundt, hRoundu, hRoundv, hFRoundd, hdpEq, hdqEq,
      hSRounds, hSRoundd⟩
  subst beta
  subst radix
  simp only [wp, PostCond.noThrow, pure, discri15_check, Id.run, ULift.up_down]
  change |_root_.F2R (beta:=2) d -
        (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b -
          _root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c)| ≤
      2 * Fulp (beta:=2) bo 2 precision d
  have hprecisionNe : precision ≠ 0 := by omega
  have hvNumGt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact_mod_cast (Nat.one_lt_pow hprecisionNe (by norm_num : 1 < (2 : Nat)))
  have hvNumReal : (bo.vNum : ℝ) = (2 : ℝ) ^ (precision : Int) := by
    rw [hvNum, Zpower_nat, Int.cast_pow]
    exact (zpow_natCast (2 : ℝ) precision).symm
  have hTotal : TotalP (EvenClosest (beta:=2) bo (2 : ℝ) precision) := by
    have h := EvenClosestTotal (beta:=2) bo 2 precision
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestTotal_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨rfl, by decide, hprecision, hvNum⟩
  have hRounded : RoundedModeP_float (beta:=2) bo 2
      (EvenClosest (beta:=2) bo (2 : ℝ) precision) := by
    have h := EvenClosestRoundedModeP_from_total (beta:=2) bo 2 (2 : ℝ) precision
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestRoundedModeP_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hTotal
  let nf : FloatSpec.Core.Defs.FlocqFloat 2 →
      FloatSpec.Core.Defs.FlocqFloat 2 :=
    fun f => Fnormalize (beta:=2) 2 bo precision f
  have normValue (f : FloatSpec.Core.Defs.FlocqFloat 2) :
      _root_.F2R (beta:=2) (nf f) = _root_.F2R (beta:=2) f := by
    have h := FnormalizeCorrect (beta:=2) 2 bo precision f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
      Id.run, ULift.up_down, nf, Int.cast_ofNat] using h ⟨rfl, by decide⟩
  have normBound (f : FloatSpec.Core.Defs.FlocqFloat 2)
      (hf : Fbounded (beta:=2) bo f) : Fbounded (beta:=2) bo (nf f) := by
    have h := FnormalizeBounded (beta:=2) 2 bo precision f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeBounded_check,
      Id.run, ULift.up_down, Fbounded', nf, Int.cast_ofNat] using
      h ⟨hf, hf, hprecisionNe, by decide, hvNum⟩
  have normCan (f : FloatSpec.Core.Defs.FlocqFloat 2)
      (hf : Fbounded (beta:=2) bo f) : Fcanonic (beta:=2) 2 bo (nf f) := by
    have h := FnormalizeCanonic (beta:=2) 2 bo precision f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
      Id.run, ULift.up_down, Fbounded', nf, Int.cast_ofNat] using
      h ⟨hf, hf, hprecisionNe, by decide, hvNum⟩
  have roundedLow (r : ℝ) (f : FloatSpec.Core.Defs.FlocqFloat 2)
      (hr : EvenClosest (beta:=2) bo (2 : ℝ) precision r f)
      (hmag : (2 : ℝ) ^ (-bo.dExp + 2 * (precision : Int) - 1) ≤ |r|) :
      (2 : ℝ) ^ (-bo.dExp + (precision : Int) - 1) ≤
        |_root_.F2R (beta:=2) f| := by
    let w : FloatSpec.Core.Defs.FlocqFloat 2 :=
      ⟨1, -bo.dExp + (precision : Int) - 1⟩
    have hwBound : Fbounded (beta:=2) bo w := by
      constructor
      · simpa [w] using hvNumGt
      · dsimp [w]
        omega
    have hwVal : _root_.F2R (beta:=2) w =
        (2 : ℝ) ^ (-bo.dExp + (precision : Int) - 1) := by
      simp [w, _root_.F2R, FloatSpec.Core.Defs.F2R]
    have hweak : (2 : ℝ) ^ (-bo.dExp + (precision : Int) - 1) ≤
        (2 : ℝ) ^ (-bo.dExp + 2 * (precision : Int) - 1) :=
      zpow_right_mono₀ (by norm_num) (by omega)
    have h := RoundAbsMonotonel_from_rounded_payload (beta:=2) bo 2
      (EvenClosest (beta:=2) bo (2 : ℝ) precision) r f w
    have hout : _root_.F2R (beta:=2) w ≤ |_root_.F2R (beta:=2) f| := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RoundAbsMonotonel_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hRounded, hwBound, hr,
          by rw [hwVal]; exact le_trans hweak hmag⟩
    simpa [hwVal] using hout
  have hpBound : Fbounded (beta:=2) bo p := hRoundp.1.1
  have hqBound : Fbounded (beta:=2) bo q := hRoundq.1.1
  have htBound : Fbounded (beta:=2) bo t := hRoundt.1.1
  have huBound : Fbounded (beta:=2) bo u := hRoundu.1.1
  have hvBound : Fbounded (beta:=2) bo v := hRoundv.1.1
  have hdBound : Fbounded (beta:=2) bo d := by
    by_cases hvLeU : _root_.F2R (beta:=2) v ≤ _root_.F2R (beta:=2) u
    · exact (hFRoundd hvLeU).1.1
    · exact (hSRoundd (lt_of_not_ge hvLeU)).1.1
  have hpLow := roundedLow
    (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b) p hRoundp hU1
  have hqLow := roundedLow
    (_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c) q hRoundq hU2
  have hU4Low : (2 : ℝ) ^ (-bo.dExp + (precision : Int) - 1) ≤
      |_root_.F2R (beta:=2) d| :=
    le_trans (zpow_right_mono₀ (by norm_num) (by omega)) hU4
  have hU7Low : (2 : ℝ) ^ (-bo.dExp + (precision : Int) - 1) ≤
      |_root_.F2R (beta:=2) t| :=
    le_trans (zpow_right_mono₀ (by norm_num) (by omega)) hU7
  have hpNormal := normalize_normal_of_abs_ge bo precision p hprecision hvNum
    hpBound hpLow
  have hqNormal := normalize_normal_of_abs_ge bo precision q hprecision hvNum
    hqBound hqLow
  have htNormal := normalize_normal_of_abs_ge bo precision t hprecision hvNum
    htBound hU7Low
  have hdNormal := normalize_normal_of_abs_ge bo precision d hprecision hvNum
    hdBound hU4Low
  have huNormal := normalize_normal_of_abs_ge bo precision u hprecision hvNum
    huBound hU5
  have hvNormal := normalize_normal_of_abs_ge bo precision v hprecision hvNum
    hvBound hU6
  have normExp (f : FloatSpec.Core.Defs.FlocqFloat 2)
      (hf : Fbounded (beta:=2) bo f)
      (hmag : (2 : ℝ) ^ (-bo.dExp + (precision : Int)) ≤
        |_root_.F2R (beta:=2) f|) : -bo.dExp ≤ (nf f).Fexp - 1 := by
    have h := FexpGeUnderf_from_expanded_bound_payload (beta:=2) bo (precision : Int)
      (-bo.dExp + (precision : Int)) (nf f)
    have h' : -bo.dExp + (precision : Int) - (precision : Int) + 1 ≤
        (nf f).Fexp := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FexpGeUnderf_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨normBound f hf, by simpa [normValue f] using hmag, by decide,
          hvNumReal, normBound f hf⟩
    omega
  have hdExp := normExp d hdBound hU4
  have htExp := normExp t htBound hU7
  have roundNorm (r : ℝ) (f : FloatSpec.Core.Defs.FlocqFloat 2)
      (hr : EvenClosest (beta:=2) bo (2 : ℝ) precision r f) :
      EvenClosest (beta:=2) bo (2 : ℝ) precision r (nf f) :=
    evenClosest_of_eq_value bo precision r f (nf f) hprecision hvNum hr
      (normBound f hr.1.1) (normValue f)
  have hRoundpN := roundNorm _ p hRoundp
  have hRoundqN := roundNorm _ q hRoundq
  have hRoundtN : EvenClosest (beta:=2) bo (2 : ℝ) precision
      (_root_.F2R (beta:=2) (nf p) - _root_.F2R (beta:=2) (nf q)) (nf t) := by
    simpa [normValue p, normValue q] using roundNorm _ t hRoundt
  have hRounduN : EvenClosest (beta:=2) bo (2 : ℝ) precision
      (3 * |_root_.F2R (beta:=2) (nf t)|) (nf u) := by
    simpa [normValue t] using roundNorm _ u hRoundu
  have hRoundvN : EvenClosest (beta:=2) bo (2 : ℝ) precision
      (_root_.F2R (beta:=2) (nf p) + _root_.F2R (beta:=2) (nf q)) (nf v) := by
    simpa [normValue p, normValue q] using roundNorm _ v hRoundv
  have hndFixed : Fnormalize (beta:=2) 2 bo precision (nf d) = nf d := by
    have h := FcanonicFnormalizeEq (beta:=2) 2 bo precision (nf d)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨normCan d hdBound, rfl, by decide, hprecisionNe, hvNum⟩
  have hfulp : Fulp (beta:=2) bo 2 precision (nf d) =
      Fulp (beta:=2) bo 2 precision d := by
    dsimp [Fulp]
    rw [hndFixed]
  have h := discri14 (beta:=2) bo 2 precision a b c
    (nf p) (nf q) (nf t) dp dq (nf s) (nf d) (nf u) (nf v)
  have hbound : |_root_.F2R (beta:=2) (nf d) -
        (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b -
          _root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c)| ≤
      2 * Fulp (beta:=2) bo 2 precision (nf d) := by
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri14_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h
      ⟨rfl, rfl, hprecision, hprecision4, hvNum, haBound, hbBound, hcBound,
        normBound p hpBound, normBound q hqBound, normBound d hdBound,
        normBound t htBound,
        (fun hlt => normBound s (hSRounds (by simpa [normValue u, normValue v] using hlt)).1.1),
        (fun hlt => hdpBoundIf (by simpa [normValue u, normValue v] using hlt)),
        (fun hlt => hdqBoundIf (by simpa [normValue u, normValue v] using hlt)),
        normBound u huBound, normBound v hvBound,
        (fun hlt => normCan s (hSRounds (by simpa [normValue u, normValue v] using hlt)).1.1),
        hdExp, htExp, hU1, hU2, hpNormal, hqNormal, hdNormal, huNormal,
        hvNormal, htNormal, hRoundpN, hRoundqN, hRoundtN, hRounduN,
        hRoundvN,
        (fun hvLeU => by
          have hvLeU' : _root_.F2R (beta:=2) v ≤ _root_.F2R (beta:=2) u := by
            simpa [normValue u, normValue v] using hvLeU
          simpa [normValue p, normValue q] using roundNorm _ d (hFRoundd hvLeU')),
        by simpa [normValue p] using hdpEq,
        by simpa [normValue q] using hdqEq,
        (fun hlt => by
          have hlt' : _root_.F2R (beta:=2) u < _root_.F2R (beta:=2) v := by
            simpa [normValue u, normValue v] using hlt
          exact roundNorm _ s (hSRounds hlt')),
        (fun hlt => by
          have hlt' : _root_.F2R (beta:=2) u < _root_.F2R (beta:=2) v := by
            simpa [normValue u, normValue v] using hlt
          simpa [normValue t, normValue s] using roundNorm _ d (hSRoundd hlt'))⟩
  simpa [normValue d, hfulp] using hbound

noncomputable def cases_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b c p q t dp dq s d u v : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq Discriminant7 theorem `cases`.

Either one of the zero cases holds, or all six no-underflow magnitude bounds
needed by the normalized `discri15` handoff hold. -/
theorem «cases» {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b c p q t dp dq s d u v : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = 2 ∧ radix = 2 ∧ 1 < precision ∧ 4 ≤ precision ∧
        bo.vNum = Zpower_nat radix precision ∧
        Fbounded (beta:=beta) bo a ∧
        Fbounded (beta:=beta) bo b ∧
        Fbounded (beta:=beta) bo c ∧
        (_root_.F2R (beta:=beta) u < _root_.F2R (beta:=beta) v →
          Fbounded (beta:=beta) bo dp) ∧
        (_root_.F2R (beta:=beta) u < _root_.F2R (beta:=beta) v →
          Fbounded (beta:=beta) bo dq) ∧
        (_root_.F2R (beta:=beta) b = 0 ∨
          (radix : ℝ) ^ (-bo.dExp + 3 * (precision : Int) - 1) ≤
            |_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b|) ∧
        (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c = 0 ∨
          (radix : ℝ) ^ (-bo.dExp + 3 * (precision : Int) - 1) ≤
            |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c|) ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b) p ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c) q ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q) t ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (3 * |_root_.F2R (beta:=beta) t|) u ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) v ∧
        (_root_.F2R (beta:=beta) v ≤ _root_.F2R (beta:=beta) u →
          EvenClosest (beta:=beta) bo (radix : ℝ) precision
            (_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q) d) ∧
        _root_.F2R (beta:=beta) dp =
          _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b -
          _root_.F2R (beta:=beta) p ∧
        _root_.F2R (beta:=beta) dq =
          _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c -
          _root_.F2R (beta:=beta) q ∧
        (_root_.F2R (beta:=beta) u < _root_.F2R (beta:=beta) v →
          EvenClosest (beta:=beta) bo (radix : ℝ) precision
            (_root_.F2R (beta:=beta) dp - _root_.F2R (beta:=beta) dq) s) ∧
        (_root_.F2R (beta:=beta) u < _root_.F2R (beta:=beta) v →
          EvenClosest (beta:=beta) bo (radix : ℝ) precision
            (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) s) d)⌝⦄
    (pure (cases_check (beta:=beta) bo radix precision
      a b c p q t dp dq s d u v) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) b = 0 ∨
        _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c = 0 ∨
        _root_.F2R (beta:=beta) d = 0 ∨
        _root_.F2R (beta:=beta) v = 0 ∨
        _root_.F2R (beta:=beta) t = 0 ∨
        ((radix : ℝ) ^ (-bo.dExp + 2 * (precision : Int) - 1) ≤
            |_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b| ∧
          (radix : ℝ) ^ (-bo.dExp + 2 * (precision : Int) - 1) ≤
            |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c| ∧
          (radix : ℝ) ^ (-bo.dExp + (precision : Int)) ≤
            |_root_.F2R (beta:=beta) d| ∧
          (radix : ℝ) ^ (-bo.dExp + (precision : Int) - 1) ≤
            |_root_.F2R (beta:=beta) u| ∧
          (radix : ℝ) ^ (-bo.dExp + (precision : Int) - 1) ≤
            |_root_.F2R (beta:=beta) v| ∧
          (radix : ℝ) ^ (-bo.dExp + (precision : Int)) ≤
            |_root_.F2R (beta:=beta) t|)⌝⦄ := by
  classical
  intro hpre
  rcases hpre with
    ⟨hbeta, hradix_eq, hprecision_gt, _hprecision_four, hvNum,
      _haBound, _hbBound, _hcBound, _hFdp, _hFdq, hU1, hU2, hRoundp, hRoundq,
      hRoundt, hRoundu, hRoundv, hFRoundd, _hdpEq, _hdqEq, hSRounds, hSRoundd⟩
  subst beta
  subst radix
  simp only [wp, PostCond.noThrow, pure, cases_check, Id.run,
    ULift.up_down]
  by_cases hbZero : _root_.F2R (beta:=2) b = 0
  · exact Or.inl hbZero
  right
  by_cases hacZero :
      _root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c = 0
  · exact Or.inl hacZero
  right
  by_cases hdZero : _root_.F2R (beta:=2) d = 0
  · exact Or.inl hdZero
  right
  by_cases hvZero : _root_.F2R (beta:=2) v = 0
  · exact Or.inl hvZero
  right
  by_cases htZero : _root_.F2R (beta:=2) t = 0
  · exact Or.inl htZero
  right
  have hprecision_ne : precision ≠ 0 := by omega
  have hprecision_pos : 0 < precision := by omega
  have hprecision_int_pos : (1 : Int) ≤ (precision : Int) := by
    exact_mod_cast hprecision_pos
  have hradix_gt : (1 : Int) < 2 := by decide
  have hradixR_pos : (0 : ℝ) < 2 := by norm_num
  have hradixR_ne : (2 : ℝ) ≠ 0 := by norm_num
  have hradixR_ge : (1 : ℝ) ≤ 2 := by norm_num
  have hBoundExp :
      ∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=2) 2 r).Fexp := by
    intro r
    have hnonneg : (0 : Int) ≤ (boundR (beta:=2) 2 r).Fexp := by
      simp [boundR, boundNat]
    exact le_trans (neg_nonpos.mpr bo.dExp_nonneg) hnonneg
  have hvNum_gt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact_mod_cast (by
      exact Nat.one_lt_pow hprecision_ne (by norm_num : 1 < (2 : Nat)))
  have hvNumReal : (bo.vNum : ℝ) = (2 : ℝ) ^ (precision : Int) := by
    rw [hvNum, Zpower_nat]
    norm_num [zpow_natCast]
  have hTotal : TotalP (EvenClosest (beta:=2) bo (2 : ℝ) precision) := by
    have h := EvenClosestTotal (beta:=2) bo 2 precision
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestTotal_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, hradix_gt, hprecision_gt, hvNum⟩
  have hRounded :
      RoundedModeP_float (beta:=2) bo 2
        (EvenClosest (beta:=2) bo (2 : ℝ) precision) := by
    have h := EvenClosestRoundedModeP_from_total (beta:=2) bo 2 (2 : ℝ) precision
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestRoundedModeP_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hTotal
  have hpBound : Fbounded (beta:=2) bo p := hRoundp.1.1
  have hqBound : Fbounded (beta:=2) bo q := hRoundq.1.1
  have htBound : Fbounded (beta:=2) bo t := hRoundt.1.1
  have runAddUnderf2 :
      ∀ f1 f2 g : FloatSpec.Core.Defs.FlocqFloat 2, ∀ e : Int,
        Closest (beta:=2) bo (2 : ℝ)
          (_root_.F2R (beta:=2) f1 + _root_.F2R (beta:=2) f2) g →
        Fbounded (beta:=2) bo f1 →
        Fbounded (beta:=2) bo f2 →
        (2 : ℝ) ^ e ≤ |_root_.F2R (beta:=2) f1| →
        (2 : ℝ) ^ e ≤ |_root_.F2R (beta:=2) f2| →
        _root_.F2R (beta:=2) g ≠ 0 →
        (2 : ℝ) ^ (e - (precision : Int) + 1) ≤
          |_root_.F2R (beta:=2) g| := by
    intro f1 f2 g e hclosest hf1 hf2 hmag1 hmag2 hgNonzero
    have hPlusExact :
        _root_.F2R (beta:=2) g ≠ 0 →
          ∃ g' : FloatSpec.Core.Defs.FlocqFloat 2,
            Fbounded' bo g' ∧
            _root_.F2R (beta:=2) g' = _root_.F2R (beta:=2) g ∧
            min f1.Fexp f2.Fexp ≤ g'.Fexp := by
      intro _
      rcases plusExactExp_from_format_payload (beta:=2) bo 2 precision rfl hradix_gt hprecision_ne
          hvNum_gt hvNum f1 f2 g hf1 hf2 hclosest with
        ⟨_r, addResidual, _hrBound, hsBound, hsVal, _hrVal, hrExp,
          hrLeS, _hsUpper⟩
      exact ⟨addResidual, hsBound, hsVal, by simpa [hrExp] using hrLeS⟩
    have h := AddExpGeUnderf2_from_plusExact_payload
      (beta:=2) bo (precision : Int) e (2 : ℝ) f1 f2 g
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, AddExpGeUnderf2_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hclosest, hf1, hf2, hmag1, hmag2, hradix_gt,
        hprecision_int_pos, hvNumReal, hf1, hf2, hPlusExact, hgNonzero⟩
  have hpGe :
      (2 : ℝ) ^ (-bo.dExp + 3 * (precision : Int) - 1) ≤
        |_root_.F2R (beta:=2) p| := by
    have h := pGeUnderf_from_total_payload (beta:=2) bo 2 precision b p
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, pGeUnderf_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, hradix_gt, hprecision_pos, hvNum, hTotal, hRoundp, hU1, hbZero⟩
  have hqGe :
      (2 : ℝ) ^ (-bo.dExp + 3 * (precision : Int) - 1) ≤
        |_root_.F2R (beta:=2) q| := by
    have h := qGeUnderf_from_total_payload (beta:=2) bo 2 precision a c q
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, qGeUnderf_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, hradix_gt, hprecision_pos, hvNum, hTotal, hRoundq, hU2, hacZero⟩
  have hqOppBound : Fbounded (beta:=2) bo (Fopp (beta:=2) q) := by
    have h := oppBounded (beta:=2) bo q
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, oppBounded_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hqBound
  have hqOppVal :
      _root_.F2R (beta:=2) (Fopp (beta:=2) q) =
        -_root_.F2R (beta:=2) q := by
    have h := Fopp_correct (beta:=2) q
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fopp_correct_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
  have hqOppGe :
      (2 : ℝ) ^ (-bo.dExp + 3 * (precision : Int) - 1) ≤
        |_root_.F2R (beta:=2) (Fopp (beta:=2) q)| := by
    simpa [hqOppVal] using hqGe
  have hRoundtClosest :
      Closest (beta:=2) bo (2 : ℝ)
        (_root_.F2R (beta:=2) p +
          _root_.F2R (beta:=2) (Fopp (beta:=2) q)) t := by
    simpa [hqOppVal, sub_eq_add_neg] using hRoundt.1
  have hvStrong :
      (2 : ℝ) ^ (-bo.dExp + 2 * (precision : Int)) ≤
        |_root_.F2R (beta:=2) v| := by
    have h :=
      runAddUnderf2 p q v (-bo.dExp + 3 * (precision : Int) - 1)
        hRoundv.1 hpBound hqBound hpGe hqGe hvZero
    have hexp :
        -bo.dExp + 3 * (precision : Int) - 1 - (precision : Int) + 1 =
          -bo.dExp + 2 * (precision : Int) := by ring
    simpa [hexp] using h
  have htStrong :
      (2 : ℝ) ^ (-bo.dExp + 2 * (precision : Int)) ≤
        |_root_.F2R (beta:=2) t| := by
    have h :=
      runAddUnderf2 p (Fopp (beta:=2) q) t
        (-bo.dExp + 3 * (precision : Int) - 1)
        hRoundtClosest hpBound hqOppBound hpGe hqOppGe htZero
    have hexp :
        -bo.dExp + 3 * (precision : Int) - 1 - (precision : Int) + 1 =
          -bo.dExp + 2 * (precision : Int) := by ring
    simpa [hexp] using h
  let uWitness : FloatSpec.Core.Defs.FlocqFloat 2 :=
    ⟨1, -bo.dExp + 2 * (precision : Int)⟩
  have huWitnessBound : Fbounded (beta:=2) bo uWitness := by
    constructor
    · simpa [uWitness] using hvNum_gt
    · dsimp [uWitness]
      omega
  have huWitnessVal :
      _root_.F2R (beta:=2) uWitness =
        (2 : ℝ) ^ (-bo.dExp + 2 * (precision : Int)) := by
    simp [uWitness, _root_.F2R, FloatSpec.Core.Defs.F2R]
  have huWitnessLeInput :
      _root_.F2R (beta:=2) uWitness ≤
        |3 * (|_root_.F2R (beta:=2) t|)| := by
    have ht_le_triple :
        |_root_.F2R (beta:=2) t| ≤
          |3 * (|_root_.F2R (beta:=2) t|)| := by
      rw [abs_mul, abs_of_pos (by norm_num : (0 : ℝ) < 3),
        abs_of_nonneg (abs_nonneg (_root_.F2R (beta:=2) t))]
      nlinarith [abs_nonneg (_root_.F2R (beta:=2) t)]
    exact le_trans (by simpa [huWitnessVal] using htStrong) ht_le_triple
  have huStrong :
      (2 : ℝ) ^ (-bo.dExp + 2 * (precision : Int)) ≤
        |_root_.F2R (beta:=2) u| := by
    have hmono := RoundAbsMonotonel_from_rounded_payload (beta:=2) bo 2
      (EvenClosest (beta:=2) bo (2 : ℝ) precision)
      (3 * |_root_.F2R (beta:=2) t|) u uWitness
    have hres :
        _root_.F2R (beta:=2) uWitness ≤
          |_root_.F2R (beta:=2) u| := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RoundAbsMonotonel_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        hmono ⟨hRounded, huWitnessBound, hRoundu, huWitnessLeInput⟩
    simpa [huWitnessVal] using hres
  have hdStrong :
      (2 : ℝ) ^ (-bo.dExp + (precision : Int)) ≤
        |_root_.F2R (beta:=2) d| := by
    by_cases hv_le_u : _root_.F2R (beta:=2) v ≤ _root_.F2R (beta:=2) u
    · have hRounddClosest :
          Closest (beta:=2) bo (2 : ℝ)
            (_root_.F2R (beta:=2) p +
              _root_.F2R (beta:=2) (Fopp (beta:=2) q)) d := by
        have hrd := hFRoundd hv_le_u
        simpa [hqOppVal, sub_eq_add_neg] using hrd.1
      have h :=
        runAddUnderf2 p (Fopp (beta:=2) q) d
          (-bo.dExp + 3 * (precision : Int) - 1)
          hRounddClosest hpBound hqOppBound hpGe hqOppGe hdZero
      have htarget :
          (2 : ℝ) ^ (-bo.dExp + (precision : Int)) ≤
            (2 : ℝ) ^ (-bo.dExp + 2 * (precision : Int)) := by
        exact zpow_right_mono₀ hradixR_ge (by omega)
      have hexp :
          -bo.dExp + 3 * (precision : Int) - 1 - (precision : Int) + 1 =
            -bo.dExp + 2 * (precision : Int) := by ring
      exact le_trans htarget (by simpa [hexp] using h)
    · have hu_lt_v : _root_.F2R (beta:=2) u < _root_.F2R (beta:=2) v :=
        lt_of_not_ge hv_le_u
      have hsBound : Fbounded (beta:=2) bo s := (hSRounds hu_lt_v).1.1
      have h := AddExpGe1Underf2_from_generic_radix_payload (beta:=2) bo 2 precision rfl hradix_gt
        hprecision_ne hvNum_gt hvNum t s d
        (-bo.dExp + 2 * (precision : Int)) (hSRoundd hu_lt_v).1
        htBound hsBound htStrong (by omega) hdZero
      have hexp :
          -bo.dExp + 2 * (precision : Int) - (precision : Int) =
            -bo.dExp + (precision : Int) := by ring
      simpa [hexp] using h
  have hbProd :
      (2 : ℝ) ^ (-bo.dExp + 2 * (precision : Int) - 1) ≤
        |_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b| := by
    have hU1mag : (2 : ℝ) ^ (-bo.dExp + 3 * (precision : Int) - 1) ≤
        |_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b| :=
      hU1.resolve_left hbZero
    have hpow :
        (2 : ℝ) ^ (-bo.dExp + 2 * (precision : Int) - 1) ≤
          (2 : ℝ) ^ (-bo.dExp + 3 * (precision : Int) - 1) := by
      exact zpow_right_mono₀ hradixR_ge (by omega)
    exact le_trans hpow hU1mag
  have hacProd :
      (2 : ℝ) ^ (-bo.dExp + 2 * (precision : Int) - 1) ≤
        |_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c| := by
    have hU2mag : (2 : ℝ) ^ (-bo.dExp + 3 * (precision : Int) - 1) ≤
        |_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c| :=
      hU2.resolve_left hacZero
    have hpow :
        (2 : ℝ) ^ (-bo.dExp + 2 * (precision : Int) - 1) ≤
          (2 : ℝ) ^ (-bo.dExp + 3 * (precision : Int) - 1) := by
      exact zpow_right_mono₀ hradixR_ge (by omega)
    exact le_trans hpow hU2mag
  have huLow :
      (2 : ℝ) ^ (-bo.dExp + (precision : Int) - 1) ≤
        |_root_.F2R (beta:=2) u| := by
    have hpow :
        (2 : ℝ) ^ (-bo.dExp + (precision : Int) - 1) ≤
          (2 : ℝ) ^ (-bo.dExp + 2 * (precision : Int)) := by
      exact zpow_right_mono₀ hradixR_ge (by omega)
    exact le_trans hpow huStrong
  have hvLow :
      (2 : ℝ) ^ (-bo.dExp + (precision : Int) - 1) ≤
        |_root_.F2R (beta:=2) v| := by
    have hpow :
        (2 : ℝ) ^ (-bo.dExp + (precision : Int) - 1) ≤
          (2 : ℝ) ^ (-bo.dExp + 2 * (precision : Int)) := by
      exact zpow_right_mono₀ hradixR_ge (by omega)
    exact le_trans hpow hvStrong
  have htLow :
      (2 : ℝ) ^ (-bo.dExp + (precision : Int)) ≤
        |_root_.F2R (beta:=2) t| := by
    have hpow :
        (2 : ℝ) ^ (-bo.dExp + (precision : Int)) ≤
          (2 : ℝ) ^ (-bo.dExp + 2 * (precision : Int)) := by
      exact zpow_right_mono₀ hradixR_ge (by omega)
    exact le_trans hpow htStrong
  exact ⟨hbProd, hacProd, hdStrong, huLow, hvLow, htLow⟩

noncomputable def discri16_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b c p q t d u v s : FloatSpec.Core.Defs.FlocqFloat beta)
    (dp dq : ℝ) : Unit :=
  ()

/-- Coq discriminant theorem `discri16`, factored at the final case split.

Upstream first dispatches zero/special cases, returning `F2R d = 0` when the
rounded discriminant vanishes.  In the non-special branches it derives the
underflow and normalization payload consumed by `discri15`. -/
theorem discri16_from_normalization_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b c p q t d u v s : FloatSpec.Core.Defs.FlocqFloat beta)
    (dp dq : ℝ) :
    ⦃⌜_root_.F2R (beta:=beta) d = 0 ∨
        discri15_precondition (beta:=beta) bo radix precision
          a b c p q t d u v s dp dq⌝⦄
    (pure (discri16_check (beta:=beta) bo radix precision a b c p q t d u v s dp dq) :
      Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) d = 0 ∨
        |_root_.F2R (beta:=beta) d -
            (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b -
              _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| ≤
          2 * Fulp (beta:=beta) bo radix precision d⌝⦄ := by
  intro hpre
  simp only [wp, PostCond.noThrow, pure, discri16_check, Id.run, ULift.up_down]
  rcases hpre with hzero | hpayload
  · exact Or.inl hzero
  · right
    have h := discri15_from_normalization_payload (beta:=beta) bo radix precision a b c p q t d u v s dp dq
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri15_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h hpayload

noncomputable def discri_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b c p q t d u v s : FloatSpec.Core.Defs.FlocqFloat beta)
    (dp dq : ℝ) : Unit :=
  ()

/-- Coq final lower discriminant theorem `discri`.

The Coq proof handles zero and special cases directly, then delegates the
non-special branch to `discri15`.  The restored `discri16` exposes that final
case split; the normality hypothesis on `d` rules out the zero alternative. -/
theorem discri_from_discri15_payload {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b c p q t d u v s : FloatSpec.Core.Defs.FlocqFloat beta)
    (dp dq : ℝ) :
    ⦃⌜1 < beta ∧ 0 < bo.vNum ∧
        Fnormal (beta:=beta) radix bo d ∧
        (_root_.F2R (beta:=beta) d = 0 ∨
          discri15_precondition (beta:=beta) bo radix precision
            a b c p q t d u v s dp dq)⌝⦄
    (pure (discri_check (beta:=beta) bo radix precision a b c p q t d u v s dp dq) :
      Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) d -
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| ≤
        2 * Fulp (beta:=beta) bo radix precision d⌝⦄ := by
  intro hpre
  rcases hpre with ⟨hbeta, hvNum_pos, hd_normal, hpayload⟩
  simp only [wp, PostCond.noThrow, pure, discri_check, Id.run, ULift.up_down]
  have h := discri16_from_normalization_payload (beta:=beta) bo radix precision a b c p q t d u v s dp dq
  have hsplit :
      _root_.F2R (beta:=beta) d = 0 ∨
        |_root_.F2R (beta:=beta) d -
            (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b -
              _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| ≤
          2 * Fulp (beta:=beta) bo radix precision d := by
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri16_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h hpayload
  rcases hsplit with hzero | hbound
  · have hd_zero_float : is_Fzero d := by
      have hz := is_Fzero_rep2 (beta:=beta) d
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, is_Fzero_rep2_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using hz ⟨hbeta, hzero⟩
    have hd_not_zero : ¬ is_Fzero d := by
      rcases hd_normal with ⟨hbounded_d, hmant⟩
      have hnz := FnormalNotZero_from_expanded_normal_payload (beta:=beta) bo radix d radix hvNum_pos hmant
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalNotZero_check,
        ULift.down_up, Int.cast_ofNat] using hnz ⟨hbounded_d, hmant⟩
    exact False.elim (hd_not_zero hd_zero_float)
  · exact hbound

/-- The minimum ulp carried by a zero-valued float is no larger than the ulp
of any bounded float.  This packages the normalization/exponent argument used
in the two zero branches of Coq Discriminant5B. -/
private theorem zero_float_ulp_le_bounded
    (bo : Fbound_skel) (precision : Nat)
    (z q : FloatSpec.Core.Defs.FlocqFloat 2)
    (hprecision : 1 < precision)
    (hvNum : bo.vNum = Zpower_nat 2 precision)
    (hz : _root_.F2R (beta:=2) z = 0)
    (hqBound : Fbounded (beta:=2) bo q) :
    Fulp (beta:=2) bo 2 precision z ≤
      Fulp (beta:=2) bo 2 precision q := by
  have hzRep : is_Fzero z := by
    have h := is_Fzero_rep2 (beta:=2) z
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, is_Fzero_rep2_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨by decide, hz⟩
  have hzUlp : Fulp (beta:=2) bo 2 precision z = (2 : ℝ) ^ (-bo.dExp) := by
    have h := Fulp_zero (beta:=2) 2 bo precision z
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fulp_zero_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hzRep
  have hqNormBound : Fbounded (beta:=2) bo
      (Fnormalize (beta:=2) 2 bo precision q) := by
    have h := FnormalizeBounded (beta:=2) 2 bo precision q
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeBounded_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hqBound, hqBound, by omega, by decide, hvNum⟩
  rw [hzUlp]
  dsimp [Fulp]
  exact zpow_right_mono₀ (by norm_num) hqNormBound.2

private theorem closest_to_representable_is_exact
    (bo : Fbound_skel) (r : ℝ)
    (f g : FloatSpec.Core.Defs.FlocqFloat 2)
    (hf : Closest (beta:=2) bo (2 : ℝ) r f)
    (hgBound : Fbounded (beta:=2) bo g)
    (hgVal : _root_.F2R (beta:=2) g = r) :
    _root_.F2R (beta:=2) f = r := by
  have hdist := hf.2 g hgBound
  rw [hgVal, sub_self, abs_zero] at hdist
  have hz : |_root_.F2R (beta:=2) f - r| = 0 :=
    le_antisymm hdist (abs_nonneg _)
  exact sub_eq_zero.mp (abs_eq_zero.mp hz)

/- Coq Discriminant7 `discri16`.  The five special cases are discharged from
the actual rounding equations; only the final nonzero branch calls `discri15`. -/
set_option maxHeartbeats 2000000 in
theorem discri16 {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b c p q t dp dq s d u v : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = 2 ∧ radix = 2 ∧ 1 < precision ∧ 4 ≤ precision ∧
        bo.vNum = Zpower_nat radix precision ∧
        Fbounded (beta:=beta) bo a ∧ Fbounded (beta:=beta) bo b ∧
        Fbounded (beta:=beta) bo c ∧
        (_root_.F2R (beta:=beta) u < _root_.F2R (beta:=beta) v →
          Fbounded (beta:=beta) bo dp) ∧
        (_root_.F2R (beta:=beta) u < _root_.F2R (beta:=beta) v →
          Fbounded (beta:=beta) bo dq) ∧
        (_root_.F2R (beta:=beta) b = 0 ∨
          (radix : ℝ) ^ (-bo.dExp + 3 * (precision : Int) - 1) ≤
            |_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b|) ∧
        (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c = 0 ∨
          (radix : ℝ) ^ (-bo.dExp + 3 * (precision : Int) - 1) ≤
            |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c|) ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b) p ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c) q ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q) t ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (3 * |_root_.F2R (beta:=beta) t|) u ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) v ∧
        (_root_.F2R (beta:=beta) v ≤ _root_.F2R (beta:=beta) u →
          EvenClosest (beta:=beta) bo (radix : ℝ) precision
            (_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q) d) ∧
        _root_.F2R (beta:=beta) dp =
          _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b -
          _root_.F2R (beta:=beta) p ∧
        _root_.F2R (beta:=beta) dq =
          _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c -
          _root_.F2R (beta:=beta) q ∧
        (_root_.F2R (beta:=beta) u < _root_.F2R (beta:=beta) v →
          EvenClosest (beta:=beta) bo (radix : ℝ) precision
            (_root_.F2R (beta:=beta) dp - _root_.F2R (beta:=beta) dq) s) ∧
        (_root_.F2R (beta:=beta) u < _root_.F2R (beta:=beta) v →
          EvenClosest (beta:=beta) bo (radix : ℝ) precision
            (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) s) d)⌝⦄
    (pure (discri16_check (beta:=beta) bo radix precision a b c p q t d u v s
      (_root_.F2R (beta:=beta) dp) (_root_.F2R (beta:=beta) dq)) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) d = 0 ∨
        |_root_.F2R (beta:=beta) d -
            (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b -
              _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| ≤
          2 * Fulp (beta:=beta) bo radix precision d⌝⦄ := by
  intro hpre
  have hcasesRaw := «cases» (beta:=beta) bo radix precision
    a b c p q t dp dq s d u v
  have hcases : _root_.F2R (beta:=beta) b = 0 ∨
      _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c = 0 ∨
      _root_.F2R (beta:=beta) d = 0 ∨ _root_.F2R (beta:=beta) v = 0 ∨
      _root_.F2R (beta:=beta) t = 0 ∨
      ((radix : ℝ) ^ (-bo.dExp + 2 * (precision : Int) - 1) ≤
          |_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b| ∧
       (radix : ℝ) ^ (-bo.dExp + 2 * (precision : Int) - 1) ≤
          |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c| ∧
       (radix : ℝ) ^ (-bo.dExp + (precision : Int)) ≤
          |_root_.F2R (beta:=beta) d| ∧
       (radix : ℝ) ^ (-bo.dExp + (precision : Int) - 1) ≤
          |_root_.F2R (beta:=beta) u| ∧
       (radix : ℝ) ^ (-bo.dExp + (precision : Int) - 1) ≤
          |_root_.F2R (beta:=beta) v| ∧
       (radix : ℝ) ^ (-bo.dExp + (precision : Int)) ≤
          |_root_.F2R (beta:=beta) t|) := by
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, cases_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using hcasesRaw hpre
  rcases hpre with
    ⟨hbeta, hradix, hprecision, hprecision4, hvNum, haBound, hbBound,
      hcBound, hdpBoundIf, hdqBoundIf, hU1, hU2, hRoundp, hRoundq, hRoundt,
      hRoundu, hRoundv, hFRoundd, hdpEq, hdqEq, hSRounds, hSRoundd⟩
  subst beta
  subst radix
  simp only [wp, PostCond.noThrow, pure, discri16_check, Id.run, ULift.up_down]
  have hprecisionNe : precision ≠ 0 := by omega
  have hvNumGt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact_mod_cast (Nat.one_lt_pow hprecisionNe (by norm_num : 1 < (2 : Nat)))
  have hpBound : Fbounded (beta:=2) bo p := hRoundp.1.1
  have hqBound : Fbounded (beta:=2) bo q := hRoundq.1.1
  have htBound : Fbounded (beta:=2) bo t := hRoundt.1.1
  have huBound : Fbounded (beta:=2) bo u := hRoundu.1.1
  have hvBound : Fbounded (beta:=2) bo v := hRoundv.1.1
  have hdBound : Fbounded (beta:=2) bo d := by
    by_cases hvLeU : _root_.F2R (beta:=2) v ≤ _root_.F2R (beta:=2) u
    · exact (hFRoundd hvLeU).1.1
    · exact (hSRoundd (lt_of_not_ge hvLeU)).1.1
  have hRounded : RoundedModeP (beta:=2) bo
      (EvenClosest (beta:=2) bo (2 : ℝ) precision) := by
    have h := EvenClosestRoundedModeP (beta:=2) bo 2 precision
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestRoundedModeP_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨rfl, by decide, hprecision, hvNum⟩
  have hOppVal (f : FloatSpec.Core.Defs.FlocqFloat 2) :
      _root_.F2R (beta:=2) (Fopp f) = -_root_.F2R (beta:=2) f := by
    have h := Fopp_correct (beta:=2) f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fopp_correct_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
  have hOppBound (f : FloatSpec.Core.Defs.FlocqFloat 2)
      (hf : Fbounded (beta:=2) bo f) : Fbounded (beta:=2) bo (Fopp f) := by
    have h := oppBounded (beta:=2) bo f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, oppBounded_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hf
  have hBoundExp : ∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=2) 2 r).Fexp := by
    intro r
    exact le_trans (neg_nonpos.mpr bo.dExp_nonneg) (by simp [boundR, boundNat])
  have hPlusZero (f g r : FloatSpec.Core.Defs.FlocqFloat 2)
      (hf : Fbounded (beta:=2) bo f) (hg : Fbounded (beta:=2) bo g)
      (hr : Closest (beta:=2) bo (2 : ℝ)
        (_root_.F2R (beta:=2) f + _root_.F2R (beta:=2) g) r)
      (hz : _root_.F2R (beta:=2) r = 0) :
      _root_.F2R (beta:=2) f + _root_.F2R (beta:=2) g = 0 := by
    have h := plusExactR0_from_format_payload (beta:=2) bo 2 precision rfl (by decide)
      hprecisionNe hvNumGt hvNum f g r hf hg hr hz
    linarith
  have hUlpComp (f g : FloatSpec.Core.Defs.FlocqFloat 2)
      (hf : Fbounded (beta:=2) bo f) (hg : Fbounded (beta:=2) bo g)
      (heq : _root_.F2R (beta:=2) f = _root_.F2R (beta:=2) g) :
      Fulp (beta:=2) bo 2 precision f = Fulp (beta:=2) bo 2 precision g := by
    have h := FulpComp (beta:=2) 2 bo precision f g
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpComp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hf, hg, heq, rfl, by decide, hprecisionNe, hvNum⟩
  by_cases hdZero : _root_.F2R (beta:=2) d = 0
  · exact Or.inl hdZero
  right
  by_cases hbZero : _root_.F2R (beta:=2) b = 0
  · have hpZero : _root_.F2R (beta:=2) p = 0 := by
      have h := ClosestZero2 (beta:=2) bo (2 : ℝ)
        (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b) p
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestZero2_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hRoundp.1, by rw [hbZero]; ring⟩
    have htVal : _root_.F2R (beta:=2) t = -_root_.F2R (beta:=2) q := by
      have h := closest_to_representable_is_exact bo
        (_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q) t (Fopp q)
        hRoundt.1 (hOppBound q hqBound) (by rw [hOppVal, hpZero]; ring)
      simpa [hpZero] using h
    have hvLeU : _root_.F2R (beta:=2) v ≤ _root_.F2R (beta:=2) u := by
      have hinput : _root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q ≤
          3 * |_root_.F2R (beta:=2) t| := by
        rw [hpZero, zero_add, htVal, abs_neg]
        nlinarith [le_abs_self (_root_.F2R (beta:=2) q),
          abs_nonneg (_root_.F2R (beta:=2) q)]
      have h := EvenClosestMonotone2 (beta:=2) bo 2 precision
        (_root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q)
        (3 * |_root_.F2R (beta:=2) t|) v u
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestMonotone2_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨rfl, by decide, hprecision, hvNum, hinput, hRoundv, hRoundu⟩
    have hdVal : _root_.F2R (beta:=2) d = -_root_.F2R (beta:=2) q := by
      have h := closest_to_representable_is_exact bo
        (_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q) d (Fopp q)
        (hFRoundd hvLeU).1 (hOppBound q hqBound) (by rw [hOppVal, hpZero]; ring)
      simpa [hpZero] using h
    have hqErr : 2 * |_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c -
        _root_.F2R (beta:=2) q| ≤ Fulp (beta:=2) bo 2 precision q := by
      have h := ClosestUlp (beta:=2) bo 2 precision
        (_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c) q
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hRoundq.1, rfl, by decide, hprecisionNe, hvNum⟩
    have hdUlpQ : Fulp (beta:=2) bo 2 precision d =
        Fulp (beta:=2) bo 2 precision q :=
      (hUlpComp d (Fopp q) hdBound (hOppBound q hqBound)
        (by rw [hdVal, hOppVal])).trans (by
          have h := Fulp_Fopp (beta:=2) bo 2 precision q
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fulp_Fopp_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro)
    rw [hdVal, hbZero, hdUlpQ]
    ring_nf at hqErr ⊢
    nlinarith [abs_nonneg
      (_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c -
        _root_.F2R (beta:=2) q)]
  by_cases hacZero : _root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c = 0
  · have hqZero : _root_.F2R (beta:=2) q = 0 := by
      have h := ClosestZero2 (beta:=2) bo (2 : ℝ)
        (_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c) q
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestZero2_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hRoundq.1, hacZero⟩
    have htVal : _root_.F2R (beta:=2) t = _root_.F2R (beta:=2) p := by
      have h := closest_to_representable_is_exact bo
        (_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q) t p
        hRoundt.1 hpBound (by rw [hqZero]; ring)
      simpa [hqZero] using h
    have hvLeU : _root_.F2R (beta:=2) v ≤ _root_.F2R (beta:=2) u := by
      have hinput : _root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q ≤
          3 * |_root_.F2R (beta:=2) t| := by
        rw [hqZero, add_zero, htVal]
        nlinarith [le_abs_self (_root_.F2R (beta:=2) p),
          abs_nonneg (_root_.F2R (beta:=2) p)]
      have h := EvenClosestMonotone2 (beta:=2) bo 2 precision
        (_root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q)
        (3 * |_root_.F2R (beta:=2) t|) v u
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, EvenClosestMonotone2_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨rfl, by decide, hprecision, hvNum, hinput, hRoundv, hRoundu⟩
    have hdVal : _root_.F2R (beta:=2) d = _root_.F2R (beta:=2) p := by
      have h := closest_to_representable_is_exact bo
        (_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q) d p
        (hFRoundd hvLeU).1 hpBound (by rw [hqZero]; ring)
      simpa [hqZero] using h
    have hpErr : 2 * |_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b -
        _root_.F2R (beta:=2) p| ≤ Fulp (beta:=2) bo 2 precision p := by
      have h := ClosestUlp (beta:=2) bo 2 precision
        (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b) p
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hRoundp.1, rfl, by decide, hprecisionNe, hvNum⟩
    have hdUlpP := hUlpComp d p hdBound hpBound hdVal
    rw [hdVal, hacZero, hdUlpP]
    ring_nf at hpErr ⊢
    have habs :
        |-_root_.F2R (beta:=2) b ^ 2 + _root_.F2R (beta:=2) p| =
          |_root_.F2R (beta:=2) b ^ 2 - _root_.F2R (beta:=2) p| := by
      rw [show -_root_.F2R (beta:=2) b ^ 2 + _root_.F2R (beta:=2) p =
        -(_root_.F2R (beta:=2) b ^ 2 - _root_.F2R (beta:=2) p) by ring,
        abs_neg]
    rw [habs]
    nlinarith [abs_nonneg
      (_root_.F2R (beta:=2) b ^ 2 -
        _root_.F2R (beta:=2) p)]
  by_cases hvZero : _root_.F2R (beta:=2) v = 0
  · have hsumZero := hPlusZero p q v hpBound hqBound hRoundv.1 hvZero
    have hpNonneg : 0 ≤ _root_.F2R (beta:=2) p := by
      have h := RleRoundedR0 (beta:=2) bo
        (EvenClosest (beta:=2) bo (2 : ℝ) precision) p
        (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b)
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RleRoundedR0_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hRounded, hRoundp, mul_self_nonneg _⟩
    have huNonneg : 0 ≤ _root_.F2R (beta:=2) u := by
      have h := RleRoundedR0 (beta:=2) bo
        (EvenClosest (beta:=2) bo (2 : ℝ) precision) u
        (3 * |_root_.F2R (beta:=2) t|)
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RleRoundedR0_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hRounded, hRoundu, mul_nonneg (by norm_num) (abs_nonneg _)⟩
    have hvLeU : _root_.F2R (beta:=2) v ≤ _root_.F2R (beta:=2) u := by
      rw [hvZero]
      exact huNonneg
    let p2 : FloatSpec.Core.Defs.FlocqFloat 2 := ⟨p.Fnum, p.Fexp + 1⟩
    have hp2Bound : Fbounded (beta:=2) bo p2 := by
      constructor
      · simpa [p2] using hpBound.1
      · dsimp [p2]
        exact le_trans hpBound.2 (by omega)
    have hp2Val : _root_.F2R (beta:=2) p2 =
        2 * _root_.F2R (beta:=2) p := by
      simp only [p2, _root_.F2R, FloatSpec.Core.Defs.F2R,
        FloatSpec.Core.Defs.FlocqFloat.Fnum,
        FloatSpec.Core.Defs.FlocqFloat.Fexp]
      rw [show p.Fexp + 1 = 1 + p.Fexp by ring,
        zpow_add₀ (by norm_num), zpow_one]
      ring
    have hp2Input : _root_.F2R (beta:=2) p2 =
        _root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q := by
      rw [hp2Val]
      linarith
    have hdVal : _root_.F2R (beta:=2) d = 2 * _root_.F2R (beta:=2) p := by
      have h := closest_to_representable_is_exact bo
        (_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q) d p2
        (hFRoundd hvLeU).1 hp2Bound hp2Input
      calc
        _root_.F2R (beta:=2) d =
            _root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q := h
        _ = 2 * _root_.F2R (beta:=2) p := by linarith
    have hpErr : 2 * |_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b -
        _root_.F2R (beta:=2) p| ≤ Fulp (beta:=2) bo 2 precision p := by
      have h := ClosestUlp (beta:=2) bo 2 precision
        (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b) p
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hRoundp.1, rfl, by decide, hprecisionNe, hvNum⟩
    have hqErr : 2 * |_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c -
        _root_.F2R (beta:=2) q| ≤ Fulp (beta:=2) bo 2 precision q := by
      have h := ClosestUlp (beta:=2) bo 2 precision
        (_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c) q
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hRoundq.1, rfl, by decide, hprecisionNe, hvNum⟩
    have hqOppP : _root_.F2R (beta:=2) q =
        _root_.F2R (beta:=2) (Fopp p) := by rw [hOppVal]; linarith
    have hqUlpP : Fulp (beta:=2) bo 2 precision q =
        Fulp (beta:=2) bo 2 precision p :=
      (hUlpComp q (Fopp p) hqBound (hOppBound p hpBound) hqOppP).trans (by
        have h := Fulp_Fopp (beta:=2) bo 2 precision p
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fulp_Fopp_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro)
    have hpLeD : _root_.F2R (beta:=2) p ≤ _root_.F2R (beta:=2) d := by
      rw [hdVal]
      linarith
    have hpUlpLeD : Fulp (beta:=2) bo 2 precision p ≤
        Fulp (beta:=2) bo 2 precision d := by
      have h := LeFulpPos (beta:=2) 2 bo precision p d
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, LeFulpPos_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hpBound, hdBound, hpNonneg, hpLeD, rfl, by decide,
          hprecisionNe, hvNum⟩
    have htri := abs_add_le
      (-(_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b -
        _root_.F2R (beta:=2) p))
      (_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c -
        _root_.F2R (beta:=2) q)
    rw [hdVal]
    have hrewrite : 2 * _root_.F2R (beta:=2) p -
          (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b -
            _root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c) =
        -(_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b -
          _root_.F2R (beta:=2) p) +
        (_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c -
          _root_.F2R (beta:=2) q) := by linarith
    rw [hrewrite]
    calc
      _ ≤ |_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b -
              _root_.F2R (beta:=2) p| +
            |_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c -
              _root_.F2R (beta:=2) q| := by simpa only [abs_neg, Int.cast_ofNat] using htri
      _ ≤ (Fulp (beta:=2) bo 2 precision p +
            Fulp (beta:=2) bo 2 precision q) / 2 := by nlinarith
      _ = Fulp (beta:=2) bo 2 precision p := by rw [hqUlpP]; ring
      _ ≤ Fulp (beta:=2) bo 2 precision d := hpUlpLeD
      _ ≤ 2 * Fulp (beta:=2) bo 2 precision d := by
        have hdUlpNonneg : 0 ≤ Fulp (beta:=2) bo 2 precision d := by
          dsimp [Fulp]
          positivity
        linarith
  by_cases htZero : _root_.F2R (beta:=2) t = 0
  · have hdiffZero : _root_.F2R (beta:=2) p -
        _root_.F2R (beta:=2) q = 0 := by
      have h := hPlusZero p (Fopp q) t hpBound (hOppBound q hqBound)
        (by simpa [hOppVal, sub_eq_add_neg] using hRoundt.1) htZero
      simpa [hOppVal, sub_eq_add_neg] using h
    have huZero : _root_.F2R (beta:=2) u = 0 := by
      have h := ClosestZero2 (beta:=2) bo (2 : ℝ)
        (3 * |_root_.F2R (beta:=2) t|) u
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestZero2_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hRoundu.1, by rw [htZero, abs_zero]; ring⟩
    have hvNonneg : 0 ≤ _root_.F2R (beta:=2) v := by
      have hpNonneg : 0 ≤ _root_.F2R (beta:=2) p := by
        have h := RleRoundedR0 (beta:=2) bo
          (EvenClosest (beta:=2) bo (2 : ℝ) precision) p
          (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b)
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RleRoundedR0_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hRounded, hRoundp, mul_self_nonneg _⟩
      have h := RleRoundedR0 (beta:=2) bo
        (EvenClosest (beta:=2) bo (2 : ℝ) precision) v
        (_root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q)
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RleRoundedR0_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h
        ⟨hRounded, hRoundv, by nlinarith⟩
    have huLtV : _root_.F2R (beta:=2) u < _root_.F2R (beta:=2) v := by
      rw [huZero]
      exact lt_of_le_of_ne hvNonneg (Ne.symm hvZero)
    have hsBound : Fbounded (beta:=2) bo s := (hSRounds huLtV).1.1
    have hdVal : _root_.F2R (beta:=2) d = _root_.F2R (beta:=2) s := by
      have h := closest_to_representable_is_exact bo
        (_root_.F2R (beta:=2) t + _root_.F2R (beta:=2) s) d s
        (hSRoundd huLtV).1 hsBound (by rw [htZero]; ring)
      simpa [htZero] using h
    have hsErr : 2 * |(_root_.F2R (beta:=2) dp - _root_.F2R (beta:=2) dq) -
        _root_.F2R (beta:=2) s| ≤ Fulp (beta:=2) bo 2 precision s := by
      have h := ClosestUlp (beta:=2) bo 2 precision
        (_root_.F2R (beta:=2) dp - _root_.F2R (beta:=2) dq) s
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨(hSRounds huLtV).1, rfl, by decide, hprecisionNe, hvNum⟩
    have hdUlpS := hUlpComp d s hdBound hsBound hdVal
    rw [hdVal, hdUlpS]
    have hrewrite : _root_.F2R (beta:=2) s -
          (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b -
            _root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c) =
        -((_root_.F2R (beta:=2) dp - _root_.F2R (beta:=2) dq) -
          _root_.F2R (beta:=2) s) := by
      rw [hdpEq, hdqEq]
      linarith
    rw [hrewrite, abs_neg]
    nlinarith [abs_nonneg
      ((_root_.F2R (beta:=2) dp - _root_.F2R (beta:=2) dq) -
        _root_.F2R (beta:=2) s)]
  rcases hcases with hb | hac | hd | hv | ht | hmags
  · exact False.elim (hbZero hb)
  · exact False.elim (hacZero hac)
  · exact False.elim (hdZero hd)
  · exact False.elim (hvZero hv)
  · exact False.elim (htZero ht)
  · rcases hmags with ⟨hbMag, hacMag, hdMag, huMag, hvMag, htMag⟩
    have h := discri15 (beta:=2) bo 2 precision a b c p q t dp dq s d u v
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri15_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h
      ⟨rfl, rfl, hprecision, hprecision4, hvNum, haBound, hbBound, hcBound,
        hdpBoundIf, hdqBoundIf, hbMag, hacMag, hdMag, huMag, hvMag, htMag,
        hRoundp, hRoundq, hRoundt, hRoundu, hRoundv, hFRoundd, hdpEq, hdqEq,
        hSRounds, hSRoundd⟩

noncomputable def discri_source_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b c p q t dp dq s d : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq Discriminant5B `discri`.  The zero-product and zero-residual cases are
discharged internally; the nonzero normal branch delegates to the restored
source-shaped `discri1`, `discri3`, or `discri9` theorem. -/
theorem discri {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a b c p q t dp dq s d : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜ beta = 2 ∧ radix = 2 ∧ 1 < precision ∧
        bo.vNum = Zpower_nat radix precision ∧
        Fbounded (beta:=beta) bo a ∧ Fbounded (beta:=beta) bo b ∧
        Fbounded (beta:=beta) bo c ∧ Fbounded (beta:=beta) bo p ∧
        Fbounded (beta:=beta) bo q ∧ Fbounded (beta:=beta) bo d ∧
        Fbounded (beta:=beta) bo t ∧ Fbounded (beta:=beta) bo s ∧
        Fbounded (beta:=beta) bo dp ∧ Fbounded (beta:=beta) bo dq ∧
        -bo.dExp ≤ d.Fexp - 1 ∧ -bo.dExp ≤ t.Fexp - 1 ∧
        (radix : ℝ) ^ (-bo.dExp + 2 * (precision : Int) - 1) ≤
          |_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b| ∧
        (radix : ℝ) ^ (-bo.dExp + 2 * (precision : Int) - 1) ≤
          |_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c| ∧
        (_root_.F2R (beta:=beta) p = 0 ∨
          Fnormal (beta:=beta) radix bo p) ∧
        (_root_.F2R (beta:=beta) q = 0 ∨
          Fnormal (beta:=beta) radix bo q) ∧
        (3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| <
            _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q →
          _root_.F2R (beta:=beta) s = 0 ∨
            Fnormal (beta:=beta) radix bo s) ∧
        Fnormal (beta:=beta) radix bo d ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b) p ∧
        EvenClosest (beta:=beta) bo (radix : ℝ) precision
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c) q ∧
        (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q ≤
            3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| →
          EvenClosest (beta:=beta) bo (radix : ℝ) precision
            (_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q) d) ∧
        (3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| <
            _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q →
          EvenClosest (beta:=beta) bo (radix : ℝ) precision
            (_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q) t) ∧
        (3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| <
            _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q →
          _root_.F2R (beta:=beta) dp =
            _root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b -
              _root_.F2R (beta:=beta) p) ∧
        (3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| <
            _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q →
          _root_.F2R (beta:=beta) dq =
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c -
              _root_.F2R (beta:=beta) q) ∧
        (3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| <
            _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q →
          EvenClosest (beta:=beta) bo (radix : ℝ) precision
            (_root_.F2R (beta:=beta) dp - _root_.F2R (beta:=beta) dq) s) ∧
        (3 * |_root_.F2R (beta:=beta) p - _root_.F2R (beta:=beta) q| <
            _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q →
          EvenClosest (beta:=beta) bo (radix : ℝ) precision
            (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) s) d)⌝⦄
    (pure (discri_source_check (beta:=beta) bo radix precision
      a b c p q t dp dq s d) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) d -
          (_root_.F2R (beta:=beta) b * _root_.F2R (beta:=beta) b -
            _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) c)| ≤
        2 * Fulp (beta:=beta) bo radix precision d⌝⦄ := by
  intro hpre
  rcases hpre with
    ⟨hbeta, hradix, hprecision, hvNum, haBound, hbBound, hcBound, hpBound,
      hqBound, hdBound, htBound, hsBound, hdpBound, hdqBound, hU0, hU1,
      hU2, hU3, hpZeroOrNormal, hqZeroOrNormal, hsZeroOrNormal, hdNormal,
      hRoundp, hRoundq, hFirstRound, hRoundtIf, hdpEqIf, hdqEqIf, hRoundsIf,
      hRounddIf⟩
  subst beta
  subst radix
  simp only [wp, PostCond.noThrow, pure, discri_source_check, Id.run,
    ULift.up_down]
  change |_root_.F2R (beta:=2) d -
        (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b -
          _root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c)| ≤
      2 * Fulp (beta:=2) bo 2 precision d
  have hprecisionNe : precision ≠ 0 := by omega
  have hvNumPos : 0 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    positivity
  have hOppVal (x : FloatSpec.Core.Defs.FlocqFloat 2) :
      _root_.F2R (beta:=2) (Fopp x) = -_root_.F2R (beta:=2) x := by
    have h := Fopp_correct (beta:=2) x
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fopp_correct_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
  rcases hpZeroOrNormal with hpZero | hpNormal
  · have hFirst : _root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q ≤
          3 * |_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q| := by
        rw [hpZero, zero_add, zero_sub]
        rw [abs_neg]
        calc
          _root_.F2R (beta:=2) q ≤ |_root_.F2R (beta:=2) q| := le_abs_self _
          _ ≤ 3 * |_root_.F2R (beta:=2) q| := by
            nlinarith [abs_nonneg (_root_.F2R (beta:=2) q)]
    have hqOppBound : Fbounded (beta:=2) bo (Fopp q) := by
      have h := oppBounded (beta:=2) bo q
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, oppBounded_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h hqBound
    have hdVal : _root_.F2R (beta:=2) d = -_root_.F2R (beta:=2) q := by
      have h := closest_to_representable_is_exact bo
        (_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q) d (Fopp q)
        (hFirstRound hFirst).1 hqOppBound
        (by rw [hOppVal, hpZero, zero_sub])
      simpa [hpZero] using h
    have hpErr : 2 * |_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b| ≤
        Fulp (beta:=2) bo 2 precision p := by
      have h := ClosestUlp (beta:=2) bo 2 precision
        (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b) p
      have hraw := h ⟨hRoundp.1, rfl, by decide, hprecisionNe, hvNum⟩
      have hbound :
          2 * |_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b -
                _root_.F2R (beta:=2) p| ≤ Fulp (beta:=2) bo 2 precision p := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using hraw
      simpa [hpZero] using hbound
    have hqErr : 2 * |_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c -
          _root_.F2R (beta:=2) q| ≤ Fulp (beta:=2) bo 2 precision q := by
      have h := ClosestUlp (beta:=2) bo 2 precision
        (_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c) q
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hRoundq.1, rfl, by decide, hprecisionNe, hvNum⟩
    have hpUlpLeQ := zero_float_ulp_le_bounded bo precision p q hprecision
      hvNum hpZero hqBound
    have hdUlpQ : Fulp (beta:=2) bo 2 precision d =
        Fulp (beta:=2) bo 2 precision q := by
      have h := FulpComp (beta:=2) 2 bo precision d (Fopp q)
      have hcomp : Fulp (beta:=2) bo 2 precision d =
          Fulp (beta:=2) bo 2 precision (Fopp q) := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpComp_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          h ⟨hdBound, hqOppBound, by rw [hdVal, hOppVal], rfl, by decide,
            hprecisionNe, hvNum⟩
      have hopp := Fulp_Fopp (beta:=2) bo 2 precision q
      have hopp' : Fulp (beta:=2) bo 2 precision (Fopp q) =
          Fulp (beta:=2) bo 2 precision q := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fulp_Fopp_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using hopp True.intro
      exact hcomp.trans hopp'
    have htri := abs_add_le
      (_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c -
        _root_.F2R (beta:=2) q)
      (-(_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b))
    rw [hdVal]
    have hrewrite : -_root_.F2R (beta:=2) q -
          (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b -
            _root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c) =
        (_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c -
          _root_.F2R (beta:=2) q) +
            (-(_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b)) := by ring
    rw [hrewrite]
    rw [abs_neg] at htri
    rw [hdUlpQ]
    have hqUlpNonneg : 0 ≤ Fulp (beta:=2) bo 2 precision q := by
      dsimp [Fulp]
      positivity
    calc
      |_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c -
          _root_.F2R (beta:=2) q +
            -(_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b)| ≤
          |_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c -
            _root_.F2R (beta:=2) q| +
          |_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b| := htri
      _ ≤ (1 / 2 : ℝ) * Fulp (beta:=2) bo 2 precision q +
          (1 / 2 : ℝ) * Fulp (beta:=2) bo 2 precision p := by
            nlinarith
      _ ≤ 2 * Fulp (beta:=2) bo 2 precision q := by nlinarith
  · rcases hqZeroOrNormal with hqZero | hqNormal
    · have hFirst : _root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q ≤
          3 * |_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q| := by
        rw [hqZero, add_zero, sub_zero]
        calc
          _root_.F2R (beta:=2) p ≤ |_root_.F2R (beta:=2) p| := le_abs_self _
          _ ≤ 3 * |_root_.F2R (beta:=2) p| := by
            nlinarith [abs_nonneg (_root_.F2R (beta:=2) p)]
      have hdVal : _root_.F2R (beta:=2) d = _root_.F2R (beta:=2) p := by
        have h := closest_to_representable_is_exact bo
          (_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q) d p
          (hFirstRound hFirst).1 hpBound (by rw [hqZero, sub_zero])
        simpa [hqZero] using h
      have hpErr : 2 * |_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b -
            _root_.F2R (beta:=2) p| ≤ Fulp (beta:=2) bo 2 precision p := by
        have h := ClosestUlp (beta:=2) bo 2 precision
          (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b) p
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          h ⟨hRoundp.1, rfl, by decide, hprecisionNe, hvNum⟩
      have hqErr : 2 * |_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c| ≤
          Fulp (beta:=2) bo 2 precision q := by
        have h := ClosestUlp (beta:=2) bo 2 precision
          (_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c) q
        have hraw := h ⟨hRoundq.1, rfl, by decide, hprecisionNe, hvNum⟩
        have hbound :
            2 * |_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c -
                  _root_.F2R (beta:=2) q| ≤ Fulp (beta:=2) bo 2 precision q := by
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using hraw
        simpa [hqZero] using hbound
      have hqUlpLeP := zero_float_ulp_le_bounded bo precision q p hprecision
        hvNum hqZero hpBound
      have hdUlpP : Fulp (beta:=2) bo 2 precision d =
          Fulp (beta:=2) bo 2 precision p := by
        have h := FulpComp (beta:=2) 2 bo precision d p
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpComp_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          h ⟨hdBound, hpBound, hdVal, rfl, by decide, hprecisionNe, hvNum⟩
      have htri := abs_add_le
        (-(_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b -
          _root_.F2R (beta:=2) p))
        (_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c)
      rw [hdVal]
      have hrewrite : _root_.F2R (beta:=2) p -
            (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b -
              _root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c) =
          -(_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b -
              _root_.F2R (beta:=2) p) +
            _root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c := by ring
      rw [hrewrite]
      rw [abs_neg] at htri
      rw [hdUlpP]
      have hpUlpNonneg : 0 ≤ Fulp (beta:=2) bo 2 precision p := by
        dsimp [Fulp]
        positivity
      calc
        |-( _root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b -
              _root_.F2R (beta:=2) p) +
            _root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c| ≤
            |_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b -
              _root_.F2R (beta:=2) p| +
            |_root_.F2R (beta:=2) a * _root_.F2R (beta:=2) c| := htri
        _ ≤ (1 / 2 : ℝ) * Fulp (beta:=2) bo 2 precision p +
            (1 / 2 : ℝ) * Fulp (beta:=2) bo 2 precision q := by
              nlinarith
        _ ≤ 2 * Fulp (beta:=2) bo 2 precision p := by nlinarith
    · by_cases hFirst : _root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q ≤
          3 * |_root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q|
      · have h := discri1 (beta:=2) bo 2 precision a b b c p q d
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri1_check, Id.run,
          ULift.up_down, Int.cast_ofNat] using h
          ⟨rfl, rfl, hprecision, hvNum, haBound, hbBound, hbBound, hcBound,
            hpBound, hqBound, hdBound, hU0, hdNormal, hqNormal, hpNormal,
            mul_self_nonneg _, hRoundp, hRoundq, hFirst, hFirstRound hFirst⟩
      · have hSecond : 3 * |_root_.F2R (beta:=2) p -
              _root_.F2R (beta:=2) q| <
            _root_.F2R (beta:=2) p + _root_.F2R (beta:=2) q :=
          lt_of_not_ge hFirst
        rcases hsZeroOrNormal hSecond with hsZero | hsNormal
        · have hRoundt := hRoundtIf hSecond
          have hdpEq := hdpEqIf hSecond
          have hdqEq := hdqEqIf hSecond
          have hRounds := hRoundsIf hSecond
          have hRoundd := hRounddIf hSecond
          have hpNonneg : 0 ≤ _root_.F2R (beta:=2) p :=
            P_positive bo (2 : ℝ)
              (_root_.F2R (beta:=2) b * _root_.F2R (beta:=2) b) p
              hRoundp.1 (mul_self_nonneg _)
          have htExact : _root_.F2R (beta:=2) t =
              _root_.F2R (beta:=2) p - _root_.F2R (beta:=2) q := by
            have h := t_exact (beta:=2) bo (2 : ℝ) precision p q t
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, t_exact_check, Id.run,
              ULift.up_down, Int.cast_ofNat] using
              h ⟨by decide, hpBound, hqBound, hpNonneg, hSecond, hRoundt⟩
          have hdqOppBound : Fbounded (beta:=2) bo (Fopp dq) := by
            have h := oppBounded (beta:=2) bo dq
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, oppBounded_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using h hdqBound
          have hdpdq : _root_.F2R (beta:=2) dp -
              _root_.F2R (beta:=2) dq = 0 := by
            have hplus := plusExactR0_from_format_payload (beta:=2) bo 2 precision rfl (by decide)
              hprecisionNe (by
                rw [hvNum, Zpower_nat]
                exact one_lt_pow₀ (by decide : (1 : Int) < 2) (by omega))
              hvNum dp (Fopp dq) s
              hdpBound hdqOppBound
              (by simpa [hOppVal dq, sub_eq_add_neg] using hRounds.1) hsZero
            rw [hsZero, hOppVal dq] at hplus
            linarith
          have h := discri3_from_residual_witness_payload (beta:=2) bo 2
            precision a b b c p q t s d (_root_.F2R (beta:=2) dp)
              (_root_.F2R (beta:=2) dq)
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri3_check, Id.run,
            ULift.up_down, Int.cast_ofNat] using h
            ⟨hRoundd, hRounds, htExact, hdpEq, hdqEq,
              ⟨s, hsBound, by linarith⟩, rfl, by decide, hprecisionNe, hvNum⟩
        · have h := discri9 (beta:=2) bo 2 precision a b c p q t dp dq s d
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, discri9_check, Id.run,
            ULift.up_down, Int.cast_ofNat] using h
            ⟨rfl, rfl, hprecision, hvNum, haBound, hbBound, hcBound,
              hpBound, hqBound, hdBound, htBound, fun _ => hsBound,
              fun _ => hdpBound, fun _ => hdqBound, fun _ => Or.inl hsNormal,
              hU0, hU1, hU2, hU3, hpNormal, hqNormal, hdNormal,
              hRoundp, hRoundq, hFirstRound, hRoundtIf, hdpEqIf, hdqEqIf,
              hRoundsIf, hRounddIf⟩

/-- Coq: `RND_Min_Pos_correct`.

The proof follows the upstream Flocq shape: canonicity gives boundedness and
the rounded value is below the input; any bounded candidate below `r` is first
normalized without changing its value, then the projector and monotonicity of
`RND_Min_Pos` bound it by the returned float. -/
theorem RND_Min_Pos_correct {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int) (r : ℝ) :
    ⦃⌜0 ≤ r ∧ beta = radix ∧ 1 < radix ∧ 1 < p ∧
        b.vNum = Zpower_nat radix p.toNat⌝⦄
    (pure (RND_Min_Pos_correct_check (beta:=beta) b radix p r) : Id Unit)
    ⦃⇓_ => ⌜isMin (beta:=beta) b radix r
        (RND_Min_Pos (beta:=beta) b radix p r)⌝⦄ := by
  intro h
  rcases h with ⟨hr_nonneg, hbeta, hradix, hp_gt, hvnum⟩
  simp only [wp, PostCond.noThrow, pure, RND_Min_Pos_correct_check,
    Id.run, ULift.up_down]
  have hp_ge : (1 : Int) ≤ p := le_of_lt hp_gt
  have hp_nat_ne : p.toNat ≠ 0 := by
    have hp_pos : (0 : Int) < p := by omega
    have hp_toNat_pos : 0 < p.toNat := by
      have hp_toNat_eq : (p.toNat : Int) = p :=
        Int.toNat_of_nonneg (le_of_lt hp_pos)
      omega
    exact Nat.ne_of_gt hp_toNat_pos
  constructor
  · have hcan := RND_Min_Pos_canonic (beta:=beta) b radix p r
    have hcan' :
        Fcanonic (beta:=beta) radix b
          (RND_Min_Pos (beta:=beta) b radix p r) := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RND_Min_Pos_canonic_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        hcan ⟨hr_nonneg, hbeta, hradix, hp_gt, hvnum⟩
    have hbound := FcanonicBound (beta:=beta) radix b
      (RND_Min_Pos (beta:=beta) b radix p r)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicBound_check,
      Fbounded', Int.cast_ofNat] using hbound hcan'
  constructor
  · have hle := RND_Min_Pos_Rle (beta:=beta) b radix p r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RND_Min_Pos_Rle_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using hle ⟨hr_nonneg, hbeta, hradix⟩
  · intro f hfbounded hf_le_r
    by_cases hf_nonneg : 0 ≤ _root_.F2R (beta:=beta) f
    ·
      let nf : FloatSpec.Core.Defs.FlocqFloat beta :=
        Fnormalize (beta:=beta) radix b p.toNat f
      have hnf_eq :
          _root_.F2R (beta:=beta) nf = _root_.F2R (beta:=beta) f := by
        have hcorr := FnormalizeCorrect (beta:=beta) radix b p.toNat f
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
          Id.run, ULift.up_down, nf, Int.cast_ofNat] using hcorr ⟨hbeta, hradix⟩
      have hnf_bounded : Fbounded (beta:=beta) b nf := by
        have hfb := FnormalizeBounded (beta:=beta) radix b p.toNat f
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeBounded_check,
          Id.run, ULift.up_down, Fbounded', nf, Int.cast_ofNat] using
          hfb ⟨hfbounded, hfbounded, hp_nat_ne, hradix, hvnum⟩
      have hnf_canonic :
          Fcanonic (beta:=beta) radix b nf := by
        have hfc := FnormalizeCanonic (beta:=beta) radix b p.toNat f
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
          Id.run, ULift.up_down, Fbounded', nf, Int.cast_ofNat] using
          hfc ⟨hfbounded, hfbounded, hp_nat_ne, hradix, hvnum⟩
      have hnf_nonneg : 0 ≤ _root_.F2R (beta:=beta) nf := by
        simpa [hnf_eq] using hf_nonneg
      have hnf_le_r : _root_.F2R (beta:=beta) nf ≤ r := by
        simpa [hnf_eq] using hf_le_r
      have hproj :
          _root_.F2R (beta:=beta)
              (RND_Min_Pos (beta:=beta) b radix p (_root_.F2R (beta:=beta) nf)) =
            _root_.F2R (beta:=beta) nf := by
        have hpj := RND_Min_Pos_projector (beta:=beta) b radix p nf
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RND_Min_Pos_projector_check,
          Id.run, ULift.up_down, Fcanonic', Int.cast_ofNat] using
          hpj ⟨hnf_nonneg, hnf_canonic, hbeta, hradix, hp_ge, hvnum⟩
      have hmono :
          _root_.F2R (beta:=beta)
              (RND_Min_Pos (beta:=beta) b radix p (_root_.F2R (beta:=beta) nf)) ≤
            _root_.F2R (beta:=beta)
              (RND_Min_Pos (beta:=beta) b radix p r) := by
        have hm := RND_Min_Pos_monotone (beta:=beta) b radix p
          (_root_.F2R (beta:=beta) nf) r
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RND_Min_Pos_monotone_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          hm ⟨hnf_nonneg, hnf_le_r, hbeta, hradix, hp_ge⟩
      calc
        _root_.F2R (beta:=beta) f =
            _root_.F2R (beta:=beta) nf := hnf_eq.symm
        _ = _root_.F2R (beta:=beta)
              (RND_Min_Pos (beta:=beta) b radix p (_root_.F2R (beta:=beta) nf)) := hproj.symm
        _ ≤ _root_.F2R (beta:=beta)
              (RND_Min_Pos (beta:=beta) b radix p r) := hmono
    ·
      have hf_lt_zero : _root_.F2R (beta:=beta) f < 0 := lt_of_not_ge hf_nonneg
      have hmin_nonneg :
          0 ≤ _root_.F2R (beta:=beta) (RND_Min_Pos (beta:=beta) b radix p r) :=
        RND_Min_Pos_nonneg (beta:=beta) b radix p r hr_nonneg hbeta hradix
      exact le_trans (le_of_lt hf_lt_zero) hmin_nonneg

/-- Coq: `RND_Max_Pos_Rle` — for nonnegative `r`, the value of
    `RND_Max_Pos r` (interpreted in ℝ) is greater than or equal to `r`. -/
theorem RND_Max_Pos_Rle {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int) (r : ℝ) :
    ⦃⌜0 ≤ r ∧ beta = radix ∧ 1 < radix ∧ 1 < p ∧
        b.vNum = Zpower_nat radix p.toNat⌝⦄
    (pure (RND_Max_Pos_Rle_check (beta:=beta) b radix p r) : Id Unit)
    ⦃⇓_ => ⌜r ≤ _root_.F2R (RND_Max_Pos (beta:=beta) b radix p r)⌝⦄ := by
  intro h
  rcases h with ⟨hr, hBetaEq, hRadixGt1, hpGt1, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, RND_Max_Pos_Rle_check, Id.run,
    ULift.up_down]
  unfold RND_Max_Pos
  by_cases hrep :
      r = _root_.F2R (beta:=beta)
        (RND_Min_Pos (beta:=beta) b radix p r)
  · rw [ite_eq_left hrep]
    exact le_of_eq hrep
  · rw [ite_eq_right hrep]
    by_cases hle_succ :
        r ≤ _root_.F2R (beta:=beta)
          (FSucc (beta:=beta) b radix p.toNat
            (RND_Min_Pos (beta:=beta) b radix p r))
    · exact hle_succ
    · exfalso
      have hsucc_lt_r :
          _root_.F2R (beta:=beta)
            (FSucc (beta:=beta) b radix p.toNat
              (RND_Min_Pos (beta:=beta) b radix p r)) < r := lt_of_not_ge hle_succ
      have hmin_correct :
          isMin (beta:=beta) b radix r
            (RND_Min_Pos (beta:=beta) b radix p r) := by
        have h := RND_Min_Pos_correct (beta:=beta) b radix p r
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RND_Min_Pos_correct_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hr, hBetaEq, hRadixGt1, hpGt1, hvNum⟩
      rcases hmin_correct with ⟨hmin_bounded, hmin_le_r, hgreatest⟩
      have hmin_can :
          Fcanonic (beta:=beta) radix b
            (RND_Min_Pos (beta:=beta) b radix p r) := by
        have h := RND_Min_Pos_canonic (beta:=beta) b radix p r
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RND_Min_Pos_canonic_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hr, hBetaEq, hRadixGt1, hpGt1, hvNum⟩
      have hp_nat_ne : p.toNat ≠ 0 := by
        have hp_pos : (0 : Int) < p := by omega
        exact Nat.ne_of_gt (by
          have hp_toNat : (p.toNat : Int) = p :=
            Int.toNat_of_nonneg (le_of_lt hp_pos)
          omega)
      have hsucc_bounded :
          Fbounded (beta:=beta) b
            (FSucc (beta:=beta) b radix p.toNat
              (RND_Min_Pos (beta:=beta) b radix p r)) := by
        have h := FBoundedSuc (beta:=beta) b radix p.toNat
          (RND_Min_Pos (beta:=beta) b radix p r)
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FBoundedSuc_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hmin_bounded, hRadixGt1, hp_nat_ne, hvNum⟩
      have hsucc_le_min :
          _root_.F2R (beta:=beta)
            (FSucc (beta:=beta) b radix p.toNat
              (RND_Min_Pos (beta:=beta) b radix p r)) ≤
          _root_.F2R (beta:=beta)
            (RND_Min_Pos (beta:=beta) b radix p r) :=
        hgreatest
          (FSucc (beta:=beta) b radix p.toNat
            (RND_Min_Pos (beta:=beta) b radix p r))
          hsucc_bounded (le_of_lt hsucc_lt_r)
      have hmin_lt_succ :
          _root_.F2R (beta:=beta)
            (RND_Min_Pos (beta:=beta) b radix p r) <
          _root_.F2R (beta:=beta)
            (FSucc (beta:=beta) b radix p.toNat
              (RND_Min_Pos (beta:=beta) b radix p r)) := by
        have h := FSuccLt (beta:=beta) b radix p.toNat
          (RND_Min_Pos (beta:=beta) b radix p r)
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FSuccLt_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hBetaEq, hRadixGt1, hp_nat_ne, hvNum⟩
      exact (not_lt_of_ge hsucc_le_min) hmin_lt_succ

/-- Coq: `RND_Max_Pos_correct`.

This is the Flocq upper-rounding correctness theorem for nonnegative inputs:
`RND_Max_Pos r` is bounded, lies above `r`, and is the least bounded float above
`r`.  The proof follows the upstream successor-of-min construction. -/
theorem RND_Max_Pos_correct {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int) (r : ℝ) :
    ⦃⌜0 ≤ r ∧ beta = radix ∧ 1 < radix ∧ 1 < p ∧
        b.vNum = Zpower_nat radix p.toNat⌝⦄
    (pure (RND_Max_Pos_correct_check (beta:=beta) b radix p r) : Id Unit)
    ⦃⇓_ => ⌜isMax (beta:=beta) b radix r
        (RND_Max_Pos (beta:=beta) b radix p r)⌝⦄ := by
  intro h
  rcases h with ⟨hr, hBetaEq, hRadixGt1, hpGt1, hvNum⟩
  simp only [wp, PostCond.noThrow, pure, RND_Max_Pos_correct_check, Id.run,
    ULift.up_down]
  have hp_nat_ne : p.toNat ≠ 0 := by
    have hp_pos : (0 : Int) < p := by omega
    exact Nat.ne_of_gt (by
      have hp_toNat : (p.toNat : Int) = p :=
        Int.toNat_of_nonneg (le_of_lt hp_pos)
      omega)
  have hmin_correct :
      isMin (beta:=beta) b radix r
        (RND_Min_Pos (beta:=beta) b radix p r) := by
    have hmin := RND_Min_Pos_correct (beta:=beta) b radix p r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RND_Min_Pos_correct_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      hmin ⟨hr, hBetaEq, hRadixGt1, hpGt1, hvNum⟩
  rcases hmin_correct with ⟨hmin_bounded, hmin_le_r, hmin_greatest⟩
  have hmax_can :
      Fcanonic (beta:=beta) radix b
        (RND_Max_Pos (beta:=beta) b radix p r) := by
    have hcan := RND_Max_Pos_canonic (beta:=beta) b radix p r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RND_Max_Pos_canonic_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      hcan ⟨hr, hBetaEq, hRadixGt1, hpGt1, hvNum⟩
  have hmax_bounded : Fbounded (beta:=beta) b
      (RND_Max_Pos (beta:=beta) b radix p r) := by
    have hbound := FcanonicBound (beta:=beta) radix b
      (RND_Max_Pos (beta:=beta) b radix p r)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicBound_check,
      Fbounded', Int.cast_ofNat] using hbound hmax_can
  have hmax_ge :
      r ≤ _root_.F2R (beta:=beta)
        (RND_Max_Pos (beta:=beta) b radix p r) := by
    have hle := RND_Max_Pos_Rle (beta:=beta) b radix p r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RND_Max_Pos_Rle_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      hle ⟨hr, hBetaEq, hRadixGt1, hpGt1, hvNum⟩
  constructor
  · exact hmax_bounded
  constructor
  · exact hmax_ge
  · intro f hfbounded hr_le_f
    unfold RND_Max_Pos
    by_cases hrep :
        r = _root_.F2R (beta:=beta)
          (RND_Min_Pos (beta:=beta) b radix p r)
    · rw [ite_eq_left hrep]
      exact le_trans hmin_le_r hr_le_f
    · rw [ite_eq_right hrep]
      by_cases hsucc_le_f :
          _root_.F2R (beta:=beta)
            (FSucc (beta:=beta) b radix p.toNat
              (RND_Min_Pos (beta:=beta) b radix p r)) ≤
          _root_.F2R (beta:=beta) f
      · exact hsucc_le_f
      · exfalso
        have hf_lt_succ :
            _root_.F2R (beta:=beta) f <
            _root_.F2R (beta:=beta)
              (FSucc (beta:=beta) b radix p.toNat
                (RND_Min_Pos (beta:=beta) b radix p r)) :=
          lt_of_not_ge hsucc_le_f
        let nf : FloatSpec.Core.Defs.FlocqFloat beta :=
          Fnormalize (beta:=beta) radix b p.toNat f
        have hnf_eq :
            _root_.F2R (beta:=beta) nf = _root_.F2R (beta:=beta) f := by
          have hcorr := FnormalizeCorrect (beta:=beta) radix b p.toNat f
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
            Id.run, ULift.up_down, nf, Int.cast_ofNat] using hcorr ⟨hBetaEq, hRadixGt1⟩
        have hnf_bounded : Fbounded (beta:=beta) b nf := by
          have hfb := FnormalizeBounded (beta:=beta) radix b p.toNat f
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeBounded_check,
            Id.run, ULift.up_down, Fbounded', nf, Int.cast_ofNat] using
            hfb ⟨hfbounded, hfbounded, hp_nat_ne, hRadixGt1, hvNum⟩
        have hnf_can :
            Fcanonic (beta:=beta) radix b nf := by
          have hfc := FnormalizeCanonic (beta:=beta) radix b p.toNat f
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
            Id.run, ULift.up_down, Fbounded', nf, Int.cast_ofNat] using
            hfc ⟨hfbounded, hfbounded, hp_nat_ne, hRadixGt1, hvNum⟩
        have hmin_can :
            Fcanonic (beta:=beta) radix b
              (RND_Min_Pos (beta:=beta) b radix p r) := by
          have hcan := RND_Min_Pos_canonic (beta:=beta) b radix p r
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RND_Min_Pos_canonic_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using
            hcan ⟨hr, hBetaEq, hRadixGt1, hpGt1, hvNum⟩
        have hmin_le_nf :
            _root_.F2R (beta:=beta)
                (RND_Min_Pos (beta:=beta) b radix p r) ≤
            _root_.F2R (beta:=beta) nf := by
          have hnf_ge_r : r ≤ _root_.F2R (beta:=beta) nf := by
            simpa [hnf_eq] using hr_le_f
          -- If `nf` lies above `r`, the lower-rounding value is below it
          -- because the lower-rounding value itself is below `r`.
          exact le_trans hmin_le_r hnf_ge_r
        have hnf_lt_succ :
            _root_.F2R (beta:=beta) nf <
            _root_.F2R (beta:=beta)
              (FSucc (beta:=beta) b radix p.toNat
                (RND_Min_Pos (beta:=beta) b radix p r)) := by
          simpa [hnf_eq] using hf_lt_succ
        by_cases hmin_eq_nf :
            _root_.F2R (beta:=beta) nf =
              _root_.F2R (beta:=beta)
                (RND_Min_Pos (beta:=beta) b radix p r)
        · have hr_eq_min :
              r = _root_.F2R (beta:=beta)
                    (RND_Min_Pos (beta:=beta) b radix p r) := by
            have hr_le_min :
                r ≤ _root_.F2R (beta:=beta)
                  (RND_Min_Pos (beta:=beta) b radix p r) := by
              calc
                r ≤ _root_.F2R (beta:=beta) f := hr_le_f
                _ = _root_.F2R (beta:=beta) nf := hnf_eq.symm
                _ = _root_.F2R (beta:=beta)
                      (RND_Min_Pos (beta:=beta) b radix p r) := hmin_eq_nf
            exact le_antisymm hr_le_min hmin_le_r
          exact hrep hr_eq_min
        · have hmin_lt_nf :
              _root_.F2R (beta:=beta)
                  (RND_Min_Pos (beta:=beta) b radix p r) <
              _root_.F2R (beta:=beta) nf :=
            lt_of_le_of_ne hmin_le_nf (Ne.symm hmin_eq_nf)
          have hsucc_le_nf :
              _root_.F2R (beta:=beta)
                (FSucc (beta:=beta) b radix p.toNat
                  (RND_Min_Pos (beta:=beta) b radix p r)) ≤
              _root_.F2R (beta:=beta) nf := by
            have hprop := FSuccPropPos (beta:=beta) b radix p.toNat
              (RND_Min_Pos (beta:=beta) b radix p r) nf
            have hmin_nonneg :
                0 ≤ _root_.F2R (beta:=beta)
                  (RND_Min_Pos (beta:=beta) b radix p r) :=
              RND_Min_Pos_nonneg (beta:=beta) b radix p r hr hBetaEq hRadixGt1
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FSuccPropPos_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using
              hprop ⟨hBetaEq, hRadixGt1, hp_nat_ne, hvNum, hmin_nonneg,
                hmin_can, hnf_can, hmin_lt_nf⟩
          exact (not_lt_of_ge hsucc_le_nf) hnf_lt_succ

/-- Closed section-context form of `RND_Min_canonic`.

The earlier translated wrapper keeps positive-side canonicity as explicit
payloads because the positive lemmas appear later in the file.  At this point
those payloads are available, so this theorem packages the Flocq section
assumptions and supplies them directly. -/
theorem RND_Min_canonic {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int) (r : ℝ) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < p ∧
        b.vNum = Zpower_nat radix p.toNat⌝⦄
    (pure (RND_Min_canonic_check (beta:=beta) b radix p r) : Id Unit)
    ⦃⇓_ => ⌜Fcanonic (beta:=beta) radix b
        (RND_Min (beta:=beta) b radix p r)⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hradix, hp, hvnum⟩
  have hMinPos :
      ∀ s : ℝ, 0 ≤ s →
        Fcanonic (beta:=beta) radix b
          (RND_Min_Pos (beta:=beta) b radix p s) := by
    intro s hs
    have h := RND_Min_Pos_canonic (beta:=beta) b radix p s
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RND_Min_Pos_canonic_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hs, hbeta, hradix, hp, hvnum⟩
  have hMaxPos :
      ∀ s : ℝ, 0 ≤ s →
        Fcanonic (beta:=beta) radix b
          (RND_Max_Pos (beta:=beta) b radix p s) := by
    intro s hs
    have h := RND_Max_Pos_canonic (beta:=beta) b radix p s
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RND_Max_Pos_canonic_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hs, hbeta, hradix, hp, hvnum⟩
  have h := RND_Min_canonic_from_positive_payload (beta:=beta) b radix p r
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RND_Min_canonic_check,
    Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hMinPos, hMaxPos⟩

/-- Closed section-context form of `RND_Max_canonic`, discharging the
positive-side canonicity payloads from the restored positive lemmas. -/
theorem RND_Max_canonic {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int) (r : ℝ) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < p ∧
        b.vNum = Zpower_nat radix p.toNat⌝⦄
    (pure (RND_Max_canonic_check (beta:=beta) b radix p r) : Id Unit)
    ⦃⇓_ => ⌜Fcanonic (beta:=beta) radix b
        (RND_Max (beta:=beta) b radix p r)⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hradix, hp, hvnum⟩
  have hMinPos :
      ∀ s : ℝ, 0 ≤ s →
        Fcanonic (beta:=beta) radix b
          (RND_Min_Pos (beta:=beta) b radix p s) := by
    intro s hs
    have h := RND_Min_Pos_canonic (beta:=beta) b radix p s
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RND_Min_Pos_canonic_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hs, hbeta, hradix, hp, hvnum⟩
  have hMaxPos :
      ∀ s : ℝ, 0 ≤ s →
        Fcanonic (beta:=beta) radix b
          (RND_Max_Pos (beta:=beta) b radix p s) := by
    intro s hs
    have h := RND_Max_Pos_canonic (beta:=beta) b radix p s
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RND_Max_Pos_canonic_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hs, hbeta, hradix, hp, hvnum⟩
  have h := RND_Max_canonic_from_positive_payload (beta:=beta) b radix p r
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RND_Max_canonic_check,
    Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hMinPos, hMaxPos⟩

/-- Closed section-context form of `RND_Min_correct`.

This is the generic Pff lower-rounding payload needed by the Pff-to-Flocq
rounding bridge: the positive lower/upper correctness obligations are supplied
from `RND_Min_Pos_correct` and `RND_Max_Pos_correct`. -/
theorem RND_Min_correct {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int) (r : ℝ) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < p ∧
        b.vNum = Zpower_nat radix p.toNat⌝⦄
    (pure (RND_Min_correct_check (beta:=beta) b radix p r) : Id Unit)
    ⦃⇓_ => ⌜isMin (beta:=beta) b radix r
        (RND_Min (beta:=beta) b radix p r)⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hradix, hp, hvnum⟩
  have hMinPos :
      ∀ s : ℝ, 0 ≤ s →
        isMin (beta:=beta) b radix s
          (RND_Min_Pos (beta:=beta) b radix p s) := by
    intro s hs
    have h := RND_Min_Pos_correct (beta:=beta) b radix p s
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RND_Min_Pos_correct_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hs, hbeta, hradix, hp, hvnum⟩
  have hMaxPos :
      ∀ s : ℝ, 0 ≤ s →
        isMax (beta:=beta) b radix s
          (RND_Max_Pos (beta:=beta) b radix p s) := by
    intro s hs
    have h := RND_Max_Pos_correct (beta:=beta) b radix p s
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RND_Max_Pos_correct_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hs, hbeta, hradix, hp, hvnum⟩
  have h := RND_Min_correct_from_positive_payload (beta:=beta) b radix p r
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RND_Min_correct_check,
    Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hMinPos, hMaxPos⟩

/-- Closed section-context form of `RND_Max_correct`, discharging the positive
lower/upper correctness payloads from restored positive rounding theorems. -/
theorem RND_Max_correct {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int) (r : ℝ) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < p ∧
        b.vNum = Zpower_nat radix p.toNat⌝⦄
    (pure (RND_Max_correct_check (beta:=beta) b radix p r) : Id Unit)
    ⦃⇓_ => ⌜isMax (beta:=beta) b radix r
        (RND_Max (beta:=beta) b radix p r)⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hradix, hp, hvnum⟩
  have hMinPos :
      ∀ s : ℝ, 0 ≤ s →
        isMin (beta:=beta) b radix s
          (RND_Min_Pos (beta:=beta) b radix p s) := by
    intro s hs
    have h := RND_Min_Pos_correct (beta:=beta) b radix p s
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RND_Min_Pos_correct_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hs, hbeta, hradix, hp, hvnum⟩
  have hMaxPos :
      ∀ s : ℝ, 0 ≤ s →
        isMax (beta:=beta) b radix s
          (RND_Max_Pos (beta:=beta) b radix p s) := by
    intro s hs
    have h := RND_Max_Pos_correct (beta:=beta) b radix p s
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RND_Max_Pos_correct_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hs, hbeta, hradix, hp, hvnum⟩
  have h := RND_Max_correct_from_positive_payload (beta:=beta) b radix p r
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RND_Max_correct_check,
    Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hMinPos, hMaxPos⟩

/-- Closed section-context form of `RND_Closest_canonic`.

This removes the explicit lower/upper canonicity payloads by using the closed
signed min/max wrappers above. -/
theorem RND_Closest_canonic {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int)
    (choice : Int → Bool) (r : ℝ) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < p ∧
        b.vNum = Zpower_nat radix p.toNat⌝⦄
    (pure (RND_Closest_canonic_check (beta:=beta) b radix p choice r) : Id Unit)
    ⦃⇓_ => ⌜Fcanonic (beta:=beta) radix b
        (RND_Closest (beta:=beta) b radix p choice r)⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hradix, hp, hvnum⟩
  have hMin : Fcanonic (beta:=beta) radix b
      (RND_Min (beta:=beta) b radix p r) := by
    have h := RND_Min_canonic (beta:=beta) b radix p r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RND_Min_canonic_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hbeta, hradix, hp, hvnum⟩
  have hMax : Fcanonic (beta:=beta) radix b
      (RND_Max (beta:=beta) b radix p r) := by
    have h := RND_Max_canonic (beta:=beta) b radix p r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RND_Max_canonic_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hbeta, hradix, hp, hvnum⟩
  have h := RND_Closest_canonic_from_min_max (beta:=beta) b radix p choice r
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RND_Closest_canonic_check,
    Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hMin, hMax⟩

/-- Closed section-context form of `RND_Closest_correct`.

This packages the nearest-rounding correctness payload once the signed
lower/upper extremal roundings have been restored. -/
theorem RND_Closest_correct {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (p : Int)
    (choice : Int → Bool) (r : ℝ) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < p ∧
        b.vNum = Zpower_nat radix p.toNat⌝⦄
    (pure (RND_Closest_correct_check (beta:=beta) b radix p choice r) : Id Unit)
    ⦃⇓_ => ⌜Closest (beta:=beta) b (radix : ℝ) r
        (RND_Closest (beta:=beta) b radix p choice r)⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hradix, hp, hvnum⟩
  have hMin : isMin (beta:=beta) b radix r
      (RND_Min (beta:=beta) b radix p r) := by
    have h := RND_Min_correct (beta:=beta) b radix p r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RND_Min_correct_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hbeta, hradix, hp, hvnum⟩
  have hMax : isMax (beta:=beta) b radix r
      (RND_Max (beta:=beta) b radix p r) := by
    have h := RND_Max_correct (beta:=beta) b radix p r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RND_Max_correct_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hbeta, hradix, hp, hvnum⟩
  have h := RND_Closest_correct_from_min_max (beta:=beta) b radix p choice r
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RND_Closest_correct_check,
    Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hMin, hMax⟩

private lemma canonical_above_min_gap {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hprecision : 1 < precision)
    (hvNum : b.vNum = Zpower_nat radix precision)
    (hxCan : Fcanonic (beta:=beta) radix b x)
    (hxExp : -b.dExp < x.Fexp) :
    (radix : ℝ) ^ x.Fexp +
        _root_.F2R (beta:=beta)
          (firstNormalPos (beta:=beta) radix b precision) ≤
      |_root_.F2R (beta:=beta) x| := by
  subst beta
  have hradixPosInt : (0 : Int) < radix := by omega
  have hradixPos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradixPosInt
  have hradixOne : (1 : ℝ) ≤ (radix : ℝ) := by
    exact_mod_cast (le_of_lt hradix)
  have hradixNe : (radix : ℝ) ≠ 0 := ne_of_gt hradixPos
  have hxNormal : Fnormal (beta:=radix) radix b x := by
    rcases hxCan with hxNormal | hxSubnormal
    · exact hxNormal
    · exfalso
      rcases hxSubnormal with ⟨_, hxExpEq, _⟩
      omega
  have hnormMinPos : 0 < nNormMin radix precision := by
    unfold nNormMin
    exact pow_pos hradixPosInt _
  have hnormMinTwo : 2 ≤ nNormMin radix precision := by
    have hradixLe : radix ≤ nNormMin radix precision := by
      have hpow : radix ^ 1 ≤ radix ^ (precision - 1) :=
        pow_le_pow_right₀ (by omega : (1 : Int) ≤ radix) (by omega)
      simpa [nNormMin] using hpow
    omega
  have hnormMul : b.vNum = radix * nNormMin radix precision :=
    PosNormMin radix b precision (by omega) hvNum
  have hxNumLower : nNormMin radix precision ≤ |x.Fnum| := by
    have hmul :
        radix * nNormMin radix precision ≤ radix * |x.Fnum| := by
      calc
        radix * nNormMin radix precision = b.vNum := hnormMul.symm
        _ ≤ |radix * x.Fnum| := hxNormal.2
        _ = radix * |x.Fnum| := by
          rw [abs_mul, abs_of_pos hradixPosInt]
    exact Int.le_of_mul_le_mul_left hmul hradixPosInt
  have hxNumLowerReal :
      (nNormMin radix precision : ℝ) ≤ |(x.Fnum : ℝ)| := by
    have hcast :
        (nNormMin radix precision : ℝ) ≤ (|x.Fnum| : Int) := by
      exact_mod_cast hxNumLower
    simpa only [Int.cast_abs, Int.cast_ofNat] using hcast
  have hcoeffInt :
      nNormMin radix precision ≤
        radix * (nNormMin radix precision - 1) := by
    nlinarith
  have hcoeffReal :
      (nNormMin radix precision : ℝ) ≤
        (radix : ℝ) * ((nNormMin radix precision : ℝ) - 1) := by
    exact_mod_cast hcoeffInt
  have hcoeffNonneg :
      0 ≤ (nNormMin radix precision : ℝ) - 1 := by
    have hone : (1 : ℝ) ≤ (nNormMin radix precision : ℝ) := by
      exact_mod_cast (show (1 : Int) ≤ nNormMin radix precision by omega)
    linarith
  have hpowStep :
      (radix : ℝ) ^ (-b.dExp + 1) ≤ (radix : ℝ) ^ x.Fexp :=
    zpow_le_zpow_right₀ hradixOne (by omega)
  have hfirstLeRemaining :
      (nNormMin radix precision : ℝ) * (radix : ℝ) ^ (-b.dExp) ≤
        ((nNormMin radix precision : ℝ) - 1) *
          (radix : ℝ) ^ x.Fexp := by
    calc
      (nNormMin radix precision : ℝ) * (radix : ℝ) ^ (-b.dExp)
          ≤ ((radix : ℝ) * ((nNormMin radix precision : ℝ) - 1)) *
              (radix : ℝ) ^ (-b.dExp) :=
        mul_le_mul_of_nonneg_right hcoeffReal
          (le_of_lt (zpow_pos hradixPos _))
      _ = ((nNormMin radix precision : ℝ) - 1) *
            (radix : ℝ) ^ (-b.dExp + 1) := by
        rw [zpow_add_one₀ hradixNe]
        ring
      _ ≤ ((nNormMin radix precision : ℝ) - 1) *
            (radix : ℝ) ^ x.Fexp :=
        mul_le_mul_of_nonneg_left hpowStep hcoeffNonneg
  have hfirstValue :
      _root_.F2R (beta:=radix)
          (firstNormalPos (beta:=radix) radix b precision) =
        (nNormMin radix precision : ℝ) * (radix : ℝ) ^ (-b.dExp) := by
    simp [_root_.F2R, FloatSpec.Core.Defs.F2R, firstNormalPos]
  have hxAbsValue :
      |_root_.F2R (beta:=radix) x| =
        |(x.Fnum : ℝ)| * (radix : ℝ) ^ x.Fexp := by
    simp [_root_.F2R, FloatSpec.Core.Defs.F2R, abs_mul,
      abs_of_pos (zpow_pos hradixPos x.Fexp)]
  rw [hfirstValue, hxAbsValue]
  calc
    (radix : ℝ) ^ x.Fexp +
          (nNormMin radix precision : ℝ) * (radix : ℝ) ^ (-b.dExp)
        ≤ (radix : ℝ) ^ x.Fexp +
            ((nNormMin radix precision : ℝ) - 1) *
              (radix : ℝ) ^ x.Fexp :=
      by
        simpa [add_comm] using
          add_le_add_left hfirstLeRemaining ((radix : ℝ) ^ x.Fexp)
    _ = (nNormMin radix precision : ℝ) * (radix : ℝ) ^ x.Fexp := by
      ring
    _ ≤ |(x.Fnum : ℝ)| * (radix : ℝ) ^ x.Fexp :=
      mul_le_mul_of_nonneg_right hxNumLowerReal
        (le_of_lt (zpow_pos hradixPos _))

private lemma closest_under_extended_of_canonical_above_min {beta : Int} [ValidRadix beta]
    (b b' : Fbound_skel) (radix : Int) (precision : Nat)
    (r : ℝ) (x : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hprecision : 1 < precision)
    (hvNum : b.vNum = Zpower_nat radix precision)
    (hbNum : b.vNum = b'.vNum) (hbExp : b.dExp ≤ b'.dExp)
    (hxCan : Fcanonic (beta:=beta) radix b x)
    (hxExp : -b.dExp < x.Fexp)
    (hxClosest : Closest (beta:=beta) b (radix : ℝ) r x) :
    Closest (beta:=beta) b' (radix : ℝ) r x := by
  have hxBound : Fbounded (beta:=beta) b x :=
    (FcanonicBound (beta:=beta) radix b x) hxCan
  have hxBoundExt : Fbounded (beta:=beta) b' x := by
    constructor
    · simpa [← hbNum] using hxBound.1
    · omega
  constructor
  · exact hxBoundExt
  · intro f hfBoundExt
    by_cases hfExp : -b.dExp ≤ f.Fexp
    · have hfBound : Fbounded (beta:=beta) b f := by
        constructor
        · simpa [hbNum] using hfBoundExt.1
        · exact hfExp
      exact hxClosest.2 f hfBound
    · have hfExpLt : f.Fexp < -b.dExp := lt_of_not_ge hfExp
      have hfAbsLtFirst :
          |_root_.F2R (beta:=beta) f| <
            _root_.F2R (beta:=beta)
              (firstNormalPos (beta:=beta) radix b precision) :=
        abs_under_extended_below_firstNormal b b' radix precision f
          hbeta hradix hprecision hvNum hbNum hfBoundExt hfExpLt
      have hxGap := canonical_above_min_gap b radix precision x hbeta hradix
        hprecision hvNum hxCan hxExp
      have hpowLeAbsDiff :
          (radix : ℝ) ^ x.Fexp ≤
            |_root_.F2R (beta:=beta) x| -
              |_root_.F2R (beta:=beta) f| := by
        linarith
      have hpowLeDist :
          (radix : ℝ) ^ x.Fexp ≤
            |_root_.F2R (beta:=beta) x -
              _root_.F2R (beta:=beta) f| :=
        le_trans hpowLeAbsDiff
          (abs_sub_abs_le_abs_sub (_root_.F2R (beta:=beta) x)
            (_root_.F2R (beta:=beta) f))
      have hprecisionNe : precision ≠ 0 := by omega
      have hxFulp :
          Fulp (beta:=beta) b radix precision x =
            (radix : ℝ) ^ x.Fexp := by
        have h := CanonicFulp (beta:=beta) radix b precision x
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          h ⟨hxCan, hbeta, hradix, hprecisionNe, hvNum⟩
      have hxClosestUlp :
          2 * |r - _root_.F2R (beta:=beta) x| ≤
            Fulp (beta:=beta) b radix precision x := by
        have h := ClosestUlp (beta:=beta) b radix precision r x
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          h ⟨hxClosest, hbeta, hradix, hprecisionNe, hvNum⟩
      have htwice :
          2 * |_root_.F2R (beta:=beta) x - r| ≤
            |_root_.F2R (beta:=beta) x -
              _root_.F2R (beta:=beta) f| := by
        calc
          2 * |_root_.F2R (beta:=beta) x - r| =
              2 * |r - _root_.F2R (beta:=beta) x| := by
                rw [abs_sub_comm]
          _ ≤ Fulp (beta:=beta) b radix precision x := hxClosestUlp
          _ = (radix : ℝ) ^ x.Fexp := hxFulp
          _ ≤ |_root_.F2R (beta:=beta) x -
                _root_.F2R (beta:=beta) f| := hpowLeDist
      have htriangle :
          |_root_.F2R (beta:=beta) x -
              _root_.F2R (beta:=beta) f| ≤
            |_root_.F2R (beta:=beta) x - r| +
              |_root_.F2R (beta:=beta) f - r| := by
        have h := abs_sub_le (_root_.F2R (beta:=beta) x) r
          (_root_.F2R (beta:=beta) f)
        simpa [abs_sub_comm r (_root_.F2R (beta:=beta) f)] using h
      linarith

/-! Coq generic Dekker theorem `Underf_Err2_aux`. -/
theorem Underf_Err2_aux {beta : Int} [ValidRadix beta]
    (b b' : Fbound_skel) (radix : Int) (precision : Nat)
    (r : ℝ) (x1 : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix precision)
    (hprecision : 1 < precision)
    (hbNum : b.vNum = b'.vNum) (hbExp : b.dExp ≤ b'.dExp)
    (hx1Can : Fcanonic (beta:=beta) radix b x1)
    (hx1Closest : Closest (beta:=beta) b (radix : ℝ) r x1) :
    ∃ x2 : FloatSpec.Core.Defs.FlocqFloat beta,
      Underf_Err (beta:=beta) b b' radix x1 x2 r (3 / 4 : ℝ) ∧
        Closest (beta:=beta) b' (radix : ℝ) r x2 := by
  have hradixPos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (show (0 : Int) < radix by omega)
  have hradixOne : (1 : ℝ) ≤ (radix : ℝ) := by
    exact_mod_cast (le_of_lt hradix)
  have hradixTwo : (2 : ℝ) ≤ (radix : ℝ) := by
    exact_mod_cast (show (2 : Int) ≤ radix by omega)
  have hradixNe : (radix : ℝ) ≠ 0 := ne_of_gt hradixPos
  have hprecisionNe : precision ≠ 0 := by omega
  have hx1Bound : Fbounded (beta:=beta) b x1 :=
    (FcanonicBound (beta:=beta) radix b x1) hx1Can
  have hx1BoundExt : Fbounded (beta:=beta) b' x1 := by
    constructor
    · simpa [← hbNum] using hx1Bound.1
    · exact le_trans (by omega : -b'.dExp ≤ -b.dExp) hx1Bound.2
  by_cases hx1ExpStrict : -b.dExp < x1.Fexp
  · have hx1ClosestExt :=
      closest_under_extended_of_canonical_above_min b b' radix precision r x1
        hbeta hradix hprecision hvNum hbNum hbExp hx1Can hx1ExpStrict
        hx1Closest
    refine ⟨x1, ?_, hx1ClosestExt⟩
    refine ⟨hx1Closest, hx1BoundExt, ?_, ?_⟩
    · rw [sub_self, abs_zero]
      positivity
    · intro _
      rfl
  · have hx1Exp : x1.Fexp = -b.dExp := by
      have hx1ExpLower := hx1Bound.2
      omega
    have hvNum' : b'.vNum = Zpower_nat radix precision := by
      rw [← hbNum]
      exact hvNum
    have hprecisionInt : (1 : Int) < (precision : Int) := by
      exact_mod_cast hprecision
    have htoNat : (precision : Int).toNat = precision := by
      simp
    let x2 : FloatSpec.Core.Defs.FlocqFloat beta :=
      RND_Closest (beta:=beta) b' radix (precision : Int) (fun _ => false) r
    have hx2Closest : Closest (beta:=beta) b' (radix : ℝ) r x2 := by
      have h := RND_Closest_correct (beta:=beta) b' radix
        (precision : Int) (fun _ => false) r
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RND_Closest_correct_check,
        Id.run, ULift.up_down, x2, htoNat, Int.cast_ofNat] using
        h ⟨hbeta, hradix, hprecisionInt, hvNum'⟩
    by_cases hx2ExpOld : -b.dExp ≤ x2.Fexp
    · have hx2BoundOld : Fbounded (beta:=beta) b x2 := by
        constructor
        · simpa [hbNum] using hx2Closest.1.1
        · exact hx2ExpOld
      have hx1ClosestExt : Closest (beta:=beta) b' (radix : ℝ) r x1 := by
        constructor
        · exact hx1BoundExt
        · intro f hfBound
          exact le_trans (hx1Closest.2 x2 hx2BoundOld)
            (hx2Closest.2 f hfBound)
      refine ⟨x1, ?_, hx1ClosestExt⟩
      refine ⟨hx1Closest, hx1BoundExt, ?_, ?_⟩
      · rw [sub_self, abs_zero]
        positivity
      · intro _
        rfl
    · have hx2ExpLt : x2.Fexp < -b.dExp := lt_of_not_ge hx2ExpOld
      have hx1Fulp :
          Fulp (beta:=beta) b radix precision x1 =
            (radix : ℝ) ^ (-b.dExp) := by
        have h := CanonicFulp (beta:=beta) radix b precision x1
        have h' :
            Fulp (beta:=beta) b radix precision x1 =
              (radix : ℝ) ^ x1.Fexp := by
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using
            h ⟨hx1Can, hbeta, hradix, hprecisionNe, hvNum⟩
        simpa [hx1Exp] using h'
      have hx1ClosestUlp :
          2 * |r - _root_.F2R (beta:=beta) x1| ≤
            Fulp (beta:=beta) b radix precision x1 := by
        have h := ClosestUlp (beta:=beta) b radix precision r x1
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          h ⟨hx1Closest, hbeta, hradix, hprecisionNe, hvNum⟩
      have hx1Error :
          |r - _root_.F2R (beta:=beta) x1| ≤
            (radix : ℝ) ^ (-b.dExp) / 2 := by
        rw [hx1Fulp] at hx1ClosestUlp
        nlinarith
      have hx2FulpLe :
          Fulp (beta:=beta) b' radix precision x2 ≤
            (radix : ℝ) ^ x2.Fexp := by
        have h := FulpLe (beta:=beta) radix b' precision x2
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpLe_check,
          Id.run, ULift.up_down, hbeta, Int.cast_ofNat] using
          h ⟨hx2Closest.1, hbeta, hradix⟩
      have hx2PowLePred :
          (radix : ℝ) ^ x2.Fexp ≤
            (radix : ℝ) ^ (-b.dExp - 1) :=
        zpow_le_zpow_right₀ hradixOne (by omega)
      have hunitEq :
          (radix : ℝ) ^ (-b.dExp) =
            (radix : ℝ) ^ (-b.dExp - 1) * (radix : ℝ) := by
        calc
          (radix : ℝ) ^ (-b.dExp) =
              (radix : ℝ) ^ ((-b.dExp - 1) + 1) := by
                congr 1
                omega
          _ = (radix : ℝ) ^ (-b.dExp - 1) * (radix : ℝ) :=
            zpow_add_one₀ hradixNe _
      have hpredLeHalf :
          (radix : ℝ) ^ (-b.dExp - 1) ≤
            (radix : ℝ) ^ (-b.dExp) / 2 := by
        have hpredNonneg :
            0 ≤ (radix : ℝ) ^ (-b.dExp - 1) :=
          le_of_lt (zpow_pos hradixPos _)
        nlinarith
      have hx2FulpHalf :
          Fulp (beta:=beta) b' radix precision x2 ≤
            (radix : ℝ) ^ (-b.dExp) / 2 :=
        le_trans hx2FulpLe (le_trans hx2PowLePred hpredLeHalf)
      have hx2ClosestUlp :
          2 * |r - _root_.F2R (beta:=beta) x2| ≤
            Fulp (beta:=beta) b' radix precision x2 := by
        have h := ClosestUlp (beta:=beta) b' radix precision r x2
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          h ⟨hx2Closest, hbeta, hradix, hprecisionNe, hvNum'⟩
      have hx2Error :
          |r - _root_.F2R (beta:=beta) x2| ≤
            (radix : ℝ) ^ (-b.dExp) / 4 := by
        nlinarith
      have hx1x2Triangle :
          |_root_.F2R (beta:=beta) x1 -
              _root_.F2R (beta:=beta) x2| ≤
            |r - _root_.F2R (beta:=beta) x1| +
              |r - _root_.F2R (beta:=beta) x2| := by
        have h := abs_sub_le (_root_.F2R (beta:=beta) x1) r
          (_root_.F2R (beta:=beta) x2)
        simpa [abs_sub_comm (_root_.F2R (beta:=beta) x1) r] using h
      refine ⟨x2, ?_, hx2Closest⟩
      refine ⟨hx1Closest, hx2Closest.1, ?_, ?_⟩
      · have hunitNonneg : 0 ≤ (radix : ℝ) ^ (-b.dExp) :=
          le_of_lt (zpow_pos hradixPos _)
        nlinarith
      · intro hx2Exp
        exact False.elim (hx2ExpOld hx2Exp)

/-! Coq generic Dekker theorem `Underf_Err2`. -/
theorem Underf_Err2 {beta : Int} [ValidRadix beta]
    (b b' : Fbound_skel) (radix : Int) (precision : Nat)
    (r : ℝ) (x1 : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix precision)
    (hprecision : 1 < precision)
    (hbNum : b.vNum = b'.vNum) (hbExp : b.dExp ≤ b'.dExp)
    (hx1Closest : Closest (beta:=beta) b (radix : ℝ) r x1) :
    ∃ x2 : FloatSpec.Core.Defs.FlocqFloat beta,
      Underf_Err (beta:=beta) b b' radix x1 x2 r (3 / 4 : ℝ) ∧
        Closest (beta:=beta) b' (radix : ℝ) r x2 := by
  let nx1 : FloatSpec.Core.Defs.FlocqFloat beta :=
    Fnormalize (beta:=beta) radix b precision x1
  have hprecisionNe : precision ≠ 0 := by omega
  have hnx1Value :
      _root_.F2R (beta:=beta) nx1 = _root_.F2R (beta:=beta) x1 := by
    have h := FnormalizeCorrect (beta:=beta) radix b precision x1
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
      Id.run, ULift.up_down, nx1, Int.cast_ofNat] using h ⟨hbeta, hradix⟩
  have hnx1Can : Fcanonic (beta:=beta) radix b nx1 := by
    have h := FnormalizeCanonic (beta:=beta) radix b precision x1
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
      Id.run, ULift.up_down, Fbounded', nx1, Int.cast_ofNat] using
      h ⟨hx1Closest.1, hx1Closest.1, hprecisionNe, hradix, hvNum⟩
  have hnx1Closest : Closest (beta:=beta) b (radix : ℝ) r nx1 := by
    constructor
    · exact (FcanonicBound (beta:=beta) radix b nx1) hnx1Can
    · intro g hg
      simpa [hnx1Value] using hx1Closest.2 g hg
  rcases Underf_Err2_aux (beta:=beta) b b' radix precision r nx1
      hbeta hradix hvNum hprecision hbNum hbExp hnx1Can hnx1Closest with
    ⟨x2, hUnder, hx2Closest⟩
  rcases hUnder with ⟨_hnxClosest, hx2Bound, hdist, hsame⟩
  refine ⟨x2, ?_, hx2Closest⟩
  refine ⟨hx1Closest, hx2Bound, ?_, ?_⟩
  · simpa [hnx1Value] using hdist
  · intro hx2Exp
    exact hnx1Value.symm.trans (hsame hx2Exp)

private lemma closest_add_exact_of_exp_le {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (p q z : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hprecision : precision ≠ 0)
    (hvNum : b.vNum = Zpower_nat radix precision)
    (hClosest : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q) z)
    (hzExp : z.Fexp ≤ min p.Fexp q.Fexp) :
    _root_.F2R (beta:=beta) z =
      _root_.F2R (beta:=beta) p + _root_.F2R (beta:=beta) q := by
  subst beta
  have hradixPos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (show (0 : Int) < radix by omega)
  let sum : FloatSpec.Core.Defs.FlocqFloat radix :=
    Fplus (beta:=radix) p q
  have hsumValue :
      _root_.F2R (beta:=radix) sum =
        _root_.F2R (beta:=radix) p + _root_.F2R (beta:=radix) q := by
    have h := Fplus_correct (beta:=radix) p q
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fplus_correct_check,
      Id.run, ULift.up_down, sum, Int.cast_ofNat] using h hradix
  have hsumExp : sum.Fexp = min p.Fexp q.Fexp := by
    simpa only [sum, Int.cast_ofNat] using (Fplus_exp_eq_min p q)
  have hdiffNonneg : 0 ≤ sum.Fexp - z.Fexp := by
    rw [hsumExp]
    omega
  let shift : Nat := (sum.Fexp - z.Fexp).toNat
  have hshiftCast : (shift : Int) = sum.Fexp - z.Fexp := by
    dsimp [shift]
    rw [Int.toNat_of_nonneg hdiffNonneg]
  let aligned : FloatSpec.Core.Defs.FlocqFloat radix :=
    Fshift (beta:=radix) radix shift sum
  have halignedValue :
      _root_.F2R (beta:=radix) aligned =
        _root_.F2R (beta:=radix) sum := by
    have h := FshiftCorrect (beta:=radix) radix shift sum
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FshiftCorrect_check,
      Id.run, ULift.up_down, aligned, Int.cast_ofNat] using h ⟨rfl, hradix⟩
  have halignedExp : aligned.Fexp = z.Fexp := by
    dsimp [aligned, Fshift]
    rw [hshiftCast]
    omega
  have herrorUlp :
      2 * |(_root_.F2R (beta:=radix) p +
          _root_.F2R (beta:=radix) q) -
          _root_.F2R (beta:=radix) z| ≤
        Fulp (beta:=radix) b radix precision z := by
    have h := ClosestUlp (beta:=radix) b radix precision
      (_root_.F2R (beta:=radix) p + _root_.F2R (beta:=radix) q) z
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hClosest, rfl, hradix, hprecision, hvNum⟩
  have hFulpLe :
      Fulp (beta:=radix) b radix precision z ≤
        (radix : ℝ) ^ z.Fexp := by
    have h := FulpLe (beta:=radix) radix b precision z
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpLe_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hClosest.1, rfl, hradix⟩
  have herrorLe :
      2 * |(_root_.F2R (beta:=radix) p +
          _root_.F2R (beta:=radix) q) -
          _root_.F2R (beta:=radix) z| ≤
        (radix : ℝ) ^ z.Fexp :=
    le_trans herrorUlp hFulpLe
  have hscalePos : 0 < (radix : ℝ) ^ z.Fexp :=
    zpow_pos hradixPos _
  let diff : Int := aligned.Fnum - z.Fnum
  have herrorScale :
      |_root_.F2R (beta:=radix) aligned -
          _root_.F2R (beta:=radix) z| =
        |(diff : ℝ)| * (radix : ℝ) ^ z.Fexp := by
    have hfactor :
        _root_.F2R (beta:=radix) aligned -
            _root_.F2R (beta:=radix) z =
          (diff : ℝ) * (radix : ℝ) ^ z.Fexp := by
      simp only [_root_.F2R, FloatSpec.Core.Defs.F2R]
      rw [halignedExp]
      dsimp [diff]
      push_cast
      ring
    rw [hfactor, abs_mul, abs_of_pos hscalePos]
  have herrorAligned :
      |(_root_.F2R (beta:=radix) p +
          _root_.F2R (beta:=radix) q) -
          _root_.F2R (beta:=radix) z| =
        |_root_.F2R (beta:=radix) aligned -
          _root_.F2R (beta:=radix) z| := by
    rw [halignedValue, hsumValue]
  have hdiffZero : diff = 0 := by
    by_contra hdiffNe
    have hdiffInt : (1 : Int) ≤ |diff| := Int.one_le_abs hdiffNe
    have hdiffReal : (1 : ℝ) ≤ |(diff : ℝ)| := by
      exact_mod_cast hdiffInt
    have hscaleLeError :
        (radix : ℝ) ^ z.Fexp ≤
          |(_root_.F2R (beta:=radix) p +
              _root_.F2R (beta:=radix) q) -
              _root_.F2R (beta:=radix) z| := by
      rw [herrorAligned, herrorScale]
      simpa using mul_le_mul_of_nonneg_right hdiffReal
        (le_of_lt hscalePos)
    nlinarith
  have halignedEq :
      _root_.F2R (beta:=radix) aligned =
        _root_.F2R (beta:=radix) z := by
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R]
    rw [halignedExp]
    have hnumEq : aligned.Fnum = z.Fnum := by
      dsimp [diff] at hdiffZero
      omega
    rw [hnumEq]
  calc
    _root_.F2R (beta:=radix) z =
        _root_.F2R (beta:=radix) aligned := halignedEq.symm
    _ = _root_.F2R (beta:=radix) sum := halignedValue
    _ = _root_.F2R (beta:=radix) p +
        _root_.F2R (beta:=radix) q := hsumValue

private lemma closest_abs_le_of_bounded_upper {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (r : ℝ)
    (q upper : FloatSpec.Core.Defs.FlocqFloat beta)
    (hClosest : Closest (beta:=beta) b (radix : ℝ) r q)
    (hUpperBound : Fbounded (beta:=beta) b upper)
    (hrAbs : |r| ≤ _root_.F2R (beta:=beta) upper) :
    |_root_.F2R (beta:=beta) q| ≤ _root_.F2R (beta:=beta) upper := by
  let lower : FloatSpec.Core.Defs.FlocqFloat beta := Fopp (beta:=beta) upper
  have hLowerBound : Fbounded (beta:=beta) b lower := by
    simpa [lower, Fbounded, Fopp, FloatSpec.Calc.Operations.Fopp, abs_neg]
      using hUpperBound
  have hLowerValue :
      _root_.F2R (beta:=beta) lower =
        -_root_.F2R (beta:=beta) upper := by
    simpa [lower] using
      (FloatSpec.Calc.Operations.F2R_opp (beta:=beta) upper) True.intro
  have hminmax :
      isMin (beta:=beta) b radix r q ∨ isMax (beta:=beta) b radix r q := by
    have h := ClosestMinOrMax_from_split_radix_payload (beta:=beta) b radix (radix : ℝ)
    have hMinOrMaxP :
        MinOrMaxP_float (beta:=beta) b radix
          (Closest (beta:=beta) b (radix : ℝ)) := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMinOrMax_check, MinOrMaxP_float,
        Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
    exact hMinOrMaxP r q hClosest
  have hr_le_upper : r ≤ _root_.F2R (beta:=beta) upper :=
    le_trans (le_abs_self r) hrAbs
  have hlower_le_r : _root_.F2R (beta:=beta) lower ≤ r := by
    rw [hLowerValue]
    exact le_trans (neg_le_neg hrAbs) (neg_abs_le r)
  rcases hminmax with hmin | hmax
  · rcases hmin with ⟨_hqBound, hq_le_r, hgreatest⟩
    have hlower_le_q : _root_.F2R (beta:=beta) lower ≤
        _root_.F2R (beta:=beta) q :=
      hgreatest lower hLowerBound hlower_le_r
    have hq_le_upper : _root_.F2R (beta:=beta) q ≤
        _root_.F2R (beta:=beta) upper :=
      le_trans hq_le_r hr_le_upper
    rw [abs_le]
    exact ⟨by simpa [hLowerValue] using hlower_le_q, hq_le_upper⟩
  · rcases hmax with ⟨_hqBound, hr_le_q, hleast⟩
    have hq_le_upper : _root_.F2R (beta:=beta) q ≤
        _root_.F2R (beta:=beta) upper :=
      hleast upper hUpperBound hr_le_upper
    have hlower_le_q : _root_.F2R (beta:=beta) lower ≤
        _root_.F2R (beta:=beta) q :=
      le_trans hlower_le_r hr_le_q
    rw [abs_le]
    exact ⟨by simpa [hLowerValue] using hlower_le_q, hq_le_upper⟩

/-! Coq generic Dekker theorem `Underf_Err3`. -/
theorem Underf_Err3 {beta : Int} [ValidRadix beta]
    (b b' : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (x x' y y' z' z : FloatSpec.Core.Defs.FlocqFloat beta)
    (rx ry epsx epsy : ℝ)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix precision)
    (hprecision : 1 < precision)
    (hbNum : b.vNum = b'.vNum) (hbExp : b.dExp ≤ b'.dExp)
    (hxUnder : Underf_Err (beta:=beta) b b' radix x x' rx epsx)
    (hyUnder : Underf_Err (beta:=beta) b b' radix y y' ry epsy)
    (hbudget : epsx + epsy ≤ (radix : ℝ) ^ ((precision : Int) - 1) - 1)
    (hz'Bound : Fbounded (beta:=beta) b' z')
    (hz'Val : _root_.F2R (beta:=beta) z' =
      _root_.F2R (beta:=beta) x' - _root_.F2R (beta:=beta) y')
    (hz'ExpX : z'.Fexp ≤ x'.Fexp) (hz'ExpY : z'.Fexp ≤ y'.Fexp)
    (hzClosest : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) y) z) :
    Underf_Err (beta:=beta) b b' radix z z'
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) y)
      (epsx + epsy) := by
  classical
  have hradixPos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (show (0 : Int) < radix by omega)
  have hradixNe : (radix : ℝ) ≠ 0 := ne_of_gt hradixPos
  have hradixTwo : (2 : ℝ) ≤ (radix : ℝ) := by
    exact_mod_cast (show (2 : Int) ≤ radix by omega)
  have hprecisionNe : precision ≠ 0 := by omega
  have hvNum_gt : 1 < b.vNum := by
    have h := vNumbMoreThanOne b radix precision
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, vNumbMoreThanOne_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨by omega, hradix, hvNum⟩
  have hbetaGt : 1 < beta := by omega
  rcases hxUnder with ⟨hxClosest, _hx'Bound, hxDist, hxSame⟩
  rcases hyUnder with ⟨hyClosest, _hy'Bound, hyDist, hySame⟩
  have hSameIfOld :
      -b.dExp ≤ z'.Fexp →
        _root_.F2R (beta:=beta) z = _root_.F2R (beta:=beta) z' := by
    intro hz'ExpOld
    have hz'BoundOld : Fbounded (beta:=beta) b z' :=
      Fbounded_of_same_vNum_of_exp_ge b b' z' hbNum hz'Bound hz'ExpOld
    have hx'ExpOld : -b.dExp ≤ x'.Fexp := le_trans hz'ExpOld hz'ExpX
    have hy'ExpOld : -b.dExp ≤ y'.Fexp := le_trans hz'ExpOld hz'ExpY
    have hxExact := hxSame hx'ExpOld
    have hyExact := hySame hy'ExpOld
    have hzClosestZ' :
        Closest (beta:=beta) b (radix : ℝ)
          (_root_.F2R (beta:=beta) z') z := by
      simpa [hz'Val, hxExact, hyExact] using hzClosest
    have hzeroDist : |_root_.F2R (beta:=beta) z -
        _root_.F2R (beta:=beta) z'| = 0 := by
      have hdist := hzClosestZ'.2 z' hz'BoundOld
      rw [sub_self, abs_zero] at hdist
      exact le_antisymm hdist (abs_nonneg _)
    exact sub_eq_zero.mp (abs_eq_zero.mp hzeroDist)
  refine ⟨hzClosest, hz'Bound, ?_, ?_⟩
  by_cases hz'ExpOld : -b.dExp ≤ z'.Fexp
  · have hzeroDist : |_root_.F2R (beta:=beta) z -
        _root_.F2R (beta:=beta) z'| = 0 := by
      rw [hSameIfOld hz'ExpOld, sub_self, abs_zero]
    rw [hzeroDist]
    have hunitNonneg : 0 ≤ (radix : ℝ) ^ (-b.dExp) :=
      le_of_lt (zpow_pos hradixPos _)
    have hxeps_nonneg : 0 ≤ epsx := by
      have hmul_nonneg : 0 ≤ epsx * (radix : ℝ) ^ (-b.dExp) :=
        le_trans (abs_nonneg _) hxDist
      exact nonneg_of_mul_nonneg_left hmul_nonneg
        (zpow_pos hradixPos _)
    have hyeps_nonneg : 0 ≤ epsy := by
      have hmul_nonneg : 0 ≤ epsy * (radix : ℝ) ^ (-b.dExp) :=
        le_trans (abs_nonneg _) hyDist
      exact nonneg_of_mul_nonneg_left hmul_nonneg
        (zpow_pos hradixPos _)
    nlinarith
  · have hz'ExpLt : z'.Fexp < -b.dExp := lt_of_not_ge hz'ExpOld
    have hxeps_nonneg : 0 ≤ epsx := by
      have hmul_nonneg : 0 ≤ epsx * (radix : ℝ) ^ (-b.dExp) :=
        le_trans (abs_nonneg _) hxDist
      exact nonneg_of_mul_nonneg_left hmul_nonneg
        (zpow_pos hradixPos _)
    have hyeps_nonneg : 0 ≤ epsy := by
      have hmul_nonneg : 0 ≤ epsy * (radix : ℝ) ^ (-b.dExp) :=
        le_trans (abs_nonneg _) hyDist
      exact nonneg_of_mul_nonneg_left hmul_nonneg
        (zpow_pos hradixPos _)
    let unit : ℝ := (radix : ℝ) ^ (-b.dExp)
    let pred : FloatSpec.Core.Defs.FlocqFloat beta :=
      ⟨pPred b.vNum, -b.dExp⟩
    have hpredCan : Fcanonic (beta:=beta) radix b pred := by
      exact FcanonicPpred (beta:=beta) radix b precision (-b.dExp)
        hradix (by omega) hvNum (by omega)
    have hpredBound : Fbounded (beta:=beta) b pred :=
      (FcanonicBound (beta:=beta) radix b pred) hpredCan
    have hpredValue :
        _root_.F2R (beta:=beta) pred =
          ((radix : ℝ) ^ (precision : Int) - 1) * unit := by
      subst beta
      have hvNumReal : (b.vNum : ℝ) = (radix : ℝ) ^ (precision : Int) := by
        rw [hvNum, Zpower_nat, Int.cast_pow, ← zpow_natCast]
      simp [pred, pPred, unit, _root_.F2R, FloatSpec.Core.Defs.F2R,
        hvNumReal, sub_mul]
    have hpredNonneg : 0 ≤ _root_.F2R (beta:=beta) pred := by
      have hpPredPos : 0 < pPred b.vNum := by
        simp [pPred]
        omega
      have hpPredPosR : 0 < (pPred b.vNum : ℝ) := by exact_mod_cast hpPredPos
      have hpowPos : 0 < (beta : ℝ) ^ pred.Fexp := by
        have hbetaPos : (0 : ℝ) < (beta : ℝ) := by exact_mod_cast (by omega : (0 : Int) < beta)
        exact zpow_pos hbetaPos _
      dsimp [pred, _root_.F2R, FloatSpec.Core.Defs.F2R]
      positivity
    have hz'Small :
        |_root_.F2R (beta:=beta) z'| ≤
          ((radix : ℝ) ^ (precision : Int) - 1) *
            (radix : ℝ) ^ (-b.dExp - 1) := by
      have hz'ExpLe : z'.Fexp ≤ -b.dExp - 1 := by omega
      have h := maxMax1_from_expanded_bound_payload (beta:=beta) b' z' (-b.dExp - 1)
      have hmax :
          |_root_.F2R (beta:=beta) z'| ≤
            _root_.F2R (beta:=beta) ⟨b'.vNum - 1, -b.dExp - 1⟩ := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, maxMax1_check,
          Id.run, ULift.up_down, Fbounded', Int.cast_ofNat] using
          h ⟨hz'Bound, hz'ExpLe, hz'Bound, hbetaGt⟩
      have hval :
          _root_.F2R (beta:=beta) ⟨b'.vNum - 1, -b.dExp - 1⟩ =
            ((radix : ℝ) ^ (precision : Int) - 1) *
              (radix : ℝ) ^ (-b.dExp - 1) := by
        subst beta
        have hvNumReal : (b'.vNum : ℝ) = (radix : ℝ) ^ (precision : Int) := by
          rw [← hbNum, hvNum, Zpower_nat, Int.cast_pow, ← zpow_natCast]
        simp [_root_.F2R, FloatSpec.Core.Defs.F2R, hvNumReal, sub_mul]
      simpa [hval] using hmax
    have htargetAbs :
        |_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) y| ≤
          _root_.F2R (beta:=beta) pred := by
        have herrTri :
            |(_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) x') +
                -(_root_.F2R (beta:=beta) y - _root_.F2R (beta:=beta) y')| ≤
              |_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) x'| +
                |_root_.F2R (beta:=beta) y - _root_.F2R (beta:=beta) y'| := by
          simpa [abs_neg, abs_sub_comm (_root_.F2R (beta:=beta) y')
            (_root_.F2R (beta:=beta) y)] using
            abs_add_le (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) x')
              (-(_root_.F2R (beta:=beta) y - _root_.F2R (beta:=beta) y'))
        have herrBound :
            |(_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) x') +
                -(_root_.F2R (beta:=beta) y - _root_.F2R (beta:=beta) y')| ≤
              (epsx + epsy) * unit := by
          calc
            |(_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) x') +
                -(_root_.F2R (beta:=beta) y - _root_.F2R (beta:=beta) y')|
                ≤ |_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) x'| +
                    |_root_.F2R (beta:=beta) y - _root_.F2R (beta:=beta) y'| := herrTri
            _ ≤ epsx * unit + epsy * unit := by
              exact add_le_add (by simpa [unit] using hxDist) (by simpa [unit] using hyDist)
            _ = (epsx + epsy) * unit := by ring
        have hdecomp :
            _root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) y =
              ((_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) x') +
                -(_root_.F2R (beta:=beta) y - _root_.F2R (beta:=beta) y')) +
                _root_.F2R (beta:=beta) z' := by
          rw [hz'Val]
          ring
        have htargetTri :
            |_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) y| ≤
              |(_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) x') +
                -(_root_.F2R (beta:=beta) y - _root_.F2R (beta:=beta) y')| +
                |_root_.F2R (beta:=beta) z'| := by
          rw [hdecomp]
          exact abs_add_le _ _
        have hpowStep :
            unit = (radix : ℝ) * (radix : ℝ) ^ (-b.dExp - 1) := by
          calc
            unit = (radix : ℝ) ^ ((-b.dExp - 1) + 1) := by
              dsimp [unit]
              congr 1
              omega
            _ = (radix : ℝ) ^ (-b.dExp - 1) * (radix : ℝ) :=
              zpow_add_one₀ hradixNe _
            _ = (radix : ℝ) * (radix : ℝ) ^ (-b.dExp - 1) := by ring
        have hprecPow :
            (radix : ℝ) ^ (precision : Int) =
              (radix : ℝ) * (radix : ℝ) ^ ((precision : Int) - 1) := by
          calc
            (radix : ℝ) ^ (precision : Int) =
                (radix : ℝ) ^ (((precision : Int) - 1) + 1) := by
                  congr 1
                  ring
            _ = (radix : ℝ) ^ ((precision : Int) - 1) * (radix : ℝ) :=
              zpow_add_one₀ hradixNe _
            _ = (radix : ℝ) * (radix : ℝ) ^ ((precision : Int) - 1) := by ring
        have hbudgetUnit :
            (epsx + epsy) * unit ≤
              ((radix : ℝ) ^ ((precision : Int) - 1) - 1) * unit := by
          exact mul_le_mul_of_nonneg_right hbudget
            (le_of_lt (zpow_pos hradixPos _))
        have htotal :
            (epsx + epsy) * unit +
              ((radix : ℝ) ^ (precision : Int) - 1) *
                (radix : ℝ) ^ (-b.dExp - 1) ≤
              ((radix : ℝ) ^ (precision : Int) - 1) * unit := by
          rw [hpowStep] at hbudgetUnit
          rw [hpowStep, hprecPow]
          calc
            (epsx + epsy) * ((radix : ℝ) *
                  (radix : ℝ) ^ (-b.dExp - 1)) +
                ((radix : ℝ) * (radix : ℝ) ^ ((precision : Int) - 1) - 1) *
                  (radix : ℝ) ^ (-b.dExp - 1)
                ≤
              ((radix : ℝ) ^ ((precision : Int) - 1) - 1) *
                  ((radix : ℝ) * (radix : ℝ) ^ (-b.dExp - 1)) +
                ((radix : ℝ) * (radix : ℝ) ^ ((precision : Int) - 1) - 1) *
                  (radix : ℝ) ^ (-b.dExp - 1) :=
                by
                  simpa [add_comm, add_left_comm, add_assoc] using
                    add_le_add_right hbudgetUnit
                      (((radix : ℝ) * (radix : ℝ) ^ ((precision : Int) - 1) - 1) *
                        (radix : ℝ) ^ (-b.dExp - 1))
            _ ≤ ((radix : ℝ) * (radix : ℝ) ^ ((precision : Int) - 1) - 1) *
                  ((radix : ℝ) * (radix : ℝ) ^ (-b.dExp - 1)) := by
              have hApos : 0 < (radix : ℝ) ^ ((precision : Int) - 1) :=
                zpow_pos hradixPos _
              have hSpos : 0 < (radix : ℝ) ^ (-b.dExp - 1) :=
                zpow_pos hradixPos _
              have hleftover :
                  0 ≤ ((radix : ℝ) * (radix : ℝ) ^ ((precision : Int) - 1)) *
                      ((radix : ℝ) - 2) * (radix : ℝ) ^ (-b.dExp - 1) +
                    (radix : ℝ) ^ (-b.dExp - 1) := by
                exact add_nonneg
                  (mul_nonneg
                    (mul_nonneg
                      (mul_nonneg (le_of_lt hradixPos) (le_of_lt hApos))
                      (sub_nonneg.mpr hradixTwo))
                    (le_of_lt hSpos))
                  (le_of_lt hSpos)
              nlinarith [hleftover]
        calc
          |_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) y|
              ≤ |(_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) x') +
                  -(_root_.F2R (beta:=beta) y - _root_.F2R (beta:=beta) y')| +
                  |_root_.F2R (beta:=beta) z'| := htargetTri
          _ ≤ (epsx + epsy) * unit +
                ((radix : ℝ) ^ (precision : Int) - 1) *
                  (radix : ℝ) ^ (-b.dExp - 1) :=
            add_le_add herrBound hz'Small
          _ ≤ ((radix : ℝ) ^ (precision : Int) - 1) * unit := htotal
          _ = _root_.F2R (beta:=beta) pred := hpredValue.symm
    let nz : FloatSpec.Core.Defs.FlocqFloat beta :=
      Fnormalize (beta:=beta) radix b precision z
    have hnzValue :
        _root_.F2R (beta:=beta) nz = _root_.F2R (beta:=beta) z := by
      have h := FnormalizeCorrect (beta:=beta) radix b precision z
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
        Id.run, ULift.up_down, nz, Int.cast_ofNat] using h ⟨hbeta, hradix⟩
    have hnzCan : Fcanonic (beta:=beta) radix b nz := by
      have h := FnormalizeCanonic (beta:=beta) radix b precision z
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
        Id.run, ULift.up_down, Fbounded', nz, Int.cast_ofNat] using
        h ⟨hzClosest.1, hzClosest.1, hprecisionNe, hradix, hvNum⟩
    have hzAbs :
        |_root_.F2R (beta:=beta) z| ≤ _root_.F2R (beta:=beta) pred :=
      closest_abs_le_of_bounded_upper b radix
        (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) y)
        z pred hzClosest hpredBound htargetAbs
    have hnzAbs :
        |_root_.F2R (beta:=beta) nz| ≤ |_root_.F2R (beta:=beta) pred| := by
      rw [hnzValue]
      simpa [abs_of_nonneg hpredNonneg] using hzAbs
    have hnzExpLePred :
        nz.Fexp ≤ pred.Fexp := by
      have h := Fcanonic_Rle_Zle (beta:=beta) radix b nz pred hbetaGt hbeta.symm
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fcanonic_Rle_Zle_check,
        ULift.down_up, Fcanonic', Int.cast_ofNat] using h ⟨hnzCan, hpredCan, hnzAbs⟩
    have hnzExpLeMin : nz.Fexp ≤ min x.Fexp (Fopp (beta:=beta) y).Fexp := by
      have hxExpLower := hxClosest.1.2
      have hyExpLower := hyClosest.1.2
      dsimp [pred] at hnzExpLePred
      exact le_min (le_trans hnzExpLePred hxExpLower)
        (by
          simpa [Fopp, FloatSpec.Calc.Operations.Fopp] using
            le_trans hnzExpLePred hyExpLower)
    have hyOppValue :
        _root_.F2R (beta:=beta) (Fopp (beta:=beta) y) =
          -_root_.F2R (beta:=beta) y := by
      have h := Fopp_correct (beta:=beta) y
      simpa [wp, PostCond.noThrow, pure, Fopp_correct_check, Id.run,
        ULift.up_down, Fopp] using h True.intro
    have hnzClosest :
        Closest (beta:=beta) b (radix : ℝ)
          (_root_.F2R (beta:=beta) x +
            _root_.F2R (beta:=beta) (Fopp (beta:=beta) y)) nz := by
      constructor
      · exact (FcanonicBound (beta:=beta) radix b nz) hnzCan
      · intro g hg
        have hbase := hzClosest.2 g hg
        simpa [hnzValue, hyOppValue, sub_eq_add_neg] using hbase
    have hzExactNorm :
        _root_.F2R (beta:=beta) nz =
          _root_.F2R (beta:=beta) x +
            _root_.F2R (beta:=beta) (Fopp (beta:=beta) y) :=
      closest_add_exact_of_exp_le b radix precision
        x (Fopp (beta:=beta) y) nz hbeta hradix hprecisionNe hvNum
        hnzClosest hnzExpLeMin
    have hzExact :
        _root_.F2R (beta:=beta) z =
          _root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) y := by
      rw [← hnzValue, hzExactNorm, hyOppValue]
      ring
    have hdecomp :
        _root_.F2R (beta:=beta) z - _root_.F2R (beta:=beta) z' =
          ((_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) x') +
            -(_root_.F2R (beta:=beta) y - _root_.F2R (beta:=beta) y')) := by
      rw [hzExact, hz'Val]
      ring
    rw [hdecomp]
    have htri :
        |(_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) x') +
            -(_root_.F2R (beta:=beta) y - _root_.F2R (beta:=beta) y')| ≤
          |_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) x'| +
            |_root_.F2R (beta:=beta) y - _root_.F2R (beta:=beta) y'| := by
      simpa [abs_neg, abs_sub_comm (_root_.F2R (beta:=beta) y')
        (_root_.F2R (beta:=beta) y)] using
        abs_add_le (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) x')
          (-(_root_.F2R (beta:=beta) y - _root_.F2R (beta:=beta) y'))
    calc
      |(_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) x') +
          -(_root_.F2R (beta:=beta) y - _root_.F2R (beta:=beta) y')|
          ≤ |_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) x'| +
              |_root_.F2R (beta:=beta) y - _root_.F2R (beta:=beta) y'| := htri
      _ ≤ epsx * (radix : ℝ) ^ (-b.dExp) +
            epsy * (radix : ℝ) ^ (-b.dExp) :=
        add_le_add hxDist hyDist
      _ = (epsx + epsy) * (radix : ℝ) ^ (-b.dExp) := by ring
  · intro hz'Exp
    exact hSameIfOld hz'Exp

/-! Coq generic Dekker theorem `Underf_Err3_bis`. -/
theorem Underf_Err3_bis {beta : Int} [ValidRadix beta]
    (b b' : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (x x' y y' z' z : FloatSpec.Core.Defs.FlocqFloat beta)
    (rx ry epsx epsy : ℝ)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix precision)
    (hprecision : 1 < precision)
    (hprecision4 : 4 ≤ precision)
    (hbNum : b.vNum = b'.vNum) (hbExp : b.dExp ≤ b'.dExp)
    (hxUnder : Underf_Err (beta:=beta) b b' radix x x' rx epsx)
    (hyUnder : Underf_Err (beta:=beta) b b' radix y y' ry epsy)
    (hbudget7 : epsx + epsy ≤ 7)
    (hz'Bound : Fbounded (beta:=beta) b' z')
    (hz'Val : _root_.F2R (beta:=beta) z' =
      _root_.F2R (beta:=beta) x' - _root_.F2R (beta:=beta) y')
    (hz'ExpX : z'.Fexp ≤ x'.Fexp) (hz'ExpY : z'.Fexp ≤ y'.Fexp)
    (hzClosest : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) y) z) :
    Underf_Err (beta:=beta) b b' radix z z'
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) y)
      (epsx + epsy) := by
  have hbudget :
      epsx + epsy ≤ (radix : ℝ) ^ ((precision : Int) - 1) - 1 := by
    have hradixTwo : (2 : ℝ) ≤ (radix : ℝ) := by
      exact_mod_cast (show (2 : Int) ≤ radix by omega)
    have hpowBase : (2 : ℝ) ^ (3 : Int) ≤
        (radix : ℝ) ^ (3 : Int) := by
      exact zpow_le_zpow_left₀ (by norm_num : (0 : Int) ≤ (3 : Int))
        (by norm_num : (0 : ℝ) ≤ (2 : ℝ)) hradixTwo
    have hradixOne : (1 : ℝ) ≤ (radix : ℝ) :=
      le_trans (by norm_num : (1 : ℝ) ≤ 2) hradixTwo
    have hpowExp : (radix : ℝ) ^ (3 : Int) ≤
        (radix : ℝ) ^ ((precision : Int) - 1) := by
      exact zpow_le_zpow_right₀ hradixOne
        (by omega : (3 : Int) ≤ (precision : Int) - 1)
    have hpow : (8 : ℝ) ≤ (radix : ℝ) ^ ((precision : Int) - 1) := by
      norm_num at hpowBase
      exact le_trans hpowBase hpowExp
    nlinarith
  exact Underf_Err3 (beta:=beta) b b' radix precision
    x x' y y' z' z rx ry epsx epsy hbeta hradix hvNum hprecision
    hbNum hbExp hxUnder hyUnder hbudget hz'Bound hz'Val hz'ExpX
    hz'ExpY hzClosest

/-- Coq Sec1 product error bound `eLe`.

The exported Lean statement keeps exactly the section facts used by the Coq
proof: the radix/precision hypotheses, the `s`/`t` range facts needed to recover
the precision side conditions, the normality of the product operands, the lower
product exponent bound, closestness of the rounded product, and the exact
decomposition of the residual.  The later split operands and the `Hst1`/`Hst2`
range facts are not consumed by this proof. -/
theorem eLe {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x y r e : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hSLe : 2 ≤ s) (hSGe : s ≤ t - 2)
    (hxNormal : Fnormal (beta:=beta) radix b x)
    (hyNormal : Fnormal (beta:=beta) radix b y)
    (hK : -b.dExp ≤ x.Fexp + y.Fexp)
    (hClosest : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * _root_.F2R (beta:=beta) y) r)
    (hee : _root_.F2R (beta:=beta) x * _root_.F2R (beta:=beta) y =
      _root_.F2R (beta:=beta) r + _root_.F2R (beta:=beta) e) :
    |_root_.F2R (beta:=beta) e| ≤
      (radix : ℝ) ^ ((t : Int) + x.Fexp + y.Fexp) / 2 := by
  subst beta
  let prod : ℝ := _root_.F2R (beta:=radix) x * _root_.F2R (beta:=radix) y
  let z : Int := x.Fexp + y.Fexp
  let upper : FloatSpec.Core.Defs.FlocqFloat radix :=
    ⟨b.vNum - 1, (t : Int) + z⟩
  let lower : FloatSpec.Core.Defs.FlocqFloat radix :=
    ⟨-(b.vNum - 1), (t : Int) + z⟩
  have ht_ne : t ≠ 0 := by omega
  have ht_pos : 0 < t := Nat.pos_of_ne_zero ht_ne
  have hvNum_gt : 1 < b.vNum := by
    rw [hvNum]
    cases t with
    | zero => contradiction
    | succ t' =>
        cases t' with
        | zero => omega
        | succ u =>
            unfold Zpower_nat
            have hpow_pos : 0 < radix ^ u := pow_pos (by omega : 0 < radix) u
            have hle : radix ≤ radix ^ u * (radix * radix) := by
              nlinarith [hpow_pos, hradix]
            exact lt_of_lt_of_le hradix
              (by simpa [pow_succ, mul_assoc, mul_comm, mul_left_comm] using hle)
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (by omega : (0 : Int) < radix)
  have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
  have hradix_ge_one : (1 : ℝ) ≤ (radix : ℝ) := by
    exact_mod_cast (by omega : (1 : Int) ≤ radix)
  have hClosestUlp := ClosestUlp (beta:=radix) b radix t prod r
  have htwo_abs_diff_le_fulp :
      2 * |prod - _root_.F2R (beta:=radix) r| ≤
        Fulp (beta:=radix) b radix t r := by
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check, Id.run,
      ULift.up_down, prod, Int.cast_ofNat] using
      hClosestUlp ⟨hClosest, rfl, hradix, ht_ne, hvNum⟩
  have hdiff_e : prod - _root_.F2R (beta:=radix) r =
      _root_.F2R (beta:=radix) e := by
    dsimp [prod] at hee ⊢
    linarith
  have htwo_abs_e_le_fulp :
      2 * |_root_.F2R (beta:=radix) e| ≤
        Fulp (beta:=radix) b radix t r := by
    simpa [hdiff_e] using htwo_abs_diff_le_fulp
  have hxBound : Fbounded (beta:=radix) b x := hxNormal.1
  have hyBound : Fbounded (beta:=radix) b y := hyNormal.1
  have hupperBounded : Fbounded (beta:=radix) b upper := by
    dsimp [upper, Fbounded]
    constructor
    · have hnonneg : 0 ≤ b.vNum - 1 := by omega
      rw [abs_of_nonneg hnonneg]
      omega
    · dsimp [z]
      omega
  have hlowerBounded : Fbounded (beta:=radix) b lower := by
    dsimp [lower, Fbounded]
    constructor
    · have hnonneg : 0 ≤ b.vNum - 1 := by omega
      rw [abs_neg, abs_of_nonneg hnonneg]
      omega
    · dsimp [z]
      omega
  have hxAbsLe :
      |_root_.F2R (beta:=radix) x| ≤
        _root_.F2R (beta:=radix)
          (⟨b.vNum - 1, x.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat radix) := by
    have h := maxMax1_from_expanded_bound_payload (beta:=radix) b x x.Fexp
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, maxMax1_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨hxBound, le_rfl, hxBound, hradix⟩
  have hyAbsLe :
      |_root_.F2R (beta:=radix) y| ≤
        _root_.F2R (beta:=radix)
          (⟨b.vNum - 1, y.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat radix) := by
    have h := maxMax1_from_expanded_bound_payload (beta:=radix) b y y.Fexp
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, maxMax1_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨hyBound, le_rfl, hyBound, hradix⟩
  have hbaseX_nonneg :
      0 ≤ _root_.F2R (beta:=radix)
        (⟨b.vNum - 1, x.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat radix) := by
    have hnum : (0 : Int) ≤ b.vNum - 1 := by omega
    have hnumR : (0 : ℝ) ≤ (b.vNum : ℝ) - 1 := by exact_mod_cast hnum
    have hpow_nonneg : 0 ≤ (radix : ℝ) ^ x.Fexp :=
      le_of_lt (zpow_pos hradix_pos _)
    simp [_root_.F2R, FloatSpec.Core.Defs.F2R]
    exact mul_nonneg hnumR hpow_nonneg
  have hprodAbsLeBase :
      |prod| ≤
        _root_.F2R (beta:=radix)
          (⟨b.vNum - 1, x.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat radix) *
        _root_.F2R (beta:=radix)
          (⟨b.vNum - 1, y.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat radix) := by
    dsimp [prod]
    rw [abs_mul]
    exact mul_le_mul hxAbsLe hyAbsLe (abs_nonneg _) hbaseX_nonneg
  have hpred_le_pow :
      ((b.vNum - 1 : Int) : ℝ) ≤ (radix : ℝ) ^ (t : Int) := by
    have hle_int : b.vNum - 1 ≤ b.vNum := by omega
    have hle_real : ((b.vNum - 1 : Int) : ℝ) ≤ (b.vNum : ℝ) := by
      exact_mod_cast hle_int
    have hv_cast : (b.vNum : ℝ) = (radix : ℝ) ^ t := by
      simp [hvNum, Zpower_nat]
    have hv_cast_int : (b.vNum : ℝ) = (radix : ℝ) ^ (t : Int) := by
      rw [hv_cast]
      rw [← zpow_natCast]
    exact le_trans hle_real (le_of_eq hv_cast_int)
  have hbaseProdLeUpper :
      _root_.F2R (beta:=radix)
          (⟨b.vNum - 1, x.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat radix) *
        _root_.F2R (beta:=radix)
          (⟨b.vNum - 1, y.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat radix) ≤
        _root_.F2R (beta:=radix) upper := by
    let A : ℝ := ((b.vNum - 1 : Int) : ℝ)
    have hA_nonneg : 0 ≤ A := by
      have hnonneg : (0 : Int) ≤ b.vNum - 1 := by omega
      simpa [A] using
        (show (0 : ℝ) ≤ ((b.vNum - 1 : Int) : ℝ) by exact_mod_cast hnonneg)
    have hpow_z_nonneg : 0 ≤ (radix : ℝ) ^ z :=
      le_of_lt (zpow_pos hradix_pos z)
    have hA_sq_le : A * A ≤ A * (radix : ℝ) ^ (t : Int) :=
      mul_le_mul_of_nonneg_left (by simpa [A] using hpred_le_pow) hA_nonneg
    have hmul_exp :
        (radix : ℝ) ^ x.Fexp * (radix : ℝ) ^ y.Fexp = (radix : ℝ) ^ z := by
      rw [← zpow_add₀ hradix_ne]
    have hupper_exp :
        (radix : ℝ) ^ ((t : Int) + z) =
          (radix : ℝ) ^ (t : Int) * (radix : ℝ) ^ z := by
      exact zpow_add₀ hradix_ne (t : Int) z
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, upper]
    calc
      A * (radix : ℝ) ^ x.Fexp * (A * (radix : ℝ) ^ y.Fexp)
          = (A * A) * (radix : ℝ) ^ z := by
            rw [← hmul_exp]
            ring
      _ ≤ (A * (radix : ℝ) ^ (t : Int)) * (radix : ℝ) ^ z :=
            mul_le_mul_of_nonneg_right hA_sq_le hpow_z_nonneg
      _ = A * (radix : ℝ) ^ ((t : Int) + z) := by
            rw [hupper_exp]
            ring
  have hprodAbsLeUpper : |prod| ≤ _root_.F2R (beta:=radix) upper :=
    le_trans hprodAbsLeBase hbaseProdLeUpper
  have hprod_bounds :
      -_root_.F2R (beta:=radix) upper ≤ prod ∧
        prod ≤ _root_.F2R (beta:=radix) upper :=
    abs_le.mp hprodAbsLeUpper
  have hupper_nonneg : 0 ≤ _root_.F2R (beta:=radix) upper := by
    exact le_trans (abs_nonneg prod) hprodAbsLeUpper
  have hlower_val : _root_.F2R (beta:=radix) lower =
      -_root_.F2R (beta:=radix) upper := by
    simp [lower, upper, _root_.F2R, FloatSpec.Core.Defs.F2R]
    ring
  have hr_le_upper :
      _root_.F2R (beta:=radix) r ≤ _root_.F2R (beta:=radix) upper := by
    by_contra hnot
    have hlt :
        _root_.F2R (beta:=radix) upper <
          _root_.F2R (beta:=radix) r := lt_of_not_ge hnot
    have hdist := hClosest.2 upper hupperBounded
    have hr_sub_nonneg :
        0 ≤ _root_.F2R (beta:=radix) r - prod := by
      linarith [hprod_bounds.2, hlt]
    have hu_sub_nonneg :
        0 ≤ _root_.F2R (beta:=radix) upper - prod := by
      linarith [hprod_bounds.2]
    rw [abs_of_nonneg hr_sub_nonneg, abs_of_nonneg hu_sub_nonneg] at hdist
    linarith
  have hlower_le_r :
      -_root_.F2R (beta:=radix) upper ≤ _root_.F2R (beta:=radix) r := by
    by_contra hnot
    have hlt :
        _root_.F2R (beta:=radix) r <
          -_root_.F2R (beta:=radix) upper := lt_of_not_ge hnot
    have hdist := hClosest.2 lower hlowerBounded
    rw [hlower_val] at hdist
    have hr_sub_nonpos :
        _root_.F2R (beta:=radix) r - prod ≤ 0 := by
      linarith [hprod_bounds.1, hlt]
    have hlower_sub_nonpos :
        -_root_.F2R (beta:=radix) upper - prod ≤ 0 := by
      linarith [hprod_bounds.1]
    rw [abs_of_nonpos hr_sub_nonpos, abs_of_nonpos hlower_sub_nonpos] at hdist
    linarith
  have hrAbsLeUpper :
      |_root_.F2R (beta:=radix) r| ≤ _root_.F2R (beta:=radix) upper :=
    abs_le.mpr ⟨hlower_le_r, hr_le_upper⟩
  let nr : FloatSpec.Core.Defs.FlocqFloat radix :=
    Fnormalize (beta:=radix) radix b t r
  have hnrCan : Fcanonic (beta:=radix) radix b nr := by
    have h := FnormalizeCanonic (beta:=radix) radix b t r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
      Id.run, ULift.up_down, nr, Int.cast_ofNat] using
      h ⟨hClosest.1, hClosest.1, ht_ne, hradix, hvNum⟩
  have hupperCan : Fcanonic (beta:=radix) radix b upper := by
    have hlowerExp : -b.dExp ≤ (t : Int) + z := by
      dsimp [z]
      omega
    simpa [upper, pPred] using
      (FcanonicPpred (beta:=radix) radix b t ((t : Int) + z)
        hradix ht_pos hvNum hlowerExp)
  have hnrVal :
      _root_.F2R (beta:=radix) nr = _root_.F2R (beta:=radix) r := by
    have h := FnormalizeCorrect (beta:=radix) radix b t r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
      Id.run, ULift.up_down, nr, Int.cast_ofNat] using h ⟨rfl, hradix⟩
  have hupperAbs :
      |_root_.F2R (beta:=radix) upper| =
        _root_.F2R (beta:=radix) upper :=
    abs_of_nonneg hupper_nonneg
  have hnrAbsLeUpper :
      |_root_.F2R (beta:=radix) nr| ≤ |_root_.F2R (beta:=radix) upper| := by
    rw [hnrVal, hupperAbs]
    exact hrAbsLeUpper
  have hnrExpLe : nr.Fexp ≤ upper.Fexp := by
    have h := Fcanonic_Rle_Zle (beta:=radix) radix b nr upper hradix rfl
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fcanonic_Rle_Zle_check,
      ULift.down_up, Fcanonic', Int.cast_ofNat] using h ⟨hnrCan, hupperCan, hnrAbsLeUpper⟩
  have hfulp_le_pow :
      Fulp (beta:=radix) b radix t r ≤
        (radix : ℝ) ^ ((t : Int) + x.Fexp + y.Fexp) := by
    unfold Fulp
    have :
        (Fnormalize (beta:=radix) radix b t r).Fexp ≤
          (t : Int) + x.Fexp + y.Fexp := by
      simpa [nr, upper, z, add_assoc] using hnrExpLe
    exact zpow_right_mono₀ hradix_ge_one this
  have htwo_abs_e_le_pow :
      2 * |_root_.F2R (beta:=radix) e| ≤
        (radix : ℝ) ^ ((t : Int) + x.Fexp + y.Fexp) :=
    le_trans htwo_abs_e_le_fulp hfulp_le_pow
  nlinarith

/-- Coq Sec1 rounded-product exponent lower bound `rExp`. -/
theorem rExp {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x y r : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hSLe : 2 ≤ s) (hSGe : s ≤ t - 2)
    (hxNormal : Fnormal (beta:=beta) radix b x)
    (hyNormal : Fnormal (beta:=beta) radix b y)
    (hK : -b.dExp ≤ x.Fexp + y.Fexp)
    (hClosest : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * _root_.F2R (beta:=beta) y) r) :
    (t : Int) - 1 + x.Fexp + y.Fexp ≤ r.Fexp := by
  subst beta
  let prod : ℝ :=
    _root_.F2R (beta:=radix) x * _root_.F2R (beta:=radix) y
  let targetExp : Int := (t : Int) - 1 + x.Fexp + y.Fexp
  let lower : FloatSpec.Core.Defs.FlocqFloat radix :=
    ⟨nNormMin radix t, targetExp⟩
  have htPos : 0 < t := by omega
  have htNe : t ≠ 0 := ne_of_gt htPos
  have hradixPosInt : 0 < radix := by omega
  have hradixPos : (0 : ℝ) < (radix : ℝ) := by exact_mod_cast hradixPosInt
  have hradixNe : (radix : ℝ) ≠ 0 := ne_of_gt hradixPos
  have hvNumPos : 0 < b.vNum := by
    rw [hvNum, Zpower_nat]
    exact pow_pos hradixPosInt t
  have hnMinPos : 0 < nNormMin radix t := by
    unfold nNormMin
    exact pow_pos hradixPosInt (t - 1)
  have hnMinCast :
      (nNormMin radix t : ℝ) = (radix : ℝ) ^ ((t : Int) - 1) := by
    unfold nNormMin
    rw [Int.cast_pow, ← zpow_natCast]
    congr 1
    omega
  have hnormalMinValue :
      ∀ f : FloatSpec.Core.Defs.FlocqFloat radix,
        Fnormal (beta:=radix) radix b f →
          _root_.F2R (beta:=radix)
              (⟨nNormMin radix t, f.Fexp⟩ :
                FloatSpec.Core.Defs.FlocqFloat radix) ≤
            |_root_.F2R (beta:=radix) f| := by
    intro f hf
    have hposNorm : b.vNum = radix * nNormMin radix t :=
      PosNormMin radix b t htPos hvNum
    have hnumBound : nNormMin radix t ≤ |f.Fnum| := by
      have hbound := hf.2
      have habsMul : |radix * f.Fnum| = radix * |f.Fnum| := by
        rw [abs_mul, abs_of_pos hradixPosInt]
      rw [hposNorm, habsMul] at hbound
      nlinarith
    have hnumBoundReal :
        (nNormMin radix t : ℝ) ≤ |(f.Fnum : ℝ)| := by
      exact_mod_cast hnumBound
    have hpowPos : 0 < (radix : ℝ) ^ f.Fexp := zpow_pos hradixPos _
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R,
      FloatSpec.Core.Defs.FlocqFloat.Fnum,
      FloatSpec.Core.Defs.FlocqFloat.Fexp]
    rw [abs_mul, abs_of_pos hpowPos]
    exact mul_le_mul_of_nonneg_right hnumBoundReal (le_of_lt hpowPos)
  have hxMin := hnormalMinValue x hxNormal
  have hyMin := hnormalMinValue y hyNormal
  let minX : FloatSpec.Core.Defs.FlocqFloat radix :=
    ⟨nNormMin radix t, x.Fexp⟩
  let minY : FloatSpec.Core.Defs.FlocqFloat radix :=
    ⟨nNormMin radix t, y.Fexp⟩
  have hminXNonneg : 0 ≤ _root_.F2R (beta:=radix) minX := by
    dsimp [minX, _root_.F2R, FloatSpec.Core.Defs.F2R]
    exact mul_nonneg (by exact_mod_cast (le_of_lt hnMinPos))
      (le_of_lt (zpow_pos hradixPos _))
  have hminYNonneg : 0 ≤ _root_.F2R (beta:=radix) minY := by
    dsimp [minY, _root_.F2R, FloatSpec.Core.Defs.F2R]
    exact mul_nonneg (by exact_mod_cast (le_of_lt hnMinPos))
      (le_of_lt (zpow_pos hradixPos _))
  have hminProductLe :
      _root_.F2R (beta:=radix) minX *
          _root_.F2R (beta:=radix) minY ≤ |prod| := by
    dsimp [prod]
    rw [abs_mul]
    exact mul_le_mul (by simpa [minX] using hxMin)
      (by simpa [minY] using hyMin) hminYNonneg (abs_nonneg _)
  have hlowerProduct :
      _root_.F2R (beta:=radix) lower =
        _root_.F2R (beta:=radix) minX *
          _root_.F2R (beta:=radix) minY := by
    simp only [lower, minX, minY, targetExp, _root_.F2R,
      FloatSpec.Core.Defs.F2R,
      FloatSpec.Core.Defs.FlocqFloat.Fnum,
      FloatSpec.Core.Defs.FlocqFloat.Fexp]
    rw [hnMinCast]
    calc
      (radix : ℝ) ^ ((t : Int) - 1) *
          (radix : ℝ) ^ ((t : Int) - 1 + x.Fexp + y.Fexp) =
          (radix : ℝ) ^
            (((t : Int) - 1) + ((t : Int) - 1 + x.Fexp + y.Fexp)) := by
              exact (zpow_add₀ hradixNe _ _).symm
      _ = (radix : ℝ) ^
            ((((t : Int) - 1) + x.Fexp) + (((t : Int) - 1) + y.Fexp)) := by
              congr 1
              ring
      _ = (radix : ℝ) ^ (((t : Int) - 1) + x.Fexp) *
            (radix : ℝ) ^ (((t : Int) - 1) + y.Fexp) := by
              rw [zpow_add₀ hradixNe]
      _ = ((radix : ℝ) ^ ((t : Int) - 1) * (radix : ℝ) ^ x.Fexp) *
            ((radix : ℝ) ^ ((t : Int) - 1) * (radix : ℝ) ^ y.Fexp) := by
              rw [zpow_add₀ hradixNe, zpow_add₀ hradixNe]
  have hlowerLeProd : _root_.F2R (beta:=radix) lower ≤ |prod| := by
    rw [hlowerProduct]
    exact hminProductLe
  have hlowerNonneg : 0 ≤ _root_.F2R (beta:=radix) lower := by
    rw [hlowerProduct]
    exact mul_nonneg hminXNonneg hminYNonneg
  have hlowerExpBound : -b.dExp ≤ targetExp := by
    dsimp [targetExp]
    omega
  have hlowerCan : Fcanonic (beta:=radix) radix b lower := by
    simpa [lower] using
      (FcanonicNnormMin (beta:=radix) radix b t targetExp
        hradix htPos hvNum hlowerExpBound)
  have hlowerBound : Fbounded (beta:=radix) b lower :=
    (FcanonicBound (beta:=radix) radix b lower) hlowerCan
  have hlowerLeRAbs :
      _root_.F2R (beta:=radix) lower ≤
        |_root_.F2R (beta:=radix) r| := by
    by_contra hnot
    have hrAbsLt :
        |_root_.F2R (beta:=radix) r| <
          _root_.F2R (beta:=radix) lower := lt_of_not_ge hnot
    by_cases hprodNonneg : 0 ≤ prod
    · have hlowerLeProd' : _root_.F2R (beta:=radix) lower ≤ prod := by
        simpa [abs_of_nonneg hprodNonneg] using hlowerLeProd
      have hrLtLower :
          _root_.F2R (beta:=radix) r <
            _root_.F2R (beta:=radix) lower :=
        lt_of_le_of_lt (le_abs_self _) hrAbsLt
      have hdist := hClosest.2 lower hlowerBound
      have hrDiffNonpos : _root_.F2R (beta:=radix) r - prod ≤ 0 := by
        linarith
      have hlowerDiffNonpos :
          _root_.F2R (beta:=radix) lower - prod ≤ 0 := by linarith
      rw [abs_of_nonpos hrDiffNonpos, abs_of_nonpos hlowerDiffNonpos] at hdist
      linarith
    · have hprodNeg : prod < 0 := lt_of_not_ge hprodNonneg
      let negLower : FloatSpec.Core.Defs.FlocqFloat radix :=
        Fopp (beta:=radix) lower
      have hnegLowerBound : Fbounded (beta:=radix) b negLower := by
        simpa [negLower, Fbounded, Fopp, FloatSpec.Calc.Operations.Fopp,
          abs_neg] using hlowerBound
      have hnegLowerValue :
          _root_.F2R (beta:=radix) negLower =
            -_root_.F2R (beta:=radix) lower := by
        simpa [negLower] using
          (FloatSpec.Calc.Operations.F2R_opp (beta:=radix) lower) True.intro
      have hlowerLeNegProd :
          _root_.F2R (beta:=radix) lower ≤ -prod := by
        simpa [abs_of_neg hprodNeg] using hlowerLeProd
      have hprodLeNegLower :
          prod ≤ _root_.F2R (beta:=radix) negLower := by
        rw [hnegLowerValue]
        linarith
      have hnegLowerLtR :
          _root_.F2R (beta:=radix) negLower <
            _root_.F2R (beta:=radix) r := by
        rw [hnegLowerValue]
        linarith [neg_abs_le (_root_.F2R (beta:=radix) r)]
      have hdist := hClosest.2 negLower hnegLowerBound
      have hrDiffNonneg : 0 ≤ _root_.F2R (beta:=radix) r - prod := by
        linarith
      have hnegLowerDiffNonneg :
          0 ≤ _root_.F2R (beta:=radix) negLower - prod := by linarith
      rw [abs_of_nonneg hrDiffNonneg,
        abs_of_nonneg hnegLowerDiffNonneg] at hdist
      linarith
  let nr : FloatSpec.Core.Defs.FlocqFloat radix :=
    Fnormalize (beta:=radix) radix b t r
  have hnrCan : Fcanonic (beta:=radix) radix b nr := by
    have h := FnormalizeCanonic (beta:=radix) radix b t r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
      Id.run, ULift.up_down, Fbounded', nr, Int.cast_ofNat] using
      h ⟨hClosest.1, hClosest.1, htNe, hradix, hvNum⟩
  have hnrVal :
      _root_.F2R (beta:=radix) nr = _root_.F2R (beta:=radix) r := by
    have h := FnormalizeCorrect (beta:=radix) radix b t r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
      Id.run, ULift.up_down, nr, Int.cast_ofNat] using h ⟨rfl, hradix⟩
  have hlowerExpLeNr : lower.Fexp ≤ nr.Fexp := by
    have h := Fcanonic_Rle_Zle (beta:=radix) radix b lower nr hradix rfl
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fcanonic_Rle_Zle_check,
      ULift.down_up, Fcanonic', Int.cast_ofNat] using
      h ⟨hlowerCan, hnrCan, by
        rw [abs_of_nonneg hlowerNonneg, hnrVal]
        exact hlowerLeRAbs⟩
  have hnrExpLeR : nr.Fexp ≤ r.Fexp := by
    have h := FcanonicLeastExp_from_explicit_radix_payload (beta:=radix) radix b r nr hradix rfl hvNumPos
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicLeastExp_check,
      ULift.down_up, Fbounded', Fcanonic', Int.cast_ofNat] using
      h ⟨hnrVal.symm, hClosest.1, hnrCan⟩
  change targetExp ≤ r.Fexp
  exact le_trans (by simpa [lower] using hlowerExpLeNr) hnrExpLeR

/-! Coq Sec1 exact residual bound `Boundedt1`.

The proof follows the upstream Dekker section: build the exact subtraction
float `r - x1 * y1`, show its exponent can be lowered to the target exponent
with `BoundedL`, and bound its real value by decomposing the residual into the
rounded-product error plus the three split cross terms. -/
theorem Boundedt1 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x x1 x2 y y1 y2 r e : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hSLe : 2 ≤ s) (hSGe : s ≤ t - 2)
    (hHst1 : (t : Int) - 1 ≤ 2 * (s : Int))
    (hHst2 : 2 * (s : Int) ≤ (t : Int) + 1)
    (hxNormal : Fnormal (beta:=beta) radix b x)
    (hyNormal : Fnormal (beta:=beta) radix b y)
    (hK : -b.dExp ≤ x.Fexp + y.Fexp)
    (hClosest : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * _root_.F2R (beta:=beta) y) r)
    (hee : _root_.F2R (beta:=beta) x * _root_.F2R (beta:=beta) y =
      _root_.F2R (beta:=beta) r + _root_.F2R (beta:=beta) e)
    (hXeq : _root_.F2R (beta:=beta) x =
      _root_.F2R (beta:=beta) x1 + _root_.F2R (beta:=beta) x2)
    (hYeq : _root_.F2R (beta:=beta) y =
      _root_.F2R (beta:=beta) y1 + _root_.F2R (beta:=beta) y2)
    (hx2Le : |_root_.F2R (beta:=beta) x2| ≤
      (radix : ℝ) ^ ((s : Int) + x.Fexp) / 2)
    (hy2Le : |_root_.F2R (beta:=beta) y2| ≤
      (radix : ℝ) ^ ((s : Int) + y.Fexp) / 2)
    (hx1Exp : (s : Int) + x.Fexp ≤ x1.Fexp)
    (hy1Exp : (s : Int) + y.Fexp ≤ y1.Fexp) :
    ∃ xprime : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) xprime =
          _root_.F2R (beta:=beta) r -
            _root_.F2R (beta:=beta) x1 * _root_.F2R (beta:=beta) y1 ∧
        Fbounded (beta:=beta) b xprime ∧
          xprime.Fexp = (t : Int) - 1 + x.Fexp + y.Fexp := by
  subst beta
  let targetExp : Int := (t : Int) - 1 + x.Fexp + y.Fexp
  let prod11 : FloatSpec.Core.Defs.FlocqFloat radix :=
    FloatSpec.Calc.Operations.Fmult (beta:=radix) x1 y1
  let residual : FloatSpec.Core.Defs.FlocqFloat radix :=
    Fminus (beta:=radix) r prod11
  have htLarge : 4 ≤ t := by omega
  have htPos : 0 < t := by omega
  have htNe : t ≠ 0 := ne_of_gt htPos
  have htPrecision : 1 < t := by omega
  have hradixPosInt : 0 < radix := by omega
  have hradixPos : (0 : ℝ) < (radix : ℝ) := by exact_mod_cast hradixPosInt
  have hradixNe : (radix : ℝ) ≠ 0 := ne_of_gt hradixPos
  have hradixOne : (1 : ℝ) ≤ (radix : ℝ) := by
    exact_mod_cast (le_of_lt hradix)
  have hbetaGt : 1 < radix := hradix
  have hnormalAbsLt :
      ∀ f : FloatSpec.Core.Defs.FlocqFloat radix,
        Fbounded (beta:=radix) b f →
          |_root_.F2R (beta:=radix) f| <
            (radix : ℝ) ^ ((t : Int) + f.Fexp) := by
    intro f hfBound
    have hnumReal :
        |(f.Fnum : ℝ)| < (radix : ℝ) ^ (t : Int) := by
      have hnumCast : ((|f.Fnum| : Int) : ℝ) < (b.vNum : ℝ) := by
        exact_mod_cast hfBound.1
      have hvReal : (b.vNum : ℝ) = (radix : ℝ) ^ (t : Int) := by
        rw [hvNum]
        have h := Zpower_nat_Z_powerRZ radix t
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Zpower_nat_Z_powerRZ_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
      simpa [Int.cast_abs, hvReal] using hnumCast
    have hpowPos : 0 < (radix : ℝ) ^ f.Fexp := zpow_pos hradixPos _
    calc
      |_root_.F2R (beta:=radix) f| =
          |(f.Fnum : ℝ)| * (radix : ℝ) ^ f.Fexp := by
            simp [_root_.F2R, FloatSpec.Core.Defs.F2R, abs_mul,
              abs_of_pos hpowPos]
      _ < (radix : ℝ) ^ (t : Int) * (radix : ℝ) ^ f.Fexp :=
          mul_lt_mul_of_pos_right hnumReal hpowPos
      _ = (radix : ℝ) ^ ((t : Int) + f.Fexp) := by
          rw [zpow_add₀ hradixNe]
  have hxAbsLt :
      |_root_.F2R (beta:=radix) x| <
        (radix : ℝ) ^ ((t : Int) + x.Fexp) :=
    hnormalAbsLt x hxNormal.1
  have hyAbsLt :
      |_root_.F2R (beta:=radix) y| <
        (radix : ℝ) ^ ((t : Int) + y.Fexp) :=
    hnormalAbsLt y hyNormal.1
  have hx2y2Bound :
      |_root_.F2R (beta:=radix) x2 * _root_.F2R (beta:=radix) y2| ≤
        (radix : ℝ) ^ (2 * (s : Int) + x.Fexp + y.Fexp) / 4 :=
    x2y2Le radix s x.Fexp y.Fexp
      (_root_.F2R (beta:=radix) x2) (_root_.F2R (beta:=radix) y2)
      hradix hx2Le hy2Le
  have hx2y1Bound :
      |_root_.F2R (beta:=radix) x2 * _root_.F2R (beta:=radix) y1| <
        (radix : ℝ) ^ ((t : Int) + (s : Int) + x.Fexp + y.Fexp) / 2 +
          (radix : ℝ) ^ (2 * (s : Int) + x.Fexp + y.Fexp) / 4 :=
    x2y1Le radix s t x.Fexp y.Fexp
      (_root_.F2R (beta:=radix) x2) (_root_.F2R (beta:=radix) y)
      (_root_.F2R (beta:=radix) y1) (_root_.F2R (beta:=radix) y2)
      hradix hYeq hx2Le hyAbsLt hx2y2Bound
  have hx1y2Bound :
      |_root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y2| <
        (radix : ℝ) ^ ((t : Int) + (s : Int) + x.Fexp + y.Fexp) / 2 +
          (radix : ℝ) ^ (2 * (s : Int) + x.Fexp + y.Fexp) / 4 :=
    x1y2Le radix s t x.Fexp y.Fexp
      (_root_.F2R (beta:=radix) x) (_root_.F2R (beta:=radix) x1)
      (_root_.F2R (beta:=radix) x2) (_root_.F2R (beta:=radix) y2)
      hradix hXeq hxAbsLt hy2Le hx2y2Bound
  have heBound :
      |_root_.F2R (beta:=radix) e| ≤
        (radix : ℝ) ^ ((t : Int) + x.Fexp + y.Fexp) / 2 :=
    eLe b radix s t x y r e rfl hradix hvNum hSLe hSGe
      hxNormal hyNormal hK hClosest hee
  have hdecomp :
      _root_.F2R (beta:=radix) r -
          _root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y1 =
        -_root_.F2R (beta:=radix) e +
            _root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y2 +
          _root_.F2R (beta:=radix) x2 * _root_.F2R (beta:=radix) y1 +
          _root_.F2R (beta:=radix) x2 * _root_.F2R (beta:=radix) y2 := by
    calc
      _root_.F2R (beta:=radix) r -
          _root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y1 =
        (_root_.F2R (beta:=radix) x * _root_.F2R (beta:=radix) y -
          _root_.F2R (beta:=radix) e) -
            _root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y1 := by
            linarith
      _ = -_root_.F2R (beta:=radix) e +
            _root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y2 +
          _root_.F2R (beta:=radix) x2 * _root_.F2R (beta:=radix) y1 +
          _root_.F2R (beta:=radix) x2 * _root_.F2R (beta:=radix) y2 := by
            rw [hXeq, hYeq]
            ring
  have hresAbsTri :
      |_root_.F2R (beta:=radix) r -
          _root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y1| ≤
        |_root_.F2R (beta:=radix) e| +
          |_root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y2| +
          |_root_.F2R (beta:=radix) x2 * _root_.F2R (beta:=radix) y1| +
          |_root_.F2R (beta:=radix) x2 * _root_.F2R (beta:=radix) y2| := by
    rw [hdecomp]
    have h1 := abs_add_le
      (-_root_.F2R (beta:=radix) e +
          _root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y2 +
        _root_.F2R (beta:=radix) x2 * _root_.F2R (beta:=radix) y1)
      (_root_.F2R (beta:=radix) x2 * _root_.F2R (beta:=radix) y2)
    have h2 := abs_add_le
      (-_root_.F2R (beta:=radix) e +
          _root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y2)
      (_root_.F2R (beta:=radix) x2 * _root_.F2R (beta:=radix) y1)
    have h3 := abs_add_le
      (-_root_.F2R (beta:=radix) e)
      (_root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y2)
    rw [abs_neg] at h3
    nlinarith
  let A : ℝ := (radix : ℝ) ^ ((t : Int) + x.Fexp + y.Fexp)
  let B : ℝ := (radix : ℝ) ^ ((t : Int) + (s : Int) + x.Fexp + y.Fexp)
  let C : ℝ := (radix : ℝ) ^ (2 * (s : Int) + x.Fexp + y.Fexp)
  let T : ℝ := (radix : ℝ) ^ (targetExp + (t : Int))
  have hresAbsLtFirst :
      |_root_.F2R (beta:=radix) r -
          _root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y1| <
        A / 2 + (B / 2 + C / 4) + (B / 2 + C / 4) + C / 4 := by
    have hx1y2Bound' : |_root_.F2R (beta:=radix) x1 *
          _root_.F2R (beta:=radix) y2| < B / 2 + C / 4 := by
      simpa [B, C, add_assoc] using hx1y2Bound
    have hx2y1Bound' : |_root_.F2R (beta:=radix) x2 *
          _root_.F2R (beta:=radix) y1| < B / 2 + C / 4 := by
      simpa [B, C, add_assoc] using hx2y1Bound
    have hx2y2Bound' : |_root_.F2R (beta:=radix) x2 *
          _root_.F2R (beta:=radix) y2| ≤ C / 4 := by
      simpa [C] using hx2y2Bound
    have heBound' : |_root_.F2R (beta:=radix) e| ≤ A / 2 := by
      simpa [A, add_assoc] using heBound
    nlinarith
  have hfirstLe :
      A / 2 + (B / 2 + C / 4) + (B / 2 + C / 4) + C / 4 ≤
        B + A + C := by
    have hA_nonneg : 0 ≤ A := by dsimp [A]; positivity
    have hB_nonneg : 0 ≤ B := by dsimp [B]; positivity
    have hC_nonneg : 0 ≤ C := by dsimp [C]; positivity
    nlinarith
  have hA_le_A1 :
      A ≤ (radix : ℝ) ^ ((t : Int) + 1 + x.Fexp + y.Fexp) := by
    dsimp [A]
    exact zpow_le_zpow_right₀ hradixOne (by omega)
  have hC_le_A2 :
      C ≤ (radix : ℝ) ^ (((t : Int) + 1 + x.Fexp + y.Fexp) + 1) := by
    dsimp [C]
    exact zpow_le_zpow_right₀ hradixOne (by omega)
  have hA1C_le :
      (radix : ℝ) ^ ((t : Int) + 1 + x.Fexp + y.Fexp) + C ≤
        (radix : ℝ) ^ (((t : Int) + 1 + x.Fexp + y.Fexp) + 1) := by
    exact powerRZSumRle radix ((t : Int) + 1 + x.Fexp + y.Fexp)
      (2 * (s : Int) + x.Fexp + y.Fexp) hradix (by omega)
  have hB_A1_C_le :
      B + (radix : ℝ) ^ ((t : Int) + 1 + x.Fexp + y.Fexp) + C ≤
        B + (radix : ℝ) ^ (((t : Int) + 1 + x.Fexp + y.Fexp) + 1) := by
    nlinarith [hA1C_le]
  have hBsum_le :
      B + (radix : ℝ) ^ (((t : Int) + 1 + x.Fexp + y.Fexp) + 1) ≤
        (radix : ℝ) ^ (((t : Int) + (s : Int) + x.Fexp + y.Fexp) + 1) := by
    exact powerRZSumRle radix ((t : Int) + (s : Int) + x.Fexp + y.Fexp)
      (((t : Int) + 1 + x.Fexp + y.Fexp) + 1) hradix (by omega)
  have hfinalPower :
      (radix : ℝ) ^ (((t : Int) + (s : Int) + x.Fexp + y.Fexp) + 1) ≤ T := by
    dsimp [T, targetExp]
    exact zpow_le_zpow_right₀ hradixOne (by omega)
  have hboundTower : B + A + C ≤ T := by
    have hBA_le : B + A + C ≤
        B + (radix : ℝ) ^ ((t : Int) + 1 + x.Fexp + y.Fexp) + C := by
      nlinarith [hA_le_A1]
    exact le_trans hBA_le
      (le_trans hB_A1_C_le (le_trans hBsum_le hfinalPower))
  have hresBound : |_root_.F2R (beta:=radix) r -
          _root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y1| < T :=
    lt_of_lt_of_le (lt_of_lt_of_le hresAbsLtFirst hfirstLe) hboundTower
  have hresidualValue :
      _root_.F2R (beta:=radix) residual =
        _root_.F2R (beta:=radix) r -
          _root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y1 := by
    have hminus := Fminus_correct (beta:=radix) r prod11
    have hminusVal :
        _root_.F2R (beta:=radix) residual =
          _root_.F2R (beta:=radix) r -
            _root_.F2R (beta:=radix) prod11 := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus,
        Id.run, ULift.up_down, residual, Int.cast_ofNat] using hminus hbetaGt
    have hmult := Fmult_correct (beta:=radix) x1 y1
    have hmultVal :
        _root_.F2R (beta:=radix) prod11 =
          _root_.F2R (beta:=radix) x1 *
            _root_.F2R (beta:=radix) y1 := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fmult_correct_check,
        Id.run, ULift.up_down, prod11, Int.cast_ofNat] using hmult hbetaGt
    rw [hminusVal, hmultVal]
  have htargetLeResidualExp : targetExp ≤ residual.Fexp := by
    have hresidualExp :
        residual.Fexp = min r.Fexp prod11.Fexp := by
      simpa [residual] using Fminus_exp_eq_min (beta:=radix) r prod11
    have hprod11Exp : prod11.Fexp = x1.Fexp + y1.Fexp := by
      simp [prod11, FloatSpec.Calc.Operations.Fmult]
    have htargetLeR : targetExp ≤ r.Fexp := by
      simpa [targetExp] using
        rExp b radix s t x y r rfl hradix hvNum hSLe hSGe
          hxNormal hyNormal hK hClosest
    have htargetLeProd : targetExp ≤ prod11.Fexp := by
      rw [hprod11Exp]
      dsimp [targetExp]
      omega
    rw [hresidualExp]
    exact le_min htargetLeR htargetLeProd
  have htargetMin : -b.dExp ≤ targetExp := by
    dsimp [targetExp]
    omega
  have hresidualBoundForBoundedL :
      |_root_.F2R (beta:=radix) residual| <
        (radix : ℝ) ^ (targetExp + (t : Int)) := by
    rw [hresidualValue]
    simpa [T] using hresBound
  rcases BoundedL b radix t
      (_root_.F2R (beta:=radix) residual) residual targetExp
      rfl hradix hvNum htPrecision htargetLeResidualExp htargetMin
      rfl hresidualBoundForBoundedL with
    ⟨xprime, hxprimeVal, hxprimeBound, hxprimeExp⟩
  refine ⟨xprime, ?_, hxprimeBound, hxprimeExp⟩
  rw [hxprimeVal, hresidualValue]

/-! Coq Sec1 exact residual bound `Boundedt2`.

This is the second Dekker residual stage: use exact `Boundedt1` for the
representable `r - x1 * y1`, subtract `x1 * y2`, then lower the exponent to
`s + x.Fexp + y.Fexp` with `BoundedL`. -/
theorem Boundedt2 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x x1 x2 y y1 y2 r e : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hSLe : 2 ≤ s) (hSGe : s ≤ t - 2)
    (hHst1 : (t : Int) - 1 ≤ 2 * (s : Int))
    (hHst2 : 2 * (s : Int) ≤ (t : Int) + 1)
    (hxNormal : Fnormal (beta:=beta) radix b x)
    (hyNormal : Fnormal (beta:=beta) radix b y)
    (hK : -b.dExp ≤ x.Fexp + y.Fexp)
    (hClosest : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * _root_.F2R (beta:=beta) y) r)
    (hee : _root_.F2R (beta:=beta) x * _root_.F2R (beta:=beta) y =
      _root_.F2R (beta:=beta) r + _root_.F2R (beta:=beta) e)
    (hXeq : _root_.F2R (beta:=beta) x =
      _root_.F2R (beta:=beta) x1 + _root_.F2R (beta:=beta) x2)
    (hYeq : _root_.F2R (beta:=beta) y =
      _root_.F2R (beta:=beta) y1 + _root_.F2R (beta:=beta) y2)
    (hx2Le : |_root_.F2R (beta:=beta) x2| ≤
      (radix : ℝ) ^ ((s : Int) + x.Fexp) / 2)
    (hy2Le : |_root_.F2R (beta:=beta) y2| ≤
      (radix : ℝ) ^ ((s : Int) + y.Fexp) / 2)
    (hx1Exp : (s : Int) + x.Fexp ≤ x1.Fexp)
    (hy1Exp : (s : Int) + y.Fexp ≤ y1.Fexp)
    (hy2Exp : y.Fexp ≤ y2.Fexp) :
    ∃ xprime : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) xprime =
          _root_.F2R (beta:=beta) r -
            _root_.F2R (beta:=beta) x1 * _root_.F2R (beta:=beta) y1 -
            _root_.F2R (beta:=beta) x1 * _root_.F2R (beta:=beta) y2 ∧
        Fbounded (beta:=beta) b xprime ∧
          xprime.Fexp = (s : Int) + x.Fexp + y.Fexp := by
  subst beta
  let targetExp : Int := (s : Int) + x.Fexp + y.Fexp
  rcases Boundedt1 b radix s t x x1 x2 y y1 y2 r e rfl hradix hvNum
      hSLe hSGe hHst1 hHst2 hxNormal hyNormal hK hClosest hee hXeq hYeq
      hx2Le hy2Le hx1Exp hy1Exp with
    ⟨t1, ht1Val, ht1Bound, ht1Exp⟩
  let prod12 : FloatSpec.Core.Defs.FlocqFloat radix :=
    FloatSpec.Calc.Operations.Fmult (beta:=radix) x1 y2
  let residual : FloatSpec.Core.Defs.FlocqFloat radix :=
    Fminus (beta:=radix) t1 prod12
  have htPrecision : 1 < t := by omega
  have hradixPosInt : 0 < radix := by omega
  have hradixPos : (0 : ℝ) < (radix : ℝ) := by exact_mod_cast hradixPosInt
  have hradixOne : (1 : ℝ) ≤ (radix : ℝ) := by
    exact_mod_cast (le_of_lt hradix)
  have hnormalAbsLt :
      ∀ f : FloatSpec.Core.Defs.FlocqFloat radix,
        Fbounded (beta:=radix) b f →
          |_root_.F2R (beta:=radix) f| <
            (radix : ℝ) ^ ((t : Int) + f.Fexp) := by
    intro f hfBound
    have hnumReal :
        |(f.Fnum : ℝ)| < (radix : ℝ) ^ (t : Int) := by
      have hnumCast : ((|f.Fnum| : Int) : ℝ) < (b.vNum : ℝ) := by
        exact_mod_cast hfBound.1
      have hvReal : (b.vNum : ℝ) = (radix : ℝ) ^ (t : Int) := by
        rw [hvNum]
        have h := Zpower_nat_Z_powerRZ radix t
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Zpower_nat_Z_powerRZ_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
      simpa [Int.cast_abs, hvReal] using hnumCast
    have hpowPos : 0 < (radix : ℝ) ^ f.Fexp := zpow_pos hradixPos _
    calc
      |_root_.F2R (beta:=radix) f| =
          |(f.Fnum : ℝ)| * (radix : ℝ) ^ f.Fexp := by
            simp [_root_.F2R, FloatSpec.Core.Defs.F2R, abs_mul,
              abs_of_pos hpowPos]
      _ < (radix : ℝ) ^ (t : Int) * (radix : ℝ) ^ f.Fexp :=
          mul_lt_mul_of_pos_right hnumReal hpowPos
      _ = (radix : ℝ) ^ ((t : Int) + f.Fexp) := by
          rw [zpow_add₀ (ne_of_gt hradixPos)]
  have hyAbsLt :
      |_root_.F2R (beta:=radix) y| <
        (radix : ℝ) ^ ((t : Int) + y.Fexp) :=
    hnormalAbsLt y hyNormal.1
  have hx2y2Bound :
      |_root_.F2R (beta:=radix) x2 * _root_.F2R (beta:=radix) y2| ≤
        (radix : ℝ) ^ (2 * (s : Int) + x.Fexp + y.Fexp) / 4 :=
    x2y2Le radix s x.Fexp y.Fexp
      (_root_.F2R (beta:=radix) x2) (_root_.F2R (beta:=radix) y2)
      hradix hx2Le hy2Le
  have hx2y1Bound :
      |_root_.F2R (beta:=radix) x2 * _root_.F2R (beta:=radix) y1| <
        (radix : ℝ) ^ ((t : Int) + (s : Int) + x.Fexp + y.Fexp) / 2 +
          (radix : ℝ) ^ (2 * (s : Int) + x.Fexp + y.Fexp) / 4 :=
    x2y1Le radix s t x.Fexp y.Fexp
      (_root_.F2R (beta:=radix) x2) (_root_.F2R (beta:=radix) y)
      (_root_.F2R (beta:=radix) y1) (_root_.F2R (beta:=radix) y2)
      hradix hYeq hx2Le hyAbsLt hx2y2Bound
  have heBound :
      |_root_.F2R (beta:=radix) e| ≤
        (radix : ℝ) ^ ((t : Int) + x.Fexp + y.Fexp) / 2 :=
    eLe b radix s t x y r e rfl hradix hvNum hSLe hSGe
      hxNormal hyNormal hK hClosest hee
  have hdecomp :
      _root_.F2R (beta:=radix) r -
          _root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y1 -
          _root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y2 =
        -_root_.F2R (beta:=radix) e +
          _root_.F2R (beta:=radix) x2 * _root_.F2R (beta:=radix) y1 +
          _root_.F2R (beta:=radix) x2 * _root_.F2R (beta:=radix) y2 := by
    calc
      _root_.F2R (beta:=radix) r -
          _root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y1 -
          _root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y2 =
        (_root_.F2R (beta:=radix) x * _root_.F2R (beta:=radix) y -
          _root_.F2R (beta:=radix) e) -
            _root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y1 -
            _root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y2 := by
            linarith
      _ = -_root_.F2R (beta:=radix) e +
          _root_.F2R (beta:=radix) x2 * _root_.F2R (beta:=radix) y1 +
          _root_.F2R (beta:=radix) x2 * _root_.F2R (beta:=radix) y2 := by
            rw [hXeq, hYeq]
            ring
  have hresAbsTri :
      |_root_.F2R (beta:=radix) r -
          _root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y1 -
          _root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y2| ≤
        |_root_.F2R (beta:=radix) e| +
          |_root_.F2R (beta:=radix) x2 * _root_.F2R (beta:=radix) y1| +
          |_root_.F2R (beta:=radix) x2 * _root_.F2R (beta:=radix) y2| := by
    rw [hdecomp]
    have h1 := abs_add_le
      (-_root_.F2R (beta:=radix) e +
        _root_.F2R (beta:=radix) x2 * _root_.F2R (beta:=radix) y1)
      (_root_.F2R (beta:=radix) x2 * _root_.F2R (beta:=radix) y2)
    have h2 := abs_add_le
      (-_root_.F2R (beta:=radix) e)
      (_root_.F2R (beta:=radix) x2 * _root_.F2R (beta:=radix) y1)
    rw [abs_neg] at h2
    nlinarith
  let A : ℝ := (radix : ℝ) ^ ((t : Int) + x.Fexp + y.Fexp)
  let B : ℝ := (radix : ℝ) ^ ((t : Int) + (s : Int) + x.Fexp + y.Fexp)
  let C : ℝ := (radix : ℝ) ^ (2 * (s : Int) + x.Fexp + y.Fexp)
  let T : ℝ := (radix : ℝ) ^ (targetExp + (t : Int))
  have hresAbsLtFirst :
      |_root_.F2R (beta:=radix) r -
          _root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y1 -
          _root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y2| <
        A / 2 + (B / 2 + C / 4) + C / 4 := by
    have hx2y1Bound' : |_root_.F2R (beta:=radix) x2 *
          _root_.F2R (beta:=radix) y1| < B / 2 + C / 4 := by
      simpa [B, C, add_assoc] using hx2y1Bound
    have hx2y2Bound' : |_root_.F2R (beta:=radix) x2 *
          _root_.F2R (beta:=radix) y2| ≤ C / 4 := by
      simpa [C] using hx2y2Bound
    have heBound' : |_root_.F2R (beta:=radix) e| ≤ A / 2 := by
      simpa [A, add_assoc] using heBound
    nlinarith
  have hsumLe :
      A / 2 + (B / 2 + C / 4) + C / 4 ≤
        B / 2 + (A / 2 + C / 2) := by
    ring_nf
    exact le_rfl
  have hAC_le_B :
      A + C ≤ B := by
    have hA_le_A1 :
        A ≤ (radix : ℝ) ^ ((t : Int) + 1 + x.Fexp + y.Fexp) := by
      dsimp [A]
      exact zpow_le_zpow_right₀ hradixOne (by omega)
    have hA1C_le :
        (radix : ℝ) ^ ((t : Int) + 1 + x.Fexp + y.Fexp) + C ≤
          (radix : ℝ) ^ (((t : Int) + 1 + x.Fexp + y.Fexp) + 1) := by
      exact powerRZSumRle radix ((t : Int) + 1 + x.Fexp + y.Fexp)
        (2 * (s : Int) + x.Fexp + y.Fexp) hradix (by omega)
    have hA1C_le_B :
        (radix : ℝ) ^ ((t : Int) + 1 + x.Fexp + y.Fexp) + C ≤ B := by
      have hpow :
          (radix : ℝ) ^ (((t : Int) + 1 + x.Fexp + y.Fexp) + 1) ≤ B := by
        dsimp [B]
        exact zpow_le_zpow_right₀ hradixOne (by omega)
      exact le_trans hA1C_le hpow
    nlinarith [hA_le_A1, hA1C_le_B]
  have hboundTower : B / 2 + (A / 2 + C / 2) ≤ T := by
    have hB_eq_T : B = T := by
      dsimp [B, T, targetExp]
      congr 1
      ring
    nlinarith [hAC_le_B]
  have hresBound :
      |_root_.F2R (beta:=radix) r -
          _root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y1 -
          _root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y2| < T :=
    lt_of_lt_of_le (lt_of_lt_of_le hresAbsLtFirst hsumLe) hboundTower
  have hresidualValue :
      _root_.F2R (beta:=radix) residual =
        _root_.F2R (beta:=radix) r -
          _root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y1 -
          _root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y2 := by
    have hminus := Fminus_correct (beta:=radix) t1 prod12
    have hminusVal :
        _root_.F2R (beta:=radix) residual =
          _root_.F2R (beta:=radix) t1 -
            _root_.F2R (beta:=radix) prod12 := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus,
        Id.run, ULift.up_down, residual, Int.cast_ofNat] using hminus hradix
    have hmult := Fmult_correct (beta:=radix) x1 y2
    have hmultVal :
        _root_.F2R (beta:=radix) prod12 =
          _root_.F2R (beta:=radix) x1 *
            _root_.F2R (beta:=radix) y2 := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fmult_correct_check,
        Id.run, ULift.up_down, prod12, Int.cast_ofNat] using hmult hradix
    rw [hminusVal, hmultVal, ht1Val]
  have htargetLeResidualExp : targetExp ≤ residual.Fexp := by
    have hresidualExp :
        residual.Fexp = min t1.Fexp prod12.Fexp := by
      simpa [residual] using Fminus_exp_eq_min (beta:=radix) t1 prod12
    have hprod12Exp : prod12.Fexp = x1.Fexp + y2.Fexp := by
      simp [prod12, FloatSpec.Calc.Operations.Fmult]
    have htargetLeT1 : targetExp ≤ t1.Fexp := by
      rw [ht1Exp]
      dsimp [targetExp]
      omega
    have htargetLeProd : targetExp ≤ prod12.Fexp := by
      rw [hprod12Exp]
      dsimp [targetExp]
      omega
    rw [hresidualExp]
    exact le_min htargetLeT1 htargetLeProd
  have htargetMin : -b.dExp ≤ targetExp := by
    dsimp [targetExp]
    omega
  have hresidualBoundForBoundedL :
      |_root_.F2R (beta:=radix) residual| <
        (radix : ℝ) ^ (targetExp + (t : Int)) := by
    rw [hresidualValue]
    simpa [T] using hresBound
  rcases BoundedL b radix t
      (_root_.F2R (beta:=radix) residual) residual targetExp
      rfl hradix hvNum htPrecision htargetLeResidualExp htargetMin
      rfl hresidualBoundForBoundedL with
    ⟨xprime, hxprimeVal, hxprimeBound, hxprimeExp⟩
  refine ⟨xprime, ?_, hxprimeBound, hxprimeExp⟩
  rw [hxprimeVal, hresidualValue]

/-! Coq Sec1 exact residual bound `Boundedt3`.

This is the third Dekker residual stage: use exact `Boundedt2` for the
representable `r - x1 * y1 - x1 * y2`, subtract `x2 * y1`, then lower the
exponent to `s + x.Fexp + y.Fexp` with `BoundedL`. -/
theorem Boundedt3 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x x1 x2 y y1 y2 r e : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hSLe : 2 ≤ s) (hSGe : s ≤ t - 2)
    (hHst1 : (t : Int) - 1 ≤ 2 * (s : Int))
    (hHst2 : 2 * (s : Int) ≤ (t : Int) + 1)
    (hxNormal : Fnormal (beta:=beta) radix b x)
    (hyNormal : Fnormal (beta:=beta) radix b y)
    (hK : -b.dExp ≤ x.Fexp + y.Fexp)
    (hClosest : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * _root_.F2R (beta:=beta) y) r)
    (hee : _root_.F2R (beta:=beta) x * _root_.F2R (beta:=beta) y =
      _root_.F2R (beta:=beta) r + _root_.F2R (beta:=beta) e)
    (hXeq : _root_.F2R (beta:=beta) x =
      _root_.F2R (beta:=beta) x1 + _root_.F2R (beta:=beta) x2)
    (hYeq : _root_.F2R (beta:=beta) y =
      _root_.F2R (beta:=beta) y1 + _root_.F2R (beta:=beta) y2)
    (hx2Le : |_root_.F2R (beta:=beta) x2| ≤
      (radix : ℝ) ^ ((s : Int) + x.Fexp) / 2)
    (hy2Le : |_root_.F2R (beta:=beta) y2| ≤
      (radix : ℝ) ^ ((s : Int) + y.Fexp) / 2)
    (hx1Exp : (s : Int) + x.Fexp ≤ x1.Fexp)
    (hy1Exp : (s : Int) + y.Fexp ≤ y1.Fexp)
    (hx2Exp : x.Fexp ≤ x2.Fexp)
    (hy2Exp : y.Fexp ≤ y2.Fexp) :
    ∃ xprime : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) xprime =
          _root_.F2R (beta:=beta) r -
            _root_.F2R (beta:=beta) x1 * _root_.F2R (beta:=beta) y1 -
            _root_.F2R (beta:=beta) x1 * _root_.F2R (beta:=beta) y2 -
            _root_.F2R (beta:=beta) x2 * _root_.F2R (beta:=beta) y1 ∧
        Fbounded (beta:=beta) b xprime ∧
          xprime.Fexp = (s : Int) + x.Fexp + y.Fexp := by
  subst beta
  let targetExp : Int := (s : Int) + x.Fexp + y.Fexp
  rcases Boundedt2 b radix s t x x1 x2 y y1 y2 r e rfl hradix hvNum
      hSLe hSGe hHst1 hHst2 hxNormal hyNormal hK hClosest hee hXeq hYeq
      hx2Le hy2Le hx1Exp hy1Exp hy2Exp with
    ⟨t2, ht2Val, ht2Bound, ht2Exp⟩
  let prod21 : FloatSpec.Core.Defs.FlocqFloat radix :=
    FloatSpec.Calc.Operations.Fmult (beta:=radix) x2 y1
  let residual : FloatSpec.Core.Defs.FlocqFloat radix :=
    Fminus (beta:=radix) t2 prod21
  have htPrecision : 1 < t := by omega
  have hradixPosInt : 0 < radix := by omega
  have hradixPos : (0 : ℝ) < (radix : ℝ) := by exact_mod_cast hradixPosInt
  have hradixOne : (1 : ℝ) ≤ (radix : ℝ) := by
    exact_mod_cast (le_of_lt hradix)
  have hx2y2Bound :
      |_root_.F2R (beta:=radix) x2 * _root_.F2R (beta:=radix) y2| ≤
        (radix : ℝ) ^ (2 * (s : Int) + x.Fexp + y.Fexp) / 4 :=
    x2y2Le radix s x.Fexp y.Fexp
      (_root_.F2R (beta:=radix) x2) (_root_.F2R (beta:=radix) y2)
      hradix hx2Le hy2Le
  have heBound :
      |_root_.F2R (beta:=radix) e| ≤
        (radix : ℝ) ^ ((t : Int) + x.Fexp + y.Fexp) / 2 :=
    eLe b radix s t x y r e rfl hradix hvNum hSLe hSGe
      hxNormal hyNormal hK hClosest hee
  have hdecomp :
      _root_.F2R (beta:=radix) r -
          _root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y1 -
          _root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y2 -
          _root_.F2R (beta:=radix) x2 * _root_.F2R (beta:=radix) y1 =
        -_root_.F2R (beta:=radix) e +
          _root_.F2R (beta:=radix) x2 * _root_.F2R (beta:=radix) y2 := by
    calc
      _root_.F2R (beta:=radix) r -
          _root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y1 -
          _root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y2 -
          _root_.F2R (beta:=radix) x2 * _root_.F2R (beta:=radix) y1 =
        (_root_.F2R (beta:=radix) x * _root_.F2R (beta:=radix) y -
          _root_.F2R (beta:=radix) e) -
            _root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y1 -
            _root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y2 -
            _root_.F2R (beta:=radix) x2 * _root_.F2R (beta:=radix) y1 := by
            linarith
      _ = -_root_.F2R (beta:=radix) e +
          _root_.F2R (beta:=radix) x2 * _root_.F2R (beta:=radix) y2 := by
            rw [hXeq, hYeq]
            ring
  have hresAbsTri :
      |_root_.F2R (beta:=radix) r -
          _root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y1 -
          _root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y2 -
          _root_.F2R (beta:=radix) x2 * _root_.F2R (beta:=radix) y1| ≤
        |_root_.F2R (beta:=radix) e| +
          |_root_.F2R (beta:=radix) x2 * _root_.F2R (beta:=radix) y2| := by
    rw [hdecomp]
    simpa [abs_neg] using abs_add_le
      (-_root_.F2R (beta:=radix) e)
      (_root_.F2R (beta:=radix) x2 * _root_.F2R (beta:=radix) y2)
  let A : ℝ := (radix : ℝ) ^ ((t : Int) + x.Fexp + y.Fexp)
  let C : ℝ := (radix : ℝ) ^ (2 * (s : Int) + x.Fexp + y.Fexp)
  let T : ℝ := (radix : ℝ) ^ (targetExp + (t : Int))
  have hresAbsLtFirst :
      |_root_.F2R (beta:=radix) r -
          _root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y1 -
          _root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y2 -
          _root_.F2R (beta:=radix) x2 * _root_.F2R (beta:=radix) y1| <
        A + C / 4 := by
    have hx2y2Bound' : |_root_.F2R (beta:=radix) x2 *
          _root_.F2R (beta:=radix) y2| ≤ C / 4 := by
      simpa [C] using hx2y2Bound
    have heBound' : |_root_.F2R (beta:=radix) e| ≤ A / 2 := by
      simpa [A, add_assoc] using heBound
    have hA_pos : 0 < A := by dsimp [A]; positivity
    nlinarith
  have hboundTower : A + C / 4 ≤ T := by
    have hC_nonneg : 0 ≤ C := by dsimp [C]; positivity
    have hC_quarter_le_C : C / 4 ≤ C := by nlinarith
    have hA_le_A1 :
        A ≤ (radix : ℝ) ^ ((t : Int) + 1 + x.Fexp + y.Fexp) := by
      dsimp [A]
      exact zpow_le_zpow_right₀ hradixOne (by omega)
    have hA1C_le :
        (radix : ℝ) ^ ((t : Int) + 1 + x.Fexp + y.Fexp) + C ≤
          (radix : ℝ) ^ (((t : Int) + 1 + x.Fexp + y.Fexp) + 1) := by
      exact powerRZSumRle radix ((t : Int) + 1 + x.Fexp + y.Fexp)
        (2 * (s : Int) + x.Fexp + y.Fexp) hradix (by omega)
    have hpow_le_T :
        (radix : ℝ) ^ (((t : Int) + 1 + x.Fexp + y.Fexp) + 1) ≤ T := by
      dsimp [T, targetExp]
      exact zpow_le_zpow_right₀ hradixOne (by omega)
    nlinarith [hC_quarter_le_C, hA_le_A1, hA1C_le, hpow_le_T]
  have hresBound :
      |_root_.F2R (beta:=radix) r -
          _root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y1 -
          _root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y2 -
          _root_.F2R (beta:=radix) x2 * _root_.F2R (beta:=radix) y1| < T :=
    lt_of_lt_of_le hresAbsLtFirst hboundTower
  have hresidualValue :
      _root_.F2R (beta:=radix) residual =
        _root_.F2R (beta:=radix) r -
          _root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y1 -
          _root_.F2R (beta:=radix) x1 * _root_.F2R (beta:=radix) y2 -
          _root_.F2R (beta:=radix) x2 * _root_.F2R (beta:=radix) y1 := by
    have hminus := Fminus_correct (beta:=radix) t2 prod21
    have hminusVal :
        _root_.F2R (beta:=radix) residual =
          _root_.F2R (beta:=radix) t2 -
            _root_.F2R (beta:=radix) prod21 := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus,
        Id.run, ULift.up_down, residual, Int.cast_ofNat] using hminus hradix
    have hmult := Fmult_correct (beta:=radix) x2 y1
    have hmultVal :
        _root_.F2R (beta:=radix) prod21 =
          _root_.F2R (beta:=radix) x2 *
            _root_.F2R (beta:=radix) y1 := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fmult_correct_check,
        Id.run, ULift.up_down, prod21, Int.cast_ofNat] using hmult hradix
    rw [hminusVal, hmultVal, ht2Val]
  have htargetLeResidualExp : targetExp ≤ residual.Fexp := by
    have hresidualExp :
        residual.Fexp = min t2.Fexp prod21.Fexp := by
      simpa [residual] using Fminus_exp_eq_min (beta:=radix) t2 prod21
    have hprod21Exp : prod21.Fexp = x2.Fexp + y1.Fexp := by
      simp [prod21, FloatSpec.Calc.Operations.Fmult]
    have htargetLeT2 : targetExp ≤ t2.Fexp := by
      rw [ht2Exp]
    have htargetLeProd : targetExp ≤ prod21.Fexp := by
      rw [hprod21Exp]
      dsimp [targetExp]
      omega
    rw [hresidualExp]
    exact le_min htargetLeT2 htargetLeProd
  have htargetMin : -b.dExp ≤ targetExp := by
    dsimp [targetExp]
    omega
  have hresidualBoundForBoundedL :
      |_root_.F2R (beta:=radix) residual| <
        (radix : ℝ) ^ (targetExp + (t : Int)) := by
    rw [hresidualValue]
    simpa [T] using hresBound
  rcases BoundedL b radix t
      (_root_.F2R (beta:=radix) residual) residual targetExp
      rfl hradix hvNum htPrecision htargetLeResidualExp htargetMin
      rfl hresidualBoundForBoundedL with
    ⟨xprime, hxprimeVal, hxprimeBound, hxprimeExp⟩
  refine ⟨xprime, ?_, hxprimeBound, hxprimeExp⟩
  rw [hxprimeVal, hresidualValue]

/-! Coq Sec1 exact residual bound `Boundedt4`.

This is the upstream `Boundedt4` payload consumed later in the Dekker section:
use the rounded-multiplication error from `errorBoundedMult`, then negate that
error float to represent the final five-term residual. -/
theorem Boundedt4 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (s t : Nat)
    (x x1 x2 y y1 y2 r : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hSLe : 2 ≤ s) (hSGe : s ≤ t - 2)
    (hxNormal : Fnormal (beta:=beta) radix b x)
    (hyNormal : Fnormal (beta:=beta) radix b y)
    (hK : -b.dExp ≤ x.Fexp + y.Fexp)
    (hClosest : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * _root_.F2R (beta:=beta) y) r)
    (hXeq : _root_.F2R (beta:=beta) x =
      _root_.F2R (beta:=beta) x1 + _root_.F2R (beta:=beta) x2)
    (hYeq : _root_.F2R (beta:=beta) y =
      _root_.F2R (beta:=beta) y1 + _root_.F2R (beta:=beta) y2) :
    ∃ xprime : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) xprime =
          _root_.F2R (beta:=beta) r -
            _root_.F2R (beta:=beta) x1 * _root_.F2R (beta:=beta) y1 -
            _root_.F2R (beta:=beta) x1 * _root_.F2R (beta:=beta) y2 -
            _root_.F2R (beta:=beta) x2 * _root_.F2R (beta:=beta) y1 -
            _root_.F2R (beta:=beta) x2 * _root_.F2R (beta:=beta) y2 ∧
        Fbounded (beta:=beta) b xprime := by
  subst beta
  have htPrecision : t ≠ 0 := by omega
  have hxBound : Fbounded (beta:=radix) b x := hxNormal.1
  have hyBound : Fbounded (beta:=radix) b y := hyNormal.1
  have hErr := errorBoundedMult (beta:=radix) b radix t x y r
  rcases (by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, errorBoundedMult_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        hErr ⟨rfl, hradix, htPrecision, hvNum, hxBound, hyBound, hK,
          hClosest⟩) with
    ⟨g, hgVal, hgBound, _hgExp⟩
  refine ⟨Fopp (beta:=radix) g, ?_, ?_⟩
  · have hOpp := Fopp_correct (beta:=radix) g
    have hOppVal :
        _root_.F2R (beta:=radix) (Fopp (beta:=radix) g) =
          -_root_.F2R (beta:=radix) g := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fopp_correct_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using hOpp True.intro
    rw [hOppVal, hgVal]
    rw [hXeq, hYeq]
    ring
  · have hBound := oppBounded (beta:=radix) b g
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, oppBounded_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using hBound hgBound

/-! Coq Sec1 exact residual bound `Boundedt4_aux`.

This is the exponent-preserving companion of `Boundedt4`: the same rounded
multiplication error is negated, and `Fopp` preserves the error exponent. -/
theorem Boundedt4_aux {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (s t : Nat)
    (x x1 x2 y y1 y2 r : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hSLe : 2 ≤ s) (hSGe : s ≤ t - 2)
    (hxNormal : Fnormal (beta:=beta) radix b x)
    (hyNormal : Fnormal (beta:=beta) radix b y)
    (hK : -b.dExp ≤ x.Fexp + y.Fexp)
    (hClosest : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * _root_.F2R (beta:=beta) y) r)
    (hXeq : _root_.F2R (beta:=beta) x =
      _root_.F2R (beta:=beta) x1 + _root_.F2R (beta:=beta) x2)
    (hYeq : _root_.F2R (beta:=beta) y =
      _root_.F2R (beta:=beta) y1 + _root_.F2R (beta:=beta) y2) :
    ∃ xprime : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) xprime =
          _root_.F2R (beta:=beta) r -
            _root_.F2R (beta:=beta) x1 * _root_.F2R (beta:=beta) y1 -
            _root_.F2R (beta:=beta) x1 * _root_.F2R (beta:=beta) y2 -
            _root_.F2R (beta:=beta) x2 * _root_.F2R (beta:=beta) y1 -
            _root_.F2R (beta:=beta) x2 * _root_.F2R (beta:=beta) y2 ∧
        Fbounded (beta:=beta) b xprime ∧
        xprime.Fexp = x.Fexp + y.Fexp := by
  subst beta
  have htPrecision : t ≠ 0 := by omega
  have hxBound : Fbounded (beta:=radix) b x := hxNormal.1
  have hyBound : Fbounded (beta:=radix) b y := hyNormal.1
  have hErr := errorBoundedMult (beta:=radix) b radix t x y r
  rcases (by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, errorBoundedMult_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        hErr ⟨rfl, hradix, htPrecision, hvNum, hxBound, hyBound, hK,
          hClosest⟩) with
    ⟨g, hgVal, hgBound, hgExp⟩
  refine ⟨Fopp (beta:=radix) g, ?_, ?_, ?_⟩
  · have hOpp := Fopp_correct (beta:=radix) g
    have hOppVal :
        _root_.F2R (beta:=radix) (Fopp (beta:=radix) g) =
          -_root_.F2R (beta:=radix) g := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fopp_correct_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using hOpp True.intro
    rw [hOppVal, hgVal]
    rw [hXeq, hYeq]
    ring
  · have hBound := oppBounded (beta:=radix) b g
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, oppBounded_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using hBound hgBound
  · simpa [Fopp, FloatSpec.Calc.Operations.Fopp] using hgExp

/-! Coq Sec1 exact product bound `Boundedx1y1_aux`.

This is the first exact product used after the four residual bounds: multiplying
the two reduced-bound leading parts stays inside the original bound because
`t <= 2 * s`. -/
theorem Boundedx1y1_aux {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x x1 y y1 : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hK : -b.dExp ≤ x.Fexp + y.Fexp)
    (hx1Exp : (s : Int) + x.Fexp ≤ x1.Fexp)
    (hy1Exp : (s : Int) + y.Fexp ≤ y1.Fexp)
    (Fx1 : Fbounded (beta:=beta) (Veltkamp_reducedBound radix b s t) x1)
    (Fy1 : Fbounded (beta:=beta) (Veltkamp_reducedBound radix b s t) y1)
    (Hst3 : (t : Int) ≤ 2 * (s : Int)) :
    ∃ xprime : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) xprime =
          _root_.F2R (beta:=beta) x1 * _root_.F2R (beta:=beta) y1 ∧
        Fbounded (beta:=beta) b xprime ∧
        xprime.Fexp = x1.Fexp + y1.Fexp := by
  subst beta
  let prod : FloatSpec.Core.Defs.FlocqFloat radix :=
    FloatSpec.Calc.Operations.Fmult (beta:=radix) x1 y1
  refine ⟨prod, ?_, ?_, ?_⟩
  · have hmult := Fmult_correct (beta:=radix) x1 y1
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fmult_correct_check, Id.run,
      ULift.up_down, prod, Int.cast_ofNat] using hmult hradix
  · rcases Fx1 with ⟨hx1Num, _hx1ExpLower⟩
    rcases Fy1 with ⟨hy1Num, _hy1ExpLower⟩
    have hradixGe : (1 : Int) ≤ radix := le_of_lt hradix
    have hradixPos : (0 : Int) < radix := by grind
    have hpowRedPos : 0 < Zpower_nat radix (t - s) := by
      simpa [Zpower_nat] using (pow_pos hradixPos (t - s))
    have hx1NumRed : |x1.Fnum| < Zpower_nat radix (t - s) := by
      simpa [Veltkamp_reducedBound] using hx1Num
    have hy1NumRed : |y1.Fnum| < Zpower_nat radix (t - s) := by
      simpa [Veltkamp_reducedBound] using hy1Num
    have hx1NumAbsNonneg : 0 ≤ |x1.Fnum| := abs_nonneg x1.Fnum
    have hprodLtRedSq :
        |x1.Fnum * y1.Fnum| <
          Zpower_nat radix (t - s) * Zpower_nat radix (t - s) := by
      rw [abs_mul]
      by_cases hxZero : |x1.Fnum| = 0
      · rw [hxZero, zero_mul]
        exact mul_pos hpowRedPos hpowRedPos
      · have hxNonzero : (0 : Int) ≠ |x1.Fnum| := fun h => hxZero h.symm
        have hxPos : 0 < |x1.Fnum| := lt_of_le_of_ne hx1NumAbsNonneg hxNonzero
        have hleft :
            |x1.Fnum| * |y1.Fnum| <
              |x1.Fnum| * Zpower_nat radix (t - s) :=
          mul_lt_mul_of_pos_left hy1NumRed hxPos
        have hright :
            |x1.Fnum| * Zpower_nat radix (t - s) ≤
              Zpower_nat radix (t - s) * Zpower_nat radix (t - s) :=
          mul_le_mul_of_nonneg_right (le_of_lt hx1NumRed) (le_of_lt hpowRedPos)
        exact lt_of_lt_of_le hleft hright
    have hredSqLeOrig :
        Zpower_nat radix (t - s) * Zpower_nat radix (t - s) ≤ Zpower_nat radix t := by
      have hsubSumLe : (t - s) + (t - s) ≤ t := by grind
      calc
        Zpower_nat radix (t - s) * Zpower_nat radix (t - s)
            = Zpower_nat radix ((t - s) + (t - s)) := by
                simp [Zpower_nat, pow_add]
        _ ≤ Zpower_nat radix t := by
            simpa [Zpower_nat] using
              pow_le_pow_right₀ hradixGe hsubSumLe
    have hnum : |prod.Fnum| < b.vNum := by
      have hlt : |x1.Fnum * y1.Fnum| < Zpower_nat radix t :=
        lt_of_lt_of_le hprodLtRedSq hredSqLeOrig
      simpa [prod, FloatSpec.Calc.Operations.Fmult, hvNum] using hlt
    have hexp : -b.dExp ≤ prod.Fexp := by
      dsimp [prod, FloatSpec.Calc.Operations.Fmult]
      grind
    exact ⟨hnum, hexp⟩
  · simp [prod, FloatSpec.Calc.Operations.Fmult]

/-! Coq Sec1 exact product bound `Boundedx1y1`.

This is the upstream wrapper around `Boundedx1y1_aux`; it keeps the same
section payload and drops only the exponent equality conjunct. -/
theorem Boundedx1y1 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x x1 y y1 : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hK : -b.dExp ≤ x.Fexp + y.Fexp)
    (hx1Exp : (s : Int) + x.Fexp ≤ x1.Fexp)
    (hy1Exp : (s : Int) + y.Fexp ≤ y1.Fexp)
    (Fx1 : Fbounded (beta:=beta) (Veltkamp_reducedBound radix b s t) x1)
    (Fy1 : Fbounded (beta:=beta) (Veltkamp_reducedBound radix b s t) y1)
    (Hst3 : (t : Int) ≤ 2 * (s : Int)) :
    ∃ xprime : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) xprime =
          _root_.F2R (beta:=beta) x1 * _root_.F2R (beta:=beta) y1 ∧
        Fbounded (beta:=beta) b xprime := by
  rcases Boundedx1y1_aux (beta:=beta) b radix s t x x1 y y1
      hbeta hradix hvNum hK hx1Exp hy1Exp Fx1 Fy1 Hst3 with
    ⟨xprime, hvalue, hbounded, _hexp⟩
  exact ⟨xprime, hvalue, hbounded⟩

/-! Coq Sec1 exact product bound `Boundedx1y2_aux`.

This is the exponent-preserving product payload for the first reduced component
and the second split component.  It consumes only the Sec1 hypotheses needed by
the upstream proof: the original bound equation, `SGe`, the product exponent
lower bound, the two component exponent bounds, and the two component
boundedness facts. -/
theorem Boundedx1y2_aux {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x x1 y y2 : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hSGe : s ≤ t - 2)
    (hK : -b.dExp ≤ x.Fexp + y.Fexp)
    (hx1Exp : (s : Int) + x.Fexp ≤ x1.Fexp)
    (hy2Exp : y.Fexp ≤ y2.Fexp)
    (Fx1 : Fbounded (beta:=beta) (Veltkamp_reducedBound radix b s t) x1)
    (Fy2 : Fbounded (beta:=beta) (Veltkamp_splitBound radix b s) y2) :
    ∃ xprime : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) xprime =
          _root_.F2R (beta:=beta) x1 * _root_.F2R (beta:=beta) y2 ∧
        Fbounded (beta:=beta) b xprime ∧
        xprime.Fexp = x1.Fexp + y2.Fexp := by
  subst beta
  let prod : FloatSpec.Core.Defs.FlocqFloat radix :=
    FloatSpec.Calc.Operations.Fmult (beta:=radix) x1 y2
  refine ⟨prod, ?_, ?_, ?_⟩
  · have hmult := Fmult_correct (beta:=radix) x1 y2
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fmult_correct_check, Id.run,
      ULift.up_down, prod, Int.cast_ofNat] using hmult hradix
  · rcases Fx1 with ⟨hx1Num, _hx1ExpLower⟩
    rcases Fy2 with ⟨hy2Num, _hy2ExpLower⟩
    have hradixGe : (1 : Int) ≤ radix := le_of_lt hradix
    have hradixPos : (0 : Int) < radix := by grind
    have hpowRedPos : 0 < Zpower_nat radix (t - s) := by
      simpa [Zpower_nat] using (pow_pos hradixPos (t - s))
    have hpowSplitPos : 0 < Zpower_nat radix s := by
      simpa [Zpower_nat] using (pow_pos hradixPos s)
    have hx1NumRed : |x1.Fnum| < Zpower_nat radix (t - s) := by
      simpa [Veltkamp_reducedBound] using hx1Num
    have hy2NumSplit : |y2.Fnum| < Zpower_nat radix s := by
      simpa [Veltkamp_splitBound] using hy2Num
    have hx1NumAbsNonneg : 0 ≤ |x1.Fnum| := abs_nonneg x1.Fnum
    have hprodLtRedSplit :
        |x1.Fnum * y2.Fnum| <
          Zpower_nat radix (t - s) * Zpower_nat radix s := by
      rw [abs_mul]
      by_cases hxZero : |x1.Fnum| = 0
      · rw [hxZero, zero_mul]
        exact mul_pos hpowRedPos hpowSplitPos
      · have hxNonzero : (0 : Int) ≠ |x1.Fnum| := fun h => hxZero h.symm
        have hxPos : 0 < |x1.Fnum| := lt_of_le_of_ne hx1NumAbsNonneg hxNonzero
        have hleft :
            |x1.Fnum| * |y2.Fnum| <
              |x1.Fnum| * Zpower_nat radix s :=
          mul_lt_mul_of_pos_left hy2NumSplit hxPos
        have hright :
            |x1.Fnum| * Zpower_nat radix s ≤
              Zpower_nat radix (t - s) * Zpower_nat radix s :=
          mul_le_mul_of_nonneg_right (le_of_lt hx1NumRed) (le_of_lt hpowSplitPos)
        exact lt_of_lt_of_le hleft hright
    have hredSplitLeOrig :
        Zpower_nat radix (t - s) * Zpower_nat radix s ≤ Zpower_nat radix t := by
      have hsubSumLe : (t - s) + s ≤ t := by omega
      calc
        Zpower_nat radix (t - s) * Zpower_nat radix s
            = Zpower_nat radix ((t - s) + s) := by
                simp [Zpower_nat, pow_add]
        _ ≤ Zpower_nat radix t := by
            simpa [Zpower_nat] using
              pow_le_pow_right₀ hradixGe hsubSumLe
    have hnum : |prod.Fnum| < b.vNum := by
      have hlt : |x1.Fnum * y2.Fnum| < Zpower_nat radix t :=
        lt_of_lt_of_le hprodLtRedSplit hredSplitLeOrig
      simpa [prod, FloatSpec.Calc.Operations.Fmult, hvNum] using hlt
    have hexp : -b.dExp ≤ prod.Fexp := by
      dsimp [prod, FloatSpec.Calc.Operations.Fmult]
      omega
    exact ⟨hnum, hexp⟩
  · simp [prod, FloatSpec.Calc.Operations.Fmult]

/-! Coq Sec1 exact product bound `Boundedx1y2`.

This is the upstream wrapper around `Boundedx1y2_aux`; it keeps the same
section payload and drops only the exponent equality conjunct. -/
theorem Boundedx1y2 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x x1 y y2 : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hSGe : s ≤ t - 2)
    (hK : -b.dExp ≤ x.Fexp + y.Fexp)
    (hx1Exp : (s : Int) + x.Fexp ≤ x1.Fexp)
    (hy2Exp : y.Fexp ≤ y2.Fexp)
    (Fx1 : Fbounded (beta:=beta) (Veltkamp_reducedBound radix b s t) x1)
    (Fy2 : Fbounded (beta:=beta) (Veltkamp_splitBound radix b s) y2) :
    ∃ xprime : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) xprime =
          _root_.F2R (beta:=beta) x1 * _root_.F2R (beta:=beta) y2 ∧
        Fbounded (beta:=beta) b xprime := by
  rcases Boundedx1y2_aux (beta:=beta) b radix s t x x1 y y2
      hbeta hradix hvNum hSGe hK hx1Exp hy2Exp Fx1 Fy2 with
    ⟨xprime, hvalue, hbounded, _hexp⟩
  exact ⟨xprime, hvalue, hbounded⟩

/-! Coq Sec1 exact product bound `Boundedx2y1_aux`.

This is the symmetric exponent-preserving product payload for the first split
component and the second reduced component.  It consumes only the Sec1
hypotheses used by the upstream proof: the original bound equation, `SGe`, the
product exponent lower bound, the two component exponent bounds, and the two
component boundedness facts. -/
theorem Boundedx2y1_aux {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x x2 y y1 : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hSGe : s ≤ t - 2)
    (hK : -b.dExp ≤ x.Fexp + y.Fexp)
    (hx2Exp : x.Fexp ≤ x2.Fexp)
    (hy1Exp : (s : Int) + y.Fexp ≤ y1.Fexp)
    (Fx2 : Fbounded (beta:=beta) (Veltkamp_splitBound radix b s) x2)
    (Fy1 : Fbounded (beta:=beta) (Veltkamp_reducedBound radix b s t) y1) :
    ∃ xprime : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) xprime =
          _root_.F2R (beta:=beta) x2 * _root_.F2R (beta:=beta) y1 ∧
        Fbounded (beta:=beta) b xprime ∧
        xprime.Fexp = x2.Fexp + y1.Fexp := by
  subst beta
  let prod : FloatSpec.Core.Defs.FlocqFloat radix :=
    FloatSpec.Calc.Operations.Fmult (beta:=radix) x2 y1
  refine ⟨prod, ?_, ?_, ?_⟩
  · have hmult := Fmult_correct (beta:=radix) x2 y1
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fmult_correct_check, Id.run,
      ULift.up_down, prod, Int.cast_ofNat] using hmult hradix
  · rcases Fx2 with ⟨hx2Num, _hx2ExpLower⟩
    rcases Fy1 with ⟨hy1Num, _hy1ExpLower⟩
    have hradixGe : (1 : Int) ≤ radix := le_of_lt hradix
    have hradixPos : (0 : Int) < radix := by grind
    have hpowSplitPos : 0 < Zpower_nat radix s := by
      simpa [Zpower_nat] using (pow_pos hradixPos s)
    have hpowRedPos : 0 < Zpower_nat radix (t - s) := by
      simpa [Zpower_nat] using (pow_pos hradixPos (t - s))
    have hx2NumSplit : |x2.Fnum| < Zpower_nat radix s := by
      simpa [Veltkamp_splitBound] using hx2Num
    have hy1NumRed : |y1.Fnum| < Zpower_nat radix (t - s) := by
      simpa [Veltkamp_reducedBound] using hy1Num
    have hx2NumAbsNonneg : 0 ≤ |x2.Fnum| := abs_nonneg x2.Fnum
    have hprodLtSplitRed :
        |x2.Fnum * y1.Fnum| <
          Zpower_nat radix s * Zpower_nat radix (t - s) := by
      rw [abs_mul]
      by_cases hxZero : |x2.Fnum| = 0
      · rw [hxZero, zero_mul]
        exact mul_pos hpowSplitPos hpowRedPos
      · have hxNonzero : (0 : Int) ≠ |x2.Fnum| := fun h => hxZero h.symm
        have hxPos : 0 < |x2.Fnum| := lt_of_le_of_ne hx2NumAbsNonneg hxNonzero
        have hleft :
            |x2.Fnum| * |y1.Fnum| <
              |x2.Fnum| * Zpower_nat radix (t - s) :=
          mul_lt_mul_of_pos_left hy1NumRed hxPos
        have hright :
            |x2.Fnum| * Zpower_nat radix (t - s) ≤
              Zpower_nat radix s * Zpower_nat radix (t - s) :=
          mul_le_mul_of_nonneg_right (le_of_lt hx2NumSplit) (le_of_lt hpowRedPos)
        exact lt_of_lt_of_le hleft hright
    have hsplitRedLeOrig :
        Zpower_nat radix s * Zpower_nat radix (t - s) ≤ Zpower_nat radix t := by
      have hsumLe : s + (t - s) ≤ t := by omega
      calc
        Zpower_nat radix s * Zpower_nat radix (t - s)
            = Zpower_nat radix (s + (t - s)) := by
                simp [Zpower_nat, pow_add]
        _ ≤ Zpower_nat radix t := by
            simpa [Zpower_nat] using
              pow_le_pow_right₀ hradixGe hsumLe
    have hnum : |prod.Fnum| < b.vNum := by
      have hlt : |x2.Fnum * y1.Fnum| < Zpower_nat radix t :=
        lt_of_lt_of_le hprodLtSplitRed hsplitRedLeOrig
      simpa [prod, FloatSpec.Calc.Operations.Fmult, hvNum] using hlt
    have hexp : -b.dExp ≤ prod.Fexp := by
      dsimp [prod, FloatSpec.Calc.Operations.Fmult]
      omega
    exact ⟨hnum, hexp⟩
  · simp [prod, FloatSpec.Calc.Operations.Fmult]

/-! Coq Sec1 exact product bound `Boundedx2y1`.

This is the upstream wrapper around `Boundedx2y1_aux`; it keeps the same
section payload and drops only the exponent equality conjunct. -/
theorem Boundedx2y1 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (s t : Nat)
    (x x2 y y1 : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hSGe : s ≤ t - 2)
    (hK : -b.dExp ≤ x.Fexp + y.Fexp)
    (hx2Exp : x.Fexp ≤ x2.Fexp)
    (hy1Exp : (s : Int) + y.Fexp ≤ y1.Fexp)
    (Fx2 : Fbounded (beta:=beta) (Veltkamp_splitBound radix b s) x2)
    (Fy1 : Fbounded (beta:=beta) (Veltkamp_reducedBound radix b s t) y1) :
    ∃ xprime : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) xprime =
          _root_.F2R (beta:=beta) x2 * _root_.F2R (beta:=beta) y1 ∧
        Fbounded (beta:=beta) b xprime := by
  rcases Boundedx2y1_aux (beta:=beta) b radix s t x x2 y y1
      hbeta hradix hvNum hSGe hK hx2Exp hy1Exp Fx2 Fy1 with
    ⟨xprime, hvalue, hbounded, _hexp⟩
  exact ⟨xprime, hvalue, hbounded⟩

/-! Coq Algo exact product bound `Boundedx2y2`.

This is the upstream theorem at `Pff.v:17710`: the binary-radix branch uses
`Veltkamp_tail2`, while the even-precision branch uses `VeltkampU`.  Both
branches share the same exact `Fmult` construction for `tx * ty`. -/
theorem Boundedx2y2 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (t : Nat)
    (x y p q hx tx p' q' hy ty : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hpGe : 4 ≤ t)
    (hK : -b.dExp ≤ x.Fexp + y.Fexp)
    (hxNormal : Fnormal (beta:=beta) radix b x)
    (hyNormal : Fnormal (beta:=beta) radix b y)
    (hA1 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x *
        ((radix : ℝ) ^ (((t - Nat.div2 t : Nat) : Int)) + 1)) p)
    (hA2 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hA3 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) q + _root_.F2R (beta:=beta) p) hx)
    (hA4 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) hx) tx)
    (hB1 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) y *
        ((radix : ℝ) ^ (((t - Nat.div2 t : Nat) : Int)) + 1)) p')
    (hB2 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) y - _root_.F2R (beta:=beta) p') q')
    (hB3 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) q' + _root_.F2R (beta:=beta) p') hy)
    (hB4 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) y - _root_.F2R (beta:=beta) hy) ty)
    (hBranch : radix = 2 ∨ Even t) :
    ∃ xprime : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) xprime =
          _root_.F2R (beta:=beta) tx * _root_.F2R (beta:=beta) ty ∧
        Fbounded (beta:=beta) b xprime ∧
        x.Fexp + y.Fexp ≤ xprime.Fexp := by
  subst beta
  let s : Nat := t - Nat.div2 t
  have hSLeInt : (2 : Int) ≤ (t : Int) - (Nat.div2 t : Int) :=
    SLe t hpGe
  have hSGeInt : (t : Int) - (Nat.div2 t : Int) ≤ (t : Int) - 2 :=
    SGe t hpGe
  have hsGe : 2 ≤ s := by
    dsimp [s]
    omega
  have hsLe : s ≤ t - 2 := by
    dsimp [s]
    omega
  have hRadixSplit : (s - 1) + (s - 1) ≤ t := by
    have h := s2Le_internal t
    dsimp [s]
    omega
  have hEvenSplit (htEven : Even t) : s + s ≤ t := by
    rcases htEven with ⟨k, hk⟩
    dsimp [s]
    rw [Nat.div2_val]
    omega
  have htNe : t ≠ 0 := by omega
  have hxBound : Fbounded (beta:=radix) b x := hxNormal.1
  have hyBound : Fbounded (beta:=radix) b y := hyNormal.1
  have product_from_split :
      ∀ (sx sy : Nat) (x2 y2 : FloatSpec.Core.Defs.FlocqFloat radix),
        _root_.F2R (beta:=radix) x2 = _root_.F2R (beta:=radix) tx →
        _root_.F2R (beta:=radix) y2 = _root_.F2R (beta:=radix) ty →
        Fbounded (beta:=radix) (Veltkamp_splitBound radix b sx) x2 →
        Fbounded (beta:=radix) (Veltkamp_splitBound radix b sy) y2 →
        x.Fexp ≤ x2.Fexp →
        y.Fexp ≤ y2.Fexp →
        sx + sy ≤ t →
        ∃ xprime : FloatSpec.Core.Defs.FlocqFloat radix,
          _root_.F2R (beta:=radix) xprime =
              _root_.F2R (beta:=radix) tx * _root_.F2R (beta:=radix) ty ∧
            Fbounded (beta:=radix) b xprime ∧
            x.Fexp + y.Fexp ≤ xprime.Fexp := by
    intro sx sy x2 y2 hx2Val hy2Val hx2Bound hy2Bound hx2Exp hy2Exp hsum
    let prod : FloatSpec.Core.Defs.FlocqFloat radix :=
      FloatSpec.Calc.Operations.Fmult (beta:=radix) x2 y2
    refine ⟨prod, ?_, ?_, ?_⟩
    · have hmult := Fmult_correct (beta:=radix) x2 y2
      have hprod :
          _root_.F2R (beta:=radix) prod =
            _root_.F2R (beta:=radix) x2 * _root_.F2R (beta:=radix) y2 := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fmult_correct_check, Id.run,
          ULift.up_down, prod, Int.cast_ofNat] using hmult hradix
      rw [hprod, hx2Val, hy2Val]
    · rcases hx2Bound with ⟨hx2Num, _hx2ExpLower⟩
      rcases hy2Bound with ⟨hy2Num, _hy2ExpLower⟩
      have hradixGe : (1 : Int) ≤ radix := le_of_lt hradix
      have hradixPos : (0 : Int) < radix := by omega
      have hpowSxPos : 0 < Zpower_nat radix sx := by
        simpa [Zpower_nat] using (pow_pos hradixPos sx)
      have hpowSyPos : 0 < Zpower_nat radix sy := by
        simpa [Zpower_nat] using (pow_pos hradixPos sy)
      have hx2NumSplit : |x2.Fnum| < Zpower_nat radix sx := by
        simpa [Veltkamp_splitBound] using hx2Num
      have hy2NumSplit : |y2.Fnum| < Zpower_nat radix sy := by
        simpa [Veltkamp_splitBound] using hy2Num
      have hx2NumAbsNonneg : 0 ≤ |x2.Fnum| := abs_nonneg x2.Fnum
      have hprodLtSplit :
          |x2.Fnum * y2.Fnum| <
            Zpower_nat radix sx * Zpower_nat radix sy := by
        rw [abs_mul]
        by_cases hxZero : |x2.Fnum| = 0
        · rw [hxZero, zero_mul]
          exact mul_pos hpowSxPos hpowSyPos
        · have hxNonzero : (0 : Int) ≠ |x2.Fnum| := fun h => hxZero h.symm
          have hxPos : 0 < |x2.Fnum| := lt_of_le_of_ne hx2NumAbsNonneg hxNonzero
          have hleft :
              |x2.Fnum| * |y2.Fnum| <
                |x2.Fnum| * Zpower_nat radix sy :=
            mul_lt_mul_of_pos_left hy2NumSplit hxPos
          have hright :
              |x2.Fnum| * Zpower_nat radix sy ≤
                Zpower_nat radix sx * Zpower_nat radix sy :=
            mul_le_mul_of_nonneg_right (le_of_lt hx2NumSplit) (le_of_lt hpowSyPos)
          exact lt_of_lt_of_le hleft hright
      have hsplitLeOrig :
          Zpower_nat radix sx * Zpower_nat radix sy ≤ Zpower_nat radix t := by
        calc
          Zpower_nat radix sx * Zpower_nat radix sy =
              Zpower_nat radix (sx + sy) := by
                simp [Zpower_nat, pow_add]
          _ ≤ Zpower_nat radix t := by
              simpa [Zpower_nat] using pow_le_pow_right₀ hradixGe hsum
      have hnum : |prod.Fnum| < b.vNum := by
        have hlt : |x2.Fnum * y2.Fnum| < Zpower_nat radix t :=
          lt_of_lt_of_le hprodLtSplit hsplitLeOrig
        simpa [prod, FloatSpec.Calc.Operations.Fmult, hvNum] using hlt
      have hexp : -b.dExp ≤ prod.Fexp := by
        dsimp [prod, FloatSpec.Calc.Operations.Fmult]
        exact le_trans hK (add_le_add hx2Exp hy2Exp)
      exact ⟨hnum, hexp⟩
    · dsimp [prod, FloatSpec.Calc.Operations.Fmult]
      omega
  rcases hBranch with hradixTwo | htEven
  · rcases Veltkamp_tail2 (beta:=radix) b radix s t x p q hx tx rfl hradix
        hvNum hsGe hsLe hradixTwo hxBound (by simpa [s] using hA1)
        hA2 hA3 hA4 with
      ⟨x2, hx2Val, _hxDecomp, hx2Bound, hx2NormExp⟩
    rcases Veltkamp_tail2 (beta:=radix) b radix s t y p' q' hy ty rfl hradix
        hvNum hsGe hsLe hradixTwo hyBound (by simpa [s] using hB1)
        hB2 hB3 hB4 with
      ⟨y2, hy2Val, _hyDecomp, hy2Bound, hy2NormExp⟩
    have hxNormEq : (Fnormalize (beta:=radix) radix b t x).Fexp = x.Fexp := by
      have h := FcanonicFnormalizeEq (beta:=radix) radix b t x
      have heq :
          Fnormalize (beta:=radix) radix b t x = x := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          h ⟨Or.inl hxNormal, rfl, hradix, htNe, hvNum⟩
      rw [heq]
    have hyNormEq : (Fnormalize (beta:=radix) radix b t y).Fexp = y.Fexp := by
      have h := FcanonicFnormalizeEq (beta:=radix) radix b t y
      have heq :
          Fnormalize (beta:=radix) radix b t y = y := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using
          h ⟨Or.inl hyNormal, rfl, hradix, htNe, hvNum⟩
      rw [heq]
    exact product_from_split (s - 1) (s - 1) x2 y2 hx2Val hy2Val
      hx2Bound hy2Bound (by simpa [hxNormEq] using hx2NormExp)
      (by simpa [hyNormEq] using hy2NormExp) hRadixSplit
  · rcases VeltkampU (beta:=radix) b radix s t x p q hx tx rfl hradix
        hvNum hsGe hsLe (Or.inl hxNormal) (by simpa [s] using hA1)
        hA2 hA3 hA4 with
      ⟨_hxAbs, _hxEq, _hxHead, hxTail⟩
    rcases hxTail with ⟨x2, hx2Val, hx2Bound, hx2Exp⟩
    rcases VeltkampU (beta:=radix) b radix s t y p' q' hy ty rfl hradix
        hvNum hsGe hsLe (Or.inl hyNormal) (by simpa [s] using hB1)
        hB2 hB3 hB4 with
      ⟨_hyAbs, _hyEq, _hyHead, hyTail⟩
    rcases hyTail with ⟨y2, hy2Val, hy2Bound, hy2Exp⟩
    exact product_from_split s s x2 y2 hx2Val hy2Val hx2Bound hy2Bound
      hx2Exp hy2Exp (hEvenSplit htEven)

/-! Coq Algo theorem `Dekker_aux`.

This is the main algebraic reconstruction for the normal Dekker path.  The
last product `tx * ty` is supplied as the same bounded witness assumed by
upstream; `Boundedx2y2` is the later theorem that constructs that witness. -/
theorem Dekker_aux {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (t : Nat)
    (x y p q hx tx p' q' hy ty x1y1 x1y2 x2y1 x2y2 r t1 t2 t3 t4 :
      FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hpGe : 4 ≤ t)
    (hxNormal : Fnormal (beta:=beta) radix b x)
    (hyNormal : Fnormal (beta:=beta) radix b y)
    (hK : -b.dExp ≤ x.Fexp + y.Fexp)
    (hA1 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x *
        ((radix : ℝ) ^ (((t - Nat.div2 t : Nat) : Int)) + 1)) p)
    (hA2 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hA3 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) q + _root_.F2R (beta:=beta) p) hx)
    (hA4 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) hx) tx)
    (hB1 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) y *
        ((radix : ℝ) ^ (((t - Nat.div2 t : Nat) : Int)) + 1)) p')
    (hB2 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) y - _root_.F2R (beta:=beta) p') q')
    (hB3 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) q' + _root_.F2R (beta:=beta) p') hy)
    (hB4 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) y - _root_.F2R (beta:=beta) hy) ty)
    (hC1 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) hx * _root_.F2R (beta:=beta) hy) x1y1)
    (hC2 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) hx * _root_.F2R (beta:=beta) ty) x1y2)
    (hC3 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) tx * _root_.F2R (beta:=beta) hy) x2y1)
    (hC4 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) tx * _root_.F2R (beta:=beta) ty) x2y2)
    (hD1 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * _root_.F2R (beta:=beta) y) r)
    (hD2 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) x1y1) t1)
    (hD3 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) t1 - _root_.F2R (beta:=beta) x1y2) t2)
    (hD4 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) t2 - _root_.F2R (beta:=beta) x2y1) t3)
    (hD5 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) t3 - _root_.F2R (beta:=beta) x2y2) t4)
    (hTxTy : ∃ xprime : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) xprime =
          _root_.F2R (beta:=beta) tx * _root_.F2R (beta:=beta) ty ∧
        Fbounded (beta:=beta) b xprime) :
    _root_.F2R (beta:=beta) x * _root_.F2R (beta:=beta) y =
      _root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) t4 := by
  subst beta
  let s : Nat := t - Nat.div2 t
  have hSLeInt : (2 : Int) ≤ (t : Int) - (Nat.div2 t : Int) :=
    SLe t hpGe
  have hSGeInt : (t : Int) - (Nat.div2 t : Int) ≤ (t : Int) - 2 :=
    SGe t hpGe
  have hsGe : 2 ≤ s := by
    dsimp [s]
    omega
  have hsLe : s ≤ t - 2 := by
    dsimp [s]
    omega
  have hHst1 : (t : Int) - 1 ≤ 2 * (s : Int) := by
    have h := s2Ge t
    dsimp [s]
    omega
  have hHst2 : 2 * (s : Int) ≤ (t : Int) + 1 := by
    have h := s2Le_internal t
    dsimp [s]
    omega
  have hHst3 : (t : Int) ≤ 2 * (s : Int) := by
    have h := s2Ge t
    dsimp [s]
    omega
  have closest_projector_eq :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat radix,
        Fbounded (beta:=radix) b p →
        Closest (beta:=radix) b (radix : ℝ)
          (_root_.F2R (beta:=radix) p) q →
        _root_.F2R (beta:=radix) q = _root_.F2R (beta:=radix) p := by
    intro p q hpBound hClosest
    have hdist := hClosest.2 p hpBound
    rw [sub_self, abs_zero] at hdist
    have hzero :
        |_root_.F2R (beta:=radix) q - _root_.F2R (beta:=radix) p| = 0 :=
      le_antisymm hdist (abs_nonneg _)
    exact sub_eq_zero.mp (abs_eq_zero.mp hzero)
  rcases VeltkampU (beta:=radix) b radix s t x p q hx tx rfl hradix
      hvNum hsGe hsLe (Or.inl hxNormal)
      (by simpa [s] using hA1) hA2 hA3 hA4 with
    ⟨hx2Le, hXeq, hxHead, hxTail⟩
  rcases hxHead with ⟨hx', hhxVal, hhxBound, hhxExpFn⟩
  rcases hxTail with ⟨tx', htxVal, htxBound, htxExp⟩
  have hhxExp : (s : Int) + x.Fexp ≤ hx'.Fexp := hhxExpFn hxNormal
  rcases VeltkampU (beta:=radix) b radix s t y p' q' hy ty rfl hradix
      hvNum hsGe hsLe (Or.inl hyNormal)
      (by simpa [s] using hB1) hB2 hB3 hB4 with
    ⟨hy2Le, hYeq, hyHead, hyTail⟩
  rcases hyHead with ⟨hy', hhyVal, hhyBound, hhyExpFn⟩
  rcases hyTail with ⟨ty', htyVal, htyBound, htyExp⟩
  have hhyExp : (s : Int) + y.Fexp ≤ hy'.Fexp := hhyExpFn hyNormal
  have htxAbsLe :
      |_root_.F2R (beta:=radix) tx| ≤
        (radix : ℝ) ^ ((s : Int) + x.Fexp) / 2 := by
    have hsub :
        _root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) hx =
          _root_.F2R (beta:=radix) tx := by
      linarith
    simpa [hsub] using hx2Le
  have htyAbsLe :
      |_root_.F2R (beta:=radix) ty| ≤
        (radix : ℝ) ^ ((s : Int) + y.Fexp) / 2 := by
    have hsub :
        _root_.F2R (beta:=radix) y - _root_.F2R (beta:=radix) hy =
          _root_.F2R (beta:=radix) ty := by
      linarith
    simpa [hsub] using hy2Le
  let e : FloatSpec.Core.Defs.FlocqFloat radix :=
    Fminus (beta:=radix) (FloatSpec.Calc.Operations.Fmult (beta:=radix) x y) r
  have hee :
      _root_.F2R (beta:=radix) x * _root_.F2R (beta:=radix) y =
        _root_.F2R (beta:=radix) r + _root_.F2R (beta:=radix) e := by
    have hmult := Fmult_correct (beta:=radix) x y
    have hmultVal :
        _root_.F2R (beta:=radix)
            (FloatSpec.Calc.Operations.Fmult (beta:=radix) x y) =
          _root_.F2R (beta:=radix) x *
            _root_.F2R (beta:=radix) y := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fmult_correct_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using hmult hradix
    have hminus := Fminus_correct (beta:=radix)
      (FloatSpec.Calc.Operations.Fmult (beta:=radix) x y) r
    have hminusVal :
        _root_.F2R (beta:=radix) e =
          _root_.F2R (beta:=radix)
              (FloatSpec.Calc.Operations.Fmult (beta:=radix) x y) -
            _root_.F2R (beta:=radix) r := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus,
        Id.run, ULift.up_down, e, Int.cast_ofNat] using hminus hradix
    rw [hminusVal, hmultVal]
    ring
  rcases Boundedx1y1 (beta:=radix) b radix s t x hx' y hy' rfl hradix
      hvNum hK hhxExp hhyExp hhxBound hhyBound hHst3 with
    ⟨prod11, hprod11Val, hprod11Bound⟩
  have hx1y1Val :
      _root_.F2R (beta:=radix) x1y1 =
        _root_.F2R (beta:=radix) hx * _root_.F2R (beta:=radix) hy := by
    have hproj :
        _root_.F2R (beta:=radix) x1y1 =
          _root_.F2R (beta:=radix) prod11 :=
      closest_projector_eq prod11 x1y1 hprod11Bound
        (by simpa [hprod11Val, hhxVal, hhyVal] using hC1)
    rw [hproj, hprod11Val, hhxVal, hhyVal]
  rcases Boundedx1y2 (beta:=radix) b radix s t x hx' y ty' rfl hradix
      hvNum hsLe hK hhxExp htyExp hhxBound htyBound with
    ⟨prod12, hprod12Val, hprod12Bound⟩
  have hx1y2Val :
      _root_.F2R (beta:=radix) x1y2 =
        _root_.F2R (beta:=radix) hx * _root_.F2R (beta:=radix) ty := by
    have hproj :
        _root_.F2R (beta:=radix) x1y2 =
          _root_.F2R (beta:=radix) prod12 :=
      closest_projector_eq prod12 x1y2 hprod12Bound
        (by simpa [hprod12Val, hhxVal, htyVal] using hC2)
    rw [hproj, hprod12Val, hhxVal, htyVal]
  rcases Boundedx2y1 (beta:=radix) b radix s t x tx' y hy' rfl hradix
      hvNum hsLe hK htxExp hhyExp htxBound hhyBound with
    ⟨prod21, hprod21Val, hprod21Bound⟩
  have hx2y1Val :
      _root_.F2R (beta:=radix) x2y1 =
        _root_.F2R (beta:=radix) tx * _root_.F2R (beta:=radix) hy := by
    have hproj :
        _root_.F2R (beta:=radix) x2y1 =
          _root_.F2R (beta:=radix) prod21 :=
      closest_projector_eq prod21 x2y1 hprod21Bound
        (by simpa [hprod21Val, htxVal, hhyVal] using hC3)
    rw [hproj, hprod21Val, htxVal, hhyVal]
  rcases hTxTy with ⟨prod22, hprod22Val, hprod22Bound⟩
  have hx2y2Val :
      _root_.F2R (beta:=radix) x2y2 =
        _root_.F2R (beta:=radix) tx * _root_.F2R (beta:=radix) ty := by
    have hproj :
        _root_.F2R (beta:=radix) x2y2 =
          _root_.F2R (beta:=radix) prod22 :=
      closest_projector_eq prod22 x2y2 hprod22Bound
        (by simpa [hprod22Val] using hC4)
    rw [hproj, hprod22Val]
  rcases Boundedt1 (beta:=radix) b radix s t x hx' tx' y hy' ty' r e
      rfl hradix hvNum hsGe hsLe hHst1 hHst2 hxNormal hyNormal hK hD1
      hee
      (by rw [hhxVal, htxVal]; exact hXeq)
      (by rw [hhyVal, htyVal]; exact hYeq)
      (by simpa [htxVal] using htxAbsLe)
      (by simpa [htyVal] using htyAbsLe)
      hhxExp hhyExp with
    ⟨u1, hu1Val, hu1Bound, _hu1Exp⟩
  have ht1Eq :
      _root_.F2R (beta:=radix) t1 = _root_.F2R (beta:=radix) u1 := by
    have hinput :
        _root_.F2R (beta:=radix) r - _root_.F2R (beta:=radix) x1y1 =
          _root_.F2R (beta:=radix) u1 := by
      rw [hu1Val, hx1y1Val, hhxVal, hhyVal]
    exact closest_projector_eq u1 t1 hu1Bound (by simpa [hinput] using hD2)
  rcases Boundedt2 (beta:=radix) b radix s t x hx' tx' y hy' ty' r e
      rfl hradix hvNum hsGe hsLe hHst1 hHst2 hxNormal hyNormal hK hD1
      hee
      (by rw [hhxVal, htxVal]; exact hXeq)
      (by rw [hhyVal, htyVal]; exact hYeq)
      (by simpa [htxVal] using htxAbsLe)
      (by simpa [htyVal] using htyAbsLe)
      hhxExp hhyExp htyExp with
    ⟨u2, hu2Val, hu2Bound, _hu2Exp⟩
  have ht2Eq :
      _root_.F2R (beta:=radix) t2 = _root_.F2R (beta:=radix) u2 := by
    have hinput :
        _root_.F2R (beta:=radix) t1 - _root_.F2R (beta:=radix) x1y2 =
          _root_.F2R (beta:=radix) u2 := by
      rw [ht1Eq, hu1Val, hx1y2Val, hu2Val, hhxVal, hhyVal, htyVal]
    exact closest_projector_eq u2 t2 hu2Bound (by simpa [hinput] using hD3)
  rcases Boundedt3 (beta:=radix) b radix s t x hx' tx' y hy' ty' r e
      rfl hradix hvNum hsGe hsLe hHst1 hHst2 hxNormal hyNormal hK hD1
      hee
      (by rw [hhxVal, htxVal]; exact hXeq)
      (by rw [hhyVal, htyVal]; exact hYeq)
      (by simpa [htxVal] using htxAbsLe)
      (by simpa [htyVal] using htyAbsLe)
      hhxExp hhyExp htxExp htyExp with
    ⟨u3, hu3Val, hu3Bound, _hu3Exp⟩
  have ht3Eq :
      _root_.F2R (beta:=radix) t3 = _root_.F2R (beta:=radix) u3 := by
    have hinput :
        _root_.F2R (beta:=radix) t2 - _root_.F2R (beta:=radix) x2y1 =
          _root_.F2R (beta:=radix) u3 := by
      rw [ht2Eq, hu2Val, hx2y1Val, hu3Val, hhxVal, hhyVal, htxVal, htyVal]
    exact closest_projector_eq u3 t3 hu3Bound (by simpa [hinput] using hD4)
  rcases Boundedt4 (beta:=radix) b radix s t x hx' tx' y hy' ty' r
      rfl hradix hvNum hsGe hsLe hxNormal hyNormal hK hD1
      (by rw [hhxVal, htxVal]; exact hXeq)
      (by rw [hhyVal, htyVal]; exact hYeq) with
    ⟨u4, hu4Val, hu4Bound⟩
  have ht4Eq :
      _root_.F2R (beta:=radix) t4 = _root_.F2R (beta:=radix) u4 := by
    have hinput :
        _root_.F2R (beta:=radix) t3 - _root_.F2R (beta:=radix) x2y2 =
          _root_.F2R (beta:=radix) u4 := by
      rw [ht3Eq, hu3Val, hx2y2Val, hu4Val, hhxVal, hhyVal, htxVal, htyVal]
    exact closest_projector_eq u4 t4 hu4Bound (by simpa [hinput] using hD5)
  have ht4Val :
      _root_.F2R (beta:=radix) t4 =
        _root_.F2R (beta:=radix) r -
          _root_.F2R (beta:=radix) x * _root_.F2R (beta:=radix) y := by
    rw [ht4Eq, hu4Val, hhxVal, hhyVal, htxVal, htyVal, hXeq, hYeq]
    ring
  rw [ht4Val]
  ring

/-! Coq Algo theorem `DekkerN`.

This is the exact upstream wrapper at `Pff.v:17820`: the branch premise builds
the bounded `tx * ty` representative with `Boundedx2y2`, then `Dekker_aux`
performs the algebraic reconstruction. -/
theorem DekkerN {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (t : Nat)
    (x y p q hx tx p' q' hy ty x1y1 x1y2 x2y1 x2y2 r t1 t2 t3 t4 :
      FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hpGe : 4 ≤ t)
    (hxNormal : Fnormal (beta:=beta) radix b x)
    (hyNormal : Fnormal (beta:=beta) radix b y)
    (hK : -b.dExp ≤ x.Fexp + y.Fexp)
    (hA1 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x *
        ((radix : ℝ) ^ (((t - Nat.div2 t : Nat) : Int)) + 1)) p)
    (hA2 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hA3 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) q + _root_.F2R (beta:=beta) p) hx)
    (hA4 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) hx) tx)
    (hB1 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) y *
        ((radix : ℝ) ^ (((t - Nat.div2 t : Nat) : Int)) + 1)) p')
    (hB2 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) y - _root_.F2R (beta:=beta) p') q')
    (hB3 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) q' + _root_.F2R (beta:=beta) p') hy)
    (hB4 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) y - _root_.F2R (beta:=beta) hy) ty)
    (hC1 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) hx * _root_.F2R (beta:=beta) hy) x1y1)
    (hC2 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) hx * _root_.F2R (beta:=beta) ty) x1y2)
    (hC3 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) tx * _root_.F2R (beta:=beta) hy) x2y1)
    (hC4 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) tx * _root_.F2R (beta:=beta) ty) x2y2)
    (hD1 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * _root_.F2R (beta:=beta) y) r)
    (hD2 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) x1y1) t1)
    (hD3 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) t1 - _root_.F2R (beta:=beta) x1y2) t2)
    (hD4 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) t2 - _root_.F2R (beta:=beta) x2y1) t3)
    (hD5 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) t3 - _root_.F2R (beta:=beta) x2y2) t4)
    (hBranch : radix = 2 ∨ Even t) :
    _root_.F2R (beta:=beta) x * _root_.F2R (beta:=beta) y =
      _root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) t4 := by
  rcases Boundedx2y2 (beta:=beta) b radix t x y p q hx tx p' q' hy ty
      hbeta hradix hvNum hpGe hK hxNormal hyNormal hA1 hA2 hA3 hA4
      hB1 hB2 hB3 hB4 hBranch with
    ⟨prod22, hprod22Val, hprod22Bound, _hprod22Exp⟩
  exact Dekker_aux (beta:=beta) b radix t x y p q hx tx p' q' hy ty
    x1y1 x1y2 x2y1 x2y2 r t1 t2 t3 t4 hbeta hradix hvNum hpGe
    hxNormal hyNormal hK hA1 hA2 hA3 hA4 hB1 hB2 hB3 hB4 hC1 hC2 hC3 hC4
    hD1 hD2 hD3 hD4 hD5 ⟨prod22, hprod22Val, hprod22Bound⟩

/-! Coq Algo2 theorem `Dekker2_aux`.

This is the upstream underflow companion after `DekkerN`: nonzero
canonical inputs are normalized into Dekker's extended bound, the product and
four correction residuals are related back to the original bound with
`Underf_Err`, and the final error is the sum of the product underflow error and
the four residual underflow steps. -/
theorem Dekker2_aux {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (t : Nat)
    (x y p q hx tx p' q' hy ty x1y1 x1y2 x2y1 x2y2 r t1 t2 t3 t4 :
      FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hpGe : 4 ≤ t)
    (hxCan : Fcanonic (beta:=beta) radix b x)
    (hyCan : Fcanonic (beta:=beta) radix b y)
    (hxNonzero : _root_.F2R (beta:=beta) x ≠ 0)
    (hyNonzero : _root_.F2R (beta:=beta) y ≠ 0)
    (hA1 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x *
        ((radix : ℝ) ^ (((t - Nat.div2 t : Nat) : Int)) + 1)) p)
    (hA2 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hA3 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) q + _root_.F2R (beta:=beta) p) hx)
    (hA4 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) hx) tx)
    (hB1 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) y *
        ((radix : ℝ) ^ (((t - Nat.div2 t : Nat) : Int)) + 1)) p')
    (hB2 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) y - _root_.F2R (beta:=beta) p') q')
    (hB3 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) q' + _root_.F2R (beta:=beta) p') hy)
    (hB4 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) y - _root_.F2R (beta:=beta) hy) ty)
    (hC1 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) hx * _root_.F2R (beta:=beta) hy) x1y1)
    (hC2 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) hx * _root_.F2R (beta:=beta) ty) x1y2)
    (hC3 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) tx * _root_.F2R (beta:=beta) hy) x2y1)
    (hC4 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) tx * _root_.F2R (beta:=beta) ty) x2y2)
    (hD1 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * _root_.F2R (beta:=beta) y) r)
    (hD2 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) x1y1) t1)
    (hD3 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) t1 - _root_.F2R (beta:=beta) x1y2) t2)
    (hD4 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) t2 - _root_.F2R (beta:=beta) x2y1) t3)
    (hD5 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) t3 - _root_.F2R (beta:=beta) x2y2) t4)
    (hBranch : radix = 2 ∨ Even t) :
    |_root_.F2R (beta:=beta) x * _root_.F2R (beta:=beta) y -
        (_root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) t4)| ≤
      (7 / 2 : ℝ) * (radix : ℝ) ^ (-(b.dExp)) := by
  subst beta
  have hdExp : 0 ≤ b.dExp := b.dExp_nonneg
  let s : Nat := t - Nat.div2 t
  let b' : Fbound_skel := Dekker_extendedBound b t
  have hsGe : 2 ≤ s := by
    have h := SLe t hpGe
    dsimp [s]
    omega
  have hsLe : s ≤ t - 2 := by
    dsimp [s]
    have h := SGe t hpGe
    omega
  have hHst1 : (t : Int) - 1 ≤ 2 * (s : Int) := by
    have h := s2Ge t
    dsimp [s]
    omega
  have hHst2 : 2 * (s : Int) ≤ (t : Int) + 1 := by
    have h := s2Le_internal t
    dsimp [s]
    omega
  have hHst3 : (t : Int) ≤ 2 * (s : Int) := by
    have h := s2Ge t
    dsimp [s]
    omega
  have hb'Num : b'.vNum = Zpower_nat radix t := by
    simpa [b', Dekker_extendedBound] using hvNum
  have hb'NumEq : b.vNum = b'.vNum := by
    rw [hb'Num]
    exact hvNum
  have hb'ExpLt : b.dExp < b'.dExp := by
    simpa [b'] using dExpPrim_from_nonnegative_payload b t hdExp
  have hb'ExpLe : b.dExp ≤ b'.dExp := le_of_lt hb'ExpLt
  have hb'ExpNonneg : 0 ≤ b'.dExp := le_trans hdExp hb'ExpLe
  have htPrecision : 1 < t := by omega
  have hb'VNumGt : 1 < b'.vNum := by
    rw [hb'Num, Zpower_nat]
    calc
      (1 : Int) < 2 := by norm_num
      _ ≤ radix := by omega
      _ = radix ^ (1 : Nat) := by simp
      _ ≤ radix ^ t := by
        exact pow_le_pow_right₀ (by omega : (1 : Int) ≤ radix) (by omega : 1 ≤ t)
  have hBoundRExp :
      ∀ z : ℝ, -b'.dExp ≤ (boundR (beta:=radix) radix z).Fexp := by
    intro z
    have hnonneg :
        (0 : Int) ≤ (boundR (beta:=radix) radix z).Fexp := by
      simp [boundR, boundNat]
    omega
  have hMinTotal : TotalP (isMin (beta:=radix) b' radix) := by
    intro z
    have h := MinEx_from_finite_box_payload (beta:=radix) b' radix z
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hb'VNumGt, hBoundRExp z⟩
  have hMaxTotal : TotalP (isMax (beta:=radix) b' radix) := by
    intro z
    have h := MaxEx_from_finite_box_payload (beta:=radix) b' radix z
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MaxEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hb'VNumGt, hBoundRExp z⟩
  have hClosestTotal : TotalP (Closest (beta:=radix) b' (radix : ℝ)) := by
    intro z
    have h := ClosestTotal_from_extrema_payload (beta:=radix) b' radix (radix : ℝ) z
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestTotal_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨hMinTotal, hMaxTotal⟩
  have closest_projector_eq :
      ∀ p q : FloatSpec.Core.Defs.FlocqFloat radix,
        Fbounded (beta:=radix) b' p →
        Closest (beta:=radix) b' (radix : ℝ)
          (_root_.F2R (beta:=radix) p) q →
        _root_.F2R (beta:=radix) q = _root_.F2R (beta:=radix) p := by
    intro p q hpBound hClosest
    have hdist := hClosest.2 p hpBound
    rw [sub_self, abs_zero] at hdist
    have hzero :
        |_root_.F2R (beta:=radix) q - _root_.F2R (beta:=radix) p| = 0 :=
      le_antisymm hdist (abs_nonneg _)
    exact sub_eq_zero.mp (abs_eq_zero.mp hzero)
  have hxBound : Fbounded (beta:=radix) b x := by
    have h := FcanonicBound (beta:=radix) radix b x
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicBound_check, Int.cast_ofNat] using h hxCan
  have hyBound : Fbounded (beta:=radix) b y := by
    have h := FcanonicBound (beta:=radix) radix b y
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicBound_check, Int.cast_ofNat] using h hyCan
  rcases NormalbPrim_from_nonnegative_payload (beta:=radix)
      b radix t x rfl hradix hvNum hpGe
      hdExp hxCan hxNonzero with
    ⟨x', hx'Normal, hx'Val, hx'ExpLower⟩
  rcases NormalbPrim_from_nonnegative_payload (beta:=radix)
      b radix t y rfl hradix hvNum hpGe
      hdExp hyCan hyNonzero with
    ⟨y', hy'Normal, hy'Val, hy'ExpLower⟩
  have hK' : -b'.dExp ≤ x'.Fexp + y'.Fexp := by
    have hb'eq : b'.dExp = 2 * b.dExp + 2 * (t : Int) + 2 := by
      simpa [b'] using dExpPrimEq b t
    omega
  rcases Underf_Err2 (beta:=radix) b b' radix t
      (_root_.F2R (beta:=radix) x * _root_.F2R (beta:=radix) y) r
      rfl hradix hvNum htPrecision hb'NumEq hb'ExpLe hD1 with
    ⟨r', hrUnder, hrClosest'⟩
  have hrClosestX' : Closest (beta:=radix) b' (radix : ℝ)
      (_root_.F2R (beta:=radix) x' * _root_.F2R (beta:=radix) y') r' := by
    simpa [hx'Val, hy'Val] using hrClosest'
  rcases Veltkampb' (beta:=radix) b radix t x p q hx tx
      rfl hradix hvNum hpGe hb'ExpLt hxBound hA1 hA2 hA3 hA4 with
    ⟨hA1', hA2', hA3', hA4'⟩
  rcases Veltkampb' (beta:=radix) b radix t y p' q' hy ty
      rfl hradix hvNum hpGe hb'ExpLt hyBound hB1 hB2 hB3 hB4 with
    ⟨hB1', hB2', hB3', hB4'⟩
  have hA1x' : Closest (beta:=radix) b' (radix : ℝ)
      (_root_.F2R (beta:=radix) x' *
        ((radix : ℝ) ^ (((t - Nat.div2 t : Nat) : Int)) + 1)) p := by
    simpa [b', hx'Val] using hA1'
  have hA2x' : Closest (beta:=radix) b' (radix : ℝ)
      (_root_.F2R (beta:=radix) x' - _root_.F2R (beta:=radix) p) q := by
    simpa [b', hx'Val] using hA2'
  have hA3x' : Closest (beta:=radix) b' (radix : ℝ)
      (_root_.F2R (beta:=radix) q + _root_.F2R (beta:=radix) p) hx := by
    simpa [b'] using hA3'
  have hA4x' : Closest (beta:=radix) b' (radix : ℝ)
      (_root_.F2R (beta:=radix) x' - _root_.F2R (beta:=radix) hx) tx := by
    simpa [b', hx'Val] using hA4'
  have hB1y' : Closest (beta:=radix) b' (radix : ℝ)
      (_root_.F2R (beta:=radix) y' *
        ((radix : ℝ) ^ (((t - Nat.div2 t : Nat) : Int)) + 1)) p' := by
    simpa [b', hy'Val] using hB1'
  have hB2y' : Closest (beta:=radix) b' (radix : ℝ)
      (_root_.F2R (beta:=radix) y' - _root_.F2R (beta:=radix) p') q' := by
    simpa [b', hy'Val] using hB2'
  have hB3y' : Closest (beta:=radix) b' (radix : ℝ)
      (_root_.F2R (beta:=radix) q' + _root_.F2R (beta:=radix) p') hy := by
    simpa [b'] using hB3'
  have hB4y' : Closest (beta:=radix) b' (radix : ℝ)
      (_root_.F2R (beta:=radix) y' - _root_.F2R (beta:=radix) hy) ty := by
    simpa [b', hy'Val] using hB4'
  rcases VeltkampU (beta:=radix) b' radix s t x' p q hx tx rfl hradix
      hb'Num hsGe hsLe (Or.inl hx'Normal)
      (by simpa [s] using hA1x') hA2x' hA3x' hA4x' with
    ⟨hxTailAbs, hxDecomp, hxHead, hxTail⟩
  rcases hxHead with ⟨chx, hchxVal, hchxBound, hchxExpFn⟩
  rcases hxTail with ⟨ctx, hctxVal, hctxBound, hctxExp⟩
  have hchxExp : (s : Int) + x'.Fexp ≤ chx.Fexp := hchxExpFn hx'Normal
  have hcxDecomp :
      _root_.F2R (beta:=radix) x' =
        _root_.F2R (beta:=radix) chx + _root_.F2R (beta:=radix) ctx := by
    rw [hxDecomp, ← hchxVal, ← hctxVal]
  have hctxAbs : |_root_.F2R (beta:=radix) ctx| ≤
      (radix : ℝ) ^ ((s : Int) + x'.Fexp) / 2 := by
    have htxEq :
        _root_.F2R (beta:=radix) tx =
          _root_.F2R (beta:=radix) x' - _root_.F2R (beta:=radix) hx := by
      rw [hxDecomp]
      ring
    simpa [hctxVal, htxEq.symm] using hxTailAbs
  rcases VeltkampU (beta:=radix) b' radix s t y' p' q' hy ty rfl hradix
      hb'Num hsGe hsLe (Or.inl hy'Normal)
      (by simpa [s] using hB1y') hB2y' hB3y' hB4y' with
    ⟨hyTailAbs, hyDecomp, hyHead, hyTail⟩
  rcases hyHead with ⟨chy, hchyVal, hchyBound, hchyExpFn⟩
  rcases hyTail with ⟨cty, hctyVal, hctyBound, hctyExp⟩
  have hchyExp : (s : Int) + y'.Fexp ≤ chy.Fexp := hchyExpFn hy'Normal
  have hcyDecomp :
      _root_.F2R (beta:=radix) y' =
        _root_.F2R (beta:=radix) chy + _root_.F2R (beta:=radix) cty := by
    rw [hyDecomp, ← hchyVal, ← hctyVal]
  have hctyAbs : |_root_.F2R (beta:=radix) cty| ≤
      (radix : ℝ) ^ ((s : Int) + y'.Fexp) / 2 := by
    have htyEq :
        _root_.F2R (beta:=radix) ty =
          _root_.F2R (beta:=radix) y' - _root_.F2R (beta:=radix) hy := by
      rw [hyDecomp]
      ring
    simpa [hctyVal, htyEq.symm] using hyTailAbs
  let err : FloatSpec.Core.Defs.FlocqFloat radix :=
    Fminus (beta:=radix)
      (FloatSpec.Calc.Operations.Fmult (beta:=radix) x' y') r'
  have herrVal :
      _root_.F2R (beta:=radix) err =
        _root_.F2R (beta:=radix) x' * _root_.F2R (beta:=radix) y' -
          _root_.F2R (beta:=radix) r' := by
    have hminus := Fminus_correct (beta:=radix)
      (FloatSpec.Calc.Operations.Fmult (beta:=radix) x' y') r'
    have hminusVal :
        _root_.F2R (beta:=radix) err =
          _root_.F2R (beta:=radix)
              (FloatSpec.Calc.Operations.Fmult (beta:=radix) x' y') -
            _root_.F2R (beta:=radix) r' := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus,
        Id.run, ULift.up_down, err, Int.cast_ofNat] using hminus hradix
    have hmult := Fmult_correct (beta:=radix) x' y'
    have hmultVal :
        _root_.F2R (beta:=radix)
            (FloatSpec.Calc.Operations.Fmult (beta:=radix) x' y') =
          _root_.F2R (beta:=radix) x' * _root_.F2R (beta:=radix) y' := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fmult_correct_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using hmult hradix
    rw [hminusVal, hmultVal]
  have hProdEq :
      _root_.F2R (beta:=radix) x' * _root_.F2R (beta:=radix) y' =
        _root_.F2R (beta:=radix) r' + _root_.F2R (beta:=radix) err := by
    rw [herrVal]
    ring
  rcases Boundedt1 (beta:=radix) b' radix s t x' chx ctx y' chy cty r' err
      rfl hradix hb'Num hsGe hsLe hHst1 hHst2 hx'Normal hy'Normal hK'
      hrClosestX' hProdEq hcxDecomp hcyDecomp hctxAbs hctyAbs
      hchxExp hchyExp with
    ⟨ct1, hct1Val, hct1Bound, hct1Exp⟩
  rcases Boundedt2 (beta:=radix) b' radix s t x' chx ctx y' chy cty r' err
      rfl hradix hb'Num hsGe hsLe hHst1 hHst2 hx'Normal hy'Normal hK'
      hrClosestX' hProdEq hcxDecomp hcyDecomp hctxAbs hctyAbs
      hchxExp hchyExp hctyExp with
    ⟨ct2, hct2Val, hct2Bound, hct2Exp⟩
  rcases Boundedt3 (beta:=radix) b' radix s t x' chx ctx y' chy cty r' err
      rfl hradix hb'Num hsGe hsLe hHst1 hHst2 hx'Normal hy'Normal hK'
      hrClosestX' hProdEq hcxDecomp hcyDecomp hctxAbs hctyAbs
      hchxExp hchyExp hctxExp hctyExp with
    ⟨ct3, hct3Val, hct3Bound, hct3Exp⟩
  rcases Boundedt4_aux (beta:=radix) b' radix s t x' chx ctx y' chy cty r'
      rfl hradix hb'Num hsGe hsLe hx'Normal hy'Normal hK' hrClosestX'
      hcxDecomp hcyDecomp with
    ⟨ct4, hct4Val, hct4Bound, hct4Exp⟩
  rcases Boundedx1y1_aux (beta:=radix) b' radix s t x' chx y' chy
      rfl hradix hb'Num hK' hchxExp hchyExp hchxBound hchyBound hHst3 with
    ⟨cx1y1, hcx1y1Val, hcx1y1Bound, hcx1y1Exp⟩
  rcases Boundedx1y2_aux (beta:=radix) b' radix s t x' chx y' cty
      rfl hradix hb'Num hsLe hK' hchxExp hctyExp hchxBound hctyBound with
    ⟨cx1y2, hcx1y2Val, hcx1y2Bound, hcx1y2Exp⟩
  rcases Boundedx2y1_aux (beta:=radix) b' radix s t x' ctx y' chy
      rfl hradix hb'Num hsLe hK' hctxExp hchyExp hctxBound hchyBound with
    ⟨cx2y1, hcx2y1Val, hcx2y1Bound, hcx2y1Exp⟩
  rcases Boundedx2y2 (beta:=radix) b' radix t x' y' p q hx tx p' q' hy ty
      rfl hradix hb'Num hpGe hK' hx'Normal hy'Normal hA1x' hA2x'
      hA3x' hA4x' hB1y' hB2y' hB3y' hB4y' hBranch with
    ⟨cx2y2, hcx2y2Val, hcx2y2Bound, hcx2y2Exp⟩
  rcases hClosestTotal (_root_.F2R (beta:=radix) hx *
      _root_.F2R (beta:=radix) hy) with
    ⟨x1y1', hC1'⟩
  rcases hClosestTotal (_root_.F2R (beta:=radix) hx *
      _root_.F2R (beta:=radix) ty) with
    ⟨x1y2', hC2'⟩
  rcases hClosestTotal (_root_.F2R (beta:=radix) tx *
      _root_.F2R (beta:=radix) hy) with
    ⟨x2y1', hC3'⟩
  rcases hClosestTotal (_root_.F2R (beta:=radix) tx *
      _root_.F2R (beta:=radix) ty) with
    ⟨x2y2', hC4'⟩
  rcases hClosestTotal (_root_.F2R (beta:=radix) r' -
      _root_.F2R (beta:=radix) x1y1') with
    ⟨t1', hD2'⟩
  rcases hClosestTotal (_root_.F2R (beta:=radix) t1' -
      _root_.F2R (beta:=radix) x1y2') with
    ⟨t2', hD3'⟩
  rcases hClosestTotal (_root_.F2R (beta:=radix) t2' -
      _root_.F2R (beta:=radix) x2y1') with
    ⟨t3', hD4'⟩
  rcases hClosestTotal (_root_.F2R (beta:=radix) t3' -
      _root_.F2R (beta:=radix) x2y2') with
    ⟨t4', hD5'⟩
  have hx1y1'Eq : _root_.F2R (beta:=radix) x1y1' =
      _root_.F2R (beta:=radix) cx1y1 := by
    apply closest_projector_eq cx1y1 x1y1' hcx1y1Bound
    simpa [hcx1y1Val, hchxVal, hchyVal] using hC1'
  have hx1y2'Eq : _root_.F2R (beta:=radix) x1y2' =
      _root_.F2R (beta:=radix) cx1y2 := by
    apply closest_projector_eq cx1y2 x1y2' hcx1y2Bound
    simpa [hcx1y2Val, hchxVal, hctyVal] using hC2'
  have hx2y1'Eq : _root_.F2R (beta:=radix) x2y1' =
      _root_.F2R (beta:=radix) cx2y1 := by
    apply closest_projector_eq cx2y1 x2y1' hcx2y1Bound
    simpa [hcx2y1Val, hctxVal, hchyVal] using hC3'
  have hx2y2'Eq : _root_.F2R (beta:=radix) x2y2' =
      _root_.F2R (beta:=radix) cx2y2 := by
    apply closest_projector_eq cx2y2 x2y2' hcx2y2Bound
    simpa [hcx2y2Val, hctxVal, hctyVal] using hC4'
  have ht1'Eq : _root_.F2R (beta:=radix) t1' =
      _root_.F2R (beta:=radix) ct1 := by
    apply closest_projector_eq ct1 t1' hct1Bound
    have hinput :
        _root_.F2R (beta:=radix) r' -
            _root_.F2R (beta:=radix) x1y1' =
          _root_.F2R (beta:=radix) ct1 := by
      rw [hct1Val, hx1y1'Eq, hcx1y1Val, hchxVal, hchyVal]
    simpa [hinput] using hD2'
  have ht2'Eq : _root_.F2R (beta:=radix) t2' =
      _root_.F2R (beta:=radix) ct2 := by
    apply closest_projector_eq ct2 t2' hct2Bound
    have hinput :
        _root_.F2R (beta:=radix) t1' -
            _root_.F2R (beta:=radix) x1y2' =
          _root_.F2R (beta:=radix) ct2 := by
      calc
        _root_.F2R (beta:=radix) t1' -
            _root_.F2R (beta:=radix) x1y2' =
          _root_.F2R (beta:=radix) ct1 -
            _root_.F2R (beta:=radix) cx1y2 := by
            rw [ht1'Eq, hx1y2'Eq]
        _ = _root_.F2R (beta:=radix) ct2 := by
            rw [hct1Val, hcx1y2Val, hct2Val, hchxVal, hctyVal]
    simpa [hinput] using hD3'
  have ht3'Eq : _root_.F2R (beta:=radix) t3' =
      _root_.F2R (beta:=radix) ct3 := by
    apply closest_projector_eq ct3 t3' hct3Bound
    have hinput :
        _root_.F2R (beta:=radix) t2' -
            _root_.F2R (beta:=radix) x2y1' =
          _root_.F2R (beta:=radix) ct3 := by
      calc
        _root_.F2R (beta:=radix) t2' -
            _root_.F2R (beta:=radix) x2y1' =
          _root_.F2R (beta:=radix) ct2 -
            _root_.F2R (beta:=radix) cx2y1 := by
            rw [ht2'Eq, hx2y1'Eq]
        _ = _root_.F2R (beta:=radix) ct3 := by
            rw [hct2Val, hcx2y1Val, hct3Val, hctxVal, hchyVal]
    simpa [hinput] using hD4'
  have ht4'Eq : _root_.F2R (beta:=radix) t4' =
      _root_.F2R (beta:=radix) ct4 := by
    apply closest_projector_eq ct4 t4' hct4Bound
    have hinput :
        _root_.F2R (beta:=radix) t3' -
            _root_.F2R (beta:=radix) x2y2' =
          _root_.F2R (beta:=radix) ct4 := by
      calc
        _root_.F2R (beta:=radix) t3' -
            _root_.F2R (beta:=radix) x2y2' =
          _root_.F2R (beta:=radix) ct3 -
            _root_.F2R (beta:=radix) cx2y2 := by
            rw [ht3'Eq, hx2y2'Eq]
        _ = _root_.F2R (beta:=radix) ct4 := by
            rw [hct3Val, hcx2y2Val, hct4Val, ← hctxVal, ← hctyVal]
    simpa [hinput] using hD5'
  have hDekker :
      _root_.F2R (beta:=radix) x' * _root_.F2R (beta:=radix) y' =
        _root_.F2R (beta:=radix) r' - _root_.F2R (beta:=radix) t4' :=
    DekkerN (beta:=radix) b' radix t x' y' p q hx tx p' q' hy ty
      x1y1' x1y2' x2y1' x2y2' r' t1' t2' t3' t4' rfl hradix hb'Num
      hpGe hx'Normal hy'Normal hK' hA1x' hA2x' hA3x' hA4x' hB1y'
      hB2y' hB3y' hB4y' hC1' hC2' hC3' hC4' hrClosestX' hD2' hD3'
      hD4' hD5' hBranch
  have hC1Exact :
      Closest (beta:=radix) b (radix : ℝ)
        (_root_.F2R (beta:=radix) cx1y1) x1y1 := by
    simpa [hcx1y1Val, hchxVal, hchyVal] using hC1
  have hC2Exact :
      Closest (beta:=radix) b (radix : ℝ)
        (_root_.F2R (beta:=radix) cx1y2) x1y2 := by
    simpa [hcx1y2Val, hchxVal, hctyVal] using hC2
  have hC3Exact :
      Closest (beta:=radix) b (radix : ℝ)
        (_root_.F2R (beta:=radix) cx2y1) x2y1 := by
    simpa [hcx2y1Val, hctxVal, hchyVal] using hC3
  have hC4Exact :
      Closest (beta:=radix) b (radix : ℝ)
        (_root_.F2R (beta:=radix) cx2y2) x2y2 := by
    simpa [hcx2y2Val, hctxVal, hctyVal] using hC4
  have hRexp :
      (t : Int) - 1 + x'.Fexp + y'.Fexp ≤ r'.Fexp :=
    rExp (beta:=radix) b' radix s t x' y' r' rfl hradix hb'Num hsGe hsLe
      hx'Normal hy'Normal hK' hrClosestX'
  have hCx1y1Under :
      Underf_Err (beta:=radix) b b' radix x1y1 cx1y1
        (_root_.F2R (beta:=radix) cx1y1) (1 / 2 : ℝ) :=
    Underf_Err1 (beta:=radix) b b' radix t cx1y1 x1y1 rfl hradix
      hvNum htPrecision hb'NumEq hb'ExpLe hcx1y1Bound hC1Exact
  have hCx1y2Under :
      Underf_Err (beta:=radix) b b' radix x1y2 cx1y2
        (_root_.F2R (beta:=radix) cx1y2) (1 / 2 : ℝ) :=
    Underf_Err1 (beta:=radix) b b' radix t cx1y2 x1y2 rfl hradix
      hvNum htPrecision hb'NumEq hb'ExpLe hcx1y2Bound hC2Exact
  have hCx2y1Under :
      Underf_Err (beta:=radix) b b' radix x2y1 cx2y1
        (_root_.F2R (beta:=radix) cx2y1) (1 / 2 : ℝ) :=
    Underf_Err1 (beta:=radix) b b' radix t cx2y1 x2y1 rfl hradix
      hvNum htPrecision hb'NumEq hb'ExpLe hcx2y1Bound hC3Exact
  have hCx2y2Under :
      Underf_Err (beta:=radix) b b' radix x2y2 cx2y2
        (_root_.F2R (beta:=radix) cx2y2) (1 / 2 : ℝ) :=
    Underf_Err1 (beta:=radix) b b' radix t cx2y2 x2y2 rfl hradix
      hvNum htPrecision hb'NumEq hb'ExpLe hcx2y2Bound hC4Exact
  have ht1Under :
      Underf_Err (beta:=radix) b b' radix t1 ct1
        (_root_.F2R (beta:=radix) r - _root_.F2R (beta:=radix) x1y1)
        ((3 / 4 : ℝ) + (1 / 2 : ℝ)) := by
    refine Underf_Err3_bis (beta:=radix) b b' radix t r r' x1y1 cx1y1
      ct1 t1 (_root_.F2R (beta:=radix) x * _root_.F2R (beta:=radix) y)
      (_root_.F2R (beta:=radix) cx1y1) (3 / 4 : ℝ) (1 / 2 : ℝ)
      rfl hradix hvNum htPrecision hpGe hb'NumEq hb'ExpLe hrUnder
      hCx1y1Under (by norm_num) hct1Bound ?_ ?_ ?_ hD2
    · rw [hct1Val, hcx1y1Val, hchxVal, hchyVal]
    · rw [hct1Exp]
      exact hRexp
    · rw [hct1Exp, hcx1y1Exp]
      omega
  have ht2Under :
      Underf_Err (beta:=radix) b b' radix t2 ct2
        (_root_.F2R (beta:=radix) t1 - _root_.F2R (beta:=radix) x1y2)
        (((3 / 4 : ℝ) + (1 / 2 : ℝ)) + (1 / 2 : ℝ)) := by
    refine Underf_Err3_bis (beta:=radix) b b' radix t t1 ct1 x1y2 cx1y2
      ct2 t2 (_root_.F2R (beta:=radix) r - _root_.F2R (beta:=radix) x1y1)
      (_root_.F2R (beta:=radix) cx1y2)
      ((3 / 4 : ℝ) + (1 / 2 : ℝ)) (1 / 2 : ℝ)
      rfl hradix hvNum htPrecision hpGe hb'NumEq hb'ExpLe ht1Under
      hCx1y2Under (by norm_num) hct2Bound ?_ ?_ ?_ hD3
    · rw [hct2Val, hcx1y2Val, hct1Val, hchxVal, hctyVal]
    · rw [hct2Exp, hct1Exp]
      omega
    · rw [hct2Exp, hcx1y2Exp]
      omega
  have ht3Under :
      Underf_Err (beta:=radix) b b' radix t3 ct3
        (_root_.F2R (beta:=radix) t2 - _root_.F2R (beta:=radix) x2y1)
        ((((3 / 4 : ℝ) + (1 / 2 : ℝ)) + (1 / 2 : ℝ)) + (1 / 2 : ℝ)) := by
    refine Underf_Err3_bis (beta:=radix) b b' radix t t2 ct2 x2y1 cx2y1
      ct3 t3 (_root_.F2R (beta:=radix) t1 - _root_.F2R (beta:=radix) x1y2)
      (_root_.F2R (beta:=radix) cx2y1)
      (((3 / 4 : ℝ) + (1 / 2 : ℝ)) + (1 / 2 : ℝ)) (1 / 2 : ℝ)
      rfl hradix hvNum htPrecision hpGe hb'NumEq hb'ExpLe ht2Under
      hCx2y1Under (by norm_num) hct3Bound ?_ ?_ ?_ hD4
    · rw [hct3Val, hcx2y1Val, hct2Val, hctxVal, hchyVal]
    · rw [hct3Exp, hct2Exp]
    · rw [hct3Exp, hcx2y1Exp]
      omega
  have ht4Under :
      Underf_Err (beta:=radix) b b' radix t4 ct4
        (_root_.F2R (beta:=radix) t3 - _root_.F2R (beta:=radix) x2y2)
        (((((3 / 4 : ℝ) + (1 / 2 : ℝ)) + (1 / 2 : ℝ)) + (1 / 2 : ℝ)) +
          (1 / 2 : ℝ)) := by
    refine Underf_Err3_bis (beta:=radix) b b' radix t t3 ct3 x2y2 cx2y2
      ct4 t4 (_root_.F2R (beta:=radix) t2 - _root_.F2R (beta:=radix) x2y1)
      (_root_.F2R (beta:=radix) cx2y2)
      ((((3 / 4 : ℝ) + (1 / 2 : ℝ)) + (1 / 2 : ℝ)) + (1 / 2 : ℝ))
      (1 / 2 : ℝ) rfl hradix hvNum htPrecision hpGe hb'NumEq hb'ExpLe
      ht3Under hCx2y2Under (by norm_num) hct4Bound ?_ ?_ ?_ hD5
    · rw [hct4Val, hcx2y2Val, hct3Val, ← hctxVal, ← hctyVal]
    · rw [hct4Exp, hct3Exp]
      omega
    · rw [hct4Exp]
      exact hcx2y2Exp
  have hunitNonneg : 0 ≤ (radix : ℝ) ^ (-b.dExp) := by
    have hradixPos : (0 : ℝ) < (radix : ℝ) := by
      exact_mod_cast (show (0 : Int) < radix by omega)
    exact le_of_lt (zpow_pos hradixPos _)
  have hrDiff :
      |_root_.F2R (beta:=radix) r - _root_.F2R (beta:=radix) r'| ≤
        (3 / 4 : ℝ) * (radix : ℝ) ^ (-b.dExp) := by
    exact hrUnder.2.2.1
  have ht4Diff :
      |_root_.F2R (beta:=radix) t4 - _root_.F2R (beta:=radix) ct4| ≤
        (((((3 / 4 : ℝ) + (1 / 2 : ℝ)) + (1 / 2 : ℝ)) + (1 / 2 : ℝ)) +
          (1 / 2 : ℝ)) * (radix : ℝ) ^ (-b.dExp) := by
    exact ht4Under.2.2.1
  have ht4Diff' :
      |_root_.F2R (beta:=radix) t4 - _root_.F2R (beta:=radix) t4'| ≤
        (11 / 4 : ℝ) * (radix : ℝ) ^ (-b.dExp) := by
    rw [ht4'Eq]
    convert ht4Diff using 1 <;> norm_num
  have hmainEq :
      _root_.F2R (beta:=radix) x * _root_.F2R (beta:=radix) y =
        _root_.F2R (beta:=radix) r' - _root_.F2R (beta:=radix) t4' := by
    rw [← hx'Val, ← hy'Val]
    exact hDekker
  calc
    |_root_.F2R (beta:=radix) x * _root_.F2R (beta:=radix) y -
        (_root_.F2R (beta:=radix) r - _root_.F2R (beta:=radix) t4)|
        = |(-(_root_.F2R (beta:=radix) r - _root_.F2R (beta:=radix) r')) +
            (_root_.F2R (beta:=radix) t4 - _root_.F2R (beta:=radix) t4')| := by
          rw [hmainEq]
          ring_nf
    _ ≤ |_root_.F2R (beta:=radix) r - _root_.F2R (beta:=radix) r'| +
          |_root_.F2R (beta:=radix) t4 - _root_.F2R (beta:=radix) t4'| := by
          simpa [abs_neg, abs_sub_comm] using abs_add_le
            (-(_root_.F2R (beta:=radix) r - _root_.F2R (beta:=radix) r'))
            (_root_.F2R (beta:=radix) t4 - _root_.F2R (beta:=radix) t4')
    _ ≤ (3 / 4 : ℝ) * (radix : ℝ) ^ (-b.dExp) +
          (11 / 4 : ℝ) * (radix : ℝ) ^ (-b.dExp) :=
          add_le_add hrDiff ht4Diff'
    _ = (7 / 2 : ℝ) * (radix : ℝ) ^ (-b.dExp) := by ring

/-! Coq Algo2 theorem `Dekker2`.

This is the exact upstream section wrapper after `Dekker2_aux`: if either
canonical input has real value zero, the rounded-operation chain collapses by
`ClosestZero2`; otherwise the nonzero case is exactly `Dekker2_aux`.
Nonnegativity of `dExp` comes from the translated bound itself. -/
theorem Dekker2 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (t : Nat)
    (x y p q hx tx p' q' hy ty x1y1 x1y2 x2y1 x2y2 r t1 t2 t3 t4 :
      FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hpGe : 4 ≤ t)
    (hxCan : Fcanonic (beta:=beta) radix b x)
    (hyCan : Fcanonic (beta:=beta) radix b y)
    (hA1 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x *
        ((radix : ℝ) ^ (((t - Nat.div2 t : Nat) : Int)) + 1)) p)
    (hA2 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hA3 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) q + _root_.F2R (beta:=beta) p) hx)
    (hA4 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) hx) tx)
    (hB1 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) y *
        ((radix : ℝ) ^ (((t - Nat.div2 t : Nat) : Int)) + 1)) p')
    (hB2 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) y - _root_.F2R (beta:=beta) p') q')
    (hB3 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) q' + _root_.F2R (beta:=beta) p') hy)
    (hB4 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) y - _root_.F2R (beta:=beta) hy) ty)
    (hC1 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) hx * _root_.F2R (beta:=beta) hy) x1y1)
    (hC2 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) hx * _root_.F2R (beta:=beta) ty) x1y2)
    (hC3 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) tx * _root_.F2R (beta:=beta) hy) x2y1)
    (hC4 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) tx * _root_.F2R (beta:=beta) ty) x2y2)
    (hD1 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * _root_.F2R (beta:=beta) y) r)
    (hD2 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) x1y1) t1)
    (hD3 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) t1 - _root_.F2R (beta:=beta) x1y2) t2)
    (hD4 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) t2 - _root_.F2R (beta:=beta) x2y1) t3)
    (hD5 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) t3 - _root_.F2R (beta:=beta) x2y2) t4)
    (hBranch : radix = 2 ∨ Even t) :
    |_root_.F2R (beta:=beta) x * _root_.F2R (beta:=beta) y -
        (_root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) t4)| ≤
      (7 / 2 : ℝ) * (radix : ℝ) ^ (-(b.dExp)) := by
  subst beta
  have hdExp : 0 ≤ b.dExp := b.dExp_nonneg
  have hRhsNonneg : 0 ≤ (7 / 2 : ℝ) * (radix : ℝ) ^ (-b.dExp) := by
    have hradixPos : (0 : ℝ) < (radix : ℝ) := by
      exact_mod_cast (show (0 : Int) < radix by omega)
    exact mul_nonneg (by norm_num) (le_of_lt (zpow_pos hradixPos _))
  have hzeroClosest :
      ∀ {z : ℝ} {f : FloatSpec.Core.Defs.FlocqFloat radix},
        Closest (beta:=radix) b (radix : ℝ) z f →
          z = 0 → _root_.F2R (beta:=radix) f = 0 := by
    intro z f hClosest hz
    have h := ClosestZero2 (beta:=radix) b (radix : ℝ) z f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestZero2_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨hClosest, hz⟩
  by_cases hxZero : _root_.F2R (beta:=radix) x = 0
  · have hrZero : _root_.F2R (beta:=radix) r = 0 :=
      hzeroClosest hD1 (by rw [hxZero]; ring)
    have hpZero : _root_.F2R (beta:=radix) p = 0 :=
      hzeroClosest hA1 (by rw [hxZero]; ring)
    have hqZero : _root_.F2R (beta:=radix) q = 0 :=
      hzeroClosest hA2 (by rw [hxZero, hpZero]; ring)
    have hhxZero : _root_.F2R (beta:=radix) hx = 0 :=
      hzeroClosest hA3 (by rw [hqZero, hpZero]; ring)
    have htxZero : _root_.F2R (beta:=radix) tx = 0 :=
      hzeroClosest hA4 (by rw [hxZero, hhxZero]; ring)
    have hx1y1Zero : _root_.F2R (beta:=radix) x1y1 = 0 :=
      hzeroClosest hC1 (by rw [hhxZero]; ring)
    have hx1y2Zero : _root_.F2R (beta:=radix) x1y2 = 0 :=
      hzeroClosest hC2 (by rw [hhxZero]; ring)
    have hx2y1Zero : _root_.F2R (beta:=radix) x2y1 = 0 :=
      hzeroClosest hC3 (by rw [htxZero]; ring)
    have hx2y2Zero : _root_.F2R (beta:=radix) x2y2 = 0 :=
      hzeroClosest hC4 (by rw [htxZero]; ring)
    have ht1Zero : _root_.F2R (beta:=radix) t1 = 0 :=
      hzeroClosest hD2 (by rw [hrZero, hx1y1Zero]; ring)
    have ht2Zero : _root_.F2R (beta:=radix) t2 = 0 :=
      hzeroClosest hD3 (by rw [ht1Zero, hx1y2Zero]; ring)
    have ht3Zero : _root_.F2R (beta:=radix) t3 = 0 :=
      hzeroClosest hD4 (by rw [ht2Zero, hx2y1Zero]; ring)
    have ht4Zero : _root_.F2R (beta:=radix) t4 = 0 :=
      hzeroClosest hD5 (by rw [ht3Zero, hx2y2Zero]; ring)
    calc
      |_root_.F2R (beta:=radix) x * _root_.F2R (beta:=radix) y -
          (_root_.F2R (beta:=radix) r - _root_.F2R (beta:=radix) t4)| = 0 := by
          rw [hxZero, hrZero, ht4Zero]
          simp
      _ ≤ (7 / 2 : ℝ) * (radix : ℝ) ^ (-b.dExp) := hRhsNonneg
  · by_cases hyZero : _root_.F2R (beta:=radix) y = 0
    · have hrZero : _root_.F2R (beta:=radix) r = 0 :=
        hzeroClosest hD1 (by rw [hyZero]; ring)
      have hp'Zero : _root_.F2R (beta:=radix) p' = 0 :=
        hzeroClosest hB1 (by rw [hyZero]; ring)
      have hq'Zero : _root_.F2R (beta:=radix) q' = 0 :=
        hzeroClosest hB2 (by rw [hyZero, hp'Zero]; ring)
      have hhyZero : _root_.F2R (beta:=radix) hy = 0 :=
        hzeroClosest hB3 (by rw [hq'Zero, hp'Zero]; ring)
      have htyZero : _root_.F2R (beta:=radix) ty = 0 :=
        hzeroClosest hB4 (by rw [hyZero, hhyZero]; ring)
      have hx1y1Zero : _root_.F2R (beta:=radix) x1y1 = 0 :=
        hzeroClosest hC1 (by rw [hhyZero]; ring)
      have hx1y2Zero : _root_.F2R (beta:=radix) x1y2 = 0 :=
        hzeroClosest hC2 (by rw [htyZero]; ring)
      have hx2y1Zero : _root_.F2R (beta:=radix) x2y1 = 0 :=
        hzeroClosest hC3 (by rw [hhyZero]; ring)
      have hx2y2Zero : _root_.F2R (beta:=radix) x2y2 = 0 :=
        hzeroClosest hC4 (by rw [htyZero]; ring)
      have ht1Zero : _root_.F2R (beta:=radix) t1 = 0 :=
        hzeroClosest hD2 (by rw [hrZero, hx1y1Zero]; ring)
      have ht2Zero : _root_.F2R (beta:=radix) t2 = 0 :=
        hzeroClosest hD3 (by rw [ht1Zero, hx1y2Zero]; ring)
      have ht3Zero : _root_.F2R (beta:=radix) t3 = 0 :=
        hzeroClosest hD4 (by rw [ht2Zero, hx2y1Zero]; ring)
      have ht4Zero : _root_.F2R (beta:=radix) t4 = 0 :=
        hzeroClosest hD5 (by rw [ht3Zero, hx2y2Zero]; ring)
      calc
        |_root_.F2R (beta:=radix) x * _root_.F2R (beta:=radix) y -
            (_root_.F2R (beta:=radix) r - _root_.F2R (beta:=radix) t4)| = 0 := by
            rw [hyZero, hrZero, ht4Zero]
            simp
        _ ≤ (7 / 2 : ℝ) * (radix : ℝ) ^ (-b.dExp) := hRhsNonneg
    · exact Dekker2_aux (beta:=radix) b radix t x y p q hx tx p' q' hy ty
        x1y1 x1y2 x2y1 x2y2 r t1 t2 t3 t4 rfl hradix hvNum hpGe
        hxCan hyCan hxZero hyZero hA1 hA2 hA3 hA4 hB1 hB2 hB3 hB4
        hC1 hC2 hC3 hC4 hD1 hD2 hD3 hD4 hD5 hBranch

/-! Coq AlgoS1 theorem `DekkerS1`.

This is the subnormal-`y` counterpart of `DekkerN`.  The zero branch follows
the upstream `ClosestZero` cascade.  In the nonzero branch, `y` is normalized
under `plusExp b t`, all closest-operation witnesses are lifted to that larger
bound with local exact operation witnesses, and the result delegates to
`DekkerN`. -/
theorem DekkerS1 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (t : Nat)
    (x y p q hx tx p' q' hy ty x1y1 x1y2 x2y1 x2y2 r t1 t2 t3 t4 :
      FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hpGe : 4 ≤ t)
    (hxNormal : Fnormal (beta:=beta) radix b x)
    (hySubnormal : Fsubnormal (beta:=beta) radix b y)
    (hK : -b.dExp ≤ x.Fexp + y.Fexp)
    (hA1 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x *
        ((radix : ℝ) ^ (((t - Nat.div2 t : Nat) : Int)) + 1)) p)
    (hA2 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hA3 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) q + _root_.F2R (beta:=beta) p) hx)
    (hA4 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) hx) tx)
    (hB1 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) y *
        ((radix : ℝ) ^ (((t - Nat.div2 t : Nat) : Int)) + 1)) p')
    (hB2 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) y - _root_.F2R (beta:=beta) p') q')
    (hB3 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) q' + _root_.F2R (beta:=beta) p') hy)
    (hB4 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) y - _root_.F2R (beta:=beta) hy) ty)
    (hC1 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) hx * _root_.F2R (beta:=beta) hy) x1y1)
    (hC2 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) hx * _root_.F2R (beta:=beta) ty) x1y2)
    (hC3 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) tx * _root_.F2R (beta:=beta) hy) x2y1)
    (hC4 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) tx * _root_.F2R (beta:=beta) ty) x2y2)
    (hD1 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * _root_.F2R (beta:=beta) y) r)
    (hD2 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) x1y1) t1)
    (hD3 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) t1 - _root_.F2R (beta:=beta) x1y2) t2)
    (hD4 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) t2 - _root_.F2R (beta:=beta) x2y1) t3)
    (hD5 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) t3 - _root_.F2R (beta:=beta) x2y2) t4)
    (hBranch : radix = 2 ∨ Even t) :
    _root_.F2R (beta:=beta) x * _root_.F2R (beta:=beta) y =
      _root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) t4 := by
  subst beta
  let s : Nat := t - Nat.div2 t
  let bPlus : Fbound_skel := plusExp b t
  have hsGe : 2 ≤ s := by
    have h := SLe t hpGe
    dsimp [s]
    omega
  have hsLe : s ≤ t - 2 := by
    have h := SGe t hpGe
    dsimp [s]
    omega
  have htGtOne : 1 < t := by omega
  have htoPlus :
      ∀ {z : ℝ} {f : FloatSpec.Core.Defs.FlocqFloat radix},
        Closest (beta:=radix) b (radix : ℝ) z f →
          z = 0 → _root_.F2R (beta:=radix) f = 0 := by
    intro z f hClosest hz
    have h := ClosestZero2 (beta:=radix) b (radix : ℝ) z f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestZero2_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨hClosest, hz⟩
  by_cases hyZero : _root_.F2R (beta:=radix) y = 0
  · have hrZero : _root_.F2R (beta:=radix) r = 0 :=
      htoPlus hD1 (by rw [hyZero]; ring)
    have hp'Zero : _root_.F2R (beta:=radix) p' = 0 :=
      htoPlus hB1 (by rw [hyZero]; ring)
    have hq'Zero : _root_.F2R (beta:=radix) q' = 0 :=
      htoPlus hB2 (by rw [hyZero, hp'Zero]; ring)
    have hhyZero : _root_.F2R (beta:=radix) hy = 0 :=
      htoPlus hB3 (by rw [hq'Zero, hp'Zero]; ring)
    have htyZero : _root_.F2R (beta:=radix) ty = 0 :=
      htoPlus hB4 (by rw [hyZero, hhyZero]; ring)
    have hx1y1Zero : _root_.F2R (beta:=radix) x1y1 = 0 :=
      htoPlus hC1 (by rw [hhyZero]; ring)
    have hx1y2Zero : _root_.F2R (beta:=radix) x1y2 = 0 :=
      htoPlus hC2 (by rw [htyZero]; ring)
    have hx2y1Zero : _root_.F2R (beta:=radix) x2y1 = 0 :=
      htoPlus hC3 (by rw [hhyZero]; ring)
    have hx2y2Zero : _root_.F2R (beta:=radix) x2y2 = 0 :=
      htoPlus hC4 (by rw [htyZero]; ring)
    have ht1Zero : _root_.F2R (beta:=radix) t1 = 0 :=
      htoPlus hD2 (by rw [hrZero, hx1y1Zero]; ring)
    have ht2Zero : _root_.F2R (beta:=radix) t2 = 0 :=
      htoPlus hD3 (by rw [ht1Zero, hx1y2Zero]; ring)
    have ht3Zero : _root_.F2R (beta:=radix) t3 = 0 :=
      htoPlus hD4 (by rw [ht2Zero, hx2y1Zero]; ring)
    have ht4Zero : _root_.F2R (beta:=radix) t4 = 0 :=
      htoPlus hD5 (by rw [ht3Zero, hx2y2Zero]; ring)
    rw [hyZero, hrZero, ht4Zero]
    ring
  · rcases bimplybplusNorm (beta:=radix) b radix s t y rfl hradix
        hvNum hsGe hsLe hySubnormal.1 hyZero with
      ⟨yy, hyyValue, hyyNormal⟩
    have hvNumPlus : bPlus.vNum = Zpower_nat radix t := by
      simpa [bPlus, plusExp] using hvNum
    have hxNormalPlus : Fnormal (beta:=radix) radix bPlus x := by
      constructor
      · rcases hxNormal.1 with ⟨hnum, hexp⟩
        constructor
        · simpa [bPlus, plusExp] using hnum
        · dsimp [bPlus, plusExp]
          omega
      · simpa [bPlus, plusExp] using hxNormal.2
    have hxExpNonneg : 0 ≤ x.Fexp := by
      have hyExp : y.Fexp = -b.dExp := hySubnormal.2.1
      rw [hyExp] at hK
      omega
    have hKPlus : -bPlus.dExp ≤ x.Fexp + yy.Fexp := by
      have hyyExp : -bPlus.dExp ≤ yy.Fexp := hyyNormal.1.2
      omega
    have liftClosest :
        ∀ (fext f : FloatSpec.Core.Defs.FlocqFloat radix) (z : ℝ),
          _root_.F2R (beta:=radix) fext = z →
          -b.dExp ≤ fext.Fexp →
          Closest (beta:=radix) b (radix : ℝ) z f →
          Closest (beta:=radix) bPlus (radix : ℝ) z f := by
      intro fext f z hfextValue hfextExp hClosest
      have hClosest' :
          Closest (beta:=radix) b (radix : ℝ)
            (_root_.F2R (beta:=radix) fext) f := by
        simpa [hfextValue] using hClosest
      have h := Closestbbplus b radix t t fext f rfl hradix hvNum
        htGtOne hfextExp hClosest'
      simpa [bPlus, hfextValue] using h
    rcases VeltkampU (beta:=radix) b radix s t x p q hx tx rfl hradix
        hvNum hsGe hsLe (Or.inl hxNormal) (by simpa [s] using hA1)
        hA2 hA3 hA4 with
      ⟨_hxResidual, _hXeq, hxHead, hxTail⟩
    rcases hxHead with ⟨hx', hhxVal, _hhxBound, hhxExpFn⟩
    rcases hxTail with ⟨tx', htxVal, _htxBound, htxExp⟩
    have hhxExp : (s : Int) + x.Fexp ≤ hx'.Fexp := hhxExpFn hxNormal
    rcases VeltkampU (beta:=radix) b radix s t y p' q' hy ty rfl hradix
        hvNum hsGe hsLe (Or.inr hySubnormal) (by simpa [s] using hB1)
        hB2 hB3 hB4 with
      ⟨_hyResidual, _hYeq, _hyHead, hyTail⟩
    rcases hyTail with ⟨ty', htyVal, _htyBound, htyExp⟩
    have hxPrimeNonneg : 0 ≤ hx'.Fexp := by omega
    have txPrimeNonneg : 0 ≤ tx'.Fexp := by omega
    let xs : FloatSpec.Core.Defs.FlocqFloat radix := ⟨x.Fnum, x.Fexp + (s : Int)⟩
    have hxsValue :
        _root_.F2R (beta:=radix) xs =
          _root_.F2R (beta:=radix) x * (radix : ℝ) ^ (s : Int) := by
      simp only [xs, _root_.F2R, FloatSpec.Core.Defs.F2R]
      rw [zpow_add₀ (by exact_mod_cast (show radix ≠ 0 by omega))]
      ring
    let xScaled : FloatSpec.Core.Defs.FlocqFloat radix := Fplus x xs
    have hxScaledValue :
        _root_.F2R (beta:=radix) xScaled =
          _root_.F2R (beta:=radix) x *
            ((radix : ℝ) ^ (s : Int) + 1) := by
      have h := Fplus_correct (beta:=radix) x xs
      have h' : _root_.F2R (beta:=radix) xScaled =
          _root_.F2R (beta:=radix) x + _root_.F2R (beta:=radix) xs := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fplus_correct_check,
          Id.run, ULift.up_down, xScaled, Int.cast_ofNat] using h hradix
      rw [h', hxsValue]
      ring
    have hxScaledExp : -b.dExp ≤ xScaled.Fexp := by
      have hmin := Fplus_exp_eq_min x xs
      have hxLeXs : x.Fexp ≤ xs.Fexp := by simp [xs]
      rw [show xScaled.Fexp = min x.Fexp xs.Fexp by simpa [xScaled] using hmin,
        min_eq_left hxLeXs]
      exact hxNormal.1.2
    let ys : FloatSpec.Core.Defs.FlocqFloat radix := ⟨y.Fnum, y.Fexp + (s : Int)⟩
    have hysValue :
        _root_.F2R (beta:=radix) ys =
          _root_.F2R (beta:=radix) y * (radix : ℝ) ^ (s : Int) := by
      simp only [ys, _root_.F2R, FloatSpec.Core.Defs.F2R]
      rw [zpow_add₀ (by exact_mod_cast (show radix ≠ 0 by omega))]
      ring
    let yScaled : FloatSpec.Core.Defs.FlocqFloat radix := Fplus y ys
    have hyScaledValue :
        _root_.F2R (beta:=radix) yScaled =
          _root_.F2R (beta:=radix) y *
            ((radix : ℝ) ^ (s : Int) + 1) := by
      have h := Fplus_correct (beta:=radix) y ys
      have h' : _root_.F2R (beta:=radix) yScaled =
          _root_.F2R (beta:=radix) y + _root_.F2R (beta:=radix) ys := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fplus_correct_check,
          Id.run, ULift.up_down, yScaled, Int.cast_ofNat] using h hradix
      rw [h', hysValue]
      ring
    have hyScaledExp : -b.dExp ≤ yScaled.Fexp := by
      have hmin := Fplus_exp_eq_min y ys
      have hyLeYs : y.Fexp ≤ ys.Fexp := by simp [ys]
      rw [show yScaled.Fexp = min y.Fexp ys.Fexp by simpa [yScaled] using hmin,
        min_eq_left hyLeYs]
      exact hySubnormal.1.2
    let xMinusP : FloatSpec.Core.Defs.FlocqFloat radix := Fminus x p
    have hxMinusPValue :
        _root_.F2R (beta:=radix) xMinusP =
          _root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p := by
      have h := Fminus_correct (beta:=radix) x p
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus,
        Id.run, ULift.up_down, xMinusP, Int.cast_ofNat] using h hradix
    have hxMinusPExp : -b.dExp ≤ xMinusP.Fexp := by
      rw [show xMinusP.Fexp = min x.Fexp p.Fexp by
        simpa [xMinusP] using Fminus_exp_eq_min (beta:=radix) x p]
      exact le_min hxNormal.1.2 hA1.1.2
    let qPlusP : FloatSpec.Core.Defs.FlocqFloat radix := Fplus q p
    have hqPlusPValue :
        _root_.F2R (beta:=radix) qPlusP =
          _root_.F2R (beta:=radix) q + _root_.F2R (beta:=radix) p := by
      have h := Fplus_correct (beta:=radix) q p
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fplus_correct_check,
        Id.run, ULift.up_down, qPlusP, Int.cast_ofNat] using h hradix
    have hqPlusPExp : -b.dExp ≤ qPlusP.Fexp := by
      rw [show qPlusP.Fexp = min q.Fexp p.Fexp by
        simpa [qPlusP] using Fplus_exp_eq_min (beta:=radix) q p]
      exact le_min hA2.1.2 hA1.1.2
    let xMinusHx : FloatSpec.Core.Defs.FlocqFloat radix := Fminus x hx
    have hxMinusHxValue :
        _root_.F2R (beta:=radix) xMinusHx =
          _root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) hx := by
      have h := Fminus_correct (beta:=radix) x hx
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus,
        Id.run, ULift.up_down, xMinusHx, Int.cast_ofNat] using h hradix
    have hxMinusHxExp : -b.dExp ≤ xMinusHx.Fexp := by
      rw [show xMinusHx.Fexp = min x.Fexp hx.Fexp by
        simpa [xMinusHx] using Fminus_exp_eq_min (beta:=radix) x hx]
      exact le_min hxNormal.1.2 hA3.1.2
    let yMinusP : FloatSpec.Core.Defs.FlocqFloat radix := Fminus y p'
    have hyMinusPValue :
        _root_.F2R (beta:=radix) yMinusP =
          _root_.F2R (beta:=radix) y - _root_.F2R (beta:=radix) p' := by
      have h := Fminus_correct (beta:=radix) y p'
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus,
        Id.run, ULift.up_down, yMinusP, Int.cast_ofNat] using h hradix
    have hyMinusPExp : -b.dExp ≤ yMinusP.Fexp := by
      rw [show yMinusP.Fexp = min y.Fexp p'.Fexp by
        simpa [yMinusP] using Fminus_exp_eq_min (beta:=radix) y p']
      exact le_min hySubnormal.1.2 hB1.1.2
    let qPlusP' : FloatSpec.Core.Defs.FlocqFloat radix := Fplus q' p'
    have hqPlusP'Value :
        _root_.F2R (beta:=radix) qPlusP' =
          _root_.F2R (beta:=radix) q' + _root_.F2R (beta:=radix) p' := by
      have h := Fplus_correct (beta:=radix) q' p'
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fplus_correct_check,
        Id.run, ULift.up_down, qPlusP', Int.cast_ofNat] using h hradix
    have hqPlusP'Exp : -b.dExp ≤ qPlusP'.Fexp := by
      rw [show qPlusP'.Fexp = min q'.Fexp p'.Fexp by
        simpa [qPlusP'] using Fplus_exp_eq_min (beta:=radix) q' p']
      exact le_min hB2.1.2 hB1.1.2
    let yMinusHy : FloatSpec.Core.Defs.FlocqFloat radix := Fminus y hy
    have hyMinusHyValue :
        _root_.F2R (beta:=radix) yMinusHy =
          _root_.F2R (beta:=radix) y - _root_.F2R (beta:=radix) hy := by
      have h := Fminus_correct (beta:=radix) y hy
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus,
        Id.run, ULift.up_down, yMinusHy, Int.cast_ofNat] using h hradix
    have hyMinusHyExp : -b.dExp ≤ yMinusHy.Fexp := by
      rw [show yMinusHy.Fexp = min y.Fexp hy.Fexp by
        simpa [yMinusHy] using Fminus_exp_eq_min (beta:=radix) y hy]
      exact le_min hySubnormal.1.2 hB3.1.2
    have hA1Plus :
        Closest (beta:=radix) bPlus (radix : ℝ)
          (_root_.F2R (beta:=radix) x *
            ((radix : ℝ) ^ (((t - Nat.div2 t : Nat) : Int)) + 1)) p := by
      have h := liftClosest xScaled p
        (_root_.F2R (beta:=radix) x *
          ((radix : ℝ) ^ (s : Int) + 1))
        hxScaledValue hxScaledExp (by simpa [s] using hA1)
      simpa [s] using h
    have hA2Plus :
        Closest (beta:=radix) bPlus (radix : ℝ)
          (_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p) q :=
      liftClosest xMinusP q
        (_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p)
        hxMinusPValue hxMinusPExp hA2
    have hA3Plus :
        Closest (beta:=radix) bPlus (radix : ℝ)
          (_root_.F2R (beta:=radix) q + _root_.F2R (beta:=radix) p) hx :=
      liftClosest qPlusP hx
        (_root_.F2R (beta:=radix) q + _root_.F2R (beta:=radix) p)
        hqPlusPValue hqPlusPExp hA3
    have hA4Plus :
        Closest (beta:=radix) bPlus (radix : ℝ)
          (_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) hx) tx :=
      liftClosest xMinusHx tx
        (_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) hx)
        hxMinusHxValue hxMinusHxExp hA4
    have hB1Plus :
        Closest (beta:=radix) bPlus (radix : ℝ)
          (_root_.F2R (beta:=radix) yy *
            ((radix : ℝ) ^ (((t - Nat.div2 t : Nat) : Int)) + 1)) p' := by
      have h := liftClosest yScaled p'
        (_root_.F2R (beta:=radix) y *
          ((radix : ℝ) ^ (s : Int) + 1))
        hyScaledValue hyScaledExp (by simpa [s] using hB1)
      simpa [s, hyyValue] using h
    have hB2Plus :
        Closest (beta:=radix) bPlus (radix : ℝ)
          (_root_.F2R (beta:=radix) yy - _root_.F2R (beta:=radix) p') q' := by
      have h := liftClosest yMinusP q'
        (_root_.F2R (beta:=radix) y - _root_.F2R (beta:=radix) p')
        hyMinusPValue hyMinusPExp hB2
      simpa [hyyValue] using h
    have hB3Plus :
        Closest (beta:=radix) bPlus (radix : ℝ)
          (_root_.F2R (beta:=radix) q' + _root_.F2R (beta:=radix) p') hy :=
      liftClosest qPlusP' hy
        (_root_.F2R (beta:=radix) q' + _root_.F2R (beta:=radix) p')
        hqPlusP'Value hqPlusP'Exp hB3
    have hB4Plus :
        Closest (beta:=radix) bPlus (radix : ℝ)
          (_root_.F2R (beta:=radix) yy - _root_.F2R (beta:=radix) hy) ty := by
      have h := liftClosest yMinusHy ty
        (_root_.F2R (beta:=radix) y - _root_.F2R (beta:=radix) hy)
        hyMinusHyValue hyMinusHyExp hB4
      simpa [hyyValue] using h
    let hxhy : FloatSpec.Core.Defs.FlocqFloat radix :=
      FloatSpec.Calc.Operations.Fmult (beta:=radix) hx' hy
    have hxhyValue :
        _root_.F2R (beta:=radix) hxhy =
          _root_.F2R (beta:=radix) hx * _root_.F2R (beta:=radix) hy := by
      have h := Fmult_correct (beta:=radix) hx' hy
      have hv :
          _root_.F2R (beta:=radix) hxhy =
            _root_.F2R (beta:=radix) hx' * _root_.F2R (beta:=radix) hy := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fmult_correct_check,
          Id.run, ULift.up_down, hxhy, Int.cast_ofNat] using h hradix
      rw [hv, hhxVal]
    have hxhyExp : -b.dExp ≤ hxhy.Fexp := by
      dsimp [hxhy, FloatSpec.Calc.Operations.Fmult]
      have hyBound : -b.dExp ≤ hy.Fexp := hB3.1.2
      omega
    let hxty : FloatSpec.Core.Defs.FlocqFloat radix :=
      FloatSpec.Calc.Operations.Fmult (beta:=radix) hx' ty'
    have hxtyValue :
        _root_.F2R (beta:=radix) hxty =
          _root_.F2R (beta:=radix) hx * _root_.F2R (beta:=radix) ty := by
      have h := Fmult_correct (beta:=radix) hx' ty'
      have hv :
          _root_.F2R (beta:=radix) hxty =
            _root_.F2R (beta:=radix) hx' * _root_.F2R (beta:=radix) ty' := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fmult_correct_check,
          Id.run, ULift.up_down, hxty, Int.cast_ofNat] using h hradix
      rw [hv, hhxVal, htyVal]
    have hxtyExp : -b.dExp ≤ hxty.Fexp := by
      dsimp [hxty, FloatSpec.Calc.Operations.Fmult]
      omega
    let txhy : FloatSpec.Core.Defs.FlocqFloat radix :=
      FloatSpec.Calc.Operations.Fmult (beta:=radix) tx' hy
    have txhyValue :
        _root_.F2R (beta:=radix) txhy =
          _root_.F2R (beta:=radix) tx * _root_.F2R (beta:=radix) hy := by
      have h := Fmult_correct (beta:=radix) tx' hy
      have hv :
          _root_.F2R (beta:=radix) txhy =
            _root_.F2R (beta:=radix) tx' * _root_.F2R (beta:=radix) hy := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fmult_correct_check,
          Id.run, ULift.up_down, txhy, Int.cast_ofNat] using h hradix
      rw [hv, htxVal]
    have txhyExp : -b.dExp ≤ txhy.Fexp := by
      dsimp [txhy, FloatSpec.Calc.Operations.Fmult]
      have hyBound : -b.dExp ≤ hy.Fexp := hB3.1.2
      omega
    let txty : FloatSpec.Core.Defs.FlocqFloat radix :=
      FloatSpec.Calc.Operations.Fmult (beta:=radix) tx' ty'
    have txtyValue :
        _root_.F2R (beta:=radix) txty =
          _root_.F2R (beta:=radix) tx * _root_.F2R (beta:=radix) ty := by
      have h := Fmult_correct (beta:=radix) tx' ty'
      have hv :
          _root_.F2R (beta:=radix) txty =
            _root_.F2R (beta:=radix) tx' * _root_.F2R (beta:=radix) ty' := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fmult_correct_check,
          Id.run, ULift.up_down, txty, Int.cast_ofNat] using h hradix
      rw [hv, htxVal, htyVal]
    have txtyExp : -b.dExp ≤ txty.Fexp := by
      dsimp [txty, FloatSpec.Calc.Operations.Fmult]
      have hyExp : y.Fexp = -b.dExp := hySubnormal.2.1
      omega
    let xy : FloatSpec.Core.Defs.FlocqFloat radix :=
      FloatSpec.Calc.Operations.Fmult (beta:=radix) x y
    have hxyValue :
        _root_.F2R (beta:=radix) xy =
          _root_.F2R (beta:=radix) x * _root_.F2R (beta:=radix) y := by
      have h := Fmult_correct (beta:=radix) x y
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fmult_correct_check,
        Id.run, ULift.up_down, xy, Int.cast_ofNat] using h hradix
    have hxyExp : -b.dExp ≤ xy.Fexp := by
      dsimp [xy, FloatSpec.Calc.Operations.Fmult]
      exact hK
    let rMinus11 : FloatSpec.Core.Defs.FlocqFloat radix := Fminus r x1y1
    have hrMinus11Value :
        _root_.F2R (beta:=radix) rMinus11 =
          _root_.F2R (beta:=radix) r - _root_.F2R (beta:=radix) x1y1 := by
      have h := Fminus_correct (beta:=radix) r x1y1
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus,
        Id.run, ULift.up_down, rMinus11, Int.cast_ofNat] using h hradix
    have hrMinus11Exp : -b.dExp ≤ rMinus11.Fexp := by
      rw [show rMinus11.Fexp = min r.Fexp x1y1.Fexp by
        simpa [rMinus11] using Fminus_exp_eq_min (beta:=radix) r x1y1]
      exact le_min hD1.1.2 hC1.1.2
    let t1Minus12 : FloatSpec.Core.Defs.FlocqFloat radix := Fminus t1 x1y2
    have ht1Minus12Value :
        _root_.F2R (beta:=radix) t1Minus12 =
          _root_.F2R (beta:=radix) t1 - _root_.F2R (beta:=radix) x1y2 := by
      have h := Fminus_correct (beta:=radix) t1 x1y2
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus,
        Id.run, ULift.up_down, t1Minus12, Int.cast_ofNat] using h hradix
    have ht1Minus12Exp : -b.dExp ≤ t1Minus12.Fexp := by
      rw [show t1Minus12.Fexp = min t1.Fexp x1y2.Fexp by
        simpa [t1Minus12] using Fminus_exp_eq_min (beta:=radix) t1 x1y2]
      exact le_min hD2.1.2 hC2.1.2
    let t2Minus21 : FloatSpec.Core.Defs.FlocqFloat radix := Fminus t2 x2y1
    have ht2Minus21Value :
        _root_.F2R (beta:=radix) t2Minus21 =
          _root_.F2R (beta:=radix) t2 - _root_.F2R (beta:=radix) x2y1 := by
      have h := Fminus_correct (beta:=radix) t2 x2y1
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus,
        Id.run, ULift.up_down, t2Minus21, Int.cast_ofNat] using h hradix
    have ht2Minus21Exp : -b.dExp ≤ t2Minus21.Fexp := by
      rw [show t2Minus21.Fexp = min t2.Fexp x2y1.Fexp by
        simpa [t2Minus21] using Fminus_exp_eq_min (beta:=radix) t2 x2y1]
      exact le_min hD3.1.2 hC3.1.2
    let t3Minus22 : FloatSpec.Core.Defs.FlocqFloat radix := Fminus t3 x2y2
    have ht3Minus22Value :
        _root_.F2R (beta:=radix) t3Minus22 =
          _root_.F2R (beta:=radix) t3 - _root_.F2R (beta:=radix) x2y2 := by
      have h := Fminus_correct (beta:=radix) t3 x2y2
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus,
        Id.run, ULift.up_down, t3Minus22, Int.cast_ofNat] using h hradix
    have ht3Minus22Exp : -b.dExp ≤ t3Minus22.Fexp := by
      rw [show t3Minus22.Fexp = min t3.Fexp x2y2.Fexp by
        simpa [t3Minus22] using Fminus_exp_eq_min (beta:=radix) t3 x2y2]
      exact le_min hD4.1.2 hC4.1.2
    have hC1Plus :
        Closest (beta:=radix) bPlus (radix : ℝ)
          (_root_.F2R (beta:=radix) hx * _root_.F2R (beta:=radix) hy) x1y1 :=
      liftClosest hxhy x1y1
        (_root_.F2R (beta:=radix) hx * _root_.F2R (beta:=radix) hy)
        hxhyValue hxhyExp hC1
    have hC2Plus :
        Closest (beta:=radix) bPlus (radix : ℝ)
          (_root_.F2R (beta:=radix) hx * _root_.F2R (beta:=radix) ty) x1y2 :=
      liftClosest hxty x1y2
        (_root_.F2R (beta:=radix) hx * _root_.F2R (beta:=radix) ty)
        hxtyValue hxtyExp hC2
    have hC3Plus :
        Closest (beta:=radix) bPlus (radix : ℝ)
          (_root_.F2R (beta:=radix) tx * _root_.F2R (beta:=radix) hy) x2y1 :=
      liftClosest txhy x2y1
        (_root_.F2R (beta:=radix) tx * _root_.F2R (beta:=radix) hy)
        txhyValue txhyExp hC3
    have hC4Plus :
        Closest (beta:=radix) bPlus (radix : ℝ)
          (_root_.F2R (beta:=radix) tx * _root_.F2R (beta:=radix) ty) x2y2 :=
      liftClosest txty x2y2
        (_root_.F2R (beta:=radix) tx * _root_.F2R (beta:=radix) ty)
        txtyValue txtyExp hC4
    have hD1Plus :
        Closest (beta:=radix) bPlus (radix : ℝ)
          (_root_.F2R (beta:=radix) x * _root_.F2R (beta:=radix) yy) r := by
      have h := liftClosest xy r
        (_root_.F2R (beta:=radix) x * _root_.F2R (beta:=radix) y)
        hxyValue hxyExp hD1
      simpa [hyyValue] using h
    have hD2Plus :
        Closest (beta:=radix) bPlus (radix : ℝ)
          (_root_.F2R (beta:=radix) r - _root_.F2R (beta:=radix) x1y1) t1 :=
      liftClosest rMinus11 t1
        (_root_.F2R (beta:=radix) r - _root_.F2R (beta:=radix) x1y1)
        hrMinus11Value hrMinus11Exp hD2
    have hD3Plus :
        Closest (beta:=radix) bPlus (radix : ℝ)
          (_root_.F2R (beta:=radix) t1 - _root_.F2R (beta:=radix) x1y2) t2 :=
      liftClosest t1Minus12 t2
        (_root_.F2R (beta:=radix) t1 - _root_.F2R (beta:=radix) x1y2)
        ht1Minus12Value ht1Minus12Exp hD3
    have hD4Plus :
        Closest (beta:=radix) bPlus (radix : ℝ)
          (_root_.F2R (beta:=radix) t2 - _root_.F2R (beta:=radix) x2y1) t3 :=
      liftClosest t2Minus21 t3
        (_root_.F2R (beta:=radix) t2 - _root_.F2R (beta:=radix) x2y1)
        ht2Minus21Value ht2Minus21Exp hD4
    have hD5Plus :
        Closest (beta:=radix) bPlus (radix : ℝ)
          (_root_.F2R (beta:=radix) t3 - _root_.F2R (beta:=radix) x2y2) t4 :=
      liftClosest t3Minus22 t4
        (_root_.F2R (beta:=radix) t3 - _root_.F2R (beta:=radix) x2y2)
        ht3Minus22Value ht3Minus22Exp hD5
    have hDekker := DekkerN (beta:=radix) bPlus radix t x yy p q hx tx
      p' q' hy ty x1y1 x1y2 x2y1 x2y2 r t1 t2 t3 t4 rfl hradix
      hvNumPlus hpGe hxNormalPlus hyyNormal hKPlus hA1Plus hA2Plus
      hA3Plus hA4Plus hB1Plus hB2Plus hB3Plus hB4Plus hC1Plus hC2Plus
      hC3Plus hC4Plus hD1Plus hD2Plus hD3Plus hD4Plus hD5Plus hBranch
    simpa [hyyValue] using hDekker

/-! Coq AlgoS2 theorem `DekkerS2`.

This is the subnormal-`x` counterpart of `DekkerN`.  The zero branch follows
the upstream zero-projection cascade.  In the nonzero branch, `x` is normalized
under `plusExp b t`, the A-side closestness hypotheses are lifted through that
normal representative, the normal `y` Veltkamp witnesses are reused for the
B-side products, and the D3/D4 subtraction order is preserved exactly before
delegating to `DekkerN`. -/
theorem DekkerS2 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (t : Nat)
    (x y p q hx tx p' q' hy ty x1y1 x1y2 x2y1 x2y2 r t1 t2 t3 t4 :
      FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hpGe : 4 ≤ t)
    (hxSubnormal : Fsubnormal (beta:=beta) radix b x)
    (hyNormal : Fnormal (beta:=beta) radix b y)
    (hK : -b.dExp ≤ x.Fexp + y.Fexp)
    (hA1 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x *
        ((radix : ℝ) ^ (((t - Nat.div2 t : Nat) : Int)) + 1)) p)
    (hA2 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hA3 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) q + _root_.F2R (beta:=beta) p) hx)
    (hA4 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) hx) tx)
    (hB1 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) y *
        ((radix : ℝ) ^ (((t - Nat.div2 t : Nat) : Int)) + 1)) p')
    (hB2 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) y - _root_.F2R (beta:=beta) p') q')
    (hB3 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) q' + _root_.F2R (beta:=beta) p') hy)
    (hB4 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) y - _root_.F2R (beta:=beta) hy) ty)
    (hC1 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) hx * _root_.F2R (beta:=beta) hy) x1y1)
    (hC2 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) hx * _root_.F2R (beta:=beta) ty) x1y2)
    (hC3 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) tx * _root_.F2R (beta:=beta) hy) x2y1)
    (hC4 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) tx * _root_.F2R (beta:=beta) ty) x2y2)
    (hD1 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * _root_.F2R (beta:=beta) y) r)
    (hD2 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) x1y1) t1)
    (hD3 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) t1 - _root_.F2R (beta:=beta) x1y2) t2)
    (hD4 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) t2 - _root_.F2R (beta:=beta) x2y1) t3)
    (hD5 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) t3 - _root_.F2R (beta:=beta) x2y2) t4)
    (hBranch : radix = 2 ∨ Even t) :
    _root_.F2R (beta:=beta) x * _root_.F2R (beta:=beta) y =
      _root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) t4 := by
  subst beta
  let s : Nat := t - Nat.div2 t
  let bPlus : Fbound_skel := plusExp b t
  have hsGe : 2 ≤ s := by
    have h := SLe t hpGe
    dsimp [s]
    omega
  have hsLe : s ≤ t - 2 := by
    have h := SGe t hpGe
    dsimp [s]
    omega
  have htGtOne : 1 < t := by omega
  have htoPlus :
      ∀ {z : ℝ} {f : FloatSpec.Core.Defs.FlocqFloat radix},
        Closest (beta:=radix) b (radix : ℝ) z f →
          z = 0 → _root_.F2R (beta:=radix) f = 0 := by
    intro z f hClosest hz
    have h := ClosestZero2 (beta:=radix) b (radix : ℝ) z f
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestZero2_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨hClosest, hz⟩
  by_cases hxZero : _root_.F2R (beta:=radix) x = 0
  · have hrZero : _root_.F2R (beta:=radix) r = 0 :=
      htoPlus hD1 (by rw [hxZero]; ring)
    have hpZero : _root_.F2R (beta:=radix) p = 0 :=
      htoPlus hA1 (by rw [hxZero]; ring)
    have hqZero : _root_.F2R (beta:=radix) q = 0 :=
      htoPlus hA2 (by rw [hxZero, hpZero]; ring)
    have hhxZero : _root_.F2R (beta:=radix) hx = 0 :=
      htoPlus hA3 (by rw [hqZero, hpZero]; ring)
    have htxZero : _root_.F2R (beta:=radix) tx = 0 :=
      htoPlus hA4 (by rw [hxZero, hhxZero]; ring)
    have hx1y1Zero : _root_.F2R (beta:=radix) x1y1 = 0 :=
      htoPlus hC1 (by rw [hhxZero]; ring)
    have hx1y2Zero : _root_.F2R (beta:=radix) x1y2 = 0 :=
      htoPlus hC2 (by rw [hhxZero]; ring)
    have hx2y1Zero : _root_.F2R (beta:=radix) x2y1 = 0 :=
      htoPlus hC3 (by rw [htxZero]; ring)
    have hx2y2Zero : _root_.F2R (beta:=radix) x2y2 = 0 :=
      htoPlus hC4 (by rw [htxZero]; ring)
    have ht1Zero : _root_.F2R (beta:=radix) t1 = 0 :=
      htoPlus hD2 (by rw [hrZero, hx1y1Zero]; ring)
    have ht2Zero : _root_.F2R (beta:=radix) t2 = 0 :=
      htoPlus hD3 (by rw [ht1Zero, hx1y2Zero]; ring)
    have ht3Zero : _root_.F2R (beta:=radix) t3 = 0 :=
      htoPlus hD4 (by rw [ht2Zero, hx2y1Zero]; ring)
    have ht4Zero : _root_.F2R (beta:=radix) t4 = 0 :=
      htoPlus hD5 (by rw [ht3Zero, hx2y2Zero]; ring)
    rw [hxZero, hrZero, ht4Zero]
    ring
  · rcases bimplybplusNorm (beta:=radix) b radix s t x rfl hradix
        hvNum hsGe hsLe hxSubnormal.1 hxZero with
      ⟨xx, hxxValue, hxxNormal⟩
    have hvNumPlus : bPlus.vNum = Zpower_nat radix t := by
      simpa [bPlus, plusExp] using hvNum
    have hyNormalPlus : Fnormal (beta:=radix) radix bPlus y := by
      constructor
      · rcases hyNormal.1 with ⟨hnum, hexp⟩
        constructor
        · simpa [bPlus, plusExp] using hnum
        · dsimp [bPlus, plusExp]
          omega
      · simpa [bPlus, plusExp] using hyNormal.2
    have hyExpNonneg : 0 ≤ y.Fexp := by
      have hxExp : x.Fexp = -b.dExp := hxSubnormal.2.1
      rw [hxExp] at hK
      omega
    have hKPlus : -bPlus.dExp ≤ xx.Fexp + y.Fexp := by
      have hxxExp : -bPlus.dExp ≤ xx.Fexp := hxxNormal.1.2
      omega
    have liftClosest :
        ∀ (fext f : FloatSpec.Core.Defs.FlocqFloat radix) (z : ℝ),
          _root_.F2R (beta:=radix) fext = z →
          -b.dExp ≤ fext.Fexp →
          Closest (beta:=radix) b (radix : ℝ) z f →
          Closest (beta:=radix) bPlus (radix : ℝ) z f := by
      intro fext f z hfextValue hfextExp hClosest
      have hClosest' :
          Closest (beta:=radix) b (radix : ℝ)
            (_root_.F2R (beta:=radix) fext) f := by
        simpa [hfextValue] using hClosest
      have h := Closestbbplus b radix t t fext f rfl hradix hvNum
        htGtOne hfextExp hClosest'
      simpa [bPlus, hfextValue] using h
    rcases VeltkampU (beta:=radix) b radix s t x p q hx tx rfl hradix
        hvNum hsGe hsLe (Or.inr hxSubnormal) (by simpa [s] using hA1)
        hA2 hA3 hA4 with
      ⟨_hxResidual, _hXeq, _hxHead, hxTail⟩
    rcases hxTail with ⟨tx', htxVal, _htxBound, htxExp⟩
    rcases VeltkampU (beta:=radix) b radix s t y p' q' hy ty rfl hradix
        hvNum hsGe hsLe (Or.inl hyNormal) (by simpa [s] using hB1)
        hB2 hB3 hB4 with
      ⟨_hyResidual, _hYeq, hyHead, hyTail⟩
    rcases hyHead with ⟨hy', hhyVal, _hhyBound, hhyExpFn⟩
    rcases hyTail with ⟨ty', htyVal, _htyBound, htyExp⟩
    have hhyExp : (s : Int) + y.Fexp ≤ hy'.Fexp := hhyExpFn hyNormal
    have hyPrimeNonneg : 0 ≤ hy'.Fexp := by omega
    have tyPrimeNonneg : 0 ≤ ty'.Fexp := by omega
    let xs : FloatSpec.Core.Defs.FlocqFloat radix := ⟨x.Fnum, x.Fexp + (s : Int)⟩
    have hxsValue :
        _root_.F2R (beta:=radix) xs =
          _root_.F2R (beta:=radix) x * (radix : ℝ) ^ (s : Int) := by
      simp only [xs, _root_.F2R, FloatSpec.Core.Defs.F2R]
      rw [zpow_add₀ (by exact_mod_cast (show radix ≠ 0 by omega))]
      ring
    let xScaled : FloatSpec.Core.Defs.FlocqFloat radix := Fplus x xs
    have hxScaledValue :
        _root_.F2R (beta:=radix) xScaled =
          _root_.F2R (beta:=radix) x *
            ((radix : ℝ) ^ (s : Int) + 1) := by
      have h := Fplus_correct (beta:=radix) x xs
      have h' : _root_.F2R (beta:=radix) xScaled =
          _root_.F2R (beta:=radix) x + _root_.F2R (beta:=radix) xs := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fplus_correct_check,
          Id.run, ULift.up_down, xScaled, Int.cast_ofNat] using h hradix
      rw [h', hxsValue]
      ring
    have hxScaledExp : -b.dExp ≤ xScaled.Fexp := by
      have hmin := Fplus_exp_eq_min x xs
      have hxLeXs : x.Fexp ≤ xs.Fexp := by simp [xs]
      rw [show xScaled.Fexp = min x.Fexp xs.Fexp by simpa [xScaled] using hmin,
        min_eq_left hxLeXs]
      exact hxSubnormal.1.2
    let ys : FloatSpec.Core.Defs.FlocqFloat radix := ⟨y.Fnum, y.Fexp + (s : Int)⟩
    have hysValue :
        _root_.F2R (beta:=radix) ys =
          _root_.F2R (beta:=radix) y * (radix : ℝ) ^ (s : Int) := by
      simp only [ys, _root_.F2R, FloatSpec.Core.Defs.F2R]
      rw [zpow_add₀ (by exact_mod_cast (show radix ≠ 0 by omega))]
      ring
    let yScaled : FloatSpec.Core.Defs.FlocqFloat radix := Fplus y ys
    have hyScaledValue :
        _root_.F2R (beta:=radix) yScaled =
          _root_.F2R (beta:=radix) y *
            ((radix : ℝ) ^ (s : Int) + 1) := by
      have h := Fplus_correct (beta:=radix) y ys
      have h' : _root_.F2R (beta:=radix) yScaled =
          _root_.F2R (beta:=radix) y + _root_.F2R (beta:=radix) ys := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fplus_correct_check,
          Id.run, ULift.up_down, yScaled, Int.cast_ofNat] using h hradix
      rw [h', hysValue]
      ring
    have hyScaledExp : -b.dExp ≤ yScaled.Fexp := by
      have hmin := Fplus_exp_eq_min y ys
      have hyLeYs : y.Fexp ≤ ys.Fexp := by simp [ys]
      rw [show yScaled.Fexp = min y.Fexp ys.Fexp by simpa [yScaled] using hmin,
        min_eq_left hyLeYs]
      exact hyNormal.1.2
    let xMinusP : FloatSpec.Core.Defs.FlocqFloat radix := Fminus x p
    have hxMinusPValue :
        _root_.F2R (beta:=radix) xMinusP =
          _root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p := by
      have h := Fminus_correct (beta:=radix) x p
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus,
        Id.run, ULift.up_down, xMinusP, Int.cast_ofNat] using h hradix
    have hxMinusPExp : -b.dExp ≤ xMinusP.Fexp := by
      rw [show xMinusP.Fexp = min x.Fexp p.Fexp by
        simpa [xMinusP] using Fminus_exp_eq_min (beta:=radix) x p]
      exact le_min hxSubnormal.1.2 hA1.1.2
    let qPlusP : FloatSpec.Core.Defs.FlocqFloat radix := Fplus q p
    have hqPlusPValue :
        _root_.F2R (beta:=radix) qPlusP =
          _root_.F2R (beta:=radix) q + _root_.F2R (beta:=radix) p := by
      have h := Fplus_correct (beta:=radix) q p
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fplus_correct_check,
        Id.run, ULift.up_down, qPlusP, Int.cast_ofNat] using h hradix
    have hqPlusPExp : -b.dExp ≤ qPlusP.Fexp := by
      rw [show qPlusP.Fexp = min q.Fexp p.Fexp by
        simpa [qPlusP] using Fplus_exp_eq_min (beta:=radix) q p]
      exact le_min hA2.1.2 hA1.1.2
    let xMinusHx : FloatSpec.Core.Defs.FlocqFloat radix := Fminus x hx
    have hxMinusHxValue :
        _root_.F2R (beta:=radix) xMinusHx =
          _root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) hx := by
      have h := Fminus_correct (beta:=radix) x hx
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus,
        Id.run, ULift.up_down, xMinusHx, Int.cast_ofNat] using h hradix
    have hxMinusHxExp : -b.dExp ≤ xMinusHx.Fexp := by
      rw [show xMinusHx.Fexp = min x.Fexp hx.Fexp by
        simpa [xMinusHx] using Fminus_exp_eq_min (beta:=radix) x hx]
      exact le_min hxSubnormal.1.2 hA3.1.2
    let yMinusP : FloatSpec.Core.Defs.FlocqFloat radix := Fminus y p'
    have hyMinusPValue :
        _root_.F2R (beta:=radix) yMinusP =
          _root_.F2R (beta:=radix) y - _root_.F2R (beta:=radix) p' := by
      have h := Fminus_correct (beta:=radix) y p'
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus,
        Id.run, ULift.up_down, yMinusP, Int.cast_ofNat] using h hradix
    have hyMinusPExp : -b.dExp ≤ yMinusP.Fexp := by
      rw [show yMinusP.Fexp = min y.Fexp p'.Fexp by
        simpa [yMinusP] using Fminus_exp_eq_min (beta:=radix) y p']
      exact le_min hyNormal.1.2 hB1.1.2
    let qPlusP' : FloatSpec.Core.Defs.FlocqFloat radix := Fplus q' p'
    have hqPlusP'Value :
        _root_.F2R (beta:=radix) qPlusP' =
          _root_.F2R (beta:=radix) q' + _root_.F2R (beta:=radix) p' := by
      have h := Fplus_correct (beta:=radix) q' p'
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fplus_correct_check,
        Id.run, ULift.up_down, qPlusP', Int.cast_ofNat] using h hradix
    have hqPlusP'Exp : -b.dExp ≤ qPlusP'.Fexp := by
      rw [show qPlusP'.Fexp = min q'.Fexp p'.Fexp by
        simpa [qPlusP'] using Fplus_exp_eq_min (beta:=radix) q' p']
      exact le_min hB2.1.2 hB1.1.2
    let yMinusHy : FloatSpec.Core.Defs.FlocqFloat radix := Fminus y hy
    have hyMinusHyValue :
        _root_.F2R (beta:=radix) yMinusHy =
          _root_.F2R (beta:=radix) y - _root_.F2R (beta:=radix) hy := by
      have h := Fminus_correct (beta:=radix) y hy
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus,
        Id.run, ULift.up_down, yMinusHy, Int.cast_ofNat] using h hradix
    have hyMinusHyExp : -b.dExp ≤ yMinusHy.Fexp := by
      rw [show yMinusHy.Fexp = min y.Fexp hy.Fexp by
        simpa [yMinusHy] using Fminus_exp_eq_min (beta:=radix) y hy]
      exact le_min hyNormal.1.2 hB3.1.2
    have hA1Plus :
        Closest (beta:=radix) bPlus (radix : ℝ)
          (_root_.F2R (beta:=radix) xx *
            ((radix : ℝ) ^ (((t - Nat.div2 t : Nat) : Int)) + 1)) p := by
      have h := liftClosest xScaled p
        (_root_.F2R (beta:=radix) x *
          ((radix : ℝ) ^ (s : Int) + 1))
        hxScaledValue hxScaledExp (by simpa [s] using hA1)
      simpa [s, hxxValue] using h
    have hA2Plus :
        Closest (beta:=radix) bPlus (radix : ℝ)
          (_root_.F2R (beta:=radix) xx - _root_.F2R (beta:=radix) p) q := by
      have h := liftClosest xMinusP q
        (_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) p)
        hxMinusPValue hxMinusPExp hA2
      simpa [hxxValue] using h
    have hA3Plus :
        Closest (beta:=radix) bPlus (radix : ℝ)
          (_root_.F2R (beta:=radix) q + _root_.F2R (beta:=radix) p) hx :=
      liftClosest qPlusP hx
        (_root_.F2R (beta:=radix) q + _root_.F2R (beta:=radix) p)
        hqPlusPValue hqPlusPExp hA3
    have hA4Plus :
        Closest (beta:=radix) bPlus (radix : ℝ)
          (_root_.F2R (beta:=radix) xx - _root_.F2R (beta:=radix) hx) tx := by
      have h := liftClosest xMinusHx tx
        (_root_.F2R (beta:=radix) x - _root_.F2R (beta:=radix) hx)
        hxMinusHxValue hxMinusHxExp hA4
      simpa [hxxValue] using h
    have hB1Plus :
        Closest (beta:=radix) bPlus (radix : ℝ)
          (_root_.F2R (beta:=radix) y *
            ((radix : ℝ) ^ (((t - Nat.div2 t : Nat) : Int)) + 1)) p' := by
      have h := liftClosest yScaled p'
        (_root_.F2R (beta:=radix) y *
          ((radix : ℝ) ^ (s : Int) + 1))
        hyScaledValue hyScaledExp (by simpa [s] using hB1)
      simpa [s] using h
    have hB2Plus :
        Closest (beta:=radix) bPlus (radix : ℝ)
          (_root_.F2R (beta:=radix) y - _root_.F2R (beta:=radix) p') q' :=
      liftClosest yMinusP q'
        (_root_.F2R (beta:=radix) y - _root_.F2R (beta:=radix) p')
        hyMinusPValue hyMinusPExp hB2
    have hB3Plus :
        Closest (beta:=radix) bPlus (radix : ℝ)
          (_root_.F2R (beta:=radix) q' + _root_.F2R (beta:=radix) p') hy :=
      liftClosest qPlusP' hy
        (_root_.F2R (beta:=radix) q' + _root_.F2R (beta:=radix) p')
        hqPlusP'Value hqPlusP'Exp hB3
    have hB4Plus :
        Closest (beta:=radix) bPlus (radix : ℝ)
          (_root_.F2R (beta:=radix) y - _root_.F2R (beta:=radix) hy) ty :=
      liftClosest yMinusHy ty
        (_root_.F2R (beta:=radix) y - _root_.F2R (beta:=radix) hy)
        hyMinusHyValue hyMinusHyExp hB4
    let hxhy : FloatSpec.Core.Defs.FlocqFloat radix :=
      FloatSpec.Calc.Operations.Fmult (beta:=radix) hx hy'
    have hxhyValue :
        _root_.F2R (beta:=radix) hxhy =
          _root_.F2R (beta:=radix) hx * _root_.F2R (beta:=radix) hy := by
      have h := Fmult_correct (beta:=radix) hx hy'
      have hv :
          _root_.F2R (beta:=radix) hxhy =
            _root_.F2R (beta:=radix) hx * _root_.F2R (beta:=radix) hy' := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fmult_correct_check,
          Id.run, ULift.up_down, hxhy, Int.cast_ofNat] using h hradix
      rw [hv, hhyVal]
    have hxhyExp : -b.dExp ≤ hxhy.Fexp := by
      dsimp [hxhy, FloatSpec.Calc.Operations.Fmult]
      have hxBound : -b.dExp ≤ hx.Fexp := hA3.1.2
      omega
    let hxty : FloatSpec.Core.Defs.FlocqFloat radix :=
      FloatSpec.Calc.Operations.Fmult (beta:=radix) hx ty'
    have hxtyValue :
        _root_.F2R (beta:=radix) hxty =
          _root_.F2R (beta:=radix) hx * _root_.F2R (beta:=radix) ty := by
      have h := Fmult_correct (beta:=radix) hx ty'
      have hv :
          _root_.F2R (beta:=radix) hxty =
            _root_.F2R (beta:=radix) hx * _root_.F2R (beta:=radix) ty' := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fmult_correct_check,
          Id.run, ULift.up_down, hxty, Int.cast_ofNat] using h hradix
      rw [hv, htyVal]
    have hxtyExp : -b.dExp ≤ hxty.Fexp := by
      dsimp [hxty, FloatSpec.Calc.Operations.Fmult]
      have hxBound : -b.dExp ≤ hx.Fexp := hA3.1.2
      omega
    let txhy : FloatSpec.Core.Defs.FlocqFloat radix :=
      FloatSpec.Calc.Operations.Fmult (beta:=radix) tx' hy'
    have txhyValue :
        _root_.F2R (beta:=radix) txhy =
          _root_.F2R (beta:=radix) tx * _root_.F2R (beta:=radix) hy := by
      have h := Fmult_correct (beta:=radix) tx' hy'
      have hv :
          _root_.F2R (beta:=radix) txhy =
            _root_.F2R (beta:=radix) tx' * _root_.F2R (beta:=radix) hy' := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fmult_correct_check,
          Id.run, ULift.up_down, txhy, Int.cast_ofNat] using h hradix
      rw [hv, htxVal, hhyVal]
    have txhyExp : -b.dExp ≤ txhy.Fexp := by
      dsimp [txhy, FloatSpec.Calc.Operations.Fmult]
      have hxExp : x.Fexp = -b.dExp := hxSubnormal.2.1
      omega
    let txty : FloatSpec.Core.Defs.FlocqFloat radix :=
      FloatSpec.Calc.Operations.Fmult (beta:=radix) tx' ty'
    have txtyValue :
        _root_.F2R (beta:=radix) txty =
          _root_.F2R (beta:=radix) tx * _root_.F2R (beta:=radix) ty := by
      have h := Fmult_correct (beta:=radix) tx' ty'
      have hv :
          _root_.F2R (beta:=radix) txty =
            _root_.F2R (beta:=radix) tx' * _root_.F2R (beta:=radix) ty' := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fmult_correct_check,
          Id.run, ULift.up_down, txty, Int.cast_ofNat] using h hradix
      rw [hv, htxVal, htyVal]
    have txtyExp : -b.dExp ≤ txty.Fexp := by
      dsimp [txty, FloatSpec.Calc.Operations.Fmult]
      have hxExp : x.Fexp = -b.dExp := hxSubnormal.2.1
      omega
    let xy : FloatSpec.Core.Defs.FlocqFloat radix :=
      FloatSpec.Calc.Operations.Fmult (beta:=radix) x y
    have hxyValue :
        _root_.F2R (beta:=radix) xy =
          _root_.F2R (beta:=radix) x * _root_.F2R (beta:=radix) y := by
      have h := Fmult_correct (beta:=radix) x y
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fmult_correct_check,
        Id.run, ULift.up_down, xy, Int.cast_ofNat] using h hradix
    have hxyExp : -b.dExp ≤ xy.Fexp := by
      dsimp [xy, FloatSpec.Calc.Operations.Fmult]
      exact hK
    let rMinus11 : FloatSpec.Core.Defs.FlocqFloat radix := Fminus r x1y1
    have hrMinus11Value :
        _root_.F2R (beta:=radix) rMinus11 =
          _root_.F2R (beta:=radix) r - _root_.F2R (beta:=radix) x1y1 := by
      have h := Fminus_correct (beta:=radix) r x1y1
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus,
        Id.run, ULift.up_down, rMinus11, Int.cast_ofNat] using h hradix
    have hrMinus11Exp : -b.dExp ≤ rMinus11.Fexp := by
      rw [show rMinus11.Fexp = min r.Fexp x1y1.Fexp by
        simpa [rMinus11] using Fminus_exp_eq_min (beta:=radix) r x1y1]
      exact le_min hD1.1.2 hC1.1.2
    let t1Minus12 : FloatSpec.Core.Defs.FlocqFloat radix := Fminus t1 x1y2
    have ht1Minus12Value :
        _root_.F2R (beta:=radix) t1Minus12 =
          _root_.F2R (beta:=radix) t1 - _root_.F2R (beta:=radix) x1y2 := by
      have h := Fminus_correct (beta:=radix) t1 x1y2
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus,
        Id.run, ULift.up_down, t1Minus12, Int.cast_ofNat] using h hradix
    have ht1Minus12Exp : -b.dExp ≤ t1Minus12.Fexp := by
      rw [show t1Minus12.Fexp = min t1.Fexp x1y2.Fexp by
        simpa [t1Minus12] using Fminus_exp_eq_min (beta:=radix) t1 x1y2]
      exact le_min hD2.1.2 hC2.1.2
    let t2Minus21 : FloatSpec.Core.Defs.FlocqFloat radix := Fminus t2 x2y1
    have ht2Minus21Value :
        _root_.F2R (beta:=radix) t2Minus21 =
          _root_.F2R (beta:=radix) t2 - _root_.F2R (beta:=radix) x2y1 := by
      have h := Fminus_correct (beta:=radix) t2 x2y1
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus,
        Id.run, ULift.up_down, t2Minus21, Int.cast_ofNat] using h hradix
    have ht2Minus21Exp : -b.dExp ≤ t2Minus21.Fexp := by
      rw [show t2Minus21.Fexp = min t2.Fexp x2y1.Fexp by
        simpa [t2Minus21] using Fminus_exp_eq_min (beta:=radix) t2 x2y1]
      exact le_min hD3.1.2 hC3.1.2
    let t3Minus22 : FloatSpec.Core.Defs.FlocqFloat radix := Fminus t3 x2y2
    have ht3Minus22Value :
        _root_.F2R (beta:=radix) t3Minus22 =
          _root_.F2R (beta:=radix) t3 - _root_.F2R (beta:=radix) x2y2 := by
      have h := Fminus_correct (beta:=radix) t3 x2y2
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus,
        Id.run, ULift.up_down, t3Minus22, Int.cast_ofNat] using h hradix
    have ht3Minus22Exp : -b.dExp ≤ t3Minus22.Fexp := by
      rw [show t3Minus22.Fexp = min t3.Fexp x2y2.Fexp by
        simpa [t3Minus22] using Fminus_exp_eq_min (beta:=radix) t3 x2y2]
      exact le_min hD4.1.2 hC4.1.2
    have hC1Plus :
        Closest (beta:=radix) bPlus (radix : ℝ)
          (_root_.F2R (beta:=radix) hx * _root_.F2R (beta:=radix) hy) x1y1 :=
      liftClosest hxhy x1y1
        (_root_.F2R (beta:=radix) hx * _root_.F2R (beta:=radix) hy)
        hxhyValue hxhyExp hC1
    have hC2Plus :
        Closest (beta:=radix) bPlus (radix : ℝ)
          (_root_.F2R (beta:=radix) hx * _root_.F2R (beta:=radix) ty) x1y2 :=
      liftClosest hxty x1y2
        (_root_.F2R (beta:=radix) hx * _root_.F2R (beta:=radix) ty)
        hxtyValue hxtyExp hC2
    have hC3Plus :
        Closest (beta:=radix) bPlus (radix : ℝ)
          (_root_.F2R (beta:=radix) tx * _root_.F2R (beta:=radix) hy) x2y1 :=
      liftClosest txhy x2y1
        (_root_.F2R (beta:=radix) tx * _root_.F2R (beta:=radix) hy)
        txhyValue txhyExp hC3
    have hC4Plus :
        Closest (beta:=radix) bPlus (radix : ℝ)
          (_root_.F2R (beta:=radix) tx * _root_.F2R (beta:=radix) ty) x2y2 :=
      liftClosest txty x2y2
        (_root_.F2R (beta:=radix) tx * _root_.F2R (beta:=radix) ty)
        txtyValue txtyExp hC4
    have hD1Plus :
        Closest (beta:=radix) bPlus (radix : ℝ)
          (_root_.F2R (beta:=radix) xx * _root_.F2R (beta:=radix) y) r := by
      have h := liftClosest xy r
        (_root_.F2R (beta:=radix) x * _root_.F2R (beta:=radix) y)
        hxyValue hxyExp hD1
      simpa [hxxValue] using h
    have hD2Plus :
        Closest (beta:=radix) bPlus (radix : ℝ)
          (_root_.F2R (beta:=radix) r - _root_.F2R (beta:=radix) x1y1) t1 :=
      liftClosest rMinus11 t1
        (_root_.F2R (beta:=radix) r - _root_.F2R (beta:=radix) x1y1)
        hrMinus11Value hrMinus11Exp hD2
    have hD3Plus :
        Closest (beta:=radix) bPlus (radix : ℝ)
          (_root_.F2R (beta:=radix) t1 - _root_.F2R (beta:=radix) x1y2) t2 :=
      liftClosest t1Minus12 t2
        (_root_.F2R (beta:=radix) t1 - _root_.F2R (beta:=radix) x1y2)
        ht1Minus12Value ht1Minus12Exp hD3
    have hD4Plus :
        Closest (beta:=radix) bPlus (radix : ℝ)
          (_root_.F2R (beta:=radix) t2 - _root_.F2R (beta:=radix) x2y1) t3 :=
      liftClosest t2Minus21 t3
        (_root_.F2R (beta:=radix) t2 - _root_.F2R (beta:=radix) x2y1)
        ht2Minus21Value ht2Minus21Exp hD4
    have hD5Plus :
        Closest (beta:=radix) bPlus (radix : ℝ)
          (_root_.F2R (beta:=radix) t3 - _root_.F2R (beta:=radix) x2y2) t4 :=
      liftClosest t3Minus22 t4
        (_root_.F2R (beta:=radix) t3 - _root_.F2R (beta:=radix) x2y2)
        ht3Minus22Value ht3Minus22Exp hD5
    have hDekker := DekkerN (beta:=radix) bPlus radix t xx y p q hx tx
      p' q' hy ty x1y1 x1y2 x2y1 x2y2 r t1 t2 t3 t4 rfl hradix
      hvNumPlus hpGe hxxNormal hyNormalPlus hKPlus hA1Plus hA2Plus
      hA3Plus hA4Plus hB1Plus hB2Plus hB3Plus hB4Plus hC1Plus hC2Plus
      hC3Plus hC4Plus hD1Plus hD2Plus hD3Plus hD4Plus hD5Plus hBranch
    simpa [hxxValue] using hDekker

/-! Coq Algo1 theorem `Dekker1`.

This is the exact upstream section wrapper at `Pff.v:18387`: split the
canonical inputs into the four normal/subnormal cases, delegate the three
possible cases to `DekkerN`, `DekkerS1`, and `DekkerS2`, and derive the final
subnormal/subnormal case contradiction from `Expoxy` and `0 < b.dExp`. -/
theorem Dekker1 {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (t : Nat)
    (x y p q hx tx p' q' hy ty x1y1 x1y2 x2y1 x2y2 r t1 t2 t3 t4 :
      FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix t)
    (hpGe : 4 ≤ t)
    (hxCan : Fcanonic (beta:=beta) radix b x)
    (hyCan : Fcanonic (beta:=beta) radix b y)
    (hExpoxy : -b.dExp ≤ x.Fexp + y.Fexp)
    (hA1 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x *
        ((radix : ℝ) ^ (((t - Nat.div2 t : Nat) : Int)) + 1)) p)
    (hA2 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) p) q)
    (hA3 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) q + _root_.F2R (beta:=beta) p) hx)
    (hA4 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x - _root_.F2R (beta:=beta) hx) tx)
    (hB1 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) y *
        ((radix : ℝ) ^ (((t - Nat.div2 t : Nat) : Int)) + 1)) p')
    (hB2 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) y - _root_.F2R (beta:=beta) p') q')
    (hB3 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) q' + _root_.F2R (beta:=beta) p') hy)
    (hB4 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) y - _root_.F2R (beta:=beta) hy) ty)
    (hC1 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) hx * _root_.F2R (beta:=beta) hy) x1y1)
    (hC2 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) hx * _root_.F2R (beta:=beta) ty) x1y2)
    (hC3 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) tx * _root_.F2R (beta:=beta) hy) x2y1)
    (hC4 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) tx * _root_.F2R (beta:=beta) ty) x2y2)
    (hD1 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x * _root_.F2R (beta:=beta) y) r)
    (hD2 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) x1y1) t1)
    (hD3 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) t1 - _root_.F2R (beta:=beta) x1y2) t2)
    (hD4 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) t2 - _root_.F2R (beta:=beta) x2y1) t3)
    (hD5 : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) t3 - _root_.F2R (beta:=beta) x2y2) t4)
    (hdExpPos : 0 < b.dExp)
    (hBranch : radix = 2 ∨ Even t) :
    _root_.F2R (beta:=beta) x * _root_.F2R (beta:=beta) y =
      _root_.F2R (beta:=beta) r - _root_.F2R (beta:=beta) t4 := by
  rcases hxCan with hxNormal | hxSubnormal
  · rcases hyCan with hyNormal | hySubnormal
    · exact DekkerN (beta:=beta) b radix t x y p q hx tx p' q' hy ty
        x1y1 x1y2 x2y1 x2y2 r t1 t2 t3 t4 hbeta hradix hvNum hpGe
        hxNormal hyNormal hExpoxy hA1 hA2 hA3 hA4 hB1 hB2 hB3 hB4 hC1
        hC2 hC3 hC4 hD1 hD2 hD3 hD4 hD5 hBranch
    · exact DekkerS1 (beta:=beta) b radix t x y p q hx tx p' q' hy ty
        x1y1 x1y2 x2y1 x2y2 r t1 t2 t3 t4 hbeta hradix hvNum hpGe
        hxNormal hySubnormal hExpoxy hA1 hA2 hA3 hA4 hB1 hB2 hB3 hB4 hC1
        hC2 hC3 hC4 hD1 hD2 hD3 hD4 hD5 hBranch
  · rcases hyCan with hyNormal | hySubnormal
    · exact DekkerS2 (beta:=beta) b radix t x y p q hx tx p' q' hy ty
        x1y1 x1y2 x2y1 x2y2 r t1 t2 t3 t4 hbeta hradix hvNum hpGe
        hxSubnormal hyNormal hExpoxy hA1 hA2 hA3 hA4 hB1 hB2 hB3 hB4 hC1
        hC2 hC3 hC4 hD1 hD2 hD3 hD4 hD5 hBranch
    · exfalso
      have hxExp : x.Fexp = -b.dExp := hxSubnormal.2.1
      have hyExp : y.Fexp = -b.dExp := hySubnormal.2.1
      omega

/-- Closed section-context form of `RND_EvenClosest_canonic`.

This discharges the signed lower/upper canonicity dependencies.  The analogous
correctness theorem still needs the Flocq midpoint parity bridge, so it remains
separate. -/
theorem RND_EvenClosest_canonic {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat) (r : ℝ) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < (precision : Int) ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (RND_EvenClosest_canonic_check (beta:=beta) b radix precision r) : Id Unit)
    ⦃⇓_ => ⌜Fcanonic (beta:=beta) radix b
        (RND_EvenClosest (beta:=beta) b radix precision r)⌝⦄ := by
  intro h
  rcases h with ⟨hbeta, hradix, hp, hvnum⟩
  have htoNat : ((precision : Int).toNat) = precision := by
    simp
  have hMin : Fcanonic (beta:=beta) radix b
      (RND_Min (beta:=beta) b radix (precision : Int) r) := by
    have h := RND_Min_canonic (beta:=beta) b radix (precision : Int) r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RND_Min_canonic_check,
      Id.run, ULift.up_down, htoNat, Int.cast_ofNat] using h ⟨hbeta, hradix, hp, hvnum⟩
  have hMax : Fcanonic (beta:=beta) radix b
      (RND_Max (beta:=beta) b radix (precision : Int) r) := by
    have h := RND_Max_canonic (beta:=beta) b radix (precision : Int) r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RND_Max_canonic_check,
      Id.run, ULift.up_down, htoNat, Int.cast_ofNat] using h ⟨hbeta, hradix, hp, hvnum⟩
  have h := RND_EvenClosest_canonic_from_min_max (beta:=beta) b radix precision r
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RND_EvenClosest_canonic_check,
    Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hMin, hMax⟩

/-- Closed section-context form of Coq `RND_EvenClosest_correct`.

The early payload lemma keeps the implementation case split available before
the min/max infrastructure is declared.  This theorem supplies those payloads
from the restored signed rounding correctness/canonicity theorems and handles
the exact odd lower-endpoint case by the uniqueness branch of `EvenClosest`. -/
theorem RND_EvenClosest_correct {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat) (r : ℝ) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < (precision : Int) ∧
        b.vNum = Zpower_nat radix precision⌝⦄
    (pure (RND_EvenClosest_correct_check (beta:=beta) b radix precision r) :
      Id Unit)
    ⦃⇓_ => ⌜EvenClosest (beta:=beta) b (radix : ℝ) precision r
      (RND_EvenClosest (beta:=beta) b radix precision r)⌝⦄ := by
  classical
  intro h
  rcases h with ⟨hbeta, hradix, hp, hvnum⟩
  subst beta
  have hprecision_ne : precision ≠ 0 := by omega
  have hpNat : 1 < precision := by omega
  have htoNat : ((precision : Int).toNat) = precision := by simp
  simp only [wp, PostCond.noThrow, pure, RND_EvenClosest_correct_check,
    Id.run, ULift.up_down]
  let rd := RND_Min (beta:=radix) b radix (precision : Int) r
  let ru := RND_Max (beta:=radix) b radix (precision : Int) r
  have hMin : isMin (beta:=radix) b radix r rd := by
    have hm := RND_Min_correct (beta:=radix) b radix (precision : Int) r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RND_Min_correct_check,
      Id.run, ULift.up_down, rd, htoNat, Int.cast_ofNat] using hm ⟨rfl, hradix, hp, hvnum⟩
  have hMax : isMax (beta:=radix) b radix r ru := by
    have hm := RND_Max_correct (beta:=radix) b radix (precision : Int) r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RND_Max_correct_check,
      Id.run, ULift.up_down, ru, htoNat, Int.cast_ofNat] using hm ⟨rfl, hradix, hp, hvnum⟩
  have hrdCan : Fcanonic (beta:=radix) radix b rd := by
    have hc := RND_Min_canonic (beta:=radix) b radix (precision : Int) r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RND_Min_canonic_check,
      Id.run, ULift.up_down, rd, htoNat, Int.cast_ofNat] using hc ⟨rfl, hradix, hp, hvnum⟩
  have hruCan : Fcanonic (beta:=radix) radix b ru := by
    have hc := RND_Max_canonic (beta:=radix) b radix (precision : Int) r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RND_Max_canonic_check,
      Id.run, ULift.up_down, ru, htoNat, Int.cast_ofNat] using hc ⟨rfl, hradix, hp, hvnum⟩
  have hClosestMinOrMax :
      ∀ q : FloatSpec.Core.Defs.FlocqFloat radix,
        Closest (beta:=radix) b (radix : ℝ) r q →
          isMin (beta:=radix) b radix r q ∨
            isMax (beta:=radix) b radix r q := by
    have hcm := ClosestMinOrMax_from_split_radix_payload (beta:=radix) b radix (radix : ℝ)
    have hfull :
        MinOrMaxP_float (beta:=radix) b radix
          (Closest (beta:=radix) b (radix : ℝ)) := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMinOrMax_check, MinOrMaxP_float,
        Id.run, ULift.up_down, Int.cast_ofNat] using hcm trivial
    exact hfull r
  have hMinUnique :
      ∀ q : FloatSpec.Core.Defs.FlocqFloat radix,
        isMin (beta:=radix) b radix r q →
          _root_.F2R (beta:=radix) q = _root_.F2R (beta:=radix) rd := by
    intro q hq
    have hm := MinEq (beta:=radix) b radix q rd r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinEq_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using hm ⟨hq, hMin⟩
  have hMaxUnique :
      ∀ q : FloatSpec.Core.Defs.FlocqFloat radix,
        isMax (beta:=radix) b radix r q →
          _root_.F2R (beta:=radix) q = _root_.F2R (beta:=radix) ru := by
    intro q hq
    have hm := MaxEq (beta:=radix) b radix q ru r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MaxEq_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using hm ⟨hq, hMax⟩
  have hrd_le : _root_.F2R (beta:=radix) rd ≤ r := hMin.2.1
  have hle_ru : r ≤ _root_.F2R (beta:=radix) ru := hMax.2.1
  have hrd_nonpos : _root_.F2R (beta:=radix) rd - r ≤ 0 := by
    linarith
  have hru_nonneg : 0 ≤ _root_.F2R (beta:=radix) ru - r := by
    linarith
  have hrdEven_of_not_odd :
      ¬ Odd rd.Fnum →
        FNeven (beta:=radix) b (radix : ℝ) precision rd := by
    intro hnot
    have hnorm := FcanonicFnormalizeEq (beta:=radix) radix b precision rd
    have hnorm_eq :
        Fnormalize (beta:=radix) radix b precision rd = rd := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        hnorm ⟨hrdCan, rfl, hradix, hprecision_ne, hvnum⟩
    have heven : Even rd.Fnum := Int.not_odd_iff_even.mp hnot
    simpa [FNeven, Feven, hnorm_eq] using heven
  unfold RND_EvenClosest
  dsimp only [rd, ru]
  by_cases hle :
      |_root_.F2R (beta:=radix) ru - r| ≤
        |_root_.F2R (beta:=radix) rd - r|
  · simp [rd, ru, hle]
    by_cases hlt :
        |_root_.F2R (beta:=radix) ru - r| <
          |_root_.F2R (beta:=radix) rd - r|
    · have hmid : _root_.F2R (beta:=radix) rd + _root_.F2R (beta:=radix) ru ≤
          2 * r := by
        rw [abs_of_nonneg hru_nonneg, abs_of_nonpos hrd_nonpos] at hlt
        linarith
      have hmaxClosest : Closest (beta:=radix) b (radix : ℝ) r ru := by
        have hc := ClosestMax (beta:=radix) b radix (radix : ℝ) r rd ru
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMax_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using hc ⟨hMin, hMax, hmid⟩
      refine ⟨by simpa [rd, ru, hle, hlt] using hmaxClosest, Or.inr ?_⟩
      intro q hq
      have hlt_abs := hlt
      rw [abs_of_nonneg hru_nonneg, abs_of_nonpos hrd_nonpos] at hlt_abs
      rcases hClosestMinOrMax q hq with hqMin | hqMax
      · have hq_rd := hMinUnique q hqMin
        exfalso
        have hqdist := hq.2 ru hMax.1
        have hq_nonpos : _root_.F2R (beta:=radix) q - r ≤ 0 := by
          have hq_le : _root_.F2R (beta:=radix) q ≤ r := hqMin.2.1
          linarith
        rw [hq_rd, abs_of_nonpos hrd_nonpos, abs_of_nonneg hru_nonneg] at hqdist
        linarith
      · simpa [RND_EvenClosest, rd, ru, hle, hlt] using hMaxUnique q hqMax
    · have hdist_eq :
          |_root_.F2R (beta:=radix) ru - r| =
            |_root_.F2R (beta:=radix) rd - r| :=
        le_antisymm hle (le_of_not_gt hlt)
      have hmid_min :
          2 * r ≤ _root_.F2R (beta:=radix) rd +
            _root_.F2R (beta:=radix) ru := by
        rw [abs_of_nonneg hru_nonneg, abs_of_nonpos hrd_nonpos] at hdist_eq
        linarith
      have hmid_max :
          _root_.F2R (beta:=radix) rd +
            _root_.F2R (beta:=radix) ru ≤ 2 * r := by
        rw [abs_of_nonneg hru_nonneg, abs_of_nonpos hrd_nonpos] at hdist_eq
        linarith
      by_cases hodd : Odd rd.Fnum
      · by_cases hrd_eq_r : _root_.F2R (beta:=radix) rd = r
        · have hru_eq_r : _root_.F2R (beta:=radix) ru = r := by
            have hdist_eq' := hdist_eq
            rw [hrd_eq_r] at hdist_eq'
            have hru_nonneg' : 0 ≤ _root_.F2R (beta:=radix) ru - r := hru_nonneg
            rw [abs_of_nonneg hru_nonneg'] at hdist_eq'
            simp at hdist_eq'
            linarith
          have hmaxClosest : Closest (beta:=radix) b (radix : ℝ) r ru := by
            have hc := ClosestMax (beta:=radix) b radix (radix : ℝ) r rd ru
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMax_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using hc ⟨hMin, hMax, hmid_max⟩
          refine ⟨by simpa [rd, ru, hle, hlt, hodd] using hmaxClosest, Or.inr ?_⟩
          intro q hq
          rcases hClosestMinOrMax q hq with hqMin | hqMax
          · have hq_rd := hMinUnique q hqMin
            have hq_ru : _root_.F2R (beta:=radix) q = _root_.F2R (beta:=radix) ru := by
              calc
                _root_.F2R (beta:=radix) q = _root_.F2R (beta:=radix) rd := hq_rd
                _ = r := hrd_eq_r
                _ = _root_.F2R (beta:=radix) ru := hru_eq_r.symm
            simpa [RND_EvenClosest, rd, ru, hle, hlt, hodd] using hq_ru
          · simpa [RND_EvenClosest, rd, ru, hle, hlt, hodd] using hMaxUnique q hqMax
        · let srd := FNSucc (beta:=radix) b (radix : ℝ) precision rd
          have hMinMax := MinMax (beta:=radix) b radix precision rd r
          have hsMax : isMax (beta:=radix) b radix r srd := by
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinMax_check, srd,
              Id.run, ULift.up_down, Int.cast_ofNat] using
              hMinMax ⟨hMin, hrd_eq_r, rfl, hradix, hprecision_ne, hvnum⟩
          have hs_eq_ru : _root_.F2R (beta:=radix) srd =
              _root_.F2R (beta:=radix) ru := by
            have hm := MaxEq (beta:=radix) b radix srd ru r
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MaxEq_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using hm ⟨hsMax, hMax⟩
          have hsCan : Fcanonic (beta:=radix) radix b srd := by
            have hc := FNSuccCanonic (beta:=radix) b (radix : ℝ) precision rd
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FNSuccCanonic_check, srd,
              Id.run, ULift.up_down, Int.cast_ofNat] using
              hc ⟨hMin.1, hradix, hprecision_ne, hvnum⟩
          have hrd_odd_norm :
              FNodd (beta:=radix) b (radix : ℝ) precision rd := by
            have hnorm := FcanonicFnormalizeEq (beta:=radix) radix b precision rd
            have hnorm_eq :
                Fnormalize (beta:=radix) radix b precision rd = rd := by
              simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
                Id.run, ULift.up_down, Int.cast_ofNat] using
                hnorm ⟨hrdCan, rfl, hradix, hprecision_ne, hvnum⟩
            simpa [FNodd, Fodd, hnorm_eq] using hodd
          have hs_even :
              FNeven (beta:=radix) b (radix : ℝ) precision srd := by
            have hs := FNoddSuc (beta:=radix) b (radix : ℝ) precision rd
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FNoddSuc_check, srd,
              Id.run, ULift.up_down, Int.cast_ofNat] using
              hs ⟨hMin.1, hrd_odd_norm, hradix, hpNat, hvnum⟩
          have hru_even :
              FNeven (beta:=radix) b (radix : ℝ) precision ru := by
            have heq := FNevenEq (beta:=radix) b (radix : ℝ) precision srd ru
              hradix hpNat hvnum
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FNevenEq_check,
              ULift.down_up, Int.cast_ofNat] using
              heq ⟨hsMax.1, hMax.1, hs_eq_ru, hs_even⟩
          have hmaxClosest : Closest (beta:=radix) b (radix : ℝ) r ru := by
            have hc := ClosestMax (beta:=radix) b radix (radix : ℝ) r rd ru
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMax_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using hc ⟨hMin, hMax, hmid_max⟩
          exact ⟨by simpa [rd, ru, hle, hlt, hodd] using hmaxClosest,
            Or.inl (by simpa [RND_EvenClosest, rd, ru, hle, hlt, hodd] using hru_even)⟩
      · have hrd_even := hrdEven_of_not_odd hodd
        have hminClosest : Closest (beta:=radix) b (radix : ℝ) r rd := by
          have hc := ClosestMin (beta:=radix) b radix (radix : ℝ) r rd ru
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMin_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using hc ⟨hMin, hMax, hmid_min⟩
        exact ⟨by simpa [rd, ru, hle, hlt, hodd] using hminClosest,
          Or.inl (by simpa [RND_EvenClosest, rd, ru, hle, hlt, hodd] using hrd_even)⟩
  · simp [rd, ru, hle]
    have hlt_abs :
        |_root_.F2R (beta:=radix) rd - r| <
          |_root_.F2R (beta:=radix) ru - r| :=
      lt_of_not_ge hle
    have hmid : 2 * r ≤ _root_.F2R (beta:=radix) rd +
        _root_.F2R (beta:=radix) ru := by
      rw [abs_of_nonpos hrd_nonpos, abs_of_nonneg hru_nonneg] at hlt_abs
      linarith
    by_cases hodd : Odd rd.Fnum
    · have hminClosest : Closest (beta:=radix) b (radix : ℝ) r rd := by
        have hc := ClosestMin (beta:=radix) b radix (radix : ℝ) r rd ru
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMin_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using hc ⟨hMin, hMax, hmid⟩
      refine ⟨by simpa [rd, ru, hle] using hminClosest, Or.inr ?_⟩
      intro q hq
      rcases hClosestMinOrMax q hq with hqMin | hqMax
      · exact hMinUnique q hqMin
      · have hq_ru := hMaxUnique q hqMax
        exfalso
        have hqdist := hq.2 rd hMin.1
        rw [hq_ru, abs_of_nonneg hru_nonneg, abs_of_nonpos hrd_nonpos] at hqdist
        rw [abs_of_nonpos hrd_nonpos, abs_of_nonneg hru_nonneg] at hlt_abs
        linarith
    · have hrd_even := hrdEven_of_not_odd hodd
      have hminClosest : Closest (beta:=radix) b (radix : ℝ) r rd := by
        have hc := ClosestMin (beta:=radix) b radix (radix : ℝ) r rd ru
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMin_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using hc ⟨hMin, hMax, hmid⟩
      exact ⟨by simpa [rd, ru, hle] using hminClosest,
        Or.inl (by simpa [RND_EvenClosest, rd, ru, hle] using hrd_even)⟩

-- Coq: `FshiftCorrectInv` — align exponents by shifting the larger one down
noncomputable def FshiftCorrectInv_check {beta : Int} [ValidRadix beta]
    (radix : Int)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem FshiftCorrectInv {beta : Int} [ValidRadix beta]
    (radix : Int)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧
        _root_.F2R x = _root_.F2R y ∧ x.Fexp ≤ y.Fexp⌝⦄
    (pure (FshiftCorrectInv_check (beta:=beta) radix x y) : Id Unit)
    ⦃⇓_ => ⌜Fshift (beta:=beta) radix (Int.natAbs (y.Fexp - x.Fexp)) y = x⌝⦄ := by
  intro ⟨hbeta_radix, hradix, hxy, hexp_le⟩
  simp only [wp, PostCond.noThrow, pure, FshiftCorrectInv_check,
    Id.run, ULift.up_down]
  have hshift_val :
      _root_.F2R (Fshift (beta:=beta) radix (Int.natAbs (y.Fexp - x.Fexp)) y) =
        _root_.F2R y :=
    (FshiftCorrect (beta:=beta) radix (Int.natAbs (y.Fexp - x.Fexp)) y)
      ⟨hbeta_radix, hradix⟩
  have hval :
      _root_.F2R (Fshift (beta:=beta) radix (Int.natAbs (y.Fexp - x.Fexp)) y) =
        _root_.F2R x :=
    hshift_val.trans hxy.symm
  have hbeta : 1 < beta := by omega
  have hdiff_nonneg : 0 ≤ y.Fexp - x.Fexp := by omega
  have hn :
      ((Int.natAbs (y.Fexp - x.Fexp) : Nat) : Int) = y.Fexp - x.Fexp :=
    Int.natAbs_of_nonneg hdiff_nonneg
  have hexp :
      (Fshift (beta:=beta) radix (Int.natAbs (y.Fexp - x.Fexp)) y).Fexp =
        x.Fexp := by
    simp only [Fshift, FloatSpec.Core.Defs.FlocqFloat.Fexp]
    rw [hn]
    omega
  exact
    (sameExpEq (beta := beta)
      (Fshift (beta:=beta) radix (Int.natAbs (y.Fexp - x.Fexp)) y) x)
      ⟨hval, hexp, hbeta⟩

-- Coq: `FshiftO` — shifting by 0 is identity
noncomputable def FshiftO_check {beta : Int} [ValidRadix beta]
    (radix : Int) (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem FshiftO {beta : Int} [ValidRadix beta]
    (radix : Int) (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜True⌝⦄
    (pure (FshiftO_check (beta:=beta) radix x) : Id Unit)
    ⦃⇓_ => ⌜Fshift (beta:=beta) radix 0 x = x ⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, FshiftO_check, Id.run,
    ULift.up_down]
  show Fshift radix 0 x = x
  simp [Fshift]

-- Coq: `FshiftCorrectSym` — equal reals imply some shifts match
noncomputable def FshiftCorrectSym_check {beta : Int} [ValidRadix beta]
    (radix : Int) (x y : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem FshiftCorrectSym {beta : Int} [ValidRadix beta]
    (radix : Int) (x y : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ _root_.F2R x = _root_.F2R y⌝⦄
    (pure (FshiftCorrectSym_check (beta:=beta) radix x y) : Id Unit)
    ⦃⇓_ => ⌜∃ n m : Nat, Fshift (beta:=beta) radix n x = Fshift (beta:=beta) radix m y⌝⦄ := by
  intro ⟨hbeta_radix, hradix, hxy⟩
  simp [wp, PostCond.noThrow, pure, FshiftCorrectSym_check]
  rcases le_or_gt x.Fexp y.Fexp with hle | hgt
  · refine ⟨0, Int.natAbs (y.Fexp - x.Fexp), ?_⟩
    have hx0 : Fshift (beta:=beta) radix 0 x = x :=
      (FshiftO (beta:=beta) radix x) trivial
    have hy_shift :
        Fshift (beta:=beta) radix (Int.natAbs (y.Fexp - x.Fexp)) y = x :=
      (FshiftCorrectInv (beta:=beta) radix x y) ⟨hbeta_radix, hradix, hxy, hle⟩
    rw [hx0]
    exact hy_shift.symm
  · refine ⟨Int.natAbs (x.Fexp - y.Fexp), 0, ?_⟩
    have hy0 : Fshift (beta:=beta) radix 0 y = y :=
      (FshiftO (beta:=beta) radix y) trivial
    have hx_shift :
        Fshift (beta:=beta) radix (Int.natAbs (x.Fexp - y.Fexp)) x = y :=
      (FshiftCorrectInv (beta:=beta) radix y x)
        ⟨hbeta_radix, hradix, hxy.symm, le_of_lt hgt⟩
    rw [hy0]
    exact hx_shift

-- Coq: `FdigitEq` — if not zero and same real/digit, floats are equal
noncomputable def FdigitEq_check {beta : Int} [ValidRadix beta]
    (radix : Int) (x y : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem FdigitEq {beta : Int} [ValidRadix beta]
    (radix : Int) (x y : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ ¬ is_Fzero x ∧ _root_.F2R x = _root_.F2R y ∧
        Fdigit (beta:=beta) radix x = Fdigit (beta:=beta) radix y⌝⦄
    (pure (FdigitEq_check (beta:=beta) radix x y) : Id Unit)
    ⦃⇓_ => ⌜x = y⌝⦄ := by
  intro ⟨hbeta_radix, hradix, hx_nonzero, hxy, hdigit⟩
  simp only [wp, PostCond.noThrow, pure, FdigitEq_check, Id.run,
    ULift.up_down]
  have hbeta : 1 < beta := by omega
  have hy_nonzero : ¬ is_Fzero y :=
    (NisFzeroComp (beta:=beta) x y) ⟨hbeta, hx_nonzero, hxy⟩
  rcases lt_trichotomy x.Fexp y.Fexp with hlt | heq | hgt
  · have hle : x.Fexp ≤ y.Fexp := le_of_lt hlt
    have n_pos :
        0 < Int.natAbs (y.Fexp - x.Fexp) := by
      have hdiff_nonneg : 0 ≤ y.Fexp - x.Fexp := by omega
      have hn :
          ((Int.natAbs (y.Fexp - x.Fexp) : Nat) : Int) = y.Fexp - x.Fexp :=
        Int.natAbs_of_nonneg hdiff_nonneg
      have hn_int : (0 : Int) < (Int.natAbs (y.Fexp - x.Fexp) : Int) := by
        rw [hn]
        omega
      exact_mod_cast hn_int
    have hshift_eq :
        Fshift (beta:=beta) radix (Int.natAbs (y.Fexp - x.Fexp)) y = x :=
      (FshiftCorrectInv (beta:=beta) radix x y) ⟨hbeta_radix, hradix, hxy, hle⟩
    have hfd_shift :
        Fdigit (beta:=beta) radix (Fshift (beta:=beta) radix
            (Int.natAbs (y.Fexp - x.Fexp)) y) =
          Fdigit (beta:=beta) radix y + Int.natAbs (y.Fexp - x.Fexp) :=
      (FshiftFdigit (beta:=beta) radix (Int.natAbs (y.Fexp - x.Fexp)) y)
        ⟨hradix, hy_nonzero⟩
    rw [hshift_eq] at hfd_shift
    have hsame :
        Fdigit (beta:=beta) radix y =
          Fdigit (beta:=beta) radix y + Int.natAbs (y.Fexp - x.Fexp) :=
      hdigit.symm.trans hfd_shift
    have hlt_nat :
        Fdigit (beta:=beta) radix y <
          Fdigit (beta:=beta) radix y + Int.natAbs (y.Fexp - x.Fexp) :=
      Nat.lt_add_of_pos_right n_pos
    rw [← hsame] at hlt_nat
    exact False.elim ((Nat.lt_irrefl (Fdigit (beta:=beta) radix y)) hlt_nat)
  · exact (sameExpEq (beta:=beta) x y) ⟨hxy, heq, hbeta⟩
  · have hle : y.Fexp ≤ x.Fexp := le_of_lt hgt
    have n_pos :
        0 < Int.natAbs (x.Fexp - y.Fexp) := by
      have hdiff_nonneg : 0 ≤ x.Fexp - y.Fexp := by omega
      have hn :
          ((Int.natAbs (x.Fexp - y.Fexp) : Nat) : Int) = x.Fexp - y.Fexp :=
        Int.natAbs_of_nonneg hdiff_nonneg
      have hn_int : (0 : Int) < (Int.natAbs (x.Fexp - y.Fexp) : Int) := by
        rw [hn]
        omega
      exact_mod_cast hn_int
    have hshift_eq :
        Fshift (beta:=beta) radix (Int.natAbs (x.Fexp - y.Fexp)) x = y :=
      (FshiftCorrectInv (beta:=beta) radix y x) ⟨hbeta_radix, hradix, hxy.symm, hle⟩
    have hfd_shift :
        Fdigit (beta:=beta) radix (Fshift (beta:=beta) radix
            (Int.natAbs (x.Fexp - y.Fexp)) x) =
          Fdigit (beta:=beta) radix x + Int.natAbs (x.Fexp - y.Fexp) :=
      (FshiftFdigit (beta:=beta) radix (Int.natAbs (x.Fexp - y.Fexp)) x)
        ⟨hradix, hx_nonzero⟩
    rw [hshift_eq] at hfd_shift
    have hsame :
        Fdigit (beta:=beta) radix x =
          Fdigit (beta:=beta) radix x + Int.natAbs (x.Fexp - y.Fexp) :=
      hdigit.trans hfd_shift
    have hlt_nat :
        Fdigit (beta:=beta) radix x <
          Fdigit (beta:=beta) radix x + Int.natAbs (x.Fexp - y.Fexp) :=
      Nat.lt_add_of_pos_right n_pos
    rw [← hsame] at hlt_nat
    exact False.elim ((Nat.lt_irrefl (Fdigit (beta:=beta) radix x)) hlt_nat)

/-- Coq: `FnormalUnique` — normal floats that agree as reals are equal. -/
theorem FnormalUnique {beta : Int} [ValidRadix beta]
    (radix : Int) (b : Fbound_skel) (precision : Nat)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜Fnormal (beta:=beta) radix b p ∧
        Fnormal' (beta:=beta) radix b p ∧
        Fnormal (beta:=beta) radix b q ∧
        Fnormal' (beta:=beta) radix b q ∧
        precision ≠ 0 ∧ beta = radix ∧ 1 < radix ∧
        b.vNum = Zpower_nat radix precision ∧
        _root_.F2R (beta:=beta) p = _root_.F2R (beta:=beta) q⌝⦄
    (pure (FnormalUnique_check (beta:=beta) radix b p q) : Id Unit)
    ⦃⇓_ => ⌜p = q⌝⦄ := by
  intro h
  rcases h with
    ⟨hnormal_p, hnormal'_p, hnormal_q, hnormal'_q, hprecision, hbeta_radix,
      hradix, hvNum, hval⟩
  simp only [wp, PostCond.noThrow, pure, FnormalUnique_check,
    Id.run, ULift.up_down]
  have hvNum_pos : 0 < b.vNum := by
    rw [hvNum, Zpower_nat]
    exact pow_pos (by omega : 0 < radix) precision
  have hp_nonzero : ¬ is_Fzero p :=
    (FnormalNotZero_from_expanded_normal_payload (beta:=beta) b radix p radix hvNum_pos hnormal'_p.2)
      hnormal_p
  have hp_digit :
      Fdigit (beta:=beta) radix p = precision :=
    (FnormalPrecision_internal (beta:=beta) radix b precision p)
      ⟨hnormal_p, hnormal'_p, hprecision, hradix, hvNum⟩
  have hq_digit :
      Fdigit (beta:=beta) radix q = precision :=
    (FnormalPrecision_internal (beta:=beta) radix b precision q)
      ⟨hnormal_q, hnormal'_q, hprecision, hradix, hvNum⟩
  exact
    (FdigitEq (beta:=beta) radix p q)
      ⟨hbeta_radix, hradix, hp_nonzero, hval, hp_digit.trans hq_digit.symm⟩

-- Least significant bit position of a float.
noncomputable def LSB {beta : Int} [ValidRadix beta]
    (radix : Int) (x : FloatSpec.Core.Defs.FlocqFloat beta) : Int :=
  Int.ofNat (maxDiv radix x.Fnum (Fdigit (beta:=beta) radix x)) + x.Fexp

-- Coq: `Fexp_le_LSB` — the encoded exponent is below the least significant bit.
noncomputable def Fexp_le_LSB_check {beta : Int} [ValidRadix beta]
    (radix : Int) (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem Fexp_le_LSB {beta : Int} [ValidRadix beta]
    (radix : Int) (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜True⌝⦄
    (pure (Fexp_le_LSB_check (beta:=beta) radix x) : Id Unit)
    ⦃⇓_ => ⌜x.Fexp ≤ LSB (beta:=beta) radix x⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, Fexp_le_LSB_check, Id.run,
    ULift.up_down]
  unfold LSB
  have hnonneg :
      (0 : Int) ≤
        Int.ofNat (maxDiv radix x.Fnum (Fdigit (beta:=beta) radix x)) :=
    Int.natCast_nonneg _
  simpa [add_comm, add_left_comm, add_assoc] using
    add_le_add_right hnonneg x.Fexp

/-- Coq: `Ulp_Le_LSigB` — the unit at the encoded exponent is below the
unit at the least significant bit position. -/
theorem Ulp_Le_LSigB {beta : Int} [ValidRadix beta]
    (radix : Int) (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    beta = radix →
    1 < radix →
    _root_.F2R (beta:=beta) (⟨1, x.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat beta) ≤
      _root_.F2R (beta:=beta)
        (⟨1, LSB (beta:=beta) radix x⟩ : FloatSpec.Core.Defs.FlocqFloat beta) := by
  intro hbeta_radix hradix
  have hbeta : 1 < beta := by omega
  have hle : x.Fexp ≤ LSB (beta:=beta) radix x :=
    (Fexp_le_LSB (beta:=beta) radix x) trivial
  exact (oneExp_le (beta:=beta) x.Fexp (LSB (beta:=beta) radix x))
    ⟨hbeta, hle⟩

/-- Coq: `FUlp_Le_LSigB` — a bounded float's normalized ulp is no larger
than the radix unit at its least significant bit. -/
theorem FUlp_Le_LSigB {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    beta = radix →
    1 < radix →
    Fbounded (beta:=beta) b x →
    Fulp (beta:=beta) b radix precision x ≤
      _root_.F2R (beta:=beta)
        (⟨1, LSB (beta:=beta) radix x⟩ : FloatSpec.Core.Defs.FlocqFloat beta) := by
  intro hbeta_radix hradix hx
  have hFulp_le :
      Fulp (beta:=beta) b radix precision x ≤ (beta : ℝ) ^ x.Fexp := by
    exact (FulpLe (beta:=beta) radix b precision x) ⟨hx, hbeta_radix, hradix⟩
  have hUlp_le :
      _root_.F2R (beta:=beta)
          (⟨1, x.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat beta) ≤
        _root_.F2R (beta:=beta)
          (⟨1, LSB (beta:=beta) radix x⟩ :
            FloatSpec.Core.Defs.FlocqFloat beta) :=
    Ulp_Le_LSigB (beta:=beta) radix x hbeta_radix hradix
  have hpow_eq :
      (beta : ℝ) ^ x.Fexp =
        _root_.F2R (beta:=beta)
          (⟨1, x.Fexp⟩ : FloatSpec.Core.Defs.FlocqFloat beta) := by
    simp [_root_.F2R, FloatSpec.Core.Defs.F2R]
  exact le_trans (by simpa [hpow_eq] using hFulp_le) hUlp_le

-- Coq: `LSB_shift` — ~is_Fzero x -> LSB x = LSB (Fshift n x)
noncomputable def LSB_shift_check {beta : Int} [ValidRadix beta]
    (radix : Int) (x : FloatSpec.Core.Defs.FlocqFloat beta) (n : Nat) : Unit :=
  ()

-- Coq: `maxDivLess` — maxDiv v p ≤ p
noncomputable def maxDivLess_check (radix : Int) (v : Int) (p : Nat) : Unit :=
  ()

theorem maxDivLess (radix : Int) (v : Int) (p : Nat) :
    ⦃⌜True⌝⦄
    (pure (maxDivLess_check radix v p) : Id Unit)
    ⦃⇓_ => ⌜maxDiv radix v p ≤ p⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, maxDivLess_check, Id.run,
    ULift.up_down]
  show maxDiv radix v p ≤ p
  classical
  induction p with
  | zero => simp [maxDiv]
  | succ p ih =>
      by_cases h : Zdivides v (Zpower_nat radix (Nat.succ p))
      · simp [maxDiv, h]
      · simpa [maxDiv, h] using Nat.le_trans ih (Nat.le_succ p)

-- Coq: `LSB_comp` — ~is_Fzero x → x = y :>R → LSB x = LSB y
noncomputable def LSB_comp_check {beta : Int} [ValidRadix beta]
    (radix : Int)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) (n : Nat) : Unit :=
  ()

-- Coq: `maxDivCorrect` — Zdivides v (radix^maxDiv v p)
noncomputable def maxDivCorrect_check (radix : Int) (v : Int) (p : Nat) : Unit :=
  ()

/-- Coq: `maxDivCorrect` — for any integer `v` and natural `p`,
`v` divides `radix^(maxDiv v p)`. We only state the property here. -/
theorem maxDivCorrect (radix : Int) (v : Int) (p : Nat) :
    ⦃⌜True⌝⦄
    (pure (maxDivCorrect_check radix v p) : Id Unit)
    ⦃⇓_ => ⌜Zdivides v (Zpower_nat radix (maxDiv radix v p))⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, maxDivCorrect_check, Id.run,
    ULift.up_down]
  show Zdivides v (Zpower_nat radix (maxDiv radix v p))
  classical
  induction p with
  | zero =>
      simp [maxDiv, Zpower_nat, Zdivides]
  | succ p ih =>
      by_cases h : Zdivides v (Zpower_nat radix (Nat.succ p))
      · simpa [maxDiv, h] using h
      · simpa [maxDiv, h] using ih

-- Coq: `LSB_rep_min` — every float has an equivalent representation at `LSB`.
noncomputable def LSB_rep_min_check {beta : Int} [ValidRadix beta]
    (radix : Int) (p : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem LSB_rep_min {beta : Int} [ValidRadix beta]
    (radix : Int) (p : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix⌝⦄
    (pure (LSB_rep_min_check (beta:=beta) radix p) : Id Unit)
    ⦃⇓_ => ⌜∃ z : Int,
      _root_.F2R (beta:=beta) p =
        _root_.F2R (beta:=beta)
          (⟨z, LSB (beta:=beta) radix p⟩ : FloatSpec.Core.Defs.FlocqFloat beta)⌝⦄ := by
  intro ⟨hbeta_radix, hradix⟩
  simp only [wp, PostCond.noThrow, pure, LSB_rep_min_check, Id.run,
    ULift.up_down]
  let md : Nat := maxDiv radix p.Fnum (Fdigit (beta:=beta) radix p)
  let powi : Int := Zpower_nat radix md
  let z : Int := Zquotient p.Fnum powi
  refine ⟨z, ?_⟩
  have hradix_pos_int : 0 < radix := by omega
  have hradix_ne_real : (radix : ℝ) ≠ 0 := by exact_mod_cast (ne_of_gt hradix_pos_int)
  have hpowi_ne : powi ≠ 0 := by
    unfold powi Zpower_nat
    exact pow_ne_zero md (ne_of_gt hradix_pos_int)
  have hdivides : Zdivides p.Fnum powi := by
    simpa [powi, md] using
      (maxDivCorrect radix p.Fnum (Fdigit (beta:=beta) radix p)) trivial
  have hnum : p.Fnum = z * powi := by
    simpa [z] using (ZdividesZquotient p.Fnum powi) ⟨hpowi_ne, hdivides⟩
  have hpow_real :
      (powi : ℝ) = (radix : ℝ) ^ (md : Int) := by
    simpa [powi] using (Zpower_nat_Z_powerRZ radix md) trivial
  have hlsb : LSB (beta:=beta) radix p = (md : Int) + p.Fexp := by
    simp [LSB, md]
  calc
    _root_.F2R (beta:=beta) p
        = (p.Fnum : ℝ) * (radix : ℝ) ^ p.Fexp := by
          simp [_root_.F2R, FloatSpec.Core.Defs.F2R, hbeta_radix]
    _ = ((z : ℝ) * (powi : ℝ)) * (radix : ℝ) ^ p.Fexp := by
          rw [hnum]
          simp [Int.cast_mul]
    _ = ((z : ℝ) * (radix : ℝ) ^ (md : Int)) * (radix : ℝ) ^ p.Fexp := by
          rw [hpow_real]
    _ = (z : ℝ) * ((radix : ℝ) ^ (md : Int) * (radix : ℝ) ^ p.Fexp) := by
          ring
    _ = (z : ℝ) * (radix : ℝ) ^ ((md : Int) + p.Fexp) := by
          rw [← zpow_add₀ hradix_ne_real (md : Int) p.Fexp]
    _ = _root_.F2R (beta:=beta)
          (⟨z, LSB (beta:=beta) radix p⟩ : FloatSpec.Core.Defs.FlocqFloat beta) := by
          simp [_root_.F2R, FloatSpec.Core.Defs.F2R, hbeta_radix, hlsb]

-- Coq: `maxDivLt` — ~Zdivides v (radix^p) → maxDiv v p < p
noncomputable def maxDivLt_check (radix : Int) (v : Int) (p : Nat) : Unit :=
  ()

/-- Coq: `maxDivLt` — if `v` does not divide `radix^p` then the maximal
exponent `maxDiv v p` is strictly less than `p`. Statement only. -/
theorem maxDivLt (radix : Int) (v : Int) (p : Nat) :
    ⦃⌜¬ Zdivides v (Zpower_nat radix p)⌝⦄
    (pure (maxDivLt_check radix v p) : Id Unit)
    ⦃⇓_ => ⌜maxDiv radix v p < p⌝⦄ := by
  intro h
  simp only [wp, PostCond.noThrow, pure, maxDivLt_check, Id.run,
    ULift.up_down]
  show maxDiv radix v p < p
  classical
  cases p with
  | zero =>
      exact False.elim (h ⟨v, by simp [Zpower_nat]⟩)
  | succ p =>
      by_cases hdiv : Zdivides v (Zpower_nat radix (Nat.succ p))
      · exact False.elim (h hdiv)
      · have hle := (maxDivLess radix v p) trivial
        simpa [maxDiv, hdiv] using Nat.lt_succ_of_le hle

-- Coq: `maxDiv_opp` — maxDiv v p = maxDiv (-v) p
noncomputable def maxDiv_opp_check (radix : Int) (v : Int) (p : Nat) : Unit :=
  ()

private lemma Zdivides_neg_iff (v m : Int) : Zdivides (-v) m ↔ Zdivides v m := by
  constructor
  · intro ⟨q, hq⟩
    refine ⟨-q, ?_⟩
    calc
      v = -(-v) := by ring
      _ = -(m * q) := by rw [hq]
      _ = m * -q := by ring
  · intro ⟨q, hq⟩
    refine ⟨-q, ?_⟩
    calc
      -v = -(m * q) := by rw [hq]
      _ = m * -q := by ring

theorem maxDiv_opp (radix : Int) (v : Int) (p : Nat) :
    ⦃⌜True⌝⦄
    (pure (maxDiv_opp_check radix v p) : Id Unit)
    ⦃⇓_ => ⌜maxDiv radix v p = maxDiv radix (-v) p⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, maxDiv_opp_check, Id.run,
    ULift.up_down]
  show maxDiv radix v p = maxDiv radix (-v) p
  classical
  induction p with
  | zero => simp [maxDiv]
  | succ p ih =>
      by_cases h : Zdivides v (Zpower_nat radix (Nat.succ p))
      · have hneg : Zdivides (-v) (Zpower_nat radix (Nat.succ p)) :=
          (Zdivides_neg_iff v (Zpower_nat radix (Nat.succ p))).2 h
        simp [maxDiv, h, hneg]
      · have hneg : ¬ Zdivides (-v) (Zpower_nat radix (Nat.succ p)) := by
          intro hn
          exact h ((Zdivides_neg_iff v (Zpower_nat radix (Nat.succ p))).1 hn)
        simp [maxDiv, h, hneg, ih]

-- Coq: `LSB_opp` — LSB x = LSB (Fopp x)
noncomputable def LSB_opp_check {beta : Int} [ValidRadix beta]
    (radix : Int) (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem LSB_opp {beta : Int} [ValidRadix beta]
    (radix : Int) (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜True⌝⦄
    (pure (LSB_opp_check (beta:=beta) radix x) : Id Unit)
    ⦃⇓_ => ⌜LSB (beta:=beta) radix x = LSB (beta:=beta) radix (Fopp x)⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, LSB_opp_check, Id.run,
    ULift.up_down]
  show LSB radix x = LSB radix (Fopp x)
  cases x with
  | mk m e =>
      change Int.ofNat (maxDiv radix m (digit radix m)) + e =
        Int.ofNat (maxDiv radix (-m) (digit radix (-m))) + e
      rw [digit_neg radix m]
      have hmax := (maxDiv_opp radix m (digit radix m)) trivial
      rw [hmax]

-- Coq: `maxDiv_abs` — maxDiv v p = maxDiv (|v|) p
noncomputable def maxDiv_abs_check (radix : Int) (v : Int) (p : Nat) : Unit :=
  ()

private lemma Zdivides_abs_iff (v m : Int) : Zdivides (|v|) m ↔ Zdivides v m := by
  rcases le_or_gt 0 v with hv | hv
  · have habs : |v| = v := abs_of_nonneg hv
    simp [habs]
  · have habs : |v| = -v := abs_of_neg hv
    simp [habs, Zdivides_neg_iff]

theorem maxDiv_abs (radix : Int) (v : Int) (p : Nat) :
    ⦃⌜True⌝⦄
    (pure (maxDiv_abs_check radix v p) : Id Unit)
    ⦃⇓_ => ⌜maxDiv radix v p = maxDiv radix |v| p⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, maxDiv_abs_check, Id.run,
    ULift.up_down]
  show maxDiv radix v p = maxDiv radix |v| p
  classical
  induction p with
  | zero => simp [maxDiv]
  | succ p ih =>
      by_cases h : Zdivides v (Zpower_nat radix (Nat.succ p))
      · have habs : Zdivides (|v|) (Zpower_nat radix (Nat.succ p)) :=
          (Zdivides_abs_iff v (Zpower_nat radix (Nat.succ p))).2 h
        simp [maxDiv, h, habs]
      · have habs : ¬ Zdivides (|v|) (Zpower_nat radix (Nat.succ p)) := by
          intro ha
          exact h ((Zdivides_abs_iff v (Zpower_nat radix (Nat.succ p))).1 ha)
        simp [maxDiv, h, habs, ih]

-- Coq: `LSB_abs` — LSB x = LSB (Fabs x)
noncomputable def LSB_abs_check {beta : Int} [ValidRadix beta]
    (radix : Int) (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem LSB_abs {beta : Int} [ValidRadix beta]
    (radix : Int) (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜True⌝⦄
    (pure (LSB_abs_check (beta:=beta) radix x) : Id Unit)
    ⦃⇓_ => ⌜LSB (beta:=beta) radix x = LSB (beta:=beta) radix (Fabs x)⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, LSB_abs_check, Id.run,
    ULift.up_down]
  show LSB radix x = LSB radix (Fabs x)
  cases x with
  | mk m e =>
      simp only [LSB, Fdigit, Fabs, FloatSpec.Calc.Operations.Fabs,
        FloatSpec.Core.Defs.FlocqFloat.Fnum, FloatSpec.Core.Defs.FlocqFloat.Fexp]
      change Int.ofNat (maxDiv radix m (digit radix m)) + e =
        Int.ofNat (maxDiv radix (m.natAbs : Int) (digit radix (m.natAbs : Int))) + e
      have heq_abs : (|m| : Int) = (m.natAbs : Int) := by
        rw [Int.abs_eq_natAbs]
      rw [← heq_abs]
      rw [digit_abs_eq radix m]
      have hmax := (maxDiv_abs radix m (digit radix m)) trivial
      rw [hmax]

-- Most significant bit position of a float.
noncomputable def MSB {beta : Int} [ValidRadix beta]
    (radix : Int) (x : FloatSpec.Core.Defs.FlocqFloat beta) : Int :=
  Int.pred (Int.ofNat (Fdigit (beta:=beta) radix x) + x.Fexp)

-- Coq: `MSB_shift` — ~is_Fzero x -> MSB x = MSB (Fshift n x)
noncomputable def MSB_shift_check {beta : Int} [ValidRadix beta]
    (radix : Int) (x : FloatSpec.Core.Defs.FlocqFloat beta) (n : Nat) : Unit :=
  ()

theorem MSB_shift {beta : Int} [ValidRadix beta]
    (radix : Int) (x : FloatSpec.Core.Defs.FlocqFloat beta) (n : Nat) :
    ⦃⌜1 < radix ∧ ¬ is_Fzero x⌝⦄
    (pure (MSB_shift_check (beta:=beta) radix x n) : Id Unit)
    ⦃⇓_ => ⌜MSB (beta:=beta) radix x = MSB (beta:=beta) radix (Fshift (beta:=beta) radix n x)⌝⦄ := by
  intro ⟨hradix, hx_nonzero⟩
  simp only [wp, PostCond.noThrow, pure, MSB_shift_check, Id.run,
    ULift.up_down]
  show MSB radix x = MSB radix (Fshift radix n x)
  unfold MSB
  have hfd :
      Fdigit (beta:=beta) radix (Fshift (beta:=beta) radix n x) =
        Fdigit (beta:=beta) radix x + n :=
    (FshiftFdigit (beta:=beta) radix n x) ⟨hradix, hx_nonzero⟩
  rw [hfd]
  simp only [Fshift, FloatSpec.Core.Defs.FlocqFloat.Fexp]
  congr 1
  have hcast :
      Int.ofNat (Fdigit (beta:=beta) radix x + n) =
        Int.ofNat (Fdigit (beta:=beta) radix x) + (n : Int) := by
    simpa using
      (Nat.cast_add (Fdigit (beta:=beta) radix x) n :
        ((Fdigit (beta:=beta) radix x + n : Nat) : Int) =
          (Fdigit (beta:=beta) radix x : Int) + (n : Int))
  rw [hcast]
  omega

-- Coq: `MSB_comp` — ~is_Fzero x → x = y :>R → MSB x = MSB y
noncomputable def MSB_comp_check {beta : Int} [ValidRadix beta]
    (radix : Int)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) (n : Nat) : Unit :=
  ()

theorem MSB_comp {beta : Int} [ValidRadix beta]
    (radix : Int)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) (n : Nat) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ ¬ is_Fzero x ∧ _root_.F2R x = _root_.F2R y⌝⦄
    (pure (MSB_comp_check (beta:=beta) radix x y n) : Id Unit)
    ⦃⇓_ => ⌜MSB (beta:=beta) radix x = MSB (beta:=beta) radix y⌝⦄ := by
  intro ⟨hbeta_radix, hradix, hx_nonzero, hxy⟩
  simp only [wp, PostCond.noThrow, pure, MSB_comp_check, Id.run,
    ULift.up_down]
  have hbeta : 1 < beta := by omega
  have hy_nonzero : ¬ is_Fzero y :=
    (NisFzeroComp (beta:=beta) x y) ⟨hbeta, hx_nonzero, hxy⟩
  rcases (FshiftCorrectSym (beta:=beta) radix x y)
      ⟨hbeta_radix, hradix, hxy⟩ with ⟨nx, ny, hshift⟩
  have hx_msb :
      MSB (beta:=beta) radix x =
        MSB (beta:=beta) radix (Fshift (beta:=beta) radix nx x) :=
    (MSB_shift (beta:=beta) radix x nx) ⟨hradix, hx_nonzero⟩
  have hy_msb :
      MSB (beta:=beta) radix y =
        MSB (beta:=beta) radix (Fshift (beta:=beta) radix ny y) :=
    (MSB_shift (beta:=beta) radix y ny) ⟨hradix, hy_nonzero⟩
  rw [hx_msb, hshift, ← hy_msb]
  rfl

-- Coq: `MSB_opp` — MSB x = MSB (Fopp x)
noncomputable def MSB_opp_check {beta : Int} [ValidRadix beta]
    (radix : Int) (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem MSB_opp {beta : Int} [ValidRadix beta]
    (radix : Int) (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜True⌝⦄
    (pure (MSB_opp_check (beta:=beta) radix x) : Id Unit)
    ⦃⇓_ => ⌜MSB (beta:=beta) radix x = MSB (beta:=beta) radix (Fopp x)⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, MSB_opp_check, Id.run,
    ULift.up_down]
  show MSB radix x = MSB radix (Fopp x)
  cases x with
  | mk m e =>
    change Int.pred (Int.ofNat (digit radix m) + e) =
      Int.pred (Int.ofNat (digit radix (-m)) + e)
    rw [digit_neg radix m]

-- Coq: `MSB_abs` — MSB x = MSB (Fabs x)
noncomputable def MSB_abs_check {beta : Int} [ValidRadix beta]
    (radix : Int) (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem MSB_abs {beta : Int} [ValidRadix beta]
    (radix : Int) (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜True⌝⦄
    (pure (MSB_abs_check (beta:=beta) radix x) : Id Unit)
    ⦃⇓_ => ⌜MSB (beta:=beta) radix x = MSB (beta:=beta) radix (Fabs x)⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, MSB_abs_check, Id.run,
    ULift.up_down]
  show MSB radix x = MSB radix (Fabs x)
  cases x with
  | mk m e =>
    change Int.pred (Int.ofNat (digit radix m) + e) =
      Int.pred (Int.ofNat (digit radix (m.natAbs : Int)) + e)
    have heq_abs : (|m| : Int) = (m.natAbs : Int) := by
      rw [Int.abs_eq_natAbs]
    rw [← heq_abs]
    rw [digit_abs_eq radix m]

-- Coq: `LSB_le_MSB` — for nonzero floats, least ≤ most significant bit
noncomputable def LSB_le_MSB_check {beta : Int} [ValidRadix beta]
    (radix : Int) (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem LSB_le_MSB {beta : Int} [ValidRadix beta]
    (radix : Int) (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜1 < radix ∧ ¬ is_Fzero x⌝⦄
    (pure (LSB_le_MSB_check (beta:=beta) radix x) : Id Unit)
    ⦃⇓_ => ⌜LSB (beta:=beta) radix x ≤ MSB (beta:=beta) radix x⌝⦄ := by
  intro ⟨hradix, hx_nonzero⟩
  simp only [wp, PostCond.noThrow, pure, LSB_le_MSB_check, Id.run,
    ULift.up_down]
  have hxnum : x.Fnum ≠ 0 := by
    simpa [is_Fzero] using hx_nonzero
  have hnot :
      ¬ Zdivides x.Fnum (Zpower_nat radix (Fdigit (beta:=beta) radix x)) := by
    have h := (NotDividesDigit radix x.Fnum) ⟨hradix, hxnum⟩
    simpa [wp, PostCond.noThrow, pure, NotDividesDigit_check,
      Id.run, ULift.up_down, Fdigit, digit] using h
  have hlt :
      maxDiv radix x.Fnum (Fdigit (beta:=beta) radix x) <
        Fdigit (beta:=beta) radix x :=
    (maxDivLt radix x.Fnum (Fdigit (beta:=beta) radix x)) hnot
  unfold LSB MSB
  simp only [Int.pred]
  have hsucc :
      maxDiv radix x.Fnum (Fdigit (beta:=beta) radix x) + 1 ≤
        Fdigit (beta:=beta) radix x :=
    Nat.succ_le_iff.mpr hlt
  have hsucc_int :
      (Int.ofNat (maxDiv radix x.Fnum (Fdigit (beta:=beta) radix x)) : Int) + 1 ≤
        Int.ofNat (Fdigit (beta:=beta) radix x) := by
    have hcast :
        ((maxDiv radix x.Fnum (Fdigit (beta:=beta) radix x) + 1 : Nat) : Int) ≤
          ((Fdigit (beta:=beta) radix x : Nat) : Int) := by
      omega
    simpa [Nat.cast_add] using hcast
  change
    Int.ofNat (maxDiv radix x.Fnum (Fdigit (beta:=beta) radix x)) + x.Fexp ≤
      Int.ofNat (Fdigit (beta:=beta) radix x) + x.Fexp - 1
  linarith

-- Coq: `Zlt_mult_simpl_l` — cancel positive multiplier on left for <
noncomputable def Zlt_mult_simpl_l_check (a b c : Int) : Unit :=
  ()

theorem Zlt_mult_simpl_l (a b c : Int) :
    ⦃⌜0 < c ∧ c * a < c * b⌝⦄
    (pure (Zlt_mult_simpl_l_check a b c) : Id Unit)
    ⦃⇓_ => ⌜a < b⌝⦄ := by
  intro ⟨hc, hab⟩
  simp only [wp, PostCond.noThrow, pure, Zlt_mult_simpl_l_check, Id.run,
    ULift.up_down]
  show a < b
  exact lt_of_mul_lt_mul_left hab (le_of_lt hc)

-- Coq: `Z_eq_bool_correct` — boolean equality correctness for Int
noncomputable def Z_eq_bool (p q : Int) : Bool := decide (p = q)

noncomputable def Z_eq_bool_correct_check (p q : Int) : Unit :=
  ()

theorem Z_eq_bool_correct (p q : Int) :
    ⦃⌜True⌝⦄
    (pure (Z_eq_bool_correct_check p q) : Id Unit)
    ⦃⇓_ => ⌜(if Z_eq_bool p q then p = q else p ≠ q)⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, Z_eq_bool_correct_check, Id.run,
    ULift.up_down]
  show if Z_eq_bool p q then p = q else p ≠ q
  simp [Z_eq_bool]

-- Coq: `Zcompare_correct` — trichotomy via a comparison function
noncomputable def Zcompare (p q : Int) : Ordering :=
  if p < q then Ordering.lt else if p = q then Ordering.eq else Ordering.gt

noncomputable def Zcompare_correct_check (p q : Int) : Unit :=
  ()

theorem Zcompare_correct (p q : Int) :
    ⦃⌜True⌝⦄
    (pure (Zcompare_correct_check p q) : Id Unit)
    ⦃⇓_ => ⌜match Zcompare p q with
            | Ordering.gt => q < p
            | Ordering.lt => p < q
            | Ordering.eq => p = q⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, Zcompare_correct_check, Id.run,
    ULift.up_down]
  show match Zcompare p q with
       | Ordering.gt => q < p
       | Ordering.lt => p < q
       | Ordering.eq => p = q
  simp only [Zcompare]
  split_ifs with h1 h2
  · exact h1
  · exact h2
  · omega

-- Coq: `Zabs_Zopp` — | -z | = | z |
noncomputable def Zabs_Zopp_check (z : Int) : Unit :=
  ()

theorem Zabs_Zopp (z : Int) :
    ⦃⌜True⌝⦄
    (pure (Zabs_Zopp_check z) : Id Unit)
    ⦃⇓_ => ⌜|-z| = |z|⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, Zabs_Zopp_check, Id.run,
    ULift.up_down]
  show |-z| = |z|
  exact abs_neg z

-- Coq: `Zle_Zpred_Zpred` — predecessor is monotone
noncomputable def Zle_Zpred_Zpred_check (z1 z2 : Int) : Unit :=
  ()

theorem Zle_Zpred_Zpred (z1 z2 : Int) :
    ⦃⌜z1 ≤ z2⌝⦄
    (pure (Zle_Zpred_Zpred_check z1 z2) : Id Unit)
    ⦃⇓_ => ⌜Int.pred z1 ≤ Int.pred z2⌝⦄ := by
  intro h
  simp only [wp, PostCond.noThrow, pure, Zle_Zpred_Zpred_check, Id.run,
    ULift.up_down]
  show Int.pred z1 ≤ Int.pred z2
  have h' : z1 ≤ z2 := h
  simp only [Int.pred]; omega

-- Coq: `Zle_n_Zpred` — cancel pred on both sides for ≤
noncomputable def Zle_n_Zpred_check (z1 z2 : Int) : Unit :=
  ()

theorem Zle_n_Zpred (z1 z2 : Int) :
    ⦃⌜Int.pred z1 ≤ Int.pred z2⌝⦄
    (pure (Zle_n_Zpred_check z1 z2) : Id Unit)
    ⦃⇓_ => ⌜z1 ≤ z2⌝⦄ := by
  intro h
  simp only [wp, PostCond.noThrow, pure, Zle_n_Zpred_check, Id.run,
    ULift.up_down]
  show z1 ≤ z2
  have h' : Int.pred z1 ≤ Int.pred z2 := h
  simp only [Int.pred] at h'; omega

-- Coq: `Zlt_1_O` — 1 ≤ z → 0 < z
noncomputable def Zlt_1_O_check (z : Int) : Unit :=
  ()

theorem Zlt_1_O (z : Int) :
    ⦃⌜1 ≤ z⌝⦄
    (pure (Zlt_1_O_check z) : Id Unit)
    ⦃⇓_ => ⌜0 < z⌝⦄ := by
  intro h
  simp only [wp, PostCond.noThrow, pure, Zlt_1_O_check, Id.run,
    ULift.up_down]
  show 0 < z
  have h' : 1 ≤ z := h; omega

noncomputable def LtR0Fnum_check {beta : Int} [ValidRadix beta]
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem LtR0Fnum {beta : Int} [ValidRadix beta]
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜1 < beta ∧ 0 < _root_.F2R x⌝⦄
    (pure (LtR0Fnum_check (beta:=beta) x) : Id Unit)
    ⦃⇓_ => ⌜0 < x.Fnum⌝⦄ := by
  intro ⟨hβ, hx⟩
  simp only [wp, PostCond.noThrow, pure, LtR0Fnum_check, Id.run,
    ULift.up_down]
  exact FloatSpec.Core.Float_prop.gt_0_F2R (beta:=beta) x hβ hx

-- Coq: `LeR0Fnum` — 0 ≤ x → 0 ≤ Fnum x
noncomputable def LeR0Fnum_check {beta : Int} [ValidRadix beta]
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem LeR0Fnum {beta : Int} [ValidRadix beta]
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜1 < beta ∧ 0 ≤ _root_.F2R x⌝⦄
    (pure (LeR0Fnum_check (beta:=beta) x) : Id Unit)
    ⦃⇓_ => ⌜0 ≤ x.Fnum⌝⦄ := by
  intro ⟨hβ, hx⟩
  simp only [wp, PostCond.noThrow, pure, LeR0Fnum_check, Id.run,
    ULift.up_down]
  exact FloatSpec.Core.Float_prop.ge_0_F2R (beta:=beta) x hβ hx

-- Coq: `LeFnumZERO` — 0 ≤ Fnum x → 0 ≤ x
noncomputable def LeFnumZERO_check {beta : Int} [ValidRadix beta]
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem LeFnumZERO {beta : Int} [ValidRadix beta]
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜1 < beta ∧ 0 ≤ x.Fnum⌝⦄
    (pure (LeFnumZERO_check (beta:=beta) x) : Id Unit)
    ⦃⇓_ => ⌜0 ≤ _root_.F2R x⌝⦄ := by
  intro ⟨hβ, hx⟩
  simp only [wp, PostCond.noThrow, pure, LeFnumZERO_check, Id.run,
    ULift.up_down]
  exact FloatSpec.Core.Float_prop.F2R_ge_0 (beta:=beta) x hβ hx

-- Coq: `R0LtFnum` — x < 0 → Fnum x < 0
noncomputable def R0LtFnum_check {beta : Int} [ValidRadix beta]
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem R0LtFnum {beta : Int} [ValidRadix beta]
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜1 < beta ∧ _root_.F2R x < 0⌝⦄
    (pure (R0LtFnum_check (beta:=beta) x) : Id Unit)
    ⦃⇓_ => ⌜x.Fnum < 0⌝⦄ := by
  intro ⟨hβ, hx⟩
  simp only [wp, PostCond.noThrow, pure, R0LtFnum_check, Id.run,
    ULift.up_down]
  exact FloatSpec.Core.Float_prop.lt_0_F2R (beta:=beta) x hβ hx

-- Coq: `R0LeFnum` — x ≤ 0 → Fnum x ≤ 0
noncomputable def R0LeFnum_check {beta : Int} [ValidRadix beta]
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem R0LeFnum {beta : Int} [ValidRadix beta]
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜1 < beta ∧ _root_.F2R x ≤ 0⌝⦄
    (pure (R0LeFnum_check (beta:=beta) x) : Id Unit)
    ⦃⇓_ => ⌜x.Fnum ≤ 0⌝⦄ := by
  intro ⟨hβ, hx⟩
  simp only [wp, PostCond.noThrow, pure, R0LeFnum_check, Id.run,
    ULift.up_down]
  exact FloatSpec.Core.Float_prop.le_0_F2R (beta:=beta) x hβ hx

-- Coq: `LeZEROFnum` — Fnum x ≤ 0 → x ≤ 0
noncomputable def LeZEROFnum_check {beta : Int} [ValidRadix beta]
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem LeZEROFnum {beta : Int} [ValidRadix beta]
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜1 < beta ∧ x.Fnum ≤ 0⌝⦄
    (pure (LeZEROFnum_check (beta:=beta) x) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R x ≤ 0⌝⦄ := by
  intro ⟨hβ, hx⟩
  simp only [wp, PostCond.noThrow, pure, LeZEROFnum_check, Id.run,
    ULift.up_down]
  exact FloatSpec.Core.Float_prop.F2R_le_0 (beta:=beta) x hβ hx

-- Coq: `LtFnumZERO` — 0 < Fnum x → 0 < x
noncomputable def LtFnumZERO_check {beta : Int} [ValidRadix beta]
    (x : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem LtFnumZERO {beta : Int} [ValidRadix beta]
    (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜1 < beta ∧ 0 < x.Fnum⌝⦄
    (pure (LtFnumZERO_check (beta:=beta) x) : Id Unit)
    ⦃⇓_ => ⌜0 < _root_.F2R x⌝⦄ := by
  intro ⟨hβ, hx⟩
  simp only [wp, PostCond.noThrow, pure, LtFnumZERO_check, Id.run,
    ULift.up_down]
  exact FloatSpec.Core.Float_prop.F2R_gt_0 (beta:=beta) x hβ hx

-- Coq: `Zlt_Zabs_inv1` — |z1| < z2 → -z2 < z1
noncomputable def Zlt_Zabs_inv1_check (z1 z2 : Int) : Unit :=
  ()

theorem Zlt_Zabs_inv1 (z1 z2 : Int) :
    ⦃⌜|z1| < z2⌝⦄
    (pure (Zlt_Zabs_inv1_check z1 z2) : Id Unit)
    ⦃⇓_ => ⌜-z2 < z1⌝⦄ := by
  intro h
  simp only [wp, PostCond.noThrow, pure, Zlt_Zabs_inv1_check, Id.run,
    ULift.up_down]
  show -z2 < z1
  have h' : |z1| < z2 := h
  have := neg_abs_le z1
  omega

-- Coq: `Zle_Zabs_inv1` — |z1| ≤ z2 → -z2 ≤ z1
noncomputable def Zle_Zabs_inv1_check (z1 z2 : Int) : Unit :=
  ()

theorem Zle_Zabs_inv1 (z1 z2 : Int) :
    ⦃⌜|z1| ≤ z2⌝⦄
    (pure (Zle_Zabs_inv1_check z1 z2) : Id Unit)
    ⦃⇓_ => ⌜-z2 ≤ z1⌝⦄ := by
  intro h
  simp only [wp, PostCond.noThrow, pure, Zle_Zabs_inv1_check, Id.run,
    ULift.up_down]
  show -z2 ≤ z1
  have h' : |z1| ≤ z2 := h
  exact neg_le_of_abs_le h'

-- Coq: `Zle_Zabs_inv2` — |z1| ≤ z2 → z1 ≤ z2
noncomputable def Zle_Zabs_inv2_check (z1 z2 : Int) : Unit :=
  ()

theorem Zle_Zabs_inv2 (z1 z2 : Int) :
    ⦃⌜|z1| ≤ z2⌝⦄
    (pure (Zle_Zabs_inv2_check z1 z2) : Id Unit)
    ⦃⇓_ => ⌜z1 ≤ z2⌝⦄ := by
  intro h
  simp only [wp, PostCond.noThrow, pure, Zle_Zabs_inv2_check, Id.run,
    ULift.up_down]
  show z1 ≤ z2
  have h' : |z1| ≤ z2 := h
  exact le_of_abs_le h'

-- Coq: `Zlt_Zabs_Zpred` — if |z1| < z2 and z1 ≠ pred z2 then |succ z1| < z2
noncomputable def Zlt_Zabs_Zpred_check (z1 z2 : Int) : Unit :=
  ()

theorem Zlt_Zabs_Zpred (z1 z2 : Int) :
    ⦃⌜|z1| < z2 ∧ z1 ≠ Int.pred z2⌝⦄
    (pure (Zlt_Zabs_Zpred_check z1 z2) : Id Unit)
    ⦃⇓_ => ⌜|Int.succ z1| < z2⌝⦄ := by
  intro ⟨h1, h2⟩
  simp only [wp, PostCond.noThrow, pure, Zlt_Zabs_Zpred_check, Id.run,
    ULift.up_down]
  show |Int.succ z1| < z2
  have h1' : |z1| < z2 := h1
  have h2' : z1 ≠ Int.pred z2 := h2
  simp only [Int.succ, Int.pred] at *
  have hab := neg_abs_le z1
  have hab2 := le_abs_self z1
  rcases le_or_gt 0 (z1 + 1) with h | h
  · rw [abs_of_nonneg h]; omega
  · rw [abs_of_neg h]; omega

-- (removed duplicate EvenO declarations)

-- Coq: `Zlt_not_eq_rev` — if q < p then p ≠ q
noncomputable def Zlt_not_eq_rev_check (p q : Int) : Unit :=
  ()

theorem Zlt_not_eq_rev (p q : Int) :
    ⦃⌜q < p⌝⦄
    (pure (Zlt_not_eq_rev_check p q) : Id Unit)
    ⦃⇓_ => ⌜p ≠ q⌝⦄ := by
  intro h
  simp only [wp, PostCond.noThrow, pure, Zlt_not_eq_rev_check, Id.run,
    ULift.up_down]
  show p ≠ q
  have h' : q < p := h
  exact ne_of_gt h'

-- Coq: `Zle_Zpred_inv` — if z1 ≤ pred z2 then z1 < z2
noncomputable def Zle_Zpred_inv_check (z1 z2 : Int) : Unit :=
  ()

theorem Zle_Zpred_inv (z1 z2 : Int) :
    ⦃⌜z1 ≤ Int.pred z2⌝⦄
    (pure (Zle_Zpred_inv_check z1 z2) : Id Unit)
    ⦃⇓_ => ⌜z1 < z2⌝⦄ := by
  intro h
  simp only [wp, PostCond.noThrow, pure, Zle_Zpred_inv_check, Id.run,
    ULift.up_down]
  show z1 < z2
  have h' : z1 ≤ Int.pred z2 := h
  simp only [Int.pred] at h'; omega

-- Coq: `Zabs_intro` — if `P` holds for `-z` and `z`, it holds for `|z|`
noncomputable def Zabs_intro_check (P : Int → Prop) (z : Int) : Unit :=
  ()

theorem Zabs_intro (P : Int → Prop) (z : Int) :
    ⦃⌜P (-z) ∧ P z⌝⦄
    (pure (Zabs_intro_check P z) : Id Unit)
    ⦃⇓_ => ⌜P (|z|)⌝⦄ := by
  intro ⟨hneg, hpos⟩
  simp only [wp, PostCond.noThrow, pure, Zabs_intro_check, Id.run,
    ULift.up_down]
  show P |z|
  rcases le_or_gt 0 z with hz | hz
  · rwa [abs_of_nonneg hz]
  · rwa [abs_of_neg hz]

-- Coq: `Zpred_Zle_Zabs_intro` — if -pred z2 ≤ z1 ≤ pred z2 then |z1| < z2
noncomputable def Zpred_Zle_Zabs_intro_check (z1 z2 : Int) : Unit :=
  ()

theorem Zpred_Zle_Zabs_intro (z1 z2 : Int) :
    ⦃⌜-Int.pred z2 ≤ z1 ∧ z1 ≤ Int.pred z2⌝⦄
    (pure (Zpred_Zle_Zabs_intro_check z1 z2) : Id Unit)
    ⦃⇓_ => ⌜|z1| < z2⌝⦄ := by
  intro ⟨h1, h2⟩
  simp only [wp, PostCond.noThrow, pure, Zpred_Zle_Zabs_intro_check, Id.run,
    ULift.up_down]
  show |z1| < z2
  have h1' : -Int.pred z2 ≤ z1 := h1
  have h2' : z1 ≤ Int.pred z2 := h2
  simp only [Int.pred] at h1' h2'
  rcases le_or_gt 0 z1 with hz | hz
  · rw [abs_of_nonneg hz]; omega
  · rw [abs_of_neg hz]; omega

-- Coq: `Zlt_Zabs_intro` — if -z2 < z1 < z2 then |z1| < z2
noncomputable def Zlt_Zabs_intro_check (z1 z2 : Int) : Unit :=
  ()

theorem Zlt_Zabs_intro (z1 z2 : Int) :
    ⦃⌜-z2 < z1 ∧ z1 < z2⌝⦄
    (pure (Zlt_Zabs_intro_check z1 z2) : Id Unit)
    ⦃⇓_ => ⌜|z1| < z2⌝⦄ := by
  intro ⟨h1, h2⟩
  simp only [wp, PostCond.noThrow, pure, Zlt_Zabs_intro_check, Id.run,
    ULift.up_down]
  show |z1| < z2
  have h1' : -z2 < z1 := h1
  have h2' : z1 < z2 := h2
  rcases le_or_gt 0 z1 with hz | hz
  · rw [abs_of_nonneg hz]; omega
  · rw [abs_of_neg hz]; omega

-- Coq: `Zpower_nat_less` — positive powers at every natural exponent
noncomputable def Zpower_nat_less_check (n : Int) (q : Nat) : Unit :=
  ()

theorem Zpower_nat_less (n : Int) (q : Nat) :
    ⦃⌜1 < n⌝⦄
    (pure (Zpower_nat_less_check n q) : Id Unit)
    ⦃⇓_ => ⌜0 < n ^ q⌝⦄ := by
  intro hn
  simp only [wp, PostCond.noThrow, pure, Zpower_nat_less_check,
    Id.run, ULift.up_down]
  show 0 < n ^ q
  exact pow_pos (lt_trans Int.zero_lt_one hn) q

-- Coq: `Zpower_nat_monotone_S` — n^(q+1) > n^q for n > 1
noncomputable def Zpower_nat_monotone_S_check (n : Int) (q : Nat) : Unit :=
  ()

theorem Zpower_nat_monotone_S (n : Int) (q : Nat) :
    ⦃⌜1 < n⌝⦄
    (pure (Zpower_nat_monotone_S_check n q) : Id Unit)
    ⦃⇓_ => ⌜n ^ q < n ^ (q+1)⌝⦄ := by
  intro h
  simp only [wp, PostCond.noThrow, pure, Zpower_nat_monotone_S_check, Id.run,
    ULift.up_down]
  show n ^ q < n ^ (q+1)
  have hn : 1 < n := h
  have : n ^ q ≥ 1 := one_le_pow₀ (le_of_lt hn)
  rw [pow_succ]
  nlinarith

-- Coq: `Zpower_nat_monotone_lt` — strict exponent monotonicity for n > 1
noncomputable def Zpower_nat_monotone_lt_check (n : Int) (p q : Nat) : Unit :=
  ()

theorem Zpower_nat_monotone_lt (n : Int) (p q : Nat) :
    ⦃⌜1 < n ∧ p < q⌝⦄
    (pure (Zpower_nat_monotone_lt_check n p q) : Id Unit)
    ⦃⇓_ => ⌜n ^ p < n ^ q⌝⦄ := by
  intro ⟨hn, hpq⟩
  simp only [wp, PostCond.noThrow, pure, Zpower_nat_monotone_lt_check,
    Id.run, ULift.up_down]
  show n ^ p < n ^ q
  exact pow_lt_pow_right₀ hn hpq

-- Coq: `Zpower_nat_anti_monotone_lt` — strict order of powers reflects strict
-- order of exponents when the integer base is greater than one.
noncomputable def Zpower_nat_anti_monotone_lt_check (n : Int) (p q : Nat) : Unit :=
  ()

theorem Zpower_nat_anti_monotone_lt (n : Int) (p q : Nat) :
    ⦃⌜1 < n ∧ n ^ p < n ^ q⌝⦄
    (pure (Zpower_nat_anti_monotone_lt_check n p q) : Id Unit)
    ⦃⇓_ => ⌜p < q⌝⦄ := by
  intro ⟨hn, hpq⟩
  simp only [wp, PostCond.noThrow, pure, Zpower_nat_anti_monotone_lt_check, Id.run, ULift.up_down]
  by_contra hnot
  have hqp : q ≤ p := Nat.le_of_not_gt hnot
  have hn_le : (1 : Int) ≤ n := le_of_lt hn
  have hpow_le : n ^ q ≤ n ^ p := pow_le_pow_right₀ hn_le hqp
  exact (not_lt_of_ge hpow_le) hpq

-- Coq: `Zpower_nat_monotone_le` — exponent monotonicity for n > 1
noncomputable def Zpower_nat_monotone_le_check (n : Int) (q r : Nat) : Unit :=
  ()

theorem Zpower_nat_monotone_le (n : Int) (q r : Nat) :
    ⦃⌜1 < n ∧ q ≤ r⌝⦄
    (pure (Zpower_nat_monotone_le_check n q r) : Id Unit)
    ⦃⇓_ => ⌜n ^ q ≤ n ^ r⌝⦄ := by
  intro ⟨hn, hqr⟩
  simp only [wp, PostCond.noThrow, pure, Zpower_nat_monotone_le_check, Id.run,
    ULift.up_down]
  show n ^ q ≤ n ^ r
  exact pow_le_pow_right₀ (le_of_lt hn) hqr

-- Alias for Coq's Zpower_nat on integers
-- (moved earlier)

-- Coq: `digitAux1` — (Zpower_nat n (S p) * r) = (Zpower_nat n p * (n * r))
noncomputable def digitAux1_check (n : Int) (p : Nat) (r : Int) : Unit :=
  ()

theorem digitAux1 (n : Int) (p : Nat) (r : Int) :
    ⦃⌜True⌝⦄
    (pure (digitAux1_check n p r) : Id Unit)
    ⦃⇓_ => ⌜Zpower_nat n (Nat.succ p) * r = Zpower_nat n p * (n * r)⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, digitAux1_check, Id.run,
    ULift.up_down]
  show Zpower_nat n (Nat.succ p) * r = Zpower_nat n p * (n * r)
  simp [Zpower_nat, pow_succ]
  ring

-- Minimal positive and digit infrastructure used by digit lemmas.
-- `Positive.val` stores the predecessor of its positive integer value, so the
-- constructor depth of Coq's binary `positive` is `log2 (val + 1)`.
def pos_length (p : Positive) : Nat := Nat.log2 (nat_of_P p)

def digitAuxFuel (n v : Int) : Int → Nat → Nat
  | r, 0 => 0
  | r, Nat.succ q =>
      if r > v then 0 else Nat.succ (digitAuxFuel n v (n * r) q)

def digitAux (n v r : Int) (q : Positive) : Nat :=
  digitAuxFuel n v r (pos_length q)

private theorem digitAuxFuel_less (n v r : Int) (q : Nat) :
    match digitAuxFuel n v r q with
    | Nat.succ r' => Zpower_nat n r' * r ≤ v
    | 0 => True := by
  induction q generalizing r with
  | zero =>
      simp [digitAuxFuel]
  | succ q ih =>
      simp only [digitAuxFuel]
      by_cases hgt : r > v
      · simp [hgt]
      · simp [hgt]
        cases haux : digitAuxFuel n v (n * r) q with
        | zero =>
            simp [Zpower_nat]
            omega
        | succ r' =>
            have hrec :
                Zpower_nat n r' * (n * r) ≤ v := by
              simpa [haux] using ih (n * r)
            have hpow :
                Zpower_nat n (Nat.succ r') * r =
                  Zpower_nat n r' * (n * r) := by
              simp [Zpower_nat, pow_succ]
              ring
            simpa [hpow] using hrec

-- Coq: `digitAuxLess`
noncomputable def digitAuxLess_check (n : Int) (v r : Int) (q : Positive) : Unit :=
  ()

theorem digitAuxLess (n : Int) (v r : Int) (q : Positive) :
    ⦃⌜True⌝⦄
    (pure (digitAuxLess_check n v r q) : Id Unit)
    ⦃⇓_ => ⌜match digitAux n v r q with
            | Nat.succ r' => Zpower_nat n r' * r ≤ v
            | 0 => True⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, digitAuxLess_check, Id.run,
    ULift.up_down]
  simpa [digitAux] using digitAuxFuel_less n v r (pos_length q)

-- Coq: `digitLess` — if q ≠ 0 then Zpower_nat n (pred (digit q)) ≤ |q|
noncomputable def digitLess_check (n : Int) (q : Int) : Unit :=
  ()

-- `digit` is defined earlier near its first use (NotDividesDigit).

theorem digitLess (n : Int) (q : Int) :
    ⦃⌜1 < n ∧ q ≠ 0⌝⦄
    (pure (digitLess_check n q) : Id Unit)
    ⦃⇓_ => ⌜Zpower_nat n (Nat.pred (digit n q)) ≤ |q|⌝⦄ := by
  intro ⟨hn, hq⟩
  simp only [wp, PostCond.noThrow, pure, digitLess_check, Id.run,
    ULift.up_down]
  have hbounds := FloatSpec.Core.Digits.Zdigits_correct_from_nonzero_payload n q hn hq
  let d := FloatSpec.Core.Digits.Zdigits n q
  have hgt : 0 < d := by
    simpa [d] using FloatSpec.Core.Digits.Zdigits_gt_0 n q hn hq
  have hpred_cast : ((Nat.pred d.toNat : Nat) : Int) = d - 1 := by
    have hdto : (d.toNat : Int) = d := Int.toNat_of_nonneg (le_of_lt hgt)
    cases hd : d.toNat with
    | zero =>
        rw [hd] at hdto
        omega
    | succ k =>
        simp only [Nat.pred_succ]
        rw [hd] at hdto
        omega
  have hpred :
      (d - 1).natAbs = Nat.pred d.toNat := by
    apply Nat.cast_injective (R := Int)
    rw [Int.natAbs_of_nonneg (by omega : 0 ≤ d - 1)]
    exact hpred_cast.symm
  simpa [digit, pffDigit_of_one_lt n q hn, Zpower_nat, d, hpred] using hbounds.1

-- Coq: `pos_length_pow` — Zpos p < Zpower_nat n (S (pos_length p))
noncomputable def pos_length_pow_check (n : Int) (p : Positive) : Unit :=
  ()

theorem pos_length_pow (n : Int) (p : Positive) :
    ⦃⌜1 < n⌝⦄
    (pure (pos_length_pow_check n p) : Id Unit)
    ⦃⇓_ => ⌜Int.ofNat (nat_of_P p) < Zpower_nat n (Nat.succ (pos_length p))⌝⦄ := by
  intro hn
  simp only [wp, PostCond.noThrow, pure, pos_length_pow_check,
    Id.run, ULift.up_down]
  show Int.ofNat (nat_of_P p) < n ^ Nat.succ (pos_length p)
  set k := nat_of_P p
  have hn2 : (2 : Int) ≤ n := by
    simpa using (Int.lt_iff_add_one_le.mp hn)
  have hk_ne : k ≠ 0 := by simp [k, nat_of_P]
  have hk_lt_nat : k < 2 ^ (Nat.log2 k + 1) :=
    (Nat.log2_eq_iff hk_ne).mp rfl |>.2
  have hk_lt : (k : Int) < (2 : Int) ^ (Nat.log2 k + 1) := by
    exact_mod_cast hk_lt_nat
  have hpow_mono : (2 : Int) ^ (Nat.log2 k + 1) ≤ n ^ (Nat.log2 k + 1) :=
    pow_le_pow_left₀ (by norm_num : (0 : Int) ≤ 2) hn2 _
  have : (k : Int) < n ^ (Nat.log2 k + 1) := lt_of_lt_of_le hk_lt hpow_mono
  simpa [Zpower_nat, pos_length, k] using this

-- Coq: `digitMore` — |q| < Zpower_nat n (digit q)
noncomputable def digitMore_check (n : Int) (q : Int) : Unit :=
  ()

theorem digitMore (n : Int) (q : Int) :
    ⦃⌜1 < n⌝⦄
    (pure (digitMore_check n q) : Id Unit)
    ⦃⇓_ => ⌜|q| < Zpower_nat n (digit n q)⌝⦄ := by
  intro hn
  simp only [wp, PostCond.noThrow, pure, digitMore_check, Id.run,
    ULift.up_down]
  by_cases hq : q = 0
  · subst q
    simp [digit, pffDigit_of_one_lt n 0 hn, Zpower_nat,
      FloatSpec.Core.Digits.Zdigits]
  · have hbounds := FloatSpec.Core.Digits.Zdigits_correct_from_nonzero_payload n q hn hq
    have hnonneg : 0 ≤ FloatSpec.Core.Digits.Zdigits n q :=
      FloatSpec.Core.Digits.Zdigits_ge_0 n q trivial
    have hnat :
        (FloatSpec.Core.Digits.Zdigits n q).natAbs =
          (FloatSpec.Core.Digits.Zdigits n q).toNat := by
      apply Nat.cast_injective (R := Int)
      rw [Int.natAbs_of_nonneg hnonneg, Int.toNat_of_nonneg hnonneg]
    simpa [digit, pffDigit_of_one_lt n q hn, Zpower_nat, hnat] using hbounds.2

private theorem digitAuxFuel_more (n v r : Int) (q : Nat)
    (hn : 1 < n) (hr : 0 < r)
    (hbound : v < Zpower_nat n q * r) :
    v < Zpower_nat n (digitAuxFuel n v r q) * r := by
  induction q generalizing r with
  | zero =>
      simpa [digitAuxFuel] using hbound
  | succ q ih =>
      simp only [digitAuxFuel]
      by_cases hgt : r > v
      · simp [hgt, Zpower_nat]
      · simp [hgt]
        have hr_le : r ≤ v := by omega
        have hn_pos : 0 < n := by omega
        have hnr : 0 < n * r := by nlinarith
        have hbound' : v < Zpower_nat n q * (n * r) := by
          have hpow :
              Zpower_nat n (Nat.succ q) * r =
                Zpower_nat n q * (n * r) := by
            simp [Zpower_nat, pow_succ]
            ring
          simpa [hpow] using hbound
        have hrec := ih (n * r) hnr hbound'
        have hpow :
            Zpower_nat n (Nat.succ (digitAuxFuel n v (n * r) q)) * r =
              Zpower_nat n (digitAuxFuel n v (n * r) q) * (n * r) := by
          simp [Zpower_nat, pow_succ]
          ring
        simpa [hpow] using hrec

-- Coq: `digitAuxMore` — complementary case for digit auxiliary
noncomputable def digitAuxMore_check (n : Int) (v r : Int) (p : Positive) : Unit :=
  ()

theorem digitAuxMore (n : Int) (v r : Int) (p : Positive) :
    ⦃⌜1 < n ∧ 0 < r ∧ v < Zpower_nat n (pos_length p) * r⌝⦄
    (pure (digitAuxMore_check n v r p) : Id Unit)
    ⦃⇓_ => ⌜v < Zpower_nat n (digitAux n v r p) * r⌝⦄ := by
  intro ⟨hn, hr, hbound⟩
  simp only [wp, PostCond.noThrow, pure, digitAuxMore_check, Id.run,
    ULift.up_down]
  simpa [digitAux] using digitAuxFuel_more n v r (pos_length p) hn hr hbound

-- Coq: `digitInv` — if n^(pred r) ≤ |q| < n^r then digit n q = r
noncomputable def digitInv_check (n : Int) (q : Int) (r : Nat) : Unit :=
  ()

theorem digitInv (n : Int) (q : Int) (r : Nat) :
    ⦃⌜1 < n ∧ Zpower_nat n (Nat.pred r) ≤ |q| ∧ |q| < Zpower_nat n r⌝⦄
    (pure (digitInv_check n q r) : Id Unit)
    ⦃⇓_ => ⌜digit n q = r⌝⦄ := by
  intro ⟨hn, hlow, hupp⟩
  simp only [wp, PostCond.noThrow, pure, digitInv_check, Id.run,
    ULift.up_down]
  by_cases hr0 : r = 0
  · subst r
    simp [Zpower_nat] at hlow hupp
    subst q
    simp at hlow
  · have hrpos : 0 < r := Nat.pos_of_ne_zero hr0
    have hpred :
        (((r : Int) - 1).natAbs) = Nat.pred r := by
      apply Nat.cast_injective (R := Int)
      rw [Int.natAbs_of_nonneg (by omega : 0 ≤ (r : Int) - 1)]
      cases r with
      | zero => contradiction
      | succ k => simp
    have hr_natAbs : ((r : Int).natAbs) = r := by
      simp
    have hq_ne : q ≠ 0 := by
      intro hq0
      subst q
      simp [Zpower_nat] at hlow hupp
      have hn_pos : 0 < n := by omega
      have hpow_pos : 0 < n ^ Nat.pred r := pow_pos hn_pos _
      have hpow_pos' : 0 < n ^ (r - 1) := by
        simpa using hpow_pos
      linarith
    have hpre :
        q ≠ 0 ∧
          n ^ (((r : Int) - 1).natAbs) ≤ (q.natAbs : Int) ∧
          (q.natAbs : Int) < n ^ ((r : Int).natAbs) := by
      refine ⟨hq_ne, ?_, ?_⟩
      · rw [hpred]
        change n ^ Nat.pred r ≤ (q.natAbs : Int)
        rw [← Int.abs_eq_natAbs q]
        simpa [Zpower_nat] using hlow
      · rw [hr_natAbs]
        change (q.natAbs : Int) < n ^ r
        rw [← Int.abs_eq_natAbs q]
        simpa [Zpower_nat] using hupp
    have hzd :=
      (FloatSpec.Core.Digits.Zdigits_unique_from_nonzero_payload
        (beta := n) (h_beta := hn) (n := q) (e := (r : Int)) (hβ := hn)) hpre
    simp only [wp, PostCond.noThrow, pure, Id.run] at hzd
    unfold digit
    rw [pffDigit_of_one_lt n q hn]
    rw [hzd]
    simp

-- Coq: `digit_monotone` — if |p| ≤ |q| then digit n p ≤ digit n q
noncomputable def digit_monotone_check (n : Int) (p q : Int) : Unit :=
  ()

theorem digit_monotone (n : Int) (p q : Int) :
    ⦃⌜1 < n ∧ |p| ≤ |q|⌝⦄
    (pure (digit_monotone_check n p q) : Id Unit)
    ⦃⇓_ => ⌜digit n p ≤ digit n q⌝⦄ := by
  intro ⟨hn, hpq⟩
  simp only [wp, PostCond.noThrow, pure, digit_monotone_check, Id.run,
    ULift.up_down]
  by_cases hp0 : p = 0
  · subst p
    simp [digit, pffDigit_of_one_lt n 0 hn, FloatSpec.Core.Digits.Zdigits]
  · have hpq_nat : p.natAbs ≤ q.natAbs := by
      rw [Int.abs_eq_natAbs, Int.abs_eq_natAbs] at hpq
      exact_mod_cast hpq
    have hpre : p ≠ 0 ∧ p.natAbs ≤ q.natAbs :=
      ⟨hp0, hpq_nat⟩
    have hle_pack :=
      (FloatSpec.Core.Digits.Zdigits_le_from_abs_payload
        (beta := n) (h_beta := hn) (n := p) (m := q) (hβ := hn)) hpre
    simp only [wp, PostCond.noThrow, pure, Id.run] at hle_pack
    rcases hle_pack with ⟨dq, hdq, hle_int⟩
    have hp_nonneg : 0 ≤ FloatSpec.Core.Digits.Zdigits n p :=
      FloatSpec.Core.Digits.Zdigits_ge_0 n p trivial
    have hq_nonneg : 0 ≤ FloatSpec.Core.Digits.Zdigits n q :=
      FloatSpec.Core.Digits.Zdigits_ge_0 n q trivial
    unfold digit
    rw [pffDigit_of_one_lt n p hn, pffDigit_of_one_lt n q hn]
    have hle_toNat_int :
        ((FloatSpec.Core.Digits.Zdigits n p).toNat : Int) ≤
          ((FloatSpec.Core.Digits.Zdigits n q).toNat : Int) := by
      rw [Int.toNat_of_nonneg hp_nonneg, Int.toNat_of_nonneg hq_nonneg]
      simpa [hdq] using hle_int
    exact_mod_cast hle_toNat_int

-- Coq: `digitNotZero` — if q ≠ 0 then 0 < digit n q
noncomputable def digitNotZero_check (n : Int) (q : Int) : Unit :=
  ()

theorem digitNotZero (n : Int) (q : Int) :
    ⦃⌜1 < n ∧ q ≠ 0⌝⦄
    (pure (digitNotZero_check n q) : Id Unit)
    ⦃⇓_ => ⌜0 < digit n q⌝⦄ := by
  intro ⟨hn, hq⟩
  simp only [wp, PostCond.noThrow, pure, digitNotZero_check, Id.run,
    ULift.up_down]
  unfold digit
  rw [pffDigit_of_one_lt n q hn]
  have hgt : 0 < FloatSpec.Core.Digits.Zdigits n q :=
    FloatSpec.Core.Digits.Zdigits_gt_0 n q hn hq
  have hnonneg : 0 ≤ FloatSpec.Core.Digits.Zdigits n q := le_of_lt hgt
  have htoNat :
      ((FloatSpec.Core.Digits.Zdigits n q).toNat : Int) =
        FloatSpec.Core.Digits.Zdigits n q :=
    Int.toNat_of_nonneg hnonneg
  have hpos_int :
      (0 : Int) < (FloatSpec.Core.Digits.Zdigits n q).toNat := by
    rw [htoNat]
    exact hgt
  exact_mod_cast hpos_int

/-- Coq: `MSB_le_abs` — the unit at the most significant bit is below the
absolute value of a nonzero float. -/
theorem MSB_le_abs {beta : Int} [ValidRadix beta]
    (radix : Int) (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    beta = radix →
    1 < radix →
    ¬ is_Fzero x →
    _root_.F2R (beta:=beta)
        (⟨1, MSB (beta:=beta) radix x⟩ : FloatSpec.Core.Defs.FlocqFloat beta) ≤
      _root_.F2R (beta:=beta) (Fabs (beta:=beta) x) := by
  intro hbeta_radix hradix hx_nonzero
  have hbeta : 1 < beta := by omega
  have hxnum : x.Fnum ≠ 0 := by
    simpa [is_Fzero] using hx_nonzero
  let d : Nat := Fdigit (beta:=beta) radix x
  have hd_pos : 0 < d := by
    simpa [d, Fdigit, digit] using (digitNotZero radix x.Fnum) ⟨hradix, hxnum⟩
  have hpred_cast : ((Nat.pred d : Nat) : Int) = (d : Int) - 1 := by
    have hsucc : Nat.succ (Nat.pred d) = d := Nat.succ_pred_eq_of_pos hd_pos
    have hcast :
        ((Nat.succ (Nat.pred d) : Nat) : Int) =
          ((Nat.pred d : Nat) : Int) + 1 := by
      simp
    omega
  have hmsb :
      MSB (beta:=beta) radix x = ((Nat.pred d : Nat) : Int) + x.Fexp := by
    unfold MSB
    have hd : Fdigit (beta:=beta) radix x = d := rfl
    rw [hd, hpred_cast]
    simp [Int.pred]
    omega
  have hpow_le_int : Zpower_nat radix (Nat.pred d) ≤ |x.Fnum| := by
    simpa [d, Fdigit, digit] using (digitLess radix x.Fnum) ⟨hradix, hxnum⟩
  have hpow_le_real :
      (radix : ℝ) ^ (((Nat.pred d : Nat) : Int)) ≤ (|x.Fnum| : ℝ) := by
    have hbridge := (Zpower_nat_Z_powerRZ radix (Nat.pred d)) trivial
    have hcast : ((Zpower_nat radix (Nat.pred d) : Int) : ℝ) ≤ (|x.Fnum| : ℝ) := by
      exact_mod_cast hpow_le_int
    rw [hbridge] at hcast
    exact hcast
  have hradix_pos_int : 0 < radix := by omega
  have hradix_pos_real : 0 < (radix : ℝ) := by exact_mod_cast hradix_pos_int
  have hradix_ne_real : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos_real
  have hscale_pos : 0 < (radix : ℝ) ^ x.Fexp := zpow_pos hradix_pos_real x.Fexp
  have hscaled :
      (radix : ℝ) ^ (((Nat.pred d : Nat) : Int)) * (radix : ℝ) ^ x.Fexp ≤
        (|x.Fnum| : ℝ) * (radix : ℝ) ^ x.Fexp :=
    mul_le_mul_of_nonneg_right hpow_le_real (le_of_lt hscale_pos)
  have htarget :
      (radix : ℝ) ^ (((Nat.pred d : Nat) : Int) + x.Fexp) ≤
        |_root_.F2R (beta:=beta) x| := by
    calc
      (radix : ℝ) ^ (((Nat.pred d : Nat) : Int) + x.Fexp)
          = (radix : ℝ) ^ (((Nat.pred d : Nat) : Int)) *
              (radix : ℝ) ^ x.Fexp := by
            exact zpow_add₀ hradix_ne_real (((Nat.pred d : Nat) : Int)) x.Fexp
      _ ≤ (|x.Fnum| : ℝ) * (radix : ℝ) ^ x.Fexp := hscaled
      _ = |_root_.F2R (beta:=beta) x| := by
            simp [_root_.F2R, FloatSpec.Core.Defs.F2R, hbeta_radix,
              abs_mul, abs_of_pos hscale_pos]
  have hleft :
      _root_.F2R (beta:=beta)
          (⟨1, MSB (beta:=beta) radix x⟩ :
            FloatSpec.Core.Defs.FlocqFloat beta) =
        (radix : ℝ) ^ (((Nat.pred d : Nat) : Int) + x.Fexp) := by
    simp [_root_.F2R, FloatSpec.Core.Defs.F2R, hbeta_radix, hmsb]
  have hright :
      _root_.F2R (beta:=beta) (Fabs (beta:=beta) x) =
        |_root_.F2R (beta:=beta) x| :=
    (Fabs_correct (beta:=beta) x) hbeta
  rw [hleft, hright]
  exact htarget

/-- Coq: `LSB_le_abs` — the unit at the least significant bit is below the
absolute value of a nonzero float. -/
theorem LSB_le_abs {beta : Int} [ValidRadix beta]
    (radix : Int) (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    beta = radix →
    1 < radix →
    ¬ is_Fzero x →
    _root_.F2R (beta:=beta)
        (⟨1, LSB (beta:=beta) radix x⟩ : FloatSpec.Core.Defs.FlocqFloat beta) ≤
      _root_.F2R (beta:=beta) (Fabs (beta:=beta) x) := by
  intro hbeta_radix hradix hx_nonzero
  have hbeta : 1 < beta := by omega
  have hlsb_msb : LSB (beta:=beta) radix x ≤ MSB (beta:=beta) radix x :=
    (LSB_le_MSB (beta:=beta) radix x) ⟨hradix, hx_nonzero⟩
  have hunit_le :
      _root_.F2R (beta:=beta)
          (⟨1, LSB (beta:=beta) radix x⟩ :
            FloatSpec.Core.Defs.FlocqFloat beta) ≤
        _root_.F2R (beta:=beta)
          (⟨1, MSB (beta:=beta) radix x⟩ :
            FloatSpec.Core.Defs.FlocqFloat beta) :=
    (oneExp_le (beta:=beta) (LSB (beta:=beta) radix x)
      (MSB (beta:=beta) radix x)) ⟨hbeta, hlsb_msb⟩
  exact le_trans hunit_le (MSB_le_abs (beta:=beta) radix x
    hbeta_radix hradix hx_nonzero)

/-- Coq: `abs_lt_MSB` — the absolute value of a float is strictly below the
unit just above its most significant bit. -/
theorem abs_lt_MSB {beta : Int} [ValidRadix beta]
    (radix : Int) (x : FloatSpec.Core.Defs.FlocqFloat beta) :
    beta = radix →
    1 < radix →
    _root_.F2R (beta:=beta) (Fabs (beta:=beta) x) <
      _root_.F2R (beta:=beta)
        (⟨1, Int.succ (MSB (beta:=beta) radix x)⟩ :
          FloatSpec.Core.Defs.FlocqFloat beta) := by
  intro hbeta_radix hradix
  have hbeta : 1 < beta := by omega
  have hradix_pos_int : 0 < radix := by omega
  have hradix_pos_real : 0 < (radix : ℝ) := by exact_mod_cast hradix_pos_int
  have hradix_ne_real : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos_real
  by_cases hxnum : x.Fnum = 0
  · have hleft :
        _root_.F2R (beta:=beta) (Fabs (beta:=beta) x) = 0 := by
      simp [Fabs, FloatSpec.Calc.Operations.Fabs, _root_.F2R,
        FloatSpec.Core.Defs.F2R, hxnum]
    have hright_pos :
        0 <
          _root_.F2R (beta:=beta)
            (⟨1, Int.succ (MSB (beta:=beta) radix x)⟩ :
              FloatSpec.Core.Defs.FlocqFloat beta) := by
      simp [_root_.F2R, FloatSpec.Core.Defs.F2R, hbeta_radix,
        zpow_pos hradix_pos_real]
    rw [hleft]
    exact hright_pos
  · let d : Nat := Fdigit (beta:=beta) radix x
    have hsucc_msb :
        Int.succ (MSB (beta:=beta) radix x) = (d : Int) + x.Fexp := by
      unfold MSB
      have hd : Fdigit (beta:=beta) radix x = d := rfl
      rw [hd]
      simp [Int.pred, Int.succ]
    have hpow_lt_int : |x.Fnum| < Zpower_nat radix d := by
      simpa [d, Fdigit, digit] using (digitMore radix x.Fnum) hradix
    have hpow_lt_real :
        (|x.Fnum| : ℝ) < (radix : ℝ) ^ (d : Int) := by
      have hbridge := (Zpower_nat_Z_powerRZ radix d) trivial
      have hcast : (|x.Fnum| : ℝ) < ((Zpower_nat radix d : Int) : ℝ) := by
        exact_mod_cast hpow_lt_int
      rw [hbridge] at hcast
      exact hcast
    have hscale_pos : 0 < (radix : ℝ) ^ x.Fexp := zpow_pos hradix_pos_real x.Fexp
    have hscaled :
        (|x.Fnum| : ℝ) * (radix : ℝ) ^ x.Fexp <
          (radix : ℝ) ^ (d : Int) * (radix : ℝ) ^ x.Fexp :=
      mul_lt_mul_of_pos_right hpow_lt_real hscale_pos
    have hleft_abs :
        _root_.F2R (beta:=beta) (Fabs (beta:=beta) x) =
          |_root_.F2R (beta:=beta) x| :=
      (Fabs_correct (beta:=beta) x) hbeta
    calc
      _root_.F2R (beta:=beta) (Fabs (beta:=beta) x)
          = |_root_.F2R (beta:=beta) x| := hleft_abs
      _ = (|x.Fnum| : ℝ) * (radix : ℝ) ^ x.Fexp := by
            simp [_root_.F2R, FloatSpec.Core.Defs.F2R, hbeta_radix,
              abs_mul, abs_of_pos hscale_pos]
      _ < (radix : ℝ) ^ (d : Int) * (radix : ℝ) ^ x.Fexp := hscaled
      _ = (radix : ℝ) ^ ((d : Int) + x.Fexp) := by
            rw [← zpow_add₀ hradix_ne_real (d : Int) x.Fexp]
      _ = _root_.F2R (beta:=beta)
            (⟨1, Int.succ (MSB (beta:=beta) radix x)⟩ :
              FloatSpec.Core.Defs.FlocqFloat beta) := by
            simp [_root_.F2R, FloatSpec.Core.Defs.F2R, hbeta_radix,
              hsucc_msb]

/-- Coq: `MSB_monotoneAux` — with equal exponents, monotonicity of absolute
values implies monotonicity of most significant bit positions. -/
theorem MSB_monotoneAux {beta : Int} [ValidRadix beta]
    (radix : Int)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) :
    beta = radix →
    1 < radix →
    _root_.F2R (beta:=beta) (Fabs (beta:=beta) x) ≤
      _root_.F2R (beta:=beta) (Fabs (beta:=beta) y) →
    x.Fexp = y.Fexp →
    MSB (beta:=beta) radix x ≤ MSB (beta:=beta) radix y := by
  intro hbeta_radix hradix hle hExp
  have hbeta : 1 < beta := by omega
  have hradix_pos_int : 0 < radix := by omega
  have hradix_pos_real : 0 < (radix : ℝ) := by exact_mod_cast hradix_pos_int
  have hscale_pos : 0 < (radix : ℝ) ^ x.Fexp := zpow_pos hradix_pos_real x.Fexp
  have hscale_pos_y : 0 < (radix : ℝ) ^ y.Fexp := zpow_pos hradix_pos_real y.Fexp
  have hxabs :
      _root_.F2R (beta:=beta) (Fabs (beta:=beta) x) =
        |_root_.F2R (beta:=beta) x| :=
    (Fabs_correct (beta:=beta) x) hbeta
  have hyabs :
      _root_.F2R (beta:=beta) (Fabs (beta:=beta) y) =
        |_root_.F2R (beta:=beta) y| :=
    (Fabs_correct (beta:=beta) y) hbeta
  have hnum_scaled_y :
      (|x.Fnum| : ℝ) * (radix : ℝ) ^ y.Fexp ≤
        (|y.Fnum| : ℝ) * (radix : ℝ) ^ y.Fexp := by
    have hle_abs : |_root_.F2R (beta:=beta) x| ≤
        |_root_.F2R (beta:=beta) y| := by
      simpa [hxabs, hyabs] using hle
    simpa [_root_.F2R, FloatSpec.Core.Defs.F2R, hbeta_radix, hExp,
      abs_mul, abs_of_pos hscale_pos_y, abs_of_pos hradix_pos_real] using hle_abs
  have hnum_scaled :
      (|x.Fnum| : ℝ) * (radix : ℝ) ^ x.Fexp ≤
        (|y.Fnum| : ℝ) * (radix : ℝ) ^ x.Fexp := by
    simpa [hExp] using hnum_scaled_y
  have hnum_le_real : (|x.Fnum| : ℝ) ≤ (|y.Fnum| : ℝ) := by
    nlinarith [hnum_scaled, hscale_pos]
  have hnum_le_int : |x.Fnum| ≤ |y.Fnum| := by
    exact_mod_cast hnum_le_real
  have hdigit :
      Fdigit (beta:=beta) radix x ≤ Fdigit (beta:=beta) radix y := by
    simpa [Fdigit, digit] using
      (digit_monotone radix x.Fnum y.Fnum) ⟨hradix, hnum_le_int⟩
  unfold MSB
  rw [hExp]
  have hdigit_int :
      (Fdigit (beta:=beta) radix x : Int) ≤
        (Fdigit (beta:=beta) radix y : Int) := by
    exact_mod_cast hdigit
  simp [Int.pred]
  omega

/-- Coq: `MSB_monotone` — absolute-value order preserves most significant bit
positions for nonzero floats. -/
theorem MSB_monotone {beta : Int} [ValidRadix beta]
    (radix : Int)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) :
    beta = radix →
    1 < radix →
    ¬ is_Fzero x →
    ¬ is_Fzero y →
    _root_.F2R (beta:=beta) (Fabs (beta:=beta) x) ≤
      _root_.F2R (beta:=beta) (Fabs (beta:=beta) y) →
    MSB (beta:=beta) radix x ≤ MSB (beta:=beta) radix y := by
  intro hbeta_radix hradix hx_nonzero hy_nonzero hle_abs
  have hbeta : 1 < beta := by omega
  have hx_unit_le_abs :
      _root_.F2R (beta:=beta)
          (⟨1, MSB (beta:=beta) radix x⟩ :
            FloatSpec.Core.Defs.FlocqFloat beta) ≤
        _root_.F2R (beta:=beta) (Fabs (beta:=beta) x) :=
    MSB_le_abs (beta:=beta) radix x hbeta_radix hradix hx_nonzero
  have hy_abs_lt_next :
      _root_.F2R (beta:=beta) (Fabs (beta:=beta) y) <
        _root_.F2R (beta:=beta)
          (⟨1, Int.succ (MSB (beta:=beta) radix y)⟩ :
            FloatSpec.Core.Defs.FlocqFloat beta) :=
    abs_lt_MSB (beta:=beta) radix y hbeta_radix hradix
  have hstrict :
      _root_.F2R (beta:=beta)
          (⟨1, MSB (beta:=beta) radix x⟩ :
            FloatSpec.Core.Defs.FlocqFloat beta) <
        _root_.F2R (beta:=beta)
          (⟨1, Int.succ (MSB (beta:=beta) radix y)⟩ :
            FloatSpec.Core.Defs.FlocqFloat beta) :=
    lt_of_le_of_lt (le_trans hx_unit_le_abs hle_abs) hy_abs_lt_next
  have hlt_succ :
      MSB (beta:=beta) radix x < Int.succ (MSB (beta:=beta) radix y) :=
    (oneExp_Zlt (beta:=beta)
        (MSB (beta:=beta) radix x)
        (Int.succ (MSB (beta:=beta) radix y)))
      ⟨hbeta, hstrict⟩
  simpa [Int.succ] using hlt_succ

private lemma abs_F2R_repr_at_lsb {beta : Int} [ValidRadix beta]
    (radix : Int) (x : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta_radix : beta = radix) (hradix : 1 < radix) :
    ∃ z : Int,
      |_root_.F2R (beta:=beta) x| =
        (z : ℝ) * (radix : ℝ) ^ (LSB (beta:=beta) radix x) := by
  have hbeta : 1 < beta := by omega
  have habs :
      _root_.F2R (beta:=beta) (Fabs (beta:=beta) x) =
        |_root_.F2R (beta:=beta) x| := by
    have h := Fabs_correct (beta:=beta) x
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fabs_correct_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h hbeta
  have hlsb_abs :
      LSB (beta:=beta) radix x =
        LSB (beta:=beta) radix (Fabs (beta:=beta) x) :=
    (LSB_abs (beta:=beta) radix x) trivial
  rcases (LSB_rep_min (beta:=beta) radix (Fabs (beta:=beta) x))
      ⟨hbeta_radix, hradix⟩ with ⟨z, hz⟩
  refine ⟨z, ?_⟩
  calc
    |_root_.F2R (beta:=beta) x|
        = _root_.F2R (beta:=beta) (Fabs (beta:=beta) x) := habs.symm
    _ = _root_.F2R (beta:=beta)
          (⟨z, LSB (beta:=beta) radix (Fabs (beta:=beta) x)⟩ :
            FloatSpec.Core.Defs.FlocqFloat beta) := hz
    _ = (z : ℝ) * (radix : ℝ) ^ (LSB (beta:=beta) radix x) := by
          simp [_root_.F2R, FloatSpec.Core.Defs.F2R, hbeta_radix,
            ← hlsb_abs]

private lemma abs_F2R_repr_at {beta : Int} [ValidRadix beta]
    (radix : Int) (x : FloatSpec.Core.Defs.FlocqFloat beta) (e : Int)
    (hbeta_radix : beta = radix) (hradix : 1 < radix)
    (he : e ≤ LSB (beta:=beta) radix x) :
    ∃ z : Int,
      |_root_.F2R (beta:=beta) x| =
        (z : ℝ) * (radix : ℝ) ^ e := by
  rcases abs_F2R_repr_at_lsb (beta:=beta) radix x hbeta_radix hradix with
    ⟨z, hz⟩
  let k : Nat := Int.toNat (LSB (beta:=beta) radix x - e)
  let scale : Int := Zpower_nat radix k
  refine ⟨z * scale, ?_⟩
  have hradix_pos_int : 0 < radix := by omega
  have hradix_pos_real : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradix_pos_int
  have hradix_ne_real : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos_real
  have hdiff_nonneg : 0 ≤ LSB (beta:=beta) radix x - e := by omega
  have hk_cast :
      (k : Int) = LSB (beta:=beta) radix x - e := by
    simpa [k] using Int.toNat_of_nonneg hdiff_nonneg
  have hscale :
      (scale : ℝ) =
        (radix : ℝ) ^ (LSB (beta:=beta) radix x - e) := by
    have hbridge := (Zpower_nat_Z_powerRZ radix k) trivial
    simpa [scale, hk_cast] using hbridge
  have hsplit :
      (radix : ℝ) ^ (LSB (beta:=beta) radix x) =
        (radix : ℝ) ^ (LSB (beta:=beta) radix x - e) *
          (radix : ℝ) ^ e := by
    calc
      (radix : ℝ) ^ (LSB (beta:=beta) radix x)
          = (radix : ℝ) ^ ((LSB (beta:=beta) radix x - e) + e) := by
              congr 1
              ring
      _ = (radix : ℝ) ^ (LSB (beta:=beta) radix x - e) *
            (radix : ℝ) ^ e :=
              zpow_add₀ hradix_ne_real (LSB (beta:=beta) radix x - e) e
  calc
    |_root_.F2R (beta:=beta) x|
        = (z : ℝ) * (radix : ℝ) ^ (LSB (beta:=beta) radix x) := hz
    _ = (z : ℝ) *
          ((radix : ℝ) ^ (LSB (beta:=beta) radix x - e) *
            (radix : ℝ) ^ e) := by rw [hsplit]
    _ = ((z * scale : Int) : ℝ) * (radix : ℝ) ^ e := by
          rw [Int.cast_mul, hscale]
          ring

noncomputable def LSB_Pred_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `LSB_Pred`.

If two floats are ordered by absolute value and the least significant bit of
`x` is no larger than that of `y`, then `|x|` is at least one `LSB x` unit
below `|y|`.  The precondition carries the upstream `GenericC` section
assumptions. -/
theorem LSB_Pred {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧
        b.vNum = Zpower_nat radix precision ∧ 1 < precision ∧ Even radix ∧
        |_root_.F2R (beta:=beta) x| < |_root_.F2R (beta:=beta) y| ∧
        LSB (beta:=beta) radix x ≤ LSB (beta:=beta) radix y⌝⦄
    (pure (LSB_Pred_check (beta:=beta) b radix precision x y) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) x| ≤
        |_root_.F2R (beta:=beta) y| -
          (radix : ℝ) ^ (LSB (beta:=beta) radix x)⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta_radix, hradix, _hvnum, _hprecision, _heven, hlt_abs, hlsb⟩
  simp only [wp, PostCond.noThrow, pure, LSB_Pred_check, Id.run,
    ULift.up_down]
  let e : Int := LSB (beta:=beta) radix x
  rcases abs_F2R_repr_at (beta:=beta) radix x e hbeta_radix hradix
      (by simp [e]) with ⟨nx, hx_repr⟩
  rcases abs_F2R_repr_at (beta:=beta) radix y e hbeta_radix hradix
      (by simpa [e] using hlsb) with ⟨ny, hy_repr⟩
  have hradix_pos_int : 0 < radix := by omega
  have hradix_pos_real : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradix_pos_int
  have hunit_pos : 0 < (radix : ℝ) ^ e := zpow_pos hradix_pos_real e
  have hnx_lt_ny_real : (nx : ℝ) < (ny : ℝ) := by
    have hscaled :
        (nx : ℝ) * (radix : ℝ) ^ e <
          (ny : ℝ) * (radix : ℝ) ^ e := by
      simpa [hx_repr, hy_repr] using hlt_abs
    exact lt_of_mul_lt_mul_right hscaled (le_of_lt hunit_pos)
  have hnx_succ_le_ny : nx + 1 ≤ ny := by
    have hnx_lt_ny : nx < ny := Int.cast_lt.mp hnx_lt_ny_real
    omega
  have hcoeff :
      ((nx : ℝ) + 1) * (radix : ℝ) ^ e ≤
        (ny : ℝ) * (radix : ℝ) ^ e := by
    have hcoeff_le : (nx : ℝ) + 1 ≤ (ny : ℝ) := by
      exact_mod_cast hnx_succ_le_ny
    exact mul_le_mul_of_nonneg_right hcoeff_le (le_of_lt hunit_pos)
  calc
    |_root_.F2R (beta:=beta) x|
        = (nx : ℝ) * (radix : ℝ) ^ e := hx_repr
    _ ≤ (ny : ℝ) * (radix : ℝ) ^ e - (radix : ℝ) ^ e := by
          nlinarith
    _ = |_root_.F2R (beta:=beta) y| - (radix : ℝ) ^ e := by
          rw [hy_repr]
    _ = |_root_.F2R (beta:=beta) y| -
          (radix : ℝ) ^ (LSB (beta:=beta) radix x) := by
          simp [e]

/-- Coq: `isMinComp` — a lower rounded value remains the lower bound for any
intermediate real strictly between the same min/max pair. -/
theorem isMinComp {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (r1 r2 : ℝ) (min max : FloatSpec.Core.Defs.FlocqFloat beta) :
    isMin (beta:=beta) b radix r1 min →
    isMax (beta:=beta) b radix r1 max →
    _root_.F2R (beta:=beta) min < r2 →
    r2 < _root_.F2R (beta:=beta) max →
    isMin (beta:=beta) b radix r2 min := by
  intro hmin hmax hmin_lt hlt_max
  rcases hmin with ⟨hmin_bounded, hmin_le, hgreatest⟩
  rcases hmax with ⟨hmax_bounded, hmax_ge, hleast⟩
  constructor
  · exact hmin_bounded
  constructor
  · exact le_of_lt hmin_lt
  · intro f hfbounded hf_le_r2
    by_cases hf_le_r1 : _root_.F2R (beta:=beta) f ≤ r1
    · exact hgreatest f hfbounded hf_le_r1
    · have hr1_lt_f : r1 < _root_.F2R (beta:=beta) f := lt_of_not_ge hf_le_r1
      have hmax_le_f : _root_.F2R (beta:=beta) max ≤ _root_.F2R (beta:=beta) f :=
        hleast f hfbounded (le_of_lt hr1_lt_f)
      have hf_lt_max : _root_.F2R (beta:=beta) f < _root_.F2R (beta:=beta) max :=
        lt_of_le_of_lt hf_le_r2 hlt_max
      exact False.elim ((not_lt_of_ge hmax_le_f) hf_lt_max)

/-- Coq: `isMaxComp` — an upper rounded value remains the upper bound for any
intermediate real strictly between the same min/max pair. -/
theorem isMaxComp {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (r1 r2 : ℝ) (min max : FloatSpec.Core.Defs.FlocqFloat beta) :
    isMin (beta:=beta) b radix r1 min →
    isMax (beta:=beta) b radix r1 max →
    _root_.F2R (beta:=beta) min < r2 →
    r2 < _root_.F2R (beta:=beta) max →
    isMax (beta:=beta) b radix r2 max := by
  intro hmin hmax hmin_lt hlt_max
  rcases hmin with ⟨hmin_bounded, hmin_le, hgreatest⟩
  rcases hmax with ⟨hmax_bounded, hmax_ge, hleast⟩
  constructor
  · exact hmax_bounded
  constructor
  · exact le_of_lt hlt_max
  · intro f hfbounded hr2_le_f
    by_cases hr1_le_f : r1 ≤ _root_.F2R (beta:=beta) f
    · exact hleast f hfbounded hr1_le_f
    · have hf_lt_r1 : _root_.F2R (beta:=beta) f < r1 := lt_of_not_ge hr1_le_f
      have hf_le_min : _root_.F2R (beta:=beta) f ≤ _root_.F2R (beta:=beta) min :=
        hgreatest f hfbounded (le_of_lt hf_lt_r1)
      have hmin_lt_f : _root_.F2R (beta:=beta) min < _root_.F2R (beta:=beta) f :=
        lt_of_lt_of_le hmin_lt hr2_le_f
      exact False.elim ((not_lt_of_ge hf_le_min) hmin_lt_f)

-- Coq: `digitAdd` — digit n (q * n^r) = digit n q + r for q ≠ 0
noncomputable def digitAdd_check (n : Int) (q : Int) (r : Nat) : Unit :=
  ()

theorem digitAdd (n : Int) (q : Int) (r : Nat) :
    ⦃⌜1 < n ∧ q ≠ 0⌝⦄
    (pure (digitAdd_check n q r) : Id Unit)
    ⦃⇓_ => ⌜digit n (q * Zpower_nat n r) = digit n q + r⌝⦄ := by
  intro ⟨hn, hq⟩
  simp only [wp, PostCond.noThrow, pure, digitAdd_check, Id.run,
    ULift.up_down]
  unfold digit
  rw [pffDigit_of_one_lt n (q * Zpower_nat n r) hn,
    pffDigit_of_one_lt n q hn]
  have hr_nonneg : 0 ≤ (r : Int) := by exact_mod_cast Nat.zero_le r
  have hmul :=
    (FloatSpec.Core.Digits.Zdigits_mult_Zpower
      (beta := n) (n := q) (k := (r : Int)) (h_beta := hn))
      ⟨hq, hr_nonneg⟩
  rcases hmul with ⟨dq, hdq, hres⟩
  have hdq_nonneg : 0 ≤ FloatSpec.Core.Digits.Zdigits n q :=
    FloatSpec.Core.Digits.Zdigits_ge_0 n q trivial
  have hpow_arg : q * n ^ (r : Int).natAbs = q * Zpower_nat n r := by
    simp [Zpower_nat]
  rw [hpow_arg] at hres
  have hres' :
      FloatSpec.Core.Digits.Zdigits n (q * Zpower_nat n r) =
        FloatSpec.Core.Digits.Zdigits n q + (r : Int) := by
    simpa [hdq] using hres
  rw [hres']
  rw [Int.toNat_add hdq_nonneg hr_nonneg]
  simp

private lemma maxDiv_ge_of_dvd (radix v : Int) (k p : Nat)
    (hkp : k ≤ p) (hdiv : Zdivides v (Zpower_nat radix k)) :
    k ≤ maxDiv radix v p := by
  induction p generalizing k with
  | zero =>
      have hk0 : k = 0 := by omega
      subst k
      simp [maxDiv]
  | succ p ih =>
      letI : Decidable (Zdivides v (Zpower_nat radix (Nat.succ p))) :=
        Classical.propDecidable (Zdivides v (Zpower_nat radix (Nat.succ p)))
      classical
      by_cases htop : Zdivides v (Zpower_nat radix (Nat.succ p))
      · simp [maxDiv, htop, hkp]
      · simp [maxDiv, htop]
        have hk_le_p : k ≤ p := by
          by_contra hnot
          have hk_eq : k = Nat.succ p := by omega
          subst k
          exact htop hdiv
        exact ih k hk_le_p hdiv

/-- Coq: `maxDivSimplAux` — if `p` is already the maximal divisor exponent
inside a larger `S (q + p)` search range, it is also the result in the
minimal `S p` search range. -/
theorem maxDivSimplAux (radix v : Int) (p q : Nat) :
    p = maxDiv radix v (Nat.succ (q + p)) →
    p = maxDiv radix v (Nat.succ p) := by
  intro hbig
  apply le_antisymm
  · have hdiv_big :
        Zdivides v (Zpower_nat radix (maxDiv radix v (Nat.succ (q + p)))) := by
      exact (maxDivCorrect radix v (Nat.succ (q + p))) trivial
    have hdiv_p : Zdivides v (Zpower_nat radix p) := by
      simpa [← hbig] using hdiv_big
    exact maxDiv_ge_of_dvd radix v p (Nat.succ p) (Nat.le_succ p) hdiv_p
  · have hle : maxDiv radix v (Nat.succ p) ≤ Nat.succ p := by
      exact (maxDivLess radix v (Nat.succ p)) trivial
    have hne : maxDiv radix v (Nat.succ p) ≠ Nat.succ p := by
      intro hsmall_top
      have hdiv_small :
          Zdivides v (Zpower_nat radix (maxDiv radix v (Nat.succ p))) := by
        exact (maxDivCorrect radix v (Nat.succ p)) trivial
      have hdiv_succ : Zdivides v (Zpower_nat radix (Nat.succ p)) := by
        simpa [hsmall_top] using hdiv_small
      have hsucc_le_big :
          Nat.succ p ≤ maxDiv radix v (Nat.succ (q + p)) := by
        apply maxDiv_ge_of_dvd radix v (Nat.succ p) (Nat.succ (q + p))
        · omega
        · exact hdiv_succ
      omega
    omega

/-- Coq: `maxDivSimpl` — the previous auxiliary simplification for any
strictly larger search bound. -/
theorem maxDivSimpl (radix v : Int) (p q : Nat) :
    p < q →
    p = maxDiv radix v q →
    p = maxDiv radix v (Nat.succ p) := by
  intro hpq hq
  have hq_shape : q = Nat.succ ((q - Nat.succ p) + p) := by
    omega
  apply maxDivSimplAux (radix:=radix) (v:=v) (p:=p)
      (q:=q - Nat.succ p)
  rw [← hq_shape]
  exact hq

/-- Coq: `maxDivUnique` — the simplified maximum exponent is exactly the
last exponent where divisibility holds. -/
theorem maxDivUnique (radix v : Int) (p : Nat) :
    p = maxDiv radix v (Nat.succ p) →
    Zdivides v (Zpower_nat radix p) ∧
      ¬ Zdivides v (Zpower_nat radix (Nat.succ p)) := by
  intro hp
  constructor
  · have hdiv :
        Zdivides v (Zpower_nat radix (maxDiv radix v (Nat.succ p))) := by
      exact (maxDivCorrect radix v (Nat.succ p)) trivial
    simpa [← hp] using hdiv
  · intro hdiv_succ
    have htop : maxDiv radix v (Nat.succ p) = Nat.succ p := by
      simp [maxDiv, hdiv_succ]
    omega

private lemma Zdivides_pow_le (radix v : Int) (a b : Nat)
    (hab : a ≤ b) (hdiv : Zdivides v (Zpower_nat radix b)) :
    Zdivides v (Zpower_nat radix a) := by
  rcases hdiv with ⟨q, hq⟩
  refine ⟨radix ^ (b - a) * q, ?_⟩
  have hb : a + (b - a) = b := Nat.add_sub_of_le hab
  calc
    v = Zpower_nat radix b * q := hq
    _ = (radix ^ a * radix ^ (b - a)) * q := by
        simp [Zpower_nat, ← pow_add, hb]
    _ = Zpower_nat radix a * (radix ^ (b - a) * q) := by
        simp [Zpower_nat]
        ring

private lemma Zdivides_mul_pow (radix v : Int) (a n : Nat)
    (hdiv : Zdivides v (Zpower_nat radix a)) :
    Zdivides (v * Zpower_nat radix n) (Zpower_nat radix (a + n)) := by
  rcases hdiv with ⟨q, hq⟩
  refine ⟨q, ?_⟩
  calc
    v * Zpower_nat radix n = (Zpower_nat radix a * q) * Zpower_nat radix n := by
      rw [hq]
    _ = Zpower_nat radix (a + n) * q := by
      simp [Zpower_nat, pow_add]
      ring

private lemma Zdivides_cancel_pow (radix v : Int) (a n : Nat)
    (hradix : 1 < radix)
    (hdiv : Zdivides (v * Zpower_nat radix n) (Zpower_nat radix (a + n))) :
    Zdivides v (Zpower_nat radix a) := by
  have hradix_pos : 0 < radix := by omega
  have hpow_ne : Zpower_nat radix n ≠ 0 := by
    exact ne_of_gt (by simpa [Zpower_nat] using pow_pos hradix_pos n)
  have hdiv' :
      Zdivides (Zpower_nat radix n * v)
        (Zpower_nat radix n * Zpower_nat radix a) := by
    rcases hdiv with ⟨q, hq⟩
    refine ⟨q, ?_⟩
    calc
      Zpower_nat radix n * v = v * Zpower_nat radix n := by ring
      _ = Zpower_nat radix (a + n) * q := hq
      _ = (Zpower_nat radix n * Zpower_nat radix a) * q := by
        have hp :
            Zpower_nat radix (a + n) =
              Zpower_nat radix n * Zpower_nat radix a := by
          simp [Zpower_nat, pow_add, mul_comm, mul_left_comm, mul_assoc]
        rw [hp]
  exact (ZdividesDiv v (Zpower_nat radix a) (Zpower_nat radix n)) ⟨hpow_ne, hdiv'⟩

/-- Coq: `maxDivSimplInvAux` — the inverse direction of
`maxDivSimplAux`. -/
theorem maxDivSimplInvAux (radix v : Int) (p q : Nat) :
    p = maxDiv radix v (Nat.succ p) →
    p = maxDiv radix v (Nat.succ (q + p)) := by
  intro hp
  have huniq := maxDivUnique radix v p hp
  rcases huniq with ⟨hdiv_p, hnot_succ⟩
  apply le_antisymm
  · apply maxDiv_ge_of_dvd radix v p (Nat.succ (q + p))
    · omega
    · exact hdiv_p
  · have hle_big : maxDiv radix v (Nat.succ (q + p)) ≤ Nat.succ (q + p) := by
      exact (maxDivLess radix v (Nat.succ (q + p))) trivial
    by_contra hnot
    have hsucc_le : Nat.succ p ≤ maxDiv radix v (Nat.succ (q + p)) := by
      omega
    have hdiv_big :
        Zdivides v (Zpower_nat radix (maxDiv radix v (Nat.succ (q + p)))) := by
      exact (maxDivCorrect radix v (Nat.succ (q + p))) trivial
    exact hnot_succ
      (Zdivides_pow_le radix v (Nat.succ p)
        (maxDiv radix v (Nat.succ (q + p))) hsucc_le hdiv_big)

/-- Coq: `maxDivSimplInv` — the inverse simplification for any larger search
bound. -/
theorem maxDivSimplInv (radix v : Int) (p q : Nat) :
    p < q →
    p = maxDiv radix v (Nat.succ p) →
    p = maxDiv radix v q := by
  intro hpq hp
  have hq_shape : q = Nat.succ ((q - Nat.succ p) + p) := by
    omega
  rw [hq_shape]
  exact maxDivSimplInvAux radix v p (q - Nat.succ p) hp

/-- Coq: `maxDivUniqueInverse` — divisibility at `p` and failure at
`S p` characterize the simplified maximum. -/
theorem maxDivUniqueInverse (radix v : Int) (p : Nat) :
    Zdivides v (Zpower_nat radix p) →
    ¬ Zdivides v (Zpower_nat radix (Nat.succ p)) →
    p = maxDiv radix v (Nat.succ p) := by
  intro hdiv_p hnot_succ
  apply le_antisymm
  · exact maxDiv_ge_of_dvd radix v p (Nat.succ p) (Nat.le_succ p) hdiv_p
  · have hle : maxDiv radix v (Nat.succ p) ≤ Nat.succ p := by
      exact (maxDivLess radix v (Nat.succ p)) trivial
    by_contra hnot
    have htop : maxDiv radix v (Nat.succ p) = Nat.succ p := by
      omega
    have hdiv_top :
        Zdivides v (Zpower_nat radix (maxDiv radix v (Nat.succ p))) := by
      exact (maxDivCorrect radix v (Nat.succ p)) trivial
    exact hnot_succ (by simpa [htop] using hdiv_top)

/-- Coq: `maxDivUniqueDigit` — the maximum divisor below the digit bound is
the last exponent where divisibility by `v` holds. -/
theorem maxDivUniqueDigit (radix v : Int) :
    1 < radix →
    v ≠ 0 →
    Zdivides v (Zpower_nat radix (maxDiv radix v (digit radix v))) ∧
      ¬ Zdivides v
          (Zpower_nat radix (Nat.succ (maxDiv radix v (digit radix v)))) := by
  intro hradix hv
  apply maxDivUnique
  apply maxDivSimpl (radix:=radix) (v:=v)
      (p:=maxDiv radix v (digit radix v)) (q:=digit radix v)
  · have hnot_digit :
        ¬ Zdivides v (Zpower_nat radix (digit radix v)) := by
      simpa using (NotDividesDigit radix v) ⟨hradix, hv⟩
    simpa using (maxDivLt radix v (digit radix v)) hnot_digit
  · rfl

/-- Coq: `maxDivUniqueInverseDigit` — divisibility/failure at adjacent
exponents characterizes the maximum divisor below the digit bound. -/
theorem maxDivUniqueInverseDigit (radix v : Int) (p : Nat) :
    1 < radix →
    v ≠ 0 →
    Zdivides v (Zpower_nat radix p) →
    ¬ Zdivides v (Zpower_nat radix (Nat.succ p)) →
    p = maxDiv radix v (digit radix v) := by
  intro hradix hv hdiv hnot_succ
  apply maxDivSimplInv (radix:=radix) (v:=v) (p:=p)
      (q:=digit radix v)
  · have hradix_nonneg : 0 ≤ radix := by omega
    have hpow_nonneg : 0 ≤ Zpower_nat radix p := by
      simpa [Zpower_nat] using (Zpower_NR0 radix p) hradix_nonneg
    have hdiv_le : |Zpower_nat radix p| ≤ |v| := by
      simpa using (ZDividesLe v (Zpower_nat radix p)) ⟨hv, hdiv⟩
    have hdigit_more : |v| < Zpower_nat radix (digit radix v) := by
      simpa using (digitMore radix v) hradix
    have hpow_lt :
        Zpower_nat radix p < Zpower_nat radix (digit radix v) := by
      calc
        Zpower_nat radix p = |Zpower_nat radix p| := by
          exact (abs_of_nonneg hpow_nonneg).symm
        _ ≤ |v| := hdiv_le
        _ < Zpower_nat radix (digit radix v) := hdigit_more
    simpa [Zpower_nat] using
      (Zpower_nat_anti_monotone_lt radix p (digit radix v))
        ⟨hradix, by simpa [Zpower_nat] using hpow_lt⟩
  · exact maxDivUniqueInverse radix v p hdiv hnot_succ

-- Coq: `maxDivPlus` — multiplicative stability of maxDiv against n-th power of radix
noncomputable def maxDivPlus_check (radix : Int) (v : Int) (n : Nat) : Unit :=
  ()

theorem maxDivPlus (radix : Int) (v : Int) (n : Nat) :
    ⦃⌜1 < radix ∧ v ≠ 0⌝⦄
    (pure (maxDivPlus_check radix v n) : Id Unit)
    ⦃⇓_ => ⌜maxDiv radix (v * Zpower_nat radix n) (digit radix v + n) =
            maxDiv radix v (digit radix v) + n⌝⦄ := by
  intro ⟨hradix, hv⟩
  simp only [wp, PostCond.noThrow, pure, maxDivPlus_check, Id.run,
    ULift.up_down]
  set d := digit radix v
  set md := maxDiv radix v d
  set prod := v * Zpower_nat radix n
  set mprod := maxDiv radix prod (d + n)
  have hmd_le_d : md ≤ d := by
    simpa [md, d] using (maxDivLess radix v (digit radix v)) trivial
  have hmd_div : Zdivides v (Zpower_nat radix md) := by
    simpa [md, d] using (maxDivCorrect radix v (digit radix v)) trivial
  have hprod_div : Zdivides prod (Zpower_nat radix (md + n)) := by
    simpa [prod] using Zdivides_mul_pow radix v md n hmd_div
  have hlower : md + n ≤ mprod := by
    have hlimit : md + n ≤ d + n := by omega
    simpa [mprod, prod] using
      maxDiv_ge_of_dvd radix prod (md + n) (d + n) hlimit hprod_div
  have hnot_digit : ¬ Zdivides v (Zpower_nat radix d) := by
    simpa [d] using (NotDividesDigit radix v) ⟨hradix, hv⟩
  have hmd_lt_d : md < d := by
    simpa [md, d] using (maxDivLt radix v d) hnot_digit
  have hnot_step : ¬ Zdivides v (Zpower_nat radix (Nat.succ md)) := by
    intro hstep
    have hsucc_le_d : Nat.succ md ≤ d := Nat.succ_le_iff.mpr hmd_lt_d
    have hle_md : Nat.succ md ≤ md :=
      maxDiv_ge_of_dvd radix v (Nat.succ md) d hsucc_le_d hstep
    omega
  have hupper : mprod ≤ md + n := by
    by_contra hnot
    have hgt : md + n < mprod := Nat.lt_of_not_ge hnot
    have hsucc_le_mprod : Nat.succ (md + n) ≤ mprod := Nat.succ_le_iff.mpr hgt
    have hmprod_div : Zdivides prod (Zpower_nat radix mprod) := by
      simpa [mprod, prod] using (maxDivCorrect radix prod (d + n)) trivial
    have hdiv_succ_sum :
        Zdivides prod (Zpower_nat radix (Nat.succ (md + n))) :=
      Zdivides_pow_le radix prod (Nat.succ (md + n)) mprod hsucc_le_mprod hmprod_div
    have hsucc_sum_eq : Nat.succ (md + n) = Nat.succ md + n := by omega
    have hdiv_succ_sum' :
        Zdivides prod (Zpower_nat radix (Nat.succ md + n)) := by
      rw [← hsucc_sum_eq]
      exact hdiv_succ_sum
    have hdiv_cancel :
        Zdivides v (Zpower_nat radix (Nat.succ md)) := by
      apply Zdivides_cancel_pow radix v (Nat.succ md) n hradix
      simpa [prod] using hdiv_succ_sum'
    exact hnot_step hdiv_cancel
  have heq : mprod = md + n := le_antisymm hupper hlower
  simpa [mprod, prod, md, d] using heq

theorem LSB_shift {beta : Int} [ValidRadix beta]
    (radix : Int) (x : FloatSpec.Core.Defs.FlocqFloat beta) (n : Nat) :
    ⦃⌜1 < radix ∧ ¬ is_Fzero x⌝⦄
    (pure (LSB_shift_check (beta:=beta) radix x n) : Id Unit)
    ⦃⇓_ => ⌜LSB (beta:=beta) radix x = LSB (beta:=beta) radix (Fshift (beta:=beta) radix n x)⌝⦄ := by
  intro ⟨_hradix, hx_nonzero⟩
  simp only [wp, PostCond.noThrow, pure, LSB_shift_check, Id.run,
    ULift.up_down]
  show LSB radix x = LSB radix (Fshift radix n x)
  cases x with
  | mk m e =>
    change Int.ofNat (maxDiv radix m (digit radix m)) + e =
      Int.ofNat (maxDiv radix (m * Zpower_nat radix n)
        (digit radix (m * Zpower_nat radix n))) + (e - (n : Int))
    have hdigit :
        digit radix (m * Zpower_nat radix n) = digit radix m + n :=
      (digitAdd radix m n) ⟨_hradix, hx_nonzero⟩
    have hmax :
        maxDiv radix (m * Zpower_nat radix n) (digit radix m + n) =
          maxDiv radix m (digit radix m) + n :=
      (maxDivPlus radix m n) ⟨_hradix, hx_nonzero⟩
    rw [hdigit, hmax]
    change ((maxDiv radix m (digit radix m) : Nat) : Int) + e =
      ((maxDiv radix m (digit radix m) + n : Nat) : Int) + (e - (n : Int))
    rw [Nat.cast_add]
    omega

theorem LSB_comp {beta : Int} [ValidRadix beta]
    (radix : Int)
    (x y : FloatSpec.Core.Defs.FlocqFloat beta) (n : Nat) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ ¬ is_Fzero x ∧ _root_.F2R x = _root_.F2R y⌝⦄
    (pure (LSB_comp_check (beta:=beta) radix x y n) : Id Unit)
    ⦃⇓_ => ⌜LSB (beta:=beta) radix x = LSB (beta:=beta) radix y⌝⦄ := by
  intro ⟨hbeta_radix, hradix, hx_nonzero, hxy⟩
  simp only [wp, PostCond.noThrow, pure, LSB_comp_check, Id.run,
    ULift.up_down]
  have hbeta : 1 < beta := by omega
  have hy_nonzero : ¬ is_Fzero y :=
    (NisFzeroComp (beta:=beta) x y) ⟨hbeta, hx_nonzero, hxy⟩
  rcases (FshiftCorrectSym (beta:=beta) radix x y)
      ⟨hbeta_radix, hradix, hxy⟩ with ⟨nx, ny, hshift⟩
  have hx_lsb :
      LSB (beta:=beta) radix x =
        LSB (beta:=beta) radix (Fshift (beta:=beta) radix nx x) :=
    (LSB_shift (beta:=beta) radix x nx) ⟨hradix, hx_nonzero⟩
  have hy_lsb :
      LSB (beta:=beta) radix y =
        LSB (beta:=beta) radix (Fshift (beta:=beta) radix ny y) :=
    (LSB_shift (beta:=beta) radix y ny) ⟨hradix, hy_nonzero⟩
  rw [hx_lsb, hshift, ← hy_lsb]
  rfl

private lemma midpoint_witness_of_lsb {beta : Int} [ValidRadix beta]
    (radix : Int)
    (x1 x2 : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta_radix : beta = radix) (hradix : 1 < radix)
    (hlsb : x1.Fexp - 2 ≤ LSB (beta:=beta) radix x2) :
    ∃ v : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) v = _root_.F2R (beta:=beta) x2 ∧
        x1.Fexp - 2 ≤ v.Fexp := by
  rcases (LSB_rep_min (beta:=beta) radix x2) ⟨hbeta_radix, hradix⟩ with
    ⟨z, hz⟩
  refine ⟨⟨z, LSB (beta:=beta) radix x2⟩, hz.symm, ?_⟩
  exact hlsb

private lemma midpoint_abs_y_lt_lsb {beta : Int} [ValidRadix beta]
    (radix : Int)
    (x2 y : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta_radix : beta = radix) (hradix : 1 < radix)
    (hy_lsb : MSB (beta:=beta) radix y < LSB (beta:=beta) radix x2) :
    |_root_.F2R (beta:=beta) y| <
      (radix : ℝ) ^ (LSB (beta:=beta) radix x2) := by
  have hbeta : 1 < beta := by omega
  have habs :
      _root_.F2R (beta:=beta) (Fabs (beta:=beta) y) =
        |_root_.F2R (beta:=beta) y| := by
    have h := Fabs_correct (beta:=beta) y
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fabs_correct_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h hbeta
  have hnext := abs_lt_MSB (beta:=beta) radix y hbeta_radix hradix
  have hnext' :
      |_root_.F2R (beta:=beta) y| <
        (radix : ℝ) ^ (Int.succ (MSB (beta:=beta) radix y)) := by
    calc
      |_root_.F2R (beta:=beta) y|
          = _root_.F2R (beta:=beta) (Fabs (beta:=beta) y) := habs.symm
      _ < _root_.F2R (beta:=beta)
          (⟨1, Int.succ (MSB (beta:=beta) radix y)⟩ :
            FloatSpec.Core.Defs.FlocqFloat beta) := hnext
      _ = (radix : ℝ) ^ (Int.succ (MSB (beta:=beta) radix y)) := by
            simp [_root_.F2R, FloatSpec.Core.Defs.F2R, hbeta_radix]
  have hindex :
      Int.succ (MSB (beta:=beta) radix y) ≤
        LSB (beta:=beta) radix x2 := by
    change MSB (beta:=beta) radix y + 1 ≤ LSB (beta:=beta) radix x2
    omega
  have hbase : (1 : ℝ) ≤ (radix : ℝ) := by
    exact_mod_cast (le_of_lt hradix)
  exact lt_of_lt_of_le hnext' (zpow_le_zpow_right₀ hbase hindex)

private lemma midpoint_abs_y_lt_abs_x2 {beta : Int} [ValidRadix beta]
    (radix : Int)
    (x2 y : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta_radix : beta = radix) (hradix : 1 < radix)
    (hx2_nonzero : ¬ is_Fzero x2)
    (hy_lsb : MSB (beta:=beta) radix y < LSB (beta:=beta) radix x2) :
    |_root_.F2R (beta:=beta) y| < |_root_.F2R (beta:=beta) x2| := by
  have hbeta : 1 < beta := by omega
  have hunit_le_abs :
      (radix : ℝ) ^ (LSB (beta:=beta) radix x2) ≤
        |_root_.F2R (beta:=beta) x2| := by
    have h := LSB_le_abs (beta:=beta) radix x2 hbeta_radix hradix hx2_nonzero
    have habs :
        _root_.F2R (beta:=beta) (Fabs (beta:=beta) x2) =
          |_root_.F2R (beta:=beta) x2| := by
      have h' := Fabs_correct (beta:=beta) x2
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fabs_correct_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using h' hbeta
    calc
      (radix : ℝ) ^ (LSB (beta:=beta) radix x2)
          = _root_.F2R (beta:=beta)
              (⟨1, LSB (beta:=beta) radix x2⟩ :
                FloatSpec.Core.Defs.FlocqFloat beta) := by
                  simp [_root_.F2R, FloatSpec.Core.Defs.F2R, hbeta_radix]
      _ ≤ _root_.F2R (beta:=beta) (Fabs (beta:=beta) x2) := h
      _ = |_root_.F2R (beta:=beta) x2| := habs
  exact lt_of_lt_of_le
    (midpoint_abs_y_lt_lsb (beta:=beta) radix x2 y hbeta_radix hradix hy_lsb)
    hunit_le_abs

private lemma midpoint_small_residual
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (x1 x2 y : FloatSpec.Core.Defs.FlocqFloat radix) (e : Int)
    (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix precision)
    (hprecision : 1 < precision) (hEven : Even radix)
    (hx2_nonzero : ¬ is_Fzero x2)
    (hy_lsb : MSB (beta:=radix) radix y < LSB (beta:=radix) radix x2)
    (hlsb_not : ¬ x1.Fexp - 2 ≤ LSB (beta:=radix) radix x2)
    (he : x1.Fexp - 2 ≤ e - 1)
    (hx2_bound :
      |_root_.F2R (beta:=radix) x2| ≤ (radix : ℝ) ^ e / 2) :
    |_root_.F2R (beta:=radix) x2 + _root_.F2R (beta:=radix) y| <
      (radix : ℝ) ^ e / 2 := by
  rcases hEven with ⟨halfRadix, hradix_half⟩
  let half : FloatSpec.Core.Defs.FlocqFloat radix := ⟨halfRadix, e - 1⟩
  have hradix_pos_int : (0 : Int) < radix := by omega
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradix_pos_int
  have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
  have hhalf_nonneg : (0 : Int) ≤ halfRadix := by omega
  have hhalf_cast : (halfRadix : ℝ) = (radix : ℝ) / 2 := by
    have hcast : (radix : ℝ) = (halfRadix : ℝ) + (halfRadix : ℝ) := by
      exact_mod_cast hradix_half
    nlinarith
  have hhalf_val :
      _root_.F2R (beta:=radix) half = (radix : ℝ) ^ e / 2 := by
    have hpow :
        (radix : ℝ) ^ (e - 1) * (radix : ℝ) =
          (radix : ℝ) ^ e := by
      calc
        (radix : ℝ) ^ (e - 1) * (radix : ℝ)
            = (radix : ℝ) ^ (e - 1) * (radix : ℝ) ^ (1 : Int) := by
                rw [zpow_one]
        _ = (radix : ℝ) ^ (e - 1 + 1) := by
              rw [zpow_add₀ hradix_ne]
        _ = (radix : ℝ) ^ e := by congr 1 <;> omega
    calc
      _root_.F2R (beta:=radix) half
          = (halfRadix : ℝ) * (radix : ℝ) ^ (e - 1) := by
              simp [half, _root_.F2R, FloatSpec.Core.Defs.F2R]
      _ = ((radix : ℝ) / 2) * (radix : ℝ) ^ (e - 1) := by
            rw [hhalf_cast]
      _ = (radix : ℝ) ^ e / 2 := by rw [← hpow]; ring
  have hhalf_pos : 0 < _root_.F2R (beta:=radix) half := by
    rw [hhalf_val]
    positivity
  have hhalf_abs :
      |_root_.F2R (beta:=radix) half| = (radix : ℝ) ^ e / 2 := by
    rw [abs_of_pos hhalf_pos, hhalf_val]
  have hx2_abs_lt_half :
      |_root_.F2R (beta:=radix) x2| <
        |_root_.F2R (beta:=radix) half| := by
    rw [hhalf_abs]
    apply lt_of_le_of_ne hx2_bound
    intro heq
    have hx2_abs_nonzero : ¬ is_Fzero (Fabs (beta:=radix) x2) := by
      have h := Fabs_Fzero (beta:=radix) x2
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fabs_Fzero_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using h hx2_nonzero
    have habs_x2 :
        _root_.F2R (beta:=radix) (Fabs (beta:=radix) x2) =
          |_root_.F2R (beta:=radix) x2| := by
      have h := Fabs_correct (beta:=radix) x2
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fabs_correct_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using h hradix
    have hsame_value :
        _root_.F2R (beta:=radix) (Fabs (beta:=radix) x2) =
          _root_.F2R (beta:=radix) half := by
      rw [habs_x2, hhalf_val]
      exact heq
    have hlsb_eq :
        LSB (beta:=radix) radix (Fabs (beta:=radix) x2) =
          LSB (beta:=radix) radix half :=
      (LSB_comp (beta:=radix) radix (Fabs (beta:=radix) x2) half 0)
        ⟨rfl, hradix, hx2_abs_nonzero, hsame_value⟩
    have hlsb_abs :
        LSB (beta:=radix) radix x2 =
          LSB (beta:=radix) radix (Fabs (beta:=radix) x2) :=
      (LSB_abs (beta:=radix) radix x2) trivial
    have hhalf_exp_le : e - 1 ≤ LSB (beta:=radix) radix half :=
      (Fexp_le_LSB (beta:=radix) radix half) trivial
    apply hlsb_not
    rw [hlsb_abs, hlsb_eq]
    exact le_trans he hhalf_exp_le
  have hx2_lsb_le_half :
      LSB (beta:=radix) radix x2 ≤ LSB (beta:=radix) radix half := by
    have hx2_lsb_lt : LSB (beta:=radix) radix x2 < x1.Fexp - 2 :=
      lt_of_not_ge hlsb_not
    have hhalf_exp_le : e - 1 ≤ LSB (beta:=radix) radix half :=
      (Fexp_le_LSB (beta:=radix) radix half) trivial
    omega
  have hx2_gap :
      |_root_.F2R (beta:=radix) x2| ≤
        |_root_.F2R (beta:=radix) half| -
          (radix : ℝ) ^ (LSB (beta:=radix) radix x2) := by
    have h := LSB_Pred (beta:=radix) b radix precision x2 half
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, LSB_Pred_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, hradix, hvNum, hprecision, ⟨halfRadix, hradix_half⟩,
        hx2_abs_lt_half, hx2_lsb_le_half⟩
  have hy_small := midpoint_abs_y_lt_lsb (beta:=radix) radix x2 y rfl
    hradix hy_lsb
  calc
    |_root_.F2R (beta:=radix) x2 + _root_.F2R (beta:=radix) y|
        ≤ |_root_.F2R (beta:=radix) x2| +
            |_root_.F2R (beta:=radix) y| := abs_add_le _ _
    _ < (|_root_.F2R (beta:=radix) half| -
          (radix : ℝ) ^ (LSB (beta:=radix) radix x2)) +
        (radix : ℝ) ^ (LSB (beta:=radix) radix x2) :=
      add_lt_add_of_le_of_lt hx2_gap hy_small
    _ = (radix : ℝ) ^ e / 2 := by rw [hhalf_abs]; ring

private lemma midpoint_positive_normal_bounds
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (x : FloatSpec.Core.Defs.FlocqFloat radix)
    (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix precision)
    (hprecision : 1 < precision)
    (hxNormal : Fnormal (beta:=radix) radix b x)
    (hxPos : 0 < _root_.F2R (beta:=radix) x) :
    (radix : ℝ) ^ (x.Fexp + (precision : Int) - 1) ≤
        _root_.F2R (beta:=radix) x ∧
      _root_.F2R (beta:=radix) x + (radix : ℝ) ^ x.Fexp ≤
        (radix : ℝ) ^ (x.Fexp + (precision : Int)) ∧
      (x.Fnum ≠ nNormMin radix precision →
        (radix : ℝ) ^ (x.Fexp + (precision : Int) - 1) +
            (radix : ℝ) ^ x.Fexp ≤
          _root_.F2R (beta:=radix) x) ∧
      (x.Fnum = nNormMin radix precision →
        _root_.F2R (beta:=radix) x =
          (radix : ℝ) ^ (x.Fexp + (precision : Int) - 1)) := by
  have hprecision_ne : precision ≠ 0 := by omega
  have hprecision_pos : 0 < precision := by omega
  have hradix_pos_int : (0 : Int) < radix := by omega
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast hradix_pos_int
  have hradix_ne : (radix : ℝ) ≠ 0 := ne_of_gt hradix_pos
  have hstep_pos : 0 < (radix : ℝ) ^ x.Fexp := zpow_pos hradix_pos _
  have hxnum_pos : 0 < x.Fnum := by
    have h := LtR0Fnum (beta:=radix) x
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, LtR0Fnum_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨hradix, hxPos⟩
  have hnormal_num : b.vNum ≤ radix * x.Fnum := by
    have hprod_nonneg : 0 ≤ radix * x.Fnum := by positivity
    simpa [abs_of_nonneg hprod_nonneg] using hxNormal.2
  have hnorm_min_eq : b.vNum = radix * nNormMin radix precision :=
    PosNormMin radix b precision hprecision_pos hvNum
  have hmin_le_num : nNormMin radix precision ≤ x.Fnum := by
    rw [hnorm_min_eq] at hnormal_num
    nlinarith
  have hnum_lt_vnum : x.Fnum < b.vNum := by
    simpa [abs_of_pos hxnum_pos] using hxNormal.1.1
  have hnum_succ_le_vnum : x.Fnum + 1 ≤ b.vNum := by omega
  have hnorm_min_cast :
      (nNormMin radix precision : ℝ) =
        (radix : ℝ) ^ ((precision : Int) - 1) := by
    have hpred_cast : ((precision - 1 : Nat) : Int) = (precision : Int) - 1 := by
      omega
    unfold nNormMin
    rw [Int.cast_pow, ← zpow_natCast, hpred_cast]
  have hvnum_cast :
      (b.vNum : ℝ) = (radix : ℝ) ^ (precision : Int) := by
    rw [hvNum]
    have h := Zpower_nat_Z_powerRZ radix precision
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Zpower_nat_Z_powerRZ_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h trivial
  have hlower_value :
      (radix : ℝ) ^ (x.Fexp + (precision : Int) - 1) =
        (nNormMin radix precision : ℝ) * (radix : ℝ) ^ x.Fexp := by
    rw [hnorm_min_cast, ← zpow_add₀ hradix_ne]
    congr 1
    ring
  have hupper_value :
      (radix : ℝ) ^ (x.Fexp + (precision : Int)) =
        (b.vNum : ℝ) * (radix : ℝ) ^ x.Fexp := by
    rw [hvnum_cast, ← zpow_add₀ hradix_ne]
    congr 1
    ring
  have hx_value :
      _root_.F2R (beta:=radix) x =
        (x.Fnum : ℝ) * (radix : ℝ) ^ x.Fexp := by
    simp [_root_.F2R, FloatSpec.Core.Defs.F2R]
  have hmin_le_real :
      (nNormMin radix precision : ℝ) ≤ (x.Fnum : ℝ) := by
    exact_mod_cast hmin_le_num
  have hsucc_le_real : (x.Fnum : ℝ) + 1 ≤ (b.vNum : ℝ) := by
    exact_mod_cast hnum_succ_le_vnum
  constructor
  · rw [hlower_value, hx_value]
    exact mul_le_mul_of_nonneg_right hmin_le_real (le_of_lt hstep_pos)
  constructor
  · rw [hx_value, hupper_value]
    have hmul := mul_le_mul_of_nonneg_right hsucc_le_real (le_of_lt hstep_pos)
    nlinarith
  constructor
  · intro hnot_min
    have hmin_succ_le : nNormMin radix precision + 1 ≤ x.Fnum := by omega
    have hmin_succ_le_real :
        (nNormMin radix precision : ℝ) + 1 ≤ (x.Fnum : ℝ) := by
      exact_mod_cast hmin_succ_le
    rw [hlower_value, hx_value]
    have hmul :=
      mul_le_mul_of_nonneg_right hmin_succ_le_real (le_of_lt hstep_pos)
    nlinarith
  · intro hmin
    rw [hx_value, hlower_value, hmin]

private lemma midpoint_closest_eq
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (x1 x2 y f : FloatSpec.Core.Defs.FlocqFloat radix) (e : Int)
    (hradix : 1 < radix) (hprecision : 1 < precision)
    (hvNum : b.vNum = Zpower_nat radix precision)
    (hxBound : Fbounded (beta:=radix) b x1)
    (hxCanonic : Fcanonic (beta:=radix) radix b x1)
    (htargetLower :
      (radix : ℝ) ^ (e + (precision : Int) - 1) ≤
        _root_.F2R (beta:=radix) x1 + _root_.F2R (beta:=radix) x2 +
          _root_.F2R (beta:=radix) y)
    (htargetUpper :
      _root_.F2R (beta:=radix) x1 + _root_.F2R (beta:=radix) x2 +
          _root_.F2R (beta:=radix) y ≤
        (radix : ℝ) ^ (e + (precision : Int)))
    (hxLower :
      (radix : ℝ) ^ (e + (precision : Int) - 1) ≤
        _root_.F2R (beta:=radix) x1)
    (heBound : -b.dExp ≤ e)
    (hsmall :
      |_root_.F2R (beta:=radix) x2 + _root_.F2R (beta:=radix) y| <
        (radix : ℝ) ^ e / 2)
    (hfClosest : Closest (beta:=radix) b (radix : ℝ)
      (_root_.F2R (beta:=radix) x1 + _root_.F2R (beta:=radix) x2 +
        _root_.F2R (beta:=radix) y) f) :
    _root_.F2R (beta:=radix) x1 = _root_.F2R (beta:=radix) f := by
  have hdist :
      |(_root_.F2R (beta:=radix) x1 + _root_.F2R (beta:=radix) x2 +
          _root_.F2R (beta:=radix) y) - _root_.F2R (beta:=radix) x1| <
        (radix : ℝ) ^ e / 2 := by
    have hresidual :
        (_root_.F2R (beta:=radix) x1 + _root_.F2R (beta:=radix) x2 +
            _root_.F2R (beta:=radix) y) - _root_.F2R (beta:=radix) x1 =
          _root_.F2R (beta:=radix) x2 + _root_.F2R (beta:=radix) y := by
      ring
    rw [hresidual]
    exact hsmall
  have h := ImplyClosestStrict (beta:=radix) b radix precision
    (_root_.F2R (beta:=radix) x1 + _root_.F2R (beta:=radix) x2 +
      _root_.F2R (beta:=radix) y) x1 e
  have hall :
      ∀ g : FloatSpec.Core.Defs.FlocqFloat radix,
        Closest (beta:=radix) b (radix : ℝ)
            (_root_.F2R (beta:=radix) x1 + _root_.F2R (beta:=radix) x2 +
              _root_.F2R (beta:=radix) y) g →
          _root_.F2R (beta:=radix) x1 = _root_.F2R (beta:=radix) g := by
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ImplyClosestStrict_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using
      h ⟨hxBound, hxCanonic, htargetLower, htargetUpper, hxLower, heBound,
        rfl, hradix, hprecision, hvNum, hdist⟩
  exact hall f hfClosest

/-- Coq GenericC lemma `Midpoint_aux_aux`.

If the second perturbation is too small in its most-significant bit to overlap
the least-significant bit of `x2`, then either the second closest rounding
still represents `x1`, or `x2` has an equivalent representation whose
exponent is at least two below that of `x1`. -/
theorem Midpoint_aux_aux {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (x1 x2 y f : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta_radix : beta = radix)
    (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix precision)
    (hprecision : 1 < precision)
    (hEven : Even radix)
    (hx1Closest : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x1 + _root_.F2R (beta:=beta) x2) x1)
    (hfClosest : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x1 + _root_.F2R (beta:=beta) x2 +
        _root_.F2R (beta:=beta) y) f)
    (hy_lsb : MSB (beta:=beta) radix y < LSB (beta:=beta) radix x2)
    (hx1Normal : Fnormal (beta:=beta) radix b x1)
    (hx1Pos : 0 < _root_.F2R (beta:=beta) x1)
    (hx2ValueNonzero : _root_.F2R (beta:=beta) x2 ≠ 0)
    (hx1Exp : -b.dExp < x1.Fexp) :
    _root_.F2R (beta:=beta) x1 = _root_.F2R (beta:=beta) f ∨
      ∃ v : FloatSpec.Core.Defs.FlocqFloat beta,
        _root_.F2R (beta:=beta) v = _root_.F2R (beta:=beta) x2 ∧
          x1.Fexp - 2 ≤ v.Fexp := by
  subst beta
  have hprecision_ne : precision ≠ 0 := by omega
  have hradix_pos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (by omega : (0 : Int) < radix)
  have hx2_nonzero : ¬ is_Fzero x2 := by
    intro hx2_zero
    have hzero : _root_.F2R (beta:=radix) x2 = 0 := by
      have h := is_Fzero_rep1 (beta:=radix) x2
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, is_Fzero_rep1_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using h hx2_zero
    exact hx2ValueNonzero hzero
  by_cases hlsb : x1.Fexp - 2 ≤ LSB (beta:=radix) radix x2
  · exact Or.inr
      (midpoint_witness_of_lsb (beta:=radix) radix x1 x2 rfl hradix hlsb)
  have hx1Canonic : Fcanonic (beta:=radix) radix b x1 := Or.inl hx1Normal
  have hnormalBounds := midpoint_positive_normal_bounds b radix precision x1
    hradix hvNum hprecision hx1Normal hx1Pos
  have hx1Normalize :
      Fnormalize (beta:=radix) radix b precision x1 = x1 := by
    have h := FcanonicFnormalizeEq (beta:=radix) radix b precision x1
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hx1Canonic, rfl, hradix, hprecision_ne, hvNum⟩
  have hfulp :
      Fulp (beta:=radix) b radix precision x1 =
        (radix : ℝ) ^ x1.Fexp := by
    simp [Fulp, hx1Normalize]
  have hx2_bound :
      |_root_.F2R (beta:=radix) x2| ≤
        (radix : ℝ) ^ x1.Fexp / 2 := by
    have h := ClosestUlp (beta:=radix) b radix precision
      (_root_.F2R (beta:=radix) x1 + _root_.F2R (beta:=radix) x2) x1
    have hdist :
        2 * |(_root_.F2R (beta:=radix) x1 +
            _root_.F2R (beta:=radix) x2) -
              _root_.F2R (beta:=radix) x1| ≤
          Fulp (beta:=radix) b radix precision x1 := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using
        h ⟨hx1Closest, rfl, hradix, hprecision_ne, hvNum⟩
    rw [hfulp] at hdist
    have hdist' :
        2 * |_root_.F2R (beta:=radix) x2| ≤
          (radix : ℝ) ^ x1.Fexp := by
      have hresidual :
          (_root_.F2R (beta:=radix) x1 + _root_.F2R (beta:=radix) x2) -
              _root_.F2R (beta:=radix) x1 =
            _root_.F2R (beta:=radix) x2 := by
        ring
      rw [hresidual] at hdist
      exact hdist
    nlinarith
  have hy_abs_lt_x2 := midpoint_abs_y_lt_abs_x2 (beta:=radix) radix x2 y
    rfl hradix hx2_nonzero hy_lsb
  have hupper_for_exp :
      ∀ hsmall :
          |_root_.F2R (beta:=radix) x2 + _root_.F2R (beta:=radix) y| <
            (radix : ℝ) ^ x1.Fexp / 2,
        _root_.F2R (beta:=radix) x1 + _root_.F2R (beta:=radix) x2 +
            _root_.F2R (beta:=radix) y ≤
          (radix : ℝ) ^ (x1.Fexp + (precision : Int)) := by
    intro hsmall
    have hpert_lt :
        _root_.F2R (beta:=radix) x2 + _root_.F2R (beta:=radix) y <
          (radix : ℝ) ^ x1.Fexp := by
      have hle := le_abs_self
        (_root_.F2R (beta:=radix) x2 + _root_.F2R (beta:=radix) y)
      nlinarith [zpow_pos hradix_pos x1.Fexp]
    linarith [hnormalBounds.2.1]
  by_cases hx1_min : x1.Fnum = nNormMin radix precision
  · by_cases hx2_nonneg : 0 ≤ _root_.F2R (beta:=radix) x2
    · have hx2_pos : 0 < _root_.F2R (beta:=radix) x2 :=
        lt_of_le_of_ne hx2_nonneg (Ne.symm hx2ValueNonzero)
      have hpert_nonneg :
          0 ≤ _root_.F2R (beta:=radix) x2 +
            _root_.F2R (beta:=radix) y := by
        have hy_abs_lt_x2' :
            |_root_.F2R (beta:=radix) y| <
              _root_.F2R (beta:=radix) x2 := by
          simpa [abs_of_pos hx2_pos] using hy_abs_lt_x2
        have hy_lower := (abs_lt.mp hy_abs_lt_x2').1
        linarith
      have hsmall := midpoint_small_residual b radix precision x1 x2 y
        x1.Fexp hradix hvNum hprecision hEven hx2_nonzero hy_lsb hlsb
        (by omega) hx2_bound
      have htarget_lower :
          (radix : ℝ) ^ (x1.Fexp + (precision : Int) - 1) ≤
            _root_.F2R (beta:=radix) x1 +
              _root_.F2R (beta:=radix) x2 +
                _root_.F2R (beta:=radix) y := by
        linarith [hnormalBounds.1]
      exact Or.inl <| midpoint_closest_eq b radix precision x1 x2 y f
        x1.Fexp hradix hprecision hvNum hx1Normal.1 hx1Canonic
        htarget_lower (hupper_for_exp hsmall) hnormalBounds.1
        (by omega) hsmall hfClosest
    · have hx2_neg : _root_.F2R (beta:=radix) x2 < 0 :=
        lt_of_not_ge hx2_nonneg
      have hpert_nonpos :
          _root_.F2R (beta:=radix) x2 +
              _root_.F2R (beta:=radix) y ≤ 0 := by
        have hy_abs_lt_x2' :
            |_root_.F2R (beta:=radix) y| <
              -_root_.F2R (beta:=radix) x2 := by
          simpa [abs_of_neg hx2_neg] using hy_abs_lt_x2
        have hy_upper := (abs_lt.mp hy_abs_lt_x2').2
        linarith
      let pred : FloatSpec.Core.Defs.FlocqFloat radix :=
        FPred (beta:=radix) b radix precision x1
      have hpred_bound : Fbounded (beta:=radix) b pred := by
        have h := FBoundedPred (beta:=radix) b radix precision x1
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FBoundedPred_check, Id.run,
          ULift.up_down, pred, Int.cast_ofNat] using
          h ⟨hx1Normal.1, hradix, hprecision_ne, hvNum⟩
      have hx1_exp_ne_min : x1.Fexp ≠ -b.dExp := by omega
      have hpred_gap :
          _root_.F2R (beta:=radix) x1 - _root_.F2R (beta:=radix) pred =
            (radix : ℝ) ^ (x1.Fexp - 1) := by
        have hdiff := FPredDiff3 (beta:=radix) b radix precision x1
          rfl hradix hprecision_ne hvNum hx1_min hx1_exp_ne_min
        have hminus :
            _root_.F2R (beta:=radix) (Fminus (beta:=radix) x1 pred) =
              _root_.F2R (beta:=radix) x1 -
                _root_.F2R (beta:=radix) pred := by
          have h := Fminus_correct (beta:=radix) x1 pred
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus,
            Id.run, ULift.up_down, Fminus, Int.cast_ofNat] using h hradix
        calc
          _root_.F2R (beta:=radix) x1 - _root_.F2R (beta:=radix) pred
              = _root_.F2R (beta:=radix) (Fminus (beta:=radix) x1 pred) :=
                hminus.symm
          _ = _root_.F2R (beta:=radix)
                (⟨1, x1.Fexp - 1⟩ : FloatSpec.Core.Defs.FlocqFloat radix) :=
              hdiff
          _ = (radix : ℝ) ^ (x1.Fexp - 1) := by
                simp [_root_.F2R, FloatSpec.Core.Defs.F2R]
      have hpred_value :
          _root_.F2R (beta:=radix) pred =
            _root_.F2R (beta:=radix) x1 -
              (radix : ℝ) ^ (x1.Fexp - 1) := by
        linarith [hpred_gap]
      have hdist := hx1Closest.2 pred hpred_bound
      rw [hpred_value] at hdist
      have hleft_expr :
          _root_.F2R (beta:=radix) x1 -
              (_root_.F2R (beta:=radix) x1 +
                _root_.F2R (beta:=radix) x2) =
            -_root_.F2R (beta:=radix) x2 := by
        ring
      have hright_expr :
          _root_.F2R (beta:=radix) x1 -
                (radix : ℝ) ^ (x1.Fexp - 1) -
              (_root_.F2R (beta:=radix) x1 +
                _root_.F2R (beta:=radix) x2) =
            -((radix : ℝ) ^ (x1.Fexp - 1) +
              _root_.F2R (beta:=radix) x2) := by
        ring
      rw [hleft_expr, hright_expr, abs_neg, abs_neg] at hdist
      have hdist' :
          |_root_.F2R (beta:=radix) x2| ≤
            |(radix : ℝ) ^ (x1.Fexp - 1) +
              _root_.F2R (beta:=radix) x2| := by
        exact hdist
      have hsq :
          (_root_.F2R (beta:=radix) x2) ^ 2 ≤
            ((radix : ℝ) ^ (x1.Fexp - 1) +
              _root_.F2R (beta:=radix) x2) ^ 2 := by
        simpa [sq_abs] using (sq_le_sq.mpr hdist')
      have hprev_pos : 0 < (radix : ℝ) ^ (x1.Fexp - 1) :=
        zpow_pos hradix_pos _
      have hx2_bound_prev :
          |_root_.F2R (beta:=radix) x2| ≤
            (radix : ℝ) ^ (x1.Fexp - 1) / 2 := by
        rw [abs_of_neg hx2_neg]
        nlinarith [hsq]
      have hsmall := midpoint_small_residual b radix precision x1 x2 y
        (x1.Fexp - 1) hradix hvNum hprecision hEven hx2_nonzero hy_lsb
        hlsb (by omega) hx2_bound_prev
      have hx1_value :
          _root_.F2R (beta:=radix) x1 =
            (radix : ℝ) ^ (x1.Fexp + (precision : Int) - 1) :=
        hnormalBounds.2.2.2 hx1_min
      have hsum := powerRZSumRle radix
        (x1.Fexp + (precision : Int) - 2) (x1.Fexp - 1)
        hradix (by omega)
      have hsum' :
          (radix : ℝ) ^ ((x1.Fexp - 1) + (precision : Int) - 1) +
              (radix : ℝ) ^ (x1.Fexp - 1) ≤
            (radix : ℝ) ^ (x1.Fexp + (precision : Int) - 1) := by
        have he1 :
            (x1.Fexp - 1) + (precision : Int) - 1 =
              x1.Fexp + (precision : Int) - 2 := by
          ring
        have hrhs :
            x1.Fexp + (precision : Int) - 2 + 1 =
              x1.Fexp + (precision : Int) - 1 := by
          ring
        rw [he1, ← hrhs]
        exact hsum
      have htarget_lower :
          (radix : ℝ) ^ ((x1.Fexp - 1) + (precision : Int) - 1) ≤
            _root_.F2R (beta:=radix) x1 +
              _root_.F2R (beta:=radix) x2 +
                _root_.F2R (beta:=radix) y := by
        have hpert_lower :
            -(radix : ℝ) ^ (x1.Fexp - 1) <
              _root_.F2R (beta:=radix) x2 +
                _root_.F2R (beta:=radix) y := by
          have hneg_abs := neg_abs_le
            (_root_.F2R (beta:=radix) x2 + _root_.F2R (beta:=radix) y)
          nlinarith [zpow_pos hradix_pos (x1.Fexp - 1)]
        rw [hx1_value]
        nlinarith [hsum']
      have htarget_upper :
          _root_.F2R (beta:=radix) x1 +
                _root_.F2R (beta:=radix) x2 +
                  _root_.F2R (beta:=radix) y ≤
            (radix : ℝ) ^ ((x1.Fexp - 1) + (precision : Int)) := by
        rw [hx1_value]
        have hexp :
            x1.Fexp + (precision : Int) - 1 =
              (x1.Fexp - 1) + (precision : Int) := by
          ring
        rw [hexp]
        simpa only [add_assoc, Int.cast_ofNat] using add_le_of_nonpos_right hpert_nonpos
      have hx1_lower_prev :
          (radix : ℝ) ^ ((x1.Fexp - 1) + (precision : Int) - 1) ≤
            _root_.F2R (beta:=radix) x1 := by
        rw [hx1_value]
        exact zpow_le_zpow_right₀
          (by exact_mod_cast (le_of_lt hradix) : (1 : ℝ) ≤ (radix : ℝ))
          (by omega)
      exact Or.inl <| midpoint_closest_eq b radix precision x1 x2 y f
        (x1.Fexp - 1) hradix hprecision hvNum hx1Normal.1 hx1Canonic
        htarget_lower htarget_upper hx1_lower_prev (by omega) hsmall hfClosest
  · have hsmall := midpoint_small_residual b radix precision x1 x2 y
      x1.Fexp hradix hvNum hprecision hEven hx2_nonzero hy_lsb hlsb
      (by omega) hx2_bound
    have hpert_lower :
        -(radix : ℝ) ^ x1.Fexp <
          _root_.F2R (beta:=radix) x2 + _root_.F2R (beta:=radix) y := by
      have hneg_abs := neg_abs_le
        (_root_.F2R (beta:=radix) x2 + _root_.F2R (beta:=radix) y)
      nlinarith [zpow_pos hradix_pos x1.Fexp]
    have htarget_lower :
        (radix : ℝ) ^ (x1.Fexp + (precision : Int) - 1) ≤
          _root_.F2R (beta:=radix) x1 +
            _root_.F2R (beta:=radix) x2 +
              _root_.F2R (beta:=radix) y := by
      linarith [hnormalBounds.2.2.1 hx1_min]
    exact Or.inl <| midpoint_closest_eq b radix precision x1 x2 y f
      x1.Fexp hradix hprecision hvNum hx1Normal.1 hx1Canonic
      htarget_lower (hupper_for_exp hsmall) hnormalBounds.1
      (by omega) hsmall hfClosest

/-- Coq GenericD lemma `Midpoint_aux`.

This is the sign-symmetric public wrapper around `Midpoint_aux_aux`. -/
theorem Midpoint_aux {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (x1 x2 y f : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta_radix : beta = radix)
    (hradix : 1 < radix)
    (hvNum : b.vNum = Zpower_nat radix precision)
    (hprecision : 1 < precision)
    (hEven : Even radix)
    (hx1Closest : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x1 + _root_.F2R (beta:=beta) x2) x1)
    (hfClosest : Closest (beta:=beta) b (radix : ℝ)
      (_root_.F2R (beta:=beta) x1 + _root_.F2R (beta:=beta) x2 +
        _root_.F2R (beta:=beta) y) f)
    (hy_lsb : MSB (beta:=beta) radix y < LSB (beta:=beta) radix x2)
    (hx1Normal : Fnormal (beta:=beta) radix b x1)
    (hx2ValueNonzero : _root_.F2R (beta:=beta) x2 ≠ 0)
    (hx1Exp : -b.dExp < x1.Fexp) :
    _root_.F2R (beta:=beta) x1 = _root_.F2R (beta:=beta) f ∨
      ∃ v : FloatSpec.Core.Defs.FlocqFloat beta,
        _root_.F2R (beta:=beta) v = _root_.F2R (beta:=beta) x2 ∧
          x1.Fexp - 2 ≤ v.Fexp := by
  subst beta
  have hradix_pos_int : (0 : Int) < radix := by omega
  have hvNum_pos : 0 < b.vNum := by
    rw [hvNum]
    simpa [Zpower_nat] using (pow_pos hradix_pos_int precision)
  rcases lt_trichotomy (_root_.F2R (beta:=radix) x1) 0 with
      hx1Neg | hx1Zero | hx1Pos
  · let ox1 : FloatSpec.Core.Defs.FlocqFloat radix := Fopp (beta:=radix) x1
    let ox2 : FloatSpec.Core.Defs.FlocqFloat radix := Fopp (beta:=radix) x2
    let oy : FloatSpec.Core.Defs.FlocqFloat radix := Fopp (beta:=radix) y
    let of : FloatSpec.Core.Defs.FlocqFloat radix := Fopp (beta:=radix) f
    have hx1OppReal :
        _root_.F2R (beta:=radix) ox1 =
          -_root_.F2R (beta:=radix) x1 := by
      have h := Fopp_correct (beta:=radix) x1
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fopp_correct_check, Id.run,
        ULift.up_down, ox1, Int.cast_ofNat] using h trivial
    have hx2OppReal :
        _root_.F2R (beta:=radix) ox2 =
          -_root_.F2R (beta:=radix) x2 := by
      have h := Fopp_correct (beta:=radix) x2
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fopp_correct_check, Id.run,
        ULift.up_down, ox2, Int.cast_ofNat] using h trivial
    have hyOppReal :
        _root_.F2R (beta:=radix) oy =
          -_root_.F2R (beta:=radix) y := by
      have h := Fopp_correct (beta:=radix) y
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fopp_correct_check, Id.run,
        ULift.up_down, oy, Int.cast_ofNat] using h trivial
    have hfOppReal :
        _root_.F2R (beta:=radix) of =
          -_root_.F2R (beta:=radix) f := by
      have h := Fopp_correct (beta:=radix) f
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fopp_correct_check, Id.run,
        ULift.up_down, of, Int.cast_ofNat] using h trivial
    have hx1OppPos : 0 < _root_.F2R (beta:=radix) ox1 := by
      rw [hx1OppReal]
      linarith
    have hx1ClosestOpp : Closest (beta:=radix) b (radix : ℝ)
        (_root_.F2R (beta:=radix) ox1 + _root_.F2R (beta:=radix) ox2) ox1 := by
      have h := ClosestOpp (beta:=radix) b (radix : ℝ) x1
        (_root_.F2R (beta:=radix) x1 + _root_.F2R (beta:=radix) x2)
      have hraw : Closest (beta:=radix) b (radix : ℝ)
          (-(_root_.F2R (beta:=radix) x1 + _root_.F2R (beta:=radix) x2)) ox1 := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestOpp_check,
          Id.run, ULift.up_down, ox1, Int.cast_ofNat] using h hx1Closest
      have harg :
          _root_.F2R (beta:=radix) ox1 + _root_.F2R (beta:=radix) ox2 =
            -(_root_.F2R (beta:=radix) x1 + _root_.F2R (beta:=radix) x2) := by
        rw [hx1OppReal, hx2OppReal]
        ring
      simpa [harg] using hraw
    have hfClosestOpp : Closest (beta:=radix) b (radix : ℝ)
        (_root_.F2R (beta:=radix) ox1 + _root_.F2R (beta:=radix) ox2 +
          _root_.F2R (beta:=radix) oy) of := by
      have h := ClosestOpp (beta:=radix) b (radix : ℝ) f
        (_root_.F2R (beta:=radix) x1 + _root_.F2R (beta:=radix) x2 +
          _root_.F2R (beta:=radix) y)
      have hraw : Closest (beta:=radix) b (radix : ℝ)
          (-(_root_.F2R (beta:=radix) x1 + _root_.F2R (beta:=radix) x2 +
            _root_.F2R (beta:=radix) y)) of := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestOpp_check,
          Id.run, ULift.up_down, of, Int.cast_ofNat] using h hfClosest
      have harg :
          _root_.F2R (beta:=radix) ox1 + _root_.F2R (beta:=radix) ox2 +
              _root_.F2R (beta:=radix) oy =
            -(_root_.F2R (beta:=radix) x1 + _root_.F2R (beta:=radix) x2 +
              _root_.F2R (beta:=radix) y) := by
        rw [hx1OppReal, hx2OppReal, hyOppReal]
        ring
      simpa [harg] using hraw
    have hy_lsb_opp : MSB (beta:=radix) radix oy < LSB (beta:=radix) radix ox2 := by
      have hmsb := MSB_opp (beta:=radix) radix y
      have hmsb' : MSB (beta:=radix) radix y = MSB (beta:=radix) radix oy := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MSB_opp_check, Id.run,
          ULift.up_down, oy, Int.cast_ofNat] using hmsb trivial
      have hlsb := LSB_opp (beta:=radix) radix x2
      have hlsb' : LSB (beta:=radix) radix x2 = LSB (beta:=radix) radix ox2 := by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, LSB_opp_check, Id.run,
          ULift.up_down, ox2, Int.cast_ofNat] using hlsb trivial
      simpa [← hmsb', ← hlsb'] using hy_lsb
    have hx1NormalOpp : Fnormal (beta:=radix) radix b ox1 := by
      have h := FnormalFop (beta:=radix) b radix x1
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalFop_check, Id.run,
        ULift.up_down, ox1, Int.cast_ofNat] using h hx1Normal
    have hx2OppValueNonzero : _root_.F2R (beta:=radix) ox2 ≠ 0 := by
      intro hzero
      apply hx2ValueNonzero
      rw [hx2OppReal] at hzero
      linarith
    have hx1OppExp : -b.dExp < ox1.Fexp := by
      simpa [ox1, Fopp, FloatSpec.Calc.Operations.Fopp] using hx1Exp
    have haux := Midpoint_aux_aux b radix precision ox1 ox2 oy of
      rfl hradix hvNum hprecision hEven hx1ClosestOpp hfClosestOpp
      hy_lsb_opp hx1NormalOpp hx1OppPos hx2OppValueNonzero hx1OppExp
    rcases haux with hEq | ⟨v, hvReal, hvExp⟩
    · left
      rw [hx1OppReal, hfOppReal] at hEq
      linarith
    · right
      refine ⟨Fopp (beta:=radix) v, ?_, ?_⟩
      · have hvOppReal :
            _root_.F2R (beta:=radix) (Fopp (beta:=radix) v) =
              -_root_.F2R (beta:=radix) v := by
          have h := Fopp_correct (beta:=radix) v
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fopp_correct_check, Id.run,
            ULift.up_down, Int.cast_ofNat] using h trivial
        rw [hvOppReal, hvReal, hx2OppReal]
        ring
      · simpa [ox1, Fopp, FloatSpec.Calc.Operations.Fopp] using hvExp
  · have hx1NotFzero : ¬ is_Fzero x1 := by
      have h := FnormalNotZero_from_expanded_normal_payload (beta:=radix) b radix x1 radix hvNum_pos
        hx1Normal.2
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalNotZero_check,
        ULift.down_up, Int.cast_ofNat] using h hx1Normal
    have hx1Fzero : is_Fzero x1 := by
      have h := is_Fzero_rep2 (beta:=radix) x1
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, is_Fzero_rep2_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using h ⟨hradix, hx1Zero⟩
    exact False.elim (hx1NotFzero hx1Fzero)
  · exact Midpoint_aux_aux b radix precision x1 x2 y f
      rfl hradix hvNum hprecision hEven hx1Closest hfClosest hy_lsb
      hx1Normal hx1Pos hx2ValueNonzero hx1Exp

noncomputable def MSBroundLSB_check {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (f1 f2 : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq: `MSBroundLSB_from_rounded_payload`.

For a nonzero rounding error, the most significant bit of the error lies
strictly below the least significant bit of the rounded value. -/
theorem MSBroundLSB_from_rounded_payload {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (f1 f2 : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ precision ≠ 0 ∧
        b.vNum = Zpower_nat radix precision ∧
        RoundedModeP_float (beta:=beta) b radix P ∧
        P (_root_.F2R (beta:=beta) f1) f2 ∧
        ¬ is_Fzero (Fminus (beta:=beta) f1 f2)⌝⦄
    (pure (MSBroundLSB_check (beta:=beta) b radix precision P f1 f2) : Id Unit)
    ⦃⇓_ => ⌜MSB (beta:=beta) radix (Fminus (beta:=beta) f1 f2) <
      LSB (beta:=beta) radix f2⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hradix, hprecision, hvNum, hRounded, hP, hdiff_nonzero⟩
  simp only [wp, PostCond.noThrow, pure, MSBroundLSB_check, Id.run,
    ULift.up_down]
  let d := Fminus (beta:=beta) f1 f2
  have hβ : 1 < beta := by omega
  have hf2_bounded : Fbounded (beta:=beta) b f2 := by
    rcases hRounded.2.2.1 (_root_.F2R (beta:=beta) f1) f2 hP with hMin | hMax
    · exact hMin.1
    · exact hMax.1
  have hmsb_le_abs :
      _root_.F2R (beta:=beta)
          (⟨1, MSB (beta:=beta) radix d⟩ :
            FloatSpec.Core.Defs.FlocqFloat beta) ≤
        _root_.F2R (beta:=beta) (Fabs (beta:=beta) d) :=
    MSB_le_abs (beta:=beta) radix d hbeta hradix
      (by simpa [d] using hdiff_nonzero)
  have habs_eq :
      _root_.F2R (beta:=beta) (Fabs (beta:=beta) d) =
        |_root_.F2R (beta:=beta) d| := by
    have h := Fabs_correct (beta:=beta) d
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fabs_correct_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h hβ
  have hminus_eq :
      _root_.F2R (beta:=beta) d =
        _root_.F2R (beta:=beta) f1 - _root_.F2R (beta:=beta) f2 := by
    have h := Fminus_correct (beta:=beta) f1 f2
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus, Id.run,
      ULift.up_down, d, Fminus, Int.cast_ofNat] using h hβ
  have hround_ulp :
      |_root_.F2R (beta:=beta) f1 - _root_.F2R (beta:=beta) f2| <
        Fulp (beta:=beta) b radix precision f2 := by
    have h := RoundedModeUlp_from_rounded_payload (beta:=beta) b radix precision P
      (_root_.F2R (beta:=beta) f1) f2
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RoundedModeUlp_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using
      h ⟨hRounded, hP, hbeta, hradix, hprecision, hvNum⟩
  have hfulp_lsb :
      Fulp (beta:=beta) b radix precision f2 ≤
        _root_.F2R (beta:=beta)
          (⟨1, LSB (beta:=beta) radix f2⟩ :
            FloatSpec.Core.Defs.FlocqFloat beta) :=
    FUlp_Le_LSigB (beta:=beta) b radix precision f2 hbeta hradix hf2_bounded
  have hunit_lt :
      _root_.F2R (beta:=beta)
          (⟨1, MSB (beta:=beta) radix d⟩ :
            FloatSpec.Core.Defs.FlocqFloat beta) <
        _root_.F2R (beta:=beta)
          (⟨1, LSB (beta:=beta) radix f2⟩ :
            FloatSpec.Core.Defs.FlocqFloat beta) := by
    calc
      _root_.F2R (beta:=beta)
          (⟨1, MSB (beta:=beta) radix d⟩ :
            FloatSpec.Core.Defs.FlocqFloat beta)
          ≤ _root_.F2R (beta:=beta) (Fabs (beta:=beta) d) := hmsb_le_abs
      _ = |_root_.F2R (beta:=beta) d| := habs_eq
      _ = |_root_.F2R (beta:=beta) f1 - _root_.F2R (beta:=beta) f2| := by
            rw [hminus_eq]
      _ < Fulp (beta:=beta) b radix precision f2 := hround_ulp
      _ ≤ _root_.F2R (beta:=beta)
          (⟨1, LSB (beta:=beta) radix f2⟩ :
            FloatSpec.Core.Defs.FlocqFloat beta) := hfulp_lsb
  exact
    (oneExp_Zlt (beta:=beta) (MSB (beta:=beta) radix d)
      (LSB (beta:=beta) radix f2)) ⟨hβ, hunit_lt⟩

-- Coq: `LSBMinus`.
noncomputable def LSBMinus_check {beta : Int} [ValidRadix beta]
    (radix : Int)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem LSBMinus {beta : Int} [ValidRadix beta]
    (radix : Int)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧
        ¬ is_Fzero (FloatSpec.Calc.Operations.Fminus (beta:=beta) p q)⌝⦄
    (pure (LSBMinus_check (beta:=beta) radix p q) : Id Unit)
    ⦃⇓_ => ⌜min (LSB (beta:=beta) radix p) (LSB (beta:=beta) radix q) ≤
        LSB (beta:=beta) radix
          (FloatSpec.Calc.Operations.Fminus (beta:=beta) p q)⌝⦄ := by
  intro ⟨hbeta_radix, hradix, hnonzero⟩
  simp only [wp, PostCond.noThrow, pure, LSBMinus_check, Fminus, Id.run,
    ULift.up_down]
  let p' : FloatSpec.Core.Defs.FlocqFloat beta :=
    ⟨Classical.choose ((LSB_rep_min (beta:=beta) radix p) ⟨hbeta_radix, hradix⟩),
      LSB (beta:=beta) radix p⟩
  let q' : FloatSpec.Core.Defs.FlocqFloat beta :=
    ⟨Classical.choose ((LSB_rep_min (beta:=beta) radix q) ⟨hbeta_radix, hradix⟩),
      LSB (beta:=beta) radix q⟩
  have hp_rep :
      _root_.F2R (beta:=beta) p = _root_.F2R (beta:=beta) p' :=
    Classical.choose_spec ((LSB_rep_min (beta:=beta) radix p) ⟨hbeta_radix, hradix⟩)
  have hq_rep :
      _root_.F2R (beta:=beta) q = _root_.F2R (beta:=beta) q' :=
    Classical.choose_spec ((LSB_rep_min (beta:=beta) radix q) ⟨hbeta_radix, hradix⟩)
  have hbeta : 1 < beta := by omega
  have hminus_pq :
      _root_.F2R (FloatSpec.Calc.Operations.Fminus (beta:=beta) p q) =
        _root_.F2R p - _root_.F2R q :=
    (Fminus_correct (beta:=beta) p q) hbeta
  have hminus_rep :
      _root_.F2R (FloatSpec.Calc.Operations.Fminus (beta:=beta) p' q') =
        _root_.F2R p' - _root_.F2R q' :=
    (Fminus_correct (beta:=beta) p' q') hbeta
  have hreal :
      _root_.F2R (FloatSpec.Calc.Operations.Fminus (beta:=beta) p q) =
        _root_.F2R (FloatSpec.Calc.Operations.Fminus (beta:=beta) p' q') := by
    calc
      _root_.F2R (FloatSpec.Calc.Operations.Fminus (beta:=beta) p q)
          = _root_.F2R p - _root_.F2R q := hminus_pq
      _ = _root_.F2R p' - _root_.F2R q' := by rw [hp_rep, hq_rep]
      _ = _root_.F2R (FloatSpec.Calc.Operations.Fminus (beta:=beta) p' q') :=
          hminus_rep.symm
  have hlsb :
      LSB (beta:=beta) radix
          (FloatSpec.Calc.Operations.Fminus (beta:=beta) p q) =
        LSB (beta:=beta) radix
          (FloatSpec.Calc.Operations.Fminus (beta:=beta) p' q') :=
    (LSB_comp (beta:=beta) radix
      (FloatSpec.Calc.Operations.Fminus (beta:=beta) p q)
      (FloatSpec.Calc.Operations.Fminus (beta:=beta) p' q') 0)
      ⟨hbeta_radix, hradix, hnonzero, hreal⟩
  have hle :
      (FloatSpec.Calc.Operations.Fminus (beta:=beta) p' q').Fexp ≤
        LSB (beta:=beta) radix
          (FloatSpec.Calc.Operations.Fminus (beta:=beta) p' q') :=
    (Fexp_le_LSB (beta:=beta) radix
      (FloatSpec.Calc.Operations.Fminus (beta:=beta) p' q')) trivial
  have hexp :
      (FloatSpec.Calc.Operations.Fminus (beta:=beta) p' q').Fexp =
        min (LSB (beta:=beta) radix p) (LSB (beta:=beta) radix q) := by
    by_cases hle_lsb :
        LSB (beta:=beta) radix p ≤ LSB (beta:=beta) radix q
    · simp [p', q', FloatSpec.Calc.Operations.Fminus,
        FloatSpec.Calc.Operations.Fplus, FloatSpec.Calc.Operations.Fopp,
        FloatSpec.Calc.Operations.Falign, hle_lsb, min_eq_left hle_lsb]
    · have hq_le :
          LSB (beta:=beta) radix q ≤ LSB (beta:=beta) radix p :=
        le_of_lt (lt_of_not_ge hle_lsb)
      simp [p', q', FloatSpec.Calc.Operations.Fminus,
        FloatSpec.Calc.Operations.Fplus, FloatSpec.Calc.Operations.Fopp,
        FloatSpec.Calc.Operations.Falign, hle_lsb, min_eq_right hq_le]
  rw [hlsb]
  simpa [hexp] using hle

-- Coq: `LSBPlus`.
noncomputable def LSBPlus_check {beta : Int} [ValidRadix beta]
    (radix : Int)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

theorem LSBPlus {beta : Int} [ValidRadix beta]
    (radix : Int)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ ¬ is_Fzero (Fplus (beta:=beta) p q)⌝⦄
    (pure (LSBPlus_check (beta:=beta) radix p q) : Id Unit)
    ⦃⇓_ => ⌜min (LSB (beta:=beta) radix p) (LSB (beta:=beta) radix q) ≤
        LSB (beta:=beta) radix (Fplus (beta:=beta) p q)⌝⦄ := by
  intro ⟨hbeta_radix, hradix, hnonzero⟩
  simp only [wp, PostCond.noThrow, pure, LSBPlus_check, Id.run,
    ULift.up_down]
  let p' : FloatSpec.Core.Defs.FlocqFloat beta :=
    ⟨Classical.choose ((LSB_rep_min (beta:=beta) radix p) ⟨hbeta_radix, hradix⟩),
      LSB (beta:=beta) radix p⟩
  let q' : FloatSpec.Core.Defs.FlocqFloat beta :=
    ⟨Classical.choose ((LSB_rep_min (beta:=beta) radix q) ⟨hbeta_radix, hradix⟩),
      LSB (beta:=beta) radix q⟩
  have hp_rep :
      _root_.F2R (beta:=beta) p = _root_.F2R (beta:=beta) p' :=
    Classical.choose_spec ((LSB_rep_min (beta:=beta) radix p) ⟨hbeta_radix, hradix⟩)
  have hq_rep :
      _root_.F2R (beta:=beta) q = _root_.F2R (beta:=beta) q' :=
    Classical.choose_spec ((LSB_rep_min (beta:=beta) radix q) ⟨hbeta_radix, hradix⟩)
  have hbeta : 1 < beta := by omega
  have hplus_pq :
      _root_.F2R (Fplus (beta:=beta) p q) =
        _root_.F2R p + _root_.F2R q :=
    (Fplus_correct (beta:=beta) p q) hbeta
  have hplus_rep :
      _root_.F2R (Fplus (beta:=beta) p' q') =
        _root_.F2R p' + _root_.F2R q' :=
    (Fplus_correct (beta:=beta) p' q') hbeta
  have hreal :
      _root_.F2R (Fplus (beta:=beta) p q) =
        _root_.F2R (Fplus (beta:=beta) p' q') := by
    calc
      _root_.F2R (Fplus (beta:=beta) p q)
          = _root_.F2R p + _root_.F2R q := hplus_pq
      _ = _root_.F2R p' + _root_.F2R q' := by rw [hp_rep, hq_rep]
      _ = _root_.F2R (Fplus (beta:=beta) p' q') := hplus_rep.symm
  have hlsb :
      LSB (beta:=beta) radix (Fplus (beta:=beta) p q) =
        LSB (beta:=beta) radix (Fplus (beta:=beta) p' q') :=
    (LSB_comp (beta:=beta) radix (Fplus (beta:=beta) p q)
      (Fplus (beta:=beta) p' q') 0)
      ⟨hbeta_radix, hradix, hnonzero, hreal⟩
  have hle :
      (Fplus (beta:=beta) p' q').Fexp ≤
        LSB (beta:=beta) radix (Fplus (beta:=beta) p' q') :=
    (Fexp_le_LSB (beta:=beta) radix (Fplus (beta:=beta) p' q')) trivial
  have hexp :
      (Fplus (beta:=beta) p' q').Fexp =
        min (LSB (beta:=beta) radix p) (LSB (beta:=beta) radix q) := by
    by_cases hle_lsb :
        LSB (beta:=beta) radix p ≤ LSB (beta:=beta) radix q
    · simp [p', q', Fplus, FloatSpec.Calc.Operations.Fplus,
        FloatSpec.Calc.Operations.Falign, hle_lsb, min_eq_left hle_lsb]
    · have hq_le :
          LSB (beta:=beta) radix q ≤ LSB (beta:=beta) radix p :=
        le_of_lt (lt_of_not_ge hle_lsb)
      simp [p', q', Fplus, FloatSpec.Calc.Operations.Fplus,
        FloatSpec.Calc.Operations.Falign, hle_lsb, min_eq_right hq_le]
  rw [hlsb]
  simpa [hexp] using hle

/-- Coq Be2NonZero lemma `be2MuchSmaller`.

When the second residuals `al2`, `u2`, and `be2` are all nonzero, the most
significant bit of `al2` is strictly below the least significant bit of `be2`.
This is the upstream transport proof: compare `be2` with
`(u1 + al1) - be1`, push the least-significant-bit lower bound through
`LSBMinus` and `LSBPlus`, obtain the two residual MSB bounds from
`MSBroundLSB_from_rounded_payload`, `TwoSumProp`, and exact representation transport, and rule out
the remaining `be1` exponent case with `plusExact1`. -/
theorem be2MuchSmaller {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a x y u1 u2 al1 al2 be1 be2 :
      FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : bo.vNum = Zpower_nat radix precision)
    (hprecision_ge : 3 ≤ precision)
    (hyBound : Fbounded (beta:=beta) bo y)
    (hu1Can : Fcanonic (beta:=beta) radix bo u1)
    (hal1Can : Fcanonic (beta:=beta) radix bo al1)
    (hu1Def : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) u1)
    (hu2Def : _root_.F2R (beta:=beta) u2 =
      _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
        _root_.F2R (beta:=beta) u1)
    (hal1Def : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) u2) al1)
    (hal2Def : _root_.F2R (beta:=beta) al2 =
      _root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) u2 -
        _root_.F2R (beta:=beta) al1)
    (hbe1Def : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) u1 + _root_.F2R (beta:=beta) al1) be1)
    (hbe2Def : _root_.F2R (beta:=beta) be2 =
      _root_.F2R (beta:=beta) u1 + _root_.F2R (beta:=beta) al1 -
        _root_.F2R (beta:=beta) be1)
    (hal2Nonzero : _root_.F2R (beta:=beta) al2 ≠ 0)
    (hu2Nonzero : _root_.F2R (beta:=beta) u2 ≠ 0)
    (hbe2Nonzero : _root_.F2R (beta:=beta) be2 ≠ 0) :
    MSB (beta:=beta) radix al2 < LSB (beta:=beta) radix be2 := by
  subst beta
  have hBoundExp :
      ∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=radix) radix r).Fexp := by
    intro r
    exact le_trans (neg_nonpos.mpr bo.dExp_nonneg) (by simp [boundR, boundNat])
  have hprecision_ne : precision ≠ 0 := by omega
  have hvNum_pos : 0 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact pow_pos (by omega : 0 < radix) precision
  have hvNum_gt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecision_ne
  have hMinTotal : TotalP (isMin (beta:=radix) bo radix) := by
    intro r
    have h := MinEx_from_finite_box_payload (beta:=radix) bo radix r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hvNum_gt, hBoundExp r⟩
  have hMaxTotal : TotalP (isMax (beta:=radix) bo radix) := by
    intro r
    have h := MaxEx_from_finite_box_payload (beta:=radix) bo radix r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MaxEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hvNum_gt, hBoundExp r⟩
  have hTotal : TotalP (Closest (beta:=radix) bo (radix : ℝ)) := by
    intro r
    have h := ClosestTotal_from_extrema_payload (beta:=radix) bo radix (radix : ℝ) r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestTotal_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨hMinTotal, hMaxTotal⟩
  have hradix_real_gt : (1 : ℝ) < (radix : ℝ) := by exact_mod_cast hradix
  have hradix_real_ge : (1 : ℝ) ≤ (radix : ℝ) := le_of_lt hradix_real_gt
  have hu1Bound : Fbounded (beta:=radix) bo u1 := by
    have h := FcanonicBound (beta:=radix) radix bo u1
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicBound_check, Int.cast_ofNat] using h hu1Can
  have hal1Bound : Fbounded (beta:=radix) bo al1 := by
    have h := FcanonicBound (beta:=radix) radix bo al1
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicBound_check, Int.cast_ofNat] using h hal1Can
  have hRoundedClosest :
      RoundedModeP_float (beta:=radix) bo radix
        (Closest (beta:=radix) bo (radix : ℝ)) := by
    refine ⟨hTotal, ?_, ?_, ?_⟩
    · have h := ClosestCompatible_from_structural_payload (beta:=radix) bo (radix : ℝ)
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestCompatible_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
    · have h := ClosestMinOrMax_from_split_radix_payload (beta:=radix) bo radix (radix : ℝ)
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMinOrMax_check, MinOrMaxP_float,
        Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
    · have h := ClosestMonotone_from_split_radix_payload (beta:=radix) bo (radix : ℝ)
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMonotone_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
  have hal2NotFzero : ¬ is_Fzero al2 := by
    intro hzero
    have h := is_Fzero_rep1 (beta:=radix) al2
    exact hal2Nonzero (by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, is_Fzero_rep1_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h hzero)
  have hu2NotFzero : ¬ is_Fzero u2 := by
    intro hzero
    have h := is_Fzero_rep1 (beta:=radix) u2
    exact hu2Nonzero (by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, is_Fzero_rep1_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h hzero)
  let prod : FloatSpec.Core.Defs.FlocqFloat radix :=
    FloatSpec.Calc.Operations.Fmult (beta:=radix) a x
  let sumAl : FloatSpec.Core.Defs.FlocqFloat radix :=
    Fplus (beta:=radix) y u2
  let sumBe : FloatSpec.Core.Defs.FlocqFloat radix :=
    Fplus (beta:=radix) u1 al1
  let be2Diff : FloatSpec.Core.Defs.FlocqFloat radix :=
    Fminus (beta:=radix) sumBe be1
  have hprodVal :
      _root_.F2R (beta:=radix) prod =
        _root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x := by
    have h := Fmult_correct (beta:=radix) a x
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fmult_correct_check,
      Id.run, ULift.up_down, prod, Int.cast_ofNat] using h hradix
  have hsumAlVal :
      _root_.F2R (beta:=radix) sumAl =
        _root_.F2R (beta:=radix) y + _root_.F2R (beta:=radix) u2 := by
    have h := Fplus_correct (beta:=radix) y u2
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fplus_correct_check,
      Id.run, ULift.up_down, sumAl, Int.cast_ofNat] using h hradix
  have hsumBeVal :
      _root_.F2R (beta:=radix) sumBe =
        _root_.F2R (beta:=radix) u1 + _root_.F2R (beta:=radix) al1 := by
    have h := Fplus_correct (beta:=radix) u1 al1
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fplus_correct_check,
      Id.run, ULift.up_down, sumBe, Int.cast_ofNat] using h hradix
  have hbe2DiffVal :
      _root_.F2R (beta:=radix) be2Diff =
        _root_.F2R (beta:=radix) u1 + _root_.F2R (beta:=radix) al1 -
          _root_.F2R (beta:=radix) be1 := by
    have h := Fminus_correct (beta:=radix) sumBe be1
    calc
      _root_.F2R (beta:=radix) be2Diff
          = _root_.F2R (beta:=radix) sumBe -
              _root_.F2R (beta:=radix) be1 := by
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus,
              Id.run, ULift.up_down, be2Diff, Int.cast_ofNat] using h hradix
      _ = _root_.F2R (beta:=radix) u1 + _root_.F2R (beta:=radix) al1 -
          _root_.F2R (beta:=radix) be1 := by rw [hsumBeVal]
  have hbe2Transport :
      _root_.F2R (beta:=radix) be2 =
        _root_.F2R (beta:=radix) be2Diff := by
    rw [hbe2Def, hbe2DiffVal]
  have hbe2DiffNotFzero : ¬ is_Fzero be2Diff := by
    intro hzero
    have h := is_Fzero_rep1 (beta:=radix) be2Diff
    have hdiffZero : _root_.F2R (beta:=radix) be2Diff = 0 := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, is_Fzero_rep1_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h hzero
    exact hbe2Nonzero (by rw [hbe2Transport, hdiffZero])
  have hsumBeRealNonzero :
      _root_.F2R (beta:=radix) sumBe ≠ 0 := by
    intro hsumZero
    have hsumParts :
        _root_.F2R (beta:=radix) u1 + _root_.F2R (beta:=radix) al1 = 0 := by
      rw [← hsumBeVal]
      exact hsumZero
    let z : FloatSpec.Core.Defs.FlocqFloat radix := Fzero radix (-bo.dExp)
    have hzBound : Fbounded (beta:=radix) bo z := by
      have h := FboundedFzero_from_positive_bound_payload (beta:=radix) bo
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FboundedFzero_check,
        Id.run, ULift.up_down, z, Int.cast_ofNat] using h hvNum_pos
    have hzVal : _root_.F2R (beta:=radix) z = 0 := by
      have h := FzeroisReallyZero (beta:=radix) (-bo.dExp)
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FzeroisReallyZero_check,
        Id.run, ULift.up_down, z, Int.cast_ofNat] using h True.intro
    have hRoundedFull :
        RoundedModeP_full (beta:=radix) bo
          (Closest (beta:=radix) bo (radix : ℝ)) := by
      have h := ClosestRoundedModeP_from_total_payload (beta:=radix) bo (radix : ℝ)
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestRoundedModeP_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h hTotal
    rcases hRoundedFull with ⟨_, _, _, _, hProjectorEq⟩
    have hbe1ClosestZero :
        Closest (beta:=radix) bo (radix : ℝ)
          (_root_.F2R (beta:=radix) z) be1 := by
      simpa [hzVal, hsumParts] using hbe1Def
    have hbe1Zero : _root_.F2R (beta:=radix) be1 = 0 := by
      have hproj := hProjectorEq z be1 hzBound hbe1ClosestZero
      simpa [hzVal] using hproj.symm
    have hbe2Zero : _root_.F2R (beta:=radix) be2 = 0 := by
      rw [hbe2Def, hsumParts, hbe1Zero]
      ring
    exact hbe2Nonzero hbe2Zero
  have hsumBeNotFzero : ¬ is_Fzero sumBe := by
    intro hzero
    have h := is_Fzero_rep1 (beta:=radix) sumBe
    exact hsumBeRealNonzero (by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, is_Fzero_rep1_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h hzero)
  have hal2LeU2 :
      |_root_.F2R (beta:=radix) al2| ≤ |_root_.F2R (beta:=radix) u2| := by
    have h := TwoSumProp (beta:=radix) bo (radix : ℝ) y u2 al1 al2
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, TwoSumProp_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨hyBound, hal1Def, hal2Def⟩
  have hu1RoundProd :
      Closest (beta:=radix) bo (radix : ℝ)
        (_root_.F2R (beta:=radix) prod) u1 := by
    simpa [hprodVal] using hu1Def
  have hal1RoundSumAl :
      Closest (beta:=radix) bo (radix : ℝ)
        (_root_.F2R (beta:=radix) sumAl) al1 := by
    simpa [hsumAlVal] using hal1Def
  have hu2AsDiff :
      _root_.F2R (beta:=radix) u2 =
        _root_.F2R (beta:=radix) prod - _root_.F2R (beta:=radix) u1 := by
    rw [hprodVal]
    exact hu2Def
  have hal2AsDiff :
      _root_.F2R (beta:=radix) al2 =
        _root_.F2R (beta:=radix) sumAl - _root_.F2R (beta:=radix) al1 := by
    rw [hsumAlVal]
    exact hal2Def
  have hprodDiff : _root_.F2R (beta:=radix)
        (Fminus (beta:=radix) prod u1) =
      _root_.F2R (beta:=radix) prod - _root_.F2R (beta:=radix) u1 := by
    have h := Fminus_correct (beta:=radix) prod u1
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hradix
  have hsumAlDiff : _root_.F2R (beta:=radix)
        (Fminus (beta:=radix) sumAl al1) =
      _root_.F2R (beta:=radix) sumAl - _root_.F2R (beta:=radix) al1 := by
    have h := Fminus_correct (beta:=radix) sumAl al1
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hradix
  have hprodDiffNotFzero : ¬ is_Fzero (Fminus (beta:=radix) prod u1) := by
    intro hzero
    have h := is_Fzero_rep1 (beta:=radix) (Fminus (beta:=radix) prod u1)
    have hdiffZero : _root_.F2R (beta:=radix) (Fminus (beta:=radix) prod u1) = 0 := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, is_Fzero_rep1_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h hzero
    exact hu2Nonzero (by rw [hu2AsDiff, ← hprodDiff, hdiffZero])
  have hsumAlDiffNotFzero : ¬ is_Fzero (Fminus (beta:=radix) sumAl al1) := by
    intro hzero
    have h := is_Fzero_rep1 (beta:=radix) (Fminus (beta:=radix) sumAl al1)
    have hdiffZero : _root_.F2R (beta:=radix) (Fminus (beta:=radix) sumAl al1) = 0 := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, is_Fzero_rep1_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h hzero
    exact hal2Nonzero (by rw [hal2AsDiff, ← hsumAlDiff, hdiffZero])
  have hmsbU2Diff :
      MSB (beta:=radix) radix u2 =
        MSB (beta:=radix) radix (Fminus (beta:=radix) prod u1) := by
    have h := MSB_comp (beta:=radix) radix u2 (Fminus (beta:=radix) prod u1) 0
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MSB_comp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, hradix, hu2NotFzero, by rw [hu2AsDiff, hprodDiff]⟩
  have hmsbAl2Diff :
      MSB (beta:=radix) radix al2 =
        MSB (beta:=radix) radix (Fminus (beta:=radix) sumAl al1) := by
    have h := MSB_comp (beta:=radix) radix al2 (Fminus (beta:=radix) sumAl al1) 0
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MSB_comp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, hradix, hal2NotFzero, by rw [hal2AsDiff, hsumAlDiff]⟩
  have hmsbU2LtLsbU1 :
      MSB (beta:=radix) radix u2 < LSB (beta:=radix) radix u1 := by
    have h := MSBroundLSB_from_rounded_payload (beta:=radix) bo radix precision
      (Closest (beta:=radix) bo (radix : ℝ)) prod u1
    have hraw :
        MSB (beta:=radix) radix (Fminus (beta:=radix) prod u1) <
          LSB (beta:=radix) radix u1 := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MSBroundLSB_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨rfl, hradix, hprecision_ne, hvNum, hRoundedClosest,
          hu1RoundProd, hprodDiffNotFzero⟩
    simpa [hmsbU2Diff] using hraw
  have hmsbAl2LtLsbAl1 :
      MSB (beta:=radix) radix al2 < LSB (beta:=radix) radix al1 := by
    have h := MSBroundLSB_from_rounded_payload (beta:=radix) bo radix precision
      (Closest (beta:=radix) bo (radix : ℝ)) sumAl al1
    have hraw :
        MSB (beta:=radix) radix (Fminus (beta:=radix) sumAl al1) <
          LSB (beta:=radix) radix al1 := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MSBroundLSB_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨rfl, hradix, hprecision_ne, hvNum, hRoundedClosest,
          hal1RoundSumAl, hsumAlDiffNotFzero⟩
    simpa [hmsbAl2Diff] using hraw
  have hmsbAl2LtLsbU1 :
      MSB (beta:=radix) radix al2 < LSB (beta:=radix) radix u1 :=
    lt_of_le_of_lt
      (MSB_monotone (beta:=radix) radix al2 u2 rfl hradix hal2NotFzero
        hu2NotFzero (by
          have hal2Abs :
              _root_.F2R (beta:=radix) (Fabs (beta:=radix) al2) =
                |_root_.F2R (beta:=radix) al2| := by
            have h := Fabs_correct (beta:=radix) al2
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fabs_correct_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using h hradix
          have hu2Abs :
              _root_.F2R (beta:=radix) (Fabs (beta:=radix) u2) =
                |_root_.F2R (beta:=radix) u2| := by
            have h := Fabs_correct (beta:=radix) u2
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fabs_correct_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using h hradix
          simpa [hal2Abs, hu2Abs] using hal2LeU2))
      hmsbU2LtLsbU1
  have hal2LtPowU1 :
      |_root_.F2R (beta:=radix) al2| < (radix : ℝ) ^ u1.Fexp := by
    have hu2RoundUlp :
        |_root_.F2R (beta:=radix) prod - _root_.F2R (beta:=radix) u1| <
          Fulp (beta:=radix) bo radix precision u1 := by
      have h := RoundedModeUlp_from_rounded_payload (beta:=radix) bo radix precision
        (Closest (beta:=radix) bo (radix : ℝ))
        (_root_.F2R (beta:=radix) prod) u1
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RoundedModeUlp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hRoundedClosest, hu1RoundProd, rfl, hradix, hprecision_ne, hvNum⟩
    have hfulp :
        Fulp (beta:=radix) bo radix precision u1 = (radix : ℝ) ^ u1.Fexp := by
      have h := CanonicFulp (beta:=radix) radix bo precision u1
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hu1Can, rfl, hradix, hprecision_ne, hvNum⟩
    have hu2Lt : |_root_.F2R (beta:=radix) u2| < (radix : ℝ) ^ u1.Fexp := by
      simpa [hu2AsDiff, hfulp] using hu2RoundUlp
    exact lt_of_le_of_lt hal2LeU2 hu2Lt
  have hal2LtPowAl1 :
      |_root_.F2R (beta:=radix) al2| < (radix : ℝ) ^ al1.Fexp := by
    have hal2RoundUlp :
        |_root_.F2R (beta:=radix) sumAl - _root_.F2R (beta:=radix) al1| <
          Fulp (beta:=radix) bo radix precision al1 := by
      have h := RoundedModeUlp_from_rounded_payload (beta:=radix) bo radix precision
        (Closest (beta:=radix) bo (radix : ℝ))
        (_root_.F2R (beta:=radix) sumAl) al1
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RoundedModeUlp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hRoundedClosest, hal1RoundSumAl, rfl, hradix, hprecision_ne, hvNum⟩
    have hfulp :
        Fulp (beta:=radix) bo radix precision al1 = (radix : ℝ) ^ al1.Fexp := by
      have h := CanonicFulp (beta:=radix) radix bo precision al1
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hal1Can, rfl, hradix, hprecision_ne, hvNum⟩
    simpa [hal2AsDiff, hfulp] using hal2RoundUlp
  have hbe1ExpGeMin : min u1.Fexp al1.Fexp ≤ be1.Fexp := by
    by_contra hnot
    have hbe1ExpLeMin : be1.Fexp ≤ min u1.Fexp al1.Fexp := by omega
    have hbe1Exact :
        _root_.F2R (beta:=radix) be1 =
          _root_.F2R (beta:=radix) u1 + _root_.F2R (beta:=radix) al1 := by
      exact plusExact1_from_format_payload (beta:=radix) bo radix precision rfl hradix hprecision_ne
        hvNum_gt hvNum u1 al1 be1 hu1Bound hal1Bound hbe1Def
        hbe1ExpLeMin
    have hbe2Zero : _root_.F2R (beta:=radix) be2 = 0 := by
      rw [hbe2Def, hbe1Exact]
      ring
    exact hbe2Nonzero hbe2Zero
  have hal2LtPowMin :
      |_root_.F2R (beta:=radix) al2| <
        (radix : ℝ) ^ (min u1.Fexp al1.Fexp) := by
    by_cases hle : u1.Fexp ≤ al1.Fexp
    · simpa [min_eq_left hle] using hal2LtPowU1
    · have hge : al1.Fexp ≤ u1.Fexp := le_of_lt (lt_of_not_ge hle)
      simpa [min_eq_right hge] using hal2LtPowAl1
  have hal2LtPowBe1 :
      |_root_.F2R (beta:=radix) al2| < (radix : ℝ) ^ be1.Fexp := by
    have hpow_le :
        (radix : ℝ) ^ (min u1.Fexp al1.Fexp) ≤
          (radix : ℝ) ^ be1.Fexp := by
      have h := Rle_powerRZ (radix : ℝ) (min u1.Fexp al1.Fexp) be1.Fexp
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Rle_powerRZ_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hradix_real_ge, hbe1ExpGeMin⟩
    exact lt_of_lt_of_le hal2LtPowMin hpow_le
  have hmsbAl2LtLsbBe1 :
      MSB (beta:=radix) radix al2 < LSB (beta:=radix) radix be1 := by
    have hunitMsbLeAbs :
        _root_.F2R (beta:=radix)
            (⟨1, MSB (beta:=radix) radix al2⟩ :
              FloatSpec.Core.Defs.FlocqFloat radix) ≤
          |_root_.F2R (beta:=radix) al2| := by
      have h := MSB_le_abs (beta:=radix) radix al2 rfl hradix hal2NotFzero
      have habs :
          _root_.F2R (beta:=radix) (Fabs (beta:=radix) al2) =
            |_root_.F2R (beta:=radix) al2| := by
        have hf := Fabs_correct (beta:=radix) al2
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fabs_correct_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using hf hradix
      simpa [habs] using h
    have hbe1ExpLeLsb : be1.Fexp ≤ LSB (beta:=radix) radix be1 := by
      exact (Fexp_le_LSB (beta:=radix) radix be1) trivial
    have hpowBe1LeUnitLsb :
        (radix : ℝ) ^ be1.Fexp ≤
          _root_.F2R (beta:=radix)
            (⟨1, LSB (beta:=radix) radix be1⟩ :
              FloatSpec.Core.Defs.FlocqFloat radix) := by
      have h := oneExp_le (beta:=radix) be1.Fexp (LSB (beta:=radix) radix be1)
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, oneExp_le_check,
        Id.run, ULift.up_down, _root_.F2R, FloatSpec.Core.Defs.F2R,
        Int.cast_one, one_mul, Int.cast_ofNat] using h ⟨hradix, hbe1ExpLeLsb⟩
    have hunitLt :
        _root_.F2R (beta:=radix)
            (⟨1, MSB (beta:=radix) radix al2⟩ :
              FloatSpec.Core.Defs.FlocqFloat radix) <
          _root_.F2R (beta:=radix)
            (⟨1, LSB (beta:=radix) radix be1⟩ :
              FloatSpec.Core.Defs.FlocqFloat radix) :=
      lt_of_le_of_lt hunitMsbLeAbs
        (lt_of_lt_of_le hal2LtPowBe1 hpowBe1LeUnitLsb)
    exact (oneExp_Zlt (beta:=radix) (MSB (beta:=radix) radix al2)
      (LSB (beta:=radix) radix be1)) ⟨hradix, hunitLt⟩
  have hmsbAl2LtLsbSumBe :
      MSB (beta:=radix) radix al2 < LSB (beta:=radix) radix sumBe := by
    have hltMin :
        MSB (beta:=radix) radix al2 <
          min (LSB (beta:=radix) radix u1) (LSB (beta:=radix) radix al1) :=
      lt_min hmsbAl2LtLsbU1 hmsbAl2LtLsbAl1
    have hlsbPlus :
        min (LSB (beta:=radix) radix u1) (LSB (beta:=radix) radix al1) ≤
          LSB (beta:=radix) radix sumBe := by
      have h := LSBPlus (beta:=radix) radix u1 al1
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, LSBPlus_check,
        Id.run, ULift.up_down, sumBe, Int.cast_ofNat] using
        h ⟨rfl, hradix, hsumBeNotFzero⟩
    exact lt_of_lt_of_le hltMin hlsbPlus
  have hmsbAl2LtMin :
      MSB (beta:=radix) radix al2 <
        min (LSB (beta:=radix) radix sumBe) (LSB (beta:=radix) radix be1) :=
    lt_min hmsbAl2LtLsbSumBe hmsbAl2LtLsbBe1
  have hlsbMinus :
      min (LSB (beta:=radix) radix sumBe) (LSB (beta:=radix) radix be1) ≤
        LSB (beta:=radix) radix be2Diff := by
    have h := LSBMinus (beta:=radix) radix sumBe be1
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, LSBMinus_check, Fminus,
      Id.run, ULift.up_down, be2Diff, Int.cast_ofNat] using
      h ⟨rfl, hradix, hbe2DiffNotFzero⟩
  have hlsbTransport :
      LSB (beta:=radix) radix be2 = LSB (beta:=radix) radix be2Diff := by
    have h := LSB_comp (beta:=radix) radix be2 be2Diff 0
    have hbe2NotFzero : ¬ is_Fzero be2 := by
      intro hzero
      have hz := is_Fzero_rep1 (beta:=radix) be2
      exact hbe2Nonzero (by
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, is_Fzero_rep1_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using hz hzero)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, LSB_comp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, hradix, hbe2NotFzero, hbe2Transport⟩
  rw [hlsbTransport]
  exact lt_of_lt_of_le hmsbAl2LtMin hlsbMinus

/-- Coq Be2NonZero lemma `gaCorrect`.

Construct the bounded correction float representing `be1 - r1 + be2`.  The
proof follows the upstream split: the zero residual branches use the rounded
projector facts, the midpoint branch either returns `be2` directly or supplies
an exact `be2` witness, and the remaining boundedness is obtained by applying
`BoundedL` to the exact sum of the `gatCorrect` witness and that midpoint
witness. -/
theorem gaCorrect {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (a x y r1 u1 u2 al1 al2 be1 be2 :
      FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : bo.vNum = Zpower_nat radix precision)
    (hprecision_ge : 3 ≤ precision) (hEven : Even radix)
    (hyBound : Fbounded (beta:=beta) bo y)
    (hbe1Normal : Fnormal (beta:=beta) radix bo be1)
    (hr1Normal : Fnormal (beta:=beta) radix bo r1)
    (hal1Can : Fcanonic (beta:=beta) radix bo al1)
    (hu1Can : Fcanonic (beta:=beta) radix bo u1)
    (hu1ExpGt : -bo.dExp < u1.Fexp)
    (hal1ExpGt : -bo.dExp < al1.Fexp)
    (hbe1ExpGt : -bo.dExp + 1 < be1.Fexp)
    (hCompat : ∀ r s : ℝ, ∀ f g : FloatSpec.Core.Defs.FlocqFloat beta,
      P r f → P s g → r = s →
        _root_.F2R (beta:=beta) f = _root_.F2R (beta:=beta) g)
    (hr1Def : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
        _root_.F2R (beta:=beta) y) r1)
    (hu1Def : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) u1)
    (hu2Def : _root_.F2R (beta:=beta) u2 =
      _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
        _root_.F2R (beta:=beta) u1)
    (hal1Def : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) u2) al1)
    (hal2Def : _root_.F2R (beta:=beta) al2 =
      _root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) u2 -
        _root_.F2R (beta:=beta) al1)
    (hbe1Def : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) u1 + _root_.F2R (beta:=beta) al1) be1)
    (hbe2Def : _root_.F2R (beta:=beta) be2 =
      _root_.F2R (beta:=beta) u1 + _root_.F2R (beta:=beta) al1 -
        _root_.F2R (beta:=beta) be1)
    (hbe2Bound : Fbounded (beta:=beta) bo be2)
    (hbe2Nonzero : _root_.F2R (beta:=beta) be2 ≠ 0)
    (hr1P : P
      (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
        _root_.F2R (beta:=beta) y) r1)
    (hbe1P : P
      (_root_.F2R (beta:=beta) u1 + _root_.F2R (beta:=beta) al1) be1) :
    ∃ v : FloatSpec.Core.Defs.FlocqFloat beta,
      _root_.F2R (beta:=beta) v =
        _root_.F2R (beta:=beta) be1 - _root_.F2R (beta:=beta) r1 +
          _root_.F2R (beta:=beta) be2 ∧
      Fbounded (beta:=beta) bo v := by
  subst beta
  have hBoundExp :
      ∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=radix) radix r).Fexp := by
    intro r
    exact le_trans (neg_nonpos.mpr bo.dExp_nonneg) (by simp [boundR, boundNat])
  have hprecision_ne : precision ≠ 0 := by omega
  have hprecision_gt_one : 1 < precision := by omega
  have hvNum_pos : 0 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact pow_pos (by omega : 0 < radix) precision
  have hvNum_gt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecision_ne
  have hMinTotal : TotalP (isMin (beta:=radix) bo radix) := by
    intro r
    have h := MinEx_from_finite_box_payload (beta:=radix) bo radix r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hvNum_gt, hBoundExp r⟩
  have hMaxTotal : TotalP (isMax (beta:=radix) bo radix) := by
    intro r
    have h := MaxEx_from_finite_box_payload (beta:=radix) bo radix r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MaxEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hvNum_gt, hBoundExp r⟩
  have hTotal : TotalP (Closest (beta:=radix) bo (radix : ℝ)) := by
    intro r
    have h := ClosestTotal_from_extrema_payload (beta:=radix) bo radix (radix : ℝ) r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestTotal_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨hMinTotal, hMaxTotal⟩
  by_cases hal2Zero : _root_.F2R (beta:=radix) al2 = 0
  · have h := FmaErr_gaCorrect_of_al2_zero
      (beta:=radix) bo P a x y r1 u1 u2 al1 al2 be1 be2
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
      FmaErr_gaCorrect_of_al2_zero_check, Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hCompat, hbe1P, hr1P, hbe2Bound, hal2Zero, hu2Def, hal2Def⟩
  · by_cases hu2Zero : _root_.F2R (beta:=radix) u2 = 0
    · have hal2Zero' : _root_.F2R (beta:=radix) al2 = 0 := by
        have h := FmaErr_al2_zero_of_u2_zero (beta:=radix) bo (radix : ℝ)
          y u2 al1 al2
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
          FmaErr_al2_zero_of_u2_zero_check, Id.run, ULift.up_down, Int.cast_ofNat] using
          h ⟨hTotal, hyBound, hu2Zero, hal1Def, hal2Def⟩
      exact False.elim (hal2Zero hal2Zero')
    · rcases gatCorrect (beta:=radix) bo radix precision
        a x y r1 u1 u2 al1 al2 be1 rfl hradix hvNum hprecision_ge hEven
        hyBound hbe1Normal hr1Normal hal1Can hu1Can hal1ExpGt
        hu1ExpGt hr1Def hu1Def hu2Def hal1Def hal2Def hbe1Def with
        ⟨gatW, hgatWVal, hgatWBound, hgatWExp⟩
      have hbe1ClosestMid :
          Closest (beta:=radix) bo (radix : ℝ)
            (_root_.F2R (beta:=radix) be1 +
              _root_.F2R (beta:=radix) be2) be1 := by
        have harg :
            _root_.F2R (beta:=radix) be1 +
                _root_.F2R (beta:=radix) be2 =
              _root_.F2R (beta:=radix) u1 +
                _root_.F2R (beta:=radix) al1 := by
          rw [hbe2Def]
          ring
        simpa [harg] using hbe1Def
      have hr1ClosestMid :
          Closest (beta:=radix) bo (radix : ℝ)
            (_root_.F2R (beta:=radix) be1 +
              _root_.F2R (beta:=radix) be2 +
                _root_.F2R (beta:=radix) al2) r1 := by
        have harg :
            _root_.F2R (beta:=radix) be1 +
                _root_.F2R (beta:=radix) be2 +
                  _root_.F2R (beta:=radix) al2 =
              _root_.F2R (beta:=radix) a *
                  _root_.F2R (beta:=radix) x +
                _root_.F2R (beta:=radix) y := by
          rw [hbe2Def, hal2Def, hu2Def]
          ring
        simpa [harg] using hr1Def
      have hbe2Small :
          MSB (beta:=radix) radix al2 < LSB (beta:=radix) radix be2 :=
        be2MuchSmaller (beta:=radix) bo radix precision
          a x y u1 u2 al1 al2 be1 be2 rfl hradix hvNum hprecision_ge
          hyBound hu1Can hal1Can hu1Def hu2Def hal1Def hal2Def
          hbe1Def hbe2Def hal2Zero hu2Zero hbe2Nonzero
      have hbe1ExpGt' : -bo.dExp < be1.Fexp := by omega
      rcases Midpoint_aux (beta:=radix) bo radix precision be1 be2 al2 r1
        rfl hradix hvNum hprecision_gt_one hEven hbe1ClosestMid
        hr1ClosestMid hbe2Small hbe1Normal hbe2Nonzero hbe1ExpGt' with
        hbe1EqR1 | ⟨be2W, hbe2WVal, hbe2WExp⟩
      · have h := FmaErr_gaCorrect_of_be1_eq_r1
          (beta:=radix) bo be1 r1 be2
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
          FmaErr_gaCorrect_of_be1_eq_r1_check, Id.run, ULift.up_down, Int.cast_ofNat] using
          h ⟨hbe2Bound, hbe1EqR1⟩
      · let sumW : FloatSpec.Core.Defs.FlocqFloat radix :=
          Fplus (beta:=radix) gatW be2W
        have hsumWVal :
            _root_.F2R (beta:=radix) sumW =
              _root_.F2R (beta:=radix) be1 -
                  _root_.F2R (beta:=radix) r1 +
                _root_.F2R (beta:=radix) be2 := by
          have hplus := Fplus_correct (beta:=radix) gatW be2W
          calc
            _root_.F2R (beta:=radix) sumW
                = _root_.F2R (beta:=radix) gatW +
                    _root_.F2R (beta:=radix) be2W := by
                  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil,
                    Fplus_correct_check, Id.run, ULift.up_down, sumW, Int.cast_ofNat] using
                    hplus hradix
            _ = _root_.F2R (beta:=radix) be1 -
                    _root_.F2R (beta:=radix) r1 +
                  _root_.F2R (beta:=radix) be2 := by
                  rw [hgatWVal, hbe2WVal]
        have hsumWExp :
            sumW.Fexp = min gatW.Fexp be2W.Fexp := by
          simpa only [sumW, Int.cast_ofNat] using Fplus_exp_eq_min (beta:=radix) gatW be2W
        have hExpr1 :
            r1.Fexp ≤ be1.Fexp + 1 :=
          Expr1 (beta:=radix) bo radix precision
            a x y r1 u1 u2 al1 al2 be1 rfl hradix hvNum hprecision_ge
            hyBound hbe1Normal hr1Normal hu1Can hal1Can hr1Def hu1Def
            hu2Def hal1Def hal2Def hbe1Def
        have hExpbe1 :
            be1.Fexp ≤ r1.Fexp + 1 :=
          Expbe1 (beta:=radix) bo radix precision
            a x y r1 u1 u2 al1 al2 be1 rfl hradix hvNum hprecision_ge
            hEven hyBound hbe1Normal hr1Normal hu1Can hal1Can
            hu1ExpGt hal1ExpGt hr1Def hu1Def hu2Def hal1Def hal2Def hbe1Def
        have hsumWExpLow : be1.Fexp - 2 ≤ sumW.Fexp := by
          rw [hsumWExp, hgatWExp]
          apply le_min
          · omega
          · omega
        have hradixRealGt : (1 : ℝ) < (radix : ℝ) := by exact_mod_cast hradix
        have hradixRealGe : (1 : ℝ) ≤ (radix : ℝ) := le_of_lt hradixRealGt
        have hbe1Can : Fcanonic (beta:=radix) radix bo be1 := Or.inl hbe1Normal
        have hr1Can : Fcanonic (beta:=radix) radix bo r1 := Or.inl hr1Normal
        have hr1ErrLe :
            |_root_.F2R (beta:=radix) a *
                  _root_.F2R (beta:=radix) x +
                _root_.F2R (beta:=radix) y -
                _root_.F2R (beta:=radix) r1| ≤
              (radix : ℝ) ^ (be1.Fexp + 1) / 2 := by
          have hclosest :
              2 * |_root_.F2R (beta:=radix) a *
                    _root_.F2R (beta:=radix) x +
                  _root_.F2R (beta:=radix) y -
                  _root_.F2R (beta:=radix) r1| ≤
                Fulp (beta:=radix) bo radix precision r1 := by
            have h := ClosestUlp (beta:=radix) bo radix precision
              (_root_.F2R (beta:=radix) a *
                  _root_.F2R (beta:=radix) x +
                _root_.F2R (beta:=radix) y) r1
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using
              h ⟨hr1Def, rfl, hradix, hprecision_ne, hvNum⟩
          have hfulp :
              Fulp (beta:=radix) bo radix precision r1 =
                (radix : ℝ) ^ r1.Fexp := by
            have h := CanonicFulp (beta:=radix) radix bo precision r1
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using
              h ⟨hr1Can, rfl, hradix, hprecision_ne, hvNum⟩
          have hpow :
              (radix : ℝ) ^ r1.Fexp ≤
                (radix : ℝ) ^ (be1.Fexp + 1) := by
            have h := Rle_powerRZ (radix : ℝ) r1.Fexp (be1.Fexp + 1)
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Rle_powerRZ_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hradixRealGe, hExpr1⟩
          nlinarith
        have hbe2ErrLe :
            |_root_.F2R (beta:=radix) be2| ≤
              (radix : ℝ) ^ be1.Fexp / 2 := by
          have hclosest :
              2 * |_root_.F2R (beta:=radix) u1 +
                    _root_.F2R (beta:=radix) al1 -
                    _root_.F2R (beta:=radix) be1| ≤
                Fulp (beta:=radix) bo radix precision be1 := by
            have h := ClosestUlp (beta:=radix) bo radix precision
              (_root_.F2R (beta:=radix) u1 +
                _root_.F2R (beta:=radix) al1) be1
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using
              h ⟨hbe1Def, rfl, hradix, hprecision_ne, hvNum⟩
          have hfulp :
              Fulp (beta:=radix) bo radix precision be1 =
                (radix : ℝ) ^ be1.Fexp := by
            have h := CanonicFulp (beta:=radix) radix bo precision be1
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using
              h ⟨hbe1Can, rfl, hradix, hprecision_ne, hvNum⟩
          have hbe2Abs :
              |_root_.F2R (beta:=radix) be2| =
                |_root_.F2R (beta:=radix) u1 +
                    _root_.F2R (beta:=radix) al1 -
                    _root_.F2R (beta:=radix) be1| := by
            rw [hbe2Def]
          nlinarith
        have hal2LeBe2 :
            |_root_.F2R (beta:=radix) al2| ≤
              |_root_.F2R (beta:=radix) be2| := by
          have hal2Abs :
              _root_.F2R (beta:=radix) (Fabs (beta:=radix) al2) =
                |_root_.F2R (beta:=radix) al2| := by
            have h := Fabs_correct (beta:=radix) al2
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fabs_correct_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using h hradix
          have hbe2Abs :
              _root_.F2R (beta:=radix) (Fabs (beta:=radix) be2) =
                |_root_.F2R (beta:=radix) be2| := by
            have h := Fabs_correct (beta:=radix) be2
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fabs_correct_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using h hradix
          have hltAbs :
              |_root_.F2R (beta:=radix) al2| <
                _root_.F2R (beta:=radix)
                  (⟨1, Int.succ (MSB (beta:=radix) radix al2)⟩ :
                    FloatSpec.Core.Defs.FlocqFloat radix) := by
            have h := abs_lt_MSB (beta:=radix) radix al2 rfl hradix
            simpa [hal2Abs] using h
          have hsuccLe :
              Int.succ (MSB (beta:=radix) radix al2) ≤
                LSB (beta:=radix) radix be2 := by
            change MSB (beta:=radix) radix al2 + 1 ≤
              LSB (beta:=radix) radix be2
            omega
          have hunitLe :
              _root_.F2R (beta:=radix)
                  (⟨1, Int.succ (MSB (beta:=radix) radix al2)⟩ :
                    FloatSpec.Core.Defs.FlocqFloat radix) ≤
                _root_.F2R (beta:=radix)
                  (⟨1, LSB (beta:=radix) radix be2⟩ :
                    FloatSpec.Core.Defs.FlocqFloat radix) := by
            have h := oneExp_le (beta:=radix)
              (Int.succ (MSB (beta:=radix) radix al2))
              (LSB (beta:=radix) radix be2)
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, oneExp_le_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hradix, hsuccLe⟩
          have hbe2NotFzero : ¬ is_Fzero be2 := by
            intro hzero
            have h := is_Fzero_rep1 (beta:=radix) be2
            exact hbe2Nonzero (by
              simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, is_Fzero_rep1_check,
                Id.run, ULift.up_down, Int.cast_ofNat] using h hzero)
          have hlsbLe :
              _root_.F2R (beta:=radix)
                  (⟨1, LSB (beta:=radix) radix be2⟩ :
                    FloatSpec.Core.Defs.FlocqFloat radix) ≤
                |_root_.F2R (beta:=radix) be2| := by
            have h := LSB_le_abs (beta:=radix) radix be2 rfl hradix
              hbe2NotFzero
            simpa [hbe2Abs] using h
          exact le_trans (le_of_lt hltAbs) (le_trans hunitLe hlsbLe)
        have htargetRewrite :
            _root_.F2R (beta:=radix) be1 -
                _root_.F2R (beta:=radix) r1 +
                _root_.F2R (beta:=radix) be2 =
              (_root_.F2R (beta:=radix) a *
                    _root_.F2R (beta:=radix) x +
                  _root_.F2R (beta:=radix) y -
                  _root_.F2R (beta:=radix) r1) +
                -_root_.F2R (beta:=radix) al2 := by
          rw [hbe2Def, hal2Def, hu2Def]
          ring
        have htargetSmallHigh :
            |_root_.F2R (beta:=radix) be1 -
                _root_.F2R (beta:=radix) r1 +
                _root_.F2R (beta:=radix) be2| <
              (radix : ℝ) ^ (be1.Fexp + 1) := by
          have htri :
              |_root_.F2R (beta:=radix) be1 -
                  _root_.F2R (beta:=radix) r1 +
                  _root_.F2R (beta:=radix) be2| ≤
                |_root_.F2R (beta:=radix) a *
                    _root_.F2R (beta:=radix) x +
                    _root_.F2R (beta:=radix) y -
                    _root_.F2R (beta:=radix) r1| +
                  |- _root_.F2R (beta:=radix) al2| := by
            calc
              |_root_.F2R (beta:=radix) be1 -
                  _root_.F2R (beta:=radix) r1 +
                  _root_.F2R (beta:=radix) be2|
                  = |(_root_.F2R (beta:=radix) a *
                        _root_.F2R (beta:=radix) x +
                      _root_.F2R (beta:=radix) y -
                      _root_.F2R (beta:=radix) r1) +
                      -_root_.F2R (beta:=radix) al2| := by
                      rw [htargetRewrite]
              _ ≤ |_root_.F2R (beta:=radix) a *
                      _root_.F2R (beta:=radix) x +
                    _root_.F2R (beta:=radix) y -
                    _root_.F2R (beta:=radix) r1| +
                  |- _root_.F2R (beta:=radix) al2| :=
                    abs_add_le _ _
          have hal2BoundLow :
              |- _root_.F2R (beta:=radix) al2| ≤
                (radix : ℝ) ^ be1.Fexp / 2 := by
            rw [abs_neg]
            exact le_trans hal2LeBe2 hbe2ErrLe
          have hpowLowLtHigh :
              (radix : ℝ) ^ be1.Fexp <
                (radix : ℝ) ^ (be1.Fexp + 1) := by
            have h := Rlt_powerRZ (radix : ℝ) be1.Fexp (be1.Fexp + 1)
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Rlt_powerRZ_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hradixRealGt, by omega⟩
          nlinarith
        have htargetBound :
            |_root_.F2R (beta:=radix) be1 -
                _root_.F2R (beta:=radix) r1 +
                _root_.F2R (beta:=radix) be2| <
              (radix : ℝ) ^ (be1.Fexp - 2 + (precision : Int)) := by
          have hpowLe :
              (radix : ℝ) ^ (be1.Fexp + 1) ≤
                (radix : ℝ) ^ (be1.Fexp - 2 + (precision : Int)) := by
            have h := Rle_powerRZ (radix : ℝ) (be1.Fexp + 1)
              (be1.Fexp - 2 + (precision : Int))
            have hexp : be1.Fexp + 1 ≤ be1.Fexp - 2 + (precision : Int) := by
              omega
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Rle_powerRZ_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hradixRealGe, hexp⟩
          exact lt_of_lt_of_le htargetSmallHigh hpowLe
        have hminExp : -bo.dExp ≤ be1.Fexp - 2 := by omega
        rcases BoundedL (beta:=radix) bo radix precision
            (_root_.F2R (beta:=radix) be1 -
              _root_.F2R (beta:=radix) r1 +
              _root_.F2R (beta:=radix) be2)
            sumW (be1.Fexp - 2) rfl hradix hvNum hprecision_gt_one
            hsumWExpLow hminExp hsumWVal htargetBound with
          ⟨corr, hcorrVal, hcorrBound, _hcorrExp⟩
        exact ⟨corr, hcorrVal, hcorrBound⟩

noncomputable def FmaErr_aux1_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a x y r1 u1 u2 al1 al2 be1 be2 gat ga :
      FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq `FmaErr_aux1`: the exact-difference witness and closest-rounding
totality used in the zero-`be2` proof are derived from the source section
assumptions. -/
theorem FmaErr_aux1 {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a x y r1 u1 u2 al1 al2 be1 be2 gat ga :
      FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧
        bo.vNum = Zpower_nat radix precision ∧ 3 ≤ precision ∧
        Even radix ∧
        Fbounded (beta:=beta) bo y ∧
        Fnormal (beta:=beta) radix bo be1 ∧
        Fnormal (beta:=beta) radix bo r1 ∧
        Fcanonic (beta:=beta) radix bo al1 ∧
        Fcanonic (beta:=beta) radix bo u1 ∧
        -bo.dExp < al1.Fexp ∧ -bo.dExp < u1.Fexp ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
            _root_.F2R (beta:=beta) y) r1 ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) u1 ∧
        _root_.F2R (beta:=beta) u2 =
          _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
            _root_.F2R (beta:=beta) u1 ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) u2) al1 ∧
        _root_.F2R (beta:=beta) al2 =
          _root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) u2 -
            _root_.F2R (beta:=beta) al1 ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) u1 + _root_.F2R (beta:=beta) al1) be1 ∧
        _root_.F2R (beta:=beta) be2 =
          _root_.F2R (beta:=beta) u1 + _root_.F2R (beta:=beta) al1 -
            _root_.F2R (beta:=beta) be1 ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) be1 - _root_.F2R (beta:=beta) r1) gat ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) gat + _root_.F2R (beta:=beta) be2) ga ∧
        _root_.F2R (beta:=beta) be2 = 0⌝⦄
    (pure (FmaErr_aux1_check (beta:=beta) bo radix precision
      a x y r1 u1 u2 al1 al2 be1 be2 gat ga) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
          _root_.F2R (beta:=beta) y =
        _root_.F2R (beta:=beta) r1 + _root_.F2R (beta:=beta) ga +
          _root_.F2R (beta:=beta) al2⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hradix, hvNum, hprecision, hEven, hyBound, hbe1Normal,
      hr1Normal, hal1Can, hu1Can, hal1ExpGt, hu1ExpGt, hr1Def, hu1Def,
      hu2Def, hal1Def, hal2Def, hbe1Def, hbe2Def, hgatDef, hgaDef,
      hbe2Zero⟩
  simp only [wp, PostCond.noThrow, pure, FmaErr_aux1_check, Id.run,
    ULift.up_down]
  subst beta
  have hBoundExp :
      ∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=radix) radix r).Fexp := by
    intro r
    have hnonneg : (0 : Int) ≤ (boundR (beta:=radix) radix r).Fexp := by
      simp [boundR, boundNat]
    exact le_trans (neg_nonpos.mpr bo.dExp_nonneg) hnonneg
  have hTotal : TotalP (Closest (beta:=radix) bo (radix : ℝ)) :=
    ClosestTotal (beta:=radix) bo radix precision
      ⟨rfl, hradix, by omega, hvNum⟩
  rcases gatCorrect (beta:=radix) bo radix precision
      a x y r1 u1 u2 al1 al2 be1 rfl hradix hvNum hprecision hEven
      hyBound hbe1Normal hr1Normal hal1Can hu1Can hal1ExpGt
      hu1ExpGt hr1Def hu1Def hu2Def hal1Def hal2Def hbe1Def with
    ⟨diff, hdiffVal, hdiffBound, _hdiffExp⟩
  exact (FmaErr_aux1_from_difference_payload (beta:=radix) bo (radix : ℝ)
    a x y r1 u1 u2 al1 al2 be1 be2 diff gat ga)
    ⟨hTotal, hdiffBound, hdiffVal, hbe2Zero, hu2Def, hal2Def, hbe2Def,
      hgatDef, hgaDef⟩

noncomputable def FmaErr_aux2_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (a x y r1 u1 u2 al1 al2 be1 be2 gat ga :
      FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq `FmaErr_aux2`: both exact correction witnesses and closest-rounding
totality are reconstructed from the nonzero-`be2` source section contract. -/
theorem FmaErr_aux2 {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (a x y r1 u1 u2 al1 al2 be1 be2 gat ga :
      FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧
        bo.vNum = Zpower_nat radix precision ∧ 3 ≤ precision ∧
        Even radix ∧
        (∀ r s : ℝ, ∀ f g : FloatSpec.Core.Defs.FlocqFloat beta,
          P r f → P s g → r = s →
            _root_.F2R (beta:=beta) f = _root_.F2R (beta:=beta) g) ∧
        Fbounded (beta:=beta) bo y ∧
        Fnormal (beta:=beta) radix bo be1 ∧
        Fnormal (beta:=beta) radix bo r1 ∧
        Fcanonic (beta:=beta) radix bo al1 ∧
        Fcanonic (beta:=beta) radix bo u1 ∧
        -bo.dExp < al1.Fexp ∧ -bo.dExp < u1.Fexp ∧
        -bo.dExp + 1 < be1.Fexp ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
            _root_.F2R (beta:=beta) y) r1 ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) u1 ∧
        _root_.F2R (beta:=beta) u2 =
          _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
            _root_.F2R (beta:=beta) u1 ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) u2) al1 ∧
        _root_.F2R (beta:=beta) al2 =
          _root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) u2 -
            _root_.F2R (beta:=beta) al1 ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) u1 + _root_.F2R (beta:=beta) al1) be1 ∧
        _root_.F2R (beta:=beta) be2 =
          _root_.F2R (beta:=beta) u1 + _root_.F2R (beta:=beta) al1 -
            _root_.F2R (beta:=beta) be1 ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) be1 - _root_.F2R (beta:=beta) r1) gat ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) gat + _root_.F2R (beta:=beta) be2) ga ∧
        Fbounded (beta:=beta) bo be2 ∧
        _root_.F2R (beta:=beta) be2 ≠ 0 ∧
        P (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
          _root_.F2R (beta:=beta) y) r1 ∧
        P (_root_.F2R (beta:=beta) u1 + _root_.F2R (beta:=beta) al1) be1⌝⦄
    (pure (FmaErr_aux2_check (beta:=beta) bo radix precision P
      a x y r1 u1 u2 al1 al2 be1 be2 gat ga) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
          _root_.F2R (beta:=beta) y =
        _root_.F2R (beta:=beta) r1 + _root_.F2R (beta:=beta) ga +
          _root_.F2R (beta:=beta) al2⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hradix, hvNum, hprecision, hEven, hCompat, hyBound,
      hbe1Normal, hr1Normal, hal1Can, hu1Can, hal1ExpGt, hu1ExpGt,
      hbe1ExpGt, hr1Def, hu1Def, hu2Def, hal1Def, hal2Def, hbe1Def,
      hbe2Def, hgatDef, hgaDef, hbe2Bound, hbe2Nonzero, hr1P, hbe1P⟩
  simp only [wp, PostCond.noThrow, pure, FmaErr_aux2_check, Id.run,
    ULift.up_down]
  subst beta
  have hBoundExp :
      ∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=radix) radix r).Fexp := by
    intro r
    have hnonneg : (0 : Int) ≤ (boundR (beta:=radix) radix r).Fexp := by
      simp [boundR, boundNat]
    exact le_trans (neg_nonpos.mpr bo.dExp_nonneg) hnonneg
  have hTotal : TotalP (Closest (beta:=radix) bo (radix : ℝ)) :=
    ClosestTotal (beta:=radix) bo radix precision
      ⟨rfl, hradix, by omega, hvNum⟩
  rcases gatCorrect (beta:=radix) bo radix precision
      a x y r1 u1 u2 al1 al2 be1 rfl hradix hvNum hprecision hEven
      hyBound hbe1Normal hr1Normal hal1Can hu1Can hal1ExpGt
      hu1ExpGt hr1Def hu1Def hu2Def hal1Def hal2Def hbe1Def with
    ⟨diff, hdiffVal, hdiffBound, _hdiffExp⟩
  rcases gaCorrect (beta:=radix) bo radix precision P
      a x y r1 u1 u2 al1 al2 be1 be2 rfl hradix hvNum hprecision hEven
      hyBound hbe1Normal hr1Normal hal1Can hu1Can hu1ExpGt
      hal1ExpGt hbe1ExpGt hCompat hr1Def hu1Def hu2Def hal1Def hal2Def
      hbe1Def hbe2Def hbe2Bound hbe2Nonzero hr1P hbe1P with
    ⟨corr, hcorrVal, hcorrBound⟩
  have hgatEq : _root_.F2R (beta:=radix) gat =
      _root_.F2R (beta:=radix) diff :=
    (FmaErr_gat_value_of_exact_difference (beta:=radix) bo
      (radix : ℝ) be1 r1 diff gat)
      ⟨hTotal, hdiffBound, hdiffVal, hgatDef⟩
  exact (FmaErr_aux2_from_correction_payload (beta:=radix) bo (radix : ℝ)
    a x y r1 u1 u2 al1 al2 be1 be2 diff gat corr ga)
    ⟨hTotal, hdiffBound, hdiffVal, hcorrBound, by
      rw [hcorrVal, hgatEq, hdiffVal], hu2Def, hal2Def, hbe2Def,
      hgatDef, hgaDef⟩

noncomputable def FmaErr_aux_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (a x y r1 u1 u2 al1 al2 be1 be2 gat ga :
      FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq `FmaErr_aux` with its source section contract.  The bounded correction
for the nonzero-`be2` branch is constructed internally by `gaCorrect`. -/
theorem FmaErr_aux {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (a x y r1 u1 u2 al1 al2 be1 be2 gat ga :
      FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧
        bo.vNum = Zpower_nat radix precision ∧ 3 ≤ precision ∧ Even radix ∧
        (∀ r : ℝ, ∀ f : FloatSpec.Core.Defs.FlocqFloat beta,
          P r f → Closest (beta:=beta) bo (radix : ℝ) r f) ∧
        (∀ r s : ℝ, ∀ f g : FloatSpec.Core.Defs.FlocqFloat beta,
          P r f → P s g → r = s →
            _root_.F2R (beta:=beta) f = _root_.F2R (beta:=beta) g) ∧
        Fbounded (beta:=beta) bo a ∧ Fbounded (beta:=beta) bo x ∧
        Fbounded (beta:=beta) bo y ∧
        Fnormal (beta:=beta) radix bo be1 ∧
        Fnormal (beta:=beta) radix bo r1 ∧
        Fcanonic (beta:=beta) radix bo al1 ∧
        Fcanonic (beta:=beta) radix bo u1 ∧
        -bo.dExp < al1.Fexp ∧ -bo.dExp < u1.Fexp ∧
        -bo.dExp + 1 < be1.Fexp ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
            _root_.F2R (beta:=beta) y) r1 ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) u1 ∧
        _root_.F2R (beta:=beta) u2 =
          _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
            _root_.F2R (beta:=beta) u1 ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) u2) al1 ∧
        _root_.F2R (beta:=beta) al2 =
          _root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) u2 -
            _root_.F2R (beta:=beta) al1 ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) u1 + _root_.F2R (beta:=beta) al1) be1 ∧
        _root_.F2R (beta:=beta) be2 =
          _root_.F2R (beta:=beta) u1 + _root_.F2R (beta:=beta) al1 -
            _root_.F2R (beta:=beta) be1 ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) be1 - _root_.F2R (beta:=beta) r1) gat ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) gat + _root_.F2R (beta:=beta) be2) ga ∧
        Fbounded (beta:=beta) bo be2 ∧
        P (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
          _root_.F2R (beta:=beta) y) r1 ∧
        P (_root_.F2R (beta:=beta) u1 + _root_.F2R (beta:=beta) al1) be1⌝⦄
    (pure (FmaErr_aux_check (beta:=beta) bo radix precision P
      a x y r1 u1 u2 al1 al2 be1 be2 gat ga) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
          _root_.F2R (beta:=beta) y =
        _root_.F2R (beta:=beta) r1 + _root_.F2R (beta:=beta) ga +
          _root_.F2R (beta:=beta) al2⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hradix, hvNum, hprecision, hEven, _hPClosest, hCompat,
      _haBound, _hxBound, hyBound, hbe1Normal, hr1Normal, hal1Can, hu1Can,
      hal1ExpGt, hu1ExpGt, hbe1ExpGt, hr1Def, hu1Def, hu2Def, hal1Def,
      hal2Def, hbe1Def, hbe2Def, hgatDef, hgaDef, hbe2Bound, hr1P, hbe1P⟩
  simp only [wp, PostCond.noThrow, pure, FmaErr_aux_check, Id.run,
    ULift.up_down]
  subst beta
  have hprecision_ne : precision ≠ 0 := by omega
  have hvNum_gt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecision_ne
  have hBoundExp :
      ∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=radix) radix r).Fexp := by
    intro r
    have hnonneg : (0 : Int) ≤ (boundR (beta:=radix) radix r).Fexp := by
      simp [boundR, boundNat]
    exact le_trans (neg_nonpos.mpr bo.dExp_nonneg) hnonneg
  have hMinTotal : TotalP (isMin (beta:=radix) bo radix) := by
    intro r
    exact (MinEx_from_finite_box_payload (beta:=radix) bo radix r) ⟨rfl, hradix, hvNum_gt, hBoundExp r⟩
  have hMaxTotal : TotalP (isMax (beta:=radix) bo radix) := by
    intro r
    exact (MaxEx_from_finite_box_payload (beta:=radix) bo radix r) ⟨rfl, hradix, hvNum_gt, hBoundExp r⟩
  have hTotal : TotalP (Closest (beta:=radix) bo (radix : ℝ)) := by
    intro r
    exact (ClosestTotal_from_extrema_payload (beta:=radix) bo radix (radix : ℝ) r)
      ⟨hMinTotal, hMaxTotal⟩
  rcases gatCorrect (beta:=radix) bo radix precision
      a x y r1 u1 u2 al1 al2 be1 rfl hradix hvNum hprecision hEven
      hyBound hbe1Normal hr1Normal hal1Can hu1Can hal1ExpGt
      hu1ExpGt hr1Def hu1Def hu2Def hal1Def hal2Def hbe1Def with
    ⟨diff, hdiffVal, hdiffBound, _hdiffExp⟩
  by_cases hbe2Zero : _root_.F2R (beta:=radix) be2 = 0
  · exact (FmaErr_aux1_from_difference_payload (beta:=radix) bo (radix : ℝ)
      a x y r1 u1 u2 al1 al2 be1 be2 diff gat ga)
      ⟨hTotal, hdiffBound, hdiffVal, hbe2Zero, hu2Def, hal2Def, hbe2Def,
        hgatDef, hgaDef⟩
  · rcases gaCorrect (beta:=radix) bo radix precision P
      a x y r1 u1 u2 al1 al2 be1 be2 rfl hradix hvNum hprecision hEven
      hyBound hbe1Normal hr1Normal hal1Can hu1Can hu1ExpGt
        hal1ExpGt hbe1ExpGt hCompat hr1Def hu1Def hu2Def hal1Def hal2Def
        hbe1Def hbe2Def hbe2Bound hbe2Zero hr1P hbe1P with
      ⟨corr, hcorrVal, hcorrBound⟩
    have hgatEq : _root_.F2R (beta:=radix) gat =
        _root_.F2R (beta:=radix) diff :=
      (FmaErr_gat_value_of_exact_difference (beta:=radix) bo
        (radix : ℝ) be1 r1 diff gat)
        ⟨hTotal, hdiffBound, hdiffVal, hgatDef⟩
    exact (FmaErr_aux2_from_correction_payload (beta:=radix) bo (radix : ℝ)
      a x y r1 u1 u2 al1 al2 be1 be2 diff gat corr ga)
      ⟨hTotal, hdiffBound, hdiffVal, hcorrBound, by
        rw [hcorrVal, hgatEq, hdiffVal], hu2Def, hal2Def, hbe2Def,
        hgatDef, hgaDef⟩

noncomputable def FmaErr_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (a x y r1 u1 u2 al1 al2 be1 be2 gat ga :
      FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq `FmaErr` with the exact Final2 case split.  The normal branch invokes
`FmaErr_aux`; the three zero branches derive the same projector equalities as
the source proof instead of exposing any intermediate correction witness. -/
theorem FmaErr {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (a x y r1 u1 u2 al1 al2 be1 be2 gat ga :
      FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧
        bo.vNum = Zpower_nat radix precision ∧ 3 ≤ precision ∧ Even radix ∧
        (∀ r : ℝ, ∀ f : FloatSpec.Core.Defs.FlocqFloat beta,
          P r f → Closest (beta:=beta) bo (radix : ℝ) r f) ∧
        (∀ r s : ℝ, ∀ f g : FloatSpec.Core.Defs.FlocqFloat beta,
          P r f → P s g → r = s →
            _root_.F2R (beta:=beta) f = _root_.F2R (beta:=beta) g) ∧
        Fbounded (beta:=beta) bo a ∧ Fbounded (beta:=beta) bo x ∧
        Fbounded (beta:=beta) bo y ∧
        Fcanonic (beta:=beta) radix bo be1 ∧
        Fcanonic (beta:=beta) radix bo r1 ∧
        Fcanonic (beta:=beta) radix bo al1 ∧
        Fcanonic (beta:=beta) radix bo u1 ∧
        (-bo.dExp < al1.Fexp ∨ _root_.F2R (beta:=beta) al1 = 0) ∧
        (-bo.dExp < u1.Fexp ∨ _root_.F2R (beta:=beta) u1 = 0) ∧
        (-bo.dExp + 1 < be1.Fexp ∨ _root_.F2R (beta:=beta) be1 = 0) ∧
        (Fnormal (beta:=beta) radix bo r1 ∨
          _root_.F2R (beta:=beta) r1 = 0) ∧
        -bo.dExp ≤ a.Fexp + x.Fexp ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) u1 ∧
        _root_.F2R (beta:=beta) u2 =
          _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
            _root_.F2R (beta:=beta) u1 ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) u2) al1 ∧
        _root_.F2R (beta:=beta) al2 =
          _root_.F2R (beta:=beta) y + _root_.F2R (beta:=beta) u2 -
            _root_.F2R (beta:=beta) al1 ∧
        _root_.F2R (beta:=beta) be2 =
          _root_.F2R (beta:=beta) u1 + _root_.F2R (beta:=beta) al1 -
            _root_.F2R (beta:=beta) be1 ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) be1 - _root_.F2R (beta:=beta) r1) gat ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) gat + _root_.F2R (beta:=beta) be2) ga ∧
        P (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
          _root_.F2R (beta:=beta) y) r1 ∧
        P (_root_.F2R (beta:=beta) u1 + _root_.F2R (beta:=beta) al1) be1⌝⦄
    (pure (FmaErr_check (beta:=beta) bo radix precision P
      a x y r1 u1 u2 al1 al2 be1 be2 gat ga) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
          _root_.F2R (beta:=beta) y =
        _root_.F2R (beta:=beta) r1 + _root_.F2R (beta:=beta) ga +
          _root_.F2R (beta:=beta) al2⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hradix, hvNum, hprecision, hEven, hPClosest, hCompat,
      haBound, hxBound, hyBound, hbe1Can, hr1Can, hal1Can, hu1Can,
      hExp1, hExp2, hExp3, hExp4, hprodExp, hu1Def, hu2Def, hal1Def,
      hal2Def, hbe2Def, hgatDef, hgaDef, hr1P, hbe1P⟩
  simp only [wp, PostCond.noThrow, pure, FmaErr_check, Id.run, ULift.up_down]
  subst beta
  have hprecision_ne : precision ≠ 0 := by omega
  have hvNum_gt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecision_ne
  have hBoundExp :
      ∀ r : ℝ, -bo.dExp ≤ (boundR (beta:=radix) radix r).Fexp := by
    intro r
    have hnonneg : (0 : Int) ≤ (boundR (beta:=radix) radix r).Fexp := by
      simp [boundR, boundNat]
    exact le_trans (neg_nonpos.mpr bo.dExp_nonneg) hnonneg
  have hMinTotal : TotalP (isMin (beta:=radix) bo radix) := by
    intro r
    exact (MinEx_from_finite_box_payload (beta:=radix) bo radix r) ⟨rfl, hradix, hvNum_gt, hBoundExp r⟩
  have hMaxTotal : TotalP (isMax (beta:=radix) bo radix) := by
    intro r
    exact (MaxEx_from_finite_box_payload (beta:=radix) bo radix r) ⟨rfl, hradix, hvNum_gt, hBoundExp r⟩
  have hTotal : TotalP (Closest (beta:=radix) bo (radix : ℝ)) := by
    intro r
    exact (ClosestTotal_from_extrema_payload (beta:=radix) bo radix (radix : ℝ) r)
      ⟨hMinTotal, hMaxTotal⟩
  have hRounded : RoundedModeP_full (beta:=radix) bo
      (Closest (beta:=radix) bo (radix : ℝ)) :=
    (ClosestRoundedModeP_from_total_payload (beta:=radix) bo (radix : ℝ)) hTotal
  rcases hRounded with ⟨_, _, _, _, hProjector⟩
  have roundExact : ∀ p q : FloatSpec.Core.Defs.FlocqFloat radix,
      Fbounded (beta:=radix) bo p →
      Closest (beta:=radix) bo (radix : ℝ)
        (_root_.F2R (beta:=radix) p) q →
      _root_.F2R (beta:=radix) q = _root_.F2R (beta:=radix) p := by
    intro p q hp hq
    exact (hProjector p q hp hq).symm
  have hFplus : ∀ p q : FloatSpec.Core.Defs.FlocqFloat radix,
      _root_.F2R (beta:=radix) (Fplus (beta:=radix) p q) =
        _root_.F2R (beta:=radix) p + _root_.F2R (beta:=radix) q := by
    intro p q
    exact (Fplus_correct (beta:=radix) p q) hradix
  have hFmult : ∀ p q : FloatSpec.Core.Defs.FlocqFloat radix,
      _root_.F2R (beta:=radix)
          (FloatSpec.Calc.Operations.Fmult (beta:=radix) p q) =
        _root_.F2R (beta:=radix) p * _root_.F2R (beta:=radix) q := by
    intro p q
    exact (Fmult_correct (beta:=radix) p q) hradix
  have hFopp : ∀ p : FloatSpec.Core.Defs.FlocqFloat radix,
      _root_.F2R (beta:=radix) (Fopp (beta:=radix) p) =
        -_root_.F2R (beta:=radix) p := by
    intro p
    exact (Fopp_correct (beta:=radix) p) trivial
  have hu1Bound : Fbounded (beta:=radix) bo u1 :=
    (FcanonicBound (beta:=radix) radix bo u1) hu1Can
  have hal1Bound : Fbounded (beta:=radix) bo al1 :=
    (FcanonicBound (beta:=radix) radix bo al1) hal1Can
  have hr1Bound : Fbounded (beta:=radix) bo r1 :=
    (FcanonicBound (beta:=radix) radix bo r1) hr1Can
  have hr1Def : Closest (beta:=radix) bo (radix : ℝ)
      (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
        _root_.F2R (beta:=radix) y) r1 := hPClosest _ _ hr1P
  have hbe1Def : Closest (beta:=radix) bo (radix : ℝ)
      (_root_.F2R (beta:=radix) u1 + _root_.F2R (beta:=radix) al1) be1 :=
    hPClosest _ _ hbe1P
  let zeroFloat : FloatSpec.Core.Defs.FlocqFloat radix := ⟨0, -bo.dExp⟩
  have hzeroBound : Fbounded (beta:=radix) bo zeroFloat := by
    exact ⟨by simpa [zeroFloat] using bo.vNum_pos, by simp [zeroFloat]⟩
  have hzeroVal : _root_.F2R (beta:=radix) zeroFloat = 0 := by
    simp [zeroFloat, _root_.F2R, FloatSpec.Core.Defs.F2R]
  have zeroOfClosest : ∀ q : FloatSpec.Core.Defs.FlocqFloat radix,
      Closest (beta:=radix) bo (radix : ℝ) 0 q →
      _root_.F2R (beta:=radix) q = 0 := by
    intro q hq
    simpa [hzeroVal] using roundExact zeroFloat q hzeroBound (by
      simpa [hzeroVal] using hq)
  have closestInputZero : ∀ (r : ℝ)
      (f g : FloatSpec.Core.Defs.FlocqFloat radix),
      Closest (beta:=radix) bo (radix : ℝ) r f →
      _root_.F2R (beta:=radix) f = 0 →
      r = _root_.F2R (beta:=radix) g → -bo.dExp ≤ g.Fexp → r = 0 := by
    intro r f g hf hfZero hrVal hgExp
    have hfBound' : Fbounded' (beta:=radix) bo f := by
      simpa [Fbounded'] using hf.1
    have hMin : ∀ q : FloatSpec.Core.Defs.FlocqFloat radix,
        Fbounded' (beta:=radix) bo q →
          |_root_.F2R (beta:=radix) f - r| ≤
            |_root_.F2R (beta:=radix) q - r| := by
      intro q hq
      exact hf.2 q (by simpa [Fbounded'] using hq)
    exact (ClosestZero1_from_expanded_closest_payload (beta:=radix) bo (radix : ℝ) r f g)
      ⟨hf, hfZero, hrVal, hgExp, hfBound', hMin, by exact_mod_cast hradix,
        hvNum_gt⟩
  rcases hExp1 with hal1Exp | hal1Zero
  · rcases hExp2 with hu1Exp | hu1Zero
    · rcases hExp3 with hbe1Exp | hbe1Zero
      · rcases hExp4 with hr1Normal | hr1Zero
        · have hbe1Normal : Fnormal (beta:=radix) radix bo be1 := by
            rcases hbe1Can with hnormal | hsub
            · exact hnormal
            · rcases hsub with ⟨_, hsubExp, _⟩
              exfalso
              omega
          rcases (FmaErr_be1_error_witness (beta:=radix) bo radix precision
              u1 al1 be1 be2)
              ⟨rfl, hradix, hprecision_ne, hvNum_gt, hvNum, hBoundExp,
                hu1Bound, hal1Bound, hbe1Def, hbe2Def⟩ with
            ⟨be2', hbe2'Val, hbe2'Bound, _hbe2'Exp⟩
          have hbe2'Def : _root_.F2R (beta:=radix) be2' =
              _root_.F2R (beta:=radix) u1 + _root_.F2R (beta:=radix) al1 -
                _root_.F2R (beta:=radix) be1 := hbe2'Val.trans hbe2Def
          have hgaDef' : Closest (beta:=radix) bo (radix : ℝ)
              (_root_.F2R (beta:=radix) gat +
                _root_.F2R (beta:=radix) be2') ga := by
            simpa [hbe2'Val] using hgaDef
          exact (FmaErr_aux (beta:=radix) bo radix precision P
            a x y r1 u1 u2 al1 al2 be1 be2' gat ga)
            ⟨rfl, hradix, hvNum, hprecision, hEven, hPClosest, hCompat,
              haBound, hxBound, hyBound, hbe1Normal, hr1Normal, hal1Can,
              hu1Can, hal1Exp, hu1Exp, hbe1Exp, hr1Def, hu1Def, hu2Def,
              hal1Def, hal2Def, hbe1Def, hbe2'Def, hgatDef, hgaDef',
              hbe2'Bound, hr1P, hbe1P⟩
        · let exactTotal : FloatSpec.Core.Defs.FlocqFloat radix :=
            Fplus (beta:=radix)
              (FloatSpec.Calc.Operations.Fmult (beta:=radix) a x) y
          have hexactTotalVal : _root_.F2R (beta:=radix) exactTotal =
              _root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
                _root_.F2R (beta:=radix) y := by
            rw [show _root_.F2R (beta:=radix) exactTotal =
                _root_.F2R (beta:=radix)
                    (FloatSpec.Calc.Operations.Fmult (beta:=radix) a x) +
                  _root_.F2R (beta:=radix) y from hFplus _ _]
            rw [hFmult]
          have hexactTotalExp : -bo.dExp ≤ exactTotal.Fexp := by
            rw [show exactTotal.Fexp = min
                (FloatSpec.Calc.Operations.Fmult (beta:=radix) a x).Fexp
                y.Fexp from Fplus_exp_eq_min _ _]
            apply le_min
            · simpa [FloatSpec.Calc.Operations.Fmult] using hprodExp
            · exact hyBound.2
          have htotalZero :
              _root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
                _root_.F2R (beta:=radix) y = 0 :=
            closestInputZero _ r1 exactTotal hr1Def hr1Zero
              hexactTotalVal.symm hexactTotalExp
          let yOpp := Fopp (beta:=radix) y
          have hyOppBound : Fbounded (beta:=radix) bo yOpp :=
            (oppBounded (beta:=radix) bo y) hyBound
          have hyOppVal : _root_.F2R (beta:=radix) yOpp =
              -_root_.F2R (beta:=radix) y := hFopp y
          have hu1Val : _root_.F2R (beta:=radix) u1 =
              _root_.F2R (beta:=radix) yOpp := by
            apply roundExact yOpp u1 hyOppBound
            have harg : _root_.F2R (beta:=radix) yOpp =
                _root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x := by
              rw [hyOppVal]
              linarith
            simpa [harg] using hu1Def
          have hu2Zero : _root_.F2R (beta:=radix) u2 = 0 := by
            rw [hu2Def, hu1Val, hyOppVal]
            linarith
          have hal1Val : _root_.F2R (beta:=radix) al1 =
              _root_.F2R (beta:=radix) y := by
            apply roundExact y al1 hyBound
            simpa [hu2Zero] using hal1Def
          have hal2Zero : _root_.F2R (beta:=radix) al2 = 0 := by
            rw [hal2Def, hu2Zero, hal1Val]
            ring
          have hbe1Zero' : _root_.F2R (beta:=radix) be1 = 0 := by
            apply zeroOfClosest be1
            simpa [hu1Val, hyOppVal, hal1Val] using hbe1Def
          have hbe2Zero : _root_.F2R (beta:=radix) be2 = 0 := by
            rw [hbe2Def, hu1Val, hyOppVal, hal1Val, hbe1Zero']
            ring
          have hgatZero : _root_.F2R (beta:=radix) gat = 0 := by
            apply zeroOfClosest gat
            simpa [hbe1Zero', hr1Zero] using hgatDef
          have hgaZero : _root_.F2R (beta:=radix) ga = 0 := by
            apply zeroOfClosest ga
            simpa [hgatZero, hbe2Zero] using hgaDef
          rw [htotalZero, hr1Zero, hgaZero, hal2Zero]
          simp [wp, PostCond.noThrow, pure]
      · let exactSum := Fplus (beta:=radix) u1 al1
        have hexactSumVal : _root_.F2R (beta:=radix) exactSum =
            _root_.F2R (beta:=radix) u1 + _root_.F2R (beta:=radix) al1 :=
          hFplus u1 al1
        have hexactSumExp : -bo.dExp ≤ exactSum.Fexp := by
          rw [show exactSum.Fexp = min u1.Fexp al1.Fexp from
            Fplus_exp_eq_min _ _]
          exact le_min hu1Bound.2 hal1Bound.2
        have hsumZero : _root_.F2R (beta:=radix) u1 +
            _root_.F2R (beta:=radix) al1 = 0 :=
          closestInputZero _ be1 exactSum hbe1Def hbe1Zero
            hexactSumVal.symm hexactSumExp
        have hbe2Zero : _root_.F2R (beta:=radix) be2 = 0 := by
          rw [hbe2Def, hbe1Zero]
          linarith
        let r1Opp := Fopp (beta:=radix) r1
        have hr1OppBound : Fbounded (beta:=radix) bo r1Opp :=
          (oppBounded (beta:=radix) bo r1) hr1Bound
        have hr1OppVal : _root_.F2R (beta:=radix) r1Opp =
            -_root_.F2R (beta:=radix) r1 := hFopp r1
        have hgatVal : _root_.F2R (beta:=radix) gat =
            _root_.F2R (beta:=radix) r1Opp := by
          apply roundExact r1Opp gat hr1OppBound
          simpa [hr1OppVal, hbe1Zero] using hgatDef
        have hgaVal : _root_.F2R (beta:=radix) ga =
            _root_.F2R (beta:=radix) gat := by
          exact roundExact gat ga hgatDef.1 (by simpa [hbe2Zero] using hgaDef)
        calc
          _ = _root_.F2R (beta:=radix) u1 +
                _root_.F2R (beta:=radix) al1 +
                  _root_.F2R (beta:=radix) al2 := by
                rw [hal2Def, hu2Def]
                ring
          _ = _root_.F2R (beta:=radix) al2 := by rw [hsumZero]; ring
          _ = _root_.F2R (beta:=radix) r1 +
                _root_.F2R (beta:=radix) ga +
                  _root_.F2R (beta:=radix) al2 := by
                rw [hgaVal, hgatVal, hr1OppVal]
                ring
    · let exactProd := FloatSpec.Calc.Operations.Fmult (beta:=radix) a x
      have hexactProdVal : _root_.F2R (beta:=radix) exactProd =
          _root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x :=
        hFmult a x
      have hexactProdExp : -bo.dExp ≤ exactProd.Fexp := by
        simpa [exactProd, FloatSpec.Calc.Operations.Fmult] using hprodExp
      have hprodZero : _root_.F2R (beta:=radix) a *
          _root_.F2R (beta:=radix) x = 0 :=
        closestInputZero _ u1 exactProd hu1Def hu1Zero
          hexactProdVal.symm hexactProdExp
      have hr1Val : _root_.F2R (beta:=radix) r1 =
          _root_.F2R (beta:=radix) y := by
        apply roundExact y r1 hyBound
        simpa [hprodZero] using hr1Def
      have hu2Zero : _root_.F2R (beta:=radix) u2 = 0 := by
        rw [hu2Def, hu1Zero, hprodZero]
        ring
      have hal1Val : _root_.F2R (beta:=radix) al1 =
          _root_.F2R (beta:=radix) y := by
        apply roundExact y al1 hyBound
        simpa [hu2Zero] using hal1Def
      have hal2Zero : _root_.F2R (beta:=radix) al2 = 0 := by
        rw [hal2Def, hu2Zero, hal1Val]
        ring
      have hbe1Val : _root_.F2R (beta:=radix) be1 =
          _root_.F2R (beta:=radix) y := by
        apply roundExact y be1 hyBound
        simpa [hu1Zero, hal1Val] using hbe1Def
      have hbe2Zero : _root_.F2R (beta:=radix) be2 = 0 := by
        rw [hbe2Def, hu1Zero, hal1Val, hbe1Val]
        ring
      have hgatZero : _root_.F2R (beta:=radix) gat = 0 := by
        apply zeroOfClosest gat
        simpa [hbe1Val, hr1Val] using hgatDef
      have hgaZero : _root_.F2R (beta:=radix) ga = 0 := by
        apply zeroOfClosest ga
        simpa [hgatZero, hbe2Zero] using hgaDef
      rw [hprodZero, hr1Val, hgaZero, hal2Zero]
      simp [wp, PostCond.noThrow, pure]
  · rcases (FmaErr_product_error_witness (beta:=radix) bo radix precision
        a x u1 u2)
        ⟨rfl, hradix, hprecision_ne, hvNum, haBound, hxBound, hprodExp,
          hu1Def, hu2Def⟩ with
      ⟨u2', hu2'Val, hu2'Bound, hu2'Exp⟩
    let exactSum := Fplus (beta:=radix) y u2'
    have hexactSumVal : _root_.F2R (beta:=radix) exactSum =
        _root_.F2R (beta:=radix) y + _root_.F2R (beta:=radix) u2 := by
      rw [hFplus, hu2'Val]
    have hexactSumExp : -bo.dExp ≤ exactSum.Fexp := by
      rw [show exactSum.Fexp = min y.Fexp u2'.Fexp from Fplus_exp_eq_min _ _]
      rw [hu2'Exp]
      exact le_min hyBound.2 hprodExp
    have hsumZero : _root_.F2R (beta:=radix) y +
        _root_.F2R (beta:=radix) u2 = 0 :=
      closestInputZero _ al1 exactSum hal1Def hal1Zero
        hexactSumVal.symm hexactSumExp
    have hbe1Val : _root_.F2R (beta:=radix) be1 =
        _root_.F2R (beta:=radix) u1 := by
      apply roundExact u1 be1 hu1Bound
      simpa [hal1Zero] using hbe1Def
    have htotalEqU1 : _root_.F2R (beta:=radix) a *
          _root_.F2R (beta:=radix) x + _root_.F2R (beta:=radix) y =
        _root_.F2R (beta:=radix) u1 := by
      rw [hu2Def] at hsumZero
      linarith
    have hr1Val : _root_.F2R (beta:=radix) r1 =
        _root_.F2R (beta:=radix) u1 := by
      apply roundExact u1 r1 hu1Bound
      simpa [htotalEqU1] using hr1Def
    have hgatZero : _root_.F2R (beta:=radix) gat = 0 := by
      apply zeroOfClosest gat
      simpa [hbe1Val, hr1Val] using hgatDef
    have hbe2Zero : _root_.F2R (beta:=radix) be2 = 0 := by
      rw [hbe2Def, hbe1Val, hal1Zero]
      ring
    have hgaZero : _root_.F2R (beta:=radix) ga = 0 := by
      apply zeroOfClosest ga
      simpa [hgatZero, hbe2Zero] using hgaDef
    have hal2Zero : _root_.F2R (beta:=radix) al2 = 0 := by
      rw [hal2Def, hal1Zero]
      linarith
    rw [htotalEqU1, hr1Val, hgaZero, hal2Zero]
    simp [wp, PostCond.noThrow, pure]

private lemma tBounded_sterbenz_bounds
    (radix : Int) (precision : Nat) (s e r u z : ℝ)
    (hradix : 1 < radix) (hprecision : 3 ≤ precision)
    (hr : r = s + e)
    (heSmall : |e| ≤ |s| / 4)
    (huNonneg : 0 ≤ u) (hzNonneg : 0 ≤ z)
    (huLe : |u| ≤
      |s| / (1 - (radix : ℝ) ^ (1 - (precision : Int)) / 2))
    (hsLe : |s| ≤
      |u| * (1 + (radix : ℝ) ^ (1 - (precision : Int)) / 2))
    (hzLe : |z| ≤
      |r| / (1 - (radix : ℝ) ^ (1 - (precision : Int)) / 2))
    (hrLe : |r| ≤
      |z| * (1 + (radix : ℝ) ^ (1 - (precision : Int)) / 2)) :
    (1 / 2 : ℝ) * z ≤ u ∧ u ≤ 2 * z := by
  let den : ℝ := 1 - (radix : ℝ) ^ (1 - (precision : Int)) / 2
  let growth : ℝ := 1 + (radix : ℝ) ^ (1 - (precision : Int)) / 2
  have hdenPos : 0 < den := by
    simpa [den] using UnMoinsPos radix precision hradix hprecision
  have hgrowthNonneg : 0 ≤ growth := by
    dsimp [growth]
    positivity
  have hfactor43 : growth * 4 / 3 ≤ 2 * den := by
    simpa [growth, den] using
      xLe2y_factor_bound radix precision hradix hprecision
  have hfactor54 : growth * 5 / 4 ≤ 2 * den := by
    have hfiveFourLeFourThirds : (5 / 4 : ℝ) ≤ 4 / 3 := by norm_num
    have hgrowthScaled : growth * (5 / 4 : ℝ) ≤ growth * (4 / 3 : ℝ) :=
      mul_le_mul_of_nonneg_left hfiveFourLeFourThirds hgrowthNonneg
    nlinarith [hgrowthScaled, hfactor43]
  have huMul : den * |u| ≤ |s| := by
    have h := (le_div_iff₀ hdenPos).1 (by simpa [den] using huLe)
    nlinarith
  have hzMul : den * |z| ≤ |r| := by
    have h := (le_div_iff₀ hdenPos).1 (by simpa [den] using hzLe)
    nlinarith
  have hsToR : |s| ≤ |r| * 4 / 3 := by
    have h := abeLeab_from_quarter_error_payload s 0 e
      (by simpa using heSmall)
    simpa [hr] using h
  have hrToS : |r| ≤ |s| * 5 / 4 := by
    have htri : |r| ≤ |s| + |e| := by
      rw [hr]
      exact abs_add_le s e
    calc
      |r| ≤ |s| + |e| := htri
      _ ≤ |s| + |s| / 4 := by nlinarith [heSmall]
      _ = |s| * 5 / 4 := by ring
  have huAbsLe : |u| ≤ 2 * |z| := by
    have hmain : den * |u| ≤ den * (2 * |z|) := by
      calc
        den * |u| ≤ |s| := huMul
        _ ≤ |r| * 4 / 3 := hsToR
        _ ≤ (|z| * growth) * 4 / 3 := by
          exact div_le_div_of_nonneg_right
            (mul_le_mul_of_nonneg_right (by simpa [growth] using hrLe)
              (by norm_num)) (by norm_num)
        _ = |z| * (growth * 4 / 3) := by ring
        _ ≤ |z| * (2 * den) :=
          mul_le_mul_of_nonneg_left hfactor43 (abs_nonneg z)
        _ = den * (2 * |z|) := by ring
    exact le_of_mul_le_mul_left hmain hdenPos
  have hzAbsLe : |z| ≤ 2 * |u| := by
    have hmain : den * |z| ≤ den * (2 * |u|) := by
      calc
        den * |z| ≤ |r| := hzMul
        _ ≤ |s| * 5 / 4 := hrToS
        _ ≤ (|u| * growth) * 5 / 4 := by
          exact div_le_div_of_nonneg_right
            (mul_le_mul_of_nonneg_right (by simpa [growth] using hsLe)
              (by norm_num)) (by norm_num)
        _ = |u| * (growth * 5 / 4) := by ring
        _ ≤ |u| * (2 * den) :=
          mul_le_mul_of_nonneg_left hfactor54 (abs_nonneg u)
        _ = den * (2 * |u|) := by ring
    exact le_of_mul_le_mul_left hmain hdenPos
  rw [abs_of_nonneg huNonneg, abs_of_nonneg hzNonneg] at huAbsLe hzAbsLe
  constructor <;> nlinarith

private lemma tBounded_zero_case
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (ph b uh z : FloatSpec.Core.Defs.FlocqFloat radix)
    (hradix : 1 < radix)
    (hvNum : bo.vNum = Zpower_nat radix precision)
    (hzBound : Fbounded (beta:=radix) bo z)
    (huhDef : Closest (beta:=radix) bo (radix : ℝ)
      (_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b) uh)
    (hsumZero : _root_.F2R (beta:=radix) ph +
      _root_.F2R (beta:=radix) b = 0) :
    ∃ v : FloatSpec.Core.Defs.FlocqFloat radix,
      Fbounded (beta:=radix) bo v ∧
        _root_.F2R (beta:=radix) v =
          _root_.F2R (beta:=radix) uh - _root_.F2R (beta:=radix) z := by
  have hvNumPos : 0 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact pow_pos (by omega : 0 < radix) precision
  let zeroFloat : FloatSpec.Core.Defs.FlocqFloat radix := Fzero radix (-bo.dExp)
  have hzeroBound : Fbounded (beta:=radix) bo zeroFloat := by
    have h := FboundedFzero_from_positive_bound_payload (beta:=radix) bo
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FboundedFzero_check,
      Id.run, ULift.up_down, zeroFloat, Int.cast_ofNat] using h hvNumPos
  have hzeroValue : _root_.F2R (beta:=radix) zeroFloat = 0 := by
    have h := FzeroisZero (beta:=radix) bo
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FzeroisZero_check,
      Id.run, ULift.up_down, zeroFloat, Int.cast_ofNat] using h True.intro
  have huhZero : _root_.F2R (beta:=radix) uh = 0 := by
    have hdist := huhDef.2 zeroFloat hzeroBound
    rw [hsumZero, hzeroValue, sub_self, abs_zero] at hdist
    exact sub_eq_zero.mp (abs_eq_zero.mp
      (le_antisymm hdist (abs_nonneg _)))
  have hoppBound : Fbounded (beta:=radix) bo (Fopp (beta:=radix) z) := by
    have h := oppBounded (beta:=radix) bo z
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, oppBounded_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hzBound
  have hoppValue :
      _root_.F2R (beta:=radix) (Fopp (beta:=radix) z) =
        -_root_.F2R (beta:=radix) z := by
    have h := Fopp_correct (beta:=radix) z
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fopp_correct_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
  refine ⟨Fopp (beta:=radix) z, hoppBound, ?_⟩
  rw [hoppValue, huhZero]
  ring

private lemma tBounded_small_case
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (a x b ph pl uh z : FloatSpec.Core.Defs.FlocqFloat radix)
    (hradix : 1 < radix)
    (hvNum : bo.vNum = Zpower_nat radix precision)
    (hprecision : 3 ≤ precision)
    (hTotal : TotalP (Closest (beta:=radix) bo (radix : ℝ)))
    (hbBound : Fbounded (beta:=radix) bo b)
    (hphNormal : Fnormal (beta:=radix) radix bo ph)
    (hzNormal : Fnormal (beta:=radix) radix bo z)
    (huhNormal : Fnormal (beta:=radix) radix bo uh)
    (hzDef : Closest (beta:=radix) bo (radix : ℝ)
      (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
        _root_.F2R (beta:=radix) b) z)
    (hphDef : Closest (beta:=radix) bo (radix : ℝ)
      (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x) ph)
    (hplDef : _root_.F2R (beta:=radix) pl =
      _root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x -
        _root_.F2R (beta:=radix) ph)
    (huhDef : Closest (beta:=radix) bo (radix : ℝ)
      (_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b) uh)
    (hPosit : 0 ≤
      _root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
        _root_.F2R (beta:=radix) b)
    (hsmall : |_root_.F2R (beta:=radix) pl| ≤
      |_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b| / 4) :
    ∃ v : FloatSpec.Core.Defs.FlocqFloat radix,
      Fbounded (beta:=radix) bo v ∧
        _root_.F2R (beta:=radix) v =
          _root_.F2R (beta:=radix) uh - _root_.F2R (beta:=radix) z := by
  have hRounded : RoundedModeP_full (beta:=radix) bo
      (Closest (beta:=radix) bo (radix : ℝ)) := by
    have h := ClosestRoundedModeP_from_total_payload (beta:=radix) bo (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestRoundedModeP_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hTotal
  have hbOppBound : Fbounded (beta:=radix) bo (Fopp (beta:=radix) b) := by
    have h := oppBounded (beta:=radix) bo b
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, oppBounded_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hbBound
  have hbOppValue :
      _root_.F2R (beta:=radix) (Fopp (beta:=radix) b) =
        -_root_.F2R (beta:=radix) b := by
    have h := Fopp_correct (beta:=radix) b
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fopp_correct_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
  have hphLower :
      _root_.F2R (beta:=radix) (Fopp (beta:=radix) b) ≤
        _root_.F2R (beta:=radix) ph := by
    have hinput :
        _root_.F2R (beta:=radix) (Fopp (beta:=radix) b) ≤
          _root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x := by
      rw [hbOppValue]
      linarith
    have h := RleBoundRoundl_from_full (beta:=radix) bo radix
      (Closest (beta:=radix) bo (radix : ℝ))
      (Fopp (beta:=radix) b) ph
      (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RleBoundRoundl_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hRounded, hbOppBound, hinput, hphDef⟩
  have hsumNonneg : 0 ≤
      _root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b := by
    rw [hbOppValue] at hphLower
    linarith
  have hzNonneg : 0 ≤ _root_.F2R (beta:=radix) z := by
    have h := zPos_from_total_payload (beta:=radix) bo radix precision a x b z
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, zPos_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hvNum, hTotal, hzDef, hPosit⟩
  have huhNonneg : 0 ≤ _root_.F2R (beta:=radix) uh := by
    have h := uhPos_from_nonnegative_sum_payload (beta:=radix) bo radix precision ph b uh
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, uhPos_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, hradix, hvNum, hTotal, huhDef, hsumNonneg⟩
  have hrealRewrite :
      _root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
          _root_.F2R (beta:=radix) b =
        (_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b) +
          _root_.F2R (beta:=radix) pl := by
    rw [hplDef]
    ring
  have huhLe := ClosestRoundeLeNormal (beta:=radix) bo radix precision
    (_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b) uh
  have huhLe' : |_root_.F2R (beta:=radix) uh| ≤
      |_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b| /
        (1 - (radix : ℝ) ^ (1 - (precision : Int)) / 2) := by
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestRoundeLeNormal_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      huhLe ⟨rfl, hradix, hprecision, hvNum, huhDef, huhNormal⟩
  have hsumLe := ClosestRoundeGeNormal (beta:=radix) bo radix precision
    (_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b) uh
  have hsumLe' :
      |_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b| ≤
        |_root_.F2R (beta:=radix) uh| *
          (1 + (radix : ℝ) ^ (1 - (precision : Int)) / 2) := by
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestRoundeGeNormal_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      hsumLe ⟨rfl, hradix, hprecision, hvNum, huhDef, huhNormal⟩
  have hzLe := ClosestRoundeLeNormal (beta:=radix) bo radix precision
    (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
      _root_.F2R (beta:=radix) b) z
  have hzLe' : |_root_.F2R (beta:=radix) z| ≤
      |_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
        _root_.F2R (beta:=radix) b| /
          (1 - (radix : ℝ) ^ (1 - (precision : Int)) / 2) := by
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestRoundeLeNormal_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      hzLe ⟨rfl, hradix, hprecision, hvNum, hzDef, hzNormal⟩
  have hrealLe := ClosestRoundeGeNormal (beta:=radix) bo radix precision
    (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
      _root_.F2R (beta:=radix) b) z
  have hrealLe' :
      |_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
        _root_.F2R (beta:=radix) b| ≤
          |_root_.F2R (beta:=radix) z| *
            (1 + (radix : ℝ) ^ (1 - (precision : Int)) / 2) := by
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestRoundeGeNormal_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      hrealLe ⟨rfl, hradix, hprecision, hvNum, hzDef, hzNormal⟩
  have hratios := tBounded_sterbenz_bounds radix precision
    (_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b)
    (_root_.F2R (beta:=radix) pl)
    (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
      _root_.F2R (beta:=radix) b)
    (_root_.F2R (beta:=radix) uh) (_root_.F2R (beta:=radix) z)
    hradix hprecision hrealRewrite hsmall huhNonneg hzNonneg
    huhLe' hsumLe' hzLe' hrealLe'
  let v : FloatSpec.Core.Defs.FlocqFloat radix :=
    Fminus (beta:=radix) uh z
  have hvBound : Fbounded (beta:=radix) bo v := by
    have h := Sterbenz (beta:=radix) bo uh z
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Sterbenz_check, Fminus,
      Id.run, ULift.up_down, v, Int.cast_ofNat] using
      h ⟨hradix, huhNormal.1, hzNormal.1, hratios.1, hratios.2⟩
  have hvValue : _root_.F2R (beta:=radix) v =
      _root_.F2R (beta:=radix) uh - _root_.F2R (beta:=radix) z := by
    have h := Fminus_correct (beta:=radix) uh z
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus,
      Id.run, ULift.up_down, v, Int.cast_ofNat] using h hradix
  exact ⟨v, hvBound, hvValue⟩

private lemma tBounded_large_initial_bounds
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (a x b ph pl : FloatSpec.Core.Defs.FlocqFloat radix)
    (hradix : 1 < radix)
    (hvNum : bo.vNum = Zpower_nat radix precision)
    (hprecision : 3 ≤ precision)
    (haBound : Fbounded (beta:=radix) bo a)
    (hxBound : Fbounded (beta:=radix) bo x)
    (hphNormal : Fnormal (beta:=radix) radix bo ph)
    (hproductExp : -bo.dExp ≤ a.Fexp + x.Fexp)
    (hphDef : Closest (beta:=radix) bo (radix : ℝ)
      (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x) ph)
    (hplDef : _root_.F2R (beta:=radix) pl =
      _root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x -
        _root_.F2R (beta:=radix) ph)
    (hlarge :
      |_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b| / 4 <
        |_root_.F2R (beta:=radix) pl|) :
    a.Fexp + x.Fexp < ph.Fexp ∧
      |_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b| <
        2 * (radix : ℝ) ^ ph.Fexp := by
  have hprecisionNe : precision ≠ 0 := by omega
  have hradixRealGe : (1 : ℝ) ≤ (radix : ℝ) := by
    exact_mod_cast (le_of_lt hradix)
  have hclosestUlp :
      2 * |_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x -
        _root_.F2R (beta:=radix) ph| ≤
          Fulp (beta:=radix) bo radix precision ph := by
    have h := ClosestUlp (beta:=radix) bo radix precision
      (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x) ph
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hphDef, rfl, hradix, hprecisionNe, hvNum⟩
  have hfulp : Fulp (beta:=radix) bo radix precision ph =
      (radix : ℝ) ^ ph.Fexp := by
    have h := CanonicFulp (beta:=radix) radix bo precision ph
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨Or.inl hphNormal, rfl, hradix, hprecisionNe, hvNum⟩
  have hplUlp :
      2 * |_root_.F2R (beta:=radix) pl| ≤ (radix : ℝ) ^ ph.Fexp := by
    rw [hplDef, ← hfulp]
    exact hclosestUlp
  rcases (by
      have h := errorBoundedMult (beta:=radix) bo radix precision a x ph
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, errorBoundedMult_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨rfl, hradix, hprecisionNe, hvNum, haBound, hxBound,
          hproductExp, hphDef⟩) with
    ⟨pl', hpl'Value, hpl'Bound, hpl'Exp⟩
  have hpl'Nonzero : _root_.F2R (beta:=radix) pl' ≠ 0 := by
    intro hzero
    have hplZero : _root_.F2R (beta:=radix) pl = 0 := by
      rw [hplDef, ← hpl'Value, hzero]
    rw [hplZero, abs_zero] at hlarge
    nlinarith [abs_nonneg
      (_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b)]
  have hphExp : pl'.Fexp < ph.Fexp := by
    have h := ClosestErrorExpStrict_from_exp_bound_payload (beta:=radix) bo (radix : ℝ) ph pl'
      (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestErrorExpStrict_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hphNormal.1, hpl'Bound, hphDef, hpl'Value, hpl'Nonzero,
        hradixRealGe, by simpa [hfulp] using hclosestUlp⟩
  constructor
  · rw [← hpl'Exp]
    exact hphExp
  · nlinarith [hplUlp]

private lemma tBounded_b_exp_lower
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (ph b : FloatSpec.Core.Defs.FlocqFloat radix)
    (hradix : 1 < radix)
    (hvNum : bo.vNum = Zpower_nat radix precision)
    (hprecision : 3 ≤ precision)
    (hphNormal : Fnormal (beta:=radix) radix bo ph)
    (hbCan : Fcanonic (beta:=radix) radix bo b)
    (hphExpMin : -bo.dExp < ph.Fexp)
    (hsumLt :
      |_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b| <
        2 * (radix : ℝ) ^ ph.Fexp) :
    ph.Fexp - 1 ≤ b.Fexp := by
  have hradixPos : 0 < radix := by omega
  have hradixRealPos : (0 : ℝ) < (radix : ℝ) := by exact_mod_cast hradixPos
  have hradixRealNe : (radix : ℝ) ≠ 0 := ne_of_gt hradixRealPos
  have hpowPos : 0 < (radix : ℝ) ^ ph.Fexp := zpow_pos hradixRealPos _
  have hfourLeSquare : (4 : Int) ≤ radix ^ (2 : Nat) := by
    norm_num [pow_two]
    nlinarith
  have hfourRadixLeCube : 4 * radix ≤ radix ^ (3 : Nat) := by
    rw [show radix ^ (3 : Nat) = radix ^ (2 : Nat) * radix by ring]
    exact mul_le_mul_of_nonneg_right hfourLeSquare (by omega)
  have hcubeLe : radix ^ (3 : Nat) ≤ radix ^ precision :=
    pow_le_pow_right₀ (by omega : (1 : Int) ≤ radix) hprecision
  have hfourRadixLe : 4 * radix ≤ bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact le_trans hfourRadixLeCube hcubeLe
  have htwoRadixLt : 2 * radix < bo.vNum := by omega
  let cNum : Int := bo.vNum - 2 * radix
  let c : FloatSpec.Core.Defs.FlocqFloat radix := ⟨cNum, ph.Fexp - 1⟩
  have hcNumPos : 0 < cNum := by dsimp [cNum]; omega
  have hcBound : Fbounded (beta:=radix) bo c := by
    constructor
    · dsimp [c, cNum]
      rw [abs_of_pos (by omega : 0 < bo.vNum - 2 * radix)]
      omega
    · dsimp [c]
      omega
  have hcNormal : Fnormal (beta:=radix) radix bo c := by
    refine ⟨hcBound, ?_⟩
    have hvNumLeTwice : bo.vNum ≤ 2 * cNum := by
      dsimp [cNum]
      omega
    have htwiceLe : 2 * cNum ≤ radix * cNum := by
      exact mul_le_mul_of_nonneg_right (by omega : (2 : Int) ≤ radix)
        (le_of_lt hcNumPos)
    dsimp [c]
    rw [abs_of_pos (mul_pos hradixPos hcNumPos)]
    exact le_trans hvNumLeTwice htwiceLe
  have hphMantissaLower :
      (bo.vNum : ℝ) / (radix : ℝ) ≤ |(ph.Fnum : ℝ)| := by
    have hnormalReal :
        (bo.vNum : ℝ) ≤ ((|radix * ph.Fnum| : Int) : ℝ) := by
      exact_mod_cast hphNormal.2
    rw [Int.cast_abs, Int.cast_mul, abs_mul,
      abs_of_pos hradixRealPos] at hnormalReal
    exact (div_le_iff₀ hradixRealPos).2 (by nlinarith)
  have hphAbs :
      |_root_.F2R (beta:=radix) ph| =
        |(ph.Fnum : ℝ)| * (radix : ℝ) ^ ph.Fexp := by
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, abs_mul,
      abs_of_pos hpowPos]
  have hphAbsLower :
      ((bo.vNum : ℝ) / (radix : ℝ)) * (radix : ℝ) ^ ph.Fexp ≤
        |_root_.F2R (beta:=radix) ph| := by
    rw [hphAbs]
    exact mul_le_mul_of_nonneg_right hphMantissaLower (le_of_lt hpowPos)
  have htri :
      |_root_.F2R (beta:=radix) ph| ≤
        |_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b| +
          |_root_.F2R (beta:=radix) b| := by
    calc
      |_root_.F2R (beta:=radix) ph| =
          |(_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b) +
            (-_root_.F2R (beta:=radix) b)| := by ring_nf
      _ ≤ |_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b| +
          |-_root_.F2R (beta:=radix) b| := abs_add_le _ _
      _ = |_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b| +
          |_root_.F2R (beta:=radix) b| := by rw [abs_neg]
  have hcAbsFormula :
      |_root_.F2R (beta:=radix) c| =
        ((bo.vNum : ℝ) / (radix : ℝ) - 2) *
          (radix : ℝ) ^ ph.Fexp := by
    have hpowShift :
        (radix : ℝ) ^ (ph.Fexp - 1) =
          (radix : ℝ) ^ ph.Fexp / (radix : ℝ) := by
      rw [zpow_sub₀ hradixRealNe]
      simp
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, c,
      FloatSpec.Core.Defs.FlocqFloat.Fnum,
      FloatSpec.Core.Defs.FlocqFloat.Fexp, abs_mul,
      abs_of_pos (zpow_pos hradixRealPos (ph.Fexp - 1)),
      Int.cast_sub, Int.cast_mul, Int.cast_ofNat]
    rw [abs_of_pos (by exact_mod_cast hcNumPos), hpowShift]
    dsimp [cNum]
    push_cast
    field_simp
  have hcAbsLtB :
      |_root_.F2R (beta:=radix) c| <
        |_root_.F2R (beta:=radix) b| := by
    rw [hcAbsFormula]
    nlinarith [hphAbsLower, htri, hsumLt, hpowPos]
  have h := Fcanonic_Rle_Zle (beta:=radix) radix bo c b hradix rfl
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fcanonic_Rle_Zle_check,
    ULift.down_up, Fcanonic', Int.cast_ofNat] using
    h ⟨Or.inl hcNormal, hbCan, le_of_lt hcAbsLtB⟩

private lemma tBounded_ph_exp_upper
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (a x ph : FloatSpec.Core.Defs.FlocqFloat radix)
    (hradix : 1 < radix)
    (hvNum : bo.vNum = Zpower_nat radix precision)
    (hprecision : 3 ≤ precision)
    (hTotal : TotalP (Closest (beta:=radix) bo (radix : ℝ)))
    (haBound : Fbounded (beta:=radix) bo a)
    (hxBound : Fbounded (beta:=radix) bo x)
    (hphNormal : Fnormal (beta:=radix) radix bo ph)
    (hproductExp : -bo.dExp ≤ a.Fexp + x.Fexp)
    (hphDef : Closest (beta:=radix) bo (radix : ℝ)
      (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x) ph) :
    ph.Fexp ≤ a.Fexp + x.Fexp + (precision : Int) := by
  have hprecisionPos : 0 < precision := by omega
  have hprecisionGtOne : 1 < precision := by omega
  have hradixPos : 0 < radix := by omega
  have hradixRealPos : (0 : ℝ) < (radix : ℝ) := by exact_mod_cast hradixPos
  have hradixRealNe : (radix : ℝ) ≠ 0 := ne_of_gt hradixRealPos
  have hpPredPos : 0 < pPred bo.vNum := by
    have h := pPredMoreThanOne bo radix precision
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, pPredMoreThanOne_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hradix, hprecisionGtOne, hvNum⟩
  let top : FloatSpec.Core.Defs.FlocqFloat radix :=
    ⟨pPred bo.vNum, a.Fexp + x.Fexp + (precision : Int)⟩
  have htopNormal : Fnormal (beta:=radix) radix bo top := by
    have h := FnormalPpred (beta:=radix) radix bo precision
      (a.Fexp + x.Fexp + (precision : Int)) hradix hprecisionPos hvNum
      (by omega)
    simpa only [top, Int.cast_ofNat] using h
  have htopNonneg : 0 ≤ _root_.F2R (beta:=radix) top := by
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, top,
      FloatSpec.Core.Defs.FlocqFloat.Fnum,
      FloatSpec.Core.Defs.FlocqFloat.Fexp]
    exact mul_nonneg (by exact_mod_cast (le_of_lt hpPredPos))
      (le_of_lt (zpow_pos hradixRealPos _))
  have haAbsLePred : |a.Fnum| ≤ pPred bo.vNum := by
    change |a.Fnum| ≤ bo.vNum - 1
    exact (Order.le_sub_one_iff).2 haBound.1
  have hxAbsLe : |x.Fnum| ≤ bo.vNum := le_of_lt hxBound.1
  have hmantissa : |a.Fnum| * |x.Fnum| ≤ pPred bo.vNum * bo.vNum := by
    exact mul_le_mul haAbsLePred hxAbsLe (abs_nonneg x.Fnum)
      (le_of_lt hpPredPos)
  have hvNumReal : (bo.vNum : ℝ) =
      (radix : ℝ) ^ (precision : Int) := by
    rw [hvNum]
    have h := Zpower_nat_Z_powerRZ radix precision
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Zpower_nat_Z_powerRZ_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
  have hproductAbs :
      |_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x| =
        (((|a.Fnum| * |x.Fnum| : Int) : ℝ) *
          (radix : ℝ) ^ (a.Fexp + x.Fexp)) := by
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, abs_mul,
      abs_of_pos (zpow_pos hradixRealPos a.Fexp),
      abs_of_pos (zpow_pos hradixRealPos x.Fexp), Int.cast_abs,
      Int.cast_mul]
    rw [zpow_add₀ hradixRealNe]
    ring
  have htopValue : _root_.F2R (beta:=radix) top =
      ((pPred bo.vNum * bo.vNum : Int) : ℝ) *
        (radix : ℝ) ^ (a.Fexp + x.Fexp) := by
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, top,
      FloatSpec.Core.Defs.FlocqFloat.Fnum,
      FloatSpec.Core.Defs.FlocqFloat.Fexp]
    push_cast
    rw [show a.Fexp + x.Fexp + (precision : Int) =
      (a.Fexp + x.Fexp) + (precision : Int) by ring,
      zpow_add₀ hradixRealNe, ← hvNumReal]
    ring
  have hproductLeTop :
      |_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x| ≤
        _root_.F2R (beta:=radix) top := by
    rw [hproductAbs, htopValue]
    exact mul_le_mul_of_nonneg_right (by exact_mod_cast hmantissa)
      (le_of_lt (zpow_pos hradixRealPos _))
  have hfull : RoundedModeP_full (beta:=radix) bo
      (Closest (beta:=radix) bo (radix : ℝ)) := by
    have h := ClosestRoundedModeP_from_total_payload (beta:=radix) bo (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestRoundedModeP_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hTotal
  have hminmax : MinOrMaxP_float (beta:=radix) bo radix
      (Closest (beta:=radix) bo (radix : ℝ)) := by
    have h := ClosestMinOrMax_from_split_radix_payload (beta:=radix) bo radix (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMinOrMax_check, MinOrMaxP_float,
      Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
  have hrounded : RoundedModeP_float (beta:=radix) bo radix
      (Closest (beta:=radix) bo (radix : ℝ)) :=
    ⟨hfull.1, hfull.2.1, hminmax, hfull.2.2.1⟩
  have hphAbsLe : |_root_.F2R (beta:=radix) ph| ≤
      _root_.F2R (beta:=radix) top := by
    have h := RoundAbsMonotoner_from_rounded_payload (beta:=radix) bo radix
      (Closest (beta:=radix) bo (radix : ℝ))
      (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x) ph top
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RoundAbsMonotoner_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hrounded, htopNormal.1, hphDef, hproductLeTop⟩
  have h := Fcanonic_Rle_Zle (beta:=radix) radix bo ph top hradix rfl
  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fcanonic_Rle_Zle_check,
    ULift.down_up, Fcanonic', top, Int.cast_ofNat] using
    h ⟨Or.inl hphNormal, Or.inl htopNormal,
      by simpa [abs_of_nonneg htopNonneg] using hphAbsLe⟩

private lemma tBounded_z_exp_lt_ph
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (a x b ph pl z : FloatSpec.Core.Defs.FlocqFloat radix)
    (hradix : 1 < radix)
    (hvNum : bo.vNum = Zpower_nat radix precision)
    (hprecision : 3 ≤ precision)
    (hTotal : TotalP (Closest (beta:=radix) bo (radix : ℝ)))
    (hzNormal : Fnormal (beta:=radix) radix bo z)
    (hphExpMin : -bo.dExp < ph.Fexp)
    (hzDef : Closest (beta:=radix) bo (radix : ℝ)
      (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
        _root_.F2R (beta:=radix) b) z)
    (hplDef : _root_.F2R (beta:=radix) pl =
      _root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x -
        _root_.F2R (beta:=radix) ph)
    (hsumLt :
      |_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b| <
        2 * (radix : ℝ) ^ ph.Fexp)
    (hplUlp : 2 * |_root_.F2R (beta:=radix) pl| ≤
      (radix : ℝ) ^ ph.Fexp) :
    z.Fexp < ph.Fexp := by
  have hprecisionNe : precision ≠ 0 := by omega
  have hradixPos : 0 < radix := by omega
  have hradixRealPos : (0 : ℝ) < (radix : ℝ) := by exact_mod_cast hradixPos
  have hradixRealNe : (radix : ℝ) ≠ 0 := ne_of_gt hradixRealPos
  have hpowPos : 0 < (radix : ℝ) ^ ph.Fexp := zpow_pos hradixRealPos _
  have hfourLeSquare : (4 : Int) ≤ radix ^ (2 : Nat) := by
    norm_num [pow_two]
    nlinarith
  have hfourRadixLeCube : 4 * radix ≤ radix ^ (3 : Nat) := by
    rw [show radix ^ (3 : Nat) = radix ^ (2 : Nat) * radix by ring]
    exact mul_le_mul_of_nonneg_right hfourLeSquare (by omega)
  have hcubeLe : radix ^ (3 : Nat) ≤ radix ^ precision :=
    pow_le_pow_right₀ (by omega : (1 : Int) ≤ radix) hprecision
  have hfourRadixLe : 4 * radix ≤ bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact le_trans hfourRadixLeCube hcubeLe
  let three : FloatSpec.Core.Defs.FlocqFloat radix :=
    ⟨3 * radix, ph.Fexp - 1⟩
  have hthreeBound : Fbounded (beta:=radix) bo three := by
    constructor
    · dsimp [three]
      rw [abs_of_pos (mul_pos (by omega : (0 : Int) < 3) hradixPos)]
      omega
    · dsimp [three]
      omega
  have hthreeValue : _root_.F2R (beta:=radix) three =
      3 * (radix : ℝ) ^ ph.Fexp := by
    have hpowShift :
        (radix : ℝ) ^ (ph.Fexp - 1) =
          (radix : ℝ) ^ ph.Fexp / (radix : ℝ) := by
      rw [zpow_sub₀ hradixRealNe]
      simp
    simp only [_root_.F2R, FloatSpec.Core.Defs.F2R, three,
      FloatSpec.Core.Defs.FlocqFloat.Fnum,
      FloatSpec.Core.Defs.FlocqFloat.Fexp, Int.cast_mul,
      Int.cast_ofNat, hpowShift]
    field_simp
  have hrealRewrite :
      _root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
          _root_.F2R (beta:=radix) b =
        (_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b) +
          _root_.F2R (beta:=radix) pl := by
    rw [hplDef]
    ring
  have hrealAbsLe :
      |_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
        _root_.F2R (beta:=radix) b| ≤
          _root_.F2R (beta:=radix) three := by
    rw [hthreeValue, hrealRewrite]
    have htri := abs_add_le
      (_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b)
      (_root_.F2R (beta:=radix) pl)
    nlinarith
  have hfull : RoundedModeP_full (beta:=radix) bo
      (Closest (beta:=radix) bo (radix : ℝ)) := by
    have h := ClosestRoundedModeP_from_total_payload (beta:=radix) bo (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestRoundedModeP_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hTotal
  have hminmax : MinOrMaxP_float (beta:=radix) bo radix
      (Closest (beta:=radix) bo (radix : ℝ)) := by
    have h := ClosestMinOrMax_from_split_radix_payload (beta:=radix) bo radix (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMinOrMax_check, MinOrMaxP_float,
      Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
  have hrounded : RoundedModeP_float (beta:=radix) bo radix
      (Closest (beta:=radix) bo (radix : ℝ)) :=
    ⟨hfull.1, hfull.2.1, hminmax, hfull.2.2.1⟩
  have hzAbsLe : |_root_.F2R (beta:=radix) z| ≤
      _root_.F2R (beta:=radix) three := by
    have h := RoundAbsMonotoner_from_rounded_payload (beta:=radix) bo radix
      (Closest (beta:=radix) bo (radix : ℝ))
      (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
        _root_.F2R (beta:=radix) b) z three
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RoundAbsMonotoner_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hrounded, hthreeBound, hzDef, hrealAbsLe⟩
  let nthree : FloatSpec.Core.Defs.FlocqFloat radix :=
    Fnormalize (beta:=radix) radix bo precision three
  have hnthreeValue : _root_.F2R (beta:=radix) nthree =
      _root_.F2R (beta:=radix) three := by
    have h := FnormalizeCorrect (beta:=radix) radix bo precision three
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
      Id.run, ULift.up_down, nthree, Int.cast_ofNat] using h ⟨rfl, hradix⟩
  have hnthreeCan : Fcanonic (beta:=radix) radix bo nthree := by
    have h := FnormalizeCanonic (beta:=radix) radix bo precision three
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
      Id.run, ULift.up_down, nthree, Int.cast_ofNat] using
      h ⟨hthreeBound, hthreeBound, hprecisionNe, hradix, hvNum⟩
  have hnthreeNonneg : 0 ≤ _root_.F2R (beta:=radix) nthree := by
    rw [hnthreeValue, hthreeValue]
    positivity
  have hzExpLeNorm : z.Fexp ≤ nthree.Fexp := by
    have h := Fcanonic_Rle_Zle (beta:=radix) radix bo z nthree hradix rfl
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fcanonic_Rle_Zle_check,
      ULift.down_up, Fcanonic', Int.cast_ofNat] using
      h ⟨Or.inl hzNormal, hnthreeCan,
        by
          calc
            |_root_.F2R (beta:=radix) z| ≤
                _root_.F2R (beta:=radix) three := hzAbsLe
            _ = _root_.F2R (beta:=radix) nthree := hnthreeValue.symm
            _ = |_root_.F2R (beta:=radix) nthree| :=
              (abs_of_nonneg hnthreeNonneg).symm⟩
  have hnthreeExpLe : nthree.Fexp ≤ three.Fexp := by
    have hvNumPos : 0 < bo.vNum := by
      rw [hvNum, Zpower_nat]
      exact pow_pos hradixPos precision
    have h := FcanonicLeastExp_from_explicit_radix_payload (beta:=radix) radix bo three nthree
      hradix rfl hvNumPos
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicLeastExp_check,
      ULift.down_up, Fbounded', Fcanonic', Int.cast_ofNat] using
      h ⟨hnthreeValue.symm, hthreeBound, hnthreeCan⟩
  dsimp [three] at hnthreeExpLe
  omega

private lemma tBounded_large_case
    (bo : Fbound_skel) (radix : Int) [ValidRadix radix] (precision : Nat)
    (a x b ph pl uh z : FloatSpec.Core.Defs.FlocqFloat radix)
    (hradix : 1 < radix)
    (hvNum : bo.vNum = Zpower_nat radix precision)
    (hprecision : 3 ≤ precision)
    (hBoundExp : ∀ r : ℝ,
      -bo.dExp ≤ (boundR (beta:=radix) radix r).Fexp)
    (hTotal : TotalP (Closest (beta:=radix) bo (radix : ℝ)))
    (hbBound : Fbounded (beta:=radix) bo b)
    (haBound : Fbounded (beta:=radix) bo a)
    (hxBound : Fbounded (beta:=radix) bo x)
    (hbCan : Fcanonic (beta:=radix) radix bo b)
    (hphNormal : Fnormal (beta:=radix) radix bo ph)
    (hzNormal : Fnormal (beta:=radix) radix bo z)
    (hproductExp : -bo.dExp ≤ a.Fexp + x.Fexp)
    (hzDef : Closest (beta:=radix) bo (radix : ℝ)
      (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
        _root_.F2R (beta:=radix) b) z)
    (hphDef : Closest (beta:=radix) bo (radix : ℝ)
      (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x) ph)
    (hplDef : _root_.F2R (beta:=radix) pl =
      _root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x -
        _root_.F2R (beta:=radix) ph)
    (huhDef : Closest (beta:=radix) bo (radix : ℝ)
      (_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b) uh)
    (hlarge :
      |_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b| / 4 <
        |_root_.F2R (beta:=radix) pl|) :
    ∃ v : FloatSpec.Core.Defs.FlocqFloat radix,
      Fbounded (beta:=radix) bo v ∧
        _root_.F2R (beta:=radix) v =
          _root_.F2R (beta:=radix) uh - _root_.F2R (beta:=radix) z := by
  have hprecisionNe : precision ≠ 0 := by omega
  have hprecisionGtOne : 1 < precision := by omega
  have hradixRealPos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (show (0 : Int) < radix by omega)
  have hradixRealNe : (radix : ℝ) ≠ 0 := ne_of_gt hradixRealPos
  have hradixRealGe : (1 : ℝ) ≤ (radix : ℝ) := by
    exact_mod_cast (le_of_lt hradix)
  have hvNumGt : 1 < bo.vNum := by
    have h := vNumbMoreThanOne bo radix precision
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, vNumbMoreThanOne_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hprecisionNe, hradix, hvNum⟩
  rcases tBounded_large_initial_bounds bo radix precision a x b ph pl
      hradix hvNum hprecision haBound hxBound hphNormal hproductExp
      hphDef hplDef hlarge with
    ⟨hproductLtPh, hsumLt⟩
  have hphExpMin : -bo.dExp < ph.Fexp := by omega
  have hbExpLower : ph.Fexp - 1 ≤ b.Fexp :=
    tBounded_b_exp_lower bo radix precision ph b hradix hvNum hprecision
      hphNormal hbCan hphExpMin hsumLt
  let sumRaw : FloatSpec.Core.Defs.FlocqFloat radix :=
    Fplus (beta:=radix) ph b
  have hsumRawValue : _root_.F2R (beta:=radix) sumRaw =
      _root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b := by
    have h := Fplus_correct (beta:=radix) ph b
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fplus_correct_check,
      Id.run, ULift.up_down, sumRaw, Int.cast_ofNat] using h hradix
  have hsumRawExp : sumRaw.Fexp = min ph.Fexp b.Fexp := by
    simpa [sumRaw] using Fplus_exp_eq_min (beta:=radix) ph b
  have hsumTargetExp : ph.Fexp - 1 ≤ sumRaw.Fexp := by
    rw [hsumRawExp]
    exact le_min (by omega) hbExpLower
  have hsumMagnitude :
      |_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b| <
        (radix : ℝ) ^ (ph.Fexp - 1 + (precision : Int)) := by
    have hpowSucc :
        2 * (radix : ℝ) ^ ph.Fexp ≤
          (radix : ℝ) ^ (ph.Fexp + 1) := by
      rw [zpow_add_one₀ hradixRealNe]
      have htwoLeRadix : (2 : ℝ) ≤ (radix : ℝ) := by
        exact_mod_cast (show (2 : Int) ≤ radix by omega)
      simpa [mul_comm] using mul_le_mul_of_nonneg_right
        htwoLeRadix
        (le_of_lt (zpow_pos hradixRealPos ph.Fexp))
    have hpowMono :
        (radix : ℝ) ^ (ph.Fexp + 1) ≤
          (radix : ℝ) ^ (ph.Fexp - 1 + (precision : Int)) :=
      zpow_right_mono₀ hradixRealGe (by omega)
    exact lt_of_lt_of_le hsumLt (le_trans hpowSucc hpowMono)
  rcases BoundedL (beta:=radix) bo radix precision
      (_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b)
      sumRaw (ph.Fexp - 1) rfl hradix hvNum hprecisionGtOne
      hsumTargetExp (by omega) hsumRawValue hsumMagnitude with
    ⟨uh', huh'Value, huh'Bound, huh'Exp⟩
  have huhValueEq : _root_.F2R (beta:=radix) uh =
      _root_.F2R (beta:=radix) uh' := by
    have hdist := huhDef.2 uh' huh'Bound
    rw [huh'Value, sub_self, abs_zero] at hdist
    have hzero :
        |_root_.F2R (beta:=radix) uh -
          (_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b)| = 0 :=
      le_antisymm hdist (abs_nonneg _)
    exact (sub_eq_zero.mp (abs_eq_zero.mp hzero)).trans huh'Value.symm
  rcases (by
      have h := errorBoundedMult (beta:=radix) bo radix precision a x ph
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, errorBoundedMult_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨rfl, hradix, hprecisionNe, hvNum, haBound, hxBound,
          hproductExp, hphDef⟩) with
    ⟨pl', hpl'Value, hpl'Bound, hpl'Exp⟩
  let exactRaw : FloatSpec.Core.Defs.FlocqFloat radix :=
    Fplus (beta:=radix) (Fplus (beta:=radix) ph pl') b
  have hexactRawValue : _root_.F2R (beta:=radix) exactRaw =
      _root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
        _root_.F2R (beta:=radix) b := by
    have hplus1 := Fplus_correct (beta:=radix) ph pl'
    have hplus2 := Fplus_correct (beta:=radix)
      (Fplus (beta:=radix) ph pl') b
    have hplus1' :
        _root_.F2R (beta:=radix) (Fplus (beta:=radix) ph pl') =
          _root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) pl' := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fplus_correct_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using hplus1 hradix
    calc
      _root_.F2R (beta:=radix) exactRaw =
          _root_.F2R (beta:=radix) (Fplus (beta:=radix) ph pl') +
            _root_.F2R (beta:=radix) b := by
              simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fplus_correct_check,
                Id.run, ULift.up_down, exactRaw, Int.cast_ofNat] using hplus2 hradix
      _ = _root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
          _root_.F2R (beta:=radix) b := by rw [hplus1', hpl'Value]; ring
  have hexactRawExpLower : a.Fexp + x.Fexp ≤ exactRaw.Fexp := by
    have hinner :
        (Fplus (beta:=radix) ph pl').Fexp = min ph.Fexp pl'.Fexp :=
      Fplus_exp_eq_min (beta:=radix) ph pl'
    have houter : exactRaw.Fexp =
        min (Fplus (beta:=radix) ph pl').Fexp b.Fexp := by
      simpa [exactRaw] using
        Fplus_exp_eq_min (beta:=radix) (Fplus (beta:=radix) ph pl') b
    rw [houter, hinner, hpl'Exp]
    apply le_min
    · exact le_min (le_of_lt hproductLtPh) (le_refl _)
    · omega
  have hzClosestRaw : Closest (beta:=radix) bo (radix : ℝ)
      (_root_.F2R (beta:=radix) exactRaw) z := by
    rw [hexactRawValue]
    exact hzDef
  rcases (by
      have h := LeExpRound2_from_format_payload (beta:=radix) bo radix precision
        (a.Fexp + x.Fexp) exactRaw z
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, LeExpRound2_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨rfl, hradix, hprecisionNe, hvNumGt, hvNum,
          hzClosestRaw, hexactRawExpLower⟩) with
    ⟨z', hz'Bound, hz'Value, hz'ExpLower⟩
  let diffRaw : FloatSpec.Core.Defs.FlocqFloat radix :=
    Fminus (beta:=radix) uh' z'
  have hdiffRawValue : _root_.F2R (beta:=radix) diffRaw =
      _root_.F2R (beta:=radix) uh - _root_.F2R (beta:=radix) z := by
    have h := Fminus_correct (beta:=radix) uh' z'
    calc
      _root_.F2R (beta:=radix) diffRaw =
          _root_.F2R (beta:=radix) uh' - _root_.F2R (beta:=radix) z' := by
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fminus_correct_check, Fminus,
              Id.run, ULift.up_down, diffRaw, Int.cast_ofNat] using h hradix
      _ = _root_.F2R (beta:=radix) uh - _root_.F2R (beta:=radix) z := by
            rw [← huhValueEq, hz'Value]
  have hdiffRawExpLower : a.Fexp + x.Fexp ≤ diffRaw.Fexp := by
    have hdiffExp : diffRaw.Fexp = min uh'.Fexp z'.Fexp := by
      simpa [diffRaw] using Fminus_exp_eq_min (beta:=radix) uh' z'
    rw [hdiffExp, huh'Exp]
    exact le_min (by omega) hz'ExpLower
  have hclosestPhUlp : 2 * |_root_.F2R (beta:=radix) pl| ≤
      Fulp (beta:=radix) bo radix precision ph := by
    have h := ClosestUlp (beta:=radix) bo radix precision
      (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x) ph
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
      Id.run, ULift.up_down, hplDef, Int.cast_ofNat] using
      h ⟨hphDef, rfl, hradix, hprecisionNe, hvNum⟩
  have hphFulp : Fulp (beta:=radix) bo radix precision ph =
      (radix : ℝ) ^ ph.Fexp := by
    have h := CanonicFulp (beta:=radix) radix bo precision ph
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨Or.inl hphNormal, rfl, hradix, hprecisionNe, hvNum⟩
  have hplUlp : 2 * |_root_.F2R (beta:=radix) pl| ≤
      (radix : ℝ) ^ ph.Fexp := by rw [← hphFulp]; exact hclosestPhUlp
  have hzExpLtPh := tBounded_z_exp_lt_ph bo radix precision a x b ph pl z
    hradix hvNum hprecision hTotal hzNormal hphExpMin hzDef hplDef hsumLt hplUlp
  have hzErrorLt :
      2 * |_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
        _root_.F2R (beta:=radix) b - _root_.F2R (beta:=radix) z| <
          (radix : ℝ) ^ ph.Fexp := by
    have hclosest :
        2 * |_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
          _root_.F2R (beta:=radix) b - _root_.F2R (beta:=radix) z| ≤
            Fulp (beta:=radix) bo radix precision z := by
      have h := ClosestUlp (beta:=radix) bo radix precision
        (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
          _root_.F2R (beta:=radix) b) z
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨hzDef, rfl, hradix, hprecisionNe, hvNum⟩
    have hzFulp : Fulp (beta:=radix) bo radix precision z =
        (radix : ℝ) ^ z.Fexp := by
      have h := CanonicFulp (beta:=radix) radix bo precision z
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨Or.inl hzNormal, rfl, hradix, hprecisionNe, hvNum⟩
    have hradixRealOneLt : (1 : ℝ) < (radix : ℝ) := by
      exact_mod_cast hradix
    have hpowLt : (radix : ℝ) ^ z.Fexp < (radix : ℝ) ^ ph.Fexp :=
      zpow_lt_zpow_right₀ hradixRealOneLt hzExpLtPh
    exact lt_of_le_of_lt (by simpa [hzFulp] using hclosest) hpowLt
  have htargetRewrite :
      _root_.F2R (beta:=radix) uh - _root_.F2R (beta:=radix) z =
        (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
          _root_.F2R (beta:=radix) b - _root_.F2R (beta:=radix) z) -
            _root_.F2R (beta:=radix) pl := by
    rw [huhValueEq, huh'Value, hplDef]
    ring
  have htargetLtPh :
      |_root_.F2R (beta:=radix) uh - _root_.F2R (beta:=radix) z| <
        (radix : ℝ) ^ ph.Fexp := by
    rw [htargetRewrite]
    have htri := abs_sub
      (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
        _root_.F2R (beta:=radix) b - _root_.F2R (beta:=radix) z)
      (_root_.F2R (beta:=radix) pl)
    nlinarith [hplUlp]
  have hphExpUpper := tBounded_ph_exp_upper bo radix precision a x ph
    hradix hvNum hprecision hTotal haBound hxBound hphNormal hproductExp hphDef
  have htargetMagnitude :
      |_root_.F2R (beta:=radix) uh - _root_.F2R (beta:=radix) z| <
        (radix : ℝ) ^ (a.Fexp + x.Fexp + (precision : Int)) := by
    exact lt_of_lt_of_le htargetLtPh
      (zpow_right_mono₀ hradixRealGe hphExpUpper)
  rcases BoundedL (beta:=radix) bo radix precision
      (_root_.F2R (beta:=radix) uh - _root_.F2R (beta:=radix) z)
      diffRaw (a.Fexp + x.Fexp) rfl hradix hvNum hprecisionGtOne
      hdiffRawExpLower hproductExp hdiffRawValue htargetMagnitude with
    ⟨v, hvValue, hvBound, _hvExp⟩
  exact ⟨v, hvBound, hvValue⟩

/-- Coq `tBounded_aux`: the difference between the rounded high addition and
the rounded fused result has a bounded float representation. -/
theorem tBounded_aux {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a x b ph pl uh z : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : bo.vNum = Zpower_nat radix precision)
    (hprecision : 3 ≤ precision)
    (hbBound : Fbounded (beta:=beta) bo b)
    (haBound : Fbounded (beta:=beta) bo a)
    (hxBound : Fbounded (beta:=beta) bo x)
    (hbCan : Fcanonic (beta:=beta) radix bo b)
    (hphNormal : Fnormal (beta:=beta) radix bo ph)
    (hzNormal : Fnormal (beta:=beta) radix bo z)
    (huhNormal : Fnormal (beta:=beta) radix bo uh)
    (hproductExp : -bo.dExp ≤ a.Fexp + x.Fexp)
    (hzDef : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
        _root_.F2R (beta:=beta) b) z)
    (hphDef : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) ph)
    (hplDef : _root_.F2R (beta:=beta) pl =
      _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
        _root_.F2R (beta:=beta) ph)
    (huhDef : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) ph + _root_.F2R (beta:=beta) b) uh)
    (hPosit : 0 ≤
      _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
        _root_.F2R (beta:=beta) b) :
    ∃ v : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo v ∧
        _root_.F2R (beta:=beta) v =
          _root_.F2R (beta:=beta) uh - _root_.F2R (beta:=beta) z := by
  subst beta
  have hBoundExp : ∀ r : ℝ,
      -bo.dExp ≤ (boundR (beta:=radix) radix r).Fexp := by
    intro r
    exact le_trans (neg_nonpos.mpr bo.dExp_nonneg) (by simp [boundR, boundNat])
  have hprecisionNe : precision ≠ 0 := by omega
  have hvNumGt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecisionNe
  have hMinTotal : TotalP (isMin (beta:=radix) bo radix) := by
    intro r
    have h := MinEx_from_finite_box_payload (beta:=radix) bo radix r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hvNumGt, hBoundExp r⟩
  have hMaxTotal : TotalP (isMax (beta:=radix) bo radix) := by
    intro r
    have h := MaxEx_from_finite_box_payload (beta:=radix) bo radix r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MaxEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hvNumGt, hBoundExp r⟩
  have hTotal : TotalP (Closest (beta:=radix) bo (radix : ℝ)) := by
    intro r
    have h := ClosestTotal_from_extrema_payload (beta:=radix) bo radix (radix : ℝ) r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestTotal_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨hMinTotal, hMaxTotal⟩
  by_cases hsumZero :
      _root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b = 0
  · exact tBounded_zero_case bo radix precision ph b uh z hradix hvNum
      hzDef.1 huhDef hsumZero
  · by_cases hsmall :
        |_root_.F2R (beta:=radix) pl| ≤
          |_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b| / 4
    · exact tBounded_small_case bo radix precision a x b ph pl uh z
        hradix hvNum hprecision hTotal hbBound hphNormal hzNormal huhNormal
        hzDef hphDef hplDef huhDef hPosit hsmall
    · have hlarge :
          |_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b| / 4 <
            |_root_.F2R (beta:=radix) pl| := lt_of_not_ge hsmall
      exact tBounded_large_case bo radix precision a x b ph pl uh z
        hradix hvNum hprecision hBoundExp hTotal hbBound haBound hxBound hbCan
        hphNormal hzNormal hproductExp hzDef hphDef hplDef huhDef hlarge

/-- Coq `tBounded`: the FMA correction term `uh - z` is representable by a
bounded float, allowing the rounded values to be either normal or zero. -/
theorem tBounded {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a x b ph pl uh z : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : bo.vNum = Zpower_nat radix precision)
    (hprecision : 3 ≤ precision)
    (hbBound : Fbounded (beta:=beta) bo b)
    (haBound : Fbounded (beta:=beta) bo a)
    (hxBound : Fbounded (beta:=beta) bo x)
    (hbCan : Fcanonic (beta:=beta) radix bo b)
    (hphNZ : Fnormal (beta:=beta) radix bo ph ∨
      _root_.F2R (beta:=beta) ph = 0)
    (hzNZ : Fnormal (beta:=beta) radix bo z ∨
      _root_.F2R (beta:=beta) z = 0)
    (huhNZ : Fnormal (beta:=beta) radix bo uh ∨
      _root_.F2R (beta:=beta) uh = 0)
    (hproductExp : -bo.dExp ≤ a.Fexp + x.Fexp)
    (hzDef : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
        _root_.F2R (beta:=beta) b) z)
    (hphDef : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) ph)
    (hplDef : _root_.F2R (beta:=beta) pl =
      _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
        _root_.F2R (beta:=beta) ph)
    (huhDef : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) ph + _root_.F2R (beta:=beta) b) uh) :
    ∃ v : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded (beta:=beta) bo v ∧
        _root_.F2R (beta:=beta) v =
          _root_.F2R (beta:=beta) uh - _root_.F2R (beta:=beta) z := by
  subst beta
  have hBoundExp : ∀ r : ℝ,
      -bo.dExp ≤ (boundR (beta:=radix) radix r).Fexp := by
    intro r
    exact le_trans (neg_nonpos.mpr bo.dExp_nonneg) (by simp [boundR, boundNat])
  have hprecisionNe : precision ≠ 0 := by omega
  have hvNumGt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecisionNe
  have hMinTotal : TotalP (isMin (beta:=radix) bo radix) := by
    intro r
    have h := MinEx_from_finite_box_payload (beta:=radix) bo radix r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hvNumGt, hBoundExp r⟩
  have hMaxTotal : TotalP (isMax (beta:=radix) bo radix) := by
    intro r
    have h := MaxEx_from_finite_box_payload (beta:=radix) bo radix r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MaxEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hvNumGt, hBoundExp r⟩
  have hTotal : TotalP (Closest (beta:=radix) bo (radix : ℝ)) := by
    intro r
    have h := ClosestTotal_from_extrema_payload (beta:=radix) bo radix (radix : ℝ) r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestTotal_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨hMinTotal, hMaxTotal⟩
  rcases huhNZ with huhNormal | huhZero
  · rcases hzNZ with hzNormal | hzZero
    · rcases hphNZ with hphNormal | hphZero
      · by_cases hPosit : 0 ≤
            _root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
              _root_.F2R (beta:=radix) b
        · exact tBounded_aux bo radix precision a x b ph pl uh z rfl
            hradix hvNum hprecision hbBound haBound hxBound hbCan
            hphNormal hzNormal huhNormal hproductExp hzDef hphDef hplDef
            huhDef hPosit
        · have hsumNeg :
              _root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
                _root_.F2R (beta:=radix) b < 0 := lt_of_not_ge hPosit
          have haOppVal :
              _root_.F2R (beta:=radix) (Fopp (beta:=radix) a) =
                -_root_.F2R (beta:=radix) a := by
            have h := Fopp_correct (beta:=radix) a
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fopp_correct_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
          have hbOppVal :
              _root_.F2R (beta:=radix) (Fopp (beta:=radix) b) =
                -_root_.F2R (beta:=radix) b := by
            have h := Fopp_correct (beta:=radix) b
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fopp_correct_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
          have hphOppVal :
              _root_.F2R (beta:=radix) (Fopp (beta:=radix) ph) =
                -_root_.F2R (beta:=radix) ph := by
            have h := Fopp_correct (beta:=radix) ph
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fopp_correct_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
          have hplOppVal :
              _root_.F2R (beta:=radix) (Fopp (beta:=radix) pl) =
                -_root_.F2R (beta:=radix) pl := by
            have h := Fopp_correct (beta:=radix) pl
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fopp_correct_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
          have huhOppVal :
              _root_.F2R (beta:=radix) (Fopp (beta:=radix) uh) =
                -_root_.F2R (beta:=radix) uh := by
            have h := Fopp_correct (beta:=radix) uh
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fopp_correct_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
          have hzOppVal :
              _root_.F2R (beta:=radix) (Fopp (beta:=radix) z) =
                -_root_.F2R (beta:=radix) z := by
            have h := Fopp_correct (beta:=radix) z
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fopp_correct_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
          have hbBoundOpp :
              Fbounded (beta:=radix) bo (Fopp (beta:=radix) b) := by
            have h := oppBounded (beta:=radix) bo b
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, oppBounded_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using h hbBound
          have haBoundOpp :
              Fbounded (beta:=radix) bo (Fopp (beta:=radix) a) := by
            have h := oppBounded (beta:=radix) bo a
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, oppBounded_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using h haBound
          have hbCanOpp :
              Fcanonic (beta:=radix) radix bo (Fopp (beta:=radix) b) := by
            have h := FcanonicFopp (beta:=radix) radix bo b
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFopp_check]
              using h hbCan
          have hphNormalOpp :
              Fnormal (beta:=radix) radix bo (Fopp (beta:=radix) ph) := by
            have h := FnormalFop (beta:=radix) bo radix ph
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalFop_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using h hphNormal
          have hzNormalOpp :
              Fnormal (beta:=radix) radix bo (Fopp (beta:=radix) z) := by
            have h := FnormalFop (beta:=radix) bo radix z
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalFop_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using h hzNormal
          have huhNormalOpp :
              Fnormal (beta:=radix) radix bo (Fopp (beta:=radix) uh) := by
            have h := FnormalFop (beta:=radix) bo radix uh
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalFop_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using h huhNormal
          have hproductExpOpp :
              -bo.dExp ≤ (Fopp (beta:=radix) a).Fexp + x.Fexp := by
            simpa [Fopp, FloatSpec.Calc.Operations.Fopp] using hproductExp
          have hzDefOppRaw :
              Closest (beta:=radix) bo (radix : ℝ)
                (-(_root_.F2R (beta:=radix) a *
                    _root_.F2R (beta:=radix) x +
                  _root_.F2R (beta:=radix) b))
                (Fopp (beta:=radix) z) := by
            have h := ClosestOpp (beta:=radix) bo (radix : ℝ) z
              (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
                _root_.F2R (beta:=radix) b)
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestOpp_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using h hzDef
          have hzDefOpp :
              Closest (beta:=radix) bo (radix : ℝ)
                (_root_.F2R (beta:=radix) (Fopp (beta:=radix) a) *
                    _root_.F2R (beta:=radix) x +
                  _root_.F2R (beta:=radix) (Fopp (beta:=radix) b))
                (Fopp (beta:=radix) z) := by
            simpa [haOppVal, hbOppVal, add_comm] using hzDefOppRaw
          have hphDefOppRaw :
              Closest (beta:=radix) bo (radix : ℝ)
                (-(_root_.F2R (beta:=radix) a *
                    _root_.F2R (beta:=radix) x))
                (Fopp (beta:=radix) ph) := by
            have h := ClosestOpp (beta:=radix) bo (radix : ℝ) ph
              (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x)
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestOpp_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using h hphDef
          have hphDefOpp :
              Closest (beta:=radix) bo (radix : ℝ)
                (_root_.F2R (beta:=radix) (Fopp (beta:=radix) a) *
                  _root_.F2R (beta:=radix) x)
                (Fopp (beta:=radix) ph) := by
            simpa [haOppVal] using hphDefOppRaw
          have hplDefOpp :
              _root_.F2R (beta:=radix) (Fopp (beta:=radix) pl) =
                _root_.F2R (beta:=radix) (Fopp (beta:=radix) a) *
                    _root_.F2R (beta:=radix) x -
                  _root_.F2R (beta:=radix) (Fopp (beta:=radix) ph) := by
            rw [hplOppVal, haOppVal, hphOppVal, hplDef]
            ring
          have huhDefOppRaw :
              Closest (beta:=radix) bo (radix : ℝ)
                (-(_root_.F2R (beta:=radix) ph +
                  _root_.F2R (beta:=radix) b))
                (Fopp (beta:=radix) uh) := by
            have h := ClosestOpp (beta:=radix) bo (radix : ℝ) uh
              (_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b)
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestOpp_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using h huhDef
          have huhDefOpp :
              Closest (beta:=radix) bo (radix : ℝ)
                (_root_.F2R (beta:=radix) (Fopp (beta:=radix) ph) +
                  _root_.F2R (beta:=radix) (Fopp (beta:=radix) b))
                (Fopp (beta:=radix) uh) := by
            simpa [hphOppVal, hbOppVal, add_comm] using huhDefOppRaw
          have hPositOpp : 0 ≤
              _root_.F2R (beta:=radix) (Fopp (beta:=radix) a) *
                _root_.F2R (beta:=radix) x +
                  _root_.F2R (beta:=radix) (Fopp (beta:=radix) b) := by
            rw [haOppVal, hbOppVal]
            nlinarith
          rcases tBounded_aux bo radix precision
              (Fopp (beta:=radix) a) x (Fopp (beta:=radix) b)
              (Fopp (beta:=radix) ph) (Fopp (beta:=radix) pl)
              (Fopp (beta:=radix) uh) (Fopp (beta:=radix) z)
              rfl hradix hvNum hprecision hbBoundOpp haBoundOpp
              hxBound hbCanOpp hphNormalOpp hzNormalOpp huhNormalOpp
              hproductExpOpp hzDefOpp hphDefOpp hplDefOpp huhDefOpp hPositOpp with
            ⟨v, hvBound, hvVal⟩
          refine ⟨Fopp (beta:=radix) v, ?_, ?_⟩
          · have h := oppBounded (beta:=radix) bo v
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, oppBounded_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using h hvBound
          · have hvOppVal :
                _root_.F2R (beta:=radix) (Fopp (beta:=radix) v) =
                  -_root_.F2R (beta:=radix) v := by
              have h := Fopp_correct (beta:=radix) v
              simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fopp_correct_check,
                Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
            rw [hvOppVal, hvVal, huhOppVal, hzOppVal]
            ring
      · have hRoundedFull :
            RoundedModeP_full (beta:=radix) bo
              (Closest (beta:=radix) bo (radix : ℝ)) := by
          have h := ClosestRoundedModeP_from_total_payload (beta:=radix) bo (radix : ℝ)
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestRoundedModeP_check,
            Id.run, ULift.up_down, Int.cast_ofNat] using h hTotal
        have hproductZero :
            _root_.F2R (beta:=radix) a *
              _root_.F2R (beta:=radix) x = 0 := by
          let ax : FloatSpec.Core.Defs.FlocqFloat radix :=
            FloatSpec.Calc.Operations.Fmult (beta:=radix) a x
          have haxVal :
              _root_.F2R (beta:=radix) ax =
                _root_.F2R (beta:=radix) a *
                  _root_.F2R (beta:=radix) x := by
            have h := Fmult_correct (beta:=radix) a x
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fmult_correct_check,
              Id.run, ULift.up_down, ax, Int.cast_ofNat] using h hradix
          have hzero := ClosestZero1_from_expanded_closest_payload (beta:=radix) bo (radix : ℝ)
            (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x) ph ax
          have hres :
              _root_.F2R (beta:=radix) a *
                _root_.F2R (beta:=radix) x = 0 := by
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestZero1_check,
              Id.run, ULift.down_up, Int.cast_ofNat] using
              hzero ⟨hphDef, hphZero, haxVal.symm, by
                simpa [ax, FloatSpec.Calc.Operations.Fmult] using hproductExp,
                by simpa [Fbounded'] using hphDef.1, by
                  intro g hg
                  exact hphDef.2 g (by simpa [Fbounded'] using hg),
                by exact_mod_cast hradix, hvNumGt⟩
          exact hres
        have hzRoundB :
            Closest (beta:=radix) bo (radix : ℝ)
              (_root_.F2R (beta:=radix) b) z := by
          simpa [hproductZero, zero_add] using hzDef
        have huhRoundB :
            Closest (beta:=radix) bo (radix : ℝ)
              (_root_.F2R (beta:=radix) b) uh := by
          simpa [hphZero, zero_add] using huhDef
        have hzEqB :
            _root_.F2R (beta:=radix) b = _root_.F2R (beta:=radix) z :=
          hRoundedFull.2.2.2.2 b z hbBound hzRoundB
        have huhEqB :
            _root_.F2R (beta:=radix) b = _root_.F2R (beta:=radix) uh :=
          hRoundedFull.2.2.2.2 b uh hbBound huhRoundB
        refine ⟨Fzero radix (-bo.dExp), ?_, ?_⟩
        · have h := FboundedFzero_from_positive_bound_payload (beta:=radix) bo
          exact h (lt_trans zero_lt_one hvNumGt)
        · have hzeroVal : _root_.F2R (beta:=radix) (Fzero radix (-bo.dExp)) = 0 := by
            have h := FzeroisZero (beta:=radix) bo
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FzeroisZero_check,
              Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
          rw [hzeroVal]
          linarith
    · refine ⟨uh, huhDef.1, ?_⟩
      rw [hzZero]
      ring
  · refine ⟨Fopp (beta:=radix) z, ?_, ?_⟩
    · have h := oppBounded (beta:=radix) bo z
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, oppBounded_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h hzDef.1
    · have hzOppVal :
          _root_.F2R (beta:=radix) (Fopp (beta:=radix) z) =
            -_root_.F2R (beta:=radix) z := by
        have h := Fopp_correct (beta:=radix) z
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fopp_correct_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
      rw [hzOppVal, huhZero]
      ring

/-- Coq `ErrFmaApprox_1_aux`: exact-`uh` FMA branch error bound, specialized
to the normal `z`/`w` subcase. -/
theorem ErrFmaApprox_1_aux {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a x b ph pl uh ul z t v w : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : bo.vNum = Zpower_nat radix precision)
    (hprecision : 3 ≤ precision)
    (hbBound : Fbounded (beta:=beta) bo b)
    (haBound : Fbounded (beta:=beta) bo a)
    (hxBound : Fbounded (beta:=beta) bo x)
    (hbCan : Fcanonic (beta:=beta) radix bo b)
    (hphNZ : Fnormal (beta:=beta) radix bo ph ∨
      _root_.F2R (beta:=beta) ph = 0)
    (huhNZ : Fnormal (beta:=beta) radix bo uh ∨
      _root_.F2R (beta:=beta) uh = 0)
    (_hzNZ : Fnormal (beta:=beta) radix bo z ∨
      _root_.F2R (beta:=beta) z = 0)
    (hplBound : Fbounded (beta:=beta) bo pl)
    (hproductExp : -bo.dExp ≤ a.Fexp + x.Fexp)
    (hzDef : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
        _root_.F2R (beta:=beta) b) z)
    (hphDef : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) ph)
    (hplDef : _root_.F2R (beta:=beta) pl =
      _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
        _root_.F2R (beta:=beta) ph)
    (huhDef : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) ph + _root_.F2R (beta:=beta) b) uh)
    (hulDef : _root_.F2R (beta:=beta) ul =
      _root_.F2R (beta:=beta) ph + _root_.F2R (beta:=beta) b -
        _root_.F2R (beta:=beta) uh)
    (htDef : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) uh - _root_.F2R (beta:=beta) z) t)
    (hvDef : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) pl + _root_.F2R (beta:=beta) ul) v)
    (hwDef : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) v) w)
    (hCase1 : _root_.F2R (beta:=beta) ul = 0)
    (hzNormal : Fnormal (beta:=beta) radix bo z)
    (hwNormal : Fnormal (beta:=beta) radix bo w) :
    |_root_.F2R (beta:=beta) z + _root_.F2R (beta:=beta) w -
        (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
          _root_.F2R (beta:=beta) b)|
      ≤ ((3 * (radix : ℝ) / 2 + 1 / 2) *
          (radix : ℝ) ^ (2 - 2 * (precision : Int)) *
            |_root_.F2R (beta:=beta) z|) := by
  subst beta
  have hBoundExp : ∀ r : ℝ,
      -bo.dExp ≤ (boundR (beta:=radix) radix r).Fexp := by
    intro r
    exact le_trans (neg_nonpos.mpr bo.dExp_nonneg) (by simp [boundR, boundNat])
  have hprecisionNe : precision ≠ 0 := by omega
  have hvNumGt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecisionNe
  have hMinTotal : TotalP (isMin (beta:=radix) bo radix) := by
    intro r
    have h := MinEx_from_finite_box_payload (beta:=radix) bo radix r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hvNumGt, hBoundExp r⟩
  have hMaxTotal : TotalP (isMax (beta:=radix) bo radix) := by
    intro r
    have h := MaxEx_from_finite_box_payload (beta:=radix) bo radix r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MaxEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hvNumGt, hBoundExp r⟩
  have hTotal : TotalP (Closest (beta:=radix) bo (radix : ℝ)) := by
    intro r
    have h := ClosestTotal_from_extrema_payload (beta:=radix) bo radix (radix : ℝ) r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestTotal_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨hMinTotal, hMaxTotal⟩
  have hRoundedFull :
      RoundedModeP_full (beta:=radix) bo
        (Closest (beta:=radix) bo (radix : ℝ)) := by
    have h := ClosestRoundedModeP_from_total_payload (beta:=radix) bo (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestRoundedModeP_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hTotal
  have hRoundedClosest :
      RoundedModeP_float (beta:=radix) bo radix
        (Closest (beta:=radix) bo (radix : ℝ)) := by
    refine ⟨hTotal, ?_, ?_, ?_⟩
    · have h := ClosestCompatible_from_structural_payload (beta:=radix) bo (radix : ℝ)
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestCompatible_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
    · have h := ClosestMinOrMax_from_split_radix_payload (beta:=radix) bo radix (radix : ℝ)
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMinOrMax_check, MinOrMaxP_float,
        Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
    · have h := ClosestMonotone_from_split_radix_payload (beta:=radix) bo (radix : ℝ)
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMonotone_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
  have htExact :
      _root_.F2R (beta:=radix) t =
        _root_.F2R (beta:=radix) uh - _root_.F2R (beta:=radix) z := by
    rcases tBounded bo radix precision a x b ph pl uh z rfl hradix hvNum
        hprecision hbBound haBound hxBound hbCan hphNZ
        (Or.inl hzNormal) huhNZ hproductExp hzDef hphDef hplDef huhDef with
      ⟨t', htBound, htVal⟩
    have htProject :
        _root_.F2R (beta:=radix) t' = _root_.F2R (beta:=radix) t := by
      exact hRoundedFull.2.2.2.2 t' t htBound (by simpa [htVal] using htDef)
    rw [← htProject, htVal]
  have hvExact :
      _root_.F2R (beta:=radix) v = _root_.F2R (beta:=radix) pl := by
    have hvProject :
        _root_.F2R (beta:=radix) pl = _root_.F2R (beta:=radix) v :=
      hRoundedFull.2.2.2.2 pl v hplBound (by simpa [hCase1] using hvDef)
    exact hvProject.symm
  have hwRoundUlp :
      |_root_.F2R (beta:=radix) t + _root_.F2R (beta:=radix) v -
          _root_.F2R (beta:=radix) w|
        < Fulp (beta:=radix) bo radix precision w := by
    have h := RoundedModeUlp_from_rounded_payload (beta:=radix) bo radix precision
      (Closest (beta:=radix) bo (radix : ℝ))
      (_root_.F2R (beta:=radix) t + _root_.F2R (beta:=radix) v) w
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RoundedModeUlp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hRoundedClosest, hwDef, rfl, hradix, hprecisionNe, hvNum⟩
  have hzRoundUlp :
      |_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
          _root_.F2R (beta:=radix) b - _root_.F2R (beta:=radix) z|
        < Fulp (beta:=radix) bo radix precision z := by
    have h := RoundedModeUlp_from_rounded_payload (beta:=radix) bo radix precision
      (Closest (beta:=radix) bo (radix : ℝ))
      (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
        _root_.F2R (beta:=radix) b) z
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RoundedModeUlp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hRoundedClosest, hzDef, rfl, hradix, hprecisionNe, hvNum⟩
  have hmainToW :
      |_root_.F2R (beta:=radix) z + _root_.F2R (beta:=radix) w -
          (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
            _root_.F2R (beta:=radix) b)|
        ≤ Fulp (beta:=radix) bo radix precision w := by
    have huhExactReal :
        _root_.F2R (beta:=radix) uh =
          _root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b := by
      linarith [hulDef, hCase1]
    have halg :
        _root_.F2R (beta:=radix) z + _root_.F2R (beta:=radix) w -
            (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
              _root_.F2R (beta:=radix) b) =
          -((_root_.F2R (beta:=radix) t + _root_.F2R (beta:=radix) v) -
            _root_.F2R (beta:=radix) w) := by
      rw [htExact, hvExact, hplDef, huhExactReal]
      ring
    rw [halg, abs_neg]
    exact le_of_lt hwRoundUlp
  have hwNormEq :
      Fnormalize (beta:=radix) radix bo precision w = w := by
    have h := FcanonicFnormalizeEq (beta:=radix) radix bo precision w
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨Or.inl hwNormal, rfl, hradix, hprecisionNe, hvNum⟩
  have hzNormEq :
      Fnormalize (beta:=radix) radix bo precision z = z := by
    have h := FcanonicFnormalizeEq (beta:=radix) radix bo precision z
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨Or.inl hzNormal, rfl, hradix, hprecisionNe, hvNum⟩
  have hwFulpRel :
      Fulp (beta:=radix) bo radix precision w ≤
        |_root_.F2R (beta:=radix) w| *
          (radix : ℝ) ^ (1 - (precision : Int)) := by
    have h := FulpLe2 (beta:=radix) radix bo precision w
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpLe2_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hwDef.1, by simpa [hwNormEq] using hwNormal, rfl, hradix,
        hprecisionNe, hvNum⟩
  have hzFulpRel :
      Fulp (beta:=radix) bo radix precision z ≤
        |_root_.F2R (beta:=radix) z| *
          (radix : ℝ) ^ (1 - (precision : Int)) := by
    have h := FulpLe2 (beta:=radix) radix bo precision z
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpLe2_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hzDef.1, by simpa [hzNormEq] using hzNormal, rfl, hradix,
        hprecisionNe, hvNum⟩
  have hwAbsBound :
      |_root_.F2R (beta:=radix) w| ≤
        3 * |_root_.F2R (beta:=radix) z| *
          (radix : ℝ) ^ (1 - (precision : Int)) := by
    have hpowNonneg :
        0 ≤ (radix : ℝ) ^ (1 - (precision : Int)) :=
      le_of_lt (zpow_pos (by exact_mod_cast (by omega : (0 : Int) < radix)) _)
    have htri :
        |_root_.F2R (beta:=radix) w| ≤
          |_root_.F2R (beta:=radix) z + _root_.F2R (beta:=radix) w -
              (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
                _root_.F2R (beta:=radix) b)| +
            |_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
                _root_.F2R (beta:=radix) b - _root_.F2R (beta:=radix) z| := by
      calc
        |_root_.F2R (beta:=radix) w| =
            |(_root_.F2R (beta:=radix) z + _root_.F2R (beta:=radix) w -
              (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
                _root_.F2R (beta:=radix) b)) +
              (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
                _root_.F2R (beta:=radix) b - _root_.F2R (beta:=radix) z)| := by
              ring_nf
        _ ≤ _ := abs_add_le _ _
    have hsum :
        |_root_.F2R (beta:=radix) w| ≤
          Fulp (beta:=radix) bo radix precision w +
            Fulp (beta:=radix) bo radix precision z := by
      exact le_trans htri (add_le_add hmainToW (le_of_lt hzRoundUlp))
    have htmp :
        |_root_.F2R (beta:=radix) w| ≤
          |_root_.F2R (beta:=radix) w| *
              (radix : ℝ) ^ (1 - (precision : Int)) +
            |_root_.F2R (beta:=radix) z| *
              (radix : ℝ) ^ (1 - (precision : Int)) :=
      le_trans hsum (add_le_add hwFulpRel hzFulpRel)
    have hsmall :
        (radix : ℝ) ^ (1 - (precision : Int)) ≤ 1 / 4 := by
      have hradixReal_ge_two : (2 : ℝ) ≤ (radix : ℝ) := by
        exact_mod_cast (by omega : (2 : Int) ≤ radix)
      have hbase :=
        zpow_le_zpow_right₀ (le_trans (by norm_num : (1 : ℝ) ≤ 2) hradixReal_ge_two)
          (by omega : (1 - (precision : Int)) ≤ -2)
      have htwo : (2 : ℝ) ^ (-2 : Int) = 1 / 4 := by norm_num [zpow_neg]
      calc
        (radix : ℝ) ^ (1 - (precision : Int)) ≤
            (radix : ℝ) ^ (-2 : Int) := hbase
        _ ≤ (2 : ℝ) ^ (-2 : Int) := by
          have hradixSqGe : (2 : ℝ) ^ 2 ≤ (radix : ℝ) ^ 2 :=
            pow_le_pow_left₀ (by norm_num : (0 : ℝ) ≤ 2) hradixReal_ge_two 2
          have htwoSqPos : 0 < (2 : ℝ) ^ 2 := by norm_num
          calc
            (radix : ℝ) ^ (-2 : Int) = 1 / (radix : ℝ) ^ 2 := by
              rw [zpow_neg]
              exact inv_eq_one_div ((radix : ℝ) ^ 2)
            _ ≤ 1 / (2 : ℝ) ^ 2 :=
              one_div_le_one_div_of_le htwoSqPos hradixSqGe
            _ = (2 : ℝ) ^ (-2 : Int) := by
              rw [zpow_neg]
              exact (inv_eq_one_div ((2 : ℝ) ^ 2)).symm
        _ = 1 / 4 := htwo
    have hcoef :
        0 < 1 - (radix : ℝ) ^ (1 - (precision : Int)) := by
      nlinarith
    nlinarith [htmp, hsmall, hpowNonneg,
      abs_nonneg (_root_.F2R (beta:=radix) w),
      abs_nonneg (_root_.F2R (beta:=radix) z)]
  have hfinalRaw :
      |_root_.F2R (beta:=radix) z + _root_.F2R (beta:=radix) w -
          (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
            _root_.F2R (beta:=radix) b)|
        ≤ 3 * (radix : ℝ) ^ (2 - 2 * (precision : Int)) *
            |_root_.F2R (beta:=radix) z| := by
    have hpowNonneg :
        0 ≤ (radix : ℝ) ^ (1 - (precision : Int)) :=
      le_of_lt (zpow_pos (by exact_mod_cast (by omega : (0 : Int) < radix)) _)
    have hstep := le_trans hmainToW hwFulpRel
    have hstep' :
        |_root_.F2R (beta:=radix) z + _root_.F2R (beta:=radix) w -
            (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
              _root_.F2R (beta:=radix) b)|
          ≤ (3 * |_root_.F2R (beta:=radix) z| *
              (radix : ℝ) ^ (1 - (precision : Int))) *
            (radix : ℝ) ^ (1 - (precision : Int)) :=
      le_trans hstep (mul_le_mul_of_nonneg_right hwAbsBound hpowNonneg)
    have hpowMul :
        (radix : ℝ) ^ (1 - (precision : Int)) *
          (radix : ℝ) ^ (1 - (precision : Int)) =
            (radix : ℝ) ^ (2 - 2 * (precision : Int)) := by
      have hradixNe : (radix : ℝ) ≠ 0 := by
        exact_mod_cast (by omega : (radix : Int) ≠ 0)
      rw [← zpow_add₀ hradixNe]
      congr 1
      ring
    calc
      |_root_.F2R (beta:=radix) z + _root_.F2R (beta:=radix) w -
          (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
            _root_.F2R (beta:=radix) b)|
          ≤ (3 * |_root_.F2R (beta:=radix) z| *
              (radix : ℝ) ^ (1 - (precision : Int))) *
            (radix : ℝ) ^ (1 - (precision : Int)) := hstep'
      _ = 3 * (radix : ℝ) ^ (2 - 2 * (precision : Int)) *
            |_root_.F2R (beta:=radix) z| := by
          rw [mul_assoc, hpowMul]
          ring
  have hcoefLe :
      3 ≤ 3 * (radix : ℝ) / 2 + 1 / 2 := by
    have hradixReal_ge_two : (2 : ℝ) ≤ (radix : ℝ) := by
      exact_mod_cast (by omega : (2 : Int) ≤ radix)
    nlinarith
  have hnonneg :
      0 ≤ (radix : ℝ) ^ (2 - 2 * (precision : Int)) *
        |_root_.F2R (beta:=radix) z| := by
    exact mul_nonneg
      (le_of_lt (zpow_pos (by exact_mod_cast (by omega : (0 : Int) < radix)) _))
      (abs_nonneg _)
  exact le_trans hfinalRaw
    (by
      simpa [mul_assoc] using
        mul_le_mul_of_nonneg_right hcoefLe hnonneg)

/-- Coq `ErrFmaApprox_1`: exact-`uh` FMA branch error bound with the
upstream normal-or-zero hypotheses for both rounded results `z` and `w`. -/
theorem ErrFmaApprox_1 {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a x b ph pl uh ul z t v w : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : bo.vNum = Zpower_nat radix precision)
    (hprecision : 3 ≤ precision)
    (hbBound : Fbounded (beta:=beta) bo b)
    (haBound : Fbounded (beta:=beta) bo a)
    (hxBound : Fbounded (beta:=beta) bo x)
    (hbCan : Fcanonic (beta:=beta) radix bo b)
    (hphNZ : Fnormal (beta:=beta) radix bo ph ∨
      _root_.F2R (beta:=beta) ph = 0)
    (huhNZ : Fnormal (beta:=beta) radix bo uh ∨
      _root_.F2R (beta:=beta) uh = 0)
    (hzNZ : Fnormal (beta:=beta) radix bo z ∨
      _root_.F2R (beta:=beta) z = 0)
    (hwNZ : Fnormal (beta:=beta) radix bo w ∨
      _root_.F2R (beta:=beta) w = 0)
    (hplBound : Fbounded (beta:=beta) bo pl)
    (hproductExp : -bo.dExp ≤ a.Fexp + x.Fexp)
    (hzDef : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
        _root_.F2R (beta:=beta) b) z)
    (hphDef : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) ph)
    (hplDef : _root_.F2R (beta:=beta) pl =
      _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
        _root_.F2R (beta:=beta) ph)
    (huhDef : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) ph + _root_.F2R (beta:=beta) b) uh)
    (hulDef : _root_.F2R (beta:=beta) ul =
      _root_.F2R (beta:=beta) ph + _root_.F2R (beta:=beta) b -
        _root_.F2R (beta:=beta) uh)
    (htDef : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) uh - _root_.F2R (beta:=beta) z) t)
    (hvDef : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) pl + _root_.F2R (beta:=beta) ul) v)
    (hwDef : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) v) w)
    (hCase1 : _root_.F2R (beta:=beta) ul = 0) :
    |_root_.F2R (beta:=beta) z + _root_.F2R (beta:=beta) w -
        (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
          _root_.F2R (beta:=beta) b)|
      ≤ ((3 * (radix : ℝ) / 2 + 1 / 2) *
          (radix : ℝ) ^ (2 - 2 * (precision : Int)) *
            |_root_.F2R (beta:=beta) z|) := by
  subst beta
  have hBoundExp : ∀ r : ℝ,
      -bo.dExp ≤ (boundR (beta:=radix) radix r).Fexp := by
    intro r
    exact le_trans (neg_nonpos.mpr bo.dExp_nonneg) (by simp [boundR, boundNat])
  have hprecisionNe : precision ≠ 0 := by omega
  have hvNumGt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecisionNe
  have hradixReal : (1 : ℝ) < (radix : ℝ) := by exact_mod_cast hradix
  have hradixPos : (0 : ℝ) < (radix : ℝ) := lt_trans zero_lt_one hradixReal
  have hMinTotal : TotalP (isMin (beta:=radix) bo radix) := by
    intro r
    have h := MinEx_from_finite_box_payload (beta:=radix) bo radix r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hvNumGt, hBoundExp r⟩
  have hMaxTotal : TotalP (isMax (beta:=radix) bo radix) := by
    intro r
    have h := MaxEx_from_finite_box_payload (beta:=radix) bo radix r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MaxEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hvNumGt, hBoundExp r⟩
  have hTotal : TotalP (Closest (beta:=radix) bo (radix : ℝ)) := by
    intro r
    have h := ClosestTotal_from_extrema_payload (beta:=radix) bo radix (radix : ℝ) r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestTotal_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨hMinTotal, hMaxTotal⟩
  have hRoundedFull :
      RoundedModeP_full (beta:=radix) bo
        (Closest (beta:=radix) bo (radix : ℝ)) := by
    have h := ClosestRoundedModeP_from_total_payload (beta:=radix) bo (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestRoundedModeP_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hTotal
  have hRhsNonneg :
      0 ≤ ((3 * (radix : ℝ) / 2 + 1 / 2) *
          (radix : ℝ) ^ (2 - 2 * (precision : Int)) *
            |_root_.F2R (beta:=radix) z|) := by
    have hcoef : 0 ≤ 3 * (radix : ℝ) / 2 + 1 / 2 := by nlinarith
    exact mul_nonneg (mul_nonneg hcoef (le_of_lt (zpow_pos hradixPos _)))
      (abs_nonneg _)
  rcases hzNZ with hzNormal | hzZero
  · rcases hwNZ with hwNormal | hwZero
    · exact ErrFmaApprox_1_aux bo radix precision a x b ph pl uh ul z t v w
        rfl hradix hvNum hprecision hbBound haBound hxBound hbCan
        hphNZ huhNZ (Or.inl hzNormal) hplBound hproductExp hzDef hphDef hplDef huhDef hulDef
        htDef hvDef hwDef hCase1 hzNormal hwNormal
    · have htExact :
          _root_.F2R (beta:=radix) t =
            _root_.F2R (beta:=radix) uh - _root_.F2R (beta:=radix) z := by
        rcases tBounded bo radix precision a x b ph pl uh z rfl hradix hvNum
            hprecision hbBound haBound hxBound hbCan hphNZ
            (Or.inl hzNormal) huhNZ hproductExp hzDef hphDef hplDef huhDef with
          ⟨t', htBound, htVal⟩
        have htProject :
            _root_.F2R (beta:=radix) t' = _root_.F2R (beta:=radix) t :=
          hRoundedFull.2.2.2.2 t' t htBound (by simpa [htVal] using htDef)
        rw [← htProject, htVal]
      have hvExact :
          _root_.F2R (beta:=radix) v = _root_.F2R (beta:=radix) pl := by
        have hvProject :
            _root_.F2R (beta:=radix) pl = _root_.F2R (beta:=radix) v :=
          hRoundedFull.2.2.2.2 pl v hplBound (by simpa [hCase1] using hvDef)
        exact hvProject.symm
      let sumtv : FloatSpec.Core.Defs.FlocqFloat radix := Fplus (beta:=radix) t v
      have hsumtvVal :
          _root_.F2R (beta:=radix) sumtv =
            _root_.F2R (beta:=radix) t + _root_.F2R (beta:=radix) v := by
        have h := Fplus_correct (beta:=radix) t v
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fplus_correct_check,
          Id.run, ULift.up_down, sumtv, Int.cast_ofNat] using h hradix
      have hsumtvExp : -bo.dExp ≤ sumtv.Fexp := by
        have htExp : -bo.dExp ≤ t.Fexp := htDef.1.2
        have hvExp : -bo.dExp ≤ v.Fexp := hvDef.1.2
        by_cases htv : t.Fexp ≤ v.Fexp
        · simpa [sumtv, Fplus, FloatSpec.Calc.Operations.Fplus,
            FloatSpec.Calc.Operations.Falign, htv] using htExp
        · simpa [sumtv, Fplus, FloatSpec.Calc.Operations.Fplus,
            FloatSpec.Calc.Operations.Falign, htv] using hvExp
      have hsumtvZero :
          _root_.F2R (beta:=radix) t + _root_.F2R (beta:=radix) v = 0 := by
        have hzero := ClosestZero1_from_expanded_closest_payload (beta:=radix) bo (radix : ℝ)
          (_root_.F2R (beta:=radix) t + _root_.F2R (beta:=radix) v) w sumtv
        have hres :
            _root_.F2R (beta:=radix) t + _root_.F2R (beta:=radix) v = 0 := by
          simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestZero1_check,
            Id.run, ULift.down_up, Int.cast_ofNat] using
            hzero ⟨hwDef, hwZero, hsumtvVal.symm, hsumtvExp,
              by simpa [Fbounded'] using hwDef.1,
              by
                intro g hg
                exact hwDef.2 g (by simpa [Fbounded'] using hg),
              hradixReal, hvNumGt⟩
        exact hres
      have htargetZero :
          _root_.F2R (beta:=radix) z + _root_.F2R (beta:=radix) w -
              (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
                _root_.F2R (beta:=radix) b) = 0 := by
        nlinarith [hplDef, hulDef, hCase1, htExact, hvExact, hsumtvZero, hwZero]
      simpa [htargetZero] using hRhsNonneg
  · let axb : FloatSpec.Core.Defs.FlocqFloat radix :=
      Fplus (beta:=radix) (FloatSpec.Calc.Operations.Fmult (beta:=radix) a x) b
    have hprodVal :
        _root_.F2R (beta:=radix) (FloatSpec.Calc.Operations.Fmult (beta:=radix) a x) =
          _root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x := by
      have h := Fmult_correct (beta:=radix) a x
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fmult_correct_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h hradix
    have haxbVal :
        _root_.F2R (beta:=radix) axb =
          _root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
            _root_.F2R (beta:=radix) b := by
      have h := Fplus_correct (beta:=radix)
        (FloatSpec.Calc.Operations.Fmult (beta:=radix) a x) b
      calc
        _root_.F2R (beta:=radix) axb =
            _root_.F2R (beta:=radix)
                (FloatSpec.Calc.Operations.Fmult (beta:=radix) a x) +
              _root_.F2R (beta:=radix) b := by
                simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fplus_correct_check,
                  Id.run, ULift.up_down, axb, Int.cast_ofNat] using h hradix
        _ = _root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
              _root_.F2R (beta:=radix) b := by rw [hprodVal]
    have haxbExp : -bo.dExp ≤ axb.Fexp := by
      have hbExp : -bo.dExp ≤ b.Fexp := hbBound.2
      by_cases hle : a.Fexp + x.Fexp ≤ b.Fexp
      · simpa [axb, Fplus, FloatSpec.Calc.Operations.Fplus,
          FloatSpec.Calc.Operations.Falign, FloatSpec.Calc.Operations.Fmult, hle]
          using hproductExp
      · simpa [axb, Fplus, FloatSpec.Calc.Operations.Fplus,
          FloatSpec.Calc.Operations.Falign, FloatSpec.Calc.Operations.Fmult, hle]
          using hbExp
    have hinputZero :
        _root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
          _root_.F2R (beta:=radix) b = 0 := by
      have hzero := ClosestZero1_from_expanded_closest_payload (beta:=radix) bo (radix : ℝ)
        (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
          _root_.F2R (beta:=radix) b) z axb
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestZero1_check,
        Id.run, ULift.down_up, Int.cast_ofNat] using
        hzero ⟨hzDef, hzZero, haxbVal.symm, haxbExp,
          by simpa [Fbounded'] using hzDef.1,
          by
            intro g hg
            exact hzDef.2 g (by simpa [Fbounded'] using hg),
          hradixReal, hvNumGt⟩
    have hbOppBound : Fbounded (beta:=radix) bo (Fopp (beta:=radix) b) := by
      have h := oppBounded (beta:=radix) bo b
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, oppBounded_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h hbBound
    have hbOppVal :
        _root_.F2R (beta:=radix) (Fopp (beta:=radix) b) =
          -_root_.F2R (beta:=radix) b := by
      have h := Fopp_correct (beta:=radix) b
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fopp_correct_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
    have hphEqOppB :
        _root_.F2R (beta:=radix) (Fopp (beta:=radix) b) =
          _root_.F2R (beta:=radix) ph := by
      have haxEqOppB :
          _root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x =
            _root_.F2R (beta:=radix) (Fopp (beta:=radix) b) := by
        rw [hbOppVal]
        linarith
      exact hRoundedFull.2.2.2.2 (Fopp (beta:=radix) b) ph hbOppBound
        (by simpa [haxEqOppB] using hphDef)
    have hphPlusBZero :
        _root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b = 0 := by
      linarith [hbOppVal, hphEqOppB]
    have huhZero :
        _root_.F2R (beta:=radix) uh = 0 := by
      have hzero := ClosestZero2 (beta:=radix) bo (radix : ℝ)
        (_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b) uh
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestZero2_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using hzero ⟨huhDef, hphPlusBZero⟩
    have htZero :
        _root_.F2R (beta:=radix) t = 0 := by
      have hzero := ClosestZero2 (beta:=radix) bo (radix : ℝ)
        (_root_.F2R (beta:=radix) uh - _root_.F2R (beta:=radix) z) t
      have harg :
          _root_.F2R (beta:=radix) uh - _root_.F2R (beta:=radix) z = 0 := by
        linarith
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestZero2_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using hzero ⟨htDef, harg⟩
    have hplZero :
        _root_.F2R (beta:=radix) pl = 0 := by
      nlinarith [hplDef, hbOppVal, hphEqOppB, hinputZero]
    have hvZero :
        _root_.F2R (beta:=radix) v = 0 := by
      have hzero := ClosestZero2 (beta:=radix) bo (radix : ℝ)
        (_root_.F2R (beta:=radix) pl + _root_.F2R (beta:=radix) ul) v
      have harg :
          _root_.F2R (beta:=radix) pl + _root_.F2R (beta:=radix) ul = 0 := by
        linarith
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestZero2_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using hzero ⟨hvDef, harg⟩
    have hwZero :
        _root_.F2R (beta:=radix) w = 0 := by
      have hzero := ClosestZero2 (beta:=radix) bo (radix : ℝ)
        (_root_.F2R (beta:=radix) t + _root_.F2R (beta:=radix) v) w
      have harg :
          _root_.F2R (beta:=radix) t + _root_.F2R (beta:=radix) v = 0 := by
        linarith
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestZero2_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using hzero ⟨hwDef, harg⟩
    have htargetZero :
        _root_.F2R (beta:=radix) z + _root_.F2R (beta:=radix) w -
            (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
              _root_.F2R (beta:=radix) b) = 0 := by
      nlinarith
    simpa [htargetZero] using hRhsNonneg

noncomputable def LeExp2_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a x b ph uh ul z : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq `ErrFmaApprox` local lemma `LeExp2`.

In the inexact `uh` branch, the exponent of the rounded `ph + b` result is at
most one greater than the exponent of the rounded FMA result.  The proof follows
the upstream argument: combine the three rounding-error ulp bounds, use
`LeExp1` to compare `Fulp ph` with `radix * Fulp uh`, derive
`|uh| <= radix * |z|`, then compare canonical exponents. -/
theorem LeExp2 {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a x b ph uh ul z : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧
        bo.vNum = Zpower_nat radix precision ∧ 4 ≤ precision ∧
        Fbounded (beta:=beta) bo b ∧
        Fnormal (beta:=beta) radix bo ph ∧
        Fnormal (beta:=beta) radix bo uh ∧
        Fnormal (beta:=beta) radix bo z ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
            _root_.F2R (beta:=beta) b) z ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) ph ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) ph + _root_.F2R (beta:=beta) b) uh ∧
        _root_.F2R (beta:=beta) ul =
          _root_.F2R (beta:=beta) ph + _root_.F2R (beta:=beta) b -
            _root_.F2R (beta:=beta) uh ∧
        ¬ _root_.F2R (beta:=beta) ul = 0⌝⦄
    (pure (LeExp2_check (beta:=beta) bo radix precision a x b ph uh ul z) :
      Id Unit)
    ⦃⇓_ => ⌜uh.Fexp ≤ z.Fexp + 1⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hradix, hvNum, hprecision, hbBound, hphNormal,
      huhNormal, hzNormal, hzDef, hphDef, huhDef, hulDef, hCase2⟩
  simp only [wp, PostCond.noThrow, pure, LeExp2_check, Id.run, ULift.up_down]
  subst beta
  have hBoundExp : ∀ r : ℝ,
      -bo.dExp ≤ (boundR (beta:=radix) radix r).Fexp := by
    intro r
    exact le_trans (neg_nonpos.mpr bo.dExp_nonneg) (by simp [boundR, boundNat])
  have hprecisionNe : precision ≠ 0 := by omega
  have hvNumGt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecisionNe
  let P : ℝ → FloatSpec.Core.Defs.FlocqFloat radix → Prop :=
    Closest (beta:=radix) bo (radix : ℝ)
  have hMinTotal : TotalP (isMin (beta:=radix) bo radix) := by
    intro r
    have h := MinEx_from_finite_box_payload (beta:=radix) bo radix r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hvNumGt, hBoundExp r⟩
  have hMaxTotal : TotalP (isMax (beta:=radix) bo radix) := by
    intro r
    have h := MaxEx_from_finite_box_payload (beta:=radix) bo radix r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MaxEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hvNumGt, hBoundExp r⟩
  have hTotal : TotalP P := by
    intro r
    have h := ClosestTotal_from_extrema_payload (beta:=radix) bo radix (radix : ℝ) r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestTotal_check, Id.run,
      ULift.up_down, P, Int.cast_ofNat] using h ⟨hMinTotal, hMaxTotal⟩
  have hCompat : CompatiblePGeneric P := by
    have h := ClosestCompatible_from_structural_payload (beta:=radix) bo (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestCompatible_check,
      PredTrans.pure, PredTrans.apply, Id.run, ULift.down, P, Int.cast_ofNat] using h trivial
  have hMinMax : MinOrMaxP_float (beta:=radix) bo radix P := by
    have h := ClosestMinOrMax_from_split_radix_payload (beta:=radix) bo radix (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMinOrMax_check, MinOrMaxP_float,
      Id.run, ULift.up_down, P, Int.cast_ofNat] using h trivial
  have hMono : MonotoneP_float P := by
    have h := ClosestMonotone_from_split_radix_payload (beta:=radix) bo (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMonotone_check,
      Id.run, ULift.up_down, P, Int.cast_ofNat] using h trivial
  have hRoundedFloat : RoundedModeP_float (beta:=radix) bo radix P :=
    ⟨hTotal, hCompat, hMinMax, hMono⟩
  have hLeExp1 : ph.Fexp ≤ uh.Fexp + 1 := by
    have h := LeExp1_from_format_payload (beta:=radix) bo radix precision b ph uh ul
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, LeExp1_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, hradix, hprecisionNe, hvNumGt, hvNum, hbBound,
        hphNormal, huhDef, hulDef, hCase2⟩
  have hradixReal : (1 : ℝ) < (radix : ℝ) := by exact_mod_cast hradix
  have hradixRealGe : (1 : ℝ) ≤ (radix : ℝ) := le_of_lt hradixReal
  have hradixRealPos : (0 : ℝ) < (radix : ℝ) := lt_trans zero_lt_one hradixReal
  have hradixRealNe : (radix : ℝ) ≠ 0 := ne_of_gt hradixRealPos
  have huhRoundUlp :
      |_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b -
          _root_.F2R (beta:=radix) uh|
        < Fulp (beta:=radix) bo radix precision uh := by
    have h := RoundedModeUlp_from_rounded_payload (beta:=radix) bo radix precision P
      (_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b) uh
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RoundedModeUlp_check, Id.run,
      ULift.up_down, P, Int.cast_ofNat] using
      h ⟨hRoundedFloat, huhDef, rfl, hradix, hprecisionNe, hvNum⟩
  have hphRoundUlp :
      |_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x -
          _root_.F2R (beta:=radix) ph|
        < Fulp (beta:=radix) bo radix precision ph := by
    have h := RoundedModeUlp_from_rounded_payload (beta:=radix) bo radix precision P
      (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x) ph
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RoundedModeUlp_check, Id.run,
      ULift.up_down, P, Int.cast_ofNat] using
      h ⟨hRoundedFloat, hphDef, rfl, hradix, hprecisionNe, hvNum⟩
  have hzRoundUlp :
      |_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
          _root_.F2R (beta:=radix) b - _root_.F2R (beta:=radix) z|
        < Fulp (beta:=radix) bo radix precision z := by
    have h := RoundedModeUlp_from_rounded_payload (beta:=radix) bo radix precision P
      (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
        _root_.F2R (beta:=radix) b) z
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RoundedModeUlp_check, Id.run,
      ULift.up_down, P, Int.cast_ofNat] using
      h ⟨hRoundedFloat, hzDef, rfl, hradix, hprecisionNe, hvNum⟩
  have hphFulp :
      Fulp (beta:=radix) bo radix precision ph = (radix : ℝ) ^ ph.Fexp := by
    have h := CanonicFulp (beta:=radix) radix bo precision ph
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨Or.inl hphNormal, rfl, hradix, hprecisionNe, hvNum⟩
  have huhFulp :
      Fulp (beta:=radix) bo radix precision uh = (radix : ℝ) ^ uh.Fexp := by
    have h := CanonicFulp (beta:=radix) radix bo precision uh
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨Or.inl huhNormal, rfl, hradix, hprecisionNe, hvNum⟩
  have hphFulpLe :
      Fulp (beta:=radix) bo radix precision ph ≤
        (radix : ℝ) * Fulp (beta:=radix) bo radix precision uh := by
    have hpow :
        (radix : ℝ) ^ ph.Fexp ≤ (radix : ℝ) ^ (uh.Fexp + 1) := by
      have h := Rle_powerRZ (radix : ℝ) ph.Fexp (uh.Fexp + 1)
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Rle_powerRZ_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using h ⟨hradixRealGe, hLeExp1⟩
    have hsucc :
        (radix : ℝ) ^ (uh.Fexp + 1) =
          (radix : ℝ) * (radix : ℝ) ^ uh.Fexp := by
      rw [zpow_add₀ hradixRealNe]
      simp
      ring
    calc
      Fulp (beta:=radix) bo radix precision ph = (radix : ℝ) ^ ph.Fexp := hphFulp
      _ ≤ (radix : ℝ) ^ (uh.Fexp + 1) := hpow
      _ = (radix : ℝ) * Fulp (beta:=radix) bo radix precision uh := by
        rw [hsucc, huhFulp]
  have hdist :
      |_root_.F2R (beta:=radix) uh - _root_.F2R (beta:=radix) z| ≤
        (1 + (radix : ℝ)) * Fulp (beta:=radix) bo radix precision uh +
          Fulp (beta:=radix) bo radix precision z := by
    have hulTerm :
        |(-(_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b -
            _root_.F2R (beta:=radix) uh))|
          ≤ Fulp (beta:=radix) bo radix precision uh := by
      rw [abs_neg]
      exact le_of_lt huhRoundUlp
    have hplTerm :
        |(-(_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x -
            _root_.F2R (beta:=radix) ph))|
          ≤ (radix : ℝ) * Fulp (beta:=radix) bo radix precision uh := by
      rw [abs_neg]
      exact le_trans (le_of_lt hphRoundUlp) hphFulpLe
    have hzTerm :
        |_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
            _root_.F2R (beta:=radix) b - _root_.F2R (beta:=radix) z|
          ≤ Fulp (beta:=radix) bo radix precision z :=
      le_of_lt hzRoundUlp
    have hdecomp :
        _root_.F2R (beta:=radix) uh - _root_.F2R (beta:=radix) z =
          -(_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b -
              _root_.F2R (beta:=radix) uh) +
            -(_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x -
              _root_.F2R (beta:=radix) ph) +
            (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
              _root_.F2R (beta:=radix) b - _root_.F2R (beta:=radix) z) := by
      ring
    rw [hdecomp]
    calc
      |(-(_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b -
            _root_.F2R (beta:=radix) uh) +
          -(_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x -
            _root_.F2R (beta:=radix) ph) +
          (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
            _root_.F2R (beta:=radix) b - _root_.F2R (beta:=radix) z))|
          ≤ |(-(_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b -
                _root_.F2R (beta:=radix) uh) +
              -(_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x -
                _root_.F2R (beta:=radix) ph))| +
            |_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
              _root_.F2R (beta:=radix) b - _root_.F2R (beta:=radix) z| :=
            abs_add_le _ _
      _ ≤ (|(-(_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b -
                _root_.F2R (beta:=radix) uh))| +
              |(-(_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x -
                _root_.F2R (beta:=radix) ph))|) +
            |_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
              _root_.F2R (beta:=radix) b - _root_.F2R (beta:=radix) z| := by
            have htri2 := abs_add_le
                (-(_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b -
                  _root_.F2R (beta:=radix) uh))
                (-(_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x -
                  _root_.F2R (beta:=radix) ph))
            linarith
      _ ≤ (Fulp (beta:=radix) bo radix precision uh +
              (radix : ℝ) * Fulp (beta:=radix) bo radix precision uh) +
            Fulp (beta:=radix) bo radix precision z := by
            exact add_le_add (add_le_add hulTerm hplTerm) hzTerm
      _ = (1 + (radix : ℝ)) *
              Fulp (beta:=radix) bo radix precision uh +
            Fulp (beta:=radix) bo radix precision z := by ring
  have huhNormEq :
      Fnormalize (beta:=radix) radix bo precision uh = uh := by
    have h := FcanonicFnormalizeEq (beta:=radix) radix bo precision uh
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨Or.inl huhNormal, rfl, hradix, hprecisionNe, hvNum⟩
  have hzNormEq :
      Fnormalize (beta:=radix) radix bo precision z = z := by
    have h := FcanonicFnormalizeEq (beta:=radix) radix bo precision z
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨Or.inl hzNormal, rfl, hradix, hprecisionNe, hvNum⟩
  let eps : ℝ := (radix : ℝ) ^ (1 - (precision : Int))
  have huhFulpRel :
      Fulp (beta:=radix) bo radix precision uh ≤
        |_root_.F2R (beta:=radix) uh| * eps := by
    have h := FulpLe2 (beta:=radix) radix bo precision uh
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpLe2_check,
      Id.run, ULift.up_down, eps, Int.cast_ofNat] using
      h ⟨huhDef.1, by simpa [huhNormEq] using huhNormal, rfl, hradix,
        hprecisionNe, hvNum⟩
  have hzFulpRel :
      Fulp (beta:=radix) bo radix precision z ≤
        |_root_.F2R (beta:=radix) z| * eps := by
    have h := FulpLe2 (beta:=radix) radix bo precision z
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpLe2_check,
      Id.run, ULift.up_down, eps, Int.cast_ofNat] using
      h ⟨hzDef.1, by simpa [hzNormEq] using hzNormal, rfl, hradix,
        hprecisionNe, hvNum⟩
  have hepsPos : 0 < eps := by
    exact zpow_pos hradixRealPos _
  have hepsNonneg : 0 ≤ eps := le_of_lt hepsPos
  have hepsLeInvCube : eps ≤ (radix : ℝ) ^ (-3 : Int) := by
    have h := Rle_powerRZ (radix : ℝ) (1 - (precision : Int)) (-3)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Rle_powerRZ_check, Id.run,
      ULift.up_down, eps, Int.cast_ofNat] using
      h ⟨hradixRealGe, by omega⟩
  have hpolyLeCube :
      1 + (radix : ℝ) + (radix : ℝ) * (radix : ℝ) ≤
        (radix : ℝ) ^ (3 : Int) := by
    have hradixGeTwo : (2 : ℝ) ≤ (radix : ℝ) := by
      exact_mod_cast (by omega : (2 : Int) ≤ radix)
    norm_num [zpow_ofNat]
    nlinarith
  have hcubeEpsLeOne :
      (radix : ℝ) ^ (3 : Int) * eps ≤ 1 := by
    have hpowNonneg : 0 ≤ (radix : ℝ) ^ (3 : Int) :=
      le_of_lt (zpow_pos hradixRealPos _)
    calc
      (radix : ℝ) ^ (3 : Int) * eps
          ≤ (radix : ℝ) ^ (3 : Int) * (radix : ℝ) ^ (-3 : Int) :=
            mul_le_mul_of_nonneg_left hepsLeInvCube hpowNonneg
      _ = 1 := by
        rw [← zpow_add₀ hradixRealNe]
        norm_num
  have hpolyEps :
      (1 + (radix : ℝ) + (radix : ℝ) * (radix : ℝ)) * eps ≤
        (radix : ℝ) - 1 := by
    have hradixGeTwo : (2 : ℝ) ≤ (radix : ℝ) := by
      exact_mod_cast (by omega : (2 : Int) ≤ radix)
    have h1 :
        (1 + (radix : ℝ) + (radix : ℝ) * (radix : ℝ)) * eps ≤
          (radix : ℝ) ^ (3 : Int) * eps :=
      mul_le_mul_of_nonneg_right hpolyLeCube hepsNonneg
    nlinarith [h1, hcubeEpsLeOne]
  have hcoefPos : 0 < 1 - (1 + (radix : ℝ)) * eps := by
    have hradixGeTwo : (2 : ℝ) ≤ (radix : ℝ) := by
      exact_mod_cast (by omega : (2 : Int) ≤ radix)
    have hquadLt :
        1 + (radix : ℝ) < (radix : ℝ) ^ (2 : Int) := by
      norm_num [zpow_ofNat]
      nlinarith
    have hpow2Nonneg : 0 ≤ (radix : ℝ) ^ (2 : Int) :=
      le_of_lt (zpow_pos hradixRealPos _)
    have hleftLt :
        (1 + (radix : ℝ)) * eps <
          (radix : ℝ) ^ (2 : Int) * eps :=
      mul_lt_mul_of_pos_right hquadLt hepsPos
    have hrightLe :
        (radix : ℝ) ^ (2 : Int) * eps ≤
          (radix : ℝ) ^ (-1 : Int) := by
      calc
        (radix : ℝ) ^ (2 : Int) * eps
            ≤ (radix : ℝ) ^ (2 : Int) * (radix : ℝ) ^ (-3 : Int) :=
              mul_le_mul_of_nonneg_left hepsLeInvCube hpow2Nonneg
        _ = (radix : ℝ) ^ (-1 : Int) := by
          rw [← zpow_add₀ hradixRealNe]
          norm_num
    have hinvLtOne : (radix : ℝ) ^ (-1 : Int) < 1 := by
      rw [zpow_neg]
      norm_num
      exact inv_lt_one_of_one_lt₀ hradixReal
    nlinarith
  have hAbsIneq :
      |_root_.F2R (beta:=radix) uh| ≤
        |_root_.F2R (beta:=radix) z| +
          (1 + (radix : ℝ)) *
            (|_root_.F2R (beta:=radix) uh| * eps) +
          |_root_.F2R (beta:=radix) z| * eps := by
    have htri :
        |_root_.F2R (beta:=radix) uh| - |_root_.F2R (beta:=radix) z| ≤
          |_root_.F2R (beta:=radix) uh - _root_.F2R (beta:=radix) z| := by
      exact abs_sub_abs_le_abs_sub (_root_.F2R (beta:=radix) uh)
        (_root_.F2R (beta:=radix) z)
    have hdistRel :
        |_root_.F2R (beta:=radix) uh - _root_.F2R (beta:=radix) z| ≤
          (1 + (radix : ℝ)) *
              (|_root_.F2R (beta:=radix) uh| * eps) +
            |_root_.F2R (beta:=radix) z| * eps := by
      have hcoefNonneg : 0 ≤ 1 + (radix : ℝ) := by nlinarith
      have huhScaled :
          (1 + (radix : ℝ)) * Fulp (beta:=radix) bo radix precision uh ≤
            (1 + (radix : ℝ)) *
              (|_root_.F2R (beta:=radix) uh| * eps) :=
        mul_le_mul_of_nonneg_left huhFulpRel hcoefNonneg
      exact le_trans hdist (add_le_add huhScaled hzFulpRel)
    nlinarith [htri, hdistRel]
  have hAbsUhLe :
      |_root_.F2R (beta:=radix) uh| ≤
        (radix : ℝ) * |_root_.F2R (beta:=radix) z| := by
    have hstep :
        (1 - (1 + (radix : ℝ)) * eps) *
            |_root_.F2R (beta:=radix) uh| ≤
          (1 + eps) * |_root_.F2R (beta:=radix) z| := by
      nlinarith [hAbsIneq]
    have hcoeffTarget :
        1 + eps ≤
          (radix : ℝ) * (1 - (1 + (radix : ℝ)) * eps) := by
      nlinarith [hpolyEps]
    have hstep' :
        (1 - (1 + (radix : ℝ)) * eps) *
            |_root_.F2R (beta:=radix) uh| ≤
          (1 - (1 + (radix : ℝ)) * eps) *
            ((radix : ℝ) * |_root_.F2R (beta:=radix) z|) := by
      calc
        (1 - (1 + (radix : ℝ)) * eps) *
            |_root_.F2R (beta:=radix) uh|
            ≤ (1 + eps) * |_root_.F2R (beta:=radix) z| := hstep
        _ ≤ (radix : ℝ) * (1 - (1 + (radix : ℝ)) * eps) *
              |_root_.F2R (beta:=radix) z| :=
            mul_le_mul_of_nonneg_right hcoeffTarget (abs_nonneg _)
        _ = (1 - (1 + (radix : ℝ)) * eps) *
              ((radix : ℝ) * |_root_.F2R (beta:=radix) z|) := by ring
    exact le_of_mul_le_mul_left hstep' hcoefPos
  let zShift : FloatSpec.Core.Defs.FlocqFloat radix := ⟨z.Fnum, z.Fexp + 1⟩
  have hzShiftNormal : Fnormal (beta:=radix) radix bo zShift := by
    rcases hzNormal with ⟨hzBound, hzNum⟩
    exact ⟨⟨hzBound.1, by
      dsimp [zShift]
      exact le_trans hzBound.2 (by omega)⟩, by
      dsimp [zShift]
      simpa using hzNum⟩
  have hzShiftAbs :
      |_root_.F2R (beta:=radix) zShift| =
        (radix : ℝ) * |_root_.F2R (beta:=radix) z| := by
    dsimp [zShift, _root_.F2R, FloatSpec.Core.Defs.F2R]
    rw [zpow_add₀ hradixRealNe]
    simp [abs_mul, abs_of_pos hradixRealPos, mul_assoc, mul_left_comm,
      mul_comm]
  have hExpLeShift : uh.Fexp ≤ zShift.Fexp := by
    have h := Fcanonic_Rle_Zle (beta:=radix) radix bo uh zShift hradix rfl
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fcanonic_Rle_Zle_check,
      ULift.down_up, Fcanonic', zShift, Int.cast_ofNat] using
      h ⟨Or.inl huhNormal, Or.inl hzShiftNormal, by
        simpa [hzShiftAbs] using hAbsUhLe⟩
  simpa [zShift] using hExpLeShift

noncomputable def LeExp3_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a x b ph uh ul z : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq `ErrFmaApprox` local lemma `LeExp3`.

The simultaneous one-exponent gaps `ph.Fexp = uh.Fexp + 1` and
`uh.Fexp = z.Fexp + 1` contradict the inexact `ul` branch.  The proof keeps the
upstream payload used at this point: `LeExp2`, `RleRoundedAbs`, the closest
rounding absolute monotonicity argument, and the bounded plus-error exponent
used to force `b.Fexp <= z.Fexp`. -/
theorem LeExp3 {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a x b ph uh ul z : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧
        bo.vNum = Zpower_nat radix precision ∧ 4 ≤ precision ∧
        Fbounded (beta:=beta) bo b ∧
        Fnormal (beta:=beta) radix bo ph ∧
        Fnormal (beta:=beta) radix bo z ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
            _root_.F2R (beta:=beta) b) z ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) ph ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) ph + _root_.F2R (beta:=beta) b) uh ∧
        _root_.F2R (beta:=beta) ul =
          _root_.F2R (beta:=beta) ph + _root_.F2R (beta:=beta) b -
            _root_.F2R (beta:=beta) uh ∧
        ¬ _root_.F2R (beta:=beta) ul = 0 ∧
        ph.Fexp = uh.Fexp + 1 ∧ uh.Fexp = z.Fexp + 1⌝⦄
    (pure (LeExp3_check (beta:=beta) bo radix precision a x b ph uh ul z) :
      Id Unit)
    ⦃⇓_ => ⌜False⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hradix, hvNum, hprecision, hbBound, hphNormal,
      hzNormal, hzDef, hphDef, huhDef, hulDef, hCase2,
      hphExp, huhExp⟩
  simp only [wp, PostCond.noThrow, pure, LeExp3_check, Id.run, ULift.up_down]
  subst beta
  have hBoundExp : ∀ r : ℝ,
      -bo.dExp ≤ (boundR (beta:=radix) radix r).Fexp := by
    intro r
    exact le_trans (neg_nonpos.mpr bo.dExp_nonneg) (by simp [boundR, boundNat])
  have hprecisionNe : precision ≠ 0 := by omega
  have hvNumGt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecisionNe
  have hradixReal : (1 : ℝ) < (radix : ℝ) := by exact_mod_cast hradix
  have hradixRealGe : (1 : ℝ) ≤ (radix : ℝ) := le_of_lt hradixReal
  have hradixRealPos : (0 : ℝ) < (radix : ℝ) := lt_trans zero_lt_one hradixReal
  have hradixRealNe : (radix : ℝ) ≠ 0 := ne_of_gt hradixRealPos
  let P : ℝ → FloatSpec.Core.Defs.FlocqFloat radix → Prop :=
    Closest (beta:=radix) bo (radix : ℝ)
  have hMinTotal : TotalP (isMin (beta:=radix) bo radix) := by
    intro r
    have h := MinEx_from_finite_box_payload (beta:=radix) bo radix r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hvNumGt, hBoundExp r⟩
  have hMaxTotal : TotalP (isMax (beta:=radix) bo radix) := by
    intro r
    have h := MaxEx_from_finite_box_payload (beta:=radix) bo radix r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MaxEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hvNumGt, hBoundExp r⟩
  have hTotal : TotalP P := by
    intro r
    have h := ClosestTotal_from_extrema_payload (beta:=radix) bo radix (radix : ℝ) r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestTotal_check, Id.run,
      ULift.up_down, P, Int.cast_ofNat] using h ⟨hMinTotal, hMaxTotal⟩
  have hCompat : CompatiblePGeneric P := by
    have h := ClosestCompatible_from_structural_payload (beta:=radix) bo (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestCompatible_check,
      PredTrans.pure, PredTrans.apply, Id.run, ULift.down, P, Int.cast_ofNat] using h trivial
  have hMinMax : MinOrMaxP_float (beta:=radix) bo radix P := by
    have h := ClosestMinOrMax_from_split_radix_payload (beta:=radix) bo radix (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMinOrMax_check, MinOrMaxP_float,
      Id.run, ULift.up_down, P, Int.cast_ofNat] using h trivial
  have hMono : MonotoneP_float P := by
    have h := ClosestMonotone_from_split_radix_payload (beta:=radix) bo (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMonotone_check,
      Id.run, ULift.up_down, P, Int.cast_ofNat] using h trivial
  have hRoundedFloat : RoundedModeP_float (beta:=radix) bo radix P :=
    ⟨hTotal, hCompat, hMinMax, hMono⟩
  have hbExpLeZ : b.Fexp ≤ z.Fexp := by
    by_contra hbnot
    have hb_ge_uh : uh.Fexp ≤ b.Fexp := by
      have hb_gt : z.Fexp < b.Fexp := by omega
      omega
    rcases errorBoundedPlus_from_format_payload (beta:=radix) bo radix precision
        rfl hradix hprecisionNe hvNumGt hvNum ph b uh hphNormal.1 hbBound huhDef with
      ⟨err, herrVal, herrBound, herrExp⟩
    have herrNonzero : _root_.F2R (beta:=radix) err ≠ 0 := by
      intro hzerr
      apply hCase2
      rw [hulDef, ← herrVal, hzerr]
    have herrExpStrict : err.Fexp < uh.Fexp := by
      have h := ClosestErrorExpStrict (beta:=radix) bo radix precision uh err
        (_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b)
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestErrorExpStrict_check,
        Id.run, ULift.up_down, Int.cast_ofNat] using
        h ⟨rfl, hradix, by omega, hvNum, huhDef.1, herrBound, huhDef,
          herrVal, herrNonzero⟩
    have herrExpGe : uh.Fexp ≤ err.Fexp := by
      rw [herrExp]
      exact le_min (by omega) hb_ge_uh
    exact not_lt_of_ge herrExpGe herrExpStrict
  let lower : FloatSpec.Core.Defs.FlocqFloat radix :=
    ⟨1, (precision : Int) + z.Fexp⟩
  have hlowerBound : Fbounded (beta:=radix) bo lower := by
    constructor
    · dsimp [lower]
      simpa using hvNumGt
    · dsimp [lower]
      exact le_trans hzNormal.1.2 (by omega)
  have hlowerVal :
      _root_.F2R (beta:=radix) lower =
        (radix : ℝ) ^ ((precision : Int) + z.Fexp) := by
    simp [lower, _root_.F2R, FloatSpec.Core.Defs.F2R]
  have hzAbsStrict :
      |_root_.F2R (beta:=radix) z| <
        (radix : ℝ) ^ ((precision : Int) + z.Fexp) := by
    have hzMantLt : ((|z.Fnum| : Int) : ℝ) < (bo.vNum : ℝ) := by
      exact_mod_cast hzNormal.1.1
    have hpowPos : 0 < (radix : ℝ) ^ z.Fexp := zpow_pos hradixRealPos _
    have hvNumReal :
        (bo.vNum : ℝ) = (radix : ℝ) ^ (precision : Int) := by
      rw [hvNum]
      simp [Zpower_nat, zpow_natCast, Int.cast_pow]
    calc
      |_root_.F2R (beta:=radix) z|
          = ((|z.Fnum| : Int) : ℝ) * (radix : ℝ) ^ z.Fexp := by
            simp [_root_.F2R, FloatSpec.Core.Defs.F2R, abs_mul,
              abs_of_pos hpowPos, Int.cast_abs]
      _ < (bo.vNum : ℝ) * (radix : ℝ) ^ z.Fexp :=
            mul_lt_mul_of_pos_right hzMantLt hpowPos
      _ = (radix : ℝ) ^ ((precision : Int) + z.Fexp) := by
            rw [hvNumReal, zpow_add₀ hradixRealNe]
  have hLowerAx :
      ((radix : ℝ) ^ (precision - 1) + - (1 / (2 * (radix : ℝ)))) *
        (radix : ℝ) ^ ph.Fexp ≤
          |_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x| := by
    have hphExpMin : -bo.dExp < ph.Fexp := by
      have hzExpMin : -bo.dExp ≤ z.Fexp := hzNormal.1.2
      omega
    have h := RleRoundedAbs bo radix precision ph
      (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RleRoundedAbs_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨hradix, hvNum, hprecision, hphDef, hphNormal, hphExpMin⟩
  have hbAbsUpper :
      |_root_.F2R (beta:=radix) b| ≤
        ((bo.vNum - 1 : Int) : ℝ) * (radix : ℝ) ^ z.Fexp := by
    have h := maxMax1_from_expanded_bound_payload (beta:=radix) bo b z.Fexp
    have hbUpper :
        |_root_.F2R (beta:=radix) b| ≤
          _root_.F2R (beta:=radix)
            (⟨bo.vNum - 1, z.Fexp⟩ :
              FloatSpec.Core.Defs.FlocqFloat radix) := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, maxMax1_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using h ⟨hbBound, hbExpLeZ, hbBound, hradix⟩
    simpa [_root_.F2R, FloatSpec.Core.Defs.F2R] using hbUpper
  have hInputLower :
      _root_.F2R (beta:=radix) lower ≤
        |_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
          _root_.F2R (beta:=radix) b| := by
    let A : ℝ := (radix : ℝ) ^ (precision : Int)
    let B : ℝ := (radix : ℝ) ^ z.Fexp
    have hBpos : 0 < B := by
      dsimp [B]
      exact zpow_pos hradixRealPos _
    have hAnonneg : 0 ≤ A := by
      dsimp [A]
      exact le_of_lt (zpow_pos hradixRealPos _)
    have hAgeOne : 1 ≤ A := by
      dsimp [A]
      simpa using
        (zpow_right_mono₀ hradixRealGe
          (by exact_mod_cast Nat.zero_le precision : (0 : Int) ≤ (precision : Int)))
    have hArith : A ≤ (radix : ℝ) * A - (radix : ℝ) / 2 - A + 1 := by
      have hbetaGeTwo : (2 : ℝ) ≤ (radix : ℝ) := by
        exact_mod_cast (by omega : (2 : Int) ≤ radix)
      have hmul :
          (radix : ℝ) - 2 ≤ ((radix : ℝ) - 2) * A := by
        have hnonneg : 0 ≤ (radix : ℝ) - 2 := by nlinarith
        nlinarith [mul_le_mul_of_nonneg_left hAgeOne hnonneg]
      nlinarith
    have hScaled :
        A * B ≤
          (((radix : ℝ) * A - (radix : ℝ) / 2) * B) -
            ((A - 1) * B) := by
      have h := mul_le_mul_of_nonneg_right hArith (le_of_lt hBpos)
      nlinarith
    have hphPow :
        ph.Fexp = z.Fexp + 2 := by omega
    have hLowerAx' :
        (((radix : ℝ) * A - (radix : ℝ) / 2) * B) ≤
          |_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x| := by
      have hpowPredMul :
          (radix : ℝ) ^ (precision - 1) * (radix : ℝ) ^ (2 : Int) =
            (radix : ℝ) * A := by
        have hpredNat : (precision - 1) + 2 = precision + 1 := by omega
        calc
          (radix : ℝ) ^ (precision - 1) * (radix : ℝ) ^ (2 : Int)
              = (radix : ℝ) ^ (precision - 1) * (radix : ℝ) ^ 2 := by
                rw [zpow_ofNat]
          _ = (radix : ℝ) ^ ((precision - 1) + 2) := by
                rw [← pow_add]
          _ = (radix : ℝ) ^ (precision + 1) := by rw [hpredNat]
          _ = (radix : ℝ) ^ precision * (radix : ℝ) := by
                rw [pow_succ]
          _ = (radix : ℝ) * A := by
                dsimp [A]
                rw [zpow_natCast]
                ring
      have hsmall :
          (1 / (2 * (radix : ℝ))) * (radix : ℝ) ^ (2 : Int) =
            (radix : ℝ) / 2 := by
        rw [zpow_ofNat]
        field_simp [hradixRealNe]
      have hcoef :
          ((radix : ℝ) ^ (precision - 1) + - (1 / (2 * (radix : ℝ)))) *
              (radix : ℝ) ^ ph.Fexp =
            (((radix : ℝ) * A - (radix : ℝ) / 2) * B) := by
        rw [hphPow]
        have hpowSplit :
            (radix : ℝ) ^ (z.Fexp + 2) =
              (radix : ℝ) ^ (2 : Int) * B := by
          dsimp [B]
          rw [show z.Fexp + 2 = 2 + z.Fexp by ring]
          exact zpow_add₀ hradixRealNe 2 z.Fexp
        rw [hpowSplit]
        rw [← sub_eq_add_neg]
        calc
          ((radix : ℝ) ^ (precision - 1) - 1 / (2 * (radix : ℝ))) *
              ((radix : ℝ) ^ (2 : Int) * B)
              = (((radix : ℝ) ^ (precision - 1) * (radix : ℝ) ^ (2 : Int)) -
                  (1 / (2 * (radix : ℝ))) * (radix : ℝ) ^ (2 : Int)) * B := by
                ring
          _ = (((radix : ℝ) * A - (radix : ℝ) / 2) * B) := by
                rw [hpowPredMul, hsmall]
      rw [hcoef] at hLowerAx
      exact hLowerAx
    have hbAbsUpper' :
        |_root_.F2R (beta:=radix) b| ≤ (A - 1) * B := by
      have hvNumReal' :
          ((bo.vNum - 1 : Int) : ℝ) = A - 1 := by
        have hv : (bo.vNum : ℝ) = (radix : ℝ) ^ (precision : Int) := by
          rw [hvNum]
          simp [Zpower_nat, zpow_natCast, Int.cast_pow]
        rw [Int.cast_sub, hv]
        dsimp [A]
        norm_num
      simpa [B, hvNumReal'] using hbAbsUpper
    have htri :
        |_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x| -
          |_root_.F2R (beta:=radix) b| ≤
        |_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
          _root_.F2R (beta:=radix) b| := by
      have h := abs_sub_abs_le_abs_sub
        (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x)
        (-_root_.F2R (beta:=radix) b)
      rw [sub_neg_eq_add] at h
      simpa [abs_neg] using h
    have hAB :
        _root_.F2R (beta:=radix) lower = A * B := by
      rw [hlowerVal]
      dsimp [A, B]
      rw [zpow_add₀ hradixRealNe]
    nlinarith [hScaled, hLowerAx', hbAbsUpper', htri, hAB]
  have hLowerLeZ :
      _root_.F2R (beta:=radix) lower ≤
        |_root_.F2R (beta:=radix) z| := by
    have h := RoundAbsMonotonel_from_rounded_payload (beta:=radix) bo radix P
      (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
        _root_.F2R (beta:=radix) b) z lower
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RoundAbsMonotonel_check,
      Id.run, ULift.up_down, P, Int.cast_ofNat] using
      h ⟨hRoundedFloat, hlowerBound, hzDef, hInputLower⟩
  rw [hlowerVal] at hLowerLeZ
  exact (not_le_of_gt hzAbsStrict) hLowerLeZ

noncomputable def LeExp_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a x b ph uh ul z : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq `ErrFmaApprox` local lemma `LeExp`.

Combines `LeExp1`, `LeExp2`, and the forbidden equality/equality corner from
`LeExp3` to bound the two ulp-sized terms by twice the successor exponent of
the rounded FMA result. -/
theorem LeExp {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a x b ph uh ul z : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧
        bo.vNum = Zpower_nat radix precision ∧ 4 ≤ precision ∧
        Fbounded (beta:=beta) bo b ∧
        Fnormal (beta:=beta) radix bo ph ∧
        Fnormal (beta:=beta) radix bo uh ∧
        Fnormal (beta:=beta) radix bo z ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
            _root_.F2R (beta:=beta) b) z ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) ph ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) ph + _root_.F2R (beta:=beta) b) uh ∧
        _root_.F2R (beta:=beta) ul =
          _root_.F2R (beta:=beta) ph + _root_.F2R (beta:=beta) b -
            _root_.F2R (beta:=beta) uh ∧
        ¬ _root_.F2R (beta:=beta) ul = 0⌝⦄
    (pure (LeExp_check (beta:=beta) bo radix precision a x b ph uh ul z) :
      Id Unit)
    ⦃⇓_ => ⌜(radix : ℝ) ^ ph.Fexp + (radix : ℝ) ^ uh.Fexp ≤
      2 * (radix : ℝ) ^ (z.Fexp + 1)⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hradix, hvNum, hprecision, hbBound, hphNormal,
      huhNormal, hzNormal, hzDef, hphDef, huhDef, hulDef, hCase2⟩
  simp only [wp, PostCond.noThrow, pure, LeExp_check, Id.run, ULift.up_down]
  subst beta
  have hprecisionNe : precision ≠ 0 := by omega
  have hvNumGt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecisionNe
  have hLeExp1 : ph.Fexp ≤ uh.Fexp + 1 := by
    have h := LeExp1_from_format_payload (beta:=radix) bo radix precision b ph uh ul
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, LeExp1_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, hradix, hprecisionNe, hvNumGt, hvNum, hbBound,
        hphNormal, huhDef, hulDef, hCase2⟩
  have hLeExp2 : uh.Fexp ≤ z.Fexp + 1 := by
    have h := LeExp2 (beta:=radix) bo radix precision a x b ph uh ul z
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, LeExp2_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, hradix, hvNum, hprecision, hbBound, hphNormal,
        huhNormal, hzNormal, hzDef, hphDef, huhDef, hulDef, hCase2⟩
  have hphLeZSucc : ph.Fexp ≤ z.Fexp + 1 := by
    by_contra hnot
    have hph_gt : z.Fexp + 1 < ph.Fexp := not_le.mp hnot
    have hph_eq : ph.Fexp = uh.Fexp + 1 := by omega
    have huh_eq : uh.Fexp = z.Fexp + 1 := by omega
    have hfalse : False := by
      have h := LeExp3 (beta:=radix) bo radix precision a x b ph uh ul z
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, LeExp3_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using
        h ⟨rfl, hradix, hvNum, hprecision, hbBound, hphNormal,
          hzNormal, hzDef, hphDef, huhDef, hulDef, hCase2,
          hph_eq, huh_eq⟩
    exact hfalse.elim
  have hradixReal : (1 : ℝ) < (radix : ℝ) := by exact_mod_cast hradix
  have hradixRealGe : (1 : ℝ) ≤ (radix : ℝ) := le_of_lt hradixReal
  have hphPow :
      (radix : ℝ) ^ ph.Fexp ≤ (radix : ℝ) ^ (z.Fexp + 1) := by
    have h := Rle_powerRZ (radix : ℝ) ph.Fexp (z.Fexp + 1)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Rle_powerRZ_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨hradixRealGe, hphLeZSucc⟩
  have huhPow :
      (radix : ℝ) ^ uh.Fexp ≤ (radix : ℝ) ^ (z.Fexp + 1) := by
    have h := Rle_powerRZ (radix : ℝ) uh.Fexp (z.Fexp + 1)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Rle_powerRZ_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨hradixRealGe, hLeExp2⟩
  calc
    (radix : ℝ) ^ ph.Fexp + (radix : ℝ) ^ uh.Fexp
        ≤ (radix : ℝ) ^ (z.Fexp + 1) + (radix : ℝ) ^ (z.Fexp + 1) :=
          add_le_add hphPow huhPow
    _ = 2 * (radix : ℝ) ^ (z.Fexp + 1) := by ring

noncomputable def vLe_aux_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a x b ph pl uh ul z : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq `ErrFmaApprox` local lemma `vLe_aux`.

The inexact low-term sum is bounded by one radix successor of the rounded FMA
exponent.  This is the direct upstream triangle-inequality proof: two
`ClosestUlp` bounds are converted with `CanonicFulp`, then combined through
`LeExp`. -/
theorem vLe_aux {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a x b ph pl uh ul z : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧
        bo.vNum = Zpower_nat radix precision ∧ 4 ≤ precision ∧
        Fbounded (beta:=beta) bo b ∧
        Fnormal (beta:=beta) radix bo ph ∧
        Fnormal (beta:=beta) radix bo uh ∧
        Fnormal (beta:=beta) radix bo z ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
            _root_.F2R (beta:=beta) b) z ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) ph ∧
        _root_.F2R (beta:=beta) pl =
          _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
            _root_.F2R (beta:=beta) ph ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) ph + _root_.F2R (beta:=beta) b) uh ∧
        _root_.F2R (beta:=beta) ul =
          _root_.F2R (beta:=beta) ph + _root_.F2R (beta:=beta) b -
            _root_.F2R (beta:=beta) uh ∧
        ¬ _root_.F2R (beta:=beta) ul = 0⌝⦄
    (pure (vLe_aux_check (beta:=beta) bo radix precision a x b ph pl uh ul z) :
      Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) pl + _root_.F2R (beta:=beta) ul| ≤
      (radix : ℝ) ^ z.Fexp * radix⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hradix, hvNum, hprecision, hbBound, hphNormal,
      huhNormal, hzNormal, hzDef, hphDef, hplDef, huhDef, hulDef, hCase2⟩
  simp only [wp, PostCond.noThrow, pure, vLe_aux_check, Id.run, ULift.up_down]
  subst beta
  have hprecisionNe : precision ≠ 0 := by omega
  have hradixRealPos : (0 : ℝ) < (radix : ℝ) := by
    exact_mod_cast (by omega : (0 : Int) < radix)
  have hradixRealNe : (radix : ℝ) ≠ 0 := ne_of_gt hradixRealPos
  have hplUlp :
      2 *
          |_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x -
            _root_.F2R (beta:=radix) ph| ≤
        Fulp (beta:=radix) bo radix precision ph := by
    have h := ClosestUlp (beta:=radix) bo radix precision
      (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x) ph
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨hphDef, rfl, hradix, hprecisionNe, hvNum⟩
  have hphFulp :
      Fulp (beta:=radix) bo radix precision ph = (radix : ℝ) ^ ph.Fexp := by
    have h := CanonicFulp (beta:=radix) radix bo precision ph
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using
      h ⟨Or.inl hphNormal, rfl, hradix, hprecisionNe, hvNum⟩
  have hplBound :
      2 * |_root_.F2R (beta:=radix) pl| ≤ (radix : ℝ) ^ ph.Fexp := by
    calc
      2 * |_root_.F2R (beta:=radix) pl|
          = 2 *
              |_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x -
                _root_.F2R (beta:=radix) ph| := by rw [hplDef]
      _ ≤ Fulp (beta:=radix) bo radix precision ph := hplUlp
      _ = (radix : ℝ) ^ ph.Fexp := hphFulp
  have hulUlp :
      2 *
          |_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b -
            _root_.F2R (beta:=radix) uh| ≤
        Fulp (beta:=radix) bo radix precision uh := by
    have h := ClosestUlp (beta:=radix) bo radix precision
      (_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b) uh
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨huhDef, rfl, hradix, hprecisionNe, hvNum⟩
  have huhFulp :
      Fulp (beta:=radix) bo radix precision uh = (radix : ℝ) ^ uh.Fexp := by
    have h := CanonicFulp (beta:=radix) radix bo precision uh
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using
      h ⟨Or.inl huhNormal, rfl, hradix, hprecisionNe, hvNum⟩
  have hulBound :
      2 * |_root_.F2R (beta:=radix) ul| ≤ (radix : ℝ) ^ uh.Fexp := by
    calc
      2 * |_root_.F2R (beta:=radix) ul|
          = 2 *
              |_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b -
                _root_.F2R (beta:=radix) uh| := by rw [hulDef]
      _ ≤ Fulp (beta:=radix) bo radix precision uh := hulUlp
      _ = (radix : ℝ) ^ uh.Fexp := huhFulp
  have hLeExp :
      (radix : ℝ) ^ ph.Fexp + (radix : ℝ) ^ uh.Fexp ≤
        2 * (radix : ℝ) ^ (z.Fexp + 1) := by
    have h := LeExp (beta:=radix) bo radix precision a x b ph uh ul z
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, LeExp_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, hradix, hvNum, hprecision, hbBound, hphNormal,
        huhNormal, hzNormal, hzDef, hphDef, huhDef, hulDef, hCase2⟩
  have htri :
      |_root_.F2R (beta:=radix) pl + _root_.F2R (beta:=radix) ul| ≤
        |_root_.F2R (beta:=radix) pl| + |_root_.F2R (beta:=radix) ul| :=
    abs_add_le _ _
  have hhalf :
      |_root_.F2R (beta:=radix) pl + _root_.F2R (beta:=radix) ul| ≤
        (radix : ℝ) ^ (z.Fexp + 1) := by
    nlinarith [htri, hplBound, hulBound, hLeExp]
  calc
    |_root_.F2R (beta:=radix) pl + _root_.F2R (beta:=radix) ul|
        ≤ (radix : ℝ) ^ (z.Fexp + 1) := hhalf
    _ = (radix : ℝ) ^ z.Fexp * radix := by
          rw [zpow_add₀ hradixRealNe]
          norm_num

/-- Coq `ErrFmaApprox` local lemma `vLe`.

The rounded low-term sum inherits the `vLe_aux` magnitude bound by monotonicity
of absolute values under closest rounding, using the bounded comparator float
`Float radix (Fexp z)`. -/
theorem vLe {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a x b ph pl uh ul z v : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧
        bo.vNum = Zpower_nat radix precision ∧ 4 ≤ precision ∧
        Fbounded (beta:=beta) bo b ∧
        Fnormal (beta:=beta) radix bo ph ∧
        Fnormal (beta:=beta) radix bo uh ∧
        Fnormal (beta:=beta) radix bo z ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
            _root_.F2R (beta:=beta) b) z ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) ph ∧
        _root_.F2R (beta:=beta) pl =
          _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
            _root_.F2R (beta:=beta) ph ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) ph + _root_.F2R (beta:=beta) b) uh ∧
        _root_.F2R (beta:=beta) ul =
          _root_.F2R (beta:=beta) ph + _root_.F2R (beta:=beta) b -
            _root_.F2R (beta:=beta) uh ∧
        ¬ _root_.F2R (beta:=beta) ul = 0 ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) pl + _root_.F2R (beta:=beta) ul) v⌝⦄
    (pure () : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) v| ≤
      (radix : ℝ) ^ z.Fexp * radix⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hradix, hvNum, hprecision, hbBound, hphNormal,
      huhNormal, hzNormal, hzDef, hphDef, hplDef, huhDef, hulDef, hulNonzero,
      hvDef⟩
  simp only [wp, PostCond.noThrow, pure, Id.run, ULift.up_down]
  subst beta
  have hBoundExp : ∀ r : ℝ,
      -bo.dExp ≤ (boundR (beta:=radix) radix r).Fexp := by
    intro r
    exact le_trans (neg_nonpos.mpr bo.dExp_nonneg) (by simp [boundR, boundNat])
  let P : ℝ → FloatSpec.Core.Defs.FlocqFloat radix → Prop :=
    Closest (beta:=radix) bo (radix : ℝ)
  let upper : FloatSpec.Core.Defs.FlocqFloat radix := ⟨radix, z.Fexp⟩
  have hprecisionNe : precision ≠ 0 := by omega
  have hvNumGt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecisionNe
  have hMinTotal : TotalP (isMin (beta:=radix) bo radix) := by
    intro r
    have h := MinEx_from_finite_box_payload (beta:=radix) bo radix r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hvNumGt, hBoundExp r⟩
  have hMaxTotal : TotalP (isMax (beta:=radix) bo radix) := by
    intro r
    have h := MaxEx_from_finite_box_payload (beta:=radix) bo radix r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MaxEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hvNumGt, hBoundExp r⟩
  have hTotal : TotalP P := by
    intro r
    have h := ClosestTotal_from_extrema_payload (beta:=radix) bo radix (radix : ℝ) r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestTotal_check, Id.run,
      ULift.up_down, P, Int.cast_ofNat] using h ⟨hMinTotal, hMaxTotal⟩
  have hCompat : CompatiblePGeneric P := by
    have h := ClosestCompatible_from_structural_payload (beta:=radix) bo (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestCompatible_check,
      PredTrans.pure, PredTrans.apply, Id.run, ULift.down, P, Int.cast_ofNat] using h trivial
  have hMinMax : MinOrMaxP_float (beta:=radix) bo radix P := by
    have h := ClosestMinOrMax_from_split_radix_payload (beta:=radix) bo radix (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMinOrMax_check, MinOrMaxP_float,
      Id.run, ULift.up_down, P, Int.cast_ofNat] using h trivial
  have hMono : MonotoneP_float P := by
    have h := ClosestMonotone_from_split_radix_payload (beta:=radix) bo (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMonotone_check,
      Id.run, ULift.up_down, P, Int.cast_ofNat] using h trivial
  have hRoundedFloat : RoundedModeP_float (beta:=radix) bo radix P :=
    ⟨hTotal, hCompat, hMinMax, hMono⟩
  have hUpperBound : Fbounded (beta:=radix) bo upper := by
    constructor
    · have hradixLtPred : radix < pPred bo.vNum := by
        have h := pPredMoreThanRadix bo radix precision
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, pPredMoreThanRadix_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h ⟨hradix, by omega, hvNum⟩
      have hradixLtVNum : radix < bo.vNum := by
        unfold pPred at hradixLtPred
        omega
      have hradixNonneg : 0 ≤ radix := by omega
      simpa [upper, abs_of_nonneg hradixNonneg] using hradixLtVNum
    · simpa [upper] using hzNormal.1.2
  have hUpperVal :
      _root_.F2R (beta:=radix) upper = (radix : ℝ) ^ z.Fexp * radix := by
    simp [upper, _root_.F2R, FloatSpec.Core.Defs.F2R]
    ring
  have hAux :
      |_root_.F2R (beta:=radix) pl + _root_.F2R (beta:=radix) ul| ≤
        (radix : ℝ) ^ z.Fexp * radix := by
    have h := vLe_aux (beta:=radix) bo radix precision a x b ph pl uh ul z
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, vLe_aux_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, hradix, hvNum, hprecision, hbBound, hphNormal,
        huhNormal, hzNormal, hzDef, hphDef, hplDef, huhDef, hulDef,
        hulNonzero⟩
  have hAbsLeUpper :
      |_root_.F2R (beta:=radix) pl + _root_.F2R (beta:=radix) ul| ≤
        _root_.F2R (beta:=radix) upper := by
    simpa [hUpperVal] using hAux
  have hvAbsLeUpper :
      |_root_.F2R (beta:=radix) v| ≤ _root_.F2R (beta:=radix) upper := by
    have h := RoundAbsMonotoner_from_rounded_payload (beta:=radix) bo radix P
      (_root_.F2R (beta:=radix) pl + _root_.F2R (beta:=radix) ul) v upper
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RoundAbsMonotoner_check, Id.run,
      ULift.up_down, P, Int.cast_ofNat] using h ⟨hRoundedFloat, hUpperBound, hvDef,
        hAbsLeUpper⟩
  simpa [hUpperVal] using hvAbsLeUpper

noncomputable def tLe_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a x b ph pl uh ul z t : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq `ErrFmaApprox` local lemma `tLe`.

The rounded high correction is bounded by the FMA exponent scaled by
`radix + 1`.  The proof first recovers the exact real value of `t` from
`tBounded` and closest-rounding projector equality, then follows the upstream
triangle split: the `z` error is bounded by `RoundedModeUlp_from_rounded_payload`, while the `ph`
and `uh` errors are bounded by `ClosestUlp` and combined through `LeExp`. -/
theorem tLe {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a x b ph pl uh ul z t : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧
        bo.vNum = Zpower_nat radix precision ∧ 4 ≤ precision ∧
        Fbounded (beta:=beta) bo b ∧
        Fbounded (beta:=beta) bo a ∧
        Fbounded (beta:=beta) bo x ∧
        Fcanonic (beta:=beta) radix bo b ∧
        Fnormal (beta:=beta) radix bo ph ∧
        Fnormal (beta:=beta) radix bo uh ∧
        Fnormal (beta:=beta) radix bo z ∧
        -bo.dExp ≤ a.Fexp + x.Fexp ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
            _root_.F2R (beta:=beta) b) z ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) ph ∧
        _root_.F2R (beta:=beta) pl =
          _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
            _root_.F2R (beta:=beta) ph ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) ph + _root_.F2R (beta:=beta) b) uh ∧
        _root_.F2R (beta:=beta) ul =
          _root_.F2R (beta:=beta) ph + _root_.F2R (beta:=beta) b -
            _root_.F2R (beta:=beta) uh ∧
        ¬ _root_.F2R (beta:=beta) ul = 0 ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) uh - _root_.F2R (beta:=beta) z) t⌝⦄
    (pure (tLe_check (beta:=beta) bo radix precision a x b ph pl uh ul z t) :
      Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) t| ≤
      (radix : ℝ) ^ z.Fexp * ((radix : ℝ) + 1)⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hradix, hvNum, hprecision, hbBound, haBound,
      hxBound, hbCan, hphNormal, huhNormal, hzNormal, hproductExp, hzDef,
      hphDef, hplDef, huhDef, hulDef, hulNonzero, htDef⟩
  simp only [wp, PostCond.noThrow, pure, tLe_check, Id.run, ULift.up_down]
  subst beta
  have hBoundExp : ∀ r : ℝ,
      -bo.dExp ≤ (boundR (beta:=radix) radix r).Fexp := by
    intro r
    exact le_trans (neg_nonpos.mpr bo.dExp_nonneg) (by simp [boundR, boundNat])
  let P : ℝ → FloatSpec.Core.Defs.FlocqFloat radix → Prop :=
    Closest (beta:=radix) bo (radix : ℝ)
  have hprecisionNe : precision ≠ 0 := by omega
  have hvNumGt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecisionNe
  have hradixReal : (1 : ℝ) < (radix : ℝ) := by exact_mod_cast hradix
  have hradixRealPos : (0 : ℝ) < (radix : ℝ) :=
    lt_trans zero_lt_one hradixReal
  have hradixRealNe : (radix : ℝ) ≠ 0 := ne_of_gt hradixRealPos
  have hMinTotal : TotalP (isMin (beta:=radix) bo radix) := by
    intro r
    have h := MinEx_from_finite_box_payload (beta:=radix) bo radix r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hvNumGt, hBoundExp r⟩
  have hMaxTotal : TotalP (isMax (beta:=radix) bo radix) := by
    intro r
    have h := MaxEx_from_finite_box_payload (beta:=radix) bo radix r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MaxEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hvNumGt, hBoundExp r⟩
  have hTotal : TotalP P := by
    intro r
    have h := ClosestTotal_from_extrema_payload (beta:=radix) bo radix (radix : ℝ) r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestTotal_check, Id.run,
      ULift.up_down, P, Int.cast_ofNat] using h ⟨hMinTotal, hMaxTotal⟩
  have hCompat : CompatiblePGeneric P := by
    have h := ClosestCompatible_from_structural_payload (beta:=radix) bo (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestCompatible_check,
      PredTrans.pure, PredTrans.apply, Id.run, ULift.down, P, Int.cast_ofNat] using h trivial
  have hMinMax : MinOrMaxP_float (beta:=radix) bo radix P := by
    have h := ClosestMinOrMax_from_split_radix_payload (beta:=radix) bo radix (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMinOrMax_check, MinOrMaxP_float,
      Id.run, ULift.up_down, P, Int.cast_ofNat] using h trivial
  have hMono : MonotoneP_float P := by
    have h := ClosestMonotone_from_split_radix_payload (beta:=radix) bo (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMonotone_check,
      Id.run, ULift.up_down, P, Int.cast_ofNat] using h trivial
  have hRoundedFloat : RoundedModeP_float (beta:=radix) bo radix P :=
    ⟨hTotal, hCompat, hMinMax, hMono⟩
  have hRoundedFull :
      RoundedModeP_full (beta:=radix) bo P := by
    have h := ClosestRoundedModeP_from_total_payload (beta:=radix) bo (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestRoundedModeP_check,
      Id.run, ULift.up_down, P, Int.cast_ofNat] using h hTotal
  rcases tBounded (beta:=radix) bo radix precision a x b ph pl uh z
      rfl hradix hvNum (by omega : 3 ≤ precision) hbBound
      haBound hxBound hbCan (Or.inl hphNormal) (Or.inl hzNormal)
      (Or.inl huhNormal) hproductExp hzDef hphDef hplDef huhDef with
    ⟨t', ht'Bound, ht'Val⟩
  have htClosestFromWitness : P (_root_.F2R (beta:=radix) t') t := by
    simpa only [P, ht'Val, Int.cast_ofNat] using htDef
  have htProjector :
      _root_.F2R (beta:=radix) t' = _root_.F2R (beta:=radix) t := by
    exact hRoundedFull.2.2.2.2 t' t ht'Bound htClosestFromWitness
  have htVal :
      _root_.F2R (beta:=radix) t =
        _root_.F2R (beta:=radix) uh - _root_.F2R (beta:=radix) z := by
    rw [← htProjector, ht'Val]
  have hzRoundUlp :
      |_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
          _root_.F2R (beta:=radix) b - _root_.F2R (beta:=radix) z| <
        Fulp (beta:=radix) bo radix precision z := by
    have h := RoundedModeUlp_from_rounded_payload (beta:=radix) bo radix precision P
      (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
        _root_.F2R (beta:=radix) b) z
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RoundedModeUlp_check, Id.run,
      ULift.up_down, P, Int.cast_ofNat] using
      h ⟨hRoundedFloat, hzDef, rfl, hradix, hprecisionNe, hvNum⟩
  have hzFulp :
      Fulp (beta:=radix) bo radix precision z = (radix : ℝ) ^ z.Fexp := by
    have h := CanonicFulp (beta:=radix) radix bo precision z
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨Or.inl hzNormal, rfl, hradix, hprecisionNe, hvNum⟩
  have hzErrBound :
      |_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
          _root_.F2R (beta:=radix) b - _root_.F2R (beta:=radix) z| ≤
        (radix : ℝ) ^ z.Fexp := by
    exact le_trans (le_of_lt hzRoundUlp) (le_of_eq hzFulp)
  have hphUlp :
      2 *
          |_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x -
            _root_.F2R (beta:=radix) ph| ≤
        Fulp (beta:=radix) bo radix precision ph := by
    have h := ClosestUlp (beta:=radix) bo radix precision
      (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x) ph
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨hphDef, rfl, hradix, hprecisionNe, hvNum⟩
  have hphFulp :
      Fulp (beta:=radix) bo radix precision ph = (radix : ℝ) ^ ph.Fexp := by
    have h := CanonicFulp (beta:=radix) radix bo precision ph
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨Or.inl hphNormal, rfl, hradix, hprecisionNe, hvNum⟩
  have hphBound :
      2 *
          |_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x -
            _root_.F2R (beta:=radix) ph| ≤
        (radix : ℝ) ^ ph.Fexp := by
    exact le_trans hphUlp (le_of_eq hphFulp)
  have huhUlp :
      2 *
          |_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b -
            _root_.F2R (beta:=radix) uh| ≤
        Fulp (beta:=radix) bo radix precision uh := by
    have h := ClosestUlp (beta:=radix) bo radix precision
      (_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b) uh
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨huhDef, rfl, hradix, hprecisionNe, hvNum⟩
  have huhFulp :
      Fulp (beta:=radix) bo radix precision uh = (radix : ℝ) ^ uh.Fexp := by
    have h := CanonicFulp (beta:=radix) radix bo precision uh
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨Or.inl huhNormal, rfl, hradix, hprecisionNe, hvNum⟩
  have huhBound :
      2 *
          |_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b -
            _root_.F2R (beta:=radix) uh| ≤
        (radix : ℝ) ^ uh.Fexp := by
    exact le_trans huhUlp (le_of_eq huhFulp)
  have hLeExp :
      (radix : ℝ) ^ ph.Fexp + (radix : ℝ) ^ uh.Fexp ≤
        2 * (radix : ℝ) ^ (z.Fexp + 1) := by
    have h := LeExp (beta:=radix) bo radix precision a x b ph uh ul z
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, LeExp_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, hradix, hvNum, hprecision, hbBound, hphNormal,
        huhNormal, hzNormal, hzDef, hphDef, huhDef, hulDef, hulNonzero⟩
  have hMidTri :
      |(-(_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x -
          _root_.F2R (beta:=radix) ph)) +
          -(_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b -
            _root_.F2R (beta:=radix) uh)| ≤
        |_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x -
          _root_.F2R (beta:=radix) ph| +
        |_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b -
          _root_.F2R (beta:=radix) uh| := by
    have htri := abs_add_le
      (-(_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x -
        _root_.F2R (beta:=radix) ph))
      (-(_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b -
        _root_.F2R (beta:=radix) uh))
    calc
      |(-(_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x -
          _root_.F2R (beta:=radix) ph)) +
          -(_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b -
            _root_.F2R (beta:=radix) uh)|
          ≤ |(-(_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x -
              _root_.F2R (beta:=radix) ph))| +
            |(-(_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b -
              _root_.F2R (beta:=radix) uh))| := htri
      _ = |_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x -
            _root_.F2R (beta:=radix) ph| +
          |_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b -
            _root_.F2R (beta:=radix) uh| := by
          simp
          rw [abs_sub_comm (_root_.F2R (beta:=radix) ph)
            (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x)]
          rw [abs_sub_comm (_root_.F2R (beta:=radix) uh)
            (_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b)]
  have hMidBound :
      |(-(_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x -
          _root_.F2R (beta:=radix) ph)) +
          -(_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b -
            _root_.F2R (beta:=radix) uh)| ≤
        (radix : ℝ) ^ (z.Fexp + 1) := by
    nlinarith [hMidTri, hphBound, huhBound, hLeExp]
  have hDecomp :
      _root_.F2R (beta:=radix) uh - _root_.F2R (beta:=radix) z =
        (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
          _root_.F2R (beta:=radix) b - _root_.F2R (beta:=radix) z) +
          ((-(_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x -
            _root_.F2R (beta:=radix) ph)) +
            -(_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b -
              _root_.F2R (beta:=radix) uh)) := by
    ring
  calc
    |_root_.F2R (beta:=radix) t|
        = |_root_.F2R (beta:=radix) uh - _root_.F2R (beta:=radix) z| := by
          rw [htVal]
    _ = |(_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
          _root_.F2R (beta:=radix) b - _root_.F2R (beta:=radix) z) +
          ((-(_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x -
            _root_.F2R (beta:=radix) ph)) +
            -(_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b -
              _root_.F2R (beta:=radix) uh))| := by
          rw [hDecomp]
    _ ≤ |_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
          _root_.F2R (beta:=radix) b - _root_.F2R (beta:=radix) z| +
        |(-(_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x -
          _root_.F2R (beta:=radix) ph)) +
          -(_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b -
            _root_.F2R (beta:=radix) uh)| := abs_add_le _ _
    _ ≤ (radix : ℝ) ^ z.Fexp + (radix : ℝ) ^ (z.Fexp + 1) :=
          add_le_add hzErrBound hMidBound
    _ = (radix : ℝ) ^ z.Fexp * ((radix : ℝ) + 1) := by
          rw [zpow_add₀ hradixRealNe]
          norm_num
          ring

noncomputable def wLe_check {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a x b ph pl uh ul z t v w : FloatSpec.Core.Defs.FlocqFloat beta) : Unit :=
  ()

/-- Coq `ErrFmaApprox` local lemma `wLe`.

The rounded total correction is bounded by the sum of the exact upstream
`tLe` and `vLe` bounds.  The proof follows Flocq: construct the bounded
comparator float `Float (2 * radix + 1) (Fexp z)`, use
`RoundAbsMonotoner_from_rounded_payload` for the closest rounding of `t + v`, and discharge the
input bound with the triangle inequality. -/
theorem wLe {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a x b ph pl uh ul z t v w : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧
        bo.vNum = Zpower_nat radix precision ∧ 4 ≤ precision ∧
        Fbounded (beta:=beta) bo b ∧
        Fbounded (beta:=beta) bo a ∧
        Fbounded (beta:=beta) bo x ∧
        Fcanonic (beta:=beta) radix bo b ∧
        Fnormal (beta:=beta) radix bo ph ∧
        Fnormal (beta:=beta) radix bo uh ∧
        Fnormal (beta:=beta) radix bo z ∧
        -bo.dExp ≤ a.Fexp + x.Fexp ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
            _root_.F2R (beta:=beta) b) z ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) ph ∧
        _root_.F2R (beta:=beta) pl =
          _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
            _root_.F2R (beta:=beta) ph ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) ph + _root_.F2R (beta:=beta) b) uh ∧
        _root_.F2R (beta:=beta) ul =
          _root_.F2R (beta:=beta) ph + _root_.F2R (beta:=beta) b -
            _root_.F2R (beta:=beta) uh ∧
        ¬ _root_.F2R (beta:=beta) ul = 0 ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) uh - _root_.F2R (beta:=beta) z) t ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) pl + _root_.F2R (beta:=beta) ul) v ∧
        Closest (beta:=beta) bo (radix : ℝ)
          (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) v) w⌝⦄
    (pure (wLe_check (beta:=beta) bo radix precision a x b ph pl uh ul z t v w) :
      Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R (beta:=beta) w| ≤
      (radix : ℝ) ^ z.Fexp * (2 * (radix : ℝ) + 1)⌝⦄ := by
  intro h
  rcases h with
    ⟨hbeta, hradix, hvNum, hprecision, hbBound, haBound,
      hxBound, hbCan, hphNormal, huhNormal, hzNormal, hproductExp, hzDef,
      hphDef, hplDef, huhDef, hulDef, hulNonzero, htDef, hvDef, hwDef⟩
  simp only [wp, PostCond.noThrow, pure, wLe_check, Id.run, ULift.up_down]
  subst beta
  have hBoundExp : ∀ r : ℝ,
      -bo.dExp ≤ (boundR (beta:=radix) radix r).Fexp := by
    intro r
    exact le_trans (neg_nonpos.mpr bo.dExp_nonneg) (by simp [boundR, boundNat])
  let P : ℝ → FloatSpec.Core.Defs.FlocqFloat radix → Prop :=
    Closest (beta:=radix) bo (radix : ℝ)
  let upper : FloatSpec.Core.Defs.FlocqFloat radix := ⟨2 * radix + 1, z.Fexp⟩
  have hprecisionNe : precision ≠ 0 := by omega
  have hvNumGt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecisionNe
  have hMinTotal : TotalP (isMin (beta:=radix) bo radix) := by
    intro r
    have h := MinEx_from_finite_box_payload (beta:=radix) bo radix r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hvNumGt, hBoundExp r⟩
  have hMaxTotal : TotalP (isMax (beta:=radix) bo radix) := by
    intro r
    have h := MaxEx_from_finite_box_payload (beta:=radix) bo radix r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MaxEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hvNumGt, hBoundExp r⟩
  have hTotal : TotalP P := by
    intro r
    have h := ClosestTotal_from_extrema_payload (beta:=radix) bo radix (radix : ℝ) r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestTotal_check, Id.run,
      ULift.up_down, P, Int.cast_ofNat] using h ⟨hMinTotal, hMaxTotal⟩
  have hCompat : CompatiblePGeneric P := by
    have h := ClosestCompatible_from_structural_payload (beta:=radix) bo (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestCompatible_check,
      PredTrans.pure, PredTrans.apply, Id.run, ULift.down, P, Int.cast_ofNat] using h trivial
  have hMinMax : MinOrMaxP_float (beta:=radix) bo radix P := by
    have h := ClosestMinOrMax_from_split_radix_payload (beta:=radix) bo radix (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMinOrMax_check, MinOrMaxP_float,
      Id.run, ULift.up_down, P, Int.cast_ofNat] using h trivial
  have hMono : MonotoneP_float P := by
    have h := ClosestMonotone_from_split_radix_payload (beta:=radix) bo (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestMonotone_check,
      Id.run, ULift.up_down, P, Int.cast_ofNat] using h trivial
  have hRoundedFloat : RoundedModeP_float (beta:=radix) bo radix P :=
    ⟨hTotal, hCompat, hMinMax, hMono⟩
  have hUpperBound : Fbounded (beta:=radix) bo upper := by
    constructor
    · have hmantNonneg : 0 ≤ 2 * radix + 1 := by omega
      have hmantLtPow4 : 2 * radix + 1 < radix ^ 4 := by
        nlinarith [sq_nonneg (radix - 2), sq_nonneg radix]
      have hpow4Le : radix ^ 4 ≤ radix ^ precision := by
        exact pow_le_pow_right₀ (by omega : (1 : Int) ≤ radix) hprecision
      have hmantLtVNum : 2 * radix + 1 < bo.vNum := by
        rw [hvNum, Zpower_nat]
        exact lt_of_lt_of_le hmantLtPow4 hpow4Le
      simpa [upper, abs_of_nonneg hmantNonneg] using hmantLtVNum
    · simpa [upper] using hzNormal.1.2
  have hUpperVal :
      _root_.F2R (beta:=radix) upper =
        (radix : ℝ) ^ z.Fexp * (2 * (radix : ℝ) + 1) := by
    simp [upper, _root_.F2R, FloatSpec.Core.Defs.F2R]
    ring
  have htBound :
      |_root_.F2R (beta:=radix) t| ≤
        (radix : ℝ) ^ z.Fexp * ((radix : ℝ) + 1) := by
    have h := tLe (beta:=radix) bo radix precision a x b ph pl uh ul z t
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, tLe_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, hradix, hvNum, hprecision, hbBound, haBound,
        hxBound, hbCan, hphNormal, huhNormal, hzNormal, hproductExp, hzDef,
        hphDef, hplDef, huhDef, hulDef, hulNonzero, htDef⟩
  have hvBound :
      |_root_.F2R (beta:=radix) v| ≤
        (radix : ℝ) ^ z.Fexp * (radix : ℝ) := by
    have h := vLe (beta:=radix) bo radix precision a x b ph pl uh ul z v
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, hradix, hvNum, hprecision, hbBound, hphNormal,
        huhNormal, hzNormal, hzDef, hphDef, hplDef, huhDef, hulDef,
        hulNonzero, hvDef⟩
  have hInputBound :
      |_root_.F2R (beta:=radix) t + _root_.F2R (beta:=radix) v| ≤
        (radix : ℝ) ^ z.Fexp * (2 * (radix : ℝ) + 1) := by
    calc
      |_root_.F2R (beta:=radix) t + _root_.F2R (beta:=radix) v|
          ≤ |_root_.F2R (beta:=radix) t| +
            |_root_.F2R (beta:=radix) v| := abs_add_le _ _
      _ ≤ (radix : ℝ) ^ z.Fexp * ((radix : ℝ) + 1) +
            (radix : ℝ) ^ z.Fexp * (radix : ℝ) :=
          add_le_add htBound hvBound
      _ = (radix : ℝ) ^ z.Fexp * (2 * (radix : ℝ) + 1) := by
          ring
  have hAbsLeUpper :
      |_root_.F2R (beta:=radix) t + _root_.F2R (beta:=radix) v| ≤
        _root_.F2R (beta:=radix) upper := by
    simpa [hUpperVal] using hInputBound
  have hwAbsLeUpper :
      |_root_.F2R (beta:=radix) w| ≤ _root_.F2R (beta:=radix) upper := by
    have h := RoundAbsMonotoner_from_rounded_payload (beta:=radix) bo radix P
      (_root_.F2R (beta:=radix) t + _root_.F2R (beta:=radix) v) w upper
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, RoundAbsMonotoner_check, Id.run,
      ULift.up_down, P, Int.cast_ofNat] using h ⟨hRoundedFloat, hUpperBound, hwDef,
        hAbsLeUpper⟩
  simpa [hUpperVal] using hwAbsLeUpper

/-- Coq `ErrFmaApprox_2_aux`: in the inexact `uh` branch, with normal rounded
intermediate floats, the composed FMA approximation error is bounded by the
upstream `((3 * radix) / 2 + 1 / 2) * radix^(2 - 2 * precision)` scale. -/
theorem ErrFmaApprox_2_aux {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a x b ph pl uh ul z t v w : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : bo.vNum = Zpower_nat radix precision)
    (hprecision : 4 ≤ precision)
    (hbBound : Fbounded (beta:=beta) bo b)
    (haBound : Fbounded (beta:=beta) bo a)
    (hxBound : Fbounded (beta:=beta) bo x)
    (hbCan : Fcanonic (beta:=beta) radix bo b)
    (hphNormal : Fnormal (beta:=beta) radix bo ph)
    (huhNormal : Fnormal (beta:=beta) radix bo uh)
    (hzNormal : Fnormal (beta:=beta) radix bo z)
    (hvNormal : Fnormal (beta:=beta) radix bo v)
    (hwNormal : Fnormal (beta:=beta) radix bo w)
    (hproductExp : -bo.dExp ≤ a.Fexp + x.Fexp)
    (hzDef : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
        _root_.F2R (beta:=beta) b) z)
    (hphDef : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) ph)
    (hplDef : _root_.F2R (beta:=beta) pl =
      _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
        _root_.F2R (beta:=beta) ph)
    (huhDef : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) ph + _root_.F2R (beta:=beta) b) uh)
    (hulDef : _root_.F2R (beta:=beta) ul =
      _root_.F2R (beta:=beta) ph + _root_.F2R (beta:=beta) b -
        _root_.F2R (beta:=beta) uh)
    (htDef : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) uh - _root_.F2R (beta:=beta) z) t)
    (hvDef : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) pl + _root_.F2R (beta:=beta) ul) v)
    (hwDef : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) v) w)
    (hulNonzero : ¬ _root_.F2R (beta:=beta) ul = 0) :
    |_root_.F2R (beta:=beta) z + _root_.F2R (beta:=beta) w -
        (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
          _root_.F2R (beta:=beta) b)|
      ≤ ((3 * (radix : ℝ) / 2 + 1 / 2) *
          (radix : ℝ) ^ (2 - 2 * (precision : Int)) *
            |_root_.F2R (beta:=beta) z|) := by
  subst beta
  have hBoundExp : ∀ r : ℝ,
      -bo.dExp ≤ (boundR (beta:=radix) radix r).Fexp := by
    intro r
    exact le_trans (neg_nonpos.mpr bo.dExp_nonneg) (by simp [boundR, boundNat])
  have hprecisionNe : precision ≠ 0 := by omega
  have hvNumGt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecisionNe
  have hradixReal : (1 : ℝ) < (radix : ℝ) := by exact_mod_cast hradix
  have hradixPos : (0 : ℝ) < (radix : ℝ) := lt_trans zero_lt_one hradixReal
  have hradixNe : (radix : ℝ) ≠ 0 := ne_of_gt hradixPos
  have hMinTotal : TotalP (isMin (beta:=radix) bo radix) := by
    intro r
    have h := MinEx_from_finite_box_payload (beta:=radix) bo radix r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hvNumGt, hBoundExp r⟩
  have hMaxTotal : TotalP (isMax (beta:=radix) bo radix) := by
    intro r
    have h := MaxEx_from_finite_box_payload (beta:=radix) bo radix r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MaxEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hvNumGt, hBoundExp r⟩
  have hTotal : TotalP (Closest (beta:=radix) bo (radix : ℝ)) := by
    intro r
    have h := ClosestTotal_from_extrema_payload (beta:=radix) bo radix (radix : ℝ) r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestTotal_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨hMinTotal, hMaxTotal⟩
  have hRoundedFull :
      RoundedModeP_full (beta:=radix) bo
        (Closest (beta:=radix) bo (radix : ℝ)) := by
    have h := ClosestRoundedModeP_from_total_payload (beta:=radix) bo (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestRoundedModeP_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hTotal
  have htExact :
      _root_.F2R (beta:=radix) t =
        _root_.F2R (beta:=radix) uh - _root_.F2R (beta:=radix) z := by
    rcases tBounded bo radix precision a x b ph pl uh z rfl hradix hvNum
        (by omega : 3 ≤ precision) hbBound haBound hxBound hbCan
        (Or.inl hphNormal) (Or.inl hzNormal) (Or.inl huhNormal) hproductExp
        hzDef hphDef hplDef huhDef with
      ⟨t', ht'Bound, ht'Val⟩
    have htProject :
        _root_.F2R (beta:=radix) t' = _root_.F2R (beta:=radix) t :=
      hRoundedFull.2.2.2.2 t' t ht'Bound (by simpa [ht'Val] using htDef)
    rw [← htProject, ht'Val]
  have hwUlp :
      2 *
          |_root_.F2R (beta:=radix) t + _root_.F2R (beta:=radix) v -
            _root_.F2R (beta:=radix) w| ≤
        Fulp (beta:=radix) bo radix precision w := by
    have h := ClosestUlp (beta:=radix) bo radix precision
      (_root_.F2R (beta:=radix) t + _root_.F2R (beta:=radix) v) w
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨hwDef, rfl, hradix, hprecisionNe, hvNum⟩
  have hvUlp :
      2 *
          |_root_.F2R (beta:=radix) pl + _root_.F2R (beta:=radix) ul -
            _root_.F2R (beta:=radix) v| ≤
        Fulp (beta:=radix) bo radix precision v := by
    have h := ClosestUlp (beta:=radix) bo radix precision
      (_root_.F2R (beta:=radix) pl + _root_.F2R (beta:=radix) ul) v
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨hvDef, rfl, hradix, hprecisionNe, hvNum⟩
  have htwovErr :
      2 * |_root_.F2R (beta:=radix) z + _root_.F2R (beta:=radix) w -
          (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
            _root_.F2R (beta:=radix) b)|
        ≤ Fulp (beta:=radix) bo radix precision w +
          Fulp (beta:=radix) bo radix precision v := by
    have hdecomp :
        _root_.F2R (beta:=radix) z + _root_.F2R (beta:=radix) w -
            (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
              _root_.F2R (beta:=radix) b) =
          -(((_root_.F2R (beta:=radix) t + _root_.F2R (beta:=radix) v -
              _root_.F2R (beta:=radix) w) +
            (_root_.F2R (beta:=radix) pl + _root_.F2R (beta:=radix) ul -
              _root_.F2R (beta:=radix) v))) := by
      rw [htExact, hplDef, hulDef]
      ring
    calc
      2 * |_root_.F2R (beta:=radix) z + _root_.F2R (beta:=radix) w -
          (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
            _root_.F2R (beta:=radix) b)|
          = 2 * |((_root_.F2R (beta:=radix) t + _root_.F2R (beta:=radix) v -
              _root_.F2R (beta:=radix) w) +
            (_root_.F2R (beta:=radix) pl + _root_.F2R (beta:=radix) ul -
              _root_.F2R (beta:=radix) v))| := by
            rw [hdecomp, abs_neg]
      _ ≤ 2 *
          (|_root_.F2R (beta:=radix) t + _root_.F2R (beta:=radix) v -
              _root_.F2R (beta:=radix) w| +
            |_root_.F2R (beta:=radix) pl + _root_.F2R (beta:=radix) ul -
              _root_.F2R (beta:=radix) v|) := by
            exact mul_le_mul_of_nonneg_left (abs_add_le _ _) (by norm_num)
      _ = 2 * |_root_.F2R (beta:=radix) t + _root_.F2R (beta:=radix) v -
              _root_.F2R (beta:=radix) w| +
            2 * |_root_.F2R (beta:=radix) pl + _root_.F2R (beta:=radix) ul -
              _root_.F2R (beta:=radix) v| := by ring
      _ ≤ Fulp (beta:=radix) bo radix precision w +
          Fulp (beta:=radix) bo radix precision v := add_le_add hwUlp hvUlp
  have hwNormEq :
      Fnormalize (beta:=radix) radix bo precision w = w := by
    have h := FcanonicFnormalizeEq (beta:=radix) radix bo precision w
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨Or.inl hwNormal, rfl, hradix, hprecisionNe, hvNum⟩
  have hvNormEq :
      Fnormalize (beta:=radix) radix bo precision v = v := by
    have h := FcanonicFnormalizeEq (beta:=radix) radix bo precision v
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨Or.inl hvNormal, rfl, hradix, hprecisionNe, hvNum⟩
  have hzNormEq :
      Fnormalize (beta:=radix) radix bo precision z = z := by
    have h := FcanonicFnormalizeEq (beta:=radix) radix bo precision z
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨Or.inl hzNormal, rfl, hradix, hprecisionNe, hvNum⟩
  let eps : ℝ := (radix : ℝ) ^ (1 - (precision : Int))
  have hepsNonneg : 0 ≤ eps := le_of_lt (zpow_pos hradixPos _)
  have hwFulpRel :
      Fulp (beta:=radix) bo radix precision w ≤
        |_root_.F2R (beta:=radix) w| * eps := by
    have h := FulpLe2 (beta:=radix) radix bo precision w
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpLe2_check, Id.run,
      ULift.up_down, eps, Int.cast_ofNat] using
      h ⟨hwDef.1, by simpa [hwNormEq] using hwNormal, rfl, hradix,
        hprecisionNe, hvNum⟩
  have hvFulpRel :
      Fulp (beta:=radix) bo radix precision v ≤
        |_root_.F2R (beta:=radix) v| * eps := by
    have h := FulpLe2 (beta:=radix) radix bo precision v
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpLe2_check, Id.run,
      ULift.up_down, eps, Int.cast_ofNat] using
      h ⟨hvDef.1, by simpa [hvNormEq] using hvNormal, rfl, hradix,
        hprecisionNe, hvNum⟩
  have hzScale :
      (radix : ℝ) ^ z.Fexp ≤ |_root_.F2R (beta:=radix) z| * eps := by
    have hzFulp :
        Fulp (beta:=radix) bo radix precision z = (radix : ℝ) ^ z.Fexp := by
      have h := CanonicFulp (beta:=radix) radix bo precision z
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check, Id.run,
        ULift.up_down, Int.cast_ofNat] using
        h ⟨Or.inl hzNormal, rfl, hradix, hprecisionNe, hvNum⟩
    have h := FulpLe2 (beta:=radix) radix bo precision z
    have hrel :
        Fulp (beta:=radix) bo radix precision z ≤
          |_root_.F2R (beta:=radix) z| * eps := by
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpLe2_check, Id.run,
        ULift.up_down, eps, Int.cast_ofNat] using
        h ⟨hzDef.1, by simpa [hzNormEq] using hzNormal, rfl, hradix,
          hprecisionNe, hvNum⟩
    simpa [hzFulp] using hrel
  have hwBound :
      |_root_.F2R (beta:=radix) w| ≤
        (radix : ℝ) ^ z.Fexp * (2 * (radix : ℝ) + 1) := by
    have h := wLe (beta:=radix) bo radix precision a x b ph pl uh ul z t v w
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, wLe_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, hradix, hvNum, hprecision, hbBound, haBound,
        hxBound, hbCan, hphNormal, huhNormal, hzNormal, hproductExp, hzDef,
        hphDef, hplDef, huhDef, hulDef, hulNonzero, htDef, hvDef, hwDef⟩
  have hvBound :
      |_root_.F2R (beta:=radix) v| ≤
        (radix : ℝ) ^ z.Fexp * (radix : ℝ) := by
    have h := vLe (beta:=radix) bo radix precision a x b ph pl uh ul z v
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Id.run, ULift.up_down, Int.cast_ofNat] using
      h ⟨rfl, hradix, hvNum, hprecision, hbBound, hphNormal,
        huhNormal, hzNormal, hzDef, hphDef, hplDef, huhDef, hulDef,
        hulNonzero, hvDef⟩
  have hmagSum :
      |_root_.F2R (beta:=radix) w| + |_root_.F2R (beta:=radix) v| ≤
        (radix : ℝ) ^ z.Fexp * (3 * (radix : ℝ) + 1) := by
    nlinarith [hwBound, hvBound]
  have hscaled :
      Fulp (beta:=radix) bo radix precision w +
          Fulp (beta:=radix) bo radix precision v ≤
        ((radix : ℝ) ^ z.Fexp * (3 * (radix : ℝ) + 1)) * eps := by
    calc
      Fulp (beta:=radix) bo radix precision w +
          Fulp (beta:=radix) bo radix precision v
          ≤ |_root_.F2R (beta:=radix) w| * eps +
              |_root_.F2R (beta:=radix) v| * eps :=
            add_le_add hwFulpRel hvFulpRel
      _ = (|_root_.F2R (beta:=radix) w| +
              |_root_.F2R (beta:=radix) v|) * eps := by ring
      _ ≤ ((radix : ℝ) ^ z.Fexp * (3 * (radix : ℝ) + 1)) * eps :=
            mul_le_mul_of_nonneg_right hmagSum hepsNonneg
  have hcoefNonneg : 0 ≤ 3 * (radix : ℝ) + 1 := by nlinarith [hradixReal]
  have hpowMul :
      eps * eps = (radix : ℝ) ^ (2 - 2 * (precision : Int)) := by
    simp only [eps]
    rw [← zpow_add₀ hradixNe]
    congr 1
    ring
  have htwiceBound :
      2 * |_root_.F2R (beta:=radix) z + _root_.F2R (beta:=radix) w -
          (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
            _root_.F2R (beta:=radix) b)|
        ≤ (3 * (radix : ℝ) + 1) *
          (radix : ℝ) ^ (2 - 2 * (precision : Int)) *
            |_root_.F2R (beta:=radix) z| := by
    calc
      2 * |_root_.F2R (beta:=radix) z + _root_.F2R (beta:=radix) w -
          (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
            _root_.F2R (beta:=radix) b)|
          ≤ Fulp (beta:=radix) bo radix precision w +
              Fulp (beta:=radix) bo radix precision v := htwovErr
      _ ≤ ((radix : ℝ) ^ z.Fexp * (3 * (radix : ℝ) + 1)) * eps :=
            hscaled
      _ ≤ ((|_root_.F2R (beta:=radix) z| * eps) *
              (3 * (radix : ℝ) + 1)) * eps := by
            exact mul_le_mul_of_nonneg_right
              (mul_le_mul_of_nonneg_right hzScale hcoefNonneg) hepsNonneg
      _ = (3 * (radix : ℝ) + 1) *
          (radix : ℝ) ^ (2 - 2 * (precision : Int)) *
            |_root_.F2R (beta:=radix) z| := by
            rw [← hpowMul]
            ring
  calc
    |_root_.F2R (beta:=radix) z + _root_.F2R (beta:=radix) w -
        (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
          _root_.F2R (beta:=radix) b)|
        ≤ ((3 * (radix : ℝ) + 1) / 2) *
          (radix : ℝ) ^ (2 - 2 * (precision : Int)) *
            |_root_.F2R (beta:=radix) z| := by
          nlinarith [htwiceBound]
    _ = ((3 * (radix : ℝ) / 2 + 1 / 2) *
          (radix : ℝ) ^ (2 - 2 * (precision : Int)) *
            |_root_.F2R (beta:=radix) z|) := by ring_nf

/-- Coq `ErrFmaApprox_2`: public inexact-`uh` FMA branch with the upstream
normal-or-zero hypotheses for `ph`, `uh`, `z`, `v`, and `w`. -/
theorem ErrFmaApprox_2 {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a x b ph pl uh ul z t v w : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : bo.vNum = Zpower_nat radix precision)
    (hprecision : 4 ≤ precision)
    (hbBound : Fbounded (beta:=beta) bo b)
    (haBound : Fbounded (beta:=beta) bo a)
    (hxBound : Fbounded (beta:=beta) bo x)
    (hbCan : Fcanonic (beta:=beta) radix bo b)
    (hphNZ : Fnormal (beta:=beta) radix bo ph ∨
      _root_.F2R (beta:=beta) ph = 0)
    (huhNZ : Fnormal (beta:=beta) radix bo uh ∨
      _root_.F2R (beta:=beta) uh = 0)
    (hzNZ : Fnormal (beta:=beta) radix bo z ∨
      _root_.F2R (beta:=beta) z = 0)
    (hvNZ : Fnormal (beta:=beta) radix bo v ∨
      _root_.F2R (beta:=beta) v = 0)
    (hwNZ : Fnormal (beta:=beta) radix bo w ∨
      _root_.F2R (beta:=beta) w = 0)
    (hproductExp : -bo.dExp ≤ a.Fexp + x.Fexp)
    (hzDef : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
        _root_.F2R (beta:=beta) b) z)
    (hphDef : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) ph)
    (hplDef : _root_.F2R (beta:=beta) pl =
      _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
        _root_.F2R (beta:=beta) ph)
    (huhDef : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) ph + _root_.F2R (beta:=beta) b) uh)
    (hulDef : _root_.F2R (beta:=beta) ul =
      _root_.F2R (beta:=beta) ph + _root_.F2R (beta:=beta) b -
        _root_.F2R (beta:=beta) uh)
    (htDef : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) uh - _root_.F2R (beta:=beta) z) t)
    (hvDef : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) pl + _root_.F2R (beta:=beta) ul) v)
    (hwDef : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) v) w)
    (hulNonzero : ¬ _root_.F2R (beta:=beta) ul = 0) :
    |_root_.F2R (beta:=beta) z + _root_.F2R (beta:=beta) w -
        (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
          _root_.F2R (beta:=beta) b)|
      ≤ ((3 * (radix : ℝ) / 2 + 1 / 2) *
          (radix : ℝ) ^ (2 - 2 * (precision : Int)) *
            |_root_.F2R (beta:=beta) z|) := by
  subst beta
  have hBoundExp : ∀ r : ℝ,
      -bo.dExp ≤ (boundR (beta:=radix) radix r).Fexp := by
    intro r
    exact le_trans (neg_nonpos.mpr bo.dExp_nonneg) (by simp [boundR, boundNat])
  have hprecisionNe : precision ≠ 0 := by omega
  have hprecision3 : 3 ≤ precision := by omega
  have hvNumGt : 1 < bo.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecisionNe
  have hradixReal : (1 : ℝ) < (radix : ℝ) := by exact_mod_cast hradix
  have hradixPos : (0 : ℝ) < (radix : ℝ) := lt_trans zero_lt_one hradixReal
  have hradixNe : (radix : ℝ) ≠ 0 := ne_of_gt hradixPos
  have hMinTotal : TotalP (isMin (beta:=radix) bo radix) := by
    intro r
    have h := MinEx_from_finite_box_payload (beta:=radix) bo radix r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MinEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hvNumGt, hBoundExp r⟩
  have hMaxTotal : TotalP (isMax (beta:=radix) bo radix) := by
    intro r
    have h := MaxEx_from_finite_box_payload (beta:=radix) bo radix r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, MaxEx_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨rfl, hradix, hvNumGt, hBoundExp r⟩
  have hTotal : TotalP (Closest (beta:=radix) bo (radix : ℝ)) := by
    intro r
    have h := ClosestTotal_from_extrema_payload (beta:=radix) bo radix (radix : ℝ) r
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestTotal_check, Id.run,
      ULift.up_down, Int.cast_ofNat] using h ⟨hMinTotal, hMaxTotal⟩
  have hRoundedFull :
      RoundedModeP_full (beta:=radix) bo
        (Closest (beta:=radix) bo (radix : ℝ)) := by
    have h := ClosestRoundedModeP_from_total_payload (beta:=radix) bo (radix : ℝ)
    simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestRoundedModeP_check,
      Id.run, ULift.up_down, Int.cast_ofNat] using h hTotal
  have hRhsNonneg :
      0 ≤ ((3 * (radix : ℝ) / 2 + 1 / 2) *
          (radix : ℝ) ^ (2 - 2 * (precision : Int)) *
            |_root_.F2R (beta:=radix) z|) := by
    have hcoef : 0 ≤ 3 * (radix : ℝ) / 2 + 1 / 2 := by nlinarith
    exact mul_nonneg (mul_nonneg hcoef (le_of_lt (zpow_pos hradixPos _)))
      (abs_nonneg _)
  let eps : ℝ := (radix : ℝ) ^ (1 - (precision : Int))
  have hepsNonneg : 0 ≤ eps := le_of_lt (zpow_pos hradixPos _)
  have hpowMul : eps * eps = (radix : ℝ) ^ (2 - 2 * (precision : Int)) := by
    simp only [eps]
    rw [← zpow_add₀ hradixNe]
    congr 1
    ring
  rcases hvNZ with hvNormal | hvZero
  · rcases hzNZ with hzNormal | hzZero
    · rcases hphNZ with hphNormal | hphZero
      · rcases huhNZ with huhNormal | huhZero
        · rcases hwNZ with hwNormal | hwZero
          · exact ErrFmaApprox_2_aux bo radix precision a x b ph pl uh ul z t v w
              rfl hradix hvNum hprecision hbBound haBound hxBound hbCan
              hphNormal huhNormal hzNormal hvNormal hwNormal hproductExp hzDef
              hphDef hplDef huhDef hulDef htDef hvDef hwDef hulNonzero
          · have htExact :
                _root_.F2R (beta:=radix) t =
                  _root_.F2R (beta:=radix) uh - _root_.F2R (beta:=radix) z := by
              rcases tBounded bo radix precision a x b ph pl uh z rfl hradix
                  hvNum hprecision3 hbBound haBound hxBound hbCan
                  (Or.inl hphNormal) (Or.inl hzNormal) (Or.inl huhNormal)
                  hproductExp hzDef hphDef hplDef huhDef with
                ⟨t', ht'Bound, ht'Val⟩
              have htProject :
                  _root_.F2R (beta:=radix) t' = _root_.F2R (beta:=radix) t :=
                hRoundedFull.2.2.2.2 t' t ht'Bound (by simpa [ht'Val] using htDef)
              rw [← htProject, ht'Val]
            let sumtv : FloatSpec.Core.Defs.FlocqFloat radix := Fplus (beta:=radix) t v
            have hsumtvVal :
                _root_.F2R (beta:=radix) sumtv =
                  _root_.F2R (beta:=radix) t + _root_.F2R (beta:=radix) v := by
              have h := Fplus_correct (beta:=radix) t v
              simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fplus_correct_check,
                Id.run, ULift.up_down, sumtv, Int.cast_ofNat] using h hradix
            have hsumtvExp : -bo.dExp ≤ sumtv.Fexp := by
              have htExp : -bo.dExp ≤ t.Fexp := htDef.1.2
              have hvExp : -bo.dExp ≤ v.Fexp := hvDef.1.2
              by_cases htv : t.Fexp ≤ v.Fexp
              · simpa [sumtv, Fplus, FloatSpec.Calc.Operations.Fplus,
                  FloatSpec.Calc.Operations.Falign, htv] using htExp
              · simpa [sumtv, Fplus, FloatSpec.Calc.Operations.Fplus,
                  FloatSpec.Calc.Operations.Falign, htv] using hvExp
            have hsumtvZero :
                _root_.F2R (beta:=radix) t + _root_.F2R (beta:=radix) v = 0 := by
              have hzero := ClosestZero1_from_expanded_closest_payload (beta:=radix) bo (radix : ℝ)
                (_root_.F2R (beta:=radix) t + _root_.F2R (beta:=radix) v) w sumtv
              simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestZero1_check,
                Id.run, ULift.down_up, Int.cast_ofNat] using
                hzero ⟨hwDef, hwZero, hsumtvVal.symm, hsumtvExp,
                  by simpa [Fbounded'] using hwDef.1,
                  by
                    intro g hg
                    exact hwDef.2 g (by simpa [Fbounded'] using hg),
                  hradixReal, hvNumGt⟩
            have hvUlp :
                2 *
                    |_root_.F2R (beta:=radix) pl + _root_.F2R (beta:=radix) ul -
                      _root_.F2R (beta:=radix) v| ≤
                  Fulp (beta:=radix) bo radix precision v := by
              have h := ClosestUlp (beta:=radix) bo radix precision
                (_root_.F2R (beta:=radix) pl + _root_.F2R (beta:=radix) ul) v
              simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestUlp_check, Id.run,
                ULift.up_down, Int.cast_ofNat] using h ⟨hvDef, rfl, hradix, hprecisionNe, hvNum⟩
            have hvNormEq :
                Fnormalize (beta:=radix) radix bo precision v = v := by
              have h := FcanonicFnormalizeEq (beta:=radix) radix bo precision v
              simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
                Id.run, ULift.up_down, Int.cast_ofNat] using
                h ⟨Or.inl hvNormal, rfl, hradix, hprecisionNe, hvNum⟩
            have hvFulpRel :
                Fulp (beta:=radix) bo radix precision v ≤
                  |_root_.F2R (beta:=radix) v| * eps := by
              have h := FulpLe2 (beta:=radix) radix bo precision v
              simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpLe2_check, Id.run,
                ULift.up_down, eps, Int.cast_ofNat] using
                h ⟨hvDef.1, by simpa [hvNormEq] using hvNormal, rfl, hradix,
                  hprecisionNe, hvNum⟩
            have hzNormEq :
                Fnormalize (beta:=radix) radix bo precision z = z := by
              have h := FcanonicFnormalizeEq (beta:=radix) radix bo precision z
              simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FcanonicFnormalizeEq_check,
                Id.run, ULift.up_down, Int.cast_ofNat] using
                h ⟨Or.inl hzNormal, rfl, hradix, hprecisionNe, hvNum⟩
            have hzScale :
                (radix : ℝ) ^ z.Fexp ≤ |_root_.F2R (beta:=radix) z| * eps := by
              have hzFulp :
                  Fulp (beta:=radix) bo radix precision z = (radix : ℝ) ^ z.Fexp := by
                have h := CanonicFulp (beta:=radix) radix bo precision z
                simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, CanonicFulp_check,
                  Id.run, ULift.up_down, Int.cast_ofNat] using
                  h ⟨Or.inl hzNormal, rfl, hradix, hprecisionNe, hvNum⟩
              have h := FulpLe2 (beta:=radix) radix bo precision z
              have hrel :
                  Fulp (beta:=radix) bo radix precision z ≤
                    |_root_.F2R (beta:=radix) z| * eps := by
                simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FulpLe2_check, Id.run,
                  ULift.up_down, eps, Int.cast_ofNat] using
                  h ⟨hzDef.1, by simpa [hzNormEq] using hzNormal, rfl, hradix,
                    hprecisionNe, hvNum⟩
              simpa [hzFulp] using hrel
            have hvBound :
                |_root_.F2R (beta:=radix) v| ≤
                  (radix : ℝ) ^ z.Fexp * (radix : ℝ) := by
              have h := vLe (beta:=radix) bo radix precision a x b ph pl uh ul z v
              simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Id.run, ULift.up_down, Int.cast_ofNat] using
                h ⟨rfl, hradix, hvNum, hprecision, hbBound, hphNormal,
                  huhNormal, hzNormal, hzDef, hphDef, hplDef, huhDef, hulDef,
                  hulNonzero, hvDef⟩
            have herrorEq :
                |_root_.F2R (beta:=radix) z + _root_.F2R (beta:=radix) w -
                    (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
                      _root_.F2R (beta:=radix) b)| =
                  |_root_.F2R (beta:=radix) pl + _root_.F2R (beta:=radix) ul -
                    _root_.F2R (beta:=radix) v| := by
              have hraw :
                  _root_.F2R (beta:=radix) z + _root_.F2R (beta:=radix) w -
                      (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
                        _root_.F2R (beta:=radix) b) =
                    -(_root_.F2R (beta:=radix) pl + _root_.F2R (beta:=radix) ul -
                      _root_.F2R (beta:=radix) v) := by
                rw [hplDef, hulDef, hwZero]
                nlinarith [hsumtvZero, htExact]
              rw [hraw, abs_neg]
            have htwice :
                2 *
                    |_root_.F2R (beta:=radix) z + _root_.F2R (beta:=radix) w -
                      (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
                        _root_.F2R (beta:=radix) b)|
                  ≤ Fulp (beta:=radix) bo radix precision v := by
              simpa [herrorEq] using hvUlp
            have hsmall :
                2 *
                    |_root_.F2R (beta:=radix) z + _root_.F2R (beta:=radix) w -
                      (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
                        _root_.F2R (beta:=radix) b)|
                  ≤ (radix : ℝ) *
                    (radix : ℝ) ^ (2 - 2 * (precision : Int)) *
                      |_root_.F2R (beta:=radix) z| := by
              calc
                2 *
                    |_root_.F2R (beta:=radix) z + _root_.F2R (beta:=radix) w -
                      (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
                        _root_.F2R (beta:=radix) b)|
                    ≤ Fulp (beta:=radix) bo radix precision v := htwice
                _ ≤ |_root_.F2R (beta:=radix) v| * eps := hvFulpRel
                _ ≤ ((radix : ℝ) ^ z.Fexp * (radix : ℝ)) * eps :=
                  mul_le_mul_of_nonneg_right hvBound hepsNonneg
                _ ≤ ((|_root_.F2R (beta:=radix) z| * eps) *
                    (radix : ℝ)) * eps := by
                  exact mul_le_mul_of_nonneg_right
                    (mul_le_mul_of_nonneg_right hzScale (le_of_lt hradixPos))
                    hepsNonneg
                _ = (radix : ℝ) *
                    (radix : ℝ) ^ (2 - 2 * (precision : Int)) *
                      |_root_.F2R (beta:=radix) z| := by
                  rw [← hpowMul]
                  ring
            have hcoef :
                (radix : ℝ) ≤ 3 * (radix : ℝ) + 1 := by nlinarith [hradixReal]
            have hcoefNonneg :
                0 ≤ (radix : ℝ) ^ (2 - 2 * (precision : Int)) *
                  |_root_.F2R (beta:=radix) z| := by
              exact mul_nonneg (le_of_lt (zpow_pos hradixPos _)) (abs_nonneg _)
            have htwiceFinal :
                2 *
                    |_root_.F2R (beta:=radix) z + _root_.F2R (beta:=radix) w -
                      (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
                        _root_.F2R (beta:=radix) b)|
                  ≤ (3 * (radix : ℝ) + 1) *
                    (radix : ℝ) ^ (2 - 2 * (precision : Int)) *
                      |_root_.F2R (beta:=radix) z| := by
              exact le_trans hsmall (by
                simpa [mul_assoc, mul_comm, mul_left_comm] using
                  mul_le_mul_of_nonneg_right hcoef hcoefNonneg)
            calc
              |_root_.F2R (beta:=radix) z + _root_.F2R (beta:=radix) w -
                  (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
                    _root_.F2R (beta:=radix) b)|
                  ≤ ((3 * (radix : ℝ) + 1) / 2) *
                    (radix : ℝ) ^ (2 - 2 * (precision : Int)) *
                      |_root_.F2R (beta:=radix) z| := by
                    nlinarith [htwiceFinal]
              _ = ((3 * (radix : ℝ) / 2 + 1 / 2) *
                    (radix : ℝ) ^ (2 - 2 * (precision : Int)) *
                      |_root_.F2R (beta:=radix) z|) := by ring_nf
        · have hsum : _root_.F2R (beta:=radix) ph +
                _root_.F2R (beta:=radix) b = 0 := by
            let sumphb : FloatSpec.Core.Defs.FlocqFloat radix := Fplus (beta:=radix) ph b
            have hsumVal :
                _root_.F2R (beta:=radix) sumphb =
                  _root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b := by
              have h := Fplus_correct (beta:=radix) ph b
              simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fplus_correct_check,
                Id.run, ULift.up_down, sumphb, Int.cast_ofNat] using h hradix
            have hsumExp : -bo.dExp ≤ sumphb.Fexp := by
              have hphExp : -bo.dExp ≤ ph.Fexp := hphNormal.1.2
              have hbExp : -bo.dExp ≤ b.Fexp := hbBound.2
              by_cases hle : ph.Fexp ≤ b.Fexp
              · simpa [sumphb, Fplus, FloatSpec.Calc.Operations.Fplus,
                  FloatSpec.Calc.Operations.Falign, hle] using hphExp
              · simpa [sumphb, Fplus, FloatSpec.Calc.Operations.Fplus,
                  FloatSpec.Calc.Operations.Falign, hle] using hbExp
            have hzero := ClosestZero1_from_expanded_closest_payload (beta:=radix) bo (radix : ℝ)
              (_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b)
              uh sumphb
            simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestZero1_check,
              Id.run, ULift.down_up, Int.cast_ofNat] using
              hzero ⟨huhDef, huhZero, hsumVal.symm, hsumExp,
                by simpa [Fbounded'] using huhDef.1,
                by
                  intro g hg
                  exact huhDef.2 g (by simpa [Fbounded'] using hg),
                hradixReal, hvNumGt⟩
          exact False.elim (hulNonzero (by rw [hulDef, hsum, huhZero]; ring))
      · have hbEqUh :
            _root_.F2R (beta:=radix) b = _root_.F2R (beta:=radix) uh := by
          exact hRoundedFull.2.2.2.2 b uh hbBound
            (by simpa [hphZero, zero_add] using huhDef)
        exact False.elim (hulNonzero (by rw [hulDef, hphZero, hbEqUh]; ring))
    · let axb : FloatSpec.Core.Defs.FlocqFloat radix :=
        Fplus (beta:=radix) (FloatSpec.Calc.Operations.Fmult (beta:=radix) a x) b
      have hprodVal :
          _root_.F2R (beta:=radix) (FloatSpec.Calc.Operations.Fmult (beta:=radix) a x) =
            _root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x := by
        have h := Fmult_correct (beta:=radix) a x
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fmult_correct_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h hradix
      have haxbVal :
          _root_.F2R (beta:=radix) axb =
            _root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
              _root_.F2R (beta:=radix) b := by
        have h := Fplus_correct (beta:=radix)
          (FloatSpec.Calc.Operations.Fmult (beta:=radix) a x) b
        calc
          _root_.F2R (beta:=radix) axb =
              _root_.F2R (beta:=radix)
                  (FloatSpec.Calc.Operations.Fmult (beta:=radix) a x) +
                _root_.F2R (beta:=radix) b := by
                  simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fplus_correct_check,
                    Id.run, ULift.up_down, axb, Int.cast_ofNat] using h hradix
          _ = _root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
                _root_.F2R (beta:=radix) b := by rw [hprodVal]
      have haxbExp : -bo.dExp ≤ axb.Fexp := by
        have hbExp : -bo.dExp ≤ b.Fexp := hbBound.2
        by_cases hle : a.Fexp + x.Fexp ≤ b.Fexp
        · simpa [axb, Fplus, FloatSpec.Calc.Operations.Fplus,
            FloatSpec.Calc.Operations.Falign, FloatSpec.Calc.Operations.Fmult, hle]
            using hproductExp
        · simpa [axb, Fplus, FloatSpec.Calc.Operations.Fplus,
            FloatSpec.Calc.Operations.Falign, FloatSpec.Calc.Operations.Fmult, hle]
            using hbExp
      have hinputZero :
          _root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
            _root_.F2R (beta:=radix) b = 0 := by
        have hzero := ClosestZero1_from_expanded_closest_payload (beta:=radix) bo (radix : ℝ)
          (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
            _root_.F2R (beta:=radix) b) z axb
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestZero1_check,
          Id.run, ULift.down_up, Int.cast_ofNat] using
          hzero ⟨hzDef, hzZero, haxbVal.symm, haxbExp,
            by simpa [Fbounded'] using hzDef.1,
            by
              intro g hg
              exact hzDef.2 g (by simpa [Fbounded'] using hg),
            hradixReal, hvNumGt⟩
      have hbOppBound : Fbounded (beta:=radix) bo (Fopp (beta:=radix) b) := by
        have h := oppBounded (beta:=radix) bo b
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, oppBounded_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h hbBound
      have hbOppVal :
          _root_.F2R (beta:=radix) (Fopp (beta:=radix) b) =
            -_root_.F2R (beta:=radix) b := by
        have h := Fopp_correct (beta:=radix) b
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fopp_correct_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using h True.intro
      have hphEqOppB :
          _root_.F2R (beta:=radix) (Fopp (beta:=radix) b) =
            _root_.F2R (beta:=radix) ph := by
        have haxEqOppB :
            _root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x =
              _root_.F2R (beta:=radix) (Fopp (beta:=radix) b) := by
          rw [hbOppVal]
          linarith
        exact hRoundedFull.2.2.2.2 (Fopp (beta:=radix) b) ph hbOppBound
          (by simpa [haxEqOppB] using hphDef)
      have hphPlusBZero :
          _root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b = 0 := by
        linarith [hbOppVal, hphEqOppB]
      have huhZero :
          _root_.F2R (beta:=radix) uh = 0 := by
        have hzero := ClosestZero2 (beta:=radix) bo (radix : ℝ)
          (_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) b) uh
        simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestZero2_check,
          Id.run, ULift.up_down, Int.cast_ofNat] using hzero ⟨huhDef, hphPlusBZero⟩
      exact False.elim (hulNonzero (by rw [hulDef, hphPlusBZero, huhZero]; ring))
  · rcases errorBoundedPlus_from_format_payload (beta:=radix) bo radix precision rfl hradix
        hprecisionNe hvNumGt hvNum ph b uh hphDef.1 hbBound huhDef with
      ⟨ul', hul'Val, hul'Bound, hul'Exp⟩
    rcases errorBoundedMult (beta:=radix) bo radix precision a x ph with hmult
    have hmult' := hmult ⟨rfl, hradix, hprecisionNe, hvNum, haBound, hxBound,
      hproductExp, hphDef⟩
    rcases hmult' with ⟨pl', hpl'Val, hpl'Bound, hpl'Exp⟩
    let sumErr : FloatSpec.Core.Defs.FlocqFloat radix := Fplus (beta:=radix) pl' ul'
    have hsumErrVal :
        _root_.F2R (beta:=radix) sumErr =
          _root_.F2R (beta:=radix) pl' + _root_.F2R (beta:=radix) ul' := by
      have h := Fplus_correct (beta:=radix) pl' ul'
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, Fplus_correct_check,
        Id.run, ULift.up_down, sumErr, Int.cast_ofNat] using h hradix
    have hsumErrExp : -bo.dExp ≤ sumErr.Fexp := by
      have hplExp : -bo.dExp ≤ pl'.Fexp := hpl'Bound.2
      have hulExp : -bo.dExp ≤ ul'.Fexp := hul'Bound.2
      by_cases hle : pl'.Fexp ≤ ul'.Fexp
      · simpa [sumErr, Fplus, FloatSpec.Calc.Operations.Fplus,
          FloatSpec.Calc.Operations.Falign, hle] using hplExp
      · simpa [sumErr, Fplus, FloatSpec.Calc.Operations.Fplus,
          FloatSpec.Calc.Operations.Falign, hle] using hulExp
    have hsumErrInput :
        _root_.F2R (beta:=radix) pl + _root_.F2R (beta:=radix) ul =
          _root_.F2R (beta:=radix) sumErr := by
      rw [hsumErrVal, hpl'Val, hul'Val, hplDef, hulDef]
    have hplulZero :
        _root_.F2R (beta:=radix) pl + _root_.F2R (beta:=radix) ul = 0 := by
      have hzero := ClosestZero1_from_expanded_closest_payload (beta:=radix) bo (radix : ℝ)
        (_root_.F2R (beta:=radix) pl + _root_.F2R (beta:=radix) ul) v sumErr
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, ClosestZero1_check,
        Id.run, ULift.down_up, Int.cast_ofNat] using
        hzero ⟨hvDef, hvZero, hsumErrInput, hsumErrExp,
          by simpa [Fbounded'] using hvDef.1,
          by
            intro g hg
            exact hvDef.2 g (by simpa [Fbounded'] using hg),
          hradixReal, hvNumGt⟩
    have htExact :
        _root_.F2R (beta:=radix) t =
          _root_.F2R (beta:=radix) uh - _root_.F2R (beta:=radix) z := by
      rcases tBounded bo radix precision a x b ph pl uh z rfl hradix hvNum
          hprecision3 hbBound haBound hxBound hbCan hphNZ hzNZ huhNZ
          hproductExp hzDef hphDef hplDef huhDef with
        ⟨t', ht'Bound, ht'Val⟩
      have htProject :
          _root_.F2R (beta:=radix) t' = _root_.F2R (beta:=radix) t :=
        hRoundedFull.2.2.2.2 t' t ht'Bound (by simpa [ht'Val] using htDef)
      rw [← htProject, ht'Val]
    have hwEqT :
        _root_.F2R (beta:=radix) t = _root_.F2R (beta:=radix) w :=
      hRoundedFull.2.2.2.2 t w htDef.1 (by simpa [hvZero, add_zero] using hwDef)
    have htargetZero :
        _root_.F2R (beta:=radix) z + _root_.F2R (beta:=radix) w -
            (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
              _root_.F2R (beta:=radix) b) = 0 := by
      rw [← hwEqT, htExact]
      nlinarith [hplDef, hulDef, hplulZero]
    simpa [htargetZero] using hRhsNonneg

/-- Coq `ErrFmaApprox`: public Total-section FMA approximation bound.

This is the upstream wrapper over the exact and inexact `ul` branches.  The
canonical representative for `b` and the bounded product residual are kept
internal, matching Flocq's `Total` section statement. -/
theorem ErrFmaApprox {beta : Int} [ValidRadix beta]
    (bo : Fbound_skel) (radix : Int) (precision : Nat)
    (a x b ph pl uh ul z t v w : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix)
    (hvNum : bo.vNum = Zpower_nat radix precision)
    (hprecision : 4 ≤ precision)
    (hbBound : Fbounded (beta:=beta) bo b)
    (haBound : Fbounded (beta:=beta) bo a)
    (hxBound : Fbounded (beta:=beta) bo x)
    (hphNZ : Fnormal (beta:=beta) radix bo ph ∨
      _root_.F2R (beta:=beta) ph = 0)
    (huhNZ : Fnormal (beta:=beta) radix bo uh ∨
      _root_.F2R (beta:=beta) uh = 0)
    (hzNZ : Fnormal (beta:=beta) radix bo z ∨
      _root_.F2R (beta:=beta) z = 0)
    (hwNZ : Fnormal (beta:=beta) radix bo w ∨
      _root_.F2R (beta:=beta) w = 0)
    (hvNZ : Fnormal (beta:=beta) radix bo v ∨
      _root_.F2R (beta:=beta) v = 0)
    (hproductExp : -bo.dExp ≤ a.Fexp + x.Fexp)
    (hzDef : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
        _root_.F2R (beta:=beta) b) z)
    (hphDef : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x) ph)
    (hplDef : _root_.F2R (beta:=beta) pl =
      _root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x -
        _root_.F2R (beta:=beta) ph)
    (huhDef : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) ph + _root_.F2R (beta:=beta) b) uh)
    (hulDef : _root_.F2R (beta:=beta) ul =
      _root_.F2R (beta:=beta) ph + _root_.F2R (beta:=beta) b -
        _root_.F2R (beta:=beta) uh)
    (htDef : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) uh - _root_.F2R (beta:=beta) z) t)
    (hvDef : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) pl + _root_.F2R (beta:=beta) ul) v)
    (hwDef : Closest (beta:=beta) bo (radix : ℝ)
      (_root_.F2R (beta:=beta) t + _root_.F2R (beta:=beta) v) w) :
    |_root_.F2R (beta:=beta) z + _root_.F2R (beta:=beta) w -
        (_root_.F2R (beta:=beta) a * _root_.F2R (beta:=beta) x +
          _root_.F2R (beta:=beta) b)|
      ≤ ((3 * (radix : ℝ) / 2 + 1 / 2) *
          (radix : ℝ) ^ (2 - 2 * (precision : Int)) *
            |_root_.F2R (beta:=beta) z|) := by
  subst beta
  have hprecisionNe : precision ≠ 0 := by omega
  have hprecision3 : 3 ≤ precision := by omega
  let bNorm : FloatSpec.Core.Defs.FlocqFloat radix :=
    Fnormalize (beta:=radix) radix bo precision b
  by_cases hulZero : _root_.F2R (beta:=radix) ul = 0
  · rcases errorBoundedMult (beta:=radix) bo radix precision a x ph with hmult
    have hmult' := hmult ⟨rfl, hradix, hprecisionNe, hvNum, haBound, hxBound,
      hproductExp, hphDef⟩
    rcases hmult' with ⟨plPrime, hplPrimeVal, hplPrimeBound, _hplPrimeExp⟩
    have hbNormCorrect :
        _root_.F2R (beta:=radix) bNorm =
          _root_.F2R (beta:=radix) b := by
      have h := FnormalizeCorrect (beta:=radix) radix bo precision b
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
        Id.run, ULift.up_down, bNorm, Int.cast_ofNat] using h ⟨rfl, hradix⟩
    have hbNormBound : Fbounded (beta:=radix) bo bNorm := by
      have h := FnormalizeBounded (beta:=radix) radix bo precision b
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeBounded_check,
        Id.run, ULift.up_down, Fbounded', bNorm, Int.cast_ofNat] using
        h ⟨hbBound, hbBound, hprecisionNe, hradix, hvNum⟩
    have hbNormCan : Fcanonic (beta:=radix) radix bo bNorm := by
      have h := FnormalizeCanonic (beta:=radix) radix bo precision b
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
        Id.run, ULift.up_down, Fbounded', bNorm, Int.cast_ofNat] using
        h ⟨hbBound, hbBound, hprecisionNe, hradix, hvNum⟩
    have hplPrimeEq :
        _root_.F2R (beta:=radix) plPrime =
          _root_.F2R (beta:=radix) pl := by
      rw [hplPrimeVal, hplDef]
    have hzDefNorm :
        Closest (beta:=radix) bo (radix : ℝ)
          (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
            _root_.F2R (beta:=radix) bNorm) z := by
      simpa [hbNormCorrect] using hzDef
    have huhDefNorm :
        Closest (beta:=radix) bo (radix : ℝ)
          (_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) bNorm) uh := by
      simpa [hbNormCorrect] using huhDef
    have hulDefNorm :
        _root_.F2R (beta:=radix) ul =
          _root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) bNorm -
            _root_.F2R (beta:=radix) uh := by
      simpa [hbNormCorrect] using hulDef
    have hvDefPrime :
        Closest (beta:=radix) bo (radix : ℝ)
          (_root_.F2R (beta:=radix) plPrime + _root_.F2R (beta:=radix) ul) v := by
      simpa [hplPrimeEq] using hvDef
    have hbranch := ErrFmaApprox_1 bo radix precision a x bNorm ph plPrime uh ul
      z t v w rfl hradix hvNum hprecision3 hbNormBound haBound
      hxBound hbNormCan hphNZ huhNZ hzNZ hwNZ hplPrimeBound hproductExp
      hzDefNorm hphDef hplPrimeVal huhDefNorm hulDefNorm htDef hvDefPrime hwDef
      hulZero
    simpa [hbNormCorrect] using hbranch
  · have hbNormCorrect :
        _root_.F2R (beta:=radix) bNorm =
          _root_.F2R (beta:=radix) b := by
      have h := FnormalizeCorrect (beta:=radix) radix bo precision b
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCorrect_check,
        Id.run, ULift.up_down, bNorm, Int.cast_ofNat] using h ⟨rfl, hradix⟩
    have hbNormBound : Fbounded (beta:=radix) bo bNorm := by
      have h := FnormalizeBounded (beta:=radix) radix bo precision b
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeBounded_check,
        Id.run, ULift.up_down, Fbounded', bNorm, Int.cast_ofNat] using
        h ⟨hbBound, hbBound, hprecisionNe, hradix, hvNum⟩
    have hbNormCan : Fcanonic (beta:=radix) radix bo bNorm := by
      have h := FnormalizeCanonic (beta:=radix) radix bo precision b
      simpa only [wp, PostCond.noThrow, pure, PredTrans.pure, PredTrans.apply, SPred.down_pure_nil, FnormalizeCanonic_check,
        Id.run, ULift.up_down, Fbounded', bNorm, Int.cast_ofNat] using
        h ⟨hbBound, hbBound, hprecisionNe, hradix, hvNum⟩
    have hzDefNorm :
        Closest (beta:=radix) bo (radix : ℝ)
          (_root_.F2R (beta:=radix) a * _root_.F2R (beta:=radix) x +
            _root_.F2R (beta:=radix) bNorm) z := by
      simpa [hbNormCorrect] using hzDef
    have huhDefNorm :
        Closest (beta:=radix) bo (radix : ℝ)
          (_root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) bNorm) uh := by
      simpa [hbNormCorrect] using huhDef
    have hulDefNorm :
        _root_.F2R (beta:=radix) ul =
          _root_.F2R (beta:=radix) ph + _root_.F2R (beta:=radix) bNorm -
            _root_.F2R (beta:=radix) uh := by
      simpa [hbNormCorrect] using hulDef
    have hbranch := ErrFmaApprox_2 bo radix precision a x bNorm ph pl uh ul
      z t v w rfl hradix hvNum hprecision hbNormBound haBound
      hxBound hbNormCan hphNZ huhNZ hzNZ hvNZ hwNZ hproductExp hzDefNorm
      hphDef hplDef huhDefNorm hulDefNorm htDef hvDef hwDef hulZero
    simpa [hbNormCorrect] using hbranch

-- Coq: `digit_abs` — digit n (|p|) = digit n p
noncomputable def digit_abs_check (n : Int) (p : Int) : Unit :=
  ()

theorem digit_abs (n : Int) (p : Int) :
    ⦃⌜True⌝⦄
    (pure (digit_abs_check n p) : Id Unit)
    ⦃⇓_ => ⌜digit n (|p|) = digit n p⌝⦄ := by
  intro _
  simp only [wp, PostCond.noThrow, pure, digit_abs_check, Id.run,
    ULift.up_down]
  show digit n (|p|) = digit n p
  exact digit_abs_eq n p

-- Coq: `digit_anti_monotone_lt` — if 1 < n and digit n p < digit n q, then |p| < |q|
noncomputable def digit_anti_monotone_lt_check (n : Int) (p q : Int) : Unit :=
  ()

theorem digit_anti_monotone_lt (n : Int) (p q : Int) :
    ⦃⌜1 < n ∧ digit n p < digit n q⌝⦄
    (pure (digit_anti_monotone_lt_check n p q) : Id Unit)
    ⦃⇓_ => ⌜|p| < |q|⌝⦄ := by
  intro ⟨hn, hlt_digits⟩
  simp only [wp, PostCond.noThrow, pure, digit_anti_monotone_lt_check, Id.run, ULift.up_down]
  by_contra hnot
  have hle_abs : |q| ≤ |p| := le_of_not_gt hnot
  have hmon := (digit_monotone n q p) ⟨hn, hle_abs⟩
  simp only [wp, PostCond.noThrow, pure, digit_monotone_check,
    Id.run, ULift.up_down] at hmon
  exact (not_lt_of_ge hmon) hlt_digits

-- ---------------------------------------------------------------------------
-- Source-facing rounded-mode contracts.  The longer proofs above retain their
-- explicitly split payloads under descriptive `_from_*_payload` names; these
-- public names expose only the hypotheses present in Pff.v.

/-- Coq `RoundAbsMonotoner`. -/
theorem RoundAbsMonotoner {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p : ℝ) (q r : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ RoundedModeP b P ∧
        Fbounded b r ∧ P p q ∧ |p| ≤ _root_.F2R r⌝⦄
    (pure (RoundAbsMonotoner_check (beta:=beta) b radix P p q r) : Id Unit)
    ⦃⇓_ => ⌜|_root_.F2R q| ≤ _root_.F2R r⌝⦄ := by
  intro ⟨hbeta, _hradix, hRounded, hr, hP, hp⟩
  subst beta
  exact RoundAbsMonotoner_from_rounded_payload (beta:=radix) b radix P p q r
    ⟨hRounded.toFloatPayload, hr, hP, hp⟩

/-- Coq `RoundAbsMonotonel`. -/
theorem RoundAbsMonotonel {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p : ℝ) (q r : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ RoundedModeP b P ∧
        Fbounded b r ∧ P p q ∧ _root_.F2R r ≤ |p|⌝⦄
    (pure (RoundAbsMonotonel_check (beta:=beta) b radix P p q r) : Id Unit)
    ⦃⇓_ => ⌜_root_.F2R r ≤ |_root_.F2R q|⌝⦄ := by
  intro ⟨hbeta, _hradix, hRounded, hr, hP, hp⟩
  subst beta
  exact RoundAbsMonotonel_from_rounded_payload (beta:=radix) b radix P p q r
    ⟨hRounded.toFloatPayload, hr, hP, hp⟩

/-- Coq `plusExpMin`. -/
theorem plusExpMin {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p q pq : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < precision ∧
        b.vNum = Zpower_nat radix precision ∧ RoundedModeP b P ∧
        P (_root_.F2R p + _root_.F2R q) pq⌝⦄
    (pure (plusExpMin_check (beta:=beta) b radix P p q pq) : Id Unit)
    ⦃⇓_ => ⌜∃ s : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded b s ∧ _root_.F2R s = _root_.F2R pq ∧
        min p.Fexp q.Fexp ≤ s.Fexp⌝⦄ := by
  intro ⟨hbeta, hradix, hprecision, hvNum, hRounded, hP⟩
  subst beta
  have hRep := RoundedModeRep b precision P hprecision hvNum hRounded
  exact plusExpMin_from_rep_payload (beta:=radix) b radix P p q pq
    ⟨rfl, hradix, hRounded.2.2.1, hRep, hP⟩

/-- Coq `plusExpUpperBound`. -/
theorem plusExpUpperBound {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p q pq : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < precision ∧
        b.vNum = Zpower_nat radix precision ∧ RoundedModeP b P ∧
        P (_root_.F2R p + _root_.F2R q) pq ∧
        Fbounded b p ∧ Fbounded b q⌝⦄
    (pure (plusExpUpperBound_check (beta:=beta) b radix P p q pq) : Id Unit)
    ⦃⇓_ => ⌜∃ r : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded b r ∧ _root_.F2R r = _root_.F2R pq ∧
        r.Fexp ≤ Int.succ (max p.Fexp q.Fexp)⌝⦄ := by
  intro ⟨hbeta, hradix, hprecision, hvNum, hRounded, hP, hp, hq⟩
  subst beta
  have hvNum_gt : 1 < b.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ hradix (by omega)
  exact plusExpUpperBound_from_split_payload (beta:=radix) b radix P p q pq
    ⟨rfl, hradix, hvNum_gt, hRounded.toFloatPayload, hP, hp, hq⟩

/-- Coq `plusExpBound`. -/
theorem plusExpBound {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p q pq : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < precision ∧
        b.vNum = Zpower_nat radix precision ∧ RoundedModeP b P ∧
        P (_root_.F2R p + _root_.F2R q) pq ∧
        Fbounded b p ∧ Fbounded b q⌝⦄
    (pure (plusExpBound_check (beta:=beta) b radix P p q pq) : Id Unit)
    ⦃⇓_ => ⌜∃ r : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded b r ∧ _root_.F2R r = _root_.F2R pq ∧
        min p.Fexp q.Fexp ≤ r.Fexp ∧
        r.Fexp ≤ Int.succ (max p.Fexp q.Fexp)⌝⦄ := by
  intro ⟨hbeta, hradix, hprecision, hvNum, hRounded, hP, hp, hq⟩
  subst beta
  have hvNum_gt : 1 < b.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ hradix (by omega)
  have hRep := RoundedModeRep b precision P hprecision hvNum hRounded
  exact plusExpBound_from_rep_payload (beta:=radix) b radix P p q pq
    ⟨rfl, hradix, hvNum_gt, hRounded.toFloatPayload, hRep, hP, hp, hq⟩

/-- Coq `minusRoundRep`. -/
theorem minusRoundRep {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p q qmp qmmp : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < precision ∧
        b.vNum = Zpower_nat radix precision ∧ RoundedModeP b P ∧
        0 ≤ _root_.F2R p ∧ _root_.F2R p ≤ _root_.F2R q ∧
        P (_root_.F2R q - _root_.F2R p) qmp ∧
        Fbounded b p ∧ Fbounded b q⌝⦄
    (pure (minusRoundRep_check (beta:=beta) b radix precision P p q qmp qmmp) :
      Id Unit)
    ⦃⇓_ => ⌜∃ r : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded b r ∧ _root_.F2R r = _root_.F2R q - _root_.F2R qmp⌝⦄ := by
  intro ⟨hbeta, hradix, hprecision, hvNum, hRounded, hp0, hpq, hP, hp, hq⟩
  subst beta
  have hprecision0 : precision ≠ 0 := by omega
  have hvNum_gt : 1 < b.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecision0
  have hBoundExp : -b.dExp ≤
      (boundR (beta:=radix) radix ((1 / 2 : ℝ) * _root_.F2R q)).Fexp := by
    have hnonneg : 0 ≤
        (boundR (beta:=radix) radix ((1 / 2 : ℝ) * _root_.F2R q)).Fexp := by
      simp [boundR, boundNat]
    exact le_trans (neg_nonpos.mpr b.dExp_nonneg) hnonneg
  exact minusRoundRep_from_rounded_payload (beta:=radix) b radix precision
    P p q qmp qmmp ⟨rfl, hradix, hprecision0, hvNum, hvNum_gt,
      hBoundExp, hRounded.toFullPayload, hRounded.toFloatPayload,
      hp0, hpq, hP, hp, hq⟩

/-- Coq `multExpUpperBound`. -/
theorem multExpUpperBound {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p q pq : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < precision ∧
        b.vNum = Zpower_nat radix precision ∧ RoundedModeP b P ∧
        P (_root_.F2R p * _root_.F2R q) pq ∧
        Fbounded b p ∧ Fbounded b q ∧
        -b.dExp ≤ p.Fexp + q.Fexp⌝⦄
    (pure (multExpUpperBound_check (beta:=beta) b radix precision P p q pq) :
      Id Unit)
    ⦃⇓_ => ⌜∃ r : FloatSpec.Core.Defs.FlocqFloat beta,
        Fbounded b r ∧ _root_.F2R r = _root_.F2R pq ∧
        r.Fexp ≤ (precision : Int) + (p.Fexp + q.Fexp)⌝⦄ := by
  intro ⟨hbeta, hradix, hprecision, hvNum, hRounded, hP, hp, hq, hexp⟩
  subst beta
  have hprecision0 : precision ≠ 0 := by omega
  have hvNum_gt : 1 < b.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecision0
  exact multExpUpperBound_from_split_payload (beta:=radix) b radix precision
    P p q pq ⟨rfl, hradix, hprecision0, hvNum_gt, hvNum,
      hRounded.toFloatPayload, hp, hq, hexp, hP⟩

/-- Coq `errorBoundedMultPos`. -/
theorem errorBoundedMultPos {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p q f : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < precision ∧
        b.vNum = Zpower_nat radix precision ∧ RoundedModeP b P ∧
        Fbounded b p ∧ Fbounded b q ∧
        0 ≤ _root_.F2R p ∧ 0 ≤ _root_.F2R q ∧
        -b.dExp ≤ p.Fexp + q.Fexp ∧
        P (_root_.F2R p * _root_.F2R q) f⌝⦄
    (pure (errorBoundedMultPos_check (beta:=beta) b radix precision P p q f) :
      Id Unit)
    ⦃⇓_ => ⌜∃ r : FloatSpec.Core.Defs.FlocqFloat beta,
        _root_.F2R r = _root_.F2R p * _root_.F2R q - _root_.F2R f ∧
        Fbounded b r ∧ r.Fexp = p.Fexp + q.Fexp⌝⦄ := by
  intro ⟨hbeta, hradix, hprecision, hvNum, hRounded, hp, hq,
    hp0, hq0, hexp, hP⟩
  subst beta
  exact errorBoundedMultPos_from_rounded_payload (beta:=radix) b radix precision
    P p q f ⟨rfl, hradix, by omega, hvNum, hRounded.toFloatPayload,
      hp, hq, hp0, hq0, hexp, hP⟩

/-- Coq `errorBoundedMultNeg`. -/
theorem errorBoundedMultNeg {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p q f : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < precision ∧
        b.vNum = Zpower_nat radix precision ∧ RoundedModeP b P ∧
        Fbounded b p ∧ Fbounded b q ∧
        _root_.F2R p ≤ 0 ∧ 0 ≤ _root_.F2R q ∧
        -b.dExp ≤ p.Fexp + q.Fexp ∧
        P (_root_.F2R p * _root_.F2R q) f⌝⦄
    (pure (errorBoundedMultNeg_check (beta:=beta) b radix precision P p q f) :
      Id Unit)
    ⦃⇓_ => ⌜∃ r : FloatSpec.Core.Defs.FlocqFloat beta,
        _root_.F2R r = _root_.F2R p * _root_.F2R q - _root_.F2R f ∧
        Fbounded b r ∧ r.Fexp = p.Fexp + q.Fexp⌝⦄ := by
  intro ⟨hbeta, hradix, hprecision, hvNum, hRounded, hp, hq,
    hp0, hq0, hexp, hP⟩
  subst beta
  exact errorBoundedMultNeg_from_rounded_payload (beta:=radix) b radix precision
    P p q f ⟨rfl, hradix, by omega, hvNum, hRounded.toFloatPayload,
      hp, hq, hp0, hq0, hexp, hP⟩

/-- Coq `RoundedModeUlp`. -/
theorem RoundedModeUlp {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p : ℝ) (q : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < precision ∧
        b.vNum = Zpower_nat radix precision ∧
        RoundedModeP b P ∧ P p q⌝⦄
    (pure (RoundedModeUlp_check (beta:=beta) b radix precision P p q) : Id Unit)
    ⦃⇓_ => ⌜|p - _root_.F2R q| < Fulp b radix precision q⌝⦄ := by
  intro ⟨hbeta, hradix, hprecision, hvNum, hRounded, hP⟩
  subst beta
  exact RoundedModeUlp_from_rounded_payload (beta:=radix) b radix precision P p q
    ⟨hRounded.toFloatPayload, hP, rfl, hradix, by omega, hvNum⟩

/-- Coq `RoundedModeErrorExpStrict`. -/
theorem RoundedModeErrorExpStrict {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p q : FloatSpec.Core.Defs.FlocqFloat beta) (x : ℝ) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < precision ∧
        b.vNum = Zpower_nat radix precision ∧ RoundedModeP b P ∧
        Fbounded b p ∧ Fbounded b q ∧ P x p ∧
        _root_.F2R q = x - _root_.F2R p ∧ _root_.F2R q ≠ 0⌝⦄
    (pure (RoundedModeErrorExpStrict_check
      (beta:=beta) b radix precision P p q x) : Id Unit)
    ⦃⇓_ => ⌜q.Fexp < p.Fexp⌝⦄ := by
  intro ⟨hbeta, hradix, hprecision, hvNum, hRounded, hp, hq, hP, hqv, hn⟩
  subst beta
  exact RoundedModeErrorExpStrict_from_rounded_payload (beta:=radix)
    b radix precision P p q x ⟨hRounded.toFloatPayload, hp, hq, hP,
      hqv, hn, rfl, hradix, by omega, hvNum⟩

/-- Coq `errorBoundedMultExpPos`. -/
theorem errorBoundedMultExpPos {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p q pq : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix) (hprecision : 1 < precision)
    (hvNum : b.vNum = Zpower_nat radix precision)
    (hRounded : RoundedModeP b P)
    (hp : Fbounded b p) (hq : Fbounded b q)
    (hp0 : 0 ≤ _root_.F2R p) (hq0 : 0 ≤ _root_.F2R q)
    (hP : P (_root_.F2R p * _root_.F2R q) pq)
    (hexp : -b.dExp ≤ p.Fexp + q.Fexp) :
    ∃ r s : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded b r ∧ Fbounded b s ∧
      _root_.F2R r = _root_.F2R pq ∧
      _root_.F2R s = _root_.F2R p * _root_.F2R q - _root_.F2R r ∧
      s.Fexp = p.Fexp + q.Fexp ∧ s.Fexp ≤ r.Fexp ∧
      r.Fexp ≤ (precision : Int) + (p.Fexp + q.Fexp) := by
  subst beta
  have hprecision0 : precision ≠ 0 := by omega
  have hvNum_gt : 1 < b.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecision0
  exact errorBoundedMultExpPos_from_split_payload (beta:=radix)
    b radix precision P rfl hradix hprecision0 hvNum_gt hvNum
    hRounded.toFloatPayload p q pq hp hq hp0 hq0 hP hexp

/-- Coq `errorBoundedMultExp`. -/
theorem errorBoundedMultExp {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (p q pq : FloatSpec.Core.Defs.FlocqFloat beta)
    (hbeta : beta = radix) (hradix : 1 < radix) (hprecision : 1 < precision)
    (hvNum : b.vNum = Zpower_nat radix precision)
    (hRounded : RoundedModeP b P)
    (hp : Fbounded b p) (hq : Fbounded b q)
    (hP : P (_root_.F2R p * _root_.F2R q) pq)
    (hexp : -b.dExp ≤ p.Fexp + q.Fexp) :
    ∃ r s : FloatSpec.Core.Defs.FlocqFloat beta,
      Fbounded b r ∧ Fbounded b s ∧
      _root_.F2R r = _root_.F2R pq ∧
      _root_.F2R s = _root_.F2R p * _root_.F2R q - _root_.F2R r ∧
      s.Fexp = p.Fexp + q.Fexp ∧ s.Fexp ≤ r.Fexp ∧
      r.Fexp ≤ (precision : Int) + (p.Fexp + q.Fexp) := by
  subst beta
  have hprecision0 : precision ≠ 0 := by omega
  have hvNum_gt : 1 < b.vNum := by
    rw [hvNum, Zpower_nat]
    exact one_lt_pow₀ hradix hprecision0
  exact errorBoundedMultExp_from_split_payload (beta:=radix)
    b radix precision P rfl hradix hprecision0 hvNum_gt hvNum
    hRounded.toFloatPayload p q pq hp hq hP hexp

/-- Coq `MSBroundLSB`. -/
theorem MSBroundLSB {beta : Int} [ValidRadix beta]
    (b : Fbound_skel) (radix : Int) (precision : Nat)
    (P : ℝ → FloatSpec.Core.Defs.FlocqFloat beta → Prop)
    (f1 f2 : FloatSpec.Core.Defs.FlocqFloat beta) :
    ⦃⌜beta = radix ∧ 1 < radix ∧ 1 < precision ∧
        b.vNum = Zpower_nat radix precision ∧ RoundedModeP b P ∧
        P (_root_.F2R f1) f2 ∧ ¬ is_Fzero (Fminus f1 f2)⌝⦄
    (pure (MSBroundLSB_check (beta:=beta) b radix precision P f1 f2) : Id Unit)
    ⦃⇓_ => ⌜MSB radix (Fminus f1 f2) < LSB radix f2⌝⦄ := by
  intro ⟨hbeta, hradix, hprecision, hvNum, hRounded, hP, hn⟩
  subst beta
  exact MSBroundLSB_from_rounded_payload (beta:=radix) b radix precision P f1 f2
    ⟨rfl, hradix, by omega, hvNum, hRounded.toFloatPayload, hP, hn⟩
